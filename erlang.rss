<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/erlang.rss</id>
    <title>Security Advisory for Erlang/Elixir packages hosted at hex.pm</title>
    <updated>2026-08-16T07:21:41.677Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Aerlang"/>
    <subtitle>Security Advisory for Erlang/Elixir packages hosted at hex.pm on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[gun] cowboy and gun affected by an HTTP Request/Response Splitting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</id>
        <link href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c"/>
        <updated>2026-07-29T15:48:41.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of CRLF Sequences in HTTP Headers ('HTTP Request/Response Splitting') vulnerability in ninenines cowlib allows HTTP response splitting via non-VCHAR bytes in structured-fields string values.</p>
<p>cow_http_struct_hd:escape_string/2 in cowlib only escapes \ and ", passing all other bytes through verbatim. This creates an encoder/decoder asymmetry: the matching parser accepts only printable ASCII (0x20–0x7E, excluding " and ), but the encoder emits any byte including CR and LF. An application that builds a structured HTTP header via cow_http_struct_hd:item/1 (or a higher-level wrapper such as cow_http_hd:wt_protocol/1) from attacker-controlled input can have \r\n injected into the serialized header value. Once on the wire, the injected CRLF terminates the current header and any following bytes are interpreted as a new header, enabling HTTP response splitting.</p>
<p>This issue affects cowlib from 2.9.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43966">https://nvd.nist.gov/vuln/detail/CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef">https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef</a></li>
<li><a href="https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42">https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43966.html">https://cna.erlef.org/cves/CVE-2026-43966.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43966">https://osv.dev/vulnerability/EEF-CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232">https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701">https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701</a></li>
<li><a href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c">https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T18:31:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cowboy] cowboy and gun affected by an HTTP Request/Response Splitting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</id>
        <link href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c"/>
        <updated>2026-07-29T15:48:41.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of CRLF Sequences in HTTP Headers ('HTTP Request/Response Splitting') vulnerability in ninenines cowlib allows HTTP response splitting via non-VCHAR bytes in structured-fields string values.</p>
<p>cow_http_struct_hd:escape_string/2 in cowlib only escapes \ and ", passing all other bytes through verbatim. This creates an encoder/decoder asymmetry: the matching parser accepts only printable ASCII (0x20–0x7E, excluding " and ), but the encoder emits any byte including CR and LF. An application that builds a structured HTTP header via cow_http_struct_hd:item/1 (or a higher-level wrapper such as cow_http_hd:wt_protocol/1) from attacker-controlled input can have \r\n injected into the serialized header value. Once on the wire, the injected CRLF terminates the current header and any following bytes are interpreted as a new header, enabling HTTP response splitting.</p>
<p>This issue affects cowlib from 2.9.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43966">https://nvd.nist.gov/vuln/detail/CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef">https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef</a></li>
<li><a href="https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42">https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43966.html">https://cna.erlef.org/cves/CVE-2026-43966.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43966">https://osv.dev/vulnerability/EEF-CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232">https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701">https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701</a></li>
<li><a href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c">https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T18:31:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] gun has an Unexpected Status Code or Return Value vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-2j82-37xg-f9wp</id>
        <link href="https://github.com/advisories/GHSA-2j82-37xg-f9wp"/>
        <updated>2026-07-29T15:46:08.000Z</updated>
        <content type="html"><![CDATA[<p>Unexpected Status Code or Return Value vulnerability in ninenines gun (gun_http module) allows a malicious HTTP server to force the client into raw protocol mode via an unsolicited 101 Switching Protocols response.</p>
<p>In gun_http:handle_inform/8, when a 101 Switching Protocols response is received over HTTP/1.1, the function verifies only that the Upgrade header is syntactically valid and that the stream reference is a plain reference(). It does not check whether the client ever sent an Upgrade or Connection: upgrade header on the corresponding request. Because this check is absent, any 101 response (solicited or not) causes gun to dispatch a gun_upgrade message to the caller and transition the entire connection to raw protocol mode.</p>
<p>A malicious or compromised HTTP server can send an unsolicited 101 response to any HTTP/1.1 request, causing the gun client to abandon HTTP framing for that connection. Once in raw mode, gun_raw applies no flow control (flow=infinity) and re-arms socket active mode after every received packet, so the server can flood the client with arbitrary bytes. These are forwarded as unbounded gun_data messages to the owner process, exhausting its mailbox and BEAM memory, ultimately crashing the VM.</p>
<p>This issue affects gun: from 2.0.0 before 2.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43974">https://nvd.nist.gov/vuln/detail/CVE-2026-43974</a></li>
<li><a href="https://github.com/ninenines/gun/commit/5b48068c29ce5e112cb149b5857c7d4dc319a81b">https://github.com/ninenines/gun/commit/5b48068c29ce5e112cb149b5857c7d4dc319a81b</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43974.html">https://cna.erlef.org/cves/CVE-2026-43974.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43974">https://osv.dev/vulnerability/EEF-CVE-2026-43974</a></li>
<li><a href="https://github.com/advisories/GHSA-2j82-37xg-f9wp">https://github.com/advisories/GHSA-2j82-37xg-f9wp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2j82-37xg-f9wp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-08T15:33:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] gun_http2 has an Origin Validation Error vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-36w4-95hv-5vwg</id>
        <link href="https://github.com/advisories/GHSA-36w4-95hv-5vwg"/>
        <updated>2026-07-29T15:45:19.000Z</updated>
        <content type="html"><![CDATA[<p>Origin Validation Error vulnerability in ninenines gun (gun_http2 module) allows cross-origin cookie injection via unvalidated HTTP/2 PUSH_PROMISE authority.</p>
<p>In gun_http2:push_promise_frame/7, the :authority pseudo-header from an incoming PUSH_PROMISE frame is stored verbatim into the promised stream record without checking that it matches the connection's origin. When gun_http2:headers_frame/9 later processes the response headers for the promised stream, it calls gun_cookies:set_cookie_header/7 with the unvalidated server-supplied authority before any status branching and before user code can act. This violates RFC 7540 §10.6 / RFC 9113 §8.4, which require receivers to treat as a protocol error any push for a resource the server is not authoritative for.</p>
<p>A malicious or compromised HTTP/2 server can plant cookies scoped to arbitrary third-party domains into the client's shared cookie store. This enables session fixation attacks against those domains and, if the planted cookie overrides a legitimate session token, may result in account takeover. No user interaction beyond making a normal HTTP/2 request to the attacker-controlled server is required.</p>
<p>This issue affects gun: from 2.0.0 before 2.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43972">https://nvd.nist.gov/vuln/detail/CVE-2026-43972</a></li>
<li><a href="https://github.com/ninenines/gun/commit/567863ff53802fed21c3b3f25812db7f7ae29676">https://github.com/ninenines/gun/commit/567863ff53802fed21c3b3f25812db7f7ae29676</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43972.html">https://cna.erlef.org/cves/CVE-2026-43972.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43972">https://osv.dev/vulnerability/EEF-CVE-2026-43972</a></li>
<li><a href="https://github.com/advisories/GHSA-36w4-95hv-5vwg">https://github.com/advisories/GHSA-36w4-95hv-5vwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-36w4-95hv-5vwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T15:32:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] gun has an Uncontrolled Resource Consumption vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-r53j-fjj5-mv77</id>
        <link href="https://github.com/advisories/GHSA-r53j-fjj5-mv77"/>
        <updated>2026-07-29T15:45:18.000Z</updated>
        <content type="html"><![CDATA[<p>Uncontrolled Resource Consumption vulnerability in ninenines gun (gun_http module) allows a malicious server to exhaust client memory via unbounded HTTP/1.1 response buffering.</p>
<p>In gun_http:handle/5, three clauses accumulate incoming TCP data into the connection's buffer field using binary concatenation with no upper-bound check: the head clause appends data until the \r\n\r\n header terminator is found; the body_chunked clause appends data whenever cow_http_te:stream_chunked/2 returns a more result indicating an incomplete chunk boundary; and the body_trailer clause appends data until the trailing \r\n\r\n is found. In each case, when the expected terminator never arrives, the enlarged binary is stored back into state and the process waits for more data, with no configurable or hard-coded ceiling on buffer size.</p>
<p>A malicious or compromised server can exploit this by sending a partial response that never completes. For example, a response may begin with HTTP/1.1 200 OK\r\nX-Pad:  followed by an unbounded stream of arbitrary bytes, never sending the header terminator. The gun connection process will continuously append the incoming data to its buffer, causing unbounded heap growth. Because BEAM imposes no per-process heap limit by default, a single malicious connection can exhaust all available memory on the node, causing a node-wide out-of-memory crash.</p>
<p>This issue affects gun: from 1.0.0 before 2.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43973">https://nvd.nist.gov/vuln/detail/CVE-2026-43973</a></li>
<li><a href="https://github.com/ninenines/gun/commit/f3e7e0568b3c4cf9fa4bea79d5116e67ce76ad25">https://github.com/ninenines/gun/commit/f3e7e0568b3c4cf9fa4bea79d5116e67ce76ad25</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43973.html">https://cna.erlef.org/cves/CVE-2026-43973.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43973">https://osv.dev/vulnerability/EEF-CVE-2026-43973</a></li>
<li><a href="https://github.com/advisories/GHSA-r53j-fjj5-mv77">https://github.com/advisories/GHSA-r53j-fjj5-mv77</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r53j-fjj5-mv77</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-08T15:33:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[req] Req vulnerable to multipart form-data header injection via unescaped name/filename/content_type]]></title>
        <id>https://github.com/advisories/GHSA-px9f-whj3-246m</id>
        <link href="https://github.com/advisories/GHSA-px9f-whj3-246m"/>
        <updated>2026-07-29T15:29:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Req's multipart form encoder interpolates the per-part <code>name</code>, <code>filename</code>, and <code>content_type</code> directly into the part headers without escaping. An attacker who can influence any of those values can inject CRLF-separated header lines, smuggle additional form fields, or prepend a whole extra part into the request the victim service sends downstream.</p>
<h3 id="details">Details</h3>
<p><code>Req.Utils.encode_form_part/2</code> in <code>lib/req/utils.ex</code> builds the per-part header iodata by concatenating the three caller-supplied strings verbatim into <code>content-disposition: form-data; name="&lt;name&gt;"; filename="&lt;filename&gt;"</code> and <code>content-type: &lt;content_type&gt;</code>. There is no CRLF stripping, no quote escaping, and no validation. A value containing <code>"\r\n</code> closes the surrounding quoted value and starts a new header line; an additional <code>\r\n--&lt;boundary&gt;</code> terminates the current part and lets the attacker prepend a smuggled part.</p>
<p>The flaw is reachable through every supported way of supplying a part. It is especially easy to hit when <code>value</code> is a <code>%File.Stream{}</code>, because <code>filename</code> then defaults to <code>Path.basename(stream.path)</code> and POSIX filenames may legitimately contain <code>\r</code> and <code>\n</code>. RFC 7578 / WHATWG form-data requires percent-encoding <code>"</code>, CR, and LF in these fields; the fix adopts that behavior.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Construct a malicious <code>filename</code> such as <code>harmless.txt"\r\nX-Smuggled: marker\r\nContent-Disposition: form-data; name="pwned</code>.</li>
<li>Call <code>Req.post!(url, form_multipart: [upload: {"benign body", filename: &lt;malicious&gt;, content_type: "text/plain"}])</code>.</li>
<li>The emitted multipart body contains a real <code>X-Smuggled:</code> header line and an extra <code>Content-Disposition</code> for <code>name="pwned"</code>, alongside Req's legitimate headers.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>HTTP request smuggling / multipart parameter smuggling in the HTTP client. Any application using Req to send <code>form_multipart</code> requests where any of <code>name</code>, <code>filename</code>, or <code>content_type</code> can be influenced by an untrusted source is affected, most commonly upload proxies and re-uploaders that derive <code>filename</code> from <code>Path.basename/1</code> on a user-controlled path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/wojtekmach/req/security/advisories/GHSA-px9f-whj3-246m">https://github.com/wojtekmach/req/security/advisories/GHSA-px9f-whj3-246m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49756">https://nvd.nist.gov/vuln/detail/CVE-2026-49756</a></li>
<li><a href="https://github.com/wojtekmach/req/commit/74506ff2c5addf74df85d79dc726e9b2e264a8ba">https://github.com/wojtekmach/req/commit/74506ff2c5addf74df85d79dc726e9b2e264a8ba</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-49756.html">https://cna.erlef.org/cves/CVE-2026-49756.html</a></li>
<li><a href="https://github.com/wojtekmach/req/releases/tag/v0.6.0">https://github.com/wojtekmach/req/releases/tag/v0.6.0</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-49756">https://osv.dev/vulnerability/EEF-CVE-2026-49756</a></li>
<li><a href="https://github.com/advisories/GHSA-px9f-whj3-246m">https://github.com/advisories/GHSA-px9f-whj3-246m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-px9f-whj3-246m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-29T15:29:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[req] Req vulnerable to unbounded archive/compression extraction triggered by response content-type]]></title>
        <id>https://github.com/advisories/GHSA-655f-mp8p-96gv</id>
        <link href="https://github.com/advisories/GHSA-655f-mp8p-96gv"/>
        <updated>2026-07-29T15:23:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Req's default response pipeline auto-decodes archive and compressed bodies based on the server-supplied <code>content-type</code> (or URL extension) and materialises the full decompressed contents in memory with no size cap. An attacker who controls (or can redirect a victim into) an HTTP endpoint reached by <code>Req.get!/1</code> can return a tiny "decompression bomb" that expands to many gigabytes on the client and exhausts the BEAM's memory.</p>
<h3 id="details">Details</h3>
<p><strong>1. Archive auto-decoding.</strong> <code>Req.Steps.decode_body/1</code> in <code>lib/req/steps.ex</code> dispatches on the response <code>content-type</code> (or URL extension) and calls Erlang's archive libraries with <code>:memory</code>, returning a <code>[{name, bytes}]</code> list of every entry fully decompressed in RAM: <code>application/zip</code> → <code>:zip.extract(body, [:memory])</code>, <code>application/x-tar</code> → <code>:erl_tar.extract({:binary, body}, [:memory])</code>, <code>application/gzip</code> / <code>.tgz</code> → <code>:erl_tar.extract({:binary, body}, [:memory, :compressed])</code>. No byte cap is enforced before decoding and no per-entry size limit is passed to <code>:zip</code> / <code>:erl_tar</code>.</p>
<p><strong>2. content-encoding chaining.</strong> <code>Req.Steps.decompress_body/1</code> walks the <code>content-encoding</code> header and chains <code>:zlib</code> / <code>:brotli</code> / <code>:ezstd</code> decoders, so a response advertising <code>content-encoding: gzip, gzip, gzip, …</code> inflates through multiple layers without bound.</p>
<p><strong>3. Default-on, attacker-chosen decoder.</strong> Both steps are part of Req's default pipeline. The caller does not need to opt in, and the attacker chooses which decoder fires by setting <code>content-type</code> and <code>content-encoding</code> on their own server (or on any host reached via Req's automatic redirect following).</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Run an HTTP server that responds 200 with <code>content-type: application/zip</code> and a body that is a zip archive whose single entry is ~400 MB of zero bytes (compressed wire payload: a few hundred KB).</li>
<li>From the victim process, call <code>Req.get!(url)</code> against that server (no special options, no opt-in to archive decoding).</li>
<li><code>decode_body/1</code> dispatches on <code>content-type</code>, invokes <code>:zip.extract(body, [:memory])</code>, and the response body becomes <code>[{~c"bomb.bin", &lt;&lt;400 MB of zero bytes&gt;&gt;}]</code>. A sub-MB request produces hundreds of MB resident memory; layering gzip on the <code>content-encoding</code> path or increasing entry size scales arbitrarily.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Memory-exhaustion denial of service against any Elixir application that uses Req with its default step pipeline to fetch URLs influenced by an untrusted party, including webhook senders, link previews, OAuth/OIDC discovery clients, package mirrors, image proxies, and any <code>Req.get!/1</code> call that may follow redirects to attacker-controlled hosts. No authentication is required; a single response can crash the BEAM and take down unrelated workloads on the same VM.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/wojtekmach/req/security/advisories/GHSA-655f-mp8p-96gv">https://github.com/wojtekmach/req/security/advisories/GHSA-655f-mp8p-96gv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49755">https://nvd.nist.gov/vuln/detail/CVE-2026-49755</a></li>
<li><a href="https://github.com/wojtekmach/req/commit/84977e5b1a83f26e749d55ad06e3625464af4e8d">https://github.com/wojtekmach/req/commit/84977e5b1a83f26e749d55ad06e3625464af4e8d</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-49755.html">https://cna.erlef.org/cves/CVE-2026-49755.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-49755">https://osv.dev/vulnerability/EEF-CVE-2026-49755</a></li>
<li><a href="https://github.com/advisories/GHSA-655f-mp8p-96gv">https://github.com/advisories/GHSA-655f-mp8p-96gv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-655f-mp8p-96gv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-29T15:23:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[protobuf] Protobuf: Unbounded recursion depth in embedded-message decoding]]></title>
        <id>https://github.com/advisories/GHSA-rv48-qqj5-crxg</id>
        <link href="https://github.com/advisories/GHSA-rv48-qqj5-crxg"/>
        <updated>2026-07-15T17:44:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Unbounded recursion depth in <code>Protobuf.Decoder</code> (Hex package <code>protobuf</code>, versions <code>&gt;= 0.8.0, &lt; 0.16.1</code>) lets an unauthenticated attacker crash any service that decodes untrusted protobuf messages whose schema contains a self-referential or cyclic message type. A small request body (a few KB to a few MB) that nests an embedded field hundreds of thousands to millions of levels deep forces the BEAM to recurse once per level, exhausting memory and pinning a scheduler. A handful of such requests can take the node offline (a request-amplification denial of service).</p>
<h3 id="details">Details</h3>
<p><code>Protobuf.Decoder.value_for_field/3</code> handles embedded message fields in its <code>embedded?: true</code> branch at <code>lib/protobuf/decoder.ex:218-243</code>. For an embedded field it calls <code>decode(bin, type)</code> recursively, which re-enters <code>build_message → handle_value → value_for_field</code>. The recursive call is not in tail position (its result is consumed by the surrounding decode after it returns), so every nesting level retains a live frame on the process stack and heap.</p>
<p>There is no recursion-depth counter anywhere in the decoder. For any schema with a self-referential message type (e.g. <code>message Tree { Tree child = 1; }</code>, a common shape for comment threads, org charts, file trees, and ASTs) or any cycle of message types, the attacker controls the nesting depth entirely through the input bytes. Each additional level costs only a 1-byte field tag plus a varint length prefix, so depth grows roughly inversely with payload size: a tiny body buys an enormous recursion depth.</p>
<p>Reference protobuf implementations (Google's C++, Java, etc.) cap recursion at 100 specifically to prevent this. The Elixir decoder enforces no comparable bound, so the recursion continues until the process exhausts memory, blows the stack, or starves the scheduler doing GC over the deep structure.</p>
<p>The fix threads a depth counter through <code>decode / build_message / handle_value / value_for_field</code> (or holds it in the process dictionary for the duration of the top-level <code>decode</code>) and raises <code>Protobuf.DecodeError</code> once it exceeds a configurable limit, defaulting to 100 to match the reference implementations.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Define a self-referential schema: <code>defmodule Tree do use Protobuf, syntax: :proto3; field(:child, 1, type: Tree) end</code>.</li>
<li>Build a wire-format body inner-to-outer: at each of <code>depth</code> levels prepend <code>&lt;&lt;0x0A, length_varint(inner_size), inner&gt;&gt;</code> (tag <code>0x0A</code> = field 1, wire type 2). Use an iolist with a running byte-size to keep generation O(depth).</li>
<li>POST the body (a few MB at <code>depth = 1_000_000</code>) as <code>application/x-protobuf</code> to any endpoint that calls <code>Tree.decode/1</code>.</li>
<li>The non-tail <code>decode(bin, type)</code> in <code>value_for_field/3</code> re-enters once per nesting level, accumulating a frame per level. The decode burns seconds of CPU and hundreds of MB on the victim node; a few concurrent requests exhaust it.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Unauthenticated, network-reachable request-amplification denial of service against any service that decodes attacker-influenced protobuf bytes into a self-referential or cyclic message type. A single small request can consume seconds of CPU and hundreds of MB of memory on the victim; a few concurrent requests can take the node offline. </p>
<h2 id="resources">Resources</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-protobuf/protobuf/commit/21ec7c5bec4fec74e10c1de0d5d1a2d8152ac5d4">https://github.com/elixir-protobuf/protobuf/commit/21ec7c5bec4fec74e10c1de0d5d1a2d8152ac5d4</a></li>
<li>Patch commit: <a href="https://github.com/elixir-protobuf/protobuf/commit/b8efa97790eece3d2d0e8e7c31a45ed409fe5338">https://github.com/elixir-protobuf/protobuf/commit/b8efa97790eece3d2d0e8e7c31a45ed409fe5338</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-protobuf/protobuf/security/advisories/GHSA-rv48-qqj5-crxg">https://github.com/elixir-protobuf/protobuf/security/advisories/GHSA-rv48-qqj5-crxg</a></li>
<li><a href="https://github.com/elixir-protobuf/protobuf/commit/21ec7c5bec4fec74e10c1de0d5d1a2d8152ac5d4">https://github.com/elixir-protobuf/protobuf/commit/21ec7c5bec4fec74e10c1de0d5d1a2d8152ac5d4</a></li>
<li><a href="https://github.com/elixir-protobuf/protobuf/commit/b8efa97790eece3d2d0e8e7c31a45ed409fe5338">https://github.com/elixir-protobuf/protobuf/commit/b8efa97790eece3d2d0e8e7c31a45ed409fe5338</a></li>
<li><a href="https://github.com/advisories/GHSA-rv48-qqj5-crxg">https://github.com/advisories/GHSA-rv48-qqj5-crxg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rv48-qqj5-crxg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T17:44:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tesla] Tesla vulnerable to multipart part smuggling via unescaped `content-disposition` values]]></title>
        <id>https://github.com/advisories/GHSA-28jh-g32x-v9v4</id>
        <link href="https://github.com/advisories/GHSA-28jh-g32x-v9v4"/>
        <updated>2026-07-10T00:04:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>Tesla.Multipart.part_headers_for_disposition/1</code> interpolates <code>Content-Disposition</code> parameter values (field name, filename, and other opts) verbatim into the part header line without encoding or escaping any special characters. An attacker who controls a filename, field name, or other disposition parameter can use unescaped double-quotes to inject extra disposition key-value pairs, or CRLF sequences to inject additional part headers or prepend bytes to the part body.</p>
<h3 id="details">Details</h3>
<p><code>part_headers_for_disposition/1</code> in <code>lib/tesla/multipart.ex</code> formats each disposition parameter as <code>k="v"</code> with no sanitization. Values flow in from <code>add_field/4</code> (the <code>name</code> argument), <code>add_file/3</code> and <code>add_file_content/4</code> (the <code>filename</code> argument and any disposition opts). A <code>"</code> in the value closes the quoted parameter early, allowing extra <code>; key="value"</code> pairs to be appended. A <code>\r\n</code> ends the <code>Content-Disposition</code> header line entirely, with subsequent bytes interpreted as additional part headers (e.g. a forged <code>Content-Type</code>); a second <code>\r\n</code> ends the whole part header block and prepends attacker bytes to the part body.</p>
<p>The default-filename path in <code>add_file/3</code> derives the name via <code>Path.basename/1</code>, which does not strip CR or LF, so any code that forwards a partially attacker-controlled file path is equally vulnerable.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Call <code>Tesla.Multipart.add_file_content/4</code> with a filename containing <code>\r\nX-Injected: evil</code>.</li>
<li>POST the multipart body to any upstream via any Tesla adapter.</li>
<li>The upstream receives <code>X-Injected: evil</code> as a standalone header line on the affected part.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Low severity (CVSS v4.0: 2.1). Any application using <code>tesla</code> 0.8.0 through 1.18.2 that passes untrusted input into <code>add_field/4</code>, <code>add_file/3</code>, or <code>add_file_content/4</code> disposition parameters is affected. Consequences range from forging part-level headers to body prepending against lenient multipart parsers. Fixed in tesla 1.18.3.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate disposition parameter values before passing them to the multipart API, rejecting any value that contains <code>\r</code>, <code>\n</code>, or <code>"</code>.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-tesla/tesla/commit/6ebfdb9abe9c6f119408045b933d82462decd351">https://github.com/elixir-tesla/tesla/commit/6ebfdb9abe9c6f119408045b933d82462decd351</a></li>
<li>Patch commit: <a href="https://github.com/elixir-tesla/tesla/commit/bb1a2c3da2775924d96e3db8e315dcc4d5d2246e">https://github.com/elixir-tesla/tesla/commit/bb1a2c3da2775924d96e3db8e315dcc4d5d2246e</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-tesla/tesla/security/advisories/GHSA-28jh-g32x-v9v4">https://github.com/elixir-tesla/tesla/security/advisories/GHSA-28jh-g32x-v9v4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48598">https://nvd.nist.gov/vuln/detail/CVE-2026-48598</a></li>
<li><a href="https://github.com/elixir-tesla/tesla/commit/bb1a2c3da2775924d96e3db8e315dcc4d5d2246e">https://github.com/elixir-tesla/tesla/commit/bb1a2c3da2775924d96e3db8e315dcc4d5d2246e</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48598.html">https://cna.erlef.org/cves/CVE-2026-48598.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48598">https://osv.dev/vulnerability/EEF-CVE-2026-48598</a></li>
<li><a href="https://github.com/advisories/GHSA-28jh-g32x-v9v4">https://github.com/advisories/GHSA-28jh-g32x-v9v4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-28jh-g32x-v9v4</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-10T00:04:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tesla] Tesla vulnerable to atom exhaustion via untrusted URL scheme]]></title>
        <id>https://github.com/advisories/GHSA-h74c-q9j7-mpcm</id>
        <link href="https://github.com/advisories/GHSA-h74c-q9j7-mpcm"/>
        <updated>2026-07-10T00:03:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In the Mint adapter for the Tesla HTTP client library, <code>Tesla.Adapter.Mint.open_conn/2</code> passes the URL scheme of every outgoing request through <code>String.to_atom/1</code> with no allow-list validation. Because BEAM atoms are permanent (never garbage-collected) and the atom table is bounded at roughly 1,048,576 entries, an attacker who can vary the URL scheme across requests can mint one fresh atom per request and eventually exhaust the table, crashing the VM.</p>
<h3 id="details">Details</h3>
<p><strong>Vulnerable call</strong> (<code>lib/tesla/adapter/mint.ex</code>, <code>open_conn/2</code>): the scheme field parsed from the request URI is passed directly to <code>String.to_atom/1</code> before being forwarded to <code>Mint.HTTP.connect/4</code>. Even though <code>Mint</code> raises for unrecognised schemes, the atom is already interned by that point. The function's HTTPS-branch guard confirms that no scheme normalisation occurs beforehand.</p>
<p>The attack surface has two entry points. First, any application-level URL-forwarding feature (webhook relay, link preview, SSRF-style proxy) where untrusted input reaches <code>Tesla.get/2</code> or equivalent. Second, any pipeline that includes <code>Tesla.Middleware.FollowRedirects</code>: a server under the attacker's control can return a <code>Location</code> header with a novel scheme, triggering the atom creation on the redirect follow.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Stand up any application that accepts a user-supplied URL and forwards it through <code>Tesla.Adapter.Mint</code>.</li>
<li>Send requests to the application, each with a distinct, previously unseen URL scheme (e.g. <code>atk1://</code>, <code>atk2://</code>, ...).</li>
<li>Each request interns one new permanent atom; <code>Mint</code> rejects the connection but the atom persists.</li>
<li>After approximately 1,000,000 requests the BEAM atom table is exhausted and the VM crashes.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>High severity (CVSS v4.0: 8.2). Any application using <code>tesla</code> 1.3.0 through 1.18.2 with <code>Tesla.Adapter.Mint</code> that allows untrusted input to influence request URLs is vulnerable to remote denial of service. No authentication or special privileges are required beyond access to the application's HTTP endpoint. Fixed in tesla 1.18.3.</p>
<h3 id="configurations">Configurations</h3>
<p>The application must use <code>Tesla.Adapter.Mint</code> and either expose a feature that forwards attacker-controlled URLs to Tesla, or include <code>Tesla.Middleware.FollowRedirects</code> in the middleware pipeline.</p>
<h3 id="references">References</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-tesla/tesla/commit/ccd0823d4ba37581a37d8f6108f9a81b263237ef">https://github.com/elixir-tesla/tesla/commit/ccd0823d4ba37581a37d8f6108f9a81b263237ef</a></li>
<li>Patch commit: <a href="https://github.com/elixir-tesla/tesla/commit/4699c3cb3e2fd6078f99f45f11cf7466aeedbf0e">https://github.com/elixir-tesla/tesla/commit/4699c3cb3e2fd6078f99f45f11cf7466aeedbf0e</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/elixir-tesla/tesla/security/advisories/GHSA-h74c-q9j7-mpcm">https://github.com/elixir-tesla/tesla/security/advisories/GHSA-h74c-q9j7-mpcm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48597">https://nvd.nist.gov/vuln/detail/CVE-2026-48597</a></li>
<li><a href="https://github.com/elixir-tesla/tesla/commit/4699c3cb3e2fd6078f99f45f11cf7466aeedbf0e">https://github.com/elixir-tesla/tesla/commit/4699c3cb3e2fd6078f99f45f11cf7466aeedbf0e</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48597.html">https://cna.erlef.org/cves/CVE-2026-48597.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48597">https://osv.dev/vulnerability/EEF-CVE-2026-48597</a></li>
<li><a href="https://github.com/advisories/GHSA-h74c-q9j7-mpcm">https://github.com/advisories/GHSA-h74c-q9j7-mpcm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h74c-q9j7-mpcm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-10T00:03:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tesla] Tesla: Authorization header leaks on cross-origin redirect via case-sensitive filtering]]></title>
        <id>https://github.com/advisories/GHSA-9m9w-gxf7-rh8m</id>
        <link href="https://github.com/advisories/GHSA-9m9w-gxf7-rh8m"/>
        <updated>2026-07-10T00:03:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>Tesla.Middleware.FollowRedirects</code> is meant to strip the <code>Authorization</code> header when following a cross-origin redirect, but performs the check with a case-sensitive comparison against the lowercase string <code>"authorization"</code>. Because Tesla preserves header keys exactly as supplied by the caller, any application that sets the header with its RFC 7235 canonical casing (<code>"Authorization"</code>) bypasses the filter entirely, leaking bearer tokens or other credentials to whatever origin the redirect points at.</p>
<h3 id="details">Details</h3>
<p>The filter list in <code>lib/tesla/middleware/follow_redirects.ex</code> is defined as <code>@filter_headers ["authorization", "host"]</code> and the membership check <code>k not in @filter_headers</code> compares the raw key string without case normalization. HTTP header names are case-insensitive per RFC 7230, but Tesla stores them verbatim. A header tuple <code>{"Authorization", "Bearer …"}</code> does not match <code>"authorization"</code>, so it passes through the filter and is forwarded to the redirect destination unchecked. The same defect applies to the <code>"Host"</code> entry.</p>
<p>An attacker who can control a <code>Location:</code> response seen by the victim client (their own endpoint, a redirect-open service, or a compromised upstream) receives the credential on the cross-origin follow. No special configuration is required beyond the victim using the standard header casing.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Configure a Tesla client with <code>Tesla.Middleware.FollowRedirects</code> and set the <code>Authorization</code> header using canonical casing (<code>{"Authorization", "Bearer &lt;token&gt;"}</code>).</li>
<li>Make a request to an endpoint that returns a <code>302</code> redirect to a different origin.</li>
<li>Observe that the <code>Authorization</code> header with its value is present in the request delivered to the redirect destination.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>High severity (CVSS v4.0: 8.2). Any application using <code>tesla</code> 1.4.0 through 1.18.2 with <code>Tesla.Middleware.FollowRedirects</code> and a non-lowercase <code>Authorization</code> header is affected. The workaround is to use all-lowercase <code>"authorization"</code> as the header key until upgrading to 1.18.3.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Normalize all header keys to lowercase before passing them to Tesla. Use <code>"authorization"</code> instead of <code>"Authorization"</code> when setting headers via <code>Tesla.put_header/3</code> or <code>Tesla.Middleware.Headers</code>.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-tesla/tesla/commit/2d937d5813d7cda5cd726f41824985fb655c920f">https://github.com/elixir-tesla/tesla/commit/2d937d5813d7cda5cd726f41824985fb655c920f</a></li>
<li>Patch commit: <a href="https://github.com/elixir-tesla/tesla/commit/db963dba67651b9abd1fc420a1d9679cf6efe182">https://github.com/elixir-tesla/tesla/commit/db963dba67651b9abd1fc420a1d9679cf6efe182</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-tesla/tesla/security/advisories/GHSA-9m9w-gxf7-rh8m">https://github.com/elixir-tesla/tesla/security/advisories/GHSA-9m9w-gxf7-rh8m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48595">https://nvd.nist.gov/vuln/detail/CVE-2026-48595</a></li>
<li><a href="https://github.com/elixir-tesla/tesla/commit/db963dba67651b9abd1fc420a1d9679cf6efe182">https://github.com/elixir-tesla/tesla/commit/db963dba67651b9abd1fc420a1d9679cf6efe182</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48595.html">https://cna.erlef.org/cves/CVE-2026-48595.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48595">https://osv.dev/vulnerability/EEF-CVE-2026-48595</a></li>
<li><a href="https://github.com/advisories/GHSA-9m9w-gxf7-rh8m">https://github.com/advisories/GHSA-9m9w-gxf7-rh8m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9m9w-gxf7-rh8m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-10T00:03:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tesla] Tesla has decompression bomb on response body]]></title>
        <id>https://github.com/advisories/GHSA-mc85-72gr-vm9f</id>
        <link href="https://github.com/advisories/GHSA-mc85-72gr-vm9f"/>
        <updated>2026-07-10T00:03:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Any Tesla client pipeline that includes <code>Tesla.Middleware.DecompressResponse</code> or <code>Tesla.Middleware.Compression</code> eagerly decompresses HTTP response bodies with no size limit. A server under attacker control (or reached via a redirect) can return a tiny gzip-encoded payload that expands into gigabytes of BEAM heap, crashing or freezing the calling process. Stacking multiple <code>content-encoding</code> tokens multiplies the amplification exponentially.</p>
<h3 id="details">Details</h3>
<p><code>decompress_body/2</code> in <code>lib/tesla/middleware/compression.ex</code> passes the full response body to <code>:zlib.gunzip/1</code> or <code>:zlib.unzip/1</code> with no cap on output size. The list of codec tokens comes from splitting the <code>content-encoding</code> header on commas, and <code>decompress_body/2</code> recurses once per token. A response advertising <code>content-encoding: gzip, gzip, gzip, gzip</code> triggers four recursive decompression passes. Each gzip layer can expand its input roughly 1000x, so a 284-byte wire payload with four layers inflates to approximately 1 GB at the innermost pass, all materialised as a single binary in the caller's heap.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Serve an HTTP response with <code>content-encoding: gzip, gzip, gzip, gzip</code> where the body is a 1 GB block of zeros compressed through four successive gzip passes.</li>
<li>Send that response to a Tesla client whose pipeline includes <code>Tesla.Middleware.DecompressResponse</code>.</li>
<li><code>decompress_body/2</code> recurses four times without any size check, materialising ~1 GB in the calling process's heap.</li>
<li>Repeated or sufficiently large requests exhaust available memory and crash or freeze the node.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>High severity (CVSS v4.0: 8.2). Any application using <code>tesla</code> 0.6.0 through 1.18.2 with <code>Tesla.Middleware.DecompressResponse</code> or <code>Tesla.Middleware.Compression</code> in its pipeline is vulnerable. The attacker only needs to control a server the client contacts, including via redirects. Fixed in tesla 1.18.3.</p>
<h3 id="configurations">Configurations</h3>
<p>The application must include <code>Tesla.Middleware.DecompressResponse</code> or <code>Tesla.Middleware.Compression</code> in its Tesla middleware pipeline.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-tesla/tesla/commit/5bd90bb5cf0d15e375edc2a66fa322292940fce2">https://github.com/elixir-tesla/tesla/commit/5bd90bb5cf0d15e375edc2a66fa322292940fce2</a></li>
<li>Patch commit: <a href="https://github.com/elixir-tesla/tesla/commit/340f75b5d191dc747ef7ac6365bd002d1cd55a9d">https://github.com/elixir-tesla/tesla/commit/340f75b5d191dc747ef7ac6365bd002d1cd55a9d</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-tesla/tesla/security/advisories/GHSA-mc85-72gr-vm9f">https://github.com/elixir-tesla/tesla/security/advisories/GHSA-mc85-72gr-vm9f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48594">https://nvd.nist.gov/vuln/detail/CVE-2026-48594</a></li>
<li><a href="https://github.com/elixir-tesla/tesla/commit/340f75b5d191dc747ef7ac6365bd002d1cd55a9d">https://github.com/elixir-tesla/tesla/commit/340f75b5d191dc747ef7ac6365bd002d1cd55a9d</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48594.html">https://cna.erlef.org/cves/CVE-2026-48594.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48594">https://osv.dev/vulnerability/EEF-CVE-2026-48594</a></li>
<li><a href="https://github.com/advisories/GHSA-mc85-72gr-vm9f">https://github.com/advisories/GHSA-mc85-72gr-vm9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mc85-72gr-vm9f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-10T00:03:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tesla] Tesla has CRLF injection in request `Content-Type` header via `add_content_type_param`]]></title>
        <id>https://github.com/advisories/GHSA-q7jx-v53g-848w</id>
        <link href="https://github.com/advisories/GHSA-q7jx-v53g-848w"/>
        <updated>2026-07-10T00:03:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>Tesla.Multipart.add_content_type_param/2</code> appends caller-supplied strings to the multipart <code>Content-Type</code> header with no validation. A param value containing <code>\r\n</code> splits the header line, allowing an attacker who controls any content-type parameter (charset, boundary parameter, etc.) to inject arbitrary headers into the outbound HTTP request.</p>
<h3 id="details">Details</h3>
<p><code>add_content_type_param/2</code> in <code>lib/tesla/multipart.ex</code> stores the supplied string directly in <code>multipart.content_type_params</code> without any CR/LF check. <code>headers/1</code> then joins all params with <code>"; "</code> and appends the result verbatim to the <code>Content-Type</code> header value. Because HTTP headers are delimited by <code>\r\n</code>, a param containing that sequence breaks out of the header field and introduces new header lines before the adapter writes the request to the socket.</p>
<p>The precondition is that untrusted input reaches <code>add_content_type_param/2</code>, which is the normal pattern for applications that accept user-supplied charset values, file type parameters, or any other content-type extension fields.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Call <code>Tesla.Multipart.add_content_type_param/2</code> with a value containing <code>\r\nX-Injected: pwned</code>.</li>
<li>Pass the resulting <code>Multipart</code> struct as the request body via any Tesla adapter.</li>
<li>The raw request on the wire contains <code>X-Injected: pwned</code> as a standalone header line.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Low severity (CVSS v4.0: 2.1). Any application using <code>tesla</code> 0.8.0 through 1.18.2 that passes untrusted input into <code>Tesla.Multipart.add_content_type_param/2</code> is affected. Consequences range from forging arbitrary outbound request headers to potential request smuggling against the upstream server. Fixed in tesla 1.18.3.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Validate content-type parameter strings before passing them to <code>Tesla.Multipart.add_content_type_param/2</code>, rejecting any value that contains <code>\r</code> or <code>\n</code>.</p>
<h3 id="reesources">Reesources</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-tesla/tesla/commit/6ebfdb9abe9c6f119408045b933d82462decd351">https://github.com/elixir-tesla/tesla/commit/6ebfdb9abe9c6f119408045b933d82462decd351</a></li>
<li>Patch commit: <a href="https://github.com/elixir-tesla/tesla/commit/23601edac5d22ba9407b427967b5bdbda201aec2">https://github.com/elixir-tesla/tesla/commit/23601edac5d22ba9407b427967b5bdbda201aec2</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-tesla/tesla/security/advisories/GHSA-q7jx-v53g-848w">https://github.com/elixir-tesla/tesla/security/advisories/GHSA-q7jx-v53g-848w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48596">https://nvd.nist.gov/vuln/detail/CVE-2026-48596</a></li>
<li><a href="https://github.com/elixir-tesla/tesla/commit/23601edac5d22ba9407b427967b5bdbda201aec2">https://github.com/elixir-tesla/tesla/commit/23601edac5d22ba9407b427967b5bdbda201aec2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48596.html">https://cna.erlef.org/cves/CVE-2026-48596.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48596">https://osv.dev/vulnerability/EEF-CVE-2026-48596</a></li>
<li><a href="https://github.com/advisories/GHSA-q7jx-v53g-848w">https://github.com/advisories/GHSA-q7jx-v53g-848w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q7jx-v53g-848w</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-10T00:03:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mint] mint: Unbounded streams map growth via PUSH_PROMISE without follow-up HEADERS]]></title>
        <id>https://github.com/advisories/GHSA-g586-ccqf-7x4r</id>
        <link href="https://github.com/advisories/GHSA-g586-ccqf-7x4r"/>
        <updated>2026-07-09T23:19:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Mint's HTTP/2 client accepts <code>PUSH_PROMISE</code> frames from any server it connects to and inserts every promised stream into a per-connection map without consulting <code>max_concurrent_streams</code>. A malicious or compromised HTTP/2 server can flood the client with <code>PUSH_PROMISE</code> frames and withhold the matching response <code>HEADERS</code>, pinning one map entry per frame indefinitely until the client process runs out of memory.</p>
<h3 id="details">Details</h3>
<p><code>'Elixir.Mint.HTTP2':handle_push_promise/3</code> in <code>lib/mint/http2.ex</code> dispatches every inbound <code>PUSH_PROMISE</code> frame to <code>'Elixir.Mint.HTTP2':decode_push_promise_headers_and_add_response/5</code>, which inserts a <code>:reserved_remote</code> entry into <code>conn.streams</code> for the promised ID. The only validation applied is that the promised ID is even and not already present; <code>client_settings.max_concurrent_streams</code> is not consulted at promise time.</p>
<p>The concurrency cap is only checked when the response <code>HEADERS</code> for the promised stream arrive. A server that emits <code>PUSH_PROMISE</code> frames and never sends the matching <code>HEADERS</code> never trips that check, and the existing tally counts only streams in open states, not <code>:reserved_remote</code> entries.</p>
<p>HTTP/2 server push is accepted by default (<code>client_settings.enable_push</code> defaults to <code>true</code>), so no application opt-in is required. A single long-lived HTTP/2 connection to a hostile server lets it pin one <code>conn.streams</code> entry per <code>PUSH_PROMISE</code> frame, with no upper bound.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Stand up a raw TCP HTTP/2 server that completes the handshake and ACKs the client's <code>SETTINGS</code>.</li>
<li>Wait for the client's request <code>HEADERS</code> and capture its odd stream ID.</li>
<li>Send a flood of <code>PUSH_PROMISE</code> frames (<code>flags = END_HEADERS</code>) associated with the captured stream, each promising a fresh even stream ID and carrying a minimal HPACK-encoded header block.</li>
<li>Never send the matching response <code>HEADERS</code> for any of the promised IDs.</li>
<li>The client's <code>conn.streams</code> map grows by one entry per <code>PUSH_PROMISE</code> frame (~148 bytes/entry); memory grows linearly and the BEAM process eventually crashes with OOM.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Remote, unauthenticated denial-of-service against any process using Mint as an HTTP/2 client against an untrusted or attacker-influenced server. Server push is on by default, so no application code change can prevent it short of disabling push or upgrading. Affected populations include outbound HTTP/2 clients in web backends, webhook delivery systems, scrapers, federated and proxy components, and any service that follows redirects to third-party HTTP/2 origins.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Disable HTTP/2 server push on connections to untrusted servers by passing <code>client_settings: [enable_push: false]</code> to <code>'Elixir.Mint.HTTP':connect/4</code>. Mint will then reject any inbound <code>PUSH_PROMISE</code> frame with a <code>PROTOCOL_ERROR</code> before the vulnerable code path is reached.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-mint/mint/commit/65c6394d05a1b8aa4a7461708c3aa173e8d7a5cf">https://github.com/elixir-mint/mint/commit/65c6394d05a1b8aa4a7461708c3aa173e8d7a5cf</a></li>
<li>Patch commit: <a href="https://github.com/elixir-mint/mint/commit/70b97b6a5209fb288b0e04d8e657dda26c59de67">https://github.com/elixir-mint/mint/commit/70b97b6a5209fb288b0e04d8e657dda26c59de67</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-mint/mint/security/advisories/GHSA-g586-ccqf-7x4r">https://github.com/elixir-mint/mint/security/advisories/GHSA-g586-ccqf-7x4r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48862">https://nvd.nist.gov/vuln/detail/CVE-2026-48862</a></li>
<li><a href="https://github.com/elixir-mint/mint/commit/70b97b6a5209fb288b0e04d8e657dda26c59de67">https://github.com/elixir-mint/mint/commit/70b97b6a5209fb288b0e04d8e657dda26c59de67</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48862.html">https://cna.erlef.org/cves/CVE-2026-48862.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48862">https://osv.dev/vulnerability/EEF-CVE-2026-48862</a></li>
<li><a href="https://github.com/advisories/GHSA-g586-ccqf-7x4r">https://github.com/advisories/GHSA-g586-ccqf-7x4r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g586-ccqf-7x4r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-09T23:19:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mint] mint: Unbounded CONTINUATION/HEADERS frame accumulation (CONTINUATION flood)]]></title>
        <id>https://github.com/advisories/GHSA-2p26-p43x-fhp8</id>
        <link href="https://github.com/advisories/GHSA-2p26-p43x-fhp8"/>
        <updated>2026-07-09T23:19:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Mint's HTTP/2 client accumulates <code>CONTINUATION</code> header-block fragments into a per-connection buffer with no cap on size or frame count. A malicious or compromised HTTP/2 server can drive the client's memory to arbitrary size by streaming an endless chain of <code>CONTINUATION</code> frames after a <code>HEADERS</code> frame that omits <code>END_HEADERS</code>, causing memory exhaustion and BEAM process death. A single connection to an attacker-controlled HTTP/2 endpoint is sufficient.</p>
<h3 id="details">Details</h3>
<p>When Mint's HTTP/2 receive path observes a <code>HEADERS</code> frame without the <code>END_HEADERS</code> flag, <code>'Elixir.Mint.HTTP2':handle_headers/3</code> parks the unparsed header-block fragment in <code>conn.headers_being_processed</code>. Every subsequent <code>CONTINUATION</code> frame on that stream is then appended to the accumulator by <code>'Elixir.Mint.HTTP2':handle_continuation/3</code>.</p>
<p>Nothing in the receive path bounds this accumulator: there is no per-stream size cap, no <code>CONTINUATION</code> frame-count cap, and <code>max_header_list_size</code> is only enforced on outgoing requests (its default is <code>:infinity</code>, and the only enforcement helper inspects <code>server_settings</code> for request encoding, never inbound header blocks). Each <code>CONTINUATION</code> payload can be up to the peer-advertised <code>SETTINGS_MAX_FRAME_SIZE</code>, so the attacker can grow <code>headers_being_processed</code> to arbitrary size at line rate.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Stand up a raw TCP server that speaks the HTTP/2 handshake.</li>
<li>After the client's request <code>HEADERS</code> arrives, respond with a <code>HEADERS</code> frame on stream 1 with <code>flags = 0</code> (no <code>END_HEADERS</code>, no <code>END_STREAM</code>) and an empty header-block fragment.</li>
<li>Stream <code>CONTINUATION</code> frames on stream 1, each with <code>flags = 0</code> and a payload up to <code>SETTINGS_MAX_FRAME_SIZE</code>. Never set <code>END_HEADERS</code>.</li>
<li>The client's process memory grows linearly with the flood and the BEAM process eventually crashes with OOM.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Remote, unauthenticated denial-of-service against any process using Mint as an HTTP/2 client against an untrusted or attacker-influenced server. A single connection is sufficient to drive memory to arbitrary size and crash the BEAM process. The default Mint configuration is vulnerable; no client-side opt-in is required. Scored CVSS v4.0 8.2 (HIGH).</p>
<h2 id="workarounds">Workarounds</h2>
<p>Restrict Mint to HTTP/1 on connections to untrusted servers by passing <code>protocols: [:http1]</code> to <code>'Elixir.Mint.HTTP':connect/4</code>. This avoids the vulnerable HTTP/2 receive path entirely, at the cost of losing HTTP/2 for those connections.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-mint/mint/commit/596ca4304504be68939c4929e0831557097962b8">https://github.com/elixir-mint/mint/commit/596ca4304504be68939c4929e0831557097962b8</a></li>
<li>Patch commit: <a href="https://github.com/elixir-mint/mint/commit/b662d127d3028b5426c88d4c9cc7fe430491a10b">https://github.com/elixir-mint/mint/commit/b662d127d3028b5426c88d4c9cc7fe430491a10b</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-mint/mint/security/advisories/GHSA-2p26-p43x-fhp8">https://github.com/elixir-mint/mint/security/advisories/GHSA-2p26-p43x-fhp8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49754">https://nvd.nist.gov/vuln/detail/CVE-2026-49754</a></li>
<li><a href="https://github.com/elixir-mint/mint/commit/b662d127d3028b5426c88d4c9cc7fe430491a10b">https://github.com/elixir-mint/mint/commit/b662d127d3028b5426c88d4c9cc7fe430491a10b</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-49754.html">https://cna.erlef.org/cves/CVE-2026-49754.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-49754">https://osv.dev/vulnerability/EEF-CVE-2026-49754</a></li>
<li><a href="https://github.com/advisories/GHSA-2p26-p43x-fhp8">https://github.com/advisories/GHSA-2p26-p43x-fhp8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2p26-p43x-fhp8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-09T23:19:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mint] mint: Content-Length header accepts non-RFC "+" sign prefix]]></title>
        <id>https://github.com/advisories/GHSA-mjqx-c6f6-7rc2</id>
        <link href="https://github.com/advisories/GHSA-mjqx-c6f6-7rc2"/>
        <updated>2026-07-09T23:19:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Mint's HTTP/1 client accepts <code>Content-Length</code> header values with a leading <code>+</code> sign (e.g. <code>+0</code>, <code>+123</code>), which RFC 7230 forbids (<code>Content-Length = 1*DIGIT</code>). On a connection shared with a strict fronting proxy or load balancer, this parser disagreement is a response-smuggling primitive: the proxy frames the body one way, Mint frames it another, and bytes meant for one response leak into the next consumer's response stream.</p>
<h3 id="details">Details</h3>
<p><code>'Elixir.Mint.HTTP1.Parse':content_length_header/1</code> in <code>lib/mint/http1/parse.ex</code> parses the header value with <code>Integer.parse/1</code>. By design, <code>Integer.parse/1</code> accepts an optional <code>+</code> or <code>-</code> sign prefix. The <code>length &gt;= 0</code> guard rules out negatives, but inputs such as <code>"+0"</code>, <code>"+123"</code>, or <code>"+1"</code> pass through and are returned as valid lengths.</p>
<p>A strict proxy or load balancer rejects or reframes <code>Content-Length: +0\r\n</code>, while Mint silently treats it as <code>0</code>. When Mint reuses the socket (keep-alive, pipelining, or any pooled connection) and the connection is shared with a proxy that frames the same bytes differently, trailing bytes the proxy attributes to response N are attributed by Mint to response N+1. Across trust boundaries (shared pools, multi-tenant fronting) this enables response smuggling.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Stand up a raw TCP server that returns <code>HTTP/1.1 200 OK\r\nContent-Length: +0\r\nConnection: keep-alive\r\n\r\n&lt;smuggled bytes&gt;</code>.</li>
<li>Connect a Mint HTTP/1 client to the server and issue a request.</li>
<li>Observe that Mint reports the response as status 200 with <code>Content-Length: "+0"</code> and an empty body, leaving the smuggled bytes sitting in the socket buffer for the next response.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Response-smuggling / request-response desync primitive in Mint's HTTP/1 client parser. Anyone using Mint (directly or via Finch, Tesla's Mint adapter, Req, etc.) to talk through a shared or pooled connection where a fronting proxy enforces RFC 7230 strictly while Mint does not is exposed. The attacker is the response producer (a malicious or compromised upstream, or anything that can inject bytes into a shared origin response); exploitation into a cross-request data leak additionally requires the deployment to share a Mint connection across trust boundaries.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-mint/mint/commit/65e0e86d799a6d3b08e4372fccdd9747535e0dd6">https://github.com/elixir-mint/mint/commit/65e0e86d799a6d3b08e4372fccdd9747535e0dd6</a></li>
<li>Patch commit: <a href="https://github.com/elixir-mint/mint/commit/47e48027480228e4e32a0b4df39db497b4804921">https://github.com/elixir-mint/mint/commit/47e48027480228e4e32a0b4df39db497b4804921</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-mint/mint/security/advisories/GHSA-mjqx-c6f6-7rc2">https://github.com/elixir-mint/mint/security/advisories/GHSA-mjqx-c6f6-7rc2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49753">https://nvd.nist.gov/vuln/detail/CVE-2026-49753</a></li>
<li><a href="https://github.com/elixir-mint/mint/commit/47e48027480228e4e32a0b4df39db497b4804921">https://github.com/elixir-mint/mint/commit/47e48027480228e4e32a0b4df39db497b4804921</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-49753.html">https://cna.erlef.org/cves/CVE-2026-49753.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-49753">https://osv.dev/vulnerability/EEF-CVE-2026-49753</a></li>
<li><a href="https://github.com/advisories/GHSA-mjqx-c6f6-7rc2">https://github.com/advisories/GHSA-mjqx-c6f6-7rc2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mjqx-c6f6-7rc2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-09T23:19:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mint] mint has potential CRLF injection in its HTTP request line via unvalidated `method`/`target`]]></title>
        <id>https://github.com/advisories/GHSA-2pg6-44cx-c49v</id>
        <link href="https://github.com/advisories/GHSA-2pg6-44cx-c49v"/>
        <updated>2026-07-09T23:19:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Mint's HTTP/1 request encoder splices the caller-supplied <code>method</code> and <code>target</code> directly into the request line without character validation. An application that forwards attacker-controlled input as the HTTP method or the target to <code>Mint.HTTP.request/5</code> is exposed to request-line CRLF injection, allowing the attacker to terminate the request line early, inject arbitrary headers, and pipeline a fully attacker-chosen second request onto the same TCP connection.</p>
<h3 id="details">Details</h3>
<p><code>encode_request_line/2</code> in <code>lib/mint/http1/request.ex</code> writes <code>method</code> and <code>target</code> to the wire verbatim. <code>encode_headers/1</code> validates header names and values, but there is no equivalent <code>validate_method!/1</code>.</p>
<p>Mint 1.7.0 added <code>validate_request_target/2</code>, which rejects CRLF and other control characters in <code>target</code> by default and closes the path/query vector. The <code>method</code> field remains unvalidated, so a CRLF-bearing method such as <code>"GET / HTTP/1.1\r\nX-Smuggled: 1\r\nGET /admin"</code> is accepted and written to the socket as-is. Bytes after the first <code>\r\n</code> are interpreted by the peer as an injected header, or, with a second <code>\r\n</code>, as an additional pipelined request.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Stand up a Mint-using gateway/proxy that calls <code>Mint.HTTP.request(conn, method, "/", [], nil)</code> with <code>method</code> taken from caller input.</li>
<li>Send a request whose forwarded method is <code>"GET / HTTP/1.1\r\nX-Smuggled-Header: pwned\r\nGET /admin/delete-everything"</code>.</li>
<li>Observe the bytes received by the upstream server: the smuggled header line and the second request line appear verbatim in the outbound stream.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>CRLF injection / HTTP request smuggling in the HTTP/1 client encoder, exploitable under default configuration whenever an application passes caller-influenced input as the HTTP method. An attacker who controls the method can inject arbitrary outbound headers (forged <code>Host</code>, <code>Authorization</code>, cache-poisoning headers) and smuggle additional, fully attacker-chosen requests to the upstream server over the same connection, potentially reaching endpoints the legitimate caller never intended to invoke.</p>
<h2 id="resources">Resources</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-mint/mint/commit/8db1acff30b6a9433762c18b1e1f891b8c1f74f7">https://github.com/elixir-mint/mint/commit/8db1acff30b6a9433762c18b1e1f891b8c1f74f7</a></li>
<li>Patch commit: <a href="https://github.com/elixir-mint/mint/commit/fad091454cbb7449b19edb8e1fee12ca7cf28c3a">https://github.com/elixir-mint/mint/commit/fad091454cbb7449b19edb8e1fee12ca7cf28c3a</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-mint/mint/security/advisories/GHSA-2pg6-44cx-c49v">https://github.com/elixir-mint/mint/security/advisories/GHSA-2pg6-44cx-c49v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48861">https://nvd.nist.gov/vuln/detail/CVE-2026-48861</a></li>
<li><a href="https://github.com/elixir-mint/mint/commit/fad091454cbb7449b19edb8e1fee12ca7cf28c3a">https://github.com/elixir-mint/mint/commit/fad091454cbb7449b19edb8e1fee12ca7cf28c3a</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48861.html">https://cna.erlef.org/cves/CVE-2026-48861.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48861">https://osv.dev/vulnerability/EEF-CVE-2026-48861</a></li>
<li><a href="https://github.com/advisories/GHSA-2pg6-44cx-c49v">https://github.com/advisories/GHSA-2pg6-44cx-c49v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2pg6-44cx-c49v</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-09T23:19:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[quic] QUIC has Broken TLS verification]]></title>
        <id>https://github.com/advisories/GHSA-2r8v-p65x-3663</id>
        <link href="https://github.com/advisories/GHSA-2r8v-p65x-3663"/>
        <updated>2026-07-01T20:32:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The QUIC client did not authenticate the server during the TLS 1.3 handshake. The CertificateVerify signature was not checked, the certificate chain was not validated, and the hostname was not compared against the certificate, so <code>verify</code> was effectively a no-op on the client. A man-in-the-middle on the network path could present any certificate and impersonate any server, defeating the confidentiality and integrity of the connection. HTTP/3 uses the same client and was equally affected. Handshakes authenticated by a PSK (session resumption) are not affected, because the peer is authenticated by the PSK binder and no certificate is sent.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in 1.4.4. The client now verifies the CertificateVerify signature, validates the certificate chain against the trust store (<code>cacerts</code> option, the operating system store by default), and checks the hostname. Client <code>verify</code> now defaults to on; set <code>verify =&gt; false</code> to accept any certificate (for example a self-signed test server).</p>
<h3 id="workarounds">Workarounds</h3>
<p>None before 1.4.4. <code>verify =&gt; true</code> had no effect, and inspecting the certificate after connecting does not help because without the signature check the peer is never proven to own the certificate it presents.</p>
<h3 id="credit">Credit</h3>
<p>Reported by benmmurphy.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/benoitc/erlang_quic/security/advisories/GHSA-2r8v-p65x-3663">https://github.com/benoitc/erlang_quic/security/advisories/GHSA-2r8v-p65x-3663</a></li>
<li><a href="https://github.com/advisories/GHSA-2r8v-p65x-3663">https://github.com/advisories/GHSA-2r8v-p65x-3663</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r8v-p65x-3663</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-01T20:32:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oban_web] oban_web missing authorization check on `save-job` event handler]]></title>
        <id>https://github.com/advisories/GHSA-389x-rgxr-8m33</id>
        <link href="https://github.com/advisories/GHSA-389x-rgxr-8m33"/>
        <updated>2026-06-30T18:33:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>oban_web</code> 2.12.0 through the current unpatched release exposes a <code>save-job</code> LiveView event handler that performs no authorization check, allowing any authenticated user (including those with <code>:read_only</code> access) to overwrite a queued job's <code>worker</code> field with any other <code>Oban.Worker</code> module present in the application. On the job's next execution attempt, Oban dispatches <code>perform/1</code> on the attacker-chosen module instead of the intended one.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/oban/web/live/jobs/detail_component.ex</code>, the sibling event handlers for destructive actions all gate their side effects via <code>can?/2</code> (cancel, delete, retry). The <code>handle_event("save-job", params, socket)</code> clause added in 2.12.0 has no equivalent guard. It builds a <code>changes</code> map from the client-supplied params (including <code>worker</code>, <code>queue</code>, <code>priority</code>, <code>max_attempts</code>, <code>scheduled_at</code>, <code>tags</code>, <code>args</code>) and unconditionally dispatches <code>{:update_job, job, changes}</code> to the parent LiveView, which writes the changes to the database.</p>
<p>The <code>disabled</code> attribute on the edit fieldset and button in the rendered HTML is advisory only. The Phoenix LiveView channel dispatches any <code>phx-event</code> pushed over the authenticated WebSocket regardless of what the DOM looks like, so the attacker pushes the event directly over the WebSocket without touching the UI.</p>
<p>The attacker is constrained to substituting an existing <code>Oban.Worker</code> module already loaded in the application (no code injection). The impact depends on what workers are available in the target application.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Obtain an authenticated session with at minimum <code>:read_only</code> access to the Oban.Web dashboard.</li>
<li>Open any job's detail panel to obtain its job ID.</li>
<li>Push a forged <code>save-job</code> event over the LiveView WebSocket with <code>"worker"</code> set to the desired target module name.</li>
<li>The server accepts the payload and updates the job row. On its next execution attempt, Oban invokes <code>perform/1</code> on the attacker-chosen module.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>CVSS 4.0 score 5.3 (Medium). Any application running <code>oban_web</code> &gt;= 2.12.0 that exposes the dashboard to users with less than full job-management privileges is affected. The only precondition is an authenticated session with <code>:read_only</code> access or higher.</p>
<h2 id="references">References</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/oban-bg/oban_web/commit/a17bc8c31286c9d516e2892cf5483d1c95e65d6c">https://github.com/oban-bg/oban_web/commit/a17bc8c31286c9d516e2892cf5483d1c95e65d6c</a></li>
<li>Patch commit: <a href="https://github.com/oban-bg/oban_web/commit/ab3c5d1d3eba06c62045f16f2cd7781c7752e248">https://github.com/oban-bg/oban_web/commit/ab3c5d1d3eba06c62045f16f2cd7781c7752e248</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/oban-bg/oban_web/security/advisories/GHSA-389x-rgxr-8m33">https://github.com/oban-bg/oban_web/security/advisories/GHSA-389x-rgxr-8m33</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48592">https://nvd.nist.gov/vuln/detail/CVE-2026-48592</a></li>
<li><a href="https://github.com/oban-bg/oban_web/commit/ab3c5d1d3eba06c62045f16f2cd7781c7752e248">https://github.com/oban-bg/oban_web/commit/ab3c5d1d3eba06c62045f16f2cd7781c7752e248</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48592.html">https://cna.erlef.org/cves/CVE-2026-48592.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48592">https://osv.dev/vulnerability/EEF-CVE-2026-48592</a></li>
<li><a href="https://github.com/advisories/GHSA-389x-rgxr-8m33">https://github.com/advisories/GHSA-389x-rgxr-8m33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-389x-rgxr-8m33</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-30T18:33:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oban_web] oban_web: Unbounded range expansion in cron describe causes memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-6xh2-93p9-vqh4</id>
        <link href="https://github.com/advisories/GHSA-6xh2-93p9-vqh4"/>
        <updated>2026-06-30T18:32:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>oban_web</code> 2.12.0 introduced a cron expression parser that expands <code>-</code>-separated ranges without validating the endpoints. An attacker with access to schedule cron jobs can submit a malicious expression; when any user with dashboard access views the cron job list, <code>Oban.Web.CronExpr.describe/1</code> is called to render it, triggering allocation of gigabytes of memory and stalling or crashing the BEAM node.</p>
<h3 id="details">Details</h3>
<p><strong>1. Scheduling:</strong> The attacker submits a cron job with a malicious expression such as <code>"0 0 1-100000000 * *"</code>. No special privilege is required beyond the ability to schedule cron jobs.</p>
<p><strong>2. Parsing:</strong> When the cron list is rendered in the dashboard, <code>describe/1</code> calls <code>parse_range/1</code> in <code>lib/oban/web/cron_expr.ex</code>, which calls <code>Integer.parse/1</code> on both endpoints of the range with no bounds check, returning <code>{:range, start_val, end_val}</code> for any integers.</p>
<p><strong>3. Eager expansion:</strong> <code>expand_dom_parts/1</code> and <code>expand_dow_parts/1</code> materialise the range via <code>Enum.to_list(start_val..end_val)</code>. The input above produces <del>100 million integers (</del>2.4 GB). A sibling helper <code>extract_dom_values</code> already validates range bounds, but the expansion helpers do not.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Schedule a cron job with expression <code>"0 0 1-100000000 * *"</code> (or any expression with an out-of-domain range).</li>
<li>Have any user with Oban.Web dashboard access navigate to the cron job list.</li>
<li>The dashboard calls <code>describe/1</code> to render the expression, exhausting BEAM memory and crashing the node.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>CVSS 4.0 score 5.9 (Medium). Affects <code>oban_web</code> &gt;= 2.12.0. Requires the ability to schedule a cron job and a dashboard user to view the cron list; no further privileges are needed.</p>
<h3 id="references">References</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/oban-bg/oban_web/commit/a97c7960bb389b05aaab4cf8042985f02ceddc24">https://github.com/oban-bg/oban_web/commit/a97c7960bb389b05aaab4cf8042985f02ceddc24</a></li>
<li>Patch commit: <a href="https://github.com/oban-bg/oban_web/commit/9998b7e284e02fdd4645dd6231760038e63b584d">https://github.com/oban-bg/oban_web/commit/9998b7e284e02fdd4645dd6231760038e63b584d</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/oban-bg/oban_web/security/advisories/GHSA-6xh2-93p9-vqh4">https://github.com/oban-bg/oban_web/security/advisories/GHSA-6xh2-93p9-vqh4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48593">https://nvd.nist.gov/vuln/detail/CVE-2026-48593</a></li>
<li><a href="https://github.com/oban-bg/oban_web/commit/9998b7e284e02fdd4645dd6231760038e63b584d">https://github.com/oban-bg/oban_web/commit/9998b7e284e02fdd4645dd6231760038e63b584d</a></li>
<li><a href="https://github.com/oban-bg/oban_web/commit/a97c7960bb389b05aaab4cf8042985f02ceddc24">https://github.com/oban-bg/oban_web/commit/a97c7960bb389b05aaab4cf8042985f02ceddc24</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48593.html">https://cna.erlef.org/cves/CVE-2026-48593.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48593">https://osv.dev/vulnerability/EEF-CVE-2026-48593</a></li>
<li><a href="https://github.com/advisories/GHSA-6xh2-93p9-vqh4">https://github.com/advisories/GHSA-6xh2-93p9-vqh4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6xh2-93p9-vqh4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-30T18:32:49.000Z</published>
    </entry>
</feed>