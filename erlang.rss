<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/erlang.rss</id>
    <title>Security Advisory for Erlang/Elixir packages hosted at hex.pm</title>
    <updated>2025-11-08T10:01:54.704Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Aerlang"/>
    <subtitle>Security Advisory for Erlang/Elixir packages hosted at hex.pm on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[ash] Ash has authorization bypass when bypass policy condition evaluates to true]]></title>
        <id>https://github.com/advisories/GHSA-pcxq-fjp3-r752</id>
        <link href="https://github.com/advisories/GHSA-pcxq-fjp3-r752"/>
        <updated>2025-10-17T20:07:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Bypass policies incorrectly authorize requests when their condition evaluates to true but their authorization checks fail and no other policies apply.</p>
<h3 id="impact">Impact</h3>
<p>Resources with bypass policies can be accessed without proper authorization when:</p>
<ul>
<li>Bypass condition evaluates to true</li>
<li>Bypass authorization checks fail</li>
<li>Other policies exist but their conditions don't match</li>
</ul>
<h3 id="details">Details</h3>
<p>Vulnerable code in: <a href="https://github.com/ash-project/ash/blob/b2e4d625/lib/ash/policy/policy.ex#L69">lib/ash/policy/policy.ex:69</a></p>
<pre><code class="language-elixir">{%{bypass?: true}, cond_expr, complete_expr}, {one_condition_matches, all_policies_match} -&gt;
  {
    b(cond_expr or one_condition_matches),  # &lt;- Bug: uses condition only
    b(complete_expr or all_policies_match)
  }
</code></pre>
<p>The final authorization decision is: <code>one_condition_matches AND all_policies_match</code></p>
<p>When a bypass condition is true but bypass policies fail, and subsequent policies have non-matching conditions:</p>
<ol>
<li><strong>one_condition_matches</strong> = <code>cond_expr</code> (bypass condition) = <strong>true</strong> (bug - should check if bypass actually authorizes)</li>
<li><strong>all_policies_match</strong> = <code>(complete_expr OR NOT cond_expr)</code> for each policy<ul>
<li>For non-matching policies: <code>(false OR NOT false)</code> = <strong>true</strong> (policies don't apply)</li>
</ul>
</li>
<li><strong>Final</strong>: <code>true AND true</code> = <strong>true</strong> (incorrectly authorized)</li>
</ol>
<p>The bypass condition alone satisfies "at least one policy applies" even though the bypass fails to authorize.</p>
<h3 id="fix">Fix</h3>
<p>Replace <code>cond_expr</code> with <code>complete_expr</code> on line 69:</p>
<pre><code class="language-elixir">{%{bypass?: true}, _cond_expr, complete_expr}, {one_condition_matches, all_policies_match} -&gt;
  {
    b(complete_expr or one_condition_matches),  # &lt;- Fixed
    b(complete_expr or all_policies_match)
  }
</code></pre>
<p>Line 52 should also be updated for consistency (though it's only triggered when bypass is the last policy, making it coincidentally safe in practice):</p>
<pre><code class="language-elixir">{%{bypass?: true}, _cond_expr, complete_expr}, {one_condition_matches, true} -&gt;
  {
    b(complete_expr or one_condition_matches),  # &lt;- For consistency
    complete_expr
  }
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-elixir">policies do
  bypass always() do
    authorize_if actor_attribute_equals(:is_admin, true)
  end

  policy action_type(:read) do
    authorize_if always()
  end
end
</code></pre>
<p>Non-admin user can perform create actions (should be denied).</p>
<p>Test demonstrating the bug:</p>
<pre><code class="language-elixir">test "bypass policy bug" do
  policies = [
    %Ash.Policy.Policy{
      bypass?: true,
      condition: [{Ash.Policy.Check.Static, result: true}],  # condition = true
      policies: [
        %Ash.Policy.Check{
          type: :authorize_if,
          check: {Ash.Policy.Check.Static, result: false},  # policies = false
          check_module: Ash.Policy.Check.Static,
          check_opts: [result: false]
        }
      ]
    },
    %Ash.Policy.Policy{
      bypass?: false,
      condition: [{Ash.Policy.Check.Static, result: false}],
      policies: [
        %Ash.Policy.Check{
          type: :authorize_if,
          check: {Ash.Policy.Check.Static, result: true},
          check_module: Ash.Policy.Check.Static,
          check_opts: [result: true]
        }
      ]
    }
  ]

  expression = Ash.Policy.Policy.expression(policies, %{})
  
  assert expression == false
  # Expected: false (deny)
  # Actual on main: true (incorrectly authorized)
end
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ash-project/ash/security/advisories/GHSA-pcxq-fjp3-r752">https://github.com/ash-project/ash/security/advisories/GHSA-pcxq-fjp3-r752</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-48044">https://nvd.nist.gov/vuln/detail/CVE-2025-48044</a></li>
<li><a href="https://github.com/ash-project/ash/commit/8b83efa225f657bfc3656ad8ee8485f9b2de923d">https://github.com/ash-project/ash/commit/8b83efa225f657bfc3656ad8ee8485f9b2de923d</a></li>
<li><a href="https://github.com/advisories/GHSA-pcxq-fjp3-r752">https://github.com/advisories/GHSA-pcxq-fjp3-r752</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pcxq-fjp3-r752</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-10-17T18:03:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash] Ash Framework: Filter authorization misapplies impossible bypass/runtime policies]]></title>
        <id>https://github.com/advisories/GHSA-7r7f-9xpj-jmr7</id>
        <link href="https://github.com/advisories/GHSA-7r7f-9xpj-jmr7"/>
        <updated>2025-10-13T13:33:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When using <strong>filter</strong> authorization, two edge cases could cause the policy compiler/authorizer to generate a permissive filter:</p>
<ol>
<li><p><strong>Bypass policies whose condition can never pass at runtime</strong> were compiled as
<code>OR(AND(condition, compiled_policies), NOT(condition))</code>.
If the condition could never be true at runtime, the <code>NOT(condition)</code> branch evaluated truthy and the overall expression became permissive.</p>
</li>
<li><p><strong>Runtime policy scenarios that reduce to “no checks are applicable”</strong> (an empty SAT scenario) were treated as an empty clause and dropped instead of being treated as <strong><code>false</code></strong>, which could again produce an overly broad (permissive) filter.</p>
</li>
</ol>
<p>These bugs could allow reads to return records that should have been excluded by policy.</p>
<h3 id="impact">Impact</h3>
<p>Projects that rely on <strong>filter-based authorization</strong> and define:</p>
<ul>
<li><code>bypass ... do ... end</code> blocks whose condition(s) are only resolvable at runtime and can never pass in a given request context, <strong>or</strong></li>
<li>runtime checks that simplify to an <strong>empty</strong> scenario for a clause</li>
</ul>
<p>may unintentionally generate a permissive query filter, potentially returning unauthorized data.</p>
<p><em>Actions primarily affected:</em> reads guarded by filter policies. Non-filter (e.g., hard forbid) policies are not impacted.</p>
<h3 id="technical-details">Technical details</h3>
<p>This patch corrects two behaviors:</p>
<ul>
<li><p><strong><code>Ash.Policy.Policy.compile_policy_expression/1</code></strong> now treats <strong>bypass</strong> blocks as
<code>AND(condition_expression, compiled_policies)</code>
instead of <code>OR(AND(...), NOT(condition_expression))</code>. This removes the permissive <code>NOT(condition)</code> escape hatch when a bypass condition never passes.</p>
</li>
<li><p><strong><code>Ash.Policy.Authorizer</code></strong> now treats <strong>empty SAT scenarios</strong> (<code>scenario == %{}</code>) as <strong><code>false</code></strong>, ensuring impossible scenarios do not collapse into a no-op and inadvertently widen the filter. The reducer also normalizes <code>nil</code> → <code>false</code> consistently when building <code>auto_filter</code> fragments.</p>
</li>
</ul>
<p>Relevant changes are in:</p>
<ul>
<li><code>lib/ash/policy/policy.ex</code> (bypass compilation)</li>
<li><code>lib/ash/policy/authorizer/authorizer.ex</code> (scenario handling / auto_filter normalization)</li>
<li>Tests added: <code>test/policy/filter_condition_test.exs</code> (<code>RuntimeFalsyCheck</code>, <code>RuntimeBypassResource</code>) validate the corrected behavior.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Avoid <code>bypass</code> policies whose conditions are only decidable at runtime and may be perpetually false in some contexts; prefer explicit <code>authorize_if</code>/<code>forbid_if</code> blocks without <code>bypass</code> for those cases.</li>
<li>Add an explicit <strong>final <code>forbid_if always()</code></strong> guard for sensitive reads as a belt-and-suspenders fallback until user can upgrade.</li>
<li>Where feasible, replace runtime-unknown checks with strict/compile-time checks or restructure to avoid empty SAT scenarios.</li>
</ul>
<h3 id="how-to-tell-if-user-is-affected">How to tell if user is affected</h3>
<p>User is likely affected if ALL of the following are true:</p>
<ul>
<li>Uses <strong>filter authorization</strong>; and</li>
<li>Defines <code>bypass</code> block with <code>access_type :runtime</code> without any policies after it; or</li>
<li>Defines <code>bypass</code> blocks whose conditions are evaluated at runtime (e.g., checks with <code>strict_check/3</code> returning <code>:unknown</code> and a runtime <code>check/4</code> that may never succeed in some contexts) without any policies after it</li>
</ul>
<p>A quick sanity test is to issue a read expected to return <strong>no</strong> rows under such a bypass or runtime-falsy condition and verify it indeed returns <code>[]</code>. The included test <code>bypass works with filter policies</code> demonstrates the corrected, non-permissive behavior.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ash-project/ash/security/advisories/GHSA-7r7f-9xpj-jmr7">https://github.com/ash-project/ash/security/advisories/GHSA-7r7f-9xpj-jmr7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-48043">https://nvd.nist.gov/vuln/detail/CVE-2025-48043</a></li>
<li><a href="https://github.com/ash-project/ash/commit/66d81300065b970da0d2f4528354835d2418c7ae">https://github.com/ash-project/ash/commit/66d81300065b970da0d2f4528354835d2418c7ae</a></li>
<li><a href="https://github.com/ash-project/ash/releases/tag/v3.6.2">https://github.com/ash-project/ash/releases/tag/v3.6.2</a></li>
<li><a href="https://github.com/advisories/GHSA-7r7f-9xpj-jmr7">https://github.com/advisories/GHSA-7r7f-9xpj-jmr7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r7f-9xpj-jmr7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-10-13T13:33:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash] Before action, Ash's hooks may execute in certain scenarios despite a request being forbidden]]></title>
        <id>https://github.com/advisories/GHSA-jj4j-x5ww-cwh9</id>
        <link href="https://github.com/advisories/GHSA-jj4j-x5ww-cwh9"/>
        <updated>2025-09-15T16:28:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Certain bulk action calls with a <code>before_transaction</code> hook and no <code>after_transaction</code> hook, will call the <code>before_transaction</code> hook before authorization is checked and a Forbidden error is returned, when called as a bulk action.</p>
<p>The impact is that a malicious user could cause a <code>before_transaction</code> to run even though they are not authorized to perform the whole action. The <code>before_action</code> could run a sensitive/expensive operation.</p>
<h3 id="impact">Impact</h3>
<p>A malicious user could cause a <code>before_action</code> to run even though they are not authorized to perform the whole action.</p>
<p>You are affected if you have an create, update or destroy action that:</p>
<ul>
<li>has a before_transaction hook on it, and no after_transaction hook on it.</li>
<li>is being used via an <code>Ash.bulk_*</code> callback (which AshJsonApi and AshGraphql do for update/destroy actions)</li>
</ul>
<p>Whether or not or how much it matters depends on the nature of those before_transaction callbacks. If those before_transaction callbacks are side-effectful, or just doing something like looking up some external data. If your API endpoints are behind authentication and what kind. </p>
<h3 id="severity">Severity</h3>
<p>The severity for this was hard to gauge. <code>before_transaction</code> hooks are not that commonly used. Additionally, any attacker must know which of these things are available to them, be authenticated to make such a request (i.e you very rarely have policies preventing the running of anonymous queries), so privileges and inside knowledge are required. Additionally, the action will always return a <code>forbidden</code> error, so no information is revealed. We will evaluate and adjust the severity in the next few days as needed.</p>
<p>It is currently marked as High, given that we really don't know what logic folks are putting in their before_transaction hooks and it could theoretically be very bad.</p>
<h3 id="workarounds">Workarounds</h3>
<p>You should update ASAP, but if for whatever reason you cannot update, you can add logic to those before_transaction hooks to prevent them from doing their logic before they should.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ash-project/ash/security/advisories/GHSA-jj4j-x5ww-cwh9">https://github.com/ash-project/ash/security/advisories/GHSA-jj4j-x5ww-cwh9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-48042">https://nvd.nist.gov/vuln/detail/CVE-2025-48042</a></li>
<li><a href="https://github.com/ash-project/ash/commit/5d1b6a5d00771fd468a509778637527b5218be9a">https://github.com/ash-project/ash/commit/5d1b6a5d00771fd468a509778637527b5218be9a</a></li>
<li><a href="https://github.com/advisories/GHSA-jj4j-x5ww-cwh9">https://github.com/advisories/GHSA-jj4j-x5ww-cwh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj4j-x5ww-cwh9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-09-15T16:28:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash_authentication_phoenix] ash_authentication_phoenix has Insufficient Session Expiration]]></title>
        <id>https://github.com/advisories/GHSA-f7gq-h8jv-h3cq</id>
        <link href="https://github.com/advisories/GHSA-f7gq-h8jv-h3cq"/>
        <updated>2025-06-17T19:56:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Session tokens remain valid on the server after user logout, creating a security gap where:</p>
<ul>
<li>Compromised tokens (via XSS, network interception, or device theft) continue to work even after the user logs out<ul>
<li>The sessions stored in the database still expire, limiting the duration during which this could be exploited</li>
</ul>
</li>
<li>Users cannot fully invalidate their sessions when logging out from shared or potentially compromised devices <ul>
<li>by default, changing one's password <em>does</em> invalidate all other sessions, so changing your password as a security measure would have been effective</li>
</ul>
</li>
<li>May cause compliance issues with security frameworks requiring complete session</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Upgrade to version 2.10.0. After upgrading, users must update their AuthController implementation to use the new <code>clear_session/2</code> function with their OTP app name. You will be prompted to do so with a compile-time error.</p>
<p>If you do not have the setting <code>require_token_presence_for_authentication?</code> set to <code>true</code> in the <code>tokens</code> section, you will see a separate error:</p>
<pre><code>** (Spark.Error.DslError) authentication -&gt; session_identifier:
Must set `authentication.session_identifier` to either `:jti` or `:unsafe`.

...
</code></pre>
<p>In order to revoke sessions on log out when not storing tokens directly in the session, we must have some unique identifier with which to do so. You should prefer to enable <code>require_token_presence_for_authentication?</code> if possible, instead of setting this to <code>:jti</code>. Note that whatever you do here, if you did not previously have <code>require_token_presence_for_authentication?</code> set to <code>true</code>, setting it to <code>true</code> <em>or</em> setting <code>authentication.session_identifier</code> to <code>:jti</code> will log out all of your currently authenticated users.</p>
<h3 id="workarounds">Workarounds</h3>
<p>You can manually revoke tokens in your <code>logout/2</code> handler in your auth controller.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/team-alembic/ash_authentication_phoenix/security/advisories/GHSA-f7gq-h8jv-h3cq">https://github.com/team-alembic/ash_authentication_phoenix/security/advisories/GHSA-f7gq-h8jv-h3cq</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication_phoenix/commit/a3253fb4fc7145aeb403537af1c24d3a8d51ffb1">https://github.com/team-alembic/ash_authentication_phoenix/commit/a3253fb4fc7145aeb403537af1c24d3a8d51ffb1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-4754">https://nvd.nist.gov/vuln/detail/CVE-2025-4754</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication_phoenix/pull/634">https://github.com/team-alembic/ash_authentication_phoenix/pull/634</a></li>
<li><a href="https://github.com/advisories/GHSA-f7gq-h8jv-h3cq">https://github.com/advisories/GHSA-f7gq-h8jv-h3cq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f7gq-h8jv-h3cq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-06-17T14:20:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hackney] Hackney fails to properly release HTTP connections to the pool]]></title>
        <id>https://github.com/advisories/GHSA-9fm9-hp7p-53mf</id>
        <link href="https://github.com/advisories/GHSA-9fm9-hp7p-53mf"/>
        <updated>2025-05-28T16:08:48.000Z</updated>
        <content type="html"><![CDATA[<p>Hackney fails to properly release HTTP connections to the pool after handling 307 Temporary Redirect responses. Remote attackers can exploit this to exhaust connection pools, causing denial of service in applications using the library.
Fix for this issue has been included in&nbsp;1.24.0 release.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-3864">https://nvd.nist.gov/vuln/detail/CVE-2025-3864</a></li>
<li><a href="https://github.com/benoitc/hackney/issues/717">https://github.com/benoitc/hackney/issues/717</a></li>
<li><a href="https://cert.pl/en/posts/2025/05/CVE-2025-3864">https://cert.pl/en/posts/2025/05/CVE-2025-3864</a></li>
<li><a href="https://github.com/advisories/GHSA-9fm9-hp7p-53mf">https://github.com/advisories/GHSA-9fm9-hp7p-53mf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9fm9-hp7p-53mf</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-05-28T12:30:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash_authentication] ash_authentication has email link auto-click account confirmation vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-3988-q8q7-p787</id>
        <link href="https://github.com/advisories/GHSA-3988-q8q7-p787"/>
        <updated>2025-04-16T00:40:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The confirmation flow for account creation currently uses a GET request triggered by clicking a link sent via email. Some email clients and security tools (e.g., Outlook, virus scanners, and email previewers) may automatically follow these links, unintentionally confirming the account. This allows an attacker to register an account using another user’s email and potentially have it auto-confirmed by the victim’s email client.</p>
<p>This does not allow attackers to take over or access existing accounts or private data. It is limited to account confirmation of new accounts only.</p>
<h3 id="patches">Patches</h3>
<p>A mitigation has been released in version <code>4.7.0</code>. You will also need to upgrade to <code>2.6.0</code> or later of <code>ash_authentication_phoenix</code> to take advantage of the autogenerated views for confirmation. The fix updates the confirmation flow to require explicit user interaction (such as clicking a button on the confirmation page) rather than performing the confirmation via a GET request. This ensures that automatic link prefetching or scanning by email clients does not unintentionally confirm accounts.</p>
<p>To mitigate, follow these steps:</p>
<ol>
<li>Upgrade <code>ash_authentication</code> &gt;= <code>4.7.0</code></li>
<li>Upgrade <code>ash_authentication_phoenix</code> &gt;= <code>2.6.0</code> (if using <code>ash_authentication_phoenix</code>)</li>
<li>Set <code>require_interaction? true</code> in your confirmation strategy.</li>
<li>Add <code>confirm_route</code> to your router, if using <code>ash_authentication_phoenix</code> <em>above</em> <code>auth_routes</code>.</li>
</ol>
<h4 id="setting-require_interaction-true">Setting <code>require_interaction? true</code></h4>
<p>modify your <code>confirmation</code> strategy like so:</p>
<pre><code class="language-elixir">confirmation &lt;strategy_name&gt; do
  ...
  require_interaction? true
end
</code></pre>
<h4 id="adding-the-confirm_route-to-your-router">Adding the <code>confirm_route</code> to your router</h4>
<p>In order to use this new confirmation flow, you will need to add this to your router to get the desired behavior. It will add a new route to the new confirmation page LiveView. Note the <code>path</code> and <code>token_as_route_param?</code> options, required for keeping backwards compatibility with current defaults. You may need to adjust if you have changed those routes in some way.</p>
<h5 id="important---above-auth_routes">IMPORTANT - above <code>auth_routes</code></h5>
<p>Make sure this goes <em>above</em> <code>auth_routes</code> if you are using the <code>path</code> option, and it begins with <code>/auth</code>,
or whatever your configured <code>auth_routes_prefix</code> is. <code>auth_routes</code> greedily handles all routes at the
configured path.</p>
<pre><code class="language-elixir">confirm_route(
  MyApp.Accounts.User,
  &lt;confirmation_strategy_name&gt;,
  auth_routes_prefix: "/auth",
  overrides: [MyAppWeb.AuthOverrides, AshAuthentication.Phoenix.Overrides.Default],
  # use these options to keep your currently issued confirmation emails compatible
  # without the options below, the route will default to `/&lt;the_strategy_name&gt;/:token`
  path: "/auth/user/&lt;confirmation_strategy_name&gt;",
  token_as_route_param?: false
)
</code></pre>
<p>Users should upgrade to version <code>4.7.0</code> as soon as possible, and set <code>require_interaction?</code> to <code>true</code> in their confirmation strategy. This will change the <code>GET</code> request generated for confirming to a <code>POST</code> request. </p>
<p>If you upgrade to this version and do not set <code>require_interaction?</code> to <code>true</code>, compilation will be fail with a message linking to this advisory. This error can be bypassed if, for example, you are confident that you are not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<p><em>Is there a way for users to fix or remediate the vulnerability without upgrading?</em>
You can disable the confirmation routes and create your own live view. We highly advised that you upgrade and take advantage of the builtin views if possible. If you are not using the provided views, you will need to <em>add</em> a confirmation LiveView, that does a <code>POST</code> to the old confirmation url instead of a <code>GET</code>. You would do this by taking the token a parameter out of the link, and adding it as a hidden field to a form. That form would have no inputs, only a button that posts to the confirmation URL. If you are using Liveview, this would be done with <code>phx-trigger-action</code> and <code>phx-action</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-3988-q8q7-p787">https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-3988-q8q7-p787</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/commit/99ea38977fd4f421d2aaae0c2fb29f8e5f8f707d">https://github.com/team-alembic/ash_authentication/commit/99ea38977fd4f421d2aaae0c2fb29f8e5f8f707d</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-32782">https://nvd.nist.gov/vuln/detail/CVE-2025-32782</a></li>
<li><a href="https://github.com/advisories/GHSA-3988-q8q7-p787">https://github.com/advisories/GHSA-3988-q8q7-p787</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3988-q8q7-p787</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-04-14T23:00:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hackney] Server-side Request Forgery (SSRF) in hackney]]></title>
        <id>https://github.com/advisories/GHSA-vq52-99r9-h5pw</id>
        <link href="https://github.com/advisories/GHSA-vq52-99r9-h5pw"/>
        <updated>2025-02-20T16:41:46.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of the package hackney from 0.0.0 are vulnerable to Server-side Request Forgery (SSRF) due to improper parsing of URLs by URI built-in module and hackey. Given the URL <a href="http://127.0.0.1?@127.2.2.2/">http://127.0.0.1?@127.2.2.2/</a>, the URI function will parse and see the host as 127.0.0.1 (which is correct), and hackney will refer the host as 127.2.2.2/. This vulnerability can be exploited when users rely on the URL function for host checking.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-1211">https://nvd.nist.gov/vuln/detail/CVE-2025-1211</a></li>
<li><a href="https://gist.github.com/snoopysecurity/996de09ec0cfd0ebdcfdda8ff515deb1">https://gist.github.com/snoopysecurity/996de09ec0cfd0ebdcfdda8ff515deb1</a></li>
<li><a href="https://security.snyk.io/vuln/SNYK-HEX-HACKNEY-6516131">https://security.snyk.io/vuln/SNYK-HEX-HACKNEY-6516131</a></li>
<li><a href="https://www.blackhat.com/docs/us-17/thursday/us-17-Tsai-A-New-Era-Of-SSRF-Exploiting-URL-Parser-In-Trending-Programming-Languages.pdf">https://www.blackhat.com/docs/us-17/thursday/us-17-Tsai-A-New-Era-Of-SSRF-Exploiting-URL-Parser-In-Trending-Programming-Languages.pdf</a></li>
<li><a href="https://github.com/benoitc/hackney/commit/9594ce58fabd32cd897fc28fae937694515a3d4a">https://github.com/benoitc/hackney/commit/9594ce58fabd32cd897fc28fae937694515a3d4a</a></li>
<li><a href="https://github.com/benoitc/hackney/releases/tag/1.21.0">https://github.com/benoitc/hackney/releases/tag/1.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vq52-99r9-h5pw">https://github.com/advisories/GHSA-vq52-99r9-h5pw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vq52-99r9-h5pw</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-02-11T06:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash_authentication] Ash Authentication has flawed token revocation checking logic in actions generated by `mix ash_authentication.install`]]></title>
        <id>https://github.com/advisories/GHSA-qrm9-f75w-hg4c</id>
        <link href="https://github.com/advisories/GHSA-qrm9-f75w-hg4c"/>
        <updated>2025-02-11T21:40:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Applications which have been bootstrapped by the new igniter installer (since AshAuthentication v4.1.0) and who have used the magic link strategy, password resets, confirmation, or are manually revoking tokens are affected by revoked tokens being allowed to verify as valid. If you did not use the new installer, then you are absolutely not affected.</p>
<p>Additionally, unless you have implemented any kind of custom token revocation feature in your application (in which case even cursory testing would have uncovered this issue), then you will not be significantly affected. </p>
<p>The impact here for users is as follows:</p>
<ul>
<li>For users using the magic link strategy, magic link tokens are reusable until they expire instead of being immediately revoked. By default magic link tokens are valid for 10 minutes.</li>
<li>For users of password resets in the password strategy, password reset tokens are reusable until they expire instead of being immediately revoked. By default password reset tokens are valid for 3 days.</li>
<li>For users of the confirmation add-on, confirmation tokens are reusable until they expire instead of being immediately revoked. By default password reset tokens are valid for 3 days.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>The flaw is patched in version 4.4.9. Additionally a compile time warning is shown to users with remediation instructions if they upgrade. 4.4.9 ships with an upgrader, so if you use <code>mix igniter.upgrade ash_authentication</code> the necessary patch will be applied for you. Otherwise you can run the upgrader manually as described in the error message</p>
<h4 id="example">Example</h4>
<pre><code class="language-elixir">[warning] Warning while compiling Tunez.Accounts.Token:

The `:jti` and `:token` options to the `:revoked?` action must allow nil values and it must return a `:boolean`.

This was an error in our igniter installer previous to version 4.4.9, which allowed revoked tokens to be reused.

To fix this, run the following command in your shell:

    mix ash_authentication.upgrade 4.4.8 4.4.9

Or:

  - remove `allow_nil?: false` from these action arguments, and
  - ensure that the action returns `:boolean`.

  like so:

    action :revoked?, :boolean do
      description "Returns true if a revocation token is found for the provided token"
      argument :token, :string, sensitive?: true
      argument :jti, :string, sensitive?: true

      run AshAuthentication.TokenResource.IsRevoked
    end
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>Delete the generated <code>:revoked?</code> generic action in your token resource This will cause it to use the one internal to AshAuthentication which has always been correct. Alternatively,  manually make the changes described above.</p>
<h3 id="references">References</h3>
<p>See the <code>#ash_authentication</code> channel on the Ash Discord.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-qrm9-f75w-hg4c">https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-qrm9-f75w-hg4c</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/commit/2dee55252df26fe3d990ff1199397cdcf1bfea8a">https://github.com/team-alembic/ash_authentication/commit/2dee55252df26fe3d990ff1199397cdcf1bfea8a</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-25202">https://nvd.nist.gov/vuln/detail/CVE-2025-25202</a></li>
<li><a href="https://github.com/advisories/GHSA-qrm9-f75w-hg4c">https://github.com/advisories/GHSA-qrm9-f75w-hg4c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrm9-f75w-hg4c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-02-11T18:12:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rabbit_common] RabbitMQ HTTP API's queue deletion endpoint does not verify that the user has a required permission]]></title>
        <id>https://github.com/advisories/GHSA-pj33-75x5-32j4</id>
        <link href="https://github.com/advisories/GHSA-pj33-75x5-32j4"/>
        <updated>2024-11-07T05:09:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Queue deletion via the HTTP API was not verifying the <code>configure</code> permission of the user.</p>
<h3 id="impact">Impact</h3>
<p>Users who had all of the following:</p>
<ol>
<li>Valid credentials</li>
<li>Some permissions for the target virtual host</li>
<li>HTTP API access</li>
</ol>
<p>could delete queues it had no (deletion) permissions for.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Disable management plugin and use, for example, <a href="https://www.rabbitmq.com/docs/prometheus">Prometheus and Grafana</a> for monitoring.</p>
<h3 id="owasp-classification">OWASP Classification</h3>
<p>OWASP Top10 A01:2021 – Broken Access Control</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rabbitmq/rabbitmq-server/security/advisories/GHSA-pj33-75x5-32j4">https://github.com/rabbitmq/rabbitmq-server/security/advisories/GHSA-pj33-75x5-32j4</a></li>
<li><a href="https://www.rabbitmq.com/docs/prometheus">https://www.rabbitmq.com/docs/prometheus</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-51988">https://nvd.nist.gov/vuln/detail/CVE-2024-51988</a></li>
<li><a href="https://github.com/advisories/GHSA-pj33-75x5-32j4">https://github.com/advisories/GHSA-pj33-75x5-32j4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pj33-75x5-32j4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2024-11-06T19:55:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash_postgres] In AshPostgres, empty, atomic, non-bulk actions, policy bypass for side-effects vulnerability.]]></title>
        <id>https://github.com/advisories/GHSA-hf59-7rwq-785m</id>
        <link href="https://github.com/advisories/GHSA-hf59-7rwq-785m"/>
        <updated>2025-04-14T22:10:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><em>What kind of vulnerability is it? Who is impacted?</em></p>
<p>In certain <em>very specific</em> situations, it was possible for the policies of an update action to be skipped. This occurred only on "empty" update actions (no changing fields), and would allow their hooks (side effects) to be performed when they should not have been. Note that this does not allow reading new data that the user should not have had access to, only triggering a side effect a user should not have been able to trigger.</p>
<p>You must have an update action that:</p>
<ul>
<li>Is on a resource with no attributes containing an "update default" (updated_at timestamp, for example)</li>
<li>can be performed atomically. </li>
<li>Does <em>not</em> have <code>require_atomic? false</code></li>
<li>Has at least one authorizer (typically <code>Ash.Policy.Authorizer</code>)</li>
<li>Has at least one <code>change</code> (on the resource's <code>changes</code> block or in the action itself)
This is where the side-effects would be performed when they should not have been.</li>
</ul>
<hr>
<ul>
<li>Is there ever a place where you call this action manually, using <code>Ash.update</code>. 
 Note that AshGraphql and AshJsonApi action calls are <em>not</em> affected as they use <code>Ash.bulk_update</code>.   </li>
<li>If so, is there ever a case where you call the action with zero inputs, and have it produce zero changing fields.</li>
<li>If so, could it then produce a side effect. This means you'd have an after_action hook that calls some other resource.</li>
<li>If so, does that side effect bypass another resource's policies, i.e using <code>authorize?: false</code>, or not providing the same actor.</li>
</ul>
<p>Everything above the line can be checked with the provided script. Everything below it, must be checked manually. The script for checking this is available in the "Might I be affected" section. </p>
<p><strong>The script can have false <em>positives</em>, but will not have any false <em>negatives</em>. So if you run the script, and it says "No potential vulnerabilities found", then all you need to do is update ash_postgres.</strong> </p>
<h3 id="patches">Patches</h3>
<p>This problem has been patched in <code>2.4.10</code> of <code>ash_postgres</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>You could:</p>
<ol>
<li>Determine that none of your actions are vulnerable using the script.</li>
<li>Add <code>require_atomic? false</code> to any potentially affected update action</li>
<li>Replace any usage of <code>Ash.update</code> with <code>Ash.bulk_update</code> for an affected action</li>
<li>add an update timestamp to your action.</li>
</ol>
<h3 id="might-i-be-affected">Might I be affected</h3>
<p>This gist provides a script you can run to detect if you are potentially vulnerable.</p>
<p><a href="https://gist.github.com/zachdaniel/e49166b765978c48dfaf998d06df436e">https://gist.github.com/zachdaniel/e49166b765978c48dfaf998d06df436e</a></p>
<h3 id="references">References</h3>
<p>Original Report/discovery: <a href="https://elixirforum.com/t/empty-update-action-with-policies/66954">https://elixirforum.com/t/empty-update-action-with-policies/66954</a>
Fix commit: <a href="https://github.com/ash-project/ash_postgres/commit/1228fcd851f29a68609e236f7d6a2622a4b5c4ba">https://github.com/ash-project/ash_postgres/commit/1228fcd851f29a68609e236f7d6a2622a4b5c4ba</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/ash-project/ash_postgres/security/advisories/GHSA-hf59-7rwq-785m">https://github.com/ash-project/ash_postgres/security/advisories/GHSA-hf59-7rwq-785m</a></li>
<li><a href="https://github.com/ash-project/ash_postgres/commit/1228fcd851f29a68609e236f7d6a2622a4b5c4ba">https://github.com/ash-project/ash_postgres/commit/1228fcd851f29a68609e236f7d6a2622a4b5c4ba</a></li>
<li><a href="https://elixirforum.com/t/empty-update-action-with-policies/66954">https://elixirforum.com/t/empty-update-action-with-policies/66954</a></li>
<li><a href="https://gist.github.com/zachdaniel/e49166b765978c48dfaf998d06df436e">https://gist.github.com/zachdaniel/e49166b765978c48dfaf998d06df436e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-49756">https://nvd.nist.gov/vuln/detail/CVE-2024-49756</a></li>
<li><a href="https://github.com/advisories/GHSA-hf59-7rwq-785m">https://github.com/advisories/GHSA-hf59-7rwq-785m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hf59-7rwq-785m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2024-10-23T17:22:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hex_core] Hex authenticity of signed packages not validated]]></title>
        <id>https://github.com/advisories/GHSA-q3cc-rr2c-87r6</id>
        <link href="https://github.com/advisories/GHSA-q3cc-rr2c-87r6"/>
        <updated>2024-05-02T12:18:23.000Z</updated>
        <content type="html"><![CDATA[<p>Hex package manager hex_core version 0.3.0 and earlier contains a Signing oracle vulnerability in Package registry verification that can result in Package modifications not detected, allowing code execution. This attack appears to be exploitable via victim fetches packages from malicious/compromised mirror. This vulnerability appears to have been fixed in 0.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2019-1000013">https://nvd.nist.gov/vuln/detail/CVE-2019-1000013</a></li>
<li><a href="https://github.com/hexpm/hex_core/pull/48">https://github.com/hexpm/hex_core/pull/48</a></li>
<li><a href="https://github.com/hexpm/hex_core/pull/51">https://github.com/hexpm/hex_core/pull/51</a></li>
<li><a href="https://github.com/advisories/GHSA-q3cc-rr2c-87r6">https://github.com/advisories/GHSA-q3cc-rr2c-87r6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3cc-rr2c-87r6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2022-05-13T01:07:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jose] erlang-jose vulnerable to denial of service via large p2c value]]></title>
        <id>https://github.com/advisories/GHSA-9mg4-v392-8j68</id>
        <link href="https://github.com/advisories/GHSA-9mg4-v392-8j68"/>
        <updated>2024-04-10T21:19:23.000Z</updated>
        <content type="html"><![CDATA[<p>erlang-jose (aka JOSE for Erlang and Elixir) through 1.11.6 allow attackers to cause a denial of service (CPU consumption) via a large p2c (aka PBES2 Count) value in a JOSE header.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-50966">https://nvd.nist.gov/vuln/detail/CVE-2023-50966</a></li>
<li><a href="https://github.com/P3ngu1nW/CVE_Request/blob/main/erlang-jose.md">https://github.com/P3ngu1nW/CVE_Request/blob/main/erlang-jose.md</a></li>
<li><a href="https://github.com/potatosalad/erlang-jose">https://github.com/potatosalad/erlang-jose</a></li>
<li><a href="https://hexdocs.pm/jose/JOSE.html">https://hexdocs.pm/jose/JOSE.html</a></li>
<li><a href="https://github.com/potatosalad/erlang-jose/commit/718d213f07b08056737923f8063d5df56dcb66ae">https://github.com/potatosalad/erlang-jose/commit/718d213f07b08056737923f8063d5df56dcb66ae</a></li>
<li><a href="https://github.com/advisories/GHSA-9mg4-v392-8j68">https://github.com/advisories/GHSA-9mg4-v392-8j68</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9mg4-v392-8j68</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2024-03-19T15:30:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix before 1.6.14 mishandles check_origin wildcarding]]></title>
        <id>https://github.com/advisories/GHSA-p8f7-22gq-m7j9</id>
        <link href="https://github.com/advisories/GHSA-p8f7-22gq-m7j9"/>
        <updated>2025-05-12T12:46:05.000Z</updated>
        <content type="html"><![CDATA[<p>socket/transport.ex in Phoenix before 1.6.14 mishandles check_origin wildcarding. NOTE: LiveView applications are unaffected by default because of the presence of a LiveView CSRF token.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2022-42975">https://nvd.nist.gov/vuln/detail/CVE-2022-42975</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/6e7185b33a59e0b1d1c0b4223adf340a73e963ae">https://github.com/phoenixframework/phoenix/commit/6e7185b33a59e0b1d1c0b4223adf340a73e963ae</a></li>
<li><a href="https://hexdocs.pm/phoenix/1.6.14/changelog.html#1-6-14-2022-10-10">https://hexdocs.pm/phoenix/1.6.14/changelog.html#1-6-14-2022-10-10</a></li>
<li><a href="https://github.com/advisories/GHSA-p8f7-22gq-m7j9">https://github.com/advisories/GHSA-p8f7-22gq-m7j9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p8f7-22gq-m7j9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2022-10-17T12:00:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oidcc] OpenID Connect client Atom Exhaustion in provider configuration worker ets table location]]></title>
        <id>https://github.com/advisories/GHSA-mj35-2rgf-cv8p</id>
        <link href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p"/>
        <updated>2025-04-04T01:10:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>DOS by Atom exhaustion is possible by calling <code>oidcc_provider_configuration_worker:get_provider_configuration/1</code> or <code>oidcc_provider_configuration_worker:get_jwks/1</code>.</p>
<p>Since the name is usually provided as a static value in the application using <code>oidcc</code>, this is unlikely to be exploited.</p>
<h3 id="details">Details</h3>
<p>Example to illustrate the vulnerability.</p>
<pre><code class="language-erlang">{ok, Claims} =
  oidcc:retrieve_userinfo(
    Token,
    myapp_oidcc_config_provider,
    &lt;&lt;"client_id"&gt;&gt;,
    &lt;&lt;"client_secret"&gt;&gt;,
    #{}
  )
</code></pre>
<p>The vulnerability is present in <code>oidcc_provider_configuration_worker:get_ets_table_name/1</code>.
The function <code>get_ets_table_name</code> is calling <code>erlang:list_to_atom/1</code>.</p>
<p><a href="https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388">https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388</a></p>
<p>There might be a case (Very highly improbable) where the 2nd argument of
<code>oidcc_provider_configuration_worker:get_*/1</code> is called with a different atom each time which eventually leads to
the atom table filling up and the node crashing.</p>
<h3 id="patches">Patches</h3>
<p>Patched in <code>3.0.2</code>, <code>3.1.2</code> &amp; <code>3.2.0-beta.3</code></p>
<h3 id="workarounds">Workarounds</h3>
<p>Make sure only valid provider configuration worker names are passed to the functions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html">https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2024-31209">https://www.cve.org/CVERecord?id=CVE-2024-31209</a></li>
<li><a href="https://euvd.enisa.europa.eu/enisa/EUVD-2024-1249">https://euvd.enisa.europa.eu/enisa/EUVD-2024-1249</a></li>
<li><a href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/advisories/GHSA-mj35-2rgf-cv8p</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/erlef/oidcc/security/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/erlef/oidcc/security/advisories/GHSA-mj35-2rgf-cv8p</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/2f304d877c7e0613d6fd952d7feacbf40dbc355c">https://github.com/erlef/oidcc/commit/2f304d877c7e0613d6fd952d7feacbf40dbc355c</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/48171fb62688fb4eec1ead0884aa501e0aa68649">https://github.com/erlef/oidcc/commit/48171fb62688fb4eec1ead0884aa501e0aa68649</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/ac458ed88dc292aad6fa7343f6a53e73c560fb1a">https://github.com/erlef/oidcc/commit/ac458ed88dc292aad6fa7343f6a53e73c560fb1a</a></li>
<li><a href="https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html">https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html</a></li>
<li><a href="https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388">https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-31209">https://nvd.nist.gov/vuln/detail/CVE-2024-31209</a></li>
<li><a href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/advisories/GHSA-mj35-2rgf-cv8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mj35-2rgf-cv8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2024-04-03T16:46:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oidcc] OpenID Connect client Atom Exhaustion in provider configuration worker ets table location]]></title>
        <id>https://github.com/advisories/GHSA-mj35-2rgf-cv8p</id>
        <link href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p"/>
        <updated>2025-04-04T01:10:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>DOS by Atom exhaustion is possible by calling <code>oidcc_provider_configuration_worker:get_provider_configuration/1</code> or <code>oidcc_provider_configuration_worker:get_jwks/1</code>.</p>
<p>Since the name is usually provided as a static value in the application using <code>oidcc</code>, this is unlikely to be exploited.</p>
<h3 id="details">Details</h3>
<p>Example to illustrate the vulnerability.</p>
<pre><code class="language-erlang">{ok, Claims} =
  oidcc:retrieve_userinfo(
    Token,
    myapp_oidcc_config_provider,
    &lt;&lt;"client_id"&gt;&gt;,
    &lt;&lt;"client_secret"&gt;&gt;,
    #{}
  )
</code></pre>
<p>The vulnerability is present in <code>oidcc_provider_configuration_worker:get_ets_table_name/1</code>.
The function <code>get_ets_table_name</code> is calling <code>erlang:list_to_atom/1</code>.</p>
<p><a href="https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388">https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388</a></p>
<p>There might be a case (Very highly improbable) where the 2nd argument of
<code>oidcc_provider_configuration_worker:get_*/1</code> is called with a different atom each time which eventually leads to
the atom table filling up and the node crashing.</p>
<h3 id="patches">Patches</h3>
<p>Patched in <code>3.0.2</code>, <code>3.1.2</code> &amp; <code>3.2.0-beta.3</code></p>
<h3 id="workarounds">Workarounds</h3>
<p>Make sure only valid provider configuration worker names are passed to the functions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html">https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2024-31209">https://www.cve.org/CVERecord?id=CVE-2024-31209</a></li>
<li><a href="https://euvd.enisa.europa.eu/enisa/EUVD-2024-1249">https://euvd.enisa.europa.eu/enisa/EUVD-2024-1249</a></li>
<li><a href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/advisories/GHSA-mj35-2rgf-cv8p</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/erlef/oidcc/security/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/erlef/oidcc/security/advisories/GHSA-mj35-2rgf-cv8p</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/2f304d877c7e0613d6fd952d7feacbf40dbc355c">https://github.com/erlef/oidcc/commit/2f304d877c7e0613d6fd952d7feacbf40dbc355c</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/48171fb62688fb4eec1ead0884aa501e0aa68649">https://github.com/erlef/oidcc/commit/48171fb62688fb4eec1ead0884aa501e0aa68649</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/ac458ed88dc292aad6fa7343f6a53e73c560fb1a">https://github.com/erlef/oidcc/commit/ac458ed88dc292aad6fa7343f6a53e73c560fb1a</a></li>
<li><a href="https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html">https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html</a></li>
<li><a href="https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388">https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-31209">https://nvd.nist.gov/vuln/detail/CVE-2024-31209</a></li>
<li><a href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/advisories/GHSA-mj35-2rgf-cv8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mj35-2rgf-cv8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2024-04-03T16:46:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oidcc] OpenID Connect client Atom Exhaustion in provider configuration worker ets table location]]></title>
        <id>https://github.com/advisories/GHSA-mj35-2rgf-cv8p</id>
        <link href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p"/>
        <updated>2025-04-04T01:10:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>DOS by Atom exhaustion is possible by calling <code>oidcc_provider_configuration_worker:get_provider_configuration/1</code> or <code>oidcc_provider_configuration_worker:get_jwks/1</code>.</p>
<p>Since the name is usually provided as a static value in the application using <code>oidcc</code>, this is unlikely to be exploited.</p>
<h3 id="details">Details</h3>
<p>Example to illustrate the vulnerability.</p>
<pre><code class="language-erlang">{ok, Claims} =
  oidcc:retrieve_userinfo(
    Token,
    myapp_oidcc_config_provider,
    &lt;&lt;"client_id"&gt;&gt;,
    &lt;&lt;"client_secret"&gt;&gt;,
    #{}
  )
</code></pre>
<p>The vulnerability is present in <code>oidcc_provider_configuration_worker:get_ets_table_name/1</code>.
The function <code>get_ets_table_name</code> is calling <code>erlang:list_to_atom/1</code>.</p>
<p><a href="https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388">https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388</a></p>
<p>There might be a case (Very highly improbable) where the 2nd argument of
<code>oidcc_provider_configuration_worker:get_*/1</code> is called with a different atom each time which eventually leads to
the atom table filling up and the node crashing.</p>
<h3 id="patches">Patches</h3>
<p>Patched in <code>3.0.2</code>, <code>3.1.2</code> &amp; <code>3.2.0-beta.3</code></p>
<h3 id="workarounds">Workarounds</h3>
<p>Make sure only valid provider configuration worker names are passed to the functions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html">https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2024-31209">https://www.cve.org/CVERecord?id=CVE-2024-31209</a></li>
<li><a href="https://euvd.enisa.europa.eu/enisa/EUVD-2024-1249">https://euvd.enisa.europa.eu/enisa/EUVD-2024-1249</a></li>
<li><a href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/advisories/GHSA-mj35-2rgf-cv8p</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/erlef/oidcc/security/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/erlef/oidcc/security/advisories/GHSA-mj35-2rgf-cv8p</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/2f304d877c7e0613d6fd952d7feacbf40dbc355c">https://github.com/erlef/oidcc/commit/2f304d877c7e0613d6fd952d7feacbf40dbc355c</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/48171fb62688fb4eec1ead0884aa501e0aa68649">https://github.com/erlef/oidcc/commit/48171fb62688fb4eec1ead0884aa501e0aa68649</a></li>
<li><a href="https://github.com/erlef/oidcc/commit/ac458ed88dc292aad6fa7343f6a53e73c560fb1a">https://github.com/erlef/oidcc/commit/ac458ed88dc292aad6fa7343f6a53e73c560fb1a</a></li>
<li><a href="https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html">https://erlef.github.io/security-wg/secure_coding_and_deployment_hardening/atom_exhaustion.html</a></li>
<li><a href="https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388">https://github.com/erlef/oidcc/blob/018dbb53dd752cb1e331637d8e0e6a489ba1fae9/src/oidcc_provider_configuration_worker.erl#L385-L388</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-31209">https://nvd.nist.gov/vuln/detail/CVE-2024-31209</a></li>
<li><a href="https://github.com/advisories/GHSA-mj35-2rgf-cv8p">https://github.com/advisories/GHSA-mj35-2rgf-cv8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mj35-2rgf-cv8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2024-04-03T16:46:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Samly] Samly access control vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-h3rw-77w7-92gf</id>
        <link href="https://github.com/advisories/GHSA-h3rw-77w7-92gf"/>
        <updated>2024-10-21T21:40:48.000Z</updated>
        <content type="html"><![CDATA[<p>In the Samly package before 1.4.0 for Elixir, <code>Samly.State.Store.get_assertion/3</code> can return an expired session, which interferes with access control because Samly.AuthHandler uses a cached session and does not replace it, even after expiry.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-25718">https://nvd.nist.gov/vuln/detail/CVE-2024-25718</a></li>
<li><a href="https://github.com/dropbox/samly/pull/13">https://github.com/dropbox/samly/pull/13</a></li>
<li><a href="https://github.com/dropbox/samly/pull/13/commits/812b5c3ad076dc9c9334c1a560c8e6470607d1eb">https://github.com/dropbox/samly/pull/13/commits/812b5c3ad076dc9c9334c1a560c8e6470607d1eb</a></li>
<li><a href="https://diff.hex.pm/diff/samly/1.3.0..1.4.0">https://diff.hex.pm/diff/samly/1.3.0..1.4.0</a></li>
<li><a href="https://github.com/dropbox/samly">https://github.com/dropbox/samly</a></li>
<li><a href="https://github.com/handnot2/samly">https://github.com/handnot2/samly</a></li>
<li><a href="https://hex.pm/packages/samly">https://hex.pm/packages/samly</a></li>
<li><a href="https://github.com/dropbox/samly/commit/7637ebeef6c6b88ec2032f5323c32edcebbacbc6">https://github.com/dropbox/samly/commit/7637ebeef6c6b88ec2032f5323c32edcebbacbc6</a></li>
<li><a href="https://github.com/advisories/GHSA-h3rw-77w7-92gf">https://github.com/advisories/GHSA-h3rw-77w7-92gf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h3rw-77w7-92gf</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2024-02-11T06:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ejabberd] Ejabberd DoS via malformed stanza]]></title>
        <id>https://github.com/advisories/GHSA-2h3q-v47h-f4rc</id>
        <link href="https://github.com/advisories/GHSA-2h3q-v47h-f4rc"/>
        <updated>2024-01-19T19:14:35.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>mod_pubsub</code> module (mod_pubsub.erl) in ejabberd 2.1.8 and 3.0.0-alpha-3 allows remote authenticated users to cause a denial of service (infinite loop) via a stanza with a publish tag that lacks a node attribute.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2011-4320">https://nvd.nist.gov/vuln/detail/CVE-2011-4320</a></li>
<li><a href="https://support.process-one.net/browse/EJAB-1498">https://support.process-one.net/browse/EJAB-1498</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2011/11/19/1">http://www.openwall.com/lists/oss-security/2011/11/19/1</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2011/11/19/2">http://www.openwall.com/lists/oss-security/2011/11/19/2</a></li>
<li><a href="http://www.process-one.net/en/ejabberd/release_notes/release_note_ejabberd_2.1.9">http://www.process-one.net/en/ejabberd/release_notes/release_note_ejabberd_2.1.9</a></li>
<li><a href="https://github.com/processone/ejabberd/commit/d3c4eab46f3cd54f7686cfed740d9c130b6801cf">https://github.com/processone/ejabberd/commit/d3c4eab46f3cd54f7686cfed740d9c130b6801cf</a></li>
<li><a href="https://github.com/processone/ejabberd/commit/d5b4d6785879f0a5192c26f5b5e218aec8104798">https://github.com/processone/ejabberd/commit/d5b4d6785879f0a5192c26f5b5e218aec8104798</a></li>
<li><a href="https://github.com/advisories/GHSA-2h3q-v47h-f4rc">https://github.com/advisories/GHSA-2h3q-v47h-f4rc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2h3q-v47h-f4rc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2022-05-17T05:32:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ejabberd] Ejabberd DoS via malformed stanza]]></title>
        <id>https://github.com/advisories/GHSA-2h3q-v47h-f4rc</id>
        <link href="https://github.com/advisories/GHSA-2h3q-v47h-f4rc"/>
        <updated>2024-01-19T19:14:35.000Z</updated>
        <content type="html"><![CDATA[<p>The <code>mod_pubsub</code> module (mod_pubsub.erl) in ejabberd 2.1.8 and 3.0.0-alpha-3 allows remote authenticated users to cause a denial of service (infinite loop) via a stanza with a publish tag that lacks a node attribute.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2011-4320">https://nvd.nist.gov/vuln/detail/CVE-2011-4320</a></li>
<li><a href="https://support.process-one.net/browse/EJAB-1498">https://support.process-one.net/browse/EJAB-1498</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2011/11/19/1">http://www.openwall.com/lists/oss-security/2011/11/19/1</a></li>
<li><a href="http://www.openwall.com/lists/oss-security/2011/11/19/2">http://www.openwall.com/lists/oss-security/2011/11/19/2</a></li>
<li><a href="http://www.process-one.net/en/ejabberd/release_notes/release_note_ejabberd_2.1.9">http://www.process-one.net/en/ejabberd/release_notes/release_note_ejabberd_2.1.9</a></li>
<li><a href="https://github.com/processone/ejabberd/commit/d3c4eab46f3cd54f7686cfed740d9c130b6801cf">https://github.com/processone/ejabberd/commit/d3c4eab46f3cd54f7686cfed740d9c130b6801cf</a></li>
<li><a href="https://github.com/processone/ejabberd/commit/d5b4d6785879f0a5192c26f5b5e218aec8104798">https://github.com/processone/ejabberd/commit/d5b4d6785879f0a5192c26f5b5e218aec8104798</a></li>
<li><a href="https://github.com/advisories/GHSA-2h3q-v47h-f4rc">https://github.com/advisories/GHSA-2h3q-v47h-f4rc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2h3q-v47h-f4rc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2022-05-17T05:32:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pleroma] Pleroma Path Traversal vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-2c28-m2m7-mf55</id>
        <link href="https://github.com/advisories/GHSA-2c28-m2m7-mf55"/>
        <updated>2024-03-01T14:35:05.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in kphrx pleroma. It has been classified as problematic. This affects the function <code>Pleroma.Emoji.Pack</code> of the file <code>lib/pleroma/emoji/pack.ex</code>. The manipulation of the argument name leads to path traversal. The complexity of an attack is rather high. The exploitability is told to be difficult. This product does not use versioning. This is why information about affected and unaffected releases are unavailable. The patch is named 2c795094535537a8607cc0d3b7f076a609636f40. It is recommended to apply a patch to fix this issue. The associated identifier of this vulnerability is VDB-242187.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-5588">https://nvd.nist.gov/vuln/detail/CVE-2023-5588</a></li>
<li><a href="https://github.com/kphrx/pleroma/pull/197">https://github.com/kphrx/pleroma/pull/197</a></li>
<li><a href="https://github.com/kphrx/pleroma/commit/2c795094535537a8607cc0d3b7f076a609636f40">https://github.com/kphrx/pleroma/commit/2c795094535537a8607cc0d3b7f076a609636f40</a></li>
<li><a href="https://vuldb.com/?ctiid.242187">https://vuldb.com/?ctiid.242187</a></li>
<li><a href="https://vuldb.com/?id.242187">https://vuldb.com/?id.242187</a></li>
<li><a href="https://github.com/kphrx/pleroma/commits/v2.5.3">https://github.com/kphrx/pleroma/commits/v2.5.3</a></li>
<li><a href="https://github.com/advisories/GHSA-2c28-m2m7-mf55">https://github.com/advisories/GHSA-2c28-m2m7-mf55</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2c28-m2m7-mf55</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2023-10-16T00:30:26.000Z</published>
    </entry>
</feed>