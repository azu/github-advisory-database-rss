<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/nuget.rss</id>
    <title>Security Advisory for .NET packages hosted at the NuGet Gallery</title>
    <updated>2026-01-27T00:06:26.175Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anuget"/>
    <subtitle>Security Advisory for .NET packages hosted at the NuGet Gallery on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[Umbraco.Forms] Umbraco.Forms CDN may cache sensitive form uploads when processed by ImageSharp]]></title>
        <id>https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</id>
        <link href="https://github.com/advisories/GHSA-7jxj-rpx7-ph2c"/>
        <updated>2026-01-22T18:06:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Protected files uploaded through Umbraco Forms may be served to unauthenticated users when a CDN or caching layer is present and ImageSharp processes the request. ImageSharp sets aggressive cache headers by default, which can cause intermediary caches to store and serve files that should require authentication.</p>
<h3 id="patches">Patches</h3>
<p>This issue affects all (supported) versions Umbraco Forms and is patched in 13.9.0, 16.4.0 and 17.1.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Add middleware to set cache headers for form uploads. Place the following code in your <code>Startup.cs</code> or <code>Program.cs</code> after <code>app.UseStaticFiles()</code> and any image processing middleware:</p>
<pre><code class="language-cs">app.Use(async (context, next) =&gt;
{
    var path = context.Request.Path.Value;

    if (!string.IsNullOrEmpty(path) &amp;&amp; path.StartsWith("/media/forms/upload/", StringComparison.OrdinalIgnoreCase))
    {
        context.Response.OnStarting(() =&gt;
        {
            context.Response.Headers["Cache-Control"] = "private, no-store, no-cache, must-revalidate";
            context.Response.Headers["Pragma"] = "no-cache";
            context.Response.Headers["Expires"] = "0";
            return Task.CompletedTask;
        });
    }

    await next();
});
</code></pre>
<p>Alternatively, configure your CDN to bypass caching for URLs matching <code>/media/forms/upload/*</code>.</p>
<p>  Note: The vulnerability requires:</p>
<ul>
<li>A CDN in front of the website</li>
<li>An authenticated user having previously requested the image</li>
<li>Knowledge of the form GUID, entry GUID, and image filename</li>
</ul>
<p>  If no CDN is in use, this vulnerability does not apply.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/umbraco/Umbraco.Forms.Issues/security/advisories/GHSA-7jxj-rpx7-ph2c">https://github.com/umbraco/Umbraco.Forms.Issues/security/advisories/GHSA-7jxj-rpx7-ph2c</a></li>
<li><a href="https://github.com/advisories/GHSA-7jxj-rpx7-ph2c">https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-01-22T18:06:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Umbraco.Forms] Umbraco.Forms CDN may cache sensitive form uploads when processed by ImageSharp]]></title>
        <id>https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</id>
        <link href="https://github.com/advisories/GHSA-7jxj-rpx7-ph2c"/>
        <updated>2026-01-22T18:06:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Protected files uploaded through Umbraco Forms may be served to unauthenticated users when a CDN or caching layer is present and ImageSharp processes the request. ImageSharp sets aggressive cache headers by default, which can cause intermediary caches to store and serve files that should require authentication.</p>
<h3 id="patches">Patches</h3>
<p>This issue affects all (supported) versions Umbraco Forms and is patched in 13.9.0, 16.4.0 and 17.1.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Add middleware to set cache headers for form uploads. Place the following code in your <code>Startup.cs</code> or <code>Program.cs</code> after <code>app.UseStaticFiles()</code> and any image processing middleware:</p>
<pre><code class="language-cs">app.Use(async (context, next) =&gt;
{
    var path = context.Request.Path.Value;

    if (!string.IsNullOrEmpty(path) &amp;&amp; path.StartsWith("/media/forms/upload/", StringComparison.OrdinalIgnoreCase))
    {
        context.Response.OnStarting(() =&gt;
        {
            context.Response.Headers["Cache-Control"] = "private, no-store, no-cache, must-revalidate";
            context.Response.Headers["Pragma"] = "no-cache";
            context.Response.Headers["Expires"] = "0";
            return Task.CompletedTask;
        });
    }

    await next();
});
</code></pre>
<p>Alternatively, configure your CDN to bypass caching for URLs matching <code>/media/forms/upload/*</code>.</p>
<p>  Note: The vulnerability requires:</p>
<ul>
<li>A CDN in front of the website</li>
<li>An authenticated user having previously requested the image</li>
<li>Knowledge of the form GUID, entry GUID, and image filename</li>
</ul>
<p>  If no CDN is in use, this vulnerability does not apply.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/umbraco/Umbraco.Forms.Issues/security/advisories/GHSA-7jxj-rpx7-ph2c">https://github.com/umbraco/Umbraco.Forms.Issues/security/advisories/GHSA-7jxj-rpx7-ph2c</a></li>
<li><a href="https://github.com/advisories/GHSA-7jxj-rpx7-ph2c">https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-01-22T18:06:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Umbraco.Forms] Umbraco.Forms CDN may cache sensitive form uploads when processed by ImageSharp]]></title>
        <id>https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</id>
        <link href="https://github.com/advisories/GHSA-7jxj-rpx7-ph2c"/>
        <updated>2026-01-22T18:06:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Protected files uploaded through Umbraco Forms may be served to unauthenticated users when a CDN or caching layer is present and ImageSharp processes the request. ImageSharp sets aggressive cache headers by default, which can cause intermediary caches to store and serve files that should require authentication.</p>
<h3 id="patches">Patches</h3>
<p>This issue affects all (supported) versions Umbraco Forms and is patched in 13.9.0, 16.4.0 and 17.1.0.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Add middleware to set cache headers for form uploads. Place the following code in your <code>Startup.cs</code> or <code>Program.cs</code> after <code>app.UseStaticFiles()</code> and any image processing middleware:</p>
<pre><code class="language-cs">app.Use(async (context, next) =&gt;
{
    var path = context.Request.Path.Value;

    if (!string.IsNullOrEmpty(path) &amp;&amp; path.StartsWith("/media/forms/upload/", StringComparison.OrdinalIgnoreCase))
    {
        context.Response.OnStarting(() =&gt;
        {
            context.Response.Headers["Cache-Control"] = "private, no-store, no-cache, must-revalidate";
            context.Response.Headers["Pragma"] = "no-cache";
            context.Response.Headers["Expires"] = "0";
            return Task.CompletedTask;
        });
    }

    await next();
});
</code></pre>
<p>Alternatively, configure your CDN to bypass caching for URLs matching <code>/media/forms/upload/*</code>.</p>
<p>  Note: The vulnerability requires:</p>
<ul>
<li>A CDN in front of the website</li>
<li>An authenticated user having previously requested the image</li>
<li>Knowledge of the form GUID, entry GUID, and image filename</li>
</ul>
<p>  If no CDN is in use, this vulnerability does not apply.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/umbraco/Umbraco.Forms.Issues/security/advisories/GHSA-7jxj-rpx7-ph2c">https://github.com/umbraco/Umbraco.Forms.Issues/security/advisories/GHSA-7jxj-rpx7-ph2c</a></li>
<li><a href="https://github.com/advisories/GHSA-7jxj-rpx7-ph2c">https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7jxj-rpx7-ph2c</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-01-22T18:06:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-AnyCPU] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-AnyCPU] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-AnyCPU] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-arm64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-x64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x86] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-arm64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-x86] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-arm64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-x64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x86] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-arm64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-arm64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-x64] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x86] ImageMagick has a NULL pointer dereference in MSL parser via <comment> tag before image load]]></title>
        <id>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</id>
        <link href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8"/>
        <updated>2026-01-21T01:06:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NULL pointer dereference in MSL (Magick Scripting Language) parser when processing <code>&lt;comment&gt;</code> tag before any image is loaded.</p>
<h2 id="version">Version</h2>
<ul>
<li>ImageMagick 7.x (tested on current main branch)</li>
<li>Commit: HEAD</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="method-1-using-imagemagick-directly">Method 1: Using ImageMagick directly</h3>
<pre><code class="language-bash">magick MSL:poc.msl out.png
</code></pre>
<h3 id="method-2-using-oss-fuzz-reproduce">Method 2: Using OSS-Fuzz reproduce</h3>
<pre><code class="language-bash">python3 infra/helper.py build_fuzzers imagemagick
python3 infra/helper.py reproduce imagemagick msl_fuzzer poc.msl
</code></pre>
<p>Or run the fuzzer directly:</p>
<pre><code class="language-bash">./msl_fuzzer poc.msl
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>ImageMagick should handle the malformed MSL gracefully and return an error message.</p>
<h2 id="actual-behavior">Actual Behavior</h2>
<pre><code>convert: MagickCore/property.c:297: MagickBooleanType DeleteImageProperty(Image *, const char *): Assertion `image != (Image *) NULL' failed.
Aborted
</code></pre>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>In <code>coders/msl.c:7091</code>, <code>MSLEndElement()</code> calls <code>DeleteImageProperty()</code> on <code>msl_info-&gt;image[n]</code> when handling the <code>&lt;/comment&gt;</code> end tag without checking if the image is NULL:</p>
<pre><code class="language-c">if (LocaleCompare((const char *) tag,"comment") == 0 )
  {
    (void) DeleteImageProperty(msl_info-&gt;image[n],"comment");  // No NULL check
    ...
  }
</code></pre>
<p>When <code>&lt;comment&gt;</code> appears before any <code>&lt;read&gt;</code> operation, <code>msl_info-&gt;image[n]</code> is NULL, causing the assertion failure in <code>DeleteImageProperty()</code> at <code>property.c:297</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>DoS</strong>: Crash via assertion failure (debug builds) or NULL pointer dereference (release builds)</li>
<li><strong>Affected</strong>: Any application using ImageMagick to process user-supplied MSL files</li>
</ul>
<h2 id="fuzzer">Fuzzer</h2>
<p>This issue was discovered using a custom MSL fuzzer:</p>
<pre><code class="language-cpp">#include &lt;cstdint&gt;
#include &lt;Magick++/Blob.h&gt;
#include &lt;Magick++/Image.h&gt;
#include "utils.cc"

extern "C" int LLVMFuzzerTestOneInput(const uint8_t *Data, size_t Size)
{
  if (IsInvalidSize(Size))
    return(0);
  try
  {
    const Magick::Blob blob(Data, Size);
    Magick::Image image;
    image.magick("MSL");
    image.fileName("MSL:");
    image.read(blob);
  }
  catch (Magick::Exception)
  {
  }
  return(0);
}
</code></pre>
<p>This issue was found by Team FuzzingBrain @ Texas A&amp;M University</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2">https://github.com/dlemstra/Magick.NET/releases/tag/14.10.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vx3-wx4q-6cj8">https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vx3-wx4q-6cj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-01-21T01:06:51.000Z</published>
    </entry>
</feed>