<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/nuget.rss</id>
    <title>Security Advisory for .NET packages hosted at the NuGet Gallery</title>
    <updated>2025-12-06T07:01:44.707Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anuget"/>
    <subtitle>Security Advisory for .NET packages hosted at the NuGet Gallery on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x86] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-arm64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-x64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-arm64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-AnyCPU] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x86] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-arm64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-x64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-arm64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x86] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-arm64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-x64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-arm64] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-AnyCPU] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-AnyCPU] ImageMagick has a use-after-free/double-free risk in Options::fontFamily when clearing family]]></title>
        <id>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</id>
        <link href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m"/>
        <updated>2025-12-03T16:25:17.000Z</updated>
        <content type="html"><![CDATA[<p>We believe that we have discovered a potential security vulnerability in ImageMagick’s Magick++ layer that manifests when <code>Options::fontFamily</code> is invoked with an empty string.</p>
<p><strong>Vulnerability Details</strong></p>
<ul>
<li>Clearing a font family calls <code>RelinquishMagickMemory</code> on <code>_drawInfo-&gt;font</code>, freeing the font string but leaving <code>_drawInfo-&gt;font</code> pointing to freed memory while <code>_drawInfo-&gt;family</code> is set to that (now-invalid) pointer. Any later cleanup or reuse of <code>_drawInfo-&gt;font</code> re-frees or dereferences dangling memory.</li>
<li><code>DestroyDrawInfo</code> and other setters (<code>Options::font</code>, <code>Image::font</code>) assume <code>_drawInfo-&gt;font</code> remains valid, so destruction or subsequent updates trigger crashes or heap corruption.</li>
</ul>
<pre><code class="language-cpp">if (family_.length() == 0)
  {
    _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
    DestroyString(RemoveImageOption(imageInfo(),"family"));
  }
</code></pre>
<ul>
<li><strong>CWE-416 (Use After Free):</strong> <code>_drawInfo-&gt;font</code> is left dangling yet still reachable through the Options object.</li>
<li><strong>CWE-415 (Double Free):</strong> DrawInfo teardown frees <code>_drawInfo-&gt;font</code> again, provoking allocator aborts.</li>
</ul>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Introduced by commit <code>6409f34d637a34a1c643632aa849371ec8b3b5a8</code> (“Added fontFamily to the Image class of Magick++”, 2015-08-01, blame line 313).</li>
<li>Present in all releases that include that commit, at least ImageMagick 7.0.1-0 and later (likely late 6.9 builds with Magick++ font family support as well). Older releases without <code>fontFamily</code> are unaffected.</li>
</ul>
<p><strong>Command Line Triggerability</strong>
This vulnerability cannot be triggered from the command line interface. The bug is specific to the Magick++ C++ API, specifically the <code>Options::fontFamily()</code> method. The command-line utilities (such as <code>convert</code>, <code>magick</code>, etc.) do not expose this particular code path, as they operate through different internal mechanisms that do not directly call <code>Options::fontFamily()</code> with an empty string in a way that would trigger the use-after-free condition.</p>
<p><strong>Proposed Fix</strong></p>
<pre><code class="language-diff">diff --git a/Magick++/lib/Options.cpp b/Magick++/lib/Options.cpp
@@ void Magick::Options::fontFamily(const std::string &amp;family_)
-      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;font);
+      _drawInfo-&gt;family=(char *) RelinquishMagickMemory(_drawInfo-&gt;family);
</code></pre>
<p>This frees only the actual family string, leaving <code>_drawInfo-&gt;font</code> untouched. Optionally nulling <code>_drawInfo-&gt;font</code> when clearing <code>font()</code> itself maintains allocator hygiene.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-65955">https://nvd.nist.gov/vuln/detail/CVE-2025-65955</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8">https://github.com/ImageMagick/ImageMagick/commit/6409f34d637a34a1c643632aa849371ec8b3b5a8</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8">https://github.com/ImageMagick/ImageMagick/commit/6f81eb15f822ad86e8255be75efad6f9762c32f8</a></li>
<li><a href="https://github.com/advisories/GHSA-q3hc-j9x5-mp9m">https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q3hc-j9x5-mp9m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-12-03T16:25:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DNN.PLATFORM] DNN Insufficient Access Control - Image Upload allows for Site Content Overwrite]]></title>
        <id>https://github.com/advisories/GHSA-3m8r-w7xg-jqvw</id>
        <link href="https://github.com/advisories/GHSA-3m8r-w7xg-jqvw"/>
        <updated>2025-10-29T21:48:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The default HTML editor provider allows unauthenticated file uploads and images can overwrite existing files.</p>
<h3 id="description">Description</h3>
<p>An unauthenticated user can upload and replace existing files allowing defacing a website and combined with other issue, injection XSS payloads.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-3m8r-w7xg-jqvw">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-3m8r-w7xg-jqvw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64095">https://nvd.nist.gov/vuln/detail/CVE-2025-64095</a></li>
<li><a href="https://github.com/advisories/GHSA-3m8r-w7xg-jqvw">https://github.com/advisories/GHSA-3m8r-w7xg-jqvw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3m8r-w7xg-jqvw</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-10-29T21:48:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN vulnerable to stored cross-site-scripting (XSS) via SVG upload]]></title>
        <id>https://github.com/advisories/GHSA-hmvq-8p83-cq52</id>
        <link href="https://github.com/advisories/GHSA-hmvq-8p83-cq52"/>
        <updated>2025-10-29T21:47:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Sanitization of the content of uploaded SVG files was not covering all possible XSS scenarios.</p>
<h3 id="details">Details</h3>
<p>DNN validates the contents of SVG's to ensure they are valid and do not contain any malicious code. These checks were introduced as part of <code>CVE-2025-48378</code>.</p>
<p>However, the checks to ensure there are no script elements within the SVG files are not comprehensive and may allow some malicious SVG files to be uploaded.</p>
<p>As this vulnerability allows for the execution of arbitrary JavaScript code within the context of the user's browser, it can lead to a range of attacks, including data exfiltration, session hijacking, and defacement of the web application to name a few.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-hmvq-8p83-cq52">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-hmvq-8p83-cq52</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64094">https://nvd.nist.gov/vuln/detail/CVE-2025-64094</a></li>
<li><a href="https://github.com/advisories/GHSA-hmvq-8p83-cq52">https://github.com/advisories/GHSA-hmvq-8p83-cq52</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmvq-8p83-cq52</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-10-29T21:47:49.000Z</published>
    </entry>
</feed>