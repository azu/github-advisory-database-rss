<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-04-18T07:24:02.739Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Google Chat app-url webhook auth accepted non-deployment add-on principals]]></title>
        <id>https://github.com/advisories/GHSA-hgwr-wr8h-rxm7</id>
        <link href="https://github.com/advisories/GHSA-hgwr-wr8h-rxm7"/>
        <updated>2026-04-18T00:57:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-mp66-rf4f-mhh8. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw before 2026.3.22 contains an improper authentication verification vulnerability in Google Chat app-url webhook handling that accepts add-on principals outside intended deployment bindings. Attackers can bypass webhook authentication by providing non-deployment add-on principals to execute unauthorized actions through the Google Chat integration.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mp66-rf4f-mhh8">https://github.com/openclaw/openclaw/security/advisories/GHSA-mp66-rf4f-mhh8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35622">https://nvd.nist.gov/vuln/detail/CVE-2026-35622</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/630f1479c44f78484dfa21bb407cbe6f171dac87">https://github.com/openclaw/openclaw/commit/630f1479c44f78484dfa21bb407cbe6f171dac87</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66">https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-improper-authentication-verification-in-google-chat-webhook">https://www.vulncheck.com/advisories/openclaw-improper-authentication-verification-in-google-chat-webhook</a></li>
<li><a href="https://github.com/advisories/GHSA-hgwr-wr8h-rxm7">https://github.com/advisories/GHSA-hgwr-wr8h-rxm7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hgwr-wr8h-rxm7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T00:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Nextcloud Talk room allowlist matched colliding room names instead of stable room tokens]]></title>
        <id>https://github.com/advisories/GHSA-5f7h-p83x-5vc2</id>
        <link href="https://github.com/advisories/GHSA-5f7h-p83x-5vc2"/>
        <updated>2026-04-18T00:55:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-xhq5-45pm-2gjr. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw before 2026.3.22 contains a policy confusion vulnerability in room authorization that matches colliding room names instead of stable room tokens. Attackers can exploit similarly named rooms to bypass allowlist policies and gain unauthorized access to protected Nextcloud Talk rooms.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xhq5-45pm-2gjr">https://github.com/openclaw/openclaw/security/advisories/GHSA-xhq5-45pm-2gjr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35624">https://nvd.nist.gov/vuln/detail/CVE-2026-35624</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/630f1479c44f78484dfa21bb407cbe6f171dac87">https://github.com/openclaw/openclaw/commit/630f1479c44f78484dfa21bb407cbe6f171dac87</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66">https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-policy-confusion-via-room-name-collision-in-nextcloud-talk">https://www.vulncheck.com/advisories/openclaw-policy-confusion-via-room-name-collision-in-nextcloud-talk</a></li>
<li><a href="https://github.com/advisories/GHSA-5f7h-p83x-5vc2">https://github.com/advisories/GHSA-5f7h-p83x-5vc2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5f7h-p83x-5vc2</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-10T00:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: BlueBubbles Webhook Missing Rate Limiting Enables Brute-Force Password Guessing]]></title>
        <id>https://github.com/advisories/GHSA-rc8f-r29c-chr6</id>
        <link href="https://github.com/advisories/GHSA-rc8f-r29c-chr6"/>
        <updated>2026-04-18T00:48:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-xq8g-hgh6-87hv. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw before 2026.3.25 contains a missing rate limiting vulnerability in webhook authentication that allows attackers to brute-force weak webhook passwords without throttling. Remote attackers can repeatedly submit incorrect password guesses to the webhook endpoint to compromise authentication and gain unauthorized access.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xq8g-hgh6-87hv">https://github.com/openclaw/openclaw/security/advisories/GHSA-xq8g-hgh6-87hv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35623">https://nvd.nist.gov/vuln/detail/CVE-2026-35623</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/5e08ce36d522a1c96df2bfe88e39303ae2643d92">https://github.com/openclaw/openclaw/commit/5e08ce36d522a1c96df2bfe88e39303ae2643d92</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-brute-force-attack-via-missing-webhook-password-rate-limiting">https://www.vulncheck.com/advisories/openclaw-brute-force-attack-via-missing-webhook-password-rate-limiting</a></li>
<li><a href="https://github.com/advisories/GHSA-rc8f-r29c-chr6">https://github.com/advisories/GHSA-rc8f-r29c-chr6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rc8f-r29c-chr6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T00:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Google Chat Authz Bypass via Group Policy Rebinding with Mutable Space displayName]]></title>
        <id>https://github.com/advisories/GHSA-j42q-r6qx-xrfp</id>
        <link href="https://github.com/advisories/GHSA-j42q-r6qx-xrfp"/>
        <updated>2026-04-18T00:48:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-52q4-3xjc-6778. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw before 2026.3.25 contains an authorization bypass vulnerability in Google Chat group policy enforcement that relies on mutable space display names. Attackers can rebind group policies by changing or colliding space display names to gain unauthorized access to protected resources.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-52q4-3xjc-6778">https://github.com/openclaw/openclaw/security/advisories/GHSA-52q4-3xjc-6778</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35617">https://nvd.nist.gov/vuln/detail/CVE-2026-35617</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/11ea1f67863d88b6cbcb229dd368a45e07094bff">https://github.com/openclaw/openclaw/commit/11ea1f67863d88b6cbcb229dd368a45e07094bff</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-authorization-bypass-via-group-policy-rebinding-with-mutable-space-displayname">https://www.vulncheck.com/advisories/openclaw-authorization-bypass-via-group-policy-rebinding-with-mutable-space-displayname</a></li>
<li><a href="https://github.com/advisories/GHSA-j42q-r6qx-xrfp">https://github.com/advisories/GHSA-j42q-r6qx-xrfp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j42q-r6qx-xrfp</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-10T00:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise-components] Flowise: Airtable_Agent Code Injection Remote Code Execution Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-v38x-c887-992f</id>
        <link href="https://github.com/advisories/GHSA-v38x-c887-992f"/>
        <updated>2026-04-18T00:46:05.000Z</updated>
        <content type="html"><![CDATA[<p>ZDI-CAN-29412: FlowiseAI Flowise Airtable_Agent Code Injection Remote Code Execution Vulnerability</p>
<p>-- ABSTRACT -------------------------------------</p>
<p>Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products:
Flowise - Flowise</p>
<p>-- VULNERABILITY DETAILS ------------------------</p>
<ul>
<li>Version tested:   3.0.13</li>
<li>Installer file:   hxxps://github.com/FlowiseAI/Flowise</li>
<li>Platform tested:  Ubuntu 25.10</li>
</ul>
<hr>
<h3 id="analysis">Analysis</h3>
<h1 id="flowiseai-flowise-airtable-agent-pythoncode-prompt-injection-remote-code-execution-vulnerability">FlowiseAI Flowise Airtable Agent pythonCode Prompt Injection Remote Code Execution Vulnerability</h1>
<p>This vulnerability allows remote attackers to execute arbitrary code on affected installations of FlowiseAI Flowise. Authentication is not required to exploit this vulnerability.</p>
<p>The specific flaw exists within the run method of the Airtable_Agents class. The issue results from the lack of proper sandboxing when evaluating an LLM generated python script. An attacker can leverage this vulnerability to execute code in the context of the user running the server.</p>
<h3 id="product-information">Product information</h3>
<p>FlowiseAI Flowise version 3.0.13 (<a href="https://github.com/FlowiseAI/Flowise">https://github.com/FlowiseAI/Flowise</a>)</p>
<h3 id="setup-instructions">Setup Instructions</h3>
<p>npm install -g <a href="mailto:flowise@3.0.13">flowise@3.0.13</a>
npx flowise start</p>
<h3 id="root-cause-analysis">Root Cause Analysis</h3>
<p>FlowiseAI Flowise is an open source low-code tool for developers to build customized large language model (LLM) applications and AI agents. It supports integration with various LLMs, data sources, and tools in order to facilitate rapid development and deployment of AI solutions. Flowise offers a web interface with a drag-and-drop editor, as well as an API, through an Express web server accessible over HTTP on port 3000/TCP.</p>
<p>One such feature of Flowise is the ability to create chatflows. Chatflows use a drag and drop editor that allow a developer to place nodes which control how an interaction with a LLM will occur. One such node is the Airtable Agent node that represents an Agent used to answer queries on a provided Airtable table.</p>
<p>When a user makes a query against a chatflow using the Airtable Agent node, the run method of the Airtable_Agents class will be called. This method will first request the contents of the Airtable table passed to the node and convert it to a base64 string. It will then set up a pyodide environment and create a python script to be executed in this environment. This python script will use pandas to extract the column names and their types from the Airtable table. The method will then create a system prompt for an LLM using this data as follows:</p>
<pre><code>You are working with a pandas dataframe in Python. The name of the dataframe is df.

The columns and data types of a dataframe are given below as a Python dictionary with keys showing column names and values showing the data types.
{dict}

I will ask question, and you will output the Python code using pandas dataframe to answer my question. Do not provide any explanations. Do not respond with anything except the output of the code.

Security: Output ONLY pandas/numpy operations on the dataframe (df). Do not use import, exec, eval, open, os, subprocess, or any other system or file operations. The code will be validated and rejected if it contains such constructs.

Question: {question}
Output Code:
</code></pre>
<p>Where {dict} is the extracted column names and {question} is the initial prompt provided by the user.</p>
<p>This system prompt will be sent to an LLM in order for it to generate a python script based on the user's prompt, and the LLM generated response will be stored in a variable name pythonCode. The method will then evaluate the pythonCode variable in a pyodide environment.</p>
<p>While the LLM-generated Python script is evaluated in a non-sandboxed environment, there is a list of forbidden patterns that are checked for before the script is executed on the server. The function validatePythonCodeForDataFrame() enumerates through a list, named FORBIDDEN_PATTERNS, which contains pairs of regex pattern and reasons. Each regex pattern is run against the Python script, and if the pattern is found in the script, the script is invalidated and is not run, responding to the request with a reason for rejection.</p>
<p>The input validation can be bypassed, which can still lead to running arbitrary OS commands on the server. An example of this is the pattern <code>/\bimport\s+(?!pandas|numpy\b)/g</code>, which intends to search for lines of code which import a module other than pandas or numpy. This can be bypassed by importing along with pandas or numpy. For example, consider the following lines of code:</p>
<pre><code class="language-python">import pandas as np, os as pandas
pandas.system("xcalc")
</code></pre>
<p>pandas is imported, but so is the os module, with pandas as its alias. OS commands can then be invoked with <code>pandas.system()</code>. </p>
<p>Using prompt injection techniques, an unauthenticated attacker with the ability to send prompts to a chatflow using the Airtable Agent node may convince an LLM to respond with a malicious python script that executes attacker controlled commands on the flowise server.</p>
<p>An attacker can use this vulnerability to execute arbitrary python code, which can lead to arbitrary system commands being executed on the target server.</p>
<p>It is also possible for an authenticated attacker to exploit this vulnerability by specifying an attacker controlled server in a chatflow. This server would respond to prompts with an attacker controlled python script instead of an LLM generated response, which would then be evaluated on the server.</p>
<p>It is also possible for an authenticated attacker to exploit this vulnerability by specifying an attacker controlled Airtable table in a chatflow. This airtable table would contain columns whose name contain prompt injections, that are later passed to an LLM to use when generating a python script.</p>
<p>comments documenting the issue have been added to the following code snippet. Added comments are prepended with "!!!".</p>
<p>From packages/components/nodes/agents/AirtableAgent/core.ts</p>
<pre><code class="language-ts">import type { PyodideInterface } from 'pyodide'
import * as path from 'path'
import { getUserHome } from '../../../src/utils'

let pyodideInstance: PyodideInterface | undefined

export async function LoadPyodide(): Promise&lt;PyodideInterface&gt; {
    if (pyodideInstance === undefined) {
        const { loadPyodide } = await import('pyodide')
        const obj: any = { packageCacheDir: path.join(getUserHome(), '.flowise', 'pyodideCacheDir') }
        pyodideInstance = await loadPyodide(obj)
        await pyodideInstance.loadPackage(['pandas', 'numpy'])
    }

    return pyodideInstance
}

export const systemPrompt = `You are working with a pandas dataframe in Python. The name of the dataframe is df.

The columns and data types of a dataframe are given below as a Python dictionary with keys showing column names and values showing the data types.
{dict}

I will ask question, and you will output the Python code using pandas dataframe to answer my question. Do not provide any explanations. Do not respond with anything except the output of the code.

Security: Output ONLY pandas/numpy operations on the dataframe (df). Do not use import, exec, eval, open, os, subprocess, or any other system or file operations. The code will be validated and rejected if it contains such constructs.

Question: {question}
Output Code:`

export const finalSystemPrompt = `You are given the question: {question}. You have an answer to the question: {answer}. Rephrase the answer into a standalone answer.
Standalone Answer:`
</code></pre>
<p>From packages/components/nodes/agents/AirtableAgent/AirtableAgent.ts</p>
<pre><code class="language-ts">import axios from 'axios'
import { BaseLanguageModel } from '@langchain/core/language_models/base'
import { AgentExecutor } from 'langchain/agents'
import { LLMChain } from 'langchain/chains'
import { ICommonObject, INode, INodeData, INodeParams, IServerSideEventStreamer, PromptTemplate } from '../../../src/Interface'
import { getBaseClasses, getCredentialData, getCredentialParam } from '../../../src/utils'
import { ConsoleCallbackHandler, CustomChainHandler, additionalCallbacks } from '../../../src/handler'
import { LoadPyodide, finalSystemPrompt, systemPrompt } from './core'
import { validatePythonCodeForDataFrame } from '../../../src/pythonCodeValidator'
import { checkInputs, Moderation } from '../../moderation/Moderation'
import { formatResponse } from '../../outputparsers/OutputParserHelpers'

class Airtable_Agents implements INode {
    label: string
    name: string
    version: number
    description: string
    type: string
    icon: string
    category: string
    baseClasses: string[]
    credential: INodeParams
    inputs: INodeParams[]

    // !!! [... Truncated for Readability ...]

    // !!! input variable holds prompt from user
    async run(nodeData: INodeData, input: string, options: ICommonObject): Promise&lt;string | object&gt; {
        const model = nodeData.inputs?.model as BaseLanguageModel
        const baseId = nodeData.inputs?.baseId as string
        const tableId = nodeData.inputs?.tableId as string
        const returnAll = nodeData.inputs?.returnAll as boolean
        const limit = nodeData.inputs?.limit as string
        const moderations = nodeData.inputs?.inputModeration as Moderation[]

        // !!! the chatflow may contain moderation nodes that search for prompt injections, but these may not protect from every type of injection
        if (moderations &amp;&amp; moderations.length &gt; 0) {
            try {
                // Use the output of the moderation chain as input for the Vectara chain
                input = await checkInputs(moderations, input)
            } catch (e) {
                await new Promise((resolve) =&gt; setTimeout(resolve, 500))
                // if (options.shouldStreamResponse) {
                //     streamResponse(options.sseStreamer, options.chatId, e.message)
                // }
                return formatResponse(e.message)
            }
        }

        const shouldStreamResponse = options.shouldStreamResponse
        const sseStreamer: IServerSideEventStreamer = options.sseStreamer as IServerSideEventStreamer
        const chatId = options.chatId

        const credentialData = await getCredentialData(nodeData.credential ?? '', options)
        const accessToken = getCredentialParam('accessToken', credentialData, nodeData)

        let airtableData: ICommonObject[] = []

        // !!! Get Airtable data
        if (returnAll) {
            airtableData = await loadAll(baseId, tableId, accessToken)
        } else {
            airtableData = await loadLimit(limit ? parseInt(limit, 10) : 100, baseId, tableId, accessToken)
        }

        let base64String = Buffer.from(JSON.stringify(airtableData)).toString('base64')

        const loggerHandler = new ConsoleCallbackHandler(options.logger, options?.orgId)
        const callbacks = await additionalCallbacks(nodeData, options)

        const pyodide = await LoadPyodide()

        // First load the csv file and get the dataframe dictionary of column types
        // For example using titanic.csv: {'PassengerId': 'int64', 'Survived': 'int64', 'Pclass': 'int64', 'Name': 'object', 'Sex': 'object', 'Age': 'float64', 'SibSp': 'int64', 'Parch': 'int64', 'Ticket': 'object', 'Fare': 'float64', 'Cabin': 'object', 'Embarked': 'object'}
        let dataframeColDict = ''
        try {
            const code = `import pandas as pd
import base64
import json

base64_string = "${base64String}"

decoded_data = base64.b64decode(base64_string)

json_data = json.loads(decoded_data)

df = pd.DataFrame(json_data)
my_dict = df.dtypes.astype(str).to_dict()
print(my_dict)
json.dumps(my_dict)`
            dataframeColDict = await pyodide.runPythonAsync(code)
        } catch (error) {
            throw new Error(error)
        }

        // !!! ask LLM to come up with python script...
        // Then tell GPT to come out with ONLY python code
        // For example: len(df), df[df['SibSp'] &gt; 3]['PassengerId'].count()
        let pythonCode = ''
        if (dataframeColDict) {
            const chain = new LLMChain({
                llm: model,
                // !!! prompt passed to LLM
                prompt: PromptTemplate.fromTemplate(systemPrompt),
                verbose: process.env.DEBUG === 'true' ? true : false
            })
            const inputs = {
                // !!! Airtable column names are also subbed into the system prompt which may also contain prompt injections
                dict: dataframeColDict,
                // !!! question, which is later subbed into the system prompt, is given the value of the user's prompt (which may contain prompt injections)
                question: input
            }
            const res = await chain.call(inputs, [loggerHandler, ...callbacks])
            // !!! the LLM's responce is assigned to the pythonCode variable
            pythonCode = res?.text
            // Regex to get rid of markdown code blocks syntax
            pythonCode = pythonCode.replace(/^```[a-z]+\n|\n```$/gm, '')
        }

        // Then run the code using Pyodide (only after validating to prevent RCE)
        let finalResult = ''
        if (pythonCode) {
            const validation = validatePythonCodeForDataFrame(pythonCode)
            if (!validation.valid) {
                throw new Error(
                    `Generated code was rejected for security reasons (${
                        validation.reason ?? 'unsafe construct'
                    }). Please rephrase your question to use only pandas DataFrame operations.`
                )
            }
            try {
                // !!! The python code is evaluated in a non-sandboxed environment
                const code = `import pandas as pd\n${pythonCode}`
                // TODO: get print console output
                finalResult = await pyodide.runPythonAsync(code)
            } catch (error) {
                throw new Error(`Sorry, I'm unable to find answer for question: "${input}" using following code: "${pythonCode}"`)
            }
        }

        // Finally, return a complete answer
        if (finalResult) {
            const chain = new LLMChain({
                llm: model,
                prompt: PromptTemplate.fromTemplate(finalSystemPrompt),
                verbose: process.env.DEBUG === 'true' ? true : false
            })
            const inputs = {
                question: input,
                answer: finalResult
            }

            if (options.shouldStreamResponse) {
                const handler = new CustomChainHandler(shouldStreamResponse ? sseStreamer : undefined, chatId)
                const result = await chain.call(inputs, [loggerHandler, handler, ...callbacks])
                return result?.text
            } else {
                const result = await chain.call(inputs, [loggerHandler, ...callbacks])
                return result?.text
            }
        }

        return pythonCode
    }
}
</code></pre>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>A proof of concept for this vulnerability is provided in ./poc.py. It expects the following syntax:</p>
<pre><code>    python3 poc.py --method [server OR chatflow OR prompt_injection] [--user &lt;USER&gt; --passwd &lt;password&gt; --host &lt;HOST&gt; --r_host &lt;R_HOST&gt; --r_port &lt;R_PORT&gt; --l_port &lt;L_PORT&gt; --port &lt;PORT&gt; --cmd &lt;CMD&gt; --chatflow_id &lt;CHAT_ID&gt; --airtable_token &lt;AIRTABLE_TOKEN&gt; --base_id &lt;BASE_ID&gt; --table_id &lt;TABLE_ID&gt;]
</code></pre>
<p>Where USER is a username of a user on the server, PASSWORD is the user's password, HOST is the ip address of the vulnerable flowise server, R_HOST is the ip address of a malicious server started by this poc, R_PORT is the port a malicious server started by this poc is listening on (default: 5000), L_PORT is the port a malicious server started by this poc should listening on (default: 5000), PORT is the port the vulnerable flowise server is listening on (default: 3000), CMD is the command to execute on the flowise server (default: xcalc), CHAT_ID is the chatflow id of a chatflow using the Airflow Agent node, AIRTABLE_TOKEN is an api key for an Airtable account, BASE_ID is the base id to find an airflow table in, and TABLE_ID is the airflow table to use. </p>
<p>This poc has three modes of operation controlled by the method argument. The method argument may have any of the values "server" OR "chatflow" OR "prompt_injection".</p>
<h4 id="method--server">method = "server"</h4>
<p>By default the poc will start a malicious server listening on the port specified by the  value. This server will respond to requests made to the "/api/chat" endpoint with a JSON object containing an LLM response that contains a malicious python script. This python script will execute a command specified by the  value.</p>
<h4 id="method--chatflow">method = "chatflow"</h4>
<p>By default, the poc will first establish an authenticated session on the server using the  and  arguments. It will then send a POST request to the "/api/v1/chatflow" endpoint with a JSON body containing a crafted chatflow using an Airflow Agent node and a ChatOllama node configured with a server specified by the  and  arguments. The Airflow Agent node will be configured using the , , and  arguments. The default values of these arguments will be valid for 14 days from 2026-02-22. The poc will then send a POST request to the "/api/v1/internal-prediction/" endpoint in order to trigger a prediction using the chatflow. </p>
<p>It is intended that the server specified in the chatflow is a server started using the server method of this poc. When making a prediction against this chatflow, flowise will send a request to the specified server in order to generate an LLM response. The response recieved by flowise will be evaluated as a python script. Upon successful exploitation, The  argument passed to the server method of this poc will be executed on the vulnerable flowise server. </p>
<h4 id="method--prompt_injection">method = "prompt_injection"</h4>
<p>By default, the poc will send a POST request to the "/api/v1/prediction/<em>chat_id</em>" endpoint, where <em>chat_id</em> is a vulnerable chatflow id specified by the  parameter. The JSON body of this request will contain a question member whose value will be a prompt containing a prompt injection. Upon successful exploitation, The  argument will be executed on the vulnerable flowise server.</p>
<p>Due to the nature of LLM responses, it may take multiple attempts to be successful or require a different prompt injection technique depending on the model used.</p>
<h3 id="testing-environment">Testing Environment</h3>
<p>The provided proof of concept was tested using FlowiseAI Flowise version 3.0.13 runing on a Ubuntu 25.10 VM. The prompt injection method was tested using the Llama3.2 model running in Ollama.</p>
<h3 id="credits">Credits</h3>
<p>Dre Cura (@dre_cura) and Nicholas Zubrisky (@NZubrisky) of TrendAI Research</p>
<h3 id="how-this-differs-from-cve-2026-41138">How This Differs from CVE-2026-41138</h3>
<p>CVE-2026-41138 introduced sanitization for the Pyodide code ran by the Airtable Agent. This advisory demonstrates a bypass of that sanitization, which we addressed separately by disallowing imports outright.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-v38x-c887-992f">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-v38x-c887-992f</a></li>
<li><a href="https://github.com/advisories/GHSA-v38x-c887-992f">https://github.com/advisories/GHSA-v38x-c887-992f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v38x-c887-992f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T00:46:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise: Airtable_Agent Code Injection Remote Code Execution Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-v38x-c887-992f</id>
        <link href="https://github.com/advisories/GHSA-v38x-c887-992f"/>
        <updated>2026-04-18T00:46:05.000Z</updated>
        <content type="html"><![CDATA[<p>ZDI-CAN-29412: FlowiseAI Flowise Airtable_Agent Code Injection Remote Code Execution Vulnerability</p>
<p>-- ABSTRACT -------------------------------------</p>
<p>Trend Micro's Zero Day Initiative has identified a vulnerability affecting the following products:
Flowise - Flowise</p>
<p>-- VULNERABILITY DETAILS ------------------------</p>
<ul>
<li>Version tested:   3.0.13</li>
<li>Installer file:   hxxps://github.com/FlowiseAI/Flowise</li>
<li>Platform tested:  Ubuntu 25.10</li>
</ul>
<hr>
<h3 id="analysis">Analysis</h3>
<h1 id="flowiseai-flowise-airtable-agent-pythoncode-prompt-injection-remote-code-execution-vulnerability">FlowiseAI Flowise Airtable Agent pythonCode Prompt Injection Remote Code Execution Vulnerability</h1>
<p>This vulnerability allows remote attackers to execute arbitrary code on affected installations of FlowiseAI Flowise. Authentication is not required to exploit this vulnerability.</p>
<p>The specific flaw exists within the run method of the Airtable_Agents class. The issue results from the lack of proper sandboxing when evaluating an LLM generated python script. An attacker can leverage this vulnerability to execute code in the context of the user running the server.</p>
<h3 id="product-information">Product information</h3>
<p>FlowiseAI Flowise version 3.0.13 (<a href="https://github.com/FlowiseAI/Flowise">https://github.com/FlowiseAI/Flowise</a>)</p>
<h3 id="setup-instructions">Setup Instructions</h3>
<p>npm install -g <a href="mailto:flowise@3.0.13">flowise@3.0.13</a>
npx flowise start</p>
<h3 id="root-cause-analysis">Root Cause Analysis</h3>
<p>FlowiseAI Flowise is an open source low-code tool for developers to build customized large language model (LLM) applications and AI agents. It supports integration with various LLMs, data sources, and tools in order to facilitate rapid development and deployment of AI solutions. Flowise offers a web interface with a drag-and-drop editor, as well as an API, through an Express web server accessible over HTTP on port 3000/TCP.</p>
<p>One such feature of Flowise is the ability to create chatflows. Chatflows use a drag and drop editor that allow a developer to place nodes which control how an interaction with a LLM will occur. One such node is the Airtable Agent node that represents an Agent used to answer queries on a provided Airtable table.</p>
<p>When a user makes a query against a chatflow using the Airtable Agent node, the run method of the Airtable_Agents class will be called. This method will first request the contents of the Airtable table passed to the node and convert it to a base64 string. It will then set up a pyodide environment and create a python script to be executed in this environment. This python script will use pandas to extract the column names and their types from the Airtable table. The method will then create a system prompt for an LLM using this data as follows:</p>
<pre><code>You are working with a pandas dataframe in Python. The name of the dataframe is df.

The columns and data types of a dataframe are given below as a Python dictionary with keys showing column names and values showing the data types.
{dict}

I will ask question, and you will output the Python code using pandas dataframe to answer my question. Do not provide any explanations. Do not respond with anything except the output of the code.

Security: Output ONLY pandas/numpy operations on the dataframe (df). Do not use import, exec, eval, open, os, subprocess, or any other system or file operations. The code will be validated and rejected if it contains such constructs.

Question: {question}
Output Code:
</code></pre>
<p>Where {dict} is the extracted column names and {question} is the initial prompt provided by the user.</p>
<p>This system prompt will be sent to an LLM in order for it to generate a python script based on the user's prompt, and the LLM generated response will be stored in a variable name pythonCode. The method will then evaluate the pythonCode variable in a pyodide environment.</p>
<p>While the LLM-generated Python script is evaluated in a non-sandboxed environment, there is a list of forbidden patterns that are checked for before the script is executed on the server. The function validatePythonCodeForDataFrame() enumerates through a list, named FORBIDDEN_PATTERNS, which contains pairs of regex pattern and reasons. Each regex pattern is run against the Python script, and if the pattern is found in the script, the script is invalidated and is not run, responding to the request with a reason for rejection.</p>
<p>The input validation can be bypassed, which can still lead to running arbitrary OS commands on the server. An example of this is the pattern <code>/\bimport\s+(?!pandas|numpy\b)/g</code>, which intends to search for lines of code which import a module other than pandas or numpy. This can be bypassed by importing along with pandas or numpy. For example, consider the following lines of code:</p>
<pre><code class="language-python">import pandas as np, os as pandas
pandas.system("xcalc")
</code></pre>
<p>pandas is imported, but so is the os module, with pandas as its alias. OS commands can then be invoked with <code>pandas.system()</code>. </p>
<p>Using prompt injection techniques, an unauthenticated attacker with the ability to send prompts to a chatflow using the Airtable Agent node may convince an LLM to respond with a malicious python script that executes attacker controlled commands on the flowise server.</p>
<p>An attacker can use this vulnerability to execute arbitrary python code, which can lead to arbitrary system commands being executed on the target server.</p>
<p>It is also possible for an authenticated attacker to exploit this vulnerability by specifying an attacker controlled server in a chatflow. This server would respond to prompts with an attacker controlled python script instead of an LLM generated response, which would then be evaluated on the server.</p>
<p>It is also possible for an authenticated attacker to exploit this vulnerability by specifying an attacker controlled Airtable table in a chatflow. This airtable table would contain columns whose name contain prompt injections, that are later passed to an LLM to use when generating a python script.</p>
<p>comments documenting the issue have been added to the following code snippet. Added comments are prepended with "!!!".</p>
<p>From packages/components/nodes/agents/AirtableAgent/core.ts</p>
<pre><code class="language-ts">import type { PyodideInterface } from 'pyodide'
import * as path from 'path'
import { getUserHome } from '../../../src/utils'

let pyodideInstance: PyodideInterface | undefined

export async function LoadPyodide(): Promise&lt;PyodideInterface&gt; {
    if (pyodideInstance === undefined) {
        const { loadPyodide } = await import('pyodide')
        const obj: any = { packageCacheDir: path.join(getUserHome(), '.flowise', 'pyodideCacheDir') }
        pyodideInstance = await loadPyodide(obj)
        await pyodideInstance.loadPackage(['pandas', 'numpy'])
    }

    return pyodideInstance
}

export const systemPrompt = `You are working with a pandas dataframe in Python. The name of the dataframe is df.

The columns and data types of a dataframe are given below as a Python dictionary with keys showing column names and values showing the data types.
{dict}

I will ask question, and you will output the Python code using pandas dataframe to answer my question. Do not provide any explanations. Do not respond with anything except the output of the code.

Security: Output ONLY pandas/numpy operations on the dataframe (df). Do not use import, exec, eval, open, os, subprocess, or any other system or file operations. The code will be validated and rejected if it contains such constructs.

Question: {question}
Output Code:`

export const finalSystemPrompt = `You are given the question: {question}. You have an answer to the question: {answer}. Rephrase the answer into a standalone answer.
Standalone Answer:`
</code></pre>
<p>From packages/components/nodes/agents/AirtableAgent/AirtableAgent.ts</p>
<pre><code class="language-ts">import axios from 'axios'
import { BaseLanguageModel } from '@langchain/core/language_models/base'
import { AgentExecutor } from 'langchain/agents'
import { LLMChain } from 'langchain/chains'
import { ICommonObject, INode, INodeData, INodeParams, IServerSideEventStreamer, PromptTemplate } from '../../../src/Interface'
import { getBaseClasses, getCredentialData, getCredentialParam } from '../../../src/utils'
import { ConsoleCallbackHandler, CustomChainHandler, additionalCallbacks } from '../../../src/handler'
import { LoadPyodide, finalSystemPrompt, systemPrompt } from './core'
import { validatePythonCodeForDataFrame } from '../../../src/pythonCodeValidator'
import { checkInputs, Moderation } from '../../moderation/Moderation'
import { formatResponse } from '../../outputparsers/OutputParserHelpers'

class Airtable_Agents implements INode {
    label: string
    name: string
    version: number
    description: string
    type: string
    icon: string
    category: string
    baseClasses: string[]
    credential: INodeParams
    inputs: INodeParams[]

    // !!! [... Truncated for Readability ...]

    // !!! input variable holds prompt from user
    async run(nodeData: INodeData, input: string, options: ICommonObject): Promise&lt;string | object&gt; {
        const model = nodeData.inputs?.model as BaseLanguageModel
        const baseId = nodeData.inputs?.baseId as string
        const tableId = nodeData.inputs?.tableId as string
        const returnAll = nodeData.inputs?.returnAll as boolean
        const limit = nodeData.inputs?.limit as string
        const moderations = nodeData.inputs?.inputModeration as Moderation[]

        // !!! the chatflow may contain moderation nodes that search for prompt injections, but these may not protect from every type of injection
        if (moderations &amp;&amp; moderations.length &gt; 0) {
            try {
                // Use the output of the moderation chain as input for the Vectara chain
                input = await checkInputs(moderations, input)
            } catch (e) {
                await new Promise((resolve) =&gt; setTimeout(resolve, 500))
                // if (options.shouldStreamResponse) {
                //     streamResponse(options.sseStreamer, options.chatId, e.message)
                // }
                return formatResponse(e.message)
            }
        }

        const shouldStreamResponse = options.shouldStreamResponse
        const sseStreamer: IServerSideEventStreamer = options.sseStreamer as IServerSideEventStreamer
        const chatId = options.chatId

        const credentialData = await getCredentialData(nodeData.credential ?? '', options)
        const accessToken = getCredentialParam('accessToken', credentialData, nodeData)

        let airtableData: ICommonObject[] = []

        // !!! Get Airtable data
        if (returnAll) {
            airtableData = await loadAll(baseId, tableId, accessToken)
        } else {
            airtableData = await loadLimit(limit ? parseInt(limit, 10) : 100, baseId, tableId, accessToken)
        }

        let base64String = Buffer.from(JSON.stringify(airtableData)).toString('base64')

        const loggerHandler = new ConsoleCallbackHandler(options.logger, options?.orgId)
        const callbacks = await additionalCallbacks(nodeData, options)

        const pyodide = await LoadPyodide()

        // First load the csv file and get the dataframe dictionary of column types
        // For example using titanic.csv: {'PassengerId': 'int64', 'Survived': 'int64', 'Pclass': 'int64', 'Name': 'object', 'Sex': 'object', 'Age': 'float64', 'SibSp': 'int64', 'Parch': 'int64', 'Ticket': 'object', 'Fare': 'float64', 'Cabin': 'object', 'Embarked': 'object'}
        let dataframeColDict = ''
        try {
            const code = `import pandas as pd
import base64
import json

base64_string = "${base64String}"

decoded_data = base64.b64decode(base64_string)

json_data = json.loads(decoded_data)

df = pd.DataFrame(json_data)
my_dict = df.dtypes.astype(str).to_dict()
print(my_dict)
json.dumps(my_dict)`
            dataframeColDict = await pyodide.runPythonAsync(code)
        } catch (error) {
            throw new Error(error)
        }

        // !!! ask LLM to come up with python script...
        // Then tell GPT to come out with ONLY python code
        // For example: len(df), df[df['SibSp'] &gt; 3]['PassengerId'].count()
        let pythonCode = ''
        if (dataframeColDict) {
            const chain = new LLMChain({
                llm: model,
                // !!! prompt passed to LLM
                prompt: PromptTemplate.fromTemplate(systemPrompt),
                verbose: process.env.DEBUG === 'true' ? true : false
            })
            const inputs = {
                // !!! Airtable column names are also subbed into the system prompt which may also contain prompt injections
                dict: dataframeColDict,
                // !!! question, which is later subbed into the system prompt, is given the value of the user's prompt (which may contain prompt injections)
                question: input
            }
            const res = await chain.call(inputs, [loggerHandler, ...callbacks])
            // !!! the LLM's responce is assigned to the pythonCode variable
            pythonCode = res?.text
            // Regex to get rid of markdown code blocks syntax
            pythonCode = pythonCode.replace(/^```[a-z]+\n|\n```$/gm, '')
        }

        // Then run the code using Pyodide (only after validating to prevent RCE)
        let finalResult = ''
        if (pythonCode) {
            const validation = validatePythonCodeForDataFrame(pythonCode)
            if (!validation.valid) {
                throw new Error(
                    `Generated code was rejected for security reasons (${
                        validation.reason ?? 'unsafe construct'
                    }). Please rephrase your question to use only pandas DataFrame operations.`
                )
            }
            try {
                // !!! The python code is evaluated in a non-sandboxed environment
                const code = `import pandas as pd\n${pythonCode}`
                // TODO: get print console output
                finalResult = await pyodide.runPythonAsync(code)
            } catch (error) {
                throw new Error(`Sorry, I'm unable to find answer for question: "${input}" using following code: "${pythonCode}"`)
            }
        }

        // Finally, return a complete answer
        if (finalResult) {
            const chain = new LLMChain({
                llm: model,
                prompt: PromptTemplate.fromTemplate(finalSystemPrompt),
                verbose: process.env.DEBUG === 'true' ? true : false
            })
            const inputs = {
                question: input,
                answer: finalResult
            }

            if (options.shouldStreamResponse) {
                const handler = new CustomChainHandler(shouldStreamResponse ? sseStreamer : undefined, chatId)
                const result = await chain.call(inputs, [loggerHandler, handler, ...callbacks])
                return result?.text
            } else {
                const result = await chain.call(inputs, [loggerHandler, ...callbacks])
                return result?.text
            }
        }

        return pythonCode
    }
}
</code></pre>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>A proof of concept for this vulnerability is provided in ./poc.py. It expects the following syntax:</p>
<pre><code>    python3 poc.py --method [server OR chatflow OR prompt_injection] [--user &lt;USER&gt; --passwd &lt;password&gt; --host &lt;HOST&gt; --r_host &lt;R_HOST&gt; --r_port &lt;R_PORT&gt; --l_port &lt;L_PORT&gt; --port &lt;PORT&gt; --cmd &lt;CMD&gt; --chatflow_id &lt;CHAT_ID&gt; --airtable_token &lt;AIRTABLE_TOKEN&gt; --base_id &lt;BASE_ID&gt; --table_id &lt;TABLE_ID&gt;]
</code></pre>
<p>Where USER is a username of a user on the server, PASSWORD is the user's password, HOST is the ip address of the vulnerable flowise server, R_HOST is the ip address of a malicious server started by this poc, R_PORT is the port a malicious server started by this poc is listening on (default: 5000), L_PORT is the port a malicious server started by this poc should listening on (default: 5000), PORT is the port the vulnerable flowise server is listening on (default: 3000), CMD is the command to execute on the flowise server (default: xcalc), CHAT_ID is the chatflow id of a chatflow using the Airflow Agent node, AIRTABLE_TOKEN is an api key for an Airtable account, BASE_ID is the base id to find an airflow table in, and TABLE_ID is the airflow table to use. </p>
<p>This poc has three modes of operation controlled by the method argument. The method argument may have any of the values "server" OR "chatflow" OR "prompt_injection".</p>
<h4 id="method--server">method = "server"</h4>
<p>By default the poc will start a malicious server listening on the port specified by the  value. This server will respond to requests made to the "/api/chat" endpoint with a JSON object containing an LLM response that contains a malicious python script. This python script will execute a command specified by the  value.</p>
<h4 id="method--chatflow">method = "chatflow"</h4>
<p>By default, the poc will first establish an authenticated session on the server using the  and  arguments. It will then send a POST request to the "/api/v1/chatflow" endpoint with a JSON body containing a crafted chatflow using an Airflow Agent node and a ChatOllama node configured with a server specified by the  and  arguments. The Airflow Agent node will be configured using the , , and  arguments. The default values of these arguments will be valid for 14 days from 2026-02-22. The poc will then send a POST request to the "/api/v1/internal-prediction/" endpoint in order to trigger a prediction using the chatflow. </p>
<p>It is intended that the server specified in the chatflow is a server started using the server method of this poc. When making a prediction against this chatflow, flowise will send a request to the specified server in order to generate an LLM response. The response recieved by flowise will be evaluated as a python script. Upon successful exploitation, The  argument passed to the server method of this poc will be executed on the vulnerable flowise server. </p>
<h4 id="method--prompt_injection">method = "prompt_injection"</h4>
<p>By default, the poc will send a POST request to the "/api/v1/prediction/<em>chat_id</em>" endpoint, where <em>chat_id</em> is a vulnerable chatflow id specified by the  parameter. The JSON body of this request will contain a question member whose value will be a prompt containing a prompt injection. Upon successful exploitation, The  argument will be executed on the vulnerable flowise server.</p>
<p>Due to the nature of LLM responses, it may take multiple attempts to be successful or require a different prompt injection technique depending on the model used.</p>
<h3 id="testing-environment">Testing Environment</h3>
<p>The provided proof of concept was tested using FlowiseAI Flowise version 3.0.13 runing on a Ubuntu 25.10 VM. The prompt injection method was tested using the Llama3.2 model running in Ollama.</p>
<h3 id="credits">Credits</h3>
<p>Dre Cura (@dre_cura) and Nicholas Zubrisky (@NZubrisky) of TrendAI Research</p>
<h3 id="how-this-differs-from-cve-2026-41138">How This Differs from CVE-2026-41138</h3>
<p>CVE-2026-41138 introduced sanitization for the Pyodide code ran by the Airtable Agent. This advisory demonstrates a bypass of that sanitization, which we addressed separately by disallowing imports outright.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-v38x-c887-992f">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-v38x-c887-992f</a></li>
<li><a href="https://github.com/advisories/GHSA-v38x-c887-992f">https://github.com/advisories/GHSA-v38x-c887-992f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v38x-c887-992f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-18T00:46:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Telegram Webhook Missing Guess Rate Limiting Enables Brute-Force Guessing of Weak Webhook Secret]]></title>
        <id>https://github.com/advisories/GHSA-r4c2-gq3j-7rpj</id>
        <link href="https://github.com/advisories/GHSA-r4c2-gq3j-7rpj"/>
        <updated>2026-04-18T00:45:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-vcx4-4qxg-mfp4. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw before 2026.3.25 contains a missing rate limiting vulnerability in Telegram webhook authentication that allows attackers to brute-force weak webhook secrets. The vulnerability enables repeated authentication guesses without throttling, permitting attackers to systematically guess webhook secrets through brute-force attacks.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-vcx4-4qxg-mfp4">https://github.com/openclaw/openclaw/security/advisories/GHSA-vcx4-4qxg-mfp4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35628">https://nvd.nist.gov/vuln/detail/CVE-2026-35628</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/c2c136ae9517ddd0789d742a0fdf4c10e8c729a7">https://github.com/openclaw/openclaw/commit/c2c136ae9517ddd0789d742a0fdf4c10e8c729a7</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-brute-force-attack-via-missing-telegram-webhook-rate-limiting">https://www.vulncheck.com/advisories/openclaw-brute-force-attack-via-missing-telegram-webhook-rate-limiting</a></li>
<li><a href="https://github.com/advisories/GHSA-r4c2-gq3j-7rpj">https://github.com/advisories/GHSA-r4c2-gq3j-7rpj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r4c2-gq3j-7rpj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T00:30:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Gateway Plugin Subagent Fallback `deleteSession` Uses Synthetic `operator.admin`]]></title>
        <id>https://github.com/advisories/GHSA-m5jp-p3r5-mfqp</id>
        <link href="https://github.com/advisories/GHSA-m5jp-p3r5-mfqp"/>
        <updated>2026-04-18T00:43:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-h4jx-hjr3-fhgc. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw before 2026.3.25 contains a privilege escalation vulnerability in the gateway plugin subagent fallback deleteSession function that uses a synthetic operator.admin runtime scope. Attackers can exploit this by triggering session deletion without a request-scoped client to execute privileged operations with unintended administrative scope.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-h4jx-hjr3-fhgc">https://github.com/openclaw/openclaw/security/advisories/GHSA-h4jx-hjr3-fhgc</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35645">https://nvd.nist.gov/vuln/detail/CVE-2026-35645</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/b5d785f1a59a56c3471f2cef328f7c9a6c15f3e7">https://github.com/openclaw/openclaw/commit/b5d785f1a59a56c3471f2cef328f7c9a6c15f3e7</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-synthetic-operator-admin-in-deletesession">https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-synthetic-operator-admin-in-deletesession</a></li>
<li><a href="https://github.com/advisories/GHSA-m5jp-p3r5-mfqp">https://github.com/advisories/GHSA-m5jp-p3r5-mfqp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m5jp-p3r5-mfqp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T00:30:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Symlink Traversal via IDENTITY.md appendFile in agents.create/update (Incomplete Fix for CVE-2026-32013)]]></title>
        <id>https://github.com/advisories/GHSA-pmf3-2q63-jmp6</id>
        <link href="https://github.com/advisories/GHSA-pmf3-2q63-jmp6"/>
        <updated>2026-04-18T00:42:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-7xr2-q9vf-x4r5. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>OpenClaw through 2026.2.22 contains a symlink traversal vulnerability in agents.create and agents.update handlers that use fs.appendFile on IDENTITY.md without symlink containment checks. Attackers with workspace access can plant symlinks to append attacker-controlled content to arbitrary files, enabling remote code execution via crontab injection or unauthorized access via SSH key manipulation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-7xr2-q9vf-x4r5">https://github.com/openclaw/openclaw/security/advisories/GHSA-7xr2-q9vf-x4r5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35632">https://nvd.nist.gov/vuln/detail/CVE-2026-35632</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-symlink-traversal-via-identity-md-appendfile-in-agents-create-update">https://www.vulncheck.com/advisories/openclaw-symlink-traversal-via-identity-md-appendfile-in-agents-create-update</a></li>
<li><a href="https://github.com/advisories/GHSA-pmf3-2q63-jmp6">https://github.com/advisories/GHSA-pmf3-2q63-jmp6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pmf3-2q63-jmp6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T00:30:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: QMD memory_get restricts reads to canonical or indexed memory paths]]></title>
        <id>https://github.com/advisories/GHSA-f934-5rqf-xx47</id>
        <link href="https://github.com/advisories/GHSA-f934-5rqf-xx47"/>
        <updated>2026-04-17T22:33:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The QMD backend <code>memory_get</code> read path accepted arbitrary workspace Markdown paths that were inside the workspace but outside the canonical memory locations or indexed QMD result set.</p>
<h2 id="impact">Impact</h2>
<p>When the QMD backend was enabled, a caller with access to <code>memory_get</code> could read arbitrary <code>*.md</code> files under the configured workspace root, even when those files were not canonical memory files and had not been returned by QMD search. Severity remains low because exploitation requires access to the memory tool surface and is limited to workspace Markdown files, but it bypassed the intended memory-path policy.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>&lt; 2026.4.15</code></li>
<li>Patched: <code>2026.4.15</code></li>
</ul>
<h2 id="fix">Fix</h2>
<p>OpenClaw <code>2026.4.15</code> restricts QMD reads to canonical memory paths or previously indexed QMD workspace paths. Workspace containment alone is no longer sufficient.</p>
<p>Verified in <code>v2026.4.15</code>:</p>
<ul>
<li><code>extensions/memory-core/src/memory/qmd-manager.ts</code> rejects non-default workspace Markdown paths unless they match an indexed QMD workspace read path.</li>
<li><code>extensions/memory-core/src/memory/qmd-manager.test.ts</code> covers QMD session search-result reads and the read-path restriction behavior.</li>
</ul>
<p>Fix commit included in <code>v2026.4.15</code> and absent from <code>v2026.4.14</code>:</p>
<ul>
<li><code>37d5971db36491d5050efd42c333cbe0b98ed292</code> via PR #66026</li>
</ul>
<p>Thanks to @zsxsoft, Keen Security Lab, and @qclawer for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-f934-5rqf-xx47">https://github.com/openclaw/openclaw/security/advisories/GHSA-f934-5rqf-xx47</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/66026">https://github.com/openclaw/openclaw/pull/66026</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/37d5971db36491d5050efd42c333cbe0b98ed292">https://github.com/openclaw/openclaw/commit/37d5971db36491d5050efd42c333cbe0b98ed292</a></li>
<li><a href="https://github.com/advisories/GHSA-f934-5rqf-xx47">https://github.com/advisories/GHSA-f934-5rqf-xx47</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f934-5rqf-xx47</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-17T22:33:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Webchat media embedding enforces local-root containment for tool-result files]]></title>
        <id>https://github.com/advisories/GHSA-mr34-9552-qr95</id>
        <link href="https://github.com/advisories/GHSA-mr34-9552-qr95"/>
        <updated>2026-04-17T22:33:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Webchat tool-result media normalization could pass local and UNC-style file paths into the host-side media embedding path without applying the configured local-root containment policy.</p>
<h2 id="impact">Impact</h2>
<p>A crafted tool-result media reference could cause the host to attempt local file reads or Windows UNC/network path access while preparing webchat media blocks. This could disclose allowed host files or trigger network credential exposure on affected Windows deployments. Severity remains medium because exploitation depends on a tool-result media path reaching the webchat embedding path, but the sink is a host-side file read before the user sees the rendered result.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>&gt;= 2026.4.7, &lt; 2026.4.15</code></li>
<li>Patched: <code>2026.4.15</code></li>
</ul>
<h2 id="fix">Fix</h2>
<p>OpenClaw <code>2026.4.15</code> hardens the webchat media path and the shared media resolver. Remote-host <code>file://</code> URLs and Windows network paths are rejected before filesystem access, and audio embedding now enforces configured <code>localRoots</code> containment before <code>stat</code> or read operations.</p>
<p>Verified in <code>v2026.4.15</code>:</p>
<ul>
<li><code>src/gateway/server-methods/chat-webchat-media.ts</code> uses safe file-URL parsing, rejects Windows network paths, and calls <code>assertLocalMediaAllowed</code> before probing local audio files.</li>
<li><code>src/media/web-media.ts</code> rejects remote-host <code>file://</code> URLs, Windows network paths, and local-root bypasses on the shared media path.</li>
<li><code>src/gateway/server-methods/chat-webchat-media.test.ts</code> covers both remote-host <code>file://</code> rejection and local-root denial before filesystem access.</li>
</ul>
<p>Fix commits included in <code>v2026.4.15</code> and absent from <code>v2026.4.14</code>:</p>
<ul>
<li><code>1470de5d3e0970856d86cd99336bb8ada3fe87da</code> via PR #67293</li>
<li><code>6e58f1f9f54bca1fea1268ec0ee4c01a2af03dde</code> via PR #67298</li>
<li><code>52ef42302ead9e183e6c8810e0a04ee4ef8ae9fc</code> via PR #67303 as defense-in-depth for trusted media passthrough anchoring</li>
</ul>
<p>Thanks to @Kherrisan for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mr34-9552-qr95">https://github.com/openclaw/openclaw/security/advisories/GHSA-mr34-9552-qr95</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/67293">https://github.com/openclaw/openclaw/pull/67293</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/67298">https://github.com/openclaw/openclaw/pull/67298</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/67303">https://github.com/openclaw/openclaw/pull/67303</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/1470de5d3e0970856d86cd99336bb8ada3fe87da">https://github.com/openclaw/openclaw/commit/1470de5d3e0970856d86cd99336bb8ada3fe87da</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/52ef42302ead9e183e6c8810e0a04ee4ef8ae9fc">https://github.com/openclaw/openclaw/commit/52ef42302ead9e183e6c8810e0a04ee4ef8ae9fc</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/6e58f1f9f54bca1fea1268ec0ee4c01a2af03dde">https://github.com/openclaw/openclaw/commit/6e58f1f9f54bca1fea1268ec0ee4c01a2af03dde</a></li>
<li><a href="https://github.com/advisories/GHSA-mr34-9552-qr95">https://github.com/advisories/GHSA-mr34-9552-qr95</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mr34-9552-qr95</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-17T22:33:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Feishu webhook and card-action validation now fail closed]]></title>
        <id>https://github.com/advisories/GHSA-xh72-v6v9-mwhc</id>
        <link href="https://github.com/advisories/GHSA-xh72-v6v9-mwhc"/>
        <updated>2026-04-17T22:32:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Feishu webhook mode accepted missing <code>encryptKey</code> configuration as valid and blank card-action callback tokens as usable lifecycle tokens. Together, those fail-open paths could allow unauthenticated webhook or card-action traffic to reach command dispatch in affected deployments.</p>
<h2 id="impact">Impact</h2>
<p>A deployment using Feishu webhook mode without a configured <code>encryptKey</code>, or handling malformed card-action callbacks with blank callback tokens, could fail open instead of rejecting the request. Severity remains critical because affected webhook deployments expose a network-triggered path into OpenClaw command handling without the expected Feishu signature or replay protection.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>&lt; 2026.4.15</code></li>
<li>Patched: <code>2026.4.15</code></li>
</ul>
<h2 id="fix">Fix</h2>
<p>OpenClaw <code>2026.4.15</code> makes Feishu webhook and card-action validation fail closed. Webhook mode now refuses to start without an <code>encryptKey</code>, missing signing configuration returns invalid instead of valid, invalid signatures return <code>401</code>, and blank card-action callback tokens are rejected before dispatch.</p>
<p>Verified in <code>v2026.4.15</code>:</p>
<ul>
<li><code>extensions/feishu/src/monitor.transport.ts</code> returns invalid when <code>encryptKey</code> is missing, refuses webhook mode without <code>encryptKey</code>, and rejects invalid signatures before JSON handling.</li>
<li><code>extensions/feishu/src/card-action.ts</code> rejects blank callback tokens in the card-action lifecycle guard.</li>
<li><code>extensions/feishu/src/monitor.webhook-security.test.ts</code> covers missing-<code>encryptKey</code> startup and transport rejection.</li>
<li><code>extensions/feishu/src/monitor.card-action.lifecycle.test.ts</code> covers malformed blank-token card actions being dropped before handler dispatch.</li>
</ul>
<p>Fix commit included in <code>v2026.4.15</code> and absent from <code>v2026.4.14</code>:</p>
<ul>
<li><code>c8003f1b33ed2924be5f62131bd28742c5a41aae</code> via PR #66707</li>
</ul>
<p>Thanks to @dhyabi2 for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xh72-v6v9-mwhc">https://github.com/openclaw/openclaw/security/advisories/GHSA-xh72-v6v9-mwhc</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/66707">https://github.com/openclaw/openclaw/pull/66707</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/c8003f1b33ed2924be5f62131bd28742c5a41aae">https://github.com/openclaw/openclaw/commit/c8003f1b33ed2924be5f62131bd28742c5a41aae</a></li>
<li><a href="https://github.com/advisories/GHSA-xh72-v6v9-mwhc">https://github.com/advisories/GHSA-xh72-v6v9-mwhc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xh72-v6v9-mwhc</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-17T22:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Matrix room control-command authorization no longer trusts DM pairing-store entries]]></title>
        <id>https://github.com/advisories/GHSA-2gvc-4f3c-2855</id>
        <link href="https://github.com/advisories/GHSA-2gvc-4f3c-2855"/>
        <updated>2026-04-17T22:32:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Matrix room control-command authorization used the effective allowlist for room traffic, which included sender IDs learned from the Matrix DM pairing store. A sender who was allowed only for a Matrix DM could therefore authorize room control commands when they also posted in a bot room.</p>
<h2 id="impact">Impact</h2>
<p>This bypass let a DM-paired Matrix sender cross the DM/room authorization boundary and run Matrix room control commands without being present in the configured room allowlist, room membership list, or group allowlist. The issue required a sender already present in the pairing store and able to send to the target Matrix room. Severity remains high because room control commands can drive privileged OpenClaw behavior depending on the deployment's command and tool policy.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>&gt; 2026.3.28, &lt; 2026.4.15</code></li>
<li>Patched: <code>2026.4.15</code></li>
</ul>
<h2 id="fix">Fix</h2>
<p>OpenClaw <code>2026.4.15</code> fixes the authorization boundary. Room control-command authorizers now use only configured sender IDs, effective room users, and group allowlists; DM pairing-store entries no longer authorize room commands. A follow-up change also skips DM pairing-store reads on room traffic.</p>
<p>Verified in <code>v2026.4.15</code>:</p>
<ul>
<li><code>extensions/matrix/src/matrix/monitor/access-state.ts</code> builds room command authorizers from <code>commandAllowFrom</code>, which excludes pairing-store sender IDs for room traffic.</li>
<li><code>extensions/matrix/src/matrix/monitor/handler.ts</code> gates room control commands through those authorizers.</li>
<li><code>extensions/matrix/src/matrix/monitor/access-state.test.ts</code> covers a DM pairing-store sender and verifies it cannot authorize room control commands.</li>
</ul>
<p>Fix commits included in <code>v2026.4.15</code> and absent from <code>v2026.4.14</code>:</p>
<ul>
<li><code>f8705f512b09043df02b5da372c33374734bd921</code> via PR #67294</li>
<li><code>2bfd808a83116bd888e3e2633a61473fa2ed81b6</code> via PR #67325</li>
</ul>
<p>Thanks to @nexrin and Keen Security Lab for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-2gvc-4f3c-2855">https://github.com/openclaw/openclaw/security/advisories/GHSA-2gvc-4f3c-2855</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/67294">https://github.com/openclaw/openclaw/pull/67294</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/67325">https://github.com/openclaw/openclaw/pull/67325</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/2bfd808a83116bd888e3e2633a61473fa2ed81b6">https://github.com/openclaw/openclaw/commit/2bfd808a83116bd888e3e2633a61473fa2ed81b6</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/f8705f512b09043df02b5da372c33374734bd921">https://github.com/openclaw/openclaw/commit/f8705f512b09043df02b5da372c33374734bd921</a></li>
<li><a href="https://github.com/advisories/GHSA-2gvc-4f3c-2855">https://github.com/advisories/GHSA-2gvc-4f3c-2855</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2gvc-4f3c-2855</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-17T22:32:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Gateway HTTP endpoints re-resolve bearer auth after SecretRef rotation]]></title>
        <id>https://github.com/advisories/GHSA-xmxx-7p24-h892</id>
        <link href="https://github.com/advisories/GHSA-xmxx-7p24-h892"/>
        <updated>2026-04-17T22:32:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Gateway HTTP and WebSocket handlers captured the resolved bearer-auth configuration when the server started. After a SecretRef rotation, the already-running gateway could continue accepting the old bearer token until restart.</p>
<h2 id="impact">Impact</h2>
<p>A bearer token that should have been revoked by SecretRef rotation could remain valid on the gateway HTTP and upgrade surfaces for the lifetime of the process. Severity remains high because the old token could continue to authorize gateway requests after operators believed it was rotated out.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>&lt; 2026.4.15</code></li>
<li>Patched: <code>2026.4.15</code></li>
</ul>
<h2 id="fix">Fix</h2>
<p>OpenClaw <code>2026.4.15</code> resolves active gateway auth from the runtime secret snapshot per request and per upgrade instead of using a stale startup-time value.</p>
<p>Verified in <code>v2026.4.15</code>:</p>
<ul>
<li><code>src/gateway/server.impl.ts</code> exposes <code>getResolvedAuth()</code> backed by the current runtime secret snapshot.</li>
<li><code>src/gateway/server-http.ts</code> calls <code>getResolvedAuth()</code> for each HTTP request and WebSocket upgrade before running auth checks.</li>
<li><code>src/gateway/server-http.probe.test.ts</code> verifies <code>/ready</code> re-resolves bearer auth after rotation and rejects the old token.</li>
</ul>
<p>Fix commit included in <code>v2026.4.15</code> and absent from <code>v2026.4.14</code>:</p>
<ul>
<li><code>acd4e0a32f12e1ad85f3130f63b42443ce90f094</code> via PR #66651</li>
</ul>
<p>Thanks to @zsxsoft, Keen Security Lab, and @qclawer for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xmxx-7p24-h892">https://github.com/openclaw/openclaw/security/advisories/GHSA-xmxx-7p24-h892</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/66651">https://github.com/openclaw/openclaw/pull/66651</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/acd4e0a32f12e1ad85f3130f63b42443ce90f094">https://github.com/openclaw/openclaw/commit/acd4e0a32f12e1ad85f3130f63b42443ce90f094</a></li>
<li><a href="https://github.com/advisories/GHSA-xmxx-7p24-h892">https://github.com/advisories/GHSA-xmxx-7p24-h892</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xmxx-7p24-h892</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-17T22:32:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[math-codegen] Remote Code Execution (RCE) via String Literal Injection into math-codegen]]></title>
        <id>https://github.com/advisories/GHSA-p6x5-p4xf-cc4r</id>
        <link href="https://github.com/advisories/GHSA-p6x5-p4xf-cc4r"/>
        <updated>2026-04-17T22:31:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>String literal content passed to <code>cg.parse()</code> is injected verbatim into a <code>new Function()</code> body without sanitization. This allows an attacker to execute arbitrary system commands when user-controlled input reaches the parser. Any application exposing a math evaluation endpoint where user input flows into <code>cg.parse()</code> is vulnerable to full RCE.</p>
<h3 id="patches">Patches</h3>
<p>The vulnerability is addressed by using <code>JSON.stringify()</code> on string literal values in <code>lib/node/ConstantNode.js</code> to ensure they are treated as data rather than code. Users should upgrade to version 0.4.3 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid passing un-sanitized user input to the parser or manually escape string literals in the input.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mauriciopoppe/math-codegen/security/advisories/GHSA-p6x5-p4xf-cc4r">https://github.com/mauriciopoppe/math-codegen/security/advisories/GHSA-p6x5-p4xf-cc4r</a></li>
<li><a href="https://github.com/mauriciopoppe/math-codegen/pull/11">https://github.com/mauriciopoppe/math-codegen/pull/11</a></li>
<li><a href="https://github.com/hits3134">https://github.com/hits3134</a></li>
<li><a href="https://github.com/advisories/GHSA-p6x5-p4xf-cc4r">https://github.com/advisories/GHSA-p6x5-p4xf-cc4r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p6x5-p4xf-cc4r</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-17T22:31:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anthropic-ai/claude-code] Claude Code: Insecure System-Wide Configuration Loading Enables Local Privilege Escalation on Windows]]></title>
        <id>https://github.com/advisories/GHSA-5cwg-9f6j-9jvx</id>
        <link href="https://github.com/advisories/GHSA-5cwg-9f6j-9jvx"/>
        <updated>2026-04-17T22:19:39.000Z</updated>
        <content type="html"><![CDATA[<p>On Windows, Claude Code loaded system-wide default configuration from <code>C:\ProgramData\ClaudeCode\managed-settings.json</code> without validating directory ownership or access permissions. Because the <code>ProgramData</code> directory is writable by non-administrative users by default and the <code>ClaudeCode</code> subdirectory was not pre-created or access-restricted, a low-privileged local user could create this directory and place a malicious configuration file that would be automatically loaded for any user launching Claude Code on the same machine. Exploiting this would have required a shared multi-user Windows system and a victim user to launch Claude Code after the malicious configuration was placed.</p>
<p>Users on standard Claude Code auto-update have received this fix already. Users performing manual updates are advised to update to the latest version.</p>
<p>Thank you to hackerone.com/edbr for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anthropics/claude-code/security/advisories/GHSA-5cwg-9f6j-9jvx">https://github.com/anthropics/claude-code/security/advisories/GHSA-5cwg-9f6j-9jvx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35603">https://nvd.nist.gov/vuln/detail/CVE-2026-35603</a></li>
<li><a href="https://github.com/advisories/GHSA-5cwg-9f6j-9jvx">https://github.com/advisories/GHSA-5cwg-9f6j-9jvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5cwg-9f6j-9jvx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-17T22:19:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: CDP /json/version WebSocket URL could pivot to untrusted second-hop targets]]></title>
        <id>https://github.com/advisories/GHSA-f7fh-qg34-x2xh</id>
        <link href="https://github.com/advisories/GHSA-f7fh-qg34-x2xh"/>
        <updated>2026-04-17T22:18:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>CDP /json/version WebSocket URL could pivot to untrusted second-hop targets.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code></li>
<li>Ecosystem: npm</li>
<li>Affected versions: <code>&lt; 2026.4.5</code></li>
<li>Patched versions: <code>&gt;= 2026.4.5</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>A browser profile could trust a CDP <code>/json/version</code> response whose <code>webSocketDebuggerUrl</code> pointed at a different host, enabling a second-hop SSRF-style pivot.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The fix normalizes and re-validates direct CDP WebSocket targets before connecting.</p>
<h2 id="fix">Fix</h2>
<p>The issue was fixed in #60469. The first stable tag containing the fix is <code>v2026.4.5</code>, and <code>openclaw@2026.4.14</code> includes the fix.</p>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>bc356cc8c2beaa747c71dd86cceab8f804699665</code></li>
<li>PR: #60469</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<p>Users should upgrade to <code>openclaw</code> 2026.4.5 or newer. The latest npm release, <code>2026.4.14</code>, already includes the fix.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to @tdjackey for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-f7fh-qg34-x2xh">https://github.com/openclaw/openclaw/security/advisories/GHSA-f7fh-qg34-x2xh</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/60469">https://github.com/openclaw/openclaw/pull/60469</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/bc356cc8c2beaa747c71dd86cceab8f804699665">https://github.com/openclaw/openclaw/commit/bc356cc8c2beaa747c71dd86cceab8f804699665</a></li>
<li><a href="https://github.com/advisories/GHSA-f7fh-qg34-x2xh">https://github.com/advisories/GHSA-f7fh-qg34-x2xh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f7fh-qg34-x2xh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-17T22:18:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Sender policy bypass in host media attachment reads allows unauthorized local file disclosure]]></title>
        <id>https://github.com/advisories/GHSA-jhpv-5j76-m56h</id>
        <link href="https://github.com/advisories/GHSA-jhpv-5j76-m56h"/>
        <updated>2026-04-17T22:17:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>OpenClaw's outbound host-media attachment read helper could enable host-local file reads based on global or agent-level read access without also honoring sender and group-scoped tool policy. In channel deployments that used <code>toolsBySender</code> or group policy to deny <code>read</code> for less-trusted senders, a denied sender could still trigger host-media attachment loading and cause readable local files to be returned through the outbound media path.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>This issue is known to affect OpenClaw 2026.4.9. Earlier versions were not confirmed during triage, so the advisory range is intentionally scoped to <code>&gt;= 2026.4.9 &lt; 2026.4.10</code>.</p>
<h2 id="impact">Impact</h2>
<p>Affected deployments are those that both allow host read or filesystem root expansion at the global/agent level and rely on sender or group-scoped policy to deny <code>read</code> for some channel participants. In that configuration, the intended sender/group authorization boundary could be bypassed for outbound media reads, potentially disclosing host-local files readable by the OpenClaw process.</p>
<p>The issue does not require treating the model prompt as the security boundary. The vulnerable behavior was a concrete policy enforcement mismatch: sender/group policy denied <code>read</code>, while the host-media read helper could still be installed without that sender context.</p>
<h2 id="resolution">Resolution</h2>
<p>Fixed in OpenClaw 2026.4.10 by PR #64459, commit <code>c949af9fabf3873b5b7c484090cb5f5ab6049a98</code>. The fix threads sender, session, channel, and account context into outbound media access resolution and intersects host-media read capability creation with the existing group tool policy for <code>read</code>. When a concrete sender/group override denies <code>read</code>, OpenClaw no longer creates the host <code>readFile</code> media capability.</p>
<p>Additional attachment canonicalization hardening shipped in 2026.4.14, but the authorization bypass described here was fixed in 2026.4.10.</p>
<h2 id="credit">Credit</h2>
<p>Thanks to @Telecaster2147 for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-jhpv-5j76-m56h">https://github.com/openclaw/openclaw/security/advisories/GHSA-jhpv-5j76-m56h</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/64459">https://github.com/openclaw/openclaw/pull/64459</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/c949af9fabf3873b5b7c484090cb5f5ab6049a98">https://github.com/openclaw/openclaw/commit/c949af9fabf3873b5b7c484090cb5f5ab6049a98</a></li>
<li><a href="https://github.com/advisories/GHSA-jhpv-5j76-m56h">https://github.com/advisories/GHSA-jhpv-5j76-m56h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jhpv-5j76-m56h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-17T22:17:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: QQBot media tags could read arbitrary local files through reply text]]></title>
        <id>https://github.com/advisories/GHSA-66r7-m7xm-v49h</id>
        <link href="https://github.com/advisories/GHSA-66r7-m7xm-v49h"/>
        <updated>2026-04-17T22:17:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>QQBot media tags could read arbitrary local files through reply text.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code></li>
<li>Ecosystem: npm</li>
<li>Affected versions: <code>&lt; 2026.4.10</code></li>
<li>Patched versions: <code>&gt;= 2026.4.10</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>QQBot outbound media tags in AI reply text could reference host-local paths outside the intended media storage boundary, allowing local file disclosure through outbound media handling.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The fix enforces the media storage boundary for all outbound QQBot local file paths.</p>
<h2 id="fix">Fix</h2>
<p>The issue was fixed in #63271. The first stable tag containing the fix is <code>v2026.4.10</code>, and <code>openclaw@2026.4.14</code> includes the fix.</p>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>604777e4414cc3b2ff8861f18f4fb04374c702c6</code></li>
<li>PR: #63271</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<p>Users should upgrade to <code>openclaw</code> 2026.4.10 or newer. The latest npm release, <code>2026.4.14</code>, already includes the fix.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to @feiyang666 of Tencent zhuque Lab (<a href="https://github.com/Tencent/AI-Infra-Guard">https://github.com/Tencent/AI-Infra-Guard</a>) for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-66r7-m7xm-v49h">https://github.com/openclaw/openclaw/security/advisories/GHSA-66r7-m7xm-v49h</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/63271">https://github.com/openclaw/openclaw/pull/63271</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/604777e4414cc3b2ff8861f18f4fb04374c702c6">https://github.com/openclaw/openclaw/commit/604777e4414cc3b2ff8861f18f4fb04374c702c6</a></li>
<li><a href="https://github.com/advisories/GHSA-66r7-m7xm-v49h">https://github.com/advisories/GHSA-66r7-m7xm-v49h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-66r7-m7xm-v49h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-17T22:17:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: busybox and toybox applet execution weakened exec approval binding]]></title>
        <id>https://github.com/advisories/GHSA-2cq5-mf3v-mx44</id>
        <link href="https://github.com/advisories/GHSA-2cq5-mf3v-mx44"/>
        <updated>2026-04-17T22:16:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>busybox and toybox applet execution weakened exec approval binding.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code></li>
<li>Ecosystem: npm</li>
<li>Affected versions: <code>&gt;= 2026.2.23 &lt; 2026.4.12</code></li>
<li>Patched versions: <code>&gt;= 2026.4.12</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>Opaque multi-call binaries such as <code>busybox</code> and <code>toybox</code> could obscure which applet or script-like behavior would actually run, weakening exec approval binding and risk classification.</p>
<h2 id="technical-details">Technical Details</h2>
<p>The fix treats <code>busybox</code> and <code>toybox</code> as opaque mutable script runners and fails closed rather than binding unsafe applet invocations.</p>
<h2 id="fix">Fix</h2>
<p>The issue was fixed in #65713. The first stable tag containing the fix is <code>v2026.4.12</code>, and <code>openclaw@2026.4.14</code> includes the fix.</p>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>666f48d9b882a8a1415ca53f9567c72499d850c9</code></li>
<li>PR: #65713</li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<p>Users should upgrade to <code>openclaw</code> 2026.4.12 or newer. The latest npm release, <code>2026.4.14</code>, already includes the fix.</p>
<h2 id="credits">Credits</h2>
<p>Thanks to @decsecre583 for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-2cq5-mf3v-mx44">https://github.com/openclaw/openclaw/security/advisories/GHSA-2cq5-mf3v-mx44</a></li>
<li><a href="https://github.com/openclaw/openclaw/pull/65713">https://github.com/openclaw/openclaw/pull/65713</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/666f48d9b882a8a1415ca53f9567c72499d850c9">https://github.com/openclaw/openclaw/commit/666f48d9b882a8a1415ca53f9567c72499d850c9</a></li>
<li><a href="https://github.com/advisories/GHSA-2cq5-mf3v-mx44">https://github.com/advisories/GHSA-2cq5-mf3v-mx44</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2cq5-mf3v-mx44</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-17T22:16:04.000Z</published>
    </entry>
</feed>