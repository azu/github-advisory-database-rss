<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-07T00:19:59.094Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[vercel] Vercel: Non-interactive mode includes CLI arguments in suggested command output]]></title>
        <id>https://github.com/advisories/GHSA-pgf8-2hgj-grqg</id>
        <link href="https://github.com/advisories/GHSA-pgf8-2hgj-grqg"/>
        <updated>2026-05-07T00:05:21.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>When the Vercel CLI runs in non-interactive mode (<code>--non-interactive</code> or auto-detected AI agent), commands that cannot complete autonomously emit JSON payloads with suggested follow-up commands. If the user authenticated via <code>--token</code> or <code>-t</code> on the command line, the token value is included verbatim in those suggestions.</p>
<h1 id="conditions">Conditions</h1>
<p>All three must be true for the token to appear in output:</p>
<ol>
<li>Token passed as a CLI argument (<code>--token</code> / <code>-t</code>). The <code>VERCEL_TOKEN</code> environment variable is <strong>not affected</strong>.</li>
<li>Non-interactive mode is active (explicit flag or AI agent auto-detection).</li>
<li>The command cannot complete on its own (e.g. missing <code>--yes</code>, ambiguous scope, API errors). Successful commands produce no suggestion output.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>The plaintext token may be captured in CI/CD logs, agent transcripts, or other automation output.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Upgrade to the patched version.</li>
<li>If developers have previously used <code>--token</code> with <code>--non-interactive</code> in their applications, review logs for exposed tokens and rotate them.</li>
<li>Prefer <code>VERCEL_TOKEN</code> environment variable for authentication.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vercel/vercel/security/advisories/GHSA-pgf8-2hgj-grqg">https://github.com/vercel/vercel/security/advisories/GHSA-pgf8-2hgj-grqg</a></li>
<li><a href="https://github.com/advisories/GHSA-pgf8-2hgj-grqg">https://github.com/advisories/GHSA-pgf8-2hgj-grqg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pgf8-2hgj-grqg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T00:05:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] Hono: bodyLimit() can be bypassed for chunked / unknown-length requests]]></title>
        <id>https://github.com/advisories/GHSA-9vqf-7f2p-gf9v</id>
        <link href="https://github.com/advisories/GHSA-9vqf-7f2p-gf9v"/>
        <updated>2026-05-06T23:50:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>bodyLimit()</code> does not reliably enforce <code>maxSize</code> for requests without a usable <code>Content-Length</code> (e.g. <code>Transfer-Encoding: chunked</code>). Oversized requests can reach handlers and return <code>200</code> instead of <code>413</code>.</p>
<h2 id="details">Details</h2>
<p>For chunked / unknown-length requests, <code>bodyLimit()</code> wraps the body in a stream that counts bytes asynchronously, then runs the handler before the size decision is final. The <code>413</code> is only applied afterwards by checking <code>c.error</code>.</p>
<p>This lets the limit be bypassed when:</p>
<ul>
<li>the handler does not read the body,</li>
<li>the handler reads only the first chunk(s) and returns, or</li>
<li>the handler reads the body but swallows the read error in <code>try/catch</code>.</li>
</ul>
<p>In all three cases the handler returns <code>200</code> before the limit check completes (or its result is observed).</p>
<p>The fix is to enforce the size decision before <code>next()</code> runs, instead of retrofitting the response via <code>c.error</code> afterwards.</p>
<h2 id="impact">Impact</h2>
<p>Applications relying on <code>bodyLimit()</code> as a hard boundary can be bypassed: oversized chunked requests can reach handler logic and return successful responses. Per-request data exposure is bounded by <code>maxSize</code>, but the documented guarantee — "oversized requests are rejected before business logic runs" — does not hold.</p>
<h2 id="credits">Credits</h2>
<ul>
<li>@lalalala5678 (slow chunked / early return variants)</li>
<li>@Jvr2022 (error handling bypass)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-9vqf-7f2p-gf9v">https://github.com/honojs/hono/security/advisories/GHSA-9vqf-7f2p-gf9v</a></li>
<li><a href="https://github.com/advisories/GHSA-9vqf-7f2p-gf9v">https://github.com/advisories/GHSA-9vqf-7f2p-gf9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9vqf-7f2p-gf9v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:50:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono/jsx has Unvalidated JSX Tag Names that May Allow HTML Injection]]></title>
        <id>https://github.com/advisories/GHSA-69xw-7hcm-h432</id>
        <link href="https://github.com/advisories/GHSA-69xw-7hcm-h432"/>
        <updated>2026-05-06T23:49:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Improper handling of JSX element tag names in hono/jsx allowed unvalidated tag names to be directly inserted into the generated HTML output.</p>
<p>When untrusted input is used as a tag name via the programmatic <code>jsx()</code> or <code>createElement()</code> APIs during server-side rendering, specially crafted values may break out of the intended element context and inject unintended HTML.</p>
<h2 id="details">Details</h2>
<p>When rendering JSX elements to HTML strings, attribute values are escaped and attribute names are validated. However, element tag names were previously inserted into the output without validation.</p>
<p>If a tag name contains characters such as <code>&lt;</code>, <code>&gt;</code>, quotes, or whitespace, it may alter the structure of the generated HTML.</p>
<p>For example, malformed tag names can:</p>
<ul>
<li>Break out of the intended element and introduce unintended HTML elements</li>
<li>Inject attributes or event handlers into the rendered output</li>
</ul>
<p>This issue arises when untrusted input (such as query parameters or database content) is used as JSX tag names via <code>jsx()</code> or <code>createElement()</code> during server-side rendering.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can control tag names used in JSX rendering may inject unintended HTML into the generated output.</p>
<p>This may lead to:</p>
<ul>
<li>Injection of unexpected HTML elements or attributes</li>
<li>Corruption of the HTML structure</li>
<li>Cross-site scripting (XSS) when combined with unsafe usage patterns</li>
</ul>
<p>This issue only affects applications that construct JSX tag names from untrusted input. Applications using static or allowlisted tag names are not affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-69xw-7hcm-h432">https://github.com/honojs/hono/security/advisories/GHSA-69xw-7hcm-h432</a></li>
<li><a href="https://github.com/advisories/GHSA-69xw-7hcm-h432">https://github.com/advisories/GHSA-69xw-7hcm-h432</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xw-7hcm-h432</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:49:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/ssr] Angular SSR has Open Redirect and Request Steering via Encoded X-Forwarded-Prefix]]></title>
        <id>https://github.com/advisories/GHSA-69xr-m8h6-h664</id>
        <link href="https://github.com/advisories/GHSA-69xr-m8h6-h664"/>
        <updated>2026-05-06T23:42:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A vulnerability exists in the <code>X-Forwarded-Prefix</code> header processing logic within Angular SSR. The internal validation mechanism fails to properly account for URL-encoded characters, specifically dots (<code>%2e%2e</code>). This allows an attacker to bypass security filters by injecting encoded path traversal sequences that are later decoded and utilized by the application logic.
When an Angular SSR application is configured to trust proxy headers and is deployed behind a proxy that forwards the <code>X-Forwarded-Prefix</code> header without prior sanitization, an attacker can provide a payload such as <code>/%2e%2e/evil</code>.</p>
<p>The vulnerability manifests in two ways:</p>
<ul>
<li>Open Redirect: The application processes a redirect (e.g., router <code>redirectTo</code>). The decoded traversal payload manipulates the Location header, forcing the browser to an unintended path or external domain.</li>
<li>Server-Side Request Steering: The manipulated prefix is used as the base path for server-side <code>HttpClient</code> requests. This causes the server to make requests to unintended internal paths or external endpoints.</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<ul>
<li>The application must use Angular SSR.</li>
<li>The application must perform internal redirects or use relative URLs in server-side <code>HttpClient</code> requests.</li>
<li>The upstream infrastructure (Reverse Proxy/CDN) must pass the <code>X-Forwarded-Prefix</code> header to the SSR process without stripping or sanitizing it.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Until the patch is applied, developers should manually sanitize the <code>X-Forwarded-Prefix</code> header in their <code>server.ts</code>. The workaround involves decoding the component to catch encoded traversal attempts before normalization:</p>
<pre><code class="language-ts">app.use((req, res, next) =&gt; {
  let prefix = req.headers['x-forwarded-prefix'];
  if (Array.isArray(prefix)) prefix = prefix[0];

  if (prefix) {
    try {
      // Decode the prefix to catch encoded characters like %2e (dots)
      const decodedPrefix = decodeURIComponent(prefix);
      
      // Sanitize: remove traversal attempts and ensure a safe leading slash
      req.headers['x-forwarded-prefix'] = decodedPrefix
        .replace(/\.\./g, '')       // Remove any dot-dot sequences
        .replace(/^[/\\]+/, '/');   // Ensure it starts with exactly one slash
    } catch (e) {
      // If decoding fails, remove the potentially malicious header entirely
      delete req.headers['x-forwarded-prefix'];
    }
  }
  next();
});
</code></pre>
<h3 id="configuring-trusted-proxy-headers">Configuring Trusted Proxy Headers</h3>
<p>By default, Angular ignores all X-Forwarded-* headers. If your application is behind a trusted reverse proxy (like a load balancer) that sets these headers, you can configure Angular to trust them.
You can configure trustProxyHeaders when initializing the application engine:</p>
<pre><code class="language-ts">const appEngine = new AngularAppEngine({
  // Trust specific headers
  trustProxyHeaders: ['x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-prefix'], 
});

const nodeAppEngine = new AngularNodeAppEngine({
  // Trust all X-Forwarded-* headers
  trustProxyHeaders: true, 
});
</code></pre>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.7</li>
<li>21.2.9</li>
<li>20.3.25</li>
<li>19.2.25</li>
</ul>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664">https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664</a></li>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
<li><a href="https://github.com/advisories/GHSA-69xr-m8h6-h664">https://github.com/advisories/GHSA-69xr-m8h6-h664</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xr-m8h6-h664</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:42:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/ssr] Angular SSR has Open Redirect and Request Steering via Encoded X-Forwarded-Prefix]]></title>
        <id>https://github.com/advisories/GHSA-69xr-m8h6-h664</id>
        <link href="https://github.com/advisories/GHSA-69xr-m8h6-h664"/>
        <updated>2026-05-06T23:42:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A vulnerability exists in the <code>X-Forwarded-Prefix</code> header processing logic within Angular SSR. The internal validation mechanism fails to properly account for URL-encoded characters, specifically dots (<code>%2e%2e</code>). This allows an attacker to bypass security filters by injecting encoded path traversal sequences that are later decoded and utilized by the application logic.
When an Angular SSR application is configured to trust proxy headers and is deployed behind a proxy that forwards the <code>X-Forwarded-Prefix</code> header without prior sanitization, an attacker can provide a payload such as <code>/%2e%2e/evil</code>.</p>
<p>The vulnerability manifests in two ways:</p>
<ul>
<li>Open Redirect: The application processes a redirect (e.g., router <code>redirectTo</code>). The decoded traversal payload manipulates the Location header, forcing the browser to an unintended path or external domain.</li>
<li>Server-Side Request Steering: The manipulated prefix is used as the base path for server-side <code>HttpClient</code> requests. This causes the server to make requests to unintended internal paths or external endpoints.</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<ul>
<li>The application must use Angular SSR.</li>
<li>The application must perform internal redirects or use relative URLs in server-side <code>HttpClient</code> requests.</li>
<li>The upstream infrastructure (Reverse Proxy/CDN) must pass the <code>X-Forwarded-Prefix</code> header to the SSR process without stripping or sanitizing it.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Until the patch is applied, developers should manually sanitize the <code>X-Forwarded-Prefix</code> header in their <code>server.ts</code>. The workaround involves decoding the component to catch encoded traversal attempts before normalization:</p>
<pre><code class="language-ts">app.use((req, res, next) =&gt; {
  let prefix = req.headers['x-forwarded-prefix'];
  if (Array.isArray(prefix)) prefix = prefix[0];

  if (prefix) {
    try {
      // Decode the prefix to catch encoded characters like %2e (dots)
      const decodedPrefix = decodeURIComponent(prefix);
      
      // Sanitize: remove traversal attempts and ensure a safe leading slash
      req.headers['x-forwarded-prefix'] = decodedPrefix
        .replace(/\.\./g, '')       // Remove any dot-dot sequences
        .replace(/^[/\\]+/, '/');   // Ensure it starts with exactly one slash
    } catch (e) {
      // If decoding fails, remove the potentially malicious header entirely
      delete req.headers['x-forwarded-prefix'];
    }
  }
  next();
});
</code></pre>
<h3 id="configuring-trusted-proxy-headers">Configuring Trusted Proxy Headers</h3>
<p>By default, Angular ignores all X-Forwarded-* headers. If your application is behind a trusted reverse proxy (like a load balancer) that sets these headers, you can configure Angular to trust them.
You can configure trustProxyHeaders when initializing the application engine:</p>
<pre><code class="language-ts">const appEngine = new AngularAppEngine({
  // Trust specific headers
  trustProxyHeaders: ['x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-prefix'], 
});

const nodeAppEngine = new AngularNodeAppEngine({
  // Trust all X-Forwarded-* headers
  trustProxyHeaders: true, 
});
</code></pre>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.7</li>
<li>21.2.9</li>
<li>20.3.25</li>
<li>19.2.25</li>
</ul>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664">https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664</a></li>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
<li><a href="https://github.com/advisories/GHSA-69xr-m8h6-h664">https://github.com/advisories/GHSA-69xr-m8h6-h664</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xr-m8h6-h664</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:42:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/ssr] Angular SSR has Open Redirect and Request Steering via Encoded X-Forwarded-Prefix]]></title>
        <id>https://github.com/advisories/GHSA-69xr-m8h6-h664</id>
        <link href="https://github.com/advisories/GHSA-69xr-m8h6-h664"/>
        <updated>2026-05-06T23:42:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A vulnerability exists in the <code>X-Forwarded-Prefix</code> header processing logic within Angular SSR. The internal validation mechanism fails to properly account for URL-encoded characters, specifically dots (<code>%2e%2e</code>). This allows an attacker to bypass security filters by injecting encoded path traversal sequences that are later decoded and utilized by the application logic.
When an Angular SSR application is configured to trust proxy headers and is deployed behind a proxy that forwards the <code>X-Forwarded-Prefix</code> header without prior sanitization, an attacker can provide a payload such as <code>/%2e%2e/evil</code>.</p>
<p>The vulnerability manifests in two ways:</p>
<ul>
<li>Open Redirect: The application processes a redirect (e.g., router <code>redirectTo</code>). The decoded traversal payload manipulates the Location header, forcing the browser to an unintended path or external domain.</li>
<li>Server-Side Request Steering: The manipulated prefix is used as the base path for server-side <code>HttpClient</code> requests. This causes the server to make requests to unintended internal paths or external endpoints.</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<ul>
<li>The application must use Angular SSR.</li>
<li>The application must perform internal redirects or use relative URLs in server-side <code>HttpClient</code> requests.</li>
<li>The upstream infrastructure (Reverse Proxy/CDN) must pass the <code>X-Forwarded-Prefix</code> header to the SSR process without stripping or sanitizing it.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Until the patch is applied, developers should manually sanitize the <code>X-Forwarded-Prefix</code> header in their <code>server.ts</code>. The workaround involves decoding the component to catch encoded traversal attempts before normalization:</p>
<pre><code class="language-ts">app.use((req, res, next) =&gt; {
  let prefix = req.headers['x-forwarded-prefix'];
  if (Array.isArray(prefix)) prefix = prefix[0];

  if (prefix) {
    try {
      // Decode the prefix to catch encoded characters like %2e (dots)
      const decodedPrefix = decodeURIComponent(prefix);
      
      // Sanitize: remove traversal attempts and ensure a safe leading slash
      req.headers['x-forwarded-prefix'] = decodedPrefix
        .replace(/\.\./g, '')       // Remove any dot-dot sequences
        .replace(/^[/\\]+/, '/');   // Ensure it starts with exactly one slash
    } catch (e) {
      // If decoding fails, remove the potentially malicious header entirely
      delete req.headers['x-forwarded-prefix'];
    }
  }
  next();
});
</code></pre>
<h3 id="configuring-trusted-proxy-headers">Configuring Trusted Proxy Headers</h3>
<p>By default, Angular ignores all X-Forwarded-* headers. If your application is behind a trusted reverse proxy (like a load balancer) that sets these headers, you can configure Angular to trust them.
You can configure trustProxyHeaders when initializing the application engine:</p>
<pre><code class="language-ts">const appEngine = new AngularAppEngine({
  // Trust specific headers
  trustProxyHeaders: ['x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-prefix'], 
});

const nodeAppEngine = new AngularNodeAppEngine({
  // Trust all X-Forwarded-* headers
  trustProxyHeaders: true, 
});
</code></pre>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.7</li>
<li>21.2.9</li>
<li>20.3.25</li>
<li>19.2.25</li>
</ul>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664">https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664</a></li>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
<li><a href="https://github.com/advisories/GHSA-69xr-m8h6-h664">https://github.com/advisories/GHSA-69xr-m8h6-h664</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xr-m8h6-h664</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:42:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/ssr] Angular SSR has Open Redirect and Request Steering via Encoded X-Forwarded-Prefix]]></title>
        <id>https://github.com/advisories/GHSA-69xr-m8h6-h664</id>
        <link href="https://github.com/advisories/GHSA-69xr-m8h6-h664"/>
        <updated>2026-05-06T23:42:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A vulnerability exists in the <code>X-Forwarded-Prefix</code> header processing logic within Angular SSR. The internal validation mechanism fails to properly account for URL-encoded characters, specifically dots (<code>%2e%2e</code>). This allows an attacker to bypass security filters by injecting encoded path traversal sequences that are later decoded and utilized by the application logic.
When an Angular SSR application is configured to trust proxy headers and is deployed behind a proxy that forwards the <code>X-Forwarded-Prefix</code> header without prior sanitization, an attacker can provide a payload such as <code>/%2e%2e/evil</code>.</p>
<p>The vulnerability manifests in two ways:</p>
<ul>
<li>Open Redirect: The application processes a redirect (e.g., router <code>redirectTo</code>). The decoded traversal payload manipulates the Location header, forcing the browser to an unintended path or external domain.</li>
<li>Server-Side Request Steering: The manipulated prefix is used as the base path for server-side <code>HttpClient</code> requests. This causes the server to make requests to unintended internal paths or external endpoints.</li>
</ul>
<h3 id="attack-preconditions">Attack Preconditions</h3>
<ul>
<li>The application must use Angular SSR.</li>
<li>The application must perform internal redirects or use relative URLs in server-side <code>HttpClient</code> requests.</li>
<li>The upstream infrastructure (Reverse Proxy/CDN) must pass the <code>X-Forwarded-Prefix</code> header to the SSR process without stripping or sanitizing it.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Until the patch is applied, developers should manually sanitize the <code>X-Forwarded-Prefix</code> header in their <code>server.ts</code>. The workaround involves decoding the component to catch encoded traversal attempts before normalization:</p>
<pre><code class="language-ts">app.use((req, res, next) =&gt; {
  let prefix = req.headers['x-forwarded-prefix'];
  if (Array.isArray(prefix)) prefix = prefix[0];

  if (prefix) {
    try {
      // Decode the prefix to catch encoded characters like %2e (dots)
      const decodedPrefix = decodeURIComponent(prefix);
      
      // Sanitize: remove traversal attempts and ensure a safe leading slash
      req.headers['x-forwarded-prefix'] = decodedPrefix
        .replace(/\.\./g, '')       // Remove any dot-dot sequences
        .replace(/^[/\\]+/, '/');   // Ensure it starts with exactly one slash
    } catch (e) {
      // If decoding fails, remove the potentially malicious header entirely
      delete req.headers['x-forwarded-prefix'];
    }
  }
  next();
});
</code></pre>
<h3 id="configuring-trusted-proxy-headers">Configuring Trusted Proxy Headers</h3>
<p>By default, Angular ignores all X-Forwarded-* headers. If your application is behind a trusted reverse proxy (like a load balancer) that sets these headers, you can configure Angular to trust them.
You can configure trustProxyHeaders when initializing the application engine:</p>
<pre><code class="language-ts">const appEngine = new AngularAppEngine({
  // Trust specific headers
  trustProxyHeaders: ['x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-prefix'], 
});

const nodeAppEngine = new AngularNodeAppEngine({
  // Trust all X-Forwarded-* headers
  trustProxyHeaders: true, 
});
</code></pre>
<h3 id="patches">Patches</h3>
<ul>
<li>22.0.0-next.7</li>
<li>21.2.9</li>
<li>20.3.25</li>
<li>19.2.25</li>
</ul>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664">https://github.com/angular/angular-cli/security/advisories/GHSA-69xr-m8h6-h664</a></li>
<li><a href="https://github.com/angular/angular-cli/pull/33031">https://github.com/angular/angular-cli/pull/33031</a></li>
<li><a href="https://angular.dev/best-practices/security#configuring-trusted-proxy-headers">https://angular.dev/best-practices/security#configuring-trusted-proxy-headers</a></li>
<li><a href="https://github.com/advisories/GHSA-69xr-m8h6-h664">https://github.com/advisories/GHSA-69xr-m8h6-h664</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xr-m8h6-h664</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:42:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-server-semgrep] mcp-server-semgrep has a Command Injection issue]]></title>
        <id>https://github.com/advisories/GHSA-86hp-qxqp-w9wv</id>
        <link href="https://github.com/advisories/GHSA-86hp-qxqp-w9wv"/>
        <updated>2026-05-06T23:28:37.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was detected in VetCoders mcp-server-semgrep 1.0.0. This affects the function analyze_results/filter_results/export_results/compare_results/scan_directory/create_rule of the file src/index.ts of the component MCP Interface. The manipulation of the argument ID results in os command injection. The attack can be executed remotely. The exploit is now public and may be used. Upgrading to version 1.0.1 is able to mitigate this issue. The patch is identified as 141335da044e53c3f5b315e0386e01238405b771. It is advisable to upgrade the affected component.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7446">https://nvd.nist.gov/vuln/detail/CVE-2026-7446</a></li>
<li><a href="https://github.com/VetCoders/mcp-server-semgrep/issues/12">https://github.com/VetCoders/mcp-server-semgrep/issues/12</a></li>
<li><a href="https://github.com/VetCoders/mcp-server-semgrep/pull/15">https://github.com/VetCoders/mcp-server-semgrep/pull/15</a></li>
<li><a href="https://github.com/VetCoders/mcp-server-semgrep/commit/141335da044e53c3f5b315e0386e01238405b771">https://github.com/VetCoders/mcp-server-semgrep/commit/141335da044e53c3f5b315e0386e01238405b771</a></li>
<li><a href="https://github.com/VetCoders/mcp-server-semgrep">https://github.com/VetCoders/mcp-server-semgrep</a></li>
<li><a href="https://github.com/VetCoders/mcp-server-semgrep/releases/tag/v1.0.1">https://github.com/VetCoders/mcp-server-semgrep/releases/tag/v1.0.1</a></li>
<li><a href="https://vuldb.com/submit/804100">https://vuldb.com/submit/804100</a></li>
<li><a href="https://vuldb.com/vuln/360187">https://vuldb.com/vuln/360187</a></li>
<li><a href="https://vuldb.com/vuln/360187/cti">https://vuldb.com/vuln/360187/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-86hp-qxqp-w9wv">https://github.com/advisories/GHSA-86hp-qxqp-w9wv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-86hp-qxqp-w9wv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-30T00:31:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@axonflow/openclaw] @axonflow/openclaw fix introduces plugin cache and credential-file permission hardening]]></title>
        <id>https://github.com/advisories/GHSA-cqmh-pcgr-q42f</id>
        <link href="https://github.com/advisories/GHSA-cqmh-pcgr-q42f"/>
        <updated>2026-05-06T23:23:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Two related permission defects in this AxonFlow plugin allowed registration credentials and cache state to be readable by other local users on hosts where the calling user's home directory was at the conventional <code>0755</code> mode.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>Versions 1.3.2 and below.</p>
<h2 id="impact">Impact</h2>
<ol>
<li><strong>Cache and config directory mode.</strong> The plugin's directories under <code>~/.config/axonflow/</code> and <code>~/.cache/axonflow/</code> were created with the umask-derived default mode (often <code>0755</code>) on first use and not subsequently re-validated. On systems where <code>~/.config/</code> is itself <code>0755</code>, the plugin's registration record (including a hashed credential and <code>instance_id</code>) was traversable by other local users.</li>
<li><strong>Credential file mode at load time.</strong> The plugin loaded its <code>try-registration.json</code> credential file without validating that the file mode was <code>0600</code>. A registration file written by a misconfigured tool, copied across systems, or restored from backup could end up world-readable, and the plugin would silently use it.</li>
</ol>
<p>The fix restores <code>0700</code> on all plugin directories on every plugin invocation (not only first creation) and refuses to load credential files with non-<code>0600</code> modes.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to the patched plugin version listed under Vulnerabilities. On startup the plugin will repair existing directory modes; existing credential files with overly permissive modes will be refused, requiring the user to re-register or <code>chmod 0600</code> the file.</p>
<h2 id="credit">Credit</h2>
<p>Identified by AxonFlow internal security review.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/getaxonflow/axonflow-openclaw-plugin/security/advisories/GHSA-cqmh-pcgr-q42f">https://github.com/getaxonflow/axonflow-openclaw-plugin/security/advisories/GHSA-cqmh-pcgr-q42f</a></li>
<li><a href="https://github.com/advisories/GHSA-cqmh-pcgr-q42f">https://github.com/advisories/GHSA-cqmh-pcgr-q42f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cqmh-pcgr-q42f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:23:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@axonflow/sdk] axonflow-sdk-typescript: Webhook signing-key (HMAC-SHA256) not exposed by SDK type, preventing signature verification]]></title>
        <id>https://github.com/advisories/GHSA-mph8-9v29-pm42</id>
        <link href="https://github.com/advisories/GHSA-mph8-9v29-pm42"/>
        <updated>2026-05-06T23:16:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The AxonFlow SDK's <code>WebhookSubscription</code> (or equivalent) type did not expose the HMAC-SHA256 signing key returned by the platform's <code>CreateWebhook</code> endpoint. Without access to the secret through the typed SDK API, callers had no path to verify the <code>X-AxonFlow-Signature</code> header on incoming webhook deliveries. Affected callers had two unsatisfactory options:</p>
<ol>
<li>Skip signature verification entirely — accepting any payload from any source that knew the webhook URL.</li>
<li>Hand-parse the raw HTTP JSON response to extract the secret, bypassing the type-safe SDK surface.</li>
</ol>
<p>This advisory is filed across all four AxonFlow SDKs (Go, Python, TypeScript, Java) because the same defect and the same fix landed in each.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>Versions 5.6.0 and below.</p>
<h2 id="impact">Impact</h2>
<p>A webhook receiver using the SDK's typed API to handle inbound deliveries had no path to authenticate the source of incoming payloads. An attacker who learned the webhook URL — through misconfiguration, log leakage, observable network traffic during setup, or any other discovery channel — could forge webhook deliveries indistinguishable from legitimate ones, causing the receiving application to act on fabricated events (e.g. simulated approval-granted callbacks, simulated policy-decision callbacks, simulated step-completion callbacks).</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to the patched version listed in Vulnerabilities below. The signing key is now exposed on the <code>WebhookSubscription</code> response type returned by <code>CreateWebhook</code>. Implementations should:</p>
<ol>
<li>Persist the secret returned by <code>CreateWebhook</code> securely (it is only returned once, at create time).</li>
<li>On each incoming webhook delivery, compute <code>HMAC-SHA256(secret, raw_body)</code> and compare it in constant time against the <code>X-AxonFlow-Signature</code> header.</li>
<li>Reject any delivery whose signature does not match.</li>
</ol>
<h2 id="credit">Credit</h2>
<p>Identified by AxonFlow internal security review during the April 2026 quality-freeze epic.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/getaxonflow/axonflow-sdk-typescript/security/advisories/GHSA-mph8-9v29-pm42">https://github.com/getaxonflow/axonflow-sdk-typescript/security/advisories/GHSA-mph8-9v29-pm42</a></li>
<li><a href="https://github.com/advisories/GHSA-mph8-9v29-pm42">https://github.com/advisories/GHSA-mph8-9v29-pm42</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mph8-9v29-pm42</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:16:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Gateway `operator.write` can reach admin-only persisted `verboseLevel` via `chat.send` `/verbose`]]></title>
        <id>https://github.com/advisories/GHSA-v3c2-39fm-jq4h</id>
        <link href="https://github.com/advisories/GHSA-v3c2-39fm-jq4h"/>
        <updated>2026-05-06T23:04:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-5h2w-qmfp-ggp6. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.3.28 contains a privilege escalation vulnerability in the chat.send endpoint that allows write-scoped gateway callers to persist admin-only verboseLevel session overrides. Attackers can exploit the /verbose parameter to bypass access controls and expose sensitive reasoning or tool output intended to be restricted to administrators.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-5h2w-qmfp-ggp6">https://github.com/openclaw/openclaw/security/advisories/GHSA-5h2w-qmfp-ggp6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41344">https://nvd.nist.gov/vuln/detail/CVE-2026-41344</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-chat-send-verbose-parameter">https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-chat-send-verbose-parameter</a></li>
<li><a href="https://github.com/advisories/GHSA-v3c2-39fm-jq4h">https://github.com/advisories/GHSA-v3c2-39fm-jq4h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v3c2-39fm-jq4h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-24T00:31:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: CLI Remote Onboarding Persists Unauthenticated Discovery Endpoint and Exfiltrates Gateway Credentials]]></title>
        <id>https://github.com/advisories/GHSA-gv2f-q4wp-fvh5</id>
        <link href="https://github.com/advisories/GHSA-gv2f-q4wp-fvh5"/>
        <updated>2026-05-06T23:04:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-3cw3-5vxw-g2h3. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.3.28 contains an authentication bypass vulnerability in the remote onboarding component that persists unauthenticated discovery endpoints without explicit trust confirmation. Attackers can spoof discovery endpoints to redirect onboarding toward malicious gateways and capture gateway credentials or traffic.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-3cw3-5vxw-g2h3">https://github.com/openclaw/openclaw/security/advisories/GHSA-3cw3-5vxw-g2h3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41342">https://nvd.nist.gov/vuln/detail/CVE-2026-41342</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-unauthenticated-discovery-endpoint-credential-exfiltration-via-remote-onboarding">https://www.vulncheck.com/advisories/openclaw-unauthenticated-discovery-endpoint-credential-exfiltration-via-remote-onboarding</a></li>
<li><a href="https://github.com/advisories/GHSA-gv2f-q4wp-fvh5">https://github.com/advisories/GHSA-gv2f-q4wp-fvh5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gv2f-q4wp-fvh5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-24T00:31:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@backstage/plugin-catalog-backend-module-unprocessed] Backstage: Catalog unprocessed read endpoints allow authenticated cross-owner data access without permission checks]]></title>
        <id>https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</id>
        <link href="https://github.com/advisories/GHSA-p7g9-rp3g-mgfg"/>
        <updated>2026-05-06T23:04:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>  The unprocessed entities read endpoints in <code>@backstage/plugin-catalog-backend-module-unprocessed</code> do not enforce permission authorization checks. Any authenticated user can access unprocessed entity records regardless of ownership. This is 
  an information disclosure vulnerability affecting Backstage installations using this module.                                                                     </p>
<h3 id="patches">Patches</h3>
<p>  This is patched in <code>@backstage/plugin-catalog-backend-module-unprocessed</code> version 0.6.11, <code>@backstage/plugin-catalog-unprocessed-entities-common</code> version 0.0.15 and <code>@backstage/plugin-catalog-unprocessed-entities</code> version 0.2.30. Users should upgrade all packages.                                    </p>
<h3 id="workarounds">Workarounds</h3>
<p>  If users cannot upgrade, they can remove the <code>@backstage/plugin-catalog-backend-module-unprocessed</code> module from their backend until the patch is applied. There is no configuration-based workaround to add permission checks to these endpoints<br>  without upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/backstage/backstage/security/advisories/GHSA-p7g9-rp3g-mgfg">https://github.com/backstage/backstage/security/advisories/GHSA-p7g9-rp3g-mgfg</a></li>
<li><a href="https://github.com/advisories/GHSA-p7g9-rp3g-mgfg">https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:04:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@backstage/plugin-catalog-unprocessed-entities] Backstage: Catalog unprocessed read endpoints allow authenticated cross-owner data access without permission checks]]></title>
        <id>https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</id>
        <link href="https://github.com/advisories/GHSA-p7g9-rp3g-mgfg"/>
        <updated>2026-05-06T23:04:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>  The unprocessed entities read endpoints in <code>@backstage/plugin-catalog-backend-module-unprocessed</code> do not enforce permission authorization checks. Any authenticated user can access unprocessed entity records regardless of ownership. This is 
  an information disclosure vulnerability affecting Backstage installations using this module.                                                                     </p>
<h3 id="patches">Patches</h3>
<p>  This is patched in <code>@backstage/plugin-catalog-backend-module-unprocessed</code> version 0.6.11, <code>@backstage/plugin-catalog-unprocessed-entities-common</code> version 0.0.15 and <code>@backstage/plugin-catalog-unprocessed-entities</code> version 0.2.30. Users should upgrade all packages.                                    </p>
<h3 id="workarounds">Workarounds</h3>
<p>  If users cannot upgrade, they can remove the <code>@backstage/plugin-catalog-backend-module-unprocessed</code> module from their backend until the patch is applied. There is no configuration-based workaround to add permission checks to these endpoints<br>  without upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/backstage/backstage/security/advisories/GHSA-p7g9-rp3g-mgfg">https://github.com/backstage/backstage/security/advisories/GHSA-p7g9-rp3g-mgfg</a></li>
<li><a href="https://github.com/advisories/GHSA-p7g9-rp3g-mgfg">https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:04:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@backstage/plugin-catalog-unprocessed-entities-common] Backstage: Catalog unprocessed read endpoints allow authenticated cross-owner data access without permission checks]]></title>
        <id>https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</id>
        <link href="https://github.com/advisories/GHSA-p7g9-rp3g-mgfg"/>
        <updated>2026-05-06T23:04:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>  The unprocessed entities read endpoints in <code>@backstage/plugin-catalog-backend-module-unprocessed</code> do not enforce permission authorization checks. Any authenticated user can access unprocessed entity records regardless of ownership. This is 
  an information disclosure vulnerability affecting Backstage installations using this module.                                                                     </p>
<h3 id="patches">Patches</h3>
<p>  This is patched in <code>@backstage/plugin-catalog-backend-module-unprocessed</code> version 0.6.11, <code>@backstage/plugin-catalog-unprocessed-entities-common</code> version 0.0.15 and <code>@backstage/plugin-catalog-unprocessed-entities</code> version 0.2.30. Users should upgrade all packages.                                    </p>
<h3 id="workarounds">Workarounds</h3>
<p>  If users cannot upgrade, they can remove the <code>@backstage/plugin-catalog-backend-module-unprocessed</code> module from their backend until the patch is applied. There is no configuration-based workaround to add permission checks to these endpoints<br>  without upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/backstage/backstage/security/advisories/GHSA-p7g9-rp3g-mgfg">https://github.com/backstage/backstage/security/advisories/GHSA-p7g9-rp3g-mgfg</a></li>
<li><a href="https://github.com/advisories/GHSA-p7g9-rp3g-mgfg">https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p7g9-rp3g-mgfg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:04:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nitropack] Nitro has an Open Redirect via Protocol-Relative URL Bypass in Wildcard Route Rules]]></title>
        <id>https://github.com/advisories/GHSA-9phm-9p8f-hw5m</id>
        <link href="https://github.com/advisories/GHSA-9phm-9p8f-hw5m"/>
        <updated>2026-05-06T23:02:45.000Z</updated>
        <content type="html"><![CDATA[<p>A redirect route rule like:</p>
<pre><code class="language-ts">routeRules: {
  "/legacy/**": { redirect: "/**" }
}
</code></pre>
<p>is intended to rewrite paths within the same host. Before the patch, an attacker could turn the rewrite into a cross-host redirect by sliding an extra slash in after the rule prefix. Example exploit:</p>
<pre><code>GET /legacy//evil.com
</code></pre>
<p>Nitro stripped <code>/legacy</code> from the matched pathname and joined the remainder against the rule's target. The remainder was <code>//evil.com</code>, which the join preserved verbatim, so Nitro responded with <code>Location: //evil.com</code>. Browsers resolve <code>//evil.com</code> as a protocol-relative URL against the current scheme, sending the user to <code>https://evil.com</code>. </p>
<h3 id="are-you-affected">Are you affected?</h3>
<p>Users may be affected if <strong>all</strong> of the following are true:</p>
<ol>
<li>Their project uses Nitro's <code>routeRules</code> with a <code>redirect</code> entry.</li>
<li>The target uses a <code>/**</code> wildcard suffix to forward sub-paths (e.g. <code>redirect: "/**"</code>, <code>redirect: "/new/**"</code>, <code>proxy: { to: "http://upstream/**" }</code>).</li>
<li>The <code>redirect</code> rule is <em>not</em> handled natively at the CDN layer. The <code>vercel</code>, <code>netlify</code>, <code>cloudflare-pages</code>, and <code>edgeone</code> presets translate <code>routeRules.redirect</code> into platform config (<code>vercel.json</code>, <code>_redirects</code>, EdgeOne v3 config) and serve the redirect at the edge — those deployments bypass the Nitro runtime entirely and are not affected. Every other preset executes the redirect through the Nitro runtime and can be vulnerable.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Open redirect from any host serving Nitro with a wildcard <code>redirect</code> rule. The redirect target is fully attacker-controlled, the URL looks legitimate (it starts with the victim's domain), and the browser silently follows it.</p>
<h2 id="patched-versions">Patched versions</h2>
<p>Upgrade to one of:</p>
<ul>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">2.13.4</a> or later (or upgrade lockfile with latest ufo 1.6.4+)</li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">3.0.260429-beta</a> or later (<a href="https://github.com/nitrojs/nitro/pull/4236">https://github.com/nitrojs/nitro/pull/4236</a>)</li>
</ul>
<p>The fix has two parts:</p>
<ol>
<li><code>ufo</code> is bumped to <code>^1.6.4</code> (<a href="https://github.com/unjs/ufo/commit/5cd9e676711af3f4e4b5398ddf6ca8d52c1c7e1f">unjs/ufo@5cd9e67</a>), which collapses any run of leading slashes to a single <code>/</code> inside <code>withoutBase</code>. This covers the typical <code>"/scope/**"</code> rule.</li>
<li>The Nitro runtime additionally collapses leading <code>//</code> before joining when the rule path itself is <code>/**</code> (in rare case which case <code>withoutBase</code> is never called and the raw pathname flows straight into <code>joinURL("", …)</code>).</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nitrojs/nitro/security/advisories/GHSA-9phm-9p8f-hw5m">https://github.com/nitrojs/nitro/security/advisories/GHSA-9phm-9p8f-hw5m</a></li>
<li><a href="https://github.com/nitrojs/nitro/pull/4236">https://github.com/nitrojs/nitro/pull/4236</a></li>
<li><a href="https://github.com/unjs/ufo/commit/5cd9e676711af3f4e4b5398ddf6ca8d52c1c7e1f">https://github.com/unjs/ufo/commit/5cd9e676711af3f4e4b5398ddf6ca8d52c1c7e1f</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">https://github.com/nitrojs/nitro/releases/tag/v2.13.4</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta</a></li>
<li><a href="https://github.com/advisories/GHSA-9phm-9p8f-hw5m">https://github.com/advisories/GHSA-9phm-9p8f-hw5m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9phm-9p8f-hw5m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:02:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nitro] Nitro has an Open Redirect via Protocol-Relative URL Bypass in Wildcard Route Rules]]></title>
        <id>https://github.com/advisories/GHSA-9phm-9p8f-hw5m</id>
        <link href="https://github.com/advisories/GHSA-9phm-9p8f-hw5m"/>
        <updated>2026-05-06T23:02:45.000Z</updated>
        <content type="html"><![CDATA[<p>A redirect route rule like:</p>
<pre><code class="language-ts">routeRules: {
  "/legacy/**": { redirect: "/**" }
}
</code></pre>
<p>is intended to rewrite paths within the same host. Before the patch, an attacker could turn the rewrite into a cross-host redirect by sliding an extra slash in after the rule prefix. Example exploit:</p>
<pre><code>GET /legacy//evil.com
</code></pre>
<p>Nitro stripped <code>/legacy</code> from the matched pathname and joined the remainder against the rule's target. The remainder was <code>//evil.com</code>, which the join preserved verbatim, so Nitro responded with <code>Location: //evil.com</code>. Browsers resolve <code>//evil.com</code> as a protocol-relative URL against the current scheme, sending the user to <code>https://evil.com</code>. </p>
<h3 id="are-you-affected">Are you affected?</h3>
<p>Users may be affected if <strong>all</strong> of the following are true:</p>
<ol>
<li>Their project uses Nitro's <code>routeRules</code> with a <code>redirect</code> entry.</li>
<li>The target uses a <code>/**</code> wildcard suffix to forward sub-paths (e.g. <code>redirect: "/**"</code>, <code>redirect: "/new/**"</code>, <code>proxy: { to: "http://upstream/**" }</code>).</li>
<li>The <code>redirect</code> rule is <em>not</em> handled natively at the CDN layer. The <code>vercel</code>, <code>netlify</code>, <code>cloudflare-pages</code>, and <code>edgeone</code> presets translate <code>routeRules.redirect</code> into platform config (<code>vercel.json</code>, <code>_redirects</code>, EdgeOne v3 config) and serve the redirect at the edge — those deployments bypass the Nitro runtime entirely and are not affected. Every other preset executes the redirect through the Nitro runtime and can be vulnerable.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Open redirect from any host serving Nitro with a wildcard <code>redirect</code> rule. The redirect target is fully attacker-controlled, the URL looks legitimate (it starts with the victim's domain), and the browser silently follows it.</p>
<h2 id="patched-versions">Patched versions</h2>
<p>Upgrade to one of:</p>
<ul>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">2.13.4</a> or later (or upgrade lockfile with latest ufo 1.6.4+)</li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">3.0.260429-beta</a> or later (<a href="https://github.com/nitrojs/nitro/pull/4236">https://github.com/nitrojs/nitro/pull/4236</a>)</li>
</ul>
<p>The fix has two parts:</p>
<ol>
<li><code>ufo</code> is bumped to <code>^1.6.4</code> (<a href="https://github.com/unjs/ufo/commit/5cd9e676711af3f4e4b5398ddf6ca8d52c1c7e1f">unjs/ufo@5cd9e67</a>), which collapses any run of leading slashes to a single <code>/</code> inside <code>withoutBase</code>. This covers the typical <code>"/scope/**"</code> rule.</li>
<li>The Nitro runtime additionally collapses leading <code>//</code> before joining when the rule path itself is <code>/**</code> (in rare case which case <code>withoutBase</code> is never called and the raw pathname flows straight into <code>joinURL("", …)</code>).</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nitrojs/nitro/security/advisories/GHSA-9phm-9p8f-hw5m">https://github.com/nitrojs/nitro/security/advisories/GHSA-9phm-9p8f-hw5m</a></li>
<li><a href="https://github.com/nitrojs/nitro/pull/4236">https://github.com/nitrojs/nitro/pull/4236</a></li>
<li><a href="https://github.com/unjs/ufo/commit/5cd9e676711af3f4e4b5398ddf6ca8d52c1c7e1f">https://github.com/unjs/ufo/commit/5cd9e676711af3f4e4b5398ddf6ca8d52c1c7e1f</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">https://github.com/nitrojs/nitro/releases/tag/v2.13.4</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta</a></li>
<li><a href="https://github.com/advisories/GHSA-9phm-9p8f-hw5m">https://github.com/advisories/GHSA-9phm-9p8f-hw5m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9phm-9p8f-hw5m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:02:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nitropack] Nitro has a proxy scope bypass via percent-encoded path traversal in `routeRules`]]></title>
        <id>https://github.com/advisories/GHSA-5w89-w975-hf9q</id>
        <link href="https://github.com/advisories/GHSA-5w89-w975-hf9q"/>
        <updated>2026-05-06T23:01:29.000Z</updated>
        <content type="html"><![CDATA[<p>A proxy route rule like:</p>
<pre><code class="language-ts">routeRules: {
  "/api/orders/**": { proxy: { to: "http://upstream/orders/**" } }
}
</code></pre>
<p>is intended to limit the proxy to URLs under <code>/api/orders/</code>. Before the patch, an attacker could bypass that scope by sending percent-encoded path traversal (<code>..%2f</code>) in the URL, causing Nitro to forward a request that the upstream resolved outside the configured scope. Example exploit:</p>
<pre><code>GET /api/orders/..%2fadmin%2fconfig.json
</code></pre>
<p>Nitro sees <code>..%2f</code> as opaque characters at match time, the <code>/api/orders/**</code> rule matched, and the raw path was forwarded to the upstream as <code>/orders/..%2fadmin/config.json</code>. An upstream that decodes <code>%2F</code> to  <code>/</code> then resolved <code>..</code> and can serve <code>/admin/config.json</code> outside the intended scope.</p>
<h3 id="are-you-affected">Are you affected?</h3>
<p>Users may be affected if <strong>ALL</strong> of the following are true:</p>
<ol>
<li>Their project uses Nitro's <code>routeRules</code> with a <code>proxy</code> entry (<code>{ proxy: { to: "..." } }</code>).</li>
<li>The proxy <code>to</code> value uses a <code>/**</code> wildcard suffix to forward sub-paths.</li>
<li>The <strong>upstream</strong> behind the proxy decodes <code>%2F</code> as <code>/</code> before routing or filesystem lookup.</li>
<li>Proxy route rules are <em>not</em> handled natively at CDN (nitro v3 and vercel)</li>
</ol>
<p>Whether the bypass actually leaks data depends on the upstream. Modern JS frameworks keep <code>%2F</code> opaque per RFC 3986 and are safe by construction. </p>
<ul>
<li><strong>Safe examples:</strong> H3 v2, Express v5, Hono v4 — modern JS frameworks keep <code>%2F</code> opaque per RFC 3986.</li>
<li><strong>Vulnerable examples:</strong> naive imlementations that decodes the URL, static file servers, CGI dispatchers, Python <code>os.path</code>-based routing, anything sitting behind another layer that decodes <code>%2F</code> (common in microservice meshes).</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Any HTTP path reachable from the Nitro server to the upstream could be requested, regardless of the configured <code>/**</code> scope. In typical deployments (API gateway, BFF, microservice proxy) this could expose internal admin endpoints, secrets endpoints, or other services the developer believed the scope rule fenced off.</p>
<h2 id="patched-versions">Patched versions</h2>
<p>Upgrade to one of:</p>
<ul>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">2.13.4</a> or later (<a href="https://github.com/nitrojs/nitro/pull/4223">https://github.com/nitrojs/nitro/pull/4223</a>) </li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">3.0.260429-beta</a> or later (<a href="https://github.com/nitrojs/nitro/pull/4222">https://github.com/nitrojs/nitro/pull/4222</a>)</li>
</ul>
<p>The fix canonicalizes the incoming pathname before building the upstream URL and rejects requests with <code>400 Bad Request</code> if the resolved path would escape the rule's base. The bytes forwarded upstream are unchanged when the request is allowed.</p>
<blockquote>
<p>Note: the fix assumes the upstream does not double-decode percent-encoding. If your upstream decodes twice (<code>%252F → %2F → /</code>), it remains your responsibility to harden it. <strong>Single-decode is standard</strong>.</p>
</blockquote>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://github.com/mHe4am">@mHe4am</a> (<a href="https://hackerone.com/he4am">@he4am on HackerOne</a>) via the <a href="https://hackerone.com/vercel-open-source?type=team">Vercel Open Source</a> program.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nitrojs/nitro/security/advisories/GHSA-5w89-w975-hf9q">https://github.com/nitrojs/nitro/security/advisories/GHSA-5w89-w975-hf9q</a></li>
<li><a href="https://github.com/nitrojs/nitro/pull/4222">https://github.com/nitrojs/nitro/pull/4222</a></li>
<li><a href="https://github.com/nitrojs/nitro/pull/4223">https://github.com/nitrojs/nitro/pull/4223</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">https://github.com/nitrojs/nitro/releases/tag/v2.13.4</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta</a></li>
<li><a href="https://github.com/advisories/GHSA-5w89-w975-hf9q">https://github.com/advisories/GHSA-5w89-w975-hf9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5w89-w975-hf9q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:01:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nitro] Nitro has a proxy scope bypass via percent-encoded path traversal in `routeRules`]]></title>
        <id>https://github.com/advisories/GHSA-5w89-w975-hf9q</id>
        <link href="https://github.com/advisories/GHSA-5w89-w975-hf9q"/>
        <updated>2026-05-06T23:01:29.000Z</updated>
        <content type="html"><![CDATA[<p>A proxy route rule like:</p>
<pre><code class="language-ts">routeRules: {
  "/api/orders/**": { proxy: { to: "http://upstream/orders/**" } }
}
</code></pre>
<p>is intended to limit the proxy to URLs under <code>/api/orders/</code>. Before the patch, an attacker could bypass that scope by sending percent-encoded path traversal (<code>..%2f</code>) in the URL, causing Nitro to forward a request that the upstream resolved outside the configured scope. Example exploit:</p>
<pre><code>GET /api/orders/..%2fadmin%2fconfig.json
</code></pre>
<p>Nitro sees <code>..%2f</code> as opaque characters at match time, the <code>/api/orders/**</code> rule matched, and the raw path was forwarded to the upstream as <code>/orders/..%2fadmin/config.json</code>. An upstream that decodes <code>%2F</code> to  <code>/</code> then resolved <code>..</code> and can serve <code>/admin/config.json</code> outside the intended scope.</p>
<h3 id="are-you-affected">Are you affected?</h3>
<p>Users may be affected if <strong>ALL</strong> of the following are true:</p>
<ol>
<li>Their project uses Nitro's <code>routeRules</code> with a <code>proxy</code> entry (<code>{ proxy: { to: "..." } }</code>).</li>
<li>The proxy <code>to</code> value uses a <code>/**</code> wildcard suffix to forward sub-paths.</li>
<li>The <strong>upstream</strong> behind the proxy decodes <code>%2F</code> as <code>/</code> before routing or filesystem lookup.</li>
<li>Proxy route rules are <em>not</em> handled natively at CDN (nitro v3 and vercel)</li>
</ol>
<p>Whether the bypass actually leaks data depends on the upstream. Modern JS frameworks keep <code>%2F</code> opaque per RFC 3986 and are safe by construction. </p>
<ul>
<li><strong>Safe examples:</strong> H3 v2, Express v5, Hono v4 — modern JS frameworks keep <code>%2F</code> opaque per RFC 3986.</li>
<li><strong>Vulnerable examples:</strong> naive imlementations that decodes the URL, static file servers, CGI dispatchers, Python <code>os.path</code>-based routing, anything sitting behind another layer that decodes <code>%2F</code> (common in microservice meshes).</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Any HTTP path reachable from the Nitro server to the upstream could be requested, regardless of the configured <code>/**</code> scope. In typical deployments (API gateway, BFF, microservice proxy) this could expose internal admin endpoints, secrets endpoints, or other services the developer believed the scope rule fenced off.</p>
<h2 id="patched-versions">Patched versions</h2>
<p>Upgrade to one of:</p>
<ul>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">2.13.4</a> or later (<a href="https://github.com/nitrojs/nitro/pull/4223">https://github.com/nitrojs/nitro/pull/4223</a>) </li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">3.0.260429-beta</a> or later (<a href="https://github.com/nitrojs/nitro/pull/4222">https://github.com/nitrojs/nitro/pull/4222</a>)</li>
</ul>
<p>The fix canonicalizes the incoming pathname before building the upstream URL and rejects requests with <code>400 Bad Request</code> if the resolved path would escape the rule's base. The bytes forwarded upstream are unchanged when the request is allowed.</p>
<blockquote>
<p>Note: the fix assumes the upstream does not double-decode percent-encoding. If your upstream decodes twice (<code>%252F → %2F → /</code>), it remains your responsibility to harden it. <strong>Single-decode is standard</strong>.</p>
</blockquote>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://github.com/mHe4am">@mHe4am</a> (<a href="https://hackerone.com/he4am">@he4am on HackerOne</a>) via the <a href="https://hackerone.com/vercel-open-source?type=team">Vercel Open Source</a> program.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nitrojs/nitro/security/advisories/GHSA-5w89-w975-hf9q">https://github.com/nitrojs/nitro/security/advisories/GHSA-5w89-w975-hf9q</a></li>
<li><a href="https://github.com/nitrojs/nitro/pull/4222">https://github.com/nitrojs/nitro/pull/4222</a></li>
<li><a href="https://github.com/nitrojs/nitro/pull/4223">https://github.com/nitrojs/nitro/pull/4223</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v2.13.4">https://github.com/nitrojs/nitro/releases/tag/v2.13.4</a></li>
<li><a href="https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta">https://github.com/nitrojs/nitro/releases/tag/v3.0.260429-beta</a></li>
<li><a href="https://github.com/advisories/GHSA-5w89-w975-hf9q">https://github.com/advisories/GHSA-5w89-w975-hf9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5w89-w975-hf9q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-06T23:01:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Gateway operator.write Can Reach Admin-Class Telegram Config and Cron Persistence via send]]></title>
        <id>https://github.com/advisories/GHSA-394x-274p-mqc6</id>
        <link href="https://github.com/advisories/GHSA-394x-274p-mqc6"/>
        <updated>2026-05-06T22:41:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-767m-xrhc-fxm7. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.3.28 contains a privilege escalation vulnerability allowing authenticated operators with write permissions to access admin-class Telegram configuration and cron persistence settings via the send endpoint. Attackers with operator.write credentials can exploit insufficient access controls to reach sensitive administrative functionality and modify persistence mechanisms.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-767m-xrhc-fxm7">https://github.com/openclaw/openclaw/security/advisories/GHSA-767m-xrhc-fxm7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41359">https://nvd.nist.gov/vuln/detail/CVE-2026-41359</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/b7d70ade3b9900dbe97bd73be9c02e924ff3c986">https://github.com/openclaw/openclaw/commit/b7d70ade3b9900dbe97bd73be9c02e924ff3c986</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-operator-write-to-admin-class-telegram-config-and-cron-persistence">https://www.vulncheck.com/advisories/openclaw-privilege-escalation-via-operator-write-to-admin-class-telegram-config-and-cron-persistence</a></li>
<li><a href="https://github.com/advisories/GHSA-394x-274p-mqc6">https://github.com/advisories/GHSA-394x-274p-mqc6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-394x-274p-mqc6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-24T00:31:52.000Z</published>
    </entry>
</feed>