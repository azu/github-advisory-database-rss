<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-06-17T15:01:08.068Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Server-Side Request Forgery via Spreadsheet Import Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-hmcr-rmjq-47qr</id>
        <link href="https://github.com/advisories/GHSA-hmcr-rmjq-47qr"/>
        <updated>2026-06-17T14:08:28.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The spreadsheet-import endpoint <code>axiosRequestMake</code> could be used as a generic
HTTP proxy. Before the fix it was reachable unauthenticated, and its
URL-extension allowlist was a regex tested against the full URL string, so
URLs whose query string ended in <code>.csv</code> (for example
<code>https://example.com/robots.txt?.csv</code>) satisfied the gate even though the
underlying request was for <code>robots.txt</code>.</p>
<h3 id="details">Details</h3>
<p>Three layers of protection now apply to the endpoint:</p>
<ul>
<li>The controller is decorated with <code>@UseGuards(DataApiLimiterGuard, GlobalGuard)</code>
and <code>@Acl('fetchViaUrl')</code>, so unauthenticated callers and callers without
the editor role are rejected before the request body is processed.</li>
<li>The extension allowlist is tested against <code>url.pathname</code> only. Callers can
no longer satisfy the regex by appending a <code>.csv</code> suffix to the query
string.</li>
<li>The downstream axios call is wired to <code>useAgent(url)</code> from
<code>request-filtering-agent</code>, which blocks RFC 1918, loopback, link-local,
and other private destinations at the socket layer.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Unauthenticated callers could previously coerce the NocoDB process to issue
HTTP requests on their behalf, including to internal services reachable from
the host. With the auth gate in place and the pathname-anchored extension
check combined with socket-layer destination filtering, the endpoint is no
longer usable as a generic proxy and can no longer reach private ranges.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by the <a href="https://securitylab.github.com/">GitHub Security Lab</a>
(<a href="https://github.com/p-">@p-</a>, <a href="https://github.com/m-y-mo">@m-y-mo</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-hmcr-rmjq-47qr">https://github.com/nocodb/nocodb/security/advisories/GHSA-hmcr-rmjq-47qr</a></li>
<li><a href="https://github.com/advisories/GHSA-hmcr-rmjq-47qr">https://github.com/advisories/GHSA-hmcr-rmjq-47qr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hmcr-rmjq-47qr</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T14:08:26.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Server-Side Request Forgery via Base Migration URL]]></title>
        <id>https://github.com/advisories/GHSA-h6vv-pcq8-7xm4</id>
        <link href="https://github.com/advisories/GHSA-h6vv-pcq8-7xm4"/>
        <updated>2026-06-17T14:08:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The base-migration endpoint accepted a caller-supplied URL that the migration worker
dereferenced without enforcing protocol or destination, allowing scheme abuse
(<code>file:</code>, <code>ftp:</code>, etc.) and probing of internal HTTP destinations.</p>
<h3 id="details">Details</h3>
<p>The <code>migrate</code> endpoint is restricted to the workspace owner role by ACL. The remaining
gaps were (a) protocol validation — the controller now parses <code>body.migrationUrl</code> as a
<code>URL</code> and rejects anything whose protocol is not <code>http:</code> or <code>https:</code> — and (b) private
destination filtering — the worker already runs through <code>useAgent(targetUrl)</code> from
<code>request-filtering-agent</code>, which blocks RFC 1918, loopback, and link-local at the
socket layer.</p>
<h3 id="impact">Impact</h3>
<p>With the workspace owner role, a malformed URL could be used to coerce the migration
worker into reading local files or talking to non-HTTP services; combined with the
HTTP-only filter, owner-supplied targets could not reach private ranges.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by Devel Group Security Research Team through <a href="https://github.com/TREXNEGRO">@TREXNEGRO</a>.
It was independently reported by <a href="https://github.com/Lihfdgjr">@Lihfdgjr</a> and [@bugbunny-research (<a href="https://github.com/bugbunny-research">https://github.com/bugbunny-research</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-h6vv-pcq8-7xm4">https://github.com/nocodb/nocodb/security/advisories/GHSA-h6vv-pcq8-7xm4</a></li>
<li><a href="https://github.com/advisories/GHSA-h6vv-pcq8-7xm4">https://github.com/advisories/GHSA-h6vv-pcq8-7xm4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h6vv-pcq8-7xm4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T14:08:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Stored Cross-Site Scripting via Secure Attachment]]></title>
        <id>https://github.com/advisories/GHSA-6mhr-74x2-98v9</id>
        <link href="https://github.com/advisories/GHSA-6mhr-74x2-98v9"/>
        <updated>2026-06-17T14:07:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>With <code>NC_SECURE_ATTACHMENTS=true</code>, an authenticated uploader could deliver <code>.html</code> or
<code>.svg</code> attachments that the browser rendered inline from the NocoDB origin instead of
forcing a download.</p>
<h3 id="details">Details</h3>
<p>The signed attachment handler stored response-header overrides under PascalCase keys
(<code>ResponseContentDisposition</code>, <code>ResponseContentType</code>) while the controller that served
the file read them under lowercase-hyphen names (<code>response-content-disposition</code>). The
mismatch dropped the <code>Content-Disposition: attachment</code> header, leaving Express to
auto-render <code>.html</code>, <code>.svg</code>, and similar inline. The fix corrects the key case and
additionally forces <code>Content-Disposition: attachment</code> and
<code>Content-Type: application/octet-stream</code> for any MIME type not on the preview
allowlist.</p>
<h3 id="impact">Impact</h3>
<p>Stored Cross-Site Scripting in the NocoDB origin from inline-rendered uploads. Script
executing in the victim's browser can read the auth JWT from <code>localStorage</code>.
Exploitation requires authenticated upload permission and the secure-attachment mode
to be enabled.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/bugbunny-research">@bugbunny-research</a>.
It was independently reported by <a href="https://github.com/DavidCarliez">@DavidCarliez</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-6mhr-74x2-98v9">https://github.com/nocodb/nocodb/security/advisories/GHSA-6mhr-74x2-98v9</a></li>
<li><a href="https://github.com/advisories/GHSA-6mhr-74x2-98v9">https://github.com/advisories/GHSA-6mhr-74x2-98v9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6mhr-74x2-98v9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T14:07:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Refresh Tokens Persist Through Password Recovery]]></title>
        <id>https://github.com/advisories/GHSA-r989-7g3j-wjhw</id>
        <link href="https://github.com/advisories/GHSA-r989-7g3j-wjhw"/>
        <updated>2026-06-17T14:07:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stolen refresh token survived a password-forgot flow and could be used to mint fresh
JWTs even after the user reset their password.</p>
<h3 id="details">Details</h3>
<p><code>passwordChange</code> and <code>passwordReset</code> deleted the user's refresh tokens, but
<code>passwordForgot</code> only rotated <code>token_version</code> and revoked OAuth tokens — it did not
call <code>UserRefreshToken.deleteAllUserToken(user.id)</code>. An attacker holding a captured
refresh cookie could still exchange it for a new access token after the victim
triggered the recovery flow.</p>
<h3 id="impact">Impact</h3>
<p>Persistent unauthorized access after password recovery. Once a refresh token leaks, the
documented "Forgot password" recovery flow did not in fact revoke the attacker's
session.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by <a href="https://github.com/bugbunny-research">@bugbunny-research</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-r989-7g3j-wjhw">https://github.com/nocodb/nocodb/security/advisories/GHSA-r989-7g3j-wjhw</a></li>
<li><a href="https://github.com/advisories/GHSA-r989-7g3j-wjhw">https://github.com/advisories/GHSA-r989-7g3j-wjhw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r989-7g3j-wjhw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T14:07:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nocodb] NocoDB: Server-Side Request Forgery via Spreadsheet Fetch URL]]></title>
        <id>https://github.com/advisories/GHSA-gprh-27j3-g5h4</id>
        <link href="https://github.com/advisories/GHSA-gprh-27j3-g5h4"/>
        <updated>2026-06-17T14:06:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The spreadsheet-fetch endpoint (<code>axiosRequestMake</code>) accepted URLs whose path
contained a permitted extension anywhere in the string, and applied a hand-rolled
regex blocklist that omitted <code>127.0.0.0/8</code> and <code>169.254.0.0/16</code>, allowing the
cloud-metadata endpoint to be reached with a crafted URL.</p>
<h3 id="details">Details</h3>
<p>The extension matcher is now anchored to the end of the path or immediately before
the query string (<code>/\.(xls|xlsx|xlsm|ods|ots)(\?|$)/i</code> and <code>/\.(csv)(\?|$)/i</code>), so
<code>http://169.254.169.254/credentials/.xlsx</code> no longer satisfies the format gate. The
hand-rolled IP blocklist is removed in favour of <code>useAgent(url)</code> from
<code>request-filtering-agent</code>, which blocks private and loopback ranges at the socket
layer.</p>
<h3 id="impact">Impact</h3>
<p>Authenticated users with editor permission could read cloud metadata and other
internal HTTP endpoints reachable from the NocoDB process. On affected installs the
spreadsheet import path was a credential-exfiltration primitive on cloud hosts.</p>
<h3 id="credit">Credit</h3>
<p>This issue was reported by Devel Group Security Research Team through <a href="https://github.com/TREXNEGRO">@TREXNEGRO</a>.
It was independently reported by <a href="https://github.com/l3tchupkt">@l3tchupkt</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocodb/nocodb/security/advisories/GHSA-gprh-27j3-g5h4">https://github.com/nocodb/nocodb/security/advisories/GHSA-gprh-27j3-g5h4</a></li>
<li><a href="https://github.com/advisories/GHSA-gprh-27j3-g5h4">https://github.com/advisories/GHSA-gprh-27j3-g5h4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gprh-27j3-g5h4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T14:06:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[chrome-devtools-mcp] Chrome DevTools for agents: daemon.pid write follows symlinks in /tmp fallback runtime directory]]></title>
        <id>https://github.com/advisories/GHSA-3pvj-jv98-qhjq</id>
        <link href="https://github.com/advisories/GHSA-3pvj-jv98-qhjq"/>
        <updated>2026-06-17T14:01:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The chrome-devtools-mcp daemon writes its PID file with <code>fs.writeFileSync()</code> to a deterministic runtime path. On typical macOS environments, and on Linux sessions where <code>$XDG_RUNTIME_DIR</code> is unset, that runtime path falls back to <code>/tmp/chrome-devtools-mcp-&lt;uid&gt;/daemon.pid</code>.</p>
<p>Because the write does not use <code>O_NOFOLLOW</code>, a local low-privilege user on the same POSIX host can pre-create <code>/tmp/chrome-devtools-mcp-&lt;victim_uid&gt;/daemon.pid</code> as a symlink to a file writable by the victim. When the victim later starts daemon mode, <code>fs.writeFileSync()</code> follows the symlink and truncates the target file to the daemon PID string.</p>
<p>This report is deliberately scoped to POSIX systems where the daemon falls back to <code>/tmp</code>: typical macOS environments and Linux sessions without <code>$XDG_RUNTIME_DIR</code>. Windows is out of scope because the default temp directory is per-user and symlink creation has additional privilege requirements.</p>
<h3 id="details">Details</h3>
<p>Affected code:</p>
<p><code>src/daemon/daemon.ts:38-42</code></p>
<pre><code class="language-ts">const pidFilePath = getPidFilePath(sessionId);
fs.mkdirSync(path.dirname(pidFilePath), {
  recursive: true,
});
fs.writeFileSync(pidFilePath, process.pid.toString());
</code></pre>
<p><code>src/daemon/utils.ts:49-68</code></p>
<pre><code class="language-ts">export function getRuntimeHome(sessionId: string): string {
  const platform = os.platform();
  const uid = os.userInfo().uid;
  const suffix = sessionId ? `-${sessionId}` : '';
  const appName = APP_NAME + suffix;

  if (process.env.XDG_RUNTIME_DIR) {
    return path.join(process.env.XDG_RUNTIME_DIR, appName);
  }

  if (platform === 'darwin' || platform === 'linux') {
    return path.join('/tmp', `${appName}-${uid}`);
  }

  return path.join(os.tmpdir(), appName);
}
</code></pre>
<p>The <code>/tmp</code> sticky bit prevents non-owner file removal, but it does not prevent another local user from creating a subdirectory under <code>/tmp</code>. If an attacker creates <code>/tmp/chrome-devtools-mcp-&lt;victim_uid&gt;/</code> first and places a symlink at <code>daemon.pid</code>, the victim's daemon process follows that link when writing the PID.</p>
<p>Preconditions:</p>
<ul>
<li>The victim is on a typical macOS environment where <code>$XDG_RUNTIME_DIR</code> is unset, or on a Linux system/session where <code>$XDG_RUNTIME_DIR</code> is unset.</li>
<li>The attacker has any local user account on the same host.</li>
<li>The victim later runs a <code>chrome-devtools</code> CLI path or MCP integration that starts daemon mode.</li>
</ul>
<h3 id="poc">PoC</h3>
<p>Realistic POSIX scenario:</p>
<pre><code class="language-bash"># Attacker, before victim starts daemon mode.
victim_uid=1000
mkdir -p "/tmp/chrome-devtools-mcp-${victim_uid}"
chmod 0755 "/tmp/chrome-devtools-mcp-${victim_uid}"
ln -s "/home/victim/.ssh/authorized_keys" \
      "/tmp/chrome-devtools-mcp-${victim_uid}/daemon.pid"

# Victim later starts daemon mode.
chrome-devtools start

# Result:
# fs.writeFileSync follows the symlink, so authorized_keys is truncated to
# the daemon PID string.
</code></pre>
<p>Lab-only PoC that touches only a fresh <code>os.tmpdir()/cdtmcp-lab-*</code> directory:</p>
<pre><code class="language-js">const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');

const lab = fs.mkdtempSync(path.join(os.tmpdir(), 'cdtmcp-lab-'));

try {
  fs.chmodSync(lab, 0o755);

  const victimSecret = path.join(lab, 'victim-secret.txt');
  fs.writeFileSync(
    victimSecret,
    'IMPORTANT VICTIM CONTENT - MUST NOT BE TRUNCATED\n',
  );

  const runtimeDir = path.join(lab, 'attacker-pre-created');
  fs.mkdirSync(runtimeDir, {recursive: true});

  const pidFilePath = path.join(runtimeDir, 'daemon.pid');
  fs.symlinkSync(victimSecret, pidFilePath);

  // Exact pattern from src/daemon/daemon.ts:39-42.
  fs.mkdirSync(path.dirname(pidFilePath), {recursive: true});
  fs.writeFileSync(pidFilePath, process.pid.toString());

  console.log(fs.readFileSync(victimSecret, 'utf8'));
  // -&gt; "&lt;pid&gt;"  (victim file was truncated/overwritten)
} finally {
  fs.rmSync(lab, {recursive: true, force: true});
}
</code></pre>
<p>Observed output from the lab PoC:</p>
<pre><code class="language-text">[setup] victim secret BEFORE attack:
  IMPORTANT VICTIM CONTENT - MUST NOT BE TRUNCATED
[attack] symlink placed: &lt;runtimeDir&gt;/daemon.pid -&gt; &lt;victimSecret&gt;
[victim ran daemon] victim secret AFTER:
  &lt;pid&gt;
[lstat pidFile] still symlink
[outcome] victim file was overwritten via attacker-placed symlink.
</code></pre>
<p>I can provide the standalone <code>pidfile_symlink_poc.cjs</code> file if needed. The attached/local version includes platform notes, Windows symlink-permission diagnostics, and cleanup guards.</p>
<h3 id="impact">Impact</h3>
<p>Who can exploit:</p>
<p>Any local user account on the same POSIX host where the victim runs the chrome-devtools-mcp daemon, when <code>$XDG_RUNTIME_DIR</code> is unset for that user session.</p>
<p>Security impact:</p>
<ul>
<li>Integrity: an attacker can truncate and overwrite any file the victim can write, with content constrained to the daemon PID string.</li>
<li>Availability: critical user configuration files can be corrupted until restored from backup.</li>
<li>Confidentiality: none directly; the written content is only the PID string.</li>
</ul>
<p>Example targets affected by truncation:</p>
<ul>
<li><code>~/.ssh/authorized_keys</code>, causing the victim to lose SSH access.</li>
<li><code>~/.bashrc</code>, <code>~/.zshrc</code>, or <code>~/.profile</code>, breaking shell startup.</li>
<li>Project <code>.env</code>, <code>secrets.json</code>, license files, or line-oriented config files.</li>
<li>Logs or local audit files writable by the victim.</li>
</ul>
<p>Suggested fix:</p>
<p>Open the PID file with <code>O_NOFOLLOW</code> and validate runtime directory ownership/permissions before writing:</p>
<pre><code class="language-ts">import {constants, openSync, writeSync, closeSync} from 'node:fs';

const fd = openSync(
  pidFilePath,
  constants.O_WRONLY |
    constants.O_CREAT |
    constants.O_TRUNC |
    constants.O_NOFOLLOW,
  0o600,
);
writeSync(fd, process.pid.toString());
closeSync(fd);
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ChromeDevTools/chrome-devtools-mcp/security/advisories/GHSA-3pvj-jv98-qhjq">https://github.com/ChromeDevTools/chrome-devtools-mcp/security/advisories/GHSA-3pvj-jv98-qhjq</a></li>
<li><a href="https://github.com/advisories/GHSA-3pvj-jv98-qhjq">https://github.com/advisories/GHSA-3pvj-jv98-qhjq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3pvj-jv98-qhjq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T14:01:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Wrong OAuth Scope on Evaluation Test Runs Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-664h-gpgq-h6xx</id>
        <link href="https://github.com/advisories/GHSA-664h-gpgq-h6xx"/>
        <updated>2026-06-17T13:56:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Three mutating endpoints in the evaluation test runs controller authorized state-changing actions using <code>workflow:read</code> instead of the action-appropriate <code>workflow:execute</code> scope. An authenticated user with <code>project:viewer</code> role on a project could start new evaluation test runs, cancel in-flight runs, and delete run records for workflows they only had read access to.</p>
<p>This issue only affects instances with Advanced Permissions (Enterprise/Cloud) where projects and viewer roles are in use.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.55, 2.25.7, and 2.26.2. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Restrict project membership to fully trusted users only.</li>
<li>Avoid granting viewer access to projects containing sensitive workflows.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-664h-gpgq-h6xx">https://github.com/n8n-io/n8n/security/advisories/GHSA-664h-gpgq-h6xx</a></li>
<li><a href="https://github.com/advisories/GHSA-664h-gpgq-h6xx">https://github.com/advisories/GHSA-664h-gpgq-h6xx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-664h-gpgq-h6xx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T13:55:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Wrong OAuth Scope on Evaluation Test Runs Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-664h-gpgq-h6xx</id>
        <link href="https://github.com/advisories/GHSA-664h-gpgq-h6xx"/>
        <updated>2026-06-17T13:56:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Three mutating endpoints in the evaluation test runs controller authorized state-changing actions using <code>workflow:read</code> instead of the action-appropriate <code>workflow:execute</code> scope. An authenticated user with <code>project:viewer</code> role on a project could start new evaluation test runs, cancel in-flight runs, and delete run records for workflows they only had read access to.</p>
<p>This issue only affects instances with Advanced Permissions (Enterprise/Cloud) where projects and viewer roles are in use.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.55, 2.25.7, and 2.26.2. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Restrict project membership to fully trusted users only.</li>
<li>Avoid granting viewer access to projects containing sensitive workflows.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-664h-gpgq-h6xx">https://github.com/n8n-io/n8n/security/advisories/GHSA-664h-gpgq-h6xx</a></li>
<li><a href="https://github.com/advisories/GHSA-664h-gpgq-h6xx">https://github.com/advisories/GHSA-664h-gpgq-h6xx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-664h-gpgq-h6xx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T13:55:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Wrong OAuth Scope on Evaluation Test Runs Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-664h-gpgq-h6xx</id>
        <link href="https://github.com/advisories/GHSA-664h-gpgq-h6xx"/>
        <updated>2026-06-17T13:56:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Three mutating endpoints in the evaluation test runs controller authorized state-changing actions using <code>workflow:read</code> instead of the action-appropriate <code>workflow:execute</code> scope. An authenticated user with <code>project:viewer</code> role on a project could start new evaluation test runs, cancel in-flight runs, and delete run records for workflows they only had read access to.</p>
<p>This issue only affects instances with Advanced Permissions (Enterprise/Cloud) where projects and viewer roles are in use.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.55, 2.25.7, and 2.26.2. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Restrict project membership to fully trusted users only.</li>
<li>Avoid granting viewer access to projects containing sensitive workflows.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-664h-gpgq-h6xx">https://github.com/n8n-io/n8n/security/advisories/GHSA-664h-gpgq-h6xx</a></li>
<li><a href="https://github.com/advisories/GHSA-664h-gpgq-h6xx">https://github.com/advisories/GHSA-664h-gpgq-h6xx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-664h-gpgq-h6xx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T13:55:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@earendil-works/pi-coding-agent] Pi Agent: Pi loads project-local extensions without approval]]></title>
        <id>https://github.com/advisories/GHSA-mqxh-6gq7-558m</id>
        <link href="https://github.com/advisories/GHSA-mqxh-6gq7-558m"/>
        <updated>2026-06-17T13:55:46.000Z</updated>
        <content type="html"><![CDATA[<h1 id="pi-loads-project-local-extensions-without-approval">Pi loads project-local extensions without approval</h1>
<p>Pi before 0.79.0 loaded project-local configuration and resources from a repository's <code>.pi</code> directory without first asking the user to trust that repository. This included project-local extensions, which are executable TypeScript or JavaScript modules loaded into the Pi process.</p>
<p>An attacker who controls a repository could place Pi-specific project resources in that repository. If a user then started Pi from that working tree, the project-local extension code could run with the same privileges as the local Pi process without the user having a convenient way to make a trust decision.</p>
<h2 id="info">Info</h2>
<p>The affected component is Pi's project resource loading path. Before 0.79.0, startup loaded project <code>.pi/settings.json</code>, auto-discovered <code>.pi</code> resources, project package-managed resources, and project instruction files as part of normal session initialization. Project-local extensions were included in the same extension set as user/global extensions and were initialized before there was a project trust boundary.</p>
<p>Extensions like pi itself are not sandboxed. They run in the Pi process and can register commands, tools, and event handlers. The vulnerable behavior was not a privilege escalation across an operating-system boundary, but it allowed repository-controlled Pi extension code to execute implicitly when a user ran Pi in that repository.</p>
<h2 id="impact">Impact</h2>
<p>Exploitation requires user interaction: the attacker must get a user to open or otherwise work in an attacker-controlled repository and start Pi there. The attacker does not need an account on the user's machine or prior privileges in Pi.</p>
<p>If exploited, project-local extension code runs with the same permissions as the user running Pi. It can access files, environment variables, credentials available to the process, the network, and local tools available to that user. In CVSS terms, this advisory rates the impact as limited confidentiality and integrity impact without a distinct availability impact because exploitation requires local user action in an untrusted repository and does not cross a privilege boundary.</p>
<p>This risk is most relevant for users who run Pi in repositories they have not reviewed or do not trust. Pi's security guidance requires users to trust the codebases they work with, or to use an external sandbox or isolation boundary for untrusted repositories.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>&lt; 0.79.0</code></li>
<li>Patched: <code>0.79.0</code></li>
</ul>
<h2 id="the-solution">The solution</h2>
<p>Pi 0.79.0 added project trust gating for project-local inputs. On interactive startup, Pi now asks before loading project-local settings, instructions, resources, package-managed resources, or extensions when a trust decision has not already been saved for the working directory.</p>
<p>Non-interactive modes do not prompt. Without a saved trust decision, they ignore project-local inputs unless <code>--approve</code> or <code>-a</code> is passed. Users can also pass <code>--no-approve</code> or <code>-na</code> to ignore project-local inputs for a single run even when a project is already trusted.</p>
<p>Before trust is resolved, Pi loads only user/global extensions and extensions passed explicitly on the command line. Those extensions can participate in the <code>project_trust</code> event; project-local extensions are not loaded until after the project is trusted.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade <code>@earendil-works/pi-coding-agent</code> to version 0.79.0 or later.</p>
<p>After upgrading, review project trust prompts carefully and only trust repositories whose Pi-specific configuration and extensions you are willing to run. The upgrade makes this trust decision explicit, but it does not change Pi's security model: users should work only in trusted repositories, or use external isolation for untrusted code. Use <code>--no-approve</code> for one-off runs where project-local inputs should be ignored. For untrusted repositories or unattended automation, follow Pi's <a href="https://pi.dev/docs/latest/security">security guidance</a> and run Pi inside an operating-system sandbox, container, VM, or other isolation boundary with only the files and credentials required for the task.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Before and after this change, Pi users should only run Pi in repositories they trust, or in an external sandbox or isolation boundary when working with untrusted code. Pi 0.79.0 adds an explicit trust prompt as a defense-in-depth and user-safety improvement; it is not a sandbox and it does not make untrusted repositories safe.</p>
<p>If upgrading is not possible and your workflow already enforces this trust boundary, continue to avoid running Pi directly in untrusted repositories. Inspect and remove project-local Pi configuration and resources before use, especially project-local extensions and package configuration. Disabling extensions and other project resources with command-line flags can reduce exposure, but trusted or isolated execution remains the primary mitigation.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-25: Report received.</li>
<li>2026-06-05: Project trust gating committed.</li>
<li>2026-06-08: Project trust refinements and security model documentation completed.</li>
<li>2026-06-08: Fixed version 0.79.0 released.</li>
<li>2026-06-08: Advisory published.</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by <a href="https://github.com/qerogram">@qerogram</a>, <a href="https://github.com/urianpaul94">@urianpaul94</a>, <a href="https://github.com/EQSTLab">@EQSTLab</a>, and <a href="https://github.com/kamalmarhubi">@kamalmarhubi</a>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-mqxh-6gq7-558m">https://github.com/earendil-works/pi/security/advisories/GHSA-mqxh-6gq7-558m</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/38f18be44727e669eb0a6e2eb8edb51b0232d83c">https://github.com/earendil-works/pi/commit/38f18be44727e669eb0a6e2eb8edb51b0232d83c</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/718215bd95b6fc6fa251580d27ea8aab857de390">https://github.com/earendil-works/pi/commit/718215bd95b6fc6fa251580d27ea8aab857de390</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/89a92207f1c9303d53d822fd9b0ac21578834cb4">https://github.com/earendil-works/pi/commit/89a92207f1c9303d53d822fd9b0ac21578834cb4</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/ce3a72444e1cc1eaa50475fb3378c7ffbb53ef49">https://github.com/earendil-works/pi/commit/ce3a72444e1cc1eaa50475fb3378c7ffbb53ef49</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/ff3e9df5f5b32368c20b0ef553a6834b3dee9350">https://github.com/earendil-works/pi/commit/ff3e9df5f5b32368c20b0ef553a6834b3dee9350</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.79.0">https://github.com/earendil-works/pi/releases/tag/v0.79.0</a></li>
<li><a href="https://github.com/advisories/GHSA-mqxh-6gq7-558m">https://github.com/advisories/GHSA-mqxh-6gq7-558m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mqxh-6gq7-558m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-17T13:55:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mariozechner/pi-coding-agent] Pi Agent: Predictable temporary extension install paths allow local privilege escalation on shared Linux hosts]]></title>
        <id>https://github.com/advisories/GHSA-jfgx-wxx8-mp94</id>
        <link href="https://github.com/advisories/GHSA-jfgx-wxx8-mp94"/>
        <updated>2026-06-17T13:55:16.000Z</updated>
        <content type="html"><![CDATA[<h1 id="predictable-temporary-extension-install-paths-allow-local-privilege-escalation-on-shared-linux-hosts">Predictable temporary extension install paths allow local privilege escalation on shared Linux hosts</h1>
<p>Pi versions with temporary npm or git extension package installs used predictable paths under the operating system temporary directory. On Linux-based multi-user systems, a local attacker who can write to the shared temporary directory could prepare the expected package location before another user runs pi with a temporary extension package source. Pi could then load attacker-controlled extension code in the victim user's process.</p>
<h2 id="info">Info</h2>
<p>The vulnerable code path affected temporary extension package sources loaded with <code>--extension</code> or <code>-e</code>, specifically npm and git package sources. The temporary npm install root and temporary git clone paths were deterministic and rooted under <code>os.tmpdir()/pi-extensions</code>. The path was derived from public source information rather than from a per-user private directory or an unpredictable temporary directory.</p>
<p>During resource resolution, pi considered an npm package or git checkout present if the expected package path already existed. Extension resources discovered from that package location were then loaded by the extension loader. Because extensions execute with the same privileges as the invoking pi process, pre-created temporary package contents could execute as the victim user.</p>
<p>The issue primarily affects Linux-based multi-user hosts where the operating system temporary directory is shared across user accounts, such as shared development machines, CI runners, HPC login nodes, and similar environments. On Windows and macOS, the default temporary directory is typically user-scoped, so default configurations are not expected to be affected unless the temporary directory is overridden to a shared writable location.</p>
<h2 id="impact">Impact</h2>
<p>A local attacker with access to the same host can exploit this only if a victim runs a vulnerable pi version with a temporary npm or git extension package source that maps to the attacker-prepared location. No network attack path is involved and no race must be won, but victim interaction is required.</p>
<p>Successful exploitation can allow arbitrary extension code execution as the victim user. This can expose or modify files accessible to that user and can also cause denial of service or data loss through malicious package contents or unsafe temporary cache entries.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li><code>@earendil-works/pi-coding-agent</code>: affected <code>&gt;= 0.74.0, &lt; 0.78.1</code>; patched <code>&gt;= 0.78.1</code></li>
<li><code>@mariozechner/pi-coding-agent</code>: affected <code>&gt;= 0.50.0, &lt;= 0.73.1</code>; no patched version was released under the old package name. Migrate to <code>@earendil-works/pi-coding-agent &gt;= 0.78.1</code>.</li>
</ul>
<h2 id="the-solution">The solution</h2>
<p>Version 0.78.1 moves temporary extension package installs to a private per-user directory under <code>~/.pi/agent/tmp/extensions</code> and enforces <code>0700</code> permissions on that directory. The same release also hardens git package source path handling so managed clone paths remain inside their intended install roots.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade to <code>@earendil-works/pi-coding-agent</code> version 0.78.1 or later. Users of the deprecated <code>@mariozechner/pi-coding-agent</code> package should migrate to the <code>@earendil-works/pi-coding-agent</code> package and upgrade to a fixed version.</p>
<p>On shared Linux hosts, avoid using temporary npm or git extension package sources with vulnerable versions. Review any third-party extensions before loading them, because pi extensions run with full access to the invoking user's account.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading immediately is not possible, avoid <code>--extension</code> or <code>-e</code> with npm or git package sources on shared Linux systems. As an additional mitigation for vulnerable versions, configure the process temporary directory environment to point at a directory owned by the invoking user with <code>0700</code> permissions before starting pi.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-29: Report received</li>
<li>2026-06-02: Fix committed</li>
<li>2026-06-04: Fixed version 0.78.1 released</li>
<li>2026-06-08: Advisory prepared for publication</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by Paul Urian and Cosmin Alexa of CrowdStrike.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-jfgx-wxx8-mp94">https://github.com/earendil-works/pi/security/advisories/GHSA-jfgx-wxx8-mp94</a></li>
<li><a href="https://github.com/earendil-works/pi/pull/5345">https://github.com/earendil-works/pi/pull/5345</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/a98e087e5d08ea2a536bf73dbb0aebb87c3ef72e">https://github.com/earendil-works/pi/commit/a98e087e5d08ea2a536bf73dbb0aebb87c3ef72e</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/ea3465a8e371a12d0167a06b60f93878e3a3df44">https://github.com/earendil-works/pi/commit/ea3465a8e371a12d0167a06b60f93878e3a3df44</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.78.1">https://github.com/earendil-works/pi/releases/tag/v0.78.1</a></li>
<li><a href="https://github.com/advisories/GHSA-jfgx-wxx8-mp94">https://github.com/advisories/GHSA-jfgx-wxx8-mp94</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jfgx-wxx8-mp94</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T13:55:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@earendil-works/pi-coding-agent] Pi Agent: Predictable temporary extension install paths allow local privilege escalation on shared Linux hosts]]></title>
        <id>https://github.com/advisories/GHSA-jfgx-wxx8-mp94</id>
        <link href="https://github.com/advisories/GHSA-jfgx-wxx8-mp94"/>
        <updated>2026-06-17T13:55:16.000Z</updated>
        <content type="html"><![CDATA[<h1 id="predictable-temporary-extension-install-paths-allow-local-privilege-escalation-on-shared-linux-hosts">Predictable temporary extension install paths allow local privilege escalation on shared Linux hosts</h1>
<p>Pi versions with temporary npm or git extension package installs used predictable paths under the operating system temporary directory. On Linux-based multi-user systems, a local attacker who can write to the shared temporary directory could prepare the expected package location before another user runs pi with a temporary extension package source. Pi could then load attacker-controlled extension code in the victim user's process.</p>
<h2 id="info">Info</h2>
<p>The vulnerable code path affected temporary extension package sources loaded with <code>--extension</code> or <code>-e</code>, specifically npm and git package sources. The temporary npm install root and temporary git clone paths were deterministic and rooted under <code>os.tmpdir()/pi-extensions</code>. The path was derived from public source information rather than from a per-user private directory or an unpredictable temporary directory.</p>
<p>During resource resolution, pi considered an npm package or git checkout present if the expected package path already existed. Extension resources discovered from that package location were then loaded by the extension loader. Because extensions execute with the same privileges as the invoking pi process, pre-created temporary package contents could execute as the victim user.</p>
<p>The issue primarily affects Linux-based multi-user hosts where the operating system temporary directory is shared across user accounts, such as shared development machines, CI runners, HPC login nodes, and similar environments. On Windows and macOS, the default temporary directory is typically user-scoped, so default configurations are not expected to be affected unless the temporary directory is overridden to a shared writable location.</p>
<h2 id="impact">Impact</h2>
<p>A local attacker with access to the same host can exploit this only if a victim runs a vulnerable pi version with a temporary npm or git extension package source that maps to the attacker-prepared location. No network attack path is involved and no race must be won, but victim interaction is required.</p>
<p>Successful exploitation can allow arbitrary extension code execution as the victim user. This can expose or modify files accessible to that user and can also cause denial of service or data loss through malicious package contents or unsafe temporary cache entries.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li><code>@earendil-works/pi-coding-agent</code>: affected <code>&gt;= 0.74.0, &lt; 0.78.1</code>; patched <code>&gt;= 0.78.1</code></li>
<li><code>@mariozechner/pi-coding-agent</code>: affected <code>&gt;= 0.50.0, &lt;= 0.73.1</code>; no patched version was released under the old package name. Migrate to <code>@earendil-works/pi-coding-agent &gt;= 0.78.1</code>.</li>
</ul>
<h2 id="the-solution">The solution</h2>
<p>Version 0.78.1 moves temporary extension package installs to a private per-user directory under <code>~/.pi/agent/tmp/extensions</code> and enforces <code>0700</code> permissions on that directory. The same release also hardens git package source path handling so managed clone paths remain inside their intended install roots.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade to <code>@earendil-works/pi-coding-agent</code> version 0.78.1 or later. Users of the deprecated <code>@mariozechner/pi-coding-agent</code> package should migrate to the <code>@earendil-works/pi-coding-agent</code> package and upgrade to a fixed version.</p>
<p>On shared Linux hosts, avoid using temporary npm or git extension package sources with vulnerable versions. Review any third-party extensions before loading them, because pi extensions run with full access to the invoking user's account.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading immediately is not possible, avoid <code>--extension</code> or <code>-e</code> with npm or git package sources on shared Linux systems. As an additional mitigation for vulnerable versions, configure the process temporary directory environment to point at a directory owned by the invoking user with <code>0700</code> permissions before starting pi.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-29: Report received</li>
<li>2026-06-02: Fix committed</li>
<li>2026-06-04: Fixed version 0.78.1 released</li>
<li>2026-06-08: Advisory prepared for publication</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by Paul Urian and Cosmin Alexa of CrowdStrike.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-jfgx-wxx8-mp94">https://github.com/earendil-works/pi/security/advisories/GHSA-jfgx-wxx8-mp94</a></li>
<li><a href="https://github.com/earendil-works/pi/pull/5345">https://github.com/earendil-works/pi/pull/5345</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/a98e087e5d08ea2a536bf73dbb0aebb87c3ef72e">https://github.com/earendil-works/pi/commit/a98e087e5d08ea2a536bf73dbb0aebb87c3ef72e</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/ea3465a8e371a12d0167a06b60f93878e3a3df44">https://github.com/earendil-works/pi/commit/ea3465a8e371a12d0167a06b60f93878e3a3df44</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.78.1">https://github.com/earendil-works/pi/releases/tag/v0.78.1</a></li>
<li><a href="https://github.com/advisories/GHSA-jfgx-wxx8-mp94">https://github.com/advisories/GHSA-jfgx-wxx8-mp94</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jfgx-wxx8-mp94</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-17T13:55:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@earendil-works/pi-coding-agent] Pi Agent: Race condition in Pi auth.json writes could expose stored credentials]]></title>
        <id>https://github.com/advisories/GHSA-r95r-rj6r-c39x</id>
        <link href="https://github.com/advisories/GHSA-r95r-rj6r-c39x"/>
        <updated>2026-06-17T13:54:38.000Z</updated>
        <content type="html"><![CDATA[<h1 id="pi-authjson-writes-could-briefly-expose-stored-credentials-to-local-users">Pi auth.json writes could briefly expose stored credentials to local users</h1>
<p>Pi stored API keys and OAuth credentials in <code>auth.json</code>. A race condition in the file write path could briefly create or rewrite this file with permissions derived from the process umask before tightening the file to owner-only permissions.</p>
<h2 id="info">Info</h2>
<p>The affected credential storage code wrote <code>auth.json</code> and then corrected the file mode in a separate operation. During the interval between those operations, a local user who could read and traverse the Pi agent configuration directory could potentially read the file before its permissions were restricted.</p>
<p>The file can contain API keys, OAuth access tokens, and OAuth refresh tokens for configured providers. The affected behavior was present in the original <code>auth.json</code> credential storage implementation and thus affects both the original <code>@mariozechner/pi-coding-agent</code> package as well as <code>@earendil-works/pi-coding-agent</code>.</p>
<h2 id="impact">Impact</h2>
<p>Exploitation requires local access to the same machine and read/traverse access to the victim's Pi agent configuration directory. Users whose <code>~/.pi/agent</code> directory is private to their account are less exposed. The main impact is disclosure of stored provider credentials, which may allow use of the configured provider accounts according to the privileges of those credentials.</p>
<p>This is not remotely exploitable by itself.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>@mariozechner/pi-coding-agent &gt;= 0.28.0, &lt;= 0.73.1</code></li>
<li>Affected: <code>@earendil-works/pi-coding-agent &gt;= 0.74.0, &lt; 0.78.1</code></li>
<li>Patched: <code>@earendil-works/pi-coding-agent &gt;= 0.78.1</code></li>
</ul>
<h2 id="the-solution">The solution</h2>
<p>Version 0.78.1 changed the credential storage writes to create <code>auth.json</code> with mode <code>0600</code> at open time. The fix applies to initial file creation and credential save paths, including OAuth token refresh writes.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade to <code>@earendil-works/pi-coding-agent</code> version 0.78.1 or later. Users still on the deprecated <code>@mariozechner/pi-coding-agent</code> package should migrate to the <code>@earendil-works/pi-coding-agent</code> package and install version 0.78.1 or later.</p>
<p>After upgrading, rotate any credentials that may have been exposed on multi-user systems where the Pi agent configuration directory was readable by other local users.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading immediately is not possible, restrict the Pi agent configuration directory so only the owning user can traverse it, restrict <code>auth.json</code> to owner-only permissions, and run Pi with a restrictive umask such as <code>077</code> until the upgrade is complete.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-29: Report received</li>
<li>2026-06-02: Fix committed</li>
<li>2026-06-04: Fixed version released</li>
<li>2026-06-08: Advisory published</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by Paul Urian and Cosmin Alexa of CrowdStrike.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-r95r-rj6r-c39x">https://github.com/earendil-works/pi/security/advisories/GHSA-r95r-rj6r-c39x</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/135fb545f99106a4a249274f129b90bc0a77d347">https://github.com/earendil-works/pi/commit/135fb545f99106a4a249274f129b90bc0a77d347</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.78.1">https://github.com/earendil-works/pi/releases/tag/v0.78.1</a></li>
<li><a href="https://github.com/advisories/GHSA-r95r-rj6r-c39x">https://github.com/advisories/GHSA-r95r-rj6r-c39x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r95r-rj6r-c39x</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-17T13:54:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mariozechner/pi-coding-agent] Pi Agent: Race condition in Pi auth.json writes could expose stored credentials]]></title>
        <id>https://github.com/advisories/GHSA-r95r-rj6r-c39x</id>
        <link href="https://github.com/advisories/GHSA-r95r-rj6r-c39x"/>
        <updated>2026-06-17T13:54:38.000Z</updated>
        <content type="html"><![CDATA[<h1 id="pi-authjson-writes-could-briefly-expose-stored-credentials-to-local-users">Pi auth.json writes could briefly expose stored credentials to local users</h1>
<p>Pi stored API keys and OAuth credentials in <code>auth.json</code>. A race condition in the file write path could briefly create or rewrite this file with permissions derived from the process umask before tightening the file to owner-only permissions.</p>
<h2 id="info">Info</h2>
<p>The affected credential storage code wrote <code>auth.json</code> and then corrected the file mode in a separate operation. During the interval between those operations, a local user who could read and traverse the Pi agent configuration directory could potentially read the file before its permissions were restricted.</p>
<p>The file can contain API keys, OAuth access tokens, and OAuth refresh tokens for configured providers. The affected behavior was present in the original <code>auth.json</code> credential storage implementation and thus affects both the original <code>@mariozechner/pi-coding-agent</code> package as well as <code>@earendil-works/pi-coding-agent</code>.</p>
<h2 id="impact">Impact</h2>
<p>Exploitation requires local access to the same machine and read/traverse access to the victim's Pi agent configuration directory. Users whose <code>~/.pi/agent</code> directory is private to their account are less exposed. The main impact is disclosure of stored provider credentials, which may allow use of the configured provider accounts according to the privileges of those credentials.</p>
<p>This is not remotely exploitable by itself.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>@mariozechner/pi-coding-agent &gt;= 0.28.0, &lt;= 0.73.1</code></li>
<li>Affected: <code>@earendil-works/pi-coding-agent &gt;= 0.74.0, &lt; 0.78.1</code></li>
<li>Patched: <code>@earendil-works/pi-coding-agent &gt;= 0.78.1</code></li>
</ul>
<h2 id="the-solution">The solution</h2>
<p>Version 0.78.1 changed the credential storage writes to create <code>auth.json</code> with mode <code>0600</code> at open time. The fix applies to initial file creation and credential save paths, including OAuth token refresh writes.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade to <code>@earendil-works/pi-coding-agent</code> version 0.78.1 or later. Users still on the deprecated <code>@mariozechner/pi-coding-agent</code> package should migrate to the <code>@earendil-works/pi-coding-agent</code> package and install version 0.78.1 or later.</p>
<p>After upgrading, rotate any credentials that may have been exposed on multi-user systems where the Pi agent configuration directory was readable by other local users.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading immediately is not possible, restrict the Pi agent configuration directory so only the owning user can traverse it, restrict <code>auth.json</code> to owner-only permissions, and run Pi with a restrictive umask such as <code>077</code> until the upgrade is complete.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-29: Report received</li>
<li>2026-06-02: Fix committed</li>
<li>2026-06-04: Fixed version released</li>
<li>2026-06-08: Advisory published</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by Paul Urian and Cosmin Alexa of CrowdStrike.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-r95r-rj6r-c39x">https://github.com/earendil-works/pi/security/advisories/GHSA-r95r-rj6r-c39x</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/135fb545f99106a4a249274f129b90bc0a77d347">https://github.com/earendil-works/pi/commit/135fb545f99106a4a249274f129b90bc0a77d347</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.78.1">https://github.com/earendil-works/pi/releases/tag/v0.78.1</a></li>
<li><a href="https://github.com/advisories/GHSA-r95r-rj6r-c39x">https://github.com/advisories/GHSA-r95r-rj6r-c39x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r95r-rj6r-c39x</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-17T13:54:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@earendil-works/pi-coding-agent] Pi Agent: Potential XSS in HTML session exports via Markdown URL sanitization bypass]]></title>
        <id>https://github.com/advisories/GHSA-7v5m-pr3q-6453</id>
        <link href="https://github.com/advisories/GHSA-7v5m-pr3q-6453"/>
        <updated>2026-06-16T23:43:15.000Z</updated>
        <content type="html"><![CDATA[<h1 id="potential-xss-in-html-session-exports-via-markdown-url-handling">Potential XSS in HTML session exports via Markdown URL handling</h1>
<p>Pi HTML exports render session Markdown into a static HTML file. Affected versions did not consistently reject unsafe Markdown link and image URL schemes. In versions with scheme filtering, C0 control characters in the URL scheme could bypass the check because browsers normalize those characters before navigation.</p>
<h2 id="impact">Impact</h2>
<p>The realistic attack path is indirect. An attacker would need to get suitable Markdown into a session, for example through prompt injection that causes the model to include an unsafe link, or through other untrusted session content. The user would then need to export the session as HTML, open or share that file, and click the link.</p>
<p>If triggered, script runs in the exported document, not in pi or the user's shell. The main risk is limited disclosure of data embedded in that exported session file.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>@mariozechner/pi-coding-agent &gt;= 0.27.5, &lt;= 0.73.1</code></li>
<li>Affected: <code>@earendil-works/pi-coding-agent &gt;= 0.74.0, &lt; 0.78.1</code></li>
<li>Patched: <code>@earendil-works/pi-coding-agent 0.78.1</code></li>
</ul>
<p>The old <code>@mariozechner/pi-coding-agent</code> package scope has no patched release. It was renamed to <code>@earendil-works/pi-coding-agent</code>; users of the old scope should migrate to the new package and upgrade to version 0.78.1 or later.</p>
<h2 id="resolution">Resolution</h2>
<p>Version 0.78.1 sanitizes Markdown link and image URLs with an allow-list after stripping C0 control characters.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade <code>@earendil-works/pi-coding-agent</code> to version 0.78.1 or later. Regenerate shared HTML exports after upgrading if the underlying sessions contained untrusted content.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-29: Report received through GitHub Security Advisories</li>
<li>2026-06-02: Fix committed</li>
<li>2026-06-04: Fixed version 0.78.1 released</li>
<li>2026-06-08: Advisory prepared for publication</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by Paul Urian and Cosmin Alexa of CrowdStrike.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-7v5m-pr3q-6453">https://github.com/earendil-works/pi/security/advisories/GHSA-7v5m-pr3q-6453</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/6cb23f9b5d5b6d1747672f535b167d0d809ac010">https://github.com/earendil-works/pi/commit/6cb23f9b5d5b6d1747672f535b167d0d809ac010</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.78.1">https://github.com/earendil-works/pi/releases/tag/v0.78.1</a></li>
<li><a href="https://github.com/advisories/GHSA-7v5m-pr3q-6453">https://github.com/advisories/GHSA-7v5m-pr3q-6453</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7v5m-pr3q-6453</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-16T23:43:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@mariozechner/pi-coding-agent] Pi Agent: Potential XSS in HTML session exports via Markdown URL sanitization bypass]]></title>
        <id>https://github.com/advisories/GHSA-7v5m-pr3q-6453</id>
        <link href="https://github.com/advisories/GHSA-7v5m-pr3q-6453"/>
        <updated>2026-06-16T23:43:15.000Z</updated>
        <content type="html"><![CDATA[<h1 id="potential-xss-in-html-session-exports-via-markdown-url-handling">Potential XSS in HTML session exports via Markdown URL handling</h1>
<p>Pi HTML exports render session Markdown into a static HTML file. Affected versions did not consistently reject unsafe Markdown link and image URL schemes. In versions with scheme filtering, C0 control characters in the URL scheme could bypass the check because browsers normalize those characters before navigation.</p>
<h2 id="impact">Impact</h2>
<p>The realistic attack path is indirect. An attacker would need to get suitable Markdown into a session, for example through prompt injection that causes the model to include an unsafe link, or through other untrusted session content. The user would then need to export the session as HTML, open or share that file, and click the link.</p>
<p>If triggered, script runs in the exported document, not in pi or the user's shell. The main risk is limited disclosure of data embedded in that exported session file.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>Affected: <code>@mariozechner/pi-coding-agent &gt;= 0.27.5, &lt;= 0.73.1</code></li>
<li>Affected: <code>@earendil-works/pi-coding-agent &gt;= 0.74.0, &lt; 0.78.1</code></li>
<li>Patched: <code>@earendil-works/pi-coding-agent 0.78.1</code></li>
</ul>
<p>The old <code>@mariozechner/pi-coding-agent</code> package scope has no patched release. It was renamed to <code>@earendil-works/pi-coding-agent</code>; users of the old scope should migrate to the new package and upgrade to version 0.78.1 or later.</p>
<h2 id="resolution">Resolution</h2>
<p>Version 0.78.1 sanitizes Markdown link and image URLs with an allow-list after stripping C0 control characters.</p>
<h2 id="recommendations">Recommendations</h2>
<p>Upgrade <code>@earendil-works/pi-coding-agent</code> to version 0.78.1 or later. Regenerate shared HTML exports after upgrading if the underlying sessions contained untrusted content.</p>
<h2 id="timeline">Timeline</h2>
<ul>
<li>2026-05-29: Report received through GitHub Security Advisories</li>
<li>2026-06-02: Fix committed</li>
<li>2026-06-04: Fixed version 0.78.1 released</li>
<li>2026-06-08: Advisory prepared for publication</li>
</ul>
<h2 id="credits">Credits</h2>
<p>Reported by Paul Urian and Cosmin Alexa of CrowdStrike.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/earendil-works/pi/security/advisories/GHSA-7v5m-pr3q-6453">https://github.com/earendil-works/pi/security/advisories/GHSA-7v5m-pr3q-6453</a></li>
<li><a href="https://github.com/earendil-works/pi/commit/6cb23f9b5d5b6d1747672f535b167d0d809ac010">https://github.com/earendil-works/pi/commit/6cb23f9b5d5b6d1747672f535b167d0d809ac010</a></li>
<li><a href="https://github.com/earendil-works/pi/releases/tag/v0.78.1">https://github.com/earendil-works/pi/releases/tag/v0.78.1</a></li>
<li><a href="https://github.com/advisories/GHSA-7v5m-pr3q-6453">https://github.com/advisories/GHSA-7v5m-pr3q-6453</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7v5m-pr3q-6453</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-16T23:43:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nuxt/rspack-builder] @nuxt/webpack-builder and @nuxt/rspack-builder dev server same-origin check bypassed when Sec-Fetch-Site, Origin, and Referer are all absent (incomplete fix for GHSA-6m52-m754-pw2g)]]></title>
        <id>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</id>
        <link href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5"/>
        <updated>2026-06-16T23:39:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>This is an incomplete fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a>. Source code may still be stolen during dev when using the webpack / rspack builder if the dev server is bound to a non-loopback address (e.g. <code>nuxt dev --host</code>) and the developer opens a malicious site on the same network.</p>
<h3 id="details">Details</h3>
<p>The fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a> added an <code>Origin</code> / <code>Referer</code> fallback to the dev-middleware same-origin check, with a <code>return true</code> branch when neither header is present so that non-browser clients (curl, the HMR client, address-bar navigation) keep working.</p>
<p>That fallback is bypassed when a cross-origin attacker request reaches the dev server with all three signal headers absent:</p>
<ul>
<li><code>Sec-Fetch-Site</code> is <a href="https://w3c.github.io/webappsec-fetch-metadata/#sec-fetch-site-header">not sent by browsers to non-potentially-trustworthy destinations</a> (HTTP on a non-loopback address).</li>
<li><code>Origin</code> is not sent on non-CORS subresource fetches (a bare <code>&lt;script&gt;</code> with no <code>crossorigin</code>).</li>
<li><code>Referer</code> can be suppressed by the attacker page with <code>&lt;meta name="referrer" content="no-referrer"&gt;</code> or <code>referrerpolicy="no-referrer"</code> on the <code>&lt;script&gt;</code> element.</li>
</ul>
<p>A classic <code>&lt;script src="http://VICTIM_LAN_IP:3000/_nuxt/app.js" referrerpolicy="no-referrer"&gt;</code> from a non-trustworthy attacker origin produces exactly that header set, the request is allowed, and the attacker page can read the built source out of <code>window.webpackChunk*</code> via <code>Function.prototype.toString()</code>.</p>
<p>Since the attack requires the dev server to be reachable via a non-potentially-trustworthy origin, only apps using <code>--host</code> (or <code>--host 0.0.0.0</code>) are affected. Chrome 142+ users are also protected by <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a Nuxt project with the webpack / rspack builder.</li>
<li>Run <code>npm run dev -- --host 0.0.0.0</code>.</li>
<li>Open <code>http://localhost:3000</code> on the developer machine.</li>
<li>From a different LAN host, serve the page below and open it in the same browser.</li>
<li>The compiled module source is exfiltrable from <code>window.webpackChunknuxt_&lt;projectname&gt;</code>.</li>
</ol>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;meta name="referrer" content="no-referrer"&gt;
&lt;script&gt;
  ['/_nuxt/runtime.js', '/_nuxt/app.js'].forEach(p =&gt; {
    const s = document.createElement('script')
    s.src = 'http://VICTIM_LAN_IP:3000' + p
    s.referrerPolicy = 'no-referrer'
    document.head.appendChild(s)
  })
  setTimeout(() =&gt; {
    const key = Object.keys(window).find(k =&gt; k.startsWith('webpackChunk'))
    for (const [, mods] of window[key]) {
      for (const id in mods) {
        console.log(id, mods[id].toString())
      }
    }
  }, 1500)
&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Users using the webpack / rspack builder with <code>nuxt dev --host</code> may get the built source code read by malicious websites on the same network, including module identifiers, the developer's local filesystem path, and any developer-controlled strings inlined into the bundle.</p>
<p>This vulnerability does not affect Chrome 142+ (and other Chromium-based browsers) users due to <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<p>The default Vite builder is not affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@nuxt/webpack-builder@4.4.7</code> / <code>@nuxt/rspack-builder@4.4.7</code> and backported to <code>@nuxt/webpack-builder@3.21.7</code> / <code>@nuxt/rspack-builder@3.21.7</code> by <a href="https://github.com/nuxt/nuxt/pull/35200">#35200</a> (4.x: commit <a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3"><code>e351de94</code></a>; 3.x: commit <a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05"><code>77187ee4</code></a>). The dev-middleware same-origin check now treats a request with no <code>Sec-Fetch-Site</code>, no <code>Origin</code>, and no <code>Referer</code> as same-origin only when the dev server is loopback-bound, closing the header-suppression bypass.</p>
<p>The fix only ships for the <code>@nuxt/webpack-builder</code> and <code>@nuxt/rspack-builder</code> packages. The default Vite builder was not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately:</p>
<ul>
<li>Don't use <code>nuxt dev --host</code>. Bind the dev server to <code>localhost</code> (the default) and tunnel from other devices via SSH or a reverse proxy that enforces same-origin checks.</li>
<li>Use Chrome 142+ or another Chromium-based browser that enforces <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</li>
<li>Switch to the Vite builder for development.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Berkan SAL (<a href="https://github.com/Uhudsavasindankacanokcu2">@Uhudsavasindankacanokcu2</a>) via the Vercel Open Source HackerOne program.</p>
<p>Independently reported by <a href="https://github.com/DavidCarliez">@DavidCarliez</a> via GitHub's coordinated disclosure flow (<code>GHSA-xw96-2f5x-v9pv</code>), closed as a duplicate of this advisory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g</a></li>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49993">https://nvd.nist.gov/vuln/detail/CVE-2026-49993</a></li>
<li><a href="https://github.com/nuxt/nuxt/pull/35200">https://github.com/nuxt/nuxt/pull/35200</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05">https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3">https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3</a></li>
<li><a href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/advisories/GHSA-x6qj-4h56-5rj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T23:39:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nuxt/rspack-builder] @nuxt/webpack-builder and @nuxt/rspack-builder dev server same-origin check bypassed when Sec-Fetch-Site, Origin, and Referer are all absent (incomplete fix for GHSA-6m52-m754-pw2g)]]></title>
        <id>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</id>
        <link href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5"/>
        <updated>2026-06-16T23:39:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>This is an incomplete fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a>. Source code may still be stolen during dev when using the webpack / rspack builder if the dev server is bound to a non-loopback address (e.g. <code>nuxt dev --host</code>) and the developer opens a malicious site on the same network.</p>
<h3 id="details">Details</h3>
<p>The fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a> added an <code>Origin</code> / <code>Referer</code> fallback to the dev-middleware same-origin check, with a <code>return true</code> branch when neither header is present so that non-browser clients (curl, the HMR client, address-bar navigation) keep working.</p>
<p>That fallback is bypassed when a cross-origin attacker request reaches the dev server with all three signal headers absent:</p>
<ul>
<li><code>Sec-Fetch-Site</code> is <a href="https://w3c.github.io/webappsec-fetch-metadata/#sec-fetch-site-header">not sent by browsers to non-potentially-trustworthy destinations</a> (HTTP on a non-loopback address).</li>
<li><code>Origin</code> is not sent on non-CORS subresource fetches (a bare <code>&lt;script&gt;</code> with no <code>crossorigin</code>).</li>
<li><code>Referer</code> can be suppressed by the attacker page with <code>&lt;meta name="referrer" content="no-referrer"&gt;</code> or <code>referrerpolicy="no-referrer"</code> on the <code>&lt;script&gt;</code> element.</li>
</ul>
<p>A classic <code>&lt;script src="http://VICTIM_LAN_IP:3000/_nuxt/app.js" referrerpolicy="no-referrer"&gt;</code> from a non-trustworthy attacker origin produces exactly that header set, the request is allowed, and the attacker page can read the built source out of <code>window.webpackChunk*</code> via <code>Function.prototype.toString()</code>.</p>
<p>Since the attack requires the dev server to be reachable via a non-potentially-trustworthy origin, only apps using <code>--host</code> (or <code>--host 0.0.0.0</code>) are affected. Chrome 142+ users are also protected by <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a Nuxt project with the webpack / rspack builder.</li>
<li>Run <code>npm run dev -- --host 0.0.0.0</code>.</li>
<li>Open <code>http://localhost:3000</code> on the developer machine.</li>
<li>From a different LAN host, serve the page below and open it in the same browser.</li>
<li>The compiled module source is exfiltrable from <code>window.webpackChunknuxt_&lt;projectname&gt;</code>.</li>
</ol>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;meta name="referrer" content="no-referrer"&gt;
&lt;script&gt;
  ['/_nuxt/runtime.js', '/_nuxt/app.js'].forEach(p =&gt; {
    const s = document.createElement('script')
    s.src = 'http://VICTIM_LAN_IP:3000' + p
    s.referrerPolicy = 'no-referrer'
    document.head.appendChild(s)
  })
  setTimeout(() =&gt; {
    const key = Object.keys(window).find(k =&gt; k.startsWith('webpackChunk'))
    for (const [, mods] of window[key]) {
      for (const id in mods) {
        console.log(id, mods[id].toString())
      }
    }
  }, 1500)
&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Users using the webpack / rspack builder with <code>nuxt dev --host</code> may get the built source code read by malicious websites on the same network, including module identifiers, the developer's local filesystem path, and any developer-controlled strings inlined into the bundle.</p>
<p>This vulnerability does not affect Chrome 142+ (and other Chromium-based browsers) users due to <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<p>The default Vite builder is not affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@nuxt/webpack-builder@4.4.7</code> / <code>@nuxt/rspack-builder@4.4.7</code> and backported to <code>@nuxt/webpack-builder@3.21.7</code> / <code>@nuxt/rspack-builder@3.21.7</code> by <a href="https://github.com/nuxt/nuxt/pull/35200">#35200</a> (4.x: commit <a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3"><code>e351de94</code></a>; 3.x: commit <a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05"><code>77187ee4</code></a>). The dev-middleware same-origin check now treats a request with no <code>Sec-Fetch-Site</code>, no <code>Origin</code>, and no <code>Referer</code> as same-origin only when the dev server is loopback-bound, closing the header-suppression bypass.</p>
<p>The fix only ships for the <code>@nuxt/webpack-builder</code> and <code>@nuxt/rspack-builder</code> packages. The default Vite builder was not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately:</p>
<ul>
<li>Don't use <code>nuxt dev --host</code>. Bind the dev server to <code>localhost</code> (the default) and tunnel from other devices via SSH or a reverse proxy that enforces same-origin checks.</li>
<li>Use Chrome 142+ or another Chromium-based browser that enforces <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</li>
<li>Switch to the Vite builder for development.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Berkan SAL (<a href="https://github.com/Uhudsavasindankacanokcu2">@Uhudsavasindankacanokcu2</a>) via the Vercel Open Source HackerOne program.</p>
<p>Independently reported by <a href="https://github.com/DavidCarliez">@DavidCarliez</a> via GitHub's coordinated disclosure flow (<code>GHSA-xw96-2f5x-v9pv</code>), closed as a duplicate of this advisory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g</a></li>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49993">https://nvd.nist.gov/vuln/detail/CVE-2026-49993</a></li>
<li><a href="https://github.com/nuxt/nuxt/pull/35200">https://github.com/nuxt/nuxt/pull/35200</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05">https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3">https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3</a></li>
<li><a href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/advisories/GHSA-x6qj-4h56-5rj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T23:39:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nuxt/webpack-builder] @nuxt/webpack-builder and @nuxt/rspack-builder dev server same-origin check bypassed when Sec-Fetch-Site, Origin, and Referer are all absent (incomplete fix for GHSA-6m52-m754-pw2g)]]></title>
        <id>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</id>
        <link href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5"/>
        <updated>2026-06-16T23:39:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>This is an incomplete fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a>. Source code may still be stolen during dev when using the webpack / rspack builder if the dev server is bound to a non-loopback address (e.g. <code>nuxt dev --host</code>) and the developer opens a malicious site on the same network.</p>
<h3 id="details">Details</h3>
<p>The fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a> added an <code>Origin</code> / <code>Referer</code> fallback to the dev-middleware same-origin check, with a <code>return true</code> branch when neither header is present so that non-browser clients (curl, the HMR client, address-bar navigation) keep working.</p>
<p>That fallback is bypassed when a cross-origin attacker request reaches the dev server with all three signal headers absent:</p>
<ul>
<li><code>Sec-Fetch-Site</code> is <a href="https://w3c.github.io/webappsec-fetch-metadata/#sec-fetch-site-header">not sent by browsers to non-potentially-trustworthy destinations</a> (HTTP on a non-loopback address).</li>
<li><code>Origin</code> is not sent on non-CORS subresource fetches (a bare <code>&lt;script&gt;</code> with no <code>crossorigin</code>).</li>
<li><code>Referer</code> can be suppressed by the attacker page with <code>&lt;meta name="referrer" content="no-referrer"&gt;</code> or <code>referrerpolicy="no-referrer"</code> on the <code>&lt;script&gt;</code> element.</li>
</ul>
<p>A classic <code>&lt;script src="http://VICTIM_LAN_IP:3000/_nuxt/app.js" referrerpolicy="no-referrer"&gt;</code> from a non-trustworthy attacker origin produces exactly that header set, the request is allowed, and the attacker page can read the built source out of <code>window.webpackChunk*</code> via <code>Function.prototype.toString()</code>.</p>
<p>Since the attack requires the dev server to be reachable via a non-potentially-trustworthy origin, only apps using <code>--host</code> (or <code>--host 0.0.0.0</code>) are affected. Chrome 142+ users are also protected by <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a Nuxt project with the webpack / rspack builder.</li>
<li>Run <code>npm run dev -- --host 0.0.0.0</code>.</li>
<li>Open <code>http://localhost:3000</code> on the developer machine.</li>
<li>From a different LAN host, serve the page below and open it in the same browser.</li>
<li>The compiled module source is exfiltrable from <code>window.webpackChunknuxt_&lt;projectname&gt;</code>.</li>
</ol>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;meta name="referrer" content="no-referrer"&gt;
&lt;script&gt;
  ['/_nuxt/runtime.js', '/_nuxt/app.js'].forEach(p =&gt; {
    const s = document.createElement('script')
    s.src = 'http://VICTIM_LAN_IP:3000' + p
    s.referrerPolicy = 'no-referrer'
    document.head.appendChild(s)
  })
  setTimeout(() =&gt; {
    const key = Object.keys(window).find(k =&gt; k.startsWith('webpackChunk'))
    for (const [, mods] of window[key]) {
      for (const id in mods) {
        console.log(id, mods[id].toString())
      }
    }
  }, 1500)
&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Users using the webpack / rspack builder with <code>nuxt dev --host</code> may get the built source code read by malicious websites on the same network, including module identifiers, the developer's local filesystem path, and any developer-controlled strings inlined into the bundle.</p>
<p>This vulnerability does not affect Chrome 142+ (and other Chromium-based browsers) users due to <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<p>The default Vite builder is not affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@nuxt/webpack-builder@4.4.7</code> / <code>@nuxt/rspack-builder@4.4.7</code> and backported to <code>@nuxt/webpack-builder@3.21.7</code> / <code>@nuxt/rspack-builder@3.21.7</code> by <a href="https://github.com/nuxt/nuxt/pull/35200">#35200</a> (4.x: commit <a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3"><code>e351de94</code></a>; 3.x: commit <a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05"><code>77187ee4</code></a>). The dev-middleware same-origin check now treats a request with no <code>Sec-Fetch-Site</code>, no <code>Origin</code>, and no <code>Referer</code> as same-origin only when the dev server is loopback-bound, closing the header-suppression bypass.</p>
<p>The fix only ships for the <code>@nuxt/webpack-builder</code> and <code>@nuxt/rspack-builder</code> packages. The default Vite builder was not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately:</p>
<ul>
<li>Don't use <code>nuxt dev --host</code>. Bind the dev server to <code>localhost</code> (the default) and tunnel from other devices via SSH or a reverse proxy that enforces same-origin checks.</li>
<li>Use Chrome 142+ or another Chromium-based browser that enforces <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</li>
<li>Switch to the Vite builder for development.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Berkan SAL (<a href="https://github.com/Uhudsavasindankacanokcu2">@Uhudsavasindankacanokcu2</a>) via the Vercel Open Source HackerOne program.</p>
<p>Independently reported by <a href="https://github.com/DavidCarliez">@DavidCarliez</a> via GitHub's coordinated disclosure flow (<code>GHSA-xw96-2f5x-v9pv</code>), closed as a duplicate of this advisory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g</a></li>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49993">https://nvd.nist.gov/vuln/detail/CVE-2026-49993</a></li>
<li><a href="https://github.com/nuxt/nuxt/pull/35200">https://github.com/nuxt/nuxt/pull/35200</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05">https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3">https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3</a></li>
<li><a href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/advisories/GHSA-x6qj-4h56-5rj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T23:39:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nuxt/webpack-builder] @nuxt/webpack-builder and @nuxt/rspack-builder dev server same-origin check bypassed when Sec-Fetch-Site, Origin, and Referer are all absent (incomplete fix for GHSA-6m52-m754-pw2g)]]></title>
        <id>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</id>
        <link href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5"/>
        <updated>2026-06-16T23:39:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>This is an incomplete fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a>. Source code may still be stolen during dev when using the webpack / rspack builder if the dev server is bound to a non-loopback address (e.g. <code>nuxt dev --host</code>) and the developer opens a malicious site on the same network.</p>
<h3 id="details">Details</h3>
<p>The fix for <a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">GHSA-6m52-m754-pw2g</a> added an <code>Origin</code> / <code>Referer</code> fallback to the dev-middleware same-origin check, with a <code>return true</code> branch when neither header is present so that non-browser clients (curl, the HMR client, address-bar navigation) keep working.</p>
<p>That fallback is bypassed when a cross-origin attacker request reaches the dev server with all three signal headers absent:</p>
<ul>
<li><code>Sec-Fetch-Site</code> is <a href="https://w3c.github.io/webappsec-fetch-metadata/#sec-fetch-site-header">not sent by browsers to non-potentially-trustworthy destinations</a> (HTTP on a non-loopback address).</li>
<li><code>Origin</code> is not sent on non-CORS subresource fetches (a bare <code>&lt;script&gt;</code> with no <code>crossorigin</code>).</li>
<li><code>Referer</code> can be suppressed by the attacker page with <code>&lt;meta name="referrer" content="no-referrer"&gt;</code> or <code>referrerpolicy="no-referrer"</code> on the <code>&lt;script&gt;</code> element.</li>
</ul>
<p>A classic <code>&lt;script src="http://VICTIM_LAN_IP:3000/_nuxt/app.js" referrerpolicy="no-referrer"&gt;</code> from a non-trustworthy attacker origin produces exactly that header set, the request is allowed, and the attacker page can read the built source out of <code>window.webpackChunk*</code> via <code>Function.prototype.toString()</code>.</p>
<p>Since the attack requires the dev server to be reachable via a non-potentially-trustworthy origin, only apps using <code>--host</code> (or <code>--host 0.0.0.0</code>) are affected. Chrome 142+ users are also protected by <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Create a Nuxt project with the webpack / rspack builder.</li>
<li>Run <code>npm run dev -- --host 0.0.0.0</code>.</li>
<li>Open <code>http://localhost:3000</code> on the developer machine.</li>
<li>From a different LAN host, serve the page below and open it in the same browser.</li>
<li>The compiled module source is exfiltrable from <code>window.webpackChunknuxt_&lt;projectname&gt;</code>.</li>
</ol>
<pre><code class="language-html">&lt;!doctype html&gt;
&lt;meta name="referrer" content="no-referrer"&gt;
&lt;script&gt;
  ['/_nuxt/runtime.js', '/_nuxt/app.js'].forEach(p =&gt; {
    const s = document.createElement('script')
    s.src = 'http://VICTIM_LAN_IP:3000' + p
    s.referrerPolicy = 'no-referrer'
    document.head.appendChild(s)
  })
  setTimeout(() =&gt; {
    const key = Object.keys(window).find(k =&gt; k.startsWith('webpackChunk'))
    for (const [, mods] of window[key]) {
      for (const id in mods) {
        console.log(id, mods[id].toString())
      }
    }
  }, 1500)
&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Users using the webpack / rspack builder with <code>nuxt dev --host</code> may get the built source code read by malicious websites on the same network, including module identifiers, the developer's local filesystem path, and any developer-controlled strings inlined into the bundle.</p>
<p>This vulnerability does not affect Chrome 142+ (and other Chromium-based browsers) users due to <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</p>
<p>The default Vite builder is not affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@nuxt/webpack-builder@4.4.7</code> / <code>@nuxt/rspack-builder@4.4.7</code> and backported to <code>@nuxt/webpack-builder@3.21.7</code> / <code>@nuxt/rspack-builder@3.21.7</code> by <a href="https://github.com/nuxt/nuxt/pull/35200">#35200</a> (4.x: commit <a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3"><code>e351de94</code></a>; 3.x: commit <a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05"><code>77187ee4</code></a>). The dev-middleware same-origin check now treats a request with no <code>Sec-Fetch-Site</code>, no <code>Origin</code>, and no <code>Referer</code> as same-origin only when the dev server is loopback-bound, closing the header-suppression bypass.</p>
<p>The fix only ships for the <code>@nuxt/webpack-builder</code> and <code>@nuxt/rspack-builder</code> packages. The default Vite builder was not affected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately:</p>
<ul>
<li>Don't use <code>nuxt dev --host</code>. Bind the dev server to <code>localhost</code> (the default) and tunnel from other devices via SSH or a reverse proxy that enforces same-origin checks.</li>
<li>Use Chrome 142+ or another Chromium-based browser that enforces <a href="https://developer.chrome.com/release-notes/142#local_network_access_restrictions">Local Network Access restrictions</a>.</li>
<li>Switch to the Vite builder for development.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Berkan SAL (<a href="https://github.com/Uhudsavasindankacanokcu2">@Uhudsavasindankacanokcu2</a>) via the Vercel Open Source HackerOne program.</p>
<p>Independently reported by <a href="https://github.com/DavidCarliez">@DavidCarliez</a> via GitHub's coordinated disclosure flow (<code>GHSA-xw96-2f5x-v9pv</code>), closed as a duplicate of this advisory.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g">https://github.com/nuxt/nuxt/security/advisories/GHSA-6m52-m754-pw2g</a></li>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/nuxt/nuxt/security/advisories/GHSA-x6qj-4h56-5rj5</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-49993">https://nvd.nist.gov/vuln/detail/CVE-2026-49993</a></li>
<li><a href="https://github.com/nuxt/nuxt/pull/35200">https://github.com/nuxt/nuxt/pull/35200</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05">https://github.com/nuxt/nuxt/commit/77187ee4015e9267fb464951542a3e09e8b5fa05</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3">https://github.com/nuxt/nuxt/commit/e351de943e82db16970618b60dc7fdbaa58630f3</a></li>
<li><a href="https://github.com/advisories/GHSA-x6qj-4h56-5rj5">https://github.com/advisories/GHSA-x6qj-4h56-5rj5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x6qj-4h56-5rj5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T23:39:16.000Z</published>
    </entry>
</feed>