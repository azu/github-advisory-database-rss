<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-06-16T17:38:31.330Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Git Node Clone and Push Operations Bypass File Sandbox]]></title>
        <id>https://github.com/advisories/GHSA-5xp3-2w67-427v</id>
        <link href="https://github.com/advisories/GHSA-5xp3-2w67-427v"/>
        <updated>2026-06-16T17:37:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could supply a local filesystem path as the source repository in the Git node's Clone operation, or as the target repository in the Push operation, bypassing the <code>N8N_RESTRICT_FILE_ACCESS_TO</code> file sandbox. This allowed the contents of any local git repository accessible to the n8n process to be cloned into an allowed path and read, circumventing the access restrictions that correctly blocked direct file reads to the same paths.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.48, 2.21.8, and 2.22.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Git node by adding <code>n8n-nodes-base.git</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-5xp3-2w67-427v">https://github.com/n8n-io/n8n/security/advisories/GHSA-5xp3-2w67-427v</a></li>
<li><a href="https://github.com/advisories/GHSA-5xp3-2w67-427v">https://github.com/advisories/GHSA-5xp3-2w67-427v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5xp3-2w67-427v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T17:37:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Git Node Clone and Push Operations Bypass File Sandbox]]></title>
        <id>https://github.com/advisories/GHSA-5xp3-2w67-427v</id>
        <link href="https://github.com/advisories/GHSA-5xp3-2w67-427v"/>
        <updated>2026-06-16T17:37:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could supply a local filesystem path as the source repository in the Git node's Clone operation, or as the target repository in the Push operation, bypassing the <code>N8N_RESTRICT_FILE_ACCESS_TO</code> file sandbox. This allowed the contents of any local git repository accessible to the n8n process to be cloned into an allowed path and read, circumventing the access restrictions that correctly blocked direct file reads to the same paths.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.48, 2.21.8, and 2.22.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Git node by adding <code>n8n-nodes-base.git</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-5xp3-2w67-427v">https://github.com/n8n-io/n8n/security/advisories/GHSA-5xp3-2w67-427v</a></li>
<li><a href="https://github.com/advisories/GHSA-5xp3-2w67-427v">https://github.com/advisories/GHSA-5xp3-2w67-427v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5xp3-2w67-427v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T17:37:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Git Node Clone and Push Operations Bypass File Sandbox]]></title>
        <id>https://github.com/advisories/GHSA-5xp3-2w67-427v</id>
        <link href="https://github.com/advisories/GHSA-5xp3-2w67-427v"/>
        <updated>2026-06-16T17:37:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows could supply a local filesystem path as the source repository in the Git node's Clone operation, or as the target repository in the Push operation, bypassing the <code>N8N_RESTRICT_FILE_ACCESS_TO</code> file sandbox. This allowed the contents of any local git repository accessible to the n8n process to be cloned into an allowed path and read, circumventing the access restrictions that correctly blocked direct file reads to the same paths.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.48, 2.21.8, and 2.22.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Git node by adding <code>n8n-nodes-base.git</code> to the <code>NODES_EXCLUDE</code> environment variable.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-5xp3-2w67-427v">https://github.com/n8n-io/n8n/security/advisories/GHSA-5xp3-2w67-427v</a></li>
<li><a href="https://github.com/advisories/GHSA-5xp3-2w67-427v">https://github.com/advisories/GHSA-5xp3-2w67-427v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5xp3-2w67-427v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T17:37:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Python sandbox escape]]></title>
        <id>https://github.com/advisories/GHSA-9pq8-m8gp-4p53</id>
        <link href="https://github.com/advisories/GHSA-9pq8-m8gp-4p53"/>
        <updated>2026-06-16T17:37:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows containing a Python Code Node could escape the sandbox and achieve arbitrary code execution on the task runner container.</p>
<p>This issue only affects instances where the Python Task Runner is enabled.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.48, 2.21.8, and 2.22.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Python Code node by adding <code>n8n-nodes-base.code</code> to the <code>NODES_EXCLUDE</code> environment variable, or disable the Python Task Runner entirely.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-9pq8-m8gp-4p53">https://github.com/n8n-io/n8n/security/advisories/GHSA-9pq8-m8gp-4p53</a></li>
<li><a href="https://github.com/advisories/GHSA-9pq8-m8gp-4p53">https://github.com/advisories/GHSA-9pq8-m8gp-4p53</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pq8-m8gp-4p53</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T17:37:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Python sandbox escape]]></title>
        <id>https://github.com/advisories/GHSA-9pq8-m8gp-4p53</id>
        <link href="https://github.com/advisories/GHSA-9pq8-m8gp-4p53"/>
        <updated>2026-06-16T17:37:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows containing a Python Code Node could escape the sandbox and achieve arbitrary code execution on the task runner container.</p>
<p>This issue only affects instances where the Python Task Runner is enabled.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.48, 2.21.8, and 2.22.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Python Code node by adding <code>n8n-nodes-base.code</code> to the <code>NODES_EXCLUDE</code> environment variable, or disable the Python Task Runner entirely.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-9pq8-m8gp-4p53">https://github.com/n8n-io/n8n/security/advisories/GHSA-9pq8-m8gp-4p53</a></li>
<li><a href="https://github.com/advisories/GHSA-9pq8-m8gp-4p53">https://github.com/advisories/GHSA-9pq8-m8gp-4p53</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pq8-m8gp-4p53</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T17:37:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[n8n] n8n: Python sandbox escape]]></title>
        <id>https://github.com/advisories/GHSA-9pq8-m8gp-4p53</id>
        <link href="https://github.com/advisories/GHSA-9pq8-m8gp-4p53"/>
        <updated>2026-06-16T17:37:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>An authenticated user with permission to create or modify workflows containing a Python Code Node could escape the sandbox and achieve arbitrary code execution on the task runner container.</p>
<p>This issue only affects instances where the Python Task Runner is enabled.</p>
<h2 id="patches">Patches</h2>
<p>The issue has been fixed in n8n versions 1.123.48, 2.21.8, and 2.22.4. Users should upgrade to one of these versions or later to remediate the vulnerability.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrading is not immediately possible, administrators should consider the following temporary mitigations:</p>
<ul>
<li>Limit workflow creation and editing permissions to fully trusted users only.</li>
<li>Disable the Python Code node by adding <code>n8n-nodes-base.code</code> to the <code>NODES_EXCLUDE</code> environment variable, or disable the Python Task Runner entirely.</li>
</ul>
<p>These workarounds do not fully remediate the risk and should only be used as short-term mitigation measures.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/n8n-io/n8n/security/advisories/GHSA-9pq8-m8gp-4p53">https://github.com/n8n-io/n8n/security/advisories/GHSA-9pq8-m8gp-4p53</a></li>
<li><a href="https://github.com/advisories/GHSA-9pq8-m8gp-4p53">https://github.com/advisories/GHSA-9pq8-m8gp-4p53</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9pq8-m8gp-4p53</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T17:37:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[astro] Astro: XSS via Unescaped Attribute Names in Spread Props]]></title>
        <id>https://github.com/advisories/GHSA-jrpj-wcv7-9fh9</id>
        <link href="https://github.com/advisories/GHSA-jrpj-wcv7-9fh9"/>
        <updated>2026-06-16T14:57:22.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>spreadAttributes</code> function in Astro's server-side rendering pipeline iterates over object keys and passes them directly to <code>addAttribute</code>, which interpolates the key into the HTML output without escaping. When a developer uses the spread syntax <code>{...props}</code> on an HTML element and the object keys come from an untrusted source (API, CMS, URL parameters), an attacker can inject arbitrary HTML attributes including event handlers like <code>onmousemove</code>, <code>onclick</code>, or break out of the attribute context entirely to inject new elements.</p>
<h2 id="details">Details</h2>
<p>The vulnerable function is <a href="https://github.com/withastro/astro/blob/main/packages/astro/src/runtime/server/render/util.ts#L81-L141"><code>addAttribute</code></a> at <code>packages/astro/src/runtime/server/render/util.ts:81-141</code>:</p>
<pre><code class="language-javascript">export function addAttribute(value: any, key: string, shouldEscape = true, tagName = '') {
    if (value == null) {
        return '';
    }
    
    return markHTMLString(` ${key}="${toAttributeString(value, shouldEscape)}"`); //  key interpolated not escaped
}
</code></pre>
<p>This function is called from <a href="https://github.com/withastro/astro/blob/main/packages/astro/src/runtime/server/index.ts#L91-L92"><code>spreadAttributes</code></a> at <code>packages/astro/src/runtime/server/index.ts:91-92</code>:</p>
<pre><code class="language-javascript">for (const [key, value] of Object.entries(values)) {
    output += addAttribute(value, key, true, _name);
}
</code></pre>
<p>The <code>toAttributeString</code> function escapes the attribute value, but the attribute name <code>key</code> is never validated or escaped. An attacker can craft a JSON object with a key containing " characters to break out of the attribute context and inject event handlers.</p>
<p>Execution flow: User controlled object keys (from API, CMS, URL params) are spread onto element via <code>{...props}</code>. The compiler generates <code>spreadAttributes(props)</code> which iterates with <code>Object.entries()</code> and calls <code>addAttribute(value, key)</code>. The key is interpolated as <code>` ${key}="${escapedValue}"`</code>. A malicious key breaks attribute context, resulting in XSS.</p>
<h2 id="poc">POC</h2>
<p>Create an SSR Astro page (<code>src/pages/index.astro</code>):</p>
<pre><code class="language-astro">---
const props = JSON.parse(Astro.url.searchParams.get('props') || '{}');
---
&lt;html&gt;
&lt;body&gt;
  &lt;h1&gt;Hello&lt;/h1&gt;
  &lt;div {...props}&gt;Move mouse here&lt;/div&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>Enable SSR in <code>astro.config.mjs</code> (for URL based demo):</p>
<pre><code class="language-javascript">export default defineConfig({
  output: 'server'
});
</code></pre>
<p>Note: SSR is not required for the vulnerability to exist. In static builds (default), the attack vector is compromised data sources at build time (API, CMS, database). SSR simply makes the PoC easier to demonstrate via URL parameters.</p>
<p>Start the dev server and visit:</p>
<pre><code>http://localhost:4321/?props={"x\" onmousemove=\"alert(document.cookie)\" y":""}
</code></pre>
<p>URL encoded:</p>
<pre><code>http://localhost:4321/?props=%7B%22x%5C%22%20onmousemove%3D%5C%22alert(document.cookie)%5C%22%20y%22%3A%22%22%7D
</code></pre>
<p>View the HTML source. The output contains:</p>
<pre><code class="language-html">&lt;div x" onmousemove="alert(document.cookie)" y=""&gt;Move mouse here&lt;/div&gt;
</code></pre>
<p>The key <code>x" onmousemove="alert(document.cookie)" y</code> breaks out of the attribute context. Moving the mouse over the div executes the JavaScript.</p>
<img width="1919" height="992" alt="Captura de tela 2026-06-02 005906" src="https://github.com/user-attachments/assets/ef69c12e-7edf-472e-97d1-3dfa540e61b4">

<h2 id="impact">Impact</h2>
<p>An attacker can execute arbitrary JavaScript in the context of a victim's browser session on any Astro application that spreads object props from untrusted sources onto HTML elements. This is a common pattern when integrating with external APIs or CMS systems. Exploitation enables session hijacking via cookie theft, credential theft by injecting fake login forms or keyloggers, defacement of the rendered page, and redirection to attacker controlled domains.</p>
<p>The vulnerability affects all Astro versions that support spread syntax on HTML elements and is exploitable in SSR, SSG (if build time data is compromised), and hybrid deployments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withastro/astro/security/advisories/GHSA-jrpj-wcv7-9fh9">https://github.com/withastro/astro/security/advisories/GHSA-jrpj-wcv7-9fh9</a></li>
<li><a href="https://github.com/advisories/GHSA-jrpj-wcv7-9fh9">https://github.com/advisories/GHSA-jrpj-wcv7-9fh9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jrpj-wcv7-9fh9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:57:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[astro] Astro: Host header SSRF in prerendered error page fetch]]></title>
        <id>https://github.com/advisories/GHSA-2pvr-wf23-7pc7</id>
        <link href="https://github.com/advisories/GHSA-2pvr-wf23-7pc7"/>
        <updated>2026-06-16T14:38:07.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Astro SSR apps with prerendered error pages (<code>/404</code> or <code>/500</code> using <code>export const prerender = true</code>) fetch those pages over HTTP at runtime when an error occurs. The URL for this fetch is derived from <code>request.url</code>, which in turn gets its origin from the incoming <code>Host</code> header. When the <code>Host</code> header is not validated against <code>allowedDomains</code>, an attacker can point the fetch at an arbitrary host and read the response.</p>
<h2 id="who-is-affected">Who is affected</h2>
<p>This affects SSR deployments that:</p>
<ol>
<li>Have a prerendered 404 or 500 page</li>
<li>Use <code>createRequestFromNodeRequest</code> from <code>astro/app/node</code> with <code>app.render()</code> <strong>without</strong> overriding <code>prerenderedErrorPageFetch</code> — this includes custom servers built on the public API and third-party adapters</li>
</ol>
<p><strong>Not affected:</strong></p>
<ul>
<li><code>@astrojs/node</code> &gt;= 9.5.4 (reads error pages from disk)</li>
<li><code>@astrojs/cloudflare</code> (uses the ASSETS binding)</li>
<li>The dev server (renders error pages in-process)</li>
</ul>
<h2 id="how-it-works">How it works</h2>
<p><code>createRequestFromNodeRequest</code> builds <code>request.url</code> from the raw <code>Host</code> / <code>:authority</code> header. The <code>allowedDomains</code> option is accepted but only gates <code>X-Forwarded-For</code> — it does not constrain the URL origin. (The public <code>createRequest</code> does fall back to <code>localhost</code> for unvalidated hosts; this internal builder did not.)</p>
<p>When <code>app.render()</code> encounters a 404 or 500 with a prerendered error route, <code>default-handler.ts</code> constructs the error page URL using the origin from <code>request.url</code> and fetches it via <code>prerenderedErrorPageFetch</code>, which defaults to global <code>fetch</code>. The response body is served to the client.</p>
<p>An attacker sends a request with <code>Host: attacker-host:port</code>, triggers an error (e.g., requesting a nonexistent path for a 404), and receives the response from the attacker-controlled host reflected back.</p>
<h2 id="remediation">Remediation</h2>
<p>The error page fetch origin is now validated against <code>allowedDomains</code> before use. When the host is validated, the original origin is preserved. Otherwise, it falls back to <code>localhost</code>. The fetch is also wrapped in a try/catch so that connection failures degrade gracefully to a plain error response.</p>
<h2 id="credit">Credit</h2>
<p>5ud0 / Tarmo Technologies</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withastro/astro/security/advisories/GHSA-2pvr-wf23-7pc7">https://github.com/withastro/astro/security/advisories/GHSA-2pvr-wf23-7pc7</a></li>
<li><a href="https://github.com/advisories/GHSA-2pvr-wf23-7pc7">https://github.com/advisories/GHSA-2pvr-wf23-7pc7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2pvr-wf23-7pc7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T14:38:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@astrojs/netlify] @astrojs/netlify broadens Astro image.remotePatterns in Netlify Image CDN config]]></title>
        <id>https://github.com/advisories/GHSA-529g-xq4f-cw38</id>
        <link href="https://github.com/advisories/GHSA-529g-xq4f-cw38"/>
        <updated>2026-06-16T14:37:34.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>@astrojs/netlify</code> converts Astro <code>image.remotePatterns</code> into Netlify Image CDN <code>images.remote_images</code> regular expressions with broader semantics than Astro's canonical matcher. A single wildcard hostname such as <code>*.example.com</code> is converted to an optional subdomain regex, so the apex host matches. A single wildcard pathname such as <code>/ok/*</code> is converted without end anchoring, so deeper paths match by prefix.</p>
<h2 id="technical-details">Technical details</h2>
<p>The Netlify adapter generates regex strings for Netlify Image CDN from <code>image.remotePatterns</code>. For <code>*.example.com</code>, it emits <code>([a-z0-9-]+\\.)?example\\.com</code>, which makes the subdomain optional. Astro's canonical helper requires exactly one subdomain and rejects the apex host.</p>
<p>For <code>/ok/*</code>, the adapter emits a segment regex but does not anchor the end of the URL. Netlify's Image CDN implementation treats <code>images.remote_images</code> entries as JavaScript regular expressions and calls <code>.test(sourceImageUrl.href)</code>, so a URL such as <code>/ok/a/b.svg</code> matches the <code>/ok/a</code> prefix even though Astro's helper rejects it.</p>
<p>The latest npm package <code>@astrojs/netlify@7.0.10</code> contains this conversion logic, and a minimal Astro build writes the broadened patterns into <code>.netlify/v1/config.json</code>.</p>
<h2 id="reproduction">Reproduction</h2>
<ol>
<li>Create an Astro app using <code>astro@6.3.8</code> and <code>@astrojs/netlify@7.0.10</code>.</li>
<li>Configure Netlify output and a restrictive image pattern, for example <code>remotePatterns: [{ protocol: 'http', hostname: '*.localhost', pathname: '/ok/*' }]</code>.</li>
<li>Build the app and observe that <code>.netlify/v1/config.json</code> contains <code>http://([a-z0-9-]+\\.)?localhost(:[0-9]+)?(\\/ok/[^/?#]+)/?([?][^#]*)?</code>.</li>
<li>Serve a canary SVG on <code>127.0.0.1:9001</code>.</li>
<li>Request <code>/.netlify/images?url=http%3A%2F%2Flocalhost%3A9001%2Fok%2Fa.svg&amp;w=100</code>. Astro's helper rejects the apex <code>localhost</code> for <code>*.localhost</code>, but Netlify Image CDN accepts it and fetches the canary.</li>
<li>As a negative control, request <code>/.netlify/images?url=http%3A%2F%2Flocalhost%3A9001%2Fnope%2Fa.svg&amp;w=100</code>. This returns <code>403 Forbidden: Remote image URL not allowed</code> and does not hit the canary.</li>
<li>Request <code>/.netlify/images?url=http%3A%2F%2Flocalhost%3A9001%2Fok%2Fa%2Fb.svg&amp;w=100</code>. Astro's <code>/ok/*</code> helper rejects this deeper path, but Netlify Image CDN accepts it and fetches the canary.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>Any Astro app deployed with <code>@astrojs/netlify</code> and a restrictive <code>image.remotePatterns</code> config can expose a wider image-fetch boundary than intended. Public requests to the Netlify Image CDN endpoint can fetch URLs that Astro's own matcher would reject, including apex hosts for <code>*.host</code> patterns and deeper paths for <code>/path/*</code> patterns. The practical impact depends on what the application intended to isolate behind the remote image allowlist, but it can disclose image-like resources from unintended hosts or paths behind the same configured remote origin family.</p>
<h2 id="remediation">Remediation</h2>
<p>Generate regexes that exactly match Astro's canonical <code>matchHostname</code> and <code>matchPathname</code> semantics, and anchor the full URL match before writing <code>images.remote_images</code>. In particular, <code>*.example.com</code> should require exactly one subdomain and should not match <code>example.com</code>, and <code>/ok/*</code> should match exactly one additional path segment and should not match <code>/ok/a/b</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withastro/astro/security/advisories/GHSA-529g-xq4f-cw38">https://github.com/withastro/astro/security/advisories/GHSA-529g-xq4f-cw38</a></li>
<li><a href="https://github.com/advisories/GHSA-529g-xq4f-cw38">https://github.com/advisories/GHSA-529g-xq4f-cw38</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-529g-xq4f-cw38</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:37:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono: Body Limit Middleware can be bypassed on AWS Lambda by understating `Content-Length`]]></title>
        <id>https://github.com/advisories/GHSA-rv63-4mwf-qqc2</id>
        <link href="https://github.com/advisories/GHSA-rv63-4mwf-qqc2"/>
        <updated>2026-06-16T14:32:50.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Body Limit Middleware trusts the request's <code>Content-Length</code> header to decide whether a body is within the limit. On AWS Lambda (API Gateway v1/v2, ALB, VPC Lattice, and Lambda@Edge) the body is delivered fully buffered and the adapter builds the request with the client-declared <code>Content-Length</code>, which need not match the actual payload. A client can declare a tiny <code>Content-Length</code> while sending a much larger body, slipping past the limit.</p>
<h3 id="details">Details</h3>
<p>When <code>Content-Length</code> is present and <code>Transfer-Encoding</code> is absent, the middleware compares the declared value against the limit and passes the request through if it is small enough. On standards-based runtimes the transport enforces that <code>Content-Length</code> matches the body, so this is safe. The Lambda adapters instead reconstruct the request from a buffered payload and copy the client's <code>Content-Length</code> verbatim, so the declared length and the real body size are decoupled.</p>
<p>This issue affects applications deployed on AWS Lambda that rely on the Body Limit Middleware to cap request body size.</p>
<h3 id="impact">Impact</h3>
<p>The declared body-size limit can be bypassed: a handler reads a payload larger than the configured maximum. Processing the oversized payload (large JSON, multipart, etc.) consumes additional CPU and memory per request. The payload remains bounded by the platform's request size limits, and Lambda isolates invocations, so the impact is increased per-request resource usage rather than full denial of service. This affects applications deployed on AWS Lambda that use the Body Limit Middleware.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-rv63-4mwf-qqc2">https://github.com/honojs/hono/security/advisories/GHSA-rv63-4mwf-qqc2</a></li>
<li><a href="https://github.com/advisories/GHSA-rv63-4mwf-qqc2">https://github.com/advisories/GHSA-rv63-4mwf-qqc2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rv63-4mwf-qqc2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:32:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono: Lambda@Edge adapter keeps only the last value of a repeated request header, dropping the rest]]></title>
        <id>https://github.com/advisories/GHSA-wgpf-jwqj-8h8p</id>
        <link href="https://github.com/advisories/GHSA-wgpf-jwqj-8h8p"/>
        <updated>2026-06-16T14:32:32.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>On AWS Lambda@Edge, CloudFront delivers a request header that appears more than once as several separate entries. The adapter writes each value with <code>Headers.set</code> instead of <code>Headers.append</code>, so every value overwrites the previous one and only the last reaches the application. Repeated request headers such as <code>X-Forwarded-For</code>, <code>Forwarded</code>, and <code>Via</code> are silently truncated to a single value.</p>
<h3 id="details">Details</h3>
<p>A repeated request header carries an ordered list of values. The adapter iterates the list but overwrites on each step, keeping only the final value. Middleware that depends on the full list — for example IP restriction that walks the <code>X-Forwarded-For</code> chain, or auditing based on <code>Forwarded</code>/<code>Via</code> hops — receives incomplete data. The API Gateway adapter already appends repeated values and is not affected.</p>
<p>This issue arises only on Lambda@Edge deployments, for requests that contain the same header more than once.</p>
<h3 id="impact">Impact</h3>
<p>Request middleware sees only the last value of a repeated header instead of the full chain. For applications that base access control on the <code>X-Forwarded-For</code> chain, this can weaken or alter that decision; for auditing, hop history is lost. This affects applications deployed on AWS Lambda@Edge that rely on multi-value request headers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-wgpf-jwqj-8h8p">https://github.com/honojs/hono/security/advisories/GHSA-wgpf-jwqj-8h8p</a></li>
<li><a href="https://github.com/advisories/GHSA-wgpf-jwqj-8h8p">https://github.com/advisories/GHSA-wgpf-jwqj-8h8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wgpf-jwqj-8h8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:32:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono: CORS Middleware reflects any Origin with credentials when `origin` defaults to the wildcard]]></title>
        <id>https://github.com/advisories/GHSA-88fw-hqm2-52qc</id>
        <link href="https://github.com/advisories/GHSA-88fw-hqm2-52qc"/>
        <updated>2026-06-16T14:15:40.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>With <code>credentials: true</code> and no explicit <code>origin</code> (the default wildcard), the CORS Middleware reflects the request's <code>Origin</code> and sends <code>Access-Control-Allow-Credentials: true</code>. Any site can then make credentialed cross-origin requests and read the responses, exposing cookie-authenticated endpoints to arbitrary origins.</p>
<h3 id="details">Details</h3>
<p>The spec forbids <code>Access-Control-Allow-Origin: *</code> with credentials and browsers reject it, so this configuration used to fail closed. In affected versions the middleware reflects the request <code>Origin</code> instead, so it now succeeds for every origin, including <code>null</code>. The preflight also echoes the requested headers back, approving non-simple credentialed requests too.</p>
<p>This issue arises when an application enables <code>credentials: true</code> and leaves <code>origin</code> unset or set to the wildcard.</p>
<h3 id="impact">Impact</h3>
<p>Any third-party page a logged-in user visits can read the application's cookie-authenticated endpoints and perform credentialed state-changing requests. This affects applications that enable credentialed CORS without restricting <code>origin</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-88fw-hqm2-52qc">https://github.com/honojs/hono/security/advisories/GHSA-88fw-hqm2-52qc</a></li>
<li><a href="https://github.com/advisories/GHSA-88fw-hqm2-52qc">https://github.com/advisories/GHSA-88fw-hqm2-52qc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-88fw-hqm2-52qc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T14:15:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono: Path traversal in `serve-static` on Windows via encoded backslash (`%5C`)]]></title>
        <id>https://github.com/advisories/GHSA-wwfh-h76j-fc44</id>
        <link href="https://github.com/advisories/GHSA-wwfh-h76j-fc44"/>
        <updated>2026-06-16T14:09:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>On Windows hosts, an encoded backslash (<code>%5C</code>) in the request path decodes to <code>\</code>, which the Windows path resolver treats as a separator. <code>serve-static</code> then resolves a single URL segment such as <code>admin\secret.txt</code> into a nested file under the root and serves it, letting an attacker read static files meant to be protected behind prefix-mounted middleware. Directory escape (<code>..</code>) remains blocked.</p>
<h3 id="details">Details</h3>
<p>The router splits paths only on <code>/</code>, so <code>/admin%5Csecret.txt</code> is one segment and middleware on <code>/admin/*</code> does not run. The <code>serve-static</code> guard rejects <code>.</code>/<code>..</code> and consecutive separators but lets a lone <code>\</code> through; on Windows the file resolver re-splits it into the protected subtree.</p>
<p>This affects Windows hosts serving static files via the Node, Bun, or Deno adapters that guard a static subtree with prefix-mounted middleware.</p>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can read static files under a middleware-guarded prefix on Windows hosts. The read stays within the configured root; escape outside the root is not possible.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-wwfh-h76j-fc44">https://github.com/honojs/hono/security/advisories/GHSA-wwfh-h76j-fc44</a></li>
<li><a href="https://github.com/advisories/GHSA-wwfh-h76j-fc44">https://github.com/advisories/GHSA-wwfh-h76j-fc44</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wwfh-h76j-fc44</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:09:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[hono] hono: AWS Lambda adapter merges multiple `Set-Cookie` headers into one value, dropping cookies on ALB single-header and Lattice]]></title>
        <id>https://github.com/advisories/GHSA-j6c9-x7qj-28xf</id>
        <link href="https://github.com/advisories/GHSA-j6c9-x7qj-28xf"/>
        <updated>2026-06-16T14:08:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>On AWS Lambda, the ALB single-header response and the VPC Lattice v2 response join multiple <code>Set-Cookie</code> headers into one comma-separated value. Because commas also appear inside cookie attributes (for example <code>Expires</code> dates), clients cannot split the value back into individual cookies and silently drop or misparse them.</p>
<h3 id="details">Details</h3>
<p>Per RFC 6265, each cookie must be its own <code>Set-Cookie</code> header line, and commas may appear inside attribute values. Joining cookies with <code>", "</code> collides with those commas, producing a value that clients cannot reliably split. Only ALB single-header mode and VPC Lattice v2 are affected; API Gateway v1/v2 and ALB with multi-value headers enabled already use an array and are unaffected.</p>
<h3 id="impact">Impact</h3>
<p>A client may receive only one of the cookies, a malformed cookie, or none. Session, CSRF, or preference cookies can silently fail to apply, breaking sessions or forcing re-authentication. This affects applications that set multiple cookies per response and run on AWS Lambda behind an ALB in single-header mode (the default) or VPC Lattice v2.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/honojs/hono/security/advisories/GHSA-j6c9-x7qj-28xf">https://github.com/honojs/hono/security/advisories/GHSA-j6c9-x7qj-28xf</a></li>
<li><a href="https://github.com/advisories/GHSA-j6c9-x7qj-28xf">https://github.com/advisories/GHSA-j6c9-x7qj-28xf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j6c9-x7qj-28xf</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T14:08:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[astro] Astro: Reflected XSS via unescaped slot name]]></title>
        <id>https://github.com/advisories/GHSA-8hv8-536x-4wqp</id>
        <link href="https://github.com/advisories/GHSA-8hv8-536x-4wqp"/>
        <updated>2026-06-16T14:05:25.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When a component uses a <code>client:*</code> directive, Astro inserts named slot content into a <code>data-astro-template</code> attribute without HTML escaping the slot name allowing an attacker to break out of the attribute context and inject arbitrary HTML, resulting in reflected XSS during SSR.</p>
<p>This is similar to GHSA-wrwg-2hg8-v723 but exploits a different injection point.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<p><code>packages/astro/src/runtime/server/render/component.ts:371:376</code></p>
<pre><code class="language-ts">// component.ts:371
`&lt;template data-astro-template${key !== 'default' ? `="${key}"` : ''}&gt;${children[key]}&lt;/template&gt;`
</code></pre>
<p>I found that key is interpolated directly into the attribute value without proper escaping.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<p>For the PoC, I set up with a minimal repository with Astro 6.3.1, Node.js: v26.0.0.</p>
<p><strong><code>astro.config.mjs</code></strong></p>
<pre><code class="language-js">import react from '@astrojs/react';
import node from '@astrojs/node';
import { defineConfig } from 'astro/config';
export default defineConfig({
  output: 'server',
  adapter: node({ mode: 'standalone' }),
  integrations: [react()],
});
</code></pre>
<h2 id="srcpagesindexastro-astro"><strong><code>src/pages/index.astro</code></strong>
```astro</h2>
<h2 id="import-wrapper-from-componentswrapperjsx-const-slotname--astrourlsearchparamsgettab--default">import Wrapper from '../components/Wrapper.jsx';
const slotName = Astro.url.searchParams.get('tab') ?? 'default';</h2>

  
    <div slot="{slotName}">content</div>
  

```

<p><strong><code>src/components/Wrapper.jsx</code></strong></p>
<pre><code class="language-jsx">export default function Wrapper() { return null; }
</code></pre>
<p><strong>Payload:</strong></p>
<pre><code>abc"&gt;&lt;/template&gt;&lt;/astro-island&gt;&lt;img src=x onerror=confirm(document.domain)&gt;&lt;!--
</code></pre>
<p>Accessing this URL will trigger the popup.</p>
<p><a href="http://localhost:4321/?tab=abc%22%3E%3C%2Ftemplate%3E%3C%2Fastro-island%3E%3Cimg+src%3Dx+onerror%3Dconfirm(document.domain)%3E%3C!--">http://localhost:4321/?tab=abc%22%3E%3C%2Ftemplate%3E%3C%2Fastro-island%3E%3Cimg+src%3Dx+onerror%3Dconfirm(document.domain)%3E%3C!--</a></p>
<img width="1268" height="592" alt="image" src="https://github.com/user-attachments/assets/675cdc04-4134-4d83-883c-abe16d751ec7">



<p>This will render in html.</p>
<pre><code class="language-html">&lt;template data-astro-template="abc"&gt;&lt;/template&gt;&lt;/astro-island&gt;
&lt;img src=x onerror=confirm(document.domain)&gt;&lt;!--"&gt;content&lt;/template&gt;
</code></pre>
<h2 id="fix">Fix</h2>
<p>I suggest leveraging the existing escape function on the slot name.</p>
<pre><code class="language-ts">// component.ts:371
`&lt;template data-astro-template${key !== 'default' ? `="${escapeHTML(String(key))}"` : ''}&gt;${children[key]}&lt;/template&gt;`
</code></pre>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withastro/astro/security/advisories/GHSA-8hv8-536x-4wqp">https://github.com/withastro/astro/security/advisories/GHSA-8hv8-536x-4wqp</a></li>
<li><a href="https://github.com/advisories/GHSA-8hv8-536x-4wqp">https://github.com/advisories/GHSA-8hv8-536x-4wqp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8hv8-536x-4wqp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T14:05:06.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nuxt] Nuxt: Reflected XSS in `<NuxtLink>` via unsanitised `javascript:` or `data:` URL]]></title>
        <id>https://github.com/advisories/GHSA-934w-87qh-qr26</id>
        <link href="https://github.com/advisories/GHSA-934w-87qh-qr26"/>
        <updated>2026-06-16T13:49:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>&lt;NuxtLink&gt;</code> did not validate the URL scheme of values bound to its <code>to</code> or <code>href</code> props before rendering them into the <code>href</code> attribute of the underlying <code>&lt;a&gt;</code> element. When an application binds attacker-controlled input (a query parameter, a CMS field, a user-supplied profile URL) to <code>&lt;NuxtLink :to&gt;</code> or <code>:href</code>, the attacker can supply a <code>javascript:</code> or <code>vbscript:</code> URL that is reflected verbatim into the rendered markup. Clicking the link executes the supplied script in the origin of the Nuxt application, resulting in reflected DOM-based cross-site scripting. A <code>data:text/html,...</code> payload reflected through the same sink does not execute in the application's origin but enables a same-tab phishing surface anchored to a legitimate application link.</p>
<p>The same value was exposed to consumers of the component's <code>custom</code> slot via the <code>href</code> and <code>route.href</code> props, so applications that re-bind those values to their own anchors were affected identically.</p>
<p>Unlike the previously reported <code>navigateTo</code> issue (CVE-2024-34343), the sink here is the rendered anchor itself; the existing <code>isScriptProtocol</code> checks in <code>navigateTo</code> and <code>reloadNuxtApp</code> are not on the code path. The <code>onClick</code> handler intentionally returns early for external links so the browser's native protocol-based navigation runs.</p>
<h3 id="affected-component">Affected component</h3>
<ul>
<li>File: <code>packages/nuxt/src/app/components/nuxt-link.ts</code></li>
<li>Sink: <code>h('a', { href: href.value, ... })</code> in the default render, plus the <code>href</code> / <code>route.href</code> props passed to the <code>custom</code> slot.</li>
<li>Broken check: external auto-detection treated any <code>hasProtocol(path, { acceptRelative: true })</code> value as an "external link", then rendered the value directly as <code>&lt;a href&gt;</code> without rejecting script-capable protocols. There was no equivalent of the <code>navigateTo</code> <code>isScriptProtocol(protocol)</code> gate in this path.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Any Nuxt application that binds user-controlled values to <code>&lt;NuxtLink :to&gt;</code> / <code>:href</code> was vulnerable. Common shapes: profile-link rendering (<code>&lt;NuxtLink :to="user.website"&gt;</code>), "share this" / "open in new tab" handlers that pass through a query parameter, CMS-driven landing pages that render <code>&lt;NuxtLink :to="cms.cta.url"&gt;</code>, and marketplace listings that show seller-supplied links.</p>
<p>For <code>javascript:</code> / <code>vbscript:</code> the primitive is reflected XSS in the application's first-party origin (session theft for non-<code>HttpOnly</code> cookies, CSRF token theft, account takeover via DOM rewriting, credential harvesting via fake login overlays). For <code>data:text/html,...</code> the attacker gets a same-tab phishing surface anchored to a legitimate application link.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>nuxt@4.4.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/0103ce06fbbbdfa079a7f020ef8ce00121eac4a3"><code>0103ce06</code></a>) and backported to <code>nuxt@3.21.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/53284043dc21210a25d629d1cec67d3ae557ffd0"><code>53284043</code></a>). The fix sanitises the resolved external <code>href</code> before it is passed to <code>&lt;a&gt;</code> or the <code>custom</code> slot: control characters and whitespace are stripped, leading <code>view-source:</code> prefixes are unwrapped, and any remaining script-capable scheme (per <code>isScriptProtocol</code>) causes the <code>href</code> to be replaced with an empty string.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Until you can upgrade, validate URLs at the source before binding them to <code>&lt;NuxtLink :to&gt;</code> / <code>:href</code>. For example, only accept paths that start with <code>/</code> (and not <code>//</code>), or run user-supplied URLs through <code>new URL(value)</code> and reject anything whose <code>protocol</code> is not in an allow-list (typically <code>http:</code> and <code>https:</code>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-934w-87qh-qr26">https://github.com/nuxt/nuxt/security/advisories/GHSA-934w-87qh-qr26</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53722">https://nvd.nist.gov/vuln/detail/CVE-2026-53722</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/0103ce06fbbbdfa079a7f020ef8ce00121eac4a3">https://github.com/nuxt/nuxt/commit/0103ce06fbbbdfa079a7f020ef8ce00121eac4a3</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/53284043dc21210a25d629d1cec67d3ae557ffd0">https://github.com/nuxt/nuxt/commit/53284043dc21210a25d629d1cec67d3ae557ffd0</a></li>
<li><a href="https://github.com/advisories/GHSA-934w-87qh-qr26">https://github.com/advisories/GHSA-934w-87qh-qr26</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-934w-87qh-qr26</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:49:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nuxt] Nuxt: Reflected XSS in `<NuxtLink>` via unsanitised `javascript:` or `data:` URL]]></title>
        <id>https://github.com/advisories/GHSA-934w-87qh-qr26</id>
        <link href="https://github.com/advisories/GHSA-934w-87qh-qr26"/>
        <updated>2026-06-16T13:49:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>&lt;NuxtLink&gt;</code> did not validate the URL scheme of values bound to its <code>to</code> or <code>href</code> props before rendering them into the <code>href</code> attribute of the underlying <code>&lt;a&gt;</code> element. When an application binds attacker-controlled input (a query parameter, a CMS field, a user-supplied profile URL) to <code>&lt;NuxtLink :to&gt;</code> or <code>:href</code>, the attacker can supply a <code>javascript:</code> or <code>vbscript:</code> URL that is reflected verbatim into the rendered markup. Clicking the link executes the supplied script in the origin of the Nuxt application, resulting in reflected DOM-based cross-site scripting. A <code>data:text/html,...</code> payload reflected through the same sink does not execute in the application's origin but enables a same-tab phishing surface anchored to a legitimate application link.</p>
<p>The same value was exposed to consumers of the component's <code>custom</code> slot via the <code>href</code> and <code>route.href</code> props, so applications that re-bind those values to their own anchors were affected identically.</p>
<p>Unlike the previously reported <code>navigateTo</code> issue (CVE-2024-34343), the sink here is the rendered anchor itself; the existing <code>isScriptProtocol</code> checks in <code>navigateTo</code> and <code>reloadNuxtApp</code> are not on the code path. The <code>onClick</code> handler intentionally returns early for external links so the browser's native protocol-based navigation runs.</p>
<h3 id="affected-component">Affected component</h3>
<ul>
<li>File: <code>packages/nuxt/src/app/components/nuxt-link.ts</code></li>
<li>Sink: <code>h('a', { href: href.value, ... })</code> in the default render, plus the <code>href</code> / <code>route.href</code> props passed to the <code>custom</code> slot.</li>
<li>Broken check: external auto-detection treated any <code>hasProtocol(path, { acceptRelative: true })</code> value as an "external link", then rendered the value directly as <code>&lt;a href&gt;</code> without rejecting script-capable protocols. There was no equivalent of the <code>navigateTo</code> <code>isScriptProtocol(protocol)</code> gate in this path.</li>
</ul>
<h3 id="impact">Impact</h3>
<p>Any Nuxt application that binds user-controlled values to <code>&lt;NuxtLink :to&gt;</code> / <code>:href</code> was vulnerable. Common shapes: profile-link rendering (<code>&lt;NuxtLink :to="user.website"&gt;</code>), "share this" / "open in new tab" handlers that pass through a query parameter, CMS-driven landing pages that render <code>&lt;NuxtLink :to="cms.cta.url"&gt;</code>, and marketplace listings that show seller-supplied links.</p>
<p>For <code>javascript:</code> / <code>vbscript:</code> the primitive is reflected XSS in the application's first-party origin (session theft for non-<code>HttpOnly</code> cookies, CSRF token theft, account takeover via DOM rewriting, credential harvesting via fake login overlays). For <code>data:text/html,...</code> the attacker gets a same-tab phishing surface anchored to a legitimate application link.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>nuxt@4.4.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/0103ce06fbbbdfa079a7f020ef8ce00121eac4a3"><code>0103ce06</code></a>) and backported to <code>nuxt@3.21.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/53284043dc21210a25d629d1cec67d3ae557ffd0"><code>53284043</code></a>). The fix sanitises the resolved external <code>href</code> before it is passed to <code>&lt;a&gt;</code> or the <code>custom</code> slot: control characters and whitespace are stripped, leading <code>view-source:</code> prefixes are unwrapped, and any remaining script-capable scheme (per <code>isScriptProtocol</code>) causes the <code>href</code> to be replaced with an empty string.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Until you can upgrade, validate URLs at the source before binding them to <code>&lt;NuxtLink :to&gt;</code> / <code>:href</code>. For example, only accept paths that start with <code>/</code> (and not <code>//</code>), or run user-supplied URLs through <code>new URL(value)</code> and reject anything whose <code>protocol</code> is not in an allow-list (typically <code>http:</code> and <code>https:</code>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-934w-87qh-qr26">https://github.com/nuxt/nuxt/security/advisories/GHSA-934w-87qh-qr26</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53722">https://nvd.nist.gov/vuln/detail/CVE-2026-53722</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/0103ce06fbbbdfa079a7f020ef8ce00121eac4a3">https://github.com/nuxt/nuxt/commit/0103ce06fbbbdfa079a7f020ef8ce00121eac4a3</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/53284043dc21210a25d629d1cec67d3ae557ffd0">https://github.com/nuxt/nuxt/commit/53284043dc21210a25d629d1cec67d3ae557ffd0</a></li>
<li><a href="https://github.com/advisories/GHSA-934w-87qh-qr26">https://github.com/advisories/GHSA-934w-87qh-qr26</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-934w-87qh-qr26</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:49:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nuxt] Nuxt dev server vite-node IPC socket is world-connectable on Linux]]></title>
        <id>https://github.com/advisories/GHSA-534h-c3cw-v3h9</id>
        <link href="https://github.com/advisories/GHSA-534h-c3cw-v3h9"/>
        <updated>2026-06-16T13:49:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When running <code>nuxt dev</code> on Linux (Node.js 20+, outside Docker / StackBlitz), Nuxt's internal vite-node IPC server binds to a Linux abstract-namespace Unix socket (<code>\0nuxt-vite-node-&lt;pid&gt;-&lt;ts&gt;.sock</code>). Abstract sockets have no filesystem inode and therefore no permission bits: any local UID on the host that can read <code>/proc/net/unix</code> can enumerate the socket and connect to it.</p>
<p>The IPC server does not perform any peer-credential or shared-secret check before dispatching requests. The <code>module</code> request type passes its <code>moduleId</code> field straight into Vite's SSR <code>fetchModule()</code>, which is not gated by Vite's HTTP-layer <code>server.fs.allow</code> deny-list. A co-resident unprivileged local user can therefore request paths like <code>/home/&lt;dev&gt;/project/.env?raw</code> or <code>~/.ssh/id_rsa?raw</code> and read the developer's secrets through the dev server's SSR plugin pipeline. The <code>resolve</code> request type additionally enables filesystem probing.</p>
<p>This affects developers running <code>nuxt dev</code> on shared multi-tenant Linux hosts (lab machines, shared bastions, CI runners shared between jobs without per-job container isolation). It does not affect:</p>
<ul>
<li>Production builds (<code>nuxt build</code> / <code>nuxt start</code>). The IPC server only runs in development.</li>
<li>macOS or Windows developers.</li>
<li>Docker / StackBlitz environments, which already fall back to a filesystem socket.</li>
<li>Single-user laptops or per-job containerised CI.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>nuxt@4.4.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/1f9f4767a8725104da9bee872bb8d35246f25ae5"><code>1f9f4767</code></a>) and backported to <code>nuxt@3.21.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/c293bf9503ccb3bc9559bff4a1f592f99063c9ea"><code>c293bf95</code></a>).</p>
<p>The fix removes the abstract-namespace branch entirely. The IPC server now always binds to a filesystem Unix socket under the OS temp directory and explicitly <code>chmod 0600</code>s it after <code>listen()</code>, restricting connections to the owning UID. If the chmod fails for any reason, the server closes rather than serve requests on an unrestricted channel.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately on an affected host:</p>
<ul>
<li>Run <code>nuxt dev</code> inside a container or VM with no other tenants. Docker already triggers the filesystem-socket fallback in vulnerable versions and that fallback is unaffected.</li>
<li>Bind the dev process to a single-user namespace (<code>unshare -U</code>, rootless containers).</li>
<li>Restrict <code>/proc/net/unix</code> visibility via <code>hidepid=2</code> mount options where applicable, though this is partial mitigation only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Affected file: <code>packages/vite/src/plugins/vite-node.ts</code></li>
<li>CWE-276: Incorrect Default Permissions</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Anthropic / Claude as part of Anthropic's coordinated vulnerability disclosure programme, reference ANT-2026-MSNKZFAT. Thanks to the Anthropic security team for the report and the detailed reproduction.</p>
<p>Independently reported by <a href="https://github.com/alcls01111">@alcls01111</a> via GitHub's coordinated disclosure flow (<code>GHSA-5gvc-46gq-948j</code>), closed as a duplicate of this advisory.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-534h-c3cw-v3h9">https://github.com/nuxt/nuxt/security/advisories/GHSA-534h-c3cw-v3h9</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/1f9f4767a8725104da9bee872bb8d35246f25ae5">https://github.com/nuxt/nuxt/commit/1f9f4767a8725104da9bee872bb8d35246f25ae5</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/c293bf9503ccb3bc9559bff4a1f592f99063c9ea">https://github.com/nuxt/nuxt/commit/c293bf9503ccb3bc9559bff4a1f592f99063c9ea</a></li>
<li><a href="https://github.com/advisories/GHSA-534h-c3cw-v3h9">https://github.com/advisories/GHSA-534h-c3cw-v3h9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-534h-c3cw-v3h9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:49:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nuxt] Nuxt dev server vite-node IPC socket is world-connectable on Linux]]></title>
        <id>https://github.com/advisories/GHSA-534h-c3cw-v3h9</id>
        <link href="https://github.com/advisories/GHSA-534h-c3cw-v3h9"/>
        <updated>2026-06-16T13:49:11.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When running <code>nuxt dev</code> on Linux (Node.js 20+, outside Docker / StackBlitz), Nuxt's internal vite-node IPC server binds to a Linux abstract-namespace Unix socket (<code>\0nuxt-vite-node-&lt;pid&gt;-&lt;ts&gt;.sock</code>). Abstract sockets have no filesystem inode and therefore no permission bits: any local UID on the host that can read <code>/proc/net/unix</code> can enumerate the socket and connect to it.</p>
<p>The IPC server does not perform any peer-credential or shared-secret check before dispatching requests. The <code>module</code> request type passes its <code>moduleId</code> field straight into Vite's SSR <code>fetchModule()</code>, which is not gated by Vite's HTTP-layer <code>server.fs.allow</code> deny-list. A co-resident unprivileged local user can therefore request paths like <code>/home/&lt;dev&gt;/project/.env?raw</code> or <code>~/.ssh/id_rsa?raw</code> and read the developer's secrets through the dev server's SSR plugin pipeline. The <code>resolve</code> request type additionally enables filesystem probing.</p>
<p>This affects developers running <code>nuxt dev</code> on shared multi-tenant Linux hosts (lab machines, shared bastions, CI runners shared between jobs without per-job container isolation). It does not affect:</p>
<ul>
<li>Production builds (<code>nuxt build</code> / <code>nuxt start</code>). The IPC server only runs in development.</li>
<li>macOS or Windows developers.</li>
<li>Docker / StackBlitz environments, which already fall back to a filesystem socket.</li>
<li>Single-user laptops or per-job containerised CI.</li>
</ul>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>nuxt@4.4.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/1f9f4767a8725104da9bee872bb8d35246f25ae5"><code>1f9f4767</code></a>) and backported to <code>nuxt@3.21.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/c293bf9503ccb3bc9559bff4a1f592f99063c9ea"><code>c293bf95</code></a>).</p>
<p>The fix removes the abstract-namespace branch entirely. The IPC server now always binds to a filesystem Unix socket under the OS temp directory and explicitly <code>chmod 0600</code>s it after <code>listen()</code>, restricting connections to the owning UID. If the chmod fails for any reason, the server closes rather than serve requests on an unrestricted channel.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately on an affected host:</p>
<ul>
<li>Run <code>nuxt dev</code> inside a container or VM with no other tenants. Docker already triggers the filesystem-socket fallback in vulnerable versions and that fallback is unaffected.</li>
<li>Bind the dev process to a single-user namespace (<code>unshare -U</code>, rootless containers).</li>
<li>Restrict <code>/proc/net/unix</code> visibility via <code>hidepid=2</code> mount options where applicable, though this is partial mitigation only.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>Affected file: <code>packages/vite/src/plugins/vite-node.ts</code></li>
<li>CWE-276: Incorrect Default Permissions</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Anthropic / Claude as part of Anthropic's coordinated vulnerability disclosure programme, reference ANT-2026-MSNKZFAT. Thanks to the Anthropic security team for the report and the detailed reproduction.</p>
<p>Independently reported by <a href="https://github.com/alcls01111">@alcls01111</a> via GitHub's coordinated disclosure flow (<code>GHSA-5gvc-46gq-948j</code>), closed as a duplicate of this advisory.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-534h-c3cw-v3h9">https://github.com/nuxt/nuxt/security/advisories/GHSA-534h-c3cw-v3h9</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/1f9f4767a8725104da9bee872bb8d35246f25ae5">https://github.com/nuxt/nuxt/commit/1f9f4767a8725104da9bee872bb8d35246f25ae5</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/c293bf9503ccb3bc9559bff4a1f592f99063c9ea">https://github.com/nuxt/nuxt/commit/c293bf9503ccb3bc9559bff4a1f592f99063c9ea</a></li>
<li><a href="https://github.com/advisories/GHSA-534h-c3cw-v3h9">https://github.com/advisories/GHSA-534h-c3cw-v3h9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-534h-c3cw-v3h9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-16T13:49:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nuxt] Nuxt: Route-rule middleware bypass via case-sensitivity mismatch between vue-router and the routeRules matcher]]></title>
        <id>https://github.com/advisories/GHSA-mm7m-92g8-7m47</id>
        <link href="https://github.com/advisories/GHSA-mm7m-92g8-7m47"/>
        <updated>2026-06-16T13:48:31.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Nuxt looks up <code>routeRules</code> for the current navigation by calling
<code>getRouteRules({ path: to.path })</code> from the page-router plugin and the
no-pages router plugin. The compiled <code>routeRules</code> matcher (built on
<code>rou3</code>) performs case-sensitive matching, while vue-router is configured
with its default <code>sensitive: false</code> and matches paths case-insensitively.</p>
<p>The two routers therefore disagree on which rules apply to a given
request path: vue-router still matches the page record for
<code>/Admin/dashboard</code>, but the <code>routeRules</code> lookup for the same path
returns no match. Any <code>appMiddleware</code> declared via <code>routeRules</code> is never
added to the middleware set and never runs, on both SSR and client
navigations. The same path skips other path-keyed route rules in the
same way (<code>ssr</code>, <code>redirect</code>, <code>appLayout</code>, and the prerender / payload
hints used client-side).</p>
<p>For applications using <code>routeRules</code> with <code>appMiddleware</code> as an
authorization gate (a documented pattern), an attacker can flip the case
of any static segment in a protected URL (for example <code>/Admin/dashboard</code>
instead of <code>/admin/dashboard</code>) to render the protected page with the
middleware skipped. The server returns the fully server-rendered page
including any <code>useFetch</code> / <code>useAsyncData</code> results captured during SSR.</p>
<p>This is an instance of CWE-178 (Improper Handling of Case Sensitivity)
leading to CWE-863 (Incorrect Authorization) for apps that treat
<code>appMiddleware</code> as an authorization boundary.</p>
<h2 id="mitigating-factors">Mitigating factors</h2>
<ul>
<li>Only affects apps that use <code>routeRules.appMiddleware</code>. The more
idiomatic <code>definePageMeta({ middleware })</code> is bound to the matched
route record and is unaffected.</li>
<li>Nuxt route middleware is documented as an app-layer concern, not a
server-side auth boundary; well-built apps enforce authorization
again at the API / data-fetching layer.</li>
<li>Apps that explicitly set <code>router.options.sensitive = true</code> are not
affected.</li>
</ul>
<h2 id="patches">Patches</h2>
<p>Fixed in <code>nuxt@4.4.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/07e39cd6f26e407b4192b7865bd17bc44536b9bb"><code>07e39cd6</code></a>) and backported to <code>nuxt@3.21.7</code> (commit <a href="https://github.com/nuxt/nuxt/commit/3f3e3fa7b5eec8e495f4f8ce0a54813a8875a11e"><code>3f3e3fa7</code></a>). The fix normalizes the path used for <code>routeRules</code> lookups so it matches vue-router's default case-insensitive semantics.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Until you can upgrade, you can mitigate by either:</p>
<ol>
<li>Setting <code>router.options.sensitive = true</code> so vue-router matches
case-sensitively (this changes route-matching behaviour app-wide).</li>
<li>Moving security-critical middleware off <code>routeRules.appMiddleware</code>
and onto <code>definePageMeta({ middleware: [...] })</code> on the protected
page components, which is bound to the matched record.</li>
<li>Enforcing authorization at the API / data-fetching layer (which you
should be doing in any case).</li>
</ol>
<h2 id="credit">Credit</h2>
<p>Reported by Anthropic / Claude through Anthropic's coordinated
vulnerability disclosure process. Reference: ANT-2026-9FSEBYMC.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/nuxt/security/advisories/GHSA-mm7m-92g8-7m47">https://github.com/nuxt/nuxt/security/advisories/GHSA-mm7m-92g8-7m47</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53721">https://nvd.nist.gov/vuln/detail/CVE-2026-53721</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/07e39cd6f26e407b4192b7865bd17bc44536b9bb">https://github.com/nuxt/nuxt/commit/07e39cd6f26e407b4192b7865bd17bc44536b9bb</a></li>
<li><a href="https://github.com/nuxt/nuxt/commit/3f3e3fa7b5eec8e495f4f8ce0a54813a8875a11e">https://github.com/nuxt/nuxt/commit/3f3e3fa7b5eec8e495f4f8ce0a54813a8875a11e</a></li>
<li><a href="https://github.com/advisories/GHSA-mm7m-92g8-7m47">https://github.com/advisories/GHSA-mm7m-92g8-7m47</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mm7m-92g8-7m47</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-16T13:48:31.000Z</published>
    </entry>
</feed>