<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-06-19T18:31:19.502Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@openzeppelin/wizard-stylus] OpenZeppelin Contracts Wizard: Line terminators in info.securityContact / info.license can inject lines into generated source]]></title>
        <id>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</id>
        <link href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc"/>
        <updated>2026-06-19T17:45:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Contracts Wizard generators printed <code>info.securityContact</code> and <code>info.license</code> verbatim into a single-line comment of the generated Solidity, Cairo, Stellar/Soroban, and Stylus source without rejecting line terminators. A newline (<code>\n</code> or <code>\r\n</code>) in either field ends the comment, so the text after it is emitted as source rather than remaining inside the comment — allowing arbitrary declarations to be injected into the generated contract.</p>
<h2 id="impact">Impact</h2>
<p>This only matters when these fields are filled from input other than the user who will use the generated contract. Normal self-service use does not meet that condition:</p>
<ul>
<li><strong>Web app, AI assistant, and CLI:</strong> the user supplies these fields and uses their own output, so a line break only affects their own contract. (These fields are not URL-derived, so shared links cannot set them.)</li>
<li><strong>Self-hosted API:</strong> same — the end user supplies the options and consumes the result.</li>
</ul>
<p>The case that matters is an integration that fills these fields from untrusted input — for example, an MCP agent whose tool arguments are derived from content it processed. There, a newline in the value can add lines to output that otherwise looks like normal Wizard source. Impact is integrity-only; there is no execution on any Wizard service.</p>
<h2 id="patches">Patches</h2>
<p>Fixed by rejecting line terminators in <code>setInfo</code> — the single code path all surfaces use — so the value can no longer break out of the comment. Upgrade to the patched versions. <code>@openzeppelin/wizard-confidential</code> and <code>@openzeppelin/wizard-uniswap-hooks</code> reuse this <code>setInfo</code> through their <code>@openzeppelin/wizard</code> dependency and receive the fix once that dependency is updated to a patched version.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc</a></li>
<li><a href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/advisories/GHSA-9wxg-vf3r-56hc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T17:45:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@openzeppelin/wizard-stellar] OpenZeppelin Contracts Wizard: Line terminators in info.securityContact / info.license can inject lines into generated source]]></title>
        <id>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</id>
        <link href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc"/>
        <updated>2026-06-19T17:45:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Contracts Wizard generators printed <code>info.securityContact</code> and <code>info.license</code> verbatim into a single-line comment of the generated Solidity, Cairo, Stellar/Soroban, and Stylus source without rejecting line terminators. A newline (<code>\n</code> or <code>\r\n</code>) in either field ends the comment, so the text after it is emitted as source rather than remaining inside the comment — allowing arbitrary declarations to be injected into the generated contract.</p>
<h2 id="impact">Impact</h2>
<p>This only matters when these fields are filled from input other than the user who will use the generated contract. Normal self-service use does not meet that condition:</p>
<ul>
<li><strong>Web app, AI assistant, and CLI:</strong> the user supplies these fields and uses their own output, so a line break only affects their own contract. (These fields are not URL-derived, so shared links cannot set them.)</li>
<li><strong>Self-hosted API:</strong> same — the end user supplies the options and consumes the result.</li>
</ul>
<p>The case that matters is an integration that fills these fields from untrusted input — for example, an MCP agent whose tool arguments are derived from content it processed. There, a newline in the value can add lines to output that otherwise looks like normal Wizard source. Impact is integrity-only; there is no execution on any Wizard service.</p>
<h2 id="patches">Patches</h2>
<p>Fixed by rejecting line terminators in <code>setInfo</code> — the single code path all surfaces use — so the value can no longer break out of the comment. Upgrade to the patched versions. <code>@openzeppelin/wizard-confidential</code> and <code>@openzeppelin/wizard-uniswap-hooks</code> reuse this <code>setInfo</code> through their <code>@openzeppelin/wizard</code> dependency and receive the fix once that dependency is updated to a patched version.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc</a></li>
<li><a href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/advisories/GHSA-9wxg-vf3r-56hc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T17:45:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@openzeppelin/wizard-cairo] OpenZeppelin Contracts Wizard: Line terminators in info.securityContact / info.license can inject lines into generated source]]></title>
        <id>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</id>
        <link href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc"/>
        <updated>2026-06-19T17:45:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Contracts Wizard generators printed <code>info.securityContact</code> and <code>info.license</code> verbatim into a single-line comment of the generated Solidity, Cairo, Stellar/Soroban, and Stylus source without rejecting line terminators. A newline (<code>\n</code> or <code>\r\n</code>) in either field ends the comment, so the text after it is emitted as source rather than remaining inside the comment — allowing arbitrary declarations to be injected into the generated contract.</p>
<h2 id="impact">Impact</h2>
<p>This only matters when these fields are filled from input other than the user who will use the generated contract. Normal self-service use does not meet that condition:</p>
<ul>
<li><strong>Web app, AI assistant, and CLI:</strong> the user supplies these fields and uses their own output, so a line break only affects their own contract. (These fields are not URL-derived, so shared links cannot set them.)</li>
<li><strong>Self-hosted API:</strong> same — the end user supplies the options and consumes the result.</li>
</ul>
<p>The case that matters is an integration that fills these fields from untrusted input — for example, an MCP agent whose tool arguments are derived from content it processed. There, a newline in the value can add lines to output that otherwise looks like normal Wizard source. Impact is integrity-only; there is no execution on any Wizard service.</p>
<h2 id="patches">Patches</h2>
<p>Fixed by rejecting line terminators in <code>setInfo</code> — the single code path all surfaces use — so the value can no longer break out of the comment. Upgrade to the patched versions. <code>@openzeppelin/wizard-confidential</code> and <code>@openzeppelin/wizard-uniswap-hooks</code> reuse this <code>setInfo</code> through their <code>@openzeppelin/wizard</code> dependency and receive the fix once that dependency is updated to a patched version.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc</a></li>
<li><a href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/advisories/GHSA-9wxg-vf3r-56hc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T17:45:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@openzeppelin/wizard] OpenZeppelin Contracts Wizard: Line terminators in info.securityContact / info.license can inject lines into generated source]]></title>
        <id>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</id>
        <link href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc"/>
        <updated>2026-06-19T17:45:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The Contracts Wizard generators printed <code>info.securityContact</code> and <code>info.license</code> verbatim into a single-line comment of the generated Solidity, Cairo, Stellar/Soroban, and Stylus source without rejecting line terminators. A newline (<code>\n</code> or <code>\r\n</code>) in either field ends the comment, so the text after it is emitted as source rather than remaining inside the comment — allowing arbitrary declarations to be injected into the generated contract.</p>
<h2 id="impact">Impact</h2>
<p>This only matters when these fields are filled from input other than the user who will use the generated contract. Normal self-service use does not meet that condition:</p>
<ul>
<li><strong>Web app, AI assistant, and CLI:</strong> the user supplies these fields and uses their own output, so a line break only affects their own contract. (These fields are not URL-derived, so shared links cannot set them.)</li>
<li><strong>Self-hosted API:</strong> same — the end user supplies the options and consumes the result.</li>
</ul>
<p>The case that matters is an integration that fills these fields from untrusted input — for example, an MCP agent whose tool arguments are derived from content it processed. There, a newline in the value can add lines to output that otherwise looks like normal Wizard source. Impact is integrity-only; there is no execution on any Wizard service.</p>
<h2 id="patches">Patches</h2>
<p>Fixed by rejecting line terminators in <code>setInfo</code> — the single code path all surfaces use — so the value can no longer break out of the comment. Upgrade to the patched versions. <code>@openzeppelin/wizard-confidential</code> and <code>@openzeppelin/wizard-uniswap-hooks</code> reuse this <code>setInfo</code> through their <code>@openzeppelin/wizard</code> dependency and receive the fix once that dependency is updated to a patched version.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/OpenZeppelin/contracts-wizard/security/advisories/GHSA-9wxg-vf3r-56hc</a></li>
<li><a href="https://github.com/advisories/GHSA-9wxg-vf3r-56hc">https://github.com/advisories/GHSA-9wxg-vf3r-56hc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9wxg-vf3r-56hc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T17:45:15.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[agentic-flow] Agentic-Flow: OS Command Injection in agentic-flow MCP server tools via unsanitized tool-parameter interpolation into execSync]]></title>
        <id>https://github.com/advisories/GHSA-vcv2-r9jh-99m5</id>
        <link href="https://github.com/advisories/GHSA-vcv2-r9jh-99m5"/>
        <updated>2026-06-19T15:13:00.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>agentic-flow</code> versions <code>&lt;= 2.0.13</code> MCP server tools interpolated attacker-influenceable tool parameters (e.g. <code>agent</code>, <code>task</code>, <code>name</code>, <code>language</code>, <code>agentdb</code> arguments) directly into shell command strings passed to <code>execSync()</code>. A malicious value reaching any of the affected MCP tools could break out of the surrounding double-quoted argument and execute arbitrary OS commands with the privileges of the user running the MCP server.</p>
<p>This was a partial-fix gap: prior commit <code>6a06854</code> (#158) fixed CWE-78 elsewhere in the project but missed the MCP server files entirely.</p>
<h2 id="impact">Impact</h2>
<p>Any MCP tool argument that the AI agent treats as data but the implementation interpolates into a shell command string becomes a command-injection vector. In MCP deployments where untrusted content (web pages, files, third-party tool output) is processed by the agent, this is reachable without direct attacker access to the host. The HTTP/SSE transports (<code>http-sse.ts</code>, <code>http-streaming-updated.ts</code>) expose the same sinks without authentication or Origin/Host validation, which may raise the effective severity in any deployment that binds them to a reachable network interface.</p>
<h2 id="affected-components">Affected components</h2>
<ul>
<li><code>src/mcp/standalone-stdio.ts</code> — <code>agentic_flow_agent</code>, <code>agentic_flow_create_agent</code>, <code>agentic_flow_list_all_agents</code>, <code>agentic_flow_agent_info</code>, <code>agentic_flow_check_conflicts</code>, <code>agentic_flow_optimize_model</code>, <code>agentic_flow_list_agents</code>, <code>agent_booster_edit_file</code>, <code>agent_booster_batch_edit</code>, <code>agent_booster_parse_markdown</code>, <code>agentdb_stats</code>, <code>agentdb_pattern_store</code>, <code>agentdb_pattern_search</code>, <code>agentdb_pattern_stats</code></li>
<li><code>src/mcp/fastmcp/servers/claude-flow-sdk.ts</code></li>
<li><code>src/mcp/fastmcp/servers/stdio-full.ts</code></li>
<li><code>src/mcp/fastmcp/servers/http-streaming-updated.ts</code></li>
<li><code>src/mcp/fastmcp/servers/http-sse.ts</code></li>
<li><code>src/mcp/fastmcp/servers/poc-stdio.ts</code></li>
<li><code>src/mcp/fastmcp/tools/agent/{execute,list,parallel}.ts</code></li>
<li><code>src/mcp/fastmcp/tools/swarm/orchestrate.ts</code></li>
<li><code>src/mcp/fastmcp/tools/hooks/pretrain.ts</code> (depth path only)</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-ts">// Pre-fix (standalone-stdio.ts, agentic_flow_agent)
let cmd = `npx --yes agentic-flow --agent "${agent}" --task "${task}"`;
const result = execSync(cmd, { encoding: 'utf-8', ... });
</code></pre>
<p>Invoking the MCP tool with:</p>
<pre><code class="language-json">{
  "agent": "coder",
  "task": "x\"; touch /tmp/INJECTED; id &gt; /tmp/rce.txt; echo \""
}
</code></pre>
<p>produces, after interpolation:</p>
<pre><code>npx --yes agentic-flow --agent "coder" --task "x"; touch /tmp/INJECTED; id &gt; /tmp/rce.txt; echo ""
</code></pre>
<p>When <code>execSync</code> hands that to <code>/bin/sh -c</code>, the shell parses three commands: the truncated <code>npx</code>, then <code>touch /tmp/INJECTED</code>, then <code>id &gt; /tmp/rce.txt; echo ""</code>. The marker file <code>/tmp/INJECTED</code> is created and the user's <code>id</code> output is written to <code>/tmp/rce.txt</code>.</p>
<h2 id="patches">Patches</h2>
<p>Fixed in <a href="https://www.npmjs.com/package/agentic-flow/v/2.0.14"><code>agentic-flow@2.0.14</code></a> — every affected call site rewritten to use <code>execFileSync(file, argv, { shell: false })</code> so attacker-controlled argv elements are passed straight to <code>execve(2)</code> without shell parsing.</p>
<p>Fix PR: ruvnet/agentic-flow#170 (merged at <code>0c2ec96</code>)</p>
<p>A regression test (<code>tests/security/cwe-78-mcp-execsync.test.ts</code>) was added that statically scans every <code>src/mcp/**/*.ts</code> file and fails the build if any new <code>execSync()</code> call is reintroduced outside of a documented exemption, plus a behavioural smoke check that the canonical PoC payload remains inert when passed as an argv element to <code>execFileSync</code>.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Upgrade to <code>agentic-flow &gt;= 2.0.14</code>. There is no in-product configuration that mitigates this without upgrading.</p>
<h2 id="downstream-pin">Downstream pin</h2>
<p>The <code>ruflo</code> / <code>claude-flow</code> / <code>@claude-flow/cli</code> packages bumped from <code>3.12.3</code> → <code>3.12.4</code> to pull the patched <code>agentic-flow</code>:</p>
<ul>
<li><code>ruflo@3.12.4</code></li>
<li><code>claude-flow@3.12.4</code></li>
<li><code>@claude-flow/cli@3.12.4</code></li>
</ul>
<p>End users running any of <code>npx ruflo@latest</code>, <code>npx claude-flow@latest</code>, or <code>npx @claude-flow/cli@latest</code> are pinned to the fixed version.</p>
<h2 id="credit">Credit</h2>
<p>Reported by <strong>hackchang</strong> via a well-scoped red-team report package (<code>npm_agentic-flow_report_package_20260618_163017.zip</code>) that included a sink inventory, a minimized PoC payload, and a clear explanation of why this was a partial-fix gap rather than intended behaviour. The sink inventory directly drove the single-grep pass that closed every reachable call site; the PoC payload became the behavioural smoke test that proves the canonical attack stays inert as an argv element.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ruvnet/agentic-flow/security/advisories/GHSA-vcv2-r9jh-99m5">https://github.com/ruvnet/agentic-flow/security/advisories/GHSA-vcv2-r9jh-99m5</a></li>
<li><a href="https://github.com/ruvnet/agentic-flow/issues/169">https://github.com/ruvnet/agentic-flow/issues/169</a></li>
<li><a href="https://github.com/ruvnet/ruflo/issues/2414">https://github.com/ruvnet/ruflo/issues/2414</a></li>
<li><a href="https://github.com/ruvnet/agentic-flow/pull/170">https://github.com/ruvnet/agentic-flow/pull/170</a></li>
<li><a href="https://github.com/ruvnet/ruflo/pull/2415">https://github.com/ruvnet/ruflo/pull/2415</a></li>
<li><a href="https://github.com/ruvnet/ruflo/releases/tag/v3.12.4">https://github.com/ruvnet/ruflo/releases/tag/v3.12.4</a></li>
<li><a href="https://www.npmjs.com/package/agentic-flow/v/2.0.14">https://www.npmjs.com/package/agentic-flow/v/2.0.14</a></li>
<li><a href="https://github.com/advisories/GHSA-vcv2-r9jh-99m5">https://github.com/advisories/GHSA-vcv2-r9jh-99m5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vcv2-r9jh-99m5</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T15:12:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] parse-server: Denial of service via exponential-time processing of deeply nested query operators]]></title>
        <id>https://github.com/advisories/GHSA-cgxm-vr2f-6fj8</id>
        <link href="https://github.com/advisories/GHSA-cgxm-vr2f-6fj8"/>
        <updated>2026-06-19T14:50:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Parse Server is vulnerable to denial of service. A remote attacker can send a single, small query (~1 KB) containing deeply nested query condition operators. Parse Server processes the nested structure with exponential time complexity, which blocks the Node.js event loop and makes the server unresponsive to all clients for the duration of processing. A single request can occupy the event loop for many seconds, and the request is repeatable. The issue affects the REST API and LiveQuery query handling and is reachable in the default configuration. Exploitation requires only the public application identifier; no user authentication is needed.</p>
<h3 id="patches">Patches</h3>
<p>The internal query-traversal helper that previously re-walked nested arrays — causing exponential-time processing of nested <code>$or</code>/<code>$and</code>/<code>$nor</code> operators — was corrected to traverse queries in linear time. Additionally, the optional <code>requestComplexity.queryDepth</code> limit was generalized so that nested logical operators are counted even when wrapped inside field-level operators (e.g. <code>$elemMatch</code>, <code>$not</code>) or plain field names, closing a bypass of the limit on both the REST API and LiveQuery.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no complete configuration-only workaround on affected versions. Setting <code>requestComplexity.queryDepth</code> to a small positive integer reduces exposure but does not fully prevent the issue, because the limit can be bypassed by nesting the operators inside a field-level operator. Upgrading is strongly recommended.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-cgxm-vr2f-6fj8">https://github.com/parse-community/parse-server/security/advisories/GHSA-cgxm-vr2f-6fj8</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10511">https://github.com/parse-community/parse-server/pull/10511</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10512">https://github.com/parse-community/parse-server/pull/10512</a></li>
<li><a href="https://github.com/advisories/GHSA-cgxm-vr2f-6fj8">https://github.com/advisories/GHSA-cgxm-vr2f-6fj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cgxm-vr2f-6fj8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:50:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[parse-server] parse-server: Denial of service via exponential-time processing of deeply nested query operators]]></title>
        <id>https://github.com/advisories/GHSA-cgxm-vr2f-6fj8</id>
        <link href="https://github.com/advisories/GHSA-cgxm-vr2f-6fj8"/>
        <updated>2026-06-19T14:50:16.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Parse Server is vulnerable to denial of service. A remote attacker can send a single, small query (~1 KB) containing deeply nested query condition operators. Parse Server processes the nested structure with exponential time complexity, which blocks the Node.js event loop and makes the server unresponsive to all clients for the duration of processing. A single request can occupy the event loop for many seconds, and the request is repeatable. The issue affects the REST API and LiveQuery query handling and is reachable in the default configuration. Exploitation requires only the public application identifier; no user authentication is needed.</p>
<h3 id="patches">Patches</h3>
<p>The internal query-traversal helper that previously re-walked nested arrays — causing exponential-time processing of nested <code>$or</code>/<code>$and</code>/<code>$nor</code> operators — was corrected to traverse queries in linear time. Additionally, the optional <code>requestComplexity.queryDepth</code> limit was generalized so that nested logical operators are counted even when wrapped inside field-level operators (e.g. <code>$elemMatch</code>, <code>$not</code>) or plain field names, closing a bypass of the limit on both the REST API and LiveQuery.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no complete configuration-only workaround on affected versions. Setting <code>requestComplexity.queryDepth</code> to a small positive integer reduces exposure but does not fully prevent the issue, because the limit can be bypassed by nesting the operators inside a field-level operator. Upgrading is strongly recommended.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/parse-community/parse-server/security/advisories/GHSA-cgxm-vr2f-6fj8">https://github.com/parse-community/parse-server/security/advisories/GHSA-cgxm-vr2f-6fj8</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10511">https://github.com/parse-community/parse-server/pull/10511</a></li>
<li><a href="https://github.com/parse-community/parse-server/pull/10512">https://github.com/parse-community/parse-server/pull/10512</a></li>
<li><a href="https://github.com/advisories/GHSA-cgxm-vr2f-6fj8">https://github.com/advisories/GHSA-cgxm-vr2f-6fj8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cgxm-vr2f-6fj8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:50:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to Set-Cookie SameSite attribute downgrade via permissive substring matching]]></title>
        <id>https://github.com/advisories/GHSA-g8m3-5g58-fq7m</id>
        <link href="https://github.com/advisories/GHSA-g8m3-5g58-fq7m"/>
        <updated>2026-06-19T14:34:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When undici parses a <code>Set-Cookie</code> header, it accepts any <code>SameSite</code> attribute value that contains <code>Strict</code>, <code>Lax</code>, or <code>None</code> as a substring, rather than the case-insensitive exact match specified by RFC 6265. Non-spec values are silently mapped to one of the three standard tokens:</p>
<ul>
<li><code>SameSite=NoneOfYourBusiness</code> is parsed as <code>None</code>, the most permissive setting.</li>
<li><code>SameSite=StrictLax</code> is parsed as <code>Lax</code>, a downgrade from <code>Strict</code>.</li>
</ul>
<p>Affected applications are those that consume <code>Set-Cookie</code> headers from server responses (for example via undici's <code>fetch</code> or proxy code paths) and then forward or rely on the parsed <code>sameSite</code> attribute. A malicious or non-compliant server can coerce the consumer's view of a cookie's SameSite policy to a weaker value, silently degrading the SameSite enforcement the cookie is supposed to provide.</p>
<p>This was introduced in undici 5.15.0 when the cookies feature was added.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>After parsing a <code>Set-Cookie</code> header, validate that the resulting <code>sameSite</code> attribute is one of <code>'Strict'</code>, <code>'Lax'</code>, or <code>'None'</code> (exact, case-insensitive) before forwarding or relying on it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-g8m3-5g58-fq7m">https://github.com/nodejs/undici/security/advisories/GHSA-g8m3-5g58-fq7m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11525">https://nvd.nist.gov/vuln/detail/CVE-2026-11525</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g8m3-5g58-fq7m">https://github.com/advisories/GHSA-g8m3-5g58-fq7m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g8m3-5g58-fq7m</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T14:34:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to Set-Cookie SameSite attribute downgrade via permissive substring matching]]></title>
        <id>https://github.com/advisories/GHSA-g8m3-5g58-fq7m</id>
        <link href="https://github.com/advisories/GHSA-g8m3-5g58-fq7m"/>
        <updated>2026-06-19T14:34:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When undici parses a <code>Set-Cookie</code> header, it accepts any <code>SameSite</code> attribute value that contains <code>Strict</code>, <code>Lax</code>, or <code>None</code> as a substring, rather than the case-insensitive exact match specified by RFC 6265. Non-spec values are silently mapped to one of the three standard tokens:</p>
<ul>
<li><code>SameSite=NoneOfYourBusiness</code> is parsed as <code>None</code>, the most permissive setting.</li>
<li><code>SameSite=StrictLax</code> is parsed as <code>Lax</code>, a downgrade from <code>Strict</code>.</li>
</ul>
<p>Affected applications are those that consume <code>Set-Cookie</code> headers from server responses (for example via undici's <code>fetch</code> or proxy code paths) and then forward or rely on the parsed <code>sameSite</code> attribute. A malicious or non-compliant server can coerce the consumer's view of a cookie's SameSite policy to a weaker value, silently degrading the SameSite enforcement the cookie is supposed to provide.</p>
<p>This was introduced in undici 5.15.0 when the cookies feature was added.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>After parsing a <code>Set-Cookie</code> header, validate that the resulting <code>sameSite</code> attribute is one of <code>'Strict'</code>, <code>'Lax'</code>, or <code>'None'</code> (exact, case-insensitive) before forwarding or relying on it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-g8m3-5g58-fq7m">https://github.com/nodejs/undici/security/advisories/GHSA-g8m3-5g58-fq7m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11525">https://nvd.nist.gov/vuln/detail/CVE-2026-11525</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g8m3-5g58-fq7m">https://github.com/advisories/GHSA-g8m3-5g58-fq7m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g8m3-5g58-fq7m</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T14:34:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to Set-Cookie SameSite attribute downgrade via permissive substring matching]]></title>
        <id>https://github.com/advisories/GHSA-g8m3-5g58-fq7m</id>
        <link href="https://github.com/advisories/GHSA-g8m3-5g58-fq7m"/>
        <updated>2026-06-19T14:34:46.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When undici parses a <code>Set-Cookie</code> header, it accepts any <code>SameSite</code> attribute value that contains <code>Strict</code>, <code>Lax</code>, or <code>None</code> as a substring, rather than the case-insensitive exact match specified by RFC 6265. Non-spec values are silently mapped to one of the three standard tokens:</p>
<ul>
<li><code>SameSite=NoneOfYourBusiness</code> is parsed as <code>None</code>, the most permissive setting.</li>
<li><code>SameSite=StrictLax</code> is parsed as <code>Lax</code>, a downgrade from <code>Strict</code>.</li>
</ul>
<p>Affected applications are those that consume <code>Set-Cookie</code> headers from server responses (for example via undici's <code>fetch</code> or proxy code paths) and then forward or rely on the parsed <code>sameSite</code> attribute. A malicious or non-compliant server can coerce the consumer's view of a cookie's SameSite policy to a weaker value, silently degrading the SameSite enforcement the cookie is supposed to provide.</p>
<p>This was introduced in undici 5.15.0 when the cookies feature was added.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>After parsing a <code>Set-Cookie</code> header, validate that the resulting <code>sameSite</code> attribute is one of <code>'Strict'</code>, <code>'Lax'</code>, or <code>'None'</code> (exact, case-insensitive) before forwarding or relying on it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-g8m3-5g58-fq7m">https://github.com/nodejs/undici/security/advisories/GHSA-g8m3-5g58-fq7m</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11525">https://nvd.nist.gov/vuln/detail/CVE-2026-11525</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-g8m3-5g58-fq7m">https://github.com/advisories/GHSA-g8m3-5g58-fq7m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g8m3-5g58-fq7m</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T14:34:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to HTTP header injection via Set-Cookie percent-decoding]]></title>
        <id>https://github.com/advisories/GHSA-p88m-4jfj-68fv</id>
        <link href="https://github.com/advisories/GHSA-p88m-4jfj-68fv"/>
        <updated>2026-06-19T14:23:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>undici's cookie parser in <code>parseSetCookie</code> percent-decodes cookie values via <code>qsUnescape</code>, turning encoded sequences like <code>%0D%0A</code>, <code>%00</code>, <code>%3B</code>, and <code>%3D</code> into their literal byte equivalents. RFC 6265 §5.4 does not specify any decoding and browsers do not decode either.</p>
<p>Applications that parse a <code>Set-Cookie</code> header and then forward the parsed value into a response header (proxies, middleware, SSR frameworks) become vulnerable to HTTP response header injection: an attacker-controlled upstream can inject arbitrary <code>Set-Cookie</code>, <code>Location</code>, or <code>Cache-Control</code> headers into the application's downstream response, enabling session fixation, open redirect, or cache poisoning.</p>
<p>Affected applications are those that use undici's cookie parsing (<code>parseSetCookie</code>, <code>parseCookie</code>, <code>getSetCookies</code>) and forward the parsed cookie value into a response header.</p>
<p>This was introduced in undici 7.0.0 via <a href="https://github.com/nodejs/undici/pull/3789">#3789</a>.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrade is not immediately possible, do not forward values returned by <code>parseSetCookie</code>/<code>parseCookie</code>/<code>getSetCookies</code> directly into response headers; sanitize the value first to strip or reject CR, LF, NUL, <code>;</code>, and <code>=</code> bytes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-p88m-4jfj-68fv">https://github.com/nodejs/undici/security/advisories/GHSA-p88m-4jfj-68fv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-9679">https://nvd.nist.gov/vuln/detail/CVE-2026-9679</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-p88m-4jfj-68fv">https://github.com/advisories/GHSA-p88m-4jfj-68fv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p88m-4jfj-68fv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:21:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to HTTP header injection via Set-Cookie percent-decoding]]></title>
        <id>https://github.com/advisories/GHSA-p88m-4jfj-68fv</id>
        <link href="https://github.com/advisories/GHSA-p88m-4jfj-68fv"/>
        <updated>2026-06-19T14:23:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>undici's cookie parser in <code>parseSetCookie</code> percent-decodes cookie values via <code>qsUnescape</code>, turning encoded sequences like <code>%0D%0A</code>, <code>%00</code>, <code>%3B</code>, and <code>%3D</code> into their literal byte equivalents. RFC 6265 §5.4 does not specify any decoding and browsers do not decode either.</p>
<p>Applications that parse a <code>Set-Cookie</code> header and then forward the parsed value into a response header (proxies, middleware, SSR frameworks) become vulnerable to HTTP response header injection: an attacker-controlled upstream can inject arbitrary <code>Set-Cookie</code>, <code>Location</code>, or <code>Cache-Control</code> headers into the application's downstream response, enabling session fixation, open redirect, or cache poisoning.</p>
<p>Affected applications are those that use undici's cookie parsing (<code>parseSetCookie</code>, <code>parseCookie</code>, <code>getSetCookies</code>) and forward the parsed cookie value into a response header.</p>
<p>This was introduced in undici 7.0.0 via <a href="https://github.com/nodejs/undici/pull/3789">#3789</a>.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrade is not immediately possible, do not forward values returned by <code>parseSetCookie</code>/<code>parseCookie</code>/<code>getSetCookies</code> directly into response headers; sanitize the value first to strip or reject CR, LF, NUL, <code>;</code>, and <code>=</code> bytes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-p88m-4jfj-68fv">https://github.com/nodejs/undici/security/advisories/GHSA-p88m-4jfj-68fv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-9679">https://nvd.nist.gov/vuln/detail/CVE-2026-9679</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-p88m-4jfj-68fv">https://github.com/advisories/GHSA-p88m-4jfj-68fv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p88m-4jfj-68fv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:21:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to HTTP header injection via Set-Cookie percent-decoding]]></title>
        <id>https://github.com/advisories/GHSA-p88m-4jfj-68fv</id>
        <link href="https://github.com/advisories/GHSA-p88m-4jfj-68fv"/>
        <updated>2026-06-19T14:23:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>undici's cookie parser in <code>parseSetCookie</code> percent-decodes cookie values via <code>qsUnescape</code>, turning encoded sequences like <code>%0D%0A</code>, <code>%00</code>, <code>%3B</code>, and <code>%3D</code> into their literal byte equivalents. RFC 6265 §5.4 does not specify any decoding and browsers do not decode either.</p>
<p>Applications that parse a <code>Set-Cookie</code> header and then forward the parsed value into a response header (proxies, middleware, SSR frameworks) become vulnerable to HTTP response header injection: an attacker-controlled upstream can inject arbitrary <code>Set-Cookie</code>, <code>Location</code>, or <code>Cache-Control</code> headers into the application's downstream response, enabling session fixation, open redirect, or cache poisoning.</p>
<p>Affected applications are those that use undici's cookie parsing (<code>parseSetCookie</code>, <code>parseCookie</code>, <code>getSetCookies</code>) and forward the parsed cookie value into a response header.</p>
<p>This was introduced in undici 7.0.0 via <a href="https://github.com/nodejs/undici/pull/3789">#3789</a>.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>If upgrade is not immediately possible, do not forward values returned by <code>parseSetCookie</code>/<code>parseCookie</code>/<code>getSetCookies</code> directly into response headers; sanitize the value first to strip or reject CR, LF, NUL, <code>;</code>, and <code>=</code> bytes.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-p88m-4jfj-68fv">https://github.com/nodejs/undici/security/advisories/GHSA-p88m-4jfj-68fv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-9679">https://nvd.nist.gov/vuln/detail/CVE-2026-9679</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-p88m-4jfj-68fv">https://github.com/advisories/GHSA-p88m-4jfj-68fv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p88m-4jfj-68fv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-06-19T14:21:30.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici WebSocket client vulnerable to denial of service via fragment count bypass]]></title>
        <id>https://github.com/advisories/GHSA-vxpw-j846-p89q</id>
        <link href="https://github.com/advisories/GHSA-vxpw-j846-p89q"/>
        <updated>2026-06-19T14:23:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The undici WebSocket client enforces <code>maxPayloadSize</code> on the cumulative byte count of fragments in a message but does not enforce a limit on the number of fragments. A malicious WebSocket server can stream many small or empty continuation frames that each pass per-frame and cumulative-size validation, collectively causing unbounded memory growth in the client process. The result is memory exhaustion and a denial of service.</p>
<p>Affected applications are those using the undici WebSocket client (<code>new WebSocket(...)</code>) or the <code>WebSocketStream</code> API that can be induced to connect to an attacker-controlled or compromised WebSocket endpoint.</p>
<p>All releases starting at undici 6.17.0 are affected.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workaround is available. The fix must be applied through an upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-vxpw-j846-p89q">https://github.com/nodejs/undici/security/advisories/GHSA-vxpw-j846-p89q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12151">https://nvd.nist.gov/vuln/detail/CVE-2026-12151</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-vxpw-j846-p89q">https://github.com/advisories/GHSA-vxpw-j846-p89q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vxpw-j846-p89q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:22:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici WebSocket client vulnerable to denial of service via fragment count bypass]]></title>
        <id>https://github.com/advisories/GHSA-vxpw-j846-p89q</id>
        <link href="https://github.com/advisories/GHSA-vxpw-j846-p89q"/>
        <updated>2026-06-19T14:23:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The undici WebSocket client enforces <code>maxPayloadSize</code> on the cumulative byte count of fragments in a message but does not enforce a limit on the number of fragments. A malicious WebSocket server can stream many small or empty continuation frames that each pass per-frame and cumulative-size validation, collectively causing unbounded memory growth in the client process. The result is memory exhaustion and a denial of service.</p>
<p>Affected applications are those using the undici WebSocket client (<code>new WebSocket(...)</code>) or the <code>WebSocketStream</code> API that can be induced to connect to an attacker-controlled or compromised WebSocket endpoint.</p>
<p>All releases starting at undici 6.17.0 are affected.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workaround is available. The fix must be applied through an upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-vxpw-j846-p89q">https://github.com/nodejs/undici/security/advisories/GHSA-vxpw-j846-p89q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12151">https://nvd.nist.gov/vuln/detail/CVE-2026-12151</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-vxpw-j846-p89q">https://github.com/advisories/GHSA-vxpw-j846-p89q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vxpw-j846-p89q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:22:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici WebSocket client vulnerable to denial of service via fragment count bypass]]></title>
        <id>https://github.com/advisories/GHSA-vxpw-j846-p89q</id>
        <link href="https://github.com/advisories/GHSA-vxpw-j846-p89q"/>
        <updated>2026-06-19T14:23:29.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>The undici WebSocket client enforces <code>maxPayloadSize</code> on the cumulative byte count of fragments in a message but does not enforce a limit on the number of fragments. A malicious WebSocket server can stream many small or empty continuation frames that each pass per-frame and cumulative-size validation, collectively causing unbounded memory growth in the client process. The result is memory exhaustion and a denial of service.</p>
<p>Affected applications are those using the undici WebSocket client (<code>new WebSocket(...)</code>) or the <code>WebSocketStream</code> API that can be induced to connect to an attacker-controlled or compromised WebSocket endpoint.</p>
<p>All releases starting at undici 6.17.0 are affected.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>No workaround is available. The fix must be applied through an upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-vxpw-j846-p89q">https://github.com/nodejs/undici/security/advisories/GHSA-vxpw-j846-p89q</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-12151">https://nvd.nist.gov/vuln/detail/CVE-2026-12151</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-vxpw-j846-p89q">https://github.com/advisories/GHSA-vxpw-j846-p89q</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vxpw-j846-p89q</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:22:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to cross-origin request routing via SOCKS5 proxy pool reuse]]></title>
        <id>https://github.com/advisories/GHSA-hm92-r4w5-c3mj</id>
        <link href="https://github.com/advisories/GHSA-hm92-r4w5-c3mj"/>
        <updated>2026-06-19T14:20:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When using <code>Socks5ProxyAgent</code>, undici reuses a single connection pool across different origins without verifying that the pool's origin matches the requested origin. All requests are dispatched through the pool connected to the first origin, regardless of the intended destination.</p>
<p>This causes cross-origin request routing: credentials and request data intended for origin B are sent to origin A, responses from the wrong origin are trusted, and HTTPS requests may be silently downgraded to HTTP.</p>
<p>Impacted users are applications that use <code>Socks5ProxyAgent</code> (directly or via <code>setGlobalDispatcher</code>) and make requests to more than one origin.</p>
<p>This was introduced in undici 7.23.0 via <a href="https://github.com/nodejs/undici/pull/4385">#4385</a> and affects all versions through 8.1.0.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v7.28.0 or v8.2.0</p>
<h2 id="workarounds">Workarounds</h2>
<p>Use a separate <code>Socks5ProxyAgent</code> instance per origin, or avoid using <code>Socks5ProxyAgent</code> with multiple origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-hm92-r4w5-c3mj">https://github.com/nodejs/undici/security/advisories/GHSA-hm92-r4w5-c3mj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6734">https://nvd.nist.gov/vuln/detail/CVE-2026-6734</a></li>
<li><a href="https://github.com/nodejs/undici/pull/5041">https://github.com/nodejs/undici/pull/5041</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-hm92-r4w5-c3mj">https://github.com/advisories/GHSA-hm92-r4w5-c3mj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hm92-r4w5-c3mj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:20:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to cross-origin request routing via SOCKS5 proxy pool reuse]]></title>
        <id>https://github.com/advisories/GHSA-hm92-r4w5-c3mj</id>
        <link href="https://github.com/advisories/GHSA-hm92-r4w5-c3mj"/>
        <updated>2026-06-19T14:20:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>When using <code>Socks5ProxyAgent</code>, undici reuses a single connection pool across different origins without verifying that the pool's origin matches the requested origin. All requests are dispatched through the pool connected to the first origin, regardless of the intended destination.</p>
<p>This causes cross-origin request routing: credentials and request data intended for origin B are sent to origin A, responses from the wrong origin are trusted, and HTTPS requests may be silently downgraded to HTTP.</p>
<p>Impacted users are applications that use <code>Socks5ProxyAgent</code> (directly or via <code>setGlobalDispatcher</code>) and make requests to more than one origin.</p>
<p>This was introduced in undici 7.23.0 via <a href="https://github.com/nodejs/undici/pull/4385">#4385</a> and affects all versions through 8.1.0.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v7.28.0 or v8.2.0</p>
<h2 id="workarounds">Workarounds</h2>
<p>Use a separate <code>Socks5ProxyAgent</code> instance per origin, or avoid using <code>Socks5ProxyAgent</code> with multiple origins.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-hm92-r4w5-c3mj">https://github.com/nodejs/undici/security/advisories/GHSA-hm92-r4w5-c3mj</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6734">https://nvd.nist.gov/vuln/detail/CVE-2026-6734</a></li>
<li><a href="https://github.com/nodejs/undici/pull/5041">https://github.com/nodejs/undici/pull/5041</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-hm92-r4w5-c3mj">https://github.com/advisories/GHSA-hm92-r4w5-c3mj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hm92-r4w5-c3mj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-19T14:20:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to HTTP response queue poisoning via keep-alive socket reuse]]></title>
        <id>https://github.com/advisories/GHSA-35p6-xmwp-9g52</id>
        <link href="https://github.com/advisories/GHSA-35p6-xmwp-9g52"/>
        <updated>2026-06-19T14:19:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Undici's HTTP/1.1 client is vulnerable to response queue poisoning on reused keep-alive sockets. An attacker-controlled upstream server can inject an unsolicited HTTP/1.1 response onto an idle socket after a request completes. When the client dispatches the next request on that socket, it associates the injected response with the new request, causing responses to be delivered to the wrong requests.</p>
<p>This requires an attacker-controlled or compromised upstream HTTP/1.1 server and keep-alive connection reuse.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Disable keep-alive connection reuse by setting <code>keepAliveTimeout: 0</code> on the Client or Pool.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-35p6-xmwp-9g52">https://github.com/nodejs/undici/security/advisories/GHSA-35p6-xmwp-9g52</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6733">https://nvd.nist.gov/vuln/detail/CVE-2026-6733</a></li>
<li><a href="https://hackerone.com/reports/3582376">https://hackerone.com/reports/3582376</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-35p6-xmwp-9g52">https://github.com/advisories/GHSA-35p6-xmwp-9g52</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-35p6-xmwp-9g52</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T14:19:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[undici] undici vulnerable to HTTP response queue poisoning via keep-alive socket reuse]]></title>
        <id>https://github.com/advisories/GHSA-35p6-xmwp-9g52</id>
        <link href="https://github.com/advisories/GHSA-35p6-xmwp-9g52"/>
        <updated>2026-06-19T14:19:15.000Z</updated>
        <content type="html"><![CDATA[<h2 id="impact">Impact</h2>
<p>Undici's HTTP/1.1 client is vulnerable to response queue poisoning on reused keep-alive sockets. An attacker-controlled upstream server can inject an unsolicited HTTP/1.1 response onto an idle socket after a request completes. When the client dispatches the next request on that socket, it associates the injected response with the new request, causing responses to be delivered to the wrong requests.</p>
<p>This requires an attacker-controlled or compromised upstream HTTP/1.1 server and keep-alive connection reuse.</p>
<h2 id="patches">Patches</h2>
<p>Upgrade to undici v6.27.0, v7.28.0 or v8.5.0.</p>
<h2 id="workarounds">Workarounds</h2>
<p>Disable keep-alive connection reuse by setting <code>keepAliveTimeout: 0</code> on the Client or Pool.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodejs/undici/security/advisories/GHSA-35p6-xmwp-9g52">https://github.com/nodejs/undici/security/advisories/GHSA-35p6-xmwp-9g52</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-6733">https://nvd.nist.gov/vuln/detail/CVE-2026-6733</a></li>
<li><a href="https://hackerone.com/reports/3582376">https://hackerone.com/reports/3582376</a></li>
<li><a href="https://cna.openjsf.org/security-advisories.html">https://cna.openjsf.org/security-advisories.html</a></li>
<li><a href="https://github.com/advisories/GHSA-35p6-xmwp-9g52">https://github.com/advisories/GHSA-35p6-xmwp-9g52</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-35p6-xmwp-9g52</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-19T14:19:13.000Z</published>
    </entry>
</feed>