<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://azu.github.io/github-advisory-database-rss/rust.rss</id>
    <title>Security Advisory for Rust crates</title>
    <updated>2026-04-16T17:26:15.198Z</updated>
    <generator>github-advisory-database-rss</generator>
    <link rel="alternate" href="https://github.com/advisories?query=type%3Areviewed+ecosystem%3Arust"/>
    <subtitle>Security Advisory for Rust crates on GitHub</subtitle>
    <rights>github-advisory-database-rss</rights>
    <category term="CRITICAL"/>
    <category term="HIGH"/>
    <category term="MODERATE"/>
    <category term="LOW"/>
    <entry>
        <title type="html"><![CDATA[[thin-vec] thin-vec: Use-After-Free and Double Free in IntoIter::drop When Element Drop Panics]]></title>
        <id>https://github.com/advisories/GHSA-xphw-cqx3-667j</id>
        <link href="https://github.com/advisories/GHSA-xphw-cqx3-667j"/>
        <updated>2026-04-15T19:24:55.000Z</updated>
        <content type="html"><![CDATA[<h3 id="summary">Summary</h3>
<p>A <strong>Double Free / Use-After-Free (UAF)</strong> vulnerability has been identified in the <code>IntoIter::drop</code> and <code>ThinVec::clear</code> implementations of the <code>thin_vec</code> crate.
Both vulnerabilities share the same root cause and can trigger memory corruption using only safe Rust code — no <code>unsafe</code> blocks required.
Undefined Behavior has been confirmed via <strong>Miri</strong> and <strong>AddressSanitizer (ASAN)</strong>.</p>
<hr>
<h3 id="details">Details</h3>
<p>Both vulnerabilities share the same root cause. When a <strong>panic occurs</strong> during sequential element deallocation, the subsequent length cleanup code (<code>set_len(0)</code>) is never executed. During stack unwinding, the container is dropped again, causing already-freed memory to be re-freed (Double Free / UAF).</p>
<h4 id="vulnerability-1--intoiterdrop">Vulnerability 1 — <code>IntoIter::drop</code></h4>
<p><strong>Location:</strong> <code>thin-vec/src/lib.rs</code> L.2308~2314</p>
<p><code>IntoIter::drop</code> transfers ownership of the internal buffer via <code>mem::replace</code>, then sequentially frees elements via <code>ptr::drop_in_place</code>.
If a panic occurs during element deallocation, <code>set_len_non_singleton(0)</code> is never reached. During unwinding, <code>vec</code> is dropped again, re-freeing already-freed elements.
The standard library's <code>std::vec::IntoIter</code> prevents this with a <strong>DropGuard pattern</strong>, but thin-vec lacks this defense.</p>
<pre><code class="language-rust">// Problematic structure (conceptual representation)
impl&lt;T&gt; Drop for IntoIter&lt;T&gt; {
    fn drop(&amp;mut self) {
        let mut vec = mem::replace(&amp;mut self.vec, ThinVec::new());
        unsafe {
            ptr::drop_in_place(vec.remaining_slice_mut()); // ← panic may occur here
            vec.set_len_non_singleton(0);                  // ← unreachable on panic
        }
        // During unwinding, vec is dropped again → Double Free
    }
}
</code></pre>
<h4 id="vulnerability-2--thinvecclear">Vulnerability 2 — <code>ThinVec::clear</code></h4>
<p><code>clear()</code> calls <code>ptr::drop_in_place(&amp;mut self[..])</code> followed by <code>self.set_len(0)</code> to reset the length.
If a panic occurs during element deallocation, <code>set_len(0)</code> is never executed. When the <code>ThinVec</code> itself is subsequently dropped, already-freed elements are freed again.</p>
<pre><code class="language-rust">// Problematic structure (conceptual representation)
pub fn clear(&amp;mut self) {
    unsafe {
        ptr::drop_in_place(&amp;mut self[..]); // ← panic may occur here
        self.set_len(0);                   // ← unreachable on panic
    }
    // ThinVec drop later → Double Free
}
</code></pre>
<h4 id="recommended-fix">Recommended Fix</h4>
<p>Both vulnerabilities can be resolved with the same pattern:</p>
<ul>
<li><strong>DropGuard pattern:</strong> Insert an RAII guard before <code>drop_in_place</code> to guarantee <code>set_len(0)</code> is called regardless of panic</li>
<li><strong>Pre-zeroing approach:</strong> Set the length to 0 before calling <code>drop_in_place</code></li>
</ul>
<hr>
<h3 id="poc">PoC</h3>
<p><strong>Requirements:</strong> Rust nightly toolchain, <code>thin-vec = "0.2.14"</code></p>
<pre><code class="language-bash"># Miri
cargo +nightly miri run

# ASAN
RUSTFLAGS="-Z sanitizer=address" cargo +nightly run --release
</code></pre>
<h4 id="poc-1-intoiterdrop">PoC-1: <code>IntoIter::drop</code></h4>
<pre><code class="language-rust">use thin_vec::ThinVec;

struct PanicBomb(String);

impl Drop for PanicBomb {
    fn drop(&amp;mut self) {
        if self.0 == "panic" {
            panic!("panic!");
        }
        println!("Dropping: {}", self.0);
    }
}

fn main() {
    let mut v = ThinVec::new();
    v.push(PanicBomb(String::from("normal1")));
    v.push(PanicBomb(String::from("panic")));  // trigger element
    v.push(PanicBomb(String::from("normal2")));

    let mut iter = v.into_iter();
    iter.next();
    // When iter is dropped: panic occurs at "panic" element
    // → During unwinding, Double Drop is triggered on "normal1" (already freed)
}
</code></pre>
<p><strong>Miri output:</strong></p>
<pre><code>error: Undefined Behavior: pointer not dereferenceable:
       alloc227 has been freed, so this pointer is dangling

stack backtrace:
   3: &lt;PanicBomb as Drop&gt;::drop           ← Double Drop entry
   6: &lt;ThinVec&lt;T&gt; as Drop&gt;::drop::drop_non_singleton
   9: &lt;IntoIter&lt;T&gt; as Drop&gt;::drop::drop_non_singleton  ← lib.rs:2310 (root cause)
</code></pre>
<p><strong>ASAN output:</strong></p>
<pre><code>==66150==ERROR: AddressSanitizer: heap-use-after-free on address 0x7afa685e0010
READ of size 7 at 0x7afa685e0010
    #0 memcpy
    #4 drop_in_place::&lt;PanicBomb&gt;        ← Double Drop entry point
    #5 &lt;ThinVec as Drop&gt;::drop::drop_non_singleton
    #6 &lt;IntoIter as Drop&gt;::drop::drop_non_singleton
</code></pre>
<h4 id="poc-2-thinvecclear">PoC-2: <code>ThinVec::clear</code></h4>
<pre><code class="language-rust">use thin_vec::ThinVec;
use std::panic;

struct Poison(Box&lt;usize&gt;, &amp;'static str);

impl Drop for Poison {
    fn drop(&amp;mut self) {
        if self.1 == "panic" {
            panic!("panic!");
        }
        println!("Dropping: {}", self.0);
    }
}

fn main() {
    let mut v = ThinVec::new();
    v.push(Poison(Box::new(1), "normal1")); // index 0
    v.push(Poison(Box::new(2), "panic"));   // index 1 → panic triggered here
    v.push(Poison(Box::new(3), "normal2")); // index 2

    let _ = panic::catch_unwind(panic::AssertUnwindSafe(|| {
        v.clear();
        // panic occurs at "panic" element during clear()
        // → set_len(0) is never called
        // → already-freed elements are re-freed when v goes out of scope
    }));
}
</code></pre>
<hr>
<h3 id="impact">Impact</h3>
<p><strong>Affected code:</strong> All code satisfying the following conditions simultaneously:</p>
<ol>
<li><code>ThinVec</code> stores heap-owning types (<code>String</code>, <code>Vec</code>, <code>Box</code>, etc.)</li>
<li>(Vulnerability 1) An iterator is created via <code>into_iter()</code> and dropped before being fully consumed, or
(Vulnerability 2) <code>clear()</code> is called while a remaining element's <code>Drop</code> implementation can panic</li>
<li>The <code>Drop</code> implementation of a remaining element triggers a panic</li>
</ol>
<p>Additionally, when combined with <code>Box&lt;dyn Trait&gt;</code> types, an exploit primitive enabling Arbitrary Code Execution (ACE) via heap spray and vtable hijacking has been confirmed. If the freed fat pointer slot (16 bytes) at the point of Double Drop is reclaimed by an attacker-controlled fake vtable, subsequent Drop calls can be redirected to attacker-controlled code.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/mozilla/thin-vec/security/advisories/GHSA-xphw-cqx3-667j">https://github.com/mozilla/thin-vec/security/advisories/GHSA-xphw-cqx3-667j</a></li>
<li><a href="https://github.com/advisories/GHSA-xphw-cqx3-667j">https://github.com/advisories/GHSA-xphw-cqx3-667j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xphw-cqx3-667j</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-15T19:24:54.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rand] Rand is unsound with a custom logger using rand::rng()]]></title>
        <id>https://github.com/advisories/GHSA-cq8v-f236-94qc</id>
        <link href="https://github.com/advisories/GHSA-cq8v-f236-94qc"/>
        <updated>2026-04-14T01:03:37.000Z</updated>
        <content type="html"><![CDATA[<p>It has been reported (by @lopopolo) that the <code>rand</code> library is <a href="https://rust-lang.github.io/unsafe-code-guidelines/glossary.html#soundness-of-code--of-a-library">unsound</a> (i.e. that safe code using the public API can cause Undefined Behaviour) when all the following conditions are met:</p>
<ul>
<li>The <code>log</code> and <code>thread_rng</code> features are enabled</li>
<li>A <a href="https://docs.rs/log/latest/log/#implementing-a-logger">custom logger</a> is defined</li>
<li>The custom logger accesses <code>rand::rng()</code> (previously <code>rand::thread_rng()</code>) and calls any <code>TryRng</code> (previously <code>RngCore</code>) methods on <code>ThreadRng</code></li>
<li>The <code>ThreadRng</code> (attempts to) reseed while called from the custom logger (this happens every 64 kB of generated data)</li>
<li>Trace-level logging is enabled or warn-level logging is enabled and the random source (the <code>getrandom</code> crate) is unable to provide a new seed</li>
</ul>
<p><code>TryRng</code> (previously <code>RngCore</code>) methods for <code>ThreadRng</code> use <code>unsafe</code> code to cast <code>*mut BlockRng&lt;ReseedingCore&gt;</code> to <code>&amp;mut BlockRng&lt;ReseedingCore&gt;</code>. When all the above conditions are met this results in an aliased mutable reference, violating the Stacked Borrows rules. Miri is able to detect this violation in sample code. Since construction of <a href="https://doc.rust-lang.org/stable/nomicon/references.html">aliased mutable references is Undefined Behaviour</a>, the behaviour of optimized builds is hard to predict.</p>
<p>Affected versions of <code>rand</code> are <code>&gt;= 0.7, &lt; 0.9.3</code> and <code>0.10.0</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-random/rand/pull/1763">https://github.com/rust-random/rand/pull/1763</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0097.html">https://rustsec.org/advisories/RUSTSEC-2026-0097.html</a></li>
<li><a href="https://github.com/advisories/GHSA-cq8v-f236-94qc">https://github.com/advisories/GHSA-cq8v-f236-94qc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq8v-f236-94qc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-14T01:03:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[rand] Rand is unsound with a custom logger using rand::rng()]]></title>
        <id>https://github.com/advisories/GHSA-cq8v-f236-94qc</id>
        <link href="https://github.com/advisories/GHSA-cq8v-f236-94qc"/>
        <updated>2026-04-14T01:03:37.000Z</updated>
        <content type="html"><![CDATA[<p>It has been reported (by @lopopolo) that the <code>rand</code> library is <a href="https://rust-lang.github.io/unsafe-code-guidelines/glossary.html#soundness-of-code--of-a-library">unsound</a> (i.e. that safe code using the public API can cause Undefined Behaviour) when all the following conditions are met:</p>
<ul>
<li>The <code>log</code> and <code>thread_rng</code> features are enabled</li>
<li>A <a href="https://docs.rs/log/latest/log/#implementing-a-logger">custom logger</a> is defined</li>
<li>The custom logger accesses <code>rand::rng()</code> (previously <code>rand::thread_rng()</code>) and calls any <code>TryRng</code> (previously <code>RngCore</code>) methods on <code>ThreadRng</code></li>
<li>The <code>ThreadRng</code> (attempts to) reseed while called from the custom logger (this happens every 64 kB of generated data)</li>
<li>Trace-level logging is enabled or warn-level logging is enabled and the random source (the <code>getrandom</code> crate) is unable to provide a new seed</li>
</ul>
<p><code>TryRng</code> (previously <code>RngCore</code>) methods for <code>ThreadRng</code> use <code>unsafe</code> code to cast <code>*mut BlockRng&lt;ReseedingCore&gt;</code> to <code>&amp;mut BlockRng&lt;ReseedingCore&gt;</code>. When all the above conditions are met this results in an aliased mutable reference, violating the Stacked Borrows rules. Miri is able to detect this violation in sample code. Since construction of <a href="https://doc.rust-lang.org/stable/nomicon/references.html">aliased mutable references is Undefined Behaviour</a>, the behaviour of optimized builds is hard to predict.</p>
<p>Affected versions of <code>rand</code> are <code>&gt;= 0.7, &lt; 0.9.3</code> and <code>0.10.0</code>.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/rust-random/rand/pull/1763">https://github.com/rust-random/rand/pull/1763</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0097.html">https://rustsec.org/advisories/RUSTSEC-2026-0097.html</a></li>
<li><a href="https://github.com/advisories/GHSA-cq8v-f236-94qc">https://github.com/advisories/GHSA-cq8v-f236-94qc</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-cq8v-f236-94qc</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-14T01:03:36.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sp1_prover] SP1 V6 Recursion Circuit Row-Count Binding Gap]]></title>
        <id>https://github.com/advisories/GHSA-63x8-x938-vx33</id>
        <link href="https://github.com/advisories/GHSA-63x8-x938-vx33"/>
        <updated>2026-04-14T00:05:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A soundness vulnerability in the SP1 V6 recursive shard verifier allows a malicious prover to construct a recursive proof from a shard proof that the native verifier would reject.</p>
<ul>
<li><strong>Affected versions:</strong> <code>&gt;= 6.0.0, &lt;= 6.0.2</code></li>
<li><strong>Not affected:</strong> SP1 V5 (all versions)</li>
<li><strong>Severity:</strong> High</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="background">Background</h3>
<p>The recursive shard verifier circuit verifies shard proofs inside a recursive proof. Each shard proof includes a jagged PCS opening, which binds trace-shape metadata into a modified commitment and uses that same shape to evaluate the committed polynomials. These two operations must agree on the committed table heights.</p>
<h3 id="the-bug">The Bug</h3>
<p>In the V6 recursion circuit's jagged verifier, the two checks above are served by separate witnesses: a vector of row counts hashed into the modified commitment (commitment side), and a separate witness of prefix sums derived from row and column counts that drives the jagged polynomial evaluator (evaluation side). The prefix sums are observed within the shard verifier.</p>
<p>The consistency check between these two witnesses was missing in the recursion sub-circuit describing the jagged PCS verifier. A malicious prover can therefore supply one trace shape for commitment binding and a different shape for polynomial evaluation.</p>
<h3 id="potential-impact">Potential Impact</h3>
<p>The vulnerability applies to both main trace and preprocessed trace metadata. Because preprocessed traces encode circuit structure (selectors, fixed columns, permutation layout), the potential impact extends beyond data forgery to misrepresentation of the circuit itself.</p>
<p>While a demonstration of a full exploit proving arbitrary statements has not been created — since modifying one table's layout incidentally constrains changes to related tables — this barrier is not by design and should not be relied upon. This is considered a soundness violation that is unacceptable regardless of current exploitability.</p>
<h3 id="why-the-native-verifier-is-not-affected">Why the Native Verifier Is Not Affected</h3>
<p>The native shard verifier uses a single jagged PCS verifier object where row counts and evaluation layout are derived from the same data, so the split-witness divergence cannot occur. The recursion circuit's shard-level checks (prefix-sum and total-area assertions) only constrain the evaluation-side parameters, not the commitment-side row counts, so they do not catch the gap.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix adds a post-evaluation consistency constraint in the recursive jagged verifier. After the jagged evaluation returns the prefix-sum values derived from the evaluation layout, the circuit reconstructs expected prefix sums from the commitment-side row counts (repeating each row count by its corresponding column count and accumulating). It then asserts element-wise equality between the reconstructed and returned prefix sums, and verifies that the final accumulated area matches the total area from the evaluation parameters.</p>
<p>This forces both witnesses to describe the same trace geometry. Any divergence is now a constraint failure.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was identified through the SP1 bug bounty program on Code4rena.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33">https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33</a></li>
<li><a href="https://github.com/succinctlabs/sp1/releases/tag/v6.1.0">https://github.com/succinctlabs/sp1/releases/tag/v6.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-63x8-x938-vx33">https://github.com/advisories/GHSA-63x8-x938-vx33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63x8-x938-vx33</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T00:05:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sp1_recursion_circuit] SP1 V6 Recursion Circuit Row-Count Binding Gap]]></title>
        <id>https://github.com/advisories/GHSA-63x8-x938-vx33</id>
        <link href="https://github.com/advisories/GHSA-63x8-x938-vx33"/>
        <updated>2026-04-14T00:05:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A soundness vulnerability in the SP1 V6 recursive shard verifier allows a malicious prover to construct a recursive proof from a shard proof that the native verifier would reject.</p>
<ul>
<li><strong>Affected versions:</strong> <code>&gt;= 6.0.0, &lt;= 6.0.2</code></li>
<li><strong>Not affected:</strong> SP1 V5 (all versions)</li>
<li><strong>Severity:</strong> High</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="background">Background</h3>
<p>The recursive shard verifier circuit verifies shard proofs inside a recursive proof. Each shard proof includes a jagged PCS opening, which binds trace-shape metadata into a modified commitment and uses that same shape to evaluate the committed polynomials. These two operations must agree on the committed table heights.</p>
<h3 id="the-bug">The Bug</h3>
<p>In the V6 recursion circuit's jagged verifier, the two checks above are served by separate witnesses: a vector of row counts hashed into the modified commitment (commitment side), and a separate witness of prefix sums derived from row and column counts that drives the jagged polynomial evaluator (evaluation side). The prefix sums are observed within the shard verifier.</p>
<p>The consistency check between these two witnesses was missing in the recursion sub-circuit describing the jagged PCS verifier. A malicious prover can therefore supply one trace shape for commitment binding and a different shape for polynomial evaluation.</p>
<h3 id="potential-impact">Potential Impact</h3>
<p>The vulnerability applies to both main trace and preprocessed trace metadata. Because preprocessed traces encode circuit structure (selectors, fixed columns, permutation layout), the potential impact extends beyond data forgery to misrepresentation of the circuit itself.</p>
<p>While a demonstration of a full exploit proving arbitrary statements has not been created — since modifying one table's layout incidentally constrains changes to related tables — this barrier is not by design and should not be relied upon. This is considered a soundness violation that is unacceptable regardless of current exploitability.</p>
<h3 id="why-the-native-verifier-is-not-affected">Why the Native Verifier Is Not Affected</h3>
<p>The native shard verifier uses a single jagged PCS verifier object where row counts and evaluation layout are derived from the same data, so the split-witness divergence cannot occur. The recursion circuit's shard-level checks (prefix-sum and total-area assertions) only constrain the evaluation-side parameters, not the commitment-side row counts, so they do not catch the gap.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix adds a post-evaluation consistency constraint in the recursive jagged verifier. After the jagged evaluation returns the prefix-sum values derived from the evaluation layout, the circuit reconstructs expected prefix sums from the commitment-side row counts (repeating each row count by its corresponding column count and accumulating). It then asserts element-wise equality between the reconstructed and returned prefix sums, and verifies that the final accumulated area matches the total area from the evaluation parameters.</p>
<p>This forces both witnesses to describe the same trace geometry. Any divergence is now a constraint failure.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was identified through the SP1 bug bounty program on Code4rena.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33">https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33</a></li>
<li><a href="https://github.com/succinctlabs/sp1/releases/tag/v6.1.0">https://github.com/succinctlabs/sp1/releases/tag/v6.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-63x8-x938-vx33">https://github.com/advisories/GHSA-63x8-x938-vx33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63x8-x938-vx33</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T00:05:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[sp1_sdk] SP1 V6 Recursion Circuit Row-Count Binding Gap]]></title>
        <id>https://github.com/advisories/GHSA-63x8-x938-vx33</id>
        <link href="https://github.com/advisories/GHSA-63x8-x938-vx33"/>
        <updated>2026-04-14T00:05:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="summary">Summary</h2>
<p>A soundness vulnerability in the SP1 V6 recursive shard verifier allows a malicious prover to construct a recursive proof from a shard proof that the native verifier would reject.</p>
<ul>
<li><strong>Affected versions:</strong> <code>&gt;= 6.0.0, &lt;= 6.0.2</code></li>
<li><strong>Not affected:</strong> SP1 V5 (all versions)</li>
<li><strong>Severity:</strong> High</li>
</ul>
<h2 id="details">Details</h2>
<h3 id="background">Background</h3>
<p>The recursive shard verifier circuit verifies shard proofs inside a recursive proof. Each shard proof includes a jagged PCS opening, which binds trace-shape metadata into a modified commitment and uses that same shape to evaluate the committed polynomials. These two operations must agree on the committed table heights.</p>
<h3 id="the-bug">The Bug</h3>
<p>In the V6 recursion circuit's jagged verifier, the two checks above are served by separate witnesses: a vector of row counts hashed into the modified commitment (commitment side), and a separate witness of prefix sums derived from row and column counts that drives the jagged polynomial evaluator (evaluation side). The prefix sums are observed within the shard verifier.</p>
<p>The consistency check between these two witnesses was missing in the recursion sub-circuit describing the jagged PCS verifier. A malicious prover can therefore supply one trace shape for commitment binding and a different shape for polynomial evaluation.</p>
<h3 id="potential-impact">Potential Impact</h3>
<p>The vulnerability applies to both main trace and preprocessed trace metadata. Because preprocessed traces encode circuit structure (selectors, fixed columns, permutation layout), the potential impact extends beyond data forgery to misrepresentation of the circuit itself.</p>
<p>While a demonstration of a full exploit proving arbitrary statements has not been created — since modifying one table's layout incidentally constrains changes to related tables — this barrier is not by design and should not be relied upon. This is considered a soundness violation that is unacceptable regardless of current exploitability.</p>
<h3 id="why-the-native-verifier-is-not-affected">Why the Native Verifier Is Not Affected</h3>
<p>The native shard verifier uses a single jagged PCS verifier object where row counts and evaluation layout are derived from the same data, so the split-witness divergence cannot occur. The recursion circuit's shard-level checks (prefix-sum and total-area assertions) only constrain the evaluation-side parameters, not the commitment-side row counts, so they do not catch the gap.</p>
<h2 id="mitigation">Mitigation</h2>
<p>The fix adds a post-evaluation consistency constraint in the recursive jagged verifier. After the jagged evaluation returns the prefix-sum values derived from the evaluation layout, the circuit reconstructs expected prefix sums from the commitment-side row counts (repeating each row count by its corresponding column count and accumulating). It then asserts element-wise equality between the reconstructed and returned prefix sums, and verifies that the final accumulated area matches the total area from the evaluation parameters.</p>
<p>This forces both witnesses to describe the same trace geometry. Any divergence is now a constraint failure.</p>
<h2 id="credit">Credit</h2>
<p>This vulnerability was identified through the SP1 bug bounty program on Code4rena.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33">https://github.com/succinctlabs/sp1/security/advisories/GHSA-63x8-x938-vx33</a></li>
<li><a href="https://github.com/succinctlabs/sp1/releases/tag/v6.1.0">https://github.com/succinctlabs/sp1/releases/tag/v6.1.0</a></li>
<li><a href="https://github.com/advisories/GHSA-63x8-x938-vx33">https://github.com/advisories/GHSA-63x8-x938-vx33</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-63x8-x938-vx33</uri>
        </author>
        <category label="severity" term="HIGH"/>
        <published>2026-04-14T00:05:19.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-consensus] nimiq-consensus panics via RequestMacroChain micro-block locator]]></title>
        <id>https://github.com/advisories/GHSA-48m6-486p-9j8p</id>
        <link href="https://github.com/advisories/GHSA-48m6-486p-9j8p"/>
        <updated>2026-04-15T20:56:43.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p> An unauthenticated p2p peer can cause the <code>RequestMacroChain</code> message handler task to panic by sending a <code>RequestMacroChain</code> message where the first locator hash that is on the victim’s main chain is a micro block hash (not a macro block hash).</p>
<p>In <code>RequestMacroChain::handle</code>, the handler selects the locator based only on "is on main chain", then calls <code>get_macro_blocks()</code> and panics via <code>.unwrap()</code> when the selected hash is not a macro block (<code>BlockchainError::BlockIsNotMacro</code>).</p>
<h3 id="patches">Patches</h3>
<p>The patch for this vulnerability](<a href="https://github.com/nimiq/core-rs-albatross/pull/3660">https://github.com/nimiq/core-rs-albatross/pull/3660</a>) is formally released as part of <a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">v1.3.0</a>.</p>
<h3 id="workarounds">Workarounds</h3>
<p>No known workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-48m6-486p-9j8p">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-48m6-486p-9j8p</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/pull/3660">https://github.com/nimiq/core-rs-albatross/pull/3660</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/commit/ae6c1e92342e72f80fd12accbe66ee80dd6802ac">https://github.com/nimiq/core-rs-albatross/commit/ae6c1e92342e72f80fd12accbe66ee80dd6802ac</a></li>
<li><a href="https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0">https://github.com/nimiq/core-rs-albatross/releases/tag/v1.3.0</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34069">https://nvd.nist.gov/vuln/detail/CVE-2026-34069</a></li>
<li><a href="https://github.com/advisories/GHSA-48m6-486p-9j8p">https://github.com/advisories/GHSA-48m6-486p-9j8p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-48m6-486p-9j8p</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-13T16:36:00.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[nimiq-blockchain] nimiq-blockchain is missing a wall-clock upper bound on block timestamps]]></title>
        <id>https://github.com/advisories/GHSA-49xc-52mp-cc9j</id>
        <link href="https://github.com/advisories/GHSA-49xc-52mp-cc9j"/>
        <updated>2026-04-14T10:39:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Block timestamp validation enforces that <code>timestamp &gt;= parent.timestamp</code> for non-skip blocks and <code>timestamp == parent.timestamp + MIN_PRODUCER_TIMEOUT</code> for skip blocks, but there is no visible upper bound check against the wall clock. A malicious block-producing validator can set block timestamps arbitrarily far in the future. This directly affects reward calculations via <code>Policy::supply_at()</code> and <code>batch_delay()</code> in <code>blockchain/src/reward.rs</code>, inflating the monetary supply beyond the intended emission schedule.</p>
<h3 id="patches">Patches</h3>
<p>TBD</p>
<h3 id="workarounds">Workarounds</h3>
<p>No know workarounds.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-49xc-52mp-cc9j">https://github.com/nimiq/core-rs-albatross/security/advisories/GHSA-49xc-52mp-cc9j</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-40093">https://nvd.nist.gov/vuln/detail/CVE-2026-40093</a></li>
<li><a href="https://github.com/advisories/GHSA-49xc-52mp-cc9j">https://github.com/advisories/GHSA-49xc-52mp-cc9j</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-49xc-52mp-cc9j</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-10T19:55:04.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has improperly masked return value from `table.grow` with Winch compiler backend]]></title>
        <id>https://github.com/advisories/GHSA-f984-pcp8-v2p7</id>
        <link href="https://github.com/advisories/GHSA-f984-pcp8-v2p7"/>
        <updated>2026-04-10T15:32:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's Winch compiler backend contains a bug where translating the <code>table.grow</code> operator causes the result to be incorrectly typed. For 32-bit tables this means that the result of the operator, internally in Winch, is tagged as a 64-bit value instead of a 32-bit value. This invalid internal representation of Winch's compiler state compounds into further issues depending on how the value is consumed.</p>
<p>One example can be seen when the result of <code>table.grow</code> is used as the address of a load operation. The load operation is tricked into thinking the address is a 64-bit value, not a 32-bit value, which means that the final address to load from is calculated incorrectly. This can lead to a situation where the bytes before the start of linear memory can be loaded/stored to.</p>
<p>The primary consequence of this bug is that bytes in the host's address space can be stored/read from. This is only applicable to the 16 bytes before linear memory, however, as the only significant return value of <code>table.grow</code> that can be misinterpreted is -1. The bytes before linear memory are, by default, unmapped memory. Wasmtime will detect this fault and abort the process, however, because wasm should not be able to access these bytes.</p>
<p>Overall this this bug in Winch represents a DoS vector by crashing the host process, a correctness issue within Winch, and a possible leak of up to 16-bytes before linear memory. Wasmtime's default compiler is Cranelift, not Winch, and Wasmtime's default settings are to place guard pages before linear memory. This means that Wasmtime's default configuration is not affected by this issue, and when explicitly choosing Winch Wasmtime's otherwise default configuration leads to a DoS. Disabling guard pages before linear memory is required to possibly leak up to 16-bytes of host data.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 43.0.1, 42.0.2, and 36.0.7 have been released with fixes for this issue.</p>
<h3 id="workaround">Workaround</h3>
<p>There are no workarounds within the Winch compiler backend while using the affected versions. Users of Wasmtime are encouraged either to upgrade to patched versions or, if that is not possible, use the Cranelift compiler backend.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-f984-pcp8-v2p7">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-f984-pcp8-v2p7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35186">https://nvd.nist.gov/vuln/detail/CVE-2026-35186</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0094.html">https://rustsec.org/advisories/RUSTSEC-2026-0094.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f984-pcp8-v2p7">https://github.com/advisories/GHSA-f984-pcp8-v2p7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f984-pcp8-v2p7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T15:32:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has improperly masked return value from `table.grow` with Winch compiler backend]]></title>
        <id>https://github.com/advisories/GHSA-f984-pcp8-v2p7</id>
        <link href="https://github.com/advisories/GHSA-f984-pcp8-v2p7"/>
        <updated>2026-04-10T15:32:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's Winch compiler backend contains a bug where translating the <code>table.grow</code> operator causes the result to be incorrectly typed. For 32-bit tables this means that the result of the operator, internally in Winch, is tagged as a 64-bit value instead of a 32-bit value. This invalid internal representation of Winch's compiler state compounds into further issues depending on how the value is consumed.</p>
<p>One example can be seen when the result of <code>table.grow</code> is used as the address of a load operation. The load operation is tricked into thinking the address is a 64-bit value, not a 32-bit value, which means that the final address to load from is calculated incorrectly. This can lead to a situation where the bytes before the start of linear memory can be loaded/stored to.</p>
<p>The primary consequence of this bug is that bytes in the host's address space can be stored/read from. This is only applicable to the 16 bytes before linear memory, however, as the only significant return value of <code>table.grow</code> that can be misinterpreted is -1. The bytes before linear memory are, by default, unmapped memory. Wasmtime will detect this fault and abort the process, however, because wasm should not be able to access these bytes.</p>
<p>Overall this this bug in Winch represents a DoS vector by crashing the host process, a correctness issue within Winch, and a possible leak of up to 16-bytes before linear memory. Wasmtime's default compiler is Cranelift, not Winch, and Wasmtime's default settings are to place guard pages before linear memory. This means that Wasmtime's default configuration is not affected by this issue, and when explicitly choosing Winch Wasmtime's otherwise default configuration leads to a DoS. Disabling guard pages before linear memory is required to possibly leak up to 16-bytes of host data.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 43.0.1, 42.0.2, and 36.0.7 have been released with fixes for this issue.</p>
<h3 id="workaround">Workaround</h3>
<p>There are no workarounds within the Winch compiler backend while using the affected versions. Users of Wasmtime are encouraged either to upgrade to patched versions or, if that is not possible, use the Cranelift compiler backend.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-f984-pcp8-v2p7">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-f984-pcp8-v2p7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35186">https://nvd.nist.gov/vuln/detail/CVE-2026-35186</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0094.html">https://rustsec.org/advisories/RUSTSEC-2026-0094.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f984-pcp8-v2p7">https://github.com/advisories/GHSA-f984-pcp8-v2p7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f984-pcp8-v2p7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T15:32:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has improperly masked return value from `table.grow` with Winch compiler backend]]></title>
        <id>https://github.com/advisories/GHSA-f984-pcp8-v2p7</id>
        <link href="https://github.com/advisories/GHSA-f984-pcp8-v2p7"/>
        <updated>2026-04-10T15:32:54.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's Winch compiler backend contains a bug where translating the <code>table.grow</code> operator causes the result to be incorrectly typed. For 32-bit tables this means that the result of the operator, internally in Winch, is tagged as a 64-bit value instead of a 32-bit value. This invalid internal representation of Winch's compiler state compounds into further issues depending on how the value is consumed.</p>
<p>One example can be seen when the result of <code>table.grow</code> is used as the address of a load operation. The load operation is tricked into thinking the address is a 64-bit value, not a 32-bit value, which means that the final address to load from is calculated incorrectly. This can lead to a situation where the bytes before the start of linear memory can be loaded/stored to.</p>
<p>The primary consequence of this bug is that bytes in the host's address space can be stored/read from. This is only applicable to the 16 bytes before linear memory, however, as the only significant return value of <code>table.grow</code> that can be misinterpreted is -1. The bytes before linear memory are, by default, unmapped memory. Wasmtime will detect this fault and abort the process, however, because wasm should not be able to access these bytes.</p>
<p>Overall this this bug in Winch represents a DoS vector by crashing the host process, a correctness issue within Winch, and a possible leak of up to 16-bytes before linear memory. Wasmtime's default compiler is Cranelift, not Winch, and Wasmtime's default settings are to place guard pages before linear memory. This means that Wasmtime's default configuration is not affected by this issue, and when explicitly choosing Winch Wasmtime's otherwise default configuration leads to a DoS. Disabling guard pages before linear memory is required to possibly leak up to 16-bytes of host data.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 43.0.1, 42.0.2, and 36.0.7 have been released with fixes for this issue.</p>
<h3 id="workaround">Workaround</h3>
<p>There are no workarounds within the Winch compiler backend while using the affected versions. Users of Wasmtime are encouraged either to upgrade to patched versions or, if that is not possible, use the Cranelift compiler backend.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-f984-pcp8-v2p7">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-f984-pcp8-v2p7</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35186">https://nvd.nist.gov/vuln/detail/CVE-2026-35186</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0094.html">https://rustsec.org/advisories/RUSTSEC-2026-0094.html</a></li>
<li><a href="https://github.com/advisories/GHSA-f984-pcp8-v2p7">https://github.com/advisories/GHSA-f984-pcp8-v2p7</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-f984-pcp8-v2p7</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-10T15:32:49.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime with Winch compiler backend on aarch64 may allow a sandbox-escaping memory access]]></title>
        <id>https://github.com/advisories/GHSA-xx5w-cvp6-jv83</id>
        <link href="https://github.com/advisories/GHSA-xx5w-cvp6-jv83"/>
        <updated>2026-04-10T15:31:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime with its Winch (baseline) non-default compiler backend may allow properly constructed guest Wasm to access host memory outside of its linear-memory sandbox.</p>
<p>This vulnerability requires use of the Winch compiler (<code>-Ccompiler=winch</code>). By default, Wasmtime uses its Cranelift backend, not Winch. With Winch, the same incorrect assumption is present in theory on both aarch64 and x86-64. The aarch64 case has an observed-working proof of concept, while the x86-64 case is theoretical and may not be reachable in practice.</p>
<p>This Winch compiler bug can allow the Wasm guest to access memory before or after the linear-memory region, independently of whether pre- or post-guard regions are configured. The accessible range in the initial bug proof-of-concept is up to 32KiB before the start of memory, or ~4GiB after the start of memory, independently of the size of pre- or post-guard regions or the use of explicit or guard-region-based bounds checking. However, the underlying bug assumes a 32-bit memory offset stored in a 64-bit register has its upper bits cleared when it may not, and so closely related variants of the initial proof-of-concept may be able to access truly arbitrary memory in-process. This could result in a host process segmentation fault (DoS), an arbitrary data leak from the host process, or with a write, potentially an arbitrary RCE.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 43.0.1, 42.0.2, and 36.0.7 have been released with fixes for this issue.</p>
<h3 id="workaround">Workaround</h3>
<p>There are no workarounds within the Winch compiler backend while using the affected versions. Users of Wasmtime are encouraged either to upgrade to patched versions or, if that is not possible, use the Cranelift compiler backend.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-xx5w-cvp6-jv83">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-xx5w-cvp6-jv83</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34987">https://nvd.nist.gov/vuln/detail/CVE-2026-34987</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0095.html">https://rustsec.org/advisories/RUSTSEC-2026-0095.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xx5w-cvp6-jv83">https://github.com/advisories/GHSA-xx5w-cvp6-jv83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx5w-cvp6-jv83</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-10T15:31:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime with Winch compiler backend on aarch64 may allow a sandbox-escaping memory access]]></title>
        <id>https://github.com/advisories/GHSA-xx5w-cvp6-jv83</id>
        <link href="https://github.com/advisories/GHSA-xx5w-cvp6-jv83"/>
        <updated>2026-04-10T15:31:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime with its Winch (baseline) non-default compiler backend may allow properly constructed guest Wasm to access host memory outside of its linear-memory sandbox.</p>
<p>This vulnerability requires use of the Winch compiler (<code>-Ccompiler=winch</code>). By default, Wasmtime uses its Cranelift backend, not Winch. With Winch, the same incorrect assumption is present in theory on both aarch64 and x86-64. The aarch64 case has an observed-working proof of concept, while the x86-64 case is theoretical and may not be reachable in practice.</p>
<p>This Winch compiler bug can allow the Wasm guest to access memory before or after the linear-memory region, independently of whether pre- or post-guard regions are configured. The accessible range in the initial bug proof-of-concept is up to 32KiB before the start of memory, or ~4GiB after the start of memory, independently of the size of pre- or post-guard regions or the use of explicit or guard-region-based bounds checking. However, the underlying bug assumes a 32-bit memory offset stored in a 64-bit register has its upper bits cleared when it may not, and so closely related variants of the initial proof-of-concept may be able to access truly arbitrary memory in-process. This could result in a host process segmentation fault (DoS), an arbitrary data leak from the host process, or with a write, potentially an arbitrary RCE.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 43.0.1, 42.0.2, and 36.0.7 have been released with fixes for this issue.</p>
<h3 id="workaround">Workaround</h3>
<p>There are no workarounds within the Winch compiler backend while using the affected versions. Users of Wasmtime are encouraged either to upgrade to patched versions or, if that is not possible, use the Cranelift compiler backend.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-xx5w-cvp6-jv83">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-xx5w-cvp6-jv83</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34987">https://nvd.nist.gov/vuln/detail/CVE-2026-34987</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0095.html">https://rustsec.org/advisories/RUSTSEC-2026-0095.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xx5w-cvp6-jv83">https://github.com/advisories/GHSA-xx5w-cvp6-jv83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx5w-cvp6-jv83</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-10T15:31:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime with Winch compiler backend on aarch64 may allow a sandbox-escaping memory access]]></title>
        <id>https://github.com/advisories/GHSA-xx5w-cvp6-jv83</id>
        <link href="https://github.com/advisories/GHSA-xx5w-cvp6-jv83"/>
        <updated>2026-04-10T15:31:20.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime with its Winch (baseline) non-default compiler backend may allow properly constructed guest Wasm to access host memory outside of its linear-memory sandbox.</p>
<p>This vulnerability requires use of the Winch compiler (<code>-Ccompiler=winch</code>). By default, Wasmtime uses its Cranelift backend, not Winch. With Winch, the same incorrect assumption is present in theory on both aarch64 and x86-64. The aarch64 case has an observed-working proof of concept, while the x86-64 case is theoretical and may not be reachable in practice.</p>
<p>This Winch compiler bug can allow the Wasm guest to access memory before or after the linear-memory region, independently of whether pre- or post-guard regions are configured. The accessible range in the initial bug proof-of-concept is up to 32KiB before the start of memory, or ~4GiB after the start of memory, independently of the size of pre- or post-guard regions or the use of explicit or guard-region-based bounds checking. However, the underlying bug assumes a 32-bit memory offset stored in a 64-bit register has its upper bits cleared when it may not, and so closely related variants of the initial proof-of-concept may be able to access truly arbitrary memory in-process. This could result in a host process segmentation fault (DoS), an arbitrary data leak from the host process, or with a write, potentially an arbitrary RCE.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 43.0.1, 42.0.2, and 36.0.7 have been released with fixes for this issue.</p>
<h3 id="workaround">Workaround</h3>
<p>There are no workarounds within the Winch compiler backend while using the affected versions. Users of Wasmtime are encouraged either to upgrade to patched versions or, if that is not possible, use the Cranelift compiler backend.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-xx5w-cvp6-jv83">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-xx5w-cvp6-jv83</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34987">https://nvd.nist.gov/vuln/detail/CVE-2026-34987</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0095.html">https://rustsec.org/advisories/RUSTSEC-2026-0095.html</a></li>
<li><a href="https://github.com/advisories/GHSA-xx5w-cvp6-jv83">https://github.com/advisories/GHSA-xx5w-cvp6-jv83</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-xx5w-cvp6-jv83</uri>
        </author>
        <category label="severity" term="CRITICAL"/>
        <published>2026-04-10T15:31:18.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has out-of-bounds write or crash when transcoding component model strings]]></title>
        <id>https://github.com/advisories/GHSA-394w-hwhg-8vgm</id>
        <link href="https://github.com/advisories/GHSA-394w-hwhg-8vgm"/>
        <updated>2026-04-10T14:40:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's implementation of transcoding strings between components contains a bug where the return value of a guest component's <code>realloc</code> is not validated before the host attempts to write through the pointer. This enables a guest to cause the host to write arbitrary transcoded string bytes to an arbitrary location up to 4GiB away from the base of linear memory. These writes on the host could hit unmapped memory or could corrupt host data structures depending on Wasmtime's configuration.</p>
<p>Wasmtime by default reserves 4GiB of virtual memory for a guest's linear memory meaning that this bug will by default on hosts cause the host to hit unmapped memory and abort the process due to an unhandled fault. Wasmtime can be configured, however, to reserve less memory for a guest and to remove all guard pages, so some configurations of Wasmtime may lead to corruption of data outside of a guest's linear memory, such as host data structures or other guests's linear memories. </p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 24.0.7, 36.0.7, 42.0.2, and 43.0.1 have been issued to fix this bug. Users are recommended to update to these patched versions of Wasmtime.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround for this issue and affected hosts/embeddings are recommended to upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35195">https://nvd.nist.gov/vuln/detail/CVE-2026-35195</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0091.html">https://rustsec.org/advisories/RUSTSEC-2026-0091.html</a></li>
<li><a href="https://github.com/advisories/GHSA-394w-hwhg-8vgm">https://github.com/advisories/GHSA-394w-hwhg-8vgm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-394w-hwhg-8vgm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-09T20:24:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has out-of-bounds write or crash when transcoding component model strings]]></title>
        <id>https://github.com/advisories/GHSA-394w-hwhg-8vgm</id>
        <link href="https://github.com/advisories/GHSA-394w-hwhg-8vgm"/>
        <updated>2026-04-10T14:40:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's implementation of transcoding strings between components contains a bug where the return value of a guest component's <code>realloc</code> is not validated before the host attempts to write through the pointer. This enables a guest to cause the host to write arbitrary transcoded string bytes to an arbitrary location up to 4GiB away from the base of linear memory. These writes on the host could hit unmapped memory or could corrupt host data structures depending on Wasmtime's configuration.</p>
<p>Wasmtime by default reserves 4GiB of virtual memory for a guest's linear memory meaning that this bug will by default on hosts cause the host to hit unmapped memory and abort the process due to an unhandled fault. Wasmtime can be configured, however, to reserve less memory for a guest and to remove all guard pages, so some configurations of Wasmtime may lead to corruption of data outside of a guest's linear memory, such as host data structures or other guests's linear memories. </p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 24.0.7, 36.0.7, 42.0.2, and 43.0.1 have been issued to fix this bug. Users are recommended to update to these patched versions of Wasmtime.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround for this issue and affected hosts/embeddings are recommended to upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35195">https://nvd.nist.gov/vuln/detail/CVE-2026-35195</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0091.html">https://rustsec.org/advisories/RUSTSEC-2026-0091.html</a></li>
<li><a href="https://github.com/advisories/GHSA-394w-hwhg-8vgm">https://github.com/advisories/GHSA-394w-hwhg-8vgm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-394w-hwhg-8vgm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-09T20:24:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has out-of-bounds write or crash when transcoding component model strings]]></title>
        <id>https://github.com/advisories/GHSA-394w-hwhg-8vgm</id>
        <link href="https://github.com/advisories/GHSA-394w-hwhg-8vgm"/>
        <updated>2026-04-10T14:40:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's implementation of transcoding strings between components contains a bug where the return value of a guest component's <code>realloc</code> is not validated before the host attempts to write through the pointer. This enables a guest to cause the host to write arbitrary transcoded string bytes to an arbitrary location up to 4GiB away from the base of linear memory. These writes on the host could hit unmapped memory or could corrupt host data structures depending on Wasmtime's configuration.</p>
<p>Wasmtime by default reserves 4GiB of virtual memory for a guest's linear memory meaning that this bug will by default on hosts cause the host to hit unmapped memory and abort the process due to an unhandled fault. Wasmtime can be configured, however, to reserve less memory for a guest and to remove all guard pages, so some configurations of Wasmtime may lead to corruption of data outside of a guest's linear memory, such as host data structures or other guests's linear memories. </p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 24.0.7, 36.0.7, 42.0.2, and 43.0.1 have been issued to fix this bug. Users are recommended to update to these patched versions of Wasmtime.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround for this issue and affected hosts/embeddings are recommended to upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35195">https://nvd.nist.gov/vuln/detail/CVE-2026-35195</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0091.html">https://rustsec.org/advisories/RUSTSEC-2026-0091.html</a></li>
<li><a href="https://github.com/advisories/GHSA-394w-hwhg-8vgm">https://github.com/advisories/GHSA-394w-hwhg-8vgm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-394w-hwhg-8vgm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-09T20:24:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has out-of-bounds write or crash when transcoding component model strings]]></title>
        <id>https://github.com/advisories/GHSA-394w-hwhg-8vgm</id>
        <link href="https://github.com/advisories/GHSA-394w-hwhg-8vgm"/>
        <updated>2026-04-10T14:40:31.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's implementation of transcoding strings between components contains a bug where the return value of a guest component's <code>realloc</code> is not validated before the host attempts to write through the pointer. This enables a guest to cause the host to write arbitrary transcoded string bytes to an arbitrary location up to 4GiB away from the base of linear memory. These writes on the host could hit unmapped memory or could corrupt host data structures depending on Wasmtime's configuration.</p>
<p>Wasmtime by default reserves 4GiB of virtual memory for a guest's linear memory meaning that this bug will by default on hosts cause the host to hit unmapped memory and abort the process due to an unhandled fault. Wasmtime can be configured, however, to reserve less memory for a guest and to remove all guard pages, so some configurations of Wasmtime may lead to corruption of data outside of a guest's linear memory, such as host data structures or other guests's linear memories. </p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 24.0.7, 36.0.7, 42.0.2, and 43.0.1 have been issued to fix this bug. Users are recommended to update to these patched versions of Wasmtime.</p>
<h3 id="workarounds">Workarounds</h3>
<p>There is no known workaround for this issue and affected hosts/embeddings are recommended to upgrade.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-394w-hwhg-8vgm</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-35195">https://nvd.nist.gov/vuln/detail/CVE-2026-35195</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0091.html">https://rustsec.org/advisories/RUSTSEC-2026-0091.html</a></li>
<li><a href="https://github.com/advisories/GHSA-394w-hwhg-8vgm">https://github.com/advisories/GHSA-394w-hwhg-8vgm</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-394w-hwhg-8vgm</uri>
        </author>
        <category label="severity" term="MODERATE"/>
        <published>2026-04-09T20:24:12.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has data leakage between pooling allocator instances]]></title>
        <id>https://github.com/advisories/GHSA-6wgr-89rj-399p</id>
        <link href="https://github.com/advisories/GHSA-6wgr-89rj-399p"/>
        <updated>2026-04-10T14:40:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's implementation of its pooling allocator contains a bug where in certain configurations the contents of linear memory can be leaked from one instance to the next. The implementation of resetting the virtual memory permissions for linear memory used the wrong predicate to determine if resetting was necessary, where the compilation process used a different predicate. This divergence meant that the pooling allocator incorrectly deduced at runtime that resetting virtual memory permissions was not necessary while compile-time determine that virtual memory could be relied upon.</p>
<p>Exposing this bug requires specific configuration values to be used. If any of these configurations are not applicable then this bug does not happen:</p>
<ul>
<li>The pooling allocator must be in use.</li>
<li>The <code>Config::memory_guard_size</code> configuration option must be 0.</li>
<li>The <code>Config::memory_reservation</code> configuration must be less than 4GiB.</li>
<li>The pooling allocator must be configured with <code>max_memory_size</code> the same as the <code>memory_reservation</code> value.</li>
</ul>
<p>If all of these conditions are applicable then when a linear memory is reused the VM permissions of the previous iteration are not reset. This means that the compiled code, which is assuming out-of-bounds loads will segfault, will not actually segfault and can read the previous contents of linear memory if it was previously mapped.</p>
<p>This represents a data leakage vulnerability between guest WebAssembly instances which breaks WebAssembly's semantics and additionally breaks the sandbox that Wasmtime provides. Wasmtime is not vulnerable to this issue with its default settings, nor with the default settings of the pooling allocator, but embeddings are still allowed to configure these values to cause this vulnerability.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 36.0.7, 42.0.2, and 43.0.1 have been issued to fix this bug. Users are recommended to update to these patched versions of Wasmtime.</p>
<h3 id="workarounds">Workarounds</h3>
<p>All four conditions above must be met to be vulnerable to this bug, and users can work around this bug by adjusting any of the above conditions. For example it is strongly recommended that guard pages are configured for linear memories which would make this bug not applicable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-6wgr-89rj-399p">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-6wgr-89rj-399p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34988">https://nvd.nist.gov/vuln/detail/CVE-2026-34988</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0088.html">https://rustsec.org/advisories/RUSTSEC-2026-0088.html</a></li>
<li><a href="https://github.com/advisories/GHSA-6wgr-89rj-399p">https://github.com/advisories/GHSA-6wgr-89rj-399p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6wgr-89rj-399p</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-09T20:23:58.000Z</published>
    </entry>
    <entry>
        <title type="html"><![CDATA[[wasmtime] Wasmtime has data leakage between pooling allocator instances]]></title>
        <id>https://github.com/advisories/GHSA-6wgr-89rj-399p</id>
        <link href="https://github.com/advisories/GHSA-6wgr-89rj-399p"/>
        <updated>2026-04-10T14:40:08.000Z</updated>
        <content type="html"><![CDATA[<h3 id="impact">Impact</h3>
<p>Wasmtime's implementation of its pooling allocator contains a bug where in certain configurations the contents of linear memory can be leaked from one instance to the next. The implementation of resetting the virtual memory permissions for linear memory used the wrong predicate to determine if resetting was necessary, where the compilation process used a different predicate. This divergence meant that the pooling allocator incorrectly deduced at runtime that resetting virtual memory permissions was not necessary while compile-time determine that virtual memory could be relied upon.</p>
<p>Exposing this bug requires specific configuration values to be used. If any of these configurations are not applicable then this bug does not happen:</p>
<ul>
<li>The pooling allocator must be in use.</li>
<li>The <code>Config::memory_guard_size</code> configuration option must be 0.</li>
<li>The <code>Config::memory_reservation</code> configuration must be less than 4GiB.</li>
<li>The pooling allocator must be configured with <code>max_memory_size</code> the same as the <code>memory_reservation</code> value.</li>
</ul>
<p>If all of these conditions are applicable then when a linear memory is reused the VM permissions of the previous iteration are not reset. This means that the compiled code, which is assuming out-of-bounds loads will segfault, will not actually segfault and can read the previous contents of linear memory if it was previously mapped.</p>
<p>This represents a data leakage vulnerability between guest WebAssembly instances which breaks WebAssembly's semantics and additionally breaks the sandbox that Wasmtime provides. Wasmtime is not vulnerable to this issue with its default settings, nor with the default settings of the pooling allocator, but embeddings are still allowed to configure these values to cause this vulnerability.</p>
<h3 id="patches">Patches</h3>
<p>Wasmtime 36.0.7, 42.0.2, and 43.0.1 have been issued to fix this bug. Users are recommended to update to these patched versions of Wasmtime.</p>
<h3 id="workarounds">Workarounds</h3>
<p>All four conditions above must be met to be vulnerable to this bug, and users can work around this bug by adjusting any of the above conditions. For example it is strongly recommended that guard pages are configured for linear memories which would make this bug not applicable.</p>
<h3 id="references">References</h3>
<ul>
<li><a href="https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-6wgr-89rj-399p">https://github.com/bytecodealliance/wasmtime/security/advisories/GHSA-6wgr-89rj-399p</a></li>
<li><a href="https://nvd.nist.gov/vuln/detail/CVE-2026-34988">https://nvd.nist.gov/vuln/detail/CVE-2026-34988</a></li>
<li><a href="https://rustsec.org/advisories/RUSTSEC-2026-0088.html">https://rustsec.org/advisories/RUSTSEC-2026-0088.html</a></li>
<li><a href="https://github.com/advisories/GHSA-6wgr-89rj-399p">https://github.com/advisories/GHSA-6wgr-89rj-399p</a></li>
</ul>
]]></content>
        <author>
            <name>GitHub</name>
            <email>GitHub@noreply.github.com</email>
            <uri>https://github.com/advisories/GHSA-6wgr-89rj-399p</uri>
        </author>
        <category label="severity" term="LOW"/>
        <published>2026-04-09T20:23:58.000Z</published>
    </entry>
</feed>